@AGENTS.md

Claude Code loads this file every session. Shared project prompt = `AGENTS.md`
(imported above). Named Routine streams also read `.claude/rules/claude-code.md`.

Owner-facing integration is on Cursor. This host still runs the five
automation Routines until the owner migrates them.

Do not duplicate `AGENTS.md` here.
