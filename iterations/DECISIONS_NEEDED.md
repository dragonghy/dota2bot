# 待 owner 决定的问题(每周汇总一封邮件,dragonghy@gmail.com)

**当前周次:2026-W35(2026-08-24 起)** — **本周汇总邮件已于 2026-08-30T01:xxZ 发出**
(总监触发,随周日效率台账 `iterations/reports/director/efficiency_202635.md`)。
下一封:2026-W36,周日(2026-09-06),或有时限的问题触及边界时提前。

**W35 那封邮件覆盖的条目**:需要 owner 拍板的 = **第 5 条**(核验不了的那一族缺陷,A/B/C;
**W34 已问过一次,未回复,本周作为增量重问一次,之后不再每周重复**)与
**第 2 条**(IAM `budgets:Describe*` 只读权限,同上);只需点鼠标的 = **第 11 条**
(`ec2:GetSpotPlacementScores`,可选,不加不影响验收)。报告性质、不需要回复的 =
**第 14 条**、本周效率台账 §0/§1b/§3(11 天零 promote + MTD $23.578 → $67.374 +
**月末预测 $80.585 会触发 $80 那一封 AWS 告警,那是预测中的不是出事**)。
**未回复 = 默认动作继续**(第 5 条的默认动作仍是 A:留在 gated,不自行免 gate 修)。

**W34 的历史头(留档)**:W34 汇总邮件 2026-08-23T0x:xxZ 发出,随 `efficiency_202634.md`。

**W34 那封邮件覆盖的条目**:需要 owner 拍板的 = 第 2 条(IAM `budgets:Describe*` 只读权限)
与第 5 条(核验不了的那一族缺陷,A/B/C);第 1 条已自行降级为无时限、默认选项 A 执行;
第 3/4/6/7/8 条是报告性质,不需要回复。**以上条目留在本文件里不删** —— 未回复 = 默认动作继续,
下周只报增量。

**2026-08-26 增量(owner 当面对主会话说的,不必等周日邮件)**:第 9 条(on-demand vs spot)
已拍板 = **Spot 优先,没容量才 on-demand,不是混合选项 C**。W35 邮件不再问这一条;
总监下一 ban 关 GH #158。档案与活规则见本文件第 9 条顶部 + `OWNER_PRIORITIES.md`。

---

## 1. **[无时限,到 8-25 生效]** 8 月最后一周批测排期需要决定

**背景**:批测台 14:07Z 报告(GH #70)第一次把每波实测成本量了出来:
- **on-demand 4 台 / 波 = $1.56**(章程/历次报告写的 $0.6-0.8 是低估约 2 倍);
- 当前 MTD **$11.696**;$45 月度围栏是 launch-time 硬检查,先于 $50 Budget freeze 与
  $90 owner 刹车线生效。
- 按现行「每 6h 一波例行、on-demand 4 台」= $6.4/天 ⇒ **$45 围栏约 8-25/26 触顶**,
  之后到 9-1 前**任何 batch launch 都会被围栏拒**。

**折算**:本月还剩约 20 波例行波次,§R 的 `capmono` 两臂已占其中 2 波。

**要 owner 定的**:8 月最后一周(8-25 到 8-31)是否需要保留批测能力?
- **选项 A**:不抬预算 ⇒ 按现行节奏,8-25/26 之后自动停到 9-1(适合近期无 promote
  候选、想让 σ≈30 的经验零点体现在决策上的情形);
- **选项 B**:抬 8 月预算到 $60/70 ⇒ 8 月余下大约再多 6-14 波(适合有 armed id 需要
  紧急买 (a)/(b) 证据的情形);
- **选项 C**:不抬预算但改节奏 ⇒ 例行波次从 6h 一波拉长到 8-10h 一波,自然把 8 月剩余
  波数拆平到 8-31。

**总监建议**:**选项 A**。理由:目前没有单一 armed id 的判读把「必须 8 月内做完」当
硬时限;`capmono` 隔离波次已经排上,`tpdying` (a) 因 #69 需要 clean 帧回读后才有意义,
其他都是可以顺延的正确性卫生活。

**无时限**:围栏是硬闸,不会有超支意外;若 owner 到 8-25 前没回信,按选项 A 执行。

> **2026-08-21T09:0xZ 总监更新(前提已变,建议不变)**:① 计费滞后追平后 MTD 是 **$18.2767**
> (不是当时的 $11.696;08-19/08-20 两天的批测集中入账,**不是新支出**),月末预测 **$21.50**;
> ② 单价按账单反推是 **$0.35/台**(4 台一波 ≈ $1.40,比当时的 $1.56 略低),
> ③ **最要紧的是节奏变了** —— 自 08-20 起执行「**无申报目的不启动**」,例行 6h 一波已停,
> 08-21 全天零批测。⇒ **$45 围栏的 8-25/26 触顶 ETA 不再成立**,这条**从「有时限」降为无时限**,
> 默认仍按选项 A 执行,owner 不回信不会造成任何损失。

> **2026-08-25T09:xxZ 总监更新(单价换了一个世界,建议仍不变)**:owner 的 cap 10 → 25
> 今天落地(P3 / GH #108)⇒ **同样 12 局/腿的一波,墙钟 ~2.5x,钱也 ~2.5x**:
> 实测基准 **$1.480/波** → 模型外推 **约 $3.05/波**(同模型在旧 cap 上算 0.60 h vs 实测
> 0.550 h,差 9%,这是它敢外推的唯一理由)。**这不是超支,是 owner 买的东西的单价** ——
> 换来的是真实胜负与高地/买活语料。当前有效 MTD **$40.79**($45 围栏余 **$4.21**)⇒
> **界后第一波按新单价已经发不出去**,除非先缩波(缩 `--games`/缩种子)或先裁第 9 条
> (on-demand → spot,$1.48 → $0.54)。**第 9 条的紧迫性因此上升了一档**,这是本次
> cap 变更唯一的连带待决项。**选项 A 仍然是默认**,owner 不回信不会造成损失。

---

## 2. **[无时限]** AWS Budget 的 freeze action 无法从本账户查证 —— 要不要补 `budgets:Describe*` 只读权限?

**事实**(批测台 08:06Z 实测,总监采信):`budgets describe-budgets` 读出
**`BudgetLimit = 100 USD`**,ACTUAL 告警在 50/80/100%(= **$50 / $80 / $100 的实际支出**);
`CLAUDE.md` 原写的「$50/month」是错的,**已按实测更正**。但 `CLAUDE.md` 还称 100% 处有一个
**freeze action** 作为硬后备,而 `budgets:DescribeBudgetActionsForBudget` **本 IAM 用户无权限**
(AccessDenied)⇒ **它是否还存在、有没有绑对策略,从这个账户里查不出来**。

**为什么归 owner**:改 IAM 权限是 owner 账户上的动作,`dota2bot-agent` 自己做不到,也不该能做。

**不决定会怎样(默认动作,已生效)**:文档里把 freeze 标成 **unconfirmed**,**不再当 hard backstop
引用**;实际刹车依赖人为控制线。**不阻塞任何工作**,只是少一层自动兜底。

---

## 3. **[无时限,预告性质]** MTD 一旦过 $50,AWS 会**先于我们的报告**给 owner 发一封告警邮件

四条线的次序(此前散落三处文档,从没并排写过):**批测台自设围栏 $45 → owner 首封 AWS 告警 $50
→ 批测台刹车线 $90 → owner 批准线 / Budget 限额 $100**。次序自洽(desk 先自停,再惊动 owner),
**但那封 $50 的邮件此前没有任何一份章程提到过** —— owner 可能先从 AWS 邮件、而不是从我们的报告里
看到这个数。当前 MTD **$18.28**,月末预测 $21.50,**本月大概率不会触及**。

**不决定会怎样(默认动作)**:总监按现有 $100 批准档继续排波次,**过 $45 围栏前先在报告里预告**;
owner 若希望更早知会,回一句即可,总监把预告线下调。

---

## 4. **[不需要决定,周日邮件的路线图条目]** Turbo 的等级天花板让一部分长期投入买不到东西

**不是一个待决问题**(处置总监已自主做完,见 GH #84),放这里只是为了**不在周日那封邮件里漏掉它** ——
它影响的是路线图,不是某一次判定。

2026-08-21T11:0xZ 用我们自己已经付过钱的 fixture 归档(94 帧 / **67 局**,零成本)量了全英雄池的
等级曲线。**晚期窗(21 局 / 210 个英雄槽 / t=541..690s)**:

- **t10 天赋:65.7% 的槽、21/21 局 ⇒ 活的**
- t15 天赋:3.3% 的槽、7/21 局 ⇒ 近死
- **t20 / t25 天赋:0 个槽、0 局 ⇒ 死**
- 大招 3 点(level 18):0.5% 的槽、1/21 局;全档见过的最高等级 = **19,一次**

**两条对 owner 有意义的推论**:

1. **每个英雄文件的 `tTalentTreeList` 都写满四档,而下半张表在 Turbo 里是死数据。**
   这不是 bug(死数据不会出错),**是一条排期事实**:agent 花在 t15/t20/t25 上的调参、
   以及 patch 升级时去核对那两档的天赋改动,**在我们的优化目标(Turbo)下买不到东西**。
   ⇒ 后续 patch 流程与英雄打磨**只认 t10**,除非 owner 另有打算(例如将来也想打普通模式)。
2. **22 处已发布的 `GetLevel() >= N`(N>=15)门,其中 12 处在通用文件里、对焦点五全部生效**,
   12 处按 N>=20(= 0/210 槽)。这是 GH #37 §4「时间常数要按 Turbo 局长重审」的**孪生轴**
   (那条查时间,这条查等级),同一根因:普通模式的节奏常数被继承进了一棵我们只在 Turbo 里测的树。
   已开 `[strategy] #84` 归口协同组,并**事先钉死三条判读约束**防止它变成又一次「数据到了再解释」。

**owner 唯一可能想插话的地方**:如果这个项目将来也要在**普通模式**下好用,那么第 1 条的
「只认 t10」就是错的排期。**默认动作是按 CLAUDE.md 的既定口径走(Turbo 优先,冲突时 Turbo 赢)**,
不需要回复。

### 4b. 追加(2026-08-23T09:xxZ 总监):**第二个独立语料把它从「近死」抬成「结构性空集」,并且它开始退回工作请求了**

上面那组数字来自 **fixture 归档(94 帧 / 67 局)**。批测台 08:15Z 用**批测语料**
(**306 局**,四种子固定镜像阵容)独立量了一遍,结论比上面更硬:

- **lion 77 局,局终等级 min 8 / med 11 / max 14 —— 0/77 到 15。**
- zuus **37/306** 到 15,crystal_maiden **12/306** ⇒ 三级大招(Thundergod 500 蓝 /
  Freezing Field 600 蓝)那一档的**分母小到不能当密度用**。

**新出现的事**(上面那条写的时候还没发生):**它开始退回具体的工作请求了。**
英雄组的 `hero-4`(Lion t15,Hex −2s)自己把口径收成「only frames at level >= 15 are in domain」
⇒ 在这份语料上是 **0/77**,**不是 UNDERPOWERED,是准入条件从未满足**
(与 GH #115「窄带上的零记 UNDERPOWERED」不冲突:那条讲连续量上的窄带)。

**这引出一个上面那条没问过的问题**:要不要抬 `SOAK_CAP_MIN`(让局更长)把 lv≥15 买回来?
代价两条:**$/局上升**,且**与现存全部语料不可比**(所有历史读数都是 ~11 分钟局)。
**我的保守默认是:不抬。** lv≥15 归 fixture 验,不再为它花波次;
`test_set.md` §AT.6 已按此执行。**owner 若想要 lv≥15 的行为在真实对局里被测到,请说一声** ——
那是唯一会翻转这个默认的输入。

> **⚠️ 2026-08-25T09:xxZ 总监更正 —— 上面那个「保守默认:不抬」当时就已经被 owner 推翻了,
> 而我不知道。** owner 在 **08-22** 就决定把 `SOAK_CAP_MIN` 抬到 25(GH #108),这段 4b 写于
> **08-23T09:xxZ** —— 也就是说,**总监在 owner 已经拍板的第二天,把同一个问题当作「待决」
> 并给出了相反的保守默认**。原因不神秘:#108 开出后**总监连续 24 轮触发零认领**(主会话
> 08-24 立为 P3 时的原话)。⇒ **这条(要不要抬 cap)不再是待决问题,已执行**;
> 「lv≥15 归 fixture 验、不为它花波次」那一半也随之作废 —— 界后的局跑到 25 分钟,
> lv≥15 第一次可能出现在真实对局里,**该由界后第一波的语料回答,不该继续引用界前的 0/77**。
> **教训不是「决定错了」,是「掉棒会让后来的每一份分析建立在过时的前提上」** ——
> 一个没人认领的 owner 决定,不会安静地等着,它会开始**产生与自己相反的结论**。

---

---

## 5. **[无时限]** 有一族缺陷,我们的验证装置**结构上永远核验不了** —— 允许它们免 gate 直接修吗?

**事实(2026-08-21T13:0xZ 总监实测,GH #87)**:自动买活路径 1 把**绝对血量**拿去跟 `0.8`
比,像在比分数(`ancient:GetHealth() < 0.8`)。遗迹有几千点血 ⇒ **只要它还站着这个测试恒假**,
整条分支是死代码;而它是这棵树**唯一**不在 `nFullRespawnTime < 60` 闸门之下的自动买活。
意图不需要猜:`mode_roshan_generic.lua:46` **已经把同一个谓词写对了**。

**卡住的地方不是修不修,是核验不了**:我们的 referee 在 **10 个游戏分钟**打 `dota_dev forcewin`
(`soak_loop.sh` 的 `SOAK_CAP_MIN`)⇒ **买过的 11,048 局没有一局进过高地攻防**;语料侧
**58 个 ancient 快照全是满血**。⇒ 队规要求的「批测核验后才 promote」对这一族**永远不会到来**,
于是**证据确凿的缺陷会永远停在 gated,在 Workshop 交付物里保持坏的** —— 而真实 Turbo 局
**确实**以遗迹被拆结束,那正是我们要交付的场景。**这一族包括:遗迹/高地防守、买活决策、
破路后的行为。**

**三个选项**:

- **A**:允许此类缺陷**免 gate 直接修**(依据 = 逻辑 + 树内已有的正确写法 + 单元测试),
  代价是这类改动没有对局证据;
- **B(总监已按此执行,默认动作)**:**gated 保留 + 一条会自失效的复活断言**
  (`tests/test_ancient_hp_unit.lua` 第 4 节现场数语料,一旦出现受伤的遗迹就报红点名),
  等观测能力跟上再 promote。**代价**:在此之前交付物里这条仍是坏的;
- **C**:**花钱买一波不封顶的局**(临时去掉 10 分钟 `forcewin`)把这一族一次性照亮。
  单波成本量级 $1.40,但局长翻倍以上 ⇒ 需要 owner 点头(且这是新的花钱目的,不属于例行波次)。

**不回复会怎样**:按 **B** 继续,不阻塞任何在跑的工作,也不会有支出。
**owner 只在两种情况下需要插话**:认为交付物的正确性优先于证据纪律(选 A),
或愿意为「照亮这一族」单独买一波(选 C)。

> **2026-08-25T09:xxZ 总监更新 —— 这条基本消解了,而且是被 owner 自己的另一个决定消解的。**
> 卡住这一族的**不是**「修不修」,是 referee 在 10 个游戏分钟 `forcewin` ⇒ 高地/买活
> **结构上永远不会出现在语料里**。今天 cap 落地为 **25**(P3 / GH #108),turbo 局约
> 15-20 分钟 ⇒ **大多数局会自然打完**,这一族**第一次进入可观测范围**。
> ⇒ **选项 C(单独买一波不封顶的局)不必买了** —— owner 的 cap 决定在例行波次里免费做到了
> 同一件事;**选项 B 仍是当前状态,但它的「等观测能力跟上」现在有了到期日**:
> 界后第一波的语料(`queue.json:director-2` + 录像组的验收 2)一到,
> `tests/test_ancient_hp_unit.lua` 第 4 节那条会自失效的断言就会点名它自己。
> **owner 仍然不需要做任何事**;A/C 两个选项可以从桌上拿掉了。

---

## 6. **[不是待决问题,周报要报告]** 我们按后来被推翻的证据发布过一次行为改动 —— 已撤回登记、保留改动、钉了回滚条件

**2026-08-21T19:0xZ 总监裁定,`[bug] #94`,全文 `iterations/streams/test_set.md` §AG。**
**owner 不需要做任何事**;放这里是因为如果他从别处而不是从我这里听说,会觉得该早点告诉他。

- **发生了什么**:`roamstale`(08-19 promote,turbo 默认开,**第一个走完三条件的 id**)的
  条件 (a) 登记的是一个 **DiD +22.9pp**。同一份语料重新分解后,**其中约 63% 住在两条跑着
  同一份出厂代码的基线腿之间** —— 那部分**不可能**是这条改动。功效更高的那个窗口**读反号**。
- **根因不是算错,是估计量选错了**:镜像两臂 bisect 里**臂级混杂按构造不存在**
  (同一棵树、同样种子、同样阵容),DiD 要消的东西根本没有;而候选侧与基线侧**在同一局里
  互为对手**,把基线腿差进来只会加方差。**已立规矩:这类装置禁用 DiD,估计量是 ARMED A−B**,
  并**已机器化**(工具现在会拒绝把两臂汇成一张表)。**历史上另有两条同类登记要免费重算**
  (`lf_rescue`、`capmono`,用已买的录像)。
- **我的决定**:**撤回 (a),但保留改动、不回滚**。依据是 (c) 是**缺陷性**的
  (变量名/唯一写者/唯一读者三者都指「本帧的小兵」,只有缺失的复位不同意),回滚等于
  故意重新发布一个陈旧句柄;(b) 仍成立。**同时钉了可证伪的回滚触发条件**
  (先扫阈值 → 再上 `roamreach` 杠杆 → 仍消不掉才退回 gate 后面)。
- **顺带省下一波钱**:17:00Z 我排了一次「有事前预言的波次」(≈$1.2),它的预言
  (劫持值 −28 gpm)已被免费数据**证伪为 0.0**,**波次撤销不跑**。

## 7. [不是待决问题,周报要报告] 我们有 14 处「已发布的行为」被文档写成「暗的」,最老的错了一个月

**2026-08-22T01:0xZ 总监,`[harness] #103`(自开自修自关),全文见该 issue。**
**owner 不需要做任何事**;放这里是因为它改变了「这一个月的报告该怎么读」。

- **发生了什么**:这个项目的方法建立在「gated 的改动是暗的,promote 之后才是活的」上,
  而**调用点的注释就是这条线的唯一登记处**。核查时发现 **15 处注释在说谎**:
  `tpsafe2` / `pushguard`(**2026-07-23 你自己拍板 ship 的那两个**)+ `nodive` / `punish` /
  `regroup` / `deathzone` / `vsafe` / `tpsafe`,助手函数**早就没有 gate、每局 Turbo 都在跑**,
  但 14 处调用点仍写着 "gated ... inert by default"。另有 1 处反方向:一个叫 `nochaselow` 的
  gate id **在整棵树里从来不存在**,**照那条注释去 arm 它 arm 的是空气**,而那一波会读成
  「测过了,中性」—— 正是 `creeppull`/`pullcamp` 十局全 SILENT 那次事故的形状。
- **为什么值得你知道**:任何读过这些注释的 agent(和你)在过去一个月里,都可能把**已经
  活着的行为当成暗的** —— 要么去 arm 一个早就 live 的 id(那一臂等于没有变量),要么把
  录像里看到的行为归因到别处。`CLAUDE.md` 自己的 promoted 清单写着 3 条,**树上实际 13 条**。
- **已做**:15 处全部改正(**`bots/` 的改动没有一行是可执行代码**),`CLAUDE.md` 按源码重写,
  并加了棘轮 `tests/test_gate_claim_consistency.lua`,**wired 集从源码读、绝不手工维护**
  —— 以后任何注释声称一个不存在的 gate,或者一个已 promote 的行为仍自称 inert,当场点名判红。
  七次变异七红。全套 **1072 tests / 0 failures**,luacheck 0 警告,**本轮零 AWS 支出**。
- **不需要决定的理由**:promote 记录本身没错(你的两次拍板、Class-B 那批都有据可查),
  错的只是登记处;数据结论没有因此需要作废的。

## 8. [不是待决问题,周报要报告] 你为 P2 钉的那一帧作废了 —— **但你的原则被同一份语料坐实了**

**2026-08-22T09:0xZ 总监,依据录像组 08:50Z 报告 §2 + GH #111。owner 不需要做任何事**,
放这里是因为 `OWNER_PRIORITIES.md` P2 正文里那句「铁证帧」现在是错的,而**结论是好消息**。

- **你钉的帧**(`…061050…/20260822_063559_slot1` t=636,Slardar 46% HP)**不是一次回家**。
  逐帧重建落点:他落在离**自家泉水 12,646u** 的地方,落地后还朝中路走了 20 秒 ——
  那是一次**打上路的前压 TP**。「无任何危险」也不成立:决策前 3 秒他掉了 20% 血,
  敌人在 1,046–1,485u,只差 85u 没被检测器的 `SAFE_RADIUS=1400` 拦下。
- **为什么会看错**:检测器 `tp_home_wasteful` **没有一行代码读 TP 的落点**
  (`detect.py:187`,判据只有「有 TP 频道 + 血量 <55% + 最近敌人 ≥1400u」),
  而它的名字和描述都在说「回家」。**它测的其实是「低血且身边无人时按下的任意 TP」。**
  同一批 42 个命中逐个重建落点后分成两群:**22 个真回家**(离泉水中位 ~110u,
  平均 19% 血,**没有一个 ≥40% 血**)和 **18 个落在别处**(中位 ~10,000u,平均 42% 血)——
  两群血量几乎不相交(Fisher 单尾 **p = 7.1e-07**)。你钉的那一帧落在**后一群**里。
- **⭐ 你的原则没有被否掉,反而被坐实了**:同一份语料里有 **22 个货真价实的低血回家 TP**。
  作废的只是**那一帧**作为证据,不是 P2 这件事。录像组已逐帧核验并给出**四个更硬的替换帧**
  (§2.4),P2 的完成定义可以照原样往下走。
- **已做/在做**:检测器缺陷已开 **GH #111**(加落点谓词 + 验收判据);
  `OWNER_PRIORITIES.md` 只有主会话能改,**请主会话把 P2 的铁证帧换成 §2.4 那四个之一**。
  这条**不影响 P2 的责任链**(球仍在协同组:决策侧 id + fixture)。

## 9. **[已拍板 2026-08-26]** 发波是走 on-demand 还是 spot —— 每波 $1.48 vs $0.54,一台丢一臂

> **⭐ owner 2026-08-26 原话**:「On demand和spot之间肯定优先用spot呀,除非没有spot的机器」
>
> 选项 **B + 容量降级**,**不是 C**。例行波和镜像裁定波同一条规则。
> 执行:默认不传 `--on-demand`;`c6i.4xlarge` spot 无容量先试 `c6a.4xlarge`;两种都没有才 `--on-demand` 并写原因。
> 回收:缺臂的那粒种子作废并补跑(spot 优先),**不要整波作废**。W14 不中断;W15 起生效。
> 档案:`OWNER_PRIORITIES.md`「常设运维:发波 Spot 优先」;活规则在批测台章程步骤 5。总监下一 ban 关 GH #158;#219 路 4 一并成立。围栏数字不动。
>
> 下面原文保留作档案,「在 owner 回复之前走 on-demand」那几句**作废**。

**2026-08-24T15:xxZ 批测台,GH #158。总监当时不裁,进 W35 汇总 —— 已由上面的拍板取代。**

**背景**:批测台自己 08-24T15:11Z 那份对账在 CE 上顺手落出来:08-24 全天两波(W4/W5)
的 EC2 费率是 **$0.673/台·h**,而 c6i.4xlarge 的 spot 现价在 us-west-2 的 a/c/d 三区
是 **~$0.240/台·h**;单波(4 台 × 0.55h + IPv4)按需 **$1.480**、spot **~$0.540**。
`describe-spot-instance-requests` 本轮为空,与「本月所有波次全部走了按需」一致 ——
`spot_run.sh` 默认是 spot,是本台**主动传的 `--on-demand`**。

**为什么现在值钱**:MTD $36.206,$45 launch fence 只剩 $8.8 ⇒ 按需 **5.9 波** / spot **16.3 波**;
`budgets` forecast $64.5 ⇒ 本月会越过 owner 的 $50 批准档并触发第一封告警。
队列里排着 W6 + `strategy-7/-8/-9/-10/-11/-12` + `hero-9`,**按需这个选择正在把剩余预算吃掉三分之二**。

**反方(批测台自己写的、总监认同)**:镜像 A/B 的两臂是**同一台实例上先后两个 wave**
(radiant 腿先、dire 腿后)⇒ 一次中段 spot 回收**不是「丢几局」,是丢掉整条后臂** ⇒
该种子的逐种子配对差**结构性作废**;4×1 拓扑下 **一台 = 一个种子**,直接损失该波 1/4 的分层。
与「Radiant 侧偏 +1.5k gold,永远 swap-and-average」是同一条约束:**缺一臂的读数不是噪声,
是偏号**。这不是「省钱 vs 谨慎」的偏好题,是**方法学题**。

**选项**:
- **A**:继续 on-demand。$1.48/波,读数完整。剩 5.9 波,越过 $50 档必然。
- **B**:改回 spot(`spot_run.sh` 默认路径),接受一台被回收 = 该波 1/4 分层作废;
  如果读数残缺按 §I.3 的「多数臂已成」规则处理,或直接把该种子排出配对差。
  剩 16.3 波,月内不越 $50 档。
- **C**:混合 —— 例行(soak/单杠杆)走 spot;镜像 A/B 里承重的裁定波走 on-demand,
  两侧显式登记选择的理由。这条本台没提但看起来符合两边的算术:承重波占少数。

**总监当时不选**(已作废):这是方法学题,不在总监的每 $50 档花钱豁免里,当时排进 W35。
**「在 owner 回复之前走 on-demand」已于 2026-08-26 作废**,见本条顶部拍板。

**已做/未做**:GH #158 已开,批测台在其中记录了完整算术与反方理由;`spot_run.sh` **未动**
(改路径需要 owner 定完选项才有意义);本文件本条与下周邮件同步。

### 9b. 追加(2026-08-25T13:xxZ 总监):**两条更新,而且它们朝相反方向走 —— 请 owner 一起看**

**(一)「已经挡住了」是算术错误,不成立。** 批测台 06:17Z 与总监 10:1xZ **各自独立**写下
「$45 围栏余 $4.21 ⇒ 下一波发不出去」。**`$4.21 < $1.48` 为假**;按界后新单价 ~$3.05 算,
`$40.79 + $3.05 = $43.84 ≤ $45` ⇒ **界后第一波过闸,余 $1.16;第二波才不过**。
⇒ **本条不是「等 owner 回复期间发不了波」,是「还剩恰好一波,发完停」。**
已在 GH #158 与批测台章程原地更正,并授权界后第一波照发(on-demand,发完停到 9-1)。
**这一条降低了本决定的紧迫性。**

**(二)但 §BE(cap 10 → 25)让反方理由变强了,这一条提高了本决定的重量。**
spot 的方法学风险与**暴露窗口**同增:单台机时从 **0.550h** 升到 **~1.13h**(约 2 倍),
而节省比例(~64%)**不变**。⇒ 选项 **B** 的期望损失翻倍,选项 **A** 的绝对代价也翻倍
($0.94/波 → ~$1.95/波)。**两边同时变贵,取舍点因此更靠近 C**:
例行/语料波走 spot,承重的镜像归因波走 on-demand。
**总监的倾向仍是 C,但仍不代 owner 拍板** —— 理由不变:代价是读数的可信度而不是钱,
不在「每 $50 档花钱豁免」里。

**给 owner 的一句话版本**(已作废,当时倾向 C):*「省 64% 还是保读数」这个题没变,但两边的赌注都翻了一倍;
如果你只想回一个字,回 **C** 我们就按「例行省钱、承重花钱」执行。*
**owner 2026-08-26 没有回 C,回的是 B + 容量降级**(没 spot 机器才 on-demand)。

---

## 10. **[不需要决定,W35 周日邮件要报告]** 你的 P1(拉野)选营已经修对了,卡住的是**拖拽距离** —— 本轮决定先不上线

**一句话(可直接进邮件)**:

> 拉野的**选营**已经修对并在真实对局里核验过了(P1 的完成定义 1–3 全部达成)。
> 卡住的不是选营,是**拖拽距离**:小野中位只跟着走 **742 码**,而营地离兵线中位还有 **1,068 码**
> —— **中位那一次尝试在几何上就够不到兵线**。所以现在每 100 局只成功拉野约 **2 次**,
> 而为此把辅助拉出线约 **22 次**。**因此本轮不上线**,下一步是把选营的距离阈值从
> 「观测到的最大拖拽 1200」收到典型值(约 992 / 742),**这一步不花钱**。

**为什么这条不需要你决定**:P1 的完成定义第 4 条自己写了两条路 ——
「三条件成立 → promote」**或**「给出明确的不 promote 理由并由 owner 知悉」。
本轮走的是第二条,理由在上面那段里,**这就是「由 owner 知悉」**。
如果你不同意(比如你更希望先上线再迭代),回一句话就能推翻。

**三条件的逐条读数**(全文 `iterations/streams/test_set.md` §BD):

| 条件 | 判定 | 依据 |
|---|---|---|
| (a) 真的执行且行为正确 | **PASS** | 录像组 01:35Z,两个物理层各自独立复现 |
| (b) 对胜负无明显负面 | **PASS** | 该杠杆每局只发生 0.223 次尝试 / 0.0255 次成功,**足迹比噪声底低 1–2 个数量级**,明显负面结构上产不出来 |
| (c) 逻辑依据 | **时刻齐、几何不齐** | 教科书的 :00/:30 兵线波 + :12/:42 仇恨标记**源码里都有**;不齐的是 742 < 1,068 那条几何 |

**顺带省了一笔钱(不需要你批,记录在案)**:上一轮批测台把「发一次 $1.48 的隔离波来买 (b)」
摆上了桌,**本轮拒绝发**。理由是可证的而不是省钱偏好:一个每局 0.223 次的杠杆,
隔离波读到零 ⇒ (b) 过;读到大额负数 ⇒ 它产不出那个数、只能判噪声 ⇒ (b) 仍过。
**没有一种结果会改变裁定**,所以那份读数买不到任何决定。MTD 因此不变(**$37.828**,三线全未触及)。

---

## 11. **[只需要你点一下鼠标,不是决策题]** 给 `dota2bot-agent` 加一个只读权限:`ec2:GetSpotPlacementScores`(GH #252 请求 2)

**一句话(可直接进邮件)**:

> 今天(08-27)一波四台批测机**全被 AWS 放进同一个可用区**,一次容量事件**在同一秒**把四台一起收走,
> 那一波 **$0.48 买到零可用数据**。分散已经修好了(脚本现在每台点一个不同的可用区,**不花钱、不用你做任何事**)。
> 剩下一个**可选**的小改进:给批测账号加一个**只读、免费**的权限
> `ec2:GetSpotPlacementScores`,发波之前能先看一眼哪个可用区有容量。
> **它必须由你操作**(那个账号按设计不能改自己的权限)。**不加也不影响任何事**,只是少一层预警。

**为什么它进这个文件而不是我自己做**:`dota2bot-agent` 被刻意做成**不能碰 IAM**,
这条改动只能由 owner 在 IAM 控制台上执行 ⇒ 它是**动作**不是**决定**。零成本(该 API 免费)、只读、
不扩大花钱面。

**为什么它排在低位**:分数是**建议不是承诺**(拿到高分仍可能被回收);
而 #252 的损失来自**四台挤在一个 AZ**,那一半已经在 15:5xZ 消掉了(`--az` + 随机起点 + 失败回退)。
**不加它,#252 的验收照样能过。**

## 12. **[FYI,不需要你决定 —— 除非你想否决]** 我给你 08-26 那句「除非没有 spot 的机器」加了一个可观测的读法

**一句话(可直接进邮件)**:

> 批测机用的是 AWS 的**便宜但可被收回**的机器(spot)。近七波里有**四波**出现同一件事:
> 机器**顺利开机**、跑到一半被 AWS 收走,而我们的测试**必须跑满约 40 分钟**才会产出可用数据
> —— 于是那一波的钱**全额**换成不可用的半份数据。你 08-26 定的规矩是「优先用 spot,
> **除非没有 spot 的机器**」,而这种失败**从来不报「没有机器」**(开机是成功的),
> 所以那个「除非」在机器上**永远不会被触发**。
> 我把它翻译成:**留不住到 40 分钟的机器,就算我们没拿到。**
> 具体做法:**只有在一波几乎颗粒无收、且确实是被收走造成的**时候,**下一波**(仅一波)
> 改用贵一些但不会被收回的机器,之后自动改回 spot。
> **钱几乎不变**:七波实测每粒有效数据 **$0.51**,改用贵机器是 **$0.53**(贵 4%);
> 省下来的是**时间** —— 那四波各占掉一个 6 小时的发波档期,合计约 **24 小时**只换回 ≤1 粒数据。
> 预算上限(围栏 / 刹车 / $100 批准线)**一律不动**。
> **不需要你做任何事;如果你不同意这个读法,回一句话我就改回去。**

**为什么它只是 FYI 而不是决策题**:它没有跨任何 $ 档,没有改围栏 / 刹车 / 批准线,
也没有推翻 owner 的裁定 —— 它是把一条**在机器上无法被触发**的例外条款翻译成可触发的形式。
按章程「绝大多数决定自己做,做了记录即可」,已执行;**放进本文件是因为它动的是 owner 亲口定的那句话**,
他有权一句话否决。全文档案 `test_set.md` §BY,落地物 `tools/batch_test/soak/reclaim_blind.py`(GH #271)。

---

## [路线图读数,不需要决定] 首个绝对标尺:当前 main 胜过原始 OHA(2026-09-05)

**P4.1 标尺波 Y1 的读数**(主会话从 stdout 恢复,`run_20260905_151656_main_vs_upstream-baseline`):
`stable(当前 main,gate 全关) vs upstream(74727e4a)`,镜像 A/B 80 局(fwd/rev 各 40,换边)。

- **main 胜率 = 44/80 = 55.0%**;80 局全部分出胜负,0 局无 signout。
- 方向为正(一个月工作相对原始版本有可测优势),幅度温和,95%CI≈44–66%,单波统计上未达显著——按 owner 验证哲学"粗粒度、方向为正即有意义"成立。
- 口径:引擎原始胜负,非经济封顶口径;Y1 撞上 con_logfile 未落地的 harness bug(80 局 json 曾全 0 字节),已修(run_batch.sh 回退 .log.stdout,commit 25d237b2),下一波读数会更干净。
- **意义**:这是项目开工以来第一个"到底变强没有"的直接测量。此前所有批测测的都是"测试版 vs 稳定版"的边际差,从不测绝对进步。建议今后每 promote 一批 / 每周跑一次标尺波,把这条曲线连起来。

*(以后按周日/触及边界补充其他条目;单条决定若在总监自主授权范围内,直接做+记录,
不进本文件。)*

## 13. **[FYI,不需要你决定 —— 除非你想收回一项权限]** 我这轮自主改了**出厂默认行为**(不是 gated 的测试集),理由和它出的岔子都在这

**背景**:录像组在 W22 录像里发现一处**没有 gate、没有 id、每一局都在发生**的缺陷 ——
一个英雄(OD)的技能点会**永久停止投放**:5/6 局停在 6 点,同局其他九个英雄 14–20 点。
它不在测试集里,所以「先 gated 再批测」那条路对它不适用:**上 gate 就等于在波次量出结论之前,
每一局真实对局继续坏着**;而粗粒度胜负读数本来也量不到一个英雄的加点。

**我做的判断**:直接改出厂行为,不上 gate。依据是**域**而不是紧急 ——
改动整体位于「这张表已经坏掉」这个条件之内,表健康时行为逐字节相同,
**它可能改变的局恰好等于今天已经坏掉的那些局**。
推理写进了 `state.json` 的 `why_not_gated` 字段(不是散文),**为的是让后来者可以否掉它**。

**⚠️ 然后它出了两个岔子,都不是"改坏了",但都值得你知道:**

1. **我的根因归因是错的,已撤回。** 我复核了 issue 推理的每一步,**却没复核那条链的输入**
   (那个英雄的大招到底在第几个技能槽)—— 而那是**离线就能读的**。
   英雄组当晚普查了 126 个英雄的 KV,直接推翻了它。
   **落地的修复本身仍然成立**(那是一个语言级的真缺陷),
   **但"这修好了 OD 停摆"这句话已经撤回** —— 停摆原因目前**未知**,归 GH #290。
2. **我关 issue 关早了,代价是一次真实波次。** 我在**还没 push** 的时候就关了 issue、
   写下五处档案引用、还让另一个 issue 依赖它们;等我跑完全量测试再 push 已是四小时后,
   期间 **W23 那一波(花了钱的)跑的仍是没修的树**,而录像组查树时看到「已修已关但树上什么都没有」。
   **这跟测试慢没关系** —— 关 issue、写引用、让下游依赖,这三件事都必须排在 `git push` 之后。
   录像组提的防复发办法(自检里加一条腿,把 issue 评论里的档案引用拿到当前 trunk 上解析一次)我已接受,
   排在下一轮第一优先。

**要你决定的只有一件(而且默认可以不管)**:
**「总监可以自主修改出厂默认行为」这项权限,你要不要收回或加条件?**
默认按现状继续(遇到无 gate 可上的出厂缺陷时自主修,理由写进 `state.json` 可被否)。
你若想改成「一律先问你」或「只有零行为差的修复才自主」,说一声即可,我按新规则走。

**顺带一条不需要你做任何事的健康提醒**:
主干上目前有 **7 个 Lua 测试是红的**(不是这轮弄红的,时间不明)。
自检每轮跑的是一个 **17 个文件的快子集**,**看不见**这两个文件,
于是「主干红」在每一轮自检里都读作绿。已列入下一轮处理。

---

## 14. **[不是待决问题,是排期题 —— 周报要报告]** 自检**正确地**报了主干红,然后三个流照常落地、一个流从红树上发了波

**2026-08-29T07:07Z 总监立。** 报告全文
`iterations/reports/director/20260829T0707Z.md`。

今天早上 04:20Z 和 04:51Z 两次落地各自把主干弄红了一条腿(一条 Lua、一条 python,
两条**都是工具/测试侧,`bots/` 的 Lua 一直是干净的**)。两个根因本轮都修完了,
主干回绿 —— 那部分不需要你做任何事。

**要报告的不是那两条红,是它们之后发生的事:**

| UTC | 事件 |
|---|---|
| 04:20Z | strategy 落地 → Lua census 红 |
| 04:51Z | hero 落地 → python 套件红 |
| 04:51Z+ | hero 又落地 2 个 commit |
| 06:20Z | **batch-desk 发 W24 —— 从这棵红树上** |
| 06:48Z | replay-check 落地一轮 |
| 07:0xZ | 总监开工自检读到 exit 3,**当天第一个读到的人** |

铁律 10 的开工自检**是每个流开工第一件事**,当天早上**至少被跑过三次**,
每次都应该打出 `TRUNK RED`。它**没有失灵** —— 它打印了,然后没有下一步。

W24 这一波**不需要杀掉重发**(两条红都在测试/工具侧,shipped 行为零 diff,
本轮 `luacheck bots game` 0 警告),但那不是因为有谁核过,是运气。

**排期题(不需要你拍板,除非你想指定方向):**
`routine_selfcheck.sh` 的 trunk-red 腿现在只**打印**。它应该
(甲)像铁律 6 的 pre-push 钩子那样**有牙齿**,还是
(乙)只让「本轮读到 exit 3」成为报告里的**必填栏**,在报告节奏里留下痕迹?
甲的代价真实:它会挡住「我这一轮正是来修这条红的」那种会话。
乙更弱但不会自锁。总监倾向**乙 + 一条例外白名单的甲**,下一轮排期。

**顺带**:上一条(#13 末尾)提到的「主干上 7 个 Lua 测试是红的、快子集看不见」——
本轮自检的快子集已经是 **21 个 tagged 文件、0 failures**,
且本轮修的 `test_level_gate_census.lua` **在**这个子集里。那条提醒的具体清单
本轮没有重新核对(工作单元用在修红上了),留给下一轮总监。

### 14 追加(2026-08-29T10:xxZ 总监,同一条排期题多了一个**量出来的**约束)

上面那句「留给下一轮总监」的清单,本轮核完了。**7 条红全部落在两个文件里**,
`tests/test_itemdesire_world_assertion.lua`(6)与 `tests/test_salvepool_missing_floor.lua`(1),
**全部已修、全部回绿**(19/0 与 25/0,改前 18/1 与 19/6)。
修法不是重新钉数,是**把再钉一次这件事变得不必要** —— 详见
`iterations/reports/director/20260829T10xxZ.md`。

**但它给「甲还是乙」加了一个新事实,而这个事实是量出来的:**

| 文件 | 本轮实测耗时 | 能不能进自检快腿 |
|---|---|---|
| `test_salvepool_missing_floor.lua` | **< 1s** | **能,本轮已打 `[ratchet]` 标签** |
| `test_itemdesire_world_assertion.lua` | **383s(6分23秒)** | **不能** |

`routine_selfcheck.sh` 的快 Lua 腿按标签发现文件,而快腿的全部价值就是它**快到没人会跳过**
(今天约 20s)。把 383s 那个文件拉进来会让开工自检变成 ~7 分钟,
**那正是脚本自己的表头写着「不要这么干」的那件事**(它上一次为 `[census]` 标签量过:4.2s → 7m08s)。

⇒ **所以覆盖缺口不是标签问题,是排期问题,并且它有下界了**:
本项目存在一类**结构上进不了任何开工级门**的测试(GH #124 的 sweep 家族),
`test_itemdesire_world_assertion.lua` 是其中之一,而它这次**红了整整一天没人看见**。
(乙)「本轮读到 exit 3 必填栏」**管不到它** —— 自检压根不跑它,不会 exit 3。
(甲)也管不到 —— pre-push 钩子跑的是静态那半,不是这个 6 分钟的文件。

**总监倾向不变(乙 + 白名单的甲),但补一条第三腿**:给这类慢文件一个
**按天而不是按轮**的跑位(例如批测台每天第一轮顺手跑一次全量、或一个独立的日更 Routine),
因为它们的失效模式恰恰是「**没有任何一轮的开工会碰到它**」。
这一条本轮**没有落地**,是排期题,列在这里等你的方向;不指定的话下一轮总监按倾向自己排。

---

## 2026-09-01T22:xxZ(总监)`hero_pool.txt` 要不要为「非焦点英雄的杠杆」扩池?

**背景**:GH #402 修完载体门之后,两条 armed id 的判读从「凭空满足」变成了正确的
`UNDRAFTABLE` —— `tormself` 的载体是 `ringmaster`、`immguard` 的载体是 `brewmaster`,
**两个都不在 47 人 `hero_pool.txt` 里**。在当前池子下,**任何种子、任何窗口、任何次数的重抽
都买不到这两条的条件 (a)**。本轮已按 §DH.3 把两条**出集**(退回,非 reject,gated 代码留在树上)。

**这就是那个分叉**:`carrier_terms.py` 自己的建议行写着两条路 ——
「要么 un-arm 这个 id,要么把这个英雄加进 `hero_pool.txt`,**永远不要再换一批种子**」。
本轮取了第一条。第二条**总监没有自行拍板**,因为它动的不是测试集,是**测量基底**:

| | 加进池子 | 不加(本轮所取) |
|---|---|---|
| 买到 | 这两条(以及未来任何非焦点英雄杠杆)的域 | — |
| 付掉 | **W1–W36 全部读数的可比性**(抽签分布变了) | 这两条在池子不变期间**永远验不了** |
| 连带 | 池子每加一人,焦点五每人被抽中的概率下降 | 非焦点英雄的修复会持续堆积成「写好了但验不了」 |

**总监倾向:不加,并且把「杠杆落在池外英雄上」当成入集前就该问的问题** ——
这正是本轮 §DH.4 给 `pending_rulings.py` 装的那道域价的抽签侧对应物(域价现在读的是**帧语料**;
`hero_pool.txt` 成员资格是**另一条**,下轮可以一并接上,让「池外英雄」在**提议**时就举手,
而不是在**发波门**上举手 —— #385/#393 两条走完了入集、armed、发了一波才被看见)。

**需要你(或 Cursor 整合者)定的是方向,不是实现**:焦点五之外的英雄修复,
我们是**当作可以做但暂时验不了**(倾向),还是**干脆不接**(那 §DG/§DD/§DE 这类提议应当在提议阶段就退回),
还是**周期性地临时扩池跑一批"池外专波"**(第三条路,成本与可比性都要另算)?
不指定的话,下一轮总监按倾向执行:不动池子,并把池外判据前移到提议阶段。

---

## 追加(2026-09-03T01:xxZ 总监,`ckpush` 入集时第二次被咬):上一条的**第二种形状** —— 英雄**在池子里**,但它是 `filler`,而卡的不再是「验不了」,是「验成了要不要 promote」

**这不是重复上一条,是把它切成两问。** 上一条的现场(`tormself`/`immguard`)是
**载体不在 `hero_pool.txt` 里 ⇒ 域按构造买不到**,所以在「测量」那一层就死了,
总监按倾向出集即可,不需要你拍板也不会造成既成事实。

**本轮(`ckpush`,GH #426,queue `strategy-38`,裁定 test_set.md §DT)是另一种**:
`chaos_knight` **在** 47 人池里(`hero_pool.txt:22`),角色 **`filler`,位置 1/3**,
载体率 **32.7%**(七个载体项里第三高)⇒ **域完全买得到**,入集的成本总监也量过了,
是**零**(§DT.3:6 项与 7 项的 `BEST min-per-term` 逐位相同)。⇒ **入集这一步没有争议,已 ADMITTED。**

**有争议的是它的下一步。** `ckpush` 若拿到干净的条件 (a)(b)(c),按现行规则就该 promote ——
而 promote 意味着**建一个 `stable-vN` 锚点、改动全 Turbo 对局的默认行为**,
**为一个 filler 英雄的推进时机**。项目声明里焦点是五个(Axe / Zeus / WK / Lion / CM),
候选池八个,**chaos_knight 两者都不在**。

**三条路,总监不自行拍板的理由是它改的是「什么算值得 promote」而不是某一条 id 的裁定**:

| | (甲) 一视同仁 | (乙) filler 只入集不 promote | (丙) filler 提议阶段就退回 |
|---|---|---|---|
| 买到 | 缺陷修在哪儿都算修好;规则最简单 | 锚点只为焦点/候选池而建,版本史干净 | 提议方不浪费工作单元 |
| 付掉 | `stable-vN` 序列里混进与目标无关的行 | 一批**验证通过却永远停在 armed** 的 id(测试集只涨不落) | 真缺陷留在树上不修(`ckpush`/`roshdist` 这种形状每周都在出) |

**总监倾向 (乙) 的一个变体**:filler 英雄的 id **可以 promote,但不单独建锚点** ——
搭在下一次焦点英雄 promote 的同一个 `stable-vN` 里落地。这样既不让测试集只涨不落,
又不让版本史为一个 filler 单开一行。

**⚠️ 时限**:`ckpush` 走到 promote 关口之前必须有答案,**不要到那一天再临时决定**
(那正是「既成事实」的形状)。不指定的话,下一轮总监按上面那个变体执行并记录。

---

## 2026-09-04T12:5xZ(总监)**不是待决问题,是我给自己写的止损条款到期了**:GH #473 甲(载体门看不见自己的第二种形状)**连续第四轮被顶掉**

**你什么都不用回。** 这一条进这份文件,是因为**我上一轮自己写下了这个条款**
(`director.md` 当前状态 09-04T07:00Z ⑧②:「再顺延一次就写进 `DECISIONS_NEEDED`」),
而本轮它**第四次**被顶掉了。写进来是让顺延这件事**有一个你看得见的地方**,不是请你裁。

**它是什么**:载体门(`carrier_terms.py` 家族)判断一条 id 的「域」时,从**文件路径**读载体
(`bots/BotLib/hero_<英雄>.lua` ⇒ 域是那个英雄)。`rotscope` 改的是 **generic 文件**、
域却**是一个具体英雄** ⇒ 载体门看不见它,**连续八波恒零而没有任何门举手**。
与 09-04T07:00Z 修掉的那条是**同一个函数 `subjects_of()` 里的两种走样**:
那条是**字段选错**,这条是**模式选错**(`HERO_FILE_IN_PROSE`)。**两条腿的沉默都长得像好消息。**

**被顶掉的四轮,每一轮的顶替物都不是可以往后放的**(按时间):

| 轮 | 顶替它的 | 为什么不能等 |
|---|---|---|
| 09-03 晚 | (排第一,没做完) | — |
| 09-04 07:00Z | 两条 trunk 红 | 红树上会发波(GH #290 那次的实测代价) |
| 09-04 10:00Z | 三行欠裁 + 两条 trunk 红 | 欠裁挡住 W46 发波 |
| 09-04 12:5xZ(本轮) | `campbind` 入集点亮的三行共臂登记 | **是我上一轮自己的裁定造成的红**,且只有总监能碰 `test_set.md` 入集节;W46 正在跑,`creepthink`/`pullcad`/`pullthink` 的逐 id (a) 读数悬在上面 |

**这不是拖延,是一台每两小时被真活填满的排程**——但**连续四轮**说明它靠「下一轮排第一」这个机制
**排不进来**,而它每多沉默一轮,就多一波数据可能**恒零而无人举手**。

**我下一轮的做法(不需要你批准,除非你想换)**:把它当**本工作单元的全部**,不再排在别的活后面 ——
即使那一轮开工自检又报了红,先把红**登记并交棒**(像本轮对 GH #479 做的那样),**不自己修**。
理由是:trunk 红有别的门会在下一轮再举一次手,而载体门这条**按定义没有任何东西会举手**。

**你唯一可能想否决的地方**:上面这句等于说「有些 trunk 红我会放着交棒而不当轮修」。
如果你更想要「红了就当轮修完,基建往后排」,回一句我就换回来。

**【2026-09-04T16:4xZ 结清】** 上面这条止损条款**已兑现,不再待决**。GH #473 **甲**本轮落地:
载体门现在从**源码**读英雄门(`if botName == 'npc_dota_hero_x' then`),`rotscope` 从
`generic` 变 `hero pudge`,62-id 活串的载体项 **7 → 8**、`unresolved` 前后都是 **0**。
全文 `test_set.md §EG`,报告 `iterations/reports/director/20260904T161949Z.md`。
**做法也照写下的执行了**:本轮开工自检又报了一条 trunk 红
(`test_detector_source_constants.py`,批测台已立 GH #490),**登记交棒,没有当轮修**。
GH #473 **乙**(按需机时常数)仍欠,排下一轮第一。**你依然什么都不用回。**

---

## 【2026-09-05,总监】九月燃烧率会在月底顶穿你批的 $100 —— 要不要现在降节奏

**这条我不裁,因为 $90 刹车线和 $100 批准线是你的地界,不是我的。**
批测台 09-05T00:11Z §二·乙 立案(GH #505),我复核过它的两条算术,都成立。

**两条互相独立的路径同向**:

| 来源 | 九月预测 | 对 $100 |
|---|---|---|
| **AWS 自己的** `ForecastedSpend`(免费读,不是我们算的) | **$119.286** | **超** |
| 批测台独算:$17.773 ÷ 3.945 天 = $4.51/天 × 30 | **$135** | **超** |

按 $4.51/天外推撞线时刻:**$50 ≈ 9-12**、**$80 ≈ 9-19**、**$90(刹车)≈ 9-21**
⇒ **九月最后 ~10 天的例行波会被刹车整段吃掉**。

**根因是结构性的,不是某一轮浪费**:闸 (i) 的 6h 窗口允许 ≤4 波/天,
而当前抢占率下的单波价(spot ~$0.75–1.02,抢占后按需补跑 ~$0.55/台)
与「每月 $100」不相容。**没有人做错事,是节奏与预算不相容。**

**三个选项,我建议 (B)**:

- **(A) 撞线再说** —— 什么都不改,9-21 前后刹车自己生效,九月最后 10 天停摆。
  代价:停摆落在**月末**,而月末通常是一轮迭代收口的时候。
- **(B) ⭐ 现在把窗口从 6h 放宽到 8h**(≤3 波/天)—— 约 $3.4/天 ⇒ 月度 ~$102…
  仍然勉强。**放宽到 9h(≤2.7 波/天)⇒ ~$90/月**,把刹车留成真正的意外阀。
  代价:每天少 1–1.3 波,但**平摊到整月**,而不是月末整段消失。
- **(C) 抬批准线** —— 你把月度批准从 $100 抬到 ~$140。
  代价:真金白银;而且只有在你认为当前波次密度**值这个钱**时才对。

**⚠️ 我不会自己选 (B)**:改闸 (i) 的窗口是我的地界(它是我写的),
但**为了迁就预算而改它,实际上是在替你做 (C) 的反面决定**,那是你的地界。
⇒ **你回一个字母就行;在你回之前我按 (A) 执行(什么都不改),因为它是不需要授权的那个。**

**顺带,一条今天已经修好的、方向相反的**:九月的围栏常数
(GH #504)本来多给了 $30 的额度 —— 已在 09-05 修成每轮现读的程序(`test_set.md §EM`)。
**修好它会让上面的撞线时刻略微提前而不是推迟**,所以这两条不互相抵消。
