# 协同组工作单元 —— `tormself`:一个以「别的单位」为域的谓词被喂了 `self`

**一句话**:`hero_ringmaster.lua:915` 的闸有两条臂,**Roshan 那条问目标、Tormentor 那条问自己** ——
`J.IsTormentor` 是纯粹的单位身份谓词(`GetUnitName()` 里找 `'miniboss'`),
我们自己的英雄叫 `npc_dota_hero_ringmaster` ⇒ **那条臂构造上恒假,外层析取的整个 Tormentor 那一半够不到函数体**。
落地 gated `tormself`(turbo-only,严格超集,出厂零变化)。

## 0. 本轮怎么选到它的(自驱)

- `[strategy]` **未认领 issue 仍为零**:open 的 `[strategy]` 全是本组自己开的(#356 #360 #362 #370 #373 #377 #378 #381 …)。
- **owner 优先项**:P1 第 1 棒、P2 均已交出;P3 责任在总监。
- ⇒ 取 backlog 最上面一条 `0FIELD` **明说没做的那两项**之一:
  「`aba_hero_sub_units.lua` / `primal_split.lua` 的 4 处连续命令**仍只普查未审计**」。
- **审计它反而先证明了一件反直觉的事**:`aba_hero_sub_units.lua` **被任何文件 require —— 零个**。
  它是被 `minion_lib/` 取代的遗留文件,**它里面的缺陷不值一个 diff**。
  **但证明它死的那次普查,正是找到唯一存活拷贝的那次普查** —— `hero_ringmaster.lua:915`。

## 1. 缺陷

```lua
if (J.IsDoingRoshan(bot) or J.IsDoingTormentor(bot))     -- 外层:按 bot 的 mode 二路分
then
    if (J.IsRoshan(botTarget) or J.IsTormentor(bot))     -- 内层:本该按 target 同样二路判别
```

`J.IsTormentor`(`jmz_func.lua:10638`)= `string.find(nTarget:GetUnitName(), 'miniboss') ~= nil`。
喂 `bot` ⇒ **恒假**。于是 bot 在 `BOT_MODE_SIDE_SHOP` 打 Tormentor 时,
`J.IsRoshan(botTarget)` 也假(目标是 miniboss 不是 roshan)⇒ **两臂同假,整半边死**。
**Roshan 那一半是好的。**

## 2. ⭐ 主判据(可复用)

**一个以「别的单位」为域的谓词被喂了 `self`,是语言抓不到、运行期也不报的类型错误** ——
`bot` 与 `botTarget` 是**同一种鸭子类型**,而这个谓词是**全函数**:对 self 不抛错,**只是恒假**。
失效方向朝**关**,而且它的静默是本档案里**最强的一种**:
**没有任何一帧、任何一局、任何一份录像上,这个错答案与一个正确的 `false` 有区别** ——
因为对 `self` 而言正确答案**就是** false。**只有调用点知道这个问题是问别人的。**

**判别特征可数、不需要帧**:枚举身份谓词的调用点,看每个被喂了什么。
实测(code-only、剔除死文件):**249 个存活 `J.IsTormentor` 调用点,227 个(90.8%)喂 `botTarget`,恰好 1 个喂 `bot`**。

**⭐ 边界(本轮要求登记的那条)**:「self-fed」**本身不是判据**。`J.IsMeepoClone(bot)` 是**合法的** self-fed。
判据是 **self-fed ∧ self 不可满足**。类棘轮 [source S5] 就是按这条写的 —— 管 `IsRoshan`/`IsTormentor`,**故意不管 `IsMeepoClone`**。

**与同族六条划界**:#348 拼错 · #368 词法作用域 · #370 未汇报副作用 · #373 闩记错后置条件 ·
#378 节流器作用域 · #381 手工字段复制引擎事实。
**本条里每个标识符都存在、拼对、在作用域内、不持状态、不过期,函数也是对的那个 —— 错的完全是「问的是哪个对象」。**

## 3. 它是缺陷不是取舍;为什么没人发现

- **同一行上就能判**:把 Tormentor 臂读成 mode 测试**更糟** —— 真正有意义的那个
  `J.IsDoingTormentor(bot)` **已经是外层的第二条臂**,那样读作者就是在真判别旁写了个恒真重复检查。
  两种读法都错;**只有 target 读法有用,而那是另外 227 个调用点的写法,包括本文件自己另外四处(436/622/846/965)**。
- **为什么没人发现**:这是**复制粘贴的一脉** —— 另有 `aba_hero_sub_units.lua:370`
  和 `familiars.lua:358`(被注释掉的)。但那个文件**零引用者**,
  **这一脉三分之二是死代码** ⇒ grep 这句话的人看到的命中绝大多数在跑不起来的文件里,
  **唯一存活的那份因此也像是同一堆死东西**。由 [source S4] 钉住(将来谁把它接回来,这条就红)。

## 4. 改动(严格超集,出厂零变化)

新增文件内局部 `IsTormentorSubject()`,按 `J.IsModeTurbo() and J.IsSoakCandidate('tormself')` 分叉。
**门关逐字返回 `J.IsTormentor(bot)`** ⇒ 出厂字节级不变;**门开返回 `J.IsTormentor(botTarget)`**。
**关的那条恒假 ⇒ 开的那条是严格超集,arming 不可能让它反而不做今天会做的事。**
915 行只换成 `IsTormentorSubject()`;**Roshan 臂与发货函数体其余一字未动**。
访问器**放在 `local botTarget`(156 行)之下**是有意的(GH #368 形状;变异 M11 实证)。

`bots/` 只改一个文件、**全在门后**;`game/` **零 diff**。

## 5. 本地验证

`tests/test_tormself_identity_domain.lua`(`[ratchet]`,**10/0**),真实帧
`f_20260828_004757_venomancer_785`(t=785.4,主体存活 1219/1219,完整十人)。

| 用例 | 读数 |
|---|---|
| **[frame F0]** 真实 `J.IsTormentor` 跑遍全语料每个英雄句柄 | **107 帧 / 993 句柄 / 41 个不同英雄名 → 为真 0 次**。**不是罕见条件,是空条件** |
| **⭐ [frame FC] 阳性对照**:同一个 `if` 的**兄弟臂**,不开门,Roshan mode + Roshan 在面前 | **HIGH**(证明这道闸真的能开) |
| **[frame F1]** 出厂 + Tormentor 在 300u + 每个世界条件都断言为真 | **NONE** |
| **[frame F2]** 同帧同注入目标,armed | **HIGH** |
| **[frame F3]** armed 但目标是普通英雄 | **NONE**(把问题问对,不是一律放开) |

**变异 11 条,11 条全 CAUGHT**(每次从文件副本还原,前后基线各验一次绿):
M1 调用点改回 self-fed · M2 shut 臂被偷偷修好(出厂不再是 no-op)· M3 去掉整道门 ·
M4 去掉 turbo-only · M5 门 id 拼错 · M6 armed 臂也问 bot · M7 别处新长出一个 self-fed 身份调用 ·
M8 身份谓词放宽到能匹配英雄 · M9 `IsRoshan` 同样放宽 · M10 访问器只在定义处改名 ·
M11 把 `local botTarget` 下移(GH #368 形状)。

## 6. ⚠️ 一次方法自伤,被本文件自己的阳性对照抓出

**[frame F1] 一开始读出 NONE 并「通过」了。** 真实原因是:主体是 venomancer,fixture 里没有
`ringmaster_funhouse_mirror`,mock 返回**未学习**的技能桩,`J.CanCastAbility` 在
`X.ConsiderFunhouseMirror` 的**第一行**就 bail —— **距离受测那条臂还有四个条件**。
**一道关着的闸,在有东西证明它能开之前,什么都没证明。**
已补世界槽 S-D 并新增 [frame FC]。**登记理由是失效方向**:提前 bail 的闸长得和正确判别后关上的闸一样,
**而它 bail 的方向恰好是这个测试想要的答案**。与 #377 的 M8、#381 的普查拼写同族 —— **读数对,理由不对**。

**另一条断言自审**:[source S2] 起初只断 `accessor > declaration`(读者那半),
变异 **M11** 保持该式为真却仍打断修复(`SkillsComplement` 于是赋值给**全局** `botTarget`)。
**M11 是帧测试抓到的,S2 没抓到。** S2 现已加断 `writer > declaration`,重跑 M11 确认 S2 会红。
**一条守得比自己名字少的断言。**

## 7. 门(铁律 6 / 铁律 10)

- `bash tools/agent/luacheck_gate.sh` **裸读**(不经管道):**exit 0 / 0 警告,未用 `RULE6_BYPASS`**,变异台前后**各跑一次**。
- 定点与邻居:`tormself` **10/0** · `smoke_load` **3/0** · `gate_claim_consistency` **10/0** ·
  `illureal` **12/0** · `illumove` **9/0**。
- **全量 Lua 套件本轮没跑完(GH #124),不声称。**
- **⚠️ 开工自检**:第一条命令我写成了 `| tail`,**被拒绝横幅当场拆穿**(同一站点连续第九轮)。
  改重定向后**第一次被我自己的 `timeout 400` 杀掉:`EXIT=124` —— 那不是通过,是没跑成**。
  后台重跑得 **43 checks / 0 failures / 9 UNCERTIFIED**,**9 条全是 GH #358 那个 120s 预算超时的
  trunk-health 腿(`5a0`/`5a`/`5a2`/`5b`/`5c`/`5d`/`5e`/`5f`/`5g`),没有一条是本轮的**;
  另有一条 python 腿报 UNCERTIFIABLE。**按铁律 10:读到「未核验」的那一侧,这轮没人看过。**

## 8. 明说没做的

1. `primal_split.lua` 的连续命令位点(`0FIELD` 留下的另一半)**仍只普查未审计**。
2. `aba_hero_sub_units.lua` 自己那份拷贝**故意不修** —— 修死代码只会制造一个量不到的 diff;
   改用 [source S4] 钉住它的「死」。
3. **⚠️ 频率未知,而且比平时更重**:**Ringmaster 不是焦点英雄** ⇒ **五个焦点英雄身上一个读数都买不到**;
   分支还要 `BOT_MODE_SIDE_SHOP` ∧ 大镜子不在 CD ∧ Tormentor 在 900u 内 ∧ 正在攻击。
   **本工作单元没有给这个合取定价 —— 那就是本修复价值的上界。**

## 9. 交棒

- **总监**:(甲)裁 `tormself` 入集(**RIDESHARE、搭车、不能当独臂**);
  (乙)§DD.2 的主判据(**self-fed ∧ self 不可满足**,以及为什么 `IsMeepoClone` 不在其中)进不进 §CR;
  ~~(丙)`strategy-29`/`strategy-30` 仍 pending 未裁~~ —— **rebase 时消掉了**:总监在本轮落地的
  **同一小时**(10:2xZ)把 `illumove`(§CZ)与 `illureal`(§DB)**两条同轮入集**
  (§DC,成员串 50 → 52),`strategy-30` 已转 `running`。**我的入集提议因此顺延为 §DD。**

  ⚠️ **总监在 §DC.3 加了一条提议里没有的限定,收割时必读**:那两条**同帧不正交** ——
  `illureal` armed 会**缩小** `illumove` 的域,而两份验收各自只 arm 自己那一个 id,
  **所以没有任何测试能看见这件事**,交集上的帧**不能分摊归因**。
- **录像组**:只缺一种读数 —— **「Ringmaster 处于 `BOT_MODE_SIDE_SHOP` 且正在攻击 Tormentor」的窗口有多少、多长**;
  `acceptance` 已按 §CJ **预登记 `METHOD-FAILED`** ⇒ **没有这种窗口(甚至根本没有 Ringmaster 出场)判 `DOMAIN-EMPTY` 退回总监**,
  **不得**自行套用「无效应 ⇒ 不 promote」。
- **批测台**:`strategy-31`,**搭车、零 AWS 增量、零 EC2**。

## 10. 产出清单

| 文件 | 变化 |
|---|---|
| `bots/BotLib/hero_ringmaster.lua` | +37/−1,**全在 `tormself` 门后**;新增 `IsTormentorSubject()`,915 行改调它 |
| `tests/test_tormself_identity_domain.lua` | 新增,`[ratchet]` 10/0 |
| `iterations/state.json` | 新键 `tormself_20260901` |
| `iterations/streams/test_set.md` | 新增 **§DD**(入集提议;§DC 在同一小时被总监的入集裁定占用) |
| `iterations/queue.json` | 新增 `strategy-31`(`bundle` 已填) |
| `iterations/streams/strategy.md` | backlog `0TORM` + 当前状态 |
| GitHub | issue **#384** |

**零 AWS、S3 零访问、零 EC2。**

## 11. Token 用量

```
TOKENS total_in=11,409,676 out=86,512 turns=70
```

(铁律 8:这是**到统计时刻为止**的数字;统计之后的收尾回合 —— commit / push / 开 issue —— 不计入。)
