# 协同组 2026-08-23T15:36Z — `itemtrip` 的域是在「行程」上量的,而 bid 是在「帧」上付的

**认领依据**:总监 14:58Z 把 `itemtrip` **退回协同组**(§AT.1 预登记第一档,X = gpm −26.44),
并且**交出来的不是「退回」两个字,是一道算术题**:域 0.038 次/局 vs 代价 −26 ~ −93 gpm,
**差约三个数量级 ⇒ 两个测量至少有一个是错的,先修哪一个决定下一步**。
Owner 优先项 P1/P2 本组交付均已落地(球权在主会话/别组,已连续多轮请核对);
`[strategy]` open issue 里 #120 是**刚被两方同时点名**的一条(录像组 15:30Z 追评撤回了
自己 13:01Z 的 (a)=WORKING 读法)。**本轮只动这一根杠杆。**

**开工自检**:`bash tools/agent/routine_selfcheck.sh` → **worst exit 0**
(UNLANDED 无 / cadence clean / trunk python **17 passed**)。`HEAD == origin/main == 029a60c`;
容器无 `lua5.1`/`luacheck`,已装。

---

## 1. 答案:错的是域那一侧,而且这个数是免费的

`tests/_itemtrip_sweep.lua`(104 个真实帧 fixture,**966 个存活英雄帧**,37 秒,零 AWS):

| | |
|---|---|
| `J.IsWastefulItemTrip` 成立 | **320 / 966 = 33.1%** |
| 首条拒绝子句 | hp 184 / ring 390 / **dist 71** / dmg 1 |

**申请、上机、定价时用的域是 0.038 次/局** —— 那是 #120 数的 **TRIP**(回城行程)。
**`GetDesire` 的 bid 是逐帧付的**,而这个谓词选的是**帧**:「健康 + 1600 内无敌人 +
离家够远」正是**一个普通打钱帧的描述**,一个 bot 可以连续几百帧落在域内却一次行程都不发起。
**两个数从来不可比**,而把它们并排读出来,正是让一个 **33% 帧域**的杠杆看起来像
**0.038 次/局**的杠杆的那一步。

**这个读数不需要新语料、不需要 .dem、不需要 AWS** —— 它一直在树上,只是没人跑。
(总监点的两条 .dem 侧检查仍然有价值,但**不必先做**:域这一侧已经足够解释代价。)

## 2. 一个杠杆,而且是一次推导订正,不是一条新子句

泉水距离下限 **5000 → 10000**(`J.IsWastefulItemTrip`,`bots/FunLib/jmz_func.lua:5038`)。

原推导(它自己的注释):#120 中位往返 40.1s,~300 移速 ⇒「~12,000u 的走动,
**即 ~6,000u 单程**」⇒ 保守取 5000。
**折半是错的**:#120 的人口是 **643 次回城 TP** —— **去程是 TP,只有回程在走**。
所以走掉的 ~12,000u **就是单程离家距离**,不是它的两倍。沿用第一版对它自己那个数用过的
保守折扣(6,000 → 5,000,≈0.83):**12,000 × 0.83 = 10,000**。

| | 5000 | 10000 |
|---|---|---|
| 域(966 帧) | 320(**33.1%**) | **135(14.0%)** |
| 泉水子句挡下的帧 | **71** | 257 |

订正前那条泉水子句**几乎没在承担它注释里声称的那份窄化**(71/966)。

**上限来自证据帧,不是来自品味**:#120 的承重帧 lina `20260822_123136` t=434.6
离自家泉水 **11,239.9u** ⇒ 10000 留 **1,240u 余量**;**任何超过 ~11,000 的下限都会让
杠杆在自己的证据帧上失效**,那正是本组付过一次学费的 Luna 追击病灶。用例把两侧都断言了。

**shipped 影响 = 零**:gate 是 load-time 的,`itemtrip` 已出集、**永不 arm**,
未 arm 时 `bots/mode_item_generic.lua` 连 `GetDesire` 都不定义。

## 3. ⚠ 这次订正的**代价**,是断言出来的不是提一句的

抬下限之后,**全语料没有一帧够得到归因伤害子句**:
「hp ≥ 0.55 + 1600 环空 + 离家 ≥ 10000 + 近期被英雄打过」= **0 / 966**。后果三条,逐条落地:

- **普查**:`WHY dmg` 从 1 变 **0** ⇒ 断言改成 `== 0`(**等式,故意的**:哪天有 fixture
  让它变回 1,那是这条子句**第一枚真实帧**,该红一次让人把合成退掉)。
- **N1 过定**:skeleton_king 离家 **9,811u**,同时落在新下限之内 ⇒ 他现在**两个理由**都答
  「不」、**不再隔离任何子句**。降级为回归用例并在头注写明;真正还在隔离 1600 环的是
  那对 boundary(11,872 / 11,959,**两侧都在新下限之外**)。
- **N4/N5 在真实几何上重切,只合成一个量**:旧的 N4(wd 6,094u)会**为了距离理由而变绿**
  ——**假绿**;旧的 N5(drow 9,462u,断言谓词成立)会**直接变红**。
  新的两例用真实帧、真实英雄位置、**真实的 3000 环占用**(N4:`es_defend_1v3`/axe,
  环内恰好 1 个敌人;N5:`lion_meatgrinder`/lion,3000 内无人),**只把 fixture 不记录的
  伤害簿记合成掉**,并在每一例里声明这一点。
  **退掉这两个合成需要一枚 fixture**(健康 + 1600 空 + 离家 ≥10000 + 近期被英雄打过,
  一枚伤害源在 3000 内、一枚在 3000 外)—— **登记为请求,不留成隐含前提**。

**没有为了让某个用例好过而挪常数** —— 反过来才对:常数由推导和证据帧定,用例跟着重切。

## 4. 普查带的那条断言从「认证现状」改成「棘轮」

旧普查写的是 `wasteful >= 250 and <= live * 0.45` —— 它**把 33.1% 认证成了正常**。
现在是 `wasteful <= live * 0.16`(**只许降不许回涨**)+ 一条防塌底 `>= 60`
(「够不到任何东西的杠杆不是更窄的杠杆,是死的」)。**重新 arm 这个 id 前先读这个数。**

## 5. 检测器侧:旧读数作废,而且原因不止一条

`tools/batch_test/behavioral/itemtrip_contract.py` 的 `FDIST_MIN` 随源码 5000 → 10000
(`tests/test_detector_source_constants.py` 保证它逐字等于 Lua 字面量)⇒
**任何 2026-08-23 之前跑出来的 IN/OUT 分桶都是按旧下限分的,不能复用。**
顺手修掉它 selfcheck 里的一处**会随常数移动而假红的字面量**:那个「干净的远处对照」行
写死 `fdist=9000`,新下限一到就不再是「远」⇒ 改成由 `FDIST_MIN` 推导;两个边界例同理。
(这是本轮第二次遇到同一形状:**一个复制了它所测常数的检查,在常数移动后测的是旧世界**。)

**另一条独立作废理由来自录像组自己**(15:30Z,GH #120):in/out 两桶差在这份语料上
**是噪声级**(同码同种子两波之间,对照桶从 0.95 荡到 0.37)⇒ **(a) 不许再用两桶比**。

## 6. 门

- `luacheck bots game --formatter plain` **exit 0 / 0 警告**。
- `tests/test_itemtrip_wasteful_trip.lua` **15 例全绿**;**7 变异 7 抓**:
  下限回 5000(3 红)/ 抬到 12000(**9 红**,含承重帧)/ 压到 9000(3 红,棘轮咬住)/
  归因否决被摘掉(N4 红)/ 归因半径 3000→100(N4 红)/ 1600 环→200(2 红)/
  用例侧把合成伤害归给错的英雄(N4 红)。
- 邻域复跑:`test_itemtrip_supply_gap` 10/10、`test_gamemode_world_assertion` 27/27、
  `test_gate_claim_consistency` 7/7、`test_smoke_load` 2/2、`test_corpus_scale` 8/8、
  `test_fieldcreep_veto` 16/16、`test_pingstamp_world_assertion` 18/18;
  python `test_detector_source_constants.py` 全绿;`routine_selfcheck` python **17 passed**。
- **全量 `run_tests.lua` 没跑**(GH #124:例行容器 900s 跑不完)。
- 本轮**没有新增 fixture** ⇒ GH #106/#107 的跨文件破坏面**结构上不存在**。

## 7. 交棒(铁律 9:同一工作单元内把下一棒交出去)

1. **总监** —— `test_set.md` 顶部 16:0xZ **重新入集提议**(退回条件「带着新的域回来」已满足)。
   **不插队**:当前名额 0、W3/W4/W5 已排定,登记的理由是接力棒要有个看得见的位置。
2. **批测台** —— `queue.json:strategy-6`,**不申请专波、搭车、零 AWS 增量**。
   申报目的是**条件 (b) 的重测**:域小 2.4 倍之后 gpm 赤字是否随之收窄。
3. **录像组** —— 两条,都写进了 acceptance:(i) **旧 `itemtrip_contract.py` 读数全部作废**
   (FDIST_MIN 变了),要重跑;(ii) 按你们自己 15:30Z 的自证,**(a) 不许再用 in/out 两桶比**,
   要**局内配对**量(同局同英雄的回城取货次数差)。
4. **录像组 / harness** —— **fixture 请求**:健康 + 1600 空 + 离家 ≥10000 + 近期被英雄打过,
   一枚伤害源在 3000 内、一枚在 3000 外 ⇒ 退掉 N4/N5 的合成伤害簿记。
5. **预登记的反向读法(最重要的一条)** —— 若下一波 armed 侧 gpm 赤字**基本不变**,
   那就**不是域的问题**:读成 `mode_item_generic.lua` 头注预登记的第二种可能 ——
   **`GetDesire` 返回 nil 被引擎读成 0 ⇒ 整局 item mode 关掉**。
   **那时不要再窄化域,要拆 nil 那条腿。**

## 8. Token 用量

```
TOKENS total_in=10,651,152 out=65,794 turns=76
```
(到统计时刻为止;统计后的收尾回合不计入。)
