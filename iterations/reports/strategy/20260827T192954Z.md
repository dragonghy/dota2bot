# 协同组 2026-08-27T19:29Z — `abilanc` 留下的那个分母买到了它的第一个杠杆:`[1]` 族的等级子句(gated `abil1st`)

## 0. 一句话

**GH #259 认领并落地**:`abilanc` 注释里写明「故意不覆盖读 `list[1]` 的那些站点,好让下一根杠杆有分母」——
W17-R 给了分母第一批帧(3 次 level<12 的远古施法,**3/3** 来自这个 population),本轮把其中
**两条被点名的通路**接到一条与 `abilanc` **同源阈值、独立 id** 的等级子句上:gated **`abil1st`**
(`J.GetFirstUnit`,turbo-only)。**zuus 那条在真实帧上端到端跑通**:出厂 HIGH 打远古 → armed 整个营不打;
mixed 营里改打普通小兵。

## 1. 开工自检(铁律 10)

- `UNLANDED` **0**;cadence **1 finding**(replay-check 6.1h,不是本组);未裁 queue 请求 **0**(open 36);
  稳定版锚点 stable-v1/v2 **各 3 项 ok**;trunk python **44 passed 0 failed 0 uncertifiable**;
  trunk 快 Lua 检测器 **12 文件 0 失败**(收尾时 **13 文件**,本轮新文件带 `[ratchet]`);
  `selfcheck worst exit` **3**(全部来自 cadence)。
- 开工 `HEAD == c107316`,与 `origin/main` 同步。容器有 `lua5.1`(自检装的)、无 `luacheck`
  (gate 自己装,apt 包名 `lua-check`)。**AWS $0**,零 EC2 调用。

## 2. 认领依据(铁律 9 → 铁律 5)

过 `OWNER_PRIORITIES.md`:常设运维球在**批测台**;P1 第 1 棒 08-22 已结、其余棒在总监/批测台/录像组;
P2 决策侧三个 id 已落地、球不在本组;P3 归总监 ⇒ **本组无未完成格**。
⇒ 取最新 open `[strategy]`:**GH #259**(18:49Z 开,本组前缀,带**两局逐帧帧证据 + 两处源码行号**,
明确写着「改法留给协同组定」)。上一轮的「下一格」(`0POLL` 等总监裁 mock 范围)**不推进**,
因为那条明写着「不扩 mock 则本条线收官,回 issue 重新认领」。

## 3. 缺陷(issue 的两帧,逐行核过源码)

| 通路 | 帧 | 源码 | 为什么 `abilanc` 够不着 |
|---|---|---|---|
| zuus Arc Lightning | `88d937/20260827_151652_slot10` t=620.4,L11,两发大蓝耗(1.00→0.81),营没拿、624.4 掉头 | `hero_zuus.lua:725-729` | 读 `nNeutralCreeps[1]`,不经过 `J.GetMostHpUnit`;**第 727 行把「只有一只、但它是远古」显式写成开火理由,且这条分支一个等级子句都没有** |
| centaur Double Edge | `88d937/20260827_151632_slot8` t=759.4,L11,自伤 11% 血,营没拿、走人 | `hero_centaur.lua:325-329` | 同样读 `[1]`;它唯一像门的那条 `J.GetHP(list[1]) > 0.33` **是反的** —— 远古恰恰永远够厚 |

## 4. 产出

**`J.GetFirstUnit( unitList )`**(`jmz_func.lua`,`J.MostHpUnitOf` 之后),gated **`abil1st`**:
- 门 = `J.IsModeTurbo() and J.IsSoakCandidate( 'abil1st' )`,**单合取、全树只解析一次**;
  阈值从 `J.Site.ANCIENT_MIN_LEVEL` 读,**不写第二个字面量 12**。
- **不armed ⇒ 逐字是 `unitList[1]`**(nil 表也照旧 nil);armed 且低于档 ⇒ **同一次 sweep 里第一个非远古**,
  或 nil(nil **不是新的结果类**:空 sweep 今天就返回 nil)。
- **`ipairs` 不是 `pairs`**:「第一个」本来就是顺序语义,无序遍历的「第一个」不是任何人能复现的决策。
- **为什么是第二个 id 不是 `abilanc` 的合取**:`pullcad` 陷阱(AGENTS.md)——
  `IsSoakCandidate('X') and IsSoakCandidate('abilanc')` 会在 `abilanc` promote 当天被冻成恒 FALSE,
  而 `check_armed_wiring.py` 仍会报 WIRED。而且两者**不是同一个缺陷**:
  「最高血」**按构造**就是远古,`[1]` 是引擎顺序、**只是有时候**是远古 —— 两个 id 在任何一波上都不会测到同一件事。

调用点各 **一处**,两处都保留出厂分支形状:
- zuus:新增 `hNeutralTarget ~= nil` 作**第一个合取项**。这不是装饰 —— 出厂第二个析取项
  (「只有一只且是远古」)armed 后仍为真,不加这条就会 **返回 HIGH desire + nil target**,
  那是本次编辑**唯一可能引入的新失效模式**(变异 M11 专门验它)。
- centaur:`hNeutralTarget ~= nil` **吞掉**了它替换的两条守卫(`~= nil` 与 `#>=1`),
  并且**血量子句改读同一个句柄** —— 否则会「按一只判、往另一只打」。

`bots/FunLib/jmz_func.lua` 里 `abilanc` 那段「故意不覆盖 `[1]`」的注释**同步更正**:分母被取走两个成员,
剩下的数**不在注释里复述**,由新测试钉住(GH #193 那条:修自己的注释,别去松别人的棘轮)。

## 5. 本地验证(真实帧;哪半是真的、哪半是声明的,都写在文件头)

新增 `tests/test_abil1st_first_unit_reader.lua` **16 例全绿**(0.87s,带 `[ratchet]` ⇒ 自动进自检快腿,
12 → **13** 个标签文件)。

- **真的**:两个 subject 与它们的**等级**(等级是这条修复读的**唯一** bot 操作数);
  zuus 那一帧**确实站在远古营里** —— dump 带 `modifier_ancient_rock_golem_weakening`。
  `f_212636_tide_ancient.lua` 一帧上同时有 tide **L10** / zuus **L11** / luna **L14**,阶梯不用编。
- **声明的**:营地本身(**[W1] 断言全语料不带任何小兵**,GH #100 §3);zuus 的蓝量与 active mode
  (**分支自己的前提,不是修复读的量**;该帧真值 268/932,而 #259 的帧是 mana 1.00)。
- **端到端(zuus)**:`X.ConsiderQ()` 在真实帧上驱动,顺序**按 #259 的要求**——
  **先**断「计划存在」(出厂 desire=HIGH、target = 远古,**这就是 620.4 那个决策**),
  **再**断 armed 后不再是远古。反过来会把「本来就没计划」读成「改动生效」(GH #250 同款坑)。
  读数:出厂 `0.75/granite` → armed `0/nil` → armed+mixed `0.75/mud_golem` → armed 别的 id `0.75/granite`。
- **centaur 端到端没做,理由写成了可执行断言**(`[limit centaur]`):它的 farm 分支在 `J.IsAttacking`
  后面,而那条读**三个 dump 不携带的动画量**(`GetAnimActivity` / `GetAttackPoint` / `GetAnimCycle`);
  为了进这个分支去声明「攻击动画」= 在已经没有帧的营地之上再叠一层没有帧的声明。
  centaur 那半因此是**源码形状断言 + 真实 L5 帧上的 helper 读数**(L5 与被点名的 L11 **在阈值同一侧**,
  语料里没有高于档的 centaur —— 这句也钉成断言)。

### ⚠ 变异测试:3 批 13 个,**首轮 12 抓 1 活**,补上判别用例后 **13/13**;两端控制项干净

活下来的那条(**M9**)是:把「第一个非远古」换成「**血最多的**非远古」——**全绿**。
原因不是断言写少了,是**语料没有判别力**:`mixed_camp()` 里只有**一只**非远古,
「第一个」与「最血多」在它上面**是同一个答案**,而我那条自称测顺序的断言拿的正是这张表。
**修法是加一张能把两条规则分开的表**(`order_camp()`:远古 1400 / mud **300**(在前)/ dark troll **800**),
armed 必须答 **300 那只**;同一张表喂 `J.GetMostHpUnit` 答 **800 那只** —— 这条对照顺带把
「两个 id 测的不是同一件事」变成了读数而不是说辞。补上后 M9 立刻转红。
**判据**:*一条自称测「按 X 选」的断言,必须跑在一张让 X 与所有近邻规则给出**不同**答案的输入上;
否则它测的是「答案对不对」,不是「按什么选的」。*

抓到的 13 条:M1 门里换错 id / M2 helper 恒返回 `[1]` / M3 `ipairs`→`pairs` / M4 删 zuus nil 守卫 /
M5 阈值换成字面量 12 / M6 centaur 血量子句读回裸 `[1]` / M7 去掉 turbo 合取 / M8 去掉等级子句 /
M9(补表后) / M10 与 `abilanc` 合取 / M11 **守卫文字保留但被 `or true` 架空**(行为侧抓,证明
`[site zuus]` 那三条不是摆设) / M13 远古判断取反 / **M12 控制项**(只改注释,必须绿——绿)。
还原走 scratchpad 快照 `cp`(`0CAMP` 教训)。

## 6. 门(铁律 6)

- `bash tools/agent/luacheck_gate.sh` → **`luacheck bots game: 0 warnings`,EXIT=0**
  (冷启自己装的 `lua-check`)。`RULE6_BYPASS` **未使用**。
- Lua:`abil1st` **16/0**、`abil*`(含 `abilanc` 全族)**76/0**、`gate_claim_consistency` **10/0**、
  `smoke_load` **3/0**、`zuus` 全族 **110/0**、`pullcamp` **51/0**、`corpus_existence_claims` **4/0**、
  `corpus_scale` **8/0**、`no_undefined_jmz_refs` **3/0**、`level_gate_census` **15/0**。
  自检快腿 **13 文件 0 失败**;python 套件 **44 passed 0 failed 0 uncertifiable**。
- **全量 Lua 套件未跑**(~100min,GH #124)。

## 7. 边界 / 没有声称的东西

1. **`abil1st` 是 gated 的 ⇒ 不是 live**。真实对局里它今天什么都不做。
2. **条件 (a) 一帧没买到**:本地能给的最强读数是「真实帧的等级 + 声明的营地」;
   「勾到远古营时真的换目标了」只能在波次的录像里看见。
3. **端到端只覆盖 zuus 一条通路**;centaur 那条是源码形状 + helper 读数(理由见 §5,已钉成断言)。
4. 分母剩 **8 个**站点仍把裸 `[1]` 当技能目标/落点交出去(`[ratchet][limit]` 钉住);
   本轮**不扩面** —— 那 8 个里一个都没有帧证据。

## 8. 交棒(铁律 9 连带规则:下一棒显式交出去)

- **总监**:裁 `abil1st` 入不入 armed 集。**本组建议:入**——
  与已在集的 `abilanc` **同源、同阈值、可独立读**,而且它是这一族里**第一个有真实浪费帧**的 id
  (#259 的两帧 + PA 的正对照都在同一份语料里);两个 id **必须分开 arm**,合并会让读数不可归因。
  另请确认:`abilanc` 那段注释的更正(分母少了两个成员)是否要同步进 test_set.md 的登记。
- **录像组**:入集发波后,(a) 证据的判读点写死为——低于 12 级的英雄在**远古营**上的技能开火数,
  以及开火后 5s 内是否离开;**正对照**(同级别打普通营并打完)在同一份语料里已有(#259)。
- **批测台**:**本轮无请求,零 AWS**。`abil1st` 只在总监裁定入集后随例行波次搭车。
- **英雄组**:无。zuus 是焦点英雄,但本轮改的是**通用 reader**,没动他的技能顺序/出装/天赋。

## 9. 下一格

等总监的入集裁定。若入集 → 等 (a) 证据回来再判 (b)/(c)。
**不为剩下 8 个 `[1]` 站点开格**,直到它们里有一个拿到帧证据 —— 这正是 `abilanc` 留分母的用法,
本轮只是第一次按它用了一回。

## 10. Token 用量

```
TOKENS total_in=10,885,275 out=59,170 turns=72
```
(统计时刻之前;此后只剩 state.json / 章程 / commit 的收尾回合。)
