# 协同组 2026-09-06T07:30Z —— 认领 GH #558:`towerfear` 执行对了、方向错了,于是在它旁边落一个收窄版

**一句话**:录像组核验 `towerfear` **WORKING** 但被释放的 episode 更常摸进塔的 700u 攻击圈
(+12.46pp / +35.21pp,两层同号),而这个块**自己的注释**早就写了 700–898u 环带里塔够不着、
校准兜底子句结构上无法开火 —— 于是沿这条几何线落新 gated id **`towerring`**:
**环带保留折半,攻击圈内恢复出厂时钟**。18/18 真实帧测试全绿,15 发变异台 **STAND GREEN**,
`luacheck` **0 警告**,出厂行为逐字节不变(普查输出前后 byte-identical)。
**armed 串一字未动、`queue.json` 一字未动**(P4.2 入集冻结);零 AWS。

---

## 0. 开工自检(铁律 10)与本轮的诚实边界

`bash tools/agent/routine_selfcheck.sh`(**用文件重定向读退出码,不走管道** —— 脚本自己会拒绝管道,
这是证据纪律第 3 条,本轮第一条命令就撞上了它并按提示改正)。

首跑 **被我自己的 600s 超时截断(EXIT=124)**,读到的部分:
稳定版锚 `stable-v1` / `stable-v2` 两条 **EXISTS/PINNED/SHIPPED 全 ok**;
promote-atom 约束 **OK**(3 个 atom,`FROZEN none`);owed executions **7 行**(2 行 DONE 待总监退役);
`expired waits` 空。**python trunk 腿 UNCERTIFIABLE**(GH #548:84 文件 / **120.1s**,骑在 120s 预算刀口上;
另外首跑时容器**还没有 `lua5.1`**,两条 `rc.sh` 检查因此也 UNCERTIFIABLE)。
⚠️ **UNCERTIFIABLE 不是通过** —— 这一轮 trunk 的那一侧,首跑没人看过。
第二次以 1500s 重跑(装好 `lua5.1` 之后)见 §6。

## 1. 认领:GH #558(`[strategy]`,未认领,带帧证据)

本组工作流第 1 步是**扫 `[strategy]` open issue,优先带帧证据的**。本轮扫到 **#558**
(录像组 07:08Z 开,**当轮最新**,248 个 R_lever episode / 65 个对局逐 episode 帧核,
带建议钉帧和建议验收方式,**无人认领**)。章程已记录的 #521 / #511 / #503 都是本组前几轮
认领并交回的,#558 是新的一条 ⇒ 直接取它,不走 backlog 自选。

同时它**恰好**满足 OWNER_PRIORITIES **P4.4(i)**:工作单元主体是一个 `bots/` 行为改动。

## 2. 立案句:两半读数指向同一个环的不同部分

| 读数 | ab | ba | balanced | 判 |
|---|---|---|---|---|
| R_lever occ%(环内占比) | +1.81 | +3.09 | **+2.45** | both-strata |
| R_lever dwell(环内停留 s) | +2.13 | +0.69 | **+1.41** | both-strata |
| R_lever bnc%(3s 内被甩出) | −22.39 | −25.37 | **−23.88** | both-strata |
| `C_low` 同窗只差等级 | — | — | — | 全部 SIGN-SPLIT ⇒ 噪声 |
| **episode 级 `<700u` 占比** | **+12.46pp** | **+35.21pp** | — | **both-strata** |

⇒ 条件 (a) 的**前半买到了**,**后半符号是反的**。录像组结论:**不建议按现状 promote**。

而 `bots/mode_retreat_generic.lua` 这个块**自己的注释**(GH #178 那次重写)已经写明:
塔的攻击距离 **700u**,块的塔环 **898u**,所以 **700–898u 环带里 `GetAttackTarget() == bot`
永远为假** —— 校准兜底子句在那里不是「罕见」,是**结构上无法开火**,而且**不需要**,
因为塔够不着(实测 72.7% 的释放帧在那个环带)。进了 700u 它是**唯一**兜底,
而它要求**炮弹已经在飞**。

**几何线在 700u,而且它是这个块自己的论证里已经有的那个数。**

## 3. 改动(一次一个小杠杆)

`bots/mode_retreat_generic.lua` X.ShouldRun「前期谨慎冲塔」块:

```lua
local nFearClock = 5 * 60
local nFearClockShipped = nFearClock                       -- (1) 捕获一次,防两份拷贝漂移
if (J.IsSoakCandidate('towerfear') or J.IsSoakCandidate('towerring'))
    and J.IsModeTurbo()
then
    nFearClock = nFearClock / 2                            -- (2) 门变成析取
end
if J.IsSoakCandidate('towerring') and J.IsModeTurbo()      -- (3) 折半之后 restore
    and nEnemyTowers[1] ~= nil
    and GetUnitToUnitDistance(bot, nEnemyTowers[1]) < 700
then
    nFearClock = nFearClockShipped
end
```

级别腿、校准子句、塔环、出厂常数**一个字未动**。

**方向按构造**:released(`towerring`) ⊊ released(`towerfear`) ⊊ 出厂放行集的补
⇒ 这个 `return 2` **只可能比 armed `towerfear` 多开、绝不可能比出厂多开**。
未 armed 时两处 `IsSoakCandidate` 都是 false ⇒ `nFearClock` 停在 `5 * 60`。

### ⭐ 为什么是并列的新 id,不是改 `towerfear`(主判据,可复用)

把几何条件塞进 `towerfear` 的门里,会让一个**正在被测量的 id 在 arm 串 md5 不变的情况下换掉语义** ——
W50 已经用那个 id 买到了 (a) 的前半,那半读数会静默地变成关于另一个杠杆的。
⇒ 落**并列**版,`towerfear` 的 gate / 代码 / 读数全部保留。

代价是必须回答「两个同时 armed 时谁赢」。答案**做成语句顺序**(restore 在 halve 之后)
并**钉在测试里**(DOMINATION,七帧全测),理由是 **pair 才是 promote 会发的配置**。
不选「把新 id 嵌进旧门里」是因为 **GH #542 / #553**:两个 id 在同一条路径上,哪个都读不出来。
本轮的排列反过来 —— **`towerring` 单独 arm 即可读**(只在环带内折半)。

## 4. 域价钱:**先跑,而它给出的是一个零,如实登记**

`tests/_towerfear_sweep.lua`:109 fixture / **1012** 活英雄帧 / **32** 帧 898 环内有敌塔 /
**17** 帧粗子句被问到 / **3** 帧开火 / **仅 1 帧**由时钟腿单独持有 —— 就是 `towerfear` 的钉帧,
**在 727.46u,属于环带**。

⇒ **在冻结的这份语料上,`towerring` 的「限制域」是 0 帧。**

**这是关于语料的事实,不是关于杠杆的**:这批 fixture 是为别的理由冻的、批测局在攻塔前自终止;
GH #558 的 248 episode 才是总体读数(armed 腿 18/55(ab)、41/90(ba) 摸进去过)。
⇒ 每一条**限制**断言都在真实帧上**只移动 `DotaTime()`**(等级、英雄表、血量、
**以及本杠杆读的那个塔距**全是帧自己的);这个写法是 `towerfear` 文件自己已经用过的
(它把钉帧的时钟拨到 149 来论证「差别就是时钟腿」)。

七个承重帧(四世界驱动读数,`shipped / towerfear / towerring / pair`):

| 帧 | 英雄 | lvl | 塔距 | shipped | towerfear | towerring | pair |
|---|---|---|---|---|---|---|---|
| `f_260819_142047_zuus_ult_denied` **(t=278.5,零操纵)** | zuus | 7 | **727.46** 环带 | 1.1 | −0.3616 | **−0.3616** | −0.3616 |
| `f_260820_102030_wk_tower_in_reach` | skeleton_king | 8 | 787.32 环带 | 1.1 | −0.4015 | **−0.4015** | −0.4015 |
| `f_260820_042607_zuus_reserve_safe` | ember_spirit | 7 | 845.08 环带 | 1.1 | −0.6542 | **−0.6542** | −0.6542 |
| `f_260820_043120_viper_defend_poked` | spirit_breaker | 7 | **174.28** 圈内 | 1.1 | 0.3698 | **1.1** | 1.1 |
| `f_260820_043124_axe_blink_flee_529` | axe | **10** | **285.08** 圈内 | 1.1 | −0.6533 | **1.1** | 1.1 |
| `f_260820_043120_viper_defend_poked` | lion | 6 | **328.01** 圈内 | 1.1 | 0.0406 | **1.1** | 1.1 |
| `f_260819_222030_jugg_tp_eaten` | obsidian_destroyer | 7 | **649.41** 圈内 | 1.1 | 0.3564 | **1.1** | 1.1 |
| `f_260819_122930_lich_rescue_doomed` **(级别腿对照)** | lina | **4** | 860.01 | 1.1 | 1.1 | **1.1** | 1.1 |

axe 那一行单独说一句:**lvl 10 是校准子句 `botLevel <= 9` 够不到的那一档**
(块注释自己量到 0.9% 的释放帧在那里**完全没有兜底**)。`towerring` 按几何而不是按等级覆盖它。

## 5. ⚠️ 诚实边界:牙齿到哪里为止

**700 这个字面量在行为上只被括在 (649.41, 727.46) 之间**,宽 **78u**。
落在这个区间里的改动**帧看不见**,只有源码断言看得见。

语料里离边界最近的那一帧(juggernaut **701.99u**)**不可用**:ShouldRun 更靠前的一个分支
在 5 个不同时钟(160/200/250/290/299)上都出价 **0.75**,四个世界全等 ⇒ **它见证不了任何东西**。
luna **245.20u** 同理。**两帧都写成断言而不是悄悄丢掉** ——
**一个长得像对照、却在每个世界给同一个答案的帧,正是最容易被当成证据的形状**;
写成断言之后,哪天它开始动了,它就自动变成本文件说自己没有的那个紧边界证人。

## 6. 门(铁律 6)

- **静态半**:`bash tools/agent/luacheck_gate.sh` → **`GATE_EXIT=0 CLEAN`**
  (容器冷启:脚本自己 `apt` 装了 **`lua-check`**;`luacheck bots game` **0 警告**)。
  `.githooks` 已上膛(`arm_push_gate.sh`,`core.hooksPath=.githooks`),**未使用 `RULE6_BYPASS`**。
- **动态半**:
  - `tests/test_towerring_attack_circle.lua` **18/18**
  - `tests/test_towerfear_clock_leg.lua` **15/15**(门断言改读两 id 析取)
  - `tests/test_gated_helper_nesting_census.lua` **10/10**
  - `test_gate_claim_consistency` **16/16**、`test_smoke_load` **3/3**、
    `test_retreat_priority_order` **3/3**、`tests/test_write_only_local_census.py` **13 判定全绿**
  - `tests/_towerfear_sweep.lua` 改动前后普查输出 **byte-identical**(17 ROW + 全部 `C` 计数逐位相同)
    ⇒ **出厂行为逐字节不变的证据不是论证是读数**
  - 全套 `lua5.1 tests/run_tests.lua`:见文末「全套结果」一节(GH #124:单进程约 100 min)
- **变异台** `tools/agent/mutstand_towerring.sh`:
  **15 发,13 如声明(12 CAUGHT + 对照 M13 SURVIVED),2 声明式 UNMEASURABLE(M6b / M7b),
  0 意外,零 NO-OP,`STAND GREEN`,EXIT=0**。

### ⭐⭐ 变异台规程:「预测 UNMEASURABLE、结果 CAUGHT」是奉承套件的那个方向

- **M7**(`<` → `<=`)首跑判 UNMEASURABLE(「没有一帧恰好在 700.00u」)—— 行为上确实如此,
  但**源码断言那一半看得见**(`<%s*700` 不匹配 `<= 700`),于是 **CAUGHT**。台子一行就说了 `re-classify`。
  改判,并补 **M7b**:同一处改动**连源码断言一起改**(扮演「调了个数、顺手把测试改对」的人),
  **这才是真正在问行为半边看不看得见**。
- 而 **M7b 自己的第一版是 NO-OP**:perl 针把 Lua 模式里的 `%)` 写成了 `)`,**一处都没匹配上**,
  于是它只是 M7 的第二份拷贝并打印 **CAUGHT**。⇒ **颜色对、程序错,GH #550 同族**,
  只是这次错的不是锚的**唯一性**而是锚的**拼写**,而两者的失败输出**逐字节相同**。
  已改成带计数断言的字面替换(`assert count == 3`,并点名第 4 处 `%s*<%s*` 属于时钟腿**不许动**)。
- **M6/M6b 是同一个成对形状**:单独的 M6(700→900)只证明帧对**远处**的改动有牙;
  单独的 M6b 只会被读成一个洞;**成对才说得出「牙齿到哪里为止」**。
- **M4** 的一行也值得单记:它的 perl 需要携带两个多行需求量穿过 bash 引号,
  首跑打印 `ENV: unbound variable` —— **那不是一个变异体结果**。
  已改成 python 字面块交换,带 `count(a+b) == 1` 前置断言。

## 7. 三个变红的检测器,三个都对这次改动有话说(按 §FF.3 的尺)

1. + 2. `_towerfear_sweep.lua` / `test_towerfear_clock_leg.lua` 的**块窗口**是从 `前期谨慎冲塔`
   起算的**字节长度**(3400 → 4800),本轮的论证注释又把它撑爆了。
   **一个失败模式是「有人解释了代码」的窗口,不是在量代码。**
   两处都改成**尾锚**(`nEnemyTowers[1]:GetAttackTarget() == bot`,**正是旧 assert 在找的那个东西**)。
   **这是第三次因散文而 widen,所以这次不是 widen,是换机制。**
3. `test_gated_helper_nesting_census.lua` 两行 gate-inside-a-gate 的 outer-id 列
   `towerfear` → `towerfear,towerring`。**分类仍是 W**,理由逐字写在行上:
   新 id 门的是**同一个块里的同一个时钟局部**,未 armed 时 `nFearClock` 停在出厂 `5*60` ——
   **它加入的那个合取的单位元**。

**没有一个是抬数字。**

## 8. 交棒(三条,已在 GH #558 下追评)

- **甲(总监)**:`towerring` 按 **P4.2** 请裁 **FROZEN-HOLD**(登记、不入集、不算掉棒);
  并请一并裁 GH #558 对 `towerfear` 的建议 —— 本轮给的是**并列收窄版**而非替换,
  `towerfear` 的 gate / 代码 / 读数全部保留,**不需要动 armed 串**。
- **乙(批测台)**:解冻后 `towerring` **单臂可读**(单独 arm 即在 700–898u 环带内折半);
  与 `towerfear` **同波必须分臂,或明确按支配读**(同时 armed ⇒ 读数属于收窄版,**不属于 `towerfear`**)。
  验收沿用 GH #558 建议:同一套 `towerfear_domain.py` 矩形 + 三对照,
  (i) 保住 R_lever 的 occ/dwell/bnc 两层同号,(ii) 让 episode 级 `<700` 差回到噪声。
- **丙(录像组)**:GH #558 建议的钉帧 `…_eff51c/20260906_035941_slot6 --t 283.4 --hero sniper`
  (塔距 **222u**,在攻击圈内)本容器**取不到**(W50 dump 不在树上,本组不碰 S3)。
  那一帧落成 `tests/fixtures/` 后,§4 的「限制域 = 0 帧」就变成非零。
  **这是本轮唯一一条没有在本容器里付清的账。**

## 8b. 落地与发表(GH #290 的顺序:先 push,后发表引用)

- 推 `claude/intelligent-hopper-x2wk5k` → 推 `origin HEAD:main`,
  两次 `git push` 各自跑了一遍铁律 6 的静态门,两次都 **`GATE_EXIT=0 CLEAN`**。
  首次 `HEAD:main` 被拒(non-fast-forward)⇒ `git pull --rebase origin main`,
  见下;重跑定向测试 + luacheck **全部复现绿色**后再推,`origin/main` 现为本轮的 tip。
- ⚠️ **rebase 冲突,而它挡下的正是 `citation_audit.py` 的 `AMBIGUOUS` 类**:
  总监 07:xxZ 那一轮(结清 GH #454)在本轮写作期间**也认领了 `§FG`**。
  两轮并行,谁都没抢谁 ⇒ **两节都保留**,本节整体改名 **§FG → §FH**,
  报告 / 章程 / `state.json` 的引用同步改。
  **值得单记的是挡下它的是什么**:两节都**追加到文件末尾**,所以 git 必然撞;
  换成两节落在文件的**不同位置**,git 会干干净净地合并出**两个 §FG**,而**没有任何东西会举手**。
- 发表前跑 `bash tools/agent/claim_precheck.sh <草稿>` → **exit 0**
  (`local commits not on origin/main: 0`;paths cited 8 / resolved on trunk 9 / refused 0;
  charter sections cited 1)⇒ 才在 GH #558 下追评:
  <https://github.com/dragonghy/dota2bot/issues/558#issuecomment-5557795387>

## 9. 不做的事

- **不动 armed 串、不动 `queue.json`**(P4.2 入集冻结,合法裁定只有 FROZEN-HOLD)。
- **不改 `towerfear`**(理由见 §3)。
- **不碰 AWS / S3 / EC2 / 波次**,零 CE 调用。

---

## 全套结果 —— ⚠️ **UNCERTIFIABLE,不是通过**

`lua5.1 tests/run_tests.lua`(无过滤)在本容器起跑,**跑到 push 时刻仍未收尾**:
**1135 个用例已跑,`F` 计数 0**(输出里一个 `F` 都没有,也没有任何 `FAIL[` 行),
但**没有到达那一行 `N tests, M failures` 的收尾**。这正是 **GH #124** 描述的形状
(单进程约 100 min,routine 容器里跑不完),不是新发现。

⇒ **按证据纪律,这条腿的结论是「没跑完」而不是「全绿」**:
已跑的 1135 个用例零失败是**读数**,「全套零失败」**不是本轮买到的东西**。
本轮实际付清的动态证据是 §6 列出的那些**定向**结果(新测试 18/18、towerfear 15/15、
nesting census 10/10、gate_claim 16/16、smoke_load 3/3、retreat_priority 3/3、
write_only_local_census 13 判定、`_towerfear_sweep` 前后 byte-identical),
外加 15 发变异台 **STAND GREEN**。

同理,开工自检第二次重跑(1500s,装好 `lua5.1` 之后)也**停在 `=== trunk health
(fast Lua detectors) ===` 之前没有收尾**;它读到的那几节(稳定版锚 / promote-atom /
owed executions / expired waits)与首跑一致,**python 腿仍是 GH #548 的 UNCERTIFIABLE
(84 文件 / 120.1s,骑在 120s 预算刀口上)**。**两条都不是通过。**

铁律 6 的**静态**那半是完整付清的:`bash tools/agent/luacheck_gate.sh` → **`GATE_EXIT=0 CLEAN`**,
`.githooks` 已上膛,**未使用 `RULE6_BYPASS`**(所以没有「这是跳过不是通过」那一行要抄)。

## Token 用量

```
TOKENS total_in=15,625,463 out=95,468 turns=91
```
(统计时刻之后的收尾回合不计入。)
