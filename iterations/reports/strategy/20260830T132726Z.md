# 协同组 2026-08-30T13:27Z — 认领 GH #333:`tpreach` 的判别器不是 mode,是算术

**一句话**:**#333 说「两个世界在数据上不可分」是因为它去找 mode;而撤退支的三条 TP
按下全部带 HP 帽,联立上确界 **0.87**,#333 自己发表的那一帧 hp=0.90 就在帽子外面 ——
撤退残渣那个世界是被算术关掉的,不需要 harness 补字段。**

**本轮零行为改动**:`bots/`/`game/` 逐字节零 diff,零新 gate id,`queue.json` 一字未动,
**零 AWS、S3 零访问**。

---

## 0. 认领依据(铁律 9)

先读 `OWNER_PRIORITIES.md`:常设运维球在批测台;P1 第 1 棒本组已交(`0P1`);
P2 本组那一格 `0SIP`/GH #300 已交棒;P3 归总监 ⇒ 回 issue 流。

open `[strategy]` 四条(#318/#319/#323/#324)**均为本组此前认领、现等他组裁定**。
13:0xZ 录像组新开三条(#332 `[batch]`、#333 `[bug]`、#334 `[harness]`),其中
**#333 §三.1 白纸黑字把下一棒交给 fixture 路线**(`make_fixture.py` + `tests/test_tpreach_band.lua`
配对),而 `tpreach` 是本组地界的 id(本组 GH #319 正在裁它的合取项)⇒ 取 **#333**。

## 1. ⭐ 主判据:撤退支的 HP 上确界是 0.87,是闭式不是估计

`bots/ability_item_usage_generic.lua` 的 TP think 里,`nMode == BOT_MODE_RETREAT`
块(:5506–:5664)**恰好三条 TP 按下**(:5581/:5616/:5662),每条都带 HP 帽:

| 支 | 条件 | 落点 |
|---|---|---|
| 撤退:1 | `botHP < 0.19` | `J.GetTeamFountain()` |
| 撤退:2 | `botHP < 0.15 + 0.24 * nEnemyCount` **且** `nEnemyCount <= (botHP < 0.4 and 2 or 3)` | `J.GetTeamFountain()` |
| 撤退:3 | `botHP < 0.34` **或** `botHP + botMP < 0.43` | `J.GetTeamFountain()` |

**只有撤退:2 会随敌人数抬高,而抬高它的那个量正被它自己的第二个合取项帽住。**
`botHP >= 0.4` 时该帽是 **3** ⇒ 它的杠杆最高只能到 `0.15 + 0.24*3` = **0.87**。
撤退:3 的析取项被操作数值域关掉(`botMP >= 0` ⇒ `botHP + botMP < 0.43` 严格弱于 `botHP < 0.43`)。

⇒ **sup = max(0.19, 0.87, 0.43) = 0.87**,即 **hp >= 0.87 关掉树上每一条撤退 TP 按下**。

**方向是保守的**:`[arith A1]` 把**所有非 HP 合取项一律当成已满足**(掉血史、卡视野、
大药、各种 modifier、离泉水距离、英雄名)。在全部给定的情况下仍然关掉,比按那一帧的
真实取值关掉**严格更强**。

## 2. ⭐⭐ 因此 #333 那一帧的裁定:不是残渣,是真漏

#333 §二 发表的帧:lion,`990f5c/20260830_063340_slot1`,**armed 腿**,t=657.0,
**hp=0.90**,death_prophet 在 **d=705**,DP 攻距 600 ⇒ reach **750**。

- **0.90 >= 0.87 ⇒ 撤退世界关闭**(§1);
- 独立佐证:三条撤退按下的落点**全是 `J.GetTeamFountain()`**,而 #333 自己观察到
  「**落点不回家**」(d(home) 8720→8479→8183 基本原地);
- ⇒ 这次按下来自**会咨询** `J.CanEnemyInterruptTpChannel` 的路径;
- `[drive D1]` 在真实谓词上驱动那一帧的操作数:**未 armed = false**(705 > 700,盲带),
  **armed = true**(705 <= 750)⇒ **armed 本该否决,而它没有**。

**其余两个咨询点不构成第三个世界**:`GetRescueTpTarget`(需 `lf_rescue`)与 mid/sup
响应 TP(需 `midtp`/`suptp`)在自己的门关着时**直接 `return nil`,根本产生不了按下**;
门开着时它们**都咨询**该谓词。⇒ 世界仍是两个,而其中一个已被算术关掉。

### ⚠️ 唯一的限制,也正是下一棒(诚实登记)

0.90 是 **1Hz 采样**。#333 自己的表把按下放在 t=657.0,**夹在 656.5 行(hp 0.90)与
657.5 行(hp 0.69,DP Carrion Swarm 656.3 之后)之间**。**0.69 < 0.87** ⇒ 若按下瞬间的
真实 HP 是后一个采样,**撤退:2 重新打开**(需 `nEnemyCount = 3`)。

⇒ 裁定**条件于一列已经在 dump 里的数**。而这恰恰是本轮最值钱的地方:
**hp 与 nEnemyCount 是 dump 已有的列,mode 不是** ⇒ **#333 §三.2 那条「需要 harness 补
mode 字段,那是另一条线」的路,本组认为不欠**。`[control C1]` 把这个刀口写成断言
(0.86/3 可达、0.69/3 可达、0.69/2 关闭),免得有人把 A1 读成「撤退永远不会 TP」。

## 3. ⭐ 退回 #333 §三.1 的 fixture 路线(只退这一步)

**在这一帧上,fixture 路线买不到判别器,而且会报绿。**

钉在那一帧上的 fixture 驱动的是 `J.CanEnemyInterruptTpChannel`,armed 得到 **true** ——
**在两个世界里都是 true**,因为谓词的答案不取决于调用者问不问它。它会**报绿而不携带
任何关于该歧义的信息** —— 与章程 `0FOG`(甲)、`0GEOM` M5、`0SENSE` M12 同族
(「不承重所以报绿」)。

要判别就必须驱动**调用点**并带一个真实 mode,而 `[corpus C3]` 实测本仓今天做不到:
`GetActiveMode` 在 `bots/` 里被调用 **360 次**、在 `tests/mock/` 里**定义为零**、
**107 个 fixture 里 0 个携带**。⇒ **fixture 路线不是绕开那个缺失操作数,是继承它。**
绕开它的是 §1 的算术。

`[corpus C3]` 写成**存在性**不是头计数:加 fixture 不会让它变红,**只有真把操作数接上
才会** —— 而那一天正是 fixture 路线开始可用的那一天。

## 4. 产出

`tests/test_tpreach_retreat_exclusion.lua`(`[ratchet]`,**8/8**,官方 runner
`run_tests.lua tpreach` = **15/0**(新 8 + 既有 7))。

**变异 11 条:11 CAUGHT / 0 SURVIVED**,分两族登记(A1 跑本文件的模型、source 断言跑树):

- 模型侧 M1–M5;**M5(模型恒 false)⇒ C1 红而 A1 全绿** —— 这就是 C1 存在的理由,
  算术那半只有靠 C1 证明「帽子下面确实够得着」才承重;
- 树侧 M6–M11;**M10(改树上的 0.24、模型不动)⇒ S2 红而 A1 绿** —— 把分工写成实测:
  S2 一旦被删,A1 会继续通过,但描述的是一棵不存在的树。

### ⚠️ 当轮自伤(第一遍红、当轮修好)

`[source S1]`/`[source S3]` 第一版锚在 `nMode ~= BOT_MODE_RETREAT` 上,**而它在本文件里
不唯一** —— 另一处在 :4029 且**排在前面**。于是 S1 的 span 从 :4284 一直吃到 :5668,
**把 :5239 那道真门吞了进去**,健康的树上 S1/S3 双红。改锚到**唯一的 wrapper 调用**
`J.ShouldNotStartInterruptibleTp( bot )`,并加 `TPSAFE2_ONCE` 把这个唯一性钉住
(M11 证明它有牙:加第二个调用点 ⇒ S1+S2+S3 三红)。与 `0GEOM` M5 同族:
**锚点唯一性本身要被断言,不能靠读一次 grep 记住。**

## 5. 诚实边界

- **无行为改动 ⇒ 无修复用 fixture**;本轮**无新帧**(#333 那一帧是**被引用不是被驱动**,
  其 timeline 在 S3,本组零 S3 访问)。
- **不重推也不反驳 #333 的 6/1010、分层、或 INDETERMINATE 裁定**。分层那条现在归
  **铁律 4(i-b)**(总监 08-30T13:03Z 按 GH #329 把 4(i) 拆成四条,`test_set.md §CL`):
  `tpreach` 的 ADDED 是**检测器计数、侧偏未消除**,正是 (i-b) 点名的典型 ⇒ **两层反号 = 噪声**
  的判读成立(不是 (i-c) 那条「已 swap-average 的估计量反号不算否决」)。
  本轮只重开它发表为**素材**的那一帧。
- **不主张 `tpreach` 该 promote,也不主张它该出集**:(b)/(c) 不归本组,**一帧不是条件 (a)**。
- hp=0.90 与 d=705 是 **#333 的数**,引用不重算;`nEnemyCount` 的语义已核到源
  (`X.GetNumHeroWithinRange(1600)` 数 `GetOpposingTeam()`,`[source S2]` 钉住)。
- `[arith A1]` 跑的是**本文件的模型**不是树;树侧靠 `[source S2]` 钉,M10 是这条的实测。
- 顺带记一条读法限制供总监参考:**mock 层吞掉 `print`** —— 手写 runner 用 `print` 报读数
  会静默丢失(本轮第一次跑就撞上,改 `io.stderr` 才见输出)。

## 6. 门(铁律 6)

- `bash tools/agent/luacheck_gate.sh` ⇒ **0 warnings,EXIT=0**(冷启 9.0s,自己装的 `lua-check`);
- `tools/agent/arm_push_gate.sh` ⇒ `already set`;**`RULE6_BYPASS` 未使用**;
- 新文件官方 runner **8/0**;`run_tests.lua tpreach` **15/0**;
- 邻居全绿:tpsafe **7/0** · tpgap **22/0** · tphome **14/0** · coarmed **11/0** ·
  gate_claim **10/0** · gated_helper **10/0** · itemdesire **25/0**;
- `git diff --stat -- bots game` **为空**(变异后已核对复原);
- 开工自检 worst exit **3**(**仅 cadence**,是 director 08-29T04:08Z→16:28Z 的 12.3h 洞、
  且自检自己标注窗口内有 3 个命名不规范文件 ⇒ **不属本组、且很可能是命名问题不是空转**),
  `UNCERTIFIABLE: none`,python 腿 **57/0**,快腿 **38/0**,锚点 stable-v1/v2 各 3 项 ok。
- **⚠️ 全量单进程套件本轮未跑完**:后台起了一次,而上一轮实测墙钟 ≈ **310 分钟**
  (GH #124),本工作单元长度装不下 ⇒ **「跑到这里没红」不是「全绿」**,不据此声称主干状态。

## 7. 交棒

- **录像组**(球在这里,而且这一棒变小了):在 `990f5c/20260830_063340_slot1` 上读
  **按下瞬间(t=657.0)的 hp 与 1600 内敌方英雄数**两列 —— 两列 dump 都已经有。
  `hp >= 0.87` ⇒ 撤退世界关闭 ⇒ 那一帧判 **BUGGY**;`hp < 0.87 且 nEnemyCount == 3`
  ⇒ 仍不可判,那时才轮到别的办法。**不需要 harness 补 mode 字段。**
- **总监**:请裁 #333 §三.3(`--reach-mode source` 那列的 0 写进 §BC 作已知读法限制)——
  本组同意写,理由与 #333 一致(`SOURCE_CITED_RANGE` 8 个英雄本语料一个没出场)。
- **不认领**:#332(`[batch]`)、#334(`[harness]`);#318/#319/#323/#324 均为本组此前
  认领、现等他组裁定。

## 8. 已发表

**先 push(`782ff956`)再发表**(铁律 6 ⭐GH #290 的顺序);`claim_precheck.sh` **exit 0**(`local commits not on origin/main: 0`;paths cited 3 / resolved on trunk 3 / refused 0)。
已发表 GH **#333** 追评 **5469014421**(裁定全文)。

## 9. Token 用量

```
TOKENS total_in=9,382,498 out=75,984 turns=67
```

(统计时刻之后的收尾回合不计入。)
