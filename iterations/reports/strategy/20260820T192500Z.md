# strategy 2026-08-20T19:25Z — DefendThink:1441 third-leg tautology pinned (post-#61)

## 一句话

`DefendThink:1441` 的「outnumbered veto」是同族第三条,与 `defnum`(1219)/
`defstale`(1314)同源:上游 `line 1069` 提前 return 把 `#ds.nInRangeEnemy`
钉成常数 0,`#ds.nInRangeAlly >= #ds.nInRangeEnemy` 恒真。**#61 落地后现在
可以在状态层把这一条实证地钉住**,不改动作层(动作层需 dist<1600 的真实
帧,当前 fixture 一帧不覆盖)。**不动 bot Lua,不新增 gate**;
`test_defnum_recent_hit_parity.lua` 的 header/MECHANISM/[reverse]
分别补一处断言,共 3 处。

## 认领

`ls -la` open `[strategy]` issue 中带新证据的只有 **#72**(录像检查组 18:36Z
提交 `capmono` 隔离 (a) 核验:SILENT,不 promote,不构成 `capmono` 有害)——
这是一份结论清单,决定权在总监(admit/reject/test_set),不是本组要写的 Lua 或
测试。所以照章程「没有可认领的新 [strategy] issue 就从 backlog 取最上面一条」
往下走,第 0z 条(#61 landing 之后 `DefendThink:1418` 是「最值钱的下一个」)
就是本轮的目标。**#61 已在 17:00Z 由总监落地**(`git show 71b510c`,
`rf.load` refuses `GetLaneFrontLocation`;6 个测试文件在同一 landing 里各自
声明了自己的假设),所以第 0z 条的头号阻塞已解除。

## 静态审计

### 缺陷本体(与 GH #62 同族第三条)

`bots/FunLib/aba_defend.lua`:

- **line 1062**: `ds.nInRangeEnemy = jmz.GetLastSeenEnemiesNearLoc(bot:GetLocation(), 1600)`
- **line 1069**: `if #ds.nInRangeEnemy > 0 or ... then return VeryLow`
  ⇒ 每一条能让 `GetDefendDesireHelper` 返回非-VeryLow 的路径,
    通过了 1062 且**必然**满足 `#ds.nInRangeEnemy == 0`。
- **line 1441**(DefendThink,已发布未 gated):
  `if (ds.weAreStronger or #ds.nInRangeAlly >= #ds.nInRangeEnemy) and dist < SEARCH_RANGE_DEFAULT then Action_AttackMove(...)`
  ⇒ 右操作数是常数 0,`#ally >= 0` 恒真,`ds.weAreStronger` 冗余,
    整个前置条件塌成 `dist < SEARCH_RANGE_DEFAULT`。
    **作者写的「被压制就不 attack-move 进 hub」这条否决(elif/else 兜底
    走 `Action_MoveToLocation`)在防守 mode 赢下竞价时不可达。**

这与 `defnum`(line 1219,`0 >= #ally`)和 dead-code line 1139
(`0 > #ally`,#62 已定为死代码)同源;`defstale`(line 1314,
`#pathEnemies > #nInRangeEnemy` 塌成 `#pathEnemies > 0`)是第四条。
`test_defnum_recent_hit_parity.lua` 的 header 早已列出这一条,称之为
「near-dead」;此前没有源码/状态层的钉子,一次重构就能悄悄改掉。

### 出价 vs 动作层的分界(诚实登记)

- **状态层已可断言**:所有能到达 line 1441 且当帧防守出价合法的路径,
  `#ds.nInRangeEnemy == 0`。这是通过驱动 `GetDefendDesireHelper` 后读
  `bot._defend.nInRangeEnemy` 得到的真实值,不是模型。
- **动作层未可断言**:「shipped 到底选 `Action_AttackMove` 还是 elif/else 的
  `Action_MoveToLocation`」需要**同一帧上** `dist < 1600` 且 defend 赢下
  竞价。当前 3 个 fixture(POKED/PAIRED/WD)的 subject 全部在离自家最远
  建筑 ~8k 的位置,`ds.distanceToLane` 远大于 `SEARCH_RANGE_DEFAULT` (1600)
  ⇒ DefendThink 走的是 `dist > SEARCH_RANGE_DEFAULT * 1.7` 的 elif,
  Action_MoveToLocation with jitter 300。line 1441 的 `Action_AttackMove`
  分支在这些 fixture 上根本不可达。
- **不伪造 lane front 到 subject 位置附近来强开 `dist < 1600`**:那样等于
  在 subject 位置捏造敌方推塔现场,不是「declare an assumption」,是模拟
  游戏世界。已按 #61 discipline 把 (a) 语料请求路由到 replay-check
  (GH #62 追评):要一份「防守 mode 赢下 + dist<1600 + `ShouldDefend` 不
  抢先 return + 没有可见敌方 hero/creep 在 hub 附近」的真实帧。

## 交付(3 处断言,合计 +32 行 test 文件,0 行 bot 代码)

`tests/test_defnum_recent_hit_parity.lua`:

1. **Header**(第 14-17 行注释):把 `line 1418` 改成实际源码里的 `line 1441`
   (代码飘动小的记账修正),补一段说明:post-#61 的钉子被加到本文件的
   MECHANISM 与 [reverse] 里,并**如实登记动作层仍未可达**、语料请求已路由。

2. **MECHANISM 测试**(既有 `MECHANISM: ds.nInRangeEnemy is empty ...` 里):
   循环体末尾追加一行断言:
   ```lua
   assert(#ds.nInRangeAlly >= #ds.nInRangeEnemy, ...)
   ```
   POKED/PAIRED/WD 三帧同时钉住第三条腿的状态层结论。

3. **[reverse] 测试**:两处新增源码断言 —
   - `#ds.nInRangeAlly >= #ds.nInRangeEnemy` 必须存在(shape pin);
   - 一段捕获整行的 `src:match` 断言 line 1441 **未被加 gate**
     (`IsSoakCandidate(...)` 未出现在该 if 里);未来若有人给它上 gate,
     strategy backlog 「已发布未 gated 的动作类缺陷」这条说法就得改。

## 验收

- `luacheck bots game --formatter plain`:**0 warnings**(bot 侧未动)。
- `lua5.1 tests/run_tests.lua`:**857/857** → **857/857**(不变;
  MECHANISM 循环体里追加一条 assert 不改例数,仅加强断言强度)。

  基线 857 是本会话安装 luacheck+lua5.1 之后跑出的数字(比章程 §当前状态
  最近提到的 838/838 高 19,来自本轮之前落地的多条 gate + fixture 测试)。

- **变异**(按预期 FAIL):
  - **(ii) 方向对调**——把 aba_defend line 1441 的
    `#ds.nInRangeAlly >= #ds.nInRangeEnemy` 改成
    `#ds.nInRangeEnemy >= #ds.nInRangeAlly` → 新加的两条 [reverse] 断言
    (shape pin + ungated pin)**同时 FAIL**(整行 `src:match` 也匹配不上)。
    **实测**(mutation applied → run tests → restore → re-run)。
  - **(iii) 加 gate**——把 line 1441 前面加 `jmz.IsSoakCandidate("defwin") and` →
    [reverse] ungated pin 的 `src:match` 整行捕获返回 nil → **FAIL**。
    **实测**(mutation applied → `lua5.1 tests/run_tests.lua defnum` → 11 tests,
    1 failure at [reverse] line 404 → restore)。
  - **(i) 填 `ds.nInRangeEnemy` 非空**——现有 MECHANISM 断言
    `#ds.nInRangeEnemy == 0` **已经**会在三帧同时 FAIL(既有 assertion,
    非本 PR 新增);新加的 `#nInRangeAlly >= #nInRangeEnemy` 断言是同一列
    的 corollary,不改变这一变异的 FAIL 结果。这一条不是本 PR 独立的
    保护面,只是同族状态断言的**一致性**。

## 世界断言合规检查

- 本 PR **不新增读 GetLaneFrontLocation 的断言**;既有 [limitation] 测试
  已在 #61 landing 时改成显式 declare origin,继续继承那份记账。
- 状态层结论只读 `bot._defend.nInRangeEnemy`(来自 helper 写入,不经 lane
  front);动作层结论**未提出**,故不涉及 #61 后续。
- 幻象污染(#69)不涉及:本 PR 读的是 `_defend.nInRangeEnemy`(可见敌人
  英雄列表,查询自 `bot:GetLocation()`,不是伤害账本)。
- 结构不可寻址(第 6 条断言,#58)不涉及:本 PR 读的是 hero 集合,
  不消费 `GetTower`。

## 不做

- **不推 gate**。line 1441 的三条候选方案(delete 死码 / 用 `#pathEnemies`
  替换 / 用 `#enemiesAtHub` 替换)每一条都需要动作层的真实帧才能验收,
  当前 fixture 一帧不覆盖。**先要语料再动杠杆**是本轮延续 backlog 第 0a/0b
  的做法(与 `lfcorelane`/`defnum` 的入集判据同一族)。
- **不改 bot Lua**。行为一位没动;shipped 到 armed 的路径与之前完全一致
  (857/857 逐位不变)。
- **不申请入 test_set**。第三条腿无 armed 语义。

## 更新的文件

- `tests/test_defnum_recent_hit_parity.lua` — 3 处断言 + 一段 header 补充。
- `iterations/state.json` — 新条目 `defthink_third_leg_pin_20260820`。
- `iterations/streams/strategy.md` — 「当前状态」新一行、backlog 第 0z 条
  更新(landing 后本组已把状态层钉住,动作层要 (a) 语料)。
- 本报告(`iterations/reports/strategy/20260820T192500Z.md`)。
- GH #62 追评(把结论回传给 defnum 那条 issue)。

## AWS

**未花 AWS 钱**(未启动任何计费资源,未提批测请求,未读 S3 —— 本轮完全是
本地 Lua/静态审计工作)。

## Token 用量

TOKENS total_in=33,314,305 out=202,292 turns=183
