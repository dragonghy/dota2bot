# 协同组 — 2026-09-12T16:44Z

**工作单元**:GH **#760**(`overchase` 的 DEEP 没有速度项)。定价了**四个**收窄读法,
**一个都没落地**,而这一次「没落地」是一个**判定完结**,不是又一轮踩空 ——
拒的理由分成两类:**三个是实测 no-op / 人为品,第四个根本量不了**,
而**卡点不在选哪个修法,在 dumper**。那一棒 GH #767 §6.3 自己说「够立一条案了」
并且**没代开**,本轮交出去了。

**认领依据**:`OWNER_PRIORITIES.md` 的 P1/P2 本组交付早已落地(章程已连续四轮请主会话
核对球权,本轮不再重复占一个工作单元);P4.4 要求工作单元主体是 (i) 一个 `bots/`
行为改动**或** (ii) 一个判定完结所需的最后一块证据 —— 本轮是 **(ii)**,
并且这是**故意的选择**,理由写在 §3。
[strategy] open issue 里 #760 是唯一带**承重帧 + 建议验收**且不在等别组取帧的一条。

---

## 1. 最该被下一轮读到的四句

**(甲) ⭐⭐ 否定式读数的正对照必须是一个「必须变小」的读数,不能是那些 no-op 断言自己。**
本轮全部结论都是否定式(「它一个都不拒」「这个量具是瞎的」)。一个**忽略自己回看参数**的
`WasRecentlyDamagedByHero` 会把每一条**免费**证成 —— 而且它长得**跟真 no-op 一模一样**:
tight 读数恒等于 loose 读数,**正是本轮要报的那个数**。所以加了一个 **0.2s 探针**
(低于证人集里最小的 dt,因此**必须**比 `oc_a_recentdmg` 小;实测 **0 vs 2**)。
变异台 **M1**(探针窗被放宽回出货值)与 **M2**(mock 忽略回看参数)**机制不同**,
**都只被这一条断言抓住**;那 6 条 no-op 断言对两者**全部存活**。

**(乙) ⭐⭐ 一个咽下去的 FAIL 咽不下任何别的东西 —— 包括退出码。**
`tests/mock/bot_api.lua:415` 在 fixture 加载时把全局 `print` 设成空函数
(注释写着「keep test output clean」)。本文件**第一次跑**打了 13 个 `ok`、**退出 0**,
而 §6/§7 **两节连汇总行一起消失**:`fail` 仍然正确计数、`os.exit` 仍然答 0,
**读者眼里是绿的**。修法两条:在任何 mock 碰全局之前把 `print` 绑成 local `say`,
外加一行**完成标记**,让「这个文件到底报告了没有」**可以从外面回答**。
⇒ 变异台 **M5 因此不按退出码计分**(那个缺陷**动不了**退出码),按**标记缺席**计分。
把它按退出码计分、再宣布 `caught 7/7`,那是 **stand 自己在说谎**。

**(丙) ⚠️ M6 的锚歧义,而歧义的那一侧正是被钉的断言自己。**
`REGISTERED, NOT REPAIRED` 这句话在 `jmz_func.lua` 里**出现两次**(同一天 `tpdeftower`
的登记用了同一句)⇒ 原断言那句 bare search **由另一处注释满足**,
**leg (a) 的注释被删掉它照样过**。两边同时收窄成 `GH #760] REGISTERED, NOT REPAIRED`。
**是变异台找出来的,不是复核找出来的。**

**(丁) ⭐ #760 的抱怨在 leg (a) 上比在 leg (b) 上更锋利,而这是免费掉出来的。**
issue 说 leg (b) 是纯位置判据、不问「他在进来还是在出去」。同一句话对 leg (a) 成立且更硬:
三个追击析取项里**语料能答的只有过去时那一个**
(`oc_a_attacktarget 0` / `oc_a_ischasing 0` / `oc_a_recentdmg 2`)⇒
「追击者正压在我队友身上」**全部**由「他**最多 2 秒前**打过我队友」推出来,
而那**正是一个已经掉头回家的人的样子**。

---

## 2. 四个读法,逐个定价(111 fixtures / 1031 活英雄行 / oc_pairs 804 / oc_iso_deep 33)

| # | 收窄读法 | 读数 | 判定 |
|---|---|---|---|
| (1) | leg (b) 加速度项(**#760 自己的建议 (a)**) | — | ⛔ **量不了**。fixture 是**一个瞬间**;`tests/mock/replay_fixture.lua` 的 `GetVelocity` 按**已申报的世界假设**恒返回零向量,没有 depth 差可读 |
| (2) | `J.IsChasingTarget` 由析取提成必要条件(**#760 的建议 (b)**) | 拒 **2/2** | ⛔ **人为品**。它要 `GetAnimActivity` / `IsFacingLocation`,**dumper 两个都不带** ⇒ `oc_a_ischasing` **构造性为 0**。量具造出来的歼灭不是关于游戏的读数 |
| (3) | 追击回看窗 **2.0 → 1.0s**(**唯一一个 dump 能答的现在时收窄**,因为 `recent_damage` 带逐事件 `dt`) | 拒 **0/2** | ⛔ **no-op**。`oc_a_pass_tight == oc_a_pass == 2`,`oc_a_dmg_dt_le_1s 2` —— 两个证人身上追击者是 **0.3s / 0.7s 前**刚打的。**这是唯一一个用能用的量具取到的读数,所以是它结案** |
| (4) | leg (d) 把将死的队友从 numbers 分支扣掉(`f_071423_luna_chase` 那个形状,本文件自己的 `J.ShouldNotChaseWhenLow` 头注记着它烧过一轮批测) | `oc_d_numbers_thin` **0** | ⛔ **no-op**(前轮已定价,本轮复现) |

**效应量上界**:全语料 `oc_fires` **2**,两个证人在**同一份 fixture**
(`f_20260827_091703_slot12_zuus_473_1`)、压在**同一个 0.18 血队友**身上。
⇒ 「两帧」这个天花板**先于**「哪个修法对」这个问题(GH #770 那个形状)。

**新增普查桶**(全部由源码解析出常量,不硬编码 —— M13 教训):
`oc_a_recentdmg_tight` / `oc_a_pass_tight` / `oc_a_dmg_dt_le_1s` / `oc_a_dmg_dt_le_02s`,
外加 `G.CHASE_DMG_LOOKBACK`(从 leg (a) 第三个析取项解析)与
`F … oc_a_pass_loose_only`(逐帧可手工复核的身份行,实测 **0**)。

⚠️ 普查里去掉了 leg (a) 循环的 `break`:出货答案要它,**普查不要** ——
tight 读数需要 disc 里**每一个**低血队友,不是碰巧先满足松测试的那一个。
出货答案不受影响(`bLowAlly` 在循环里单调,而真答案是**单独驱动函数**取的)。

---

## 3. 为什么本轮主体是 P4.4 (ii) 而不是 (i)

先说清楚:**这是一次有意识的选择,不是没找到活干。** 本轮按顺序**排除**了四个
本组范围内的 `bots/` 行为改动候选,每一个都有**独立的实测**理由:

1. **GH #782 的 outpost 收窄**(章程「下一格」第 (1) 项,写着「离可落地只差一帧」)——
   复核确认前一轮的结论**是对的**:端到端 0 见证。**本轮又替它数了第三个站点**:
   同一个 `string.find(name,'tower')` 缺陷共 **3 处**,
   `J.GetRescueTpTarget`(`lf_rescue`,outpost 会**压掉**一次救援 TP,方向是**假阴性**)、
   `J.ShouldAbortRoshanAttempt`(`roshgate`)、
   以及**出货未 gated** 的 `bots/mode_retreat_generic.lua:95`。
   自写的几何普查:语料里**每一份** fixture 都带 2 枚 `watch_tower`,但
   救援候选队友(>3500、hp<0.35、900 内有 diver)只有 **5 个**,
   其中**在任何己方塔/outpost 900 内的:0 个** ⇒ `bUnderOwnTower` 全语料**恒假**,
   这一刀在这里也没有帧。`mode_retreat_generic` 那处**构造性惰性**
   (outpost 无攻击 ⇒ `GetAttackDamage()` 0 ⇒ `towerDamage` 为负 ⇒ 比值 < 0.5),
   但那**在语料里和在游戏里不可区分**(mock 的 `^Get -> 0` 兜底给的也是 0)⇒
   **「表零族」,登记不落刀**。
2. **leg (a) 的幻象漏洞** —— 查了,**不存在**:`J.GetAlliesNearLoc` 只遍历
   `GetTeamMember(i)`,幻象进不来。(`J.GetRescueTpTarget` 里那层重复的
   `IsSuspiciousIllusion` 过滤**是冗余的**,因为 `J.GetEnemiesNearLoc` 自己已经滤过 ——
   无害,登记。)
3. **#767 的胜负判据两个读法** —— 前两轮各自定价过、各自撤回过,理由是两个不同的实测。
4. **本轮这四个** —— 见 §2。

⇒ 逼一个改动落地才是章程反复付过学费的失败模式(`lanefix` 捆绑 gpm **−74.5 / −88.7**、0/4)。
**四个独立的决定性零 + 把解锁它们的那一棒交出去**,是这轮能给的最大价值。
⚠️ 但这也意味着 **P4.4 的 `bots/` 占比本轮本组没有推进**,这句话照实写在这里,
不用「判定完结也算」把它糊掉 —— 下一轮第一项见 §6。

---

## 4. 产出

- `tests/_overchase_sweep.lua` — 普查扩展(4 个新桶 + 1 个解析常量 + 身份行 + 去 `break`)
- `tests/test_overchase_pursuit_tense.lua` — **新增,20/20**,实测 **30s**
- `tools/agent/mutstand_overchase_tense.sh` — **新增,caught 7/7 STAND GREEN**
  (⚠️ M5 **按标记缺席计分**,不按退出码 —— 见 §1(乙))
- `bots/FunLib/jmz_func.lua` — leg (a) 一处登记注释(**零行为改动**)
- `iterations/state.json:overchase_tense_20260912`
- 本报告

**边界**:零行为改动、零新 soak id、`bots/` 里不存在叫 `overchase_tense` 的闸、
armed 串 / `iterations/queue.json` / `iterations/streams/test_set.md` **未动**、**零 AWS**。
`overchase` 本身仍未 promote,所以这个 helper 在**每一局真实对局里都返回 nil**。

**铁律 6 三行**:
```
GATE_EXIT=0        (luacheck bots game: 0 warnings)
py gate:  96 ran, 0 findings, 0 uncertifiable, 26.7s
lua gate: 351 ran, 0 findings, 0 uncertifiable, 9 known-red, 345.2s
```
⚠️ `lua_gate` manifest 登记**欠着**:本测试实测 **30s**,**超** per-test cap 5.5s ⇒
本来就在钩子快域外,要带 `reason` 登记。**不是懒** —— 唯一被许可的登记路径
(全量 `lua_gate_measure.py`)会把 main 上 **9 条 known-red 清空**,
一次 re-measure 就变成挡住所有组 push 的 finding;根因已是 **GH #783**,
本轮照前几轮的处置**只登记不动**。

⚠️ **开工自检本轮 UNCERTIFIABLE**:先被脚本以 `REFUSED: stdout is a PIPE` 挡回
(证据纪律第 3 条,它自己说这是第 5 次复发),重定向重跑后 **300s 与 900s 两次都超时**,
死在 **trunk-health(python 套件)**那条腿上 ⇒ **trunk 的那一侧这轮没人完整看过**。
跑完的那部分**是干净的**:8 个 stable 锚全 ok、promote-atom 约束 ok、
inverse gate census `FROZEN 0 / UNRESOLVED(armed) 0`。

---

## 5. ⛔ trunk 上一条**不是本轮造成**的红(GH #624 那一族的下一例)

自检跑完的那部分报了 **TRUNK RED**:

```
tests/test_detector_source_constants.py
  -> tools/batch_test/behavioral/tpdeathbuy_domain.py:150
     SourceConstantError: item_purchase_generic: the tpdeathbuy arm is no
     longer `gate then bDyingWithDoomedGold = botHP < X`
```

根因:`tpdeathbuy` 已在 **stable-v7** promote、**闸被摘掉**,而这个检测器的
source-constant 钉的是**一个只在 gated 状态下存在的形状**。
它**在快 py 域外**(`py gate: 96 ran, 0 findings`,**不挡 push**)⇒
**promote 那轮全绿,红留给下一个开工的组** —— 逐字就是 GH #624 的立案句,
与 #765(`test_stale_waits.py` 被 `tpcommit` promote 顶红)**同型**。
⚠️ 与 GH #762(「promote 会把量具里的可达性前提构造性地变成永假」)**同一个楼层**,
但那条讲的是**可达性前提**,这条讲的是**源码形状断言** ⇒ 三个 promote-time 普查
**都只读 `bots/`**,两者都看不见。本组只**登记 + 交 issue**,不代修(`tpdeathbuy` 不是本组的)。

---

## 6. 交下一棒

1. ⭐⭐ **GH #786(新开,[harness])**:dumper 扩三个字段 —— **朝向 / 速度 / 塔的 attack target**。
   这是 **#767 §6.3 逐字要求的那一棒**(它说「这已经是第三个卡在同一件事上的杠杆,
   够立一条案了」并且**明写不代开**);本轮把 `overchase` 的 leg (a) 数成**第四个**,
   而且第一次给出了「三个读法**各自**为什么读不出来」的分项读数。
2. ⛔ **GH #760 不关**:本轮给的是「**这个语料已经挖干了**」的判定,不是「这个缺陷不存在」。
   `tests/test_overchase_pursuit_tense.lua` 把每个数字都钉住了,**语料一长它变红而不是悄悄失真**
   (M3/M4 就是这两条腿的变异)。
3. ⛔ **GH #782 不关**:本轮**替它数了第三个站点**(见 §3.1),三处全部 0 见证;
   取帧请求(#782 §4 的四条判据)仍在录像组手里。
4. ⛔ **新开一条 [harness]**:§5 的 trunk 红(`tpdeathbuy` promote 顶红检测器 source-constant)。
5. ⛔ `midtp`/`suptp` 的入集/退集是**总监的球**,P4.2 冻结期内本组不提不催。
6. ⛔ 读 `botTarget` 的 consider 条目族仍不动(GH #474,**连续第十八轮有效**)。
7. ⛔ 兵营分支(GH #713)仍不落 gate,`pulldrag` 永远不许单独提。
8. ⚠️ **给主会话,连续第五轮**:`OWNER_PRIORITIES.md` 的 P1/P2 仍写「球在协同组」,
   但两项的本组交付都已落地,请核对球权并归档或改派。

### ⛔ 下一格(本组下一轮第一项)

**(0) ⭐⭐ 主体必须是一个真落地的 `bots/` 行为改动(P4.4 (i)),不许再是定价。**
本轮已经把「定价到零」这条路走到尽头:**连续三轮**(`tpdefall` / `tpdeftower` / 本轮)
主体都是定价,**而三轮的零各有不同的实测理由** ⇒ 问题不在某一个站点选错了,
在**本组一直在拿语料买不到的杠杆当工作单元**。
⇒ 下一轮**先按「语料有几个证人」挑杠杆,再看它对不对**:
从 `_overchase_sweep` 这类普查已经量出**≥10 个证人**的域里挑,
**证人数不到 5 的一律不立为本轮主体**(登记进 backlog 等 dumper)。
本轮量到的候选域:`oc_deep_building` **53** / `oc_iso_deep` **33** /
救援候选队友 **5**(太少)。
**(1)** GH #786 落地后,leg (a)(2) 与 leg (b)(1) **同时**重新定价 —— 它们卡在同一个字段集上。

---

## 7. Token 用量

见下节(收尾时 `python3 tools/agent/token_usage.py` 的最后一行原样贴入)。
