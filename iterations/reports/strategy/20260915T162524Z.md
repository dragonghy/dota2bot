# 协同组 2026-09-15T16:25Z —— 一条待入集的杠杆,它的宿主引擎从来没调用过

**出口:4.4 (ii)**(一个判定完结所需的最后一块证据)。`bots/` 有 diff 但**是注释**,
所以本轮**不声称 4.4 (i)**,见 §6。**零 EC2 / 零 CE / S3 读取 0 个对象**。
**未提入集**(P4.2 冻结);本轮的结论恰恰是**一个 id 不应该入集**。

---

## 〇 铁律 9 与选题路径(先走门,再取 backlog)

- **P1**(拉野/creeppull,球在协同组 (1)):上一轮已结案交总监(GH #809),本轮无新棒。
- **P2**(低血不回家,球在协同组):TP 腿仍卡在
  `owed_executions.json:wandlimbo_charge_instrument` 的**两端仪器墙**(RULING 37)。
  查过那一行的 `executor`:买它要动 **dumper 侧 `main.go`(Go,需 `get_dumper.sh` 重建)
  + fixture 侧 `replay_fixture.lua` + `make_fixture.py`**,**两端都要**。那是**量具类**工作,
  按 **P4.4** 每轮至多作为附带一条、**不得成为工作单元主体** ⇒ 本轮不取它。
- ⇒ 走章程 **1b**,取 backlog 最上面一条 **0NEXT22**。

**0NEXT22 的判据原文**:*grep 注释里拿别处当反面对照的句子(`unlike`、`carries no`、
「不像」…),对每一个被当成反面教材的地方,去数它有没有 id;没有 id 的对照物就是选题,
而且它的定价依据已经被写好了。*

**判据照跑了,而它交出来的不是一个杠杆,是一个已存在杠杆的处置。** 这是本轮最该被
下一轮继承的一句。

---

## 一 ⭐⭐ 头条:`waitclar` 的宿主没有调用点,而且从来没有过

`bots/mode_roam_generic.lua`:

| 事实 | 位置 |
|---|---|
| 函数定义 | `~1571` `function ConsiderWaitInBaseToHeal()` |
| **唯一**调用点,**注释掉的** | `~115` `-- if ConsiderWaitInBaseToHeal()` |
| 去注释后该 token 在全文件的出现次数 | **1 次**,就是它自己的 `function` 行 |
| `waitclar` 的门 | `~1676`,**在这个死函数体内** |

⚠️ **不是谁改坏的**:上游 OHA 快照 `74727e4a` 里这段在**它的第 74 行**就已经是注释掉的
⇒ **这个函数在本仓库全部历史里一次都没活过**。

⭐ **连带死第二层**:函数对外唯一效果是 `~1681` 的 `ShouldWaitInBaseToHeal = true`,
而那是全文件该 flag **唯一**的 `= true`;`~484` 的 "Heal in Base" 分支读它
⇒ **一个死调用点同时杀掉消费分支**。

### 为什么是现在要紧

`waitclar` **不在 `armed_since.json` 里** —— 它是**已落地、待入集**(P4.2 冻结)。
`test_set.md` 已经写好交棒:「解冻后 `waitclar` **单臂可读**」。
⇒ **入集后的第一波会测出一个结构性的零**,并把它报成「tested, no effect」,
**而没有任何东西会举手** —— AGENTS.md 逐字记过的那个形状。**这一轮就是那只手。**

### 0906 那次到底测了什么

`tests/_waitclar_sweep.lua` 报「1012 活体帧里出厂函数答 true 的 6 帧,5 帧走 MANA 腿」。
这些读数**对条件内部是对的**,对「谁在问」是沉默的:sweep **直接调用**
`ConsiderWaitInBaseToHeal()`(它自己的两个 pcall 点),**而那是全仓库唯一的调用者**
⇒ **探针在读自己**。

与 `stayfield` 的 `CALLSITE-EMPTY`(RULING 37)同族,**但更强**:
stayfield 的调用点**存在**、是调用点自己的合取吃掉了 24 帧里的 23 帧;
这里调用点**不存在,而且从来没存在过**。

---

## 二 ⛔ 两种零,这一轮把 0NEXT22 刚立的分界线用上了(而且指向相反的处置)

| | `siegecap`(同组,09-15T13:41Z) | `waitclar`(本轮) |
|---|---|---|
| 零的种类 | **语料覆盖** | **构造性** |
| 理由 | 分支对每个被抽到的英雄、每局都跑,只是语料停在 850s | 没人调用宿主 |
| 能被什么解除 | **一帧**(GH #837 / `queue.json:strategy-47`) | 只有**恢复调用点**,那是一次未 gated 的出货改动 |
| 处置 | **落地 gated + 欠条 + tripwire** | ⛔ **不入集** |

📌 **在自动读者眼里两者都是一个 `0`**。区分它们要问的问题,0NEXT22 已经写好:
**「这个零是发波人群的性质,还是这批帧的性质?」** 本轮第一次拿它判出了**反方向**的处置,
这比上一轮拿它判「落」更能说明它有用。

---

## 三 判据自己交出来的候选,以及**为什么它不落**

0NEXT22 点名了一个具体候选,而且它**完全符合**判据的形状:
`waitclar` 自己的 **honest bounds (4)** 逐字写着 ——
HP 腿那张**十个** modifier 的否决表**漏了 `modifier_bottle_regeneration`**,
「*a separate finding with a separate domain, filed rather than bundled in*」
⇒ **一条被登记在注释里、没有 id 的缺陷**,连定价都已经有人写好了。

**它没落,理由就是本轮的头条**:宿主是死的 ⇒ 它的域是**构造性零** ⇒ 按 §二 不落。

⭐ **旁证,而且是自洽的那种**:**活的**孪生分支**早就带着这一格** ——
`ability_item_usage_generic.lua:5745` 与 `:6141` 两处都有
`and not bot:HasModifier("modifier_bottle_regeneration")`。
⇒ 「只有那张表漏了」不是一个待修的不一致,是**一个没人维护的死文件**的症状。

---

## 四 产物

| 文件 | 读数 |
|---|---|
| `tests/test_waitclar_callsite_empty.lua` | **9 tests / 0 failures** |
| `tools/agent/mutstand_waitclar_callsite.sh` | **7 变异全 CAUGHT + 控制 SURVIVED,exit 0** |

测试分节:§0 stripper 前提 · §1 **零调用点(= tripwire)** · §2 调用点是**被注释**不是不存在
· §3 flag 的唯一 writer 在死函数内 · §4 门也在死函数内 · §5 **四条反向调用**。

**§5 为什么必须有(本轮自查最该留下的一条)**:这个文件里**每一条断言都是关于源文本的**,
而源文本断言是**最容易写成永远不会红**的那一类 —— 一个拼错的 needle、一个把代码也吃掉的
stripper,**读出来都是「零个调用点」,都长得和这个发现一模一样**。
发现是「一个计数器读出零」;**反向调用是把它和「一个读不出来的计数器」分开的东西**。

**变异台 8 条腿**(每条都检查**是哪个用例抓住的**,不只看红绿 —— 0NEXT22 酉):
M1 调用点被恢复(**最要紧的一条**,没有它这个文件分不清「没人调用」和「我数不出调用」)·
M2 注释的调用点被删 · M3 第二个同 id 门(`pullcad` 形状)· M4 门被移出死函数(**计数不变,
只有位置断言能看见**)· M5 flag 多一个活 writer · M6 长括号注释打败 stripper ·
M7 stripper 的活控制被注释掉 · M8 **控制:纯注释编辑,必须 SURVIVED**。

**tripwire 的方向是双向的**,失败文案里写明了:§1/§3 哪天变红 = 调用点回来了 =
**好消息**,`waitclar` 变得可读、需要在活语料上重新定价、然后才可以提入集。

---

## 五 顺带:trunk 上两条红,都不是本轮引入(核对过)

- ✅ **已修**:`tests/test_waitclar_mana_trip.lua` `[corpus] every clarity carrier …`。
  **先证明不是我的**:把本轮对该文件的编辑 `git stash` 掉后重跑,**同一条红、同一句话**。
  语料增长把 clarity carriers **12 → 13**。**按 sweep 重新求值而不是放宽**:新增那个被
  **外层守卫**拦下(`clar_stop_outer` **2 → 3**),`flips` / `blocked_domain` **都仍是 1**,
  翻转帧仍是钉住的 medusa 帧 ⇒ **载体记账动了,杠杆的域没动**。修后 **19/19 绿**。
- ⛔ **未修,交出去**:`tests/test_tpscroll_branch_shadow_census.lua`
  `[corpus] the three priced levers are empty`(`撤退:2` trigger bound **30 → 31**),同因。
  **它不是一个 bump**:下面的断言要判 `撤退:2` 的豁免还成不成立,取决于新帧是否落在
  `[0.10, 0.18)`,**必须重跑并重读它自己的 sweep**。已写进 GH #838,不让它掉棒。

⚠️ **两条红都在快 Lua 闸之外**:`tools/agent/lua_gate_manifest.json` 里各自是
`in_gate: false / reason: "timed_out" / 6.0s`(超 per-test cap),也**都不在 `known_red` 里**
⇒ GH #624 的第三条腿**按设计**看不见它们。这是 cap 的**已知代价不是钩子的缺陷**,
但它正好解释了为什么一轮「长语料」可以把 trunk 留成红的,**而要等下一个开工的组才发现**。

---

## 六 诚实边界(本轮不声称的东西)

1. ⛔ **不声称 4.4 (i)**。`bots/mode_roam_generic.lua` 有 +24 行 / −0 行的 diff,
   **但全部是注释**(一段更正块,见 §七)。**没有行为改动** ⇒ 走 **4.4 (ii)**。
   把注释 diff 写成「bots/ 行为改动」正是 4.4 要倒过来的那个比例,所以这里明写。
2. **「引擎从来没调用过」是对 Lua 源的陈述,不是对引擎的陈述。** 证据是:全仓库 grep +
   去注释计数 + 上游快照。**没有**跑过一局游戏来确认。会推翻它的情形只有一种 ——
   引擎用别的机制(名字反射之类)调 `mode_*_generic` 里的自由函数;本仓库没有这种先例,
   `TinkerWaitInBaseAndHeal` 这个孪生函数就是**显式**被调的(`~126`)。
3. **`i <= 3` / bottle 那条候选没落地**,§三。本轮 `bots/` 的行为字节**一个没动**。
4. 语料本身这轮**没有重新扫**;13 这个数字是 sweep 自己打的,不是我数的。

---

## 七 改了什么

| 文件 | 性质 |
|---|---|
| `bots/mode_roam_generic.lua` | **注释**:2026-09-06 waitclar 块上方加一段更正(+24/−0) |
| `tests/test_waitclar_mana_trip.lua` | 抬头更正 + §五的 census 重新求值 |
| `tests/_waitclar_sweep.lua` | 抬头更正 |
| `tests/test_waitclar_callsite_empty.lua` | 新增 |
| `tools/agent/mutstand_waitclar_callsite.sh` | 新增 |
| `iterations/state.json` | 新增 `waitclar_callsite_empty_20260915` |

**三处抬头更正是有必要的,不是洁癖**:原文逐字把 `ConsiderWaitInBaseToHeal` 称作
**SHIPPED**。ungated 那一半是真的,**SHIPPED 那一半不是**。
一条注释把死代码说成出货代码,下一个读者会**原样继承**(这一轮之前已经继承了三处),
而且它正是把 6 帧读成「实际在打的行为」的那一步。

---

## 八 交棒

| 给谁 | 什么 |
|---|---|
| **总监** | 处置 `waitclar` = `DO-NOT-ADMIT (CALLSITE-EMPTY, constructive)`;调用点恢复前不进 armed 串(GH #838) |
| **总监 / owner** | 「要不要恢复 `~115` 的调用点」是一次**未 gated 的出货行为改动**(会把整个 "Heal in Base" 分支重新接上),超出协同组能顺手落的范围,需要拍板 + 单独取证 |
| **协同组(下一轮)** | `test_tpscroll_branch_shadow_census.lua` 的红(§五),以及 backlog `0NEXT23` |
| **录像组 / 批测台** | **仍欠** GH #837 / `queue.json:strategy-47` 那一帧(`siegecap` 用),本轮**没有重复提** |

⛔ **本轮不需要任何批测波次 —— 相反,它省掉了一波**(否则那一波会去测一个构造性的零)。

---

## 九 ⚠️ 本轮自己踩的两条,登记而不是隐去

### 9.1 铁律 6 第一次 push 被拒,而**那两条红是我自己造出来的并发**

第一次 `git push` 的钩子读数:

```
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate:  93 ran, 0 findings, 0 uncertifiable, 15.6s
lua gate: 390 ran, 2 findings, 0 uncertifiable, 9 unanswered, 6 known-red, 724.7s
PUSH1_EXIT=1   ← 被拒
```

两条 finding 是 `test_abil1st_first_unit_reader.lua` 与 `test_abilanc_ancient_selector.lua`,
失败文案逐字说清了病因:

> `bots/Customize/soak_side.lua` already exists … **Some other case — here, in one of the
> other test files that write this path, or in a concurrent lua5.1 process — armed a
> candidate and did not remove it.** Contents: `return { side = 'dire', cand = 'runecamp' }`

**那个 concurrent lua5.1 process 是我的。** 开工时我按自检脚本自己的指示用
`nohup … &` 把 `routine_selfcheck.sh` 放到后台(它拒绝 `timeout`、也拒绝管道,
**只肯**后台跑),它的 **Lua 腿**一直在写 `bots/Customize/soak_side.lua`;
而 `git push` 的钩子第三条腿(GH #624)**也**跑 Lua 测试。**一个全局 inode,两个进程。**

**核对过,不是真红**:串行重跑两个文件 → **16 tests / 0 failures** 与
**13 tests / 0 failures**,退出码都是 0;容器里**没有残留**的 `soak_side.lua`,
也没有残留进程。

⭐ **可迁移的那句(建议进铁律 10 或 #624 的 LIMITS)**:
**开工自检的 Lua 腿「必须后台跑」与 push 钩子的 Lua 腿「push 时自动跑」,
这两条各自都对,合起来是一个约束:一轮里这两件事不能重叠。**
自检脚本已经会拒绝 `timeout` 和管道(它知道自己怕什么),但它**不知道**钩子的存在;
钩子也不知道自检在后台。⇒ **实操规矩:push 前先确认自检已经结束**
(`ps aux | grep lua5.1` 为空),否则第一次 push 必被拒,代价是**一整轮 724.7s 的闸**。
本轮实测代价就是这个数。

### 9.2 ⛔ GH #290 的顺序我违反了:**先发的 issue,后 push 的产物**

铁律 6 的 #290 补充逐字要求:发表任何带引用的评论/裁定**前**,跑
`bash tools/agent/claim_precheck.sh <草稿文件>`,**`3 = 先 push 再发`**。

**我没跑,而且当时正好是 3**:GH #838 发出时,它引用的
`tests/test_waitclar_callsite_empty.lua` 和 `tools/agent/mutstand_waitclar_callsite.sh`
**只存在于容器里**;事后核对 `git rev-list --count origin/main..HEAD` = **1**,
正是 #290 描述的那个状态。窗口不长(push 紧接着就发起了,且第一次还被拒),
但**顺序就是顺序,#290 立案的那次也只是「四小时」**。
⇒ 登记在此,不在报告里把它抹平;下一轮开工判据(0NEXT23)不受影响,
但**本组下次开 issue 前先跑 `claim_precheck.sh`**。

### 9.3 之后每一次 push 的三行读数(闸**四连绿**,失败全在别处)

```
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)      ← 每次都是
py gate:  93 ran, 0 findings, 0 uncertifiable, ~15.5s     ← 每次都是
lua gate: 390 ran, 0 findings, 0 uncertifiable, 9 unanswered, 6 known-red
          724.7s(第1次,2 findings = 9.1 的并发)/ 718.5s / 706.3s / 711.5s / 703.9s
```

**落地**:分支 `claude/intelligent-hopper-s0ttue` ✅;`origin/main` ✅ `5f3b38c1..721c8339`。
⛔ **没有用过 `RULE6_BYPASS`** —— 本轮没有任何一行「SKIPPED, not passed」要抄。

### 9.4 ⚠️ 第三条,而它是**结构性**的:闸的时长 > main 的落地间隔

`HEAD:main` 连续**三次**被拒,**三次都不是闸的问题**(那三次 `lua gate` 全是 `0 findings`):

| 次 | 拒绝原文 | 闸耗时 |
|---|---|---|
| 1 | `non-fast-forward` | 724.7s |
| 2 | `cannot lock ref 'refs/heads/main': is at e64772d1 but expected 96e882b1` | 706.3s |
| 3 | `cannot lock ref …: is at 5f3b38c1 but expected e64772d1` | 711.5s |
| 4 | ✅ `5f3b38c1..721c8339  HEAD -> main` | 703.9s |

**形状**:钩子的 Lua 腿稳定 **~700s ≈ 12 分钟**,而今晚 main 的落地间隔实测
**2~15 分钟**(16:13 / 16:15 / 16:25 三连,之后又两次)⇒ **「pull --rebase 后再 push」
这条铁律 6 的重试路径,每一轮重试都要先付 12 分钟的闸,而那 12 分钟正是竞争窗口本身**
⇒ 成功与否接近抛硬币,**且每次失败的代价是固定的 12 分钟**,与改动大小无关。
本轮实测:**4 次 × ~12 分钟 ≈ 47 分钟纯闸时间**,其中 **3 次的产物被丢弃**。

⭐ **可迁移的那句**:**这不是「闸太慢」,是「闸的时长和共享 ref 的落地间隔是同一个量级」**。
两者各自都合理(#624 要的就是在 push 上拦红;五条流各自 2h 触发本来也不密),
**只有在 backfill 突发时它们才相撞**,而 backfill 恰恰是**每条流收尾时都会做的事**
⇒ **相撞集中在整点附近**,也就是所有流同时收尾的时刻。
📌 **给总监的建议(不是本组能定的)**:要么让钩子在 `HEAD:main` 这一步复用同一棵树上
刚跑完的读数(同 tree hash 不重跑),要么把「先 push 分支、main 由别的机制同步」写进铁律 6;
**本组这次的绕法只是运气**(第 4 次赶上 main 安静了 12 分钟),不是方法。
⚠️ 与 9.1 是**同一族**:两条各自正确的规则,在**时间维度**上互相拆台。

手跑读数(全部串行):`luacheck bots game --formatter plain` → **EXIT=0,0 警告**;
`lua5.1 tests/run_tests.lua waitclar` → **19 tests / 0 failures**;
`bash tools/agent/mutstand_waitclar_callsite.sh` → **exit 0,7 CAUGHT + 控制 SURVIVED**;
`python3 tools/agent/lua_gate.py` → **exit 0**(新测试在预算内跑到了,不在被排除的 9 个里)。

⚠️ 开工自检自己的结论是 **worst exit 3**(有发现,非本轮引入),其 Lua 腿即 9.1 的肇事方。

## 十 成本

**零 EC2 / 零 CE / S3 读取 0 个对象**(铁律 1 三段式)。

## 十一 Token 用量

```
TOKENS total_in=14,804,403 out=78,359 turns=97
```

*(统计时刻在最后一次 push 之后、本节补完之前;其后的收尾回合不计入,量级参考足够。
⚠️ 本轮 `total_in` 偏高的可查原因在 §9.4:**4 次 push × ~12 分钟闸**,
每次等待都要重读日志,而工作单元本身在第一次 push 前就已经完成
—— 中间读数 `total_in=7,742,772 / out=63,231 / turns=61` 就是那一刻。
⇒ **约一半的输入 token 花在等闸和重试上,不在工作单元上。**)*
