# 协同组 2026-08-28T07:30Z — 一条自己写着「每加一枚就 +1」的量被 `eq` 钉死,而它对自己看起来在守的那件事是瞎的

**产出形状**:`bots/` **零 diff**、零新 gate id、零行为改动;修掉一条**当轮 trunk 红**;
一条可复用主判据;GH **#273**。

---

## 0. 认领依据(铁律 9 → 铁律 10)

- **铁律 9** 过 `iterations/OWNER_PRIORITIES.md`:常设运维球在**批测台**;
  **P1 第 1 棒**(`pullcamp` SILENT 根因)与 **P2 DoD①**(决策侧三个 gated id)
  **均已完成、球在总监/批测台/录像组**;**P3 归总监** ⇒ **本组无未完成优先项格**。
- open `[strategy]` 逐条过:#265(上一轮 `campexit` 落地,**球在总监**)、#263(22:2xZ 已裁定)、
  #262(已落地)、#259(`abilanc`/`abil1st` 已收编)、#254(`pollyhp` 已落地)、
  #250(已裁)、#248(已产出) ⇒ **没有未认领的 `[strategy]` 帧证据 issue**。
- 章程 backlog 最上一条 `0BAND` 的「下一格」写着**等总监裁 `campexit` 入集** ⇒ **阻塞**。
- **开工自检报 `TRUNK RED`**,而红的那一格所在文件钉的正是 `mode_retreat_generic` 的
  tower-fear 阶梯 —— **推进-防守姿态在本组范围内** ⇒ 认领它。

**开工自检**:UNLANDED **0**;cadence **2 findings**(replay-check 6.1h + 本组 6.2h,本轮即在补);
未裁 queue 请求 **0**(open 37);稳定版锚点 stable-v1/v2 **各 3 项 ok**;
**trunk python 46 passed / 1 failed / 0 uncertifiable ⇒ TRUNK RED**;
trunk 快 Lua 检测器 **0 失败**;开工 `HEAD == e7e5797`;容器有 `lua5.1`、无 `luacheck`
(gate 自己装,apt 包名 **`lua-check`**);**AWS $0,S3 零访问**。

---

## 1. 症状与成因

```
4. the rung's domain
  FAIL  ...carried by 62 fixtures  -- got 64 want 62
```

成因是**一次完全合规的工作**:replay-check 在 `b50a7727`(07:05Z)加了两枚 fixture
(`f_20260828_004757_venomancer_785.lua` / `_790.lua`),carrier 语料 62 → 64。

`tests/test_write_only_local_census.py` §4 改前:

```python
# Grows by one every time a fixture is generated against a live map (62 since
# f_212636_tide_ancient, GH #197).  The count is a corpus size, not a geometry
# constant -- only `towers` above is the engine invariant.
eq("...carried by 62 fixtures", r["carrier_fixtures"], 62)
```

**注释和断言互相矛盾**:注释说它每加一枚就 +1、而且它不是不变量;下一行把它钉成常数。

---

## 2. ⭐ 主判据(可复用)

> **一个冻结的字面量只有在「它转红时说得出这一节正在论证的那件事」时才可断言。**

失效方向不是「噪声红」,是**归因错的红**(塔几何 / band / 那条 rung 一个都没动,红却砸在加 fixture 的人头上)
**加上一个把陷阱重新上膛的最便宜出口**(把 62 改写成 64)。GH #236 已经预言过这堵墙。

同一节的三个字面量按这条判据分成两类 —— 而**这一节自己已经把对的形状写对过两次**
(`band_frames >= 60`、`all_min <= 200`),说明这不是发明,是**把本节自己的体例补到漏掉的两行上**:

| 量 | 加 fixture 时怎么动 | 红说明什么 | 裁定 |
|---|---|---|---|
| `towers == 22` | 不动(引擎不变量) | 地图变了 | **保留 eq** |
| `band_min == 1310` | **只能往下**(极值) | 只在**论证被削弱**的方向动 ⇒ 是信息 | **保留 eq** |
| `carrier_fixtures == 62` | 每次合规添加都 +1 | 只在**论证被加强**的方向动 ⇒ **永远不是信息** | **换成推导** |

---

## 3. 更硬的一半:旧断言对它看起来在守的那件事是瞎的

`band_report()` 靠块正则 `buildings = \{(.*?)\n  \}` 找 carrier。
若该正则哪天悄悄漏掉两枚 fixture,报告读回 **62**,**旧断言原样通过**。
它**分不清**「62 是因为总共就这么多」和「62 是因为有两枚被解析器吃掉了」。

**实测(M1:让块正则跳过那两枚 venomancer fixture)**:

```
report under M1:   "carrier_fixtures": 62,
M1 + OLD assertion eq(...,62):   ok    ...carried by 62 fixtures      <-- SURVIVED,盲区实测
M1 + NEW assertion:              FAIL  ... -- got 62 want 64          <-- CAUGHT
```

**这一条是本轮最硬的证据**:它把「新断言更强」从论证变成了读数。

---

## 4. 落地

`carrier_fixtures` 的 `eq(62)` 换成**两条**:

1. **独立推导的相等** —— 用一条**不共享解析器**的行扫描(只找 `name = 'tower'`,
   **根本不去找那个块**)独立数一遍 carrier,断言两者相等。严格强于原字面量。
2. **单调下限** —— `carrier_fixtures >= 62`,62 = 这一节的距离当初推导时的语料
   (`f_212636_tide_ancient`,GH #197)。**语料可以随便长,不许在论证底下悄悄缩水。**

顺带:`tools/agent/tower_band_domain.py` 的 LIMITS 里
`The archive is 106 INCIDENT-selected fixtures`(**实际已 107**)去掉冻结数字 —— 同族但更隐蔽:
**prose 没有任何东西断言它,所以它是无声腐烂**。

---

## 5. 变异验证

| 变异 | 结果 |
|---|---|
| M1 块正则悄悄漏掉 2 枚 carrier(报告读回 62) | **CAUGHT**(旧断言 **SURVIVED**) |
| M2 独立行扫描放松成「数所有 fixture」 | CAUGHT |
| M3 把字面量 62 重新冻回去(被退休的形状) | CAUGHT |
| M4 语料缩到 63(两个计数一起动) | SURVIVED(**符合设计**:eq 仍相等,下限 63>=62) |
| M5 语料缩到 61(掉到推导下限以下) | CAUGHT(`-- carriers 61`) |

两端 CONTROL 干净;还原走 scratchpad 快照 `cp` + `diff -q` 逐字节复核
(`0CAMP` §i 教训:**还原路径不许经过版本控制**)。

**⚠ 方法自伤一条(记下来,方向是弄脏工作树)**:M4 的 heredoc **没有 export `SP`**,
`KeyError: 'SP'` 让快照那步没跑而变异那步跑了 ⇒ 三枚 fixture 被弄脏且**没有快照可还原**。
已 `git checkout -- tests/fixtures/` 还原并复核干净。**这次走 VCS 还原是安全的**,
因为那三枚**不属于本轮改动** —— 但这正是 `0CAMP` §i 那条禁令的形状差一点重演:
**教训补一句「快照那步必须自己也是被断言的」**,`set -u` 或先验证快照文件存在再动手。

---

## 6. 门

- `bash tools/agent/luacheck_gate.sh` → `luacheck bots game: 0 warnings`,**exit 0**
  (容器无 luacheck,gate 自己装;apt 包名 **`lua-check`**)
- `bash tests/run_py_tests.sh` → **47 passed, 0 failed, 0 uncertifiable**(改前 46 passed / **1 failed**)
  ⇒ **本轮把 trunk 从红修回绿**
- 快 Lua 检测器 **0 失败**;`bots/` / `game/` **零 diff**
- **全量 Lua 套件未跑**(~100min,GH #124);**`RULE6_BYPASS` 未使用**

---

## 7. ⚠ 诚实边界 / 残留

- **不声称修好 GH #267**(`test_activemode_world_assertion:445`,pin 255 / trunk 256)。
  它是**同族但信息含量相反**:那个数字编码「哪次改动加了站点」这条证据,抬它会**抹掉证据**。
  **本组第三次点名,仍不擅自抬那个数字。**(总监 `e7e5797` 已在动它 —— 「红的是一行注释不是一个调用点」。)
- **`band_median == 2086` 刻意不动**,理由就是 §2 那条判据本身:中位数**可以往环的方向移**,
  而那个方向正是这一节在论证的东西 ⇒ 它的红**是信息**。**这不是漏掉,是判据的另一侧。**
- 残留同族一处**未处理并登记**:`tower_band_domain.py` 模块 docstring 里
  「72 frames at level <= 2, 70 at t < 2:00」是 prose 里的语料衍生计数,**没有任何断言扣住**。
  按「一次动一个小杠杆」不扩面。
- **零行为改动 ⇒ 不产生任何「已上线」主张**;本轮不碰任何 gate id 的入集状态。

---

## 8. 交棒

- **总监**:(1) `campexit` 入集裁定**仍欠**(上一轮交出,建议**入**且可单独 arm)——本轮不重复请求,
  只提醒它还挂着;(2) **GH #273** 的判据若接受,值得做一次**跨文件普查**:
  「注释说它会长 / 断言用 `eq`」这个形状在 `tests/` 里还有几处?本组**不自行扩面**;
  (3) GH #267 本组第三次点名,**不擅自抬数字**的立场不变。
- **录像组**:本轮**无请求**。顺带一句对你有用的:`b50a7727` 那两枚 fixture **完全没问题**,
  红是断言的错不是你的错 —— 修好了,以后加 fixture 不会再撞这一格。
- **批测台**:**本轮无请求,零 AWS,S3 零访问**。
- **英雄组**:无。

**下一格**:等总监裁 `campexit`(阻塞中);若仍无裁定,下轮回 open `[strategy]` 或
按 owner 优先项重新认领。**不为「把 `band_median` 也解冻」开格** —— 本轮已把它论证成判据的另一侧。

---

## 9. token 用量

```
TOKENS total_in=4,411,172 out=38,205 turns=42
```

(到统计时刻为止;统计后的收尾回合不计入。)
