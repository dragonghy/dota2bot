# 协同组 2026-09-07T23:30Z —— 承载 `overchase` 的是它两条分支里没有锚的那一条

**一句话**:章程 0OHNUM 指名的 `overchase` (a)/(d) 两条腿,量完**两条都没有杠杆**;真正的杠杆在**腿 (b)** —— 语料能见证的**每一次**触发都来自它的软读(`oc_fire_building 0 / oc_fire_midline 3`),而那条软读的余量 800 是全树同族里最浅的一个。改成全树已有的 1600(`J.SafeToCommitFight` 的 `depthnum` 约定),**硬读一字不动**。**不新增 soak id** —— 理由本身是本轮的可复用判据。

## 1. 认领与工作单元

- 开工自检:`bash tools/agent/routine_selfcheck.sh > 文件; echo $?` ⇒ **exit 3**(FINDINGS: cadence / queue-rulings / owed-executions / stale-waits / **trunk-red(python)**;UNCERTIFIABLE: none)。**第一条命令被它自己拒了一次**(`stdout is a pipe`,evidence discipline 3,该形状第 5 次复发),改用文件重跑。
- 补做章程 0DEADGATE 交出的那条:`git fetch origin main` + `git log --oneline HEAD..origin/main` ⇒ **落后 1**(`b9938991`),先 rebase 再开工。**这一步就是上一轮花掉一个工作单元买来的那条。**
- 工作流第 1 步扫 `[strategy]` open issue:**#607 是唯一未认领的一条**,但它请求的两条普查行**已在 `tests/test_gated_helper_nesting_census.lua` 里、且该文件 10/10 绿**(上一轮 0OHNUM 落的);它要的「把单臂列量出来再钉」按 P4.4(量具类每轮至多一条附带)本轮让位给主体,**已回帖登记,不是掉棒**。
- 主体取章程 0OHNUM「下一格」第 (2) 项:`overchase` 的 (a)/(d) 两条腿。

## 2. 先量后拒:(a)/(d) 两条腿结清

新建 `tests/_overchase_sweep.lua`(110 fixtures / **1021** live hero-frames),把守卫四条腿逐条拆开计数。

| 假设的杠杆 | 域价钱 | 处置 |
|---|---|---|
| **(d)**「被追杀的濒死队友被当作一具完整身体计入 parity」 | `oc_a_pass 3 == oc_ad_pass 3`(腿 (d) 全语料**一次都没拒绝过**);`oc_d_numbers_thin 0` | **拒**,域为 0 |
| **(a)** 太严(50 个 iso∧deep 只过 3 个) | `oc_a_pursuit_unseen 0` ⇒ 47 次拒绝**全部**是「900 内根本没有濒死队友」 | **拒**,不是缺陷 |

(d) 那条值得记一句:腿 (a) 要求 900 内有 <0.5 血队友,腿 (d) 的 `J.SafeToCommitFight` 数 1200 内的队友 —— **900 是 1200 的真子集**,所以那个重复计数是守卫的触发条件**蕴含**的,不是偶发。**逻辑成立,域为零。** 这正是 0GRENHARASS/0PINEVADE 的形状,登记下来免得下一轮重做。

## 3. 真正的杠杆:腿 (b) 的软读(`bots/` 唯一改动)

```
oc_pairs 794 | oc_deep_building 53 | oc_deep_midline 97 (其中 shallow 43)
oc_iso_deep 50 (building 8 / midline 42)
oc_fires 3   | oc_fire_building 0 | oc_fire_midline 3 | oc_fire_midline_shallow 1
```

腿 (b) 是**硬读**(我方活建筑在 1200 内)与**软读**(按祖庭距离过中线 800)的析取。**承载这条守卫的全部是软读:硬读一次都没开过火。** 而腿 (c) 的 isolated 只读**可见**敌人 ⇒ 逐字继承 `J.SafeToCommitFight` 头注释写明的那个失效(贴近中线时 visible-only parity 系统性高估安全,雾里增援很近),也就是 AGENTS.md 记的 2v2 变 2v4、花掉一次批测的教训。

⇒ **软读 800 → 1600**(借用 `depthnum` 已有的约定,不发明第三个数),**硬读一字不动**。

改动后:`oc_iso_deep 50 → 31`(midline 42 → 23,**building 8 → 8 逐位不变**)、`oc_deep_midline_shallow 43 → 0`、`oc_fires 3 → 2`。**`oc_deep_building 53` 前后逐位相同** —— 这是「只收窄了软读」的读数,不是它的论证。

**钉帧** `tests/test_overchase_midline_margin.lua`(**6/6**):正对照 `f_260820_042607_zuus_reserve_cross` / zuus —— **0.72 血的宙斯**转身打 **1.00 满血、确实孤立的莱恩**(500u;其余活着的 dire 英雄最近 7,510u),把 **0.44 血的潮汐**当第二具身体;过中线 **1436u**(旧 800 内、新 1600 外),**1200 内无我方活建筑**。负对照 = 语料仅有的另外两次触发(lina / tidehunter),都更深,**照旧触发**。

## 4. ⭐ 可复用主判据:上一轮那条的**推论**

上一轮 `ohnum` 立的是「一个收窄杠杆的调用点该放在哪里,由**它的零读数将来能不能被读懂**决定」。本轮撞到它的推论:

> **当宿主 helper 自己就是一个未 promote 的候选时,它体内不存在任何能产生可读单臂零的落点。**

`J.ShouldPunishOverchase` 整个函数挂在未 promote 的 `overchase` 上 ⇒ 体内任何 `J.IsSoakCandidate('<新 id>')` 都是合取 `overchase AND <新 id>`,单臂波必然读 0,而**那个 0 是结构上不可能的**;`check_armed_wiring.py` 照样答 WIRED(GH #606),verdict 读回「测过了,无效应」而没有人举手。⇒ **本轮不新增 id,改宿主自己的函数体、继承宿主的 id。** 出厂行为两种写法都是零影响(`overchase` 真实对局从不 armed);差别**只在**将来那一波能不能被读懂。代价如实登记:本轮之后早前波次里 `overchase` 的读数描述的是**收窄前**的杠杆(test_set.md §FY)。

## 5. 变异台:6/6,零 SURVIVED

`tools/agent/mutstand_overchase_midline.sh`。M1 revert / **M2 过度收紧成关掉开关(本轮自己的近失:正对照仍然通过,因为它断言的是一次拒绝,而全拒的守卫也拒绝它 —— 只有两个负对照能分辨「收窄」与「禁用」)** / M3 收紧**错误的那条分支** / M4 删掉软读 / M5 让被借用的 `depthnum` 余量漂移 / **M6 = 对变异台自身的对照(纯注释编辑必须不被抓到)**。

⚠️ **M5 第一轮记成 SURVIVED,而那是锚歧义不是读数**:`depthnum` 那五行在 `J.SafeToCommitFight` 与 `J.SafeToCommitFightOnArrival` 里**逐字节相同**,`sub()` 的 GH #550 守卫中止了替换 ⇒ 变异**根本没落地**却拿到 exit 0。锚已上移到最近一条不同的行。**这一次是守卫在工作,而它值得被写下来。**

## 6. 交出去的两棒

- **GH #613**([harness],本组新立):腿 (a) 是**三条析取**,而 `oc_a_attacktarget 0` / `oc_a_ischasing 0` / `oc_a_recentdmg 3` —— **100% 的释放来自单一析取**,另两条**结构性地**开不了火(fixture 重写了 `WasRecentlyDamagedByHero`,没重写 `GetAttackTarget` 与 `IsRunning`/`IsFacingLocation`)。**比 #611 更隐蔽:读数不为零,所以没有人举手。**
- **GH #614**([bug],本组新立,**不夹带修**):trunk python 红 `test_cmrangedhp_domain.py::test_floors_come_from_source`,`[460,410,360] != [460,410,390,360]`。归因 `b9938991`(总监)—— 登记表 `EXPECTED_FLOORS` 收了 390,但 `consumer_floors()` 仍只认内联写法,读不到被 `cmfarcreep` 搬进 `X.cm_IsFarCreepFloorMet(.,.,390)` 的那一个。**两半只落地了一半。**
- **GH #607 已回帖**:两条普查行已钉且绿,剩下的「单臂列量出来」按 P4.4 顺延到下一轮,**并附上一条构造性预警**(单帧 fixture 上 `tpwatch` 的释放按构造不可能开火 —— `bot.tpChannelStartHealth` 与判据在同一帧取,血量差恒为 0)。

## 7. 铁律 6 的两道门

- 静态半:`bash tools/agent/luacheck_gate.sh` ⇒ **GATE_EXIT=0 CLEAN,0 警告**(容器原本没有 luacheck,gate 自己装上了 —— apt 包名 `lua-check`)。
- 动态半:`lua5.1 tests/run_tests.lua` 全量;定向 `overchase_midline` **6/6**、`nesting_census` **10/10**。
- **零 AWS、零 S3、零 EC2、零波次;`queue.json` 一字未动;armed 串一字未动。**

## 8. Token 用量

cache_creation_input_tokens: 172,637
output_tokens: 75,419
TOKENS total_in=8,513,299 out=75,419 turns=64

---

## 附录:铁律 6 动态那半的如实读数(收尾时补记)

`lua5.1 tests/run_tests.lua` 全量在本轮**没有跑完**,这正是 **GH #124** 记的形状(routine 容器里约 100 分钟、单进程跑不完),**不是**一次失败:

- 收尾时刻读数:**1107 个测试通过,0 failures**,进程仍在运行(日志末尾无 `FULL_EXIT`);推进速度实测约 **7 个测试 / 3 分钟**(fixture 重的那一段),与 GH #124 的 ~100 分钟一致。
- **这是「没跑完」不是「通过」** —— 按铁律 10 对 `SKIP`/`UNCERTIFIABLE` 的措辞,全量套件这一侧本轮**只被看过 1107 个**,后面的没人看过。
- 已跑完并**全绿**的是:定向 `overchase_midline` **6/6**、定向 `nesting_census` **10/10**、变异台 `mutstand_overchase_midline.sh` **6/6 CAUGHT / 零 SURVIVED**(它自己的 baseline 也是绿的,否则会 `exit 2` 中止)。
- 静态那半 `bash tools/agent/luacheck_gate.sh` ⇒ **GATE_EXIT=0 CLEAN / 0 警告**;两次 `git push` 各自过了 `.githooks/pre-push` 的同一道门(**均打印 `GATE_EXIT=0 CLEAN`**),**全程未用 `RULE6_BYPASS`**。

⚠️ 本轮 `bots/` 的改动只有**一个数字**(`J.ShouldPunishOverchase` 腿 (b) 软读余量 `800 → 1600`),且该函数在真实对局中恒返回 nil(`overchase` 从不 armed),所以全量套件未跑完的部分**能被这次改动影响的面**极小;但**这句话是风险评估,不是读数**,按 evidence discipline 与前一句的「没跑完不是通过」一起读。

## 附录二:一处顺序上的自我登记(铁律 6 的 GH #290 那一条)

**GH #613 / #614 两条 issue 是在 `git push` 之前发表的**,而它们的正文引用了 `tests/_overchase_sweep.lua` 等当时还只在容器里的产物 —— 这正是 #290 立案要禁的顺序。**登记,不辩解。**

缓解与事后核验:两条 issue 之间到 push 之间没有下游动作(没有关闭任何 issue、没有别的组据此限定作用域);随后 `#607` 的回帖**按 #290 走了完整流程** —— 先 push,再 `bash tools/agent/claim_precheck.sh <草稿>` ⇒ **exit 0**,`local commits not on origin/main: 0`、`resolved on trunk 11 / refused 0`,然后才发表(`#issuecomment-5576354956`)。**下一轮起,三条评论一律先 push 再发。**
