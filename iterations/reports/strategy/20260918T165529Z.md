# 协同组 2026-09-18T16:55Z — `pipetower`:我方战力那一栏里的塔是他们的,而这条契约的解释就写在同一个文件里

**一句话**:`item_pipe` 的「保护团队」分支问「我们这儿有没有第三具身体」,
而它加进**我方**求和的那张表是 `bot:GetNearbyTowers( 1200, true )` —— **敌方**的塔。
⛔ 两个方向同时错(站在敌塔边读成有后援、站在自家塔下读成什么都没有);
⭐⭐ **而「`GetNearbyTowers` 相对锚点」这条契约的解释,逐字写在同一个文件缺陷上方 4000 行处**,
是为另一个已落地的 id(`dusttower`,GH #441)写的。
本轮改的是那一个布尔实参,gated(`pipetower`,turbo-only),真实帧 fixture **10/10**,变异台 **8 抓 0 存活**。
**零 EC2 / 零 CE / S3 读取 0 个对象(出网未计价)**。

---

## 一、开工:铁律 10 再铁律 9

**自检 `EXIT=3`**(后台重定向起跑,`legs run 15`,全程 **~47 分钟**):

- `FINDINGS (exit 3)     : cadence queue-rulings owed-executions lua-coverage`
- `UNCERTIFIABLE (exit 2): trunk-red(python)`
- `NOT RUN (inside a leg): tests/test_lua_gate.py tests/test_luacheck_gate_soakswitch.py tests/test_selfcheck_lua_leg.py`
- `trunk health (fast Lua detectors)`:**`138 tagged detector file(s), 0 failures`**
- `lua-coverage`:`UNCOVERED 114 of 516 (22%)`,**本轮新增 2 条都不是本轮产物**
  (`test_fieldsip_transfer_receiving_site.lua` / `test_lion_w_fight_seed.lua` —— 后者是英雄组
  `-205` 刚刚证明「按构造不可达」的那一条)。

⚠️ **`trunk-red(python)` 这一行本轮读不出结论,而我自己单独跑的那次是绿的**:
自检自己那条腿打的是 *"a python test did NOT run (could not read its input)"*,
并且**自检自己的提示语就写着**「re-run on a quiet tree (nothing writing under bots/)」——
本轮 `bots/` 在自检期间**一个字节都没被碰**(见下),真正在写 `bots/Customize/soak_side.lua` 的是
**自检自己的 fast-Lua 腿**(GH #898 / #882 族,**归总监**)。
⇒ 我在**动 `bots/` 之前**独立跑了一次 `python3 tools/agent/py_gate.py`:
**`136 ran, 0 findings, 0 uncertifiable, 50.0s`**。
⛔ **这不等于「python 全绿」**:py 闸自己声明它**不管**那 17 个慢 python 测试,
而自检那条腿盖的正是那一侧 ⇒ **trunk 的那一侧本轮仍然没人看过**。

⭐ **0NEXT45/46 §(c) 那个排法本轮第三次照抄并且成立**:自检后台起跑 → 只做只读测量
(选择器普查 / 塔契约普查 / 语料 sweep / 证人挑选 / 写 `tests/` 与 `tools/`)→ **等它退出**
(⛔ `bots/` 一个字节都不碰)→ 才改 `bots/` → 变异台。三条腿的读数因此可以引用。

### ⚠️ 管道防呆第 20 次,以及 0NEXT46 那条**当场兑现**

0NEXT46 §三留的判据是:「凡用 `pgrep -f <字符串>` 判那东西还在不在,先问我这条命令行里有没有这个字符串」。
本轮**两次撞上它的两种形态**,而它们的代价不同:

1. **开工第一条命令** `bash tools/agent/routine_selfcheck.sh 2>&1 | tail -40` 被自检**自己拒绝**
   (`stdout is a pipe ⇒ exit 2, nothing checked`,证据纪律 3,它自己数着「已复发 5 次」);
   第二次 `timeout 900 …` 又被拒(`an ancestor process is timeout ⇒ 会被中途杀掉`,已复发 3 次)。
   ⭐ **两次都是被工具挡住而不是被我想起来**,而它们**都是免费的** —— 挡下来的成本是一次重跑。
2. 收尾清理时我用 `pgrep -fa "lua_gate|run_tests"` 确认没有残留进程,
   **它报回来一条命中,内容是那条 `pgrep` 自己的命令行** ⇒ 与 0NEXT46 记的**同一个错误**,
   只是这次是**读**而不是**等**,所以只花了一次困惑,没花一轮。
   📌 **那条判据本轮的净收益因此是正的,但它没能阻止我第二次写出同样的命令** ——
   ⭐ 可迁移的是更窄的一句:**`pgrep -f` 的结果要先减掉自己**(`pgrep -f X | grep -v $$`,
   或直接问 `ps -o ppid=` 的父子关系),而不是「记住别这么写」。

**认领依据(铁律 9)**:先读 `iterations/OWNER_PRIORITIES.md` ——
**P1(1)** 拉野 SILENT 根因球在录像组(#862 的复读空带,已停六天);
**P2** 决策侧仍卡在 `owed_executions.json:wandlimbo_charge_instrument`(dumper 侧 `main.go` +
fixture 侧 `replay_fixture.lua`,**两端都要**,且按 P4.4 **量具类不得作为工作单元主体**);
⇒ **4.4 (i) 球在本组且本组能动** ⇒ 取 4.4。
[strategy] open issue 里带帧证据且未落地的一条也没有(#887 / #889 / #891 / #893 / #896 / #900
都是本组自己已落地的登记;#818 / #862 在等别人)。

---

## 二、选杠杆:三条候选被**读默认值**读掉,第四条才是活的

0NEXT46 留的两根「下一轮可以直接做」的杠杆**本轮都还不能动**
(`J.GetAlliesNearLoc` 的主语不对称要等 `tormring` 裁定;`J.GetClosestCore` 要等 `corerole` 裁定),
所以本轮是**新找**。⭐ **0NEXT41 那条「先读输入的默认值,再去数域」本轮省掉了三整根杠杆的工作量**:

| 候选 | 形状 | 为什么不是本轮的杠杆 |
|---|---|---|
| `J.GetClosestUnit`(`jmz_func.lua:5667`) | 名字写「最近」、函数体 `return units[1]` —— **`helpnear` 的第三份拷贝** | 唯一生产调用点在 `hero_earth_spirit.lua:342`,**非焦点英雄** ⇒ 语料域 0,**不可测**。登记 |
| `J.ShouldWalkNotTp` 的 refuge 判据(**PROMOTED**,每局 turbo 都活) | 注释说「genuinely CLOSER safe point」,代码只问「1200 内有没有队友/塔/树」,**不问那个避难所在不在追兵那一侧** | ⛔ 先读默认值:`bChasing` 靠 `GetExtrapolatedLocation(0.5)`,而 `replay_fixture.lua:644` **返回当前位置** ⇒ 语料上 `bChasing` **恒 FALSE** ⇒ 整个谓词域 0。**不可测,登记**(仪器缺口,那段注释自己就写着「any domain count taken through it is a LOWER BOUND」) |
| `GetBestLastHitCreep` / `GetBestDenyCreep`(对线期,本组范围) | 「Best」返回的是**第一个**能击杀的兵 | 语料**一个小兵都没有**(`make_fixture.py` 只 dump 英雄与建筑,`grep npc_dota_creep tests/fixtures` = 0)⇒ **不可测,登记** |
| **`item_pipe` 的「保护团队」求和** | 一个布尔实参的**符号错** | **语料能测**:塔是 dump 真值(`nearby_structures`,69/112 份 fixture 带塔),英雄几何是真值,**而且持 pipe 的那一帧真的在语料里** ⇒ **本轮的杠杆** |

⭐ **第四条不是靠读代码找到的,是靠一个 20 行的扫描器**:全仓 `GetNearbyTowers` 的赋值,
凡把 `#<var>` 与另一个 `#<var>` **相加**的站点,逐个打印锚点与布尔实参。
全仓**只有 4 个**这样的站点,其中 **3 个是对的**
(`ability_item_usage_generic.lua:2035` 把 `true` 的结果加进**敌方**伤害;
`override_generic/mode_attack_generic.lua:118/145` 两半各自锚点正确),
**第 4 个是本轮这一个**。

---

## 三、缺陷:求和里那具「第三具身体」属于对面

`bots/ability_item_usage_generic.lua`,`X.ConsiderItemDesire["item_pipe"]`,出厂逐字:

```lua
local nNearbyAllyHeroes = J.GetNearbyHeroes(bot, 1200, false, BOT_MODE_NONE )
local nNearbyEnemyHeroes = J.GetNearbyHeroes(bot, 1600, true, BOT_MODE_NONE )
local nNearbyAllyTowers = bot:GetNearbyTowers( 1200, true )        -- :4068
if ( #nNearbyAllyHeroes >= 2 and #nNearbyEnemyHeroes >= 2 )
    or ( #nNearbyEnemyHeroes >= 2 and #nNearbyAllyHeroes + #nNearbyAllyTowers >= 2 )
then … BOT_ACTION_DESIRE_HIGH, '保护团队'
```

`bEnemies` **相对锚点**成立(`tests/test_ring_subject_census.py` §2 对着仪器断言了这条契约,
`replay_fixture.lua` 的复原实现是 `h:GetTeam() ~= self:GetTeam()`),**锚点就是 `bot`**
⇒ `true` 取到的是**敌方**的塔。于是:

- `#nNearbyAllyHeroes` 以**我**为心、**不装我** ⇒ `>= 2` 的意思是「我 + 至少两具身体」;
- 第二个析取项 `#allies + #towers >= 2` 的意思本该是「我 + 一个队友 + **一座为我们开火的塔**」;
- **实际是「我 + 一个队友 + 一座正在打我们的塔」** —— 求和把**对方的**火力
  记在**我们**那一栏。

⛔ **两个方向同时错,而错的方式互相遮蔽**:
站在**敌塔**边 ⇒ 出厂树以为有后援,按下一个 CD 三十秒的团队道具,**而我们恰好是弱的那一边**;
站在**自家塔**下被以少打多 ⇒ 出厂树**看不见**真的后援,于是这个分支**本来要抓的那一格它抓不到**。

### ⭐⭐ 本轮买到的可迁移句:**解释这条契约的注释,不是这个文件守住了它的证据**

`bots/ability_item_usage_generic.lua:70-73`(`DustDiveBlocked` 包装器)抬头**逐字**写着:

> *"GetNearbyTowers answers relative to the unit it is called on, so the shipped
> `enemyHero:GetNearbyTowers(700, true)` reads OUR towers and the guard is inverted
> in both directions"*

那是**已落地的** gated id `dusttower`(GH #441,英雄组)的理由。
⇒ **同一个文件既载着这条契约的完整解释,也载着它的第二处违例,两者相距 4000 行**;
而那段解释**定价的是 dust 分支**,`item_pipe` 的求和在它里面**一个字都没有**。

📌 **判据**:读到一段解释「某个引擎契约为什么容易被用错」的注释时,
⛔ **不要把它读成「这个文件已经守住了这条契约」** ——
去 **grep 这个契约在同一个文件里的其它调用点**。
⭐ 这是 0NEXT42(「注释写着某分支曾经数错 ⇒ 去数那个分支有几个调用点」)与
0NEXT45(「已登记 ≠ 已处理」)的**第三种形态,也是最便宜的一种**:
前两次要读别的文件、别的棘轮表,这一次**要读的东西就在同一个文件里**,
而它之所以没被读,是因为那段注释读起来像一份结案报告。

---

## 四、修法:一个布尔实参,gated

`J.GetBackupTowerCount( hBot, nRadius )`,放在 **`J.IsDustDiveBlocked` 正下方**
(`bots/FunLib/jmz_func.lua`)—— 同一条契约的两处违例并排放,下一个读的人看得见:

```lua
if J.IsModeTurbo() and J.IsSoakCandidate( 'pipetower' )
then
    return #hBot:GetNearbyTowers( nRadius, false )   -- 为我们开火的塔
end
return #hBot:GetNearbyTowers( nRadius, true )        -- 出厂表达式,逐字
```

调用点只剩一次委托,局部名改成 `nBackupTowerCount`
(出厂名 `nNearbyAllyTowers` 在 disarmed 时**是假的**,留着它等于把缺陷的名字当成修好的名字)。

**一次一个小杠杆**。**不碰**:半径 1200、阈值 `>= 2`、前面那条「保护 <40% 血队友」分支、
同文件另外 **18** 处 `GetNearbyTowers` 调用(逐条读过:把 `true` 的结果当**危险**读的那些方向是对的;
⭐ `:6517` `bot:GetNearbyTowers( 1200, false )` 是同一个问题**在同一个文件里已经写对**的那一份,
它同时是条件 (c) 的仓内佐证)。

### ⛔ 方向不是单向的,声明因此写窄

与 `helpself` / `tormring`(构造上单向)不同,与 `helpnear` 同型:它换的是那一项**数谁**,
所以两个方向都能走 —— SHAPE 列 `up 18 / down 12` 就是这件事的**读数**,不是噪声。
⇒ **没有单调性论证可倚**,能声明的只有窄的那句:
**一个「我们有没有第三具身体」的求和,只能加上为我们开火的单位。**
⚠️ 批测读数若反向,**不能**读成「这个杠杆让 bot 过度保守 / 过度激进」。

---

## 五、域:两种计数单位分开登记

`tests/_pipetower_sweep.lua`(手跑,零 arm,112 fixture / **1039** 活体帧):

```
P fixtures 112      P live 1039     P towers_seen 255   P pipes 4
LIVE   live_reached 4    live_up 0    live_down 1
SHAPE  shape_reached 1039  shape_etower 63  shape_atower 192
       shape_decides 244   shape_up 18      shape_down 12
       fixtures_with_a_change 11
```

⭐ **两种单位是按 0NEXT42 §三分开的,而这一次差别不是主语膨胀,是「分支能不能跑」**:
LIVE 只数**主体真的持有 pipe** 的帧(分支的前置就是 `bot:GetItemInSlot`),
SHAPE 数全部主体帧 —— 它回答的是「等语料能看到 pipe 存在的那个游戏分钟,这个分支会怎么做」。

⛔ **`pipes 4` 是仪器不是稀有度**:pipe 是中期物品,而本语料被 cap 在 10–25 游戏分钟
(GH #184 / #291)⇒ 全语料**只有 4 个**持 pipe 的主体帧,**而且四个都是同一个英雄(Lich)**。
⛔ **两列都不是发生率**:分支还要 `J.CanCastAbility`、要前面那条「<40% 血队友」出口落空。

📌 **这一格值得单独记一句**:`live_down 1` 长得像「域几乎为零、不值得发」——
而 `helpring`(同组 09-18 早,`up 1/1`、天花板 2)正是**以那个理由没发船的**。
⭐ **分得开两者的不是数字,是缺陷的种类**:`helpring` 是两个**半径**该不该统一(一个**选择**,
需要域来支撑「值得动」);这一条是**符号错**(一个单位的火力记在了错的一栏),
**它不需要域来证明它错** —— 这与 `bbancient`(域实测 **0**,58 个 ancient 快照全 hp=1.0,
仍然 gated 落地)是同一个判据。

---

## 六、本地验证:证人帧里 pipe 真的在手上,而且**出厂树真的按下去了**

`tests/test_pipetower_backup_tower.lua`,**10 tests / 0 failures**,0.31s,**零 stub**。

**翻转证人(LIVE)**:`f_260819_222559_od_eclipse_pair` / **lich(dire)**,t=631.5(10:31)。
- slot 2 **真是 `item_pipe`**(断言先验这一条,再验判词);
- 1200 内 **1 个**队友(Medusa **122u**);1600 内 **2 个**敌人(OD 396u / CM 902u);
- **一座天辉塔 407u**,而**自家塔在 4000u 内一座都没有**(断言写成 `GetNearbyTowers(4000,false) == 0`
  —— ⛔ 一个自家塔恰好停在 1300u 的帧会让测试通过而**声明弱得多**);
- ⇒ disarmed `1 + 1 = 2 >= 2` **为真**,发动 pipe,动机字符串 `'保护团队'`;
  armed `1 + 0 = 1 < 2` ⇒ **不发**。
- ⭐ **这一帧上这个分支是唯一能让它发动的路**:函数只有两条出口,前一条要一个 `< 0.4` 血的队友,
  而 Medusa 是 **227/230 = 98.7%** ⇒ 前一条为假。

⭐⭐ **一条不是我造的证据**:同一帧 Medusa 身上挂着
`modifier_item_pipe_barrier`(`remaining 7.0, elapsed 1.0`)与 `modifier_item_pipe_aura`
⇒ **出厂树在这一帧前约 1 秒真的按下了 pipe**。
⛔ **诚实边界**:按下的那一瞬是 t≈630.5,**我量的是 631.5** ⇒ 这条是**旁证不是归因**,
不能据此说「就是这个分支按的」(另一条出口在 630.5 的读数我没有帧)。
📌 但它买到的东西是真的:这个分支**不是**一条「理论上会走到」的分支,
**这个语料里它所在的那一格有人走过**。

**反方向证人(SHAPE)**:**同一份 fixture 的另一侧** —— crystal_maiden(radiant),
1 队友 / 2 敌人 / **敌塔 0、自家塔 1** ⇒ disarmed **不发**、armed **发**:
正是这个分支本来要抓、而出厂表达式看不见的那一格。
⭐ 两个证人同帧异侧,**顺带把 side gate 也驱动了**。

**对照**:juggernaut(radiant),1200 内两队的塔**都是 0** ⇒ armed 与 disarmed **同值**,
且理由与 gate 无关 —— 「§六的翻转不是 gate 在响」这句话因此有一个阳性对照。

**inertness 四条**:disarmed 逐字等于出厂表达式;armed **别的 id**(`tormring`)不串;
armed 但**非 turbo** 不动(⛔ 模式在 load **之后**、第一次读**之前**翻,因为 `J.IsModeTurbo` 首调即缓存,
并且先断言 `J.IsModeTurbo() == false` 免得一个失效的覆盖让真声明免费通过);
armed 在**另一侧**不动。

### 变异台:`tools/agent/mutstand_pipetower.sh` —— **8 抓 / 0 存活 / 控制绿 / 恢复 VERIFIED**

⚠️ **第一轮 8 抓 6、2 个「红了但消息不对」,而两个都不是变异体的问题,是我的 `want` 的问题**
(证据纪律 2:变异体存活先怀疑断言 —— 这里测试**确实抓到了**,只是抓它的是**更早**的那条断言):

- **M4**(armed 腿也读敌方 flag):我要的是判词行「armed still wants the pipe」,
  而实际先响的是 `nTower2 == 0` 那条计数断言 —— **计数排在判词前面**;
- **M8**(helper 长出自己的半径):我原来的变异体把 `nRadius, false` **删掉了**,
  于是先响的是「exactly one own-flag read, found 0」。
  ⇒ **M8 被换掉了**,不是换 `want`:新的 M8 在函数开头插一句
  `if nRadius > 1200 then nRadius = 1200 end`(**`J.GetAttackableWeakestUnit` 里就有这么一句**,
  所以这是个真实的变异),它**行为上是 no-op**、两个 flag 读数都完好
  ⇒ **只有那条 `not code:find('1200')` 的字面量钉子能看见它**。
  📌 **判据:一个变异体如果让「更早的断言」先响,它测的就不是你以为的那条钉子** ——
  改 `want` 只是把记分改对,**换变异体才是把钉子驱动出来**。

---

## 七、铁律 6 的门(三条腿,读数照抄)

- `bash tools/agent/luacheck_gate.sh` → **`GATE_EXIT=0 CLEAN`**,`luacheck bots game: 0 warnings`
  (改 `bots/` 之前也跑过一次基线:同样 `GATE_EXIT=0`);
- `python3 tools/agent/py_gate.py` → **`136 ran, 0 findings, 0 uncertifiable, 51.2s`**;
- `python3 tools/agent/lua_gate.py`:⚠️ **我单独跑它时它跑的是全集**,原因写在
  `.claude/rules/claude-code.md`(总监 RULING 69):它的 scope 是
  `git diff --name-only origin/main...HEAD`,**未提交的改动 diff 为空 ⇒ 空表按设计跑全集**
  (fail-closed)。⇒ **我停掉了那次独立运行**,把这条腿交给 **push 钩子**在**已提交**的树上按真 scope 跑;
  钩子的三行读数抄在 §九。
  ⛔ 同时这也是**不要反序推**的第二个理由的现场:`soak_side.lua` 是**一个全局 inode**,
  两个并发 `lua5.1` 进程会抢它(GH #229/#365)⇒ 我在跑变异台之前确认了
  `bots/Customize/` 只有 `general.lua` 与 `hero/`,**没有残留开关**。
- 定向跑过的普查:`test_smoke_load` 3/0、`test_gate_claim_consistency` 16/0、
  `test_gated_helper_nesting_census` 10/0、`test_activemode_call_site_census` 2/0。

**`tools/agent/lua_gate_manifest.json` 手加一行**(新测试若没有 manifest 行,
下一轮就会以 `no_manifest_row` 进 `lua-coverage` 的 UNCOVERED 集):
`tests/test_pipetower_backup_tower.lua`,本机 best-of-three **0.308s**(0.308/0.324/0.334)。
⭐ **英雄组 `-205` 那条教训本轮**先**用上了**:动这张表之前先算
`tests/test_lua_gate_budget_backstop.py` case 4 的 **2× 后备**,不是 `budget_seconds` 本身 ——
`2 × 267.436 = 534.872 ≤ 540.0` **过**(剩余余量 **2.564s**,不是 ~272s)。
`selected_count 351 → 352`、`selected_total_seconds 267.128 → 267.436` 一并重算。

---

## 八、登记(⛔ 未提入集,P4.2 冻结期)

- `iterations/state.json:pipetower_20260918` —— 缺陷 / 与 `dusttower` 的关系 / 修法 / 一个杠杆 /
  域(两种单位)/ 证人 / 方向 / 本地验证 / **本轮登记不修的两条**;
- `iterations/queue.json:strategy-64` —— **只要登记,零 EC2 / 零 CE / 不申请专波**
  (⚠️ GH #885 未修 ⇒ 这句话写在 `question` **开头**;⛔ 这是绕过不是修复);
  **预期裁定就是 FROZEN-HOLD**(armed 25 > 20),**读到 FROZEN-HOLD 不要当成掉棒**;
- GH issue:**push 之后才开**(GH #290:引用先可解析再发表),号码回填见本文件末尾与 §十。

---

## 九、push 与三条腿的读数

见本节末尾的回填(先推分支、再推 main —— **顺序是承重的**,`RULING 69`:
`git push origin HEAD:main` 会把本地 `origin/main` 挪到 HEAD,于是排在它后面的那一次 push
会同时吃到「lua 腿 scope 恒空 ⇒ 跑全集」和「rule6 memo 键恒 MISS」两笔钱)。

---

## 十、交棒 / 下一轮

(见章程 backlog **0NEXT47**。)

---

## 十一、成本

**零 EC2 / 零 CE / S3 读取 0 个对象(出网未计价)**
(本轮未 bootstrap AWS,未下载任何 `.dem` 或 `analysis.json`;按铁律 1 RULING 48 写成三段)。

## 十二、token 用量

(收尾时回填。)
