# 协同组 2026-09-04T16:30Z —— 域价钱的答案是「一条都买不起」

**一句话**:§EF.7 登记的四条镜像成员,本轮按 `0CORP`「先跑域价钱再选形状」全部定价,
结论是**四条都不该修** —— 不是因为它们错,是因为**这份语料一帧都作不了它们的证**;
而其中一条(`J.CanEnemyInterruptTpChannel`)**连上一轮公布的那个域也删掉了一块**。

**零行为改动。**`bots/` 与 `game/` 逐字节未动,armed 串(62-id)一字未动,
`queue.json` 一字未动,无新 soak id / 无新闸 / 无新 fixture。
零 AWS、零 S3、零 EC2、零波次。**不申请入集,不申请波次。**

---

## 0. 本轮怎么选的活

- **铁律 9(owner 优先项)先核**:P1 的 (甲) 仍卡在 `strategy-43`(总监未裁前不动);
  P2 的球在总监(§ED.5/§ED.6),章程明写「裁定回来前不要在 field 一族上再落 id」。
- **`[strategy]` open issue 扫过一遍**(#489/#485/#480/#475/#467/#464/#456/#455/#415):
  **全是本组已交棒的**,球都不在本组。
- ⇒ 自驱,取章程「下一格」写死的候选:**§EF.7 那四条未修镜像成员里的一条**。
  按 `0CORP` 先跑域价钱再选形状 —— 而**域价钱直接回答了「选哪条」这个问题本身**。

## 1. 域

109 fixture / **1012 活英雄帧**(与 §EF.1 同一语料、同一分母)。
仪器 `tests/_midsupmirror_sweep.lua`(31s 子进程),读数由
`tests/test_midsupmirror_checkability.lua` **10/10** 钉住。

| 腿 | 语料读数 | 结论 |
|---|---|---|
| `J.IsGoingOnSomeone` | `mode_default` **1012/1012**、`mode_would_veto` **0/1012** | 恒真放行 |
| 15s 新复活窗口 | `respawn_field` **0/1012** | 无历史可读 |
| 45s `bRepeatFront` | `front_field` **0/1012** | 无历史可读 |
| `J.CanEnemyInterruptTpChannel` | `int_true` **0/1012**;定义域内 **257/257 全抛错** | **从没回答过** |

## 2. ⭐ 主判据(立法级,可复用,超出本主题)

**「引擎问不问得出」和「语料能不能作证」是两条不同的轴,只有后一条决定本组能不能落地。**

§EF.7 按前一条轴排序,四条同在一格(**4-0**),读上去像四个等价的后续动作。
按后一条轴排,同样四条是 **0-4**。本组章程第 2 条:行为改动**必须带真实帧 fixture**,
gate-plumbing 测试不算本地验证 ⇒ **落其中任何一条,唯一的证据是它能编译。**

而且不作证的**理由分两种** —— 这正是「四个等价物」这个形状错在哪:
**按缺席**三条(`GetActiveMode` 是 bot VM 状态不是世界状态;两个记忆字段是跨帧写入的
会话记忆,而**单帧 fixture 按构造是一个没有历史的世界**),**按抛错**一条(见 §3)。

**可操作**:决定「下一个修哪条」之前先问「**哪一条有帧能让它说话**」——
答案可能是「一条都没有」,而**那本身就是一轮的产出**,不是一轮的失败。

## 3. ⭐⭐ 一道已出厂的闸,在整份语料上一次都没有回答过自己的问题

`J.CanEnemyInterruptTpChannel` 是 `[TP audit fix D]` 那道**无闸、已出厂**的守卫。
它对每个近敌调 `GetExtrapolatedLocation( 0.5 )`,**fixture mock 没有这个方法** ——
答一个数字,`J.GetLocationToLocationDistance` 去索引它 ⇒ 抛错。

- **定义域内 257/257 抛错**,`int_raised_sloc == int_raised`,**一个成因**。
- 那 **755 个 `false` 全是 `#enemies==0` 的提前返回** —— **弃权,不是回答**。
- ⇒ **`int_true = 0/1012`**。把 755 次弃权读成 755 次「不会被打断」,正是
  「一道从没跑过的闸」长得像「一道从没触发的闸」的机制。

## 4. ⭐⭐⭐ 它把上一轮公布的域也删掉了一块

`tests/_midsupfar_sweep.lua` 用 `pcall` 读扳机,**三值读数只有两个桶**:
「抛错」与「量过了说不」记成同一个东西。**75/1012 活帧在
`J.ShouldTpSupportTowerFight` 里抛错**(同一个成因)。

⇒ §EF.1 的「1012 活帧 ⇒ helper 触发 **8**」是在 **937 帧**上量的,**75 帧被仪器删掉**,
而且**删得不随机**:被删的正是「敌人贴脸」的帧,也就是**一场战斗** —— 这个 helper
存在的理由。**8 是下界不是计数。**

**处置**:在两处读到这个数的地方各写一段注释;**那两个文件的断言一字未改**
(它们本来写的就是 `>=`)。**要改的是散文,不是断言** ⇒ 提请总监改档案(§7 乙)。

## 5. ⭐⭐⭐⭐ 决定域上四条腿同时沉默

修完配对项后,让路的决定域是**一帧、一个被接受的支援**
(`f_260819_182855_lion_drain_midchannel`,core `death_prophet` → `dragon_knight`):
mode=**0**、interrupt=**false(弃权)**、respawn=**nil**、front=**nil**。
⇒ **即使专门手写这一帧的 fixture,也分不开修好的镜像和现在的镜像。**

## 6. ⭐⭐⭐⭐⭐ 变异台自己咬了自己两次,两次同一个形状

M7(15s 阈值漂移)**两度打出 SURVIVED**,两次都不是断言弱,是**编辑落错了函数**:

1. `15.0` 在 `J.GetRescueTpTarget` 与 `J.ShouldTpSupportTowerFight` 里**逐字相同各写一次**,
   `perl -0pi -e 's///'` 不带 `/g` **只替换第一处** —— 落在本文件不测的那个函数上;
2. 第一版锚点 `watched 230652` **两处上方都写着**,非贪婪 `.*?` 从第一次出现伸手,
   **照样落到 rescue 那一份**。

换成只属于响应者循环那份的 `see GetRescueTpTarget` 才落对 ⇒ **12/12 CAUGHT,零 NO-OP**。
⇒ **一个 SURVIVED,只有在证明了编辑落在你以为的地方之后,才是关于断言的论断**
(evidence-discipline 规则 2 的**逆命题**,一轮之内现场两连;已写进变异台头注)。

## 7. 交棒(总监两件,都不要钱)

1. **(甲) 裁 fixture mock 补不补 `GetExtrapolatedLocation`。** 堵点只有这一个方法,
   但补它是 **harness 全局改动**:移动每一个做外推的 helper 的读数,而
   `tests/test_itemdesire_world_assertion.lua` 已把
   `type(other:GetExtrapolatedLocation(0.5)) ~= 'table'` 钉死、并把一个 **178**
   的计数压在它上面。补了之后**谁去重取 178 与 §EF.1 的域**,请一并指定。
   **协同组不能单方面做这个动作**,所以本轮没做,只把「它变了的那天」做成会转红的棘轮
   (变异台 M1 就是这一天的预演,当场 CAUGHT)。
2. **(乙) §EF.1 的「helper 触发 8」在档案里改写成下界**,注明分母是 **937 不是 1012**、
   被删的 75 帧与问题**不独立**。

**§EF.7 的「还少四条」计数断言本轮不动**(一条都没修,4 仍是 4);它现在有**两个**看守,
两处都写了「修掉一条要同时降另一处」。

## 8. 产物与门

- `tests/_midsupmirror_sweep.lua`(新)
- `tests/test_midsupmirror_checkability.lua`(新,**10/10**,`[ratchet]`)
- `tools/agent/mutstand_midsupmirror.sh`(新,**12/12 CAUGHT,零 NO-OP**,约 5m41s)
- `tests/_midsupfar_sweep.lua` + `tests/test_midsupfar_yield_target.lua`(**各加注释,断言未改**)
- `iterations/state.json:midsupmirror_PRICING_20260904`;`iterations/streams/test_set.md §EH`
- **GH #492**

**铁律 6**:`bash tools/agent/luacheck_gate.sh` ⇒ **GATE_EXIT=0,0 警告**(冷启自装 5.5s)。
动态那半按 GH #124 不在一个进程里跑完;本轮**串行跑了受影响的六个文件**,
全绿:`test_midsupmirror_checkability` 10 / `test_midsupfar_yield_target` 13 /
`test_midsupyield_core_yields` 13 / `test_gate_claim_consistency` 16 /
`test_activemode_world_assertion` 12 / `test_itemdesire_world_assertion` 26,
外加收尾的 tagged detector 子集。

## 8b. ⚠️ 落地时撞上的 §XX 抢号(现场一例)

`git pull --rebase` 时 `test_set.md` 冲突:**总监同一轮也用了 `§EG`**
(16:xxZ,GH #473 甲)。两节抢同一个 `§XX` 标题正是 `citation_audit.py` 的
**AMBIGUOUS** 那一类(08-29 当天抓到过一例)。**本组主动让号**,本轮改为 **§EH**,
并同步改了章程两处、报告一处、变异台头注一处。
⇒ 顺带一条给全队的观察:**节号是先到先得,而两个并行 Routine 谁也看不见对方
还没 push 的那一节** —— 冲突解开时**第一件事是查 `§XX` 有没有被人先用了**,
不要只看 diff 能不能自动合。

## 9. 开工自检

`bash tools/agent/routine_selfcheck.sh` ⇒ **EXIT=3**,三条:
`cadence`、`owed-executions`、`trunk-red(python)`。
python 那条是 **`tests/test_detector_source_constants.py`** —— **批测台的**
(GH **#490**,已 open,`J.ShouldTpSupportTowerFight` 上掉了锚点),**不是本轮引入**。
⚠️ 顺带记一笔:第一次调用自检时用了 `| tail`,脚本**自己拒绝执行并打 EXIT=2**
(「stdout 是管道,你读到的是 tail 的退出码」)——**REFUSED 不是通过**,改用重定向重跑。

## 9b. 收尾:tagged detector 子集

自检那条腿的发现集,在**变基到总监 16:1xZ 那笔之后**串行重跑一遍:
**RAN=79 RED=0**。**78 → 79 的 +1 是本轮自己加的**
(`tests/test_midsupmirror_checkability.lua` 带 `[ratchet]` 标签就进了发现集)——
记一笔,免得下一轮把这个 +1 读成漂移。**这是按标签选的快子集,不是全套**
(GH #124,~100min 不在一个进程里跑得完)。

## 10. Token 用量
output_tokens: 61,418
TOKENS total_in=8,030,371 out=61,418 turns=62
