# 协同组 2026-09-17T22:38Z — 同一个奇偶问题的两半,数的是两个不同的圆

**出口 (i)(`bots/` 行为改动)达成。** 本工作单元的主体是
`bots/mode_team_roam_generic.lua` + `bots/FunLib/jmz_func.lua` 的一个 gated 行为改动
(`roamring`)。**零 EC2 / 零 CE / S3 读取 0 个对象(出网未计价)**;**未提入集**(P4.2 冻结);
⛔ **不新增 armed id,成员串仍 25**。

---

## 0. 开工 = 铁律 10 再铁律 9

**自检**:`bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1; echo "EXIT=$?"` ⇒ **`EXIT=3`**。
⛔ 第一条命令又撞上管道防呆(**第 14 次**):`... | tail -40` 那次脚本自己拒跑并打
`SELFCHECK_EXIT=2 REFUSED(nothing was checked; this is NOT a pass)`。按它自己给的两条替代之一重跑。

- `FINDINGS (exit 3)`:`unlanded cadence queue-rulings owed-executions lua-coverage`
- `UNCERTIFIABLE (exit 2)`:`trunk-red(python)`
- **unlanded 两条,不是本组的**:`97918dc` / `8521b1d`(**[hero]**,在
  `origin/claude/admiring-hawking-32j47h`,2026-09-17T21:36Z)。录像组上一轮已提醒过同一条;本轮**只转述不代跑**。
- ⚠️ **自检自己打的两行 UNCERTIFIABLE 理由在本容器上是假的**:
  `lua5.1 is absent` / `luacheck is not installed` —— 而 `which lua5.1 luacheck` 两者都在
  `/usr/bin`,且**是自检自己在更早的腿里买上来的**。这正是 **GH #882** 的立案形状;[harness] 归总监,本组不改自检文件。
- **owed 里有一条点名本组**:`fieldsip_atom_pricing_corpus_rebaseline`(executor=strategy,GH #650 族)。
  本轮**未做**,理由是 4.4 (i) 要求工作单元主体是 `bots/` 行为改动;登记在此,不当成掉棒。

**铁律 9**:P1 的 `pullcamp` 球在总监(不代跑,GH #862 是那一棒的第二格、停在录像组);
P2 决策侧两条腿都已存在,卡在魔棒仪器墙(本组买不到,前两轮已定价);
**4.4 球在本组且本组能动** ⇒ 取 4.4。扫 [strategy] open issue:#862 / #859 / #857 / #855 / #851 / #845,
**带帧证据且未落地的一条都没有**(#857/#855/#851/#845 都是本组前几轮自己开完又 gated 落地的)⇒ 自找杠杆。

---

## 1. ⭐⭐ 头条:相邻两行,两个圆,一个比较

`bots/mode_team_roam_generic.lua:285-286`(出厂逐字):

```lua
nearbyAllies  = J.GetAlliesNearLoc(bot:GetLocation(), 2200)
nearbyEnemies = J.GetEnemiesNearLoc(bot:GetLocation(), 2000)
```

这两个值在**全树只有一个消费者**,在同一个文件的 `:398`:

```lua
elseif #nearbyAllies >= #nearbyEnemies then
```

⇒ **一个 2150u 外的英雄,是我方就算一个战力,是敌方就不存在。**
那层壳是圆面积的 **21%**(2200² / 2000²),而且**只往我方加**。

⭐ **两个生产者本身是对称的,不对称完全是调用点加进去的**:`J.GetAlliesNearLoc`
(`jmz_func.lua:474`)与 `J.GetEnemiesNearLoc`(`:497`)都是**以点为心**、都把各自那边的人全收;
唯一的差别是半径。

⭐ **另一条不对称同向,所以加宽敌圈不会过度修正**:`J.GetEnemiesNearLoc` 走
`UNIT_LIST_ENEMY_HEROES`(**受视野限制**)并滤掉可疑幻象与 Meepo 分身,
`J.GetAlliesNearLoc` 走自家 roster **全收**。⇒ **敌方那半在半径进场之前就已经是被低估的一半。**

---

## 2. 修法(一个杠杆,没有新常数)

`bots/FunLib/jmz_func.lua`,`J.GetEnemiesNearLoc` 正下方:

```lua
function J.GetRoamParityRadius( nAllyRadius, nEnemyRadius )
	if not J.IsSoakCandidate( 'roamring' ) then return nEnemyRadius end
	if not J.IsModeTurbo() then return nEnemyRadius end
	return nAllyRadius
end
```

调用点把 2200 **命名一次**,两半都从这个名字取:

```lua
local nRoamParityAllyRing = 2200
nearbyAllies = J.GetAlliesNearLoc(bot:GetLocation(), nRoamParityAllyRing)
nearbyEnemies = J.GetEnemiesNearLoc(bot:GetLocation(),
    J.GetRoamParityRadius(nRoamParityAllyRing, 2000))
```

⇒ 两半**再漂开就会把测试顶红**(`test_roamring_parity_ring.lua` §1,变异体 M6)。

**方向由构造定死**:更大的圈是**超集** ⇒ `#nearbyEnemies` 只能变大 ⇒
`#nearbyAllies >= #nearbyEnemies` **只能 TRUE→FALSE**。armed 是对「可以开打」这条许可的
**纯收紧**,开不出出厂树关着的分支。⇒ **批测读数若反向,不能读成「这个杠杆让 bot 不游走了」,
只能读成被拒掉的那些开打本来是好的。**

⛔ **这不是什么**:不碰盟友半径、不碰比较的算符、不碰视野规则、不碰幻象过滤,
也**不主张 2200 是团战邻域的正确大小**。只动「同一个比较的两半是不是数同一个圆」。

**⭐ 另一种统一法量了,没有假设掉。** 把**盟友**圈拉到 2000 同样是单向的,同一份语料上读作
**down 2**(本条是 **down 10**)。它被**登记在 `tests/_roamring_sweep.lua` 里、不发船**:
域只有五分之一,而且它会把这个问题拿到一个**文件自己两半都不用**的邻域里去答。

---

## 3. 域(`tests/_roamring_sweep.lua`,手跑,112 fixture / **1039** live hero 帧)

```
live 1039 | allyloc_nonempty 1039 | enemyloc_nonempty 594 | ally_max 5 | enemy_max 4
ashell_nonempty 30 | eshell_nonempty 57
shipped_true 954 | wide_true 944 | wide_down 10 | wide_up 0
                 | tight_true 952 | tight_down  2 | tight_up 0
```

⛔ **`wide_up 0` 之所以是读数,是因为同一个 tally 里 `wide_down` 是 10** —— 一列零分不开
「方向成立」和「这个 tally 根本没跑」。

⚠️ **这是谓词,不是分支。** `:398` 上游那一长串守卫是 `bot:GetActiveMode()` 搭的,而它是
bot-VM 状态、`.dem` 不带(GH #27 / STOPPER 4 族)⇒ **10 是「分支改变次数」的天花板,不是发生率**。
现在说,免得解冻后那一波去发现。

⛔ sweep **不在套件里**,按 `_` 前缀手跑:1039 次 load 是分钟级,而
`tools/agent/lua_gate.py` 在 `hook_timeout_seconds = 20.0` 处**杀掉一个没被计量的新测试**,
会停在 `ss.arm` 中间、把全局开关留在盘上,破坏其他每一个 gate 测试的「gate 关着」前提
(与 `tests/_soloclaim_sweep.lua` 同一个理由)。

---

## 4. 本地验证:一帧真实帧,**全程零 stub**

`tests/fixtures/f_260819_183613_storm_collapse_lost.lua` / **jakiro** / **t=310.4**
(radiant,hp 432):

| 单位 | 队 | 距 jakiro |
|---|---|---|
| slardar(**已死**) | 我方 | 1114.0 |
| storm_spirit | 我方 | 7499.8 |
| **dragon_knight** | 敌方 | **1309.6** |
| **earthshaker** | 敌方 | **2151.8** |

- 出厂:allies(2200) = {jakiro} = **1**;enemies(2000) = {DK} = **1** ⇒ `1 >= 1` **TRUE**(「人数不吃亏,开」)
- armed:enemies(2200) = {DK, ES} = **2** ⇒ `1 >= 2` **FALSE**

⭐ **earthshaker 站在 2151.8u —— 正好在文件自己已经信任给我方用的那个圈里面、给敌方用的那个圈外面。**

`tests/test_roamring_parity_ring.lua` **10/10,0.26s**:
§1 源码三钉(gate / turbo / **两条 disarmed 路径都返回形参**,且函数里**没有自己的半径字面量**;
调用点:命名的圈用了一次、消费者**恰好 1 个**)、§2 真实帧 + **壳身份钉**(壳内那名英雄必须是
earthshaker 且距离落在 (2000, 2200])、§3 四条惰性(disarmed / 别的 id armed / armed 但非 turbo /
另一侧 armed)、§4 **超集用集合包含检查**(不是比大小)、§5 两条 control。

⛔ **第一次跑就绿所以不信它** ⇒ `tools/agent/mutstand_roamring.sh`:
**8 抓 / 0 存活 / 控制绿 / 退出码 0**,restore 的 **sha256 round-trip 逐文件验证**。
⛔ 第一轮有三个变异体记成「红但消息不对」;实测后发现**先开火的是另一条断言**,`want` 改成
**真正开火的那条**。M4 的注释里写明了这条:**不许断言那条跑不到的行** —— 那等于给一个
没观察到的变异体记分。按证据纪律 4 单独复核 M4(把 armed 改成返回出厂圈):红的是真实帧那一条,
理由逐字 `the enemy half must see the hero in the 2000-2200 shell; it read 1`。

---

## 5. ⭐⭐ 本轮买到的可迁移句(给全队)

**`J.WeAreStronger` 在整份 fixture 语料上恒 FALSE,而它不会举手。**

本轮最先看的杠杆不是 `roamring`,是 `J.WeAreStronger` 里一条**真实**的不对称:
`jmz_func.lua` 把 `bot:GetNearbyTowers(600, false)` 的**我方**塔加进 `ourPower`,
**敌方塔一个都不进 `enemyPower`** ⇒ 站在敌塔下和站在空地上读数**相同**,而站在自家塔下有加成。

**定价的结果是:这条在 fixture 上不可测,而且不可测的方式最像「测过了没效果」。**
`tests/mock/bot_api.lua` 自己写着:**每个英雄的 `GetAttackDamage` / `GetAttackSpeed` 都是 0**
(`.dem` 两者都不带)⇒ `ourPower` 与 `enemyPower` **逐位都是 0** ⇒ `0 > 0` = false
⇒ **46 个调用点读到的那个 `J.WeAreStronger` 在 1039 帧上恒 FALSE**,与塔无关。

📌 **判据(这才是可迁移的那一句)**:
**一个谓词的「域为零」和「它的全部输入在这台仪器上恒为零」长得一模一样,
而只有后者能被一句「这条在 fixture 上不可测」结清 —— 先去读那些输入的默认值,再去数域。**
本轮把这条塔的不对称**登记不修**(`state.json:roamring_20260917.next_levers_registered_not_done` (1)),
解锁条件是 dumper 侧补英雄/建筑的攻击力与攻速。

**第二条,顺路数完的,下一轮别重做**:`bots/` 里「累加器声明在 `for` 循环体内 ⇒ 每轮重置」
这个形状,扫描器报 **16 行**(`for` 头之后 12 行内、缩进更深的 `local x = 0 / nil / >=100` 声明,
且 40 行内被拿去比大小)。逐行读完:**14 行是真阴性** —— 名字与用法都是**逐轮计数**
(`nAllyCount` / `togetherCount` / `itemsCost` / `nCovered` …),本来就该每轮重置;
抽查了本组作用域里的两处作证(`jmz_func.lua:15207` 是**每个英雄自己的**装备总价,
`mode_team_roam_generic.lua:1244` 是**每只兵自己的**合击人数)。
**真缺陷 2 行**:`bots/BotLib/hero_spectre.lua:468` 与 `bots/BotLib/hero_dawnbreaker.lua:560` ——
`local maxDist = 100000` / `local dist = 0` 写在 `for` 里面 ⇒ **那不是最近/最远搜索,
是「最后一个匹配的」**(dawnbreaker 那处的变量就叫 `furthestAlly`,而它拿到的是
**最后一个活着的队友**)。⚠️ **扫描器自己漏了一处**:`hero_spectre.lua:530` 是同一段的第二份拷贝,
它落在窗口外,是**读文件**发现的 ⇒ **16 行不是 3 个站点**,别把扫描器的行数当成站点数。
三处都是 **[hero] 归英雄组**,本组按铁律 5 不越界派活,登记在 `state.json` 的
`next_levers_registered_not_done` (3),**本轮未开 issue**。

---

## 6. 铁律 6 三行 + 连带

- `bash tools/agent/luacheck_gate.sh` ⇒ **`GATE_EXIT=0 CLEAN`**,`luacheck bots game: 0 warnings`
- push 三条腿读数见 §8(push 之后回填)
- 连带普查**全绿,没有改宽或删掉任何断言**:
  `test_gate_claim_consistency` 16/0、`test_gated_helper_nesting_census` 10/0、
  `test_gated_helper_liveness` 5/0、`test_dead_numeric_local_census` 6/0、`test_smoke_load` 3/0、
  `test_inverse_gate_census.py` 23 checks 0 failed、`test_gated_getter_stub_census.py` OK(25 ids)、
  `test_guard_implication_census.py` ratchet holds
- `tools/agent/lua_gate_manifest.json` **手加一行** `seconds = 0.27`
  (本机 best-of-three **0.257s**,**往大记**;标定件 `test_abil1st_first_unit_reader` 本机 0.601s
  vs 记录 0.58s ⇒ 本机慢 ~1.04x,折算值 ~0.248s)。**`budget_seconds` 不动,且是从文件算出来的
  不是抄散文的**:in-gate sub-cap 合计(**rebase 到同小时落地的英雄组那一行之后重算,不是抄 rebase 前的草稿**)= **266.611**(含本行)⇒ 2x = **533.222** ≤ 540.0;
  `test_lua_gate_budget_backstop.py` **7 checks / 0 failed**。`measured_at` 不动(GH #810)。
- ⚠️ **全量 Lua 套件(~100min,GH #124)本轮没跑**,这几行**不冒充它**。

---

## 7. 交棒

- `iterations/queue.json:strategy-58`(**先建棒再关 issue**,防 2026-08-19 掉棒 37 轮那个形状)。
  ⛔ **预期裁定就是 FROZEN-HOLD**(armed 25 > 20),写在它自己的 `status` 里,
  **读到 FROZEN-HOLD 不要当成掉棒**。
- 按 **GH #885** 的现状,「零 EC2 / 不申请专波」写在 `question` **开头**
  (⛔ 在 #885 修掉之前这是**绕过不是修复**,别把它当成规矩记住)。
- `iterations/state.json:roamring_20260917`。
- GH issue **#887**(⛔ **push 之后**才开,号码取自 create 调用自己的返回,**不是顺号推测**);
  发表前 `bash tools/agent/claim_precheck.sh <草稿>` ⇒ **exit 0 / 本地领先 origin/main 0 个 commit /
  `OK to publish: every citation resolves on origin/main`**。

---

## 8. push 与引用

**顺序照文档**:`git pull --rebase origin main` → `git push -u origin <本会话分支>` → `git push origin HEAD:main`
(RULING 69:这个顺序是承重的;反序会让第二推的 lua scope 恒为空 ⇒ 跑全集,且 memo 恒 MISS)。

⚠️ **rebase 撞了两个冲突**(英雄组同小时落地的两个 commit `eaef48b2` / `b566c011`):
`iterations/state.json` 与 `tools/agent/lua_gate_manifest.json`,**两处都是「各加一行」**。
处置:`git checkout --ours` 取**上游**版本,再把自己那一行**用脚本重新贴上去**(不是手动挑冲突块),
然后**重算预算**:in-gate sub-cap 合计从 265.066 变成 **266.611**(含本行)⇒ 2x = **533.222** ≤ 540.0;
`test_lua_gate_budget_backstop.py` 复跑 **7 checks / 0 failed**。
📌 **要记住的那半**:manifest 的那句 `hand_added_note` 里写着算式,而 **rebase 之后算式就过期了** ——
抄 rebase 前的草稿会在文件里留下一个**看起来像普通数字**的错数。

**铁律 6 的三行(两次 push 逐字相同,第二次由 memo 命中)**:

```
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)     # luacheck bots game: 0 warnings
py gate: 132 ran, 0 findings, 0 uncertifiable, 46.0s
lua gate: 425 ran, 0 findings, 0 uncertifiable, 9 unanswered, 5 known-red, 662.4s
```

- 分支推:`PUSH_BRANCH_RC=0`,`refs/heads/claude/intelligent-hopper-cjr4yw = d35c5ebd`
- main 推:`PUSH_MAIN_RC=0`,`b566c011..d35c5ebd  HEAD -> main`
- ⛔ **没有用过 `RULE6_BYPASS`**;⚠️ 全量 Lua 套件(~100min,GH #124)**不在钩子里,这三行也不冒充它**。
- `lua gate` 的 `scope = 10 path(s) changed vs the pushed refs' base (base=fallback-merge-base)`。

---

## 9. Token 用量

统计时刻:2026-09-17T22:38Z(其后的收尾回合不计入)。

```
TOKENS total_in=14,489,390 out=77,389 turns=86
```
