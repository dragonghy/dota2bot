# 协同组 2026-08-31T16:36Z — Pudge 块的连续攻击命令**由一个它不读的变量守卫**:`local` 遮蔽的作用域随块结束,命令排在那个 `end` 之下 —— 落地 gated `rotscope`,并补掉一个让它对全仓不可见的量具洞

**一句话**:`bots/mode_roam_generic.lua` 的 Pudge 块里,`if Rot:GetToggleState()` 块内的
`local botTarget` 遮蔽了文件级同名变量,**遮蔽随块结束**,而
`bot:ActionQueue_AttackUnit(botTarget, false)` 就排在那个 `end` 之下 ⇒
四行之上的 `J.IsValidTarget(...) and dist > 400` **守的是另一个变量**;
真实帧上出厂代码把 `ActionQueue_AttackUnit(nil, false)` 下成了**整个
`ThinkIndividualRoaming` 唯一的一条命令**,而**全仓没有一个测试能看见它**——
`record_actions` 直到本轮都没挂 `ActionQueue_*` 攻击钩子。

```
LAND id=rotscope  gated=turbo+IsSoakCandidate  frame=f_113203_pudge_homeroute_silent
TESTS rotscope_shadowed_target 13/13 [ratchet] | propertarget_corpus_domain 4/4 (untagged, 29s)
MUTATIONS 10/10 CAUGHT   GATE luacheck BARE_EXIT=0, 0 warnings
```

---

## 0. 本轮的边界

- **零 AWS 花费**;S3 **零访问**;EC2 零;CE 零;`iterations/queue.json` **一字未动**。
- `bots/` 改动**只有** `mode_roam_generic.lua` 的 Pudge 块,**全部在 `rotscope` 门之后**;
  `game/` **零改动**;成员串一字未动。
- 新 gate id **1 个**(`rotscope`),已登记 `state.json:rotscope_20260831`,
  入集提议写在 `test_set.md §CU`(**搭车、零 AWS 增量、不申请专波**)。
- 认领依据:`[strategy]` 未认领 issue 为零(open 的 9 条 `[strategy]` 全是本组自己开的),
  owner P1 第 1 棒早已交出、P2 上一轮已交棒 ⇒ 取章程 backlog **`0d`** 里明写
  「**还没查的**:其余 mode 文件里的 `Action_AttackUnit(x, false)` / `Action_MoveToUnit`」那一条。

---

## 1. ⭐ 主判据(可复用,超出本主题):**一个 `local` 的遮蔽作用域随它的块结束,而守卫是按名字读的**

```lua
if botName == 'npc_dota_hero_pudge' then
    local Rot = bot:GetAbilityByName('pudge_rot')
    if Rot:GetToggleState()
    then
        local botTarget = J.GetProperTarget(bot)          -- 遮蔽开始
        if J.IsValidTarget(botTarget) and GetUnitToUnitDistance(bot, botTarget) > 400
        then
            bot:Action_MoveToLocation(botTarget:GetLocation())
            return
        end
    end                                                    -- 遮蔽结束
    bot:ActionQueue_AttackUnit(botTarget, false)           -- 读的是文件级那个
end
```

文件级 `botTarget` 声明在文件头的 `local nInRangeEnemy, ..., botTarget, ...` 里,
**只在 `GetDesireHelper` 内被赋值一次**。位置证明已钉成断言(`[source S1]`):
遮蔽行、守卫行、**中间那个 `end`**、消费行,四者的相对次序由源码解析得出,不由记忆得出。

**一般形**:**守卫与被守卫者同名,不等于同一个变量。** 凡是「守卫在 `if` 块内、
消费在块外」的形状,守卫对消费点**零信息**——而它读起来像有信息,因为名字一样。
(这与 GH #348 的 `hero_sniper` 同族但**不同因**:那里是「判 nil 的前一行就索引了」,
是**顺序**;这里是**作用域**,两个变量。)

## 2. ⭐⭐ 三条互相独立的后果,每一条都单独钉住

| # | 后果 | 断言 |
|---|---|---|
| 1 | **无条件** —— 命令在 Rot 块**之外**,Rot 开不开、目标有效无效、任何距离都发 | `[drive]` 两条:toggle 喂 `0`(mock 默认,**在 Lua 里是真**)与喂 `false`,出厂**都**发那一条 |
| 2 | **连续型** —— `bOnce=false`,`roamreach`(GH #45)存在的理由那一形 | `[source S2]`:本文件每条 `ActionQueue_AttackUnit` 都断言 `, false)` |
| 3 | **可陈旧** —— 文件级赋值排在「无敌/死亡/幻象/非英雄」早返回**之下** | `[source S4]`:早返回行号 < 赋值行号,从树里解析 |

第 3 条把它接回 `roamstale`(GH #39,**已 promote,stable-v1**):**同一条病,第二个文件**。
第 2 条把它接回 `roamreach`:一条连续命令,在它的 mode 不再赢下竞价之后**没有人**会再评估它,
而唯一的释放就写在那个不再被调用的 `Think` 里。

## 3. ⭐⭐⭐ 为什么它**不能**吃 §CR.2 的豁免 —— 判据是差集,而这里的差集越界

§CR.2(总监 04:xxZ 立)给 **ABORT-CONTAINED** 类修复免掉 `gated-fix`,条件 **(乙)**:
**兜底分支的触发条件 ⊆ 出厂代码的抛错条件**。

本修复**过不了 (乙)**:armed 同时压掉了「**Rot 没开**、而文件级句柄是一个完全有效
(可能陈旧)的英雄」那一类帧上的命令 —— **那不是抛错帧**。
`nil` 那一半也许是 abort-contained;**「无条件」那一半可证不是**。
而 §CR.2 明写「豁免管的是**差集**,不管动机」⇒ **本条走 `gated-fix`,gate 已上。**
(这是本轮对总监那条裁定的第一次外部应用,**结论是"不豁免"** —— 一条判据第一次被用来
**拒绝**自己,比再一次批准更能说明它可用。)

## 4. ⭐⭐⭐⭐ 顺带修掉一个**让这条缺陷对全仓不可见**的量具洞

`tests/mock/replay_fixture.lua` 的 `record_actions` **直到本轮为止没有挂**
`ActionQueue_AttackUnit` / `ActionQueue_AttackMove`。

- 出厂树:`ActionQueue_AttackUnit` **5 个**调用表达式(**3 个**就在 `mode_roam_generic`
  的 Think 路径上:Leshrac `:931`、Wisp `:944`、Pudge `:999`)、`ActionQueue_AttackMove` **1 个**;
  **六个全是连续型**。
- ⇒ **每一个读这份日志的测试,在真的下了命令的帧上都答「没有下命令」。**
  本轮第一次探针跑出来 `log` 是空的,而同一帧其实下了一条 `ActionQueue_AttackUnit(nil, false)`。
- 两个钩子已补;**13 个现存 `record_actions` 消费方全部重跑绿**
  (`defstale_defend_bail 13/0`、`lf_rescue_final_action 12/0`、`lf_salve_cast_type 11/0`、
  `pullthink_anim_throttle 10/0`、`replay_072738_zuus_script 4/0`、`replay_260819_lion_drain 38/0`、
  `replay_260819_od_eclipse_aoe 23/0`、`replay_260819_zuus_ult_manalock 17/0`、
  `replay_260819_zuus_w2_leak 16/0`、`replay_260820_181711_wk_l1trade 12/0`、
  `replay_260820_wk_blast_overclaim 12/0`、`replay_260820_zuus_reserve_cross 24/0`、
  `replay_260820_zuus_static_band 15/0`、`roamreach_bounded_chase 19/0`、
  `roamstale_collapse_action 8/0`)。
  **加钩子只会增加条目**:一个因此变红的测试读的是盲区,不是行为改动。
- 已钉成 `[source S5]`,不是一句注释。

**这一条比本 id 值钱**:它是 GH #341/#345 那一族(工具从未求值某条子句)在**动作侧**的第一例。

## 5. 读数:**UNMEASURABLE 不是 EMPTY**(GH #171/#205 分界)—— 而作废面比本主题宽

`tests/_propertarget_sweep.lua`,993 个存活英雄帧,零 AWS:

```
COUNT frames=993 nil=993 hero=0 nonhero=0 other=0
SKIP  nonhero_far=0 nonhero_near=0 hero_far=0
SRC   tgt=0 atk=0 both=0
```

`J.GetProperTarget(bot)` 在 **993/993** 帧上为 `nil`,而**原因有名字、不是游戏条件**:
`bot:GetTarget()` / `bot:GetAttackTarget()` 都在 `tests/mock/bot_api.lua` 的
`handle_getters` 里(答 `nil`),而 **107 个 fixture 里 0 个**给出覆盖值。

**⚠️ 作废面比本 id 宽**:`J.GetProperTarget` 在 `bots/` 下有 **351 个调用表达式**、
散在 **164 个文件**里 ⇒ **本仓在 fixture 帧上对其中任何一个下过的结论,
都是在它返回 `nil` 的帧上下的。** 与 08-31T10:50Z(GH #362)的 `gate=0`
世界断言**同族、同一轮里第二例**。本轮**只登记不审计**。

⇒ 缺陷的**另一半**在本语料上买不到:`J.IsValidTarget` **就是** `IsValidHero`
(`jmz_func.lua` 自己的 FUSE RECORD:「同一个委托、同一个答案,只有名字不同,而名字在撒谎」),
而 `J.GetProperTarget` 只把**同队**英雄/建筑置 nil ⇒ **一个野怪/兵是完全正常的返回值**。
在四个同胞站点(见 §7)那意味着**距离测试被整个跳过**、下出一条不受界的连续攻击。
本语料产不出非 nil 目标 ⇒ 这一半登记为 `[limit]`,**不写进结论**。

## 6. 落地与验收

- `bots/mode_roam_generic.lua`:Pudge 块头部一个 `local rotscope = J.IsModeTurbo() and
  J.IsSoakCandidate('rotscope')`;armed 时命令**移进** Rot 块、下在**被守卫过的那个句柄**上,
  且只在 `J.IsValid(botTarget)`(**宽**谓词,野怪算数)时下;unarmed 时出厂那条语句
  **原样、同变量、同位置**跑在 `if not rotscope` 之后。**不动任何出价**(已断言)。
- `tests/test_rotscope_shadowed_target.lua`(`[ratchet]`,**13/13**,秒级):
  5 条 `[source]` + 3 条真实帧 `[drive]` + 2 条 `[control]` +
  1 条真实句柄对照(fixture 自己的队友 726u > 400 ⇒ **两臂都走 move 分支**)+
  1 条**明标合成**(fixture 里没有任何「有效且 ≤400u」的单位 —— 唯一队友 726u、零野怪 ⇒
  用一个声明过的桩,**只**用来断言 armed 把命令下在被守卫过的句柄上)+ 1 条 `[recorded]`。
- `tests/test_propertarget_corpus_domain.lua`(**不打标签**,29s,4/4)+ `tests/_propertarget_sweep.lua`。
  **两个文件故意拆开,只有便宜那半带标签** —— GH **#358** 已把自检 Lua 腿量成 133.3s,
  再给每流每次触发加 29s 正是那条 issue 的成本;这是预算决定不是疏漏(已写进文件头)。
- **变异 10 条:10 CAUGHT**。台子:树外 `cp` 还原(**不走 git**)、每条前后 `sha256sum -c`、
  退出码**裸读未经管道**、**先确认绿基线**。
  M1 删 armed 分支 / M2 出厂无条件发 / M3 去掉 turbo / M4 换 id 串 / M5 `bOnce=true` /
  M6 armed 用窄谓词 `IsValidTarget` / M7 拆掉量具钩子 / M8 守卫 `400→4000` /
  M9 删 `bot_api` 的 `GetTarget` 声明 / M10 把文件级赋值提到早返回之上。
  ⚠️ 头一遍 M5/M8 打的是 `MUTANT-NOT-APPLIED`(我的模式在文件里不唯一),
  **那不是 CAUGHT 也不是 SURVIVED**,补唯一上下文后重跑才成立 —— 照实登记。

## 7. 明说**没做**的(一次只动一个小杠杆)

同一文件里**四个同胞站点**把 `J.IsValidTarget` 写成距离测试的**合取项**,
于是守卫自己的失败落进那条**跳过了距离测试**的 `else` 连续攻击:

| 行 | 触发 | 形状 |
|---|---|---|
| `:871` | `modifier_marci_unleash` | `if IsValidTarget(t) and d > range+200 then move else Action_AttackUnit(t,false) end` |
| `:884` | `modifier_muerta_pierce_the_veil_buff` | 同上 |
| `:912` | `modifier_faceless_void_chronosphere` | 同上 |
| `:926` | `modifier_leshrac_pulse_nova` | 同上,但 `> 400` 且 `ActionQueue_` |

而**同一个文件把同一条决策安全地写过两次** —— `:1071`(疯狂面具)与 `:1092`(复活/不朽尸王盾):
**嵌套形式 + 宽谓词 `J.IsValid`**。⇒ **同一个文件、同一条三行决策、两种写法,
其中一种把自己的守卫失败交给了那条假设守卫成立的分支。**

**本轮不动它们**,理由是可查的而不是偏好:
`marci` / `muerta` / `faceless_void` / `leshrac` 在**全部 107 个 fixture 里各出现 0 次**
⇒ 今天在那里上的 gate **一帧也驱动不了**,而 gate-plumbing 不算本地验证(章程第 4 步)。

## 8. 门与自检读数(照实)

- `bash tools/agent/luacheck_gate.sh` **裸读 `BARE_EXIT=0`**,`luacheck bots game: 0 warnings`,
  **未用 `RULE6_BYPASS`**。(容器冷启,gate 自己装的 `lua-check`。)
- 开工自检:**首次调用被它自己拒绝** —— `REFUSED: stdout is a PIPE ... SELFCHECK_EXIT=2
  REFUSED (nothing was checked; this is NOT a pass)`。
  **这是 `0SGN`/`0SIB` 连续两轮同一站点自伤的第三轮,而这一轮它没能发生**:
  `a5578526` 落地的那道拒绝在管道下**直接不跑**,把一个会被误读成 0 的退出码变成了一次拒绝。
  改走 `> /tmp/sc.log 2>&1; echo $?` 后重跑,读数:
  `70 passed, 1 failed, 1 uncertifiable` / `TRUNK RED tests/test_rc_wrapper.py`
  —— 即 GH **#364** 已立案的那条 flaky,**本组不复核不重裁**。
  另外三条 `GAP cadence`(director 3.5h / replay-check 3.8h / strategy 3.6h)是节奏洞,非本轮产物。
- 子集绿(**均裸读 rc=0**):`smoke_load 3/0`、`gate_claim_consistency 10/0`、
  `nil_guard_then_body 6/0`、`coarmed_attribution 11/0`、`roamreach_bounded_chase 19/0`、
  `roamstale_collapse_action 8/0`、`towercreep_stale_source 7/0`、`rotscope_shadowed_target 13/0`;
  `[detector]/[ratchet]` 全腿读数见 §10。
- **全量单进程套件未跑完**(~100min,GH #124)⇒ 上界是子集,不是整棵树。

## 9. ⚠️ 诚实边界

- **`ActionQueue_AttackUnit(nil, false)` 在引擎里到底怎样,本仓无法观测**
  (`print()` 到不了控制台、`error in error handling` 吞掉全部错误文本)。
  本报告**不主张**它抛错、也不主张它是 no-op;主张的是**与引擎无关的那一半**:
  那条语句后面没有别的命令,**这一帧就以它结束**。
- **缺陷频率未知**:本 id 只对 Pudge 可达,而 Pudge 不在焦点五里。
  `[drive]` 证明的是**在这一帧上出厂确实这么做**,不是「每局发生 N 次」。
- **`nil` 是语料的性质不是游戏的性质**(§5);
  far-creep 那一半 **UNMEASURABLE 不是 EMPTY**。
- **合成桩是明标的**:它带一个 `__index` 兜底返回 `false` 的元表,已写进用例注释 ——
  每一个 `false` 只可能让**更早**的分支放弃,不可能让被断言的那条**开火**。
- **不裁任何人**:本轮不给任何已有 id 入集/出集/改级;`rotscope` 的入集由总监裁。
- §7 那四条**没有**被裁成「不是杠杆」,只是**今天驱动不了**。

## 10. 下一格(交棒)

- **总监**:
  (甲)**裁 `rotscope` 入集**(`test_set.md §CU`;搭车、零 AWS 增量、
  **排期约束:只对 Pudge 可达 ⇒ RIDESHARE,永远不能当独臂**);
  (乙)**`J.GetProperTarget` 的世界断言要不要立案** —— 351 个调用表达式 / 164 个文件,
  与 GH #362 的 `gate=0` 同族、**同日第二例**,它作废的是一整类历史结论;
  (丙)§CR.2 的豁免第一次被用来**拒绝**一条修复(§3),这条「用它来说不」的用法要不要写进 §CR。
- **录像组**:缺的不是判据是**帧**。带回 ① 任何一局里 `bot:GetTarget()` /
  `bot:GetAttackTarget()` 的真实取值(它一个人锁着 351 个调用点的可测性,**最值钱**);
  ② `marci` / `muerta` / `faceless_void` / `leshrac` 任一的一帧(§7 的四条同胞当轮即可钉帧落 gate);
  ③ 一局有 Pudge 的对局,`rotscope` 的条件 (a) 就有通道了。
- **批测台**:**无请求、零 AWS**;`rotscope` 搭车即可,不申请专波。

## 11. Token 用量

见文末 `TOKENS` 行。

```
TOKENS total_in=15,092,103 out=77,942 turns=94
```
