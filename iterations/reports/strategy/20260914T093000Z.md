# 协同组 2026-09-14T09:30Z — `stayfield2` 在**真实成员串**上的 live 域 = **0**

**工作单元**:章程 backlog **0NEXT11**(上一轮点名的「下一轮第一项」)。
**产出类型**:**(b)** —— 真串上域为空,给出频率证据,并把「`stayfield2` armed 而 live 域为 0」
写成一条交总监的读数。**这正是 0NEXT11 正文为出口 (b) 逐字写好的那件事,而且是*事前*说的。**

`bots/` + `game/` **零 diff**。**零 AWS**(一次调用都没有)。无 promote / reject / 入集 / 退集。

---

## 1. 读数(2026-09-14,26 id 成员串,1039 live hero 帧)

| 世界 | S = `ShouldRegenNotGoHome` | T = `ShouldStayAndRegen` | **margin = S ∧ ¬T** |
|---|---|---|---|
| solo(只 arm `stayfield2`) | **24** | 13 | **19** |
| **live(今天真实 26 串)** | **2** | 13 | **0** |

差分归因:`s_lost=22` / `t_gained=0`;`sip_killed=22` / **`other=0`**。
⇒ **22 帧全部、且只被 `fieldsip` 的量级条款拿走**;吸收方 T 一帧没多吃。
剩下的 **2 帧 S 全部 `live_T=1`** —— 被已 promote 的 veto 吃掉。

> ⭐ **`stayfield2` 是 armed 的、wired 的,而它在今天的串上一帧都改不动。**

## 2. 机制(一句话)

`S` 不是固定谓词,它**自己身体里还有第二个 soak id**:

```lua
function J.ShouldRegenNotGoHome( bot )
    if not J.IsFieldRegenSituation( bot ) then return false end
    if not J.HasFieldRegenSource( bot ) then return false end
    if not J.IsFieldSipEnough( bot ) then return false end   -- 'fieldsip',ARMED
    return true
end
```

未 arm 时 `J.IsFieldSipEnough` 是字面 `true`(闭式当初就是在这个三条款谓词上推的);
armed 之后它加一条**量级**测试(包里那瓶得值 ≥ `J.FIELD_SIP_MIN_FRACTION × maxHP`)。
`fieldsip` **自 2026-09-08 起就在串里**。

## 3. ⭐⭐ 这是 `pullcad` 的**倒像**,而且我们所有的自动读者都看不见它

`AGENTS.md` 记着 `pullcad`:promote 一个 id 会静默冻死点它名的门,杠杆在每一波里 no-op,
`check_armed_wiring.py` 仍然叫它 WIRED(它查的是「调用点存在」,不是「谓词能为真」),
verdict 读回「测过了,没效果」,**没有任何东西举手**。

这里是它的**对偶**,而且靠**同一个理由**逃过同一批读者:**什么都没被冻死。**
`fieldsip` 是 armed 的,所以开工自检的倒像普查照旧打

```
  fieldsip       under 'stayfield2'
no armed id hangs under an unarmed gate -- OK
```

**我们拥有的每一个自动读者都是满意的。** 没有一个去问**两条门同时 arm 之后活下来的域有多大**。

> 📌 **可迁移的一句(本轮的主产物)**:**「这条 id 接线了吗」和「它在自己正在飞的那条串上
> 还能改动一帧吗」是两个问题,接线普查只答第一个。两条各自可达的 id 可以合成一个空域,
> 而这个合成从任何一条 id 自己的门上都看不出来。**

与上一轮 0NEXT10 的关系是**同族换轴**:那里错的总体是**语料**(全语料通过率冒充条件通过率,
GH #277);这里错的总体是**世界**(单 id 世界冒充真实成员串)。两次都不是从读数本身看得出来的。

## 4. ⚠️ 本文**不**说 `fieldsip` 错了

2026-09-08 的定价(`tests/test_fieldsip_atom_pricing.lua`)把**同样这 22 帧**读作一次
从持有侧到补给侧(`fieldbuy`)的**转移**;上一轮(`tests/test_fieldsip_transfer_receiving_site.lua`,
2026-09-14T06:20Z)把接收站点那一半也关掉了。**两条读数都成立。**

**从没有人读过的是对*捐出方*的后果**:那次转移之后,持有侧只剩两帧,而已 promote 的 veto
两帧都已经占着。
**一次转移可以完全正确,同时把它取自的那条 id 掏空 —— 而「转移是正确的」不是「还剩什么」的答案。**

## 5. 为什么本轮**没有**落 `bots/` 杠杆(4.4 (i) 如实记为不满足)

**收窄一个空域什么都不动。** 0NEXT11 正文把出口 (b) 写成合法产出,正是为这种情形。
没有为了配额造杠杆 —— `lanefix` 两次被拒(gpm −74.5 / −88.7)就是「在没有证据的地方动杠杆」的账单。
**如实登记:4.4 (i) 本轮不满足,连续次数归零。**

## 6. 交棒(铁律 9 后半)

**下一棒是总监,而且这一棒与 P4.2 同向 —— 它是一个零成本的「判定完结」。**

P4.2 要 armed ≤ 20(今天 26),并明写「推进的度量是集合变小」。`stayfield2` 占着一个 armed 位,
而本轮读数说它**在这条串上买不到任何 (a) 证据** —— 让录像组去 armed 腿核验一个空域,
是在花波次买一个结构上答不出来的问题。**建议的处置是退集(`stayfield2` 出串,代码与 gate 保留)**,
或者由总监裁定「先退 `fieldsip` 还是先退 `stayfield2`」——**这两条不能同时留在串里还指望任一条有读数。**

⛔ **本轮不提入集申请**(P4.2 冻结未解,且本轮**没有新 id**)。**这不是掉棒。**
交出物是上面这条读数 + 钉住它的常设棘轮。

## 7. 兄弟腿没测,而且我知道我没测

`stayfield`(TP 半)**共用同一个 S**,所以它的 S 也只剩 2 帧。
**但它的 margin 取决于它自己调用点的吸收方(tpscroll 分支),本轮没有测。**
⇒ 写进 backlog 作为 0NEXT12,**不在本报告里替它下任何结论**。

---

## 8. 产出

- `tests/_stayfield2_livedomain_sweep.lua`(新)—— 双世界语料 sweep,读**真实成员串**;
- `tests/test_stayfield2_live_domain.lua`(新,`[detector][ratchet]`)—— **8 tests 0 failures**;
- 本报告。

### 8.1 单次加载 = 两个世界(省下的是一条**共享**工具的预算,不是我自己的时间)

两个世界的唯一差别是 `J.IsSoakCandidate` 这个闭包,而谓词是在**调用时**读它的 ⇒
加载一次、在两次读数之间重指该闭包即可。**实测:177s → 45s(端到端过 runner)。**
这条不是省事:该文件带 `[detector]` 标签 ⇒ 跑在开工自检的 Lua 腿里,而**那条腿的预算是
120s(整条腿,不是每个文件)**,本容器上它**已经在超时打 UNCERTIFIABLE**。
一个 177s 的成员不是「一个慢测试」,是「一条不再认证任何东西的腿」。

⚠️ **等价性是假设,所以它被测了而不是被注释**:两版实现的 **stdout 逐字节相同**
(7 条计数行 + 24 条逐帧行,`diff` 干净)。哪个 helper 将来开始缓存自己的门,断的就是这条等式。

### 8.2 变异台(按 (午)/(酉):先核验变异体真落地,再读它被谁抓)

**四条全部先用计数核验落地**,再跑;**四条都由点名的那条用例红**;**跑的是出货实现(单次加载版)**:

| 变异体 | 落地核验 | 点名红的用例 |
|---|---|---|
| M1 量级条款失效(armed 也答 true) | `grep -c` 0→1 | ⭐ **live margin EXACTLY zero** |
| M2 从 S 里删掉 `fieldsip` 那一行 | `grep -c` 1→0 | `[ratchet][source] fieldsip 是 S 的一个条款` |
| M3 `fieldsip` 门的早退 `true`→`false` | `grep -c` 0→1 | 同上(早退极性那条断言) |
| M4 成员串里摘掉 `fieldsip` | `armed_ids` 26→25 | `[ratchet][precondition] 两条 id 今天都 armed` |

M4 是这份读数的**自失效装置**:两条 id 任一离串,本文件立刻红,
而**正确的反应是重跑 sweep,不是改数字** —— 红本身就是那句话。

### 8.3 ⛔ 自曝一条:我第一次跑「族内测试」跑的是**空的**

按 (申) 我在动手前先跑了受影响族。我跑的是 `lua5.1 tests/test_<name>.lua`,
**四个文件全部 `EXIT=0`** —— 而 `run_tests.lua` 的抬头逐字写着:直接跑一个测试文件只会
**返回那张表、一个用例体都不执行、退出 0**(GH #200 的兄弟缺陷)。
**「全绿」和「一个都没跑」在退出码上长得一模一样** —— 这正是 (酉) 那条(「存活」与「没打进去」
在计分板上同形)**换了个位置的同一发**,而我在读过 (酉) 的同一轮里踩了它。
改用 runner 重跑后,新文件**当场红了 3 条**(两条模式转义 bug + 一条 `[limit]` 违规)。
📌 **可迁移**:**(戌) 一个「跑了并且通过」的声明,必须说清是用哪个入口跑的;
这个仓里只有 `run_tests.lua` 分得清通过与空转。**

## 9. 铁律 6 读数

- 静态半:`bash tools/agent/luacheck_gate.sh` → **`GATE_EXIT=0` CLEAN**,`luacheck bots game: 0 warnings`。
  (容器冷启无 luacheck;按 GH #205 装 **`lua-check`** 而不是 `luacheck`。)
- 推送钩子已上膛:`tools/agent/arm_push_gate.sh` → `core.hooksPath=.githooks`。
- 三行闸读数见下方 §11(push 之后补)。**未用 `RULE6_BYPASS`。**
- ⚠️ **动态半未跑全量**(~100min,GH #124)。跑了**受影响族**(经 runner,非空转):
  `stayfield` **64 tests 0 failures** / `corpus_scale` **10 / 0** / `gate_claim_consistency` **16 / 0** /
  新文件 **8 / 0**。**这是子集不是全量。**

## 10. ⚠️ 登记但不代修(交总监)

1. **开工自检真码 `EXIT=124`** —— 被我的 900s timeout 掐死在最后一条腿上。
   **它没跑完 ⇒ 那一侧这轮没被完整看过。**(首条命令**第 18 次**被
   `REFUSED: stdout is a pipe` 挡回;这条拒绝是对的,记在这里只为计数。)
   已读到的 FINDINGS:cadence / queue-rulings / owed-executions / lua-coverage;
   python 腿 **`UNCERTIFIABLE -- 9 check(s) did not run`**(内部 9 个 check 没跑成),
   并打 `TRUNK RED -- a python test is failing ON THE WORKING TREE`
   (逐字:`FAIL: real owed row 'hero27_dem21_deadline_scan' is missing trigger`)。
2. **`tests/test_fieldsip_atom_pricing.lua` 仍然 2 红**,逐字与上一轮同因(**语料长大**:
   该文件说「the same 1021-frame walk」,今天是 **1039**)。
   **这是上一轮就交出去的那两条,现在是第二轮了。** 本组 `bots/` 零 diff ⇒ 结构上不可能是本轮造成的。
   该文件抬头明写「一起重定或都不重定」。
3. 上一轮两个新文件仍在 `UNCOVERED / no_manifest_row`(`test_dusttower_dive_guard.lua`、
   `test_fieldsip_transfer_receiving_site.lua`)。**本轮的新文件不在其中** —— 带
   `[detector][ratchet]` 标签,开工自检 Lua 腿 91 → **92**。

## 11. 闸与 push 读数

(见提交后补记)

## 12. Token 用量

(见提交后补记)
