# 协同组 2026-09-10T01:44Z — 撤退的方向,是在不看「从谁那里撤」的情况下算出来的

**工作单元主体(P4.4 归属)= (i) 一个 `bots/` 行为改动**(连续第八轮 (i))。
新 gated id **`hrflee`**,零 AWS、零 S3、零 EC2、零波次。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1; echo "EXIT=$?"` —— **非管道**
(第一次照惯性写成 `| tail -40`,脚本自己**拒跑并打了 REFUSED**:`stdout is a PIPE`,
`SELFCHECK_EXIT=2 REFUSED`。evidence-discipline 第 3 条,这是它第 5 次在同一位置复现)。

**worst exit 3**:
- FINDINGS(exit 3):`cadence` / `queue-rulings` / `owed-executions` / `a-evidence-owed`
- UNCERTIFIABLE(exit 2):`trunk-red(python)` —— `tests/test_luacheck_gate_soakswitch.py`
  与 `tests/test_selfcheck_lua_leg.py` **没跑**(当时容器还没有 luacheck)。
  **UNCERTIFIABLE 不是通过**:trunk 的那一侧本轮开工时没人看过。
- 其余:187 个 live gate id 扫描,**FROZEN none**(没有 live gate 点名已 promote 的 id,
  pullcad 陷阱清);5 个 promote 原子全 ok;87 个 tagged 快检测器 0 失败。

**没有一条 FINDING 指向本组本轮的产物。**

## 1. 认领依据(工作流第 1 步)

扫 open issue:唯一一条**新**的 `[strategy]` 条目是 **GH #687**(2026-09-10T00:52Z)。
读完它:**它的诉求是注释口径,自己明说「不请求改 1600 这个值」** ⇒ 不是 `bots/` 行为改动,
按 **P4.4 的附带条**处理(见 §5),**不占工作单元主体**。

主体因此取章程 backlog 顶条 `0OWHSRULER`「下一格」**第 (4) 项**:同族里**还没查**的邻居
—— `J.GetLaneHarassResponse` 的 **420u 撤步落点**。上一轮把它挂在那儿时给的指示是
「**先查两处半径各自有没有写下来的理由**,照 `ShouldRetreatLaneBurst` 的先例,**定价然后拒绝**
也是合格产出」。

**查的结果推翻了问题本身的形状:**

| | 有没有写下来的理由 |
|---|---|
| `GetOffWaveHarassSpot` 的 **550u** | **有** —— 逐字「so the step clears the 500 aggro radius」 |
| `GetLaneHarassResponse` 的 **420u** | **没有**,全树只有三处提到它,都是描述不是理由 |

但**「420 太短/太长」不是这里的缺陷**,而且这一条要写下来,因为它是本轮唯一一次
**险些照着上一轮的模板做错事**:这条指令**每帧重发**(它在 Think 的战斗响应地板上),
所以 420 实际上只是一个**朝向的路标**,量级几乎不承重。承重的是**朝向**。
于是本轮没有按「查两个半径」的原计划收尾,而是换了问题。

## 2. 缺陷

```lua
if #tValid > nOurs then            -- ← tValid:刚数完的那群人,判定「寡不敌众」
    ...
    return 'back', Vector( vB.x + dx / n * 420, ... )   -- ← dx,dy 只来自 fountain
end
```

**`tValid` 在朝向里没有投票权。** 敌人站在 bot 与家之间时,那条**叫做 'back' 的指令
把 bot 直接送进他们中间**。

⭐ **同族前七轮抓的都是「一次比较的两侧用了两把尺」;本轮是同族的另一种形状:
一个刚建好的集合,在下一行被整个丢掉。**(判据 (5) 的姊妹形态第五次可用。)

### 证人帧(语料自己点的名,不是找出来的)

真实帧 `f_260820_102645_cm_es_reach`,**bristleback t=391.5,hp 0.45**,dire,
离自家泉水 **12,186u**:

| | 坐标 | 距 bot |
|---|---|---|
| bot (bristleback) | (5466, −5726) | — |
| Crystal Maiden | (5594, −5184) | **557u** |
| 冥魂大帝 | (5651, −5163) | **593u** |
| 敌群形心 | (5622, −5173) | 575u |
| **出厂落点** | (5516, −5309) | **距敌群 173u** |

**420u 的步子里有 402u 花在「靠近」上**,终点落在冥魂大帝的近战射程内 —— 而这一步的名字叫撤退。
两人几乎正压在 bot→泉水 的连线上(`dot(home, away) = −0.988`)。

## 3. 改动(gated,turbo-only,自带闸)

新 id **`hrflee`**。armed 时,当回家朝向会**朝敌群靠近**(`nDot < 0`)时,把朝向
**投影到垂直于敌群的方向**上 —— **绕过去,不是穿过去**。

证人帧上:落点 **173u → 711u**(距敌群),**同时仍朝家里推进 59u**。

⭐ **没有发明任何数字。** 420 与回家朝向都是这条分支自己的;敌群形心来自它自己已经建好的
`tValid`。唯一的新常数是 `1e-6`,那是**除零保护不是阈值**(只在敌群正好落在回家直线上、
切向不存在时接管),并已就地注明。

⭐⭐ **测试断言的是性质不是坐标**:「armed 落点到敌群的距离 **不小于** 原地不动」。
将来重调 420,测试**跟着走**而不是从旁边绕过去。

### 判据 (5):读调用点闸表达式的每一个析取项 ⇒ **必须自带 id**

调用点 `bots/mode_laning_generic.lua:453`:
`bCustomLastHit or bSupLastHit or bLaneFixSupport or bLaneFixCoreLH or bBodyBlock`,
而 `bCustomLastHit = local_mode_laning_generic or (pos1 and IsPosxHuman(5)) or (IsSoakCandidate('c3') and pos<=3)`
—— **前两个析取项不需要任何 armed id** ⇒ **这个 helper 在真实对局里是活的**
(与 `deepnum` 那轮同一发现)。所以 `0OVERCHASE` 的「继承宿主」**不适用**
(那条规则要求宿主**真被闸锁住**)。**M7 就是专门守这一条的腿。**
顺手把 helper 抬头从「BOTH of its behaviour changes」改成三条,并逐条点名。

## 4. 读数(110 fixtures,`tests/_lanekill_domain_sweep.lua` **第六次扩列**)

**没有**新建第七个 sweep。`hf_*` 20 列 + 2 类 F 证人行。

**⭐ 这份域不吃 stub。** 同一个 helper 里的 `hrreach` 有 84/84 帧吃 `GetAttackRange`
的 150 常数 stub(GH #656);本轮**每一个数都读自英雄位置**,那是每份 fixture 都真带的数据。

| 计数 | 值 |
|---|---|
| `hf_back`(出厂答 'back' 的帧) | **18** |
| `hf_into_mob`(落点比原地更靠近敌群) | **1** |
| `hf_away_ok` | **17**(闭合:18 = 1 + 17) |
| `hf_into_worst_u` / `hf_into_lt400` | **402** / **1** |
| `hf_land_moved`(armed 实际移动的落点) | **1** |
| `hf_armed_no_closer` / `hf_armed_best_u` | **1** / **+137u** |
| **禁止方向** `hf_verdict_moved` | **0** |
| **禁止方向** `hf_missed_defect` | **0** |
| **禁止方向** `hf_armed_still_closes` | **0** |

**两条独立的路给出同一个 1**:`hf_into_mob` 是对原始坐标做算术;`hf_land_moved` 是把
shipped 函数 armed 驱一遍看它**实际干了什么**。M10 现场把前者打到 0,断言看得见
(`hf_into_mob 0 vs hf_land_moved 1`)。

`hf_verdict_moved` 的 0 是**结构性**的而不是运气:闸在 'back' 分支**体内**,两条出口
都返回 `'back'` ⇒ **不可能增删撤退**。

### ⛔ 发波前先说(GH #622),而且结论**与姊妹 id 相反**

`hrparity` 的职责是**删掉撤退**,而本 id **只在撤退里面才存在**。
本语料上它修的那**唯一一帧,正是 `hrparity` 会取消掉的那一帧**:

- `hf_lost_to_parity` **1**,`hf_pair_keeps` **0**

⇒ **一条同时带 `hrparity` 的腿会让 `hrflee` 的域为空,(a) 白买。**
**建议:不要与 `hrparity` 同腿**(`hrreach` 无妨 —— 它只碰 'fire' 分支,已实测落点不变)。
`hrparity` 自己那轮的建议是 **co-arm**,方向恰好相反,**所以更不能靠记忆**:
这条读数由 `tests/test_hrflee_retreat.lua` 第 6 节钉死、由 **M11** 守着,
断言的失败信息直接写着「**re-read it before launching, do not just update the number**」。

### 本地验证

- `tests/test_hrflee_retreat.lua` **12/12**(真实帧证人 + 真实帧负对照
  `f_260819_222030_jugg_tp_eaten` juggernaut:出厂就在离开敌群 ⇒ armed **逐位不变**)
- `tools/agent/mutstand_hrflee.sh` **12 腿:11 变异体全 CAUGHT + 控制项 SURVIVED,
  零 SURVIVED,STAND GREEN**

⚠️ **两条腿的 `want` 第一版写错,已就地改正并把原因留在脚本里**(不是绕过,
同 `mutstand_deepnum` 的先例):
- **M3**(死闸)第一版期待证人帧的那句话,实际**先红的是普查断言**
  (`hf_land_moved` 掉到 0、`hf_missed_defect` 升到 1,第 5 节排在第 2 节的帧驱动之前);
- **M5**(自造 800 步长)第一版期待「计数」那条断言,实际出厂 return 仍留着两处 `* 420`
  ⇒ 计数照过,**抓住它的是同一个测试里的第二条断言**「没有别的量级混进来」。
两条都是「红得对、但这一行匹配不到」—— `WRONG MESSAGE` 计分规则**正是为这种红设的**。

**最承重的一条腿是 M1**,它是**下一个读者最可能写出的那个错修复**:直接背着敌群跑。
它**把实测缺陷修得干干净净**(落点不再靠近),代价是把 bot **带离家 415u**。
只有第 2 节的第二条性质断言分得开这两者 —— 这也是那条断言存在的理由。

## 5. 附带项:GH #687(注释口径)

#687 报的是:`nInvadeDepth` 是**两个圣坛距离之差**,连线上恒等于**过中点距离的 2 倍**;
于是 `ownhalf` 边注里那句「800u past the midline is the river bank」用来**否定旧常数 800**
的理由,换算过来说的正是**新常数 1600**。**已按它的验收方式落地(只改口径,不动任何常数;
#687 自己也明说不请求改值):**

- `ShouldPunishDive` 的 `ownhalf` 边注:写下**恒等式本身**+ #687 的实测斜率
  (W62 364764 圣坛坐标,400/800/1200/1600u ↔ depth 273/1073/1873/2673,**斜率 2**),
  并立下**全族单位规则**:「一个 depth 是差值,任何说它『Nu past the midline』的句子,
  要么减半,要么把 `ancient-distance` 说出来」;
- 深线固守两档 prose、overchase midline 分支、`midguard`、`pushguard`、`nodive` 的目标判据
  —— **6 处**统一口径。
- **机械可核验**:全文件里「`past the midline` 同一行带数字」而**没有**换算或 `ancient-distance`
  标注的行数 = **0**(#687 §六 第一条验收)。

**未做**(明确交回):#687 §四 的两条语料量(域宽 9.5×、收窄定价 20.9%/23.4% vs 边注的 44.3%)
要**下一波语料重算**才能定,不是本轮能买的;#687 §六 第二条因此**留着**。

## 6. 没动的东西

**armed 串 / `iterations/queue.json` / `iterations/streams/test_set.md` 一字未动**
(P4.2 入集冻结期:新 id **一律不入集**,搭车也不行;`hrflee` 登记在
`state.json:hrflee_20260910`,等冻结解除后再提入集,提的时候**必须带上 §4 的
「不要与 hrparity 同腿」**)。零 AWS、零 S3、零 EC2、零波次。

## 7. 限度(登记,不辩护)

1. **域只有 1 帧。** 这不是「几乎没有」的委婉说法,是这份 110-fixture 语料的实测数,
   而 `hf_back` 本身只有 18。**批测语料上的真实频率本轮没买到**,随 GH #688 交出。
2. **形心不是最近敌人。** 两人以上的敌群里朝向由形心决定,「一个贴脸 + 一个很远」会被平均掉。
   本证人两人 557/593u 近似等距,该差异**在这一帧上不承重,但在别的帧上会**。
3. **没有考虑地形。** 投影方向可能撞树或撞悬崖;引擎自行寻路,本轮**不声称**落点可达。
4. 铁律 6 动态那半(全量套件 ~100min,GH #124)见 §8 —— **本轮如实记录跑到哪儿**。

## 8. 铁律 6

- **静态半**:`bash tools/agent/luacheck_gate.sh` → **EXIT=0 CLEAN**,
  `luacheck bots game: 0 warnings`。push 门已按 `arm_push_gate.sh` 上膛。
- **动态半**(`lua5.1 tests/run_tests.lua`,~100min / GH #124):**在本轮容器里没跑完**,
  push 时仍在跑。**跑到的部分出现 1 条 FAIL,而它不是本轮引入的**:

  ```
  FAIL[1]: test_bagsalve_backpack_source.lua ::
    [reverse] turbo is structural: both callers ask the situation first
    J.HasFieldRegenSource has 10 call sites, not 9.
  ```

  **归因是复现出来的不是推断的**(evidence-discipline 第 4 条):
  `git worktree add /tmp/cleanhead HEAD --detach` 起一棵**干净的 HEAD 树**,
  在那上面单跑该测试 → **同一条失败、同一句话、exit 1**。
  本轮 diff 里 `HasFieldRegenSource` 出现 **0 次**。**已开 [bug] issue 交出去**(GH #689),
  形状与 GH #650 同族:**一个写死的普查计数,被别处新增的调用点打红**。

- **逐个跑了本改动可能碰坏的邻居**(同 helper / 同 sweep / 同族),全绿:

  | 测试 | 结果 |
  |---|---|
  | `test_hrflee_retreat`(本轮新增) | **12/12** |
  | `test_hrparity_guard`(同 helper) | 12/12 |
  | `test_hrreach_guard`(同 helper) | 14/14 |
  | `test_deepnum_parity`(同族机制 3) | 13/13 |
  | `test_owhs_side_ruler`(同族机制 4) | 14/14 |
  | `test_replay_megabundle_laning`(同 Think) | 6/6 |
  | `test_lanekill_domain_census`(同 sweep) | 6/6 |
  | `test_gate_claim_consistency`(闸声明) | 16/16 |
  | `test_smoke_load`(Lua 5.1 语法/装载) | 3/3 |

  另:开工自检的 **87 个 tagged 快检测器 0 失败**(那是**快子集不是全量**,自检自己这么写的)。

## 9. 下一格(本组下一轮第一项)

1. ⭐ **主体继续是一个 `bots/` 行为改动**(P4.4)。
2. ⛔ **不要**再动这个 helper —— 三条支路(`hrparity` 判定 / `hrreach` 开火 / `hrflee` 落点)
   现在**各自带闸、各自有域读数**,它已经被读干净了。
3. ⭐ **判据 (5) 的姊妹形态第六次可用,而本轮把它推广了**:前七轮找的是
   「**两把尺**」,本轮找到的是「**建好的集合被丢掉**」。**这是同一条判据的两种形状**,
   下一轮按**新的那种**扫:*一个 helper 算出了某个集合/量,然后在下游的决策里没有用它*。
   **第一个候选已经点好**:`J.ShouldRetreatLaneBurst` —— 上一轮**定价后拒绝**过它
   (所以不是行为改动的候选),但**它算 `nIncoming` 的那份威胁清单在撤退方向上同样没有投票权**,
   与本轮完全同形;先查**它有没有落点**,没有落点就换下一个。
4. ⛔ 仍然**不要**碰:落点 vs poke 目标的攻击射程(要 `GetAttackRange()` 投影,GH #656/#657)、
   兵线形心的轴(要 lane 几何,GH #648/#652)。
5. ⛔ **`hrflee` 的接力棒**(铁律 9 连带规则,不许掉):冻结解除后提入集时,
   **必须同时交出「不要与 `hrparity` 同腿」**,否则这一轮的 (a) 结构上买不到。
   本条已写进 `state.json:hrflee_20260910.wave_caveat` 与 GH #688。

## 10. 本次会话 token 用量

```
TOKENS total_in=11,666,649 out=75,355 turns=79
```

(`python3 tools/agent/token_usage.py`,统计时刻在 commit 之前;此后的收尾回合不计入。)
