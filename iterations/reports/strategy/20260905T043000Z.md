# 协同组 2026-09-05T04:1x–04:4xZ —— 认领 GH #511,判它的修法是 no-op

**一句话**:GH #511 报的**事实是对的**(抢瞭望塔的 channel 75% 被中断,两条腿一样 ⇒ 出厂缺陷),
它给的**根因和那一行修法都不对** —— `if bot:IsChanneling() then return end` 在 `Think()` 的
**第一条语句里已经跑过了**(住在 `J.CanNotUseAction` 之内),落地它**与未修树逐位相同**;
而它自己的帧证据**按构造反证它自己的机制**:移除**先**、施法**后**,**0/28**。

**零行为改动**:`bots/`/`game/` 逐字节未动(`git diff -- bots game` 为空),armed 串一字未动,
`queue.json` 一字未动,无新 soak id / 闸 / lua fixture。
**零 AWS 花费**:只读 S3(4 份 `.dem` + dumper 缓存命中),零 EC2、零波次、零 CE 调用。

```
VERIFY id=(none, GH #511 的提案) verdict=NO-OP price=0/36 casts
```

---

## 一、认领与选题

`[strategy]` 前缀本轮扫过 **#511**/#503/#500/#495/#492/#489/#485/#480/#475/#467/#464/#456/#455/#452/#445。
**#511 是 04:07Z 新开、未被认领、且不是本组已交棒的**(它是录像组 04:00Z 那轮顺手撞上的头号产出),
带完整帧证据 ⇒ 章程「每次触发的工作流」第 1 条(优先处理带帧证据的 `[strategy]` issue)成立,
**本轮没有换域**。上一轮(§EN / #503)的下一格球在总监(`midsupyield` 入不入 armed 集),不归本轮。

## 二、根因是错的,而且是两个独立的错

### 2.1 守卫已经在了,住在一次调用之内

#511 的确证段写着「全文件没有 `bot:IsChanneling()` 守卫」。**逐字为真,承重为零。**

```lua
-- bots/mode_outpost_generic.lua:132-134
function Think()
	if J.CanNotUseAction(bot) then return end        -- <= 第一条语句
	if J.Utils.IsBotThinkingMeaningfulAction(...) then return end
```
```lua
-- bots/FunLib/jmz_func.lua:97-103
function J.CanNotUseAction( bot )
	return not bot:IsAlive()
			...
			or bot:IsChanneling()                     -- <= 就在这里
```

⇒ 提案那一行加的是一个**同一帧上、更早、已经求过值**的析取项。
**§EK 的 `argfix` 形状**:改了源码、过得了评审、读上去像修复,**什么也没动**。

**⭐ 主判据(立法级,可复用,超出本主题):一个「守卫不存在」的论断,必须按 helper 展开后的
求值集合来立,不能按文件的 token 集合来立。** grep 一个文件回答的是「这个字符串在不在这个
文件里」;守卫的问题是「这一帧上这个谓词求过值没有」。有 helper 的代码里两者**系统性地不同**,
而且差得**恰好是让修复读起来必要的那个方向**。

### 2.2 时序是反的 —— 它的帧证据反证它自己(本报告最该被读的一条)

仓库里 `ability_capture` **唯一**的施法点是 `mode_outpost_generic.lua:144`,就在那道守卫**下面**
⇒ **每一次被记录的施法,按构造都是 `bot:IsChanneling()` 已经为假的那一帧。**
这条推理**不依赖语料**,对 #511 那 53 次尝试同样成立。

独立地,事件流说同一件事。W47 补跑机 4 局(`spot_…_695907`,种子 4763),
**8 个 episode / 36 次施法 / 36 对 channel ADD-REMOVE**:

| 腿 | 读数 |
|---|---|
| `issuefix` 施法落在施法者自己活 channel 区间内(= 提案唯一能拦的集合) | **0 / 36** |
| `selfresend` 施法**严格早于**它本该解释的那次移除(= #511 的机制) | **0 / 28** |
| 并列(dump 0.1s 量化,**定不了序**) | **4 / 28** ← UNDECIDABLE,**不并进任何一侧** |
| 移除严格在先 | **24 / 28** |

移除 → 下一次施法的**中位间隔 0.1s**。⇒ **重发是中断的下游,不是它的原因。**

## 三、排掉本文件自己的三条放弃子句 ⇒ 杠杆在文件的另一头

36 个中断瞬间上,`mode_outpost_generic.lua` 自己的放弃子句命中:

- (a) `GetDesireHelper` 离塔 <600u **且**视野内有敌人 → **2 / 36**
- (b) `WasRecentlyDamagedByAnyHero(5)` → **0 / 36**
- (c) `存活数 我方 < 敌方` → **0 / 36**

⇒ **34/36 的中断在本文件里找不到任何理由**:模式**从没停止过想要这座塔**。

**铁证帧** `20260905_010205_slot7` **t=1349.9–1370.7,luna**:
距塔 **140u**、hp **1.00 全程**、窗口内 `DAMAGE` 命中 **0**、最近敌人 **≥2618u**(1800u 视野外)、
存活 **5v5 全程不变**;14 次施法、14 次 channel、**一次也没占下来**,三条子句**一条都没真过**。

剩下的活解释是**模式仲裁**(不在 dump 里 —— **照实说,不当结论**):别的模式抢走一个 tick、
它的命令掐断 channel,瞭望塔模式 **0.1s** 后重新胜出并重发;那 0.1s 正是观测到的中位间隔。
⇒ **杠杆是 `GetDesire()` 里的承诺,不是 `Think()` 里的守卫 —— 文件的另一头。**

## 四、承诺那一版本轮**不落地**,而「买不到」在这里是一句有地址的话

承诺修法的谓词会是 `ClosestOutpost:HasModifier('modifier_watch_tower_capturing')` ——
那个 modifier **挂在瞭望塔上,不在英雄身上**(事件流里 `target` 就是 `#DOTA_OutpostName_North`,
这一点也纠正了「英雄身上上身 0.1–2.9 秒」的读法)。
dumper 的 `buildings` 记录只有 `name/team/x/y/hp/alive`,**没有 modifier 列表**
⇒ 瞭望塔的 modifier 状态**不在 dump 里、进不了 fixture**。

本组纪律是「行为改动必须带真实帧 fixture,gate-plumbing 不算」⇒ **本轮不落地它**,
把缺的那个字段**指名交出去**,而不是落一个验不了的改动。
**这是一个缺失字段的地址,不是一句「不好测」。**

## 五、仪器:每一个 0 都配了能出非零的对照

本轮全部产出是**否定读数**,而**坏掉的探针报的也是 0**。四条对照:

1. `issuefix` 腿是**实测**(施法时刻是否落在该施法者自己的 ADD..REMOVE 区间内),
   这个数**可以**非零 —— **非零就说明 #511 是对的、本裁定是错的**;
2. 该腿自带 `issuefix_intervals`(重建了几个区间):**空区间列表报的 0 和真读数的 0 一模一样**;
3. 子句 (a) 的**同一个估计量**在全部 **58978** 个采样帧上跑出 **19127** 非零;
4. axe 切片(`20260905_010226_slot3` t=1020.0–1025.6)让子句 (a) 在**中断瞬间本身**上出 **2**
   ⇒ 它不是「在中断瞬间结构上开不了口」。

### 5.1 两处自伤,当场收账

- **工具第一版的汇总行印着「every gap > 0」,而它自己下一行的分布里有 4 个 0** ——
  **结论与自己的读数矛盾**。已改成 before/tie/after 三分,并把并列登记为 **UNDECIDABLE**。
- **变异台第一次跑 12 发里 5 发不合格**(1 NO-OP + 4 SURVIVED)。按 evidence-discipline 规则 2
  **先怀疑断言**,结果 **2 个是真实的断言缺口**:
  (i) 空区间列表(M8)—— 补 `issuefix_intervals` 断言;
  (ii) 并列被折进主张一侧(M9)—— **第一份切片里根本没有并列**,所以 `gap < 0` 与 `gap <= 0`
  对全套测试**不可见**;补了第二份真帧切片(axe,带 2 个并列 + 2 次子句 (a) 为真)才补上。
  另 3 个是变异本身写错(regex 打错文件 / 守卫仍在施法之上 / 单文件 M6 今天不咬)。
  **没有把不合格的那一版当成通过记下来。**

## 六、产出

| 文件 | 是什么 |
|---|---|
| `tools/batch_test/behavioral/outchan_domain.py` | 三条腿 + 两条反真空对照的域价钱工具 |
| `tests/test_outchan_domain.py` | **31/31**,10 条 `[source]` ratchet + 21 条真帧读数 |
| `tests/fixtures/tl_260905_010205_luna_outchan.json` | 真帧逐字切片(luna,14 casts) |
| `tests/fixtures/tl_260905_010226_axe_outchan.json` | 真帧逐字切片(axe,带并列 + 子句 (a) 为真) |
| `tools/agent/mutstand_outchan.sh` | **13/13 CAUGHT,零 NO-OP,零 SURVIVED**,含 1 条 must-not-fire 对照 |
| `iterations/state.json:outchan_PRICING_20260905` | 档案 |
| `iterations/streams/test_set.md §EO` | 全文档案 |

**ratchet(§EN 那条法的应用)**:`tests/test_outchan_domain.py` 在下列任一情况变红 ——
`J.CanNotUseAction` 掉了 `IsChanneling` 析取项 / `Think()` 不再调它 / 守卫沉到施法之下 /
`ability_capture` 出现第二个施法点 / **有人照 #511 原样把那一行落进 `mode_outpost_generic.lua`**
(报错文案直说「**这是冗余不是错**,去 `GetDesire` 修承诺」)。
**立这些断言的理由**:推翻本裁定的编辑住在 `jmz_func.lua`,**协同组 backlog 上那天没有任何一条在看那里**。

## 七、交棒(铁律 9 的连带规则:同一工作单元内显式交出下一棒)

- **(甲) 录像组 / dumper**:`buildings` 记录补 `modifiers`(至少 `modifier_watch_tower_capturing`),
  否则承诺修法**永远进不了 fixture**,§EO.4 的门一直关着。
- **(乙) 本组下一轮或谁先拿到**:字段到位后落 `outcommit`(gated,turbo-only)——
  channel 进行中把瞭望塔模式的 desire 抬到不被抢 tick。
- **(丙) 总监**:`tests/test_outlatch_capture_liveness.py` 检查 **1b** 要改**说明**(不是改断言)。
  断言本身(「本文件里没有 `IsChanneling` 守卫」)**会一直通过**,本轮也没动它;错的是旁边写的
  **意思**(`:12-13`、`:57`:「the whole 75%-abort finding is about that missing guard」/
  「if this fails, the guard landed」)。守卫**没有缺失**;那个 token 落进本文件的那天不是
  「守卫落地了」,是**有人落了一个 no-op**。
  ⇒ **一条会一直通过、而且写着与事实相反的理由的断言,比红的更难被发现。**

## 八、铁律 6 读数

- **静态半:`bash tools/agent/luacheck_gate.sh` → `GATE_EXIT=0 CLEAN`,`luacheck bots game: 0 warnings`。**
  容器冷启,gate 自己装的 luacheck(apt 包名 `lua-check`)。
  **两次 `git push` 各自又跑了一遍同一道 gate(`.githooks/pre-push`,GH #213),都是 `GATE_EXIT=0 CLEAN`。
  没有用 `RULE6_BYPASS`。**
- **动态半:`lua5.1 tests/run_tests.lua` —— 启动了、干净地跑着,但在本工作单元内没跑完(GH #124 的已知形状)。**
  **必须说清楚的一点:这个 runner 把 `failures[]` 攒到收尾一次性打**(见 `tests/run_tests.lua:28,36`),
  所以**「跑到现在没打出失败」不是一个读数** —— 中途的沉默对失败与否零信息。
  截至收尾:**1474 个断言点**,**没有 exit code**。⇒ **动态半这一轮 UNCERTIFIABLE,不是通过。**
  ⚠️ 期间还制造并当场收拾了一次自伤:**先后起了两个 `run_tests.lua` 写同一个日志文件**
  (并发写 = 工件不是读数,§EK/§EN 已经记过两次)。已全部杀掉,**重开一个干净进程写新日志**,
  上面这个 1474 是那个干净进程的。
  **风险面**:本轮 `bots/` 与 `game/` **逐字节未动**(`git diff -- bots game` 为空),
  **没有新增或修改任何 `.lua`** ⇒ 动态半覆盖的那一侧本轮没有被触碰。
  可作证的部分读数:开工自检的**快 Lua 检测器子集** **83 个文件 0 失败**(它明说这是子集,不是全量)。
- 新增的 python 测试:`python3 tests/test_outchan_domain.py` **exit=0,31/31**。
- 变异台:`bash tools/agent/mutstand_outchan.sh` **exit=0,13/13 CAUGHT,零 NO-OP,零 SURVIVED**。
- 引用预检:`bash tools/agent/claim_precheck.sh`(#511 评论草稿)**exit=0**,
  `local commits not on origin/main: 0`,`paths cited 11 / resolved on trunk 12 / refused 0`。
  **评论是在 push 之后才发的**(GH #290 的顺序)。
- 开工自检:`bash tools/agent/routine_selfcheck.sh` **worst exit 3(FINDINGS)** ——
  cadence / owed-executions / stale-waits / trunk-red(python)。**trunk 的 python 红是本轮之前
  就有的**(GH #506 / #497 / #490 / #482 一族,不归本组),本轮新增的 python 测试自己是绿的。
  (第一次调用被 `routine_selfcheck.sh` 自己**拒绝**了 —— stdout 是管道,退出码会读成 `tail` 的;
  按它给的写法重跑才拿到真读数。evidence-discipline 规则 3。)

## 九、AWS 用量

只读 S3:4 份 `.dem`(约 88 MB)+ dumper 缓存命中(`get_dumper.sh` cache HIT,未重建)。
**零 EC2、零波次、零 CE 调用、零 PUT。** 本轮不属于批测台,没有花钱的动作。

## 十、token 用量

```
TOKENS total_in=15,570,878 out=94,384 turns=96
```
(统计时刻之后的收尾回合不计入。)
