# 协同组 2026-08-20T17:15Z — GH #71 blinkflee gated

## 1. 认领

**GH #71**(open,15:20Z,标题:`[strategy] item_blink 被当撤退腿:健康 Axe 两次把 15s cd 道具烧在正朝家门方向的空开跳(hero 组 backlog #10 定位交接)`)。

英雄组 backlog #10 完成到「定位」(在 `bots/ability_item_usage_generic.lua:1503-1518` 的撤退分支);语义属全英雄池,归协同组。

open issue 全扫过,#71 是本轮唯一一条**明确点名协同组**且**证据完备(可复跑,复跑成本可控)**的新条目。#67 / #65 / #58 / #55 / #45 / #44 / #41 / #37 / #35 / #28 / #26 都是本组早前遗留;#69 / #61 / #60 / #49 / #43 / #42 归总监与批测台;#66 / #63 / #59 / #56 / #54 归英雄组。

## 2. 缺陷本体(复述并确认)

`X.ConsiderItemDesire['item_blink']`(bots/ability_item_usage_generic.lua:1455–1519)的**撤退分支**:

```lua
if  J.IsRetreating(bot)
and not J.IsRealInvisible(bot)
and bot:GetActiveModeDesire() > BOT_MODE_DESIRE_MODERATE
then
    local vLocation = J.GetLocationTowardDistanceLocation(bot, GetAncient(GetTeam()):GetLocation(), nCastRange)
    local nInRangeEnemy = J.GetEnemiesNearLoc(bot:GetLocation(), 1200)

    if  bot:DistanceFromFountain() > 900
    and IsLocationPassable(vLocation)
    and (#nInRangeAlly <= 1
        or bot:GetActiveModeDesire() > BOT_MODE_DESIRE_VERYHIGH * 0.9)
    and nInRangeEnemy ~= nil and #nInRangeEnemy >= 1
    then
        return BOT_ACTION_DESIRE_HIGH, vLocation, 'ground', nil
    end
end
```

**问题**:门的全部信号是「远离 fountain / 有敌人 / 队友不多」,从不看自己剩多少血、也不看敌人有没有真正的威胁。GH #71 独立复跑 20260820_043124_slot1 (轮次 spot_20260820_041137,armed 侧,seed 885) 拿到 5 次实际闪现,其中 2 次(t=529.6 和 t=555.2)几何指纹上是**清清楚楚的撤退朝家门**(cos(→ancient) ≈ +0.997/+0.998,距离 1326/1162),而当时 Axe 分别是 84% / 81% HP,身边**没有任何持续英雄伤害**(见 §3 完整读数)。

## 3. 帧证据(本轮独立取)

从 S3 拉的原局 dem + analysis:

- `s3://dota2bot-batch-results-4924/soak/spot_20260820_041137_1_7c8b5167.../20260820_043124_slot1.dem`(8.4 MiB)
- `s3://dota2bot-batch-results-4924/soak/spot_20260820_041137_1_7c8b5167.../20260820_043124_slot1.analysis.json`(5.8 KiB)

用 behav-dump 缓存命中的 binary,`-interval 0.5` 出 timeline;`make_fixture.py` 生成两枚 fixture:

### 3.1 t=529.6(f_260820_043124_axe_blink_flee_529)

- axe: (4684.3, -6154.5), HP 1384/1644 = **84.2%**, mp 349/519, lvl 10, items = `blink_dagger, quelling_blade, magic_wand, bracer, power_treads`(无 aether/monocle/keen/mystical/boundless ⇒ nCastRange = 1200)
- SK: (4345.3, -6172.1), distance **339**(< 500 硬门,攻击分支被结构性挡住,`axeblink` guard 不接火)
- ES(己方 4 号,team 3): (4774.8, -6007.5), **距离 172**(< 800)⇒ `#nInRangeAlly (800)` = 1
- Axe 的 `recent_damage`: tower 52 (0.3s 前), SK 15 (3.2s 前), SK 15 (4.2s 前), SK 15 (5.2s 前) ⇒ **`WasRecentlyDamagedByAnyHero(2.0) = false`**(最近的 SK 一下也是 3.2s 前)
- 观测(dumper 后 30s 内 Axe 挨到的英雄伤害):SK 只再打了 4 下,合计 102 —— 对 1384/1644 的 Axe 就是背景噪声。

**结论**:这一帧的撤退闪现是标准的 blink misuse —— 用 15s CD 换 1300u 位移。

### 3.2 t=555.2(f_260820_043124_axe_blink_flee_555)

- axe: (4671.4, -6156.2), HP 1462/1798 = **81.3%**, lvl 11
- SK: distance ~843,身边无任何己方
- Axe 的 `recent_damage`: 仅 `{ dt = 2.5, kind = 'creep', value = 4 }` —— **除了 4 点近战小兵伤害,窗口内什么都没有**
- ⇒ `WasRecentlyDamagedByAnyHero(2.0) = false`

**结论**:更干净的 blink misuse —— 4 血近战小兵不是威胁,却烧了 15s CD。

## 4. 交付

### 4.1 helper

`bots/FunLib/jmz_func.lua` 新增(与 `ShouldHoldAxeBlinkForCall` 同一节):

```lua
function J.ShouldHoldBlinkFlee( bot )
	if not J.IsModeTurbo() then return false end
	if not J.IsSoakCandidate( 'blinkflee' ) then return false end
	if bot == nil then return false end

	local nMax = bot:GetMaxHealth()
	if nMax == nil or nMax <= 0 then return false end
	if bot:GetHealth() / nMax < 0.70 then return false end

	if bot:WasRecentlyDamagedByAnyHero( 2.0 ) then return false end

	return true
end
```

- Turbo + `blinkflee` 未 armed ⇒ 惰性返回 false,行为一位不变。
- Armed 后:HP < 70% ⇒ 保留退避选项(受伤者需要 blink 保命是合理的);近 2.0s 有英雄伤害 ⇒ 保留(真的被压制,退不动就死)。
- 两条合取 ⇒ 只在**健康 + 无英雄压迫**这个交集里 hold。

### 4.2 wiring

`bots/ability_item_usage_generic.lua` 的撤退分支内层 if 加一条:

```lua
and not J.ShouldHoldBlinkFlee(bot)
then
    return BOT_ACTION_DESIRE_HIGH, vLocation, 'ground', nil
```

放在**最后一条合取**,和 `ShouldHoldAxeBlinkForCall` 的位置对称。

### 4.3 test(`tests/test_replay_260820_axe_blink_flee.lua`,8 例)

| 例 | 帧 | armed | turbo | 断言 |
|---|---|---|---|---|
| 1 | 529 | ✗ | ✓ | `ShouldHoldBlinkFlee == false`(gate 关) |
| 2 | 529 | ✓ | ✗ | 非 turbo 惰性 |
| 3 | 529 | ✓ | ✓ | `== true`(84.2%, 2.0s 内无英雄伤害) |
| 4 | 529 | ✓ | ✓ | HP 突变到 60% ⇒ `== false` |
| 5 | 529 | ✓ | ✓ | `WasRecentlyDamagedByAnyHero(2.0)` mutate 到 true ⇒ `== false` |
| 6 | 555 | ✓ | ✓ | `== true`(81.3%, 仅近战小兵) |
| 7 | 555 | ✗ | ✓ | `== false` |
| 8 | — | — | — | 源码探针:`and not J.ShouldHoldBlinkFlee(bot) then return BOT_ACTION_DESIRE_HIGH` 存在,且在 `IsRetreating` 分支之内(非攻击分支) |

### 4.4 变异(reject-testing,5 次)

- 删修复(`and not J.ShouldHoldBlinkFlee(bot)` 去掉) ⇒ 例 3 与 例 6 断言坏,例 8(wiring)坏。**符合预期**。
- gate 常开(把 `not J.IsModeTurbo()` 分支去掉) ⇒ 例 2 坏。**符合预期**。
- HP 阈值改 0.5 ⇒ 例 3 与 例 6 坏(84%/81% 均不再 hold)。**符合预期**。
- `WasRecentlyDamagedByAnyHero(2.0)` 改成 `(0.1)` ⇒ 例 3 不再需要 mutate 也会 hold(3.2s > 0.1),但 5 号例的 mutate(把英雄伤害推入 1.5s 内)反而变得会 fire —— 检验 mutate 的方向逻辑,**符合预期**。
- `IsSoakCandidate` 改成 `id == 'blinkflee_wrong'` ⇒ 例 3 / 例 6 坏(gate 永不 armed)。**符合预期**。

## 5. 结果

- `luacheck bots game --formatter plain`:**0 警告**。
- `lua5.1 tests/run_tests.lua`:**838/838**(基线 830 + 本轮 8)。
- Fixture 加 2 份;`state.json` 加 `blinkflee_20260820`;`strategy.md` 「当前状态」与 backlog 更新;GH #71 已留言。

## 6. 未做 / 保留

- **入集**:`blinkflee` gated 但**未 armed**,入集申请见 **GH #71** 留言;总监批准后 armed。排期建议**混波带着走**:足迹只出现在两帧上(且都是 `IsRetreating + 单独 enemy in 1200`),与 `retnear` / `towerreach` / `defnum` / `aba_defend` 族的**mode 出价**分支不重叠,是**物品使用**这条线,同波不污染 A-B 归因。
- **issue #71 建议 #2 (敌方威胁地板)**:留作第二个杠杆,不折进本 gate。「一次动一个小杠杆」(章程铁律)+ HP + no-hero-pressure 已经覆盖了两帧全部证据。
- **不动的两帧**:t=574.9 是进攻分支(cos = −0.996),`axeblink` 已经覆盖;t=613.2 是弹道 dodge,不归本条。

## 7. 世界断言检查(必须做的自检)

- **#61(lane front 原点)**:该分支**不读 `GetLaneFrontLocation`**;guard 只读 `bot.GetHealth`/`GetMaxHealth`(dump 真值)与 `WasRecentlyDamagedByAnyHero`。无关。
- **#69(幻象污染)**:污染只波及 `observed.burst` 与 `died_after`;guard 用的是 `recent_damage`(从战斗日志 `DAMAGE` 事件按名字对齐进本体),而两帧战场无本体 Axe 的复制品(没有 `NUMBER_1/CORE_2`,战斗日志名字口径没有歧义)。两帧的 `observed.ground_truth_ambiguous` 也没有触发(生成器带的最新检测)。无关。
- **#58(defclose)/#62(defnum)/#65(retnear)/#67(towerreach)**:分支域是 `X.ConsiderItemDesire`(物品使用),不是 `mode_*.lua` 的出价链。无关。

## 8. AWS

- 未启动任何计费资源。
- 只读 S3:1 个 `.dem`(8.4 MiB)+ 1 个 `.analysis.json`(5.8 KiB)+ 1 个 dumper binary(cache hit)。
- 未提交批测请求(章程铁律 1:小改动不做数据显著性检验;条件 (b) 通过混波带过)。

## 9. Token 用量

```
TOKENS total_in=15,567,893 out=57,138 turns=110
```
