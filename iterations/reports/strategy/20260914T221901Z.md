# 协同组 2026-09-14T22:19Z — 0NEXT16 裁掉:`IsValidCreep` 的 `> 9` **不动**,而否掉它的不是章程点名的那条读数

**工作单元**:backlog **0NEXT16**(上一轮点名的「下一轮第一项」)。
**出口**:**(b)** —— `bots/` + `game/` **零 diff**(如实登记,4.4 (i) **不满足**;
上一轮 19:28Z 的 `camppick` 满足了 (i),本轮没有接上)。
**AWS**:零花费,未 bootstrap。**新 gated id**:无(`state.json` 未改)。

---

## 0. 铁律 9 / 1b

- `iterations/OWNER_PRIORITIES.md`:P1 DoD① 已完成;P2 的球写着协同组,但其 TP 腿
  **卡在 `owed_executions.json:wandlimbo_charge_instrument` 的仪器墙上**(RULING 37,
  两端 dumper + fixture 都要),**不是本组能在一个工作单元里推动的格**;P4 的 4.4 属本组,
  见 §5 的如实登记。⇒ 无可推进的 owner 格 ⇒ 走章程 1b。
- 开着的 `[strategy]` issue 无带**新**帧证据的未认领项 ⇒ 取 backlog 最上面一条 = **0NEXT16**。

## 1. 0NEXT16 问的那条读数,**没买到,而且它不是决定性的那条**

0NEXT16 的原文把一条读数设成闸:*「`GetFarmLaneTarget` 那条路上 `IsAncientCreep()` 是不是恒假?
若恒假 ⇒ 对齐 `> 9` 是闭式 no-op,可以安全收窄,那是一条**比 `camppick` 更靠上游**的杠杆
(且能同时覆盖 `camppick` 够不着的消费者)。」*

**(1) 那条读数今天买不到,按 UNCERTIFIABLE 登记,不按任一方向假设。**
dumper 写 creep 行只有 `{t, team, x, y}` —— 没有 id、没有名字、没有血量
(`make_fixture.py`:*"no entity id, no name, no health"*),`IsAncientCreep()` 在
fixture 落盘时就被丢掉了。这与 `test_camppick_target_tier.lua` 的 `[world]` 撞的是同一堵墙。
**已写成断言不是散文**:`[world] creep identity is not in the corpus…` —— dumper 哪天开始发
creep 身份,它就变红,0NEXT16 的兵线分支自动重新可买。

**(2) 而它买不买得到都不改变结论**,因为一条**更便宜的源码级读数直接否掉了前提**:
收窄 `> 9` **不是**比 `camppick` 更上游、覆盖更广的杠杆,它在**它自己要管的那条路上就是不完整的**,
而且它真咬到的地方**改的不止是「拒绝远古」**。

## 2. 两条否掉前提的读数(真实帧,shipped 代码,已钉成断言)

**读数 A —— 不完整:装甲削减快捷路径跑在 `IsValidCreep` 前面。**
`GetMaxHPCreep` / `GetMinHPCreep` 的循环第一句是

```lua
if not creep:IsNull() and ____exports.HasArmorReduction(creep) then return creep end
```

它**先于** `IsValidCreep` 返回。实测(真实帧 viper **L9**,即 `> 9` 已经在拒绝远古的那一级):
一只带 `modifier_slardar_amplify_damage` 的远古 **两个选择器都照样交回**;
去掉那个 modifier 的对照组则正确拒绝。
⇒ **收窄那个字面量关不掉这两个选择器的远古洞** —— 它根本不是那条路上的一道闸,
只是那条路**两个分支里的一个**上的闸。

**读数 B —— 不是更窄,是更宽:`nearest` 农民在收窄后的世界里会**悄悄换掉选择器类型**。**
`GetNearestCreep` **只测 `creepList[1]`**,不扫描。所以收窄后,最近的一只是远古的那一帧,
`nearest` 农民拿到的**不是「最近的非远古」**,而是 nil,顺着
`FindFarmNeutralTarget` 的 `targetCreep or GetMinHPCreep(tPick)` 尾巴**变成 minHP 农民**。

实测(真实帧 **sven L4**,`nearest` 农民,sub-10 = 收窄世界的可观测模型;
sweep = 远古 1400hp@260u / 普通 550hp@400u / 普通 300hp@800u):

| | 选中 |
|---|---|
| shipped(= 收窄后的世界) | **300hp 的那只,800u 外**(minHP) |
| `camppick` 过滤后的表 | **550hp 的那只,400u**(仍然是 nearest) |

**两条杠杆都拒绝了同一只远古,攻击的却是不同的 creep** —— 这就是上一轮要求的
「能把两者分开的那条断言」,而且它指的方向是**收窄那个字面量更差**。

**读数 C —— `camppick` 两个洞都关得掉**,因为它在任何选择器走这张表之前就过滤好了
(装甲削减快捷路径也一并被关在外面),且**调用方的表逐字不动**(`#sweep == 2` 是断言)。

⇒ **裁定:`bots/FunLib/utils.lua` 的 `> 9` 不动。`camppick` 的调用层做法是终局**
(0NEXT16 的分支 (ii) 结论成立,但**到达它的路径和 0NEXT16 设想的完全不同**)。

## 3. 产物

- `tests/test_isvalidcreep_bound_completeness.lua` —— **7 tests / 0 failures / 0.08s**。
  真实帧:viper L9(`f_260820_102645_cm_es_reach.lua`)、sven L4(`f_071903_sven_idle.lua`)。
  creep 是**申报的替身**(理由见 §1(1)),载着 shipped 选择器实际读的那几个字段,
  文件里**没有任何一个数被当作语料数据**。
- `tools/agent/mutstand_isvalidcreep_bound.sh` —— **6 抓 + 控制 SURVIVED + 0 NO-OP**,`exit 0`。
  变异全部打在**出货实现**上(evidence-discipline 规则 1),每条先 `grep -c` 证明落地,
  还原用 `sha256sum` 校验。六条分别是:M1/M2 两个 HP 选择器的快捷路径各自「被修好」、
  M3 `GetNearestCreep` 改成扫描、M4 字面量不再拒绝远古、M5 `camppick` 过滤器 no-op、
  **M6 过滤器改去动调用方的表(= campfarm 形状)**。
  M1/M2 成对存在的理由和 camppick 台上的 M2/M3 一样:**只驱动一个选择器的台子会把
  「不完整」在一个分支上证明、在另一个分支上假设**。
- **两个文件加了 `[ratchet]` 标签**(见 §4)。

## 4. 附带一条:把本组上一轮自己捅出来的 UNCOVERED 洞补上

本轮开工自检报 `UNCOVERED SET GREW`,三个文件里**第一个就是本组上一轮落的**
`tests/test_camppick_target_tier.lua`(`no_manifest_row`)。GH #806 / #624 的形状:
**没有任何自动读者跑的测试,它的红由下几个小时后开工的下一个组发现**,而作者已经走了。
两个文件里的源码普查断言(「`IsValidCreep` 恰好 3 个调用点」「`GetFarmLaneTarget` 恰好 2 个文件」
「四个阈值仍是 9/10/10/12」)**正是任何组落一个新调用点就会顶红的那一类**。

⇒ 把两个文件的源码普查块从 `[source]` 改名为 **`[ratchet]`**,于是 `routine_selfcheck.sh`
的 fast Lua 腿(`grep -l '\[detector\]\|\[ratchet\]'`)当天就发现它们。
**按该腿 header 的要求先计时再打标签**:`isvalidcreep_bound` **0.08s**、
`camppick_target_tier` **0.37s**,都在那一腿「从来没超过毫秒级」的带内。
⛔ **没碰** `lua_gate_measure.py` 全量重测、**没手改** `lua_gate_manifest.json`
(GH #813 / #783 两个坑还开着;push 闸的入闸与否仍是总监的格)。

## 5. 4.4 配额,如实登记

- **(i) 不满足**:`bots/` + `game/` 零 diff。
  这是**刻意的**:本轮的产出就是「不要落那条杠杆」,而唯一还能落的相邻改动
  (给 `HasArmorReduction` 快捷路径加合法性过滤)**在中立路上是 `camppick` 的重复**
  (`camppick` 已经把远古从表里拿走了),**在兵线路上则依赖同一条买不到的读数**。
  按上一轮继承下来的判据 —— *写不出能把两者分开的断言,就说明它确实是重复的,应当不落* ——
  收窄字面量那条**写得出**分开的断言(§2 读数 B),而那条断言说的是**它更差**;
  快捷路径那条**写不出**(在中立路上)。两条都不落。
- **(ii)**:本轮是 backlog 裁定完结,不是 promote/reject 判定完结,**不认领 (ii)**。
- 连续第一轮重新出现 `bots/` 零 diff。下一轮(0NEXT17)**必须**以 `bots/` 行为改动为主体。

## 6. 铁律 6 读数

- 静态半:`bash tools/agent/luacheck_gate.sh` → **`GATE_EXIT=0` CLEAN,`luacheck bots game: 0 warnings`**。
  **未用 `RULE6_BYPASS`。**
- 动态半:**未跑全量**(GH #124,~100min)。跑了本轮触及的子集:
  `isvalidcreep_bound` **7/0**(0.08s)、`camppick_target_tier` **13/0**(0.37s)。**是子集不是全量。**
- 变异台 **exit 0**(6 CAUGHT / CONTROL SURVIVED / 0 NO-OP),跑完 `sha256sum` 校验还原,
  `git diff bots/ game/` **为空**(已核)。
- 推送三行读数见 §8。

## 7. 开工自检

首条命令**两次被自检自己拒绝并给出正确用法**(先 pipe → `REFUSED ... stdout is a pipe`,
再 `timeout` → `REFUSED ... running under timeout`),第三次按它印的
`nohup … > /tmp/sc.log 2>&1 &` 跑。**两次 REFUSED 都是 exit 2「什么都没检」,不是通过。**
本轮**没有把它当成跑过了**。截至收尾,python 测试套件那一腿仍在跑
⇒ **自检的最终 `worst exit` 本轮未读到**,如实登记为**未核验**,不是绿。
已读到的腿里与本组相关的:`unlanded work` **OK**;`cadence` 4 处历史 GAP(含本组
09-14T01:45→06:20Z 与 10:26→14:09Z,均为已过去的触发洞,非本轮可动);
`UNCOVERED SET GREW` 见 §4。

## 8. 交棒

- **总监**:本轮**不提任何入集申请**(4.2 冻结下 armed 25 > 20)。要裁的只有一件:
  `[ratchet]` 标签让两个文件进了开工自检的 fast Lua 腿(+0.45s),
  **push 闸的 manifest 入闸与否仍未动,是你的格**。
- **录像组**:0NEXT16 的兵线分支(`GetNearbyLaneCreeps` 里会不会出现远古)
  **在 dumper 发出 creep 身份之前结构上买不到**;若 `#581`(creeps 的 `name/hp/idx`)
  哪天落地,`[world]` 那条断言会自己变红点名它。
- **下一轮本组**:0NEXT17(见章程),主体必须落在 `bots/`。

## 9. Token 用量

`python3 tools/agent/token_usage.py` 最后一行,原样:

```
TOKENS total_in=7,247,400 out=52,261 turns=56
```

(统计时刻之后的收尾回合不计入。)
