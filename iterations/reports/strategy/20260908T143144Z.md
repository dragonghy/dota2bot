# 协同组 · 2026-09-08T14:31:44Z

**本轮 issue:GH #635**(结论 + 两条请裁定 + 附带的恒真子句 + 变异台通用教训)

**工作单元主体 = 一个 `bots/` 行为改动**(OWNER_PRIORITIES **P4.4(i)** 达成)。
认领依据:上一轮「下一格」第 (1) 条(主体必须是 `bots/` 改动)+ **OWNER_PRIORITIES P2**
(决策侧,球在本组)。上一轮「下一格」第 (4) 条明令 `J.IsFieldRegenSituation`
的四条子句**矿脉挖完、不要再找第五条** —— 本轮**没有**碰那个函数,换了一个
**结构维度**:不是「再加一条子句」,而是**问这个判断被摆在了哪条分支上**。

**零 AWS、零 S3、零 EC2、零波次。armed 串 / `queue.json` / `test_set.md` 一字未动**
(P4.2 冻结期,新 id 一律 **FROZEN-HOLD**)。

---

## 0. 开工自检(铁律 10)

**第一次调用被工具自己拒了,而拒得对。** 我把它写成
`routine_selfcheck.sh 2>&1 | tail -60`,脚本检测到 stdout 是管道后
**REFUSED,exit 2,什么都没检查**,并打出「这是证据纪律 3,已第 5 次复发,
每次都是当轮第一条命令」。改走 `> file; echo EXIT=$?` 重跑。
**记在这里不是为了自嘲**:这条护栏之所以有用,正是因为它把
「读的是 tail 的退出码」变成了一次**拒绝**而不是一次**误读**。

重跑读数:

- **`selfcheck worst exit: 3`**;`legs run 10`。
- FINDINGS(exit 3):`unlanded` / `cadence` / `queue-rulings` / `owed-executions`。
- UNCERTIFIABLE(exit 2):`trunk-red(python)` —— python 腿 **113 passed / 0 failed
  / 2 uncertifiable**(`test_luacheck_gate_soakswitch.py`、`test_selfcheck_lua_leg.py`
  当时**没跑成**,原因是那一刻容器里还没有 luacheck)。**「没跑成」不是通过,
  也不是 trunk 红的证据。**
- trunk fast Lua detectors:**86 个 tagged 文件,0 failures**。
- 锚点 6 个全 OK;promote-atom 约束 OK;`FROZEN none`。
- 推送闸已上膛:`arm_push_gate.sh` → `core.hooksPath=.githooks`(本轮 exit 0,
  已是 set 状态)。**本轮没有用过 `RULE6_BYPASS`。**

---

## 1. 立案:**一个永远跑不到的分支上的守卫不是守卫**

`X.ConsiderItemDesire["item_tpscroll"]` 里有**四条把 bot 送回自家泉水的分支**,
按源码顺序排列,而且**每一条都 `return`**。owner P2 家族的三个 id 全部挂在
**后两条**上:

| 分支 | 源码位置 | 守卫 |
|---|---|---|
| `撤退:1` | `if botHP < 0.19`(retreat 块内,~5576) | 只有 PROMOTED `J.ShouldStayAndRegen` |
| `撤退:2` | 多敌可卡视野 | 无(**真逃生,家族明令不碰**) |
| `撤退:3` | `bot:GetLevel() >= 9` | gated `stayfield` |
| `回复状态` | ~5961 | gated `tprecov` + `tpdeep` |

**没有任何人问过:上游的 `撤退:1` 会不会先把这一帧接走。**

全语料驱动(`tests/_tpquiet_sweep.lua`,**1021 活帧**,读数取自 **shipped** 的
姊妹函数,不重新实现):

```
tpdeep_true                  2    -- 姊妹 tpdeep 的全部域
tpdeep_true_in_r4            2    -- 两帧都在它自己的分支触发里
                                  --   (= 它自己发表的 domain_and_branch_open 2)
tpdeep_true_in_r4_shadowed   1    -- ...其中 1 帧同时在 '撤退:1' 里,而 '撤退:1' 先 return
both_triggers                6    -- 两条分支触发同时开着的帧
```

⇒ **姊妹 `tpdeep` 在本语料上的活域是 1,不是 2;发表读数高了一倍。**
被遮住的那一帧,**正是姊妹测试文件自己点名的那一帧**
(`tests/test_tpdeep_recover_band.lua` 断言 `nLion == 1`):
`f_megabundle_051728_ogre_lanefront_deep` **lion,11.2% 血,6 级**。

**这不是姊妹算错了。** 它问的是「**我的**分支开着吗」,答案正确;
它问不出「会不会有**更早的**分支先 return」,因为树里当时没有这个概念。
这是 **GH #606 那一族**(闸址不等于可达性)**往上游挪了一条分支**。

### 为什么偏偏是 `撤退:1` 值得一根杠杆

两件事同时成立,而这个组合是罕见的:

1. **它是四条里最安静的一条。** 它要求 `nEnemyCount == 0` —— **1600 环全空**;
   `回复状态` 容许 1 个,`撤退:3` 也容许 1 个。**它自己的触发条件比任何姊妹都更
   努力地证明「没人在追我」。**
2. **它又是活血量带里唯一完全没有 bag-aware 守卫的。** 它唯一的守卫
   `J.ShouldStayAndRegen`(**PROMOTED,每局 turbo 都活着**)带 `[0.18, 0.75]`,
   而分支自己封顶 `botHP < 0.19` ⇒ **只剩一个百分点的交集**
   (这条算术**不是本轮新发现的**,是 08-29 本组自己写下的更正,已钉在
   `tests/test_tphome_tp_leg_counterfactual.lua`)。而本轮量出来:
   **7 个语料触发帧里有 6 帧在 0.18 以下** —— 也就是在**任何守卫都够不着的地方**。

---

## 2. 落地:`tpquiet`(**未 armed**,FROZEN-HOLD)

- `J.ShouldSipNotTpQuietHome`(`bots/FunLib/jmz_func.lua`)
- 唯一调用点:`撤退:1` 分支(`bots/ability_item_usage_generic.lua`)
- `tests/_tpquiet_sweep.lua`(全语料 manifest)
- `tests/test_tpquiet_shadowed_branch.lua` **15/15**
- `tools/agent/mutstand_tpquiet.sh` **13/13(STAND GREEN)**
- `iterations/state.json:tpquiet_20260908`
- `tests/test_gated_helper_nesting_census.lua` 补一行(见 §5)

**⭐ 新的不是判断,是问这个判断的位置。** 谓词与姊妹 `tpdeep` **逐个常数相同**
(带 `[0.10, 0.18)`、伤害窗 **6.0s 不归因**、环 **2500**、塔 **1200**、
`J.HasFieldRegenSource`)。这不是「抄」,这是**杠杆的全部合法性**:
它必须是**同一个判断**,只是被问在**真正会执行的那条分支上**。
所以「常数与姊妹相同」**不是注释里的一句话,是两个被解析出来的函数之间的算术**
—— 任何一侧漂移,`test_tpquiet_shadowed_branch.lua` 立刻红,杠杆的许可证当场作废。

### 为什么不直接在 `撤退:1` 上调 `J.ShouldDeepSipNotTpRecover`

那是最省事的写法,也是错的:**一个 armed id 会同时推动两个调用点**,
于是**已经排给 `tpdeep` 的那一波读不出结论了** —— verdict 说不出是哪条分支产生的。
一个 id 一个调用点。变异体 **M5**(「顺手也挂到 `回复状态` 上,反正只会更保险」)
就是这条,**被抓**。

### `overlap` 这一列**故意不断言为 0**

姊妹 sweep 断言 `overlap 0`,因为 `tprecov` 与 `tpdeep` **共用调用点**,
那里带不相交是可归因的**前提**。本杠杆**与谁都不共用调用点**,
所以重叠**是结论本身**,不是缺陷。按铁律 4 **(i-a)**,读数**照登记**:

```
both_armed_true                     2
both_armed_true_and_both_triggers   1
```

取而代之要成立的是**源码顺序**:`撤退:1` 在 `回复状态` 触发**之前 return**,
所以两个 id 都 armed 时**只有 `tpquiet` 能改变行为**。
**那是一个关于顺序的事实,不是一个计数** —— 因此**结构钉**,永远不拿计数替身。

### 域与分区(前缀走,**逐位闭合**)

```
t1_trigger 7  =  band_high 1 + band_low 4 + source 0 + damage 0
               + ring 1 + tower 0 + domain 1
```

- `quiet_true_in_t1` = **1**(反事实域 = helper 与分支的**交集**,不是任一因子)
- `quiet_armed_true` = **2**(另一帧在分支触发外,**无行为效果** —— 照实说)
- `quiet_shipped_true` = **0**(未 armed 逐字节相同)
- 方向:`flips 2` / `flip_false_to_true 0`,**换腿**后 `flips_swapped 0` /
  `flip_false_to_true_swapped 2` ⇒ 方向经过一个**被证明会计数**的计数器
- `arm_leak 0`(每次只 arm 一个 id)
- 反真空:`quiet_with_any_tower 4`(`stop_tower 0` 是关于 1200 的读数,不是关于 mock)
- 环边际:`quiet_ring_margin 3`(分支自己的 1600 放行、helper 的 2500 拦下)

**⛔ 两条带沿的域代价都是 0**(`band_low_otherwise_domain 0`、
`band_high_otherwise_domain 0`)⇒ **只能结构钉**(钉在姊妹的常数上),
**不许拿域计数当替身**。这是姊妹 M6/M7 的教训,**在它咬人之前**用上。

---

## 3. ⭐ 变异台的头条:**M10 第一版 SURVIVED,而它教的正是本轮该学的**

顺序钉原本写成:**sweep 计算** `G.T1_RETURN_BEFORE_R4`,**测试断言**它 `== 1`。
M10 把 sweep 里那一行换成字面 `G.T1_RETURN_BEFORE_R4 = 1`,结果:

- **每一个计数逐位不变**(源码顺序不是计数),
- 断言**高高兴兴地读了变异体自己写进去的常数**,
- 整台 **exit 0,SURVIVED**。

⇒ **断言的输入不许由被检查的东西生产。** 顺序改成**在断言它的那个文件里**
从源码求(`order_facts()`),manifest 的副本降级成「**必须与之一致**」。
修好后:M10(把 manifest 改成 `0`)被**一致性检查**抓住。

**⭐⭐ 而补上的 M13 又暴露了朴素写法的第二个洞。** M13 = 把 `撤退:1` 自己的
`return` 删掉(分支改成穿透到 `撤退:2`)。第一版 `order_facts` 用**全文件 `find`**
去定位那个 return —— 但**同一行 `return ... tpLoc ...` 也结尾了 `撤退:2` 和
`撤退:3`**,所以删掉之后 `find` **会被姊妹的 return 满足**,断言照绿。
⇒ return 必须**在分支自己的范围内**找(`[本分支 cast motive, 下一条分支 cast motive)`)。

**⭐⭐⭐ M9 是 GH #550 的正面复现,而且它长得像一次「整理」。**
把本 helper 的两条带行「整理」成姊妹的 inline 写法 —— **行为一字不变**,
所有读数不动 —— 却让 `tools/agent/mutstand_tpdeep.sh` 的 `BAND_HI`/`BAND_LO` 锚
**变成歧义**,而那台把歧义锚当**整台失败**处理 ⇒ **以姊妹的名义中止姊妹的整台台子,
起因是一个跟姊妹毫无关系的改动**。所以本 helper **给常数起名**
(`QUIET_BAND_HI` 等),并把「四条 inline 行在全文件各只出现 1 次」
**钉在做这件事的地方**(`INLINE_* == 1`),而不是钉在会被误归因的地方。

**最终:13/13 —— 12 个变异体全部 CAUGHT,控制项(改注释)OK,零 SURVIVED。**
还原经 `sha256sum -c` 验证,`git status` 干净。

---

## 4. 附带一条(不作主体):**一条恒真的模式排除,而且语料永远证不了它**

`ability_item_usage_generic.lua` 另一条 `撤退:1`(**~5292,拿去补装备那条**,
不是本轮改的那条)里有:

```lua
and (bot:GetActiveMode() ~= BOT_MODE_PUSH_TOWER_TOP
  or bot:GetActiveMode() ~= BOT_MODE_PUSH_TOWER_MID
  or bot:GetActiveMode() ~= BOT_MODE_PUSH_TOWER_BOT
  or bot:GetActiveMode() ~= BOT_MODE_ATTACK)
```

一个值不可能同时等于四个互不相同的常量 ⇒ **这个合取项恒为 `true`**,
即「推塔/进攻时不要回家补装备」这条排除**从来没有生效过**(作者要的是 `and`)。

**⛔ 但它不能作为本轮的主体,理由是算术不是偏好:** 按第 13 条 world assertion
(`tests/test_activemode_world_assertion.lua`),`GetActiveMode()` 不在 `.dem` 流里,
mock 答 `0`,而所有 `BOT_MODE_*` 都 `>= 1001` ⇒ 语料上**四个 `~=` 全为真**,
De Morgan 修正版(`and` 链)**同样恒真**。也就是说 armed 与 unarmed 在
**整个 1021 帧上逐字节相同,域为 0** —— 落地它就是 `stayattr` 那个陷阱
(「在实验室正在跑的串上是空的杠杆」)。**⇒ 开 issue(GH #635 §附带),不落地**,
需要的是实机/dumper 侧的答案,不是又一个 gated no-op。

---

## 5. 顺带结清:census 新增行的身份问题

`test_gated_helper_nesting_census.lua` 当场抓到新的 gate-inside-a-gate:
`tpquiet | J.ShouldSipNotTpQuietHome | J.HasFieldRegenSource | bagsalve`。
它要求先回答「未 armed 时内层 helper 是不是合取的单位元」。
**答案与上面两行逐字相同**:未 armed `bagsalve` 时 `HasFieldRegenSource` 的背包块
被跳过,返回主格扫描结果 —— **additive-only**,只会把 FALSE 变 TRUE,
永远不会拿掉 shipped 扫描已经找到的补给(本组 2026-09-08 早些时候量过:
`src_bag_more 15 / less 0`)。而「外层单独 armed 不是 no-op」**不留作推论**:
sweep 以 `tpquiet` 为**唯一** armed id(`arm_leak 0`)跑完全语料,
读到 `quiet_armed_true 2` 对 `quiet_shipped_true 0`。行已登记,分类 **(A)**。

---

## 6. 验证(铁律 6)

| 门 | 命令 | 读数 |
|---|---|---|
| 静态半 | `bash tools/agent/luacheck_gate.sh` | **exit 0**,`luacheck bots game: 0 warnings` |
| 本轮新文件 | `lua5.1 tests/run_tests.lua tpquiet` | **15 tests, 0 failures** |
| 变异台 | `bash tools/agent/mutstand_tpquiet.sh` | **13/13,STAND GREEN**(exit 0) |
| 姊妹 | `... tpdeep` / `... tprecov` | exit 0 / exit 0(**读数逐位不变**,不是假设,是跑过) |
| 邻居 | `coarmed` 11/0 · `gate_claim` 16/0 · `gated_helper` 15/0 · `tphome` 14/0 · `stayfield` 56/0 · `smoke_load` 3/0 · `retreat_priority` 3/0 · `census` 131/0 |

**动态半的诚实边界**:全量套件(~100min,GH #124)**本轮没有整跑**;
上面是**受影响面的定向子集**,不冒充全绿。
**本轮没有用 `RULE6_BYPASS`。**

---

## 7. ⛔ 下一格(本组下一轮第一项)

1. **主体仍必须是一个 `bots/` 行为改动。**
2. **本轮量出来的缺口 = `both_triggers 6`。** 被遮住的只有 1 帧,但**两条分支触发
   同时开着的有 6 帧** ⇒ 这类 shadow **不止一处**。**同一形状还没查过的**:
   `stayfield` 挂在 `撤退:3` 上,会不会被 `撤退:1`/`撤退:2` 遮住?
   `撤退:2` 与 `回复状态` 之间有没有同样的交叠?
   **先量再改** —— 这一族的读数现在有现成的量具(`_tpquiet_sweep.lua` 的
   两个 `*_trigger` 上界 + S 行),扩它,**不要新建第三个全语料 sweep**。
3. **不要**去改共享地板,**不要**回 `overchase`,**不要**再找
   `J.IsFieldRegenSituation` 的第五条子句(上一轮已封)。
4. 上一轮的 `nosrc_attr_only 0` 语料请求**仍然挂着**(需要一帧:地板以下 + 两手空空
   + 1600 环空 + 3 秒内被一个 3000 外的英雄打过);本轮**没有**去动它,
   因为它写明「第一步是要那一帧,不是先改代码」。

---

## 8. Token 用量(铁律 8)

```
TOKENS total_in=19,146,990 out=119,010 turns=101
```

注:这是**统计时刻为止**的数字,统计之后的收尾回合(commit / push / 开 issue)不计入。
本轮偏高的那一段可定位:**在写第一行代码之前先跑了两次全语料探针**
(先量「这根杠杆有没有域」,再量「姊妹的域是不是被遮住的」),
以及**变异台跑了三遍**(M9 的 bash 引用 bug、M10 SURVIVED 后的返工、M13 的 want 串)。
前者是**故意的顺序**——它把一个本来会落地成空杠杆的设计(`level < 6` 那版,域 **0**)
在写代码前就否掉了;后者是**返工**,其中 M10 那次的产出写进了 §3。
