# 协同组 2026-09-02T16:25Z — 阶梯写反了,第二级永远够不到:落地常设普查 `threshold_chain_census`(4000:1),缺陷登记不修;而拆穿本轮域读数的是本轮自己写的那个测试

**自驱**(`[strategy]` 未认领 issue 仍为零 —— open 的 #400/#406/#411/#415/#422/#426 全是本组自己开的;
owner P1 第 1 棒、P2 均已交出,P3 责任在总监;
**backlog 最上面一条 `0CKPUSH` 逐字交待本组下一轮「`0PUSHCLUSTER` 原样有效,下一个杠杆去别的域挑,
照旧先跑域价钱再选形状」⇒ 本轮就是照这条做的**)。

**零 AWS、S3 零访问、零 EC2;`bots/` 与 `game/` 零 diff。**

---

## 1. 一句话

全仓 4000 条 `if/elseif` 链里,**恰好 1 条**把同一个量的阈值阶梯**写反了方向**,于是第二级
**任何输入都够不到**;把这个形状提成常设普查工具 + 棘轮,**缺陷登记不修**——
因为域价钱说 107 份 fixture 里**一份都没有**能进入那条分支的道具。

## 2. ⭐ 主判据(可复用,超出本主题)

> **一个 `>=` 阶梯必须降序写。升序写时第二级不是「少见」,是 `∅`——
> 它是第一级的严格子集,而 `elseif` 已经把第一级排除掉了。**

这条判据的价值不在它难,在于**本仓每一道门都看不见它**:

| 门 | 为什么瞎 |
|---|---|
| `tests/test_smoke_load.lua` | 它是合法 Lua,加载得干干净净 |
| `luacheck bots game`(铁律 6 静态半) | `.luacheckrc` 是 `only = {"1"}`,只管 1xx 全局访问;死分支不是全局访问 |
| 对局观察 | 唯一症状是一个数**停在低一档**;不崩、不报错 |
| `print()` 调试 | AGENTS.md:`print()` 根本到不了服务器控制台 |

⇒ 它可以**无限期存活**,而且读代码的人会看见两级都写着、于是相信两级都活着。

## 3. 缺陷(可数、不需要帧、不需要跑)

`bots/ability_item_usage_generic.lua:1540-1546`,`X.ConsiderItemDesire["item_blink"]`,
`item_enhancement_keen_eyed` 那一支:

```lua
if J.HasItemInInventory('item_enhancement_keen_eyed') then
    if DotaTime() >= (J.IsModeTurbo() and 7.5*60 or 15*60) then
        nCastRange = nCastRange + 125
    elseif DotaTime() >= (J.IsModeTurbo() and 12.5*60 or 25*60) then   -- ← 够不到
        nCastRange = nCastRange + 135
    end
end
```

`750 >= 450`(turbo)与 `1500 >= 900`(normal)⇒ **两个模式里第二级都是死的**,
跳刀施法距离的估值**永远停在 +125**,`+135` 那一档一次也不会用上。

**形状是可数的**:去注释扫全仓 `bots/**/*.lua`,**4000 条 `if` 链 : 1 条死级**。
三个兄弟分支(`magnifying_monocle` 无条件、`mystical`、`boundless`)都是**单级**,
所以这条是全仓唯一一条多级时间阶梯,**也是唯一一条写反的**——
没有第二条可以拿来说「`* 30`/升序是本仓惯例」。

## 4. ⭐⭐ 域价钱:三个理由各自独立,而**承重的那个不是我第一次以为的那个**

`corpus_hero_census.py` 的逃生口(共享代码 ⇒ 全语料为域)在这里**只买到一半**:
文件确实是共享的 `bots/ability_item_usage_generic.lua`,但**分支自己带道具门**。
逐条量(107 份 fixture,619 帧):

| 条件 | 语料读数 | 是否封死 |
|---|---|---|
| 背包里有 **enhancement** | **0 / 107** | **⭐ 承重** |
| 背包里有跳刀 | 8 / 107(`blink_dagger`) | 否 |
| 帧时刻 ≥ 750s(分开两级所必需) | 2 / 619(最大 790.4s) | 否 |
| **同时**「≥750s」且「带跳刀」 | **0 / 107** | 是(即使白送 enhancement) |

⇒ **条件 (a) 在本语料上按构造买不到** ⇒ 依 `0ARCSCAN` / `0CKTWIN` 先例:**登记,不修**。

## 5. ⛔ 本轮最该被读的一格:**上表里有两行,我第一版写反了,而拆穿它的是我自己刚写的那个测试**

第一版的 §3 断言是「语料里既没有跳刀、也没有任何一帧到得了 750s」。**两条都是假的。**
它们之所以被写下来,是因为两次测量各用错了一个 token:

- 查 `item_blink` —— 但 **fixture 存背包时把 `item_` 前缀剥掉了**(存 `'blink'` 家族,不是 `'item_blink'`);
- 改查 `'blink'` —— 但语料里的拼法是 **`blink_dagger`**,精确匹配又漏掉了。

两次都得到 `0`,两次都**长得像一个干净的读数**。真值是 **8 份 fixture 带跳刀**、
**2 帧到 790.4s**。

**这与 GH #329「读数不是局数」是同一族**(用错的量满足了断言),但这次的教训更窄也更好用:

> **一个「零」在被当成域价钱之前,必须先证明分母不是零 —— 而且要证明的是
> 「这个 token 的确是语料使用的那个拼法」,不是「这个 grep 跑成功了」。**

**处置**:§3 现在**同时钉住正确读数与那两个错误 token**(`test_threshold_chain_census.py`),
并把承重的零单独标出来。变异 **M11 / M12 就是把这两行烂回第一版**,两个都被抓住 ——
所以「这次改对了」不是一句声明,是台子上的两格。

## 6. 产出(全部零行为改动)

| 文件 | 是什么 | 读数 |
|---|---|---|
| `tools/agent/threshold_chain_census.py` | 常设普查:全仓死级扫描,turbo/normal 两腿分别判定 | `SCANNED if-chains 4000` / `FINDINGS 1 (judged 1, new 0)` / exit **0** |
| `tests/test_threshold_chain_census.py` | 四层棘轮 + 反向断言 + 域价钱 | **32/0** |
| `tools/agent/mutstand_deadrung.sh` | 变异台(自带 `trap restore EXIT`) | **12/12 CAUGHT,0 SURVIVED,0 ABORTED** |

工具沿用本仓退出码词汇:**0 干净 / 2 没跑成(`CorpusVanished`)/ 3 有未登记的新发现**。
`JUDGED` 表让**新增**的死级在第一轮就打 `*NEW*` 并把退出码抬到 3。

### 测试的四层(为什么不是「一个数」)

- **LAYER 0 premise**:出厂那两行**确实**是升序写的 —— 断言打在**源文本**上,
  不是打在工具上,否则工具与源可以一起漂走还互相同意。
- **LAYER 1 ratchet**:恰好 1 条、在那个坐标、**两腿都死**,**而且分母同时钉住**
  (`if-chains >= 3500`)—— 扫到空和扫到干净都印 `FINDINGS 0`,不看分母分不开。
- **LAYER 2 reverse**:13 条合成语料钉住 "subsumes" 的定义。**危险方向是假阳性**——
  一条**正确的降序阶梯**被报出来,下一轮就会把能用的阶梯「修」坏。
- **LAYER 3 domain**:§4 的四个读数,**双侧**钉住。语料哪天真长出 enhancement,
  这一层**变红**并说出来 —— 那天正是修复变得可买的那天,**不许它静悄悄地过去**。

### 逐个复核了「抓住的理由」(不是只看 12/12)

| 变异 | 打什么 | **实际**死在哪条 |
|---|---|---|
| **M5** | turbo 三元不再被解析(扫描器仍走完 275 个文件、仍数出 4000 条链) | 死在 **ratchet 的 findings 计数**,**不是**分母 —— 这正是 M4 的分母**抓不到**的那一格 |
| **M6** | subsumption 丢掉等号 | 死在 **`>=` 不是严格**那条反向断言 |
| **M11** | 把跳刀读数烂回本轮第一版的错 token | 死在 **「跳刀确实在语料里,拼作 `blink_dagger`」** |
| **M12** | 把 750s 读数烂回第一版 | 死在 **「语料确实够得到 750s,但只有 2 帧」** |

M1/M2/M3 打出厂缺陷本身(**M1 = 悄悄把阶梯修好**:一条登记不修的杠杆上**无闸的行为改动**,
正是登记要防的漂移;M2 = 直接删掉死级,连证据一起删)。

## 7. 第二条发现:**登记不修**,而且它是本档案第一条**崩溃级**的

`bots/BotLib/hero_ringmaster.lua:497`

```lua
local nCastRange = J.GetProperCastRange(bot, false, TameTheBeasts:GetCastRange())
--                                      ↑     ↑
-- function J.GetProperCastRange( bIgnore, hUnit, abilityCR )   ← 参数整体错位一位
```

**形状可数:229 个调用点,228 个第一参数写 `false`,只有这 1 个写 `bot`** ⇒ **228 : 1**。

**后果不是「判断错」,是硬崩**:`J.GetProperCastRange` 第一行就是
`local attackRng = hUnit:GetAttackRange()`,而这里 `hUnit = false`。lua5.1 实测:

```
canonical  :  true   600
ringmaster :  false  attempt to index local 'hUnit' (a boolean value)
```

⇒ `X.ConsiderTameTheBeastsCrack()` 每次走到这里都抛错,而**引擎的错误处理器是坏的**
(AGENTS.md:`error in error handling` 吞掉全部 Lua 错误文本)⇒ **静默**。

**为什么仍然登记不修**:`corpus_hero_census.py --hero ringmaster` 答
**`DOMAIN-EMPTY files=0 games=0`(exit 3)** ⇒ 条件 (a) 按构造买不到;
且**一次一个杠杆**(`0ROSHTWIN` / `0CKTWIN` 先例)。issue 已开,球交出去。

⭐ 顺带记一条**方法论**:这一处是用另一个形状扫出来的 ——
**「按匈牙利前缀声明为 `b<Name>` 的形参在调用点收到了非布尔值」**。
该扫描 **25447 个可解析调用点**,`b<Name>`/`h<Name>` 两类的残渣只有 9 行,
其中 7 行是命名松散(`nTarget`/`nUnit`/`hItem` 实际收的是句柄/字符串,逐条看过),
**2 行是同一个调用**——就是这一处。**`n<Name>` 那一类在本仓不可用**
(全仓把 `nTarget` 当句柄名用,假阳性 192 条),所以它**没有**被提成常设工具:
一个形状要进 `tools/agent/`,先得在本仓的命名习惯下站得住。

## 8. `0SHAPE4` 之后本轮又扫空的形状(**登记,别再重扫**)

1. **`if/elseif` 链里逐字重复的同一个条件**(死分支):全仓 ⇒ **0**;
2. **数值 for 循环的循环变量在循环体里一次都没被读**:⇒ **3**,全部是合法的「重复 N 次」;
3. **`math.max` 收了一个 cap 命名的操作数 / `math.min` 收了 floor 命名的操作数**:⇒ **3**,
   逐条看过全部正确(`aba_push.lua:175` 的 `math.max(nMaxDesire, 0.95)` 是**故意抬天花板**,
   同一函数里 `math.min(nMaxDesire, 0.35)` 是正确方向的压顶);
4. **在 `pairs(t)` 迭代中对 `t` 自己 `table.remove`**:⇒ **3**,均在 `FretBots/` 与
   `hero_elder_titan.lua`(本组不动别人的域,且两处 domain 未量);
5. **循环体顶层的无条件 `return`/`break`**(扫描只看第一个候选):⇒ **2**,
   两条都是我的块解析器误报(`repeat...until true` 的 continue 惯用法 + 嵌套 `if`),**真发现 0**。

## 9. 开工自检(铁律 10)

- 第一次调用**被工具自己拒绝**:`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.`
  (证据纪律 3;它自报「这个形状已复发 5 次,每次都是当轮第一条命令」)⇒ 改走
  `> file 2>&1; echo EXIT=$?`。**这一条按设计工作,记在这里当第 6 次的分母。**
- 第二次跑完,**真实退出码 3**,两条 trunk 红:
  - **python**:`test_illumove_pairs` 家族(**GH #410**,已开、非本轮)。
  - **lua**:`test_incoming_damage_callsite_census.lua` published 43 → 44。
    ⚠️ **这条在我的工作树上红,在 `origin/main` 上是绿的** —— 我的 checkout 落后
    3 个 commit。`git pull --rebase origin main` 之后 `lua5.1 tests/run_tests.lua
    incoming_damage_callsite` = **6 tests, 0 failures**。
- ⭐ 连带一条给自检工具的读数:`unlanded_commits.py` 把 `8546d71` 报成
  **UNLANDED**,而它其实**已经以 rebase 双胞胎 `7e9e76f2` 落地在 main 上**。
  这**正是该工具 LIMIT 4 自己写明的情形**(「OFF-TRUNK 可能是已改头换面落地的同一份工作」),
  所以**不是 bug、不开 issue**;记在这里只是因为:**读到那一行的下一轮,
  第一件事应该是 `git fetch` 而不是 cherry-pick。** 本轮差一点就去修一条 main 上不存在的红。

## 10. 门(铁律 6)

```
bash tools/agent/luacheck_gate.sh   ->  GATE_EXIT=0  CLEAN
    installed luacheck (it was missing; apt package lua-check, bounded)
    luacheck bots game: 0 warnings
```

**裸读退出码,中间无管道;未用 `RULE6_BYPASS`。**

动态半:本轮 **`bots/`、`game/`、`tests/*.lua` 一个字节都没动**(产出是 2 个 python + 1 个 bash),
⇒ Lua 套件在结构上不受影响。仍跑了两个子集作证:
`test_smoke_load` **3/0**、`incoming_damage_callsite` **6/0**(rebase 后)。
**全量 `lua5.1 tests/run_tests.lua` 本轮没有跑完**(~100min,GH #124)——
**这是跳过,不是通过**。python 全量套件读数见 §12。

## 11. 交棒(球在谁手上)

| 交给谁 | 什么 |
|---|---|
| **总监** | 裁 `keen_eyed` 死级:**登记(现状)** vs **修**(修就是把两级换序,行为改变 = 12.5 turbo 分钟后跳刀施法距离估值 +125 → +135)。⚠️ 修之前请注意条件 (c) 本轮**没有**被买到:Keen Eyed Enhancement 到底给不给这两档、给多少,本会话无法核验 —— 这是**开闸前必须补的一格**。 |
| **录像组** | 若认为该修:语料需要**同时**满足「带 enhancement」的帧。目前 0/107。 |
| **总监 / 英雄组** | `hero_ringmaster.lua:497` 的参数错位(崩溃级,228:1)。ringmaster **DOMAIN-EMPTY**,所以它是「什么时候修」而不是「能不能测」的问题。 |

**本组下一轮**:`0PUSHCLUSTER` **仍原样有效**;`0SHAPE4` + 本轮 §8 的五条**已棘轮,别再重扫**;
继续去别的域挑,照旧先跑域价钱再选形状 —— 本轮给这条添的新论据是:
**域价钱本身也会读错,所以它自己也要有分母。**

## 12. 全量 python 套件(rebase 后重跑,`bash tests/run_py_tests.sh`)

```
75 passed, 1 failed, 1 uncertifiable      PY_EXIT=1
failed:        tests/test_detector_source_constants.py
uncertifiable: tests/test_selfcheck_lua_leg.py
```

- **`test_detector_source_constants.py` = GH #410**,逐字同一条:
  `illumove_pairs:HP_CUT is classified (UNREGISTERED)`。**已开、非本轮、与本轮改动无交集**
  (本轮三个新文件都不碰 `illumove` 也不碰 HP 常数普查),按 `0ROSH` 的撞车教训**不动**。
- **`test_selfcheck_lua_leg.py` = UNCERTIFIABLE 不是 FAIL**:那条腿的 120s 预算在本容器上没跑完
  (`73 file(s) ... in 120.1s`),它自己的 NOTE 行写明**墙钟分不开「集合变大」与「容器变慢」**。
  **这是没跑成,不是通过**。
- 本轮新增的 `tests/test_threshold_chain_census.py` 在这次全量里 **PASS**。

## 13. 本次会话 token 用量

```
TOKENS total_in=14,463,826 out=88,568 turns=98
```

(统计时刻之后的收尾回合不计入。)
