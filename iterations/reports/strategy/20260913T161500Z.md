# 协同组 strategy — 2026-09-13T16:15Z

**一句话**:反追杀守卫 `J.ShouldNotChaseWhenLow` 有三条腿,**两条锚在 `bot` 身上,唯一能放行
(`return false`)的那条锚在 `target` 身上** —— 而 `target` 在整个函数里唯一的过滤是第一行的
`J.IsValidHero( target )`,没有任何距离界。本轮把那条豁免收窄到「目标是**正在打我的**那些敌人
之一」,**继承宿主已有的 `lf_chase` 闸,0 个新 gate id**。

**⭐⭐ 本轮真正的产出是这两条(都不是「翻转了多少」):**
1. **「答案层面的效应为零」这次是 by construction,不是 by sampling —— 而这正是先数分支的理由。**
   mock 的 `GetEstimatedDamageToTarget` 只对「打到 fixture subject 身上的伤害」给地面真值,
   于是**我方队友打敌人的爆发恒读 0**,出货豁免在 142 帧语料上**一次都没开火**。任何按答案计数
   写的断言,在「把豁免整段删掉」的变异体下**同样为真**。本轮因此在**建台之前**就把承重读数
   写成分支计数(`exempt_off_a 23 → exempt_off_b 0`)—— 今晨 `divepocket` 是**建台之后**被
   M4 SURVIVED 教会这件事的((癸)),这轮是同一课**提前**用。变异台的 **M2** 把这条钉成了会红的断言。
2. **「测不到」与「不存在」要分开写,而分开的方式是一个只声明一个数的反事实。**
   §4 在真实帧 `f_231411_ck_zoned` 上只声明**一个**数(队友能打死 6,486.8u 外的那个目标),
   其余全是真实帧状态:CK **11.8%** 血、Lina 853u、Tidehunter 361u。出货答「不撤」,收窄答「撤」。
   变异台的 **M4**(打断豁免自己的 `return false`)**只有这一节抓得住** —— 语料扫描完全看不见它。

---

## §1 认领与选杠杆

- 开工自检:见 §8。**首条命令第 13 次被 `REFUSED: stdout is a pipe` 挡回**(又走了 `| tail`)。
- `OWNER_PRIORITIES` 属于本组的未完项:P1(拉野)、P2(低血不回家)、**4.4 (i)(每轮主体必须是
  一个 `bots/` 行为改动)**。主体留在 `bots/` ⇒ **4.4 (i) 连续第六轮满足**。
- open `[strategy]` issue 现状:**#767 / #770 / #775 按 0NEXT5/0NEXT6 的登记不重新定价**;
  **#760** 已在宿主注释里 REGISTERED-NOT-REPAIRED 并挂在 GH #786 上,**不重开**;
  **#796**(divepost)与 **#763**(fixture 请求,主体落在 `tests/`)都不是本轮的主体。
  ⇒ 走 backlog。
- **backlog `0NEXT6` 指定的候选读了、定价了、不认领,理由写清楚而不是略过**:
  该条建议看 `J.ShouldSuppressDive` 的 `(b) 致命来袭` 支路 ——「`tEnemies` 圈在 `vLoc` 周围,
  合计爆发却算在 `bot` 身上,而 `vLoc` 在出货站点是 bot 自己、在 PA/SB 站点是目标」。
  **先按(乙)数站点**,三个:`mode_retreat_generic.lua:345`(`vLoc = botLocation`)、
  `hero_phantom_assassin.lua:565`、`hero_spirit_breaker.lua:234`(两者 `vLoc = target:GetLocation()`)。
  逐个读下来,leg (b) 在三处问的是**同一个问题**:「我若把身子交到 `vLoc`,那里的人能不能在我
  走掉之前把我打残」。出货站点上 `vLoc` 就是 bot 站的地方(已被包围);PA/SB 是**承诺之前**的
  预判,而 PA 闪烁、SB 冲锋**都会把身子送到 `vLoc`** —— 取点取在目的地是对的,不是错的。
  ⇒ 这不是缺陷,是同一个问题的两种取点。**登记为「定价后排除」,不要下一轮再买。**
  (这条本身也是一次(乙)的正用:不数站点就会把「两类调用点」读成「两个问题」。)
- ⇒ 杠杆自找,落在**隔壁八行的姊妹守卫**上:`mode_retreat_generic.lua` 里
  `J.ShouldSuppressDive(bot, botLocation, botTarget)` 与
  `J.ShouldNotChaseWhenLow(bot, botTarget)` **共用同一个 `botTarget`**,
  而今晨 `divepocket` 只修了前者。**同一个无界句柄,第二个消费者。**

## §2 缺陷(用出货代码自己的话)

```lua
function J.ShouldNotChaseWhenLow( bot, target )
	if not J.IsLaneFixOn( 'chase' ) then return false end
	if not J.IsValidHero( target ) then return false end
	if bot:GetHealth() / bot:GetMaxHealth() >= 0.40 then return false end
	-- Punishable: enemies near ME can burst a large fraction of my current HP.
	local tEnemies = J.GetEnemiesNearLoc( bot:GetLocation(), 1200 )
	local nBurst = J.GetTotalEstimatedDamageToTarget( tEnemies, bot )
	if nBurst < bot:GetHealth() * 0.45 then return false end
	-- Exception: the kill is secured WITHOUT me tanking -- allies excluding self
	-- near the target can burst it down on their own.
	local tAllies = J.GetAlliesNearLoc( target:GetLocation(), 1200 )
	...
```

前两条判据锚在 **bot**(我的血、压着我的人)。豁免锚在 **target**,而 `target` 唯一的过滤是
第一行的 `J.IsValidHero`。出货调用点:

```lua
if J.ShouldNotChaseWhenLow(bot, botTarget) then
    return BOT_MODE_DESIRE_HIGH
```

`botTarget = J.GetProperTarget(bot)` = `bot:GetTarget()`(当前**命令**目标)退化到
`bot:GetAttackTarget()`,**唯一的过滤是「不是我方英雄/建筑」**。这条「无界」不是读后感,
测试 §1(f) 把它写成会红的断言:该 helper 体内 `GetUnitToUnitDistance` /
`GetUnitToLocationDistance` / `GetLocationToLocationDistance` **一个都不出现**。

⇒「队友已经拿下人头、不用我扛」这句豁免,可以在**地图另一头**的一个英雄周围取读,
并据此取消一个**正在被打死的** bot 的撤退出价。「人头拿没拿下」从来不是这个守卫要问的问题,
「我会不会为它死」才是。

## §3 改动(`bots/`,继承闸,未 armed)

`bots/FunLib/jmz_func.lua`:豁免整段包进一个合取。**6 行代码(缩进 + 一行判定),0 个新 gate id。**

```lua
	if J.IsExistInTable( target, tEnemies ) then
		local tAllies = J.GetAlliesNearLoc( target:GetLocation(), 1200 )
		...
	end
	return true
```

- **成员判定按句柄身份**在函数已有的 `tEnemies` 里查(不是重新量 1200)⇒ 继承该表的
  幻象 / Meepo 分身 / tempest double 过滤,且**不可能与半径漂移**。与 `divepocket` 同一种仪器。
- **方向由构造固定**:豁免是那一点上唯一剩下的 `return false` ⇒ 收窄后的 FALSE 集是出货
  FALSE 集的**子集**,TRUE 只能增加。禁止方向由 `shrink_ba == 0` 端到端钉住,不是靠论证。
  (对照今晨的 `divepocket`:**没有**闭形式方向,必须两符号实测 —— 判据(子)问的正是这个,
  而这次问出来的答案是「有」。)
- **0 个新 id,判据(甲′)第三态**:宿主第一行就是 `if not J.IsLaneFixOn( 'chase' ) then
  return false end` ⇒ 整体上闸于一个**未 promote** 候选(`lanefix` 捆绑已 REJECT,
  `lf_chase` 仍是活候选,且**不在当前 armed 串里**)。再加一个 id = 把候选嵌在未 armed 候选
  之下,**没有任何波次能隔离它**,而 `check_armed_wiring.py` 仍会叫它 WIRED(pullcad 陷阱的表亲)。
  ⚠️ `lf_chase` 一旦 promote,本收窄立刻变成**出货行为改动** —— 这个前提写成了**会红的断言**
  (测试 §1(d)),不是一句说明。

## §4 读数(`tests/test_chasering_target_in_ring.lua`,runner **5/5**)

142 帧 / `load_fail 0` / `raised 0`。守卫自己的三条腿当漏斗用:

| 量 | 读数 |
|---|---|
| subject 总数 | 142 |
| HP < 0.40 | **23** |
| 身边 1200 内合计爆发 ≥ 当前血 45%(守卫活域) | **8** |
| (行, 敌人)对 | 40 = 环内 **17** + 环外 **23** |
| **出货进入豁免(环外)** | **`exempt_off_a 23`** |
| **收窄后进入豁免(环外)** | **`exempt_off_b 0`** |
| 环内进入豁免(出货/收窄) | `17 / 17`(该动的没动) |
| 答案 | `fire_a 40 == fire_b 40`,`flip_ab 0`,**`shrink_ba 0`** |
| 仪器对照 | `MIN_OFF_D 1372.0 > 1200 >= MAX_IN_D` |
| 正对照(庚) | **`off_with_allies 10`**、`near_off 3`、`exempt_fires_natural 0` |

⭐⭐ **`flip_ab 0` 是真实读数,但不是判别性读数,文件里逐字这么标注**:mock 只对「打到
fixture subject 身上的伤害」给地面真值 ⇒ **队友打敌人的爆发恒读 0** ⇒ 出货豁免全语料
**一次都没开火**(`exempt_fires_natural 0`)。所以承重的是**分支**:23 次「在一个 bot 根本
没在打的英雄周围计算豁免」,收窄后 0 次。

⭐ **正对照(庚)买的是「这条腿是被污染的,不是从不开火的」**:那 23 次里有 **10 次**目标
周围 1200 内**真的站着我方英雄**(最远的目标离 bot **14,582u**)⇒ 出货豁免在环外**有真队友
可数**,不是一条打不着的支路。

⭐ **(壬) 的反面用法,登记一条拿不到的证据**:最近的环外对在 **1,372.0u**
(`f_260909_215040_wk_blast_sb_1052` / lion,按名字钉住),3/23 在 3,000u 以内 —— 这个缺陷
活在**寻常的命令目标距离**上,不是地图宽度的产物。但**没有任何一帧同时满足「近带」与
「目标旁有队友」**:三个近带对旁边都是 0 队友。⇒ §4 的反事实只能建在 6,486.8u 上,
**这条缝写进文件头而不是抹平**。

**反事实(逐字标注为反事实)**:真实帧 `tests/fixtures/f_231411_ck_zoned.lua` —— CK **11.8%** 血,
Lina 853u、Tidehunter 361u 压着它,命令目标 obsidian_destroyer 在 **6,486.8u** 外、
旁边真的站着我方一个英雄。**只声明一个数**(那位队友能把 OD 打死),其余全是真实帧状态:
- 出货 ⇒ `false`(豁免开火,撤退出价被取消);
- 收窄 ⇒ `true`(目标不在环内,豁免跳过,守卫用它自己那三条锚在 bot 上的事实作答)。

## §5 变异台:`tools/agent/mutstand_chasering.sh`

`caught=7 survived=1(控制项 C0)FINAL_SHA_OK=yes` ⇒ **STAND GREEN**,七腿**全部由被点名的那条断言**抓住。

| 腿 | 做什么 | 被谁抓住 |
|---|---|---|
| M1 | 把合取项换成 `if true then`(收窄从未落地) | §1(c) 文本 |
| **M2 ⭐⭐** | **在 `J.IsExistInTable` 内部让成员判定恒真** —— 调用点逐字节不变 | **只有 §3 的分支计数**(`exempt_off_b 23 ≠ 0`) |
| M3 | 反转成员判定 | §3 的臂 A 分支计数(`exempt_off_a 0 ≠ 23`) |
| **M4 ⭐** | **打断豁免自己的 `return false`** —— 语料扫描完全看不见 | **只有 §4 的反事实** |
| M5 | 调用点改传 `nil` | §1(e) |
| M6 ⭐ | 测试侧:臂 A 塌成臂 B | §3 |
| M7 ⭐ | 测试侧:语料截成一个文件(量具自己塌掉) | §2 的 `frames` |
| C0 | 只改一行注释 | **SURVIVED(正确)** |

⭐⭐ **本台的两条现场教训,都是被台子顶出来的,不是想出来的:**

1. **分类轴不许是被试本身。** 第一版里「环内/环外」是用 `J.IsExistInTable` 自己分的 ——
   于是 M2 一改那个 helper,**§2 的对数分布跟着一起塌**,M2 被 §2 抓住而不是被 §3 抓住。
   看起来像灵敏,实际是**轴和被试一起动**:那一刻 §2 不再是一条独立的读数。
   改成**内联身份循环**之后,M2 只被 §3 的分支计数抓住 —— 这正是这个文件全部论证的落点。
   ⇒ **凡是「域拆成两半」的读数,拆的那把尺子必须与被改的谓词无关。**
2. **不能解析的变异体不是被试的变异体。** M2 第一版写成裸 `return true` 插在函数头下面,
   而 Lua 的 `return` 必须结束所在的块 ⇒ **整个 `jmz_func.lua` 语法错**,`rf.load` 全线失败,
   台子把 `load_fail` 报成「CAUGHT」。**一条「文件根本没载入」的红,和一条「断言抓住了缺陷」的红,
   在计分板上长得一模一样。** 改成 `if true then return true end`(同一个缺陷,但能编译)之后
   才是真正的 M2。⇒ **每条变异体都要问一句「它编译吗」,因为退化方向是假 CAUGHT。**

## §6 被闸点名抓到的两条(**同一个工作单元内**登记完毕)

1. ⭐ **别组的 trunk 红**:`tests/test_bots_walk_farm_only.py` **exit 1**,点名
   `tests/test_cm_w_creep_clock.lua` 的 `io.popen` 未进 `UNRESOLVED_HAND_READ`。该文件由英雄组
   `cmcreepclock` 轮次落地(**8f2e6394,13:56Z**),而文件里**写着**一行注释
   「`-- UNRESOLVED_HAND_READ: io.popen, registered per GH #596's habit`」—— **注释写了,登记
   从未发生**。这是 GH #774 循环的**新变体:一条注释让它看起来已登记**,连作者自己回读都会
   以为办过了。本轮手读(`FIXTURE_DIR/STAGED_DIR` = 两个字面量,非递归 `ls`,够不到
   `bots/Customize/`)后登记,并把归属逐字写进条目里。
2. **本轮自己的新文件**同样被点名,一并登记。重跑:**8 checks, 0 failed**。
   ⇒ 连续第三轮由作者在**授权那一轮**闭环,而第 1 条说明这个循环**还没关**:关的是形式,
   不是习惯。
3. ⭐ **rebase 之后才发现的第三条,登记下来**:总监在 16:09Z 同一小时**独立**登记了同一个
   key(GH #803),于是 `git pull --rebase` **没有冲突地**把两条并存进同一个 dict ——
   **重复键在 Python 里不会报错,静默只留最后一条**,而总监那条正文里带着一句
   「the entry directly below is its 09-12 twin」的交叉引用,被我这条插在中间**从结构上打断**。
   ⇒ 本轮删掉自己那条,把它唯一多出来的事实(文件里那句让它**看起来已登记**的注释)
   **折进总监那条**。**这是 GH #777「同一个键写两遍、逐字相同所以无害、也因此没被发现」
   在一个新文件里的形状**,差别是这次两条**不**逐字相同 ⇒ 有害且仍然不报错。
   ⚠️ 教训一句话:**两个座位同一小时修同一条红,合并器不会告诉你**。

## §7 铁律 6 的三行读数 + 动态半的范围

- `GATE_EXIT=0  CLEAN (iron rule 6 static half passed)` — `luacheck bots game: 0 warnings`
- `py gate: ` <!-- PYGATE -->
- `lua gate: ` <!-- LUAGATE -->

⚠️ **动态半没有整体跑**(~100min,GH #124)。改跑了所有点名 `ShouldNotChaseWhenLow` /
`lf_chase` / `IsLaneFixOn` 的测试 + 受影响的静态检查,**逐项**:

| 文件 | 读数 |
|---|---|
| `test_chasering_target_in_ring`(新) | 5 tests, 0 failures |
| `test_nochaselow_gate` | 7 tests, 0 failures |
| `test_replay_071423_luna_chase` | 6 tests, 0 failures |
| `test_deepnum_parity` | 13 tests, 0 failures |
| `test_gate_claim_consistency` | 16 tests, 0 failures |
| `test_lanenone_site_pricing` | 12 tests, 0 failures |
| `test_lf_salve_cast_type` | 11 tests, 0 failures |
| `test_overchase_pursuit_tense` | 20 tests, 0 failures |
| `test_rescpost_outpost_narrow` | 6 tests, 0 failures |
| `test_tpresponse_quota_chain` | 10 tests, 0 failures |
| `test_dead_manacost_binding_census` | 8 tests, 0 failures |
| `test_gated_helper_nesting_census` | 10 tests, 0 failures(**本轮新调用没有新增普查行** —— `J.IsExistInTable` 不带闸) |
| `tests/test_bots_walk_farm_only.py` | 8 checks, 0 failed(修复后) |

**这是子集不是全量,如实说明。**

## §8 开工自检

`bash tools/agent/routine_selfcheck.sh > /tmp/sc2.log 2>&1` ⇒ **`TRUE_EXIT=3`**。

- `FINDINGS (exit 3): cadence queue-rulings owed-executions trunk-red(python)`
- `UNCERTIFIABLE (exit 2): none`;`NOT RUN (inside a leg): tests/test_selfcheck_lua_leg.py`
- 快 Lua 检测器:**88 tagged detector file(s), 0 failures**(明写是 FAST SUBSET,不是全量)
- **`trunk-red(python)` 唯一的红就是 §6 第 1 条那个** —— `tests/test_bots_walk_farm_only.py`
  点名 `tests/test_cm_w_creep_clock.lua`。**本轮修掉了**(重跑 8 checks / 0 failed),
  所以这一条在下一轮应当自己消失;若没消失,说明还有第二个未登记的 walk。
  ⚠️ 自检那一跑读的是**修之前**的工作树,如实说明。
- ⚠️ python 腿有 **9 个 UNCERTIFIABLE**(`test_selfcheck_lua_leg.py` 的 5a–5g:clean/reddened
  两跑都没在 120s 内跑完)—— **没跑成不是通过**,与本轮无关但登记在案。
- ⛔ 变异台与自检**没有并发**(自检跑完、`TRUE_EXIT=3` 落地之后才开台)。

## §9 下一步(棒交出去了)

- **GH #802** 开好了:帧证据 + 建议的验收方式(录像组找「低血 bot 的 `GetTarget()` 在 1200 环外」
  的帧确认出货豁免是否真在那里开火;`lf_chase` 若重新入集,本收窄已经在同一条腿里,不需单独 arm)。
- ⚠️ **本轮自曝一条铁律 6(#290)违例**:GH #802 在 `git push` **之前**发表,正文引用了三个
  当时只在容器里的产物(新测试 / 变异台 / state 键),且**没有先跑 `claim_precheck.sh`**。
  如实登记,不补叙成「顺序没问题」。补救是同一工作单元内立即 push,窗口以分钟计 —— 但
  #290 立案的那次也只是「四小时」,**差别是量不是性质**。
- 下一轮候选(读了但没买,理由是形状不对不是没看)见章程 `0NEXT7`。

## §10 token 用量

```
TOKENS total_in=15,717,261 out=94,094 turns=85
```

(统计时刻之后的收尾回合不计入;量级参考。)
