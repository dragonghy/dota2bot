# 协同组 2026-09-16T13:48Z — 「解开它什么都不用买」是真的,而它后面没有杠杆

**出口**:P4.4 **(ii)**(一个判定完结所需的最后一块证据)。⛔ **4.4 (i) 的连续六轮
`bots/` 主体本轮断了,这是有意的,理由写在 §4** —— 本轮量出来的唯一候选杠杆域是 **0**,
而 0NEXT28 (酉) 刚刚立过「把运算符一改就当 gated fix 落地会是一次诚实性事故」。
为了续一条连胜而落一个域 0 的 gated fix,正是那一条禁的事。

**铁律 9**:`OWNER_PRIORITIES.md` 里 **P2「当前球在:协同组」** ⇒ 做 P2,
**不走章程 1b,不取 backlog 0NEXT29**。⚠️ 这一步差点又错:0NEXT29 写着「下一轮第一项」,
而章程工作流第 1 步的警示段(2026-09-13T22:45Z 补)逐字预言了这次 ——
「两条互相加固,而它们都排在铁律 9 之下」。**本轮 0NEXT29 一格未动。**

**成本**:零 EC2 / 零 CE / **S3 读取 0 个对象**(全程只读本地语料与仓库)。
**未提入集**(P4.2 冻结,armed 25 > 20)。

---

## 1. 头条:P2 的 TP 腿不是「卡住」,是**已经做完了**,而球早就不在本组

`stayfield`(TP 腿)2026-09-14 按 RULING 37 退集,处置写的是 **CALLSITE-EMPTY**,
并附一句 **「⇒ 解开它**什么都不用买**」**。本轮把那句话当作待验证命题去量,结论是:

> **那句话对「成本」成立,对「有东西可解」不成立。** 撤退:3 的域之所以是空的,
> **压倒性地由一个把 bot 往安全侧推的合取项造成**,解开它会**制造** owner P2 禁止的
> 病例,而不是消除它。

### 1.1 漏斗,逐合取项(`tests/_stayfield_tpleg_sweep.lua`,1039 帧,live 25 串)

复跑完全复现档案里的头条(`solo_S 24 / live_S 2 / branch_open 4 / margin_solo 1 /
margin_live 0`),**档案没错**。本轮新增的是它**没有**的那一层 —— 76 → 4 是**谁**吃掉的:

| 关掉它的合取项 | 帧数 | 方向 |
|---|---|---|
| `bot:GetLevel() >= 9` | **59** (78%) | ⬅ **安全侧**(不发车) |
| `nEnemyCount <= 1` | 7 | ⬅ 安全侧 |
| `#nAttackAllyList == 0` | 4 | ⬅ 安全侧 |
| `nAllyCount <= 2` | 1 | ⬅ 安全侧 |
| 八个 heal modifier 否决 | 1 | ⬅ 安全侧 |
| `itemFlask == nil` / 英雄名 / `GetAttackTarget()` | 0 / 0 / 0 | — |
| **全部通过(= `branch_open`)** | **4** | |

`59 + 7 + 4 + 1 + 1 + 0 + 0 + 0 + 4 = 76` —— 与 trigger 集**逐位相等**,
这是 sweep「每个 trigger 帧都发一行」契约的**恒等式形式**,也是反真空那一列。

⭐ **方向是构造,不是口味**:这八项全是 `and` 进一个**函数体就是回家**
(`tpLoc = J.GetTeamFountain()`)的分支头 ⇒ **任一项读 FALSE 都是「不回家」**。
所以吃掉 78% 的那一项**松开之后多出来的是 TP 回家,不是留在野区**。
⇒ **撤退:3 不需要杠杆,而且它现在的样子是对的。**

### 1.2 四个幸存帧,逐帧关掉,**四个不同的理由**

一个收在 4 的漏斗,很容易被写成「基本是空的」而不去看那 4 个。看了:

| 帧 | HP/等级 | 关它的理由 |
|---|---|---|
| `f_181441` zuus | 0.158 / 12 | **`item_empty_bottle`**,`HasFieldRegenSource=false`(没东西喝)**+** 低于 0.18 地板 —— 两个独立理由 |
| `f_260820_103630` lina | 0.257 / 14 | 1200 内**真有敌方塔**(危险撤退合法,P2 自己豁免)**+** 它的 `item_faerie_fire` **在背包里** |
| `f_631400` centaur | 0.096 / 11 | 没东西喝 **+** 低于 0.10 算术下界 |
| `f_260822_063722` lina | 0.318 / 9 | **P2 自己的铁证帧**:`solo_S=true`、`live_S=false`、`SipValue=85` ⇒ **只**被 `fieldsip` 量级条款关掉;slot 1 = `item_magic_wand` ⇒ **85 是下界不是测量值**,`UNCERTIFIABLE`(RULING 37 / `owed_executions.json:wandlimbo_charge_instrument`) |

⇒ **四个里只有一个的关闭理由是仪器缺口**,其余三个是**真的不该拦**。

### 1.3 于是 P2 完成定义 1 的账要这样算

P2 完成定义 1 要「决策侧新 gated id(含 **TP** 和**步行回泉**两种回家路径)」。
按源码点名,本组**已经写完了**,而且都带 fixture:

| 回家路径 | 分支 / 文件 | id | 状态 |
|---|---|---|---|
| TP(撤退:3) | `ability_item_usage_generic` | `stayfield` | 退集(域 0,**且本轮证明不该有域**) |
| TP(回复状态,**语料真正到达的那条**,31 帧) | 同上 | `tprecov` [0.18,∞)、`tpdeep` [0.10,0.18) | 写完,**未 armed** |
| 步行回泉 | `mode_retreat_generic` | `stayfield2` | **armed** |
| 背包补给可达性 | `mode_team_roam_generic` | `bagtango`(GH #734) | 写完,**未 armed** |

⛔ **`tprecov` / `tpdeep` / `bagtango` 三个都不在 armed 串里** —— 不是因为被拒,
是 **P4.2 入集冻结**(armed 25 > 20)。而 P2 完成定义 3 要的 (a) 证据,
**必须先入集才买得到**。

⇒ **P2「当前球在:协同组」这一行,按今天的源码是错的。** 本组该交的都交了;
卡住 P2 的是 **入集冻结(总监)** 与 **取证波(批测台)**,
外加铁证帧那一格的**充能仪器**。这一句是本轮交给总监的判定完结素材
(⚠️ `OWNER_PRIORITIES.md` 本组**不得增删**,所以只写在这里 + `state.json`)。

---

## 2. 一个附带发现:`bagsalve` 的背包放宽**只能放宽到 flask**,而正确的放宽方式是发船不是改谓词

`f_260820_103630` 的 lina **身上有 `item_faerie_fire`,在 slot 6(背包)**,
`HasFieldRegenSource` 答 false。这看起来像个可以顺手放宽的地方 —— **不是**。
`jmz_func.lua` 自己的注释已经把账算清:背包里的 tango/faerie_fire/bottle
**没有任何 shipped 的换手器**,所以把它们数进来 = 「让 bot 拿着喝不到的东西留在野区」。
⭐ 而**诚实的放宽方式已经在仓库里了**:`bagtango`(GH #734)的
`TrySwapInvItemForFieldRegen()` 把背包里的补给**换进主格**,
即**让它变得可达**,而不是**假装它可达**。本轮把这条钉成断言(§3 M6)。

---

## 3. 本地验证:`tests/test_t3_funnel_safe_side.lua`(新,11 例全绿)

不重算普查(sweep 才是仪器);每次跑**重新推导**三件事:
(甲) 那些计数**必须满足的算术**(漏斗和 = trigger;`stop_open == branch_open`;
等级项 > 其余项之和);(乙) **方向**,从源码读(八个合取项都在 `and` 链上、
函数体是回家);(丙) **四个帧,真的驱动**。

**变异台(10 发,9 CAUGHT,1 声明越界)**:

| # | 变异 | 结果 |
|---|---|---|
| M1 | `FUNNEL.lvl` 59→5 | CAUGHT ×2 |
| M2 | `FUNNEL.open` 4→3 | CAUGHT ×2 |
| M3 | 撤退:3 头里删掉 `GetLevel() >= 9` | CAUGHT |
| M4 | 函数体改成不落泉水 | CAUGHT |
| M5 | `HasFieldRegenSource` 把**空瓶**算成补给 | CAUGHT ×2 |
| M6 | `bagsalve` 背包放宽到 faerie_fire | ⛔ **第一次 SURVIVED** → 见下 |
| M7 | `bagtango` 换手器去掉 gate | CAUGHT |
| M8 | `item_faerie_fire` 85→300 | CAUGHT |
| M9 | 放宽 `FieldRegenSipValue` **自己**的背包腿 | **越界,已声明** |
| M10 | 已登记的 `ls` 走查改成递归 `find` | CAUGHT ×2(双向:还报「登记的调用点不存在了」) |

⭐⭐ **M6 是本轮真正学到的东西,而且它是 evidence-discipline 第 2 条的现场**
(「变异活下来时,先怀疑断言」):
我写的断言是 `HasFieldRegenSource(lina) == false`,失败信息里**责怪的是放宽**,
**而它当时其实在测「`bagsalve` 没 armed」** —— 背包循环整个没进去,
所以把放宽从 flask 改成 faerie_fire **一个字都不影响**。
📌 **可迁移句**:**一个挂在 gate 后面的谓词,它的断言默认测的是「gate 关着」,不是「谓词对」。**
源码注释的原话是那个放宽「must leave every one of them FALSE」(55 个背包帧)——
那是**关于放宽**的命题,所以必须**在放宽真的会跑的那个世界里驱动**。
改法:同一帧再驱动一次 `armed() .. ',bagsalve'`。之后 M6 **CAUGHT**。

⚠️ **另一个读数丢失事故(本轮第二次踩「零输出长得像通过」)**:
第一版探针 `lua5.1 probe.lua` **EXIT=0、零行输出**,我差点读成「四帧都没答案」。
原因:`tests/mock/bot_api.lua:415` 把 `print` 置成空函数
(`G.print = function() end`,为了测试输出干净)。⇒ **在 mock 世界里探针必须写
`io.stdout:write`**;`print` 的静默**与「没跑」同形**。
(同族:`routine_selfcheck.sh` 本轮第一条命令就因为我接了 `| tail` 而 **REFUSED**,
它自己数到这是**第 5 次**复发。两次都是「读数经过一根管子就没了」。)

---

## 4. 为什么本轮**没有** `bots/` 行为改动(4.4 (i) 断档,明写)

按 §1.1/§1.2,撤退:3 上**唯一**在语料里有形状的候选杠杆是「把 `tpdeep` 的
[0.10,0.18) 带照搬到撤退:3」。本轮把它量了:该带里的 `branch_open` 帧只有
zuus 0.158(**`empty_bottle`,没东西喝**)与 centaur 0.096(**低于 0.10,出带**)
⇒ **域 = 0**。落一个域 0 的 gated fix 会满足 4.4 (i) 的字面,
同时满足 `test_stayfield_callsite_domain` 立的那个陷阱(「在实验室正在跑的串上是空的杠杆」)。
**不落。** 本轮主体按 4.4 **(ii)** 结账。

---

## 5. 交出去的棒(三条)

1. **总监**:`state.json:t3_funnel_20260916`。两件事 ——
   (a) P2 完成定义 1 **按源码已满足**,「当前球在:协同组」建议改为
   **总监(入集)/ 批测台(取证波)**(本组不得改该文件);
   (b) `stayfield` 的 CALLSITE-EMPTY 处置**建议补一句**:域空的成因是
   `GetLevel() >= 9`(59/76)且**方向在安全侧**,所以「解开它什么都不用买」
   **后面没有杠杆** —— 与「看见了、是空的」不矛盾,但阻止下一个人去解它。
2. **批测台 / 录像组**:`queue.json:strategy-54`(新)——
   ⚠️ 与 `strategy-51/52/53` + **GH #837** 是**同一堵仪器墙**:
   dumper 的每个 slot 需要 **`charges`** 一个字段。本轮把那堵墙的**宽度**量出来了:
   它不止挡魔棒(`wandlimbo`),**bottle 与 tango 的充能同样不在 dump 里**
   ⇒ `FieldRegenSipValue` 的 bottle 腿在**任何 fixture 里永远答不了 TRUE**
   (mock 的 `GetCurrentCharges` 默认 0,源码注释已声明)。
   **一个字段解三条腿**,顺手一起量最省。
3. **英雄组(知悉,不用回头做)**:`tests/test_bots_walk_farm_only.py` 的 trunk 红
   本轮修了(见 §6)。**它的失效方式值得记**:红的原因不是没人知道,
   `test_lion_hex_panic_level.lua:119-120` **自己的注释**就写着
   「this file belongs on the hand-read list of tests/test_bots_walk_farm_only.py (GH #774)」,
   **然后照样落了地**。⇒ **一句点名义务的注释,不是那个义务被履行。**
   而 `py_gate.py` **不覆盖这个文件** ⇒ 没有任何 push 钩子会拒它,
   于是它由**下一个开工的组**在 开工自检 里发现(GH #624/#806 同族)。

---

## 6. 铁律 6 三条腿 + 附带的 trunk 修复

- `GATE_EXIT=0` — `luacheck bots game: 0 warnings`(**CLEAN**)
- `py gate: 127 ran, 0 findings, 0 uncertifiable, 59.1s`(预算 90.0s)
- `lua gate: 403 ran, 3 findings, 0 uncertifiable, 10 unanswered, 6 known-red, 788.0s`
  (预算 520.0s;本容器 1.45x 于 manifest 定价,且 81 个不在 manifest 的新测试被照跑了,
  占 416.28s —— 这是**预算超支的成因**,不是我的改动)
  ⚠️ **那 3 个 findings 不是 trunk 红,逐字核实过**:`bbfight_turbo_respawn_ceiling` /
  `bbrespawn_double_subtract` / `bbshort_turbo_respawn_floor` 三个的失败都是
  **setup error**,内容一字不差:`bots/Customize/soak_side.lua already exists ... Contents:
  return { side = 'dire', cand = 'siegecap' }` —— 即**同一轮里另一个测试写了那个全局
  gate 文件没清掉**(`test_siegecap_ancient_roster.lua` 也在本轮「不在 manifest 却照跑」
  的 81 个里)。串行单跑四个文件(含 siegecap)**全绿 20 / 16 / 21 / 20**,
  且 `bots/Customize/soak_side.lua` **现已不存在**(gitignored,`.gitignore:76`)。
  ⇒ 这是**一个全局可变文件 + 一个会写它的测试**造成的**非确定性**红,
  它会**随机拒绝任何人的 push**,值得单独立案(本轮只登记,不改别组的测试)。
- ⛔ **没有用 `RULE6_BYPASS`**。
- **开工自检 `SELFCHECK_EXIT=3`**(有发现),findings 逐字:
  `cadence queue-rulings owed-executions lua-coverage trunk-red(python)`。
  ⚠️ 其中 **`trunk-red(python)` 本轮由本组修掉**:
  `tests/test_bots_walk_farm_only.py` 8 checks / 0 failed(修前 EXIT=1)。
  修法是那条失败信息**自己开的方子**(把走查登进 `UNRESOLVED_HAND_READ`),
  并先核实安全性:两个目录是 `'tests/fixtures'`/`'tests/frames'` 两个文件域字面量、
  只赋值一次,且**裸 `ls` 不递归**,够不到 `bots/Customize/` ——
  与已登记的 `test_wk_reserve_rank_blind.lua` **同形**。
  M10 证明这条登记**没有把整个文件消音**(键是命令原文,且该普查**双向**:
  登记了而调用点消失也会红)。
  ⚠️ 自检另有 **9 条 UNCERTIFIABLE**(它自己的 Lua 腿在本容器 120s 内没跑完)——
  **那不是通过**,本轮没有据此声称任何事。

---

## 7. 收尾补录(push 之后的读数)

### 7.1 铁律 6 三条腿 —— **钩子自己打出来的那一份**(权威读数)

`git push -u origin claude/intelligent-hopper-5888ck` 与 `git push origin HEAD:main`
各跑一次钩子,三条腿逐字:

```
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 127 ran, 0 findings, 0 uncertifiable, 60.4s
lua gate: 403 ran, 0 findings, 0 uncertifiable, 10 unanswered, 6 known-red, 816.9s
```

⛔ **没有用 `RULE6_BYPASS`**。落地:`10bcc31a..79aa9e22 HEAD -> main`。

⭐⭐ **这一份读数把 §6 里那 3 个 findings 的归因坐实了**:**同一批 403 个测试,
同一棵树,findings 从 3 变成 0**,而中间我**没有改任何一个测试**——
⇒ 那 3 个红**确实**是 `bots/Customize/soak_side.lua` 被泄漏的**非确定性**红,
不是 trunk 红。📌 **值得单独立案的形状**:一个**全局可变文件** + 一个**会写它的测试**
= **会随机拒绝任何人 push 的闸**。它今天没拒我,是因为第二次跑碰巧没撞上;
**"碰巧没撞上" 不是修好了**。

⚠️ 另一条顺带读数(同样不是我的改动):钩子这一跑里 **81 个不在 manifest 的新测试被照跑了,
占 423.62s**,于是 `lua gate` 的真实成本 **816.9s vs 预算 520.0s**;
另有 **10 个新测试因超预算被 EXCLUDED**(含 `test_stayfield2_live_domain.lua` ——
**P2 一族自己的域测试之一现在在闸外**)。建议总监按 GH #616 约束 1 重测一次
(`python3 tools/agent/lua_gate_measure.py`)。

### 7.2 引用自检(铁律 6 ⭐ GH #290:**push 排在发表引用之前**)

- push **之前**:`claim_precheck.sh` **EXIT=3**,逐字点名两条
  (`tests/test_t3_funnel_safe_side.lua` / `state.json:t3_funnel_20260916`
  "no such key at origin/main")—— **这正是它该做的**,本轮**没有**在那个状态下发表任何引用。
- push **之后**:`PRECHECK_EXIT=0` / `clean` / `OK to publish: every citation
  resolves on origin/main`。唯一的 `IGNORED-BY-DESIGN` 是 `bots/Customize/soak_side.lua`
  (被 trunk 自己的 `.gitignore` 匹配 —— **故意不存在,不是没推**)。

### 7.3 Token 用量

```
TOKENS total_in=12,611,101 out=90,643 turns=79
```

(铁律 8:这是"到统计时刻为止"的数字,本节之后的收尾回合不计入。)
