# 协同组 2026-08-22T01:30Z —— 角色债治疗第四帧:`f_260820_103216_cm_es_aftershock`

**`bots/` 一位没动,`tests/mock/` 一位没动,不产 gate,不申请入集,不提批测请求,零 EC2 支出。**

## 0. 一句话

上一轮(2026-08-21T07:40Z)治好这对镜像帧中的一半 `f_260820_102645_cm_es_reach`,
读出来的是 **CONTROL**:主角 CM 恰好是置换的不动点(槽位 5 = 抽签 5),
所以那条路上**唯一一次角色读取**(出装表 key)没动。
**本轮治另一半,拿到的正是它缺的那个 EXPERIMENT** —— 同一个 seed(906)、另一侧(dire)、
主角**不是**不动点(槽位 4 → 抽签 5),**出装表 key 真的从 `pos_4` 移到 `pos_5`**;
而且这一次**不用推理**:**这一帧自己的背包直接证伪了治疗前那个答案**。

## 1. 认领依据

`[strategy]` open issue 里最新的一条是 #72(录像检查组 22:46Z 的 §AG.2 分解),
其结论是「NOT-PROMOTE 不变、可引用范围收窄」,§6 的四条处置全部落在检测器与总监裁量上,
**没有留下本组可动的杠杆**(§6.5 唯一没试过的路要 ≫4 seed 批测)。
⇒ 按章程回到 backlog:第 0i 条(等级门普查)已于 22:00Z 关闭并写明
「下一条工作单元不再走等级门这条线 —— 回到 0b/0c 的逐帧治疗队列或 0a」,
0a 仍卡在「打到基地的局」语料。**取 0c 队列点名的下一帧。**

开工时 `git ls-remote origin main` = `450c490`,与本地 HEAD 一致(backlog 0m 的做法)。

## 2. 治疗本身

```
s3://…/spot_20260820_101145_1_75646608…/20260820_103216_slot1.dem        (8.7 MiB)
                                        …/20260820_103216_slot1.analysis.json
behav-dump: get_dumper.sh → cache HIT(key a2b8788c…),没有本地构建
make_fixture.py tl_103216.json --t 473.5 --hero crystal_maiden [--roles …]
```

- **先不带 `--roles` 重生成一遍,与仓库里那份 `diff` 逐字节相同** ——
  这是本轮唯一能证明「后面那份差异只来自 `--roles`」的一步,不是走过场:
  它同时说明这份 `.dem` → timeline → fixture 的链路在两天后仍然可复现。
- 带 `--roles` 之后,**与原文件的差异恰好是新增的 12 行 `roles` 表**,其余一位不动
  (`observed.burst` zuus 152 / earthshaker 98、`died_after = 1.0` 全部重现)。
- 生成器**没有**吐 `ground_truth_ambiguous` / `recent_damage_ambiguous`
  ⇒ backlog 0e(幻象伤害混入 burst)的判据在这一帧上是干净的。
- `script_version = mirror:…:s906:dire`,**与上一轮那帧同 seed、异侧**。

## 3. 世界动了什么(实测,不是推断)

| 槽位 | 英雄 | 治疗前(槽位派生) | 治疗后(抽签) | `IsCore` 翻面 |
|---|---|---|---|---|
| 1 | viper | 1 | **2** | 否 |
| 2 | phantom_assassin | 2 | **1** | 否 |
| 3 | sniper | 3 | **4** | **是(核心→辅助)** |
| 4 | **crystal_maiden(主角)** | 4 | **5** | 否 |
| 5 | skeleton_king | 5 | **3** | **是(辅助→核心)** |

**五个全动,翻两面。** 关键对照:**上一轮那帧是同一个 seed、同一份抽签**,
翻的却是 **sniper 与 viper**;本轮翻的是 **sniper 与 skeleton_king**。
同一份抽签、不同的槽位分配 ⇒ **翻面集合本身是每帧的测量**。
四次治疗四种结果(seed 868 翻 1 / seed 885 五动零翻 / seed 906-radiant 三动翻 2 /
seed 906-dire 五动翻 2),**「核心辅助划分通常稳定」始终不构成跳过某一帧的理由**。

**敌方那一半按构造惰性**(GH #81 / 第十二条世界断言):fixture 现在带着真实的 dire 置换
(juggernaut 1 / zuus 2 / bristleback 3 / earthshaker 4 / jakiro 5),
而角色链**对五个敌人一律答 3**。已写成用例,并把「五个里有四个抽签值 ≠ 3」也断言了,
免得将来这条变成空断言。

## 4. 51 条消费方用例为什么一条没动

**测出来的,不是假设的**:两个入口(`J.GetPosition` 包装 与 `Role.GetPosition` 模块,
**不是同一个函数对象**)同时挂钩后,
`X.cm_IsRSafeToOpen` 读 **0 次**位置,`X.ConsiderR` 读 **0 次**。
`test_replay_260820_cm_es_aftershock.lua` 的 27 条 + `test_replay_260820_cm_r_selfstate.lua`
的 24 条因此**结构上免疫**这次治疗。两个零都配了非空见证
(`J.GetReadyHardCc(earthshaker) ~= nil`),不让它变成「什么都没跑」的空绿。

## 5. 本轮真正的产出:唯一那次角色读取**移动了**,而且语料自己作证

那次读取在**英雄文件 load 时**发生,不在任何决策里:`hero_crystal_maiden.lua` 被 `dofile`
的瞬间问 `Item.GetRoleItemsBuyList` 要出装表 → `aba_item.lua:1600` → `Role.GetPosition`,
**全仓库唯一绕过 J 包装的调用点**(与 axe 那轮找到的是同一个)。
本帧**恰好一次、落在主角身上、走模块**——已断言。

**治疗前 `pos_4`,治疗后 `pos_5`。** 两张表不是同一份表换个名字:

```
pos_4 (11 项): priest_outfit | urn_of_shadows | mekansm | glimmer_cape | guardian_greaves | …
pos_5 (13 项): blood_grenade | mage_outfit    | ancient_janggo | glimmer_cape | boots_of_bearing | …
```

**比 axe 那轮强的地方在这里**:axe 那次是从 `item_blink` 在两张表里的**次序**推出来的;
本轮**这一帧自己的背包直接判了**:

| 判别项 | 归属 | t=473.5 她身上 |
|---|---|---|
| `item_tranquil_boots` | `item_mage_outfit`(pos_5) | **有** |
| `item_null_talisman` | `item_mage_outfit`(pos_5) | **有** |
| `item_blood_grenade` | pos_5 第 1 项,**pos_4 全表没有** | **有** |
| `item_arcane_boots` | `item_priest_outfit`(pos_4) | **没有** |
| `item_urn_of_shadows` | `item_priest_outfit`(pos_4) | **没有** |
| `item_magic_wand` / `item_flask` | 两张表都有 | 有(**不判别,列出来说明它不判别**) |

⇒ **槽位派生的世界把她挂在一份她显然没在跑的出装表上。**
这一条同时给 GH #56(`axebuyblink` 的出装表 key)那条立论补了第二个独立实例。

## 6. 变异(六条,逐条 apply + rollback)

| # | 变异 | 应红的用例 | 实测 |
|---|---|---|---|
| M1 | 去掉 `roles` 表(回到治疗前的世界) | 三条 re-read + 棘轮 | 3 红 + 棘轮 1 红 ✅ |
| M2 | 把产生翻面的两个抽签值对调(sniper↔SK) | 翻面身份 | 1 红 ✅ |
| M3 | `aba_role` 敌方分支改成 `return bot.assignedRole or 3` | 敌方惰性 | 1 红 ✅ |
| M4 | `Item.GetRoleItemsBuyList` 钉死 `'pos_4'` | 两条出装 key | 2 红 ✅ |
| M5 | 在 `cm_IsRSafeToOpen` 头上加一次 `J.GetPosition` | 两条零读取 | 2 红 ✅ |
| M6 | 给 `pos_4` 表塞一颗血雾 | 背包判别 | 3 红 ✅ |

**M3 第一版没咬住,原因值得记**:`return 3` 在 `aba_role.lua` 里不止一处,
`str.replace(..., 1)` 落在 **`GetPositionForCM` 里那处**(307 行),
不在 `GetPosition` 的敌方分支(398 行)—— **全绿,而且看起来像「断言没牙」**。
这正是 backlog 0m 记的第四种失败形状:**变异跑了、生效了,但没落在被测量上**。
改成从 `____exports.GetPosition = function(bot)` 起 `index`、并断言前 120 字符里有
`GetEnemyPosition` 之后才咬住。**顺带证明了一件事**:loader 确实给敌人 `rawset` 了抽签角色,
是**已发布的敌方分支主动把活数据扔掉**(GH #81 的机制第一次由变异而不是读代码证实)。

## 7. 一条给全组的做法(本轮亲手犯的)

新增的五条用例里,有一条的**名字与上一轮那帧的用例逐字相同**。
`tests` 是普通 Lua 表 ⇒ **新函数把旧的那条静默替换掉了**:不报错、不失败,
**唯一的破绽是计数读 31 而不是 27+5=32**。
⇒ **加了 N 条用例就必须核对总数涨了 N**;否则一次绿跑可以藏着一条被删掉的断言。
(与 0m 记的「变异要真的落在被测量上」是同一类:绿色本身从来不是证据。)

## 8. 棘轮

`tests/test_fixture_roles.lua` 的 `SLOT_DERIVED_ROLES` **6 → 5**。剩下:

- `f_221200_od_roles.lua` —— **永远治不好**(裸树 sha `829202a`,无 seed,`positions_for_game` 拒绝);
  它同时是 loader「无 roles 回退」契约的锚点;
- `f_260819_182855_lion_drain_jungle.lua` / `…_midchannel.lua`;
- `f_260819_222559_od_eclipse_pair.lua` / `…_solo.lua`。

**下一批建议**:两个 `lion_drain_*`(同一对,先取一枚;GH #97 刚把 `liondrain` 判成
SIBLING-DOMINATED,治完正好重读那两枚钉住的结论)。规矩不变:**一次一个、重生成、重读结论**。

## 9. 验收

```
luacheck bots game --formatter plain      -> 0 warnings(bots/ 本轮未改)
lua5.1 tests/run_tests.lua cm_es_aftershock -> 32 tests, 0 failures   (治疗前 27)
lua5.1 tests/run_tests.lua cm_r_selfstate   -> 24 tests, 0 failures   (逐条未改)
lua5.1 tests/run_tests.lua fixture_roles     -> 10 tests, 0 failures
lua5.1 tests/run_tests.lua                   -> 1052 tests, 0 failures
```

**全套 1052/0。** 诚实说明基线:本轮**没有**留下一份干净的治疗前全套读数 ——
第一次全套跑在治疗中途被污染(见下),已 kill。
`1052 = 1047 + 5` 是**算出来的**,不是量出来的;真正量过的治疗前后对照是三个 filter
(`cm_es_aftershock` 27 → 32、`cm_r_selfstate` 24 → 24、`fixture_roles` 10 → 10)。
被污染的那次是这么来的:M1 变异的回滚写成了 `git checkout -- <fixture>`,
而**治疗本身还没提交** ⇒ 回滚把治疗一起撤了,M2/M3 第一遍因此跑在未治疗的树上(读数已作废)。
改成从 mutation 前的副本恢复后重跑。**记一句**:凡是回滚**未提交**的改动,
`git checkout` 不是回滚,是丢弃。

**AWS**:EC2 **$0**;S3 只读 —— 1 个 `.dem`(8.7 MiB)+ 1 个 `.analysis.json`(5.6 KiB)
+ 1 个已缓存的 `behav-dump`(20 MiB)。**未调用 Cost Explorer。**

`state.json:cmesaftershock_ROLE_HEAL_20260822`。

## 10. 本次会话 token 用量(铁律 8)

```
TOKENS total_in=22,819,125 out=99,911 turns=149
```
