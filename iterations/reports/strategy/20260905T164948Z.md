# 协同组 2026-09-05T16:49Z —— `staysrc`:一个 PROMOTED 函数的 docstring 与它的代码是两份不同的规格

**工作单元**:OWNER_PRIORITIES **P4.4**(每轮主体必须是一个 `bots/` 行为改动)+ **P2**(低血不回家,
决策侧,责任链当前球写着协同组)。章程 `0OUTCOMMIT`「下一格」逐字点名的那一格。

**认领扫描(章程第 1 条)**:本轮扫过的 open `[strategy]` issue —— **#521 / #511 / #503 / #500 /
#495 / #489 / #485 / #480 / #475 / #467 / #464 / #456 / #455 / #452 / #445**,
**无一条未认领**(#521 / #511 是本组前两轮已交棒的)⇒ 按铁律 9 取优先项。

**一句话**:`J.ShouldStayAndRegen` 是 **PROMOTED** 的('tphome',28 局 A/B,+51 GPM,
**每一局 Turbo 都活着**),而它上方 47 行的 docstring 把已批准行为写作
「can afford a regen consumable (gold >= 90) **or already carries one**」,
代码却把「already carries one」实现成 **只认 `item_flask`** ——
主格位里的 tango / tango_single / faerie fire / 有充能的 bottle 一律读作「什么都没带」,
决定权交给金钱项,**一个残血、无人追击、揣着三个 tango 和 40 块钱的 bot 被放回家**。
这正是 owner 优先项 P2 逐字禁止的那趟路。

---

## 1. ⭐ 主判据(立法级,可复用,超出本主题)

**一个函数自己的 docstring 是「当初被批准的行为」的档案,而它下面的代码可以只实现其中一个子集,
树里没有任何东西会注意到。**

这不是「注释过期了」—— **注释描述的才是对的那一个**,代码是窄的那一个。
它们分岔的地方,就是**那次 A/B 到底批准了什么这件事已经没人知道**的地方。
⇒ 读一个 PROMOTED 函数时,**docstring 与代码要当成两份可能不同的规格分别读**。

**⭐⭐ 而且倒挂是精确的不是修辞的**:**90 正是 tango 的价钱**。
所以出厂这个函数**把「买一个 tango 的钱」当作原地续航的证据,却拒绝「那个 tango 本身」** ——
它信一笔还没花的钱,胜过信一件已经拿在手里的东西。

## 2. ⭐⭐⭐ 为什么动的是这条子句,而不是章程点名的那个常量

章程「下一格」问的是「`bot:GetGold() < 90` 在 Turbo 里还是不是对的门」。
**答案是结构性的:这个问题在任何录像帧上都答不出,而且永远答不出。**
gold 不进 `.dem`(`tools/batch_test/behavioral/hometp_invfull_lag.py` 诚实边界节原话:
「`GetStashValue`, gold and the bot's mode are not networked into a replay」),
于是 `bot:GetGold()` 落在 mock 的 `^Get -> 0` **类型断言**上(GH #495)。

**驱动实测:1012/1012 活帧 gold 读 0**(`gold_nonzero == 0`,棘轮断言;变异体 M11 打它)。
⇒ 改那个 90 **结构性不可 fixture 化**,本组章程第 2 条禁止落地。

**⇒ 一个常量量不出来的时候,先给同一个 `and` 的另一个合取项定价,再断言这条子句够不着。**

## 3. 改动(一条合取项宽,gated,turbo-only,**未 armed**)

`bots/FunLib/jmz_func.lua`,`J.ShouldStayAndRegen` 的补给子句:

```lua
local bHasRegen = bHasFlask
if not bHasRegen and J.IsSoakCandidate( 'staysrc' ) then
    bHasRegen = J.HasFieldRegenSource( bot )
end
if not bHasRegen and bot:GetGold() < 90 then return false end
```

借的是兄弟谓词 `J.HasFieldRegenSource` —— gated `stayfield` 家族对着**同一趟回家路**
已经在用的那一个,不新写谓词、不改兄弟、不碰 0.18 血量地板、1200 环与追击行。
**方向由构造定死**:加宽 `bHasRegen` 只能让否决更难成立 ⇒ armed 的 TRUE 集是 shipped 的严格超集。
未 armed 时 `and` 在 callee 之前短路,`J.HasFieldRegenSource` 里的 `bagsalve` 闸**根本到不了**。

## 4. 域价钱(109 fixture / 1012 活英雄帧,与 §EF.1 / §EH / §EQ 同分母)

| 量 | 读数 |
|---|---|
| turbo(首行不吃帧) | **1012 / 1012** |
| **走到**补给子句 | **125** |
| 被补给子句**否决** | **112** |
| ↳ 身上确实有可喝的(杠杆全部作用域) | **44** |
| ↳ 什么都没带(杠杆完全惰性) | **68** |
| `flip_true_to_false` | **0** |
| `ship_true → arm_true` | **13 → 57** |

**走到这里之后,补给读数是占压倒多数的那道否决(112/125)** —— 不是一个角落。
二分区**数出来不是减出来**,和 `== 112` 由断言钉住;
**两条独立路线交叉核对**:驱动读数 `flips` 与前缀桶 `blocked_with_src` 必须相等
(一条漂移了的影子会在这里变红,而不是达成一致)。
44 帧载体:tango **30** / faerie fire **13** / tango_single **1** / **bottle 0** / **flask 0**。

## 5. 钉帧与对照

`tests/fixtures/f_260822_182012_sb_fieldbuy_gate_307.lua` **t=307.4**
(与 owner P2 催生的 `fieldbuy` 取证同族的那盘):
**lina 434/1088 = 39.9% 血,最近敌方英雄 6,830u 外,3 秒内无人打过她,
主格位 0 一个 tango、格位 5 一个空瓶**。shipped 放她回家;armed 她留下喝完。

- **同世界同一瞬间两条负对照**:viper(55.7% 血,主格位无可喝之物)、
  spirit_breaker(36.1% 血,身上是 **blood grenade —— 武器不是补给**)⇒ 两条腿都 false。
- **归因对照**:摘掉她格位 0 的 tango,armed 腿必须退回 shipped 答案(驱动实测)。
- 那只**空瓶**正是「bottle 腿空洞」的现场证人,**而且它不是翻转的原因**。

## 6. ⭐⭐⭐⭐ 本轮最该被读的一条:AND-of-vetoes(一条**发波前**的量具级约束)

这个函数用**两道相互独立、各自充分**的否决守着 P2 禁止的那趟路 ——
**追击子句**(`stayattr`,同日几小时前落地)与**补给子句**(本轮)。

在 **owner 优先项 P2 自己钉的那一帧**(`f_260822_063722_lina_tp_home`,
lina 346/1088 = **31.8%** 血,身上一个 faerie fire),四次驱动:

| 臂 | 读数 |
|---|---|
| shipped | **false**(回家) |
| 单 armed `stayattr` | **false** |
| 单 armed `staysrc` | **false** |
| **两个都 armed** | **true** |

全语料 `flips_pair_only` = **1**,**而那 1 帧就是 P2 那一帧**。

⇒ **「一次动一个小杠杆」(lanefix 教训)与「这条优先项的钉帧翻过来」在这里互相矛盾**:
任何**单臂隔离波**在这一帧上都会读到一个零,**而且每一次都是正确的零**。
**AND-of-vetoes 的结构必须在设计波次之前量出来,而不是等波次空手回来之后再猜。**
变异体 **M12** 直接打这条腿(「both armed」那次驱动偷偷只 arm 一个 ⇒ 该列读 0):
**一个没有任何变异体能打破的成对结论,只是一句被写下来的巧合。**

## 7. ⛔ 本轮收紧了一条既有棘轮(不是放宽)

`tests/test_stayattr_global_ult.lua` 原先断言 `STAY_NIDS == 1`。
那是 pullcad 陷阱的**代理量**,不是陷阱本身 —— 陷阱是**两个 id 合取在同一个条件里**。
本轮在**另一条子句**上加了第二个**独立**杠杆,代理量在一棵**没有陷阱的树上变红**。
两个 sweep 新增 `STAY_IDS_MAX_PER_COND`(逐 `if … then` 条件取最大),断言移到它上面;
**总数仍登记并断言 ≥ 1**。M4 证明收紧后的断言仍然咬得住。

## 8. 本地验证与变异台

- `tests/test_staysrc_field_supply.lua` **12/12**;重语料扫描 `tests/_staysrc_sweep.lua`(子进程,四次驱动/帧)。
- `tools/agent/mutstand_staysrc.sh`:**15 发全部如声明 —— 14 CAUGHT + 1 声明式 UNMEASURABLE(M11b),
  对照 M14 SURVIVED,零 NO-OP,零非声明存活,EXIT=0**。
  M11b 按 §EQ 的 M8 同族原因声明为不可测:在一份 gold 处处为 0 的语料上,
  **「探了发现是 0」与「假设是 0」给出逐字节相同的计数**。
- nesting census 两行((P) + (A)),见 test_set.md §EY.7。

## 9. 诚实边界(四条)

1. **量到的翻转集是「穷钱超集」**:fixture 读不到 gold,每帧都按「不足 90」计分;
   真实对局里 gold ≥ 90 的帧本来就没被这条子句否决过。方向定死(真实集是**子集**),
   **大小未知,不作声称**。
2. **bottle 腿在本语料上空洞**(44 个翻转里 0 个由 bottle 承载)。按零断言,不粉饰。
3. **有 ≠ 够**。44 个翻转里 **13 个由 faerie fire 承载 —— 85 点血,正是 `fieldsip` 立案那一行**。
   本文件不声称那 13 个是好的停留;它声称的是出厂函数把它们读作了「什么都没带」。
4. 一帧是一个瞬间。

## 10. 门与自检

- **开工自检**:`bash tools/agent/rc.sh bash tools/agent/routine_selfcheck.sh` ⇒ **RC_EXIT=3**(findings),
  来源 5 条:`cadence / queue-rulings / owed-executions / trunk-red(python) / trunk-red(lua)`。
  ⚠️ 首次调用被工具自己**拒绝**(`SELFCHECK_EXIT=2 REFUSED` —— stdout 是管道,读到的是 `tail` 的退出码;
  证据纪律 3,该形状已复发 5 次),改走 `rc.sh` 后取到真退出码。
  python 腿的红是既有的(GH #514 / #526 / #497 / #506 / #501 / #490 一族),**非本轮引入**;
  lua 腿当时唯一那条红,正是自检**当场点到本轮工作树**的 nesting census(新 gate-inside-gate 未登记)——
  **已在同一工作单元内按 (P)/(A) 登记并转绿**。
- **铁律 6 静态半**:`bash tools/agent/rc.sh bash tools/agent/luacheck_gate.sh` ⇒
  **GATE_EXIT=0,`luacheck bots game: 0 warnings`**(容器冷启,gate 自己装的 `lua-check`)。
  push 门已上膛(`arm_push_gate.sh` ⇒ `core.hooksPath=.githooks`)。**未用 `RULE6_BYPASS`。**
- **铁律 6 动态半(定向)**:`staysrc_field_supply` **12/12**、`stayattr_global_ult` **11/11**、
  `gated_helper_nesting_census` **10/10**;相邻组 `stay` **78/78**、`field` **212/212**、
  `soak` **31/31**、`gate_claim_consistency` **16/16**,全部 EXIT=0。
  全量套件 ~100min(GH #124)**未跑**,不声称。

## 11. 交棒

- **(甲) 总监**:按 **P4.2 冻结**把 `staysrc` 记作 **FROZEN-HOLD**(登记、不入集、不算掉棒)。
  ⛔ 另请**单独收下第 6 节** —— 它不是本 id 的入集问题,是一条**发波前**的量具级约束。
- **(乙) 批测台**(已立案 **GH #532**):P2 的取证波若把 `stayattr` 与 `staysrc` **分两臂单独 arm**,
  P2 自己的钉帧**按构造读不出任何东西**,而每一臂的「无效果」读数**都是正确的**。
  要么**同一腿共同 arm(声明为一个 PAIR)**,要么在波次记录里写明**这一帧不在射程内**。
  本组本轮**不发波、不排队、不花钱**(冻结期照章不提 queue 请求)。
- **(丙) 录像组 / 语料侧**:两张帧能把两处空洞变成有支撑的读数 ——
  (i) 主格位带**有充能 bottle** 的真帧;(ii) 任何一张**带真实 gold** 的帧
  (会让 `[gold]` 断言当场变红,并第一次让第 2 节那个问题**可回答**)。
- **(丁) 下一轮本组**:P2 决策侧的下一格**不再是那个常量**(已结构性排除)。

## 11b. 并发落地(rebase 时发现,登记而不转述)

`git push origin HEAD:main` 被拒 ⇒ `git pull --rebase origin main`,冲突两处
(`state.json` / `test_set.md`,都是各自在文件尾追加),**两侧全保留**;
本组的节号与总监同轮的 §EX **撞号**,本组**改号为 §EY**(总监那节先落地)。

⚠️ 顺带登记一件**与本轮结论直接相关**的并发事实,只做指针不转述:
总监 15:5xZ 那轮按 P4.2 把 **`stayattr` 退回出集(63 → 62)**、`zusstatic` 退回(62 → 61)。
⇒ 第 6 节那条约束**不因此失效,反而更早生效**:`stayattr` 现在也不在串里,
所以 P2 的取证波将来重新 arm 时,**是从零开始决定要不要成对**,而不是在一个已经单臂在跑的串上改主意。

## 12. AWS

**零 AWS、零 S3、零 EC2、零波次。** P4.2 冻结期内**未提入集、armed 串一字未动、
`queue.json` 一字未动**。

## 13. Token 用量

```
TOKENS total_in=11,673,217 out=87,480 turns=77
```
(`python3 tools/agent/token_usage.py` 最后一行原样。这是「到统计时刻为止」的数字,
统计后的收尾回合不计入。)
