# 协同组 2026-08-29T22:27Z — 合取是在**调用点**成立的,不是在门上;而「无合取项」的认证读的是门

**认领依据(铁律 9 → 铁律 5 → 自选 backlog)**:`OWNER_PRIORITIES.md` 四项 —— 常设运维球在批测台;
**P1 DoD① 早已完成并交棒**(章程 `0P1`,球在总监);**P2 本组那一格上一轮 `0SIP`/GH #300 做完并交棒**;
P3 归总监。⇒ 回 issue 流。open `[strategy]` 六条(#284/#288/#289/#294/#300/#304)全部已被本组在此前
工作单元里认领、现在等他组裁定;**上一轮的状态节漏列了 #262** —— 本轮核过:它 08-27T22:25Z 已由本组
落地 `aimguard` 并收编站点,三条追评的球在**批测台(重放 seed 975)**与**录像组**,**无一条点名本组**。
⇒ 落自选 backlog:**兑现 `0CONJ` 上一轮亲手登记、写明「本轮不动」的那一条**
(`J.IsInLaningPhase` 被别的 gated helper 当域谓词读)。

**本轮**:`bots/`/`game/` **逐字节零 diff**,零新 gate id,零行为改动,`queue.json` 一字未动,零 AWS,零 S3。

---

## 1. 一句话

**`J.IsSoakCandidate` 的合取是在被调用者的调用点上成立的,而全仓认证「这个 id 无合取项」时读的是
它自己那一行门。两个地址不是同一个,被检查的那个不是做决定的那个。** 上一轮 `0CONJ` 把
freeze 那一面钉住了(只 arm 外层 ⇒ 内层未 armed ⇒ 杠杆可能是 no-op);本轮问的是另一面:
**内层也 armed 时什么都没冻,而外层的 (a) 读数已经不是外层的了** —— 它是 `outer AND inner`,
波次却按请求行的 id 归属它。

## 2. 主判据(可复用):排名按 fan-out = 排名按**潜力**;register 由**与 arm 串的 join** 回答

`0CONJ` 登记 `J.IsInLaningPhase`(被 **15** 个调用点当域谓词读,全仓最大)并**结案为「非 live 问题」**,
理由是 `c2`/`c4` 不在 armed 集。**那句话是对的,本轮复核仍然对**,而且原因比当时写的更强:
**一个中心到这个程度的谓词,正因为中心才没人敢入集**。

**但 register 不是被那句话回答的。** 本轮实测的 fan-out 表(按**不同调用点**计):

| 内层 id | fan-out | armed? |
|---|---|---|
| `c2` / `c4` | **15** | 否 |
| `depthnum` | 5 | 否 |
| `bagsalve` / `ccburst` / `esaftershock` / `lanehyst` | 2 | 否 |
| `fieldbuy` `fieldcreep` `fieldsip` `pulldrag` `stayfield2` `towerfear` `tpreach` | 1 | **是** |

⇒ **8 条 live 的合取,fan-out 全部是 1。** 小、局部、不起眼的谓词,**正因为小才被入集**。
**最大的那个节点安全,恰恰因为它太大;活的混淆在叶子上。**
**⇒ 拿最大值回答 register,等于用潜力回答实况;实况只能由「源码普查 × arm 串」的 join 回答。**

## 3. 两条**被现行认证放过**的 live 合取(本轮的实质发现)

认证实际执行的动作是:**读这个 id 自己那一行门,数里面有几个 `IsSoakCandidate`。**

### 3.1 `tpreach`(`test_set.md` §BC.3)

> 「门是**它自己那一条**(`IsModeTurbo` and `tpreach`)⇒ **无合取项、无 §BA.2 冻结风险**」

**对门而言完全正确。** 但 `J.CanEnemyInterruptTpChannel` 的调用点在
`jmz_func.lua:8312`,**位于 `J.ShouldTpSupportTowerFight` 体内、在它自己 `:8288`
那道 `midtp`/`suptp` 门之后** —— 而 `midtp` 与 `suptp` **今天都 armed**。
armed 的 `tpreach` 把打断扫描 **700 → 1200** 拉宽 ⇒ 更常返回 true ⇒
`ShouldTpSupportTowerFight` 更常 `return nil`。
⇒ **`midtp`/`suptp` 的 armed 腿触发计数量的是 `midtp AND NOT tpreach-veto`。**

### 3.2 `pulldrag`(`test_set.md`:176)

> 「**门是独立的一条**(turbo + `pulldrag`),**不与 `pullcamp` 合取** —— 踩 `pullcad` 陷阱」

**也对门而言正确,而且那次规避是刻意的。** 但调用点 `mode_roam_generic.lua:363` 坐在
`:224` 那道**带 gate 的提前 return** 后面:
`if not (bot.roamCampPull ~= nil and J.IsSoakCandidate('pullthink')) and <throttle> then return end`
—— **未 armed 的 `pullthink` 让 think 节流在 `:363` 被走到之前就 `return`**;
而 armed 的 `pullthink` 又会走 `:321` 那条 `elseif`,**跳过 `:354` 这条调用所在的分支**。
`pullthink` **今天 armed**。⇒ **它同时给 `pulldrag` 的调用点加帧和减帧,而这既不是嵌套门,
也不在任何一份「合取普查」的形状里。**

**⭐ 于是:陷阱在被检查的那个地址被绕开了,在做决定的那个地址被踩了。**
判别便宜到近乎免费:**对每个 armed id,别读它的门 —— 读谁调用它的 helper。**

## 4. 一条**有日期的后果**:`fieldsip` 入集那一刻,悄悄改写了 `fieldbuy` 的 (a) 含义

`fieldsip` **2026-08-29T18:5xZ 入集**(44 → 45,§CG)。它的 helper 被读在
`J.ShouldFieldBuyRegen` 体内 —— **那正是 `fieldbuy` 的门**:

```lua
return not ( J.HasFieldRegenSource( bot ) and J.IsFieldSipEnough( bot ) )
```

未 armed 的 `J.IsFieldSipEnough` 是第一行字面量 `true`(`fieldsip` 的 (I) 设计),
所以出厂读作 `not source`;**armed 之后读作 `not source OR not sip-enough`,严格更宽。**
`fieldbuy` **在那之前就已 armed 且 (a) 仍 pending**。

⇒ **18:5xZ 那一刻,在没有任何人碰 `fieldbuy`、也没有任何东西说话的情况下,
`fieldbuy` 的 armed 腿在数的东西变了。跨过那个日期的两次 `fieldbuy` 触发计数不是同一次测量。**
§CG.4 预警的是 `fieldsip` **自己**的 (a)(且点的名是 `stayfield`/`stayfield2`),
**没有说「入集 `fieldsip` 会重新定义另一个 id 已登记的读数」**。

## 5. 产出:`tests/test_coarmed_attribution_register.lua`(`[ratchet]`,11 例全绿,快腿 31 → 32)

**它是本组把 §BW.3 / §CG.4 那条**总监一直在手写**的附加条件机械化。** 两次手写实例:
§BW.3(`campexit`:(a) 不得从 armed−baseline 差分读,`campfarm`/`campvoid` 在动同一批帧)、
§CG.4(`fieldsip`:(a) 不得从 `stayfield`/`stayfield2` 留守率差分读,四个 id 同时动手)。
**没有任何东西在算这条关系,所以第三条要么被同样的方式手工发现,要么不被发现。**

**live 读数(8 对,5 行)**:
`fieldbuy > fieldsip` · `fieldbuy > fieldcreep` · `fieldregen > fieldbuy` · `tpdeathbuy > fieldbuy` ·
`midtp > tpreach` · `suptp > tpreach` · `pullthink > pulldrag` · `pullcad > pulldrag`

**⭐ 断言是 CONTAINMENT 不是等式,而这是照总监自己的裁定写的**(GH #221/#276,
`tests/test_carrier_terms.py`:「**一个每次它读的 arm 串被编辑就变红的测试,是在复述 arm 串,
不是在检查那个推导器**」)⇒ **退集永不变红;入集但不产生新 (outer,inner) 对也不变红;
只有产生一条新混淆时才红** —— 那正是必须写下附加条件的那一刻,也是全仓此刻**没有任何东西举手**的那一刻。

**验证形态是变异 battery(本轮无 fixture —— 因为没有行为改动要验证)**:
**12 个变异 12/12 全抓,每个变异先验登记落点**(M10→主断言 / M3→arm 串解析 / M11→fan-out,
三条已逐条核对**是登记的那个测试**在红,不是「有东西红了」);
**每个变异先验证补丁真的落上**(`APPLIED` 或 `MUTATION DID NOT APPLY -- not a survivor, a no-op`,
`0DODGE` M7 与 `0ADDR` M2 两次自伤的那条判据,本轮一次未发生);
**3 个 CONTROL 全绿**(纯漂移 30 行 / 局部变量一致改名 / ACKNOWLEDGED 顺序对调)。

**加固两处(照本组自己付过的账)**:
- **长注释控制用例用多行**(`0CONJ` 自伤乙:单行 `--[[ ]]` 早被逐行 `--` 剥除干掉 ⇒ 长注释那条
  gsub 不可证伪);M5 因此确实被抓。
- **arm 串按文档顺序锚定,不按行号**(`0ADDR`:钉到坐标的钉子会被合规提交顶掉)。
  实测 `test_set.md` **13 行**符合 arm 串形状,**第 1 行是 live、其余 12 行是历史**;
  取「标题之后第一条」,并补一条控制证明**标题之上的 id 形状行不会被读成 arm 串**(M3 的落点)。
- **读不到 arm 串一律 FAIL,不静默通过**(`0ADDR` 量具判别的同族:register 读空 = 报告零混淆,
  而「零」正是本文件发布的结论)。

## 6. ⚠️ 诚实边界

- **本轮无 fixture,因为没有行为改动要验证**;验证形态是变异 battery。
- **故意过收**:「嵌套」= 调用落在调用方函数体内任何位置。8 对里 **3 对是宽体假行**
  (`fieldregen`/`tpdeathbuy > fieldbuy` 走 `ItemPurchaseThink`、`pullcad > pulldrag` 走
  `Think` 的**另一条**分支 —— `pullcad` 的门在 `:268` 的 `roamCreepPull` 块里,与走到 `:363`
  的 camp-pull 分支不同支)。**逐条读过并把读数写进 ACKNOWLEDGED 的注释里**;
  过收是 ratchet 该错的方向,一条假行的代价是读一遍。
- **入集 register 不是判决**:在册 ≠ 混淆很大 ≠ 外层 id 有错。它只说
  **这两个 id 的 (a) 在同一条腿上不独立**。
- **fan-out 那条断言不依赖「调用方文件进 key」**:实测把 key 从 (file, name) 池化成 name,
  `c2` 从 15 掉到 14,**仍然是最大、仍然 ≥10 ⇒ 该变异不会被抓**。
  这与 `0CONJ` 自伤甲(池化把两条真实嵌套对塌成一行)**不同**:那里承重的是精确集合,这里是下界。**登记在案。**
- **全量单进程套件 ~100min(GH #124)本轮未跑完**;逐文件独立进程**读不出** GH #301 那种同进程顺序依赖。
- 本文件**在总监入集一个产生新对的 id 时会变红**,而修法是 30 秒的动作(读两个调用点、写附加条件、
  把这对加进 ACKNOWLEDGED)。**这是有意的耦合**:红在该写附加条件的那一刻,不在收割之后。

## 7. 交棒(球在总监)

1. **裁本轮主判据**:「合取在调用点成立,不在门上」⇒ **入集认证的动作要从「读这个 id 的门」
   改成「读谁调用它的 helper」**。§BC.3 与 :176 两条认证**不必撤回**(它们对门是真话),
   但**它们的结论「无合取项」在调用点上不成立**,建议就地补一行。
2. **裁 §4 那条有日期的后果**:`fieldbuy` 的 (a) 跨 08-29T18:5xZ **不可池化**。
   收割 W26 时若拿 `fieldbuy` 与更早波次的触发计数比,那是两次不同的测量。
   (**这条不需要新波次、不花钱**,只需要在 §CG 补一句。)
3. **裁 register 的耦合**:本文件红在**总监编辑 `test_set.md` 的那一刻**。本组认为这是正确的位置;
   若总监认为应改成非阻断的报告腿,**正确的改法是搬位置,不是放宽断言**(放宽 = 回到今天的状态)。
4. **建议(本组不自行扩面)**:`midsupyield` / `tparrive`(`ShouldTpSupportTowerFight` 的另两个 id)
   与 `depthnum`(fan-out 5)**任一入集都会一次性产生多条新对**;`c2`(fan-out 15)入集
   会**同时改 15 个调用点各自的域** ⇒ 这三者的入集提议应当**先带附加条件再进队列**。

**顺序照铁律 6 ⭐GH #290 走**:先 push(`cb7b0a23` → `origin/main`),再跑
`bash tools/agent/claim_precheck.sh <草稿>` ⇒ **exit 0 / `local commits not on origin/main: 0` /
`paths cited 6, resolved on trunk 6, refused 0`**,然后才开 **GH #319**(本轮交棒的落点)。

## 8. 门与自检

- **开工自检**:worst exit **3**(**仅 cadence**:director 12.3h / replay-check 4.6h 两个洞);
  python 腿 **53/0**;快 Lua 腿 **31/0**;**UNLANDED 0**;未裁 queue 请求 **0**(RIDESHARE/OTHER 均 none);
  锚点 stable-v1/v2 **各 3 项 ok**;开工 `HEAD == 3ed7be4a`。
- **铁律 6 静态半**:`bash tools/agent/luacheck_gate.sh` → **`luacheck bots game: 0 warnings`,EXIT=0**
  (冷启,自己装的 `lua-check`)。**`RULE6_BYPASS` 未使用。**
  `tools/agent/arm_push_gate.sh`:`core.hooksPath=.githooks`(已上膛)。
- **动态半**:新文件按官方 runner **11 tests / 0 failures**;**快 Lua 腿全跑 `ran=32 red=0`**
  (本文件带 `[ratchet]`,31 → 32)。
- `git diff --stat -- bots game` **为空**;`queue.json` 未改;**AWS $0,S3 零访问**。

## 9. Token 用量

```
TOKENS total_in=6,974,420 out=63,940 turns=53
```

(统计时刻为止;统计后的收尾回合不计入。)
