# 协同组 2026-08-22T09:33Z

**认领依据**:铁律 9 —— 先读 `iterations/OWNER_PRIORITIES.md`。**P2 的球明写在协同组**
(「当前球在:**协同组**(决策侧 id + fixture)」),而且它的第一棒 30 分钟前刚被录像组
**改了目标**:GH #111 §2.3 证明 P2/#110 原钉的那一帧不是回家 TP。所以本轮不走 backlog,
做 P2 第 1 条,并且**用录像组给的替换帧**,不是照着已作废的那一帧钉。

**类型**:一个 gated 行为改动(P2 决策侧第一个 id)+ 两枚真实帧 fixture + 20 例 + 8 条变异。
**零 EC2 支出**(2 次 S3 GET 取 `.dem`、2 次取 `analysis.json`,dumper **cache HIT**,
未调用 Cost Explorer)。**未提批测请求**(入集批准之前提波次没有意义,见 §6 接力棒)。

---

## 1. 一句话

**Turbo 里「我被英雄打了」被读成「有英雄在打我」——一发 7,533 码外的宙斯大招,
就足以让一个 31.8% 血、方圆 6,596 码内没有任何敌人、包里揣着仙灵之火的莉娜
TP 回家、20.3 秒不在场上。** 本轮把决策侧的第一个 gate(`stayfield`)钉在这一帧上。

## 2. 证据帧(自己从 `.dem` 重新量过,不是转述)

`20260822_063722_slot1`(`spot_20260822_061041_…`,seed 888,dire armed,
stamp `mirror:roamreach:s888:dire`),**Lina t=349.1**:

| t | 位置 | 血 | 最近敌人 | 备注 |
|---|---|---|---|---|
| 346.4 | (-602,-537) | 555 (51.0%) | 6,676 spirit_breaker | |
| **347.4** | (-541,-466) | **342 (31.4%)** | 6,696 | 掉血的是 **346.4 的 `zuus_thundergods_wrath` 204 + `static_field` 15** |
| **348.4** | (-553,-279) | **346 (31.8%)** | **6,596** | **决策帧**(`tp_cd = 0`) |
| 349.4 | (-498,-210) | 365 | 6,677 | `tp_cd = 39.8` ⇒ TP 已按下(事件流:349.1 `item_tpscroll` + `modifier_teleporting`) |
| 352.4 | **(6928,6372)** | 379 | 11,808 | **落点 = 自家泉水**(实测 dire 泉水 (6902,6420),距 **33u**) |
| 361.0 | | | | `modifier_fountain_aura_buff` 移除 ⇒ 离泉;**369.4 才重新出门** |

**代价**:决策 349.1 → 再次离泉 369.4 = **20.3 秒**,而 Turbo 一局 ~20 分钟。
**期间没有任何人在追她**:`observed.burst` 空,`died_after = 110.6`。

**fixture 取 `--t 349.0` 不是 349.1**:`modifier_teleporting` 是在 **349.1 整**加上的,
按 349.1 生成会把**这次决策的后果**写进「决策前状态」(而 `ConsiderItemDesire['item_tpscroll']`
的开场否决表第一条就读它)。349.0 的英雄标量与 349.1 逐字相同(同一个 348.4 采样),
差别只有那条 modifier 和 `dt` 少 0.1。

## 3. 为什么已发布的那个否决在这一帧上说不上话(两个独立原因)

`J.ShouldStayAndRegen`(GH #2,**已 promote 为 `tphome`,无 gate,每局 Turbo 在跑**):

1. **它只接在 `撤退:1` 那条腿上**(`aiug:5440`),而那条腿要 `botHP < 0.19`。
   31.8% 的血走的是 **`撤退:3`**(等级 ≥9、环内 ≤1 敌、无大药),**那条腿一个回复否决都没有**。
2. **它的危险判据是 `bot:WasRecentlyDamagedByAnyHero(3.0)`**,而决策前 2.6 秒落在她身上的
   是**宙斯的全图大招**(施法者 7,533 码外)⇒ 判据答 TRUE,helper 直接 `return false`。
   **「一个英雄打了我」和「一个英雄在打我」被当成同一件事。**

新 helper 两条都答:接在 `撤退:3` 上;**把「最近被英雄打了」做归因** ——
只有当那个英雄**此刻仍在 3000 内**才算危险。

## 4. 改了什么(一个杠杆)

- `bots/FunLib/jmz_func.lua`:
  - `J.HasFieldRegenSource(bot)` —— **只数 0-5 号可用格**(背包里的大药喝不着);
    计 flask / tango / tango_single / faerie_fire,bottle **只在有充能时**算。
    **故意不数魔棒**:录像组重建的 22 次真·回家 TP 里 **21 次带魔棒** ⇒ 数它等于恒真,
    而决定它能不能回血的**充能数不在 dump 里**。魔棒是 `wandbleed` 自己的杠杆。
  - `J.ShouldRegenNotTpHome(bot)` —— turbo + `J.IsSoakCandidate('stayfield')` 双闸;
    血量带 (0.18, 0.55];**1600 环内 0 个敌人**(它守的那条分支容忍 1 个 ⇒ 本 helper
    **严格更保守**);归因后的受击判据;包里要有能喝的。
- `bots/ability_item_usage_generic.lua`:`撤退:3` 的条件里加一句
  `and not J.ShouldRegenNotTpHome( bot )`。**gate 关 = 逐位是旧行为。**
- **id 名用 `stayfield` 不是我原来写的 `regenstay`** —— GH #110 正文建议的就是这个名字,
  下游(总监入集、批测串、录像组核验)对得上比我的偏好重要。

**顺带只改了一行注释**(`mode_retreat_generic.lua:213`):它把**已 promote** 的
`J.ShouldStayAndRegen` 描述成「Candidate-gated … inert off-farm」,而那条 `if` **每局 Turbo 都在跑**。
与 2026-08-21 修的 `aiug:5161`(`tpsafe2` 被写成 inert)同一类:
**章程说「gated 的不算 shipped」,这是它的反面。**

## 5. 本地验证

- **两枚真实帧**:证据帧 `f_260822_063722_lina_tp_home`;
  **反例帧 `f_260822_063559_slardar_tp_forward`** —— 就是 P2/#110 原来钉的那一帧
  (`--t 635.9`,TP 在 636.0 按下),46.0% 血、tidehunter **1,565** 码。
  它现在以**它真实的身份**进仓库:一次**前压 TP**(GH #111 实测落点离自家泉水 12,646 码),
  正好是本 helper **必须不碰**的形状。
- **20 例 0 failures**(0n 计数核对:文件内 20 条,`tests[` 计数 = 20,无重名覆盖)。
- **8 条变异逐条 apply + rollback,每条点名红了它该红的用例**:
  M1 拿掉 gate → 两条 gate-OFF 用例红;M2 环 1600→600 → 「环是承重的」红;
  M3 归因环 3000→1500 → 「归因子句放行」红;M4 把归因换回**已发布的那种无归因否决** →
  「gate ON → STAY」红(**这条就是缺陷本身,变异复现了它**);M5 把魔棒算进回复品 → 三条红;
  M6 扫到背包 → 「背包里的大药不算」红;M7 拆掉调用点 → wiring 红;M8 血量带上界 0.55→0.75 → 带用例红。
  rollback 后 20/20,`git status bots/` 干净。
- `luacheck bots game --formatter plain`:**0 警告**。

**两条诚实边界,写进了用例而不是留在脑子里:**

1. **这一帧的最终出价断言不了**,只能断言 **helper** + **用排除法归因分支**。
   `nMode == BOT_MODE_RETREAT` 在任何 fixture 上恒假(GH #89 第十三条),
   `ConsiderItemDesire` 根本没人调(GH #100 第十六条)。排除法本身是可断言的:
   `撤退:1` 要 <0.19、`撤退:2` 要 <0.15(环空)、`回复状态` 要 <0.20 或 血+蓝<0.30,
   而她是 **0.318 血 / 0.576 蓝** ⇒ 三条全排除;`撤退:3` 里**帧能答的每一条都成立**。
   帧**答不了**的两条(`X.CanJuke()`、`GetAttackTarget()`)按「这是 mock 默认不是测量」标注。
2. **反例帧的余量只有 35 码**(1,565 vs 环 1,600)。GH #107 若动 dumper 采样相位,
   这枚反例可能**只因相位**就翻面 —— 已写在用例注释里(第 0q 条「挪动量要和结论余量放在一起」)。
3. **bottle 那条腿在 fixture 上结构不可达**(mock 的 `GetCurrentCharges` 默认 0),
   已按第 0p 条**显式声明**为「源码钉子,不是帧钉子」;莉娜身上那个恰好是 `empty_bottle`
   (dumper 读的是实体类名 `CDOTA_Item_EmptyBottle`),名字判据本来就排除它。

## 5b. 加两枚 fixture 的**连带代价**(GH #106 那一类,本轮又发作了一次)

`tests/fixtures/` 是共享且只增长的资源,而**四个普查文件把语料规模钉成等式**,
于是「加两枚 fixture」变成一次跨文件改动。**24 例失败,没有一条失败在发现上,全在分母上**
(以及两条失败在**行号**上:我往 `aiug` 里插了 6 行,`test_level_gate_census` 按 `file:line`
钉门 ⇒ `5753→5759`、`5793→5799`)。逐个按各文件**自己写下的规矩**处理:

| 文件 | 动了什么 | 发现有没有动 |
|---|---|---|
| `test_level_gate_census` | 98/71/980 → **100/73/1000**;两条门行号 +6 | 没有:`ge20` 仍 0、`ge18` 仍 1、`ge15` 仍 8、高水位仍 19 |
| `test_activemode_world_assertion` | 98/196/911 → **100/200/930**;`in_teamfight_1500` 81 → **86** | 没有:`mode_nonzero` 仍 0,过滤仍在 **930/930** 上被丢掉 |
| `test_gamemode_world_assertion` | 98 → **100**;对线出价移动 22 → **23**(仍 **全部向下**) | 没有 |
| `test_pingstamp_world_assertion` | 98 → **100**;push 地板 72/37 → **74/38**;`farm_floor_stale` 92 → **94**;存活英雄帧 911 → **930** | 没有:farm 仍 6 个移动(1 出价 5 崩溃)、`#satisfied` 仍 **4** 且是同样那四帧 |
| `test_itemdesire_world_assertion`(**第五个,收尾时才发现**) | 98/980/911/5774 → **100/1000/930/5896**;`driven` 882 → **901**;TP 就绪 658 → **674**;`alive_H` 508 → **527**;崩溃 209 → **210** | 没有:`slot_castable` 仍 **0**、`outer_and_H` 仍 **3**、无 buildings 的 fixture 仍 **43** 个 |

**两处不是纯搬分母,按该文件自己的要求实测过再写下的**:

1. **`gamemode` 的竞价**(该文件明写「**不许搬分母然后祈祷**,要把新帧两读法各跑一遍」):
   - `f_260822_063722_lina_tp_home`:**两读法都是 `laning 0.369`,0 个移动格**;
   - `f_260822_063559_slardar_tp_forward`:**赢家两读法都是 `retreat 0.860`**,
     但**有 1 个移动格**(它的对线出价 as-loaded 0.369 / 诚实 0.200)。
   ⇒ **移动格 22 → 23(分母 2058 → 2100),移动赢家不变 19,换 mode 的不变 11**。
   **这是该记录里第一次分子与分母移动量不同** —— 也正是「格」和「赢家」要分开数的理由。
   赢家分布同步 +1/+1(as loaded laning 37 / retreat 30;诚实 retreat 33 / laning 26),
   **三条转移量(11/8/3)一位没动**。
2. **`pingstamp` 的 push 份额**:两枚**都**在 as-loaded 下出地板,但**只有 slardar 那枚**
   在「声明 ping 陈旧」后说了真话(0 → 0.1)⇒ **戳自己的份额 35 → 36**。

**给下一个加 fixture 的人**:GH #106 §4 提的「分母改成 `>=` 登记值」**这四个文件都还没做**,
本轮是**同一天里第二次**执行这个仪式(00:30Z 英雄组做过一次)。本组把它记进 backlog,
**但没有顺手在本工作单元里做** —— 那是另一个杠杆,而且它要动的是**别人证据文件里
哪些等式是发现、哪些只是分母**的判断,值得单独一轮。


### 5c. 收尾时抓到的第五个文件,以及它里面**一条不是分母、也不是本轮造成**的移动

**过程本身值得记**:我按「被我碰过的文件」逐个跑,漏了 `test_itemdesire_world_assertion`
—— 它是在**修 census 之前**启动的那次全套跑完之后,我去读它的失败清单才发现的
(**1159 tests, 24 failures,五个文件:2+5+8+3+6**)。**「我以为我知道我碰了谁」不算清单**,
全套的失败枚举才是。

那 8 条里 7 条是分母。**第 8 条不是**:

- 该文件钉 `crash_2597 == 179` / `crash_total == 209`(`J.CanEnemyInterruptTpChannel`
  经 `GetExtrapolatedLocation` 崩掉的帧数)。
- **本轮两枚 fixture 的贡献量到了**:98 → 100 枚时 **2597 +1、3325 +2**(实测 177/30/207 → 178/32/210)。
- **但 179 在今天的任何一棵树上都复现不出来**:在 **98 枚**语料上,`bots/` 取
  `cac2fa5` / `41df6b4` / `477d0d4` / `3dc30f2` **四棵树读数全是 177/30/207**。
- **而且不是本组的锅**:100 枚语料下,带本轮 `bots/` 改动与不带(`3dc30f2` 的 `bots/`)
  **读数逐位相同 178/32/210** —— gate 关着,helper 第二行就 `return false`。
⇒ **这枚 pin 在本轮之前就已经红了,红了 −2**。
**收尾 rebase 时读到总监 09:xxZ 的条目(`bef59de`),他把它 bisect 出来了,而答案是本组自己**:
**05:30Z 的 `cf7bb4c`**(上一轮治疗 `f_260819_182855_lion_drain_jungle`)——
**同一个 `--t` 落到早 ~0.33 秒的采样**,于是**一枚已存在 fixture 的帧变了状态**,
全语料绝对计数跟着动(`e8e9421` 绿 / `41df6b4` 红,同样那两条)。**采信他的 bisect,
撤回我这段原来写的两句**(「无人认领」「已 promote 的 `tpsafe2` 路上的行为变化」)。
**我的四棵树测量本身没错,但它只变了 `bots/`、从没变语料内容** ⇒
**没变的那条轴,就是你无法排除的那条**。总监把「重新钉」升级成了下一次协同组触发的 P0 ——
**这次触发就是它,已落地**。
**GH #106 的规矩因此要补后半句**:共享语料**不只被「加一枚」打破,也被「治一枚」打破**,
而且治疗**改的是别人已经量过的帧**,两边都收不到机制预警。

**这也解释了它为什么没人发现**:该文件在字母序里靠后,而全套在本容器要 ~65 分钟
(总监 09:06Z 的独立观察),两边的全套跑都在到达它之前被 kill 掉过。

## 6. 接力棒(铁律 9:同一工作单元交出下一棒)

1. **总监**:`iterations/streams/test_set.md` 顶部已留**入集提议行**(eligible 那行**逐字未变**,
   `stayfield` 是「待批」)。建议与 `fieldregen` / `wandbleed` 编成**野区续航族**同一波买 (a)。
2. **批测台**:入集后一波,申报目的 = 该族条件 (a)。**本轮不提 `queue.json` 请求** ——
   没批准之前提波次是空转;批准即可直接排。
3. **总监(新增一条)**:`test_itemdesire_world_assertion` 的 `crash_2597` 在本轮之前
   就少了 2,四棵树复现不出原 pin(见 §5c)。它是**已 promote 的 `tpsafe2` 路上的行为变化**,
   归 GH #100 那一系,本组只测不裁。
4. **录像组**:核验按 **#110 已废止旧判读标准**后的口径(有危险撤退合法;无危险低血回家 = 病例)。
5. **本组下一个杠杆(不折进本 gate)**:**步行回泉那一半**。`mode_retreat_generic:217` 已经
   接了(已 promote 的)`J.ShouldStayAndRegen`,所以走路回家**有部分覆盖** ——
   但**两个盲点一模一样**(受击不归因、回复品集合只有 flask/tango + 金钱 ≥90)。
   把新 helper 接到那里是下一个**单**杠杆。
6. **给 owner 的一条免费事实(录像组量的,本组转记)**:22 次真·回家 TP 里带**大药**的是
   **0/22** ⇒ owner 说的「买大药」在当前 buy list 下**没有可买对象**;
   **补给侧可能才是 P2 承重的那一半**,决策侧单独上很可能只是把 bot 钉在线上干耗。

## 7. 验收

```
luacheck bots game --formatter plain        -> 0 warnings
lua5.1 tests/run_tests.lua                  -> 见下面的诚实说明
tests/test_replay_260822_lina_tp_home.lua   -> 20 tests, 0 failures(单跑)
```

**全套的诚实说明(不粉饰)**:本轮 push 时 `lua5.1 tests/run_tests.lua` **还在跑**
(单进程 ~1145 例,本容器实测 >45 分钟未完;上一次同样的全套跑到一半被我 kill 掉过)。
**在它之外,本轮把每一个被我碰过的文件都单独跑了,而且是修完之后跑的**:

| 文件 | 结果 |
|---|---|
| `test_replay_260822_lina_tp_home`(新增) | **20 例 0 failures** + 8 条变异 |
| `test_level_gate_census` | **15 例 0 failures**(修前 3 红) |
| `test_activemode_world_assertion` | **13 例 0 failures**(修前 2 红) |
| `test_gamemode_world_assertion` | **27 例 0 failures**(修前 5 红) |
| `test_pingstamp_world_assertion` | **18 例 0 failures**(修前 6 红) |
| `test_gate_claim_consistency` / `test_fixture_roles` / `test_smoke_load` | 7 / 10 / 2 例,**全 0 failures** |

**全套仍在跑,收尾时如果它判红,本组下一次触发的第一件事就是修它并记进报告** ——
按 0m 的规矩,这句写成**有时间戳的观察**,不写成「全套是绿的」。
(为什么不等:stop hook 要求提交推送,而全套在本容器里的耗时是分钟级到小时级的不确定量;
**把已验证的东西留在本地不推**才是本项目吃过亏的那一类,见铁律与 0m。)

`state.json:stayfield_20260822`(含全部数字、变异清单、下一棒)。
`new_gated_awaiting_ab` 追加 `stayfield`。

## 8. 本次会话 token 用量

```
TOKENS total_in=67,484,909 out=192,354 turns=310
```
(统计时刻在 push 之前;之后的收尾回合不计入。**本轮偏高的成因是可解释的**:
四个普查文件的分母搬迁要**反复整语料驱动**(gamemode 的竞价、pingstamp 的 push 地板、
activemode 的 930 帧扫描各跑了两三遍),这部分不是重复劳动,是 GH #106 那一类的连带代价。)
