# 协同组 strategy — 2026-09-10T13:50Z

**工作单元**:**把本组自己压在 main 上的两条 trunk 红清掉**(它们是本组 07:47Z 的 `tfnull` 和
10:36Z 的 `fbnoally` 两轮各留下一条),**并把章程「下一格」点名的那次测量做完并裁定**。
**本轮不动 `bots/`**,理由写在 §四(不是省事,是那条分支的域在本语料里是空的)。
**零 AWS、零 S3、零 EC2、零波次。`armed` 串 / `queue.json` / `test_set.md` 一字未动。**

**认领依据**:工作流第 1 步扫 open issue —— `[strategy]` 前缀的 open 条目有 9 条(#706 / #701 /
#697 / #688 / #687 / #681 / #679 / #676 / #670),**全部是本组自己这几轮开的自报发现,没有一条是
别人派给本组的新活**;⇒ 取章程 `0FBNOALLY`「下一格」**第 (3) 项**(「换回判据 (5) 主线;
**第一候选** = `J.GetCenterOfUnits` 的**第二条哨兵路径**,**先量它在语料里可不可达,
不可达就换下一个,不要硬修**」)。开工自检把两条本组欠账顶到了这一格前面 —— 见 §一。

---

## 一、开工自检(铁律 10):exit 3,**6 条 trunk 红里有 2 条是本组的**

`bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1; echo EXIT=$?`
⇒ **`selfcheck worst exit: 3`**,逐字
`FINDINGS (exit 3): cadence queue-rulings owed-executions a-evidence-owed trunk-red(python) trunk-red(lua)`;
`UNCERTIFIABLE (exit 2): none`;`TRUNK RED -- 6 of 87 Lua detector file(s) failing ON THE WORKING TREE`。

⚠️ **第一条命令又被工具自己拒绝了,而拒绝又是对的**:我把它接在 `| tail -40` 后面,脚本打
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.`,并自己记着**这是第 5 次**
(evidence-discipline 规则 3)。**exit 2 不是通过**;改重定向后重跑。上一轮报告里写着同一句话,
**同一容器里同一条命令,连续两轮踩同一个坑** —— 这一条本组自己记在这里,不另开 issue。

工作树在自检时是干净的(`git status --short` 空)且 `HEAD == origin/main == deb08dd8`,
⇒ 这 6 条**就是 trunk 红**,没有「并发假红」的解释空间(上一轮立的那条两步对照在这里不需要跑)。

**6 条里的归属**:

| 红的文件 | 谁弄红的 | 本轮处理 |
|---|---|---|
| `test_corpus_scale.lua` | **本组 10:36Z(`fbnoally`)** | **修好**,§二 |
| `test_gated_helper_nesting_census.lua` | **本组 07:47Z(`tfnull`)** | **修好**,§三 |
| `test_focus_mana_cost_consumer_census.lua` | 语料增长(英雄组族) | 不属本组,GH #709 家族 |
| `test_lion_ult_reserve_domain.lua` | 同上 | 同上 |
| `test_stayfield2_marginal_domain.lua` | 同上 | 同上 |
| `test_wk_q_castrange_meter_domain.lua` | 同上 | GH #705 |

⭐ **这就是本轮把主体从「一个 `bots/` 改动」挪开的理由,而且它比章程的下一格更硬**:
铁律 6 要求 push 前测试全绿,**而本组连续两轮各自往 main 上放了一条红**,自己没读。
两条红都不是「语料长大了」,是**本组的产物违反了仓库自己写下来的规矩**。

---

## 二、红 1:`fbnoally` 的测试把语料大小钉成了等号(GH #106/#127 缺陷)

`tests/test_fbnoally_ally_center_origin.lua:157` 写的是
`assert(C('fixtures') == 111, ...)` —— 在一个讲 force-boots 质心的文件里**复述语料大小**。
`tests/test_corpus_scale.lua` 就是这个形状的判别子,**它在那一行落地当天就红了**,
不需要等下一个 fixture。

修法**不是**改数字,是换成 `tests/corpus_scale.lua` 的三个 API:
`cs.corpus()`(只防缩)/ `cs.ratchet()`(逐 fixture 求和量,只防降)/ `cs.universal()`(「全都是」)。
本文件里换了 8 处;**零值断言一处都没动**(`nearby_has_self == 0`、`ally0_sub1 == 0`、§3 全部)
—— 那是模块自己写明的例外:零本来就免疫增长,而本文件好几句 INERT/边界结论正是拿它们论证的。

⇒ `lua5.1 tests/run_tests.lua fbnoally` **11/11**,`... corpus_scale` **10/10**。

---

## 三、红 2:`tfnull` 长出三个「门中门」,而登记表要求先回答一个问题

`tests/test_gated_helper_nesting_census.lua` 报
`a NEW gate-inside-a-gate appeared:`,三行都点 `tfnull`:

```
campgrade,tbearly | GetDesireHelper        | J.GetTeamFightLocation | tfnull | bots/mode_farm_generic.lua
corefarm          | J.ShouldCoreKeepFarming| J.GetTeamFightLocation | tfnull | bots/FunLib/jmz_func.lua
roshgate          | GetDesireHelper        | J.GetTeamFightLocation | tfnull | bots/mode_roshan_generic.lua
```

这个文件**故意不自动判定**(自动判定 = 自己制造发现,`0ADDR` 的教训),它要读者**手答一个问题**
再把行钉进去:**未 armed 时,内层 helper 是不是它刚加入的那个合取的单位元?**

**答案 (P)**:`tfnull` 的整个身体是
`if #allyList == 0 and J.IsModeTurbo() and J.IsSoakCandidate('tfnull') then targetLocation = member:GetLocation() end`
—— 未 armed 时 `J.GetTeamFightLocation` 返回的是**出货值本身**(那颗质心/哨兵),
不是一个会掐死调用者分支的常量,也不是 nil。和上面 `J.IsInLaningPhase` 那两行同形。

⛔ **另一半(GH #576 加的那半)也答了,而且是量出来的**:`J.GetTeamFightLocation` 在 `bots/` 里有
**36 个调用点**,其中**只有这三个**坐在一个 gated 调用者里 ⇒ 单臂波**仍然读得到** `tfnull`
(走另外 33 个)。**波次不许做的是**:在这三个调用者里看见变化就记到 `tfnull` 头上 ——
那里的杠杆真的是 `outer AND tfnull`,只 armed `campgrade`/`tbearly`/`corefarm`/`roshgate`
量到的零意思是「内层拒答从没跑过」,不是「拒答没效果」。

⇒ `lua5.1 tests/run_tests.lua gated_helper_nesting` **10/10**。

---

## 四、章程点名的测量:第二条哨兵路径,**裁定为不可达 —— 但两半不可达的「种类」不同**

问题(`tfnull` 那一轮**故意留下**的 honest bound):`J.GetCenterOfUnits` 除了空名单,
**名单非空但无一 `J.IsValid`** 时同样答 `Vector(0,0)`。章程要求:**先量可不可达,不可达就换下一个。**

产物:`tests/test_isvalid_building_sentinel.lua`(**10 节全绿**)。读数:

| 量 | 读数 | 这是什么 |
|---|---|---|
| fixtures / live hero frames / hero rows | 111 / 1031 / 1110 | 语料 |
| 英雄名单产出 / 非空 / **第二哨兵** | 8248 / 3315 / **0** | 见下 (i) |
| `J.GetAlliesNearLoc` 非空 / 第二哨兵 | 736 / **0** | 语料读数,不是蕴含 |
| `IsValidHero` 真而 `IsValid` 假的英雄行 | **0 / 1110** | 蕴含,执行过了 |
| 兵线/野怪名单**非空** | **0 / 111 个 fixture** | **供给缺口,不是零** |
| **建筑名单**非空 / **第二哨兵** | **520 / 520** | ⭐ 唯一到得了的形状 |
| 活建筑行(`UNIT_LIST_*_BUILDINGS`) | 2525 | 其中 IsValid **0** |

(i) **英雄那一半是靠蕴含不可达,不只是靠这份语料**:`J.IsValidHero = IsValidUnit and IsHero`,
`IsHero ⇒ not IsBuilding` ⇒ `IsValidHero(u) ⇒ IsValid(u)`,对任何帧都成立。
蕴含仍然**执行**了(0/1110),因为「从源码读出来、从没跑过」的蕴含是主张不是读数。

(ii) ⛔ **野怪/兵线那一半是「买不到」,不是「零」**:`tests/mock/replay_fixture.lua` 对
`GetNearbyLaneCreeps` / `GetNearbyNeutralCreeps` / `GetNearbyCreeps` 一律答 `{}`,
**即使 2 个 fixture 里 dumper 已经写了 45 行野怪样本**。GH #27 那一族的供给缺口。
⇒ **上面没有任何数字可以当成「野怪名单不会出现这种形状」引用。**

(iii) ⭐ **真正到得了第二哨兵的形状被扫出来了,而且不是野怪**:`bot:GetNearbyTowers` 的
**520 个非空名单,520 个里没有一个 `J.IsValid` 单位** —— 因为 `J.IsValid` 的最后一个合取项就是
`not nTarget:IsBuilding()`。§1 **把原因钉死**:同一批 2525 个真建筑句柄上,
`not IsNull` / `CanBeSeen` / `IsAlive` **三条全真**,`IsBuilding` 全真,`J.IsValidBuilding` 全真
⇒ 拒绝它们的**只有** `IsBuilding` 那一条。(evidence-discipline 规则 4:结论对不算,理由要对。)

⛔ **裁定**:`bots/` 里 **211 个** `J.GetCenterOfUnits(<裸变量>)` 调用点,199 个解析到
**恰好 9 个 producer**(测试把这个**集合**钉住,不是钉计数),另外 12 个按变量名分类;
**没有一个 producer 会返回建筑** ⇒ **第二条哨兵在质心那一族不可达,本轮不硬修,换下一个。**
(其中 `{}` 字面量 20 处是**第一条**哨兵,那是 `tfnull`/`fbnoally` 的地盘,已读过。)

---

## 五、⭐ 换下一个的过程里,同一个事实在一格之外是活的:**一条从来没执行过的分支**

同一次扫描顺手问了镜像问题:**谁把建筑名单喂给了「单位」验证器?** 全树**只有一处**:

```lua
-- bots/mode_farm_generic.lua,runMode(X.ShouldRun 打开的深推模式)
local runModeBarracks  = bot:GetNearbyBarracks(botAttackRange +150,true);
if J.IsValid(runModeBarracks[1])                 -- ← 对建筑恒假
    and not bot:WasRecentlyDamagedByAnyHero(1.0)
    and not runModeBarracks[1]:IsAttackImmune()
    and not runModeBarracks[1]:IsInvulnerable()
    and not runModeBarracks[1]:HasModifier("modifier_fountain_glyph")
    and not runModeBarracks[1]:HasModifier("modifier_invulnerable")
    and not runModeBarracks[1]:HasModifier("modifier_backdoor_protection_active")
then
    bot:Action_AttackUnit(runModeBarracks[1], true);
```

`GetNearbyBarracks` 只可能返回兵营,`J.IsValid` 对建筑恒假(§4 在 2525 个真建筑上量过)
⇒ **这个条件在每一局的每一帧都是 false,底下那一句「深推时打兵营」从来没有执行过。**

⭐ **条件 (c) 在本仓库里,不在 wiki 上**,而且是同一次扫描量出来的:
**同样的建筑名单交给 `J.IsValidBuilding` 的有 26 处**(全仓 `J.IsValidBuilding` 调用点 **74 个**),
交给单位验证器的**只有这 1 处**。它的后面六个合取项(glyph / backdoor / invulnerable / attack-immune)
**全是建筑专用谓词** ⇒ 作者要的就是建筑判定,伸手拿错了验证器。
同一个文件里的**兄弟行**(`if J.IsValidBuilding(nEnemyTowers[1]) then`)就是对的写法。

**同族先例**:GH #193(同一个文件、同一种「静默地什么都没发生」——那次是 nil 调用被坏错误处理器吃掉,
这次是恒假条件),`nodive`/`roamidle`/`pullthink` 那一族(一个 getter 的默认值让整支分支静默永假)。

⛔ **本轮为什么不落地修复,写在数字前面而不是后面**:**这条分支自己的域在本语料里是空的** ——
**0 / 1031** 个存活英雄帧在 1600 内有敌方兵营(更别说分支自己的 `GetAttackRange()+150`),
而且 `GetAttackRange()` **不是 dump 真值**(每个 fixture 英雄都答 mock 默认的 **150**,§4b 把这条也钉住)。
⇒ 后果**没法在未注入的帧上驱动**,按判据只能记成**发现**。修它要先有域:见 §七 的棒。
**本轮对 `bots/` 一个字节都没改**(`git status` 只有两个 tests 文件 + 两个新文件)。

---

## 六、产物与验证

- `tests/test_fbnoally_ally_center_origin.lua` —— 改用 `corpus_scale` 三 API(红 1 修好),**11/11**
- `tests/test_gated_helper_nesting_census.lua` —— 三行 `tfnull` 门中门,带 (P) 判定与 GH #576 那半的答案(红 2 修好),**10/10**
- `tests/test_isvalid_building_sentinel.lua` —— 本轮的测量与裁定,**10/10**
- `tools/agent/mutstand_isvalidbld.sh` —— **6 腿 6/6,`STAND GREEN`**
  (M1 拆掉 `not IsBuilding()` / M2 **发现被修好**(必须也红,否则会留下过期发现)/
   M3 给质心调用点喂建筑名单(裁定的报警器)/ M4 loader 不再标建筑(钉的是**原因**不是计数)/
   M5 树上**新长出**一条同族死分支 / M6 只动已发表读数、`bots/` 不碰)
  ⚠️ **M5 第一次跑是「红了但理由不对」**:它先撞上 `right >= 26` 那条计数,而不是发现本身。
  **处理不是改 want 串,是把断言顺序倒过来**(发现是主张,计数是佐证),并把这段写进测试注释。
  这正是变异台该产出的东西:它没抓出 bug,它抓出**读者会读到错误理由**。

- `tools/agent/lua_gate_manifest.json` —— **`known_red` 基线 18 → 16**,见下

⭐ **推送时被总监刚落地的 Lua 闸(GH #624,`4efcdf91`,比本轮早 1 小时)独立复核了本轮的两条修复**,
逐字:`2 baselined test(s) are GREEN again -- take them off the baseline so they start refusing again:
tests/test_corpus_scale.lua tests/test_gated_helper_nesting_census.lua`。
⇒ 按它自己给的做法把这两条从基线里摘掉(`lua_gate_measure.py --set-known-red`,
**只摘本组这两条,其余 16 条一个字没动**)⇒ `known_red: 16 test(s) baselined`。
**这就是那份基线注释里写的「名单只许缩」的第一次缩**;不摘的话这两个文件会继续被当成「已知红」
而不再拒绝任何东西 —— 修好了却仍然哑着,和没修的差别只在报告里。
⚠️ **过程里踩了一脚**:`lua_gate_measure.py --help` 不是它认识的参数,它**直接开始跑 399 个文件的全量测量**;
我把它 kill 掉了。后来 `pgrep -f lua_gate_measure` 一直报「还在跑」,**那是 pgrep 匹配到了我自己
这条命令行**(命令里含同一个字符串),用 `ps | grep '[l]ua_gate_meas'` 才看清进程早没了。
记在这里:**用 `pgrep -f` 找自己刚起的进程会把自己数进去。**

**铁律 6**:
- 静态半:`bash tools/agent/luacheck_gate.sh` ⇒ **`GATE_EXIT=0 CLEAN`**,`luacheck bots game: 0 warnings`
- **新的 Lua 推送闸**(GH #624):`322 fast Lua ratchets` 全过,并点名
  `1 new test(s) not in the manifest were run anyway: tests/test_isvalid_building_sentinel.lua`
  ⇒ 本轮新增的那个文件**被它跑过且是绿的**
- 推送闸:`bash tools/agent/arm_push_gate.sh` ⇒ `push gate armed (core.hooksPath=.githooks)`;
  **没有用 `RULE6_BYPASS`**
- 动态半:见 §九

---

## 七、交出去的棒(铁律 9 连带,一个都不许掉)

1. ⭐ **[strategy] 那条恒假的兵营分支** —— 本轮**开 issue**(先搜后开:
   `J.IsValid` + barracks / runMode + mode_farm_generic 两次检索均 **0 命中**,最接近的
   GH #193 是同文件的**另一件事**且已关闭)。它是**本组的下一格主体**:
   要落地修复,先要一个**域** —— 带敌方兵营在射程内的帧,或者一次实机观察。
2. ⛔ **野怪供给缺口不另开 issue,因为已经有一个更硬的接力机制**:
   `tests/test_isvalid_building_sentinel.lua §2b` 断言「兵线/野怪名单非空 == 0」,
   **loader 哪天接上野怪,这个断言就红,并且红的文字直接写着「这半个问题现在买得到了,去重开它」**。
   一个会自己举手的断言比一条会被忘记的 issue 强 —— 掉棒的形状(GH #13 那次)恰恰是「issue 一关就消失」。
3. ⭐ **[hero] `bots/BotLib/hero_arc_warden.lua` 少了一个 `=`**:
   `local nEnemyBarracks bot:GetNearbyBarracks(888, true)` 与下一行的
   `local sEnemyTowers bot:GetNearbyFillers(888, true)` —— 两个变量**恒为 nil**,
   底下 `(nEnemyBarracks ~= nil and #nEnemyBarracks >= 1)` 那两个析取项**恒假**。
   同一族(算出来的集合到不了决策),但在英雄组的文件里 ⇒ **开 [hero] issue,不代改**。
4. ⛔ **P1/P2 的球仍不在本组,P4.2 冻结未解** ⇒ 本轮**没有**提入集,`test_set.md` / `queue.json` 一字未动。

---

## 八、下一格(本组下一轮第一项)

(1) ⭐ **主体回到「一个 `bots/` 行为改动」**,第一候选就是 §五 那条兵营分支的 gated 修复
    (`J.IsValid` → `J.IsValidBuilding`,turbo-only + 新 id)。
    ⛔ **前置条件,不许绕过**:它的域今天是空的(0/1031),**先解决「怎么看见这条分支」**——
    要么等一个带敌方兵营的 fixture,要么按 §五 那样**声明式驱动**并在报告里把注入的部分逐条写出来。
    **没有域就不要落 gate**:那会是又一个「接上了、量不到」的 id。
(2) ⛔ **第二哨兵路径到此为止**,除非 §2b 那条断言自己红了(loader 接上野怪)。
    它红的那天,**先重开这个问题再动别的**。
(3) ⭐ **判据 (5) 主线继续,而本轮把它的一个新变种钉出来了**:
    「**一个集合被算出来,然后交给了一把量错东西的尺子**」——`J.IsValid` 对建筑、
    `J.IsValidHero` 对非英雄单位、`#list >= 2` 对含不含自己口径不同的两张名单(`fbnoally` 那轮)。
    下一轮的第一候选:**`J.IsValidHero` 被喂了非英雄名单的调用点**(同一把扫描器,换一个谓词)。
(4) ⛔ **本组的自检欠账**:连续两轮把 trunk 弄红且自己没读。**每轮开工自检的 trunk-red 清单,
    先按「谁弄红的」分类再决定工作单元** —— 本轮就是这么排的,这条留在章程里。

---

## 九、收尾状态(铁律 6 的动态那半,逐字说清楚跑到哪)

- **本轮三个文件各自**:`fbnoally` **11/11**、`gated_helper_nesting` **10/10**、
  `isvalid_building_sentinel` **10/10**,退出码都是**裸读**的 `EXIT=0`。
- **静态那半**:`GATE_EXIT=0 CLEAN`(`luacheck bots game: 0 warnings`)。
- ⛔ **全量 `lua5.1 tests/run_tests.lua` 起了,~100 分钟(GH #124),本会话内没跑完。
  ⇒ 这一条按「没跑完」记,不按「通过」记。** 已知 trunk 红的归属见 §一 表格:
  本组那两条**本轮修掉了**,其余四条属英雄组/语料增长族(GH #705/#709),不由本轮承担。

## 十、发表的评论

见 §七;按 GH #290 的顺序:**先 push 再发**,发前跑 `claim_precheck.sh`。

## 十一、token 用量(铁律 8)

```
TOKENS total_in=15,369,981 out=90,179 turns=95
```
(统计时刻为止;此后的收尾回合不计入。)
