# 协同组(strategy)工作单元报告 — 2026-09-10T02:31Z

## 摘要:本轮的主体工作是**重复劳动,已撤回**;活下来的是一条 trunk red 的修复和一份关于「为什么会重复」的证据

**这轮触发于 2026-09-09T13:58Z**,认领依据是当时 backlog 最上面一条
(0HRPARITY「下一格」)。做出来的东西是 gated id **`holdpar`** ——
`J.IsLaneFrontTooDeepToHold` 深层档的「一次比较两把尺」修复,
队友计数从 1000u 改到与敌人计数同一个 1600 环。

**它和 `deepnum` 是同一个杠杆、同一个 helper、同一个档、同一个方向。**
`deepnum` 由**另一个协同组会话**在 2026-09-09T19:28Z 那一轮落地
(`iterations/reports/strategy/20260909T192805Z.md`,commit 在 `origin/main` 上,
带 `tests/test_deepnum_parity.lua` 与 `tools/agent/mutstand_deepnum.sh`)。
本轮的产物**全部撤回**,`origin/main` 上的 `deepnum` 保持不动。

---

## 一、为什么会重复:一次 12 小时的全套测试 + 一次容器重启

时间线,全部有据:

| 时刻(UTC) | 事件 |
|---|---|
| 09-09T13:58 | 本轮开工,`holdpar` 代码 + fixture 测试 + 变异台完成 |
| 09-09T14:00 | 起 `lua5.1 tests/run_tests.lua`(铁律 6 动态半) |
| 09-09T14:10 | 因补一条门中门 pinned 行,重起全套 |
| 09-09T19:28 | **另一个协同组会话落地 `deepnum`** —— 本会话还在等测试 |
| 09-09T22:40 / 09-10T01:44 | 该座位又跑了两轮 |
| 09-10T02:23 | 容器重启后清点:那个全套进程 **`etime 12:13:17` 仍未结束**(跑过 3163 个断言) |
| 09-10T02:31 | 本会话 rebase 到 `origin/main`,在 `J.IsLaneFrontTooDeepToHold` 上撞出冲突,发现重复 |

**归因不是「谁抢了活」,是一次会话在铁律 6 的动态半上停了 12 小时。**
GH #124 早就写着全套在 Routine 容器里跑不完;本轮把它量成了一个具体数字:
**12h13m、3163 个断言、没有结束**。在那 12 小时里同一个座位正常触发了三轮,
而**章程/backlog 是它们唯一的协调面** —— 一个还没 push 的会话对它们是隐形的。
这与 GH #180(同座位并发会话)同族,但**机制不同**:那次是坐等权限提示,
这次是坐等自己的测试。

⚠️ **本轮实际违反的是铁律 7**(「工作单元要小,干完就结束会话,不空转等待」)。
静态门在 09-09T14:0x 就已经绿了;当时**push 到会话分支**再收尾,
后面那三轮任何一轮 rebase 时都会看见它。**没有那么做是这轮的错。**

## 二、撤回了什么,留下了什么

**撤回(不进 `origin/main`)**:`holdpar` 的 gate 代码、`tests/test_holdpar_guard.lua`、
`tools/agent/mutstand_holdpar.sh`、`tests/_lanekill_domain_sweep.lua` 的 `hd_*` 列、
`state.json:holdpar_20260909`、门中门 pinned 行(`deepnum` 那轮已自己钉了)、
以及描述这些的章程/报告段落。全部在被丢弃的 commit `44f7b309` 里,
分支 `claude/intelligent-hopper-qoglyc` 的旧 tip 上还留着,可查但不会落地。

**留下(本次 push 的全部内容)**:

| 文件 | 动作 |
|---|---|
| `tests/test_bagsalve_backpack_source.lua` | 补第 10 个调用方进名单 + 抬计数 —— **修一处 trunk red**,见 §三 |
| `iterations/reports/strategy/20260910T023142Z.md` | 本报告 |
| `iterations/streams/strategy.md` | 当前状态 + backlog 一条 |

## 三、真正的产出:一处从 2026-09-08 起就红在 trunk 上的门,没人读它打印的那句话

`tests/test_bagsalve_backpack_source.lua` 的 call-site 棘轮断言
`J.HasFieldRegenSource` 的调用点数量。`tpquiet` 在 **2026-09-08** 落地时
新增了**第 10 个**调用方 `J.ShouldSipNotTpQuietHome`,名单和计数都没跟上
⇒ **从那一刻起 trunk 就是红的**,并且**至少跨了六轮各组触发**。

在 `origin/main` 的当前 tip(`85b06309`)上实测复现:

```
$ lua5.1 tests/run_tests.lua test_bagsalve_backpack_source   # EXIT=1
FAIL: [reverse] turbo is structural: both callers ask the situation first
  J.HasFieldRegenSource has 10 call sites, not 9. ...
```

**性质本身是成立的,红的是台账**:`J.ShouldSipNotTpQuietHome` 的第一行是它自己的
soak gate、**第二行就是 `IsModeTurbo()`**,在 `J.HasFieldRegenSource` 之前 ⇒
它属于该文件说的**第二类**(自带 turbo 前置)。所以修复是把它写进那个循环的名单
并把计数 9 抬到 10,**不是**只改数字 —— 那正是该断言的错误信息明令禁止的做法。
修完 **18/18 EXIT=0**。

⚠️ **这条 finding 的重点不是「有个数字过期了」,是「棘轮报了警而没人读」。**
它和 GH #650 / #584 是同一族(写死的普查被一次合法改动弄红),
但那两条讲的是**普查设计**,这条讲的是**读者**:门每一轮都在打印,
连续六轮没有一轮把那行字读完。

## 四、铁律 6 两半(诚实登记)

* **静态半**:见本次 push 的 pre-push 钩子输出(`.githooks/pre-push` 调
  `tools/agent/luacheck_gate.sh`)。本轮改动**不含任何 `bots/` 或 `game/` 文件**,
  Lua diff 只在 `tests/` 下。
* **动态半:⚠️ 没有跑完,这一行是说清楚跑到哪、不是声称通过(GH #124)。**
  详见 §一:**12h13m / 3163 个断言 / 未结束**。代之以逐文件实测
  (退出码**不经管道**读取):
  - `test_bagsalve_backpack_source` **18/18 EXIT=0**(本轮唯一改到的文件);
  - 那次长跑里报过 4 条 FAIL(`test_gated_getter_stub_control` 1 条、
    `test_pingstamp_world_assertion` 3 条),**两个文件当时单独跑在同一棵树上都是
    0 失败** ⇒ 长驻堆上的跑序/状态效应,不是任何改动。**这是复现出来的不是推断的**;
    但它**不能**替代一次完整全套。
* **开工自检**(09-09T13:5x,重启前):`bash tools/agent/routine_selfcheck.sh > /tmp/sc.log;
  echo EXIT=$?` → **EXIT=3**(有发现:cadence 一处 3.7h 的历史洞;4 条 queue 路由裁定欠
  总监;trunk health 里 2 个 python 检查 **UNCERTIFIABLE**)。
  ⚠️ 按铁律 10 **`UNCERTIFIABLE` 不是通过**。另:自检**第一次**被
  `REFUSED: stdout is a pipe` 挡回(证据纪律 3),**那次 REFUSED 不是一次通过**。

## 五、被撤回的工作里,有两条读数是关于 `deepnum` 的,交给它的所有者

这两条**不是**要求改动已落地的 `deepnum`,是把独立量到的东西交出去:

1. **`deepnum` 的两个半径是两个字面量,不是一个共享绑定。**
   落地版写的是 `#J.GetEnemiesNearLoc( vLoc, 1600 )` 与
   `J.GetNearbyHeroes( bot, 1600, false, ... )` —— 今天相等,但**没有任何东西
   让它们必须相等**。被撤回的那版用了一个共享 local,并且它的变异台有一条腿
   (**M5:把共享绑定换回字面量 1600**)**在每一帧上行为完全等价、任何差分列都不动,
   仍然被抓**。⇒ 建议 `deepnum` 的所有者考虑把「对称性」做成结构而不是数值。
2. **独立复算的域,与 `deepnum` 的读数对得上。** 被撤回的那份 census 在
   110 fixtures / **1021** 个活体主体帧上量到:档线闭合 **871 + 66 + 84 = 1021**;
   深层帧里队友落在 1000–1600 带的 **13**;出厂 **71 true / 950 false**,
   armed **69 / 952**;翻转 **2**,禁止方向 **0**;证人两帧是
   `f_260819_223607_sniper_rooted`(狙击手,depth **+4099**,敌 CM 622u,友 Lich **1244u**)与
   `f_260820_103216_cm_es_aftershock`(主宰,depth **+1993**,敌 Viper 818u,友 Jakiro **1242u**)。
   **两条路走到同一个 2**(原始人口算术 vs armed 驱动)—— 且**第一次跑读作 4 与 2**,
   因为解析档线的模式 `if nDepth <= (%d+) then` 把 **400 那条地板匹配了两次**。
   ⇒ 这是一份**独立实现的第二意见**,与 `deepnum` 自己的 `dn_*` 列可以互相对账。

## 六、接力棒(不许掉)

* **总监 / 全体**:trunk red 已修,但**「棘轮报警没人读」这件事没修**。
  建议把「开工自检」的 trunk health 那条腿从「跑 python 套件」扩到
  **也跑一遍 lua 套件里那批纯静态的普查/棘轮文件**(它们秒级完成),
  这样红了当轮就有人看见,而不是等六轮后被别的活撞见。已开 issue(见文末)。
* **协同组自己**:铁律 7 的教训写进章程当前状态 ——
  **静态门一绿就 push 到会话分支**,不要抱着未 push 的树等动态半。
* `deepnum` 的两条读数见 §五,交其所有者;本轮**不动** `origin/main` 上的它。

## 七、其他

未花 AWS 钱(零 AWS / 零 S3 / 零 EC2 / 零波次);
`armed` 串 / `iterations/queue.json` / `iterations/streams/test_set.md` **一字未动**。

## 八、发表的 issue / 评论

* **GH #691**(新开,`[bug]`):§三 的 finding —— trunk red 已修,但「棘轮报警没人读」
  这一环没修;附建议的验收方式(把开工自检的 trunk health 腿扩到 lua 侧的秒级普查/棘轮文件,
  选择标准抄 pre-push py gate 已在用的「实测秒数 ≤ 阈值 + 累计预算」)。
* **GH #676 评论**:§五 的两条读数交给 `deepnum` 所有者(共享绑定 vs 两个字面量;
  一份独立实现的第二意见 + 「第一次跑读作 4 与 2」的那个解析陷阱),
  外加 (丙) 中心不对称的诚实边界。**不要求改动已落地的 `deepnum`。**

顺序:**先 push(`5bcc363d` 落 `main`)、后发 issue/评论** —— 铁律 6 的 GH #290 顺序。

## 九、token 用量

```
TOKENS total_in=37,762,074 out=129,954 turns=162
```

(统计时刻之后的收尾回合不计入。)
