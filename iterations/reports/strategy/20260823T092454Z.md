# 协同组 2026-08-23T09:24Z —— GH #119 的下一根杠杆:**DO NOT WRITE**,而拒的理由是一次测量,不是一次顾虑

**认领**:GH #119(`[strategy]`,录像组 02:36Z 与 06:55Z 两次给出同一条建议,并在
06:55Z 交了钉帧 `f_260823_002103_wk_ancient_camp_634.lua`)。
**交付**:`tests/_fightback_sweep.lua` + `tests/test_fightback_world_assertion.lua`
(**第二十条世界断言**,11 例全绿,**4 变异 4 抓**)。**`bots/` 零改动,不产 gate。**
**零 AWS 支出,不提 queue 请求。**

---

## 0. 开工自检

`bash tools/agent/routine_selfcheck.sh` **worst exit 0** —— UNLANDED 无(可认证窗口内)、
cadence clean、trunk python **15 passed**。容器无 `lua5.1`/`luacheck`,已装。
开工时本地落后 3 个提交,`git pull --rebase` 后 `HEAD == origin/main == 043ee81`。

**认领依据**:`OWNER_PRIORITIES.md` 的 P1/P2 本组交付都已落地(球权核对已连续三轮
请主会话处理,本轮不再重复推理);`[strategy]` open issue 里 **#119 是唯一一条
「别组明确点名下一根杠杆该本组来写、并且刚刚为它交了钉帧」** 的。取它。

**先按 0FIX 验了一次**(上一轮那条流程条的第一次自用):`git ls-tree` 确认
`f_260823_002103_wk_ancient_camp_634.lua` **这一轮已经在 `origin/main` 上了** ——
上一轮(07:55Z)报的「不在树上」当时属实,**现在已被录像组补上,那条记录到此结清**。

---

## 1. 被要求写的是什么

录像组两次、原话一致:

> 判据要读**方向**,不要读量级 …… `GetAttackTarget() ~= nil`(或「同窗口内自己
> 打出过伤害」)一句就能把这一族排除干净

**这条建议的立论是硬的,本报告一个字都不打算驳它**:armed 腿 90 个纯野怪 episode 里
**41 个打出 > 挨的**;钉帧那位 11 级骷髅王 28 秒里打出 2261 / 挨 2110、打死两只石头人 ——
他不是「被野怪按着啃」,他在打野,`fieldcreep` 的否决在他身上是**反向**的。

问题只在**能不能在本地写出来并验它**。答案是**不能**,而且三种写法**不是同一种失败**。

## 2. ⭐ 三个读法,两种符号 —— 危险的是第三个

全语料 **104 fixture / 966 个存活英雄帧**,逐帧驱动:

| 读法 | 语料上的答案 |
|---|---|
| `bot:GetAttackTarget()` | **nil,966/966**(handle getter,loader 从不接线) |
| `J.IsAttacking(bot)`(树上现成,`jmz_func.lua:3067`) | **false,966/966**(读 `GetAnimActivity()`,全语料 **0**,而 `ACTIVITY_ATTACK` 是自动哨兵 **1158**) |
| `GameTime() - bot:GetLastAttackTime() <= 3.0`(「同窗口内自己打出过伤害」唯一的引擎写法,`jmz_func.lua:2052` 就是这个形状) | **TRUE,966/966** ← **陷阱** |

第三行是 **0 − 0**:`GetLastAttackTime()` 是没接线的 `Get*`(0),而 mock 的 `GameTime()`
是常数 0、loader 却用真实帧时间答 `DotaTime()`。**这个时钟不是本轮的发现** ——
它是**第十四条世界断言的成因 (1)**(`test_pingstamp_world_assertion.lua:55`)。
本轮记的是**同一个时钟落在一个新消费方上,符号相反、后果更坏**。

## 3. ⭐ 后果是量出来的,不是论证出来的

`fieldcreep` 在本语料上把 situation 关掉 **5** 帧(54 → 49)。把三个候选**作为排除条件**
(「我在还手就别否决」)分别套到这 5 帧上,各自交还:

| 排除条件 | 交还 | 读起来像什么 |
|---|---|---|
| `GetAttackTarget() ~= nil` | **0 / 5** | 杠杆是个 no-op |
| `J.IsAttacking(bot)` | **0 / 5** | 杠杆是个 no-op |
| `GameTime() - GetLastAttackTime() <= 3.0` | **5 / 5** | **整条子句消失了** |

**第三行请按它作者会读的方式读一遍**:普查打印 `vetoed 5 → 0`,打野帧不再被否决,
全部 fixture 绿,结论写「方向修法完美生效」。**它做的事是在每一帧上删掉整条子句**,
包括那两帧它本该留住的纯小兵帧。**这是一次朝着作者希望的方向假绿的绿**,
比 backlog `0p`(不可达分支静静地把功劳记给一个 gate)更难看出来。

## 4. ⭐ 而且「把时钟修好」不解决它 —— 只把谎话翻个面

任何人第一个会伸手去改的地方:让 loader 用帧时间答 `GameTime()`。同一条子句、同一批帧,
结果变成 **FALSE,966/966**,交还 **0 / 5** —— 从「全删」变成「全不动」。
**两种读数都是结构性的,没有一种是对那一帧的测量。** 这一条在用例里是**探针**不是散文
(与第十四条断言自己那个探针同法)。

## 5. ⭐ 钉帧本身也够不到这条子句,而且是第三个独立原因

`f_260823_002103_wk_ancient_camp_634`:hp **817/1307 = 62.51%**,
而 `J.IsFieldRegenSituation` 自己的血量带是 `nHP < 0.18 or nHP > 0.55 ⇒ false`。
**这一帧在 `fieldcreep` 上面四条子句就已经离开函数了**,armed 与不 armed 读数相同。
它是一枚真帧、也是它另一个立案(GH #137 等级阶梯,已落地为 `campgrade`)的正确钉帧,
**但这一族钉不到它上面**。`WasRecentlyDamagedByCreep(3.0)` 在这一帧**为真**(花岗岩/石头人
逐条带 `src`)—— 所以它**看起来**像个 fieldcreep 病例,这正是要把它写下来的理由。

## 6. ⭐ 一条被自己的用例当场抓住的错误(已进 backlog 0SRC)

第一版把血量上界**从整个文件里 `match`**:`jmz_func.lua` 有**两行**长得一模一样的
`if nHP < a or nHP > b then return false end`(**4674 是 0.18/0.75**,4767 才是本函数的
0.18/0.55),`string.match` 拿到的是**前一个**。于是断言报「WK 帧(0.6251)在带内
(0.18..0.75)」—— **整条结论会读反**。修法是**先抠出函数体再 match**,
已连同这段说明写进用例。**M13 那条「常数要从源码读、不要抄」是对的,但不够:
读的时候还得说清楚读的是哪一个函数的常数。**

## 7. 门

- `luacheck bots game --formatter plain` **exit 0 / 0 警告**。
- 新用例 **11/11**;**4 变异 4 抓**,每次都还原了树:
  - **M1** 把建议的读法真的写进 `J.IsFieldRegenSituation` → **棘轮转红**并指回本文件;
  - **M2** loader 学会答 `GetAttackTarget` → 2 红(`0 of 966 -- no longer universal`
    + `exclusion now fires on 5 vetoed frames`);
  - **M3** 血量上界 0.55 → 0.75 → 钉帧那条转红(**这正是 §6 那个错误的形状**);
  - **M4** loader 学会答 `GetLastAttackTime` → 2 红(陷阱读数不再普适 + 「全删」不再成立)。
- **M2 第一次跑的时候不是干净的红**:`cs.universal` 在 `nil` 上崩成
  `bad argument #3 to 'format'` —— **该出现结论的地方出现了一个崩溃**。已修:
  sweep **把每个桶预登记为 0**,所有 `== nil` 判据改成 `== 0`。**变异不只验断言有没有牙,
  也验红的时候说不说人话。**
- **定向复跑**:`grep` 出来的两个**枚举 `ls tests` 的普查**(`test_corpus_scale` 8/8、
  `test_defend_ping_declaration_ratchet` 8/8 —— 新增测试文件会不会绊到别人,按 0LN 的
  规矩查了),加上本轮引用其读数的一族:`test_fieldcreep_veto` **16/16**、
  `test_pingstamp_world_assertion` **18/18**、`test_gate_claim_consistency` **7/7**、
  `test_smoke_load` **2/2**。
- **全量没跑**(GH #124,约 100 分钟)。**本轮 `bots/` 一个字节没改、也没加 fixture**
  ⇒ GH #106/#107 那类跨文件破坏面**结构上不存在**,行钉普查(0LN/0LN2)同样不适用:
  `grep -rn` 确认没有任何文件写下我这两个新文件的坐标。**本轮不把它写成「整套又绿了一次」。**

## 8. 交棒(README 铁律 9 的连带规则:「修好」不等于「做完」)

1. **录像组 / GH #119** —— 已留言:**建议本身没被驳,是本地买不到**。要它可写,最便宜的
   解锁是 **`make_fixture.py` 顺手带一份「打出去」的伤害行**(dumper 的战斗日志已经有,
   06:55Z 那条留言就是从里面逐条读出来的:`DAMAGE skeleton_king → rock_golem 228`),
   放在现成的 `recent_damage` 旁边即可。**三个读数会同时变诚实。**
2. **总监** —— 无入集提议(**本轮不产 gate**)。要请你收下的只有一条口径:
   `fieldcreep` 的 (a) 维持 INDETERMINATE,而它的**下一根杠杆在语料能答之前不许写** ——
   用例底部的棘轮就是这条口径的执行体,它红的时候会指回本文件。
3. **批测台** —— 无请求。
4. **主会话** —— P1/P2 球权,**连续第四轮**请求核对(本组两项交付都已落地)。

## 9. 本组下一轮

`fieldcreep` 这条线**在语料补齐前是堵的**,不要再来撞。章程 backlog 上面全是流程条,
取第 **2** 条(−18 econ 残差归因,用批测归档录像做行为差分)或第 **4** 条
(`suptp × midtp` 协同仲裁,owner 点名)。若 §8.1 的「打出去」伤害行落地了,
**优先回来把这根杠杆写掉** —— 它的立论早就够了,缺的一直只是分母。

## 10. Token 用量

见文末(收尾时 `python3 tools/agent/token_usage.py` 的最后一行,原样贴入)。
