# 协同组 2026-09-17T16:35Z — 上一轮点名的「下一轮首选」,四条出口全部定价为零

**出口:(i) 未达成。** 本轮**没有**落任何 `bots/` 行为改动 —— 不是没找,是**每一条候选都被读数淘汰**。
**4.4 (i) 连续第八轮到此中断,这一行照实写,不美化。**
**零 EC2 / 零 CE / S3 读取 0 个对象(出网未计价)**;**未提入集**(P4.2 冻结);
⛔ **本轮不新增任何 armed id,成员串仍 25**。

产物:`tests/test_ring_subject_census.py`(4 checks)、`tools/agent/mutstand_ringcensus.sh`
(7 抓 / 0 存活 / 控制绿 / restore sha256 校验)、`tests/_claimring_probe.lua`(定价语料走查)。

---

## 0. 开工顺序,以及本轮欠掉的一件事

铁律 9 先读 `OWNER_PRIORITIES.md`。**P1 / P2 的球名义上都在协同组**,实况:

- **P1**(拉野):`pullcamp` 空带复读的球在**总监**(0NEXT38 (a)),本组不开第三条催办、不代跑。
- **P2**(低血不回家):**比优先项散文里写的完备得多** —— 决策侧两条腿**都已存在**:
  `stayfield`(TP 腿,已退集 `CALLSITE-EMPTY`)与 **`stayfield2`(步行回泉腿,`jmz_func.lua:6415`)**,
  外加 `tpstale` / `tprupt` / `fieldsip` / `fieldbuy`。
  ⛔ **我一开始判断「步行回泉腿从没做过」,读了源码后作废** —— P2 DoD 第 1 条基本已落地,
  卡住的是**买 (a) 证据**(魔棒仪器墙 + P4.2 冻结 + queue 十二条 pending),**那件事本组买不到**。
  ⇒ P2 这一轮对本组**没有可推进的工作单元**,不是被晾。

于是按 0NEXT38 取上一轮点名的「**下一轮首选**」:`J.IsOtherAllysTarget` / `J.IsAllysTarget`
的**圈心扎在发问者身上**(问题关于 `unit`,圈画在 `bot` 上 ⇒ 站在 `unit` 上但离我 900 的队友两者都隐形)。

---

## 1. ⭐⭐ 头条:那条「更大、而且可测」的杠杆,两个宿主各自因**不同**理由不可做

上一轮把它登记为「⭐ 更大,而且可测(几何全在 dump 里)⇒ 下一轮首选」。
**几何确实在 dump 里。可做性不在。** 两个宿主要分开算:

### (甲) `J.IsOtherAllysTarget` —— 域为空,而且不是「差一点」

`tests/_claimring_probe.lua`,112 个 fixture / 1039 个 live 英雄帧:

| 读数 | 值 | 含义 |
|---|---|---|
| `live` | 1039 | 语料 |
| `ally2plus` | **55** | 800 圈内 ≥2 个队友 ⇒ **出厂守卫在 984/1039 帧上开火** |
| `frame_has_extra` | **31**(3.0%) | 某个可见敌人 800 内有队友、而发问者的圈看不见他 = 杠杆的**原始**人口 |
| `pair_extra` | 43 | 同上,按 (帧, 敌人) 对计 |
| `frame_has_extra_2plus` | **0** | ⛔ 上两者的**交集**:0 / 1039 |

⇒ **只扩循环的迭代集合(守卫仍数出厂表)= 0 帧到得了**,与 0NEXT38 §甲 site A 的
`631 + 390 = 1021` 同形,逐字「an unpriceable repair is not a lever」。

⇒ **换成让守卫去数并集**?那样**这条杠杆只在未 promote 的 `soloclaim` 也 armed 时才活** ——
`pullcad` 合取陷阱(GH #622),章程明令禁止。
⭐ **而这里有一句可迁移的**:`#hAllyList <= 1` 这个守卫**数的就是那张圈**,
所以**「守卫」和「圈心」在这个函数里不是两个缺陷,是一个** ——
上一轮把圈心登记成「第二个、更大的缺陷」时,**它已经和 `soloclaim` 耦合了,只是当时没人算这一步**。

⚠️ `frame_has_extra_2plus = 0` **不报成巧合**:800 内 ≥2 个队友意味着**队伍是抱团的**,
抱团时贴着敌人的队友通常也在我圈里。**结构性原因,按实测读数登记。**

### (乙) `J.IsAllysTarget` —— 不可定价,理由是**语料里没有它的实参**

它**没有**守卫,所以圈心修法在它身上本是**独立单杠杆**。但:

- **16/16 个调用点传的是 creep / neutral / tower**(lich ×2、drow、zuus `nTowerTarget`+creep、
  huskar、shadow_shaman、phantom_lancer ×2、`mode_team_roam_generic` 的 `X.` 那份 ×6);
- 语料实测:**112 个 fixture / 1120 个英雄单位 / 非英雄单位 `0` 个**。

⇒ **它的实参在今天的语料里一个都不存在。** 与 0NEXT38 §乙 同族(那次是 `GetNearbyCreeps` 答空表,
这次更靠根:**dumper 就没出过非英雄单位**)。

⛔ **还有一道具名挡板,照 0NEXT38 §甲 先读测试再读代码,读到了**:
`tests/test_soloclaim_lone_ally_claim.lua:130-149` 把 `J.IsAllysTarget` **棘轮成判别子** ——
它的函数体**必须**保持同一个圈、**且不许**长出 `<= 1` 或 `ally ~= bot`。
而圈心修法要用的 `J.GetAlliesNearLoc` **包含 bot 自己**(它走 `GetTeamMember`),
去掉自己最自然的写法就是 `ally ~= bot` ⇒ **正面撞上那条棘轮,并且会拆掉上一轮整个论证赖以成立的判别子**。
⇒ **这是「上一个做它的人写好的挡板」的第二个实例**,而且是**写在测试里不在散文里**的那种。

---

## 2. 全树普查:把这个缺陷形状数完(⛔ 先问可测性,再问重不重要)

写了两个扫描器(`tools/agent/mutstand_ringcensus.sh` 里的 M1/M7 钉住它们还在数):

**(A) 主语/圈心不一致**:带 `unit|target|vLoc` 形参、而圈画在 `bot`/`GetBot()` 上
⇒ **230 个带主语形参的函数里 11 处**。逐条定价(全部进 `ASKER_ANCHORED` 表,**每行带理由**):

| 处 | 理由 |
|---|---|
| `J.IsOtherAllysTarget` / `J.IsAllysTarget` | 见 §1 |
| `J.IsHeroBetweenMeAndLocation` / `J.IsEnemyBetweenMeAndLocation` | ⭐ **出厂就是对的**:「between **ME** and」——发问者**就是**主语,而且它**两侧都读**(翻 flag) |
| `J.GetVulnerableUnitNearLoc` | **出厂就是对的**:圈是**施法者**的 `nCastRange`,之后再按 `nRadius` 过滤到 `vLoc` 附近 |
| `J.IsLaneFrontTooDeepToHold` | **前一轮已定价并登记**(加载器拒 `GetLaneFrontLocation`,GH #61;lane 几何欠着 GH #648/#652)⇒ 照 0NEXT38 §甲 **不重新发现** |
| `J.GetAttackEnemysAllyCreepCount` / `J.GetAllyCreepNearLoc` / `updateBotStateCache` | creep 圈;语料非英雄单位 0 |
| `X.IsAllysTarget` / `X.IsEnemysTarget` | 同形**第三份拷贝**;实参是 creep/neutral,且 `GetProperTarget` 在 1039/1039 帧上为 nil(GH #27) |

**(B) 名字/flag 不一致**:函数名说一侧、`GetNearbyHeroes` 的 `bEnemy` 字面量读另一侧
⇒ **2069 个带字面量 flag 的圈里 13 处**。12 处是**真阴性**(名字带 team/ally 但合法地需要读敌方,
`J.IsInTeamFight` 读敌方**正是上一轮 `fightfoe` 加的**)。剩一处是真的 ⇒ §3。

⭐ 顺路一条,**照 0NEXT38 §乙 挂标签**:`J.GetVulnerableUnitNearLoc` 的
`weakestHP = 10000` 是个硬上限,HP ≥ 10000 的单位永不被选。**语料最大英雄 HP = 2646**
⇒ **0/1039 可达 ⇒ 不可定价**。⛔ 不要读成「测过了没效果」。

---

## 3. ⭐ 本轮找到并**证明**的新缺陷(登记,**不发船**,并给它装了触发器)

`jmz_func.lua:14276`,`J.IsAllyHeroBetweenMeAndTarget(hSource, hTarget, vLoc, nRadius)`:

```lua
local nEnemyHeroes = J.GetNearbyHeroes(hTarget, 1600, true, BOT_MODE_NONE)
```

**`bEnemy` 是相对**圈心**的,而圈心是 `hTarget`(敌人)** ⇒ `true` 交出的是**我方**那五个。
于是这个「有没有人挡在我和目标之间」的函数:**第二个循环把队友又数了一遍**
(只是改成以目标为心),**敌方英雄一个都没枚举过** —— 而一个敌人的身体挡 Hookshot 挡得一样死。

**四条互相独立的判别子(⛔ 任一条单独都不够,合起来是闭式的)**:

1. **仪器**:`tests/mock/replay_fixture.lua:1472` `isEnemy = other:GetTeam() ~= self:GetTeam()` —— **相对圈心**。
2. ⭐⭐ **成对读数**,而且**就在调用点上方 7 行**(`rubick_hero/rattletrap.lua:429-430`):
   `nInRangeAlly = J.GetNearbyHeroes(enemyHero, 1200, **true**, …)` /
   `nTargetInRangeAlly = …(enemyHero, 1200, **false**, …)` ——
   **同一个圈心、两个 flag、名字正好相反**。这只在「相对圈心」下自洽。
   `hero_abyssal_underlord.lua:321-322` 与 `hero_spectre.lua:218-219` 逐字复现。
   ⇒ **全树 300+ 个非 self 圈心的调用点是对的**,本处是**离群的那一个**。
3. **同族的正确实现**:`J.IsHeroBetweenMeAndLocation`(§2)干**同一件事**,
   两个圈**都扎在 self 上**、靠翻 flag 拿到两侧。
4. ⭐ **归谬**:那个循环只排除 `hSource`。**若那张表真是敌方**,`hTarget` 自己就坐在 `vEnd` 上
   (垂距 0、`within` 为真)⇒ 函数**每次调用都返回 TRUE**,调用它的 Hookshot 分支**永远不可能开火**。
   它会开火 ⇒ **那张表不是敌方**。

### ⛔ 为什么登记而不发船(理由是数,不是判断)

- **调用点恰好 1 个**,在 `bots/FunLib/rubick_hero/rattletrap.lua`(Rubick 偷到发条的钩);
- **`tools/batch_test/soak/hero_pool.txt`(42 英雄池)里没有 rubick / rattletrap / clockwerk**
  ⇒ **那个调用点在任何批测局里都执行不到,在场域 0**。与 0NEXT38 §乙「两个调用点都在赏金猎人
  (不在草稿池)」**同一条判据**;
- 另外 `PointToLineDistance` 是引擎全局,**`.luacheckrc:321` 白名单里有、mock 里没有实现**
  ⇒ 要钉真帧 fixture 还得先买那个仪器。

⭐ **但「不发船」这次不是散文** —— `test_parked_reason_is_rederived_not_trusted` **把它的理由重新推导一遍**:
停摆的理由是**一个会变的外部事实**(rubick 不在池里),**今天没有任何东西会注意到它哪天变了**。
该断言读 `hero_pool.txt`,**rubick 一进池就报红**,并在失败信息里**写好该怎么修**
(`J.GetEnemiesNearLoc(vEnd, 1600)`,排除 `hTarget`/`hSource`,**可加性 ⇒ 只 FALSE→TRUE ⇒
对「往身体挡位开火」这条许可的纯收紧**)。

---

## 4. ⚠️ 本轮自己踩出来的一条,**不是趣闻,是给全队的**

⛔ **我把 `routine_selfcheck.sh` 放在后台跑,同时让变异台改写 `bots/FunLib/jmz_func.lua`。**
自检的 fast Lua 腿于是读到一棵**正在被改写的树**,报出:

- `test_lion_ult_reserve_domain.lua`:`jmz_func.lua:16305: '<eof>' expected near 'function'`(**语法错**);
- `test_lanekill_domain_census.lua`:`live hero frames FELL to 1018 (registered 1021)`、
  `l1trade 136 (registered 138)`、`153 (registered 155)` —— **三个计数"下降"**,
  而那条断言的文案正是「**Appending fixtures cannot lower a per-fixture count,
  so this is a behaviour change or a lost fixture — re-read the finding, do not re-baseline**」。

⇒ **一个并发写的树能伪造出「行为改动」的全部外观**,而**最危险的一面是它劝你去 re-baseline**。
📌 上一轮把这条互斥登记为「**靠排序**没有复发、**便宜处置而非修复**」——
**本轮复发了,而且排序没救它:我并没有颠倒顺序,我是把两件事同时跑了。**
⇒ **「靠排序」对并发无效**,这条应当升级成真闸(建议:自检开跑前对 `bots/` 取一次指纹、
收尾再取一次,不一致就打 `UNCERTIFIABLE` 而不是报 finding)。**⛔ 本组不改自检文件,交总监。**

⭐ 而**正确的处置已经在仓库里**:`lua_corpus.read_lua()` / `CorpusVanished` / 退出码 2(GH #856)
就是干这个的 —— 本轮的新测试**因此走 `lua_corpus`**(见 §5),
`tests/test_lua_corpus_stability.py` 也**正是这么逮住我的**。

---

## 5. 产物与闸门

**`tests/test_ring_subject_census.py`,4 checks,0.17s**:
§1 §2(A) 的 11 行**必须逐字相符**(新增一处 ⇒ 红,要么修圈要么补一行**带理由**;消失一处 ⇒ 红);
§2 **仪器契约**(`isEnemy` 相对圈心)—— 它一翻,§2 表里每条「读的是我方」的理由同时作废;
§3 缺陷的源码三钉 + **调用点集合恰好 = 那一个**;§4 **停摆理由重新推导**(rubick 进池即红)。

⛔ **它第一次跑就是绿的,所以我不信它** ⇒ **`tools/agent/mutstand_ringcensus.sh`:7 抓 / 0 存活 / 控制绿 / 退出码 0**
(M1 新圈、M2 仪器翻成 bot-relative、M3 缺陷被"修好"、**M4 rubick 进池**、M5 被定价的圈消失而行还在、
M6 `hTarget` 排除出现、**M7 扫描器自己停止匹配**)。

⚠️ **三条变异体原本不忠实,改的是台子不是测试**(0NEXT38 §丁 第二次兑现):
(a) M5 原本用**改名** ⇒ 先踩中 `new` 那条断言 ⇒ **RED-with-wrong-message**,对 `gone` 那半零信息,改成**真删圈**;
(b) M6 的 perl 用 `\t\t\t` ⇒ **一个字符都没匹配**,而 **perl 无匹配也退 0** ⇒ 计为绿色存活;
(c) 改成两个 tab 后**仍然绿** —— 它匹配到**全文件第一处** `if enemyHero ~= hSource`,
那处在 `J.IsEnemyBetweenMeAndLocation`(**另一个函数**)⇒ **变异落在断言不看的地方**。
📌 **判据:变异体要锚在被测函数的唯一串上,不要锚在同族共享的循环头上;
并且「mutate 命令退 0」不等于「mutate 生效了」。**

**两处是别人的闸替我逮住的,都已修**:
(i) `tests/test_mutstand_restore_trap.py` —— 变异台**必须用 sha256/git diff 证明 restore round-trip**,
不是只装 trap。已加,收尾打 `restore VERIFIED (sha256 round-trip on 4 file(s))`;
(ii) `tests/test_lua_corpus_stability.py` —— **不许再 open-code `os.walk(bots/)`**,要走 `lua_corpus`。已改(见 §4)。

### 闸门读数(照抄)

- `bash tools/agent/luacheck_gate.sh` → **`GATE_EXIT=0 CLEAN`,`luacheck bots game: 0 warnings`**。
- `bash tests/run_py_tests.sh`(**安静树重跑**)→ **`147 passed, 0 failed, 1 uncertifiable`**;
  uncertifiable = `tests/test_selfcheck_lua_leg.py`(**既有**的 120s 预算慢腿,GH #358,不是本轮的)。
  首跑是 `145 passed, 2 failed, 1 uncertifiable`,**两条 failed 都是本轮的**,已修(见上)。
- 单跑真码(⛔ 不经管道,evidence-discipline 3):
  `test_ring_subject_census.py` **EXIT=0** / `test_lua_corpus_stability.py` **EXIT=0** /
  `test_mutstand_restore_trap.py` **EXIT=0** / `mutstand_ringcensus.sh` **EXIT=0**。
- ⚠️ **`lua5.1 tests/run_tests.lua` 全量套件没跑完(~100min,GH #124),这一行不冒充它。**
  本轮**未改任何出厂 Lua**(`bots/` 在变异台收尾后经 **sha256 round-trip** 证明逐字复原;
  唯一新增 Lua 是 `tests/_claimring_probe.lua`,前导下划线 ⇒ `run_tests.lua` 不 glob 它)。
- **开工自检**:首跑 `EXIT=2` 且 **Lua 腿读数因 §4 的并发写而作废**。
  ⭐ **安静树重跑,把 §4 坐实**:**fast Lua detectors `135 tagged detector file(s), 0 failures`**
  —— 先前那三个"下降的计数"和那条语法错**全部是我造的 artifact,trunk 没红**。
  该轮 `legs run 13`、`FINDINGS (exit 3): cadence queue-rulings owed-executions lua-coverage`、
  `UNCERTIFIABLE (exit 2): trunk-red(python)`、**`selfcheck worst exit: 3`**。
  ⛔ **这四条 findings 没有一条是本轮的**:`lua-coverage` 点的是
  `test_campbind_poke_real_frame` / `test_dusttower_dive_guard` / `test_fieldsip_transfer_receiving_site`
  三个**别人的 Lua** 测试(本轮新增的是 python);其余三条是 queue/裁定/欠账积压。
  ⚠️ 管道防呆**第 13 次**、`timeout` 防呆**第 2 次**,本轮**两条都在第一条命令上撞到**。

### 覆盖面,照实说

`tests/test_ring_subject_census.py` **不在 `py_gate_manifest.json` 里** ⇒ 在 push 钩子里是
**fail-open(会跑,但拒不了 push)**。⛔ 不手改那个 manifest(它自己写着 "Do not hand-edit; re-measure"),
而 `py_gate_measure.py` **没有单测量模式**,全量重测会重写 147 行计时、
在一台有负载的容器上**挪动别人的入闸成员资格** ⇒ **本轮不做,登记为欠账**。
✅ 但它**有自动读者**:`tests/run_py_tests.sh` 按 `tests/test_*.py` glob,而开工自检每轮跑它
⇒ **不属于 GH #806 的 uncovered 集**(那一族是「没有任何自动读者」)。

---

## 6. 交给别人的棒

- **总监**:(i) §4 的并发写互斥**升级成闸**(建议指纹对比 ⇒ `UNCERTIFIABLE`),
  「靠排序」已被本轮证伪;(ii) `py_gate_measure.py` 加**单测量/增量模式**,
  否则每个新 python ratchet 都只能在 fail-open 里待着;(iii) 管道/`timeout` 防呆**前置成闸**(第 13/2 次)。
- **总监(待裁,本轮不催)**:`soloclaim_20260917` 等七条仍等着;armed 串仍 **25**。
- **语料(欠账,不绕)**:非英雄单位(creep/neutral/tower)**dumper 一个都没出** ⇒
  `IsAllysTarget` 一族**在今天的语料上永久不可定价**。这条比 GH #648/#652 更靠根,建议并入同一条语料请求。
- **本组下一轮**:⛔ **不要再去碰圈心那条杠杆** —— §1 两个宿主各有一条**已测得的**理由。
  下一个候选见章程 0NEXT39。

## 7. Token 用量

```
TOKENS total_in=14,557,895 out=94,060 turns=93
```
(统计时刻之前;此后的收尾回合不计入。本轮预算**前一半花在定价四条候选并全部淘汰**,
后一半花在普查 + 变异台 + 两处别人闸门的修复。)
