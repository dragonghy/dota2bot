# 协同组 2026-09-13T10:26Z — `roshpost`:Roshan 团的「我们有塔在挨打」把瞭望塔算了进去

**一句话**:`J.ShouldAbortRoshanAttempt` 的 leg (b) 用 `string.find( name, 'tower' )` 认塔,
它**匹配 `npc_dota_watch_tower`** ⇒ 一座被我方占领的瞭望塔周围 900 内有任何敌人,就读作
「我们有一座塔正被压」,**整次 Roshan 团被叫停**。而 leg (b) 自己的注释写的是
「**Roshan waits, towers don't**」—— 整条论证的力量在于**塔会掉**,而瞭望塔掉不了。
**1 行 `bots/` 代码,0 个新 gate id**,GH #782 名字测试那一族的**第四个也是最后一个**站点。

---

## 1. 本轮是什么、为什么是它

章程 backlog `0NEXT4` 写着「⚠️ 仍然 open 且本轮未认领的 `[strategy]` issue:#767 / #770 / #775
—— **下一轮先看它们**,再看本条」。三条都读了,**都没认领**,理由逐条写在 §2。
实际认领的是**上一轮自己登记的那个未收窄站点**:`tests/test_rescpost_outpost_narrow.lua` §2
的 allowed-open 集里**恰好一个**名字,`J.ShouldAbortRoshanAttempt`(宿主 `roshgate`)。

⇒ 主体是一个 `bots/` 行为改动(OWNER_PRIORITIES **4.4 (i)**,**连续第四轮满足**,不要断)。

## 2. 三条 open issue 各自为什么没被认领(读了,定价了,没买)

| issue | 读到的 | 为什么本轮不认领 |
|---|---|---|
| **#767** 防守 TP 的胜负判据问任意塔下敌人 | issue **自己**已经把两条出路定价完了,而且 §3.1 量出「照注释修成 nearest」是**零行 no-op**(四种读法里前三种计数相等 ⇒ 同一个集合) | 唯一动得了的读法**捆绑了另外两个候选**(`tparrive` 的 parity 帧、`midsupyield` 的 NODROP 帧),issue 自己写着「把跨杠杆代价买在改动之前」。那是**一整轮的定价工作**,不是一个小杠杆;⛔ 而且宿主 `midtp`/`suptp` 是**退集**的(GH #539 的 NaN 落点),NaN 修好前这个站点上任何杠杆都测不了 |
| **#770** 瞭望塔一族的域几乎为零 | `IsEnemyTier2Down` 在 18 个队伍局里 5 个从未成立 | 这是一条**给 `outlatch`/`outcommit` 的效应量上界**,结论是「先别在那一族投入」;它**不产生**一个 `bots/` 杠杆 |
| **#775** `arbheart` 与出厂守卫 :615 逐条等价 | 有效域被卡窄到 6 条旁路出口;§4 另有一处**注释错误**(gate 注释对 `:589` 说错了话) | 建议的验收方式 (1) 是**在旁路出口上钉 fixture** —— 要预置 `preferedCamp` 并强制 `GetDesireHelper` 从 `:523`/`:606` 返回正欲望,主体会落在 `tests/`;§4 的注释更正是**一行字但不是行为改动**,按 4.4 只能当附带。**登记为下一轮候选**(见 §8) |

⭐ 三条都不是「没看」,是**看了之后定价发现不合本轮的形状**(小、gated、`bots/` 主体、一个杠杆)。

## 3. 缺陷

`bots/FunLib/jmz_func.lua`,`J.ShouldAbortRoshanAttempt` leg (b):

```lua
--   (b) one of our towers is under visible pressure while the attempt is
--       still early (Roshan unseen or > 60%) -- Roshan waits, towers don't.
...
	if bEarly then
		for _, b in pairs( GetUnitList( UNIT_LIST_ALLIED_BUILDINGS ) or {} ) do
			if J.IsValidBuilding( b )
			and string.find( b:GetUnitName(), 'tower' ) ~= nil     -- 匹配 npc_dota_watch_tower
			and #J.GetEnemiesNearLoc( b:GetLocation(), 900 ) > 0 then
				return true                                         -- 整次 Roshan 团叫停
```

⭐ **判据是出货注释自己说出来的**:「Roshan waits, **towers don't**」—— 值得走开的理由是
**一座塔会在我们打大哥的时候掉**。瞭望塔**掉不了**:它没有血条可失,只能被 channel 重新
占领、从不被摧毁。那里没有任何东西在赛跑,也就没有要走开的理由。

标准打法同时支持**出货规则**与**本次收窄**,理由同一条:**塔是你会失去的建筑,瞭望塔是你只能借用的建筑**。

## 4. 落法:判据(甲′)的第三态,也是最干净的一态

宿主头两行:

```lua
	if not J.IsModeTurbo() then return false end
	if not J.IsSoakCandidate( 'roshgate' ) then return false end
```

⇒ **整体上闸于一个未 promote 候选**。按上一轮立的三态判据:

| 宿主状态 | 落法 | 本族实例 |
|---|---|---|
| **已 promote** | **必须**加新 id(没有 gate 可继承) | `divepost`(宿主 `punish`) |
| **未 promote 候选**(合取式里的一项) | **不要**加(pullcad 陷阱) | `tpdeftower`、`ohnum` |
| **整体上闸**(自己头一行早返回) | **必须不加**(再加就是候选嵌在未 armed 候选之下,**没有任何波次能隔离它**,而 `check_armed_wiring.py` 仍叫它 WIRED) | `rescpost`、**本轮 `roshpost`** |

⇒ **armed 串 / `iterations/queue.json` / `iterations/streams/test_set.md` 一字未动,
没有新 id 需要入集**(P4.2 冻结期内最干净的落法)。四个站点把三态**各走了一遍**。

## 5. 读数(142 帧 / **657** 活英雄行 / `load_fail 0` / `raised 0`)

普查:`pred_true 97` / `pred_false 1721`、**`PRED_DISAGREES 0`**;
leg-(b) 站点 `site 17` 帧、`site_has_wt 1`、**`site_wt_only 1`**(域,**非空**)。

四条臂(臂 A 强制谓词为假 = **收窄前的树逐字重建**,`and not false` 是恒等式):

| 臂 | 含义 | 开火 |
|---|---|---|
| A | armed,谓词强制 **FALSE** = 出厂树 | **75** |
| B | armed,**真谓词** = 落地后的树 | **71** |
| C | armed,谓词强制 **TRUE**(全部建筑读作瞭望塔) | **0** |
| D | **未 armed**,同臂 A 的强制世界 | **0** |

`flip_ab` **4** / `grow_ba` **0**(禁止方向未出现)/ `flip_ad` **75 == fire_a** /
`fire_a2` **657 == live** / `real_pred_drives` **657 == live**。

### ⭐⭐ 本轮最该被下一轮读到的三句

**(甲) `flip_ab 4` 是本族前三个站点都买不到的那个读数。**
tpdeftower / divepost / rescpost 的头条都是**零翻转**(构造安全但本语料无效应)。
这一次收窄**在真实帧上改变了答案**,承重帧钉了名字:
`tests/fixtures/f_260820_102030_wk_tower_out_of_reach.lua`,**DotaTime 436**,dire 侧 ——
一座被占领的 `watch_tower`,**jakiro 在 609.8u、zuus 在 450.2u**,是全图**唯一**一座
「正被压的塔」,于是该帧**四个活着的 dire 英雄全部**为一座掉不了的建筑叫停了 Roshan。
测试 §4 **按名字钉住这帧**:语料换了它会**红**,而不是悄悄变绿成一个空集。

**(乙) ⭐ 正对照要买两条,第二条是前三个站点没写的。**
第一条是老规矩(距离量具活着:`wt_allied 97`、最近敌人-瞭望塔 **450.2u < 900** ⇒ 小读数
**归因几何不归因死量具**)。第二条是 **`tower_with_enemy 17 > 0`** —— **真塔**在本语料上
确实会被压 ⇒ 这次收窄修的是一条**被污染**的腿,**不是清空一条从不开火的腿**。
「收窄后 leg (b) 全域为空」会是**另一个**发现,不该静悄悄通过。

**(丙) ⭐ 「我传了什么参数」必须写成会红的断言,不能写成一句说明。**
所有臂都传 `hRoshan = nil`。两半都是**读数**:(i) 语料里**一个 Roshan handle 都没有**
(`rosh_seen 0` / 142 帧,dumper 只出英雄与建筑)⇒ 唯一能驱动的形状;(ii) 它也是**出货形状**
——宿主注释自陈早期窗口是「Roshan **unseen** or > 60%」。后果是 leg (a) 在每一行上都够不着,
**这正是让臂间差异可归因于 leg (b) 单独一条腿的东西**;`fire_c 0` 是它的端到端确认。
⇒ §7 断言 `rosh_seen == 0`:**哪天语料里出现 Roshan,它会变红**,而不是继续默默通过。

## 6. 变异台:`tools/agent/mutstand_roshpost.sh`

```
M1 drop the narrowing conjunct          CAUGHT by the named case
M2 misname the outpost unit             CAUGHT by the named case
M3 invert the outpost predicate         CAUGHT by the named case
M4 predicate stops reading a literal    CAUGHT by the named case
M5 delete the host gate                 CAUGHT by the named case
M5b neuter the host gate in place       CAUGHT by the named case
M6 kill the corpus census               CAUGHT by the named case
M7 arm B collapses onto arm A           CAUGHT by the named case
M8 truncate the corpus to one fixture   CAUGHT by the named case
M9 admission terms out of order         CAUGHT by the named case
C0 comment-only edit (must survive)    SURVIVED (correct: the stand is not simply always red)

caught=10 survived=1
FINAL_SHA_OK=yes
STAND GREEN (10 mutants caught, 1 control survived)
```

关键两条:
- **M5b**(把宿主闸**就地**废掉:`if false and not J.IsSoakCandidate( 'roshgate' )`)——
  GATE_ID 仍解析得到、§1 的顺序断言仍真、普查/翻转/承重帧全绿,**只有臂 D 看得见**。
  这是让「一个整体上闸的宿主**就是**那道闸」这句话**可证伪**而不是装饰的那条腿。
  ⭐ **M5(直接删闸)保留在旁作为姊妹台实测过的对照**:它在 **LOAD 期**就被 GATE_ID 解析器
  抓住,**根本到不了臂 D** —— rescpost 台的第一稿曾声称「只有臂 D 看得见」,那是错的。
- **M8**(把语料截成一个 fixture)—— 本组 2026-09-13 规则要求的那条
  **「量具自己塌掉」**的变异体:被试**没有任何错**,只是每个 `== 0` 都变免费了。
  没有这条腿,分不出「文件灵敏」和「文件几乎没在量东西」。

## 7. ⭐ 被 push 前的闸**点名**抓到的一条(GH #774 环在作者座位上闭合)

`python3 tests/test_bots_walk_farm_only.py` **exit 1**:

```
FAIL: a tests/ io.popen command cannot be resolved statically and is not on the hand-read list
  ["tests/test_roshpost_outpost_narrow.lua  ::  'ls ' .. dir .. ' 2>/dev/null'"]
```

⇒ **同一个工作单元内**读了那条命令并登进 `UNRESOLVED_HAND_READ`(它 loop 的是两个字面量
`{'tests/fixtures','tests/frames'}`,非递归 `ls`,永远到不了 `bots/Customize/`),重跑
**8 checks, 0 failed**。**两个姊妹文件都是落地当天红在 trunk 上、由下一个开门的台子补登的**
(GH #774 / #624)——这次作者与登记者是**同一个座位**。

## 8. 交出去的棒

1. ⛔ **本族名字测试那一半到此收口,不要再在这里找杠杆。**
   `tests/test_rescpost_outpost_narrow.lua` §2 的 allowed-open 集**已清空**、断言改成
   `#open == 0`。⛔ **这不是「把红弄没」**:那条断言说的就是「恰好一个未收窄站点」,
   站点被收窄了,登记状态跟着动。**机制刻意保留** —— 空的 allowed 集 + `#open == 0`
   才是让**下一个**未收窄站点自己举手的东西;新文件 §2 反过来断言那份普查**还在**
   (两个字符串都在),所以**两个文件不能单独过期**。
2. ⛔ **族里仍有一个允许的未收窄「距离锚点」**(另一半形状):
   `tests/test_divepost_outpost_narrow.lua §1` 登记的 `J.ShouldPunishOverchase` leg (b)
   DEEP 建筑支路(`overchase`,端到端 `oc_fire_building 0`)。**已被定价并排除、不要重买**;
   哪天要动,**先改那条断言**。
3. **下一轮候选(本轮读了但没买)**:GH #775 §4 的注释更正 —— `bots/mode_farm_generic.lua:821`
   对它点名的守卫 `:589` **说错了话**(那条守卫只要 `J.IsFarming`,与 `arbheart` 同一个谓词)。
   ⚠️ 它是**一行注释、不是行为改动**,按 4.4 只能当附带一条,**不能当工作单元主体**。
4. ⚠️ **`roshgate` 的一条旧账**(不是本轮开的,只是撞见):`state.json:groupC_VERDICT` 写着
   「roshgate NO-SCENE(20 分钟 turbo 里没有 Roshan 团)」且 `keep_optimize` 写着「roshgate 
   parked(再说)」。**本轮不提入集、不申请波次**(P4.2 冻结期),只登记:这个宿主的条件 (a)
   历史上买不到,与本次收窄的对错无关。

## 9. 铁律 6 三行 + 自检

```
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)      # luacheck bots game: 0 warnings
py gate:  见 §9 的 push 记录
lua gate: 见 §9 的 push 记录
```

- 定向 Lua:`test_roshpost_outpost_narrow` **7/7(2.8s)**、`test_rescpost_outpost_narrow`
  **6/6(21.9s)**、`outpost` 全族 **31/31**、`test_replay_230124_roshgate` **5/5**、
  `test_gate_claim_consistency` **16/16**、`test_smoke_load` **3/3**。
  `tests/test_bots_walk_farm_only.py` **8 checks, 0 failed**(修复前 1 failed,见 §7)。
- ⚠️ 新文件**未登进** `tools/agent/lua_gate_manifest.json` ⇒ **今天不在快 Lua 闸里**,
  一条红**挡不住任何人的 push**(GH #624 形状,如实登记而不是抹平;唯一合法入口是全量
  re-measure,那会清空 `known_red` 赦免名单 = GH #783,不是一个小工作单元的事)。
- **开工自检 `TRUE_EXIT=3`**(先被脚本以 `REFUSED: stdout is a PIPE` 挡回**第 11 次**,
  重定向后重跑):findings = `cadence` / `queue-rulings` / `owed-executions` /
  **`trunk-red(python)`**。python 那条唯一的红是 **`tests/test_py_gate_hook.py`**
  = **GH #795**([harness],同日 09:36Z 已立案),与本轮无关;
  Lua 快检测器 **87 个文件 0 失败**;3 个 python 文件 **UNCERTIFIABLE(未跑,不是通过)**。
- ⛔ **排程**:变异台与开工自检**没有并发**(等自检跑完才开台),FINAL_SHA_OK=yes。

## 10. 成本与 token

**零 AWS、零波次、armed 串一字未动**(gated 且未 armed ⇒ 出货行为逐字节不变)。

```
TOKENS total_in=8,881,245 out=56,018 turns=58
```
(统计时刻之后的收尾回合不计入。)
