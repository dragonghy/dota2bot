# 协同组 2026-09-11T16:30Z — `lvlgroup`:接走 `lvlany` 接力棒的第二根,并**显式换掉一把杆**

**工作单元归属(P4.4)= (i) 一个 `bots/` 行为改动。**
工作流第 1 步先扫 open issue:`[strategy]` 未见新件;本组前三轮已认领的仍在原位。
⇒ 取 backlog `0PULLCHEW`「下一格」的项:第 (0) 项(GH #250 §4 的「数量」那一半)**今天仍然落不了地**
——它要一个带真实中立实体的 dumper 读数,那是**录像组/总监的棒,不是本组能自己变出来的**,
上一轮已把这一条钉进 `test_pullcamp_trigger_census.lua`。⇒ 本轮取第 (1) 项:
**`lvlany` 留下的第三根兄弟,`X.CarryFindTarget` 第二处 12 级站点**。
`lvlcarry` 的 `state.json:next` 逐字点名的也是它(「优先取 …… 群推支路」)。

---

## 0. 一句话

`X.CarryFindTarget` 的**群推支路**(和友军一起抢我方塔即将击杀的兵)把一个关于**等级**的
**存在量化**问句交给了**最近**的那个英雄回答。落地 gated id **`lvlgroup`**
(`X.NoNearbyEnemyAtLevelGroup`,turbo-only,**FROZEN-HOLD**)。
产出:`tests/test_lvlgroup_group_push_level_quantifier.lua` **20/20**、
`tools/agent/mutstand_lvlgroup.sh` **17 腿 17/17 STAND GREEN**、`state.json:lvlgroup_20260911`。
`armed` 串 / `queue.json` / `test_set.md` **一字未动**;零 AWS、零波次。

---

## 1. ⭐⭐ 本轮最该被下一轮读到的四句

### (甲) ⛔⛔ **本轮换了一把杆,而换杆比落地那个 id 重要 —— 所以三个文件在同一个 commit 里一起改。**

上一轮 `lvlcarry` 的 §7b 用**更严的一把杆**判剩下那根兄弟买不到:
「**整条谓词会不会翻转**」。本轮在**自己这个站点上量到了一模一样的零** ——
两个 miss 行**都栽在同一个 `#allies >= 2` 合取项上**,`X.CanAttackTogether(bot)` 在两行上都是 false,
⇒ **arm 改变支路结果的行数 = 0**。

按 §7b 那把杆,本站点**也**该判「绿而空、不要拿」。**本轮仍然把它拿了**,用的是另一把杆:
> **杠杆自己的谓词改动是否在真实行上被驱动**(这里:**2** 行),
> **支路可达性另账登记为更弱的界**(§5,**按等式钉**)。

⭐ **关键不在于哪把杆对,而在于同一族的两根兄弟不许用两把杆。**
那是「标准在没人决定改它的情况下自己烂掉」的形状。
⇒ 本 commit **同时**改了 `lvlcarry` §7b 与 `lvlany` §7 的注释,写明:
现行是哪把杆、这把杆是被什么现场推出来的、以及**在这把杆下剩下那根同样是买得到的**
(它自己那格 r=600/10 级有 **4** 个 miss 行)。
⛔ **本轮不拿它** —— 一次一个杠杆,那是 lanefix 教的(gpm −74.5,再 −88.7,0/4 comps)。

### (乙) ⭐ **零还立着的时候扛住这个杠杆的是「方向」,而方向是量出来的不是从 diff 上读的。**

armed 答 `not any(level >= nLevel)`,出货答 `[1] < nLevel`;`[1]` 存在时**前者蕴含后者**
⇒ armed 是**纯收紧**:它只能**撤回**这条支路需要的那个「安全」答案,**永远不会发一个 baseline 没发的**。
⇒ 支路 armed 只会开得**更少**,不会更多。
§3e 在**全部 1306 行**上量这条蕴含(`dir_violation == 0`,**等式**),
并双向防空转(`armed_true > 0` + `dir_narrows == site_miss`)。
这与 `glyphany` / `wkqdmg` 是同一条安全形状,**也正是「一个支路在语料上打不开的 gated 杠杆可以安心留在树上」的全部理由**。
⛔ 这是对**改动方向**的界,**不是命中率**;本文件全文无一句真实对局命中率主张。

### (丙) ⭐ **那个零不是结构性的 —— 这才是它值得钉、而不是值得认栽的原因,而且它直接写出了一条 fixture 规格。**

本语料里**13 行**同时满足「1200 内 ≥2 友军」且「650 内 ≥2 敌人」(miss 需要的形状),
**13 行里没有一行恰好是 miss**。⇒ 这是**小样本零**,不是不可能。
§5 因此把 `miss_cat == 0` 钉成**等式**并逐字写着:
> 「⭐ 它红了是**好消息**:支路终于能端到端驱动。**拿那个驱动替换这条断言,不许重新基线化。**」

⇒ **本轮唯一的外部依赖,写成一条精确请求**(见 §4):冻一帧
「某英雄 1200 内 ≥2 友军、650 内 ≥2 敌人,且**最近**那个敌人**低于 12 级**而另有一个 **≥12 级**」。
那一帧**同时**解锁 `lvlgroup` 的端到端驱动**和** `lvlcarry` §7b 的那根兄弟。

### (丁) ⭐ **本文件自己的发现:这条支路把同一个问题问了两遍,两格不同 —— 于是「修一格能修掉多少」是个可量的量。**

支路里 r=650/**12 级**(本杠杆),`X.CanAttackTogether` 里面另有一格 r=600/**10 级**
(**阈值更严、半径更小**)。⇒ 只修这一格,复合守卫在另一格上**仍然**是「拿最近的那个回答」。
§1 实测这代价:
- 两个 miss 行的 ≥12 级英雄**都在 600 以内**(600–650 **环带的故事在本语料上是空的**,`miss_high_in_annulus == 0`,按等式钉 —— 那会是本族的**新形状**);
- 内层守卫在**两行里的一行**上**本来就已经**读「不安全」(`miss_inner_already_blocks = 1 < site_miss = 2`,并断言这个严格小于 —— 若它等于 2,本杠杆就没从复合守卫里拿掉任何东西,**该重新论证 id 而不是重新基线化**)。

这就是剩下那根兄弟**继续挂在接力棒上、不被顺手打包**的理由。

---

## 2. 改了什么

| 文件 | 改动 |
|---|---|
| `bots/mode_team_roam_generic.lua` | 新 helper `X.NoNearbyEnemyAtLevelGroup`(闸 `J.IsModeTurbo() and J.IsSoakCandidate('lvlgroup')`)+ 群推支路调用点换成 `and X.NoNearbyEnemyAtLevelGroup(nNearbyEnemyHeroes, 12)`,**合取式里的位置不变 ⇒ 短路顺序不变** |
| `tests/test_lvlgroup_group_push_level_quantifier.lua` | 新增,**20/20** |
| `tools/agent/mutstand_lvlgroup.sh` | 新增,**17/17 STAND GREEN** |
| `tests/test_lvlcarry_carry_deny_level_quantifier.lua` | §7 表里 12 级那条**移出**(total 2→1);§7b 注释按 §1(甲)改杆 |
| `tests/test_lvlany_first_member_level_quantifier.lua` | §7 同上(12 级 key **整条移出**,total 2→1),**新增一条**:两个 12 级站点必须**因为挂了 id 而消失**,不是因为守卫被删 |
| `iterations/state.json` | `lvlgroup_20260911`(**纯新增**,19 行 +,1 行 −(尾括号)) |

⛔ **为什么是第三个 helper 而不是给 `lvlcarry` 那个加第二个调用者**:共用 = 两个站点挂同一个 id、
一起 arm(lanefix 捆绑手工重建);给共用 helper 挂两个 id = pullcad 陷阱。
而且 `lvlcarry` 的 §5b **把它的调用者数钉死在 1**,共用会**当场把那个文件顶红** —— 那是钉子在干活,不是障碍。

### 接力棒:三根 → **剩一根**,三个文件同时记账

`lvlgroup` §7(12 级形状必须 **0** 处未修、10 级形状必须**恰好 1** 处)、
`lvlcarry` §7(total 2→1)、`lvlany` §7(12 级 key 整条移出,total 2→1 + 「因 id 而消失」那条)。
⇒ 「还剩一根」不是一句没人重读的话(GH #13 那个形状当年掉了 37 轮),是**三处都会变红**的断言。
⭐ 这两处红**是我改动时被它们自己顶出来的**(`lvlany` §7 当场红,报错原文就是
「If one was repaired, move it out of this table — do not just lower the number」),
**按它说的做了:移出,不是降数**。

---

## 3. 变异台 17 腿,**第一轮 17/17**

M1–M14 沿用本族(闸泄漏三形状 / armed 腿两种假修 / pullcad / 捆绑到兄弟 id /
驱动格漂移 / 调用点不再提问 / 接力棒侵蚀 / 前提失序 / 语料换边 / 自我排除 / oracle 塌成常量)。

⭐⭐ **M15/M16/M17 是本杠杆自己的三腿,它们存在是因为 §5 是个零。**
零是本套件里**最容易在不动任何一条断言的情况下变得毫无意义**的东西:
- **M15** 把 `X.CanAttackTogether(bot)` 从支路里拿掉 ⇒ 零**失去了主语**,而支路**悄悄变得可达**。只有 §5c 的**顺序**检查看得见。
- **M16/M17** 让**测试里那份 `X.CanAttackTogether` 手抄复制品**的常数漂移(1200→1600、10→12)⇒ 零量的是**另一个谓词**,而**所有棘轮照绿**(它们全是下界,更宽的半径只会**添**行)。

⚠️ **诚实标注**:M16/M17 这个洞是**我在跑变异台之前就预判到并先把 §5b 补好的** ——
§5b 的钉用字符串改成**从本文件自己的常数拼出来**,而不是打成字面量。
所以变异台在这两腿上**确认了预判,不是发现了它**;第一版那个字面量列表**会同时放过这两腿**。
(这一条按 evidence-discipline 第 4 条写:结论对不等于理由对,来源也要说对。)

⛔ **一条替换故意不上台并写明理由**:把驱动里的裸 `h:GetNearbyHeroes` 换成 `J.GetNearbyHeroes` 会**存活**
——那不是洞,是 §4d 已声明的界(`jfilter_differs == 0`,本语料分不开两个 producer)。
哪天有 fixture 分得开,**§4d 先红**。

---

## 4. 交出去的棒(⚠️ 铁律 9 连带规则:同一工作单元内显式交出)

**给录像组 / 总监 —— 一条精确 fixture 请求(本轮唯一外部依赖)**:

> 冻一帧:某英雄 **1200 内 ≥2 友军**、**650 内 ≥2 敌人**,
> 且这些敌人里**最近的那个低于 12 级**、另有至少一个 **≥12 级**。

- 它**同时**解锁两件事:`lvlgroup` §5 的零转红(端到端驱动)与 `lvlcarry` §7b 那根兄弟。
- 本语料现状:候选形状 **13** 行,miss **2** 行,**两者不相交**。
- ⛔ MCP GitHub 工具在本容器**未验证可用**,按**铁律 11 不空转等待** ⇒ 本请求**写在报告里**,
  并已写进 `state.json:lvlgroup_20260911.next` 第 (2) 项;下轮若 MCP 可用则补开 `[strategy]` issue。

---

## 5. 验证读数(⛔ 全部**不经管道**读退出码,evidence-discipline 第 3 条)

**铁律 6 三条腿**:
```
GATE_EXIT=0          luacheck bots game: 0 warnings
py gate:  EXIT=0     96 ran, 0 findings, 0 uncertifiable, 31.5s
lua gate: EXIT=0     338 ran, 0 findings, 0 uncertifiable, 12 known-red, 315.7s
```
⚠️ lua gate 的 338 里**包含本轮新增的** `tests/test_lvlgroup_group_push_level_quantifier.lua`
(它在「17 new test(s) not in the manifest were run anyway」那一行里被点名,即**跑了但还没进 manifest**;
要登记跑 `python3 tools/agent/lua_gate_measure.py`,**本轮不改别人的 manifest**)。
`12 known-red` 与本组无关(lion / stayfield2 / wk 那三族的 amnesty 名单,闸自陈
「⛔ this list is not an exemption and it is meant to SHRINK」)。
- `lua5.1 tests/run_tests.lua lvl` → **52 tests, 0 failures**(`lvlany` + `lvlcarry` + `lvlgroup` 三文件)。
- `bash tools/agent/mutstand_lvlgroup.sh` → **STAND_EXIT=0,17/17 caught,STAND GREEN**。
- **未用 `RULE6_BYPASS`。**

⚠️ **开工自检:`SELFCHECK_EXIT=124`,而 124 是我自己的 `timeout` 砍的,不是自检的判决。**
它死在最后一段 `=== trunk health (python test suite) ===` 的**开头**
⇒ **那条腿这一轮没人看过,按铁律 10 记 UNCERTIFIABLE,不是通过**。
在它之前跑完并读到的各段都是 OK(unlanded / cadence+citations / 未裁定 queue /
domain watch / owed executions / condition-(a) / expired waits /
stable 锚点 7/7 / promote-atom 约束 OK / inverse gate census「no armed id hangs under an unarmed gate」)。
⚠️ 另记一条**第一条命令就踩到的**现场:`routine_selfcheck.sh` **拒绝了管道调用**
(`stdout is a PIPE ⇒ 退出码是 tail 的不是它的`),并自陈**「已复发 5 次,每次都是当轮第一条命令」**——
本轮是第 6 次,**已按它给的两种写法之一改**。

---

## 6. 没做什么(逐条,防止被读成掉棒)

1. ⛔ **未提入集** —— P4.2 冻结未解,唯一合法裁定是 FROZEN-HOLD。
   `tombhp`/`anyhero`/`lvlany`/`lvlcarry`/`bagtango`/`tpchew`/`pullreach`/`pullchew` 的裁定请求仍未答,**本轮不催**。
2. ⛔ **未拿最后一根兄弟**(`X.CanAttackTogether` 里 r=600/10 级)——一次一个杠杆;它在本轮确立的杆下**是买得到的**,下轮主体。
3. ⛔ **GH #250 §4 的「数量」那一半仍未落地** —— 要 dumper 读数,**不是本组能自己变出来的棒**。
4. ⛔ **读 `botTarget` 的 consider 条目族不动**(GH #474,**连续第十轮有效**)。
5. ⛔ **兵营分支(GH #713)不落 gate**,接力棒仍是 `tests/test_isvalid_building_sentinel.lua §2b`。
6. ⛔ **裸 `bot:GetNearbyHeroes` 不过 `J.IsValidHero`/meepo 过滤**是既有独立问题,**故意不碰**(§4d 已登记)。
7. ⛔ `pulldrag` 永远不许单独提;`pullcamp`+`pulldrag` 重新入集仍挂在
   `owed_executions.json:pullcamp_atom_readmission`,**`lvlgroup` 不代它提**。

---

## 7. Token 用量

```
TOKENS total_in=10,940,809 out=81,538 turns=71
```
(统计时刻为止;此后的收尾回合不计入。)
