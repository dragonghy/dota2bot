# 协同组 2026-08-23T05:30Z — 章程 backlog 第 8 条收官:`lf_rescue` 的最终动作**买得到**

## 0. 开工自检

`bash tools/agent/routine_selfcheck.sh` → **worst exit 0**。
UNLANDED 无(certifiable window 内)、cadence clean、trunk python 14 passed / 0 failed。
本地 `HEAD == origin/main == 0753a84`。容器无 `lua5.1`/`luacheck`,已 apt + luarocks 装好。

## 1. 认领依据

`OWNER_PRIORITIES.md` 的 P1/P2 都写「球在协同组」,但两项的本组交付都已落地
(P1 第 1 棒、P2 决策侧 `stayfield`/`stayfield2`),主判据在录像组/总监手上 ——
与上一轮 04:15Z 的判断一致,已在 §6 再次请主会话核对球权。
open issue 里 `[strategy]` 前缀的 #132 是本组上一轮自己开的(交棒中,不自领),
#120/#123/#119/#117/#110/#109/#77/#74 都已交出下一棒或在等别组读数。
⇒ 取章程 backlog **第 8 条**,它名下**只剩最后一个 id `lf_rescue`**(上一轮明写)。

## 2. 判决(正面,本组第一次)

**第 8 条的规矩**:凡在注释里论证「这个值要压过 X」的分支,验收必须断言**最终出价**
(过完所有下游变换),不是 helper 返回值。

`lf_rescue` 名下现存的每一个用例断言的都是 helper:
`test_replay_071423_sky_rescue.lua` 断言 `J.GetRescueTpTarget(bot) == luna`;
`test_lf_rescue_landing_reach.lua` 断言 `J.GetNearbyLocationToTp` 的距离。
**从没有人驱动过出厂入口。**

本轮驱动了。结论:

> **`lf_rescue` 的最终量在本地买得到 —— 而且这是本组从 item 层读出的第一个正面结果。**
> armed 时驱动出厂 `ItemUsageThink()`,**39 个救援帧里 37 个各产出恰好一条**
> `Action_UseAbilityOnLocation(item_tpscroll, <落点>)`,落点与 helper 的答案**逐字相等**
> —— consider 与 `Action_*` 之间**没有任何下游变换**。未 armed 时同样这些帧**零动作**。

两个例外**是枚举出来的、不是容忍掉的**:
(i) `vengeful_spirit` 那一帧 item 文件根本装不起来(GH #82 的命名分裂);
(ii) 一次**出厂守卫的正当拒绝** —— 见 §4。

### ⭐ 而且它**根本没有出价**

`ItemUsageThink`(aiug:8531)把 `ItemUsageComplement()` 当**裸语句**调用、丢掉返回值;
物品循环自己那句 `return nSlot + 1`(aiug:1031)死在同一个地方;
调用方对 desire 只问一句 `nItemDesire > 0`,**从不跨物品比大小**。

⇒ **对这一族,第 8 条的规矩只能对着「动作」兑现。** 这一条写下来是给后面的 id 用的:
在这里把「断言最终出价」按字面读,会去找一个**不存在的数**。

### ⭐ 对**第十六条世界断言**的一处订正

`tests/test_itemdesire_world_assertion.lua` 记的是:把 TP 卷轴弄诚实、重驱动全部 882 帧,
得到「整个语料上没有任何真实 item 决策,只有崩溃和一个原点幻影」。
**那个读数只对「出厂默认」成立** —— 那次普查**一个 soak candidate 都没 arm**,
于是 TP consider 顶上那两条 gated 分支在它眼里是死的。
**arm 上 `lf_rescue`,同一份语料上立刻出现 37 个真实决策**,
而且它们出现在**那些跑不动的表面之上**:救援分支在 **aiug:5117 就 return 了**,
在产出那 209 次崩溃的 `GetExtrapolatedLocation` / `GetFarmLaneDesire` 通用块**之前**。
**第十六条的缺口不在它的算术,在它的 arm。**

## 3. ⭐ 抢跑(第 8 条的顾虑 (i))从「假设」变成「测量」,而测量把自己大半否掉了

出厂循环扫 `{5,4,3,2,1,0,15,16}`,**首个 desire>0 即 return**,TP 卷轴排第七。
章程写的是「要数语料」。数了:

| arm | 结果 |
|---|---|
| 只把 TP 弄诚实 | 37/39 出 TP |
| **把物品栏里每一件都弄诚实** | **10/37 被更靠前的槽抢跑** |

但那 10 个里 **8 个是 harness 伪影**,并且已经作为**第十九条世界断言**钉住(§5):
抢跑者是 `item_power_treads`,而它整条分支选择压在 `GetPowerTreadsStat()` 上,
**loader 从不接线** —— 全语料 **270/270** 个鞋handle 读出 **0**,
而 0 **不等于** mock 发出的 `ATTRIBUTE_STRENGTH/AGILITY/INTELLECT` 中的任何一个。
剩下 2 个是**诚实读数**(`item_arcane_boots` 在 <58% 蓝、`item_clarity` 给缺蓝队友),
读的都是 fixture 真带的字段。

⇒ **本地答案是一个区间,不是一个率**:

```
诚实下界 2/37        上界 10/37
```

**机制毫无疑问**(它就是出厂那句 `return`),**只有大小买不到**,而这份语料量不出来。
凭一个静态形状去改共享的物品循环,正是 `lanefix` 的入口 —— **`bots/` 本轮零改动**。
真实对局里的发生率是**录像组的问题**,已随 issue 交出去。

## 4. 顺出来的两件事

**(a) GH #37 的 frame B 在出厂链上是哑的。**
#37 把 frame B 的落点(离尸体 1579u)直接从 `J.GetNearbyLocationToTp` 读出来。
端到端驱动的话,**Lina 根本走不到那里** —— 她站在**敌方**塔 888u 内,
TP consider 在 **aiug:5095** 就拒了,**在救援分支上面两个分支**。
所以 #37 给 frame B 的那个落点数,描述的是**出厂链在这张快照上不会发出的一次 TP**。
**诚实边界**:fixture 是决策瞬间的 1Hz 快照(GH #107/#121),
「在这张快照上」就是全部主张 —— 真实那一次施法可能落在前后一个采样。
**已交回 #37,本轮不动它。**

**(b) `J.GetRescueTpTarget` 是单发的,写下一条这种普查的人必须知道。**
它的最后一个合取是 `J.TryTakeTpResponseSlot()`(全队一窗口一个应答名额),
成功还会戳 `J.NoteRescueResponse`。**同一帧问两次,第二次答 nil** ——
不是帧变了,是第一次把票花掉了。
**对 `lf_rescue` 自己这是正确的、不是 `tpclaim` 那个缺陷**:配额是最后一个合取,
只有其余条件全过才花,而调用方**在同一帧**把这个答案变成动作 —— **问和去是一步**。
后果只落在写用例的人身上:**先用 helper 预筛、再在同一次 load 里驱动,量到的是一个空世界。**
本文件每一个计数在预筛与每条 arm 之间**都重新 load**。

## 5. 第十九条世界断言(新)

```
GetPowerTreadsStat() == 0   on 270/270 treads handles / 101 fixtures
0 matches NONE of ATTRIBUTE_STRENGTH / ATTRIBUTE_AGILITY / ATTRIBUTE_INTELLECT
```

⇒ `ConsiderItemDesire["item_power_treads"]` 的**每一条普通分支**都是拿这个 0
去和三个常量比出来的,在本语料上**分支选择由一个没接线的默认值决定**。

**没有断言的那一半,以及为什么不断言**:引擎自己的编号在本仓库里**不可知**。
`docs/BOT_API_REFERENCE.md:1931` 只列了名字、从不给值。
所以「引擎的 `ATTRIBUTE_STRENGTH` 会不会**恰好就是 0**」——
那会让这个没接线的默认值**悄悄地表示「力量腿」而不是「没有属性」**—— **是开着的**。
值得查清,因为两种读法把出厂分支送进**不同的臂**。已写进 issue。

**同族第二条,顺手钉住**:mock 的自动常量号**跨 load 不稳定** ——
`ATTRIBUTE_STRENGTH` 在 940 次 load 里取过 **两个**值(1051 在 760 次、1068 在 250 次),
按**首次触碰顺序**发号。**任何用例都不许硬编码它。**

## 6. 本轮的诚实边界(引用任何数字前先读)

- **这份语料看不见「`--roles` 原样」的救援帧**。GH #81:每个英雄都读 pos 3 /
  `IsCore() == true`,而收窄后的 helper 拒绝对线期核心。**同一次运行里两端都量了**:
  出厂 `J.IsCore` 下 **10** 帧,override 成 false 下 **39** 帧(所有同胞 lf_rescue 用例
  用的都是这个 override)。**两个都不是对局里的率**:10 是「全是核心」的世界,
  39 是「全是辅助」的世界,真值被**夹住了,不是被定位了**。
- `J.CanCastAbility` 在语料里对每一个物品 handle 恒 false(第十六条),
  所以驱动必须给 TP 卷轴补上 loader 从不接线的两条(IsTrained/IsActivated)——
  就是 `tests/_itemdesire_sweep.lua` 的 honest 那一遍做的事,**别的一律没碰**。
- **承重 fixture `f_071423_sky_rescue` 不带 `buildings` 块** ⇒ `GetTower` 11 次全 nil ⇒
  `J.GetNearbyLocationToTp` 走**无塔兜底**、答**泉水** —— 离天怒站的地方 540u。
  **那一帧钉的是管道,不是目的地**;目的地钉在 GH #37 那三帧上(它们带真实建筑)。

## 7. 交付物

`tests/test_lf_rescue_final_action.lua`(**12 例全绿,3 变异 3 抓**):

- `[final action]` 承重帧上 armed → 恰好一条 `Action_UseAbilityOnLocation` 带 TP 卷轴,
  目标与 `J.GetNearbyLocationToTp` 逐点相等;
- `[honest bound]` 同一帧断言 `fx.buildings == nil` + 11 座塔全 nil + 落点 == 泉水
  ⇒ **把「这一帧是自 TP」明确记成语料的话、不是 bot 的话**;
- `[final action]` GH #37 三帧(带建筑)上落点**不是泉水**,frame B 按 §4(a) 断言**哑**
  且断言它哑的**理由**仍成立;
- `[off-candidate equivalence]` 四帧未 armed → 零动作;
- `[no bid]` 源码断言三条:`ItemUsageComplement()` 是裸语句(不是 `return`、不是赋值)、
  循环 `return nSlot + 1`、选择判据是 `nItemDesire > 0` ——
  **任何一条变成排序,这一族就有出价了,第 8 条要重问**;
- `[census]` 全语料重算两半,**floors 不是等式**(GH #106 的教训:增长免费、丢失照抓),
  armed/none/err 三路**必须闭合到 hits**,例外**逐个点名**;
- `[bracket]` 断言 `hits_core < hits`,并在消息里写明:**若 GH #81 被修好、这条转红,
  区间就塌成一个真实率,§6 那段必须重写**;
- `[preemption]` 位置断言(TP 仍是倒数第二槽)+ 上下界 + 伪影/诚实两半的关系;
- `[nineteenth world assertion]` 两条(270/270 == 0 且无匹配;跨 load 至少两个 id);
- `[successor hygiene]` 归还 aiug 装的五个全局。

**扫描槽位顺序是从出厂源码 parse 出来的、不是抄进用例的**(M13 的教训:
普查restate 一个常量就是在量自己的副本)。变异 M1 把 `{...,15,16}` 换成 `{...,16,15}`
当场点红,证明这条钉子是活的。

**本文件在语料上跑 ~43s**(1 次预筛 load/subject + 只在命中帧上驱动 3 条 arm)。
第一版是 ~118s:把「出厂 IsCore」那一问也放在全部 940 个 subject 上了。
**修法用的是单调性** —— override 只会**放松** helper ⇒ 出厂世界的命中是 override 世界的
**子集** ⇒ 只在已命中的 39 帧上再问一次,同样的数、一半的墙钟。
(GH #124 在抱怨整套跑不完;新文件不该再往里加分钟。)

## 8. 门

- `luacheck bots game --formatter plain` → **exit 0 / 0 警告**;
- `lua5.1 tests/run_tests.lua test_lf_rescue_final_action` → **12 tests, 0 failures**;
- 变异 3/3:M1 槽序移动 → `[preemption]` 红;M2 救援分支返 `NONE` → 3 条红
  (承重帧 / 三帧 / census 的 37→0);M3 循环改返 desire → `[no bid]` 红。**每次都把树还原了**;
- 全量 `lua5.1 tests/run_tests.lua`:见本文件末尾「全量」一节。

## 9. 交棒

1. **录像组** —— 抢跑的**真实发生率**:一次 rescue TP 被更靠前的物品槽推迟多少个
   item-think tick(本地只买到 2/37..10/37 的区间,且 8/10 是伪影)。
   顺带:`item_power_treads` 分支在真实对局里到底走哪条臂。
2. **总监 / GH #37** —— frame B 的落点数(1579u)描述的是出厂链在那张快照上
   **不会发出**的一次 TP(敌方塔守卫拒在两个分支之上)。#37 的三帧验收口径要不要动,
   是 #37 的主人的判断,不是本组的。
2b. **GH #133**(新开,`[harness]`)—— 第十九条世界断言;
   录像组那一棒作为 #133 的**第一条评论**显式交出去了(真实抢跑率 / 真实 treads 臂 /
   两条诚实抢跑的占比),连同「拿到秒数之前不给这条链加 gate」的理由。
   **GH #37 的 frame B 那一棒作为 #37 的评论交出去了。**
3. **总监** —— **章程第 8 条到此全部做完**(`tpcommit`/`ownhalf`/`l1trade`/`tpwatch`/
   `midtp`+`suptp`/`teambrain`/`lf_rescue`)。这一条从 2026-08-19 立到今天,
   产出 4 个 gated id(`tpdying`/`roamstale`/`tparrive`/`tpclaim`)、
   一条出集建议(`tpwatch`)、两条负结果(`teambrain` 结构上买不到、本轮的 no-bid)。
   **本组下一轮从 backlog 上面重新取一条**。
4. **主会话** —— `OWNER_PRIORITIES.md` 的 P1/P2 球权仍写「协同组」,
   而两项的本组交付都已落地。**连续第二轮请求核对。**

## 10. Token 用量

```
TOKENS total_in=12,805,411 out=81,234 turns=86
```
(收尾统计时刻的数字;此后的 commit/push 回合不计入。)
