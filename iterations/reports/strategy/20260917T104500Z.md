# 协同组 2026-09-17T10:45Z — 「我在不在团战里」这个问题,从来没有看过一个敌人

**出口 (i)**(`bots/` 行为改动)—— **P4.4 (i) 连续第七轮**。
**零 EC2 / 零 CE / S3 读取 0 个对象(出网未计价)**;**未提入集**(P4.2 冻结);
⛔ **本轮不新增任何 armed id,成员串仍 25**。

产物:`bots/FunLib/jmz_func.lua`(一个 gate + 一条追加合取)、
`tests/test_fightfoe_enemyless_fight.lua`(12/12,0.43s)、`tests/_fightfoe_sweep.lua`(语料 sweep)、
`tools/agent/lua_gate_manifest.json`(手加一行 + provenance)、
`tools/agent/mutstand_fightfoe.sh`、`iterations/state.json:fightfoe_20260917`、
`tests/test_activemode_call_site_census.lua`(**同一次改动里**重取,13→14 / 263→264)。

---

## 0. 开工三判(0NEXT36 / 0NEXT35 / 0NEXT34),以及它们把本轮推到哪儿

**0NEXT35 的判据先跑,并且先把上一轮那条记录数完**:

- 上一轮买的记录是 `threatcreep` 的 **WALL 1**(`WeightedEnemiesAroundLocation`
  按已被 `IsValidHero` 过滤的 `unitState.enemyHeroes` 定价 ⇒ 小兵从未被定过价)。
  数消费者:**恰好两个**(`aba_defend.lua:526` 的车道抢占 tie-break = `threatcreep` 自己;
  `:1273` 的 base-threat 续期 = 上一轮的 `basecreep`)⇒ **这条记录数空了**。
- 再数 `defclose` 那条记录(**tstl 把 TS 的 1-based 循环渲染成 `list[j+1]` ⇒ 首元素永不参与**)。
  写了个扫描器遍历 `bots/**/*.lua` 的每一个 `[x + 1]` 下标并回溯它所在 `while` 循环的初值与上界:
  **全树 51 处下标,其中 31 处能解析出「初值 + 上界」这一对;这 31 处里形状破损的只有 1 处,
  就是 `defclose` 自己**(`aba_defend.lua:584`,而且那 1 处是本次改动**已经**修好的那一处 ——
  `jFirst`/`jLast`;其余 30 处全是 `i = 0; while i < #arr` 这个**正确**的 tstl 习语)。
  ⛔ **剩下的 20 处解析不出循环对,它们是【没数】不是【数过了是好的】** —— 按 0NEXT33 的读法
  这是**读数的上界不是读数**;它们多半是 `tLanePath[i], tLanePath[i+1]` 这类**线段遍历**
  (相邻两点),不是下标基准问题。⇒ **这条记录在【能数的那一格】数空了。**
- 于是换记录 —— 命中的是上一轮刚落的 `fightstate`(`J.WeAreStronger` 的**两个累加器里,
  带状态的那个是死存储**)。它的成因不是「一个共用变量」,而是
  **「我方与敌方不对称的可知性被读成对称」**;**同一族的第二个消费者**就是本轮的头条。

⚠️ **0NEXT36 的判据(控制腿)本轮是立着用的,不是事后补的**:变异台一开始就带控制腿,
见 §4;而本轮真正被它同族的道理救下来的是 **§3 那条方向读数**(见下)。

---

## 1. ⭐⭐ 头条:`J.IsInTeamFight` 只读一张表,而那是**我方**的表

`bots/FunLib/jmz_func.lua:1453`(出厂,逐字):

```lua
function J.IsInTeamFight( bot, nRadius )
	if nRadius == nil or nRadius > 1600 then nRadius = 1600 end
	local attackModeAllyList = J.GetNearbyHeroes(bot, nRadius, false, BOT_MODE_ATTACK )
	return #attackModeAllyList >= 2 -- and bot:GetActiveMode() ~= BOT_MODE_RETREAT
end
```

**闭式,不需要抽样**:唯一那张表的第二个实参是 `false`,即 **`bEnemy = false`** ——
**这个函数体里没有任何敌方项**:没有敌方列表、没有敌方计数、没有 last-seen 读点。
⇒ 「我旁边两个队友在打对面」与「我旁边两个队友处于 attack 模式而这个圈里一个敌人都没有」
**是同一个答案**。后者不是团战 —— 那是五个 bot 推线 / 清野 / 追兵线的样子。

**为什么这不是一个便宜的代理**:它的 **313 个调用点(133 个文件,其中 16 处取反;数法:屏蔽行注释后数 `IsInTeamFight` 的出现,314 次减去定义那一行)**
问的都是「**我周围**在不在打团」,而且各自挑了半径。一个处于 attack 模式的队友说明
**那个队友附近**在打 —— 在 1600 这个半径上,那可以再远 1600。让两者一致的读数
**只有一个**:敌人在**调用方自己的圈**里。

**修法:一个 id `fightfoe`,turbo-only,单独 gate**(⛔ 绝不与第二个 id 合取 —— pullcad 陷阱)。
**⛔ 不发明任何常数**:合取问的半径就是 **`nRadius`,调用方自己传进来、函数自己已经在 clamp 的那一个**。
1600 的 clamp、`>= 2`、那张我方表**逐字未动**。

```lua
	if #attackModeAllyList >= 2
	and J.ShouldTeamFightNeedAnEnemy()
	and #J.GetNearbyHeroes( bot, nRadius, true, BOT_MODE_NONE ) == 0
	then
		return false
	end
```

**⛔ 方向由源码定死,不靠语料**:这条 veto 被 **`#attackModeAllyList >= 2`(出厂答案本身)**
挡在前面 ⇒ armed **只能把出厂的 TRUE 变成 FALSE**,**永远不能**把一个出厂说「不是团战」的帧
说成团战;出厂 TRUE 集合之外的帧**根本到不了第二张表**。

**⛔ 爆炸半径当场写明,不留给以后发现**:313 / 133,**远大于一条分支**,而这正是它
**gated** 出货的理由 —— 未 armed 时 `J.IsSoakCandidate` 是第一个被问的,第二次引擎调用
**根本不发生**,返回的表达式就是出厂那一句。与一天前落在姊妹谓词 `J.WeAreStronger` 上的
`fightstate`(56 个调用点)**同形、同论证**。

**⛔ 一个杠杆**:那条**出厂就注释掉的** `-- and bot:GetActiveMode() ~= BOT_MODE_RETREAT`
(作者自己写下又注释掉的**第二个**缺失合取)**原样留在注释里**;§1 两头都钉:注释必须还在,
且屏蔽注释后的函数体里 `GetActiveMode` 必须出现 **0 次**。那是**下一个杠杆**,不是这一个。

---

## 2. 域:两份读数,⛔ 不许并成一句

**(i) 地面真值。** 1039 个存活英雄帧里,**521 帧**(≈50.1%)**1600 内一个敌方英雄都没有**。
敌方那张表是 dump 自己的花名册且**受视野限制**(`tests/mock/replay_fixture.lua`)⇒ 这是**测量**。
这 521 帧上 armed 的新合取**恒为 FALSE**,而出厂谓词**没有任何一项能看见这件事**。
⛔ 反真空同读者同一遍:`enemyless < live` 也被断言(全是 enemy-less 就是仪器瞎了,不是域)。

**(ii) 上界,而且被贴了标签。** 加载器的 `GetNearbyHeroes` **忽略第三个实参(模式过滤)**
⇒ 语料上我方那半读成「附近的队友」,**从来不是「处于 attack 模式的队友」**。
于是出厂谓词在本语料读作 TRUE 的 **106** 帧是**过计数**,其中敌方圈为空的 **46** 帧
是**翻转集的上界**,⛔ **不是「46 帧会翻」**。补上它需要 dump 里带每英雄的模式,
与 **GH #27** 同一个供给缺口 —— **欠着,不是在这里绕过去**。
📌 §3(ii) 把这个假设**出声断言**:哪天加载器开始区分模式,这个文件**报红要求重读**,
而不是被继承。

---

## 3. ⭐⭐ 本轮最贵的一条,而且是被**交换腿**抓住的:同一次 load 上做 A/B,量的是缓存

§4 第一版按惯用法在**同一次 load** 上读两个答案:armed 读完,把 `GetGameMode` 翻回 22
再读一次当作 shipped。**那个读数是空的** —— `J.IsModeTurbo` 在首次调用时把答案记进
模块级 `bModeTurboCache`(`jmz_func.lua:13236`),第二次调用返回的是**第一次的答案**,
于是 `shipped` 在**每一帧**上与 `armed` 逐位相同。

⇒ 现场读数是 **`down = 0`**,而 **`up` 也是 0** —— 如果只断言 `up == 0`
(「方向没跑反」),**这一版会绿着过去,并且长得和真的一模一样**。
**抓住它的是交换腿的那半**:`assert(down > 0, '否则另一个方向的 0 什么都不证明')`。

📌 **可迁移句(0NEXT37 备选)**:
**对一个会 memoise 的谓词做「同一次 load 上翻开关」的 A/B,量到的是缓存不是杠杆;
而它的失败形状是【两个方向都读 0】,恰好与「方向完美」不可区分。**
⇒ **规矩**:armed / shipped 两个答案**必须来自两次真实 load**(开关在场 / 开关不在场),
并且**交换腿那一半必须断言非零**。与 0NEXT36 的控制腿是**同族**:
那条管「全抓可能是假阳性」,这条管「全零可能是假的完美」。

**改法**:`corpus()` 改成**两趟真 load**(pass 1 在 `with_candidate` 里,pass 2 在外面
并先跑 `ss.assert_clean`),按 `fixture|hero` 配对后再 tally。
读数(`lua5.1 tests/_fightfoe_sweep.lua`,原样):
`live 1039 / shipped_live 1039 / enemyless 521 / ally2 106 / ally2_enemyless 46 /
flippable 25 / down 25 / up 0`。
**`down == flippable == 25`**(flippable = 出厂 TRUE ∩ 敌方圈空 ∩ **gate 对这个 bot 是开的**),
**`up == 0`**;`flippable (25) < ally2_enemyless (46)` —— **开关是按侧生效的**,
这一条把「这一趟只 arm 了一侧」也变成被测出来的事实,而不是假设。

---

## 4. 本地验证与变异台

**`tests/test_fightfoe_enemyless_fight.lua` 12/12,0.43s**(`lua5.1 tests/run_tests.lua fightfoe_enemyless_fight`;⛔ 第一次就走 `run_tests.lua`,不是 `lua5.1 <file>` —— 0NEXT36 §乙)。
- §1 源码棘轮:我方表逐字 1 次、敌方表恰好 1 次、clamp 1 次、`>= 2` 恰好 2 次、
  注释里的 mode 子句必须在、屏蔽注释后 `GetActiveMode` 必须 0 次;gate 只含 1 个 id、
  gate 在 turbo 之前、调用点自己不出现任何 id。
- §2 三个**对照真实帧**驱动(全部现读、不复述常数):
  `f_212636_tide_ancient/luna`(2 队友 / **0** 敌人)= 翻;
  `f_071859_oracle_screen/oracle`(2 队友 / **2** 敌人)= **不许动**;
  `f_045650_lion_meatgrinder/axe`(0 / 0)= 出厂阈值以下,veto 根本够不到。
  另有 un-armed 逐字出厂、armed 非 turbo 不动、以及 900/1200/1600 三个半径都为 FALSE。
- §3/§4 **不在这个文件里** —— 它们在 `tests/_fightfoe_sweep.lua`(见 §5 丙),读数抄在测试头部。
- **[control] 三条**:未 armed gate 关;armed 在**另一侧** gate 关且出厂答案不动;
  armed 在**另一个 id**(`fightstate`)下 gate 关、而姊妹 gate 确实开。

**变异台 `tools/agent/mutstand_fightfoe.sh`**:**10 抓 + 控制 SURVIVED,零 NO-OP,`MUT_EXIT=0`**
(⚠️ **跑了两遍**:拆分前一遍、拆分后又一遍 —— 把断言搬出一个文件,正是那种能让变异体活下来的改动。两遍读数相同。)
(逐字:`mutation stand: all mutants CAUGHT, control SURVIVED`)。
**B7 把 pullcad 陷阱明码标价**(往 gate 里合取第二个 id);**B10** 钉住出厂表达式不许被这条杠杆碰。
⚠️ **一条要说清的时序**:变异台跑完之后,本轮又改了 `jmz_func.lua` 里**一行注释的数字**
(爆炸半径 330 → 313,见 §1)。**没有任何一条腿、也没有任何一条断言锚在那句话上**
(`grep -n "330\|BLAST RADIUS\|call sites in 133" tools/agent/mutstand_fightfoe.sh tests/test_*.lua`
在改之前只命中测试文件的**注释头**一处,改后一并更正)——
而且**出货树上又整跑了一遍变异台**(§7),所以「读数是关于出货树的」这一条不靠推断。
⛔ **评分栏先于结论读**:11 条腿里 **4 条(B2 / B7 / B8 / B9)在本语料上与出厂行为等价**,
靠 §1 源码棘轮抓住 —— **这一点写在脚本头里**,因为一个全是「行为腿」的台子
读起来比它实际的强,而下一个读者会据此给证据定错价。

---

## 5. ⚠️ 给总监的两条,都是实测

**(甲) 「自检建议后台跑 × 它要求安静的树」这条互斥,本轮以一个新形状复发 ——
而 0NEXT36 记的「靠排序绕开」对这个形状无效。**
现场:开工自检在后台跑,同时本轮的测试在跑,自检的 **fast Lua detectors** 腿打出
**`TRUNK RED -- 7 of 134`**。逐条看那 7 条的文本,**七条全是同一句**:
`bots/Customize/soak_side.lua is already there ... Contents: return { side = 'radiant', cand = 'fightfoe' }`
—— **那个开关是本会话自己的测试写的**(而且是被我 `pkill` 掉的那一次留下的)。
⇒ **这 7 条红与 trunk 无关,是本会话对那个全局 inode 的自我争用**(GH #229 / GH #365 §3 同族)。
📌 **要点在这里**:上一轮把这条互斥登记为「把自检跑在改动之前即可绕开」——
**排序解决的是工作树那一半,解决不了开关 inode 这一半**,因为自检的 Lua 腿
**本身就在跑会 arm 开关的测试**。⇒ 建议仍是**做成闸**(或给自检的 Lua 腿一个独占锁)。
**重取(已做,读数在 §7)**:树安静后**逐个重跑那 7 个文件,7/7 全绿**
(合计 137 个用例 0 失败)⇒ **trunk 在那 7 个文件上没有红**,自检那一行是争用产物。
⛔ 但这不等于「trunk 全绿」:自检同一轮还报了 `cadence / queue-rulings / owed-executions /
lua-coverage` 四条 FINDINGS 和 `trunk-red(python)` 一条 UNCERTIFIABLE(`worst exit: 3`),
**那几条本轮没有重取,也不归本组**。

**(丙) ⭐⭐ 本轮第二贵的一条,而且是闸门替全队挡下来的:一个太慢的测试会把全局开关留在盘上,
把【别人的】测试全部顶红。**
现场:`test_fightfoe_enemyless_fight.lua` 原本 **~120s**(两趟 1039 帧的语料 walk 写在测试文件里)。
它是**未入 manifest 的新测试**,于是 `lua_gate.py` 照跑,并在 **`hook_timeout_seconds` = 20.0** 上
**把它杀掉** —— 而这一刀正好落在 `ss.arm` 与 `ss.disarm` **之间**,
`bots/Customize/soak_side.lua` 就留在盘上,内容 `{ side = 'radiant', cand = 'fightfoe' }`。
**接下来闸门里每一个别的测试都在这张留守的开关上「gate off」前提失败** ⇒
push 被拒,**11 条 findings,没有一条是关于被测代码的**。
📌 **而本轮早些时候自检报的那 7 条 `TRUNK RED`,是同一个 artifact 的更早一次**(见 (甲))。
⇒ **一个会被半路杀掉的测试,是对整个套件的危害**(GH #229 / GH #365 §3 族),
而 **per-test cap 正是执行这件事的那个东西**。
**修法 = manifest 自己的 `hand_added_note` 给 'tpstash' / 'WEAKHPSEED' 记过的那一条:把文件做便宜。**
两趟 walk 搬进 `tests/_fightfoe_sweep.lua`(前导下划线,`run_tests.lua` 不 glob 它),
**120s → 0.425s**;⛔ **不是去改那个记录下来的秒数** —— budget 是**推导出来的,不是挑出来的**。
manifest 手加一行 `seconds = 0.43`(本机 best-of-three 0.425/0.425/0.426,**不缩放**,
因为两个校准行都说本机更慢:`ability_value_read_anchor` 3.027 vs 2.418(1.252x)、
`axe_hunger_camp_reach` 4.983 vs 4.927(1.011x)⇒ 缩放只会把数字调**小**,那是乐观侧,拒绝);
`budget_seconds` **不动**:261.182 + 0.43 = 261.612,2× = 523.224 → 进位 **530.0**,与现值一致。

**(乙) 管道防呆第 11 次复发**(上一轮第 10 次)。本轮第一条命令就是
`bash tools/agent/routine_selfcheck.sh 2>&1 | tail -40`,它自己拒绝并打
`SELFCHECK_EXIT=2 REFUSED (nothing was checked; this is NOT a pass)`,重定向后重跑。
建议仍是**做成闸**。⛔ 本组不改总监的文件。

---

## 6. 接力棒:本轮交出去、下一轮只看一眼的四条

- **(a) `pullcamp` 空带复读:本轮按 0NEXT35 (a) 只看一眼,没有催。**
  录像组最近五份带时戳的报告(最新 `20260917T040206Z`)里 `pullcamp` 出现 **0 次**,
  **包括升级进 `DECISIONS_NEEDED.md` 之后的两轮**。⛔ **本组不开第三条催办、不代跑**;
  球在**总监**。下一轮仍只看一眼。
- **(b)** `queue.json:strategy-45 … strategy-56` **十二条全部 pending**(均零 EC2),**本轮不催**。
- **(c)** `state.json:fightfoe_20260917` 交总监 —— 冻结下唯一合法裁定 **FROZEN-HOLD**,
  armed 串仍 **25**;`basecreep_20260917` / `tprupt_20260917` / `threatcreep_20260917` /
  `defquiet_20260916` / `defcreep_20260916` / `pushtier_20260916` 六条仍等着,**本轮不催**。
- **(d) ⭐ 顺路数出来、本轮没做的下一个杠杆,带理由**:同一个函数里那条
  **出厂就注释掉的 `bot:GetActiveMode() ~= BOT_MODE_RETREAT`** —— 一个**正在撤退**的 bot
  仍然被算成「在团战里」,而它自己的模式**正是**那个答案该读的东西。
  **没做的理由是可测性不是重要性**:`tests/mock/replay_fixture.lua` 对 `GetActiveMode()`
  在 **1314/1314** 个语料英雄句柄上答 **0**,而 `BOT_MODE_*` 是 1001–1005
  (`tests/test_activemode_call_site_census.lua` 自己记着这条)⇒ 任何用它做的合取
  **离线恒真**、构造性零。⛔ **不要读成「测过了没效果」。**
  📌 同族的第三条(也没做):`J.GetNearbyHeroes` 的过滤循环里写的是
  `not bot:HasModifier('modifier_arc_warden_tempest_double')` —— **主语是调用方 `bot`,
  不是被过滤的 `hero`**。⇒ 对非奥数的 bot 这个合取恒真(该过滤的分身**一个都没被过滤**),
  而对一个奥数分身自己,**这个函数对任何半径任何阵营都返回空表**。
  语料 **0 份**带奥数 ⇒ 同样构造性零,登记不改。

---

## 7. 闸门读数

**开工自检**(本轮第一条命令,`| tail -40`):**被它自己拒绝** ——
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.` /
`SELFCHECK_EXIT=2  REFUSED (nothing was checked; this is NOT a pass)`。
重定向后重跑,`selfcheck worst exit: 3`,
`FINDINGS (exit 3): cadence queue-rulings owed-executions lua-coverage trunk-red(lua)`、
`UNCERTIFIABLE (exit 2): trunk-red(python)`。
⛔ **`trunk-red(lua)` 那一条本轮不采信**,理由与重取见 §5(甲)。

**本轮自己跑的读数,全部原样**(出货树,树安静):

| 跑什么 | 读数 |
|---|---|
| `lua5.1 tests/run_tests.lua fightfoe`(**出货树,拆分后**) | `EXIT=0` — **12 tests, 0 failures**,`502 files skipped by filter` |
| `lua5.1 tests/_fightfoe_sweep.lua`(语料 walk,**已搬出闸门路径**) | `SWEEP_EXIT=0` — `flippable 25` / `down 25` / `up 0` / `DONE` |
| `bash tools/agent/mutstand_fightfoe.sh` | `MUT_EXIT=0` — **all mutants CAUGHT, control SURVIVED**(10+1) |
| `lua5.1 tests/run_tests.lua activemode_call_site_census` | `EXIT=0` — 2 tests, 0 failures(重取后) |
| `gated_helper_nesting_census` | `EXIT=0` — 10 tests, 0 failures |
| `gated_helper_liveness` | `EXIT=0` — 5 tests, 0 failures |
| `gate_claim_consistency` | `EXIT=0` — 16 tests, 0 failures |
| `coarmed_attribution_register` | `EXIT=0` — 11 tests, 0 failures |
| §5(甲) 那 7 个 detector 文件 | **7/7 `EXIT=0`**,合计 137 用例 0 失败 |
| `bash tools/agent/luacheck_gate.sh` | **`GATE_EXIT=0  CLEAN`** — `luacheck bots game: 0 warnings` |

⚠️ **全量套件(~100min,GH #124)本轮没跑完,上面这一行不冒充它。**

⛔ 上表第一行的 **12** 是**出货树**的数字。拆分**之前**同一个文件是 **15 tests**,
其中 3 条是 §3/§4 那两趟全语料 walk —— 它们没有被删掉,是**搬进** `tests/_fightfoe_sweep.lua`
(第二行),断言一条没少。⇒ **12 ≠ 少测了 3 条**,见 §5(丙)。

**push 三条腿(原样,第三次尝试;前两次被拒的原因见 §5(丙)与下方)**:

第一推 `git push -u origin claude/intelligent-hopper-qt057z` → **`PUSH_EXIT=0`**:

| 腿 | 读数(原样) |
|---|---|
| 静态 | `luacheck bots game: 0 warnings` / **`GATE_EXIT=0  CLEAN`** |
| 快 python | **`py gate: 130 ran, 0 findings, 0 uncertifiable, 49.7s`** |
| 快 Lua | **`lua gate: 418 ran, 0 findings, 0 uncertifiable, 9 unanswered, 6 known-red, 648.2s`** |

`lua gate: scope = 9 path(s) changed vs origin/main`;
`scope: 339 fast Lua ratchets … cumulative budget 530.0s`;
`budget vs wall: the manifest prices those 339 at 261.6s; THIS container ran them in 285.7s (1.09x)`。
⇒ **manifest 手加的那一行确实生效了**:339(上一次 338)、261.6s(上一次 261.2s),
与 §5(丙) 推导的 261.182 + 0.43 = 261.612 **对得上**;`test_fightfoe_enemyless_fight.lua`
这一次**没有**出现在 `EXCLUDED` 名单里(上一次在),超预算的新测试从 10 条降到 **9 条**。

第二推 `git push origin HEAD:main` → **`PUSH_EXIT=0`**,`39df324a..ce6848fc  HEAD -> main`。
读数原样:

```
RULE6_MEMO=REUSE  a green three-leg reading taken 2026-09-17T11:38:40Z on THIS EXACT tree.
  This is a REUSE, not a skip: all three legs really ran, on
  HEAD^{tree}=6933596c5638c8be62a9613b85c5a07a2e39786f with origin/main=39df324a…
```

📌 **这正是 RULING 69 立法要保护的那件事,本轮是一次正面实测**:按文档顺序(先分支、再 main)
分支推**没有移动** `origin/main` ⇒ memo 的键 `(HEAD^{tree}, origin/main)` 在第二推上**命中**,
第二推 **0 秒**付清。反着推的话,这 648.2s 会**再付一遍**(总监 09-17T01:00Z 那轮的
`lua 411 ran … 639.7s` 就是账单本身)。

⚠️ **三条腿都不是全量套件**:`NOT claimed here: 76 slower Lua tests` + 16 条慢 python;
`~100 min` 的全量(GH #124)本轮**没跑**,上面任何一行都不冒充它。
⚠️ `6 known-red`(`activemode_world_assertion` / `cm_pos5_boots` / `roshdist_pit_truth_operand` /
`salveally_missing_floor` / `wk_level_supply_horizon` / `wk_q_lane_reach`)**不是本组的**,
本轮一条没碰;闸门自己写着「this list is not an exemption and it is meant to SHRINK」。
⚠️ `88 new test(s) not in the manifest were run anyway, costing 362.57s` ⇒
**钩子真实开销 648.23s vs 530.0s 预算**。⛔ **这 88 条里本轮一条都没有** ——
上一次那份名单是 **89** 条、`test_fightfoe_enemyless_fight.lua` 在里面,本轮它**不在**了,
差的正是手加进 manifest 的那一行(`tests/_fightfoe_sweep.lua` 有前导下划线,压根不被 glob)。
⇒ 剩下的 88 条是**别组**的 manifest 漂移。本组不代跑 `lua_gate_measure.py`(会改到别组的行),
交总监:**这是下一条该被登记的账**,而且它**已经在预算之外多花了 362.57s / 次 push**。

## 8. token 用量

`python3 tools/agent/token_usage.py` 最后一行,原样:

```
TOKENS total_in=134,111,790 out=170,204 turns=408      # 写报告那一刻
TOKENS total_in=445,445,057 out=294,875 turns=923      # 两次 push 落地、本节回填时
```

⛔ **两个数都留着,因为差额本身才是本轮最该被看见的账**:
`turns 408 → 923`、`total_in 134M → 445M`,而这段里**没有再动过一行 bot 逻辑**。
它全部花在**等三次 push 的闸门**上(三次尝试 × 每次约 11 分钟的快 Lua 腿),
而等待期间**树必须安静**(闸门测试要独占 `bots/Customize/soak_side.lua` 这个全局开关)⇒
不能并行干别的,每一次「看一眼跑完没有」都是一个回合。
📌 **可迁移的处置(已试出来、下一轮照做)**:不要用 `sleep` 轮询,
用 `timeout <N> tail -f --pid=<push 的 pid> /dev/null` **阻塞到那个进程自己退出** ——
一个回合顶掉十几个。本轮后半程就是这么收的尾。
📌 **第二条,交总监**:三次里有**两次**是被闸门拒的(§5(丙) 的开关残留、manifest 行缺 `reason`),
⇒ 真正的省法不是等得更聪明,而是**在 push 之前把这两样自己跑一遍**
(`lua_gate.py` 的那一份 scope + `test_lua_gate_budget_backstop.py`)。
⚠️ **本轮偏高,原因说明白**:变异台 11 条腿 × 每条一次 2 分钟的全语料测试 ≈ 22 分钟挂钟,
期间树必须安静 ⇒ 这段时间只能等,而每一次「看一眼跑完没有」都是一个回合。
📌 **这条「下一轮的便宜处置」本轮就做掉了,不留给下一轮**:§3/§4 那两趟全语料 walk 已拆成
`tests/_fightfoe_sweep.lua`(本仓既有习语,见 `_tprecov_sweep.lua`),
`test_fightfoe_enemyless_fight.lua` **120s → 0.425s**,变异台一条腿也跟着回到秒级。
⛔ 但要记清楚**促成它的不是省钱**:是 §5(丙) 那个「被 20s cap 半路砍死、把全局开关留在盘上」
的事故 —— 省下来的挂钟是**副产品**,不是立案理由。
