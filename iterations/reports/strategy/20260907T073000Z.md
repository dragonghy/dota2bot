# strategy 2026-09-07T07:30Z —— **一个 25 金、51 个购买表都有、整个对线期反复补货的消耗品,两条分支都是击杀确认**

**一句话**:`X.ConsiderItemDesire['item_blood_grenade']` 是**发货的、未 gated 的**,而它的
**两条分支都要求「这一下就把人打死」** —— loop 1 要手雷自己的 125 够杀(275 那档坐在一个
**15 度**的朝向锥里),loop 2 要**队友自己的伤害已经够杀**。⇒ 一个对线期消耗品只会被当处决用。
本轮追加**第三条 gated 分支**(`grenharass`,turbo 显式,**未 armed**),条件写在**道具自己的尺子**上:
`enemyHero:GetHealth() <= totalDmg * 3`(手雷能削掉对方剩余血量的至少三分之一)。
域 **8**/40,**两把独立量具**(合取项前缀行走 与 驱动引擎动作)**同时读到 8**。

**零 AWS、零 S3、零 EC2、零波次。armed 串一字未动、`queue.json` 一字未动**(P4.2 入集冻结)。

---

## 0. 认领与开工自检

工作流第 1 步扫 `[strategy]` open issue —— `#582`/`#578`/`#575`/`#572`/`#568`
(本组前五轮已交付、等总监裁)与 `#558`(已认领并交回),更早的
`#385/#300/#254/#201/#198/#26` 同为存量或无帧证据 ⇒ **无未认领的带帧证据条目**,
按铁律 9 取 owner 优先项 **P4.4(i)**(工作单元主体 = 一个 `bots/` 行为改动)。
章程 `0CORPUSPIN` 的「下一格」明写:**甲路**(`J.IsInTeamFight` 单调用点合取项)
需要录像组的团战帧 —— 本轮扫 `iterations/reports/replay-check/` 最新四份报告,
**团战帧未到位**(最新一轮 06:30Z 是补自家 trunk RED 与重扫账)⇒ 走**乙路**。

**开工自检 `RC_EXIT=3`**(`bash tools/agent/rc.sh bash tools/agent/routine_selfcheck.sh`):
- FINDINGS(exit 3):`cadence` / `queue-rulings` / `owed-executions`;
- UNCERTIFIABLE(exit 2):`trunk-red(python)` —— **没跑完,不是红**;
- Lua trunk:**84 个 tagged detector,0 failures**。

⚠️ **第 8 次**当轮第一条命令撞上它对**管道**的拒绝(`SELFCHECK_EXIT=2 REFUSED` 与外层
读到的 `tail` 的 **0** 并排印在同一屏)。本轮改走 `rc.sh` 后拿到真实退出码。

---

## 1. ⭐ 先纠一句本组自己的记法:那条缝的根因**不是**「没有 consider 函数」

章程把这条缝记作「**100 携带者 / 0 次投掷**」。本轮开工第一遍
`grep 'ConsiderItemDesire\["'` **读回了「这个道具根本没有 entry」** —— **那是错的**。
这个 entry 用的是**单引号**:`X.ConsiderItemDesire['item_blood_grenade']`。

**数出来的**:该文件里双引号的键 **168** 个,单引号的 **8** 个
(`item_blood_grenade`、`item_disperser`、`item_dust`、`item_harpoon`、`item_pavise`、
`item_pirate_hat`、`item_smoke_of_deceit`、`item_soul_ring`)。
⇒ **任何按 `ConsiderItemDesire\["` 做的普查,系统性漏掉的不是一个道具,是 8 个道具的整个决策层。**
(已作为「丁」交给英雄组登记。)

**而真正的根因更值得修**:entry 在,只是**两条分支都是击杀确认**。

---

## 2. 主判据:两条分支,两个都要求「这一下就把人打死」

| | 已发货分支的开火条件 | 本语料上的命中 |
|---|---|---|
| loop 1 | `J.CanKillTarget(enemyHero, totalDmg, MAGICAL)` —— 手雷自己的 **125**(50 撞击 + 15/s×5s)必须够杀。有一条 **275** 的延伸,但坐在 `IsInRange(bot,e,GetAttackRange())` **且** `bot:IsFacingLocation(enemyHero:GetLocation(), 15)` 这个**15 度**锥形后面 | **0** / 40 |
| loop 2 | `J.IsGoingOnSomeone(bot)` **且**队友正在追同一个目标 **且** `J.GetTotalEstimatedDamageToTarget(nInRangeAlly, enemyHero) >= enemyHero:GetHealth()` —— **队友自己的伤害已经够杀,手雷是尸体上的装饰** | **0**(驱动读数 `cast_ship == 0`) |

**条件 (c)(检索佐证 + 仓库内计数)**:血之荼蘼 = 施法距离 900 / 半径 300 /
撞击 50 + 15 每秒 × 5 秒(共 125)/ 15% 减速 / 地面指向 / 最大囤 2
(Liquipedia、dotacoach 7.41e;dotacoach 逐字:「carried everytime in the laning stage by
the supports that is usual rebought all througout the laning phase」)。
仓库内旁证**数出来的**:`bots/BotLib/` 下 **51** 个英雄文件的购买表里有它。
⚠️ 这些常数**不是本轮抄进来的** —— 它们是这个 entry **自己声明的 local**
(`nCastRange`/`nRadius`/`nImpactDamage`/`nDPS`/`nDuration`/`nHealthCost`),测试逐个钉住。

---

## 3. 域价钱(漏斗,每一层都是数出来的)

```
1012 活体帧 / 109 fixture
 238  携带手雷(任意格)
 100  携带在主槽 0-5                       <- 章程记的「100 携带者」
  40  且 900 施法距离内有敌方英雄
  40  且全部通过 valid / 非魔免 / 非幻象    (cand == loop:过滤器一帧没删)
   0  且 CanKillTarget(e, 125, MAGICAL)     <- 已发货 loop 1 的门
   3  (若换成 275)                          <- loop 1 只能经由 15 度锥形够到
  40  且 nHealth > nHealthCost * 2          <- 自保下限,本语料上不约束
   8  且 GetHealth() <= totalDmg * 3        <- 本杠杆的域
```

八帧(目标当前血量 225–363):

```
f_163714_zuus_commit_pin            shadow_shaman   361
f_222428_lion_lich_burst            lich            354
f_260819_122930_lich_rescue_doomed  venomancer      225
f_260819_123546_axe_rescue_ok       crystal_maiden  246
f_260819_123546_jakiro_landed_ok    venomancer      326
f_260819_181742_ss_chase_stalled    shadow_shaman   333
f_260819_182323_lion_drain_calm     earthshaker     280
f_260819_182855_lion_drain_midchannel lich          363
```

---

## 4. ⭐ 两把独立的量具落在同一个数上,而测试断言的是**等式**

- **前缀行走**(在帧上逐条求值该 entry 的合取项):`domain` **8**;
- **驱动**(每帧跑两遍已发货的 `_G.ItemUsageThink`,读回**记录到的引擎动作**):
  `cast_ship` **0** / `cast_armed` **8**。

测试断言 `domain == cast_armed`,所以一把量具悄悄替另一把说话是会红的(变异台 **M8**)。

驱动那一列需要给**手雷把手**补 `IsTrained`/`IsActivated`/`IsFullyCastable`
—— `J.CanCastAbility` 对每一个 fixture 道具把手都短路在 `not IsTrained()`
(**第十六条世界断言**,`tests/test_itemdesire_world_assertion.lua`),
补法与 `urnself` 那轮逐字相同,**且只补手雷这一个把手**。
**M13** 打的就是这个:把 `IsTrained` 拿掉,`cast_armed` 读回 **0** ——
**和「这个杠杆什么都没做」印出来一模一样**。

---

## 5. ⭐⭐ 本轮最强的变异体在本语料上不动任何一个数字(M7)

方向在这里是**构造**不是论证:块被追加在两条已发货分支的 `return` **之后**
⇒ arming 只能把 `BOT_ACTION_DESIRE_NONE` 变成一次投掷,**永远不可能改道、延迟或
压过已发货代码本来就要做的那次投掷**,也压不掉任何别的道具(`~:1084` 的分发循环
只有在没有更早的槽位出价时才走到这个 entry)。

**M7 把这个块原样搬到两条分支上面**:语法没问题,**本语料上没有任何一个计数会动**
(已发货分支在这里一次都不开火),`domain`/`cast_armed`/`flips` 逐位不变。
抓住它的**只有结构断言**(gate 必须落在最后一条已发货 `return BOT_ACTION_DESIRE_HIGH`
与最终 `return BOT_ACTION_DESIRE_NONE` 之间)。

⇒ **可复用的一条(已交总监做规程建议):当一个 gated 块的方向来自它在函数里的
「位置」而不是来自谓词,守住方向的断言必须是位置断言;任何行为计数对这类变异都是瞎的。**

方向零的补集照旧办(GH #171 形状):`flip_true_to_false == 0`,而**同一个 `tally()`
对调双腿再调一次** ⇒ `flip_true_to_false_swapped == flips == 8`、`flips_swapped == 0`
(**M6 / M6b** 分别删一次调用,两发都 CAUGHT)。
另有 `driven + undriven == loop`(40 帧里 **2** 帧驱动不起来)——
**一次死掉的驱动不许悄悄缩小分母**。

---

## 6. ⭐ 变异台第一版 **M12 幸存**,而缺陷在**断言**不在变异体

`M12`(把 **sweep** 的 `strip_comments` 换成恒等式)第一版 **SURVIVED**。
按证据纪律第 2 条先查断言:sweep 自己那半**结构解析算了,但没有任何断言读它**
—— 测试用**自己的 parser** 把同样的事实又读了一遍。⇒ **那半是死仪表**。

修法**不是删掉它**,而是新增 section 4「**两个 parser 对表**」:
sweep 走的施法距离/伤害常数、gate 位置、id 计数、已发货两条分支的击杀确认计数,
逐条与测试自己的读数断言相等。**理由是算术不是整洁**:§3 的 40 与 §4 的 8
是**用 sweep 解析出来的数**量的,两个 parser 不对表,就排除不掉
「sweep 量的是另一个谓词」。补上后 **M12 CAUGHT**。

**变异台最终:15 发 / 15 如声明 / `STAND GREEN`**(M1–M13 全 CAUGHT,C1 控制组 SURVIVED)。

---

## 7. 诚实边界(写在前面,不埋)

1. mock **不施加魔抗**(`GetActualIncomingDamage` 返回原始伤害)⇒ 每条 `CanKillTarget`
   读数都是**上界**。这对「已发货击杀确认命中 **0**」是**安全**方向,对任何
   「这一投能杀」的声称是**不安全**方向 —— **本杠杆不声称击杀**,测试里没有这样的断言。
2. `nHealth > nHealthCost * 2` 在 **40/40** 全通过:**本语料上它不约束**。
   保留它是因为它是**已发货 entry 自己的规矩**,不是因为量到它有用。
   **M5** 删掉它时**没有任何域数字会动**,抓住它的是「抄写条款计数 == 3」那条。
3. 本语料为 owner 优先项 **P2 的回城 TP 调查**而切,**不是**为对线期而切 ——
   **40 是这份语料的几何,不是真实对局里的频率**。

---

## 8. 同轮结清一条 trunk 红,而它是 **GH #574 的第三次实例**

本轮 `bots/ability_item_usage_generic.lua` 加了 82 行,`tests/test_chain_member_census.py`
的**新鲜度检查**因此报红:一条 judged 行的**行号**从 `8341` 漂到 `8423`
(`bots/ability_item_usage_generic.lua`,`notallyHero:HasModifier("modifier_juggernaut_healing_ward_heal")`)。

**工具本身是绿的** —— GH #442 之后这一行是**按链文本做键**的,键把三次漂移全吸收了,
工具只打 `LINE NOTE`。**收费的只有那条新鲜度断言。**
本轮**付了这笔编辑费**(`tools/agent/chain_member_census.py:194`,`8341 → 8423`),
并把「这是第三次」写进它自己的注释旁边 ——
⇒ **GH #574 的答案现在是一个测量不是一个意见:同一个文件三次插入、三次改这个字面量、
每一次都由一个跟这条 finding 毫无关系的轮次买单。**(已在 #574 追评。)

---

## 8b. 同轮结清**第二条** trunk 红,而它是本文档案里点过名的那个形状:**有人写了一句话**

Lua 全量套件跑到 `tests/test_incoming_damage_callsite_census.lua` 报红:

```
the published 46 is a grep line count; it now reads 47 -- update the mock header,
this file and state.json together. The two load-bearing counts are pinned ABOVE
this line: if they are green and only this one is red, the move is PURE PROSE and
nothing in the census below moves. (code_lines=40, calls=41)
```

**红的原因是本轮的注释**:`grenharass` 块的诚实边界那一条写了
「mock 不施加魔抗(`GetActualIncomingDamage` 返回原始伤害)」—— 一句**散文**,
里面出现了那个标识符。`tests/mock/bot_api.lua` 的表头自己早就把这件事写下来了:
grep 计数 **42 → 43 → 44 → 46**,「**因为有人写了一句话,四次**」;
本轮是**第五次**,`46 → 47`。

⭐ **而断言的排序当场把话说完了,没人需要重推**:两条承重的钉子
(`code_lines == 40`、`calls == 41`)**都是绿的**,只有 grep 那一行红
⇒ 「**这次是纯散文,底下的普查一行没动**」是**由哪条断言开火说出来的**,
不是读者自己判断的。这正是那份文件把断言排成那个顺序的理由。

照它自己写的规程办:**三处一起更新** ——
`tests/mock/bot_api.lua` 表头(六/46 → 七/47,并记下第五次是谁)、
`tests/test_incoming_damage_callsite_census.lua:257`(`published = 47` + 变更注释)、
`iterations/state.json:mockdmg_ZERO_20260829.census_20260829_follow_up`(第四次更新记录)。
重跑 **12 tests, 0 failures**。

⇒ **与 §8 并排读出一句**:本轮两条 trunk 红**都不是行为红**,一条是**行号**、
一条是**散文**,而两条都由**本轮的插入**触发、都由**本轮**买单。
两条的共同形状是:**一个数字被登记成了「今天的树长什么样」,而不是「这条 finding 是什么」。**
区别在于 §8 那条**已经有键**在承担后者(所以工具是绿的),
而这一条的两条承重钉子也在承担后者(所以只有 grep 那行红)——
**两套设计都是对的,收费的都是那条「今天」。**

---

## 9. 铁律 6 的两道门

- **静态半**:`bash tools/agent/rc.sh bash tools/agent/luacheck_gate.sh` ⇒
  `installed luacheck (it was missing; apt package lua-check, bounded)` /
  `luacheck bots game: 0 warnings` / **`GATE_EXIT=0 CLEAN`**。**未用 `RULE6_BYPASS`。**
- **动态半**:
  - 本杠杆自己的文件:`lua5.1 tests/run_tests.lua grenharass` ⇒ **12 tests, 0 failures**;
  - python 套件:`bash tests/run_py_tests.sh` ⇒ 修复前 `109 passed, 1 failed`
    (即 §8 那条),修复后 **`110 passed, 0 failed, 1 uncertifiable`**
    (`tests/test_selfcheck_lua_leg.py`,与开工自检报的同一条,**不归本组**,只登记);
  - `census` 过滤器全跑:**125 tests, 0 failures**;`gate_claim` **16/16**、
    `itemdesire` **26/26**、`gated_helper` **10/10**、`incoming_damage` **12/12**;
  - Lua 全量套件:**本轮没跑完**(GH #124)⇒ **不声称全树绿**。
    ⚠️ 但它在被打断前**抓到了 §8b 那条红**,而那条红只有全量套件会碰到 ——
    登记为一条实测:**「全量套件跑不完」不等于「跑它没有价值」。**

---

## 10. 产出

- `bots/ability_item_usage_generic.lua`(`grenharass` 块,+82 行)
- `tests/test_grenharass_domain.lua`(**12/12**)
- `tests/_grenharass_sweep.lua`
- `tools/agent/mutstand_grenharass.sh`(**15/15 `STAND GREEN`**)
- `tools/agent/chain_member_census.py`(§8 的编辑费 + 「第三次」的注释)
- `tests/mock/bot_api.lua` + `tests/test_incoming_damage_callsite_census.lua`
  + `iterations/state.json:mockdmg_ZERO_20260829`(§8b 的三处一起更新)
- `iterations/state.json:grenharass_20260907`
- `iterations/streams/test_set.md` **§FS**
- 本报告

## 11. 交棒(总线 **GH #590**)

- **甲 → 总监**:`grenharass` 登记在案,**不提入集**(FROZEN-HOLD)。
  规程建议一条:**§5 的位置断言规则**。
- **乙 → 批测台**:解冻后 `grenharass` **单臂可读**(entry 内只有这一个 id)。
  ⚠️ 取证不能只靠这份 fixture 语料 —— 40 帧是 P2 语料的几何。
- **丙 → 录像组**:核验形状 = 真实 Turbo 对线期里一个**主槽带血之荼蘼**的辅助,
  **900 内有敌方英雄且该英雄当前血量 ≤ 375** 的时刻:baseline 腿不投、armed 腿投。
  ⚠️ **按「能不能杀」筛会一帧都筛不到** —— 已发货的门就是击杀确认,本语料命中 0。
- **丁 → 英雄组(登记,不催)**:§1 的单引号/双引号普查缺口(8 个道具)。

## 12. Token 用量

```
TOKENS total_in=22,191,424 out=98,469 turns=123
```
(`python3 tools/agent/token_usage.py`,统计时刻 = 收尾前;此后的收尾回合不计入。)
