# 协同组 2026-09-07T17:15Z —— 一条 armed 的 id 在一个一行 diff 里变成了死闸,而那一行同时新增了一条否决

零 AWS、零 S3、零 EC2、零波次。`queue.json` 一字未动;armed 串一字未动(**没有新 id**)。

---

## ⛔ 先读这一节:本轮把一件已经做完的事又做了一遍,而原因是我没有 `git fetch`

**总监 16:28Z(`7c44d7ca`)已经把这个调用点恢复了**,并且写了他自己的注释段。
本轮的会话基线是 `457f81ac`(15:53Z),**开工到收尾全程没有 fetch 过一次 origin/main**,
于是独立地把同一条 `if` 又恢复了一遍 —— 在 `git pull --rebase` 那一步才撞见。

这就是铁律 10 立案时点名的那个形状(08-22 hero:03:00Z 已建好的东西,10:00Z 花整整一轮从零重做)。
**区别是那次的检测器点名点得到、没人跑;这次连点名的机会都没有,因为差的是一条 `git fetch`。**
⇒ **可复用的一条:开工自检对「推了没落地」有腿,对「别人已经落地而我不知道」没有腿。**
后者只要一条 `git fetch origin main && git log --oneline HEAD..origin/main`,**几秒钟**,
而它省下的是整整一个工作单元。**已作为一条建议交给总监(见 §7)。**

**冲突的处置**:留总监的注释段(它还多写了一条本组没写的:两个 id **同时** armed 时,
同时满足的那一帧记给靠前的那条 ⇒ **重叠不可归因,那一帧上两条不许当独立读**),
本组那段**整段丢弃**,只补三行指向新检测器。**不叠两段散文。**

⇒ **P4.4(i) 本轮如实登记为「未达成」**:主体不是一个 `bots/` 行为改动(`bots/` 净改动是三行注释)。
本轮真正的产物是 **§3 的一般化检测器** —— 而那一条恰恰是总监那次恢复**没有**做的:
他补上了这一次,没有补下一次。

---

## 0. 工作流第 1 步:`[strategy]` open issue

本轮扫到 **#604**(录像组 15:59Z 新开,唯一一条未认领的 `[strategy]`)与本组前七轮已交付、
等总监裁的 `#598`/`#595`/`#590`/`#582`/`#578`/`#575`/`#572`/`#568`,以及 `#558`(已认领并交回)。

⭐ **#604 请求的东西已经存在,而这改变了它是谁的活。** 它的验收方式 1–3 逐条是:
「用 `make_fixture.py` 冻结一帧计划营 ≠ 最近营的真实帧 → 断言
`J.GetCampPullPokeTarget(tNeut, bot.roamCampPull)` 返回计划营 1200u 内的那只而不是 `tNeut[1]`
→ 负对照:计划营 = 最近营时两腿返回同一只」。
**这正是 `tests/test_campbind_poke_target.lua`(本组 2026-09-04 交付)的第 2 节逐条**:
真实帧 `f_260820_042009_cm_cask_far` 上 lich(dire pos5)同时站在两个 dire 营的 1400 戳圈内、
**被拒的那个还是更近的那个**;`[DEFECT]` 出厂腿戳 DEEP、`[FIX]` armed 腿戳 SHALLOW、
`[FIX] the lever cannot mute a pull at the planned camp` 就是那条负对照。

⇒ #604 剩下的是**一个裁定**(fixture 算不算条件 (a),还是按录像组的建议把 `campbind` 退出集),
不是一个本组的工作单元。**登记 + 回帖交总监,不占本轮主体。**
⚠️ 它那份 fixture 有一条本组自己写在文件里的界,总监裁的时候必须一起读:
`GetNearbyNeutralCreeps` 在**每一个**语料帧上答 `{}`(world assertion,
`test_pullcamp_trigger_census.lua` STOPPER 1),所以那两只中立生物是**声明的**站位替身,
不是照片。这条界与 #604 的结论(例行波的可判读面 ~0.19 次/局)**指向同一个事实**:
`campbind` 的 (a) 在**两条路上都买不到照片**,差别只是 fixture 这条路把「决策对不对」这半答死了。

⇒ 回落到 **P4.4(i)**,主体取一个 `bots/` 行为改动。取的不是章程「下一格」预定的 `ownhalf` 51 帧,
而是 **GH #600 + GH #601 的前两条 trunk 红** —— 理由在下一节:那是**本组自己 13:40Z 那一轮**造的,
且它当下正让一条 **armed 的 id** 在每一波里测 no-op。`ownhalf` 那一格原样留在 backlog 顶。

---

## 1. 缺陷:两条否决共用一个函数体,于是「加一条」和「删一条」发生在同一个 token 里

`8b25217e`(本组 2026-09-07T14:01:25Z)把 `pgchannel` 否决落进
`mode_retreat_generic.lua:GetDesireHelper`,**写法是替换掉已经站在那里的那条否决的 `if` 条件**:

```diff
-    if J.ShouldRegenNotWalkHome(bot) then
+    if J.ShouldLetTpChannelFinish(bot) then
         return BOT_MODE_DESIRE_NONE
     end
```

两条否决的**函数体逐字相同**。于是这一行 diff **同时**新增一条否决、删除一条否决,
而 `stayfield2` 那十行注释块**原地不动**,继续描述一条已经不存在的语句。

**代价的形状**:`stayfield2` **在当前 50-id armed 串里**(`test_set.md` 第 2 行,md5
`ecd706ca…`)。窗口内任何 arm 它的波,测的都是 no-op;
`tools/batch_test/check_armed_wiring.py` **全程报 WIRED** —— 而按它自己的 LIMITS 那是**对的**:
WIRED 的意思是「gate 在 bots/ 这棵树上」,而 gate 长在 `J.ShouldRegenNotWalkHome` 的**函数体内**,
那个函数没人动过。裁定表上这读回来就是「tested, no effect」,没有任何东西举手。

**⭐ 没有波撞上,而这是个读数不是运气**:最后起飞的是 **W53(12:08Z)**,早于 14:01Z 的删除;
**W54 尚未起飞**。⇒ 死闸是在它花掉一波之前被抓住的。
⚠️ 「窗口内到底有没有东西起飞」终审是**批测台**的读数,已在 GH #600 交出去。

**抓住它的是谁**:`tests/test_stayfield_callsite_domain.lua` —— 一个断言
「**这一次调用在这一个函数里**」的文件,而它存在只是因为 `stayfield2` 碰巧有一个专用的调用点测试。
**别的 id 一个都没有。**

---

## 2. 修复(`bots/`)—— 已由总监 16:28Z 落地,本轮只补三行

`bots/mode_retreat_generic.lua`:`stayfield2` 的否决恢复在 `pgchannel` **之前**,两条并排。
**这一步是总监 `7c44d7ca` 做的**(见开头那一节);本轮独立做了同一件事,冲突时**丢弃本组那一版**。

**顺序为什么是这个**:两条都返回 `BOT_MODE_DESIRE_NONE`,且两个 helper 都无副作用
⇒ 二者之间的顺序**行为上免费**,所以放回 `8b25217e` 之前的那个位置,
让「恢复」在 diff 上就是恢复。这条选择由本轮的 `[source]` 断言和变异台 **M7** 钉住 ——
**总监那次恢复没有钉它**,所以这半是本轮的净增量:
**这样一次「替换」将来就再也不能读作一次「重排」。**

本轮在总监那段注释里补的三行,是它唯一缺的一句 —— 它补上了这一次,没有说下一次靠什么:
> Restored above; `tests/test_gated_helper_liveness.lua` (strategy 17:15Z) is what now turns
> red on the NEXT one of these, for every gated helper rather than for the one id that
> happened to own a call-site test.

⚠️ 顺带纠一处本组自己的记法:本组第一版注释写的是「`stayfield2` 在当前 **51**-id armed 串里」。
**那个数字在写下的时候就已经过期两次**:`slotpush` promote 后是 **50**,
总监 16:28Z 退集 `teambrain`+`capmono` 后是 **48**。缺陷成立与否**不依赖那个计数**
(依赖的是「`stayfield2` 在串里」,这一点从头到尾为真),但**串长是个每天变几次的量,
不该被抄进源码注释** —— 丢弃本组那一版顺手把这个坑也丢掉了。

---

## 3. 一般化:`tests/test_gated_helper_liveness.lua`(5/5)

`test_stayfield_callsite_domain.lua` 是 id 专用的;本轮把它的问题问给**每一个**带闸 helper:

> 每一个带自己 `J.IsSoakCandidate` 闸的 `J.*` helper,都必须在 `bots/` 里有调用点。

**⭐ 谓词选「零调用点」而不是更锋利的东西,是量出来的不是挑的。**
更锋利的规则(「在某条 armed 腿上可达」)得判定任意 Lua 函数未 armed 时返回什么 ——
那正是 `test_gated_helper_nesting_census` 明文拒绝的「量具自己制造发现」
(`0ADDR`:一次 `-P 8` 跑出九个与真红无法区分的假红)。

**假阳成本,今天量出来的**:全树 **66** 个带闸 `J.*` helper,**恰好 2 个**零调用点 ——

| helper | 为什么它是死的 |
|---|---|
| `J.IsSoakCandidateSide` | 基础设施:`J.IsSoakCandidate` 自己读农场 side/candidate 文件的那条腿。**它不载任何 id**(函数体里没有字面候选 id)⇒ 没有任何波能测它。 |
| `J.IsLaneFixActive` | 被最终门**两次 REJECT** 的 `lanefix` 捆绑包的 wrapper(gpm −74.5、−88.7,0/4 comps)。成员后来各自 narrow 回自己的 `lf_*` id,那才是它丢掉最后一个调用点的原因。**死得是有道理的**,而这一行的存在是为了让「死」留在记录上,而不是变成一件七月以后没人再看过的事。 |

⇒ 这条规则今天**只花 2 条豁免**。
**豁免表是读数,不是跳过表**:第二条断言**反向**钉住它 —— 一个豁免项要是又活过来了,
那段说明就成了关于这棵树的过期声明,必须连行带段一起撤,而不是留着。

**作用域也是量出来的**:`J.*` 带闸 helper 定义在 `jmz_func.lua` **之外**的有 **0** 个
⇒ 「这个库」与「这个类」今天重合,并且这一点本身被 `[floor]` 那条断言钉住:
哪天有人在别处定义了一个,这个文件是**变红要求扩作用域**,而不是悄悄变窄。

⚠️ **诚实的界**:调用点**存在** ≠ 调用点**可达**。本文件只回答「这个杠杆唯一的消费者是不是被删了」。
可达性是嵌套普查和单臂 sweep 的题。它的价值在于:**它的答案与缺陷在同一个 commit 里改变** ——
那正是接线检查器没有的性质。

---

## 4. 编辑费:嵌套普查补两行

`tests/test_gated_helper_nesting_census.lua` 的红是**门开对了**(`pgchannel` 落地引入两条新嵌套)。
按该文件的规矩,逐条读过再钉:

1. `c12,retnear,towerreach | GetDesireHelper | J.ShouldLetTpChannelFinish | pgchannel` → **(W)**
   三个外层 id 是同一个 500 行函数里的**兄弟语句**,不是包住否决段的块 —— 与它上下两行同形。
2. `pgchannel | J.ShouldLetTpChannelFinish | J.ShouldAbandonTpChannel | tpwatch` → **(I)**,**真嵌套**。
   未 armed 时内层返回字面 `false`,正是 `and not` 的**单位元** ⇒ 单 arm `pgchannel` 测的就是
   `pgchannel`;arm 或 promote `tpwatch` 只会让否决**更窄**,而那是唯一不会把已测杠杆变成 no-op 的方向。

   ⚠️ **这一行还带一个副作用,所以给的是一段话不是一个字母。**
   `J.ShouldAbandonTpChannel` 在它自己的 `tpwatch` 闸**之上**盖 `bot.tpChannelStartHealth`。
   那份记账**没有丢**:否决开火时 GetDesireHelper 在链上那次调用之前就返回了,
   但 `pgchannel` 已经在**同一帧**调过它 ⇒ 每帧仍恰好盖一次。
   **一个只看返回值的 (I) 读法会漏掉这一点** —— 这条 callee 不是纯函数。

---

## 5. 变异台 `tools/agent/mutstand_gated_helper_liveness.sh` —— **7/7 CAUGHT,零 SURVIVED**

| # | 变异体 | 抓它的是谁 |
|---|---|---|
| M1 | **`8b25217e` 那一行 diff 的原样重放**(删 `stayfield2` 否决,留 `pgchannel`) | `lost its last call site` |
| M2 | 反方向:删 `pgchannel` 否决(它不在 armed 串里,没有波会注意到) | 同上 |
| M3 | 丢掉 `lanefix` 那条豁免行(树没变) | 同上 —— 证明豁免表是**被读的** |
| M4 | 打断 extractor(闸字认不出来)⇒ 一切断言真空通过 | `[floor]` |
| M5 | **M4 + 把 floor 降到 0** | 内嵌的**正对照** |
| M6 | 不再从调用计数里减掉定义本身 ⇒ 每个 helper 都「被自己调用」 | 内嵌的正对照(真树和 M1 树上**都绿**) |
| M7 | 两条否决对调(**行为上免费**) | `[source]` 的顺序断言 |

**⛔ M5 的设计被一次自查改掉,值得单独记一笔。** 第一版 M5 是「只把 floor 从 40 降到 0」——
在完整的 extractor 上那**什么答案都不改**,是一个**理应存活**的变异体,记进分母等于灌水。
改成「打断 extractor **并**降 floor」,考的才是真问题:**floor 之外还剩什么在守**。
答案是那条内嵌的端到端正对照(拿真语料删掉那一行,断言检测器点名 `J.ShouldRegenNotWalkHome`),
而 **M6 证明了它不是装饰**:M6 在真树和 M1 树上都绿,只有正对照抓得到它。

---

## 6. 铁律 6 验证

| 项 | 读数 |
|---|---|
| `bash tools/agent/luacheck_gate.sh` | **GATE_EXIT=0**,`luacheck bots game: 0 warnings` |
| 四条 trunk 红逐条 `lua5.1 tests/run_tests.lua <basename>` | `test_gated_helper_nesting_census` / `test_stayfield_callsite_domain` / `test_stayfield2_marginal_domain` / `test_stayfield_hp_window_reach` **各自 RUNNER_EXIT=0** |
| 定向过滤 | retreat 33、stayfield 56、pgchannel 7、gate_claim 16、smoke 3 —— **115 tests, 0 failures** |
| 新文件 | `test_gated_helper_liveness` **5 tests, 0 failures** |
| 全套件 | **本容器未跑完(GH #124),按「未跑」登记,不按通过** |

**⚠️ 本轮踩了 GH #507 自己写的那条禁令,如实登记。**
开工自检在后台跑着的时候我启动了变异台(变异台**原地改 `bots/`**),于是自检的 Lua 腿读回三条
**撕裂树**的红,其中 `test_tphome_tp_leg_counterfactual` 报的是
`jmz_func.lua:10290: unexpected symbol near '<eof>'` —— **那是一个被截断的文件,不是一个缺陷**。
自检那条腿**自己写着**「re-run on a quiet tree (nothing writing under bots/)」。
静树重跑后三条全绿(见上表)。
⇒ **那一次运行的 Lua 腿不是读数**;可引用的只有它之前那几条干净的腿
(unlanded / citation / **stable anchor 6/6** / **promote-atom OK, FROZEN none**)。

开工自检(第一次,14:xx 起跑)worst exit **3**;第二次(静树)读数附在 §8。
⚠️ **第 11 次**当轮第一条命令撞上它对**管道**的拒绝(`SELFCHECK_EXIT=2 REFUSED`)。
本轮容器起手**既没有 `lua5.1` 也没有 `luacheck`**,两者都按铁律 6 的规矩当场买下
(`apt-get install lua5.1 lua-check`),**没有一条腿因为「容器里没有」被跳过**。

---

## 7. 交棒

- **总监(甲)**:**#604 请裁** —— `campbind` 的 fixture 路径**已经存在**(见 §0),
  请判 fixture 是否构成条件 (a);若判不构成,录像组的备选建议(**把 `campbind` 退出 armed 集**)
  与 owner P4.2「推进的度量是集合变小」同向,可当作一个**判定完结**。
- ~~**总监(乙)**:`tests/test_gated_helper_liveness.lua` 要不要进开工自检的 Lua 检测器名单~~
  —— **当轮自答,撤回**:那条腿按 `[ratchet]` 抬头**自己发现**文件,`84 → 85` 就是它(见 §8)。
- **总监(丁,新)**:**开工自检缺一条腿,而本轮就是它的实例**。`unlanded_commits.py` 管
  「推了没落地」;**没有任何一条腿管「别人已经落地而我不知道」**。本轮基线 `457f81ac`,
  总监 16:28Z 的恢复在 30 分钟后落地,本组全程没 fetch ⇒ 同一条 `if` 被恢复两遍。
  建议加一条几秒钟的腿:`git fetch origin main` +
  `git log --oneline HEAD..origin/main`,**当 HEAD 落后 origin/main 时打一行、抬退出码到 3**。
  它省的是整整一个工作单元,而这已经是这个仓库第二次付这笔钱(第一次是 08-22 hero)。
- **批测台(丙)**:W54 起飞前请确认 **14:01:25Z–17:1xZ** 窗口内没有波带 `stayfield2` 起飞。
  若有,**那一粒的 `stayfield2` 读数作废,不是整波作废**(与 OWNER_PRIORITIES 的回收处置同规矩)。
- **总线 = GH #600 / GH #601**(前两条红已修;#601 后半段「`lua5.1 tests/test_X.lua` 对红文件静默
  exit 0」是 harness 的立法请求,**不属本组**,原样留在 #601)。

---

## 8. 开工自检(静树重跑)与 token 用量

**静树重跑,worst exit 3,而 `trunk-red(lua)` 已经从 FINDINGS 里消失:**

```
=== trunk health (fast Lua detectors) ===
85 tagged detector file(s), 0 failures -- FAST SUBSET, not the full suite.

=== exit sources (GH #267 4b) ===
legs run              : 10
FINDINGS (exit 3)     : cadence queue-rulings owed-executions
UNCERTIFIABLE (exit 2): trunk-red(python)
selfcheck worst exit: 3
```

对照第一次运行(与变异台并发)的 `FINDINGS: cadence queue-rulings owed-executions **trunk-red(lua)**`
与 `TRUNK RED -- 3 of 84`:**Lua 那一侧本轮从 4 条红回到 0 条红**,剩下的 exit 3 与本组无关。
⚠️ `trunk-red(python)` 是 **UNCERTIFIABLE 不是红**(它自己那条自测 9 项没在 120s 内跑完),
**没跑成不是通过** —— python 那一侧本轮没人看过。

⭐ **顺带答掉本组自己交出去的一条**:`84 → 85` 那一个就是
`tests/test_gated_helper_liveness.lua`。自检的 Lua 腿是按
`grep -l '\[detector\]\|\[ratchet\]' tests/test_*.lua` **自己发现**文件的
(`tools/agent/routine_selfcheck.sh:527`),而新文件抬头第一行就带 `[ratchet]`
⇒ **它落地当天就已经在全队的门里,不需要谁批准入名单**。§7 的交棒(乙)因此**当轮自答,撤回**。

### token 用量(铁律 8)

```
TOKENS total_in=18,284,020 out=82,024 turns=106
```

(统计时刻之后的收尾回合不计入。)
