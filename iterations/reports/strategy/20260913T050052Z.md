# 协同组 2026-09-13T05:00Z — 同一族的第三个站点,而它是第一个活着的

**一句话**:`J.ShouldPunishDive`(出厂 promoted `punish`,**每一局 Turbo 都在跑**)的建筑
循环没有名字测试,于是**一个被我方占领的瞭望塔**读作「我们正被钻塔」并邀请全队 collapse;
落了 gated 收窄 `divepost`(2 行 `bots/` 代码)。**而本轮真正该被下一轮读到的不是这把刀,是
昨天那份普查抬头逐字写着「The two no-name-test anchor sites in bots/」——是三个,漏掉的那个
恰好是唯一一个出货中的。**

---

## 0. 认领依据

- 章程 backlog **0NEXT2**:主体必须在 `bots/`(OWNER_PRIORITIES **4.4 (i)**),并且优先在
  **未决 armed id** 里找「方向由构造固定」的收窄(不需要证人就能落,证人数只用来定价)。
- 工作流第 1 步扫过 `[strategy]` open issue:#775(arbheart,§4 的注释错误上一轮已修,剩下
  的 §6.1 是 `tests/` 主体)、#770 / #767 / #763 / #756(取帧 / 上界类)、#760(**四个收窄全部
  定价并排除,卡在 dumper = #786,不重买**)。都不满足「本轮主体必须是 `bots/` 行为改动」,
  于是按 0NEXT2 的起点建议走。
- P1 / P2 的球**仍不在本组**(P1 卡重新入集=总监,P2 卡决策侧入集+取证波,P4.2 冻结未解)
  ⇒ 本轮**没有**提入集。

## 1. 缺陷:一个没有名字测试的邻近锚

`bots/FunLib/jmz_func.lua:J.ShouldPunishDive` 的出厂域循环(收窄前逐字):

```lua
for _, building in pairs( tBuildings or {} ) do
    if J.IsValidBuilding( building )
    and GetUnitToUnitDistance( enemy, building ) <= 1200
    then bInDomain = true; break end
end
```

- `tBuildings = GetUnitList( UNIT_LIST_ALLIED_BUILDINGS )`,而**一个被占领的瞭望塔就在这张表里
  并且通过 `J.IsValidBuilding`** —— 这不是推的:`tests/_outpost_anchor_sweep.lua` 实测
  `wt_fixtures 68 / wt_allied 68 / wt_valid 68`(在表里的瞭望塔**一个都没被过滤掉**)。
- 这个循环**一个名字测试都没有** ⇒ 站在我方瞭望塔 1200 内的敌人读作「在钻我们的塔」,
  helper 返回它,`team_roam` 出价把队友拉过去 collapse。
- 为什么这是错的(验证哲学条件 (c),标准策略):瞭望塔**没有攻击**、身后**没有兵线**、
  丢了不掉建筑;离它近**不是**「越界进入我方防御地带」,而这条分支自陈的唯一意图就是后者
  (它自己的注释说 1200 是 tower-aggro 的替身,因为引擎不给那个信号)。

## 2. ⭐⭐ 为什么这个站点要自己的 id,而前两个恰好相反

`punish` 是 **promoted 默认**(AGENTS.md 的 promoted 清单;helper 抬头也写着
`PROMOTED (was soak-candidate 'punish')`)⇒ 这里**没有宿主 gate 可继承**,不加 id 就是在
**没有任何波次背书的情况下改出货行为**。

同族前两个站点都挂在**未 promote**的宿主下(`ohnum` 2026-09-12、`overchase`),那里**加新 id
才是错的**(pullcad 陷阱:`宿主id AND 新id`,单臂隔离零是结构性不可能而 `check_armed_wiring.py`
仍读 WIRED)。**判据不是「要不要加 id」而是「宿主是不是已 promote」**,而这条规则以前只写过
一半(只写了「宿主是候选时不要加」)。本轮把另一半写进宿主注释与
`tests/test_divepost_outpost_narrow.lua §1`:§1 断言宿主**没有**自己的候选闸,并说明哪天它
重新上闸,这个合取项就变成 pullcad 形状、`divepost` 的隔离读数当场失效。

## 3. ⭐⭐ 本轮真正的发现:普查按「谁在被定价」枚举,于是漏掉了唯一活着的那个

`tests/_outpost_anchor_sweep.lua` 抬头逐字:

> The two no-name-test anchor sites in bots/:

**是三个。** 漏掉的第三个就是本轮这个,而它是**唯一出货中的**那个。归因不是粗心:那份普查是
在给两个**候选**(`ohnum` / `overchase`)定价时写的,**没人在定价的站点就不在它的视野里**;
而「the two sites」事后读起来像一个**封闭枚举**,下一个读者不会去重数。

解药与 09-13 那条同源(「让测试解析出货表达式,不要复述它」)再往前一步:**让测试按树数,
不要按散文清单数**。新文件 §1 把每个 `GetUnitToUnitDistance( _, building ) <=` 锚点与它所在
合取式里的 `IsOutpostBuilding` **配对**(回看 240 字符),断言**恰好一个**未收窄锚点
(登记在案的 `J.ShouldPunishOverchase` leg (b),端到端 `oc_fire_building 0`,不重买):

- 为什么不是「两个总数相等」:今天两个总数**恰好都是 3**,但那是巧合 ——
  三个 cut 里有一个narrow 的是 `J.ShouldTpSupportTowerFight` 里一个 `>` 距离测试,
  **根本不是这个形状的锚点**。两个都错的数相等是世界上最容易误得的绿。
- 变异台 **M9** 往树里落一个新的未收窄锚点,§1 必须变红 —— 第四个站点落地当天自己举手。

## 4. 落地(2 行 `bots/` 代码,1 个新 gate id,gated 且 UNARMED)

```lua
local bDivePost = J.IsSoakCandidate( 'divepost' )      -- 读在循环之上
...
    if J.IsValidBuilding( building )
    and not ( bDivePost and J.IsOutpostBuilding( building ) )
    and GetUnitToUnitDistance( enemy, building ) <= 1200
```

- turbo-only **靠继承**:宿主第一行 `if not J.IsModeTurbo() then return nil end`(§1 钉住这一行,
  因为 `divepost` 的 turbo 性**只**来自它)。
- 谓词是上一轮为同族落的**无闸纯谓词** `J.IsOutpostBuilding`,零新常数。
- **方向由构造固定**:合取项加在**准入式**上 ⇒ 准入集是出厂集的严格子集,`bInDomain` 只能
  true→false ⇒ 响应集只可能变小,**造不出一个 punish 目标**。收窄至多让下面的 `ownhalf`
  分支在同一个敌人上按入侵深度重新判一次,而那本身也是子集判据。未 armed 时
  `not ( false and ... )` 是这个 `and` 的单位元 = 出厂字节。

## 5. 读数(`tests/test_divepost_outpost_narrow.lua`,runner **5/5**,实测 **3.9s**)

语料 **141 帧 / 653 活英雄行**(subject 同队;`load_fail 0`、`raised 0`)、
`pairs 557`(出厂 collapse 半径内的 (bot, 可见敌人) 对)。

| 读数 | 值 | 它管什么 |
|---|---|---|
| `anchor` | **62** | 出厂建筑锚点命中的对数(**正对照:循环确实在准入建筑**) |
| `anchor_has_wt` | **0** | 其中锚在瞭望塔上的 —— **本语料的域为零** |
| `MIN_WT` | **2257.3** | 同一量具报出的最近「敌人↔我方瞭望塔」距离(> 1200)⇒ 那个零**归因几何,不归因死量具** |
| `pred_true / pred_false` | **357 / 6500** | 谓词在真 handle 上两边都非零(活体) |
| `PRED_DISAGREES` | **0** | 谓词与**从它自己源码解析出的**字面量 `watch_tower` 一致 |
| `fire_a`(出厂) | **32** | 端到端驱动器不是死的(2026-09-09 登记 28,语料长大后 32) |
| `fire_b`(armed) | **32** ⇒ `flip_ab` **0** | **效应量在本语料上为零,而且是量出来的不是假设的** |

**⭐⭐ 两条真正有鉴别力的臂(§4)。** 无操作读数分不出「合取项承重且已上闸」与「死代码」。
两条臂都在**同一条代码路径**上只改一个词项(不出树拷贝、不重实现 40 行宿主):

- **臂 C**:`divepost` armed + `J.IsOutpostBuilding` 强制为真 ⇒ 32 次出厂开火**全部被压掉**
  (`fire_c 0`、`cf_suppressed 32`)。**这是「承重」的读数。**
- **臂 D**:同样的覆盖但 `divepost` **未 armed** ⇒ **一次都不变**(`fire_d 32`、`flip_ad 0`)。
  **这是「已上闸」的读数**,也是本组此前不会写的那条:一个漏写 `bDivePost and` 的落地会通过
  本文件**其他每一节**,只被臂 D 抓住 —— 「闸在场」与「闸承重」的分界(09-12 M8 的同型)。
- §5 附带一条设计自检:四条臂共用一次 `rf.load`,只有在 helper **不跨调用留状态**时才成立
  (防守 TP 那一族就因为 `J.TryTakeTpResponseSlot` 必须每臂 fresh load)⇒ 每行在 B/C/D 之后
  **重驱动一次臂 A**,653/653 逐字相同。

**变异台** `tools/agent/mutstand_divepost.sh` **caught=9 survived=0 STAND GREEN /
FINAL_SHA_OK=yes**。本台存在的理由是 **M4**:把 §1 读的字节**一字不动**,在下一行
`bDivePost = true` 覆盖掉闸 ⇒ §1/§2/§3 全绿,**只有臂 D 变红**。M6 杀臂 C(退回无覆盖驱动),
只被 §4 抓到。M9 见 §3。

⚠️ **登记一条排除**:本文件**不给准入项的顺序**写变异体。合取项夹在 `IsValidBuilding` 与距离
测试之间是一条**结构**断言(它在同一个 `if` 条件里),**不是执行顺序要求** —— 谓词自己 null
检查,提上去语料看不出任何差别。姊妹文件的顺序断言**是**行为(它的测试重算同一个合取式减
一项),本文件不冒充。

## 6. 附带(两条,都不是主体)

1. **本组自己造的 trunk 红修掉**:`tests/test_bots_walk_farm_only.py` 的
   `UNRESOLVED_HAND_READ` 缺 `tests/test_tpdeftower_outpost_narrow.lua` 的 `ls` ——
   **上一轮落地当天没登记,红留给了本轮开工**(GH #624/#774 的形状,这次作者与登记者是同一个
   座位)。连本轮新文件一起补;修后 **8 checks, 0 failed**。
2. `tests/test_gated_helper_nesting_census.lua` **两行 re-key**(`ownhalf` →
   `divepost,ownhalf`):**不是新嵌套**,是该普查按宿主的 id **集合**做键的既知性质(它自己在
   `outcommit | slotpush` 那条注释里写过同一件事)。嵌套分类 (P)/(I) 与一切已测读数不变;
   普查问的那个问题(未 armed 时内层是不是单位元)在本轮**有实测答案**:臂 D。**10/10 绿**。

## 7. 铁律 6 / 自检 / 残留

- **开工自检**:`EXIT=3`(FINDINGS: cadence queue-rulings owed-executions trunk-red(python);
  **UNCERTIFIABLE: none**)。⚠️ 首条命令**第 9 次**被 `REFUSED: stdout is a pipe` 挡回
  (`| tail` 读的是 reader 的退出码)—— 改走 `> /tmp/sc.log; echo EXIT=$?`。
- **trunk python 红 4 条,没有一条是本轮新造的**:`test_bots_walk_farm_only.py`(**本组的,
  本轮修掉**)、`test_carrier_terms.py`(英雄组)、`test_detector_source_constants.py`(GH #787)、
  `test_py_gate_hook.py`(GH #795)。本轮跑完整 python 套件:**125 passed, 2 failed,
  1 uncertifiable**,failed 两条即 `carrier_terms` + `detector_source_constants`,**都先于本轮
  且都有号**。⚠️ `test_py_gate_hook.py` 在本轮套件里 **PASS**,而自检那一遍 FAIL ⇒ 如实登记为
  **观察到两个不同读数,不冒充诊断**;它是 #795,harness 台已立案。
- **Lua**:受影响文件逐个跑绿 —— `divepost_outpost_narrow 5/5`、`gated_helper_nesting_census
  10/10`、`tpdeftower_outpost_narrow 4/4`、`tpdeftower_anchor_pricing 7/7`、`ohnum_refusal 9/9`、
  `ohnum_outpost_anchor 9/9`、`ownhalf_margin 7/7`、`punish_dive_gate 2/2`、`punish_overchase 7/7`、
  `replay_ownhalf_standoff 5/5`、`replay_212625_lion_ownhalf_punish 13/13`、
  `roamstale_collapse_action 8/8`、`roamreach_bounded_chase 19/19`、`lanekill_bid_reachable 6/6`。
  动态半(全套 ~100min,GH #124)**未跑,不声称**。
- **新测试不在钩子里,如实登记**:3.9s 在 5.5s per-test cap 内,但进 `lua_gate_manifest.json`
  的唯一合法动作是**全量 436 文件重测**(会重写整份 manifest,GH #783 那轮演示过代价),
  不是一个小工作单元该干的 ⇒ **这份文件的红今后挡不住任何人的 push**(GH #624 形状)。
  上一轮的 `test_tpdeftower_outpost_narrow.lua` 同样在闸外。
- ⚠️ **本轮踩到两次同一个坑,值一条通用教训**:`pgrep -f <名字>` **会匹配到发出这条命令的
  shell 自己**(命令行里就有那个名字)⇒ 我两次把「早就跑完了」读成「还在跑」,白等了约 15
  分钟。要么用 `ps -eo comm,args | awk '$1=="python3"'` 这类按进程名筛,要么读后台任务的
  输出文件本身。
- ⚠️ **另一次自伤,已止损**:`python3 tools/agent/lua_gate_measure.py --help` **没有 --help**,
  它当场开始全量重测(会重写 manifest);被 `| head -30` 的 SIGPIPE 打断,`git status` 确认
  `lua_gate_manifest.json` **零 diff**。⇒ **对会写仓库的工具不要试探性地传 `--help`**。
- **零 AWS**(一次调用都没有)、零波次、armed 串**一字未动**(P4.2 冻结;本 id gated 且
  **UNARMED**,出货行为逐字节不变)、`test_set.md` / `queue.json` 未动。

## 8. 下一棒(铁律 9 连带规则:修好 ≠ 做完)

- **GH #796**(本轮新开,`[strategy]`):登记第三个站点 + 普查少数一个 + `divepost` 落地;
  §取帧请求 = **一帧「敌人在我方瞭望塔 1200 内、且不在任何真建筑 1200 内」**(本语料
  `anchor_has_wt 0`、最近 2257.3u ⇒ 这一帧只能从批测录像里取,不是本地能造的)。
- 入集解冻后 `divepost` 需要:入集提议 + 条件 (a) 核验(证人形状同上)。
- 同族**第四个**站点的判据已钉在 §1 的按树计数上,**不靠散文**。

## 9. 铁律 6 三行(push 时钩子自己打的)

落地那棵树上钩子自己打的读数(`RULE6_MEMO=REUSE`,05:53:23Z,同一棵树):

```
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate:  98 ran, 0 findings, 0 uncertifiable, 41.9s
lua gate: 358 ran, 0 findings, 0 uncertifiable, 9 known-red, 506.0s
```

`RULE6_BYPASS` **未用**;动态半(全套 ~100min,GH #124)**未跑不声称**。
最终落地 `origin/main = 1b5dc9bf`,分支 `claude/intelligent-hopper-9d9me8` 已对齐同一 commit。
GH **#796** 已开(发表前 `claim_precheck.sh` **EXIT=0** / `local commits not on origin/main: 0` /
`paths cited 9, refused 0` / `state.json keys cited 1`)。

### ⚠️ 推送竞态:这一轮付了 5 遍钩子,而其中两遍是白付的

`push origin HEAD:main` **被拒 4 次**,main 在 9.5 分钟的钩子期间连跳
`83a2f0df → e9066883 → 881a4729 → 7cc02e8b`。**输掉竞速的原因是钩子时长,不是网络**;
per-tree memo 救不了(rebase 换 tree ⇒ 必然 miss),与 01:37Z 那轮同型、这轮更贵。
两条新东西值得登记:

1. ⚠️ **rebase 撞上了另一个组的同一条修复。** 英雄组在同一小时里也修了
   `test_bots_walk_farm_only.py` 的那条红(登记了**上一轮**那份 `ls`)⇒ 冲突。解法:**保留他们那条**
   (先落地、且归因更准:「作者没登记,下一个开工的人登记的」),只加本轮新文件那条。
   ⇒ **两个组同时修同一条 trunk 红,是 GH #624 的一个副作用而不是巧合**:红留给「下一个开工的人」,
   而同一小时里开工的人可能有两个。
2. ⭐ **一次被 harness 回收掉的后台 push,服务端其实已经落地了。** 第 4 次尝试报
   `cannot lock ref 'refs/heads/main': is at 1b5dc9bf` —— 那个 sha **就是本轮的 commit**,
   即前一次被杀掉的 push 已经更新了 ref,只是它的本地输出没了。⇒ **读到 ref-lock 冲突时先看那个
   「is at」的 sha 是不是自己的**,不要直接当成又输了一轮(否则会再付一遍 9.5 分钟的钩子;本轮
   就是这么付掉第 5 遍的)。落地读数因此以 `RULE6_MEMO=REUSE` 那一遍为准。

## 10. Token 用量

```
TOKENS total_in=26,686,011 out=99,775 turns=129
```

⚠️ 这个数偏高,归因写清楚:约 15 分钟花在**两次 `pgrep -f` 自匹配**的空等上(§7),
5 遍推送钩子里**有 2 遍是竞态白付的**(§9),其余是一次完整 python 套件 + 14 个受影响
Lua 文件逐个跑绿。
