# 协同组 strategy — 2026-09-14T06:20Z

**工作单元**:铁律 9 / 章程 backlog **0NEXT10** = `OWNER_PRIORITIES.md` **P2**(低血不回家,当前球在协同组)。

**产出类型:(b) —— `bots/` 一行未动。P4.4 (i) 本轮不满足,「连续满足次数归零」如实登记。**
本轮**确实落过**一个 `bots/` lever(`buymain`),**但它是一个已被定价并排除的改动,棘轮把它抓住了,已在同一工作单元内回退。**
⛔ **没有为了保住 4.4 配额留着它。**

---

## 0. 先说本轮最该被下一轮读到的一句

> ⭐⭐ **一个由「两个读者对同一份状态读数不一致」论证出来的缺陷,只是半个论证。
> 另外半个是:是不是已经有**第三个站点**在替它们对账 —— 而这半个,
> 从那两个读者身上都看不出来。**

本轮的现场:三个谓词读同一份物品栏、为同一瓶大药,停在不同的槽
(`J.HasFieldRegenSource` **0..5** / 买方 block 的槽位条款 **0..8** / 复购闸 `FindItemSlot` **看得见背包**)。
两个不一致的读者都在 `jmz_func` / `aba_item` 里;**对账的那个在一个 mode 文件里**
(`TrySwapInvItemForFlask()`,`mode_team_roam_generic.lua:2237`),
而且是通过**引擎每帧轮询所有 mode 的 `GetDesire()`** 这条**调用图上看不见**的路径到达的。
**grep 那两个读者 —— 本轮就是这么做的 —— 找不到它。**

配套的一句(顺序,不是速度):
⛔ **动手前先跑一次受影响族的测试,不要只跑自己新写的那个文件。**
本轮的棘轮 `tests/test_fieldbuy_backpack_rescuer.lua` 从头到尾都在,
它是在 lever 已经落地、fixture 已经写完、变异台已经跑完**之后**才被跑到的。

---

## 1. 复核先救了一次重复劳动(0NEXT10 的正文已陈旧)

0NEXT10 写着「决策侧自 `lf_recover` 被拒 / `homeroute` 被删之后**无任何 id 在管**」。
**这句话是陈旧的。** 决策侧现在有**五条** id,两条家路都覆盖:

| 家路 | 分支 / 调用点 | 管它的 id |
|---|---|---|
| TP | `撤退:1` (`botHP < 0.19`) | `J.ShouldStayAndRegen`(**已 promote**)+ `tpquiet` |
| TP | `撤退:2` | 无 —— 按实测豁免(`t2_ring_empty 2`,两帧都在 0.10 以下) |
| TP | `撤退:3` (`botHP < 0.34 or sum < 0.43`, lvl≥9) | `stayfield` |
| TP | `回复状态` (lvl≥6) | `tprecov` + `tpdeep` |
| 走路 | `mode_retreat_generic` | `J.ShouldStayAndRegen`(已 promote)+ `stayfield2` |

⇒ 再开第六个「决策侧 id」是重买。**章程 0NEXT10 已在本轮更新**(留历史行)。
**并请主会话 / Cursor 更新 `OWNER_PRIORITIES.md` P2 的「现状缺口」段** —— 它同样写着
「决策侧无任何 id 在管」,本组不自行改该文件。

---

## 2. 转而问的那个问题,以及它为什么是新的

2026-09-08 的 `tests/test_fieldsip_atom_pricing.lua` 在**谓词层**给 `fieldsip` 的转移定价:
armed 之后 hold 侧掉 **22** 帧,买方侧得同样这 **22** 帧,并判**这次转移正确**,
因为 owner P2 的规则(「买大药」)长在买方侧。

那是关于 `J.ShouldFieldBuyRegen` **答什么**的读数。
**它没有问接收方的 call site 能不能对这 22 帧动手** —— 而「域 = 谓词 ∩ 它被放进去的那个合取」
正是本仓自己铸出来的判据(`tests/test_stayfield_callsite_domain.lua`)。

> ⭐ 本轮是这条判据第一次被用在**接收**转移的那条腿上,而不是失去转移的那条腿上。

### 2.1 转移本身,本轮重算不引用

`tests/_buysite_sweep.lua`,112 fixture / **1039** live hero-frame:

```
C hold_bare 24   C hold_sip 2   C transferred 22
C IMPOSSIBLE_transfer_without_hold 0
```

并在**实际 armed 串**下直接读 P2 铁证帧 `f_260822_063722_lina_tp_home`:
`sip 85 < 0.25*1088 = 272` ⇒ `IsFieldSipEnough false` ⇒
`ShouldRegenNotTpHome false` **且** `ShouldRegenNotWalkHome false`,`ShouldFieldBuyRegen true`。
**两条家路都不拦,买方侧接管** —— 与 09-08 一致。

### 2.2 接收侧的槽位读数

```
C transferred                22
C site_main_slot              7   -- 有空主槽
C no_main_slot               15   -- 没有 ⇒ 大药只能投进背包
C ceiling_readable           18   -- 其余可读条款全满足的天花板
C ceiling_readable_main_slot  6
```

P2 铁证帧就在这 15 帧里(lina 六主槽全满、背包三空)。

---

## 3. ⛔ 我据此落的 lever 是一个**已被定价并排除**的改动,棘轮抓住了

落的是 `buymain`:给买方 block 追加一条「要有空**主**槽」的 gated 条款。
**这正是 GH #123 的提案**,2026-08-23 已实测并**否决**,
`tests/test_fieldbuy_backpack_rescuer.lua` 就是为它立的**常设棘轮**,抬头里写着反证:

- `TrySwapInvItemForFlask()`(`mode_team_roam_generic.lua:2237`)把背包里的大药**换进主槽**;
- 它**不是 roam 专属**:经 `ItemOpsDesire()` → `GetDesireHelper()` → `GetDesire()`,
  而引擎**每帧对每个 bot 的每个 mode 都轮询 `GetDesire()`**
  (`docs/BOT_API_REFERENCE.md` 原文,不是推断)⇒ 全员、每帧、6.2s 节流;
- 代价实测:该提案会在**干燥域** 28 帧里**噤声 13 帧(46.4%)**,
  而**那 13 帧里救得回来的是 13/13,卡住 0**。
- ⇒ 「用放弃 owner P2 要的行为的 46.4% 去换一个 12.9% 的浪费」,**lanefix 形状**。

**棘轮红在本轮的子集跑里** ⇒ **同一工作单元内回退**:
`bots/FunLib/jmz_func.lua` / `bots/item_purchase_generic.lua` / 该 census 的 PINNED 行
全部 `git checkout` 复原,`tests/test_buymain_receiving_slot.lua` 删除。
**`git status` 现在 `bots/` 零 diff。**

⚠️ 附带一条给下一个人的机械事实:该棘轮是靠 `src:sub(i, i+1200)` 取块的,
**本轮那段注释把 `ActionImmediate_PurchaseItem` 顶出了 1200 字符窗口**,
于是它红的那一条是「block no longer ends in a purchase」而**不是**它真正想抓的那句。
**它仍然抓住了,而且方向对** —— 但如果改动的注释更短一点,它可能一声不吭。
**登记为窗口脆弱,交总监**(不在本组作用域内代修)。

---

## 4. 本轮真正的产出:把那条 ruling 留下的**唯一**缺口补上并关掉

GH #123 的 13/13 是在**干燥域**上读的 —— 那些 bot **什么都没带**。
**转移集是另一个总体**:那些 bot 主槽里**带着** faerie fire / tango,
是被**量级**判为供给不足的,不是被**空手**判的。
**这个集合在那条 ruling 写下时还不存在**(`fieldsip` 的转移晚十六天)⇒ **它的获救率从没被读过。**

本轮用**救援器自己的谓词**(`J.Item.GetMainInvLessValItemSlot ~= -1`,驱动而非重写)读:

```
C no_main_slot            15
C no_main_slot_rescuable  15      ⭐
C no_main_slot_stuck       0      ⭐
C rescuable               22      -- 全部 22 帧都不卡
```

> ⭐⭐ **GH #123 的裁定**延伸到转移集,15/15。棘轮该关着,而且现在它关在**两个**总体上而不是一个。

产出文件:
- `tests/_buysite_sweep.lua`(语料普查器,含救援腿)
- `tests/test_fieldsip_transfer_receiving_site.lua` —— **8 tests, 0 failures**

### 4.1 变异台(按 (午):点名要点在**用例**上,而且点名本身跑过)

| 变异 | 落地核验 | 结果 | 红的用例 |
|---|---|---|---|
| **M1** `GetMainInvLessValItemSlot` 恒答 `-1`(救援器永远找不到可换的槽) | 已核验 | **CAUGHT** 2 failures | `[census] ...and the shipped rescuer clears all 15 of them` / `[frame] and it has no main slot…` |
| **M2** 删掉 `ItemOpsDesire` 里的救援器调用 | 计数 2→1 已核验 | **CAUGHT** 1 failure | `[source] the reconciler is reached from GetDesire, not from a Think` |
| **M3** 断开 `GetDesireHelper → ItemOpsDesire` | 计数已核验 | **CAUGHT** 1 failure | 同上 |

⚠️⚠️ **(午) 本轮又吃了一次,登记**:M2 第一版打的是 **SURVIVED**,
而真相是**变异体根本没落地** —— 那一行是**四个空格**缩进,我的 perl 锚写的是**两个 tab**。
**「存活」和「没打进去」在计分板上长得一模一样。**
⇒ **每个变异体都要先核验它真的改到了东西**(本轮改成打印改动前后的计数),
**再去读它是被谁抓的。** 这是 (午) 的下一层:(午) 说点名要跑一遍,这一条说**变异体本身也要跑一遍**。

---

## 5. 不声称什么

- ⛔ **不声称 `fieldsip` 该被 un-arm,也不声称 hold 侧本该留住那 22 帧。** 本轮 `bots/` 零 diff。
- ⛔ **不含金币 / 库存。** `bot:GetGold()` 在全部 1039 个 live 帧为 0(GH #495)⇒
  所有 `site_*` 是接收域的**天花板**,不是域。
- ⛔ **不声称换出去的是哪件道具。** `GetItemCost` 离线答 0 ⇒
  `GetMainInvLessValItemSlot` 的比价退化、返回第一个可换槽(每行 XFER 都读 0,就是这个原因)。
  **只有「存不存在可换槽」(`~= -1`,救援器自己读的那一位)是真结果**,也只用了这一位。
- ⛔ **不声称投送一定落进背包而非留在 stash。** 两种引擎结局都在帧之外。
- ⛔ **不是端到端购买**(GH #89/#100:fixture 到不了 `ItemPurchaseThink`)。

---

## 6. trunk 上三条红,**都不是本轮造成的**(干净树独立复现,本组不代修)

1. **`tests/test_fieldsip_atom_pricing.lua` —— 2 failures**(`/tmp/wt2`,`git worktree add HEAD`,
   干净树逐字复现 `30 tests, 2 failures`)。原因是**语料长大了**:
   - `the corpus walk now covers 1039 live hero frames, not 1021`
   - `961 live frames are at or under the flask bar, not 944`
   ⭐ **这一条与本轮直接相关**:我审的那条 ruling 所在的文件,**它自己的数已经过期**。
   本轮的 `_buysite_sweep.lua` 是在**今天的语料**上重算的(24 / 2 / **22**),
   **转移计数 22 稳定未变** —— 漂的是帧总数与 flask bar 那两个计数。
   该文件抬头写着「re-baseline them together or not at all」⇒ **交总监**,不单方面改基线。
2. **`tests/test_tpscroll_branch_shadow_census.lua`** ——
   `[corpus] the three priced levers are empty, and each names its clause`,
   `/tmp/wt` 干净树 `9 tests, 1 failure` 复现。本组 2026-09-13 已开 **GH #807**。
3. **python:`tests/test_pending_rulings.py`(810 checks, 1 failed)** —— 见 §7。

---

## 7. 闸与自检读数

**静态半**:`bash tools/agent/luacheck_gate.sh` → **`GATE_EXIT=0`**,`luacheck bots game: 0 warnings`。
**未用 `RULE6_BYPASS`。**

**三行闸读数(GH #624 RULING 9,会话分支那次推送,逐字抄)**:
```
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 86 ran, 0 findings, 0 uncertifiable, 10.7s
lua gate: 370 ran, 0 findings, 0 uncertifiable, 7 unanswered, 8 known-red, 531.8s
```
⚠️ **`HEAD:main` 连推两次被挡回,都是别的流在我 ~9 分钟的钩子跑期间落了地**:
第一次 `non-fast-forward`,第二次 `cannot lock ref 'refs/heads/main': is at fda473a3
but expected cdda7bfc`。两次都 `git pull --rebase origin main`(`EXIT=0`)后重推,
第三次成功(`fda473a3..d69df683`)。**三条腿每次重推都重跑了一遍,读数逐次一致**
(第二、三次 `lua gate` 是 `371 ran` —— 本轮新增的测试文件被算了进去)。

**动态半:不声称全量**(~100min,GH #124)。本轮跑的子集:
`smoke` 3/0 · `gate_claim` 16/0 · `stayfield` 56/0 · `buyband` 16/0 · `buytower` 18/0 ·
`buyring` 19/0 · `buydeep` 17/0 · `tprecov` 14/0 · `tpquiet` 15/0 · `itemtrip` 25/0 ·
`census` 141/1(§6.2) · `fieldbuy` 35/0(回退后)· `fieldsip` 30/2(§6.1,trunk 既有)·
新文件 8/0。**这是子集不是全量。**

**开工自检**:`routine_selfcheck.sh` 跑满,**真码 `EXIT=3`**。
首条命令**第 17 次**被 `REFUSED: stdout is a pipe` 挡回(它现在会打出正确重跑方式,照做即可)。
- `FINDINGS (exit 3)` = `cadence` `queue-rulings` `owed-executions` `lua-coverage` `trunk-red(python)`
- `UNCERTIFIABLE (exit 2)` = none;**但 python 腿内部 9 个 check 没跑成**
  (`5a/5a0/5a2/5b/5c/5d/5e/5f/5g`,两次子跑都没在 120s 内结束)⇒ **那一侧这轮仍未被完整看过。**
- `trunk red (python)` = **`tests/test_pending_rulings.py`,810 checks / 1 failed**。
  本轮一行 python 未动,**不是本轮造成的**,交总监。
- `trunk health (fast Lua detectors)` **90 files 0 failures** —— 它自称 **FAST SUBSET**,
  **不能据此说 GH #807 好了**,不替总监下判决。

---

## 8. 交棒(铁律 9 后半)

- **总监**:
  1. `tests/test_fieldsip_atom_pricing.lua` 的基线重定(1021→1039、944→961)——
     该文件抬头明写「一起重定或都不重定」,**跨四个 census**,不是本组能单方面做的;
  2. `tests/test_fieldbuy_backpack_rescuer.lua` 棘轮的 **1200 字符取块窗口**脆弱(§3 末);
  3. trunk 三条红的处置(§6)。
- **协同组自己(下一轮,已写进 backlog)**:
  P2 完成定义第 1 条按 §1 的表**视为已覆盖**;下一条杠杆改问
  **走路腿在实际 armed 串下的 live 域**(`stayfield2` 的边际域 = `S and (¬T3 or ¬T5)`,
  `tests/test_stayfield2_marginal_domain.lua` 已给闭式,**但它没在含 `fieldsip` 的串上读过**——
  与本轮同族的问题,换一条腿)。
- ⛔ **本轮不提入集申请**:P4.2 冻结期未解除,且本轮**没有新 id**(lever 已回退)。
  `state.json:buymain_20260914` 登记的是**回退记录**,不是 FROZEN-HOLD 候选。**这不是掉棒。**

---

## 9. Token 用量

```
TOKENS total_in=18,392,092 out=103,684 turns=100
```
(统计时刻为止;其后的收尾回合不计入。)
