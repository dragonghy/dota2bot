# strategy — 2026-08-21T22:00Z

## One line

**Closed the level-gate census (GH #84 §5).** The sixth and last TEETH level
constant — `ability_item_usage_generic.lua:5749` (guard-relic TP) — also cannot
be built into a full-shape (gated + real-frame + assert the FINAL desire) fix,
but for a reason distinct from the other five. **bots/ untouched, tests/mock/
untouched, no gate, no test_set request, no new corpus request, zero AWS.**
Artifact: `tests/test_relicguard_siege_gate.lua` (8 tests).

## What I picked and why

- Two open `[strategy]` issues: **#77** (replay-check on `l1trade` — a scheduling
  recommendation to the director to bisect `l1trade` as the sole variable; not a
  Lua lever this group acts on directly) and **#62** (our own `defnum`, still
  blocked on #61). Neither is a fresh actionable lever this session.
- Backlog top actionable item is **0i**: from the 6 TEETH level constants pick
  ONE and do the full shape (gated + real frame + assert final desire). The last
  three rounds tried `285` / `535` / `228` and each was un-doable; 0i names the
  next default as **`ability_item_usage_generic:5749`** (guard-relic TP).

## The finding, measured on a real frame

The guard-relic region has an outer gate (5751-5756) and **two** inner sub-paths,
each ending in `BOT_ACTION_DESIRE_HIGH`.

**The census level clause is SATISFIABLE** — this is what separates `5749` from
`285/535/228`, whose gates the corpus cannot reach at all. Exactly one of the 96
fixtures carries a subject at level ≥ 15 — `f_260820_043120_viper_defend_paired`,
viper at level 15 — and on that frame the **entire outer gate opens**:

| clause | site | value on frame |
|---|---|---|
| `bot:GetLevel() >= 15` | 5751 | **15** ✓ |
| `#nNearEnemyList == 0` | 5752 | 0 ✓ |
| `J.Role.ShouldTpToFarm()` | 5753 | true ✓ |
| `DistanceFromFountain() > 2000` | 5754 | 8773 ✓ |
| `dist(bot,ancient) > nMinTPDistance-200` (5500-200) | 5755 | 7582 ✓ |
| `GetAroundTargetAllyHeroCount(ancient,1400) == 0` | 5756 | 0 ✓ |

So **the level constant is NOT what holds this branch closed.** What holds it
closed is that both inner sub-paths fire only when the bot's **own ancient is
under direct siege**, a world the self-terminating Turbo batch corpus never
reaches (backlog 0a: games end ~640s with one tower down; nearest enemy to any
ancient in the 0411xx wave was 4925u):

- **Path 1 (守护遗迹, 5758-5778)** reads `J.GetNearestLaneFrontLocation`, which
  calls the **GH #61-refused** `GetLaneFrontLocation` three times
  (`jmz_func:3476-3478`). So path 1's final HIGH desire (5778) is **unassertable
  in the fixture world without declaring a lane-front assumption** — exactly the
  footing on which `defnum`/`defclose` ride the #61 stub. Its geometry clause
  `GetUnitToLocationDistance(nAncient, nEnemyLaneFront) <= 1600` means the enemy
  front has reached within 1600 of my own ancient = base siege.
- **Path 2 (保护遗迹, 5781-5810)** needs **both** tier-4 towers destroyed
  (`GetTower(team,9)` and `(team,10)` both nil) **and** an enemy creep within 800
  of the ancient. On the viper frame both tier-4 towers are alive (dump ground
  truth, non-nil handles) and `UNIT_LIST_ENEMY_CREEPS` is empty (the dumper
  carries no creeps), so path 2 is **closed twice over**. It also carries a
  **second** copy of the census constant, `bot:GetLevel() >= 15` at 5791.

## Census closed

All six TEETH are now examined; **none is buildable into a shippable gated fix
with the current corpus**, each blocked by a distinct reason:

| TEETH | blocker |
|---|---|
| `mode_farm:285` | dumper gap (star-frame quantity not in the .dem) |
| `mode_farm:535` | corpus gap (outer runMode never co-occurs in sampled frames) |
| `mode_farm:392/506` | dead disjunct leg (`J.IsLateGame()` 0/94 in self-terminating corpus) |
| `item_purchase:228` | not in Turbo at all (dispatched past on the Turbo leg) |
| **`aiug:5749`** | **base-siege corpus gap (0a) + #61 lane-front wall on path 1** |

**The level-gate census yields zero shippable levers.**

## Corpus request — NOT new, consolidate onto 0a

The base-assault frame this needs is the **same** "games that reach the base"
request backlog **0a** already tracks. A base-assault game unlocks 0a's
retreat/defend clauses (`mode_retreat_generic:774`, `aba_defend:939`/panic) **and**
this guard-relic TEETH at once. No new corpus request opened.

## A harness note (recorded, not acted on)

`GetAncient` falls back to the **map origin (0,0)** on fixtures lacking ancient
building data (`tests/mock/replay_fixture.lua:687-690`, `bot_api.lua:288-293`).
So a corpus-wide "min enemy → own ancient" scan is contaminated — e.g.
`f_073148_zuus_lina` reports enemy Lina 65.3u from a *phantom origin-ancient*.
The finding is therefore grounded on the viper frame (real ancient at
~(-5920,-5352)) alone, and 0a's 4925u number is **cited, not re-derived**. This
is already documented in the mock (comment 687-690: "decides every base-threat
and fountain-direction question in the wrong place").

## Verification

- **luacheck 0 warnings** on `bots game` and on the new test file (bots/ and
  tests/mock/ both untouched — confirmed with `git status`).
- New file: **8 tests, 0 failures** standalone (a `[premise]` leave-behind test
  was added, see below).
- Full suite: **1032 tests, 0 failures** (`lua5.1 tests/run_tests.lua`).
- **2 external mutations, each verified to apply AND roll back:**
  - **M-A** disable the loader's #61 refusal (return a Vector instead of
    raising) → `path 1 hits the #61 lane-front wall` test goes **red** with the
    expected message; rollback → green.
  - **M-B** make `GetTower(team, 9/10)` return nil (simulate tier-4 destroyed) →
    `path 2 is closed twice` test goes **red** on the tower assertion; rollback →
    green.
- Plus **2 in-test mutation self-checks**: declaring the lane front removes the
  raise (proves the raise is conditional on non-declaration); a front declared
  AT the ancient satisfies the `<= 1600` clause (proves that clause is not
  vacuous — it just requires the front to have reached the base).

## State

- `iterations/state.json` → `relicguard_LEVEL_GATE_CENSUS_CLOSE_20260821`.
- Charter "当前状态" and backlog 0i updated.

## Token usage

`TOKENS total_in=30,526,756 out=197,589 turns=190`

(As-of the stats moment; the closing commit/push turns are not counted. The
large `total_in` is dominated by prompt-cache reads while blocking on a severely
degraded container — the full suite took ~40 min of wall-clock to run 1032
tests, and the wait was spent re-reading the same buffered progress file.)
