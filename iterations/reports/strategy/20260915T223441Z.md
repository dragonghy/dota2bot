# 协同组 2026-09-15T22:34Z — 三个眼位从来没有存在过:gated `warddupkey`

**出口 (i)**(`bots/` 行为改动,owner 优先项 4.4 (i) 不断档,连续第二轮)。
**成本:零 EC2 / 零 CE / S3 读取 0 个对象**(本轮未触碰 AWS,未下载任何 `.dem`
或 `analysis.json`;按铁律 1 RULING 48 写成三段)。
**未提入集**(P4.2 冻结期 ⇒ 登记进 `state.json`,不进 `test_set.md`)。

## 〇 选活路径(铁律 9 → 章程 1b)

先读 `iterations/OWNER_PRIORITIES.md`:

- **P1(1)** 拉野 SILENT 根因 —— 球上轮已交总监(GH #809),本组无未完成项;
- **P2** 决策侧 TP 腿 —— 仍卡在 `owed_executions.json:wandlimbo_charge_instrument`
  (dumper `main.go` + fixture 侧 `replay_fixture.lua` **两端都要**)。那是**量具类**,
  按 P4.4 **不得作为工作单元主体**;
- **P4.4** 对本组的要求是主体必须是 `bots/` 行为改动 ⇒ 满足。

⇒ 走章程 1b,取 backlog **0NEXT24**(它自己点名的「下一轮第一项」)。

## 一 0NEXT24 要求的开工先查项,答案是「不用改」

0NEXT24 留了一条「下一轮开工先查这一件再决定要不要合并」:
`AfterEnemyTowerFall__Dire[TOWER_MID_3]` 只有 `[1]` 和 `[3]`(**没有 `[2]`**),
于是 `pairs()` 读两个而 `#` 只读 1 —— **有没有读者用 `#`,上一轮没查**。

**查了:没有。** 两个 producer 里 8 处组遍历全是
`for tower, spots in pairs(...)` + `for _, spot in pairs(spots)`,另有 2 处
对已建成候选表的遍历(`X.GetClosestObserverWardSpot` 的 argmin 和哨兵侧的
chooser),**也都是 `pairs`**;全文件 `#spots` / `#WardLocations` **零命中**。
⇒ 那个洞在今天的读者上是**惰性的**,不与本轮合并;同时这条正是本轮杠杆
「在 max 整数键 + 1 处追加」得以成立的前提,所以它**被钉成断言**
(`[source] neither reader uses # on a spot group`,10 = 8 组遍历 + 2 表遍历,
拆开登记以便日后归因),而不是留在散文里。

## 二 ⭐⭐ 头条:三个眼位不是「位置错了」,是**从来没有存在过**

`bots/FunLib/aba_ward_utility.lua` 里三个表构造器把同一个整数键写了两遍。
Lua 保留**最后**一次赋值 ⇒ 每组里第一个点在运行期**根本不存在**,
**组比它看上去短一个,而没有任何东西说出这件事**:

| 表 | 组 | 重复键 | 被吞掉的点 |
|---|---|---|---|
| `WardLocationsBeforeAllyTowerFall__Dire` | `TOWER_MID_1` | `[4]` 两次 | `(-2400.793457, 1431.276611)` |
| `WardLocationsBeforeAllyTowerFall__Dire` | `TOWER_TOP_3` | `[4]` 两次 | `(605.637573, 6996.875977)` |
| `WardLocationsAfterEnemyTowerFall__Dire` | `TOWER_TOP_2` | `[1]` 两次 | `(-5217.980957, -1648.555908)` |

⛔ **推送闸上两个自动读者按构造都看不见它**:重复键是**合法 Lua** ⇒ luacheck
(它是 linter)沉默;**文件能加载** ⇒ smoke loader 也沉默。这与上一轮
`wardcomma` 是**同一个盲区、不同的失效方式** —— 逗号**挪走**一个点,重复键
**删掉**一个点 —— 所以是**另一个 id**,不合并(捆绑会让证据无法归因,lanefix 教训)。

## 三 ⛔ 上一轮的立案把表名和边**记错了**,而这个错**压低了域一个数量级**

0NEXT24 原文把三处记成 `AfterEnemyTowerFall__Radiant[TOWER_MID_1]`、
同表 `[TOWER_TOP_3]`、`AfterEnemyTowerFall__Dire[TOWER_TOP_2]`。
**实测:两处在 `BeforeAllyTowerFall__Dire`,而且三处全是 DIRE。**

这不是笔误级的差别:

- `AfterEnemyTowerFall` 要**敌方**塔倒了才读;
- `BeforeAllyTowerFall[TOWER_MID_1]` 是在**我方自己的**中一塔**还站着**时读的 ——
  **即每一局 dire 方从零分钟起**。

⇒ 立案时记下来的域比真实域**稀有一个数量级**。这一条已钉成断言
(`[source] all three duplicates are DIRE tables, two of them BeforeAllyTowerFall`,
走源码、按首次出现取,免得 resolver 里同名的三个常量把表名盖掉),
**这样下一个读者不会从散文里继承同一个错误**。

📌 **可迁移句(本轮买到的)**:**一条立案记录里的「在哪个表」不是元数据,
它就是域本身;抄进 backlog 的那一刻它就不再被任何东西检查了。**
0NEXT24 的判据(选一类「语法合法但语义一定错」的形状、扫遍 `bots/`、
把命中数当结论的一部分)**跑通了**,但它管的是**形状**;**形状对、归属错**
这一格上一轮没有读者。

## 四 普查:7 处 / 275 文件 —— 是笔误不是约定

新工具 `tools/agent/table_dup_key_census.py`(brace 精确:注释/字符串按长度
致盲后跟踪 `{}`/`()` 深度,只在字段真能开始的位置解析键;退出码 0/3/2)。

```
CENSUS scanned=275 files, duplicate_keys=7
  bots/FretBots/Soundboard.lua:155  LYRICAL      (first at :144)
  bots/FretBots/Soundboard.lua:645  WW13         (first at :635)
  bots/FretBots/Soundboard.lua:646  WW14         (first at :636)
  bots/FretBots/VoiceoverHeroes.lua:70  npc_dota_hero_bloodseeker (first at :64)
  bots/FunLib/aba_ward_utility.lua:233 [4]  / :268 [4]  / :308 [1]
```

⚠️ **写工具时撞出的缺陷,记在这里免得重学**:第一版把字符串按长度致盲后
**再从致盲文本里读键**,于是 `["foo"]` 变成 `[     ]` ⇒ **每个不同的字符串键都
塌缩成同一个空键**,`bots/` 读出**几百处「重复」**。结构必须从致盲文本读,
**键的文本必须从原文切** —— 两者是两个问题,第一版用一个答案回答了两个。

⛔ **故意没捆绑的第四处**:`bots/FretBots/VoiceoverHeroes.lua:70` 在
`-- Bounty Hunter` 注释下把键写成 `npc_dota_hero_bloodseeker`,映射到
`'npc_dota_hero_bounty_hunter'`。两个后果:Bloodseeker 的别名现在解析成
Bounty Hunter,**而且根本没有 `npc_dota_hero_bounty_hunter` 这个键**。
这是一个**真的名字解析 bug**,但**文件不同、失效方式不同、后果不同** ⇒
另立工作单元,不塞进本 id。已写进 `state.json:warddupkey_20260915.filed_not_bundled`。

## 五 域:⛔ **零写在最前面,不被绕过**

`tests/_warddupkey_sweep.lua`,**每帧用它自己的塔状态,什么都不声明**:

| 读数 | 值 |
|---|---|
| fixture / 英雄行 / **dire** 英雄行 | 125 / 1250 / **625** |
| dire 且 `J.GetPosition >= 4`(`mode_ward_generic` 自己第一行放行的人群) | **126** |
| 其中自家中一塔**还站着**(MID_1 组被读的状态) | **82/126** ⇒ 分支是**活的** |
| armed 候选表**正好长一个**且含补回点 | **80/126** |
| **argmin(真正会走过去插的那个点)改变** | **0/126** |

⇒ **在这份语料上,这个杠杆一次插眼都没改变。** 这句话写在源码抬头第一段、
`state.json` 的 `the_zero` 字段、issue 和本报告里,**没有任何一处把它藏在
「但是」后面**。

⛔ **它不是「远远够不着」**:补回点按距离进前三的有 **13/80**;最近的一帧
(`f_260820_043524_wd_defend_alone`,Lich,pos 5,t=587.4)它离 bot **951u**,
出厂 argmin **548u** —— **差 403u**。同一帧上 bot 再站开 400u 就翻过来了。
⇒ 这是 **语料覆盖的零**(GH #838 的那一半),**不是构造性的零**:宿主在跑、
点被放行,只是语料没抽到它能赢的站位。按 0NEXT23 立下的两分法,
**这一边是「落,gated + 欠条 + tripwire」**。

## 六 ⭐⭐ 单帧语料**按构造**看不见的那条通道,是这个缺陷真正的代价

一个眼位被插过之后有 **360s** 的 `plant_time_obs` 冷却。于是一局里组会**抽干**,
**4 个点比 5 个点先抽干** —— 而当最后一个没过冷却的点**正是被重复键删掉的那个**,
出厂 bot 对那座塔**一个点都没有,哪儿都不插**。

单帧语料永远看不见这个。所以测试**把它驱动出来**(真实帧、真实过滤器、
真实 producer,**只声明 plant 时间**):出厂 **0 个**可用点,armed **正好 1 个**
—— 就是那个补回的点。

## 七 落地(一次一个小杠杆)

- `bots/FunLib/aba_ward_utility.lua`:三个被吞值**提升为具名常量**
  (`WARD_DUP_DIRE_*_EATEN`),**重复键原样留着**(出厂答案必须还是第二个值,
  而且重复键本身是这份文件是它自己的唯一见证);
  `X.ApplyWardDupKeyFix()` 在 **turbo + `IsSoakCandidate('warddupkey')`** 下
  在 max 整数键 + 1 处追加三个点,由**两个 producer 的头部**各调一次。
- **对世界幂等,不对标志位幂等**(按身份查在场):producer **每 tick** 都调它,
  非幂等追加会让组**整局无限增长**;这一条是变异体 M8。
- **单 id,不与任何 id 合取**(pullcad 陷阱);未 armed 时函数在第一个合取项
  上落空,每张表都是出厂的那张,**连对象身份都是**。
- ⚠️ **dire-only BY CONSTRUCTION**:三处重复键全在 `__Dire` 表里,`GetTeam()`
  选边 ⇒ 镜像 A/B 里 **radiant 腿的零不是关于本 lever 的证据**。它是
  `wardcomma`(radiant-only)的**镜像**;两者合起来才覆盖两边,但**必须分开**。

## 八 验证读数(直读退出码,不过管道)

| 闸 | 读数 |
|---|---|
| `GATE_EXIT=` | **0 CLEAN**(`luacheck bots game: 0 warnings`) |
| `py gate:` | **0**(94 ran, 0 findings, 0 uncertifiable, 15.0s) |
| `lua gate:` | 见文末「收尾」节 |
| `tests/run_tests.lua warddupkey` | **13 tests, 0 failures** |
| `tools/agent/mutstand_warddupkey.sh` | **9 个变异体全 CAUGHT + control SURVIVED,exit 0** |

变异台的 9 条里两条是本 lever 独有的:

- **M5(本台为它而存在)**:**把重复键改个号就完事** —— 出厂默认值**无波次
  挪动**。armed 与 unarmed 从此一致,**所有反事实用例全绿**,**没有任何行为腿
  看得见它**;抓住它的只有那条源码普查。与 `wardcomma` 的 M5 同型。
- **M8**:**去掉幂等守卫** —— armed 下**第一次** producer 调用仍然正确,
  于是**所有单次调用的断言都满足**;组从第二个 tick 起每帧长一个。
- **M9**:补回点丢掉 `plant_time_obs` ⇒ 它永远不过期、永远不被记为用过。
  表长腿和 argmin 腿都看不见,**只有 drain 腿看得见**。

## 九 交棒(三条交出去,下一轮要看一眼)

(a) **`queue.json:strategy-49`**(新):球在**录像组/批测台**。**要一帧,不要一波**
   —— dire pos>=4 的一帧使补回点成为 argmin(最近差 403u),**或**一帧里某个
   tower 组的出厂眼位全在 360s 冷却内(抽干)。⛔ 零 EC2、不申请发波、
   不申请入集(P4.2)。
(b) **`warddupkey` 的处置在总监**:P4.2 冻结 ⇒ 预期裁定 `FROZEN-HOLD`
   (登记、不入集、不视为掉棒)。`wardcomma`(GH #842)同样仍在总监手上。
(c) **FretBots `npc_dota_hero_bounty_hunter` 键缺失**:本轮**故意没动**,
   已登记在 `state.json`,下一轮或英雄组认领。

⛔ **本轮没有重复提** GH #828(`tpscroll` census 30→31)、GH #838(`waitclar`)、
GH #814(已由总监 backfill 修好,上一轮已止)。

## 十 ⚠️ 本轮实测到一次并发事故,读数因此**重跑过**

开工自检(铁律 10)跑了很久,与本轮的变异台**时间上重叠**。自检报告里有一行:

> ⚠️ a killed test left `bots/Customize/soak_side.lua` ARMED; removed it.

那正是 GH #229 / #365 §3 点名的**一个全局 inode**:两个进程抢同一个开关,
失效方向是「**闸没有打开**」—— 而「闸没打开」恰好是 gated 测试里**多数用例
期望的结果**,所以它会读成通过。

⇒ **不赌方向**:自检结束、确认无其他进程在跑之后,
`tests/run_tests.lua warddupkey` 与 `mutstand_warddupkey.sh` **串行各重跑一次**,
**两份读数逐字复现**(13 绿 / 9 全 CAUGHT + control SURVIVED,exit 0 直读)。
本报告第八节引用的是**重跑后**的读数。
⛔ `mutstand_warddupkey.sh` 抬头那句「run it SERIALLY」不是客套;本轮它第一次被自己撞上。

## 十一 开工自检读数(铁律 10)

`bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1; echo EXIT=$?` ⇒ **EXIT=3**
(0 干净 / 2 未核验 / 3 有发现)。

⚠️ **第一次调用被它自己拒绝了,而那是对的**:我最初把它的 stdout 接进了 `tail`,
它答 `REFUSED: stdout is a PIPE, so the exit code your harness reports is the
READER's` —— 证据纪律 3,它说这是**第 5 次复发,每次都是当轮第一条命令**。

- `FINDINGS (exit 3)`:`queue-rulings` `owed-executions` `lua-coverage`
  `trunk-red(python)`;`UNCERTIFIABLE (exit 2)`: none。
- `UNCERTIFIABLE` 9 条全部来自 **5a0「该腿没能在 120s 内跑完」**(115 个检测器 / 120.1s),
  按工具自己的 LIMITS,**这不能区分「发现集变大」与「容器变慢」**。
- ⛔ **这些发现都早于本轮的任何改动**(自检在本轮第一条命令时启动,当时工作树未动过),
  **不是本轮引入的**;同时 `trunk-red(python)` 那一行自己也写着
  「Whether main is red too is NOT established by this line」。
  本轮三条闸腿(见第八节)在改动之后**全绿**,其中 py gate 在我改完
  `state.json` **和** `queue.json` 之后**又跑了一次**(94 ran, 0 findings)。

## 十二 收尾

| 闸 | 读数(改动落地后,串行) |
|---|---|
| `GATE_EXIT=` | **0 CLEAN** — `luacheck bots game: 0 warnings` |
| `py gate:` | **0** — 94 ran, 0 findings, 0 uncertifiable, 15.7s |
| `lua gate:` | **0** — 393 ran, 0 findings, 0 uncertifiable, 10 unanswered, 6 known-red, 748.2s |

⛔ **没有用过 `RULE6_BYPASS=1`** —— 本容器三条腿都跑得动,无「SKIPPED, not passed」行可抄。

```
TOKENS total_in=14,187,675 out=75,840 turns=100
```

(按铁律 8:这是**统计时刻为止**的数字,统计后的收尾回合不计入。)

