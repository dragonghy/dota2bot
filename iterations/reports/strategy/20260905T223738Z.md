# 协同组 2026-09-05T22:3xZ —— `pullcad` 陷阱的第二种形态:两个 id 在同一条**路径**上,而不是同一个条件里

**一句话**:`J.ShouldStayAndRegen`(**PROMOTED**,每一局 Turbo 都活着)这条路上
**每一个库存读数都停在格位 5**,于是一个残血、无人追击、**大药就在背包里**的 bot 被读成
「两手空空」,被放回家 —— owner P2 逐字禁止的那趟路。本轮落 `staybag`(gated,turbo-only,**未 armed**)。
**但本轮最该被读的不是这条杠杆,是它为什么必须是一个独立 id**:那个行为**本来就在树上**,
只是需要 `'staysrc'` **与** `'bagsalve'` **同时 armed** 才够得着 —— 两个站点各写一个 id,
所有审阅规则都读作干净,而**任何一次一个 id 的隔离波在这两个 id 上读到的都是正确的零**。

**工作单元来源**:`[strategy]` open issue 本轮扫过 **#521 / #511 / #503 / #500 / #495 / #489 / #485**
—— **无一条未认领**(全是本组前几轮已认领并交回的)⇒ 按铁律 9 取
**OWNER_PRIORITIES P4.4(bots/ 主体配额)+ P2(决策侧,球在本组)**,
并做章程 `0STAYBTL`「下一格」逐字点名的两个候选之一
(`J.HasFieldRegenSource` 的 backpack 不对称在 **PROMOTED 侧**的同族问题)。

---

## 1. 立案句(先跑域价钱,再动代码)

| 读数 | 出处 | 扫到哪 |
|---|---|---|
| 出厂 `bHasFlask` | `J.IsItemAvailable`(`jmz_func` ~1758) | `slot >= 0 and slot <= 5` |
| `staysrc` 加宽(§EY) | `J.HasFieldRegenSource` 主循环 | `for i = 0, 5` |

两条独立的路各自停在格位 5 ⇒ 背包里的大药读作「什么都没带」,
决定权落到 `bot:GetGold() < 90`,bot 被放回家。
四个 G 事实(`AVAIL_MAX_SLOT == 5`、`SRC_MAIN_LOOP_HI == 5`、
`STAY_SHIPPED_USES_AVAIL == 1`、`STAY_SHIPPED_SLOTREADS == 0`)**全部从源码解析**,不是复述。

## 2. ⭐ 主判据(立法级,可复用,超出本主题)

**`pullcad` 陷阱有第二种形态,而它没有任何 grep 找得到:两个 id 摊开在一次函数调用的两侧。**

已知形态是 `IsSoakCandidate('X') and IsSoakCandidate('Y')`——promote 任一个就冻结成 FALSE,
审阅者和 `check_armed_wiring.py` 都在找它。本轮量到的是同一个合取**跨过一次调用**:
从 `J.ShouldStayAndRegen` 够到背包里的大药,需要 `'staysrc'`(才会调到 `J.HasFieldRegenSource`)
**与** `'bagsalve'`(那个 backpack 块才会执行)**同时 armed**。
每个站点各自只写了一个 id;`test_gated_helper_nesting_census.lua` 把这一对钉为 **SAFE**,
而且在那份普查问的意义上它**确实是** safe。它只是**买不到**。

⇒ **「additive-and-safe」与「single-arm-visible」是两个不同的性质,
只有后者决定一次波能不能量到这个行为。**

## 3. 读数是四个驱动之间的「关系」,不是翻转数(1012 活英雄帧,五次驱动/帧)

| 量 | 读数 |
|---|---|
| `flips_staysrc`(单臂) | **44** |
| `flips_bagsalve`(单臂) | **0** —— 它的 gate 在 `'staysrc'` 的短路后面,**根本没被走到** |
| `flips_pair`(成对) | **46** |
| `pair_gain` | **2** |
| `flips`(本 id 单臂) | **2** |
| `pair_gain_not_flips` | **0** |
| `flips_both_levers` | **0** |

⇒ **两个 id 隔着一次调用能买到的那 2 帧,恰好就是本 id 一条臂买到的那 2 帧。**
钉帧上逐帧复现:单 `staysrc` false / 单 `bagsalve` false / **成对 true** / 单 `staybag` **true**。

## 4. 域价钱

前置数由独立前缀行走复算,与 §EY / §FA **逐位对上**:
**turbo 1012/1012、走到补给子句 125、被否决 112**。
本杠杆域:**15 帧背包里有大药 → 11 帧在 0.18–0.75 带外 → 余 4 帧里 2 帧在到达补给子句前
就被 1200 环或追击子句否决** ⇒ **域内恰好 2 帧**。
两路交叉核对(驱动 `flips` == 前缀桶 `blocked_with_bag`);`flip_true_to_false` **0**;
`ship_true 13 → arm_true 15`;`bag_with_main_src == 0`(**不相交的根在语料不在杠杆**)。

**钉帧**:`f_260822_182012_sb_backpack_rescue_372`,**spirit_breaker 499/1378 = 36.2% 血**,
六个主格位全是非消耗品,**背包格位 6 是 `item_flask`**,3 秒内无英雄伤害,1200 环内无人,
最近敌人 1,307u。第二帧:`f_260822_063559_slardar_tp_forward`,lion 67.9%。
**四条负对照**:带内但被更早子句否决 ×2(1200 环里有人 / 3 秒内被打过)、
带外两侧 ×2(1.0 在上限外;**0.049 在 0.18 地板之下 —— 真正的逃命撤退那一侧**)。

## 5. 为什么只认大药一件(论据有锚)

`TrySwapInvItemForFlask()`(`mode_team_roam_generic.lua:1889)是 **SHIPPED 且无 gate**,
每帧从 `ItemOpsDesire` 跑,把背包里的大药换进主格位;
tango / tango_single / faerie_fire / bottle **没有**任何 shipped 换位器。
三条都从源码解析,变异体 **M18**(改名换位器)与 **M14**(改读背包 tango)分别打这两半。

## 6. ⚠️ 自伤一处,而它比它修掉的东西更值钱

**M13 是我写错的,不是测试写松的。** 原声明 `caught`:「格位范围放宽到 0-8,
杠杆就悄悄兼了 `'staysrc'` 的活」。实测 **SURVIVED**。
按证据纪律第 2 条先怀疑断言 —— 断言是对的,**错的是模型**:
控制流只有在 `bHasRegen` 仍为 false 时才走到这一块,而 `bHasRegen` 的初值就是出厂那个
**已经扫过格位 0-5** 的读数 ⇒ 主格位里的大药**根本不可能出现在这里等着被找到**。
把变异打上去重跑 sweep:**25 个计数器、15 行 carrier、2 行 flip 逐字节相同**。
⇒ 变异台新增第四类 **`equivalent`**,与 `unmeasurable` **不许混为一谈**
(后者:这份语料见证不了;前者:没有差别可见证)。**两者都不是通过。**

⭐ 副产品:这条杠杆只有**半个物品栏**宽,和它只有**一件物品**宽是同一个理由 ——
**不是 gate 把它按在那里,是它能读的其它东西上面那几行已经答过了。**

### 6.1 ⚠️ 第二处自伤,而它只有把同一台变异台在一次**无关编辑**前后各跑一遍才看得见

`M5`(sweep 停止 strip 注释)第一次跑 **CAUGHT**,第二次跑 **SURVIVED** —— 而 M5 自己一个字没改。
真正在抓它的是隔壁那条**精确等值** `STAY_NIDS == 4`:不 strip 时,
注释里自己举的 `IsSoakCandidate('X') and IsSoakCandidate('Y')` 例子会把计数撑大。
而我把那条总数放宽成下限,是**对的且无关的**改动(见 §7 第二条:这个函数的 id 总数
已经被钉成 `== 1`、`== 3`、`== 4` 三次,每次都被下一条杠杆撞红)——
**放宽的同时把那个变异体唯一的检测器一起带走了。**

⇒ **一个只在隔壁某个数恰好是等值时才有效的守卫,不是守卫。**
sweep 现在直接吐 `STAY_STRIPPED` / `SRC_STRIPPED`,测试直接断言「strip 真的发生了」;
单发复验:打上 M5 ⇒ `EXIT=1`,失败行正是那条新断言。
**这条之所以值得写下来,是因为它在任何一次单独运行里都看不见** ——
要在一次无关编辑前后各跑一遍同一台台子才会浮出来,而没有人会去安排这件事。

## 7. 顺手修掉的**三**条棘轮(同一个缺陷族,一天之内三次,都是本组自己埋的)

**这三条本身就是一条读数**:同一个家族在同一天、同一批文件里复发了三次,
每一次的形状都是**「拿一个跨语料/跨注释的原始计数,去顶一个结构性主张」**。

- **GH #538(认领并修掉)**:`tests/test_staybottle_inflight_regen.lua` 的
  `C('fixtures') == 109` / `C('live') == 1012` 两条**等值钉语料规模**,改用
  `cs.corpus` / `cs.ratchet`(GH #106 / #127 缺陷族)。兄弟文件
  `test_staysrc_field_supply.lua:339` 一直就是这么写的。
- **同一族的第二次复发,本轮当场撞上**:同一个文件的 `STAY_NIDS == 3`
  被本轮第四条杠杆撞红,**而树上没有任何缺陷**。改为 `>= 3`(自己的新文件同样写
  `>= 4`),理由抄自 `test_stayattr_global_ult.lua:155` —— 那里 `'staysrc'` 把
  `== 1` 撞红时**已经把这个道理写下来过一次**了:**总数是代理量,
  真正管 `pullcad` 陷阱的是 `STAY_IDS_MAX_PER_COND == 1`。**
- **第三次,在定向复跑里当场撞上**:同一个文件的
  `[wiring]` 断言把 `'staybottle'` 在**三个文件的原始源码**里数一遍并要求 `== 1`。
  本轮杠杆的注释里写了一句「Appended AFTER the 'staybottle' block」——
  **树上依然只有一个 arming point,断言却红了**。
  那条断言想说的是「**一个 arming point**」,而原始文本计数说不出这件事。
  已改为**先 strip 注释再数**(自己的新文件同一条断言同样改掉,不给下一条杠杆留同一个坑)。

## 8. 产物

- `bots/FunLib/jmz_func.lua`:`J.ShouldStayAndRegen` **追加**(不是插入)一个 gated 块
  `staybag`(turbo-only,**未 armed**,STANDALONE),位置在 `'staybottle'` 块之后
  ⇒ 未 armed 时两个兄弟杠杆求值**逐字节不变**(`STAY_ORDER_OK` 钉住);
- `tests/test_staybag_backpack_salve.lua`(**23/23**)、`tests/_staybag_sweep.lua`;
- `tools/agent/mutstand_staybag.sh`:**20 发全部如声明 —— 16 CAUGHT + 2 UNMEASURABLE(M11b / M17)
  + 1 EQUIVALENT(M13)+ 对照 M15 SURVIVED,零 NO-OP,零非声明存活,STAND GREEN,EXIT=0**;
- `iterations/state.json:staybag_20260905`、`iterations/streams/test_set.md §FC`、
  `tests/test_gated_helper_nesting_census.lua` 两行、
  `tests/test_staybottle_inflight_regen.lua` 三条棘轮修正。

**armed 串一字未动、`queue.json` 一字未动(P4.2 入集冻结)**;
**零 AWS、零 S3、零 EC2、零波次。**

## 9. 门与自检

- **铁律 6 静态门**:`bash tools/agent/luacheck_gate.sh` → 见 §9.1;
- **动态半(定向)**:`lua5.1 tests/run_tests.lua staybag_backpack_salve` → **23 tests, 0 failures**;
  另跑 `test_staybottle_inflight_regen` / `test_staysrc_field_supply` /
  `test_stayattr_global_ult` / `test_gated_helper_nesting_census` → 见 §9.1。
  **全量套件 ~100min(GH #124)未跑,本轮不声称它绿。**
- **开工自检**(铁律 10):`bash tools/agent/routine_selfcheck.sh > 文件 2>&1; echo $?` ⇒ 见 §9.1。
  ⚠️ 首次运行时**与本轮的变异台并发**,Lua 腿因此读到**半份 `jmz_func.lua`**
  (`:10275: '<name>' expected near '<eof>'`)—— 那正是 **GH #507** 立案的撕裂窗口,
  **不是 trunk 的红**;已在树静止后重跑,读数以重跑为准。

### 9.1 读数

**铁律 6 静态半**(`bash tools/agent/luacheck_gate.sh`,重定向读真码):
```
installed luacheck (it was missing; apt package lua-check, bounded)
luacheck bots game: 0 warnings
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
```
⇒ **GATE_EXIT=0 / 0 警告**。(容器冷启,gate 自己把 `lua-check` 装上 —— GH #205 那条。)

**铁律 6 动态半(定向,非全量)**,每条都是 `run_tests.lua <filter>` 的真码:

| 文件 | 读数 | EXIT |
|---|---|---|
| `test_staybag_backpack_salve`(新) | 23 tests, 0 failures | 0 |
| `test_staybottle_inflight_regen` | 21 tests, 0 failures | 0 |
| `test_staysrc_field_supply` | 12 tests, 0 failures | 0 |
| `test_stayattr_global_ult` | 11 tests, 0 failures | 0 |
| `test_gated_helper_nesting_census` | 10 tests, 0 failures | 0 |
| `test_gate_claim_consistency` | 16 tests, 0 failures | 0 |

⚠️ **全量套件(~100min,GH #124)未跑,本轮不声称它绿。**

**变异台**:`bash tools/agent/mutstand_staybag.sh` ⇒
`mutants run: 20 landed as declared: 17 unmeasurable+equivalent: 3 NOT as declared: 0` / **STAND GREEN** /
**EXIT=0**(16 CAUGHT + 2 UNMEASURABLE + 1 EQUIVALENT + 1 对照 SURVIVED;零 NO-OP)。

**开工自检(铁律 10)**,树静止后重跑,重定向读真码 ⇒ **SELFCHECK_EXIT=3**:

- `FINDINGS (exit 3)`:cadence / queue-rulings / owed-executions / **trunk-red(python)**;
  `UNCERTIFIABLE (exit 2)`:**none**。
- ⭐ **`trunk health (fast Lua detectors)` 回到 `84 tagged detector file(s), 0 failures`**
  —— 那正是 **GH #538 自己写的验收条件**,本轮达成(边界照抄它自己那句:
  该腿自陈 **FAST SUBSET, not the full suite**,它绿**不等于**全量套件绿)。
- python 腿 **7 failed + 1 uncertifiable**:其中 5 条
  (`test_bots_walk_farm_only` / `test_detector_source_constants` / `test_lua_corpus_stability` /
  `test_mutstand_restore_trap` / `test_wkqdmg_domain`)与本组上一轮记的**是同一组**,不认领;
  新增两条 `test_wave_gate_keys` / `test_wave_throttle` 的失败正文点名的是
  **W49 波次记录**(`W49 machine 0 (seed 5404) has no launched_at`)—— **批测台的腿**,
  本轮没碰任何 wave 文件。本轮新增的两个变异台检查项在同一份日志里是 `ok`(第 422–426 行)。
- ⚠️ **首跑作废并说明原因**:第一次自检**与本轮变异台并发**,Lua 腿读到**半份 `jmz_func.lua`**
  (`:10275: '<name>' expected near '<eof>'`)—— **GH #507** 立案的那个撕裂窗口,
  **不是 trunk 的红**;以上读数全部来自树静止后的重跑。
- ⚠️ 自检**首次调用被工具自己拒绝**(stdout 是管道 ⇒ `SELFCHECK_EXIT=2 REFUSED`,
  证据纪律第 3 条;工具自陈该形状**已复发 5 次**,本轮是第 6 次)。改为重定向到文件后读 `$?`。

## 10. 交棒

- **(甲) 总监**:按 P4.2 冻结把 `'staybag'` 记作 **FROZEN-HOLD**;
  并请单独收下 §2 作为 **GH #532** 那条约束的**第二种形态**;
  建议给 nesting census 的 **(A) 分类补一句限定**:(A) 保证的是
  「arming 外层 id 仍然量到外层 id」,**不保证**「内层 id 的行为对任何单臂波可达」。
- **(乙) 批测台**:解冻后本 id 可**单臂**发波(与 `staysrc` 重叠实测 0)。
  另:**`'bagsalve'` 单臂发波在本语料上按构造读不出任何东西**(`flips_bagsalve == 0`),
  若历史上给它发过单臂波,那一波的零应按「域未到达」而非「已测试且中性」重读(GH #531 同族)。
- **(丙) 录像组 / 语料侧**:欠两张帧 —— (i) **主格位带充能 bottle**(§FA 起就欠着);
  (ii) **背包带大药且主格位也有补给**的帧,有了它 M17 才能从 UNMEASURABLE 改判为 caught。
- **(丁) 下一轮本组自己**:P2 决策侧继续 —— **112 里那 68 个「什么都没带」的帧,
  `fieldbuy` 为何没买上**(`IsFieldRegenSituation` 的 0.55 上限**严于**本函数的 0.75)。
  **照旧先跑域价钱,并先断言控制帧够得着被控制的代码。**

## 11. Token 用量

```
TOKENS total_in=29,883,428 out=118,138 turns=144
```
(`python3 tools/agent/token_usage.py`,统计时刻之后的收尾回合不计入。)
