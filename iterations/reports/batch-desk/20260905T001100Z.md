# 批测台 2026-09-05T00:11–00:4xZ

**本轮一句话:补跑 W47 缺的两粒(4723/4763)把 62-id 家族做到 8 粒 —— 但真正的产出不是那两台机器,
是围栏那个数在 9 月 1 日就已经失去了它自己的推导,而它失效的方向是「可以多花」。**

---

## 一、自检(铁律 10)

⚠️ **开工第一条命令又误用管道**(`| tail -60`),脚本自己拒绝并打出
「REFUSED: stdout is a pipe; exit 2, nothing checked … 已复发 5 次,每次都是本轮第一条命令」。
**这是第 6 次,而且仍然是本轮第一条命令。** 立刻改重定向重跑。
⚠️ 第二次运行 `timeout 300` **超时(EXIT=124)**;第三次放到后台、`timeout 900`。
本节的 `SELFCHECK_EXIT` 见文末「自检收尾」——**没跑完的腿不声称绿**。

已读到的腿(逐项裸读,不手工归因,GH #267):

- `owed executions`:2 行。`campgrade_wave_type_premise_reading`(#480/#241)**executor 含本台**,
  `campgrade` 独占波**仍不存在**,本轮不是它 ⇒ **这一棒本台仍欠着**(连续第 N 轮)。
  `fixture_mock_extrapolated_location`(#492)executor=总监,不是本台。
- `expired admission waits`:6 份章程 / 75 个已裁 id,**无过期等待**。
- `stable version anchors`(铁律 3):`stable-v1` / `stable-v2` 两个锚点 **EXISTS/PINNED/SHIPPED 全 ok**。
- `promote-time constraints`(pullcad 陷阱):141 个活 gate id、13 个带 PROMOTED 注,
  **FROZEN none**;唯一登记的 co-promote atom `field_hold_needs_magnitude` 三个 id 全 GATED ⇒ OK。
- `trunk health (python)`:**本节收尾时补**(见文末)。上一轮该腿报了两条红
  (`test_detector_source_constants.py` #490、`test_bots_walk_farm_only.py` #497),**本轮不预判它们已修**。

## 二、成本与围栏(闸 (iii))

`COSTS_EXIT=0`。九月 MTD **`$17.773`**(免费 `budgets/dota2bot-batch`,
**`refreshed 2026-09-04T22:42:52Z`**),`running/pending` **空**,常驻仍只有 AMI
(`ami-0a990a26d89c66547`)。

**滞后判定(甲)**:快照 22:42:52Z 距 W47 起飞(18:31Z)**4.19h**、距末台自毁(19:27Z)**3.26h**,
**落在 4.3–11.3h 滞后带的下缘之内** ⇒ **按构造不能假定含 W47**。
⛔ 按 (乙) **不做「MTD 增量对得上波费 ⇒ 已计入」的对账**(该手法已两次给出错误结论)。

围栏算术(逐波按其自身市场类型取价):

```
17.773  MTD(budgets,可能不含 W46/W47)
+ 1.02  W46(spot,已结)
+ 0.7413 W47(spot,W47 轮已结的账单侧实测,不用常数)
+ 1.10  本轮两台按需补跑(2 × ~$0.55,W45 同型补跑实测)
= $20.63
```

对 **$50 / $80 / $90 / $100** 四条线 ⇒ **全部满足**,本轮发波在钱这一侧无阻。

### ⭐⭐⭐ 二·甲:围栏那个数在 9 月 1 日失去了推导,而且是往"可以多花"的方向

**这是本轮的头号产出,请总监下一次触发必裁。**

章程闸 (iii) 在 2026-08-28 被总监**重写过一次,而重写的要点恰恰是取消这个数的自由度**:

> ⇒ **新写法:围栏 = 下一个尚未跨过的 owner 可见 Budget ACTUAL 告警档。**
> `dota2bot-batch` 的告警在限额 $100 的 50/80/100%,即 **$50 / $80 / $100**;
> $50 已跨(书面解释见 08-27T15:15Z 报告 §2),**下一档就是 $80**。
> … **当月唯一的操作数是上面的 $80。**

三件事本轮**从 AWS 自己读出来**(`budgets describe-budgets`,免费):

1. `"TimeUnit": "MONTHLY"`、`"BudgetLimit": 100.0`、`"ActualSpend": 17.773`。
   **MONTHLY ⇒ ACTUAL 告警档每月重新上膛。**
2. 九月 MTD **$17.773 < $50** ⇒ **九月的 $50 档一次都没跨过**。
3. 于是按那条规则**自己的文字**:「下一个**尚未跨过**的 owner 可见告警档」
   在九月是 **$50**,**不是 $80**。

而 $80 那个数**是 8 月的推导结果**,原文自己写明「**当月**唯一的操作数」——
「当月」是八月。**9 月 1 日 MTD 归零的那一刻,$80 就不再是那条规则的输出了,
但没有任何东西重算它**:九月至今每一轮报告(含上一轮 W47)都照抄 `$45/$80/$90/$100` 这一行。

⛔ **失效方向是危险的那一侧**,而且与本章程反复点名的那一族同型:

- 它**多给额度**($50→$80 这一段共 $30),不少给;
- 它**不报错、不缺读数**,那一行看上去和以前每一轮逐字相同;
- 它让本台可以**一路发波越过一封 owner 会收到的告警邮件**,而那封邮件正是
  08-28 那次立法**唯一的立法目的**(「恰好停在 owner 会收到一封他没有解释的邮件之前」)。

**本轮不受影响**(围栏值 $20.63 对 $50 也满足),**所以这条不是靠疼被发现的** —— 这正是要现在裁的理由:
按下面的燃烧率,它会在**九月中旬**第一次真正咬人,而那时它已经默默生效两周。

### 二·乙:燃烧率把 owner 的批准线顶穿了(独立于上一条)

- AWS **自己的** forecast:**`$119.286`** vs `BudgetLimit $100.0` ⇒ **AWS 预测九月超 owner 批准线**。
- 本台独立算:$17.773 / 3.945 天(9-01T00:00Z → 快照 9-04T22:42Z)= **$4.51/天** ⇒ 30 天 **$135**。
- 两条独立路径**同向**:九月按当前节奏**收不住在 $100 内**。
- 按 $4.51/天 外推撞线时刻:**$50 ≈ 9-12**、**$80 ≈ 9-19**、**$90(刹车)≈ 9-21**。
  ⇒ **刹车线大约在九月下旬吃掉本台最后 10 天的全部例行波**。

**本台不自行裁定任何一条**(围栏值是总监的地界,$90/$100 是 owner 的地界)。
本轮开 issue 交总监,见 §八。

## 三、收割:本轮无新数据(不是"没去看")

W47 已在上一轮(21:1xZ)收割完毕。本轮复查 S3 `soak/` 前缀:
最新对象 **`…_9782b8/20260904_191221_slot13.analysis.json` @ `2026-09-04T19:25:20Z`**,
**早于 W47 收割轮** ⇒ 自那之后**零新对象**,`validation/` 同样无新数据。
**本轮零收割,理由是没有新数据,不是没跑收割。**

## 四、发波:W47 补跑(不是新波)

**这是 W47 的 `rerun` 块,不是 W48。** 依据是章程步骤 5 的事先登记处置
(「一台被抢占 = 该种子缺臂,这一粒的配对差作废,不要整波作废;缺的那粒种子再发一台补跑」)。

### 四·甲 市场:两台都直接 `--on-demand`(GH #408,不是违反 #158)

判据取**那一台自己的 SIR `Status.Code`**,不取印象:

| 种子 | W47 SIR 码 | GH #408 处置 |
|---|---|---|
| 4723 | `instance-terminated-no-capacity` | ⇒ 补跑**直接 `--on-demand`**,跳过 (1)(2)(3) 阶梯 |
| 4763 | `instance-terminated-no-capacity` | ⇒ 同上 |

两条围栏均成立:**每粒种子只补跑一次**(两粒各一次)、**每波最多 2 台按需补跑**(恰好 2 台)。

### 四·乙 闸 (i):上一轮交总监的问题**本轮不需要它的答案**

上一轮把「补跑受不受闸 (i) 管」交棒总监(GH #498),**至本轮 00:1xZ `issue_read get_comments` 返回 `[]`,无裁定**。
⇒ 按上一轮预登记的**保守默认(当作受管)**执行:**等到解锁再发**,
于是**两种读法下这次发波都合法**,这个问题在本轮被绕过而不是被赌掉。

⭐ **顺带给 #498 交一份它缺的证据(本台自己的记录,不是新推理)** —— 三条都指向"不受管":

1. **章程条文**:「**登记在案的补跑机(`rerun` 块)不开新窗口**(W40/W42 先例,从未被质疑)。
   这条按 schema **结构性**成立:工具只读 `machines[]`,不读 `rerun`。」
2. **W45 先例是可量的**:W45 slate 06:30:52Z,其两台补跑机 **07:12:53Z / 07:12:58Z 起飞 = +42min**。
   **若补跑受闸 (i) 管,W45 那一轮就是一次未被记录的突破**;而 `W45_wave.json:rerun_gate_i_note`
   当轮就把它写成了合规。
3. **W45 的成本注记已经给出定性**:「the disposition itself was PRE-REGISTERED in the charter …
   **a standing authorization executed, not a new spending decision**」。

⇒ 本台**不自裁**,但请总监按这三条裁,而不是重新猜。**裁定前本台继续走保守默认(等解锁)。**

### 四·丙 树:补跑测的是 W47 那棵树,不是"现在的 main"

- **闸前 00:15Z** `git ls-remote origin main` = **`e3b24e54…`** = 当时的本地 HEAD(一致)。
  ⚠️ **这个值在发波瞬间已经不是它了 —— 见 §五·甲。**
- W47 树 `15b0d9fd…` **是 origin/main 的祖先**(`git merge-base --is-ancestor` **exit 0**),实例 checkout 得到。
- **`git diff --stat 15b0d9fd..origin/main -- bots/ game/` 输出为空** ⇒ `bots/`+`game/` **逐字节相同**
  (其间的 commit 全在 `iterations/` 等非行为路径)。**闸前与发波后各验一次,两次都空**(§五·甲)。
- 仍显式 `--ref 15b0d9fd…` 钉树:**行为等价时也钉,因为要声称的是「这是 W47 的补跑」而不是「差不多」**。
  **本轮这不是形式主义 —— 等待期间 main 真的动了(§五·甲)。**

### 四·丁 臂串:**本轮重算,不抄**

`sed -n '2p' iterations/streams/test_set.md` 现场重算:
**`ids 62` / `bytes 550` / `md5 c7e1f92c739f8f8dc0ee0c9484288628`**
⇒ 与 W46/W47 登记值**逐字节相同**,补跑与被补的波同臂。
`check_armed_wiring.py --cand <ARM>`:**`ARM_EXIT=0`,`all 62 armed ids wired on HEAD`**。
⚠️ 登记一个踩到的坑:该工具**要 `--cand`**,位置参数会 **exit 2**(「没跑成不是通过」)。

### 四·戊 闸 (iv) 与闸 (ii)

- 闸 (iv) `reclaim_blind.py --wave-json W47_wave.json`:**`RB_EXIT=0`**,
  `yield 2 paired seed(s) of 4`、`attribution 2 machine(s) reclaimed before the flip`、
  `VERDICT: not blinded`、`NEXT WAVE: spot (the default; GH #158 unchanged)`。
  ⚠️ 那句 `NEXT WAVE: spot` **管的是下一个新波,不是本轮的补跑机**;补跑机的市场由 GH #408 单独裁定(§四·甲)。
  ⚠️ **GH #499 那个标签缺陷本轮复现**:4723 行仍打 `[ab/ba only (no arm_depth given)]` 而 `arm_depth` 给了 `0.0`
  —— 分类对、打印的理由假,issue 已在上一轮开出,本轮**只登记不再重开**。
- 闸 (ii):62-id 家族当前 **6 粒计分种子**(W46 四粒 + W47 两粒)**< 8** ⇒ **满足**。
  补跑落地后才到 8 粒,`GH #363/#449` 的并池分叉才有意义。
- `seed_roster_index.py --build`:**`ROSTER_EXIT=0`**(增量,秒级)。
  ⚠️ 本轮**不选种**:补跑按定义打的是**原来那两粒**(4723/4763),不是新抽。

### 四·己 闸 (i):在**动作时点**跑,不是决定时点

`wave_throttle.py` 作为 `spot_run.sh` 之前的**最后一条命令**跑在发波脚本里(GH #469,
「三次突破 W12 差 21s / W20 差 3m31s / W44 差 3m00–4m19s 是同一个机制:钟只读了一次」):

```
anchor wave      : W47 (4 machine(s))
last machine up  : 2026-09-04T18:32:27Z  <- the anchor (GH #469 ruling 1)
slate spread     : 39s
unlock           : 2026-09-05T00:32:27Z
now              : 2026-09-05T00:32:46Z
margin           : +19s past unlock
WAVE_THROTTLE: UNLOCKED (exit 0)
```

**`WT_EXIT=0`,读的是工具打的那一行,不是章程里手打的数。**
本轮 00:12Z 第一次跑该工具时是 `WT_EXIT=3`(`1226s SHORT`),**发波脚本自带 `if WT != 0 then abort`**,
所以「等到解锁」不靠本会话记得,靠脚本。

## 五、发波结果:两台按需补跑全部起飞

`LAUNCH_SCRIPT_EXIT=0`,两台 `RERUN_EXIT=0`。

| 种子 | instance | run_id 尾 | 请求 AZ | 实得 AZ | reaimed | LaunchTime |
|---|---|---|---|---|---|---|
| 4723 | `i-07dea406c173a8742` | `…_b34547` | us-west-2b | **us-west-2b** | False | `2026-09-05T00:32:48Z` |
| 4763 | `i-0bc15fcc426fe50a0` | `…_695907` | us-west-2c | **us-west-2c** | False | `2026-09-05T00:32:53Z` |

**市场核验(GH #408 三样登记的第一样)**:`describe-instances` 的 `InstanceLifecycle`
两台都是 **`None`** ⇒ **确认按需**。
⚠️ **`run_id` 的 `spot_` 前缀是 `spot_run.sh` 硬写的字符串,与市场类型无关,不可当证据** —— 证据是上面那个 `None`。

**GH #252 验收(本对)**:**两个不同 AZ**、`requested == actual` 两台都成立、
**零环内改投、零阶梯降级**、零 `InsufficientInstanceCapacity`。
⇒ 与 W47 本体形成对照:W47 是 4 台 3 个 AZ + 一次两跳改投,本对是 2 台 2 个 AZ + 零改投。

**其余两样登记(存活分钟、该台增量账单)按构造要到停机后才有** ——
两台 2h 看门狗、**预计 ~02:32Z 自毁**,下一轮收割时填进 `W47_wave.json:rerun[]`。
⚠️ **`status_code` 有一个按构造会关上的窗口**:按需机**没有 SIR**,唯一可能的码是 EC2 `StateReason.Code`,
而它 **~1h 后过期(GH #375)**。⇒ **下一轮必须在 ~03:32Z 之前读**,否则这两行又会像 W45 的补跑行一样
永久记成 `UNREADABLE`。**这条已写进 `rerun[].status_code_note`,不靠谁记得。**

### 五·甲 ⚠️ 等待期间 `origin/main` 动了 —— 钉树这件事本轮不是防御性的

- 00:15Z 闸前读:`git ls-remote origin main` = **`e3b24e54`**
- 00:32Z 发波瞬间读:**`ba6d2961`**(总监在这 20 分钟里推了 `b4d5f01f` + `ba6d2961`)

**若本轮没有 `--ref` 钉树,两台机器 clone 到的会是一棵与闸前核对过的树不同的树。**
(这正是 2026-07-23 那次险情的形状,章程步骤 5 第一句就是为它写的。)

本轮实际影响 **为零,而且是量出来的不是猜的**:
`git diff --stat <树>..origin/main -- bots/ game/` 对
`15b0d9fd..e3b24e54` 与 `15b0d9fd..ba6d2961` **两次都输出空** ⇒ 三棵树的 `bots/`+`game/` **逐字节相同**
(两个新 commit 全在 `iterations/`)。且 `15b0d9fd` 是 `origin/main` 的祖先
(`git merge-base --is-ancestor` **exit 0**),实例 checkout 得到。

## 六、泄漏检查:干净

`LEAK_EXIT=0`。`running/pending` **恰好是本轮启动的那两台**
(`i-07dea406c173a8742` / `i-0bc15fcc426fe50a0`,均 `dota2bot-soak-od-1`),
**没有第三台、没有该关没关的**;`describe-volumes --filters status=available` **无游离卷**;
常驻仍只有 AMI `ami-0a990a26d89c66547`。
两台均 `--instance-initiated-shutdown-behavior terminate` + 2h 看门狗,**有自毁路径**。

## 七、队列

`QUEUE_ROWS_CHANGED=11`,diff **11 插入 / 11 删除**(= 11 行 `rides_wave` 各改一行,
**没有把真实改动埋进重排**;写回前 `indent=2` round-trip 验证,改前改后各一次)。
受影响 id:`strategy-10/13/14/15/16/18/20/21/40/41` + `hero-15`。

**`status` 与 `last_wave` 一律未动**,理由写在行里:**机器还在飞、什么都没收割**,
condition (a) 的核验仍是录像组的棒且仍欠着。本轮只把「W47 缺的两粒正在补跑」写进 `rides_wave`。

⭐ **本轮在发波轮就动队列是刻意的**:上一轮记下「这 11 行在本轮之前已经落后两波,
而两个发波轮各记『队列零翻动』,中间没有任何东西举手」。**发波轮不碰队列正是那条掉棒的机制**,
所以本轮把它改掉,并把这句理由写进了行内。

## 八、自检收尾(铁律 10 的 `SELFCHECK_EXIT`)

`SELFCHECK_EXIT=0`(后台跑完,`timeout 900`,**重定向不经管道**)。
⚠️ **但 exit 0 不等于全绿**:python 腿自己打了 `TRUNK RED`,并附带告诫
「Whether main is red too is NOT established by this line: re-run after `git stash`」。

```
91 passed, 3 failed, 1 uncertifiable
failed: tests/test_bots_walk_farm_only.py tests/test_detector_source_constants.py tests/test_mutstand_restore_trap.py
```

⭐ **照那句告诫做了,没停在"工作树红"上**:把本轮唯一的未跟踪文件(本报告)移开,
`git status --porcelain` 只剩 `M iterations/data/seed_roster_index.json`(本轮 `--build` 产生的数据文件,
与这三个测试无关),逐个单跑:

| 测试 | 干净树单跑 | 判定 |
|---|---|---|
| `test_mutstand_restore_trap.py` | `MUT_CLEAN_EXIT=1` | **红在 trunk**,不是本会话弄红的 |
| `test_bots_walk_farm_only.py` | `WALK_CLEAN_EXIT=1` | **红在 trunk**(GH #497,第二轮仍红) |
| `test_detector_source_constants.py` | `DET_CLEAN_EXIT=1` | **红在 trunk**(GH #490,**第三轮**仍红) |

**⭐ 第三条是新的,而且上一轮不可能看到它**:`test_mutstand_restore_trap.py` 挂在
`tools/agent/mutstand_campbind_poke.sh` 上,该文件由 **`6d8522d2`(录像检查组 2026-09-04T21:59:41Z)** 引入,
而上一轮批测台自检跑在 **21:1xZ** —— **它落地时那一轮已经过去了。不是谁漏看。**

⚠️⚠️ **那条 FAIL 打出来的理由,实测是假的。** 检查项打:

```
FAIL tools/agent/mutstand_campbind_poke.sh traps a function that exists (cp)  [the trap fires and does nothing]
```

该脚本的 trap 是**内联**的 `trap 'cp "$SAFE/orig.py" "$SRC"; rm -rf "$SAFE"' EXIT`。
**按证据纪律,不靠读源码下结论 —— 建了同形状最小复现台:**

```
during run: MUTATED
after exit:  ORIGINAL      <- 内联 trap 确实还原了
```

⇒ **分类可能对(它确实不合本仓库"trap 一个具名函数"的约定),但「什么都没做」是个假的事实断言。**
这与 **GH #499** 同族:*后果不是读数错,是审计错。* 已开 issue,见 §九。

## 九、本轮开出的 issue

**按 GH #290 的顺序执行,本轮未复发那个程序错**:先 `git push`(`a109e80b` 落到 `main`),
**再**发表这四件带引用的产物。四份草稿逐个过
`bash tools/agent/claim_precheck.sh`:**四个 `PRECHECK_EXIT=0`**
(三份 `clean / OK to publish: every citation resolves on origin/main`;
`#505` 那份是 `NO CITATIONS -- nothing to certify, nothing to strand. OK to publish.`
—— ⭐ 该工具**对"零引用"是 REFUSE 而不是静默放行**,理由写得很好:
「'nothing bad' and 'nothing matched' are the same output, so this refuses」)。

| # | 类型 | 内容 |
|---|---|---|
| **#504** | `[batch]` | **围栏常数 `$80` 在 9-01 失去了推导**:闸 (iii) 的规则是「下一个**尚未跨过**的 owner 可见 Budget ACTUAL 告警档」,budget 是 `TimeUnit=MONTHLY`,九月 MTD `$17.773` **未跨 `$50`** ⇒ 九月的围栏按规则是 **`$50`** 不是 `$80`。失效方向**多给 $30**。请总监裁,并建议把它变成 `check_costs.sh` 当场算的读数(与 GH #469(乙) 对闸 (i) 同一味药)。 |
| **#505** | `[batch]` | **九月燃烧率顶穿 owner 批准线**:AWS 自己的 forecast **`$119.286`** vs limit `$100`;本台独算 `$4.51/天 ⇒ $135/月`。**两条独立路径同向。** 按此外推 **`$90` 刹车线 ≈ 9-21** ⇒ 九月最后 ~10 天例行波被整段吃掉。根因是结构性的:闸 (i) 的 6h 窗口(≤4 波/天)与当前抢占率下的单波价不相容。请总监裁「现在降节奏」还是「撞线再说」。 |
| **#506** | `[bug]` | **trunk 第三条红 `test_mutstand_restore_trap.py`**(§八),含**实测证明那句 `[the trap fires and does nothing]` 是假的**,并给出两条互斥的修法。 |
| **#498 追评** | — | 交上闸 (i) 与补跑关系的**三条既有证据**(章程条文、W45 +42min 先例可量、W45 成本注记的定性),请总监**按证据裁不要重猜**;并说明本轮已用"等解锁"绕过,两种读法下都合法。 |

**本轮不开的**:GH #499(上一轮已开,本轮只复现登记)、#490/#497(已有 issue,本轮只记"仍红")。

## 十、局数(owner 2026-08-19 要求)

- **本轮收割局数:0**(S3 自 W47 收割后零新对象,§三)。
- **本轮启动局数(预期)**:2 台 × 12 局/种子 × 2 腿 = **~48 计分局**,外加每台约 6 局热身(不计分)。
  ⚠️ **这是预期不是读数**;W47 本体 4 台实得 128 计分局(热身每台恰好 6 局)。
- **62-id 家族累计计分种子**:目前 **6 粒**(W46 四粒 + W47 的 4678/4950);
  这两粒落地后到 **8 粒**,`GH #363/#449` 的并池分叉才有意义(**第七次出现,仍未裁**)。

## 十一、铁律 6 / 11

- **铁律 6 静态半**:`GATE_EXIT=0 CLEAN`,`luacheck bots game: 0 warnings`
  (容器缺 luacheck 由门**自己装上**,apt 包名 `lua-check`)。
  **未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行要抄。**
  推送门已按铁律 10 上膛:`arm_push_gate.sh` ⇒ `ARM_HOOK_EXIT=0`,`core.hooksPath=.githooks`。
- **铁律 6 动态半**(`lua5.1 tests/run_tests.lua`,~100min,GH #124):**未跑,不声称**。
  本会话 **`bots/`/`game/` 一行未改**(改动全在 `iterations/`)。
- `tests/test_wave_gate_keys.py` 对改后的 W47 记录**当场跑过**:`WGK_EXIT=0`,**241 checks 0 failed**
  (改前也跑了一次基线,同样 241/0)。
- **铁律 11**:本轮**未触发**任何 `requires approval`。**无空转等待** —— 闸 (i) 那 20 分钟里
  完成了自检 python 腿、臂串重算、wiring 门、闸 (iv)、roster build、成本/预算取数、
  三份 issue 草稿与本报告主体,发波由**定时脚本**在解锁瞬间执行。

## 十二、Token 用量

```
TOKENS total_in=8,240,848 out=63,826 turns=67
```

(统计时刻之后的收尾回合不计入,当量级参考。)
