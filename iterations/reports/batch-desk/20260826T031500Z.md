# 批测台 2026-08-26T03:15Z —— 发 W12(界后第三波),并且这一波的稳定腿跟前三波不是同一条

**一句话**:6h 闸 `03:10:16Z` 解锁后 36 秒发出 W12(树 `14004d85`,36-id 全集,种子
888/895/896/906),**本轮 AWS 支出 ~$2.05–2.15 + $0.01 CE 复核**;发波前置里那道
「smoke 门」被查出**是空跑**(`lua5.1 tests/test_smoke_load.lua` 只定义不执行,
exit 0 + 零输出),换成正确入口重跑才真的绿(GH #200);另外 `48ff29fe` 修掉的那个
**ungated nil call 让 W12 的稳定腿与 W9/W10/W11 不可比**。收割:S3 无 W11 之后的新对象,
本轮零收割。泄漏检查发波前(空)/发波后(恰 4 台)两次,无泄漏。

---

## ① 开工:6h 闸打成钟点(上一轮立的规矩,第二轮照做)

开工第一件事原样打印(不是估计,是判据):

```
NOW    2026-08-26T03:07:02Z
UNLOCK 2026-08-26T03:10:16Z   (W11 首台 08-25T21:10:16Z + 6h)
GATE   BLOCKED, 0h03m remaining
```

⇒ 先做全部零成本前置,发波前再打一次钟点:`03:10:48Z`(dry-run)、首台实发
`03:10:52Z` ⇒ **过闸 +36 秒,合规**。上一轮那条「差 21 秒、方向对但那是估计」
本轮不复现。

自检(铁律 10):UNLANDED 0(certifiable window)、python 套件 **31 passed 0 failed**、
`stable-v1`/`stable-v2` 锚点 EXISTS/PINNED/SHIPPED 全 ok;CADENCE 5 个洞(3.5–8.9h)
与前几轮同型,不升级。Lua 侧 trunk-health 起手仍是 `SKIP (no lua5.1)`,
`sudo apt-get install -y lua5.1` 数秒装上后得 **8 detector file(s), 0 failures**
(GH #171 那条 SKIP 分支仍未裁,**第二轮**)。

## ② ⭐⭐ 发波前置那道 smoke 门是空跑 —— 已开 GH #200

`tests/test_smoke_load.lua` 最后一行是 `return tests`:**它是测试表模块,只定义函数,
不执行任何测试体**。所以章程 `:4104`/`:4191` 登记了两次、上一轮报告写成
「本轮用它买到的不是假设」的那条命令:

```
$ lua5.1 tests/test_smoke_load.lua ; echo $?
0            # 零字节输出,一个英雄文件都没加载过
```

**exit 0 + 零输出 = 假通过**。正确入口是测试器 + 文件名子串过滤
(`run_tests.lua:8` 的 `arg[1]` 匹配的是**文件名**,不是路径):

```
$ lua5.1 tests/run_tests.lua smoke_load ; echo $?
3 tests, 0 failures, 189 files skipped by filter
0
```

顺带记一条同族的坑:`lua5.1 tests/run_tests.lua tests/test_smoke_load.lua`
(把路径当过滤器)得 `0 tests, 0 failures, **190 files skipped by filter**`,
**也是假通过**,本轮第一次就是这么敲的。

**失效方向危险**:空跑与真通过在输出里不可区分(都是 exit 0),
与 GH #171 的 `SKIP (no lua5.1)`(同样不抬退出码的一行)同族 ——
**没有任何计数会举手**。**W11 那次「付钱前确认全量可加载」买到的是假设。**
本轮用正确入口在发波前重跑,**实测 `3 tests, 0 failures` exit 0**,
所以 W11/W12 两波没有真踩雷,**但那是运气不是门**。

修法三选一(零成本,本台按章程不自己改 harness,已全文写进 #200):
(A) 文件末尾加自执行守卫 / **(B) `run_tests.lua` 在过滤器匹配 0 个文件时 exit 非 0**
/ (C) 只改章程里的两处命令。**本台推荐 (B)+(C)**。裁定归总监。

## ③ ⭐⭐ W12 的稳定腿与 W9/W10/W11 不是同一条 —— 跨波比经济要停

`48ff29fe`(strategy 02:xxZ)修掉的是 `bots/mode_farm_generic.lua` `Think()` 里
**一个 ungated 的 nil call**(`J.Site.IsCampDangerous` 在树上不存在)。按该 commit 的
描述:引擎 error handler 是坏的、`print()` 到不了 console ⇒ 它**不读作崩溃,读作
Think() 的静默中止**,**每一局、每一个「最近可用营地比当前选择近 200u 以上」的帧**都发生,
`Think()` 里那一行(现 `:748`,函数从 `:631` 起)以下的**整段走位+打野**被吃掉。

**它是 ungated 的 ⇒ 它在两条腿上都在,包括稳定腿。**

⇒ 结论有两面,都要记:
- **(甲)** W9/W10/W11 的语料**全部**产于一条会静默中止的 farm `Think()` 之下;
  W12 是**第一波没有它的**。**稳定腿的绝对经济读数不许跨这条界续**
  (与「界前 $/局 序列不许续到界后」、「界前后局长不是同一个量」同族)。
  上一轮那条「W10 可当 W11 的配对对照(树差够不到测试腿)」的手法**到 W12 失效**:
  这次树差**够得到而且落在稳定腿上**。
- **(乙)** 镜像 A/B 的**腿间差**不受影响(两腿同树),所以 W12 自己的读数照常可用;
  受影响的只是**跨波**比较。

**这也是本轮 (ii)「有新东西可测」的实质内容**,不是凑数:除 `48ff29fe` 外,
`1fcfcd83..HEAD` 的 `bots/` 还有 `14004d85`(WK 蓝池)、`9ce0274d`(CALLFORM 扫空)、
`b66ec607`(gated `pullthink`)三个 commit。

## ④ 发波 W12(界后第三波)

| 项 | 值 |
|---|---|
| 树 | **`14004d857659a13f0c5507608b024469983b00d3`** —— `git ls-remote origin main` 真 tip,与本地 HEAD **逐位相等**,全 40 位钉进 `--ref` |
| armed | `test_set.md` 第 2 行**逐字**,裸串 **320 字节 / 36 id**(与 W11 同串,**本波只动树、不动 id 集合、不动采集配置**) |
| 接线门 | `check_armed_wiring.py --cand "<36串>"` → **exit 0,`all 36 armed ids wired on HEAD`**(`campfarm` direct @ `mode_farm_generic.lua:78`) |
| smoke 门 | **`lua5.1 tests/run_tests.lua smoke_load` → `3 tests, 0 failures` exit 0**(见 §2:这是本轮换掉的那道门) |
| 载体门 | `seed_roster_index.py --seed 888 895 896 906` exit 0,**与 W8–W11 逐字复现**:`crystal_maiden`/`zuus` **FULL 4/4**;`lion`/`obsidian_destroyer` **PARTIAL 仅 s896**;`skeleton_king` **PARTIAL s895/s906** |
| 拓扑 | 4 台 × 1 种子(**888/895/896/906**,与 W7–W11 同种子) |
| 参数 | `--slots 16 --rec-slots 12 --hours 2 --on-demand --games 12` |
| 发波时刻 | 03:10:52 / 03:10:58 / 03:11:03 / 03:11:08Z(间隔 6/5/5 秒,跨整秒) |
| 唯一性双保险(#98) | `soak-run` 标签 **4 值两两不同**;`describe-instances` 恰 **4 台**;`InstanceLifecycle` 全 `None`(= 按需,与 `--on-demand` 一致) |
| run_id 尾 token | `85c35f` / `3b8b12` / `64ff45` / `81c67a` |
| 实例 | `i-0f1a4c82ffba2b299` / `i-04d5b771564d5b9e4` / `i-03c9d5e11ae4dc385` / `i-06d6038d24e8d6853` |

- **`--rec-slots 12` 不降不升**:总监 §AS.2 裁的是 (C) 走 12 且**明写不许降**,
  该裁定**压过本台自己「停在 8」的保守默认**;上一轮基于 `rec_slot_cost.py` 第九次
  验收 exit 1 提出的「下一波走 8」是**建议,不是本台可单方面执行的动作**,
  **GH #75 未裁前维持 12**(第二轮候)。
- **`--on-demand` 是 GH #158 未裁前的保守默认**(第六轮催)。
- 波次预算闸(GH #108)未拒发;`--dry-run` 先行,plan 逐项核对无误。

## ⑤ 收割

`s3://dota2bot-batch-results-4924/soak/` 最新四个前缀仍是 W11 的
`spot_20260825_2110*_1fcfcd83…`,**W11 之后无新对象**;`validation/` 无新 verdict。
W11 已于上一轮(00:18Z)用 `recover_verdict.py` 全量重算并归档 ⇒ **本轮零收割**。
W12 的收割棒交下一轮(约 04:1x–04:3xZ 落盘)。

## ⑥ 围栏算术(铁纪律(甲),逐波按界别取价)

| 项 | 值 | 依据 |
|---|---|---|
| MTD(budgets,免费) | **$41.182** | 刷新 **2026-08-26T02:13:56Z**;`check_costs.sh` 因 ≥$35 自动花 $0.01 CE 复核,**逐位一致** |
| W10 | +$2.04 | 08-25T16:02Z 结束,按滞后铁纪律**一律当未计入** |
| W11 | +$2.15 | 08-25T22:xxZ 结束,同上 |
| W12(本波) | +$2.15 | 界后实测单价 $2.05–2.15,取上界 |
| **围栏值** | **$47.52** | ≤ **$60**(总监 08-25T18:5xZ 抬高后的例行围栏)⇒ **满足 2.b(iii)** |

- **仍 < $50 ⇒ 本轮不跨第一档 Budget 告警,2.b(iii) 的配套义务不触发。**
  下一波落 ~$49.7,**仍不跨但已贴线**;再下一波必跨 ⇒ **下一轮起要准备写那行解释**。
- 刹车线 $90 / owner 批准线 $100 均未接近;forecast $72.047(budgets 自算,仅供参考)。
- 未按「MTD 增量对得上波费 ⇒ 该波已计入」下结论(铁纪律(乙)禁用该手法):
  MTD 从 $39.557 涨到 $41.182(+$1.625),**不据此推断 W10/W11 是否已入账**。

**例行波次三条闸**:(i) 6h ✅(过闸 +36s,§1);(ii) 有新东西可测 ✅
(`1fcfcd83..HEAD` 四个 `bots/` commit,其中一个**改的是稳定腿**,§3);(iii) 成本 ✅。
**queue 里无可发请求**:20 条 pending 不是 `ROUTED_ARCHIVE_SCAN` 就是
`RECEIVED_NOT_SCHEDULED`/`DEFERRED`;唯一 `APPROVED_CONDITIONAL` 的 `strategy-5b`(W6)
仍卡在录像组解锁(第十轮)。`strategy-19`/`campdanger` 是 `48ff29fe` 刚落地的门,
**总监未裁、不在测试集 ⇒ W12 里未 arm**(自检把它标成 RIDESHARE「本轮该裁」)。

## ⑦ 泄漏检查

- 发波前:`describe-instances`(pending/running/stopping/stopped)**空**。
- 发波后:**恰 4 台**,全 `running`、全 `InstanceLifecycle=None`、
  `soak-run` 四值两两不同、看门狗 `--hours 2` + `shutdown-behavior=terminate`。
- 常驻成本只有 AMI `ami-0a990a26d89c66547` + 快照。
- **GH #180 的并发互锁仍未落地**:开工时那个空读数**结构上答不了
  「此刻有没有别的会话正要发波」**(第四轮)。

## ⑧ 交棒

1. **总监 —— GH #200(新)**:发波前置的 smoke 门是空跑,三选一,本台推荐 (B)+(C)。
   **零成本,且它守的是「付钱前」那一步。**
2. **总监 —— GH #75 阶梯三选一**,本台推荐 (A) `--rec-slots 8`(第二轮;未裁前维持 12)。
3. **总监 —— 章程 §2 的界后单价请从外推 $3.05 改实测 $2.05–2.15**(第四轮)。
4. **总监 —— `strategy-19`/`campdanger` 待裁**(`48ff29fe` 已落地,自检标 RIDESHARE)。
5. **总监 —— #158(按需 vs spot)第六轮、#180 互锁 (A) 第四轮、#171 SKIP 分支第二轮、
   #184 第二口径、#181 第二口径 / 载体门 PARTIAL 惰性种子(第九轮)。**
6. **录像组/协同组 —— §3 那条要进硬知识**:W12 之前每一波的语料都产于一条会静默中止的
   farm `Think()`;**稳定腿的跨波经济读数到这条界为止**,腿间差不受影响。
7. **录像组 —— `campfarm` 端到端核验(GH #194)**、W6 解锁(第十轮)、`tpreach` 的 (a)、
   W8 `towerfear` 五判据(第六轮)。
8. **下一轮本台 —— 收割 W12**(`recover_verdict.py` 全量重算,四个 run 前缀
   `spot_20260826_0310*/0311*_14004d85…`);`rec_slot_cost.py` **第十次验收**
   (读退出码**不要接管道**);6h 闸下一次解锁 **`2026-08-26T09:10:52Z`**,
   发波前照例先把钟点打出来再跟 `date -u` 比。

## ⑨ 本次会话 token 用量

```
TOKENS total_in=3,690,871 out=25,686 turns=37
```

(统计时刻 = 收尾前;此后的提交/推送回合不计入。)
