# 批测台 2026-09-04T12:15Z — 发 W46 = 62-id 家族第一波

**一句话**:本轮真正的产出不是那四台机器,是**闸 (iv) 第一次拒绝跑**——
`reclaim_blind.py` 在 W45 上打 `exit 2 / UNDECIDABLE`,原因是**上一轮收割把两台被回收
机器的 `ab`/`ba` 写成了 `null`**,而**那一轮读上去是干净的**:没跑成的闸只有在
**下一个真的要发波的轮次**才会举手,而举手的方式是拦住一次发波。

---

## 一、自检(铁律 10)

⚠️ **开工第一条命令又误用管道**(`| tail -60`),脚本自拒并打 `REFUSED ... 这是 evidence
discipline 3;已复发 5 次,每次都是当轮第一条命令` —— **同形第 7 次**。改重定向重跑,
`EXIT=3`(**裸读 `$?`**)= **有发现**,不是干净也不是没跑成。

`exit sources (GH #267 4b)`:9 条腿,`FINDINGS (exit 3) = cadence / owed-executions /
trunk-red(python) / trunk-red(lua)`,`UNCERTIFIABLE (exit 2): none`。
**不写「exit 3,全是 cadence」** —— 那句手工归因在 08-27 错过 22 小时(GH #267)。

已跑完的腿:stable-v1/v2 锚点 `EXISTS/PINNED/SHIPPED` 全 ok;`unlanded_commits` 无 OFF-TRUNK;
`owed executions` 1 行 `OWED campgrade_wave_type_premise_reading`(GH #480/#241,
executor=batch-desk **排 `campgrade` 独占波的那一轮** + 录像组;`campgrade` 此刻还不存在,
**本波不是它**,本轮不认领)。

### 1.1 ⭐⭐ trunk 红(Lua),而且红在**本波刚armed 的那个 id 上**

```
RED  test_coarmed_attribution_register.lua
      tests/test_coarmed_attribution_register.lua:407: a NEW co-armed conjunction is live:
            creepthink > campbind
            pullcad    > campbind
            pullthink  > campbind
TRUNK RED -- 1 of 76 Lua detector file(s) failing ON THE WORKING TREE.
```

`campbind` 正是 W45 之后新入集的两个 id 之一,也就是**本波(W46)第一次真的花钱去测的
两个 id 之一**。检测器要的不是把 id 撤下来,是**把这三条合取登记进 `test_set.md` 的
入集节,再进 `ACKNOWLEDGED`**(它自己的报错原文:`Do NOT add it to make this green.`)。

**为什么这条对本台要紧,而不只是协同组的活**:检测器说的是
「`campbind` 的 helper 被读在另外三个**同样 armed** 的 id 的 gated 函数体内 ⇒ 两个 id 走同一条腿
⇒ 外层 id 的逐 id (a)(armed 腿触发计数)从此测的是 `outer AND inner`」。
**那正是本台买单的那个量**。⇒ 本波的家族级读数照常成立(它本来就是家族级的),
但**任何拿 W46 去说 `creepthink` / `pullcad` / `pullthink` 逐 id (a) 的话,都必须先看这条**;
入集前后的 (a) 读数**不是同一个测量**。

已开 issue 交协同组/总监(见第七节)。**本台不自行改 `test_set.md` 的入集节,也不撤 id** ——
那是入集裁定的地界。

⛔ **python 腿**:上一轮亲手复现的 `tests/test_bots_walk_farm_only.py` 红,本轮自检
`trunk-red(python)` 仍在 FINDINGS 里 ⇒ **仍红,未修,本台不声称已修**(第三轮点名)。

---

## 二、成本(闸 (iii))

- 九月 MTD **`$14.981`**(免费 `budgets describe-budgets`,`refreshed 2026-09-04T11:29:50Z`)。
  MTD `< $35` ⇒ CE 未触发,**没花 `$0.01`**。
- ⚠️ 这张快照 `11:29:50Z` 与 W45 关机(~08:0xZ)相距 ~3.5h,**仍在 4.3–11.3h 的滞后带内**
  ⇒ **按构造不能假定它含 W45**。
- **围栏(铁纪律甲,逐波按自己的市场取价)**:
  `14.981 + W44 按需 2.15 + W45 spot 0.90 + W45 按需补跑 1.10 + W46 本波 spot 0.90 = $20.03`
  vs `$45 / $80 / $90 / $100` ⇒ **闸 (iii) 通过,九月未及第一档 $50 告警**,
  **不触发任何刹车**,无「跨档要写一行」的义务。
- ⛔ **未用被禁的「MTD 增量对得上波费 ⇒ 该波已计入」手法**(铁纪律乙)。
- **(a) W44 对账第四次顺延**:快照 `11:29:50Z` 距 W44 关机约 10.5h,**仍在滞后带内**,按构造结不了。

---

## 三、收割

**本轮零收割**:上一轮(09:15Z)已把 W45 收干净、四粒全过 `arm_depth` 门、
六台全部自毁、零残留;S3 自那以后没有新的 verdict 前缀(本轮 `--build` 只新扫到 5 个,
全部是 W45 自己的六个 run 里此前未索引的部分)。

**局数(铁律 7 (a) 上一波实测)**:W45 语料 245 局 / `scored_games 215` / `unfinished 0`,
四粒 `arm_depth` 27.11 / 20.22 / 17.95 / 23.66,`min_arm_depth 8`、`thin_arm_seeds []`。
四量 `gpm −6.19 (2/4)`、`xpm +0.24 (2/4)`、`deaths −0.06 (3/4)`、`last_hits −0.79 (2/4)`;
胜负通道 `DEGENERATE`,winrate 一律不作读数(GH #352 / §CT)。**详见上一轮报告,本轮不重算。**

---

## 四、⭐⭐ 头号发现:闸 (iv) 在 W45 上**没跑成**,而没跑成只在下一次发波时才现形

发波前照章程跑闸 (iv):

```
RB_EXIT=2
=== reclaim-blind check (GH #271 ruling) -- wave W45 ===
changeover 40.0 min  (measured bracket 34.80 < x <= 40.63, ...)  min_arm_depth 8.0
UNDECIDABLE: seed 3968: leg count 'ab' is not an integer
```

**`exit 2` 是「什么都没检查」不是「通过」。** 原因不是工具坏了:

- `W45_wave.json:machines[]` 里,**两台被回收的机器**(3968 / 3927)的 `ab`/`ba` 是 `null`;
  该粒种子的**并池后**腿数被写在了 `rerun[]` 行上(3968 `ab56/ba15`、3927 `ab31/ba15`)。
- 但工具的 schema 不是这样:**`machines[]` 装的是那一台自己的腿数**,而且工具**根本不读
  `rerun`**(`reclaim_blind.py:423` 只取 `wave.get("machines")`)。
- **先例逐字对得上**:W42 同样是「一台被抢占 + 一台补跑」,`exit 0`,因为它的
  `machines[]` 里被抢占的 3040 写的是**它自己的** `ab10 / ba0`,判 `NO-PAIR`。
- W42 / W44 本轮重跑均 `RB_EXIT=0`,**只有 W45 是 2** ⇒ 不是工具漂移,是**那一轮的登记**。

**这条的形状值得写下来**:上一轮的收割报告读上去是干净的——四粒全过门、零泄漏、
四量齐全,**没有任何东西举手说「你写的这两个 null 会让下一道闸瞎掉」**。
闸 (iv) 是**输入侧**的闸,它检查的是**上一波**;于是「上一波登记写坏了」这件事,
**结构上只能由下一个真的要发波的轮次发现**,而发现的方式是**拦住一次发波**。
与 GH #469(闸 (i) 靠人脑算术)同族但**反向**:那一条是**没有检查**,这一条是
**有检查,但它的输入由上一轮写、上一轮自己不跑它**。

### 4.1 回填是**量出来的**,不是按常数假设的

⛔ 不能直接写「37.87 min < 40.0 min ⇒ 没翻腿」:changeover 的实测夹逼是
`34.80 < x <= 40.63`,而 **37.87 落在夹逼区间内** ⇒ 单凭常数**判不了**。
改用 S3 的原始事实:

- **3968 原机**:`soak/<run_id>/` 下 **35 个不同的对局 tag**(与登记的「原机 35 局」逐位一致);
  **最后一局的开始时刻 `06:57:27Z`,距起飞 `06:30:38Z` = 26.8 分钟**,
  **低于夹逼区间的下界 34.80** ⇒ **区间内任何一个 changeover 值都不会把任何一局推过翻腿点**
  ⇒ `ab=35 / ba=0` 是**测出来的**。
  (⚠️ 登记里写明:35 是**已落盘**局数(S3 对象计数),不是**计分**局数;该粒的 12 局
  `unfinished` 无法只凭 S3 列表在原机与补跑机之间拆分。)
- **3927 原机**:落盘 **零** `analysis.json`(13.52 分钟被回收)⇒ `ab=0 / ba=0`。

回填后重跑:

```
RB_EXIT=0
  seed 3968   37.9 min  instance-terminated-no-capacity   ab35/ba0   NO-PAIR
  seed 3892   55.5 min  instance-terminated-by-user       ab33/ba23  PAIRED  [depth-checked]
  seed 3954   46.4 min  instance-terminated-by-user       ab29/ba13  PAIRED  [depth-checked]
  seed 3927   13.5 min  instance-terminated-no-capacity   ab0 /ba0   NO-PAIR
yield      : 2 paired seed(s) of 4 machine(s)
attribution: 2 machine(s) reclaimed before the flip
VERDICT: not blinded -- the wave delivered 2 paired seed(s).
NEXT WAVE: spot (the default; GH #158 unchanged).
```

**⚠️ 一处不许含糊**:`yield 2` 指的是**原机**层面的配对数;W45 最终四粒全部配对,
是**补跑机**买回来的,而工具按 #271 的定义**只看原机**(它问的是「这一波的采集有没有被
回收致盲」,不是「最终有没有数据」)。两个读数都对,**问的不是同一个问题**。

---

## 五、发波:W46 = 62-id 家族第一波

### 5.0 闸 (i):**在动作时点被真的行使了一次,而且拦住了本台**

第一次跑启动脚本:`LS_REMOTE_TIP` 核对通过 → `wave_throttle.py` 打

```
unlock : 2026-09-04T12:30:52Z
now    : 2026-09-04T12:24:12Z
margin : 400s SHORT of unlock
A launch at this instant BREACHES gate (i) by 6m40s.
WAVE_THROTTLE: THROTTLED (exit 3) -- do not launch. Wait 400s.
```

脚本 `exit 8`,**一台都没发**。

⭐ **这一次不是形式主义**:本台在这轮里对墙钟的心算**偏了约 25 分钟**(以为已到 12:5xZ,
实际 12:24Z)——**正是 GH #469 立案时描述的那个机制**(钟只读一次,然后去做几分钟的门,
回来直接发),只不过这次偏的方向让本台**以为自己早就解锁了**。
**闸 (i) 变成可执行检查之后的第一次真实拦截。** 章程里那个手打的解锁时刻这轮**没有被读**,
读的是工具打的那一行。

⭐ 顺带,工具对本轮新写的 `W46_wave.json` 打
`skipped W46 (no machines[] -- opened no window)` ⇒
**「一个还没上机的波次不开窗」按 schema 结构性成立**,不依赖任何人诚实填字段。

### 5.1 闸 (ii):有新东西可测

- 臂串**从发波树亲手重算**(不是抄章程的计划句):
  **62 id / 550 字节 / md5 `c7e1f92c739f8f8dc0ee0c9484288628`**。
  对 W45(`60 / 530 / eef5fb2e…`)的集合差:**`+campbind`、`+zusboltdom`,无删除**。
  ⇒ **上一轮预登记的 (a) 又一次兑现**:章程的计划句只说「发 W46」,没有臂串;
  重算把它钉成了一个**新家族**。
- `bots/`/`game/` 漂移(两步走,认退出码不认空输出):
  `git fetch --depth 1 origin 27165f8c…` → `git log 27165f8c..HEAD -- bots/ game/`,
  `LOG_EXIT=0`,两个 commit:`a2762804`(hero,gated `zusboltdom`,GH #477 选项 2)、
  `dd19aa70`(hero,`BOT_API_REFERENCE` 行号引用改按标题锚)。
  HEAD `efa7ba70` 只动 `tests/` 与 `iterations/`(`git show --stat` 核过,**不是从路径过滤器推的**)。
- **⇒ W46 不与 W45(60-id)、W44(58-id)、W41∪W42(57-id)任何一个并池。**

### 5.2 选种

`seed_roster_index.py --build` **在选种之前**跑(`ROSTER_EXIT=0`):341 个 run 前缀、
新扫 5 个、索引 **205 粒种子 / 41 英雄 / 21278 局**。不 build 就会把 W45 刚烧掉的
3968/3892/3954/3927 当「从未用过」献回来。

窗口**右移**到 `[4201,4600]`(W45 在 3892–3968),400 粒未用;
7 个 term 上的 popcount 分布 `{0:39, 1:110, 2:128, 3:90, 4:27, 5:5, 6:1}`
⇒ **本波无组合学耗尽**(对比 `[1600,1800]` 那次 max popcount 3、12 个载体槽恰好顶死)。
满足「每 term ≥2 载体」的不同掩码四元组 **8170** 组。

选定 **4209 / 4401 / 4470 / 4252**,`pudge` 平局判据同 W45(4 粒里 3 粒抽到 Pudge,
维持 `rotscope` 条件 (a) 的帧级通道可执行;GH #472 / queue `strategy-26`)——
**这是载体门解空间内部的免费平局判据,不是门的改动,也不是新 term**。

### 5.3 四道程序化门的读数

| 门 | 命令 | 退出码 | 读数 |
|---|---|---|---|
| (ii) 载体 term | `carrier_terms.py --arm-file test_set.md` | `CT_EXIT=0` | 62 id = 12 hero-scoped / 50 generic / **0 unresolved** ⇒ **7 term**,与 W43/W44/W45 **同一个 term 集**(`campbind` generic;`zusboltdom` → zuus,集合里本来就有) |
| (ii) 载体覆盖 | `seed_draft.py <4 粒> --assert-carrier-from-arm "$ARM"` | `CARRIER_GATE_EXIT=0` | ids=12 seeds=4;逐 term 载体数 ck 3 / cm 2 / lion 2 / od 2 / sk 2 / sb 2 / zuus 2,**min-per-term = 2,7 个 term 全满足** |
| 布线 | `check_armed_wiring.py --cand "$ARM"` | `ARM_EXIT=0` | **62 个 armed id 全部 wired on HEAD**,含两个新 id(`campbind` direct `bots/FunLib/jmz_func.lua:8993`;`zusboltdom` direct `bots/BotLib/hero_zuus.lua:1032`) |
| 铁律 6 静态半 | `bash tools/agent/luacheck_gate.sh` | `LUACHECK_EXIT=0` | 冷启自己装上 luacheck(apt 包名 `lua-check`),`luacheck bots game: 0 warnings`,`GATE_EXIT=0 CLEAN` |

⚠️ 一处自己造成的读错,登记:`check_armed_wiring.py` 第一次按
`tools/batch_test/soak/` 调用得 `ARM_EXIT=2 / No such file` —— **那个 2 是「没跑成」不是「没布线」**;
工具在 `tools/batch_test/`(不在 `soak/`)。同一形状本轮出现两次(另一次是把
`wave_throttle.py` 用 `bash` 跑,拿到一串 shell 语法错和一个 `exit 2`)。
**两次都没有被当成读数。**

### 5.4 起飞

**闸 (i) 在动作时点重跑**(`git ls-remote` 与 dry-run 之后、`spot_run.sh` 之前,脚本内裸读 `$?`):

```
now    : 2026-09-04T12:31:13Z
margin : +21s past unlock
WAVE_THROTTLE: UNLOCKED (exit 0) -- gate (i) passes at this instant.
```

`WT_EXIT=0` ⇒ 发波。四次独立调用,**每次显式一个 `--az`,四次四个不同**,
`--slots 16 --hours 2 --rec-slots 8 --ref efa7ba70…(40 位) --validate "<62-id 臂串> <种子> --games 12"`。

| 种子 | 实例 | AZ(请求→实得) | `run_id` 尾 token | `LaunchTime` | `SPOT_RUN_EXIT` | SIR |
|---|---|---|---|---|---|---|
| 4209 | `i-0430f3a3b2e759c31` | 2a → **2a** | `84e984` | 12:31:15Z | `0` | `sir-aivfjwvp` |
| 4401 | `i-03fe1ddddfd201f80` | 2b → **2b** | `d21f34` | 12:31:20Z | `0` | `sir-nibfgrjq` |
| 4470 | `i-0642a8c11a626ce8d` | 2c → **2c** | `d7082b` | 12:31:25Z | `0` | `sir-xhkqk7pm` |
| 4252 | `i-085a71f7c65b9acba` | 2d → **2d** | `b77771` | 12:31:30Z | `0` | `sir-ygm7kg3m` |

- **市场**:`describe-instances` 的 `InstanceLifecycle` = **`spot`,四台全核过**。
  `run_id` 的 `spot_` 前缀是硬写字符串,**不是证据**。
  **零 `InsufficientInstanceCapacity` ⇒ 容量降级阶梯 (1)(2)(3) 一步都没进**。
- **GH #252 验收干净**:四个 AZ 互不相同,`requested == actual` 四台全对。
- **GH #256 验收干净**:`re-aiming inside the ring` 与 `AZ RING EXHAUSTED`
  **两行一行都没出现**。
- **自毁**:`--hours 2` 看门狗 + `shutdown-behavior=terminate` + **one-time** spot 请求
  (被回收不会静默重发)。四个 SIR 全 `active / fulfilled`。
- **预期局数**(铁律 7 (b)):~96 计分局(4 粒 × 12 局 × 2 腿)+ 暖场局(不计分)。
  起飞时 S3 零对象;按 W42 量到的上传节奏(每 ~12 分钟一批、**第一个对象落在起飞后
  ~14.5 分钟**),**14 分钟内 `OBJ_COUNT=0` 是预期值不是症状**。

**预登记两条(写在任何数据落地之前)**:
1. **胜负通道**:已连续四波 `DEGENERATE`(W41/W42/W44/W45;W43 是沉默不算一波)。
   W46 若第五次 `DEGENERATE`,`winrate` 与 `comps_better` 的 winrate 行**一律不得写成读数**
   (GH #352 / §CT);若读到 `RECOVERED`,**那本身就是本波的发现**。
2. **GH #454**:W46 是**纯 spot**波。**若它跑完**,收割轮必须**当场**把账单侧单波成本对
   `$0.90` 结账,并明写自动失效条款**触发/不触发**。W45 结不了(四台里两台被早回收,
   2.554 机时 vs W29 干净读数 3.424)。

---

## 六、泄漏与点名

**launch-round 点名两条都跑了**(章程步骤 6,零成本):

1. `pending,running` 的 `soak-run` 标签 = **恰好四个,两两不同**,
   与 `machines[].run_id` 逐一对上(`84e984 / d21f34 / d7082b / b77771`)——
   **不多(无泄漏)不少(无掉臂)**。
2. `terminated,shutting-down` 返回 **空**。⚠️ 这个空是**「还没有任何一台死掉」**
   (波次 ~1 分钟大),**不是「记录过期了」**(GH #375 的 ~1h 窗口)。
   本波窗口内**无 `Server.SpotInstanceTermination`**。

`check_costs.sh --leak-only`:只列出**本波这四台**,无第五台;
`describe-volumes` 四个卷 = 这四台的根卷(**不是游离卷**);
`open,active` 的 spot 请求四个,与四台一一对应。常驻成本仍只有 AMI。

**上一波(W45)的六台已于上一轮确认全部自毁清理完毕,零残留** —— 本轮复核仍为零。

---

## 七、开的 issue / 交棒

**开了两条 issue**:

1. **GH #486 `[bug]` co-armed 登记册在 trunk 上红,且红在本波刚 armed 的 `campbind` 上** ——
   交协同组/总监。三条未登记合取 `creepthink > campbind` / `pullcad > campbind` /
   `pullthink > campbind`;调用形状本台亲手核过(helper 在
   `bots/FunLib/jmz_func.lua:8993`,被读在 `bots/mode_roam_generic.lua:264/265/343/412`
   四个 armed 门内)。⚠️ **不是 `pullcad` 陷阱的重复:那条是合取,这条是嵌套。**
   验收:检测器转绿**且**绿是因为先落地了 `test_set.md` 入集节的 caveat,
   不是因为直接往 `ACKNOWLEDGED` 里加三行。
2. **GH #487 `[batch]` 闸 (iv) 在 W45 上 `exit 2`,而写坏它输入的那一轮读上去是干净的** ——
   交总监。数据已同轮回填(`RB_EXIT` 2 → 0),开这条是为了那个**结构**:
   输入侧的闸只能由**下一个真要发波的轮次**发现,发现方式是**拦住一次发波**。
   请裁三选一(本台倾向 (3) 两者都要):(1) 把「被回收机器的 `machines[].ab/ba`
   必须是那一台自己的整数」写死;(2) 让收割轮就跑一次这个零成本的闸;(3) 两者。
   另请把 **`yield`(原机层面)vs「最终配对」(补跑机买回来的)** 这两个不同的问题
   在 #271 的定义里写死 —— 报告里只写一个数字时极易被读成互相矛盾。

**交棒**:

- ⭐⭐⭐ **协同组/总监**(GH #486) —— co-armed 登记册的红,**W46 已经在飞**,
  逐 id (a) 的解释权卡在这条上(见 §1.1)。
- ⭐⭐⭐ **总监**(GH #487) —— 闸 (iv) 的结构缺口(见 §四),含 `yield` 语义的定名请求。
- ⭐⭐ **本台下一轮必做** —— 收割 W46:
  ① `machines[].ab/ba` 填**每台自己的**整数(落盘零的填 `0/0`),**不许留 null、不许填并池值**;
  ② 臂串从发波树 `efa7ba70` 重算并对 `62 / 550 / c7e1f92c…`;
  ③ **GH #454 当场结账**(若本波纯 spot 跑完);
  ④ 照抄 `winrate_channel` 与 `mean.winrate_headroom`。
- ⭐⭐ **总监/英雄组** —— trunk 的 **python 腿仍红**(`tests/test_bots_walk_farm_only.py`),
  **第三轮点名,本台不声称已修**。
- ⭐ **总监** —— W44 账单侧对账**第四次顺延**(快照仍在滞后带内),按构造结不了。
- ⭐ 存量:#480/#241(`campgrade` 独占波尚不存在,本波不是它)/ #473 甲乙 / #472
  (W44 那 48 份 pudge `.dem` 约 09-25 过期)/ #427 / #408 / #420 / #449 / #290 /
  `strategy-5b` / **#388 的 W34 103 份 `.dem` 2026-09-22 到期(18 天)**。

---

## 八、本轮 token

```
TOKENS total_in=11,763,949 out=71,518 turns=81
```

(统计时刻之后的收尾回合不计入;量级参考。)
