# 批测台 2026-08-23T00:06Z(第四十九次触发)

**一句话**:例行波次的 6h 节流窗口在 00:11:22Z 到点,本轮**启动了 §AR.0 的 24 id 全集波**
(`fieldcreep` 首次上机),4 台 × 1 种子 on-demand、16 槽、**`--rec-slots 8`**、2h 看门狗;
无可收割的数据(18:11Z 那波 20:09Z 已全量收完);零泄漏。
**外加一条免费的启动前置自查第一次真跑**:24 个 armed id **逐个核到活的 gate 调用点**,
其中 `lf_rescue` 的字面量 grep 是 **0 命中**,不是死 id,而是它走 `IsLaneFixOn` 的**拼接形式** ——
这条差一点被读成 `creeppull` 那类死分支,记在案免得下次重读。

---

## 1. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` worst exit **3**:

- **UNLANDED 2 条**:`5b155ff` / `093840c`(hero 21:48Z,Lion t10 +20 移速 / t15),
  在 `origin/claude/vibrant-heisenberg-qypj8j`,**不属批测台,只转述不接管**。
  (与 22:06Z 报的 `483b818` 是同一份工作的新 SHA — hero 组换了分支重推,仍未落地。)
- **CADENCE**:`hero` 组 19:50Z → now **4.3h** 空档(门槛 3.5h),同样只转述。
- trunk python 套件 **12 passed / 0 failed**。

## 2. 成本与刹车(全免费读数)

| 项 | 读数 |
|---|---|
| MTD | **$23.578**(免费 `budgets/dota2bot-batch`,forecast 23.139,limit $100,refreshed 2026-08-22T21:26:04Z) |
| 与 22:06Z 比 | **持平**(那一轮无支出,预期) |
| CE 调用 | **未调**($23.578 ≪ `COST_CONFIRM_AT=$35`,省 $0.01) |
| $45 围栏 | 未触及($23.578 + 本波预估 ~$1.5 = **~$25.1 ≤ $45** ✅) |
| $90 刹车 / $100 owner 线 | 未触及 |

## 3. 收割:本轮无可收

`soak/` **160 个 run**,与 20:09Z / 22:06Z **逐位一致** ⇒ 18:11Z 那波 20:09Z 已全量收完,
本轮没有可收的东西,**不是漏收**。`dem21/` **12**、`unattributed/` **0**。
无在跑波次(开工时实例 0)。

## 4. 启动决策:**启动**(本轮四条门槛全过)

### 4a 队列(`iterations/queue.json`,5 条)
没有一条需要为它单开波:`strategy-1` = done;`hero-1`/`hero-2`/`hero-3` **自述
NO NEW WAVE NEEDED**(全是档案侧扫描请求);`strategy-2` = `approved-as-rider`
(总监 §AP.1 驳回其专波,裁定搭全集波顺路买)⇒ **它搭本波**,`pullcamp` 在 24 id 串里 ✅。

### 4b 例行波次三条节流
- **(i) 满足**:上一次例行波次启动 **18:11:22Z**,门槛 **00:11:22Z**;本轮**等到点才发**
  (见 §5 的时间戳),不援引 06:07Z 那条「差 2 分钟按立法目的照常启动」的先例 —— 能等就等。
- **(ii) 满足**:被测集合从 §AP.0 的 23 id 变成 **§AR.0 的 24 id**(`fieldcreep` 总监
  23:0xZ 批准入集,**首次上机**);两步法核树另读出 5 个 `bots/` 漂移 commit(见 §5)。
- **(iii) 满足**:见 §2。

### owner 优先项核对
P1 的球在协同组(1)/录像组(3 的核验),P2 的球在协同组。**本波对两项都是正向**:
P1 的 `creeppull`+`pullcamp`(修好选点后)与 P2 的 `stayfield`/`stayfield2`/`fieldbuy`
(+ 本波首次带上 `fieldcreep`)都在 armed 串里,本波买的就是它们的 (a) 语料。

---

## 5. 上机记录

**核树(两步法,§5 纪律)**:`git ls-remote origin main` = **`cde1d6c4f3b83b0b75904575eae1f4a599ac65be`**
= 本地 HEAD ⇒ 要测的树就是远端 tip ✅。漂移读数:`git log 05b5e42..origin/main -- bots/ game/`
**exit 0 + 5 条**(`a1dedc0`/`b50529d` fieldcreep 一族、`0c95526` Zeus/CM t15、`01ab933` pullcamp 选点、
`270bbea`),**不是那个会被误读成「无漂移」的 exit 128 空输出**。
(附:本轮 `git fetch --depth 1 origin 05b5e424` 打印 `couldn't find remote ref` —— 短 SHA 取不回,
但 22:06Z 那次已经把对象拉进本地库,`git log` 因此 exit 0。**教训**:两步法第一步要用**全 40 位** SHA。)

```
SHA=cde1d6c4f3b83b0b75904575eae1f4a599ac65be    # = 远端 main tip,ls-remote 已核
CAND="l1trade,l5combo,midtp,suptp,tpcommit,tpdying,lf_rescue,teambrain,ownhalf,overchase,\
fieldregen,wandbleed,capmono,cmrguard,tpdead,zusult,blinkflee,liondrainstop,creeppull,\
pullcamp,stayfield,stayfield2,fieldbuy,fieldcreep"      # = §AR.0 的 24 id,逐字核过
for s in 888 895 896 906; do
  bash tools/batch_test/aws/spot_run.sh --count 1 --on-demand --slots 16 --rec-slots 8 \
       --hours 2 --ref "$SHA" --validate "$CAND $s --games 22"
  sleep 2
done
```

| Seed | Instance | Launch (UTC) | run_id |
|------|----------|--------------|--------|
| 888 | `i-0461d2bb48ea04581` | 00:11:29 | `spot_20260823_001127_1_cde1d6c4…_3fb2ec` |
| 895 | `i-0ab483718b10c41de` | 00:11:33 | `spot_20260823_001131_1_cde1d6c4…_590502` |
| 896 | `i-06e62c475dc92f900` | 00:11:38 | `spot_20260823_001136_1_cde1d6c4…_febb54` |
| 906 | `i-042a5926b10004335` | 00:11:42 | `spot_20260823_001140_1_cde1d6c4…_c3ba4a` |

**run_id 唯一性(§5 那条)双保险都成立**:四个 `STAMP` 两两不同(001127/001131/001136/001140,
间隔 4/5/4 秒),四个 3 字节 `RUN_TOKEN` 也两两不同(`3fb2ec`/`590502`/`febb54`/`c3ba4a`)——
即 `#98` 的 token 修法**已在现役脚本里**,唯一性不再押在那一秒上。
全部 `running`、c6i.4xlarge、on-demand、2h 看门狗、`--validate` 跑完即 terminate。
**收割最早 2026-08-23T02:1xZ。**

## 6. ⭐ 本轮的真发现:启动前置多加了一条免费自查,第一次跑就差点误报一个死 id

**做法(零成本、只读、今后每波照做)**:把要 armed 的 24 个 id **逐个 grep 到活的 gate 调用点**。
立这条的理由是 `creeppull` 那个教训的**上游版本**:那次是 gate 点亮了但分支是死的(要四十轮才发现);
更廉价的一档错误是 **id 字符串根本没有对应的调用点**(串里打错字 / 改名后没同步),
后果一模一样(整波静默),但**在启动前一秒就查得出来**。

**结果:24 / 24 全部落到活的调用点**,但其中一个的读法必须写下来 ——

> `lf_rescue` 的字面量 grep(`IsSoakCandidate('lf_rescue')`)是 **0 命中**。
> **它不是死 id**:`jmz_func.lua:5813-5816` 的 `J.IsLaneFixOn( sub )` 做的是
> `J.IsSoakCandidate( 'lf_' .. sub )` —— **拼接**,所以字面量永远搜不到。
> 真正的调用点是 `jmz_func.lua:6119` 的 `if not J.IsLaneFixOn( 'rescue' ) then return nil end`
> (`J.GetRescueTpTarget` 的第一行),消费者在 `ability_item_usage_generic.lua:5098`。
> 全仓库共 8 个这种拼接 sub:`chase/mana/recover/rescue/revive/salve/support/threat`。

**⇒ 这条自查的正确判据是「字面量命中 **或** 落在 `IsLaneFixOn` 的 8 个 sub 里」**,
单看字面量会把 `lf_rescue` 报成死 id ——**假阳性,而且长得跟真问题一模一样**。
记在这里,免得下一轮(或别的组)重新踩一遍再花一轮去查。

**顺带核到的两条(都是行号级读数,不是推断)**:
- `dem_claim.sh:144` 的 `[ "$rec_slots" = "1" ]` 护栏**逐字仍在** —— 20:09Z 实测
  `by_mtime` 在 8 槽下 **0/157 全错**,这条护栏是「实测必需」不是「设计上的谨慎」;本波 8 槽,
  结构上仍选不中 mtime。
- `J.IsSoakCandidate`(`jmz_func.lua:4634-4654`)对逗号串走的是干净的 `string.gmatch('[^,]+')`,
  **无个数上限**,24 id 与此前 21/22/23 id 同路径。

## 7. 泄漏检查(四层 + spot 请求,全免费只读)

| 层 | 开工 | 收尾 |
|---|---|---|
| 实例(running/pending) | **0** | **4**(= 本波刚起的四台,全部 2h 自毁 + `--validate` 跑完即 terminate) |
| 游离卷(available) | 0 | 0 |
| 快照 | 1(`snap-0ad026b386c804288`,唯一常设成本) | 同 |
| EIP | 0 | 0 |
| open/active spot 请求 | 0 | 0(本波走 on-demand,不产生 spot 请求) |

**无泄漏。** 固定栏位:`soak/` **160**(与 20:09Z / 22:06Z 逐位一致,本波的四个 run 目录要等
第一局结束才出现)、`dem21/` **12**、`unattributed/` **0**、`validation/` 最新条目仍是
2026-07-23(标准收割路径走 `recover_verdict.py`,陈旧是**预期**)、远端 main tip 开工/收尾
均为 `cde1d6c`。

## 8. 局数

上一波(18:11Z)**270 有效 + 24 暖场 = 294**,逐 run analysis 73/79/72/70,
三元组 `analysis 294 / 录制局 157 / demclaim 157 / .dem 156`(20:09Z 已全量收完)。
**本波在跑,尚无局数。**

## 9. 下一波必查项(事先登记,收割时照做,不许事后改判据)

承接 22:06Z 的 8 条,本波**新增/改向**的部分单独标出:

① 暖场 `script_version` 全是 `cde1d6c4…`;
② 分 run 下载再带时分秒前缀合并,对账一个不丢;
③ S3 真实 cand 串与 **§AR.0 的 24 id** 逐字交叉验证;**`pullcamp` 必须出现 ≥1 次**;
   **【新】`fieldcreep` 首次上机,必须出现 ≥1 次**;`wandlimbo`/`odaoe` 必须 0 次;
④ `rec_slot_cost.py` 验收(8 槽有波内对照腿 ⇒ 可跑)**判据不许改、末尾不许接管道**,
   **认 exit code 不认空输出**;**注意 §6/22:06Z 的裁定:通过也不再自动上 16 槽**
   (16-of-16 结构上无对照腿 ⇒ `rec_slot_cost.py:218-220` 按构造 exit 2),阶梯待总监裁 (A)/(B)/(C);
⑤ 三元组第一栏分母是**「录制局数」不是 `analysis`**(8 < 16 槽);
⑥ 经济读数是 **24 杠杆合读,不可归因单 id**,不与前波并排当趋势(§AF.3);
⑦ `candidates` 分布 + 三判据一致性再读(8 槽,与 20:09Z 的 157/157 同构,可直接对读);
⑧ `strategy-2` 的三条臂内读数(连接率 / 开拉点离泉水中位 <9,403u / 深野营 poke 帧占比)+ 正控;
⑨ **【新,§AR.3 甲】`fieldcreep` 的每一次否决命中,必须写清打他的是野怪还是小兵** ——
   只报「触发了 N 次」**不算买到 (a)**,那个数在总监 §AR.1 的两个世界里指向相反结论。
   **§AR.3 乙的退出条件同时生效**:若 camp 啃人的帧确实出现而 `fieldcreep` 一次没点亮,
   **这个 id 立刻出 armed 集**并退回协同组重估修法。

## 10. 跨组

- `strategy-2`(queue)已置 `running` 并写进 run_id / 树 / 24 id 串 / 收割时点。
- **不新开 issue**:`[batch] #70`、`[harness] #98`/`#95`/`#75`/`#33`、`[bug] #96`、
  `[strategy] #117`/`#116`/`#119` 仍开着。§6 那条「拼接 sub 让字面量自查假阳性」按其归属
  追评在 **GH #45**(拥有采集/归属自查话题的 issue)下。
- **给录像组的抬头**:本波 `--rec-slots 8` ⇒ 帧通道 8/16,`.dem` 归属侧按 20:09Z 的实证
  **100% 走 `logname`**;P1(`creeppull`/`pullcamp` 选点修法后的第一波)与 P2
  (`stayfield`/`stayfield2`/`fieldbuy` + 首次带 `fieldcreep`)的 (a) 语料都在这一波里。

## 11. 验证(铁律 6)

本会话**未改 Lua**(改动仅 `iterations/` 下的报告 / 章程 / `queue.json`),
容器无 `luacheck` / `lua5.1`,铁律 6 无适用对象。**本轮无估算项**;§5/§6/§7 全部是
命令输出或源码行号级读数。

## 12. 本次会话 token 用量

`python3 tools/agent/token_usage.py` 最后一行原样:

```
TOKENS total_in=7,777,870 out=36,111 turns=89
```

(到统计时刻为止;统计后的收尾回合不计入。)
