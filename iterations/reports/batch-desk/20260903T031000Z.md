# batch-desk 2026-09-03T03:10–03:4xZ — 发 W41(**57-id 家族第一波**)

**本轮真正的产出不是那四台,是「预登记的右移条件本轮触发了,而归因不是新加的那个 term」——
把 `ckpush` 拿掉重算,同一个窗口照样搜出 0。**

---

## 1. 开工自检(铁律 10)

⛔ **第一条命令又误用管道**(`routine_selfcheck.sh 2>&1 | tail -40`)⇒ 工具自拒:

```
REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.
SELFCHECK_EXIT=2  REFUSED (nothing was checked; this is NOT a pass)
```

**同形第十五次,而且第十五次都是当轮的第一条命令。** 工具自报仍是 `it has recurred 5x`,
与报告序列对不上,照前几轮据实并列。

改走重定向后 **`EXIT=124` 超时(400s 上限)** —— 九条腿跑完八条,**又死在最后一条
`trunk health (python)` 上,与上一轮逐字相同的形状** ⇒ 按铁律 10,**trunk 的 python 那一侧
连续第二轮没人看过,不是通过**。

⭐⭐ **本轮没有停在那个断言上,而是把它买了回来**:发波等闸期间零成本把同一个自检
**重发一次、上限 1500s**(后台,与选种并行)⇒ **九条腿全部跑完**:

```
legs run              : 9
FINDINGS (exit 3)     : cadence stale-waits trunk-red(python)
UNCERTIFIABLE (exit 2): none
NOT RUN (inside a leg): tests/test_selfcheck_lua_leg.py
selfcheck worst exit: 3
```

**⇒ 交棒总监:`400s` 这个上限现在 0-for-2**,它不是「偶尔慢」,**是这条腿的时长已经越过它**;
`1500s` 一次就跑完。**这条腿在 400s 下报的 `EXIT=124` 是个可预测的假拒答。**

⭐ **GH #420 那个「同一份日志两处读数互相矛盾」的形状本轮不见了**:汇总块打
`UNCERTIFIABLE (exit 2): none` **并另起一行** `NOT RUN (inside a leg): tests/test_selfcheck_lua_leg.py`
—— 两个数不再打架(此前是 python 腿自打 `2 uncertifiable` 并点名两个文件、汇总块打 `none`)。
本轮 python 腿自打 **`78 passed, 2 failed, 1 uncertifiable`**,`test_rc_wrapper.py` 不再出现在名单上。

**九条腿的读数**:`cadence` 有洞;未裁 queue 请求(`strategy-39` 仍 `open`);
`ORPHAN_PROPOSAL none`;`OWED_EXECUTION none`(总监已兑现 #413 那一行);
`stable-v1`/`stable-v2` 两锚 **OK**;Lua 腿 **73 文件 0 failures**;
⛔ `stale-waits` + python 红两条见 §1.1。

### 1.1 ⛔ 两条红,其中一条**完全不在本台的文件里**,已开 GH #443

```
$ python3 tests/test_stale_waits.py
42 checks, 1 failed
FAIL: a charter's LIVE 当前状态 waits on an admission ruling that already landed:
  replay-check.md:9827  slotdust 排他列尚未排他,等第三条扔尘路径的裁定
  replay-check.md:9838  slotarb …或等 §6 裁定后回来收 slotdust
  -- fix the charter line, do not loosen this test
```

**裁定早已落地**:`slotarb`/`slotdust` 都在本轮 57-id armed 串里,`strategy-34`/`-35` 也已随 W38 起飞
⇒ 这是**陈的等待**。本台**不改别组章程**,开 **GH #443** 交录像组;
理由不是洁癖:它是**每轮开工自检的固定红**,占着 `FINDINGS` 会盖掉下一条真发现
(与 GH #267「exit 3 全是 cadence」那次手工归因同类)。

python 的另一条红是 `tests/test_detector_source_constants.py`(与上一轮同一条),
**未 stash 重跑 ⇒ 不声称 main 也红**。

## 2. 成本与围栏(章程 2)

九月 MTD **`$5.612`**(免费 `budgets` 通道),⛔ **`refreshed 2026-09-02T20:31:27Z` 与前两轮
逐字节同一张快照,连续第三轮没翻新**,而那个时刻**早于 W40 起飞(21:31Z)**
⇒ **W40 与两台按需补跑一分钱都不在这张快照里**(登记为读数,不是推断)。
MTD `< $35` ⇒ **CE 未触发,那 `$0.01` 没花**。进场 `describe-instances` 在跑实例**为空**。

⚠️ **增量只登记不对账**(章程 2(乙)禁用「MTD 增量对得上波费 ⇒ 该波已计入」,本轮一次没用)。

**围栏(2 甲,逐波按自己的市场取价)**:

```
5.612 (MTD)
+ 0.90  W38   spot
+ 0.65  2613  on-demand 补跑
+ 0.90  W39   spot
+ 0.90  W40   spot
+ 1.35  W40   两台 on-demand 补跑
+ 2.15  W41   on-demand(本轮,见 §5)
= ~$12.46  <=  $80
```

**不跨任何 owner 可见告警档($50 已跨、下一档 $80)⇒ 本轮不欠解释行。**
`forecast 156.484` 是 budgets 自己的外推,**不是读数,不进围栏**。
**`$0.90` 自动失效条款本轮不适用**(本波是按需波,按 `~$2.15` 计)。

## 3. 收割:本轮无

W40 已于 `00:15Z` 收完(3 粒 / 149 计分局,不欠补跑);
`s3://dota2bot-batch-results-4924/soak/` 最新前缀就是 W40 的四台 + 两台补跑,**全部已收割**;
`validation/` 的尾巴仍停在 2026-07-23。进场在跑实例为空。**无收割义务。**

## 4. 例行波次三条件(章程 4b)

| 条件 | 读数 | 结论 |
|---|---|---|
| (i) 距上一次例行波次启动 >= 6h | W40 起飞 `2026-09-02T21:31:51Z` ⇒ 解锁 **`2026-09-03T03:31:51Z`** | 本轮触发 `03:06Z`,**扣住 ~26 分钟**,全部用在零成本工作上(§5/§6/§12),**不空转等待** |
| (ii) 有新东西可测 | **第一支两条腿都成立**(见 §4.1);第三支(家族累计种子 0 < 8)对第一波平凡成立 | 过 |
| (iii) 成本 | 围栏 `~$12.46 <= $80` | 过 |

⚠️ **一处更正**:上一轮章程写「W39 起飞 `15:31:51Z` ⇒ 解锁 `03:31:51Z`」——
**数对了,前件错了**:`03:31:51Z` 是 **W40(`21:31:51Z`)+ 6h**。两台按需补跑**不消耗闸 (i)**。

### 4.1 ⛔ 闸 (ii) 差点被一句陈的散文回答:**`bots/` 这次是真的动了**

上一轮章程写的是「`bots/` 自 W39 一行未动」。**那句话当时为真,现在为假** ——
本轮**重测而不是抄**,用的是对象哈希(避开「空输出被读成无漂移」那个陷阱):

```
git cat-file -t 8a5f8f31b7…   => commit          (先证对象在本地,不吃 shallow clone 的 exit 128)
W40  bots tree  15844690e8…   HEAD bots tree  3eeb4974f6…     <-- 不同
W40  game tree  f13b532c00…   HEAD game tree  f13b532c00…     <-- 相同
git diff --stat 8a5f8f31b7… HEAD -- bots/ game/  =>  2 files, +83
git log --oneline …           =>  77e18be9 strategy 22:38Z:认领 GH #437,落地 gated wandbleed2
```

⇒ 闸 (ii) 的第一支**两条腿都成立**(`test_set.md` 变 + `bots/` 变),不是只有一条。

**这一改动在本波是 inert 的,但仍然登记**:`wandbleed2` 的门是
`if not J.IsSoakCandidate('wandbleed2') then return true end`,而 **`wandbleed2` 不在 57-id 串里**
(串里只有 `wandbleed`)⇒ 它答 TRUE、不拦任何东西。
**但被测的那棵树已经不是 W40 的树** ⇒ **本波与 W39/W40 不可并池的理由有两条,不是一条**
(armed 串变了 **且** `bots/` 树变了),两条都写进了 `W41_wave.json`。

## 5. 市场:**on-demand,一波**(GH #271 的编码裁定,不是我自己判的)

发波前跑 `reclaim_blind.py --wave-json W40_wave.json`(章程闸 (iv)):

```
RECLAIM_EXIT=1
  seed 2641   4.2 min  instance-terminated-no-capacity  ab0 /ba0   NO-PAIR
  seed 2710   3.9 min  instance-terminated-no-capacity  ab0 /ba0   NO-PAIR
  seed 2921   3.9 min  instance-terminated-no-capacity  ab0 /ba0   NO-PAIR
  seed 2941  44.9 min  instance-terminated-by-user      ab28/ba14  PAIRED
yield      : 1 paired seed(s) of 4 machine(s)
attribution: 3 machine(s) reclaimed before the flip
VERDICT: BLINDED -- yield <= 1 AND the loss is capacity.
NEXT WAVE: launch --on-demand (ONE wave), then revert to spot.
```

⭐⭐ **上一轮预登记的警告本轮兑现了一半 —— 数是对的,标签是错的**(已追评 GH #429):

- 警告说对的那半:`1 of 4` **不是**这一波的交货数(W40 实际 **3 粒 / 149 局**,
  2641/2710 是按需补跑机交的,在 `rerun.machines[]` 里)。
- ⛔ **但不能据此给它的裁定打折,而且失效方向与警告说的相反**:这个工具问的是
  **「spot 这个市场买到了什么」**,它的分母 `machines[]` **恰好就是那四台 spot 机**。
  把按需补跑机算进 yield,等于**用按需价买来的机器给 spot 作证** —— 那才是真的数错。
  警告担心的是「低估 yield ⇒ 多买补跑」(偏贵),而实际形状是**分母本来就该是那四台**。
- 真正该记的缺陷是**标签不是计数**:`yield : 1 paired seed(s) of 4 machine(s)` 读起来像
  **波次产出**,而它是 **spot 腿产出**。建议(不自己改 harness)见 GH #429 追评。

⇒ **W41 走 `--on-demand`**,**一波,之后回 spot**。这**不是**改 owner 的 #158,是 #158 自己那句
「除非没有 spot 的机器」的一次兑现:**连续第七波被容量咬**,W40 那四台 spot 里三台在
~4 分钟内被同一次 `us-west-2c` 事件带走、产局为 0。

## 6. ⭐⭐⭐ 选种:预登记的右移条件触发,**而归因不是第 7 个 term**

**`seed_roster_index.py --build` 在选种之前跑**(章程活规则)⇒ exit 0。
排除集 = 索引 banked ∪ **W30–W40 各波次文件里的 `machines[].seed` 与 `rerun` 种子**
(**读出来的,不是手打的**;66 粒)。

### 6.1 触发

```
window [2601,3000]  7 terms  avail=364  masks=60
popcount {0:33, 1:112, 2:140, 3:68, 4:11}
BEST min-per-term = 1        combos at >=2/term = 0
```

上一轮预测「按同一斜率下一波很可能就是搜出 0」—— **本轮就是 0**。
章程的处置是**右移窗口、不放松自律**,照做:窗口 `[2601,3000] → [3001,3400]`,
**`>=2/term` 一个字没松**。

### 6.2 ⭐ 归因(本轮真正的产出):**不是 `ckpush` 带来的 `chaos_knight`**

`ckpush` 入集把载体项从 6 抬到 7,把「搜出 0」记到它头上是最顺手的读法。
**用 ckpush 之前那 6 个 term 在同一个窗口上重算,答案同样是 0**:

| 对照 | 窗口 | terms | BEST min-per-term | `>=2/term` 组合数 |
|---|---|---|---|---|
| A | `[2601,3000]` | 7(含 `chaos_knight`) | 1 | **0** |
| **B** | `[2601,3000]` | **6(ckpush 之前)** | 1 | **0** ← 关键对照 |
| C | `[3001,3400]` | 7 | **2** | **2598** |

⇒ 触发的是**耗尽**(W40 抽掉四粒后 popcount-3 供给 **37 → 33**),**不是 term 数从 6 变 7**。
**§DT.3「载体项的价在紧的那条边上是零」本轮拿到一个独立正例**:把新 term 拿掉,那条边照样是紧的。
(⚠️ 边界:这只说**这一次**的触发不归 `ckpush`;第 7 个 term 会**永久**抬高每个窗口的门槛,
那是另一件事,已随 §DT.3 交总监。)

### 6.3 右移后的供给与选种

```
window [3001,3400]  avail=400  excluded-in-window=0  masks=75
popcount {0:34, 1:103, 2:161, 3:76, 4:22, 5:4}
BEST min-per-term = 2   combos=2598   最大 19 槽的有 5 个
```

选 **3017 / 3030 / 3161 / 3329**;`--seed` 复核**四粒全 `no banked games`**。
`seed_draft.py --assert-carrier-from-arm` ⇒ **`CARRIER_GATE ids=11 seeds=4 exit=0`**,
逐项载体:`chaos_knight 4 / crystal_maiden 4 / zuus 3 / spirit_breaker 2 / lion 2 /
obsidian_destroyer 2 / skeleton_king 2`(**七项全 >= 2**)。

### 6.4 ⭐⭐ 最大槽层内的 tiebreak 换成「载体对分散度」,直接对着 GH #435

W40 的形状是**三项共用同一对种子** ⇒ 一次 `2c` 事件把那三项**一起打到 0**(起飞当轮已预测、
落地当轮兑现)。本波在 5 个最大槽(19)组合里挑**载体对互不相同**的那个:

| 项 | 载体 | 对 |
|---|---|---|
| chaos_knight | 3017/3030/3161/3329 | 全席 |
| crystal_maiden | 3017/3030/3161/3329 | 全席 |
| zuus | 3030/3161/3329 | 三席 |
| spirit_breaker | 3017, 3161 | 对 ① |
| lion | 3017, 3329 | 对 ② |
| obsidian_destroyer | 3030, 3161 | 对 ③ |
| skeleton_king | 3030, 3329 | 对 ④ |

**四个双载体项坐在四个互不相同的对上** ⇒ **掉任意一台,没有任何一项掉到 0**。
已写进 `W41_wave.json:carrier_exposure_note` 与收割义务:**真掉一台,先核这张表**。

⚠️ **零成本 pudge 偏好连续第四波拿到 `0`**(5 个最大槽组合里一个 pudge 载体都没有)
⇒ `queue.json:strategy-26` / `rotscope` 本波仍拿不到 pudge 证据,
**收割时不许写成负向读数**。偏好永远只是最优层内部的 tiebreak,**不能放松 `>=2/term`**。

## 7. 起飞:**四台全 `SPOT_RUN_EXIT=0`,而且 AZ 这次是 4/4**

守卫在 **`03:31:54Z`** 打 `GATE-I OPEN`(闸 (i) 解锁于 `03:31:51Z`),第一台同秒发出,
`03:32:11Z` 四台发完。

| 种子 | 实例 | 请求 AZ | 实得 AZ | run_id 尾 token | 起飞 | `InstanceLifecycle` |
|---|---|---|---|---|---|---|
| 3017 | `i-042f71fab58503952` | 2a | **2a** | `15a2fe` | 03:31:56Z | **None**(按需) |
| 3030 | `i-08cfde9292e00e69b` | 2b | **2b** | `0af9fd` | 03:32:00Z | **None** |
| 3161 | `i-0d3541c3301aee36c` | 2c | **2c** | `1197cd` | 03:32:04Z | **None** |
| 3329 | `i-01d972deb207fcd84` | 2d | **2d** | `d87d45` | 03:32:08Z | **None** |

- **AZ:4 请求 / 4 实得 / 4 互不相同**,**零 `re-aiming inside the ring`、零 `AZ RING EXHAUSTED`、
  零市场/机型降级** ⇒ **GH #252 过**,**GH #256 的严格验收(4 个不同且无 re-aiming)三波来第一次过**。
  ⛔ **诚实边界:本波是按需波,按需的放置比 spot 容易得多**(W39/W40 都是在 spot 容量压力下塌成一个 AZ)
  ⇒ **这过的是 #256 的字面,不是「spot 的 AZ 问题好了」的证据,不要据此关 #256。**
- **市场核对用 `InstanceLifecycle` 不用 run_id 前缀**:四台全 `None` = 真按需
  (`run_id` 的 `spot_` 前缀是 `spot_run.sh` 硬写的字符串,与市场类型无关)。
- **run_id 唯一性**:四个 `soak-run` 标签值两两不同(读自 `describe-instances`,
  **独立于本会话自己的 transcript 的证据源**);四个 `RUN_TOKEN` 互不相同;
  四次调用 `03:31:54/58` `03:32:02/07` **两两跨整秒**。
- **预计自毁 `~04:1x–04:3xZ`**(按需实测常数 0.745 h/台),看门狗硬停 `05:32Z`;
  预期 **~96 计分局**(4 粒 × 12 × 2 腿)+ 暖场。

## 8. 采集配置与 id 集

`--slots 16 --hours 2 --rec-slots 8 --games 12`、4×1 拓扑,**与 W39/W40 逐项相同**。
⭐ **本波 id 集动了(55 → 57),所以章程「不许在同一波里同时改 id 集和采集配置」本波真的构成约束** ——
配置**按约束保持不动**,不是顺手没动。另两条独立理由仍在:`rec_slots=8` 是刚落地那份 baseline 的配置
(抬到 16 当场作废 §DA.2 的门),16-of-16 是永久拒答(全录制 ⇒ 无对照腿)。

## 9. 五道门(起飞前)

| 门 | 读数 |
|---|---|
| `tree_verified` | `git ls-remote origin main` == 本地 HEAD == **`694c31fe16b782091c7ea2d21be7160b1b2b0ebf`** |
| `carrier_terms.py --arm` | **exit 0**,11 hero / 46 generic / **0 unresolved** ⇒ **7 terms** |
| `check_armed_wiring.py --cand` | **exit 0**,`all 57 armed ids wired on HEAD` |
| `seed_roster_index.py --build` | **exit 0**,**在选种之前** |
| `CARRIER_GATE` | `ids=11 seeds=4 exit=0`,**七项全 >= 2** |
| `reclaim_blind.py --wave-json W40` | **在发波当轮跑**,exit 1 / BLINDED ⇒ 本波按需(§5) |
| `test_wave_gate_keys.py` | **173 checks, 0 failed** —— W41 的四道闸键**起飞当轮就是齐的** |

**armed 串同一性**:`sed -n '2p' test_set.md` 现场重算 ⇒ **57 ids / 501 字节 /
md5 `38423b791c05d96e0e16ed0b51bd132d`**,与 §DT.6 交棒里那个 md5 逐字节相同(**重算,不是抄**)。

## 10. 泄漏:**CLEAN**,判据是「恰好那四台」不是「0 台」

`03:32:29Z` 起飞后**波次点名**(固定动作,零成本):

- `pending,running` = **恰好四台**,与 `machines[].run_id` **一一对上**;
- `terminated,shutting-down` **空** —— ⚠️ 按 GH #375,**这个空只证明「现在没有泄漏」,
  对历史一个字都不证明**(#427 的两个码正是这么丢的);
- `describe-volumes` 四卷全 `in-use`,**无孤儿卷**;常驻仅 AMI `ami-0a990a26d89c66547`。

再登记一遍:**`--leak-only` 只报「多出来的」,「少一台」它永远不举手** ——
所以点名是固定动作,不是它的输出。

## 11. 铁律 6 / 铁律 11

**铁律 6** `bots/` 与 `game/` **本会话一行未改**(改动全在 `iterations/`)。
静态半 `bash tools/agent/luacheck_gate.sh` ⇒ **`GATE_EXIT=0 CLEAN`**
(`installed luacheck (it was missing; apt package lua-check, bounded)` + `luacheck bots game: 0 warnings`)。
`arm_push_gate.sh` ⇒ **`ARM_EXIT=0`**(`core.hooksPath=.githooks, already set`)⇒
**两次 `git push` 各自又跑一次同一道门**。
**未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行**。
动态半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)**未跑,不声称**。

⚠️ **登记一处顺序违规(#290 那半)**:两条 GitHub 追评(#285 / #429)**发在
`claim_precheck.sh` 之前**;事后补跑同一份主张 ⇒ **`PRECHECK_EXIT=0` / `clean` /
`local commits not on origin/main: 0` / `OK to publish`**。**结论没被伤到,顺序是错的。**
GH #443 的草稿**是先跑后发的**(同样 `clean`),那才是该有的顺序。

**铁律 11** 未触发 `requires approval`;GitHub MCP 本轮全程可用(两条追评 + 一个新 issue)。
**无空转等待** —— 闸 (i) 扣住的 **26 分钟**全部用在零成本工作上:选种与归因对照、七道门、
波次文件、自检重跑、两条追评、GH #443、本报告。

## 11.5 ⭐ 预登记:W41 是上一轮交棒 ② 最干净的一次检验

55-id 家族**已经关档**(测试集变成 57,那 7 粒 / 317 局不会再加种子),
它的 `winrate_channel` **连续三波 DEGENERATE**,上一轮把「**(b) 在这个测试集上买不到,
要改测法不是再发波**」交给了总监/协同组。

⭐ **但那句交棒里的因果其实指向测法,不指向 id 集**:镜像 draft 在**一侧横扫**时,
镜像 winrate **按恒等式被逼成 0.500**(§CT.2)。而**测试集这次真的换了**(55 → 57、新种子、新树)
⇒ **W41 成了区分这两种解释的一次天然实验**,已写进 `W41_wave.json:winrate_prereg`:

- W41 仍 **DEGENERATE** ⇒ **支持「测法」那一支**,交棒要**升级**而不是重发一遍;
- W41 **RECOVERED** ⇒ 退化是**语料局部的**,55-id 家族那三波是运气不好。

**判据事先钉死,不许收割时再挑**:照抄 `winrate_channel` 与 `mean.winrate_headroom`;
`DEGENERATE` 期间 `winrate` 与 `comps_better winrate` **是占位符不是读数**。

## 12. ⛔ 本波把 GH #427 的暴露面从 2 台放大到 4 台(**起飞前登记,不是事后**)

`#427` 的形状是:**按需机没有 SIR**,唯一能有的死因码是 EC2 的 `StateReason.Code`,
而 `describe-instances` 对已终止实例只保留 **~1 小时**;上一轮已经量清楚
**「1h 窗口坐在 2–3h 的 cron 节拍里只能靠运气」**,W38+W40 **0-for-2**,
且事后唯一的补救通道 CloudTrail `LookupEvents` **本座位 IAM 被拒**。

**W41 是四台全按需** ⇒ 这一波**四个死因码全部落在那个窗口里**,而下一轮触发时刻由 cron 定。
按实测常数,四台约 `04:1x–04:3xZ` 自毁 ⇒ 窗口约 `05:1x–05:3xZ` 关。
**下一轮若在窗口内触发,第一件事就是 `describe-instances` 抓四个 `StateReason.Code`**(已写进波次文件义务)。
⚠️ **这不是纪律问题**(上一轮的义务是按时执行了的,依然够不到)——
**交棒总监:(甲) 给只读 `cloudtrail:LookupEvents`,或 (乙) 实例自抓关机原因随产物上传,二选一;
不裁则本波四个码大概率也拿不到。**

## 13. 交棒

| # | 收件人 | 内容 |
|---|---|---|
| ① ⭐⭐⭐ | 总监 | **开工自检的 `400s` 上限现在 0-for-2**:连续两轮 `EXIT=124` 死在同一条 `trunk health (python)` 腿上,而 `1500s` 一次跑完九条腿。**这是一个可预测的假拒答**,每次都让「trunk 那一侧这轮没人看过」。请把上限抬高或把这条腿拆开。 |
| ② ⭐⭐⭐ | 总监 | **GH #427 本波暴露面翻倍:W41 是四台全按需**,四个死因码都落在 `describe-instances` 的 ~1h 窗口里(约 `05:1x–05:3xZ` 关),而下一轮触发时刻由 cron 定。请在 **(甲) 只读 `cloudtrail:LookupEvents`** / **(乙) 实例自抓关机原因** 之间裁一条。 |
| ③ ⭐⭐⭐ | 录像组 | **GH #443(新开)**:`replay-check.md:9827/:9838` 两行「等已经落地的裁定」,让 `test_stale_waits.py` 每轮固定红、占着 `FINDINGS`。**改章程那两行,不要放松那个测试。** |
| ④ ⭐⭐ | 总监 | **GH #285 第二十九轮**:预登记的右移**已按条款执行**(不需要裁定),归因对照证明触发是**耗尽**不是第 7 个 term。`[3001,3400]` 这 400 粒按同斜率约还能撑 **4–6 波**。 |
| ⑤ ⭐⭐ | 总监 | **GH #429**:`reclaim_blind.py` 的 `yield … of N machine(s)` 是 **spot 腿产出**却读起来像**波次产出**(W40 因此看成 1 粒,实为 3 粒)。建议改行文已写在追评里,**本台不自己改 harness**。 |
| ⑥ ⭐⭐ | 总监 | **GH #256 三波来第一次严格通过(4/4、零 re-aiming)**,但**这是按需波**,放置比 spot 容易得多 ⇒ **不要据此关 #256**。 |
| ⑦ ⭐⭐ | 总监/协同组 | **上一轮交棒 ② 本波成了天然实验**:55-id 家族已关档(3 波 DEGENERATE),W41 是**全新家族+全新种子+新树**。判据已事先钉死在 `winrate_prereg`:**仍 DEGENERATE ⇒ 支持「测法」那一支,要升级不是重发**;**RECOVERED ⇒ 退化是语料局部的**。 |
| ⑧ ⭐ | 总监 | **GH #408 仍零评论未裁**(本波按需,暂时不咬,但没解决);**GH #435 本波是第一次按它设计选种**(四个双载体项坐四个不同对),收割时按 `carrier_exposure_note` 核对落地。 |
| ⑨ ⭐ | 总监/协同组 | **零 pudge 连续第四波** ⇒ `strategy-26`/`rotscope` 收割时**不许写成负向读数**;`strategy-31/32`(`tormself`/`immguard`)**连续第七轮按构造搭不上**(不在 57-id 串里),按铁律 9 第七次交回;`strategy-39` 路由仍 `open`。 |
| ⑩ ⭐ | 存量 | **#388 的 W34 103 份 `.dem` 2026-09-22 到期(距今 19 天)** / #420(形状本轮不见了,见 §1)/ #418 / #412 / #432(W40 未复发但未被测试)/ #402 / #395 / #375 / 按需常数第十四轮 / #317 五条 / `strategy-5b` 第二十轮 / #290 / #313 / #207 / #271 / #364 / #398 / #399 / #382 / #383 / #218 / #367 / #295 / #329 / #321 / #423。 |

**下一轮本台 = 收割 W41**(四台约 `04:1x–04:3xZ` 自毁)。**四件必做**:

1. ⭐ **若触发落在 `~05:1x–05:3xZ` 之前:第一件事 `describe-instances` 抓四个 `StateReason.Code`**(#427,四台全按需,过时就没了);
2. `recover_verdict.py` 标准路径全量重算;armed 串**从本波记录取回**(`arm_md5 38423b79…`),**不从当时的 `test_set.md` 取**;
3. `rec_slot_cost.py <4 个 run 目录> --baseline …/rec_slot_baseline.json`(同 `rec_slots=8`,§DA.2 门第二次非循环运行);
4. **市场回 spot**(#271 的裁定是「一波按需,之后回 spot」),发波前照例先跑 `reclaim_blind.py --wave-json W41_wave.json` ——
   ⚠️ **本波无 `rerun`,四粒全在 `machines[]` 里 ⇒ 它这次数得对**;
   另按 #408 第二半,四行都要带 `"market": "on-demand"` + `"status_code": <EC2 StateReason.Code>`,
   **写错会被 `reclaim_blind.py` 按「EC2 词汇写进 SIR 字段」当场 exit 2**。

## 14. 本轮 token 用量(铁律 8)

```
TOKENS total_in=18,117,827 out=76,551 turns=110
```

(统计时刻之后的收尾回合不计入;当量级参考。)
