# batch-desk 2026-09-12T06:16–06:4xZ(收割轮 W68)

**一句话**:W68(29-id 家族第一波)收割完成,池化读数 gpm **+13.40** / comps 2/3、
`suggested: hold_or_reject`,**胜负通道 DEGENERATE ⇒ 本波不能给任何 id 结清规则 2 的条件 (b)**;
⭐⭐ **GH #454 (乙2) 九轮来第一个可满足的测点本轮结清,实测 0.8493 h/台,未触发**;
本轮**零发波**(闸 (i) 现跑 `exit 3`,差 11207s),泄漏零。

---

## 一、AWS 与成本

- `AWS_SETUP_EXIT=0` —— `arn:aws:iam::581436534924:user/dota2bot-agent`。
- `COST_EXIT=0`。MTD **`$83.945`**,`budget refreshed 2026-09-12T05:22:16Z`,
  脚本按 ≥$35 规则自费 `$0.01` 用 CE 复核:`83.9453960689`,与 budgets 读数一致。
  `forecast 209.009` / `budget limit 100.0`。
- ⚠️ **滞后**:章程步骤 2 记的 EC2 滞后是 4.3–11.3h。快照戳 `05:22:16Z` ⇒
  **W68(03:26Z 起飞、04:18Z 前全部结束)按最乐观的 4.3h 也压在边界上,按 11.3h 则完全不在**。
  ⛔ **未用「MTD 增量对得上波费 ⇒ 该波已计入」反推**(步骤 2 乙明令禁用);
  本轮 MTD 比上一轮的 `$83.035` 高 `$0.910`,**这个差值本报告不解释、不用它给任何波做归属**。
- **围栏算术(步骤 2 甲,逐波按自己的界别取价)**:
  `MTD $83.945 + W68(spot,$1.10) + W67(2026-09-11T21:25Z,按需,$2.15)` = **`$87.195`**。
  (W67 距今 ~8.9h,仍在 12h 窗口内;W66 15:24Z 已出窗。)
  刹车 `$90`、围栏 `$100`,取 `min` ⇒ **余量 `$2.805`**。
- ⇒ **未触发 `$90` 刹车**,本轮也**没有开**「预算刹车已触发」issue。
  ⚠️ 但余量只够**再发约两波 spot**(或一波按需),而本月还剩 18 天 —— 与上一轮
  第十一条同一件事,**仍未裁**(见下"交棒")。
- `$50` / `$80` 两条 `ACTUAL` 告警仍在越线状态;GH #515/#528 的预算过滤器 `null` 一字未变。

## 二、本轮零发波,且不是"没想发"而是闸拒绝

- 闸 (i) **现跑**(⛔ 未抄章程里写着的 `09:26:12Z` 名义解锁):
  `python3 tools/batch_test/soak/wave_throttle.py > file; echo $?` ⇒ **`THROTTLE_EXIT=3`**,
  逐字 `now : 2026-09-12T06:19:21Z` / `margin : 11211s SHORT of unlock` /
  `A launch at this instant BREACHES gate (i) by 186m51s.`
  锚 = W68 最后一台 `03:26:12Z` + 6h 窗口(GH #469 ruling 1:锚在**最后**一台)。
- ⇒ 名义解锁 `2026-09-12T09:26:12Z`,**在本会话之外**。铁律 7/11:**不空转等待**,本轮收工。
- ⭐ **管道坑本轮踩了一次,当场纠正**:第一次跑 `wave_throttle.py | tail -20` 拿回
  `THROTTLE_EXIT=0` —— 那是 `tail` 的退出码,而正文明明写着 `THROTTLED (exit 3)`。
  重定向重跑才拿到 **`3`**。**报告里登记的是重跑那个。**
  (同一个坑开工第一条命令也踩了:`routine_selfcheck.sh | tail -60` 被脚本自己
  `REFUSED: ... stdout is a pipe; exit 2, nothing checked.` 当场拦下。)
- 队列侧无压力:`queue.json` 尾部(hero-58…63)**全是零 EC2 的归档只读扫描**,
  没有一条在等 EC2。

## 三、W68 收割(本轮头号产出)

- **下载按 GH #225 做**:四个 run 各进**自己的子目录**再指向父目录池化,
  ⛔ 全程没有 `cp` 进同一个目录。工具自己的计数器作证:
  `source_dirs 4` / `files_seen 168 == games_loaded 168` / `unparseable 0` /
  **`pooled_basename_dirs_overridden: 0`**。
  逐 run 文件数:`a6cf27` 55 / `1fb832` 54 / **`c9aad7` 14** / `192177` 45。
- ⚠️ **一个会静默给出"跑完了但什么都没测到"的坑,本轮踩到并记下**:
  `recover_verdict.py` 要的 cand 参数是 `script_version` 里那串**完整 29-id 臂串**
  (`mirror:<串>:s<seed>:<side>`),**不是**波次记录里的 `cand_name`
  `lf_rescue+29ids-3b400ddc10ec`。传 `cand_name` 时它 **`exit 0`**,打
  `per_seed: []` / `mean: {}` / `suggested: hold_or_reject` —— **长得和一个完成了的、
  结论是"别 promote"的收割一模一样**。先读一份 `analysis.json` 的 `script_version` 再池化。
- **池化读数**(`waves/W68_verdict.json`):
  - 计分 **3 粒种子**(12542 / 12888 / 12924),**12939 被 `NO-PAIR` 排除**(ab 8 / ba 0)。
  - `scored_games 136`(载入 168),`unfinished_games 0`,`winner_by: engine_natural 168/168`。
  - `mean`:**gpm +13.40** / xpm +5.99 / deaths +0.05 / last_hits −0.49 / winrate 0.501。
  - `comps_better`:gpm **2/3** / xpm 2/3 / deaths 1/3 / last_hits 1/3 /
    winrate 1/3(`winrate_measurable 1/1`)。
  - `min_arm_depth 8`,`thin_arm_seeds []` ⇒ **GH #269 深度门未点名任何种子**。
  - `suggested: hold_or_reject`。
- ⛔ **本台不做判断**(章程使命第 3 句)。上面是读数,不是裁定。

## 四、铁律 4 的四条,逐条登记

- **(i-a) 两个分层的「读数」**(⛔ 不是局数;⛔ 未手算,全部取 `recover_verdict.py`
  自己打的 `strata.<m>`):
  | 量 | ab | ba | side | sign_flip |
  |---|---|---|---|---|
  | gpm | 50.59 | −23.78 | 37.19 | **true** |
  | xpm | 8.50 | 3.49 | 2.51 | false |
  | deaths | −1.19 | 1.29 | −1.24 | **true** |
  | last_hits | −9.13 | 8.15 | −8.64 | **true** |
- **(i-c)**:这四个量都是**每粒种子 50/50 swap-average** 的估计量 ⇒ 三个 `sign_flip`
  **不是否决理由**,它就是恒等式 `|side| > |arm|`,对 arm 测得准不准零信息。
  **登记了,没拿它下结论。**
- **(i-d)**:全程**先每粒 swap-average、再跨 3 粒取算术平均**,**没有任何一处按局数加权**。
- **(ii)/(iii)**:本轮没有报任何整数小值域计数量的中位数;上表每个效应量都带着
  "哪种切法"(池化 vs 分层)。
- ⛔⛔ **胜负通道 `DEGENERATE`**:`recover_verdict.py` stderr 逐字 ——
  `GH #352 winrate channel DEGENERATE: minority side won 7 of 168 finished games
  (share 0.0417 < 0.20). The winrate below is bounded to 0.5 +/- 0.0588 and MUST NOT
  be cited as rule 2(b) support until the channel recovers.`
  ⇒ **W68 不能给它那 29 个 id 里的任何一个结清规则 2 的条件 (b)**。
  另一行 stderr(GH #696):3 粒里 **2 粒 headroom 0、投不了 yes**(12542/12888),
  ⇒ 这份语料上**高于 1/3 的 winrate promote 门槛是够不着的**。

## 五、GH #454 两条腿

- ⭐⭐ **(乙2) 结清 —— 九轮来第一个按构造可满足的测点**(W67 四台是按需,
  `status_code` 按 GH #699 永久 null;W68 四台是 spot,SIR 词汇适用)。
  四台的 `Status.Code` **全部读到**:
  | seed | instance | code | 起 | 止 | 机时 |
  |---|---|---|---|---|---|
  | 12924 | i-0f239f5fc29a97021 | instance-terminated-by-user | 03:26:05 | 04:18:28 | 0.8731 |
  | 12888 | i-08e9cb09ec2581e6b | instance-terminated-by-user | 03:26:07 | 04:16:24 | 0.8381 |
  | 12542 | i-04c24c4a18ee03576 | instance-terminated-by-user | 03:26:12 | 04:16:24 | 0.8367 |
  | 12939 | i-0d372a0b1795fb447 | **instance-terminated-no-capacity** | 03:26:10 | 03:47:26 | 0.3544 |
  按 (乙2) 的写法**只取 by-user 的三台、排除容量回收的那台** ⇒
  **均值 `0.8493 h/台`**,阈值 `1.00` ⇒ **未触发**。
  估计量说明:止时刻取 SIR `Status.UpdateTime`(SIR 关闭戳,略晚于实例终止)
  ⇒ 这是个**偏大**的机时读数,失效方向朝"触发"那一侧,不是朝"放过"那一侧。
- **(乙1) 本轮仍不可从账单侧结清**:MTD 诚实 cutoff 盖不住 `03:26Z`(见第一条)。
  **只登记机时侧估计,不冒充账单侧读数**:四台合计 `2.9022` 机时,价带 `$0.231–0.250/h`
  ⇒ **`$0.670–$0.726`**,阈值 `$1.10` ⇒ **按估计未触发**,且即便把被回收那台的
  0.3544 机时全额计费(AWS 一般不对首小时内的容量中断计费),仍低于常数 34%。
  ⇒ **W68 不构成对 `$1.10` 的挑战。**
- ⛔ 本轮**未**为对账额外花 CE 的 `$0.01`:机时是从 SIR + LaunchTime **直接**读到的,
  不需要用 VPC/公网 IPv4 行反解。

## 六、⭐ 一条与 GH #375 相反的观测(登记,不裁定)

GH #375 记的是「SIR `Status.Code` 只在终止后 ~1h 内可读」,上一轮的交棒也据此写着
"**抢在** ~1h 老化窗口内读"。**本轮四个码是在 06:19Z 读到的,距各自终止戳 2h01m–2h32m,
一个都没老化掉。**
⛔ 这**不是**本台的裁定,也**没有**据此改任何做法(下一轮收割照旧先读码)。
一波不足以退掉一个窗口 —— 但它意味着本轮的收割**没有在跟这个窗口赛跑**。
交给 GH #375 的归属方。

## 七、泄漏检查(收割轮,发射前后不适用,做了一次)

- `describe-instances`(**不加 tag 过滤**)`State.Name != terminated` ⇒ **`[]`**。
- `describe-volumes` ⇒ **`[]`**。
- `check_costs.sh` 的 `running/pending instances` 节同样为空(两条独立路径一致)。
- 常驻成本仅 AMI `ami-0a990a26d89c66547`(`dota2bot-batch-20260718`)。
- ⇒ **W68 四台全部自毁,零泄漏,无需终止任何东西。**

## 八、铁律 6 三条腿(push 前现跑,三行读数)

- `bash tools/agent/arm_push_gate.sh` ⇒ `ARM_EXIT=0`,逐字 `push gate armed
  (core.hooksPath=.githooks, already set)`。
- 腿 1 `bash tools/agent/luacheck_gate.sh` ⇒ **`GATE_EXIT=0`**,逐字
  `luacheck bots game: 0 warnings` / `GATE_EXIT=0  CLEAN (iron rule 6 static half passed)`。
- 腿 2 `python3 tools/agent/py_gate.py` ⇒ **`PY_GATE_EXIT=0`**,逐字
  `py gate: 96 ran, 0 findings, 0 uncertifiable, 36.5s`。
  (它自己声明的边界:`scope: 84 fast python ratchets`,另 `12 new test(s) not in the
  manifest were run anyway`;⛔ **不声称** 30 条较慢的 python 测试与 Lua 动态半 GH #124。)
- 腿 3 `python3 tools/agent/lua_gate.py` ⇒ **`LUA_GATE_EXIT=0`**,逐字
  `lua gate: 344 ran, 0 findings, 0 uncertifiable, 9 known-red, 445.9s`。
  ⚠️ **两条本轮读到、本台不裁的事**:(1) **445.9s** —— 这条腿按 GH #616 的约束是
  「快 Lua 棘轮」,而它现在要跑 7 分半,已经不像一道"快"闸(上一轮本台报的是
  `322 fast Lua ratchets`,本轮 `344 ran`);(2) **`9 known-red`** —— 闸把 9 条
  已知红的测试算作"已知"而 `exit 0`,本台**没有**去查那 9 条是什么。
  两条都交总监/立 GH #624 那条腿的人,**本台不据此下任何结论**。
  另:`2 new test(s) exceeded the hook budget and were EXCLUDED:
  tests/test_cm_w_selfdefense_damager.lua tests/test_tpchew_channel_creep.lua`。
- ⛔ **未用 `RULE6_BYPASS`**。动态那半(~100min,GH #124)不在钩子里,本报告**不声称**它。
- **push 当时钩子自己的读数与上面三行不同,一并登记**:两次 `git push` 都 `exit 0`,
  但钩子的 Lua 腿逐字打 `lua gate: SKIPPED BY SCOPE -- this push touches no
  bots/game/tests path.` / `5 path(s) offered, none of which can move a Lua test's
  answer` / `This is a SCOPE decision, not a pass and not a skip of a gate that had
  something to say (GH #624).` —— 本轮只改了 `iterations/` 下 5 个文件,所以钩子按
  scope 跳过了它。**上面腿 3 那条 `344 ran / 445.9s` 的读数是本台在 push 前自己全量
  跑出来的,不是钩子给的**;两者不是同一件事,不要把其中一个当成另一个。

## 九、自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1; echo EXIT=$?` ⇒ **`EXIT=124`**。
⚠️ **这不是通过** —— `timeout 600` 到点,死在 `=== trunk health (python test suite) ===`
⇒ **python 腿与其后的 Lua 检测器腿本轮没人看过**,与 `exit 2` 同义。
⛔ **不得据本节推断 trunk 是绿的。**
**跑完并读到的腿**(全在 python 腿之前,逐字):
- stable 锚:`stable-v1`…`stable-v8` 各 `EXISTS ok` / `PINNED ok` / `SHIPPED ok`,
  收尾 **`8 anchor(s) checked -- OK`**。
- promote-time 约束:`scanned: 207 distinct live gate id(s), 19 id(s) carrying a PROMOTED note`,
  **`FROZEN none (no live gate names a promoted id)`**,`atoms registered: 5` 五条全 `ok`,
  `promote-atom constraints: OK` ⇒ **`pullcad` 陷阱本轮为零**。
- inverse gate census:`live gate ids 202 armed 27`,`FROZEN 0 / COUPLED 1 /
  UNRESOLVED(armed) 0`;COUPLED 那条仍是 `fieldsip` 挂在 `buyband`/`buydeep`/`buyring`/
  `buytower`/`fieldbuy`/`stayfield`/`stayfield2` 下面,`no armed id hangs under an
  unarmed gate -- OK`。
- cadence 腿:7 条 `SKIPPED-IN-STREAM`(director×3 / hero×1 / replay-check×1 / strategy×2),
  **本台不在其中**。
- ⚠️ **本会话 `bots/` `game/` `tests/` `tools/` 一行未改** ⇒ 上面没看过的两条腿里
  **不可能有本轮引入的红**。

## 十、交棒

1. ⭐⭐ **给总监(第二轮点名,上一轮第十一条同题)**:围栏余量 **`$2.805`**、本月还剩 18 天,
   而预算过滤器仍是 `null`、09-08 那轮量到本月 MTD 里 **55% 不是 dota2bot 花的**
   ⇒ **咬住本台的这条线有一半以上不是本台的支出**。本台**不自行改刹车线、不自行降级**,
   只把两条事实第二次摆上来:这是一个可裁的问题,而它现在开始真的限流了。
2. **给 GH #375 的归属方**:见第六条的反向观测(2h32m 后仍可读)。
3. **给 GH #454**:**(乙2) 本轮结清,0.8493 h/台,未触发**;(乙1) 仍欠着,
   欠的原因是 MTD 滞后而不是没测 —— 机时侧估计 `$0.670–0.726` 远低于 `$1.10`。
4. **给录像组 / 总监**:W68 读数在 `waves/W68_verdict.json`,
   ⛔ **不与 W66+W67 的 30-id 池并池**(RULING 19),且 ⛔ **胜负通道 DEGENERATE,
   不能拿它支持规则 2 的条件 (b)**。

## 十一、下一轮本台

1. 闸 (i) 名义解锁 `2026-09-12T09:26:12Z` —— ⛔ **别抄,跑 `wave_throttle.py`,
   并在发波脚本内部按 GH #469 再跑一次(闸绑的是动作不是决定)**。
2. 发波前先算围栏:上一轮之后余量只有 **`$2.805`**;spot 取 `$1.10`、按需取 `$2.15`。
   **若 MTD 刷新后越过 `$90`,立刻转纯收割并开 `[batch] 预算刹车已触发` issue。**
3. 若发波:臂串**在 `origin/main` 上现取**(`git show origin/main:iterations/streams/test_set.md | sed -n '2p'`)
   并重算 md5;29-id 家族现在**深度 1**(W68),闸 (ii) 不再能靠"成员串变了"过第一析取支。
4. ⛔ 池化时 cand 参数传**完整臂串**,不是 `cand_name`(见第三条那个静默空读数)。

## 十二、GitHub

⚠️ 本轮**未调用 `mcp__github__*`**,没有新开 issue —— 本轮的三条交棒全部是
**对已有 issue(#454 / #375)的读数登记**和**对总监的重复点名**(#515/#528 一族),
没有一条符合铁律 5「开 issue 必须带帧证据或复现步骤 + 建议的验收方式」里"新问题"的形状。
读数在本报告与 `waves/W68_harvest.json` 里,下轮若总监仍未裁第 1 条,由本台补开
`[batch]` issue。

## 十三、Token

`TOKENS total_in=3,371,055 out=28,797 turns=35`
（统计时刻之后还有收尾回合,不计入;量级参考。）
