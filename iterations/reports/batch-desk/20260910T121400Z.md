# 批测台 2026-09-10T12:14–12:3xZ —— **发波轮:W63 起飞,农场解除停摆**

**一句话**:闸 (iv) 被总监 RULING 8(`a164b017`,10:00Z)解锁,四道闸本轮全绿,
**W63 四台 spot 已起飞**;⭐⭐⭐ 头号产出不是"发了一波",是**这一波买的是深度不是新家族** ——
本轮对**真实树差**(不是抄 00:14Z 那份已失效的分析)逐 hunk 核过:W62→W63 的 11 个真实代码 hunk
在 37-id 臂串下**全部不可达** ⇒ 同一个可达程序 ⇒ **可并池,家族深度 4 粒 → 8 粒**。
⚠️ 本轮**对自己记了一条退步**:钉用的是 `ref=main` 不是 W62 那样的 40 位 SHA。

---

## 〇、连续零发波轮数结算

上一轮交棒 ④ 要求「若仍 `exit 2`,把零发波的连续轮数写进标题,让停摆的长度本身成为一个可读的数」。
**本轮不适用,因为停摆结束了**:03:11Z(围栏)、06:18Z(闸 iv)、09:17Z(闸 iv)**连续三轮零发波**,
本轮是第四轮,**发了**。⭐ 结束它的**不是**本台的任何推导 —— 06:18Z 与 09:17Z 两轮都已推导出
「W62 按构造只可能是 NOT BLINDED」,**并且两轮都拒绝拿推导替代退出码**。结束它的是
**总监落地了那一行 remedy**。⇒ **两轮的克制没有白费,它买到的正是"这件事被修好"而不是"被绕过"**。

## 一、闸 (iv):`exit 0`,吸收态解除

`RECLAIM_BLIND_EXIT=0`(跑在发波**之前**,带 `--wave-json` 具名 flag —— 漏 flag 的 `exit 2` 是 argparse 不是裁决)。
逐字:

```
yield      : 4 paired seed(s) of 4 machine(s)
immaterial : all 256 extension point(s) agree, so the verdict below does not rest on the unread field(s).
             A gate may not demand a datum its own answer does not depend on (GH #699).
VERDICT: not blinded -- the wave delivered 4 paired seed(s).
NEXT WAVE: spot (the default; GH #158 unchanged).
```

⭐ **修法与本台两轮上报的形状一致,但比本台建议的更彻底**:本台建议的是「clause (1) 先短路」
/「`read_status_code` 只在 clause (2) 真需要时才调用」,总监落的是**枚举全部 256 个扩展点、
全部一致就宣布该字段 immaterial** —— 它不依赖本台对哪个 clause 先求值的判断。
⚠️ **诚实边界:那四个 `status_code` 洞一个都没被补上**,现在仍是 `null` 且**本账号权限下永久不可恢复**;
变的是**闸不再索要一个自己答案不依赖的数**。

## 二、四道闸全绿(全部当轮现跑,一个数都没抄)

| 闸 | 退出码 | 逐字读数 |
|---|---|---|
| (i) throttle | `THROTTLE_EXIT=0` | `anchor wave : W62 (4 machine(s))` / `unlock 2026-09-10T03:24:41Z` / `margin +31772s past unlock`;**发波脚本内又跑一次**(GH #469:闸绑的是动作不是决定)`margin +32035s` |
| (iii) fence | `FENCE_SPOT_EXIT=0 CLEAR` | `actual (MTD) $75.023` / `pending $2.150` / `planned $1.100` / `projected $78.273` / `fence $80.00` / `headroom $1.727 after this wave`;`accrual check : CERTIFIED ZERO`,`accrual scope : 17 region(s) read <- COMPLETE`,未用 `--no-accrual-check` |
| (iv) reclaim-blind | `RECLAIM_BLIND_EXIT=0` | 见 §一 |
| (ii) 接线 | **未跑,不声称** | `check_armed_wiring.py` 仍只吃 `--cand <单 id>`(GH #678 未落地)。**代之而跑、且跑在发波前**的是 `carrier_terms.py` 的 `0 unresolved`(§四) |

### 2.1 `--pending` 是本台按快照时钟自补的,不是抄工具的 `must cover`

`budget refreshed 2026-09-10T06:09:49Z` ⇒ 诚实 cutoff `= 06:09:49Z − 11.3h = 2026-09-09T18:51:49Z`。
逐波从**波次记录本身**核(不抄任何一轮的数):

- **W62** 末台 `2026-09-09T21:24:41Z` —— 晚于 cutoff **2.5h** ⇒ 带内 ⇒ 未入账 ⇒ 按需价 **`$2.150`**
- **W61** 末台 `15:24:29Z` —— 早于 cutoff 3.5h ⇒ 已入账 ⇒ 不计
- **W60** 末台 `09:23:33Z` —— 早于 cutoff 9.5h ⇒ 已入账 ⇒ 不计

合计 **`$2.150`**。工具**独立**打 `must cover : 1 wave(s) listed above (W62)` —— 两者一致。
⛔ **未**使用章程步骤 2(乙)禁用的「MTD 增量对得上波费 ⇒ 该波已计入」手法。

### 2.2 ⭐ GH #692 已落地(本台 03:11Z 立的案,本轮结清)

工具的时钟行现在逐字是 `clock from budget snapshot`,**不再**是 03:11Z 那轮实测的
`clock from now (budget LastUpdatedTime unreadable)`。⇒ 那条 **100% 触发、方向偏许可、
足以把闸从 `exit 3` 翻成 `exit 0`** 的 float-epoch 解析降级**不再在本账户上发生**。
⭐ 本轮**同时**是它的验收:本台自算的 cutoff `18:51:49Z` 与工具打的 cutoff **逐位相同**。

### 2.3 ⚠️ MTD 与上一轮完全一致,而这一次仍是零信息

`$75.023`,快照戳 `06:09:49Z` —— **与 06:18Z 那轮同一个快照**。
⇒ 中间六小时「MTD 没涨」**不得读作「没花钱」**(这六小时本来也确实没发波,但那是波次记录说的,不是这个数说的)。
CE `$0.01` 复核 `75.0233630746`,两源一致。`$50` 告警 `state=ALARM` 仍越,`$80`/`$100` `state=OK`。
⛔ GH #515/#528 **一字未变**:`budget filters : none` —— MTD 里**外来支出那一半不受本台任何闸约束**。

## 三、收割:无,而且是查过不是假定

`s3api list-objects-v2 --prefix validation/` 按 `LastModified` 排,最新对象 `2026-09-09T22:18:47Z`,
全部属 W62 run 前缀(`_20260909_2126*`),而 W62 已由 00:14Z 轮完整收割
(`W62_verdict.json`,4 粒,`scored 203`)⇒ 早于收割 ⇒ 全部已收。**`harvest_owed = NONE`**。

⭐ **第二条独立读数**:`seed_roster_index.py --build` 打
`405 run prefixes in S3, 401 already indexed, 4 to scan`,索引从 **261 → 265 粒**,
**新增的四粒正是 W62 的 10601/10607/10803/10813** ⇒ W62 的局**确实入库了**。
(对照:W61 那次的 `0 to scan` / `index unchanged` 正是它零产出的第二条独立证据。)

## 四、发波前的仪器

- **臂串**:`iterations/streams/test_set.md` **第 2 行**(不是第 3 行,GH #631),当轮现算:
  **37 id / 335 字节 / md5 `b525d51d4b4957e0e40f22f203aea641`** —— 与 W60/W61/W62 逐字节相同。
  **发波脚本内第二次重算**,md5 漂移即 `exit 8`。
  ⚠️ **登记一条自己的读数瑕疵**:脚本日志那行 `ARM_IDS=36` 是
  `tr ',' '\n' | wc -l` 在**无尾换行**字符串上的计数假象(真值 37,python `split(',')` 为准);
  **真正把门的是 md5 相等判据,它逐位命中**。写在这里是因为**一个错的数出现在日志里就要被登记**,
  哪怕它没把门。
- **载体闸**:`CARRIER_GATE ids=9 seeds=4 exit=0`,`grep -c EMPTY` = **0**。
  term **从臂串机械推导**(`--assert-carrier-from-arm`,GH #276,不手打):
  `9 hero-scoped, 28 generic, 0 unresolved => 7 term(s)`,
  `TERMS crystal_maiden,lion,obsidian_destroyer,pudge,skeleton_king,spirit_breaker,zuus`
  —— 与 W53–W62 逐字节相同(臂串没动)。`0 unresolved` = 37 个 id **每一个**在本 HEAD 上都找到真实调用点。
- **`--dry-run`**:`DRYRUN_EXIT=0`,**裸读不过管道**,plan 行 **`plan: 1 x SPOT c6i.4xlarge`**
  —— 这是「市场 = spot」在花钱之前的**独立第二读**(闸 (iv) 是第一读,`InstanceLifecycle` 是第三读)。
  波次预算闸**未拒**(无 `REFUSED` 行)⇒ 未传也不需要 `--allow-short-watchdog`。

### 4.1 种子:窗口不右移,而且这次的最优性是可证的

`ROSTER_EXIT=0` **先跑完**,才写下面的互斥声明(顺序,不是速度)。
窗口 **[10601,11000] 保持不动**:与新建索引的交集是
**恰好且仅有 W62 那四粒 `{10601,10607,10803,10813}`**(打印过的,不是假设)⇒ 窗内 396 粒可证未用。
⇒ **不需要像 W62 那轮那样右移窗口**。

选 **10614 / 10714 / 10842 / 10889**,7 个 term 的逐 term 覆盖
`{skeleton_king 3, spirit_breaker 3, crystal_maiden 2, lion 2, obsidian_destroyer 2, pudge 2, zuus 2}`
⇒ **min 2 / total 16**。⭐ **全局最优可证**:去掉 W62 四粒后窗内 popcount 分布
`{0:40, 1:115, 2:152, 3:74, 4:15}` ⇒ **最大 popcount = 4** ⇒ 任意四粒 total ≤ **16**(本组取到);
且 `min≥3` 需 total ≥ **21 > 16** ⇒ **min 2 就是天花板**。
⚠️ **不与 W62 的 `total 19` 比大小**:那时窗内还有三粒 popcount=5,**正是 W62 自己消耗掉的**。

## 五、W63 已起飞

四台 **spot**,四个不同 AZ,**全部按请求直给**,零 `re-aiming`、零 `AZ RING EXHAUSTED`,slate spread **7s**:

| seed | instance | AZ(请求=实际) | `InstanceLifecycle` | 起飞 |
|---|---|---|---|---|
| 10614 | `i-0c4a62df047d11326` | us-west-2a | **spot** | 12:18:38Z |
| 10714 | `i-0e8228a306af84d36` | us-west-2b | **spot** | 12:18:40Z |
| 10842 | `i-0a044690fc9e2b1e5` | us-west-2c | **spot** | 12:18:43Z |
| 10889 | `i-02005852a6dd3ebb4` | us-west-2d | **spot** | 12:18:45Z |

配置 `--slots 16 --hours 2 --rec-slots 8 --games 12`,与 W39–W62 逐字相同;
`--rec-slots 8` **显式传**(默认是 1,W51 就是吃了默认被 70 秒后杀掉的那一波)。
⚠️ **`run_id` 的 `spot_` 前缀是硬编码字符串不是市场证据**(W62 四台**按需**机的 run_id 也全以 `spot_` 开头);
市场的证据是上表 `InstanceLifecycle` 那一列,**逐台查的**。
⚠️ **4/4 AZ 分散不是种子间独立性的证据**:W59 就是 4/4 分散仍 3/4 被回收、其中两台**跨 AZ 同秒**。
**本行不能用来给 GH #403 收案。**

### 5.1 ⛔ 本轮对自己记一条退步:钉用的是 `ref=main`,不是 40 位 SHA

W62 传的是 `--ref <40 位 SHA>`(它的 run_id 里嵌着 `a91efb69…`);
**本轮四台的 run_id 读作 `_main_`** —— 即本台走了 `spot_run.sh` 的默认 `REF=main`。

- **守住的部分**:发波脚本**第一条语句**就是 `git ls-remote origin main` 与钉比对、不等即 `exit 9`,
  在**任何花钱动作之前**打了 `REMOTE_MAIN=cfa966e393…` / `PIN=cfa966e393…` 两行,两者相等。
- **⛔ 没守住的部分**:**实例是在开机时才 clone 那个 ref 的**,比上面那条守卫晚几分钟。
  守卫关掉的是**发波前**的竞态,**关不掉守卫与 clone 之间那个窗口**。
  若期间有别的 stream 推了 main,机器会**静默地**测一棵不是钉的树,**没有任何东西会举手**
  —— 正是 `AGENTS.md` 硬知识里那条近失事故(`batch instances clone origin/main at launch`)的形状。
- **本轮没有实际发生**:`12:19:12Z` 与 `12:21:03Z` 两次重读 `origin/main` 仍等于钉。
- ⇒ **登记为「轮询验证」而非「构造性钉死」**,并**开 issue**(§八,已发表 **GH #710**)。
  ⭐ 写在这里的理由:**这是本台对自己记的一条退步**,W62 做对了而本轮做差了;
  「这次没出事」不是它可以不被登记的理由 —— 那正是近失事故的定义。

## 六、⭐⭐⭐ 这一波买的是深度:W63 与 W62 可并池(当轮对真实树差核过)

⛔ **00:14Z 那份可并池分析已失效,本轮没有抄它**:它算的是钉 `0c6a89ab`、树差「3 文件 +162/−17」;
而 main 此后走到 `cfa966e3`,**真实树差是 5 文件 +566/−46**。

**方法**:`git diff a91efb69..cfa966e3 -- bots/ game/` = **24 hunk**;
把每一条非空、非 `--` 注释的增删行抽出来(`grep -c "^[+-].*--\[\["` = **0**,
⇒ 没有块注释,行级过滤器**在这份 diff 上是可靠的**)⇒ **11 个真实代码 hunk / 114 行**。
diff 里出现的 **8 个 soak id**(`cmqpoke`/`hrflee`/`tfnull`/`wkqodds`/`fbnoally`/`l5trees`/`zusultstrand`/`wkbonefight`)
**逐个程序化比对臂串:无一 armed**。

逐 hunk 判据(**从当前树的源码读出来的,不是从 diff 猜的** —— 章程「要判当前状态就去读当前树的源码」):

| # | 改动 | 未 armed 时为何等价 |
|---|---|---|
| 1 | CM `cm_ShouldSpendSurplusNova` | 门下 `cmqpoke`;未 armed 时返回 `bShippedSurplus` **逐字**,而调用点传进去的正是**旧条件** |
| 2 | WK `wk_IsCatchAllOddsOk` | 门下 `wkqodds`;未 armed 返回 `true`,调用点是 `and true` |
| 3 | WK `IsBoneGuardEnemyCountOk` 签名扩容 | 门下 `wkbonefight`;未 armed 的返回是 `return nEnemies == 1`,**与旧函数体逐字相同**;调用点新的 `nEngageRange` 字面量就是 **650**,即它替换掉的那个常数 |
| 4 | zuus `X.nUltCashChaseRadius 1600→700` | ⚠️ **常数本身没有门**;但 `git grep` 全仓库**唯一读者**在 `hero_zuus.lua:1464`,**位于** `if not ( J.IsModeTurbo() and J.IsSoakCandidate( 'zusultstrand' ) ) then return false end` **之下** |
| 5 | `J.GetTeamFightLocation` 的 `tfnull` 支 | 整块在自己门内 |
| 6 | `J.GetOffWaveHarassSpot` **改了返回值** | ⚠️ **函数本身没有门**;但 `git grep` 全仓库**唯一调用点** `mode_laning_generic.lua:406`,其 `if` 是**纯合取且以 `J.IsSoakCandidate('l5trees')` 开头** |
| 7 | `J.GetLaneHarassResponse` 的 `hrflee` 支 | 整块闭合在门内;门后**未 gate 的落地路径**(`return 'back', Vector( vB.x + dx/n*420, … )`)**逐字未变** |
| 8 | force_boots `fbnoally` | 未 armed 时 `bNoAllyRefuse` 恒 false ⇒ `if not false and botTarget:IsFacingLocation(…)` **化归原条件** |

⇒ **同一个可达程序** ⇒ 按总监 §GH.5.4 **可并池** ⇒ **家族深度 4 粒 → 8 粒**。

⭐ **这正是本台连续几轮抱怨的「depth-1 跑步机」的反面**:本波**没有**开新家族,
它把 W62 那四粒的语料**翻倍**。⛔ 同时这也是收割轮**不许**把它读成「一个新 lever 的独立复现」的理由 ——
它是**同一个程序上的更多局**,而那恰恰是本台自己的噪声底(单粒 gpm σ≈30)说唯一缺的那个量。
(该判据已写进 `W63_wave.json:gate_ii_new_to_test`,逐字 `NOTHING NEW -- and that is this wave's whole point`。)

## 七、波次记录 + 泄漏

- `W63_wave.json` **当轮写完**(不是留给收割轮重建),`launched_at` 与 `launch_time` **两个键都写**(GH #544)。
  `tests/test_wave_gate_keys.py` **当场跑**:初次 **2 红**(缺 `gates.new_to_test` 与 `gates.inputs`),
  ⭐ **补的是诚实值不是为了变绿的值**:gate (ii) 写的就是 **`NOTHING NEW`**(§六 的结论),
  `inputs` 写的是「无待收割」**外加**那条**永久不可恢复**的 W62 `status_code` 洞。
  补后 **`WGK_EXIT=0`,417 checks 0 failed**。
- **`machines[]` 的 `ab`/`ba`/`arm_depth`/`status_code` 现在是 `null`,这是正确的**(波还在飞),
  ⛔ 但 `harvest_notes (b)` 已把**上一次掉棒**写进记录:00:14Z 收满四粒却没回填,
  下一轮闸 (iv) 读的是**波次记录不是 verdict**,于是卡死 —— **收割轮必须回填这四个键**。
- **泄漏 `LEAK_EXIT=0`**:us-west-2 全部 `pending,running,stopping,stopped` = **恰好本轮这四台**,
  全部 `spot` / `c6i.4xlarge`,**无一台不该在跑**;常设仍只有 AMI `ami-0a990a26d89c66547`;
  外来 `r7a`/`c7a` region 内 0 条。

## 八、GH #454 两条腿 + 本轮开的 issue

- **GH #454**:本波是 spot ⇒ **它自己就是那个测量点**,但**读数属于收割轮**
  (账单侧单波成本、以及「跑完自终」机器的机时均值)。
  ⇒ 连续两轮「无测量点」的状态**本轮结束**,判定交下一轮。**本轮不预判、不外推。**
- **本轮开 issue**:`[batch]` —— §5.1 那条 `ref=main` 退步(本台对自己记的,**GH #710**),
  建议 `spot_run.sh` 在 `--validate` 波次里**要么强制 `--ref <40 位 SHA>`、要么在 ref 是可移动分支名时打一行警告**,
  并把「守卫与 clone 之间的窗口」写进 harness 硬知识。

## 九、自检(铁律 10)与铁律 6

- ⭐ **本轮第一条命令仍然接了管道**,工具当场自拒,逐字
  `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.` /
  `it has recurred 5x, every time as the first command of the round.`
  —— 上一轮记成第 9 次,**本轮第 10 次,又是当轮第一条命令**。
  harness 给那条复合语句报的 `SELFCHECK_EXIT=0` 是 **`tail` 的**,不是自检的。
  改重定向 + 后台后重跑。⚠️ **连续十轮撞同一个位置** ⇒ 拒绝门**有效**(没让 reader 的 0 冒充通过),
  但它**拦住的是错误读数,不是这个动作本身**。
- **自检后台跑完并取到真退出码:`SELFCHECK_EXIT=3`**(约 30min)。
  ⚠️ **harness 给这条背景命令报的 `exit code 0` 又是那条复合语句自己的码**
  (`cmd > log 2>&1; echo EXIT=$?`),真码在输出里 `EXIT=3` —— 与本轮第一条命令的管道坑
  **同一族:退出码被一层壳吃掉**,上一轮已记过一次,**本轮复现**。
  逐字:`legs run : 11` /
  `FINDINGS (exit 3) : cadence queue-rulings owed-executions a-evidence-owed trunk-red(python) trunk-red(lua)` /
  **`UNCERTIFIABLE (exit 2): none`** / `NOT RUN (inside a leg): tests/test_luacheck_gate_soakswitch.py tests/test_selfcheck_lua_leg.py`。
  ⭐ **比上一轮好的地方**:09:17Z 那轮 python 腿是 `UNCERTIFIABLE`(那一侧没人看完),
  **本轮两条腿都真跑完了** ⇒ trunk 两侧本轮**都被看过一遍**,看到的是红。
  **`exit 2` 变 `exit 3` 是仪器变好,不是树变坏。**
- **trunk 红:python 1 条 + Lua `7 of 87`**(**登记来源不做归因**):
  python `tests/test_bots_walk_farm_only.py`;Lua ——
  `test_cm_ult_reach_meter_domain.lua:403`(live-CM instants 65→66)、
  `test_corpus_scale.lua:286`(点名 `test_fbnoally_ally_center_origin.lua:157` 把语料数写成 `== 111` 的等式)、
  `test_focus_mana_cost_consumer_census.lua:424`(axe census n=26→27)、
  `test_gated_helper_nesting_census.lua:904`(新的 gate-inside-a-gate,三条**全是 `× tfnull`**)、
  `test_lion_ult_reserve_domain.lua:367`(rank-2 Finger instants 2→4)、
  `test_stayfield2_marginal_domain.lua:591`(negative-natural-bid count 从 56 移动)、
  `test_wk_q_castrange_meter_domain.lua:412`(live-WK instant 38→44)。
  ⚠️ **归因三分**:(甲) 本会话 `bots`/`game`/`tests`/`tools` **一行未改**
  (`git status --porcelain` 只有 `iterations/data/seed_roster_index.json` 与新建的 `W63_wave.json`)
  ⇒ **没有一条是本轮引入的**;
  (乙) `test_gated_helper_nesting_census` / `test_lion_ult_reserve_domain` /
  `test_wk_q_castrange_meter_domain` **上一轮 09:17Z 已报**(GH #705 一族)⇒ **不开重复 issue**;
  (丙) 其余四条(含 python 那条)**看着像**同一族(GH #106/#127/#585:把语料规模写死成等式,
  下一个 fixture 落地就打红一个什么都没变的地方),而**今日协同组/英雄组确实刚落了新 fixture**
  (`test_corpus_scale.lua` 自己点名的 `test_fbnoally_ally_center_origin.lua` 正是本日 `2cb556d` 的产物)
  —— ⚠️ **但这是相关不是归因**,本台**只登记不裁**,交总监。
  ⭐ 顺带,`test_wk_q_castrange_meter_domain.lua:509` 那句**不是计数漂移**:
  它点名三帧 `separated {meter zero, fed shipped, fed armed}`,逐字 `That is the frame GH #390 asked for`
  —— **不要当计数漂移销掉**(09:17Z 已提，本轮复现，仍未被认领)。
- **铁律 6**:本会话**未改任何 Lua**,静态门由 `.githooks/pre-push` 在 push 时自跑(读数见收尾追记)。
  **未用 `RULE6_BYPASS`** ⇒ 无「SKIPPED, not passed」行。动态半(~100min)未跑,**不声称**。
- **铁律 11**:全程无 `requires approval`,**零空转等待**。

## 十、交棒

1. ⭐⭐⭐ **收割轮(下一轮或下下轮)—— W63 的四粒**:
   走 `recover_verdict.py` 从 S3 逐局重算(标准路径);
   ⛔ **必须把 `ab`/`ba`/`arm_depth`/`status_code` 回填进 `W63_wave.json:machines[]`**,不只写 verdict
   —— **这正是 GH #699 吸收态的上游**(00:14Z 那次掉的就是这一棒);
   spot 波的 `Status.Code` 在 SIR 里**不会像按需那样 ~1h 老化**,但**仍要早读**。
2. ⭐⭐⭐ **收割轮 —— 并池报告**:W63 与 W62 **可并池已在本轮核实**(§六),
   报告请按**并池 8 粒**给读数,并遵守计量四条:
   每粒先 swap-average、再跨种子取**算术**平均,⛔ **永不按局数加权**(#329 (i-d))。
3. ⭐⭐ **总监 + harness —— §5.1 的 `ref=main` 窗口**(本台自记退步,已开 issue):
   建议 `spot_run.sh` 对 `--validate` 波次强制 40 位 SHA,或在 ref 是可移动分支名时告警。
4. ⭐⭐ **总监 —— 「收割轮零 null 检查」仍未落地**(06:18Z、09:17Z 两轮已交,本轮第三次):
   它是 GH #699 的上游,而 W63 现在就是下一个会踩它的波。
5. ⭐ **总监 —— GH #692 与 GH #699 本轮均已验收**(§2.2 / §一),对应的 `owed_executions` 判据可结清。
6. ⭐ **owner —— `$50` 仍越线**:MTD `$75.023`、`state=ALARM`、`forecast $152.875`,
   预算过滤器仍 `null`(GH #515/#528)⇒ MTD 里**外来支出那一半不受本台任何闸约束**。
   本波之后 headroom **`$1.727`**(⚠️ **时间的函数,下轮必须现跑**)。
7. ⚠️ **自检管道坑第 10 次**,连续十轮同一位置、同为当轮第一条命令。

**下一轮本台** = ① **收割轮**(W63 预计 ~14:20–14:50Z 落地,`--hours 2` 看门狗);
② ⛔ **围栏/闸一律当轮现跑** —— `$75.023` / `$1.727` / `$2.150` **全部作废**;
③ 闸 (i) 名义解锁 `2026-09-10T18:18:45Z`(末台,GH #469 ruling 1)—— **别抄,跑 `wave_throttle.py`**;
④ ⚠️ 开工第一条命令**重定向 + 后台 + 不设短 `timeout`**(第 11 次)。

---

## 十一、Token 用量

见下节(收尾时 `python3 tools/agent/token_usage.py` 原样贴入)。

```
TOKENS total_in=8,604,150 out=61,662 turns=67
```

(统计时刻为收尾前;此后的收尾回合不计入,量级参考足够。)
