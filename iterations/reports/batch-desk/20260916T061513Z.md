# batch-desk 2026-09-16T06:15:13Z — 刹车第三十一轮持有;⭐ 本轮把「刹车咬住的这条线里有近一半不是本台的钱」和「月末可能靠空转越过 `$100`」放在同一张表上

**一句话**:快照连续第二轮解冻(`$91.003 → $91.293`,戳 `01:56:47Z → 04:33:16Z`),刹车仍在;
实质交付是三件**现跑**的核实 —— ⭐ `budgets:DescribeBudgetActionsForBudget` 的权限缺口**本轮第一手重测**(不是抄 `AGENTS.md`)、
⭐ GH #850 的修法**未落地**(⑦(d) 因此不成立,已在树上核过)、
⭐ `bots/Customize/soak_side.lua` 在本容器**不存在**(⑦(b) 提前做掉,并给 GH #848 一个反向证人)。
零发波、零收割欠、零泄漏,⛔ `bots`/`game`/`tests`/`tools` 一行未改。

---

## 一、成本(当轮现跑,⛔ 下轮不得抄作前提)

`AWS_SETUP_EXIT=0`(容器无 CLI,自装 `aws-cli/1.46.1 Python/3.11.15 botocore/1.43.62`;
身份逐字 `(restricted batch-runner user — correct)`);`COST_EXIT=0`(⛔ 未走管道)。

```
91.293	USD
   source: budgets/dota2bot-batch (free) | forecast 102.519 | budget limit 100.0 | budget refreshed 2026-09-16T04:33:16Z
```

**headroom 对 `$90` = `$-1.293`**。

### 1.1 快照连续第二轮解冻

| 量 | 上一轮(03:15Z) | 本轮 | 差 |
|---|---|---|---|
| MTD | `$91.003` | **`$91.293`** | **+`$0.290`** |
| 预算戳 | `2026-09-16T01:56:47Z` | **`2026-09-16T04:33:16Z`** | **+2.608h** |
| forecast | `102.229` | **`102.519`** | +0.29 |

### 1.2 ⭐⭐ 两个读数,**同一份数据,差 5.7 倍** —— ⛔ 本台两个都登记,一个都不当成速率

```
seg(00:16Z→03:15Z) : +$0.221 / 11.275h = $0.470/day
seg(03:15Z→06:15Z) : +$0.290 /  2.608h = $2.669/day     <- 本轮这一段
cumulative(14:40:18Z→04:33:16Z) : +$0.511 / 13.883h = $0.883/day
```

⭐ **短段读作 `$2.669/day`,是已登记空转区间 `$0.40–0.92/day` 上沿的 2.9 倍;
而把同两个端点之间的整段一起读,是 `$0.883/day`,落在区间内(贴上沿)。**

⛔ **本台不据短段主张「支出在加速」**,理由是算术而不是谨慎:
`budgets` 的 MTD 对 EC2 有 **4.3–11.3h 滞后**(章程铁纪律,甲/乙两条),
⇒ **分子是一段 11.3h 窗口的落账、分母却是两次快照之间的 2.6h** —— **两者量的不是同一段时间**。
一笔落在窗口里的旧支出,会让**任何**短分母读出一个任意大的「速率」。
📌 **与铁律 4 §CL (i-a) 同型**:`+$0.290 / 2.608h` 是一个**真数**,
它只是**不回答**「这 2.6 小时花了多少」这个问题。
⇒ **规矩(登记给下一轮)**:**空转速率一律用距今 ≥ 11.3h 的两个端点算**(至少覆盖一个滞后窗口);
短于此的段**照登为原始差值,⛔ 不换算成 `/day`**。
⚠️ **空转日样本仍是 n=4**(09-13 `$0.9185` / 09-14 `$0.3997` / 09-15 `$0.213` / `$0.47`),
⛔ 本轮**不新增第五点**(本轮的合规读数 `$0.883` 与上一轮的 `$0.47` **端点重叠**,不是独立点),
⛔ 也不收窄那个区间。

### 1.3 GH #801 四行整块照抄(⛔ 未用 `COST_CONFIRM_AT` 自救,用默认值跑)

```
   >= $35, but headroom to the $90.00 brake is $-1.293
   < $1.10 (cheapest wave) — CE confirmation SKIPPED, NOT passed (GH #801).
   Nothing can launch at this MTD, so confirming it buys nothing.
   Resumes by itself as soon as headroom >= $1.10.
```

⚠️ **这是 SKIP 不是 pass**;⛔ 欠条 `gh801_confirm_headroom_first_live_read` **仍未结清**
(headroom 仍为负,走的还是 SKIP 分支)。

accrual probe 两行照抄:

```
   accrual probe (GH #779): alpha = 0 accruing instance(s) account-wide; beta = 0 wave record(s) after 2026-09-15T17:15:16Z
   stamp unknown (fresh container) -- alpha and beta are both zero -- a frozen stamp over an idle account opens no blind spot
```

⭐ accrual epoch 本轮再次跟着快照前移(`2026-09-15T14:38:47Z` → `2026-09-15T17:15:16Z`)——
**同一条耦合的第三次登记,窗口跟着戳走。**

### 1.4 成本三段式(RULING 48)

**零 EC2 / 零 CE / S3 读取 0 个对象(出网未计价)**

⚠️ **诚实边界照登**:本轮发 **1 次 LIST 请求**(`validation/ --recursive`),
**LIST 是没有被 `<N>` 计量的 S3 请求**;⛔ 不写成零、⛔ 不冒充 `<N>`。
⭐ 本轮 `<N> = 0` 是**真的零对象读取**(上一轮为 1,取了 W69 的 `run.log`);
`soak/` 本轮**一次都没列**(⑦(c) 的条件 `beta ≠ 0` 未成立)。

---

## 二、闸 (iii)(`--planned 1.10`):`FENCE_EXIT=3`

```
actual (MTD)     : $91.293   <- re-read this run, never cached
pending waves    : $0.000   (launched, may not be in MTD yet)
planned          : $1.100
projected total  : $92.393
fence            : $100.00   <- lowest ACTUAL alert not yet crossed (Ruling 1)
brake            : $90.00   (owner's line, not derived here)
operative ceiling: $90.00   = min(fence, brake)
A launch at this instant would put MTD past the $90.00 BRAKE line.
WAVE_FENCE: THROTTLED (exit 3) -- brake, not fence. Stop and report to the owner; do not wait it out.
```

RULING 5/6/7 三行照抄:

```
accrual scope    : 17 region(s) read [...]  <- COMPLETE: every region this account has enabled
wave accrual     : records after 2026-09-15T17:15:16Z (= 2026-09-16T04:33:16Z - 11.3h lag, clock from budget snapshot)
accrual check    : CERTIFIED (0 accruing instances account-wide, read this run) -- pending $0.000 is a reading, not a default.
```

⭐ **钟没有降级** ⇒ RULING 7 (甲) 仍有效。
⛔ 未传任何 `--no-*`/`--pending`/`--director-crossing` ⇒ **无一行「这是 SKIP 不是 pass」**。
`crossing registry: 0 record(s) …, none in force`(RULING 15 退休传导仍正确)。

⇒ **连续第三十一轮,不发波的原因是闸不是本台的处置。**

---

## 三、收割:零欠(⛔ 不是漏收)

`S3VAL_EXIT=0`,`validation/` **`OBJ_COUNT=592`** 与已登记基线**逐位相同**。
最新对象按 **RULING 47 规定的取法**(`--recursive | sort | tail -1`,⛔ 不裸 `| tail`):

```
2026-09-12 10:18:26      15187 validation/lf_rescue+27ids-d177b87026dd_20260912_1018_run.log
```

⇒ **仍是 W69,距今 4.8 天零新对象**。`recover_verdict.py` **未跑 ——「没得收」不是「漏收」**。
⚠️ 欠条 `games_ledger_cross_wave_accounting` 仍 OWED(`--ledger` 至今未被任何真波行使)。
⛔ 本台未改 `queue.json` 任何一条 `status`(没有一条能在 `FENCE_EXIT=3` 下执行)。

---

## 四、⭐⭐ 实质交付:三件**现跑**的核实(⛔ 一件都不是抄来的)

### 4.1 ⭐⭐ `$100` 那道线**后面有没有东西**,本轮第一手重测 —— 仍然读不到

`AGENTS.md` 写着「100% 的冻结动作**无法从本账户核实**(`dota2bot-agent` 缺
`budgets:DescribeBudgetActionsForBudget`)」。⛔ **本台不抄这句,本轮现跑**(该 API 免费):

```
BACT_EXIT=255
An error occurred (AccessDeniedException) when calling the DescribeBudgetActionsForBudget
operation: User: arn:aws:iam::581436534924:user/dota2bot-agent is not authorized to perform:
budgets:DescribeBudgetActionsForBudget on resource: arn:aws:budgets::581436534924:budget/dota2bot-batch
because no identity-based policy allows the budgets:DescribeBudgetActionsForBudget action
```

⇒ **权限缺口 2026-09-16 仍在**,`AGENTS.md` 那句话**本轮有一个当日读数支撑它**,不再只是一句留档。
⚠️ **⛔ 这不证明冻结动作存在,也不证明它不存在** —— 它只把「不可知」钉成**本轮实测的不可知**。
⭐ **为什么这件事今天才值钱**:见 §七 —— 月末 `$100` 可能**靠空转越过**,
而越过之后会发生什么,**本台在算术上无法回答**。

### 4.2 GH #850(§4.5 那条 harness 缺陷)**未落地** ⇒ 上一轮交棒 ⑦(d) 本轮不成立

⛔ **在树上核过,不是凭记忆**:

```
git log --oneline -3 -- tools/batch_test/aws/spot_run.sh
  6188c0f7 director 2026-09-15T19:08Z (backfill 3): repair two lines a shell expansion emptied

grep -n "soak_farm.log" tools/batch_test/aws/spot_run.sh
  246:exec > /var/log/soak_farm.log 2>&1
  263:        echo "SPOT INTERRUPTION: \$ACT" >> /var/log/soak_farm.log
  267:        aws s3 cp /var/log/soak_farm.log "\$S3_RUN/soak_farm.log" --quiet

grep -n "git fetch|git checkout|git pull|git log --oneline" tools/batch_test/aws/spot_run.sh
  278:sudo -u ubuntu git fetch -q origin '$REF' || true
  279:sudo -u ubuntu git checkout '$REF' || true
  280:sudo -u ubuntu git pull --ff-only origin '$REF' || true
  281:sudo -u ubuntu git log --oneline -1 || true
```

⇒ **三个 `|| true` 一字未动;唯一上传点仍在抢占分支里(267 在 263 同一块)。**
⇒ ⑦(d)(「若 §4.5 修法落地,下一波第一件事看 `$S3_RUN/` 里有没有树记录对象」)
**前提不成立**,⛔ 本轮不执行、⛔ 也不退休它 —— **逐字保留到修法落地那一轮**。
⛔ 本台仍未代修(harness 编排权不在本台,先例 GH #33)。

### 4.3 ⭐ `bots/Customize/soak_side.lua` 在本容器**不存在** —— 给 GH #848 一个反向证人

上一轮交棒 ⑦(b) 要求「发波前先看一眼 `soak_side.lua`」。本轮**闸是红的、发不了波**,
但这个检查**零成本且与发波无关**,故提前做掉:

```
ls -la bots/Customize/soak_side.lua
  ls: cannot access 'bots/Customize/soak_side.lua': No such file or directory
```

⭐ **与 GH #848 并读**:上一轮开工自检的 Lua 腿在**它那个容器**里打
`RED test_siegecap_ancient_roster.lua`,理由逐字是「`soak_side.lua` 已经在那儿了,而这个进程没写它」,
内容 `return { side = 'radiant', cand = 'aegisgroup' }`。
⇒ **本容器无此文件** ⇒ 若本轮 Lua 腿不红,那就是**第三个证人**说这条红是
**容器局部的遗留文件**造成的,⛔ **不是 trunk 红**。
⚠️ ⛔ **本轮拿不到 Lua 腿的读数就不许写这个结论** —— 见 §八,腿级判词本轮的状态在那里。
⛔ 本台不代修、不归因(该文件 gitignored,谁写的读不到)。

---

## 五、泄漏:五条独立路径全零(⛔ 不合并成一句)

| # | 路径 | 读数 |
|---|---|---|
| ① | `check_costs.sh` 自带区块 | **空** |
| ② | `describe-instances` 五态(pending/running/shutting-down/stopping/stopped),**不加 tag 过滤** | `INST_EXIT=0`、**`LINES=0`** |
| ③ | 账户级 accrual | **17 区 `COMPLETE`**、`alpha = 0`、`CERTIFIED` |
| ④ | AMI | 仍 `ami-0a990a26d89c66547`(`dota2bot-batch-20260718`)一张,常设成本非泄漏 |
| ⑤ | 闸 | `pending waves : $0.000`,且逐字 `CERTIFIED ... is a reading, not a default` |

⛔ 不合并 —— ①②③ 问「现在有没有机器」、④ 问「有没有常设资产」、⑤ 问「有没有已发未计的波」。
⚠️ 本轮**未**买账单侧第六条(逐日 `EC2 - Compute`),理由同 GH #801 四行(headroom 为负,买不到决策)。

---

## 六、⭐⭐⭐ 归因:咬住本台的这 `$91.293` 里,**近一半不是本台的钱**(GH #779 第二十二轮零表态)

GH #779 本轮现读:`state=open`、`comments=16`、`updated_at 2026-09-15T15:53:44Z`
⇒ **owner 仍未在该 issue 选定三条路中的任何一条**。

该 issue 里已两次第一手量到的外来支出(09-08 / 09-10 两轮逐日 CE):
`c7a.16xlarge $23.2805` + `r7a.16xlarge $14.1550` = **`$37.4355`**,
且**只存在于 09-05 / 09-06 两天**、此后一分未涨(⛔ **不是正在扩大的洞**)。

| 量 | 值 |
|---|---|
| 预算读到的 MTD | `$91.293` |
| 其中外来(已钉死,未再增长) | `$37.4355`(**41.0%**) |
| ⇒ **本台可归因 MTD** | **`$53.86`** |

⇒ ⭐ **`$90` 刹车咬住的是账户总额,而本台自己的支出离它还有 `$36` 以上。**
⚠️ **归因边界逐字保留**:能证明那两个机型不是农场用的(W1–W69 发波记录只有 `c6i.4xlarge`,界前偶有 `c6a`),
**⛔ 不能**证明是谁跑的(读不到别人的标签)。
⛔ 本台**不建议、不预选** GH #779 的任何一条路,也**不自行**改刹车线。

---

## 七、月末外推:⭐ `$100` 可能**靠空转越过**(口径不变,⛔ 不收窄)

`$91.293 + 14 天 × $0.40–0.92/天 − $0.8`(`dem21/` 09-22 自动过期省 `~$0.8`,已扣进两端)

⇒ 9/30 MTD 落在 **`$96.1 – $103.4`**(上一轮口径下是 `$95.8 – $103.1`,右移 `~$0.3`)。

⭐⭐ **本轮要点名的不是那个区间,是它的上半段意味着什么**:
**在整月零发波的前提下**,区间上沿 `$103.4` **越过 `$100`**,
而 `$100` 正是 §二 里 `fence` 这一行的来源(`lowest ACTUAL alert not yet crossed`)。
⇒ **有可能出现这样一个月末:本台一波没发、买到零局数据,而账户越过了它的预算线。**
⚠️ **越过之后会发生什么,§4.1 已现跑证明本台读不到**(缺 `DescribeBudgetActionsForBudget`)。
⛔ **本台不主张 `$100` 一定会被越、也不主张一定不会** —— 区间跨在它两侧,
⛔ **不外推到十月、不改任何常数**。
⚠️ AWS 自己的 `forecast 102.519` 也落在这个区间内 —— ⛔ **巧合,不当互证**(口径不在本台手上)。

---

## 八、铁律 9 / 开工自检 / 自捉

**铁律 9**:P4.1 upstream 标尺波**连续第二十四轮欠**,⛔ **唯一阻因是闸 (iii) `exit 3`(预算)**,
不是未认领也不是排期。

**⛔ 自捉一条(方向对本台不利,照登)**:上一轮交棒 ⑦(a) 的字面模板
(`bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1; echo "EXIT=$?"`,⛔ 无管道)
**第八轮仍未照抄** —— 本轮第一条自检命令是
`bash tools/agent/routine_selfcheck.sh 2>&1 | tail -40`,**带管道**,当场被拒,逐字:

```
REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.
REFUSED: stdout is a PIPE, so the exit code your harness reports is
         the READER's (tail/head almost always succeed), not this
         script's. This is evidence discipline 3; it has recurred 5x,
         every time as the first command of the round.
```

📌 `exit 2 =「没跑成」不是「没发现」` ⇒ ⛔ 那一次什么都没检查。
第二次改成重定向裸读(⛔ 无管道)**跑起来了**。
⭐ **拒绝腿再次自己工作**(它第 6 次挡住同一个假读数);不工作的是本台的第一条命令 —— **八轮七次**。
⚠️ 脚本自陈的复发计数是 **5x**,而上一轮报告记的是「本轮第 6 次」——
⇒ **脚本的计数器不含上一轮**(它数的是自己见过的,上一轮那次没让它累加,或计数不跨容器);
⛔ 本台**不解释这个差值**,只登记两个数不一致。

**开工自检状态(⛔ 按上一轮 §十二 的教训写)**:
自检在后台**仍在运行**(写本节时 `ps` 数到 4 个匹配进程,日志停在
`=== trunk health (python test suite) ===` 之后 —— 那是 **python trunk 腿正在跑**,
⛔ **不是「它没跑完」**;「tail 里没有」不是「它没产生」,上一轮 §十二 正因把这两句读混而自捉)。
**腿级判词(`selfcheck worst exit` / `FINDINGS` / `UNCERTIFIABLE`)见收尾 follow-up commit**;
⛔ 在拿到之前,**本轮不写 trunk 绿也不写 trunk 红**,§4.3 那个「第三个证人」的结论也**悬着不下**。

**跑到的那一段**(⛔ 只是跑到的一段,不是腿级判词):

```
=== Lua tests no automatic reader runs (GH #806) ===
LUA GATE COVERAGE  disk 484 | push gate 332 | 开工自检 leg 118 | known_red 6
  UNCOVERED (no automatic reader runs these): 114 of 484 (24%)  {'no_manifest_row': 52, 'timed_out': 60, 'too_slow': 2}
    NOW COVERED    tests/test_fieldsip_atom_pricing.lua
UNCOVERED SET GREW -- 2 file(s) that nothing reads automatically:
    NEW UNCOVERED  no_manifest_row    tests/test_dusttower_dive_guard.lua
    NEW UNCOVERED  no_manifest_row    tests/test_fieldsip_transfer_receiving_site.lua

no armed id hangs under an unarmed gate -- OK
```

⭐ 与上一轮**逐位相同的 2 个文件**(⛔ 未恶化也未修复);
⚠️ 但 `disk`/`push gate`/`leg` 三个分母各 **+2 / +2 / +1**(482→484 / 330→332 / 117→118),
`UNCOVERED` 的**绝对数与百分比却逐位不变**(114 / 24%)——⛔ 本台只登记,不归因。
⛔ **本台不代修、不调成员资格**(`lua_gate.py` 编排权归总监)。

---

## 九、issue / push / 闸读数

- **本轮未开新 GH issue** —— ⛔ 理由是**前提**不是省事:§4.1/§4.2/§4.3 三件都是**核实**
  (两件确认既有 issue 的前提仍成立、一件是反向证人),**没有一件是新立案面**;
  §七 的月末越线属 GH #779 的同一张桌子,⛔ 不另开一条把 owner 的注意力劈成两半。
- **已在 GH #779 追加一条评论**(§六 + §七 全文),⛔ 发在 `git push` **之后**(GH #290 顺序),
  并先跑 `claim_precheck.sh`。读数见 follow-up commit。
- push 闸三行读数(`GATE_EXIT=` / `py gate:` / `lua gate:`)见 follow-up commit。

---

## 十、交棒

- ① ⭐⭐⭐ **owner —— 刹车第三十一轮持有,GH #779 第二十二轮零表态。本轮球的形状变了,请看两个新数**:
  (甲) **`$90` 里 `$37.4355`(41.0%)不是本台的支出**,本台可归因 MTD 仅 **`$53.86`**(§六);
  (乙) **整月零发波的情况下,9/30 区间上沿 `$103.4` 越过 `$100` 预算线**(§七),
  而越过之后会发生什么 **§4.1 现跑证明本台读不到**。
  ⇒ GH #779 的三条路(维持 `$90` / 给预算加过滤器 / 一次性解禁到某个数)**一条都还没选**,
  ⛔ 本台无权自裁、不预选。
- ② ⭐⭐ **总监/协同组 —— GH #850 未落地**(§4.2 在树上核过,三个 `|| true` 与抢占分支里的唯一上传点一字未动)。
  最小修法逐字重贴:把 `git log --oneline -1` 的输出单独写一个小文件并**无条件**上传到 `$S3_RUN/`
  ⇒ 一个对象、零 EC2 增量、把「测的是哪棵树」从不可查变成可查。
- ③ ⭐ **总监 —— `s3_attribution_gap_remeasure_under_ruling48` 触发判据请重裁(连续第六轮未获回应)**。
  建议的日期判据「第一个干净的裁定后整日」已到期:**最早可读时刻 `~09-17T11:20Z`,即明天**。
  ⛔ 本台不自行改欠条(写入权归总监,先例 GH #33),状态仍 **OWED**。
  ⭐ **顺带:本轮 `<N> = 0`,是 RULING 48 落地以来第一个真正的零对象轮** —— 这是该欠条要的语料的一部分。
- ④ **总监 —— 上一轮交棒 ②③④⑥ 与更早的 ⓪③④⑤ 逐字保留,本轮未获回应**,⛔ 本台不自行退休任何一行。
  其中 ④(python trunk 腿)本轮**不重复主张**:该腿本轮**仍在跑**(§八),判词见 follow-up。
- ⑤ **下一轮本台**:
  (a) 开工第一条命令 **`bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1; echo "EXIT=$?"`**
      —— ⛔ 无管道、⛔ 无 `timeout`,**八轮七次犯法,一次到位**;
  (b) 刹车解除(headroom ≥ `$1.10`)时按规格发 P4.1 标尺波、**收割必带
      `--ledger iterations/games_ledger.jsonl --ledger-wave W<N>`**,并抄 GH #801 四行**恢复后**的样子;
      `soak_side.lua` 的发波前检查本轮已提前做掉(§4.3),下轮**仍要重做**(它是容器局部的);
  (c) 若某轮 `beta ≠ 0`,**`soak/` 必须列**(本轮 `beta = 0`,未列);
  (d) ⭐ **§4.2 的 ⑦(d) 逐字结转**:若 GH #850 修法落地,下一波第一件事看 `$S3_RUN/` 里有没有那个树记录对象;
  (e) ⭐ **新规矩(§1.2 立)**:空转速率只用距今 **≥ 11.3h** 的两个端点算;
      更短的段**照登原始差值,⛔ 不换算成 `/day`**。

## 十一、token 用量(铁律 8)

见 follow-up commit。
