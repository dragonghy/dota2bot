# 批测台报告 2026-08-22T10:10Z(第四十二次触发)

**类型:收割轮 + 启动轮。** 收割 08:10Z 的 18-id 全集波次(五条必查项全过),
并按总监 §AL(`test_set.md` 4270-4340,09:0xZ)启动**下一波全集默认波次**:
armed 串 **19 id**(§U.0 的 18 id 逐字不变 + 重新入集的 `creeppull`),
采集配置 **`--rec-slots` 1 → 8**。

---

## 1. AWS 引导与成本

- `session_setup.sh` → `AWS ready: arn:aws:iam::581436534924:user/dota2bot-agent`
  (容器无 AWS CLI,脚本自行 pip 装了 `aws-cli/1.46.0`)。之后一律 `awsx`。
- `check_costs.sh`:**MTD $18.605**,forecast **$23.006**,budget limit $100,
  budget refresh **`2026-08-22T06:30:39Z`** —— **与 08:11Z 是同一次刷新,数字逐位相同
  ($18.605),本轮的 MTD 读数不构成新信息**(与 22:18Z→06:11Z 连续五轮同值同理)。
- 免费 Budgets 路径**连续第十三轮**,**CE 调用 0 次**。
- 围栏与刹车:$45 围栏余 **$26.40**,$90 刹车线余 **$71.40**;本波预估 **$1.56**
  ⇒ MTD ≈ $20.2。**未触发任何预算刹车。**
- **CE 必查项:目标日仍 08-23 查 08-22 完整日**(02:06Z 定,不再逐轮顺延)。
  08-21 已判定被污染(~16 次),**看到 16 不要误判成修法失败**。

---

## 2. 收割:08:10Z 的 18-id 全集波次(4 种子全齐,295 局有效局)

树 `12ef3de`,armed 串 = §U.0 的 18 id,种子 888/895/896/906,一台一种子,`--rec-slots 1`。

### 2.1 08:11Z 立的五条必查项,逐条结果

| # | 必查项 | 结果 |
|---|---|---|
| ① | 暖场 stamp 必须是 `12ef3de` | **通过** —— 24 个暖场局 stamp 逐个都是 `12ef3de`,裸 SHA checkout 成功,**数据有效** |
| ② | 先按 run 分目录下载再带前缀合并 | **照做,且当场抓到一次真丢局**,见 §2.2 —— 最终 80+80+79+80 = **319 个 `analysis.json`,合并后仍 319,一个不丢** |
| ③ | S3 真实 cand stamp 应恰好等于那 18 id 串一字不差 | **通过** —— 全部 295 局的 stamp 都是那 18 id 串,顺序一字不差;**`wandlimbo`/`odaoe` 一个都没出现** |
| ④ | 全集波次的经济读数是 18 个 id 的合成效应,不可归因到任何单个 id;凡打 A−B 的表每个指标都要打基线腿 | **照做**,见 §2.4;本报告**不对任何单个 id 下结论** |
| ⑤ | `blinkflee`/`liondrainstop` 读到 0 次不构成 SILENT(§V.5);`killed` 不可用(§AJ.3) | **未使用** —— 本台不产出事件计数,也不产出 `killed` |

### 2.2 ⭐ 本轮新踩到的坑:「带前缀合并」的前缀**必须是区分 run 的那一段**,而它已经搬家了

08:08Z 立的规矩是「先按 run 分目录下载,再**带前缀**合并」。这句话没说**用哪一段做前缀**。
自 08-19T20:09Z 起 run_id 改成以**全 40 位 SHA 结尾**(`spot_<日期>_<时分秒>_1_<SHA>`),
而**同一波四台机器的 SHA 是同一个** —— 我第一次合并顺手取了 `${r##*_1_}`(即 SHA)做前缀,
**四个 run 拿到了同一个前缀,319 个文件静默塌成 316,丢了 3 局**。改用时分秒段
(`100932`/`100937`/…)做前缀后 319 全数到齐。

**教训(比原规矩更硬的版本)**:合并后**必须把计数与逐 run 计数之和对账**;
对不上就是前缀没区分开。前缀要取 run_id 里**逐 run 不同**的那一段 —— 在现行命名下
是**时分秒**,不是 SHA。这次是我自己的对账逮住的,不是数据里看出来的:
315 和 319 在读数上没有任何可见差别。

### 2.3 局数与配平

| 种子 | radiant wave | dire wave | 合计 |
|---|---|---|---|
| 888 | 42 | 32 | 74 |
| 895 | 42 | 32 | 74 |
| 896 | 42 | 31 | 73 |
| 906 | 41 | 33 | 74 |
| **合计** | **169** | **126** | **295** |

**4 种子两 wave 全齐,无饿死。** 另有 24 局暖场(不算有效局),295 + 24 = 319。
radiant 打满 `--games 22` 配额(41-42),dire 126 —— 与 08-19T20:11Z 的结论一致,
**不建议继续加 `--games`**,补 dire 要动 stall 上限(harness 侧)。

### 2.4 读数(标准路径 `recover_verdict.py` 全量重算)

**按 §AJ 通例二,每个指标都打印基线腿:**

| 指标 | 候选腿(18 id armed) | **基线腿(gate 全关)** | A−B 均值 | sd | SE | z | comps_better |
|---|---|---|---|---|---|---|---|
| gpm | 811.31 | **839.20** | **−35.95** | 26.57 | 13.28 | −2.71 | 1/4 |
| xpm | 565.91 | **589.54** | **−30.35** | 17.26 | 8.63 | −3.52 | 0/4 |
| deaths | 2.31 | **2.04** | **+0.30** | 0.17 | 0.09 | +3.53 | 0/4 |
| last_hits | 23.01 | **24.20** | **−1.84** | 1.00 | 0.50 | −3.68 | 0/4 |

逐种子 gpm:888 **−49.94** / 895 **−34.11** / 896 **+0.50** / 906 **−60.24**。
脚本 `suggested` 字段输出 `hold_or_reject`(**该字段是阈值机械判,不是判定**)。

**本台只做一句纯观察,不做判定**:这个 18-id 全集读数(−35.95)落在历次多-id 波次
同一条负残差带内(14-id −34.59 / 16-id −33.17 / 15-id −38.66 / 13-id −24.06 / 12-id −32.19),
**既没消失也没显著恶化**。按 §AF.3 总监已裁定「十波多-id 测量彼此不可区分 ⇒ 『比较残差』
这门手艺作废」,**所以上面这句并排只能当背景,不许据以对任何 id 或任何趋势下结论**;
且按 08-22T04:06Z 传下来的第①条,跨波并排引用 gpm 前应查
`iterations/data/wave_tree_index.tsv` 的 `drift_vs_head` —— 本波树 `12ef3de` 是今天的树。

### 2.5 交接

四个 run 前缀已在 S3,承重的域内检测器读数归录像组(GH #45)。**本波 `--rec-slots 1`,
帧通道仍只有 ~1/16**;这正是本轮启动那一波要拔掉的塞子。

---

## 3. 启动:19-id 全集波次,`--rec-slots 8`

### 3.1 申报目的(两条,都不是本台自选)

1. **owner 08-22T07:23Z 常设指示**:默认波次 = 全测试集 armed(本台章程末节)。
2. **总监 §AL/§AK.2 裁定**:下一次全集默认波次起 **`--rec-slots 8`**,两臂同值,
   通过验收后再上 16。这条直接服务 **owner 优先项 P1/P2** —— 两项都卡在条件 (a)
   「真实对局里核验到,带帧证据」,而帧通道长期只有 1/16。**零额外 EC2 支出。**

例行三条件形式上亦全满足((i) 距上次波次 08:10Z 约 2h,但本波是**总监排定的目的波次**,
按 06:10Z 先例不算例行波次、不受 6h 节流;(ii) armed 串 18→19 id 且树有变更;
(iii) $18.61 + $1.56 ≈ $20.2 ≪ $45)。

### 3.2 ⭐ 一处自己立的规矩本轮被总监的裁定合法地突破,记录在案

08:11Z 我自己立过「**不要在同一波里同时改被测 id 集合和采集配置**」(理由:经济读数与历史
不可比)。本波**两样都改了**(18→19 id,rec-slots 1→8),**这是总监 §AL 同一节里同时下的
两条指令**,不是本台自选。为什么在这里无害:

- `--rec-slots` 的验收判据是**波内自控**(slot 1-8 录 / 9-16 不录,同机同时同池),
  **结构上不依赖跨波比较** —— 这正是总监选 8 而不选 16 的理由;
- 经济侧本来就已经因为 `creeppull` 入集而换了组合,rec-slots 不是**新增**的不可比来源。

**诚实边界**:若验收工具返回 exit 2(语料答不了),本波就**同时**失去了经济可比性和
采集验收 —— 那种情况下按总监「exit 2 是拒答不是通过」,退回 `--rec-slots 1` 重跑一波。

### 3.3 armed 串 = §AK.0 逐字照抄(19 id),本台没做任何组合上的自主选择

```
l1trade,l5combo,midtp,suptp,tpcommit,tpdying,lf_rescue,teambrain,ownhalf,overchase,
fieldregen,wandbleed,capmono,cmrguard,tpdead,zusult,blinkflee,liondrainstop,creeppull
```
`wandlimbo` 仍**不 armed**(§J.1.4 机会普查是硬前置,**连续第十轮无人做**);
`odaoe` 按 §AB.1 仍不进串。种子仍 **888/895/896/906**。

**上机前静态核对**:19 个 id 逐个 grep,**18 个有裸字面量**;**`lf_rescue` 命中 0 是已知
正确的**,它经 `J.IsLaneFixOn('rescue')`(`jmz_func.lua:5876`)展开为
`J.IsSoakCandidate('lf_'..sub)`。

### 3.4 树:钉全 40 位 SHA `19b90b95ea11c9857167644852653686819213b9`

- `git ls-remote origin main` 真值 = `19b90b9…`(**不信本地 remote-tracking ref**,
  08-19T20:11Z 那次它落后 43 个 commit);本地 HEAD 与之相同。
- **空 clone 实测 `SHA_FETCH_OK`**(不赌裸 SHA 可 fetch)。
- **树漂移两步法 + 正控**:先 `git fetch --depth 1 origin 12ef3deb…`(全 40 位)取回上一波
  的树,再 `git log 12ef3de..HEAD -- bots/ game/` → **非空,exit 0**:2 个 commit,
  3 个文件 +130/−3。正控 = 同区间不限路径 9 个 commit ⇒ **exit 0 且非空,是真漂移**。
- **漂移的行为学后果 = 零,逐个核过**:
  - `477d0d4`(GH #105)全部在 `J.IsSoakCandidate('corerole')` 之后(`jmz_func.lua:8923`);
  - `19b90b9`(owner P2 决策侧)全部在 `J.IsSoakCandidate('stayfield')` 之后
    (`jmz_func.lua:4750`);
  - `mode_retreat_generic.lua` 那 9 行**是纯注释修正**(把一句陈述为 gated 的
    已 promote 行为改成 PROMOTED 说明),`git diff` 逐行确认无代码变化。
  - `corerole`/`stayfield` **都不在本波 armed 串里** ⇒ 两者在本波逐位 no-op。

### 3.5 上机

4 台 c6i.4xlarge **on-demand**(`InstanceLifecycle=None` 逐台复核,14:12Z「短波次别用 spot」
规矩),一台一种子,**16 槽**,**`--rec-slots 8`**,**2h 看门狗**,`--games 22`。

| 种子 | 实例 | run_id 时分秒段 |
|---|---|---|
| 888 | `i-00bed94dc859bcbd3` | `_100932` |
| 895 | `i-01bcef5a6c036a642` | `_100937` |
| 896 | `i-0574e61a69d82ac2c` | `_100941` |
| 906 | `i-0281d9dee4e644c8c` | `_100946` |

run_id 完整形式 `spot_20260822_1009xx_1_19b90b95ea11c9857167644852653686819213b9`。

**run_id 唯一性纪律(22:0xZ 立)本轮第三次有适用对象且通过**:四次 `COUNT=1` 调用之间
`python3 time.sleep(2.4)` 保证跨过整秒(**实测间隔 5s/4s/5s**);启动后读**独立证据源**
`soak-run` 标签,**四值互异(distinct=4)**。

**本波收割不需要人工映射表**(四台同串同树,per-seed 由 stamp
`mirror:<19id串>:s<seed>:<side>` 自述);上表只为排障留痕。

预计 **~10:45-11:00Z 落地**,预期 **~290 局有效局**(+24 暖场)。

### 3.6 验收(事先登记,收割时照做,不许事后改判据)

```
python3 tools/batch_test/soak/rec_slot_cost.py <本波 4 个 run 目录> \
    --baseline tools/batch_test/soak/rec_slot_baseline.json
```
- **exit 0 = 通过** ⇒ 下一波上 `--rec-slots 16`;
- **exit 1 = 有录制槽超差** ⇒ **退回 `--rec-slots 1`**,把逐槽表贴进报告,由总监重裁;
- **exit 2 = 语料答不了(拒答,不是通过)** ⇒ 按 §3.2 退回 1 重跑。

另按总监指示:**08:10Z 那波(305→319 局、16 槽、同机型、同种子、`--rec-slots 1`)
应并进基线剖面**(工具收多个 run 目录),它与本波除采集配置外最接近同构。

---

## 4. 下次触发的收割必查项(六条)

1. **暖场 stamp 必须是 `19b90b9`** —— 否则裸 SHA checkout 静默失败、跑的是 AMI 陈树,数据作废。
2. **先按 run 分目录下载再带前缀合并**,**前缀取时分秒段不是 SHA**(§2.2 本轮实测的坑),
   且**合并后必须与逐 run 计数之和对账**。
3. **S3 真实 cand stamp 应恰好等于那 19 id 串一字不差**,`wandlimbo`/`odaoe` 一个都不该出现;
   `creeppull` 必须在串里(这是它三天后重新入集的第一波)。
4. **跑 §3.6 的 `rec_slot_cost.py` 验收**,exit code 照 §3.6 三分支处理;**exit 2 不是通过**。
5. **全集波次的经济读数是 19 个 id 的合成效应,不可归因到任何单个 id**(含 `creeppull`);
   凡打 A−B 的表每个指标都要打基线腿。
6. **`blinkflee`/`liondrainstop` 读到 0 次不构成 SILENT**(§V.5);**`killed` 不可用**(§AJ.3)。

---

## 5. 泄漏检查(四层 + spot 请求,全免费只读)

| 层 | 开工 | 收尾 |
|---|---|---|
| 在跑/待起实例 | **0** | **4**(全是本轮有意启动的自毁 on-demand) |
| available(游离)卷 | 0 | **0** |
| 自有快照 | 1(`ami-0a990a26d89c66547`,唯一常设成本) | **1** |
| EIP | 0 | **0** |
| open/active spot 请求 | 0 | **0** |

**无泄漏。** 收尾这次要的是「实例数 = 有意启动数」,不是 MTD 数字(MTD 与 08:11Z 同一次
budget refresh,逐位相同,同轮第二次读数从不带信息)。

---

## 6. 局数(章程第 7 条固定栏位)

- **上一波(08:10Z,18 id,`--rec-slots 1`)最终有效局数**:**295**
  (radiant 169 / dire 126;888=74 / 895=74 / 896=73 / 906=74),**+24 暖场**(不算有效局)。
  ab/ba 不对称:radiant wave 打满 `--games 22` 配额,dire wave 被 35min stall 截断。
- **本轮在跑波次(10:09Z,19 id,`--rec-slots 8`)实时进度**:刚上机,
  S3 `soak/<run_id>/` 下 `analysis.json` 计数 **0**;预期 **~290 局**(暖场不算)。
- 本轮下载 `analysis.json` **319** 个。
- 档案累计:**+4 run(144→148)、+1 波次(85→86)**(增量推算,本轮未重跑全桶 LIST;
  `soak/` 前缀 `s3 ls` 实测 **148**)。

---

## 7. 其他固定栏位

- `queue.json`:**无 pending 请求**(`director-1` 已 `done`)⇒ 走 4b/目的波次路径,本轮未改该文件。
- off-roster 实例 **0**;`unattributed/` **0**;`dem21/` **0**;`.demclaim.json` 全桶 **0**
  —— **注意本波 `--rec-slots 8` 后,`dem21/` 与 `.demclaim.json` 这两条链下一轮起会
  第一次有真实数据**(此前连续第十五轮为 0 = 从未被真实数据验证过)。
- 跨组:在既有 **GH #45** 下追评(**不新开**)。`[batch] #70`、`[harness] #98`/`#75`/`#33`、
  `[bug] #96` 仍开着,不重复开。

---

## 8. 验证(铁律第 6 条)

本会话**未改动任何 Lua**(改动仅 `iterations/` 下的报告与章程),且本容器**未装
`luacheck`/`lua5.1`** —— 按铁律第 6 条立法目的,本轮无适用对象。

本报告第 2-3 节每个数字均由实跑得出:`s3 ls`(148 前缀)/ `s3 cp` 319 个对象 /
`recover_verdict.py` / 319 个 `analysis.json` 的 `script_version` 与 `players[]` 逐个解析 /
`git ls-remote` / `git fetch --depth 1 <全40位SHA>` / `git log` / `git diff` / `grep` /
`ec2 describe-instances`(含独立证据源 `soak-run` 标签)/ `describe-volumes` /
`describe-snapshots` / `describe-addresses` / `describe-spot-instance-requests`。
**唯一估算项是费用 $1.56**(引自 08-20T14:07Z 同配置实测),以及 §6 的档案累计增量(推算)。

---

## 9. 必须传下去的四条

1. **合并前缀取时分秒不取 SHA,且合并后与逐 run 计数对账** —— 本轮真丢过 3 局(§2.2)。
2. **`rec_slot_cost.py` 的验收是事先登记的**,三个 exit code 三种处置,**exit 2 是拒答不是通过**;
   通过才上 16。
3. **AG.4 第 (3) 步仍不许跳步** —— 等录像组的 `roamreach` 域内读数(GH #45)。
4. **owner 的「默认波次 = 全集」是常设指示** —— 默认串取 `test_set.md` 最新 §x.0
   (现为 §AK.0 的 19 id),**不由本台自选组合**;隔离波次要在申报目的里写清
   「为什么全集波次不行」。

---

## 10. 本次会话 token 用量

```
TOKENS total_in=6,496,841 out=54,061 turns=63
```
(统计时刻之后的收尾回合不计入,当量级参考。)
