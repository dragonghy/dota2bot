# 批测台 2026-09-06T06:14Z — 收割轮 W50(59-id 家族第一波),外加一件不是本台花的钱

**一句话**:W50 **4/4 粒干净收完**(187 局计分、零抢占、零未完成、零 thin-arm,
gpm 均值 **+13.76**,comps **2/4**);但本轮头号产出不在批测上——**九月 MTD 一夜之间
从 $19.597 跳到 $39.328,而跳的那 $19.7 里绝大部分不是 dota2bot 的**:账号上另一个项目
(`Project=final-table-trainer`)09-05 用 `c7a.16xlarge` spot 烧掉 **$17.109 / 14.99 机时**,
**而且此刻还有一台 `r7a.16xlarge` 在跑**(`i-0a7d4c7fbeb3a6712`,02:10:36Z 起,现价
$1.82–2.79/hr)。⇒ **闸 (iii) 的 $50 围栏只剩 $9.652 headroom,按那台机器的费率约五小时后
(~11:30Z)被别人的算力撞线**,而 **owner 收到的第一封预算邮件抬头会是 `dota2bot-batch`**。
这正是 **GH #515** 预判的形状,本轮它从预判变成了读数。

---

## 一、自检(铁律 10)

⭐ **开工第一条命令又误用管道,脚本自拒** —— `REFUSED: routine_selfcheck.sh stdout is a
pipe; exit 2, nothing checked`,脚本自己点名「**同形已复发 5 次,每次都是当轮第一条命令**」。
那个 `EXIT=2` 是 **REFUSED 不是通过**。改重定向 + 放后台后重跑(上一轮交棒明写「重定向不管道、
给足 ≥45min」——**这一半照办了,管道那一半没有**)。

**它跑满约 40 分钟,在两次 push 之前跑完。** ⚠️ **真码读脚本自打的那一行,不读 harness 报的**
(后台任务通知的 `exit code 0` 是末尾那个 `echo` 的码):

```
legs run              : 10
FINDINGS (exit 3)     : cadence queue-rulings owed-executions
UNCERTIFIABLE (exit 2): trunk-red(python)
NOT RUN (inside a leg): tests/test_rc_wrapper.py tests/test_selfcheck_lua_leg.py
selfcheck worst exit: 3
```

**`3` = 有发现,不是没跑成。** 逐条:

- `cadence` **1 条 `GAP cadence strategy`**,**不是本台**;另 7 条 `SKIPPED-IN-STREAM`
  是历史畸形报告名,与历轮同。
- `queue-rulings`:`strategy-43 status=registered ruled=True`。
- `owed-executions` 里**点名本台的仍是那一条**:
  `OWED campgrade_wave_type_premise_reading GH #480 / GH #241
  executor=batch-desk(排 campgrade 独占波的那一轮)+ 录像组(读数)`
  ⇒ 与第六节交棒第 5 条是同一件事,**连续第三轮欠着**。
- ⛔ **`trunk health (python)` 这一腿本轮 UNCERTIFIABLE(exit 2)——「没跑成」不是「绿」**。
  工具自己的话:`re-run on a quiet tree (nothing writing under bots/)`。归因大概率是本轮
  在跑它的同时正在写 `iterations/` 下的文件。⇒ **本轮 python 侧 trunk 红不红,本台答不了**。
- ⭐ `trunk health (fast Lua detectors)`:**`84 tagged detector file(s), 0 failures`**
  ⇒ Lua 侧跑过且绿。⚠️ 边界照抄:`FAST SUBSET, not the full suite. Untagged tests ...
  are NOT covered here.`

另有一条当场登记(逐字):`rc.sh` 自测有 **2 个 UNCERTIFIABLE**,两条原因相同——
**`no lua5.1 on PATH`**,于是那两条检查会「空过」(`127 != 2` 恒真);同一容器里 `5a0` 腿报
`84 file(s) ... in 120.1s (budget 120s)` 超预算 ⇒ 连带四条 `UNC`。
**这些是「没跑成」不是「跑成了是绿的」**,与 GH #171 同族。

## 二、⭐⭐⭐ 成本与闸 (iii):本轮的头号产出,而且这笔钱不是本台花的

**`COSTS_EXIT=0`**,九月 MTD **`$39.328`**(免费 `budgets` 通道,`refreshed
2026-09-06T06:02:46Z`;`check_costs.sh` 因读数 ≥$35 自动付了一次 CE 确认,
逐位复现 `39.3278311289`)。**较上一轮(09-06T03:11Z)的读数跳了约 $19.7**,
而本台在这段时间里只发了 W50 一波(登记 ~$1.02)。⇒ **差额不是本台的**。

**闸 (iii) 走工具不抄常数**(`wave_fence.py --planned 1.02`,**`FENCE_EXIT=0`**):

```
limit $100.00 | actual (MTD) $39.328 | planned $1.020 | projected $40.348
  ACTUAL alert $50.00  NOT yet crossed   AWS state=OK
  ACTUAL alert $80.00  NOT yet crossed   AWS state=OK
  ACTUAL alert $100.00 NOT yet crossed   AWS state=OK
fence $50.00 | brake $90.00 | operative ceiling $50.00
headroom $9.652 after this wave
WAVE_FENCE: CLEAR (exit 0)
```

### 2.1 归因:CE 两次付费读数把它钉死了

第一次(DAILY × SERVICE,`CE_EXIT=0`):

| 日 | 合计 | EC2 |
|---|---|---|
| 09-01 | 4.471 | 2.936 |
| 09-02 | 4.198 | 3.673 |
| 09-03 | 4.115 | 3.679 |
| 09-04 | 5.800 | 5.221 |
| **09-05** | **20.742** | **20.106** |

第二次(09-05 × USAGE_TYPE,`CE2_EXIT=0`)把那 $20.106 拆开:

```
USW2-SpotUsage:c7a.16xlarge   $17.109  qty=14.99      <- 不是本台的机型
USW2-BoxUsage:c6i.4xlarge     $ 2.081  qty=3.06
USW2-SpotUsage:c6i.4xlarge    $ 0.917  qty=3.27       <- 本台的 spot 波
```

⭐ **本台的批测机型是 `c6i.4xlarge`(备选 `c6a.4xlarge`),从来不是 `c7a.16xlarge`**。
09-05 本台发了 W48、Y1 标尺波、W49 三波,对应的就是那两条 `c6i` 行,合计 **$2.998**;
**`c7a.16xlarge` 那 $17.109 与本台无关**。

### 2.2 ⛔ 此刻仍在跑的那一台(全账号实例普查,不只查本台 tag)

```
i-0a7d4c7fbeb3a6712  r7a.16xlarge  spot  running  LaunchTime 2026-09-06T02:10:36Z
  Tags: Name=fft-cache-v1-repair | Project=final-table-trainer
```

现货报价(`describe-spot-price-history`,06:00Z):`r7a.16xlarge` **$1.82–2.79/hr**。
起飞至读数时刻已约 **4.2 小时**。

**推论(算术,不是猜测)**:围栏余量 $9.652 ÷ 约 $2/hr ≈ **4.8 小时** ⇒ 约 **11:30Z**
撞 $50。而 $50 是**三档 ACTUAL 告警里最低的一档**,也就是 **owner 的第一封预算邮件**,
**抬头是 `dota2bot-batch`**。同时 `budgets` 自报 **forecast $179.873 vs limit $100** ——
按当前速率九月会**冲穿上限**。

**⛔ 本台没有动那台机器,也不打算动。** 它带着**另一个项目的 tag**
(`Project=final-table-trainer`),不是本台的批测实例,也不在本台的自毁体系里;
终止别的项目正在跑的算力是**破坏性且越权**的动作,归 owner 判断。章程里
「还在跑就该终止」那一条的对象是**批测实例**,不是账号上任何一台机器——
本轮特意把这条边界写下来,免得下一轮有人照着字面去关。

**已推送手机通知给 owner**(本轮唯一一次),内容 = 本节 + 下面 W50 一行结论。

## 三、⭐ 收割 W50(4/4 干净)

**闸 (i) 先答「本轮该不该发波」:不该。** `wave_throttle.py` 自打的那一行:
`WAVE_THROTTLE: THROTTLED (exit 3) -- do not launch. Wait 11045s.`,锚点 = W50 末台
`2026-09-06T03:20:34Z`,解锁 `2026-09-06T09:20:34Z`。⚠️ **真码是 3**:第一次调用误用
`| tail` 得 `THROTTLE_EXIT=0`,那是 **tail 的码不是工具的码**(evidence-discipline 3,
本轮第二次踩同一个坑);去掉管道重跑得 **`THROTTLE_EXIT_TRUE=3`**。
⇒ **本轮是收割轮,不发波**,与 P4.1 无冲突(标尺波 Y1 已于 09-05 15:16Z 发出并在
18:0xZ 收割,那一棒已交)。

**下机状态**:四台 SIR `Status.Code` **全为 `instance-terminated-by-user`**
(GH #408 的判据是 SIR 的 `Status.Code`,不是 EC2 的 `StateReason.Code`)⇒
**自毁,零抢占**,与 W45 那次两台被回收形状相反。

**输入普查(GH #225)**:逐 run 下载进**四个独立子目录**(不 flatten),并把 S3 侧
`s3 ls --recursive | grep -c analysis.json` **逐 run** 与本地文件数对读:
**48/48、48/48、51/51、64/64 ⇒ 211 对象 / 211 文件,零丢失**。
`recover_verdict.py` **`RV_EXIT=0`**,无任何 R1/R2/R3 拒绝;
`input = {files_seen 211, games_loaded 211, source_dirs 4, unparseable 0}`。
每 run 恰好一粒种子(295740→5813、e56ba0→5830、f2182d→6072、eff51c→6128,
与 `machines[]` 逐一对上),每 run 6 局暖场(共 24)**不是计分局**。

**臂串复核(不抄 prose)**:`git show b73358bc:iterations/streams/test_set.md | sed -n 2p
| tr -d '\n'` ⇒ **59 ids / 524 bytes / md5 `572b60753699bc68b2ad5ec9020f2512`**,
与记录逐字节相同。⭐ **发波树 `b73358bc` 在本容器里不 deepen 就能看见**(`TREE_PRESENT`)
⇒ **GH #520 的浅克隆坑本轮没咬到**(上一轮它咬过一次)。

### 3.1 读数

| 量 | mean | comps_better |
|---|---|---|
| gpm | **+13.76** | 2/4 |
| xpm | +2.46 | 1/4 |
| deaths | +0.07 | 1/4 |
| last_hits | +1.13 | 2/4 |

`scored_games` **187**、`unfinished_games` **0**、`min_arm_depth` 8、
`thin_arm_seeds` **空**、`excluded_seeds` **无**(最浅 arm_depth **17.14**,是门槛的两倍多)。
逐粒 arm_depth:5813 **17.14**、5830 **19.29**、6072 **18.49**、6128 **23.17**。

⚠️ **离散度是本波的头号保留意见**:逐粒 gpm **−39.01 / −26.67 / +1.08 / +119.64**。
**+13.76 这个均值由一粒(6072)扛着**,去掉它剩下三粒均值 **−21.53**。按铁律 4(i-c),
说精度的量是 **arm 自己的跨种子离散度**,而这个离散度宽到足以让本台**登记均值但不为它背书**。
`suggested = hold_or_reject`——工具只读 gpm 的提示:**+13.76 过了 >5 那一半,
comps 2/4 没过「多数」那一半**(`2*2 > 4` 为假)。**这是读数不是裁定**,
59-id 家族的 promote/reject 归总监。

### 3.2 铁律 4(i-a):两个分层的**读数**(不是局数),全部取自工具自己的字段

| 量 | strata.ab | strata.ba | side | sign_flip |
|---|---|---|---|---|
| gpm | −131.61 | +159.12 | **−145.37** | true |
| xpm | −85.48 | +90.41 | −87.94 | true |
| deaths | +1.03 | −0.90 | +0.96 | true |
| last_hits | −3.85 | +6.12 | −4.99 | true |

四个量**全部 sign_flip=true**。按 4(i-c) 这四个都是**已消掉侧偏**的估计量(每粒 50/50
swap-average),所以反号是恒等式 `|side|>|arm|`,**不是否决理由**;**登记,不当判决读**。

⭐ **交总监的一条**:side 项**巨大且相对 W49 反了向** —— W49 gpm side **+46.37**,
W50 **−145.37**。这是**抽到的阵容**在说话,不是臂在说话;而它恰好演示了 4(i-d) 为什么
禁止按局数并池:本波 ab 腿对 ba 腿是 **131:56**,按局加权会把 `(131−56)/187 = 0.40`
份的 **−145.37** 直接灌进答案里。**先每粒 swap-average、再跨种子取算术平均**,本轮照做。

### 3.3 winrate:第九次 DEGENERATE,而这一次它是**独立于金钱**的

`winrate_channel` **DEGENERATE**、`mean.winrate_headroom` **0.0096**、
`winrate_side_census` **{radiant 1, dire 210}**、`winrate_minority_side_share` **0.0047**。
按本波**数据落地前**就写死的预登记:**winrate 与 comps_better.winrate 不得写成读数,
且不逐波复议**。

⭐ **但有一件新东西必须登记**:`winner_by` = **`{engine_natural: 211}`**,
`winrate_independent_of_gold` = **`211/211 games`** —— **本波每一局都是真的推掉了遗迹**,
econ_winner 改写**一次都没触发**。⇒ 这是通道**完全独立于金钱**的一波,**而它仍然没有信号**,
且 0.0047 是**至今最差的一次**(W49 是 0.0208)。**这是 W49 交给总监那条「该通道对本
harness 结构性无信息」主张的最强形态**:退化不是 econ 改写的假象,因为本波根本没改写。

### 3.4 ⭐ 闸 (iv) 本轮先报 UNDECIDABLE,原因是**发波轮没记 survival 通道**

`reclaim_blind.py --wave-json W50_wave.json` 第一次跑 **`RB_EXIT=2`**:
`UNDECIDABLE: seed 5813: needs survival_min, or both create and update`。
**W50 的 `machines[]` 四个字段全是 null,而发波轮也没写 `survival_min`**
(W48/W49 都写了 `survival_min`,W47 写的是 `sir`+`create`)。

处置:**第一手**从 SIR 行读回 `CreateTime` / `Status.UpdateTime`(工具第 103 行文档的形状),
补进 `machines[].sir/create/update` 后重跑 ⇒ **`RB_EXIT=0`**:
`VERDICT: not blinded -- the wave delivered 4 paired seed(s). NEXT WAVE: spot (GH #158 unchanged).`

⭐ **顺带买到一个此前没量过的数**:四台**都没跑满 2h 看门狗**,而是**干完活自毁**——
存活 **44.98 / 47.12 / 55.15 / 56.73 分钟**(5813 / 5830 / 6128 / 6072)。
⇒ 真实机时约 **3.4** 而不是 2h×4=8;账单侧 09-05 那条 `c6i` spot **$0.917 / 3.27 机时**
正是同一形状。**本波落在 ~$1.02 常数的便宜一侧。**

⚠️ **这是一个会复发的洞,不是一次性失误**:闸 (iv) 要的 `update` **只有终止后才存在**,
而 SIR 行**不会永远留着**;`sir`+`create` 却**发波当轮就能写**(W47 写过)。
一旦某轮收割赶在 SIR 行消失之后,闸 (iv) 就**永久 UNDECIDABLE**,而闸 (iv) 决定的是
**下一波的市场类型** ⇒ 那是把市场决策弄瞎。**与 GH #544 把 `launched_at` 挪到发波轮
写死是同一个缺陷族**。本轮**开 issue**(见第五节),并在下一轮发波时**当轮写 `sir`+`create`**。

## 四、局数(章程第 7 步)

**W50 结清照登不重算**:四粒完整(`ab/ba` = 30/12、27/15、32/13、42/16),
**187 局计分**,0 局未完成,24 局暖场不计。**59-id 家族现有 4 粒**(W50 自成一族,
按数据落地前的预登记**与 W46+W47、W48、W49 都不并池**——四个 md5 各不相同)。

预期局数那一格照上一轮的告诫处理:记录里登记的是 ~96,**实交 187**,
**继续印证「~96 这个预登记量偏低约一倍,不得用作『交付不足』的分母」**(W49 实交 168)。

## 五、本轮开出的 issue / 追评

按铁律 6 / GH #290 的**顺序**:**先 push,再发表**,发表前草稿跑 `claim_precheck.sh`。
读数见第七节。

1. **GH #515 追评**(预算被账号上别的项目吃掉):第二节全文,带两次 CE 付费读数、
   机型对照、和此刻仍在跑的 `i-0a7d4c7fbeb3a6712`。**这是从预判变读数**,不新开 issue。
2. **`[batch]` 新 issue**:发波轮必须当轮写 `machines[].sir` 与 `create`,
   否则闸 (iv) 依赖一个会过期的 SIR 窗口(第 3.4 节)。

## 六、交棒

1. ⭐⭐⭐ **owner** —— `i-0a7d4c7fbeb3a6712`(`final-table-trainer`)要不要继续跑,
   以及 `dota2bot-batch` 预算要不要**按项目拆开**。**本台不动它**;不拆的话,
   本台会在别人的花费上被闸 (iii) 挡住发波,而 owner 的告警邮件会指向错误的项目。
2. ⭐⭐⭐ **总监** —— **59-id 家族 W50 的 promote/reject**:gpm **+13.76 / comps 2/4**,
   **且均值由单粒 6072 扛着**(去掉后 −21.53)。请连 3.1 的离散度一起裁,不要只读均值。
3. ⭐⭐ **总监** —— **winrate 通道第九次 DEGENERATE,而本波是 `engine_natural 211/211`**
   ⇒ 「结构性无信息」这条主张现在有了**独立于金钱**的证据(3.3 节),该结案了。
4. ⭐⭐ **录像组** —— W50 的 `rotscope` 条件 (a):**只有 5813 / 5830 两粒带 pudge**
   (W48 的一半),**不许把更小的计数读成更小的效应**;`zusboltdom` 的反向刹车
   (zusult 触发数 **必须**与击杀落地数同时给)**本台的经济裁定答不了**,归录像组。
5. ⭐ **本台** —— `campgrade` 独占波仍不存在(#480/#241),那一棒**连续第三轮**欠着。
6. ⭐ **总监** —— `$0.90` 单波成本常数的自动失效条款**仍未重裁**(连轮点名);
   本轮实测又给了一个新数据点:W50 真实机时 3.4,落在常数的便宜一侧。

## 七、铁律 6 / 11 与收尾读数

(收尾时回填:静态门真码、推送门、两次 push、泄漏复检、自检最终读数、token。)
