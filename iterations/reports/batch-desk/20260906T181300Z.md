# 批测台 2026-09-06T18:13–18:5xZ

**发波轮 —— 连续第六轮欠着的 `campgrade` 独占入集波,本轮发了出去(W51)。**
**⭐⭐⭐ 头号产出不是那四台机器,是本轮自己在起飞后 70 秒抓到并杀掉的一波:
它每一道闸都是绿的,却买不到它唯一存在的理由。**

---

## 一、自检(铁律 10):EXIT=124,python 腿这轮没人看过

⚠️ **开工第一条命令又误用管道** —— `routine_selfcheck.sh | tail` 触发脚本自拒
(`REFUSED: stdout is a pipe; exit 2, nothing checked`,**同形第 7 次,又是当轮第一条命令**)。
改重定向重跑得 **`EXIT=124`** = **`timeout 400` 砍死在最后一条腿(python 套件)上**,
**既不是干净也不是发现**。上一轮的教训(「不设短 `timeout`、放后台给足 ≥45min」)
**本轮没照做**,这是本轮第二个自伤。

**已跑完并读到的**:stable-v1/v2/**v3** 锚点 `EXISTS/PINNED/SHIPPED` 全 ok
(v3 = `slotwait`,`0d0a7201b8f2`,本轮第一次在自检里看到它);
promote-time 约束 **`160 live gate id(s)` / `FROZEN none`** ⇒ **`pullcad` 陷阱无复发**,
三个 co-promote 原子(`field_hold_needs_magnitude` / `tp_response_releases_need_commit` /
`static_field_needs_binding`)全 `ok`;`owed executions` registry 9 行,
其中 **`campgrade_wave_type_premise_reading` 仍是 OWED** —— 那正是本轮要买的东西。

⛔ **python 腿这轮没人看过**,不声称 trunk 是干净的,也不声称它是红的。

## 二、成本:`$50` 越线后的第一轮,而围栏**按上一轮的预登记自动升了一档**

`AWS_SETUP_EXIT=0`(`arn:aws:iam::581436534924:user/dota2bot-agent`)。
`COSTS_EXIT=0`。九月 MTD **`$50.804`**,来源 `budgets/dota2bot-batch`(免费),
**`budget refreshed 2026-09-06T15:19:34Z`** —— ⭐ **翻过了** `06:02:46Z`
(那个数在 09:12Z / 12:12Z / 15:20Z **三轮逐位不动**,滞后量到 ≥9.3h)。
因 ≥`$35` 自动付一次 CE 复核:**`50.8036495163`**,与 `budgets` 一致。

⭐⭐ **上一轮那条「有界不是死锁」的预言本轮兑现,一个字都不用改**:
15:20Z 那轮的活工具打 `THROTTLED`,离线喂 CE 实测打 `CLEAR`,**翻转 100% 在「哪一档算已跨」上**;
那一轮**拒绝自行取用 CLEAR 那一读**,并写下「`budgets` 一刷新过 `$50`,围栏自动变 `$80`,本条自解」。
本轮 `budgets` 刷新了,**围栏自己变成了 `$80`,活工具自己打了 `CLEAR`** —— 不需要任何人推翻任何工具。

**闸 (iii) 走工具不抄常数**,`wave_fence.py --planned 1.10 --pending 10.93`,**`FENCE_EXIT=0`**:

| 量 | 读数 |
|---|---|
| limit | `$100.00` |
| actual (MTD) | `$50.804` |
| pending | `$10.930` |
| planned | `$1.100` |
| **projected total** | **`$62.834`** |
| `$50.00` | **crossed**(AWS state=**ALARM**) |
| `$80.00` / `$100.00` | NOT yet crossed(state=OK) |
| fence | **`$80.00`**(最低那档未跨的 ACTUAL 告警) |
| brake | `$90.00` |
| operative ceiling | **`$80.00`** |
| **headroom** | **`$17.166`**(发完本波之后) |

**`--pending` 是本轮重估的,不是抄的**(上一轮交棒 ② 点名警告过):CE 本轮读 `50.8036495163`,
与 15:20Z 那次**逐位相同** ⇒ **CE 在这 ~3h 里一分钱都没入账** ⇒ 15:20Z 证明仍未入账的那
**`$10.926`** 外来 `r7a` 燃烧**至今仍未入账**。`pending = 诚实总额 − 工具当轮读到的 actual
= $61.73 − $50.804 = $10.93`。
⚠️ **这是下界,而且它的出处本轮已经无法复算**:那两台外来机的 SIR 行本轮返回 **0 行**
(窗口 ≲3h,15:20Z 量到的那条性质),数字只能靠上一轮报告里抄下的值活着,
**而上一轮自己证明了那份重建偏低**。⇒ **GH #565 那条「按 SIR 补回已终止机时」必须在花钱那一轮当场跑**,再次坐实。

⭐ **外来负载确实是停的**:全账号 `describe-instances`(全状态、不带过滤器)在发波前返回 **0 条**,
`describe-spot-instance-requests` 同样 **0 条** ⇒ 燃烧率 `$0/h`。
⛔ **未用被禁的「MTD 增量对得上波费」手法**(铁纪律乙)。

## 三、闸:三道 pre-launch 全绿,**第四道我漏跑了**

- **闸 (i) `wave_throttle.py`(不带管道)**:`THROTTLE_EXIT=0`,锚点 W50 末台
  `2026-09-06T03:20:34Z` ⇒ unlock `09:20:34Z`,`margin +32003s past unlock`。
  **发波脚本里作为最后一条命令又跑了一次**(工具自己写着「it is the action, not the
  decision, that the gate binds」):**`THROTTLE_EXIT_AT_LAUNCH=0`**,`margin +32264s`。
- **闸 (ii)**:本波 armed 串是 **1 个 id 的独占串**,与任何一波都不逐字相同 ⇒ 最强分支通过。
- **闸 (iii)**:见第二节,`FENCE_EXIT=0 CLEAR`。
- ⛔ **闸 (iv):本轮漏在起飞前,是起飞后被测试文件抓出来的。**
  `tests/test_wave_gate_keys.py` 在我写完波次记录后打 **3 个 FAIL**,其中两个是
  「W51 没有 gate (iv) 的两半」。补跑:`reclaim_blind.py --wave-json W50_wave.json`
  **`RECLAIM_BLIND_EXIT=0`**,`VERDICT: not blinded -- the wave delivered 4 paired seed(s)`,
  四台全 `instance-terminated-by-user`、0 台在 flip 前被回收,
  且工具自己的收尾行 **`NEXT WAVE: spot (the default; GH #158 unchanged)`** 反过来给本波的市场选择背书。
  **两半都绿 ⇒ 发波决定不变**;但**「碰巧是绿的」不等于「事先查过」**,这一条按原样写进了波次记录,
  没有抹平。下一次发波脚本要把 (iv) 和 (i)-(iii) 排在一起。

## 四、⭐⭐⭐ 本轮头号产出:一波闸全绿、却买不到自己存在理由的波,起飞 70 秒后被杀掉

**18:18:19–18:18:30Z 我先发出了四台**,`SPOT_RUN_EXIT` 全 0、四个 AZ 全部拿到、零容量错误 ——
**从每一个门的角度看它都是一波好波**。它错在一个**没有任何门会看的地方**:
我没传 `--rec-slots 8`,于是它吃了 `spot_run.sh` 的默认 **`REC_SLOTS=1`**。

**为什么这是缺陷不是口味,两条独立理由**:

1. **它在同一波里同时挪动了「被测 id 集」和「采集配置」。** 章程那条
   「never move the tested id set and the acquisition config in the same wave」
   正是为了让一个意外读数**不能被两个变化同时解释**。本波的 id 集挪动幅度是**最大的一种**
   (55-id 成员串 → 1-id 独占串),偏偏这时候把采集配置也挪了。
   `--slots 16 --hours 2 --rec-slots 8 --games 12` 自 **W39–W50 逐字未变**。
2. **更要命的是它买不到这一波唯一存在的理由。** 本波是为了买 **一条**行为读数
   (GH #241 `.type` 前提),而那条读数跑在 **behav-dump 时间线**上,
   dump 来自**被录制的对局**;`rec_slots=1`(16 槽里录 1 槽)⇒ 录制语料约为 W39–W50 标准的 **1/8**。
   最可能的结局是 **`DOMAIN-NOT-REACHED`** —— 而那条裁定**明文禁止**把它读成
   「测过了,`.type` 不是字符串」。**一波答不出自己问题的波,比省下的 `$0.06` 更贵。**

⇒ **`terminate-instances` 四台,`KILL_EXIT=0`**,四台全部 `running → shutting-down`,
存活约 70 秒,代价 **≈ `$0.06`**(4 × ~0.02h × ~$0.28/h),**低于本轮每一个围栏读数的分辨率**。

⚠️ **重发换了四粒新种子,不是原来那四粒**(6211/6277/6343/6409 → 6421/6487/6553/6619)。
理由不是洁癖:70 秒几乎不可能产出哪怕一局暖场,但**「几乎不可能」是个假设**,
而 `recover_verdict.py` 是**按 `s<SEED>` 戳跨 run 并池**的 ⇒ 死前缀下只要漏出一局,
它就会并进本波的读数里,**而没有任何一个门会举手**。换种子是把假设**去掉**,不是依赖它。
⇒ **6211/6277/6343/6409 现在算已花掉的种子**,`seed_roster_index.py` 只有在那些前缀真有局时才学得到,**要手动当成 USED**。

## 五、W51 发波登记

| 项 | 值 |
|---|---|
| 波次 | **W51**,`campgrade` **独占入集波**(GH #480 / #241) |
| armed 串 | **`campgrade`** 单 id,**9 字节**,md5 **`185481d3d036c8dcf44313a4ad2b249a`**(本轮 `printf \| md5sum` 现算) |
| 树 | `44329f419173e5f081a139e345eceecabf8a4348`(`--ref` 用 40 位 SHA,不用 `main`) |
| 树核对 | `git ls-remote origin main` **读两次**:闸时一次,发波脚本**第一条命令**再一次(tip 若移动则 exit 9、一台不发)。两次都等于本地 HEAD |
| 拓扑 | 4 台 × 1 粒种子,`c6i.4xlarge`,16 槽,**`--rec-slots 8`**,`--hours 2`,`--games 12` |
| 种子 | **6421 / 6487 / 6553 / 6619**(选种前先 `seed_roster_index.py --build`,`ROSTER_EXIT=0`,225 粒已存;四粒逐个验过未用) |
| 市场 | **spot**,由 `describe-instances` 的 `InstanceLifecycle=spot` **四台逐台核实**(run_id 的 `spot_` 前缀不是证据);未传 `--on-demand`,容量降级阶梯**一步都没进** |
| AZ | **四个不同 AZ**,每次调用显式 `--az`,四次全部拿到**所请求的那个**;零 re-aiming、零 `RING EXHAUSTED`、零 `InsufficientInstanceCapacity` |
| 接线 | `check_armed_wiring.py --cand campgrade` **`WIRING_EXIT=0`**,`1/1 WIRED`,direct,1 站点 `bots/mode_farm_generic.lua:304` |
| 载体门 | **不适用**,且这是总监原话:`queue.json:strategy-4` 写「独占波,载体门对它是 no-op(按等级不按英雄)」⇒ 故意**没跑** `seed_draft.py --assert-carrier` |
| 成本登记 | **`~$1.10`**(GH #454 本轮重裁的常数)+ 第四节那 `~$0.06` |
| 自毁 | spot_run.sh 自终止 + 2h 看门狗 + one-time 请求(被回收不会静默重发) |

**⛔ 排波前置(本波的合法性就挂在这一条上)**:`owed_executions.json` 那行写着
**`campfarm` 与 `campgrade` 不得同腿 arm** —— `campgrade` 在上游把远古营从名单里删掉,
同腿则本方远古 engagement 的下降**在两个 id 之间不可归因**。
`campfarm` **就在当前 55-id 成员串里**(本轮实测 `sed -n 2p` 数得),
⇒ **独占波是唯一满足该前置的形状**,而本波正是独占波。
顺带实测:`campgrade` **不在**成员串里(`grep -c` = 0)—— 它是**待入集的候选**,不是成员。

**四台的 SIR 全部在发波当轮读下来了**(GH #556 + 上一轮量到的「SIR 窗口 ≲3h」):
`sir-6j67gkip` / `sir-2mcfj4gn` / `sir-7c9zjdbm` / `sir-rmn7jrbq`,四条全 `active / fulfilled`,
`create` 与 `launch_time` 一并写进 `machines[]`(GH #544:**收割时不必再回头修**)。
末台 `18:19:42Z`,slate spread **10s** ⇒ 下一波闸 (i) unlock `2026-09-07T00:19:42Z`
(⚠️ **别抄这个数,跑 `wave_throttle.py` 读它打的那行**)。

**波次记录**:`iterations/reports/batch-desk/waves/W51_wave.json`。
写完后按上一轮交棒 ③ 复跑两个测试:**`test_wave_gate_keys.py` 285 checks / 0 failed**
(先红 3 条,补齐 gate (ii) 别名与 gate (iv) 两半后转绿)、
**`test_wave_throttle.py` 55 checks / 0 failed**。

## 六、收割:无新数据(查过的,不是默认的)

`soak/` 最新四个前缀就是 W50 那四台(`spot_20260906_0320*`),**已由 06:14Z 那轮 4/4 收完**;
`validation/` 最新仍是 **2026-07-23** 的历史物。⇒ 本轮**无新数据可收割**,
这也正是闸 (iv) 第一半(inputs — no harvest owed)成立的读数。

## 七、交棒

1. ⭐⭐⭐ **录像组** —— **W51 的语料 ~20:20Z 前落地,那条欠了六轮的读数现在买得到了**:
   本方 10..11 带远古 engagement,`campgrade` armed 腿 vs baseline 腿。
   **尺子已存在,不要另发明一把**:`ancient_camp_domain.py` + `campgrade_ladder.py`
   (与 `campfarm` 入集验收逐字同一把)。
   **判据的算术**:反驳假设下 `campgrade` armed **动不了这个数** ⇒ **降 ⇒ `.type` 是字符串,前提结案;
   不降 ⇒ 不确定,不是通过**。**反向护栏**:远古 engagement 总数**不许塌到 0**。
   **铁律 4(i-a)**:ab / ba 两层各自登记**读数**(不是局数)。
   ⛔ **域为零 ⇒ `DOMAIN-NOT-REACHED` 退回**;归类取不到 ⇒ **回总监重裁**,执行方不得自行套用后果条款。
   全文已转载进 `W51_wave.json:harvest_obligations[0]`。
2. ⭐⭐ **总监** —— `campgrade_wave_type_premise_reading` 的 **trigger 本轮已触发**
   (那条常驻行的 `trigger` 写的就是「`campgrade` 的独占入集波 —— 它此刻还不存在」;**现在存在了**)。
   读数落地后可退休该行。同时 **`wave_fence_ruling4_first_live_run`(GH #515)本轮已结清** ——
   见第二节的活跑读数与逐字抄下的 accrual 行,可一并退休。
3. ⭐⭐ **总监** —— **`strategy-4` 的 `status` 是 `harvested`,但 `campgrade` 从未被任何一波 arm 过。**
   实测:W24–W50 + Y1 共 29 张波次记录 **`grep -rl campgrade` 零命中**,
   且 09-04 的裁定原文说这条独占波「此刻还不存在」。⇒ 那个 `harvested` 是 W4 时代的**陈账**,
   本轮**没有据它做任何决定**(发波依据取的是 `director.ruling = APPROVED_CONDITIONAL`
   与它自己那句「名额是集合增长的闸门,不是发波的闸门」)。**请修正该 status,否则下一个读它的人会以为这波跑过了。**
4. ⭐ **总监** —— 闸 (iv) 本轮**漏在起飞前**(第三节),两半事后补跑全绿。
   立案点不是这一次的结果,是**发波脚本里没有 (iv) 这一条** ⇒ 它靠的是事后有个测试文件碰巧会红。
5. ⭐ **总监** —— GH #454 的两条腿(乙1 账单侧单波 > `$1.10`;乙2 自终止机机时均值 > `1.00 h/台`)
   **发波时不可评估**,欠在 W51 的收割轮,已写进 `harvest_obligations`。
6. ⭐ **owner** —— `$50` 档仍是越线状态,**诚实 MTD ≥ `$61.73`**(下界),
   其中约 **61% 不是批测的**(外来 `final-table-trainer`)。外来负载**已停**(燃烧率 `$0/h`),
   ⇒ **不紧急**;但已花的不退,且 `budgets` 自报 forecast **`$185.828` vs limit `$100`**。
   两条路不变:给预算加 cost filter(⚠️ 走 `Project` 标签**必须先给批测机器补标签**,
   否则 fence 恒读 0,**一个永远 CLEAR 的闸比现在更坏**),或给 `final-table-trainer` 单开预算。

## 八、下一轮本台

1. **先收割 W51**(`recover_verdict.py` 走 S3 逐局全量重算的标准路径;
   **先按 run 分目录下载再带前缀合并** —— 同秒级启动的 basename 撞车曾静默丢掉 15.9% 的局)。
2. 收割当轮**当场**结清三条 `harvest_obligations`:`.type` 前提读数(交录像组)、
   GH #454 两条腿、以及按 SIR 补回机时(**SIR 窗口 ≲3h,过了就不可复算**)。
3. ⚠️ **开工第一条命令**:重定向、`nohup`、**不管道、不设短 `timeout`** ——
   管道那一坑本轮又踩了一次,`timeout 400` 又砍死了 python 腿。
4. 下一波发波前**把闸 (iv) 排进发波脚本**,和 (i)-(iii) 并列。

## 九、铁律 6 / 11

- `bots/` / `game/` **本会话一行未改**;改动只在 `iterations/`(报告、波次记录、章程、
  `queue.json` 一个 key、`seed_roster_index.json` 由 `--build` 重建)。
- **铁律 11**:本会话无 `requires approval`、**无空转等待**。
- CE 付费 **1 次(`$0.01`)**,是 `check_costs.sh` 在 ≥`$35` 时的自动复核。
- 动态半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)**未跑,不声称**。

## 十、本轮 token

`TOKENS total_in=7,611,651 out=55,596 turns=57`(到统计时刻为止;统计后的收尾回合不计入)。
