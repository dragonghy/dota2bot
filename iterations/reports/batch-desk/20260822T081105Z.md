# 批测台报告 2026-08-22T08:11:05Z(第四十一次触发)

**类型:收割轮 + 启动轮。** 收割 06:10Z 的 `roamreach` 单 id 波次(AG.4 阶梯第 (2) 步),
并按 owner 08-22T07:23Z 新指示(commit `823a48e`,写进本台章程末节)启动**全测试集 armed
的默认波次** —— 这是自 08-20T18:08Z 以来第一次全集波次。

---

## 1. AWS 引导与成本

- `session_setup.sh` → `AWS ready: arn:aws:iam::581436534924:user/dota2bot-agent`(容器无 AWS CLI,
  脚本自行 pip 装了 `aws-cli/1.46.0`)。之后一律 `awsx`。
- `check_costs.sh`:**MTD $18.605**,forecast **$23.006**,budget limit $100,
  budget refresh **`2026-08-22T06:30:39Z`**(**是新的一次刷新**,不同于前五轮共用的
  `08-21T21:23:52Z`;数字从 $18.587 动到 $18.605,+$0.018)。
- 免费 Budgets 路径**连续第十二轮**,**CE 调用 0 次**。
- 围栏与刹车:$45 围栏余 **$26.40**,$90 刹车线余 **$71.40**;本波预估 **$1.56** ⇒ MTD ≈ $20.2。
  **未触发任何预算刹车。**
- **CE 必查项:目标日仍 08-23 查 08-22 完整日**(02:06Z 定,不再逐轮顺延)。08-21 已判定被污染
  (~16 次),**看到 16 不要误判成修法失败**。

---

## 2. 收割:`roamreach` 单 id 波次(4 种子全齐,282 局有效局)

树 `03316d1`,armed 串 = `roamreach` 单 id,种子 888/895/896/906,一台一种子。

### 2.1 06:06Z 立的五条必查项,逐条结果

| # | 必查项 | 结果 |
|---|---|---|
| ① | 暖场 stamp 必须是 `03316d1` | **通过** —— 23 个暖场局 stamp 逐个都是 `03316d1`,裸 SHA checkout 成功,**数据有效** |
| ② | 先按 run 分目录下载再带前缀合并 | **照做** —— 77+79+77+72 = **305 个 `analysis.json`,合并后仍 305,一个不丢** |
| ③ | S3 真实 cand stamp 应恰好是 `roamreach` 单串 | **通过** —— 全部 282 局的 stamp 都是 `mirror:roamreach:s<seed>:<side>`,**20 个 eligible id 一个都没出现** |
| ④ | 承重判据是录像组域内检测器,gpm 只作背景;打 A−B 的表必须每个指标都打基线腿 | **照做**,见 2.3;经济读数在本报告里**只作背景** |
| ⑤ | `killed` 指标已被 §AJ.3 判为不可用 | **未使用**(本台不产出 `killed`) |

### 2.2 局数与配平

| 种子 | radiant wave | dire wave | 合计 |
|---|---|---|---|
| 888 | 42 | 29 | 71 |
| 895 | 42 | 31 | 73 |
| 896 | 42 | 29 | 71 |
| 906 | 43 | 24 | 67 |
| **合计** | **169** | **113** | **282** |

**4 种子两 wave 全齐,无饿死。** 另有 23 局暖场(不算有效局)。radiant 打满 `--games 22` 配额
(42-43 局),dire 仍被 35min stall 上限截断 —— 与 08-19T20:11Z §5 的结论一致,**不建议继续加
`--games`**(只会拉长 radiant 加大不对称),补 dire 要动 stall 上限(harness 侧)。

### 2.3 读数(标准路径 `recover_verdict.py` 全量重算)

**按 §AJ 通例二,每个指标都打印基线腿(不只打承重的那个):**

| 指标 | 候选腿(armed) | **基线腿(gate 全关)** | A−B 均值 | sd | SE | z | 同号 |
|---|---|---|---|---|---|---|---|
| gpm | 830.35 | **820.71** | **+2.00** | 9.60 | 4.80 | +0.42 | 1/4 |
| xpm | 585.62 | **576.84** | **+2.81** | 9.66 | 4.83 | +0.58 | 2/4 |
| deaths | 2.07 | **2.11** | **−0.01** | 0.09 | 0.05 | −0.22 | 3/4 |
| last_hits | 24.68 | **23.75** | **+0.35** | 0.59 | 0.29 | +1.20 | 3/4 |

逐种子 gpm:888 **+16.01** / 895 **−1.53** / 896 **−0.75** / 906 **−5.74**。
`comps_better`:gpm 1/4、xpm 2/4、deaths 3/4、last_hits 3/4。脚本 `suggested` 字段输出
`hold_or_reject`(**该字段是阈值机械判,不是判定** —— 判定归总监)。

**四个指标全 null。** 按 06:06Z (5)④ 事先登记,**这就是预期结果**:4 种子经济 MDE ≈ 30-35 gpm,
单 id 在经济通道上结构上不可判。**本台不对 `roamreach` 下任何结论。**

### 2.4 ⭐ 一个此前没人报过的数:**单臂 per-seed sd 在单 id 波次上只有 9.6**

GH #30 的经验零点 σ **30.24**(`l1xpsoak` solo,4 种子)、历次多 id 波次单臂 sd **16.6–19.6**、
跨臂配对差 sd **35.62**。本波单 id 单臂 **gpm sd = 9.60 / xpm 9.66**,是有记录以来最小的一档。

**诚实边界(这条不要过度引用)**:n=4,sd 的抽样误差本身很大(χ² 下 4 样本的 sd 95% CI 约
0.57–2.9 倍 ⇒ 真值可能落在 5.5–28),**它与 30.24 并不显著不同**,不足以据此重估任何历史波次的
功效。可以说的只有:**本波的读数没有出现历史 σ 那一档的离散**,若后续单 id 波次复现同一档,
才谈得上"单 id 波次的噪声本底比多 id 波次低"这个假说。**在复现之前,MDE 仍按 30-35 gpm 用。**

### 2.5 交接

AG.4 阶梯**第 (3) 步(`roamstale` 退回 gate 后面)不许跳步**,其前置是"实测确认 `roamreach`
仍消不掉域内成本"。**这个实测归录像组**(域内检测器 ARMED A−B),不是本台的经济读数。
四个 run 前缀(见 §3)已在 S3,`.dem` 可取。已在 **GH #45** 下追评交付。

---

## 3. 启动:全测试集 armed 的默认波次

### 3.1 为什么这轮启动(此前十六轮的"无目的不启动"封印如何解除)

**owner 08-22T07:23Z 指示(commit `823a48e`,直接写进本台章程末节「波次开关策略」)**:

> **默认波次 = 全测试集 armed**(test_set.md 最新 §x.0 的完整串)。批测和录像的第一目的都是看
> "测试版"的合成行为 —— owner 的原始定义就是"测试版 = 稳定版 + 最近的策略改动(全部打开)"。
> **单 id / 少 id 的隔离波次是例外**,只在归因问题明确、且全集波次答不了的时候开。
> 录像组选局核验、以及给 owner 做回放展示,**优先用全集波次的局**。

这条指示**本身就是常设申报目的**:观察测试版的合成行为 + 给录像组和 owner 供全集波次的局。
它由 owner 直接下达,层级高于本台此前自设的"无目的不启动"封印。**这是本轮启动的唯一依据**,
不是例行三条件凑够了就开。

### 3.2 例行三条件(形式上也全部满足)

| 条件 | 结果 |
|---|---|
| (i) 距上一次**例行**波次 ≥ 6h | **满足** —— 上一次例行波次 08-20T16:09:02Z,已 **~40h**;06:10Z 是 AG.4 排定的目的波次,按 02:09Z/14:12Z 先例**不重置例行计时** |
| (ii) 有新东西可测 | **满足** —— 距上一次**全集**波次的树 `11a8de33`,`bots/`/`game/` 有 **6 个 commit** 的变更;且本波 armed 串(18 id)与上一波(1 id)完全不同 |
| (iii) 当月已花 + 本波预估 ≤ $45 | **满足** —— $18.61 + $1.56 ≈ **$20.2** |

### 3.3 armed 串:§U.0 的 18 id,逐字照抄

```
l1trade,l5combo,midtp,suptp,tpcommit,tpdying,lf_rescue,teambrain,ownhalf,overchase,fieldregen,wandbleed,capmono,cmrguard,tpdead,zusult,blinkflee,liondrainstop
```

**这是"test_set.md 最新 §x.0 的完整串"的字面兑现**,不是本台自己选的组合:
- 最新带串的 §x.0 仍是 **§U.0**(18 id),`§X.0`(08-21T01:00Z)明写"不变,仍是 §U.0 的 18 id",
  文件头第 25 行(最近更新)亦写"现在是 §U.0(18 id …;种子仍 888/895/896/906)"。
- 文件第 2 行的 20 id 是 **eligible 集,不是下一波串**(文件自己用 ⚠️ 标着)。两个差额 id
  **不 armed 有明文依据**:`wandlimbo` —— §J.1.4 的机会普查是硬前置,**连续九轮无人做**,
  文件明写"仍**不可 arm**";`odaoe` —— §AB.1 入集时明写"§U.0 的 18 id 下一波串**逐字不变**"。
- **本台没有做任何组合上的自主选择。**

**注**:§U.0 原申报目的(买 `blinkflee`/`liondrainstop` 的 (a))已被 **§V.5 作废**(录像组
pre-flight:`blinkflee` armed 侧期望 ≈0.5 次/波、`liondrainstop` 1Hz 采样下 98% 频道不可分辨)。
**本波不继承那个目的**,本波的目的是 owner 指示的合成行为观察 —— 两个 id 只是随全集 armed,
**读到 0 次仍不构成 SILENT**(§V.5 裁定继续有效,收割时不许改口)。

### 3.4 上机前静态核对

18 个 id 逐个 grep `bots/`:17 个有裸字面量(2-7 处不等)。**`lf_rescue` 命中 0 是已知正确的** ——
它经 `J.IsLaneFixOn('rescue')`(`jmz_func.lua:5876`)展开为
`J.IsSoakCandidate('lf_' .. sub)`(`jmz_func.lua:5570-5573`,同函数另接受 bundle id `lanefix`),
逗号串可表达。

### 3.5 树:钉全 40 位 SHA `12ef3deb476651364a6d0445e7ee043daed2b29f`

- `git ls-remote origin main` **真值** = `12ef3deb…`(不信本地 remote-tracking ref,20:11Z 教训),
  与本地 HEAD 一致。
- 空 clone 实测 `git fetch --depth 1 origin <全40位SHA>` → **`SHA_FETCH_OK`**,不赌。
- **树漂移两步法 + 正控**:`git log 03316d1…..HEAD -- bots/ game/` 空且 **exit 0**;
  **正控**:同区间不限路径有 **14 个 commit**(全在 `iterations/`)⇒ **空 diff 是真的无漂移,
  不是 shallow clone 的 exit-128 假空**(04:06Z 立的那条"exit 128 = 不可比,不是无漂移";
  "空 diff 要配正控才算数")。距上一次全集波次树 `11a8de33`:`bots/`/`game/` **6 个 commit**。
- 副产品:`RUN_ID` 以 SHA 结尾,收割**不依赖人工映射表**。

### 3.6 上机

`CAND` = §3.3 的 18 id 串,种子 **888/895/896/906**(§U.0 指定,亦与刚收割的 `roamreach` 波次
**同种子** ⇒ draft 逐局可与之并排看),4 台 **c6i.4xlarge on-demand**(`InstanceLifecycle=None`
逐台复核,14:12Z「短波次别用 spot」规矩),**一台一种子**,16 槽,**2h 看门狗**,`--games 22`。

| 种子 | 实例 | run_id |
|---|---|---|
| 888 | `i-0d36b27c69c603a82` | `spot_20260822_081028_1_12ef3deb…` |
| 895 | `i-035e873390d0cd7c7` | `spot_20260822_081033_1_12ef3deb…` |
| 896 | `i-03be940b416d916f9` | `spot_20260822_081038_1_12ef3deb…` |
| 906 | `i-06874027bfaed8d48` | `spot_20260822_081043_1_12ef3deb…` |

**run_id 唯一性纪律(22:0xZ 立)本轮第二次有适用对象且通过**:四次 `COUNT=1` 调用之间
`python3 time.sleep(2.4)` 保证跨过整秒(**实测间隔 5s / 5s / 5s**);启动后读独立证据源
`soak-run` 标签,**四值互异(distinct = 4)**,且四台 `InstanceLifecycle` 全 `None`。

预计 **~08:45-09:00Z 落地**,预期 **~280 局有效局**(+24 暖场)。

### 3.7 一个留给总监的配置问题(本台不自行决定)

owner 指示明写"录像组选局核验、给 owner 做回放展示**优先用全集波次的局**",而
`--rec-slots` 仍是默认 **1**:16 槽里只有 1 槽录 `.dem`(`[harness] #75`)⇒ 本波 ~280 局里
**帧级通道只拿到 ~1/16**。提高 `rec-slots` 能直接给 P1/P2 供更多帧证据,但
`spot_run.sh:34-39` 明写它的吞吐代价未测,且要求"两臂 identically"。
**本波保持默认 1**(保守:不在同一波里同时改被测集合和采集配置,否则经济读数与历史不可比),
**是否为后续全集波次提高 rec-slots 归总监裁定**,已随 GH #45 追评上报。

---

## 4. 局数(章程第 7 条固定栏位)

- **上一波次最终有效局数**:`roamreach` 单 id 波次 **282 局**(radiant 169 / dire 113;
  888=71、895=73、896=71、906=67;ab/ba 不对称 = dire 被 35min stall 截断,radiant 打满配额),
  另有 **23 局暖场**(不算有效局)。**4 种子两 wave 全齐,无饿死。**
- **本轮在跑波次实时进度**:刚上机(08:10Z),S3 `soak/<run_id>/` 里 `analysis.json` 计数 **0**。
  预期 **~280 局**(暖场不算有效局)。
- **本轮下载 `analysis.json` 305 个**(收割用),合并后仍 305。
- 档案累计:上一轮全桶递归 LIST 的 30,461 对象 / 14,455 `analysis.json` / 140 run / 84 波次,
  本轮 **+4 run(140 → 144)**、**+1 波次(84 → 85)**、`analysis.json` **+305**;本轮未重跑全桶
  LIST,以上是增量推算,**不是实测**。

---

## 5. 泄漏检查(四层,全免费只读,开工与收尾一致)

| 层 | 开工(08:06Z) | 收尾(08:11Z) |
|---|---|---|
| 实例 | **0** | **恰好 4 台,全是本轮有意启动的自毁 on-demand** |
| AMI 快照 | 1(`ami-0a990a26d89c66547` / `dota2bot-batch-20260718`,唯一常设成本) | 同 |
| EBS 卷 / EIP / open-active spot 请求 | 0 | 0 |

**无泄漏。** 四台均 `--instance-initiated-shutdown-behavior terminate` + 2h 看门狗,
且 `--validate` 跑完即 terminate(不等看门狗)。

---

## 6. 验证(铁律第 6 条)

本会话**未改动任何 Lua**(改动仅 `iterations/` 下的本报告与章程),且本容器**未装
`luacheck`/`lua5.1`** —— 按铁律 6 的立法目的,本轮**无适用对象**,两者均未运行。

本报告第 2-3 节每个数字均由实跑得出:`s3 ls --recursive` / `s3 cp` 305 个对象 /
`recover_verdict.py` / 305 个 `analysis.json` 的 `script_version` 与 `players[]` 逐个解析 /
`git ls-remote` / `git fetch --depth 1 <全40位SHA>` / `git log` / `grep` /
`ec2 describe-instances`。**唯一估算项是费用 $1.56**(引自 08-20T14:07Z 同配置实测);
§4 的档案累计增量已标明是推算。§2.4 的 sd 置信区间是 χ² 解析式,不是实测。

---

## 7. 下次触发

**第一件事 = 收割本波(全集 18 id,树 `12ef3deb…`,种子 888/895/896/906)**,五条必查项:

1. **暖场 stamp 必须是 `12ef3de`** —— 否则裸 SHA checkout 静默失败、跑的是 AMI 陈树,数据作废;
2. **先按 run 分目录下载再带前缀合并**(08:08Z 跨 run 同名撞车曾静默丢 15.9% 的局;本波四台
   run_id 已含 SHA 后缀,但 per-game basename 仍可能同秒撞名);
3. **S3 真实 cand stamp 应恰好等于 §3.3 那 18 id 串,一字不差** —— `wandlimbo`/`odaoe`
   **一个都不该出现**;
4. **这是全集波次,经济读数是 18 个 id 的合成效应,不可归因到任何单个 id**(§A0/§AJ 通例);
   按 §AJ 通例二,**凡打印 A−B 的表每个指标都要打印基线腿**;
5. **`blinkflee` / `liondrainstop` 读到 0 次不构成 SILENT**(§V.5 事前裁定,落地后不许改口);
   **`killed` 指标不可用**(§AJ.3,基线腿 −11.8pp)。

其余固定栏位:核树走章程步骤 5 两步法 + 全 40 位 SHA(**exit 128 = 不可比,不是无漂移;
空 diff 要配正控才算数**);泄漏检查沿用四层版;**CE 必查项 08-23 查 08-22 完整日**;
**跨波并排引用 gpm 前先查 `iterations/data/wave_tree_index.tsv` 的 `drift_vs_head`**
(本波树 `12ef3de` 与上一波 `03316d1` 在 `bots/` 上**逐位相同**,两波可直接并排)。

**必须传下去的三条**:
1. **AG.4 阶梯第 (3) 步不许跳步** —— `roamreach` 的经济 A−B 已 null(结构上必然),
   `roamstale` 是否退回 gate 后面**等录像组的域内检测器读数**,GH #45 已交付语料;
2. **owner 的「默认波次 = 全集」指示是常设的** —— 以后无特殊归因需求时,默认串取
   `test_set.md` 最新 §x.0,**不由本台自选组合**;隔离波次要在申报目的里写清"为什么全集波次不行";
3. **§2.4 的 sd=9.60 未经复现,不得用来重估任何历史波次的功效**;MDE 仍按 30-35 gpm 用。

**跨组**:在既有 **GH #45** 下追评(**不新开**,06:06Z 已定)。
`[batch] #70`、`[harness] #98`/`#75`/`#33`、`[bug] #96` 仍开着,不重复开。

---

## 8. 本次会话 token 用量(铁律第 8 条)

```
model_turns: 68
input_tokens (uncached): 136
cache_read_input_tokens: 6,525,086
cache_creation_input_tokens: 363,270
output_tokens: 61,910
TOKENS total_in=6,888,492 out=61,910 turns=68
```

(统计时刻之后的收尾回合不计入。按 §AJ.8 新口径:主指标 `out=61,910`,次指标 `turns=68`,
`total_in/turns` = 平均上下文 **101k**。本轮 `total_in` 偏高的可查原因:章程
`batch-desk.md` 已达 **1,920 行 / ~105k token**,单次全读会撑爆上下文,故本轮改为
`tail -120` + `grep` 定位读取 —— 即便如此它仍是每轮上下文的大头。**建议总监考虑给
超长 stream 章程定期归档**(把 N 轮之前的「当前状态」条目切到
`iterations/streams/archive/` 并在正文留索引),否则各组的平均上下文会随轮次单调涨。)
