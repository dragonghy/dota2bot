# batch-desk 2026-09-09T06:07–06:4xZ — 收割轮

**一句话**:W59 收割完毕,**只买到 1 粒配对种子 / 4 台**,闸 (iv) 判 **BLINDED** ⇒
**下一波必须 `--on-demand` 发一波再退回 spot**(这是工具打的,不是本台的判断)。
本轮零发波、零新增 EC2 支出,唯一支出是 `$0.01` 的 CE 逐日拆解。
⭐⭐⭐ 头号产出不是那个 verdict,是本轮**第一次用一个独立数据源直接测了 SIR
`Status.UpdateTime` 的方向**:三台被回收的机器,S3 上传在 SIR 更新之后**还继续了
9–83 秒** ⇒ 对被回收的机器,`UpdateTime` 是存活的**下界不是上界**,而上一轮把
「(甲) UpdateTime 滞后 ⇒ 机时被高估」留成了 GH #454 唯一还站着的候选机制。
**符号反了。**

---

## 一、成本

- `AWS_SETUP_EXIT=0`(`arn:aws:iam::581436534924:user/dota2bot-agent`,受限用户,正确)。
- `COSTS_EXIT=0`。MTD **`$69.465`**(免费 `budgets`,`refreshed 2026-09-08T22:46:49Z`),
  ≥$35 ⇒ 自动 `$0.01` CE 复核 **`69.4654098749`**,**两源逐位一致**。
  `forecast $181.976`(月度 limit `$100`)—— **原样登记,本台不据 forecast 决策**。
- ⚠️ **那张 `budgets` 快照早于本轮 7.4 小时**,而章程钉死的 EC2 滞后是 4.3–11.3h
  ⇒ **MTD 系统性偏低**,与上一轮同一形状(上一轮是 4.5h)。
- **在跑实例:0 台**(`describe-instances` running/pending,**账号级不按 tag 过滤**)
  ⇒ W59 四台全部消失。常设成本仍只有 AMI `ami-0a990a26d89c66547`。
- `$80` 未跨,`$90` 刹车未触发,**到刹车余量 `$20.53`**。本轮**不跨任何告警档**(零支出)。
- 外来负载(`c7a`/`r7a`)**09-07 / 09-08 / 09-09 三天仍然一个都没有** ⇒ 累计
  **`$37.4355`** 仍顶在**无 filter 的预算**上但**未再增长**(连续第三轮如此);
  扣掉后本台自己 MTD 约 **`$32.03`**,离 `$90` 还有 `$58`。**本台不擅改任何一条线。**

## 二、W59 收割(`RV_EXIT=0`)

**臂串收割那一刻现算,钉的是发波树 `66a8a15b` 的第 2 行**(GH #631):
**39 id / 354 字节 / md5 `433070c6d8fde3758555302db49047ab`**,与发波记录**逐位一致**。
⭐ **而今天 `origin/main` 的第 2 行是 37 id / 335 字节 / `b525d51d4b4957e0e40f22f203aea641`**
—— W59 起飞后成员串**又退了 2 个**。照章程散文「读 `origin/main`」收割,本轮**又会**拿
一个机器从没跑过的臂串重算;**连续第二轮在同一个坑边上走过**(GH #631 那句未修的散文)。

四前缀**各自子目录不拍平**(GH #225)。`files_seen 80 == games_loaded 80`、
`source_dirs 3`、`unparseable 0`、`pooled_basename_dirs_overridden 0`、
`scored_games 42`、`unfinished 0`、`min_arm_depth 8`、`thin_arm_seeds []`。

**逐粒**(铁律 4 (i-a),`_ab`/`_ba` **全取自工具字段,未手算**):

| 种子 | AZ | ab/ba | arm_depth | gpm | gpm_ab | gpm_ba | 判定 |
|---|---|---|---|---|---|---|---|
| 9531 | 2a | 28/14 | 18.67 | **−7.79** | +30.72 | −46.30 | 计分 |
| 9532 | 2b | 10/0 | 0.00 | — | — | — | **NO-PAIR** |
| 9583 | 2c | 0/0 | 0.00 | — | — | — | **全损**(S3 只有 1 个对象、0 个 `.analysis.json`) |
| 9839 | 2d | 10/0 | 0.00 | — | — | — | **NO-PAIR** |

**跨种子**(先每粒 swap-average 再算术平均,⛔ 未按局数加权):
gpm **−7.79**(0/1)、xpm **+5.29**(1/1)、deaths **−0.10**(1/1)、last_hits **−0.24**(0/1)。
`strata` 四量 `sign_flip` 全 true,gpm `side 38.51` vs `arm −7.79`(`side_gt_arm 1/1`)
—— 按铁律 4 (i-c) **登记,但不是否决理由、不含精度信息**。

⚠️⚠️ **只有 1 粒计分种子。** `mean` 就是那一粒本身,`comps_better` 的分母是 1,
**跨种子离散度信息为零**。按 `reclaim_blind.py` 自己的文件头逐字:
「a one-seed wave is not a wave-level answer either -- it is an orphan with better
paperwork」。`suggested: hold_or_reject` 是**工具的字段**,本台不做判断分析。
被排除的三粒**在此点名**(它们的局数是真花了钱的:9532 十局、9839 十局、9583 零局)。

**局数(章程步骤 7)**:本波实测 `scored_games 42`、`games_loaded 80`、`unfinished 0`。
发波时预登记的 `~96` 按 `expected_games` 键自己的话**不得用作交付不足的分母**。

## 三、闸 (iv):`RECLAIM_BLIND_EXIT=1` —— **BLINDED**

逐字:

```
yield      : 1 paired seed(s) of 4 machine(s)
attribution: 3 machine(s) reclaimed before the flip
VERDICT: BLINDED -- yield <= 1 AND the loss is capacity.
NEXT WAVE: launch --on-demand (ONE wave), then revert to spot.
  GH #158 stands: a spot machine that cannot be KEPT past the flip is a spot machine we did not get.
```

**⇒ W60 发 `--on-demand`,一波,然后退回 spot。市场类型是闸打的,不是本台判的。**
连带:W60 的闸 (iii) 围栏算术要用**按需单波价 `~$2.15`**,不是 spot 的 `~$1.10`。

⚠️ **本轮第一次调用是 `RECLAIM_BLIND_EXIT=2`**,逐字
`UNDECIDABLE: seed 9531: missing leg count 'ab'` —— **归因是本台自己**:
发波轮没有(也不可能)写 `ab`/`ba`/`arm_depth`,而上一轮已把「收割轮把逐粒
`ab`/`ba`/`arm_depth` 回填进波次文件」写成**固定动作**。本轮补齐后
`exit 1`(BLINDED 的正常码)。**登记为那道固定动作本轮是承重的,不是登记为工具坏了。**
⭐ 9583 **零局**,它在 `per_seed` 里根本不出现 —— 它的 `0/0` 是**显式写进**波次文件的,
因为「键缺席」正是 W41 让同一道闸 `exit 2` 的那个形状。

## 四、⭐⭐⭐ GH #454(甲):SIR `UpdateTime` 的方向本轮被一个独立数据源直接测了,**符号反了**

上一轮把 09-07 那个「实付比当日最低报价还低 18%」的三个候选机制收窄到
(甲)「SIR `Status.UpdateTime` 在实例真死后才异步更新 ⇒ 机时被系统性**高估**」独自扛着,
并据此登记「本台价格侧估算系统性偏高 33%,朝安全侧失效」。

**本轮有了一个独立于 SIR 的第二时钟:S3 最后一个对象的 `LastModified`**
(GH #332 立 `survival_bound: "lower"` 时用的正是它;**S3 的上传不过期**):

| 种子 | SIR `Status.UpdateTime` | S3 最后对象 | 差 | SIR 码 |
|---|---|---|---|---|
| 9531 | 04:08:34Z | 04:05:07Z | SIR **晚** 3.45 min | by-user |
| 9532 | 03:47:06Z | 03:48:19Z | SIR **早 73 秒** | no-capacity |
| 9583 | 03:32:13Z | 03:32:22Z | SIR **早 9 秒** | no-capacity |
| 9839 | 03:47:06Z | 03:48:29Z | SIR **早 83 秒** | no-capacity |

⇒ **对三台被回收的机器,机器在 SIR 更新之后还活着并且还在上传**,
`UpdateTime` 是存活的**下界不是上界** —— 与 (甲) 断言的方向**恰好相反**。
机制上说得通(spot 回收是**先发通知再终止**,SIR 状态跟的是通知),
但那正是要点:**(甲) 在自终机器上可能成立,在被回收的机器上按测量不成立**,
而**被回收的机器正是把机时压低、让 `$1.10` 那条判据绕开自己的那一部分**(#454 (乙) 的立案句)。

⚠️ **诚实边界,登记为「方向被测反」不写成「(甲) 被证伪」**:
(i) 本轮只有**一台**自终机(9531),它上面 SIR 晚 3.45 min,**与 (甲) 同向**,一台不成序列;
(ii) 09-07 那一天的机器构成本台**没有逐台读过**,不能把本轮的符号搬过去;
(iii) S3 最后对象只是**上传**的时刻,不是关机的时刻,它同样只是一个界。
⇒ **净效果**:本台的机时数在被回收的机器上**偏低**(每台最多 ~2 min,三台合计
≤0.1 h / 1.71 h ≈ 6%),而 09-07 那个「实付低于最低报价 18%」的谜**没有被解开**,
只是不再由 (甲) 独自扛着。**三个候选机制回到台面上,连数字交总监。**

## 五、GH #454 两条腿(**无论是否触发都要报**)

**(乙2) 不触发。** 「跑完自终」的机器只有 9531 一台,机时 **0.7588 h/台** < `1.00`。
被回收的三台按条款自身文字排除。

**(乙1) 第六轮欠着。** 09-09 的 CE **一行都没有**(本轮 `$0.01` 拉的 09-07→09-09
逐日 × 逐 `USAGE_TYPE`,`2026-09-09` 分组为**空**),W59 尚未落账 ⇒ 账单侧无从判。
**价格侧单波估算 `$0.4658`**(EC2 spot `$0.4572` + `1.7102 h × $0.005` IPv4 `$0.0086`;
逐台 = 9531 `$0.2073` / 9532 `$0.1034` / 9583 `$0.0426` / 9839 `$0.1039`)
—— **这不是 (乙1) 要的账单侧读数**。

⭐⭐ **W59 是 #454 (乙) 那条立案句的一个教科书实例,而且是本台记录里最干净的一个**:
本波**便宜恰恰因为它被毁了** —— `$0.4658` 只有常数 `$1.10` 的 **42.3%**,
而买到的是 **1 粒配对种子**。旧条款「实测单波成本 > $1.10 才复查常数」在这样一波上
**永远不会触发**,尽管这正是市场最紧张、最该复查的时刻。**(乙2) 的机时腿也不触发**
(自终机只剩一台且它跑满了)⇒ **两条腿在一个 3/4 被容量回收的波上同时静默**。
本台不主张改判据,只登记:**#454 换判据换对了方向,但换过之后的两条腿在本波上都没抓到它。**

⭐ **09-08 连续第三次逐位不动**:`SpotUsage:c6i.4xlarge` **`$1.3413479699`**、
`PublicIPv4:InUseAddress` **`$0.029349995`**、当日合计 **`$1.9576207`** ——
00:1xZ / 03:2xZ / 06:2xZ **三次读数逐位相同,跨度 6 小时,且该日已结束 >30 小时**。
09-07 的 `SpotUsage` **`$1.8373143091`** 同样第三次不动。
⇒ 上一轮把「三小时没动」登记为对机制 (乙)(当日没落全)的**削弱**;本轮跨度翻倍、
且**两天都不动**,削弱加强。⚠️ **硬边界逐字不变**:「六小时没动」≠「终态」,
CE 仍可回填,**本台无工具断言终态**;且本推理与章程步骤 2 (乙) 禁用的那一手同族
(拿一个数字的行为替代一个不可见的状态),区别只在用的是**不动**而非**吻合**。
对 09-08 的欠额读数不变:`$1.3413479699 ÷ 10.1499 h = $0.1322/h` = 当日最低报价的 51%;
独立的 IPv4 行反解 `5.87` 地址小时 = 已知机时的 57.8%。

## 六、GH #591:写死的 `40.0 min` 常数,本轮**零信息**(第六轮)

闸头逐字:`changeover 40.0 min (measured bracket 34.80 < x <= 40.63, margin above
the constant 0.63 min)`。本波四台落点:

- 最长**孤儿**(未翻面):9532 **24.03 min** —— 远在下界 `34.80` **之下** ⇒ 不抬下界;
- 最短**配对**存活者:9531 **45.53 min** —— 远在上界 `40.63` **之上** ⇒ 不压上界。

⇒ **括号一寸没动**。上一轮把它收紧到距常数 38 秒(靠 24.9 min 未翻面 + 44.5 min 已翻面),
本轮四个读数**全部落在括号外**。它**不会自己收窄**,再多波也一样。

## 七、⭐⭐ AZ 分散 4/4 的一波,仍然 3/4 被容量回收 —— 而且**两台跨 AZ 同秒**

W59 是自 W55 以来第一个 **4/4 AZ 全部按请求给足、零 re-aim** 的波(发波轮已登记)。
本轮的收割读数是:**那 4/4 什么也没买到**。

- 9583(**2c**)03:32:13Z 被回收 —— 起飞后 **9.1 分钟**;
- 9532(**2b**)与 9839(**2d**)**同一秒 03:47:06Z** 被回收 —— **两个不同的 AZ**。

⇒ 这是「一次 AZ 事件只损失一粒」被证伪的**第三个测量点**,而且是**最强的一个**:
W55(2b+2d 同秒)、W57(2a+2b 同秒)的同秒对**本就跨 AZ**,本轮在**四个 AZ 全部拿满**
的最有利配置下复现了同一形状。**分散度不是种子间独立性的证据** ——
发波轮的 `az_spread` 键在四个数落地之前就写了这句,本轮是它的兑现。
⚠️ **本台不主张移除 `--az` 分散**(它压的是另一个量,且成本为零);
只主张 **GH #403 不能靠「本轮 4/4 自愈」收案**。

## 八、发波决策:**不发波**

`THROTTLE_EXIT=3`,逐字:`anchor wave: W59 (4 machine(s))` /
`last machine up 2026-09-09T03:23:10Z <- the anchor (GH #469 ruling 1)` /
`slate spread 8s` / `unlock 2026-09-09T09:23:10Z` / `now 2026-09-09T06:18:05Z` /
`margin 11105s SHORT of unlock` / `A launch at this instant BREACHES gate (i) by 185m05s.`
**读的是工具打的那一行,不是章程里抄来的时刻。**

**步骤 4a 复核**:`queue.json` 99 行,非终态行的裁定普查 =
`ROUTED_ARCHIVE_SCAN` 16 / `APPROVED-SCAN` 20 / `RECEIVED_NOT_SCHEDULED` 2 /
`DEFERRED_PENDING_W3_RESOLVE` 2 / `REJECTED` 2 / `ROUTED_RIDESHARE` 2 /
`APPROVED_CONDITIONAL` 1(`strategy-5b`,条件仍未满足、未排期)/
`FROZEN-HOLD` 1 / 其余各 1 —— **没有任何一行要求新波次、专机或新机时**。
`hero-51/52/53` 三行**无 `director` 字段**⇒ 按章程 4a「总监还没裁」,不排波;
其中 `hero-51` 的 `result` 已写 `APPROVED-SCAN`(P4.2 冻结期内登记解冻后的 arm 顺序),
**字段与散文不一致,交总监**。
**P4.1 不欠本台**:upstream 标尺波 Y1 已于 09-05T15:16Z 发出、18:0xZ 收割。

## 九、GH #352 胜负通道

工具在 stderr 自拒,**连续第十波 `DEGENERATE`**,逐字
`minority side won 0 of 80 finished games (share 0.0000 < 0.20)`,
`winrate_side_census {radiant: 0, dire: 80}`,`winner_by {engine_natural: 80}`。
⭐ 少数侧占比序列现在是 **`0.0645 → 0.0245 → 0.0000 → 0.1552 → 0.0000`** ——
上一轮登记的那个「四波以来第一个非零读数、不支持单调恶化」**本轮没有延续**,
回到 0。**两轮都不足以支持任何趋势读法**,本台只登记序列。
`winrate 0.5 ± headroom 0.0` **不可引作铁律 2 条件 (b) 的支撑**(照 §CT 写成占位符)。

## 十、泄漏

`LEAK_EXIT=0`,全账号(**不按 tag 过滤**)`running,pending` = **0 台**;
常设成本仍只有 AMI `ami-0a990a26d89c66547`。**无孤儿、无外来负载。**
⛔ GH #515/#528 立案理由一字未变:预算的 `budget filters : none`。
(本轮是收割轮、零发波 ⇒ 不适用发波轮那条「波次点名」的第二条命令。)

## 十一、自检(铁律 10)

⭐ **第一条命令走了管道,工具自拒**,逐字
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.` /
`it has recurred 5x, every time as the first command of the round` ⇒
**登记为那道自拒门起作用的一次**(连续第二轮它自己挡住了本台)。
改重定向重跑,**但带了 `timeout 400`** ⇒ `EXIT=124`,**是本台自己砍的**(GH #514 的形状,
上一轮已自陈过一次)。第三次:**后台 + 重定向 + 不设 `timeout`**。

**读得到的腿照登、读不到的不声称**(收尾时它**仍在跑**,卡在 python trunk health 腿上):
- 锚点 **6/6 OK**(`stable-v1..v6`,`EXISTS`/`PINNED`/`SHIPPED` 全 ok);
- **183 distinct live gate id** / **18** 个 `PROMOTED` 注 / **`FROZEN none`**
  (没有任何活 gate 指名一个已 promote 的 id ⇒ `pullcad` 陷阱本轮干净);
- promote 原子 **5/5 全 GATED**(`field_hold_needs_magnitude` / `tp_response_releases_need_commit`
  / `static_field_needs_binding` / `tower_fear_ring_disjunction` / `impale_kill_needs_damage`)。
- ⛔ **trunk health(python)、Lua 检测器腿、cadence、`unlanded_commits`、`citation_audit`
  本轮读不到 —— 那几侧本轮没人看过,`SKIP`/未跑完都不是通过。**
  ⚠️ 上一轮那三条红(`test_bots_walk_farm_only.py` / `test_carrier_terms.py` /
  `test_coarmed_attribution_register.lua`)**本轮无从复核**;⭐ 但**其中两条的触发条件
  本轮又被满足了一次**:那两条断言写的是「这个集合永远不许小于 40」,而今天
  `origin/main` 的臂串是 **37 id** —— 比上一轮打红它们时的 39 还小 2。
- 用 **`pgrep -af`(带 `-a`)** 看清匹配到谁,不只数个数。
- 全程 `soak_side.lua` 出现 0 次 ⇒ 反证本轮未建门文件。

## 十二、交棒

1. ⭐⭐⭐ **本台下一轮(= 发波轮,解锁 `2026-09-09T09:23:10Z`,⛔ 不要抄,跑 `wave_throttle.py`)——
   W60 必须 `spot_run.sh --on-demand`,一波,然后退回 spot。** 闸 (iv) 判 W59 **BLINDED**
   (`RECLAIM_BLIND_EXIT=1`,逐字 `NEXT WAVE: launch --on-demand (ONE wave), then revert to spot.`)。
   连带:围栏 (iii) 用**按需价 `~$2.15`**,不是 `$1.10`;按需机**没有 SIR**,
   波次文件那四行必须写 `"market": "on-demand"` + `"status_code": "<EC2 StateReason.Code>"`
   (GH #408 第二半;写错方向会把一个 BLINDED 波藏掉)。
2. ⭐⭐⭐ **总监 —— GH #454 (甲) 的方向被独立时钟测反了**(见四)。
   三台被回收机的 S3 上传在 SIR `UpdateTime` **之后**还继续 9–83 秒 ⇒ 对被回收的机器
   `UpdateTime` 是**下界**。上一轮让 (甲) 独自扛 09-07 那个谜,本轮**它扛不住了**,
   三个候选机制回到台面。连带:本台「价格侧估算高估 33%」那句**同样失去了它的解释**,
   仍按 (丙) 先例登记。
3. ⭐⭐⭐ **总监 —— #454 的两条新腿在一个 3/4 被容量回收的波上同时静默**(见五)。
   W59 `$0.4658` = 常数的 42.3%,买到 1 粒种子。判据从钱换到机时是对的方向,
   但 (乙2) 只看自终机 ⇒ 一波只剩一台自终机时它也测不到市场紧张。**请裁是否要第三条腿**
   (建议的可验收形状:**一波里 `no-capacity` 回收台数 ≥ 半数** ⇒ 复查常数;
   W17 / W17-R / W19 / W19-R / W59 会触发,W18 / W20 / W21 / W24 / W25 / W28 不会)。
4. ⭐⭐ **总监 —— 家族深度被退集节奏封死,第五个测量点**(不重复开 issue,只补点):
   W59 起飞时 39 id,**今天 `origin/main` 已是 37 id / `b525d51d`** ⇒
   `(39-id × 66a8a15b)` 家族终值 **1 波 / 1 粒配对种子**,是记录上最浅的一个。
5. ⭐⭐ **总监/harness(#403)—— 4/4 AZ 分散的一波仍然 3/4 被回收,两台跨 AZ 同秒**(见七)
   ⇒ **不要据「本轮 4/4 自愈」收案**。
6. ⭐⭐ **总监/harness(#591)—— 写死的 `40.0 min` 第六轮零信息**(见六),四个读数全在括号外。
7. ⭐ **总监 —— `hero-51` 的 `director` 字段缺失但 `result` 已写 `APPROVED-SCAN`**
   (`hero-52`/`hero-53` 同样无 `director` 字段),字段与散文不一致,按 4a 本台按「未裁」处理。
8. ⭐ **总监/录像组(#352)—— 第十波 `DEGENERATE`**,少数侧占比回到 `0.0000`,
   上一轮那个 `0.1552` 未延续。
9. ⭐ **owner —— `$50` 仍越线**(`$69.465`,AWS state `ALARM`),其中 55%(`$37.4355`)
   非本项目且**连续第三轮未再增长**;预算过滤器仍 `null`,**本台不擅改**。
10. ⭐ **本台 —— `rec_slot_baseline` 并入仍欠着**(零成本,连掉多轮)。

## 十三、铁律 6/11

- `bots/` / `game/` / `tests/` / `tools/` **本会话一行未改**(只写 `iterations/` 下的
  报告、`W59_wave.json`、本章程节)。
- 静态门 **`GATE_EXIT=0 CLEAN`**(逐字 `luacheck bots game: 0 warnings` /
  `GATE_EXIT=0  CLEAN (iron rule 6 static half passed)`),**未用 `RULE6_BYPASS`
  ⇒ 无「SKIPPED, not passed」行**;推送门 `ARM_GATE_EXIT=0`(`core.hooksPath` 已上膛)。
- 动态半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)**未跑,不声称**。
- **铁律 11**:全程**无** `requires approval`、**零空转**(自检在后台跑,本台没有坐等;
  等解锁不适用 —— 本轮是收割轮,闸 (i) 已 `exit 3` 定案)。
- 退出码**一律裸读不过管道**:`RV_EXIT` / `RECLAIM_BLIND_EXIT` / `THROTTLE_EXIT` /
  `LEAK_EXIT` / `COSTS_EXIT` / `CE_EXIT` / `AWS_SETUP_EXIT` 全部如此。

## 十四、token 用量

```
TOKENS total_in=4,973,895 out=42,914 turns=39
```

(这是「到统计时刻为止」的数字,统计后的收尾回合不计入。)

## 收尾补记

- **自检收尾时仍在跑**(卡在 python trunk health 腿),`pgrep -af` 确认进程还在。
  §十一 里登记的锚点 / gate id / promote 原子三条是**实际读到的**;
  trunk health、Lua 检测器腿、cadence、`unlanded_commits`、`citation_audit`
  **本轮没有读数,不声称**。⚠️ 若它在本会话结束后打出 `SELFCHECK_EXIT`,
  那个码**本报告没有引用**——下一轮不要把它当作本轮读过。
- **收尾泄漏复检**:`LEAK_EXIT=0`,账号级 `running,pending` **0 台**(见 §十)。
  本轮零发波 ⇒ 无 `soak-run` 点名义务。
