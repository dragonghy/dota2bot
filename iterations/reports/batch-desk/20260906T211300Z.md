# 批测台 2026-09-06T21:13–21:4xZ — 收割轮(W51)

**收割轮 —— 上一轮发出的 `campgrade` 独占入集波 W51,本轮全量收割完毕。**
**头号产出有两件:①** W51 的 2(b) 读数干净落地(4/4 粒、214 局计分、零未完成、零抢占),
**而 2(a) 一个字都没买到 —— 那是录像组的活,本轮把语料交出去**;
**②** GH #454 的 `$1.10` 常数两条腿本轮结清,**都没触发,常数站住**;
但**它站住的方式和它被设计来防的方式不是同一件事** —— 见第四节。

**闸 (i) `THROTTLED (exit 3)`,`margin 10933s SHORT`,解锁 `2026-09-07T00:19:42Z`
⇒ 本轮按构造不发波,不存在预算问题。**

---

## 一、开工自检(铁律 10)

⚠️ **第一条命令又踩了同一个坑,这是连续第九轮**:`routine_selfcheck.sh` 走管道
(`| tail -40`)⇒ 工具**自己拒答**并打 `REFUSED: stdout is a pipe; exit 2, nothing checked`,
**而我的 harness 报的 `SELFCHECK_EXIT=0` 是 `tail` 的退出码,不是它的**。
⇒ 立刻改 `> 文件 2>&1` 重跑。**登记:`exit 2` = 没跑成,永远不是通过**(证据纪律 3)。

重跑(后台、重定向、不设短 timeout)跑完:**`SELFCHECK_EXIT=3`**,`legs run 10`。
⭐ **归因照抄工具自己打的那一行,不手编**(GH #267:手编归因曾错了 22 小时):

```
FINDINGS (exit 3)     : cadence queue-rulings owed-executions trunk-red(lua)
UNCERTIFIABLE (exit 2): trunk-red(python)
NOT RUN (inside a leg): tests/test_rc_wrapper.py tests/test_selfcheck_lua_leg.py
selfcheck worst exit: 3
```

逐腿:

| 腿 | 读数 |
|---|---|
| 推送门上膛 | `core.hooksPath=.githooks` **已上膛** |
| 未落地工作 | **OK**,certifiable 窗口内无未落地提交(25 条可核验 / 843 条 uncertifiable,shallow clone) |
| 报告节奏 | **2 findings** —— 协同组两个 3.6h 洞(`09-05T22:37→09-06T02:11Z`、`09-06T07:30→11:05Z`) |
| 稳定版锚点 | **3/3 OK**(`stable-v1`/`v2`/**`v3` `slotwait` 09-06T13:2xZ 新增**) |
| promote 约束 | **OK**,161 个活 gate、14 个 PROMOTED 注记,`FROZEN none`,3 个共促原子全绿 |
| trunk python | **UNCERTIFIABLE**(106 passed / 0 failed / **2 未跑成**:`test_rc_wrapper.py`、`test_selfcheck_lua_leg.py`,容器无 `lua5.1` + 120s 预算超时)⇒ **这一侧本轮没人看过,不是绿的** |
| trunk Lua 检测器 | ⛔ **RED `test_gated_helper_nesting_census.lua`**(见下) |

### 1.1 ⛔ 自检抓到一条**不属于本台**的红:新的 gate-inside-a-gate

```
c12,retnear,towerreach | GetDesireHelper | J.ShouldStayAndRegen |
    stayattr,staybag,staybottle,staysrc,staytower,stayurn | bots/mode_retreat_generic.lua
stayattr,staybag,staybottle,staysrc,staytower,stayurn | J.ShouldStayAndRegen |
    J.HasFieldRegenSource | bagsalve | bots/FunLib/jmz_func.lua
```

时间上对得起来的来源是 **`6c089cf` 协同组 19:46Z**(`stayurn`,"an urn heal arrives from
someone else's inventory")。检测器自己把该问的问题写在断言里了:**未 arm 时,内层
helper 是不是它刚加入的那个合取的单位元**?不是的话,**单 arm 外层 id 测到的是 no-op,
而 `check_armed_wiring.py` 仍会说它 WIRED** —— 这正是本台章程里"静默失效"那一族。

⚠️ **工具自己的边界照抄**:`TRUNK RED -- 1 of 84 Lua detector file(s) failing ON THE
WORKING TREE. Whether main is red too is NOT established by this line: re-run after
`git stash`.` —— 本台**没有跑那次 stash 复核**,所以**不主张 main 是红的**。
本台能诚实说的只有:**本会话 `bots/` 一行未改**,所以这条红**不是本会话引入的**。

⇒ **交协同组 + 总监**(本台不改 `bots/`,也不替它判)。**本台的利害在于**:
`stayattr` 一族若真是 no-op,那么它进任何一波成员串都是**买不到读数的局**,
而波是本台花钱发的。

---

## 二、AWS 引导与成本(章程第 1–2 步)

- `session_setup.sh` **`AWS_SETUP_EXIT=0`**,身份 `arn:aws:iam::581436534924:user/dota2bot-agent`(受限用户,正确)。
- `check_costs.sh` **`COST_EXIT=0`**:
  - **MTD `$50.804`**,来源 `budgets/dota2bot-batch`(免费),`budget refreshed 2026-09-06T15:19:34Z`;
    读数 ≥ `$35` ⇒ 脚本按章程自动花 `$0.01` 用 CE 复核:**`50.8036495163`**,两者一致。
  - `forecast **$185.828**` vs `budget limit $100.0`。
  - **`running/pending` 全账号为空** ⇒ 见第六节。
- **刹车线 $90 未触发**;**围栏 $80 未触发**;**owner 批准线 $100 未触发**。
- ⚠️ **`$50` 那条 owner 告警仍在越线状态**(`AWS state=ALARM`,上一轮已交棒 ⑥)。
  本轮**新增一条对它有利的事实**:**外来负载已经停了**(见第六节),
  所以那条 forecast `$185.828` 是**按外来负载还在跑的速率外推的,不再成立**。
  ⛔ **但已花的不退**,且本台**无法从这个账号区分**「$50.804 里哪些是谁的」——
  上一轮量到的诚实下界是 **≈61% 不是批测的**。

---

## 三、⭐⭐⭐ W51 收割(本轮主体)

**`RV_EXIT=0`,无任何拒答。** 语料 `s3://dota2bot-batch-results-4924/soak/<run>/`
(**`soak/` 前缀不是 `validation/`**),四个 run **各自一个子目录**下载
(GH #225 不压平:`3af107`/`3c2be6`/`fff95f`/`73aa9d` = **64/48/64/62** 个文件)。

`input`: `files_seen 238 / games_loaded 238 / source_dirs 4 / unparseable 0 /
pooled_basename_dirs_overridden 0` ⇒ **同秒 basename 撞车一局没丢**(W14 那次丢了 15.9%)。

### 3.1 逐机(`machines[]` 已全部填齐,**零 null**)

| 种子 | run | AZ | ab | ba | arm_depth | 计分局 | status_code |
|---|---|---|---|---|---|---|---|
| 6421 | `3af107` | 2a | 42 | 16 | 23.17 | 58 | `instance-terminated-by-user` |
| 6487 | `3c2be6` | 2b | 30 | 12 | 17.14 | 42 | `instance-terminated-by-user` |
| 6553 | `fff95f` | 2c | 40 | 18 | 24.83 | 58 | `instance-terminated-by-user` |
| 6619 | `73aa9d` | 2d | 39 | 17 | 23.68 | 56 | `instance-terminated-by-user` |

**四台全是 `instance-terminated-by-user`**(消息 `Spot Instance terminated due to
user-initiated termination.`)⇒ **完工自毁,零抢占** —— 抢占会读
`Server.SpotInstanceTermination` / `instance-terminated-by-price` /
`instance-terminated-no-capacity`。**四粒完整,没有缺臂,没有一粒作废。**

⭐ **SIR 窗口本轮抢在关闭前读下了**(GH #375:终止后仅存 ~1h;四台 `19:03–19:14Z` 终止,
本台 `21:1xZ` 读到,**是这一轮唯一一件有时限的事,所以排在第一位做**)。
**这正是上一轮新开的 GH #556 要制度化的形状** —— 本轮它是靠「本台记得先做」成立的,
不是靠机制。

- `scored_games` **214** / `unfinished` **0** / `seeds_scored` **4/4**
- `thin_arm_seeds` **[]**、`thin_arm_winrate_seeds` **[]**、`min_arm_depth` **8**

### 3.2 读数(铁律 4(i-a):**两个分层的读数**都登记,不是局数)

| 量 | mean | comps_better | ab | ba | side | sign_flip | side>arm |
|---|---|---|---|---|---|---|---|
| gpm | **−2.19** | 3/4 | +87.22 | −91.60 | 89.41 | 是 | 4/4 |
| xpm | **−3.00** | 2/4 | +53.49 | −59.50 | 56.50 | 是 | 3/4 |
| deaths | **+0.07**(略差) | 1/4 | −0.50 | +0.64 | −0.57 | 是 | 4/4 |
| last_hits | **−0.48** | 3/4 | +13.80 | −14.75 | 14.28 | 是 | 4/4 |

四量**全部反号**。⭐ **按铁律 4(i-c) 这不是否决理由**:这四个是**侧偏已消除**的估计量
(每粒种子 50/50 swap-average),反号 ⟺ `|side| > |arm|`,是**恒等式不是诊断** ——
它只说这一波抽到的阵容侧偏比效应大,**对 arm 测得多准零信息**。
登记是因为 4(i-a) 要求登记,**不是**按 4(i-b) 读成噪声(那一条管的是侧偏**未**消除的
检测器计数,不管这四个)。四量均由工具自己的 `per_seed[].<m>_ab/_ba` + `strata.<m>` 给出,
**未手算,更未按局数加权**(4(i-d))。

### 3.3 ⛔ 胜率通道 `DEGENERATE` —— **工具自己拒的,不许引用**

```
GH #352 winrate channel DEGENERATE: minority side won 13 of 238 finished games
(share 0.0546 < 0.20). The winrate below is bounded to 0.5 +/- 0.0882 and MUST NOT
be cited as rule 2(b) support until the channel recovers.
```

四粒里**三粒的 `winrate_headroom` 是 0.0**(侧别普查 radiant 0 / dire 58、0/42、0/58),
只有 6619 有(12/44,headroom 0.3529)。⇒ **`winrate 0.507` 与 `comps_better 1/4`
本报告不作为读数使用**,也**不许**被下游当 2(b) 支持引用。

### 3.4 这份读数**是什么**、**不是什么**

**是**:规则 2(b)「对胜负没有明显负面影响」的**粗粒度**读数。gpm −2.19、xpm −3.00、
deaths +0.07、last_hits −0.48 —— 四个都**远在镜像波噪声底之内**(随机 draft SD ≈ 600 gpm/局),
胜率通道又不可用。⇒ **2(b) 没有明显负面。**

⛔ **不是入集**:本波存在的**唯一理由**是买 2(a) —— GH #241 那个 `.type` 前提的**否定式读数**,
执行人是**录像组**不是本台。**四个经济数字对它一个字都没说。**
⛔ **也不与任何波并池**:1-id 独占串(9 字节,md5 `185481d3…`),
与任何成员串波(那些腿上还挂着 55–62 个 id)**不可比**。

---

## 四、⭐⭐ GH #454 `$1.10` 常数两条腿 —— 本轮结清,**都没触发,常数站住**

| 腿 | 判据 | 实测 | 结论 |
|---|---|---|---|
| 乙2 | 自毁机的**平均机时** > 1.00 h/台 | **0.8610 h/台**(3.4442 / 4) | **未触发** |
| 乙1 | **单波账单成本** > $1.10 | **$0.9774** | **未触发** |

- 乙2 的排除条款(抢占机不计入均值)**本波是空的**:四台全是自毁,没有一台被抢占。
- 乙1 推导(逐台按**它自己那个 AZ** 在 `18:19–19:14Z` 的现货报价):
  `2a 0.8997h × $0.2914 = $0.2622`;`2b 0.7317h × $0.2732 = $0.1999`;
  `2c 0.9114h × $0.2972 = $0.2709`;`2d 0.9014h × $0.2521 = $0.2272`
  ⇒ EC2 **$0.9602**,加 `4 × 0.861h × $0.005` 公网 IPv4 **$0.0172** ⇒ **$0.9774**。

### 4.1 ⚠️ **它站住的方式,和它被设计来防的方式不是同一件事**(交总监)

常数是**在机时上守住的,不是在价格上**:

- **机时很正常** —— 0.861 h/台,落在常数拟合所用的 0.745–0.9 里,乙2 离触发还很远;
- **价格不正常** —— 本波现货报价 **$0.2521–$0.2972**,而此前**每一个**结算点都是在
  **$0.231–$0.250** 这个带上拟合的,**高 9–19%**;
- **$0.9774 落在 W46 那个区间 `$0.959–1.022` 之内,并高于其余每一个结算点**(其余全部 ≤ `$0.892`)。⚠️ **订正本节初稿**:初稿写的是「迄今最高单波结算值」,**那是错的** —— W46 上沿 `$1.022` 比它高。正确说法是**它与 W46 同处最高档,把该档从一个孤点变成两个点**;订正**不改变**本节结论,反而让它更强。

⇒ **乙1 只剩约 13% 的价格余量**。而**两条腿都不直接点名价格**:乙2 只看时长,
乙1 只看乘积。**下一次把 `$1.10` 顶掉的,大概率会以价格移动的形式到达,
而现有判据只能等它先穿过一个乘积才看得见。** 本台**不自裁**,交总监。

---

## 五、收割义务三条,逐条结清(章程:收割当轮当场结)

| id | 状态 |
|---|---|
| `GH_454_constant_legs` | ✅ **本轮结清**,见第四节 |
| `wave_fence_ruling4_first_live_run` | ✅ 由 **18:13Z 发波轮**结清(`gates.gate_iii_fence` 逐字载着活路径读数)⇒ **请总监退休该 registry 行** |
| `campgrade_wave_type_premise_reading` | ⏳ **OPEN,且不归本台关** —— 本台那一半(排这波)发波当轮已履行;**读数属录像组**。本轮新增的是:**语料现在存在了**(见下) |

**⇒ 语料已落地:119 个 `.dem` 在 S3 四个 run 前缀下(32/24/32/31)。**
那一行 registry 此前的 trigger 原文是「`campgrade` 的独占入集波——它此刻**还不存在**」;
**它现在存在了,且录像组要的帧就在里面。这条不能再以"没有波"为由挂着。**

---

## 六、泄漏检查(章程第 8 步)

`LEAK_EXIT=0`,`running,pending` **全账号为空**:

1. **本台侧干净** —— W51 四台全部自毁(SIR 四条 `instance-terminated-by-user`,
   终止时刻 `19:03–19:14Z`),**没有第五台,没有一台留着**;常驻仍只有 AMI
   `ami-0a990a26d89c66547`。
2. ⭐ **外来负载停了** —— 前两轮点名的 `final-table-trainer`
   (`i-0ce7ddf08ea24cc7f` `c7a.16xlarge` → `i-0a7d4c7fbeb3a6712` `r7a.16xlarge`)
   **本轮 region 内不在跑**。⇒ 上一轮那句「headroom 约一天内被吃光」**暂停计时**,
   **但 GH #515 / #528 的立案理由不变**:预算 `dota2bot-batch` 的 `CostFilters=null`,
   **本台的发波围栏仍读着一个它不控制的账号级数字**,那台机器随时可以再起来。

---

## 七、局数(章程第 7 步)

**收割型 ⇒ 报实得**:`scored 214 / unfinished 0`,四粒 `58/42/58/56`,
`arm_depth 17.14–24.83`,`min_arm_depth 8`,`thin_arm_seeds` 空。
预登记 `expected_games` 是 **~96**,实得 **214**;⚠️ 按上一轮已交总监的那条,
**`expected_games` 这个预登记量偏低约 75%,不许拿它当"超交付"或"少交付"的分母** ——
两个方向都不许。

---

## 八、铁律 6 / 11

- **铁律 6**:`bots/` / `game/` / `tests/` **本会话一行未改**(只动 `iterations/` 下的
  报告、波次记录、章程"当前状态"节)。静态门读数见收尾节。
  动态半(#124,~100min)**未跑,也不声称**。
- **铁律 11**:**未触发任何 `requires approval`**,**无空转等待**。
  本轮唯一的"等"是闸 (i) 自己 `THROTTLED` —— 那是**门在拒绝**,不是空转;
  且本轮按构造就是收割轮,不打算发波。

---

## 九、交棒

| # | 收件人 | 内容 |
|---|---|---|
| ① ⭐⭐⭐ | **录像组** | **欠了七轮的 `campgrade` 条件 (a) 读数,语料现在真的在手上了**:W51 的 **119 个 `.dem`**,四个 run 前缀。尺子**已存在不要另发明**(`ancient_camp_domain.py` + `campgrade_ladder.py`);判读:**降 ⇒ `.type` 是字符串,前提结案;不降 ⇒ UNCERTAIN 不是通过**;反向护栏:远古 engagement **总数不许塌到 0**;铁律 4(i-a) 两层各登记**读数**;⛔ 域为零 ⇒ `DOMAIN-NOT-REACHED`,**不许读成"测过了,`.type` 不是字符串"**;归类取不到 ⇒ **回总监重裁,执行者不得自行套用后果条款**。全文在 `iterations/reports/batch-desk/waves/W51_wave.json:harvest_obligations[0]`。 |
| ② ⭐⭐ | **总监** | **GH #454 的两条腿本轮结清(都未触发,`$1.10` 站住)**,但**它是在机时上守住的,价格已经跑出拟合带 9–19%,乙1 只剩 ~13% 余量**(第四节)。**两条腿都不直接看价格。** |
| ③ ⭐⭐ | **协同组 + 总监** | ⛔ **自检 RED:新的 gate-inside-a-gate**(`stayattr,staybag,staybottle,staysrc,staytower,stayurn` → `J.ShouldStayAndRegen` → `J.HasFieldRegenSource`/`bagsalve`),时间上对应 `6c089cf`。**未 arm 时内层是不是单位元?不是的话单 arm 外层测到的是 no-op,而 `check_armed_wiring.py` 仍说 WIRED。** 本台的利害:no-op 的 id 进成员串 = **本台花钱发的局买不到读数**。 |
| ④ ⭐⭐ | **总监** | 请**退休** `wave_fence_ruling4_first_live_run` 行(18:13Z 已结清)。 |
| ⑤ ⭐ | **总监** | **GH #556**(闸 (iv) 依赖会过期的 SIR 窗口)本轮**再次靠"本台记得先做"成立** —— 21:1xZ 读到的四条 SIR 距终止仅 ~2h,**再晚一轮就没了**。这不是机制。 |
| ⑥ ⭐ | **owner** | **`$50` 告警仍在越线**(MTD `$50.804`,CE 复核一致)。**好消息**:外来 16xlarge 负载**已停**,`forecast $185.828` 是按它还在跑外推的,**不再成立**;**坏消息**:已花的不退,且预算 `CostFilters=null` 意味着**下次它再起来,关上的是批测的闸**(GH #515/#528)。 |

---

## 十、下一轮本台

1. **闸 (i) 解锁 `2026-09-07T00:19:42Z`**(锚点 W51 末台 `18:19:42Z`)——
   ⛔ **不要读这个数,读 `wave_throttle.py` 打的那一行**,并且**把钟读在动作上**
   (循环前最后一条 + 发波脚本内裸 `$?`)。
2. 解锁后**若发波**:按 §波次开关策略默认 = **全成员串**(现 55 id),
   spot 优先(不传 `--on-demand`),`--ref` 钉 40 位 SHA 且 `git ls-remote origin main` 读两次;
   **闸 (iii) 必须活跑** —— MTD 已在 `$50` 之上,`$80` 围栏是当前操作天花板。
3. **把闸 (iv) 排进发波脚本**,与 (i)–(iii) 并列(上一轮 ④,本轮未做,继续欠着)。
4. ⚠️ **开工第一条命令重定向、不管道、后台给足** —— 本轮第九次踩同一个坑。

---

---

## 十二、本轮开出/追评的 issue(铁律 #290 顺序:**先 push 再发表**)

三份草稿**都在 `eb4e8b28` 推上 `main` 之后**才发表,发表前各跑一次 `claim_precheck.sh`:

| 目标 | 内容 | precheck |
|---|---|---|
| **GH #576 新开 `[harness]`** | `stayattr` 一族 6 个 id 嵌进 `J.ShouldStayAndRegen`(自检 RED,§1.1);#542 / #484 那一族的第三形态 | `PRECHECK_NEST_EXIT=0`,`paths cited 6 / resolved on trunk 6 / refused 0`,`clean` |
| **GH #454 追评** | `$1.10` 两条腿结清 + 价格才是下一个失效方向(§4);**不新开** —— #454 已 open 且正是这条 | `PRECHECK_454B_EXIT=0`,`paths cited 2 / resolved 2 / refused 0`,`clean` |
| **GH #480 追评** | §5 预登记的买法:语料(119 `.dem`)现在在手上,判读规则原样转抄,交录像组 | 同上草稿族,发表前 `local commits not on origin/main: 0` |

### 12.1 ⚠️ 本轮订正了本台自己的一句话

§4 初稿写 **「`$0.9774` 是迄今最高单波结算值」——那是错的**:W46 的上沿 `$1.022` 比它高。
正确说法是**它落在 W46 区间 `$0.959–1.022` 之内,并高于其余每一个结算点**(其余 ≤ `$0.892`)。
⭐ **订正不削弱结论,反而加强它**:高档从「一个孤点」变成「两个点」,
即 `$0.95+` 的结算不再能被当成一次异常打发。
三处写过这句话的地方(报告 / 章程当前状态 / `W51_wave.json`)**已在 `eb4e8b28` 一并订正**,
且**订正先于三条 issue 发表** —— 否则 #454 那条追评会带着错的最高值出去。

---

## 十一、token 用量

`TOKENS total_in=4,333,251 out=36,906 turns=38`(到统计时刻为止;统计后的收尾回合不计入)。
