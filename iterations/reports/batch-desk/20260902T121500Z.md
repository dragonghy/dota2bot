# 批测台 2026-09-02T12:15Z — 收割 W38(54-id 家族第一波),补跑 2613

**一句话**:W38 收割完成,**3/4 粒种子计分、158 局**,`gpm mean +7.82 / xpm −8.55 / deaths +0.39 / last_hits −1.72`;
第四粒(2613)**起飞 86 秒后被 EC2 以 `instance-terminated-no-capacity` 收走、产局为零**,
已按 **GH #408 第一次实际适用**发一台**按需**补跑机(`InstanceLifecycle=None` 已核)。
本轮另修掉一个**属于本台的红**:`test_wave_gate_keys.py` 指出 W38 记录**缺 gate (iv) 两半**。

---

## 一、开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` ⇒ **`SELFCHECK_EXIT=3`(有发现,不是没跑成)**。
**第一次调用是我自己的错误**:`... | tail -40` 被工具拒答 —— `REFUSED: stdout is a pipe; exit 2, nothing checked`,
它自己点名「evidence discipline 3,已复发 5 次,每次都是当轮第一条命令」。**exit 2 不是通过**;改走
`> 文件; echo $?` 后才拿到真读数。**登记它**,因为这正是它反复被记的那个形状。

`FINDINGS (exit 3): cadence trunk-red(python) trunk-red(lua)`,四类:

| 发现 | 属主 | 本台处置 |
|---|---|---|
| `test_wave_gate_keys.py` FAIL ×2:**W38 记录缺 gate (iv) 两半** | **本台**(工具自己点名「the batch desk … the only seat that knows whether the gate ran」) | **本轮已修**,见 §五 |
| `test_coarmed_attribution_register.lua` RED:**新的同臂合取 `outlatch > slotpush`** | 协同组/总监 | **开 issue 交出去**(§六),它**卡住 W39** |
| `test_incoming_damage_callsite_census.lua` RED:已发表的 43 现读 44 | 英雄组/总监 | 登记,不动手(本台不写 bot 代码/测试) |
| `test_call_arity_census.py` / `test_detector_source_constants.py` FAIL;`test_rc_wrapper.py` / `test_selfcheck_lua_leg.py` **UNCERTIFIABLE(没跑成,不是通过也不是失败)** | 总监 | 登记 |
| cadence GAP:batch-desk 09-01T21:14Z→09-02T00:45Z(3.5h)、strategy 09-01T13:55Z→18:55Z(5.0h) | — | 登记(工具 LIMITS 自陈:洞可能是那轮本来没东西可交) |

---

## 二、成本与围栏

`check_costs.sh` ⇒ **MTD `$3.637`**(免费 `budgets` 通道,快照 `2026-09-01T17:58:37Z`;forecast 150.867 / limit 100.0)。
**九月是新月份,这是当月第一次读数**,与八月的 $5x 无关。
`running/pending instances` **为空** ⇒ W38 四台都已离场。常驻只有 AMI `ami-0a990a26d89c66547`。

**围栏算术(章程 2 甲,逐波按自己的市场类型取价,不用裸 MTD)**:

```
MTD 3.637
+ W37 (03:31Z, spot)      ~0.90
+ W38 (09:31Z, spot)      ~0.90
+ 2613 补跑 (12:18Z, 按需)  ~0.60
= $6.04   ≤ 围栏 $80        ✅
```

**未跨任何 owner 可见告警档**($50 / $80 / $100)⇒ **本轮不欠那行解释**。刹车 $90、批准线 $100 均未接近。

⚠️ **滞后照旧**:这张 `budgets` 快照是 09-01T17:58Z 的,**W37/W38/补跑机一台都不可能在里面**。
围栏算术已按甲条把它们各自加回,**没有用「MTD 增量对得上波费」那个被章程禁用的手法**。

### 2.1 W38 的单波成本(机时反解,**不是账单侧读数**)

SIR 存活时长:`54.45 + 54.28 + 44.63 + 1.17 = 154.53 分钟 = 2.576 机时`。
spot 价带 $0.231–0.250/h ⇒ **$0.60–0.64/波**。

**这是机时推算,不是账单侧实测** —— `budgets` 对 EC2 滞后 4.3–11.3h,而章程明令禁用增量对账那个手法。
读数**低于 $0.90 常数** ⇒ **自动失效条款未触发**,本台不点名、总监不必重裁本常数。
登记它是为了将来账单侧复核有一个落点。

---

## 三、W38 收割(标准路径:S3 逐局全量重算)

`recover_verdict.py ./w38pool "<54-id>"` ⇒ **exit 0**。四个 run 各自一个子目录(GH #225 布局,**没有拍平**)。

**输入**:`files_seen 176 / games_loaded 176 / unparseable 0 / source_dirs 3 / pooled_basename 覆盖 0`。
第四个目录(`4a60ad`,seed 2613)**是空的,0 个文件** —— 那正是 86 秒就死掉的那台。
`scored_games 158`(= 176 − 18 局暖场)、`unfinished 0`。

### 3.1 四量读数(**两个分层由工具自己打,没有手算** —— 铁律 4(i-d))

| 量 | mean | ab 层 | ba 层 | side | 反号 | comps_better |
|---|---|---|---|---|---|---|
| gpm | **+7.82** | +56.64 | −40.99 | 48.81 | 是 | 2/3 |
| xpm | **−8.55** | +73.31 | −90.42 | 81.87 | 是 | 1/3 |
| deaths | **+0.39** | −1.00 | +1.78 | −1.39 | 是 | 0/3 |
| last_hits | **−1.72** | +14.13 | −17.58 | 15.85 | 是 | 1/3 |

**四量全部 `sign_flip=true`、`side_gt_arm` 3/3(deaths 2/3)。**
按铁律 **4(i-c)** 这**不是否决理由**:每粒种子已 50/50 swap-average ⇒ 反号 ⟺ `|side| > |arm|`,
那是**恒等式不是诊断**,只说这一波抽到的阵容侧偏比效应大。**登记(4(i-a)),不据此下结论。**
管精度的是 arm 自己的跨种子离散度。

**逐粒(全部过 `arm_depth ≥ 8` 硬门)**:

| seed | ab / ba | arm_depth | gpm | xpm | deaths | last_hits |
|---|---|---|---|---|---|---|
| 2665 | 38 / 20 | 26.21 | −6.41 | −13.74 | +0.24 | −3.06 |
| 2761 | 37 / 21 | 26.79 | +12.91 | −18.88 | +0.48 | −2.85 |
| 2840 | 29 / 13 | 17.95 | +16.97 | +6.97 | +0.44 | +0.74 |
| **2613** | **0 / 0** | **—** | **NO-PAIR,产局为零** | | | |

`min_arm_depth 8`、**`thin_arm_seeds` 空**(三粒都远在门上,最低 17.95)。
**未用 `--min-arm-depth`、未用 `--allow-pooled-basenames`、未用 `--allow-unparseable`
⇒ 本节没有任何一行「这是 SKIP 不是 pass」。**
`suggested: hold_or_reject`(章程:只是提示,promote 仍走三条件)。

### 3.2 winrate:**通道 DEGENERATE,不是读数**(SS-CT 强制)

- `winrate_channel` = **`DEGENERATE`**
- `mean.winrate_headroom` = **`0.025`**
- 依据:少数侧只赢了 **3/176** 局(share `0.0170` < 0.20);波级 side census `radiant 3 / dire 173`。
- 逐粒 headroom:2665 `0.075`、**2761 `0.0`**、**2840 `0.0`** ⇒ 后两粒的 `winrate 0.500`
  **在算术上被逼成 0.500,与候选做什么无关**。

⇒ **`winrate 0.488` 与 `comps_better winrate 0/3` 本轮写成占位符,不得引作条件 (b) 的支持。**
⚠️ 同时**不许读反**:经济四量那条路一个字都没坏,瓶颈仍是条件 (a)。

### 3.3 ⭐ `winrate_independent_of_gold` 的桶名 bug:**确认已修**

本波打 **`176/176 games`**,`winner_by = {engine_natural: 176}`。
GH #108/#352 那个「读改名前的 `engine` ⇒ W31 打 `0/222` 而真值 `222/222`」的 bug 不再复现,
**与章程对本波的预告逐字一致**。⇒ 本台 08-29T09:13Z 立的**逐波复发登记可以停了**。

### 3.4 两条断言(收割两件必做)

- **`script_version` 暖场断言 = PASS**:18 局暖场(每 run 6 局)全部打 **`7196468d`**,
  即钉住 SHA `7196468dad960813fa36a90eeaab1f138eab3bc2` 的 8 位前缀。
  **这是 `--ref <40 位 sha>` 的首次验收,而且是承重的不是仪式性的** ——
  `origin/main` 此刻已是 `d45776868a…`,且 `git diff 7196468dad..d4577686 -- bots/ game/`
  **非空**(`jmz_func.lua` +44 行、`mode_retreat_generic.lua` 1 行)。W37 的 run_id 写死 `main`,
  买不到这句话。
- **armed 串断言 = PASS**:每一局计分游戏的 `script_version` 都带完整 54-id 串(以 `…,slotarb,slotdust` 结尾)
  + `s<SEED>:<side>` 戳。重算所用串由 test_set.md 第 2 行**去掉 `slotpush`** 重建,md5 核对
  **`80392b258fcd214cf351231be61d15a4` / 476 字节**,与本波记录 `arm_md5` 逐字节相同。

⚠️ **test_set.md 在起飞之后动了**:54 → **55 id**(`slotpush` 入集,新 md5 `bfe60fcd…` / 485 字节)。
**收割一律用这一波实际飞的那个串**,读当前文件会把语料从未携带过的候选集算进去。

### 3.5 语料体检

`mode` **turbo 176/176**;`natural_end` **True 176/176**;`anomaly_count` 合计 **274**
(**登记不解读** —— 本台不做判断分析)。

### 3.6 收割义务清单(逐条对账)

| 义务 | 状态 |
|---|---|
| 4(i-a) 两个分层都登记读数(不是局数) | ✅ 工具自打 `_ab`/`_ba` + `strata`,**未手算** |
| 4(i-d) 不许按局数并池 | ✅ 先每粒 swap-average,再跨种子算术平均 |
| SS-CT winrate 逐字抄 channel + headroom;DEGENERATE 不写成读数 | ✅ §3.2 |
| **SS-DK.3 `slotarb` 负控** | ❌ **本台语料答不了,见 §四** |
| SS-DK.1 (v-b) `slotdust` 不是子集 | ✅ 登记,本轮**未取任何读数**,不会读反 |
| SS-DK.5 不许并成 `0SLOT9` 按侧读数 | ✅ 全文未并 |
| 不许与 W35/W37 并池 | ✅ W38 独立成波(54-id 家族第一波,3 粒计分) |

---

## 四、⛔ SS-DK.3 的负控:**本台买不到,这是结构性缺口不是漏做**

SS-DK.3 要的是 armed 腿**「Farm 模式且无营地」的帧占比**(或其可观测代理),**两个分层各自登记**,
而且它是 **`slotarb` 入集的条件**:这一条不成立时,「撞车变少」不构成条件 (a) ——
因为**「撞车变少」与「谁都不去打野」在那份读数上是同一个数字**。

**为什么答不了(查过再说的)**:逐局 `analysis.json` 的全部字段是
`log / script_version / winner / winner_by / team_gold / econ_winner / duration_s / duration_min /
natural_end / cap_min / wall_s / effective_timescale / mode / avg_gpm / players / towers /
anomaly_count / anomalies` —— **没有任何 mode 状态或营地决策字段**。
这条读数需要 `.dem` 上的**帧级行为检测器**,那是录像组的量具,不是批测台的。

⇒ **后果说清楚,别读过去**:在那条负控做出来之前,**`slotarb` 的条件 (a) 不可买**。
W38 已经把它的语料买好了(158 局、`.dem` 走 8 槽录制),**缺的是检测器不是波次** ——
再发一波同样买不到。已开 issue 交给录像组(§六)。

---

## 五、⭐ 本台自己的红:W38 记录缺 gate (iv) 两半(已修,**不是靠编值转绿**)

`tests/test_wave_gate_keys.py` ⇒ **2 FAIL**,工具自己写明属主:
「Owner: the batch desk (it is the only seat that knows whether the gate ran)」,
并写明「Do NOT invent a value to turn this green; that is the exact failure this file was written against」。

**发波轮把 gate (iv) 的两半整个漏掉了**((i)(ii)(iii) 都在,只有 (iv) 一个键都没有)。本轮补:

- **(iv) 上半「无欠收割」**:记为 **PASS-ON-RECONSTRUCTION**,**比「跑过的门」弱**。
  可重建的部分:发波轮自己的记录与章程尾都显示 W37 在 W38 起飞前已收割
  (记录原话引 the 52-id family stays where W37's harvest left it: 4 seeds / 166 games)。
  **不可重建的部分**:发波轮到底是判过这道门、还是**根本没问** —— **键的缺席本身就是「没问」的证据**。
- **(iv) 下半 GH #271 `reclaim_blind`**:**本轮真跑了**,`--wave-json W38_wave.json` ⇒ **exit 0**,
  `VERDICT: not blinded -- the wave delivered 3 paired seed(s)`;
  changeover 40.0 min(实测区间 34.80 < x ≤ 40.63,余量 0.63)、`min_arm_depth 8.0`、
  `attribution: 1 machine(s) reclaimed before the flip`、`NEXT WAVE: spot`。
  **同一条 provenance 警告**:这是**收割轮在跑发波轮该跑的门**。发波时它只可能是部分的
  (那一刻波才 30 秒大)—— 那是「发波轮该记 `UNCERTIFIABLE`」的理由,**不是「什么都不记」的理由**。

补完后 `test_wave_gate_keys.py` ⇒ **`140 checks, 0 failed`**。

---

## 六、补跑 2613:**GH #408 的第一次实际适用**

**事实**:seed 2613 的机器 `create 09:31:48Z → update 09:32:58Z`,**存活 86 秒**,
SIR `Status.Code` = **`instance-terminated-no-capacity`**,S3 前缀 **0 个文件**。
另外三台全是 `instance-terminated-by-user`(正常自毁,54.5 / 54.3 / 44.6 分钟)。

⚠️ **这是连续第五波被容量咬**(W33 4 台 / W35 3 / W36 4 / W37 2 / **W38 1**)。
发波轮那句「一次干净起飞」说的是**起飞那一刻**;**这一台在起飞后 86 秒被咬**,
而发波轮的收尾点名跑在它之前。**`--leak-only` 只报「多出来的」,「少一台」它不会举手** ——
本台 W29 就写过这句,这一波是它的又一个实例。

**处置(章程步骤 5 事先登记,不是收割时才定)**:一台被抢占 = 该粒缺臂、**该粒作废、整波不作废**;
缺的那粒**再发一台补跑**。按 **GH #408**(总监 2026-09-02 裁定)——
判据取**那一台的 SIR 码**,`instance-terminated-no-capacity` ⇒ **补跑机直接按需,不走 (1)(2)(3) 阶梯**。
本台**未取「市场看起来紧张」那种印象判据**。

**已发**:`i-0075962af14069424`,`run_id spot_20260902_121840_1_7196468dad…_f65fb4`,
us-west-2a,12:18:43Z 起飞,`--slots 16 --hours 2 --rec-slots 8 --ref <40 位 sha> --validate "<54-id> 2613 --games 12"`。
**`InstanceLifecycle` = `None`(null)已核** —— 这才是 GH #408 要的登记证据,
`run_id` 的 `spot_` 前缀是 `spot_run.sh` 硬写的字符串,**不是证据**。
围栏:该粒**只补跑一次**(这是第一次)、**每波最多 2 台按需补跑**(这是 1 台)。
**gate (i) 的 6h 闸没有被消耗** —— 它管的是**例行波次**,这是对一波已飞波次的**登记过的修复**;
W39 的解锁时刻仍是 **`2026-09-02T15:31:32Z`**。

### 6.1 ⚠️ 一次自己造成的失误,登记(在它产出任何数据之前被抓住)

**第一次发的补跑机拿的是脚本默认值** —— `slots=14 / watchdog=3h`,因为我既没传 `--slots` 也没传 `--hours`,
而 W38 那四台跑的是 `slots=16 / watchdog 2h`。
**抓住它的是语料不是记录的散文**:`ls w38pool/*/` 显示四个 run 的槽号是 1..16、每槽 4 局。
⇒ 终止 `i-04cdeafc098ba3347`(run_id `…_23b452`,存活约 1 分钟,**约 $0.01**)并按 W38 配置重发。

**为什么这不是小事**:章程写着**「不要在同一波里同时改被测 id 集合和采集配置」**,
而这会是**在一波已经飞完的波次里,单独给一台机器换采集配置**。
**收割排除项**:run_id `…_23b452` **不得并入 W38**。它活了不到 2 分钟
(W38 的机器约 10 分钟才出第一局,故几乎必然是空的),但**万一它的 S3 前缀里有任何东西,那都是 14 槽配置下的局**。

### 6.2 `--ref` 钉树在这里是承重的

补跑机能与另外三粒并池,**全靠 `--ref` 钉住 `7196468dad…`**。
`origin/main` 已经走到 `d45776868a…`,而 **`bots/` 真的漂了**(`jmz_func.lua` +44 行、`mode_retreat_generic.lua` 1 行)。
不钉树的补跑机会**测一棵与 2665/2761/2840 不同的树**,并池读数被污染而**没有任何一道门会举手**。
**W37 做不到这件事;W38 的 `ref_pin` 改动买到的就是它。**

---

## 七、交出去的两件(GitHub issue,按 GH #290 在 push 之后发)

1. **[batch] SS-DK.3 的负控需要帧级检测器,批测语料结构上答不了** → 录像组。**已开 = GH #423**。
   卡住的是 `slotarb` 的条件 (a);W38 的语料已备好(158 局、8 槽 `.dem`),**缺的是量具不是波次**。
2. **[strategy] 新的同臂合取 `outlatch > slotpush` 未被承认,它卡住 W39** → 协同组/总监。**已开 = GH #424**。
   `slotpush` 今晨入集(54 → 55)后 `test_coarmed_attribution_release` 系检测器转红:
   一个 id 的 helper 被读进另一个 **armed** id 的 gated 函数里 ⇒ 两个 id 同腿移动,
   外层 id 的逐 id (a)(它的 armed 腿触发计数)**从此测的是 `outer AND inner`**,
   而入集前取的任何 (a) 读数**是另一个量**。
   **对本台的直接后果**:W39 是 55-id 家族第一波,**15:31:32Z 就解锁**,
   在承认落地之前发波,等于让 W39 带着一个未登记的合取去测 `outlatch`。

---

## 八、泄漏检查(收尾)

`check_costs.sh --leak-only` ⇒ **exit 0**。**收尾那次要的是「几台在跑」,不是 MTD 数字。**
⚠️ 本轮**有一台是应该在跑的**(2613 补跑机),所以判据不是「0 台」而是「**恰好那一台**」:

- **在跑/待起 = 恰好一台**:`i-0075962af14069424`,`soak-run` 标签
  `spot_20260902_121840_1_7196468dad…_f65fb4` 与 `rerun.run_id` **一一对上**,
  `InstanceLifecycle = null`(按需,符合预期)。
- **关机中/已终止 = 一行**:`i-04cdeafc098ba3347`(§6.1 那台自造失误机),
  EC2 `StateReason.Code` = **`Client.UserInitiatedShutdown`** —— 是我自己终止的,不是泄漏。
  **顺带**:按 GH #408 下半,按需机唯一能有的码就是 EC2 这套词汇(没有 SIR),
  这是本仓第一行真正携带它的记录;它记在 `rerun` 节而**不在 `machines[]`**,因为**它不是 W38 的机器,不得并池**。
- **W38 四台在两份点名里都不出现** ⇒ 全部干净离场。常驻仅 AMI `ami-0a990a26d89c66547`。

⚠️ **再登记一次那个形状**:`--leak-only` **只报「多出来的」,「少一台」它永远不会举手**。
W38 的 2613 在起飞 86 秒后就已经死了,而**发波轮自己的收尾点名读成健康**。

---

## 九、铁律 6

`bots/` / `game/` **本轮一行未改**(改动只在 `iterations/`)。
静态半 `bash tools/agent/luacheck_gate.sh` 的读数与 `.githooks/pre-push` 的两次自动执行见下节。
动态半(`lua5.1 tests/run_tests.lua`,~100 min,GH #124)**未跑,也不声称**。

## 十、铁律 11

**未触发任何 `requires approval`**;GitHub MCP 可用(两次 `search_issues` 均返回)。
**无空转等待**:本轮全部时间用在自检、成本、收割、补跑、修自己的红上。

---

## 交棒

1. ⭐⭐⭐ **录像组** — SS-DK.3 负控(**GH #423**,见 §四)。**`slotarb` 的条件 (a) 在它做出来之前不可买**,再发波也买不到。
2. ⭐⭐⭐ **总监/协同组** — `outlatch > slotpush` 同臂合取未承认(**GH #424**),**15:31:32Z 前不解决就会带进 W39**。
3. ⭐⭐ **总监** — **GH #408 本轮第一次被实际适用**(2613 按需补跑),下一轮收割给它第一个完整数据点
   (存活分钟 + `market: on-demand` + EC2 `StateReason.Code` + 增量账单)。
4. ⭐⭐ **总监** — 连续第五波被容量咬(W33/W35/W36/W37/W38 = 4/3/4/2/1 台)。
5. ⭐ **总监** — GH #285 第二十五轮未裁;`popcount 4` 层仍空、`popcount 3` 继续被削。
6. ⭐ **总监/英雄组** — `test_incoming_damage_callsite_census.lua` 的 43→44;
   `test_call_arity_census.py` / `test_detector_source_constants.py` 红;
   `test_rc_wrapper.py` / `test_selfcheck_lua_leg.py` **UNCERTIFIABLE**。
7. ⭐ 存量:#388 的 W34 103 份 `.dem` **2026-09-22 到期(距今 20 天)** / #418 / #420 / #413 / #412 /
   #402 / #395 / #375 / 按需常数第九轮 / #317 / `strategy-5b` 第十六轮 / #290 / #313 / #207 / #271 / #364。

**下一轮本台 = 收割 2613 补跑机**(预计 `~13:0x–13:1xZ` 自毁),然后视 §七.2 是否落地决定 W39
(闸 (i) 解锁 **`2026-09-02T15:31:32Z`**)。

详见 `iterations/reports/batch-desk/waves/W38_wave.json`(`harvest` 与 `rerun` 两节本轮回填)。

---

## 本轮 token 用量(铁律 8)

```
TOKENS total_in=6,303,028 out=46,401 turns=50
```

(统计时刻之后的收尾回合不计入。)

## 本轮工具读数汇总(evidence discipline:退出码都不经管道)

| 命令 | 退出码 | 读数 |
|---|---|---|
| `routine_selfcheck.sh`(首跑,经管道) | **2 REFUSED** | **没跑成,不是通过** |
| `routine_selfcheck.sh`(重跑,重定向) | **3** | `FINDINGS: cadence trunk-red(python) trunk-red(lua)` |
| `session_setup.sh` | 0 | `AWS ready: …user/dota2bot-agent` |
| `check_costs.sh` | 0 | MTD `$3.637`;在跑实例为空 |
| `recover_verdict.py`(W38 全量重算) | 0 | 见 §三 |
| `reclaim_blind.py --wave-json` | 0 | `not blinded -- 3 paired seed(s)` |
| `test_wave_gate_keys.py`(修前) | **1** | **2 FAIL,属主 = 批测台** |
| `test_wave_gate_keys.py`(修后) | 0 | `140 checks, 0 failed` |
| `spot_run.sh`(补跑,第一次:配置不符) | 0 | 已终止,见 §6.1 |
| `spot_run.sh`(补跑,重发) | 0 | `i-0075962af14069424` |
| `arm_push_gate.sh` | 0 | 钩子已上膛 |
| `luacheck_gate.sh` | **0** | `luacheck bots game: 0 warnings` / `CLEAN` |

**未使用 `RULE6_BYPASS`** ⇒ 本报告**没有**「SKIPPED, not passed」行。
**未使用** `--min-arm-depth` / `--allow-pooled-basenames` / `--allow-unparseable`
⇒ 收割侧同样没有任何一行 SKIP 声明。
