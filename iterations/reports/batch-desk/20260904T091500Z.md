# 批测台 2026-09-04T09:15–09:4xZ

**一句话**:收割 W45 = **60-id 家族第一波也是唯一一波**(4 粒全部过 `arm_depth` 门,
215 计分局)。**本轮零发波**(闸 (i) 明确 THROTTLED,还差 3h13m)、**零新增支出**、
**零泄漏**。真正的产出是两件,而且都不在那四个数里:**(1) 上一轮欠的
`rerun[].status_code` 不是"没来得及读",是按构造读不到** —— 按需补跑机没有 SIR,
EC2 记录 1 小时过期,而补跑机在起飞后 ~45–60 分钟自毁,下一次 Routine 触发在 ~2–3
小时后:**这个字段在正常触发节奏下永远填不上**。**(2) 同一次查询给出了它的对偶:
SIR 比 EC2 记录活得久** —— 两台幸存 spot 机的死因码是在终止 **1.8 小时后**读到的,
而同一时刻 `describe-instances` 对它们已经返回 `[]`。**spot 有一条耐久的死因路线,
按需没有。**

## 1. 自检(铁律 10)

⚠️ **开工第一条命令又误用管道,自检自拒**(`REFUSED: stdout is a pipe; exit 2,
nothing checked`)—— **同形第 6 次,又是当轮第一条命令**。工具自己把它打成
「**这不是通过**」并抬码到 2;改重定向重跑。

重跑 `EXIT=124` = **超时(400s)死在最后一条腿(trunk health / python 套件)上**,
**不是干净也不是发现**。已跑完的腿:`stable-v1`/`stable-v2` 锚点
`EXISTS/PINNED/SHIPPED` **全 ok**;`OWED_EXECUTION: none`;
`unlanded_commits` **无 OFF-TRUNK**;cadence 有 `GAP cadence strategy` +
7 条 `SKIPPED-IN-STREAM`(**均为 08-24~08-29 历史,按 LIMIT 2 不是本轮发现**)。
⛔ **python 腿这轮没人看过** —— 上一轮(06:30Z)亲手复现的 trunk 红
(`tests/test_bots_walk_farm_only.py`,`be232487` 引入)**本轮未复核,不声称已修**。

## 2. 成本(免费路径)

`check_costs.sh` **EXIT=0**。九月 MTD **`$12.647`**,`source: budgets/dota2bot-batch
(free)`,快照 `refreshed 2026-09-04T04:11:48Z`,forecast 119.148 / limit 100.0。
MTD `< $35` ⇒ **CE 未触发,本轮没花那 `$0.01`**。

⚠️ **这张快照按构造不含 W45**(04:11Z 早于 W45 的 06:30Z 起飞)。围栏算术照铁纪律
(甲)**用 `MTD + Σ(近 12h 已发波 × 该波自己市场的单波价)`**,不用裸 MTD:

```
  12.647  MTD(budgets,免费)
+  2.15   W44   按需
+  1.80   W45   实测上界(spot 0.64 + 按需补跑 1.16),见第 4 节
= 16.60   vs 围栏 $45/$80、刹车 $90、owner 线 $100
```

**未触发任何刹车;九月至今未触及 Budget 第一档 ACTUAL 告警($50)。**
⛔ **未使用被禁的「MTD 增量对得上波费 ⇒ 该波已计入」手法**(铁纪律乙)。

## 3. 收割:W45(60-id 家族,第一波有数据)

**`RV_EXIT=0`**,读的是**重定向后的裸 `$?`**,不是管道尾巴的码。
**没用 `--allow-pooled-basenames`、没用 `--allow-unparseable`、没用 `--min-arm-depth`
⇒ 本轮没有任何「这是 SKIP 不是 pass」的行要抄。**

**臂串核对(本轮亲手从发波树重算,不抄散文,也不抄章程计划句)**:
`git fetch --depth 1 origin 27165f8c…`(**认退出码 0**)→
`git show 27165f8c…:iterations/streams/test_set.md` 第 2 行 ⇒
**60 id / 530 字节 / md5 `eef5fb2ef553d96a50960988d482ca4e`**,
与 `W45_wave.json` 登记的 `arm_ids`/`arm_bytes`/`arm_md5` **逐位相同**。

语料:**245 个 `*.analysis.json`**,`files_seen 245 / games_loaded 245 /
unparseable 0 / pooled_basename_dirs_overridden 0`,`scored_games 215`,
`unfinished 0`。

⚠️ **两处读数纪律,都是本台自己先读错再纠正的**:
- **S3 前缀**:第一次数六个前缀全打 **0 对象**,原因是**数错了前缀** ——
  逐局数据在 `soak/<run_id>/`,不在 `validation/<run_id>/`(后者是扁平的
  verdict/log 对象)。**那个 0 是我的读法,不是空波**;换对前缀后是
  122/154/120/1/129/120 个对象。⭐ 与上一轮 07:1xZ 那次「`aws s3 ls` 不展开通配符」
  **同族不同因**:两次都是**前缀读法**给出一个长得像数据的 0。
- **`source_dirs 5` 不是 6**:六个 run 前缀都下载了,**3927 的原机贡献了一个空目录**
  (它被回收时落盘 0 局)。**那个 5 是对 0 局回收的读数,不是丢了一个 run。**

**并池**:六个 run **各自独立子目录**(GH #225 —— 同秒起飞 + 无 run token 的 basename,
拍平就是 W14 那次静默丢 15.9% 的形状)。**按种子跨机并池是工具原生行为**
(按 `s<SEED>` 戳分组,不按目录):3968 = 原机 35 局 + 补跑机 48 局,
3927 = **完全由补跑机贡献**(原机 0 局)。

### 3.1 四量(每粒先 swap-average,再跨种子取算术平均;**永不按局加权**,铁律 4(i-d))

| | gpm | xpm | deaths | last_hits |
|---|---|---|---|---|
| 均值 | **−6.19** | **+0.24** | **−0.06** | **−0.79** |
| `comps_better` | 2/4 | 2/4 | 3/4 | 2/4 |

逐粒 gpm:`3892 −48.55` / `3954 −23.01` / `3927 +6.19` / `3968 +40.62`(**极差 89.2**)。

**`min_arm_depth = 8`,`thin_arm_seeds = []` —— 四粒全部过门**
(`arm_depth` 27.11 / 20.22 / 17.95 / 23.66)。
**没有任何一粒种子的局数是花了钱又被门扔掉的**,本节无被排除种子要点名。

### 3.2 两个分层的**读数**(铁律 4(i-a);`recover_verdict.py` 自己打的 `strata`,**未手算**)

| 量 | ab | ba | side | 反号 | `side_gt_arm` |
|---|---|---|---|---|---|
| gpm | −101.58 | +89.20 | −95.39 | 是 | 3/4 |
| xpm | −59.98 | +60.46 | −60.22 | 是 | 3/4 |
| deaths | +0.38 | −0.48 | +0.43 | 是 | 4/4 |
| last_hits | −10.96 | +9.40 | −10.18 | 是 | 3/4 |

按 **(i-c)**:四个量都是**每粒 50/50 swap-average 的、侧偏已消除的**估计量,
所以四处反号 ⟺ `|side| > |arm|`,**那是恒等式不是诊断**,
**登记(那是 (i-a))但不当否决理由**。

### 3.3 ⛔ 胜负通道:**DEGENERATE,连续第四波**

`recover_verdict.py` 自己在 stderr 打:

> `GH #352 winrate channel DEGENERATE: minority side won 3 of 245 finished games
> (share 0.0122 < 0.20). The winrate below is bounded to 0.5 +/- 0.0246 and MUST NOT
> be cited as rule 2(b) support until the channel recovers.`

侧别普查 **radiant 3 / dire 242**。⇒ W41、W42、W44、**W45** 是**连续第四波有数据的波**
胜负通道结构性不可读(W43 是**沉默**,不是其中一波)。
**上一轮的预登记分支逐字兑现**:按 GH #352 / `test_set.md` §CT,
**本波 `winrate 0.489` 与 `comps_better` 的 winrate 行(0/4)一律不得写成读数,
更不得当铁律 2(b) 的支撑。** 上面两张表**已按此把 winrate 行整行排除**。

### 3.4 `suggested = hold_or_reject` —— 是提示不是裁定

它**只读 gpm**。铁律 2 的三条件才是尺子,而卡住这个 id 家族的**从来是条件 (a)**
(录像确认改动真的执行)。⚠️ 且按 §CO.4,**本台不再用 SE / 显著性给一整波下
「够不够裁」的结论** —— 本节只交读数,promote/reject 归总监。
**n=1、4 粒:W45 不与 W44(58-id)并池,也不与 W41∪W42(57-id)并池。**

## 4. 账单侧机时与三笔欠账

| 机器 | 市场 | 机时 | 死因码 | 码的来源 |
|---|---|---|---|---|
| 3968 原 | spot | 0.631 h | `instance-terminated-no-capacity` | EC2,**上轮**在 1h 窗内读,精确 |
| 3892 原 | spot | 0.924 h | `instance-terminated-by-user` | **SIR,本轮 09:15Z 读到** |
| 3954 原 | spot | 0.774 h | `instance-terminated-by-user` | **SIR,本轮 09:15Z 读到** |
| 3927 原 | spot | 0.225 h | `instance-terminated-no-capacity` | EC2,**上轮**在 1h 窗内读,精确 |
| 3927 补跑 | 按需 | **≥0.943 h** | ⛔ **UNREADABLE 永久** | 见第 5 节 |
| 3968 补跑 | 按需 | **≥0.673 h** | ⛔ **UNREADABLE 永久** | 见第 5 节 |

两台按需机的机时是**下界**(末次 S3 上传 − LaunchTime)。W42 在三台正常自毁机上量过
这条路线:下界偏短 **3.19/3.28/3.65 分钟**(**紧,方向对**)⇒ 真值约 **59.8 / 43.7 分钟**。
**n=3、一波 ⇒ 只登记,不据此改任何常数。**

**(a) W44 账单侧对账**:MTD 快照 `refreshed 04:11:48Z`,W44 关机在 **09-04 01:0xZ** 附近
⇒ 距快照 ~3h,**仍落在 4.3–11.3h 滞后带内**。**按构造仍结不了,第三次顺延**,
且**未用被禁的增量对账手法**。

**(b) ⛔ W45 结不了 GH #454,上一轮的自我更正成立。** 四台 spot 只跑了 **2.554 机时**
(W29 那波干净读数是 3.424),因为**两台中途被回收**。`$0.90` 常数要的是**一波跑完的
spot**,W45 是混合波。实测 spot 侧 **$0.59–0.64 < $0.90** ⇒ **自动失效条款也不触发**
(它只在 **>$0.90** 时触发)。**#454 仍欠一波跑完的 spot。**

**(c) ⭐ 按需机时常数 0.745 h/台 连续第二波读低(GH #473 乙)。**
W44 给出下界 **0.765**(已在常数之外);W45 给 **0.943 / 0.673**,均值 **≥0.808 h**
(修正后 ~0.863)= **比常数高 ~16%**。⚠️ **失效方向是危险的那一侧** ——
单波价记低 ⇒ 围栏算术读出比实际更多的余量,与「滞后让 MTD 系统性偏低」**同向叠加**。
**而与 `$0.90` spot 常数不同,这个常数身上没有自动失效条款** —— 那个缺口就是 #473 乙。
本波实测总额 **~$1.68–1.80 vs 发波时登记的 ~$2.00**:**登记值偏高,朝安全侧失效**。

## 5. ⭐⭐ 头号发现:按需补跑机的死因码**按构造读不到**,而 SIR 比 EC2 记录活得久

上一轮把 `rerun[].status_code` 留成 `pending -- to be read at harvest`,并交棒
「(a) 补跑机按需 ⇒ 没有 SIR,唯一能有的码是 EC2 `StateReason.Code`」。**本轮去读,读不到。**
`describe-instances` 对**四台**(两台幸存 spot + 两台按需补跑)在 **09:14:46Z** 返回 **`[]`**。

**这不是"忘了读",是三条已知事实的合取,而它们此前从未被放在一起看**:
1. 按需机**没有 SIR**(GH #408)⇒ 唯一可能的码是 EC2 `StateReason.Code`;
2. EC2 记录 **~1 小时**过期(GH #375);
3. 补跑机在起飞后 **~45–60 分钟**自毁,而 Routine **每 ~2–3 小时**触发一次。

⇒ **窗口在任何收割轮开工之前就已经关上。** 本波:补跑机约 **08:0xZ** 结束,
下一次触发 **09:07Z** —— 迟到约 1 小时。**这不是本波的运气,是节奏与窗口的构造关系:
按当前 cron,这个字段永远填不上。** 上一轮那句「留空待读」因此是一张**无法兑付的欠条**,
而**没有任何东西会举手说它不可兑付**。

⭐ **同一次查询给出了它的对偶,这才是能用的那一半**:两台幸存 spot 机的死因码
(`instance-terminated-by-user`)+ 终止时刻,是在终止后 **~1.8 小时**从
`describe-spot-instance-requests` 读到的 —— **同一时刻 EC2 那边对它们已经是 `[]`**。
⇒ **SIR 记录比 EC2 记录耐久得多**:**spot 机有一条能扛过 1h 窗口的死因路线,
按需机一条都没有。** 这条不对称此前没被登记过(GH #375/#408/#427 各自只碰到一半)。

**处置(本台不自行改工具/常数,交总监)**:开 `[batch]` issue,给出三条候选路线 ——
(i) 发波脚本在**机器自毁那一刻**把 `StateReason.Code` 落进 S3(在窗口内,不靠人赶节奏);
(ii) 收割侧**只对 spot 走 SIR 路线**并把按需机的该字段正式标成 `BLINDED-BY-CONSTRUCTION`
(**别再留"待读"这种无法兑付的欠条**);(iii) 接受失明并从 `machines[]` schema 删掉该字段。
⚠️ **本台的倾向是 (ii)+(i),但这是裁定不是读数,不自行落地。**

## 6. 发波:闸 (i) 明确 THROTTLED,**本轮不发**

`wave_throttle.py`(**在 `tools/batch_test/soak/` 不在 `aws/`** —— 本台先按
`aws/` 路径调用得到 `WT_EXIT=2 / No such file`,**那个 2 是"没跑成"不是"没锁"**,
改对路径重跑)在 **09:17:46Z**:

```
anchor wave W45 (4 machine(s)) | last machine up 06:30:52Z <- the anchor (GH #469 ruling 1)
unlock 2026-09-04T12:30:52Z | margin 11586s SHORT | breaches gate (i) by 193m06s
WAVE_THROTTLE: THROTTLED (exit 3) -- do not launch. Wait 11586s.
```

**`WT_EXIT=3` 裸读 `$?` 不经管道。** ⇒ **本轮零发波、零新增支出。**
⭐ 顺带:锚点落在 `machines[]` 的 06:30:52Z 而**不是**补跑机的 07:12:58Z ——
**「补跑机不开新的闸 (i) 窗口」这条按 schema 结构性成立**(工具只读 `machines[]`),
上一轮说它「不依赖任何人诚实填字段」,**本轮是它第一次被真的行使**。

## 7. 泄漏检查:干净

- `pending,running,shutting-down,stopping,stopped` 全状态实例 ⇒ **`[]`**;
- `describe-volumes` ⇒ **`[]`**,**无游离 EBS**;
- `state=open,active` 的 spot 请求 ⇒ **`[]`**;
- 常驻成本只有 AMI `ami-0a990a26d89c66547`(照旧)。

**W45 六台机器全部自毁并清理完毕,零残留。**

## 8. 队列

W45 是**家族级读数不是逐 id 裁定**,`suggested` 只是提示 ⇒ **本轮不翻动任何 id 的
queue 状态**(含上轮翻成 `running` 的 `strategy-40` `arbheart` / `strategy-41` `slotwait`)。
promote/reject 走铁律 2 三条件,归总监。

## 9. 铁律 6 / 11

`bots/` / `game/` **本会话一行未改**(改动全在 `iterations/`)。
`luacheck_gate.sh` / `arm_push_gate.sh` 读数见提交记录;
**未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行**;
动态半(#124,~100min)**未跑,不声称**。
**铁律 11**:未触发任何 `requires approval`,未坐等。

## 10. 交棒

1. ⭐⭐⭐ **总监 —— 新 `[batch]` issue:按需补跑机死因码按构造失明 + SIR 比 EC2 耐久**
   (第 5 节,三条候选路线)。**上一轮那张"待读"欠条无法兑付,别再开同类欠条。**
2. ⭐⭐ **总监 —— GH #473 乙 第二个数据点**:按需机时常数 **0.745 连续第二波读低**
   (W44 下界 0.765;W45 均值 ≥0.808),**失效方向危险且无自动失效条款**(第 4(c) 节)。
3. ⭐⭐ **总监/英雄组 —— trunk 红本轮未复核**:上轮亲手复现的
   `tests/test_bots_walk_farm_only.py` 红,本轮自检 python 腿**超时未跑**,
   **不声称已修**(第 1 节)。
4. ⭐ **总监 —— GH #352 / §CT:胜负通道连续第四波 DEGENERATE**(第 3.3 节)。
   这已经不像单波偶然,值得当成结构问题裁一次。
5. ⭐ **GH #454 仍欠一波跑完的 spot**(第 4(b) 节;W45 混合波结不了)。
6. ⭐ 存量:GH #449(57-id 并池)/ #473 甲(载体门看不见单英雄域的 id)/ #472
   (W44 那 48 份带 pudge 的 `.dem`,约 09-25 过期)/ #427 / #408 / #420 / #290 /
   `strategy-5b`。

**下一轮本台 = 视闸 (i)(`2026-09-04T12:30:52Z` 解锁)发 W46**;
若在解锁前触发则**只做巡检不发波**。**预登记两条,写在任何数据落地之前**:
(a) **W46 的臂串必须从发波树重算** —— 章程的计划句不是臂串(W45 就是这么抓到
`+arbheart +slotwait` 的);(b) **若 W46 是一波跑完的纯 spot,它就是 GH #454 等的
那一波**,账单侧单波成本要**当场对 `$0.90` 常数结账并触发/不触发自动失效条款。

## 11. 本轮 token 用量

```
TOKENS total_in=2,856,243 out=33,976 turns=30
```

(统计时刻为收尾前;此后的收尾回合不计入,量级参考足够。)
