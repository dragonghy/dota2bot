# 批测台 2026-09-10T15:14–15:5xZ(收割轮:W63)

**一句话**:W63 收割完成 —— **3/4 粒配对**(第四粒 10714 被 spot 回收,`NO-PAIR`),
本波 126 局;**与 W62 并池后家族深度 4 粒 → 7 粒、203 局 → 329 局**,这是该程序第一次
拿到两位数粒数量级的语料。⭐⭐⭐ 头号发现**不是**那个并池读数,而是收割时从语料自己
读出来的两件事:**(甲) `recover_verdict.py` 每波静默丢掉 6 局/台(本波 24 局 = 13.6%),
而它自己的三个计数字段全部读作"零损失"**;**(乙) GH #352 那条"横扫"通道的少数侧占比
按种子分层看是 0.0%–23.8%,不是均匀的** —— 那把归因从"某个非门控 commit 打坏了侧别平衡"
指向"多数阵容本来就一边倒,少数不是"。

---

## 〇、本轮定位

上一轮(12:14Z)发了 W63 四台 spot,`--hours 2`,预计 ~14:20–14:50Z 落地。
本轮 15:14Z 开工,四台**均已终止**,`harvest_owed = W63` ⇒ **本轮是收割轮**,
按交棒单 ①②执行。**本轮零发波**,且这是结构性的不是选择:闸 (i) 名义解锁
`2026-09-10T18:18:45Z`(末台 + 6h),现在 15:1xZ **还没到**;⛔ 未抄该时间戳作为判据
之外的任何用途,也未跑发波路径。

## 一、成本(当轮现跑,上一轮的数字全部作废)

`AWS_SETUP_EXIT=0`,身份 `arn:aws:iam::581436534924:user/dota2bot-agent`(受限 IAM)。
`COST_EXIT=0`,MTD **`$77.847`**(免费 `budgets`,`budget refreshed 2026-09-10T14:18:09Z`),
CE `$0.01` 复核 **`77.8469680665`** 逐位一致。`forecast 182.287` **原样登记,不据以决策**。

⭐ **本轮快照终于动了**:上两轮连续读到同一个 `06:09:49Z` 冻结戳(那两轮已各自登记
"MTD 没涨是零信息")。本轮 `14:18:09Z` 是**新戳**,MTD `$75.023 → $77.847`,涨 **`$2.824`**。
⛔ **但不得把这 `$2.824` 读成"W63 花了多少"** —— 章程步骤 2(乙)禁的就是这个手法,
而且这里结构上更明确:按 11.3h 滞后,诚实 cutoff = `14:18:09Z − 11.3h` = **`2026-09-10T03:00:09Z`**,
**W63 首台 12:18:38Z 远在 cutoff 之后 ⇒ W63 一分钱都还没落账**。这 `$2.824` 里含的是
**W62 那一波**(末台 09-09T21:24:41Z,现已越过 cutoff 入账)**外加外来支出**,两者不可拆。

`$50` 告警 `state=ALARM` 仍越线;`$80` / `$100` 仍 `OK`。⛔ GH #515/#528 一字未变
(预算过滤器仍 `null`),⇒ **MTD 里外来那一半仍不受本台任何闸约束**。

## 二、终止码:⭐ 4/4 全部读到,**含一台被回收**

按发波记录 `harvest_notes (a)` 的预测**先读码后收割**。`describe-spot-instance-requests`
`SPOT_EXIT=0`,四台 `State=closed`,`Status.Code` **逐台读到**:

| 种子 | instance | `Status.Code` | 终止时刻 | 机时 |
|---|---|---|---|---|
| 10614 | `i-0c4a62df047d11326` | `instance-terminated-by-user` | 13:04:30Z | 0.7644 h |
| **10714** | `i-0e8228a306af84d36` | **`instance-terminated-no-capacity`** | **12:47:03Z** | 0.4731 h |
| 10842 | `i-0a044690fc9e2b1e5` | `instance-terminated-by-user` | 13:03:18Z | 0.7431 h |
| 10889 | `i-02005852a6dd3ebb4` | `instance-terminated-by-user` | 13:05:36Z | 0.7808 h |

⭐ **这正是 W62 供不出来的那个数**(按需码 ~1h 老化,GH #375),spot 波保住了它 ——
发波记录 `harvest_notes (a)` 逐字预测「这是普通情况」,**本轮兑现**。

⚠️ **10714 在起飞 28 分钟后被容量回收**,只跑完 ab 腿(26 局)、ba 腿 0 局 ⇒ `NO-PAIR`。
⛔ **不要把它读成"AZ 分散没用"**:发波记录已按本台 W59 的发现登记过「4/4 AZ 分散**不是**
种子间独立性的证据」,本轮 1/4 被回收**与那条一致,不是新证据,也不给 GH #403 收案**。

## 三、收割读数(W63 单波)

`recover_verdict.py` 走标准路径:**先按 run 分四个子目录下载再合并**(`/tmp/pool/{201fec,16a195,14565d,715cc0}`),
⛔ 未 `cp` 进同一目录(GH #225 那个 15.9% 静默丢局的坑)。逐 run 落盘数与 S3 侧
`s3 ls | grep -c` **逐个相等**(48/32/48/48 = 176),零 `[Errno 36]`。
臂串当轮现算:**37 id / 335 字节 / md5 `b525d51d4b4957e0e40f22f203aea641`**,与发波时逐位相同。
`RECOVER_EXIT=0`,`unparseable 0`,`pooled_basename_dirs_overridden 0`,`min_arm_depth 8`,
`thin_arm_seeds []`。

| 种子 | ab/ba | `arm_depth` | gpm (ab / ba) | xpm | deaths | last_hits | 计分 |
|---|---|---|---|---|---|---|---|
| 10614 | 28/14 | 18.67 | **+21.68** (−15.85 / +59.21) | +14.97 | −0.17(更好) | −0.92 | 42 |
| **10714** | **26/0** | **0.0** | — | — | — | — | **`NO-PAIR` 排除** |
| 10842 | 28/14 | 18.67 | **−45.04** (−202.84 / +112.76) | −10.77 | +0.36 | +1.44 | 42 |
| 10889 | 29/13 | 17.95 | **−21.65** (−125.12 / +81.82) | −9.16 | +0.10 | −0.68 | 42 |

跨种子(**先每粒 swap-average、再跨种子算术平均**;⛔ 未按局数加权 —— 铁律 4 (i-d)):

| 量 | mean | comps_better | ab 层 | ba 层 | side | sign_flip |
|---|---|---|---|---|---|---|
| gpm | **−15.00** | 1/3 | −114.60 | +84.60 | −99.60 | true |
| xpm | −1.65 | 1/3 | −73.05 | +69.74 | −71.40 | true |
| deaths | +0.10(更差) | 1/3 | +1.32 | −1.12 | +1.22 | true |
| last_hits | −0.05 | 1/3 | −14.95 | +14.84 | −14.89 | true |

铁律 4 (i-a) 两层读数**已全部登记**。四个量 `sign_flip=true`、`side_gt_arm=3/3` ——
按 **(i-c)**:经济四量每粒 50/50 swap-average、**侧偏已消除** ⇒ **反号不是否决理由,
也不含精度信息**(`|side|>|arm|` 是恒等式的改写)。⚠️ 本波 side 幅度**很大**(gpm 侧偏
−99.6,是 arm 的 6.6 倍),这说明**抽到的三个阵容侧偏很重**,不说明 arm 测得准或不准。

工具字段 `suggested: "hold_or_reject"` **原样转录** —— 章程第 5 行:本台不做判断分析,
promote/reject 归主会话与总监。

## 四、⭐⭐ 并池读数(W62 + W63):家族深度 **7 粒 / 329 局**

发波轮已当轮逐 hunk 核过可并池(11 个真实代码 hunk 在 37-id 臂串下全部不可达,
8 个 soak id 无一 armed)⇒ **同一个可达程序**,按 §GH.5.4 可并池。
本轮**未重做该核验**,也**未推翻它**(树未动:语料里 `script_version` 四台全读 `cfa966e3` = 钉,见 §六)。

**并池方法**:取两波**全部 7 粒已配对种子**的 per-seed 读数(每粒工具自己已 swap-average),
**跨种子取算术平均**。⛔ **未按局数加权**(#329 (i-d):W27+W28 上那个手法把 +26.60 读成 +9.20)。

| 量 | 并池 mean(7 粒) | comps_better | 跨种子 SD | W62 单独(4 粒) | W63 单独(3 粒) |
|---|---|---|---|---|---|
| gpm | **−10.55** | 3/7 | 23.78 | −7.20 | −15.00 |
| xpm | **+5.10** | 5/7 | 11.50 | +10.17 | −1.65 |
| deaths | **−0.04**(更好) | 4/7 | 0.23 | −0.14 | +0.10 |
| last_hits | **+0.32** | 3/7 | 1.44 | +0.60 | −0.05 |

⭐ **这一波买到的东西在这一行上兑现了**:同一个程序的两波,gpm 读数相差 **7.8**
(−7.20 vs −15.00),而并池跨种子 SD = **23.78** ⇒ 均值标准误 `23.78/√7` = **8.99**。
⇒ **两波之差(7.8)小于单波读数自己的标准误** —— 这正是"depth-1 跑步机"上看不见的东西:
**W62 单独那个 −7.20 与 W63 单独那个 −15.00 从来不是两个结论,是同一个量的两次抽样。**

⚠️ **本台不声称这个 −10.55 "显著"**(铁律 2 本来也不要显著性),也**不给 promote/reject 建议**。
本台只交出:并池后 gpm `−10.55 ± 8.99`,`comps_better 3/7`;xpm 是四个量里唯一
`comps_better` 过半的(5/7)。判读归主会话与总监。

## 五、⭐⭐⭐ 头号发现(甲):`recover_verdict.py` 每波静默丢 6 局/台,而三个计数字段都读作零损失

**现场**:本波 `input` 逐字 `files_seen 176 / games_loaded 176 / unparseable 0`,
`scored_games 126`。176 → 126 的差是 50;其中 26 局在 `NO-PAIR` 的 10714 上、
verdict 里**指名可查**。**剩下 24 局在 verdict 里没有任何字段提到过。**

**这 24 局是什么**:逐台**恰好 6 局**(48→42 / 32→26 / 48→42 / 48→42),
全部是每台**最早的 6 局**(`slot1`–`slot6`,12:19:30Z–12:20:20Z)。它们的 `script_version`
读作 **`cfa966e3`**(裸 git SHA),而不是 `mirror:<37-id 臂串>:s<seed>:<side>` 那个戳 ⇒
**它们跑在 `write_soak_side.sh` 写下镜像腿之前**,归不到任何一条腿上。

**⇒ 排除它们是对的。缺陷不在排除,在于排除是无声的。** `recover_verdict.py:205`
`STAMP_SEED.match(a.get("script_version") or "")` 不匹配就 `continue`,**没有计数器、
没有 refusal**;而该文件自己的头注释为同一类失效(basename 撞车、文件解析不了)
写了 **R1/R2/R3 三道会 `exit 2` 的门**,理由逐字是「a file that does not parse -> a lost game」。
**这一道门后面的那扇窗没有关。**

**为什么这不是"已知且无害"**:本台核对了 W62 —— `files_seen 227 / games_loaded 227`、
`scored_games 203`,per-seed 四粒腿数合计**也是 203** ⇒ **W62 同样丢了恰好 24 局(10.6%)**。
⇒ 这**不是本波偶发,是每波都在发生的常量**(6 局/台 × 4 台)。
今天它无害,是因为丢的恰好是无戳的热身局;**但同一条无声路径也会吞掉任何一次戳格式漂移** ——
若 `write_soak_side.sh` 的戳格式变了一个字符,`scored_games` 会从 126 掉到 0 附近,
而 `files_seen`/`games_loaded`/`unparseable` **三个字段会全部继续读作"零损失"**。
**那正是 GH #225 立案时 W14 那 15.9% 的形状,只是换了一扇门。**

⇒ 开 **[harness] issue**(见 §八),建议:verdict 里加 `unstamped` 计数 + 无戳局的
`script_version` 取值直方图;并考虑把"无戳局占比 > 某阈值"做成 R4 refusal。
⚠️ **本台不改工具代码**(章程:本台不写 bot/harness 代码),只立案并交出复现读数。

## 六、⭐⭐ 发现(乙):GH #352 的"横扫"按种子分层是 0.0%–23.8%,不是均匀的

`recover_verdict.py` 本波打两条 stderr,**逐字**:

> `GH #696 winrate comps_better: 3 of 3 seed(s) had headroom 0 and could not vote yes (seeds 10614,10842,10889). The pooled fraction 0/3 counts them; comps_better.winrate_measurable 0/0 does not. A promote bar above 0/3 is unreachable on this corpus.`

> `GH #352 winrate channel DEGENERATE: minority side won 1 of 176 finished games (share 0.0057 < 0.20). The winrate below is bounded to 0.5 +/- 0.0 and MUST NOT be cited as rule 2(b) support until the channel recovers.`

⇒ `mean.winrate 0.5` / `winrate_headroom 0.0` **登记但不可引用**;#352 的验收判据
(「少数侧占比 ≥ 20%」)**本波未达成**(0.57%),⇒ **#352 保持 open**。

⭐ **但把两波的 `winrate_side_census` 按种子摊开,看到的不是一条均匀的横扫**:

| 波 | 种子 | radiant | dire | 少数侧占比 |
|---|---|---|---|---|
| W62 | 10601 | 0 | 58 | 0.0% |
| W62 | 10607 | 1 | 44 | 2.2% |
| W62 | 10803 | 0 | 58 | 0.0% |
| **W62** | **10813** | **10** | **32** | **23.8%** ← **单独看达标** |
| W63 | 10614 | 0 | 42 | 0.0% |
| W63 | 10842 | 0 | 42 | 0.0% |
| W63 | 10889 | 0 | 42 | 0.0% |

**一粒种子(10813)的少数侧占比是 23.8%,越过了 #352 自己写的那条 20% 线,
而它同波的三个姐妹是 0.0% / 2.2% / 0.0%。** 两波七粒里六粒 ≤2.2%、一粒 23.8% ——
**这个分布不是"通道坏了"的形状,是"多数阵容一边倒、少数阵容不是"的形状。**

⇒ 这条线索**改变 #352 的搜索方向**:#352 现在的归因假设逐字是「非门控修复两条腿一起变,
正是那种能整体挪动侧别平衡的形状」,即**在树里找一个 commit**。但一个 commit 造成的
全局侧别倾斜**没有理由在同一棵树的四个阵容上读出 0.0% / 2.2% / 0.0% / 23.8%**。
**种子(= 镜像双方的那 10 个英雄)才是随分层变的那个变量。**

⚠️ **诚实边界,三条**:(1) 这是 **7 粒的分层描述,不是归因裁定** —— 本台按章程不做判断分析;
(2) 10813 那粒**只有 42 局**,23.8% 的少数侧占比自身抽样误差不小(10 局,`±`约 6.6pp);
(3) ⛔ **不得据此宣布 #352 的通道"其实是好的"** —— 七粒里六粒仍然读不出胜负信号,
铁律 2 条件 (b) 在**绝大多数阵容上依然测不到臂**,止血条款(#352 建议 (a))**继续有效**。

⇒ 以**评论**形式交给 #352(不开重复 issue,#352 open 且正是这件事)。

## 七、GH #454 两条腿:⭐ 连续三轮"无测量点"结束,(乙2) 本轮**可读且读出来了**

发波记录 `cost.gh454_recheck_legs` 把这两条腿指名交给收割轮:

- **(乙2) 自终机器的平均机时 > 1.00 h/台?** —— **可读,答案 `否`。**
  三台 `instance-terminated-by-user` 的机时 **0.7644 / 0.7431 / 0.7808 h**,
  均值 **`0.7628` h/台 < 1.00** ⇒ **不支持上调常数**。被回收的 10714(0.4731 h)
  **按判据明文排除**在均值之外。
- **(乙1) 账单侧单波成本 > `$1.10`?** —— **本轮不可读,不猜。** 理由在 §一:
  预算快照 `14:18:09Z`,诚实 cutoff `03:00:09Z`,**W63(12:18Z 起飞)整波都在滞后窗内、
  一分钱未落账**。⛔ 未用"MTD 增量对账"倒推(章程步骤 2(乙)明禁),也**未**拿
  机时 × 现货价估一个数冒充账单读数。**这条腿交给下一个能看到含 W63 的快照的轮次。**

⇒ GH #454 **保持 open**,但它从"连续两轮没有测量点"变成**一条腿已结、一条腿有明确到期条件**。

## 八、波次记录回填:⭐ ⛔ 那条交棒单的头号项**已执行,零 null**

`W63_wave.json:machines[]` **本轮回填完毕**:`ab` / `ba` / `arm_depth` / `scored` /
`status_code` / `status_update_time` / `machine_hours` 逐台写入,**`null` 计数 = 0**
(程序化核过并打印)。10714 额外带 `excluded: "NO-PAIR"`。

⭐ **这正是 GH #699 那个吸收态的上游**:00:14Z 那轮把 W62 收满四粒却**没有回填**,
下一轮闸 (iv) 读的是**波次记录不是 verdict**,于是从 06:18Z 到 09:17Z 连续两轮
`exit 2 UNDECIDABLE`、三轮零发波。**本轮把那个上游关上了** ——
下一个发波轮的闸 (iv) 读 `W63_wave.json` 时,四台的 `status_code` 都在。

`tests/test_wave_gate_keys.py` **当场跑**:**`WGK_EXIT=0`,417 checks 0 failed**。
verdict 全文归档 `iterations/reports/batch-desk/waves/W63_verdict.json`。

⚠️ 顺带登记:本台**仍未**拿到总监那个「收割轮零 null 检查」(06:18Z / 09:17Z / 12:14Z
已交三次)。本轮是**人工纪律**做对的,不是闸做对的 —— **人工纪律恰好是上一次掉棒的那个东西**。

## 九、⭐ 钉的窗口(GH #710)本轮拿到**事后实证**,但性质不变

上一轮自记退步:传了 `ref=main` 而非 40 位 SHA,守卫关不掉「守卫 → 实例 clone」之间的窗口。
**本轮从语料侧读到了那个窗口的实际结果**:176 个 analysis 文件的 `script_version`
(无戳的 6 局/台)读作 **`cfa966e3`**,有戳的读作 `mirror:...`;而 `cfa966e3` **正是钉
`cfa966e39384cffb83613b4265476eeaecc41950` 的短 SHA** ⇒ **四台确实 clone 了钉的那棵树。**

⭐ 但**这不改变 GH #710 的立案**,理由要写清楚:本轮是**事后从产物里读到没出事**,
不是**构造上不可能出事**。窗口仍在;下一波若在窗口里被别的 stream 推了 main,
**仍然没有任何东西会举手**,而下一轮收割**未必会想起来去读 `script_version`**。
⇒ **GH #710 保持 open**,建议不变(`--validate` 波次强制 40 位 SHA)。
⭐ 顺带交给 #710 一条**本轮才发现的免费实证路径**:`analysis.json` 的 `script_version`
在无戳局上**就是被 clone 的那棵树的短 SHA** ⇒ 收割轮可以零成本地事后核钉。

## 十、泄漏:零,两条独立路径

`LEAK_FILTERED_EXIT=0`:us-west-2 `pending,running,stopping,stopped` 过滤 —— **空**。
`LEAK_EXIT=0`:us-west-2 **不带状态过滤**、只排除 `terminated` —— **空**。
⇒ W63 四台**全部真的终止了**,零遗留。常设成本仍只有 AMI `ami-0a990a26d89c66547`
(`dota2bot-batch-20260718`),无孤儿快照行。
⚠️ 两条都是 **us-west-2 的读**;账户级零本轮**未买**(未跑发波路径 ⇒ 未跑 `wave_fence.py`
的 17-region accrual 扫描)⇒ **不声称账户级零**,只声称 us-west-2 零。

## 十一、铁律 6 / 10 / 11

- **铁律 6**:`bots/` `game/` `tests/` `tools/` **本会话一行未改**(只动 `iterations/` 下的
  报告、`W63_wave.json`、`W63_verdict.json` 与本章程节)。push 时三条腿的读数抄在下面。
  **未用 `RULE6_BYPASS`** ⇒ 无「SKIPPED, not passed」行。动态半(~100min)未跑**不声称**。
- **铁律 10**:自检**本轮跑了**,读数见 §十二。⚠️ **本轮第一条命令又踩了管道坑
  (第 11 次)** —— `routine_selfcheck.sh` 自己 `REFUSED` 并打 `exit 2`,逐字
  「stdout is a PIPE, so the exit code your harness reports is the READER's」。
  ⭐ **本轮的不同**:那道拒绝**起作用了** —— 它把假读数挡在外面,本台改走
  `> /tmp/selfcheck.log` 重定向 + 后台。**工具赢了这一次,但连续 11 轮同一位置**说明
  防的是症状不是原因(⇒ 已在交棒里点名)。
- **铁律 11**:全程**未触发任何 `requires approval`**;`mcp__github__*` 读写均正常。
  **零空转等待**(自检在后台跑,本台并行完成收割与报告)。

## 十二、闸 (i):⭐ **零发波是工具说的,不是本台抄时间戳推的**

§〇 里"闸 (i) 未解锁"这句话,本台**当轮跑了工具**而不是抄发波记录里那个
`18:18:45Z`(章程明写「别抄,跑 `wave_throttle.py`」)。`THROTTLE_EXIT=3`,逐字:

```
anchor wave      : W63 (4 machine(s))
last machine up  : 2026-09-10T12:18:45Z  <- the anchor (GH #469 ruling 1)
window           : 6h
unlock           : 2026-09-10T18:18:45Z
now              : 2026-09-10T15:38:56Z
margin           : 9589s SHORT of unlock
A launch at this instant BREACHES gate (i) by 159m49s.
WAVE_THROTTLE: THROTTLED (exit 3) -- do not launch. Wait 9589s.
```

⇒ **本轮零发波是闸 (i) 的裁定,不是本台的选择**,也不是"没想到要发"。
⭐ 顺带核到一件事:工具把锚正确地认成 **W63**(`4 machine(s)`)——
即 §八 那次回填**下游立刻就有读者**。

## 十三、自检读数(铁律 10)

⭐ **`SELFCHECK_EXIT=3`,读到了真码**,逐字:

```
legs run              : 11
FINDINGS (exit 3)     : cadence queue-rulings owed-executions a-evidence-owed trunk-red(python) trunk-red(lua)
UNCERTIFIABLE (exit 2): none
NOT RUN (inside a leg): tests/test_selfcheck_lua_leg.py
selfcheck worst exit: 3
```

⚠️ **一条要写清楚的时序**:这条横幅是在**本报告第一次 push 之后**才打出来的
(Lua 腿在本容器上单腿超过 20 分钟,与 GH #358 记的"同样 50 个文件在本台测
133.3s、在别处 99.5s"同族)。第一次 push 时本节写的是**"不报 `EXIT=`,
没读到的退出码不写"**,现在是**补上真码**,不是当时就有。
⭐ **`UNCERTIFIABLE (exit 2): none`** ⇒ **trunk 两侧本轮都被真跑完看过一遍**。
⚠️ 但同一份输出还打了 `NOT RUN (inside a leg): tests/test_selfcheck_lua_leg.py`
—— **腿级 `none` 与腿内 `NOT RUN` 并存**,见 (甲)。
自检自己紧跟着的那句限定也照抄:
`Read this before writing "exit 3, all of it cadence": that sentence was hand-made
attribution for days, and on 2026-08-27 it was wrong for 22 hours (GH #267).`
⇒ 本台**不把这 6 项 findings 归成一句**,逐项照抄如下。

下面每一条都是**打印出来、逐字抄的**。

**(甲) python 腿:`TRUNK RED` + `UNCERTIFIABLE` 同时成立。**

- `FAIL  tests/test_bots_walk_farm_only.py`,逐字
  `a tests/ walk reaches bots/Customize/ without excluding the farm-only switches
  -- add ! -path "bots/Customize/soak_*.lua" to the find … ['tests/test_isvalid_building_sentinel.lua:95']`
- `UNCERTIFIABLE  tests/test_selfcheck_lua_leg.py  (did NOT run -- this is not a pass and not a failure)`,
  其下 **9 条 check 全部 `UNCERTIFIABLE`**(`5a0`/`5a`/`5a2`/`5b`/`5c`/`5d`/`5e`/`5f`/`5g`),
  原因逐字 `the clean run did not finish inside 120s`,并自带一句要紧的限定:
  `5a in particular would pass vacuously`。收尾横幅逐字
  **`UNCERTIFIABLE -- 9 check(s) did not run; this is NOT a pass.`**
- 该腿自己的限定也照抄:`TRUNK RED -- a python test is failing ON THE WORKING TREE.
  Whether main is red too is NOT established by this line: re-run after git stash.`

**(乙) Lua 腿(打印到 push 时为止):两条 `RED`。**

- `test_focus_mana_cost_consumer_census.lua:424` —— `axe: corpus flip census moved --
  expected n=26 farm=5 spam=3 either=6, got n=27`;同文件 `:471`
  `the control must sweep the same 173 frames, saw 176`
- `test_lion_ult_reserve_domain.lua:367` —— `the tree now holds 4 rank-2 Finger instants
  (EXACT rank), not 2`;`:505` 点名最小池实例已换成
  `tests/frames/f_260909_215412_axe_cull_cm_838.lua`

**(丙) ⛔ 归因三分,本台只登记不裁**:

1. **本会话 `bots`/`game`/`tests`/`tools` 一行未改** ⇒ **无一条由本轮引入**。
2. `test_bots_walk_farm_only.py`、`test_focus_mana_cost_consumer_census.lua`、
   `test_lion_ult_reserve_domain.lua` **12:14Z 那轮已全部报过**(GH #705 一族)
   ⇒ **不开重复 issue**。
3. 形状仍是 GH #106/#127/#585 那一族:**把语料规模写死成等式**,
   下一个 fixture 落地就打红一个什么都没变的地方(`n=26→27`、`173→176 帧`、
   `rank-2 2→4`)。⚠️ **这是相关不是归因** —— 交总监。

⭐ 另外**已跑并读到真码的三个门**(都不经管道):
`WGK_EXIT=0`(417 checks 0 failed,§八)、`THROTTLE_EXIT=3`(§十二)、
`PRECHECK1_EXIT=0` / `PRECHECK2_EXIT=0`(两份草稿各 `clean` /
`OK to publish: every citation resolves on origin/main.`)。

## 十四、铁律 6 三条腿读数(push 时逐字抄,GH #624)

两次 `git push` 各跑一次,读数**两次相同**:

```
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 94 ran, 0 findings, 0 uncertifiable, 36.6s
lua gate: SKIPPED BY SCOPE -- this push touches no bots/game/tests path.
  4 path(s) offered, none of which can move a Lua test's answer: …
  This is a SCOPE decision, not a pass and not a skip of a gate that had something to say (GH #624).
```

⚠️ **第三条腿的读数是 `SKIPPED BY SCOPE`,不是绿** —— 按 #624 的措辞它既不是 pass
也不是"有话要说却被跳过",**照抄不改写**。`tools/agent/arm_push_gate.sh` 本容器
`ARM_EXIT=0`(铁律 10 要求的每容器上膛)。**未用 `RULE6_BYPASS`**。

## 十五、Token 用量

`TOKENS total_in=5,276,002 out=45,520 turns=48`
(统计时刻 15:3xZ;之后的收尾回合不计入,当量级参考足够)。

---

## 交棒

1. ⭐⭐⭐ **总监 / 主会话 —— 并池读数已就绪,判读归你们**:该程序现在有 **7 粒 / 329 局**,
   gpm `−10.55 ± 8.99`(`comps_better 3/7`),xpm `+5.10`(**5/7**,四个量里唯一过半)。
   本台**不给** promote/reject 建议。⛔ 并池是 **7 粒不是 8 粒**(10714 被回收未配对)。
2. ⭐⭐⭐ **harness —— `recover_verdict.py` 无声丢局**(§五,本轮开 issue):
   两波各丢 24 局(13.6% / 10.6%),三个计数字段全读"零损失"。建议加 `unstamped` 计数
   + 无戳 `script_version` 直方图 + 考虑 R4 refusal。
3. ⭐⭐ **总监 —— GH #352 的搜索方向**(§六,本轮以评论交付):七粒分层 0.0%–23.8%,
   **不是均匀横扫** ⇒ 归因假设"树里的某个非门控 commit"与这个分布不合;
   变的是种子(阵容)。⛔ 止血条款仍然有效,不得据此宣布通道恢复。
4. ⭐⭐ **总监 —— 「收割轮零 null 检查」第四次请求**(06:18Z / 09:17Z / 12:14Z / 本轮)。
   本轮零 null 是**人工做到的**,而人工纪律正是上次掉棒的那个东西。
5. ⭐ **GH #454 —— (乙2) 已结:`0.7628 h/台 < 1.00`,不支持上调 `$1.10`**;
   (乙1) 等一个含 W63 的预算快照(W63 现在整波在滞后窗内)。
6. ⭐ **GH #710 保持 open**,并收下 §九那条免费实证路径(无戳局的 `script_version`
   就是被 clone 的短 SHA)。
7. ⚠️ **owner —— `$50` 仍越线**(MTD `$77.847`,`ALARM`,`forecast $182.287`),
   预算过滤器仍 `null` ⇒ MTD 里外来那一半**不受本台任何闸约束**(GH #515/#528)。
8. ⚠️ **自检管道坑第 11 次**,连续十一轮同一位置、同为当轮第一条命令。

## 下一轮本台

1. **发波轮**(若闸放行):闸 (i) 名义解锁 `2026-09-10T18:18:45Z` —— ⛔ **别抄,跑 `wave_throttle.py`**。
2. ⛔ **围栏/闸/成本一律当轮现跑**:`$77.847` / `$2.824` / cutoff `03:00:09Z` **全部作废**。
3. ⭐ **闸 (iv) 本轮已备好输入**:`W63_wave.json:machines[]` 零 null,四台 `status_code` 齐全。
4. ⭐ **`--pending` 要含 W63**:W63 整波(`$1.10` 常数)在本轮 cutoff 之后 ⇒ 下一轮大概率仍未落账。
5. ⚠️ 开工第一条命令**重定向 + 后台 + 不设短 `timeout`**(第 12 次提醒)。
