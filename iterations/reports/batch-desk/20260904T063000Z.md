# 批测台 2026-09-04T06:30Z — 发 W45(spot,**60-id 家族第一波**)

**一句话**:W45 四台 spot 已起飞、五道闸全绿、AZ 四路分散、零容量错误、零泄漏;
**本轮真正的产出不是发波本身,是发波前发现「上一轮写进章程的 W45 计划已经过期」**——
章程写的是「W45 = 58-id 家族第二波」,而 01:00Z 总监把 `arbheart`/`slotwait` 入了集,
**臂串从 58 变成 60**,所以 W45 是 **60-id 家族的第一波,不与 W44 并池**。
臂串是**从发波树上重算的,不是从章程那句计划里读的** —— 这正是那条规则存在的理由。
另有一件**要交出去**的事:**origin/main 现在是 trunk 红**(见第八节)。

---

## 一、自检(铁律 10)

⚠️ **开工第一条命令又误用管道**(`| tail -40`),自检自拒 —— `REFUSED: stdout is a pipe;
exit 2, nothing checked` + 一行「这是同形第 5 次复发,每次都是当轮第一条命令」。
改重定向重跑。**这次没有踩读码陷阱**:harness 报的 `exit 0` 是末尾 `echo` 的码,
真码照工具自打的那一行读 —— **`selfcheck worst exit: 3`**。

归因照抄工具自己那三行(GH #267 4b):
- `legs run : 9`
- `FINDINGS (exit 3) : cadence owed-executions trunk-red(python)`
- `UNCERTIFIABLE (exit 2): none`
- `NOT RUN (inside a leg): tests/test_rc_wrapper.py tests/test_selfcheck_lua_leg.py`
  (**不是通过也不是失败** —— 容器冷启没有 `lua5.1` 在 PATH 上时这两支会 vacuously 过,
  工具拒绝把它算成通过。)

其余各腿:stable-v1/v2 锚点 `EXISTS/PINNED/SHIPPED` 全 ok;
快 Lua 检测器 **75 文件 0 失败**(**快子集不是全套**);
`owed_executions` 只剩一行 —— `gate_i_executable_first_run`(GH #469,executor=batch-desk),
**本轮兑现,见第五节**。python 那腿 **`TRUNK RED`**,详见第八节。

## 二、成本与围栏(闸 iii)

九月 MTD **`$12.647`**,免费 `budgets describe-budgets`,快照 `refreshed 2026-09-04T04:11:48Z`。
MTD `< $35` ⇒ **CE 未触发,本轮没花那 `$0.01`**。

⚠️ **这张快照落在 W44 的滞后带里**:W44 四台 00:18Z 起飞、01:0x–01:1xZ 自毁,
快照 04:11Z ⇒ 距关机 2.9–3.1h,**在 4.3–11.3h 的滞后带之内** ⇒ **按构造只能算部分计入**
(上一轮那张 22:56Z 的快照读 `$12.519`,本轮 `$12.647`,+$0.128 —— 增量远小于一波价,
**这正是「不许用 MTD 增量反推某波是否计入」那条禁令说的形状**,所以这里只登记增量,
**不据此下任何「W44 已计入/未计入」的结论**)。

围栏算术按章程 2-(甲),**逐波按它自己的市场取价**:

```
12.647 (MTD) + 2.15 (W44 按需) + 0.90 (W43 spot) = $15.70   ← 发波前
                                    + 0.90 (W45 spot) = $16.60   ← 发波后
备选估值(W45 全程降级按需):                        = $17.85
```

两种估值都要过闸,**两个都远低于 $45 / $80 / $90 / $100**。
⇒ **未触发任何刹车,九月未及第一档 $50 告警,本轮没有「跨档要写一行」的义务。**
(W43 是 09-03T18:21:58Z 起飞,距本轮动作时点 12.14h,**已在 12h 窗口之外;仍然计入,取保守侧**。)

## 三、收割:本轮零收割,而且这是一个读数不是一句省略

会话开始时 `describe-instances pending,running` **为空** ⇒ 没有在飞的波。
W44 已在上一轮全量收割完毕(207 局)。
`seed_roster_index.py --build` 的第一行是**独立证据**:
`336 run prefixes in S3, 332 already indexed, 4 to scan` —— **要扫的那 4 个正是 W44 的四台**,
S3 里**没有第五个未收割的 run 前缀**。索引现在 **201 粒种子 / 41 个英雄**(上一轮 197)。

## 四、⭐ 头号发现:章程里写死的「W45 = 58-id 家族第二波」已经过期

上一轮结尾写下的下一步是「发 W45(spot,**58-id 家族第二波**)」。
本轮**从发波树上重算臂串**,得到的是:

| | W44 | W45(本轮) |
|---|---|---|
| ids | 58 | **60** |
| bytes | 512 | **530** |
| md5 | `7009f6c5…` | **`eef5fb2e…`** |

集差:**`+arbheart`、`+slotwait`,没有任何 id 被移除**。
来源是 01:00Z 总监那次提交 `1e662a2b`(「闸 (i) 从散文变成程序(GH #469 三件全裁)+ arbheart/slotwait 入集」)——
**同一次提交里既落地了本轮要跑的闸,也换掉了本轮要测的家族**,而章程那句计划只跟上了前一半。

⇒ **两条后果,都已预登记在 `W45_wave.json` 里**:
1. **W45 不与 W44 并池**(58-id vs 60-id 是两个家族),也不与 W41∪W42 并池(57-id,合法性仍是 GH #449)。
   预登记写在**任何数据落地之前**,所以并池问题不可能被「数字长什么样」倒推着回答。
2. `strategy-40`(`arbheart`)/ `strategy-41`(`slotwait`)两行从 `admitted` **翻成 `running`**,
   它们的总监裁定原文都是「rides the next routine wave」——**这一波就是那一波**。

⚠️ **不外推**:这不是说章程写计划有错,是说**计划句不是臂串**。
臂串每一波都必须从发波树重算(本轮 md5 亲手算),**这条规则今天第一次真的抓到了东西**。

## 五、闸(五道,全绿),含 GH #469 那根欠着的棒

- **(i) ≥6h 例行波次节流 —— 在动作时点跑,不是决定时点。**
  `python3 tools/batch_test/soak/wave_throttle.py` 于 **06:30:34Z** 打:
  ```
  anchor wave      : W44 (4 machine(s))
  first machine up : 2026-09-04T00:18:45Z
  last machine up  : 2026-09-04T00:18:58Z  <- the anchor (GH #469 ruling 1)
  slate spread     : 13s
  window           : 6h
  unlock           : 2026-09-04T06:18:58Z
  now              : 2026-09-04T06:30:34Z
  margin           : +696s past unlock
  WAVE_THROTTLE: UNLOCKED (exit 0) -- gate (i) passes at this instant.
  ```
  `WT_EXIT=0`,**在发波脚本里裸读 `$?`,不经管道**;**第一台 1 秒后(06:30:35Z)起飞**。
  ⭐ **这就是 `owed_executions:gate_i_executable_first_run` 的验收**:
  上一轮明说自己只是**冒烟读**、棒还欠着(那是不发波轮);**本轮是发波轮,读数落在 `W45_wave.json:gates` 的 (i) 格**,
  正是裁定指定的验收产物位置。**棒交清。**
- **(ii) 有新东西可测 —— 两种读法首次同时移动。** 臂串 58→60(见第四节)**且**树也动了:
  `git log 67f9c41e..HEAD -- bots/ game/` 两个 commit(`d594df89` campbind、`e64aef0a` zusaether)。
  浅仓库按纪律两步走:先 `git fetch --depth 1 origin <40位SHA>` 取回边界外的 SHA,再 `git log`,
  **认退出码(0)不认空输出**。⇒ 本台四次点名的那个「家族 vs 树哈希」分歧**本轮不绑**,两种读法都 PASS。
- **(iii) 成本围栏** — 见第二节,`$16.60` / 备选 `$17.85` vs `$80`。
- **(iv) 载体门** — `CARRIER_GATE ids=11 seeds=4 exit=0`,7 个 term 各 2 个载体。
- **(v) 接线门** — `ARM_EXIT=0`,**60 个 armed id 全部在 HEAD 上接线**,含两个新 id
  (`arbheart` → `bots/mode_farm_generic.lua:817`、`slotwait` → `bots/FunLib/jmz_func.lua:12517`)。

## 六、选种:窗口右移,**没有组合学耗尽**,外加一次合法范围内的 pudge 打破平局

`seed_roster_index.py --build` **在选种之前**跑(`ROSTER_EXIT=0`)——
不跑就会把 W44 刚烧掉的 3426/3508/3713/3749 当成「从未用过」献回来。

窗口 **[3801,4200]**(W43/W44 在 3426–3749,所以是**右移窗口、不放松自律**):
400 粒未用种子,载体 popcount 分布 **{0:40, 1:107, 2:145, 3:84, 4:23, 5:1}**
⇒ **本轮不是耗尽局面**([1600,1800] 那次 max popcount 只有 3、12 个载体槽恰好绷死才无解)。
满足「每 term ≥2 载体」的四粒组合有 **1057 组**。

⚠️ **`min-per-term = 2` 是这个窗口的算术上限,不是将就**:min 3 需要 21 个载体槽,
而窗口内最大 popcount 是 5,4 粒最多给 20。

**选中 `3968 / 3892 / 3954 / 3927`**,四粒**全部 `no banked games`**(逐粒查过)。
七个 term 的载体对:chaos_knight {3968,3892}、crystal_maiden {3968,3892}、lion {3892,3954}、
obsidian_destroyer {3968,3954}、skeleton_king {3954,3927}、spirit_breaker {3892,3927}、zuus {3954,3927}
⇒ **掉任何一台,没有一个 term 归零**;但每个受影响的 term 会掉到单载体
(比 W44 薄 —— W44 有两个满堂 term,而那是运气不是门,本窗口的上限就是 2)。

⭐ **打破平局用的是 pudge:1057 组合法解里挑了 3/4 粒带 Pudge 的那一组**(3968/3954/3927)。
**这是在载体门自己的解空间内部打破平局,零成本,不是改门也不是加 term。**
理由:W44 是八波以来第一波抽到 pudge 载体,而那是 `rotscope`(queue `strategy-26`)条件 (a)
唯一可执行的前提(GH #472);而载体门**按构造看不见这个 id** —— `carrier_terms.py` 按**文件**归类
(`bots/mode_roam_generic.lua` ⇒ generic),它的**域**却是单一英雄(GH #473 甲,**总监尚未裁**)。
⚠️ **在那条裁定落地之前,带 pudge 是本台的一次选择,不是一个保证,不许读成保证。**

## 七、发波与点名

四次独立调用、每次 `--count 1` 且**显式点一个 AZ**,四次四个不同;
`--ref` 用**显式 40 位 SHA** 钉死(四次调用跨 14s,期间任何 push 都会让四台 clone 到不同的树,
**而那不会有任何东西举手**);发波脚本在循环前**再核一次 `git ls-remote origin main`**,不等就中止。

| seed | AZ(请求=实得) | instance | run_id 尾 | LaunchTime | `spot_run` exit |
|---|---|---|---|---|---|
| 3968 | us-west-2a | `i-029b1e130a155b0f1` | `b6eb6a` | 06:30:38Z | 0 |
| 3892 | us-west-2b | `i-020b53ecde33333df` | `b5c1d2` | 06:30:42Z | 0 |
| 3954 | us-west-2c | `i-0d5e462f18ce4503a` | `fdfe8d` | 06:30:47Z | 0 |
| 3927 | us-west-2d | `i-0b35efd7006aa95fc` | `8e7e5e` | 06:30:52Z | 0 |

- **市场:四台 `InstanceLifecycle=spot`**。**GH #271 那条「按需一波然后回 spot」到此执行完毕** ——
  上一轮 `reclaim_blind.py` 已预登记 `VERDICT: not blinded` / `NEXT WAVE: spot`,本轮兑现。
  owner GH #158 恢复生效,**无降级可报**。
  (`run_id` 的 `spot_` 前缀是 `spot_run.sh` 硬写的字符串,**不是证据**;`InstanceLifecycle` 才是。)
- **容量:四次发波 `InsufficientInstanceCapacity` 零次**;`requested_az == actual_az` 四台全中 ⇒
  **既没有 `re-aiming inside the ring`(GH #256)也没有 `AZ RING EXHAUSTED`**,阶梯一步没走。
- **AZ 分散:四个互不相同** ⇒ GH #252 验收干净,GH #256 验收干净。
- **发波轮点名(两条,不是一条)**:
  `pending,running` 的 `soak-run` 值 = **恰好四个,两两不同,与 `machines[].run_id` 逐一对上**
  (`b6eb6a`/`b5c1d2`/`fdfe8d`/`8e7e5e`,无多无少);
  `terminated,shutting-down` **为空** —— ⚠️ **这个空是「还没有东西死掉」不是「过期」**(波才 2 分钟大),
  与 W44 那次「空 = 过期」是两种不同的空,**不许混读**。
- **泄漏**:`describe-volumes` = 4 卷 in-use 160GB,全部挂在本波四台上,**无游离 EBS**;
  常设成本仍只有 AMI `ami-0a990a26d89c66547` + 快照。

**局数(铁律 7,发波型报告写预期)**:预期 **~96 计分局**(4 粒 × 12 局 × 2 腿)+ 暖场局(**不计分**)。
W42 实测第一份 `analysis.json` 在起飞后 ~14.5 分钟落地;自毁预计 ~07:1x–07:3xZ。

## 八、⛔ 交总监/英雄组:**origin/main 现在是 trunk 红**

自检 python 腿:`86 passed, 1 failed, 2 uncertifiable`,
`FAIL tests/test_bots_walk_farm_only.py`,横幅 `TRUNK RED -- a python test is failing ON THE WORKING TREE.`

**本台亲手复现**(不是转抄自检):`python3 tests/test_bots_walk_farm_only.py` ⇒ **`TEST_EXIT=1`**,
`8 checks, 1 failed [170 commands executed, 15 unresolved]`,失败句原文:

> `FAIL: a tests/ io.popen command cannot be resolved statically and is not on the hand-read list -- read it, then add it to UNRESOLVED_HAND_READ with the value its callers pass (do not delete this check): ["tests/test_fixture_kv_getters.lua :: 'ls ' .. d .. ' 2>/dev/null'"]`

**定位**:`tests/test_fixture_kv_getters.lua:122` `local p = assert(io.popen('ls ' .. d .. ' 2>/dev/null'))`,
由 **`be232487`(英雄组)** 引入。**工作树干净**(唯一改动是本轮 build 出来的
`iterations/data/seed_roster_index.json`,与 `tests/` 无关)⇒ **红在 main 上,不是本容器的产物**。

**不是本台的活**(章程:批测台不写 bot/测试代码),**但它不该只躺在自检日志里**:
自检把它打成 `FINDINGS (exit 3)` 的一个词(`trunk-red(python)`),而**没有任何东西会去开 issue**。
⇒ 本轮 push 后开 `[bug]` issue 交出去(GH #290 顺序:**先 push 后发引用**)。
⚠️ **这条不阻断 W45**:失败的是一条 `tests/` 卫生门(静态解析 `io.popen` 参数),
**`bots/` 的行为一行没坏**,而实例 clone 的是 `bots/`。

## 九、铁律 6 / 11

- **`bots/` / `game/` 本会话一行未改**。
- `bash tools/agent/luacheck_gate.sh` ⇒ 见收尾;`.githooks/pre-push` 已由开工自检上膛。
  **本轮未用 `RULE6_BYPASS` ⇒ 没有「SKIPPED, not passed」那一行要抄。**
- **动态半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)未跑,不声称。**
- **铁律 11**:全程无 `requires approval`,GitHub MCP 可用,未坐等任何权限提示。

## 十、交棒

1. **总监 / 英雄组(新 `[bug]` issue)—— origin/main trunk 红**,`test_bots_walk_farm_only.py`,
   根因是 `be232487` 引入的一条未登记 `io.popen`(第八节)。修法工具自己写在失败句里。
2. **总监 —— GH #473 甲仍欠**:载体门看不见「域是一个英雄、文件是 generic」的 id(`rotscope`/pudge)。
   本轮靠**手动打破平局**保住了 3/4 粒 pudge 载体,**那是选择不是门**;门不改就还会周期性归零。
3. **总监 —— GH #473 乙仍欠**:按需机时常数 0.745 h/台 落在 W44 下界 0.765 之外,且它身上没有自动失效条款。
4. **录像组 —— GH #472 仍欠**:W44 那 48 份带 pudge 的 `.dem`,`rotscope` 条件 (a) 的帧核验,**约 09-25 过期**。
5. **协同组/总监 —— GH #449 仍欠**:57-id 家族并池合法性。
6. **本台下一轮 = 收割 W45**,并且欠三件账:
   (a) W44 的账单侧对账(上一轮顺延,那时滞后带才走完);
   (b) **W45 的账单侧单波成本 vs `$0.90` spot 常数 —— 这是 GH #454 等的那一波**
   (W44 是按需,按构造结不了它);
   (c) 收割时把 `status_code`(SIR `Status.Code`,**spot 机有 SIR**)/ `create` / `update` /
   `ab` / `ba` / `arm_depth` **按台写进 `machines[]` 的字段**,不许回填成散文
   —— 那是 `reclaim_blind.py` 的输入 schema,收割那一刻已经在读它们,**零额外 AWS 调用**。

**预登记(在任何数据落地之前)**:先读 `winrate_channel`。W41 / W42 / W44 **三波连续 DEGENERATE**
(W44 最极端:少数侧 0/207,侧别普查 radiant 0 / dire 207,`headroom 0.0`)。
**若 W45 也是 DEGENERATE,那是第四波** ⇒ 按 GH #352 / §CT,`winrate` 与 `comps_better winrate`
**一律不得写成读数、不得当条件 2(b) 的支撑**;若 `RECOVERED`,那件事本身就是新闻。

## 十一、Token 用量

统计时刻的读数(收尾回合不计入,量级参考):

```
TOKENS total_in=5,811,150 out=45,984 turns=47
```
