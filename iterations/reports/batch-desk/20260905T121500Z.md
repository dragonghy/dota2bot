# batch-desk 2026-09-05T12:18–12:4xZ

**一句话**:**W48 发波** —— 挡了三轮的那一件事本轮不在了。总监 **11:xxZ 裁定 `strategy-44`
(`stayattr`)`APPROVED_RIDESHARE_ADMISSION`**,臂串 **62 → 63**(md5 `c7e1f92c…` → `4aefc887…`),
于是闸 (ii) **三轮来第一次走 (一) 支:串自己动了**,不必再手做树漂移那段算术。
四台 spot、四个互不相同的 AZ、零 `re-aiming`、零 `InsufficientInstanceCapacity`
⇒ **GH #252 与 #256 的验收本轮双双通过**(W47 是 3/4)。四台 `InstanceLifecycle` 全为 `spot`。
预估 **~$1.02**,**未跨任何 Budget 告警档**(fence $50,MTD $19.597,发波后 headroom $29.503)。

---

## 一、自检(铁律 10)

⭐ **本轮开工第一条命令又误用了管道** —— 脚本自拒并逐字点名
(`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked` /
`SELFCHECK_EXIT=2  REFUSED`),**同形第二十五次,又是当轮第一条命令**。那个 `EXIT=2` 是
REFUSED,**不是通过**。改重定向后重跑,并按上一轮交棒**放后台给足时间**(实测本轮约 40 分钟跑满,
两次 `git push` 之前就已跑完)。

⚠️ **真码读脚本自打的那一行,不读 harness 报的**(后台任务通知的 `exit code 0` 是末尾 `echo` 的码):

- **`selfcheck worst exit: 3`**,`legs run 10`,**`UNCERTIFIABLE (exit 2): none`**
- `FINDINGS (exit 3)`:**`cadence owed-executions stale-waits trunk-red(python)`**
- `NOT RUN (inside a leg)`:`tests/test_rc_wrapper.py` `tests/test_selfcheck_lua_leg.py`
- **`3` = 有发现,不是没跑成。**

⭐ **两处相对上一轮的移动,而且两处都指向同一件事:**

1. **`un-ruled queue requests` 这一腿本轮空了。** 上一轮它报的是
   `strategy-44  status=registered prio=2 bundle=stayattr`,那一行**就是**上一轮不发波的全部理由。
   本轮它不在了 —— 这是**独立于 queue.json 读数**的第二条证据,说明裁定真的落了。
2. **`stale-waits` 是本轮新出现的那一条**,内容是 **`STALE batch-desk.md:9472`**,
   `id=stayattr  armed in test_set.md line 2`,指着本台自己上一轮写下的交棒句
   「先看 `strategy-44` 裁了没有;裁进集 ⇒ 臂串 63 id ⇒ 发 W48」。
   **它之所以 STALE,正是因为条件已经满足了** ⇒ 本轮发波 + 第六节改写「当前状态」即为其处置。
   同源地,`tests/test_stale_waits.py` 本轮**回红**(上一轮刚转绿),红的原因就是这一行;
   随本轮改写一并消解。

其余各腿:

- `stable version anchors`(铁律 3):`stable-v1` / `stable-v2` **EXISTS / PINNED / SHIPPED 全 ok**。
- `promote-time constraints`:**`FROZEN none`**(143 个活 gate id / 13 个带 PROMOTED 注),
  `pullcad` 陷阱未复发;atom `field_hold_needs_magnitude`(`stayfield`/`stayfield2`/`fieldsip`)三者仍全 GATED。
  ⭐ **这一条本轮承重**:`stayattr` 动的是 **PROMOTED** 的 `J.ShouldStayAndRegen`,
  而它**不落进**这个 atom(总监 §ET 已核:本 id 不读 `HasFieldRegenSource`/`IsFieldSipEnough`)。
- **`trunk health (python)`:`95 passed, 5 failed, 2 uncertifiable`**(工作树 == `origin/main` `54b839d1`,
  本会话对 `bots/`/`game/` 一行未改)。较上一轮(4 failed / 101 语料)**红从 4 回到 5**,
  **回来的正是 `tests/test_stale_waits.py`**(原因见上,本轮处置)。其余 4 条与上一轮同名、本轮不新增点名:
  `test_bots_walk_farm_only.py`(GH #497)、`test_detector_source_constants.py`(GH #514)、
  `test_lua_corpus_stability.py`(GH #514,与前者同源:同一个 `_mockscalar_sweep.lua:86` 式裸 walk)、
  `test_mutstand_restore_trap.py`(GH #506 / #501)。
  两条 `UNCERTIFIABLE` 照登:**不是通过也不是失败**。
  ⚠️ 该腿自打的边界照抄:`Whether main is red too is NOT established by this line` ——
  但本会话工作树逐字 == `origin/main`,故等价。
- ⭐ **`trunk health (fast Lua detectors)`:`84 tagged detector file(s), 0 failures`** ⇒ Lua 侧本轮跑过且绿。
  ⚠️ **边界照抄**:`FAST SUBSET, not the full suite. Untagged tests … are NOT covered here.`
  ⇒ **GH #508 是否已修,这条读数答不了**,仍挂着。
- `owed executions`:registry **2 行 OWED**,与上一轮同。其中
  **`campgrade_wave_type_premise_reading`(#480/#241)executor 含本台,仍欠着** ——
  `campgrade` 独占波此刻仍不存在(queue `strategy-43` 明写它「rides the future campgrade
  exclusive admission wave, which does not exist yet」)。另一行 `mock_isprefix_ordering`(#495)
  是否决权不是待办,不归本台。
- `cadence` 1 finding **不是本台**(与历轮同形,7 个历史畸形报告名 `SKIPPED-IN-STREAM`)。

## 二、成本与闸 (iii)

`bash tools/batch_test/aws/check_costs.sh` ⇒ **`COSTS_EXIT=0`**。

- 九月 MTD **`$19.597`**(免费 `budgets` 通道),`budget refreshed 2026-09-05T11:21:28Z`。
  较上一轮 `$18.448` **+$1.149**,是 **W47 的落账滞后,不是新支出**(本台自 W47 起至本轮发波前未起过任何实例)。
- `running/pending instances` 段**发波前为空**;常驻成本仍只有 AMI `ami-0a990a26d89c66547`(2026-07-18),与历轮一致。

闸 (iii) 按 GH #504 **走工具、不抄常数**,并按章程的保守法**两种市场类型各过一次**:

| 估值 | 命令 | 读数 |
|---|---|---|
| spot | `wave_fence.py --planned 0.90` | **`FENCE_SPOT_EXIT=0`**,`WAVE_FENCE: CLEAR`,projected `$20.497`,headroom **`$29.503`** |
| 全程降级按需 | `wave_fence.py --planned 2.15` | **`FENCE_OD_EXIT=0`**,`WAVE_FENCE: CLEAR`,projected `$21.747`,headroom **`$28.253`** |

工具**本轮现读**的操作数:limit `$100.00`、actual `$19.597`、三档 ACTUAL 告警
`$50 / $80 / $100` **全部 NOT yet crossed**、fence **`$50.00`**(最低未跨档)、brake `$90.00`、
**operative ceiling `$50.00`**。
⇒ **本轮未跨任何告警档,owner 不会因本波收到 Budget 告警邮件。**

⭐ **`$0.90` 那条自动失效条款仍然活着而且仍然没被重裁。** W46 账单侧实测 `$0.959–$1.022` > `$0.90`,
按章程「任一波账单侧实测单波成本 > $0.90 ⇒ 本台在该轮报告里点名,**总监下一次触发必须重裁本常数**」
已点名多轮,至今未见重裁。**本轮再次点名**,并照上一轮做法在波次记录里**登记 `~$1.02` 而不是 `$0.90`**
—— 用高的那个数,失效方向朝安全侧。(参考:W47 账单侧实测 `$0.7413`。)

## 三、收割:无对象

`describe-instances` 的 `running,pending` 在发波前**没有任何本台实例**;S3 侧无未收割 run。
W47 上一轮已确认 **4/4 粒完整**(两粒原始 + 两粒按需补跑;gpm `+9.89`;promote/reject 归总监,GH #510)。
⇒ **本轮无收割动作。**

## 四、闸 (ii):三轮来第一次走 (一) 支 —— 串自己动了

上一轮的交棒第 1 条要求「在它变成工具之前,每一轮发波前手做那段算术」(GH #513 仍 `open`,
`tools/batch_test/soak/` 下仍无第三个工具)。**本轮不需要那段算术**,理由是分支变了不是纪律松了:

- 上两轮闸 (ii) 只能靠 **(三) 支**(家族种子数 < 8)或手做的 **(二) 支**(树漂移)撑着,
  因为 **(一) 支是死的** —— 臂串与 W46/W47 **逐字节相同**。
- 本轮 **(一) 支活了**:`iterations/streams/test_set.md` 第 2 行
  **63 id / 559 字节 / md5 `4aefc887f3f8c9173e7ac7024b3c20c9`**,
  对 W46/W47 登记的 **62 / 550 / `c7e1f92c…`**。差的那一个 id 是 **`stayattr`**。
  md5/字节/id 数**本轮重算两次**(闸前一次,发波脚本内再一次),不抄散文。

⚠️ **树漂移本轮明确不声称**:W47 的树 `15b0d9fd`(2026-09-04T17:52:13Z)**早于本容器浅克隆的
graft 点**(main 最早可见 commit `544fdf66`,2026-09-04T19:45:52Z)⇒ `git log 15b0d9fd..HEAD`
在这里**按构造答不了**(exit 128 + 空 stdout,而章程点名这正是**会被读成「无漂移」的危险方向**)。
**没有跑它,也没有把它的沉默当读数。** 它不影响本波:闸 (ii) 由串自己扛,是直接测得的。

## 五、发波:W48(63-id 家族第一波)

**记录全文 `iterations/reports/batch-desk/waves/W48_wave.json`。**

### 5.1 树与串

- `--ref` 钉 **40 位 SHA `54b839d1469eb9a855534644480c8f72399ff7f3`**,不是 `main`。
- **`git ls-remote origin main` 读了两次**:闸时点一次、**发波脚本内第一条命令再一次**
  (`LS_REMOTE_TIP` 与钉住的 SHA 比对,不等就 exit 9 什么都不发)。两次都是 `54b839d1`。
- 臂串取自发波树的 `test_set.md` 第 2 行。

### 5.2 四道闸(逐条真码)

| 闸 | 读数 |
|---|---|
| (i) ≥6h 节流 | **跑了两次**:决定时点 `12:19:28Z` `UNLOCKED` margin `+42421s`;⭐ **动作时点、发波循环前最后一条、脚本内裸 `$?`**:`12:23:57Z` `UNLOCKED` margin `+42690s`,**`THROTTLE_EXIT_AT_ACTION=0`**。锚点 W47 末台 `2026-09-04T18:32:27Z`,解锁 `2026-09-05T00:32:27Z`。本轮余量约 11.9h,**不是压线** |
| (ii) 有新东西可测 | **走 (一) 支**,见第四节 |
| (iii) 围栏 | 两种估值 **`FENCE_SPOT_EXIT=0` / `FENCE_OD_EXIT=0`**,见第二节 |
| (iv) reclaim-blind | **`RB_EXIT=0`** 对 `W47_wave.json`:`not blinded -- 2 paired seed(s) of 4 machine(s); 2 machine(s) reclaimed before the flip;` **`NEXT WAVE: spot (GH #158 unchanged)`** |

附两道内容闸:
- **`CT_EXIT=0`** —— 63 id:13 hero-scoped / 50 generic / **0 unresolved** ⇒ **8 项**,
  `TERMS` 行与 W47 **逐字节相同**。⭐ **`stayattr` 对载体项零贡献**:`carrier_terms.py` 判它
  `kind=generic via bots/FunLib/jmz_func.lua:5116`,没有 `botName` 守卫 ——
  与总监 §ET 裸读源码得到的闸址(`jmz_func.lua:5115-5119`)**一致**。
- **`ARM_EXIT=0`** —— **all 63 armed ids wired on HEAD**,`stayattr` 在内(direct,1 个调用点)。
  ⚠️ **本轮第一次调用误用 `--arm` 返回 `exit 2`;这个工具吃的是 `--cand`。
  `exit 2` = 「没跑成」,**永远不是**「没接线」** —— 与章程记的「调错路径得 exit 2」同一条纪律,
  下一轮直接用 `--cand`。

### 5.3 选种

- ⭐ **`seed_roster_index.py --build` 在选种之前跑**(`ROSTER_EXIT=0`):351 个 run 前缀,
  349 已索引,**2 个新扫**(正是 W47 那两台按需补跑机),索引现 **213 粒种子 / 41 个英雄**。
  **不 build 就会把 W47 刚烧掉的 `4723`/`4763` 当成「从未用过」献回来** —— 章程点名的那个危险方向。
- 窗口**右移**至 **`[5001,5400]`**(W47 在 `4601–5000`),**400 粒全部未用**。
  载体 popcount 分布 `{0:20, 1:106, 2:132, 3:102, 4:31, 5:9}`。
- 对 popcount ≥3 的 **142** 粒穷举,**2506** 组四元解满足「8 项各 ≥2 载体」。
  ⭐ **本轮不紧** —— 窗口的高 popcount 层(4 有 31 粒、5 有 9 粒)**完好**,
  章程说的「每一波都削掉载体最富的那一层」这次还没咬到。
- 平局判据沿 W45/W46/W47 先例、在解空间**内部**取、零成本:**最大化 pudge 载体**。
  选定 **`5025 5037 5058 5344`** —— ⭐ **四粒全带 pudge**(W47 只做到 3 粒),
  `rotscope` 的条件 (a) 帧通道保持可执行(GH #472 / queue `strategy-26`)。
- **`CARRIER_GATE ids=13 seeds=4 exit=0`**(`--assert-carrier-from-arm`,项由串**推导**不手打)。
  逐项载体数:chaos_knight 2 / crystal_maiden 2 / lion 2 / obsidian_destroyer 2 /
  **pudge 4** / skeleton_king 2 / spirit_breaker 2 / zuus 2。

### 5.4 市场与放置

**spot(不传 `--on-demand`,GH #158 owner 拍板的默认)**;采集配置
`--slots 16 --hours 2 --rec-slots 8 --games 12`,4×1 拓扑,**与 W39–W47 逐字节相同**
⇒ 「id 集与采集配置不在同一波一起动」本波满足(本波动的是 id 集)。

⭐ **GH #252 / #256 验收:本轮双双通过,而且是验收写下来以来第一次干净的 4/4。**

| 机 | 种子 | 请求 AZ | 实得 AZ | `InstanceLifecycle` | 实例 | 上机 |
|---|---|---|---|---|---|---|
| 1 | 5025 | us-west-2a | **us-west-2a** | **spot** | `i-02179695e32355408` | 12:23:59Z |
| 2 | 5037 | us-west-2b | **us-west-2b** | **spot** | `i-0c7787ea8a46e43b0` | 12:24:03Z |
| 3 | 5058 | us-west-2c | **us-west-2c** | **spot** | `i-0feb226442eff5fb7` | 12:24:08Z |
| 4 | 5344 | us-west-2d | **us-west-2d** | **spot** | `i-0469876ada96483ca` | 12:24:12Z |

- **四个互不相同的 AZ,每一台都拿到它请求的那一个。**
- **零 `! re-aiming inside the ring` 行、零 `!! AZ RING EXHAUSTED` 行、零 `InsufficientInstanceCapacity`**
  —— 四次调用全部一次成功,**无降级**。(W47 是 3 个 AZ:第 4 台连吃两次容量不足,
  两次改投后落回已有两台的 `us-west-2a`。)
- 市场证据取 **`InstanceLifecycle`**;run_id 的 `spot_` 前缀是 `spot_run.sh` 硬写的字符串,**不是证据**。
- 末台上机 **`12:24:12Z`** = **W49 的闸 (i) 锚点**(GH #469 裁定 ①,取末台);slate spread **13s**。
- 自毁:`spot_run.sh` self-terminate + **2h 看门狗** + one-time spot request(被抢占不会静默重发)。

### 5.5 收尾波次点名(章程第 6 步,发波轮固定两条,零成本)

**必须在发波那一轮做 —— terminated 实例在 API 里只留约 1 小时(GH #375)。**

1. `pending,running` 的 `soak-run` 值 = **恰好四个**,与 `machines[].run_id`
   **一一对上**(`a8dec3` / `4bf01d` / `f634e4` / `272131`):**没有第五台(无泄漏),一台不少(无掉臂)**。
2. `terminated,shutting-down` **为空** ⇒ 本波窗口内**没有** `Server.SpotInstanceTermination`,
   起飞头几分钟没掉臂(W33 那个形状本轮未出现)。

## 六、局数(章程第 7 步,owner 2026-08-19 要求)

**(a) 上一波次(W47)的最终有效局数** —— 上一轮已收割结清,本轮照登不重算:
四粒种子完整(两粒原始 `4678`/`4950` + 两粒按需补跑 `4723`/`4763`);
原始 `4678` `ab28/ba14`、`4950` `ab33/ba23`;被回收作废的两台原始局**不并入**(章程预登记的处置)。
补跑后 W47 四粒齐,62-id 家族(W46+W47)达 8 粒。

**(b) 本轮在跑波次(W48)的实时进度**:**发波型报告,写预期局数。**
预期 **~96 局计分**(4 粒 × 12 局 × 2 腿),外加暖场局 —— **暖场局不算有效局**。
发波时点 **0 局**;按 W42 实测的上传节奏,**头 14 分钟内 `OBJ_COUNT=0` 是预期值不是症状**。

## 七、交给下一棒的东西(都已落在文件里,不靠人记得)

1. ⭐ **`stayattr` 条件 (a) 的那一格**已按总监 §ET 的指定**写进 `W48_wave.json:harvest_obligations`**
   (总监明写它「属于一个 id 和一波,不是常驻义务,所以不进 `owed_executions.json`」)。
   判据逐字钉住:**「留没留下」不是「活没活下来」**;
   **⛔ 禁止拿胜负/存活率给它背书**(诚实边界 4);铁律 4(i-a) ab/ba 两层**读数**分别登记,
   计数类且侧偏未消除 ⇒ 4(i-b) 反号读噪声、4(ii) 不报中位数;
   域为空按 **DOMAIN-NOT-REACHED** 退回,**不许**读成「测过了无效应」。
   ⭐ 离线域价钱已知(109 fixture / 1012 帧:95 帧触发否决,其中 **88 帧**伤害确实来自 3000 内)
   ⇒ **杠杆按构造在约 93% 的域里惰性,波上域小是预期形状不是缺陷**。
2. 同一份记录里另有三条随波限定照旧带上:`campbind` 的共armed 未登记合取(trunk 仍红)、
   `pulldrag` 与 `campbind` **不正交**(不许与 W45 及更早并池)、
   `zusboltdom` 的**反向刹车**((1) 归零必须同时给击杀落地数,(1) 零 + (2) 明显下降 = REJECT)。
3. **并池主张预登记(反拟合,数据落地前写死)**:**W48 不与 W46/W47 并池** ——
   臂串非逐字节相同(63 vs 62,md5 不同),按本台一贯用来为 W46+W47 并池背书的**同一条**臂串读法,
   这是新家族。W48 自成 4 粒,等下一波同 63-id 串来凑。
4. `winrate` 通道**预登记**:已连续 **六**波有数据的波次打 `DEGENERATE`(W41/W42/W44/W45/W46/W47)。
   **若 W48 第七次打 DEGENERATE**,建议不再逐波复议,而是作为「该通道对本 harness
   结构性无信息」的发现交总监。
5. **`$0.90` 常数的自动失效条款仍未被重裁**(第二节)。
6. **下一轮本台** = ① 收割 W48(`recover_verdict.py` 从 S3 逐局全量重算,走 `soak/` 不走
   `validation/`;填 `machines[]` 的 `ab`/`ba`/`arm_depth`/`status_code`,**不许留 null**
   —— 那正是 W45 把闸 (iv) 弄瞎的手法);② 闸 (i) 的锚点是 **`2026-09-05T12:24:12Z`**,
   **但不要读这个数,读 `wave_throttle.py` 打的那一行**(GH #469 立案句就是「手打的时刻会打错」)。

## 八、铁律 6 读数

- 静态门:见下方收尾登记(`GATE_EXIT` / 是否用过 `RULE6_BYPASS`)。
- 动态半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)**未跑,不声称**。
- 本会话对 `bots/` / `game/` **一行未改**(只动 `iterations/` 下的报告、波次记录、queue、章程)。

## 九、本轮 token

见收尾补记。
