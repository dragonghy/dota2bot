# 批测台 2026-09-10T21:18–21:5xZ(零发波轮第二发:围栏仍红,但本轮买到的是**并池没断**)

**一句话**:闸 (i)(iv) 本轮**都绿了**(节流 `+10675s past unlock`、`VERDICT: not blinded` /
`NEXT WAVE: spot`),**唯一拦住波的仍是闸 (iii)** —— `Headroom was $0.647, this wave needs $1.100`。
⭐⭐⭐ 头号发现**不是**"又没发成波",而是零成本查出来的一件本来会**悄悄毁掉深度**的事:
main 自 W63 的钉以来**多了一处 ungated 的行为改动**(`tombhp` 的崩溃修复,作者自己在注释里
逐字声明"**NOT gated**"),按"同一个可达程序才可并池"的字面判据,下一波**本该与 W62+W63 断池、
家族深度 7 → 0**;而实测下来它**不断池** —— 因为那条改动的**外层守卫要求敌方有 Undying,
而 Undying 根本不在 41 英雄的抽签池里**(`hero_pool.txt` grep rc=1)。
⇒ **可达性可以由抽签池关闭,不是只能由 gate 关闭** —— 这条方法本轮第一次被用上。

---

## 〇、本轮定位

上一轮(18:14Z)交棒逐字:"**若 `FENCE_EXIT=3` 仍在,九月剩余每一轮都是零发波轮**
(除非 GH #721 三条路之一落定),那种轮次的正确产出是**收割(若欠)+ 泄漏检查 +
把 #721 往前推一格**,**不是每两小时重跑一次同一道红闸**"。

⇒ 本轮**照办**,并额外交出两件零成本产物(§四、§五)。**本轮零 EC2 支出**;
总支出 **`$0.02`**(`check_costs.sh` 自动的 `$0.01` CE 复核 + §二 主动拉的 `$0.01` 逐日 × 逐 `USAGE_TYPE`)。

⚠️ **上一轮交棒 ② 逐字要求"先跑 `wave_fence.py` 再跑 `wave_throttle.py`"**,理由是
"闸 (i) 会自己变绿,闸 (iii) 不会"。**本轮没有按这个顺序**(先跑了 (iv) 再 (ii) 再 (iii) 再 (i)),
**登记为一次未照办**:代价是零(闸 (i) 早已解锁 3 小时,没有产生任何等待),
但那条建议的立法理由本轮仍然成立,下一轮照它走。

---

## 一、成本(当轮现跑,上一轮数字全部作废)

- `AWS_SETUP_EXIT=0`,身份 `arn:aws:iam::581436534924:user/dota2bot-agent`(受限 IAM,正确)。
- `COST_EXIT=0`,MTD **`$78.253`**(免费 `budgets`,`budget refreshed 2026-09-10T19:46:38Z`),
  脚本按 `≥$35` 自动花 `$0.01` CE 复核 **`78.2534452025`**,**两源逐位一致**。
- `forecast 182.287` / `budget limit 100.0` —— **原样登记,本台不据 forecast 决策**。
- ⭐ **快照本轮又动了**:上一轮 `14:18:09Z` → 本轮 `19:46:38Z`,MTD `$77.847 → $78.253`,
  涨 **`$0.406`**。⛔ **不得读成"W63 花了 $0.406"**:诚实 cutoff = `19:46:38Z − 11.3h`
  = **`2026-09-10T08:28:38Z`**,W63 首台 `12:18:38Z` 远在其后 ⇒ **整波仍一分钱未落账**
  (§二 的逐日拆解独立佐证:09-10 当天 `c6i` 行 **`$0.0000`**)。这 `$0.406` 是别的东西,不可拆。

---

## 二、⭐⭐ 外来支出:本轮**第一手**复查,金额五天一分未涨

上两轮这件事一轮是第一手、一轮"未再复查(不花第二次 CE)"。本轮**花 `$0.01` 重新拉**
09-01→09-10 逐日 × 逐 `USAGE_TYPE`,**不是转载**:

| usage type | 月度 | 归属 |
|---|---|---|
| `USW2-SpotUsage:c7a.16xlarge` | `$23.2805` | **外来** |
| `USW2-BoxUsage:c6i.4xlarge` | `$18.9505` | 农场(按需) |
| `USW2-SpotUsage:r7a.16xlarge` | `$14.1550` | **外来** |
| `USW2-SpotUsage:c6i.4xlarge` | `$13.0702` | 农场(spot) |
| 其余(EBS/传输/IPv4/HostedZone/Requests…) | `$8.7972` | 共摊 |

**农场自己(`c6i`/`c6a`)= `$32.0207`;外来(`c7a`/`r7a`)= `$37.4355` = MTD 的 47.8%。**

⭐ **两件本轮才敢说的话**:

1. **金额与 09-08 那轮记的 `$37.4355` 逐位相同** ⇒ **五天一分未涨**,不是正在扩大的洞。
2. **逐日看,它只存在于两天**:09-05 `$21.6331`、09-06 `$15.8024`,
   **其余八天(含最近四天)全部 `$0.0000`** —— 这比"月度总额没变"强,
   因为总额不变也可能是"涨了又退款"。

⚠️ **归因边界**:本台能证明的是**机型不是农场用的**(W1–W63 每条发波记录只有 `c6i.4xlarge`,
界前偶有 `c6a`),**不能**证明是谁跑的 —— `dota2bot-agent` 读不到别人的资源标签。
`final-table-trainer` / `fft-cache-v1-attempt3` 这些名字**本轮没有再看见**,不重复主张。

⇒ **决策相关的算术(交 owner,GH #721 已开,本轮不重复开)**:
扣掉外来那笔,本台自己的 MTD 约 **`$40.82`**,到 `$80` 还有 **`$39.18`** ≈ **35 波 spot**。
**含着它,余量 `$0.647`,一波都发不出。**

---

## 三、四道闸(全部当轮现跑,读数不抄)

| 闸 | 退出码 | 逐字关键行 |
|---|---|---|
| **(iv)** 致盲 | **`RECLAIM_BLIND_EXIT=0`** | `yield : 3 paired seed(s) of 4 machine(s)` / `VERDICT: not blinded -- the wave delivered 3 paired seed(s).` / **`NEXT WAVE: spot (the default; GH #158 unchanged).`** |
| **(ii)** 有无新东西可测 | 见 §四 | **两条腿都不成立 ⇒ NOTHING NEW**(与 W63 同型) |
| **(iii)** 围栏 | **`FENCE_EXIT=3`** | `WAVE_FENCE: THROTTLED (exit 3) -- do not launch. Headroom was $0.647, this wave needs $1.100.` |
| **(i)** 节流 | **`THROTTLE_EXIT=0`** | `anchor wave : W63 (4 machine(s))` / `unlock : 2026-09-10T18:18:45Z` / `now : 2026-09-10T21:16:40Z` / `margin : +10675s past unlock` / `UNLOCKED (exit 0)` |

⭐ **闸 (iv) 本轮走的是材料性路径(GH #699 RULING 8 的落地效果),四台 `status_code` 齐全**
(上一轮 §八 把回填做掉了)。逐字保留它自己的限定:
`immaterial : all 16 extension point(s) agree, so the verdict below does not rest on the unread field(s). A gate may not demand a datum its own answer does not depend on (GH #699).`
⚠️ 四台 `survival` 字段仍**未读**(`unread : 4 of 4 machine(s)`),
`attribution` 因此只给区间 `0..1 machine(s) reclaimed before the flip` —— **登记,不当缺陷**:
判决本身不依赖它,这正是 RULING 8 要的形状。

**闸 (iii) 完整读数**(⛔ 下一轮不许抄,围栏是时间的函数):
`budget filters : none <- so every instance in the account does land in this budget, whoever owns it` /
`accrual scope : 17 region(s) read ... <- COMPLETE: every region this account has enabled` /
`wave accrual : records after 2026-09-10T08:28:38Z (= 2026-09-10T19:46:38Z - 11.3h lag, clock from budget snapshot)` /
`un-accrued? : W63 last machine up 2026-09-10T12:18:45Z <- inside the lag window` /
`accrual check : CERTIFIED ZERO (0 accruing instances account-wide), but --pending $1.100 was given and is used as the larger claim.` /
`must cover : 1 wave(s) listed above (W63)` /
`actual (MTD) $78.253` / `pending waves $1.100` / `planned $1.100` / `projected total $80.453` /
`fence $80.00` / `brake $90.00` / `operative ceiling $80.00`。
**未用 `--no-accrual-check`** ⇒ 无 "SKIPPED, not certified" 行。**未用 `--director-crossing`** ——
跨 `$80` 需总监**当轮**明确裁定,**本台不预支**。

**`--pending` 自推导(不抄工具的 `must cover`,但与它核对)**:快照 `19:46:38Z`,
诚实 cutoff `08:28:38Z`;W63 末台 `12:18:45Z` 在其后 ⇒ 未落账 ⇒ 按 **spot 常数 `$1.10`** 计;
W62 末台 `2026-09-09T21:24:41Z` 在 cutoff 之前 ⇒ 已落账 ⇒ 不计。合计 `$1.100`。
**工具独立点名 `1 wave(s) (W63)`,两者一致。**

---

## 四、⭐⭐⭐ 闸 (ii):两条腿都不成立,而**第二条腿差一点被判错**

**腿 1(臂串)—— 没变。** `origin/main:test_set.md` 第 2 行当轮现算:
**37 id / 335 字节 / md5 `b525d51d4b4957e0e40f22f203aea641`**,与 W63 发波时**逐位相同**
(⇒ 与 W60/W61/W62 也相同)。**零准入、零撤出。**

**腿 2(树)—— 变了,但可达程序没变。** 钉 `cfa966e3…` → `origin/main` `6219d4b5…`,
`bots/`+`game/` **4 文件 +274/−1**。逐 hunk 读**源码**(不是读 diff)后:

| # | 改动 | 门 | 在 37-id 串下 |
|---|---|---|---|
| a | `hero_lion.lua` `lion_IsFieldImpaleEngagementOk` + 调用点 | `IsSoakCandidate('lionqfight')` | **未 armed**;未 armed 时函数 `return true` ⇒ 调用点 `and true` **恒等** |
| b | `hero_zuus.lua` `zuus_ArcExecuteFinishes` + 调用点 | `IsSoakCandidate('zusarcexec')` | **未 armed**,同上 |
| c | `aba_special_units.lua` 多英雄扫描 | `IsSoakCandidate('anyhero')` | **未 armed**,整块不进 |
| d | `mode_roam_generic.lua:1961` | **半条未 gate** | ⭐ 见下 |

**四个新 id `lionqfight` / `zusarcexec` / `anyhero` / `tombhp` 与臂串的交集 = 空集**(程序化求交)。
⭐ **整份 `bots/`+`game/` 差异里只有 1 行删除,就是 (d)**;其余全是纯新增。

**(d) 才是本节的正题,而它差一点被判成"断池"。** 作者在注释里**主动、诚实地**声明这半条不受门约束:

> `* removing the CRASH is unconditional and is NOT gated -- the gate short-circuits before J.GetHP is ever called, so the unarmed leg is crash-free too. It is deliberately not byte-identical to shipped;`

逐字核对源码(**不是从 diff 猜的**):旧行 `J.GetHP(nInRangeEnemy)` 把**数组**交给
`J.GetHP`,而 `jmz_func.lua:4140-4141` 的第一条语句是 `local nCurHealth = unit:GetHealth()`;
`nInRangeEnemy` 在本文件 `:67`/`:468` 由 `bot:GetNearbyHeroes(1200, true, BOT_MODE_NONE)` 赋值,
全文件其它读者一律按数组用(`#`、`[1]`、`pairs`)⇒ 旧行**若被执行必然抛错**并中断
`ConsiderGeneralRoamingInConditions`;新行把 `IsSoakCandidate('tombhp')` 排在 `J.GetHP` **之前**,
未 armed 时短路 ⇒ 不再抛错。**⇒ 按字面,新旧两棵树的可达程序不同 ⇒ 与 W62/W63 断池,
家族深度 7 → 0。**

⭐⭐⭐ **但它不断池,而理由在抽签池里不在代码里。** 该分支的外层守卫是
`if bot:HasModifier("modifier_undying_tombstone_zombie_deathstrike_slow") then`
—— 这个 modifier 只能由**敌方 Undying 的墓碑僵尸**施加。而:

- `tools/batch_test/soak/hero_pool.txt` **41 行,`grep -in undying` 退出码 1(零命中)**;
- `seed_draft.py --rates --scan 400` 列出 **41 个英雄,`grep -c undying` = 0**;
- W62 四粒 + W63 四粒 **共 8 份 10 人阵容全部展开,Undying 出现 0 次**(逐粒打印在案)。

⇒ **抽签池里没有 Undying ⇒ 该 modifier 在本语料任何一局都不可能出现 ⇒ 外层守卫恒假 ⇒
(d) 的两条腿(旧树的崩溃、新树的短路)在每一局里都不可达** ⇒ **新旧树在本语料上是同一个可达程序**
⇒ **可并池,家族深度保住 7 粒 / 329 局**(下一波若配对四粒可到 11 粒)。

⚠️ **三条诚实边界**:
1. **这条论证锚在 `hero_pool.txt` 上,不锚在代码上。** 哪一轮往池子里加了 Undying,
   **这条结论当场失效**,而且**没有任何东西会举手** —— 与本章程反复点名的"静默失效"同族。
   ⇒ 建议把它做成一条断言(见交棒 ②)。
2. 作者注释里写的是"**the OUTER guard ... has none [rows] in this corpus**"(**没测到**);
   本节主张的是**按抽签池构造不可能**(**测不到**)。**后者严格强于前者**,而两者结论一致
   —— 但它们不是同一句话,不要互相引用。
3. ⛔ **本节不主张 (d) 这条修复是对的或错的**,也不碰 `tombhp` 的入集与否
   (作者已自记 FROZEN-HOLD,`state.json:tombhp_20260910`)。**本台只判"能不能并池"。**

⇒ **闸 (ii) 结论:NOTHING NEW TO TEST。** 下一波仍是**纯买深度**的复制波
(与 W63 同型),**不是新 lever 的独立重复**,收割轮不得那样写。

---

## 五、两根**免费**的棒本轮结清(上一轮 selfcheck 点名 OWED)

`owed_executions.json` 里挂在 `executor=batch-desk` 且**不需要发波**的两根,由**同一次**
`wave_fence.py` 活跑同时满足:

1. **`gh692_epoch_clock_first_live_read`(GH #692 缺陷 + GH #693 政策)** ——
   结清判据钉在逐字串 **`clock from budget snapshot`**(立行当日在 `iterations/` 下 0 次)。
   本轮活跑打出:
   `wave accrual : records after 2026-09-10T08:28:38Z (= 2026-09-10T19:46:38Z - 11.3h lag, clock from budget snapshot)`
   ⭐ **要点是"不是 `now`"**:窗口右端锚在 `budget refreshed 19:46:38Z` 上,而当轮 `now` 是
   `21:16Z`,**差 1.5h** ⇒ §GL.2 那条"设计成罕见、落地当天却是 100%"的降级腿**本轮没触发**。
   已把读数写进 **`test_set.md §GL.8`**(判据检查器读的就是那个文件)。
   ⚠️ 写的时候**显式标注"这是下游抄回来的读数,不是上游写下的判据"** —— §GL.6 最后一条
   点名 `path_contains_all` 区分不了这两者,**那个盲区没被本轮修好,只是没被本轮利用**。

2. **`wave_fence_ruling6_first_live_read`(GH #683,`kind=manual`)** ——
   判据钉在 RULING 6 之前**不存在**的新前缀 **`wave accrual :`**,本轮同一次运行打出(见上)。
   配套两行也在:`un-accrued? : W63 last machine up ... inside the lag window` /
   `must cover : 1 wave(s) listed above (W63)`。**该行是 manual,本台交读数,结清归总监。**

⛔ **同族第三根 `gh696_winrate_measurable_first_live_read` 本轮不结清且不能结清**:
它钉在 `recover_verdict.py` 的 stderr 新前缀上,**只有收割轮跑得出来**,而本轮无波可收。
**这不是掉棒**,登记表自己已写明"大概率要等到 10-01 之后"。

---

## 六、收割与泄漏

- **收割:零欠。** W63 已由 15:14Z 那轮全量收割(`W63_verdict.json`,3 粒配对 / 126 局),
  `machines[]` 零 null。本轮**无新波、无新 verdict**,`validation/` 未新增。
- **泄漏:零,两条独立路径 + 一条账户级。**
  带状态过滤(`pending,running,stopping,stopped`)**空**;
  不带状态过滤(只排除 `terminated`)**空**;
  ⭐ 另有闸 (iii) 自带的账户级普查 `accrual scope : 17 region(s) read <- COMPLETE`
  + `CERTIFIED ZERO (0 accruing instances account-wide)` ——
  ⇒ 本轮**买到了 17 区账户级零**,不只是 us-west-2 零(上一轮只能声称后者)。
- 常设成本仍只有 AMI `ami-0a990a26d89c66547`(`dota2bot-batch-20260718`)。

---

## 七、⚠️ 章程"当前状态"节被劈成了两段,而上一轮的条目落在读不到的那一段

**现象**:`batch-desk.md` 有两个 `## ` 节承载轮次条目 ——
`## 当前状态`(861–9614)与其后的 `## 波次开关策略`(9615–EOF)。
**最近 47 条轮次条目(09-04 → 09-10)全部落在 `波次开关策略` 里**(即文件末尾),
而 **18:14Z 那一轮的条目落在 `当前状态` 的末尾(行 9546)**,
它前面一条是 **09-07T03:05**,后面隔着 900 多行才是 09-04 起的另一条流。

**为什么这是缺陷不是洁癖**:本台开工按章程读"章程末尾的当前状态节",
读到的最后一条是 **15:14Z**,**18:14Z 那一轮整轮不可见**。
本轮因此**差一点重复开 GH #721**(那正是 18:14Z 已经开掉的 issue),
靠的是收尾时顺手 `ls reports/` 看见一个没读过的文件名 —— **不是靠任何机制**。

⭐ 更贵的一层:`routine_selfcheck.sh` 的 cadence/admission 腿明确区分
"**live `当前状态` block**"与"outside ... (history -- not a finding)",
逐字打 `no expired admission wait in any live 当前状态 block (6 charter(s), 55 settled id(s))`
⇒ **落在 `波次开关策略` 里的那 47 条(含本台最近全部轮次)对该腿而言是"历史"**,
本台的活状态**结构上不在它的检查范围内**。

⚠️ **本台不擅自重排 1.8MB 的章程**(节序调整影响所有读者,且 `当前状态` 与
`波次开关策略` 谁该在前是总监的编排权)。**本轮的处置**:条目仍写在**文件末尾**
(与最近 47 条一致,保证下一轮读 tail 能接上),并把本节交总监(交棒 ①)。

---

## 八、铁律 6 三条腿(GH #624)

见 §十一。

## 九、自检(铁律 10)

⚠️ **第 12 次踩同一个管道**:本轮**第一条命令**仍是
`bash tools/agent/routine_selfcheck.sh 2>&1 | tail -40`,脚本自己 `REFUSED` 并打 `exit 2`,
逐字:
`REFUSED: stdout is a PIPE, so the exit code your harness reports is the READER's (tail/head almost always succeed), not this script's. This is evidence discipline 3; it has recurred 5x, every time as the first command of the round.`
⭐ **那道拒绝再一次起了作用**(把假读数挡在外面),但**连续 12 轮同一位置**说明
防的是症状不是原因 —— 上一轮交棒 ⑤ 已第 13 次提醒,**仍然没防住**。

**改用重定向后**:第一次 `timeout 400` 太短,`SELFCHECK_EXIT=124`(死在
`=== trunk health (python test suite) ===` 一节里)。第二次改后台 + `timeout 900` 重跑,
**到本报告收尾时仍未跑完那一节** ⇒ ⛔ **本轮不报 `SELFCHECK_EXIT=`**:
**没读到的退出码不写**(这是上一轮 §十一之一 自己立的规矩)。

**已跑完并读到的部分逐字登记**(全部在 python 测试腿之前):
- `no expired admission wait in any live 当前状态 block (6 charter(s), 55 settled id(s))`
- stable 锚 6 个全绿:`stable-v1`…`stable-v6`,各 `EXISTS ok` / `PINNED ok` / `SHIPPED ok`,
  收尾 `6 anchor(s) checked -- OK`
- `scanned: 195 distinct live gate id(s), 18 id(s) carrying a PROMOTED note` /
  **`FROZEN none (no live gate names a promoted id)`** / `atoms registered: 5` /
  五条 atom 全 `ok` / `promote-atom constraints: OK`
  ⇒ **`pullcad` 那个陷阱本轮为零**。
- cadence 腿:`GAP cadence director`(×2)、`GAP cadence hero` ⇒ **本台无 GAP**。
- owed-executions 腿:本台名下四根,处置见 §五。

**未读到 ⇒ 不声称**:python 腿与 Lua 腿本轮**没有结论**,
⛔ 不得据本节推断 trunk 是绿的(上一轮 12:14Z / 15:14Z 两轮都报过 trunk 两侧有红,
`test_bots_walk_farm_only.py`、`test_focus_mana_cost_consumer_census.lua`、
`test_lion_ult_reserve_domain.lua`,GH #705 一族)。**本会话 `bots`/`game`/`tests`/`tools`
一行未改** ⇒ **无一条可能由本轮引入**。

---

## 十、本轮已发表(push 之后再发,GH #290 顺序)

见 §十二"交棒"末尾登记。

## 十一、铁律 6 三条腿读数

见本报告随附的章程"当前状态"条目(与 push 同轮登记)。

## 十二、交棒

1. ⭐⭐⭐ **总监 —— 章程"当前状态"节被劈成两段,本台活状态落在 selfcheck 眼里的"历史"区**(§七)。
   请裁两件:(a) 轮次条目该写在哪一节(本台按"文件末尾"执行,与最近 47 条一致);
   (b) `波次开关策略` 该不该移到 `当前状态` 之前,让"章程末尾 = 最新状态"这条读法重新成立。
   ⚠️ 具体代价已量:本轮**差一点重复开 GH #721**。
2. ⭐⭐⭐ **harness/总监 —— 把"Undying 不在抽签池里"做成断言**(§四 边界 1)。
   本轮的并池结论(深度 7 保住)完全依赖 `hero_pool.txt` 不含 `undying`;
   往池里加它的那一轮**没有任何东西会举手**,而那一刻 `mode_roam_generic.lua:1961`
   的 `tombhp` 分支会同时(i) 变成可达、(ii) 让旧钉的波与新波静默断池。
   建议:`tests/` 加一条断言,把"池子成员"与"被判定为池外不可达的守卫"绑在一起。
3. ⭐⭐ **总监 —— 闸 (ii) 连续第二轮 NOTHING NEW,而这一次是"纯买深度"有明确收益**(§四)。
   GH #663(复制波到底可不可以发)仍未裁;本轮给它一个新论据:
   **W64 若能发,家族深度 7 → 11**,而这正是本台连续多轮点名缺的那个量。
   ⛔ 但本轮它是**假设句**:围栏先把它拦住了。
4. ⭐⭐ **owner/总监 —— GH #721 三条路仍未落定,本轮给它第一手复查**(§二):
   外来 `$37.4355` **五天一分未涨**,且**只存在于 09-05/09-06 两天**,其余八天为零。
   ⇒ 它**不是正在扩大的洞**,但**仍占着 47.8% 的围栏**;扣掉它本台还有 ≈35 波 spot 的余量。
   **本台不擅改任何一条线**,已按 Routine 规程推给 owner。
5. ⭐ **总监 —— 两根免费的棒本轮结清**(§五):`gh692_epoch_clock_first_live_read`
   读数已写进 `test_set.md §GL.8`;`wave_fence_ruling6_first_live_read`(manual)读数在本报告。
   第三根 `gh696_...` **结构上要等收割轮**,登记在案不算掉棒。
6. ⚠️ **自检管道坑第 12 次**(§九),且本轮**没有** `SELFCHECK_EXIT=`(python 腿未跑完)。
7. ⚠️ **上一轮交棒 ② 的闸序建议本轮未照办**(§〇),代价为零,但下一轮照它走。

## 下一轮本台

1. **大概率仍是零发波轮**:围栏只会随 MTD 单调收紧,**不会自愈**;
   九月剩余每一轮都该按 18:14Z 那条交棒执行(收割若欠 + 泄漏 + 推 #721),
   **不是每两小时重跑一次同一道红闸**。
2. ⛔ **围栏/闸/成本一律当轮现跑** —— `$78.253` / `$0.647` / `$1.100` / cutoff `08:28:38Z`
   **全部作废**。
3. ⭐ **闸序按上一轮建议**:先 `wave_fence.py`(不会自愈的那道)再 `wave_throttle.py`。
4. ⭐ **若总监解禁跨 `$80`**:市场按闸 (iv) 本轮所裁 = **spot**;并池主张见 §四
   (⚠️ 三处可达性要在**发波那一刻的新钉**上重跑,`hero_pool.txt` 也要重新 grep)。
5. ⚠️ **开工第一条命令重定向 + 后台 + 不设短 `timeout`**(第 14 次提醒)。

---

## token 用量

见文末追加(收尾时 `python3 tools/agent/token_usage.py`)。

```
TOKENS total_in=6,026,974 out=41,777 turns=48
```
(统计时刻为止;其后的收尾回合不计入。)

---

## 补记(第一次 push 之后,GH #290 顺序)

### 补记 A — 铁律 6 三条腿(两次 push 读数相同,GH #624 逐字)

```
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 95 ran, 0 findings, 0 uncertifiable, 31.6s   (第二次 push: 30.5s)
lua gate: SKIPPED BY SCOPE -- this push touches no bots/game/tests path.
  This is a SCOPE decision, not a pass and not a skip of a gate that had something to say (GH #624).
```

⚠️ **第三条腿是 `SKIPPED BY SCOPE` 不是绿**,照抄不改写。
`arm_push_gate.sh` 本容器 `ARM_EXIT=0`(逐字 `push gate armed (core.hooksPath=.githooks, already set)`);
**未用 `RULE6_BYPASS`** ⇒ 无「SKIPPED, not passed」行。
`PUSH1_EXIT=0`(`claude/great-sagan-c64j1n`)/ `PUSH2_EXIT=0`(`HEAD:main`,`6219d4b5..ff8fcc36`)。

### 补记 B — 自检真码到手:`SELFCHECK_EXIT=124`,**不是 0 也不是 3**

后台那次(`timeout 900`)**也被超时杀掉**,`SELFCHECK_EXIT=124`。
⛔ **124 不是自检的判决,是容器的判决** —— 它在 `=== trunk health (fast Lua detectors) ===`
一节里被杀,**没有打出收尾横幅**(`legs run` / `FINDINGS` / `selfcheck worst exit` 三行都没有)。
⇒ **本轮没有 `selfcheck worst exit`,不写。** 但它比第一次(`timeout 400`)多跑完两条腿,
下面是**真读到的**部分:

**python 腿(`TRUNK RED`)**:逐字 `122 passed, 2 failed, 1 uncertifiable` /
`failed: tests/test_bots_walk_farm_only.py tests/test_py_gate.py`。
⭐ **`test_py_gate.py` 是今天新出现的红**(12:14Z / 15:14Z 两轮都只有 `test_bots_walk_farm_only.py`),
失败条目逐字 `5c: the selected total (12.03s) fits the recorded budget (12.0s)`;
而 `test_bots_walk_farm_only.py` 那条**长大了**,现在点名
`tests/test_anyhero_first_member_quantifier.lua` 与 `tests/test_lion_q_field_engagement.lua`
—— 与今天落地的两个新 soak id(`anyhero` / `lionqfight`)对得上。
⛔ 照抄该腿自己的限定:`Whether main is red too is NOT established by this line: re-run after git stash.`
另 9 条 `UNCERTIFIABLE`(`5a0`–`5g`,`test_selfcheck_lua_leg.py` 120s 内没跑完),
收尾横幅逐字 `UNCERTIFIABLE -- 9 check(s) did not run; this is NOT a pass.`(GH #358 同族)。

**Lua 腿(两条 `RED`,均属已报过的 GH #705 一族,本轮不重复立案)**:
`test_focus_mana_cost_consumer_census.lua:424`(`axe: … expected n=26 …, got n=27`)+ `:471`
(`the control must sweep the same 173 frames, saw 176`);
`test_lion_ult_reserve_domain.lua:367`(⭐ **`7 rank-2 Finger instants`,15:14Z 那轮还是 `4`** ——
同一条断言的计数**今天之内又动了一次**)+ `:512`(最小池实例换成
`tests/frames/f_260909_215412_axe_cull_cm_838.lua`)。

⛔ **归因**:本会话 `bots`/`game`/`tests`/`tools` **一行未改** ⇒ **无一条由本轮引入**;
形状仍是「把语料规模写死成等式」那一族,**相关不是归因**,交总监。

⇒ **本报告 §八「本轮无 `SELFCHECK_EXIT=`」由本补记取代**:真码是 **124**,
且**它说的是容器跑不完,不是 trunk 干净**。

### 补记 C — 本轮已发表(全部 push 之后,三份草稿各过 `claim_precheck.sh`)

`PRECHECK1_EXIT=0` / `PRECHECK2_EXIT=0` / `PRECHECK3_EXIT=0`,
三份各打 `clean` / `OK to publish: every citation resolves on origin/main.`

- **GH #726 `[harness]`** —— 把「Undying 不在抽签池里」做成断言(§四 边界 1)。
- **GH #727 `[batch]`** —— 章程「当前状态」被劈成两段,47 条轮次条目落在 selfcheck 的「历史」区(§七)。
- **GH #728 `[harness]`** —— `tests/test_py_gate.py` trunk 红(12.03s > 12.0s),闸自己看不见(补记 B)。
- **GH #721 追评** —— 外来支出第一手复查:`$37.4355` 五天未涨、只存在于 09-05/09-06 两天;
  围栏 `Headroom $0.647`,三条路仍未落定(§二)。**不开重复 issue。**

⇒ 交棒 ①②④ 已各自落到 #727 / #726 / #721;交棒 ⑥ 的形状由 #728 承接。
