# 批测台 — 2026-08-28T03:13Z

**一句话**:W19 四台被回收三台,净买到 **1 粒形态正常的种子**;而那粒种子四个量两层全反号 ⇒
**本波读不出任何结论**。收割中发现 `scored` 门**没有最小臂深**(种子 928 以 `ba_games=1` 通过),
它把波均值挪了 **76.5 gpm** 而什么都不露 ⇒ **新立案 GH #269**。已发 **W19-R** 补三粒种子。

---

## 1. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` — **worst exit 3**(有发现,非阻断):

| 腿 | 结果 |
|---|---|
| push gate 上膛 | ok(`core.hooksPath=.githooks`) |
| unlanded work | **OK**(certifiable 窗口内无未落地;shallow ⇒ 433/477 refs REFUSED,照旧) |
| 两个 stable 锚点 | **ok**(`stable-v1` 6db5921c5945 / `stable-v2` 136a332fdea4,SHIPPED 均 ok) |
| cadence | **1 处洞:`replay-check` 07:19Z→13:28Z 6.1h** —— **连续第六轮同一处同一值**,不是本台的 |
| 未裁队列 | RIDESHARE **none** / OTHER **none** |
| trunk python | **46 passed / 0 failed**(上一轮 45,**+1**) |
| 快 Lua 检测器 | **15 个文件 0 失败**(与上一轮持平) |

⇒ 上一轮交棒项④(trunk 保持全绿)**继续全绿**。

## 2. 成本

- `budgets` MTD **$50.671**;CE 复核 **$50.6709276212** ⇒ **逐位一致,连续第十三轮**。
- forecast **76.999** / limit **100.0**。
- ⚠️ 快照时刻 **`2026-08-26T23:46:06Z`** —— **与上四轮同一张、逐位未动**。
  ⇒ W15-R 起(含 W18、W18-R、W19)**一台都没进去**,滞后照旧朝**危险侧**失效(§2 甲)。
- **围栏算术**(逐波按各自市场类型取价):
  `50.671 + 1.69 + 0.80 + 0.190 + 0.479 + 0.313 + 0.613(W18) + 1.25(W18-R) + **0.62(W19 实测)** = **$56.63**`
  `+ W19-R spot 预估 0.70 = **$57.33**`。
  **全程降级按需的悲观估值**(3 × 0.92h × $0.673 = $1.86)⇒ **$58.49**,**两种估值都过闸**(§2 甲的对价)。
- **⭐ 闸 (iii) 本轮起是 $80**(总监 2026-08-28T00:5xZ 落地,§4b(iii) 改写)。
  ⇒ **上一轮交棒项④「$60 最迟下下轮咬人」已由总监解决**,且解法不是换个数:
  围栏改绑 **下一个尚未跨过的 owner 可见 Budget ACTUAL 告警档**($50 已跨,下一档 **$80**)。
  本轮余量 $80 − $57.33 = **$22.67**,按 spot 单波 ~$0.7 计还有 **~30 波**,
  **(iii) 不再是近期的约束**。
- **无新增跨档**(MTD 逐位未动)。刹车线 $90 未接近。

## 3. 收割 W19

### 3.1 先查 SIR(照做上一轮的前置动作)

| 种子 | AZ | SIR 状态 | 读法 |
|---|---|---|---|
| 928 | us-west-2a | `instance-terminated-no-capacity` | **回收** |
| 930 | us-west-2c | `instance-terminated-no-capacity` | **回收** |
| 932 | us-west-2c | `instance-terminated-no-capacity` | **回收** |
| 935 | us-west-2d | `instance-terminated-by-user` | **跑完自终** |

⇒ **四台回收三台**。#239 路 4 **第六次实证**(spot 波才有 SIR 这条通道)。

**⭐ 一条要更正的默认**:回收横跨 **2a 与 2c×2**,**2b 反而没被碰**。
⇒ 「`2b` 容量差」那个印象(W17/W17-R/W18 三波留下的)**本波被证伪**;
真实形状是 **us-west-2 的 `c6i.4xlarge` spot 容量整体紧张,不是某个 AZ 的属性**。
这**支持**总监 2026-08-27T19:xxZ「不把 `2b` 移出 `AZ_LIST`」的裁定 —— 移出会白白丢掉一路分散,
而本波恰好是 `2b` 活下来。
⚠️ 同时它**削弱** #252 的「一次 AZ 事件最多丢 1 粒」上界:**跨 AZ 的同时回收是真实事件**,
四路分散压的是**相关性**不是**总量**。

### 3.2 逐粒读数(`recover_verdict.py` 全量重算,每 run 独立子目录 → 指向父目录,**未传 `--cand-ref`**)

arm 串反解 = **40 id / 354 字节**,与发波登记逐字一致 ✅;树 `3110f323`。

| 种子 | ab | ba | gpm | 形态 |
|---|---|---|---|---|
| 928 | 41 | **1** | −176.4 | ⚠️ **薄臂**(见 §3.3) |
| 932 | 27 | **0** | — | 单腿孤儿,不入池 |
| 935 | 38 | 15 | −23.4 | **唯一形态正常** |

census:`files_seen 139 / games_loaded 139 / source_dirs 3 / unparseable 0`、`scored 95`、`unfinished 0`。
(第四个 run `…_6c47b6` = 种子 930,S3 上**只有 1 个对象、0 个 analysis.json** ⇒ 起飞 9 分钟即被回收,空目录已剔除。)

### 3.3 ⭐⭐ 本轮的新发现:`scored` 门没有最小臂深 ⇒ **GH #269**

`recover_verdict.py:216` 判可计分的条件是 **`if AB and BA`**,**没有最小局数要求**;
而 `:220` 的 `row[m] = (ab + ba) / 2` 给两条腿**各 50% 权重**。
⇒ 种子 928 以 **ab41 / ba1** 通过了门,**一局 dire 决定了该粒读数的一半**。

**代价可计量**:

| 池 | gpm |
|---|---|
| 只用形态正常的 935 | **−23.4** |
| 加入 928 | **−99.9** |

**⇒ 一粒 `ba=1` 把波均值挪了 −76.5 gpm,而 `suggested` 照常印 `hold_or_reject`。**

**这是已登记同族的第三个成员,三个都朝危险方向失效**(照常出数、不举手):
W14 跨 run basename 碰撞(已由 #225 R1/R2 堵住)→ W17-R「`per_seed` 非空 ≠ 有可用种子」→ **本条「`scored` ≠ 可用种子」**。
本条更隐蔽:前两条至少让 `mean` 变 `{}` 或让 census 露馅,而 **`ba=1` 什么都不露**
(`scored_games` 42 看着正常、`unfinished` 0、`per_seed` 非空、`mean` 有值)——
**唯一能看出问题的字段是 `ba_games` 本身**。

**本轮读法**:按建议 (A) 的精神执行,**结论不引用 −99.9**。三个选项已写进 #269 请总监裁。

### 3.4 铁律 4(i):两层都给 ⇒ **本波读不出结论**

| 种子 | 量 | ab 层 | ba 层 | 同号? |
|---|---|---|---|---|
| **935**(唯一形态正常) | gpm | **−244.72** | **+197.92** | ❌ |
| | xpm | −130.15 | +102.27 | ❌ |
| | deaths | +2.33 | −2.04 | ❌ |
| | last_hits | +8.12 | −12.69 | ❌ |
| 928(薄臂,ba=1) | gpm | −176.61 | −176.20 | (同号但 **n=1**,不作证) |
| 932(孤儿) | gpm | +164.08 | NA | — |

**⇒ 唯一形态正常的种子上,四个量全部两层反号 ⇒ 按铁律 4(i) 一条都不写进结论。**
**W19 的净产出是语料,不是读数。**

**⭐ 顺带给 #204 的一个新数据点**:932 的 ab **+164.08** 与 928 的 ab **−176.61**
在**同一棵树、同一条 40-id arm 串、同一层**上相差 **340 gpm**。
这与 #204 §2「逐粒阵容强度差是主项、不是 `CLAUDE.md` 的 Radiant 常数」**同向且更极端**。

### 3.5 单波成本

`describe-instances` 对四台**已返回空**(超出保留窗)⇒ 改用 **S3 时间戳法**(launch → 末对象):
928 **0.693h** / 930 **0.160h** / 932 **0.562h** / 935 **0.865h** = **2.280 台·时**。
× $0.2607/h = **$0.594**,加末对象→真正终止的尾巴 ⇒ 记 **$0.62**。
预估 $0.96 ⇒ **实测为预估的 ~65%**(原因:三台跑不满)。
**诚实边界**:时间戳法不是账单法,尾段是估的,**本波没有第二个独立通道**(#233(a) 的新数据点)。

## 4. 发波 W19-R(补跑,不受 §4b(i);例行锚点仍取 W19 首台)

- **§4a 无适用对象**:队列无 `APPROVED*` 的待发请求。
- **§4b(i) 本轮不满足**:例行锚点 = **W19 首台 `2026-08-28T00:20:03Z` ⇒ W20 解锁 `06:20:03Z`**;
  `date -u` 实读 **03:08Z**,**还差 ~3.2h ⇒ 本轮不发例行波**。发的是**补跑**(先例:W17-R / W18-R)。
- **pin 同树 `3110f323ead440674cef721a4797352366516d29`**(显式 40 位 sha,不写 `main`)
  + **同 40id/354 字节 arm 串** + **W19 丢掉的三粒种子 928 / 930 / 932**。
  ⇒ 本轮收尾提交**不会**改变实例克隆到的树(远端 tip 已走到 `c04e783`,与本波无关)。
- **两道门**:接线门 **exit 0**(`all 40 armed ids wired on 3110f323…`,**先落盘再取 `$?`**,没接 `| tail`);
  载体门 **exit 0**(`terms=5 seeds=3`)—— 逐项 `skeleton_king` 2/3(928,930)、`zuus` 2/3、
  `lion` 2/3、`crystal_maiden` 1/3(928)、`obsidian_destroyer` 1/3(930):**焦点五全部有载体**。
  角色门/等级门 id 的载体门仍 **no-op**(**GH #140,第二十四轮**)。
- **配置**:3 台 × 1 种子,`--slots 16 --rec-slots 12 --hours 2 --games 12`,
  **未传 `--on-demand`**(owner GH #158)、**未传 `--cand-ref`**(⇒ 收割时也不要传);`--dry-run` 先行,预算闸未拒发。
- **市场与放置**(全部核实,`spot_` 前缀没当证据):

| 种子 | AZ | 实例 | Lifecycle | run 前缀 |
|---|---|---|---|---|
| 928 | **us-west-2d** | `i-04532b5b2a00d989c` | **spot** | `…_031235_…_05bde4` |
| 930 | **us-west-2a** | `i-08fd9034e4d26f8da` | **spot** | `…_031240_…_bfb16e` |
| 932 | **us-west-2b** | `i-0c2ed84a2215d15e4` | **spot** | `…_031244_…_eef708` |

  **三台三个不同 AZ**、`soak-run` 标签**两两不同**、**降级阶梯第 1 级即成功**
  (未出现 `re-aiming inside the ring`,也未出现 `AZ RING EXHAUSTED`)⇒ **本轮无降级可报**。
  AZ 选择说明:取本波**存活的 2d** + 2a + 2b;**未选 2c**(本波两台都在 2c 被回收),
  这是**逐波的容量观察**,不是把 2c 移出 `AZ_LIST`(总监 08-27T19:xxZ 裁定不动列表)。
- 单波预估 **$0.70**(spot)/ 悲观 $1.86(全程降级按需),两种估值都已过闸 (iii)。

## 5. 局数(铁律 7)

**(a) W19 定案**:**139 落盘 / 95 计分 / unfinished 0 / 暖场 44**;
预期 ~230 落盘 / ~205 计分 ⇒ **−40% / −54%**,**差额全部由三台回收解释**。
- 可配对种子:**935 一粒**(53 局);薄臂 **928 一粒**(42 局,见 §3.3);
- **孤儿新增 27 局**(932 的 ab 单腿)⇒ 孤儿存量 **281 → 308 局**。
- `3110f323` 树的池子:**1 粒形态正常**(上一轮预告「新的一串,从 n=4 起算」——**实际起算于 n=1**)。

**(b) W19-R 在跑**:预期 **~156 落盘 / ~135 计分**(3 台 × ~52/~45);
**诚实边界**:spot 回收风险让这只是期望值不是下界 —— 本波母波刚刚 3/4 被回收。

## 6. 泄漏检查(两次)

- **开工**:`check_costs.sh` 的 running/pending 段**空**(W19 四台已全部离场)。
- **收尾**:`--leak-only` 见 §8,**恰 3 台**,逐个对得上 §4 的发波表(全是本轮自己发的,全 `running`/`spot`);
  常驻只剩 AMI `ami-0a990a26d89c66547` + 快照 —— **不是泄漏**。
- ⚠️ **作证边界(第四轮保留)**:`--leak-only` **不能**证明「过去这一轮没漏过」——
  终止实例约 1h 后从 `describe-instances` 消失,**它不报错,它返回空,而空会被读成「没有实例」**;
  本轮 §3.5 对 W19 四台拿到 `[]` 就是这条边界的又一次实地兑现。

## 7. 铁律 6

本轮 `bots/` / `game/` **一行未改**(只动 `iterations/` 下的报告与章程)。
静态门由 `.githooks/pre-push` 自动跑,**未使用 `RULE6_BYPASS`**;
动态半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)**未跑且不声称**。

## 8. 交棒

1. **⭐ 下一轮本台 —— 收割 W19-R**(`…_05bde4` 928 / `…_bfb16e` 930 / `…_eef708` 932,
   每 run 独立子目录再指向父目录,**不传 `--cand-ref`**,arm 串反解应得 40 id / 354 字节)。
   **⚠️ 池化规则(逐粒不同,别一刀切)**:
   - **930**:母波 0 个 analysis.json ⇒ 无冲突,直接用补跑目录;
   - **932**:母波是 **ab 单腿孤儿(27 局)** ⇒ **绝不放进池**(会单侧灌 ab 腿,W18-R 已踩过);
   - **928**:母波**两腿都在**(ab41/ba1),**不是纯 ab-inflater** ⇒ 本台建议**与补跑目录合池**
     (同树、同 arm 串、同种子,合池后 ba 腿从 1 长到 ~16,正好解掉 §3.3 的薄臂)。
     **这一条请总监复核** —— 它与 W18-R 那条「只放补跑目录」的纪律形式相似但前提不同。
2. **6h 闸锚点:W19 首台 `2026-08-28T00:20:03Z` ⇒ W20 解锁 `2026-08-28T06:20:03Z`**
   (**W19-R 是补跑,不作锚点**),**先 `date -u` 再比,别写死 epoch**。
3. **⭐⭐ 总监 —— GH #269(本轮唯一新立案)**:`scored` 门无最小臂深,代价实测 **76.5 gpm**;
   请在 (A) 硬门 / (B) 加权 / (C) 只报不拦 中裁一个。**本轮已按 (A) 的精神读**。
4. **⭐ 总监 —— GH #204(第十四轮)**:本轮**没有**新的池化读数(W19 只买到 1 粒形态正常的种子,
   且四量两层全反号)⇒ **「n≳31 还买不买」那条裁定挂第三轮**;
   新数据点:同树同 arm 同层的两粒 ab 读数相差 **340 gpm**(§3.4),§2 算术**比 issue 原文更强**。
5. **⭐ 总监 —— 上一轮交棒项④(闸 iii 的 $60)已由您解决**,本轮记录:围栏改绑 Budget 告警档,
   余量 $22.67 ≈ 30 波 spot ⇒ **(iii) 不再是近期约束**;本台不再每轮催这一条。
6. **⭐ 总监 —— 「2b 容量差」印象本波被证伪**(回收落在 2a + 2c×2,2b 存活)⇒
   **您 08-27T19:xxZ 不移出 `2b` 的裁定被本波支持**;但 **#252 的「一次事件最多丢 1 粒」上界
   同时被削弱** —— 跨 AZ 同时回收是真实事件,四路分散压的是相关性不是总量。请复核这条读法。
7. **⭐ 总监 —— 章程费率**:本轮 W19 三台跑不满,**不提供**「跑满一台 ~0.90h」的新证据;
   上一轮的 0.745 → 0.92 更正**维持**(本轮围栏已用 0.92)。
8. 总监 —— **#233(a)** 未裁;新数据点:**W19 实测 $0.62 vs 预估 $0.96 = 65%**
   (方向与 W18 的 3.7 倍、W18-R 的 1.24 倍**又不同** ⇒ 三个数据点三个方向,**偏离本身无系统性**)。
9. 总监 —— **#239 路 4 第六次实证**(spot 波 SIR 是唯一能区分「回收 vs 自终」的通道;
   §3.5 里 `describe-instances` 的 `[]` 是反面例证)。
10. 总监 —— **#207(第十四轮)** `zusstatic` **连续第十三波空转**;
    **#218 后续** `salveally`/`salveyield` **连续第九轮不在第 2 行**。
11. 录像组 —— `abilanc` (a) 取证:**W19 的 139 局 `.dem` 里 95 局可配对**(935 的 53 局形态最好);
    932 的 27 局单腿孤儿仍可逐帧;W18-R 的 127 局两腿全可配对(**最好用**);
    **W14 的 156 份约 09-16 过期,优先消化**;**W19-R 新增(`--rec-slots 12` × 三台)**。
12. replay-check 组 —— 报告节奏 **6.1h 洞**(**连续第六轮**,同一处同一值 07:19Z→13:28Z)。
13. 存量催办:#217(第十三轮)/#211(第十四轮)/#225/#180(第二十轮)/#171(第十八轮)/
    #200(第十七轮)/#181/载体门 PARTIAL(第二十四轮)/`stable-v*` tag(第二十二轮)/
    `campdanger`(第十七轮)/§BL.4 机械化/#75(第十六轮,维持 12)。

## 9. Token 用量(铁律 8)

```
TOKENS total_in=3,597,158 out=32,438 turns=34
```
(统计时刻之后的收尾回合不计入。)
