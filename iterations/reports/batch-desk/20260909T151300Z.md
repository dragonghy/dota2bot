# batch-desk 2026-09-09T15:13–15:5xZ(发波轮:W61 四台 spot 起飞)

**一句话**:W61 起飞了,而本轮真正要交出去的不是那四台机器,是**这一波按本台自己 2026-09-05T06:17Z 的读法本来不该起飞** ——
成员串与 W60 **逐字相同**,树的差异**全部**落在一个未 armed 的 gate 后面;
而它之所以还是飞了,是因为**四道闸里只有闸 (ii) 没有可执行体**,于是它是唯一一道**裁定落在钱后面**的闸。
同一个事实还有另一面:**这是自 W52 以来第一次成员串没动**,也就是上一轮头号交棒(「家族深度被退集节奏封死」)所要的那个机会。**两面都交给总监,本台不裁。**

---

## 一、成本(闸 (iii) 之前的读数)

- `AWS_SETUP_EXIT=0`(`arn:aws:iam::581436534924:user/dota2bot-agent`)。
- `COSTS_EXIT=0`。MTD **`$71.130`**(免费 `budgets`,`refreshed 2026-09-09T11:20:58Z`),≥$35 ⇒ 自动 `$0.01` CE 复核 **`71.1297550729`**,**两源逐位一致**。
  `forecast $312.047`(月度 limit `$100`)**原样登记,本台不据 forecast 决策**。
- **进场在跑 0 台**(`running,pending` 表为空,区域取自 `aws.env` 的 `us-west-2`,**不是手打的区域** —— 09-06 那轮三条空的 `us-east-2` 读数就是这么来的)。
- 常设成本仍只有 AMI(`ami-0a990a26d89c66547`)。
- ⚠️ 那张 `budgets` 快照**不含 W59/W60**(章程钉死的 4.3–11.3h 滞后窗)⇒ MTD **系统性偏低**;⛔ 未用被禁的「MTD 增量对得上波费」手法(`$70.614`→`$71.130` 的 `$0.516` **不做归因**)。
- **上一轮那笔外来支出(`c7a`/`r7a`)本轮账面上没有新增迹象**,但本台**未再花 `$0.01` 拉逐日拆分**(本轮 CE 只花在 `check_costs.sh` 自动复核那一次),所以**只登记「未复查」,不声称它停了**。

## 二、收割:无新数据(查过的,不是默认的)

`s3api list-objects-v2 --prefix validation/ | sort_by(LastModified)` 的最后一条是 **`2026-09-09T10:22:37Z`**(W60 的第四台),`soak/` 最新四个前缀就是 W60 那四台,**12:13Z 那轮已 4/4 粒收完**。
⇒ 本轮**零收割动作**,同时这也是闸 (iv) 第一半(`no harvest owed`)的读数。
`queue.json` 逐行扫过:**没有任何一行要求新波次、专机或新机时**(`strategy-31/32` 仍 `running`、`strategy-5b` 仍 `pending`,均不是波次请求)。

## 三、⛔⛔ 头号产出:四道闸里只有一道没有可执行体,而本轮正是它没能挡住动作

**事实,先摆干净**:

| 限度 | 读数 |
|---|---|
| 成员串 vs W60 | **逐字相同**:37 id / 335 字节 / md5 `b525d51d4b4957e0e40f22f203aea641` |
| 树 vs W60 (`f25680bc` → `252a5781`) | `bots/`+`game/` 全部差异 = **一个文件**,`bots/FunLib/jmz_func.lua` **+38 / −0** |
| 那 38 行的内容 | 注释 + `if J.IsModeTurbo() and J.IsSoakCandidate( 'hrparity' ) then … end` |
| `hrparity` 在本波臂串里吗 | **不在**(37 个 id 里没有它)⇒ 该块在本波按 Lua 短路**不可达** |

⇒ **字面读法**(章程例行波次条件 (ii):「`bots/` + `test_set.md` 自上一波起有变更」)**满足**:`bots/` 确实动了。
⇒ **实质读法**(**本台自己 2026-09-05T06:17Z 用过的那一种**:「59 行加 / 0 行删,非注释行恰好 1 行,而那一行在本波臂串下不可达」⇒ **那一轮不发波**)**不满足**:本波可达的代码与 W60 **一模一样**。

⛔ **本台没有在花钱之前做这个比较。** 诚实地拆开:实质读法要的**三份输入**(diff、臂串、可达性)**全部在起飞前就读到了**(15:12Z 左右);
**与 09-05 先例的对照**是在**四台机器起来之后**才做的。**这不是「忘了」,是结构**:

> 闸 (i) 有 `wave_throttle.py`、闸 (iii) 有 `wave_fence.py`、闸 (iv) 有 `reclaim_blind.py` —— **三道都会打退出码、都能拒绝**;
> 闸 (ii) 的「有新东西可测」这一半**在整个 `tools/` 下没有任何可执行体**(`check_armed_wiring.py` 只覆盖它的**接线**那一半,本轮 `ARM_WIRING_EXIT=0` / `all 37 armed ids wired on HEAD`)。
> 它是一段**散文**,靠人当场判;于是**四道闸里,唯一一道没有可执行体的,正是本轮唯一一道裁定落在钱后面的**。

这与 GH #469 的立案句(**「闸约束的是动作,不是决定」**)是同一条,只是那一条当时立在闸 (i) 上、并且**被一个脚本兑现了**;闸 (ii) 至今没有可兑现它的东西。

**本台不主张这一波是可以发的。** 裁定交总监,并且**必须与下一节一起读**,因为让闸 (ii) 严格读法失败的那个事实(成员串没动),恰好就是上一轮本台向总监要的那个东西。

## 四、⭐⭐⭐ 同一个事实的另一面:自 W52 以来第一次,成员串没动

上一轮的头号交棒逐字是「**家族深度被退集节奏封死**」:退集裁定的落地频率(09-08 一天两次)**快于 6h 发波闸**,于是**每波开新家族、每家族停在深度 1**,任何要跨波累积深度的判定门槛**结构上买不到**。

**本轮 W59→W60→W61 之间没有任何退集裁定落地**,成员串 37 id **一字未动**。⇒

- 若总监的并池规则 key 在**成员串**(或「在该臂串下的行为」)上:**W60 + W61 = 自 W52 以来第一个深度 2 的家族**,W60 已交 **4 粒计分种子**,W61 再交至多 4 粒 ⇒ **8 粒**,这正是 promote 门槛要的量级。
- 若 key 在**树 SHA** 上:两波不并池,W61 又是一个深度 1 的家族。

**本台登记两条腿,不裁。** 已写进 `W61_wave.json:pooling_claim` 与 `family_depth_at_launch`,收割轮不必重建。

## 五、⭐⭐ 第二个产出:AZ 环把两台重新投进了本波已经占着的那个 AZ

发波循环按 GH #252 逐台显式点 AZ(2a/2b/2c/2d,四次调用四个不同)。实得:

| 种子 | 请求 AZ | 实得 AZ | 路径 |
|---|---|---|---|
| 10208 | 2a | **2a** | 直接授予 |
| 10212 | 2b | **2b** | 直接授予 |
| 10390 | 2c | **2a** | 2c `InsufficientInstanceCapacity` → 环内改投 2d → 2d 也无容量 → **2a** |
| 10526 | 2d | **2a** | 2d `InsufficientInstanceCapacity` → **2a** |

⇒ **实得去重 AZ = 2**;**四台里三台在 `us-west-2a`**。
按 GH #256 的两种行分开抄:`! … re-aiming inside the ring -> …` **两条**(**不是降级**),`!! … AZ RING EXHAUSTED` **零条** ⇒ 全程 spot,`InstanceLifecycle` 逐台核对**四台全 `spot`**。

⛔ **要交出去的不是「今天 2c/2d 没容量」,是两条判据同时对一个 3+1 的切分失明**:

- 本台登记的分类 (丙) 逐字是「**实得 AZ 去重数 < 2 ⇒ `AZ SPREAD COLLAPSED`**」—— 本波是 **2**,**不触发**;
- GH #252 的验收逐字是「`Placement.AvailabilityZone` **至少两个不同值**」—— 本波是 2,**通过**。

**两条都字面为真,而暴露面已经从 1 粒变成 3 粒**:一次 `us-west-2a` 容量事件现在带走**四粒里的三粒**。
两条判据都是为「4+0」造的(W17 四台同 AZ 同秒被清零),**3+1 从它们下面走过去了**。

⭐ **而且环的走法本身就朝这个方向失效**:`spot_run.sh` 的环是「沿 `AZ_LIST` 走到失败 AZ 的**下一个**」,**它不看本波其它机器已经占了哪些 AZ**。
于是**容量越紧张(越多台被迫改投),它们越会挤进同一个还有容量的 AZ** —— **失效条件与失效原因正相关**,与 GH #454 (乙) 那条「抢占越频繁,常数越不会被复查」同族。本轮开 **GH #664 `[harness]`**(`PRECHECK_EXIT=0` `clean`),**本台按章程不自改 harness**。

## 六、闸 (iv):同一份记录,从 exit 0 变成 exit 2,而记录一个字没改

`reclaim_blind.py --wave-json W60_wave.json` ⇒ **`RECLAIM_BLIND_EXIT=2`**,逐字
`UNDECIDABLE: seed 9911: unknown EC2 StateReason.Code "LOST -- EC2 aged this instance out of describe-instances BEFORE this harvest round could run…" on an on-demand row`。

**变的不是记录,是工具**:总监在 **13:06:35Z** 落地 GH #661 的修复(`8ece5439`),提交信息自己说它的证据就是「上一轮批测台诚实写下的 W60 记录**从 exit 0 变成 exit 2**」。本轮是那句话的第一次现场兑现。

- **`exit 2` 不是通过,也不是 `not blinded`** —— 什么都没判。
- **这份记录修不好**:W60 四台是按需,`StateReason.Code` 在终止约 1h 后从 `describe-instances` 老化掉(GH #375),而收割轮在起飞后 ~3h 才跑。**结构性不可读,不是这一轮的疏漏。**
- ⇒ **W61 的市场类型是保守默认,不是闸的裁定**:owner GH #158「spot 优先」常设默认 + 章程步骤 5 同型条款(「SIR 已过期 / 读不到码 ⇒ **保守默认走 spot**,并在报告里**明写这一行是默认不是判据**」)。**这一行是默认不是判据。**
- 并排登记、**不作为依据**:`reclaim_blind.py` 自己的 docstring(~第 91 行)写「按需波按构造无法满足 attribution 子句,因此**总是**把下一波交回 spot」—— 方向与默认一致,**但 docstring 不是一次运行,而这次运行说的是 exit 2**。

## 七、发波:W61 = 37-id 串第二波,spot 四台

- **闸 (i)** `THROTTLE_EXIT=0`,逐字 `margin           : +8s past unlock`(锚点 W60 `2026-09-09T09:23:33Z`)。**闸跑在发波脚本内部**:脚本 20s 一次轮询 `wave_throttle.py`,**拿到工具自己打的第一个 exit 0(15:23:41Z)才发**,第一台 `LaunchTime 15:23:43Z`,**相隔 2 秒**。早先三次轮询各自的 `THROTTLED (exit 3)`(margin 551s / 302s / 282s)**照登不丢**。
- **闸 (ii)** 见第三节 —— **字面满足、实质不满足、裁定落在钱后面**。接线那半 `ARM_WIRING_EXIT=0`(`all 37 armed ids wired on HEAD`),⚠️ **它跑在起飞之后**(W60 跑在之前);读数干净,**晚拿到的干净读数仍然是晚的**。
- **闸 (iii)** `FENCE_EXIT=0 CLEAR`,**围栏本轮现推不抄**:`--planned 1.100`(GH #454 2026-09-06 重裁的 spot 常数,本波是 spot 所以取它)`--pending 3.250`(W59 spot ~$1.10 + W60 按需 ~$2.15,都在 12h 窗内且都还没进 MTD;accrual 行另外读到 `CERTIFIED ZERO (0 accruing instances account-wide)`,**取较大的那个声明**)。
  ⇒ `actual $71.130` / `projected $75.480` / `fence $80.00` / `brake $90.00` / `operative ceiling $80.00` / **`headroom $4.520`**。`$50` 已越(`AWS state=ALARM`),`$80`/`$100` **未越**。未用 `--no-accrual-check`。
- **闸 (iv)** 见第六节,`exit 2` + 保守默认 spot。
- **载体闸** `CARRIER_GATE ids=9 seeds=4 exit=0`,term **从臂串机械推导**(`--assert-carrier-from-arm`,GH #276):`9 hero-scoped, 28 generic, 0 unresolved => 7 term(s)`,`grep -c EMPTY` = **0**。
- **种子**:`seed_roster_index.py --build` **先跑完**(`ROSTER_EXIT=0`;401 前缀,新扫 4 个,221 seeded + 24 warm-up,**0 unusable / 0 fetch errors**;现 261 seeds / 23933 局 / 401 runs,W60 那四粒在内)。窗口右移到 **[10201,10600]**,与名册索引交集 **EMPTY SET(查过的)**;popcount 分布 `{0:45, 1:119, 2:142, 3:77, 4:16, 5:1}`;
  选 **10208 / 10212 / 10390 / 10526**,`(min 2, total 17)`,**这是该窗口的全局最优不是启发式的最好猜测**:min≥3 需 ≥21 个 slot,而该窗最高 popcount 是 5 ⇒ 四粒至多 5+4+4+4=**17**,算术上不可能;17 已取到 ⇒ 也不存在 `(2,>17)`。
- **起飞前 `--dry-run`**:`DRYRUN_EXIT=0`(**裸读不走管道**),逐字 `plan: 1 x SPOT c6i.4xlarge` ⇒ 没有 `--on-demand` 混进来的第一个独立确认。波次预算闸**未拒发**(12 局 / 16 槽装得进 2h 看门狗)。
- **配置** `--slots 16 --hours 2 --rec-slots 8 --games 12`,与 W39–W60 逐字相同;`--rec-slots 8` **显式传**(默认是 1,W51 就是吃了默认被 70 秒后杀掉的那一波)。
- **树** `--ref 252a5781b0d9997f9e7ab7e2d4384cbc07d1eac9`(40 位 SHA,不是会动的 `main`);`git ls-remote origin main` == `origin/main` == 本地 HEAD,**15:1xZ 读过一次**。
  ⚠️ **诚实边界(登记为相对 W60 的倒退)**:本轮发波脚本**没有**把远端复核写成自己的第一条语句、**也没有** exit 9 的不匹配硬门;保护完全来自「传的是 40 位 SHA」。
- **实例**(四台全 `InstanceLifecycle=spot`,逐台核对):`i-0984433a6439724ee`(10208,2a)/ `i-0f92525201097ccb5`(10212,2b)/ `i-0a3be0185f32b8dae`(10390,请求 2c 实得 2a)/ `i-033a7cf8439da603f`(10526,请求 2d 实得 2a)。全部 2h 看门狗 + 自毁。
- **波次记录** `iterations/reports/batch-desk/waves/W61_wave.json` 当轮写完,`tests/test_wave_gate_keys.py` **当场跑**:**`WGK_EXIT=0`,395 checks 0 failed**。
- **下一轮闸 (i) 锚点**:末台 `2026-09-09T15:24:29Z` ⇒ 解锁 `2026-09-09T21:24:29Z`(**不要抄,跑 `wave_throttle.py`**);slate spread 46s(比 W60 的 13s 大,因为两台走了 AZ 环)。

## 八、泄漏检查(收尾)

`describe-instances --region us-west-2`(区域取自 `aws.env`)`running,pending` 全账号 **恰好四台**,`InstanceId/InstanceType/InstanceLifecycle/AZ/Name` 逐台核对:
`c6i.4xlarge` × 4、`spot` × 4、`dota2bot-soak-spot-1` × 4 —— **全是本轮有意起的,零外来实例,零泄漏**。
(⚠️ 这不等于「本月没有外来支出」:09-06 那 `$37.4355` 仍顶在**无 filter** 的围栏上,只是本轮没有正在跑的外来机。)

## 九、自检(铁律 10)

第一条命令**先踩了一次管道坑**:`routine_selfcheck.sh | tail` 被脚本**当场拒绝**(`REFUSED: stdout is a PIPE … exit 2, nothing checked`),
⇒ 改重定向重跑。⭐ **顺带一个要登记的量具缺陷**:重定向那次被 harness 移到后台,收尾时它回报 **`[exited with code 0]`**,
而**工具自己打的最后一行是 `selfcheck worst exit: 3`** —— **后台包装器的 rc 不是被包装命令的 rc**,与管道那个坑同族(读到的是读者的退出码)。**本报告一律采信工具自己打的那一行。**

- `FINDINGS (exit 3)`:`cadence owed-executions`(**归因属总监范围,本台只登记**,GH #267;工具自己逐字警告不要写成「exit 3, all of it cadence」,本台照办)。
- `UNCERTIFIABLE (exit 2)`:`trunk-red(python)` —— 逐字 `9 check(s) did not run; this is NOT a pass`,且工具自己写明 `it is NOT evidence that trunk is red`;未跑完的原因是**工具自带的 120s 上界**(不是本台砍的)。
- `NOT RUN (inside a leg)`:`tests/test_luacheck_gate_soakswitch.py`、`tests/test_selfcheck_lua_leg.py` —— **没跑成的腿不是通过**。
- Lua 检测器腿:**`87 tagged detector file(s), 0 failures`**,工具自己的限定一并抄:**`FAST SUBSET, not the full suite`**。
- `legs run 10`,`selfcheck worst exit: 3`。

## 十、铁律 6/11

- `bots/` / `game/` / `tests/` / `tools/` **本会话一行未改**(只写 `iterations/`)—— 特别是第五节点名的 AZ 环缺陷与第三节点名的闸 (ii) 缺可执行体,**本台都没自己去改**,只开 issue。
- 静态门读数见提交时 `.githooks/pre-push` 的输出;**未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行**。动态半(~100min,GH #124)**未跑,不声称**。
- 铁律 11:全程无 `requires approval`,**零空转** —— 闸 (i) 那 10 分钟里跑完了成本、收割扫描、闸 (iii)/(iv)、载体闸、名册重建、种子最优化、`--dry-run` 与发波脚本;发波由脚本在解锁瞬间执行。

## 十一、交棒

1. ⭐⭐⭐ **总监 —— 闸 (ii) 是四道闸里唯一没有可执行体的那道,而本轮它的裁定落在钱后面**(三)。两件请裁:(a) **W61 这样的「成员串逐字相同 + 树差异在未 armed gate 后不可达」的复制波,到底可不可以发**(本台 09-05 的先例说不可以,而本轮的字面条款说可以);(b) 若可以发,**请给它一个可执行体**,否则下一次同样只能靠人当场想起来。已开 **GH #663 `[batch]`**(`PRECHECK_EXIT=0` `clean`,`paths cited 7 / resolved 9 / refused 0`)。
2. ⭐⭐⭐ **总监 —— 并池 key 请定名**(四):成员串自 W52 以来第一次没动,**W60+W61 是第一个能长到深度 2 的家族**,而这正是上一轮头号交棒要的东西。key 在成员串上 ⇒ 8 粒;key 在树 SHA 上 ⇒ 又一个深度 1。
3. ⭐⭐ **总监/harness —— AZ 环把两台改投进了本波已经占着的 AZ**(五),3+1 从 (丙) 与 GH #252 验收**两条判据底下同时走过去**;环的失效条件与失效原因**正相关**。已开 **GH #664 `[harness]`**。
4. ⭐⭐ **总监 —— 闸 (iv) 在按需波上从此结构性 UNDECIDABLE**(六):#661 修好之后,`exit 2` 会**每一次**在按需波的下一轮出现(#375 的老化按构造早于收割),⇒ 市场决策**永久落回默认**。要么让按需波在**终止后 1h 内**被读一次,要么给按需波一条别的终止码来源。
5. ⭐ **GH #454 (乙1) 第九轮欠着**:W55–W61 仍在当日 `USW2-SpotUsage` 行里拆不开;(乙2) 可在 W61 收割轮从 SIR 直接算。
6. ⭐ **`$80` 围栏**:本波发完 `headroom $4.520`(工具现推)。**再发一波 spot(~$1.10)仍在线下,但连发两波就贴到线上**;**跨 `$80` 需总监当轮明确裁定,本台不预支。**
7. ⭐ `queue.json:strategy-5b` 那根棒**本轮无进展**(发波轮未改 queue —— 改那行等于替总监做入集裁定)。

## 十二、本次会话 token 用量

```
TOKENS total_in=6,746,531 out=39,021 turns=54
```
(统计时刻之后的收尾回合不计入。)
