# 批测台 — W36 收割(2026-09-02T00:18–00:5xZ)

**工作单元:收割 W36。本轮不发波** —— 闸 (i) 6h 节流解锁于 **2026-09-02T03:31:10Z**(W36 起飞 21:31:10Z + 6h),
本轮 00:18Z 触发,**距解锁还有约 3h13m,一次付费调用都没有发出**。

---

## 0. 开工自检(铁律 10)与管道守卫

⚠️ **本轮第一条命令又误用了管道**,自检**自己拒跑**并打:

```
REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.
SELFCHECK_EXIT=2  REFUSED (nothing was checked; this is NOT a pass)
```

这是**管道守卫的第 11 个外部验收点**、同一形状**第七次**。按它自己给的处方改成重定向后重跑:

```
timeout 600 bash tools/agent/routine_selfcheck.sh > <log> 2>&1; echo "SELFCHECK_EXIT=$?"
SELFCHECK_EXIT=124
```

**`124` = 600s 超时,自检没跑完 ⇒ 本轮不存在「自检总判决」。** 这是**连续第二轮**(上一轮跑完了,再上两轮也是 124)。
日志里已落地的部分点名了两条与上轮同一形状的红:

- `TRUNK RED -- a python test is failing ON THE WORKING TREE.`(日志自己附了那句诚实边界:**「main 是否也红,这一行并未确立;`git stash` 后重跑才算」**——本轮没跑那一步,故**不声称**)
- Lua:`test_incoming_damage_callsite_census.lua` —— **GH #394 第四次复现未修**(`published 43 is a grep line count; it now reads 44`)

**本台不代修、不代裁**,原样交总监/英雄组。

## 1. AWS 引导

```
bash tools/batch_test/aws/session_setup.sh   => AWS_SETUP_EXIT=0
AWS ready: arn:aws:iam::581436534924:user/dota2bot-agent
(restricted batch-runner user — correct)
```

## 2. 成本(发波前后同一读数,本轮无支出)

```
bash tools/batch_test/aws/check_costs.sh  => COST_EXIT=0
== running/pending instances (should be empty unless a batch is live) ==   [空]
== month-to-date cost ==
3.637   USD
   source: budgets/dota2bot-batch (free) | forecast 150.867 | budget limit 100.0
   | budget refreshed 2026-09-01T17:58:37Z
```

- 九月 MTD **$3.637**,**免费通道**(`budgets`)。MTD `< $35` ⇒ **CE 复核未触发,那 `$0.01` 没花**。
- ⚠️ 快照时刻 **`17:58:37Z`,与上一轮同一张快照** ⇒ **本轮无新围栏信息**,W36 的实际支出尚未进这个数。
- ⚠️ `forecast 150.867` 仍是开月两天外推,**不构成刹车信号**。围栏 $60 / 刹车 $90 / 批准线 $100,**一条都不接近**。

## 3. ⭐⭐⭐ W36 结算:六台里四台被回收,**两台补跑机同一秒死**

`describe-spot-instance-requests` 六个 SIR 全部第一手(#382 对 spot 波的反驳继续成立:`describe-instances` 已经空了,SIR 行还在):

| SIR | 实例 | 种子 | CreateTime | Status.Code | Status.UpdateTime | AZ | 存活 | 产局 |
|---|---|---|---|---|---|---|---|---|
| sir-ngnfhq3n | i-0855f32a8187db9c6 | 2745 原机 | 21:31:44Z | `…no-capacity` | 21:33:22Z | 2c | 1.63 min | 0 |
| sir-i637gxvn | i-08025db836450f86a | **2838** | 21:32:16Z | `…by-user` | 22:29:47Z | 2d | 57.5 min | **64** |
| sir-abvzjtvn | i-047a15b6e787fd7b7 | **2850** | 21:32:33Z | `…by-user` | 22:26:48Z | 2d | 54.3 min | **49** |
| sir-88afj15n | i-0b6cdde6f6b96267b | 2922 原机 | 21:32:38Z | `…no-capacity` | 21:38:01Z | 2d | 5.38 min | 0 |
| **sir-kwi7gnvq** | i-02b40982d2bb555a0 | **2745 补跑** | 21:44:43Z | `…no-capacity` | **21:50:21Z** | **2c** | 5.63 min | **0** |
| **sir-wjbqgh9q** | i-0469e3ea69e73c438 | **2922 补跑** | 21:44:58Z | `…no-capacity` | **21:50:21Z** | **2c** | 5.38 min | **0** |

**两台补跑机的 `Status.UpdateTime` 精确到秒相同、AZ 相同** ⇒ 一次相关容量事件带走两台。

**第二条独立通道**:两个补跑前缀在 S3 里**各只有一个对象** `soak_farm.log`(3.6 KiB,21:50:2xZ 由中断处理器上传),
各自以 `SPOT INTERRUPTION: {"action":"terminate","time":"2026-09-01T21:52:21Z"}` 收尾 —— 同一次翻转的两分钟通知,
与 SIR 的 21:50:21Z **相互印证**。**零个 `.analysis.json`**:farm 已经起来(`soak farm up: … (16 slots)`)、
`validate_onspot.sh` 刚被调用,通知就到了。

⇒ **种子 2745 与 2922 各自「原机 + 补跑机」双双阵亡,产局为零。**

### 3.1 章程的回收处置按设计执行了,而且什么也没买到 —— **GH #408**

预登记条款是「一台被抢占 = 该粒缺臂,**不要整波作废**,缺的那粒再发一台(仍 spot 优先)」。
上一轮**照做了**:两台补跑机都钉 W36 自己的树 `32abbfbb`、阶梯一级没下、市场没降级。
**然后两台都死了。** 往一个正在拒绝容量的窗口里重发,是**重抽同一张必输的票**;这条处置
**没有退避、没有市场升级、没有第二次补跑**,所以它**一次就终止,而那粒种子仍然是空的**。

### 3.2 #403 的预测在**六分钟**内兑现,代价正是它点名的那两粒

上一轮本台把「环内改投在容量紧张下**收敛**不是分散」立成 **GH #403**,并记下补跑后 AZ 从 1+3 变 2+2 时那句
**「一次 AZ 事件现在带走两粒不是三粒」**。那句话在**六分钟后**被验证。
**杀死补跑机的是集中,不是环内改投本身** —— 环确实走了、每次也确实落到当时还有容量的那个 AZ(两台都是 2c),
而这恰好把两台放进同一个篮子。⛔ **本台不改市场/阶梯,不代数 #256 的重开计数。**

### 3.3 连续第三波

| 波 | 机器 | `no-capacity` 回收 | 计分种子 |
|---|---|---|---|
| W33 | 5 | 4(四个 AZ 全中) | **1/4** |
| W35 | 4 | 3(15:42:25Z 同一瞬间,跨 2b/2c) | **1/4** |
| W36 | 6 | **4**(含两台补跑机**同秒**) | **2/4** |

W36 发波+补跑共吃 **9 次 `InsufficientInstanceCapacity`**。`us-west-2` 的 `c6i.4xlarge` spot 容量**连续三波紧张**。
这是 owner 自己那条 **#158「spot 优先」**的输入。⛔ **本台不重裁 owner 的规则**,读数交 #408。

## 4. 收割读数(登记,**不是判读**)

```
python3 tools/batch_test/soak/recover_verdict.py ./pool <54-id 串>   => RV_EXIT=0
```

**语料同一性先钉死**:交给工具的 54-id 串 md5 **`74273b3ce5f58fcb50de769a700ab074`** / **477 字节**,
与 `W36_wave.json` 起飞时写下的 `arm_md5`/`arm_bytes` **逐字节相同** ⇒ **算的就是发出去的那个串**。
⚠️ `test_set.md:2` 此后已变成 52 条(总监 §DH 出集 `tormself`+`immguard`,GH #402)⇒
**W36 是 54-id 家族的第一波,也是唯一一波。**

**输入普查(可对账,不靠信任)**:`files_seen 113 / games_loaded 113 / source_dirs 2 / unparseable 0 /
pooled_basename_dirs_overridden 0`。113 = 64(9fb8e2)+ 49(3c7e26);两个前缀分别 160 / 122 个对象
(每局 analysis+demclaim+log.gz)。**R1/R2/R3 零拒答、零 override。**
**GH #269 深度门:零粒被排除**,两粒都带margin 过关(23.17 / 17.30),**门没被调低、没传任何 override 旗标**。

| 量 | mean | comps_better | 2838 | 2850 |
|---|---|---|---|---|
| gpm | **−26.63** | **1/2** | −64.22 | +10.95 |
| xpm | −13.28 | 1/2 | −29.10 | +2.54 |
| deaths | +0.07 | 1/2 | +0.24 | −0.11 |
| last_hits | −1.10 | 1/2 | −2.69 | +0.48 |

`suggested: hold_or_reject`;`scored_games 101 / unfinished 0`。

### 4.1 ⛔ 为什么这个 mean **不能**被当成判读

**n=2,而且两粒在每一项经济量上符号相反**(`comps_better` 四项全 `1/2`)⇒
**跨种子离散度大于均值本身**。按铁律 4(i-c),管精度的正是 arm 自己的跨种子离散度,
而 **n=2 没有可用的离散度估计**。更要命的是:**54-id 家族只发过这一波、且已被 §DH 解散,
这个读数永远深不下去。** 本台**报数不判读**。

### 4.2 铁律 4(i-a) 披露 —— 两个分层的**读数**,取自工具自打的 `strata`,**未手算**(4(i-d))

| 量 | ab | ba | side | sign_flip | side_gt_arm |
|---|---|---|---|---|---|
| gpm | 114.47 | −167.74 | 141.11 | **true** | 2/2 |
| xpm | 95.37 | −121.93 | 108.65 | **true** | 2/2 |
| deaths | −1.75 | 1.88 | −1.81 | **true** | 2/2 |
| last_hits | −1.82 | −0.39 | −0.72 | false | 1/2 |

**4(i-c) 读法**:这四项都是**已消侧偏的估计量**(每粒 50/50 swap-average 由工具完成),
故反号是**披露不是否决理由** —— `|side| > |arm|` ⟺ `ab·ba < 0` 是**恒等式不是诊断**。
登记于此是因为 (i-a) 要求,**不因为它决定任何事**。

### 4.3 ⚠️ 胜负通道 DEGENERATE(GH #352)—— **连续第四波扫边**

工具自打拒绝横幅:

```
GH #352 winrate channel DEGENERATE: minority side won 2 of 113 finished games
(share 0.0177 < 0.20). The winrate below is bounded to 0.5 +/- 0.0364 and MUST
NOT be cited as rule 2(b) support until the channel recovers.
```

⇒ `winrate 0.464` / `comps_better winrate 0/2` 被语料**限死**,**不得**作铁律 2(b) 依据。
扫边史:W30 231:1 / W31 221:1 / W35 45:0 / **W36 111:2**,**四波全是 dire 横扫**。

### 4.4 ⭐ 一个反向的好消息,交总监做 GH #108 的数据点

`winner_by` = **`{"engine_natural": 113}`**,`winrate_independent_of_gold: "113/113 games"` ——
**本波 100% 的完局是「远古被推掉」自然结束**,即这里的胜负读数**完全不依赖金钱**(尽管它侧别退化)。
`SOAK_CAP_MIN=25` 之后自然结束率在这份语料上是 **1.00**,而这正是 **#108 验收点名的量**。
**本台只提供数据点,#108 的裁定不是本台的面。**

### 4.5 两笔债的处置

- **§DC.3 (甲)(`illumove` × `illureal` 同帧不正交)**:W35 因 n=1 还不上、滚到 W36;
  W36 只买到 n=2 且**两粒符号相反**,而 54-id 家族已解散 ⇒ **这一族再没有下一波可以还它**。
  **本轮显式交回总监:债不随家族解散而消失,要跟着这两条 id 下一次入集的那一波走。**(铁律 9 的接力棒)
- **§DF.6(`tormself` / `immguard` 按构造域空)**:真载体 ringmaster / brewmaster **都不在 47 人 `hero_pool.txt`**(GH #402),
  本波对它们**按构造域空**,**本轮不把任何读数当作对它们的证据**。总监已在 §DH 把两条同轮**出集**(54→52);
  **出集不是 reject**,gated 代码留树、门关闭。下一棒 = 总监决定扩池还是永久搁置。

## 5. 成本核对与泄漏检查(收尾固定两条)

**成本**:约 **2.2 机时** —— 两台存活 0.96h / 0.90h,四台被回收合计约 0.30h(1.63 + 5.38 + 5.63 + 5.38 分钟)。
按 `c6i.4xlarge` spot 价远低于 `$0.90/波` 常数 ⇒ **自动失效条款(>$0.90 须点名重裁)未触发**。

**泄漏:CLEAN。**

1. **波次点名**:`describe-instances`(**不加状态过滤,全状态**)返回**空** —— 零个实例。
   ⇒ 六个 `soak-run` 标签无一残留,**也不存在陌生实例**。
2. **回收普查**:六个 SIR 全 `closed`、全落在本波窗口内、四 `no-capacity` + 两 `by-user`,**无第七个请求**。
3. `describe-volumes` 返回**空** —— 无孤儿 EBS。
4. 常驻仅 AMI `ami-0a990a26d89c66547`(唯一的常设开销,未变)。

⚠️ 留档一条**通道缺口**:两台被回收的机器都上传了 `soak_farm.log`(中断处理器干的),
而**两台正常自毁的机器,S3 前缀里根本没有 `soak_farm.log`** ⇒ **干净退出这条路上没有 farm-log 通道**,
结算只剩 SIR 一条。本波不受影响(SIR 够用),但**下一次 SIR 也失效时,干净退出的机器将无第二通道**。交总监/harness 记档。

## 6. 本轮**不发波**(闸 (i) 未解锁)

| 闸 | 状态 |
|---|---|
| (i) 6h 节流 | ⛔ **未解锁** —— W36 起飞 21:31:10Z + 6h = **2026-09-02T03:31:10Z**;本轮 00:18Z 触发,差约 3h13m。**本轮一次付费调用都没发。** |
| (ii) 新东西可测 | 成员串已 54 → **52**(§DH 出集),下一波是 52-id 家族的**第一波** |
| (iii) 预算 | MTD $3.637 ≪ 围栏 $60 / 刹车 $90 |
| (iv) 输入 | ✅ **本轮已清** —— W36 收割完毕、wave 记录回填、queue 写回 |

**不为了「这轮得干点什么」去发一个闸没开的波** —— 那是铁律 7 的反面。工作单元干完就结束。

## 7. queue.json 写回(铁律 9,接力棒显式交出)

**10 行** `rides_wave: W36`(`strategy-25/26/27/28/29/30/31/32` + `hero-24/25`)全部写入 W36 收割结果,
`rides_wave` 改记为「W36(54-id 家族**唯一**一波,已收割:2/4 粒入账、6 台中 4 台 no-capacity 回收含两台补跑机同秒;
n=2 且符号相反 ⇒ **登记不判读**;家族已由 §DH 解散)」。`strategy-29/30` 另附 §DC.3 (甲) 的滚债说明,
`strategy-31/32` 另附 §DF.6 的域空说明。**十行 status 一律保持 `running`。**

## 8. 铁律 6

`bots/` / `game/` **一行未改**(本轮只动 `iterations/`)。静态半仍按 `.githooks/pre-push` 在 push 时自跑
(`luacheck_gate.sh`);**未使用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行**。
动态半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)**未跑,也不声称**。

## 9. 交棒

1. ⭐⭐⭐ **总监 — GH #408(本轮新立)**:补跑机制在区域容量紧张下无效,**4 粒种子的 promote 门槛事实上够不着**(三波 1/1/2 粒)。
   四条候选处置(退避 / 补跑打散 AZ / 只给补跑那台降阶梯 / 改判读门槛)已列进 issue,**本台不代裁**。
2. ⭐⭐⭐ **总监 — GH #403 兑现**:环内改投的收敛在**六分钟**后就把它预测的两粒吃掉了。
3. ⭐⭐ **总监 — §DC.3 (甲) 的债无家可归**:54-id 家族解散,债需跟随 `illumove`/`illureal` 下次入集的波。
4. ⭐⭐ **总监 — GH #108 数据点**:`engine_natural 113/113`,cap-25 后自然结束率 **1.00**。
5. ⭐⭐ **总监 — GH #352 连续第四波扫边**(W36 111:2 dire),胜负通道对 arm 无响应。
6. ⭐ **总监/英雄组 — GH #394 第四次复现未修**(`test_incoming_damage_callsite_census.lua`)。
7. ⭐ **总监/harness — 自检连续第二轮 `EXIT=124` 超时**(本轮无总判决);**管道守卫第 11 个验收点、同形状第七次**。
8. ⭐ **总监/harness — farm-log 通道缺口**:干净自毁的机器不上传 `soak_farm.log`,结算只剩 SIR 一条。
9. ⭐ 存量:**#388 W34 的 103 份 `.dem` 2026-09-22 到期(距今 20 天)** / #402 / #395 三件待裁 /
   按需常数第五轮重裁 / **#285 第二十一轮未裁** / #352 / #317 五条 / `strategy-5b` 第十二轮 / #398 / #399 /
   #382 / #383 / #290 / #313 / #375 / #207 / #218 / #367 / #295 / #252 / #256 / #282 / #329 / #321 / #271。

**下一轮本台 = 发 W37**(52-id 家族第一波),闸 (i) 解锁 **2026-09-02T03:31:10Z**。
⚠️ 发波前必读 **GH #408**:若总监已裁,按裁定改补跑策略;**未裁则本台仍按预登记保守默认走 spot**
(理由是「拒答不是判决」,**不是**因为当前市场被判 ok)。

## 10. 本轮 token 用量

```
TOKENS total_in=3,072,435 out=30,721 turns=28
```

(统计时刻之后的收尾回合不计入,量级参考足够。)
