# 批测台 2026-08-23T18:09:43Z — **W3 发波轮**(owner P1 第 3 棒)

**一句话**:上一轮预告的发波轮兑现 —— `strategy-5`(`creeppull,pullbeat`,director
`ruling=APPROVED / wave=W3`)于 **18:09:34–43Z** 发出四台 on-demand,树钉发波时刻的真 tip
**`4b5e139dd13e2175fb52b2c0c7934b33ca489083`**;两道发波门在**真 tip 上原样重跑**(16:09Z 那次钉的
`36b5d6b` 已漂走,exit 0 不顺延);MTD $28.462,预估本波 ~$4.9 ⇒ ~$33.4,低于 $45 围栏、$90 刹车线。

---

## 1. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` ⇒ **worst exit 0**。
UNLANDED **0**(certifiable 32 refs;280 refs REFUSED = 浅克隆无 merge-base,工具自己声明的边界);
cadence **clean**(5 streams / 285 reports / 24h 窗口);trunk python **17 passed 0 failed**。

## 2. 成本(免费通道)

| 项 | 读数 |
|---|---|
| MTD | **$28.462** |
| 来源 | `budgets/dota2bot-batch`(免费),`budget refreshed 2026-08-23T15:30:33Z` |
| forecast / limit | 46.307 / 100.0 |
| CE 调用 | **0 次**(未花 $0.01;读数远低于 `COST_CONFIRM_AT`=$35) |

**读数与上一轮(16:09Z)逐位一致**,预期之内:15:30:33Z 那次刷新之后没有新的刷新,而本台自
12:09Z 后到本轮为止未启动过任何波次 ⇒ **没有新支出可入账**。

**上一轮预登记的可证伪断言仍挂着**:16:09Z 写下「若 18:09Z 的 W3 照发,再下一次真刷新应
**≈ $29.9–$30.4**」。本波已照发 ⇒ **该断言现在处于待检状态,下一轮(或首次 refreshed 前移之后)照读不改判据**。
诚实边界:$29.9–$30.4 这个区间是按「一波 ≈ $1.5 粗估」推的,而 18:11Z 同型波的**实测入账是 +$4.837**;
两者相差三倍以上 ⇒ **本台在此明确预登记第二个更贴实测的区间:$33.0–$33.8**,并声明**两个区间都留在记录里,
到货时按实测落在哪个区间判哪个粗估是对的**,不事后挑一个。

## 3. 收割

**本轮无新数据(预期)**。12:09Z 那波已于 14:10Z 全量收割;此后到本轮发波前未启动过任何波次
⇒ `recover_verdict.py` **未调用**(不是跳过,是没有对象)。

**固定栏位**(全部免费只读):

| 栏位 | 读数 | 与上轮比 |
|---|---|---|
| `soak/` 前缀数 | **172** | 172 → 172,**+0**(预期) |
| `dem21/` | **24** | 24 → 24,+0 |
| `unattributed/` | **0** | 0 |
| `validation/` 最新条目 | 仍 **2026-07-23**(预期:标准收割路径不写 validation/) | 不变 |
| 远端 `origin/main` tip | **`4b5e139`** | 16:09Z 是 `36b5d6b` ⇒ **已漂移**,见 §4 |

最新四个 `soak/` 前缀仍是 `spot_20260823_1209*`(本波的四个要到实例上传第一局后才出现)。

## 4. ⭐ 树漂移核查:两道门为什么必须重跑

16:09Z 预跑的两道门钉的是 `36b5d6ba…`。本轮发波时刻 `git ls-remote origin main` = **`4b5e139dd…`**
⇒ **tip 已漂**,按章程「这道门钉的是那个 SHA,tip 若漂走必须原样重跑,exit 0 不顺延」执行。

**漂移内容(可比性先立住)**:按章程的两步走,`git fetch --depth 1 origin <全40位SHA>` 取回后再比,
`git log 36b5d6ba..4b5e139dd… -- bots/ game/` ⇒ **exit 0**(可比,不是「空输出=无漂移」那条危险失效),
**1 个 commit**:`4b5e139 strategy 17:45Z: gated bagsalve`。

- `git diff --stat 52042db 4b5e139 -- bots/ game/` ⇒ `bots/FunLib/jmz_func.lua | 59 ++++`,**纯 +59 行,单文件**。
- `git diff 52042db 4b5e139 -- bots/ game/ | grep -cE 'creeppull|pullbeat|ShouldCreepPullLane'` ⇒ **0**。

⇒ **本波两个被测 id 的行为与 16:09Z 预检时逐字节相同**;`bagsalve` 自身 gated 且**不在本波 armed 串里** ⇒ 惰性。
(附一条工具坑:对刚 `--depth 1` 取回的 SHA 跑 `git show --stat` 会把**每个英雄文件都列成新增** ——
那是浅化边界的伪影,不是真 diff;与其父 commit 显式对比才是真的。这条差点被读成「这次改了 127 个文件」。)

### 4.1 门 ①:接线门(在真 tip 上)

```
python3 tools/batch_test/check_armed_wiring.py --cand creeppull,pullbeat \
        --ref 4b5e139dd13e2175fb52b2c0c7934b33ca489083
```
⇒ **exit 0,`all 2 armed ids wired`**:

| id | form | sites | first site |
|---|---|---|---|
| `creeppull` | direct | 1 | `bots/FunLib/jmz_func.lua:7055` |
| `pullbeat` | direct | 1 | `bots/mode_roam_generic.lua:194` |

`creeppull` 的行号从 16:09Z 的 **6996 → 7055**:正是 `bagsalve` 那 +59 行插在上方所致(6996+59=7055,逐数对上)
⇒ **是位移不是改址**,与 §4 的 grep=0 互为独立佐证。

### 4.2 门 ②:载体门 —— 结构上表达不了本波的载体轴(与上轮同,不是新发现)

`seed_draft.py --assert-carrier` 的 term 只有 `hero` / `hero:pos` 两种形式,而 `creeppull` 的门是
**`J.IsCore(bot)`(pos 1-3)的角色门、无任何英雄条件**,`pullbeat` 继承同一角色门 ⇒ 用任何 hero term
测的都不是这个 id 依赖的那条轴;不给 term 则 exit 2 =「什么都没查 ≠ 通过」。

**保守默认(不自裁改门)**:按总监给 `campgrade` 的先例读作 **no-op**,**并附正面证据** ——
`python3 tools/batch_test/soak/seed_draft.py 888 895 896 906` 发牌表(exit 0):

| seed | radiant pos1-3 | dire pos1-3 |
|---|---|---|
| 888 | sven / zuus / viper | drow_ranger / lina / tidehunter |
| 895 | dragon_knight / lina / bristleback | skeleton_king / necrolyte / slardar |
| 896 | luna / lina / tidehunter | slardar / obsidian_destroyer / necrolyte |
| 906 | phantom_assassin / viper / skeleton_king | juggernaut / zuus / bristleback |

4 种子 × 2 边 × pos1-3 = **24/24 核心槽全部有人** ⇒ 载体轴 **satisfied 4/4 verdict FULL**。

**LIMITS(原样保留)**:这只证牌桌上有核心,**不证** `ShouldCreepPullLane` 其余六个前置条件可达;
帧证据仍只能由本波自己买。表达力缺口在 **GH #140**(已于上轮追评交总监),不阻塞发波。

## 5. 发波:参数与实例

**启动决策:启动。** 三条节流条件与 director 时刻闸门逐条:
(i) 距上一波 12:09:32Z **6h 00m 02s ≥ 6h** ✅;更硬的一条 —— director 在机器字段里写死的
「最早 2026-08-23T18:09:32Z」,**实际首台 18:09:34Z,过线 2 秒** ✅(**不提前，也不援引任何“差几秒照发”的先例**);
(ii) 有新东西可测:W3 是 `creeppull,pullbeat` 的**首个独占波**,该组合从未上过机 ✅;
(iii) $28.462 + ~$4.9 ≈ **$33.4 ≤ $45** ✅。

参数逐条按 director 裁定 + §AT.0:钉**全 40 位 SHA**(**不用 `--ref main`**)、`--slots 16`、
**`--rec-slots 12`**(director 明写「不许降」—— 本台自己的保守默认是 8,**裁定压过保守默认**)、
`--hours 2` 看门狗 + `shutdown-behavior=terminate`、`--games 22`。
机型选择:**on-demand `c6i.4xlarge`**,`InstanceLifecycle=None` 已用 `describe-instances` 实测确认。
走 on-demand 不是随手降级:本账户 spot 在 08-20 18:08Z 那次是 **4/4 `InsufficientInstanceCapacity`**,
此后同型波一律 on-demand;64 vCPU 的 on-demand 配额正好容下 4 台,这也是**不能再多开的硬顶**。

| seed | instance | run_id |
|---|---|---|
| 888 | `i-0ac0d1b457b3ec8fc` | `spot_20260823_180934_1_4b5e139dd…_7442b4` |
| 895 | `i-0f9063db3030be717` | `spot_20260823_180937_1_4b5e139dd…_376749` |
| 896 | `i-0117700b42b20a56a` | `spot_20260823_180940_1_4b5e139dd…_c2016c` |
| 906 | `i-0ef0367f94c6fbc73` | `spot_20260823_180943_1_4b5e139dd…_998b48` |

**#98 run_id 唯一性(纪律照做,两条都做了)**:(a) 四次调用**各自跨过一个整秒**(发起于 34 / 37 / 40 / 43 秒);
(b) 启动后用**独立证据源**核对 —— `ec2 describe-instances` 的 `soak-run` 标签取回 **4 个值,`sort -u` 后仍 4 个,两两不同**;
四个 `RUN_TOKEN`(`7442b4`/`376749`/`c2016c`/`998b48`)亦互不相同。**#98 第三次生效。**

## 6. 局数(铁律:每份报告固定一节)

- **(a) 上一波次(12:09Z,已于 14:10Z 收割)最终有效局数**:**275 有效 + 24 暖场 = 299**;
  per-seed(ab/ba)888 **42/26**、895 **42/23**、896 **41/31**、906 **40/30** ——
  **ab/ba 不对称**,原因是 dire wave 被 2h 看门狗截断;`unfinished` **0**。
- **(b) 本轮在跑波次的实时进度**:本报告写于发波后 ~2 分钟,四个 `soak/<run_id>/` 尚无 `analysis.json`
  (实例仍在 boot + 拉仓库 + 生成池阶段)⇒ **实测计数 0,这是预期不是异常**。
  **预期局数 ~275 ± 25**(同拓扑同参数,以上一波为锚;**暖场局不算有效局**)。
  预计 **~20:10Z** 自毁,下一轮(约 20:0xZ–22:0xZ)用 `recover_verdict.py` 从 S3 逐局数据**全量重算**收割。

## 7. 泄漏检查(四层 + spot 请求)

| 层 | 开工 | 收尾 |
|---|---|---|
| running/pending 实例 | **0** | **4**(= 本波四台,**逐个 instance-id 对上**,无第五台) |
| 游离(available)卷 | 0 | **0** |
| 快照 | 1(AMI 底座,唯一常设成本) | 1 |
| EIP | 0 | **0** |
| open/active spot 请求 | 0 | **0** |

**无泄漏。** 收尾这次走 `--leak-only`(**零成本,完全不查花费**)。
本轮收尾**要的不是「0 台在跑」**而是「**在跑的恰好是本波这四台**」—— 有活波次时前者才是异常。

## 8. 验证(铁律 6)

本会话 `bots/` / `game/` **逐字未动**(改动只在 `iterations/` 下:`queue.json` + 本报告 + 章程)
⇒ luacheck / lua 单测**无适用对象**;容器内亦无 `luacheck` / `lua5.1`(`which` 复核 exit 1),
**不声称跑绿过 Lua 全量**。JSON 侧做了实测:`json.load(iterations/queue.json)` **ok**,
且回写按原文件的 `indent=2` 格式化 ⇒ `git diff -w` **仅 2 行**(只有我加的那两处),无全文件重排噪声。

## 9. 交棒(铁律 9:「修好」不等于「做完」)

- **收割棒 → 下一轮批测台**:~20:10Z 后用 `recover_verdict.py ./g "creeppull,pullbeat"` 全量重算,
  必查项照抄上一波的十条(尤其 ⑩ `demclaim` 数 vs `dem21/` `.dem` 数**逐 run 对账**,差值非 0 必须点名到 tag);
  收割时同步兑现 §2 那两个**预登记成本区间**。
- **主判据棒 → 录像组(owner P1 第 4 棒)**:本波 `--rec-slots 12` ⇒ 帧通道 **12/16**,
  按【离开兵线 → 走向己方野营 → 拉仇恨 → 回拖】四段判 **WORKING/SILENT/BUGGY**;
  本波特有的阴性判据仍有效:若仍 SILENT,先看 armed 侧的辅助**有没有「离开兵线走向营地」**——
  它是「触发器没开」与「动作没执行」的分界。**这一棒不必等收割**,`.dem` 一落 `dem21/` 即可开工。
- **`queue.json`**:`strategy-5` 已置 `status=running`,发波全部细节写进它的 `notes`(不散在报告里)。
- **载体门表达力棒**:仍在总监手上(**GH #140**),本轮无新增,不重复交。

## 10. 本次会话 token 用量(铁律 8)

```
TOKENS total_in=2,499,352 out=11,770 turns=29
```
(统计时刻之后的收尾回合不计入。)
