# 批测台 2026-08-23T10:15Z —— 预检轮(不启动,节流门 12:11:11Z 未到)

**一句话**:本轮零 AWS 支出、零启动;把 12:11:11Z 那一波(总监 §AT / GH #140 已裁定)
**全部预检做完**,并在预检里撞出一条会让**下一波之后那一波静默失效**的结构冲突:
**`campgrade` 在钉死的树 `7159ff3` 里根本不存在**,而 §AT.3 恰恰把它条件性批进了那一波。

---

## 1. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` → **worst exit 0**:

- UNLANDED:**0 条**(certifiable window 内无未落地工作;270/293 refs 因浅克隆
  不可判定被 REFUSE,是工具的既有边界,不是新情况);
- cadence + citations:**clean**;
- trunk python 测试:**15 passed / 0 failed**。

上一轮(08:15Z)自检报的 2 条 UNLANDED(`894efa6`/`7bb6711`,英雄组 GH #136 WK 魔棒)
**本轮已消失** —— 对得上:`8426438 hero: WK builds a Magic Wand at last -- GH #136` 现在就在
`origin/main` 上。**当时判「大概率只是还没同步」,本轮兑现,不是漏报。**

## 2. 成本

`check_costs.sh`(免费 `budgets describe-budgets`):**MTD $23.578**,forecast 23.139,
limit $100。$45 launch fence / $90 刹车线 / $100 owner 批准线**全未触及**;
读数 ≪ `COST_CONFIRM_AT=$35` ⇒ **未花 $0.01 调 CE**。

**读数仍是零信息,判据是 refresh 时刻**(04:06Z 立的规矩):`budget refreshed`
= **2026-08-22T21:26:04Z**,与 02:06Z / 04:06Z / 06:11Z / 08:15Z **逐秒相同**
⇒ **同一份快照的第五次复读**。可证伪形式沿用不变:**晚于 06:11Z 的第一次真刷新应 ≈ $26.6**
(两波各 ~$1.5 尚未入账)。本轮零支出 ⇒ 该预测**不因本轮变动**。

## 3. 收割:无新数据(预期)

S3 扫描:

| 前缀 | 本轮 | 08:15Z | 判读 |
|---|---|---|---|
| `soak/` | **168** | 168 | 无新波次(预期,本轮无启动、上轮已收完) |
| `dem21/` | **20** | 20 | 同上 |
| `unattributed/` | **0** | 0 | 前缀不存在 |
| `validation/` 最新条目 | **2026-07-23** | 2026-07-23 | **预期**:标准收割路径是 `recover_verdict.py`,不写 `validation/` |

最新四个 `soak/` 前缀仍是 `spot_20260823_0611{11,13,15,18}_1_7159ff3…`,**08:15Z 已全量收割**
(282 有效局 + 24 暖场 = 306,gpm −93.30 / winrate 0.243)。**本轮无可收之物,不是漏收。**

## 4. 局数(铁律级固定栏位)

- **上一波(已收完)**:`spot_20260823_0611{11,13,15,18}`,树 `7159ff3`,25 id,
  **282 有效局 + 24 暖场 = 306**;四种子两 wave 全齐 `unfinished 0`。
  逐种子 gpm(**按总监 §AT 新口径带号**):`seed=888 gpm=−122.10` / `seed=895 gpm=−119.94` /
  `seed=896 gpm=−105.72` / `seed=906 gpm=−25.46`。
- **本轮在跑波次**:**无**(未启动)⇒ 无实时进度可报。
- **下一波预期局数**:4 台 × 1 种子 × `--games 22` ≈ **88 局上限**,按前两波实测落在
  **278–306**(暖场另计)—— 实际由 2h 看门狗与局时决定,不由 `--games` 决定。

## 5. 为什么本轮不启动

章程 §4b 三条节流门:

| 条件 | 状态 |
|---|---|
| (i) 距上一次例行波次启动 ≥ 6h | **不满足**:上一波 06:11:11Z,本轮 10:06Z,**差 2h05m** |
| (ii) 有新东西可测 | 满足(总监 §AT 裁定的 24-id 归因波) |
| (iii) 当月已花 + 本波预估 ≤ $45 | 满足($23.578 + ~$1.5 ≈ $25.1) |

**只有 (i) 不满足,门槛时刻 12:11:11Z。** 2 小时的缺口**明确不适用** 06:11Z 那条
「差 11 秒照发」的先例(那条自己写明适用面是**分钟/秒级**),与 22:06Z/20:09Z/16:10Z/08:08Z
那批「差 2–4 小时不启动」的先例**同类**。⇒ 不启动,不留痕例外。

### 5.1 预登记:12:0xZ 那次触发怎么办(免得下一会话在门口犹豫)

每 2h 的触发点大概率落在 **12:0x**Z,即门槛前 **约 5 分钟**。事先写死,免得到时候临场发明:

- **缺口 ≤ 2 分钟** ⇒ **照发**,直接援引 06:07Z 那条既有先例(适用面分钟/秒级)。
- **缺口 2–15 分钟** ⇒ **照发,但必须在报告里留痕**,理由只能是立法目的:
  节流的立法目的是「防止每 2h 一波把预算烧穿」(6h 门 ⇒ ≤4 波/天);5 分钟对该目的的
  影响是 **6h → 5h55m,即 ≤4 波/天不变**。**这是本台自己的规则,本台可以按目的解释,
  但不许把它悄悄推广到小时级** —— 上面那条 2h 缺口不启动的判定同时成立,两者不冲突。
- **缺口 > 15 分钟** ⇒ **等下一次触发**。

**同时不变的硬约束**:发之前仍要重跑 §7 的四项预检(树可能又漂了)+ 成本 ≤ $45 fence。

## 6. 本轮的实际交付:12:11Z 波次预检(零成本,四项全过)

总监 §AT / GH #140 的裁定已落到 `test_set.md` §AT.0–§AT.3。本台把启动前**能提前做的全做完**:

### 6.1 armed 串:24 id,**程序取出、程序核对**(不是手抄)

```
l1trade,l5combo,midtp,suptp,tpcommit,tpdying,lf_rescue,teambrain,ownhalf,overchase,fieldregen,wandbleed,capmono,cmrguard,tpdead,zusult,blinkflee,liondrainstop,creeppull,pullcamp,stayfield,stayfield2,fieldbuy,fieldcreep
```

逐元素断言(`sed -n '5204p'` = §AR.0,`sed -n '5292p'` = §AS.0,§AT.0 取自围栏块):

- `AR.0 count = 24`、`AS.0 count = 25`、`AT.0 count = 24` ✅
- `AS.0 去掉 itemtrip == AR.0` → **True** ✅
- `AT.0 == AR.0`(逐元素) → **True** ✅
- `itemtrip ∈ AT.0` → **False** ✅;重复 id → **无** ✅;串长 **218 字符**

### 6.2 钉的 SHA:`7159ff3ef3da17b5716ad491e80c23a6c3818c8e`

- 本地**已存在**(无需 `git fetch --depth 1` 补取);
- `git merge-base --is-ancestor 7159ff3 origin/main` → **exit 0 = YES,是祖先** ✅
  (总监点名的自证步骤,本轮已做,**不要等到实例上才发现 checkout 不到**)。

### 6.3 gate 覆盖:**24/24 有效**(23 字面量 + 1 拼接),在**要测的那棵树上**做的

在 `7159ff3` 上逐 id `git grep`:**23 个有 `IsSoakCandidate('<id>')` 字面量**;
`lf_rescue` 字面量 **0 是预期**(走 `jmz_func.lua:5907` 的
`J.IsSoakCandidate('lanefix') or J.IsSoakCandidate('lf_' .. sub)` 拼接,GH #70 §5 早有记录)。

> **⚠️ 本轮新踩的坑,值得写进硬知识**:第一版用的是**紧模式** `IsSoakCandidate('$id')`
> (括号后无空格),结果报 **3/24**,`missing` 21 个 —— 看上去像一次灾难性的 gate 塌陷。
> 真因是源码写作 `J.IsSoakCandidate( 'x' )`,**括号内有空格**。
> ⇒ **覆盖核对必须用空格容忍的正则** `IsSoakCandidate\( *['\"]<id>['\"] *\)`。
> 这个坑往**惊吓**方向失效(误报塌陷),但它的镜像 —— 模式太松把注释里的 id 数进来 ——
> 往**危险**方向失效(把没接线的 id 认成已接线)。两边都只有「在要测的树上、用登记好的
> 正则、逐 id 打印计数」能挡住,所以上面那张逐 id 表照打不省。

### 6.4 树漂移:钉 SHA 第三次证明必要

`7159ff3` → 现远端 main `0ca2a58` 的 `bots/` 差异:**9 文件 +160/−36**
(`hero_axe` / `hero_lion` / `hero_skeleton_king` / `hero_zuus` / `hero_tidehunter` /
`hero_life_stealer` / `hero_abyssal_underlord` / `aba_site` / `mode_farm_generic`),
来自 5 个 commit(`b9798ea` Lion t15 重测、`498bad4` strategy GH #137 `campgrade`、
`8426438` WK 魔棒 GH #136、`ff90c67`/`5229c7d` GH #139 census)。

⇒ **12:11Z 那一波(钉 `7159ff3`)测的树不含这 9 个文件的改动。** 这句话收割时必须贴着读数写
(镜像 A/B 内部自洽不受影响,这是**语料边界不是缺陷**)。**若改用 `--ref main`,四台
先后 clone 就可能落在漂移两侧 —— 一次镜像 A/B 内部混进两棵树,且没有任何计数会报警。**

## 7. ⭐ 预检撞出的结构冲突:`campgrade` 不在钉死的树里

**事实(两条,都可复现)**:

1. `IsSoakCandidate` 的 id 全集,`7159ff3` → `0ca2a58` 的差集是 **`campgrade` 一个,只增不减**;
2. 它的**唯一调用点** `bots/FunLib/aba_site.lua` 的
   `J.Site.RefreshCamp(bot, J.IsModeTurbo() and J.IsSoakCandidate('campgrade'))`
   由 `498bad4`(strategy 07:55Z,GH #137)引入 —— **`7159ff3` 之后**。

**为什么这是个问题**:§AT.3 把 `campgrade` **条件性批准**进「归因波之后那一波」,
`queue.json` 的 `strategy-4` 也写着「不申请专波,请并进下一波已经在跑的串里」。
可是**如果那一波继续钉 `7159ff3`**,`campgrade` 在实例 clone 出来的树里**没有任何调用点**
⇒ 它 armed 了也是**逐字节 no-op**,读数会呈现为「`campgrade` 无影响」——
**一次假阴性,而且没有任何计数会报警**(串里有它、count 对得上、gate 覆盖表还会给它打 0/1,
正好被读成 `lf_rescue` 那种「预期的 0」)。

**两条路,代价都摆出来,本台不自裁**(章程:不做判断分析、不改 harness):

- **(甲) 那一波改钉 ≥ `498bad4` 的新 SHA。** 得到:`campgrade` 真的上机。
  代价:那一波与 `−93.30 / X` 这对**同树配对**脱钩 ⇒ 它不能再当第三个可比读数,
  且 §AT.3 刚改写的「(乙) = 同树同种子配对波对」下一次要用时得**重新买前半**。
- **(乙) 那一波仍钉 `7159ff3`,`campgrade` 顺延一波。** 得到:配对链完整。
  代价:`campgrade`(GH #137)再等 ≥6h,而它已经在 `queue.json` 里 pending。

**⇒ 归总监,最好在 12:11Z 那一波收割前给结论**(那一波本身两条路都不受影响,
钉 `7159ff3` 是 §AT 已裁定的,本轮预检**不动它**)。

**已交出去的棒**(铁律 9 的连带规则,不许只在报告里说):
GH #140 追评一条(§AT.3 的直接后续,不新开 issue —— `search_issues` 核过 #137 谈的是
`campgrade` 缺陷本身,不是它的**上机时机与钉树冲突**);`queue.json` 的 `strategy-4`
加结构性附注,状态留 `pending`(它等的是总监裁定,不是等本台开波)。

## 8. 队列与其他请求的处置(本轮无状态跃迁)

| id | 状态 | 本轮动作 |
|---|---|---|
| `strategy-4`(campgrade) | pending | **加 §7 的结构性附注**(钉树冲突);等总监 |
| `hero-1`(WK `wkqaim` 域) | pending | 分母侧已于 08-22T14:12Z 交付(153/308 局含 WK;08:15Z 波另有 152/306)。**帧级扫描是录像/英雄组的检测器手艺**,章程内本台已交完 |
| `hero-2`(Axe) | pending | **分母 0**,结构上买不到;等一个含 Axe 的种子(GH #46 追评已给现成答案 `--find axe` → 899/910/911)。**总监 §AT 明确「这一波不许顺手换种子」** ⇒ 本轮不动 |
| `hero-3`(Zeus/CM t15) | pending | 分母已交(306/306),等英雄组做检测器 |
| `hero-4`(Lion t10/t15) | pending | 分母已交;part (2) 结构性空集(0/77 到 lv15) |
| `hero-5`/`hero-6`/`hero-7` | pending | 都是「搭下一波顺风车」的读数请求。**注意**:`hero-6`(WK pos_3 魔棒,`8426438`)与 `hero-7` 落在 `7159ff3` **之后** ⇒ **12:11Z 这一波带不上它们**,要等下一次换钉树。本轮给 `hero-6` 加了这条附注 |
| `strategy-3`(itemtrip) | done | 08:15Z 已收割;§AT.1 的归因波就是为它开的 |

## 9. 泄漏检查(四层 + spot 请求,全免费只读)

开工与收尾**逐层同上**:

| 层 | 开工 | 收尾 |
|---|---|---|
| 非终止实例 | **0** | **0** |
| 游离(available)卷 | 0 | 0 |
| 自有快照 | 1(`snap-…`,160GB,唯一常设成本) | 1 |
| EIP | 0 | 0 |
| open/active spot 请求 | 0 | 0 |

**无泄漏。** 06:11Z 那四台在 08:06Z 就已确认自然收工(跑完 `--games 22`,非看门狗强杀)。

## 10. 验证(铁律 6)

本会话**未改 Lua**(改动仅 `iterations/` 下的报告 / 章程 / queue.json)⇒ 铁律 6 无适用对象;
容器无 `luacheck` / `lua5.1`(已复核,与前几轮一致)。
本轮**唯一的推断项**是「两波共 ~$3 尚未入账」,已挂在 `budget refreshed` 时刻上给出可证伪形式(§2)。

## 11. 下一波(12:11:11Z 门开)—— 预检已过,可直接发

| 参数 | 值 | 出处 |
|---|---|---|
| armed 串 | 上面 §6.1 那 24 个,**逐字节** | 总监 §AT.0 / GH #140 |
| 树 | **`7159ff3ef3da17b5716ad491e80c23a6c3818c8e`**(已验祖先) | §AT.0 表 |
| 种子 | **888 / 895 / 896 / 906**(**不许借机换种**) | §AT.0 表 + GH #46 追评 |
| 拓扑 | 4 台 × 1 种子,on-demand c6i.4xlarge | 既有 |
| 槽 | `--slots 16`,**`--rec-slots 12`**(§AS.2 回退触发器未触发) | §AT.0 表 |
| 看门狗 / 局数 | `--hours 2` / `--games 22`(不再上调) | 既有 |
| 预估 | ~$1.5;MTD $23.578 + 1.5 ≈ $25.1 ≤ $45 fence | §2 |

**必查项(收割时照做,10 条)** = 08:15Z 那 9 条,**加第 10 条**:
**逐 run 核对 armed stamp 里 `campgrade` 不在串内**(它本来就不该在,§7 是说下一波;
这一条存在的意义是:若它意外出现在串里,那它在 `7159ff3` 上是 no-op,读数必须当场作废重发)。
其中第 1 条(暖场 stamp 应 **24/24 = `7159ff3`**)本轮又多了一条独立理由:§6.4 量到的 9 文件漂移。

## 12. Token 用量(铁律 8)

```
TOKENS total_in=2,517,314 out=25,097 turns=27
```

(到统计时刻为止;之后的 commit/push 收尾回合不计入。本轮零 AWS 支出、零启动,
用量全部花在预检与 §7 那条结构冲突的取证上。)
