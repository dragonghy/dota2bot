# 批测台 2026-09-18T09:17:20Z

**刹车第四十八轮持有;零发波、零收割欠、零泄漏。⭐⭐ 本轮两件实质:(甲) 连续四轮冻结的预算快照
解冻了,而它给出的速率点 `$0.282/天` **落在本台自己登记的 `$0.40–0.92/天` 带的下界之外**;
(乙) 上一轮自己挂的诚实边界 ②(「报告级读数不是逐数字读数」)本轮量完了,结论对上一轮**有利**,
但同一把尺子往下走一格,把那 25 份**再拆开**:真正「什么都没举手」的是 **15 份**,窗口 `08-26 → 08-30`
**五天**,上界又是缺陷被发现的前一天。**

---

## 一、开工自检(铁律 10)

⚠️ **第一条命令踩了上一轮钉过的坑,当场改姿势**:首跑用了 `... 2>&1 | tail -40`,脚本自卫逐字
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.` + `it has recurred 5x,
every time as the first command of the round.` ⇒ **`SELFCHECK_EXIT=2  REFUSED(nothing was checked;
this is NOT a pass)`**。⛔ **这一次不记成「管道坑断了」的反例式辩解** —— 上一轮把取法钉成
「`> 文件 2>&1`,⛔ 不加 `timeout`」,本台**第一跑没抄**,第二跑抄了就通过了。**同形第九例,归因是姿势不是工具。**

第二跑 `bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1`:首行逐字
`push gate armed (core.hooksPath=.githooks); \`git push\` now runs the rule 6 static gate`。
已跑完腿:`OK: no unlanded work in the certifiable window.`;
`LUA GATE COVERAGE disk 513 | push gate 348 | 开工自检 leg 138 | known_red 5`、
`UNCOVERED (no automatic reader runs these): 114 of 513 (22%) {'no_manifest_row': 51, 'timed_out': 60, 'too_slow': 3}`、
`UNCOVERED SET GREW -- 2 file(s)` → `test_fieldsip_transfer_receiving_site.lua`(**连续第五轮同一个**,卡 GH #783)
+ `test_lion_w_fight_seed.lua`(`too_slow`)—— **与上一轮同两个,⛔ 不是新的两个**;
`NOW COVERED tests/test_fieldsip_atom_pricing.lua`;
`PY GATE COVERAGE disk 151 | push gate 131 | leg 151`、`UNCOVERED 0 of 151 (0%)`。
⭐ **盘面 512→513 / 闸 347→348 各 +1,而 `UNCOVERED` 仍是 114** ⇒ 新落盘那个 Lua 文件**进了闸**;
⛔ 本轮 `bots`/`game`/`tests`/`tools` **一行未改** ⇒ 两条 `NEW UNCOVERED` 无一条由本轮引入,⛔ 不代修。
**收尾真码见「补记」节**(按上一轮立的取法:后台跑时**收尾前回读一次输出文件再落笔**)。

## 二、预算 —— ⭐ 快照解冻,新速率点在带外(低侧)

闸 (iii) `FENCE_EXIT=3`(**连续第四十八轮是闸拒绝,不是本台的处置**),逐字:

```
actual (MTD)     : $92.292   <- re-read this run, never cached
pending waves    : $0.000   (launched, may not be in MTD yet)
planned          : $0.000
projected total  : $92.292
fence            : $100.00   <- lowest ACTUAL alert not yet crossed (Ruling 1)
brake            : $90.00   (owner's line, not derived here)
operative ceiling: $90.00   = min(fence, brake)
WAVE_FENCE: THROTTLED (exit 3) -- brake, not fence. Stop and report to the owner; do not wait it out.
```

RULING 5/6/7 三行照抄:`accrual scope : 17 region(s) read ... COMPLETE: every region this account has enabled` /
`wave accrual : records after 2026-09-17T19:40:29Z (= 2026-09-18T06:58:29Z - 11.3h lag, clock from budget snapshot)` /
`accrual check : CERTIFIED (0 accruing instances account-wide, read this run) -- pending $0.000 is a reading, not a default.`
⭐ **钟没有降级**,⛔ 未传任何 `--no-*`。`crossing registry: 0 record(s) ... none in force` + `1 retired record(s)`;
`budget filters : none`。

**⭐⭐ 解冻**:MTD **`$92.292`** / 戳 **`2026-09-18T06:58:29Z`**,对上四轮那张冻结快照
(`$92.163` / `2026-09-17T19:59:13Z`)**两量都动了** ⇒ 这是**一个真段不是零段**:

- Δ = **`+$0.129`** / **`+10.9878 h`** ⇒ **`$0.2818/天`**。
- ⭐ **它落在本台登记的 `$0.40–0.92/天` 带的下界之外**(低侧)⇒ 带按算术扩成 **`$0.28–0.92/天`**。
  ⛔ **不收窄、⛔ 不取代、⛔ 不解释为「烧得慢了」** —— 一个 11 小时的短段,遇上 EC2 侧 11.3h 的滞后,
  **段长与滞后同量级**,这正是本章程反复登记的那类「短段读数不稳」的形状。**登记为带外低点,n 增一。**

**月末(到 `2026-10-01T00:00Z`,余 12.709 天)按扩后带重算,两端都写**:

| 速率 | 9/30 收盘 MTD | `$100` 越线 |
|---|---|---|
| **`$0.2818/天`**(本轮新点) | **`$95.87`** | **九月不越** |
| `$0.40/天`(旧带下界) | `$97.38` | 10-07(出月) |
| `$0.92/天`(带上界) | **`$103.98`** | **`2026-09-26T16:03Z`** |

⚠️ **兜底日动了 ~8 小时,方向朝紧**:上四轮逐字结转的是 `2026-09-27T00:00Z`,本轮按同一上界重算得
**`2026-09-26T16:03Z`** —— 位移**全部来自 MTD 本身涨了 `$0.129`**,⛔ **不是速率变了**(上界那个数没动)。
**头号不确定性仍是带宽不是算术**:同一张表里月末落点从 `$95.87` 到 `$103.98`,跨 `$100` 硬线。

**headroom 对 `$90` = `$-2.292`**(⛔ 与上四轮的 `$-2.163` 不同,不得再读成「四轮逐位相同」)。
⚠️ 脚本自打一行照抄:`stamp unknown (fresh container) -- alpha and beta are both zero -- a frozen stamp
over an idle account opens no blind spot`;`alpha = 0` / `beta = 0` 本轮实读;
`forecast 115.334`(⛔ 不解释不采信;它比本台自己的上界 `$103.98` 高 11%,**两者不互证**)。

**GH #801 四行整块照抄**(**默认值**跑,⛔ 未用 `COST_CONFIRM_AT=999` 自救):

```
   >= $35, but headroom to the $90.00 brake is $-2.292
   < $1.10 (cheapest wave) — CE confirmation SKIPPED, NOT passed (GH #801).
   Nothing can launch at this MTD, so confirming it buys nothing.
   Resumes by itself as soon as headroom >= $1.10.
```

**SKIP 不是 pass。** 欠条 `gh801_confirm_headroom_first_live_read` **仍登记为 OWED**,
⛔ **本台不自行结清**(上一轮交总监的那个一句话自问原样结转:判据到底是「那一行打出来了」
还是「headroom ≥ $1.10 那轮真付了 `$0.01`」—— 若是后者,**刹车解除前它在原理上结不清**)。

## 三、收割:零欠(⛔ 不是漏收)

`validation/` **592 对象**、`soak/` **72,079 键** —— 与前四轮**逐位相同**;
最新对象按 RULING 47 取法仍是 `lf_rescue+27ids-d177b87026dd_20260912_1018_run.log`
(`2026-09-12 10:18:26`)= **W69**。在跑波次:**无**。`recover_verdict.py` 未跑 —— **没得收,不是漏收**。

## 四、交棒 ② —— GH #184 **连续第三轮零表态**

现读 `state: open` / **`comments: 3`** / `updated_at 2026-09-18T00:36:03Z` ⇒ 与前两轮**三值逐位相同**。
⚠️ #184 已 open **24 天**,验收是**零 AWS 成本**的。按交棒 ② 严格执行:**只登记戳/评论数,
⛔ 不重复发表同一份证据表**;连带交棒 ③(~4,700 GET 加厚)**以总监裁定为前提 ⇒ ⛔ 本轮未做**。

**交棒 ④(b) 缺陷现读:没修。** `analyze_log.py` 的 `natural_end = fort is not None` /
`winner_by = "engine_natural"` 逐字未动;`referee_surrender` 在 `tools/batch_test/soak/` 下仍 **0 处**
⇒ 刹车解除后第一波的 `winrate` 仍是裁判伪影。

## 五、⭐⭐ 本轮实质:把上一轮的诚实边界 ② 量完,并把那 25 份再拆开

### 5.0 先复现上一轮,再往下走

同一台尺子(全量,非抽样):界后(`>= 20260826`)报告 **925** 份、
收紧判据 `winrate[^\n]{0,40}?([01]\.\d{2,4})` ⇒ 引过 winrate 数字 **62** 份、
其中 `< 20260917` 的 **59** 份、带 `DEGENERATE` **34** / 不带 **25**,
不带的窗口 `20260826 → 20260831`、分布 `batch-desk 24 + director 1`。
⭐ **四个关键数 `62/59/34/25` 与窗口与分布逐位复现** ⇒ 语料同一,可以往下接。
⚠️ 两处不复现,**照登不藏**:分母 `920 → 925`(本轮多 5 份,应为其后落盘的新报告)、
`malformed report names 7 → 23` ⇒ **⛔ 不主张上一轮错**,只登记「分母与畸形名计数两轮不同」。

### 5.1 边界 ②(横幅与那个数字的相邻性)—— 量完了,结论**对上一轮有利**

上一轮自己写的边界逐字:「⛔ 未核对横幅与那个具体数字的相邻性,**25 是「完全无横幅」的形状,
34 ≠「34 个数字被覆盖」**」。本轮先测了一个**更狠的**前提:`DEGENERATE` 这个串在本库里**不止一个主语** ——
全库 **129** 份报告含它,而其中一族是**几何调试输出**(`front tower (...) d_fountain=... => DEGENERATE -> 0/0 -> NaN`),
与胜负通道**毫无关系**。若那 34 份里有靠几何行凑数的,则「34 覆盖」虚高、**25 就是低估**,失效朝危险侧。

**实测:这个假设被证伪。**

- 那 34 份里,**每一行 `DEGENERATE` 都是几何语境**的报告:**0 份**。
- 34 份共 **91** 行非几何 `DEGENERATE`,**同一行自己就点名胜负主语的 82 行**;
  剩 9 行逐条看过(`占位符` / `仍 DEGENERATE` / `通道仍是 DEGENERATE` 一类),**上下文全部是胜负通道** ⇒ **91/91 主语正确**。
- **相邻性(每份取最坏那一个 winrate 数字到最近一条非几何 `DEGENERATE` 的行距)**:
  `≤2 行` **16** / `3–10 行` **14** / `11–40 行` **3** / `>40 行` **1**
  ⇒ **30/34 份的每一个数字都在 10 行内**,**33/34 在 40 行内**;唯一的外点是
  `director/20260910T053000Z.md`(**42 行**,6 个 winrate 数字,横幅在第 27 行与第 235 行,**同主语**)。

⇒ **边界 ② 关闭,方向对上一轮有利**:报告级存在性检验在这批语料上是个**好代理**,
`34` 与「34 组数字被覆盖」的差是**一个外点**,不是一个系统性缺口。**⇒ 25 这个面站得住,不是被虚高的横幅顶出来的。**

### 5.2 ⭐⭐ 再拆一格:25 份里真正「什么都没举手」的是 **15 份**,窗口 **五天**

**立这一格的理由在数据自己身上**:那 25 份里有三份是 `20260831` 的,而 `DEGENERATE` 这个**串**
是 08-31 那道闸落地才有的 —— **闸之前的报告不可能带它,但它们可以用散文举手**。
于是把判据从「有没有那个串」换成「有没有**任何一句**关于胜负通道的散文自陈」
(`占位符|恒等式|横扫|测不到臂|不得再引|撤回|伪影|不是读数|minority side`):

| | 份数 | 说明 |
|---|---|---|
| 25 份里 **报告内任一处**有散文举手 | **10** | 举了手,只是没用那个串 |
| 其中 **每一个** winrate 数字都在举手 10 行内 | **4** | `20260828T091330` + `20260831` 的三份(**发现日自己**) |
| ⭐ **报告里什么都没举手** | **15** | 48 个引用中的绝大多数落在这里 |

**⭐ 判别子又一次在数据自己身上**:那 **15** 份的日期区间是 **`20260826 → 20260830`**,
而全库第一份点名该缺陷的报告是 **`batch-desk/20260831T032500Z.md`**(§4「胜负通道已经测不到臂了」)
⇒ **上界正是发现日的前一天**。上一轮量出「25 份窗口的上界 = `DEGENERATE` 首次出现那天」,
本轮用**另一把尺子**量出「15 份窗口的上界 = 缺陷被发现那天」——
**两把独立的尺子、两条相邻的边界,都落在同一个事件上** ⇒ 这不是散落的遗漏,是**一个连续 5 天的前置窗口**。

**⭐ 而且这个面里有一个已被点名的确凿落地引用,不是理论**:
`director/20260830T220607Z.md`(唯一一份非批测台的)落在「什么都没举手」那一桶,
它逐字用 `+23.55 gpm` / **`winrate 0.503`** / `deaths 0.00` 论证「**没有任何负面**」= 铁律 2 条件 (b);
而 `20260831T032500Z.md` §4.4 第 2 点正是**点名要撤回**的那一处(§CO.4 里 W29 的 `winrate 0.503`,
该波实为 **227:1 的 dire 横扫**,`0.503` 是恒等式产物)。⇒ **15 这个面不是空的。**

### 5.3 诚实边界(七条,逐条)

① **关键词启发式,非语义解析**:`WARN` 那串词是**手搭的**,换一种措辞的举手会被漏判
⇒ **15 是「没举手」的上界、10 是「举了手」的下界**(失效方向:**高估缺口**,对本台自己不利那侧)。
② 「报告内任一处有举手」是**报告级**读数;`≤10 行` 那一列才是逐数字的代理,**⛔ 两者不可互换**。
③ ⛔ **未核对那 15 份里的数字是不是界后语料** ⇒ 与上一轮同款,**15 仍是复查面的上界,不是「15 个错数」**。
④ ⛔ **不点名任何 promote/(b) 判定引过它们** —— §5.2 末尾那一处是 `20260831T032500Z.md` **自己**点的名,
本台只登记「它落在这一桶里」,⛔ 不重开已关档裁定、⛔ 不代裁要不要撤回。
⑤ 几何族的判据是 `d_fountain|front tower|0/0|NaN` 四个词,**也是手搭的**;它只用来**证伪**一个会让
读数朝危险侧失效的假设,证伪成立即止,⛔ 不反过来主张「非几何 = 一定是胜负主语」(那一步是靠逐行看的 91 行)。
⑥ 窗口起点 `2026-08-26` 取自既有界后纪元定义,⛔ 未重新论证。
⑦ 全库 `DEGENERATE` **129** 份是**存在性**计数,⛔ 未分主语 —— 分了主语的只有那 34 份里的 91 行。

**取法可复现(零 AWS、零 S3、纯本地)**:语料全在仓库里;正则 + 行距 + 关键词分层,聚合后只回传汇总表。

## 六、局数

本月无新波次;本轮**未读 `analysis.json`、未读 `verdict.json`**(⛔ 0 局)。

## 七、成本(铁律 1 三段式)

**零 EC2 / 零 CE / S3 读取 `~74` 个对象(出网未计价)** —— `s3 ls --recursive` 2 次
(592 键 + 72,079 键 ⇒ `ceil(592/1000) + ceil(72079/1000)` = **1 + 73 = 74 LIST**),**不下载任何对象**。
⚠️ **与上一轮不一致,照登**:上一轮在**逐位相同的两个键数**上写的是 `~79 LIST`;本轮的算术给 **74**。
⛔ **不主张 79 错**(可能用了别的页大小或把别的调用算了进去),只登记「同一对键数两轮得出两个数」,
**并把算术写出来给下一轮直接核**。**本轮新增 EC2 计费 `$0.00`**;`check_costs.sh` **未买 CE**(GH #801 SKIP)。
⭐ §五那两个新测量**一个 S3 对象都没碰**。

## 八、泄漏五条独立路径全零(⛔ 不合并成一句)

① `check_costs.sh` 自带区块**空**(`COSTS_EXIT=0`);
② `describe-instances` 五态(`pending,running,shutting-down,stopping,stopped`)**不加 tag 过滤** ⇒ `INST_EXIT=0` / **`LINES=0`**;
③ `describe-spot-instance-requests State==open||active` ⇒ `SIR_EXIT=0` / **`SIR_LINES=0`**;
④ 闸 (iii) 账户级 **17 区 COMPLETE** + `CERTIFIED (0 accruing instances account-wide, read this run)`;
⑤ AMI 仍 `ami-0a990a26d89c66547` 一张(`dota2bot-batch-20260718`,常设成本非泄漏),快照无新增。

## 九、发表纪律:⛔ 本轮零发表

零 issue、零评论。⚠️ 自问过该不该把 §五 发到 #184:**否**,理由与上一轮**同款且更强** ——
#184 三值连续两轮冻结、上一轮那条长评论**至今无人读**,此刻追第三条**增加的是噪声不是信息**;
而这个数**不会过期**(语料在仓库里)⇒ **棒交下一轮**:总监一旦表态,
**连同存档侧 `0/185`、报告侧 `25 份/6 天`、本轮 `15 份/5 天` 三个读数一起答**。
⇒ 本轮**未跑** `claim_precheck.sh`(⛔ 无草稿可检)—— **⛔ 这是「没有可发表的东西」,不是「检查通过」。**

## 十、通知判据(09-16T18:15Z 收窄的四条,逐条现读)

MTD `$92.292` < `$100` ✗;`alpha = 0` ✗;headroom `$-2.292` < `$1.10` ✗;
今天 `2026-09-18` < 兜底日 ✗ ⇒ **四条一条未中 ⇒ ⛔ 本轮不推送。**
⭐ 与前几轮同样严格:§二的兜底日虽然朝紧动了 ~8 小时、§五虽是新测量,但
**两者都不改任何阈值、都不需要 owner 做任何决定**(刹车僵局的球在 **GH #721** 的 owner 侧,已推送过两次)
⇒ **第 48 次「刹车仍持有」对 owner 是零信息**。⚠️ 09-18T00:24Z 那轮交总监的「要不要加第 ⑤ 条(量具级缺陷)」
**仍零表态** ⇒ 判据仍是那四条,⛔ 本台不自行改判据。
⚠️ **交下一轮一条要盯的**:兜底日现读 `2026-09-26T16:03Z`,**距今 8.4 天**;
第四条判据一旦够到,推送就该发,**⛔ 不要等到那天才开始算**。

## 十一、没做的事

⛔ 未发波(闸 exit 3);⛔ `bots`/`game`/`tests`/`tools` **一行未改**;
⛔ 未做交棒 ③ 的加厚;⛔ 未重开任何已关档裁定;⛔ 未代裁 §CO.4 那处引用要不要撤回;
⛔ 未改 `state.json`/`test_set.md`/`OWNER_PRIORITIES.md`/`queue.json`;
⛔ 未自行结清 GH #801 欠条;⛔ 未收窄速率带(只按算术扩了低侧);
⛔ 闸 (i)/(ii)/(iv) **未跑**(发波轮的闸;(iii) 已 exit 3)—— **未跑不记成通过**。

---

## 补记(第一次 push 之后,GH #290 顺序)

**(A) 铁律 6 三条腿(两次 push)** —— 逐字:
`GATE_EXIT=0  CLEAN (iron rule 6 static half passed)` /
`py gate: 134 ran, 0 findings, 0 uncertifiable, 44.6s` /
`lua gate: scope = 2 path(s) changed vs the pushed refs' base (base=fallback-merge-base)` /
`lua gate: SKIPPED BY SCOPE -- this push touches no bots/game/tests path.`
⚠️ **第三条腿是 SKIPPED BY SCOPE 不是绿**(本轮只改 `iterations/` 两个文件)。
`ARM_EXIT=0`(`core.hooksPath=.githooks`,开工自检已上膛);**⛔ 未用任何 `RULE6_BYPASS`**;
`PULL_EXIT=0`(`Current branch claude/great-sagan-r9xflv is up to date.` ⇒ 无 rebase、无 `--force-with-lease`);
`PUSH1_EXIT=0`(`* [new branch] claude/great-sagan-r9xflv`)/ `PUSH2_EXIT=0`(`75b0cf4b..da79e8e1  HEAD -> main`)。
⭐ **按文档顺序先分支后 main,memo 自己把理由打出来了**,逐字
`RULE6_MEMO=REUSE  a green three-leg reading taken 2026-09-18T09:22:43Z on THIS EXACT tree.`
⇒ 分支推未移动 `origin/main`,第二推 memo 免费答掉,⛔ 未反序(RULING 69 / GH #865)。

**(B) 自检真码:⛔ 本轮不可得,并且这一次是实证不是推断。**
收尾按上一轮立的取法**回读了输出文件**(`/tmp/sc.log` **675 行**,停在 `=== trunk health (fast Lua detectors) ===`),
**收尾三行横幅一个都没打** ⇒ **无 `selfcheck worst exit`,⛔ 不写、⛔ 不读成通过**。
⭐ **与上一轮的差别要记住**:上一轮把「我现在读不到」写成了「这轮读不到」、事后订正;
本轮**多做了一步** —— `pgrep -af routine_selfcheck` 现读 **进程仍在**(pid 513 / 18665)
⇒ 「本轮不可得」这句话**这次有实证支撑**,不是从「读不到」推出来的。
按铁律 7/11 ⛔ 不空转等它。**未跑完的腿这轮没人看过**(已跑完的那几条见 §一)。
**取法再补一条给下一轮**:回读之外**再 `pgrep` 一次** —— 「文件没写完」与「进程已死」是两件事,
只有后者才允许下「这轮跑不完」的结论。

**(C) 收尾泄漏复查**:`check_costs.sh --leak-only` → **`LEAK_EXIT=0`**,
`running/pending instances` 区块**空**,常驻仍只有 AMI `ami-0a990a26d89c66547` 一张
⇒ **收尾与开工两次读数一致**。

**(D) 发表**:⛔ 零 issue、零评论 ⇒ `claim_precheck.sh` **未跑** ——
**⛔ 这是「没有可发表的东西」,不是「检查通过」。**

**(E) token 用量(铁律 8)**:`TOKENS total_in=4,166,466 out=35,566 turns=37`(统计后的收尾回合不计入)。
参考上一轮 `3.16M / 28.7k / 32`,本轮**回升约 32%**;归因两条、都不是空转:
① 章程 2.6 MB **仍是按行区间取**(⛔ 从不全读),但本轮多取了一段(`10600–10908` 那次取大了,
已被系统落盘截断,**这是一次可避免的浪费,登记下来**);② §五那两个全量普查**在 python 里聚合后只回传汇总表**
(925 份报告、129 份 `DEGENERATE` 语料**一份都没贴进上下文**)⇒ 普查本身的 token 成本仍接近零。
**交下一轮**:章程取行区间前先 `grep -n "^- 2026-.."` 定位,⛔ 不按猜的行号取大段。
