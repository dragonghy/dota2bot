# batch-desk 工作单元报告 — 2026-08-19T02:09:19Z

## 1. AWS 引导
`session_setup.sh` 成功。身份:`arn:aws:iam::581436534924:user/dota2bot-agent`
(受限 batch-runner IAM 用户,确认正确)。

## 2. 成本检查(启动前)
MTD = **$3.45**(远低于 $90 刹车线)。启动前无在跑/挂起实例,无泄漏。

## 3. 收割 — 上一波次(2026-08-19T00:11Z 触发,14-id 全集 vs 稳定版,种子 851-854)
两台实例(`spot_20260819_001001_1_main` 种子 851/852、`spot_20260819_001007_1_main`
种子 853/854)均已自毁终止(本轮检查时无在跑实例)。按章程标准收割路径,从 S3 拉全部
`*.analysis.json`(194 个文件)用 `recover_verdict.py` 逐局重算(未采信实例自产的单份
verdict)。

**发现:种子 852 完全没有数据** —— 只找到 851/853/854 三个种子的 mirror 戳,`96f49dc`
(裸 commit 戳,非 mirror 格式,属于同实例上跑的其他游戏,与本候选无关,已过滤)。种子
851 所在实例大概率整段 4h 窗口都耗在种子 851 上(该种子单边就有 41+19=60 局),没能
轮到种子 852 就到看门狗自毁点 —— 不是抢占丢失,是同一 4h 窗口内两种子分配不均。

**重算结果(3 种子,不含 852)**:

| seed | ab_games | ba_games | gpm | xpm | deaths | last_hits |
|---|---|---|---|---|---|---|
| 851 | 41 | 19 | -14.69 | -21.30 | +0.15 | -1.10 |
| 853 | 42 | 32 | -2.75 | -3.26 | +0.12 | -0.17 |
| 854 | 32 | 16 | -63.80 | -41.93 | +0.47 | -1.12 |

均值:gpm **-27.08**,xpm -22.16,deaths +0.25(候选更差),last_hits -0.80。
`comps_better`:gpm 0/3、xpm 0/3、deaths 0/3、last_hits 0/3 —— 三个种子全部同向偏差
(候选组比稳定版差)。`recover_verdict.py` 自动判读:`hold_or_reject`(仅工具阈值输出,
非批测台判断)。

这与 07-31 已记录的同一 14-id 组合减 wandbleed/tpwatch 的历史 HOLD 结论
(gpm -18~-26,详见 `iterations/state.json`)方向一致,是该已知残差问题的又一数据点。
判读留给协同组/录像组,批测台仅提供数据。

## 4. 本轮启动 — 补种子 852
- 队列 `iterations/queue.json` 仍为空(`requests: []`, `done: []`)。
- 按章程优先级落到 (b),但鉴于上一波次缺一个种子未达 4-seed 判定门槛,选择**补跑单个
  种子 852**(同一 14-id 组合)而非重开全新 4-seed 波次,更省钱且能补齐上一波的数据集。
- 启动前核对树:`git ls-remote origin main` → `ce5c3d23b5f951fd7c1f0c9f844cedd929924279`。
  与本地 HEAD 一致(本会话执行了 `git push origin HEAD:main` 完成同步;之前 main 落后
  10 个 commit,现已追平)。
  - 核对 96f49dc(上一波测试的 commit)到 ce5c3d2 之间 `bots/`/`game/` 唯一改动是
    `bots/FunLib/jmz_func.lua`(commit b1a3520,l1xpsoak 绝对锚点重设计)——该改动
    **gated 在 `l1xpsoak` 后面,不在当前 test_set.md 的 14-id 集合内**,对本次测试的
    候选组合行为无影响,种子 852 与前 3 个种子在同一有效代码路径下可比。
- 工具:`spot_run.sh --validate`,先 `--dry-run` 核对计划无误再实跑。
- 实例:`i-0f4ab8e723c013387`,run_id `spot_20260819_020910_1_main`,c6i.4xlarge spot,
  16 槽,4h 看门狗自毁,单种子 852,15 局。
  `s3://dota2bot-batch-results-4924/soak/spot_20260819_020910_1_main/`
- 测试 commit:`ce5c3d23b5f951fd7c1f0c9f844cedd929924279`(= origin/main 当前 tip)。
- 预估成本:c6i.4xlarge spot ≈ $0.25/h × ~1.5-2h ≈ **$0.4-0.5**。

## 5. 成本检查(结束前)
MTD 仍 $3.45(账单滞后,符合历史规律)。`describe-instances` 确认只有刚启动的这一台
在跑(`dota2bot-soak-spot-1`,spot,running)。**无泄漏。**

## 6. 局数汇总(章程要求)
- **上一波次(种子 851/853/854,种子 852 缺失)最终有效局数**:851 = 41(radiant 侧)+
  19(dire 侧)= 60 局;853 = 42+32 = 74 局;854 = 32+16 = 48 局。三种子合计 182 局
  mirror 有效局(不含 12 局无关的 `96f49dc` 戳游戏)。ab/ba 明显不对称(如 854 是
  32 vs 16),按 recover_verdict.py 的口径仍取两侧均值再跨侧平均,已在上表体现。
- **本轮在跑波次实时进度**:刚启动(02:09 UTC),尚无 analysis.json 产出,预计
  15 局(--games 15)在 ~1.5-2h 内跑完并自毁上传;暖场局不计入有效局,下次触发按标准
  收割路径核实实测局数。

## 7. 待跟进 / 上报
- 未触发预算刹车(MTD $3.45)。
- 种子分配不均导致单波内某种子完全无产出,不是抢占问题而是"单波 4h 窗口 + 单种子局数
  过多吃满时间"——已用补种子的方式规避,未对 harness 提 issue(非 bug,是本轮参数
  组合偶然导致,后续若重复出现再考虑给 `--validate` 加单种子超时保护)。
- 14-id 全集连续两个数据点(07-31 缺 wandbleed/tpwatch 的 12-id 子集,08-19 全 14-id)
  均显示明显负 gpm 残差且方向一致 —— 留给协同组/录像组判读,不在此报告下结论。

## 8. 涉及路径
- `iterations/queue.json`(空,未变)
- `iterations/streams/test_set.md`(本轮测试组合来源,未变)
- `iterations/state.json`(历史残差记录参照)
- `tools/batch_test/soak/recover_verdict.py`(标准收割路径)
- `tools/batch_test/aws/spot_run.sh`(补种子启动脚本)
- S3:`soak/spot_20260819_001001_1_main/`、`soak/spot_20260819_001007_1_main/`(已收割)、
  `soak/spot_20260819_020910_1_main/`(新启动,跑中)

## 9. Token 用量
TOKENS total_in=5,669,425 out=33,311 turns=67
