# 批测台 2026-09-06T15:20Z — 不发波轮:`$50` 由**实测**越线(CE `$50.804`),而同一个滞后把**围栏冻在一档太低**

**一句话**:上一轮靠 SIR 重建出来的「已越 `$50`」本轮**由付费 CE 直接量到**(`$50.8036`,
而 `budgets` 仍逐位停在 `$39.328`);⭐⭐⭐ **本轮头号发现**:同一份滞后让 `wave_fence.py`
在**同一个 `projected total $55.773`** 上给出两个相反判决 ——
按 `budgets` 的陈操作数是 `THROTTLED (exit 3)`(围栏 `$50`),
按 CE 实测的操作数是 `CLEAR (exit 0)`(围栏 `$80`);**翻转全部发生在「哪一档算已跨」上,
不在投影上**。⛔ **本轮仍不发波**:活工具 exit 3 是有约束力的那一个,且即便按 CLEAR 读,
跨档也需要总监当轮明确裁定 —— **两条独立理由指向同一个动作**,本台不需要先解决围栏之争
就能定本轮,也**不许**为了发波去说服自己越过一道红闸。

---

## 一、自检(铁律 10)

⚠️ **管道那一坑第 10 次踩**:开工第一条命令仍写成 `... | tail -60`,脚本逐字自拒
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked`,
并自陈「evidence discipline 3;已复发 5 次,每次都是当轮第一条命令」。
**那个 `EXIT=2` 是 REFUSED 不是通过。** 随即改重定向重跑。

⛔ **而重跑那次也没交出判决,原因是上一轮交棒 ⑤ 的另一半也没照办**:交棒写的是
「**放后台给足 ≥45min**」,本台给的是 `timeout 900`(15 min)⇒ 日志停在
`=== trunk health (fast Lua detectors) ===` 就断了,**没有 `selfcheck worst exit` 那一行**。
⚠️ **harness 的完成通知说 `exit code 0`,那是末尾 `echo` 的码不是自检的码**(章程点名过的坑)
⇒ **本轮那一跑 UNCERTIFIABLE,不是通过**。已用 `nohup` 无超时重跑,真码见第九节。

**断点之前真跑过的腿照登**(它们在断点之前就完成了):
- `cadence`:**2 条 `GAP cadence strategy`**(`09-05T22:37Z→09-06T02:11Z`、`09-06T07:30Z→11:05Z`,各 3.6h)⇒ **不是本台**。
- `owed-executions`:registry 9 行,点名本台的仍是
  **`campgrade_wave_type_premise_reading`(GH #480 / #241)—— 连续第六轮欠着**,
  ⭐ 欠的理由连续第三轮是**钱**(闸 iii),不是遗忘:闸 (i) 本轮 `UNLOCKED`(第四节)。
- `queue-rulings`:`RIDESHARE (rule this round; outcome per owner P4.2, not §BB.4): 1`(`hero-38`)
  —— ⭐ **工具本轮已把措辞改成引 owner 的 P4.2**,本台连续三轮点的那处规则冲突**已修**,不再重复上报。
- `rc.sh` 自测两条 `UNCERTIFIABLE`,理由逐字 `no lua5.1 on PATH`(GH #171 同族,时序产物)。
- python 侧 `trunk-red(python)` **UNCERTIFIABLE**(`5a0` 腿 `84 file(s) ... in 120.1s (budget 120s)` 超预算)
  ⇒ ⚠️ 照抄腿自己的边界:`This line is NOT a pass, and it is NOT evidence that trunk is red`。
  ⭐ `84 file(s)` 与上一轮**逐位相同** ⇒ 按工具自己那句「跨轮读 `NOTE` 行分辨集合变大还是容器变慢」,
  本轮动的**不是集合**。

⭐ **「工作树红 = main 红」的结构性论证本轮同样成立**:
`git diff --stat origin/main -- bots/ game/ tests/ tools/` 为空(本会话改动全在 `iterations/` 下)
⇒ 两个命题是同一个,**不需要 `git stash`**。

## 二、⭐⭐⭐ 成本:`$50` 不再是重建出来的,是量到的

### 2.1 两个来源本轮**第一次分岔**

`AWS_SETUP_EXIT=0`(`arn:aws:iam::581436534924:user/dota2bot-agent`),`COSTS_EXIT=0`。

| 来源 | 读数 | 备注 |
|---|---|---|
| `budgets describe-budgets`(免费) | **`$39.328`** | `budget refreshed 2026-09-06T06:02:46Z` —— ⚠️ **与 09:12Z / 12:12Z 两轮逐位相同、刷新时点也逐位相同** |
| `ce get-cost-and-usage`(自动复核,`$0.01`) | **`$50.8036495163`** | ⭐ **上一轮这两个数是逐位相同的 `$39.3278`;本轮分岔 `$11.476`** |

⇒ **`budgets` 对 EC2 的滞后本轮量到 ≥ 9.3 小时**(`06:02:46Z` → `15:20Z`,且仍在计)——
落在 `AGENTS.md` 记的 4.3–11.3h 区间的上半段。
⇒ ⭐⭐ **`$50` 档已越线,而这一次不是重建、是账单侧读数**:`$50.804 > $50`。

### 2.2 归因(付费 CE 一次,`$0.01`,按机型;逐位对得上)

| 机型 | 金额 | 归属 |
|---|---|---|
| `c6i.4xlarge` | **`$20.0961`** | **本台**(批测机型) |
| `c7a.16xlarge` | **`$23.2805`** | **外来** `final-table-trainer` |
| `r7a.16xlarge` | **`$3.3186`** | **外来** `final-table-trainer` |
| `NoInstanceType` | `$4.1084` | 存储/传输等,未拆 |
| **合计** | **`$50.8036`** | 与上面的 CE 总额**逐位相同 ⇒ 拆分完备** |

⇒ **账单侧**:外来 **`$26.599`(52.4%)**、本台 **`$20.096`(39.6%)**、未拆 `$4.108`(8.1%)。
⭐ **顺带一条确认**:CE 里**没有 `c6a` 行** ⇒ **整个九月没有任何一波走到过容量降级阶梯第 (2) 步**,
18 波全部留在 `c6i.4xlarge`。

### 2.3 ⭐⭐ 上一轮的 SIR 重建法:**方向被证实,数值被证明偏低**

- **证实**:上一轮宣称有 `$15.345` 已花未落账;本轮 CE 比上一轮多出 **`$11.476`**。
  **方向与量级都对得上** —— 这是 SIR 重建法**第一次被一次更晚的 CE 读数复核**。
- ⛔ **但它同时是个下界,不是估计值**:上一轮把 `c7a $17.109` 当作**已结清**、未给它留 pending,
  而它本轮长到 **`$23.281`(+`$6.172`)**;`r7a` 重建值 `$14.245` 对 CE 的 `$3.319`
  ⇒ **仍有 `$10.926` 没落账**。
  ⇒ **诚实 MTD ≥ `$50.804 + $10.926` = `$61.73`**,外加 `c7a` / `c6i` 两条未知的尾巴。
  **上一轮那个 `$54.673` 偏低了至少 `$7`。**
- ⇒ **诚实归因**:外来 ≈ `$23.281 + $14.245` = **`$37.53`(61%)**;本台 ≈ **`$20.10`(33%)**。
  **越线的钱里,大头仍然不是批测的,而且占比比上一轮更高。**

⚠️ **一处不许含糊**:`$61.73` 是**下界**,不是读数。`c7a` 的尾巴本轮**无法量**——见第六节。

## 三、⭐⭐⭐ 头号发现:同一个滞后把围栏**冻在一档太低**,判决因此翻转

### 3.1 两次运行,同一个投影,相反的判决

**活跑(有约束力的那一个),`FENCE_LIVE_EXIT=3`**:

```
accrual check    : CERTIFIED ZERO (0 accruing instances account-wide), but --pending $15.345 was given and is used as the larger claim.
actual (MTD)     : $39.328   <- re-read this run, never cached
pending waves    : $15.345 ;  planned : $1.100 ;  projected total : $55.773
  ACTUAL alert   : $50.00   NOT yet crossed   AWS state=OK
fence            : $50.00   <- lowest ACTUAL alert not yet crossed (Ruling 1)
operative ceiling: $50.00   = min(fence, brake)
WAVE_FENCE: THROTTLED (exit 3) -- do not launch. Headroom was $-4.673, this wave needs $1.100.
```

**反事实(离线,把 CE 实测喂进同一个工具),`FENCE_CE_EXIT=0`**:

```
actual (MTD)     : $50.804 ;  pending : $3.869 ;  planned : $1.100 ;  projected total : $55.773
  ACTUAL alert   : $50.00   crossed
fence            : $80.00   <- lowest ACTUAL alert not yet crossed (Ruling 1)
WAVE_FENCE: CLEAR (exit 0)  ...  headroom : $24.227 after this wave
```

⚠️ **反事实那次自打的两行边界,照抄**:
`source : OFFLINE (--actual/--limit/--thresholds). This is for tests and post-hoc audit; a launch must use the AWS read.`
`accrual check : SKIPPED, NOT CERTIFIED (offline mode: no live account to enumerate). $3.869 below is an assumption, not a reading; quote this line in the round's report.`

### 3.2 ⭐ 立案句:**`projected total` 两次逐位相同(`$55.773`),翻转 100% 在「哪一档算已跨」上**

这不是「一个数不准」,是**判据读了一个陈操作数**。而 `wave_fence.py` 的立案故事(GH #504)
写的正是「一个**导出值**被写成了**字面量**」;本轮它每轮都重导了,
**但导出所用的输入落后现实 ≥9.3 小时**。

⭐⭐ **两个失效方向相反,这是本条最该被读到的一半**:
- 陈 `actual` 让**投影**偏低 ⇒ **偏放行**(这正是上一轮 `CERTIFIED` 那条盲区,GH #565);
- 陈 `actual` 让**已跨集合**偏小、围栏偏低一档 ⇒ **偏拦截**。
- 本轮**第二个方向压过第一个** ⇒ 本台被拦在 `$50`,而诚实位置早已过 `$50`、诚实围栏是 `$80`。

⚠️ **诚实边界:这是有界的阻塞,不是死锁。** 九月已花的钱不会变少 ⇒ `budgets` 一旦刷新过
`$50`,围栏自己变成 `$80`,本条自解。**但它的时长由一次没人控制的 AWS 刷新决定,
且已经跨三轮持续 ≥9.3h。**

### 3.3 本轮的决定,以及它为什么**不依赖**上面那场争论

⛔ **不发波。两条独立理由指向同一个动作:**
1. **活工具 `exit 3`**。章程逐字:「**0 才可发;2 和 3 都不是通过**」;工具自己也写
   `a launch must use the AWS read`。**离线那次是审计不是许可。**
2. **即便按 CLEAR 读**,`$50` 是 owner 可见告警档,**跨档需要总监当轮明确裁定** ——
   本轮没有。

⇒ 本台**不需要**先赢下围栏之争就能定本轮;⭐ 而**反过来的动作是不许做的**:
上一轮本台推翻的是一个**绿**工具(总监事后裁「你是对的」),那是往**少花钱**的方向;
本轮若推翻一个**红**工具,那是往**花钱**的方向 —— **不是同一种动作,不援引上一轮的先例。**

## 四、闸 (i):UNLOCKED —— 本轮的阻塞确实只是钱

`wave_throttle.py`(**不带管道**,`THROTTLE_EXIT=0`):锚点 W50 末台 `2026-09-06T03:20:34Z`
⇒ `unlock 09:20:34Z`,`now 15:20:09Z`,**`margin +21575s past unlock`**,**`UNLOCKED (exit 0)`**
(`slate spread 13s`;另打 Y1 候选并按 ruling 3 取最晚者)。

## 五、收割:本轮无新数据(查过的,不是默认的)

- `s3://.../soak/` 最新四个前缀就是 W50 那四台(`...295740 / e56ba0 / f2182d / eff51c`),
  **已由 06:14Z 那轮 4/4 全量收割完毕**。
- `validation/` 最新对象仍是 **07-23** 的历史物。
- 本轮零实例、零新 run ⇒ **无收割义务,不重复收割**。
- **队列**:76 条 open 里,需要**新波次**且带可发裁定的 **0 条** —— 绝大多数是
  `ROUTED_ARCHIVE_SCAN` / `APPROVED-SCAN`(零 EC2 归档遍历,不归本台发波),
  `strategy-31/32` 是 `ROUTED_RIDESHARE`(搭车,等下一波)。**本轮无被钱耽误的显式波次请求。**

## 六、泄漏检查 + ⭐ 一条新的窗口读数

- `describe-instances --region us-west-2`,**全状态、账号级、不带过滤器** ⇒ **`Reservations` 0 条**;
  `pending,running` 的 `soak-run` 标签 ⇒ **空**(`RC1_EXIT=0`)。
- `describe-spot-instance-requests`(账号级)⇒ **0 行**。
- 常驻仍只有 AMI `ami-0a990a26d89c66547`。
⇒ **本台无泄漏;外来机也确实不在了。**

⭐⭐ **新读数(交总监,直接影响 GH #565 的修法):上一轮那两条 SIR 已经过期了。**
12:12Z 那轮的重建**全靠**两行 SIR(`sir-rmaqhkvp` / `sir-ctifj6bp`);本轮同一条查询返回 **0 行**
⇒ **窗口 ≲3 小时**,而不是 GH #375 记的「terminated 实例约留 1 小时」的那条(SIR 是另一张表)。
⛔ **后果不是不便,是可证性**:上一轮那个 `$14.245` **本轮已无法从原始证据复算**
—— 它现在只能靠上一轮报告里抄下来的数字活着,而第 2.3 节刚证明**那份重建偏低**。
⇒ **GH #565 建议的「按 SIR 补回已终止机时」必须在花钱的那一轮当场跑,过一轮就取不到证据**;
取不到时按 #565 的验收条件应打 **`UNCERTIFIABLE` 而不是 `CERTIFIED`**。

## 七、局数(章程第 7 步)

- **(a) 上一波 W50 最终有效局数**(上轮已结清,照登不重算):四粒全配对、零抢占 ——
  `5813 ab30/ba12`、`5830 ab27/ba15`、`6072 ab32/ba13`、`6128 ab42/ba16`;
  **`scored 187`**、gpm **`+13.76`**、`comps_better` **2/4**;
  ⚠️ 均值由单粒 `6072` 扛着(去掉后 `−21.53`)—— **裁定权在总监,本台不判**。
  winrate 第九次 `DEGENERATE`(`engine_natural 211/211`)⇒ 按 §CT **不写成读数**。
- **(b) 本轮在跑波次**:**无**。零实例、零新局。

## 八、⭐ `$1.10` 常数(GH #454):一个**自上而下**的新校验点,两条触发腿都没触发

九月 `c6i` 账单侧 **`$20.0961`**,对应
**18 个四机波(W33–W50)+ 4 台登记在案的补跑机 + Y1 的单机**
⇒ **≥ `$1.058`/波(账单侧,已落账部分)**。

⭐ **这是一条与既有方法不同族的校验**:章程的常数一直由**每波**「机时 × 现货价」重建而来,
这一条是**月度总额自上而下除**,**不依赖任何一次逐波重建**。两条路给出的数**互相靠得很近**
(`$1.058` vs 常数 `$1.10`),⚠️ 而 `$1.058` 是**下界**(月末那几波的尾巴还在落账)。

- **(乙1) 未触发**:⛔ 本台**拒绝**把 `c6i` 的本轮增量(`$18.505 → $20.096`,`+$1.591`)
  读成「W50 实测 `$1.591` > `$1.10`」—— 那个增量里**混着仍在落账的 W49 尾巴**,
  月度聚合**分不开两波**。**没有可用的单波账单读数 ⇒ 不点名。**
- **(乙2) 未触发**:本轮未发波,无「跑完自终」机时新样本。

## 九、铁律 6 / 11 与收尾读数

- `bots/` / `game/` / `tests/` / `tools/` **本会话一行未改**(改动 = `iterations/` 下的报告、本节、章程状态节)。
- **铁律 11**:全程无 `requires approval`、**无空转等待**;自检在后台跑,本台**没有为它停下**。
- **付费 CE 本轮 2 次(`$0.02`)**:一次是 `check_costs.sh` 因 ≥`$35` 自动复核(它正是本轮头号发现的来源),
  一次是第 2.2 节按机型归因 —— 章程允许的「确认贴近围栏的读数」,而 owner 的决策正压在归因上。
- 动态半(GH #124)**未跑不声称**。

### 9.1 自检真码

见第十一节收尾补记(**读脚本自打的 `selfcheck worst exit` 那一行,不读 harness 报的码**)。

## 十、交棒

1. ⭐⭐⭐ **owner —— `$50` 由实测越线,诚实 MTD ≥ `$61.73`,其中 ~61% 不是批测的。**
   上一轮说的「已越 `$4.67`」本轮修正为**至少 `$11.7`**。外来负载 `final-table-trainer` 本轮
   **仍是停的**(账号零实例)⇒ **不紧急,但已花的不退**。两条路仍不变:给 `dota2bot-batch`
   加 cost filter(⚠️ 走 `Project` 标签路线**必须先给批测机器补标签**,否则 fence 恒读 0,
   **一个永远打 CLEAR 的闸比现在更坏**),或给 `final-table-trainer` 单开预算。**已推手机通知。**
2. ⭐⭐⭐ **总监 —— 请裁:`wave_fence.py` 的「已跨档」判据读的是滞后操作数**(第三节)。
   同一个 `projected total $55.773` 上 `THROTTLED` / `CLEAR` 二选一,翻转全在围栏档位;
   与 GH #565 是**同一份滞后的另一个方向**(#565 是偏放行,本条是偏拦截)。
   **本轮开 `[harness] GH #569`**(三选一的候选与本台的倾向写在里面)。**本台不自行取用 CLEAR 那一读。**
3. ⭐⭐ **总监 —— `campgrade` 独占波(#480/#241)连续第六轮欠着**,闸 (i) 已 UNLOCKED,
   **唯一阻塞是闸 (iii)**;`budgets` 一刷新过 `$50`(围栏自动变 `$80`)或交棒 1 落地,
   **下一轮第一件事就是它**。
4. ⭐⭐ **总监(承上轮未结)** —— W50 的 promote/reject:gpm `+13.76` / comps 2/4,
   **均值由单粒 `6072` 扛着**,请连离散度一起裁;winrate 第九次 `DEGENERATE` 该结案。
5. ⭐ **总监 —— `$1.10` 常数**:本轮无单波账单读数,但有一个**自上而下**的新校验点
   `≥$1.058/波`(第八节),与常数同量级。**(乙1)(乙2) 均未触发。**
6. ⭐ **总监 —— SIR 窗口 ≲3h**(第六节):GH #565 的修法**必须当轮跑**,否则证据已不在。

## 十一、下一轮本台

1. **先 `check_costs.sh`,盯 `budget refreshed` 有没有翻过 `06:02:46Z`。**
   - 翻过且 `actual ≥ $50` ⇒ **围栏自动变 `$80`**,活工具会自己打 CLEAR ⇒ **第一件事发 `campgrade` 独占波**。
   - 没翻过 ⇒ **继续不发波**,不要用离线那一读去发波(第 3.3 节)。
2. ⚠️ **`--pending` 要重估,不要抄本轮的 `$15.345`** —— CE 已吃掉其中 `$11.476`;
   正确的量 = 诚实总额 − 工具当轮读到的 `actual`。
3. 发波当轮把 `machines[].sir` / `create` / `launched_at` 写进波次记录(GH #556 / #544),
   写完**之后**复跑 `test_wave_gate_keys.py` 与 `test_wave_throttle.py`;
   发波前**最后一条命令**仍是 `wave_throttle.py`(**动作时点,不是决定时点**)。
4. ⚠️ **开工第一条命令:不管道(本轮第 10 次踩)、重定向、`nohup` 后台、不设短 `timeout`**
   —— 本轮 `timeout 900` 把自检砍在半路,交棒 ⑤ 的两半**各犯了一次**。

## 十二、收尾补记(自检真码 + 铁律 6 读数 + 发表 + 收尾泄漏)

### 12.1 自检真码 —— ⚠️ 读脚本自打的那一行

`nohup` 无超时重跑,**约 12 分钟**跑完(这就是 `timeout 900` 砍在半路的原因,
也是交棒说「给足 ≥45min」的原因)。脚本自己打的是:

```
legs run              : 10
FINDINGS (exit 3)     : cadence queue-rulings owed-executions
UNCERTIFIABLE (exit 2): trunk-red(python)
NOT RUN (inside a leg): tests/test_selfcheck_lua_leg.py
selfcheck worst exit: 3
```

**`3` = 有发现,不是没跑成。十条腿全部真跑过。** 逐条见第一节;补三条:

- ⭐ **`trunk health (fast Lua detectors)`:`84 tagged detector file(s), 0 failures`** ⇒ **Lua 侧绿**;
  ⚠️ 边界照抄:**`FAST SUBSET, not the full suite`**,未打标签的测试不在覆盖内。
- ⛔ **`trunk-red(python)` 仍 `UNCERTIFIABLE`** ⇒ **python 侧 trunk 红不红,本台本轮答不了**。
  照抄腿自己的边界:`This line is NOT a pass, and it is NOT evidence that trunk is red`。
  该腿自测 `49 checks, 0 failures, 9 uncertified`,9 条都是 `5a0` 超预算(`84 file(s) ... in 120.1s`)的连带。
- ⭐ **`NOT RUN` 本轮只剩一个**(`tests/test_selfcheck_lua_leg.py`),上一轮是两个 ——
  `tests/test_rc_wrapper.py` 本轮跑成了,因为更靠前的腿已经把 `lua5.1` 装上(GH #171 那条修复的时序效果)。
- **promote-time 约束**:`160 distinct live gate id(s)`,`FROZEN none` ⇒ **`pullcad` 陷阱本轮无复发**;
  三个 co-promote 原子全 `ok`。

### 12.2 铁律 6 读数

静态门两次 `git push` **各自跑了同一个 gate 并各自自打**
**`GATE_EXIT=0  CLEAN (iron rule 6 static half passed)`**
(`luacheck bots game: 0 warnings`;门自己装的 luacheck:`installed luacheck (it was missing; apt package lua-check, bounded)`);
推送门 `ARM_HOOK_EXIT=0`(`core.hooksPath=.githooks, already set`);
**未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行**。
**`PUSH1_EXIT=0`**(`claude/great-sagan-c8z6fp`,新分支)/
**`PUSH2_EXIT=0`**(`a13cbb4d..52bf26eb  HEAD -> main`,**未被拒,无需 rebase**)。
动态半(GH #124)**未跑不声称**。

### 12.3 发表(GH #290 顺序:先 push 再发)

发表在 `52bf26eb` push 之后,发表前 **`PRECHECK_EXIT=0`** ——
`local commits not on origin/main: 0`,`paths cited 1 / resolved on trunk 1 / refused 0`,
`clean` / `OK to publish`。
**新开 `[harness] GH #569`**(第三节全套:同一投影上的两个判决、两个相反的失效方向、
三选一候选与本台倾向 (c)、以及本台**不自行取用 CLEAR** 的声明)。

### 12.4 收尾泄漏复检

**`LEAK_EXIT=0`** —— 本轮未发波,`running/pending` **空**,常驻仍只有 AMI `ami-0a990a26d89c66547`。
⚠️ 不发波轮要的是「本台零台在跑」;本轮账号侧也是零(第六节的全状态普查)。

## 十三、本轮 token

`TOKENS total_in=3,650,330 out=38,975 turns=33`(到统计时刻为止;统计后的收尾回合不计入)
