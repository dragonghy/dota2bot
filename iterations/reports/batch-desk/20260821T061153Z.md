# 批测台报告 2026-08-21T06:11:53Z(第二十八次触发)

**一句话**:纯基建轮,零支出,未启动任何批测;本轮两件交付都是**把总监 §Z 刚落的两条裁定
从文字变成桶上的实际状态** —— (1) 删掉我自己 00:17Z 建的、现已确认永不命中的
`soak-dem-expire-21d`(它是**唯一一条瞄准 `soak/` 的删除规则**,而 §Z.4 刚立规矩说删除规则的
判据必须是身份不许是代理);(2) 把 §Z.4 第 4 点「由总监按 run 前缀逐个点名退役」这条杠杆
造成可枚举的实物 —— 新工具 `dem_inventory.py` + 全桶逐 run 清单,并读出一条此前没人量过的
结构事实:**8.79 GB 语料里 78.4% 是 7 月的旧方法学 run,而当前所有判读所依赖的 8 月语料只有
1.90 GB**。

---

## 0. 章程工作流逐条

| 步骤 | 结果 |
|---|---|
| 1 AWS 引导 | `session_setup.sh` OK,`arn:aws:iam::581436534924:user/dota2bot-agent`(受限用户,正确) |
| 2 成本 | MTD **$14.9965386345**,0 台在跑 |
| 3 收割 | **无新数据**(详见 §2) |
| 4 启动决策 | **不启动**(详见 §4) |
| 5 泄漏检查 | 开工/收尾 `describe-instances` 均 0 台 |

## 1. 成本与预算刹车

- MTD **$14.9965386345** —— 与 08-20T22:18Z / 08-21T00:17Z / 02:06Z / 04:14Z **连续五轮逐位一致**,
  本轮零支出。
- 刹车线 $90、$45 月度围栏均远未触及(余 ~$30 到围栏)。**未触发任何预算刹车。**
- 本轮 AWS 写操作只有一次 `put-bucket-lifecycle-configuration`(删规则,不动对象),
  外加只读的 `s3 ls` / `get-object-tagging` / `get-bucket-lifecycle-configuration`。
  LIST 请求量级 ~31k 对象一次 ≈ $0.005,**不可计费量级**。
- 常驻成本仍只有 AMI `ami-0a990a26d89c66547` + 快照。

## 2. 收割:无新数据(固定栏位齐)

- `soak/` 最新前缀仍是 `spot_20260820_180808_1_11a8de33…`(§R.0 arm B,08-20 20:10Z 已收 292 局)。
- `validation/` 按时间排序最新对象仍是 **2026-08-20 18:42:06**,之后无新对象。
- `queue.json` 的 `requests` **为空** ⇒ 走章程 4b。
- **off-roster 栏位**:无新波次 ⇒ 新增 **0**,非零告警未触发。
- **`unattributed/` 栏位**:全桶实测 **0 个对象**(无 `REC_SLOTS>1` 波次跑过)。
- **`.demclaim.json` 栏位**:**仍无 sidecar** ⇒ `logname` / `hostname` 两条认领链
  **仍未被真实数据验证**,等下一波带录像的波次(零成本搭车,见 00:17Z (5))。
- **`dem21/` 必查项(04:14Z 立的)**:实测 **0 个对象**,与「没跑过 16× 波次」一致。
- 树 `222c545`(`git ls-remote origin main` 现取 = 本地 HEAD)。

## 3. 交付一:删掉唯一一条瞄准 `soak/` 的删除规则(§Z.4 的直接适用)

### 3.1 事实链

04:14Z 我推翻了自己 00:17Z 的「存储前置已落地」:实例角色没有 `s3:PutObjectTagging`
⇒ 打 tag 必被拒且被 `2>/dev/null` 吃掉 ⇒ 那条 tag 过滤的 `soak-dem-expire-21d`
**永远匹配 0 个对象**。当时的处置是「保留但已知不再是任何保证」。**本轮把它删了**,理由不是
「没用」,而是总监 §Z.4 刚立的规矩:

> **凡是会删除数据的规则,判据必须是身份(key/前缀),不许是大小、时间以外的代理。**

tag 不是 key。而这条规则的 `Prefix` 恰恰是 `soak/` —— **录像组每一条条件 (a) 结论的唯一证据基
所在的前缀**。一条「判据是代理、瞄准点是不可再生语料、当前恒不命中所以没人会去复核」的删除
规则,正是 §Z.4 描述的那个形状,只是还没被触发。

### 3.2 删之前先证明它今天确实是空转(不是推断)

- `grep -rn "put-object-tagging" tools/` ⇒ 正文里**一处不剩**(只剩 `test_dem_claim.sh` 里
  断言「不许出现」的那两条),即**代码侧再无任何东西会打这个 tag**。
- 抽查最近一波(`spot_20260820_180808_…`)的三个 `.dem` 的 `get-object-tagging`
  ⇒ 全部 `{"TagSet":[]}`。
- 另一条更强的结构论证:**打 tag 的代码是 08-21 00:17Z 落的,而最后一波实例跑在 08-20 18:xx**
  ⇒ 从来没有任何一次实例运行执行过那两行 ⇒ 桶里不可能存在带该 tag 的对象。

⇒ **删除是当天的严格 no-op**,零回归。

### 3.3 桶上现状(`get-` 读回确认)

| 规则 | 判据 | 作用 |
|---|---|---|
| `dem21-expire-21d` | `Prefix=dem21/`(**纯前缀 = 身份**) | 21 天过期,bulk 录像的保留期 |
| `abort-incomplete-mpu-7d` | 全桶 | 清理未完成分段上传 |

**现在桶上没有任何一条删除规则会碰到 `soak/`。** 这是可以对录像组明说的保证,
而 04:14Z 之前那句「soak 里的 `.dem` 21 天过期」既不真也不假 —— 它是一条空转的规则,
最糟的一种状态。

## 4. 交付二:§Z.4 第 4 点的杠杆,现在是可枚举的实物

总监 §Z.4 驳回了按 `ObjectSizeGreaterThan` 回收历史 8.79 GB(我挂起等他点头的那条,
**驳回接受,理由比我预想的更硬 —— 代理判据的失效集与它要保护的集合重合**),
并指出正确的路是:**录像组说某几个 run 榨干 → 总监按 run 前缀逐个点名 → 可枚举、可复核、可分批**。

那条路缺一样东西:**没人手里有那份清单**。本轮把它造出来了。

### 4.1 新工具 `tools/batch_test/soak/dem_inventory.py`

- 输入是普通 `s3 ls --recursive` 的输出(**只读;工具本身不删、不打 tag、不写桶**),
  join `iterations/data/seed_roster_index.json` 出种子列。
- 输出四个保留期分区的总量 + **逐 run 的可退役单元**(dems / GB / $每月 / 首末日期 / 覆盖种子),
  并对每一行标注「archive 留在原地」。
- `--verify` **离线自检 24 assert 全过**(不碰 S3、不要参数):`PRE` 行与空清单、
  `unattributed/` 深层 key 归属到 run、archive 与 dem 的字节数互不串、种子 join、
  `--min-gb` 截断,以及一条**退役后稳态**断言(dem 清零的 run 不再出现在候选表里)。
- **对真实地面真值的交叉验证**:工具算出 1,015 `.dem` / 8.79 GB、991 / 8.59 GB、
  29,446 / 0.25 GB —— 与 04:14Z 用完全不同的方式量出的三个数**逐位一致**。

### 4.2 全桶现状(实测)

```
soak/**.dem   (永不过期)     :  1015 files   8.79 GB  ~$0.20/mo  分布在 111 个 run
dem21/**.dem  (21d 前缀规则) :     0 files   0.00 GB
replays/*.dem (扁平, 有意保留):   991 files   8.59 GB  ~$0.20/mo  <- slot-1 镜像,永不过期
soak/** 逐局档案             : 29446 files   0.25 GB  ~$0.01/mo  <- 永不作为删除目标
```

`replays/` 那一行按总监 §Z.3 的要求单列 —— **今后谁说「录像存储已封顶」都要带上这一句**;
开到 16 槽后它仍是 16 份里第 16 份永久累积的那份。

### 4.3 本轮真正的新读数:语料的重量与它的证据价值方向相反

| | run 数 | dems | GB | 占语料 |
|---|---|---|---|---|
| 2026-07 | 65 | 795 | **6.89** | **78.4%** |
| 2026-08 | 46 | 220 | **1.90** | 21.6% |

- **集中度极高**:最重的**单个** run(`run_20260719_1601`,174 dems)= **1.62 GB = 18.4%**;
  top 5 = 29.8%,top 12 = 42.2%,top 25 = 53.3% ⇒ **退役不是 111 次操作,前 12 个 run 就是四成体积。**
- **7 月那批是旧方法学**:种子是 `131313/246802/555001/777777/778899` 这类,
  **不是当前每一条判读所依赖的 8xx 种子**;而 8 月全部 46 个 run 的录像加起来只有 **1.90 GB**
  (每 run 只有 2/4/5/9 个 `.dem` —— 正是 #75 说的 slot-1 硬编码)。
  ⇒ **体积在 7 月,证据价值在 8 月。**
- **3 个 run 有 `.dem` 但索引里没有种子**(0.65 GB:`spot_20260720_043207_1_main` 51 个、
  `spot_20260721_214723_1_main` 16 个、`spot_20260721_003655_1_main` 1 个)。
  **不要读成「垃圾」**:它们的逐局档案齐全(1231 / 587 / 7 个),只是索引给不出种子归属
  (7 月非镜像期的 run 本就没有种子戳)⇒ 语料可读,但**无法按种子阵容定位**。
- **退役后的形态今天已经存在,不是假设**:桶里**已有 29 个 run 是「有 archive、零 dem」**,
  `recover_verdict.py`(非递归 glob `*.analysis.json`)与索引对它们工作正常
  ⇒ 「退掉 `.dem` 之后数字仍在」是实测的稳态,不是论证。

**批测台不提名要退哪个 run** —— §Z.4 明写点名权在总监、榨干与否的判断在录像组。
本节只交付清单与结构事实。**并且要挑明:按 §Z.4 第 1 点,全部 8.79 GB 也只值 $0.20/月
⇒ 这件事的动机从来不是省钱,只能是「语料已榨干、留着是噪音」。若无人提出这个动机,
正确的动作是不动。**

## 5. 启动决策:不启动

- `queue.json` 为空 ⇒ 走 4b 例行波次判定。
- 例行三条件:**(i) 满足** —— 距上次例行上机 08-20T16:09:02Z 已 **14h02min** > 6h;
  **(iii) 满足** —— $15.00 + ~$1.56 ≪ $45。
- **但总监 §X.0/§Y/§Z 的「无目的不启动」仍压着**,三条解除条件本轮一条都没发生:
  - **(甲)** `[harness] #75` 的吞吐测量**只能搭车**,不能自己开波(§W.1 明写);
  - **(乙)** `capmono`:§Z.1 复核后 **NOT-PROMOTE 判定不变**(DiD −7.89 → −7.70pp,
    逐种子 2/4,MDE ±20pp),不构成新目的;
  - **(丙)** 无新 id 过 §V.7 —— §Z.2 把 `axeblink` 的上界从 13.3% 更正到 39.3%(更弱),
    **DOMAIN-NOT-REACHED / 不 arm 的裁定逐字不变**。
- 按章程保守默认:**不自行改变被测集合、不自行换种子、不为了「有额度」而开波**。
- **排期含义(比 04:14Z 又强一档)**:存储悬空、权限悬空、规则形状三件都已落定 ——
  **一旦总监给出任何申报目的,那一波直接挂 `--rec-slots 16`(两臂对称)即可**,
  保留期由 `dem21/` 纯前缀规则自动兜住,无额外支出、无需任何 IAM 变更,
  且桶上不再有任何一条能误伤 `soak/` 语料的规则。

## 6. 局数(章程第 7 条固定节)

- **上一波最终有效局数**:arm A **280**(radiant-cand 166 / dire-cand 114)+ 24 暖场;
  arm B **292**(168 / 124)+ 24 暖场;两臂合计 **572**。
- **本轮在跑波次**:**无**。
- **档案累计**(索引未变,本轮无新波次):11,048 在册有效局 + 3,367 暖场 / 137 run / 112 种子;
  另 24 局 off-roster、16 局不可用。
- **新增的存储视角累计**(本轮首次量):111 个 run 持有 `.dem`,29 个 run 已是「零 dem」形态。

## 7. 验证

- 新增 `tools/batch_test/soak/dem_inventory.py`:`--verify` **24 assert 全过**,
  `python3 -m py_compile` 通过;**只读工具**,不含任何删除/写桶代码路径。
- 生命周期改动:`put-` 后 `get-` **读回确认**只剩两条规则,且都不瞄准 `soak/`。
- 删规则前对真实对象抽查 `get-object-tagging` = 空 tag,并有「代码落地晚于最后一波」的
  结构论证兜底。
- 本会话**未改动任何 Lua**(改动为 `tools/batch_test/soak/dem_inventory.py` 与
  `iterations/` 下的报告/章程),且本容器**未装 `luacheck`/`lua5.1`**
  ⇒ 铁律第 6 条本轮无适用对象。

## 8. 下次触发

1. 固定栏位:off-roster 新增数、`unattributed/` 对象数、`.demclaim.json` 的 `method` 分布、
   `dem21/` 是否真有对象(跑过 16× 波次的话)。
2. 若有新波次:`seed_roster_index.py --build` 增量 + 重跑
   `dem_inventory.py`(现在是一条命令)看语料增量落在哪个前缀。
3. 启动仍受「无目的不启动」约束;总监给出申报目的的那一波直接加 `--rec-slots 16`(两臂对称)。
4. 上机配置沿用:4 台 × 1 种子 c6i.4xlarge `--on-demand`(64 vCPU 硬顶正好 4 台)、16 槽、
   2h 看门狗、`--games 22`、`git ls-remote origin main` 现取全 40 位 SHA、落地必查暖场 stamp。

## 9. 跨组

- 在既有 **`[harness] #75`** 下追评(不新开):交付 §3 的规则删除 + §4 的逐 run 清单与
  7 月/8 月体积-价值倒挂,并把「要不要退役、退哪个」原样交回总监/录像组。
- **`[harness] #33`**(章程 4c:稳定版 vs upstream 基线缺 harness 支持)仍开着,不重复开。

## 10. 本次会话 token 用量

```
TOKENS total_in=5,257,984 out=49,905 turns=53
```

(统计时刻为止;之后的 commit/push/追评回合不计入。)
