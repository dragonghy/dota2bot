# 批测台 2026-08-26T18:15Z — 收割 W14(界后第五波,首个 37-id 波次)

**一句话**:W14 全量重算完成(208 局落盘 / 184 计分 / unfinished 0),
**gpm −36.81(1/4)、winrate 0.499(1/4)、`suggested = hold_or_reject`**;
**零发波**(6h 闸差 2h59m 未到);**本轮 AWS 支出 $0.01**(仅 CE 复核);**无泄漏**。
本轮两条实质产出:**(甲) GH #204 的 `(ab−ba)/2 = D_s` 第四次独立复现,而且这次四个种子里
出现了 −211 的反号极值**;**(乙) 波次级 gpm −36.81 完全由单个种子 906(−138.2)驱动,
种子中位数只有 −6.06 —— 同一份语料两种切法差 6 倍**(铁律 4(iii),两种切法均登记)。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` → **worst exit 3**(有发现,非阻断):

- **unlanded**:1 条,`978dbd5`「Record owner ruling: Spot first, on-demand only when
  no Spot capacity」在 `origin/cursor/spot-first-policy-a205`,尚未上 main。
  ⚠️ **这条与本台直接相关,而且是本轮最重要的一条**(详见 §1.2)。
  **查到 PR 后更正一句措辞**:它**不是掉棒/搁浅** —— **PR #224 已开(17:54:48Z,base main,
  状态 open)**,Cursor 座位按 `.claude/rules/claude-code.md` 本来就走「feature branch + PR」,
  **这是正常在途,不是异常**。检测器只能看见「不在 main 上」,**看不见 PR** ⇒
  它报的是问题不是判决,本轮的判决是「在途,等合入」。
  其余 385/422 ref 因浅 clone 被 REFUSED —— **拒答不是通过**。
- **cadence**:2 处洞(replay-check 08-25 3.7h、strategy 08-26T10:34→14:23Z 3.8h),
  本台本轮无新洞。
- **两锚点** `stable-v1`/`stable-v2`:EXISTS / PINNED / SHIPPED 全 ok。
- **trunk python 37 passed 0 failed**(上一轮 33);快 Lua 检测器 **9 文件 0 失败**。
- **RIDESHARE 本轮该裁**:none;**un-ruled 队列请求**:none;总开放请求 33。

## 1. 成本与预算闸

| 量 | 读数 |
|---|---|
| `budgets` MTD(免费) | **$43.463**,刷新时刻 **2026-08-26T09:32:22Z**,forecast 76.29,limit 100.0 |
| CE 复核($0.01,因读数 ≥ $35 自动触发) | **$43.4628** — 与 budgets **逐位一致**(连续第二轮一致) |
| 常驻资源 | 只有 AMI `ami-0a990a26d89c66547` + 快照 |
| running/pending 实例(开工) | **空**(W14 四台已自毁) |

**围栏算术(章程 2 (甲))**:MTD $43.463 + 今日三波 W12/W13/W14 × ~$2.15 = **$54.11**,
与上一轮登记值不变(**本轮零发波,围栏不动**)。自停线 $60,刹车线 $90,批准档 $100。

### 1.1 W14 的实测单波成本(新读数,精化 GH #219 的余量算术)

四台的 launch → 末次 S3 上传:

| run | launch | 末次上传 | 墙钟 |
|---|---|---|---|
| `8b9082` | 15:14:24Z | 15:54:13Z | 0.664 h |
| `a2baf3` | 15:14:27Z | 16:07:01Z | 0.876 h |
| `31afa3` | 15:14:30Z | 15:54:05Z | 0.659 h |
| `2c6d8e` | 15:14:32Z | 16:01:30Z | 0.783 h |

合计 **2.982 台·小时**(均 **0.745 h/台**),加每台 ~2 min 关机尾巴 ≈ **3.12 台·小时**
⇒ **$0.673/h × 3.12 = $2.10**,加 IPv4 $0.005/h ≈ **$2.12**。
**⇒ W14 实测 $2.01–$2.12,与近几轮沿用的 $2.15/波 吻合(略低)。**
顺带**再次证伪章程 2 那条 cap-25 外推的 `1.13 h/台 ⇒ $3.05/波`**:实测 0.745 h/台,
**外推高估 52%**;近几轮已改用 $2.15,本轮给出第五波的独立实测支撑,建议总监把章程里
那句 `~$3.05` 直接改成 **`~$2.15(W10–W14 五波实测,0.66–0.88 h/台)`**。

**余量(GH #219)**:按需口径 $54.11 → W15 后 ~$56.3 → W16 后 ~$58.4 → **W17 必被自停线 (iii) 拒**。
8 月还剩 5 天。**$50 配套义务上一轮已履行**(15:15Z 报告 §2.4 写下了那行解释),
本轮跨线状态不变,不重复触发。
**⇒ 但 §1.2 那条 owner 裁定一旦生效,这段算术整个换掉**:spot 界后约 **$0.8/波**
⇒ W15 后 ~$54.9、W16 后 ~$55.7 …… **余量从 2.7 波变成约 7 波**,
**GH #219 的四条路里 owner 已经自己选了「转 spot」那条**。

### 1.2 ⭐⭐ owner 已就 GH #158 拍板:**Spot 优先,没容量才 on-demand**(PR #224,待合入)

本轮自检的 unlanded 那条查下去,是**本轮最重要的发现**:

- 提交 `978dbd5`(**2026-08-26T17:54:26Z**,即本轮触发前 14 分钟),
  **PR #224 open,base `main`**,标题「Spot first: on-demand only when no Spot capacity (GH #158)」。
- 提交信息原文:*「GH #158 is option B plus a capacity fallback, not hybrid C. Batch-desk
  must stop passing `--on-demand` by default; reclaim drops that seed's pair and relaunches
  the missing seed. **W14 stays in flight; W15 follows this.**」*
- 它改的正是**本台章程的步骤 5**,owner 原话被逐字记进去:
  *「On demand和spot之间肯定优先用spot呀,除非没有spot的机器」* —— **明确否决了本台倾向的 (C) 混合**。
- 落地规则(章程步骤 5 新增,`OWNER_PRIORITIES.md` 常设运维项):
  (1) **默认不传 `--on-demand`**;(2) 启动后用 `describe-instances` 核 `InstanceLifecycle`
  **要的是 `spot` 不是 `None`**(`run_id` 的 `spot_` 前缀不是证据);
  (3) **容量降级阶梯是走到按需的唯一路径**:`c6i.4xlarge` spot →
  `InsufficientInstanceCapacity`/等不到 → 同参数换 `--type c6a.4xlarge` spot →
  两种机型都没有 → 才 `--on-demand`,且**降级必须写进当轮报告**(错误码、试过的机型、最终市场类型);
  (4) **回收处置事先登记**:一台被抢占 = 该种子缺臂,**只作废这一粒的配对差,不整波作废**,
  已落盘局用 `recover_verdict.py` 重算,缺的那粒种子按同一阶梯补发一台。
- **W14 是拍板前已起飞的按需波,裁定明写「不中断、不杀」** ⇒ 本台本轮对 W14 的处置(收割)正确。

**⚠️ 本台的预登记(W15,给下一轮的自己)**:**W15 走 spot**,
**不以 PR #224 是否已合入为条件**。理由是 §AW.1 那条教训的正面用法:裁定已经作出、
owner 原话已被逐字记录、且**点名 W15**,此时「main 上的旧章程还写着 on-demand」
只是**送达延迟**,不是裁定不存在 —— 「保守默认与裁定相反」正是那条教训要防的形状。
若届时 #224 已合入,则逐字照新章程步骤 5 执行(以合入版为准)。

## 2. 收割 W14(全量重算)

**树 `039cb1ae82ae5c83ef4d64e2d1c99007e3b0f13e`**;4 run;种子 888/895/896/906;
`--slots 16 --rec-slots 12 --games 12 --on-demand`。

**⚠️ 收割姿势(本轮新登记的一条坑,与 06:15Z 那条 `--include` 同族但更隐蔽)**:
把四个 run 的 `*.analysis.json` **平铺进一个目录**时,**208 个文件只落地 188 个** ——
同秒开局的 basename 跨 run 撞名(章程已知「全桶 21 个 basename 跨 run」),
`cp` 静默覆盖。**后果是读数**:平铺口径算出 `mean gpm −40.38 / scored 170`,
**加 `<run>__` 前缀**后是 **`−36.81 / scored 184`** —— 14 局计分局被无声吃掉、
效应量偏移 3.6 gpm,**而 `recover_verdict.py` 只看得到少了的那些文件,不会举手**。
本报告一切读数用**加前缀**的口径。**收割 checklist 追加一行**:
`--include "*.analysis.json" --include "*.demclaim.json"` **且**下载后按 run 前缀重命名再池化。

### 2.1 verdict(`recover_verdict.py`,判据一字未改)

| 量 | mean | comps_better |
|---|---|---|
| gpm | **−36.81** | 1/4 |
| xpm | −11.81 | 2/4 |
| deaths | +0.22 | 1/4 |
| last_hits | −0.28 | 2/4 |
| winrate | **0.499**(中性 0.5) | 1/4 |

`scored_games` 184 / `unfinished` 0 / `winner_by` = `engine_natural` 208/208 /
`winrate_independent_of_gold` 0/208(后两条为已知产物,不重复发现)。
**`suggested = hold_or_reject`。**

### 2.2 逐种子(铁律 4(iii):两种切法均登记)

| 种子 | ab 局 | ba 局 | **E=(ab+ba)/2 = 效应** | **D=(ab−ba)/2** | winrate |
|---|---|---|---|---|---|
| 888 | 28 | 14 | **−8.67** | **−211.25** | 0.500 |
| 895 | 41 | 17 | **−3.46** | **+110.17** | 0.549 |
| 896 | 28 | 14 | **+3.07** | **+146.53** | 0.446 |
| 906 | 30 | 12 | **−138.18** | **+138.81** | 0.500 |

- **切法 A(种子均值,`recover_verdict.py` 的口径)**:gpm **−36.81**
- **切法 B(种子中位数)**:gpm **−6.06**
- **切法 C(池化两腿)**:ab **+19.09** / ba **−77.68** ⇒ E **−29.29**,D **+48.39**

**⭐ 读数一:波次级 −36.81 完全是单种子 906 的 −138.2 撑起来的。** 另外三个种子
(−8.67 / −3.46 / +3.07)全部落在 ±9 以内,**符号还两正一负**。种子均值与种子中位数
**差 6 倍**,是铁律 4(iii) 立法时那个刀口的经济量版本。**本台不作判断**,只登记:
读「37-id 测试集拖了 −36.8 gpm」和读「三种子中性、一种子塌了」是同一份语料的两种切法。

**⭐ 读数二(GH #204 第四次独立复现,而且更硬)**:`D_s` = **−211.25 / +110.17 /
+146.53 / +138.81** —— **换种子就换符号,幅度 O(150) 是效应 O(30) 的 5 倍**,
真侧偏(≈ +58 gpm/人,本波局长 25.75 min)是常数、不会翻号。W13 那次是
−171.07 / +37.73 / +150.36 / +142.29,**四波读下来 888 恒为异号的那一个**。
**机械施 4(i) 于 gpm 的后果就摆在切法 C 里**:池化 ab **+19.09** 与 ba **−77.68**
**反号 ⇒ 按 4(i) 字面判「噪声,不写进结论」**,而四个种子各自的 E 里有一个 −138。
**裁定仍归总监(GH #204 候裁第四轮)**,本台按上一轮预登记两种切法都登记,不施判据。

### 2.3 armed 串核验

**8 个 mirror 分组全部是同一条 37-id 串**(348 字节含 `:sSEED:radiant` 尾,
345 字节含 `:dire` 尾),逐字为:
`l1trade,l5combo,midtp,suptp,tpcommit,tpdying,lf_rescue,teambrain,ownhalf,overchase,fieldregen,wandbleed,capmono,cmrguard,tpdead,zusult,wandlimbo,blinkflee,liondrainstop,odaoe,pullcamp,stayfield,stayfield2,fieldbuy,fieldcreep,pullcad,pulllane,towerfear,tpreach,pulldrag,tpgap,campsel,tbearly,tpdeathbuy,zusstatic,campfarm,abilanc`
⇒ **上一轮「§BL 裁定压过陈旧的 `test_set.md` 第 2 行」的预登记,在落盘语料上核实为真**:
`abilanc` 确实上了机,第 37 位。`campgrade` 不在串内(§BG.3/§BK 互斥前置满足)。

### 2.4 局时

`duration_min` 均 **25.75** / 中位 **25.85** / min **21.00** / max **27.10**(208 局,cap 25.0)。
与 W12(25.74/25.90)、W13(25.77/25.80)逐位同族。

## 3. 预登记读法的执行(15:15Z 报告 §5.1,判据未改)

### 3.1 `abilanc` —— **UNREAD,不是 SILENT**(这两者必须分开)

预登记写的是「**找不到 ⇒ 报 SILENT**」。本轮的事实是**扫描根本跑不起来**:
`strategy-20` 的 acceptance 指定的工具是 `tools/batch_test/behavioral/campfarm_target.py`,
它的位置参数是 **`sweeps`(逐帧 dump 扫描文件)**,而本台侧 `soak/<run>/` 实测只有
`*.analysis.json` / `*.demclaim.json` / `*.log.gz` 三种对象(W14 每 run 48/36/48,
`a2baf3` 是 64/48/64),**逐帧字段一个没有**。

**「跑了、没找到」与「没跑成」在结论上是两件事** —— 这正是铁律 10 那条
「`SKIP`/`UNCERTIFIABLE` 不是通过」的同型。**本台登记 `abilanc` = UNREAD**,
不把它写成 SILENT,也不据此怀疑修法。

**好消息:W14 的帧语料齐全,棒可以立刻交出去。**
`dem21/<run>/` 下 W14 共 **156 个 `.dem`**(36/48/36/36),与 156 个 `.demclaim.json`
**逐个对上**;另有 4+1 个 slot1 镜像在扁平 `replays/`。**21 天过期**,即
**约 2026-09-16 前有效**。已把 run 前缀与局数写进 GH #196 追评(§7.2)。

### 3.2 `zusstatic` —— 仍 **UNDETERMINED**(GH #207 第三轮)

同 §3.1 的理由,批测侧读不到该腿的触发证据;上一轮录像组已用帧证据否掉
STRUCTURAL-ZERO(gate-off 出口返回 0.09 不是 0,121/121 局有 Zeus)。
**GH #207 三选一未裁 ⇒ W14 已是它连续第二波空转同一条腿。**

## 4. `rec_slot_cost.py` 第十一次验收:exit 1,槽 1 净 **−7.4%**

判据一字未改,退出码直接读(未接管道)。

| 槽 | 净值 | | 槽 | 净值 |
|---|---|---|---|---|
| 1 | **−7.4% ← 唯一超差** | | 9 | −0.2% |
| 2 | −0.6% | | 10 | −1.0% |
| 3 | −0.3% | | 11 | −0.2% |
| 4 | +0.5% | | 12 | −1.2% |
| 5 | +0.9% | | 13–16 | −0.0% / −0.5% / −0.0% / +0.6% |
| 6 | −0.0% | | box factor(对照槽 now/baseline) | **1.100**(4 槽) |
| 7 | −0.7% | | 语料 | 208 局,录制 156 / 对照 52 |
| 8 | −0.0% | | | |

**这是同一个失败模式的第三次独立复现**(W11 槽 1 −8.0%、W12 −7.9%、**W14 −7.4%**),
归因已在 06:15Z 报告里钉死并被本轮再次确认:`rec_slot_baseline.json` 的 `rec_set` 是 **`[1]`**,
基线的槽 1 **是那份 305 局语料里唯一的录制者**,而本工具的头条发现正是「录制槽是最快的那一槽,
残差 +8.1%」⇒ **同槽号跨波比在基线的录制槽上按构造失效,偏差 +8.1%,与三波报出的
−8.0/−7.9/−7.4 逐个对到 1 个百分点以内。**

**本波内那条自洽通道(工具自己设计的主通道)方向相反**:以本波 4 个对照槽拟合的趋势
(`timescale ~ 0.01123·slot + 1.9352`)来看,**12 个录制槽的残差全部 ≥ −0.1%,槽 1 是
最大的正残差 +2.2%(录制槽比趋势更快)** ⇒ **本波测不出任何录制赤字。**

**第二通道本波仍无信息**:录制槽与对照槽**全部恰好 13.00 局**(min 13 / max 13),
被 `--games` 封顶、方差为零,**按构造测不出赤字**(与 W12 同型;W11 那次的 −4.5%
既未复现也未证伪)。

**处置**:本台**仍不单方面执行「exit 1 ⇒ 退回 1」**(失败归因落在基线**输入**上,
不在容差上;退回 1 会把帧通道从 75% 砍回 6%,代价直接落在 owner P1/P2 头上),
**也不据此上 16**;`--rec-slots` **维持 12**(§AS.2 裁的 (C),明写不许降)。
**GH #75 第五轮催办**,本台推荐仍是 **(A)+(D)**。

## 5. 发波三闸(章程 4b)—— 本轮 **零发波**

- **(i) 6h 节流:BLOCKED** —— W14 末台 **15:14:32Z** ⇒ 解锁 **2026-08-26T21:14:32Z**;
  先打钟点(`date -u` = **18:08:53Z**)再比,**差 3h05m39s**。
- **(ii) 有新东西可测**:满足(远端 tip `0c6087a0` ≠ W14 树 `039cb1ae`,
  `bots/` 有 `bbrespawn`/`bbfight` 两个新 gated id)。
- **(iii) 成本**:$54.11 ≤ $60,满足,余量 $5.89。

**(i) 单条封锁 ⇒ 不发波。** 队列侧亦无到期的 `director.wave` 请求(见 §0 的 un-ruled = none)。

## 6. 局数(章程 7)

- **(a) 上一波(W14)最终有效局数**:**落盘 208 / 计分 184 / unfinished 0**;
  24 局是裸 sha 排空局(`script_version` = `039cb1ae` 而非 `mirror:`),
  与 W13 的 24 / W12 的 23 同型。
  逐种子 **ab/ba** = 888 **28/14**、895 **41/17**、896 **28/14**、906 **30/12**
  ⇒ 合计 **127/57**,**ba 占比 0.310**(W11 0.322 / W12 0.325 / W13 0.306)
  ⇒ **ab/ba 不对称第四次复现,结构性,ba 层噪声恒约 1.5×**。
  sidecar **156/208 = 0.750 = rec-slots 12/16**(逐位命中设计值)。
- **(b) 本轮在跑波次**:**无**(零发波)。

## 7. 泄漏检查(两次)

- **开工**:`check_costs.sh` 的 running/pending 段**空**。
- **收尾**:`check_costs.sh --leak-only` **0 台在跑**;常驻只剩 AMI + 快照。
- **本轮零发波 ⇒ 未新增任何实例。**

---

## 8. 交棒(下一棒必须交出去,否则接力棒会掉)

1. **⭐ Cursor 整合者 —— 请在 `2026-08-26T21:14:32Z`(W15 解锁时刻)之前合入 PR #224**:
   owner 对 GH #158 的裁定(Spot 优先)已写好并开了 PR,**base main,状态 open**,
   但**尚未合入** ⇒ 下一轮本台开会话时,main 上的章程步骤 5 读到的仍是「未裁前走 `--on-demand`」。
   **本台已按 §1.2 预登记「W15 走 spot,不以合入为条件」**,所以这不是阻断;
   但**合入了才有那份逐字的降级阶梯与回收处置规则可照做**,不合入本台只能按提交信息复述。
   本台不自行 cherry-pick 他座位在途的分支(越界),故以 issue/报告交棒。
2. **录像组 —— `abilanc` 的 (a) 取证,语料已就位(GH #196 / `strategy-20`)**:
   W14 四个 run `8b9082`/`a2baf3`/`31afa3`/`2c6d8e`,树 `039cb1ae`,
   `dem21/<run>/` 共 **156 个 `.dem`**(**21 天过期,约 09-16 前有效**),
   judgement 工具 `campfarm_target.py`(`--selfcheck` 21 PASS),判据见 `strategy-20`
   的 acceptance(**存在性主判据,明确不报 `cast_at_camp` 的计数差分**)。
   **批测侧读不到,这一格只能在录像组手上关闭** —— 即 GH #211 那条路由错配的第一个具体受害者。
3. **总监 —— GH #207**(第三轮):`zusstatic` 三选一仍未裁,**W14 已是连续第二波空转同一条腿**。
4. **总监 —— GH #204**(第四轮):`(ab−ba)/2 = D_s` 又一次复现,且本波出现 **−211** 的反号极值;
   同时 §2.2 切法 C 摆出了「机械施 4(i) 于 gpm 会把一个 −138 的种子判成噪声」的具体算例。
5. **总监 —— GH #210**(第三轮):`test_set.md` 第 2 行仍是 36-id;W14 落盘语料已证实实跑 37-id
   ⇒ **行不改,下一轮同一个歧义原样再来**。
6. **总监 —— GH #218**(第二轮):`bbrespawn` / `bbfight` 路由未裁,**W15 仍将不 arm 它们**。
7. **总监 —— GH #217**(第二轮):围栏公式 12h 窗口 vs MTD 刷新边界不重合。
8. **总监 —— GH #219 可以结案了**:owner 在 GH #158 上选的「转 spot」**正是 #219 摆出的四条路之一**
   (§1.1:余量从 2.7 波变成约 7 波)⇒ 抬 (iii) / 降频 / 缩 `--games` 三条**都不必再裁**。
   顺带请把章程 §2 的按需单价 `~$3.05/波` 订正为 **`~$2.15/波`**(W10–W14 五波实测,
   本轮 §1.1 给出第五个独立读数 **0.745 h/台**;PR #224 已顺手改了这一格,合入即生效)。
9. **总监 —— GH #75**(第五轮):`rec_slot_cost.py` 第十一次 exit 1,失败模式第三次复现,
   本台推荐 **(A)+(D)**;在裁定前 `--rec-slots` 维持 12。
10. **总监 —— GH #211**(第三轮):13 条 `ROUTED_ARCHIVE_SCAN` 路由错配 —— **§3.1 是它的第一个
    实证受害者**(`abilanc` 的预登记读法写给了拿不到帧的一方)。
11. **总监 —— 存量催办**:#180(第九轮)、#171(第七轮)、#200(第六轮)、#181、
    载体门 PARTIAL(第十四轮)、`stable-v*` tag(第十一轮)、`campdanger`(第六轮)、
    §BL.4 合取检查机械化(进基建 backlog)。
12. **⚠️ 基建 backlog(本轮新增,零 AWS)**:`recover_verdict.py` 收多个 run 时**按 basename 池化
    会静默丢局**(§2 实测 208→188,计分 170 vs 184,效应量偏 3.6 gpm)。
    最小修法:工具内部按 `<run>__<basename>` 去重,或在读到重名时**拒答**而不是静默取一份。
    **已开 GH #225**(推荐 (A) 工具侧去重 + (B) 撞名拒答)。**按章程本台不自己改 harness。**
13. **下一轮本台 —— 发 W15**:**6h 闸解锁 `2026-08-26T21:14:32Z`**(先打钟点再跟 `date -u` 比);
    **⭐ 市场类型:走 spot(§1.2 的预登记,不以 PR #224 是否合入为条件)** —— 不传 `--on-demand`,
    启动后核 `InstanceLifecycle == spot`(**`run_id` 的 `spot_` 前缀不是证据**);没容量按
    `c6i.4xlarge` → `c6a.4xlarge` → 按需的阶梯降级,**降级要把错误码 / 试过的机型 / 最终市场类型
    写进报告**;被抢占只作废那一粒种子的配对差、补发该种子,**不整波作废**。
    其余发波前置照旧:`git ls-remote origin main` 核树(**浅 clone 两步走,exit 128 = 不可比
    不是无漂移**)+ 接线门 + smoke 门 + 载体门 + `soak-run` 标签两两不同;
    arm 串**逐字取 `test_set.md` 第 2 行**,若第 5 项仍未落地则沿用「裁定压过陈旧行」的既有预登记;
    围栏届时 **spot 口径 ~$54.9**(按需口径 ~$56.3),均 < $60。

---

## 9. 本次会话 token 用量(铁律 8)

```
TOKENS total_in=9,069,166 out=54,567 turns=71
```

(统计时刻之后的收尾回合不计入。)

**铁律 6 的两道门**:本轮**零 Lua / 零 `bots/` 改动**(改动面 = 本报告 + 章程「当前状态」
+ `queue.json` 三处),按章程「Markdown-only 可跳过全量套件」执行;
静态门 `bash tools/agent/luacheck_gate.sh` 本轮**实跑**:
容器缺 luacheck ⇒ 脚本自装(apt 包 `lua-check`),**`luacheck bots game: 0 warnings`,exit 0**。
自检里的 trunk python(**37 passed 0 failed**)与快 Lua 检测器(**9 文件 0 失败**)同轮已跑。
