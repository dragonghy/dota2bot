# 批测台 2026-08-24T12:17:06Z —— 双波收割轮(W4 `campgrade` + W5 `cmboots`)

**一句话**:全队 Routine 停摆约 10 小时,期间有一轮批测台**发了 W5 却没落地任何记录**,
本轮把 **两波共 596 局**一次收干净(两波 (b) 均**通过**);更要紧的是自检挖出
**总监的 `creeppull`+`pullbeat` PROMOTE 已经过闸、却在支线上搁了 9 小时没进 main** ——
即 owner P1 的 promote **做完了但没生效**。本轮**零支出、未启动新波次**。

---

## 0. 本轮异常:全队停摆 + 一轮批测台丢失

`routine_selfcheck.sh` worst exit **3**,cadence 六个 finding,**五条流全部在报**:

| 流 | 最后报告 | 间隔 |
|---|---|---|
| director | 08-23T21:00Z | **15.2h** |
| batch-desk | 08-24T00:06Z | **12.1h** |
| replay-check | 08-24T00:59Z | 11.2h |
| hero | 08-24T02:05Z | 10.1h |
| strategy | 08-24T01:27Z | 10.7h |

**这不是"某一轮没东西可交"**(那是本台自己反复援引的良性解释),
**五条流同时停在 02:05Z 之前**,而本台此刻正常触发 ⇒ 是**宿主侧的停摆**,不是各组懒惰。

**但停摆期间 AWS 上是有动作的**:S3 `soak/` 出现 **`spot_20260824_0310*` 四个 run**
(ref `5a694f5`,armed `cmboots`)—— 即 **W5 在 03:10:28Z 被发出来过**,
而 `iterations/reports/batch-desk/` 里**没有 03:xxZ 的报告**、`queue.json` 里
`hero-8` 仍是 `status=pending`、章程"当前状态"停在 00:06Z。
⇒ **有一轮批测台发了波、花了钱(~$1.4)、然后整轮蒸发,交棒全掉。**
本轮把那一波**连同 W4 一起收割并补记**,`hero-8` 的 director 预登记(3 种子分母)照做。

---

## 1. AWS 引导 + 成本

`session_setup.sh` ⇒ `AWS ready: arn:aws:iam::581436534924:user/dota2bot-agent` ✅

| 项 | 值 |
|---|---|
| MTD | **$33.027** |
| 来源 | `budgets/dota2bot-batch`(**免费**,未花 $0.01 调 CE) |
| refreshed | **2026-08-24T07:57:03Z**(上轮是 08-23T15:30:33Z ⇒ **真刷新,不是复读**) |
| forecast / limit | 46.307 → **64.338** / 100.0 |
| 三线 | $45 围栏 / $90 刹车 / $100 owner 线 —— **全未触及** |

### 1.1 ⭐ 连载两轮的预登记成本区间:**到货,兑现在低区间**

上轮(00:06Z §5.1)从 `run.log` 的 `VALIDATE_ONSPOT_DONE` 独立推出**单波 ≈ $1.41**,
并预登记"下次真刷新的增量"作可证伪判据。本轮拿到真刷新:

```
$33.027 − $28.462 = +$4.565   (覆盖窗口 08-23T15:30Z → 08-24T07:57Z)
```

窗口内的付费动作**恰好四笔**:W3(08-23 18:09Z)、抢跑后立即终止的四台(00:09–00:12Z)、
W4(00:12Z)、W5(03:10Z)。按 $1.41/波 + ~$0.05 抢跑 = **$4.28**,
实测 $4.565,差 **$0.28** = S3 存储/请求等杂项。**吻合。**

**判据兑现:落低区间 ⇒ (甲) 完成即关成立**,与 22:07Z 那条"~1.2h/台空转 ≈ $3.3/波 ≈ 69%"
**相反**——那条上轮已判为错、本轮由账单侧**独立第二次证伪**。
`--hours 2 → 1` 的省钱提案**继续作废**。

**诚实边界**:`budgets` 快照的**数据截止时刻不可见**。若它其实**没含 W5**,
增量就要用 $2.87 的波费解释 $4.565,剩 $1.69 的杂项——比历史杂项高一个量级,
**拟合明显更差**。两种读法里证据偏向"三波全含",但这是拟合优度不是直接观测。

---

## 2. 收割:两波、596 局(必查项 ①②③ 一并兑现)

**标准路径** `recover_verdict.py`(实例自产 verdict 只当免费预览,见 §2.4)。
**必查项 ②(分 run 下载 + 前缀合并 + 逐位对账)**:本轮把前缀从 `<HHMMSS>__`
升级为 **`<HHMMSS>_<token>__`**(GH #95 的 run token 进前缀),对账逐位一致:

| 波 | 逐 run analysis 之和 | 合并后 | demclaim 之和 | 合并后 |
|---|---|---|---|---|
| W4 `campgrade` | 73+78+75+73 = **299** | **299** ✅ | 57+60+59+57 = **233** | **233** ✅ |
| W5 `cmboots` | 76+74+75+72 = **297** | **297** ✅ | 60+58+59+56 = **233** | **233** ✅ |

**必查项 ①(暖场 stamp = 发波时钉的 SHA)**:W4 暖场 24 局全 `6411851` ✅、
W5 暖场 24 局全 `5a694f5` ✅ —— **两波都没测错树**。
**必查项 ③(armed 串)**:两波 mirror stamp 各 8 个、armed id 集合各 **1 个**
(`{campgrade}` / `{cmboots}`),**独占波如裁**,count=uniq=1 ✅。

### 2.1 W4 `campgrade` verdict(树 `6411851…`,种子 888/895/896/906)

| seed | ab | ba | gpm | xpm | deaths | lh | wr | scored |
|---|---|---|---|---|---|---|---|---|
| 888 | 40 | 27 | −5.78 | −1.92 | +0.02 | −0.44 | 0.515 | 67 |
| 895 | 42 | 30 | −14.06 | −10.22 | +0.14 | −0.22 | 0.500 | 72 |
| 896 | 42 | 27 | **+19.37** | +14.93 | −0.06 | +0.89 | 0.525 | 69 |
| 906 | 42 | 25 | **+16.38** | +1.45 | −0.08 | −0.13 | 0.489 | 67 |
| **MEAN** | | | **+3.98 (2/4)** | +1.06 (2/4) | +0.01 (2/4) | +0.03 (1/4) | **0.507 (2/4)** | **275** |

`contrast=vs_stable`,`suggested=hold_or_reject`,`unfinished` **0**,有效局 275 + 暖场 24 = 299。

### 2.2 W5 `cmboots` verdict(树 `5a694f5`,同四种子)—— **分母按 3 个种子**

director 预登记(§AV.4/§AV.5)要求发波前跑载体门并把结果抄进来。**本轮补跑,逐字兑现**:

```
CARRIER seed=888 present=yes side=dire pos=4 satisfied=no      <-- 惰性种子
CARRIER seed=895 present=yes side=dire pos=5 satisfied=yes
CARRIER seed=896 present=yes side=radiant pos=5 satisfied=yes
CARRIER seed=906 present=yes side=radiant pos=5 satisfied=yes
CARRIER term=crystal_maiden:5 seeds=4 satisfied=3 verdict=PARTIAL carriers=895,896,906
```

| seed | ab | ba | gpm | xpm | deaths | lh | wr | scored |
|---|---|---|---|---|---|---|---|---|
| **888(惰性)** | 42 | 28 | **−5.41** | +4.52 | −0.09 | +0.13 | **0.518** | 70 |
| 895 | 41 | 27 | −5.48 | +0.75 | −0.11 | −0.88 | 0.489 | 68 |
| 896 | 42 | 27 | +5.54 | +1.18 | +0.00 | −0.07 | 0.466 | 69 |
| 906 | 42 | 24 | **+21.02** | +6.67 | −0.08 | +0.43 | 0.536 | 66 |
| **4 种子 MEAN(工具默认,不作数)** | | | +3.92 | +3.28 | −0.07 | −0.10 | 0.502 | 273 |
| **⭐ 3 种子 MEAN(director 口径)** | | | **+7.03 (2/3)** | **+2.87 (3/3)** | **−0.06 (2/3)** | −0.17 (1/3) | **0.497 (1/3)** | **203** |

`recover_verdict.py` **不知道载体门**,它按四种子平均 ⇒ **归档时必须带这张 3 种子行**,
否则惰性种子把效应稀释了 44% 还没人看得出来。

### 2.3 ⭐⭐ 真发现①:本台第一次拿到**波内自带的零控制腿**,噪声底有数了

`cmboots` 在 `hero_crystal_maiden.lua:121` 的唯一入口是
`sRoleItemsBuyList['pos_5'] = ArcaneBootsBuild(...)`,而 `X['sBuyList'] = sRoleItemsBuyList[sRole]`
⇒ **CM 不是 pos_5 时,armed 腿与 stable 腿逐字节等价**(镜像草稿保证两侧同为 pos_4)。
种子 888 正是这种情况(载体门 `satisfied=no`)⇒ **它是一条结构性的零腿**,
而且**跟候选腿在同一波、同一批实例、同一小时里跑**。

它读出来的不是 0,是:

```
seed 888 (structural null):  gpm −5.41   xpm +4.52   deaths −0.09   wr 0.518   n = 70 局
```

**这是本桌历史上第一个波内噪声底**——此前只有跨波 sd(45.81)和"随机草稿 SD ≈ 600 gpm/局"
这类**跨波读数**,而 §AF.3 早就说过跨波读数不作数。

**直接后果**:`cmboots` 的 3 种子效应 **+7.03 gpm** 与同一波零腿的 **|−5.41| gpm** **同量级**
⇒ 本波能买到的是**"没有明显负面"**,**买不到"它让经济变好"**。
winrate 侧同理:零腿偏离中性 **0.018**,候选 3 种子偏离 **−0.003**,**比噪声还小**。

**诚实边界**:n=1 条零腿 ⇒ 这是一个**点估计不是分布**;它只钉住"这个量级的读数不值钱",
不能反过来当显著性检验用。**建议**(交总监,不自裁):以后凡是**角色门/载体门 PARTIAL**
的 id,**别把惰性种子当损失** —— 它是免费的波内零控制,应当**在验收判据里预先声明为零腿**。

### 2.4 必查项 ⑧ + 实例自产 verdict 的第二次对账

`winner_by`:W4 = `economy_10min_cap` **296** / `economy_forcewin_recovery` **2** / **`engine` 1**;
W5 = `economy_10min_cap` **297**,`engine` **0**。
⇒ W4 的"独立于金钱"档 **1/299 局**,W5 **0/297** ⇒ **winrate 仍然只是 gpm 的符号粗化**,
不是第二个独立证人。**注:`engine` 档非 0 是本桌首次**(此前四波连续 0),
但 1 局不足以承载任何结论,**记录在案不解读**。

**必查项(上轮真发现④)第二次兑现**:实例自产 verdict `campgrade_…_-217` 读 888 = ab 40 / **ba 25**,
本轮全量重算 = ab 40 / **ba 27** ⇒ **仍是少 2 局**。`recover_verdict.py` 作为标准收割路径**第三次被坐实**。

---

## 3. 铁律 4(i) 强制项:ab / ba 两层分读 —— **两波都是纯侧偏形态**

| 波 | ab 均值 gpm | ab 均值 wr | ba 均值 gpm | ba 均值 wr | 残下的 candidate |
|---|---|---|---|---|---|
| W4 `campgrade` | **+64.12** | 0.702 | **−56.17** | 0.312 | **+3.98 gpm / wr 0.507** |
| W5 `cmboots` | **+65.71** | 0.654 | **−57.87** | 0.350 | **+3.92 gpm / wr 0.502** |
| (参考)W3 `creeppull` | +50.25 | 0.696 | −62.55 | 0.304 | −6.15 / wr 0.500 |

两层**逐项反号**,量级 **±56–66 gpm**,**比候选效应大一个数量级**;
winrate 两层几乎精确互补(0.70/0.31、0.65/0.35)。这是"Radiant 侧偏 ≈ +1.5k 金,
永远 swap-and-average"的**教科书形态第二、第三次复现**。

**⭐ 顺带一条可复用的常数**:三波独立语料上侧偏量级 **+50 ~ +66 / −56 ~ −63 gpm**,
**跨候选、跨树、跨日稳定** ⇒ 它是**装置常数不是候选性质**。任何单层读数(只看 ab 或只看 ba)
在这个装置上**必然被 ±60 gpm 的偏置吃掉**,这就是铁律 4(i) 的算术依据。

**按铁律 2(b) 判定**:两波 winrate 均值 0.507 / 0.497(3 种子口径),
四指标无一项呈明显负面,`unfinished` 全 0 ⇒
**`campgrade` 条件 (b) = 通过;`cmboots` 条件 (b) = 通过**(粗粒度,非显著性)。
**本台不裁 resolve/promote** —— (a) 在录像组、(c) 在申请组、裁定权在总监。

---

## 4. `--rec-slots 12` 第四、第五次验收 —— **两波均 exit 0**

```
W4: box factor 1.051 (4 control slots); compared 16 slot(s); 0 beyond tolerance ; OK on 299 games
W5: box factor 1.039 (4 control slots); compared 16 slot(s); 0 beyond tolerance ; OK on 297 games
```

净值(已扣 box factor):W4 录制槽 1–12 = **−3.2% ~ +5.9%**,W5 = **−4.0% ~ +5.5%**。
按 §AW.5 的单边读法(只有**赤字**超差才报警),**两波都是真通过**。

吞吐第二通道同向:两波录制槽 18–20 局/槽 vs 对照槽 16–17 ⇒
**12 个并发 SourceTV 仍然没吃掉任何一局**,超线性争用在这一档**第三次未出现**。

**⚠️ 但有一条要盯的趋势,现在报出来而不是等它撞线**:
**唯一的赤字槽始终是 slot 1**,而且**三波单调变深**:

```
W3  slot 1  net −3.3%
W4  slot 1  net −3.2%
W5  slot 1  net −4.0%      <-- 容差 5%
```

其余 11 个录制槽**全为正**。slot 1 的特殊性是**结构性的**(它起得最早、
且 `soak_loop.sh:170` 的扁平 `replays/` 镜像**只留 slot 1**,即它比别的录制槽**多一次上传**)。
**这不是判决,是一条趋势**:若下一波 slot 1 越过 −5%,验收会以"12 槽超差"的名义 exit 1,
而**真因是 slot 1 的额外镜像上传,不是 12 槽并发**。已写进 §7 的交棒。

---

## 5. 归属三判据逐局重核(必查项 ⑦)—— ⭐ 真发现②:上轮登记的预期**被证伪**

| 波 | claims | `method` | `logname==hostname` | `by_mtime` 同意 | `candidates` 分布 |
|---|---|---|---|---|---|
| W4 | 233 | logname 233/233 | **233/233** | 0/233(设计) | 4–12(众数 **10**) |
| W5 | 233 | logname 233/233 | **233/233** | 0/233(设计) | 3–12(众数 **10**) |

上轮(22:07Z)在 `dem_claim.sh:131-133` 定位到 `grep -aqF "$tag"` **无锚定前缀假阳性**
(`slot1` 是 `slot10` 的前缀),并据此把必查项 ⑦ 的期望值改成
**"期望 slot 1 在 `rec_slots ≥ 10` 时**稳定分歧**"**。

**本轮 466 份 sidecar 实测:分歧 0 例。** 加上上轮的 1/236,累计 **1 / 702**。

⇒ **"稳定分歧"这条预期是错的,本轮撤回。** 缺陷本身(无锚定前缀匹配)**仍然是真的、GH #152 不撤**,
但它的**发生率取决于候选池的遍历顺序**(谁先被 `grep` 撞上),
是一个 **~0.14% 的顺序竞态**,不是一个每局必现的确定性错配。
**修正后的期望值**:`logname==hostname` 期望 **≈100% 但不保证 100%**;
任何一例分歧都要点名到 tag,**不要因为"预期会分歧"而放过它**。
(`hostname_hits` 一栏保持**作废**,理由不变:`dem_claim.sh:131` 的短路使它结构上 ∈ {0,1}。)

---

## 6. 必查项 ⑩:`demclaim` vs `dem21/` 逐 run 对账 —— **W5 零差,W4 差 1(已点名到 tag)**

| 波 | run 尾 token | claims | `.dem` | 差 |
|---|---|---|---|---|
| W4 | `2651cb` | 57 | **56** | **−1** |
| W4 | `c17493` / `904f6d` / `2126ba` | 60 / 59 / 57 | 60 / 59 / 57 | 0 |
| W5 | `a6cb45` / `a2b6a9` / `1d9ae5` / `a3fa29` | 60 / 58 / 59 / 56 | 60 / 58 / 59 / 56 | **全 0** |

**点名**(GH #147 要求):丢的是 run `spot_20260824_001230…_2651cb` 的
**`20260824_003755_slot8`**,claim 本身完好且判得对
(`method=logname`、`by_logname==by_hostname`、`candidates=11`),**只是 `.dem` 没上到 S3**。

### 6.1 ⭐ 真发现③:这一例把 GH #147 从"偶尔丢录像"收窄成一个**有机制的尾局竞态**

把该 run 的 57 个 claim tag 排序,`20260824_003755_slot8` 是**最后一个**;
把 299 个 analysis 排序,它同样是**该 run 最后开局的那一局**。
而 `spot_run.sh:38-44` 的 `--validate` 语义是 **verdict 上传后立即 terminate、不等看门狗**,
该 run 的 `VALIDATE_ONSPOT_DONE` 落在 **00:43Z**。

⇒ **丢的不是随机一局,是"最后一局"**:末局的 `.dem` 上传**输给了立即终止**。
这解释了为什么丢失率极低(**1/466 = 0.2%**,且**每 run 至多 1 局**),
也解释了 12:09Z 那次的 **−2**(两个 run 各丢末局)。
**这是 harness 缺口,按章程不自己改** —— 已并进 **GH #147** 追评。

**真实帧通道**:W4 **233/299 = 77.9%**、W5 **233/297 = 78.5%**(构造上限 12/16 = 75%,
略高是因为录制槽比对照槽多跑了局)。**两波合计 466 个真 `.dem` 交给录像组。**

---

## 7. 英雄普查分母表(必查项 ⑨,固定交付)

| 英雄 | W4(299 局) | W5(297 局) | 局终等级 min/med/max (W4 / W5) | ≥15 (W4) | ≥15 (W5) |
|---|---|---|---|---|---|
| zuus | 299 (100%) | 297 (100%) | 8/12/19 / 6/12/17 | 32 (**10.7%**) | 36 (**12.1%**) |
| crystal_maiden | 299 (100%) | 297 (100%) | 8/11/17 / 7/11/18 | 13 (4.3%) | 17 (**5.7%**) |
| skeleton_king | 151 (50.5%) | 146 (49.2%) | 8/12/18 / 8/12/17 | 14 (9.3%) | 16 (11.0%) |
| lion | 75 (25.1%) | 75 (25.3%) | 8/11/**14** / 8/11/**15** | **0/75** | **1/75** |
| **axe** | **0** | **0** | — | — | — |
| **death_prophet** | **0** | **0** | — | — | — |

**⭐ 真发现④:`lion` 的 t15 域**不是**结构性空集 —— 本轮出现第一例。**
前三波合计 **0/231**,本台据此写过"在 Turbo 10 分钟裁判上限下是**结构性空集**,不是采样不足"。
**W5 出现 1/75**(局终 15 级)⇒ 那句断言**过强,本轮撤回**。
正确读法:五波累计 **1/381 ≈ 0.26%**,是**极稀有但可达**。
对 `hero-4` 的实际影响**几乎不变**(要在这个域上买读数需要三位数的波次),
但**"不可能"和"0.26%"是两种不同的话**,前者会让人去改判据,后者只让人换种子。

**其余三条第四/第五次复现**:`axe` / `death_prophet` 分母**仍是 0**
(`hero-2`、`hero-9` 在现行四种子下**结构上买不到**,等的仍是 `--find axe` ⇒ 899/910/911,**仍未上机**);
`zuus` ≥15 在 **10.7–15.7%** 之间、`crystal_maiden` 在 **3.9–8.7%** 之间来回摆
⇒ **`hero-3` 的分母必须按波报,别当常数用**(第四次复现)。

---

## 8. ⭐⭐⭐ 真发现⑤(本轮最要紧的一条):owner P1 的 PROMOTE **做完了、过闸了、没上 main**

`unlanded_commits.py` 报 **9 条 UNLANDED**,全是**总监的**,分布在两条支线上:

```
2026-08-24T03:11:23Z  2b8b573  director 01:xxZ: full lua suite closes at 1658 tests / 0 failures
                               -- the gate 8.5 held main behind is passed      [busy-bardeen-u45ms4]
2026-08-24T01:05:49Z  fae4468  director: PROMOTE creeppull + pullbeat to turbo defaults (stable-v1)
                                                                              [busy-bardeen-uxvcdx]
2026-08-24T00:52:19Z  11d5878  director: PROMOTE creeppull + pullbeat to turbo defaults (stable-v1)
                                                                              [busy-bardeen-u45ms4]
   (+ 6 条 rescue / rename / 修正)
```

**为什么这条压过本轮其他一切**:

1. **它是 owner P1 的终点**。W3 已买到 (b)(08-23 22:07Z 本台)、录像组已交 (a) WORKING(GH #149),
   总监在 00:52–01:07Z 做出了 promote 并在 **03:11Z 跑绿全量 lua 套件(1658 tests / 0 failures)**
   —— 即**上一轮报告里那句"main 故意不推,等全套关闭"的条件,03:11Z 就已经满足了**。
   然后宿主停摆,**这条棒掉在支线上 9 小时**。
2. **上一轮(00:06Z)本台亲手把它归为"已知有主,不升级"** —— 当时那是对的
   (commit 自己写明在等闸门)。**现在闸门过了,它就不再是"已知有主",而是掉棒。**
3. **它污染本台的基线定义**:批测实例**从 `origin/main` clone**。只要 promote 不在 main,
   `creeppull`/`pullbeat` 就仍是 gated ⇒ 本台后续每一波的**"稳定版"腿都少了两个已裁定的默认行为**,
   而 test_set.md 那边已经按 stable-v1/v2 记账。**这是会静默测错基线的那一类不一致。**
4. `bots/` 侧确认是真 promote:`jmz_func.lua` / `mode_roam_generic.lua` / `mode_laning_generic.lua`
   里 `if not J.IsSoakCandidate('creeppull') then return nil end` 被删、
   换成 `PROMOTED (was soak-candidate 'creeppull') 2026-08-23` 注释(与章程的 promote 约定一致);
   `hero_axe.lua` 的 `axebhpure` 门也一并去掉。

**本台不落地它**(章程:不写 bot 代码、不做判断裁定;而且该支线相对当前 main **已经发散** ——
`git diff origin/main u45ms4` 把 main 后来的 `hero 02:05Z`、`strategy 01:27Z`、`5a694f5`
显示成"删除",直接 cherry-pick 会回退别人的工作,**必须由总监 rebase**)。
**已开 `[batch]` GH #155 交总监**,并在 §13 交棒。

---

## 9. 启动决策:**不启动**(节流三条**全部满足**,卡的是排期权不是钱)

| 条件 | 读数 | 结论 |
|---|---|---|
| (i) 距上一波 ≥ 6h | 上一波 **03:10:28Z**,现在 12:2xZ ⇒ **9.2h** | ✅ 满足 |
| (ii) 有新东西可测 | 当前树 `5a694f5` 上全集累计种子数 **0 < 8** | ✅ 满足 |
| (iii) 已花 + 预估 ≤ $45 | $33.027 + ~$1.5 ≈ **$34.5** | ✅ 满足 |

**三条全过,本台仍然不发。理由是排期,不是成本**:

- **W6(`strategy-5b`)有 `APPROVED_CONDITIONAL` 但条件未兑现**:§AX.2 要求发波前先给出
  **事件轴量级**(每局塔防 TP 响应次数 / 核心占几次),该数在**已买的 `.dem` 里、零 AWS、属录像组**。
  裁定原文写死"**拿到之前不排发波时刻**" ⇒ 本台无权提前。
- **`strategy-7` / `strategy-8` 是 `DEFERRED_PENDING_W3_RESOLVE`**,解锁时刻 = W3 对 `creeppull`
  按 §AT.1 处置的那一轮。**总监其实已经 resolve 了(§8 的 promote),但它没上 main** ⇒
  **解锁条件在事实上成立、在仓库状态上不成立**。本台**不替总监宣布解锁**。
- **无 `director` 字段的请求**(`strategy-9` `creeppull,pullbeat,pullcad`、`strategy-10` `campsel`、
  `strategy-11` `pullcamp,pulllane`)按章程 4(a) 原规则是可发的,**但 `strategy-8` 的裁定明写**
  `pullzone` / GH #149 / GH #143 §3 "**是同一根绳子的两端,应当一起排,否则会买三次同一个域**",
  而 `strategy-9` 正在这根绳子上 ⇒ **单发它就是那条裁定明令避免的重复采购**。
- **不援引 4(b) 例行全集波**:队列**并非为空**(只是全被别的组卡住),
  而 §AU.2 把 W3–W6 定为**连续独占波**;在总监离线 15 小时、无法反对的情况下,
  由**唯一能花钱的 agent** 单方面插一个全集波去改变波次计划,
  正是 00:12Z 那次"抢跑 18 秒也要终止重发"所维护的同一条先例——**门槛不能因为对方不在场就变得可议价**。

**更实际的一条**:本轮刚交付 **596 局 / 466 个真 `.dem`** 两份全新语料,
而下游(录像组、总监、协同组)**全部停摆 10–15 小时、一份都没消化**。
在两份未分析的语料上再叠第三份,是**把钱花在瓶颈的下游**。

---

## 10. 局数(owner 2026-08-19 固定节)

**(a) 上一波(其实是上两波)的最终有效局数**:

| 波 | 有效局 | 暖场 | 合计 | per-seed ab/ba | unfinished |
|---|---|---|---|---|---|
| W4 `campgrade` | **275** | 24 | **299** | 888 40/27、895 42/30、896 42/27、906 42/25 | **0** |
| W5 `cmboots` | **273** | 24 | **297** | 888 42/28、895 41/27、896 42/27、906 42/24 | **0** |

**ab/ba 不对称照旧**(radiant wave 全部跑满 40–42,dire wave 24–30),
且**再次不是截断**:两波 `run.log` 的 `n/TARGET` 计数器实测**每个 wave 都达标返回**
(如 888 radiant 收在 **24/22**、888 dire 收在 **25/22**),`--games 22` 的 `TARGET` 被超射。
**"dire wave 被 2h 看门狗截断"这条已被第三次独立证伪。**
`run.log` 另给出**开波到首局 ≈ 7 分钟**(00:14 起 0/22,00:21 跳到 9/22),与上轮 7.5 分钟一致。

**(b) 本轮在跑波次的实时进度**:**无在跑波次**(§9 未启动)。

---

## 11. 泄漏检查(四层 + spot 请求)

| 层 | 开工 | 收尾 |
|---|---|---|
| 运行中实例 | **0** | **0** |
| 游离卷 | 0 | 0 |
| 快照 | 1(AMI `ami-0a990a26d89c66547` 基底,唯一常设成本) | 1 |
| EIP | 0 | 0 |
| spot 请求 | 0 | 0 |

**无泄漏。** W4/W5 八台均已按 `--validate` 语义完成即自毁(W4 ~00:43Z、W5 ~03:42Z,
`VALIDATE_ONSPOT_DONE` 为证)。**本轮无活波次 ⇒ 收尾判据回到"0 台在跑"**,收尾走 `--leak-only`(零成本)。

**固定栏位**:`soak/` **184**(176 → 184,+8 = W4 四个 + W5 四个,**预期**)、
`dem21/` **36**(28 → 36,+8)、`unattributed/` **0**、
`validation/` 最新 **2026-08-24T03:42:42Z**(`cmboots_20260824_0342_run.log`)、
远端 main tip **`5a694f5`**(与 W5 所测树一致;**但见 §8 —— 它少了已裁定的 promote**)。

---

## 12. 验证(铁律 6)

本会话 **`bots/` / `game/` 逐字未动**(改动仅 `iterations/` 下)⇒ 铁律 6 无适用对象;
容器无 `luacheck` / `lua5.1`,**不声称跑绿过 Lua 全量**。
`queue.json` 实测 `json.load` ok 且按原 `indent=2` 回写。
**开工自检 worst exit 3**:UNLANDED **9**(§8,**本轮升级为掉棒并开 issue**)、
trunk python **18 passed / 0 failed**、cadence **6 个 finding**(§0)。

---

## 13. 铁律 9 的交棒(每一棒都点名下一个持棒人)

1. **⭐ PROMOTE 落地棒 → 总监(最高优先)**:`creeppull` + `pullbeat`(+ `axebhpure`)的 promote
   在 `origin/claude/busy-bardeen-u45ms4`(`2b8b573`),**闸门 03:11Z 已过**(1658 tests / 0 failures),
   相对当前 main **已发散,需 rebase 不是 cherry-pick**。**未上 main = 每一波的稳定版腿都是错的**。
   已开 **`[batch]` GH #155**。
2. **(a) 核验棒 → 录像组(两条,互不阻塞)**:
   - `campgrade`:`dem21/spot_20260824_0012*/` 下 **233 个真 `.dem`**,按 `strategy-4.acceptance`
     三条判(**含不许省的反向判据 ②:armed 侧全体远古交火不许塌成 0**);
     **务必先读 00:06Z 报告 §3.1 的死代码提醒**(`GetClosestNeutralSpwan` 的 `>= 10` 远古门两臂都死
     ⇒ 若 ≤11 级远古交火没降到接近 0,**有第二个现成解释**)。
   - `cmboots`:`dem21/spot_20260824_0310*/` 下 **233 个真 `.dem`**,
     **分母按 3 个种子(895/896/906),种子 888 是零腿不是样本**(§2.2/§2.3)。
   - 跨 run 统计**一律用 run 前缀限定 tag**(裸 tag 会静默少算,§AZ 已记)。
3. **W6 解锁棒 → 录像组**:`strategy-5b` 卡的那个**事件轴量级**(每局塔防 TP 响应次数 / 核心占几次)
   在**本轮新交付的 466 个 `.dem`** 里,零 AWS。**这个数一到,W6 就能排发波时刻。**
4. **resolve / 排期棒 → 总监**:`campgrade` (b) 通过、`cmboots` (b) 通过(3 种子口径)
   ⇒ 两个名额的算术请总监重算;`strategy-7` / `strategy-8` 的解锁条件**事实上已成立**(见第 1 棒)。
   另请裁一条新规:**载体门 PARTIAL 的惰性种子应在验收判据里预先声明为波内零腿**(§2.3)。
5. **`[harness]` 棒 → 总监/harness**:
   - GH #147 追评:末局 `.dem` 输给 `--validate` 的立即终止,**机制已定位**(§6.1),
     每 run 至多丢 1 局,**修法是终止前 flush 上传队列**;
   - GH #152 修正:"slot 1 稳定分歧"**证伪**,实为 ~0.14% 的顺序竞态(§5),**缺陷不撤、期望值改**;
   - **新**:slot 1 net 三波单调走深(−3.3% → −3.2% → **−4.0%**,容差 5%),
     结构原因是它**比别的录制槽多一次扁平 `replays/` 镜像上传**(`soak_loop.sh:170`)⇒
     **撞线时不要误判成「12 槽并发超差」**(§4);已并进 **GH #75** 追评。
6. **停摆棒 → owner / Cursor 整合者**:五条流同时停 10–15 小时,且**丢了一整轮批测台**
   (发了 W5、花了 ~$1.4、零记录、交棒全掉)。已 PushNotification。

---

## 14. 本次会话 token 用量(铁律 8)

```
usage_records: 102 (blocks; 50 duplicate)
input_tokens (uncached): 104
cache_read_input_tokens: 5,822,118
cache_creation_input_tokens: 176,173
output_tokens: 56,647
TOKENS total_in=5,998,395 out=56,647 turns=52
```

(统计时刻之后的收尾回合不计入,当量级参考。)
