# 批测台报告 2026-08-22T02:06Z(第三十八次触发)

**纯运维 + 归档取证轮,未启动任何批测,本会话真实 AWS 支出 ≈ $0.00**
(免费 Budgets 路径**连续第九轮**生效,CE 调用 **0** 次;本轮**未下载任何 `analysis.json`**,
付费面只有若干次 `s3 ls`,Tier2 LIST ≈ $0.0001)。

---

## 1. AWS 引导与成本

- `bash tools/batch_test/aws/session_setup.sh` → `AWS ready: arn:aws:iam::581436534924:user/dota2bot-agent`
  (容器无 AWS CLI,脚本自行 pip 装了 aws-cli/1.46.0)。之后一律 `awsx`。
- `check_costs.sh`:MTD **$18.587**,forecast **$23.11**,budget limit $100,
  **budget refresh 时间戳 `2026-08-21T21:23:52Z`** —— 与 22:06Z / 00:06Z **读的是同一次刷新**,
  故这是**第三轮**引用同一个数,**不构成新信息**(按 20:06Z 立的口径如实标注)。
- $45 围栏余 **$26.41**;$90 刹车线余 $71.41。**未触发任何预算刹车。**
- CE 调用 **0** 次(连续第九轮)。

## 2. 收割:无新数据(连续第十五轮)

- `soak/` 前缀总数 **140**,逐字不变;最新前缀仍
  `spot_20260820_180808_1_11a8de336248927265d20af677f944085a02d248/`。
  ⇒ 索引及派生栏位按构造不变,不重跑 `dem_inventory.py`。
- `validation/` 最新对象仍是 **2026-07-23** 的历史 verdict(`…midguard_20260723_0045_run.log`)。
- `iterations/queue.json`:`requests` 为空(`director-1` 已 `done`)⇒ 走章程 4b。

## 3. ⭐ 实质交付:基线腿**确实漂移了**,漂在 6 个点上 —— 而 5 个是「修好了缺陷」

这是 00:06Z 明确挂出的候选免费工作单元(「逐行审那 356 行新增代码的 gate 覆盖率,
给出『基线腿整体是否漂移』的完整答案」)。**答案:漂了,可枚举,清单在下面。**

### 3.1 为什么这是批测台的活

总监 §AI.4 立的规矩要求以「对照臂 = 0」立条件 (a) 的必须同时报**该签名在出厂代码上的本底率**,
并写明它「在镜像装置里是**免费的** —— 就是基线腿」。**这句话在一波之内为真,跨树未必为真**:
基线腿 = **那棵树**的出厂代码,而不是一个跨时间的常量。批测台是归档持有方,
所以「档案里那些 `.dem` 的基线腿,和今天 HEAD 的基线腿是不是同一份代码」由本台回答。

比对区间:语料树 `c2181e05afde6c46dd16c5ecd8321bda82e80406`(`lf_rescue` 语料)→ HEAD `450c490`。
`bots/` + `game/` 共 **13 文件 / +1354 −52**。

### 3.2 方法(两层,第二层是必须的)

**第一层(机械)**:剥掉 Lua 注释与字符串字面量后按块结构走每个文件,维护 `(if/while/for/do/function)`
栈,凡任一入栈块的条件里出现 `IsSoakCandidate` / `IsLaneFixOn` / `IsLaneFixActive` 即判该行 gated。
1354 行新增里:**注释/空行 991、gated 72、live 291**。

**第二层(逐点手核,不可省)**:那 291 行里**语义上真活的只有约 20 行 / 6 个点**,
**词法法过报了约 48 倍**。过报的三个来源全部记录在案,下次做同类审计直接照抄:

1. **新 helper 的函数体词法上必然「live」,gate 在它自己第一行**
   (`od_GetEclipseAoeLocation` / `zuus_ShouldSaveManaForUlt` / `_roamreach_BoundedChase` /
   `lion_IsDrainSafeToStart` / `lion_ShouldStopDrain` 五个都是这个形状),
   或 gate 在**调用点**(`BlinkFirstBuild` 两处调用点 gated)。
2. **多行条件**:`jmz_func.lua:7135` 的 `J.SafeToCommitFightOnArrival` 词法判 live,
   而 `J.IsSoakCandidate('tparrive')` 就在 **7134 行**,同一个 `and` 链里。
3. **早返回式 gate**:`J.GetReadyHardCc` 的第二遍扫描前有
   `if not J.IsModeTurbo() or not J.IsSoakCandidate('esaftershock') then return nil end`,
   块栈模型看不见早返回。

⇒ **纪律:gate 覆盖率不能只用词法工具报数。工具用来把 1354 行收敛到 291 行,
结论必须逐点手核。** 这与 §AH.2 同族:一个按构造会过报的检查,它的「没漂移」同样不可信,
只是这次失效方向是**保守**的(过报),不像 `exit 128` 那次朝危险方向失效。

### 3.3 清单:**live(基线腿上真活)的 6 个点**

| # | 位置 | 改动 | 基线腿上的净效果 |
|---|---|---|---|
| **D1** | `mode_team_roam_generic.lua:244-246` | `roamstale` **已 promote 为默认**(去 gate,Turbo-only):`hTargetCreep = nil` 每帧重置 | **真漂移**,roam 模式的动作选择 |
| **D2** | `aba_defend.lua` **×9**(`WeightedEnemiesAroundLocation`、`ShouldDefend`) | `({string.find(…)}) ~= nil` → `__TS__StringIncludes(…)` | **真漂移**,见 3.4 |
| **D3** | `utils.lua` `IsUnitWithName` / `FindAllyWithName` | 同一族 truthiness 缺陷 → 真子串判定 | **真漂移**,影响全部消费方 |
| **D4** | `jmz_func.lua:4458` `J.CanBreakTeleport` | `J.GetCastPoint` → `J.GetCastDelay` | **真漂移**,打断 TP 的判定 |
| **D5** | `jmz_func.lua:8792` 新增 `J.IsThereCoreInLocation` | `hero_largo.lua:416` 原本是 **nil 调用**(运行时报错)→ 现在是可用谓词 | **真漂移**,仅 Largo |
| **D6** | `hero_invoker.lua:1198` | `J.Unit.` → `J.Utils.`(修 nil 索引) | **名义漂移,行为不变** —— 该 `or` 右支不可达(`J.IsValidTarget` 与 `J.IsValidHero` 是同一个委托,`A or (A and X)` ≡ `A`),已有测试钉住 |

### 3.4 D2/D3 那一族值得单独说:**旧树的基线腿跑的是「永真」的代码**

`{string.find(name, "x")}` 是**表构造式**,永远非 nil ⇒ 语料树 `c2181e0` 上:

- `WeightedEnemiesAroundLocation` 的权重链里,凡走到 elseif 链的单位**一律命中第一条
  `upgraded_mega` 分支 +0.6**;`siege and not upgraded`(写成 `~= nil and == nil`)**恒假**,
  `warlock_golem`/`lone_druid_bear`/普通小兵三条分支是**死代码**。
- `ShouldDefend` 的同型链首条 `siege and not upgraded` **恒假**,第二条 `upgraded_mega` **恒真**。
- `utils.IsUnitWithName` 旧实现 `local result = {string.find(...)}; return result ~= nil`
  ⇒ **恒返回 true**。

⇒ **结论的锋利处不在「有 6 处改动」,而在方向**:6 处里 5 处是**修复**(永真判定 / nil 调用),
所以**语料树的基线腿比今天的基线腿更坏** —— 它不是一个稳定的「出厂参考」,
而是**当天恰好还活着的那批缺陷的快照**。

### 3.5 给 §AI.4 的具体口径(建议,裁定归总监)

§AI.4 (ii) 的「本底率是免费的」在**同一波之内**成立(00:06Z 已从
`J.IsSoakCandidate` 源码复核:id 命中后仍过侧向判定,基线腿每个 gate 都关)。
**跨树引用要加一条**:从归档语料读出的本底率是**那棵树的**本底率,
只有当该签名的装置避开 D1–D5 时才等于今天的出厂率。

- **已核干净**:`lf_rescue` 的救援-TP 装置(00:06Z 逐点核过 `J.GetNearbyLocationToTp`
  函数体 md5 相同、9 个调用点逐字相同)⇒ `#96` 的 `4/16` 本底率今天仍可引用。
- **有风险的签名族(点名,便于各组自查)**:读 defend 模式 desire / 权重的(**D2**,
  这条改动量最大)、读 roam 模式攻击动作的(**D1**)、读 TP 打断的(**D4**)、
  Largo 相关的(**D5**)、任何用 `IsUnitWithName` 的检测器(**D3**)。

### 3.6 已核实为 no-op 的删除(**是查过的,不是假设的**)

- `hero_lion.lua` 删 `nKeepMana = 400`:全 `bots/` **零读者**(在树注释里已记录并复核)。
- `hero_tiny.lua` 删 `and bot:GetHealth() > 0.15`:`GetHealth()` 返回**绝对 HP**,
  活着即 ≥1 ⇒ 恒真,删除行为等价(`tests/test_tiny_treegrab_hp_noop.lua` 钉住)。
- `mode_retreat_generic.lua` 的 `towerreach` 改写:未 arm 时 `nTowerVeto == #nEnemyTowers`,
  与旧式 `#nEnemyTowers == 0` 逐字等价。
- 同文件 `retnear`:未 arm 时 `nSeenAugment == unseenCount`,与旧式等价。
- `hero_crystal_maiden.lua` 删 `cmrguard` 整段:删的是 gated 代码,基线腿无感。

### 3.7 已确认 inert 的 gate(逐个读过第一行/调用点)

`odaoe`、`zusult`/`zusultx`、`liondrain`、`liondrainstop`、`roamreach`、`blinkflee`、
`tparrive`、`tpdead`、`retnear`、`towerreach`、`esaftershock`、`defclose`、`defnum`、
`defstale`、`axebuyblink`(经 `BlinkFirstBuild` 调用点)、CM 的 R 系列。

### 3.8 诚实边界

- 本审计只覆盖 `c2181e0..450c490` 的 `bots/` + `game/`,**不是**「所有归档语料树 vs HEAD」;
  更早的语料树(07-19/07-2x 那批)未审,它们与 HEAD 的距离**只会更大**。
- 第二层是**逐点手核 291 行的落点**,不是对 gated 分支做语义等价性证明;
  对 gated 点我读的是「它第一行/调用点确实有 gate」,没有逐个证明其未 arm 分支与旧代码逐位等价
  (3.6 那五条是例外,那五条真的核了)。

## 4. 启动决策:**不启动**

例行三条件**形式上全部满足**:
(i) 距上一次**例行**波次 08-20T16:09:02Z 已 **~34h**;
(ii) `11a8de33..` 之后 `bots/` 有变更;
(iii) $18.59 + ~$1.40 ≈ $20.0 ≪ $45。

**但「无申报目的则不启动」仍压着。** 逐节核对 `test_set.md` 尾部:
最新一节仍是 **§AI(23:0xZ)**,自 00:06Z 那轮起**逐字未变**
(`git log -- iterations/streams/test_set.md` 最新提交 `db358e8 director 23:00Z`)。
§AI.1(`#99` 词法剥离 prose)/§AI.2(section 7 六变异六红)/§AI.3(`#96` 判 `lf_rescue`
(a) = WORKING+BUGGY,id 不动)/§AI.4(分母通例)/§AI.5(`#37` 验收帧口径)
**全是判读、纪律与工具条款,测试集逐字未变,无一条排波次**。

下一波唯一已知的有目的候选仍是 §AG.4 (1)(2) 之后的 `roamreach`,
而 (1) 的 `VICTIM_HP` 扫描是**免费离线**且 §AG.6 已把它与 `[harness] #92` 并成同一工作单元
⇒ 在它出数前本台不排波次。跑道按 $1.40/波仍是 **~18 波 4 台单臂 或 ~9 波 8 台两臂**。

**run_id 唯一性纪律(22:06Z 新增)本轮无适用对象** —— 未做任何 `spot_run.sh` 调用。

## 5. CE 必查项:**改排到 08-23**,不再逐轮顺延

目标日 08-22 的 CE 用量在 UTC **02:06Z** 只覆盖 2 小时,且 CE 数据本身有滞后 ⇒
现在查既花 $0.01 又读不到一个完整日。**00:06Z 已因同样理由顺延过一次,再逐轮顺延两小时是空转** ——
改为**在 08-23 的任一轮一次性查 08-22 的完整日**(期待 qty 0-2)。
08-21 已判定被污染(~16 次),**看到 16 不要误判成修法失败**。

## 6. 泄漏检查(四层,全免费只读,开工与收尾一致)

| 层 | 结果 |
|---|---|
| 实例(pending/running/stopping/stopped) | **0** |
| EBS 卷 | **0** |
| 自有快照 | **1**(`snap-0ad026b386c804288`,160 GB,即 AMI) |
| Elastic IP | **0** |
| open/active spot 请求 | **0** |

**无泄漏。**

## 7. 验证(铁律 6)

本会话**未改动任何 Lua**(改动仅 `iterations/` 下报告与章程);容器无 `luacheck` / `lua5.1`,
两者均未运行,按立法目的本轮无适用对象。**未新增工具脚本**——第 3 节的块结构分析器是一次性
`python3` 脚本,落在 scratchpad,不入库(若总监认为该审计要可重复,再提 `[harness]` 立案)。
第 3 节每个数字均由实跑 `git fetch --depth 1 <全40位SHA>` + `git diff -U0` + `git show` +
逐点 `sed`/`grep` 得出,**无估算项**;唯一未做的事已在 3.8 明写。

## 8. 局数

- **本轮无在跑波次。**
- 上一波(`lf_rescue` 两臂)最终有效局数:arm A **280**(radiant 166 / dire 114)+ 24 暖场,
  arm B **292**(radiant 168 / dire 124)+ 24 暖场,两臂 **572**。
- **本轮下载 `analysis.json` 0 个**(只读了 140 个前缀名 + 若干 `s3 ls`)。
- 档案累计未变:**11,048 在册 + 3,367 暖场 / 137 run / 112 种子**;整桶计费 13.7 GB,
  其中 `.dem` 8.79 GB ≈ $0.20/月。
- **新增可引用**:`c2181e0..HEAD` 的 `bots/`+`game/` = 13 文件 / +1354 −52;
  1354 行新增 = 注释/空行 991 + 词法 gated 72 + 词法 live 291;
  **291 行词法 live 收敛到 6 个语义 live 点(其中 1 个仅名义),过报约 48 倍。**

## 9. 跨组

在既有 **`[bug] #96`** 下追评(**不新开** —— §AI.4 正是从 #96 长出来的,
本轮交付的是 §AI.4 那个「免费本底率」在**跨树引用**下的边界)。
`[batch] #70`、`[harness] #98`/`#92`/`#75`/`#33` 仍开着,不重复开。

## 10. 下次触发

- 固定栏位照旧(MTD 走免费 Budgets,注明 refresh 时间戳是否新;`soak/` 前缀数;
  off-roster / `unattributed/` / `dem21/` / `.demclaim.json` sidecar 四个 0)。
- **CE 必查项:08-23 查 08-22 完整日(qty 0-2)**,不要再按 2 小时窗口顺延。
- 核树走章程步骤 5 两步法 + **全 40 位 SHA**(**exit 128 = 不可比,不是无漂移**;
  **空 diff 要配正控才算数**);泄漏检查沿用四层版本;**无申报目的则继续不启动**。
- **必须传下去的三条**:
  1. **基线腿跨树不是常量** —— D1–D5 五个点是真漂移,方向是「旧树更坏」;
     §AI.4 (ii) 的本底率跨树引用要先核该签名的装置(3.5 的风险签名族已点名)。
  2. **gate 覆盖率的词法工具过报约 48 倍**,三个过报来源(helper 内部 gate、多行条件、
     早返回 gate)记在 3.2;结论必须逐点手核。
  3. 20:06Z / 22:06Z 两条仍有效 —— `lf_rescue` 语料是 **11 个 run 不是 8 个**,
     重算取 **8 run / 16 vs 16 / 8r-8d**;上机时四次 `COUNT=1` 调用之间**跨过整秒**
     + 启动后读 `soak-run` 标签核对四值互异。
- **候选的免费工作单元**:把 3.3 的方法推到**更早的语料树**(07-19/07-2x 那批),
  给出「每个归档波次的基线腿与今天差多少」的一张表 —— 那是 `[harness] #75` 语料可得性
  之外的另一半:**语料的可比性**。

## 11. Token 用量

```
TOKENS total_in=8,931,135 out=81,931 turns=78
```
(`python3 tools/agent/token_usage.py` 的最后一行,原样贴入。注:这是"到统计时刻为止"的数字,
统计后的收尾回合不计入;`turns=78` 明显大于本会话实际的工具轮数,该脚本读的是容器内全部
transcript,量级参考即可。)

## 12. 收尾泄漏复查

`awsx ec2 describe-instances`(pending/running/stopping/stopped)= **0 台在跑**,与开工时一致。
按章程步骤 6,收尾这次要的是"0 台在跑"而不是 MTD 数字,故未重读花费(零成本)。
