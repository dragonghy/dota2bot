# 批测台 2026-09-05T21:13–21:5xZ

**一句话**:发 W49(61-id 家族第一波,spot 四台,~$1.02)——挡了三轮的东西这轮全部让开了,
臂串自己动了(63→61,总监按 owner 的 P4.2 把 `stayattr` 退了集);
**但本轮真正该被读的不是那四台机器,是泄漏检查里那台不属于本项目的机器 ——
它上一轮被记成「已不在」,而它从来没走,已经连跑 16.3 小时。**

---

## 一、自检(铁律 10)

⚠️ **开工第一条命令又误用管道** —— 脚本自拒并逐字点名
(`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked`,
自陈「evidence discipline 3,已复发 5 次,每次都是当轮第一条命令」)。
本台**第七次**。那个 `SELFCHECK_EXIT=2` 是 **REFUSED,不是通过**。
改重定向重跑,`timeout 400` 截断(`EXIT=124`,同样不是通过);
第三次按上一轮交棒③**放后台跑**,收尾时取真码。

⭐ **上一轮交棒 ② 本轮结清,而且不需要 `git stash`。**
交棒说「确认 trunk 那五条红是不是 main 也红」,做法写的是 stash 后重跑。
本轮**不必**:`git diff --stat origin/main -- bots/ game/ tests/ tools/` **为空**
(本会话唯一改动是 `iterations/data/seed_roster_index.json`,由 `--build` 写)
⇒ **工作树在这四个目录上逐字节就是 `origin/main`**,
「工作树红」与「main 红」在本轮**是同一个命题**,不是两个。
⇒ **那五条红是 main 的红**,不是任何人本地的脏树。
(这是结构性论证,不是又跑了一遍 —— 自检那句诚实边界
「main 是不是也红这一行不能确立」预设的是工作树**可能**有改动;本轮它没有。)

真码与其余各腿见第九节(收尾补记)。

## 二、成本与闸 (iii)

`AWS_SETUP_EXIT=0`(`arn:aws:iam::581436534924:user/dota2bot-agent`),`COSTS_EXIT=0`。

九月 MTD **`$19.597`**(免费 `budgets`,`refreshed 2026-09-05T11:21:28Z`)——
**与 12:15Z、18:07Z 两轮逐位相同**,即 **W48(12:24Z)与 Y1(15:16Z)两波都还没落账**,
正是章程写死的 4.3–11.3h 滞后。⇒ 按**章程 2 甲**,围栏算术**不用裸 MTD**,
把这两波按各自界别计进 `--pending`:W48 spot **`$1.02`** + Y1 按需 **`$1.09`** = **`$2.11`**。

闸 (iii) 按 GH #504 走工具、不抄常数,并按保守法**两种市场类型各过一次**:

| 估值 | 命令 | 退出码 | projected | headroom |
|---|---|---|---|---|
| spot | `--planned 0.90 --pending 2.11` | **`FENCE_SPOT_EXIT=0`** | `$22.607` | **`$27.393`** |
| 全程降级按需 | `--planned 2.15 --pending 2.11` | **`FENCE_OD_EXIT=0`** | `$23.857` | `$26.143` |

工具**现读**的操作数:limit `$100.00`、actual `$19.597`、三档 ACTUAL 告警
`$50` / `$80` / `$100` **全部 NOT yet crossed**、fence **`$50.00`**、brake `$90.00`、
operative ceiling **`$50.00`**。⇒ **本轮未跨任何告警档,owner 不会因本波收到 Budget 邮件。**

⚠️ **`$0.90` 单波成本常数的自动失效条款仍活着且仍未被重裁**(W46 实测 `$0.959–$1.022` 已越过)。
**第三轮连续点名。** 本波照旧在波次记录里登记**高的那个数 `~$1.02`**,失效方向朝安全侧。

## 三、收割:无对象

`running,pending` 进场为空;S3 无未收割 run。**W48 与 Y1 都已在上两轮收完**
(W48 gpm `+27.25`,4/4 干净;Y1 胜率 `0.550`,gpm 无量具)。
本轮是**发波轮不是收割轮**。

## 四、⭐⭐ 挡了三轮的东西这轮全让开了 —— 而且是被 owner 的优先项让开的

前三轮(06:17Z / 09:13Z 的「三道闸全过仍不发波」)卡住的是**闸 (ii) 读不出差别**:
臂串与 W46/W47 逐字节相同,只能靠手算 GH #513 那段。本轮**串自己动了**:

| | W48 | 本轮 |
|---|---|---|
| ids | 63 | **61** |
| bytes | 559 | **540** |
| md5 | `4aefc887f3f8c9173e7ac7024b3c20c9` | **`824ec2842e234693cb3c4094f73d6a14`** |

**差的两个 id 是 `stayattr` 与 `zusstatic`,方向是「出集」不是「入集」,新增为零。**

`stayattr` 的退集就是 **owner 的 P4.2** 点名要求的那一次纠正
(「2026-09-05T10:xxZ 的 `stayattr` 入集(62→63)即为违例:下一总监轮把 stayattr 退集回 62」)。
⇒ ⭐ **本轮是「优先项推进」第一次以批测台能测的形式出现**:
P4.2 的完成度量是**集合变小**,而这一波测的正是变小之后的那个集合。

⭐ **冻结确实在生效,而这有独立证据**:自 W48 发波以来 `bots/` 落了**四个新 gated id**
(`zusboltdmg` / `staybottle` / `wkbonefight` / `axecallbkb`),
**一个都没有进臂串** —— 若冻结是空话,串应该是 63+4,而不是 61。

**闸 (ii) 的第二支本轮也第一次活着**:61-id 家族**累计 0 粒种子** ≪ 8。
⇒ 两支同时成立,**GH #513 那段手算本轮用不上**(该 issue 仍 `open`,仍未变成工具 —— 下一轮串若不动就又要手做)。

## 五、⭐⭐⭐ 发波 W49(61-id 家族第一波),记录全文 `waves/W49_wave.json`

**树**:`--ref` 钉 40 位 SHA **`066219d61009c35357de49cfe3e8521940b09109`**,不是 `main`。
`git ls-remote origin main` **读两次**(闸时点 + **发波脚本内第一条命令**,不等就 exit 9 什么都不发),
两次都是 `066219d6`;脚本还**从发波树自己重新导出臂串**与已过闸的那份逐字比对(不等则 exit 8)。

**四道闸真码**:

- **(i)** ⭐ **本轮是修好后的 `wave_throttle.py` 首飞**(GH #534,总监 `57bd9338`)。
  它现在**两个族都打、锚取最晚那台**:
  ```
  family candidate : Y1   last machine 15:16:56Z -> unlock 21:16:56Z  <- ANCHOR
  family candidate : W48  last machine 12:24:12Z -> unlock 18:24:12Z
  ```
  **这正是上一轮只能手算、并明写工具看不见的那个保守时刻。**
  跑两次:决定时点 `21:15:35Z` 在解锁**之前**,**没有拿它当通过**;
  **动作时点、发波循环前最后一条、脚本内裸 `$?`**:`21:19:55Z`
  **`THROTTLE_EXIT_AT_ACTION=0`**,`UNLOCKED`,**margin `+179s`**。
  ⚠️ **这个余量薄,如实登记**:波是在一个 6h 窗口打开 3 分钟后发的 —— 但发在**正确的那个(更晚的)锚上**。
- **(ii)** 见第四节。
- **(iii)** 两种估值 `FENCE_SPOT_EXIT=0` / `FENCE_OD_EXIT=0`,见第二节。
- **(iv)** **`RB_EXIT=0`** 对 `W48_wave.json`:`not blinded -- the wave delivered 4 paired seed(s)`、
  抢占前回收 0 台、四粒全 depth-checked,**`NEXT WAVE: spot (GH #158 unchanged)`**。
  ⚠️ **第一次调用用裸位置参数得 `exit 2`,该工具吃 `--wave-json`;`exit 2` = 没跑成,永远不是「没瞎」。**

**两道内容闸**:

- **`CT_EXIT=0`**(61 id ⇒ 12 hero-scoped / 49 generic / **0 unresolved** ⇒ **8 项**)。
  ⭐ **`TERMS` 行与 W48 逐字节相同**,尽管集合少了两个 id —— 因为退集的两个**都不是任何一项的唯一载体**。
  ⚠️ **第一次调用误用 `--cand` 得 `exit 2`**:`carrier_terms.py` 吃 `--arm`,
  `check_armed_wiring.py` 吃 `--cand`,**两个工具旗标不同**。`exit 2` 不是内容读数。
- **`ARM_EXIT=0`** —— **all 61 armed ids wired on HEAD**。

**选种**:⭐ `seed_roster_index.py --build` **在选种之前**跑(`ROSTER_EXIT=0`;
355 前缀 / 351 已索引 / **4 个新扫正是 W48 那四台** / 现 **217 粒 41 英雄**)——
不 build 就会把 W48 刚烧掉的四粒当「从未用过」献回来。
窗口**右移**至 **`[5401,5800]`**(W48 在 `5001–5400`),400 粒全未用;
popcount 分布 `{0:27,1:107,2:136,3:93,4:30,5:5,6:2}`;
对 popcount≥3 的 130 粒穷举得 **2456** 组满足「8 项各 ≥2」的四元解(W48 是 2506,**相当,不紧**)。
平局判据沿 W45–W48 先例、在解空间内部取、零成本:**最大化 pudge 载体**
⇒ 本窗口最好只有 **3/4**(W48 是 4/4),50 组并列,取字典序最小者以求确定性
⇒ 选定 **`5404 5502 5574 5705`**。
⚠️ **一条朝坏方向的读数要登记**:本窗口 **pudge 供给只有 49**(W48 那个窗口是 70),
popcount≥4 那层也更薄(30+5+2 vs 31+9)——
**章程说的「每波削掉最富那层」这次咬在最稀缺的那一项上。**
**`CARRIER_GATE ids=12 seeds=4 exit=0`**(项**由串推导**不手打),
逐项 ck2/cm2/lion3/od2/**pudge3**/wk2/sb2/zuus2。

**市场与放置**:**spot**(不传 `--on-demand`,GH #158);
采集配置 `--slots 16 --hours 2 --rec-slots 8 --games 12` 4×1,
**与 W39–W48 逐字节相同** ⇒「id 集与采集配置不同波动」本波满足(本波动的是 id 集)。
⭐ **GH #252 / #256 验收连续第二轮干净 4/4**:四台四个互不相同的 AZ 且**每台都拿到它请求的那个**,
**零 re-aiming、零 AZ RING EXHAUSTED、零 InsufficientInstanceCapacity、无降级**;
**四台 `InstanceLifecycle` 全为 `spot`**(run_id 的 `spot_` 前缀是硬写字符串**不是证据**)。

| 种子 | 实例 | AZ | 上机 |
|---|---|---|---|
| 5404 | `i-090caadb7b7f89fd5` | 2a | `21:19:57Z` |
| 5502 | `i-0b09b8b9cfcd5129c` | 2b | `21:19:59Z` |
| 5574 | `i-010c6ec51f7e8cf87` | 2c | `21:20:02Z` |
| 5705 | `i-0acdcb2ded174213a` | 2d | `21:20:05Z` |

**末台上机 `2026-09-05T21:20:05Z` = W50 的闸 (i) 锚点**(GH #469 裁定 ① 取末台),slate spread **8s**。
自毁 = self-terminate + **2h 看门狗** + one-time spot request。

**收尾波次点名**(章程第 6 步,发波轮固定两条,零成本,**必须当轮做** —— terminated 只留约 1h,GH #375):
① `pending,running` 的 soak 机 = **恰好四台**且与 `machines[].run_id` **一一对上**
(`f4f478`/`220154`/`fd9ac7`/`a30046`)⇒ **无第五台(无泄漏)、一台不少(无掉臂)**;
② 按 soak 标签过滤的 `terminated,shutting-down` **为空** ⇒ 本波窗口内**无** `Server.SpotInstanceTermination`。

## 六、局数(章程第 7 步)

(a) **W48/Y1** 上两轮已结清,照登不重算(W48 四粒完整 `ab30/ba12`、`ab36/ba17`、`ab32/ba15`、`ab30/ba12`,
184 局计分;Y1 80/80 局有 signout)。
(b) **W49 发波型报告写预期局数**:预期 **~96 局计分**(4 粒 × 12 局 × 2 腿)外加暖场局
(**暖场局不算有效局**);发波时点 **0 局**,按 W42 实测上传节奏**头 14 分钟 `OBJ_COUNT=0` 是预期值不是症状**。

## 七、⛔⛔ 泄漏检查:本台干净,但账号侧那台**不是走了,是一直没走**

**dota2bot 侧干净**:带过滤器(`tag:Name=dota2bot-soak-spot-*`)⇒ **恰好本波那四台**,
**没有第五台**;常驻仍只有 AMI `ami-0a990a26d89c66547`。

⛔ **而不带过滤器的那一次查询推翻了上一轮的一句话。**
上一轮(18:07Z)写「上轮报的那台外来实例 `fft-cache-v1-attempt3` 本轮**已不在**
⇒ GH #528 降级为『只求一条长期规则』」。**本轮实测**:

```
i-0ce7ddf08ea24cc7f  c7a.16xlarge  running  2026-09-05T05:02:24.000Z  spot  ami-0bea529386a62a2ad
    Name=fft-cache-v1-attempt3   Project=final-table-trainer
```

**同一个 InstanceId、同一个 `LaunchTime 05:02:24Z`、状态 `running`。**
⇒ **它从来没有停过**,到本轮点名时**已连续跑 16.3 小时**。
上一轮那句「已不在」**是一个错读**,而且形状是本章程反复点名的那一个:
**空输出被当成读数** —— 用 `dota2bot-*` 过滤器查出来的空,回答的是
「本台有没有泄漏」,**不是**「那台外来机还在不在」。
⇒ **GH #528 的降级撤回,恢复原级别。**(⚠️ 不追究上一轮:同一个座位、同一份章程,
这正是「过滤器决定了问题」这类缺陷不靠纪律能防住的形状。)

**⛔ 未做任何处置**:该机带别人的项目标签(`Project=final-table-trainer`),
终止既越权又不可逆。**不主张它不该跑。**

**⭐ 账单侧本轮花 `$0.01` 取了硬证据**(章程允许的情形:确认一个近刹车读数 / 需要拆解):
`ce get-cost-and-usage --group-by INSTANCE_TYPE`(09-01→09-06)⇒

```
c6i.4xlarge      16.50675
NoInstanceType    3.090497        (合计 19.597 = MTD)
```

⇒ ⭐ **账单上至今没有 `c7a` 这一行** —— 九月已落账的钱**全部是本台的**,
GH #505「燃烧率是本台的」那部分**仍然成立**。
**但这恰恰是要点**:那 16.3 小时**已经烧掉了、只是还没落账**。
按上一轮实测的 `c7a.16xlarge` spot 价带 `$1.008–1.210/h`
⇒ **已发生但未落账约 `$16.4–19.7`**;
而 AWS 自己的 `forecast` 全天在动:`111.209`(03:xxZ)→ `149.164`(09:13Z)→ **`150.822`(本轮)**,
与该量级一致。**forecast 不是读数、不进围栏**,此处只作为「未落账不等于没花」的旁证。

**算术(交 owner,本台不自裁)**:headroom **`$27.393`** ÷ **`$24–29/天`** ⇒ **约 1 天**见底。
到那时:
- **(甲)** 闸 (iii) 会对**本台**打 `THROTTLED`,而本台九月只花了 `$19.6`、发一波只要 ~$1.02
  —— **读起来像批测超支,实际不是**;
- **(乙)** owner 收到的第一封 `$50` ACTUAL 告警邮件**抬头是 `dota2bot-batch`,而它指着错的项目**。

**根因已量出(GH #515)**:`dota2bot-batch` 的 `CostFilters = null` ⇒ **它是账号级预算,不是项目级**。
**建议(两条都不改本台任何工具)**:给 `dota2bot-batch` 加 cost filter(按 `Project` 标签或机型),
或给 `final-table-trainer` 单开预算。

## 八、铁律 6 / 11

`bots/` / `game/` / `tests/` / `tools/` **本会话一行未改**
(改动 = `iterations/` 下的报告、波次记录、章程本节,加 `iterations/data/seed_roster_index.json` —— 后者是 `--build` 写的纯索引数据)。
静态门与推送门读数见第九节;**动态半(#124)未跑,不声称**。
**铁律 11** 未触发 `requires approval`(`mcp__github__issue_read` 直接返回),**无空转等待**
—— 唯一一次「等」是自检自己在后台跑,本轮**没有为它停下**。

## 九、收尾补记(静态门 / 自检真码 / token)

见本文件末尾追加节。

## 十、交棒

1. ⭐⭐⭐ **owner / 总监 —— GH #528 + #515,本轮从「前瞻」变成「有一台机器连跑 16.3 小时」。**
   上一轮的「已不在」是错读,已撤回。headroom 约 1 天见底,届时**闸 (iii) 会因为别人的算力对本台打 THROTTLED**,
   **而 owner 的第一封 `$50` 邮件抬头会是 `dota2bot-batch`**。修法两条,都不动本台工具。
2. ⭐⭐ **总监 —— `$0.90` 单波成本常数的自动失效条款,第三轮连续点名,仍未重裁。**
3. ⭐⭐ **录像组 —— 61-id 家族条件 (a) 仍欠**;W48 的 promote/reject 归总监(GH #510)。
   ⭐ `stayattr` 已退集,其 W48 读数按总监 §ET 归档为额外 (a) 证据,**不因退集作废**。
4. ⭐ **总监 —— GH #513**(把闸 (ii) 那段手算变成工具)仍 `open`:**本轮串动了所以用不上,
   下一轮串若不动就又要手做**,而没有任何东西保证那时有人做。
5. ⭐ **本台 —— `campgrade` 独占波仍不存在**(#480 / #241),那一棒仍欠着。

## 十一、下一轮本台

1. **收割 W49**(`recover_verdict.py` 从 S3 逐局全量重算,走 `soak/` 不走 `validation/`;
   填 `machines[]` 的 `ab`/`ba`/`arm_depth`/`status_code`,**不许留 null** —— 那正是 W45 把闸 (iv) 弄瞎的手法);
   收割义务全文在 `W49_wave.json:harvest_obligations`,**不要只读本报告摘要**。
2. 闸 (i) 的锚点是 **`2026-09-05T21:20:05Z`**,但**不要读这个数,读 `wave_throttle.py` 打的那一行**。
3. ⚠️ **发波前必查 headroom 是不是被账号上别的项目吃掉了** —— 第七节说这大约在一天内发生,
   **而那时的 `THROTTLED` 不是本台超支**。
4. 开工第一条命令**重定向不管道、放后台给足 ≥45min**(本轮第七次踩同一个坑)。

---

# 收尾补记(第九节正文)

## 9.1 自检真码到手:`RC_EXIT=3`,⭐ **而本轮 Lua 那条腿是新红的**

后台跑完(约 40 分钟),**读脚本自打的那一行不读 harness 报的**:

```
legs run              : 10
FINDINGS (exit 3)     : cadence queue-rulings owed-executions trunk-red(python) trunk-red(lua)
UNCERTIFIABLE (exit 2): none
NOT RUN (inside a leg): tests/test_selfcheck_lua_leg.py
selfcheck worst exit: 3
```

**`3 = 有发现,不是没跑成。** `UNCERTIFIABLE: none` ⇒ 十条腿本轮都真的跑过。

### ⭐⭐ `trunk-red(lua)` 是本轮**新出现**的那条,而且能指名道姓

上一轮(18:07Z)Lua 腿是 **`84 tagged detector file(s), 0 failures`**。本轮:

```
RED  test_corpus_scale.lua
FAIL: test_corpus_scale.lua :: [detector] no test pins the live corpus size with an equality
      tests/test_corpus_scale.lua:208: 1 assertion(s) compare a literal to the current
      fixture count (109). That is the GH #106 / GH #127 defect: landing the next fixture
      turns them red without anything they measure having changed.
        tests/test_staybottle_inflight_regen.lua:223: assert(C('fixtures') == 109, ...)
TRUNK RED -- 1 of 84 Lua detector file(s) failing ON THE WORKING TREE.
```

**责任方可判,而且不是本台**:那一行来自协同组 19:31Z 的 `staybottle` 提交
(`efe70b75`),**晚于**上一轮自检、**早于**本轮 ⇒ 时间窗内只有它。
**修法自带**(检测器正文逐字给了):改用 `tests/corpus_scale.lua` 的
`ratchet()` / `universal()` / `corpus()`,不要拿字面量 `109` 去等值钉语料规模。
⭐ **按第一节那个结构性论证,这条同样是 `main` 的红**(工作树在 `tests/` 上逐字节 == `origin/main`)。

### `trunk-red(python)`:`96 passed, 5 failed, 1 uncertifiable`

`test_bots_walk_farm_only`(#497,`8 checks, 2 failed`)/ `test_detector_source_constants`(#514)/
`test_lua_corpus_stability`(#514)/ `test_mutstand_restore_trap`(#506·#501)/ `test_wkqdmg_domain`。
**与上一轮五条同名,不新增点名** —— ⭐ **但上一轮欠的那个判断本轮结清了:这五条是 `main` 的红**(第一节)。

### 其余各腿

- `cadence`:**`GAP cadence director`**,**不是本台**。
- ⭐ `queue-rulings`:**`RIDESHARE(§BB.4:本轮该裁)1 条 —— `hero-32 status=pending bundle=zusboltdmg`**(`ruled=False`)。
  ⚠️ **本台不裁**,但要指出一处**规则冲突**交总监:§BB.4 说「搭车提议当轮放行」,
  而 **owner 的 P4.2 明写它「废止并压过 §BB.4」,冻结期唯一合法裁定是 `FROZEN-HOLD`**。
  自检那句 `§BB.4: rule this round` 是**工具还在引已被废止的条款**,登记交总监。
- `owed-executions`:**5 行 OWED**(上轮 2 行),其中 **`campgrade_wave_type_premise_reading`(#480/#241)含本台,仍欠着**
  —— `campgrade` 独占波此刻仍不存在。
- 边界照抄:**`REFUSED refs 802 of 827`**(浅克隆,同形非新增);`stable-v1`/`v2` `EXISTS/PINNED/SHIPPED` 全 ok;
  `promote-time constraints` **`FROZEN none`**(`pullcad` 陷阱未复发),三条 atom 全 ok;`expired waits` 空。

## 9.2 铁律 6 读数

- 静态门:**`GATE_EXIT=0  CLEAN`**(`luacheck bots game: 0 warnings`;门自己装的 luacheck ——
  `installed luacheck (it was missing; apt package lua-check, bounded)`)。
- 推送门:`ARM_HOOK_EXIT=0`(`core.hooksPath=.githooks, already set`);
  两次 `git push` 各自跑了同一个 gate 并打 `GATE_EXIT=0 CLEAN`。
  **未用 `RULE6_BYPASS` ⇒ 本轮无「SKIPPED, not passed」行可抄。**
- **动态半(#124)未跑,不声称。**
- 推送:`PUSH_BRANCH_EXIT=0`(`claude/great-sagan-ywt2y1`)、`PUSH_MAIN_EXIT=0`(`066219d6..87a70e83 HEAD -> main`),未被拒、未 rebase。

## 9.3 发表纪律(铁律 6 ⭐ / GH #290)

GH #528 的更正评论**在 push 之后**发表。发表前跑
`claim_precheck.sh`:**`PRECHECK_EXIT=0`**,`local commits not on origin/main: 0`,
`paths cited 1 / resolved on trunk 1 / refused 0`,`OK to publish`。
同轮改了 #528 的标题(降级撤回后原标题「请 owner/总监确认是关是留」已不足以表达紧迫性)。

## 9.4 本轮 token

```
TOKENS total_in=9,838,363 out=63,116 turns=68
```

(到统计时刻为止;统计后的收尾回合不计入。)

## 9.5 本轮开的 issue

**GH #538 `[bug]`** —— `main` 的 Lua 检测器腿从绿变红,`tests/test_staybottle_inflight_regen.lua:223` 的 `== 109` 等值钉住语料规模(GH #106/#127 族)。交协同组,修法自带。

同轮在 **GH #528** 发表更正评论并改了标题(降级撤回),见 §9.3。
