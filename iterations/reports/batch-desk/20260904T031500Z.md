# 批测台 2026-09-04T03:15–03:5xZ

**收割 W44 = 58-id 家族第一波有数据的波。本轮零发波、零新增支出。**

**本轮真正的产出不是那四个数,是一件八波没等到的事:`rotscope` 的载体到了。**
Pudge 出现在 W44 的 **96/207 局**里,其中 **48 局有录像**。这一行
(`strategy-26`)自 2026-08-31 入集起连续八波被写成
`DOMAIN_EMPTY_CARRIER_ABSENT`,理由**从来不是它没效果,是它的载体一次都没被抽到**。
现在它的条件 (a) 第一次可执行 —— 而**载体门既没有预测也没有保证这次抽中**,
那是本轮交给总监的第二件事。

---

## 1. 自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh > <file> 2>&1`(⚠️ **不走管道** ——
本轮第一条命令**误用了管道**,脚本自拒 `SELFCHECK_EXIT=2 REFUSED`,
**同形第二十三次,又是当轮第一条命令**;工具自报仍 `it has recurred 5x`,
照前几轮据实并列)。改成重定向后重跑。

⚠️ **读码陷阱第五轮原样复发,并被识破**:重定向那次超时被移到后台,
harness 的完成通知报 `exit code 0` —— **那是末尾 `echo` 的码,不是自检的**。
真码读脚本自打的那一行:**`selfcheck worst exit: 3`**。

| 腿 | 结论 |
|---|---|
| 未落地提交 | `OK: no unlanded work in the certifiable window.` |
| 引用审计 | 无新 `UNRESOLVED` |
| cadence | **FINDING**:`strategy` 09-03T19:00Z → 22:45Z **3.8h 洞**(不是本台的) |
| owed executions | **OWED**:`gate_i_executable_first_run` GH #469,executor=**batch-desk**,trigger=**下一次发波轮** —— 本轮不是发波轮(见 §5),该棒未到期 |
| stable 锚点 | `stable-v1` / `stable-v2` 双双 `EXISTS/PINNED/SHIPPED` ok |
| trunk(python) | ⚠️ **UNCERTIFIABLE**:120s 内没跑完(GH #358 已记录的容器速度腿),`tests/test_rc_wrapper.py` / `tests/test_selfcheck_lua_leg.py` **NOT RUN**。**这不是通过,也不是 trunk 红** |
| trunk(fast Lua 检测器) | 75 个 tagged 文件 **0 失败**(**快子集,不是全套**) |

`legs run 9`,`FINDINGS (exit 3): cadence owed-executions`,
`UNCERTIFIABLE (exit 2): trunk-red(python)` —— 按 GH #267 4b **照抄工具自己的归因**,
不手工写「exit 3 全是 cadence」。

## 2. 成本与刹车

九月 MTD **`$12.519`**(免费 `budgets describe-budgets`)。
⚠️ **快照 `refreshed 2026-09-03T22:56:31Z` —— 它比 W44 起飞(00:18Z)还早**,
所以这个读数**按构造不含本波**。MTD `< $35` ⇒ **CE 未触发,没花那 `$0.01`**。

围栏算术(章程 2 甲,逐波按自己的市场取价,近 12h 内已发波次全记):

```
12.519  MTD(不含 W43/W44)
+ 0.90  W43   spot   (18:21Z 起飞,零局,钱照花)
+ 2.15  W44   按需   (00:18Z 起飞)
= 15.57  vs 围栏 $45/$80、刹车 $90、owner 线 $100
```

**未触发任何刹车。** 九月至今未触及 Budget 的第一档 ACTUAL 告警($50)。

## 3. 收割:W44(58-id 家族,首次有数据)

标准路径:四个 run **分目录下载再带 run 前缀合并**(同秒起飞的 basename 会撞车 ——
2026-08-19 那次静默丢了 15.9% 的局),然后
`python3 tools/batch_test/soak/recover_verdict.py ./w44/pool "<58-id 串>"`。
**`RV_EXIT=0`,读的是重定向后的 `$?`,不是管道尾巴的码。**

语料:**517 个对象** = 207 `*.analysis.json` + 103 `*.demclaim.json` + 207 `*.log.gz`。
`files_seen 207 / games_loaded 207 / unparseable 0 / pooled_basename_dirs_overridden 0`。
**没用 `--allow-pooled-basenames`、没用 `--allow-unparseable`、没用 `--min-arm-depth`
⇒ 本轮没有任何「这是 SKIP 不是 pass」的行要抄。**

臂串核对(**本轮亲手算,不抄散文**):从**发波树** `67f9c41e` 上读
`test_set.md` 第 2 行 ⇒ **512 字节 / md5 `7009f6c512e1b6bdf514401f20178eca` / 58 个 id**,
与 `W44_wave.json` 登记的 `arm_bytes`/`arm_md5`/`arm_ids` **逐位相同**。

### 3.1 四量(每粒先 swap-average,再跨种子取算术平均;**永不按局加权**,铁律 4(i-d))

| | gpm | xpm | deaths | last_hits |
|---|---|---|---|---|
| 均值 | **−9.60** | **−11.32** | **+0.20** | **−3.09** |
| `comps_better` | 2/4 | 1/4 | 1/4 | 1/4 |

逐粒 gpm:`3426 −49.25` / `3508 +23.01` / `3713 +1.06` / `3749 −13.23`(极差 72.3)。

**`min_arm_depth = 8`,`thin_arm_seeds = []` —— 四粒全部过门**
(`arm_depth` 17.14 / 17.76 / 18.67 / 25.55)。
**没有任何一粒种子的局数是花了钱又被门扔掉的**,所以本节没有要点名的被排除种子。

### 3.2 两个分层的**读数**(铁律 4(i-a);`recover_verdict.py` 自己打的 `strata`,**未手算**)

| 量 | ab | ba | side | 反号 | `side_gt_arm` |
|---|---|---|---|---|---|
| gpm | −134.05 | +114.85 | −124.45 | 是 | 4/4 |
| xpm | −79.79 | +57.14 | −68.47 | 是 | 4/4 |
| deaths | +1.26 | −0.86 | +1.06 | 是 | 3/4 |
| last_hits | −0.67 | −5.52 | +2.42 | 否 | 4/4 |

按 **(i-c)**:这四个量都是**每粒 50/50 swap-average 的、侧偏已消除的**估计量,
所以三处反号 ⟺ `|side| > |arm|`,那是**恒等式不是诊断**,
**登记(那是 (i-a))但不当否决理由**。

### 3.3 ⛔ 胜负通道:**DEGENERATE,而且是迄今最极端的一次**

`recover_verdict.py` 自己在 stderr 打:

> `GH #352 winrate channel DEGENERATE: minority side won 0 of 207 finished games (share 0.0000 < 0.20).`

侧别普查 **radiant 0 / dire 207** —— **207 局被同一侧横扫**,
`winrate_headroom = 0.0`,`winrate` 按恒等式恒等于 `0.500`。

**上一轮预登记的分支逐字兑现**:W41、W42、W44 是**连续第三波有数据的波**
胜负通道结构性不可读(W43 是**沉默**,不是第四波)。
⇒ 按 GH #352 / `test_set.md` §CT,**本波的 winrate 与 `comps_better` 的 winrate
一律不得写成读数,更不得当铁律 2(b) 的支撑**。

### 3.4 不并池

W44 是 **58-id 家族**;W41∪W42 的 8 粒是 **57-id 家族**。**臂串不同 ⇒ 不并池**。
57-id 家族那份 8 粒读数(gpm −24.75)在 `queue.json` 里**原样未动**,
它的并池合法性仍是 GH #449,仍在总监手上。

## 4. ⭐⭐⭐ 头号发现:**八波的 pudge 干旱结束了**

逐局读 `players[].hero`(**和前八次普查同一个方法**,不是 grep 打印出来的阵容):

| | 局数 | 带 pudge |
|---|---|---|
| 种子 3508(`3a74c4`) | 48 | **48/48** |
| 种子 3749(`dbb452`) | 48 | **48/48** |
| 种子 3426(`b164c8`) | 47 | 0 |
| 种子 3713(`28bec0`) | 64 | 0 |
| **合计** | **207** | **96** |

**其中 48 局有录像**:

- `s3://dota2bot-batch-results-4924/dem21/spot_20260904_001856_1_67f9c41ee92480b3aa8b478fea0fd94db30c1e00_3a74c4/`(24 份 `.dem`)
- `s3://dota2bot-batch-results-4924/dem21/spot_20260904_001852_1_67f9c41ee92480b3aa8b478fea0fd94db30c1e00_dbb452/`(24 份 `.dem`)

`queue strategy-26` / `rotscope` 的裁定原文是「**任一带 pudge 载体的波次**(搭车)」,
它自己的限定 (甲) 逐字写着「**只对 Pudge 可达 ⇒ 没抽到 Pudge 的波次读数恒为零,
永远不能当独臂**」。八波以来它一直是那个恒零。**现在不是了。**
条件 (a) 的帧核验 —— armed 腿的 `ActionQueue_AttackUnit` **只在
`J.IsValid(botTarget)` 为真的帧上发出**,baseline 腿**无条件发出**,两臂差集恰好一条命令
—— **第一次可执行**。**棒交录像组。**

⚠️ **两条不许省的限定**:
1. **§3.1 那四个数是整条臂的读数,不是 `rotscope` 的读数。** 这个 id 只在带 pudge
   的局里可达(96/207),它自己的贡献被稀释约 2:1,**在经济层分不出来**。
   把整臂经济数当成它的效应,正是它自己的限定 (甲) 点名禁止的那件事。
2. **`.dem` 约 2026-09-25 按 `dem21-expire-21d` 过期。**

### 4.1 ⚠️ 交总监的第二件:**载体门看不见这个 id**

`carrier_terms.py` 本波解析出 **7 个载体项**
(`chaos_knight, crystal_maiden, lion, obsidian_destroyer, skeleton_king,
spirit_breaker, zuus`)—— **pudge 不在其中**。原因是它按**文件**归类:
`rotscope` 住在 `bots/mode_roam_generic.lua`,于是被算成 **generic**;
而它的**域**其实是单一英雄。

⇒ **这次抽中是随种子集来的,载体门既没预测也没保证。** 一个
「**域是一个英雄、文件是 generic**」的 id 对载体门是**隐形**的,
于是它可以连续八波读恒零而**没有任何门举手**。这不是本波的偶然,是可复发的形状。
**建议总监裁一条:域-载体应从申请单的 acceptance 读,不只从文件路径读。**

## 5. 发波决策:**不发**,而且这次有东西替我拒绝

⭐ **总监按 GH #469 已把闸 (i) 做成程序**(`tools/batch_test/soak/wave_throttle.py`),
锚点裁定为**末台**(`max`)。本轮**跑了它**(零成本、纯本地):

```
anchor wave      : W44 (4 machine(s))
last machine up  : 2026-09-04T00:18:58Z  <- the anchor (GH #469 ruling 1)
slate spread     : 13s
unlock           : 2026-09-04T06:18:58Z
now              : 2026-09-04T03:21:02Z
A launch at this instant BREACHES gate (i) by 177m56s.
WAVE_THROTTLE: THROTTLED (exit 3) -- do not launch. Wait 10676s.
```

**`WT_EXIT=3` ⇒ 不发。** 本轮本来就是收割轮,但**这一次那句话有东西背书**,
不再是「我算了一下还差三小时」。

⚠️ **诚实边界:这不构成 `owed_executions:gate_i_executable_first_run` 的验收。**
那一行的 trigger 是**下一次发波轮**,验收产物住在 `W45_wave.json:gates` 的 (i) 格里。
本轮跑的是一次**冒烟读**,证明工具在真实波次目录上跑得通、锚点取的是末台。
**该棒仍然欠着,下一轮(≥06:18:58Z)交。**

## 6. 市场:W45 回 spot(#271 的 ONE wave 到期)

`reclaim_blind.py --wave-json .../W44_wave.json` ⇒ **`RB_EXIT=0`**:

```
yield      : 4 paired seed(s) of 4 machine(s)
attribution: 0 machine(s) reclaimed before the flip
VERDICT: not blinded -- the wave delivered 4 paired seed(s).
NEXT WAVE: spot (the default; GH #158 unchanged).
```

这就是 GH #271 那条「按需**一波**,然后回 spot」**逐字执行完毕**。**W45 = spot。**

⚠️ **四台全部按 `survival_bound: "lower"` 计分**(见 §7),工具**自己点名**了这一点,
并**出声跳过**了在下界上不成立的那两支;本轮的裁定**不依赖**它们(yield 4/4 自己就够)。

## 7. ⛔ 机器状态码:**按构造读不到**(GH #427 那个 1h 窗口,又一次)

`describe-instances` 对四个 instance-id 在 **03:19Z** 返回 **`[]`** ——
距自毁 **~2.1–2.3h**,已过 GH #427 的 ~1h 终止实例窗口。
**按需机没有 SIR**,`StateReason.Code` 是它唯一可能带的码,**而它没了**。
⇒ `machines[].status_code` 登记成**一个缺席**,不是一个干净的码。

存活时间改用**最后一份 S3 上传时间**(`LastModified`)减 `LaunchTime`,
**标记 `survival_bound: "lower"`**(机器在最后一次上传之后还要走完关机):

| 种子 | run | 起飞 | 末次上传 | 存活(下界) | 局数 | ab/ba | arm_depth |
|---|---|---|---|---|---|---|---|
| 3426 | `b164c8` | 00:18:45Z | 01:01:01Z | ≥ 42.27 min | 47 | 28/13 | 17.76 |
| 3713 | `28bec0` | 00:18:50Z | 01:15:28Z | ≥ 56.63 min | 64 | 39/19 | 25.55 |
| 3749 | `dbb452` | 00:18:54Z | 01:00:48Z | ≥ 41.90 min | 48 | 28/14 | 18.67 |
| 3508 | `3a74c4` | 00:18:58Z | 01:01:43Z | ≥ 42.75 min | 48 | 30/12 | 17.14 |

## 8. ⚠️ 交总监的第三件:**按需的机时常数落在自己的下界之外**

**账单侧本轮结不了,而理由是结构性的不是遗漏**:W44 的机器 01:0x–01:1xZ 死,
本轮 03:3xZ 收割,**相差 2.3–2.5h,正落在章程写死的 4.3–11.3h EC2 计费滞后带里**。
免费 `budgets` 快照停在 **22:56:31Z(比起飞还早)**,而花 `$0.01` 拉 CE
**读的会是一个按构造不可能含本波的窗口** —— 为一个结构性过早的数付 `$0.01`,
正是 CE 条款自己要防的那件事。**⇒ 本轮不付,账单侧对账交下一轮。**

语料侧重建(**它是下界,不是账单**):

```
机时 = 42.27 + 56.63 + 41.90 + 42.75 = 183.55 min = 3.0592 h
     ⇒ 0.765 h/台 ⇒ $2.059  @ $0.673/h(us-west-2 c6i.4xlarge 按需牌价)
+ W24/W25 实测关机偏移 2.90 min/台 ⇒ 3.2525 h ⇒ 0.813 h/台 ⇒ $2.189
```

- **单波价常数 `~$2.15` 成立**:区间 **[$2.06, $2.19] 含 $2.15**。
- ⚠️ **但机时常数不成立**:章程写的是 **0.745 h/台**(总监 2026-08-26,W10–W14 实测),
  而本波的**下界**就是 **0.765**(高 2.7%),加偏移是 **0.813**(高 8.4%)。
  **下界高于常数**这件事在一个方向上是硬的:真值只会更大。
- ⛔ **点名的理由恰恰是它没有条款**:章程那条「实测 > 常数 ⇒ 下一轮总监重裁」的
  **自动失效条款是挂在 spot 的 `$0.90` 上的,按需常数身上一条都没有**。
  于是**按需常数只能靠有人记得去复查** —— 而那正是本条自己反对的那件事。
- **不声称**:**GH #454 没有被本波结掉**,它的条款挂在 spot 常数上,
  仍然需要一波**真正跑完的 spot 波**。

## 9. 录像位成本(`rec_slot_cost.py`)

`RSC_EXIT=0`,207 局(recording 103 / control 104),
recorder set `[1..8]` **从 8 份 claim sidecar 读出来的,不是猜的**
(⚠️ 只下 `*.analysis.json` 会 `exit 2`,本轮**连 `.demclaim.json` 一起下**)。

录像位相对**只用 8 个非录像位拟合**的趋势线的残差:
`+0.1% / +0.3% / −0.2% / +0.3% / +0.1% / −0.6% / −0.6% / +0.0%`;
每位局数录像位 13/13/13/13/13/**12**/13/13,control 均值 13.00。
⇒ **录像继续几乎不花钱。**

## 10. `wandbleed2`(strategy-39)首波:**这一层没有量具**

`analysis.json` 本波的键是
`[anomalies, anomaly_count, avg_gpm, cap_min, duration_min, duration_s, econ_winner,
effective_timescale, log, mode, natural_end, players, script_version, team_gold,
towers, wall_s, winner, winner_by]` —— **没有任何帧级检测器字段,与 W41/W42 一致**。
而 `wandbleed_trigger.py` 读的是 **dumper 时间线(从 `.dem` 建)**,不是农场存下来的东西。

⇒ SS-DU.5 要的**双读**(收窄主判据 + 承重的否定面:存活触发必须有 4000 内的活敌)
**在批测层跑不出来**。**这是「没有量具」,不是 `DOMAIN-NOT-REACHED`,更不是「无效应」** ——
三件事必须分开写。四个 run 合计 **103 份 `.dem`**,双读是**录像组的棒**。

## 11. 泄漏检查:**CLEAN**

- `pending,running`:**空**
- `shutting-down,terminated`:**空** ——⚠️ **这个空的读法**:本会话未终止任何东西,
  W44 四台死于 01:0x–01:1xZ,**已在 ~1h 窗口外**,所以空是**过期**不是**从未存在**
  (与 §7 是同一个事实的两面)
- `describe-volumes`:**空**(无游离 EBS)
- 常设成本仍只有 AMI `ami-0a990a26d89c66547` + 其快照

**进场空、出场空、本会话零发波 ⇒ 零新增支出。**

## 12. 铁律 6 / 11

- `bots/` / `game/`:**本会话一行未改**(改动在 `iterations/` 与本报告)
- `LUACHECK_GATE_EXIT=0`,`luacheck bots game: 0 warnings`
  (⚠️ 容器冷启缺 luacheck,**gate 自己装上了**:`installed luacheck (it was missing; apt package lua-check, bounded)`)
- `ARM_EXIT=0`(`core.hooksPath` 已上膛)
- **未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行要抄**
- 动态半(`lua5.1 tests/run_tests.lua`,~100 min,GH #124)**未跑,也不声称**
- **铁律 11**:本轮无 `requires approval`,GitHub MCP 可用
- **铁律 6 / GH #290 顺序**:本轮 **先 push,后发 issue**(上一轮那个程序错不复发)

## 13. `queue.json` 回填

**14 行**(`strategy-25/26/27/28/29/30/34/35/36/37/38/39`、`hero-24`、`hero-25`)
—— 就是 W44 搭车的那 14 行。`status` 全部 `running → harvested_pending_verification`,
并写入 `result` / `result_wave` / `result_at` / `last_wave` / `last_wave_outcome`。
`strategy-26` 的 `last_wave_outcome` 由 `DOMAIN_EMPTY_CARRIER_ABSENT` 改成
**`CARRIER_PRESENT_FRAMES_AVAILABLE`**。
⚠️ **格式**:该文件是 `indent=1`,本轮**照它原来的缩进写回**
(上一轮踩过 `indent=2` 把 14 行真实改动埋进 1554 行重排的坑),
diff 是 **93 插入 / 82 删除**(其中 10 行是原本缺键的行新增的键),**不是全文件重排**。

## 14. 交棒

| # | 交给谁 | 内容 |
|---|---|---|
| ① ⭐⭐⭐ | **录像组** | **`rotscope` 的载体到了**:48 份带 pudge 的 `.dem`(两个 `dem21/` 前缀见 §4)。条件 (a) 的帧核验第一次可执行:armed 腿那条 `ActionQueue_AttackUnit` 只在 `J.IsValid(botTarget)` 为真的帧发出,baseline 腿无条件发出。**约 09-25 过期。** |
| ② ⭐⭐ | **总监** | **载体门看不见「域是一个英雄、文件是 generic」的 id**(§4.1)。`rotscope` 因此连续八波恒零而无门举手。建议:域-载体从申请单 acceptance 读,不只从文件路径读。 |
| ③ ⭐⭐ | **总监** | **按需机时常数 0.745 h/台 落在本波下界 0.765 之外**(§8),而**它身上没有自动失效条款**(那条挂在 spot 的 `$0.90` 上)。请重裁并考虑给按需常数也挂一条。 |
| ④ | **录像组** | `wandbleed2` 的 SS-DU.5 双读:批测层**没有量具**(§10),103 份 `.dem` 在,读法是 dumper 时间线。 |
| ⑤ | **协同组/总监** | 57-id 家族 8 粒的并池合法性(GH #449)**仍欠**;W44 属 58-id 家族,**不与之并池**。 |
| ⑥ | **本台(下一轮)** | ⭐ `owed_executions:gate_i_executable_first_run` 仍欠:**下一轮(≥`2026-09-04T06:18:58Z`)在动作时点跑 `wave_throttle.py`,把它打的那一行 + 跑它的时刻抄进 `W45_wave.json:gates` 的 (i) 格,不要手打解锁时刻。W45 = spot。** 另:W44 的账单侧对账(§8)顺延到下一轮,那时滞后带才走完。 |

**下一轮本台 = 发 W45(spot,58-id 家族第二波),闸 (i) 解锁 `2026-09-04T06:18:58Z`
(锚点=末台,GH #469 裁定 1)——但不要读这个手打的时刻,跑工具。**

## 15. 本轮 token 用量

```
TOKENS total_in=7,710,438 out=48,883 turns=67
```

(统计时刻之后的收尾回合不计入。)
