# 批测台 2026-08-25T06:17Z —— 发波 W9(全集 29 id,`tpreach` 首次上机)

**一句话**:上一轮唯一挡着发波的是 6h 节流(当时距 W8 只有 2h56min);本轮它到期了,
三条闸全过,W9 于 **06:16:33/39/44/49Z** 发出 —— 而**本轮唯一的新事实是 GH #167 的门槛
被跨过去了:越界的不再只是 verdict 全名,是 cand 串本身(259 > 255)**,于是
「换个短后缀」这条看起来像修法的路结构上关闭了。

---

## 1. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` —— **worst exit 3**,逐条判读:

| 项 | 读数 | 判读 |
|---|---|---|
| UNLANDED | 4 commit(director 22:3xZ 的 `08ed7c2`/`fc79986`,16:xxZ 的 `4af32ae`/`ec46147`) | **不重复开 issue,也不抢救** —— 工具点 SHA 不点内容,而**内容已在树上**:`grep tpreach bots/` 命中 `jmz_func.lua:5857/5878/5894`,即 22:3xZ 那两个 commit 的产出已经以别的 SHA 落地。这正是工具 LIMIT 4 说的「改头换面落地的同一份工作」。**但这一条本轮是有钱后果的**(见 §3):批测台发波前必须确认 armed 的 id 在 `origin/main` 上真的存在,所以这个判读不是纸面的,是接线门替我做的 |
| CADENCE | 11 finding | 全是 08-24 白天五条流停摆的残影 + 发波/收割轮之间的必然空轮,现均已恢复 |
| trunk python | **22 passed / 0 failed** | 干净 |
| trunk Lua | **SKIP(容器无 lua5.1)** | 见 §6 的诚实边界 |

---

## 2. 收割:本轮无可收

W8(00:16:28Z 发出)已于**上一轮 03:12Z 完整收割**(277 计分 + 24 暖场 = 301,unfinished 0)。
本轮开工时 `describe-instances` 四层全空 ⇒ **无在跑波次、无待收 verdict**。
`queue.json` 因此**逐字未动**:无收割 ⇒ 无 status 可更新。

---

## 3. 发波 W9 —— 三条闸逐条

| 闸 | 判据 | 本轮读数 | 结论 |
|---|---|---|---|
| (i) 6h 节流 | 距上一波发波 ≥ 6h | W8 = **00:16:28Z**,门槛 **06:16:28Z**;实际发波 **06:16:33Z** | ✅ **过闸 5 秒**,见下方「为什么等那 83 秒」 |
| (ii) 当前树累计种子 | < 8 | 树 `57f3b68` 上全集累计种子 **0** | ✅ |
| (iii) $45 围栏 | MTD + 滞后 + 本波 ≤ $45 | **$37.828** + $1.48(W8 滞后) + $1.48(W9) = **$40.79** | ✅,未越 owner 的 $50 档 |

**为什么等那 83 秒**:开工时是 06:14:41Z,离门槛还有 107 秒。W4 那次(章程 3283-3286 行)
早发 18 秒被判违规、花 ~$0.05 kill+relaunch 立了先例 —— **门槛的算术目的不在乎 83 秒,
但「谁来判断多少秒不算数」这件事在乎**。所以本轮把发波脚本挂在一个 `until date -u ≥ 门槛`
的闸后面,由脚本自己跨过去,发波时刻由脚本盖戳:**06:16:33Z,门槛 +5s**。

### 3.1 发波参数

- **armed = 头部成员串 29 逐字**(W8 的 28 + `tpreach`,§BC 总监自写自批那条)
- **接线门 `check_armed_wiring.py` = 29/29 WIRED,exit 0** @ `57f3b68`
  —— **本轮它不是走过场**:自检报 `tpreach` 所在的两个 director commit UNLANDED,
  接线门是唯一能回答「那 armed 它到底是不是 ABSENT」的东西,答案是 **direct,1 site,
  `jmz_func.lua:5878`** ⇒ 内容在树上,可以 arm
- **载体门 `seed_draft.py --assert-carrier` exit 0**,与 W8 逐字复现:
  `crystal_maiden` **FULL 4/4**、`zuus` **FULL 4/4**、`lion` **PARTIAL 仅 s896**、
  `obsidian_destroyer` **PARTIAL 仅 s896**、`skeleton_king` **PARTIAL s895/s906**
  (**PARTIAL 的惰性种子 = 波内零腿,该新规仍未裁,第五轮催**)
  ⚠️ **踩坑留档**:`--assert-carrier crystal_maiden:4` 里的 `:4` 是**位置**不是**局数**,
  写成 `:4` 会把 FULL 读成 PARTIAL/ABSENT。W8 报告里的 `cm/zuus FULL 4/4` 用的是**裸英雄名**形式
- **钉树 `57f3b68219f5eb211f3a048263f7bfaec83f5ca5`** —— 发波时刻 `git ls-remote origin main` 真 tip
  (06:14:41Z 与开工时 06:12Z 两次读数一致)
- 4 台 × 1 种子(**888/895/896/906**,与 W7/W8 同种子)、`--on-demand`(#158 未裁前的保守默认)、
  `--slots 16`、**`--rec-slots 12`**(只动 id 集合、不动采集配置 —— 章程那条边界)、
  `--hours 2` + terminate、`--games 22`
- run_id 尾 token `b8025e`/`4a1d7a`/`365bc2`/`ea4dd6`,实例
  `i-0b62ebbf03d2d1048`/`i-06e7485163145962b`/`i-0df5216cee0bf91f6`/`i-0624d61156ed445e1`
- **唯一性双保险(GH #98)**:四次调用发波时刻 **06:16:33/39/44/49Z**,间隔 **6/5/5 秒**
  (远高于那条 2 秒实测下界);`soak-run` 标签 **4 值两两不同**;
  `describe-instances` 只回 **4 台**、`InstanceLifecycle` 全 **None**(= 按需,与 `--on-demand` 一致)

### 3.2 为什么是全集波而不是隔离波

章程「波次开关策略」:**默认波次 = 全测试集 armed**。上一轮交给总监的岔路
(「接受全集读数当 owner P1 的 (b)」vs「发一次 $1.48 的 `pullcamp`+`pulllane` 隔离波」)
**本轮未见裁定** ⇒ **本台不自行发隔离波**,按默认走全集。
`queue.json` 里唯一 `APPROVED_CONDITIONAL` 的 `strategy-5b`/W6 **仍卡在录像组的解锁棒上**
(事件轴量级,零 AWS),**本波编号取 W9,W6 继续留给它**。
`strategy-7`/`strategy-8` 仍是 `DEFERRED_PENDING_W3_RESOLVE`,**本台不代宣**,且不在成员串 ⇒ 带不上。
新落地的两个 gated id `zusstatic`(GH #173)/`tpdeathbuy`(GH #168)**不在成员串** ⇒ 不 armed,
等总监裁入集。

### 3.3 预登记(可证伪,收割时照做,不许事后改判据)

1. **W8 是 W9 的 28-id 对照**:两波种子相同、采集配置相同(`--rec-slots 12`),
   armed 只差 `tpreach` 一个 id。
   **诚实边界**:这是**跨波**比较,08-23 已实测该类量**波间自噪声与效应同量级**
   ⇒ **只用于帧证据侧的定性对照,不许用来给 gpm/winrate 下结论**(经济量仍只看波内 ab/ba 两层)。
2. **计量三条(GH #148)照办**:ab / ba 两层分别报 gpm 与 winrate;两层反号先按侧偏分解
   ((ab−ba)/2 = 侧偏,(ab+ba)/2 = 效应),再看有没有不受侧偏污染的读法。
3. **`--rec-slots 12` 第八次验收**:`rec_slot_cost.py <4 run 目录> --baseline rec_slot_baseline.json`,
   exit 0 = 通过。**判据方向按 15:xxZ 总监裁定**:单边,只有**赤字**超 5% 才报警,正值不报警。
4. **`tpreach` 的 (a) 不归本台**:§BC.4 写死由**录像组独立核验**(非撤退 mode 下、
   敌人在 `(700, reach]` 带内的按下点,armed 腿不按 / baseline 腿按)。
   **本台的 (b) 是「胜负无明显负面」,不单列 `tpreach`。**
5. **收割走 `recover_verdict.py` 全量重算**,自产 verdict 只当免费早期预览
   —— 上一轮把这条坐实到第五次(自产的 **ba 腿四个 run 全短 2–3 局**,s906 gpm 差 6.8,
   与本波要测的效应同量级)。
6. **下载判据是 `ls` 出的文件数,不是命令 exit code**(见 §4)。
7. 预期 **~275 ± 25 有效局**,**预计自毁 ≈ 06:50Z**(账单侧 0.550 h/台),不是 08:16Z 看门狗。

---

## 4. ⭐ 本轮唯一的新事实:GH #167 的门槛跨过去了

```
cand 串本身            = 259 字节        <-- 上一轮 251,本轮 +8(tpreach)
verdict basename 预期  ≈ 292 字节        (<cand>_YYYYmmdd_HHMMSS_-NNN.verdict.json)
```

对读:W8 实测 **285**;issue 标题写的 266 是 26-id 时代的数。

**新的是 `259 > 255`** —— **裸 cand 串已经不能当本地文件名**。
在此之前所有失败都归因于「cand + 时间戳后缀」这个**组合**,于是「去掉后缀 / 换个短后缀」
看起来像一条修法;**本轮起这条路结构上关闭了**:任何 `<cand>.<ext>` 形状的本地落盘,
不管后缀多短,在 ext4 的 255 上限下都必然 `[Errno 36]`。

**不变的两点**:① **S3 侧完好**(key 上限 1024 字节),实例侧写 verdict 从未失败过,
缺陷纯在本地落盘;② **绕法零改动仍成立**:`s3api get-object --key '<长 key>' <短本地路径>`。
⇒ **#167 维持降级,不阻塞收割。**
坑仍在:`--recursive` 的失败**混在进度输出里、不改退出码** ⇒ **判据必须是下载后 `ls` 出的文件数**。
真修法方向(key 换成 `run_id + cand 短哈希`,cand 全文移进 verdict JSON)已追评交总监 —— 收割侧
要跟着改,**不是批测台单方面能落的**。

---

## 5. 成本与泄漏

**成本**:MTD **$37.828**(`budgets describe-budgets`,refreshed **2026-08-25T04:37:21Z**;
脚本按 $35 门槛自动 CE 复核 **$37.8282782356** 逐位吻合),forecast **74.693** / limit **100.0**,
三线($50/$80/$100)全未触及。**本轮 CE 支出 $0.01**,除 W9 的 EC2 外零支出。

**滞后账(纪律(甲)):** 上一轮快照 $36.245 @ 08-24T21:12Z → 本轮 $37.828 @ 08-25T04:37Z,
**+$1.583 ≈ 一波**。W7(08-24T18:12Z)与 W8(08-25T00:16Z)两波在窗口内,只到货了约一波
⇒ **保守按「W8 尚未到货」记账**:有效 MTD = $37.828 + $1.48 = **$39.31**,加 W9 = **$40.79**。

> **⚠️ 给下一轮**:$45 围栏余 **$4.21 ≈ 不足 1 波按需**。
> **下一波按需发不出去了** —— 要么等 W7/W8/W9 滚出窗口、要么总监裁 GH #158 转 spot
> (~$0.54/波,同样的钱能发 2.7 倍的波)。**这是 #158 从「排期被挤」升级成「真的挡住了」的一轮。**
> 并且重复上一轮那条提醒:**W7 下一轮就滚出 12h 窗口,余量会「凭空」多 $1.48 —— 那是窗口滚动
> 不是省下的钱,不要据此放宽。**

**泄漏**:开工 / 收尾各一次,四层区域 + 卷 + EIP + spot 请求全查:
开工 **实例 0**;收尾 **us-west-2 恰好 4 台(= 本波,`InstanceLifecycle` 全 None)、
其余三区 0**、**可用卷 0**、**EIP 0**、**spot 请求 0**、**快照 1**(`snap-0ad026b386c804288`,
160 GB,AMI 常设)⇒ **无泄漏,无第五台**。

**固定栏位**:`soak/` **192**、`dem21/` **44**、`unattributed/` **0**、
`validation/` 最新 **2026-08-25T00:47:49Z**(W8)、远端 main tip **`57f3b68`**、
`stable-v1`/`stable-v2` **tag 在远端仍不存在**(`git ls-remote --tags` 零命中,**第三轮提醒总监**)。

---

## 6. 验证与诚实边界

- `bots/` / `game/` **逐字未动**(本轮改动只在 `iterations/` 下)⇒ 铁律 6 无适用对象。
- 容器**无 `luacheck` / 无 `lua5.1`** ⇒ **不声称跑绿过 Lua 全量**;自检的 trunk Lua 那一格
  是 **SKIP 不是 PASS**(这正是 GH #171 说的那种失明,本轮如实记下)。
- `queue.json` **逐字未动**(无收割 ⇒ 无 status 变更),`git diff --stat` 只含本报告与章程状态节。
- **接线门与载体门都是发波前跑的、不是事后补的**,读数原样贴在 §3.1。

---

## 7. 交棒(铁律 9 —— 每一条都点名下一棒)

1. **⭐ 总监 —— GH #158(按需 vs spot)本轮从「排期被挤」变成「真的挡住了」。**
   $45 围栏余 **$4.21 < $1.48**,**下一波按需发不出去**。裁 (B)/(C) 无需任何 harness 改动。
   这是**第三轮**催,且第一次带着「下一轮就发不了波」的硬后果。
2. **⭐ 总监 —— owner P1 的岔路仍未裁**:「接受全集读数当 (b)」vs「发 `pullcamp`+`pulllane` 隔离波」。
   ⚠️ **与第 1 条互相挤**:围栏已不够发那个隔离波,**先裁 #158 才有第二条路**。
3. **⭐ 录像组 —— W6 解锁棒(第六轮催)**:`strategy-5b` 的事件轴量级(每局塔防 TP 响应次数 /
   其中核心占几次),**零 AWS**,`capmono_refusal.py` 已有现成的 `modifier_teleporting` 通道,
   **是唯一还挡着 W6 的东西**。
4. **⭐ 录像组 —— `tpreach` 的 (a) 点名交给你们**(§BC.3 第 1 条写死「不是总监自己看」)。
   W9 是它第一次上机,语料 ~06:50Z 起落在 `dem21/spot_20260825_0616*/`。
   判据 §BC.4:非撤退 mode、敌人在 `(700, reach]` 带内的按下点,armed 腿**不按** / baseline 腿**按**;
   `tp_channel_death.py` 已算了 `near`,**只差按 mode 分层**。
5. **录像组 —— W8 语料仍未消化完**:`strategy-12`(`towerfear`)的五条正/反判据还没人量,
   **反向判据「死亡数不许上升」不许省**(波级 deaths 是 10 人全局均值,替代不了它)。
6. **总监 —— 载体门 PARTIAL 的惰性种子 = 波内零腿,该新规仍未裁(第五轮催)**。
   本波 `lion`/`obsidian_destroyer` 只在 s896、`skeleton_king` 只在 s895/s906 ⇒ 另外的种子上
   这些 id 结构上零腿,而收割时它们照样进池化读数。
7. **总监 —— `strategy-7`/`strategy-8` 正式 resolve 并排期**(本台不代宣)
   + **`stable-v1`/`stable-v2` 的 tag 在远端仍不存在**(第三轮)。
8. **`[harness]` 棒**:**#167 门槛跨越已追评**(259 > 255,「换短后缀」这条路关闭,
   附真修法方向);#152 / #147 本轮无新增语料,维持上一轮判读。

---

## 8. 本次会话 token 用量(铁律 8)

```
TOKENS total_in=5,934,797 out=24,208 turns=53
```
