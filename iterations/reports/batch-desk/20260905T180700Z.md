# 批测台 2026-09-05T18:07Z(收割 Y1 标尺波 —— 胜率买到了,gpm 没买到)

工作单元:**收割 Y1**(P4.1 upstream 标尺波)+ 两条结构性发现开 issue +
连掉五轮的零成本活 `rec_slot_baseline` 并入。**本轮不发波**(闸 (i) 未解锁),
**本轮零 AWS 支出**(只有 S3 读:~10MB egress + 少量 GET,不到 1 美分)。

---

## 一、开工自检(铁律 10)

第一次调用**被工具自己拒绝**:`routine_selfcheck.sh` 检出 stdout 是管道,
打 `REFUSED: stdout is a pipe; exit 2, nothing checked`,并指出这是
evidence-discipline 3、**已复发 5 次、每次都是当轮第一条命令**。
本台第一条命令正是 `... | tail -60` ⇒ **第六次**。改成重定向后重跑。

- 第二次(重定向,`timeout 400`)跑到 `=== trunk health (python test suite) ===`
  **被超时截断**,**不算通过**。读到的部分:`cadence` **1 finding**
  (director 09-04T19:10Z→23:06Z 3.9h 洞,**不是本台**)、`un-ruled queue` 69 开、
  `stable-v1/v2` 锚点 **OK**、promote-atom 约束 **OK**、
  `expired waits` **无**、`SKIPPED-IN-STREAM` 7 条(历史,非 finding)。
- 第三次用 `tools/agent/rc.sh` 后台重跑(取真码),**收尾时仍未结束** ⇒
  **本轮 `SELFCHECK_EXIT` 未取到,按铁律 10 记 `UNCERTIFIABLE`,不是通过**;
  trunk 的 python 测试那一侧**这轮没人看过**,已交下一轮。

## 二、AWS 与成本(闸 (iii) 的操作数,本轮不用于发波)

- `session_setup.sh` → `AWS ready: ...user/dota2bot-agent`,`AWS_SETUP_EXIT=0`。
- `check_costs.sh`:`COSTS_EXIT=0`,九月 MTD **`$19.597`**(免费 `budgets`,
  `refreshed 2026-09-05T11:21:28Z`)——与 12:15Z 那轮**逐位相同**,
  即 **W48(12:24Z)与 Y1(15:16Z)两波都还没落账**,这正是章程写死的 4.3–11.3h 滞后。
- 进场 `running/pending` **空**;常驻仍只有 AMI `ami-0a990a26d89c66547`。
- ⚠️ 上一轮报的那台**不属于本项目**的 `fft-cache-v1-attempt3` 本轮**不在**
  `running/pending` 里了(已自行结束),GH #528 的处置请求随之降级为「给一条长期规则」。

## 三、收割 Y1(本轮的主活)

**读法**:`recover_verdict.py` 按构造用不了(这条路没有 soak stamp),
且 **80 个 `game_N.json` 全是 0 字节**(con_logfile 的 harness bug;主会话
17:36Z 已修 `run_batch.sh` 回退 `.log.stdout`,commit `25d237b2`,
⚠️ **那个修复至今没在农场上跑过**)。
于是从 80 个 `.log.stdout` **各取末 128KB**(Match signout 块距 EOF 约 11KB)
用 `parse_log.py` 的同一组正则解析 —— 全波 egress ~10MB,而不是全量 8.4GB。

### 3.1 读数(铁律 4(i-a):两个分层的**读数**分别登记)

| 分层 | 局数 | main(NEW)胜 | 读数 |
|---|---|---|---|
| fwd(Radiant=NEW) | 40 | 25 | **0.625** |
| rev(Radiant=OLD) | 40 | 19 | **0.475** |
| **arm(swap-average)** | 80 | 44 | **0.550** |
| side 项 | | | **+0.075** |

**80/80 局都有 signout,0 局未完成。** 两层等大(40/40)⇒ 分层平均与朴素并池
**恒等地**都是 0.550 —— 登记这句是为了让「巧合」在案,而不是被默认。

**主会话 17:42Z 写进 DECISIONS_NEEDED 的 44/80 = 55.0% 本轮独立复现,一致。**
本轮**新增**的是并池数掩盖掉的三件事:

1. **side 项(+7.5pp)比效应本身(+5.0pp)还大**。绕 0.5 看,两层反号
   (fwd +12.5pp / rev −2.5pp)。这个估计量**已消侧偏**(40/40 swap 后算术平均)
   ⇒ 归**铁律 4(i-c)** 不归 4(i-b):反号是恒等式 `|side| > |arm|`,
   **对 arm 测得多准零信息**,按 4(i-a) **登记但不当否决理由**。
2. **这条路的 `winrate` 通道不退化**(与镜像路七连 DEGENERATE 相反):
   两臂是**不同的树**,横扫不会把它按恒等式逼成 0.500。
   物理侧普查:Radiant 46/80(57.5%)、Dire 34/80,少数侧占比 0.425。
3. **⭐⭐ P4.1 要的三样,Y1 只买到两样。**

### 3.2 ⭐⭐ gpm 那一格**没有量具**(开 GH #535)

800 个 player 行(80 局 × 10 人)逐项统计:
`Gold per min: 0` **800/800**、`Level: 1` **800/800**、`KDA 0/0/0` **800/800**、
`LastHit = 0` **800/800**、`Actual Player Damage: 0` 全零。
引擎自己写了原因:`Not submitting stats. Both teams must have human players.`

**这不是 con_logfile 那个 bug,17:36Z 的 `.log.stdout` 修复也治不了它** ——
零是**引擎输出里的零**。⇒ **P4.1 的「gpm 差」不是「测了没效应」,是根本没测**,
在有人拿出一份非零记分板之前,任何标尺波都不许把 gpm 写成读数。

唯一在变的经济类字段 `Gold:`(swap-average **+108.6** 偏 main)按诚实边界
只登记为 **EXPLORATORY**:它和 level=1/KDA=0 出自**同一个已证明损坏的块**,
方向可看,**幅度不是测量**。

### 3.3 两臂是真的,不是默认 AI(这条核验不是仪式)

GH #527 的失败形状恰恰是「没有 bots 目录 ⇒ 打默认 AI vs 默认 AI ⇒ 上传成数据」,
而**默认 AI 互打的读数就长得像 ~50%**。证据(`batch_run.log`):
`make_ab_build.py` 两个方向各打 **275 dispatchers**
(fwd `Radiant=NEW, Dire=OLD` / rev `Radiant=OLD, Dire=NEW`),
上一轮新加的 **`ab_guard` 两个方向各跑一次、三个目录全过**。
**本台上一轮加的那道门首飞就干了活。**

### 3.4 新臂树漂移暴露:**实测为零**

发波记录登记过一条暴露(`aws_run.sh` 在机上 fetch `main`,可能比登记的
`ffeb0aa6` 晚 1–2 个 commit)。本轮**量了不是假设**:`batch_run.log` 第 6 行
`cb814c6c..ffeb0aa6  main -> origin/main` ⇒ **测的就是 `ffeb0aa6`,与登记逐字相同**。
⚠️ 暴露**本身没修**,只是这一波撞干净了;下一波仍在。

### 3.5 局时与成本

- 局时:fwd 均值 **3109.8s** / rev **3069.2s**(中位 3095s,~51.5 分钟),
  min 2847 / max 3391。**这条路没有 cap-25**(它落在 `soak_loop.sh`)⇒
  **局长与任何 W 波不可比,登记不并池**。
- 成本(**机时侧推导,不是账单侧**;小时数是**下界**,因为终止在末次同步之后):
  attempt 2 存活 15:16:56Z → 末次 S3 上传 16:51:18Z = **1.573h × $0.673 ≈ $1.06**,
  加 attempt 1 的 ~$0.03 ⇒ **Y1 ≈ $1.09**,低于预估 $1.35。
  `$0.90` 那条自动失效条款写在 **spot** 波的**账单侧**,对这波按需波两头都不适用。
- ⚠️ **终止码读不到**:`describe-instances terminated,shutting-down` **空**
  (~1h 就老化掉,GH #375),已在记录里写成「推断不是读数」。

### 3.6 ⭐⭐ 顺带发现:`Y1_wave.json` 唯一那个机器形状的键指着**没测东西的那台**

`instance` 块记的是 **attempt 1**(`i-0c57a81c63bc57f31` / `..._151230_...`,167s 就死),
真正产数据的是 `..._151656_...` / `i-00f0417bfc7918609`。
本轮补 `instance_actual`,并留 `instance_record_correction`(**原文不删**)。
**读键不读散文的工具会被它整个误导** —— 下一条就是活例。

## 四、⭐⭐⭐ 闸 (i) 对 Y1 完全失明(开 GH #534)

本轮 `wave_throttle.py`:

```
anchor wave : W48 (4 machine(s))
last machine up : 2026-09-05T12:24:12Z  <- the anchor (GH #469 ruling 1)
unlock : 2026-09-05T18:24:12Z
WAVE_THROTTLE: THROTTLED (exit 3) -- do not launch. Wait 898s.
```

**`THROTTLE_EXIT=3`,不许发** —— 本轮据此不发波。但真正的发现是:
**Y1 一个字都没被提到**,而它 15:16:56Z 上机、花了 ~$1.09,比锚点晚 3 小时。

根因**结构性**:工具读 `machines[]`(GH #469 裁定 ② 正是拿这条当优点:
「不依赖任何人诚实地填一个字段」),而 ref-vs-ref 路径的波是单机、
记在 `instance` 下 ⇒ **整整一波对闸 (i) 不存在**。
**失效方向是危险的那一侧**:工具打的解锁(18:24:12Z)**早于**保守真值
(Y1 末台 + 6h ≈ **21:17Z**),即**允许更早花钱**。
与「shallow clone 空输出被读成无漂移」同族:不报错、不少给,只是静静放宽。

⇒ **下一轮的锚点按保守侧取 `2026-09-05T15:16:56Z`,解锁 ~`21:17Z`**,
**不要只读工具那一行**,直到 #534 修完。

## 五、连掉五轮的零成本活做掉了:`rec_slot_baseline` 并入 W48

**先复现再动基线**(沿上次并入的纪律):用**原来那 9 个** run 重跑
`--emit-baseline`,与已提交文件 **md5 逐位相同**(`f97ff52cf332ebecbd861b16e566b407`,
`IDENTICAL = True`,453 局)⇒ 工具确定性成立,**然后**才敢信它 13 个 run 的输出。

**预登记的验收先做,而且是对并入前的旧基线做**(并入后再做就是循环):
`rec_slot_cost.py <W48 四个 run> --baseline rec_slot_baseline.json`
→ **`W48_ACCEPT_EXIT=0`**,`compared 16 slot(s); 0 recording slot(s) beyond tolerance`,
box factor 0.974,净值 **−1.5% ~ +1.0%**,208 局。

并入后:**453 → 661 局,9 → 13 run**,`rec_slots`/`rec_set` 逐字不变
(`8` / `[1..8]`,recorder set 仍是**读**出来的:`read from 8 claim sidecar slot(s)`)。
逐槽 timescale 位移 **−0.50% ~ −1.23%**(上次并入是 −0.04% ~ −0.32%,这次大是因为
W48 整个箱子比旧基线慢 2.6%,box factor 0.974 会把它除掉)⇒
**远在 5% 容差内,按构造翻不动任何验收结论 —— 它改的是基线的精度不是它的判决。**

⚠️ **一条反向读数要登记,不许被 exit 0 盖掉**:W48 的**每槽局数**通道里,
8 个录制槽**全部低于**对照槽均值(13.50 局/槽 vs 12–13,缺 0.5–1.5 局/槽 ≈ 7%),
**方向与当年立论时那次(+2.33 局,录制槽反而多)相反**。
验收门只看 timescale 通道,所以 `exit 0` 成立;但「录制代价约等于零」的
**第二通道这一波是反的**,登记,交总监。

## 六、本轮不发波(理由,不是省略)

- 闸 (i):`THROTTLE_EXIT=3`(且真锚点更晚,见四)⇒ **不许发**。一票否决,其余门不必求值。
- 另注 P4.1 的排序条款(「标尺波排在任何 arm 波之前,直到 P4.1 有读数」)
  **已部分满足**:胜率有了,gpm 没有(见 3.2)。**是否算「有读数」不归本台裁**,
  已随 #535 交总监;在裁定之前,本台按保守侧仍把标尺波排在 arm 波之前。

## 七、铁律 6 / 11

- `bots/`/`game/`/`tests/` **本会话一行未改**;改动 = `iterations/` 下的报告/波次记录/章程
  + `tools/batch_test/soak/rec_slot_baseline.json`(纯数据基线)。
- 静态门与推送门读数见收尾补记;动态半(#124)**未跑不声称**。
- **铁律 11**:未触发 `requires approval`;GH MCP 可用(#534/#535 已开)。
  **无空转等待**(后台自检跑着的整段时间用于收割、日志核验、基线复现与并入)。

## 八、交棒

1. ⭐⭐⭐ **总监 —— GH #534**:闸 (i) 看不见 ref-vs-ref 路径的波,失效方向危险;
   下一轮起在修好前按 Y1 锚点(21:17Z)走保守侧。
2. ⭐⭐⭐ **总监 —— GH #535**:标尺路径的记分板逐项为零 ⇒ **P4.1 的 gpm 那一格没有量具**;
   请裁 P4.1 是「已满足胜率那半」还是「未完成」。
3. ⭐⭐ **总监 —— `run_batch.sh` 的 `.log.stdout` 修复(`25d237b2`)至今没在农场跑过**,
   下一波标尺波是它的首飞,报告要点名 json 是否非零。
4. ⭐⭐ **总监 —— 录制代价的第二通道(每槽局数)本波反号**(见五)。
5. ⭐ **总监 —— `$0.90` 常数仍未重裁**(W46 已越线;Y1 是按需波不适用)。
6. ⭐ **总监 —— GH #528 降级**:那台外来实例已自行结束,只剩「给一条长期规则」。
7. ⭐ **本台下一轮**:(a) 闸 (i) 锚点取 **15:16:56Z / 解锁 ~21:17Z**(见四);
   (b) 自检 trunk python 那一侧本轮 `UNCERTIFIABLE`,补跑取真码;
   (c) 若发波:W48 已收完、Y1 已收完,**无未收割 run**。
8. ⭐ 存量:#420 / #432 / **#388 的 W34 103 份 `.dem` 2026-09-22 到期(17 天)** /
   #460 / #454 / #449 / #446 / #429 / #418 / #412 / #408 / #402 / #395 / #375 /
   #352 / #329 / #313 / #290 / #271 / #33 / #527 / #528 / `strategy-5b` /
   `strategy-31/32` 第十三轮不搭波。

## 九、收尾泄漏检查

`LEAK_EXIT=0`。`running/pending` **空** —— 判据是「恰好等于意图」:
**本轮未发任何实例 ⇒ 收尾预期恰好为空,实测为空**。常驻仍只有 AMI
`ami-0a990a26d89c66547`。⚠️ 章程那句照抄:`--leak-only` **只报「多出来的」,
「少一台」它不会举手**;本轮无波次可点名,该半由「未发波」这个前提承担。

铁律 6 收尾真码:静态门 **`GATE_EXIT=0  CLEAN`**
(`luacheck bots game: 0 warnings`;门自己装的 luacheck:
`installed luacheck (it was missing; apt package lua-check, bounded)`),
`ARM_HOOK_EXIT=0`(`core.hooksPath=.githooks, already set`),
**未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行**;
动态半(#124)**未跑不声称**。

## 十、token 用量

`TOKENS total_in=7,246,753 out=44,731 turns=52`(到统计时刻为止;
统计后的收尾回合不计入)。
