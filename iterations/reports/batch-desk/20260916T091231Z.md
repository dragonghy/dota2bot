# 批测台 2026-09-16T09:12Z —— 刹车第三十二轮持有;零发波、零收割、零泄漏

**一句话**:MTD `$91.293`,headroom 到 `$90` 刹车 = **`−$1.293`**,闸 `WAVE_FENCE: THROTTLED (exit 3) -- brake`
⇒ **本轮零发波**(连续第三十二轮);GH #779 owner **第二十三轮零表态**。
本轮唯一实质产出是一条**计量纪律**的现场:上一轮 §七 用来核验 GH #850 的那条读数**在浅克隆上不成立**,
而这条事实**全队 22 天前就写下过**。⛔ 被推翻的是那条**读数的资格**,不是 GH #850 的结论(见三)。

---

## 一、成本(RULING 48 三段式)

`零 EC2 / 零 CE / S3 读取 0 个对象(出网未计价)` —— 本轮**一次 `s3 cp`/`s3 sync` 都没跑**,
`.dem` / `analysis.json` 一个没下。⭐ 这是 RULING 48 落地以来**第二个真正的零对象轮**
(上一轮是第一个),属交棒 ③ 要的那份语料。

`check_costs.sh`(`COSTS_EXIT=0`)逐字:

```
91.293	USD
   source: budgets/dota2bot-batch (free) | forecast 102.519 | budget limit 100.0 | budget refreshed 2026-09-16T04:33:16Z
   accrual probe (GH #779): alpha = 0 accruing instance(s) account-wide; beta = 0 wave record(s) after 2026-09-15T17:15:16Z
   stamp unknown (fresh container) -- alpha and beta are both zero -- a frozen stamp over an idle account opens no blind spot
   >= $35, but headroom to the $90.00 brake is $-1.293
   < $1.10 (cheapest wave) — CE confirmation SKIPPED, NOT passed (GH #801).
   Nothing can launch at this MTD, so confirming it buys nothing.
   Resumes by itself as soon as headroom >= $1.10.
```

⭐⭐ **本轮的 MTD 不是一个新读数,是上一轮那一个**:`budget refreshed` = **`2026-09-16T04:33:16Z`**,
与上一轮(06:15Z 那轮)**逐位相同**,值也**逐位相同**(`$91.293`)⇒ 快照在本轮读数时刻已陈旧
**`4h39min`**,本轮**没有买到任何关于支出的新信息**。⛔ 不据此说「支出停了」——
`budgets` 对 EC2 有 4.3–11.3h 滞后,而**零发波轮里滞后的是刷新本身**,两者不是一回事。

**空转速率(照交棒 ⑤(e):只用距今 ≥ 11.3h 的两个端点)**,端点取各轮报告里的 `budget refreshed` 戳:

| 端点对 | Δt | Δ$ | 速率 |
|---|---|---|---|
| 09-15T04:18:45Z `$90.569` → 09-16T04:33:16Z `$91.293` | **24.24h** | `$0.724` | **`$0.717/天`** |
| 09-15T14:40:18Z `$90.782` → 09-16T04:33:16Z `$91.293` | **13.88h** | `$0.511` | **`$0.883/天`** |

⛔ **短段照登原始差值,不换算**:09-16T01:56:47Z `$91.003` → 04:33:16Z `$91.293` = **`+$0.290 / 2.61h`**。
⭐ 两个合规速率都落在月末口径假设的 `$0.40–0.92/天` 带内(`$0.717` 近带中),⇒ **本轮不收窄该带**。

**月末(口径不变不收窄)**:`$91.293 + 13.9 天 × $0.40–0.92/天 − $0.8` ⇒ 9/30 落在 **`$96.1 – $103.3`**
(上一轮 `$96.1 – $103.4`,左移 `~$0.1`,唯一自变量是剩余天数)。
⭐⭐ 要点名的仍是上半段:**整月零发波的前提下,上沿 `$103.3` 越过 `$100`**,而 `$100` 正是闸里
`fence` 那一行的来源。⛔ 不主张 `$100` 一定会/不会被越(区间跨在它两侧)、⛔ 不外推到十月、⛔ 不改任何常数。
⚠️ AWS 自己的 `forecast 102.519` 落在区间内 —— ⛔ **巧合,不当互证**。

**归因边界逐字保留**:GH #779 两次第一手量到的外来支出
`c7a.16xlarge $23.2805 + r7a.16xlarge $14.1550 = $37.4355`(只存在于 09-05/09-06 两天,此后一分未涨,
⛔ **不是正在扩大的洞**)⇒ 咬住本台的 `$91.293` 里 **41.0% 不是本台的钱**,本台可归因 MTD ≈ **`$53.86`**。
⚠️ 能证明那两个机型不是农场用的(W1–W69 只有 `c6i.4xlarge`,界前偶有 `c6a`),**⛔ 不能**证明是谁跑的。

## 二、闸:`FENCE_EXIT=3`,是刹车不是围栏

```
accrual scope    : 17 region(s) read  <- COMPLETE: every region this account has enabled
accrual check    : CERTIFIED (0 accruing instances account-wide, read this run) -- pending $0.000 is a reading, not a default.
actual (MTD)     : $91.293   <- re-read this run, never cached
pending waves    : $0.000   (launched, may not be in MTD yet)
fence            : $100.00   <- lowest ACTUAL alert not yet crossed (Ruling 1)
brake            : $90.00   (owner's line, not derived here)
operative ceiling: $90.00   = min(fence, brake)
WAVE_FENCE: THROTTLED (exit 3) -- brake, not fence. Stop and report to the owner; do not wait it out.
```

⛔ 未传 `--no-accrual-check`、未传 `--director-crossing`;`crossing registry: 0 record(s) ... none in force`。
⇒ **零发波**,P4.1 upstream 标尺波**连续第二十五轮欠**,唯一阻因是预算,**不是未认领也不是排期**。

## 三、⭐⭐⭐ 上一轮 §七 那条核验读数在本容器上不成立 —— 而结论仍然成立

上一轮为核验 GH #850 是否落地,报告里写的是:

> `git log --oneline -3 -- tools/batch_test/aws/spot_run.sh` 顶是 `6188c0f7 director 2026-09-15T19:08Z (backfill 3)`

本轮**同一条命令**在本容器答 **`0b86d66b replay-check 2026-09-15T21:46Z`**,且 `-3` **只回来一条**。
原因现跑:

```
git rev-parse --is-shallow-repository        => true
git rev-list --count HEAD                    => 50
cat .git/shallow                             => 0b86d66b..., 9737166...
git rev-list HEAD | tail -1                  => 0b86d66b...        (= graft 边界本身)
git log --oneline -- tools/batch_test/aws/spot_run.sh | wc -l  => 1
```

⇒ 那条「顶是 X」**不是关于文件的事实,是关于本容器 graft 点在哪的事实**;graft 点每轮不同,
于是同一条命令**每轮给不同的答案,而每一个看起来都像正常读数**。

⭐ **失效方向是伪造出处,不是空输出**(这一点比已知的那条更坏):

```
git show --stat 0b86d66b -- tools/batch_test/aws/spot_run.sh
 tools/batch_test/aws/spot_run.sh | 473 +++++++++++++++++++++++++++++++++++++++
 1 file changed, 473 insertions(+)        # 且 diff 头是 `new file mode 100755`
```

即 git **明说**这个 commit 新建了一个 473 行文件,而它并没有。空输出至少长得可疑;
**这个读数长得完全正常、有日期、有 subject、有全量 diff**。

⛔ **honest boundary —— GH #850「未落地」的结论不受影响**,因为那个结论**靠的是文件内容不是历史**:
本轮现跑 `grep -n "soak_farm.log" spot_run.sh` 仍是 **`246/263/267`**(唯一上传点 267 仍在抢占分支里),
`278/279/280` **三个 `|| true` 一字未动**,`281 git log --oneline -1 || true` 仍只进那条不上传的日志。
⇒ **上一轮交棒 ⑦(d) 本轮仍不成立,⛔ 不执行、⛔ 也不退休它,逐字保留到修法落地那一轮。**
⛔ 本台仍未代修(harness 编排权不在本台,先例 GH #33)。

⭐⭐ **这条事实全队 22 天前就写下过,而它上一轮还是复发了。** 逐字出处
`iterations/reports/strategy/20260825T192600Z.md:190`(标题就写着「连带的 harness 事实(全队适用)」):

> 在 shallow clone 上,`git log -S <string> -- <path>` 会把**边界 commit** 报成引入者,
> 而且**看起来完全正常**(有日期、有 subject、diff 显示 `/dev/null → +行`)。
> 要按历史下结论,先 `git rev-parse --is-shallow-repository`。

⇒ 缺的**不是这条知识**,是它**住在一个批测台不读的文件里**:它是某一轮的报告散文,
没进任何章程、任何工具、任何闸。**与「apt 包名是 `lua-check`」被独立记录三轮、
铁律 6 第一道门仍一次没开过(GH #205)同族。** 本轮开一条 `[harness]` issue(见六);
⛔ **issue 号在 follow-up commit 里补** —— 按 GH #290 顺序,发表在 `git push` 之后,
⛔ 不预先写一个还不存在的号。
⛔ 本台**不主张**任何历史结论有问题、⛔ **不主张** Routine 不该用浅克隆 —— 该改的是读浅克隆的读法。

⚠️ 顺带的诚实边界:本轮**也**用了一条历史读数 —— `git merge-base --is-ancestor HEAD origin/main`
⇒ `YES`、`git rev-list --count HEAD..origin/main` ⇒ **2**(本地分支落后 main 两个 commit)。
这一条**能答**,因为两端都在 graft 之上;⛔ 但它与上面那条同属一类,**不得据此认为浅历史一般可信**。

## 四、收割:无可收

`alpha = 0` 台在计费、`beta = 0` 条波次记录(cutoff `2026-09-15T17:15:16Z` 之后)、
`pending waves : $0.000` 且逐字 `CERTIFIED ... is a reading, not a default`。
⇒ 无在飞波次、无未收割 run。⛔ 交棒 ⑤(c) 本轮不触发(`beta = 0`,故 `soak/` 未列)。

## 五、泄漏五条独立路径全零(⛔ 不合并成一句)

① `check_costs.sh` 自带「running/pending instances」区块**空**;
② `describe-instances` 五态(pending/running/shutting-down/stopping/stopped)、**不加 tag 过滤**
⇒ `INST_EXIT=0`、`LINES=0`;
③ 账户级 **17 区 `REGIONS_COMPLETE=17` / `INSTANCES_ACCOUNTWIDE=0`**,闸独立复核 `alpha = 0`、`CERTIFIED`;
④ AMI 仍 `ami-0a990a26d89c66547`(`dota2bot-batch-20260718`)**一张**(常设成本,非泄漏);
⑤ 闸的 `pending waves : $0.000`。
⚠️ 未买账单侧第六条(逐日 `EC2 - Compute`),理由同 GH #801 四行(headroom 为负,买不到决策)。

**`bots/Customize/soak_side.lua` 在本容器不存在**(现跑 `ls: cannot access ... No such file or directory`)
—— 交棒 ⑤(b) 要求的发波前检查,本轮零成本先做掉;⛔ 下轮仍要重做(它是容器局部的)。
⭐ 与 GH #848 并读:这是**第四个**说那条红是容器局部遗留文件、**不是 trunk 红**的证人。

## 六、issue 与交棒

**新开一条 `[harness]` issue(号见 follow-up commit)**(见三):建议把「按历史下结论前先验 `--is-shallow-repository`」
从某一轮的报告散文变成**读得到的东西**(自检/`claim_precheck.sh` 打一行 shallow 标志,
或章程 tool-pitfalls 一行),并附本轮的伪造出处现场。⛔ 本台不代修。

**交棒**:
1. ⭐⭐⭐ **owner —— 刹车第三十二轮持有,GH #779 第二十三轮零表态**(本轮现读 `state=open`、
   `comments=17`、`updated_at 2026-09-16T06:22:00Z`,而那第 17 条**是本台上一轮自己发的**
   ⇒ ⛔ 不得读成 owner 有动作)。三条路(维持 `$90` / 给预算加过滤器 / 一次性解禁到某个数)
   一条都还没选;两个数照旧并排:(甲) `$90` 里 **41.0%** 不是本台的支出,本台可归因 MTD ≈ `$53.86`;
   (乙) **整月零发波下 9/30 上沿 `$103.3` 越过 `$100`**。⛔ 本台无权自裁、不预选。
2. ⭐⭐ **总监/协同组 —— GH #850 未落地**(三在树上按**内容**核过);最小修法逐字重贴:
   把 `git log --oneline -1` 的输出单独写一个小文件并**无条件**上传到 `$S3_RUN/`。
3. ⭐ **总监 —— `s3_attribution_gap_remeasure_under_ruling48` 触发判据请重裁(连续第七轮未获回应)**;
   建议的日期判据「第一个干净的裁定后整日」**最早可读 `~09-17T11:20Z`,即明天**。
   ⭐ 本轮 `<N> = 0` 是该欠条要的语料的第二个点。状态仍 **OWED**,⛔ 本台不自行改欠条(先例 GH #33)。
4. **总监 —— 上一轮交棒 ②③④⑥ 与更早的 ⓪③④⑤ 逐字保留,本轮未获回应**,⛔ 不自行退休任何一行。
5. **下一轮本台**:(a) 开工第一条命令 `bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1; echo "EXIT=$?"`
   —— ⛔ 无管道(本轮**又犯**,见七);(b) 刹车解除(headroom ≥ `$1.10`)时按规格发 P4.1 标尺波、
   收割必带 `--ledger iterations/games_ledger.jsonl --ledger-wave W<N>`;`soak_side.lua` 发波前检查**重做**;
   (c) 若某轮 `beta ≠ 0`,`soak/` 必须列;(d) ⑦(d) 逐字结转(GH #850 落地后看 `$S3_RUN/` 有无树记录对象);
   (e) 空转速率只用距今 **≥ 11.3h** 的两个端点算,更短的段照登原始差值;
   (f) ⭐ **新增:任何「`git log` 顶是 X」式的读数,先打 `git rev-parse --is-shallow-repository`**,
   是 `true` 就只用**文件内容**下结论(本轮三就是这么救回来的)。

## 七、铁律 9 / 开工自检 / ⛔ 自捉

**铁律 9**:P4.1 标尺波连续第二十五轮欠,阻因是预算。

**⛔ 自捉(方向对本台不利,照登)**:交棒 ⑤(a) 的字面模板**第九轮仍未照抄** —— 本轮第一条自检命令
是 `bash tools/agent/routine_selfcheck.sh 2>&1 | tail -40`,**带管道**,当场被拒,逐字:
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.` +
`This is evidence discipline 3; it has recurred 5x, every time as the first command of the round.`
📌 `exit 2 =「没跑成」不是「没发现」` ⇒ ⛔ 那一次什么都没检查。第二次改成重定向裸读跑起来了。
⭐ **拒绝腿再次自己工作**(第 7 次挡住同一个假读数);不工作的是本台第一条命令 —— **九轮八次**。
⚠️ 脚本自陈计数仍是 `5x`(上一轮同样读到 `5x`)⇒ 计数器不跨容器,⛔ 本台只登记不解释。

**开工自检**:写本节时自检**仍在运行**(`ps` 数到 **3** 个匹配进程,日志停在
`=== trunk health (python test suite) ===` 之后)—— 那是 **python trunk 腿正在跑**,
⛔ **不是「它没跑完」**;「tail 里没有」不是「它没产生」。**腿级判词见 follow-up commit**;
⛔ 在拿到之前**不写 trunk 绿也不写 trunk 红**。跑到的那一段:
`LUA GATE COVERAGE  disk 485 | push gate 332 | 开工自检 leg 119 | known_red 6` /
`UNCOVERED ... 114 of 485 (24%)  {'no_manifest_row': 52, 'timed_out': 60, 'too_slow': 2}` /
`NOW COVERED tests/test_fieldsip_atom_pricing.lua` / `UNCOVERED SET GREW -- 2 file(s)`
(`test_dusttower_dive_guard.lua` / `test_fieldsip_transfer_receiving_site.lua`)/
`no armed id hangs under an unarmed gate -- OK`。
⭐ 与上一轮**逐位相同的 2 个文件**(⛔ 未恶化也未修复);⚠️ 三个分母各 **+1/+0/+1**
(484→485 / 332→332 / 118→119),`UNCOVERED` 的绝对数与百分比**逐位不变**(114 / 24%)
—— ⛔ 只登记不归因。⛔ 本台不代修、不调成员资格。

## 八、token 用量

见 follow-up commit。
