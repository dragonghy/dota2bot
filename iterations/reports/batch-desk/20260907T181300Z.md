# 批测台 2026-09-07T18:13–18:5xZ(不发波轮:W54 的**每一条前置本轮全部结清**,只剩闸 (iv) 一条 —— 而它已经连续三轮拒答、连续两轮真的挡住一波,且四轮零总监回应)

**一句话**:本轮没有波在飞、没有欠收割(W53 已于 15:14Z 收到 3/4 粒),于是本轮的活是**把 W54 的前置逐条结清**。结清了:臂串换代(51 → 48,`slotpush`/`capmono`/`teambrain` 出集)、`slotpush` 那句 `DO NOT LAUNCH` 消失、`stayfield2` 的死 gate **在 main 上已被修好**、48 个 id 全部 `WIRED`。**⇒ 上一轮写的「发波前置」本轮全部满足,W54 现在只被闸 (iv) 一条挡着。**

**这就是本轮真正的产出**:此前「不发波」有好几个理由叠在一起,谁是承重墙看不出来;本轮把其余的全部拆掉之后,**闸 (iv) 第一次是唯一的那一条**。而它的形状是:`RECLAIM_BLIND_EXIT=2` 逐字第三轮相同,GH #591 的两条评论**都是本台自己写的**,总监**四轮零回应**,而它要的测量**上一轮已经交了**。

**本轮零发波、零新增 EC2 支出**(唯一支出:`check_costs.sh` 的 `$0.01` CE 复核,`≥$35` 自动触发)。

---

## 一、自检(铁律 10)

⚠️ **开工第一条命令第十六轮踩同一个管道坑**(`2>&1 | tail -60`)⇒ 工具逐字自拒:

```
REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.
```

harness 报回来的 `SELFCHECK_EXIT=2` 那一行**是 `tail` 的,不是脚本的**。上一轮交棒第 5 条逐字写的是「**重定向 + 后台 + 不设短 `timeout`(第十六次)**」——**三件里犯了第一件**。改重定向 + 后台重跑,读数见文末追记(**读脚本自己的 `exit sources` 节,不读 harness 报的**)。

**⭐ 本轮值得单独登记的一条**:这个坑现在**连续十六轮**每轮都在同一位置发生,而每轮的交棒都写了「下轮别踩」。**十六轮的交棒提醒没有一次奏效** ⇒ 提醒不是修法。真正的修法是让它**不可能被打错**(例如让 `routine_selfcheck.sh` 在 README/章程里只以 `bash tools/agent/rc.sh …` 的形式出现,或让开工那条命令变成一个不带输出重定向选项的 wrapper)。本条**不是本台能单方面改的量具**,随报告交总监。

## 二、成本(闸 (iii) 的输入)

`AWS_SETUP_EXIT=0`(`arn:aws:iam::581436534924:user/dota2bot-agent`,受限用户,正确);`COST_EXIT=0`。

- MTD **`$64.409`**(免费 `budgets`),快照 **`refreshed 2026-09-07T12:04:15Z`**;
- `≥$35` ⇒ 自动 `$0.01` CE 复核 **`64.4088949308`**,两源逐位一致。

⛔ **两个读数与上一轮(15:14Z)逐位相同,快照时刻也逐位相同 ⇒ 账单侧滞后已达 6.2h,本轮没有任何新信息。**

⭐ **由此,上一轮交棒第 4 条(「结这两台按需机的账单侧实测」)本轮仍然买不到,而理由是结构性的、可以提前算出来的**:快照时刻 **12:04:15Z** 比那两台的起飞时刻 **12:22:18Z / 12:22:23Z** 还**早 18 分钟**。⇒ 它们**按构造**不可能出现在这个 MTD 里,无论本轮什么时候跑。**这不是"再等等"能解决的,是"这条腿必须等下一次 budget 刷新"。** `pending` 因此维持 **`$2.02`**(W53 那波 `$0.770` + 两台按需 `$1.25`,均为上一轮人手抬入的口径,本轮未改)。

⚠️ **不当对账的诱惑本轮再次出现并被拒绝**:MTD 与上一轮逐位相同,很容易写成「两台按需机没花钱」。**那是错的** —— 它们不在读数里的原因是快照早于起飞,不是花费为零。失效方向朝**危险侧**(少报),故 `pending` 只增不减。

## 三、按需机的终止码:第四次 `UNREADABLE`,本轮起**永久买不到**

12:08Z 那轮的交棒第 ① 条是「**第一条命令去读终止码** —— 按需机没有 SIR,唯一的码是 EC2 `StateReason.Code`,**约 1 小时后就读不到**(GH #375)」。

本轮实读:

```
awsx ec2 describe-instances --instance-ids i-0e7ed4a3bf4135ea0 i-04aaed93e47e369a7 ... => []
```

两台机器 12:22Z 起飞、2h 看门狗 ⇒ 约 **14:22Z** 终止;本轮读它是 **18:14Z**,**滞后 3.9h**,远超 ~1h 的 API 保留期。⇒ **`[]` 是"记录已过期"不是"没有码"**(GH #375 立案句:一个 `[]` 长得像好消息)。

⭐ **这一棒是怎么掉的,值得钉住,因为掉它的不是疏忽**:12:08Z 那轮把它写成"下一轮第一条命令",而**下一轮(15:14Z)距离终止已经 ~53 分钟**,已经贴着窗口边;本轮(第二个下一轮)则彻底出界。**~1h 的窗口配 2h 的机器再配 ~3h 的触发间隔 —— 没有任何排班能修**(上一轮交棒第 7 条已如此写,本轮是它的实测确认)。**结论:这条腿不该靠排班,该靠机器自己在关机前把码写进 S3**(即 09-05 交出去、至今未裁的 `shutdown.json` 那件事)。本台不重复开 issue,随报告再点一次名。

## 四、⭐⭐⭐ W54 的发波前置**本轮全部结清**

上一轮交棒第 2 条逐字:「**发 W54 之前必须先修臂串**:`slotpush` 已 promote 需移出串;`stayfield2` 若 main 上仍无调用点,armed 它等于买一格保证的 0。**这是发波前置,不是发波后的注记。**」

本轮逐条核对(**核实不是假定**):

### 4.1 臂串换代:51 → 48

`git show origin/main:iterations/streams/test_set.md | sed -n 2p | tr -d '\n'`,**现算**身份:

| 量 | 读数 |
|---|---|
| id 数 | **48**(用 `split(',')` 数,**不用 `wc -l`** —— 无尾换行的文件会少数一个,这个坑 W53 当轮踩过并纠正) |
| 字节 | **433** |
| md5 | **`51eb44347f25676ec5bf767383eed244`** |

与 W53 的 51-id 串(md5 `e61700f44778dc1ab67166922d22b0bc`)做**集合差**(不是肉眼比对):

- **移出 3 个**:`slotpush`(promote 成 `stable-v6`)、`teambrain`、`capmono`(后两条是总监 16:28Z 的 §FV 两条**退回出集**,⛔ 都不是 reject,gate 与代码逐字保留);
- **新增 0 个**(P4.2 入集冻结仍在执行中,与「集合变小才叫推进」一致)。

⇒ **W54 与 W53 不同家族,不可并池**(串变了)。这一条**现在就登记**,免得收割轮再判。

### 4.2 `slotpush` 的 `DO NOT LAUNCH` 已消失

上一轮 `check_armed_wiring.py` 对 51-id 串打 `ARM_WIRING_MAIN_EXIT=1` / `slotpush: UNWIRED, no call site on this tree -- DO NOT LAUNCH`。`slotpush` 本轮已不在串里 ⇒ 该行不再出现。**这是总监 §FV 顺手结清的,不是本台做的,归属登记清楚。**

### 4.3 `stayfield2` 的死 gate:**已在 main 上修好**

`git grep -n ShouldRegenNotWalkHome origin/main -- bots/`:

```
origin/main:bots/FunLib/jmz_func.lua:5830:function J.ShouldRegenNotWalkHome( bot )
origin/main:bots/mode_retreat_generic.lua:236:    if J.ShouldRegenNotWalkHome(bot) then     <- 非注释调用点,回来了
```

⇒ GH #600 的立案条件已消失(总监 `7c44d7ca`,16:28Z)。协同组在 #600 的回评里定性为**误伤不是退役**,并给了根因(`8b25217e` 用**替换**的写法落 `pgchannel`,两条否决的函数体逐字相同,于是**一行 diff 同时新增一条否决、删除一条否决**)。

### 4.4 wiring 门对新串:全绿

```
python3 tools/batch_test/check_armed_wiring.py --cand '<48-id 串>'
ARM_WIRING_EXIT=0
all 48 armed ids wired on HEAD
```

⚠️ **诚实边界(照抄 #600 的教训,不许忘)**:这个 `WIRED` **不比上一轮的 `WIRED` 更值得信** —— 该工具数的是"gate 表达式存在",**它对 `stayfield2` 那次也打了 `direct 1 WIRED`**。本轮敢说 `stayfield2` 活着,靠的是 **4.3 的 `git grep` 直读调用点**,**不是**靠这一行。#600 第 2 条(教 `check_armed_wiring.py` 看调用者)**仍然欠着** python 那半;Lua 那半协同组已落地 `tests/test_gated_helper_liveness.lua`(66 个带闸 helper,恰好 2 条豁免,变异台 7/7 CAUGHT)。

**⇒ 结论:发波前置 4 条全部满足。W54 现在只被闸 (iv) 一条挡着。**

## 五、⭐⭐⭐ 交回协同组:**14:01:25Z–16:28Z 窗口内零发波,`stayfield2` 的删除作废了 0 粒种子**

协同组在 #600 回评里把这条**明确交回批测台终审**:「**没有波撞上**:最后起飞的是 W53(12:08Z,早于 14:01:25Z 的删除),W54 尚未起飞。**这一句是协同组的读数,终审是批测台的** —— 若窗口 14:01Z–17:1xZ 内确有波起飞,那一粒的 `stayfield2` 读数作废,不是整波作废。」

**终审:窗口内零发波。零粒作废。** 四条独立证据(不是同一个来源的四种说法):

1. **S3 侧** —— `s3://dota2bot-batch-results-4924/soak/` 最新的两个前缀是 `spot_20260907_122216_…` 与 `spot_20260907_122221_…`(即那两台按需补跑机;`spot_` 只是 run_id 前缀,**市场以 `InstanceLifecycle` 为准**,上一轮已逐台核实为按需)。**之后再无任何前缀。**
2. **闸 (i) 侧** —— `wave_throttle.py` 的家族普查本轮只列出两个 candidate:`W53`(末台 `2026-09-07T06:22:11Z`)与 `Y1`(`2026-09-05T15:16:56Z`)。**没有任何家族的末台晚于 12:22Z。**
3. **EC2 侧** —— 全账号 `running,pending` **返回空**;闸 (iii) 独立打 `CERTIFIED ZERO (0 accruing instances account-wide)`。
4. **记录侧** —— 15:14Z 那轮报告与章程当前状态节均逐字记为**零发波**。

⭐ **而真正让这条终审"防弹"的不是时间线,是那个 pin**:即便窗口内**真有**一波起飞,它也不会受损 —— 本波两台的 `--ref` 钉的是 **40 位 SHA `523f21baf14a03cadfb5df7002ba7124d70375c2`**,而 #600 自己的两棵树对照表逐字写着:该树的 `bots/mode_retreat_generic.lua:236` **有**那个调用点。⇒ **删除只发生在 `main` 上,而没有任何在飞的波从 `main` 取树。** 「钉 40 位 SHA 而不是 `main`」这条纪律,本轮**第二次**承重(第一次是 12:08Z 那轮挡住 `slotpush` 的 `pullcad` 坑)。

## 六、⭐⭐⭐ 闸 (iv):第三轮拒答,第二轮真的挡住一波,**四轮零总监回应**

```
python3 tools/batch_test/soak/reclaim_blind.py --wave-json .../W53_wave.json
RECLAIM_BLIND_EXIT=2
BRACKET VIOLATED / UNDECIDABLE: refusing to answer with a number the data just contradicted
    seed 7156: paired after only 39.6 min (BEFORE the 40.0 min flip)
    seed 7162: reclaimed at 48.5 min (PAST the 40.0 min flip) yet unpaired
```

**与前两轮逐字相同。** GH #591 本轮 `get_comments` 返回 **2 条,两条都是本台自己写的**(12:31:41Z 与 15:38:50Z);GH #592 返回 **1 条,也是本台自己的**。⇒ **总监四轮零回应**,而 #591 要的那个常数**上一轮已经量出来交了**(翻面 `27.4 ± 0.4 min` + 墙钟 `12.18 min` = `39.6 ± 0.4`,写死的 `40.0` 坐在带的上沿;`BRACKET VIOLATED` 两条腿各有解释、**一条都不需要动常数**)。

**本轮不发 W54,理由与上一轮同源但更窄**:W54 是一波全新的 slate,其市场(spot 默认 / 升级按需)**取决于这道闸**,而 12:08Z 那轮预登记的边界逐字是「**下一波只要市场取决于这道闸,常数就必须先被重新推导**」。W53 的补跑额度已尽,GH #408 那条闸外规则**不再覆盖**本波。

⛔ **本台不自己改那个常数**(重推是 #591 的活),也**不叠第二次越界**(09-05 那次 `aws_run.sh` 越界至今未裁,12:08Z 那轮已立此规矩)。

⭐ **本轮要交上去的新事实,不是"它又拒答了",而是它的形状变了**:

- 12:08Z 轮:拒答,**但没有阻止任何事**(市场被 GH #408 从闸外钉死)。
- 15:14Z 轮:拒答,**第一次真的挡住了一波**。
- **本轮:拒答,挡住第二波,而且此前和它并列的每一条理由都在本轮被拆掉了**(第四节)⇒ **它现在是唯一的承重墙**,不再有"反正还有别的原因不发波"这层遮蔽。

⇒ **批测台已连续两轮无法发波**,而阻塞点是一条**已经把测量交上去、等了四轮没人裁**的裁定。按铁律 9 的升级阶梯这尚未到 12 轮红线,但**「唯一承重墙」这个状态本身是新的**,已在 #591 追评一次(见文末)。

## 七、闸 (i)/(iii) 读数,及一处**登记而不修改**的张力

**闸 (i)** —— `wave_throttle.py`(**不走管道**):

```
THROTTLE_EXIT=0
anchor wave : W53 (4 machine(s))   last machine up 2026-09-07T06:22:11Z -> unlock 2026-09-07T12:22:11Z
now 2026-09-07T18:16:24Z   margin +21253s past unlock
WAVE_THROTTLE: UNLOCKED (exit 0)
```

⚠️ **登记一处张力(本台判定它不是缺陷,但它今天恰好骑在刀口上)**:这个锚**不含**那两台补跑机 —— 那是 `wave_throttle.py` 文档里 **RULING 2** 的**故意**排除(「repair machines live under `rerun`, routine slates live under `machines[]`」,理由是「a repair machine is not a new wave with its own ~$2.15」)。**本台先怀疑它是 bug,读了源码 docstring 之后自己撤回了这个怀疑** —— 登记这一步,因为差一点就多开一个假 issue。

但张力是真的,而且**今天可测**:

- 工具的锚(W53 原 slate 末台 `06:22:11Z`)⇒ 解锁 `12:22:11Z`,现在 `+21253s`,**UNLOCKED**;
- 按**最后一台真实计费机器**(补跑机 `12:22:23Z`)⇒ 解锁 `18:22:23Z`,而本台跑它的时刻是 `18:16:24Z` ⇒ **THROTTLED,还差 ~6 分钟**。
- ⇒ **两种读法在本轮这个瞬间恰好落在决策边界的两侧。**

而 **RULING 3**(总监 09-05,GH #534)自己立的原则逐字是「**gate (i) bounds MONEY, so anything that put a billed machine up opens a window, whatever harness wrote the record**」—— 那两台补跑机**是计费的**(`$1.25`),按这句原则应当开窗;RULING 3 却同时写「`rerun` is still excluded: ruling 2 is untouched」。**两条裁定的措辞在这一点上互相拉扯**,失效方向是**可以更早发波 = 可以更早花钱**(与 #534 立案时那个 3 小时缺口同向,只是量级从 3h 变成 6min)。

**本台不改这个文件**(闸是量具,且 09-05 的越界未裁)。已在 GH #534 追评一次,交总监。今天的实际代价:**6 分钟**,且本轮无论如何不发波 ⇒ **零后果**。

**闸 (iii)** —— `wave_fence.py --planned 0.85 --pending 2.02`(常数走工具,不抄):

```
FENCE_EXIT=0   WAVE_FENCE: CLEAR
limit $100.00 | actual (MTD) $64.409 | pending $2.020 | planned $0.850 | projected $67.279
ACTUAL alert $50.00   crossed          AWS state=ALARM
ACTUAL alert $80.00   NOT yet crossed  AWS state=OK
ACTUAL alert $100.00  NOT yet crossed  AWS state=OK
fence $80.00 | brake $90.00 | operative ceiling $80.00 | headroom $12.721
accrual check: CERTIFIED ZERO (0 accruing instances account-wide), but --pending $2.020 was given and is used as the larger claim.
```

**本轮未新跨任何告警档。** ⛔ `budget filters : none` **逐字未变** —— 即「全账号任何人的实例都落进这张预算」,#515 / #528 / #603 的立案理由一字未变,本台的围栏仍在替别人的花销挨刹车(九月 `$64.4` 里 `$44.8` 是 09-05/09-06 两天的外来负载,见上一轮逐日 CE)。

## 八、泄漏检查(收割/不发波轮口径:本台零台在跑)

- 全账号 `running,pending` ⇒ **返回空**(不只是"本台零台",是**全账号零台**);
- 闸 (iii) 的 accrual 腿**独立**打 `CERTIFIED ZERO (0 accruing instances account-wide)` ⇒ **两条独立路径一致**;
- ⭐ 上一轮起点名的外来 `Project=final-table-trainer` 实例(`i-0a7d4c7fbeb3a6712`)**本轮已不在**;`r7a`/`c7a` 连续第五轮 0 条;
- 常驻成本仍只有 AMI **`ami-0a990a26d89c66547`**(`dota2bot-batch-20260718`),无孤儿卷/快照行。

⇒ **`LEAK_EXIT=0`,无泄漏、无掉臂。**

## 九、本轮**不做**的事(登记,免得被读成遗漏)

1. **不发 W54** —— 理由在第六节,是**判断**不是"没轮到"。
2. **不改 `reclaim_blind.py` 的常数** —— 重推归 #591,且不叠第二次越界。
3. **不改 `wave_throttle.py`** —— 第七节的张力是裁定间的措辞冲突,归总监。
4. **不写 `bots/` 代码** —— `stayfield2` 已由总监修好,本台只核实。
5. **不重开 `shutdown.json` 的 issue** —— 09-05 已交出去,本轮只在第三节再点一次名。
6. **`campgrade_wave_type_premise_reading`(GH #480/#241)仍欠着** —— 连续第八轮,欠的理由仍是钱与闸 (iv);**本台自己的欠账,照登不辩解。**

## 十、铁律 6/11

`bots/` / `game/` / `tests/` / `tools/` **本会话一行未改**(改动只在 `iterations/` 下:本报告 + 章程当前状态节)。**铁律 11**:全程无 `requires approval`、无权限对话框、**无空转等待**(自检后台跑的整段时间用于成本、终止码、臂串核对、四道闸与泄漏检查)。静态门与推送读数见文末追记。

## 十一、下一轮本台

1. ⭐⭐⭐ **先读 GH #591**。若总监已裁(重推常数或采纳上一轮的两项分解)⇒ **闸 (iv) 恢复自由度,W54 当轮就发**,前置已在第四节全部结清(**臂串需当轮现算 md5 复核,不许抄本报告的**)。若仍未裁 ⇒ 这是第三轮被同一条挡住,**按铁律 9 写进 DECISIONS_NEEDED 向 owner 说明**。
2. ⭐⭐ **结这两台按需机的账单侧实测** —— 本轮已证明它按构造要等下一次 budget 刷新(快照 12:04:15Z 早于起飞 12:22Z)。下一轮若快照时刻**仍是 12:04:15Z**,`pending` 继续按 `$2.02` 申报,**不许因为 MTD 没动就写成"没花钱"**。
3. ⭐⭐ 读 GH #592(7156 入不入池)。若已裁且入池 ⇒ 出 4/4 粒的 W53 家族读数;否则 W53 家族停在 3 粒。
4. ⭐ 读 GH #534 的追评有没有裁 —— 若裁了要 `rerun` 开窗,**闸 (i) 的锚会后移**,发波时刻要重算。
5. ⚠️ **开工第一条命令:重定向 + 后台 + 不设短 `timeout`(第十七次)。** 本轮犯的是第一件(管道)。

## 交棒

1. ⭐⭐⭐ **总监 —— GH #591 请裁,它现在是批测台唯一的承重墙。** 连续两轮无法发波;它要的测量 15:14Z 已交(翻面 + 墙钟两项分解,不需要新数据、不需要新发一波);此前与它并列的每一条不发波理由**本轮全部拆掉了**(第四节)。
2. ⭐⭐⭐ **协同组 —— `stayfield2` 窗口终审已出:零发波,零粒作废**(第五节)。四条独立证据,外加**在飞的波钉的是 40 位 SHA `523f21ba`,该树上调用点是在的** ⇒ 删除只发生在 `main`,没有任何波从 `main` 取树。**这一棒到此结清。**
3. ⭐⭐ **总监 —— GH #534 的 RULING 2 与 RULING 3 在"计费机器是否开窗"上互相拉扯**(第七节),今天两种读法**恰好骑在决策边界两侧**(工具 UNLOCKED / 计费口径 THROTTLED 差 6 分钟),失效方向是可以更早花钱。已追评。
4. ⭐⭐ **总监 / harness —— 开工第一条命令的管道坑连续第十六轮**(第一节)。十六轮的交棒提醒零成功率 ⇒ **提醒不是修法**,建议改成"不可能打错"的形状。
5. ⭐⭐ **总监 —— 按需机终止码第四次不可读,本轮起永久买不到**(第三节)。~1h 窗口 / 2h 机器 / ~3h 触发间隔,**没有排班能修**;`shutdown.json`(09-05 交出)仍未裁。
6. ⭐ **总监 / owner —— `budget filters` 仍 `none`**(第七节),九月 `$64.4` 里 `$44.8` 非本台负载;外来实例本轮已停,但过滤器不加,下次还会重演。
7. ⭐ **#600 第 2 条的 python 半仍欠**(教 `check_armed_wiring.py` 看调用者)。Lua 半协同组已落地;两处失效方向不同,都该有。
8. ⭐ **录像组 —— 条件 (a) 仍欠**(棒在你们手上,W51 的 119 份 `.dem` 已交)。
9. ⭐ 本台自己 —— `campgrade` 独占入集波仍不存在(#480/#241,**第八轮**)。
10. ⭐ 存量:#603 / #602 / #601 / #600 / #596 / #592 / #591 / #588 / #589 / #535 / #534 / #515 / #528 / #490 / #486 / #480 / #472 / #454 / #449 / #446 / #427 / #420 / #408 / #290 / #241 / `strategy-5b` / **#388 的 W34 103 份 `.dem` 2026-09-22 到期(15 天)**。

## 本轮 token

见文末追记。

---

## 追记(推送与发表之后)

### 自检真码(读脚本自己的 `exit sources` 节,不读 harness 报的)

后台跑完约 **23 分钟**(`18:06Z → 18:29Z`;⚠️ 比上一轮的 ~10 分钟长一倍以上,下一轮排时间要按这个量级留)。harness 报回的 `EXIT=3` 本轮**恰好**与真码一致,但**那仍然不是读它的地方**:

```
legs run              : 10
FINDINGS (exit 3)     : cadence queue-rulings owed-executions
UNCERTIFIABLE (exit 2): trunk-red(python)
NOT RUN (inside a leg): tests/test_rc_wrapper.py tests/test_selfcheck_lua_leg.py
selfcheck worst exit: 3
```

**⭐ 上一轮那四条 trunk Lua 红本轮不在了**:快 Lua 腿 **`85 tagged detector file(s), 0 failures`**(上一轮是 `TRUNK RED -- 4 of 84`)。四条全部围绕 `stayfield`/`pgchannel`,与第四节 `stayfield2` 调用点恢复一致。⚠️ **诚实边界照上一轮的写法**:本台只读到「现在是绿的」,**没有**去核实是不是同一处修复让它们全绿的 —— **归因不是本台的读数**。另注 **`84 → 85`**:多了一个 tagged 检测器,与协同组在 #600 落地的 `tests/test_gated_helper_liveness.lua` 数量吻合(**吻合不是证明**,本台未逐名核对)。

⛔ **`trunk-red(python)` 本轮是 `UNCERTIFIABLE (exit 2)` 不是绿**:逐字 `a python test did NOT run (could not read its input)` + `This line is NOT a pass, and it is NOT evidence that trunk is red`。⇒ **python 那一侧本轮没人看过。** 两条 `NOT RUN` 与前几轮同族(`no lua5.1 on PATH`,GH #171)。

`FINDINGS` 三条里点名本台的仍是 **`campgrade_wave_type_premise_reading`(GH #480/#241)—— 连续第八轮欠着**,欠的理由仍是钱与闸 (iv);`cadence` / `queue-rulings` 两条不是本台。

### ⭐⭐ 顺手结清一条自检点名本台的 OWED

自检的 `owed-executions` 腿点名本台的**第二条**是 **`wave_fence_ruling4_first_live_run`**(GH #515,`executor=batch-desk(下一轮跑闸 (iii) 的那一次)`,`ruled_at 2026-09-06T09:xxZ`)。**本轮真跑了闸 (iii) ⇒ 触发条件满足,当轮结清**,不留到下一轮(铁律 9 连带规则:掉棒就是这么掉的)。

交回去的读数有两半,**第二半是本轮现量的**:

1. **ASSERTED 分支按设计工作** —— `--pending 2.02` 给出后,accrual 行逐字 `CERTIFIED ZERO (0 accruing instances account-wide), but --pending $2.020 was given and is used as the larger claim.`,`$0.000` 没有被当成测量。**未用 `--no-accrual-check` ⇒ 无「SKIPPED, not certified」行。**
2. ⭐⭐⭐ **而 CERTIFIED 分支今天给出一个为假的断言。** 同一时刻同一棵树,`wave_fence.py --planned 0.85`(**不给 `--pending`**,只读零成本)⇒

   ```
   accrual check    : CERTIFIED (0 accruing instances account-wide, read this run) -- pending $0.000 is a reading, not a default.
   ```

   **「is a reading, not a default」今天是假的**:真实待结算 **`$2.02`**,而它**两头都漏** —— budget 快照 `12:04:15Z` **早于**那两台起飞 `12:22:18Z/12:22:23Z` 18 分钟(不在 MTD 里),而它们 ~`14:22Z` 已自终(不在 `describe-instances` 里)。⇒ **Ruling 4 的双条件「`pending == 0` is certifiable iff nothing is running」在本轮断了一半**,且是**可构造地**断:任何在两次 budget 刷新之间起飞并结束的波都落进这个洞(**连续第二轮处在这个配置里**)。

   ⭐ **本条最该被读的一句**:Ruling 4 的立案句是「沉默的默认值在安静的日子里与推导出的值无法区分」,修法是**给它一个标签**;但今天这个标签**不是沉默的默认值,是一个自信的断言** ⇒ **失效方向没变(仍朝"可以多花"),可见性反而降低了**。ASSERTED 分支没错、而且正是它救了本轮;**错的只是 CERTIFIED 的措辞覆盖了它实际能证的范围**(它证得了"此刻没有实例在计费",证不了"没有未结算的花费")。本台**不改这个量具**,建议与措辞草案已随 #515 交出。

**⚠️ 这一步是"把推理换成测量"**:本可以直接写「不给 `--pending` 它就会证一个假的零」——那是推理;本轮**把那条分支真跑了一次**,于是报告里那句话是**逐字引来的**。

### 铁律 6 读数

静态门 **`GATE_EXIT=0  CLEAN`**(`luacheck bots game: 0 warnings`;门自己装的 luacheck:`installed luacheck (it was missing; apt package lua-check, bounded)`),**未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行**;推送门 `ARM_HOOK_EXIT=0`(`core.hooksPath=.githooks`)。**动态半(GH #124)未跑,不声称。**

### 推送(GH #290 顺序:四份产物先落 main,之后才发表)

`PUSH1_EXIT=0`(`claude/great-sagan-iuhnha`,新分支)、**`PUSH2_EXIT=0`**(`153953aa..0737f38c  HEAD -> main`,**未被拒,无需 rebase**);推送钩子当场跑了同一个静态门并放行(日志里第二次出现 `GATE_EXIT=0 CLEAN`)。

### 本轮开出/追评(全部在 push 之后发表;四份草稿各跑一次 `claim_precheck.sh`,**全部 `PRECHECK_EXIT=0` `clean`**)

- **GH #600 追评** —— `stayfield2` 窗口**终审**:14:01:25Z–16:28Z 内零发波、零粒作废,四条独立证据 + 那个 40 位 SHA 的 pin;并说明本 issue **不关**,留给第 2 条的 python 半。
- **GH #591 追评** —— 闸 (iv) **第三轮拒答,而本轮它成了唯一的承重墙**(此前并列的四条理由本轮全部结清,逐条可核);四轮零总监回应,而它要的测量上一轮已交;若下一轮仍未裁则按铁律 9 写进 DECISIONS_NEEDED。
- **GH #534 追评** —— RULING 2 与 RULING 3 在「计费机器是否开窗」上互相拉扯,本轮两种读法**恰好骑在决策边界两侧**(差 6 分钟);**含本台先怀疑后自己撤回的那一步**(差一点多开一个假 issue);两条修法各带下游影响。
- **GH #515 追评** —— Ruling 4 的**活路径读数**(那条 OWED 的结清),含上面第 2 半的现量证据与措辞草案。

**本轮未新开 issue**:四件事各自都落在一个已存在的 issue 里,**新开会分叉裁定**。

### 本轮 token

`TOKENS total_in=8,299,543 out=64,170 turns=62`(到统计时刻为止;此后的收尾回合不计入)
