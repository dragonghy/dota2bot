# batch-desk 2026-09-02T18:30Z — 收割 W39;本轮零发波、零支出

**一句话**:W39 四粒全收(168 计分局),而本轮真正的产出是
**「被抢占 ≠ 丢臂」第一次被语料证伪** —— 只读 SIR 码会让下一轮去补跑一粒**已经跑完的种子**。

---

## 1. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh > 文件; echo $?` ⇒ **`EXIT=3`**(有发现,**不是没跑成**)。
`legs 8`;`FINDINGS = cadence trunk-red(python)`;汇总块打 `UNCERTIFIABLE: none`。

- ⚠️ **第一条命令我又误用了管道**(`... | tail -60`),工具当场拒答:
  `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked`。
  **`exit 2` 不是通过**;改成重定向才拿到真读数。工具自陈 `it has recurred 5x`,
  与报告序列的计数依旧对不上,照前几轮据实并列,不替它改口径。
- ⭐ **GH #420 原样复发(第三轮)**:python 腿自己打 `74 passed, 1 failed, 2 uncertifiable`
  并**点名** `tests/test_rc_wrapper.py` 与 `tests/test_selfcheck_lua_leg.py`,
  而汇总块打 **`UNCERTIFIABLE (exit 2): none`**。**两处读数在同一份日志里互相矛盾**。
- python 红:`tests/test_detector_source_constants.py`(与上一轮同一条;未 stash 重跑,
  **不声称 main 也红**)。
- ⭐ **好消息,登记**:Lua 腿本轮 **73 个 tagged detector 文件、0 failures**。
  上一轮那条 `test_incoming_damage_callsite_census.lua` 43→44 的红**已经不在了**
  (英雄组 `774f4f5` / `b5c11ed` / `864b7d6` 落在中间)。

## 2. 成本与围栏(铁律 1)

`check_costs.sh` ⇒ `COST_EXIT=0`。九月 MTD **`$3.637`**(免费 `budgets` 通道,
快照 `2026-09-01T17:58:37Z`)。

- ⚠️ **连续第五轮是同一张快照 ⇒ 它不携带任何新的围栏信息**,W38/W39 一分钱都还没进去。
  这正是章程 2(甲)存在的理由。
- **在跑实例:空**。常驻成本仅 AMI `ami-0a990a26d89c66547`。
- MTD `< $35` ⇒ **CE 未触发,那 `$0.01` 没花**。
- 围栏(2 甲,逐波按自己的市场取价):
  `3.637 + W38 spot ~0.60–0.64 + 2613 按需补跑 ~0.60–0.63 + W39 spot 0.70–0.76 = ~$5.6` ≤ **$80**。
  **未跨 $50 / $80 / $100 任一 owner 可见告警档 ⇒ 不欠解释行。**
- **本轮零发波、零新增支出。**

### 2.1 W39 单波实测机时

由 SIR 的 `create`/`update` 反解:`0.770 + 0.709 + 0.726 + 0.840 = **3.045 机时**`。
按 us-west-2 `c6i.4xlarge` spot 价带 `$0.231–0.250/h` ⇒ **本波 `$0.703–$0.761`**。
**低于 `$0.90` 常数 ⇒ 自动失效条款不触发**(它只在账单侧实测单波 **高于** $0.90 时才要求总监重裁)。

⚠️ **这是机时反解,不是账单侧读数**;章程禁用的「MTD 增量对得上波费 ⇒ 该波已计入」那个手法**一次都没用**。

## 3. ⭐⭐⭐ W39 收割:被抢占 ≠ 丢臂(本轮的主要产出)

标准路径,S3 逐局全量重算,四个 run **各自子目录未拍平**:

```
recover_verdict.py <四个 run 目录的父目录> "<本波记录里的 55-id 串>"   ⇒ RV_EXIT=0
files 192 seen / 192 loaded / 0 unparseable / 4 source dirs ;  scored 168 ; unfinished 0
```

**语料同一性**:交给工具的 armed 串**从本波自己的记录取回**,不是从当前 `test_set.md` 取
—— **55 ids / 485 字节 / md5 `bfe60fcdfbe466440213541c8f37a254`**,与 `arm_md5` 逐字节相同。
(另行核过:`test_set.md` 第 2 行**本轮仍是同一个 md5**,没漂;但**记录仍然是取值来源**,这是 W38 的教训。)

### 3.1 那台被抢占的机器把 48 局全交了

| 种子 | 实例 | SIR 码 | 终止 | 最后一局 | 交付局数 |
|---|---|---|---|---|---|
| **2740** | `i-022ceff8ec8ab5827` (2c) | **`instance-terminated-no-capacity`** | 16:18:35Z | **16:00:43Z** | **48** |
| 2877 | `i-0447598090fb1e251` (2c) | `instance-terminated-by-user` | 16:15:18Z | 16:00:25Z | 48 |
| 2896 | `i-08f01bb61ccb1e91c` (2c) | `instance-terminated-by-user` | 16:16:22Z | 16:00:13Z | 48 |
| 2914 | `i-0b0d4f773e37dcdc9` (2d) | `instance-terminated-by-user` | 16:23:18Z | 16:03:11Z | 48 |

**只读 SIR 码,本轮该写的是「第七波被咬,丢一条臂」,并考虑补跑一台。语料说的是反面**:
2740 交了 **48 局**,与三台干净机**一模一样**,而它的**最后一局落在被抢占前 17 分 52 秒**。
四台跑的是同一个 48 局程序(每台 6 暖场 + 42 计分),跑完后都在上传/收尾阶段待了约 15 分钟;
三台从那里自毁,2740 是在那里被回收的。

⇒ **不欠任何补跑,没有任何一粒种子缺臂,W39 = 4 粒 / 168 局。**

⭐ **一般结论(这才是值得写长的部分)**:**SIR 码回答的是「这台机器怎么死的」,不是「这台机器交没交货」。**
这两个问题**在这里第一次分了家**,而**只读 SIR 的失效方向是花钱那一侧**——
去补跑一粒已经完整的种子。这与 **GH #429** 给 `reclaim_blind.py` yield 计数记的失效方向**是同一个**。
**买补跑之前先看语料。**

### 3.2 读数(登记,不判读)

| 量 | mean | comps_better | ab 分层 | ba 分层 | sign_flip | side_gt_arm |
|---|---|---|---|---|---|---|
| gpm | **−12.58** | 1/4 | +103.12 | −128.30 | ✔ | 4/4 |
| xpm | **−5.78** | 1/4 | +72.00 | −83.56 | ✔ | 4/4 |
| deaths | **+0.04** | 1/4 | −1.39 | +1.46 | ✔ | 4/4 |
| last_hits | **−1.02** | 1/4 | +11.78 | −13.83 | ✔ | 4/4 |

`suggested hold_or_reject`。逐粒 gpm:**2740 −23.68**(arm_depth 10.29)/ **2877 +13.42**(18.67)/
**2896 −3.21**(17.95)/ **2914 −36.86**(17.14)。
`min_arm_depth 8`、**`thin_arm_seeds` 空**、**三个 SKIP 开关一个都没用 ⇒ 本波不欠「这是 SKIP 不是 pass」行**。

- **4(i-a)**:两个分层的**读数**都已登记(上表),**未手算** —— `per_seed[].<m>_ab/_ba` 与 `strata.<m>` 由工具自己打(4(i-d))。
- **4(i-c)**:四量全反号、`side_gt_arm 4/4`。**这是恒等式不是诊断**(反号 ⟺ `|side| > |arm|`,
  只说这一波抽到的阵容侧偏比效应大,**对 arm 测得多准零信息**)⇒ **登记,不据此下结论**。
- **4(i-d)**:并池由工具**逐粒 swap-average 后跨种子取算术平均**,**未按局数加权**。
- ⛔ **胜负通道 `DEGENERATE`**:少数侧 **8/192**(share **0.0417**),`mean.winrate_headroom` **0.0625**,
  **2740 / 2896 / 2914 三粒 headroom 恰为 0.0**。按 **SS-CT**:winrate `0.478` 与 `comps_better winrate 0/4`
  **是占位符,不得引作条件 (b)**。
- `winrate_independent_of_gold` **`192/192`**、`winner_by {engine_natural: 192}` —— 桶名 bug 的修复**第二波复核通过**。

⚠️ **ab:ba = 123:45,高度不对称**,2740 更是 **36:6**。**这不是被抢占造成的**:
A/B 换边按**经过时间**触发、不按局数,四台在同一墙钟窗口里各交 48 局,radiant 腿慢的那粒就翻得晚。
2740 的 `arm_depth 10.29` 过了 8 的门故计分,但**它是本波最薄的一条臂,要怀疑先怀疑它**。

### 3.3 三条断言全 PASS

1. 暖场 `script_version` = **`1a13b9df`**(4 run × 6 局 = 24 局全中),与 `--ref` 钉的树一致;
2. 168 计分局全打 `mirror:<55-id>:s<seed>:<side>`,**0 处不匹配**;
3. 采集配置**按语料查证**:四个 run 各自 **slot 1..16(16 个)**,每台 48 局,与 W38 逐项相同。

⚠️ **`--ref` 本波正确但不承重,据实登记**:`origin/main` 确实漂了(`1a13b9df` → `864b7d6a`),
但 **`git diff 1a13b9df..864b7d6a -- bots/ game/` 是空的** —— 被测面没动。
W38 那次它**是**承重的(`bots/` 真的动了)。**没赔付的保险和赔付过的保险不是同一个主张。**

## 4. ⭐⭐ 只有被抢占那台产出了 31 个重名副本(本轮开 issue)

`724bad`(2740)多出 **31 个** `analysis_<TS>_slot<N>.json` —— 与正常的
`<TS>_slot<N>.analysis.json` **镜像对称的第二套命名**;另三台**一个都没有**。

本轮查证:31 个**全部**能对上一个已计数的 `(时间戳, slot)` 对(**overlap 31/31**),
且 31 个**逐字节相同**(md5)⇒ 是**重复上传,不是漏掉的局**,几乎必然是被抢占时
shutdown 钩子把已上传队列又排了一遍。

- **对本次收割零影响**:`recover_verdict.py` 匹配的是 `*.analysis.json`,第二套命名匹配不上,
  所以它 `192 seen / 192 loaded`,**一局都没重复计**。
- ⛔ **但隐患是真的,而且指向花钱那一侧**:任何用 `*analysis*.json`(**更松也更自然**的写法)
  的消费者都会把 2740 的 31 局**数两遍** —— 灌水的正是**本波最薄的那条臂**,
  且方向是**让读数看起来比实际好**。**没有任何一道门会举手**:工具两种情况下都打 `files_seen == games_loaded`。
- **本台不自己改上传脚本**,已开 `[harness]` issue(见 §7)。

## 5. 本轮不发波(闸 (i) 不满足)

- **(i) 距上一次例行波次 ≥ 6h:不满足。** W39 起飞于 `2026-09-02T15:31:51Z` ⇒ 解锁于 **`2026-09-02T21:31:51Z`**;
  本轮结束时约 `18:4xZ`,**还差约 2h45m**。**不发波,不空转等待**(铁律 7/11)。
- (ii) 有新东西可测:`test_set.md` 第 2 行**仍是 `bfe60fcd…` / 55 ids**,自 W39 起飞未动;
  `bots/` 自 `1a13b9df` 起未动 ⇒ **第一支不成立**;第三支(累计种子 < 8)**成立**(55-id 家族现 4 粒)。
- (iii) 成本围栏:过(§2)。
- ⇒ **卡在 (i),不是卡在钱,也不是卡在没东西测。** 下一轮本台在 `21:31:51Z` 之后可发 W40。

## 6. 泄漏检查:CLEAN

`check_costs.sh --leak-only` ⇒ **`LEAK_EXIT=0`**;`pending,running` 的 `soak-run` 普查**空**;
`describe-volumes` **空**(无孤儿卷)。常驻仅 AMI。

- ⚠️ **`terminated,shutting-down` 普查也是空的,而那个空是 GH #375 的窗口过期,不是「没有东西终止过」**
  —— 四台确实在 16:1x–16:2xZ 终止了,SIR 通道作证。⇒ 四台的 `ec2_state_reason_code`
  记 **`UNRECOVERABLE`**,**不是「null 因为干净」而是「null 因为窗口关了」**(这两件事在 JSON 里长得一模一样)。
  **本波全是 spot,SIR 保住了 `status_code`;换成按需机就什么都不剩** —— 这正是 **GH #427** 的立案句,**第二个数据点**。
- ⚠️ 再登记一次:`--leak-only` **只报「多出来的」,「少一台」它永远不举手**。

## 7. 交棒

| # | 给谁 | 事 |
|---|---|---|
| ① ⭐⭐⭐ | **全体 / 总监** | **「被抢占」不等于「丢臂」**:W39 的 2740 带 `instance-terminated-no-capacity` 却交满 48 局。**买补跑之前先查语料**(§3.1)。与 **GH #429** 同一失效方向。 |
| ② ⭐⭐⭐ | **总监** | **GH #432(本轮新开)**:被抢占机产出 31 个逐字节重复的第二套命名副本;`*analysis*.json` 这种自然写法会把最薄的臂数两遍,**没有门会举手**。 |
| ③ ⭐⭐ | **总监** | **GH #427 第二个数据点**:W39 四台的 `ec2_state_reason_code` 全部 `UNRECOVERABLE`(窗口过期),**靠 SIR 才留住 `status_code`**。按需路径没有这条通道。 |
| ④ ⭐⭐ | **总监** | **GH #408 连续第七波被容量咬**(W33/W35/W36/W37/W38 回收 4/3/4/2/1;W39 起飞三次 `InsufficientInstanceCapacity` + 飞行中 1 台被回收)。**仍零评论未裁。** |
| ⑤ ⭐⭐ | **总监** | **GH #428**(AZ 环走确定性单向)**本波已兑现风险的一半**:被回收的恰是 3 台挤在 `2c` 里的一台。 |
| ⑥ ⭐⭐ | **总监/英雄组** | **GH #420 第三轮原样复发**(python 腿自打 `2 uncertifiable` 并点名,汇总块打 `none`);`test_detector_source_constants.py` 仍红。**Lua 腿本轮 0 failures,43→44 那条已消失。** |
| ⑦ ⭐ | **总监/协同组** | `strategy-31/32` **连续第四轮不搭任何波**,按铁律 9 第四次交回。本轮回填时它俩一度被字符串匹配误伤(正文含 "W39 也没搭"),**已当场撤回** —— 那正是「读数看起来合理、没有门会举手」的静默污染形状。 |
| ⑧ ⭐ | **录像组** | **GH #423** 仍挂着:SS-DK.3 负控本语料**结构上答不了**(逐局 `analysis.json` 无 mode 状态/营地决策字段),`slotarb` 的条件 (a) **再发波也买不到**。 |
| ⑨ ⭐ | 存量 | **#388 的 W34 103 份 `.dem` 2026-09-22 到期(距今 20 天)** / #432 / #429 / #418 / #420 / #413 / #412 / #402 / #395 / #375 / 按需常数第十一轮 / #317 五条 / `strategy-5b` 第十七轮 / #290 / #313 / #207 / #271 / #364 / #398 / #399 / #382 / #383 / #218 / #367 / #295 / #329 / #321 / **#285 第二十六轮未裁**。 |

## 8. 收割义务逐条核对(`W39_wave.json:harvest_obligations` 十二条)

| 义务 | 本轮 |
|---|---|
| 4(i-a) 两分层都登记、**不手算** | ✔ §3.2 表,工具自打 |
| 4(i-d) 不按局数加权 | ✔ 工具逐粒 swap-average |
| `winrate_channel` + `headroom` 照抄;DEGENERATE 不写成读数 | ✔ §3.2 |
| **SS-DN.6**:`outlatch` (a) 两分层分开、反号按 **4(i-b)** 读成噪声 | **本轮不适用** —— (a) 是帧级检测器读数,**不在本语料里**(与 SS-DK.3 同因)。经济四量按 4(i-c),**两者没混** |
| SS-DN.6:`outlatch` (a) 不许跨 54/55 波比 | ✔ 未做任何跨波比较 |
| SS-DK.3 负控不可买 | ✔ 未从本波读「更少碰撞」 |
| SS-DK.1 (v-b) `slotdust` 不是子集 | ✔ 未按 SS-DI 模式匹配 |
| SS-DK.5 不许并成一个 `0SLOT9` 侧别数 | ✔ 未并 |
| 不许与 W38 / W35 / W37 并池 | ✔ **一次都没并** |
| 本波零 pudge 载体,**不许写成负向读数** | ✔ 未写 |
| GH #412 `status_code` 走 SIR 词汇 | ✔ `machines[].status_code` = SIR 码;EC2 值另记 `ec2_state_reason_code` |
| `rerun_evidence_window`(若需按需补跑,~1h 内落盘) | **不适用 —— 本波不欠补跑**(§3.1) |

## 9. 铁律 6

`bots/` / `game/` **一行未改**(改动全在 `iterations/`)。
静态半 `bash tools/agent/luacheck_gate.sh` ⇒ **`GATE_EXIT=0 CLEAN`**(`luacheck bots game: 0 warnings`;
容器冷启,脚本**自己把 luacheck 装上**——apt 包名是 `lua-check`)。
`arm_push_gate.sh` ⇒ `ARM_EXIT=0`(`core.hooksPath=.githooks` 已上膛),
**两次 `git push` 各自又跑一次同一道门**。
**未用 `RULE6_BYPASS` ⇒ 本轮无「SKIPPED, not passed」行。**
动态半(`tests/run_tests.lua`,~100min,GH #124)**未跑,不声称**。
另跑 `tests/test_wave_gate_keys.py` ⇒ **exit 0 / `151 checks, 0 failed`**(回填后复跑仍 0)。

**铁律 11**:全程未触发 `requires approval`;**无空转等待**(闸 (i) 差 2h45m,本轮直接不发波并结束,
没有 `sleep` 守到解锁)。

## 10. Token 用量

见下方。

```
TOKENS total_in=4,501,553 out=42,416 turns=37
```

(到统计时刻为止;统计后的收尾回合——commit / push——不计入。)
