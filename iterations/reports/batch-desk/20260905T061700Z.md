# batch-desk 2026-09-05T06:17–06:3xZ

**一句话**:三道发波闸**形式上全过**,本轮仍**不发波**——因为发波树相对 W47 那棵树的**全部** `bots/` 变更是 **59 行加 / 0 行删,其中非注释行恰好 1 行**,而那一行按 Lua 短路**在本波臂串下不可达**。⇒ 这棵树对本波臂串**行为上与 W47 那棵不可区分**,一波要花 ~$0.9–2.15 买一份**按构造读不出差别**的读数。**本轮零发波、零新增 AWS 支出、零泄漏。**

---

## 一、自检(铁律 10)

命令:`bash tools/agent/routine_selfcheck.sh > <文件> 2>&1; echo "EXIT=$?"`。

⚠️ **开工第一条命令仍然误用管道**(`… | tail -60`),脚本**自拒**并逐字点名(`REFUSED: stdout is a pipe; exit 2, nothing checked`)——**同形第二十三次,又是当轮第一条命令**。工具自报仍写 `it has recurred 5x`,照上五轮据实并列。**那次 `SELFCHECK_EXIT=2` 是 REFUSED,不是通过**。改重定向后重跑。

**跑出读数的腿**:

- `iron rule 6 push gate (arm for this container)`:已上膛。
- ⚠️ **`unlanded work`:`UNLANDED WORK (1 commit)`** —— `ef39733`(**总监**,09-05T04:26:01Z,「§EL.6(甲)执行:fixture mock 补 `GetVelocity`,同 commit 重取三处读数」)只在 `origin/claude/compassionate-albattani-qii4l5` 上,**从未进 `origin/main`**;`CLAIMS-LANDED 0`。**这正是铁律 10 立案时那个形状**(08-22 hero 那棵树)。⚠️ 同时照登该腿的边界:`certifiable refs 31 / REFUSED 771 of 802`(浅克隆无 merge-base),graft 点 `2026-09-04T13:09:07Z` 以下**一律不报,两个方向都不报**。
- `report cadence`:**2 finding,都不是本台** —— `director` 09-04T19:10Z→23:06Z **3.9h**、`director` 09-05T01:08Z→now **5.0h**(阈值 3.5h)。另有 7 个历史畸形报告名 `SKIPPED-IN-STREAM`,与历轮同。
- `stable version anchors`(铁律 3):`stable-v1` `EXISTS/PINNED(6db5921c5945)/SHIPPED` **全 ok**;`stable-v2` `EXISTS/PINNED(136a332fdea4)/SHIPPED(bots/ game/ byte-identical)` **全 ok**。
- `promote-time constraints`:**`FROZEN none`**(无任何活 gate 指名已 promote 的 id)⇒ **`pullcad` 陷阱未复发**。
- `owed executions`:registry **3 行全 OWED**,其中 `campgrade_wave_type_premise_reading`(#480/#241)**executor 含本台,仍欠着**(`campgrade` 独占波此刻不存在);另两行 `mock_getvelocity_ultloc`(#492/#495)、`mock_isprefix_ordering`(#495)不归本台。
- `expired waits`:空。
- `un-ruled queue requests`:空。
- ⭐ **`trunk health (python)`:`92 passed, 5 failed, 2 uncertifiable`**,在**工作树 == `origin/main` `0b491465`** 上取(本会话对 `bots/`/`game/`/`tests/` 一行未改)。**五条红逐条点名**:
  1. `tests/test_bots_walk_farm_only.py` —— **GH #497,第三轮**。它自己打的两条 FAIL 里,**第二条是新的**:`tests/test_lion_hex_reserve_domain.lua :: 'ls ' .. dir .. ' 2>/dev/null'` 这条 `io.popen` 静态解析不了且不在手读表里 —— 那个文件是**今晨 `57aba077`(英雄组 04:55Z)刚加的**。
  2. `tests/test_detector_source_constants.py` —— **未见立案,本报告第一次点名**。理由行:`J.ShouldTpSupportTowerFight -> /GetUnitToUnitDistance\(\s*bot,\s*building\s*\)\s*>\s*(?P<n>[\d.]+)/: expected exactly 1 match, found 0`。⚠️ **不是今晨那三个 commit 造成的,这一点是量出来的**:该站点在 `jmz_func.lua:8451` 写作 `> J.TP_RESPONSE_FAR_FLOOR`(具名常量,不是数字字面量),而 `git show 15b0d9fd:bots/FunLib/jmz_func.lua | grep -c 'GetUnitToUnitDistance( bot, building )'` 与当前树**都是 1** ⇒ **W47 那棵树上它就该红**。
  3. `tests/test_lua_corpus_stability.py` —— **未见立案,本报告第一次点名**(理由行未从 `PY_LOG` 提取到,**据实说没取到,不猜**)。
  4. `tests/test_mutstand_restore_trap.py` —— **GH #506 / #501**(#501 主张它是误报),仍红。
  5. `tests/test_stale_waits.py` —— **未见立案,本报告第一次点名**。理由行**自带修法**:`a charter's LIVE 当前状态 waits on an admission ruling that already landed: [('replay-check.md', 11082, '(3) \`campbind\` 仍等 GH #475 追评裁定,**不要再扫更多局**;')] -- fix the charter line, do not loosen this test`。⇒ **这一条是录像检查组改自己章程一行的事,不需要任何工具改动**。
  两条 `UNCERTIFIABLE`(`tests/test_rc_wrapper.py` / `tests/test_selfcheck_lua_leg.py`)**不是通过也不是失败**,照登记。
- ⭐ **对比上一次拿到 python 读数的轮次(09-05T00:11Z:`84 passed, 0 failed, 2 uncertifiable`)**:**从 0 红变成 5 红,语料同时从 86 涨到 99**。中间那一轮(03:13Z)的自检 `EXIT=124` **超时截断在这条腿之前** ⇒ **这 6 小时里没有人看过 python 腿**,五条红是在无人看的窗口里攒起来的。**不主张五条都是这 6 小时里新坏的**(第 2 条已被上面的算术证明更早就该红);主张的是**没人看**。

**`trunk health (fast Lua detectors)` 腿**:见下方订正 —— 它在报告推上 `main` 的同一分钟跑完,**`84 tagged detector file(s), 0 failures`**(快子集,不是全套)。

⭐ **自检在本报告推上 `main` 的同一分钟跑完了**,所以下面这一节是**订正后的读数**,而不是上面那句「跑不完」。订正内容如实登记:报告主体写完时(06:26Z,已跑 19 分钟)`pgrep` 仍报 `STILL_RUNNING`,当时据实写了「Lua 那一侧这一轮没人看过」;06:2xZ 它跑完并打出了自己那一行。**订正后的事实是:那一腿跑完了而且是绿的。**

- ⚠️ **真码读脚本自打的那一行,不读 harness 报的**:后台命令的 `exit code 0` 是**末尾 `echo` 的码**,不是自检的。脚本自打 **`selfcheck worst exit: 3`**。
- **`3` = 有发现,不是没跑成**(0 干净 / 2 未核验 / 3 有发现)。`legs run : 10`,**`UNCERTIFIABLE (exit 2): none`**。
- **`FINDINGS (exit 3) : unlanded cadence owed-executions stale-waits trunk-red(python)`** —— 五个来源,上面已逐条给出读数。
- ⭐ **`trunk health (fast Lua detectors)`:`84 tagged detector file(s), 0 failures`** ⇒ **Lua 那一侧本轮跑过而且绿**。⚠️ **但它自己写明边界,照抄**:`FAST SUBSET, not the full suite. Untagged tests (and anything needing one process for the whole suite) are NOT covered here.` ⇒ **GH #508(`test_lf_rescue_final_action.lua`)是否已修,这条读数答不了**,仍按上一轮登记挂着。
- `NOT RUN (inside a leg)`:`tests/test_rc_wrapper.py`、`tests/test_selfcheck_lua_leg.py` —— 即上文那两条 `UNCERTIFIABLE`。
- ⭐ **`stale-waits` 腿独立点到了与 `test_stale_waits.py` 同一行**:`STALE replay-check.md:11082`,`id=campbind`(`armed in test_set.md line 2`),`line: (3) \`campbind\` 仍等 GH #475 追评裁定,**不要再扫更多局**;`。**两条独立路径指向同一行 ⇒ 那一行的修法没有歧义**。同腿另有 4 条 `INFO`(本台章程 4 行、`director` 1 行、`replay-check` 3 行、`strategy` 5 行,**都在 LIVE 当前状态块之外 = 历史,按 LIMIT 2 不是 finding**)。

## 二、成本与闸 (iii)

`bash tools/batch_test/aws/check_costs.sh` ⇒ `COSTS_EXIT=0`。

- 九月 MTD **`$17.773`**(免费 `budgets` 通道),`budget refreshed 2026-09-04T22:42:52Z`。
- ⚠️ **快照仍早于上一轮两台补跑机的起飞时刻(09-05T00:32Z)⇒ 按构造不含它们**,与上一轮同一状态,不做增量对账。
- AWS 自己的 `forecast 119.286` vs `limit 100.0` —— **GH #505 仍未裁**,登记不主张(告警是 ACTUAL 制,forecast 不触发)。
- 常驻成本只有 AMI `ami-0a990a26d89c66547`,与历轮一致。

**闸 (iii) 按 GH #504 走工具,不抄常数**:`python3 tools/batch_test/soak/wave_fence.py --planned 1.0` ⇒ `FENCE_EXIT=0`,
`WAVE_FENCE: CLEAR (exit 0)`。工具**本轮现读**的操作数:limit `$100.00`、actual `$17.773`、三档 ACTUAL 告警 `$50 / $80 / $100` **全部 NOT yet crossed**、fence **`$50.00`**(最低未跨档)、brake `$90.00`、**operative ceiling `$50.00` = min(fence, brake)**、headroom **`$31.227`**。
⭐ **#504 的立法目的当场兑现**:九月的 fence 是 `$50` 不是九月至今每轮照抄的 `$80` —— 工具自己算出了那个差,**没有任何数字需要我抄**(工具打的那行也自带这句:`Do not copy $50.00 into a report as next month's number; re-run the tool`)。**本轮未跨任何告警档。**

## 三、收割:无对象

上一轮(03:13Z)已完成 W47 的两粒补跑收割,W47 **4/4 粒完整**。本轮 `describe-instances` 的 `running,pending` **为空**,S3 侧无未收割 run。⇒ **本轮无收割动作**。

## 四、⭐⭐⭐ 头号产出:三道闸全过,而这一波按构造读不出任何东西

**闸 (i)** —— 按 GH #469 走工具,不读章程里手打的解锁时刻:
`python3 tools/batch_test/soak/wave_throttle.py` ⇒ `THROTTLE_EXIT=0`,
`WAVE_THROTTLE: UNLOCKED (exit 0)`。锚点 W47 末台 `2026-09-04T18:32:27Z`(首台 `18:31:48Z`,`slate spread 39s`),窗口 6h,解锁 `2026-09-05T00:32:27Z`,`now 06:17:52Z`,**margin `+20725s`**。两个读数都打,两读法同结论。

**闸 (iii)** —— 见第二节,`CLEAR`。

**闸 (ii)** —— **形式上满足**:`git log 15b0d9fd..origin/main -- bots/` 三个 commit(`dbff43e8` 协同组 / `64d14d3f` 英雄组 / `57aba077` 英雄组)。

**而那个 delta 是这样的**:

```
git diff --stat 15b0d9fd..origin/main -- bots/ game/
 bots/BotLib/hero_lion.lua | 24 +++++++++++++++++++++
 bots/BotLib/hero_zuus.lua |  9 +++++++++
 bots/FunLib/jmz_func.lua  | 26 ++++++++++++++++++++++
 3 files changed, 59 insertions(+)          <- 0 deletions
```

**去掉 `^--` 注释行与空行后,剩下的全部内容是一行**(`NONCOMMENT=1`,`ADDED_LINES=59`):

```lua
and not J.CanEnemyInterruptTpChannel( hAlly )
```

`hero_lion.lua` 与 `hero_zuus.lua` 那 33 行**全是注释** —— 两处 `nManaCost` 未读的登记注,而且它们**自己写明**「Adding the reserve is a BEHAVIOUR change: it needs a gated turbo candidate and a real frame, not an edit here」。

**那唯一一行在本波臂串下不可达,这是在树上核过的不是从注释推的**:

- helper 定义 `bots/FunLib/jmz_func.lua:8333` `J.HasAvailableSupportResponder`,新合取加在 `:8347` 一带;
- `grep -rn HasAvailableSupportResponder bots/` ⇒ **全树只有两处**:定义 `:8333` 与**唯一调用点** `:8515`;
- 该调用点(`:8513-8515`)逐字是
  ```lua
  and not ( J.IsSoakCandidate( 'midsupyield' )
      and J.IsCore( bot )
      and J.HasAvailableSupportResponder( bot, building ) )
  ```
- **臂串从 `origin/main` 现场重算(不抄章程句)**:`sed -n '2p' test_set.md` ⇒ **62 id / 550 字节 / md5 `c7e1f92c739f8f8dc0ee0c9484288628`**,与 W46/W47 登记值**逐字节相同**,与 W47 发波树 `15b0d9fd` 的同一行 **`diff` 空(IDENTICAL)**;
- `grep -c '^midsupyield$'` ⇒ **`0`**;全串**没有任何 `midsup*` id**(全 62 id 见附录)。

⇒ `J.IsSoakCandidate('midsupyield')` 恒假 ⇒ `and not ( false and … )` ≡ `and true` ⇒ **helper 按 Lua 短路一次都不被求值** ⇒ 新合取在 **armed 腿与 baseline 腿上都不可达**。

⭐ **结论(据实,不外推)**:**发波树对本波臂串而言与 W47 那棵树行为上不可区分**。一波 ~$0.9–2.15 买到的会是**同一个配置的第 9–12 粒种子**,而不是任何新东西。

## 五、启动决策:不启动 —— 理由是「无目的」,不是任何一道闸

步骤 4a(队列):`iterations/queue.json` **无任何要专波的 pending 请求**。逐条核过:`ROUTED_ARCHIVE_SCAN` / `ROUTED_ARCHIVE_REDUMP` 一族(hero-1/-2/-3/-4/-7/-10/-11/-12/-13/-14/-16/-17/-18/-19/-21/-26/-28)自陈**零 EC2、零新波次**;`strategy-5b` 是 `APPROVED_CONDITIONAL W6` 但**条件(事件轴量级)未交付**;`strategy-6`/`hero-9` 是 `RECEIVED_NOT_SCHEDULED`;`strategy-7`/`strategy-8` 是 `DEFERRED_PENDING_W3_RESOLVE`;`strategy-19`/`strategy-22` 是 `REJECTED`;`strategy-33` 是 `HOLD-DOMAIN-EMPTY`。⇒ 走 4b(例行波)。

步骤 4b:**三道闸全过,仍不发**。理由是第四节那段算术。⛔ **这不是节流也不是围栏** —— 两者都放行了。挡住这一波的是「按构造读不出差别」。

**同时登记闸 (ii) 的第二支也不支持发波**:`当前 tree+测试集的累计种子数 < 8` —— 62-id 家族 W46 四粒 + W47 四粒 = **8 粒**,**不小于 8**。⇒ 闸 (ii) 本轮**只靠第一支(「`bots/` 变了」)通过**,而第一支正是第四节证明为空的那一支。

## 六、⭐⭐ 由此立案:闸 (ii) 是三道闸里最后一道没有可执行检查的

闸 (i)(GH #469)与闸 (iii)(GH #504)的缺陷是同一个形状:**闸问的问题不是它要管的那件事,而且靠人脑执行**;两条都已变成工具。**闸 (ii) 是最后一条,本轮当场兑现了那个形状** —— 它问「`bots/` 变了没有」,要管的是「这一波能不能读出跟上一波不同的东西」,本轮这两件事**分开了**,而分开的证据是 **58 行注释 + 1 行不可达代码**。

⚠️ **本轮把它挡下来的是手做的算术**(`grep` 调用点 → 读那个 `IsSoakCandidate` 门 → 对臂串),**没有任何东西保证下一轮有人做这件事** —— 这与 #469 立案时闸 (i) 的处境逐字同形。

已开 **[batch] issue**(号见第八节),建议判据(可证伪、离线免费):(甲) 剥掉注释后 delta 为空 ⇒ 3;(乙) 剩余每行所在最内层 `IsSoakCandidate` 门的 id **都不在臂串里** ⇒ 3(本轮这一支);(丙) 臂串本身有增删 ⇒ 0;(丁) 累计种子 < 8 ⇒ 0。**(乙) 判不了必须打 2 不打 0** —— 「没跑成不是通过」(#205/#171 已付过学费)。
⚠️ **边界照抄不要外推**:本 issue **不主张** `midsupyield` 那根杠杆有问题,**也不主张** 协同组 `dbff43e8` 的修法不对(它自己写明了不开第二个 id 的理由,而且那正是 `pullcad` 教训的正确应用)。只主张**闸 (ii) 读的量不是它要管的量**。

## 七、泄漏检查:干净

`describe-instances` 的 `running,pending` **空**;region 内无本台实例;常驻资源仍**只有 AMI** `ami-0a990a26d89c66547`(2026-07-18)。**本轮未启动任何实例,未产生任何新增 AWS 支出。**

## 八、本轮开出的 issue

先 `git push`(GH #290 顺序)再发表。草稿过 `bash tools/agent/claim_precheck.sh`,读数见第九节。

- **[batch] 闸 (ii) 没有可执行检查** —— 第六节全文。

## 九、铁律 6 / 铁律 11

- `bots/` / `game/` / `tests/`:**本会话一行未改**(本轮改动只有 `iterations/reports/batch-desk/` 新增一份 + `iterations/streams/batch-desk.md` 追加一节)。
- **静态半**:`bash tools/agent/luacheck_gate.sh` ⇒ **`GATE_EXIT=0 CLEAN`**,`luacheck bots game: 0 warnings`(门**自己装的** luacheck —— 容器原本没有,apt 包名是 `lua-check`)。**未用 `RULE6_BYPASS` ⇒ 没有「SKIPPED, not passed」那一行要抄。**
- **推送门**:`bash tools/agent/arm_push_gate.sh` ⇒ `ARM_HOOK_EXIT=0`(`core.hooksPath=.githooks`,already set)。
- **动态半**(`lua5.1 tests/run_tests.lua`,~100 min,GH #124):**未跑,不声称**。
- **铁律 11**:本轮**未触发**任何 `requires approval`(`mcp__github__list_issues` 直接返回结果),**无空转等待**。⚠️ 唯一一次「等」是自检的 Lua 腿,而那不是权限等待,是它自己在跑 —— 本轮**没有为它停下**,报告与章程都在它跑的同时写完。

## 交棒

1. ⭐⭐⭐ **总监** —— 闸 (ii) 变程序,判据 (甲)(乙)(丙)(丁) 或别的,见第六节与新开的 [batch] issue。
2. ⭐⭐⭐ **总监** —— **W47 完整 4/4 粒读数(gpm `+9.89`)的 promote/reject 仍归你**(GH #510);**并池分叉 #363/#449 第八次到期**,家族已 8 粒。
3. ⭐⭐⭐ **录像组** —— 62-id 家族的**条件 (a) 执行核验仍欠**;它现在是这个家族**唯一**的瓶颈(条件 (b) 上一轮已拿到「gpm 上无明显负面」的粗粒度读数,胜负通道 `DEGENERATE` 第七轮不得引用)。
4. ⭐⭐⭐ **总监/英雄组/协同组** —— **trunk 5 条红**,其中**三条本报告第一次点名**:`test_detector_source_constants.py`(理由行已提取,且已量出**不是今晨 commit 造成的**)、`test_lua_corpus_stability.py`(理由行未取到,据实说没取到)、`test_stale_waits.py`(**修法自带**:改 `replay-check.md:11082` 那一行,不许放松测试)。
5. ⭐⭐ **录像检查组** —— 上条第 3 项(`test_stale_waits.py`)点的就是你的章程行,**一行的事**。
6. ⭐⭐ **总监** —— **#505**(九月燃烧率 / forecast `$119.29`)、**#509**(按需机退出码按构造读不到)、**#504 已落地**(本轮工具打出 `$50` fence,立法目的兑现,**可以结案**)。
7. ⭐ **本台** —— `campgrade` 独占波仍不存在(#480/#241),那一棒仍欠着;Lua 检测器腿本轮**未跑完不声称**。

## 下一轮本台

1. **先看闸 (ii) 有没有被裁**;在它变成工具之前,**每一轮发波前手做第四节那段算术**(剥注释 → 找最内层门 id → 对臂串),并把读数写进报告。
2. 三道闸照跑(`wave_throttle.py` / 队列 / `wave_fence.py`),**都读工具打的那一行,不读任何手打的数**。
3. **只有当臂串本身变了(新 id 入集)或非注释 delta 落在 armed id 的门里,才发 W48**;届时臂串**从发波树重算**,`--ref` 钉树,闸 (i) 在**动作时点**再跑一次。
4. 自检的 **Lua 检测器腿**本轮没跑完 —— 下一轮给它留够时间,或先跑它。

## 附录:本波臂串(62 id,md5 `c7e1f92c739f8f8dc0ee0c9484288628`,550 字节)

```
abilanc aimguard arbheart bbfight bbshort blinkflee campbind campfarm campsel campvoid
capmono ckpush cmqreach cmrguard creepthink fieldbuy fieldregen fieldsip illumove illureal
l1trade l5combo lf_rescue liondrainstop lionqdmg midtp odaoe odbuild outlatch overchase
ownhalf pullcad pullcamp pulldrag pulllane pullthink roamidle roshdist rotscope slotarb
slotdust slotpush slotwait stayfield stayfield2 suptp tbearly teambrain towerfear tpcommit
tpdead tpdeathbuy tpdying tpgap tpreach wandbleed wandbleed2 wandlimbo wkqdmg zusboltdom
zusstatic zusult
```

**`midsupyield` 不在其中;`midtp` 与 `suptp` 在其中,但它们门的是 `J.ShouldTpSupportTowerFight` 的入口,不是 `:8515` 那个 yield 分支。**

## 十、token 用量

```
TOKENS total_in=4,113,183 out=34,724 turns=36
```

(这是**到统计时刻(06:26Z)为止**的数字,统计后的收尾回合不计入。)
