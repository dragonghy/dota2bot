# batch-desk 2026-09-16T03:15:38Z — 刹车第三十轮持有;交棒 ⑥ 的「一个对象就能收口」**收不了口,而收不了口的原因本身是本轮的交付**

**一句话**:预算快照**解冻了**(四轮来第二次有新支出测量,`+$0.221 / 11.27h = $0.47/天`),刹车仍在;
实质交付是上一轮交棒 ⑥ —— 结论是 **(甲)/(乙) 分不开,因为分开它们的那条记录在正常跑完的波上从不上传**,
而这条「从不上传」是一条有据可查的 harness 缺陷(已开 GH issue,见 §六)。
零发波、零收割欠、零泄漏,⛔ `bots`/`game`/`tests`/`tools` 一行未改。

---

## 一、成本(当轮现跑,⛔ 下轮不得抄作前提)

`AWS_SETUP_EXIT=0`(容器无 CLI,自装 `aws-cli/1.46.1 Python/3.11.15 botocore/1.43.62`;
身份逐字 `(restricted batch-runner user — correct)`);`COST_EXIT=0`(⛔ 未走管道)。

```
91.003	USD
   source: budgets/dota2bot-batch (free) | forecast 102.229 | budget limit 100.0 | budget refreshed 2026-09-16T01:56:47Z
```

**headroom 对 `$90` = `$-1.003`**。

### 1.1 ⭐⭐ 快照解冻了 —— 这是本轮第一件事实

上四轮(15:20Z / 18:06Z / 21:08Z / 00:16Z)读的是同一张冻结快照(`$90.782` / 戳 `2026-09-15T14:40:18Z`)。
本轮两量**都动了**:

| 量 | 上一轮(00:16Z) | 本轮 | 差 |
|---|---|---|---|
| MTD | `$90.782` | **`$91.003`** | **+`$0.221`** |
| 预算戳 | `2026-09-15T14:40:18Z` | **`2026-09-16T01:56:47Z`** | **+11.274h** |
| forecast | `186.702` | **`102.229`** | **−84.47** |

⇒ **折算 `$0.221 / 11.274h × 24 = `$0.47/天`**,落在已登记的 **`$0.40–0.92/天`** 区间内。
⭐ **空转日样本由 n=3 变 n=4**(09-13 `$0.9185` / 09-14 `$0.3997` / 09-15 `$0.213` / 本点 `$0.47`),
⛔ **但不据这一个点收窄那个区间**(章程明禁)。

⚠️ **`forecast` 掉了 84.47,⛔ 本台不解释也不采信**。它是 AWS 自己的外推,
口径不在本台手上;它**恰好**落进本台自己的月末区间(见 §七)这件事**是巧合不是互证**
—— 📌 与铁律 4 §CL (i-a) 同型:两个数碰在一起不等于一个数验证了另一个。

### 1.2 GH #801 四行整块照抄(⛔ 未用 `COST_CONFIRM_AT` 自救,用默认值跑)

```
   >= $35, but headroom to the $90.00 brake is $-1.003
   < $1.10 (cheapest wave) — CE confirmation SKIPPED, NOT passed (GH #801).
   Nothing can launch at this MTD, so confirming it buys nothing.
   Resumes by itself as soon as headroom >= $1.10.
```

⚠️ **这是 SKIP 不是 pass**;⛔ 欠条 `gh801_confirm_headroom_first_live_read` **仍未结清**
(headroom 仍为负,走的还是 SKIP 分支)。

accrual probe 两行照抄:

```
   accrual probe (GH #779): alpha = 0 accruing instance(s) account-wide; beta = 0 wave record(s) after 2026-09-15T14:38:47Z
   stamp unknown (fresh container) -- alpha and beta are both zero -- a frozen stamp over an idle account opens no blind spot
```

⭐ **accrual epoch 本轮跟着快照一起前移了**(`2026-09-15T03:22:18Z` → `2026-09-15T14:38:47Z`)
⇒ 上两轮登记的那条耦合(「快照冻住时 accrual 窗口也跟着冻住」)**本轮方向相反地复现了一次**:
窗口跟着戳走,**这是同一条耦合的正面,不是它被修好了**。

### 1.3 成本三段式(RULING 48)

**零 EC2 / 零 CE / S3 读取 1 个对象(出网未计价)**

⚠️ **诚实边界照登**:本轮另发 **3 次 LIST 请求**(`validation/ --recursive` 1 次、
`soak/` 非递归 1 次、`soak/spot_20260912_092622_1_main_896712/` 1 次),
**LIST 是没有被 `<N>` 计量的 S3 请求**;⛔ 不写成零、⛔ 不冒充 `<N>`。
⭐ `soak/` 本轮**故意不递归**(该前缀 72k+ 对象 ⇒ 递归约 73 个 LIST);
非递归只要 1 个请求就够回答本轮的问题。

---

## 二、闸 (iii)(`--planned 1.10`):`FENCE_EXIT=3`

```
actual (MTD)     : $91.003   <- re-read this run, never cached
pending waves    : $0.000   (launched, may not be in MTD yet)
planned          : $1.100
projected total  : $92.103
fence            : $100.00
brake            : $90.00   (owner's line, not derived here)
operative ceiling: $90.00   = min(fence, brake)
WAVE_FENCE: THROTTLED (exit 3) -- brake, not fence. Stop and report to the owner; do not wait it out.
```

RULING 5/6/7 三行照抄:

```
accrual scope    : 17 region(s) read [...]  <- COMPLETE: every region this account has enabled
wave accrual     : records after 2026-09-15T14:38:47Z (= 2026-09-16T01:56:47Z - 11.3h lag, clock from budget snapshot)
accrual check    : CERTIFIED (0 accruing instances account-wide, read this run) -- pending $0.000 is a reading, not a default.
```

⭐ **钟没有降级** ⇒ RULING 7 (甲) 仍有效。
⛔ 未传任何 `--no-*`/`--pending`/`--director-crossing` ⇒ **无一行「这是 SKIP 不是 pass」**。
⚠️ 照登不藏:`$90`–`$100` 在 AWS 侧**仍可花**(`$100` 告警 `state=OK`),挡住它的是 owner 这条线不是 AWS。

⇒ **连续第三十轮,不发波的原因是闸不是本台的处置。**

---

## 三、收割:零欠(⛔ 不是漏收)

`S3VAL_EXIT=0`,`validation/` **`OBJ_COUNT=592`** 与已登记基线**逐位相同**。
最新对象按 **`LastModified` 排序**(⛔ 不按 key 名)读作:

```
2026-09-12 10:18:26      15187 validation/lf_rescue+27ids-d177b87026dd_20260912_1018_run.log
```

⇒ **仍是 W69,距今 4.7 天零新对象**。`recover_verdict.py` **未跑 ——「没得收」不是「漏收」**。
⚠️ 欠条 `games_ledger_cross_wave_accounting` 仍 OWED(`--ledger` 至今未被任何真波行使)。

`queue.json`:140 条,**65 条 `pending`**,⛔ 但**没有一条能在本轮执行**
(要么归口别流,要么要发波而闸是 `exit 3`),⛔ 本台未改任何一条 `status`。

---

## 四、⭐⭐⭐ 实质交付:上一轮交棒 ⑥ —— 答案是「这个对象回答不了」,而**为什么回答不了**才是交付

上一轮 §4.5 留了一个开放观测:W69 的暖场局 `script_version` 是 `52ea2be4`,
而该 commit **从未在 main 上**(只在 `origin/claude/admiring-hawking-091iyr`)。
两个都活着的解释:**(甲)** 实例真的 clone 了非 main 的树;**(乙)** stamp 记的不是 clone 出来的 tip。
交棒 ⑥ 逐字说:「W69 的 `run.log` 已在 S3,取 **1 个对象**就能把 (甲)/(乙) 分开。」

### 4.1 那个对象取了,`CP_EXIT=0` —— 而它里面**一行 git 都没有**

```
CP_EXIT=0        15187 bytes
grep -niE "clone|git|commit|branch|HEAD|sha|origin"  ⇒  零命中
```

该 `run.log` 是 **`validate_onspot.sh` 的逐分钟波次进度日志**,第一行是
`===== lf_rescue,...,arbheart seed=13052 wave RADIANT =====`,起点 **09:28**
—— 而 clone 发生在 **09:26 之前**。⇒ **它在时间上就晚于要查的那件事。**
⛔ **「grep 零命中」在这里不是「实例没 clone」,是「这个对象不记这件事」** —— 两句不同。

### 4.2 stamp 的出处查清了(⛔ 这一半是确定的)

```
tools/batch_test/soak/analyze_log.py:146   "script_version": os.environ.get("SOAK_SCRIPT_VERSION", "unknown")
tools/batch_test/soak/soak_loop.sh:160     SOAK_SCRIPT_VERSION="$VER"
tools/batch_test/soak/soak_loop.sh:81-83   VER=$(cat /opt/soak/ab_version 2>/dev/null \
                                               || (cd "$REPO" && git describe --tags --always --dirty 2>/dev/null) \
                                               || echo unknown)
```

⇒ **两条分支,正好对上上一轮观测到的两种形状**:
- **入臂局(`mirror`)**:`/opt/soak/ab_version` 存在 ⇒ 它赢 ⇒ 字段被 `mirror:<ids>:s<seed>:<side>` 整个占掉。
  **上一轮 §4.5 第 1 条(「台账那个关键字段对参与评分的局不记树」)本轮在代码上坐实。**
- **暖场局(`warmup`)**:该文件还不存在 ⇒ 回退到 **`git describe` 于 `$REPO`**。

### 4.3 ⭐ 而发波路径**只 fetch `main`** —— 于是 (甲) 需要一个本台给不出的额外机制

```
tools/batch_test/aws/spot_run.sh:34    REF=main            # 默认
tools/batch_test/aws/spot_run.sh:278   sudo -u ubuntu git fetch -q origin '$REF' || true
tools/batch_test/aws/spot_run.sh:279   sudo -u ubuntu git checkout '$REF' || true
tools/batch_test/aws/spot_run.sh:280   sudo -u ubuntu git pull --ff-only origin '$REF' || true
tools/batch_test/aws/spot_run.sh:281   sudo -u ubuntu git log --oneline -1 || true
```

`git fetch origin main` **只取 `main` 这一个 ref**;AMI(`dota2bot-batch-20260718`,**07-18**)
的那份 clone 不可能含有 **09-12** 的提交。
⇒ **`52ea2be4` 经由这条路径进不到实例的 HEAD 上。**

⛔⛔ **但本台到此仍不下结论,理由写在 §4.4**:这**削弱** (甲) 而**不等于**证明 (乙)。
⭐ 并且它顶出了上一轮没列的**第三个解释**:
**(丙)** 该 stamp 根本不来自 W69 的实例,而来自**某个本地 soak 运行**
(`$REPO` = 某条流容器自己的工作树,那棵树**正在**会话分支上 ⇒ `git describe` 恰好给出会话分支的 commit)。
`52ea2be4` 是一条 **hero 流**的会话分支提交,与 (丙) 的形状吻合。
⛔ **本台无上一轮取样的出处,证不了 (丙),只登记为候选。**

### 4.4 ⭐⭐⭐ 分不开的原因是结构性的:**记录这件事的那条日志,在正常跑完的波上从不上传**

`spot_run.sh:246` `exec > /var/log/soak_farm.log 2>&1` + `set -x`
⇒ **§4.3 那四行 git(含 `git log --oneline -1` 打出的「实例最终在哪棵树上」)确实被记下来了**,记在 `/var/log/soak_farm.log`。

而这个文件在整个 `spot_run.sh` 里**只有一个上传点**:

```
tools/batch_test/aws/spot_run.sh:267   aws s3 cp /var/log/soak_farm.log "$S3_RUN/soak_farm.log" --quiet
```

**它在 `spot_watch.sh` 的「收到 spot 抢占通知」分支里面。**
⇒ **只有被抢占的波才会上传这条日志;正常跑完自终的波,它随实例一起消失。**

**真语料复核(⛔ 不只读代码)** —— W69 的一个正常完成 run 目录:

```
s3://dota2bot-batch-results-4924/soak/spot_20260912_092622_1_main_896712/
OBJ = 120   (analysis.json / .dem / .log.gz)
grep -iE "soak_farm"  ⇒  零命中
```

⇒ **120 个对象里没有 `soak_farm.log`。实测与代码一致。**

**这就是交棒 ⑥ 的答案**:(甲)/(乙)(以及新的 (丙))**不是本台没去取那个对象,
是能把它们分开的那个对象从来没被上传过**。

### 4.5 ⭐⭐ 顺带撞出的那条缺陷,比 W69 本身重要得多

把 §4.3 的 `|| true` 和 §4.4 的「不上传」放在一起:

1. `git fetch` / `checkout` / `pull` **三行全是 `|| true`** ⇒ 任一行失败**静默**,
   实例就停在 **AMI 那棵 07-18 的树**上继续跑整波。
2. 唯一能事后看出这件事的记录(`git log --oneline -1`)只进 `/var/log/soak_farm.log`。
3. 该日志**只在被抢占时**上传 ⇒ **正常跑完的波,事后无从查证它跑的是哪棵树**。
4. verdict / `analysis.json` 一切正常,**没有任何东西会举手**。

📌 **这正对着 `AGENTS.md` 里那条用真金白银买来的教训**
(「Batch instances clone `origin/main` at launch — verify the remote tip **BEFORE** launching」):
那条教训教的是**发波前**核 `git ls-remote origin main`,而它核的是**远端**,
**不是实例最终落在哪棵树上**;中间隔着三个 `|| true`,而**事后核对的通道被 §4.4 关掉了**。
⇒ **门在,但被结构性绕开** —— ⭐ 与 GH #454 (乙)、`pullcad` 同族,
**失效方向是危险那一侧**(measured 的是错的树,而读数看上去完全正常)。

⛔ **本台未代修**(harness 编排权不在本台,先例 GH #33),已开 issue 见 §六。

---

## 五、泄漏:五条独立路径全零(⛔ 不合并成一句)

| # | 路径 | 读数 |
|---|---|---|
| ① | `check_costs.sh` 自带区块 | **空** |
| ② | `describe-instances` 五态,**不加 tag 过滤** | `INST_EXIT=0`、**`LINES=0`** |
| ③ | 账户级 accrual | **17 区 `COMPLETE`**、`alpha = 0` |
| ④ | AMI | 仍 `ami-0a990a26d89c66547` 一张(常设成本非泄漏) |
| ⑤ | 闸 | `pending waves : $0.000`,且逐字 `CERTIFIED ... is a reading, not a default` |

⛔ 不合并 —— ①②③ 问「现在有没有机器」、④ 问「有没有常设资产」、⑤ 问「有没有已发未计的波」。
⚠️ 本轮**未**买账单侧第六条(逐日 `EC2 - Compute`),理由同 GH #801 四行。

---

## 六、通知与 issue

**通知判据:本轮推。** 写死的字面判据是「两量都动了」⇒ §1.1 两量都动了 ⇒ **推**
(先例:09-15T15:20Z 推、18:06Z/21:08Z/00:16Z 三轮两量逐位相同故不推)。

**已开 issue**(⛔ 按 GH #290,**发表在 `git push` 之后**,并先跑 `claim_precheck.sh`):
见 §九。

---

## 七、月末外推(口径不变,⛔ 不收窄)

`$91.003 + 14 天 × $0.40–0.92/天 − $0.8`(`dem21/` 09-22 自动过期省 `~$0.8` 已扣进两端)
⇒ 9/30 MTD 落在 **`$95.8 – $103.1`**(上一轮口径下是 `$96.0 – $103.8`)。

⚠️ **诚实边界:空转日样本 n=4**(本轮 `$0.47/天` 是第四个点)
⇒ **区间宽是真实的不确定性不是排版**;⛔ 不外推到十月、⛔ 不改任何常数、
⛔ 不主张 `$100` 一定会/不会被越。
⚠️ AWS 自己的 `forecast 102.229` 落在这个区间上沿附近 —— ⛔ **巧合,不当互证**(§1.1)。

---

## 八、铁律 9 / 开工自检 / 自捉

**铁律 9**:P4.1 upstream 标尺波**连续第二十三轮欠**,⛔ **唯一阻因是闸 (iii) `exit 3`(预算)**,
不是未认领也不是排期。

**⛔ 自捉一条(方向对本台不利,照登)**:上一轮交棒 ⑦(a) 的字面模板**第七轮仍未照抄** ——
本轮**第一条命令**是 `bash tools/agent/routine_selfcheck.sh 2>&1 | tail -40`,**带管道**,
脚本按 evidence-discipline 3 当场拒绝,逐字:

```
REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.
```

(脚本自陈**已复发 5 次,每次都是当轮第一条命令**;本轮第 6 次。)
📌 **exit 2 =「没跑成」不是「没发现」** ⇒ ⛔ 那一次什么都没检查、不得记成一次干净的自检。
第二次改成重定向裸读(⛔ 无管道)**跑起来了**。
⭐ **这条拒绝腿自己在工作(它挡住了一个假读数);不工作的是本台的第一条命令。**

**⚠️⚠️ 开工自检 python trunk 腿:本轮**连续第二轮**没跑完**(写报告时已跑 >55 分钟,仍停在
`=== trunk health (python test suite) ===` 之后无输出)。
上一轮交棒 ⑦(b) 逐字要求:「若再次跑不完,就把『这条腿在 Routine 容器里跑不完』立成 issue,
⛔ 不要再连续两轮把它记成『读不到』。」⇒ **本轮照办,见 §九。**

**跑完的那部分腿级读数**(⛔ 只是跑到的那一段,不是腿级判词 —— 腿级判词本轮拿不到):

```
=== Lua tests no automatic reader runs (GH #806) ===
LUA GATE COVERAGE  disk 482 | push gate 330 | 开工自检 leg 117 | known_red 6
  UNCOVERED (no automatic reader runs these): 114 of 482 (24%)  {'no_manifest_row': 52, 'timed_out': 60, 'too_slow': 2}
    NOW COVERED    tests/test_fieldsip_atom_pricing.lua
UNCOVERED SET GREW -- 2 file(s) that nothing reads automatically:
    NEW UNCOVERED  no_manifest_row    tests/test_dusttower_dive_guard.lua
    NEW UNCOVERED  no_manifest_row    tests/test_fieldsip_transfer_receiving_site.lua
```

⭐ 与上一轮**逐位相同的 2 个文件**(⛔ 未恶化也未修复);
`no armed id hangs under an unarmed gate -- OK`。
⛔ **本台不代修、不归因、不调成员资格。**

⚠️ **⛔ 本轮拿不到 `selfcheck worst exit` / `FINDINGS` / `UNCERTIFIABLE` 三行**
⇒ 按铁律 10 (ii),**trunk 的两侧本轮都不能说被看过**;⛔ 不写「trunk 绿」也不写「trunk 红」。

---

## 九、issue / push / 闸读数

见本报告随附的 commit 与 §六。三条 push 闸读数与 issue 编号在 follow-up commit 中补齐
(⛔ `RULE6_BYPASS` / `--no-verify` 均未使用)。

---

## 十、交棒

- ① ⭐⭐ **owner —— 刹车第三十轮持有,GH #779 第二十一轮零表态**。
  本轮**有新支出事实**(快照解冻,`+$0.221 / 11.27h`)⇒ **按判据已推一条通知**。
  球仍在 owner:**提高预算线,还是接受本月剩余时间批测全停**,⛔ 本台无权自裁。
- ② ⭐⭐⭐ **总监/协同组 —— §4.5 是本轮最重的一件**(issue 见 §九):
  `spot_run.sh:278-280` 三个 `|| true` + `soak_farm.log` 只在抢占时上传
  ⇒ **正常跑完的波,事后无从查证它跑的是哪棵树**,而 `AGENTS.md` 那条教训核的是远端不是实例。
  ⛔ 本台未代修。**最小修法**(写在 issue 里):把 `git log --oneline -1` 的输出
  单独写一个小文件并**无条件**上传到 `$S3_RUN/`,⇒ 一个对象、零 EC2 增量、
  **把「测的是哪棵树」从不可查变成可查**。
- ③ ⭐⭐ **总监 —— 交棒 ⑥ 本轮已执行完毕并关闭,结论是「该对象回答不了」**(§四):
  (甲) 被 §4.3 **削弱**(`fetch origin main` 取不到会话分支提交),
  ⛔ 但**不等于** (乙) 成立;新增候选 **(丙)**(stamp 来自某条流的本地 soak,`$REPO` 在会话分支上)。
  ⛔ **在 §4.5 修好之前,这三者对任何「跑完自终」的历史波都分不开** ——
  ⛔ 因此**仍不许**说「W69 测的不是 main」,也**不许**据此否定 W69 任何读数。
- ④ ⭐⭐ **总监 —— python trunk 腿连续两轮在 Routine 容器里跑不完**,按上一轮交棒 ⑦(b) 已立 issue(§九);
  连带后果照登:**本轮 trunk 两侧都没人看过**(拿不到腿级判词)。
- ⑤ ⭐⭐ **总监 —— `s3_attribution_gap_remeasure_under_ruling48` 触发判据请重裁**
  (逐字保留,**连续第五轮未获回应**)。建议改**日期判据**:
  第一个干净的裁定后整日 = **09-16(即今天)**,最早可读 **~09-17T11:20Z**。
  ⛔ 本台不自行改欠条(写入权归总监,先例 GH #33),状态仍 **OWED**。
- ⑥ **总监 —— 上一轮交棒 ⓪③④⑤ 逐字保留,本轮未获回应**,⛔ 本台不自行退休任何一行。
- ⑦ **下一轮本台**:
  (a) 开工第一条命令 **`bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1; echo "EXIT=$?"`**
      —— ⛔ 无管道、⛔ 无 `timeout`,**七轮六次犯法,一次到位**;
  (b) 刹车解除(headroom ≥ `$1.10`)时按规格发 P4.1 标尺波、**收割必带 `--ledger`**,
      并抄 GH #801 四行**恢复后**的样子;**发波前先看一眼 `bots/Customize/soak_side.lua`**;
  (c) 若某轮 `beta ≠ 0`,**`soak/` 必须列**;
  (d) ⭐ 若 §4.5 的修法落地,**下一波是它的第一次真语料验收** —— 收波时第一件事看
      `$S3_RUN/` 里有没有那个树记录对象。

## 十一、token 用量(铁律 8)

见 follow-up commit。
