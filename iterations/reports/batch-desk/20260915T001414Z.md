# 批测台 2026-09-15T00:14:14Z — 刹车第二十一轮持有;⭐⭐⭐ 空转支出归因归到底:烧的是**出网流量**,而产生它的是本队自己每轮的 `.dem` 下载

**一句话**:MTD `$90.373`(与上一轮**逐位相同**,快照亦未刷新)、**零发波、零收割欠、零泄漏、`bots`/`game` 一行未改**;
本轮的实质工作单元是花 `$0.01` 买上一轮交棒 ③ 点名的那次 `GroupBy=USAGE_TYPE` CE,
**把「S3 的三分之二到四分之三未归因」这个洞关掉** —— 答案是 **`DataTransfer-Out-Bytes`(出网)占 7 天 S3 的 84.2%、占 09-14 当天的 99.5%**,
**不是存储**;而桶现测**完全私有** ⇒ 那是**带凭证的自己人下载**,不是外部白嫖。
⇒ owner 手上那个「提高预算线 / 本月批测全停」的二选一,**第三个选项本轮才第一次有证据**:
**空转烧的 97% 是本队可以自己关掉的东西。**

---

## 一、成本(当轮现跑,⛔ 下轮不得抄作前提)

- `AWS_SETUP_EXIT=0`。容器无 CLI,自装 `aws-cli/1.46.1 Python/3.11.15 botocore/1.43.62`;
  身份逐字 `AWS ready: arn:aws:iam::581436534924:user/dota2bot-agent` + `(restricted batch-runner user — correct)`。
- `COST_EXIT=0`(⛔ **未走管道**,重定向后裸读)。
- MTD **`$90.373`** / `forecast 196.979` / `budget limit 100.0` /
  `budget refreshed 2026-09-14T20:27:33Z`;**headroom 对 `$90` = `$-0.373`**。
- ⚠️ **MTD 与快照戳与上一轮(21:22Z)逐位相同** ⇒ 距快照 **3.78h** 未刷新。
  ⛔ **这不是「3.78 小时没涨」,是「3.78 小时没测」**(09-14T09:11Z 那轮已立此读法)。
- 探针行逐字:`accrual probe (GH #779): alpha = 0 accruing instance(s) account-wide; beta = 0 wave record(s) after 2026-09-14T09:09:33Z`
  + `stamp unknown (fresh container) -- alpha and beta are both zero -- a frozen stamp over an idle account opens no blind spot`。

**GH #801 四行整块照抄**(⛔ **未用 `COST_CONFIRM_AT` 自救,默认值跑**):

```
   >= $35, but headroom to the $90.00 brake is $-0.373
   < $1.10 (cheapest wave) — CE confirmation SKIPPED, NOT passed (GH #801).
   Nothing can launch at this MTD, so confirming it buys nothing.
   Resumes by itself as soon as headroom >= $1.10.
```

⚠️ **这是 SKIP 不是 pass。**

**通知判据(本轮拆成两问,⛔ 不混为一谈)**:
- **成本增量通知:不推。** 判据是「戳变了 **且** MTD 变了」,本轮**两个合取项都不成立**(逐位相同)⇒ 判据守字面,**不推、不发 #779 新的成本增量评论**。
- **本轮另有一条推送,理由不是成本增量而是 §三的归因结论** —— 它**改变 owner 手上那个决定的输入集**(从二选一变成三选一)。⭐ 这两条判据是**不同**的东西,本报告不把后者算成前者的例外。

---

## 二、闸

**(iii) `wave_fence.py --planned 1.10` ⇒ `FENCE_EXIT=3`。**
⛔ 未传任何 `--no-accrual-check` / `--pending` / `--director-crossing` ⇒ **本轮无一行「这是 SKIP 不是 pass」**。

```
actual (MTD)     : $90.373   <- re-read this run, never cached
pending waves    : $0.000   (launched, may not be in MTD yet)
planned          : $1.100
projected total  : $91.473
fence            : $100.00   <- lowest ACTUAL alert not yet crossed (Ruling 1)
brake            : $90.00   (owner's line, not derived here)
operative ceiling: $90.00   = min(fence, brake)
A launch at this instant would put MTD past the $90.00 BRAKE line.
WAVE_FENCE: THROTTLED (exit 3) -- brake, not fence. Stop and report to the owner; do not wait it out.
```

**RULING 5/6/7 三条判据行各自照抄**(⛔ 只抄 `accrual check :` 不算):

```
accrual scope    : 17 region(s) read [ap-northeast-1, ap-northeast-2, ap-northeast-3, ap-south-1, ap-southeast-1, ap-southeast-2  +11 more]  <- COMPLETE: every region this account has enabled
wave accrual     : records after 2026-09-14T09:09:33Z (= 2026-09-14T20:27:33Z - 11.3h lag, clock from budget snapshot)
accrual check    : CERTIFIED (0 accruing instances account-wide, read this run) -- pending $0.000 is a reading, not a default.
```

⭐ 钟**未降级**(`clock from budget snapshot`,非 fallback 措辞)。
`crossing registry: 0 record(s) ... none in force`,`1 retired record(s)`(GH#754/RULING 15)。
`budget filters : none <- so every instance in the account does land in this budget, whoever owns it`。
三条告警照登:`$50.00 crossed [ALARM]` / `$80.00 crossed [ALARM]` / `$100.00 NOT yet crossed [OK]`。
⚠️ 照登不藏:`$90`–`$100` 之间 AWS 侧**仍可花**,挡住的是 owner 这条线不是 AWS。

**(i) `wave_throttle.py` ⇒ `THROTTLE_EXIT=0`。** 锚 W69 末台 `2026-09-12T09:26:31Z`,
`margin +204555s past unlock`(**56.8h**)⇒ **闸 (i) 不是本轮不发波的原因**。

⚠️ **工具路径更正(登记给下一轮)**:章程散文里的 `tools/batch_test/aws/wave_fence.py` **不存在**,
第一跑 `FENCE_EXIT=2` + 逐字 `can't open file`。真实路径是 **`tools/batch_test/soak/wave_fence.py`**
(`wave_throttle.py` 同目录)。⭐ **`exit 2` 当场把它暴露了** —— 若那条命令的码是通过管道读的,
这一轮会拿一个「没跑成」冒充「闸没拦」。

⇒ **五道闸只有 (iii) 不过,而它一条就够。**

---

## 三、⭐⭐⭐ 实质工作单元:上一轮交棒 ③ 的那 `$0.01` 本轮买了,洞关掉了

### 3.1 为什么本轮买

上一轮(21:22Z §三.3)测得:全桶 **184.51 GB** 纯存储折算 **`~$0.141/天`**,而实测 S3 是 **`$0.39–0.66/天`**
⇒ **三分之二到四分之三未归因**,并逐字写下「下一步是 `GroupBy=USAGE_TYPE` 的一次 CE(`$0.01`)」。
本轮买它的理由**不是**「owner 要削 S3」(owner 仍零表态),而是:
**那个未归因的量是 owner 待决问题的主项**——§六 的 `$96–104` 区间里,16 天 × 空转速率的**速率**全靠它,
而「它是存量(09-22 自己过期)」与「它是流量(每天照烧)」对那个决定的含义**完全相反**。

**一次请求,`$0.01`**(DAILY × `GroupBy=USAGE_TYPE` × `Filter=SERVICE:S3` 同在一次请求里,`CE_EXIT=0`)。

### 3.2 读数(`ce get-cost-and-usage`,09-08 → 09-15,DAILY,SERVICE=S3,GroupBy=USAGE_TYPE)

| 日期 | S3 合计 | **DataTransfer-Out-Bytes** | TimedStorage-ByteHrs | Requests-Tier1 | Tier2 |
|---|---|---|---|---|---|
| 09-08 | 0.6156 | **0.4653** | 0.1248 | 0.0230 | 0.0025 |
| 09-09 | 1.1328 | **0.9789** | 0.1285 | 0.0231 | 0.0023 |
| 09-10 | 1.1575 | **1.0150** | 0.1333 | 0.0072 | 0.0020 |
| 09-11 | 1.2606 | **1.0959** | 0.1369 | 0.0250 | 0.0029 |
| 09-12 | 0.5508 | **0.3962** | 0.1411 | 0.0120 | 0.0015 |
| **09-13**(零实例全天) | 0.6599 | **0.5188** | 0.1380 | 0.0024 | 0.0007 |
| **09-14**(零实例) | 0.3893 | **0.3873** | —(未出) | 0.0016 | 0.0004 |

**七天合计**:`USW2-DataTransfer-Out-Bytes` **4.8574(84.2%)** ·
`USW2-TimedStorage-ByteHrs` 0.8026(13.9%) · `Requests-Tier1` 0.0943(1.6%) ·
`Requests-Tier2` 0.0122(0.2%) ⇒ 合计 **5.7665**。

### 3.3 四条结论,逐条带限度

1. ⭐⭐⭐ **空转期烧的是出网流量,不是存储。** 09-14(零实例、零发波、零收割)
   S3 `$0.3893` 里 **`$0.3873` = 99.5%** 是 `DataTransfer-Out-Bytes`。
   按 `$0.09/GB` 折算 ⇒ **≈ 4.30 GB/天被从桶里拉到公网**,**在没有任何机器活着的日子里**。
   ⭐ 这**独立地证实并收窄**了上一轮那条「存储解释不了三分之二到四分之三」——
   上一轮只能说「剩下那部分说不了,⛔ 不猜」,本轮**它有名字了**。
2. ⭐⭐ **拉它的是自己人,不是外部。** 桶现测**完全私有**(⛔ 不猜,四项逐条读):
   `PAB_EXIT=0` ⇒ `BlockPublicAcls/IgnorePublicAcls/BlockPublicPolicy/RestrictPublicBuckets` **四项全 `true`**;
   `POL_EXIT=255` ⇒ 逐字 `NoSuchBucketPolicy ... The bucket policy does not exist`;
   `ACL_EXIT=0` ⇒ `Grants` **只有 owner 一条 `FULL_CONTROL`**,**无 `AllUsers`/`AuthenticatedUsers`**。
   ⇒ 出网**必然是带凭证的请求**(即本队工具/各流的 `aws s3 cp`),**排除公开桶被白嫖**这一支。
   ⭐ **这一支必须被排除**,因为它若成立,处置是安全事件而不是用量管理,**方向完全不同**。
3. ⭐⭐ **机制对得上,量只对得上一部分(⛔ 不把部分说成全部)。**
   replay-check 自己 09-14T21:47Z 的报告逐字写 **「只读 S3(8 个 `.dem`),零 EC2 / 零 CE / **零支出**」**。
   现测 `dem21/` 的 `.dem`:**6,756 份 / 134.51 GB / 均值 19.9 MB / 中位 22.0 MB / 区间 7.8–24.8 MB**
   ⇒ **8 份 ≈ 0.16 GB ≈ `$0.014`/轮**;该流 **8 轮/天** ⇒ **`~$0.11`/天**。
   ⚠️ **那只占 09-14 出网 `$0.3873` 的 ≈28%**,**其余 72% 本轮未归因**
   (候选:replay-artifact 的建页下载、各流零散 `s3 cp`、本台自己;⛔ **不点名、不分摊、不猜**)。
   ⭐ **「零支出」那句话是错的,而这正是缺陷的形状**:读 S3 在各流的账里记成免费,
   于是**没有任何一条记账把它加起来**,而它是本台此刻**全部**空转支出的 97%。
4. ⚠️ **归因到「哪个前缀 / 哪个调用方」本轮做不到,且本台没有自行打开那扇门。**
   `get-bucket-logging` 现跑 `BLOG_EXIT=0` 且**返回空** ⇒ **服务端访问日志从未开启**;
   CloudTrail 数据事件同理未开。⇒ 要点名具体调用方,只能新开日志(**自身有成本、且是配置变更**)
   或买 CloudTrail 数据事件(**贵得多**)。⛔ **本台不擅自开启任何一个** —— 那是配置变更不是读数,
   交总监/owner(交棒 ②)。

### 3.4 ⭐ 顺带更正上一轮的一个数(方向:上一轮低估了 `.dem` 的份数)

上一轮 §三.4 写「`dem21/` 6,756 个对象,其中 `.dem` 大概 103 份,其余是伴生对象」。
**现测:该前缀下 `.dem` 就是 6,756 份**(`LS_EXIT=0`,全量 `--recursive` 列举后按后缀计数,⛔ 非抽样),
**即该前缀下每一个对象都是 `.dem`**,没有伴生对象。⇒ 上一轮那句「103 大概是 `.dem` 本身的份数」**不成立**,
本轮照登更正。(⭐ 它不改变上一轮的结论——134.51 GB 与 72.9% 占比两个数都是对的。)

---

## 四、⭐⭐ 这条归因对 owner 那个待决问题做了什么(⛔ 本台无权自裁,只摆输入)

上一轮把选项写成二选一:**提高预算线 / 接受本月剩余时间批测全停**。
**本轮的归因加了第三条,而且它是三条里唯一不用花钱也不用停工的**:

| | 处置 | 它动的是什么 | 代价 |
|---|---|---|---|
| 甲 | 提高预算线 | 天花板 | owner 的钱 |
| 乙 | 本月剩余全停 | 产出 | 15 天零批测 |
| **丙(本轮新)** | **削掉每天 ~4.3 GB 的 `.dem` 出网** | **空转速率本身** | **各流改下载习惯(缓存/少拉/只拉需要的片段)** |

⭐ **丙的量级**:空转速率 `$0.39–0.66/天` 的 **97%** 是出网 ⇒ 若出网归零,速率落到 **`~$0.14/天`**
(纯存储),09-22 `dem21/` 过期后**更低**。按 16 天算,**甲需要的余量从 `$6–14` 掉到 `~$2.2`**。
⚠️ **诚实边界,三条**:
(a) 出网**不可能真归零**——录像组的活就是看录像,它必须拉;丙是**削**不是**关**;
(b) §三.3 只归因了其中 28%,**剩下 72% 的可削性未知**;
(c) 速率样本仍是 **n=2 个空转日**(`$0.9185` 全天 / `$0.3997` 20.5h),两天差近 2 倍 ⇒
⛔ **不外推到十月、不据此改任何常数、不主张 `$100` 一定会/一定不会被越**。

⛔ **本台不建议、不预选、不自行改任何一条线,也不去改别的流的下载行为**(那是它们的作用域)。

---

## 五、收割 / 泄漏

**收割:零欠。** `S3VAL_EXIT=0`,`validation/` **592 对象**,与上一轮基线**逐位相同**;
最新对象仍是 `tpcommit+46ids-24c30245be01_20260907_2218_run.log`(`2026-09-07 22:18:52`)
⇒ **零新对象、零下载、零 S3 出网由本轮产生**,`recover_verdict.py` 本轮**未跑**
(**没有可收的东西,不是漏收**)。⛔ 不重算已收波次。
**局数**:无波在跑 ⇒ 进度 **N/A 不是 0 局**;距上一波起飞 **56.8h**。

**`queue.json` 126 条**(上一轮 122):`pending 51` / `harvested_pending_verification 30` /
`ruled_approved_scan 11` / `done 9` / `harvested 6` / `frozen_hold 5` /
`delivered-and-consumed 4` / `ruled 4` / **`running 2`** / 其余各 1。
⭐ **`running` 从 3 掉到 2**(`strategy-31`/`strategy-32`;**`hero-27` 已不在** ——
与 `iterations/reports/batch-desk/hero27_savemana_archive_scan.md` 已存在一致,
即 09-14T09:11Z 补记 (B-bis) 接下的那根棒**已履行**)。
⚠️ 闸 (iii) `exit 3` ⇒ **队列里任何请求本轮都发不出**,与零发波不构成冲突。

**泄漏:五条独立路径全零(⛔ 不合并成一句)。**
① `check_costs.sh` 自带 `running/pending instances` 区块 **空**;
② `describe-instances` 五态(`pending,running,shutting-down,stopping,stopped`)
**不加 tag 过滤** ⇒ **零行**(`INST_EXIT=0`,`LINES=0`);
③ `describe-volumes` **全账户不过滤** ⇒ **零行**(`VOL_EXIT=0`,`VOL_LINES=0`);
④ 闸 (iii) 账户级普查 `CERTIFIED (0 accruing instances account-wide)` + `17 region(s) <- COMPLETE`;
⑤ **账单侧**(09-14T21:22Z 立的第五条):本轮 §三.2 的逐服务/逐用量读数里,
09-13 / 09-14 两天 **`EC2 - Compute` 逐字不出现** ⇒ 过去两天**没有花过机器的钱**。
⚠️ ⑤ 的限度照抄不放宽:**只对滞后带已过去的日子成立,⛔ 不能给今天的波背书**(RULING 6 的域)。
AMI 仍只 `ami-0a990a26d89c66547`(`dota2bot-batch-20260718`)一张 —— **常设成本,非泄漏**。

---

## 六、铁律 9(owner 优先项)

`OWNER_PRIORITIES.md` **P4.1 upstream 标尺波:连续第二十轮欠。**
⛔ **唯一阻因是闸 (iii) `exit 3`(预算),不是本台未认领,也不是排期。**
棒已在 `DECISIONS_NEEDED.md §15` 与 GH #779,⛔ 本轮不重复立条。
常设运维项(Spot 优先)本轮无适用面 —— 零发波。

---

## 七、⛔ 本会话 `bots/` `game/` `tests/` `tools/` 一行未改。

产物只有 `iterations/` 下的本报告与章程条目。⇒ **无一条 trunk red 可能由本轮引入。**

---

## 八、开工自检(铁律 10)与铁律 6

⚠️ **管道坑第 32 次**:本轮**第一条命令**仍走了管道(`routine_selfcheck.sh 2>&1 | tail -60`),
脚本**当场自卫拦下**,逐字:

```
REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.
REFUSED: stdout is a PIPE, so the exit code your harness reports is
         the READER's (tail/head almost always succeed), not this
         script's. This is evidence discipline 3; it has recurred 5x,
         every time as the first command of the round.
```

⇒ **零损失,但拦住它的又一次是工具不是纪律。**
第二跑改 `> /tmp/sc.log 2>&1` 裸读;⚠️ **但第二跑带了 `timeout 400`**(上一轮交棒 ⑧(2) 明确要求**不设**)
⇒ 被 harness 搬到后台。**两道坑本轮各踩了一道,如实登记不辩解。**
此后每个读退出码的命令一律重定向后裸读,零管道。

**⚠️ 一处本轮自己抓到的管道违规(照登)**:§三.3 那组桶配置读数的**第一跑**是
`awsx ... | head -N`,于是 `PAB_EXIT/POL_EXIT/ACL_EXIT` **读的是 `head` 的码不是 awsx 的**
⇒ **当场作废重跑**(重定向裸读),本报告里那三个码**全部来自第二跑**。
⭐ 第二跑把 `POL_EXIT` 从假 `0` 纠正成真 **`255`**(= `NoSuchBucketPolicy`)——
**同一个事实,第一跑的码是错的。**

**自检读数(写作时 Lua 腿仍在跑,⛔ 无收尾横幅 ⇒ `SELFCHECK_EXIT` 本轮未知,不读成通过,也不空转等它)**:
- ⭐⭐ **python 腿 `133 passed, 0 failed, 3 uncertifiable`** ⇒ **上一轮(21:22Z)的 `1 failed` 与
  09-14T09:11Z 补记 (B) 的 `test_pending_rulings.py` 红,在 trunk 上已经不红了**。
  ⚠️ **本台不主张是谁修的**(本轮未查 commit),只登记「本轮第一手读到 0 failed」。
  3 条 `uncertifiable` 仍是 `5a0` 那个 `120s` 预算截断(`5a`/`5d` 等四条 `did NOT run`)⇒ **不是通过**。
- `no expired admission wait in any live 当前状态 block (6 charter(s), 48 settled id(s))`;
  `9 anchor(s) checked -- OK`;`FROZEN none (no live gate names a promoted id)`(⇒ `pullcad` 陷阱本轮为零);
  `promote-atom constraints: OK`;`FROZEN 0 COUPLED 1 UNRESOLVED(armed) 0 other 223`。
- `LUA GATE COVERAGE disk 465 | push gate 325 | 开工自检 leg 102 | known_red 7`,
  `UNCOVERED 115 of 465 (25%)`,`UNCOVERED SET GREW -- 2 file(s)`:
  `test_dusttower_dive_guard.lua`(**连续第五轮**)、`test_fieldsip_transfer_receiving_site.lua`(**连续第三轮**)。
  ⭐ 与 09-14T09:11Z 对照:`disk 455→465` / `leg 92→102` / **`UNCOVERED 115→115`** ⇒
  **十个新落地的 Lua 测试全部进了自检腿,缝没变宽。** ⛔ 本台未开新 issue(#624/#806 已立案)。
- `registry rows: 71`;`RIDESHARE: 8` / `total open requests: 116`。
- ⭐ `grep -cE "^GAP +cadence batch-desk"` = **0** ⇒ **本台无 cadence GAP。**

**铁律 6 三条腿**:见文末补记(push 时现跑)。

**铁律 11**:零 `requires approval`、零权限对话框、零空转(自检后台跑,成本/闸/收割/泄漏/CE 归因并行推进)。

---

## 九、交棒

1. ⭐⭐⭐ **owner —— 刹车第二十一轮持有,GH #779 第十六轮零表态;但本轮选项集从二选一变成三选一(§四)。**
   ⭐ **丙(削出网)是三条里唯一不花钱也不停工的**,量级:空转速率的 **97%** 是 `.dem` 出网,
   削掉后 `~$0.14/天`。⛔ 本台不建议、不预选、不代改别的流的行为。**已推送 owner 一条。**
2. ⭐⭐⭐ **总监 —— 「读 S3 是免费的」这条集体错觉要一条制度来挡。**
   现场逐字:replay-check 09-14T21:47Z 报告写「只读 S3(8 个 `.dem`),零 EC2 / 零 CE / **零支出**」,
   而那一句对应 **`~$0.014`/轮**;全队没有任何一条记账把这些加起来,于是它涨到**空转支出的 97%**
   才被看见,**而且是被一次 `$0.01` 的 CE 看见的,不是被任何记账看见的**。
   请总监裁:(a) 各流报告里的「零支出」是否必须改成「零 EC2 / 零 CE,S3 出网 ~$X」;
   (b) 要不要开 S3 服务端访问日志做调用方归因(**它自身有成本、是配置变更**)——
   ⛔ **本台未擅自开启**(§三.3 限度 4)。**已开 GH issue 承载,见报告末。**
3. ⭐ **总监 —— 上一轮交棒 ② 仍未收到回应**(逐日 CE 是否写成定期读数、谁付 `$0.01`);
   本轮的 `USAGE_TYPE` 那 `$0.01` 是**同一族**问题,⛔ 本台未自行立规。
4. ⚠️ **harness —— 章程散文里的 `tools/batch_test/aws/wave_fence.py` 路径是错的**(§二),
   真实路径 `tools/batch_test/soak/wave_fence.py`。⛔ 本台**未改章程散文**(1.8MB 章程的编排权在总监),
   只在本报告与本轮条目里登记,免得下一轮再吃一次 `exit 2`。
5. ⚠️ **下一轮本台 —— 开工第一条命令仍是三件一次到位**:(1) 重定向 ⛔ 不走管道;
   (2) `run_in_background` ⛔ **不设 `timeout`**;(3) 起跑前确认没有上一次的自检还活着。
   **本轮 (1) 又犯、(2) 又犯、(3) 做到了。**

---

## 补记(push 之后,GH #290 顺序)

### (A) 铁律 6 三条腿(两次 push 逐字相同)

```
luacheck bots game: 0 warnings
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 89 ran, 0 findings, 0 uncertifiable, 11.8s
lua gate: SKIPPED BY SCOPE -- this push touches no bots/game/tests path.
```

⚠️ **第三条腿是作用域判定不是绿**(工具自带逐字 `This is a SCOPE decision, not a pass and not a skip of a gate that had something to say (GH #624).`)。
`ARM_EXIT=0`;⛔ **未用 `RULE6_BYPASS`** ⇒ 无「SKIPPED, not passed」行。
`PUSH1_EXIT=0`(`* [new branch] claude/great-sagan-6d0x2t`)、`PUSH2_EXIT=0`(`e2cb9186..57cfe5a0 HEAD -> main`)
⇒ **零推送竞态、零 rebase**。

### (B) 已发表(草稿过 `claim_precheck.sh`,`PRECHECK_EXIT=0`,逐字 `clean` / `OK to publish: every citation resolves on origin/main.`)

- **GH #826 `[batch]`** —— 承载交棒 ②(「读 S3 是免费的」这条集体错觉 + 请总监裁的两件)。
- **GH #779 第十三条评论**(`issues/779#issuecomment-5672768606`)—— 承载交棒 ①(选项集从二选一变三选一)。
  ⭐ 评论开头**先写明本轮不报成本增量及其判据**,⛔ 不让「有新事实」冒充「成本又涨了」。
- **owner 推送一条**(手机/邮箱),内容同上并整块带上 §四 的三条诚实边界。
  ⛔ **未推第二条**,⛔ 未请求任何不可逆动作的授权(对照 09-14T21:22Z §四 的教训)。

### (C) 自检(铁律 10)真码

⛔ **本轮仍无收尾横幅**:收尾时 `grep -c "selfcheck worst exit"` = **0**,停在 `=== trunk health (fast Lua detectors) ===` 腿上
⇒ **`SELFCHECK_EXIT` 本轮未知,⛔ 不读成通过,⛔ 也不空转等它。**
已跑完腿的读数见 §八(其中 **python 腿 `133 passed, 0 failed, 3 uncertifiable`** 是本轮最有价值的一条:
**上一轮的 trunk red 在 trunk 上已不红**)。
⚠️ 登记给下一轮:本轮**第二跑带了 `timeout 400` 被搬后台**(交棒 ⑤(2) 又犯一次),
而**不带 timeout 的后台跑本来就是那条要求的全部意思**。

### (D) 本次会话 token 用量(铁律 8)

```
TOKENS total_in=3,878,960 out=38,669 turns=36
```

(⚠️ 这是「到统计时刻为止」的数字,统计后的收尾回合不计入。)
