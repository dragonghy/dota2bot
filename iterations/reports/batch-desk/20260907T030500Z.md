# batch-desk 2026-09-07T03:05–03:3xZ(收割轮:W52 = 52-id 成员串家族第一波)

**一句话**:W52 四粒全收,**读数一致为负** —— `gpm` 均值 **−64.72**、`comps_better`
**0/4 五量全负**、`suggested=hold_or_reject`,这是近七波里唯一一次五量 0/4;
⚠️ **但这一波的参照腿本身动过**(W50→W52 之间 `slotwait`/`odbuild`/`illumove` 三个 id
promote 进了稳定版),所以「成员串变差了」**不是这份读数唯一能支持的解释**,归因交总监/协同组。
本轮零发波(闸 (i) `THROTTLED`,解锁 `06:21:45Z`),零泄漏,单波实测 **$0.9865**。

---

## 一、自检(铁律 10)

- ⚠️ **第一条命令第十一轮踩同一个坑**:`bash tools/agent/routine_selfcheck.sh 2>&1 | tail -40`
  ⇒ 工具自拒 `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked`
  (它自己那行写着「已复发 5 次,每次都是本轮第一条命令」)。
  改重定向 + 后台重跑(上一轮交棒 ⑤ 点名的坑),**给足时间不设短 `timeout`** ⇒ 本轮跑到
  python 腿与锚点腿都出了结果,**Lua 腿在本报告写完时仍未跑完**(与上一轮读数一致:
  整份自检在本容器上 >900s)。逐腿照抄工具自己的行:
  - 稳定版锚点 **5/4→5 个,全 OK**:⭐ **`stable-v5` = `ckpush`,`3fe17f9dd08b`,本轮首见**
    (`EXISTS`/`PINNED`/`SHIPPED` 三格全 ok)。
  - promote 约束:`161 distinct live gate id(s)` / **17** 个带 PROMOTED 注 / **`FROZEN none`**;
    4 个 co-promote 原子全 ok(`pullcad` 陷阱本轮未触发)。
  - ⏳ **trunk Lua 腿本轮未跑完 ⇒ 既不声称干净也不声称红。**
  - **2 条 `UNCERTIFIABLE`**(`rc.sh` 的两格,理由逐字:`no lua5.1 on PATH`)——
    **没跑成不是通过**。

## 二、⛔ trunk python RED —— 而红的又是登记表的形状,不是数据(与上一轮同族,第三例)

`107 passed, 1 failed, 2 uncertifiable`,唯一 FAIL:**`tests/test_carrier_hero_guard.py`**,
逐字 `FAIL pre-existing term chaos_knight survives the change`(`carrier_hero_guard: 32 checks, 1 failed`)。

**诊断(算术,不是判断)**:该断言(`tests/test_carrier_hero_guard.py:112-114`)读
`iterations/streams/test_set.md` **第 2 行的活体 arm 串**,再硬编码要求 7 个词项各自仍在。
本轮 `ckpush` 被 promote 成 **stable-v5**(`3fe17f9dd08b`,`armed 52 -> 51`),而 `ckpush`
正是 `chaos_knight` 这个载体词项的来源 ⇒ **活体串 51 id(现算确认 `ckpush in ids? False`)、
`chaos_knight` 随之消失、断言按构造转红**。

⭐ **立案的是形状**:这条测试的立案句是「GH #473 这次改动**不丢词项**」,不是
「`chaos_knight` 永远是词项」;把后者写成硬编码列表之后,**任何一次 promote 都能让它转红,
而红的地方与 promote 无关**。这与 **`pullcad`**(promote 一个 id 会静默冻死引用它的 gate)、
以及上一轮的 **GH #579**(`stable-v4` 双 id 塞进单键 `state_json_key`)**同族**:
门在、数据在、两边各自都对,**约定在两者之间没写下来**。

⚠️ **诚实边界**:
- **本会话 `bots/`/`game/`/`tests/` 一行未改** ⇒ 这条红**不是本会话引入的**;
- **未跑 `git stash` 复核**,「main 也红」是推断(依据:本轮改动只在 `iterations/` 下,
  且 red 的输入是 `test_set.md` 第 2 行 + `bots/`,两者本会话都没动),自检自己那行也这么说;
- 上一轮的 `test_stable_anchors.py`(GH #579)**本轮 PASS** ⇒ 那条已被修好。

**本台的利害**:`carrier_terms.py` 就是发波前载体门的来源。词项从 8 项掉到 7 项
(`chaos_knight` 出局)**是 promote 的正确后果**,下一波的 `CARRIER_GATE` 应当按 7 项跑;
但一条会被正常 promote 打红的 trunk 测试,会让下一轮读到红的人**先怀疑自己的波**。

## 三、AWS 引导与成本(章程第 1–2 步)

- `session_setup.sh` **`AWS_SETUP_EXIT=0`**,身份 `arn:aws:iam::581436534924:user/dota2bot-agent`(受限用户,正确)。
- `check_costs.sh` **`COST_EXIT=0`**:
  - **MTD `$62.042`**,来源 `budgets/dota2bot-batch`(免费),`budget refreshed 2026-09-07T02:04:33Z`;
    ≥$35 ⇒ 按章程自动花 `$0.01` 用 CE 复核 **`62.0418196925`**,两源一致。
  - `forecast $205.267` vs `budget limit $100.0`。
  - **`running/pending` 全账号为空**(见第七节)。
- ⚠️ **读数比上一轮(`$50.804`)高 `$11.238`**,而上一轮申报的 `pending` 是 `$11.910`
  (外来 `r7a` 燃烧 `$10.926` + W51 自身 `$0.977`)。
  ⛔ **本台不把这个吻合当成对账** —— 章程步骤 2 (乙) 明令禁用「MTD 增量对得上波费 ⇒ 该波已计入」
  这个手法(它已两次给出错误结论)。**能说的只有**:上一轮申报的 pending 现在大部分已入账,
  **具体是哪几笔本账号无法区分**(预算过滤器仍是 `null`)。
- **围栏(工具自算,不抄常数)**:`wave_fence.py --planned 1.10 --pending 0.99`
  **`FENCE_EXIT=0 CLEAR`** —— `actual $62.042` / `pending $0.990` / `planned $1.100`
  ⇒ `projected $64.132`;**`$50.00 crossed (AWS state=ALARM)`**、`$80`/`$100` 未跨
  ⇒ fence **`$80.00`**、brake `$90.00`、operative ceiling **`$80.00`**、**headroom `$15.868`**。
  accrual 行逐字:`CERTIFIED ZERO (0 accruing instances account-wide), but --pending $0.990 was given and is used as the larger claim.`
  (**未用 `--no-accrual-check` ⇒ 无「SKIPPED, NOT CERTIFIED」行**。)
- 刹车线 `$90` 未触发;owner 批准线 `$100` 未触发。**本轮未新跨任何告警档。**

## 四、⭐ 有时限的那件事排在第一位:四条 SIR 抢在老化前读下

`SIR_EXIT=0`,四条**全部**还在(终止后约 1.8h 读到):

| 种子 | 实例 | AZ | `Status.Code` | `Status.UpdateTime` | 机时 |
|---|---|---|---|---|---|
| 6743 | `i-0d3c9cf26be9c9ac3` | 2a | `instance-terminated-by-user` | `01:16:27Z` | 0.9136 h |
| 6858 | `i-04e6ba47de2c8293a` | 2b | `instance-terminated-by-user` | `01:16:22Z` | 0.9117 h |
| 6877 | `i-0bbc48e26b83a8a4b` | 2c | `instance-terminated-by-user` | `01:07:21Z` | 0.7608 h |
| 6907 | `i-010d948664a594efa` | 2d | `instance-terminated-by-user` | `01:16:45Z` | 0.9167 h |

**四台全是完工自毁,零抢占**(抢占会读 `Server.SpotInstanceTermination` /
`instance-terminated-by-price` / `instance-terminated-no-capacity`)。
⭐ **`UpdateTime` 同时写进 `update`(`reclaim_blind.py` 读的那个键)与 `terminated_at`**
—— 这正是 `W52_wave.json` 预置 `update: null` + `update_note` 要防的那个缺陷(GH #580),
**本轮它没有静默复发,因为记录自己把要求写在每台机器上**。

⚠️ SIR 行的 `SpotPrice` 四台全读 `0.680000` —— **那是出价上限不是成交价**,不许拿它算钱。

## 五、⭐⭐⭐ W52 收割(本轮主体)

**`RV_EXIT=0`,无任何拒答。** 语料 `s3://dota2bot-batch-results-4924/soak/<run>/`
(**`soak/` 前缀不是 `validation/`**),四个 run **各自一个子目录**下载(GH #225 不压平):
`212ea5`/`7e6283`/`af2874`/`0e6ccf` = **64/61/48/56** 个文件。
`input`:`files_seen 229 / games_loaded 229 / source_dirs 4 / unparseable 0 /
pooled_basename_dirs_overridden 0` ⇒ **同秒 basename 撞车一局没丢**。

### 5.1 逐机(`machines[]` 已填齐,**零 null**)

| 种子 | run | AZ | ab | ba | arm_depth | 计分局 | unfinished | status_code |
|---|---|---|---|---|---|---|---|---|
| 6743 | `212ea5` | 2a | 42 | 16 | 23.17 | 56 | 2 | `instance-terminated-by-user` |
| 6858 | `7e6283` | 2b | 41 | 14 | 20.87 | 55 | 0 | `instance-terminated-by-user` |
| 6877 | `af2874` | 2c | 28 | 14 | 18.67 | 42 | 0 | `instance-terminated-by-user` |
| 6907 | `0e6ccf` | 2d | 36 | 14 | 20.16 | 50 | 0 | `instance-terminated-by-user` |

`scored_games` **203** / `unfinished` **2** / `seeds_scored` **4/4** /
`min_arm_depth` **8** / `thin_arm_seeds` **[]** / `thin_arm_winrate_seeds` **[]`**
⇒ **一粒都没被 #269 的门挡掉,一粒都没作废。**

### 5.2 读数(铁律 4(i-a):登记的是**两个分层的读数**,不是局数)

| 量 | mean | comps_better | ab | ba | side | sign_flip | side>arm |
|---|---|---|---|---|---|---|---|
| gpm | **−64.72** | **0/4** | +22.21 | −151.65 | 86.93 | 是 | 2/4 |
| xpm | **−44.28** | **0/4** | +6.93 | −95.48 | 51.21 | 是 | 3/4 |
| deaths | **+0.67**(正 = 更差) | **0/4** | −0.46 | +1.79 | −1.13 | 是 | 3/4 |
| last_hits | **−4.03** | **0/4** | −1.50 | −6.57 | 2.54 | 否 | 2/4 |
| winrate | 0.47 | 0/4 | — | — | — | — | — |

**逐粒 gpm**:6858 **−18.34** / 6907 **−60.21** / 6743 **−68.45** / 6877 **−111.87**
⇒ **四粒同号为负**。`suggested` = **`hold_or_reject`**。

- **四量取自工具自己的 `strata.<m>` 与 `per_seed[].<m>_ab/_ba`,未手算、未按局加权**(4(i-d))。
- **4(i-c) 的读法**:gpm/xpm/deaths 三量跨层反号,`last_hits` 不反号。这四量是
  **侧偏已消除**的估计量(每粒 50/50 swap-average)⇒ 反号**不是**否决理由,它只是恒等式
  `|side| > |arm|`。⭐ **但本波的读数不靠反号承载**:承载它的是 **arm 本身四粒同号为负**
  (`comps_better` gpm **0/4**),那是关于 arm 的陈述,与侧偏无关。
- ⛔ **`winrate` 通道 DEGENERATE,工具自己拒的**:少数侧只赢了 227 局里的 **5** 局
  (`share 0.0220 < 0.20`),读数被界定在 `0.5 ± 0.044`;侧别普查 **radiant 5 / dire 222**。
  ⇒ **`0.47` 与 `0/4` 不许被当作铁律 2(b) 的证据,两个方向都不许。**

### 5.3 ⭐⭐⭐ 本轮头号产出:负读数是真的,而「成员串变差了」不是它唯一支持的解释

近七波成员串家族读数(**同一张表放在一起看是背景不是对照**):

| 波 | id 数 | gpm 均值 | comps_better gpm |
|---|---|---|---|
| W46 | 62 | −20.26 | 2/4 |
| W47 | 62 | −5.95 | 1/2 |
| W48 | 63 | **+27.25** | 3/4 |
| W49 | 61 | +11.70 | 3/4 |
| W50 | 59 | +13.76 | 2/4 |
| **W52** | **52** | **−64.72** | **0/4** |

W52 是这串里**最负的一次**,也是**唯一一次五量全 0/4**。

⚠️ **可证伪的另一半(算术,本台能说的部分)**:这个对比**不是受控对照** ——
`recover_verdict.py` 量的是「成员串腿 **减** 稳定版腿」,而 **W50→W52 之间稳定版自己动了三次**:
`slotwait`(stable-v3,`0d0a7201b8f2`)、`odbuild`+`illumove`(stable-v4,`0d3f857fb683`),
**三个 id 从「候选腿 armed」变成「两条腿都默认开」**。
⇒ 同一个负号可以由**至少两种**机制产生:(甲) 成员串本身变差;
(乙) **参照腿变强了**(promote 进去的东西在稳定版上确实有用,于是候选腿相对下滑)。
**这两者本台区分不了**,它需要的是录像/协同组的行为读数或一次专门的对照,
**不是批测台的判断**(章程:本台不做判断分析)。
⇒ 交总监 + 协同组,**GH #583**。

⚠️ 另一处**不许读成结论**的:`ckpush` 在 W52 的串里 armed,而它在 **W52 落地之后**
(`01:xxZ`)被 promote 成 stable-v5。**W52 的树 pin 在 promote 之前**
(`git merge-base --is-ancestor` 确认 `7a49dc71` 是 `3fe17f9d` 的祖先)⇒
**W52 量到的 `ckpush` 是 gated 状态的它,读数有效**;但它**不是**本波负读数的解释,
本台没有任何 per-id 归因能力(家族级读数**永远不是** per-id 读数)。

## 六、GH #454 两条腿(章程要求:**无论是否触发都要报**)

**逐机按各自 AZ 的现货报价计价**(`describe-spot-price-history c6i.4xlarge Linux/UNIX`),
再加 `机时 × $0.005/h` 的公网 IPv4,**不用单一账号级费率**:

| 种子 | AZ | 机时 | 该 AZ 报价 | EC2 |
|---|---|---|---|---|
| 6743 | 2a | 0.9136 | $0.2880 | $0.2631 |
| 6858 | 2b | 0.9117 | $0.2728 | $0.2487 |
| 6877 | 2c | 0.7608 | $0.2955 | $0.2248 |
| 6907 | 2d | 0.9167 | $0.2534 | $0.2323 |

**EC2 合计 `$0.9689` + IPv4 `$0.0175` = 单波实测 `$0.9865`。**

- **(乙1)** 账单侧单波成本 `$0.9865` **> `$1.10`?否 ⇒ 不触发**(余量 10.3%)。
- **(乙2)** 自终机器机时均值 **`0.8757 h/台`** > `1.00`?**否 ⇒ 不触发**(余量 12.4%)。
  四台**全部**是 `instance-terminated-by-user` ⇒ **没有任何一台被排除在这个均值之外**。

⭐ **第三个测量点,而且它与上一轮交棒 ⑥ 的预测形状一致**:上一轮说「`$1.10` 下一次会死在
**价格**不是机时上」。本波机时**稳稳压住**(0.8757,比 1.00 低 12%),而**四个 AZ 里三个的报价
高于 `$0.231–0.250` 这条常数赖以建立的价带**(2c 的 `$0.2955` 比带顶高 **18%**);
成本没破线**只是因为机时短**。**两条腿都不直接看价格** —— 这不是 (乙2) 的漏洞,
它是 (乙1) 的分辨率问题:价格涨、机时同时缩,就能把成本压回线下。**登记,不改常数**
(改常数是总监的事,本台的义务是报点)。

## 七、泄漏(章程第 7 步)与外来负载

**`LEAK_EXIT=0`** —— 全账号(**不按 tag 过滤**)`running,pending` = **`[]` 空**
⇒ 本波四台全部自毁、**无第五台**;常驻成本仍只有 AMI `ami-0a990a26d89c66547`。
⭐ **外来 `r7a`/`c7a` 负载本轮 region 内 0 条**(与上一轮一致,已停)。
⛔ **GH #515/#528 的立案理由一字未变**:`wave_fence.py` 自己第一行仍是
`budget filters : none <- so every instance in the account does land in this budget, whoever owns it`
—— 本台的围栏**仍读着一个它不控制的账号级数字**。

## 八、发波(章程第 4–5 步):本轮**不发**,门自己说的

`wave_throttle.py` **`THROTTLE_EXIT=3`**,逐字:
`family candidate : W52 last machine 2026-09-07T00:21:45Z -> unlock 2026-09-07T06:21:45Z <- ANCHOR`,
`now 2026-09-07T03:13:45Z`,`margin 11280s SHORT`,
`A launch at this instant BREACHES gate (i) by 188m00s.` ⇒ **THROTTLED,本轮零发波、零支出**
(除 `$0.01` 的 CE 复核)。⭐ 锚点**已自动是 W52**,本台**读的是工具打的那一行,不是自己算的数**
(上一轮交棒 ② 要求)。

## 九、收割义务(`W52_wave.json:harvest_obligations` 四条,**当场结清**)

| id | 结清 |
|---|---|
| `sir_window_read_first` | **DONE** —— AWS 引导后**第一件事**,排在任何 S3 工作之前;四行全在(第四节)。 |
| `gh454_constant_legs` | **DONE** —— 两条腿都算了都报了,**都不触发**(第六节)。 |
| `recover_verdict_per_run_download` | **DONE** —— 四个独立子目录,`pooled_basename_dirs_overridden 0`,`source_dirs 4`,`229 = 229`。 |
| `campbind_zusboltdom_admission_discharge` | **DONE** —— `queue.json` 的 `strategy-42`(`campbind`)与 `hero-29`(`zusboltdom`)已从 `admitted` 改为 `harvested_pending_verification` 并附 W52 结果行;两者**本轮 grep 逐字确认在 W52 的 arm 串里**。⚠️ 两者的**条件 (a)(armed 腿上的帧证据)仍欠录像组**,语料已在 S3。 |

## 十、铁律 6 / 11

- **`bots/`/`game/`/`tests/` 本会话一行未改**(只动 `iterations/` 下的报告、`W52_wave.json`、
  `queue.json` 两行、本章程「当前状态」节)。
- 静态门 **`GATE_EXIT=0 CLEAN`**(见文末读数),**未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行**;
  动态那半(GH #124)**不在钩子里,本轮也不声称跑过**。
- **铁律 11** 未触发任何 `requires approval`。本轮**没有任何等待**:闸 (i) 的 188 分钟不是等,
  是**门直接拒绝发波**,本台按章程转成纯收割轮并结束。

## 十一、交棒

1. ⭐⭐⭐ **总监 + 协同组 —— W52 五量 0/4 为负,而参照腿在 W50→W52 之间动过三次**
   (`slotwait` / `odbuild`+`illumove` promote 进稳定版)⇒ **负号至少有两种机制**,
   本台区分不了。**GH #583**。
2. ⭐⭐⭐ **总监 —— trunk 红在登记表形状不是数据,这是本周第三例**
   (`pullcad` → GH #579 → 本轮):`test_carrier_hero_guard.py:112-114` 把 7 个载体词项
   **硬编码**成「必须仍在活体 arm 串里」,于是 **`ckpush` 一 promote 它就红**,
   而它的立案句是「GH #473 这次改动不丢词项」。**要修的是约定不是这次的列表**。**GH #584**。
3. ⭐⭐ **总监 —— `$1.10` 的第三个测量点已到,形状与预测一致**:机时 0.8757(余量 12%)、
   而四个 AZ 里三个报价越过 `$0.231–0.250` 价带(2c 高 18%),成本压在线下**只因为机时短**。
   **两条腿都不直接看价格**,这是 (乙1) 的分辨率问题。
4. ⭐⭐ **录像组 —— 三笔条件 (a) 欠着,语料都在手上**:`campgrade`(第九轮)、
   61-id 家族(W49)、**新增 `campbind`/`zusboltdom`(W52,本轮入集结清)**。
5. ⭐ **总监 —— 开工第一条命令的管道坑第十一轮**,仍靠人记得(请给独有退出码或 wrapper)。
6. ⭐ **owner —— `$50` 仍越线**(`$62.042`,`AWS state=ALARM`,`forecast $205.267`);
   外来负载已停,但**已花的不退**,且预算过滤器仍是 `null`(GH #515/#528)。

## 十二、下一轮本台

1. **发波轮**(闸 (i) `06:21:45Z` 后解锁;**读工具打的那一行,不要自己算**);
   下一波是 **51-id 成员串**(`ckpush` 已 promote 出集,`chaos_knight` 载体词项随之从 8 项掉到 7 项
   —— **那是 promote 的正确后果,不是波的缺陷**)。
2. 四道闸**全部在起飞前跑完**(上一轮立的顺序:(iv) 排在 (i) 之前);
   `reclaim_blind.py` 本轮的输入(`update` 键)已由本轮填齐,应当直接 `exit 0`。
3. ⚠️ **开工第一条命令:重定向 + 后台 + 不设短 `timeout`**(第十一次)。
4. 载体门按 **7 项**跑,发波前 `CARRIER_GATE` 的项数变化**要在报告里点名**(它变了,而且有正当理由)。

## 十三、本轮 token

见文末追加。
