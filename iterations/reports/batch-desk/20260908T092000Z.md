# 批测台 2026-09-08T09:20Z — W56 发波轮(44-id 家族第一波,四台全 spot)

**一句话**:W56 四台按 spot 起飞、四个不同 AZ、`SPOT_RUN_EXIT` 四个 0,而本轮**唯一
值得被读的一件事是闸 (i) 先拒了一次** —— 09:21:14Z 那次 `THROTTLED (exit 3)`,
`margin 8s SHORT of unlock`,**本台没有在那个读数上发任何东西**;起飞用的是 09:21:31Z
第二次读到的 `UNLOCKED`(`margin +9s`),而且那次读数与 `spot_run.sh` 循环
**在同一个脚本里、中间一条命令都没有**。章程记的三次突破(W12 差 21s / W20 差 3m31s /
W44 差 3m00–4m19s)全是「钟只读一次,然后去做五分钟的门,回来直接发」;本轮把那个
形状**结构性地**关掉了。

---

## 一、自检(铁律 10)

⚠️ **第一条命令第二十一轮踩同族管道坑**:`2>&1 | tail -60` ⇒ 工具当场自拒,逐字
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked`,
harness 报回来的 `SELFCHECK_EXIT=0` **是 `tail` 的码**。改重定向重跑。
**第二十一次,仍然是本轮第一条命令。** 上两轮的交棒都写了「提醒不是机制」,本轮再次坐实。

已跑完的腿:

- 推送门 **`push gate armed (core.hooksPath=.githooks)`**(逐字第 2 行)。
- 锚点 **6/6 OK**(`stable-v1..v6` 全部 EXISTS/PINNED/SHIPPED)。
- **`173 distinct live gate id` / 18 PROMOTED / `FROZEN none`(没有活 gate 点名一个已 promote 的 id)/ `promote-atom constraints: OK`**。
- **trunk health(python)**:逐字 **`113 passed, 0 failed, 1 uncertifiable`**,
  `uncertifiable: tests/test_selfcheck_lua_leg.py`(腿内那条没跑成 —— **不是通过**,
  原因是它的 120s 预算撑不住:`5a0 cost: 86 file(s) ... in 120.0s (budget 120s)`)。
  ⚠️ **不许把这条读成「trunk python 本轮 GREEN」——本报告的初稿就是这么写的,而工具
  自己的归因栏把它放在另一栏**:`UNCERTIFIABLE (exit 2): trunk-red(python)`。
  也就是说,**跑到的 113 条里零失败**是真的,但**这条腿本轮没有认证 trunk**
  (它自己那条 `test_selfcheck_lua_leg.py` 没跑成)。可以说的只有前半句。
  在这个前提下登记一处变化:上一轮 python 的两条红
  (`tests/test_bots_walk_farm_only.py` / `tests/test_chain_member_census.py`)
  **不在本轮的失败清单里**。⚠️ **本台只登记读数,不认领别组的修复。**
- **trunk health(fast Lua detectors)**:**`86 tagged detector file(s), 0 failures`**,
  工具自己注明 **`FAST SUBSET, not the full suite`**(未打标签的测试不在覆盖内)。
  上一轮那条 Lua 红(`test_gated_helper_nesting_census.lua` / `tprecov`)不在本轮的失败里。
- **`SELFCHECK_EXIT=3`**,`exit sources` 逐字:`legs run 10` /
  `FINDINGS (exit 3): cadence queue-rulings owed-executions` /
  `UNCERTIFIABLE (exit 2): trunk-red(python)` /
  `NOT RUN (inside a leg): tests/test_selfcheck_lua_leg.py`。
  三项 findings 全属总监巡检范围,**本台只登记不处置**。

## 二、AWS 引导与成本

- `AWS_SETUP_EXIT=0` ⇒ `arn:aws:iam::581436534924:user/dota2bot-agent`(受限身份正确)。
- `COST_EXIT=0`。MTD **`$66.105`**(免费 `budgets`,`refreshed 2026-09-08T08:06:09Z`),
  ≥$35 ⇒ 自动 `$0.01` CE 复核 **`66.1049723885`**,**两源逐位一致**;`forecast 166.252`。
- **跨档披露**:本波**不跨任何档**。`$50` 早已跨(state=ALARM,owner 那封 Budget 邮件
  早已发出);`$80` / `$100` 仍 `NOT yet crossed`(AWS state=OK),发完本波的
  projected total 是 **$67.985**。

## 三、收割:本轮零欠

`validation/` 最新对象是 **2026-09-08T04:16Z** 的 `tpcommit+45ids-e490d4b9c513_*`
= W55 自己的;`soak/` 最新四个前缀 `spot_20260908_0321*` 也是 W55 的,**没有更新的东西**。
W55 已在 06:11Z 轮全量收割完(`RV_EXIT=0`,2/4 粒计分,verdict 落 `W55_verdict.json`)。
⇒ **本轮无收割欠账**,直接进发波流程。

## 四、⭐⭐⭐ 发波:四道闸,以及**被拒的那一次**

**(iv) 先于 (i) 跑**(章程要求),四道全部起飞前跑完:

| 闸 | 读数 | 结论 |
|---|---|---|
| (iv) reclaim-blind | `RECLAIM_BLIND_EXIT=0`,`VERDICT: not blinded -- the wave delivered 2 paired seed(s).` / **`NEXT WAVE: spot`** | 市场 = spot,**闸自己打的不是本台选的** |
| (ii) 有新东西可测 | 两条腿各自独立成立 | 见下 |
| (ii) wiring | `ARM_WIRING_EXIT=0`,`all 44 armed ids wired on HEAD` | 无 DO NOT LAUNCH |
| (iii) 围栏 | `WAVE_FENCE: CLEAR (exit 0)` | headroom `$12.015` |
| (i) 节流 | 第一次 **`THROTTLE_EXIT=3`**,第二次 **`0`** | 见下 |

**(ii) 的两条腿**:(1) 树从 `7d96067` 动到 `6aeea8c`,**并且成员串本身变了**
(45 → 44,`fieldregen` 退集,总监 §FZ);(2) 本树+本成员串的累计配对种子 = **0 < 8**。

**(iii) 的输入是被声明过的,不是默认的**:`--pending 0.780`(W55 的账单侧实测单波成本)。
理由写在这里以便复核:**W55 04:19Z 结束,而 budgets 快照 08:06:09Z 刷新,只差 ~3.8h,
落在已知的 4.3–11.3h EC2 滞后区间之内** ⇒ 按章程 (甲) 视为**尚未计入 MTD**。
工具同轮打的 `accrual check : CERTIFIED ZERO (0 accruing instances account-wide)`
是**读数不是默认值**(Ruling 4),它同时说明「取 `--pending` 这个更大的声明」。

**⭐ 闸 (i) 的两次读数,以及为什么第一次必须被写下来**:

```
09:21:14Z  margin       : 8s SHORT of unlock
           A launch at this instant BREACHES gate (i) by 0m08s.
           WAVE_THROTTLE: THROTTLED (exit 3) -- do not launch. Wait 8s.
09:21:31Z  margin       : +9s past unlock
           WAVE_THROTTLE: UNLOCKED (exit 0) -- gate (i) passes at this instant.
```

**8 秒。** 章程点名的三次突破量级分别是 21s / 3m31s / 4m19s —— 也就是说,如果本轮
按老形状走(读一次钟 → 去跑另外三道门 → 回来发波),**这一轮就会是第四次突破,
而且它会长得跟合规的一模一样**。处置**不是「记得再读一次」**:第二次读数与
`spot_run.sh` 的四次调用**被放进同一个脚本**,脚本第一条是 `git ls-remote` 漂移检查
(硬 `exit 9`),第二条是 `wave_throttle.py`,`!= 0` 直接 `exit`,之后才是发波循环。
**中间不可能插进任何东西,这不再依赖任何人记得。**

## 五、W56 是什么

- **树**:`6aeea8c4b3467b0d40dbe39d21333b12d86ff145`。`git ls-remote origin main`
  读了两次(门期一次、发波脚本第一条一次),两次一致;**并且这个 SHA 作为 `--ref`
  传给了 `spot_run.sh`** ⇒ 实例 clone 的是钉死的树,不是会动的 `main`。
- **臂串**:**44 id / 397 字节 / md5 `fe7a309fc06a229e97290b2db4c3bed3`** ——
  **现算**(`split(',')` + md5 over stripped line),与 `test_set.md` 第 3 行写的那句
  **逐位一致**。相对 W55 的变动:`-1 id`(`fieldregen` 退集,**不是 reject**,`bots/` 零 diff)。
- **⛔ 并池边界(发波时登记,不留到收割轮)**:**不与 W55/W54/W53 并池**。两条理由各自充分:
  (1) 臂串变了(45 → 44);(2) 树变了。**44-id 家族从本波起深度 = 0。**
- **选种**:窗口右移到 `[8301,8700]`(W55 用到 8010)。`seed_roster_index.py --build`
  **在写下互斥声明之前**跑完(`ROSTER_EXIT=0`,**245 粒**已索引,W55 那四粒已在其中)——
  不 build 就会把刚烧掉的四粒当成「从未用过」献回来。窗口 400 粒**全部未用**。
  载体 popcount 分布 `{"0":43,"1":121,"2":146,"3":77,"4":13}`;选法是**先最大化每个 term
  的最小载体数、再最大化总载体槽**。
- **载体闸**:`CARRIER_GATE ids=10 seeds=4 exit=0`,term **从臂串机械导出**
  (`--assert-carrier-from-arm`,GH #276),不是手打的。
  逐 term 载体数 **crystal_maiden 3 / lion 3 / obsidian_destroyer 2 / pudge 2 /
  skeleton_king 2 / spirit_breaker 2 / zuus 2** ⇒ **七个 term 全部 ≥2**,16 个载体槽,
  四粒**每粒都带 4 个 term**(窗口最高 popcount)。
  ⚠️ term 集合与 W55/W54/W53 **逐字相同**:退集的 `fieldregen` 是 generic 不是 hero-scoped,
  解空间不受影响(`10 hero-scoped, 34 generic, 0 unresolved => 7 term(s)`)。
- **采集参数**:`--slots 16 --hours 2 --rec-slots 8 --games 12`,与 W39–W55 逐字节相同;
  `--rec-slots 8` **显式传**(默认是 1)。先跑 `--dry-run`(`DRYRUN_EXIT=0`),
  **波次预算闸没有拒**。

| 种子 | AZ | 实例 | run_id 尾 | LaunchTime | SIR | lifecycle |
|---|---|---|---|---|---|---|
| 8334 | 2a | `i-05c7cef6dc4da77f7` | `_0eb22d` | 09:21:33Z | `sir-vnkzqqwg` | **spot** |
| 8358 | 2b | `i-0e0bdd7d450c34f43` | `_40e63a` | 09:21:35Z | `sir-pd4zn1dj` | **spot** |
| 8437 | 2c | `i-07eb9f9f3ab263efd` | `_cd3359` | 09:21:38Z | `sir-9dt7m1xh` | **spot** |
| 8588 | 2d | `i-04a53da665a2a74a7` | `_8ef6e7` | 09:21:40Z | `sir-64w7ma9k` | **spot** |

`InstanceLifecycle` 四台全部读到 `spot`(**`run_id` 的 `spot_` 前缀是硬写字符串,不是证据**);
四个 SIR 全部 `active / fulfilled`。四个 AZ **requested == actual,零 ring-walk**。
`SPOT_RUN_EXIT` 四个 **0**(**裸读,不经管道**),但成功仍从 `describe-instances` /
`describe-spot-instance-requests` 断言,不只从退出码。

**slate 锚点**:末台 **09:21:40Z** ⇒ 闸 (i) 下次解锁 **15:21:40Z**(首台读数会是 15:21:33Z,
`slate spread 7s`)。⚠️ **下一轮不要抄这个时刻,跑 `wave_throttle.py` 读它打的那一行。**

## 六、⭐⭐ 本轮把上一轮交出去的三个坑当场兑现了两个

**(甲)`launched_at` 键补上了。** 上一轮交棒 ③:W55 的 `machines[]` 只写 `launch_time`,
而 `wave_throttle.py` 读的是 `launched_at` ⇒ 收割轮第一次调用直接 `THROTTLE_EXIT=2`
`UNCERTIFIABLE: W55 machine 0 (seed 7921) has no launched_at`。
W56 的四台**两个键都写**(同值同源:`describe-instances LaunchTime`,GH #544)。
**现场验证**:写完记录后重跑 `wave_throttle.py`,它**认出 W56 是新锚点**
(`family candidate : W56 last machine 2026-09-08T09:21:40Z -> unlock 15:21:40Z <- ANCHOR`),
**没有 exit 2**。

**(乙)`update` 不再冒充终态。** 上一轮交棒 ②:发波轮预填的 `update` 是 SIR
**被满足**那次更新,而闸 (iv) 要的是**终态**;照抄会把存活算成 ~0.3 min 并**凭空打出
一个 BRACKET VIOLATED**。W56 的记录把这个值存成 **`sir_fulfilled_at`**,并在每台上
写死一句 `sir_update_semantics`:**收割轮必须第一手重读 `Status.UpdateTime`**。
⚠️ 上一轮救回那个读数**靠的是 SIR 行比 EC2 行活得久**(终止约 2.3h 后
`describe-instances` 按 id 查已经返回 `[]`)—— 那是运气,不是机制。

**(丙)`gates.gate_iv_inputs_harvest_owed` 这个键从发波轮就写对。**
W55 的发波轮把这个结论写在了**顶层**,`tests/test_wave_gate_keys.py` 因此
**从那一刻起就让 trunk 红着,而没有人在那一轮 push 时看见**。W56 直接写进 `gates` 下,
顶层保留作 provenance。重跑该测试:**`WAVEGATE_EXIT=0`,`340 checks, 0 failed`**
(W55 修好那轮是 329,本轮多出来的 11 条就是 W56 自己那一组)。

## 七、⚠️ 四 AZ 分散:这一波**没有**在它上面挂任何承重预期

W55 的发波记录写过一句推断:「没有 AZ 带两粒 ⇒ 单个 AZ 容量事件只损失一粒」。
上一轮的收割**第一手证伪了它**:2b 与 2d **同一秒**(03:47:16Z)双双
`instance-terminated-no-capacity`。W56 **仍然请求四个 AZ 分散**(它确实改善部分失效形状),
但**记录里不再登记任何「最多损失一粒」的收益预期** —— 那正是 W55 记录里被证伪的那一句。
**一次观测不给频率**,本台不据此改 seed 数或重跑围栏,只是不再把它当承重件。

## 八、成本

- **本波预估**:`~$1.10`(spot,GH #454 (甲) 的章程常数)。
- **当下四个 AZ 的 spot 报价**(`describe-spot-price-history`,09:00Z 起):
  2a `$0.2830` / 2b `$0.2619` / 2c `$0.2854` / 2d `$0.2583` —— 与 W55 那波
  (0.2834 / 0.2659 / 0.2854 / 0.2585)**同一价带**,无紧张迹象。
- **GH #454 两条腿是收割轮的义务,本轮不声称**:(乙1) 账单侧实测单波成本 `>$1.10`、
  (乙2) 自终机时均值 `>1.00 h/台`,**无论是否触发都要在收割轮报出来**。
  ⚠️ 上一轮登记的两个测量点仍然有效且在收紧:(乙2) 余量只剩 **5.7%**(记录最紧),
  (乙1) 归一化后只差 **4.5%**,而波次总额那个「门面数字」读着 29.4% 宽松。

## 九、泄漏

发波后 `describe-instances` 过滤 `running,pending`、**账号级不按 tag 过滤** ⇒
**恰好四台**,全是 W56 的,全部 `spot`,一台一个 AZ。**无第五台、无孤儿、无外来负载。**
常驻成本仍只有 AMI `ami-0a990a26d89c66547`。`LEAK_EXIT=0`。

## 十、铁律 6 / 11

- `bots/` / `game/` / `tests/` / `tools/` **本会话一行未改**(只动 `iterations/` 下的
  报告、`W56_wave.json`、本章程节)。
- **铁律 11**:全程无 `requires approval`,**零空转**。闸 (i) 那次 `exit 3` 之后本台
  **没有坐等** —— 它是在同一条命令链里被重读并放行的,不是靠等一个人来点什么。

## 十一、交棒

1. ⭐⭐⭐ **本台下一轮(收割)** —— W56 的收割有三件**写死的**义务:
   (a) 第一手重读四个 SIR 的**终态** `Status.UpdateTime`(**不许抄 `sir_fulfilled_at`**,
   第六节乙);(b) 在 W56 自己的记录上重跑 `reclaim_blind.py`;
   (c) GH #454 两条腿**无论是否触发都报**。
2. ⭐⭐ **总监 / harness —— 闸 (i) 本轮差 8 秒被拒**,而本台是靠「把闸和动作塞进同一个
   脚本」才没有变成第四次突破。**建议把这个形状写成工具的一部分**(例如
   `wave_throttle.py --then <cmd>`,或让 `spot_run.sh` 自己调闸),
   否则它仍然依赖每一轮的会话**自己想到**要这么写。三次历史突破 + 本次 8 秒 = 四个数据点。
3. ⭐⭐ **总监 / harness —— `launched_at` 与 `update` 语义这两条建议本轮已自证可测**
   (第六节甲/乙):W56 的记录被 `wave_throttle.py` 与 `test_wave_gate_keys.py`
   **当场接受**。建议把这两个键的语义一并写进 `test_wave_gate_keys.py`,
   与已经进去的 `gate_iv_inputs_harvest_owed` 同族。
4. ⭐ **总监 —— 闸 (iv) 那行 `measured bracket` 仍是写死常数**,W56 第四波逐字相同。
   **GH #591 第八轮零回应。**
5. ⭐ **总监 —— 四 AZ 分散的收益预期已被证伪**(第七节),W56 记录里**已删掉**那句承重推断。
   请裁定 GH #403 追评里那条是否还能用于任何承重推理。
6. ⭐⭐ **总监 —— 一个「读数看着像结论」的现场例子,登记给规则用**(第一节):
   python 腿打 `113 passed, 0 failed, 1 uncertifiable`,本报告初稿据此写下
   「trunk python 本轮 GREEN」,而工具**自己的归因栏**把同一条腿放在
   `UNCERTIFIABLE (exit 2): trunk-red(python)`。**零失败不等于已认证**,
   而这两句在同一屏上长得几乎一样 —— 与铁律 10 (ii) 的
   「`SKIP`/`UNCERTIFIABLE` 不是通过」逐字同族,只是这次伪装成一个漂亮的绿色计数。
   顺带登记(**不认领**):上一轮的两条 python 红与一条 Lua 红都不在本轮失败清单里。
7. ⭐ **owner —— `$50` 仍越线**(`$66.105`,`forecast 166.252`,预算限额 `$100`,
   刹车 `$90`)。发完 W56 的 projected total `$67.985`,**不跨 `$80`**。
8. ⭐ **总监 / harness —— 开工第一条命令管道坑第二十一轮**(第一节)。**提醒不是机制**,
   这是连续第三轮写同一句话。

## 十二、铁律 6 读数

- 静态门 **`GATE_EXIT=0  CLEAN`**,逐字 `luacheck bots game: 0 warnings` /
  `GATE_EXIT=0  CLEAN (iron rule 6 static half passed)`。
  **未用 `RULE6_BYPASS` ⇒ 本报告里没有「SKIPPED, not passed」那一行。**
- 推送门 `arm_push_gate.sh` **`ARM_GATE_EXIT=0`**
  (`push gate armed (core.hooksPath=.githooks, already set)`)。
- 动态那半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)**本轮未跑,本条不声称它**。
- python 快棘轮(推送门内)`WAVEGATE_EXIT=0`,`340 checks, 0 failed`(第六节丙)。
- 自检:**`SELFCHECK_EXIT=3`**,归因见第一节。

## 十三、本轮 token

`TOKENS total_in=6,119,481 out=41,635 turns=47`(到统计时刻为止;统计后的收尾回合不计入)
