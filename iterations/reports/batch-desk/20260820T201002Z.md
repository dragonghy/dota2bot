# batch-desk 报告 — 2026-08-20T20:10:02Z

**收割轮(§R.0 arm B `capmono` OFF)+ A−B 交付,零支出,未启动任何批测。**

## 0. 成本与泄漏

- **MTD $11.695767137**(与 14:07Z / 16:08Z / 18:06Z **逐位一致**;16:08Z arm A + 18:08Z arm B
  两波 on-demand 费用仍未入账,计费滞后延续 —— **不是泄漏**,开工时 `check_costs.sh` 与收尾时
  `describe-instances` 均确认 **0 台在跑**)。
- 未触发任何预算刹车($11.70 ≪ $90 owner 刹车线,≪ $45 月度围栏)。标准 AMI 快照 `ami-0a990a26d89c66547`
  是唯一常驻成本($0.13/天,与批测量无关)。
- 远端核对:`git ls-remote origin main` = **`658f3b4`** = 本地 HEAD(`git fetch` 又打出
  `+ 46d381d...658f3b4 (forced update)`,本地 remote-tracking ref 再次落后几十个 commit ——
  **核树一律以 `git ls-remote` 为准**,先例 20:11Z/02:06Z/06:13Z)。

## 1. 收割 §R.0 arm B(`capmono` OFF,15 id,种子 888/895/896/906,树 `11a8de33`)

标准路径 `recover_verdict.py`,**先分 run 下载再带前缀合并**(4 个 run 的 per-game 文件跨 run 同名,
08:08Z 曾静默丢 15.9%,故必须分目录):

| run_id 后缀 | 文件数 |
|---|---|
| `spot_20260820_180759_1_11a8de33…` | 79 |
| `spot_20260820_180802_1_11a8de33…` | 80 |
| `spot_20260820_180805_1_11a8de33…` | 79 |
| `spot_20260820_180808_1_11a8de33…` | 78 |
| **合并后** | **316(一个不丢)** |

**落地必查项全部通过**:
- **24 个暖场局 stamp 逐个 `11a8de3`** ⇒ 裸 SHA checkout 成功(**连续第五波通过**)。
- **S3 真实 cand 串八个 wave 一字不差 = 15 id**,`capmono` 连同 `tpwatch`/`roamstale`/`wandlimbo`/
  `axebuyblink`/`zusultx` **共 6 个全缺席** ⇒ 与 arm A(16 id,`capmono` ON)**唯一变量 = `capmono`** 成立
  (20:11Z「事后交叉验证比事前 grep 更硬」)。
- **4 种子 × 2 wave 全齐无饿死**(1 种子/实例拓扑连续第八波奏效)。

**292 局有效局(+24 暖场)** → gpm **−12.38**、xpm −20.45、deaths +0.20、last_hits −1.10,
`comps_better` 1/4·0/4·0/4·1/4,`hold_or_reject`。逐种子 gpm:888 −20.67 / 895 −15.81 /
896 **+7.31** / 906 −20.33(sd 12.51,SE 6.26)。**单臂读数,按 §R/§H 不给任何单 id 判条件 (b)、
不与历史 −24~−39 负残差带直接比较。**

## 2. A−B 逐种子配对差(本请求的交付物;arm A = `capmono` ON − arm B = `capmono` OFF)

arm A 本轮**重新从 S3 全量重算逐位复现 18:06Z 读数**(gpm −18.36,per-seed 888 −6.51 / 895 −6.47 /
896 −22.16 / 906 −38.29;304 文件),四个指标的 per-seed 都取到后做配对：

| 指标 | arm A 均值 | arm B 均值 | **A−B 均值** | sd | SE | z(sd) | z(σ0=30.24) | 同号 |
|---|---|---|---|---|---|---|---|---|
| gpm | −18.36 | −12.38 | **−5.98** | 21.10 | 10.55 | −0.57 | −0.40 | 2/4 |
| xpm | −21.35 | −20.45 | −0.90 | 12.02 | 6.01 | −0.15 | −0.06 | 1/4 |
| deaths | +0.17 | +0.20 | −0.03 | 0.05 | 0.03 | −1.10 | −0.00 | 3/4 |
| last_hits | −1.31 | −1.10 | −0.21 | 0.39 | 0.20 | −1.05 | −0.01 | 3/4 |

per-seed A−B gpm:888 +14.16 / 895 +9.34 / 896 −29.47 / 906 −17.96。

- **四个经济指标全 null。** gpm A−B = −5.98(方向上 `capmono` ON 略差,但 |−5.98| ≪ 4 种子 MDE ≈ 35.6 gpm,
  且配对差 sd 21.10 已是单臂 sd 12.51 的约 1.7 倍 —— 22:12Z 立的「配对把噪声放大到单臂约 2 倍」第三次印证:
  跨臂无可配对的共同随机源,A−B 是四个 wave 均值之差、噪声叠加)。
- **判读口径按 §R:主判据是行为检测器(录像组 clean 域拒扑率 45.2% vs 28.6%,OR=2.06,χ²≈3.02,p≈0.08),
  经济 A−B 只当上界**;`capmono` 条件 (b) 的经济 A−B = null **只是上界、不构成「无害」**(先例 16:10Z 收
  `lf_rescue` 时「null 只是上界」)。**判定归总监/录像组,批测台只做字面比对(§H 落地后不许改口)。**

## 3. 启动决策:不启动

- `queue.json` 无 pending(`director-1` 已 `done`)⇒ 走章程 4b 例行波次。
- **例行三条件**:(ii)(iii) 满足;**(i) 距上一波例行上机 16:09:02Z(arm A,重置例行计时;arm B 18:08Z
  是两臂补跑不重置,先例 02:09Z/14:12Z/20:11Z)、门槛 22:09:02Z,现在 20:10Z、差约 2 小时 —— 不满足即不启动**
  (小时级差额,无 06:07Z 那种「差 2 分钟按立法目的」的例外;先例 00:09Z/02:06Z/06:13Z/08:08Z/10:06Z/12:08Z/14:07Z)。
- **下一波例行最早 22:09:02Z。** 章程 4c 缺口 `[harness] #33` 仍开着,不重复开。

## 4. 免费交付 —— §U.0 下一波上机前功课(≥22:09:02Z 可直接上机)

按 `test_set.md` **§U.0**:下一波 = **18 id**(§R.0 那 16 id 逐字不变 + 新入集 `blinkflee` + `liondrainstop`),
种子仍 **888/895/896/906**,**单臂**(不是两臂波),申报目的 = 买 `blinkflee`/`liondrainstop` 的条件 (a)。

**cand 串(逐字照抄 §U.0)**:
```
l1trade,l5combo,midtp,suptp,tpcommit,tpdying,lf_rescue,teambrain,ownhalf,overchase,fieldregen,wandbleed,capmono,cmrguard,tpdead,zusult,blinkflee,liondrainstop
```

**静态核对(带空格 pattern `IsSoakCandidate\( *'<id>' *\)`,树 `658f3b4` = `git ls-remote` 远端真值 = 本地 HEAD)**:
- 18 个 armed id 里 **17 个各恰好 1 个读者**,`lf_rescue` 裸字面量 **0 是已知正确的**(经
  `J.IsLaneFixOn( 'rescue' )`,`jmz_func.lua:5459` → 唯一消费点 `:5765`)。
- **两个新 id 各有 1 个读者**:`blinkflee` ✓、`liondrainstop` ✓(都是「有 fixture、有检测器口径、一帧上机
  证据都没有」的 id,本波去买它们的 (a))。
- **必须缺席的五个**:`roamstale` **0 读者**(promote 落地属实)、`wandlimbo` 1(§J.1.4 机会普查未做,不 arm)、
  `axebuyblink` 1(§O.1 出集代码保留)、`tpwatch` 1(§Q.1 reject 代码保留永不 arm)、`zusultx` 1(GH #59 引入,
  不在 eligible)⇒ **出集/不 arm 唯一的闸门就是「不写进 cand 串」**。
- `retnear`/`towerreach`/`defclose`/`defstale`/`defnum`/`esaftershock`(§R 申请中)**一个都不加**。

**配置(沿用)**:4 台 × 1 种子 c6i.4xlarge **`--on-demand`**(64 vCPU 配额正好 4 台,硬顶;短波次别用 spot)、
16 槽、2h 看门狗、`--games 22`(radiant 已打满 42 局/种子配额、dire 撞 35min stall 上限,再提只会加大不对称)、
上机前现取全 40 位 SHA 并空 clone 实测 `SHA_FETCH_OK`。**成本按 14:07Z 实测 $1.56/波**(不再用旧的 $0.6-0.8),
MTD $11.70 + $1.56 ≈ $13.3 ≤ $45 围栏。

**注(U.1.1 是协同组域内、不改批测台动作)**:总监 §U.1.1 要求把 `blinkflee` 的 (a) 检测器窗口从 5s/HP>70%
改成 gate 的 2.0s/HP≥70%,否则动机帧被筛掉 —— 这是**录像组/协同组的检测器口径**,批测台只负责把
`.dem` 语料跑出来,不动这条。

**§U.0 语料预判(选种 888/895/896/906,§K.5 挑种子波次)**:`blinkflee` 挂持刀英雄(Axe/ES 的 `item_blink`
撤退分支),`liondrainstop` 挂 `hero_lion.lua` 的 ConsiderStopDrain。选种含 Lion 的只有 896(§O.2/06:13Z 实测
896 有 lion),Axe 这批 0/4(867-870/888-906 均无 Axe)⇒ **`liondrainstop` 语料只有种子 896 一套阵容,
`blinkflee` 的 Axe 载体本批缺席**;能不能买到 (a) 取决于 ES 是否在这批种子里持刀 blink。此为**语料预判、
不改被测集合**;若总监要更多 Axe/ES/Lion 语料,可考虑 §K.5 之外的选种,但按章程「以届时 `test_set.md`
逐字为准 / 不自行改变被测集合」,默认仍 888/895/896/906,已在 `[batch] #70` 下如实标注。

## 5. 局数(章程步骤 7)

- **本轮收割的 arm B(15 id,`capmono` OFF)**:888=73(radiant-cand 42 / dire-cand 31)、895=74(42/32)、
  896=73(42/31)、906=72(42/30),合计 **292** 有效局(radiant-cand **168** / dire-cand **124**)+ 24 暖场。
- **对照 arm A(16 id,`capmono` ON,18:06Z 已收割,本轮复核复现)**:888=68(41/27)、895=74(42/32)、
  896=74(42/32)、906=64(41/23),合计 **280**(radiant-cand 166 / dire-cand 114)+ 24 暖场。
- **两臂共 572 局有效局**;radiant-cand 整齐打满 `--games 22` 配额(42/种子),dire-cand 27-32 仍撞
  35min stall 上限,**再提 `--games` 只会加大不对称**,补 dire 要动 harness 侧 stall 上限。
- 本轮无在跑波次(未启动)。

## 6. 验证

本会话未改动任何 Lua(改动仅 `iterations/` 下报告/章程),且本容器**未装 `luacheck`/`lua5.1`**
(`which` 确认),铁律第 6 条本轮无适用对象。

## 7. 跨组

在既有 `[batch]` issue **#70** 下**追评**(不新开):交付 arm B 读数 + **A−B 四指标全 null(上界,判 (b)
归行为检测器)** + §U.0 语料预判。

## 8. Token 用量

```
TOKENS total_in=5,255,785 out=48,132 turns=52
```
(到统计时刻为止,收尾回合不计入。)
