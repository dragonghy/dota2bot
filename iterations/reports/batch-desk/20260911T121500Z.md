# 批测台 2026-09-11T12:07–12:4xZ — 收割轮:W65 **4/4 粒、222 局、零回收零补跑**,本台在册最干净的一波

**头号产出(⭐⭐⭐)**:**GH #352 的胜负通道不是「坏了」,是在**变坏** —— 而这是第一次有人把它按波排开看。**
最近三波少数侧占比 **0.0057 / 0.0045 / 0.0090**,#352 自己的恢复线是 **≥ 0.20** ⇒ **差 20 倍以上,而且方向是背离。**
⛔ 并且这条通道恰好是铁律 2(b)(「对**胜负**没有明显负面影响」)的决定性半边,
而今天 10:37Z 刚落地两条 promote。**总监自己把这件事标注得很诚实**(§GU 的 (b) 逐字写了「不是正面证据」),
所以这**不是对那次 promote 的异议**;是对**规则本身**的一条读数:**2(b) 的字面含义与实际执行的含义已经分家六波了。**

**第二产出(⭐⭐⭐)**:**⚠️ 收割时 `origin/main` 的成员串已经不是 W65 飞的那一串,而差一点就喂错了。**
W65 飞 **34 id**,开工时 trunk 已是 **32**(10:37:01Z 的 promote 拿走 `liondrainstop` + `tpdeathbuy`)。
**语料没被污染**(末台 10:22:22Z 自毁,早于那次 commit 15 分钟),但**收割必须喂发波时那一串**,
而它只在 `W65_wave.json` 里 —— 该记录自报的三个字段被当场重算、**逐位复现** ⇒ 记录能自证,不依赖任何一棵活树。

**第三产出(⭐⭐)**:**波级均值 −9.84 gpm 与零不可区分,本轮拒绝从它主张任何东西**(§二)。

---

## 〇、定位与本轮支出

上一轮(09:15Z)发波(W65,四台 spot)。**本轮是它的收割轮**,交棒 1 逐字点名的三件事全部照办。
**本轮不发波**:闸 (i) 锚点 = W65 末台 `2026-09-11T09:23:44Z` ⇒ 解锁 `2026-09-11T15:23:44Z`,
本轮 `12:07Z` 触发,**差约 3h10m**。

**本轮支出:`$0.01`**(`check_costs.sh` 在读数 ≥ `$35` 时自动花的 CE 复核)。**未发波,未起任何实例。**

⚠️ **上一轮交棒 6 的「自检管道坑」本轮照办并首次生效**:第一条命令就是
**重定向 + 后台 + 不设短 `timeout`**(`nohup timeout 1500 … > selfcheck.log 2>&1 &`),
**没有再经过管道** ⇒ 连续 13 轮的那个形状本轮**没有重现**。(读数见 §七。)

---

## 一、成本:数字没动,而「没动」本身要读对

`AWS_SETUP_EXIT=0` → `AWS ready: arn:aws:iam::581436534924:user/dota2bot-agent`(受限 IAM,正确)。
容器无 AWS CLI,脚本自己装的 `aws-cli/1.46.1`。

`COST_EXIT=0`;MTD **`$79.547`**,`forecast 191.407` / `budget limit 100.0`,
`budget refreshed 2026-09-11T08:15:19Z`;≥ `$35` ⇒ 自动 `$0.01` CE 复核 **`79.5474105249`**,两源逐位一致。

⚠️ **MTD 与上一轮(09:15Z)逐位相同,预算快照时刻也一字未动** ⇒ **本轮零新围栏信息**。
⛔ **不得把「六小时 MTD 没涨」读成「W65 没花钱」**:诚实 cutoff = `08:15:19Z − 11.3h` = **`2026-09-10T20:57:19Z`**,
而 W65 首台 `09:23:35Z` **在其后** ⇒ **W65 一分钱都还没落账**,这是记账滞后不是省钱。
⛔ 未用章程步骤 2(乙)禁用的「MTD 增量对得上波费」手法。

**九月余量(⛔ 本节初稿算错了一次,更正连同错法一起留在这里)**:
`$85.00` 天花板是**带到期日的裁定**(`DIRECTOR CROSSING … expires 2026-09-30T23:59:00+00:00`,GH #721)。

⛔ **初稿写的是「已用 ≈ `$80.6`、余量 ≈ `$4.4`、还能发约 3 波」,那是错的** ——
它把 MTD 之外**只算了 W65**,**漏掉了同样未落账的 `$2.350`**(W64 `$1.10` + W64 补跑机 `$1.25`)。
正确的是上一轮围栏工具自己打的那串:
`actual (MTD) $79.547` + `pending waves $2.350` + `planned $1.100` = **`projected total $82.997`**,
对 `$85.00` 天花板 ⇒ **headroom `$2.003`** ⇒ **按 `$1.10` 常数「还能发 1 波、发不了第 2 波」**
(与发波轮交棒 4 逐字一致)。

⭐ **错的方向正是危险的那一侧**(把余量说大 2 倍,`3 波` vs `1 波`),
⭐⭐ **而错法本身值得留档**:漏的不是一个难算的量,是**一个已经被上一轮工具打印出来、本台自己抄进过报告的量** ——
「未落账」这件事本台在同一节里刚强调过(cutoff `2026-09-10T20:57:19Z`),却只把它应用到 W65 身上,
**没应用到同样在 cutoff 之后的 W64 与它的补跑机**。⇒ **`pending` 是复数,而口算天然只盯最近那一波。**

⚠️ 即便更正后,本行仍是**本台按常数的推算,不是围栏工具当轮的裁定** ——
围栏本轮没跑(不发波,不需要它)⇒ **下一轮发波前必须当轮现跑 `wave_fence.py --pending … --planned …`**,
本行**不得被下轮抄作前提**。

---

## 二、收割 W65:标准路径全量重算

**语料同一性先钉死**(§GV.1):喂给工具的是 **`W65_wave.json:arm_string`**,当场重算得
`34 ids / 309 bytes / md5 7be691dcd2c2fd1ee73a630274007044`,与该记录自报三个字段**逐位相同**。
⛔ **不是当轮 trunk 那一串**(已是 32 / 284 / `3d41ae21…`)。

**下载**:四个 run 各自落进自己的子目录(GH #225,⛔ 未 `cp` 平)。
**逐 run 落盘数与 S3 侧逐个相等:120 / 160 / 154 / 120 = 554,零 `[Errno 36]`。**

`RECOVER_EXIT=0`;`files_seen 222 = games_loaded 222`,`source_dirs 4`,`unparseable 0`,
`pooled_basename_dirs_overridden 0`,`min_arm_depth 8`,**`thin_arm_seeds []`**,
`unfinished_games 0`,`scored_games 198`。

| 种子 | ab/ba | `arm_depth` | gpm | xpm | deaths | last_hits | 计分 |
|---|---|---|---|---|---|---|---|
| 10926 | 28/14 | 18.67 | **−68.40** | −14.16 | +0.41(更差) | −3.35 | 42 |
| 10927 | 39/19 | 25.55 | **+3.68** | −12.12 | +0.40(更差) | −3.46 | 58 |
| 10998 | 33/23 | 27.11 | **−8.42** | −2.95 | +0.19(更差) | −1.52 | 56 |
| 11034 | 28/14 | 18.67 | **+33.78** | +14.54 | −0.06(更好) | −1.19 | 42 |

**波级** mean gpm **−9.84** / xpm −3.67 / deaths +0.23 / last_hits **−2.38**;
`comps_better` gpm 2/4、xpm 1/4、deaths 1/4、**last_hits 0/4**、winrate 2/4;`suggested: hold_or_reject`。

**铁律 4(i-a) 分层读数**(取自工具自打的 `strata`,**未手算**,4(i-d)):
gpm `ab −21.47 / ba +1.79 / side −11.63` `sign_flip=true` `side_gt_arm 3/4`;
xpm `ab −4.52 / ba −2.82 / side −0.85` `sign_flip=false` `3/4`;
deaths `ab +0.59 / ba −0.12 / side +0.36` `sign_flip=true` `4/4`;
last_hits `ab −7.12 / ba +2.35 / side −4.74` `sign_flip=true` `4/4`。
**4(i-c)**:四个都是每粒 50/50 swap-average 的**已消侧偏估计量** ⇒ **反号是披露不是否决理由**。

⛔ **本轮拒绝从均值主张任何东西,理由是算术**:gpm 四粒 `−68.40 / +3.68 / −8.42 / +33.78`,
样本 SD ≈ **43.6**,n=4 ⇒ 均值标准误 ≈ **21.8**,而均值 **−9.84** ⇒ **|均值| 不足自己标准误的一半**。
按 4(i-c),管精度的正是 arm 自己的跨种子离散度,**而它本轮压倒效应量**。
⭐ 唯一四粒同号的是 **`last_hits`(0/4,−2.38)**,登记;
⚠️ 按 AGENTS.md 硬知识「Turbo 经济是杀/推/被动驱动,不是补刀驱动,**低 CS 是症状不是杠杆**」,
⛔ **不得据此提任何修复**,本台也不提。

⛔ **家族边界**:W65 是 34-id 串**唯一**的一波,**深度 4 粒**;不与 W62+W63+W64(37-id)并池(发波时已写死),
⭐ **也不会与下一波并池** —— 串已经动到 32 ⇒ 下一波是**第三个新家族**。
**连续三轮「纯买深度」的账,本轮仍然是零。**

---

## 三、⭐⭐⭐ 胜负通道:六波纵向普查(本轮的新东西)

全部取自各波 `*_verdict.json` 的 `winrate_side_census`,**未手算**:

| 波 | radiant 胜 | dire 胜 | **少数侧占比** | 通道 |
|---|---|---|---|---|
| W55 | 12 | 174 | **0.0645** | DEGENERATE |
| W58 | 18 | 98 | **0.1552** | DEGENERATE |
| W62 | 12 | 215 | **0.0529** | DEGENERATE |
| W63 | 1 | 175 | **0.0057** | DEGENERATE |
| W64 | 1 | 221 | **0.0045** | DEGENERATE |
| **W65** | **2** | **220** | **0.0090** | DEGENERATE |

**(甲) 它在背离恢复线,不是停在原地。** #352 的验收线 ≥ **0.20**;最近三波是 **0.0057 / 0.0045 / 0.0090**,
比 W58 的 **0.1552** 低了**一个数量级**。⛔ **本台不主张成因**(n=6、非等距、串每波都在变,都不支持一条趋势线);
主张的只是**水平**:**当前距恢复线差 20 倍以上。**

**(乙) 它是铁律 2(b) 的决定性半边,而 2(b) 已经靠经济代理量过了六波的门。**
2(b) 逐字是「批测显示对**胜负**没有明显负面影响」。§GU 两条 promote 的 (b) 逐字是
「五波家族级均值 −2.66 gpm / 722 计分局,**winrate 通道全 DEGENERATE** —— 粗粒度『无明显负面』,**不是正面证据**」。
⇒ **总监没有拿退化通道当证据,本台核过并确认**;⛔ **本节不是对 §GU 的异议**
(那两条各有 (a) 录像 WORKING 与 (c) 逻辑依据)。
要登记的是结构性的一层:**那五波正是上表前五行** ⇒ **2(b) 的胜负半边连续六波结构性缺席**。

**(丙) ⚠️ 一条让「dire 就是更强」讲不通的边界读数。**
`winner_by = {"engine_natural": 222}` / `winrate_independent_of_gold 222/222`
⇒ **222 局全部自然结束(遗迹被推掉),没有一局是 cap-25 判的** ⇒ **不是判分口径的产物**。
而 AGENTS.md 硬知识逐字「**Radiant 侧偏 ≈ +1.5k gold**」,**镜像开局两边是同样的十个英雄**
⇒ **经济上占优的那一侧输掉了 99.1% 的局**。
⛔ 本台**无逐帧证据、不主张成因**;⭐ 但两条读数并排足以说明**这是一等线索不是统计噪声**,
且它直接影响项目的**真实**目标(真实对局里我方有一半时间在 radiant)。⇒ 已开 issue 交录像组/总监。

---

## 四、GH #454 两条腿

- **(乙2) 不触发,而且这次是真量出来的。** 四台**全部** `Status.Code = instance-terminated-by-user`
  ⇒ **零容量回收**(⭐ 本台在册第一次)。机时(SIR `CreateTime` → `Status.UpdateTime`):
  `10926 0.7158h / 10927 0.9133h / 10998 0.9781h / 11034 0.7492h` ⇒ **均值 0.8391 h/台**,合计 **3.3564 机时**。
  判据 `> 1.00 h/台` ⇒ **不触发**。
  ⚠️ **量具限定**:`Status.UpdateTime` 是**请求状态翻转**时刻,不是计费停止时刻,两者可差几十秒;
  偏差方向未知,但 0.8391 距 1.00 有 **19%** 余量 ⇒ **该限定不改变结论**。
- **(乙1) 无测量点,⛔ 也不声称不触发。** 账单侧零推进(同一张快照),W65 未落账。**留给下一轮。**
  ⚠️ 顺带:**GH #743**(「波 + 补跑」的总额该不该拿去比 `$1.10`)**仍未裁**;
  ⭐ **本波零补跑 ⇒ 该问题在 W65 上不出现**,但它下次有补跑时还会回来。

---

## 五、两根棒:一根结清、一根**故意不结清**

**(甲) `gh696_winrate_measurable_first_live_read` —— 结清,已移入 `retired`。**
结清判据钉在 **stderr 新前缀**(因为本次改动是「旁边」不是「替换」,任何旧字段都作不了证)。
本轮**真实收割**逐字打出:
> `GH #696 winrate comps_better: 2 of 4 seed(s) had headroom 0 and could not vote yes (seeds 10926,10927). The pooled fraction 2/4 counts them; `comps_better.winrate_measurable` 2/2 does not. A promote bar above 2/4 is unreachable on this corpus.`

两个新键也都落盘:`winrate_forced_seeds ["10926","10927"]`(**点名不只计数**)、`comps_better.winrate_measurable "2/2"`。
⭐ **这正是该行买的东西**:立行时总监明说新代码路径「**在真实收割上一次都没被执行过**」(W62 的新字段是离线导出的)。
⚠️ **该行的 ⛔ 条款全部照办**:`winrate_measurable 2/2` **没有**被拿去过任何门,池化分数仍是 **2/4**。

**(乙) `overchase_new_body_first_reading` —— 只写 `claimed_by` / `residual`,⛔ 不结清。**
机器判据满足(W65 的 34-id 串含 `overchase`,收割于换体之后)。
⛔ **但该行的验收句要的是「第一份」,而那个词已经被 W64 占了** ——
`a_evidence_overchase_instrument` 行逐字:「W64 是换体之后的第一份波次读数(§GR.2)」⇒ **W65 是第二份**。
**本轮若自称第一份就是发表一条假的『第一』。** ⇒ 交总监(该行 `executor` 后半句本就是「总监做后续判定」)。
⚠️ **补一条该行自己没写的读数**:W65 的波级数字**不可归因到 `overchase` 或任何单条 id**(34-id bundle + §二的离散度)
⇒ **即便结清,它买到的也不是关于 `overchase` 的证据**,只是「存在一份换体后的读数」。

---

## 六、泄漏与常驻成本

**三条路径全部零泄漏**:
1. `check_costs.sh` 的 `running/pending` 区块 —— **空**;
2. us-west-2 账户级 `describe-instances`,**不加 tag 过滤**,状态 `pending,running,stopping,stopped` ⇒ **返回空**
   ⇒ 四台 W65 已全部消失、**无孤儿、无第五台、无外来负载**;
3. `describe-volumes` ⇒ **空**(被回收机器会留卷,本波零回收,与之一致)。

常驻仍只有 AMI `ami-0a990a26d89c66547`。

---

## 七、自检(铁律 10)

⭐ **管道坑本轮没有重现**:第一条命令即重定向 + 后台,**自检的退出码没有经过任何读者**。
⚠️ **但收尾时它仍在 `=== trunk health (python test suite) ===` 一节里没跑完** ⇒
**本轮没有 `SELFCHECK_EXIT=`,没读到的退出码不写,也不据本节推断 trunk 是绿的。**

**已读到并逐字登记的部分**(全在 python 腿之前):
- promote-atom:`promote-atom constraints: OK`,五条 `ok`;
- ⭐ **倒像普查(§GT 新落地的那条腿)**:`INVERSE-GATE CENSUS  live gate ids 197  armed 32` /
  **`FROZEN 0`** / `COUPLED 1` / `UNRESOLVED(armed) 0` / 收尾 `no armed id hangs under an unarmed gate -- OK`
  ⇒ ⭐ **`pullcad` 陷阱本轮为零,而这轮尤其要看它** —— 今天刚 promote 了两条 id,
  「促进一个 id 会悄悄冻死点它名的门」正是该在 promote 当天核的事,**核过,零**;
- `COUPLED` 的那一条点名 **`fieldsip` 挂在 `buyband`/`buydeep`/`buyring`/`buytower`/`fieldbuy`/`stayfield`/`stayfield2` 之下**
  (七处 early-return guard),工具自己判「Not a defect today」;
  ⚠️ **本台核过为什么今天不是缺陷**:这七个宿主**全部仍在 32-id 串里** ⇒ 无一被 promote/退集 ⇒ 域未塌。
  ⭐ **但这是一条给下一次 promote 裁定的活警告**:`fieldsip` 与它七个宿主里的**任何一个**同轮动,都要先读这一行。

⛔ **python 腿与 Lua 腿本轮无结论。** **本会话 `bots`/`game`/`tests`/`tools` 一行未改**
⇒ **无一条可能由本轮引入**。

---

## 八、铁律 6 / 11

本会话只改 `iterations/` 下的文件(报告、`test_set.md` §GV、`W65_wave.json`、`W65_verdict.json` + stderr、
`owed_executions.json`、章程本节);**`bots`/`game`/`tests`/`tools` 一行未改**。三条腿读数见「补记」。

**铁律 11**:全程**零** `requires approval`、**零空转**(自检后台跑,收割与成本/泄漏并行推进)。

---

## 交棒

1. ⭐⭐⭐ **总监/owner —— 铁律 2(b) 的胜负半边已经结构性缺席六波**(§三)。
   请裁:(a) 在通道恢复前,2(b) 是否就**明确改写**成「经济代理量 + 录像 WORKING」,
   让**规则的字面与执行一致**(现状是每条 promote 各自在正文里打补丁,靠裁定者的诚实兜底);
   (b) #352 的 `≥0.20` 恢复线**是否还是一条可达的线** —— 最近三波距它 20 倍以上且在背离。
   ⛔ **本台不代裁,也不建议放宽或收紧任何门。**
2. ⭐⭐⭐ **录像组 —— 一等线索:经济占优的一侧输掉 99.1% 的局,且 222/222 是自然结束**(§三丙)。
   建议验收:**逐帧看一局 radiant 方的自然落败**,定位遗迹被推掉前最后一个可挽回决策。
   ⛔ 本台无逐帧证据、不主张成因;⭐ 这条直接影响真实对局(我方一半时间在 radiant),
   **不是只影响量具**。
3. ⭐⭐ **总监 —— `overchase_new_body_first_reading` 该不该按 W64 结清**(§五乙)。
   本台只认领不结清,理由是「第一份」已被 W64 占;附一条该行自己没写的限定:**结清也买不到关于 `overchase` 的证据**。
4. ⭐ **总监 —— `fieldsip` 的七处 COUPLED 是给下一次 promote 裁定的活警告**(§七),
   今天不是缺陷(七个宿主全在串里),但它与任何一个宿主同轮动都要先读那一行。
5. ⚠️ **上一轮交棒 6(自检管道坑)本轮照办并首次生效**(§七);
   ⚠️ 但自检**仍未跑完 python/Lua 两腿** ⇒ **本轮无 `SELFCHECK_EXIT=`**。
6. ⚠️ **GH #743 仍未裁**(§四),本波零补跑所以没碰上,下次有补跑会回来。

## 下一轮本台

1. **闸 (i) 锚点 = W65 末台 `2026-09-11T09:23:44Z` ⇒ 解锁 `2026-09-11T15:23:44Z`。**
   下一轮若在其后触发,闸 (i) 放行。
2. ⛔ **围栏/闸/成本一律当轮现跑** —— 本报告的 `$79.547`、cutoff `2026-09-10T20:57:19Z`、
   以及 §一**更正后**的「还能发 1 波、发不了第 2 波」**全部作废,不得被抄作前提**
   (那一句是本台按常数的推算,不是围栏当轮的裁定;⛔ 更不要抄初稿那句错的「约 3 波」)。
   **发波前必须当轮现跑 `wave_fence.py --pending <所有未落账波次之和> --planned <本波>`** ——
   ⚠️ `--pending` 是**复数**,§一那次算错正是只数了最近那一波。
3. **闸 (ii) 现在满足**:串已从 34 动到 **32**(§GU 的两条 promote)⇒ 第一析取支成立。
   ⛔ **但下一波是第三个新家族、深度 0**,收割时不得写成「W65 再加四粒」。
4. **结清 GH #454 (乙1)**:下一轮若拿到新的预算快照且 cutoff 越过 `09:23:35Z`,W65 的账单侧就有测量点了。
5. **回填本轮 issue 号**到 `test_set.md` §GV.3 的「立案回填」行(见「补记」)。

---

## 补记(第一次 push 之后;GH #290 顺序)

### (A) ⭐⭐ 推送门抓到的是**本轮自己的**缺陷,而它的失效形状正是那个字段存在的理由

第一次 push **被拒**:`PUSH1_EXIT=1`,`tests/test_pending_rulings.py (exit 1)` / `625 checks, 1 failed`,逐字:
> `FAIL: live owed row 'overchase_new_body_first_reading' carries a claim this tool cannot read: claimed_at='2026-09-11T12:1xZ' is not an ISO-8601 UTC instant (the fuzzed house style `T07:xxZ` is not a time) -- reading this as OWED`

⭐ **这条红是本台本轮自己造的,不是 trunk 上原有的。** 本台把**叙述字段**的模糊时刻写法
(`T12:1xZ`,本文件通篇在用,那是对的)带进了一个**机器字段**。
⭐⭐ **值得留档的是它的代价形状**:出错的结果**不是崩溃**,是
**一条认领被读回成「未认领」** —— 也就是 `claimed_by`/`claimed_at` 这对字段
(GH #518)**当初正是为了防止的那种掉棒**。⇒ 若没有这道门,本台会一边在报告里写「已认领」,
一边让 `pending_rulings.py` 继续把它报成无人认领。

**修法**:`claimed_at` 改成真实 ISO-8601 瞬时 **`2026-09-11T12:21:38Z`**(叙述用的 `claimed_by` 保持轮次写法);
⛔ **没有**为了让门变绿去放宽那条断言。**当场复现修复**:`python3 tests/test_pending_rulings.py`
⇒ `PENDING_RULINGS_EXIT=0` / **`625 checks, 0 failed`**。
该行 `residual` 里也把这条失效形状留了档。

### (B) 铁律 6 三条腿(两次 push 的读数)

- **第一次**:`GATE_EXIT=0  CLEAN (iron rule 6 static half passed)`(`luacheck bots game: 0 warnings`)/
  `py gate: 96 ran, 1 findings, 0 uncertifiable, 29.8s` ⇒ **REFUSED**(见 (A))。
- ⚠️ **顺带登记一条本台没动、也不归本台的读数**:py 闸自报
  `12 new test(s) not in the manifest were run anyway`(点名 `test_inverse_gate_census.py`、
  `test_lua_gate.py`、`test_verdict_winrate_comps.py` 等 12 个)⇒ **清单落后于树**。
  本轮**不改清单**(那是 harness/总监的编排权),**登记交出去**。
- **第二次**:读数见下方 `PUSH1_EXIT` / `PUSH2_EXIT` 行。

### (C) 已发表

见本节末尾「立案回填」——issue 号在 push 之后开出并回填(GH #290:**先 push 再发表引用**,
因为 issue 正文要引 `test_set.md §GV.3`,读者按 `origin/main` 解析)。

### (D) 已发表 + 回填

**GH #749 `[batch]`** —— §三 全文(通道纵向普查 + 请裁两条 + 给录像组的一等线索 + 复现脚本)。
发表前草稿过 `claim_precheck.sh`:**`PRECHECK_EXIT=0`**,逐字 `clean` /
`OK to publish: every citation resolves on origin/main.`(`paths cited 2` / `charter sections cited 1` / `refused 0`)。
⛔ **不开重复 issue**:#749 同时承载交棒 ① 与 ②(总监/owner 裁规则、录像组逐帧),
两件事同源于一份读数,拆成两条会让第二条失去它的论据。

号已回填 `test_set.md` §GV.3 的「立案回填」行。
⭐ 那一行顺带留了一句:真号确实是 `#749`,**但那是开出来才知道的,不是预判对了** ——
`#748` 与 `#749` 之间没有任何东西保证没有别的流插队,**猜中过一次不构成下次可以猜**。

### (E) 三次 push 的铁律 6 三条腿

| | 静态(Lua) | py 棘轮 | Lua 快测 |
|---|---|---|---|
| **push1(分支)** | `GATE_EXIT=0  CLEAN (iron rule 6 static half passed)` | `96 ran, 1 findings, 0 uncertifiable, 29.8s` ⇒ **REFUSED** | 未到 |
| **push2(分支)** | `GATE_EXIT=0  CLEAN` | `96 ran, 0 findings, 0 uncertifiable, 28.8s` | `SKIPPED BY SCOPE` |
| **push3(main)** | `GATE_EXIT=0  CLEAN` | `96 ran, 0 findings, 0 uncertifiable, 29.2s` | `SKIPPED BY SCOPE` |

⚠️ **第三条腿是 `SKIPPED BY SCOPE` 不是绿**,工具自己的限定逐字:
`This is a SCOPE decision, not a pass and not a skip of a gate that had something to say (GH #624).`
(本会话 `bots`/`game`/`tests` 一行未改 ⇒ 该 scope 判断与事实相符。)

`ARM_EXIT=0`(`push gate armed (core.hooksPath=.githooks)`);**全程未用 `RULE6_BYPASS`**;
`REBASE_EXIT=0`;`PUSH2_EXIT=0`;`PUSH_MAIN_EXIT=0`(**`5f10cb0a..1aef4867`**)。

---

## 本会话 token 用量(铁律 8)

```
TOKENS total_in=7,715,405 out=59,778 turns=64
```
(统计时刻之后的收尾回合不计入。)
