# 批测台 2026-08-25T03:12Z —— 纯收割轮:W8(owner P1 取证波)收割完成,条件 (b) 的读数交总监

**一句话**:W8 整波落地并全量重算(**301 局 / 277 计分 / unfinished 0**),owner P1 的 DoD 第 3 步
**两半都齐了** —— (a) 由录像组 01:35Z 交卷(`pulllane` = WORKING),(b) 由本轮交出:
28-id 全集 vs `stable-v2` 池化 gpm **−23.27**、**winrate 0.432**(中性 0.5),comps gpm **0/4**、winrate **0/4**,
**四个种子的 winrate 首次全部落在中性以下**(0.346/0.464/0.450/0.467)。
**但这是全集的合成读数,不是 `pulllane` 的读数**,而本台上一轮已预先禁掉用 W7↔W8 跨波差给 gpm/winrate 下结论
⇒ **本台不判读、不归因,把数交给总监**。本轮**零支出、零启动**(例行波三条节流的 (i) 距上一波仅 2.9h,不满足),
MTD **$36.245**(读数未刷新),无泄漏。另有四条审计发现,其中一条是新的:**实例自产 verdict 这次 4/4 run 的
ba 腿都少 2–3 局**,且某种子的 gpm 因此差 **6.8**(−3.27 → −10.08)——与被测效应同量级。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` ⇒ **worst exit 3**。

- **CADENCE 10 finding**:全部是 08-24 白天五条流停摆的残影(batch-desk 12.2h、director 12.0h、
  hero 11.9h、replay-check 12.0h、strategy 12.6h),外加本台自己「08-24T18:15Z → 08-25T00:19Z 6.1h」
  与 hero/replay-check 的同类间隔。五条流现均已恢复,不阻塞,不开 issue。
- **trunk python 套件 19 passed / 0 failed**。
- UNLANDED 与录像组 01:35Z 的判读一致(工具点的是换过的 SHA,内容已在 main),**不重复开 issue**。

## 1. 成本与预算(三线全未触及)

- MTD **$36.245**,来源 `budgets/dota2bot-batch`(免费),**budget refreshed `2026-08-24T21:12:20Z`**;
  脚本按 `$35` 阈值自动花 **$0.01** 用 CE 复核 ⇒ **$36.2450323784**,逐位吻合。
  forecast **64.471** / limit **100.0**。本轮 CE 花费 **$0.01**,除此之外**零支出**。
- **纪律(乙)照办**:读数与上一轮(00:19Z)**逐位相同**,而窗口内 W8 已跑完 —— 这**不是**「W8 没花钱」,
  是 **budgets 快照停在 08-24T21:12Z、W8 的 EC2 行(00:16→00:47Z)滞后未到货**。这是时间戳给的,不是拟合。
  **未用**「MTD 增量对得上波费」做任何推断。
- **纪律(甲)的围栏算术**(留给下一轮):MTD $36.245 + W7 $1.48(**8.9h 前,在 12h 窗口内**)
  + W8 $1.48(**2.9h 前**) = **$39.21**。**$45 围栏余 $5.79 ≈ 1 波按需**;越 **$50** 需 **owner 批准**。
  ⚠️ **W7 落在 12h 窗口的边缘,下一轮(约 05:1xZ)它就滚出窗口了** —— 那时余量会「凭空」多出 $1.48,
  **那是窗口滚动不是省下的钱**,不要据此放宽。

## 2. ⭐ 收割 W8 —— owner P1 条件 (b) 的读数

`recover_verdict.py` 从 S3 逐局数据**全量重算**(标准收割路径),4 run 池化:

| 量 | 读数 | comps_better |
|---|---|---|
| gpm | **−23.27** | **0/4** |
| xpm | −16.27 | 1/4 |
| deaths | +0.15 | 0/4 |
| last_hits | −0.52 | 1/4 |
| **winrate** | **0.432**(中性 0.5) | **0/4** |

**局数**:301 局 / **277 计分** / **unfinished 0**;`suggested` = `hold_or_reject`。

逐种子:

| seed | ab | ba | gpm | xpm | winrate |
|---|---|---|---|---|---|
| 888 | 40 | 30 | −36.35 | −24.49 | **0.346** |
| 895 | 42 | 28 | −31.12 | −21.17 | 0.464 |
| 896 | 40 | 30 | −15.52 | +0.50 | 0.450 |
| 906 | 41 | 26 | −10.08 | −19.92 | 0.467 |

### 2.1 ⭐ 真发现:**四个种子的 winrate 首次全部低于中性**

历史全集波的 winrate 读数依次是 **0.243 / 0.394 / 0.500 / 0.491(W7) / 0.432(W8)** —— 数值上 W8 落在
既有区间**内部**,**不是**突然变坏;新的是**分布形状**:W7 的逐种子散布 0.450–0.569 **骑在中性两侧**,
W8 的 0.346–0.467 **整体在中性以下,0/4**。合起来的可陈述事实是
**「测试版(armed 全集)作为一个合成体,五次有记录的全集波读数从未高于中性」**。

**这句话的边界必须一起读**:

- **它是全集的读数,不是任何单个 id 的读数。** 28 个 armed id 里 `pulllane`/`towerfear` 只是新进的两个。
- **不许用 W7 差 W8 来归因。** 本台 00:19Z 的预登记原文就写了:W7↔W8 是**跨波**比较,
  该类量**波间自噪声与效应同量级**,**只可用于帧证据侧的定性对照,不许对 gpm/winrate 下结论**。
  本轮**照办**:虽然 0.491 → 0.432 看着像 `pulllane`+`towerfear` 的代价,**本台不宣称这一点**。
- **winrate 不是第二个证人。** `winner_by` = `economy_10min_cap` **301 / 301**,
  `winrate_independent_of_gold` = **0/301** ⇒ winrate 是 gpm 的符号化粗读。gpm 0/4 与 winrate 0/4
  是**同一个读数的两种粒度**,不是交叉验证。

### 2.2 计量三条(GH #148)照办 —— ab/ba 两层分层读数

| 分层 | n(计分) | gpm | xpm | winrate(候选腿) |
|---|---|---|---|---|
| **ab**(cand = radiant) | 163 | **+63.58** | +58.45 | **0.693**(113/163) |
| **ba**(cand = dire) | 114 | **−110.12** | −90.99 | **0.175**(20/114) |

**两层看起来反号,但这不是「反号 = 噪声」那一档,是侧偏**,而且可以分解开来给:

- 侧偏估计 = (ab − ba)/2 = **+86.85 gpm**(与 CLAUDE.md 记的「Radiant 侧偏 ≈ +1.5k 金」同一件事);
- 效应估计 = (ab + ba)/2 = **−23.27 gpm**(即上表的池化值,swap-and-average 后的量)。
- **换成不受侧偏污染的读法,两层同号**:Radiant 的胜率在 ab 层是 **0.693**、在 ba 层是 **0.825**;
  若只有侧偏、armed 腿无效应,这两个数该相等。差 **13.2pp** —— **armed 腿在两个物理层里各自都吃了亏**,
  方向一致。这也正是池化 winrate 0.432 = (0.693 + 0.175)/2 的来源。
- 计数类量(逐种子 winrate)按 (ii) 报**均值 + 逐种子分布**,不报中位数,见 2.1 的表。

### 2.3 交棒的措辞(不越权)

本台**不做判读**。交给总监的是:铁律 2 的条件 (b) 问「批测显示对胜负有无明显负面」,
W8 给出的答案是 **全集腿 winrate 0.432 / 0/4 / gpm −23.27 / 0/4**,
而 (a) 已由录像组 01:35Z 判为 **WORKING**。**(b) 的这份读数是全集的,不能直接当作 `pullcamp`+`pulllane` 的 (b)**;
若总监要的是**单 id 的 (b)**,那需要一次隔离波(**成本 $1.48,围栏余 $5.79**),
这一步归总监裁,本台不自行发。

## 3. 语料侧四项审计

### 3.1 `--rec-slots 12` 验收:**第七次 exit 0**

`rec_slot_cost.py … --baseline rec_slot_baseline.json` ⇒ **exit 0**,301 局,4 个对照槽,
box factor **1.024**,**`0 recording slot(s) beyond tolerance`**。

- **slot 1 净值 −1.5%**。历史序列 −3.3% → −3.2% → −4.0% → −2.4% → **−1.5%** ⇒
  上一轮撤回的「单调走深」**本轮再次被证伪**,撤回站得住,**不需要 harness 加急**。
- **slot 1 仍是唯一赤字录制槽**(slot 2–12 净值 **+4.2% ~ +6.7%** 全正),结构性理由不变:
  `soak_loop.sh:170` 的扁平镜像只留 slot 1 ⇒ 它多一次上传。
- 吞吐侧同向:录制槽 19–20 局/槽,对照槽均值 16.25 局/槽(**录制槽反而多 +2.75 ~ +3.75 局**)。

### 3.2 归属分歧(GH #152):**第五例,仍然只在 slot 1**

236 份 sidecar:`method` = `logname` **236/236**;`by_logname == by_hostname` **235/236**。
唯一分歧:`…ae722d/20260825_003605_slot1` —— logname 判 **slot1**,hostname 判 **slot12**。
`candidates` 分布 5–12(众数 11)。

累计:**1/236 + 0/466 + 2/235 + 1/236 = 4/1173,四例无一例外全是 `slot1` 撞 `slot1X`**
(slot10 / slot12 / slot12)。上一轮钉下的期望值 **「slot 2–12 恒 100%;slot 1 在 `--rec-slots ≥ 10` 下
以池共存概率分歧」** ⇒ **第三次被数据支持,不修正**。`hostname_hits` 保持作废。**本波零错误归属**
(主判据 236/236 全对)。**任何非 slot 1 的分歧仍属新缺陷,必须点名。**

### 3.3 `.dem` 丢失(GH #147):本波 **1/236**

逐 run `.dem` / sidecar:364fb1 **59/59** ✅、ae722d **59/60 ⚠️ 少 1**、6a2b32 **60/60** ✅、c1eace **57/57** ✅。
⇒ 本波丢 **1 局**(尾局竞态),**累计 2/937**。仍不撤 #147(机制在),**保持降级、不加急**。
给录像组:`dem21/spot_20260825_0016*/` 共 **235 个 `.dem`**。

### 3.4 ⭐ 真发现:实例自产 verdict 这次 **4/4 run 都短**,且差值与效应同量级

第四次对账(自产 vs 全量重算):

| seed | 自产 ab/ba | 重算 ab/ba | 自产 gpm | 重算 gpm | 差 |
|---|---|---|---|---|---|
| 888 | 40 / **27** | 40 / **30** | −37.20 | −36.35 | −3 局 |
| 895 | 42 / **26** | 42 / **28** | −34.70 | −31.12 | −2 局 |
| 896 | 40 / **28** | 40 / **30** | −16.51 | −15.52 | −2 局 |
| 906 | 41 / **24** | 41 / **26** | **−3.27** | **−10.08** | −2 局,**gpm 差 6.8** |

三件事同时成立:

1. **短的一律是 ba(dire)腿,ab 腿四个种子逐位相同** ⇒ 机制清楚:自产 verdict 在**最后一批 dire 局
   上传落地之前**就算完了。
2. **上一轮那句「又是 888 的 ba 腿」到此可以撤销**:本波 **4/4 全短**,不是某台机器特异,
   **是收波顺序的通病**(与 §3.3 的尾局竞态同源)。
3. **代价是可量的**:s906 的 gpm 从 **−3.27** 变成 **−10.08**,差 **6.8** —— 而本波要测的效应本身是 **−23.27**。
   ⇒ **自产 verdict 只能当免费早期预览,任何进结论的数字必须走 `recover_verdict.py`**,第五次被坐实。

### 3.5 GH #167 的预测**兑现**,且找到零改动的绕法

上一轮预测 W8 的 verdict basename 会 **~284 字节**。实测 **285**(> `NAME_MAX` **255**)⇒
`aws s3 cp --recursive validation/` 对四个 verdict **仍然全部 `[Errno 36]`**。

**新信息(可直接写进 #167)**:**S3 侧 key 与内容完好,缺陷纯在本地落盘文件名**,
所以 **`s3api get-object --key '<长 key>' <短本地路径>` 能完整取到**,本轮四份 verdict 就是这么拿到的
(`v1..v4.json`)。⇒ **#167 不阻塞任何收割**,优先级可降;要修就修 `validate_onspot.sh` 的命名
(例如用 armed 串的哈希代替全串)。**坑仍在**:`--recursive` 的失败混在进度输出里,`| tail` 之后
极易读成「跑完了」—— 判据必须是**下载后 `ls` 出的文件数**,不是命令看起来的结束。

## 4. 启动决策:**不发新波**(节流条件 (i) 不满足)

- **(i) 距上一波(W8,`00:16:28Z`)仅 2h 56min,差 3h 以上未满 6h ⇒ 不满足,即不启动。**
  与 2026-08-19T06:07Z 那次「差 2 分钟」的目的解释不同,本轮**无例外情形**:余量本来就只剩一波,
  没有任何理由抢时间。**下一波最早 06:16Z。**
- (ii) 满足(树 `aadd993` → `089bee2` 有变更;当前树+成员串累计种子 0 < 8)。
- (iii) 满足但**紧**:$39.21 + $1.48 = **$40.69 ≤ $45**(见 §1)。
- `queue.json` 里**没有**可发的显式请求:唯一带 `APPROVED_CONDITIONAL` 且未跑的是
  **`strategy-5b`(W6)**,它**仍卡在录像组的解锁棒上**(`strategy-5b` 的事件轴量级,零 AWS)——
  **这是第五轮催,也是唯一还挡着 W6 的东西**。其余 `pending` 条目自报「NO NEW WAVE NEEDED /
  搭车,零 AWS 增量」,不构成发波理由。
- 远端 main tip 核对照做:`git ls-remote origin main` = **`089bee2`**(W8 测的是 `aadd993`,
  之后 hero/strategy/replay-check 三条流各落了报告类改动)。

## 5. 局数(铁律 7)

- **(a) 上一波(W8)最终有效局数**:**277 计分 + 24 暖场 = 301**,**unfinished 0**。
  per seed / per side:888 **40/30**、895 **42/28**、896 **40/30**、906 **41/26**。
  **ab/ba 不对称(radiant 波普遍多 10–15 局)是既有形态,不是本波异常**(W7 同形)。
- **(b) 本轮在跑波次**:**无**(未启动)。S3 `soak/` 前缀 **192**(188 → 192,W8 四个 run 已全部落地)。

## 6. 泄漏检查

开工与收尾各一次,四层 + spot 请求:

| 层 | 开工 | 收尾 |
|---|---|---|
| 实例(pending/running/shutting-down/stopping/stopped) | **0** | **0** |
| 可用卷 | 0 | 0 |
| 自有快照 | 1(AMI 自带,常设) | 1 |
| EIP | 0 | 0 |
| spot 请求(open/active) | 0 | 0 |

⇒ **无泄漏**。本轮无活波次 ⇒ 收尾判据就是章程要的**「0 台在跑」**,已满足。
W8 四台在 **00:47Z** 前后自毁完毕(verdict 上传时间 00:47:21–00:47:49Z),**没有留下第五台**。

## 7. 固定栏位

- `soak/` **192**(188 → 192)、`dem21/` **44**(40 → 44)、`unattributed/` **0**;
- `validation/` 最新 **2026-08-25T00:47:49Z**(W8 的四份 verdict + run.log);
- 远端 main tip **`089bee2a8a179889d183a6f80d3ff09299cb806c`**;
- 远端 `stable-v1` = `6db5921` / `stable-v2` = `136a332` 均存在(tag 仍空 —— **第二轮提醒总监**)。

## 8. 验证(铁律 6)

- `bots/` / `game/` **逐字未动**(本轮改动只在 `iterations/` 下)⇒ 铁律 6 的两条命令**无适用对象**;
- 容器内无 `luacheck` / `lua5.1`,**不声称跑绿过 Lua 全量**;
- `queue.json` 按原 `indent=2` + 原无尾换行回写,`git diff --stat` **4 增 4 删**,无重排噪声。

## 9. 交棒(铁律 9)

1. **⭐ 总监 —— owner P1 的 (b) 到货,球在你那里。** (a) = WORKING(录像组 01:35Z)、
   (b) = 本报告 §2(**全集读数,非单 id**)、(c) 归协同组。**需要裁的是一个具体的岔路**:
   接受全集读数当 (b),还是要一次 **$1.48 的 `pullcamp`+`pulllane` 隔离波**(围栏余 **$5.79**)。
   本台不自行发隔离波(§2.3)。
2. **⭐ 总监 —— 按需 vs spot(GH #158)现在真的卡排期了(第二轮催)。** 围栏余 **$5.79 ≈ 1 波按需**;
   裁 (B)/(C) 无需 harness 改动,本台下一波即可执行。**若 §9.1 选了隔离波,这两件事会互相挤。**
3. **⭐ 录像组 —— W6 解锁棒(第五轮催)**:`strategy-5b` 的事件轴量级,**零 AWS**,
   **这是唯一还挡着 W6 的东西**。
4. **录像组 —— W8 语料已就位**:`dem21/spot_20260825_0016*/` **235 个 `.dem`**,
   `strategy-12`(`towerfear`)的五条正/反判据还没人量(**反向判据「死亡数不许上升」不许省** ——
   波级 deaths +0.15 是 10 人全局均值,**替代不了**它)。跨 run 统计一律用 run 前缀限定 tag。
5. **总监 —— 载体门 PARTIAL 的惰性种子 = 波内零腿**(第四轮催)。
6. **总监 —— `strategy-7`/`strategy-8` 正式 resolve 并排期**(本台不代宣);
   **`stable-v1`/`stable-v2` 的 tag 仍未打**(分支引用已建)。
7. **`[harness]` 棒**:**#167 降级 + 绕法**(§3.5,`s3api get-object` 到短本地名可完整取回,不阻塞收割)、
   **#152 第五例**(§3.2,域再次吻合,期望值不修正)、**#147 保持降级**(§3.3,累计 2/937)、
   **新记一条**(§3.4:自产 verdict **4/4 run 的 ba 腿短 2–3 局**,gpm 可差 6.8 —— 与 #147 同源,
   不另开 issue,并入 #147 追评)。

## 10. Token 用量(铁律 8)

```
TOKENS total_in=4,117,939 out=34,004 turns=41
```

(统计时刻为收尾前;此后的 commit/push 回合不计入。)
