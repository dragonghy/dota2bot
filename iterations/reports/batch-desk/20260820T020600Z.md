# 批测台工作单元 — 2026-08-20T02:06Z

**纯运维/备料轮,零支出,未启动任何批测。**

## 1. AWS 引导与成本

- `session_setup.sh`:容器初始无 AWS CLI,已自动装(aws-cli/1.46.0);
  身份 `arn:aws:iam::581436534924:user/dota2bot-agent`(受限批测用户,正确)。
- `check_costs.sh`:**MTD $4.0579158114**,在跑/待起实例 **0 台**,
  唯一常驻成本仍是 AMI `ami-0a990a26d89c66547` + 快照。
- **未触发任何预算刹车**($90 刹车线 / $45 月度围栏,均有数量级余量)。
- **计费滞后:这是连续第七轮读到完全相同的 $4.0579。** 尚未入账的机器:
  14:11Z 的 3 台 + 18:08Z 的 4 台 + 20:09Z 的 4 台 + 22:11Z 的 4 台 = **共 15 台
  on-demand**,估约 $5-6。**这不是失控** —— 每一轮 `describe-instances` 都确认 0 台残留,
  滞后的是账单不是机器。预计后续某轮一次性跳到 ~$9-10,届时不要误读成泄漏或新支出。

## 2. 收割:无新数据

- `s3://.../soak/` 最新前缀仍是 22:11Z 那波的四个
  `spot_20260819_2211xx_1_829202ac…`,**已在 00:09Z 轮全量收割完毕**(276 局)。
- `s3://.../validation/` 无 00:09Z 之后的新对象。
- 22:11Z 之后没有任何实例跑过 ⇒ **本轮结构上不可能有新数据**,不是漏收。
- `iterations/queue.json`:`requests` 为空(`director-1` 已 `done`)。

## 3. 启动决策:**不启动**

章程 4b 的例行波次三条件:

| 条件 | 实测 | 判定 |
|---|---|---|
| (i) 距上一次**例行**波次启动 ≥ 6h | 上一波 **22:11Z**,现在 02:06Z = **3h55min**,差约 **2 小时** | ❌ **不满足** |
| (ii) 有新东西可测 | `bots/` 有变更(hero 组 `816d0b3` 修 GH #50 largo 崩溃) | ✓ |
| (iii) 当月已花 + 本波预估 ≤ $45 | $4.06 + ~$0.7 ≈ $4.8 | ✓ |

**(i) 不满足即不启动。** 与 06:07Z 那轮「差 2 分钟、按立法目的照常启动」不同,
本轮差额是**小时级**,无例外情形(先例:00:09Z 差 4h、10:06Z 差 2h、16:10Z 差 2h,
三轮都是不启动)。**下一波例行最早 04:11Z。**
queue.json 无 pending 请求 ⇒ 章程 4a 无触发。

## 4. 本轮交付(免费,零 AWS 支出):下一波的选种可以白拿 3 倍取证语料

### 4.1 背景

§K.5 裁定「(a) 取证波挑种子,(b) 经济波不挑」,并点名下一波用
**872 / 875 / 885 / 887**(给 `axebuyblink`),另给了一组「若要给 `liondrain` 铺路」
的备选 **872 / 910 / 974**。00:09Z 轮已用真实 S3 地面真值验过 `seed_draft.py`
(4/4 种子、40/40 英雄槽逐位命中),本轮 `--selftest` 复跑仍 PASS。

### 4.2 发现:存在同时含 Axe + Zeus + Lion 的种子,而且够四个

`seed_draft.py --find axe,zuus,lion --from 871`:**872 / 910 / 1024 / 1043**
(下一批 1091 / 1176)。四个种子里 **Axe 全部是 pos3**(池子里 `axe,3,focus`
本就只有 pos3 资格),Zeus 落 pos2/pos4,Lion 落 pos4/pos5。

| 种子集 | 不同英雄数 | axe | zuus | lion | skeleton_king | crystal_maiden |
|---|---|---|---|---|---|---|
| 867-870(上一波,连号) | 27/42 | **0/4** | 1/4 | **0/4** | 2/4 | 2/4 |
| **872/875/885/887(§K.5 指定)** | 25/42 | **4/4** | **1/4** | **1/4** | 1/4 | 2/4 |
| **872/910/1024/1043(本轮算出)** | 23/42 | **4/4** | **4/4** | **4/4** | 1/4 | 1/4 |

**价值**:下一波的 armed 串里 `zusult` 是 armed 的,而 §K.5 指定的种子集只给它
**1/4 种子**的 Zeus 语料(上一波正是 1/4 = 70 局,§J.0 的检测器要数「压住 W 之后
那笔蓝有没有留到大招」,70 局且零阵容多样性)。换成三重集,`zusult` 的 (a) 语料从
**~70 局 → ~276 局(4×)**,且拿到四套不同阵容;`axebuyblink` 的 4/4 覆盖**一点不损失**;
`liondrain`(尚未入集)将来要语料时**已经躺在同一批 `.dem` 里**,不必再排一波。
**增量成本 $0** —— 同样 4 台 × 1 种子,只是换了四个数字。

### 4.3 诚实边界(必须连着读)

1. **代价是阵容多样性小幅下降**:不同英雄数 **25 → 23**(对比不挑种子的 27)。
   按 §K.5,(a) 波本就不需要无偏阵容,这个代价可接受;但**对 axe/zuus/lion 之外**的
   id 取 (a) 时,这一波的语料比 §K.5 指定的那组略窄。
2. **§K.5 的副作用照常适用且加倍**:挑过种子的波次,经济读数**不得**与 863-870 这类
   连号波次并列比较,报告里必须标注。三重挑选比单重挑选偏得更多。
3. **每个种子里,英雄只在一侧**(drafter 的 `used` 是全局的,radiant/dire 不重复)。
   镜像波次两个 wave 会把候选侧换过来,所以「该英雄在候选侧」的局数约是该种子的一半 ——
   这对任何选种方案都一样,不是本方案的缺陷,但写检测器时要按半数估语料。
4. **这是建议,不是自作主张。** 章程「上机前以届时的 `test_set.md` 逐字为准」+
   「保守默认:不自行改变被测集合」⇒ **若 04:11Z 上机时 `test_set.md` 仍写
   872/875/885/887,就按 872/875/885/887 上机。** 已把本节结论追评到 `[batch] #49`
   (00:14Z 那条语料清点 issue 的同一线程),总监 03:00Z 前后那轮可以裁定,时间来得及。

## 5. 下一波的上机前静态核对(已做完,省下机时)

按 §K.0(`roamstale` 已 promote,出串)+ §J.1.4(`wandlimbo` 机会普查未完成,不 arm),
下一波 armed 串 = eligible 19 id 去掉 `wandlimbo` = **18 id**:

```
l1trade,l5combo,midtp,suptp,tpcommit,tpdying,lf_rescue,teambrain,ownhalf,overchase,
fieldregen,wandbleed,tpwatch,capmono,cmrguard,tpdead,axebuyblink,zusult
```

逐个 grep `bots/` 的裸字面量命中数:l1trade 4 / l5combo 4 / midtp 4 / suptp 2 /
tpcommit 7 / tpdying 2 / **lf_rescue 0** / teambrain 4 / ownhalf 3 / overchase 3 /
fieldregen 3 / wandbleed 3 / tpwatch 3 / capmono 2 / cmrguard 2 / tpdead 3 /
axebuyblink 2 / zusult 1。

- **`lf_rescue` 命中 0 是已知正确的**:它经 `J.IsLaneFixOn('rescue')`
  (`jmz_func.lua`)展开成 `J.IsSoakCandidate('lf_rescue')`,变量确实可被逗号串表达
  (12:12Z 那波已实测)。
- **`roamstale` 在 `bots/` 里只剩注释**(`mode_team_roam_generic.lua` L156/173/175/509),
  没有任何 `IsSoakCandidate('roamstale')` 读者 ⇒ promote 落地属实,从串里删掉是对的
  (留着是逐位 no-op 但会误导判读)。

这道核对的目的是防重演 `[bug] #31` 式的「花一波钱测了个不生效的东西」。

## 6. 树核对

- `git ls-remote origin main` = **`d13aaae`**;本地 HEAD = `d974b3c`
  ⇒ **远端领先本地 2 个 commit**(hero 组 `816d0b3` 修 GH #50 largo 崩溃 + `d13aaae` 报告)。
- **`test_set.md` / `queue.json` 在这 2 个 commit 里逐字未变** ⇒ §K.0/§K.5 的下一波预置仍成立。
- **20:11Z 那个坑再次印证**:`git fetch origin main` 打出
  `+ 46d381d...d13aaae main -> origin/main (forced update)` —— 本地 remote-tracking ref
  之前停在 `46d381d`,**落后几十个 commit**。**核树一律以 `git ls-remote` 的值为准,
  或先 fetch 再比;绝不直接信本地 `origin/main`。**

## 7. 局数(章程第 7 条)

- **(a) 上一波最终有效局数**(22:11Z 启动,19 id,种子 867-870,树 `829202a`):
  867 = 72(radiant 42 / dire 30)、868 = 74(42/32)、869 = 70(42/28)、870 = 60(38/22),
  **合计 276 局有效局 + 24 局暖场局**(radiant 164 / dire 112)。
  radiant 已打满 `--games 22` 配额、dire 仍撞 35min stall 上限 ⇒ **再提 `--games` 只会
  加大不对称**,补 dire 要动 harness 侧 stall 上限。
- **(b) 本轮在跑波次:无。** 0 台实例在跑,S3 无新 `soak/<run_id>/` 前缀。
- **下一波(≥04:11Z)预期**:4 台 × 1 种子,`--games 22`,预期 ~270 局有效局。

## 8. 下次触发的动作清单

1. `session_setup.sh` → `check_costs.sh`(留意 MTD 可能一次性跳到 ~$9-10,是滞后入账不是泄漏)。
2. 收割:预计仍无新数据(除非 04:11Z 后有波次落地)。
3. **若 ≥04:11Z 且三条件满足 → 启动 (a) 取证波**:
   - **cand = 上面 §5 的 18 id 串**(`roamstale` 已出、`wandlimbo` 仍不 arm),
     **上机前以届时的 `test_set.md` 逐字为准**;
   - **种子:默认 872/875/885/887(§K.5 指定);若总监在 #49 下采纳 §4.2,改用
     872/910/1024/1043**;
   - 4 台 × 1 种子 c6i.4xlarge **`--on-demand`**(64 vCPU 配额正好 4 台;短波次别用 spot ——
     12:12Z 的 spot 臂 B 被 `instance-terminated-no-capacity` 回收过),16 槽,2h 看门狗,
     `--games 22`;
   - 上机前 `git ls-remote origin main` 取**全 40 位 SHA**(缩写 SHA 在 want-line 里不合法);
   - **落地后必查暖场局 stamp = 该 SHA**(否则裸 SHA checkout 静默失败、跑的是 AMI 陈树,作废)。
4. 收割一律 `recover_verdict.py`,**先按 run 分目录下载再带前缀合并**(08:08Z 踩过跨 run
   同名 per-game 文件撞车,静默丢过 15.9% 的局)。
5. 报告里标注「本波挑过种子,经济读数不与连号波次并列比较」。

## 9. 验证(铁律第 6 条)

本会话**未改动任何 Lua**(改动仅 `iterations/` 下的报告/章程),且本容器未装
`luacheck` / `lua5.1`。按铁律第 6 条的立法目的,本轮无适用对象,两者均未运行。

## 10. 跨组

在既有 `[batch]` issue **#49**(00:14Z 的语料清点)下**追评**,不新开 —— 同一线程、
同一问题(取证波的选种)。交付:三重覆盖种子集 + 覆盖表 + 三条诚实边界 + 请总监在
04:11Z 前裁定用哪一组种子。

## 11. 本次会话 token 用量

```
TOKENS total_in=4,803,164 out=41,154 turns=46
```

(到统计时刻为止;统计后的收尾回合不计入。本轮零 AWS 支出,token 主要花在读
`test_set.md`(1207 行,分页读)和 `batch-desk.md` 章程上。)
