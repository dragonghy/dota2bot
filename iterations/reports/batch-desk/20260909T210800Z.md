# batch-desk 2026-09-09T21:08–21:3xZ(发波轮:W62 四台**按需**起飞)

**一句话**:W62 起飞了,而本轮真正要交出去的是**它凭什么可以起飞而 W61 不可以** ——
两波的臂串**逐位相同**(37 id / 335 字节 / md5 `b525d51d…`),但 W61 的树差**全部**落在未 armed 的
gate 后面,W62 的树差里有**一行改在 armed 的 `ownhalf` 自己的身体上**(`nInvadeDepth >= 800` → `>= 1600`)。
⇒ 按总监 §GH.4 的**新措辞**,W61 三条腿全不成立(故「不予追认」),W62 **限 (2) 实质成立**:
**不是新代码,是新信息**,而这正是 §GH.3 说旧措辞读反了的那个区别。

⚠️ **第二件必须一起读的事**:闸 (iii) 现推 **`headroom $0.292`** ⇒ **W62 是本月 `$80` 围栏下的最后一波**,
**连一波 spot(~$1.10)都塞不进去**。下一个发波轮**要么围栏已随月初重置,要么必须停下来问 owner**。

---

## 一、成本(闸 (iii) 之前的读数)

- `AWS_SETUP_EXIT=0`(`arn:aws:iam::581436534924:user/dota2bot-agent`,受限用户,正确)。
- `COST_EXIT=0`。MTD **`$74.308`**(免费 `budgets`,`refreshed 2026-09-09T20:23:37Z`),≥$35 ⇒ 脚本自动花 `$0.01` CE 复核 **`74.3077543792`**,**两源逐位一致**。
  `forecast $312.047`(月度 limit `$100`)**原样登记,本台不据 forecast 决策**。
- **进场在跑 0 台**(`running,pending` 表为空;区域 `us-west-2` **取自 `aws.env`**,不是手打 —— 09-06 那轮三条空的 `us-east-2` 读数就是手打来的)。
- 常设成本仍只有 AMI(`ami-0a990a26d89c66547`)。
- ⚠️ **滞后**:快照戳 `20:23:37Z`,W61 起飞 `15:23Z` ⇒ 相隔 **5.0h**,落在章程钉死的 **4.3–11.3h** 窗内 ⇒ W61 **可能不在**这个数里;W60(`09:23Z`)相隔 **11.0h**,在窗的**远端**,**可能已经在**。两种可能都按**朝安全侧**处理(见闸 (iii) 的 `--pending`)。
- ⛔ **未用被禁的「MTD 增量对得上波费」手法**:`$71.130` → `$74.308` 的 **`$3.178`** **不做任何归因**。
- ⭐ **仍顶在围栏上的那笔外来支出**(09-05/09-06 的 `c7a`/`r7a.16xlarge`,农场从不用这两种机型,上一轮测得占 MTD 的 `54.9%`)**本轮未再复查**(本轮 CE 只花在 `check_costs.sh` 的自动复核那一次),**只登记「未复查」,不声称它停了**。它是 `headroom` 只剩 `$0.292` 的主因。
- **本轮新增支出**:那 `$0.01` CE + W62 一波(计划价 `$2.150`)。

## 二、收割:无新数据(查过的,不是默认的)

- `s3api list-objects-v2 --prefix validation/ | sort_by(LastModified)` 的最后一条仍是 **`2026-09-09T10:22:37Z`**(W60 的第四台)。
- **第二个独立读数**:`seed_roster_index.py --build` 打 `401 run prefixes in S3, 401 already indexed, 0 to scan` / `nothing new; index unchanged`。
- ⇒ 两条独立路径都说 **W61 之后 S3 上一个新对象都没有**,与上一轮「W61 全损」的裁定一致。本轮**零收割动作**;这同时是闸 (iv) 第一半(`no harvest owed`)的读数。
- `queue.json` **逐行扫过**:`status=running` 只有 3 行(`strategy-31` `tormself`、`strategy-32` `immguard`、`hero-27` `savemana`),三行**都在自己的 axis 文本里自述零 EC2 / 不申请专波**;**全表无任何一行申请新波次、专机或新机时**。

## 三、⭐⭐⭐ 头号:W62 与 W61 的区别,逐条落在总监 §GH.4 的三条腿上

**§GH.4 的三条腿,按顺序判:**

| 腿 | W61(总监裁:不予追认) | W62(本轮) |
|---|---|---|
| (1) 臂串与上一波不同 | **否**(与 W60 逐位相同) | **否**(与 W60/W61 逐位相同) |
| (2) 该臂串下**可达代码**与上一波不同 | **否** —— 树差 1 文件 +38/−0,非注释行全在 `IsSoakCandidate('hrparity')` 后面,`hrparity` **不在**臂串里 | ⭐ **是,而且是实质的** —— 见下 |
| (3) 家族深度 < promote 门槛 | **未生效**(总监:门槛那个数没裁,「一道门槛没写出来的闸不是闸」) | **仍未生效**(同一条,本轮没有新裁定) |

**限 (2) 在 W62 上成立的证据,逐条摆开。** 树差 `252a5781`(W61 的钉)→ `a91efb69`(本波的钉),`bots/`+`game/` 共 **2 文件 +215/−20**。**非注释**的改动**恰好三处**:

| # | 位置 | 改动 | 在本波臂串下可达吗 |
|---|---|---|---|
| (a) | `bots/FunLib/jmz_func.lua:9177`(`J.ShouldPunishDive` 内) | `if nInvadeDepth >= 800` → **`>= 1600`** | ⭐ **可达。守它的是 `local bOwnHalf = J.IsSoakCandidate( 'ownhalf' )`,而 `ownhalf` 是 37-id 串的第 3 个** |
| (b) | 同文件,新块 | `if J.IsModeTurbo() and J.IsSoakCandidate( 'deepnum' ) then …` | **不可达**(`deepnum` 不在臂串里) |
| (c) | `bots/BotLib/hero_axe.lua` | 新增 `X.axe_HasHungerAlternative` / `X.axe_IsHungerHarassIdle`,改写 `X.axe_IsBattleHungerFresh` 三个调用点 | **可达但可证为 no-op**(见下) |

- **(a) 是这一波。** `ownhalf` 是 **armed** 的,它的**判定域自己变窄了**(midline 后 800u → 1600u,strategy 组在码内自称 `STRICTLY NARROWING`)。⇒ **没有任何一波测过这个 `ownhalf`。** 这是**新信息**,不是新代码。
  ⭐ **守卫归属不是我从 diff 猜的**:`carrier_terms.py` 独立地把 `ownhalf` 解析到 **`bots/FunLib/jmz_func.lua:9092`**,正是那行 `local bOwnHalf = J.IsSoakCandidate( 'ownhalf' )` —— 与改动行同在 `J.ShouldPunishDive`(`:9075` 起)内。**两条独立路径指向同一个守卫。**
- **(c) 过了 §GH.2 的副作用腿。** 唯一未 gate 的那处是 `npcEnemy:GetAttackTarget() == nil` → `X.axe_IsHungerHarassIdle( npcEnemy )`:**同一个读、同一个运算符、同一个答案**,`GetAttackTarget()` **两边各调一次**,无新引擎调用、**无 RNG**、无 I/O;第三个实参是**函数值**(在调用点**不被调用**)。新增的 `X.axe_HasHungerAlternative` 只从 `IsSoakCandidate('axebhrecast')` 之后的合取项进入,而 `axebhrecast` **不在**臂串里 ⇒ 短路,不可达。
- ⛔ **诚实边界**:本台**没有**跑 Lua 动态半(~100min,GH #124),**不声称 (a) 的窄化是对的** —— 只声称它**可达、armed、且从未被测过**。

**⇒ 闸 (ii) 本轮在花钱之前就判完了,而且判的是实质读法。** 这是对 W61 那条根因(「四道闸里唯一没有可执行体的那道,正是唯一裁定落在钱后面的那道」)的**当轮补偿,不是修复**:闸 (ii) 依然没有可执行体,依然靠人当场读;本轮只是把它**排在了钱前面**。GH #669 系列里总监已准了「给闸 (ii) 一个可执行体」,那是下一个工作单元,不是本轮。

## 四、⭐⭐ 并池:本波**不可并池**,而这一次是普通答案不是难题

总监 §GH.5.4 逐字:「树差一旦碰到**未 gate(已 promote)的代码**,哪怕臂串逐字相同,两波在**两条腿上**都不同 ⇒ **不可并池**」。
W62 的树差**不止碰到未 gate 的代码,它改的是一条 armed 腿自己的行为** ⇒ **W62 与 W60/W61 测的不是同一个程序,并池会是错的**。

⇒ **W62 开新家族,深度 1**;W60 的池仍是 **4 粒**。
⭐ **但这一次不要读成上一轮那句「家族深度被退集节奏封死」的又一例。** 那句话说的是**买不到新东西却又开了新家族**的波;本波开新家族**是因为真的有新东西**。两者形状相同、含义相反,已写进 `W62_wave.json:family_depth_at_launch` 免得收割轮读错。

⚠️ **本台本来准备交上去、最后没交的那个问题,登记在此备查**:如果树差**只有 (c)**(一个未 gate 但**可证为 no-op** 的别名抽取),§GH.5.4 会不会因此**误杀**一次本可成立的并池?本轮 (a) 的存在让这个问题**不必用钱去问**,所以本台**不占用总监一轮**去问它;等下一次真的只有 (c) 型树差时再提。

## 五、发波:W62 = 37-id 串第三波,**按需**四台

- **闸 (i)** `THROTTLE_EXIT=0`,逐字 `margin           : +3s past unlock`(锚点 W61 末台 `2026-09-09T15:24:29Z` ⇒ 解锁 `21:24:29Z`)。**闸跑在发波脚本内部**(GH #469:闸约束的是动作):脚本 20s 一轮询,拿到**工具自己打的**第一个 exit 0(`21:24:32Z`)才发,第一台 `LaunchTime 21:24:34Z`,**相隔 2 秒**。
  **七次拒绝照登不丢**:进场手跑两次(`21:09:15Z` margin **914s**、`21:13:00Z` margin **689s**),脚本内五次(**97s / 77s / 57s / 37s / 17s** SHORT)。
- **闸 (ii)** 见第三节 —— **实质成立,且判在钱之前**。
- **闸 (iii)** `FENCE_EXIT=0 CLEAR`,**围栏本轮现推不抄**:`--planned 2.150`(按需常数,因闸 (iv) 已把本波裁成按需)`--pending 3.250`(W60 按需 `~$2.15` + W61 spot `~$1.10`,都在 12h 窗内)。
  ⇒ `actual $74.308` / `projected $79.708` / `fence $80.00` / `brake $90.00` / `operative ceiling $80.00` / **`headroom $0.292`**。
  `$50` 已越(`AWS state=ALARM`),`$80`/`$100` **未越**,**本轮未新跨任何档**。accrual 行 `CERTIFIED ZERO (0 accruing instances account-wide)`,**取较大的那个声明**;**未用 `--no-accrual-check`**。
- **闸 (iv)** `RECLAIM_BLIND_EXIT=1` on `W61_wave.json`,**跑在发波之前**。逐字 `yield: 0 paired seed(s) of 4 machine(s)` / `attribution: 4 machine(s) reclaimed before the flip` / `VERDICT: BLINDED` / **`NEXT WAVE: launch --on-demand (ONE wave), then revert to spot.`**
  **1 = BLINDED**(源码:`2` 才是 UNDECIDABLE,`0` 是 not blinded)⇒ **这是裁定不是默认**,与 W61 那条「保守默认走 spot」(当时 exit 2)正好互为对照。**W63 回 spot**,除非它自己那轮的闸 (iv) 另有说法。
- **载体闸** `CARRIER_GATE ids=9 seeds=4 exit=0`,term **从臂串机械推导**(`--assert-carrier-from-arm`,GH #276):`9 hero-scoped, 28 generic, 0 unresolved => 7 term(s)`;`grep -c EMPTY` = **0**。逐 term:`obsidian_destroyer 4 / crystal_maiden 3 / pudge 3 / spirit_breaker 3 / lion 2 / skeleton_king 2 / zuus 2`。
- **种子**:`seed_roster_index.py --build` **先跑完**(`ROSTER_EXIT=0`)才写下面的互斥声明。窗口右移到 **[10601,11000]**,与名册索引交集 **EMPTY SET(打印过的,不是假设)**;popcount 分布 `{0:40, 1:115, 2:152, 3:74, 4:16, 5:3}`;
  选 **10601 / 10607 / 10803 / 10813**,`(min 2, total 19)`,**该窗口的全局最优不是启发式的最好猜测**:min≥3 需 ≥21 slot,而该窗只有 **3** 粒 popcount=5 ⇒ 四粒至多 5+5+5+4=**19**,算术上不可能;19 已取到 ⇒ 也不存在 `(2,>19)`。
  ⭐ **为什么不复用 W61 那四粒**:它们**一局没存**,所以**不在**名册索引里,`seed_draft` 会把它们当「从没用过」再发一次;而**它们要不要退回可用窗口,是上一轮交给总监、至今未裁的问题**。**右移窗口零成本**,于是本台**拒绝用花钱的方式去替总监把这个问题定死**。问题**原样再交一次**(见第八节)。
- **起飞前 `--dry-run`**:`DRYRUN_EXIT=0`(**裸读不走管道**),逐字 **`plan: 1 x on-demand c6i.4xlarge`** ⇒ `--on-demand` 生效的第一个独立确认(W61 那次是 `plan: 1 x SPOT` 的镜像)。波次预算闸**未拒发**(12 局 / 16 槽装得进 2h 看门狗)。
- **配置** `--slots 16 --hours 2 --rec-slots 8 --games 12`,与 W39–W61 逐字相同;`--rec-slots 8` **显式传**(默认是 1,W51 就是吃了默认被 70 秒后杀掉的那一波)。
- **树** `--ref a91efb699a63726f775b4e20ad7d4df274f17981`(40 位 SHA,不是会动的 `main`)。
  ⭐ **W61 自己登记的那条倒退,本轮补上了**:发波脚本的**第一条语句**就是 `git ls-remote origin main` 并与钉比对,**不等就 `exit 9`**;它打了 `REMOTE_MAIN=a91efb69…` / `PIN=a91efb69…` 两行**在任何花钱动作之前**。
- **实例**(四台 `InstanceLifecycle` 逐台核对为 **absent/None** = 按需标记;spot 会打 `spot`):

| 种子 | instance | 请求 AZ | 实得 AZ | LaunchTime |
|---|---|---|---|---|
| 10601 | `i-046fe13456914381a` | 2a | **2a** | `21:24:34Z` |
| 10607 | `i-08fd4fef0e94a0993` | 2b | **2b** | `21:24:36Z` |
| 10803 | `i-0dd115a91af3cf747` | 2c | **2c** | `21:24:39Z` |
| 10813 | `i-0a951b0024605351d` | 2d | **2d** | `21:24:41Z` |

  ⚠️ **run_id 的 `spot_` 前缀是 `spot_run.sh` 里的硬编码字符串,不是市场证据** —— 这四台按需机的 run_id **全都**以 `spot_` 开头。
- **波次记录** `iterations/reports/batch-desk/waves/W62_wave.json` 当轮写完,`tests/test_wave_gate_keys.py` **当场跑**:**`WGK_EXIT=0`,406 checks 0 failed**。
- **下一轮闸 (i) 锚点**:末台 `2026-09-09T21:24:41Z` ⇒ 解锁 `2026-09-10T03:24:41Z`(**不要抄,跑 `wave_throttle.py`**);slate spread **7s**(近期最紧,因为没有一台走 AZ 环)。

## 六、⭐⭐ AZ:本波 4+0,而这**不是** GH #664 被修好了

请求 2a/2b/2c/2d,**实得 2a/2b/2c/2d,四粒四个 AZ,全部直接授予**;发波日志里 `re-aiming inside the ring` **零条**、`AZ RING EXHAUSTED` **零条**。
⇒ 本波是环能给出的**最大分散度**,GH #664 点名的那个 **3+1 敞口**(两条判据都字面通过、而一次 AZ 事件带走四粒里的三粒)**在本波上不适用**。

⛔ **但这是关于这张 slate 的事实,不是一个修复**:环**仍然不看本波其它机器已经占了哪些 AZ**,下一次容量紧张时它会和 W61 一样把机器投进已占的 AZ。**GH #664 仍然开着**,已写进波次记录,免得收割轮把 W62 的存活读成「环修好了」。

## 七、闸 (ii) 接线那半:**没跑成,不是通过**

`tools/batch_test/check_armed_wiring.py` **需要 `--cand`**,整串调用被它当场拒绝(逐字 `error: the following arguments are required: --cand`,退出码 **2**)。
⇒ **登记为 NOT RUN**,**不登记为 pass**(W61 那轮它是 exit 0 `all 37 armed ids wired on HEAD`,本轮**没有**这条读数)。
**代之而跑、且跑在发波之前的是另一件仪器**:`carrier_terms.py` 对同样 37 个 id 打 `CARRIER_TERMS_EXIT=0` / **`0 unresolved`** —— 意思是 37 个 id **每一个都在 HEAD 上找到了真实调用点**,覆盖的是同一类失败(armed 了但后面什么都没有)的**另一条路**。
⛔ **但它是另一件仪器,本台不把它当作 `check_armed_wiring.py` 的读数。**(逐 id 跑 37 次 `--cand` 是可行的;本轮为了赶在闸 (i) 解锁前完成种子与载体两道门,没跑,**如实登记为本轮的一个缺口**。)

## 八、交棒

1. ⭐⭐⭐ **下一轮本台 = 收割轮 W62**(四台按需,2h 看门狗,预计 `23:2xZ` 前后自终)。
   ⛔ **收割须知 (a)**:按需机的 `StateReason.Code` 在终止后 **~1h** 从 `describe-instances` 老化掉(GH #375),**而且没有 SIR 可退**(`describe-spot-instance-requests` 对按需机什么都不返回,本轮实测 SIR count 已是 0)。**要读终止码就必须早读**。
2. ⭐⭐⭐ **总监 —— `headroom` 只剩 `$0.292`,下一个发波轮结构上发不出任何一波**(连 spot 的 `~$1.10` 都塞不下)。请裁:是等 MTD 月初重置,还是现在就把「外来支出占 MTD 过半」这件事升级给 owner。**这不是请求批钱,是请求裁定下一轮的动作。**
3. ⭐⭐ **总监 —— W61 那四粒种子(10208/10212/10390/10526)第二轮欠着裁定**:一局没跑就作废、已从名册划掉、**且不在索引里所以会被当成全新种子再发一次**。本轮**用右移窗口绕开**(零成本),但绕开不是裁定。
4. ⭐⭐ **[harness] 闸 (ii) 的接线检查在整串上不可调用**(第七节):`check_armed_wiring.py` 只接受 `--cand <单个 id>`,于是「整波臂串接线干净」这个读数**每一轮都要么循环 37 次、要么被跳过**。建议加一个 `--arm`/`--arm-file` 入口,与 `carrier_terms.py` 同形。
5. ⭐ **GH #664 仍开**;本波 4+0 **不是**它被修好的证据(第六节)。
6. ⭐ **GH #672**(`reclaim_blind.py` 对给了 `arm_depth` 的行也照打 `no arm_depth given`)本轮**在 W61 记录上再次复现**,未修,不重复开 issue。
7. ⭐ **GH #454 (乙1)** 第 11 轮欠着(W55–W62 仍拆不开日 `USW2-SpotUsage` 行);**(乙2)** 本波是**按需**波,自终机时要等收割轮才有,**本轮结构上不可算**。

## 九、泄漏检查(收尾)

- **进场**:`describe-instances --region us-west-2`(区域取自 `aws.env`)`running,pending` ⇒ `LEAK_EXIT=0` **count=0**。
- **收尾**:同一条路径 ⇒ **恰好四台**,`InstanceId/InstanceType/InstanceLifecycle/AZ/Name` 逐台核对:`c6i.4xlarge` × 4、`InstanceLifecycle=None`(按需)× 4、`dota2bot-soak-od-1` × 4 —— **全是本轮有意起的,零外来实例,零泄漏**。
- ⭐ **进场那个 0 的正对照**:同一条 EC2 API 路径上 `describe-images` **返回了 1 行**(AMI `ami-0a990a26d89c66547`,在 `check_costs.sh` 里)⇒ **调用通、凭据对,那个 0 是读数不是失败**。
  ⚠️ **本轮不能用 `describe-spot-instance-requests` 做正对照**:它现在也返回 **0**(W61 的四个 SIR 已老化),**两个 0 互相证明不了任何东西** —— 上一轮能用它,是因为当时它还是 4。
- ⚠️ 一如既往:**零泄漏 ≠ 本月没有外来支出**(见第一节)。

## 十、自检(铁律 10)

⛔ **第一条命令第十九轮踩同族坑**,而且这次**是工具自己拦下的**:`routine_selfcheck.sh 2>&1 | tail -60` ⇒ 脚本当场自拒,逐字
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.`,而 harness 报的 `SELFCHECK_EXIT=0` 是 **`tail` 的**。

⛔ **第二次仍没拿到读数,原因是我自己**:改重定向后我给了 `timeout 400`,后台任务回报 **`EXIT=124`**(超时被杀),而 **harness 在同一条通知里写 `[exited with code 0]`** —— **后台包装器的 rc 不是被包装命令的 rc**,与管道那个坑同族。日志停在 `=== trunk health (python test suite) ===`。
第三次**不设 timeout** 重跑,写本报告时**仍在 `=== trunk health (fast Lua detectors) ===` 这一腿上**。

⇒ **本轮自检登记为 UNCERTIFIABLE(未完成),不是通过。** 已拿到的部分逐条登记(**都是「没有发现」,不是「全绿」**):
- **稳定版锚点(铁律 3)**:6 个锚点 `stable-v1..v6` 全 `EXISTS/PINNED/SHIPPED ok` ⇒ `6 anchor(s) checked -- OK`。
- **promote-time 约束(pullcad 陷阱 + co-promote 原子)**:`scanned: 186 distinct live gate id(s), 18 id(s) carrying a PROMOTED note`;**`FROZEN none`**(没有活 gate 点名已 promote 的 id);5 个原子全 `ok` ⇒ `promote-atom constraints: OK`。
- **admission wait**:`no expired admission wait in any live 当前状态 block (6 charter(s), 55 settled id(s))`。
- **trunk python 腿**:打出 `UNCERTIFIABLE -- a python test did NOT run (could not read its input).` 并**自带限定**:`This line is NOT a pass, and it is NOT evidence that trunk is red`。**照抄,不引申。**
- 另:`python3 -m pytest` 在本容器**不存在**(`No module named pytest`)⇒ `tests/test_wave_gate_keys.py` 改为**直接执行**,得 `WGK_EXIT=0` / `406 checks, 0 failed`。

## 十一、铁律 6 / 11

- `bots/` / `game/` / `tests/` / `tools/` **本会话一行未改**(只写 `iterations/`)—— 第六节的 AZ 环缺陷、第七节的接线工具缺口,**本台都没自己去改**,只交棒。
- 静态门读数见提交时 `.githooks/pre-push` 的输出;**未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行**。动态半(~100min,GH #124)**未跑,不声称**。
- 铁律 11:全程无 `requires approval`,**零空转** —— 闸 (i) 那 15 分钟里跑完了成本、S3 收割扫描、queue 逐行扫描、闸 (iii)/(iv)、树差三处逐条判读、载体推导、名册重建、种子最优化与互斥证明、`--dry-run`,发波由脚本在解锁瞬间执行。

## 十二、Token 用量

`TOKENS total_in=7,858,088 out=60,313 turns=62`(统计时刻 `2026-09-09T21:3xZ`,之后的收尾回合不计入)。
