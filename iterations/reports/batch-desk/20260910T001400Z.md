# batch-desk 2026-09-10T00:14–00:4xZ(收割轮:W62 **4/4 粒全部计分**;不发波,**两道理由各自充分**)

**一句话**:W62 收割完毕,**4/4 粒配对、203 局计分、零 NO-PAIR 零 THIN-ARM**,这是自 W60 以来第二次满收,
也是**唯一一波测过「`ownhalf` 判定域 800→1600」这个程序**的语料;本轮**不发波**,
**两道各自独立、各自充分的理由**:(甲) 闸 (i) 打 `THROTTLE_EXIT=3`(解锁 `03:24:41Z`,还差 185m18s);
(乙) 总监 09-09T22:xxZ 已裁 **`NO_WAVE_NEXT_LAUNCH_ROUND`**(GH #677)。**都是裁定,不是本台的选择。**

⭐ **第二件必须一起读的事,而且要读全**:上一轮登记的 `headroom $0.292`「下一个发波轮**结构上**发不出任何一波」
**本轮已不成立** —— W60 已滑出 12h pending 窗 ⇒ 安全侧 headroom **`$2.442`**,**一波 spot(~$1.10)塞得进去**。
⛔ **但「塞得进去」不是「可以发」**:(乙) 那道裁定**独立地**挡着,而且它是**总监的**,不是本台可以拿新前提自行宣布失效的。
⇒ 本台**把变更后的前提交回总监**(§五 末 / §十一 issue 1),**并把「若解禁该发什么」算完**(§七)——
**算完不是下单**。⚠️ 若下一轮只读到 (甲),会误以为「等到 03:24Z 就能飞」。

⭐⭐ **第三件**:上一轮那笔「顶在围栏上、只登记未复查」的外来支出,**本轮花 `$0.01` 拿到硬读数并结清**:
`c7a.16xlarge $23.281 + r7a.16xlarge $14.155 = **$37.435 = MTD 的 50.4%**`。**九月的批测预算有一半被别的项目吃掉了。**

---

## 一、成本(闸 (iii) 之前的读数)

- `AWS_SETUP_EXIT=0`(`arn:aws:iam::581436534924:user/dota2bot-agent`,受限用户,正确)。
- `COST_EXIT=0`。MTD **`$74.308`**(免费 `budgets`,`refreshed **2026-09-09T20:23:37Z**`),≥$35 ⇒ 脚本自动花 `$0.01` CE 复核 **`74.3077543792`**,**两源逐位一致**。`forecast 312.047` / `limit 100.0` —— **forecast 原样登记,本台不据它决策**。
- ⚠️ **这张快照的戳与上一轮逐字相同(`20:23:37Z`)** ⇒ **它一分钱都没刷新**,而 W62 **21:24:34Z 才起飞** ⇒ **快照比本波第一台机器还早 61 分钟,结构上不可能含 W62**。这不是章程 §2(甲)「4.3–11.3h 滞后」的估计,是**时间戳直接读出来的**(与 09-08T18:16Z 那轮同形)。
- **进场在跑 0 台**(区域 `us-west-2` 取自 `aws.env`,不是手打)。常设成本仍只有 AMI `ami-0a990a26d89c66547`。
- ⛔ **未用被禁的「MTD 增量对得上波费」手法**:`$74.308` 与上一轮**逐位相同**,**不做任何归因**。

### ⭐⭐⭐ 上一轮那笔外来支出:本轮花 `$0.01` 结清,它已经落账了

上一轮只能写「未复查,不声称它停了」,并估「已发生未落账约 `$16.4–19.7`」。本轮 `ce --group-by INSTANCE_TYPE`(`2026-09-01 → 2026-09-11`)**逐行**:

| 机型 | 金额 | 归属 |
|---|---|---|
| `c6i.4xlarge` | **$29.652** | **本台**(农场唯一机型) |
| `c7a.16xlarge` | **$23.281** | **外来**(`final-table-trainer`;农场从不用此机型) |
| `r7a.16xlarge` | **$14.155** | **外来**(同上,**上一轮连量都没量到这一项**) |
| `NoInstanceType` | $7.220 | 混合(EBS/VPC/传输),**本台不拆,无二次读数不猜** |
| **合计** | **$74.307** | 与 `budgets` 的 `$74.308` **逐位吻合** |

- ⇒ **外来 = `$37.435` = MTD 的 `50.4%`**。上一轮「c7a 至今没上账单」**已作废**(那是 09-06 截止的读数);**本轮它上账了,而且比上一轮的估计上限还高**($37.4 vs 估 $16.4–19.7 —— 差额主要是**上一轮没算 r7a**)。
- ⚠️ **诚实边界**:`NoInstanceType $7.220` **未拆分**,所以「本台九月花了多少」的准确值是 **`$29.652 ≤ x ≤ $36.872`**,本台**只报区间不报点值**。
- ⭐ **好消息侧**:`wave_fence.py` 的 `accrual check` 本轮打 **`CERTIFIED (0 accruing instances account-wide, read this run)`**,**17 个 region 全读**⇒ **外来那台已经不在跑了**,新的外来消耗**当下为零**。这条与下面的泄漏检查互为独立读数。

## 二、W62 收割(`RV_EXIT=0`)—— **4/4 粒全部计分**

**臂串收割这一刻现算**(GH #631:章程散文写「第 3 行」,**第 2 行才是串**):
`git show a91efb69…:iterations/streams/test_set.md | sed -n 2p`
⇒ **37 id / 335 字节 / md5 `b525d51d4b4957e0e40f22f203aea641`**,与 W62 发波记录**逐位一致**。

语料走 `soak/` **四个前缀各自子目录、不拍平**(GH #225):
`files_seen 227 == games_loaded 227`、`source_dirs 4`、`unparseable 0`、`pooled_basename_dirs_overridden 0`、
`scored_games 203`、`unfinished_games 0`、`min_arm_depth 8`、`thin_arm_seeds []`、`thin_arm_winrate_seeds []`。

### 逐粒(铁律 4 (i-a):两个分层的**读数**都登记;`_ab`/`_ba` **全部取自工具字段,未手算**,理由是 (i-d))

| 种子 | AZ | ab/ba | arm_depth | gpm (ab / ba) | xpm (ab / ba) | deaths (ab / ba) | last_hits (ab / ba) | 计分局 |
|---|---|---|---|---|---|---|---|---|
| 10601 | 2a | 42/16 | 23.17 | **+12.47** (−84.37 / +109.30) | +16.96 (−4.32 / +38.24) | −0.19 (−0.40 / +0.03) | −1.34 (+15.58 / −18.26) | 58 |
| 10607 | 2b | 31/14 | 19.29 | **−21.02** (−94.72 / +52.69) | +0.91 (−44.88 / +46.70) | +0.06 (+0.94 / −0.83) | +1.50 (−6.59 / +9.60) | 45 |
| 10803 | 2c | 34/24 | 28.14 | **+4.06** (−32.75 / +40.86) | +10.22 (−9.12 / +29.55) | −0.11 (+0.04 / −0.25) | +2.40 (−6.75 / +11.55) | 58 |
| 10813 | 2d | 30/12 | 17.14 | **−24.32** (+297.83 / −346.47) | +12.60 (+220.05 / −194.85) | −0.31 (−3.21 / +2.58) | −0.17 (+19.75 / −20.10) | 42 |

⭐ **零 NO-PAIR、零 THIN-ARM、零全损**:四粒 `arm_depth` **17.14–28.14,最小的也是 GH #269 那道 8 门的 2.1 倍**,`scored: true` ×4。
自 W52 以来第二次 4/4(上一次是 W60,也是按需波)。⚠️ **不要把这读成「按需波不会被回收所以更稳」的第二个证据就完事**:
W62 发波记录自己的 `harvest_notes (b)` 逐字写着 **AZ 环(GH #664)没被动过,这一波只是四个 AZ 都要到了**。

### 跨种子(**先每粒 swap-average、再跨种子算术平均**;⛔ 未按局数加权 —— 铁律 4 (i-d))

| 量 | mean | comps_better | ab 层 | ba 层 | side | sign_flip |
|---|---|---|---|---|---|---|
| gpm | **−7.20** | 2/4 | +21.50 | −35.91 | +28.70 | true |
| xpm | **+10.17** | **4/4** | +40.43 | −20.09 | +30.26 | true |
| deaths | **−0.14**(更好) | 3/4 | −0.66 | +0.38 | −0.52 | true |
| last_hits | **+0.60** | 2/4 | +5.50 | −4.30 | +4.90 | true |

铁律 4 (i-a) 两层读数**已全部登记**。四个量**全部** `sign_flip=true`、`side_gt_arm=4/4` —— 按 **(i-c)**:
经济四量是**每粒 50/50 swap-average 的估计量,侧偏已消除** ⇒ **反号不是否决理由,也不含精度信息**
(`side_gt_arm` 是恒等式 `|side|>|arm|` 的改写,不是诊断)。管精度的是 `mean` + `comps_better` + #269 深度门。

**工具字段 `suggested: "hold_or_reject"` 原样转录** —— 章程第 5 行:**本台不做判断分析**,promote/reject 归主会话与总监。

### ⛔ 胜率通道 DEGENERATE —— 本波**不能**用于铁律 2(b)

`recover_verdict.py` 打到 stderr 的话,**逐字**:

> `GH #352 winrate channel DEGENERATE: minority side won 12 of 227 finished games (share 0.0529 < 0.20). The winrate below is bounded to 0.5 +/- 0.1131 and MUST NOT be cited as rule 2(b) support until the channel recovers.`

`winrate_side_census: radiant 12 / dire 215`。⇒ `mean.winrate 0.531` **登记但不可引用**。

## 三、⭐⭐ W62 与 W60 并排:同一条臂串,`ownhalf` 判定域从 800 挪到 1600

W62 存在的理由就是这一条(发波轮 §GH.4 限 (2)):**臂串与 W60 逐位相同**(同一个 `b525d51d`),
差别只在 `J.ShouldPunishDive` 里 `nInvadeDepth >= 800` → **`>= 1600`**,而守它的 `ownhalf` 是 armed 的。

| 量 | W60(`ownhalf` @800) | W62(`ownhalf` @1600) |
|---|---|---|
| gpm | −20.73(0/4) | **−7.20**(2/4) |
| xpm | −13.02(1/4) | **+10.17**(4/4) |
| deaths | +0.47 更差(1/4) | **−0.14 更好**(3/4) |
| last_hits | −0.88(1/4) | **+0.60**(2/4) |
| 计分局 | 221 | 203 |

**四个量全部朝好的方向动,`comps_better` 四个全部上升。** 但——

⚠️⚠️ **本台把这一段登记为读数,并当场给出它的三条限定,免得它被当成结论转手**:
1. **两波不是配对比较**:种子集完全不同(W60 = 9911/10079/10097/10133,W62 = 10601/10607/10803/10813)⇒ 两组阵容人口不同,**差值里含抽样差**,没有任何一粒是同阵容重跑。
2. **各自的跨种子离散度都很大**(W62 四粒 gpm:+12.47 / −21.02 / +4.06 / −24.32,跨 37 gpm),`comps_better` 也没有一个量是 4/4 之外的一致方向 —— **本台不声称这个改善「显著」**,铁律 2 本来也不要显著性。
3. **这是该程序的第一波、家族深度 4 粒**。判读与 promote/reject **归主会话与总监**;本台只交读数。

## 四、闸 (iv)(收割义务):`RECLAIM_BLIND_EXIT=**2**` —— **UNDECIDABLE,不是通过**

在 W62 自己的记录上重跑,**逐字**:

> `changeover 40.0 min  (measured bracket 34.80 < x <= 40.63, margin above the constant 0.63 min)  min_arm_depth 8.0`
> `UNDECIDABLE: seed 10601: market is on-demand and status_code is missing -- an on-demand row still has to say how the machine ended (EC2 StateReason.Code, e.g. Client.UserInitiatedShutdown)`

- ⭐ **这正是 W62 发波记录 `harvest_notes (a)` 预言的那件事,而且预言得一字不差**:on-demand 机器的 `StateReason.Code` 在终止后 ~1h 从 `describe-instances` 老化掉(GH #375),本轮在 `T+2.9h` 才收割。**本轮实测证实**:按四个 id 查 `describe-instances`,**返回空**(四台全部老化掉),`describe-spot-instance-requests` 对按需机器**结构上没有 SIR 可退**。⇒ **这个读数在 W62 上已不可挽回**,本台**不编、不推断、不拿 W60 的历史值顶替**。
- **对下一波市场类型的后果**:闸 (iv) 给的是 **2 = UNDECIDABLE(什么都没查)**,**不是** 1(BLINDED)⇒ **不构成「再来一波按需」的裁定** ⇒ 落回 **owner 的常设 spot 默认**(与 W61 那次同形,W62 记录 `market_source` 自己也写着「W63 is a spot wave unless its own gate (iv) run says otherwise」)。⇒ **W63 = spot**。
- ⛔ **表头那个 bracket 仍是写死的历史常数**(GH #591),**第四波逐字相同**,本轮 `exit 2` 对它**零信息**。

## 五、闸 (iii) 围栏:工具 CLEAR,但**工具的 `pending` 不是本台的 `pending`**

`FENCE_EXIT=0`,**逐字关键行**:

> `budget filters   : none  <- so every instance in the account does land in this budget, whoever owns it`
> `accrual scope    : 17 region(s) read [...]  <- COMPLETE: every region this account has enabled`
> `accrual check    : CERTIFIED (0 accruing instances account-wide, read this run) -- pending $0.000 is a reading, not a default.`
> `fence            : $80.00   <- lowest ACTUAL alert not yet crossed (Ruling 1)`
> `operative ceiling: $80.00   = min(fence, brake)`
> `headroom         : $5.692 after this wave`
> `WAVE_FENCE: CLEAR (exit 0) -- gate (iii) passes with the fence DERIVED THIS RUN. Do not copy $80.00 into a report as next month's number; re-run the tool.`

(总监 2026-09-09T22:xxZ 要求收割轮也跑一次并原样抄 `accrual scope` 那行 —— **已抄,见上**,`owed_executions.json:wave_fence_ruling5_multiregion_first_read` 的结清判据。⭐ 总监同轮修的就是这一行:此前它把**一个区域**的读数印成 `account-wide`;本轮打的是 **`17 region(s) read … COMPLETE`**,**修法在生效**。)

⚠️⚠️ **`headroom $5.692` 不可直接用作发波余量,而失效方向是危险的那一侧。** 工具的 `pending waves $0.000` 说的是**「此刻没有机器在计费」**(它是 accrual census,读的是**在跑的机器**);章程 §2(甲) 的 `pending` 说的是**「近 12h 内已发波次 × 单波价」**(读的是**已经跑完但还没进 MTD 的钱**)。**W62 恰好是后者而不是前者** —— 它跑完了、自终了、四台一台不剩,所以 accrual census 看它是 0;而它的钱**证明不在** `$74.308` 里(§一 的时间戳)。**本台自己补这道算术**:

| 项 | 金额 | 依据 |
|---|---|---|
| MTD(budgets,`20:23:37Z`) | $74.308 | CE 逐位复核 |
| \+ W62(21:24Z,**按需**) | $2.150 | 快照早 61 分钟,**证明未入账** |
| \+ W61(09-09 15:23Z,**spot**) | $1.100 | 早于快照 5.0h,落在 4.3–11.3h 滞后窗内 ⇒ **朝安全侧计为 pending** |
| W60(09-09 09:23Z) | — | 距今 **14.9h**,**已滑出 12h 窗**,不计 |
| **安全侧 projected** | **$77.558** | |
| **对 $80 围栏的 headroom** | **$2.442** | |

⇒ **一波 spot(`~$1.10`,GH #454)塞得进去**(projected `$78.658`);**一波按需(`~$2.15`)塞不进去**(`$79.708`,只剩 $0.29,不留任何余量)。
⇒ 上一轮那句「下一个发波轮**结构上**发不出任何一波」**本轮已不成立** —— **W60 滑出窗口就让出了 $2.15**。⚠️ 这句纠正**不是**说上一轮算错了:上一轮那个 `$0.292` 在**它自己那一刻**是对的(W60 当时还在窗内)。**围栏是时间的函数,不是一个可以抄的数** —— 与工具自己那句 `Do not copy $80.00 into a report` 同源。

⛔⛔ **但「塞得进去」不等于「可以发」,而这一句是本轮最容易出事的地方。** 总监已在 `2026-09-09T22:xxZ` 就 GH #677 **裁定 `NO_WAVE_NEXT_LAUNCH_ROUND`**(commit `4efe8d71`,`W62_wave.json:director`,档案 `test_set.md §GI.1–§GI.3`),三条逐字要点:
1. **下一个发波轮不发波** —— 围栏照旧成立,且**必须当轮现跑 `wave_fence.py` 求值,不许抄 `$0.292` 或任何数**。
2. **「等月初重置」作为计划被否**:重置后的天花板是 **`$50` 不是 `$80`**(围栏 = 下一个尚未跨过的告警档;总监的离线重构打 `fence $50.00 / headroom $48.900`,并自带 `OFFLINE` 与 `SKIPPED, NOT CERTIFIED` 两行免责)。
3. **「现在升级给 owner」作为一封新邮件被否**:同一问题已随 W36 邮件(09-06)在 owner 手里(`DECISIONS_NEEDED` 第 15 条),以增量随周日 W37 那封发。

⇒ **本台不把「headroom 从 $0.292 回到 $2.442」自行解读成该裁定失效。** 裁定的**前提数变了**(而且是朝放松的方向),但**裁定本身是总监的,不是本台的** —— 章程第 5 行同源:本台交读数,判定归总监。**⇒ W63 不飞,除非总监在读到本轮 §五 的新前提后自己改口。** 本台把变更后的前提**交回去**(§十一 issue 1),**不代它裁**。

## 六、泄漏检查:干净,**而且这次是两条独立路径都说干净**

- **带过滤器**(`tag:Name=dota2bot-soak-spot-*`,`running,pending,stopping,stopped`):**空**。
- **不带过滤器**(`us-west-2` 全部非 terminated 实例):**空** —— 上一轮点名的那个缺陷形状(「用 `dota2bot-*` 过滤器查出来的空,回答的不是『那台外来机还在不在』」)**本轮用去掉过滤器的那次查询正面回答了**。
- **第三条,跨 region**:`wave_fence.py` 的 `accrual check` **17 个 region 全读**,`CERTIFIED (0 accruing instances account-wide)`。
- ⇒ **本台干净;账号侧此刻也没有任何机器在跑** —— 上一轮那台连跑 16.3h 的 `c7a` **已经停了**(它的钱已落账,§一)。
- ⚠️ **一个不作数的读数,登记为不作数**:按 id 查 `i-0ce7ddf08ea24cc7f` 返回**空**。**空输出不是「它没了」的读数**(终止的实例会从 API 老化掉)—— 载重的是上面那两条**正面**census,不是这一条。
- **常设成本**:仍只有 AMI `ami-0a990a26d89c66547`,无额外快照。
- **本轮支出**:CE `$0.01` ×1(`check_costs.sh` 自动复核)+ CE `$0.01` ×1(§一 的机型拆解,章程允许「确认近刹车读数」时花)= **`$0.02`**,**零 EC2**。

## 七、⭐ 本轮不发波 —— **两道各自独立、各自充分的理由**,而本台把下一波**算完但不下单**

`THROTTLE_EXIT=**3**`,**逐字**:

> `anchor wave      : W62 (4 machine(s))` / `last machine up  : 2026-09-09T21:24:41Z  <- the anchor (GH #469 ruling 1)`
> `unlock           : 2026-09-10T03:24:41Z` / `now              : 2026-09-10T00:19:23Z` / `margin           : 11118s SHORT of unlock`
> `A launch at this instant BREACHES gate (i) by 185m18s.` / `WAVE_THROTTLE: THROTTLED (exit 3) -- do not launch. Wait 11118s.`

⇒ **不是本台选择不发,是闸不让发。** ⭐ 而且**即便闸 (i) 已解锁,本轮也发不了** —— §五 那条总监裁定 `NO_WAVE_NEXT_LAUNCH_ROUND` 独立地挡在前面。**两道理由各自充分,本台两道都登记**(只写一道就会让读者以为「等到 03:24Z 就能飞」)。

本台把这 3 小时用在把下一波**算完**,而不是留给下一轮从零重做 —— ⚠️ **算完不是下单**:下面这一节是**交给总监的输入**,不是给下一轮的发波授权。

### ⭐⭐ W63 的可并池性:**本轮实测,它与 W62 是同一个程序 ⇒ 可并池 ⇒ 家族深度 4 → 8**

树差 `a91efb69`(W62 的钉)→ `0c6a89ab`(本轮 `git ls-remote origin main`),`bots/`+`game/` 共 **3 文件 +162/−17**。
**非注释改动恰好三处,逐处判它在 37-id 臂串下是否可达:**

| # | 位置 | 改动 | 可达? |
|---|---|---|---|
| (a) | `bots/BotLib/hero_zuus.lua:1351` | `X.nUltCashChaseRadius = 1600` → **`700`** | **不可达**。全文件只有一处读它(`:1464`),而那个函数的**第一条语句** `:1462` 是 `if not ( J.IsModeTurbo() and J.IsSoakCandidate( 'zusultstrand' ) ) then return false end`,**`zusultstrand` 不在臂串里** |
| (b) | `bots/BotLib/hero_skeleton_king.lua:1330` | `IsBoneGuardEnemyCountOk` 加参数 + 新析取项 `X.wk_CountBoneGuardEngaged(...) >= 2` | **不可达**。新析取项整个在 `if J.IsModeTurbo() and J.IsSoakCandidate( 'wkbonefight' )` 分支内,**`wkbonefight` 不在臂串里**。⭐ **未 gate 的落地路径逐字未变**:旧版 `:1282` 与新版 `:1336` **都是 `return nEnemies == 1`**(本轮从 `a91efb69` 的源码里直接读出来核对的,不是从 diff 猜的);gated 分支里那处 `>= 1` → `== 1` 同样在 `wkbonefight` 后面 |
| (c) | `bots/FunLib/jmz_func.lua:~7290`(`J.GetOffWaveHarassSpot`) | 侧别选择改读自己的 1200 半径普查(`tSideEnemies`)而非 `tEnemies` | **不可达**。**唯一调用点** `bots/mode_laning_generic.lua:406`,其 `if` 是纯合取且**以 `J.IsSoakCandidate('l5trees')` 开头**,`l5trees` **不在臂串里**。⭐ 码里那句自称「the one call site … is a pure conjunction」**本轮独立核实过**(`git grep GetOffWaveHarassSpot` 全仓库只有这一个调用点),**不是继承散文** |

⇒ **三处全部不可达** ⇒ 在 37-id 臂串下,`0c6a89ab` 与 `a91efb69` 是**同一个可达程序** ⇒ 按 §GH.5.4 **W63 与 W62 可并池** ⇒ 家族深度 **4 粒 → 8 粒**。
⭐ **这正是本台连续几轮抱怨的「家族深度被退集节奏封死(depth-1 跑步机)」的反面**:这一波**买的是深度,不是又一个 depth-1 家族**,而且它是九月围栏下**唯一还塞得进去的那一波**。

### ⛔ 不是发波单,是**给总监的一份「如果解禁,该发什么」的规格** —— `NO_WAVE_NEXT_LAUNCH_ROUND` 未撤销前,**下一轮不发波**

⇒ **交给总监的问题(§十一 issue 1)恰好一句**:裁定 `NO_WAVE_NEXT_LAUNCH_ROUND` 立时的前提是 `headroom $0.292`(发不出任何一波);现在前提是 **`$2.442`(恰好一波 spot)**,而那一波**不是又一个 depth-1 家族,是把唯一测过窄化 `ownhalf` 的家族从 4 粒加深到 8 粒**,并且**九月只剩这一次机会**(10-01 之后天花板降到 `$50`,而外来支出的速率不在本台控制内)。**请总监裁:维持还是解禁。** 若解禁,规格如下(以下每一条都要**当轮重跑**,本节是读数不是授权):

- **市场:spot**(闸 (iv) exit 2 = UNDECIDABLE,不构成按需裁定 ⇒ 落回 owner 常设 spot 默认;W62 记录也这么写)。**容量阶梯**:`c6i.4xlarge` spot → `c6a.4xlarge` spot → 才 `--on-demand`,降级要在报告里写错误码。
- **臂串:37 id / 335 字节 / md5 `b525d51d4b4957e0e40f22f203aea641`** —— ⚠️ **发波轮必须自己在那一刻重算**(GH #631),本行是**读数不是授权**。
- **钉:发波那一刻的 `git ls-remote origin main` 40 位全 SHA**,不是 `main`,不是本报告里的 `0c6a89ab`(本轮的收尾 commit 会推动它;§七的可达性结论要在新钉上**重跑一次**,三处 gate id 都不在臂串里这件事很稳,但**稳不等于免检**)。
- **围栏:当轮现跑 `wave_fence.py`,不抄本报告的 `$2.442`**(总监 09-09T22:xxZ 明令;而且 §五 刚证明围栏是时间的函数)。**若届时 MTD 已被新落账的外来支出推高 ⇒ 该轮可能真的一波都发不出,那就停下来按 GH #677 走。**
- **种子**:与 W62 的 10601/10607/10803/10813 **互斥**(走 `seed_roster_index.py` / `seed_draft.py`)。
- **发波记录**:`launched_at` **和** `launch_time` 两个键都要写(GH #544,W55 那次只写一个就把下一轮的闸 (i) 打成 `UNCERTIFIABLE`)。

## 八、queue.json:全表无一行申请新波次

逐行扫过:`status=running` 三行(`strategy-31` TORMSELF、`strategy-32` IMMGUARD、`hero-27` SAVEMANA),
**三行都在自己的文本里自述「零 EC2 / 零新局 / 零 AWS 增量 / 不申请专波」**;
`pending` 行数十条,**全部**是归档语料扫描 / 搭车类(`ROUTED_ARCHIVE_SCAN` 家族),**无任何一行申请新波次、专机或新机时**。
⇒ 若解禁,本台按章程 (b) 走例行「测试版 vs 稳定版」即可,**不需要为谁开专波** —— 也就是说,**没有任何一个下游组因为 `NO_WAVE_NEXT_LAUNCH_ROUND` 而被卡住**(这一句是给总监裁「维持还是解禁」时用的:维持的代价**不是**下游停摆,是**九月唯一一次把家族从 4 粒加深到 8 粒的机会过期**)。

## 九、开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1; echo EXIT=$?` —— 脚本超出 400s 工具超时被移入后台,**收尾前跑完并取到真退出码**:**`SELFCHECK_EXIT=3`**。
工具自己的 `exit sources` 节**逐字**(⚠️ 它自带警告:`Read this before writing "exit 3, all of it cadence": that sentence was hand-made attribution for days, and on 2026-08-27 it was wrong for 22 hours (GH #267)` —— **照办,本台只登记来源不做归因**,归因属总监):

> `legs run              : 11`
> `FINDINGS (exit 3)     : cadence queue-rulings owed-executions a-evidence-owed`
> `UNCERTIFIABLE (exit 2): trunk-red(python)`
> `NOT RUN (inside a leg): tests/test_luacheck_gate_soakswitch.py tests/test_selfcheck_lua_leg.py`

- **python 腿 `UNCERTIFIABLE`**,逐字 `49 checks, 0 failures, 9 uncertified` / `UNCERTIFIABLE -- 9 check(s) did not run; this is NOT a pass.`,
  且工具自带限定 `it is NOT evidence that trunk is red: re-run on a quiet tree`。**照抄,不引申。**
  9 条未核验的原因工具自己写了:`the clean run did not finish inside 120s`(容器慢,`5a0 cost: 87 file(s) ... in 120.1s (budget 120s)`;工具**自己**提醒 `wall clock cannot separate a grown discovery set from a slow container`)。
- ⚠️ **`UNCERTIFIABLE`/`NOT RUN` 不是通过**(GH #171)⇒ **python 那一侧本轮没人完整看过**,登记为**没看过**。
- **Lua 检测器腿**:**`87 tagged detector file(s), 0 failures`**,工具的限定一并抄:**`FAST SUBSET, not the full suite.`**
- **稳定版锚点(铁律 3)**:`stable-v1..v6` 全 `EXISTS ok` ⇒ `6 anchor(s) checked -- OK`。
- **promote-time 约束(pullcad 陷阱 + co-promote 原子)**:**`FROZEN none (no live gate names a promoted id)`**;`promote-atom constraints: OK`(5 个原子全 `ok`,含 `tower_fear_ring_disjunction` / `impale_kill_needs_damage`)。
- ⭐ **头号操作教训(第一条命令就踩)**:`routine_selfcheck.sh` 现在会**主动拒绝**通过管道调用 ——
  逐字 `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.` / `this is evidence discipline 3; it has recurred 5x, every time as the first command of the round.` **本台就是第 6 次**,改用重定向后正常。这道拒绝门**有效**。

## 十、铁律 6 / 11

- `bots/` / `game/` / `tests/` / `tools/` **本会话一行未改**(只写 `iterations/`)。§四的 bracket 写死常数、§五的 `pending` 语义歧义,**本台都没自己去改**,只登记 + 交棒。
- 静态门与 push 路径见文末补记(收尾时填)。
- 铁律 11:全程无 `requires approval`,**零空转** —— 闸 (i) 挡住发波之后,本台没有原地等 3 小时,而是把 W63 的可并池性、市场、臂串、种子约束全部算完交棒(§七)。

## 十一、本轮要交出去的 issue

1. **GH #683 `[batch]`(接 #677 的裁定,请总监复核前提)** —— 四件事一起交:(甲) **裁定 `NO_WAVE_NEXT_LAUNCH_ROUND` 的前提数已变**:立时是 `headroom $0.292`(发不出任何一波),现在是 **`$2.442`**(恰好一波 spot)—— **本台按裁定不发,把改口权交回总监**;(乙) 若解禁,那一波**不是又一个 depth-1 家族**:本轮实测三处树差在 37-id 臂串下**全部不可达** ⇒ 与 W62 **可并池** ⇒ 唯一测过窄化 `ownhalf` 的家族 **4 粒 → 8 粒**,且**九月只剩这一次**(10-01 后天花板降到 `$50`);(丙) 外来支出**已落账**的硬读数(`c7a $23.281 + r7a $14.155 = $37.435 = MTD 的 50.4%`),#677 里那个「未复查」的量现在有数字了,**并且比上一轮估的上限还高**;(丁) 根因仍是 `dota2bot-batch` 的 `CostFilters = null`(GH #515)—— **建议给它加 `Project` 标签过滤器,或给 `final-table-trainer` 单开预算**,否则 owner 收到的 `$80` 告警**抬头是批测台而钱是别人的**(这一条已在 `DECISIONS_NEEDED` 第 15 条随周日 W37 邮件走,**本条只补数字不另发信**,遵总监 §GI.3)。
2. **GH #684 `[harness]`** —— `wave_fence.py` 的 `pending waves` 与章程 §2(甲) 的 `pending` **是两个不同的量**(在跑的机器 vs 近 12h 已发未入账的波),而工具那句 `pending $0.000 is a reading, not a default` 会让读者以为它已经把后者算进去了。**失效方向是危险的那一侧**(headroom 被高报 W62+W61 = `$3.25`)。建议:工具读发波记录目录自己算 12h 窗,或把那行改成明说「this does NOT include waves that already finished but have not accrued」。
3. **GH #685 `[batch]`** —— 闸 (iv) 在按需波上**结构性拿不到读数**(§四):`StateReason.Code` 1h 老化 + 按需无 SIR 可退 ⇒ 只要收割轮晚于 T+1h,按需波的闸 (iv) **必然** `UNDECIDABLE`。W62 是第二例。建议:发波脚本在自终后**主动把终止码写进 `<wave>_wave.json`**,或收割改由发波轮 +1h 的一次轻量轮次抓码。

## 十二、Token 用量

`TOKENS total_in=7,123,496 out=50,535 turns=60`(统计时刻 `2026-09-10T00:4xZ`,之后的收尾回合不计入)。

## 十三、收割产物

`iterations/reports/batch-desk/waves/W62_verdict.json` —— `recover_verdict.py` 本轮的完整输出原样存档
(与 `W55_verdict.json` / `W58_verdict.json` 同惯例),含四粒 `per_seed` 的 `_ab`/`_ba` 全字段与 `strata`,
**报告里的每一个数都可以在这个文件里逐位对回去**。

## 十四、铁律 6 / 11 与 push 路径

- `bots/` / `game/` / `tests/` / `tools/` **本会话一行未改**(只写 `iterations/`)。§四的 bracket 写死常数、§五的 `pending` 语义歧义、§九的按需终止码缺口,**本台都没自己去改**,只登记 + 交棒(#685 / #684 / #683)。
- **静态门跑过且干净**:`ARM_EXIT=0`(`core.hooksPath` 已上膛);`.githooks/pre-push` 两次 push 各跑一次,逐字 **`luacheck bots game: 0 warnings`** / **`GATE_EXIT=0  CLEAN (iron rule 6 static half passed)`**;py gate **`89 ran, 0 findings, 0 uncertifiable, 23.9s`**,并自带声明 `NOT claimed here: 30 slower python tests ... and the Lua dynamic half (GH #124)`。**未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行。** Lua 动态半(~100min,GH #124)**未跑,不声称**。
- ⚠️ **两件不许混的读数并排登记**:自检的 `trunk-red(python)` 腿是 **`UNCERTIFIABLE`(没跑成)**,而 pre-push 的 py gate 是 `89 ran, 0 findings`。**这不是矛盾,是两件仪器跑两份清单**(pre-push 自己声明只覆盖 84 个 fast ratchet)。**本台不用前者的干净读数去覆盖后者的没跑成。**
- **push 路径**:`git push -u origin claude/great-sagan-cldszb` **`PUSH_BRANCH_EXIT=0`**;`git push origin HEAD:main` **`PUSH_MAIN_EXIT=0`**(`0c6a89ab..7a90b01b`,**一次通过,未被拒**)。
- **发表顺序(GH #290)**:**先 push 后发表** —— 三条 issue 均在 `7a90b01b` 落 `main` 之后发出;草稿 `claim_precheck.sh` **`PRECHECK_EXIT=0`**,逐字 `clean` / `OK to publish: every citation resolves on origin/main.`
- **铁律 11**:全程无 `requires approval`,**零空转** —— 闸 (i) 挡住发波之后没有原地等 3 小时,而是把 W62 与 W60 的并排、W63 的可并池性(三处树差逐处判)、市场、臂串、种子约束全部算完交棒。
