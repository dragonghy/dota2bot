# batch-desk 2026-09-12T00:11Z — W67 收割轮(零发波)

**一句话**:W67 四台按需全配对收回,与 W66 并池后 **30-id 家族第一次拿到深度 5**(239 局,
五粒种子无一 thin arm);⭐⭐⭐ 头号不是读数,是 **W67 自己写下的收割义务在写下的那一刻就不可能被满足** ——
它点名本波是 GH #454 (乙2) 「最干净的测点」,理由是四台按需**必然**走到 `instance-terminated-by-user`,
而 `reclaim_blind.py` 自己的文档第 2–3 条早就写着:**按需波的 `status_code` 在收割时是永久 null**。
本轮零发波(闸 (i) 距解锁 190 分钟),**本轮新增支出 = CE 两次 $0.02,无 EC2**。

---

## 一、支出与围栏(全部当轮现跑,⛔ 下轮不得抄作前提)

- `AWS_SETUP_EXIT=0` — `AWS ready: arn:aws:iam::581436534924:user/dota2bot-agent`
- `COST_EXIT=0` — MTD **`$83.035`**,`budget refreshed 2026-09-11T21:41:17Z`,
  CE 复核 **`83.0348379061`** 逐位一致。
- **围栏 `FENCE_EXIT=0`**(`--planned 0 --pending 3.25 --snapshot-instant 2026-09-11T21:41:17Z`):
  - `crossing registry: 0 record(s) ... none in force -- the gate runs at the DERIVED fence.`
  - `operative ceiling: $90.00   = min(fence, brake)`,其下**无** `DIRECTOR CROSSING:` 行。
    ⇒ **总监 RULING 15 的退休第二次在下游闸上实测兑现**(上一轮结清欠条,本轮独立复现)。
  - `fence $100.00` / `brake $90.00` / `actual $83.035` / `pending $3.250` / `planned $0.000`
    / `projected $86.285` / **`headroom $3.715`**
  - `budget filters : none` / `accrual scope : 17 region(s) read <- COMPLETE`
  - `accrual check : CERTIFIED ZERO (0 accruing instances account-wide), but --pending $3.250 was given and is used as the larger claim.`
  - ⚠️ **必须抄的那一行**:`WAVE_FENCE CLOCK : the wave-accrual window above was anchored to
    snapshot instant ASSERTED by the operator (--snapshot-instant) -- a claim, not a reading.`
  - `--pending` 自推导:cutoff = `21:41:17Z − 11.3h` = **`2026-09-11T10:23:17Z`**;
    W66 `15:24:58Z`(spot `$1.10`)+ W67 `21:25:21Z`(按需 `$2.15`)**均在 cutoff 后** ⇒ `$3.25`;
    W65 首台 `09:23:35Z`、W64 补跑 `03:19:5xZ` **均在 cutoff 前** ⇒ 不计。
    工具自己点名 `must cover : 2 wave(s) listed above (W67, W66).` —— 本轮**与工具清单一致**,
    无 GH #742 那种手加项。
- **⚠️ 本轮自己的一笔浪费,留档**:泄漏检查时为取 `running/pending` 块**重跑了一次
  `check_costs.sh`**,而该脚本在读数 ≥ `$35` 时会自动再花 `$0.01` 走 CE ⇒ **本轮 CE 共两次 `$0.02`**。
  只要 `describe-instances` 就够了。**下轮:泄漏检查直接用 `describe-instances`,不要为了那个块重跑 `check_costs.sh`。**
- ⚠️ **owner 可见项**:`$50` / `$80` 两条 ACTUAL 告警**均在 `ALARM`**,`forecast 191.545`;
  `budget filters : none` ⇒ 外来支出不受本台任何闸约束。

## 二、⭐⭐⭐ 头号:W67 的收割义务在写下的那一刻就不可满足

W67 的 `harvest_obligations` 第 5 条逐字写着,本波是 GH #454 (乙2)
「the cleanest measurement point the desk has had in several rounds -- four machines, none
reclaimable」,判据是每台都会走到 `instance-terminated-by-user`。

**而 `reclaim_blind.py` 的文档第 2–3 条(GH #699,总监 09-10 立)早已写着**:按需机器没有 spot
request,唯一可能的码是 EC2 `StateReason.Code`,它**在 `describe-instances` 里约 1 小时过期
(GH #375)**,而波次节奏把收割排在起飞后 ~3 小时 ⇒ **按需波的行到了收割时 `status_code` 是
`null`,而且是永久的**。⇒ 那条义务**不是没做到,是不可能做到**,并且**否定它的那句话就写在
本台每轮都要调的那个工具里**。

**本轮实测坐实**(不是引用):四个 id 全部 `Reservations: []`、`exit 0`。
⭐ **并且跑了判别控制,免得把「读不到」读成「什么都没发生」**:把一个真 id 改一个字符 ⇒
`InvalidInstanceID.Malformed` **`exit 255`**;真 id ⇒ `Reservations: []` **`exit 0`**。
⇒ 空列表是**过期**不是盲查。**与 GH #750(SIR 保质期)同族,但这条已被 #375/#699 记载在案,
本台不重复立案。**

⛔ **诚实边界**:本轮**未**尝试 `cloudtrail lookup-events`(W62 09-10 试过,`AccessDeniedException`)。
「永久不可恢复」是照抄 #699 的结论 + 本轮 `describe-instances` 实测,**不是本轮独立证明的**。

## 三、闸 (iv):GH #699 的设计第一次在「全产出的按需波」上兑现

`RECLAIM_BLIND_EXIT=2` 第一次(`UNDECIDABLE: seed 11574: missing leg count 'ab'`)——
**与上一轮 W65 同形**:记录里没有腿数。回填后 **`RECLAIM_BLIND_EXIT=0`**,逐字:

- `yield : 4 paired seed(s) of 4 machine(s)`
- `unread : 4 of 4 machine(s) do not supply a field this check reads`(码与存活各四条)
- **`immaterial : all 256 extension point(s) agree, so the verdict below does not rest on the unread field(s).`**
- `VERDICT: not blinded -- the wave delivered 4 paired seed(s).`
- **`NEXT WAVE: spot (the default; GH #158 unchanged).`**

⇒ ⭐ **「闸不得索要自己答案并不依赖的数据」在它立法所针对的那个形状上第一次真跑通**:
W62 是产出低的按需波,W67 是**产出满**的按需波,四个洞全部 immaterial。
⛔ 本台**没有**为了让闸过去编任何码。

**回填的逐字段出处(全部写进 `W67_wave.json`,不混成一句)**:
| 字段 | 出处 | 性质 |
|---|---|---|
| `ab` / `ba` / `arm_depth` / `scored` | 本轮 `recover_verdict.py` 全量重算 | **第一手** |
| `instance_id` | `20260911T212500Z.md §五` 散文 | **转载**,已无法再向 AWS 核对 |
| `survival_min_lower_bound` | `launched_at` → 该 run 最后一个 S3 对象 | **下界**,故意**不**写进 `survival_min` |
| `status_code` | 无 | **不可恢复**,按 W62 先例带 `status_code_unrecoverable` 块 |

## 四、收割:W67 单独 + 与 W66 并池(深度 0 → 深度 5)

标准路径 `recover_verdict.py`,**五个 run 各下各的目录**(GH #225);臂串喂
`W67_wave.json:arm_string`(**不喂活树**),与 W66 **逐位相同**(30 id / 266 B / md5
`7d4c638dab526c68d9404526a3195663`,本轮现算复核)⇒ 并池腿 1 成立。
W66 三台被回收的 run 前缀**零 `.analysis.json`**(与 48 秒回收一致),已从池中移除。

**W67 单独**(`W67_EXIT=0`,193 局,4/4 计分,无 thin arm):

| 指标 | mean | ab | ba | comps_better |
|---|---|---|---|---|
| gpm | **−11.40** | −95.28 | +72.48 | 2/4 |
| xpm | +0.04 | −55.79 | +55.86 | 2/4 |
| deaths | +0.17 | +0.43 | −0.09 | 0/4 |
| last_hits | −0.36 | −6.80 | +6.09 | 2/4 |

**并池 W66+W67**(`POOL_EXIT=0`,**239 局 / 5 粒全部计分 / `thin_arm_seeds: []` / `min_arm_depth 8`**):

| 指标 | mean | ab | ba | side | comps_better |
|---|---|---|---|---|---|
| gpm | **−11.78** | −99.09 | +75.54 | −87.31 | 2/5 |
| xpm | +4.45 | −52.06 | +60.96 | −56.51 | 3/5 |
| deaths | −0.03 | +0.54 | −0.59 | +0.56 | 1/5 |
| last_hits | +0.65 | −5.68 | +6.98 | −6.33 | 3/5 |

`suggested: hold_or_reject`。每粒 gpm:11395 −13.28 / 11574 −39.12 / 11646 +5.53 /
11678 −37.43 / 11856 +25.42(**跨种子离散度极大,极差 64.5**)。

**铁律 4 逐条**:
- **(i-a)** 两层**读数**已登记(上表 ab/ba 两列),取自 `per_seed[].<m>_ab`/`_ba` + `strata.<m>`,
  **未手算**。
- **(i-c)** 四个指标 `sign_flip: true`、`side_gt_arm` 4/5–5/5。这四个量**每粒种子 50/50 swap-average
  ⇒ 侧偏已消除** ⇒ **反号不是否决理由**,它等价于 `|side| > |arm|`,是恒等式不是诊断。
  **登记,不写进结论。**
- **(i-d)** **未按局数加权**:每粒先 swap-average,再跨种子取算术平均。
- ⛔ **本台不裁好坏**,判读归协同组/总监。

## 五、⭐⭐ 胜负通道连续第六波 DEGENERATE,而且方向与写在 CLAUDE.md 里的侧偏**相反**

`GH #352 winrate channel DEGENERATE: minority side won 2 of 268 finished games (share 0.0075 < 0.20).`
本轮把六波的侧别普查拉齐(**跨三个不同臂串家族:37-id / 34-id / 30-id**):

| 波 | radiant 胜 | dire 胜 | 少数侧占比 | 结束方式 |
|---|---|---|---|---|
| W62 | 12 | 215 | 0.0529 | 227/227 `engine_natural` |
| W63 | 1 | 175 | 0.0057 | 176/176 |
| W64 | 1 | 221 | 0.0045 | 222/222 |
| W65 | 2 | 220 | 0.0090 | 222/222 |
| W66 | 0 | 52 | 0.0000 | 52/52 |
| W67 | 2 | 214 | 0.0093 | 216/216 |
| **合计** | **18** | **1097** | **0.0161** | **1115/1115 自然结束** |

⭐ 三件事同时成立:(1) **与臂串无关**(三个家族、六波、同一形状);(2) **不是回收产物**
(W67 四台按需全配对,W62 同样满产出);(3) **全部自然结束**,无一局被改写成经济赢家
(`winner_by: {engine_natural: N}`,`winrate_independent_of_gold` 满分)。
⇒ **赢的那一侧恰好是 CLAUDE.md 记载「+1.5k 金」优势的对立面**(dire)。

⇒ **铁律 2(b) 的胜负半边已结构性缺席第六波**:`comps_better.winrate` 的池化分数被
headroom-0 的种子压住(本波 3/4 粒 headroom 0,`A promote bar above 1/4 is unreachable on this corpus.`)。
⛔ **本台不主张成因、不代裁、不建议放宽或收紧任何门。** 这是上一轮交棒 ①② 的同一件事,
本轮把它从「一波 222 局」抬成 **「六波 1115 局、跨三个家族」**。

## 六、启动决策:不发波(闸 (i) 现跑)

`THROTTLE_EXIT=3`,逐字:`anchor wave : W67 (4 machine(s))` / `last machine up :
2026-09-11T21:25:21Z  <- the anchor` / `unlock : 2026-09-12T03:25:21Z` / `now :
2026-09-12T00:15:15Z` / `margin : 11406s SHORT of unlock` /
`A launch at this instant BREACHES gate (i) by 190m06s.` / `WAVE_THROTTLE: THROTTLED (exit 3) -- do not launch.`

⭐ **顺带:这一次现跑同时核了总监 RULING 18 的修复** —— 闸 (i) 打出
`family candidate : W67  last machine 2026-09-11T21:25:21Z` ⇒ **它确实读到了回填后的
`launched_at`,锚点不再瞎**。RULING 18 在总监侧已自测,本轮是**执行侧的独立复现**。

⇒ 本轮是**纯收割轮**,`--planned 0`,**零 EC2 支出**。

## 七、泄漏三路全零

(1) `check_costs.sh` 的 `running/pending` 块**空**;(2) us-west-2 账户级 `describe-instances`
**不加 tag 过滤**、状态 `pending,running,stopping,stopped` ⇒ **`[]`**(`LEAK2_EXIT=0`);
(3) `describe-volumes` ⇒ **`[]`**(`LEAK3_EXIT=0`)。常驻仅 AMI `ami-0a990a26d89c66547`。
账户级 17 区零由闸 (iii) 的 `CERTIFIED ZERO` 单独买,**不与上面三条合并成一句**。

**收割零欠(查过不是假定)**:`validation/` 按 `LastModified` 倒排,最新对象
`2026-09-11T22:22:08Z`,全部属 W67 四个 run;W66 的最新对象 `16:16:55Z` 已由上一轮收割。
⇒ `harvest_owed = NONE`。

## 八、GH #454 两条腿

- **(乙1) 账单侧实测单波成本:第 13 轮欠着,且本轮按它自己的判据就不该结清。** W67 记录第 6 条
  义务写的是「cutoff 越过 `21:25:21Z` 才能结清」,而本轮 cutoff = **`10:23:17Z`**,
  盖不住 W66 也盖不住 W67。⛔ **不用「MTD 增量对得上波费」反推**(章程步骤 2 乙明令禁用;
  MTD `$80.413 → $83.035` 的 `+$2.622` 本台**不做任何归因**)。
- **(乙2) 机时:`0.8462 h/台`(n=4),不触发 `1.00 h/台`。** ⚠️ **但这不是义务指定的那条路**
  —— 那条路(`status_code` 分辨哪些台该计入)已按 §二 永久关闭。本台改走**按构造**推导:
  **按需台不可能被容量回收**,故四台全部计入。
  机时取自 `launched_at → 该 run 最后一个 S3 对象`,**是下界**
  (0.9489 / 0.8689 / 0.8694 / 0.6978,最后一次上传之后的关机尾巴未计)。
  ⇒ 要把均值推过 `1.00`,需要每台再多 **~9.2 分钟**的尾巴。
  ⛔ **本台不声称已测出真值**,只声称**下界均值 0.8462 且不触发**。

## 九、自检(铁律 10)与铁律 6

⚠️ **管道坑第 16 次,本轮本台自己踩的**:开工第一条命令 `... | tail -40`,工具当场
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.` ——
**`SELFCHECK_EXIT=2` 什么都没检查,不是通过**。改**重定向 + 后台 + 不设短 timeout** 重跑
(中间还有一次 `timeout 300` 把它砍成 `EXIT=124`,同样不是读数)。

**已逐字读到的部分**:`FROZEN none (no live gate names a promoted id)`;
**倒像普查** `INVERSE-GATE CENSUS  live gate ids 201  armed 30` / **`FROZEN 0`** /
`COUPLED 1` / `UNRESOLVED(armed) 0` / `no armed id hangs under an unarmed gate -- OK`
⇒ **`pullcad` 陷阱本轮为零**。`COUPLED` 仍是 `fieldsip` 挂在七个宿主之下
(`buyband`/`buydeep`/`buyring`/`buytower`/`fieldbuy`/`stayfield`/`stayfield2`);
⚠️ **本轮核过为什么今天仍不是缺陷:七个宿主全在 30-id 臂串里** ⇒ 无一被 promote/退集 ⇒ 域未塌;
⭐ **但这仍是给下一次 promote 裁定的活警告**(第三轮连续登记)。

⛔ **收尾时自检仍卡在 `=== trunk health (python test suite) ===` 未跑完 ⇒ 本轮无
`SELFCHECK_EXIT=`,没读到的退出码不写,也不据此推断 trunk 是绿的。**

⛔ **本会话 `bots`/`game`/`tests`/`tools` 一行未改**(改动全在 `iterations/`)⇒
trunk 上任何红都**不可能由本轮引入**。铁律 6 三条腿读数见 §十一(push 之后回填)。

## 十、交棒

1. ⭐⭐⭐ **总监 —— 收割义务可以写成不可满足的,而且否定它的话就在本台每轮都调的工具里**(§二)。
   建议:发波轮写 `harvest_obligations` 时,凡点名 `status_code` 的义务,按需波一律**同时**写下
   「本波按 GH #699 该字段在收割时是永久 null」,否则下一轮会去追一个已被记载为不存在的东西。
   ⛔ 本台不自行改 harness。
2. ⭐⭐⭐ **总监/owner —— 铁律 2(b) 胜负半边缺席第六波,证据已从 222 局抬到 1115 局、跨三个家族**(§五)。
   请裁 (a) 通道恢复前 2(b) 是否明确改写成「经济代理量 + 录像 WORKING」让字面与执行一致,
   (b) GH #352 的 `≥0.20` 是否还是可达的线。⛔ 本台不代裁。
3. ⭐⭐ **录像组 —— 一等线索,且比上一轮硬**:dire 赢 1097/1115,**全部自然结束**,
   **跨三个臂串家族** ⇒ 与任何单个 armed id 无关。建议逐帧看一局 radiant 方的自然落败,
   定位遗迹被推掉前最后一个可挽回决策。⛔ 本台无逐帧证据不主张成因。
4. ⭐⭐ **协同组/总监 —— 30-id 家族第一次有深度 5 的读数可判**(§四):
   gpm −11.78 / 2/5,`suggested: hold_or_reject`,跨种子极差 64.5。
5. ⭐ **总监 —— RULING 18 的修复已在执行侧独立复现**(§六),RULING 15 的退休第二次兑现(§一)。
6. ⚠️ **GH #454 (乙1) 第 13 轮欠着**(§八),(乙2) 本轮有下界读数但**不是义务指定的那条路**。
7. ⚠️ **自检第三轮未跑完 python 腿**,本轮无 `SELFCHECK_EXIT=`。

## 十一、下一轮本台

1. **闸 (i) 解锁 `2026-09-12T03:25:21Z`**(⛔ 别抄,跑 `wave_throttle.py`);在其后触发即可发波。
2. **市场 = spot**(闸 (iv) 本轮逐字 `NEXT WAVE: spot`),但**当轮现跑** `reclaim_blind.py`。
3. **30-id 家族现在深度 5** ⇒ 下一波若仍是同串,闸 (ii) 的腿 2(`5 < 8`)仍成立,但**腿 1 大概率照旧为空**;
   ⛔ 收割时不得写成「新杠杆的独立重复」。
4. ⛔ **围栏/闸/成本一律当轮现跑**:本条目的 `$83.035`、cutoff `10:23:17Z`、`headroom $3.715`、
   解锁 `03:25:21Z` **全部作废不得抄作前提**。
5. **泄漏检查直接用 `describe-instances`**,别为了那个块重跑 `check_costs.sh`(本轮多花 `$0.01`,§一)。
6. ⚠️ 开工第一条命令**重定向 + 后台 + 不设短 `timeout`**(**第 17 次**登记)。
7. **回填本轮 issue 号**(§十二)。

## 十二、本轮 issue

见文末回填。

## 十三、Token 用量

见文末回填。
