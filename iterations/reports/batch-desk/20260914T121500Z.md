# batch-desk 2026-09-14T12:15Z — 第十七轮:零发波,但**不是**零产出

**一句话**:闸 (iii) 连续第十七轮 `exit 3`(headroom **$0.142**),⇒ **零发波、零收割欠、零泄漏**;
本轮的工作单元是上一轮补记 B-bis 自定的那件**不花 EC2 的活** —— `hero-27` 归档扫描 **已完成并落成文件**。
本轮 AWS 新增计费 **不是 $0.00**:CE `$0.01` + S3 出网估 **~$0.4**(见 §5,已开 GH #819)。

---

## ⭐⭐⭐ 头号:刹车将**由闲置**越过,不是由测试

上一轮起「戳」第一次变了(`2026-09-13T20:32:29Z` → **`2026-09-14T10:54:23Z`**),
于是两条写死的判据**同时解锁**,本轮**照字面执行了**:

1. **买 `$0.01` 逐日 CE**(判据:戳变了 且 时点 > `02:00Z`)⇒ **买了**,顺带 `GROUP BY SERVICE`(同一次请求,不加价)。
2. **推 owner 通知**(判据:戳变了 且〔headroom 跨 `$1.10` **或** 零批测日完整行第一次可得 **或** MTD 越 `$90`〕)
   ⇒ 第二个析取项 **「零批测日完整行」本轮第一次拿到** ⇒ **推了**(连续 12 轮不推之后的第一次)。

**那张完整行(09-13,一个零批测日)**:

| service | 09-13 |
|---|---|
| Amazon Simple Storage Service | **$0.6599** |
| AWS Cost Explorer | $0.1200 |
| Amazon Route 53 | $0.0007 |
| **合计** | **$0.7806** |

⭐ **算术**:MTD `$89.858`,headroom `$0.142`,而**闲置日烧 `$0.78`** ⇒ **约 4.4 小时后 MTD 自己越过 `$90`**;
到 09-30 还有 16 天 ⇒ **~$102**,**越过 `$100` 的 budget limit**,而这中间**一波都没发**。
⇒ **「刹车挡住了测试」这句话从本轮起不再准确:刹车即将挡不住任何东西,因为越线的是闲置本身。**
GH #779 的三选一因此**从"几天"变成"几小时"的时效**。

⚠️ **诚实边界**:$0.78 是**一天**的读数,不是趋势;`forecast 196.979` 原样登记**不据以决策**。
MTD 单调上升 + `brake` 是常数,这两条才是上面那个结论的全部依据。

## ⭐⭐ 第二:那 `$0.66` 的 S3 **不是存储**,是 agent 自己在读桶

桶现读 **184.5 GB / 81,897 对象** ⇒ 按牌价存储 ~**$0.141/天**,只解释 **~21%**。
两个"隐形存储"候选被现读**排除**:`get-bucket-versioning` **空**(未启用)、
`list-multipart-uploads` = **1**(且有 `abort-incomplete-mpu-7d`)。
⇒ 余下 **~$0.52/天**只能是请求与出网,而零批测日里读这个桶的**只有 agent 自己**。

⛔ **这直接推翻了 `ROUTED_ARCHIVE_SCAN` 裁词里的「零 AWS 增量」** —— 归档扫描要把 `.dem` 拉出来,那是计费的。
**本轮自己就是一笔**:W34 的 103 份 = 2.13 GB,按 ~$0.09/GB 估 **~$0.19**,**且因一个 bug 重跑过一次 ⇒ ~$0.4**。
而 headroom 是 **$0.142** —— **一次"零成本"的归档扫描比全部余量还大**。已开 **GH #819 `[bug]`**。

⚠️ **诚实边界**:`dota2bot-agent` **没有** `cloudwatch:GetMetricStatistics`(本轮实读 `AccessDenied`),
所以**计费存储量测不到**,`$0.141` 是按 `s3 ls` 可见量 + 牌价**算的**。
⇒ 「~$0.52 是请求+出网」是**推论不是测量**;要坐实需要一次 `GROUP BY USAGE_TYPE` 的 CE 读($0.01),
**本轮没买**(headroom 只剩 $0.142,且它不改变结论方向)。

---

## 一、MTD 与闸

`COST_EXIT=0`(⛔ 未走管道,重定向到文件后裸读):

```
89.858	USD
   source: budgets/dota2bot-batch (free) | forecast 196.979 | budget limit 100.0 | budget refreshed 2026-09-14T10:54:23Z
   >= $35, but headroom to the $90.00 brake is $0.142
   < $1.10 (cheapest wave) — CE confirmation SKIPPED, NOT passed (GH #801).
```

| 读数 | 09:11Z(上轮) | **本轮 12:15Z** |
|---|---|---|
| MTD | `$89.505` | **`$89.858`**(+$0.353) |
| `budget refreshed` | `2026-09-13T20:32:29Z`(冻 5 轮) | **`2026-09-14T10:54:23Z`(换了)** |
| headroom | `$0.495` | **`$0.142`** |

⭐ **冻结期结束,支出侧恢复测量**:`$0.353 / 14.37h` = **$0.59/天**,与逐日表的 09-13 `$0.78` **同量级但不同值**
(两个独立 API + 账单滞后;⛔ 不把它们当成同一个数的两次读,也不取平均)。

**(iii) 围栏/刹车 —— `--planned 1.10` ⇒ `FENCE_EXIT=3`**(连续第十七轮是**闸拒绝**,不是本台的处置):

```
accrual scope    : 17 region(s) read  <- COMPLETE: every region this account has enabled
accrual check    : CERTIFIED (0 accruing instances account-wide, read this run) -- pending $0.000 is a reading, not a default.
actual (MTD)     : $89.858      pending waves : $0.000
planned          : $1.100       projected     : $90.958
fence $100.00 / brake $90.00 / operative ceiling $90.00
WAVE_FENCE: THROTTLED (exit 3) -- brake, not fence. Stop and report to the owner; do not wait it out.
```

缺口 **$0.858**。告警档:`$50` crossed / `$80` crossed / `$100` NOT yet crossed。
⛔ 未用 `--no-accrual-check` / `--no-crossing-file` / `--director-crossing` / `--snapshot-instant` / `--pending`
⇒ **本报告无任何「SKIP 不是 pass」行**(铁律 6 的 `RULE6_BYPASS` 同样**未用**)。
⚠️ `crossing registry: 0 record(s) ... none in force`,`1 retired`(GH#754/RULING 15)⇒ `operative ceiling` = brake 本身。

**(i) 节流 —— `THROTTLE_EXIT=0`**:锚 **W69**,`unlock 2026-09-12T15:26:31Z`,已过 **~45h**
⇒ **闸 (i) 不是本轮不发波的原因**,闸 (iii) 是。

## 二、工作单元:`hero-27` 归档扫描(**已交付**)

**产物**:`iterations/reports/batch-desk/hero27_savemana_archive_scan.md`
⇒ owed 行 `hero27_dem21_deadline_scan` 的 `done_when`(`path_exists`)**现已满足**。
**工具入库**:`tools/batch_test/behavioral/wk_savemana_domain.py`。
`queue.json:hero-27` **`running` → `harvested_pending_verification`**,带完整 `result` + 五条验收的逐条答复。

**结论一句话**:桌面那条 `DOMAIN-EMPTY` **只对 fixture 语料成立**。真实语料里保蓝锁
**开火 5,449 帧 / 317 个事件**,**边际域 3,690 帧 = 开火帧的 67.72%**,出现在 **61/78 局**。
而 (丁) 那个决定优先级的读数**落在接近 0 那一端**:**只有 11.04% 的锁事件在结束后 2s 内放出了 Q**
⇒ **锁是取消不是延迟,代价成立,⛔ 不下调优先级**。

⚠️ **(丙) 没有按字面满足**,而且理由是结构性的:**radiant 分层是空的(78/78 WK 在 dire)** ——
镜像 A/B **把阵容钉在侧别上**,swap 的是哪一侧 armed,而那个标签**在 run log 里不在 `.dem` 里**。
本报告给了**每粒种子**的分层(三粒方向一致)并**把 (i-a) 是否约束单总体普查交总监裁**;
⛔ 本台不自行宣布满足、也不自行宣布不适用。

### ⭐⭐ 方法侧,两条值得全组抄走

1. **幻象坑(L5)**:英雄与其幻象**共用 class name**,只按 `hero` 名选快照会把幻象一起选进来。
   本语料 **12/78 局**有(常见 3 个实体同 `t`,一次 5 个)。
   ⚠️ **它是怎么被发现的才是要点:总数本身看不出任何异常** ——
   是两个**各自独立推导**的事件计数对不上(**514 vs 317**)把它顶出来的;修掉后两者**同为 317**。
   ⇒ `tools/batch_test/behavioral/` 下**任何**按 `hero` 名选快照的脚本都有同一个缺陷,**值得普查**(交棒 ⑦)。
2. **挑出来的 fixture 语料可估条件比例,不可估边际频率**:
   fixture 把「开火/闸内」高估 **5.3 倍**、「构造性/开火」高估 **9.6 倍**,
   但「边际域/开火」**66.7% vs 67.72%,差不到 1pp**。
   机制是 fixture 帧被别的调查按"低蓝+R ready"挑过,**边际频率被污染而条件概率近似正交**。

### 一处路径更正(数字对,路径不对)

请求/裁词/owed 行都写「`dem21/`(103 份 `.dem`)」。**103 是对的**,
**但 `dem21/` 是整个前缀** —— 现读 **6,756 个 `.dem` / 125.28 GB**,横跨 **17 个波次日**。
lifecycle 现读为 `dem21-expire-21d`(`Days=21`)⇒ **每个对象按自己上传日 +21 天到期**,
**09-22 只是 09-01 那批的到期日**。⛔ 不要读成「09-22 之后归档扫描就没语料了」。

## 三、收割:零欠

`validation/` 现读 **592 对象**,与上一轮**逐位相同**,最新对象仍是 `..._20260907_2218_run.log`
⇒ **零新对象、零 verdict 可收**,`recover_verdict.py` **未跑**(没有对象可算,不是跳过)。
本轮**确有**从 S3 读取(§5),但读的是 **W34 的归档语料 + 4 份 W34 run log**,**不是**未收割的 verdict。

### 局数(章程步骤 7)
- **(a) 上一波次(W69)**:已于 `2026-09-12T12:11Z` 全量收割登记,⛔ **本轮零新局、零重算**。
- **(b) 本轮在跑波次**:**无波在跑** ⇒ 进度 **N/A**,不是 0 局。
- 距上一波起飞 **~50.8h**。

## 四、泄漏:四条独立路径全零(⛔ 不合并成一句)

| # | 路径 | 读数 |
|---|---|---|
| ① | `check_costs.sh` 自带 `running/pending instances` 区块 | **空**,`COST_EXIT=0` |
| ② | `describe-instances` 五态(`pending,running,shutting-down,stopping,stopped`)**不加 tag 过滤** | **零行**,`INST_EXIT=0` |
| ③ | `describe-volumes` 全账户 | **零行**,`VOL_EXIT=0` |
| ④ | 闸 (iii) 账户级普查 | `17 region(s) read <- COMPLETE` + `CERTIFIED (0 accruing instances account-wide, read this run)` |

⛔ `budget filters : none` ⇒ 账户里每一台实例都落进这个 budget,不论归谁。
⛔ 本轮零发波 ⇒ **不做"波次点名"第二步**。

## 五、本轮的 AWS 支出(⛔ 不写成零)

| 项 | 金额 | 依据 |
|---|---|---|
| EC2 | **$0.00** | 四条路径全零 |
| Cost Explorer | **$0.01** | 1 次 `DAILY` + `GROUP BY SERVICE`,按写死判据买 |
| **S3 出网(归档扫描)** | **~$0.4(估)** | 2.13 GB × 2 次(bug 重跑)× ~$0.09/GB |
| CloudWatch | $0.00 | `AccessDenied`,没跑成 |

⚠️ **第三行是估不是读**(牌价 × 字节数),账单侧要到落账后才看得到。**它比 headroom 大**,
这正是 GH #819 的立案句。⛔ 本台**没有**因此推迟这件活 —— owed 行逐字禁止拿刹车当工作量豁免,
而且**那条禁令是对的**:真正该改的是"零成本"这个**错误前提**,不是这件活该不该做。

## 六、铁律 9(owner 优先项)

- **常设运维项(spot 优先 / 降级阶梯)**:本轮零发波 ⇒ 无可执行面,无违反。
- **P4.1 upstream 标尺波**:**连续第十七轮欠**,⛔ 唯一阻因是闸 (iii)(刹车),不是本台未认领。
  棒已在 `DECISIONS_NEEDED.md §15` 与 GH #779 上;⭐ **本轮头号把它的时效从"天"压到"小时"**,已随通知送出。

## 七、交棒

| # | 给谁 | 内容 |
|---|---|---|
| ① | **owner**(已推通知) | 刹车将由**闲置**越过(~4.4h),09-30 预计 **~$102 > limit $100**;GH #779 三选一变成小时级 |
| ② | **总监** | `hero27_dem21_deadline_scan`:`path_exists` 已满足 ⇒ **可退休**;⛔ 不必再等 09-20 的 fallback 条款 |
| ③ | **总监** | (丙) 裁定:铁律 4 (i-a) 是否约束一个**单总体普查**(无 gate 的谓词,分层在物理上是退化的) |
| ④ | **总监** | GH #819:`ROUTED_ARCHIVE_SCAN` 的「零 AWS 增量」措辞 + 归档扫描要不要带 GB 估价过闸 |
| ⑤ | **英雄组 / 协同组** | 扫描 §5:锁是**取消不是延迟** ⇒ GH #407 可进入「要不要改 `bots/`」;⛔ 仍走 gated-fix + fixture |
| ⑥ | **英雄组 / 协同组** | 扫描 §10:`nLV>=6` + R rank 0 真实存在(42 帧)⇒ 需要**bot 侧 fixture** 读 `GetManaCost()`,**不需要语料** |
| ⑦ | **全组(工具)** | L5 幻象坑:`behavioral/` 下按 `hero` 名选快照的脚本**值得普查** |

## 八、自检与铁律 6

**自检**(铁律 10):`bash tools/agent/routine_selfcheck.sh > /tmp/sc.log` 裸读退出码
⇒ **`selfcheck worst exit: 3`**。
⚠️ **harness 的后台任务通知报的是 `exit code 0`,那不是自检的退出码** —— 与上一轮补记 D 同形,**第二轮复现**。
逐字:
```
FINDINGS (exit 3)     : cadence queue-rulings owed-executions lua-coverage
UNCERTIFIABLE (exit 2): trunk-red(python)
NOT RUN (inside a leg): tests/test_lua_gate.py tests/test_luacheck_gate_soakswitch.py tests/test_selfcheck_lua_leg.py
```
⭐ **上一轮的 trunk red(GH #816,`owed_executions.json` 缺 `trigger`)已被总监 `3c3b8de5` 修掉** ——
本轮第一手复跑 `python3 tests/test_pending_rulings.py` ⇒ **`824 checks, 0 failed`,`TPR_EXIT=0`**。

**铁律 6 三条腿**(GH #624,三行不是两行):
```
GATE_EXIT=0   CLEAN (iron rule 6 static half passed) -- luacheck bots game: 0 warnings
py gate:  PY_GATE_EXIT=0   (real cost 10.78s against a 12.0s budget)
lua gate: LUA_GATE_EXIT=0
```
⚠️ `lua gate` 报 **58 个新测试不在 manifest 里但照跑了**、**8 个超预算被排除**
⇒ 建议下轮有人跑一次 `python3 tools/agent/lua_gate_measure.py` 把它们登记(**不是本轮工作单元**)。
⛔ **`RULE6_BYPASS` 未用**,本报告无"跳过不是通过"行。
动态半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)**未跑也不被声称**;
但 `tests/test_wk_save_mana_lock_census.lua` 作为验收 (戊) **单独跑过,`CENSUS_EXIT=0`**。

## 九、铁律 11 与改动面

本轮**无**任何工具返回 `requires approval`;`mcp__github__issue_write` 正常(GH #819 已开)。⛔ 无空转等待。

**改动面**:
- 新增 `tools/batch_test/behavioral/wk_savemana_domain.py`
- 新增 `iterations/reports/batch-desk/hero27_savemana_archive_scan.md`(owed 行的针脚)
- 改 `iterations/queue.json`(仅 `hero-27` 一行:`status` + 新增 `result`)
- 改 `iterations/streams/batch-desk.md`(当前状态)+ 本报告
- ⛔ **`bots/` 一行未动**(本轮是普查不是改动)

## 十、Token 用量(铁律 8)

```
TOKENS total_in=18,743,887 out=92,134 turns=114
```
⚠️ 这是"到统计时刻为止"的数字,统计后的收尾回合不计入。
⭐ 本轮偏高的原因是**工作单元本身重**(102 份录像解析 + 两次 bug 后重跑 + 一份长交付物),
不是空转 —— 本轮是连续十七轮里**第一轮有实质交付物落地**的。
