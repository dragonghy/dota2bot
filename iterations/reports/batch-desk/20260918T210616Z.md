# 批测台 2026-09-18T21:06:16Z

**一句话**:刹车第五十一轮持有(MTD `$92.462`,快照与上一轮**逐位相同** ⇒ 冻结轮、⛔ 无新速率点);
零发波、零收割欠、零泄漏。**⭐⭐ 本轮实质:结清 RULING 80 (乙) 的欠条 —— `aws_run.sh` 长出 `--dry-run` 彩排出口**,
并在结清的同一轮**撞出一条它自己的结构洞**:那条欠条**永远打不出「请退休我」**,因为总监开行时把
`unmet_at_ruling` 的时刻写成了 `2026-09-18T16:xxZ` 这种**散文式模糊戳**,而同一个 registry 在
`_claim_note` 与 `_witness_note` 两处**都逐字禁止**它。⛔ 不自行改证词。

## (一) 开工自检
⭐ **第一条命令这次一次到位**(上一轮交棒 ⑤(a),同形第十一例之后):
`nohup bash -c '... > /tmp/sc.log 2>&1; echo "SELFCHECK_EXIT=$?" >> /tmp/sc.log' &`
—— ⛔ 无管道、⛔ 无 `timeout`、退出码**写进文件**而不是靠 harness 回显。

**⛔ 自检真码本轮「尚未可得」**(按订正后的取法说):正文回读 **645 行**,停在
`=== trunk health (python test suite) ===`,收尾三行横幅一个没打;判活死**只用 `pgrep -af`**
(⛔ 不用 `pgrep -c <名字>` —— 名字 >15 字符时**恒零**,上一轮就是这么差点把「还在跑」读成「跑完了」),
现读 **pid 492 / 2075 仍在** ⇒ **只许写「尚未」**。回读放在整轮最后(见补记)。

## (二) 预算 —— ⭐ 冻结轮,本轮**不**贡献速率点
闸 (iii) `FENCE_EXIT=3`(**连续第五十一轮是闸拒绝,不是本台的处置**),逐字:

```
actual (MTD)     : $92.462   <- re-read this run, never cached
pending waves    : $0.000   (launched, may not be in MTD yet)
planned          : $0.000
projected total  : $92.462
fence            : $100.00
brake            : $90.00
operative ceiling: $90.00   = min(fence, brake)
WAVE_FENCE: THROTTLED (exit 3) -- brake, not fence.
```

RULING 5/6/7 三行照抄:`17 region(s) read [...] COMPLETE` /
`records after 2026-09-18T01:51:04Z (= 2026-09-18T13:09:04Z - 11.3h lag, clock from budget snapshot)` /
`CERTIFIED (0 accruing instances account-wide, read this run)`;`budget filters : none`;
⭐ 钟没有降级,⛔ 未传任何 `--no-*`。headroom 对 `$90` = **`$-2.462`**。

⭐⭐ **MTD 与快照戳与上一轮(18:05:55Z)逐位相同**(`$92.462` / `2026-09-18T13:09:04Z`)
⇒ **这是同一个快照的第二次读数,不是一个新的区间端点**。按上一轮交棒 ⑤(c)
(端点 ≥11.3h、**只取不相交区块**),本轮**⛔ 不登记任何速率点**,速率带**逐字保留**
`$0.28–1.02/天`,兜底日**逐字保留** `2026-09-25T00:00Z`。
📌 这正是上一轮那条订正要防的事:**把同一个右端点再读一次不是一个新点**。

GH #801 四行整块照抄(**默认值跑**,⛔ 未用 `COST_CONFIRM_AT=999` 自救):

```
>= $35, but headroom to the $90.00 brake is $-2.462
< $1.10 (cheapest wave) — CE confirmation SKIPPED, NOT passed (GH #801).
Nothing can launch at this MTD, so confirming it buys nothing.
Resumes by itself as soon as headroom >= $1.10.
```

**SKIP 不是 pass**;欠条 `gh801_confirm_headroom_first_live_read` **仍 OWED**(⛔ 不自行结清)。

## (三) ⭐⭐ 实质:结清 RULING 80 (乙) —— `aws_run.sh` 的彩排出口

**买的是什么**:`spot_run.sh` 有 `--dry-run`,`aws_run.sh` 没有 ⇒ **P4.1 唯一要走的那条发波路径,
结构上不花钱就走不了一遍**。而 P4.1 的 ref-form 缺陷**死在渲染出来的 user-data 里**
(`tests/test_aws_run_ref_form.py` 的立案句),**本地从来没有人看得见那一段** —— 那正是它藏四十九轮的载体。

**落地**(`tools/batch_test/aws/aws_run.sh`):
- `--dry-run` 打印计划 + **完整渲染的 user-data** + 退出 0,**一次 AWS 调用都不发**;
- `--max-hours` 现在记录**是显式传的还是吃了默认值**,按 **(甲1)** 分别打 `EXPLICIT` / `DEFAULTED`;
- 按 **(甲2)** 打围栏价:**按该波自己的看门狗 × 按需价**,并**自我声明**它不是 `$1.10`、不是预测。

**两次实跑读数**(⛔ 零 AWS 调用,见下):

| 传参 | 看门狗 | 围栏价行 |
|---|---|---|
| 吃默认值 | `--max-hours 12 (DEFAULTED -- RULING 80 (甲1) forbids this on a real wave)` | `1 x $0.673/h x 12h = $8.08` |
| `--max-hours 2` | `--max-hours 2 (EXPLICIT -- copy this value into the launch record)` | `1 x $0.673/h x 2h = $1.35` |

⭐ **RULING 80 的立案句现在是一条本地命令打出来的,不再是散文**:默认值那一波在围栏里
**按 `$1.10` 记账,而它自己的看门狗上限是 `$8.08`** —— **少记 7.3 倍**,方向正是危险那一侧。

**⭐ 「零 AWS 调用」是**实测**不是断言,而且做成了变异台**:PATH 塞一个会记账并 `exit 99` 的 `awsx` 影子,
- 影子**先自证活着**:`command -v awsx` → `/tmp/shim/awsx`,直呼一次 → `rc=99` + 记下 1 行;
- 带守卫跑 `--dry-run` ⇒ **`AWS_CALLS=0`**;
- **把守卫变异掉**(`-eq 1` → `-eq 99`)再跑同一条命令 ⇒ **`ec2 run-instances` 被影子截下**(1 次调用,
  日志 86 行是 user-data 自带换行)。
⇒ **变异体没活下来** ⇒ 上面那个 `0` 是**读数不是静默的 no-op**。

**棘轮**:新增 `tests/test_aws_run_dryrun.py`,**15 条全绿**(`NEWRATCHET_EXIT=0`)。
⭐ 它把上面那个变异台**写进测试自己**:stub 不是记录一次发波而是**让发波致命**,
然后**用同一个 stub 跑一份守卫被关掉的副本**,要求那份**必须**够到发波 ——
⛔ 没有这后半条,「没有 AWS 调用」和「stub 根本没接上」**逐字节无法区分**。
既有棘轮 `tests/test_aws_run_ref_form.py` 复跑 **`REFRATCHET_EXIT=0`**(⛔ 未被本轮改动顶红)。

**⚠️ 写测试时撞红一次,是测试错不是代码错,照实记**:`12h 价 = 2h 价 × 6` 那条断言用了
`< 0.02` 的容差而实读 `8.08` vs `1.35×6 = 8.10`。两行**各自**四舍五入到分
(`0.673×2 = 1.346 → 1.35`,`0.673×12 = 8.076 → 8.08`),6 倍比较**天然继承** `6×0.005+0.005 = 0.035`
的舍入余量 ⇒ **改的是断言的容差(→ `≤0.04`)并把理由写进代码**,⛔ 没有去动脚本让数字凑上。

**诚实边界四条**:① 本行买的是**可彩排**,⛔ **不是**「P4.1 定价对了」——(甲1)/(甲2) 由**发波记录**自证,
不由这个出口自证;② 彩排**不证明**那一波launch 得起来、ref 在 AMI 的 clone 上解析得开、或它真实花多少钱 ——
只有 AWS 和账单答得了,⛔ 代码注释与测试 docstring 里都逐字写了;③ `$0.673/h` 是**章程 2026-08-24 的常数**,
本轮**未重新询价**,⛔ 不主张它仍然现行 —— 做法是**把日期打在输出里**并留 `OD_RATE_USD_H=` 覆盖口,
让**过期的价自己举手**(写死一个不说话的价,失效方向是围栏少记,正是本条自己反对的);
④ ⛔ **未动 `MAX_HOURS=12` 这个默认值** —— (甲3) 逐字说这是决定不是遗漏。

**⭐ 同轮入闸登记(GH #803:登记与产物同一个工作单元)**:新测试**本轮就进了 py 闸**,
⛔ 不留给下一个开工的组去撞红。`py_gate_measure.py` 默认值跑完(`PGM_EXIT=0`),前后读数:
`measured_at` `2026-09-18T19:14:33Z → 21:19:05Z`;`selected_count` **137 → 138**;
`measured_count` **154 → 155**;`selected_total_seconds` **53.236 → 51.598**(`budget_seconds` 90.0,余量 38.4s);
新行逐字 `{"seconds": 0.058, "in_gate": true, "reason": "fast"}`(per-test cap 3.0s,远在线下)。
**驱逐块没有打印**,逐字只有 `1 test(s) entered the hook: tests/test_aws_run_dryrun.py` ⇒ **零驱逐**。
⚠️ **顺带记一条与本轮无关但实测到的**:measure 打了 `a killed test left bots/Customize/soak_side.lua ARMED; removed it.`
—— 那是既有欠条 `soak_side_concurrent_writer_misattribution`(归总监)的形状,⛔ 本台未处理,只登记;
收尾复查 `bots/Customize/soak_side.lua` **现读不存在**(且它本就在 `.gitignore:76`)。

## (四) ⭐⭐ 撞出来的结构洞:那条欠条**永远打不出「请退休我」**
结清之后照规矩复读 `pending_rulings.py --owed-only`,该行读的**不是 DONE**:

```
UNCERTIFIABLE aws_run_dryrun_and_watchdog_pricing ...
  done_when: tools/batch_test/aws/aws_run.sh exists and mentions all 1 rideshare id(s)
  -- ... and this row's `unmet_at_ruling` is not a readable reading: it carries no
  parseable ISO-8601 UTC instant (a fuzzed `T19:xxZ` stamp is prose, not a time
  -- same rule as `claimed_at`)
```

**两半分开读**(⛔ 不并成一句):
- **键那半满足**:`contains '--dry-run' -> True`,且 `pending_rulings.py:1215-1242` 的证词闸
  **只在键已满足时才被走到**(它是 DONE / BORN-DONE / UNCERTIFIABLE 的分叉);
- **证词那半读不出来**:该行 `unmet_at_ruling` 逐字以 `立案时刻 2026-09-18T16:xxZ:` 开头,
  正则实读 **无可解析的 ISO-8601 瞬间**。

⇒ **这一行按它今天的写法,交付得再干净也只会读 UNCERTIFIABLE,永远不打印那句「请退休我」。**
⭐ **判别子在 registry 自己身上**:`_claim_note` 逐字要「**真的 ISO-8601 UTC 瞬间**,不是散文里那种
`T07:xxZ` 模糊写法」,`_witness_note` 逐字要「一个真的 ISO-8601 UTC 瞬间」——
**同一个文件在两处禁止的写法,被用在了第三处**,而第三处正是决定这根棒能不能放下的那一处。
📌 与 `pullcad`「promote 一个 id 会静默冻死引用它的 gate」**同族**:门在,被结构性绕开。

⛔ **不是本台可以自行修的**:改 `unmet_at_ruling` 等于**替总监伪造一份关于我没在场的那个时刻的证词**,
而 `_witness_note` (2) 逐字说这一行「**编造就能绕过**,它消灭的只是沉默的那个方向」——
执行方去补自己那根棒的证词,恰好是它唯一防不住的那个方向。⇒ **交总监**(见 (十) ②)。
⛔ 也**未**据此退休该行:`_note` 逐字「A row is retired by the director」。
⛔ **未回填 `claimed_by`/`claimed_at`**:`_claim_note` (2) 逐字「认领必须在干活之前推上去,
跟成品一起落地的认领是记录不是信号」⇒ 事后补一个认领是**装样子**,不写。

## (五) 收割:零欠
`validation/` **592 对象**、`soak/` **72,079 键**(与前八轮**逐位相同**);最新对象仍是
`validation/lf_rescue+27ids-d177b87026dd_20260912_1018_run.log`(`2026-09-12 10:18:26`)= **W69**。
在跑波次:**无**。

## (六) 局数
本月无新波次;本轮**未读 `analysis.json`、未读 `verdict.json`**(⛔ **0 局**)。
(三) 那份实质产出**不涉及任何对局数据**。

## (七) 成本(铁律 1 三段式)
**零 EC2 / 零 CE / S3 读取 `74` 个对象(出网未计价)** ——
`s3 ls --recursive` 2 次(`validation/` 592 键 = 1 页,`soak/` 72,079 键 = 73 页)= `1 + 73` = **74 LIST**,
**⛔ 不下载任何对象**。⚠️ **记的是 LIST 请求数,不是键数** —— 与前八轮同规格(那个 `<N>` 是给后来的人
乘单价用的,⛔ 不是「看过多少键」)。本轮新增 EC2 计费 **`$0.00`**;`check_costs.sh` **未买 CE**(#801 SKIP)。
⭐ **(三) 那份实质产出一个 S3 对象都没碰**(纯本地 + 变异台)。

## (八) 泄漏五条独立路径全零(⛔ 不合并成一句)
① `check_costs.sh` 自带区块**空**(`COSTS_EXIT=0`);
② `describe-instances` 五态**不加 tag 过滤** ⇒ `INST_EXIT=0` / **`LINES=0`**;
③ `describe-spot-instance-requests State==open||active` ⇒ `SIR_EXIT=0` / **`SIR_LINES=0`**;
④ 闸 (iii) 账户级 **17 区 COMPLETE** + `CERTIFIED (0 accruing instances account-wide)`;
⑤ AMI 仍 `ami-0a990a26d89c66547` 一张(常设成本非泄漏)。
⭐ **变异台不留残留**:影子 `awsx` 只在 `PATH=` 前缀里活了三条命令,变异副本写在 scratchpad 并已 `rm -rf`,
`git status --porcelain` 现读只有本轮的两个产物。

## (九) 通知判据(按上一轮收紧后的四条)逐条现读
MTD `$92.462` < `$100` ✗;`alpha = 0` ✗;headroom `$-2.462` < `$1.10` ✗;
今天 `2026-09-18` < 兜底日 `2026-09-25T00:00Z`(距今 **6.5 天**)✗ ⇒ **一条未中 ⇒ ⛔ 本轮不推手机通知**。
⭐ 破例自问答**否**:(三)(四) 是**本台与总监之间的 harness 事务**,⛔ 不改 owner 的选项集
(#779 三条路一条未增减)、⛔ 不需要 owner 做任何决定;owner 已第三十二轮零表态,
在未答的同题上再推一条不改选项的通知 = 噪声。**兜底条款已替它兜住**(6.5 天后 ④ 自己响)。

## (十) 交棒
① ⭐⭐⭐ **owner —— 刹车第五十一轮持有,GH #779 第三十二轮零表态**。球仍在 owner:
提高预算线 / 给预算加过滤器 / 接受本月剩余批测全停。⛔ 本台无权自裁。
② ⭐⭐ **总监 —— 请修 `owed_executions.json:aws_run_dryrun_and_watchdog_pricing` 的 `unmet_at_ruling`
并退休该行**:键那半已满足(实读 `contains '--dry-run' -> True`),卡住的是**总监自己写的证词**
用了 `T16:xxZ` 模糊戳。⛔ 本台不代改(理由见 (四))。⭐ **顺带请裁一条通则**:
开行时的 `unmet_at_ruling` 能不能**在写入那一刻就被校验**(`_witness_note` 已经定义了合法形状,
但**没有任何东西在开行时拒绝一个坏的**)—— 本行是这条洞的第一个实例,而它的代价是
**一根已经付清的棒留在 OWED 表里**,与 §DR「一条只活在散文里的欠裁平均寿命是一轮」方向相反但同源。
③ ⭐ **总监 —— RULING 80 (甲2) 的前提请复核一处**:章程逐字写「`aws_run.sh` 是**按需**路径」,
而源码 `aws_run.sh:16` 现读 `MARKET="--instance-market-options MarketType=spot"` ⇒ **默认是 spot**,
`--on-demand` 才把它清空。**结论方向不变**(按需价 = 保守侧 = 多记,(甲2) 照样对),
⛔ 因此**本台未改任何东西**,只登记前提与源码不一致 —— 免得下一个读章程的人据它推别的结论。
④ **总监 —— 上一轮交棒 ②③④ 逐字结转**(重叠窗口速率点是否升格通则 / GH #291 / 更早那两行),
⛔ 本台不自行退休任何一行。
⑤ **下一轮本台**:(a) 开工第一条命令**仍不许带管道**(本轮做对了,保持);
(b) 收尾回读自检,`pgrep -af` 活 ⇒ 只许写「尚未可得」,死 + 无横幅 ⇒ 才可写「本轮不可得」,
核 `FINDINGS` 时**连 `UNCERTIFIABLE` 一行一起核**;
(c) 速率点**只取不相交区块**(≥11.3h),**⛔ 快照戳没动的轮次不贡献点**(本轮即是,已照做);
(d) 通知判据四条,④ = `2026-09-25T00:00Z`;
(e) 刹车解除(headroom ≥ `$1.10`)时按规格发 **P4.1 标尺波** ——
⭐ **现在可以先 `--dry-run` 彩排一遍再花钱**,且按 (甲1) **必须显式传 `--max-hours` 并把实际值抄进发波记录**;
收割必带 `--ledger`,并抄 #801 四行**恢复后**的样子。

## (十一) 没做的事
⛔ 未发波(闸 `exit 3`);⛔ `bots`/`game` **一行未改**;⛔ 未重开已关档裁定;
⛔ 未改 `state.json`/`test_set.md`/`OWNER_PRIORITIES.md`/`queue.json`;
⛔ 未自行结清 #801 欠条;⛔ **未自行退休、未自行改证词、未回填认领**(见 (四));
⛔ **本轮未登记速率点**(冻结快照);⛔ 未改 `$90`、未预选 #779 三条路;
⛔ 未逐局回读 `analysis.json`;⛔ 未改 `MAX_HOURS=12` 默认值(RULING 80 (甲3));
⛔ 闸 (i)/(ii)/(iv) **未跑**(发波轮的闸)——**未跑不记成通过**。

**铁律 9**:`P4.1` upstream 标尺波**连续第五十轮欠**,⛔ 唯一阻因是闸 (iii) `exit 3`(预算)。
⭐ 但**它的一个非预算阻因本轮被移除了**:这条路径此前**不花钱就走不了一遍**,现在走得了。

## 补记(push 之后,GH #290 顺序)

### (A) ⛔ 订正正文 §八一处计数
正文 §八 写「`git status --porcelain` 现读只有本轮的**两个**产物」——**实读 5 项**
(`M iterations/streams/batch-desk.md`、`M tools/agent/py_gate_manifest.json`、
`M tools/batch_test/aws/aws_run.sh`、`?? iterations/reports/batch-desk/20260918T210616Z.md`、
`?? tests/test_aws_run_dryrun.py`)。**要说的那句(变异台不留残留)仍为真**,错的是计数,照实订正。

### (B) ⭐⭐⭐ 第一次 push 被拒,原因是**两条铁律规定的程序互相把对方顶红**
**⛔ 先说清楚哪个码是真的**:harness 后台通知说 `completed (exit code 0)` ——
**那不是 push 的码**,复合命令的码是最后那个 `tail` 的;真码在输出文件第一行:**`PUSH1_EXIT=1`**。
📌 与开工第一条命令的管道坑同族,本轮**第二次**踩到同一条铁律的另一面,**这次读对了**。

**三条腿读数(第一次 push)**:`luacheck bots game: 0 warnings` / `GATE_EXIT=0  CLEAN` /
`py gate: 138 ran, 0 findings, 0 uncertifiable, 51.6s` /
**`lua gate: 433 ran, 1 findings, 0 uncertifiable, 9 unanswered, 5 known-red, 679.7s`**
⇒ `PUSH REFUSED -- iron rule 6 fast LUA TEST leg is RED`。

**那 1 条 finding 的正文逐字**:
```
FAIL[1]: test_lion_hex_panic_level.lua :: §7.3 the REAL J.IsSoakCandidate answers to this exact literal
      bots/Customize/soak_side.lua is already there and this process did not write it, so arming
      would clobber someone else's switch (and deleting it afterwards would make THEIR failure
      unattributable). Contents: return { side = 'radiant', cand = 'tpstash' }
```
⇒ **它不是一条断言失败,是一条 setup 拒绝**:测试发现开关**被别人armed 着**,于是拒绝动它。
⛔ **与本轮 diff 无关**:本轮 `bots`/`game` **一行未改**,改的是 `aws_run.sh`(bash)、
一个 python 测试、manifest、报告与章程 —— **没有一行 Lua**。

**⭐ 谁是那个「别人」:两条腿互相顶红,证据是候选名对不上**
- 开工自检的 Lua 腿(21:22–21:25)读到 `cand = 'x'`(8 次)与 `cand = 'all'`(2 次);
- push 钩子的 Lua 腿(21:22:30–21:33)读到 `cand = 'tpstash'`(2 次)。
⇒ **三个不同的候选名** ⇒ 各自看见的是**对方**写的开关,不是自己的残留。
⇒ **自检报 `TRUNK RED -- 5 of 139 Lua detector file(s) failing ON THE WORKING TREE`,
push 钩子报 `PUSH REFUSED ... LUA TEST leg is RED`,而两边都不是 trunk 的事实** ——
**5 个 FAIL 的正文全是同一句 collision**(含第 5 个 `test_siegecap_ancient_roster.lua`:
它的**标题**看着像真断言,**正文**仍是那句 collision)。

**⭐⭐ 立案价值在于两条腿都是铁律规定动作**:铁律 10 要求**开工第一件事**跑自检(约 20 分钟),
铁律 6 要求**push 时**跑钩子(Lua 腿实测 679.7s)—— 一个工作单元通常**短于自检**,
于是**「自检还在跑时就 push」是常态不是例外**,而 `bots/Customize/soak_side.lua`
是**一个全局可变文件**(40 个测试文件写它),**没有任何锁**。
⇒ **这不是偶发竞态,是两条强制程序的结构性冲突。**
📌 而两边给出的补救话术,恰好是 `py_gate_measure.py` docstring **早就预言的那两种反应**:
`RULE6_BYPASS`(钩子自己印在拒绝行下面)与「someone else broke main」(自检的 TRUNK RED 行)。

**⛔ 本台没有按那两条走**:**未用 `RULE6_BYPASS`**;**未把它报成 trunk 红**。
**做法是等自检跑完(`SELFCHECK_EXIT=3`、`soak_side.lua` 实读不存在)再重推一次**,
⇒ 代价是**这一轮多花约 23 分钟闸时间**(679.7s 白跑 + 再跑一次),**但没有一个读数是买来的**。

**⭐⭐ 而这一等,免费买到了一个对照实验 —— 它比任何论证都硬**:

| | 树 | 容器 | Lua 腿读数 |
|---|---|---|---|
| 第一次 push | `d63f4373` | 自检**同时在跑** | `433 ran, **1 findings**, 679.7s` ⇒ `PUSH1_EXIT=1` |
| 第二次 push | **同一棵 `d63f4373`** | **安静** | `433 ran, **0 findings**, 677.2s` ⇒ `PUSH2_EXIT=0` |

**同一棵树、同样 433 个测试、唯一变量是并发** ⇒ **红是伪影,不是 trunk、不是本轮 diff**,
这不是推断,是**受控对照**。⛔ 反过来也要照实说:它**不证明** trunk 的 Lua 侧是干净的
(那要 `git stash` 后重跑,本轮**没跑**),它只证明**这一次红的归因**。

⇒ **归总监既有欠条 `soak_side_concurrent_writer_misattribution`**,本轮是它的**一次完整现场**
(两条腿、三个候选名、一次真实的 push 拒绝)。⛔ 本台**未改 harness**(编排权在总监,先例 GH #33)。
**便宜的方向**(仅供总监参考,⛔ 未实施):给这个路径一把锁 / 让每条腿用**各自的**开关路径
(`soak_side.<pid>.lua`)/ 或让自检与钩子**不并发**。

### (C) 顺带登记,⛔ 未处理:Lua manifest 已陈旧
同一条 Lua 腿逐字:`89 new test(s) not in the manifest were run anyway, costing 372.33s`;
`=> the hook's REAL cost this run is 679.67s against a 540.0s budget`;
`9 new test(s) exceeded the hook budget and were EXCLUDED`(并附 `re-measure to record them:
python3 tools/agent/lua_gate_measure.py`)。
⇒ 这是既有欠条 `gh806_lua_manifest_remeasure` / `lua_gate_stale_manifest_refusal_port` 的形状。
⛔ **本台未重测**:本轮**没有动 Lua 测试语料**(该欠条 executor 逐字是「下一个动 Lua 测试语料的组」),
且重测本身要跑满全集;**只登记不认领**。
⚠️ 另:自检 `owed-executions` 腿现读 `done_when: tools/agent/lua_gate.py exists but 2 of 2 required
mention(s) are absent: LUA GATE COULD NOT RUN -- STALE MANIFEST, unmeasured_slack_seconds`
⇒ 该欠条**自己的键也没满足**,归总监。

### (D) 开工自检真码(铁律 10;正文 (一) 的「尚未可得」由此取代)
`SELFCHECK_EXIT=3`,收尾三行横幅**全打了**:`legs run : 15` /
`FINDINGS (exit 3) : cadence queue-rulings owed-executions lua-coverage trunk-red(lua)` /
`UNCERTIFIABLE (exit 2): trunk-red(python)` /
`NOT RUN (inside a leg): tests/test_lua_gate.py tests/test_luacheck_gate_soakswitch.py
tests/test_selfcheck_lua_leg.py` / `selfcheck worst exit: 3`。
⭐ **按上一轮交棒 ⑤(b) 两行一起核,本轮真的有桶间迁移**(这正是一起核才看得见的):
上一轮 `FINDINGS … trunk-red(python)` + `UNCERTIFIABLE: none`;
本轮 **`trunk-red(python)` 从 FINDINGS 迁到 UNCERTIFIABLE**,而 **`trunk-red(lua)` 新进 FINDINGS**。
⛔ **后者按 (B) 是并发伪影,不是 trunk 红**;前者(python)本轮**读作未核验**,
⛔ **未跑不记成通过**,也⛔ 不据此宣称 GH #894 已修或未修。

### (E) 发表(GH #290 顺序:两次 push 都在发表之前)
`claim_precheck.sh` 第一跑即 **`PRECHECK_EXIT=0`**,逐字 `local commits not on origin/main: 0` /
`paths cited 4 ... resolved on trunk 3 refused 0` / `clean` /
`OK to publish: every citation resolves on origin/main.`
⚠️ **中间那行的算术照实读**:`cited 4` 而 `resolved 3`、`refused 0`,三个「by design/construction/DECLINED」
桶**又都是 0** ⇒ **少掉的那一个本台没有查出它去了哪个桶**,⛔ 只登记差异不解释
(工具自己的结论行是 `OK to publish`,本台按它发了)。
**GH #832** 第一条评论已发(`issues/832#issuecomment-5736665755`),带署名脚注
⇒ 该 issue **立案三天来的第一条评论**;⛔ 未关闭它(修法归总监/harness 域)。
⛔ **未开新 issue** —— 本轮那条证词缺陷与 #832 **是同一个缺陷类**,开新号是重复立案。

### (F) 铁律 6 三条腿(三次 push 的读数,⛔ 不合并)
- **第一次(branch,被拒)**:`luacheck bots game: 0 warnings` / `GATE_EXIT=0  CLEAN` /
  `py gate: 138 ran, 0 findings, 0 uncertifiable, 51.6s` /
  **`lua gate: 433 ran, 1 findings, 0 uncertifiable, 9 unanswered, 5 known-red, 679.7s`** ⇒ **`PUSH1_EXIT=1`**;
- **第二次(branch,通过)**:同前两腿 + **`lua gate: 433 ran, 0 findings, ..., 677.2s`** ⇒ **`PUSH2_EXIT=0`**;
- **第三次(main)**:memo 命中,逐字
  `RULE6_MEMO=REUSE  a green three-leg reading taken 2026-09-18T21:53:48Z on THIS EXACT tree.`
  + `This is a REUSE, not a skip: all three legs really ran`
  (`HEAD^{tree}=d63f4373...`,`origin/main=bfc7f916...`)⇒ **`PUSH3_EXIT=0`**,`bfc7f916..023d3a5b  HEAD -> main`。
⭐ **按文档顺序先分支后 main**(RULING 69),⛔ 未反序;`ARM_EXIT=0`;
**⛔ 全程未用任何 `RULE6_BYPASS`** —— 这一点是本轮的实质之一(见 (B));
`PULL_EXIT=0`(`up to date` ⇒ 无 rebase、无 `--force-with-lease`)。
⭐ **落地独立复核不靠 push 回显**:`git merge-base --is-ancestor HEAD origin/main` ⇒ **真**,
且 `git ls-remote origin main` 与本地 HEAD **同为 `023d3a5be5ea`**。
⚠️ **第三腿本轮是真绿不是 `SKIPPED BY SCOPE`**(本轮改动含 `tests/` 路径)——
与前两轮那句「⛔ 不记成 Lua 测试通过」**不同**,本轮**记成通过**。

### (G) token 用量(铁律 8)
`TOKENS total_in=22,169,456 out=90,387 turns=131`(统计时刻的数,其后收尾回合不计入)。
