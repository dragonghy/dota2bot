# 批测台 2026-09-09T00:12Z — 纯收割轮(W58),零发波,本轮新增支出 $0.01

**一句话**:W58 收割完毕(2/4 粒配对,`RV_EXIT=0`);⭐⭐⭐ **头号产出是「家族深度」**——
**W58 的成员串在它起飞后 1–3 小时就被改掉了**,那个家族**永远停在 1 波 / 2 粒**,
而这已经是**连续第三波三个不同的成员串**(44 / 42 / 41);⭐⭐ 上一轮那笔
**「本月 55% 不是 dota2bot 花的」在 09-07、09-08 两天都没有再出现**。

---

## 一、开工自检(铁律 10)

第一条命令用**重定向不走管道**(连续第二轮避开 GH #514 那个坑),
且**没有** `timeout`(上一轮的自陈教训:那次是「我自己砍的」)。

**⚠️ 截至写报告时它仍在 `=== trunk health (python test suite) ===` 这一行上跑,
已 ~20 分钟**,与上一轮同一位置卡住的形状一致。**读得到的腿照登,读不到的不声称:**

- 稳定版锚点 **6/6 OK**(stable-v1…v6,`EXISTS`/`PINNED`/`SHIPPED` 全 ok)
- promote 期约束:**179 个 distinct live gate id**、18 个带 `PROMOTED` 注、
  **`FROZEN none`**(没有 live gate 点名一个已 promote 的 id ⇒ `pullcad` 陷阱本轮不成立)
- promote 原子 **5/5 全 GATED**
- 到期 admission wait:**6 份章程 / 59 个 settled id,零到期**
- **未读到**:python trunk health、Lua detector 腿、cadence / `unlanded_commits` /
  `citation_audit` —— **这几条本轮没人看过,不声称通过**。
- ⭐ **本轮验证了上一轮那条「检查器把自己算了进去」的教训**:用 `pgrep -af`
  (带 `-a`)看清了匹配到谁 —— pid 1969 是**本会话自己那条含该字符串的命令行**,
  pid 1971 / 2998 才是真正的脚本进程。若照上一轮那样只用 `pgrep -f` 计数,
  这一轮会把「1 个自己 + 2 个真进程」读成同一堆东西。

## 二、成本与泄漏(章程步骤 2)

`COSTS_EXIT=0`。**MTD `$69.4654`**,`budgets/dota2bot-batch`(免费)与 Cost Explorer
**逐位一致 `69.4654098749`**(脚本按 `>= $35` 自动花 $0.01 复核)。

- **在跑实例:0 台**(`running/pending` 段为空)⇒ W58 四台已全部消失,**无泄漏**。
- 常设成本仍只有 AMI `ami-0a990a26d89c66547`。
- 围栏:`$80` 未跨、`$90` 刹车未触发、`$100` 批准线未跨。**到 `$90` 余量 `$20.53`。**
- ⚠️ **`budgets` 自己的 forecast 是 `$181.976`,月度 limit 是 `$100`** —— 登记原样读数,
  本台不据 forecast 做任何决策(它是 AWS 的外推,不是账单)。

### ⭐⭐ 上一轮那笔外来支出:**09-07 和 09-08 两天都没有再出现**

本轮多花 `$0.01` 拉了 09-07→09-09 的逐日 + 逐 `USAGE_TYPE`。**两天的行里
一个 `c7a` / `r7a` 都没有**,只有本项目自己的 `c6i.4xlarge`:

| 日 | 行 | 金额 |
|---|---|---|
| 09-07 | `USW2-SpotUsage:c6i.4xlarge` | `1.837314` |
| | `USW2-BoxUsage:c6i.4xlarge`(按需补跑) | `1.621422` |
| | `USW2-EBS:*` + `PublicIPv4` + `Requests` + `TimedStorage` + `APIRequest` | `0.654` |
| | **当日合计** | **`4.113271`** |
| 09-08 | `USW2-SpotUsage:c6i.4xlarge` | `1.341348` |
| | `USW2-DataTransfer-Out-Bytes` | `0.382530` |
| | 其余 | `0.234` |
| | **当日合计** | **`1.957621`**(**仍在动,见五**) |
| 09-09 | — | `0.000000`(本报告时刻 00:1xZ) |

⇒ 上一轮点名的 **`$37.4355` 外来支出(09-05 `$26.40` + 09-06 `$18.41` 里的
`c7a.16xlarge` / `r7a.16xlarge`)自 09-06 之后没有再增加**。它**没有消失**——
那笔钱**仍然顶在本台的无 filter 围栏上**——但**它不再是一个正在扩大的洞**。
扣掉它,本台自己的 MTD 约 **`$32.03`**。

**给 owner 的算术更新(上一轮的三选一仍然成立,只是不再紧急)**:
到 `$90` 刹车余量 `$20.53`,本台自己日烧约 `$2–4.3`(09-07 `$4.11`,09-08 至今 `$1.96`)
⇒ **约 5–10 天到刹车,而本月还剩 21 天**。⭐ 关键在于:**这 `$20.53` 里有 `$37.44`
是被别人先花掉的**——若加 tag filter 或按项目归因,本台自己离 `$90` 还有 `$58`。
**本台不擅改任何一条线,继续按 `$90` 执行。**

## 三、W58 收割(`recover_verdict.py` 全量重算,不用实例自产 verdict)

`RV_EXIT=0`。语料走 `soak/` **四个前缀各自子目录、不拍平**(GH #225)。

**臂串本轮现算,而且这件事本轮是承重的**:
`git show e4fc013d…:iterations/streams/test_set.md | sed -n 2p`
⇒ **42 id / 380 字节 / md5 `f5b9da19646c9d7c2b96a19e768a8b7b`**,与 W58 发波记录**逐位一致**。
⭐ **注意钉的是发波树 `e4fc013d…`,不是今天的 `origin/main`**:今天 `origin/main`
的第 2 行是 **41 id / 371 字节 / md5 `fd21d5dd…`**(`cmrguard` 在 W58 起飞后退集)。
**照章程散文「读 origin/main」去收割,本轮会拿一个机器从没跑过的臂串重算**——
GH #631 那句未修的散文("第 3 行")之外,这是**同一条路径上的第二个坑**,
而它只在成员串刚好在两轮之间变动时才发作。

语料落盘:`files_seen 116 / games_loaded 116 / source_dirs 3 / unparseable 0 /
pooled_basename_dirs_overridden 0`,`scored_games 85`,`unfinished 0`,
`min_arm_depth 8`、`thin_arm_seeds []`、`thin_arm_winrate_seeds []`。

### 逐粒(铁律 4 (i-a):两个分层的**读数**都登记;`_ab`/`_ba` 全部取自工具字段,**未手算**,理由是 (i-d))

| seed | AZ | ab/ba | arm_depth | gpm (ab / ba) | xpm (ab / ba) | deaths (ab / ba) | last_hits (ab / ba) |
|---|---|---|---|---|---|---|---|
| 9145 | 2a | 13/0 | 0.00 | — | — | — | — |
| 9360 | 2c | 28/14 | **18.67** | **+118.64** (+197.91 / +39.36) | +40.14 (+150.29 / −70.00) | −0.32 (−2.82 / +2.17) | +3.06 (+36.30 / −30.17) |
| 9373 | 2c | 31/12 | **17.30** | **−37.49** (+301.37 / −376.35) | −14.43 (+224.14 / −253.00) | +0.04 (−3.37 / +3.45) | +0.02 (+28.36 / −28.32) |
| 9418 | 2d | 0/0 | — | — | — | — | — |

9145 `excluded: NO-PAIR`(13 局 radiant 腿、dire 腿零局);9418 **S3 上只有 1 个对象、
0 个 `.analysis.json`** —— 它 11.5 分钟就被回收,一局都没落盘。
两粒计分种子的 `arm_depth` **18.67 / 17.30,都远在 GH #269 的 8 门之上**。

### 跨种子(先每粒 swap-average、再取算术平均;⛔ 未按局数加权,(i-d))

| | mean | comps_better | ab 层 | ba 层 | side | sign_flip |
|---|---|---|---|---|---|---|
| gpm | **+40.58** | 1/2 | +249.64 | −168.50 | +209.07 | true |
| xpm | **+12.86** | 1/2 | +187.21 | −161.50 | +174.36 | true |
| deaths | **−0.14** | 1/2 | −3.09 | +2.81 | −2.95 | true |
| last_hits | **+1.54** | 2/2 | +32.33 | −29.25 | +30.79 | true |

⭐ `side` 仍比 `arm` 大**一个数量级**(gpm:`side 209.07` vs `arm 40.58`,`side_gt_arm 1/2`)。
按铁律 4 **(i-c)** 这是恒等式 `|side| > |arm|` 的体现,**不是否决理由、也不含精度信息**,
**但必须登记**(那是 (i-a))。
⚠️ **只有 2 粒,而且这两粒逐粒 gpm 是 `+118.64` 与 `−37.49`,极差 156**
—— 对照 GH #30 的经验零点 `σ=30.24`,**这两粒的离散度本身就 5 倍于零点**。
`comps_better` 的分母是 2,`1/2` 与 `2/2` 在两粒上**几乎不携带跨种子离散度信息**,
不要当成「一致」读。**本台不做判断分析**;`suggested: hold_or_reject` 是**工具的字段**。

### ⛔ 胜负通道(GH #352):**仍 DEGENERATE,但四波以来第一次离开 0**

工具在 stderr 自己拒的,**连续第九波**:
`minority side won 18 of 116 finished games (share 0.1552 < 0.20)`,
`winrate_side_census {radiant: 18, dire: 98}`,`winner_by {engine_natural: 116}`。

⭐ **少数侧占比序列现在是 `0.0645 → 0.0245 → 0.0000 → 0.1552`** ——
上一轮登记的是**单调走向 0**,本轮它**回弹到 0.1552**,是 `0.20` 门槛的 78%。
这是**四波以来第一个非零读数**,也是**第一次不支持「单调恶化」那个读法**。
`winrate 0.389 ± headroom 0.3363` **仍不可引作铁律 2 条件 (b) 的支撑**。

## 四、⭐⭐⭐ 头号产出:W58 的家族在它起飞后 1–3 小时就被封死在深度 1

| 波 | 起飞 | 成员串 | md5 | 配对种子 |
|---|---|---|---|---|
| W56 | 09-08 09:21Z | 44 id | `fe7a309f…` | 4 |
| W57 | 09-08 15:21Z | 44 id | `fe7a309f…` | 2 |
| **§GC 裁定 09-08 19:xxZ** | | **44 → 42**(`wandlimbo`、`tpdead` 退集) | | |
| W58 | **09-08 21:22Z** | **42 id** | `f5b9da19…` | **2** |
| **§GD 裁定 09-08 2x:xxZ** | | **42 → 41**(`cmrguard` 退集) | | |
| 下一波 | ≥09-09 03:22Z | **41 id** | `fd21d5dd…` | ? |

**W58 起飞到它的成员串被改掉,间隔 1–3 小时;发波闸的窗口是 6 小时。**
⇒ **`(42-id × e4fc013d)` 这个家族此生只有一波、只有 2 粒配对种子,而且再也长不大**
(除非成员串一字不差地回到那 42 个 id)。W58 发波记录里那句
`family_depth_at_launch: 0 paired seeds` 现在有了终值:**2,且是终值。**

**这不是本波的偶然,是当前节奏的结构性后果**:退集裁定的落地频率
(09-08 一天两次,间隔 ~5 小时)**快于发波闸的 6 小时窗口**。
在这个节奏下,**每一波都开一个新家族、每一个家族都停在 1 波深**,
而单波在两台被回收后只交付 **2 粒**配对种子。
⇒ **任何需要跨波累积深度的判定门槛,在当前节奏下结构上买不到。**

⚠️ **归属边界(本台只报形状,不裁)**:退集本身是总监的裁定权,
两次退集各自都有 §GC / §GD 的完整理由,**本台不主张它们错**。
本台主张的只有一件事:**「退集在两波之间落地」与「家族靠跨波并池长深度」这两件事
当前是互斥的**,而没有任何一方的流程写着要检查另一方。已开 issue 交总监。

## 五、GH #454:(乙2) 结清不触发;(乙1) 第四轮欠着,但这次给出了**量化的**欠账理由

**SIR 终态本轮第一手读到**(`describe-spot-instance-requests`,四行全 `closed`)。
⚠️ **读的是 `Status.UpdateTime`,不是 `sir_fulfilled_at`** —— 上一轮交棒 ③ 逐字警告过
抄后者会给每台算出 ~62 秒存活并凭空造出 `BRACKET VIOLATED`;本轮照办。
`describe-instances` 本轮**已查不到这四台**(实例记录已被回收),所以 SIR 是唯一一手来源。

| seed | AZ | `launched_at` | SIR `Status.UpdateTime` | 存活 | `Status.Code` |
|---|---|---|---|---|---|
| 9145 | 2a | 21:22:36Z | 21:47:29Z | **24.9 min** | **`instance-terminated-no-capacity`** |
| 9360 | 2c | 21:22:49Z | 22:07:20Z | 44.5 min | `instance-terminated-by-user` |
| 9373 | 2c | 21:22:51Z | 22:13:20Z | 50.5 min | `instance-terminated-by-user` |
| 9418 | 2d | 21:22:54Z | 21:34:21Z | **11.5 min** | **`instance-terminated-no-capacity`** |

- **(乙2) 不触发**:跑完自终两台的机时均值 = `(0.7419 + 0.8414) / 2` = **`0.7917 h/台`**,
  低于 `1.00 h/台` 阈值。被回收的两台**按条款自身文字排除**——它们正是这条判据要绕开的那部分。
- **价格侧单波估算 `$0.6193`**(EC2 spot `$0.6084` + IPv4 `2.1889 h × $0.005` = `$0.0109`)。
  各 AZ 价在整个波次窗口内**一个点没变**(`describe-spot-price-history`:2a `0.2773` /
  2c `0.2805` / 2d `0.2585`)。**这不是 (乙1) 要的账单侧读数。**
- **(乙1) 仍欠,第四轮。但本轮的理由是量化的、不是软话**:
  09-08 的 `USW2-SpotUsage:c6i.4xlarge` 在上一轮 21:xxZ 读作 **`$0.5245869928`**,
  现在(00:1xZ)读作 **`$1.341348`** —— **同一条 usage-type 行三小时内动了 `+$0.8168`**,
  所以它还不能在 W55/W56/W57/W58 四波之间拆分。
  ⭐ **量化那一半**:09-08 四波的**已知机时合计 `10.1499 h`**
  (W55 `2.7500` + W56 `3.1094` + W57 `2.1017` + W58 `2.1889`),而当日**独立的**
  `PublicIPv4:InUseAddress` 行 `$0.029350 ÷ $0.005/h` 只反解出 **`5.87` 地址小时
  = 已知机时的 57.8%**。⇒ **当日账单大约只落了一半。**

### ⚠️ 本轮想立、但**当场被自己的第一次应用证伪**的一条方法

本台原打算把「**用 IPv4 行反解机时、和波次记录里的已知机时对表,以此判定某一天的账单是否已落全**」
立成方法(章程本来就推荐 IPv4 行做独立反解)。**09-07 上它看起来完美**:
当日已知机时 = spot `8.8000`(W52 `3.5028` + W53 `2.7269` + W54 `2.5703`)
+ 按需 `2.409`(`$1.621422 ÷ $0.673/h`)= **`11.21 h`**,
而 IPv4 反解 `$0.056656 ÷ $0.005` = **`11.33 h`** —— **差 1.1%**。

**但同一天的第三个读数把它推翻了**:09-07 的 spot 行 `$1.837314 ÷ 8.8000 h`
= **`$0.2088/h`**,而当日各 AZ 报价带是 **`$0.2554–$0.2950`** ——
**实付费率比当日最低报价还低 18%**,这在 spot 计价上说不通。
三种机制都能解释,而**这个对表分不开它们**:
(甲) SIR `Status.UpdateTime` 在实例真死之后才异步更新 ⇒ 机时被系统性**高估**;
(乙) 09-07 也还没落全;
(丙) IPv4 按小时进位计费 ⇒ 对表是**虚假地**吻合的。
⇒ **本台不采纳这个方法,也不声称 09-07 已落全**;把三个候选机制和数字一起交总监。
**登记这条的理由**:它和章程里那些「门在,但它测的不是它以为的那个东西」是同族,
而这一次**在它变成一条章程之前**就被自己的第一次应用逮住了。

**顺带,价格侧估算的失效方向**:若 (甲) 成立,则本台一直在用的
「机时 × AZ 报价」价格侧估算**系统性偏高**(09-07 上高 33%:估 `$2.44` vs 账单 `$1.837`)。
**高估朝安全侧失效**(少发波),但按章程 GH #454 (丙) 的先例,**朝安全侧失效的常数
同样要登记**,因为它不会自己举手。

## 六、GH #591:写死的 changeover 常数本轮**不再是零信息**,而且只差 38 秒

`RECLAIM_BLIND_EXIT=0`(gate (iv),跑在 W58 自己的记录上 ⇒ **W58 的收割义务由此兑现**)。
逐字:`yield: 2 paired seed(s) of 4 machine(s)` / `attribution: 2 machine(s) reclaimed
before the flip` / `VERDICT: not blinded` / **`NEXT WAVE: spot`**(市场类型是闸打的,不是台判的)。

⭐ 头一行逐字:`changeover 40.0 min (measured bracket **34.80 < x <= 40.63**, margin above
the constant **0.63 min**)`。

上一轮交棒 ⑤ 报的是「**写死 bracket 第三波零信息**」(W57 四台 10.3/10.3/53.0/52.5 min
**全在括号外**)。**本轮不是**:W58 的 9145 在 **24.9 min** 死掉时 `ba=0`(还没翻面),
9360 在 **44.5 min** 时 `ba=14`(已翻面)—— 这两台**各自落在括号的一侧**,
于是测量括号收紧到 `34.80 < x <= 40.63`,而**写死的 `40.0` 距上界只剩 `0.63 min = 38 秒`**。
⇒ **一次在 ~39–40 min 翻面的波就会把这个常数证伪。** 交总监/harness。

## 七、发波决策(章程步骤 4)

**不发波。** `THROTTLE_EXIT=3`,逐字:
`anchor wave: W58 (4 machine(s))` / `last machine up 2026-09-08T21:22:54Z` /
`slate spread 18s` / `unlock 2026-09-09T03:22:54Z` / `now 2026-09-09T00:13:41Z` /
`margin 11353s SHORT of unlock` / `A launch at this instant BREACHES gate (i) by 189m13s.`
⇒ **本轮是纯收割轮,零机器,本轮新增 AWS 支出只有那 `$0.01` 的 CE 复核。**

步骤 4a 复核:`queue.json` 里**没有任何一行要求新波次、专机或新机时**
(pending 的都是 hero-*/strategy-* 的分析与核验请求)。

## 八、跨组交棒

① ⭐⭐⭐ **总监 —— 家族深度被退集节奏封死**(见四)。退集裁定间隔 ~5h < 发波闸 6h
⇒ 每波开一个新家族、每家族停在深度 1、每波只交付 2 粒。已开 `[batch]` issue。
② ⭐⭐ **owner(上一轮那项的更新)—— 外来支出停在 09-06,没有再增加**(见二)。
三选一(加 tag filter / 抬刹车线 / 降节奏)仍成立,但**不再紧急**;
`$90` 余量 `$20.53`,其中 `$37.44` 是别人花的。
③ ⭐⭐ **总监/harness(#591)—— 写死的 40.0 min 常数距实测上界只剩 38 秒**(见六)。
④ ⭐⭐ **总监 —— 一条差点被本台立成章程的方法,以及它的三个候选失效机制**(见五)。
连带:价格侧估算在 09-07 上高估 33%,朝安全侧失效,按 (丙) 先例应登记。
⑤ ⭐ **总监/录像组(#352)—— 少数侧占比四波以来第一次离开 0**(`0.0000 → 0.1552`),
仍 DEGENERATE,仍不可引作条件 (b) 支撑,但**「单调恶化」这个读法本轮不再被支持**。
⑥ ⭐ **本台下一轮 = 发波轮**,解锁 `2026-09-09T03:22:54Z`(**不要抄,跑 `wave_throttle.py`**)。
发波须知:成员串**现算**(今天是 41 id / 371 字节 / `fd21d5dd…`,但**发波那一刻要重算**);
`reclaim_blind.py` 已判 `NEXT WAVE: spot`;`--wave-json` 是**必需的具名参数**
(本轮位置参数报 `exit 2`,不是通过)。
⑦ ⭐ **本台 —— #454 (乙1) 第四轮欠着**,09-08 的行还在动;落全后一次补
W55/W56/W57/W58 四波。

## 九、铁律 6 / 11

- `bots/` / `game/` / `tests/` / `tools/` **本会话一行未改**(只写 `iterations/`)。
- 静态门与推送门:见收尾补记。
- 动态半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)**未跑,不声称**。
- 铁律 11:**全程无 `requires approval`、零空转**。自检在后台跑,本台没有坐等它。

---

## 收尾补记(本节在两次 push 之后写,数字全是本轮现读)

### 已发表的跨组产物(id 本轮现读,非事后追忆)

| 去处 | 内容 | id |
|---|---|---|
| **新开 issue #646** `[batch]` | 家族深度被退集节奏封死(§4) | `5393344513` |
| #591 追评 | 写死 `40.0 min` 距实测上界只剩 38 秒(§6) | `5593821557` |
| #352 追评 | 少数侧占比四波以来第一次离开 0(§3) | `5593823536` |
| #454 追评 | (乙2) 不触发 + (乙1) 第四轮欠账的量化理由 + 那条被证伪的方法(§5) | `5593828518` |

发表前按铁律 6(GH #290)跑了 `bash tools/agent/claim_precheck.sh`:
`PRECHECK_EXIT=0`,逐字 `local commits not on origin/main: 0` /
`paths cited 4 ... resolved on trunk 5  refused 0` / `OK to publish: every citation
resolves on origin/main.` ⇒ **先 push 再发表,顺序而非速度。**

### 铁律 6 / 11 的实际读数

- 静态门:`GATE_EXIT=0`,逐字 `luacheck bots game: 0 warnings` /
  `GATE_EXIT=0  CLEAN (iron rule 6 static half passed)`。
- 推送门:`ARM_GATE_EXIT=0`(`core.hooksPath=.githooks, already set`)。
- `PUSH1_EXIT=0`(`[new branch] claude/great-sagan-raqqj5`)、
  **`PUSH2_EXIT=0`**(`cec7da6a..16415ca1  HEAD -> main`,**未被拒、未 rebase**)。
- **未用 `RULE6_BYPASS` ⇒ 本轮没有「SKIPPED, not passed」那一行。**
- 动态半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)**未跑,不声称**。
- 铁律 11:**全程无 `requires approval`、零空转**。

### 收尾泄漏复检(账号级,非 tag filter)

`LEAK_EXIT=0`。`running/pending` 段**为空** ⇒ **0 台**,W58 四台全部消失,
**无孤儿、无第五台、无外来工作负载**。MTD 仍读 **`$69.4654`**
(`budgets` 与 CE 逐位一致 `69.4654098749`;本轮那两三次 `$0.01` 的 CE 调用尚未落账)。
常设成本仍只有 AMI `ami-0a990a26d89c66547`。

### 自检最终状态(诚实登记,不粉饰)

**⛔ 到收尾时刻,`routine_selfcheck.sh` 仍在 `=== trunk health (fast Lua detectors) ===`
这一行上跑,已 ~40 分钟,`selfcheck worst exit` 一次都没打出来。**
本台**没有坐等它**(铁律 11),也**没有像上一轮那样用 `timeout` 砍它** ——
两者是不同的失败,按 GH #358 必须分开写:**本轮是「它还没跑完」,不是「跑不成」,
更不是「我砍了它」**。

⇒ **本轮没有 `SELFCHECK_EXIT` 可报。** 读到的腿在 §1 已逐条登记
(锚点 6/6、`FROZEN none`、promote 原子 5/5、到期 wait 零);
**python trunk health 是 `UNCERTIFIABLE`**(工具自己逐字
`this is NOT a pass` 且 `it is NOT evidence that trunk is red`,9 项未跑);
**Lua detector 腿、cadence、`unlanded_commits`、`citation_audit` 本轮没人看过,不声称。**

⭐ 这正好是上一轮交棒 ⑧ 请求的那件事的第七个实例:
**「没跑完」「跑不完」「被砍断」三种形状在日志里长得一模一样** ——
本轮只能靠 `pgrep -af` 数真进程才区分得出第一种。**请把可复读的进度戳做进工具。**

### Token 用量(铁律 8)

```
TOKENS total_in=6,884,207 out=50,546 turns=57
```
(统计时刻之前的数字;此后的收尾回合不计入。)
