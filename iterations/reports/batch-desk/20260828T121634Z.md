# 批测台 2026-08-28T12:16:34Z

**一句话**:W20 上一轮已定案、本轮无新 verdict 可收;**发 W21**(树 `4b2ee334`、43-id 全集、
四粒新种子、四台 spot 四个 AZ),**而本轮真正的产出是两道门各拦了一次** ——
6h 闸在**动作时点**连拒四次(最后一次差 6 秒),载体门第二次改掉了本台的发波参数。

---

## 1. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` — **worst exit 3(非阻断)**。

| 腿 | 读数 |
|---|---|
| 稳定版锚点 | `stable-v1` / `stable-v2` 两枚 **EXISTS / PINNED / SHIPPED 全 ok** |
| unlanded commits | 无 |
| cadence | **1 处洞:`strategy` 08-28T01:24Z → 10:35Z,9.2h**(**不是本台的**) |
| 未裁队列(director 字段) | RIDESHARE **none**、OTHER **none**;总 open 37 |
| trunk python 套件 | **48 passed / 0 failed / 0 uncertifiable**(上一轮 44,**+4**) |
| trunk 快 Lua 检测器 | **17 个 tagged 文件、0 失败**(上一轮 12,**+5**);FAST SUBSET,不覆盖全套 |
| 退出码来源(GH #267 4b) | legs=7;**FINDINGS(exit 3)= cadence**;**UNCERTIFIABLE(exit 2)= none** |

⇒ exit 3 **全部**来自 cadence,且**是工具自己报的归因**,不是本台手工写的那句话
(#267 记的正是手工归因错了 22 小时那次)。

## 2. 成本(§2)

| 项 | 值 |
|---|---|
| MTD(`budgets describe-budgets`,免费) | **59.749** |
| CE 复核(≥$35 自动花 $0.01) | **59.7486783649** ⇒ **逐位一致(连续第十一轮)** |
| Budget 快照刷新时刻 | **2026-08-28T08:48:11Z** |
| forecast / limit | 86.694 / 100.0 |

**⭐ 快照终于动了。** 上一轮记的是 `08-26T23:46:06Z` **连续两轮逐位未动**;这次跳到
`08-28T08:48:11Z`,MTD 从 **$50.671 → $59.749(+$9.078)**。这一跳吃进的是 W15-R…W20 那一串
积压 —— 但**本台不做「增量对得上波费 ⇒ 该波已计入」的反解**(铁律「乙」明令禁用,
它已两次给出错误结论)。**照旧按滞后处理:W20(06:16Z,距今 6.0h,在 12h 窗内)仍计入围栏。**

**围栏算术(铁律「甲」:MTD + 近 12h 已发波次 × 各自单波价)**:

| 项 | 值 |
|---|---|
| MTD | 59.749 |
| W20(08-28 06:16Z,spot) | +0.80 |
| **W21(本轮发)** | spot 口径 **+0.80** / **全程降级按需口径 +2.15** |
| **围栏(spot 口径)** | **$61.35** |
| **围栏(保守:全程降级按需)** | **$62.70** |

⇒ **闸 (iii) 通过**,两种市场类型口径**都过**(§4b(iii) 明文要求两种都过);
围栏 $80 余量 **$17.30**,**刹车线 $90 未接近**。

**告警档一行(§4b(iii) 配套义务)**:$50 档**此前已跨**(书面解释在 08-27T15:15Z 报告 §2);
**本轮不跨任何新档** —— 最坏口径 $62.70 距下一档 $80 尚有 $17.30,
**owner 本轮不会因本台收到任何新的 Budget 告警邮件**。

## 3. 收割:本轮无新 verdict

`s3://dota2bot-batch-results-4924/soak/` 最新四个前缀就是 **W20 的四台**
(`…_061632_…_f48af2` / `…_061636_…_f49eda` / `…_061641_…_f4eda4` / `…_061645_…_60e52a`,
树 `62ad1803`),**上一轮 09:13Z 已定案**(204 落盘 / 154 计分 / 3 粒计分种子,
`min_arm_depth=8` 新门下 959 因回收出集)。此后**无新 run 落盘**,
`validation/` 前缀最新条目停在 2026-08-22。⇒ **本轮收割动作为空,不是没做,是没有对象。**

`queue.json` 因此**无 status/result 需要更新**。

## 4. §4a:队列无适用对象

22 条 pending **逐条查 `director` 字段**:

- `ROUTED_ARCHIVE_SCAN` / `ROUTED_ARCHIVE_SCAN_CONDITIONAL` / `ROUTED_ARCHIVE_REDUMP`(零 EC2、零新波次):
  `hero-1/2/3/4/7/10/11/12/13/14/16/17/18/19/21`
- `REJECTED`:`strategy-19`、`strategy-22`
- `RECEIVED_NOT_SCHEDULED`:`hero-9`、`strategy-6`
- `DEFERRED_PENDING_W3_RESOLVE`:`strategy-7`、`strategy-8`
- 唯一 `APPROVED_CONDITIONAL`:**`strategy-5b`,但 `director.wave=W6`(历史波次,非本轮排期)**

⇒ **无一条 `APPROVED*` 且需要本轮新波次**;无 `director` 字段的 pending **为空**。
本轮走 **§4b 例行波**。

## 5. 发波 W21(例行波)

### 5.1 ⭐ 6h 闸:上一轮的防复发条款首次实测,**当场拦住四次**

上一轮(W20)**自认早发 3 分 31 秒**,归因写得很清楚:`date -u` 跑在**决定**的时点、
没跑在**动作**的时点,交棒②因此要求「`date -u` 跑在发波命令的**同一个 shell 块**里并显式比较」。

**本轮照办,而它立刻兑现了**:

| 尝试 | 块内 `date -u` | 与解锁 `12:16:32Z` 之差 | 结果 |
|---|---|---|---|
| 1 | 12:15:37Z | **−55s** | `NOT UNLOCKED -- refusing to launch`,exit 1 |
| 2 | 12:15:59Z | **−33s** | 同上,拒发 |
| 3 | 12:16:16Z | **−16s** | 同上,拒发 |
| 4 | 12:16:26Z | **−6s** | 同上,拒发 |
| **5** | **12:16:34Z** | **+2s** | **PASS ⇒ 进入发波循环** |

**这正是上一轮那 3 分 31 秒的形状**:本轮第一次读表(§2 成本检查同一时段)是 **12:12:23Z**,
若按 W20 的老做法「读一次表、去干别的、回来直接发」,**四次里有四次会早发**。
**锚点** = W20 首台 `2026-08-28T06:16:32Z` ⇒ W21 解锁 `12:16:32Z`,实发 `12:16:34Z`,**+2 秒,合规**。
⇒ **交棒②可销号:它不是一句提醒,是一段会 exit 1 的代码,且已实地拦住四次。**

### 5.2 ⚠️ 载体门第二次改掉发波参数

`seed_draft.py --find axe --from 975 --count 4` 给出 **983 / 986 / 995 / 999**,
载体门 **exit 1**:

```
CARRIER term=obsidian_destroyer seeds=4 satisfied=0 verdict=ABSENT carriers=none
CARRIER_GATE terms=5 seeds=4 exit=1
```

(其余四项均 PARTIAL:`crystal_maiden`←999、`zuus`←995、`lion`←986、`skeleton_king`←983,999。)

**处置**:不降门、不改判据 —— 在 `975..1399` 的 66 粒含 axe 种子上做覆盖搜索,
换成 **983 / 986 / 995 / 1138**,复跑载体门 **exit 0**:

| term | satisfied | carriers |
|---|---|---|
| crystal_maiden | 1 | 1138 |
| zuus | 1 | 995 |
| lion | 2 | 986, 1138 |
| obsidian_destroyer | 1 | **1138** |
| skeleton_king | 2 | 983, 1138 |

`CARRIER_GATE terms=5 seeds=4 exit=0`。**1138 是这一波的关键载体**(独扛 od 与 cm)。

**⚠️ 一条诚实边界(登记给收割侧)**:五项里**四项 satisfied=1**,
即 **`od` / `cm` / `zuus` 各只有一粒种子带载体** —— 那一粒若被回收,该英雄这一波**读数直接归零**,
不是变薄。W20 的 959 被回收就是这个形状。**这不是门的缺陷**(门只保证"至少有"),
**是 4 粒 × 10 槽 = 40 个英雄位对 42 英雄池的结构性稀疏**,与 `seed_draft.py` 头注里
`axe` 15.8%/4 粒漏 50.4% 同源。

### 5.3 配置与两道门

- **pin 显式 40 位 sha `4b2ee33416df1f41617387c93838052296c2aab7`**(不写 `main`)
  ⇒ 本轮收尾提交**不会**改变实例克隆到的树。发波前
  `git ls-remote origin main` **==** 本地 `origin/main` **==** 该 sha ✅
  (2026-07-23 险些测错树的常规动作)。
- **接线门 exit 0**(先落盘再取 `$?`,没接 `| tail`):
  `all 43 armed ids wired on 4b2ee334…`。两个新 id 直接命中:
  `campvoid` → `bots/mode_farm_generic.lua:119`、`campexit` → `bots/mode_farm_generic.lua:892`。
  角色/等级门 id 的载体门仍 no-op(**GH #140,第二十六轮**)。
- **arm 串 = `test_set.md` 第 2 行当前全集:43 id / 381 字节**
  (W20 的 41 id + **`campvoid`**(§BT/§BW)+ **`campexit`**(§BV/§BW),总监已核单向性并裁入集)。
  ⚠️ **W21 的读数不可与 W20 的 41-id 读数直接比**(上一轮交棒④已登记)。
- `--slots 16 --rec-slots 12 --hours 2`,`--validate "<43id串> <seed> --games 12"`;
  **未传 `--on-demand`**(owner GH #158)、**未传 `--cand-ref`**(⇒ **收割时也不要传**);
  `--dry-run` 先行通过;**波次预算闸未拒发**。
- **一处自纠**:首次 dry-run 把 `--games 12` 误放在顶层 ⇒ `unknown arg --games`,
  **脚本拒绝执行、零支出**;`--games` 只属于 `--validate` 串内。dry-run 的价值在此。

### 5.4 市场与放置:阶梯第 1 级即成功

| 种子 | 请求 AZ | 实得 AZ | 机型 | 实例 | Lifecycle |
|---|---|---|---|---|---|
| 983 | us-west-2a | **us-west-2a** | c6i.4xlarge | `i-0ba0143dacef03d97` | **spot** |
| 986 | us-west-2b | **us-west-2b** | c6i.4xlarge | `i-0e3fb1084fbeea984` | **spot** |
| 995 | us-west-2c | **us-west-2c** | c6i.4xlarge | `i-0276fc1e024ffa124` | **spot** |
| 1138 | us-west-2d | **us-west-2d** | c6i.4xlarge | `i-0a7e8ca6c7350999f` | **spot** |

- **四台四个 AZ、请求即实得(4/4)、`InstanceLifecycle` 四台全 `spot`**
  (`run_id` 的 `spot_` 前缀**没有**当证据)⇒ **#252 / #256 验收再次通过**。
- stderr **无 `re-aiming inside the ring`、无 `AZ RING EXHAUSTED`**,
  无 `InsufficientInstanceCapacity` ⇒ **本轮无降级可报**(阶梯第 1 级成功)。
- **⭐ 与 W20 的一处刻意不同:本轮四台全 `c6i`,没有再混 `c6a`。**
  理由:降级阶梯第 2 级的**触发条件(launch 期容量失败)本轮没有满足**,
  而 W20 已经答完「`c6a` 能不能用」(974 跑满两腿、产出全波最好的一粒)。
  在 **#271 未裁**之前继续混机型,只会把机型与 AZ 继续混杂进读数,
  **买不到新信息却弄脏这一波** —— 这与上一轮自己写的诚实边界(甲)是同一条推理。

## 6. 局数预期

**W21 预期 ~208 落盘 / ~180 计分**(4 台 × 16 槽 × 12 局,两腿 + 暖场)。
**诚实边界照旧**:近六波里三波被 spot 回收清零,这只是「不被回收时」的期望值、**不是下界**;
按 §4 阶跃读法,**存活 <35–40 min 的台一粒也买不到**。
本轮**新增**一条 §5.2 的边界:`od`/`cm`/`zuus` 各只有一粒载体,**回收会让这三个英雄归零而非变薄**。

## 7. 泄漏两次

- **开工**:`running/pending` **空** ✅(W20 四台已离场)
- **收尾** `--leak-only`:**恰 4 台**,逐个对上 §5.4 发波表(id / 机型 / 起飞时刻全对得上),
  常驻只剩 **AMI + 快照** ⇒ **不是泄漏**。
- ⚠️ **作证边界第七轮保留**:`--leak-only` 只能证明**此刻**没有多余实例,
  **不能证明「过去这一轮没漏过」**。**#239 路 4 第九次实证。**

## 8. 铁律 6

`bots/` / `game/` **一行未改**(本轮只写报告 + 章程)。
`core.hooksPath=.githooks` **已上膛**(开工自检所为)⇒ 静态门在 `git push` 时自动跑;
**未使用 `RULE6_BYPASS`**。
动态半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)**未跑,也不声称跑过**。

## 9. 交棒

1. **⭐⭐ 下一轮本台 —— 收割 W21**
   (`…_121634_…_352458` 983/2a、`…_121638_…_460956` 986/2b、`…_121642_…_11c470` 995/2c、
   `…_121646_…_add5f8` 1138/2d;每 run 独立子目录,**不传 `--cand-ref`**,
   arm 串反解应得 **43 id / 381 字节**,树 `4b2ee334`)。
   **收割时必做**:逐台记存活时长并与 ~35–40 min 换腿点对照;
   照抄 `min_arm_depth` / `thin_arm_seeds`,被排除的种子**仍要点名**;
   ⚠️ **1138 若被回收,`od`/`cm` 两项这一波直接归零**(§5.2)。
2. **⭐ 6h 闸锚点:W21 首台 `2026-08-28T12:16:34Z` ⇒ W22 解锁 `2026-08-28T18:16:34Z`**;
   **块内 `date -u` 守卫已成型,原样复用**(本轮拦住四次,见 §5.1)。
3. **⭐ 上一轮交棒②(6h 闸防复发)可销号** —— 已从"提醒"变成"会 exit 1 的代码"并实地生效。
4. **⭐ 总监 —— GH #275 仍未裁**(上一轮唯一新立案,卡着「本装置能不能读出任何经济读数」);
   本轮无新语料贡献(未收割),**但 W21 是它落地后的第一波三粒以上语料候选**。
5. 总监 —— **#271**:本轮**主动不混 `c6a`**(理由见 §5.4),
   ⇒ 若要 (A) 路的 n>1,需要**一次显式裁定**,本台不再自取。
6. 总监 —— **#233(a)** 未裁;**#269** 第三次实地生效(本轮无收割,未触发);
   **#207** `zusstatic` **连续第十六波空转**;**#218 后续** `salveally`/`salveyield` **连续第十二轮不在第 2 行**;
   **#140** 载体门 no-op 面(第二十六轮)。
7. **⭐ 新登记(不立案,先留证)**:载体门**第二次**改掉发波参数(W20 是第一次),
   且两次都是**同一个结构性稀疏**(4 粒 × 10 槽 vs 42 英雄池)。
   若第三次复发,建议由总监考虑「发波前用 `--find <焦点五>` 做覆盖搜索」写进 §5 的常规动作,
   而不是每轮靠本台临时写搜索脚本。
8. 录像组 —— **W20 的 154 局可配对语料(三粒)是最近六波里唯一一份三粒配对语料,优先用它**;
   W18-R 的 127 局两腿全可配对;**W14 的 156 份约 09-16 过期,优先消化**;**孤儿矿 396 局**。
9. replay-check 组 —— 本轮 cadence **未点名**(上一轮 6.1h 洞连续第八轮,本轮自检未复现);
   **strategy 组 —— 9.2h 洞(上轮 7.7h,继续扩大,连续第三轮)**。
10. 存量催办 #217(第十六轮)/#211(第十七轮)/#225/#180(第二十三轮)/#171(第二十一轮)/
    #200(第二十轮)/#181/载体门 PARTIAL(第二十七轮)/`stable-v*` tag(第二十五轮)/
    `campdanger`(第二十轮)/§BL.4 机械化/#75(第十九轮,维持 12)。

## 10. 本次会话 token 用量(铁律 8)

```
TOKENS total_in=2,974,346 out=15,107 turns=33
```

(读数取自 `python3 tools/agent/token_usage.py`,统计时刻在发波前;
其后的发波、核验、写报告与收尾回合**不计入**,当量级参考。)
