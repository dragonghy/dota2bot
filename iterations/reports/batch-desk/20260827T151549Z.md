# 批测台 2026-08-27T15:15Z(第 N+1 次触发)

**一句话**:**W17 整波归零** —— 四台 spot **在同一秒**(12:47:33Z)被 `us-west-2b` 的容量事件带走,
落盘 128 局**全是 radiant 单腿孤儿**,`per_seed: []`,约 **$0.48** 买到**零可用种子**;
上一轮预登记的「8 粒看 ab 层是否稳住」**这一轮答不了**,池子逐位停在 4 粒。
本轮补发 **W17-R**(同树同 arm 同四粒种子),并把根因立成 **GH #252**:
**4×1 拓扑的冗余在放置层被完全抵消 —— 四台全在一个 AZ,而 `spot_run.sh` 没有任何 AZ 分散参数**
(W17-R 起飞后**又全部落在 `us-west-2b`**,暴露面与刚归零的那一波相同)。

---

## 1. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` — **worst exit 3**(有发现,非未核验):

| 腿 | 结果 |
|---|---|
| push gate 上膛 | `core.hooksPath=.githooks` **已 armed** |
| unlanded work | `OK: no unlanded work in the certifiable window`(⚠️ shallow clone,457 refs 里 **418 条 REFUSED/不可核验**) |
| 报告节奏 | **1 处洞**:`replay-check` 08-27T07:19Z→13:28Z **6.1h**(上一轮同一处是 4.9h,**扩大了**) |
| 未裁队列请求 | RIDESHARE **0** / OTHER **0**;total open 36 |
| stable-v* 锚点 | `stable-v1` / `stable-v2` **两个都 ok**(EXISTS/PINNED/SHIPPED 全绿) |
| trunk python 套件 | **43 passed / 0 failed / 0 uncertifiable**(上一轮 42,+1) |
| trunk 快 Lua 检测器 | **12 文件 0 失败**(⚠️ FAST SUBSET,不是全套) |

---

## 2. 成本与预算闸

- `check_costs.sh`:MTD **$50.671**(budgets,免费)= CE **$50.6709276212** **逐位一致(连续第九轮)**。
- ⚠️ **仍是同一张快照,`budget refreshed 2026-08-26T23:46:06Z` 逐位未动** ⇒ **W15-R / W16 / W16-R / W17 一台都没进去**,
  §2(甲) 的滞后**照旧朝危险侧失效**。围栏算术因此**不用裸 MTD**:

| 项 | 金额 | 口径 |
|---|---|---|
| MTD(budgets,滞后) | $50.671 | 免费读数 |
| W15-R(00:16Z,按需) | $1.69 | 已登记 |
| W16(06:16Z,spot) | $0.80 | 已登记上界 |
| W16-R(09:14Z,spot) | $0.190 | 上一轮账单侧实测 |
| **W17(12:20Z,spot,整波回收)** | **$0.479** | **本轮账单侧实测**,见 §3.3 |
| W17-R(15:14Z,spot 4 台) | $0.869 | 本轮预估,见 §5.4 |
| **围栏合计** | **$54.70** | **≤ $60 ⇒ 闸 (iii) 满足** |

- **$50 档的书面解释(章程 §4b(iii) 配套义务,续记)**:MTD 早在前几轮已越过 $50,
  **本轮 budgets 读数逐位未动 ⇒ 无新增跨档**。owner 那封 Budget ACTUAL 告警邮件对得上的账是
  **8 月至今累计的批测波次**;本轮新增 **W17 实测 $0.479(零产出)+ W17-R 预估 $0.869**。
  刹车线 **$90** 未接近;owner 批准线 **$100**。
- 常驻资源只有 AMI `ami-0a990a26d89c66547` + 快照。

---

## 3. ⭐⭐ 本轮主产出:W17 整波归零(GH #252)

### 3.1 四台**同一秒**被回收,原因是容量不是价格

`describe-spot-instance-requests`:

| SIR | 实例 | AZ | Code | Create | Update |
|---|---|---|---|---|---|
| `sir-wkdzh59q` | `i-038c4d8cde6d410d7` | us-west-2b | `instance-terminated-no-capacity` | 12:19:59Z | **12:47:33Z** |
| `sir-crazggxp` | `i-0a4d07d624cfdf53d` | us-west-2b | `instance-terminated-no-capacity` | 12:20:04Z | **12:47:33Z** |
| `sir-4kbzgccm` | `i-0543895dee4b0b051` | us-west-2b | `instance-terminated-no-capacity` | 12:20:08Z | **12:47:33Z** |
| `sir-wc3qkcvp` | `i-0128b1907e7a736ff` | us-west-2b | `instance-terminated-no-capacity` | 12:20:13Z | **12:47:33Z** |

**价格因素已排除**:`describe-spot-price-history` 四个 AZ 全部正常,**无尖峰**
(2a $0.2214 / 2c $0.2334 / 2d $0.2467 / 2b $0.2557)⇒ **纯容量事件**。
**事前查不到**:`GetSpotPlacementScores` 对 `dota2bot-agent` 是 `UnauthorizedOperation`。

### 3.2 后果是「整波归零」,不是章程 §5 预设的「缺一条臂」

镜像 A/B 一台跑两腿(先 ab 后 ba)。27.5 分钟存活窗里**没有一台跑到第二腿**:

```
pool8/258c39 {'radiant': 26}   pool8/2c8641 {'radiant': 26}
pool8/49b83e {'radiant': 26}   pool8/657f7f {'radiant': 26}
```

```
recover_verdict.py ./w17only "<40-id / 354 字节串>"
  files_seen 128 / games_loaded 128 / source_dirs 4 / unparseable 0
  "per_seed": []          ← 零可用种子
  contrast=vs_stable, cand_ref=null
```

**⇒ 章程 §5「回收处置」那条(「一台被抢占 = 该种子缺臂,不要整波作废」)默认回收是独立事件。
4×1 的四台由 AWS 放进同一个 AZ,AZ 级事件同时带走四台 ⇒ 拓扑上的冗余在放置层被完全抵消。**
这与「shallow clone 的空输出被读成无漂移」同族:保护措施看起来在,实际不承重。

### 3.3 单波成本(账单侧实测)

存活窗 12:19:59Z → 12:47:33Z = **0.4593 h**;us-west-2b spot **$0.2557/h** + $0.005 IPv4
⇒ `4 × 0.4593 × 0.2607` = **$0.479**。**买到零效应读数。**

### 3.4 8 粒池化:预登记这一轮答不了,读数逐位未动

八个 run 目录(W16 三粒 + W16-R 一粒 + W17 四粒,**每 run 独立子目录**,GH #225 的 R2 拒答保护):

```
input: files_seen 351 / games_loaded 351 / source_dirs 8 / unparseable 0 / pooled_overridden 0
per_seed: 888 −34.27 (ab30/ba12) | 895 +6.72 (39/19) | 896 −45.61 (29/13) | 906 −87.72 (41/17)
mean: gpm −40.22  xpm −19.09  deaths +0.28  last_hits −1.89  winrate 0.499
comps_better: gpm 1/4  xpm 1/4  deaths 1/4  last_hits 0/4  winrate 2/4
scored_games 200  unfinished 0   suggested: hold_or_reject
```

**351 局进、200 局计分、仍是 4 粒 —— W17 那 128 局一局没进分子。**
均值与上一轮 **逐位相同**(−40.22 / −19.09 / +0.28 / −1.89 / 0.499),这正是「W17 贡献为零」的独立佐证。

⇒ **上一轮 §3.4 的预登记(「n 翻倍 ⇒ SE 应从 ±77 降到 ±55,看 ab 层是否稳住」)本轮无法执行**,
不是被证伪也不是被证实,是**语料没到手**。本台**不**拿 4 粒读数去冒充那个检验。
**保守默认照旧**:两层读数都给、都只作登记,结论带 n 与半差 SE;主读数仍是逐粒均值。

⛔ 孤儿存量又长了一截:W15 的 **30 局** + W16 里 896 的 **27 局** + **W17 的 128 局** = **185 局单腿孤儿**,至今未并进任何池。

---

## 4. W16-R 之后的收割纪律(照做)

- **不传 `--cand-ref`**(本波是 vs_stable 单臂)✅
- **每 run 独立子目录再指向父目录**(R2 拒答保护)✅ `source_dirs 8`、`pooled_overridden 0`
- arm 串核对:`test_set.md` 第 2 行 **40 id / 354 字节**,与发波记录逐字相同 ✅
- **W16 里 896 的 27 局孤儿 ab 腿(run `…_8ca428`)整个目录未进池** ✅(连续第二轮照办)

---

## 5. W17-R 启动(补跑,**不受 §4b(i) 6h 闸**)

### 5.1 为什么可以发

- **性质是补跑**:章程 §5「回收处置」明写「缺的那粒种子再发一台补跑(仍走降级阶梯)」。
  本波四粒**全部**缺,故补发四台。补跑不受 §4b(i) 节流(09:14Z W16-R 已立先例,上一轮 §6.1 复用过)。
- **闸 (iii) 满足**:围栏 **$54.70 ≤ $60**(§2)。
- **例行 6h 闸(W18)不受影响**:锚点仍是 **W17 首台 `12:19:57Z` ⇒ W18 解锁 `18:19:57Z`**。
  **诚实边界**:上一轮把「补跑不作锚点」的裁法写死了,本轮照同一条裁法执行 ——
  W17-R 的 `15:14:19Z` **不作例行锚点**。

### 5.2 两道门(发波前)

| 门 | 调用 | 结果 |
|---|---|---|
| 远端可达 | `git fetch origin d9585a29…` | **commit**,可达 ✅(注:远端 tip 已是 `5418cef`,**本波不测 tip**) |
| **接线门** | `check_armed_wiring.py --cand <40串> --ref d9585a29…` | **exit 0**,`all 40 armed ids wired` |
| **载体门** | `seed_draft.py 918 921 924 926 --assert-carrier 'crystal_maiden,zuus,lion,obsidian_destroyer,skeleton_king'` | **exit 0**(`CARRIER_GATE terms=5 seeds=4`) |

载体剖面与既有 4 粒逐项相同(CM/zuus FULL 4/4;`skeleton_king` PARTIAL 2 = 918/924;
`lion` PARTIAL 1 = 921;`obsidian_destroyer` PARTIAL 1 = 926)。
角色门 / 等级门 id 的载体门仍 **no-op**(缺口 **GH #140**,**第二十轮**)。

### 5.3 为什么仍 pin `d9585a29` 而不是 tip

与上一轮 §6.2 **同一套理由,且本轮更强**:池子仍是 4 粒(§3.4),缺的是**种子**不是新树;
发 tip 会把池子清零重来。**诚实边界**:tip `5418cef` 上的天赋定价 commit **本波仍测不到** ——
这是**连续第二轮**记在账上的权衡,不是遗漏。

### 5.4 启动纪律与核对

`--dry-run` 先行(ref / slots 16 / rec-slots 12 / watchdog 2h / us-west-2 全对上,**波次预算闸未拒发**);
**未传 `--on-demand`**、**未传 `--cand-ref`**;四次调用之间隔 2s。

| 种子 | 实例 | run_id 尾 | `InstanceLifecycle` | AZ | launch |
|---|---|---|---|---|---|
| 918 | `i-04ab17618729dd4e3` | `…_89f466` | **spot** | **us-west-2b** | 15:14:19Z |
| 921 | `i-0ac674ab4654b964f` | `…_9079da` | **spot** | **us-west-2b** | 15:14:24Z |
| 924 | `i-0fe6d12c62731ee1c` | `…_333bf7` | **spot** | **us-west-2b** | 15:14:28Z |
| 926 | `i-094bb86ed818d494c` | `…_88d937` | **spot** | **us-west-2b** | 15:14:33Z |

- **`InstanceLifecycle` 四台全 `spot`** —— `run_id` 的 `spot_` 前缀**没有**被当证据 ✅
- **容量降级阶梯一级都没用上**(launch 阶段无 `InsufficientInstanceCapacity`)✅
- **⚠️⚠️ 四台又全部落在 `us-west-2b`** —— **与三小时前清空 W17 的是同一个 AZ,暴露面完全相同**。
  `spot_run.sh` **没有 `--az` / `--subnet` 参数**,`aws.env` 只有单个 `SUBNET_ID` ⇒
  **批测台没有任何手段分散**,按章程**不自改 harness**,已立 **GH #252**(§7)。
  **本波因此是一次「明知同样暴露面仍要发」的选择**:补跑的价值(池子卡在 4 粒)压过再输一波 $0.87 的风险,
  而唯一的替代(`--on-demand`)与 owner GH #158「spot 优先」相悖,且降级阶梯的触发条件(launch 期容量失败)**并未满足**。
- 拓扑 4 台 × 1 种子;`--slots 16`、`--rec-slots 12`(GH #75 第十二轮维持 12)、`--games 12`、
  看门狗 2h、自毁 + `shutdown-behavior=terminate`。
- **单波预估**:`4 × 0.8328 h × ($0.2557 + $0.005)` = **$0.869**。

---

## 6. 局数(铁律 §7)

- **(a) 上一波次定案 —— W17**:**128 落盘 / 0 计分**(radiant 26 × 4,ba 腿 **0**)/ 暖场 24 / **零可用种子**。
  上一轮预期 **~208 落盘 / ~180 计分** ⇒ **落盘 −38%、计分 −100%**。**原因是整波回收,不是抖动**(§3.1)。
- **`d9585a29` 树池子**:**351 落盘 / 200 计分 / 4 粒**(与上一轮的 223/200/4 相比,**落盘 +128、计分 +0**)。
  ba 占比(计分)= 61/200 → **0.305**,与 W11–W16 的 0.304–0.325 并列(**W17 未参与该分母**)。
- **(b) W17-R 在跑**:4 台 × 1 种子 × `--games 12` × 2 腿,预期 **~208 落盘 / ~180 计分**。
  **诚实边界**:W17 刚刚证明这**只是期望值不是下界**,且本波在**同一个 AZ**。

---

## 7. ⭐ GH #252 立案(新)

**`[harness] spot_run.sh 无 AZ 分散能力:4×1 拓扑一次 AZ 容量事件整波归零(W17 实证)`**

请求两条:(1) `spot_run.sh` 加 `--subnet` / `--az`(或内置轮转),`aws.env` 带 subnet 表而非单个 `SUBNET_ID`;
(2) 考虑给 `dota2bot-agent` 加**只读免费**的 `ec2:GetSpotPlacementScores`,让发波前能看一眼容量。
**验收零额外支出**:下一波 4×1 起飞后 `Placement.AvailabilityZone` 至少两个不同值,搭下一波的车即可验。

---

## 8. 泄漏检查(两次)

- **开工**:`check_costs.sh` 的 running/pending 段 **空** —— W17 四台**已被回收终止**,W16/W16-R 均已自终,**无泄漏**。
- **收尾**:`--leak-only` **恰 4 台在跑**(本轮自己发的 W17-R,id 与 §5.4 逐个对上),常驻只剩 AMI + 快照 —— **不是泄漏**。

⚠️ **保留作证边界(连续第四轮)**:`--leak-only` 读到 N 台**只能**证明「此刻在跑的是哪几台」,
**不能**证明「过去这一轮没漏过」。路 4(SIR)本轮再次补上了部分回溯(它正是 §3.1 那张表的来源,
**而且这次它是唯一能说清「四台是被回收不是自终」的证据源**),**但其保留窗口仍未知,不构成完整回溯审计**。

---

## 9. 铁律 6

- 静态门 `bash tools/agent/luacheck_gate.sh` 由 `.githooks/pre-push` 在 push 时自动执行,
  `core.hooksPath` 开工自检已上膛;**未使用 `RULE6_BYPASS`**。
- 动态半(`lua5.1 tests/run_tests.lua`,~100 min,GH #124)**未跑,也不声称**。
- **本轮 `bots/` 与 `game/` 一行未改**(改动只有 `iterations/` 下的报告与章程)。

---

## 10. 交棒

1. **⭐ 下一轮本台 —— 收割 W17-R**,四个 run 前缀 `…_89f466 / _9079da / _333bf7 / _88d937`,
   **不传 `--cand-ref`**;**每 run 独立子目录再指向父目录**;与既有 4 粒合成 **8 粒**读数,
   **执行上一轮 §3.4 那条预登记**(两层都给、带 n 与半差 SE、看 ab 层是否稳住;n 翻倍 ⇒ SE ±77→±55)。
   **⚠️ 先查 SIR**:本波与 W17 在**同一个 AZ**,若再度整波回收,`per_seed` 会再次为 `[]` ——
   **那时不要把 4 粒读数当成 8 粒的检验结果**。
2. **6h 闸**:例行锚点 = **W17 首台 `12:19:57Z` ⇒ W18 解锁 `2026-08-27T18:19:57Z`**
   (**W17-R 是补跑,不作锚点**)。先 `date -u` 再比。
3. **⭐⭐ 总监 —— GH #252(新)**:AZ 分散缺口。**这是本轮唯一的新立案**,
   且它**当轮就复发了一次**(W17-R 又全落 us-west-2b)。
4. **⭐ 总监 —— GH #204(第十轮)**:上一轮撤回的建议维持撤回;新问题「池化到几粒才有判别力」
   (当前估计 **n ≳ 15**)**本轮拿不到新证据**,原因是 §3.2 而非统计。请带 n 重裁。
5. **⭐ 总监 —— GH #233(a)** 仍未裁。本轮新增一个数据点:W17 四台**整波回收**的实测 **$0.479**,
   说明「按按需口径保守登记」在**回收波**上偏离更大(回收波跑不满,时长只有 0.46h)。
6. **总监 —— GH #239 路 4 第三次实证**:本轮 SIR **是唯一能区分「回收 vs 自终」的证据源**(§8)。保留窗口仍未知。
7. 总监 —— **#207(第十轮)**:`zusstatic` **连续第九波空转**(W17 归零,这一波连语料都没有)。
8. 总监 —— **#218 后续**:`salveally` / `salveyield` **连续第五轮不在第 2 行**。
9. **录像组** —— `abilanc` (a) 取证语料:W17 那 128 局**虽不计分但 `.dem` 仍在**(12 槽 × 4 台),
   **单腿孤儿对逐帧取证一样可用**;W14 的 156 份 `.dem` **约 09-16 过期**,优先消化。
10. **replay-check 组 —— 报告节奏 6.1h 洞**(§1,上一轮 4.9h,**连续第二轮且扩大**)。
11. 存量催办:#217(第九轮)/ #211(第十轮)/ #225 / #180(第十六轮)/ #171(第十四轮)/
    #200(第十三轮)/ #181 / 载体门 PARTIAL(**第二十轮**,GH #140)/ `stable-v*` tag(第十八轮)/
    `campdanger`(第十三轮)/ §BL.4 机械化 / #75(**第十二轮,维持 12**)。

---

## 11. 本次会话 token 用量(铁律 8)

```
TOKENS total_in=3,905,040 out=30,519 turns=39
```

(统计时刻为写报告前;此后的收尾回合不计入。)
