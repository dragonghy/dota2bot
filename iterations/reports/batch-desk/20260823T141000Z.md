# 批测台 2026-08-23T14:10:00Z —— 纯收割轮:§AT.1 归因波读数到货,`itemtrip` **落在第一档**

**一句话**:24-id 归因波全量收割(275 有效局 + 24 暖场 = 299,四种子两 wave 全齐、unfinished 0),
**X = gpm −26.44**;§AT.1 预登记的三档里**第一档触发**(`itemtrip` 吃下的不是 ~−50 而是
**逐种子配对 +66.98 ± 22.36 SE**)⇒ 按预登记读法 **`itemtrip` 退回协同组**,§AT.2 的
「开脱 ⇒ 查 `9fa4898`」分支**未触发**;`--rec-slots 12` 第二次验收 exit 0、归属一致率 231/231;
本轮零支出、未启动任何波次((i) 差 ~4h),新查出 **claim 干净但 2 个 `.dem` 没上传成**。

---

## 1. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` ⇒ **worst exit 0**。
- UNLANDED **0**(certifiable 窗口内无未落地工作;276/302 refs 因浅克隆不可判定,按工具 LIMITS 属预期);
- cadence **clean**(streams 5 / reports 275 / 窗口 24h);
- trunk python **17 passed / 0 failed**(上轮 16/0,+1)。

## 2. AWS 引导 + 成本

`session_setup.sh` ⇒ `AWS ready: arn:aws:iam::581436534924:user/dota2bot-agent`(受限用户,正确)。
容器无 AWS CLI,脚本自行 pip 装了 `aws-cli/1.46.0` —— 幂等分支照常工作。

**MTD $23.578**(免费 `budgets/dota2bot-batch`,forecast 23.139,limit $100)。
$45 launch fence / $90 brake / $100 owner 线**全未触及**;**未花 $0.01 调 CE**。
**本轮零支出**(未启动任何实例)。

**读数仍是零信息,判据是 refresh 时刻不是轮次差**:`budget refreshed` = **2026-08-22T21:26:04Z**,
与 02:06Z/04:06Z/06:11Z/08:15Z/10:15Z/12:09Z **逐秒相同** ⇒ **同一份快照第七次复读**。
沿用的可证伪形式不变:**晚于 12:09Z 的第一次真刷新应 ≈ $28.1**(06:11Z 波 ~$1.5 + 12:09Z 波 ~$1.5
叠在 12:09Z 那次读的 $23.578 上)。这一轮再次**没有**给出任何新证据,如实记为复读。

## 3. 收割:12:09Z 归因波,全量重算

标准路径 `recover_verdict.py`,**先分 run 下载再带 `<HHMMSS>__` 前缀合并**。

| seed | run_id 尾 | analysis | demclaim | radiant wave | dire wave |
|---|---|---|---|---|---|
| 888 | `…_9b1fe4` | 74 | 58 | 42 | 26 |
| 895 | `…_feab35` | 71 | 55 | 42 | 23 |
| 896 | `…_f0efcc` | 78 | 59 | 41 | 31 |
| 906 | `…_545d80` | 76 | 59 | 40 | 30 |
| **合计** | | **299** | **231** | | |

**局数(铁律 7)**:
- **上一波(本波)最终有效局数**:**275 有效局 + 24 暖场 = 299**。
  per-seed / per-side:888 = 42 ab / 26 ba、895 = 42/23、896 = 41/31、906 = 40/30。
  **ab/ba 严重不对称**(radiant wave 一律跑满 42 上下,dire wave 只有 23–31)——
  与 06:11Z 波同型,是 dire wave 被 2h 看门狗截断,不是饿死;四种子两 wave 全在,
  `unfinished_games` = **0**,`recover_verdict.py` 的两 wave 平均因此全部可算。
- **本轮在跑波次进度**:**无**(开工时 0 台在跑,本轮也未启动)。

### 3.1 verdict(24 id @ `7159ff3` @ 种子 888/895/896/906)

```
gpm       -26.44   comps_better 0/4
xpm       -32.83   comps_better 0/4
deaths     +0.33   comps_better 0/4
last_hits  -1.85   comps_better 0/4
winrate     0.441  comps_better 1/4   (neutral 0.5, scored 275/275)
suggested = hold_or_reject
```

逐种子 gpm:**888 −38.90 / 895 −26.47 / 896 −14.71 / 906 −25.69**(sd 9.95,SE 4.98)。
逐种子 winrate:0.365 / 0.460 / **0.507** / 0.433。

`winner_by` = `economy_10min_cap` **299/299**,`engine` 档 **0** ⇒
**必查项 ⑧ 的可证伪预测第三次兑现**(GH #135 的构造性论证成立);
按 `wr()` 的自陈边界,winrate 在这份语料里**不是独立于金钱的第二个证人**,
是同一信号的符号粗化,读的时候必须带这句。

### 3.2 §AT.1 预登记读法 —— 落第一档,但**落在档位之外的那一侧**

预登记(数据到货前写死,`test_set.md:5526`):已有 25-id @ `7159ff3` @ S = **−93.30**,
本波买 24-id @ 同树同种子 = **X**。

| 档 | 判读 | 本波 |
|---|---|---|
| X ≈ **−40** 量级 | `itemtrip` 吃下 ~−50 ⇒ **退回协同组** | **← 触发** |
| X ≈ −90 量级 | `itemtrip` 被开脱 ⇒ 走 §AT.2(`9fa4898` CM arcane boots 头号嫌疑) | 未触发 |
| 两者之间 | 报逐种子 Δ 离散度,不归因 | 未触发 |

**X = −26.44。** 与两个门柱的距离是 **13.6 vs 63.6**,归属没有歧义。
**诚实边界(不追加、不改写门柱)**:−26.44 **不在**预登记那三档覆盖的区间里 ——
它掉出了梯子**好的那一头**。预登记没有为「比 −40 还好」写档,本台**照第一档执行**
(方向、结论、下一棒都由第一档决定),同时明写实测量级**大于**预登记的 ~−50。
**不为这个读数事后新增第四档。**

### 3.3 归因量:逐种子**配对**差(同树、同种子集、只差 armed 串一个变量)

§AU.3 的链失效条件在发波前逐条查过且未触发(12:09Z 报告 §5.3),两波之间
`7159ff3` 逐位相同、种子集逐位相同 ⇒ 可以**按种子配对**读,不是跨波残差比较。

| seed | 25-id(含 itemtrip) | 24-id(本波) | Δ = 移除 `itemtrip` 的收益 |
|---|---|---|---|
| 888 | −122.10 | −38.90 | **+83.20** |
| 895 | −119.94 | −26.47 | **+93.47** |
| 896 | −105.72 | −14.71 | **+91.01** |
| 906 | −25.46 | −25.69 | **+0.23** |
| **均值** | −93.30 | **−26.44** | **+66.98**(sd 44.71,**SE 22.36**,3.0×SE) |

**方向 4/4**,但**离散度是这张表的重点**:三个种子 +83~+93 一致,**906 完全没动**(+0.23)。
按 08-19 立的零点纪律(单种子 gpm 差 **±30 以内属纯噪声**),+83/+93/+91 **三个都远在噪声之外**,
+0.23 **就是零**。⇒ 可读的形状是「`itemtrip` 在 4 个种子里的 3 个上花掉约 −90 gpm,在第 4 个上不花」,
**不是「平均 −67」**。906 在 25-id 波里本来就是最不坏的那个种子(−25.46),24-id 波里仍是 −25.69 ——
**它这两波逐字没动**,这本身是给协同组的线索(`itemtrip` 的域与种子阵容相关)。

### 3.4 一个白拿的同串跨树复核

00:11Z 波是**同一个 24-id 串**、树 `cde1d6c`,读数 **−42.49 / winrate 0.394**;
本波同串、树 `7159ff3`,**−26.44 / 0.441**。§AF.3 作废的是「跨树 **+** 跨集合」的残差比较,
这一对**只跨树不跨集合**,所以能读的很有限但不是零:**24-id 残组两次独立落在 −26…−42 带内**,
而含 `itemtrip` 的那一次是 **−93.30**,在带外一倍以上。**这不是显著性检验**,
是给 §3.3 的配对结论加一条不同来源的旁证。

**它买不到什么(§AT.1 原话,照抄)**:**不能开脱另外 24 个 id。−26.44 仍然是 −26.44。**
五指标里四项 0/4、winrate 1/4 且均值 0.441 < 0.5 ⇒ **测试版整体相对稳定版仍是净负**,
`itemtrip` 只是解释了其中翻倍的那一半。

## 4. `--rec-slots 12` 第二次验收(必查项 ⑥)—— **通过**

```
python3 tools/batch_test/soak/rec_slot_cost.py <本波 4 个 run> \
    --baseline tools/batch_test/soak/rec_slot_baseline.json
⇒ OK: measured on 299 games, 4 control slots.   exit 0
   compared 16 slot(s); 0 recording slot(s) beyond tolerance
```

事先登记的三条触发器**一条都没触发**:非 exit 0(否)/ 任何槽 beyond tolerance(0 个)/
一致率跌破 100%(见 §5,231/231)。⇒ **`--rec-slots 12` 保持**。

净值逐槽(已扣 box factor 1.035,4 个对照槽 13–16):
录制槽 1–12 = **−2.1 / +4.9 / +5.0 / +5.7 / +6.4 / +4.5 / +5.1 / +6.0 / +5.2 / +4.8 / +4.9 / +4.9 %**,
对照槽 13–16 = −1.0 / −0.9 / +1.2 / +0.7 %。

> **读法提醒(避免误报)**:`rec_slot_cost.py:307` 的判据是 `rel < -tolerance`,
> **只对赤字(录制槽比基线慢)报警**。本波全部是**正值 = 录制槽比基线更快**,所以
> 「0 beyond tolerance」是真通过,不是判据放水。但章程里「都在 5% 以内」那句措辞
> 会让人以为 +6.4% 该报警 —— **它不该**,方向反了。**这句歧义留给总监决定要不要改章程措辞**;
> 本台不自改章程正文以外的裁定性文字。
> 顺带:录制槽普遍**快过**同机对照槽,与 08-22 基线波观察到的「slot 1 是最快那一槽」同向,
> 单录制者代价实测为零那条结论**在 12 个并发录制者下依然没有反例**。

`games per slot`:录制槽 18–20 局,对照槽 17 局/槽(4 槽) ⇒ 录制槽**多**跑了 1–3 局。

## 5. 归属三判据逐局重核(必查项 ⑦)

231 个 claim,`method` = **`logname` 231/231**,`hostname_hits` = **1 231/231**。

| 判据对 | 一致 |
|---|---|
| `by_logname` == `by_hostname` | **231/231(100.0%)** |
| `by_logname` == `by_mtime` | **0/231(0.0%)** |
| 三判据全一致 | 0/231 |

- **100% 是相对上一波的回升**:06:11Z(16 槽)是 306/308,本波(12 槽)**回到 231/231**。
- **`by_mtime` 0/231 是设计兑现,不是缺陷,且这是第二次实测**(08:15Z 是 0/237)。
  `dem_claim.sh` 头注明写 mtime「只在 REC_SLOTS=1 下成立,永不用来在真实录制者之间断连」——
  **两波共 468 局给了这句话一个硬数字:在 ≥12 个并发录制者下,老启发式会 100% 判错。**
  它现在只作为「would have picked」记录,不参与选择。
- `candidates` 分布 5–12(众数 10,49 局 11、44 局 12)⇒ **多录制者歧义在 12 槽下是常态**,
  不再是 08-22 那句「从未真正演练过」。它被 logname 一路正确解掉。

## 6. ⭐ 新发现:claim 干净,但 **2 个 `.dem` 没上传成**(第一次实测到 claim→upload 的丢失)

| run | demclaim | `dem21/` 里的 `.dem`(锚 `\.dem(\.gz)?$`)| 差 |
|---|---|---|---|
| `…_9b1fe4`(s888) | 58 | **56** | **−2** |
| `…_feab35`(s895) | 55 | 55 | 0 |
| `…_f0efcc`(s896) | 59 | 59 | 0 |
| `…_545d80`(s906) | 59 | 59 | 0 |
| 合计 | **231** | **229** | −2 |

丢的两局:**`20260823_123403_slot4`** 与 **`20260823_123417_slot7`**。
两者的 claim sidecar **完全干净**:`method=logname`、`by_logname == by_hostname`、
`hostname_hits=1`、`path` 已写(`/opt/soak/slot<N>/<tag>.dem`)。
`soak/<run>/` 下这两局也**没有** `.dem`(该目录本波 4 个 run 全是 0,符合 `dem_bulk_prefix` 设计)。

**能确定的**:失败发生在**认领成功之后的上传步骤**,不在归属逻辑里 ——
`dem_claim.sh` 的「no claim => no upload」doctrine 没有被违反,丢的是**有主的**文件。
**不能确定的**:具体原因。两局在 **12:34:03 / 12:34:17**,距开波 ~25 分钟,
**不是收尾截断**;相邻槽(4 和 7)、相隔 14 秒,像是并发上传路径上的一次竞争或瞬时失败,
但**实例日志已随自毁消失,本台无法再查**。

**影响**:帧通道真实值是 **229/299 = 76.6%**,不是 231/299。§7 的英雄普查「有 `.dem`」一栏
是按 **claim** 数算的,因此对这两局**最多高估 2 局**(两局的 slot4/slot7 阵容各贡献 ≤1)。
**已开 `[harness]` GH #147,按章程不自己改 harness。**

## 7. 英雄普查分母表(必查项 ⑨,固定交付)

**语料**:本波 299 局,231 局有 claim(229 局真有 `.dem`,见 §6)。四种子各一套固定 10 人镜像阵容。

| 英雄 | 出现局数 | 有 `.dem`(按 claim)| 局终等级 min/med/max | ≥7 | ≥15 |
|---|---|---|---|---|---|
| zuus | **299 (100%)** | 231 | 8 / 13 / 18 | 299/299 | **47/299** |
| crystal_maiden | **299 (100%)** | 231 | 7 / 12 / 17 | 299/299 | **26/299** |
| skeleton_king | 147 (49.2%) | 114 | 8 / 12 / 16 | 147/147 | 6/147 |
| lion | 78 (26.1%) | 59 | 8 / 11 / **14** | 78/78 | **0/78** |
| **axe** | **0 (0%)** | — | — | — | — |
| **death_prophet** | **0 (0%)** | — | — | — | — |

全阵容频次(局数/299):crystal_maiden 299、zuus 299、lina 223、jakiro 221、tidehunter 152、
viper 150、slardar 149、necrolyte 149、skeleton_king 147、bristleback 147、
luna/lion/obsidian_destroyer/ogre_magi 各 78、phantom_assassin/sniper/juggernaut/earthshaker 各 76、
sven/spirit_breaker/witch_doctor/drow_ranger 各 74、dragon_knight/venomancer 各 71。

**两条 08:15Z 结论在独立语料上复现**(同种子集,不同波次):
- **`axe` / `death_prophet` 分母仍是 0** ⇒ `hero-2` 那类请求在现行四种子下**结构上买不到**,
  等的是含 Axe 的种子(§AU.3 已登记 `--find axe,lion` → 974/986/1024,尚未上机);
- **`lion` 局终等级上限 14,0/78 到 15** ⇒ `hero-4` 的 t15 域在 Turbo 10 分钟裁判上限下
  **仍是结构性空集**,两波合计 **0/155**。
- 变化项:`zuus` ≥15 从 37/306(12.1%)升到 **47/299(15.7%)**,`crystal_maiden` 从
  12/306(3.9%)升到 **26/299(8.7%)** ⇒ `hero-3` 要的 rank-3 那一档分母**比上一波宽了一倍多**,
  但仍必须带着分母报,别当密度用。

## 8. 启动决策:**不启动**

| 条件 | 判定 |
|---|---|
| (i) 距上一波 ≥6h | 上一波 **12:09:32Z** ⇒ 门槛 **18:09:32Z**;现在 ~14:10Z,**差约 4 小时,不满足** |
| (ii) 有新东西要测 | 有(W3 名额的三条 claimant),但被 (i) 挡住 |
| (iii) 成本 | ✅ $23.578 ≤ $45 fence |

**(i) 差约 4 小时属于「不启动」那一类先例**(22:06Z / 20:09Z / 16:10Z / 08:08Z / 16:06Z),
**明确不适用**于 12:09Z 那次「差 99 秒照发」的最轻档。本轮不援引任何例外。

`queue.json` 有 pending 请求(`strategy-4` `campgrade`、`strategy-5` `creeppull,pullbeat`、
`hero-8` `cmboots`、`strategy-5b` `midtp,suptp,midsupyield`,以及四条不要波次的档案侧请求),
**它们不受 (i)(ii) 节流,只受成本约束** —— 但三条 W3 claimant 等的是**排期裁定不是波次**
(12:09Z §8 已把这一棒显式交总监,截止 18:09:32Z),本台**不自裁**,因此本轮不代为启动。

## 9. 跨组交接(铁律 9,不落在报告里)

1. **GH #140 追评**(§AT/§AU 裁定所在的活线程,不新开):送 §3.1–3.4 的读数与
   §AT.1 第一档判定,**并同时交出下一棒** —— `itemtrip` **resolve** ⇒ 按 §AT.3 的算术
   **W3 的那一个名额现在真的开出来了**,三条 claimant 的排期裁定截止 18:09:32Z 不变,
   未裁的保守默认仍是 `strategy-4` `campgrade`(三条里唯一有明文批准的)。
2. **新开 `[harness]` GH #147**:§6 的 claim 干净但 `.dem` 未上传(2/231),带两局 tag、
   claim sidecar 全文、以及「实例日志已随自毁消失」这条查证边界。
3. **`queue.json`**:`strategy-3`(`itemtrip`)的 `result` 补上归因结论并标 `done`
   (它的条件 (a) 帧证据仍在 `dem21/spot_20260823_0611*` 那 237 个 `.dem` 里,归录像组);
   四条 W3/pending 请求状态**不动**(等裁定不等波次)。
4. **给录像组的语料**:本波 `s3://dota2bot-batch-results-4924/dem21/spot_20260823_1209*/`
   下 **229 个 `.dem`**,帧通道 12/16,归属 100% 可信(231/231 logname==hostname);
   `creeppull` / `pullcamp` 在候选腿 armed(串里第 19/20 位)⇒ **owner P1 条件 (a) 的第二份语料**,
   与 06:11Z 那份同树同种子、只差 `itemtrip`。

## 10. 验证(铁律 6)

本会话**未改 Lua**(改动只在 `iterations/` 下)⇒ 铁律 6 无适用对象;
容器无 `luacheck` / `lua5.1`(复核过)。开工自检的 trunk python 17/0 已记在 §1。

## 11. 固定栏位

| 栏位 | 开工 | 收尾 |
|---|---|---|
| 实例(running/pending/stopping/stopped) | **0** | **0** |
| 游离卷(status=available) | 0 | 0 |
| 快照(owner self) | 1 | 1 |
| EIP | 0 | 0 |
| spot 请求(open/active) | 0 | 0 |
| `soak/` 前缀 | **172** | 172 |
| `dem21/` 前缀 | **24** | 24 |
| `unattributed/` | **0** | 0 |
| off-roster | **0** | 0 |
| `validation/` 最新条目 | **2026-07-23**(标准路径走 `recover_verdict.py`,陈旧是**预期**)| 同 |
| 远端 `main` tip | `9be4f4a` | `9be4f4a`(= 本地 HEAD) |

**无泄漏。** 12:09Z 那四台已全部自毁(2h 看门狗 + `shutdown-behavior=terminate`),
收尾 `--leak-only` 零成本复核为 0 台。

## 12. 下一波

**最早 2026-08-23T18:09:32Z**(不变)。预置:
- **串看总监对 W3 名额的裁定**(截止 18:09:32Z,未裁默认 `strategy-4` `campgrade` 独占);
- 独占波按 §AU.3 **钉发波时刻的 `origin/main` tip**,不钉 `7159ff3`(独占波无跨波配对义务);
- 种子:若总监要 axe 语料则登记 `--find axe,lion` → **974/986/1024**(本轮普查再次证明
  888/895/896/906 里 **axe 分母为 0**);否则退回 888/895/896/906;
- 4 台 × 1 种子 on-demand、`--slots 16`、**`--rec-slots 12`**(§4 第二次验收通过)、
  2h 看门狗、`--games 22` 不再上调;
- **发波前必跑 `check_armed_wiring.py`,非 0 不发**;四次调用之间**跨过一个整秒**,
  启动后用 `soak-run` 标签核对四个 run_id 两两不同。

**必查项(10 条,沿用)**,下一波的具体期望值:
1. 暖场 stamp 全等于**届时钉的那个 SHA**(独占波钉发波时 tip);
2. 分 run 下载 + 前缀合并,与逐 run 之和逐位一致;
3. S3 真实 cand 串与届时 §x.0 逐字节比对 + count/uniq/字符数;
4. `demclaim` 与 `analysis` 一起下;
5. `.dem` 数 `dem21/`,**锚 `\.dem(\.gz)?$`**;
6. `--rec-slots 12` **第三次**验收(触发器不变;注意 §4 的方向提醒:只有**赤字**才报警);
7. 归属三判据逐局重核(期望 logname==hostname 100%;`by_mtime` **期望仍是 0%**,那是设计);
8. `winner_by` 的 `engine` 档**再次为 0 = 预期兑现**,非 0 当场查;
9. **英雄普查分母表**四个焦点英雄一个不落(不交就是掉棒);
10. **🆕 `demclaim` 数 vs `dem21/` `.dem` 数逐 run 对账**(§6 第一次查出 −2;
    以后每波固定核,差值非 0 必须点名到 tag)。

## 13. Token 用量(铁律 8)

```
TOKENS total_in=4,886,458 out=40,483 turns=46
```
(统计时刻为止;此后的收尾回合不计入。)
