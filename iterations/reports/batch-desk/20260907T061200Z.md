# batch-desk 2026-09-07T06:12–06:3xZ(发波轮:W53 = 51-id 成员串家族第一波)

**一句话**:W53 四台 spot 全部起飞(`06:22:02–06:22:11Z`,一 AZ 一台),四道闸**全部在起飞前跑完**、
闸 (i) 在**动作那一刻**读到 `UNLOCKED (exit 0) margin +14s past unlock`;
⭐⭐⭐ 本轮头号产出**不是那四台机器,是闸 (i) 在同一轮里赢了两次**——
一次赢了本台自己手算的容器钟(它比工具晚约 4 分钟,而"手算说门开了"正是发波前**唯一**
不需要任何人同意就能越过闸的路径),一次赢在 `exit 9` **真的挡下了第一次发波尝试**。
零泄漏,MTD `$62.042`,围栏 headroom `$15.868`。
⛔ trunk python **RED**,红在**今晨 04:27Z 刚落地的检测器没登记它自己的两个 HP 常数**——
**本周同族第四例,但这一例的门是对的**;trunk Lua 腿本轮**干净**(上一轮的 GH #585 不复现)。

---

## 一、自检(铁律 10)

- ⚠️ **第一条命令第十二轮踩同一个坑**:`... | tail -40` ⇒ 工具自拒
  `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked`
  (它自己那行写着「已复发 5 次,每次都是本轮第一条命令」)。改重定向 + 后台重跑。
  **⭐ 这一轮值得记的不是又踩了,是踩法变了**:上一轮交棒 ⑤ 已请总监「给独有退出码或 wrapper」,
  未裁;而本轮**已经知道这个坑**、**上一轮的报告就写着它**,仍然踩了。
  ⇒ **「靠人记得」这条路本轮再次被证伪,而这次的证据是本台自己**。
- 逐腿照抄工具自己的行(重定向那次):
  - 推送门:`push gate armed (core.hooksPath=.githooks)` ✅
  - `unlanded_commits`:`OK: no unlanded work in the certifiable window.`(shallow clone,
    858/885 refs `REFUSED` 为不可核验——**这是工具的诚实边界不是发现**)
  - `CADENCE`:**2 findings**,都在**协同组**(`09-06T07:30Z→11:05Z` 3.6h;`09-07T01:20Z→05:00Z` 3.7h),
    门限 3.5h。**本台不解读别组的节奏**,登记转总监。
  - 稳定版锚点 **5 个全 OK**:`stable-v5 = ckpush`,`3fe17f9dd08b`,`EXISTS/PINNED/SHIPPED` 三格全 ok。
  - promote 约束:`162 distinct live gate id(s)` / **17** 个带 PROMOTED 注 / **`FROZEN none`**;
    4 个 co-promote 原子全 ok(`pullcad` 陷阱本轮未触发)。
  - `OWED` registry:10 行。**归本台的两行见第八节。**
  - `ORPHAN_PROPOSAL` none;`expired waits` 无。
  - `RIDESHARE (rule this round)`:**1**(`hero-40`,`liondrainmi`,pending,**未 armed**,
    P4.2 冻结期内不请求入集)⇒ 这就是 `queue-rulings` 那条 finding 的来源,**归总监裁**。
  - ⭐ **trunk Lua 腿本轮跑完了,而且干净**:逐字
    `84 tagged detector file(s), 0 failures -- FAST SUBSET, not the full suite.`
    ⇒ **上一轮的 `test_corpus_scale.lua` 红(GH #585)本轮不复现**。
    ⚠️ 工具自己那行的诚实边界照抄:这是**快子集**,未打标签的测试不在其中。
  - **`SELFCHECK_EXIT=3`**(后台跑完后取的**真退出码**,不经管道)。
    `exit sources` 节逐字:`legs run 10`;
    `FINDINGS (exit 3): cadence queue-rulings owed-executions trunk-red(python)`;
    **`UNCERTIFIABLE (exit 2): none`**;
    `NOT RUN (inside a leg): tests/test_rc_wrapper.py tests/test_selfcheck_lua_leg.py`。
    ⚠️ 口径分清:**腿一级没有 `UNCERTIFIABLE`**,但**腿内部有两个测试没跑**
    ——**没跑成不是通过**,这两条本轮无人看过。

## 二、⛔ trunk python RED —— 而这一次门是对的,红的是没走登记这一步的落地

`108 passed, 1 failed, 2 uncertifiable`,唯一 FAIL:**`tests/test_detector_source_constants.py`**。
直接单跑复核 `DSC_EXIT=1`,逐字两条:

```
FAIL zusultstrand_domain:HP_GATE is classified (UNREGISTERED) -- a new module-level HP constant
     appeared and no one said what it is.  Add a row to HP_CENSUS: MIRROR (and pin it in section 2),
     PROXY (import it, do not retype it), or INDEPENDENT (say why it may share a number).
FAIL zusultstrand_domain:ULTCASH_HP_GATE is classified (UNREGISTERED) -- ...(同上)
```

**溯源(算术,不是判断)**:`tools/batch_test/behavioral/zusultstrand_domain.py` 由**录像组
`6eaebe38`(2026-09-07T04:27:42Z,"hero-37 zusultstrand archive domain census (152 games)")**
落地,距本轮开工约 1h45m。它带进两个模块级 HP 常数,`HP_CENSUS` 里没有对应行 ⇒ 断言按构造转红。

⭐ **本周第四例「红在登记表的形状」,但这一例与前三例的方向相反,这才是要点**:

| 例 | 红的原因 | 门本身 |
|---|---|---|
| GH #579 `test_stable_anchors.py` | `stable-v4` 双 id 塞进单键 | 门有缺陷 |
| GH #584 `test_carrier_hero_guard.py` | 硬编码 `chaos_knight`,被一次正当 promote 证伪 | 门有缺陷 |
| GH #585 `test_corpus_scale.lua` | 硬编码语料规模 109,会被下一个 fixture 证伪 | 门有缺陷 |
| **本轮 `test_detector_source_constants.py`** | **新检测器没登记自己的常数** | **门是对的** |

前三例是「门把一个正常动作误判成红」,**本例是门抓到了一个真的漏做的步骤**。
⇒ **修法不同**:前三例要改门(把硬编码换成 ratchet/派生),**本例要改落地方**
(给两个常数补 `HP_CENSUS` 行,择其一:MIRROR / PROXY / INDEPENDENT)。
⚠️ **把它当成前三例的第四个实例去"修门",恰好是这条门存在的理由的反面。**

**诚实边界(本轮能给出比上一轮更强的一句)**:
`HEAD == origin/main == 523f21baf14a`,且 `git diff origin/main -- tests/ bots/ game/ tools/` **为空**
(`git status` 只有 `iterations/data/seed_roster_index.json` 一个 M,是本轮 `--build` 写的)
⇒ **这条红就在 main 上,不是工作树伪影**,不需要 `git stash`。

**另两条本轮的好消息(逐条核过,不是推断)**:
- 上一轮的 `test_carrier_hero_guard.py`(GH #584)**本轮 PASS**(`108 passed` 里有它)⇒ 已修好。
- **GH #490 点名的那条断言不再是失败项**:`J.ShouldTpSupportTowerFight` 的正则从
  `source_constants.py:193` 挪进了 `tests/test_detector_source_constants.py:682-685`,
  本轮该文件的第 1、2 节**逐条 ok**(含 `tpdefend_events:HEAT_HP` 的 MIRROR 行)。
  ⚠️ **本台只声称「#490 点名的那条现在解析得到且为 ok」**,不声称 #490 可以关——关不关归总监。

## 三、AWS 引导与成本(章程第 1–2 步)

- `session_setup.sh` **`AWS_SETUP_EXIT=0`**,身份 `arn:aws:iam::581436534924:user/dota2bot-agent`(受限用户,正确)。
  容器无 AWS CLI,脚本自己装上(`aws-cli/1.46.1`)——**「容器里没有 CLI」不是跳过的理由**。
- `check_costs.sh` **`COST_EXIT=0`**:
  - **MTD `$62.042`**,来源 `budgets/dota2bot-batch`(免费),`budget refreshed 2026-09-07T02:04:33Z`;
    ≥$35 ⇒ 按章程自动花 `$0.01` 用 CE 复核 **`62.0418196925`**,两源一致。
  - `forecast $205.267` vs `budget limit $100.0`。
  - **与上一轮(03:05Z)逐位相同**,`refreshed` 时间戳也相同 ⇒ **这是同一张快照**,
    不是「三小时没花钱」的读数。⛔ 本台**不**从中推断 W52 是否已入账(章程步骤 2 乙 明令禁用)。
  - 常驻成本仍只有 AMI `ami-0a990a26d89c66547`。

## 四、发波:W53 四台 spot 全部起飞

| 种子 | 实例 | AZ | LaunchTime | SIR | 生命周期 |
|---|---|---|---|---|---|
| 7144 | `i-0d10b2db261c57b13` | us-west-2a | 06:22:02Z | `sir-99r7gyxm` | spot / active / fulfilled |
| 7154 | `i-00a6e66d54b1a9fb3` | us-west-2b | 06:22:05Z | `sir-e8azhk8m` | spot / active / fulfilled |
| 7156 | `i-0fbfedf2a0cc4de69` | us-west-2c | 06:22:08Z | `sir-wcmzk25n` | spot / active / fulfilled |
| 7162 | `i-02bad63ba3c9b03af` | us-west-2d | 06:22:11Z | `sir-rsk7g9mm` | spot / active / fulfilled |

- **市场类型逐台由 `InstanceLifecycle=spot` 核实**,不读 run_id 的 `spot_` 前缀(那是硬写字符串)。
  **四台全 spot,零容量降级**——与 W46 那次「两个 AZ 无容量」形成对比。
- **SIR 行在发波轮就读了**(GH #556):四行 `active/fulfilled`,`CreateTime` 与 LaunchTime 一致。
- 臂串:**51 id / 460 字节 / md5 `e61700f44778dc1ab67166922d22b0bc`**,
  从**发波树** `origin/main` 上重算(不是抄上一轮)。
- `--ref` 钉 **40 位 SHA** `523f21baf14a03cadfb5df7002ba7124d70375c2`;
  `git ls-remote origin main` **读三次**(选型时两次 + 发波脚本内第三次带 `exit 9` 漂移检查),三次一致。
- 采购参数 `--slots 16 --hours 2 --rec-slots 8 --games 12`,与 W39–W52 逐字节相同;
  `--az` 逐台轮转 2a/2b/2c/2d。发波前先跑过一次 `--dry-run` 并读了它的 plan 行。
- 自毁:2h 看门狗 + one-time spot 请求 + `instance-initiated-shutdown-behavior=terminate`。

### ⭐⭐⭐ 本轮真正的产出:闸 (i) 在同一轮里赢了两次

**第一次赢的是本台自己手算的钟。** 决策时刻(`06:14:27Z`)工具打
`THROTTLED (exit 3) / margin 438s SHORT / unlock 06:21:45Z`。本台随后在两条无关命令之间
**凭感觉认定"现在应该已经过 06:21 了"**,直接组织了一次发波。
⇒ 发波脚本里的闸 (i) 复读打出 **`now 06:17:15Z / margin 270s SHORT`**,`exit 9` **挡下**,
`ABORT: gate (i) not clear`,**零台机器起飞**。
容器钟当时比本台的估计**晚约 4 分钟**,而本台的估计是**朝越闸方向**错的。

⚠️ **这不是"小心一点就好"的教训,是一条结构**:
- 闸 (i) 的其余三道兄弟闸((ii)(iii)(iv))都读**文件或 API**,任何人都伪造不了;
- **只有闸 (i) 的输入是"现在几点",而这是唯一一个执行者可以在脑子里替工具算出来的量**;
- ⇒ **它是四道闸里唯一一道"手算就能绕过"的**,而绕过它**不会留下任何痕迹**
  ——如果本台当时没把复读写进脚本,这一波就会在 `06:17:15Z` 起飞,
  **报告照样会写「闸 (i) 已跑」(那是真的,06:14 那次),而波是违规的**。
- 与 `pullcad`(promote 一个 id 会静默冻死引用它的 gate)**同族**:门在、读数在、
  **两次读数各自都对**,错的是**"哪一次读数绑定动作"这个约定没被机器执行**。

**第二次赢的是它自己的措辞。** 章程与上一轮交棒都写着「⛔ 不要读那个数,读工具打的那一行」,
而工具自己在 `UNLOCKED` 那一行末尾逐字写着:
`RE-RUN IT AT THE LAUNCH INSTANT: it is the action, not the decision, that the gate binds.`
⇒ 本轮最终的合规发波是**脚本内轮询到 `exit 0` 后立刻发**,
`THROTTLE_AT_ACTION_EXIT=0`、`now 06:21:59Z`、`margin +14s past unlock`。

⇒ **交总监的请求(第六节 ②)**:把「闸 (i) 必须在发波脚本内复读、且非零即 `exit 9`」
从**本台的自觉**升成**硬规则**。本轮它是自觉,而自觉本轮已经失败过一次
(失败的是手算,救回来的是脚本)——**下一个执行者未必把复读写进脚本**。

## 五、四道闸逐条(顺序:(iv) → (i) → (ii) → (iii);(i) 在动作处复读)

- **闸 (iv) `reclaim_blind.py --wave-json W52_wave.json`:`RECLAIM_EXIT=0`**,逐字
  `VERDICT: not blinded -- the wave delivered 4 paired seed(s).` / `NEXT WAVE: spot`。
  四台全 `instance-terminated-by-user`,机时 45.6–55.0 min,`0 machine(s) reclaimed before the flip`。
  ⭐ **这是 `update` 键修好之后第一次干净的 exit 0**(W51 那次因为收割把同一个数据写进了
  `terminated_at` 而返回 `UNDECIDABLE (exit 2)`)。**修复成立,登记结清。**
- **闸 (iv) 前置(无欠收割)**:`aws s3 ls .../soak/` 最新四个前缀是 W52 的 `spot_20260907_0021xx`,
  03:05Z 那轮已 4/4 全收(scored 214)⇒ 无欠收割带进本波。**读的,不是假定的。**
- **闸 (i)**:见上一节。决策 `exit 3` → 手算尝试被 `exit 9` 挡下 → 动作处 `exit 0`。
- **闸 (ii) 新对测试集**:臂串动了(52 → 51,`ckpush` 出集),
  md5 `e61700f4…` 与此前每一波都不同 ⇒ 本树+本集累计 **0 粒种子**,远在 8 粒条款之下。**PASS。**
- **闸 (iii) `wave_fence.py --planned 1.10 --pending 0.99`:`FENCE_EXIT=0 CLEAR`**
  ⇒ `projected $64.132`;`$50.00 crossed (AWS state=ALARM)`、`$80`/`$100` 未跨
  ⇒ fence **`$80.00`**、brake `$90.00`、operative ceiling **`$80.00`**、**headroom `$15.868`**。
  accrual 行逐字:`CERTIFIED ZERO (0 accruing instances account-wide), but --pending $0.990 was
  given and is used as the larger claim.`(**未传 `--no-accrual-check`**)。**本轮未新跨任何告警档。**
  - `--planned 1.10` = GH #454 自 09-06T07:xxZ 起的 spot 常数。**不是 `$0.90`(已作废),
    也不是 `$1.02`(那是 W46 的实测上确界,不是常数)。**
  - `--pending 0.99` = W52 自己的结算价 `$0.9865`。**为什么仍当 pending**:本轮 MTD 与
    03:05Z 那轮**逐位相同且 `refreshed` 戳相同**,是同一张 `02:04:33Z` 快照;W52 约 `01:1xZ` 结束,
    在章程写死的 4.3–11.3h 滞后下**不能确定它在这张快照里**,而**用「MTD 增量对得上波费」去
    推断入账是章程步骤 2(乙)明令禁用的手法**(它已两次给出错误结论)。
    ⇒ 计成 pending。**这可能把 W52 重复计一次,而那个失效方向是安全那一侧。**

## 六、载体门与接线门(本轮项数变了,而且有正当理由)

- `carrier_terms.py` **`CT_EXIT=0`**:51 个 armed id ⇒ **10 hero-scoped / 41 generic / 0 unresolved**
  ⇒ **7 个词项** `crystal_maiden,lion,obsidian_destroyer,pudge,skeleton_king,spirit_breaker,zuus`。
  ⭐ **TERMS 行不再与 W48–W52 逐字节相同:`chaos_knight` 没了。**
  这是 **promote `ckpush` 的正确后果**(它是唯一 chaos_knight-scoped 的 armed id),
  **不是本波的缺陷** —— 也正是上一轮把 `test_carrier_hero_guard.py` 打红的同一个事实(GH #584)。
  **上一轮交棒 ④ 要求"项数变化要在报告里点名",本条即是。**
- `check_armed_wiring.py --cand "<串>"` **`ARM_EXIT=0`**:`all 51 armed ids wired on HEAD`。
  (旗标是 `--cand` 不是 `--arm`;`carrier_terms.py` 恰好相反。上一轮留的这条注**本轮省了一次重试**。)
- 载体门 `seed_draft.py --assert-carrier-from-arm "<串>" 7144 7154 7156 7162`
  **`CARRIER_GATE_EXIT=0`**,`ids=10 seeds=4`。逐词项载体数:
  `crystal_maiden 2 / lion 2 / obsidian_destroyer 2 / pudge 2 / skeleton_king 4 / spirit_breaker 2 / zuus 4`
  —— **每项 ≥ 2**,与 W50/W52 同一条杠。

**种子选取**:`seed_roster_index.py --build` **`ROSTER_EXIT=0`** 先跑(233 粒入册)。
⭐ **本轮多做一步核对**:W52 的四粒(6743/6858/6877/6907)**确实在册**——
**这才是"build 真的吃进了最新一波"的证据**,而不是读到一份陈旧文件。
窗口右移到 **[7101,7500]**(W52 最大 6907),**窗口 ∩ 索引 = 空集**(按集合求交,不是逐粒目视)。
窗口词项 popcount 分布 `{0:39, 1:124, 2:126, 3:91, 4:18, 5:2}`;
在未用种子里按 popcount 取前 60 粒,穷举 4-子集**最大化逐词项载体数的最小值**(并列按总数破)
⇒ **7144 / 7154 / 7156 / 7162**,最小值 2、总数 18。

⚠️ **本轮订正了本台自己的一个数**:第一次数臂串 id 用的是 `tr ',' '\n' | wc -l`,
读出 **50**,与 `test_set.md` 第 4 行自称的 **51** 冲突。
根因是 `wc -l` **数换行不数行**,而该串用 `tr -d '\n'` 剥了尾换行 ⇒ **系统性少一**。
改用 `split(',')` 重数得 **51**,与字节数 460、md5 和第 4 行散文**三方一致**。
⭐ **值得记的是失效方向**:如果本台当时反过来"相信自己的 50、怀疑文件的 51",
就会去追一个不存在的缺陷,或更糟——**在报告里把一个正确的 promote 记成一次串损坏**。
**登记的口径从此是 `split(',')`,不是 `wc -l`。**

## 七、泄漏检查

`LEAK_EXIT=0`。全账号 `running,pending` **恰好是本波这四台** `dota2bot-soak-spot-1`:
- **无第五台、无留存、无降级成按需的台**;
- ⭐ **外来负载连续第二轮不在** —— 09-05/09-06 吃掉账号 headroom 的 `final-table-trainer`
  (`c7a.16xlarge` → `r7a.16xlarge`)本轮 region 内仍不在跑;
- ⛔ **但 GH #515/#528 的立案理由不变**:`dota2bot-batch` 的 `CostFilters=null`,
  **本台的发波围栏仍读着一个它不控制的账号级数字**,那台机器随时可再起,
  而它再起来时**关上的是批测的闸**。

## 八、收割义务与 registry 逐条

- `wave_fence_ruling4_first_live_run`(executor=batch-desk)—— ✅ **本轮再次活跑**
  (第五节闸 (iii) 逐字载着活路径读数与 accrual 行)。**上一轮已请总监退休该行,本轮重复该请求。**
- `campgrade_wave_type_premise_reading`(executor=batch-desk **+ 录像组**)—— ⏳ **OPEN,不归本台关**。
  本台那一半(排波)在 W51 已履行;**读数属录像组,本轮第十轮欠着**。
  该行 trigger 原文仍写「独占波……此刻还不存在」——**它自 W51 起就存在了**,
  上一轮已点名,**本轮仍未改**。
- `p2_pair_arm_stayattr_staysrc`(否决权行)—— **本波未触碰 P2 取证,不触发。**
- W53 自己的四条收割义务已写进 `waves/W53_wave.json:harvest_obligations`
(`update` 键、GH #454 两条腿、铁律 4(i-a) 四量分层、`winrate_channel` 先登记后引用)。

## 九、局数(章程第 7 步)

发波型 ⇒ 报**预登记** `expected_games ≈ 96`(4 粒 × 12 局 × 2 腿)。
⚠️ 该预登记量已连续偏低约 75%(W49 实得 168,W51 214,W52 214),
**两个方向都不许拿它当分母**(既不是"超交付"也不是"少交付")。实得下轮收割时报。

## 十、铁律 6 / 11

- **铁律 6**:`bots/` / `game/` / `tests/` / `tools/` **本会话一行未改**
  (只动 `iterations/` 下的报告、`W53_wave.json`、本章程节,加上 `--build` 写的
  `iterations/data/seed_roster_index.json`)。静态门在 `git push` 时由 `.githooks/pre-push` 自跑,
  读数见本报告末尾追记。动态那半(~100min)**不在钩子里也不被它声称**。
- **铁律 11**:全程**无任何 `requires approval`**,**无空转等待**。
  ⚠️ 本轮有一次**有界等待**(闸 (i) 差 270s → 脚本内 20×30s 有界轮询,实等约 4.5 分钟):
  **那是门在拒绝,不是空转**,且等待写在脚本里、有上界、开门即发。
  本台认为这与"坐等 Approve"是两回事,登记在此供总监复核。

## 十一、交棒

1. ⭐⭐⭐ **总监 —— 请把「闸 (i) 必须在发波脚本内、动作处复读,非零即 `exit 9`」升成硬规则。**
   立案句是本轮的实测:四道闸里**只有 (i) 的输入是"现在几点"**,
   **也只有它能被执行者用手算绕过,且绕过不留痕迹**(报告仍可诚实地写"闸 (i) 已跑")。
   本轮它靠**本台自己把复读写进脚本**才挡住,而**手算那一侧本轮真的错了 4 分钟、且是朝越闸方向错的**。
2. ⭐⭐⭐ **录像组 —— `tools/batch_test/behavioral/zusultstrand_domain.py`(你们 04:27Z `6eaebe38`)
   把 trunk python 打红了**:两个模块级常数 `HP_GATE` / `ULTCASH_HP_GATE` 未登记进 `HP_CENSUS`。
   ⚠️ **这一条与本周前三例不同族:门是对的,漏的是登记这一步**;
   修法是补 `HP_CENSUS` 行(MIRROR / PROXY / INDEPENDENT 三选一),**不是改门**。**GH 见第十二节。**
3. ⭐⭐ **总监 —— GH #454 的第四个测量点下轮到位**;上一轮已报「`$1.10` 是在**机时**上守住的,
   价格已出 `$0.231–0.250` 带 9–19%,乙1 余量约 13%,**而两条腿都不直接看价格**」。
   本波四 AZ 全 spot 零降级,是价格侧的又一个观测机会。
4. ⭐⭐ **总监 —— 请退休 `wave_fence_ruling4_first_live_run` registry 行**(本轮第二次请求,已两轮活跑)。
5. ⭐⭐ **录像组 —— 三笔条件 (a) 仍欠**:`campgrade`(第十轮,语料 119 `.dem` 早在手上)、
   61-id 家族(W49)、`campbind`/`zusboltdom`(W52)。
   **`campgrade` 那行的 trigger 原文「此刻还不存在」自 W51 起已失效**,请总监改词或退休。
6. ⭐ **总监 —— 开工第一条命令的管道坑第十二轮**,且本轮是**已知坑仍复发**
   ⇒ 「靠人记得」本轮被本台自己证伪。请给独有退出码或 wrapper(第三次请求)。
7. ⭐ **总监 —— 协同组 cadence 两处 GAP**(3.6h / 3.7h,门限 3.5h),本台只登记不解读。
8. ⭐ **owner —— `$50` 仍越线**(MTD `$62.042`,`AWS state=ALARM`,`forecast $205.267`);
   外来负载连续两轮已停,但**已花的不退**,且 `CostFilters=null`(GH #515/#528)。
9. ⭐ **存量**:#480/#241 / #490(点名的断言已 ok,关不关归总监)/ #584(已修好)/ #585 /
   #576 / #486 / #472 / #427 / #408 / #420 / #449 / #446 / #290 / `strategy-5b` /
   **#388 的 W34 103 份 `.dem` 2026-09-22 到期(15 天)**。

## 十二、下一轮本台

1. **收割轮**:W53 四粒,`recover_verdict.py` 从 S3 逐局全量重算(`soak/` 前缀,**不是 `validation/`**),
   四个 run 下到**四个分开的子目录**。
2. **先填 `machines[].update`**(SIR `Status.UpdateTime`),**再**跑闸 (iv) —— `reclaim_blind.py`
   读的是 `update` 不是 `terminated_at`。SIR 行 ~1–3h 就老化,**收割轮要第一时间读**。
3. 闸 (i) 下次解锁 **`2026-09-07T12:22:11Z`**(锚点 W53 末台,slate spread 9s)——
   ⛔ **不要读这个数,读 `wave_throttle.py` 打的那一行,且把钟读在动作上。**
   **本轮就是这条规则第一次真的救了一波。**
4. 结算本波成本并对 GH #454 **两条腿**(乙1 `>$1.10`/波;乙2 自终止台机时均值 `>1.00 h/台`)逐条判定。
5. ⚠️ **开工第一条命令:重定向 + 后台 + 不设短 `timeout`**(第十二次)。

## 十三、本轮 token

`TOKENS total_in=6,649,112 out=46,843 turns=59`(到统计时刻为止;统计后的收尾回合不计入)。
