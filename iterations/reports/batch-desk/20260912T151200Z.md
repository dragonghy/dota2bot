# batch-desk 2026-09-12T15:12–15:5xZ — 持有轮:刹车仍在力,零发波、零收割欠、零泄漏

**一句话**:owner 对 GH #779 **尚未表态**,刹车按上一轮的交棒 ① 继续持有 ⇒ **本轮不发波**;
三条独立理由任一单独成立(闸 (iii) `exit 3` 打在 `$90` 刹车上 / 闸 (i) 差 **732s** 未解锁 /
owner 未表态)。收割**零欠**(W69 上一轮已全量收割),泄漏**四条路径全零**。
本轮新增支出 **`$0.01`**(一次 CE 复核,`check_costs.sh` 因 `≥$35` 自动付),**零 EC2**。

⭐ **本轮唯一的新东西是一句零成本的算术,它收窄了 owner 在 #779 上要答的那个问题**:
`$90.058` 越线 **`$0.058`**,而这 `$0.058` **小于 `$1.10` 覆盖档自己的余量** ——
用章程 (甲) **同一条推导线**上的账单侧上确界 `$1.022` 重算,projected 是 **`$89.902`**,
**在刹车之下 `$0.098`**。⛔ **本台不据此发波、不据此改线**(见 §三 的三条边界)。

---

## 〇、上一轮(12:11Z)交棒执行情况

| # | 交棒 | 本轮 |
|---|---|---|
| 1 | ⛔ owner 对 #779 表态前不发任何波;每轮仍做成本现跑 + 泄漏 + 收割若欠 | ✅ **owner 未表态**(#779 零评论)⇒ 未发波;三项全做,见 §一/§二/§四 |
| 2 | ⛔ 上轮的 `$86.758` / cutoff / `pending $2.200` / `projected $90.058` 全部作废,当轮现跑 | ✅ 全部现跑,见 §二;**读数与上轮相同,但原因是快照没刷新不是抄的**(§二 ⚠️) |
| 3 | 闸 (i) 名义解锁 `15:26:31Z`,⛔ 别抄,跑 `wave_throttle.py` | ✅ `THROTTLE_EXIT=3`,`margin : 732s SHORT` |
| 4 | 收割零欠,无待收波次 | ✅ 核过 S3 无新 verdict 前缀,零欠 |
| 5 | ⚠️ 开工第一条命令重定向 + 后台 + 不设短 `timeout`(**第 20 次**) | ⚠️ **第 20 次仍踩**,见 §五 |
| 6 | ⚠️ `origin/main` 的 python/Lua 红不是本台引入 | ✅ 本会话 `bots`/`game`/`tests`/`tools` **一行未改** |

---

## 一、成本(当轮现跑,⛔ 下轮不得抄作前提)

`AWS_SETUP_EXIT=0`(容器无 CLI,脚本自装 `aws-cli/1.46.1`;身份逐字
`arn:aws:iam::581436534924:user/dota2bot-agent`,restricted batch-runner user)。

`COST_EXIT=0`;MTD **`$86.758`**,`budget refreshed 2026-09-12T12:08:45Z`;
`≥$35` 自动 `$0.01` CE 复核 **`86.7576682213`** 逐位一致;
`forecast 209.009` / `limit 100.0` 原样登记,**不据以决策**。

⚠️ **必须点名的一条:快照与上一轮(12:11Z)逐位相同,`LastUpdatedTime` 也逐位相同
(`12:08:45Z`,已 **3.1h** 未刷新)。** ⇒ 本轮的 `$86.758` **不是**「又量了一次得到同一个数」,
是**同一张快照被读了第二次**。⛔ 因此本轮**不做任何 MTD 增量归因**,也不把「MTD 没涨」
读成「没花钱」。

诚实 cutoff = `12:08:45Z − 11.3h` = **`2026-09-12T00:50:45Z`**(闸自己推的,与本台独立一致)⇒
**W68(`03:26:12Z`)与 W69(`09:26:31Z`)两波都还在滞后窗内,一分未落账**。

本轮新增支出 = **CE 复核 `$0.01`,未起任何实例**。

## 二、闸 (iii):刹车仍在力(两个 `--planned` 各跑一次,全部裸读)

**本体(`--pending 2.20 --planned 0`)`FENCE_EXIT=0`**:

```
crossing registry: 0 record(s) at .../director_rulings.json, none in force -- the gate runs at the DERIVED fence.
budget filters   : none  <- so every instance in the account does land in this budget, whoever owns it
accrual scope    : 17 region(s) read ... <- COMPLETE: every region this account has enabled
accrual check    : CERTIFIED ZERO (0 accruing instances account-wide), but --pending $2.200 was given and is used as the larger claim.
  must cover     : 2 wave(s) listed above (W69, W68). This tool does not price them; check your sum covers each.
actual (MTD)     : $86.758   <- re-read this run, never cached
pending waves    : $2.200   (launched, may not be in MTD yet)
projected total  : $88.958
fence            : $100.00   <- lowest ACTUAL alert not yet crossed (Ruling 1)
brake            : $90.00   (owner's line, not derived here)
operative ceiling: $90.00   = min(fence, brake)
headroom         : $1.042 after this wave
```

- **RULING 15 的退休仍然传导正确**:`none in force`,天花板是工具自 derive 的 `$90.00`,
  其下**没有** `DIRECTOR CROSSING:` 行。**未用** `--no-accrual-check`(无 SKIPPED 行)、**未传** `--director-crossing`。
- **一发 spot 波(`--planned 1.10`)⇒ `FENCE_SPOT_EXIT=3`**,逐字:
  `projected total : $90.058` / `A launch at this instant would put MTD past the $90.00 BRAKE line.` /
  `WAVE_FENCE: THROTTLED (exit 3) -- brake, not fence. Stop and report to the owner; do not wait it out.`

⚠️ `--pending 2.20` 仍是本台**手加**的(`= W68 1.10 + W69 1.10`,两波都在 cutoff 之后),
工具逐字只说 `must cover : 2 wave(s) listed above (W69, W68). This tool does not price them` ⇒
**定价这一步至今在操作员脑子里,不在工具里**(GH #742 一族,**本轮只作第四个测量点,不重复开 issue**)。

⚠️ **第五轮同题并排登记**:`budget filters : none`,而 09-08/09-10 两轮第一手量到本月 MTD 里
外来 `c7a.16xlarge` + `r7a.16xlarge` = **`$37.4355` ≈ 47.8%**(只存在于 09-05/09-06 两天,此后一分未涨)
⇒ **咬住本台的这条 `$90` 里有接近一半不是本台的支出**。⛔ 本台不据此主张放宽任何线(GH #721 三条路仍未落定)。

## 三、⭐ 零成本的一句算术:越线量 `$0.058` 落在覆盖档自己的余量之内

**这一条是本轮唯一的新东西,写下来是因为它直接收窄 owner 在 #779 上要答的那个问题。**

`$1.10` 不是一次测量,是章程 (甲) 取的**覆盖档**:它盖住全部账单侧实测点,上确界是
**`$1.022`**(W46,现货紧张),余量 **+7.6%**。把 pending 换成**同一条推导线上的**那个上确界:

| pending 取值 | 来源 | projected(含 `--planned 1.10`) | 对 `$90.00` |
|---|---|---|---|
| `$2.200` | 覆盖档 `$1.10 × 2` | **`$90.058`** | **越线 `$0.058`** |
| `$2.044` | 账单侧上确界 `$1.022 × 2` | **`$89.902`** | **线下 `$0.098`** |

⇒ **「九月的钱用完了」与「九月的覆盖档用完了」在本轮是两个不同的答案**,而它们之间
只差 `$0.156` —— **小于覆盖档自己的余量**。

⛔⛔ **三条边界,一条都不能省**:

1. **本台不据此发波。** 覆盖档就是围栏该用的数,它**故意**朝安全侧失效;
   用一个更便宜的算法去挤过一道红闸,正是 RULING 15 §3 点名禁止的那个动作
   (`do NOT pick a cheaper market to fit under it`)的同构版本。工具逐字 `do not wait it out`。
2. **⛔ 不用机时侧复核。** 本台手上有 W68 `2.9023 h` / W69 `3.4978 h`(SIR 第一手,§四),
   乘以现货价带确实落在 `$0.67–0.87/波`,**但章程 (丙) 明令这条线不得用来复核 (甲)**
   (「两条各自独立登记」;相乘校对 = 章程自己否掉的那种外推)。**故此处只登记机时,不做乘法。**
3. **本台不裁,也不主张 owner 该选哪条。** 上表是**披露**不是建议。

⭐ **能把这件事一次性问死的那个测量已经存在,而且免费/近免费**:一旦预算快照推过
`W69 09:26:31Z + 11.3h ≈ 20:46Z`,两波 spot 就落账,**一次 `$0.01` 的逐日 CE 就给出
两波 spot 的账单侧真值** —— 那正好**同时结清 GH #454 (乙1)**(欠了 12 轮,欠的一直是
账单侧 cutoff 不是测量)。⇒ **下一轮(≥21:00Z)是第一个能拿到它的轮次。**

## 四、GH #454 两条腿 / SIR 机时(登记,不作乘法)

- **(乙1) 第 12 轮欠着,原因仍是账单侧空白不是本台没查**:快照 `12:08:45Z` 未刷新、
  cutoff `00:50:45Z`,W68/W69 均在其后 ⇒ **一分未落账,没有可比的账单侧数字**。
  ⭐ 见 §三 末:**下一轮起它第一次可得**。
- **(乙2) 不触发**(上一轮已第一手结清,本轮不重测):W69 四台**全部** by-user、均值 **`0.8744 h/台`** < `1.00`。
- 机时留档(⛔ 不用于任何定价推导,见 §三 边界 2):W68 **`2.9023 h`**(4 台,其中 `12939`
  `instance-terminated-no-capacity` 仅 `0.3544 h`)、W69 **`3.4978 h`**(4 台全 by-user)。

## 五、收割与泄漏

**收割零欠**:W69 已由上一轮全量收割(4 粒 / 224 局 / `machines[]` 零 null);
S3 `validation/` 与 `soak/` 无本台未收的新前缀。**本轮不重算已收波次**。

**泄漏四条独立路径全零**(⛔ 不合并成一句):

1. `check_costs.sh` 的 `running/pending instances` 区块 **空**;
2. `describe-instances` **不加 tag 过滤**、状态 `pending,running,shutting-down,stopping,stopped` ⇒ **`[]`**;
3. `describe-volumes` ⇒ **`[]`**(零孤儿卷);
4. 闸 (iii) 自带账户级普查 `accrual scope : 17 region(s) read <- COMPLETE` +
   `accrual check : CERTIFIED ZERO (0 accruing instances account-wide)` ⇒ **17 区账户级零**。

常驻仍只有 AMI `ami-0a990a26d89c66547`(`dota2bot-batch-20260718`)。

## 六、闸 (i)(现跑,⛔ 未抄上轮)

`THROTTLE_EXIT=3`,逐字:`anchor wave : W69 (4 machine(s))` / `last machine up : 2026-09-12T09:26:31Z <- the anchor` /
`slate spread : 7s` / `window : 6h` / `unlock : 2026-09-12T15:26:31Z` / `now : 2026-09-12T15:14:19Z` /
`margin : 732s SHORT of unlock` / `A launch at this instant BREACHES gate (i) by 12m12s.`

⇒ **本轮不发波的三条理由互相独立**:闸 (iii) 刹车(§二)、闸 (i) 未解锁(本节)、
owner 对 #779 未表态(上一轮交棒 ①)。⚠️ **闸 (i) 会在 `15:26:31Z` 自己解锁,
另两条不会** —— ⛔ 下一轮**不得**把「闸 (i) 开了」读成「可以发了」。

## 七、自检(铁律 10)

⚠️ **管道坑第 20 次,开工第一条命令又踩** —— 脚本自卫当场拒绝,逐字
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.`
并自己点名 `it has recurred 5x, every time as the first command of the round`。
⭐ **零损失(工具拦住了),但连续 20 轮同一位置** ⇒ **工具拦住了 ≠ 这轮没犯**;
防的是症状不是原因,已连续多轮交棒而形状未变。

改重定向 + 后台重跑,读数见文末「补记」节(⛔ 不预写没读到的退出码)。

⛔ **本会话 `bots`/`game`/`tests`/`tools` 一行未改** ⇒ **无一条 trunk red 可能由本轮引入**;
`origin/main` 的 python/Lua 红族属 GH #624/#705/#751/#774,⛔ **本台不改、不立新案、不开重复 issue**。

## 八、铁律 6 三条腿

见文末「补记」节(push 时由钩子自己打的权威读数,⛔ 不预写)。

## 九、交棒

1. ⭐⭐⭐ **owner —— GH #779 仍零评论,刹车第二轮持有**。九月剩余 **18 天**,
   本台在表态前不发任何波。⭐ **本轮给这道题加了一个零成本的约束(§三)**:
   越线量 `$0.058` **小于** `$1.10` 覆盖档自己的余量,用同一条推导线上的账单侧上确界
   `$1.022` 重算是 `$89.902`(**线下**)⇒ **问题从「九月还有没有钱」收窄成
   「九月是没钱了,还是只是没有覆盖档的钱了」**。⛔ 本台不选、不据此发波(§三 三条边界)。
   并排第二个数仍是:`budget filters : none`,外来机型占本月 MTD **≈47.8%**(GH #721 三条路未落定)。
2. ⭐⭐⭐ **下一轮本台(≥`20:46Z`,实际即下一次触发)—— 去拿那个能问死它的读数**:
   预算快照一旦推过 `W69 + 11.3h ≈ 20:46Z`,花 `$0.01` 拉**逐日 CE**,
   第一次得到**两波 spot 的账单侧真值** ⇒ **同时结清 GH #454 (乙1)**(已欠 12 轮)
   并给交棒 ① 一个实测答案。⛔ 快照没推过就**不要**编,按本轮同样的方式登记「仍欠」。
3. ⭐⭐ **总监 —— GH #742 第四个测量点**(§二):闸逐字
   `This tool does not price them; check your sum covers each` ⇒ **定价这一步仍在操作员脑子里**,
   失效方向是危险的那侧。**不重复开 issue**。
4. ⭐ **闸 (i) 在 `2026-09-12T15:26:31Z` 解锁,⛔ 这不解锁另外两条**(§六)。
   ⛔ 本报告的 `$86.758` / cutoff `00:50:45Z` / `pending $2.200` / `projected $90.058` **全部作废**,
   一律当轮现跑。
5. ⚠️ **自检管道坑第 20 次**,仍是当轮第一条命令(§七);下一轮**第 21 次**。
6. 上一轮交棒 ②③(胜负通道量的是地图不是臂,GH #780)在**总监/录像组**手上,本台无动作。
7. ⭐⭐ **英雄组 —— 一根可能掉在地上的棒**(见「补记 (D2)」):自检 `unlanded` 腿点名
   `1455ce9`(`hero: cmrsolo …`,2026-09-12T14:40:39Z)**推到
   `origin/claude/admiring-hawking-4m5ld4` 但从未上 `origin/main`**。
   ⛔ 本台**不替其 cherry-pick**(作用域不对,且 `bots/` 改动要走它自己的闸),
   ⚠️ 也**不主张它一定掉了**(工具 LIMIT:OFF-TRUNK 可能是已改头换面落地的同一份工作)——
   **只做铁律 10 要求的那一眼并交出去**,请英雄组下一轮自行确认落地或补 push。

---

## Token 用量

见文末补记(收尾时现跑 `tools/agent/token_usage.py`)。

---

## 补记(第一次 push 之后,GH #290 顺序)

### (A) 铁律 6 三条腿(push 时钩子自己打的权威读数,⛔ 非预写)

- `luacheck bots game: 0 warnings` ⇒ **`GATE_EXIT=0  CLEAN (iron rule 6 static half passed)`**
- **`py gate: 96 ran, 0 findings, 0 uncertifiable, 38.2s`**
  (另打 `12 new test(s) not in the manifest were run anyway`,全绿)
- **`lua gate: SKIPPED BY SCOPE -- this push touches no bots/game/tests path.`**
  ⚠️ 第三条腿按钩子自己的措辞登记成**作用域决定** —— 逐字
  `This is a SCOPE decision, not a pass and not a skip of a gate that had something to say (GH #624).`
  ⛔ **不是绿**;钩子自己列出它看到的 2 条路径,均在 `iterations/` 下。
- ⛔ **未用 `RULE6_BYPASS`**,无「SKIPPED, not passed」行;`ARM_EXIT=0`
  (`push gate armed (core.hooksPath=.githooks, already set)`)。
- 动态那半(~100min,GH #124)**不在钩子里,本轮不声称**。

### (B) push:`PUSH1_EXIT=0`;主干被拒两次,均为并发不是门

- `PUSH1_EXIT=0`(session branch `claude/great-sagan-896pdv`,新建)。
- `git push origin HEAD:main` **`PUSH2_EXIT=1`**,逐字 `! [rejected] HEAD -> main (fetch first)`
  ⇒ `git pull --rebase origin main` **`REBASE_EXIT=0`**(`9c920f16..97240360`)。
- 重试仍 **`PUSH3_EXIT=1`**,而这次逐字是
  `! [remote rejected] HEAD -> main (cannot lock ref 'refs/heads/main': is at 739c60f2… but expected 97240360…)`
  ⇒ **main 在本台两次命令之间又被推动了一次**(其他 stream 并发)。
  ⭐ **两次拒绝都不是铁律 6 的门** —— 三条腿每次都跑完且读数相同;登记下来免得下轮把
  `remote rejected` 读成闸红。
- 退避重试第 1 次即成功:**`PUSH_EXIT=0`**,`739c60f2..d7052557  HEAD -> main`。

### (C) GitHub(⛔ 按 GH #290 **push 之后**才发)

草稿过 `claim_precheck.sh` ⇒ **`PRECHECK_EXIT=0`**,逐字
`local commits not on origin/main: 0` / `clean` /
`OK to publish: every citation resolves on origin/main.`
(`paths cited 1  resolved on trunk 1  refused 0`;⭐ 与上一轮 `issue_brake.md` 那张
「零引用也落到 OK」的读数不同,本轮**确有 1 条引用且真的解析过**。)

- **GH #779 追评**(不另开 issue):刹车第二轮持有 + §三 那句零成本算术 + 三条边界
  + 「下一轮起可得账单侧真值并结清 GH #454 (乙1)」。
- ⛔ **本轮零新开 issue**:GH #742(定价不在工具里)、GH #721(外来支出)、
  GH #352/#780(胜负通道)、GH #624/#705/#751/#774(trunk red)本轮**只作读数登记**,
  不重复立案。

### (D) 自检(铁律 10)真码 —— ⭐ 跑完了,取代本报告 §七 的「未跑完」

⭐ **后台那次在收尾前跑完了**:**`SELFCHECK_EXIT=3`**(真码 —— 命令是
`… > /tmp/sc.log 2>&1; echo "SELFCHECK_EXIT=$?"`,**重定向后读的是自检自己的码**;
⚠️ harness 报的 `exit code 0` 是**读者的**码,正是本轮第 20 次踩到那条管道纪律的另一面)。
⇒ **本节取代 §七 与本报告初稿中「本轮无 `SELFCHECK_EXIT=`」那句**(初稿在它跑完前落笔)。

逐字:`legs run : 12` / **`UNCERTIFIABLE (exit 2): none`** /
`FINDINGS (exit 3) : unlanded cadence owed-executions trunk-red(python) trunk-red(lua)` /
`NOT RUN (inside a leg): tests/test_lua_gate.py tests/test_luacheck_gate_soakswitch.py tests/test_selfcheck_lua_leg.py`
(**腿内三条没跑成,⛔ 不是通过**)。⭐ **12 条腿全部跑到,没有「没人看过」的一侧**。

**trunk 红两侧**(族属 GH #624/#705/#751/#774,⛔ **本台不改、不立新案、不开重复 issue**):
- python 腿:`TRUNK RED -- a python test is failing ON THE WORKING TREE.`
- Lua 快腿:**`RED test_stayfield2_marginal_domain.lua`**,逐字
  `TRUNK RED -- 1 of 87 Lua detector file(s) failing ON THE WORKING TREE.`
  ⇒ **与上一轮同一条**(census 断言被别组正当改动顶红的老形状)。

⚠️ 照抄该腿自己的限定:`Whether main is red too is NOT established by this line: re-run after 'git stash'.`
⛔ 本轮**未**用 `git stash` 独立复验 main。**但本会话对仓库的唯一改动是 `iterations/` 下的
报告与章程**(`bots`/`game`/`tests`/`tools` **一行未改**)⇒ **无一条红可能由本轮引入**;
`相关不是归因`,交归属方。
⭐ 对照登记:**push 钩子三条腿全绿放行**,而自检在同一棵树上读到**两侧**红
⇒ **GH #774 又一个直接证据**(红都落在钩子快域之外)。

### (D2) ⭐ `unlanded` 这条**不是本台的**,是掉在地上的一根棒(铁律 10 要看的那一眼)

`unlanded_commits.py` 逐字报 **`UNLANDED WORK (1 commit(s)) -- committed and pushed, never on origin/main:`**

```
2026-09-12T14:40:39+00:00  1455ce9  hero: cmrsolo -- the ally term Crystal Maiden's Freezing Field branch 1 never had
    on origin/claude/admiring-hawking-4m5ld4   ->  git cherry-pick 1455ce9
  of which CLAIMS-LANDED : 0 (see LIMIT 4 -- text match, judge the quoted line)
```

⇒ **英雄组 2026-09-12T14:40Z 的那份工作推到了分支但没上 `origin/main`**
(`commits examined : 1`、`refs scanned : 1103`、`shallow clone : YES`,
`REFUSED below 2026-09-12T02:19:22+00:00 (7eb89ff)` ⇒ **该窗口之下不可判,不是「没有更多」**)。
⛔ **本台不替英雄组 cherry-pick**(不是本台的作用域,且 `bots/` 改动要走它自己的闸),
⚠️ 也**不主张它一定掉了** —— 工具自己的 LIMIT 说 OFF-TRUNK 可能是**已改头换面落地的同一份工作**。
**本台只做铁律 10 要求的那一眼并把它交出去**(见 §九 交棒 ⑦)。
⭐ 立这条的现场正是 08-22 hero 那次:**检测器点名点得到,没人跑 ⇒ 两轮从零重做**。

### (E) Token 用量

`TOKENS total_in=3,813,977 out=29,871 turns=32`

⚠️ 这是**统计时刻为止**的数字,其后的收尾回合不计入。
⭐ 本轮是**持有轮**(零发波、零收割、零下载),用量约为上一轮收割轮
(`total_in=10,012,894`)的 **38%** —— 量级与工作单元相称,无空转。
