# batch-desk 2026-09-12T09:06–09:3xZ(发波轮 W69 —— 27-id 家族第一波)

**⭐⭐⭐ 头号产出不是那四台机器,是闸 (iv) 本轮开局 `exit 2` 完全跑不动 —— 而堵住它的不是 AWS、不是代码,是上一轮收割把四台机器的读数只写进了散文和 `W68_harvest.json`,没写回 `reclaim_blind.py` 真正去读的那个结构。**
**⭐⭐ 第二:SIR 记录本轮已经老化掉(`describe-spot-instance-requests` 返回 `[]`),把 GH #375 的窗口夹成 `>2h32m` 且 `≤5h50m` —— 上一轮那条"一个都没老化掉"的反向观测,本轮拿到了另一端。**

---

## 〇、定位

上一轮(06:16Z 收割轮)交棒四条:① 闸 (i) 名义解锁 `09:26:12Z`,⛔ 别抄、现跑、并按 GH #469 在发波脚本内部再跑一次;② 发波前先算围栏,余量只剩 `$2.805`;③ 若发波,臂串在 `origin/main` 上现取并重算 md5 —— **29-id 家族已深度 1,闸 (ii) 不再能靠"成员串变了"过第一析取支**;④ 池化时 cand 传完整臂串。

本轮四条全部照办。其中 ③ 的**前提在本轮被事实推翻**(见 §三):成员串**又变了**,闸 (ii) 因此仍然过在第一析取支。

---

## 一、成本(当轮现跑,⛔ 未抄上一轮任何数)

- `AWS_SETUP_EXIT=0` —— `AWS ready: arn:aws:iam::581436534924:user/dota2bot-agent`(受限 IAM,正确)
- `COST_EXIT=0`
- MTD **`$83.945`**,`budget refreshed 2026-09-12T05:22:16Z`,`forecast 209.009` / `budget limit 100.0`
- `≥$35` 自动 `$0.01` CE 复核:**`83.9453960689`** 逐位一致

⚠️ **快照与上一轮(06:16Z)逐位相同 —— 戳、MTD 两个量都没动**。⇒ 中间隔着的 W68 整波仍**一分钱未落账**。诚实 cutoff = `05:22:16Z − 11.3h` = **`2026-09-11T18:04:16Z`**,W67(`21:25:21Z`)与 W68(`03:26:12Z`)**都在它之后**。
⛔ **未用「MTD 增量对得上波费」反推**(步骤 2 乙禁用);⛔ 本轮未额外买 CE 读数(除 `check_costs.sh` 自动那 `$0.01`)。

`$50` / `$80` 两条 `ACTUAL` 告警仍越(`AWS state=ALARM`);预算过滤器仍是 `null`(`budget filters : none`,一字未变)。

---

## 二、四道闸全部当轮现跑

### 闸 (i) throttle —— 本轮**三次**读数,拒绝了两次,放行了一次

| 时刻 | 读数 | 用途 |
|---|---|---|
| 决策时刻 1 | `THROTTLE_EXIT=3`,逐字 `margin : 758s SHORT of unlock` | 不判零发波,改为挂起 |
| 决策时刻 2 | `THROTTLE_EXIT=3`,逐字 `margin : 474s SHORT of unlock` | 同上 |
| **动作时刻**(发波脚本内部) | 见 §四 `THROTTLE_EXIT_AT_LAUNCH` | GH #469:闸绑的是**动作**不是决定 |

⭐ **本轮 GH #469 是实打实用上的,而且它真的拦了一次**:第一次跑 `launch_w69.sh` 时脚本内部的闸自己打出 `margin : 357s SHORT of unlock` 并 **`ABORT: gate (i) does not pass at the launch instant. Nothing launched.`(脚本 exit 3)** —— 即**发波脚本自己拒绝了本台**。锚 = W68 末台 `2026-09-12T03:26:12Z` → 解锁 `09:26:12Z`。

### 闸 (ii) new-to-test —— 过在**第一析取支**,上一轮的预判被事实推翻

- 腿 1 **臂串变了**:W68 `33047ce53c029f3901e5f63eab872ecb`(**29 id / 257 字节**)→ 本波 **`76a888b622124fc5488503aa36ef6b25`(27 id / 239 字节)**
- 变动来源:`test_set.md` 第 3 行逐字 **「两条 `RETURNED`(退集,29 → 27,`outlatch` + `rotscope`)」**,总监裁定 §HA
- ⭐ **字节数独立佐证,不靠读那句话**:`239 + len("outlatch,") 9 + len("rotscope,") 9 = 257` = W68 的 `arm_bytes` **逐位相等**

⚠️ **一个单位陷阱,本轮当场踩到并纠正**:`test_set.md` 登记的是 **239 字节 / `76a888b6…`**,而 `sed -n '2p'` 带出的尾换行给出 **240 字节 / `b8227a7f…`**。两个 md5 都"对",但只有**去掉尾换行那个**与登记口径同量。本报告与 `W69_wave.json` 一律登记 **239 / `76a888b6…`**。

⇒ **W69 = 27-id 家族,深度 0。⛔ 不与 W68(29-id)、更不与 W66+W67(30-id)并池。**

### 闸 (iii) fence —— `FENCE_EXIT=0`

⚠️ **首跑 `FENCE_EXIT=2`(`UNCERTIFIABLE`)**,逐字 `nothing is accruing at this instant, but 2 wave(s) above launched inside the 11.3h ActualSpend lag, so pending=$0.000 is NOT certifiable (Ruling 6)`。⇒ 按工具自己的指示补 `--pending`(本台自算:W68 spot `$1.10` + W67 按需 `$2.15` = **`$3.25`**),复跑取 `0`。⛔ **exit 2 没有被读成通过。**

含本波 `--planned 1.10` 的终读数,逐字:

```
actual (MTD)     : $83.945   <- re-read this run, never cached
pending waves    : $3.250   (launched, may not be in MTD yet)
planned          : $1.100
projected total  : $88.295
fence            : $100.00   <- lowest ACTUAL alert not yet crossed (Ruling 1)
brake            : $90.00   (owner's line, not derived here)
operative ceiling: $90.00   = min(fence, brake)
headroom         : $1.705 after this wave
WAVE_FENCE: CLEAR (exit 0)
```

`accrual scope : 17 region(s) read` / `COMPLETE: every region this account has enabled`。
钟表免责声明逐字转录:`Do not copy $100.00 into a report as next month's number; re-run the tool.`

### 闸 (iv) reclaim-blind —— 见 §三(本轮的头号产出)

最终 `BLIND_EXIT=0`,逐字 `VERDICT: not blinded -- the wave delivered 3 paired seed(s).` / **`NEXT WAVE: spot (the default; GH #158 unchanged).`**

### 额外两道(非四闸,但都是发波前门)

- **接线** `WIRING_EXIT=0`,逐字 **`all 27 armed ids wired on 52ea2be45696c7d547ea00754ece1e32c0090061`** —— ⭐ 跑在**远端 tip** 上,不是工作树
- **carrier 闸** `CARRIER_GATE_EXIT=0`,逐字 `CARRIER_GATE ids=5 seeds=4 exit=0`(详见 §五)

---

## 三、⭐⭐⭐ 闸 (iv) 开局 `exit 2`:上一轮把读数交在了散文里,没交在结构里

**现场**:本轮第一次跑 `reclaim_blind.py --wave-json waves/W68_wave.json` 拿回

```
BLIND_EXIT=2
UNDECIDABLE: seed 12924: missing leg count 'ab'
```

**成因不是数据不存在**。上一轮(06:16Z)**确实读到了全部四个 SIR `Status.Code` 和全部机时**,并把它们写进了报告正文和 `W68_harvest.json:gh_454_obligations` —— 但 `W68_wave.json:machines[]` 是**发波时**写的,四台的 `ab`/`ba`/`arm_depth`/`status_code` 全是 `null` 或缺失,而 `reclaim_blind.py` 读的正是这个结构。⇒ **人读得到、闸读不到。**

⚠️ 这不是新做法的缺失,而是**已确立做法的一次漏做**:交棒历史里 W63 那轮明确自夸过「闸 (iv) 输入本轮已备好(`W63_wave.json` 零 null,四台 `status_code` 齐全)」—— **回填本来就是收割轮的活**。

**本轮处置**(⛔ 本台不改 bot 代码、不改闸):把上一轮**第一手读到**的数回填进 `W68_wave.json:machines[]`,并逐条标注来源与成色 ——

| seed | ab/ba | arm_depth | status_code | 机时 → survival_min |
|---|---|---|---|---|
| 12924 | 32/17 | 22.20 | `instance-terminated-by-user` | 0.8731h → 52.39 |
| 12888 | 34/14 | 19.83 | `instance-terminated-by-user` | 0.8381h → 50.29 |
| 12542 | 26/13 | 17.33 | `instance-terminated-by-user` | 0.8367h → 50.20 |
| 12939 | 8/0 | 0.00 | `instance-terminated-no-capacity` | 0.3544h → 21.27 |

- `ab`/`ba`/`arm_depth` 取 `W68_verdict.json:per_seed` 的 `ab_games`/`ba_games`/`arm_depth` **逐字**
- `status_code` / 机时 **标成 `BACKFILLED … NOT re-read first-hand`**(理由见 §六),来源指向 `W68_harvest.json`

**回填后两次复跑,两个都登记**:

1. 只补 `ab`/`ba`/`status_code` ⇒ `BLIND_EXIT=0`,但 `unread : 4 of 4 machine(s)`、`attribution: 0..1 machine(s) reclaimed before the flip -- a RANGE`。工具自己声明该洞 `immaterial : all 16 extension point(s) agree` ⇒ **结论不架在未读字段上**。
2. 再补 `survival_min` ⇒ `BLIND_EXIT=0` 且 **`attribution: 1 machine(s) reclaimed before the flip`(确定值,不再是区间)**,四台 survival 全部读出(52.4 / 50.3 / 21.3 / 50.2 min,changeover 40.0 min)。

⭐ **第 2 步与独立记录对上**:`W68_harvest.json:spot_reclaim` 记 12939 `03:26:10Z -> 03:47:26Z = 21m16s`,与回填值 **21.27 min** 同一个数;它 `< 40.0 min` 的 changeover ⇒ 那台**确实在翻腿之前**就被回收,这正是它 `ba 0` 的机制解释。

⚠️ **成色边界**:`survival_min` 由 SIR `Status.UpdateTime − LaunchTime` 得来,`UpdateTime` 是 SIR 关闭戳、略晚于终止 ⇒ **偏大**,失效方向朝「活过了翻腿」那侧,不朝「早被回收」那侧。

---

## 四、W69 发波

`DRYRUN_EXIT=0`,波次预算闸**未拒发** ⇒ ⛔ **未用 `--allow-short-watchdog`**。
四次 `spot_run.sh --count 1 --hours 2 --validate "<27-id 串> <seed> --games 12"`。
`LAUNCH_SCRIPT_EXIT=0`。

**动作时刻的闸 (i)**(脚本内部,GH #469):`THROTTLE_EXIT_AT_LAUNCH=0`,逐字 `now : 2026-09-12T09:26:21Z` / **`margin : +9s past unlock`**。

**树从远端读**:`REF_PIN=52ea2be45696c7d547ea00754ece1e32c0090061`,由发波脚本内部现跑 `git ls-remote origin main` 得到;脚本随后**把远端 tip 上的第 2 行与已过闸的臂串逐字节比对**,逐字 `arm string on the remote tip matches the gated string (239 bytes)` —— 若两者不等脚本 `exit 4` 不发波。⇒ **实例 clone 到的树与过闸的树是同一棵,这是断言不是假设。**

⛔ **成败不从退出码断言**(GH #158):

| seed | instance | AZ | LaunchTime | `InstanceLifecycle` | `State.Name` | shutdown(逐台单查) |
|---|---|---|---|---|---|---|
| 13019 | `i-023af1cbf9c998329` | us-west-2c | 09:26:24Z | `spot` | `running` | `terminate` |
| 13027 | `i-0a802c6f7687025dd` | us-west-2c | 09:26:26Z | `spot` | `running` | `terminate` |
| 13034 | `i-07eaed0571a85d81d` | us-west-2b | 09:26:29Z | `spot` | `running` | `terminate` |
| 13052 | `i-0594672d686a7556f` | us-west-2d | 09:26:31Z | `spot` | `running` | `terminate` |

- **slate spread 7s**(09:26:24Z → 09:26:31Z),与 W67/W68 并列最紧
- **AZ 分散 3/4** —— 2c 抽中两次,2a 一次未中(az 环随机起点,GH #252)
- `InstanceLifecycle` 是 `describe-instances` 读来的,⛔ **不看 run_id 的 `spot_` 前缀**

### ⚠️ 本轮自己弄丢了一个读数:四个 `run_id`

发波脚本把每次 `spot_run.sh` 的输出**接了 `tail -6`**,而 `run_id=...` 正好在被截掉的那几行里 ⇒ `grep -c run_id /tmp/w69_launch.log` = **0**。

**这一条记在本台头上,不记在工具头上**,而且它与章程记了 17 次的那个 stdout 管道坑**是同一族**:**一个显示用的过滤器毁掉了一个读数**。⭐ 正确做法是 `tee` 而不是 `tail`。

**没有东西不可恢复**(这一句是核过的,不是安慰):四个 run 前缀会在机器首次上传后出现在 `s3://…/soak/spot_20260912_0926*_1_main_*/`(09:2xZ 查时还没有,首局未完之前本就应该没有),而 **seed↔run 的对应关系独立可从 `analysis.json:script_version` 复原**(它把 seed 盖在里面)。⇒ 收割轮照常可做,代价是多一次 S3 列举。

`cand_name` 同理本轮未读,收割轮从 `script_version` 现取 —— ⛔ 这正好也是上一轮交棒第 4 条点名的那个量(池化时 cand 传**完整臂串**,不是 `cand_name`)。

---

## 五、种子与 carrier 闸

`carrier_terms.py --arm <239字节串>` ⇒ `CARRIER_TERMS derived from 27 armed ids: 5 hero-scoped, 22 generic, 0 unresolved => 5 term(s)`,逐字 `TERMS crystal_maiden,lion,obsidian_destroyer,skeleton_king,spirit_breaker`。

五个 hero-scoped id 与其载体:`odaoe→obsidian_destroyer` / `aimguard→spirit_breaker` / `wkqdmg→skeleton_king` / `lionqdmg→lion` / `cmqreach→crystal_maiden`。

**选种不是随手取的**:在 `13000–13299` 上用 `seed_draft.py` 的 `position_map` 枚举,先求**能覆盖全部 5 个载体**的四粒组合,再在其中**最大化载体冗余**(载体槽位总数)。

- 首个可行解 `[13000, 13001, 13002, 13034]` —— 覆盖全但只有 **6** 个载体槽
- **采用解 `[13019, 13027, 13034, 13052]` —— 13 个载体槽**,冗余高一倍有余

| seed | 覆盖的载体 |
|---|---|
| 13019 | crystal_maiden, lion, skeleton_king |
| 13027 | obsidian_destroyer, skeleton_king, spirit_breaker |
| 13034 | crystal_maiden, lion, spirit_breaker |
| 13052 | crystal_maiden, lion, obsidian_destroyer, skeleton_king |

官方闸复核:`seed_draft.py 13019 13027 13034 13052 --assert-carrier-from-arm <串>` ⇒ **`CARRIER_GATE ids=5 seeds=4 exit=0`**,五个 term 无一 `ABSENT`。

⚠️ 诚实边界:`--find`/`position_map` 钉的是**哪 10 个英雄、哪一边、什么位置**,**不钉 pick slot**(`seed_draft.py` 文件头自述,GH #57)。

---

## 六、⭐⭐ 与 GH #375 相反方向的另一端:SIR 本轮**已经**老化掉了

上一轮交棒记了一条与 GH #375 相反的观测:四个 `Status.Code` 在**终止后 2h01m–2h32m** 仍读得到,而 #375 记的是「~1h 内可读」。

**本轮把另一端量到了**:`awsx ec2 describe-spot-instance-requests`(不加任何过滤)返回 **`[]`**,`SIR_EXIT=0`。W68 四台终止于 `03:47Z–04:16Z` 一带,本轮读取时刻 `09:1xZ` ⇒ 距终止约 **5h–5h30m**,**四条 SIR 全部消失**。

⇒ 两端合起来把窗口夹成:**`> 2h32m`(上一轮实测仍在)且 `≤ 约 5h30m`(本轮实测已无)**。⛔ **本台不改 GH #375 的常数**,只交这一对读数给它的归属方;⭐ 但它对本台有一条直接的操作含义,已写进交棒:**`status_code` 的回填必须由收割轮当轮做完,拖到下一轮就只剩转抄,不再是第一手**。

这也正是 §三 里那些值只能标 `BACKFILLED … NOT re-read first-hand` 的原因 —— 本轮**想**第一手复核,`[]` 说明已经来不及了。

---

## 七、泄漏检查

- **发射前**:`describe-instances`(`instance-state-name` ∈ pending/running/stopping/stopped,**不加 tag 过滤**)⇒ `[]`;`describe-volumes` ⇒ `[]`
- **发射后**:`describe-instances`(同样不加 tag 过滤)⇒ **恰好本轮有意启动的 4 台**,`life` 全 `spot`、`state` 全 `running`,**无一多余**
- **卷**:发射前 `[]`;发射后 `4`,逐卷查 ⇒ 四卷全 `in-use` 且**各自挂在那 4 台里的一台上**(`vol-00a4f92ed86c26e5f→i-023af…` / `vol-0ae66ad4938d62e9a→i-0a802…` / `vol-08b20fcc8b848fa35→i-07eae…` / `vol-03fe156465c56a7f2→i-05946…`)⇒ **零孤儿卷**。⭐ 只数个数会把"4 个卷"读成可疑,逐卷查挂载才说明它们就是那四台的根卷
- 常驻仅 AMI `ami-0a990a26d89c66547`(`dota2bot-batch-20260718`)
- ⭐ 按上一轮交棒直接用 `describe-instances`,**未**为泄漏块重跑 `check_costs.sh`,省下那 `$0.01`

---

## 八、自检 —— ⚠️ 未跑完,**这不是通过**

**⭐ 管道坑第 18 次,本台开工第一条命令又踩了** —— 接了 `| tail -60`,脚本**自己当场拒绝**:

```
REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.
REFUSED: stdout is a PIPE, so the exit code your harness reports is
         the READER's (tail/head almost always succeed), not this script's.
         This is evidence discipline 3; it has recurred 5x,
         every time as the first command of the round.
```

⭐ **值得单独记一笔:上一轮交棒第 6 条逐字预警过这件事,本台照样踩** —— 但**这一次它没有造成任何损失**,因为脚本里那道自卫把它当场变成了一条明确的拒绝,而不是一个看起来像 0 的假绿。**这就是"那条交棒需要一个跑得起来的东西挡着"的验证**:交棒没挡住,脚本挡住了。

改用重定向 + 后台重跑(⛔ 未设短 `timeout` —— 上一轮记的 900s 跑不完)。**收尾时仍卡在 `=== trunk health (fast Lua detectors) ===`**(已跑约 25 min)⇒ **Lua 检测器那一侧本轮没人看过**,与 `exit 2` 同义,⛔ **不得据此推断那一侧是绿的**。

**跑完并读到的腿**:

- anchors:**`8 anchor(s) checked -- OK`**(stable-v1…v8 各 `EXISTS`/`PINNED`/`SHIPPED` ok)
- promote-time constraints:`scanned: 208 distinct live gate id(s), 19 id(s) carrying a PROMOTED note` / **`FROZEN none`**(⇒ `pullcad` 陷阱为零)/ `atoms registered: 5` 五条全 ok / `promote-atom constraints: OK`
- inverse gate census:`live gate ids 203 armed 27` ⇒ ⭐ **`armed 27` 与本轮臂串逐位一致,是对闸 (ii) 的独立佐证**;`FROZEN 0 / COUPLED 1 / UNRESOLVED(armed) 0`,COUPLED 仍是 `fieldsip` 挂在六个 `buy*`/`stayfield*` 下;`no armed id hangs under an unarmed gate -- OK`
- python 腿:**`TRUNK RED`** —— 见 §十

---

## 十、⚠️⚠️ `origin/main` 上现在红着**四条** python 测试(上一轮是三条)

自检的 python 腿打 `TRUNK RED`,并自己声明**那一行不足以判断 main**(逐字 `Whether main is red too is NOT established by this line: re-run after 'git stash'`)。

⇒ **本台照它说的做了**:`git stash -u` 到干净的 `52ea2be4`(`git status --porcelain` = **0 行**),逐个复跑,**四条全部复现,退出码全 1**:

| 测试 | 失败逐字 | 归属 |
|---|---|---|
| `tests/test_stale_waits.py` | `FAIL: member string parsed to only 27 ids -- line 2 looks truncated`(`49 checks, 1 failed`) | **GH #765 已立**(本轮是它的第二个数据点) |
| `tests/test_bots_walk_farm_only.py` | `FAIL: a tests/ io.popen command cannot be resolved statically and is not on the hand-read list …`(`8 checks, 1 failed [245 commands executed, 49 unresolved]`) | ⭐ **新,本轮首见 ⇒ 本轮新开 issue** |
| `tests/test_carrier_terms.py` | `FAIL zusult -> zuus` / `FAIL liondrainstop -> lion` 等 5 条 | GH #765 线索节,属英雄组 |
| `tests/test_detector_source_constants.py` | 同族(`zusult` / `liondrainstop` 口径) | 同上 |

⭐ **`test_stale_waits.py` 这一条本轮把 GH #765 的诊断又证实了一次,而且是更强的一次**:上一轮它读出 `29`(被 `tpcommit` promote 顶红),本轮读出 **`27`**(被 `outlatch`+`rotscope` 两条退集顶红)⇒ **同一条硬编码 `30` 的断言,被两种不同的合法成员串变动各顶红一次**。⛔ 促使红的三次变动(1 次 promote + 2 次 RETURNED)**全部是正确的**。

⭐⭐ **新的那条是 GH #624 立案形状的第六例,而它的触发源是"别的组加了测试文件"**:四个新 `tests/*.lua` 用了 `io.popen('ls ' .. dir .. ' 2>/dev/null')` 而没进 `UNRESOLVED_HAND_READ` ——
`test_tpdefall_tower_commit_quantifier.lua` / `test_tpdefnan_tower_tp_landing.lua` / `test_wk_q_commit_ration.lua` / `test_zuus_arc_retreat_immunity.lua`
(后两个对得上 `1ad7faa hero: wkqcommit …` 那条 commit)。⇒ **落测试的那一轮钩子对它全绿**(这条测试同样在 py 清单里、在钩子快域外),红留给下一个开工的组 —— 本轮就是那个组。

⛔ **本台一条都不修**:章程明写本台不写 bot 代码、不做判断分析;而这四条的正确修法(硬编码现读 / 把四个文件加进手读名单 / `zusult`+`liondrainstop` 的 hero-scoped 口径)分别归**总监**与**英雄组**的登记口径。
⭐ **本轮发波未受影响**:臂串是从 `origin/main` **现读**的,读到的就是 27 id,且 `WIRING_EXIT=0` 在同一个 ref 上跑过。

---

## 十一、⚠️ 给总监(第三轮同题点名)

本波之后围栏余量 **`$1.705`**,而九月还剩 **18 天**。⇒ **本月大概率发不出第二波**(spot `$1.10` 已经吃不下,除非 MTD 快照下修)。

⛔ 本台**不自行改刹车线、不自行降级**,第三次只摆两条事实:

1. `brake $90` 是 owner 的线,`fence $100` 是派生的,闸取 `min` ⇒ 真正咬住本台的是 `$90`
2. 预算过滤器仍是 **`null`**(本轮 `wave_fence.py` 逐字 `budget filters : none <- so every instance in the account does land in this budget, whoever owns it`),而 09-08 那轮已量到本月 MTD 里 **55% 不是 dota2bot 花的**

⇒ **咬住本台的这条线有一半以上不是本台的支出**,而这是连续第三轮报同一件事(前两轮见章程 09-12T03:13Z §十一、06:16Z §十)。GH #721 三条路仍未落定。

---

## 十二、GitHub

本轮**开一条** issue:**GH #774** `[harness]` —— `tests/test_bots_walk_farm_only.py` 在 `origin/main` 上红,四个新 `tests/*.lua` 的 `io.popen` 未进手读名单(GH #624 第六例)。
⛔ 按 GH #290 的**顺序**规定,它在 `git push` **之后**才发表,草稿先过 `claim_precheck.sh`:`PRECHECK_EXIT=0`,逐字 `local commits not on origin/main: 0` / `paths cited 14 … resolved on trunk 12  refused 0` / `OK to publish: every citation resolves on origin/main.`。
GH #765 本轮**不新开也不重复开**,只在 §十 里给它记第二个数据点(`27` ids);`zusult`/`liondrainstop` 两条属英雄组,不在本台立案范围。
GH #375 的窗口两端读数(§六)**不开新 issue** —— 它是对既有 issue 的读数登记,不是铁律 5 意义上的新问题。

---

## 十三、闸与门的读数汇总(便于下一轮核对)

| 项 | 读数 |
|---|---|
| `AWS_SETUP_EXIT` | `0` |
| `COST_EXIT` | `0` |
| MTD | `$83.945`(快照 `05:22:16Z`,**与上一轮逐位相同**) |
| `THROTTLE_EXIT`(决策 ×2) | `3`(`758s SHORT`)、`3`(`474s SHORT`) |
| `THROTTLE_EXIT`(脚本内首次尝试) | `3`(`357s SHORT`)⇒ 脚本 `ABORT`,**未发波** |
| `THROTTLE_EXIT_AT_LAUNCH` | **`0`**(`+9s past unlock`) |
| 闸 (ii) | 过在**腿 1**:27 id / 239 字节 / `76a888b622124fc5488503aa36ef6b25` |
| `FENCE_EXIT` | 首跑 `2`(`UNCERTIFIABLE`)→ 补 `--pending` 后 **`0`**;`headroom $1.705` |
| `BLIND_EXIT` | 首跑 `2`(`UNDECIDABLE`)→ 回填后 **`0`**;`not blinded` / `NEXT WAVE: spot` |
| `WIRING_EXIT` | `0`(`all 27 armed ids wired on 52ea2be4…`) |
| `CARRIER_GATE_EXIT` | `0`(`ids=5 seeds=4 exit=0`) |
| `DRYRUN_EXIT` | `0`(未拒发 ⇒ 未用 `--allow-short-watchdog`) |
| `LAUNCH_SCRIPT_EXIT` | `0`,四台全 `spot`/`running`/`terminate` |
| 自检 | ⚠️ **未跑完**(卡在 fast Lua detectors)= 不是通过 |
| 泄漏 | 发射前 `[]`/`[]`;发射后 4 台(全有意)+ 4 卷(全 `in-use` 且各挂其一)⇒ 零泄漏 |
| 铁律 6 三条腿 | 见 §十四 |

---

## 十四、铁律 6 三条腿(**push 时钩子自己打的权威读数**,两次 push 各一遍)

⛔ **未用 `RULE6_BYPASS`**,两次 push 全部 `exit 0` 放行:

```
luacheck bots game: 0 warnings
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 96 ran, 0 findings, 0 uncertifiable, 30.9s   (第二次 31.0s)
lua gate: SKIPPED BY SCOPE -- this push touches no bots/game/tests path.
  This is a SCOPE decision, not a pass and not a skip of a gate that had something to say (GH #624).
```

⭐ **第三条腿本轮是 `SKIPPED BY SCOPE`,本报告据钩子自己的措辞把它登记成「作用域决定」,不是通过,也不是「一条本来有话说的闸被跳过」** —— 本轮改动只有 `iterations/` 下的报告/波次/章程三个文件,一行 `bots`/`game`/`tests` 未动。
⚠️ py 腿另打一行:`12 new test(s) not in the manifest were run anyway`(清单外新测试被顺带跑了,全绿)。
⚠️ 动态那半(`lua5.1 tests/run_tests.lua`,~100 min,GH #124)**不在钩子里,本报告不声称它**。

⭐⭐ **这四行读数同时是 GH #774 的直接证据**:钩子三条腿在 `8f89522d` 上**全绿放行**,而 `origin/main` 当时**正红着四条 python 测试**(§十)—— 推的人的闸对它们无话可说,这正是 GH #624 的立案形状。

**push 结果**:`git push -u origin claude/great-sagan-mun6nv` ⇒ `PUSH1_EXIT=0`(new branch);`git push origin HEAD:main` ⇒ `PUSH2_EXIT=0`,`52ea2be4..8f89522d  HEAD -> main`。

---

## 交棒(下一轮本台)

1. ⭐ **收割 W69**(seeds `13019`/`13027`/`13034`/`13052`,四台 instance id 见 §四)。
   ⛔ **先按 run 分子目录下载再带前缀合并**(GH #225);⛔ **cand 传完整 27-id 臂串,不是 `cand_name`**(上一轮那个静默空读数)。
   ⚠️ **四个 `run_id` 本轮弄丢了**(§四),从 `s3 ls s3://dota2bot-batch-results-4924/soak/ | grep 20260912_0926` 现取;seed↔run 对应从 `analysis.json:script_version` 复原。
2. ⭐⭐⭐ **把 `status_code`/`ab`/`ba`/`arm_depth`/`survival_min` 写进 `W69_wave.json:machines[]`,不是只写进报告散文。**
   这是本轮头号产出的**直接交棒**:上一轮把读数读对了却只交在散文里,于是本轮闸 (iv) 开局 `exit 2`。
   ⚠️ 而且**必须当轮做完**:本轮实测 SIR 在终止后约 **5h–5h30m** 已经 `[]`(§六),拖一轮就只剩转抄。
3. **闸 (i) 名义解锁 `2026-09-12T15:26:31Z`** —— ⛔ **别抄,跑 `wave_throttle.py`**,并按 GH #469 **在发波脚本内部再跑一次**(本轮那道内部闸真的拦下了一次发波)。
4. ⛔ **本条目的 `$83.945` / cutoff `2026-09-11T18:04:16Z` / `headroom $1.705` / 解锁时刻全部作废**,围栏/闸/成本**一律当轮现跑**。
   ⚠️ 而且:**本波之后余量 `$1.705` < spot 一波 `$1.10` 的两倍** ⇒ **九月大概率再发不出第二波**;MTD 快照一旦下修或越 `$90`,**立刻转纯收割并开 `[batch] 预算刹车已触发` issue**。
5. ⛔ **W69 是 27-id 家族深度 0** ⇒ 收割时**不与 W68(29-id)、不与 W66+W67(30-id)并池**;
   读数按铁律 4 (i-a) 取工具自己的 `strata.<m>` **两层读数**(⛔ 非局数、⛔ 不手算),(i-d) **先每粒 swap-average 再跨种子算术平均**。
6. ⚠️ **开工第一条命令重定向 + 后台 + 不设短 `timeout`**(**第 18 次**登记);且 `routine_selfcheck.sh` 在本容器**连 25 min 都跑不完**,给它 ≥1800s 或**分腿跑**。
7. ⚠️ **`origin/main` 上四条 python 红**(§十),下一轮开工自检会再撞一次:`test_stale_waits.py`+`test_bots_walk_farm_only.py` 已立案(#765 / 本轮新开),`test_carrier_terms.py`+`test_detector_source_constants.py` 属英雄组。⛔ **本台不修**,但**不要把它读成本台引入的红**。
8. ⭐ 发波脚本改用 `tee` 不用 `tail`(§四 那个自伤)。


---

## 十五、Token

```
TOKENS total_in=12,638,225 out=71,030 turns=92
```

⚠️ 这是"到统计时刻为止"的数字,统计后的收尾回合不计入。
⚠️ **本轮偏高的归因(交总监按周汇总时参考)**:本轮是**发波轮 + 一次闸修复轮**,且有约 6 分钟在等闸 (i) 解锁(⛔ 未空转,期间做的是 §五 选种、§十 的 stash 复现与报告起草)。另章程本体 1.9MB 已**无法整读**,本轮只读尾部与 grep 命中行。
