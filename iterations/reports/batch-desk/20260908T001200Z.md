# 批测台 2026-09-08T00:12–00:5xZ(收割轮:W54 全量收割)

**⭐⭐⭐ 头号产出:闸 (iv) 这一轮**通过了,而它是靠 38 秒通过的。** 上一轮在 W53 上量出
那个常数「可行集为空」(17 个取值全 `exit 2`);本轮在 W54 自己的记录上重测,可行集**非空**
—— 但工具自己打的那一行是 `measured bracket 34.80 < x <= 40.63, margin above the constant
0.63 min`:出货常数 **40.0 只比上界低 0.63 分钟**。⇒ **GH #591 没有因为本轮 `exit 0` 而消失**,
本轮只是抽到了一个 38 秒的余量;下一波只要最快那次配对落在 ~40 分钟,同一道闸就会在
出货常数上打回 `UNDECIDABLE`,也就是此前挡住两波的那个停摆。

**第二产出:GH #454 的 (乙1) 盲区第一次被现场演示。** 本波账单侧实测 **$0.7271**,比常数
`$1.10` 低 **33.9%** —— 而它低成这样的**原因**是一台机器在 97 秒时被容量抢占,机时从分子里
消失了。这正是 #454 立案句说的形状(「抢占越频繁,常数越不会被复查,而抢占频繁正是市场
紧张的时候」),这次不是推理,是读数。

---

## 一、自检(铁律 10)

⚠️ **第一条命令第十八轮踩同族坑**:`... 2>&1 | tail -40` ⇒ 工具**当场自拒**
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked`,harness 报的
`SELFCHECK_EXIT=0` 是 `tail` 的。改**重定向 + 后台 + 不设短 `timeout`** 后重跑。
**十八轮提醒零成功率 —— 提醒不是修法**(第三次交总监/harness)。

写报告时自检仍在**最后一条腿(Lua 检测器)**上没跑完 ⇒ **Lua 那侧本轮既不声称干净也不声称红**
(与前两轮一致:整份自检在本容器 >900s)。已跑完的两条腿逐字:

- **⛔ trunk python RED**:`109 passed, 1 failed, 2 uncertifiable`,唯一 FAIL
  `tests/test_bots_walk_farm_only.py`,逐字
  `FAIL: a tests/ io.popen command cannot be resolved statically and is not on the hand-read
  list -- read it, then add it to UNRESOLVED_HAND_READ with the value its callers pass
  (do not delete this check): ["tests/test_lion_ult_cash_weakest.lua :: 'ls ' .. dir .. ' 2>/dev/null'"]`。
  **归属不是推断**:`git status` 只有本台自己那一个 `iterations/` 文件,`HEAD == origin/main`
  (`8b7c47655f4e42ae4b5b82e624c924cee0e814e6`),`git diff origin/main -- tests/ bots/ game/ tools/`
  **为空** ⇒ **红在 main 上,不是工作树伪影**(不需要 `git stash`)。来源提交 `10b474b9`(英雄组,
  Lion `lionultcash`),**不是本会话**。
  ⭐ **形状与本周那几例同族**:检测器要求新落地的 `.lua` 把它的 `io.popen` 登记进白名单,
  于是**任何一次新 fixture/测试落地都能打红一个与它无关的地方** —— 与 GH #584
  (`chaos_knight` 硬编码被 promote 证伪)、GH #585(`test_corpus_scale.lua` 语料规模等式)
  同一族;区别只在这一条**自带修法**(把命令加进 `UNRESOLVED_HAND_READ`)。
- **2 条 `UNCERTIFIABLE`**(`rc.sh` 两格,逐字 `no lua5.1 on PATH`)—— **没跑成不是通过**。
- `NOTE 5a0 cost: 86 file(s) [discovered -- the leg never reached its count line] in 120.1s
  (budget 120s)` —— Lua 腿预算再次刚好用完。

## 二、成本(章程步骤 2)

- `AWS_SETUP_EXIT=0`(`arn:aws:iam::581436534924:user/dota2bot-agent`);`COST_EXIT=0`。
- MTD **`$64.409`**(免费 `budgets`),`refreshed 2026-09-07T12:04:15Z`;≥$35 ⇒ 自动 `$0.01`
  CE 复核 **`64.4088949308`**,两源一致;`forecast 209.016`。
- ⛔ **快照戳与上一轮逐位相同 ⇒ 账单侧滞后已 12.2h**,W54 那四台 spot(21:14–22:09Z)与
  按需补跑机(21:35–~22:2xZ)**按构造**不可能在这张快照里。**不许因为 MTD 没动就写成没花钱。**
- ⛔ **未使用被禁的「MTD 增量对得上波费 ⇒ 该波已计入」手法**(章程步骤 2 (乙))。
- **本轮零发波、零付费工作**(唯一支出是那 `$0.01` CE 复核)⇒ 不做围栏算术的发波判定。
  `$50` 档仍越线(`AWS state=ALARM`),`$80`/`$100` 未跨;**本轮未新跨任何档**。

## 三、⭐ 有时限的那件事排第一位(章程「下一轮本台」①)

**做到了一半,另一半永久丢了 —— 而丢的原因是可登记的。**

- **SIR:四条全部第一手读到**(终止后 ~2.1h,仍在窗口内),`SIR_EXIT=0`。
  `Status.UpdateTime` **同时写进 `update`(闸 (iv) 读的键)与 `terminated_at`**(GH #580 的
  约定,本轮没有静默复发)。
  | 种子 | 实例 | AZ | create | Status.UpdateTime | Status.Code |
  |---|---|---|---|---|---|
  | 7527 | `i-0f54672e00323252c` | 2a | 21:14:48Z | 21:16:25Z | `instance-terminated-no-capacity` |
  | 7529 | `i-09623dfd42eb501c2` | 2c | 21:15:04Z | 22:09:12Z | `instance-terminated-by-user` |
  | 7719 | `i-02b609861c82db5bf` | 2c | 21:15:08Z | 22:09:12Z | `instance-terminated-by-user` |
  | 7789 | `i-061b45e795aec1058` | 2d | 21:15:11Z | 21:59:37Z | `instance-terminated-by-user` |
  ⚠️ SIR 的 `SpotPrice` 四台全读 `0.680000` —— **那是出价上限不是成交价**,不拿它算钱。
- ⛔ **按需补跑机 `i-041a2044ddd3741b7` 的 `StateReason.Code` 永久丢失**(GH #375 兑现,
  **连续第五波这条读数处在危险里,本轮是第一次真的没买到**)。`describe-instances` 对**五个
  实例 id 全部**返回**空 reservation 列表**(`ALL_EXIT=0`),EC2 已把终止实例老化出 API。
  ⭐ **空输出不是「没事」,是读数没了** —— 与章程点名的「shallow clone 空输出被读成无漂移」同族,
  本轮已把这句写进 `W54_wave.json:rerun.status_code`。
- ⭐⭐ **可登记的死因(交总监,新)**:上一轮把截止时刻锚在**看门狗上限**上(23:35Z + ~1h
  ⇒「约 00:35Z 后读不到」),本轮 00:1xZ 去读就已经没了。原因是**自终机器按设计跑不到看门狗**:
  它最后一个 S3 对象是 **22:18:31Z**,按同波 spot 机的「末次上传→终止」间隔(2m32s / 4m22s)
  它约 **22:21–22:23Z** 自终 ⇒ ~1h 窗口约 **23:25Z** 就关了,比预测早约 **70 分钟**。
  **失效方向是危险的那一侧**(以为还有时间)。⇒ **这类截止时刻必须锚在预期完工上,不是看门狗上限上。**

## 四、⭐⭐⭐ W54 收割(本轮主体)

`RV_EXIT=0`,无拒答。语料走 `soak/` 前缀,四个交货 run **各自子目录、没有拍平**(GH #225):
`8b5d8e`/`77e00d`/`3958d7`/`e8a868` = **160/159/120/120** 对象。
臂串**本轮现算**核对:**46 id / 417 字节 / md5 `cd3b46d4e3ca4a446272b330a09a2751`** = 发波记录逐字相同。

`files_seen 224 / games_loaded 224 / source_dirs 4 / unparseable 0 / pooled_basename_dirs_overridden 0`;
`scored 200 / unfinished 0 / seeds 4/4 / min_arm_depth 8 / thin_arm_seeds []`。

逐机(`machines[]` 现已**零 null**):

| 种子 | ab | ba | arm_depth | 计分局 | 来源 |
|---|---|---|---|---|---|
| 7527 | 27 | 15 | 19.29 | 42 | **按需补跑机**(spot 那台 97 秒被抢占,S3 前缀里**只有 1 个对象**`soak_farm.log`,零局) |
| 7529 | 42 | 16 | 23.17 | 58 | spot |
| 7719 | 34 | 24 | 28.14 | 58 | spot |
| 7789 | 29 | 13 | 17.95 | 42 | spot |

**读数(铁律 4(i-a):取工具自己的 `strata.<m>` 与 `per_seed[].<m>_ab/_ba`,未手算、未按局加权)**

| 量 | mean | ab | ba | side | 反号 | side>arm | comps_better |
|---|---|---|---|---|---|---|---|
| gpm | **−7.45** | −120.98 | +106.08 | −113.53 | 是 | 4/4 | **2/4** |
| xpm | **−3.11** | −66.83 | +60.60 | −63.72 | 是 | 4/4 | 1/4 |
| deaths | **+0.24** | +1.14 | −0.66 | +0.90 | 是 | 4/4 | 1/4 |
| last_hits | **−0.98** | −1.34 | −0.62 | −0.36 | 否 | 4/4 | 1/4 |

- 逐粒 gpm:**+4.14 / −16.90 / −29.64 / +12.59** —— **四粒不同号**(W52 是四粒同号为负)。
  ⚠️ 本台**不判**;只登记一条本台自己立过的纪律:**单种子 gpm 差 ±30 属纯噪声**
  (2026-08-19 镜像零点,`per-seed σ=30.24`),**四粒全部落在 ±30 之内**。
- **4(i-c)**:三量反号**不是否决理由**(恒等式 `|side|>|arm|`),但本波**也没有反向的承载物** ——
  W52 的负号靠「arm 四粒同号」承载,W54 **没有那个东西**。
- ⛔ **winrate 通道 DEGENERATE(工具自拒)**:少数侧只赢 224 局里的 **2** 局
  (share `0.0089 < 0.20`),界定在 `0.5 ± 0.0167`;侧别普查 radiant 2 / dire 222。
  ⇒ **`winrate 0.496` 与 `comps_better 0/4` 两个方向都不许当铁律 2(b) 证据。**
- `suggested = hold_or_reject`(工具的字段,**不是本台的判读**)。
- **家族边界**:W54 是 **46-id 家族第一波**(累计种子 4),与 W52(52-id)/W53(51-id)
  **不可并池**;`_note` 已写在波次记录里。

## 五、⭐⭐⭐ 闸 (iv) 在 W54 自己的记录上重测(收割义务 ④)

**先说结论的形状:上一轮的「不变性」结论没有继承过来 —— 而这正是上一轮自己要求重测的理由。**

- 真工具 `reclaim_blind.py --wave-json .../W54_wave.json` ⇒ **`RECLAIM_BLIND_EXIT=0`**,逐字
  `yield: 3 paired seed(s) of 4 machine(s)` / `attribution: 1 machine(s) reclaimed before the flip` /
  `VERDICT: not blinded` / **`NEXT WAVE: spot`**。
- ⚠️ **第一次调用是 `exit 2`**,逐字 `UNDECIDABLE: seed 7527: leg count 'ab' is not an integer`
  —— 因为本台先把被抢占那台的 `ab`/`ba` 填成 `null`。**修法是填这台机器自己的真相 `ab0/ba0`**
  (它确实一局没交),并在记录里注明**种子 7527 的读数来自按需补跑机、不来自这台**。
  **`exit 2` 是「什么都没检查」,永远不是通过。**
- ⭐ **常数扫描(12 个取值,真工具未改一字)**:`c ≤ 40`(测了 10/20/30/34/35/40)全部
  **`exit 0` `not blinded`**;`c ≥ 45`(45/50/60/80/100/120)全部 **`exit 2`**,逐字
  `refusing to answer with a number the data just contradicted (this is exit 2, not a pass and not
  a BLINDED)`。⇒ **可行集非空但很窄**,工具自己打的界是
  **`34.80 < x <= 40.63`,出货常数 40.0 的余量 `0.63 min` = 38 秒**。
  ⇒ **GH #591 的题目没变,只是本波没撞上它**:常数在这批数据上**是承重的**(裁决随它翻转),
  而它离塌方只有 38 秒。**下一波再快一点,同一道闸就在出货常数上停摆。**

## 六、GH #454 两条腿(无论是否触发都报)

逐机按**各自 AZ 的现货报价**计价(`describe-spot-price-history`,21:00–22:30Z 窗口:
2a `$0.2834` / 2c `$0.2858` / 2d `$0.2582`)+ `机时 × $0.005/h` IPv4:

| 种子 | AZ | 机时 | 费率 | EC2 |
|---|---|---|---|---|
| 7527 | 2a | 0.0269 | 0.2834 | $0.0076 |
| 7529 | 2c | 0.9022 | 0.2858 | $0.2579 |
| 7719 | 2c | 0.9011 | 0.2858 | $0.2575 |
| 7789 | 2d | 0.7406 | 0.2582 | $0.1912 |

**spot 波实测 EC2 `$0.7142` + IPv4 `$0.0129` = `$0.7271`**。

- **(乙1)** `$0.7271 > $1.10`?**否**(余量 33.9%)。
- **(乙2)** 自终机器(`instance-terminated-by-user`)机时均值 **`0.8480 h/台`**(3 台)`> 1.00`?
  **否**(余量 15.2%)。被抢占那台按条款**不进这个均值** —— ⭐ **这次那条排除是有分量的**:
  把它算进去均值变 **`0.6427`**(低 24%)。
- ⭐⭐ **本轮是 (乙1) 盲区的第一次现场演示,不是推理**:$0.7271 之所以低 33.9%,是因为
  **一台机器 97 秒就没了**,它的机时从分子里消失,而常数按**四台跑满**标定。
  归一化看:三台跑满 EC2 `$0.7066` ⇒ `$0.2355/台` ⇒ **假想的四台满波 ≈ `$0.958`**,
  余量只剩 **12.9%**。
- ⭐ **上一轮交棒 ⑥ 的预测第四次坐实**:机时稳稳压在线下,而**三个 AZ 的报价全部越出
  `$0.231–0.250` 拟合带**(2a **+13.4%**、2c **+14.3%**、2d **+3.3%**)。
  **两条腿都不直接看价格** —— 这是 (乙1) 的分辨率问题,**本轮登记,不擅自改常数**。
- **按需补跑机**:无 SIR ⇒ **按构造进不了 (乙2) 的均值**;机时**下界 `0.7178 h`**
  (起飞 21:35:27Z → 末次 S3 上传 22:18:31Z,**这是事实**),上界 2h 看门狗;
  类比点估 ~0.76h ⇒ **~$0.51,这是推断不是测量**,记进 `pending` 用点估、报告里带边界。

## 七、收割义务逐条结清(全文在 `W54_wave.json:harvest_obligations`)

1. `recover_verdict.py` 全量重算,不用实例自产 verdict — **DONE**(第四节)。
2. 抄 `winrate_channel` 与 `mean.winrate_headroom`;DEGENERATE 则不把 winrate 当读数 — **DONE**。
3. 四台 `update` 填 SIR `Status.UpdateTime`,**早读** — **DONE**(第三节;按需那台的另一半永久丢失)。
4. 闸 (iv) 在 W54 自己的记录上重跑 + 重测(不继承) — **DONE**(第五节,**结论确实变了**)。
5. **2c 双押的兑现检查** — **DONE,而且反过来了**:2c 扛的两粒(7529 `ab42/ba16`、
   7719 `ab34/ba24`)**全须全尾**,2c 零事件;真正掉臂的是**没人点名的 2a**(单粒)。
   ⚠️ **这不是「预登记错了」**,一次抽样说不了那个;登记的是**风险没有在被点名的地方兑现**。

## 八、发波:本轮不发,门自己说的

`wave_throttle.py` **`THROTTLE_EXIT=3`**,逐字:
`anchor wave: W54 (4 machine(s))` / `last machine up 2026-09-07T21:15:11Z <- the anchor (GH #469 ruling 1)` /
`unlock 2026-09-08T03:15:11Z` / `now 2026-09-08T00:17:33Z` / `margin 10658s SHORT of unlock` /
`A launch at this instant BREACHES gate (i) by 177m38s.` / `WAVE_THROTTLE: THROTTLED (exit 3) -- do not launch.`

⇒ **零发波、零支出**。**读的是工具打的那一行,不是本台自己算的数。**
(顺带:锚点是 21:15:11Z 那台 spot,**不是** 21:35:27Z 的按需补跑机 —— 补跑机不开新窗口,
GH #498 ③ **就是这么裁的**,本轮**不当缺陷登记**。)

## 九、泄漏

`describe-instances` 全账号(**不按 tag 过滤**)`running/pending` = **空**;
`check_costs.sh` 的同一节也空 ⇒ **四台 spot + 一台按需补跑全部自毁,无第五台、无孤儿**。
常驻仍只有 AMI `ami-0a990a26d89c66547`。⭐ **外来 `r7a`/`c7a` 负载本轮 region 内 0 条**。
⛔ **GH #515/#528 立案理由一字未变**:预算过滤器仍是 `null`,本台围栏**仍读着一个它不控制的账号级数字**。

## 十、铁律 6/11

- `bots/`/`game/`/`tests/`/`tools/` **本会话一行未改**(只动 `iterations/` 下的报告、
  `W54_wave.json`、本章程「当前状态」节)。
- **铁律 11**:全程未触发任何 `requires approval`;**本轮没有任何空转等待** —— 闸 (i) 的
  177 分钟是**门直接拒绝发波**,本台按章程转成纯收割轮。

## 十一、交棒

1. ⭐⭐⭐ **总监 — GH #591 不因本轮 `exit 0` 而消失**:闸 (iv) 的出货常数 40.0 在 W54 上
   **只余 0.63 分钟(38 秒)**,而裁决在 `c≥45` 上确实翻成 `exit 2` ⇒ **常数是承重的**,
   上一轮 W53「可行集为空」+ 本轮「可行集 = `(34.80, 40.63]`」是同一件事的两个测量点。
   **第六轮零总监回应。**
2. ⭐⭐⭐ **总监 — GH #375 的截止时刻锚错了地方**:按需补跑机的 `StateReason.Code` 本轮
   **永久丢失**,因为上一轮把窗口锚在**看门狗上限**而机器**按设计提前自终**(早约 70 分钟);
   要的是把锚点改成预期完工,或在自终当轮就读。
3. ⭐⭐ **总监 — GH #454 的 (乙1) 盲区第一次有现场读数**(第六节):$0.7271 低 33.9% 的
   **原因就是抢占**;归一化满波 ~$0.958(余量仅 12.9%),三个 AZ 报价越带 3.3–14.3%。
   **本台不擅自改常数,登记第四个测量点。**
4. ⭐⭐ **协同组/总监 — W54 读数四粒不同号、全部落在 ±30 噪声带内**;本台不判,
   但**这一波没有「arm 同号」这个承载物**,与 W52 的负号不是同一种证据。
5. ⭐ **总监/英雄组 — trunk python 红在 main 上**(第一节):`test_bots_walk_farm_only.py`
   要求新落地 `.lua` 的 `io.popen` 登记进 `UNRESOLVED_HAND_READ`,`10b474b9` 落地的
   `tests/test_lion_ult_cash_weakest.lua` 触发;**与 #584/#585 同族,但这条自带修法**。
6. ⭐ **总监/harness — 开工第一条命令的管道坑第十八轮**,提醒零成功率,请给机制。
7. ⭐ **owner — `$50` 仍越线**(`$64.409`,`AWS state=ALARM`,`forecast 209.016`),
   预算过滤器仍 `null`(GH #515/#528)。

## 十二、下一轮本台

1. **发波轮**(闸 (i) `03:15:11Z` 后解锁;**读工具打的那一行**)。下一波按 `NEXT WAVE: spot`
   (闸 (iv) 本轮的裁决,GH #158 默认未变)。
2. **四道闸全部起飞前跑完,(iv) 排在 (i) 之前**;⚠️ **(iv) 的余量只剩 38 秒** ——
   若它这次打 `exit 2`,**那不是新缺陷,是 GH #591 兑现**,当轮就要点名。
3. 臂串**现算**(id 数 / 字节 / md5),与 W54 的 `46/417/cd3b46d4…` 比对并写出 ±差;
   `seed_roster_index.py --build` 先跑,窗口右移。
4. 若本波仍有按需补跑机:**它的 `StateReason.Code` 要在自终当轮读**(交棒 2)。
5. ⚠️ **开工第一条命令重定向 + 后台 + 不设短 `timeout`,`2>&1 | tail` 也算管道**(第十九次)。

## 十三、Token 用量

(见文末追记)
