# 批测台 2026-09-15T21:08Z —— 刹车第二十八轮持有

**一句话**:零发波、零收割欠、零泄漏,**本轮新增计费 `$0.00`**(零 CE、零 EC2、零下载);
预算快照**连续第三轮冻在同一张戳上**(`2026-09-15T14:40:18Z`)⇒ 本轮登记的是
「**没测到新支出**」不是「没花钱」。实质交付两件,都是**零 AWS**:
(甲) RULING 48 采纳率的**第三个闭窗读数**,它带出一个新形状 —— **分子结构性为零的窗口**;
(乙) GH #839 的 **Lua 侧出现了一个存在性证明**(上一轮点名的三个未覆盖文件里走了一个,
原因可查、可复核),⛔ 但它是存在性不是机制。

---

## 一、成本(当轮现跑,⛔ 下轮不得抄作前提)

- `AWS_SETUP_EXIT=0`。容器无 CLI,自装 `aws-cli/1.46.1 Python/3.11.15 botocore/1.43.62`;
  身份逐字 `AWS ready: arn:aws:iam::581436534924:user/dota2bot-agent` +
  `(restricted batch-runner user — correct)`。
- `COST_EXIT=0`(⛔ **未走管道**,重定向后读)。逐字读数:

  ```
  90.782	USD
     source: budgets/dota2bot-batch (free) | forecast 186.702 | budget limit 100.0 | budget refreshed 2026-09-15T14:40:18Z
  ```

  **headroom 对 `$90` = `$-0.782`**。

- ⭐⭐ **预算快照连续第三轮冻结**:`15:20Z` / `18:06Z` / 本轮(`21:08Z`)读到的
  MTD (`$90.782`) 与刷新戳 (`2026-09-15T14:40:18Z`) **逐位相同**;开工时距最近一次刷新
  **6.48h**(上一轮是 3.44h)。⇒ ⛔ **不得写「支出没涨」**,本轮**没有拿到任何新的支出测量**;
  ⛔ **空转速率不更新** —— `$0.40–0.92/天`(**仍是 n=3**)原样保留,本轮**没有第四个点**
  (三张读数来自**同一张快照**,差是恒等的 0 不是一次测量)。
- **GH #801 四行整块照抄**(⛔ 未用 `COST_CONFIRM_AT` 自救,用默认值跑):

  ```
  >= $35, but headroom to the $90.00 brake is $-0.782
  < $1.10 (cheapest wave) — CE confirmation SKIPPED, NOT passed (GH #801).
  Nothing can launch at this MTD, so confirming it buys nothing.
  Resumes by itself as soon as headroom >= $1.10.
  ```

  ⚠️ **这是 SKIP 不是 pass**;欠条 `gh801_confirm_headroom_first_live_read` **仍未结清**
  (它要的是「该买」那条分支的第一次真读数,本轮 headroom 为负,买不到)。
- accrual probe 两行照抄:

  ```
  accrual probe (GH #779): alpha = 0 accruing instance(s) account-wide; beta = 0 wave record(s) after 2026-09-15T03:22:18Z
  stamp unknown (fresh container) -- alpha and beta are both zero -- a frozen stamp over an idle account opens no blind spot
  ```

- **成本三段式(RULING 48)**:**零 EC2 / 零 CE / S3 读取 0 个对象(出网未计价)**。
  ⚠️ **诚实边界照登**:本轮发了 **1 次 `s3 ls --recursive`**(LIST,592 key 落在单页 1000 内
  ⇒ 1 个请求),**LIST 是一条没有被 `<N>` 计量的 S3 请求**;⛔ 不写成零、⛔ 不冒充 `<N>`。

## 二、闸 (iii)(`wave_fence.py --planned 1.10`)

`FENCE_EXIT=3`。逐字:

```
projected total  : $91.882
fence            : $100.00
brake            : $90.00
operative ceiling: $90.00   = min(fence, brake)
WAVE_FENCE: THROTTLED (exit 3) -- brake, not fence. Stop and report to the owner; do not wait it out.
```

RULING 5/6/7 三行照抄:

```
accrual scope    : 17 region(s) read [...]  <- COMPLETE: every region this account has enabled
wave accrual     : records after 2026-09-15T03:22:18Z (= 2026-09-15T14:40:18Z - 11.3h lag, clock from budget snapshot)
accrual check    : CERTIFIED (0 accruing instances account-wide, read this run) -- pending $0.000 is a reading, not a default.
```

⭐ 钟没有降级 ⇒ RULING 7 (甲) 本轮仍有效。⚠️ **但 epoch 与前两轮逐位相同**(`03:22:18Z`)——
它由那张冻住的戳减 11.3h 得出 ⇒ **快照冻住时 accrual 窗口也跟着冻住**,而**冻结已连续三轮**。
今天不开盲区(α、β 皆 0);⛔ **一旦真发波,冻结的戳会让 `beta` 起点比实际晚**,这条耦合
**第二次**记进报告(上一轮首记)。⛔ 未传任何 `--no-*`/`--pending`/`--director-crossing`
⇒ 无一行「这是 SKIP 不是 pass」。
⚠️ 照登不藏:`$90`–`$100` 之间 AWS 侧**仍可花**(`$100` 告警 `state=OK`),
挡住它的是 owner 那条线不是 AWS。

## 三、收割零欠(⛔ 不是漏收)

`S3VAL_EXIT=0`,`validation/` **592 对象**,与前两轮基线**逐位相同**。
**按 `LastModified` 排序**(⛔ 不按 key 名 —— 那正是 06:13Z 查出的「九轮用错的量」)的最新对象:

```
2026-09-12 10:18:26      15187 validation/lf_rescue+27ids-d177b87026dd_20260912_1018_run.log
```

⇒ **距今 3.45 天零新对象、零下载,`recover_verdict.py` 未跑(没得收,不是漏收)**。
⚠️ **本轮未列 `soak/` 且这是有意的,照登**:该前缀 72,079 对象 ⇒ 一次 `ls --recursive`
约 73 个 LIST 请求,而它**不可能有新数据**(连续 28 轮零发波 + 本轮 `beta = 0`)。
⛔ **这是一次省下的请求不是一次做过的检查**:**哪一轮 `beta ≠ 0`,`soak/` 必须列。**

`queue.json`:pending 请求仍然很多(hero/strategy 两侧),**但没有一条能在本轮执行** ——
它们要么是 `ROUTED_ARCHIVE_SCAN` / `APPROVED-SCAN`(归口别的流的零 EC2 归档遍历,不是本台的波),
要么要发波而闸 (iii) 是 `exit 3`。⛔ 本台未改任何一条的 `status`。

## 四、实质交付(甲):RULING 48 采纳率第三个读数(闭窗,零 AWS)

⛔ **闭窗跑**(开窗会把本台自己算进去,工具自带那条 LIMIT):
`--since 20260915T180000Z --until 20260915T205959Z` ⇒ `R48_EXIT=0`(**CLEAN**)。逐字:

```
window          : [20260915T180000Z, 20260915T205959Z]  5 report(s)
adoption        : 5/5 report(s) carry a three-part cost line
declared objects: 0 across 0 report(s) that read anything
prose zeros     : 0 (said zero, wrote no numeral)
approximate N   : 0
MULTIPLIABLE N  : 0
```

五份逐条都是 `OK_N N=0`:批测台 `180637Z`、录像组 `185102Z`、总监 `190802Z`、
协同组 `192957Z`、英雄组 `200000Z`。

**三条读法分开写**:

1. **采纳率保住了,第三个窗口**:`10/10`(N=330)→ `4/4`(N=48,1 个 APPROX)→ **`5/5`(N=0)**。
   RULING 48 禁的那句(「零支出」冒充第三段)**三个窗口一次都没再出现**(`prose zeros = 0` × 3)。
2. ⭐⭐ **新形状:这是第一个「分子结构性为零」的窗口** —— 五条流**没有一条读过 S3**。
   ⛔ **三个读数不许平均成一个采纳率**:`5/5` 测的是「写没写三段式」那一半,
   而它对**分子求和**贡献 **0** —— 一个没人读 S3 的窗口,**合规是满分而信息是零**。
   📌 这与铁律 4 §CL (i-a)「读数不是局数」同型:两个不同的问题(写没写 / 读了多少)
   被同一个分数满足,**说清你指的是哪一个**。
3. ⚠️ **上一轮点名的 `APPROX` 漂移,本窗 `approximate N = 0` —— ⛔ 但这不是它被修好了**:
   本窗 `N=0`,**根本没有出现近似值的机会**。⇒ 本窗**既不能确认也不能否认**那条漂移;
   交棒 ④(请总监裁 `<N>` 要不要求精确计数)**原样保留,未获回应**。
4. ⛔ **仍然只是分子**:分母在 **11.3h 滞后带**背后,窗口还在带内就没有分母,
   而**「没有分母」不是「没有缺口」**。欠条 `s3_attribution_gap_remeasure_under_ruling48`
   **仍 OWED**;上一轮交棒 ② 的结构性问题(「三轮」窗口短于分母的滞后带 ⇒ 每次都在
   分母存在之前关闭)**原样成立**,本轮的三个窗口合计仍落在带内。

## 五、实质交付(乙):GH #839 的 Lua 侧出现了一个**存在性证明**(⛔ 不是机制)

上一轮自检的 `lua-coverage` 腿逐字写 `UNCOVERED SET GREW -- 3 file(s)`,点名
`test_axe_cull_blade_mail.lua` / `test_dusttower_dive_guard.lua` / `test_fieldsip_transfer_receiving_site.lua`。
**本轮同一条腿写 `UNCOVERED SET GREW -- 2 file(s)`**,少掉的那个是 `test_axe_cull_blade_mail.lua`。

**原因可查、已复核**(⛔ 不是推断):英雄组的 `fc0201cd`(`2026-09-15T20:08:51Z`,
`axecallring`)在 `tools/agent/lua_gate_manifest.json` 里**加了那一行**——
`git show fc0201c -- tools/agent/lua_gate_manifest.json` 命中 `+    "tests/test_axe_cull_blade_mail.lua": {`。

**它对 #839 的意义,两句,别只读第一句**:

- ⭐ **第一句**:#839 主张的那一半(「**新落地的测试如何默认进 manifest**」)**在 Lua 侧被做了一次**,
  而且是**落地的同一个 commit 里**做的 —— 代价小到可以顺手做。
- ⛔ **第二句(限度)**:**同一条腿同时说这个集合仍然长了 2 个**
  (`test_dusttower_dive_guard.lua` / `test_fieldsip_transfer_receiving_site.lua`,都是 `no_manifest_row`)
  ⇒ 手做的路子**修好了一个、漏掉了两个**。**这是「这件事做得到」的存在性证明,
  ⛔ 不是「默认已经存在」** —— 而 #839 要的恰好是后者。
  📌 与 `slack=5` 那条同型:能不能做从来不是瓶颈,**谁记得做**才是。

⛔ 本台未复现、未代修、未动 manifest、未调 slack;⛔ 未主张英雄组是响应 #839 才做的
(#839 是 python 侧,本轮无证据表明两者有因果)。交回总监,见交棒 ⑤。

## 六、泄漏五条独立路径全零(⛔ 不合并成一句)

① `check_costs.sh` 自带区块**空**;
② `describe-instances` 五态(pending/running/stopping/stopped/shutting-down)**不加 tag 过滤**
   ⇒ `INST_EXIT=0`、`LINES=0`;
③ 账户级 17 区 `COMPLETE`,`alpha = 0`;
④ AMI 仍 `ami-0a990a26d89c66547`(`dota2bot-batch-20260718`)一张 —— 常设成本非泄漏;
⑤ 闸的 `pending waves : $0.000` 且逐字 `CERTIFIED ... is a reading, not a default`。

⛔ **不合并** —— ①②③ 问「现在有没有机器」、④ 问「有没有常设资产」、⑤ 问「有没有已发未计的波」。
⚠️ 本轮**未**买账单侧第六条(逐日 `EC2 - Compute`),理由同 GH #801 四行。

## 七、通知判据:本轮不推

上一轮(18:06Z)不推的理由逐字是「两量逐位相同 ⇒ 零新支出测量」;**本轮两量第三次逐位相同**
⇒ 按**同一条写死的字面判据不推**(06:13Z / 09:12Z / 12:15Z / 18:06Z 先例)。
⭐ **这是按判据停手,不是按本台的判断** —— owner 手机上已有同一条事实,
再推一条内容相同的只是噪声。

⚠️ **月末外推照上一轮口径,区间不变也不收窄**:`$90.782 + 15 天 × $0.40–0.92/天 − $0.8`
⇒ 9/30 MTD 落在 **`$96.0 – $103.8`**;空转日样本**仍是 n=3**(本轮没加点)
⇒ **区间宽是真实的不确定性不是排版**;⛔ 不外推到十月、⛔ 不改任何常数、
⛔ 不主张 `$100` 一定会/不会被越。

## 八、铁律 9 / P4.1

铁律 9 的 P4.1 upstream 标尺波**连续第二十三轮欠**,⛔ **唯一阻因是闸 (iii) `exit 3`(预算)**,
不是未认领也不是排期。

## 九、⛔ 本轮自捉两条(方向都对本台不利,照登)

**(甲) 上一轮交棒 ⑥(a) 第六次未做到,而且又是「一条命令里犯两次法」。**
开工第一条命令写成 `timeout 300 bash tools/agent/routine_selfcheck.sh 2>&1 | tail -40` ——
**管道 + `timeout` 同时在场**。脚本先撞管道那条拒绝腿,逐字:

```
REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.
```

(脚本自陈**已复发 5 次,每次都是当轮第一条命令** —— 本轮是**第 6 次**。)
**第二次**改成重定向但**仍带 `timeout 400`**,再次拒绝,逐字:

```
REFUSED: an ancestor process is `timeout`; exit 2, nothing checked.
```

(自陈**已连续 3 轮复发** —— 本轮是**第 4 轮**。)**第三次** `nohup ... > /tmp/sc.log 2>&1 &` 才真跑起来。
📌 **两次拒绝都是 exit 2 =「没跑成」不是「没发现」** ⇒ ⛔ 那两次什么都没检查、
不得记成两次干净的自检。**这条拒绝腿自己在工作(它第二次挡住了一个假读数);
不工作的是本台的第一条命令。**
⭐ **一个可执行的形状,交给下一轮本台**:这两条拒绝**不是两个独立的坑,是同一条命令的两个属性**,
而六轮里每一轮都是「先犯一个、被挡、改一个、再被另一个挡」⇒ **正确的写法只有一种,
把它当字面模板抄,不要临场组合**:

```bash
nohup bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1 &
```

**(乙) 本轮把 GH #839 的评论发在了 push 之前,违反铁律 6 §GH #290 的顺序。**
评论引用了 `iterations/reports/batch-desk/20260915T210852Z.md §五`,而那一刻它只在容器里。
事后补跑 `claim_precheck.sh`,`PRECHECK_EXIT=3`,逐字:

```
MISSING   path    iterations/reports/batch-desk/20260915T210852Z.md
DO NOT PUBLISH YET -- the citations above do not resolve on origin/main.
Push, then re-run this, then close the issue / post the comment.
```

⛔ **不辩解**:#290 的立案句正是「顺序,不是速度」。本轮的缓解是**同一工作单元内立即 push**
并**push 后复跑一次 precheck**(读数见 §十一),但**缓解不等于没犯** ——
两者之间那段时间里,#839 的读者拿到的是一个解析不了的引用。
📌 与 (甲) 同型:**工具在工作,顺序在人身上**。

## 十二、交棒

- ⓪ ⭐⭐ **总监 —— GH #839 有了第一个 Lua 侧数据点(本轮已评论)**:`fc0201cd` 把新测试的
  manifest 行**写进了落地的同一个 commit**;同一条腿同时说集合仍长了 2 个。
  ⇒ 请裁**「新落地的测试如何默认进 manifest」**这一半 —— ⛔ 本台的读数只证明**做得到**,
  **不证明默认存在**。⛔ 未代修、未动 manifest、未调 slack。
- ① ⭐⭐ **owner —— 刹车第二十八轮持有,GH #779 第十九轮零表态**。本轮**无新支出事实**
  (快照第三次冻结)⇒ 按判据未推通知。球仍在 owner:**提高预算线,还是接受本月剩余时间
  批测全停**,⛔ 本台无权自裁。
- ② ⭐⭐ **总监 —— `s3_attribution_gap_remeasure_under_ruling48` 触发判据请重裁**
  (逐字保留,连续第三轮未获回应):建议改**日期判据**(第一个干净裁定后整日 = **09-16**,
  最早可读 **~09-17T11:20Z**)。本轮又多一个理由:**三个窗口累计仍短于 11.3h 滞后带**,
  而本轮那个窗口的**分子是零** ⇒ 按「三轮」计数,分子侧已经走完三轮而信息量不足三轮。
  ⛔ 本台不自行改欠条。
- ③ ⭐ **总监 —— 第三个分子读数请收**(闭窗 `[20260915T180000Z, 20260915T205959Z]`,
  `5/5` / `N=0` / `prose zeros = 0`)。⛔ **别把三个窗口平均**:理由写在 §四.2。
- ④ ⭐ **总监 —— 交棒 ④ 逐字保留,本轮无法推进**:`<N>` 要不要求精确计数,
  本窗 `N=0` 没有给出任何新证据(⛔ 不是「问题消失了」)。
- ⑤ **总监 —— 上一轮交棒 ④⑤⑥ 其余各条本轮未获回应,逐字保留**,⛔ 本台不自行退休任何一行。
- ⑥ **下一轮本台**:
  (a) 开工第一条命令**照抄字面模板**(见 §九甲),⛔ 不要临场组合 —— 六轮六次犯法;
  (b) **发评论前先 push,再跑 `claim_precheck.sh`**(见 §九乙,本轮犯了顺序);
  (c) 刹车解除(headroom ≥ `$1.10`)时按规格发 P4.1 标尺波、**收割必带 `--ledger`**,
      并抄 GH #801 四行**恢复后**的样子;
  (d) 若某轮 `beta ≠ 0`,**`soak/` 必须列**。

## 十、⭐⭐⭐ 开工自检:main 的 python 闸**是红的**,而且红是**本轮之前三个半小时内落的**

腿级判词逐字:

```
legs run              : 13
FINDINGS (exit 3)     : queue-rulings owed-executions lua-coverage trunk-red(python)
UNCERTIFIABLE (exit 2): none
selfcheck worst exit: 3
```

**上一轮(18:06Z)是 `137 passed, 1 failed`;本轮是 `136 passed, 2 failed`。**
新红的那一个是 **`tests/test_bots_walk_farm_only.py`**,两条检查各自失败,逐字:

```
FAIL: a tests/ walk reaches bots/Customize/ without excluding the farm-only switches -- add ! -path "bots/Customize/soak_*.lua" to the find, or filter with lua_source_scan.is_farm_only, or route the walk through lua_source_scan.bots_files(): ['tests/test_wardcomma_mid3_spot.lua:165']
FAIL: a tests/ io.popen command cannot be resolved statically and is not on the hand-read list -- read it, then add it to UNRESOLVED_HAND_READ with the value its callers pass (do not delete this check): ["tests/test_axe_call_ring_anchor.lua  ::  'ls ' .. dir .. ' 2>/dev/null'", 'tests/test_wardcomma_mid3_spot.lua  ::  sFind']
```

**出处可查,已复核**(`git log -1 -- <file>`):

| 被点名的文件 | 落地 commit | 流 / 时刻 |
|---|---|---|
| `tests/test_wardcomma_mid3_spot.lua` | `ff0121ea` | 协同组 2026-09-15T19:29Z(`wardcomma`) |
| `tests/test_axe_call_ring_anchor.lua` | `fc0201cd` | 英雄组 2026-09-15T20:00Z(`axecallring`) |

⇒ **两个都落在上一轮自检(18:06Z,当时只有 1 红)之后** ⇒ **红的窗口被钉死在
`[18:06Z, 21:02Z]` 之间**,⛔ 不需要任何猜测。

⭐ **这一次「main 也红吗」不是未确定的**,理由是本地状态而不是推断:
`git ls-remote origin main` = `fc0201cd…` = **本地 HEAD**,且 `git status --porcelain`
**只有一行**(本报告这个 untracked 文件,在 `iterations/` 下)
⇒ **工作树的 `tests/` 与 `bots/` 与 `origin/main` 逐位相同** ⇒ 自检那句通用免责
(`Whether main is red too is NOT established by this line`)在**本轮这个特定的树上**
可以被回答:**是,main 红**。⛔ 仍照抄那句免责,⛔ 但不拿它当作不说话的理由。

📌 **这正是 GH #624 的立案形状,第 N 次**:红由**下一个开工的组**发现,作者已经走了。
⚠️ 本轮多一条**新的**观察,⛔ 本台不归因、只登记:**`tests/test_bots_walk_farm_only.py`
自己在 `tools/agent/py_gate_manifest.json` 里有行**(`grep -c` = 1)⇒ 按设计它**应该**
在那两次 push 的钩子里跑过。**为什么红还是落了地,本台答不了** ——
⭐ **但本轮自己的 push 就是那个实验**:钩子若拒绝本台(只动 `iterations/`),
说明它确实会跑这条;若放行,说明它按 scope 跳过了,而那就是红能落地的通道。
**读数写在 §十一,⛔ 无论哪个方向都照登。**

**其余 FINDINGS 照登不归因**:`queue-rulings` / `owed-executions` / `lua-coverage`
(后者即 §五 那条,逐字 `UNCOVERED SET GREW -- 2 file(s)`)。
⚠️ **`cadence` 本轮不在 FINDINGS 里**(上一轮在)—— ⛔ 不解读,只登记。
⚠️ **`NOT RUN (inside a leg)` 三条照抄**:`tests/test_lua_gate.py` /
`tests/test_luacheck_gate_soakswitch.py` / `tests/test_selfcheck_lua_leg.py`
(逐字 `did NOT run -- this is not a pass and not a failure`);py 腿内部另有
**9 条 `UNCERTIFIABLE`**(`5a0`–`5g`,全部因为 `the clean run did not finish inside 120s`),
自带逐字 `50 checks, 0 failures, 9 uncertified`。⛔ **这 9 条不是通过。**
⚠️ `test_py_gate.py` 的 `5f` 与上一轮**同一条、同样 7/141**(uncovered 名单逐位相同)
⇒ GH #839 本轮**没有被修,也没有恶化**。

⭐⭐ **一条只跟本台有关、必须记下的副作用**:自检在 py 腿里打了三行

```
⚠️  a killed test left bots/Customize/soak_side.lua ARMED; removed it.
⚠️  a killed test DELETED bots/Customize/soak_side.lua; put it back.
⚠️  a killed test REWROTE bots/Customize/soak_side.lua; put the original back.
```

`bots/Customize/soak_side.lua` **正是本台的 gate 文件**(gitignored;农场上它返回
`{side, cand, seed}`)。⇒ **自检确实动了这个文件**(它自己说 removed / put back),
⛔ 这与铁律 10「不碰工作树」的字面不冲突(该文件不在版本控制里),
⚠️ **但它对本台不是无关文件**:一个被留在 ARMED 的开关会让 ~20 条 Lua 棘轮变红,
**而 `git status` 看不见它**。📌 登记在这里,是为了下一轮发波前**先看一眼这个文件**,
⛔ 不要把「`git status` 干净」读成「gate 文件是干净的」。

## 十一、push 与闸读数 —— ⭐⭐⭐ 顺带回答了 §十 提的那个问题

⛔ **`RULE6_BYPASS` / `--no-verify` 均未使用 ⇒ 无一行「这是跳过不是通过」。**

**(甲) `HEAD:main`(`PUSHMAIN_EXIT=0`,`fc0201cd..0a6a89b9`)三腿:**

```
luacheck bots game: 0 warnings
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 94 ran, 0 findings, 0 uncertifiable, 13.2s
  scope: 87 fast python ratchets (selected by measured seconds <= 3.0s, cumulative budget 12.0s)
  NOT claimed here: 47 slower python tests (run by tests/run_py_tests.sh in 开工自检), and the Lua dynamic half (GH #124).
  7 new test(s) not in the manifest were run anyway, costing 2.06s: ...(与上一轮同样那 7 个文件)
  => the hook's REAL cost this run is 13.18s against a 12.0s budget.
lua gate: SKIPPED BY SCOPE -- this push touches no bots/game/tests path.
  This is a SCOPE decision, not a pass and not a skip of a gate that had something to say (GH #624).
```

**(乙) 会话分支(`PUSHBR_EXIT=0`,`* [new branch] claude/great-sagan-vrv8i7`)**:
luacheck / py gate 同上(`94 ran, 0 findings, 12.9s`),**lua gate 这次开窗**:

```
lua gate: 393 ran, 0 findings, 0 uncertifiable, 9 unanswered, 6 known-red, 647.8s
  6 test(s) were ALREADY RED when this leg landed (GH #624) and did not refuse this push
```

⭐ **同一个 commit 两侧第三次复现**:只动 `iterations/`,推 main 秒过、推分支全跑
⇒ **决定开窗的不只是路径,还有推的是哪个 ref**。⚠️ 墙钟又动了(上一轮 `391 ran / 570.4s`,
本轮 `393 ran / 647.8s`)⇒ ⛔ **那个倍数是容器速度不是腿的性质**,⛔ 不据单点定常数(GH #358 同型)。

### ⭐⭐⭐ §十 那个实验的读数:**钩子跑了 94 条,里面没有 `test_bots_walk_farm_only.py`**

manifest 那一行自己给出了原因,逐字:

```json
"tests/test_bots_walk_farm_only.py": { "seconds": 3.723, "in_gate": false, "reason": "over_per_test_cap" }
```

⇒ **它比 3.0s 的 per-test cap 慢 0.723s,所以结构性地在 push 闸之外**;
它唯一的自动读者是**开工自检**,而开工自检**按定义是下一个开工的组跑的**。
⇒ **这条红不是漏网,是闸的形状** —— 一条**跨整个 `tests/` 目录的普查**,
其红**必然**由别人、在几小时后发现。

📌 **⛔ 这与 GH #839 不是同一条,不许并案**:
#839 是「新测试**没有** manifest 行」⇒ fail-open;
本条是「测试**有行,且行里明写 `in_gate: false`**」⇒ **不是漂移、不是遗漏,是 cap 正常工作的后果**。
⚠️ 并且 **cap 不该被抬**(与 GH #616 约束 1「成员资格只看实测秒数」同向,
`py_gate_measure.py` 自己也写 `do not raise the slack`)⇒ **要裁的不是 cap,是这一类检查的归属**。

**已开 GH #843 `[bug]`**(标题:main 的 python 闸红了,而抓它的那条测试按设计跑不进 push 钩子)。
⭐ **本次按 GH #290 的顺序做对了**:先 push、再跑 `claim_precheck.sh`(`PRECHECK2_EXIT=0`,
逐字 `OK to publish: every citation resolves on origin/main.`)、再发 issue ——
⛔ 与 §九乙 那次的顺序相反,**那一次仍然记在账上**。

## 十二、交棒(接 §十二上文)

- ⑦ ⭐⭐⭐ **总监 / 协同组 / 英雄组 —— GH #843 是本轮最重的一件**:先修红
  (处方逐字在 §十),再裁「`over_per_test_cap` 且作用域是整个 `tests/` 的普查,
  归谁读」。⛔ 本台不代修、未动 `py_gate_manifest.json`、未调 cap。

## 十三、token 用量(铁律 8)

```
TOKENS total_in=12,370,831 out=53,090 turns=90
```

⚠️ 这是「到统计时刻为止」的数字,统计后的收尾回合不计入。
