# 批测台 2026-08-24T15:11:49Z

**一句话**:本轮**不发波**(连续第二轮),卡的仍是排期权不是钱 —— 但本轮买到了**为什么**的
第三个独立读数:**停摆已经结束,五条流回来了四条,唯一没回来的是总监**,而 owner P1 的 promote、
`strategy-7/-8` 的解锁、W6 的排期**三根棒全压在它一个座位上**。
另有两条账单侧的真发现:**(1) `budgets` 的 MTD 对 EC2 滞后 4.3–11.3 小时以上,上一轮据此宣布的
「三波全含 ⇒ 吻合」是建在一张不含 W4/W5 的快照上的巧合拟合,本轮撤回其算术(结论另有直接证据支持)**;
**(2) 波次走的是按需不是 spot,按需占波次账单的 63.5%($1.48/波 vs ~$0.54/波),
而 $45 围栏只剩 5.9 波按需 / 16.3 波 spot**。

零 EC2 支出(未发波);`bots/` / `game/` **逐字未动**;泄漏 **0**。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` → **worst exit 3**

| 检查 | 结果 |
|---|---|
| UNLANDED | **9**(全部是总监的 promote / rescue 链,见 §2) |
| cadence | **6 个 finding**(08-24T02:05Z 前后全队停摆,见 §2.2) |
| trunk python 套件 | **18 passed / 0 failed** |

## 1. AWS 引导

`bash tools/batch_test/aws/session_setup.sh` → 容器无 AWS CLI,脚本自行 pip 安装
(aws-cli/1.46.0),`AWS ready: arn:aws:iam::581436534924:user/dota2bot-agent`(受限用户,正确)。
之后全程 `awsx`。

## 2. ⭐ 真发现①:停摆结束了,**只有总监没回来** —— 这是本轮不发波的根因

### 2.1 promote 仍未上 main(第三个独立读数)

```
git log --oneline origin/main -1
  a5b7dab  strategy 14:04Z: record GH #157 ...

git grep -n "creeppull" origin/main -- bots/FunLib/jmz_func.lua
  origin/main:bots/FunLib/jmz_func.lua:7125:  if not J.IsSoakCandidate( 'creeppull' ) then return nil end

git grep -n "PROMOTED (was soak-candidate 'creeppull')" origin/main -- bots/
  (无输出)

git ls-remote --tags origin
  (无输出 —— stable-v1 / stable-v2 在远端都不存在)
```

闸门 `2b8b573`(1658 tests / 0 failures)落 **08-24T03:11:23Z**,距本轮 **12.0 小时**。
上一轮(12:17Z)开了 GH #155,录像组 13:00Z §0 独立复核过一次,本轮是**第三次**。

### 2.2 新增事实:cadence 的六个 finding 不是同一件事

上一轮把它整体归为「全队停摆」,当时是对的。**现在停摆已经过去**:

| 流 | 最近报告 | 距 15:11Z |
|---|---|---|
| replay-check | 2026-08-24T13:00Z | 2.2h ✅ |
| hero | 2026-08-24T13:57Z | 1.2h ✅ |
| strategy | 2026-08-24T14:04Z | 1.1h ✅ |
| batch-desk | 12:17Z → 本轮 | ✅ |
| **director** | **2026-08-23T21:00Z** | **18.2h** ❌ |

⇒ **五条流恢复了四条**。这不再是一次宿主抖动,而是**唯一能落地 promote 的座位持续缺席**。
GH #155 的「下一棒:总监」**没有接棒人在场**。已在 #155 追评并 PushNotification 给 owner。

### 2.3 它对批测台的实际约束(为什么这条属于本台)

批测实例**启动时 clone `origin/main`**。promote 不在 main ⇒ `creeppull` / `pullbeat` 仍 gated
⇒ **任何现在发出的波次,其「稳定版」腿都少两个已裁定的默认行为**,而 `test_set.md` 已按
stable-v1/v2 记账。**这是会静默测错基线的那一类不一致**,与 2026-07-23 那次「实例 clone 的是 main、
而 8 个 fix 只在本地」同族。本台不落地它(不写 bot 代码;且支线已相对 main 发散,须 rebase 不是 cherry-pick)。

## 3. ⭐ 真发现②:`budgets` 的 MTD 对 EC2 有 4.3–11.3 小时以上的滞后 —— 上一轮的对账算术撤回

### 3.1 读数

`check_costs.sh`:MTD **$36.206**,`refreshed` **2026-08-24T14:37:15Z**,forecast **64.471** / limit 100.0。
读数 ≥ $35 ⇒ 脚本自动花 $0.01 用 CE 复核:**$36.2056796871**,一致。
本轮**另花 $0.01** 拉逐日 + 逐服务拆解(理由:MTD 较上轮 +$3.179 而窗口内**零付费动作**,
这正是围栏该问的问题):

```
2026-08-18 total=0.145 | EC2-Other=0.133; S3=0.010
2026-08-19 total=8.031 | CE=0.240; EC2-Other=0.411; EC2=7.248; S3=0.051; VPC=0.081
2026-08-20 total=6.780 | CE=0.230; EC2-Other=0.291; EC2=6.162; S3=0.051; VPC=0.046
2026-08-21 total=0.328 | CE=0.170; EC2-Other=0.133; S3=0.024
2026-08-22 total=8.118 | EC2-Other=0.328; EC2=7.673; S3=0.059; VPC=0.057
2026-08-23 total=6.304 | EC2-Other=0.284; EC2=5.910; S3=0.066; VPC=0.044
2026-08-24 total=3.045 | EC2-Other=0.040; EC2=2.961; S3=0.022; VPC=0.022
```

### 3.2 撤回上一轮的「吻合」

12:17Z 报告写:MTD $33.027(`refreshed` 07:57:03Z),较上轮 +$4.565,
「窗口内付费动作恰好四笔(W3 + 抢跑四台 + W4 + W5)= $4.28,差 $0.28 = S3 杂项 ⇒ **吻合**」,
并把自己写下的诚实边界(**「若它其实没含 W5……」**)判给了「证据偏向三波全含」。

**逐日账单说那条边界才是真的,而且比它设想的更糟。** 08-24 一整天 EC2 是 **$2.961**;
07:57Z 快照里 08-24 的成分只有 **~$0.13**(EC2-Other + S3)。
**W4 00:43Z 结束、W5 03:42Z 结束**,分别比那张快照早 **4.3h / 11.3h**,**一台都没进去**。
⇒ 那 $4.565 里**根本没有 W4/W5**;凑出的「四笔 $4.28」是**巧合拟合,不是对账**。

**这条往危险方向失效**,与章程 08-21T12:xxZ 那条「shallow clone 的空输出被读成无漂移」同族:
滞后让 MTD **系统性偏低** ⇒ 连发两波时,第二波的围栏判定会拿着**不含第一波**的读数说自己还有余量。

### 3.3 即刻生效的两条纪律(已写进章程)

- **(甲)** 刚结束(< 12h)的波次**不许指望它已进 MTD**;围栏算术一律用
  `MTD + Σ(近 12h 内已发波次 × $1.48)`,**不用裸 MTD**。
- **(乙)** **不许再用「MTD 增量对得上波费」来确认一波是否计入。** 该手法已两次给出错误结论
  (12:17Z 的「三波全含」;22:07Z 的「69% 空转」—— 方向相反、手法相同)。
  要对账就用逐日 CE($0.01,按需),**并优先用 VPC/IPv4 行反解机时**(它与 EC2 行独立)。

### 3.4 副产品:GH #153 的结论由账单侧**直接**坐实(不是拟合)

用**与 EC2 行独立**的 VPC 公网 IPv4 反解机时:`$0.022 / (8 台 × $0.005/h)` = **0.550 h/台 = 33.0 分钟**
—— 与 transcript 的开波→`VALIDATE_ONSPOT_DONE`(W4 00:12:30→~00:43Z、W5 03:10:28→~03:42Z)独立吻合,
且该行无剩余成分。于是:

| 量 | 值 |
|---|---|
| 机时 | **0.550 h/台** = `--hours 2` 看门狗的 **27.5%** |
| 有效费率 | $2.961 / (8 × 0.550) = **$0.673 /台·小时** |
| 单台 | **$0.370** |
| **单波(4 台)** | **$1.480**(把口口相传的「~$1.4/波」钉死) |

跑满 2h 应记 8 × 2 × $0.673 ≈ **$10.8**,实记 **$2.96** ⇒ **「完成即关」第三次成立,这次是账单证明**;
`--hours 2 → 1` 省不到钱的结论不变。已追评 GH #153。

## 4. ⭐ 真发现③:波次走的是**按需**不是 spot,按需 = 波次账单的 63.5%(新开 GH #158)

$0.673/台·小时 = us-west-2 `c6i.4xlarge` 的**按需**牌价($0.68,1% 差来自机时取整)。
同期 spot 实测(`describe-spot-price-history`):us-west-2a **$0.2306** / 2c **$0.2388** / 2d **$0.2501**。

| | $/h | 单波(4 台 × 0.55h,含 IPv4) |
|---|---|---|
| 实付(按需) | $0.673 | **$1.480** |
| spot | ~$0.240 | **~$0.540** |

⇒ **按需贵 2.80×,每波多 $0.94。**

**不是 bug**:`spot_run.sh:44-45` 默认就是 spot,是本台主动传 `--on-demand`
(00:06Z 报告 §6.1 已记,`InstanceLifecycle=None` 四台实测确认);本轮
`describe-spot-instance-requests` 返回**空**,与此一致。
**顺手纠正一条会被误用的证据**:run_id 的 `spot_` 前缀是 `spot_run.sh:185` 硬写的字符串,**与市场类型无关**。

**为什么现在才值钱**:$45 围栏剩 $8.794 ⇒ **5.9 波按需 vs 16.3 波 spot**,而队列里排着
W6 + `strategy-7/-8/-9/-10/-11/-12` + `hero-9`。forecast $64.471 ⇒ 本月会越过 owner 的 **$50 批准档**。

**但本台不自行改**,因为反方理由是真的:镜像 A/B 的两臂是**同一台上时间先后的两个 wave**,
一次中段 spot 回收**不是「丢几局」,是丢掉整条后臂** ⇒ 4×1 拓扑下直接作废该种子的分层
(与「Radiant 侧偏 +1.5k,永远 swap-and-average」同一条约束:**缺一臂不是噪声大,是有偏**)。
三条路(A 维持按需 / B 全面转 spot / C 归因波按需、语料波 spot)与「回收后处置规则必须事先登记」
一并交总监,见 **GH #158**。**本台倾向 (C),保守默认是 (A)。**

## 5. 收割:**本轮无新 verdict**

`validation/` 最新仍是 **2026-08-24T03:42:42Z**(`cmboots_20260824_031208_0-10.verdict.json`),
与上一轮收尾时相同 ⇒ W4/W5 之后**没有任何新波次产出**(与「本台上一轮未发波」一致)。
`queue.json` 本轮**逐字未动**:无收割 ⇒ 无 status/result 可更新;`strategy-4` / `hero-8` 停在
`harvested`(它们的 (a) 核验分别在录像组手里,不由本台改判为 `done`)。

## 6. 启动决策:**不启动**(连续第二轮)—— 三条节流全过,卡的是排期权

| 节流条件 | 判定 |
|---|---|
| (i) 距上一波 ≥ 6h | ✅ 上一波 03:10:28Z ⇒ **12.0h** |
| (ii) 有新东西可测 | ✅ 当前树 `a5b7dab` 上全集累计种子数 **0 < 8**;且 main 自 `5a694f5` 后进了 `towerfear` 等改动 |
| (iii) 成本 | ✅ $36.206 + $1.48 = **$37.69 ≤ $45**(按 §3.3(甲) 加计近 12h 波次:近 12h 内**零波次**,故与裸 MTD 相同) |

**不发的五条理由**(前四条承自上一轮且仍然成立,第五条是本轮新增的决定性一条):

1. **⭐ 基线是错的(决定性)。** promote 未上 main ⇒ 现在发波 = **明知故犯地买一份错基线的语料**
   (§2.3)。上一轮这条是「掉棒」,本轮它已是**连续第二轮、闸门后 12 小时**。
2. **W6(`strategy-5b`)的 `APPROVED_CONDITIONAL` 条件仍未兑现。** §AX.2 要的事件轴量级在录像组、零 AWS;
   本轮核对录像组 13:00Z 报告,**零处**提及 `midsupyield` / 事件轴 / W6 —— 那一轮整轮花在 W5 `cmboots` 上。
   裁定原文写死「拿到之前不排发波时刻」。
3. **`strategy-7` / `strategy-8` 仍是 `DEFERRED_PENDING_W3_RESOLVE`。** 解锁条件**在事实上成立、
   在仓库状态上不成立**(总监已 resolve 但未上 main);本台**不替总监宣布解锁**。
4. **`strategy-9` 无 `director` 字段、按 4(a) 本可发,但不能单发。** `strategy-8` 的裁定明写
   `pullzone` / GH #149 / GH #143 §3 「是同一根绳子的两端,应当一起排,否则会买三次同一个域」,
   而 `strategy-9`(`creeppull,pullbeat,pullcad`)正在这根绳子上。同理 `strategy-11`(`pullcamp,pulllane`)。
5. **不援引 4(b) 例行全集波。** 队列**并非为空**(只是全被别的组卡住),§AU.2 把 W3–W6 定为连续独占波;
   在总监离线 **18.2 小时**、无法反对时,由**唯一能花钱的 agent** 单方面插一个全集波改变波次计划,
   正是 00:12Z「抢跑 18 秒也要终止重发」所维护的**同一条先例:门槛不能因为对方不在场就变得可议价**。

**上一轮那条「下游瓶颈」理由本轮不再援引** —— 录像组 13:00Z 已消化完 W5(`cmboots` (a) = WORKING),
strategy 14:04Z 也已出活。下游动起来了,**唯一的堵点是总监**。
(W4 `campgrade` 的 (a) 仍未买到:录像组本轮明确登记「W4 剩余 ~32 局补扫顺延一轮」。)

## 7. 泄漏检查:**无泄漏**

开工与收尾各扫一次,四层 + spot 请求:

| 层 | 读数 |
|---|---|
| 实例(pending/running/stopping/stopped/shutting-down) | **0** |
| 卷 | **0** |
| EIP | **0** |
| spot 请求(全状态) | **0** |
| 快照 | **1**(`snap-0ad026b386c804288`,160 GiB = AMI `ami-0a990a26d89c66547` 的底,唯一常设成本) |

本轮无活波次 ⇒ 收尾判据即「0 台在跑」,已满足。

## 8. 固定栏位

| 栏位 | 值 | 较上轮 |
|---|---|---|
| `soak/` | **184** | 184 → 184(持平,无新波) |
| `dem21/` | **36** | 36 → 36(持平) |
| `unattributed/` | **0** | 持平 |
| `validation/` 最新 | **2026-08-24T03:42:42Z** | 持平 |
| 远端 main tip | **`a5b7dab`** | `5a694f5` → `a5b7dab`(strategy 两笔 + hero 一笔) |
| MTD | **$36.206**(refreshed 14:37:15Z) | $33.027 → $36.206(**+$3.179 = 滞后到货的 W4/W5,见 §3.2**) |
| forecast / limit | **64.471 / 100.0** | — |
| $45 围栏余量 | **$8.794** = 5.9 波按需 / 16.3 波 spot | — |
| CE 花费 | **$0.02**(脚本自动复核 $0.01 + 本轮逐日拆解 $0.01) | — |

## 9. 验证(铁律 6)

本会话 `bots/` / `game/` **逐字未动**(改动仅在 `iterations/` 下)⇒ 铁律 6 无适用对象。
容器无 `luacheck` / `lua5.1`,**不声称跑绿过 Lua 全量**。
`queue.json` 本轮未修改 ⇒ 无重排噪声风险。

## 10. 交棒(铁律 9)

1. **⭐ 总监 —— promote 落地(最高优先,连续第二轮)**:rebase
   `origin/claude/busy-bardeen-u45ms4`(tip `2b8b573`)到 `a5b7dab` 后落地,并补
   `stable-v1` / `stable-v2` tag(**远端目前一个 tag 都没有**)。GH #155 已追评。
   **在它落地之前,本台不会发出任何波次** —— 不是排队,是基线错的。
2. **⭐ owner / Cursor 整合者 —— director Routine 的 cron 是否还活着**:五条流里唯一没从
   08-24T02:05Z 停摆恢复的座位,而 owner P1 的落地、`strategy-7/-8` 的解锁、W6 的排期三根棒全在它身上。
   已 PushNotification。
3. **总监 —— 按需 vs spot 的方法学裁定**:GH #158,三条路 + 「回收后处置规则必须事先登记」。
   本台倾向 (C) 混合,保守默认 (A) 维持按需。**裁 (B)/(C) 无需 harness 改动**,本台下一波即可执行。
4. **录像组 —— W6 解锁棒(第二轮催)**:`strategy-5b` 卡的事件轴量级(每局塔防 TP 响应次数 /
   其中核心占几次)在已交付的 466 个 `.dem` 里,**零 AWS**,`capmono_refusal.py` 已有
   `modifier_teleporting` 通道。本轮未做,W6 因此仍不排发波时刻。
5. **录像组 —— W4 `campgrade` 的 (a)**:本轮由录像组自己登记为「顺延一轮」,记在这里以免掉棒。
6. **本台自己 —— 已写进章程的两条纪律**:§3.3 的 (甲) 围栏用 `MTD + 近 12h 波费`、
   (乙) 禁用「MTD 增量对账」。

## 11. 本次会话 token 用量(铁律 8)

```
TOKENS total_in=3,273,736 out=37,134 turns=30
```

(统计时刻之后的收尾回合不计入。)
