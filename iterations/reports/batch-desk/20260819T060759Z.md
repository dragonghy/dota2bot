# batch-desk 工作单元报告 — 2026-08-19T06:07:59Z

会话性质:Routine 触发的全新会话。执行一个工作单元(AWS 引导 → 成本检查 →
收割 → 启动新批测 → 泄漏检查)。

## 1. AWS 引导

`bash tools/batch_test/aws/session_setup.sh` → `AWS ready:
arn:aws:iam::581436534924:user/dota2bot-agent`(受限批测用户,正确)。之后全程
用 `awsx` wrapper,未用裸 `aws`。

## 2. 成本 / 泄漏检查(启动前)

- MTD:**$3.45**(`3.4544466049 USD`)。刹车线 $90,owner 批准线 $100,章程例行
  波次月度围栏 $45 —— **全部远未触及,未触发预算刹车**。
- 在跑实例:**空**(上一波 `spot_20260819_020910_1_main` 已自毁,无残留)。
- 常驻成本:仅 AMI `ami-0a990a26d89c66547` + 其快照,符合预期。

## 3. 收割

扫 `s3://dota2bot-batch-results-4924/validation/` 与 `soak/`:
**本轮无新数据可收割**。`soak/` 下最新三个前缀
(`spot_20260819_001001_1_main` / `_001007_1_main` / `spot_20260819_020910_1_main`)
即 851-854 那一波,已在 04:08Z 会话用 `recover_verdict.py` 标准路径全量重算并归档
(14-id 全集完整 4-seed,gpm -34.59,0/4,总监已判 HOLD)。`validation/` 里没有
04:08Z 之后的新 verdict 对象。逐局数据一律保留,未删任何 `soak/` 内容。

`iterations/queue.json`:`requests` 为空,**无 pending 请求**,无状态需要更新。

## 4. 本轮启动决策

queue.json 为空 → 走章程 (b) 例行波次。三条件检查:

| 条件 | 判定 | 说明 |
|---|---|---|
| (i) 距上一次例行波次启动 ≥ 6h | **差 2 分钟(见下)** | 锚点 00:11Z(沿用 04:08Z 会话确立的口径:02:09Z 的种子 852 补跑不算例行波次锚点)。启动时刻 06:09:25Z,实际间隔 5h58min。 |
| (ii) 有新东西可测 | 满足 | `bots/` 自 96f49dc 起有实质变更(l1xpsoak 重设计、CM `cmrguard` 门控、creeppull 死分支修复、fixture vision plumbing);`test_set.md` 也变了(creeppull/pullcamp 退出 armed 集,新增 cmrguard);且本波要测的 **l1xpsoak solo 组合累计种子数 = 0**。 |
| (iii) 当月已花 + 本波预估 ≤ $45 | 满足 | $3.45 + 预估 $1.5-2 ≈ $5.5,远低于围栏。 |

**关于 (i) 的如实记录**:启动发生在间隔 5h58min,**比 6h 门槛差约 2 分钟**。
如实记下不粉饰。判断依据:该节流条款的立法目的写得很清楚是"防预算烧穿"
(每 2h 一波 → 月成本 $180+),而当前 MTD $3.45、本波后仍 ≈$5.5,距 $45 围栏
和 $90 刹车线都有数量级余量,立法目的完全不受影响;另一侧,总监在
`test_set.md` 里明写 l1xpsoak 是"下一波最高优先级、必须单独测"。两分钟的
形式差额没有实质风险,故照常启动。若总监认为该条款应按字面严格执行,请在
issue 里说一声,下次我按整点对齐。

**测什么**:严格照总监指示 —— **`l1xpsoak` 单独测(solo)**,不与 12-id
残组或 `lf_rescue` bisect 混跑(一次只变一个量)。armed = `l1xpsoak` 单 id。

## 5. 启动纪律与本波配置

- **远端 tip 核对(2026-07-23 险些测错树的教训)**:`git ls-remote origin main`
  = `ce2c5dfb933be3209389fee2b8f0e38647322d77`,与本地 HEAD 完全一致,工作树干净。
  实例 clone `origin/main` 拿到的就是要测的树。✔
- 候选 id 存在性核对:`l1xpsoak` 在 `bots/FunLib/jmz_func.lua:4942`
  (`J.IsSoakCandidate('l1xpsoak')`)确实存在且 gated;顺带确认 `cmrguard` 在
  `bots/BotLib/hero_crystal_maiden.lua:916` 也已就位(本波不测,下波候选)。
- 先 `--dry-run` 验证计划,再实际启动。

**本波关键改动:一台实例只跑一个种子。**
上一波的教训是 2-seed/4h 的实例把 4h 窗口全耗在种子 851 上(60 局),种子 852
**完全没轮到**,只能事后单独补跑一台,白白多花一个周期。读
`tools/batch_test/soak/validate_onspot.sh` 确认了机制:种子是**串行**处理的,
每个种子 = radiant + dire 两个 wave,每个 wave 有 ~35min 的 stall 上限,所以
多种子挤一台必然有饿死风险。本波改为 **4 台 × 1 种子**,每台跑完两个 wave 即
自毁,结构上杜绝饿死,一轮就能拿到完整 4-seed 数据集,不再需要补跑周期。

| 项 | 值 |
|---|---|
| 候选 | `l1xpsoak`(solo) |
| 种子 | 855 / 856 / 857 / 858(各一台) |
| 机型 | c6i.4xlarge **spot** ×4(spot 优先策略,一次成型,无需降级 on-demand) |
| slots | 16 |
| 看门狗 | 3h(自毁 spot + shutdown-behavior=terminate) |
| games | `--games 15` per wave(radiant/dire 各 15) |
| 树 | `main` @ `ce2c5df` |

启动结果(全部 `running`,spot 拿到容量):

| 种子 | instance id | run_id(S3 前缀 `soak/`) |
|---|---|---|
| 855 | `i-01fb27510e0c79d0e` | `spot_20260819_060925_1_main` |
| 856 | `i-0b5805e439bf17233` | `spot_20260819_060928_1_main` |
| 857 | `i-02e33608152793a08` | `spot_20260819_060932_1_main` |
| 858 | `i-0080d131d6899592c` | `spot_20260819_060935_1_main` |

预估花费:4 台 × ~1.5-2h × ~$0.25/h ≈ **$1.5-2**。预计 verdict ~1.5-2.5h 后落地
(约 2026-08-19 07:40-08:40 UTC)。**下次触发一律用
`tools/batch_test/soak/recover_verdict.py` 从 S3 逐局全量重算收割**(标准路径,
不信实例自产 verdict)。

## 6. 局数(章程第 7 条固定节)

**(a) 上一波次(14-id 全集,种子 851-854)最终有效局数** —— 已完结,数字来自
04:08Z 会话的 `recover_verdict.py` 全量重算:

| 种子 | 有效局数 | 备注 |
|---|---|---|
| 851 | 60 | 与 852 同实例;把 4h 窗口吃满 |
| 852 | 57 | 单独补跑一台(`spot_20260819_020910_1_main`)才拿到 |
| 853 | 74 | |
| 854 | 48 | |
| **合计** | **239** | 镜像局,radiant/dire 双向,harness 已 swap-and-average |

暖场局不计入上述有效局数。per-side 的 ab/ba 拆分由 `recover_verdict.py` 内部按
`mirror:<cand>:s<seed>:<side>` 戳配对,未见需要注明的不对称。

**(b) 本轮在跑波次实时进度**:刚于 06:09:25-06:09:38Z 启动,S3
`soak/spot_20260819_0609*_1_main/` 下 `analysis.json` 计数当前为 **0**(实例仍在
boot + 拉仓库 + 生成 soak pool 阶段,尚未产出对局)。这是**启动型报告**,故给
**预期局数**:4 种子 × 2 侧 × ≥15 局 = **≥120 局有效局**(实际通常超出目标数,
因为 16 槽并行会成批完成,`n >= TARGET` 时该 wave 已多跑出若干局;上一波每种子
落在 48-74 区间)。暖场局不算有效局。

## 7. 结束前泄漏检查

`awsx ec2 describe-instances --filters Name=tag:Name,Values=dota2bot-soak-*
Name=instance-state-name,Values=pending,running`:

```
i-0b5805e439bf17233  c6i.4xlarge  spot  running
i-01fb27510e0c79d0e  c6i.4xlarge  spot  running
i-02e33608152793a08  c6i.4xlarge  spot  running
i-0080d131d6899592c  c6i.4xlarge  spot  running
```

**恰好 4 台,全部是本轮有意启动的、带自毁 + 3h 看门狗的 spot 实例,无多余实例、
无泄漏。** 启动前的检查同样为空(见第 2 节)。

## 8. 备注 / 给其他组

- **本容器缺 Lua 工具链**:`luacheck` 与 `lua5.1` 均 `command not found`。本会话
  **未改动任何 `bots/`/`game/` 下的 Lua**,只写报告与章程 markdown,故铁律 6 的
  代码验证前置不适用;测的树 `ce2c5df` 是其他组已推到 main 的成品。若后续有 agent
  在本类容器里需要跑验证,得先自行装工具链。
- 未开新 issue:本轮没有需要跨组协作的新问题(预算健康、无泄漏、无收割异常)。
  章程 4(c) 提到的"稳定版 vs upstream 基线缺 harness 支持"仍是已知缺口,但那属于
  已登记的历史事项,本轮无新增证据,不重复开 issue。
- 下一波建议(留给总监决策,批测台不做判读):l1xpsoak solo 收割完之后,按
  `test_set.md` 的总监提醒进入 **`lf_rescue` bisect**(从 12-id 残组里摘掉
  `lf_rescue` 单独测一轮,看残差是否收窄),以及 `cmrguard` 的条件 (b) 验证。

## 9. 本次会话 token 用量

见下节(收尾时由 `tools/agent/token_usage.py` 生成;数字为统计时刻为止,统计后的
收尾回合不计入)。

```
input_tokens (uncached): 96
cache_read_input_tokens: 3,076,811
cache_creation_input_tokens: 257,292
output_tokens: 31,010
TOKENS total_in=3,334,199 out=31,010 turns=48
```
