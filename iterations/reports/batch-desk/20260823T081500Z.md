# 批测台 2026-08-23T08:15Z —— 收割 06:11Z 的 25-id 波:赤字翻倍,而唯一变的串位是 `itemtrip`

**工作单元**:开工自检 → AWS 引导 → 成本/泄漏检查 → **收割 06:11Z 波(8 条必查项照做)**
→ **补交欠了 37 轮的档案侧分母(hero-1..hero-4)** → 泄漏复查 → 报告/章程/issue。
**不启动新波**(节流门 12:11:11Z 未到,现 08:15Z)。**本轮 AWS 增量支出 $0**(只有 S3 GET)。

---

## 1. 一句话

06:11Z 那波(25 id、树 `7159ff3`、282 有效局)交出 **gpm −93.30 / xpm −96.97 /
deaths +0.71 / last_hits −6.16 / winrate 0.243,五指标全 0/4**;上一波(24 id、树
`cde1d6c`、278 有效局)是 **gpm −42.49 / winrate 0.394**。**赤字翻倍还多,胜率从
「四局输六」掉到「四局输三」**。两波之间**armed 串只差一个 id:`itemtrip`**
(树也漂了,见 §4 的诚实边界)。**归因不由本台裁**,但这次「不可归因」的形状
和历轮不同:**变量只有一个**,§6 把可选项连代价摆给总监。

**第二件事,与本波读数无关但更硬**:补做档案侧普查时发现 **Axe 在 306 局里出现 0 次**
(Death Prophet 同)。Axe 是五个焦点英雄之一 ⇒ **它的任何改动在现行四种子下结构上测不到、
也录不到**。hero-2 请求的 Culling Blade 域扫描在这份语料上**分母为 0**,不是稀有是不存在。

---

## 2. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` → **worst exit 3**。
- **UNLANDED 2 条**:`894efa6` / `7bb6711`(英雄组 08:00Z,GH #136,WK 魔棒),
  在 `origin/claude/vibrant-heisenberg-hdsuf8` 上。**不属批测台,只转述不接管。**
  (提交时间 07:46/07:54Z,离自检不到 20 分钟 ⇒ 大概率只是还没同步 main,不是掉棒。)
- cadence **clean**;trunk python **15 passed / 0 failed**。

## 3. 成本与泄漏

| 项 | 开工 08:06Z | 收尾 08:15Z |
|---|---|---|
| running/pending 实例 | **0** | **0** |
| 游离卷(status=available) | 0 | 0 |
| 快照 | 1(`snap-0ad026b386c804288`,160GB,唯一常设成本) | 同 |
| EIP | 0 | 0 |
| open/active spot 请求 | 0 | 0 |

**无泄漏。06:11Z 起的四台 on-demand 已自毁完毕**(2h 看门狗 + `shutdown-behavior=terminate`;
08:06Z 查时已经全空,说明它们是跑完 `--games 22` 自然收工,不是被看门狗砍的)。

**MTD $23.578**(免费 `budgets`,forecast 23.139,limit $100)。`$45 / $90 / $100` 三线全未触及,
**未花 $0.01 调 CE**。**读数仍是零信息**:`budget refreshed` = `2026-08-22T21:26:04Z`,与
02:06Z / 04:06Z / 06:11Z **逐秒相同** ⇒ 这是同一份快照的第四次复读。
**可证伪形式续期**:06:11Z 那波(~$1.5)与 00:11Z 那波(~$1.5)都还没入账;
**晚于 06:11Z 的第一次真刷新应 ≈ $26.6**(此数与 06:11Z 报告给的相同,本轮未开波故不变)。

**固定栏位**:`soak/` **168**(+4 = 本波四个前缀)、`dem21/` **20**(+4)、
`unattributed/` **0**(该前缀不存在)、`validation/` 最新条目仍 **2026-07-23**
(标准路径走 `recover_verdict.py`,陈旧是**预期**)、远端 main tip 开工/收尾均 **`498bad4`**。

---

## 4. 收割:8 条必查项逐条兑现

波次:`spot_20260823_0611{11,13,15,18}_1_7159ff3…_{89ea04,c05658,902447,05202d}`,
seed 888/895/896/906,4 台 × 1 种子 on-demand c6i.4xlarge,16 槽,`--rec-slots 12`,
2h 看门狗,`--games 22`。下载 77/79/77/73 = **306** 个 `*.analysis.json` + **237** 个
`*.demclaim.json`(必查项 ④:两者**一起下**,`rec_slot_cost.py` 才不会因 `sidecars=0` 拒答)。

| # | 必查项 | 结果 |
|---|---|---|
| ① | 暖场 stamp 全部 = `7159ff3` | **✅ 24/24**(逐 run 6/6/6/6)。**钉 SHA 这条路没有洞** —— main 在本波跑的两小时里从 `7159ff3` 漂到 `498bad4`(含 `bots/` 改动),四台各自 clone 时刻不同,`--ref main` 就会把两棵树混进一次镜像 A/B。 |
| ② | 合并必须带 run 前缀,且与逐 run 单跑逐位一致 | **✅**。带前缀合并 306 局;四个 per-seed 读数与逐 run 单跑**逐位相同**(−122.10/−119.94/−105.72/−25.46)。 |
| ③ | S3 真实 cand 串 vs §AS.0 逐字节 | **✅ IDENTICAL**,**count = 25**;`itemtrip` **首次上机**;`wandlimbo`/`odaoe` 各 **0** 次。 |
| ④ | demclaim 与 analysis 一起下 | **✅ 照做**,237 个 sidecar 全在,`rec_slot_cost.py` 未拒答。 |
| ⑤ | `.dem` 数 `dem21/` 不数 `soak/`,且别锚 `\.dem$` | **✅ dem21 = 60/60/60/57 = 237;`soak/` = 0**。**本轮踩到一个新坑并当场改正**:`grep -c '\.dem'` 会把 `*.demclaim.json` 数进去(第一次跑 `soak/` 得 60,长得像「录像放错了目录」),必须用 `grep -E '\.dem(\.gz)?$'` 锚行尾。 |
| ⑥ | `--rec-slots 12` 验收 + 回退触发器 | **✅ exit 0,0 recording slot beyond tolerance** ⇒ 按 §AS.2 事先写死的触发器,**下一波留 12,不退 8**。净残差(已扣 box factor 1.035)slot1 −2.8%、slot2-12 +3.8%~+6.2%(正 = 比趋势更快),slot13-16 对照腿 ±0.4% 以内。**§AS.2 自己写下的那个真代价也量到了**:对照腿拟合点 8 → **4**;`--min-games` 那道门**过得去**(4 个对照槽各 n=19~20,均 ≥ 阈值,工具正常出数)。吞吐代价第三次**测不出来**:录制槽完成 19~20 局,对照槽均值 17.25(**+1.75~+2.75 局**)。 |
| ⑦ | 12 录制者下重测归属歧义 | **✅ 且这次歧义真的更难了**:`candidates` 众数 **11**(8 槽那波是 7-8),最小 3,最大 12;`by_logname == by_hostname` **237/237**,`hostname_hits` 恒 **1**;`method` 全 `logname`。**`by_mtime` 0/237 全错**(第三次实测),`dem_claim.sh:144` 的 mtime 护栏第三次被证明必需。 |
| ⑧ | `winner_by` 分布(**本报告 §3.3 的可证伪预测:再次 `engine 0`**) | **✅ 预期兑现,不是新信息**:`economy_10min_cap 305 / economy_forcewin_recovery 1 / engine 0`,`winrate_independent_of_gold: 0/306`。旁证也对上了:局时 min **9.1** / med **11.0** / max 15.2,**只有 1 局 < 9.5 分钟**,正是落进 `economy_forcewin_recovery` 的那局。⇒ GH #135 的构造性论证成立,`winrate` 仍是 gpm 的符号化粗读,**不是第五个独立证人**。 |

## 5. 读数

**282 有效局 + 24 暖场 = 306**,四种子两 wave 全齐,`unfinished 0`。
radiant 166 : dire 116(dire 侧仍偏少,与历轮同形)。

```
mean:  gpm -93.30   xpm -96.97   deaths +0.71   last_hits -6.16   winrate 0.243
comps_better: 0/4 0/4 0/4 0/4 0/4      suggested: hold_or_reject
per-seed gpm: 888 -122.10 | 895 -119.94 | 896 -105.72 | 906 -25.46   (sd 45.81, SE 22.90)
per-seed wr : 888  0.228  | 895  0.131  | 896  0.176  | 906  0.439   (中性 0.5, sd 0.136)
```

**四个种子同号,且三个种子的 winrate 在 0.13~0.23** —— 按 owner 铁律 2(b) 的粗粒度口径,
这已经不是「无明显负面」的边缘情况:**armed 侧四局输三**。

### 5.1 与上一波的对读,以及它的诚实边界

| | 00:11Z 波 | 06:11Z 波 |
|---|---|---|
| 树 | `cde1d6c` | `7159ff3` |
| armed 串 | **24 id** | **25 id**(+`itemtrip`) |
| 有效局 | 278 | 282 |
| gpm | **−42.49** | **−93.30** |
| winrate | 0.394 | **0.243** |
| `--rec-slots` | 8 | 12 |

**§AF.3「跨波残差比较整门作废」仍然管用,我不拿这张表当判据。**
但两点使它不能被扔掉:
(a) 表里的量是**波内自比的差**(镜像 A/B 的候选腿减基线腿),不是跨波的绝对读数;
(b) **两波之间 armed 串只差一个 id**。

**诚实边界(三条,必须和上面的表贴在一起读)**:
1. **树也漂了**。`git diff --stat cde1d6c 7159ff3 -- bots/ game/` = 7 个文件 +328/−14,
   其中 `hero_axe.lua` / `hero_crystal_maiden.lua`(CM pos_5 改 arcane boots,`9fa4898`)/
   `hero_lion.lua`(t10 翻 +20 移速)是**ungated 纯数值改动** ⇒ **两条腿同时吃**,
   不直接造成 A/B 差;但**基线变了,armed 集合与新基线的交互不是常数**。
   `jmz_func.lua` +130 / `mode_item_generic.lua` +73 是 `itemtrip` 的实现(gated)。
2. `--rec-slots` 8 → 12 **两臂同值**(镜像的两臂是同机先后两个 wave),不构成 A/B 偏置;
   §4⑥ 也再次测到录制的吞吐代价为负(录制槽反而更快)。
3. 种子 906 的 −25.46 与另外三个的 −105 ~ −122 差一个量级,`sd 45.81` ⇒
   **均值本身的不确定度就有 ±23**,「翻倍」这个词描述的是 −42 → −93 的量级差,不是精确倍数。

## 6. 交给总监的一件事(不自裁)

**问题**:这个 −93.3 / 0.243 要不要动作,以及怎么归因。三条路连代价:

- **(A) 什么都不做,下一波原样再发 25 id。** 代价 ~$1.5,但**下一波仍是 25 个 id 的合成读数**,
  同样不可归因;若读数再落在 −90 附近,只是把同一个不可归因的数买第二遍。
- **(B) 下一波退回 §AR.0 的 24 id(摘掉 `itemtrip`),树钉当时的 tip。** 代价 ~$1.5,
  **一次波次就能把变量收敛到一个**:若赤字回到 −40 量级 ⇒ 嫌疑落在 `itemtrip`;
  若仍 −90 ⇒ 嫌疑落在两波之间那 328 行 ungated 树漂移(Axe/CM/Lion 三处纯数值改动),
  那是英雄组三笔已 ship 改动的问题,比 `itemtrip` 严重得多。**本台建议这条。**
- **(C) 开 `itemtrip` 单 id 隔离波。** 代价 ~$1.5,但**违反「默认波次 = 全测试集」**,
  按章程只在「归因明确 + 全集波答不了」时才开;而 (B) 用全集波形状就能答同一个问题,
  且顺带把树漂移这条更大的可能性一起证伪/证实。**(C) 严格劣于 (B)。**

**本台不自裁的理由**:入集/退集是总监的权,`itemtrip` 是 §AS 刚批准入集的(02:51Z 批、
05:0x 补位),摘它需要总监点头。**节流门 12:11:11Z 之前给结论即可。**

## 7. 补交欠账:hero-1..hero-4 的档案侧分母(零 AWS 成本)

06:11Z 报告点名的「读法性掉棒」——`queue.json` 的 `hero-1..hero-4` 四条自述
「NO NEW WAVE NEEDED … **because the batch desk owns the archive**」,
**它们要的不是波次,是档案侧的分母**,而 37 轮没人交。本轮交了。

**语料**:06:11Z 波 306 局(其中 237 局有 `.dem`,帧通道 12/16)。

### 7.1 英雄普查(306 局,24 个不同英雄,四种子各一套固定 10 人镜像阵容)

| 英雄 | 出现局数 | 有 `.dem` | 局终等级 min/med/max | ≥7 | ≥15 |
|---|---|---|---|---|---|
| zuus | **306 (100%)** | 237 | 2 / 11.5 / 18 | 305/306 | **37/306** |
| crystal_maiden | **306 (100%)** | 237 | 7 / 11.0 / 18 | 306/306 | **12/306** |
| skeleton_king | 152 (49.7%) | 117 | 7 / 11.0 / 18 | 152/152 | 4/152 |
| lion | 77 (25.2%) | 60 | 8 / 11.0 / **14** | 77/77 | **0/77** |
| **axe** | **0 (0%)** | — | — | — | — |
| **death_prophet** | **0 (0%)** | — | — | — | — |

全阵容频次(局数/306):zuus 306、crystal_maiden 306、lina 233、jakiro 229、
slardar 156、necrolyte 156、tidehunter 154、bristleback 152、skeleton_king 152、
viper 150,其余 14 个各 73~79(= 单种子专属)。

### 7.2 对四条请求分别是什么意思

- **hero-2(Axe `nKillDamage` 25 点带)**:**分母为 0,不是稀有是不存在。**
  这份档案里没有一局有 Axe ⇒ 请求正文的「count Culling Blade CASTS … over turbo replays」
  在现行四种子下**结构上买不到**。这条不能标 done,也不该继续挂在「等批测台扫档案」上 ——
  **它等的是一个含 Axe 的种子**,那是波次配置问题(见 §8 的 GH issue)。
- **hero-4(Lion t15,Hex −2s)**:请求 06:00Z 自己更新过口径「**only frames at level >= 15
  are in domain at all**」。**77 局 Lion 的局终等级上限是 14,0/77 到 15** ⇒
  **t15 的域在 Turbo 10 分钟裁判上限下是结构性空集,不是 UNDERPOWERED**。
  这与 GH #115 的「窄带上的零要记 UNDERPOWERED」不冲突:那条讲的是连续量上的窄带,
  这里是**准入条件从未满足**。part (1)(Mana Drain 按目标分类的 channel 数)仍可做,
  60 局有 `.dem`。
- **hero-3(Zeus t15 / CM `cmkeep` 预检)**:**分母最大的一条,且现在可做**——
  Zeus 与 CM **每一局都在**,237 局有 `.dem`。但同一个等级栏位提醒:
  Thundergod / Freezing Field 的 rank 3(500 / 600 蓝)只在 lv≥15 出现,
  **Zeus 37/306、CM 12/306** ⇒ 请求要的「按 rank 分档的 unaffordable RATE」里,
  **rank 3 那一档的分母很小,报出来时必须带这个数**,否则又是一次拿窄样本当密度。
- **hero-1(WK `wkqaim`)**:英雄组 15:47Z 已裁「不写」,本表只补一格分母:
  **skeleton_king 152 局 / 117 局有 `.dem`**,若将来要复核那个裁定,语料在这里。

**边界**:以上全部是**局级分母**,不是帧级命中率。帧级扫描仍是录像组/英雄组的检测器手艺
(参照 `es_aftershock_domain.py`),批测台按章程不越界写它。**但「有多少局、其中多少局有帧」
这个分母是本台的活,以后每波收割固定交付。**

## 8. 跨组交接

- **新开 GH #140 [batch]**:25-id 波赤字翻倍(−42.5 → −93.3,winrate 0.394 → 0.243),
  唯一 armed 串差异是 `itemtrip`,§6 的三条路连代价 → **总监裁,12:11:11Z 前给结论,
  未裁则默认 (A)**。(`search_issues` 搜过:#135 拿的是 `winrate` 独立性那条线,
  不是本条;故新开而非追评。)
- **追评 GH #46(不新开)** —— 「一个种子 = 一套阵容,Axe 抽不到」这条线 **08-19 录像检查组
  已经拥有**,连 `seed_draft.py` 选种工具都交了。本轮做的是:(i) 用 306 局实测**第二次验证**
  那个工具(拿它没见过的 888/895/896/906 离线算,与实测普查逐个对上,`--selftest` 仍 PASS);
  (ii) 报告它的建议 §4.1 **四天没被采纳**,Axe 又缺席 306 局;(iii) 补一条 #46 没覆盖的新轴
  —— **等级天花板**:Lion 局终上限 14 ⇒ t15 域是结构性空集,**选种解决不了它**,
  要买得抬 `SOAK_CAP_MIN`(波次配置变更)或承认只能 fixture 验。**归总监。**
  ⚠️ **这是一次险些的重复劳动**:本轮起初打算「新开 issue 报 Axe 0/306」,
  `search_issues` 才捞出 #46 —— 铁律 5 的搜重这一步本轮第二次兑现价值。
- `queue.json`:`strategy-3` → **`done`** 并附本波 verdict;
  `hero-2` 附注「分母 0,等的是含 Axe 的种子不是本台扫档案」;
  `hero-3`/`hero-4` 附注分母与等级栏位。

## 9. 下一波与必查项

**最早 `2026-08-23T12:11:11Z`**(06:11:11Z + 6h)。预置:
**串看总监对 §6 的裁定** —— 未裁则默认 (A) 原样 25 id;若裁 (B) 则 §AR.0 的 24 id;
种子 888/895/896/906;4 台 × 1 种子 on-demand;16 槽;**`--rec-slots 12`**(§4⑥ 验收通过,
触发器未触发);2h 看门狗;`--games 22` 不再上调;**钉全 40 位 SHA,不用 `--ref main`**
(§4① 本轮再次兑现:main 在本波两小时内漂了,且漂的是 `bots/`)。

**收割必查项(沿用 8 条,两条改期望值)**:
1. 暖场 stamp 全等于**届时钉的那个 SHA**(逐 run 核)。
2. 合并带 run 前缀,与逐 run 逐位一致。
3. S3 真实 cand 串与届时 §x.0 逐字节比对 + count。
4. `demclaim` 与 `analysis` 一起下。
5. `.dem` 数 `dem21/`,**且 grep 必须锚 `\.dem(\.gz)?$`**(本轮新踩)。
6. `--rec-slots 12` 第二次验收(触发器不变:非 exit 0 / 任何槽 beyond tolerance /
   一致率跌破 100% ⇒ 退 8 且不再试 12)。
7. 归属三判据逐局重核。
8. `winner_by` 的 `engine` 档:**再次为 0 = 预期兑现**;非 0 必须当场查。
9. **🆕 本波起固定交付**:§7.1 那张英雄普查表(局数 / 有 `.dem` 的局数 / 等级分位),
   四个焦点英雄一个不落 —— 它是英雄组所有「档案扫描」请求的分母,零成本,不交就是掉棒。

## 10. 验证

本会话**未改 Lua**(改动只在 `iterations/` 下:报告、章程、`queue.json`)。
容器无 `luacheck` / `lua5.1`(已复核,三个都空)⇒ 铁律 6 无适用对象。
**本轮唯一推断项**是「两波共 ~$3 尚未入账」,已按 §3 挂在 refresh 时刻上给出可证伪形式。
§5.1 那张跨波表已按 §AF.3 明确标注**不作判据**,三条边界与表贴在一起。

## 11. token 用量

```
TOKENS total_in=3,963,429 out=39,823 turns=38
```
(统计时刻之后的收尾回合不计入,量级参考。)
