# 批测台 2026-08-28T18:16:38Z —— 发 W22(四粒全起飞,但**是靠阶梯第 2 级救回来的**)

**一句话**:W22 发出去了(树 `2f20fe82`、43-id 全集、四粒种子 `975/976/977/978`),
**但这是本台历史上第一次真正走到降级阶梯的第 2 级** —— `c6i.4xlarge` 的 spot 在
**四个 AZ 全部** `InsufficientInstanceCapacity`,两粒种子(975/976)第一轮**颗粒无收**,
换 `c6a.4xlarge` 后两粒都起飞了。**代价是放置分散度掉到 2 个 AZ**(2a×2 + 2b×2):
#252 验收(≥2 个不同)**通过**,#256 验收(四个互不相同)**不通过**。
另有一处日志侧的新问题已立案 **GH #282**。

---

## 一、开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` — **worst exit 3(非阻断,唯一那处不是本台的)**:

- 两个稳定版锚点 `stable-v1` / `stable-v2`:`EXISTS` / `PINNED` / `SHIPPED` **全 ok**。
- `unlanded_commits.py`:**无**推了没落地的树。
- cadence:1 处洞 —— **`strategy` 08-28T01:24Z → 10:35Z,9.2h**,**与上两轮逐位同值**
  (同区间同读数 ⇒ **不是新洞**,是同一个洞第三轮被点名);`replay-check` **连续第三轮未被点名**。
- 未裁队列:RIDESHARE **none** / OTHER **none**;总开放请求 **37**。
- trunk 健康:python **50 passed / 0 failed / 0 uncertifiable**(上轮 49,**+1**);
  快 Lua 检测器 **17 个文件 0 失败**(持平;**FAST SUBSET,不是全套**)。
- 退出码来源由工具自报(#267):`FINDINGS (exit 3) = cadence`、`UNCERTIFIABLE (exit 2) = none`。

## 二、成本与围栏

- `budgets` MTD **$61.719** = CE **$61.7186197295**(**逐位一致,连续第十八轮**);
  快照 **`2026-08-28T14:20:52Z`** —— **与上轮同一张,本轮没有前进**;forecast / limit **88.194 / 100**。
- **围栏算术**(按铁律甲,逐波按其市场类型取价,**不做「增量对得上波费 ⇒ 已计入」的反解**):

  | 项 | 金额 |
  |---|---|
  | MTD(快照 14:20:52Z) | $61.719 |
  | W21(12:16Z 起飞,实测) | $0.747 |
  | W22(本波,spot 口径) | $0.80 |
  | **围栏值(spot)** | **$63.27** |
  | W22(全程降级按需的保守口径 $2.15) | ($2.15) |
  | **围栏值(保守)** | **$64.62** |

  ⇒ **闸 (iii) $80 通过,保守口径余量 $15.38**;**刹车 $90 未接近**;
  **本轮不跨任何新告警档,owner 不会因本台收到新的 Budget 告警邮件。**
- **W22 的成本口径说明**:四台里 **两台 `c6i` + 两台 `c6a`**,两种机型的 spot 单价不同
  (近期实测 c6i ~$0.225–0.260/h,c6a 未在本台单独计过价)。**围栏仍按 ~$0.80/波取值**,
  实测单波价**下轮收割时按 SIR 时间法结算**并作为 #233(a) 的第七个点。
  ⚠️ **诚实边界**:混机型的一波,`$/局` 与费率序列**不要直接续到纯 c6i 的序列上**
  (与 W20 同一条边界)。

## 三、收割:**空,但不是没做,是没有对象**

- `s3://…/soak/` 最新四个前缀就是 W21 那四台(树 `4b2ee334`,`…_352458`/`…_460956`/`…_11c470`/`…_add5f8`),
  **上轮 15:15Z 已定案**(208 落盘 / 185 计分 / 四粒全计分);此后**无新 run**。
- `validation/` 无新对象。⇒ `queue.json` **无 status/result 需要更新**。

## 四、⭐ 回收致盲检查(GH #271,发波前必做)

`python3 tools/batch_test/soak/reclaim_blind.py --wave-json /tmp/wave.json` ⇒ **exit 0**:

```
changeover 40.0 min  (measured bracket 34.8 < x <= 42.6)  min_arm_depth 8.0
  seed 983        44.9 min  instance-terminated-by-user        ab28 /ba14   PAIRED   [depth-checked]
  seed 986        53.9 min  instance-terminated-by-user        ab39 /ba19   PAIRED   [depth-checked]
  seed 995        42.6 min  instance-terminated-by-user        ab30 /ba12   PAIRED   [depth-checked]
  seed 1138       45.0 min  instance-terminated-by-user        ab29 /ba14   PAIRED   [depth-checked]
yield      : 4 paired seed(s) of 4 machine(s)
attribution: 0 machine(s) reclaimed before the flip
VERDICT: not blinded -- the wave delivered 4 paired seed(s).
NEXT WAVE: spot (the default; GH #158 unchanged).
```

无 `BRACKET VIOLATED`。⇒ **W22 照常走 spot**(这也是本轮实际发波的市场类型)。

⚠️ **一条诚实边界要登记**:W21 的 SIR **已经从 `describe-spot-instance-requests` 里消失**
(EC2 在请求关闭约 1h 后清掉,本轮实读**空列表**)。因此 `wave.json` 的 `create`/`update`
**是从上轮报告的存活表反推的**,不是本轮现取的 API 读数。**可核验性**:反推后工具打出的
存活时长 **44.9 / 53.9 / 42.6 / 45.0 min** 与上轮报告**逐位相同** ⇒ 分类不受影响。
**但这条方向上是危险侧的**(反推的输入永远"自洽",不会自己举手)⇒ **建议**:
收割那一轮就把 `wave.json` 落盘进 run 目录,不要留到下一轮再反推。

## 五、发波决定:**发**(§4b 例行波),三条闸逐条

- **§4a 无适用对象**:22 条 pending 逐条查 `director` —— `ROUTED_ARCHIVE_SCAN` 13 /
  `ROUTED_ARCHIVE_SCAN_CONDITIONAL` 1 / `ROUTED_ARCHIVE_REDUMP` 1 / `REJECTED` 2 /
  `RECEIVED_NOT_SCHEDULED` 2 / `DEFERRED_PENDING_W3_RESOLVE` 2;唯一 `APPROVED_CONDITIONAL`
  是 `strategy-5b`,但 `director.wave=W6`(**历史波次,非本轮排期**);**无 `director` 字段的 pending 为空**。
- **(i) 6h 闸:通过,+4 秒**。锚点 W21 首台 `12:16:34Z` ⇒ 解锁 `18:16:34Z`。
  块内 `date -u` 守卫读数:**18:11:42Z(−4m52s)`NOT UNLOCKED, refusing to launch` exit 1**、
  **18:12:13Z(−4m21s)同样 exit 1**,第三次 **18:16:38Z(+4s)`GUARD PASS`** 才进循环。
  ⇒ **实发 +4 秒,合规**(上轮的守卫原样复用,本轮又拦住两次)。
- **(ii) 有新东西可测:满足**。W21 测的树是 `4b2ee334`;此后 `bots/` 有变更 ——
  `9224ff21 hero 13:51Z: GH #279 -- the removed Boots of Bearing terminus, priced`。
  (`git fetch --depth 1 origin <40 位 sha>` 先取回再 `git log <SHA>..HEAD -- bots/ game/`,
  **exit 0,非空输出** ⇒ 是真漂移,不是浅克隆的空输出。)
- **(iii) 成本闸:通过**,见 §二。

## 六、两道发波门(都过)

**接线门** `check_armed_wiring.py --cand "$ARM" --ref 2f20fe82…` ⇒ **exit 0**:
`all 43 armed ids wired on 2f20fe82be5827231fcd3df93711b3ac67a23eaa`。

**载体门**(GH #276 的机械推导形式,`--assert-carrier-from-arm`,先落盘再取 `$?`)⇒ **exit 0**:

```
CARRIER_TERMS derived from 43 armed ids: 6 hero-scoped, 37 generic, 0 unresolved => 5 term(s)
CARRIER_GATE ids=6 seeds=4 exit=0
```

逐项满足度(**这一栏是给收割侧的**):

| id | term | satisfied | 载体种子 |
|---|---|---|---|
| `aimguard` | `spirit_breaker` | **1/4** | **975** |
| `cmrguard` | `crystal_maiden` | **1/4** | **978** |
| `liondrainstop` | `lion` | **1/4** | **975** |
| `odaoe` | `obsidian_destroyer` | **1/4** | **975** |
| `zusstatic` | `zuus` | 3/4 | 975, 977, 978 |
| `zusult` | `zuus` | 3/4 | 975, 977, 978 |

⚠️ **诚实边界(比上轮更尖)**:**`975` 一粒同时扛 `aimguard` / `liondrainstop` / `odaoe` 三个 id**。
**975 若被回收,这三个 id 这一波直接归零而不是变薄** —— 而 975 恰好是本轮**最后靠 `c6a` 才救回来的那一粒**。
这不是门的缺陷,是「4 粒 × 10 槽 vs 42 英雄池」的结构性稀疏(种子是总监 13:xxZ 替 W22 选定的)。

**载体门的 no-op 面仍在**(角色门 / 等级门的 id 表达不了)⇒ **#140 第二十九轮**。

## 七、⭐⭐ 市场与放置:**本台第一次真正走到阶梯第 2 级**

### 7.1 第一轮(`c6i.4xlarge` spot,四次调用四个 AZ)

| seed | 请求 `--az` | 结果 |
|---|---|---|
| 975 | `us-west-2a` | **失败**:`AZ RING EXHAUSTED`,四个 AZ 全 `InsufficientInstanceCapacity` |
| 976 | `us-west-2b` | **失败**:`AZ RING EXHAUSTED`,同上 |
| 977 | `us-west-2c` | 起飞,**实得 `us-west-2a`** |
| 978 | `us-west-2d` | 起飞,**实得 `us-west-2a`** |

**两种 stderr 行都出现了,按章程分开抄**:

- 环内改投(**不是降级**)—— 975 与 976 各三条:
  `! dota2bot-soak-spot-1: re-aiming inside the ring -> us-west-2c` / `-> us-west-2d` / `-> us-west-2a` 等;
- **真降级,波次级告警** —— **两条**:
  `!! dota2bot-soak-spot-1: AZ RING EXHAUSTED (us-west-2a us-west-2b us-west-2c us-west-2d) -- falling back to EC2-chosen placement, which is where #256's correlation comes back. Treat this line as a wave-level warning.`

错误码原文两种都见到:泛型的 `There is no Spot capacity available that matches your request.`
与具名的 `We currently do not have sufficient c6i.4xlarge capacity in the Availability Zone you requested (us-west-2d).`

### 7.2 阶梯第 2 级(`--type c6a.4xlarge`,仍 spot)—— **两粒都救回来了**

| seed | 请求 `--az` | 结果 |
|---|---|---|
| 975 | `us-west-2b` | **起飞,实得 `2b`**,`i-0c9f3796dc6de6f00`,run `…_af08aa` |
| 976 | `us-west-2c` | 环内改投 `2c`✗ → `2d`✗ → `2a`✗ → **`2b`✓**,`i-039ca829122b2b8d4`,run `…_daf92f` |

976 的三条 `re-aiming` 行**全部打出来了**,且第一条失败行写的就是被点的 `2c`(**对得上**)。
**没有第二次 `AZ RING EXHAUSTED`** ⇒ 阶梯**没有**走到第 3 级,**全波未使用 `--on-demand`**。

### 7.3 全波最终状态(`describe-instances` 为准,`spot_` 前缀没当证据)

| seed | 实例 | 机型 | `InstanceLifecycle` | 实得 AZ |
|---|---|---|---|---|
| 977 | `i-0c7db0594a27389d2` | `c6i.4xlarge` | **spot** | `us-west-2a` |
| 978 | `i-0a8064e73454aed22` | `c6i.4xlarge` | **spot** | `us-west-2a` |
| 975 | `i-0c9f3796dc6de6f00` | `c6a.4xlarge` | **spot** | `us-west-2b` |
| 976 | `i-039ca829122b2b8d4` | `c6a.4xlarge` | **spot** | `us-west-2b` |

- **四台全 `spot`** ⇒ owner GH #158「Spot 优先」**本轮全程守住**,一台按需都没用。
- **run_id 唯一性**:四个 `soak-run` 标签两两不同(尾 token `80d801` / `bc7f45` / `af08aa` / `daf92f`),
  **#98 的 `RUN_TOKEN` 第 N 次生效**。
- **放置分散度 2 个 AZ(2a×2 + 2b×2)**:**#252 验收(至少两个不同值)通过**;
  **#256 验收(四个互不相同)本轮不通过**。一次 AZ 容量事件的暴露面**从 1 粒回到 2 粒**。

### 7.4 ⭐ 新立案 **GH #282**:请求 AZ 与实得 AZ 不符,且**没有 `re-aiming` 行**

977 请求 `2c` **实得 `2a`**、978 请求 `2d` **实得 `2a`**,而这两个日志块里
**既无失败行也无 `re-aiming` 行**,只有一行 `launched … az=us-west-2a`。
§5 里 #256 的验收原文要求「若某台出现 `re-aiming`,日志里必须是那一行**而不是** `az=<ec2 chose>`」
⇒ **本波无法在日志上执行这条判据**。

另一处独立可疑点(**只记观察不定机制**):975 请求 `2a` 而**第一条失败行写的是 `2b``;
976 请求 `2b` 第一条失败行是 `2c` —— 两台都差一格,且失败行都只有 **3 条**(环长 4);
**而同一轮阶梯第 2 级里 976 请求 `2c` 的第一条失败行就是 `2c`(对得上)** ⇒ 两种调用形式行为不一致。
按章程**不自己改 harness**,已开 **GH #282** 并给了建议验收方式(起飞路径无条件打
`requested=<az> actual=<az>`,成功时也打)。

## 八、W22 配置(登记,收割时逐条核)

- **树 pin:显式 40 位 sha `2f20fe82be5827231fcd3df93711b3ac67a23eaa`**(不写 `main`)⇒ 本轮收尾提交
  **不会改变实例克隆的树**;`git ls-remote origin main` == 本地 `origin/main` == 该 sha ✅。
- **arm 串 43 id / 381 字节**,与 W21 **逐字一致**(`test_set.md` 第 2 行本轮未变)
  ⇒ ⭐ **W22 与 W21 是同串同拓扑,读数可以直接比** —— 这是近几波里第一次成立。
- `--slots 16 --rec-slots 12 --hours 2`,`--validate "<ARM> <seed> --games 12"`(`--games` 在串内,不放顶层)。
- **未传 `--on-demand`**;**未传 `--cand-ref`**(⇒ 收割时**也不要传**,否则会归档出一个标着
  `contrast=vs_stable` 的两臂读数)。
- dry-run 先行(`[dry-run] would launch …`,零支出),**波次预算闸未拒发**。

## 九、局数(铁律:发波轮写预期)

- **(a) 上一波 W21 定案**(上轮已结):**208 落盘 / 185 计分 / unfinished 0 / 暖场 23 / mirror 185**,
  ab/ba = 28/14、39/19、30/12、29/14,四粒全计分,`thin_arm_seeds []`。
- **(b) W22 预期**:**~208 落盘 / ~180 计分**(四台各两腿)。
  **诚实边界**:这是「不被回收时」的期望值**不是下界**;近八波有三波被回收清零。
  另加本轮特有的一条 —— **975 一粒扛三个 id**(§六),它若被回收,那三项**归零不是变薄**。
- **孤儿存量 396 局,本轮零新增**(W22 尚未产出)。

## 十、泄漏检查(两次)

- **开工**:`check_costs.sh` 的 running/pending 段 **空**。
- **收尾**:`check_costs.sh --leak-only`(零成本)⇒ **恰 4 台**,
  `i-0c7db0594a27389d2` / `i-0a8064e73454aed22` / `i-0c9f3796dc6de6f00` / `i-039ca829122b2b8d4`,
  机型与起飞时刻(`18:19:55Z` / `18:20:14Z` / `18:20:50Z` / `18:21:40Z`)**逐个对上 §7.3 的发波表**
  ⇒ **不是泄漏**。常驻只剩 AMI `ami-0a990a26d89c66547` + 快照。
- ⚠️ 作证边界**第九轮保留**;**#239 路 4 第十一次实证**。
- 四台均 `--hours 2` 看门狗 + 自毁,`instance-initiated-shutdown-behavior=terminate`。

## 十一、铁律 6

`bots/` / `game/` **一行未改**(本轮只动 `iterations/` 下的报告与章程)。
`core.hooksPath=.githooks` 已由开工自检上膛,静态门随 `git push` 自动跑;
**未使用 `RULE6_BYPASS`** ⇒ 没有「这是 SKIP 不是 pass」的行要抄。
动态半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)**未跑且不声称**。

## 十二、交棒

1. **⭐⭐ 下一轮本台 —— 收割 W22**:四个 run
   `…_80d801`(977 / c6i / 2a)、`…_bc7f45`(978 / c6i / 2a)、
   `…_af08aa`(**975 / c6a / 2b**)、`…_daf92f`(**976 / c6a / 2b**);
   **不传 `--cand-ref`**,arm 串反解应得 **43 id / 381 字节**,树 `2f20fe82`。
   **必做**:逐台记存活时长对照 ~35–40min 换腿点;照抄 `min_arm_depth` / `thin_arm_seeds`;
   被排除的种子**仍要点名**;**把 `wave.json` 落盘进 run 目录**(见 §四那条边界)。
   ⚠️ **975 若被回收,`aimguard`/`liondrainstop`/`odaoe` 三项归零**。
   ⭐ **W22 与 W21 同串同拓扑 ⇒ 这是近期第一次可以直接对比的两波**(唯一差异是机型混了 c6a)。
2. **6h 闸锚点:W22 首台 `2026-08-28T18:19:55Z`(EC2 起飞时刻)/ 发波循环 `18:16:38Z`(守卫通过时刻)
   ⇒ W23 解锁 `2026-08-29T00:16:38Z`**(按发波循环时刻取,与上两轮同法);**块内 `date -u` 守卫原样复用**。
3. **⭐⭐ 总监 —— GH #282(本轮唯一新立案)**:请求 AZ ≠ 实得 AZ 且无 `re-aiming` 行,
   **#256 的验收判据在这份日志上无法执行**;建议起飞路径无条件打 `requested/actual`。
4. **⭐⭐ 总监 —— #271 本轮拿到全新形状的语料**:这是**第一次**「起飞期容量事件」而非「起飞后回收」——
   `InsufficientInstanceCapacity` 在**四个 AZ 同时**发生,阶梯的第 1 级**真的不可达**,
   **第 2 级(`c6a`)一次就救回两粒**。⇒ (a) `c6a` 的「跑满两腿」样本本轮**有机会从 n=1 抬到 n=3**
   (975/976 若跑满);(b) #271 里「`c6a` 不是回收的解药」仍然成立(那说的是**起飞后**回收),
   **但本轮证明它是「起飞期无容量」的解药** —— 这两件事在原文里没分开,建议总监补一句。
5. **⭐ 总监 —— #252 / #256 验收本轮分岔**:#252 **通过**(2 个不同 AZ),
   #256 **不通过**(要求四个互不相同)。**触发的是环耗尽后的 EC2 自选放置**,不是发波循环没点 `--az`。
   按 08-27T19:xxZ 的重开条件,「被某 AZ 回收清零」计数**仍是 1 次(W17)**,本轮是**起飞失败**不是回收,不计。
6. 总监 —— **GH #275 仍未裁**(卡着「本装置能不能读出任何经济读数」);本轮未收割,无新语料,
   **W22 是它落地后第二份四粒候选**。
7. 总监 —— **#233(a)** 未裁;W22 将是**第七个点**,但**混机型** ⇒ 建议单独标注不并进纯 c6i 序列。
8. 总监 —— **#269** 本轮无收割未触发;**#207** `zusstatic` **连续第十八波空转**;
   **#218 后续** `salveally` / `salveyield` **连续第十四轮不在第 2 行**;**#140** 载体门 no-op 面(第二十九轮)。
9. **上轮交棒⑦ 计数器**:载体门本轮**被调用且 exit 0,没有改掉发波参数**(种子是总监预选的)
   ⇒ 「第三次改掉发波参数」**计数不变(仍 2 次)**,该提案继续挂着。
10. **录像组** —— **W21 的 185 局四粒配对语料仍是近八波最好的一份**(树 `4b2ee334`,43-id);
    W20 的 154 局三粒次之;W18-R 的 127 局两腿全可配对;**W14 的 156 份约 09-16 过期,优先消化**;
    **孤儿矿 396 局**;**W22 约 19:0xZ 起陆续落盘**。
11. **strategy 组 —— 9.2h 洞连续第三轮同值**(同区间同读数,**不是新洞**);
    replay-check **连续第三轮未被点名**。
12. 存量催办 #217(第十八轮)/ #211(第十九轮)/ #225 / #180(第二十五轮)/ #171(第二十三轮)/
    #200(第二十二轮)/ #181 / 载体门 PARTIAL(第二十九轮)/ `stable-v*` tag(第二十七轮)/
    `campdanger`(第二十二轮)/ §BL.4 机械化 / #75(第二十一轮,维持 12)。

## 十三、Token 用量(铁律 8)

```
TOKENS total_in=22,405,675 out=47,344 turns=169
```

**⚠️ 本轮 `turns=169` 明显偏高,原因要写清楚,免得被总监读成空转**:6h 闸解锁时刻
(`18:16:34Z`)落在本会话开工后 ~7 分钟,而本容器里**前台 `sleep` 被禁**、
后台任务的完成通知是唯一的等待机制 ⇒ 等解锁 + 等四次发波调用 + 等阶梯第 2 级两次调用,
**绝大部分 turn 是「读一次后台输出文件」的轮询**,不是重复劳动。
**下一轮的省法**(建议本台自己采纳):把「等到解锁 → 发四台 → 阶梯降级 → `describe-instances` 核对」
写成**一个**后台脚本一次交出去,只在它退出时读一次输出;本轮已经证明守卫块本身是可以内嵌的。
