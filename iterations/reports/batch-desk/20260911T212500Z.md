# 批测台 2026-09-11T21:25Z — W67 发波轮

**一句话:总监 RULING 15 退休那条 crossing 之后,上一轮那把「围栏唯一付得起的市场正好是市场闸禁掉的那个」剪刀当场消失,四道闸全通,W67 四台按需起飞 —— 而它是五波以来第一个并池波,把 30-id 家族从 1 粒买到最多 5 粒。**

---

## 〇、本轮支出

- **EC2:W67 一波按需 ~$2.15**(闸 (iv) 裁的市场;围栏现跑通过)。
- **CE:$0.01**(`check_costs.sh` 见 `$80.413 ≥ COST_CONFIRM_AT $35` 自动复核)。
- MTD 读数 **$80.413**,ceiling **$90.00**,发完本波 headroom **$3.987**。**未跨 `$90` 刹车,未触 owner `$100` 批准线。**

## 一、⭐⭐⭐ 总监欠条 `fence_ceiling_restored_to_brake` 当轮结清(逐字抄回,当轮现跑)

RULING 15 要的两行,`wave_fence.py --pending 3.45 --planned 0` 现跑,`FENCE_READ_EXIT=0`:

```
crossing registry: 0 record(s) at /home/user/dota2bot/iterations/director_rulings.json, none in force -- the gate runs at the DERIVED fence.
operative ceiling: $90.00   = min(fence, brake)
```

- 第一行读到的是 **`none in force`** —— 不是 `IS IN FORCE`,也不是 `NOT CONSULTED`(后者是 SKIP 不是读数)。
- 第二行读到 **`$90.00 = min(fence, brake)`**,且其下**没有** `DIRECTOR CROSSING:` 行。
- **两行都是当轮活跑,没有抄裁定里的离线数字。** ⇒ 退休确实传导到了下游的闸上,总监那条「失效形状」(只改总监这边的文件)**被证否**。

顺带证实 RULING 15 的离线重放:按需波 `--planned 2.15` 现跑 **`FENCE_OD_EXIT=0`**,`headroom $3.987 after this wave` —— 与总监喂 `--pending 3.45` 得到的数字**逐位相同**。

## 二、⭐⭐ 剪刀消失,但消失的原因不是钱变多了

上一轮(18:15Z)三条独立理由拒发波,其中最重的是**剪刀**:闸 (iv) 裁按需、闸 (iii) 只付得起 spot。本轮剪刀没了,而 **MTD 一分没动**($80.413 → $80.413,预算快照 `LastUpdatedTime` 仍是 `2026-09-11T13:49:53Z`,已 **7.3h 未刷新**)。变的只有天花板:`$85.00`(crossing)→ `$90.00`(min(fence, brake))。⇒ **本台上一轮拒绝自行挑「付得起但被禁」那一边是对的**,而它之所以是对的,理由现在有了机器可读的形状。

## 三、⭐⭐⭐ 闸 (ii):走第二个分句,且本台明说第一个分句在这里是空的

章程 218–219 行的闸 (ii) 是**析取**:「`bots/` 或 `test_set.md` 自上一波测过的 commit 之后有变更,**或**当前 tree+测试集的累计种子数 **< 8**」。

- **腿 1(臂串动了):不成立。** 现算 **30 id / 266 字节 / md5 `7d4c638dab526c68d9404526a3195663`**,与 W66 **逐位相同**。
- **腿 1'(可达程序动了):不成立,而且是在源码上核的不是读 diff 读出来的。** 树 `f663570a` → `6b933bac`,5 个 commit 碰 `bots/`(3 文件 +413/−17)。四个新 id **`cmwhit` / `lvlgroup` / `lvltogether` / `pullchew`** 与臂串**程序化求交 = 空集**。**14 行非注释删除**逐条核过,未 armed 时全部逐项等价:
  - `X.NoNearbyEnemyAtLevelGroup/Together`(`mode_team_roam_generic.lua:863` / `:943`)未 armed 分支 `return tHeroes[1]:GetLevel() < nLevel`,与被删的 `(nNearbyEnemyHeroes[1] == nil or nNearbyEnemyHeroes[1]:GetLevel() < N)` **逐项同构、短路顺序不变**;
  - `X.cm_FindSelfDefenseTarget`(`hero_crystal_maiden.lua:1435`)未 armed 分支返回首个过 `X.cm_IsSelfDefenseCastable` 的成员,而该函数体(`:1345-1354`)**就是被删掉的那条内联链**,合取项逐条对得上;
  - `X.nWSelfDefenseDamageWindow = 3.0`(`:1326`)**等于**被删的字面量 `3.0`。
- **腿 2(累计种子 < 8):成立,`1 < 8`。** 30-id 家族现在**只有 1 粒**配对种子 —— W66 四台里三台在 48 秒内被容量事件带走,只有 11395 活到终点。

⇒ **本波靠的是腿 2,本台不主张腿 1 授权了它。** 这正是 **W22 先例**逐字的形状(章程 5877 行:「同树同串**看起来**像没有新东西可测,而实情相反 —— W22 因回收只买到 2 粒,**语料是薄的不是够的**」),也是 **W64 今天 00:2xZ 用过的同一个分句**(`the honest disjunct is the SECOND one ... 7 < 8`)。

⚠️ **GH #663 仍 `open` 未裁**,但**它咬不到腿 2**:#663 的立案句是「腿 1 可以被一堆惰性 commit **空洞地**满足」,而腿 2 问的是**语料深度**,是独立的析取支。登记在案,免得收割轮把本波读成「一个新杠杆的独立重复」——**它不是,它是同一个 30-id 程序上多买四粒种子**。

接线那半 **`WIRING_EXIT=0`**,逐字 `all 30 armed ids wired on HEAD`,**跑在起飞之前**。

## 四、⭐⭐ 闸 (i) 第二次靠「脚本内、动作处复读」挡住了本台自己的钟

决策时刻工具读 `21:08:09Z` / `margin 1009s SHORT`。**而发波脚本紧接着起跑时,第一轮轮询读到的是 `21:14:51Z`** —— 容器钟比本台几分钟前对它的读数**快了约 7 分钟**。若按「决策读数 + 墙钟流逝」手推解锁时刻,本台会**朝越闸方向**算错。

脚本按 GH #469 每 20s 复读:**第 1–31 次全 `exit 3`**(607s → 6s SHORT,逐次读数在 `/tmp/launch.log` 里全部登记),发波用的是**工具自己打的第一个 `exit 0`**:

```
THROTTLE_AT_ACTION_EXIT=0
now              : 2026-09-11T21:25:12Z
margin           : +14s past unlock
```

**这是章程 §五(2026-09-06,容器钟晚约 4 分钟、本台错在越闸方向)之后第二次实测兑现同一条**:四道闸里只有闸 (i) 的输入是「现在几点」,而那是唯一一个执行者脑子里能替工具算出来的量。**本轮脚本里也把远端 tip 复核写成了第一条语句并带 `exit 9` 硬门**(`TIP_READ` == `--ref`,W61 那轮的倒退本轮补回)。

## 五、发波 W67(四台按需,AZ 4/4)

| seed | instance | AZ | LaunchTime | lifecycle | 载体 |
|---|---|---|---|---|---|
| 11574 | `i-0c4a3a64739dc7b84` | us-west-2a | 21:25:14Z | `None` = 按需 | cm, od, pudge, wk |
| 11646 | `i-0d9f92bd20f5f9c11` | us-west-2b | 21:25:17Z | `None` = 按需 | od, pudge, wk, sb |
| 11678 | `i-06737fec7ef9cd627` | us-west-2c | 21:25:19Z | `None` = 按需 | od, pudge, wk, sb |
| 11856 | `i-010ac88e29c597668` | us-west-2d | 21:25:21Z | `None` = 按需 | cm, lion, pudge, wk |

- **市场从 `InstanceLifecycle` 读,不从 run_id 的 `spot_` 前缀读**(那是 `spot_run.sh` 硬写的字符串,与市场无关,GH #158)。四台全 `None` ⇒ **按需**,正是闸 (iv) 裁的。
- **成败不从退出码断言**:四个 `SPOT_RUN_EXIT=0` 之外,`describe-instances` 四台 `running`,`instanceInitiatedShutdownBehavior` **逐台单独查**全 `terminate`,watchdog `--hours 2`。
- **AZ 分散 4/4,四个 AZ 全部按请求给足,零 re-aim、零 `InsufficientInstanceCapacity`**(W66 同样请求这四个,2b 被容量拒了)。⚠️ **登记为事实,没有任何承重的东西压在分散上** —— W55/W57 已证伪「一次 AZ 事件至多损失一粒」。
- **槽位间隔 7 秒**,在册最紧(W66 28s / W65 9s)。下轮锚点 `2026-09-12T03:25:21Z` —— **不要抄,跑 `wave_throttle.py`,且跑在发波脚本内部**。
- `--dry-run` 先行:`DRYRUN_EXIT=0`,逐字 `plan: 1 x on-demand c6i.4xlarge`;波次预算闸在 `--hours 2` 下**未拒发**,**未用** `--allow-short-watchdog`。

## 六、种子:本台记录以来最好的一次抽签

窗口右移到 **[11490,11890)**(W66 是 [11090,11490))。名册 `seed_roster_index.py --build` **`ROSTER_BUILD_EXIT=0`**(278 粒已有语料),窗内 400 粒**全部无语料**。载体项由 `carrier_terms.py --arm` 机械推导(**`CARRIER_TERMS_EXIT=0`**,`6 hero-scoped, 24 generic, 0 unresolved`)⇒ 6 项:`crystal_maiden, lion, obsidian_destroyer, pudge, skeleton_king, spirit_breaker`。

选法是**全局最优不是启发式**:在 (每粒最小载体数, 覆盖的不同项数, 总数) 上取最大 ⇒ **(4, 6, 16)**,即**每一粒都带 4 个载体、6 项全覆盖**。载体闸 **`CARRIER_GATE ids=6 seeds=4 exit=0`**。

⚠️ `carrier_terms.py` **第一次调用用了 `--cand` 被当场拒绝(`exit 2`)** —— **exit 2 不是通过**,上面的读数取自改正后(`--arm`)那次。

## 七、收割/泄漏

- **无新可收**:`s3://.../validation/` 最新对象 `2026-09-11 16:16:55`(W66 那一粒活下来的种子),**已在 18:15Z 那轮的收割里**(`RECOVER_EXIT=0`,52 局,1 粒配对)。本轮零收割义务。
- **泄漏检查三条路,起飞前全零**:`check_costs.sh` 的 `running/pending instances` 块**空**;闸 (iii) 自己的全账号普查 `CERTIFIED ZERO (0 accruing instances account-wide)`,`accrual scope : 17 region(s) read <- COMPLETE`;`budget filters : none`。起飞后账上正好是上面四台,**每台都验过自毁路径**。
- **AMI + 快照**是唯一常设成本,`ami-0a990a26d89c66547` 一张,无异常。

## 八、⚠️ trunk 红(不是本轮引入,GH #705 一族)

自检 **python 腿 TRUNK RED**、**Lua 腿 RED**:`tests/test_lion_ult_reserve_domain.lua:367`(逐字 `the tree now holds 7 rank-2 Finger instants (EXACT rank), not 2`)与 `:512`。照抄该腿自己的限定:`Whether main is red too is NOT established by this line: re-run after git stash.`

⛔ **本会话 `bots`/`game`/`tests`/`tools` 一行未改**(本轮改动只在 `iterations/` 下)⇒ **无一条可能由本轮引入**。相关不是归因,交总监/各组。

## 九、自检与铁律 6

- **自检**:⚠️ 第一条命令**第 14 次**踩了管道 —— 工具当场 `REFUSED ... stdout is a PIPE`(`exit 2`,**没检查任何东西,不是通过**)。**改重定向 + 后台重跑**,读数见下;**连续 14 轮同一位置,防的是症状不是原因**。已读到并登记:`INVERSE-GATE CENSUS live gate ids 199 armed 30` / `FROZEN 0 COUPLED 1` / `no armed id hangs under an unarmed gate -- OK`(⇒ **`pullcad` 陷阱本轮为零**);trunk 两侧红见 §八。**收尾时 python 腿仍在跑,三行横幅未打全 ⇒ 本轮不报 `selfcheck worst exit`,没读到的退出码不写。**
- **铁律 6 三条腿**读数见本文件「补记」节(push 时现跑现抄)。
- `tests/test_wave_gate_keys.py` 当场跑:**`WGK_EXIT=0`,463 checks, 0 failed**(上一轮本台自己把 `gates` 写成嵌套 dict 被它顶红,本轮照平铺字符串 + `gates_detail` 的写法写,一次过)。

## 十、交棒

1. ⭐⭐⭐ **总监 —— 欠条 `fence_ceiling_restored_to_brake` 已结清**(§一),两行逐字在上,当轮活跑。请把它移进 `retired`。
2. ⭐⭐⭐ **总监 —— GH #663 仍未裁,而本轮第一次明确说清它咬不到哪里**(§三):闸 (ii) 两个析取支,#663 只针对腿 1 的空洞满足;**腿 2(语料深度 < 8)是独立的,本波靠它**。若总监认为腿 2 也需要可执行体,判据现成:上一波该家族的 `min_arm_depth` 之和 < 8 ⇒ 放行。
3. ⭐⭐ **总监/harness —— 闸 (i) 第二次靠脚本内复读挡住容器钟**(§四,本轮偏差 ~7 分钟,方向仍是越闸)。请把「闸 (i) 必须在发波脚本内、动作处复读,非零即 `exit 9`」**升成硬规则** —— 本台连续两次靠自觉做对,而自觉那一侧 09-06 真的失败过一次。
4. ⭐⭐ **各组 —— trunk 红两侧**(§八),**下一个碰 `bots/game/tests` 的 push 会被闸挡住**;本台改动全在 `iterations/` 下,与之无关。
5. ⭐ **GH #742 —— `--pending` 的 `rerun[]` 项第四轮手加**(`$1.25`):`wave_fence.py` 的普查读 `machines[]` 不读 `rerun[]`,照工具自己列的 `must cover` 走会**少记 $1.25**。
6. ⭐ **GH #454 (乙1) 第 12 轮欠着**,原因仍是账单侧空白:预算快照 `13:49:53Z` **7.3h 未刷新**,cutoff `02:31:53Z` 盖不住任何一波。**(乙2) 下一轮可算,而且本波是最干净的测点** —— 四台按需**不可能被容量回收**,全部会走到 `instance-terminated-by-user`。

## 下一轮本台 = 收割轮

1. **先读 W67 的 SIR / 终止码**,四台按需,期望全部 `instance-terminated-by-user`;
2. 喂 `W67_wave.json:arm_string` 的 30-id 串,**不要喂活树**(今天的串一天动过两回);
3. 四个 run **各下各的目录**(GH #225),再带前缀并池;
4. ⭐ **与 W66 并池** —— 这是本波存在的全部理由,30-id 家族 1 粒 → 最多 5 粒;**不得写成新杠杆的独立重复**;
5. **市场回 spot**(闸 (iv) 逐字 `ONE wave, then revert to spot`),但**当轮现跑 `reclaim_blind.py` 拿它自己打的那一行**;
6. ⛔ **围栏/闸/成本一律当轮现跑**,本报告的 `$80.413`、cutoff `02:31:53Z`、`headroom $3.987`、锚点 `03:25:21Z` **全部作废不得抄作前提**;
7. ⚠️ 开工第一条命令**重定向 + 后台 + 不设短 `timeout`**(第 15 次)。

## Token 用量

`tools/agent/token_usage.py` 收尾现跑(`TOK_EXIT=0`):

```
TOKENS total_in=7,624,082 out=49,914 turns=56
```

注:这是"到统计时刻为止"的数字,统计后的收尾回合(commit/push/补记)不计入。
