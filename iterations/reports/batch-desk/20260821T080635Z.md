# batch-desk 2026-08-21T08:06:35Z — 计费滞后落地($15.00 → $18.28),并把「查花费」这件事本身的花费砍掉

**一句话**:五轮逐位一致的 `$14.9965` **不是稳态,是滞后中段** —— 08-19/08-20 两天的
批测机今天一次性入账,MTD 落到 **$18.2767**;同一次拆账查出两件此前没人量过的事:
**(1) 我们自己查花费的 Cost Explorer 调用是 $0.01/次、$0.24/天、~$7.3/月**,
**(2) 账上那个「$50/月」的 AWS Budget 实际是 $100/月**。两件都已就地修掉/更正。
**本轮不启动任何批测**(总监 §W.5 三条解除条件仍全未满足,§AA.4 已明写)。

---

## 1. 成本与泄漏

| 项 | 值 |
|---|---|
| MTD(收割时) | **$18.2767015777**(CE)/ **$18.277**(Budgets,免费,两者一致) |
| 上一轮记录 | $14.9965386345(22:18Z 起连续五轮逐位一致) |
| 变化 | **+$3.28**,全部来自 08-19/08-20 的补账,**不是新支出** |
| 预测月末 | $21.495(Budgets forecast) |
| 在跑实例 | 开工 **0 台**、收尾 **0 台**,**无泄漏** |
| 刹车线 | $45 围栏余 **$26.72**;$90 刹车线余 $71.7;均未触发 |

**逐日拆账(本轮唯一花钱的诊断,$0.02)**:

| 日期 | 合计 | EC2-Compute | 说明 |
|---|---|---|---|
| 08-01..08-18 | $0.1443/天 | — | 全是 AMI 快照的常驻成本(`EC2 - Other` $0.1334/天 ≈ $4/月) |
| **08-19** | **$8.0312** | **$7.2478 / 14.62 机时** | 当天 8 波共 ~30 台 |
| **08-20** | **$6.6469** | **$6.1621 / 9.47 机时** | |
| 08-21 | **CE 上还没有这一天的行** | — | 今日零批测 |

⇒ **全月的批测支出集中在 08-19/08-20 两天($13.41)**;08-19 期间报的 `MTD $3.4651`
是纯滞后幻觉。**滞后现在基本追平了**(两天都已完整入账,此后无波次),
$18.28 可以当作接近真值的数字用。

**经验单价(第一次用账单反推,而不是估)**:14.62 机时 / ~30 台 = **0.487 h/台**
(与 14:12Z 立的「一波实际只要 ~30 分钟」吻合),c6i.4xlarge on-demand $0.68/h
⇒ **$0.33/台 + EBS/公网 IP 约 $0.01 ⇒ 4 台一波 ≈ $1.35-1.40,8 台两臂 ≈ $2.7**。
⇒ **总监 §W.5 引用的 $1.56/波是对的(略保守);而本章程 08-19 那几条里写的
「4 台预估 $0.6-0.8」是 ~2 倍偏低** —— 今后排期一律用 **$0.35/台**。

## 2. 收割:无新数据(固定栏位全过)

- `validation/` 最新对象仍是 **08-20 18:42:06**;`soak/` 最新前缀仍是
  `spot_20260820_180808_1_11a8de33…` ⇒ **20:10Z 之后无任何新对象**。
- `queue.json` 的 `requests` **为空** ⇒ 走章程 4b。
- 固定栏位:off-roster 新增 **0**;`soak/*/unattributed/` **0 个对象**;
  `dem21/` **0 个对象**(与「没跑过 16× 波次」一致,04:14Z 立的必查项);
  `.demclaim.json` **仍无 sidecar**(`logname`/`hostname` 两条链**仍未被真实数据验证**,
  连续第四轮)。

## 3. 启动决策:**不启动**

例行三条件 (i)(iii) 形式上满足((i) 距 08-20T16:09:02Z 已 **16h**;(iii) $18.28 + $1.40 ≪ $45),
**但 §X.0/§Y/§Z/§AA 的「无目的不启动」仍压着**,§W.5 三条解除条件逐条复核:

- **(甲)** #75 吞吐测量按 §W.1 只能搭车,不能自己当理由开波 —— 未满足;
- **(乙)** `capmono` HP 轴已被 §Z.1 复核为 NOT-PROMOTE 不变 —— 未满足;
- **(丙)** 无 id 过 §V.7 三个数;英雄组 08:15Z 交的 `odaoe` 被总监 §AA.2 **暂缓入集**
  (承重子句 `od_eclipse_aoe_domain.py:257` 跑在 #78 判死的 `hp>0` 代理上,
  等两条零支出的列)—— 未满足。

保守默认:**不自行改被测集合、不自行换种子、不为了「有额度」而开波**。
总监 §AA.4 已独立写明「下一波仍不启动」,与本轮判断一致。

## 4. 交付一:**把「查花费」的花费砍掉**(本轮最实的一件)

**发现**:账单上有一条从没人读过的服务行 —— **`AWS Cost Explorer`,08-19 $0.2400(qty 24)、
08-20 $0.2300(qty 23)**。CE 的 `get-cost-and-usage` 是 **$0.01/请求**。
本章程步骤 2 + 步骤 6 各调一次 `check_costs.sh`,12 轮/天 ⇒ **恰好 24 次/天**,数字对得上。

**量级**:**$0.24/天 = ~$7.3/月 = $45 围栏的 16%**,且**在零批测的日子里它是最大的一条
边际支出 —— 比 AMI 快照的常驻成本($4/月)还高 1.8 倍**。
⇒ **02:06Z / 04:14Z / 06:12Z 那三轮报的「零支出」字面上不成立**:每轮实际花了 ~$0.02
在「确认自己没花钱」上。这条更正我先记在自己头上。

**修法**(`tools/batch_test/aws/check_costs.sh`):
1. **MTD 默认改读 `budgets describe-budgets`,免费**。同一时刻交叉验证:
   Budgets `18.277` vs CE `18.2767015777` —— 一致;**且 Budgets 更新时刻 06:37:31Z,
   而 CE 连当天的行都没有 ⇒ 免费的那个反而更新鲜**。
2. **`--leak-only`:收尾那次完全不查花费**。理由不是省钱而是**信息为零** ——
   22:18Z→06:11Z **连续五轮 MTD 逐位一致**,同轮第二次读数从来没带过任何信息。
3. **近刹车线自动升级**:读数 ≥ `$COST_CONFIRM_AT`(默认 **$35**,压在 $45 围栏之下)
   时自动花 $0.01 用 CE 复核 —— **免费读数只在离刹车线远的时候够用**。
4. `--ce` 强制付费读(要逐日/逐服务拆解时);`--budgets-only` 禁止回落。

**验证(四条路径全部实跑,不留未验证的回落分支 —— 这正是总监 §AA 本周反复裁的那个形态)**:
`bash -n` 过;默认路径免费出数;`--leak-only` 跳过成本节;
**回落路径实测**(`BUDGET_NAME=does-not-exist` ⇒ 打印告警并用 CE 出 `18.2767015777`);
**拒绝回落实测**(`--budgets-only` ⇒ 报错 **exit 1**);
**升级路径实测**(`COST_CONFIRM_AT=10` ⇒ 免费读数后自动附上 CE 复核行)。
章程步骤 2/6 已同步改写。**省下 ~$3.7-7.3/月**(收尾轮免费 + 开工轮免费,
只在近刹车线或要拆解时付费)。

## 5. 交付二:**「$50/月 Budget」是错的,实配 $100/月;而 freeze action 无法验证**

`budgets describe-budgets` 实读:**`BudgetLimit = 100.0 USD`**,
通知阈值 ACTUAL > **50% / 80% / 100%**,即 **$50 / $80 / $100 的实际支出**。
而 `CLAUDE.md` 两处都写着「$50/month AWS Budget」「alerts at 50/80/100%」
(若限额是 $50,告警本应在 $25/$40/$50)。**已按实测更正 `CLAUDE.md` 两处。**

**更该记一笔的是不能验证的那半条**:`CLAUDE.md` 称「a freeze action at 100% caps its
EC2 spend as a hard backstop」,而 **`budgets:DescribeBudgetActionsForBudget` 本用户
无权限**(AccessDenied,原文已留档)⇒ **freeze action 是否还存在、是否绑对了策略,
从这个账户里查不出来**。按总监 §AA.1/§Z.4 刚立的规矩(**不许拿未审计的代理当保证**、
**空转的规则是最糟的一种状态**),这条不能继续当「hard backstop」引用,已在 `CLAUDE.md`
标注为 unconfirmed。**要不要给 agent 补 `budgets:Describe*` 读权限,归 owner/总监裁。**

**顺带把四条线的次序摆出来**(此前散落在三处文档,从没并排写过):
**批测台自设围栏 $45 → owner 首封告警邮件 $50 → 批测台刹车线 $90 → owner 批准线
/ Budget 限额 $100**。次序是自洽的:desk 先自停,再惊动 owner。
**但「$50 那封邮件」的存在此前没有任何一份章程提到过** —— 意味着 MTD 一旦过 $50,
owner 会先从邮件而不是从我们的报告里知道。

## 6. 局数

本轮无在跑波次,无新数据。上一波(§R.0)最终局数不变:
**arm A 280(radiant 166 / dire 114)+ 24 暖场;arm B 292(168 / 124)+ 24 暖场;两臂 572**。
档案累计未变:**11,048 在册 + 3,367 暖场 / 137 run / 112 种子**;
存储视角:111 个 run 持有 `.dem`,29 个 run 已是零-dem 形态。

## 7. 验证

`check_costs.sh` `bash -n` 通过 + 四条路径实跑(见 §4)。
本会话**未改动任何 Lua**(改动为 `tools/batch_test/aws/check_costs.sh`、`CLAUDE.md`、
本章程、本报告),且本容器**未装 `luacheck`/`lua5.1`**,按铁律第 6 条立法目的本轮无适用对象。

**本会话真实 AWS 支出**:CE 调用 5 次 = **$0.05**(开工 check_costs 1 次 + 逐日拆账 2 次
+ 回落路径验证 1 次 + 升级路径验证 1 次)。其余全部为免费只读调用。
**下一轮起同样的工作量只需 $0.00-0.01。**

## 8. 下次触发

- 固定栏位照旧:off-roster / `unattributed/` / `.demclaim.json` 的 `method` 分布 / `dem21/` 有无对象。
- **新增**:确认 `check_costs.sh` 默认路径仍免费出数(账单上 `AWS Cost Explorer` 的
  **qty 应从 ~24/天掉到 ≤ 4/天**;若没掉,说明有人还在走 `--ce` 或回落分支天天触发)。
- 启动仍以 `test_set.md` 届时的指示为准;一旦总监给出任何申报目的,那一波直接挂
  `--rec-slots 16`(两臂对称),存储/权限/规则形状三件已落定,**排期与支出零额外成本**。
- 跨组:`[batch]` issue 交付本轮两条发现;`[harness] #75` / `#33` 仍开着,不重复开。

## 9. Token 用量

```
TOKENS total_in=5,707,892 out=56,695 turns=57
```
(统计时刻为止;此后的收尾回合未计入。)

**收尾泄漏复查**:`check_costs.sh --leak-only` ⇒ **0 台在跑,无泄漏**(且这次复查本身零成本 —— 新路径的第一次实用)。
