# 批测台 2026-09-13T09:08Z — 持有第八轮(零发波)

⭐⭐⭐ **头号:闸 (iii) 在本台一分钱 EC2 都没花的三小时里,从 `exit 0` 翻回 `exit 3`,
而本轮零成本把机制钉死了 —— 吃掉那点余量的不是「今天的常驻计费」,是**一个已经过去的日子自己长大了**。**
上一轮(06:15Z)拒发的理由**方向对、机制错**,本轮是**更正不是确认**;而更正后的形状对围栏更不利:
`projected = actual + pending + planned` 把 `actual` 当**已结算**,实测它不是。

---

## 〇、定位与本轮支出

- 持有第八轮。**零发波、零 EC2 支出。** 新增计费 **`$0.02`** = `check_costs.sh` 的 `≥$35` 自动 CE 复核 `$0.01`
  + 本台主动付费逐日 CE `$0.01`(§二,近刹车线 + 确实需要逐日拆分 ⇒ 章程允许的那两种情形**同时**成立)。
- 收割**零欠**;泄漏**四条独立路径全零**;闸 (ii)/(iv) **未评估**(闸 (iii) 已 `exit 3`,走不到发波决策),
  ⛔ **不声称它们跑过**。

## 一、成本(现跑)

- `AWS_SETUP_EXIT=0`(容器无 CLI,脚本自装 `aws-cli/1.46.1`;身份 `arn:aws:iam::581436534924:user/dota2bot-agent`)。
- `COST_EXIT=0`。MTD **`$88.917`**;`≥$35` 自动 `$0.01` CE 复核 **`88.9165223764`**;`forecast 181.855` / `limit 100.0`
  **登记,不据以决策**。
- `budget refreshed` **`2026-09-13T07:29:49Z`** ⇒ ⭐ **快照是活的**(距读数 ~1.6h),且与上一轮(06:15Z)的
  `2026-09-13T01:19:49Z` **不是同一张** ⇒ **本轮的 MTD 是第二次测量,不是同一张快照被读第二次**;
  §二 的增量比较因此**合法**(章程步骤 2 (乙) 禁的是「用 MTD 增量反推波费」,不是禁比较)。
- 诚实 cutoff `07:29:49Z − 11.3h` = **`2026-09-12T20:11:49Z`**。⇒ 闸 (iii) 的 `pending $0.000` 本轮**是真读数**:
  W68(`09-12T03:26Z`)与 W69(`09-12T09:26Z`)两波都在 cutoff **之前**,已落账。
  ⚠️ 这正是 00:13Z 交棒 ③ 点名的那个场合(「快照晚于 `2026-09-12T20:44:31Z` 时 `--pending` 要本台自己抬」)——
  本轮**核过了**:该抬的两波已经在 `actual` 里,**再抬一次是重复计数**,故**不抬**。

## 二、⭐⭐⭐ 头号:一个已收盘的日子长大了 `+10.49%`,而它比闸给的全部余量还多

### (甲) 两轮之间的事实

| | 06:15Z 轮 | 本轮 09:08Z |
|---|---|---|
| budget 快照戳 | `2026-09-13T01:19:49Z` | **`2026-09-13T07:29:49Z`**(新) |
| MTD | `$88.692` | **`$88.917`** |
| 闸 (iii) `--planned 1.10` | **`exit 0`**,`headroom $0.208` | **`exit 3`**,`projected $90.017` |
| 本台发波 | 0 | 0 |

两轮之间本台**发了零波**:S3 `validation/` **592 对象**与上一轮登记的基线**逐个相同**;
`describe-instances`(六态)**零行**;`describe-volumes` **零行**;闸自带账户级普查
`CERTIFIED (0 accruing instances account-wide, read this run)`。
⇒ **MTD 涨的 `$0.2245` 没有一分是本台这三小时里花的。**

### (乙) 逐日 CE 把它定位到了**一天**(`CE_DAILY_EXIT=0`,第二个 `$0.01`)

自洽核对:**十三天逐日求和 `$88.916522`,与 MTD `88.9165223764` 逐位相同** ⇒ 这张表**就是那条线本身**。
与 03:11Z 轮第一手拉的同口径表逐日对照:

| 日 | 03:11Z 读数 | 本轮读数 | 差 |
|---|---|---|---|
| 09-01 … 09-11 | (十一天) | **逐日逐位相同** | `0.0000` |
| **09-12** | `2.1391` | **`2.3635`** | **`+0.2244`(+10.49%)** |
| 09-13 | — | **`0.0000`** | — |

⇒ **全部增量落在 09-12 一天上**,而 09-12 在 03:11Z 那轮**早已是一个收了盘的过去日**;
⇒ **09-13 至今仍是 `$0.0000`**,本月第一个零批测日**尚未出账**(与 06:15Z 轮所立的 ≥19.9h 滞后一致)。

### (丙) 因此这是**更正**,不是上一轮的确认

- 06:15Z 轮把「`headroom $0.208` 不可花」挂在**今天的常驻计费**上(`NoInstanceType` 最小一天 `$0.4375`)。
  **本轮实测:今天(09-13)贡献 `$0.0000`。** ⇒ **那个机制本轮没有发生。**
- 真正吃掉余量的是**另一个机制**:`actual` 里一个**已经落账的日子被向上修订**。
- ⭐ **对围栏的意思更硬**:`wave_fence.py:983` 的 `projected = actual + pending + planned` 里,
  `pending` 有自我限定(`pending $0.000 is a reading, not a default.`)、`planned` 是本台传的,
  而 **`actual` 一个限定都不打** —— 它被当成已结算。本轮量到**它不是**。
- ⭐⭐⭐ **量级刚好骑在刀口上**:这次修订 `$0.2244` **大于**闸 2.9 小时前给出的**全部余量** `$0.208`(1.079×)。
  ⇒ **上一轮若把 `CLEAR` 读成许可而发了波,那一波会越过刹车线。** 拒发是对的,**而它给的理由不是真正救了它的那条**。

### (丁) ⛔ 诚实边界

1. **n=1 次修订事件**,⛔ 不主张任何修订率、不外推到别的日子、不说「每天都会这样」。
2. ⛔ **不据此改任何常数、不改 harness、不自行放宽或收紧任何线**(先例 GH #33)。
3. 本条**不否定** 06:15Z 轮那个缺口(今天的常驻计费确实不在 `projected` 里),
   只是说**本轮翻转发波决定的不是它**。两条是同一条缝(GH #565 / #603)的两个肢。
4. ⛔ 未对 `+$0.2244` 做任何**归因**(是波驱动的尾巴、还是常驻计费的补记,本轮量不出来)。

## 三、闸

- **闸 (i)** `THROTTLE_EXIT=0`(裸读):`anchor wave : W69 (4 machine(s))` / `last machine up : 2026-09-12T09:26:31Z <- the anchor` /
  `slate spread : 7s` / `unlock : 2026-09-12T15:26:31Z` / `now : 2026-09-13T09:10:01Z` / **`margin : +63810s past unlock`**。
  ⚠️ **(i) 开着不构成许可**:它是时间函数会自愈,(iii) 只随 MTD 单调收紧 —— 两者不是同类闸。
- **闸 (iii) 裸跑** `FENCE_EXIT=0`:`accrual check : CERTIFIED (0 accruing instances account-wide, read this run) -- pending $0.000 is a reading, not a default.` /
  `actual (MTD) : $88.917` / `pending waves : $0.000` / `planned : $0.000` / `projected total : $88.917` /
  `fence : $100.00` / `brake : $90.00` / `operative ceiling: $90.00 = min(fence, brake)` / **`headroom : $1.083 after this wave`** /
  `WAVE_FENCE: CLEAR (exit 0)`;`crossing registry: 0 record(s) …, none in force`;
  `accrual scope : 17 region(s) read <- COMPLETE`;`budget filters : none`。
- **闸 (iii) `--planned 1.10`(最便宜的那一档)** ⇒ **`FENCE_SPOT_EXIT=3`**,逐字:
  `projected total : $90.017` / `operative ceiling: $90.00 = min(fence, brake)` /
  `A launch at this instant would put MTD past the $90.00 BRAKE line.` /
  **`WAVE_FENCE: THROTTLED (exit 3) -- brake, not fence. Stop and report to the owner; do not wait it out.`**
  ⇒ ⭐ **差 `$0.017`**(`$1.100 − $1.083`)。
- ⛔ **未用** `--no-accrual-check`、**未传** `--director-crossing` / `--pending`(理由见 §一)⇒ **本轮无任何「这是 SKIP 不是 pass」行**。
- **闸 (ii)/(iv) 未评估**,⛔ 不声称跑过。

⇒ **本轮不发波,理由是闸 (iii) 的 `exit 3`,不是本台的处置。**(与 03:11Z / 06:15Z 两轮相反:那两轮闸是绿的、不发是处置。)

## 四、九月的算术(交 owner)

```
余量(现跑)          = $90.00 − $88.917 = $1.083
最便宜的一波(覆盖档) = $1.10            ⇒ 塞不下
09-13 已走 9.1 小时,对 MTD 的贡献仍是 $0.0000(尚未出账)
MTD 单调上升,brake 是常数
```

⇒ **在 owner 表态之前,九月剩余 17 天本台没有任何算术路径发出下一波。**
⚠️ 上一轮那句「还剩恰好一波」**本轮已作废**,而作废它的是**零成本的时间流逝**,不是本台花的钱。
⚠️ 反方向的论点照登不藏:预算按月重置,这 `$1.083` 不花就在 10-01 作废。

## 五、收割 / 泄漏

- **收割零欠**:`S3VAL_EXIT=0`,`s3://dota2bot-batch-results-4924/validation/` **592 对象**,
  与上一轮登记的基线**逐个相同** ⇒ 无 W69 之后的新数据。W68/W69 均已收割完毕。**本轮未新增任何 verdict。**
- **泄漏四条独立路径全零**:(1) `check_costs.sh` `running/pending` 区块**空**;
  (2) 闸 (iii) 账户级普查 `17 region(s) read <- COMPLETE` + `CERTIFIED (0 accruing instances account-wide)`;
  (3) `INST_EXIT=0`,us-west-2 `describe-instances` 取 `pending,running,shutting-down,stopping,stopped` **零行**;
  (4) `VOL_EXIT=0`,`describe-volumes` **零行**(无孤儿 EBS)。
  常驻仍只有 AMI `ami-0a990a26d89c66547`(`dota2bot-batch-20260718`)。

## 六、⛔ 本会话未改任何 bot 逻辑

`bots` / `game` / `tests` / `tools` **一行未改** ⇒ **无一条 trunk red 由本轮引入**。
本轮产物只有 `iterations/` 下的报告与章程条目。

## 七、管道自查(上上轮交棒所立:每一个读了退出码的命令)

`AWS_SETUP_EXIT` / `COST_EXIT` / `THROTTLE_EXIT` / `FENCE_EXIT` / `FENCE_SPOT_EXIT` / `S3VAL_EXIT` / `INST_EXIT` /
`VOL_EXIT` / `CE_DAILY_EXIT` / `ARM_EXIT` —— **全部重定向到文件后裸读,零管道**。

⚠️ **但开工第一条命令仍然踩了(第 25 次登记)**:`routine_selfcheck.sh | tail -40`,
工具当场拒绝,逐字 **`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.`** /
`SELFCHECK_EXIT=2  REFUSED (nothing was checked; this is NOT a pass)`。
⭐ **工具拦住了 ≠ 这轮没犯。** 第二次重定向重跑,见 §八。

## 八、自检(铁律 10)

- 第二跑 `timeout 400 bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1` ⇒ **被本台自己的 `timeout 400` 砍断在
  `=== trunk health (python test suite) ===` 那条腿上** ⇒ ⛔ **没跑完就不是通过**,
  与前几轮交棒登记的「本容器 ≥25min / 给它 ≥1800s」**逐条吻合**(第 24 次那条交棒说的正是「**不设短 `timeout`**」,
  本台第二跑仍然设了 —— **同一条交棒在同一轮里被违反了两次,第一次是管道,第二次是超时**)。
- 已跑完的腿(第二跑日志实读):`promote-atom constraints: OK`;
  `INVERSE-GATE CENSUS live gate ids 211 armed 26 / FROZEN 0 COUPLED 1 UNRESOLVED(armed) 0`;
  `no armed id hangs under an unarmed gate -- OK`。
- 第三跑已按交棒**后台 + 无 `timeout`** 重新发起,收尾读数见「补记」。

## 九、交棒

1. ⭐⭐⭐ **owner —— GH #779 第八轮零表态,刹车第八轮持有。** 本轮加的**不是余量也不是新论点,是一条状态变更**:
   上两轮的「**还剩恰好一波**」**已经没了**,而拿走它的是**时间**不是本台的支出(§二/§四)。
   `headroom $1.083 < $1.10`,差 `$0.017`。三条选项不变(本台不建议、不预选):
   **维持 `$90`** / **给 `dota2bot-batch` 加成本分配标签过滤器**(现为 `budget filters : none`)/
   **一次性解禁到某个数**(并说明是否含外来支出)。
   并排第二个数不变:外来 `c7a.16xlarge + r7a.16xlarge = $37.4355` = MTD 的 **42.10%**,
   只存在于 09-05 / 09-06 两天、此后一分未涨 ⇒ 咬住本台的这条 `$90` 里**四成以上不是本台的支出**(GH #721 三条路未落定)。
2. ⭐⭐⭐ **总监/harness —— GH #565/#603 拿到一个新肢,且它比原肢更硬**(§二 丙):
   `projected` 的三项里 `actual` **一个限定都不打**,而本轮实测**一个已收盘的日子被向上修订 `+10.49%`**,
   修订量 `$0.2244` **大于**闸 2.9h 前给出的全部余量 `$0.208`。
   建议收窄方向:`headroom` 小于「本月逐日修订观测量级」时**至少打一行限定**;
   ⛔ 本台**不定新常数、不改 harness**。⛔ **n=1,不主张修订率。**
3. ⭐⭐ **下一轮本台 —— (乙1) 判据更新**:09-13 逐日仍 `$0.0000`。
   **09-14T02:00Z 之后**一次 `$0.01` 逐日 CE 才第一次给出「**一波都不发时这条线一天涨多少**」
   (= 06:15Z 轮交棒 ③ 那个测点,本轮**仍拿不到**,因为 09-13 未出账)。⛔ 在那之前不花那第二个 `$0.01`。
   ⭐ **新增一项**:同时**比对 09-12 会不会再长一次** —— 那是 §二 (丁)1 那个 n=1 变成 n=2 的唯一免费途径。
4. ⚠️ **开工第一条命令第 25 次**:**重定向 + 后台 + 不设短 `timeout`**。本轮**两条都犯了**(§七/§八),
   ⛔ 下轮起自检**一律** `nohup … &` 起,正文里不给它任何 `timeout`。
5. ⛔ 本报告的 `$88.917` / `headroom $1.083` / `budget refreshed 07:29:49Z` / S3 `592 对象` /
   闸 (i) 锚点 `2026-09-12T09:26:31Z` / 逐日表 **全部作废**,围栏/闸/成本/收割/泄漏**一律当轮现跑**。
6. ⭐ 若 owner 说「发」:按 06:15Z 轮交棒 ⑤,下一波是 `OWNER_PRIORITIES.md` **4.1 标尺波**
   (`stable(当前 main,gate 全关) vs upstream 74727e4a`,不 arm 任何 candidate),**排在任何 arm 波之前**;
   ⚠️ 它**同样要过闸 (iii)**。

## 十、Token 用量(铁律 8)

```
TOKENS total_in=3,514,288 out=27,278 turns=33
```

⚠️ 这是**到统计时刻为止**的数字,统计后的收尾回合(push / 发评论 / 补记)**不计入**。
与前几轮持有轮同量级,**无空转**(铁律 11 全程零 `requires approval`、零等待)。
