# 批测台工作单元报告 — 2026-08-20T10:06:31Z

**类型:启动轮(章程 4b 例行波次,§O.0/§P.0 逐字执行)。收割部分为空(结构上不可能有新数据)。**

---

## 1. AWS 引导与成本

- `bash tools/batch_test/aws/session_setup.sh` → `AWS ready: arn:aws:iam::581436534924:user/dota2bot-agent`
  (容器初始无 AWS CLI,脚本自行 pip 装了 aws-cli/1.46.0)。之后一律 `awsx`。
- `check_costs.sh`(开工时):**MTD $5.2934350747**,与 04:05Z / 06:13Z / 08:08Z **逐位一致**
  —— 04:11Z 那 4 台 on-demand 仍未入账。累计约 19 台机器只入账了一部分,**不是泄漏**:
  开工时 `running/pending instances` 一节为空(0 台在跑)。
- 预算刹车:**未触发**($5.29 ≪ $90 刹车线;$5.29 + 本波 ~$0.7 ≈ $6.0 ≤ $45 月度围栏)。
- 常驻成本仍只有 AMI `ami-0a990a26d89c66547` + 快照。

## 2. 收割

**本轮无新数据可收割,且结构上不可能有**:04:11Z 那波之后没有任何实例跑过,
`soak/` 最新四个前缀仍是 `spot_20260820_041132/041134/041137/041139_1_7c8b5167…`
(06:13Z 已全量收割 291 局),`validation/` 最新对象停在 2026-07-23。
`queue.json` 无 pending(`director-1` 已 `done`)⇒ 走章程 4b 例行波次。

## 3. 启动决策:**启动**(例行三条件全满足)

| 条件 | 判定 |
|---|---|
| (i) 距上一波例行上机 ≥ 6h | 上一波 **04:11:32Z** → 门槛 **10:11:32Z**;开工时刻 10:06:31Z 尚差 ~5 分钟,**不援引 06:07Z 那种「差 2 分钟按立法目的」的例外**,而是先把静态核对/SHA fetch 实测做完,**等门槛真的过了才上机**(与 04:05Z 同一处置)。实际上机见 §4。 |
| (ii) 有新东西可测 | 树 `8641837`→`7564660` 有 **4 个 `bots/` 文件变更**:`aba_defend.lua`+`utils.lua`(GH #51,已发布默认行为)、`hero_zuus.lua`(GH #59)、`hero_invoker.lua`(GH #50)。满足。 |
| (iii) 成本 | $5.29 + ~$0.7 ≈ $6.0 ≤ $45。满足。 |

## 4. 本波配置(**逐字照抄 §O.0,§P.0 未改这两行**)

```
cand  = l1trade,l5combo,midtp,suptp,tpcommit,tpdying,lf_rescue,teambrain,ownhalf,
        overchase,fieldregen,wandbleed,tpwatch,capmono,cmrguard,tpdead,zusult     (17 id)
seeds = 888 895 896 906
```

- 拓扑:**4 台 × 1 种子**,`c6i.4xlarge` **`--on-demand`**(64 vCPU 配额正好 4 台;短波次别用 spot),
  **16 槽**,**2h 看门狗**,**`--games 22`**(再提只会加大 radiant/dire 不对称)。
- 树:**全 40 位 SHA `75646608acc64bd5a25fe55a492d9969f6309ff9`**,取自 `git ls-remote origin main`
  的远端真值(= 本地 HEAD;**不信本地 `origin/main`**,20:11Z/02:06Z/06:13Z 三次踩过)。
  上机前在空 clone 里实测裸 SHA 可 fetch:**`SHA_FETCH_OK`**。
- **§K.5 副作用照章标注**:这是挑种子波次,经济读数**不得**与 863-870 这类连号波次并列比较,
  **不得**给任何单 id 判条件 (b)(4 种子 MDE ≈ 35.6 gpm)。本波主产出是 `.dem` 语料。

### 4.1 Launch note(§P.0 硬要求 + 本轮自查发现的两条追加)

1. **(§P.0 要求的那句)树含 GH #51 修复** —— `bots/FunLib/aba_defend.lua` / `utils.lua` 的
   `string.find` 恒真谓词已改为 `.includes()`/`__TS__StringIncludes`(实测 aba_defend 13 处、
   utils 5 处调用)。**共模、两臂逐字相同**,不影响任何 id 的隔离;但**本波之后的 defend 家族
   行为读数与 10:11Z 之前的不在同一个世界**(小兵威胁 3 倍膨胀已消失),§P.2 已立此提醒。
2. **【追加,§P.0 未提】树含 GH #59 的 `zusult` 修复,并引入了一个新 gate id `zusultx`——
   本波不 arm 它。** `hero_zuus.lua:249-273`:`bPostSpend = J.IsSoakCandidate( 'zusultx' )`,
   **只有 `zusultx` 才减去本次施法花费**;`zusultx` 缺席时 `nSpend` 恒为 0,那条判据
   **与已发布的窄域 gate 逐字等价**(代码注释自述 byte-equivalent,已逐行复核)。
   ⇒ **本波 `zusult` 买到的仍是窄域行为**,与 04:11Z 那波同域,可比。
   `zusultx` 不在 eligible 集、不在 §O.0 串里,按章程「不自行改变被测集合」**不 arm**。
3. **【追加】树含 GH #50 的 invoker 改名(`J.Unit.` → `J.Utils.IsUnitWithName`),
   非 gate 但可证明惰性**:该行所在分支是 `A or (A and X)` = `A`,右半支不可达,
   改名前后行为逐字相同(hero 组已实测名字判据零调用)。记一句以免下次收割时被当成混杂。

### 4.2 上机前静态核对(防 `[bug] #31` 式「测了个不生效的东西」)

用 08:08Z 立的**带空格 pattern** `grep -rE "IsSoakCandidate\( *'<id>' *\)"`(无空格版会假阴性):

- **17 个 armed id:16 个各恰好 1 个读者**;**`lf_rescue` 裸字面量 0 是已知正确的** ——
  它经 `J.IsLaneFixOn( 'rescue' )`(`jmz_func.lua:5459` 定义 → `:5765` 唯一消费点)展开。
- **必须缺席的三个**:`roamstale` **读者 0**(promote 落地属实)、`wandlimbo` 读者 1
  (代码在,按 §J.1.4 机会普查未做**不 arm**)、`axebuyblink` 读者 1(§O.1 出集但**代码保留**
  在 `hero_axe.lua:107`)⇒ **出集/不 arm 唯一的闸门就是「不写进 cand 串」,已逐字照抄。**
- 另加本轮新出现的 `zusultx`:**不在串里** ⇒ 窄域(见 §4.1.2)。

### 4.3 实例映射

**实际上机 10:11:38 – 10:11:45Z**(门槛 10:11:32Z,过线后 6-13 秒),4/4 全部 `rc=0` 拿到容量
(与 18:08Z 那次 spot 4/4 `InsufficientInstanceCapacity` 形成对照 —— on-demand 64 vCPU 配额
正好容下 4 台 c6i.4xlarge,这也是本波不能再多开的硬顶)。

| 种子 | 实例 | run_id |
|---|---|---|
| 888 | `i-07c1bd2d6aa138aca` | `spot_20260820_101138_1_75646608acc64bd5a25fe55a492d9969f6309ff9` |
| 895 | `i-0aa7319526334c4aa` | `spot_20260820_101140_1_75646608acc64bd5a25fe55a492d9969f6309ff9` |
| 896 | `i-054305d13b4b00727` | `spot_20260820_101143_1_75646608acc64bd5a25fe55a492d9969f6309ff9` |
| 906 | `i-0f1294d7539ef3e83` | `spot_20260820_101145_1_75646608acc64bd5a25fe55a492d9969f6309ff9` |

**同波四台 SHA 后缀相同、只靠秒级时间戳区分种子** ⇒ 映射表仍需要,但**收割以每局 stamp
`mirror:<cand>:s<seed>:<side>` 自带的种子号为准**,上表只做交叉校验(20:11Z 立的规矩)。
预估 **$0.6-0.8**,预计 **~10:45-11:00Z 落地**(2h 看门狗是外沿,实测一波约 30-40 分钟)。

## 5. 局数(章程第 7 条)

- **上一波(04:11Z,18 id,挑种子 872/875/885/887,树 `7c8b516`)最终有效局数**:
  872 = 73(radiant 40 / dire 33)、875 = 74(41/33)、885 = 70(40/30)、887 = 74(42/32),
  **合计 291 有效局(radiant 163 / dire 128)+ 24 暖场局(暖场不算有效局)**。
  radiant 基本打满 `--games 22` 配额、dire 仍撞 35min stall 上限。
- **本轮在跑波次(10:11Z)进度**:刚上机,S3 `soak/<run_id>/` 尚无 `analysis.json`。
  **预期 ≥280 局有效局 + 24 暖场**(按上一波 4 台 × ~73 局外推)。

## 6. 下次收割必查项

1. **暖场 stamp 必须是 `7564660`** —— 若不是,说明裸 SHA checkout 在实例上静默失败、跑的是
   AMI 里的陈树,**该轮数据作废**(该校验已连续通过三波)。
2. **S3 真实 cand 串逐字 = §4 那 17 id**,且 `roamstale` / `wandlimbo` / `axebuyblink` /
   **`zusultx`** 四个全部缺席(事后交叉验证比事前 grep 更硬,20:11Z 的经验)。
3. **先按 run 分目录下载再带前缀合并**,再 `recover_verdict.py` 全量重算 ——
   同波次多实例的 per-game 文件名会跨 run 撞车,08:08Z 那次静默丢了 15.9% 的局。
4. 判读按 §K.4/§K.5:**本波主产出是 `.dem` 语料**(`zusult` 窄域、`cmrguard` 的条件 (b) 行为),
   经济读数只当整集合粗看板,**不得给任何单 id 判条件 (b)**。
5. **给录像组的语料量口径(08:08Z 已算,原样重申)**:侧别是**种子**的属性不是 wave 的属性
   (两个 wave 抽签完全相同,翻的只是 gate 装哪边)⇒ 同英雄的候选侧 vs 基线侧是**同种子同队伍
   同侧**配对,**不含 radiant 侧偏置**,可做 DiD;**但两侧局数不等**(radiant-candidate wave
   ~40.75 局/种子打满配额,dire-candidate wave ~32.0 局/种子撞 stall)。外推本波:
   **Zeus(R/R/R/D)候选侧 ≈154 vs 基线侧 ≈137(候选侧多约 12%)**;
   **CM(D/D/R/R)候选侧 ≈146 vs 基线侧 ≈146,完全平衡**。
   ⇒ **`zusult` 的检测器若比裸计数,这 12% 会直接变成假阳性 —— 必须按局归一化(次/局)
   或按种子配对后再平均**;`cmrguard` 无此问题但建议同口径。

## 7. 泄漏检查

- 开工时:`check_costs.sh` 的 running/pending 一节为空(0 台)。
- 收尾时:见 §8 的复查结果。

## 8. 收尾复查

10:11:58Z 再跑 `check_costs.sh`:**恰好 4 台 `dota2bot-soak-od-1`,全是本轮有意启动的自毁机**
(`i-07c1bd2d6aa138aca` / `i-0aa7319526334c4aa` / `i-054305d13b4b00727` / `i-0f1294d7539ef3e83`),
无任何多余实例、无 stopped 残留;MTD 仍 **$5.2934350747**;常驻成本只有 AMI + 快照。**无泄漏。**

## 9. 铁律第 6 条(验证)

本会话**未改动任何 Lua**(改动仅 `iterations/` 下的报告/章程),且本容器**未装
`luacheck` / `lua5.1`**,两者均未运行,按立法目的本轮无适用对象。

## 10. 跨组

在既有 `[batch] #49` 下**追评**(不新开),交付:本波已上机 + launch note 的两条追加
(`zusultx` 存在但不 arm、`zusult` 仍是窄域可与 04:11Z 比)+ 语料量口径。

## 11. Token 用量

```
TOKENS total_in=10,048,776 out=44,733 turns=91
```
(到统计时刻为止;统计后的收尾回合不计入。本轮 91 个 turn 里有相当一部分是**等 10:11:32Z 门槛**
的轮询 —— 开工时刻比门槛早约 5 分钟是排期问题,不是工作量问题。)
