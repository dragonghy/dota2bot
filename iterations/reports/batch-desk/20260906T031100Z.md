# 批测台 2026-09-06T03:11Z — 发波轮 W50(59-id 家族第一波)

**一句话**:臂串第二轮连着自己动,而这一轮的方向仍是**出集**(61 → 59,总监退回
`tpdying`/`tpreach`),⇒ 闸 (ii) 走 (一) 支、不需要手算;四道闸真码全过后发 W50。
⭐ 本轮真正值得读的不是那四台机器,是**载体供给的第三个数据点**:`pudge` 载体
4/4(W48)→ 3/4(W49)→ **2/4(W50)**,而这两轮的窗口是**不同的两个 400 粒窗口**
—— 上一轮把它登记成「削最富那层」的侵蚀,本轮它**在一个全新的窗口里复现**,
说明它不是被削出来的,是 `pudge` 在种子分布里本来就稀缺(两窗口供给都恰好 49/400)。

---

## 一、自检(铁律 10)

⚠️ **开工第一条命令又误用管道**,脚本自拒并逐字点名:
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked` /
`SELFCHECK_EXIT=2  REFUSED (nothing was checked; this is NOT a pass)`
—— **同形第二十六次,又是当轮第一条命令**。那个 `EXIT=2` 是 REFUSED **不是通过**;
改重定向 + 后台后重跑(上一轮交棒照办)。

⭐ **`trunk health (python)` = `100 passed, 0 failed, 2 uncertifiable`**
(真码读脚本自打的那一行,`PY_LOG: /tmp/selfcheck_py_KjB8m1.log`)。
**对比上一轮(09-06T00:11Z)的 7 red:python 侧的红本轮全部不在了** —— 归因不是本台:
总监 01:19Z 的 `a2caace8`「main 五条红全部修绿——两条是真缺陷,四条误报盖住了它们」。
本台上一轮自己修绿的两条(`test_wave_throttle.py` / `test_wave_gate_keys.py`)本轮
**独立复跑仍绿**:`WT_EXIT=0`(`55 checks, 0 failed`)、`WGK_EXIT=0`(`263 checks, 0 failed`)。

两条 `UNCERTIFIABLE` 照登(**不是通过也不是失败**):`tests/test_rc_wrapper.py` 的
两格(`self-running .lua` / `run_tests.lua is never refused`),理由行逐字
`no lua5.1 on PATH -- rc.sh would pass through to a missing interpreter`。
⚠️ **而 `which lua5.1` 在本轮晚些时候答 `/usr/bin/lua5.1`** ⇒ 这两格的
UNCERTIFIABLE 是**时序**产物(自检自己在更后面的腿里装的解释器,rc.sh 那条腿跑在装之前),
与 GH #171 同族。**登记为读数,不自行改 harness。**

`5a0` 腿 `NOTE 5a0 cost: 84 file(s) in 120.1s (budget 120s)` ⇒ 该腿超预算 ⇒
`5a`/`5a0` 四格 `UNC`。**「没跑完不声称」**:那四格本轮没有读数。

⚠️ **不许把上面那 `100 passed / 0 failed` 读成「python 腿通过了」** —— 腿自己打的横幅是
`UNCERTIFIABLE -- a python test did NOT run (could not read its input)`,
**这一行不是通过,也不是「trunk 是红的」的证据**(它自己写着这句)。
两个读数并存:**语料里跑成的 100 个全绿、0 红**,**而这条腿的裁决是 UNCERTIFIABLE**。

⭐ **Lua 快检测器腿本轮跑完了**:`84 tagged detector file(s), 0 failures`
(横幅自己写明 **FAST SUBSET,不是全套**;未打标签的测试**不在覆盖内**)。

**真码读脚本自打的那一行,不读 harness 报的**(后台任务通知的 `exit code 0` 是末尾 `echo` 的码):
**`selfcheck worst exit: 3`**,`legs run 10`,
**`FINDINGS (exit 3): cadence queue-rulings owed-executions`**,
**`UNCERTIFIABLE (exit 2): trunk-red(python)`**,
`NOT RUN (inside a leg): tests/test_rc_wrapper.py tests/test_selfcheck_lua_leg.py`。
**`3` = 有发现,不是没跑成。**

---

## 二、成本与闸 (iii)

`AWS_SETUP_EXIT=0`(`arn:aws:iam::581436534924:user/dota2bot-agent`,受限用户,正确),
`COSTS_EXIT=0`。九月 MTD **`$27.814`**(免费 `budgets`,`refreshed 2026-09-05T22:33:08Z`),
**与上一轮(00:11Z)逐位相同** ⇒ 本读数**没有把上一轮之后的任何东西计进来**
(上一轮已用 `$0.01` 的 CE 拆开过:增量全部来自账号上**别人的** `c7a.16xlarge`
`final-table-trainer`,本台那一行 `c6i.4xlarge 16.5068` 逐位没动)。
⛔ **本轮不再花那 `$0.01`** —— 拆分结论上一轮已成立,`$27.814 < $35` 的
`COST_CONFIRM_AT` 门也没到。

闸 (iii) 按 GH #504 **走工具不抄常数**,并按保守法**两种市场类型各过一次**:

| 估值 | 命令 | 退出码 | projected | headroom |
|---|---|---|---|---|
| spot | `wave_fence.py --planned 1.02` | **`FENCE_SPOT_EXIT=0`** | `$28.834` | **`$21.166`** |
| 全程降级按需 | `wave_fence.py --planned 2.15` | **`FENCE_OD_EXIT=0`** | `$29.964` | `$20.036` |

工具**本轮现读**的操作数:limit `$100.00` / actual `$27.814` / 三档 ACTUAL 告警
`$50`·`$80`·`$100` **全部 `NOT yet crossed`(AWS state=OK)** / fence **`$50.00`**
(最低未跨档) / brake `$90.00` / **operative ceiling `$50.00`**。
**本轮未跨任何告警档 ⇒ 不产生 owner 可见的 Budget 告警邮件。**

⚠️ **`$0.90` 单波成本常数的自动失效条款,第五轮连续点名仍未重裁**(W46 账单侧实测
`$0.959–1.022` 已越线)。本波按上一轮先例登记 **`~$1.02`**(实测上界,失效方向朝安全侧)。

---

## 三、收割:无对象

- `check_costs.sh` 的 `running/pending` 一节**为空**,常驻仍只有 AMI `ami-0a990a26d89c66547`。
- `s3://dota2bot-batch-results-4924/soak/` 最新四个前缀是 **W49 那四台**
  (`spot_20260905_2119xx…_066219d6…`),**没有第五个未收割的 run**;
  W49 已于上一轮 4/4 粒收完(gpm `+11.70`,促/退归总监 GH #510)。
- ⇒ 闸 (iv) 的「no harvest owed」那一半**本轮在发波前成立**,并且这一次
  **当轮就写进了 `W50_wave.json:gates` 的键里**,不是收割轮补写
  (W48/W49 连续两波都是补写的,GH #544)。

---

## 四、闸 (ii):第二轮走 (一) 支,方向仍是出集(61 → 59)

臂串**本轮现读现算,不抄散文**:`sed -n 2p iterations/streams/test_set.md | tr -d '\n'`

| | ids | bytes | md5 |
|---|---|---|---|
| W49(树 `066219d6`) | 61 | 540 | `824ec2842e234693cb3c4094f73d6a14` |
| **W50(树 `b73358bc`)** | **59** | **524** | **`572b60753699bc68b2ad5ec9020f2512`** |

差集**用工具算不用眼看**:`diff <(tr , '\n' <W49串|sort) <(tr , '\n' <W50串|sort)` ⇒
**只减不加,减掉的是 `tpdying` 与 `tpreach`**。出处是总监的 commit
`7335dcd3 director 22:xxZ: P4.2 two rulings closed (tpdying/tpreach returned, 61 -> 59)`。
⇒ 闸 (ii) **由串直接扛**(GH #513 那段手算本轮同样不需要;#513 仍 `open`)。

**并池预登记(数据落地之前写死,防拟合)**:W50 **自成一族**,
**不与 W49(61 id)、不与 W46+W47(62 id)、不与 W48(63 id)并池** ——
四个家族的臂串两两不逐字节相同,md5 各不相同。这是本台当初用来**支持** W46+W47
并池的同一条读法,方向一致地用在这里。

---

## 五、发波 W50 —— 记录全文 `iterations/reports/batch-desk/waves/W50_wave.json`

**树**:`--ref` 钉 40 位 SHA **`b73358bc0f764fd4a22171da2ae4556a0d5d38f7`** 不是 `main`;
`git ls-remote origin main` **读两次**(闸时点 + **发波脚本内第一条命令**,不等就 exit 9
什么都不发),两次都是 `b73358bc…`,且等于本地 HEAD。

### 5.1 四道闸真码

| 闸 | 真码 | 读数 |
|---|---|---|
| (i) 6h 节流 | `wave_throttle.py` ×4 | 决定时点 `03:12:58Z` **THROTTLED(exit 3)`margin 427s SHORT`**;`03:15:56Z`/`03:16:09Z` 仍 THROTTLED(249s / 236s)——**发波脚本两次都拒绝发,一台没起**;**动作时点、循环前最后一条、脚本内裸 `$?`**:`03:20:19Z` **`UNLOCKED (exit 0)`,`margin +14s past unlock`**,`THROTTLE_EXIT_AT_ACTION=0` |
| (ii) 有新东西可测 | 见第四节 | 走 (一) 支,串自己动了 |
| (iii) 预算围栏 | `wave_fence.py` ×2 | `FENCE_SPOT_EXIT=0` / `FENCE_OD_EXIT=0`,见第二节 |
| (iv) reclaim-blind | `reclaim_blind.py --wave-json W49_wave.json` | **`RB_EXIT=0`**,`4 paired seed(s) of 4 machine(s)` / `0 machine(s) reclaimed before the flip` / `not blinded` / **`NEXT WAVE: spot (GH #158 unchanged)`** |
| (iv) 输入:无欠收割 | 见第三节 | ⭐ **本轮在发波轮当轮写进 `gates` 键**,不是收割轮补写 |

⭐⭐ **闸 (i) 的 14 秒值得单独登记。** 章程记过的三次突破(W12 差 21s、W20 差 3m31s、
W44 差 3m00–4m19s)是**同一个机制**:钟只读一次,然后去做五分钟的门,回来直接发。
本轮把钟读在**动作上**,于是它在 `03:12` / `03:15` / `03:16` **三次说不**、
脚本三次什么都没发,直到 `03:20:19Z` 才说是 —— **这道闸本轮不是形式通过的,是真的挡过。**

### 5.2 两道内容闸

- **`CT_EXIT=0`**:59 id ⇒ **12 hero-scoped / 47 generic / 0 unresolved ⇒ 8 项**,
  `TERMS` 行与 W48/W49 **逐字节相同** —— 退回的 `tpdying`/`tpreach` **对载体项零贡献**,
  于是**串动了而载体要求没动**(与 W48 `stayattr` 入集同形,方向相反)。
  ⚠️ **本轮第一次调用误用 `--cand` 得 `exit 2`;这个工具吃 `--arm`。`exit 2` = 没跑成,
  永远不是「没有载体项」。**
- **`ARM_EXIT=0`**:**all 59 armed ids wired on HEAD**。
  ⚠️ **第一次调用走了 `tools/batch_test/soak/check_armed_wiring.py` 得 `exit 2`
  (`No such file or directory`);真路径是 `tools/batch_test/check_armed_wiring.py`
  ——不在 `soak/` 下。`exit 2` = 没跑成,永远不是「没接线」**(与章程记的同一条纪律)。

### 5.3 选种

`seed_roster_index.py --build` **在选种之前**跑(`ROSTER_EXIT=0`,索引现 **221 粒**已用种子)
—— 不 build 就会把 W49 刚烧掉的 `5404/5502/5574/5705` 当「从未用过」献回来。
窗口**右移**至 **`[5801,6200]`**(W49 在 `5401–5800`),**400 粒全未用**;
popcount 分布 `{0:26, 1:99, 2:153, 3:99, 4:19, 5:4}`。
对 popcount ≥3 的 **122 粒穷举** ⇒ **142 组**满足「8 项各 ≥2」的四元解。
选定 **`5813 5830 6072 6128`**,`CARRIER_GATE ids=12 seeds=4 exit=0`
(项由串推导不手打),逐项 ck3/cm2/lion3/od2/**pudge2**/wk2/sb2/zuus2。

### 5.4 ⭐⭐⭐ 本轮头号产出:`pudge` 载体供给的第三个数据点,而它换掉了一种解释

| 波 | 窗口 | 该窗口 pudge 供给 | 可行四元解 | 实得 pudge 载体 |
|---|---|---|---|---|
| W48 | `[5001,5400]` | 70 / 400 | 2506 | **4 / 4** |
| W49 | `[5401,5800]` | 49 / 400 | 2456 | **3 / 4** |
| **W50** | **`[5801,6200]`** | **49 / 400** | **142** | **2 / 4** |

**2/4 不是本台挑的,是 142 组解里的最大值**(穷举自己给的上界)。
⭐ **上一轮把 49 这个数登记成章程说的「每波削掉最富那层」的侵蚀。本轮否掉了那个解释**:
`[5801,6200]` 是一个**从未被任何波碰过**的全新窗口,侵蚀在它身上按构造不成立,
而 pudge 供给**还是 49**,同窗口其余七项是 65–158。
⇒ 更省的读法是:**`pudge` 本来就是八个载体项里最稀缺的那个**(约 12% 的种子带它,
其余项 26–40%),**载体要求会一直卡在 pudge 上,换哪个窗口都一样**。
**交总监 / 协同组:骑在 pudge 上的帧通道(`rotscope` 条件 (a),GH #472 / queue `strategy-26`)
不能按「4 粒全有」去排期。** 本波它的域只有两粒 —— 收割时这是**域的大小**,
**不许把更小的计数读成更小的效应**(已写进 `W50_wave.json:harvest_obligations`)。

### 5.5 起飞与市场

**spot**(不传 `--on-demand`,GH #158),`--slots 16 --hours 2 --rec-slots 8 --games 12`
4×1,**与 W39–W49 逐字节相同** ⇒ 「id 集与采集配置不同波动」本波满足(动的是 id 集)。

| 种子 | run_id 尾 | 实例 | 请求 AZ | 实得 AZ | 上机 |
|---|---|---|---|---|---|
| 5813 | `…_295740` | `i-01c5afea8732762b3` | 2a | **2a** | `03:20:21Z` |
| 5830 | `…_e56ba0` | `i-0c93e460fa74ae9be` | 2b | **2b** | `03:20:25Z` |
| 6072 | `…_f2182d` | `i-04428cac86ab59e53` | 2c | **2c** | `03:20:29Z` |
| 6128 | `…_eff51c` | `i-0201d8d5fad9ccb3b` | 2d | **2d** | `03:20:34Z` |

**四台 `InstanceLifecycle` 全为 `spot`**(run_id 的 `spot_` 前缀是硬写字符串**不是证据**);
**零 `re-aiming inside the ring` 行、零 `AZ RING EXHAUSTED` 行、零 `InsufficientInstanceCapacity`,
无降级** ⇒ GH #252 / #256 的验收**连续第二波干净 4/4**。
末台上机 **`2026-09-06T03:20:34Z` = W51 的闸 (i) 锚点**(GH #469 裁定 ①),slate spread **13s**
—— ⚠️ **下一轮不要抄这个数,读 `wave_throttle.py` 打的那一行**(本轮已复核它确实按 W50 锚:
`unlock 2026-09-06T09:20:34Z`)。
自毁 = self-terminate + **2h 看门狗** + one-time spot request。
`--dry-run` 先过一遍(`DRY_EXIT=0`,`#108` 的波次预算闸未拒发),四次 `SPOT_RUN_EXIT=0`。

⭐ **`launched_at` 本轮在发波轮就写齐了(GH #544)**:四行同时带 `launch_time` 与
`launched_at`(源 `describe-instances LaunchTime`,`launched_at_source` 写明),
**上一轮那个「下轮闸 (i) 会再次 UNCERTIFIABLE」的形状在 W50 上按构造不会复发**。
落地后**当轮复跑两条检测器**:`WGK_EXIT=0`(**274 checks, 0 failed**,含 W50 的 11 项新检查)、
`WT_EXIT=0`(**55 checks, 0 failed**)—— ⭐ **这正是上一轮「检测器和它要看的产物顺序反了」
那条的修法**:本轮把检测器跑在**写完波次记录之后**。

---

## 六、局数(章程第 7 步)

(a) **上一波 W49 最终局数**(上一轮已结清,照登不重算):四粒完整、零抢占 ——
`5404 ab29/ba13`、`5502 ab28/ba14`、`5574 ab28/ba14`、`5705 ab27/ba15`,
`scored_games 168 / unfinished 0`,`arm_depth 17.95–19.29`(门限 8),`thin_arm_seeds` 空。

(b) **本波 W50 为发波型报告 ⇒ 写预期局数**:预期 **~96 局计分**(4 粒 × 12 局 × 2 腿),
外加暖场局(**暖场局不算有效局**);发波时点 **0 局**。
⚠️ 上一轮已把 `expected_games` 这个预登记量**偏低约 75%** 交给总监(W49 实交 168)——
本轮仍按 96 登记以保持可比,**但它现在不能当「少交付」的分母**。

---

## 七、发波轮收尾波次点名(章程第 6 步,两条,零成本,必须当轮做)

`RC1_EXIT=0` / `RC2_EXIT=0`。

**① `pending,running`** —— 本台的 `soak-run` 标签**恰好四个**,与
`W50_wave.json:machines[].run_id` **一一对上**(`295740` / `e56ba0` / `f2182d` / `eff51c`)
⇒ **没有第五台(无泄漏)、一台不少(无掉臂)**;四台 `InstanceLifecycle` 全 `spot`;
四个 AZ 互不相同且都是请求的那个。
⚠️ **发波轮收尾要的不是「0 台在跑」,而是「恰好在跑本波登记的那四台」**;
「少一台不会举手」那一半正是由这条承担。

**② `terminated,shutting-down`** —— **为空** ⇒ 本波窗口内**无** `Server.SpotInstanceTermination`,
起飞头几分钟没掉臂(W33 那个形状本轮未出现)。terminated 只留约 1 小时(GH #375),
**这一步过期就读不到,所以必须在发波这一轮做。**

**账号侧的第五台不是本台的**:`i-0a7d4c7fbeb3a6712`,**`r7a.16xlarge`**,`us-west-2d`,
`02:10:36Z` 起,`Name=fft-cache-v1-repair` / `Project=final-table-trainer`,
机型/标签两处与本台对不上 ⇒ **不是本台的泄漏**;⛔ **未做任何处置**
(带别人的项目标签,终止既越权又不可逆)。
⚠️ **它是新的一台** —— 上一轮点名的是 `i-0ce7ddf08ea24cc7f`(`c7a.16xlarge`,
`fft-cache-v1-attempt3`)。⇒ **GH #515 / #528 那条「headroom 被账号上别的项目吃掉」
不是一次性事件,是一条在续跑的作业**;上一轮已落账 `$7.995`,本轮又换了一台
16xlarge 在跑。**本台无法自解,交 owner / 总监。**

---

## 八、铁律 6 / 11

- `bots/` / `game/` / `tests/` **本会话一行未改**(只动 `iterations/` 下的报告、波次记录、queue、章程)。
- **铁律 11**:本轮未遇到任何 `requires approval` / 权限对话框。
  唯一一次「等」是**闸 (i) 自己的 4 分钟未解锁**(见第五节),那是**门在拒绝**不是空转等待,
  且等待期间本台在做选种与写记录。

---

## 九、交棒

1. ⭐⭐⭐ **owner / 总监 —— GH #515 / #528 不是一次性事件。** 账号上 `final-table-trainer`
   **换了一台新的 16xlarge 在跑**(`fft-cache-v1-repair`,`r7a.16xlarge`,02:10:36Z 起),
   上一轮那台已落账 `$7.995`。本台自己那一行(`c6i.4xlarge`)逐位没动,
   **MTD 的全部增量都不是批测花的**,但**闸 (iii) 的 headroom 是共用的** ——
   `$21.166` 按上一轮估的燃烧率约一天内会被吃光,那时的 `THROTTLED`
   **不是本台超支**。本台无法自解。
2. ⭐⭐⭐ **总监 —— `$0.90` 单波成本常数的自动失效条款,第五轮连续点名仍未重裁。**
   W46 账单侧实测 `$0.959–1.022` 已越线;条款原文写着「**这个常数不许靠人记得去复查**」,
   而它现在正靠这个。
3. ⭐⭐ **总监 / 协同组 —— `pudge` 载体供给是结构性的,不是侵蚀**(第 5.4 节,三个数据点、
   两个不同窗口、供给都是 49/400)。**骑在 pudge 上的帧通道不能按 4 粒排期**;
   本波它的域是 **2 粒**。
4. ⭐⭐ **录像组 —— 59-id 家族的条件 (a) 从本波起才买得到**;另外 **61-id 家族(W49)
   的条件 (a) 仍欠**(那四粒的 `.dem` 已在 S3)。
   ⚠️ `tpdying`/`tpreach` 已出集 —— W50 对它们**一个字都没说**,不许拿本波给它们背书。
5. ⭐ **总监 —— GH #513**(把闸 (ii) 那段手算变成工具)仍 `open`。
   **连续两轮 (一) 支自己扛住了**,所以本轮和上轮都没用上它 —— 但那是运气不是机制:
   哪一轮串不动,手算就回来。
6. ⭐ **总监 —— GH #544 的另一半**:本轮已把 `launched_at` 在**发波轮**写齐、
   并在**写完记录之后**复跑两条检测器(`WGK 274/0`、`WT 55/0`)⇒ **W50 上不会复发**;
   但那仍是「本台每轮记得做」,`wave_throttle.py` 接受 `launch_time` 作别名的修法
   **仍未落地**。
7. ⭐ **本台 —— `campgrade` 独占波仍不存在**(#480 / #241,`owed_executions` 本轮仍报它)。

## 十、下一轮本台

1. **收割 W50**(`recover_verdict.py` 从 S3 逐局全量重算,走 `soak/` 不走 `validation/`;
   填 `machines[]` 的 `ab`/`ba`/`arm_depth`/`status_code`,**不许留 null**;
   **收割义务全文在 `W50_wave.json:harvest_obligations`,不要只读本报告的摘要**)。
2. 闸 (i) 的锚点是 W50 末台 —— **但不要读这个数,读 `wave_throttle.py` 打的那一行**
   (本轮已复核工具确实改锚到 W50)。
3. 开工第一条命令**重定向不管道、放后台给足 ≥45min**(本轮第八次踩管道那个坑)。

## 十一、铁律 6 读数

（见文末补记。）

## 十二、本轮 token

（见文末补记。）
