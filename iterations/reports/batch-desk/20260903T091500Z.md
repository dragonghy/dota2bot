# 批测台 2026-09-03T09:15–09:4xZ —— 发 W42(57-id 家族第二波)

**本轮真正的产出不是那四台机器,是「GH #427 丢掉的证据窗口顺手把 GH #271 的闸也关了,
而且关得没有声音」** —— `reclaim_blind.py` 本轮第一次跑 `RB_EXIT=2`,原因不是它自己坏了,
是 W41 收割轮从没往 `machines[]` 里写 survival/ab/ba,而精确读数早已随 #427 的 1h 窗口死掉。
按工具自己文件头里为这种情况写好的那条路(S3 最后一次上传时间当**下界**)recover 之后
`RB_EXIT=0`。**S3 的上传不过期,#427 的窗口过期** —— 这条绕路本来就该是收割轮的固定动作。

第二件:**W42 与 W41 的 `bots/` 树对象哈希不同,但两棵树编译出来逐字节同构**,
用编译器当判官量出来的,不是拿散文或注释剥离启发式量的。这关系到 57-id 家族能不能
一步凑够 8 粒。

---

## 1. 自检(铁律 10)

⚠️ **第一条命令又误用管道**(`| tail`),自检自拒并 `REFUSED: stdout is a pipe; exit 2,
nothing checked`(工具自报 `it has recurred 5x`;**同形第十七次,每次都是当轮第一条命令**)。
改走重定向后重跑。

⛔ **同一个错本轮犯了第二次,而且是在自检已经当面拒过之后** —— 发波循环里写的是
`bash spot_run.sh … | tail -12` 然后 `SPOT_RUN_EXIT[$s]=$?`,**那个 `$?` 是 `tail` 的**。
自检那次有工具举手,发波这次**没有任何东西举手**:`tail` 几乎总是成功,于是一台起飞失败
也会打出 `=0`。⇒ 本轮**不引用循环里打的 `SPOT_RUN_EXIT`**,起飞成败一律以
`describe-instances` 为准(它本来就是更强的证据:**独立于本会话自己的 transcript**)。
**登记这一条的意义在于它的形状**:自检把这个错抓了十七次,抓的都是**当轮第一条命令**,
而它真正咬人的地方是**发波循环**——那里没有守卫,失效方向是「起飞失败被读成起飞成功」。

⭐ **本轮九条腿全部跑完,真退出码 `EXIT=3`**(`legs run 9` /
`FINDINGS (exit 3) = cadence trunk-red(python)` / **`UNCERTIFIABLE (exit 2): none`** /
`NOT RUN (inside a leg): tests/test_rc_wrapper.py tests/test_selfcheck_lua_leg.py` /
`worst exit 3`)⇒ **本轮没有「没人看过」的那一侧**,exit 3 是**有发现**不是没跑成。

⭐ **上一轮那根「400s 上限」的棒本轮再次证实是误报**:命令在 400s 时被 harness 移到后台
继续跑,约 **25 分钟**后正常收尾(`[exited with code 0]`,脚本自己打的 `worst exit: 3`)。
交棒维持上一轮的**降级**状态。

**trunk 侧读数**:python 腿 `80 passed, 1 failed, 2 uncertifiable`。
- ⛔ **`test_detector_source_constants.py` 仍红**(GH #420 家族,**第五轮原样复发**)。
  **未 stash 重跑 ⇒ 不声称 main 也红**(工具自己那行:`Whether main is red too is NOT
  established by this line`)。
- ⭐ **`test_stale_waits.py` 本轮转绿** —— 上一轮为它开的 **GH #443** 那条红线不在了。
  **诚实边界**:本台只看到「这一轮它是绿的」,**没有查是谁修的、也没查它是否改了域**;
  录像组/总监关 #443 之前应各自核一眼。
- `UNCERTIFIABLE`:`test_rc_wrapper.py` / `test_selfcheck_lua_leg.py`(同前几轮)。
- Lua 腿真跑了:`73 tagged detector file(s), 0 failures`,但工具自声明这是
  **FAST SUBSET 非全套**,照此不夸大。
- `cadence` 两洞;`stable-v1` 锚 OK。

## 2. 成本与围栏

九月 MTD **`$7.056`**(免费 `budgets`;`refreshed 2026-09-03T04:41:39Z`)。
⭐ 这张快照**晚于** W40(09-02T21:31Z)**和** W41(09-03T03:32Z)两次起飞,
所以两波都已进入这个数。`forecast 111.209` 不是读数,不进围栏。
MTD `< $35` ⇒ CE 未触发,**`$0.01` 没花**。

**围栏(2 甲,保守:W41 即便已进 MTD 也再计一次)**
= `7.056 + W41 按需 2.15 + W42 spot 0.90` = **`~$10.11`** ≤ **$80**
⇒ 不跨任何 owner 可见告警档($50/$80/$100),**不欠解释行**。

进场实例普查:`state=pending,running` **空**;常驻只有 AMI `ami-0a990a26d89c66547`。

## 3. ⭐⭐⭐ GH #271 的闸本轮差一点就静默失效了

**第一次跑**:`reclaim_blind.py --wave-json W41_wave.json` ⇒ **`RB_EXIT=2`**,
`UNDECIDABLE: seed 3017: needs survival_min, or both create and update`。

**归因不是工具**:W41 的 `machines[]` 里只有 `launched_at`,**没有存活时长、没有 ab/ba/
arm_depth** —— 收割轮(06:15Z)拿到了这些数(报告 §4 里逐粒都有)却没写回波次文件。
而精确的那一路(EC2 `describe-instances` 的启动+终止时刻)在上一轮就已经确认**全丢**
(四台 `[]`,GH #427 0-for-3)。⇒ **#427 丢的不只是死因码,它把 #271 的闸也一起关了**,
而闸关掉的形状是 `exit 2` —— **按纪律「exit 2 不是通过」,但它也不会自己变成一个决定**。

**recover 的路是工具文件头自己写好的**(`survival_bound: "lower"` 那一节,立它的正是
GH #332):S3 里 `soak/<run_id>/` 最后一个对象的 `LastModified` 减去 `launched_at`,
是存活时长的**下界**(机器还得走完关机)。**S3 的上传不过期**。逐台实测:

| 种子 | `launched_at` | 最后一次上传 | 存活下界 |
|---|---|---|---|
| 3017 | 03:31:56Z | 04:24:19Z | **≥ 52.4 min** |
| 3030 | 03:32:00Z | 04:24:23Z | **≥ 52.4 min** |
| 3161 | 03:32:04Z | 04:24:32Z | **≥ 52.5 min** |
| 3329 | 03:32:08Z | 04:13:46Z | **≥ 41.6 min** |

四粒的 `ab/ba/arm_depth` 从 06:15Z 收割报告逐粒抄回(37/19/25.11、31/17/21.96、
37/16/22.34、29/13/17.95),`status_code` 据实写成 `LOST -- GH #427 window shut …`
(**不编一个没读到的码**),四行都带 `survival_source` / `depth_source` 说明出处。
回填进 `W41_wave.json` 后重跑 ⇒ **`RB_EXIT=0`**:

```
yield      : 4 paired seed(s) of 4 machine(s)
attribution: 0 machine(s) reclaimed before the flip
survival   : 4 of 4 machine(s) carry a LOWER BOUND (>=), not a reading
VERDICT: not blinded -- the wave delivered 4 paired seed(s).
NEXT WAVE: spot (the default; GH #158 unchanged).
```

⇒ **GH #271 在 W40 之后下的那道「一波按需」升级由 W41 消耗完毕,W42 回 spot。**

**这条要变成固定动作,不是本轮的巧思**:收割轮**必须**把 `survival_min`(或 `create`+
`update`)与 `ab/ba/arm_depth` 写进 `machines[]`。不写,下一轮的 #271 闸就答不了;
而答不了的形状是 exit 2,**恰好是最容易被读成「跑过了」的那一种**。已写进 W42 的收割义务。
⚠️ 边界:下界只对「活过换腿点」这一侧成立,对「换腿点之前就配上对」那一侧不成立
(工具自己会点名跳过),本波四台全在 40 min 之上,不吃这条。

## 4. ⭐⭐ 树漂移:哈希变了,编译出来没变

W41 的 ref 之后 `bots/` 只动了一处:`bots/BotLib/hero_chaos_knight.lua`,
commit `9053d199`(英雄组 07:53Z,GH #447),+18 行**注释**。
`game/` 树 `f13b532c` 逐字节未动。

**量了三次,并把互相打架的那一次也登记下来**:

| 判官 | 读数 | 采信 |
|---|---|---|
| `git diff` 过滤掉注释行 | 非注释改动 **0 行** | 支持 |
| 仓库自己的 `strip_lua_comments` + 逐行比 | **395/395** 非空行逐字相同(原始差 18 字节是这个朴素剥离器留下的行尾空白) | 支持 |
| **`luac5.1 -l -l` 反汇编,归一化指针地址与行号** | **差 0 行** | ⭐ **决定性** |
| `luac5.1 -s` 的 md5 | **不同** | ⛔ **未采信,但登记** |

最后一条值得单写:一个「编译后哈希」式的朴素判官会说**这是两棵不同的树**,
而带列表的反汇编说指令与常量池逐条相同、**唯一的差是行号**(注释把代码往下推了 18 行)。
⇒ **W42 与 W41 测的是同一棵树、同一个 arm 串。**

**这对家族凑数是有后果的**:57-id 家族要 8 粒,W41 给了 4 粒,W42 再给 4 粒。
「能不能并池」到目前为止**按树对象哈希判**,而按那条判据这两波不能并。
本台的读数是**能并**,理由是上面那张表。⛔ **并池的决定权在总监不在本台** ——
所以它写进了 `W42_wave.json:pooling_claim` **预登记**,在四个数落地之前钉住,
免得事后按结果反向拟合。

**闸 (ii) 因此走第三支不走第一支**:arm 串逐字节未变、树编译后未变 ⇒ 第一支**不成立**;
57-id 家族现 4 粒 / 199 计分局 < 8 ⇒ **第三支成立**。

## 5. 发波前的门(全过)

| 门 | 命令 | 读数 |
|---|---|---|
| 载体项 | `carrier_terms.py --arm` | `CT_EXIT=0`,11 hero-scoped / 46 generic / **0 unresolved** ⇒ 7 项 |
| 接线 | `check_armed_wiring.py --cand` | `CAW_EXIT=0`,**57 个 armed id 全部 wired on HEAD** |
| 种子索引 | `seed_roster_index.py --build` | `SRI_EXIT=0`(**在选种之前**跑,章程活规则) |
| 载体门 | `seed_draft.py --assert-carrier-from-arm` | **`CARRIER_GATE ids=11 seeds=4 exit=0`** |
| 波次键 | `tests/test_wave_gate_keys.py` | `WGK_EXIT=0`,**184 checks, 0 failed**(含新写的 W42) |
| 树钉 | `git ls-remote origin main` | `== local HEAD == 1baa449b…`(40 位,起飞前) |

⚠️ **一处章程勘误**:上一轮的「下一轮必做」把接线门写成
`tools/batch_test/soak/check_armed_wiring.py`,**那个路径不存在** ——
按它跑会 `EXIT=2` 打 `No such file or directory`,而 **exit 2 不是通过**。
真实路径是 `tools/batch_test/check_armed_wiring.py`(无 `soak/`)。已在本报告与
`W42_wave.json:wiring_gate` 里更正,并写进章程当前状态。

## 6. 选种:五个 2-载体项坐在**五对互不相同**的种子上

窗口 `[3001,3400]`(上一轮按预登记条款右移),`avail=396`、`excluded-in-window=4`
(恰好是 W41 抽走的四粒)。`masks=71`,popcount `{0:34,1:103,2:161,3:76,4:21,5:1}`,
**BEST min-per-term = 2**,787 个 mask 组合,最大 17 槽的有 25 个。**自律未放松。**

选 **3040 / 3101 / 3117 / 3204**,`--seed` 复核**四粒全 `no banked games`**。
逐项载体(全部 ≥ 2):

| 项 | 载体种子 |
|---|---|
| zuus | 3040, 3101, 3117, 3204(满席) |
| crystal_maiden | 3101, 3117, 3204 |
| spirit_breaker | **{3040, 3101}** |
| chaos_knight | **{3101, 3117}** |
| lion | **{3040, 3117}** |
| obsidian_destroyer | **{3117, 3204}** |
| skeleton_king | **{3040, 3204}** |

⭐ **五个 2-载体项落在五对互不相同的种子上**(W41 是四对)⇒ **掉任何一台,每一项都还剩
≥1 个载体**,逐座核过。这正是 GH #435 想买的东西。

⚠️ **诚实边界两条**:(1) 搜索**穷举了 mask 组合**,但每个 mask 组合只取了**一个**种子
指派 ⇒ 上面这个是 25 个最大槽组合里的最好,**不是全局最优**;
(2) 搜索脚本把 banked 索引找到了错路径(`tools/batch_test/soak/` 而非 `iterations/data/`),
于是索引贡献了 0 条排除 —— **事后核过,无害**:索引里 193 粒 banked 与 `[3001,3400]` 的
交集恰好是 `{3017,3030,3161,3329}`,四粒早已由波次文件排除,且 `--seed` 复核四粒全新。

⚠️ **零 pudge 连续第六波**(四份阵容 grep `pudge` = 0)⇒ `queue.json:strategy-26` /
`rotscope` 本波仍拿不到 pudge 证据,**收割时必须写成「无载体」,不许写成负向读数**。

## 7. 起飞:四台全 spot、四个不同 AZ、`requested == actual` 四比四

守卫在 **`09:32:03Z`** 打 `GATE-I OPEN`(闸解锁 `09:31:51Z`;09:19:14Z 那次调用被它按
**exit 9** 挡下并打 `remaining=757s`),`09:32:21Z` 四台发完。树钉 `--ref 1baa449b…`(40 位)。

| 种子 | 实例 | 请求 AZ | 实得 AZ | RUN_TOKEN | 起飞 | `InstanceLifecycle` |
|---|---|---|---|---|---|---|
| 3040 | `i-08f6554de561a016b` | 2a | **2a** | `1db27d` | 09:32:05Z | **spot** |
| 3101 | `i-0371d22b2345adff0` | 2b | **2b** | `13d27c` | 09:32:10Z | **spot** |
| 3117 | `i-0d24250f341351b93` | 2c | **2c** | `1f4265` | 09:32:14Z | **spot** |
| 3204 | `i-036f2c0ccba66ba56` | 2d | **2d** | `93ca3e` | 09:32:18Z | **spot** |

- **市场用 `InstanceLifecycle` 核不用 run_id 前缀**:四台全 **`spot`** ⇒ owner GH #158
  的 spot-first 默认已恢复,#271 的一波按需升级由 W41 消耗完毕。
- **零容量降级**:四台 `c6i.4xlarge` spot 请求全部在**请求的那个 AZ 一次填满**,
  没有退到 `c6a`、没有 `--on-demand`。
- ⭐ **GH #256 的严格验收(4 个不同 AZ 且无 re-aiming)本波再次通过,而且这次是在 spot 上。**
  W41 过的时候本台自己写了诚实边界:「按需放置比 spot 容易得多,**这过的是字面**,
  不是 spot 的 AZ 问题好了的证据」。本波把那条边界补上了 —— 这是 W36 以来
  **spot 容量压力下第一次严格通过**。⛔ **仍然是一波**:W36–W40 连续七波被咬,
  一次通过不构成「修好了」,**不要据此关 #256**。
- ⛔ **起飞成败不引用循环里打的 `SPOT_RUN_EXIT`**(见 §1,那是 `tail` 的码)。
  证据是 `describe-instances`:四台 running / spot / 四个两两不同的 `soak-run` 标签 /
  四个不同 RUN_TOKEN,四次调用 `09:32:03/07/12/16` **两两跨整秒**。
- **预计自毁 `~10:1x–10:3xZ`**(0.745 h/台),看门狗硬停 **11:32Z**,预期 **~96 计分局**。

## 8. 泄漏检查:CLEAN(判据是「恰好等于意图的那四台」)

- `state=pending,running` 普查(**固定动作,不是 `--leak-only`**):**恰好四台**,
  与 `machines[].run_id` **逐一对上**,没有多的也**没有少的**。
  ⚠️ 再登记:`--leak-only` 只报「多出来的」,**「少一台」永远不举手** ——
  这就是本轮用全量点名而不用它的原因。
- `describe-volumes`:**四卷全 `in-use`**,零孤儿卷。
- 常驻成本只有 AMI `ami-0a990a26d89c66547`。

## 9. 铁律 6 / 铁律 11

- **铁律 6**:`bots/` / `game/` **本会话一行未改**(改动全在 `iterations/`)。
  静态半 `bash tools/agent/luacheck_gate.sh` ⇒ **`GATE_EXIT=0 CLEAN`**
  (容器冷启自己装 luacheck,apt 包名 `lua-check`;`luacheck bots game: 0 warnings`);
  `arm_push_gate.sh` ⇒ **`ARM_EXIT=0`**(`core.hooksPath=.githooks`)⇒ 两次 `git push`
  各自又跑一次同一道门。**未用 `RULE6_BYPASS` ⇒ 本报告无「SKIPPED, not passed」行。**
  动态半(~100 min,GH #124)**未跑,不声称**。
- ⭐ **GH #446 本轮主动绕开**:发评论前先 `git fetch --deepen=600 origin main`
  (`DEEPEN_EXIT=0`,底板 **50 → 766** commit),免得 `claim_precheck.sh` 在 shallow clone
  上把正确引用报成 `OFF-TRUNK`。**这是绕路不是修复**,#446 保持 open。
- **铁律 11**:未触发 `requires approval`(GitHub MCP 本轮可用)。**无空转等待** ——
  闸 (i) 扣住的 ~13 分钟全部用在零成本工作上(成本、#271 recover、六道门、选种、
  波次文件、报告、deepen)。

## 10. 交棒

| # | 给谁 | 事 |
|---|---|---|
| ① ⭐⭐⭐ | 总监 | **并池判据要裁 —— GH #449 已开**:W41 与 W42 树哈希不同但**编译产物逐条相同**,按现行「树对象哈希」判据不能并,按编译器能并。**57-id 家族的 8 粒门槛现在卡在一个纯注释 commit 上**。预登记在 `W42_wave.json:pooling_claim`,**四个数落地之前**写的。 |
| ② ⭐⭐⭐ | 总监 | **GH #427 追评已发**(`#issuecomment-5523706619`):那个窗口丢的**不只是死因码,它把 GH #271 的闸一起关了**(本轮 `RB_EXIT=2`),而且失效形状是 exit 2。建议验收从「二选一」改成**两条并行**,新增的第 2 条(收割轮写 `survival_min` + `ab/ba/arm_depth`,拿不到精确值就用 S3 下界)**今天就能做、零成本**。 |
| ③ ⭐⭐⭐ | 总监 | **GH #352 仍是头号**:上一轮三条轴全换仍 DEGENERATE。W42 **故意一条轴都不换**(同 arm、同树、只换种子),是对「病灶是语料不是 id 集/种子数」这句话的**干净复制检验**,两个分支已预登记。 |
| ④ ⭐⭐ | 总监 | **GH #256 本波在 spot 上严格通过**(W41 那次是按需,本台自己标过是弱证据)。**仍是一波,不要据此关**。 |
| ⑤ ⭐⭐ | 总监 | **章程勘误**:上一轮「下一轮必做」把接线门路径写成 `tools/batch_test/soak/check_armed_wiring.py`,**该路径不存在**,照它跑是 `exit 2` 而不是通过。真实路径无 `soak/`。已在章程当前状态更正。 |
| ⑥ ⭐⭐ | 录像组 | `ckpush` / `roshdist` 的 (a) 取证:本波 `.dem`(四 run,`rec_slots=8`)。⚠️ W41 已证实本语料的 `analysis.json` **不带帧级检测器字段**,所以这活要么走 `.dem`,要么先加字段。 |
| ⑦ ⭐ | 录像组/总监 | **GH #443 本轮转绿**(`test_stale_waits.py` 通过)。本台只看到「这一轮绿」,**没查是谁修的、也没查域有没有改** —— 关 issue 前各自核一眼。 |
| ⑧ ⭐ | 总监/协同组 | **零 pudge 第六波** ⇒ `strategy-26`/`rotscope` 收割时写「无载体」,**不许写成负向读数**。 |
| ⑨ ⭐ | 总监 | **GH #435 本波拿到比 W41 更强的预登记**:五个 2-载体项坐在**五对互不相同**的种子上,**代码核过**掉任一台都不会把任何项打到 0。⚠️ 但**只深一层**:6 种两台同掉里有 **5 种**会把至少一项打到 0。 |
| ⑩ ⭐ | 存量 | `test_detector_source_constants.py` 第五轮原样红(#420) / #432 保持 open / **#388 的 W34 103 份 `.dem` 2026-09-22 到期(19 天)** / #446 / #429 / #418 / #412 / #408 / #402 / #395 / #375 / #285 / #290 / #313 / #271 / #329 / `strategy-5b` / `strategy-31/32/37/38`。 |

**下一轮本台 = 收割 W42**(四台预计 `~10:1x–10:3xZ` 自毁;看门狗 11:32Z)。
**收割四件本波特有**:
(a) ⭐ **spot 机有 SIR** ⇒ 死因码走 `describe-spot-instance-requests`(它活过
`describe-instances` 的 1h 窗口),**并且必须把 `survival_min` 与 `ab/ba/arm_depth`
写回 `machines[]`** —— 不写,下一轮的 #271 闸就又答不了(本轮的教训);
(b) **两种读数都报**:W42 单独 + W41∪W42 并池,标清哪个是哪个,并池交总监裁,
**一律先每粒 swap-average 再跨种子取算术平均**(铁律 4(i-d),永不按局数加权);
(c) `rec_slot_cost.py … --baseline rec_slot_baseline.json` 第三次非循环跑,
⚠️ **下载时要连 `.demclaim.json` sidecar 一起下**(只下 `*.analysis.json` 会 `exit 2`,
而 exit 2 不是通过 —— 上一轮踩过);
(d) 按 `winrate_prereg` 的两个分支报,**不许反向拟合**。

闸 (i) 对下一次例行发波解锁于 **`2026-09-03T15:32:03Z`**。

## 11. 本轮 token 用量

```
TOKENS total_in=16,199,133 out=66,768 turns=110
```

⚠️ 这是「到统计时刻为止」的数字,统计之后的收尾回合(commit / push / 两条 GitHub 产物)
不计入。⭐ **本轮偏高的归因,据实写**:闸 (i) 扣住的那 13 分钟里为了不空转做了额外工作
(#271 的 recover、四个判官的树等价判定、载体分散度的代码核验、shallow clone deepen),
以及**发波等待期间反复读同一个后台输出文件**(那部分是浪费,下轮改成一次阻塞式等待)。
