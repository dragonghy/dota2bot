# 批测台 2026-08-31T09:30Z — 纯收割轮(W31)

**一句话**:W31 四台全部落盘,`recover_verdict.py` **BARE_EXIT=0**、零 SKIP 旗标、零排除种子
(222 loaded / 198 scored / unfinished 0),gpm **+23.20**(3/4);**47-id 家族至此 8 粒并池
gpm +20.77 / 6-8 / SE 6.57** —— 三轮点名的「第一块在变厚」本轮变厚完成。
本轮**未发新波**(闸 (i) 12:17:21Z 才解锁),**未花任何 EC2 钱**,收尾零泄漏。
两件要交出去的:**W31 是 221:1 的 dire 横扫**(GH #352 拐点后第三个满波点,未恢复),
以及**自检 120s 预算第二次实测超支**,且它自己那条本该消歧的 NOTE 行**在超时那一路打的是 `?`**。

---

## 1. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` — **EXIT=3**,`legs run 8`。

- `FINDINGS (exit 3)`:**cadence**,一条 —— `GAP cadence strategy 08-31T04:20Z → 07:55Z (3.6h)`。
  不是本台的节奏洞,登记不认领。
- `UNCERTIFIABLE (exit 2)`:**trunk-red(python)** —— `67 passed, 0 failed, 1 uncertifiable`,
  未跑成的是 `tests/test_selfcheck_lua_leg.py`。
  **按铁律 10「`UNCERTIFIABLE` 不是通过」,本轮没有把它读成绿**,而是单独重跑了它(见 §6)。
- 其余各腿:`unlanded_commits` **OK**(`no unlanded work in the certifiable window`);
  引用审计无 `AMBIGUOUS`;稳定版锚点 `stable-v1` / `stable-v2` 两个 **EXISTS/PINNED/SHIPPED 全 ok**;
  快 Lua 检测器子集 **52 文件 0 失败**(**明写是子集,不是全套**);
  孤儿入集提议 **none**;过期等待 **none**;`UNKNOWN STATUS` **4 条**(#317,词表漂移,存量)。

---

## 2. 成本与围栏(铁律 1 / 步骤 2)

`check_costs.sh` —— 开工 running/pending **空**。

- **MTD `$73.90`**(`budgets`,快照 `2026-08-31T04:55:37Z`),CE 复核 **`$73.8997453436`** 逐位一致,
  **连续第三十六轮**。forecast 77.101 / limit 100.0。
- **快照仍是上一轮那一张**(04:55:37Z),**早于 W31 起飞(06:17Z)一个多小时** ⇒ 按滞后条款 (甲),
  W31 **确定不在这张快照里**;W30(00:16Z 起飞、01:12Z 自毁)距快照 3.7h,**仍在 4.3–11.3h 的滞后带内**,
  照记一波价。
- **围栏 = `73.90 + 0.90`(W30)`+ 0.90`(W31)= `$75.70` ≤ `$80`。**
- **明确未用条款 (乙)** 那个「MTD 增量对得上波费 ⇒ 已计入」的手法。
- **本轮不跨任何告警档**($80 未到)⇒ 不欠跨档解释行;刹车 $90 / owner 线 $100 未接近。
- **本轮 EC2 支出 = $0**(未发波)。唯一常驻成本仍只有一个 AMI `ami-0a990a26d89c66547`。

### 2.1 W31 单波成本结算 —— 自动失效条款**未触发**

四个 SIR 的 `create→update` **本轮第一手读到**(`describe-instances` 已把终止实例老化掉,
`describe-spot-instance-requests` 还在),未用 S3 末次上传代理:

| 种子 | SIR | create → update | 机时 |
|---|---|---|---|
| 2375 | `sir-kj4zjwkq` | 06:17:23 → 07:00:35 | 0.7200 h |
| 2393 | `sir-p8bfhbgq` | 06:17:28 → 07:12:24 | 0.9156 h |
| 2403 | `sir-2afzgtnn` | 06:17:33 → **07:10:21** | 0.8800 h |
| 2444 | `sir-7sjqh66p` | 06:17:37 → 07:12:48 | 0.9197 h |

合计 **3.4353 机时**,价带 $0.231–0.250/h ⇒ **实测单波 $0.794–$0.859**。
**上限 $0.859 ≤ $0.90 ⇒ 章程的自动失效条款不触发,常数不需重裁。**

---

## 3. ⭐ W31 吃下一次真抢占,而且这次**没有补发**

`sir-2afzgtnn`(种子 2403,`us-west-2c`)的终止码是 **`instance-terminated-no-capacity`**,
另外三台都是 `instance-terminated-by-user`(= 自己 `shutdown -h` 走的)。
**这是 EC2 在 07:10:21Z 收回的一台真抢占**,不是自毁。**连续第二波出现抢占**(W30 丢的是 2286 首发)。

**处置与 W30 不同,且是按章程判的而不是按习惯**:W30 那台在 flip 之前就被收走(`ab0/ba0`,`NO-PAIR`),
所以补发了一台;**W31 这台两条腿都在**(`ab36/ba10`),`arm_depth 15.65` = 深度门 8 的 **1.96 倍**
⇒ **它照常计分,不缺臂,不需要补发**。章程原文「一台被抢占 = 该种子缺臂」在这一波**不成立**,
因为抢占发生在 flip 之后。

**代价看得见,登记下来**:2403 的 ba 腿只有 **10 局**,是本波最薄的一条腿(另三粒 14/16/15),
**而它恰好是本波 side 项最大的一粒**(`gpm_ab −152.50` / `gpm_ba +242.88`)——
**一条被砍短、没排空的腿长什么样,就是这个样子**。它没有被任何门拦住(1.96x 阈值),
也不该被拦住;这里只是把「最薄的腿 = 被收走的那台」这个对应关系写下来,
**因为它是本波 side 项异常大的一个现成解释,而归因不是本台的活。**

`reclaim_blind.py --wave-json …W31_wave.json`(回填后当轮重跑,**BARE_EXIT=0**,stderr 空):

```
changeover 40.0 min  (measured bracket 34.80 < x <= 40.63, margin above the constant 0.63 min)  min_arm_depth 8.0
  seed 2375  43.2 min  instance-terminated-by-user       ab28/ba14  PAIRED [depth-checked]
  seed 2393  54.9 min  instance-terminated-by-user       ab39/ba16  PAIRED [depth-checked]
  seed 2403  52.8 min  instance-terminated-no-capacity   ab36/ba10  PAIRED [depth-checked]
  seed 2444  55.2 min  instance-terminated-by-user       ab40/ba15  PAIRED [depth-checked]
yield      : 4 paired seed(s) of 4 machine(s)
attribution: 0 machine(s) reclaimed before the flip
VERDICT: not blinded -- the wave delivered 4 paired seed(s).
NEXT WAVE: spot (the default; GH #158 unchanged).
```

⇒ **下一波走 spot 是读数点的,不是习惯**;`0 machine(s) reclaimed before the flip` 正是 §3 那段判断的机器版。

---

## 4. 收割 W31

`recover_verdict.py`,四个 run token **各自一个子目录**(`731a21` / `fde133` / `b5b4b9` / `c44eb5`),
**BARE_EXIT=0**(裸读,不经管道),stderr 空。
**未用** `--allow-pooled-basenames` / `--allow-unparseable` / `--min-arm-depth`
⇒ **本次收割没有任何「这是 SKIP 不是 pass」行可抄**。

- 语料:`files_seen 222` / `games_loaded 222` / `source_dirs 4` / `unparseable 0` /
  `pooled_basename_dirs_overridden 0`;**scored 198 / unfinished 0**。
- arm 串:**47 id / 414 字节**,`git show 1dd5705f…:iterations/streams/test_set.md` 第 2 行,
  与 W30 逐字节相同。

### 4.1 深度门(GH #269):**零排除**

`min_arm_depth 8`、`thin_arm_seeds []`。四粒 `arm_depth` **18.67 / 22.69 / 15.65 / 21.82**。
**最薄的一粒(2403,就是被抢占那台)是阈值的 1.96 倍** —— 过门,但**比 W30 最薄的一粒(19.29)还薄**,
是本家族两波里最薄的读数。`mean` 与 `comps_better` 的分母**是实打实的 4**。

### 4.2 四量读数(铁律 4(i-a):两个分层的**读数**一并登记,非局数;四量由工具自己打,**未手算**)

| 量 | arm 均值 | ab 层 | ba 层 | side | 反号 | side>arm | comps_better |
|---|---|---|---|---|---|---|---|
| gpm | **+23.20** | −69.00 | +115.41 | −92.21 | **是** | 4/4 | 3/4 |
| xpm | **+8.44** | −27.85 | +44.73 | −36.29 | **是** | 3/4 | 3/4 |
| deaths | **+0.04**(正=更差) | +0.51 | −0.44 | +0.47 | **是** | 4/4 | 3/4 |
| last_hits | **−1.00** | +0.40 | −2.38 | +1.39 | **是** | 3/4 | 1/4 |

**四个量全部反号,gpm 的 side 项 −92.21 是 arm(+23.20)的近四倍。**
按铁律 4(i-c),这四个量是**每粒 50/50 swap-average 之后的估计量**,
所以「反号 ⟺ |side| > |arm|」是**恒等式不是诊断**,对 arm 测得多准**零信息**。
**登记它是因为 (i-a) 要求登记,不是因为它构成任何反对意见。**
管精度的是 arm 自己的跨种子离散度 —— 见 §5。

**逐粒 gpm**:2375 **+30.75** / 2393 **−6.60** / 2403 **+45.19** / 2444 **+23.48**。

### 4.3 `suggested: hold_or_reject`

**本台不裁。** 按总监 §CO.4(08-30T22:0xZ),`suggested` 只是提示,
promote/reject 走铁律 2 的三条件。**并且见 §7 —— 条件 (b) 这一轮仍然没有可用的胜负通道。**

### 4.4 已知量具 bug 第 N 次复发(**不重复开 issue**)

`winrate_independent_of_gold` 打 **`0/222`**,而 `winner_by` 是 `{engine_natural: 222}`
⇒ **真值是 222/222**。成因是 `recover_verdict.py:416` 读 GH #108 改名前的旧桶名 `engine`。
本台 **08-29T09:13Z 报告 §3.3 已完整立案**,本轮只登记复发。

---

## 5. ⭐⭐ 47-id 家族并池到 8 粒 —— 三轮点名的那块**变厚完成了**

W30 与 W31 是**同一条 47-id arm 串**(逐字节相同)、**同在 cap-25 界后**,可以并池。

| | n | gpm 均值 | sd | SE | comps_better |
|---|---|---|---|---|---|
| W30 | 4 | +18.34 | 17.71 | 8.85 | 3/4 |
| W31 | 4 | +23.20 | 21.82 | 10.91 | 3/4 |
| **47-id 并池** | **8** | **+20.77** | **18.58** | **6.57** | **6/8** |

逐粒:`+38.03 / +19.58 / +20.76 / −5.02 / +30.75 / −6.60 / +45.19 / +23.48`。

**并池手法(铁律 4(i-d),写出来备查)**:每粒**已经**被 `recover_verdict.py` 做过 swap-average,
并池是对这 8 粒取**普通算术平均**,**没有按局数加权**。
(i-d) 点名的那个 −17.40 偏差在这里的形状会是:W30 231 局、W31 222 局,按局加权系数虽小,
**但那条禁令的理由不是系数大小,是 `按局加权 = arm + side·(N_ab−N_ba)/(N_ab+N_ba)` 这个恒等式**,
而本波 side 项 **−92.21** 是有记录以来最大的之一 ⇒ **这一波恰恰是最不能按局加权的那种波。**

**家族碎片化现状(总监,第五轮点名)**:45-id **8 粒**(gpm +26.60,7/8,冻结)/
44-id **4 粒**(W29,冻结)/ **47-id 8 粒(本轮变厚完成,+20.77,6/8)**。
三轮点名以来第一块在变厚的,现在厚完了。

---

## 6. ⭐⭐ 自检 120s 预算第二次实测超支,而**用来消歧的那条 NOTE 行在超时那一路打的是 `?`**

按 §1,单独重跑 `python3 tests/test_selfcheck_lua_leg.py`:**BARE_EXIT=2**,
`43 checks, 0 failures, 9 uncertified`。逐字:

```
NOTE  5a0 cost: ? file(s) in 120.1s (budget 120s) -- two numbers on purpose: set size and container speed move independently
UNC   5a0: the leg finishes inside 120s [it did not on THIS container -- ... the same 50 files measured 99.5s here and 133.3s on the batch desk (GH #358) ...]
UNC   5a / 5a2 / 5b / 5c  [the clean run did not finish inside 120s -- these four did NOT run; 5a in particular would pass vacuously]
NOTE  5a0 cost: ? file(s) in 120.0s (budget 120s)
UNC   5d / 5e / 5f / 5g   [the reddened run did not finish inside 120s -- these four did NOT run, and none of them is a statement about the leg]
UNCERTIFIABLE -- 9 check(s) did not run; this is NOT a pass.
```

**这是 GH #358 的第二个实测点**(上一轮 133.3s / 本轮两路各 120.1s、120.0s 撞顶)。
**本轮多出来的一件事值得追评到 #358**:该测试自己给的消歧办法是
「**跨轮读 NOTE 行,看动的是集合大小还是容器速度**」,
而 NOTE 行在**超时那一路**打的是 **`? file(s)`** —— **文件数只有跑完才知道**。
⇒ **恰好在需要消歧的那一路(超时),消歧所需的那个数是缺的**;
能读到文件数的轮次都是没超时的轮次,而那些轮次本来就不需要消歧。
**失效方向是「看起来给了工具,实际在唯一用得上的场合是空的」**,
与本仓库反复记的那类「读数照常产出、没有门举手」同族。
**修法建议不变(#358 header 已写):挪 sweep 进函数,不要退集** —— 本条只是补上
「顺便让 NOTE 行在超时路径上也打出已扫描文件数」,否则 #358 修好之前它无法自证。

---

## 7. ⭐⭐⭐ GH #352:拐点后第三个满波点,**横扫没有恢复**

按上一轮交棒的承诺(**零额外支出**,直接数已下载语料里的赢家):

```
W31 winner:    {'dire': 221, 'radiant': 1}
W31 winner_by: {'engine_natural': 222}
```

| 波(日期) | radiant | dire | dire 占比 |
|---|---|---|---|
| 08-25 | 33 | 43 | **0.566** ← 正常竞争语料 |
| 08-27 | 0 | 48 | 1.000 |
| 08-29 | 1 | 58 | 0.983 |
| W29(08-30,全四台) | 1 | 227 | 0.991 |
| W30(08-31,全四台) | 0 | 231 | 1.000 |
| **W31(08-31,全四台)** | **1** | **221** | **0.9955** |

**逐粒**:2375 `0/48`、2393 `0/61`、2403 `0/52`、2444 **`1/61`**。

**它逐位解释了本波的 winrate 读数**:三粒纯横扫 ⇒ `winrate` 恰好 `0.500`(恒等式);
2444 那一局 radiant 胜 ⇒ `0.512`。**与 W29 的 `0.503` 是同一个产物形状。**
⇒ 本波的 `winrate 0.503` 与 `comps_better winrate 1/4` **都不是读数,是占位符**;
**铁律 2 条件 (b) 在这几波上无法由 winrate 出具。** 拐点已跑满六天,**未恢复**。
`winner_by` 全 `engine_natural` ⇒ 222 局**全是遗迹真的倒了**,不是判定口径产物。

---

## 8. 本轮**不发波**(闸 (i) 未解锁)

- **(i) 6h 节流:不满足。** W31 起飞 `2026-08-31T06:17:21Z` ⇒ 解锁 **`2026-08-31T12:17:21Z`**;
  本轮工作时刻约 09:1x–09:3xZ,**差约 2.8 小时**。⇒ **例行波次本轮跳过,这就是那条不满足的**。
- **(ii)(iii)(iv) 未构成放行**:队列 25 条 pending 全数**不要求专波** ——
  `ROUTED_ARCHIVE_SCAN` / `ROUTED_ARCHIVE_REDUMP` / `ROUTED_RIDESHARE` / `REJECTED` /
  `RECEIVED_NOT_SCHEDULED` / `DEFERRED_PENDING_W3_RESOLVE`,**无一条持 `APPROVED*` 且待发**。
  显式请求本可豁免 (i)(ii),**但没有这样一条**。
- 围栏 $75.70 ≤ $80(§2),**不是本轮不发波的原因**;原因只有 (i)。
- **顺带交一条给总监(见 §10 ⑦)**:`strategy-5b` 仍是 `status=pending` /
  `ruling=APPROVED_CONDITIONAL` / `wave=W6` —— **W6 已过去二十余波**,
  这一行要么是早已执行完的陈状态,要么是掉在地上的棒。**本台不自行改判**,点名交出。

---

## 9. 局数(owner 2026-08-19 要求,固定一节)

**(a) 上一波次(W31)最终有效局数** —— 收割型报告,写实测:

| 种子 | ab | ba | arm_depth | scored | gpm | 备注 |
|---|---|---|---|---|---|---|
| 2375 | 28 | 14 | 18.67 | 42 | +30.75 | |
| 2393 | 39 | 16 | 22.69 | 55 | −6.60 | 唯一负 gpm |
| 2403 | 36 | **10** | 15.65 | 46 | +45.19 | **被抢占那台**;本波最薄的腿 + 最大的 side 项 |
| 2444 | 40 | 15 | 21.82 | 55 | +23.48 | |

合计 `222 loaded / 198 scored / unfinished 0 / source_dirs 4 / 零排除`。
**ab/ba 严重不对称(约 36:14),已按 (i-a) 注明**;标称是 4×12×2 = 96 局,实测 222 局。

**(b) 本轮在跑波次进度**:**无**。四台已于 07:00:35Z–07:12:48Z 全部终止,S3 零新增。

---

## 10. 交棒

1. **⭐⭐⭐ 总监 —— GH #352 仍是最大一件**,本轮交上**拐点后第三个满波点**(W31 221:1,0.9955,
   六天未恢复)。**重裁前不得再引 `winrate ≈ 0.5` 支撑铁律 2 条件 (b)**;§CO.4 里 W29 的
   `0.503` 待撤回,**本轮再添一个同形产物 `0.512`**。
2. **⭐⭐⭐ 总监 —— 47-id 家族已厚到 8 粒**(§5:+20.77 / 6-8 / SE 6.57),三轮点名的那块变厚完成。
   **本台不裁**,但请注意它与 45-id(+26.60,7/8,8 粒)现在是**两块同等厚度、方向一致**的读数,
   而 promote 卡的一直是条件 (a)。
3. **⭐⭐ 总监 —— GH #358 第二个实测点 + 一条新情节**(§6):消歧用的 NOTE 行
   **在超时那一路打 `?`**,即恰好在需要它的场合是空的。建议随 #358 一并修。
4. **⭐⭐ 总监 —— W30 的 promote 判断第三轮点名**(前两轮已交,未见裁定);
   W31 的 `suggested: hold_or_reject` 一并交出,**本台不裁**。
5. **⭐⭐ 总监 —— #285 第十五轮催**(本轮未选种,趋势线无新点,但天花板 16 的读数仍立在那里)。
6. **⭐ 总监 —— #308 第十次点名**(帧通道 `--rec-slots 1`,owner P1/P2 条件 (a) 卡在这里);
   **#271 建议写成必填键第六轮催**(本轮回填 W31 记录后 `test_wave_gate_keys.py` **14 checks 0 failed**);
   **#252 / #256 上一波双验收通过,第二次建议一并关闭**;
   **#282 连续第十三波零 UNEXPLAINED,第十六次建议关闭**。
7. **⭐ 总监 —— `strategy-5b` 状态存疑**(§8 末):`pending` + `APPROVED_CONDITIONAL` + `wave=W6`,
   而现在是 W31。请裁定是「陈状态,关掉」还是「掉了的棒,重新排期」。
8. **⭐ 存量**:#207 `zusstatic` 第三十八波 armed;#218 后续第三十四轮;#295 建议关闭;
   #329 / #321 待裁;`UNKNOWN STATUS` 4 条(#317)建议归一到文档化词表;
   #313 / #290 / #291 / #298 / #299 / #349 / #350(#355)照旧。
9. **queue.json 本轮已回填**:`strategy-25` / `hero-24` / `hero-25` 三条 ADMITTED 搭车行
   **两波以来一直 `result` 为空**,本轮写入 W30+W31 并池读数与那条 winrate 警告。
   **status 保持 `pending` 未动** —— 不自造新词,避免加大 #317 的词表漂移。

---

## 11. 泄漏检查(步骤 6)

- **开工**:`check_costs.sh` running/pending **空**。
- **收尾**:`check_costs.sh --leak-only`(零成本,不查花费)running/pending **空**。
  本轮**未发任何实例**,故不需要「逐个 id 对起飞记录」那一步;**要的是「0 台在跑」,本轮就是 0。**
- 常驻成本仍只有一个 AMI `ami-0a990a26d89c66547`(+ 其快照)。

---

## 12. 铁律 6(push 前)

- **`bots/` / `game/` 本轮一行未改**(改动只有 `iterations/` 下的报告、章程、queue.json、波次记录)。
- 静态半:`bash tools/agent/luacheck_gate.sh` ⇒ 见下方收尾登记;**退出码裸读不经管道**。
- `core.hooksPath` 已由开工自检上膛。
- 动态半(`lua5.1 tests/run_tests.lua`,~100 min,GH #124)**未跑,且本报告不声称它跑过**。
- **顺序条款(GH #290)**:本轮**先 push,再发表 GH #358 的追评**;
  草稿先过 `claim_precheck.sh`(容器为浅克隆,已按上一轮教训先 `git fetch --unshallow`,
  否则会复现那三条假 `OFF-TRUNK`)。

## 13. 铁律 11

本轮 MCP / 工具**未触发** `requires approval`,**无空转等待**。

---

## 14. 下一轮

**下一轮本台 = 发 W32**(闸 (i) `2026-08-31T12:17:21Z` 解锁)。届时需要:
`seed_roster_index.py --build` 先跑;窗口 `[2201,2600]` 的天花板上一轮已掉到 **16**,
**按上一轮实测的「一波两格」斜率,本次选种很可能需要右移窗口** —— 那是发波轮的活,本轮不预支。
`reclaim_blind` 的 `NEXT WAVE: spot` 已在 §3 打出。

## 15. 本轮 token 用量

见收尾登记(`python3 tools/agent/token_usage.py`)。
