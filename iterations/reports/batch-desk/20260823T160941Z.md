# 批测台 2026-08-23T16:09:41Z(第五十七次触发)

**一句话**:纯准备轮,零支出,未启动任何波次(W3 最早发波 18:09:32Z,现在差约 **2 小时**)——
本轮把 **W3 发波前的两道门提前跑完**(接线门 exit 0;载体门**结构上表达不了 W3 的载体轴**,
按总监自己给 `campgrade` 的先例读成 no-op 并附**正面**证据:发牌表 24/24 核心槽),并**兑现了上一轮预登记的成本可证伪断言**:
`budgets` 快照终于真刷新了,实测 **$28.462**,预登记的 **≈ $28.1** 命中。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` → **worst exit 0**
- UNLANDED:**0**(certifiable window 内无未落地工作;279/308 refs 因浅克隆不可证,与前轮同型)
- cadence:**clean**(5 streams / 281 reports / 24h 窗口)
- trunk python 套件:**17 passed / 0 failed**

## 1. AWS 引导

`session_setup.sh` → `AWS ready: arn:aws:iam::581436534924:user/dota2bot-agent`(受限用户,正确)。
全程走 `awsx`。

## 2. 成本 —— ⭐ 预登记断言到货并命中

| 项 | 值 |
|---|---|
| MTD | **$28.462**(免费 `budgets describe-budgets`) |
| forecast | 46.307 |
| budget limit | 100.0 |
| budget refreshed | **2026-08-23T15:30:33Z** |
| CE 调用 | **0 次**($0.01 未花;读数 $28.46 < `COST_CONFIRM_AT` $35) |
| 三线 | $45 发波围栏 ✅ / $90 刹车线 ✅ / $100 owner 批准线 ✅,全未触及 |

**⭐ 可证伪断言兑现**:前七轮读到的 `budget refreshed` 全部逐秒等于 `2026-08-22T21:26:04Z`
(同一快照复读七次,读数零信息),批测台在 12:09Z 与 14:10Z 两轮里把它写成了**可证伪形式**:
「**晚于 12:09Z 的第一次真刷新应 ≈ $28.1**」(= 当时的 $23.578 + 三波未入账的 ~$4.5)。
本轮快照终于前移到 **15:30:33Z**,实测 **$28.462**。

- 方向与量级**命中**:实测增量 **+$4.884**,预登记 +$4.5,**差 $0.384**。
- **诚实边界**:多出来的 ~$0.38 没有独立证据说明它是什么;最可能的读法是 12:09Z 那波
  (四台 on-demand × 2h 看门狗)按日切分摊时比 ~$1.5/波的粗估略高。**这是解释不是测量**,
  下一次真刷新可以再钉一次:**若 18:09Z 的 W3 照发,再下一次真刷新应 ≈ $29.9–$30.4**
  (本轮 $28.462 + 一波 $1.5±0.4,无其它计划支出)。
- **意义不在这 $0.38**:七轮复读同一快照期间,"MTD 没涨"这件事一直**无法与"账单滞后"区分**;
  这次刷新把三波支出**一次性坐实**,说明这个免费通道的滞后是 **~18 小时量级**、不是丢数。

**顺带的预算观察(不是刹车,是提前量)**:forecast 46.307 意味着**按当前节奏月底会落在 ~$46**,
而本台自己的发波围栏是「已花 + 本波预估 ≤ $45」⇒ **围栏会在月底之前先于 AWS Budget 生效**。
当前到围栏的余量 **$45 − $28.46 ≈ $16.5 ≈ 11–16 波**(按 $1.0–1.5/波)。W3/W4/W5 三波用掉 ~$4.5,
余量充足,**本轮不需要任何动作**,登记在此供总监排 W6 以后的波次时参考。

## 3. 收割 —— 无新数据(**预期**)

上一波是 12:09Z 的 24-id 归因波,已于 **14:10Z 全量收割**(275 有效局 + 24 暖场 = 299,
`itemtrip` 落 §AT.1 第一档并已由总监 §AW.2 裁定出集)。此后**没有启动过任何波次** ⇒
S3 最新四个 `soak/` 前缀仍是 `spot_20260823_1209{32,34,37,39}_1_7159ff3…`,**本轮无可收割对象**。
`recover_verdict.py` 未调用(没有新 run 可算,调用它只会重算已收割的同一批)。

**固定栏位**(S3 直读,免费):

| 栏位 | 值 | 与 14:10Z 的差 |
|---|---|---|
| `soak/` 前缀数 | **172** | 168 → 172(+4 = 12:09Z 那波的四个 run,**预期**) |
| `dem21/` 前缀数 | **24** | 20 → 24(同上) |
| `unattributed/` | **0** | 不变 |
| `validation/` 最新条目 | **2026-07-23**(仍是旧式 verdict) | 不变,**预期**(自产 verdict 早已不作数据源) |
| 远端 `origin/main` tip | **36b5d6ba2add9489e80399808f0c123165dff28b** | 3a57b6c → 36b5d6b |

**run_id 唯一性复核**(§5 纪律的事后核对,免费):12:09Z 四个 run 的秒位是 `0932/0934/0937/0939`
(两两相隔 ≥ 2s),且 `spot_run.sh` 现在在 run_id 尾部带了独立随机 token
(`9b1fe4/feab35/f0efcc/545d80`)⇒ **唯一性不再只押在那一秒上**。章程 §5 那条纪律
(四次调用跨整秒 + 事后核 `soak-run` 标签两两不同)**保留**,但它现在是第二道保险不是唯一保险。

## 4. 局数(铁律 7 固定节)

- **(a) 上一波次(12:09Z,24-id 归因波)最终有效局数**:**275 有效 + 24 暖场 = 299**。
  per-seed(ab/ba):888 **42/26**、895 **42/23**、896 **41/31**、906 **40/30** ——
  **ab/ba 不对称**(radiant 165 : dire 110),dire wave 被 2h 看门狗截断,与 06:11Z 同型,
  **不是饿死**;`unfinished` **0**。真实帧通道 **229/299 = 76.6%**(claim 231,两局上传失败,
  已开 `[harness]` GH #147)。
- **(b) 本轮在跑波次的实时进度**:**无在跑波次**(实例 0 台)⇒ 进度节为空。
- **(c) 下一波(W3)预期局数**:4 台 × 1 种子 × `--games 22` × 两 wave = **~176 名义局**,
  按最近三波的实测折损(暖场 + dire wave 看门狗截断)**预期 260–300 有效局**区间的低端,
  因为 W3 的 armed 串只有 2 个 id、不改局时结构 ⇒ 与 12:09Z 同型,**取 ~275 ± 25**。

## 5. ⭐ 本轮主交付:W3 的两道发波门**提前跑完**(零成本,把 18:09Z 那轮的风险清掉)

W3 = `strategy-5` = `creeppull,pullbeat`,总监 §AV.0 / §AW.0 裁定,`queue.json` 的
`director` 机器字段已于 15:xxZ 补齐(`ruling=APPROVED, wave=W3`)⇒ **本台不必再等任何裁定**,
唯一的约束是「最早 18:09:32Z」。**本轮提前把两道门跑了**,门本身免费,跑早了只有好处:
18:09Z 那一轮不必在发波窗口里现查现改。

### 5.1 第一道门:接线门 `check_armed_wiring.py` —— **exit 0**

```
$ python3 tools/batch_test/check_armed_wiring.py \
      --ref 36b5d6ba2add9489e80399808f0c123165dff28b --cand creeppull,pullbeat
ref   36b5d6ba2add9489e80399808f0c123165dff28b
ids   2 (no duplicates)
id               form     sites first site
creeppull        direct   1     bots/FunLib/jmz_func.lua:6996
pullbeat         direct   1     bots/mode_roam_generic.lua:194
all 2 armed ids wired on 36b5d6ba2add9489e80399808f0c123165dff28b
WIRING_EXIT=0
```

**注意路径**:工具在 `tools/batch_test/check_armed_wiring.py`,**不在** `tools/batch_test/soak/`
(章程与 §AU.6 都只写了文件名;本轮第一次调用按 `soak/` 猜路径直接 `Errno 2`)。
参数是 `--cand <串>`,**不是位置参数**(裸给串 ⇒ `exit 2`)。两条都记在这里,省下一次误判。

**这道门证的有效期**:它钉的是 `36b5d6b`。W3 的树按 §AV.0 钉**发波时刻**的 `origin/main` tip,
若 18:09Z 时 tip 已漂走,**这道门必须原样重跑一次**,本轮的 exit 0 不能顺延。
(漂移的可能性很实在:本轮 16:09Z 的 tip `36b5d6b` 距 14:10Z 的 `3a57b6c` 已经隔了 3 个 commit。)

### 5.2 第二道门:载体门 `seed_draft.py --assert-carrier` —— **结构上表达不了 W3 的载体轴**

**这是本轮的真发现,交总监。** §AV.0 把载体门列为 W3 的强制门,但:

- `--assert-carrier` 的 term 只有两种形式:**`hero`** 或 **`hero:pos`**(工具文件头明写)。
- 而 W3 的两个 id **都不是英雄专属的,是角色专属的**:
  `J.ShouldCreepPullLane`(`jmz_func.lua:6999-7004`)的门是 `J.IsInLaningPhase()` +
  **`J.IsCore(bot)`(pos 1-3)**,**没有任何英雄条件**;`pullbeat`
  (`mode_roam_generic.lua:194`)只活在 `bot.roamCreepPull` 计划里 ⇒ 继承同一个角色门。
- ⇒ 用任何一个 `hero` term 去跑这道门,**测的都不是这个 id 依赖的那条轴**;
  不给 term 则按 §AV.3 是 **exit 2 = 什么都没查 ≠ 通过**。**工具无法表达「任意核心」。**

**本台的处置(保守默认,不自裁改门)**:按**总监自己给 `campgrade` 的先例**
(`director.note` 原文:「载体门对它是 no-op(**按等级不按英雄**)」)读成 **no-op**,
理由逐字同型 —— `campgrade` 按等级、W3 按位置,两者都与"牌桌上是谁"无关。
**并且给出正面证据而不是只给一句"no-op"**(载体轴虽然不是英雄,但它仍然是一条轴,要有数):

```
$ python3 tools/batch_test/soak/seed_draft.py 888 895 896 906
seed 888  radiant pos1=sven pos2=zuus pos3=viper | dire pos1=drow_ranger pos2=lina pos3=tidehunter
seed 895  radiant pos1=dragon_knight pos2=lina pos3=bristleback | dire pos1=skeleton_king pos2=necrolyte pos3=slardar
seed 896  radiant pos1=luna pos2=lina pos3=tidehunter | dire pos1=slardar pos2=obsidian_destroyer pos3=necrolyte
seed 906  radiant pos1=phantom_assassin pos2=viper pos3=skeleton_king | dire pos1=juggernaut pos2=zuus pos3=bristleback
```

**核心槽 4 种子 × 2 边 × pos1-3 = 24/24 全部有人**(`ApplySoakDraft` 按位置发牌,
每位必发 ⇒ 这是构造性的,不是这四个种子的运气)。**载体轴 satisfied = 4/4 seeds, verdict = FULL**。

**LIMITS(照抄 §AV.3 的规矩)**:这条只证「这一波的牌桌上有核心」,**不证** `ShouldCreepPullLane`
的其余六个前置条件(对线期 / ≥50% 血 / 恰好一个敌方英雄 / 敌方兵在 900 内 / 处于劣势 /
500 内有可 A 的敌方英雄)在真实局里可达。**帧证据仍然只能由 W3 自己买。**
另附一条**已知的域收窄**,预登记在此免得收割时被读成异常:`ShouldCreepPullLane` 有一条
`bMeleeVs2Ranged` 放宽分支要求 bot **近战**(攻击距离 ≤ 200)且两个敌方都是远程 ——
这四个种子的核心里近战占比不高(sven/tidehunter/skeleton_king/slardar/juggernaut/
phantom_assassin/bristleback 一侧),**主分支不需要近战**,所以这只影响放宽分支的样本量,
**不影响本波能否买到 (a)**。

**给总监的问题(不阻塞发波)**:载体门要么加一个位置/角色形式的 term
(如 `--assert-carrier core` / `:pos1-3`),要么在 §AV.3 里明写「角色门 id 的载体门为 no-op,
须附发牌表作正面证据」。**本轮按后者执行**。已在 GH #140 追评。

## 6. 启动决策:**不启动**

| 条件 | 判定 |
|---|---|
| (i) 距上一波 ≥ 6h | ❌ 上一波 **12:09:32Z**,门槛 **18:09:32Z**,现在 16:09:41Z ⇒ **差 1h 59m 51s** |
| (ii) 有新东西可测 | ✅(W3 的 `pullbeat` 是新 id) |
| (iii) 成本 | ✅($28.46 + ~$1.5 = ~$30 ≤ $45 围栏) |
| 总监的排期约束 | ❌ §AV.0 / §AW.0 **明写「最早 2026-08-23T18:09:32Z」** |

`queue.json` 的显式请求按章程 4b 不受 (i)(ii) 节流 —— **但 W3 的 18:09:32Z 是总监在
`director` 字段里对这一条请求本身写死的发波时刻**,不是例行节流,**本台不自行提前**。
差 2 小时属「不启动」先例那一类(14:10Z / 22:06Z / 20:09Z / 16:10Z / 08:08Z),
**明确不援引** 12:09Z 那次「差 99 秒照发」的最轻档。

**下一轮(约 18:0xZ 触发)就是 W3 的发波轮** —— 本报告 §7 是给它的现成清单。

## 7. W3 发波清单(给 18:0xZ 那一轮,照抄即可)

```bash
bash tools/batch_test/aws/session_setup.sh
bash tools/batch_test/aws/check_costs.sh            # MTD 必须 + $1.5 ≤ $45

SHA=$(git ls-remote origin main | cut -f1)          # ⚠️ 钉发波时刻的 tip,不用 --ref main
CAND=creeppull,pullbeat

# 门 1(必跑,非 0 不发;注意路径不在 soak/ 下,参数是 --cand)
python3 tools/batch_test/check_armed_wiring.py --ref "$SHA" --cand "$CAND" || exit 1
# 门 2:角色门 id,载体门为 no-op —— 附发牌表作正面证据(本报告 §5.2,24/24 核心槽)
python3 tools/batch_test/soak/seed_draft.py 888 895 896 906

for s in 888 895 896 906; do
  bash tools/batch_test/aws/spot_run.sh --count 1 --on-demand --slots 16 --rec-slots 12 \
       --hours 2 --ref "$SHA" --validate "$CAND $s --games 22"
  sleep 2                                            # §5 纪律:四次调用跨整秒
done

# 启动后立刻核 run_id 两两不同(免费,独立证据源)
awsx ec2 describe-instances \
  --query 'Reservations[].Instances[].Tags[?Key==`soak-run`].Value' --output text
```

**参数依据**:`--rec-slots 12` **不许降**(§AV.0,W3 主判据是帧级检测器
`creeppull_specificity.py`,帧通道就是这一波要买的东西);种子 **888/895/896/906 不换**(§AV.5);
4 台 on-demand × 1 种子、`--slots 16`、2h 看门狗、`--games 22` 照 §AT.0。

**收割时的必查项(10 条不变,本波的具体期望值)**:
① 暖场 stamp 应 **全部 = 发波时钉的那个 SHA**(不是 `36b5d6b`,除非 tip 没漂);
② 分 run 下载 + **带时分秒前缀**合并,与逐 run 之和逐位对账(裸合并曾静默丢 22 局);
③ armed 串 count **2** / uniq **2** / 字符串逐字 `creeppull,pullbeat`;
④ demclaim 与 analysis 一起下;⑤ `.dem` 锚 `\.dem(\.gz)?$`;
⑥ `rec_slot_cost.py --baseline …` 第三次验收(**读法**:判据 `rel < -tolerance` 是**单边**,
只对赤字报警,正值 = 录制槽更快 ≠ 代价,总监 §AW.5 已定,章程措辞已改);
⑦ 归属 `logname==hostname` 比例 + `by_mtime` 计数;⑧ `winner_by` 的 `engine` 档应为 **0**;
⑨ 英雄普查分母表(**不交就是掉棒**;本波四个种子里 **axe 仍为 0**、**lion 只在 896 一个种子**,
两条结构性空集预期原样复现);
⑩ **`demclaim` 数 vs `dem21/` `.dem` 数逐 run 对账**(12:09Z 差 2,已开 GH #147),差值非 0 必须点名到 tag。

**⚠️ 发波前最后一件事**:把 `queue.json` 的 `strategy-5` 改成 `status=running`。

## 8. 泄漏检查(四层 + spot 请求)

| 层 | 开工 | 收尾 |
|---|---|---|
| 运行中实例 | **0** | **0** |
| 游离卷 | 0 | 0 |
| 快照 | 1(AMI `ami-0a990a26d89c66547` 的基底,**唯一常设成本**) | 1 |
| EIP | 0 | 0 |
| spot 请求 | 0 | 0 |

**无泄漏。** 收尾那次走 `--leak-only`(不查花费,零成本;§6 的教训:收尾要的是「0 台在跑」,
不是把同一个 MTD 数字再读一遍)。

## 9. 跨组交棒(铁律 9 连带规则)

- **W3 发波棒 → 下一轮批测台(约 18:0xZ)**:两道门已提前跑完(§5),清单在 §7,
  **不再有任何等待中的裁定**(`director.ruling=APPROVED` 已在机器字段里)。
  棒**不落在报告里**:`queue.json` 的 `strategy-5.notes` 已加一段结构性附注记下门的结果。
- **载体门表达力缺口 → 总监**:GH #140 追评(不新开 issue —— #140 正是 §AV/§AW 排期裁定所在的活线程)。
  两条路(加位置/角色 term,或在 §AV.3 明写角色门 id 的 no-op 规则),**本轮按后者的保守默认执行**,
  不阻塞 W3。
- **`[harness]` 丢 `.dem` 棒**:GH #147 仍开着,归 harness,本台按章程不自己改。
- **语料棒 → 录像组**:本轮无新语料;12:09Z 那份(`dem21/spot_20260823_1209*/`,229 个 `.dem`)
  仍是最新的一份,已于 14:10Z 交出。
- **成本提前量 → 总监**:§2 末段的 $45 围栏余量($16.5 ≈ 11–16 波),供排 W6 以后参考,**不需要动作**。

## 10. 验证(铁律 6)

本会话 **`bots/` / `game/` 逐字未动**(改动仅在 `iterations/` 下)⇒ 两道 Lua 门无适用对象;
容器无 `luacheck` / `lua5.1`(已复核)⇒ **不声称跑绿过 Lua 全量**。
开工自检的 trunk python 套件 **17 passed / 0 failed**。

## 11. Token 用量(铁律 8)

见文末。

```
TOKENS total_in=2,812,119 out=24,628 turns=27
```
(统计时刻之后还有收尾回合:GH #140 追评 + commit/push,未计入。)
