# 批测台 2026-09-06T09:12Z — 不发波轮:闸 (iii) 打 CLEAR,而诚实重建说 $50 已经越线了

**一句话**:上一轮(06:14Z)预判「约 11:30Z 被别人的算力撞 $50」——**本轮它已经发生,
而且工具看不见**。`wave_fence.py` 打 `CLEAR / headroom $9.572`,但它读的 `$39.328` 是
**06:02:46Z 的快照**,而 **CE 对 2026-09-06 一行数据都没有**(实测,不是推测)。把今天
账号上已经跑掉的机时按**同一 AZ 的实测现货价**补回去,真实 MTD ≈ **$53.12**
⇒ **$50 围栏已被突破约 $3.12,突破它的 $12.69 里没有一分是批测的**。
⇒ **本轮不发波**(闸 (i) 也差 11m08s 未解锁,但那不是主因;主因是闸 (iii) 诚实算出来是红的)。

---

## 一、自检(铁律 10)

⭐ **开工第一条命令第 6 次误用管道** —— 脚本自拒:`REFUSED: routine_selfcheck.sh stdout
is a pipe; exit 2, nothing checked`,并自己点名「同形已复发 5 次,每次都是当轮第一条命令」
(本轮是第 6 次)。**那个 `EXIT=2` 是 REFUSED 不是通过。** 改重定向后重跑。

⛔ **第二件事,而且是本台自己造成的:重跑那次被我自己的 `timeout 600` 砍断了 —— `EXIT=124`。**
⇒ **本轮自检没有跑完,没有 `selfcheck worst exit` 那一行**。**「没跑完」不是「通过」**,
这与铁律 10 里 `SKIP`/`UNCERTIFIABLE` 那条同族。
⚠️ **交棒给下一轮:上一轮交棒写的是「重定向不管道、给足 ≥45min」——
本轮把管道那一半又犯了一次,把 ≥45min 那一半也犯了(给了 10min)。两半都要照办。**

**跑完并读到结论的腿(逐字):**

```
no expired admission wait in any live 当前状态 block (6 charter(s), 72 settled id(s))
stable-v1 / stable-v2 anchors: 2 anchor(s) checked -- OK
promote-time constraints: scanned 157 live gate id(s), 13 carrying a PROMOTED note
                          FROZEN none | atoms registered: 3, all ok
=== trunk health (python test suite) ===
102 passed, 0 failed, 2 uncertifiable
uncertifiable: tests/test_rc_wrapper.py tests/test_selfcheck_lua_leg.py
```

- ⇒ **`pullcad` 陷阱本轮 clean**(无一个活 gate 点名已 promote 的 id)。
- ⇒ **python trunk 侧 102 passed / 0 failed** —— 但 ⚠️ **两个 UNCERTIFIABLE 照抄边界**:
  `tests/test_rc_wrapper.py` 与 `tests/test_selfcheck_lua_leg.py` **没跑**,原因同族且逐字都是
  **`no lua5.1 on PATH`**(rc.sh 自测的两条会「空过」,因为 `127 != 2` 恒真)——GH #171 同族。
- `cadence`:**1 条 `GAP cadence strategy`,不是本台**。
- `owed-executions` 里点名本台的仍是那一条:`OWED campgrade_wave_type_premise_reading
  GH #480 / GH #241  executor=batch-desk(排 campgrade 独占波的那一轮)+ 录像组(读数)`
  ⇒ **连续第四轮欠着**(理由见 §6.3)。
- `5a0` 腿:`84 file(s) [discovered -- the leg never reached its count line] in 120.1s
  (budget 120s)` **超预算** ⇒ 连带 4 条 `UNC`,与上一轮同形。

**⛔ 没跑到的腿:`trunk health (fast Lua detectors)` 与最后的汇总横幅。**
⇒ **Lua 侧 trunk 本轮红不红,本台答不了**(上一轮它是 `84 tagged detector file(s), 0 failures`,
但那是上一轮的读数,不是本轮的)。

## 二、⭐⭐⭐ 成本与闸 (iii):工具说 CLEAR,诚实算术说已经越线

### 2.1 工具的两个读数(原样)

`COSTS_EXIT=0`:

```
== running/pending instances (should be empty unless a batch is live) ==
                                    <- 空(⚠️ 这是带 tag:Name=dota2bot-* 过滤器的空,见 §4)
== month-to-date cost ==
39.328  USD
   source: budgets/dota2bot-batch (free) | forecast 179.873 | budget limit 100.0
   budget refreshed 2026-09-06T06:02:46Z          <- ⭐ 三小时前的快照
   >= $35 — confirming against cost-explorer ($0.01): 39.3278311289 USD
```

`wave_fence.py --planned 1.10`,`FENCE_EXIT=0`:

```
actual (MTD)     : $39.328
pending waves    : $0.000
planned          : $1.100
projected total  : $40.428
  ACTUAL alert   : $50.00   NOT yet crossed   AWS state=OK
fence            : $50.00   operative ceiling: $50.00
headroom         : $9.572 after this wave
WAVE_FENCE: CLEAR (exit 0)
```

### 2.2 ⛔ 为什么这个 CLEAR 不能用:它的输入对「今天」是结构性失明的

**这不是怀疑,是量出来的。** CE 逐日 × SERVICE(`2026-09-04..09-07`,`CE_EXIT=0`):

| 日 | EC2-Compute | 备注 |
|---|---|---|
| 09-04 | 5.2210 | |
| 09-05 | 20.1059 | 其中 $17.109 是 `c7a.16xlarge`,不是本台机型(上轮已钉死) |
| **09-06** | **(整日无任何分组行)** | ⭐ **CE 对今天一行都没有** |

⇒ `$39.328` 实质是**「截至 09-05 收盘」**的数。章程铁纪律(甲)说的「`budgets` 的 MTD 对
EC2 有 4.3–11.3 小时滞后、失效方向是**系统性偏低**」在本轮**顶格成立**。

### 2.3 把今天补回去(实测机时 × 实测同 AZ 现货价,不是估)

**两台都不是批测的**,都带 `Project=final-table-trainer`,都在 **us-west-2d**:

| 实例 | 机型 | 起 | 止 | 机时 |
|---|---|---|---|---|
| `i-0a7d4c7fbeb3a6712` (`fft-cache-v1-repair`) | r7a.16xlarge spot | 02:10:36Z | **08:23:29Z**(`User initiated`) | **6.2147 h** |
| `i-0114b249d00ad956e` (`fft-cache-v1-repair2`) | r7a.16xlarge spot | **08:24:02Z** | 仍在跑 | 0.7567 h(至 09:09:26Z,**还在涨**) |

⭐ **上一台被终止 33 秒后就起了下一台** —— 这是一个**修复重试循环**,不是一次性作业。

**价格不取价带,取实测点**:`describe-spot-price-history`,`r7a.16xlarge` / us-west-2d,
09-05T20:00Z 起 **全窗口平**:`1.8363 → 1.8209 (02:00Z) → 1.8209 (06:00Z)`。
⇒ 取 **$1.8209/h**(上一轮报的 `$1.82–2.79` 是**跨 AZ** 价带;这两台都在最便宜那个 AZ,
所以**取带内最低点是有据的,不是乐观**)。

```
外部 #1 : 6.2147 h × $1.8209 = $11.316
外部 #2 : 0.7567 h × $1.8209 = $ 1.378   (以 $1.82/h 继续增长)
本台 W50: 章程常数                $ 1.100   (实测 3.4 机时 ⇒ 账单侧约 $0.82)
未入账合计                        $13.794   其中本台 $1.10 = 8.0%
```

**重建后的真实账号 MTD ≈ $39.328 + $13.794 = `$53.12`**

⇒ **`$50` 这一档 ACTUAL 告警已经越线约 `$3.12`**,而**越线的那 $12.69 不是批测花的**。
⇒ 按闸 (iii) 自己的算术(fence = 最低的未越线 ACTUAL 档),**诚实输入下本轮是 THROTTLED**。

### 2.4 往前看:不是「快到了」,是「已经在里面」

外部那台按 $1.8209/h 持续跑 = **$43.7/天**。即使本台**一波都不发**,账号也会在**约一天多**
之后撞 `$100` 上限。`budgets` 自己的 forecast **`$179.873` vs limit `$100`** 与此一致。
⚠️ **owner 那封 `$50` 告警邮件随时会到,抬头是 `dota2bot-batch`,而九月至今批测侧只花了约 $4.1**
(09-01..09-05 的 `c6i.4xlarge` 行 + 09-06 的 W50)。

## 三、闸 (i):本轮也没解锁(但这不是不发波的理由)

`wave_throttle.py`,`THROTTLE_EXIT=3`:

```
anchor wave      : W50 (4 machine(s))
last machine up  : 2026-09-06T03:20:34Z  <- the anchor (GH #469 ruling 1)
slate spread     : 13s
unlock           : 2026-09-06T09:20:34Z
now              : 2026-09-06T09:09:26Z
margin           : 668s SHORT of unlock
WAVE_THROTTLE: THROTTLED (exit 3) -- do not launch. Wait 668s.
```

⭐ **登记清楚:本轮不发波的决定性理由是 §2 的闸 (iii),不是这 11 分钟。**
等到 09:20:34Z 之后闸 (i) 就会解锁,而那时闸 (iii) 只会更红(外部那台又多跑了 11 分钟)。
**不要让下一轮把「等 11 分钟」当成本轮的结论。**

## 四、收割:本轮无新数据

S3 `validation/` 最新对象是 **W50 自己的四份**(`l1trade+59ids-bfd72fe79ac8_20260906_0321*`,
落盘 04:02–04:14Z),**已由 06:14Z 那轮全量收割完毕**(4/4 粒干净,187 局计分,
gpm 均值 +13.76,comps 2/4)。⇒ **本轮零新 verdict,不重复收割。**

## 五、泄漏检查:dota2bot 侧干净(范围按 GH #515 订正后的写法)

- `check_costs.sh` 的实例段返回空 —— ⚠️ 那是**带 `tag:Name=dota2bot-*` 过滤器**的空,
  **对本台的泄漏检查这是正确的过滤器**,但按构造看不见别的项目。
- **不带过滤器的全账号普查**(本台自 GH #515 起每轮做):region 内 `running/pending`
  **恰好一台**,`i-0114b249d00ad956e` / `Project=final-table-trainer` ⇒ **不是本台的**。
- ⛔ **未做任何处置,也不打算做。** 终止别人项目正在跑的算力是**破坏性且越权**的动作,归 owner。
- ⇒ **dota2bot 侧泄漏:干净**(本轮本台一台未起,W50 四台已于 04:14Z 前全部自终)。

## 六、交棒

1. ⭐⭐⭐ **owner(升级为「现在」,不再是「预判」)** —— GH #515 的两个后果**本轮双双兑现**:
   (甲) 批测的发波闸被别人的算力关上(本轮就是);(乙) `$50` 告警即将以 `dota2bot-batch`
   的抬头发出,而其中约 **92%** 不是批测的。**要的动作二选一,都不改本台任何工具**:
   给 `dota2bot-batch` 加 cost filter(注意 #515 里那条警告:走 `Project` 标签路线必须
   先给批测机器补 `Project` 标签,否则 fence 恒读 0 —— **一个永远打 CLEAR 的闸比现在更坏**),
   或给 `final-table-trainer` 单开预算。**本台不动那台机器。**
2. ⭐⭐ **总监** —— **闸 (iii) 该不该带一条「今天未入账机时」的补正项**。本轮的形状是:
   工具打 `CLEAR`,而正确答案是 `THROTTLED`,**差别全在一个工具按构造读不到的量**。
   本台按章程铁纪律(甲)的**理由**(而非其字面 —— 字面只覆盖「本台自己近 12h 的波」)
   做了外推并据此不发波;**这属于本台自裁,请总监确认或推翻**。
   若确认,建议 `wave_fence.py` 增一条:全账号 `running` 实例 × 实测同 AZ 现货价 × 已跑机时,
   计入 `pending waves`(它现在恒为 `$0.000`,本轮就是它该非零的那一轮)。
3. ⭐ **本台(连续第四轮欠着)** —— `campgrade` 独占波仍不存在(#480/#241)。
   **本轮欠的理由第一次不是遗忘,是钱**:闸 (iii) 诚实算出来是红的。
   ⇒ **一旦 §6.1 落地(预算拆开)或账号今日花费入账后重算仍在 $50 内,下一轮第一件事就是它。**
4. ⭐⭐ **总监(承上轮未结)** —— 59-id 家族 W50 的 promote/reject(gpm +13.76 / comps 2/4,
   均值由单粒 6072 扛着,去掉后 −21.53);winrate 通道第九次 DEGENERATE 的结案。
5. ⭐ **总监** —— 本轮给 `$1.10` 常数(GH #454)一个新数据点:W50 实测 3.4 机时 ⇒ 账单侧
   约 $0.82,**落在常数的便宜一侧**;(乙2) 的机时判据本轮**未触发**(3.4/4 = 0.85 h/台 < 1.00)。

## 七、铁律 6 / 11

- `bots/` / `game/` / `tests/` **本会话一行未改**(本轮只写 `iterations/` 下的文件)。
- 静态门:见下方 push 记录;**未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行**。
- 动态半(`lua5.1 tests/run_tests.lua`,~100min,GH #124):**未跑,不声称**。
- **铁律 11**:本轮无 `requires approval`,无空转等待。CE 付费调用一次($0.01),
  用途是**确认贴近围栏的读数**(章程明文允许的情形),并且**正是它证明了 09-06 无数据**。

## 八、收尾读数

- **push**:`PUSH1_EXIT=0`(`claude/great-sagan-6xph4j`,新分支)/ `PUSH2_EXIT=0`
  (`ac10e95a..fb2bb3ef  HEAD -> main`)。静态门两次都自打
  **`GATE_EXIT=0  CLEAN`**(`luacheck bots game: 0 warnings`;门自己装的 luacheck,
  `installed luacheck (it was missing; apt package lua-check, bounded)`)。
  **未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行。**
- **发表前置(GH #290)**:`PRECHECK_EXIT=0` —— `local commits not on origin/main: 0`,
  `paths cited 1 / resolved on trunk 1 / refused 0`,`clean / OK to publish`。
  **顺序照办:先 push `fb2bb3ef` 再发评论。**
- **本轮追评**:**GH #515** comment `5558273147`
  (<https://github.com/dragonghy/dota2bot/issues/515#issuecomment-5558273147>)。
  本轮**未新开 issue** —— 本轮的事就是 #515 预判的那件事本身,另开一条会把同一条线拆散。
- **本轮 token**:`TOKENS total_in=4,520,087 out=31,548 turns=43`(到统计时刻为止)。
