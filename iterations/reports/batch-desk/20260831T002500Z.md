# 批测台 2026-08-31T00:25Z — 发波轮(W30,47-id 家族首波)

**一句话**:闸 (i) 于 `00:16:07Z` 解锁,本轮 **00:16:31Z 发出 W30**(四台全 spot,零市场类型降级),
但 **AZ 分散度掉到 2/4(`us-west-2b` 三台)** —— 2a 与 2d 同时报 `InsufficientInstanceCapacity`,
#256 的环把它们都改投到了 2b。另有两条要交给总监的实质发现:
**(1) 开工自检 `TRUNK RED` 是真的**(`creepthink > pulldrag` 未登记的共臂合取,静树复现);
**(2) `reclaim_blind` 在 W29 的归档记录上第一次跑就 exit 2** —— 字段名写错了,发波前已修。

---

## 1. 开工自检(铁律 10)

后台单次,**worst exit 3**;归因照抄工具(GH #267 4b),不手编:

```
legs run              : 8
FINDINGS (exit 3)     : trunk-red(lua)
UNCERTIFIABLE (exit 2): trunk-red(python)
selfcheck worst exit: 3
```

- ⚠️ **一处自曝的读数陷阱**:本轮我把自检接了 `| tail -60; echo "SELFCHECK_EXIT=$?"`,
  于是打出来的是 **`SELFCHECK_EXIT=0`**,而工具自己那一行写的是 **`selfcheck worst exit: 3`**。
  这正是铁律 10 点名的 PIPESTATUS 坑,**本轮在自己身上复现了一次**;结论以工具自打的那一行为准。
  (`evidence-discipline`:退出码不许经管道读。)
- **⭐⭐ `TRUNK RED` 本轮是真的,不是 GH #339 那个假红。**
  失败的是 `tests/test_coarmed_attribution_register.lua`:
  ```
  a NEW co-armed conjunction is live:
        creepthink > pulldrag
  ```
  **静树独立复现**:`lua5.1 tests/run_tests.lua test_coarmed_attribution_register.lua`
  **裸退出码(不经管道)`BARE_EXIT=1`**,当时工作树只有 `iterations/data/seed_roster_index.json`
  一处改动(**不在 `bots/`**)⇒ 与 #339 那个「自检跑着时换掉 `bots/`」的形状**无关**。
  ⚠️ 顺带一条方法论自纠:我第一次是用 `lua5.1 tests/test_coarmed_attribution_register.lua` 跑的,
  得到 **exit 0、零输出**,差点写成「静树通过」——**那个 0 的理由是错的**(该文件是模块不是 runner,
  必须经 `tests/run_tests.lua`)。**结论对不对与理由对不对是两件事**,按 `evidence-discipline` 记在这里。
- **来源**:director `ae767765`(08-30T22:06Z)把 `creepthink` 入集时没有附检测器要求的登记。
- **为什么它没有拦住 W30**:这条发现是关于**单 id 归因**的,不是代码正确性 ——
  `creepthink` 的 helper 被读在 `pulldrag` 的 gated body 里,两者同腿移动,
  于是 `pulldrag` 的 per-id (a) 触发计数从此测的是 `pulldrag AND creepthink`。
  本波买的是**全集聚合(条件 (b))**,该合取不扰动它。**但谁都不要在没有这条 caveat 的情况下
  从本波语料里读单 id (a)**。检测器自己写明的解法(读两处调用点、把 caveat 写进 test_set.md 的
  入集节)是**总监/协同组**的活,而且它明说**不许为了变绿而直接加进 ACKNOWLEDGED**。
- python 腿:**62 passed / 0 failed / 1 uncertifiable**(`tests/test_selfcheck_lua_leg.py` 没跑成)。
  **`UNCERTIFIABLE` 不是通过,也不是失败。**
- 其余腿:未裁 queue 请求 **none**(上一轮那三条 `strategy-25`/`hero-24`/`hero-25` 本轮**全被总监裁了**);
  `ORPHAN_PROPOSAL none`;`ROWLESS` 3 条(§BR/§BT/§BV,informational);
  unlanded `OK`;cadence 本轮无洞行;`stable-v1`/`v2` **2/2 OK**;
  UNKNOWN STATUS 4 条(词汇漂移,GH #317,总监地界)。

## 2. 成本(铁律:围栏算术不用裸 MTD)

- 开工 `check_costs.sh`:**running/pending 空**(W29 四台已自毁)。
- **MTD `$72.835`**(`budgets`,快照 `2026-08-30T21:56:54Z` — **本轮终于前进了**,
  上一轮连续四轮冻在 `$71.458`);读数 ≥ $35 ⇒ 脚本自动花 $0.01 用 CE 复核:
  **`$72.8346279518`,逐位一致(连续第三十三轮)**。forecast 77.557 / limit 100。
- **围栏(甲)= MTD + Σ(近 12h 已发波 × 该波界别单价)**:
  `72.835 + 0.90(W29,18:16Z 起飞,在 12h 窗内) + 0.90(W30 本波) = **$74.635 ≤ $80**`,余量 **$5.365**。
  最坏情形两波全降级按需 `72.835 + 2.15 + 2.15 = $77.135`,**同样过闸**。
  **发波后修正(§4.6 补发一台)**:`+0.23`(四分之一波)⇒ **`$74.865 ≤ $80`**,余量 **$5.135**。
  **不跨任何新告警档**($80 未跨);刹车 $90 / owner 线 $100 未接近。
- **本轮用的是总监新裁的 spot 常数 `$0.90`**(`ae767765`,由 `$0.80` 上调)。
  本台 21:12Z 提的是 `$0.85`,**总监没取,理由比本台的好**:`$0.85` 仍落在 W29 实测带
  `$0.79–0.86` 之内 ⇒ 在 W29 自己身上就可能少记,失效方向正是本台点名的危险那一侧。**本台接受。**
- **本轮现金**:CE $0.01 + S3 LIST/GET + **W30 四台 EC2(预估 $0.90)**。

## 3. 收割

**本轮无可收割物**:S3 `soak/` 最新前缀仍是 W29 的四个(末次对象 `08-30T19:08:45Z`),
`validation/` 无新对象。W29 已于上一轮(21:12Z)完整收割并归档。

`HARVEST` 行本轮无(没有收割)。

## 4. 发波:W30(47-id 家族首波)

### 4.1 四道闸 + 两道门,逐条

| 闸/门 | 判据 | 读数 |
|---|---|---|
| (i) 6h 节流 | W29 起飞 `08-30T18:16:07Z` +6h = `00:16:07Z` | **PASS**,发波块内 `date -u` 守卫读 `00:16:31Z`,余量 **24 秒** |
| (ii) 有新东西可测 | 分支 (a) 或 (b) | **PASS,分支 (a) 且两个子句都成立**(见下) |
| (iii) 成本 | 围栏 ≤ $80 | **PASS** `$74.635`,最坏按需 `$77.135` |
| (iv) `reclaim_blind` | exit 0 | **PASS —— 但第一次跑是 exit 2,修了输入才拿到**(§4.3) |
| 接线门 | `check_armed_wiring.py --cand … --ref …` | **exit 0,47/47 全部 wired** |
| 载体门 | `seed_draft.py --assert-carrier-from-arm` | **exit 0,`ids=10 seeds=4`** |
| 核树 | `git ls-remote origin main` == 钉的 40 位 sha | **相等**,发波块内复核,守卫未触发 |

**(ii) 分支 (a) 两个子句**:
- **新代码**:`git log be442fb9..0d58ce09 -- bots/ game/` = **2 个 commit**
  (`389012e5` hero,gated `cmqreach`;`fde733d0` hero,GH #346 的 nil 守卫修复)。
- **新 arm 串**:**44 → 47 id**(`386 → 414` 字节),director `ae767765` 同轮裁了三条搭车提议,
  `creepthink` / `lionqdmg` / `cmqreach` **全部入集**。
- ⭐ **与 W29 的诚实读法形成对照**:上一轮本台必须记下「分支 (a) 的那个 commit 是惰性的,
  因为 id 没 arm,真正给正当性的是分支 (b)」。**本轮同样三个 id 已经 armed,分支 (a) 自己就承重。**

### 4.2 配置与起飞

- 树钉 **`0d58ce09c65d3bdc34d1573c90e24bff2adf96a6`**(显式 40 位,从不写 `main`);
  arm = `test_set.md` 第 2 行,**47 id / 414 字节**;
  拓扑 **4×1**、显式四个不同 `--az`、`c6i.4xlarge`、**16 槽**、**`--rec-slots 1`**、**2h 看门狗**、**12 games**。
- **载体项机械推导**(GH #276,不手写):
  `CARRIER_TERMS derived from 47 armed ids: 10 hero-scoped, 37 generic, 0 unresolved => 6 term(s)`
  ⇒ 与 W27–W29 **同样的六项**(新入集的三个 id 里两个挂在既有项上,一个是 generic,项集合没动)。
- **载体门读数**:`lion 4 / skeleton_king 4 / obsidian_destroyer 3 / spirit_breaker 3 /
  crystal_maiden 2 / zuus 2` = **18 个载体格**。
  **诚实边界(第八轮,不减)**:「每 term ≥2」是**本台自订纪律**,不是门 —— 官方门只要 ≥1,
  报 `PARTIAL` 仍 exit 0。**GH #285 第十三轮待裁。**
- **起飞(以 `describe-instances` 为准,不拿 `spot_` 前缀当证据)**:

  | 种子 | 请求 AZ | 实得 AZ | 实例 | SIR | token | LaunchTime |
  |---|---|---|---|---|---|---|
  | 2204 | 2a | **2b** | `i-052290aef9c351705` | `sir-az9qhqhn` | `89e581` | 00:16:45Z |
  | 2214 | 2b | 2b | `i-057d28b7c09c9a2df` | `sir-s5nzhwhq` | `6eb30e` | 00:16:50Z |
  | 2286 | 2c | 2c | `i-0bd785794d2c2e188` | `sir-pjkzj29n` | `9666cf` | 00:16:54Z |
  | 2315 | 2d | **2b** | `i-09ad304d872f8993d` | `sir-3kr7ke3m` | `69e067` | 00:17:20Z |

  四台 **`InstanceLifecycle=spot` + `c6i.4xlarge` + `running`**,阶梯停在**第 1 级**
  (未到 `c6a`、更未到 `--on-demand`)⇒ **连续第八波零市场类型降级**,GH #158 不动。
  四个 `soak-run` 标签两两不同(独立证据源)。

  ⚠️ **这张表在发波后 8 分钟就作废了一行 —— 见 §4.6:种子 2286 被抢占,同轮已补发。**

### 4.3 ⭐⭐ `reclaim_blind` 在归档记录上第一次跑就 exit 2 —— 字段名,不是数据

```
UNDECIDABLE: seed 1801: missing leg count 'ab'
```

**根因**:W29 的收割轮把三个腿计数写成了 `harvest_ab` / `harvest_ba` / `harvest_arm_depth`,
而 `reclaim_blind.py`(GH #271 发波门)读的是**裸名** `ab` / `ba` / `arm_depth`。
**数据一直在文件里,门读不到。**

**处置(按章程:exit 2 = 未查 ≠ 通过 ⇒ 先修输入,不许降门)**:把三个键**改名**成文档化的 schema
(**值一个没动,什么都没重算**),并在 `W29_wave.json` 里留下 `machines_schema_note` 说明原委。
改完**第二次跑 exit 0**:

```
changeover 40.0 min  (measured bracket 34.80 < x <= 40.63)  min_arm_depth 8.0
  seed 1801  52.3 min  instance-terminated-by-user  ab32/ba16  PAIRED [depth-checked]
  seed 1842  55.1 min  instance-terminated-by-user  ab38/ba20  PAIRED [depth-checked]
  seed 1902  54.9 min  instance-terminated-by-user  ab35/ba23  PAIRED [depth-checked]
  seed 2013  43.2 min  instance-terminated-by-user  ab28/ba15  PAIRED [depth-checked]
yield: 4 paired seed(s) of 4 | attribution: 0 reclaimed before the flip
VERDICT: not blinded | NEXT WAVE: spot
```

⭐ **这是 W28 那个病的第二种形态,而且更隐蔽。** W28 把 `status_code` 写成了 `harvest` 里的**散文**;
W29 把腿计数写成了**字段,但换了个前缀名**。两次的失效方向相同:**数据落盘了,门读不到,
而门自己会说 `UNDECIDABLE`** —— 幸好它说了。
**`tests/test_wave_gate_keys.py` 两次都没抓到**,因为它只断言 `gates` 那几个键,
**从不断言门真正消费的 `machines[]` 输入 schema**。⇒ 交棒里点名建议扩这个测试。

### 4.4 ⭐⭐ AZ 分散度掉到 2/4 —— 是容量,不是配置

**四台实得:`us-west-2b` ×3(2204 / 2214 / 2315)、`us-west-2c` ×1(2286)。**
**GH #256 的验收判据(四个互不相同)本波不通过**,而且不通过的方向正是 #252 要压的那个:
**3 粒种子现在共享同一个 AZ 的容量命运。**

日志两种行**分开抄**(章程要求):

```
  ! …: launch in us-west-2a failed -- InsufficientInstanceCapacity … c6i.4xlarge … (us-west-2a)
  ! …: re-aiming inside the ring -> us-west-2b            ← 环内改投,不是降级
  ! …: PLACEMENT MISMATCH requested=us-west-2a actual=us-west-2b re-aimed=yes
  ! …: launch in us-west-2d failed -- InsufficientInstanceCapacity … (us-west-2d)
  ! …: re-aiming inside the ring -> us-west-2a
  ! …: launch in us-west-2a failed -- InsufficientInstanceCapacity … (us-west-2a)
  ! …: re-aiming inside the ring -> us-west-2b            ← 一次发射里跳了两跳
  ! …: PLACEMENT MISMATCH requested=us-west-2d actual=us-west-2b re-aimed=yes
```

- **无 `AZ RING EXHAUSTED`** ⇒ **不是真降级**,环走到第三个 AZ 就填上了。
- **先看 plan 头再下结论**(章程 W22 那条):四次调用的 `--az arg=` 分别是
  `us-west-2a` / `us-west-2b` / `us-west-2c` / `us-west-2d`,**都不为空**
  ⇒ #282 的故事 (ii)(「这个进程根本没收到你的 `--az`」)**本波排除**。
- **GH #282**:两条 mismatch **全是 `re-aimed=yes` 且同块日志里有配套的 `re-aiming` 行**
  ⇒ **零 UNEXPLAINED,连续第十波满足判据**(**第十三次建议关闭**)。
  四台的 `Placement.AvailabilityZone` 已**独立**于 launcher 自己的行核对过。
- **GH #256 重开时钟**:章程的重开条件是「**第三波**被 2b 的回收清零,或**某 AZ 连续三波**起飞失败」。
  本波是 2a/2d 起飞失败的**第一波**,两个计数器都没到阈值 ⇒ **`AZ_LIST` 本轮不动**。
  记在这里是为了下一轮**去数**,而不是重新推导。
- **⚠️ 事先登记的风险(不许事后补)**:若未来 2h 内 `us-west-2b` 出容量事件,
  **本波一次丢 3 粒种子**,下一轮 `reclaim_blind` 会读出 `BLINDED`。
  按 owner 已登记的回收处置:**一台被抢占 = 该粒作废,不整波作废**;缺的种子再发一台补跑。

### 4.5 ⭐ 选种:窗口右移 [1801,2200] → [2201,2600],天花板 16→15→**14** 已是三个点

**两个窗口都先穷举再选**,所以这是**量出来的决定不是口味**:

| 窗口 | 已用 | 未用 | popcount 分布 | 互异掩码 | 合法四元组(≥2/term) | **最优载体格** |
|---|---|---|---|---|---|---|
| `[1801,2200]`(旧) | 12 | 388 | `{4:4, 3:70, 2:128, 1:142, 0:44}` | 43 | 291 | **14** |
| `[2201,2600]`(新) | 0 | 400 | `{5:2, 4:8, 3:67, 2:136, 1:137, 0:50}` | 50 | 5014 | **18** |

- **天花板趋势不再是预言,是三个点**:**16(W28)→ 15(W29)→ 14(W30)**,**一波掉一格**;
  同两波里旧窗口的 pop-4 供给 **7 → 4**。章程点名的机制**坐实**:
  每一波都挑载体最富的四粒,于是**每一波都在削掉自己那个窗口 popcount 最高的那一层**。
- **处置按章程自己的规则**(「选种窗口是参数,不是常量;处置是右移窗口、不放松自律」):
  右移买到 **18 > 14** 的载体密度,**零成本**,**且没有放松「每 term ≥2」的自律**。
  新窗口还含该窗口的**头两粒 pop-5 种子**。
- ⛔ **诚实边界**:右移**不修机制**,它只是换了一块还没被削过的地 ——
  新窗口会以**同样的速率**开始被削。**GH #285 该裁的仍然是三选一**:
  改自律(退回门自己的 ≥1)、改窗口策略(固定右移 / 滚动)、还是改选择规则(不总取最富的四粒)。
- **本波四粒**:`2204 / 2214 / 2286 / 2315`,`seed_roster_index --seed` 逐粒 **`no banked games`**
  (索引已先 `--build`,exit 0)。
  载体:2204 = cm+lion+od+sk+sb(**pop-5**);2214 = lion+od+sk+sb+zuus(**pop-5**);
  2286 = lion+od+sk+sb;2315 = cm+lion+sk+zuus。

### 4.6 ⭐⭐ 发波轮内就被抢占一台(本台首次),已按 owner 登记的处置补发

**收尾泄漏检查只列出 3 台**,而本波发了 4 台 —— 差的那台就是这条发现的入口
(**这正是收尾那次要「0 台/对得上」而不是要 MTD 数字的理由**)。

| 项 | 读数 |
|---|---|
| 机器 | 种子 **2286**,`i-0bd785794d2c2e188`,`us-west-2c`,`sir-pjkzj29n` |
| SIR | `closed` / **`instance-terminated-no-capacity`** |
| 存活 | `create 00:16:54Z` → `update 00:22:33Z` = **5.65 分钟**(实例状态转换 `00:24:37Z`) |
| 原文 | `Server.SpotInstanceTermination: Spot instance termination` / `no Spot capacity available` |
| 产出 | 该前缀里**只有 1 个对象 `soak_farm.log`,零局** |

- **5.65 min ≪ 40.0 min 换腿点** ⇒ 这台**按构造就是孤儿**(`arm_depth 0`)。
- **处置照 owner 2026-08-26 事先登记的那条,不是本轮临时决定**:
  「一台被抢占 = 该种子缺臂,这一粒的配对差作废;**不要整波作废**;缺的那粒种子再发一台(仍 spot 优先)补跑」。
  ⇒ **同轮补发**:`00:29:20Z`,`i-0196162092b80eddb`,**请求 `us-west-2a` 并且拿到了**
  (`requested==actual`,**无 re-aiming 行**),`InstanceLifecycle=spot`,`c6i.4xlarge`,`running`,
  run_token `4ecca6`,仍 16 槽 / `--rec-slots 1` / 2h 看门狗 / 12 games / 同一个 40 位树钉。
- ⭐ **一个对 #252/#256 有用的观测**:`us-west-2a` **00:16Z 报 `InsufficientInstanceCapacity`,
  00:29Z 就给了** —— 同机型同 region,**13 分钟内容量翻面**。
  ⇒「某 AZ 这次拒了」**不等于**「这个 AZ 今晚是死的」;章程里那条「移出 `AZ_LIST` 会把 4 路分散
  变成 3 路、反而抬高相关性」的推理**在今晚这组数据上又强了一分**。
  补发**故意不投 2b**(那里已经三台)、也不投刚抢占过的 2c。
- **阶梯全程没爬**:补发这台也是 `c6i.4xlarge` spot,**没用 `c6a`,没用 `--on-demand`**。
- **钱**:损失约 **5.65 min 机时 ≈ $0.03**;补发按四分之一波 **~$0.23** 记进围栏
  ⇒ 围栏 `72.835 + 0.90 + 0.90 + 0.23 = **$74.865 ≤ $80**`,**仍不跨任何告警档**。
- **收割侧的交代(下一轮照做)**:种子 2286 有**两个 run 前缀** ——
  `9666cf`(被抢占,0 局)与 `4ecca6`(真正那条腿)。
  `recover_verdict.py` 按种子跨 source dir 并池,**两个都传**;
  空的那个不贡献任何东西,**但要写出来,不许悄悄丢掉**。

## 5. 局数(铁律 7)

**(a) 上一波(W29)最终**:229 落盘 / 207 带戳 / **206 计分** / 22 暖场 / `unfinished` 1;
per-seed(ab/ba 不对称)1801 **32/16**、1842 **38/20**、1902 **35/23**、2013 **28/15**;
`min_arm_depth 8`、`thin_arm_seeds []` **零排除**;四台全自毁零抢占。
(读数与裁定见上一轮报告 `20260830T211200Z.md`,本轮不重复。)

**(b) 本轮在跑波次(W30)实时进度**(发报时刻 `00:24:50Z`,起飞后 **~8 分钟**):
`89e581` 0 / `6eb30e` 0 / `9666cf` 1 / `69e067` 0 个 S3 对象。
**这是正常的**:开机 + 拉仓库 + 生成 soak 池要十几分钟才出第一局,
且**暖场局不算有效局**。
**⚠️ 事后看,`9666cf` 那 1 个对象就是 `soak_farm.log`,那台随后被抢占,零局**(§4.6);
它的替身 `4ecca6` 于 `00:29:20Z` 起飞,**比另外三台晚约 13 分钟**,仍在同一个 2h 看门狗内。
**预期局数**:4 种子 × 12 局 × 2 侧 = **96 计分**(名义),
按 W29 实测(229 落盘 / 206 计分)**实际会显著高于名义值**;
种子 2286 因为晚起飞,预计比另外三粒薄一点。
若 2b 出容量事件,预计那三粒会**一起**只交出半截腿。

## 6. 泄漏检查(两次)

- **开工**:`check_costs.sh` 的 running/pending 段 **空**。
- **收尾第一次(`00:27Z`)**:`--leak-only` 只列出 **3 台**,而本波发了 4 台 ——
  **少的那台不是泄漏,是被抢占**(§4.6),按 id 逐个对上后确认。
  ⚠️ **注意失效方向**:`--leak-only` 只会报「多出来的」,**「少一台」它不会举手** ——
  是本台拿它的输出**去对本轮自己的起飞记录**才看见的。这条纪律(逐个 id 对账,不是数个数)本轮买到了东西。
- **收尾第二次(补发后,`00:31Z`)**:running/pending **恰好 4 台**,
  逐个 id 与「三台原班 + 一台补发」对上,**零陌生实例 ⇒ 零泄漏**。
  四台均 `terminate-on-shutdown` + 2h 看门狗 + **一次性(非 persistent)spot 请求**,自毁路径齐备。
  被抢占那台 `i-0bd785794d2c2e188` 已 `terminated`,不再计费。
- 常驻成本仍只有 `ami-0a990a26d89c66547` 一个 AMI。

## 7. 铁律 6

- `bots/` / `game/` **一行未改**(本轮改的是 `iterations/` 下的归档/报告/章程 + 一个索引缓存)。
- 静态半 `bash tools/agent/luacheck_gate.sh` ⇒ **`luacheck bots game: 0 warnings`**,
  **退出码 bare 读取(不经管道)`BARE_EXIT=0`**;容器冷启,脚本自己把 `luacheck` 装上(apt 包名 `lua-check`)。
- python 套件 **62 passed / 0 failed / 1 uncertifiable**,其中 `tests/test_wave_gate_keys.py` **PASS**
  ⇒ 新写的 `W30_wave.json` 带齐了 `gates` 必填键。
- **动态半(#124,~100min)未跑且不声称。**
- **未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行。** `core.hooksPath` 已上膛。
- **顺序条款(GH #290)**:本轮**先 push 再发表任何带引用的评论**。

## 8. 交棒

1. **⭐⭐⭐ 总监 —— `TRUNK RED` 是真的,而且是 `ae767765` 自己带来的**(§1):
   `creepthink > pulldrag` 是未登记的共臂合取,**静树 `BARE_EXIT=1` 独立复现**。
   解法是检测器自己写的:读两处调用点、把 caveat 按 §BW.3 / §CG.4 的样子写进 test_set.md 的入集节,
   再把这一对加进 ACKNOWLEDGED —— **不许为了变绿直接加**。
   **W30 正带着这个合取在跑**:本波的全集聚合读数不受影响,
   但**谁都不要从本波语料里读 `pulldrag` 的单 id (a)**,除非带上这条 caveat。
2. **⭐⭐⭐ 总监 —— 扩 `tests/test_wave_gate_keys.py`,让它断言 `machines[]` 的输入 schema**(§4.3):
   同一个病两波两种形态(W28 写成散文、W29 写成带前缀的字段名),**测试两次都没抓到**,
   因为它只看 `gates`。建议:对**已收割**的 wave.json,断言每台带
   `status_code` / `create` / `update` / `ab` / `ba` / `arm_depth` **这六个裸名**。
3. **⭐⭐ 下一轮本台 —— 收割 W30**(闸 (i) 于 `2026-08-31T06:16:31Z` 解锁,多半只收割不发波)。
   收割时**必须**把 `status_code` / `create` / `update` **按台写成 `machines[]` 字段**(SIR 数小时内即 `NotFound`),
   并且腿计数**用裸名 `ab`/`ba`/`arm_depth`**,不要再加 `harvest_` 前缀。
   **本波多一件事**:种子 2286 有**两个 run 前缀**(`9666cf` 被抢占 0 局 / `4ecca6` 真腿),
   `recover_verdict.py` **两个都传**,空的那个要在报告里写出来不许悄悄丢;
   补发那台的 SIR 号本轮未取(它还在跑),**收割轮取回来补进 `machines[]`**。
   另:那台**晚 13 分钟起飞**,若 2286 的 `arm_depth` 偏低,**归因是起飞时刻不是门**。
4. **⭐⭐ 总监 —— 家族碎片化现在有三块了,而且没有一块在变厚**:
   45-id **8 粒**(gpm +26.60、7/8,GH #329/#332 待裁)、44-id **4 粒**(W29,`suggested: promote`)、
   47-id **本波起 4 粒**。**本台按章程「默认波次 = 全测试集」发波,没有别的选项**;
   要让哪一块变厚,得由总监明确指定重钉某个串。
5. **⭐⭐ 总监 —— GH #285 第十三轮催,而且现在有三个点的趋势线**(§4.5):
   16→15→14,一波一格;本轮已右移窗口(14→18 格)但**机制未修**。三选一:改自律 / 改窗口策略 / 改选择规则。
6. **⭐ 总监 —— GH #256 本波验收不通过(2/4),但原因是容量不是配置**(§4.4):
   2a 与 2d 同时 `InsufficientInstanceCapacity`,环把两台都改投 2b ⇒ **3 台同 AZ**。
   重开 `AZ_LIST` 的两个计数器**都没到阈值**,本轮不动;下一轮请**去数**。
   **同轮还多了一台被抢占**(2c,5.65 min,§4.6)⇒ **今晚 13 分钟内四个 AZ 里有三个拒过或掉过 c6i spot**
   (2a/2d 00:16Z 拒、2c 00:22Z 抢占、**2a 00:29Z 又给了**)。
   ⚠️ **最后那一格是关键**:「拒过的 AZ」不等于「死的 AZ」,
   ⇒ 章程里「不把 2b 移出 `AZ_LIST`,否则 4 路分散退成 3 路、反而抬高相关性」的推理**今晚又强了一分**。
   若下一轮再遇同样的容量紧张,可考虑请总监裁「起飞期是否允许直接上阶梯第 2 级 `c6a.4xlarge`」——
   **本台不自行改阶梯**。
7. **⭐ 总监 —— GH #282 连续第十波零 UNEXPLAINED MISMATCH(第十三次建议关闭)**;
   本波两条 mismatch 全部 `re-aimed=yes` 且有配套 `re-aiming` 行,plan 头 `--az arg=` 四条全非空
   ⇒ 故事 (ii) 排除。
8. **⭐ 总监 —— #308 第七波按 (A) 跑 `rec_slots 1`**,帧通道仍 **1/16**,
   owner P1/P2 条件 (a) 继续等,**第八次点名**。
   **#271 建议就此写成 `wave.json:gates` 必填键、空缺即视为没跑**(第四轮催)。
9. **⭐ 本台自曝 —— 铁律 10 的 PIPESTATUS 坑本轮在本台身上复现了一次**(§1),
   打出的 `SELFCHECK_EXIT=0` 与工具自打的 `worst exit: 3` 相反。已按工具那一行写结论。
10. **⭐ 单价常数**:总监的 `$0.90`(`ae767765`)本轮首次用于围栏算术,**本台接受**,
    并按其**自动失效条款**登记:**若 W30 账单侧实测单波成本 > $0.90,下一轮本台点名、总监重裁。**
11. 存量:**#207 `zusstatic` 第三十五波 armed**;**#218 后续第三十一轮**;**#295 建议关闭**;
    **#329 / #321 待裁**;#313/#290/#291/#298/#299 照旧;
    #217/#211/#225/#180/#171/#200/#181/载体门 PARTIAL/`stable-v*` tag/`campdanger`/§BL.4/#75 照旧;
    cadence 的 7 个命名不合法的报告文件**本轮未检查是否修好**(工具本轮无洞行)。

## 9. 本轮 token 用量

```
TOKENS total_in=7,456,911 out=54,775 turns=54
```

(统计时刻之后的收尾回合不计入,当量级参考。)

---

**铁律 11**:MCP/工具本轮未触发 `requires approval`,无空转等待。
