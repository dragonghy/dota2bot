# 批测台 2026-08-24T18:15Z —— **发波(W7 全集例行波)**:连续两轮的封锁解除,而这一波暴露出两条「差一个批准」的搭车 id

**一句话**:上两轮(12:17Z / 15:11Z)不发波的**决定性理由**是「promote 没上 main ⇒ 基线是错的」——
本轮开工第一件事复核,**promote 已经在 main 上**(`origin/main` = `03fb378`,`jmz_func.lua:7124` 是
`PROMOTED (was soak-candidate 'creeppull')`,`PROMOTED` 标记全文件 **9** 处),封锁解除;
三条节流全过 ⇒ **18:12Z 发出 W7 全集例行波**(成员串 26 个 id 逐字、钉 `03fb378`、4 台 × 1 种子、
`--on-demand --slots 16 --rec-slots 12 --hours 2 --games 22`)。
**本轮真发现**:`pulllane` 与 `towerfear` 两条自称「搭车、不申请专波」的 id **代码在树上、接线门 WIRED、
只差总监把它们批进成员串**,于是**眼睁睁空过了一波全集** —— 其中 `pulllane` 是 **owner P1(拉野)连接率**
那一格。已开 **GH #164**。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` ⇒ **worst exit 3**。

- **UNLANDED 8 commit**:全是总监 promote/rescue 链的历史分支(`compassionate-albattani-uv8bab`、
  `busy-bardeen-uxvcdx`、`busy-bardeen-u45ms4`),其中 2 条带 CLAIMS-LANDED 标记。
  **本轮判读与前两轮不同,而且是相反的**:这些提交的**内容**已经由总监 16:xxZ 的抢救链落上 main
  (`ccb3708` / `c542b41` / `9a128b2`),自检点的是**那几条 SHA 本身**没有出现在 main 的历史里
  (cherry-pick 换了 SHA)。⇒ **本轮不再当作掉棒,也不再开 issue**;判据不是工具输出,是下面 §1 的
  **源码侧直接核实**。
- **CADENCE 6 finding**:全部是 08-24 早晨那段停摆的历史洞;**五条流本轮全部在 6h 内有产出**
  (director 16:00Z、hero 16:52Z、strategy 16:41Z、replay-check 15:57Z、batch-desk 本轮)。
  **上一轮点名的「总监缺席 18.2h」本轮已消除** —— 总监 13:xxZ + 16:xxZ 连开两轮。
- trunk python 套件 **18 passed / 0 failed**。

## 1. ⭐ 封锁解除:promote 在 main 上,基线第一次是对的

上两轮不发波的第①条(决定性)理由逐字是「**基线是错的** —— promote 未上 main ⇒ 现在发波 =
明知故犯地买一份错基线的语料」。本轮**源码侧直接核实**(不是读报告、不是读自检):

| 核实项 | 上一轮(15:11Z) | 本轮 |
|---|---|---|
| `origin/main` tip | `a5b7dab` | **`03fb378`** |
| `bots/FunLib/jmz_func.lua:7125` | `if not J.IsSoakCandidate('creeppull') then return nil end` | **`-- PROMOTED (was soak-candidate 'creeppull') 2026-08-23, together with its sibling 'pullbeat'`** |
| `PROMOTED (was soak-candidate` 命中数 | 0 | **9** |
| `git ls-remote --tags origin` | 空 | 空(**tag 仍无 —— 见 §6 交棒②**) |

**⇒ 稳定版腿现在真的是 `stable-v2`**。本波是 **`stable-v2` 落地后的第一波**,也是
**creeppull + pullbeat 作为 turbo 默认出现在两条腿上的第一份语料** —— 这本身对录像组有价值
(此前所有语料里这对行为都只在 armed 腿上)。

**诚实边界**:`stable-v2` 的**分支引用**在远端**还不存在**(`git ls-remote origin 'refs/heads/stable-*'`
本轮未查到任何 `stable-*`,`--tags` 也是空)。§BA.4 写的是「promote 后打分支引用」。
main 的**树**是对的,**锚点没建** ⇒ 以后要复现「stable-v2 是哪棵树」只能靠本报告与 `state.json` 的散文。

## 2. 成本(章程步骤 2)

- **MTD $36.206**(`budgets/dota2bot-batch`,refreshed **2026-08-24T14:37:15Z**),
  脚本按 `COST_CONFIRM_AT=$35` 自动花 **$0.01** 用 CE 复核 ⇒ **$36.2056796871**,两个来源逐位吻合。
  forecast **64.471** / limit **100.0**。三线(本台 $45 围栏 / $90 刹车 / owner $50 档)**全未触及**。
- **按新纪律(甲)算围栏**:`MTD + Σ(近 12h 内已发波次 × $1.48)` —— 近 12h 内(06:15Z 之后)**零波次**
  (上一波 03:10:28Z)⇒ 与裸 MTD 同值 **$36.206**;**本波后预计 $37.69**,离 $45 围栏余 **$7.31**。
- **纪律(乙)照办**:本轮**没有**用「MTD 增量对得上波费 ⇒ 该波已计入」这个手法做任何推断。
  `budgets` 快照停在 14:37Z,本波 18:12Z 发出 ⇒ **本波必然不在这个读数里**,这是时间戳给的,不是拟合。
- 本轮 CE 花费 **$0.01**(仅脚本的自动复核一笔;未拉逐日/逐服务拆解 —— 上一轮刚拉过,无新问题要问)。

## 3. 收割(章程步骤 3):**无新 verdict,与「上一轮未发波」一致**

`s3://dota2bot-batch-results-4924/validation/` 按 `LastModified` 排序的最后一条仍是
**`cmboots_20260824_0342_run.log`(2026-08-24T03:42:42Z)** —— 与上一轮收尾**逐位相同**。
W5 之后零产出,**无 verdict 可用 `recover_verdict.py` 重算,无 `queue.json` status 因收割而变**。

## 4. ⭐ 本轮真发现:两条「搭车」id 差一个批准,空过一波(GH #164)

发波前跑接线门时顺手核实的,不是猜的:

```
$ python3 tools/batch_test/check_armed_wiring.py --cand "pulllane,towerfear" --ref 03fb378...
pulllane   direct 1  bots/FunLib/jmz_func.lua:8037
towerfear  direct 1  bots/mode_retreat_generic.lua:907
all 2 armed ids wired on 03fb378...      (exit 0)
```

**两条的代码都在树上、都 WIRED、都自称「搭任何一波都行 / 搭下一波全集,零 AWS 增量」,
而两条都不在可 arm 串里** —— `test_set.md` 头部成员串仍是 **26**(23:xxZ 那一行),
总监 13:xxZ 逐字「成员串 **26** … 逐字未动」、16:xxZ §4「本轮不新做 promote/reject … **不加急**」。

**为什么这不是「等下一轮就好」**:

- `strategy-11` 的门是 **`pullcamp and pulllane` 的合取**。只 arm `pullcamp`(它在成员串里)⇒
  **逐字节 no-op,而且没有任何计数会报警** —— §BA.2 记的正是这个形状(`pullcad and pullbeat`
  被 promote 冻死那次)。它不会 exit 非 0,只会安静地什么都没测到,verdict 回来是「测过了,无影响」。
- `strategy-11` 是 **owner P1(拉野)的连接率那一格**:08-22 的 own-side 子句买到了安全那一半
  (20s 内死亡 2/97 → 0/146),**连接率没买到**(21.5% → 12.1%/9.9%),`pulllane` 就是买它的子句。
- `towerfear` 自带的排波说明是「单独 arm 就有意义、无结构性同伴、搭任何一波都行」。
  本轮真有一波全集,**它只差一个批准**。

**批测台不自行把未批准的 id 塞进 armed 串** —— 那是入集裁定不是排波,§AW.1 那条先例管的是
「不要在裁定已作出时用相反的保守默认」,不是「可以替总监作裁定」。⇒ 开 **GH #164** 交总监,
并在 `queue.json:strategy-11 / strategy-12` 的 `result` 里写明**空过的原因是入集未批、不是波次没排**。

## 5. 启动决策:**启动 W7(全集例行波)**

| 判据 | 读数 |
|---|---|
| 上两轮的第①条封锁(基线错) | **已解除**(§1,源码侧核实) |
| (i) 距上一波 ≥6h | ✅ 上一波 **03:10:28Z** ⇒ **15.0h** |
| (ii) 有新东西可测 | ✅ 两条都成立:树从 `a5b7dab` → `03fb378`(**含 promote,稳定版腿变了**);当前树 + 当前成员串的累计种子数 **0 < 8** |
| (iii) 成本 | ✅ $36.206 + $1.48 = **$37.69 ≤ $45**(纪律(甲):近 12h 零波次,加计 $0) |
| 波次类型 | 全集(owner 2026-08-22:**默认波次 = 全测试集 armed**;隔离波是例外) |

**为什么这一轮可以发、上一轮不可以 —— 三条都变了,不是标准松了**:

1. **基线**:promote 已落 main(§1)。上一轮的第①条理由**按其自身措辞**已不成立。
2. **排期权**:上一轮不发的第⑤条理由逐字是「在总监离线 18.2h、无法反对时,由**唯一能花钱的 agent**
   单方面插一个全集波改变波次计划」。**总监本轮在场**,16:xxZ 报告 §6 逐字写
   「**总监不加急买波** … `strategy-7/-8/-12` 与 `hero-9` **由批测台按排期走**」⇒ 排期权已交回本台,
   那条先例(「门槛不能因为对方不在场就变得可议价」)**保护的是对方缺席的情形,不适用于对方在场并已表态**。
3. **下游**:五条流全部在 6h 内产出(§0),W4/W5 两份语料已被录像组消化(15:57Z 报告 208 局全语料
   + 一条 `tpsafe2` 的 [bug])⇒ 12:17Z 那条「把钱花在瓶颈的下游」不再成立。

**W6 的占位没有被动过**:`strategy-5b`(`midsupyield`)的 `APPROVED_CONDITIONAL` 条件
(§AX.2 的事件轴量级)本轮复核录像组 15:57Z 报告**仍零处提及** ⇒ 裁定原文「拿到之前不排发波时刻」继续生效;
本波编号取 **W7**,**W6 仍然留给 `strategy-5b`**。同理 `strategy-6`(`RECEIVED_NOT_SCHEDULED`)不发;
`strategy-7`/`strategy-8` 的新 id(`bagsalve`/`pullzone`)**不在成员串里** ⇒ 本波带不上,
本台**不替总监宣布它们的 `DEFERRED_PENDING_W3_RESOLVE` 解锁**(虽然 W3 的 resolve 事实上已经落地)。

### 5.1 发波参数(实际值)

- **armed 串(26,逐字 = `test_set.md` 头部成员串)**:
  `l1trade,l5combo,midtp,suptp,tpcommit,tpdying,lf_rescue,teambrain,ownhalf,overchase,fieldregen,wandbleed,capmono,cmrguard,tpdead,zusult,wandlimbo,blinkflee,liondrainstop,odaoe,pullcamp,stayfield,stayfield2,fieldbuy,fieldcreep,pullcad`
- **接线门**:`check_armed_wiring.py --cand <上串> --ref 03fb378…` ⇒ **26/26 WIRED,exit 0**
  (调用形式按章程硬知识:工具在 `tools/batch_test/` 下,`--cand` + `--ref`,不是位置参数)。
- **载体门**:`seed_draft.py 888 895 896 906 --assert-carrier "crystal_maiden,zuus,lion,obsidian_destroyer"`
  ⇒ **exit 0**。`crystal_maiden` / `zuus` = **FULL**(4/4 种子);`lion` / `obsidian_destroyer` = **PARTIAL**
  (只有 s896)⇒ `liondrainstop` / `odaoe` 在 888/895/906 三个种子上是**波内零腿**
  (这正是上一轮请总监裁的那条新规,**仍未裁**,本波照旧按零腿读)。
  **正面证据(发牌表,不只写一句 no-op)**:s895 dire pos1 与 s906 radiant pos3 是 `skeleton_king`
  ⇒ `queue.json:hero-6`(WK 双树枝,UNGATED)本波**有分母**。
- **树**:`03fb37877def67cc2be320b960f2521192e4a716` —— **发波时刻 `git ls-remote origin main` 的真 tip**
  (章程步骤 5 的核对,2026-07-23 那次险些测错树的规矩)。
- **拓扑/参数**:4 台 × 1 种子(888/895/896/906),`--on-demand` `c6i.4xlarge`
  (实测 `InstanceLifecycle=None` 四台确认)、`--slots 16`、**`--rec-slots 12`**(第五次验收 exit 0,不降;
  且本波遵守「不在同一波里同时改被测 id 集合和采集配置」—— 变的只有 id 集合)、
  `--hours 2` 看门狗 + `shutdown-behavior=terminate`、`--games 22`。
  **`--on-demand` 是保守默认**:GH #158 / DECISIONS #9(按需 vs spot)总监 16:xxZ 明写「不裁,进 W35,
  在 owner 回复之前批测台按 §6.1 走 on-demand」。

| 种子 | InstanceId | run_id 尾 token | 发波时刻 |
|---|---|---|---|
| 888 | `i-0e924f755d0178c3b` | `a0d128` | 18:12:41Z |
| 895 | `i-04c7b5aac41f20225` | `2b3d86` | 18:12:45Z |
| 896 | `i-05788db3056cede53` | `cdc15c` | 18:12:49Z |
| 906 | `i-0fd42d13739476ad2` | `188e3c` | 18:12:52Z |

**run_id 唯一性双保险**:①四次调用各跨整秒(41/45/49/52);②**独立证据源** `soak-run` 标签实测
**4 个值两两不同**(`sort -u` 4 行),且 `describe-instances` 只回 **4 台**(无第五台)。

## 6. 局数(铁律 7)

- **(a) 上一波(W5,`cmboots`)最终有效局数**:**273 有效 + 24 暖场 = 297**;
  per-seed ab/ba:888 **42/28**、895 **41/27**、896 **42/27**、906 **42/24**,`unfinished` **0**
  (12:17Z 报告收割读数,本轮无新收割 ⇒ 原样引用,不重算)。
- **(b) 本轮在跑波次实时进度**:两次读数,四个 run 前缀下 `analysis.json` **都是 0 局** ——
  **+2.1 分钟(18:14:49Z)0 / 0 / 0 / 0**;**+7.4 分钟(18:20:06Z)0 / 0 / 0 / 0**。
  **这仍在预期内但已经压到点上**:W3 实测首局在开波 **+7.5 分钟**,`run.log` 另测开波到首局 ≈7 分钟
  ⇒ 本轮收尾时刻恰好落在首局该出现的那一刻,**没有看到任何正样本就结束了会话**。
  连带事实:`s3://…/soak/` 的前缀数本轮读到 **184(持平)** —— **S3 前缀要等第一个对象落地才存在**,
  所以「四个新前缀还没出现」与「四个 run 各 0 局」是**同一件事的两种读法,不是两条独立证据**。
  **下一轮触发的第一件事应是核对这四个 run 有没有真的开始产局**(若仍为 0 = boot 或 clone 出了问题,
  而不是慢)。**预期 ~275 ± 25 有效局 + 24 暖场**,暖场局**不算**有效局。
- **预计自毁 ≈ 18:45Z**(按账单侧钉死的 **0.550 h/台** 推,**不是** 20:12Z 看门狗时刻 ——
  `--validate` 语义是 verdict 上传后立即 terminate)。

## 7. 泄漏检查(章程步骤 6)

| 层 | 开工(18:0xZ) | 收尾(18:1xZ) |
|---|---|---|
| 实例 | **0** | **4 = 本波四台逐个对上,无第五台** |
| 游离卷 | 0 | 0 |
| 快照 | 1(`snap-0ad026b386c804288`,160 GiB = AMI 底,唯一常设成本) | 1 |
| EIP | 0 | 0 |
| spot 请求 | 0 | 0(本波走 `--on-demand`,与 `describe-spot-instance-requests` 空返回一致) |

**无泄漏**。本波四台按 `--validate` 语义完成即自毁;**下一轮触发时的收尾判据回到「0 台在跑」**,
若届时仍有台在跑 = 看门狗还没到 20:12Z,属正常。

## 8. 验证(铁律 6)

本会话 `bots/` / `game/` **逐字未动**(改动只在 `iterations/` 下:报告 + 章程状态节 + `queue.json`)
⇒ 铁律 6 无适用对象;容器无 `luacheck` / `lua5.1`,**不声称跑绿过 Lua 全量**。
`queue.json` 实测 `json.load` ok 且按原 `indent=2` 回写 ⇒ `git diff --stat` **9 增 7 删**,无重排噪声。

## 9. 交棒(铁律 9:「修好」不等于「做完」)

1. **⭐ 总监 —— 批 `pulllane` / `towerfear` 入集(GH #164)**。两条代码就绪、WIRED、自称搭车,
   本轮空过一波;`pulllane` 是 owner P1 连接率那一格。批准 ⇒ 成员串 26 → 27/28,下一波全集自动带上,
   **零 AWS 增量**;退回 ⇒ 请写理由,本台把两条 queue 请求改 `rejected`。
   **按 §BA.4 补则:裁定必须落到头部成员串,`director.note` / 报告散文不算送达。**
2. **总监 —— `stable-v2` 的分支引用还没建**。`git ls-remote origin 'refs/heads/stable-*'` 与 `--tags`
   本轮**都是空**;main 的树是对的,锚点没建 ⇒ 「stable-v2 是哪棵树」目前只活在散文里(§1 诚实边界)。
3. **总监 —— 载体门 PARTIAL 的惰性种子 = 波内零腿**(上一轮已请裁,**仍未裁**)。
   本波 `lion` / `odaoe` 各只有 1/4 种子有载体,读数分母按 1 种子算还是 4 种子算,现在靠口头约定。
4. **录像组 —— W7 收割后的三份 (a) 核验**:① `pullcad`(`queue.json:strategy-9`,acceptance 已预登记
   FLIP / 兵线位移 / 右键次数,**ab/ba 两层**);② `hero-6`(WK 是否同时持有两个铁树枝,s895/s906 有载体);
   ③ 顺带:这是 **creeppull + pullbeat 作为默认出现在两条腿上的第一份语料**。
5. **录像组 —— W6 解锁棒(第三轮催)**:`strategy-5b` 卡的事件轴量级(§AX.2)在已交付的语料里,零 AWS;
   15:57Z 报告仍零处提及。**在它到货之前 W6 排不了发波时刻**,这是裁定原文。
6. **录像组 —— W4 `campgrade` 的 (a)**:15:57Z 已补齐 208 局全语料重跑,读数**更 SILENT**;
   处置(退回/收窄/关闭)归总监,记在此以免掉棒。

## 10. token 用量(铁律 8)

```
TOKENS total_in=4,441,127 out=27,680 turns=39
```

(统计时刻在 §7 泄漏收尾之后、commit/push 之前;统计后的收尾回合不计入。)
