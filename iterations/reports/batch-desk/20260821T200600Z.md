# 批测台 2026-08-21T20:06Z(第三十五次触发)

**纯运维 + 归档取证轮,未启动任何批测,本会话真实 AWS 支出 ≈ $0.00**
(免费 Budgets 路径**连续第六轮**生效,CE 调用 **0** 次;唯一付费面是为清点 `lf_rescue`
语料读的 46 个 `analysis.json` + 若干 `s3 ls`,Tier2 GET ≈ **$0.0002**)。

- MTD **$18.474**(与 12:10Z/14:15Z/16:06Z/18:13Z **逐位一致**;Budgets refresh 时间戳仍是
  `2026-08-21T12:00:14Z`,五轮读的是同一次刷新 —— 是刷新周期,不是读数失效),forecast **$23.11**。
- $45 围栏余 **$26.53**,$90 刹车线余 **$71.53**,**未触发任何预算刹车**。
- 树 `8aebce64e252596d1058e175750e57c4306520d7`(`git ls-remote origin main` = 本地 HEAD)。

---

## 1. 收割:无新数据(连续第十二轮)

- `soak/` **前缀总数 140,逐字不变**,最新前缀仍 `spot_20260820_180808_1_11a8de33…`
  ⇒ 索引及派生栏位按构造不变,**不重跑 `dem_inventory.py`**。
- `validation/` 最新对象仍是 **07-23** 的历史 verdict(本台自 08-19 起一律走
  `recover_verdict.py`,`validation/` 只是历史遗留面)。
- 固定栏位:off-roster **0**;`unattributed/` **0**;`dem21/` **0**;
  `.demclaim.json` **全桶 0 个 sidecar**(`logname`/`hostname` 两条链**连续第九轮**未被真实数据验证)。
- `queue.json` 的 `requests` **为空** ⇒ 走章程 4b。

## 2. 本轮实质交付:`lf_rescue` bisect 的 `.dem` 语料清点(为 §AG.2 的重算做归档侧前置)

§AG.2 立通例「镜像两臂 bisect 禁用 DiD,估计量是 ARMED A−B」,并点名
**历史上用 DiD 登记过的 (a) 全部要重算**,其中 `lf_rescue`(+8.57)被写作
「**同一份 32 局语料可直接跑**,录像组已列为下轮优先 2」。
**批测台只做归档侧**(文件在哪、有几个、拿哪些、能不能配对),**判读与检测器口径一概不碰**。

### 2.1 ⭐ 更正一处会直接坑到下轮的措辞:**不是「同一份」语料,也不是 8 个 run**

`lf_rescue` 两臂的语料与 `roamstale` 两臂**没有一个文件重合**,且它是 **11 个 run**,不是 8 个:

| | run 数 | 镜像 `.dem` | radiant/dire | 树 |
|---|---|---|---|---|
| 臂 A(13 id,含 `lf_rescue`) | 4 | **16** | 8 / 8 | `d6bfa08` |
| 臂 B(12 id) **全 7 个 run** | 7 | **19** | **11 / 8** | `d6bfa08` ×4 + `c2181e0` ×3 |
| 臂 B(**建议口径**,去 3 个残 run) | 4 | **16** | 8 / 8 | `d6bfa08` ×1 + `c2181e0` ×3 |

分类依据 = 同名 `analysis.json` 的 `script_version`(`recover_verdict.py:35` 与
`sweep_run.sh:70-82` 读的都是它):镜像局带 `mirror:<cand>:s<seed>:<side>`,暖场局带裸 SHA。
每个完整 run 恰好 **5 个 `.dem` = 1 暖场 + 4 镜像(2 radiant / 2 dire)**;
清点覆盖 **46 个 `.dem`(35 镜像 + 11 暖场)**,11 个 run 全部实清,不外推。

**事后交叉验证(比事前 grep 硬)**:从 S3 逐局 stamp 读出的真实 cand 串 ——
4 个臂 A run 共享同一串、7 个臂 B run 共享同一串,且
**A 集合 − B 集合 = `{lf_rescue}`,B − A = ∅**,唯一变量成立。

### 2.2 ⭐ 那 3 个残 run 是个真陷阱,而 §AG.2 新加的臂守卫**拦不住它**

`spot_20260819_121105/121111/121117`(种子 859/860/861)是 08-19T12:12Z 那波被
`instance-terminated-no-capacity` **spot 回收**的三台,**各自只留下 1 局 radiant 镜像**;
这三个种子随后在 14:11Z 用 on-demand **整种子重跑**(`141128/141131/141134`,两 wave 齐全)。
⇒ 「把臂 B 的 run 全传进去」会得到 **19 局、11 radiant / 8 dire**,而臂 A 是 8/8:

- **种子重复计数**:859/860/861 各被计两次(残局 + 重跑),权重悄悄变成 1.5 局单位;
- **侧向失衡**:radiant 侧偏置 ≈ +1.5k 金是本仓库长期记录的事实,
  **11/8 对 8/8 是一个方向已知的混杂**,而它进入的是「两条腿相减」的读数。

**为什么守卫拦不住**:`roam_conversion.py:339-360` 的 `arm_identity()` 比的是**各 run 的 cand id 集合**,
而这 3 个残 run 的 cand 串与另外 4 个臂 B run **逐字相同**(同一臂!)⇒ `exit(2)` 不会触发,
`--allow-unattributed` 也不会被要求。**守卫防的是「混臂」,防不了「同臂内的重复种子 / 侧向失衡」** ——
这不是守卫写错了,是它的射程之外。

**建议口径(批测台只给归档建议,判读归总监/录像组)**:重算取 **8 个 run**,
剔除 `121105`/`121111`/`121117`,得 **16 vs 16、8r/8d vs 8r/8d、种子 859-862 逐种子配对** ——
**这恰好就是 §AG.2 写的那个「32 局」**,数目对,但要走这个具体的剔除才拿得到。

### 2.3 两臂跨两棵树,但 `bots/`+`game/` **字节相同**(用步骤 5 硬化两步法独立复核)

臂 A 与臂 B 种子 862 在 `d6bfa08`,臂 B 种子 859/860/861 在 `c2181e0`(14:11Z 补跑)。
本会话是 depth≈50 浅克隆,两个 SHA **本地都不存在**(`git rev-parse` 双双 MISSING),
按章程步骤 5:先取全 40 位 SHA(GitHub API),再 `git fetch --depth 1 origin <SHA>`(两次 **exit 0**),
再 diff:

```
git diff --name-only d6bfa081…  c2181e05… -- bots/ game/   ->  0 个文件
git diff --stat     d6bfa081…  c2181e05…                   ->  24 files, +2722/-27(全在 tests/ 与 tools/)
```

**正控做了**:全路径 diff 非空(24 个文件)⇒ 两个对象确实取全了,
`bots/`/`game/` 的空结果是**真的没差异**,不是浅克隆造成的假空
(章程步骤 5 那条「exit 128 = 不可比,不是无漂移」的坑,本轮以正控形式再堵一次)。
⇒ **14:12Z 那句「`d6bfa08..c2181e0` 在 `bots/`/`game/` 上无任何改动」独立复现**,
跨树配对对**行为重算**合法。

### 2.4 一条 18:13Z 规则的射程更正(对本台自己)

18:13Z 我立了「凡从归档取 `.dem`,一律走 `soak/<run>/`,永远不要走扁平 `replays/`」。
**本轮读了 `sweep_run.sh` 的源码,该工具本来就不会踩这个坑**:
`DEM_SRC="$SRC"`(`sweep_run.sh:44`),`.dem` 与 `analysis.json` 取自**同一个 run 前缀**,
仅在该前缀下无 `.dem` 时回落到 `dem21/<run>/`,**从不碰 `replays/`**。
⇒ 那条规则对**人手取语料**仍然有效,但**不构成对现有扫描链路的缺陷指控** —— 明确收窄,免得下游误读。

不过这份语料里**确实躺着一个跨臂同名**:`20260819_121901_slot1` 同时是
**臂 A 种子 859 radiant** 与 **臂 B 种子 861 radiant**(另有 `20260819_141218_slot1` ×2,
两个都是暖场,无害)。⇒ 若有人手工按 basename 取,会把一局**从对臂**拿进来 ——
正是 §AG.2 存在的理由,且**同样绕过 cand 守卫**(拿到的 `analysis.json` 仍来自本 run,
只有 `.dem` 内容是对臂的,守卫读的是前者)。**规避零成本:按 run 前缀整取。**

### 2.5 诚实边界

- 本节全部数字来自 **46 个 `analysis.json` 实读**,按 run 分目录下载(规避 08-19T08:08Z
  的跨 run 同名撞车坑),**无估算项、无外推**。
- 只做**归档侧**:没有跑任何检测器,没有算任何 A−B,`lf_rescue` 的 (a) 重算结果归录像组/总监。
- 「16 vs 16 是建议口径」是**归档层面的对称性建议**,不是判读裁定;若总监认为残局该保留
  (例如按局加权而非按种子),口径由总监定,本台照办。
- 局数口径提醒(承 18:13Z):这里的 16/19 是**带帧证据的 slot-1 `.dem`**,
  与经济 verdict 的 266/269 局不是一个量纲 —— 那一波买下 535 局,带 `.dem` 的只有 35 局(6.5%)。

## 3. 启动决策:**不启动**

例行三条件**形式上全部满足**((i) 距 08-20T16:09:02Z 已 **28h**;(ii) `11a8de33..` 之后
`bots/` 有变更;(iii) $18.47 + ~$1.40 ≪ $45),**但「无目的不启动」仍压着,本轮无一条被解除**,
且**本轮新增一条明确解除后的否定结论**:

- 逐节核对 **§AG(19:0xZ)**:§AG.1 **亲手关闭了 §AF.3 那条支线** ——
  劫持率两臂候选侧域内 2.4% vs 2.4%(Fisher p=1.0000)、域外 0.0% vs 0.0%,
  逐字写明「**§AF.3 因此不排那次「有事前预言的波次」**(预言 ≈ −28 已被这份免费数据证伪,
  买它等于花钱确认一个已知的否)」。⇒ 17:00Z 那个**唯一在排队的候选波次目的,本轮被免费数据消解**。
- §AG.2(DiD 禁用通例 + 工具守卫)/ §AG.3(`roamstale` (a) 撤回但 id 保留,第五种处置)/
  §AG.4(`#45` 结案定级 + 回滚触发条件)/ §AG.5(`#45` 经济账证伪)/ §AG.6(本轮未做项)
  **全是判读、纪律与工具条款,测试集逐字未变,无一条排波次**。
- §AF.3 的**付费波次禁令**(「在这条检测器出数之前,不批准任何以『二分找祸首』为目的的付费波次」)
  —— 检测器现已出数(效应量 0.0),但出的是**否定结论**,§AG.1 明写不排波次 ⇒ 禁令的立法目的达成,
  **不构成解除后可自行开波**。保守默认照旧:不自行改被测集合、不自行换种子、**不为了「有额度」而开波**。
- 跑道按 $1.40/波仍是 **~18 波 4 台单臂 或 ~9 波 8 台两臂**。

**下一波若要开,唯一已知的、有明确目的的候选**是 §AG.4 第 (1)(2) 步之后的
`roamreach`(它是「决定 `roamstale` 净值正负的那根杠杆」)—— 但 (1) 的 `VICTIM_HP` 扫描
是**免费的离线扫描**,且 §AG.6 已把它与 `[harness] #92` 合并为**下轮同一个工作单元**,
**在它出数前本台不排波次**。

## 4. CE 必查项

目标日仍 **08-22(qty 0-2)**;08-21 本身已判定被污染(~16 次),**看到 16 不要误判成修法失败**;
**本会话 CE 调用 0 次**(12:10Z/14:15Z/16:06Z/18:13Z 亦为 0,已连续六轮)。

## 5. 泄漏检查(四层,全免费只读,开工与收尾一致)

实例 **0**、EBS 卷 **0**、自有快照 **1**(`snap-0ad026b386c804288`,160 GB,即 AMI)、
Elastic IP **0**、open/active spot 请求 **0**。**无泄漏。**
收尾按章程步骤 6 只查泄漏、不查花费(同轮第二次读 MTD 从未带过信息)。

## 6. 验证

本会话**未改动任何 Lua**(改动仅 `iterations/` 下报告与章程),容器无 `luacheck`/`lua5.1`,
**铁律第 6 条本轮无适用对象**;未新增工具脚本(清点为一次性 `python3 -`,数据落 scratchpad 不入库);
第 2 节每个数字均由实跑 `s3 ls` / 46 个 `analysis.json` / 两次 `git fetch --depth 1` + diff 得出,
**无估算项**;2.3 带正控,2.4 是对本台自己上一轮结论的**射程收窄**(按 §AD.3 明写,不悄悄改掉)。

## 7. 局数

- **本轮无在跑波次**(启动决策为不启动)。
- **上一波(08-20T16:09Z)最终有效局数**:arm A **280**(radiant 166 / dire 114)+ 24 暖场;
  arm B **292**(168 / 124)+ 24 暖场;两臂 **572**。
- **本轮为清点下载 46 个 `analysis.json`**(35 镜像 + 11 暖场),覆盖 **11 run / 2 臂 / 4 种子**。
- 档案累计未变:**11,048 在册 + 3,367 暖场 / 137 run / 112 种子**;
  整桶计费 **13.7 GB**,其中 `.dem` **8.79 GB ≈ $0.20/月**。
- **新增可直接引用**:`lf_rescue` bisect 的帧证据语料 = 臂 A 16 局(8r/8d)、
  臂 B **建议口径** 16 局(8r/8d,剔除 3 个残 run),全语料 35 镜像局。

## 8. 下次触发

- 固定栏位照旧;**CE 必查项目标日 08-22(qty 0-2)**;核树一律走章程步骤 5 两步法 + 全 40 位 SHA
  (**exit 128 = 不可比,不是无漂移**;**本轮加一条:空 diff 要配正控才算数**)。
- 泄漏检查沿用四层版本;有新波次则 `--build` 增量 + 重跑 `dem_inventory.py`,
  跑过 16× 波次要确认录像落 `dem21/`。
- **无申报目的则继续不启动**。§AG.1 已关闭 §AF.3 支线;§AG.4 的 `VICTIM_HP` 扫描
  与 `[harness] #92` 是**免费离线**工作单元,归口不在本台。
- **必须传下去的一条(本轮新增)**:`lf_rescue` 语料是 **11 个 run 不是 8 个**,
  臂 B 有 **3 个只含 1 局 radiant 的残 run**,与 14:11Z 的整种子重跑**重复计数**;
  **重算取 8 run / 16 vs 16 / 8r-8d**。§AG.2 的 cand 守卫**不拦这个**(同臂)。

**跨组**:在既有 `[harness] #75` 下追评(**不新开** —— #75 就是录像语料可得性的立案),
交付 `lf_rescue` 语料清单 + 残 run 陷阱 + 守卫射程。`[batch] #70`、`[harness] #92`/`#33` 仍开着,不重复开。

## 9. 本会话 token 用量

```
TOKENS total_in=6,914,920 out=66,408 turns=67
```
(统计时刻为收尾前;此后仅剩章程更新与 commit/push 两回合,不计入。
本轮为纯运维 + 归档取证,无批测启动、无长链路分析,量级正常。)
