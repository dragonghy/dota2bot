# 批测台工作单元 — 2026-08-30T03:30Z

**本轮 = 收割 W27(45-id 家族的第一份读数)。不发波 —— 卡在闸 (i) 的 6h 节流(解锁 `2026-08-30T06:18:47Z`),(ii)(iii) 都过。**

---

## 〇、开工自检(铁律 10)

前台跑,不接管道、不加 `timeout`、不用 `nohup &`(那三个「壳的退出码冒充被测者」的形状本轮都没用)。

- `unlanded_commits` **OK**
- **cadence 2 findings**:`director 08-29T04:08Z → 08-29T16:28Z` **12.3h**(**连续第四轮同一个洞**);
  `replay-check 08-29T00:55Z → 05:30Z` **4.6h**。窗口 24h,阈值 3.5h,5 streams / 520 reports。
- 未裁请求 **none / none**;`ORPHAN_PROPOSAL` **none**;`ROWLESS` 3(`aimguard`/`campvoid`/`campexit`,已在别处裁,信息项)
- 过期等待 **无**(6 charter / 58 settled id)
- `stable-v1` / `stable-v2` **2/2 OK**(EXISTS/PINNED/SHIPPED 全绿)
- trunk python **53 passed / 0 failed / 0 uncertifiable**;fast Lua **33 个带标检测器文件,0 失败**
  (⚠️ 工具自己声明这是 FAST SUBSET,未带标的测试与「要一个进程跑完整套」的部分**不在覆盖内**)
- **`worst exit 3`,归因照抄工具**(GH #267 4b):`FINDINGS = cadence`、`UNCERTIFIABLE = none`。
  ⇒ 本轮**没有任何一条腿是没跑成的**。

---

## 一、成本与泄漏(章程步骤 2)

- 开工 running/pending 实例:**空**(W27 四台已在 `01:02–01:14Z` 自毁)。
- **MTD `$67.374`**(`budgets/dota2bot-batch`,免费);≥ $35 触发 CE 复核 $0.01:
  **`$67.3735002094`**,与 budgets **逐位一致,连续第二十六轮**。
- ⚠️ **快照没有刷新**:`budget refreshed 2026-08-29T22:17:33Z` —— 与上一轮**同一张快照**。
  W27 于 `00:18Z` 起飞、`01:14Z` 收官,**整段都在这张快照之后 ⇒ W27 的钱一分没进这个裸 MTD**。
  这正是章程 (甲) 存在的理由,算术照旧不省。
- **围栏(章程 (甲),逐波按界别取价)**:`67.374 + W27 0.80(spot) = **$68.17**` ≤ **$80**,
  余量 **$11.83**。最坏情形(把 W27 按「全程降级按需」估成 $2.15)= **$69.52**,**同样过闸**。
  刹车 **$90** 与 owner 批准线 **$100** 均未接近。
- **本轮未跨任何 owner 可见告警档**($50 早已跨并已书面解释,$80 未跨)⇒ 无「本轮跨过 $X」那一行。
- **本轮现金 = CE 复核 $0.01 + S3 取数(可忽略)+ 零 EC2**(W27 的 ~$0.80 是上一轮发波时花的)。
- AMI/快照:`ami-0a990a26d89c66547`(唯一常设成本)照旧。

---

## 二、收割 W27(章程步骤 3)—— 四台干净落地,4/4 计分,零排除

### 2.1 收官形状

`describe-spot-instance-requests` 四条全部 `closed / instance-terminated-by-user`
(**自毁,零抢占**),收官时刻 `01:02:23Z – 01:14:32Z` ⇒ 每台 **43.6 – 55.1 分钟**,
在 2h 看门狗内跑完。**放置:4/4 `requested == actual`,四个不同 AZ(2a/2b/2c/2d),
无 re-aiming、无 `AZ RING EXHAUSTED`、无降级** ⇒ GH **#282 连续第六波零 MISMATCH**、
GH **#256 第六波拿满**。

### 2.2 收割路径(降级旗标一个没用)

标准路径:四个 run 各自一个子目录(**从不 flatten**)→ `recover_verdict.py` 指向父目录。

- `files_seen 235 / games_loaded 235 / source_dirs 4 / unparseable 0 /
  pooled_basename_dirs_overridden 0`
- **未用 `--allow-pooled-basenames`、未用 `--allow-unparseable`、未用 `--min-arm-depth`**
  ⇒ **本轮没有任何一行「这是 SKIP 不是 pass」要抄。** stderr 为空。

### 2.3 语料自证(不靠散文,靠戳)

212 个带戳局的 `script_version` 解出**唯一一个** arm 串:**`ids=45 bytes=396`**,
与 `test_set.md` 第 2 行**逐字节相同**。另有 **23 个未带戳文件**,`script_version` 是裸树 sha
`5d9b9927` ⇒ **暖场局,不计分**(W25/W26 各 24 个,同构)。

### 2.4 读数

| 种子 | ab | ba | `arm_depth` | 计分局 | gpm | xpm | deaths | last_hits | winrate |
|---|---|---|---|---|---|---|---|---|---|
| 1827 | 26 | 16 | 19.81 | 42 | +19.43 | −18.31 | +0.19 | −4.16 | 0.519 |
| 1828 | 39 | 19 | 25.55 | 58 | **−24.73** | −16.33 | +0.35 | −1.96 | 0.513 |
| 1835 | 40 | 15 | 21.82 | 55 | +63.10 | +35.03 | −0.42 | +3.00 | 0.500 |
| 2103 | 38 | 19 | 25.33 | 57 | +61.89 | +29.41 | −0.44 | +3.66 | 0.500 |

- **均值:gpm +29.92,xpm +7.45,deaths −0.08,last_hits +0.14,winrate 0.508**
- `comps_better`:gpm **3/4**、xpm 2/4、deaths 2/4、last_hits 2/4、winrate 2/4
- **`min_arm_depth` = 8,`thin_arm_seeds` = []** —— **本波无一粒被 THIN-ARM 排除**
  (最薄的 1827 是 19.81,离门 8 有一倍以上余量)。**没有种子需要点名。**
- `scored_games 212`、`unfinished 0`、`winner_by = engine_natural 235/235`、
  `winrate_independent_of_gold 0/235`
- `suggested: promote` —— **这只是提示**,三条件的 (a) 帧核验不在本台,promote 仍走三条件。

### 2.5 量程(**本台只报量程,不判读**)

| 量 | mean | sd | SE | (mean−ref)/SE |
|---|---|---|---|---|
| gpm | +29.92 | 41.71 | 20.86 | **1.43** |
| xpm | +7.45 | 28.71 | 14.35 | 0.52 |
| winrate | 0.508 | 0.010 | 0.005 | **1.67** |
| deaths | −0.08 | 0.41 | 0.21 | −0.39 |
| last_hits | +0.14 | 3.81 | 1.90 | 0.07 |

**留一法(gpm)四个读数全部为正**:drop-1827 **+33.42** / drop-1828 **+48.14** /
drop-1835 **+18.86** / drop-2103 **+19.27**,区间 **+18.86 … +48.14**
⇒ **符号不押在任何单一种子上**(对照 W25 单波:去掉 1633 就从 +40.16 塌到 +4.99)。
**winrate 四粒全部 ≥ 0.5**(0.519 / 0.513 / 0.500 / 0.500)。

⚠️ **两条诚实边界**:
1. gpm 的四粒散布是 **−24.73 … +63.10**,`mean/SE 1.43` **不是显著性**,而铁律 2 也不要显著性;
   这是四粒读数,不是一个结论。
2. **ab/ba 两层严重不对称**(ab 26–40 / ba 15–19,ba 腿普遍是 ab 的 ~½)。
   `arm_depth` 用的是调和平均,四粒都远在门上,**但铁律 4(i) 的记名义务照办**:
   本表每粒的 ab/ba 都已列出;四量本身是逐种子 swap-and-average 后的值,不是并池单侧读数。

### 2.6 ⭐ 排波事实:**W27 不可并进 8 粒池**

W27 的 arm 串是 **45 id / 396 字节**;W25+W26 那份 8 粒池是 **44 id / 387 字节**。
⇒ **两者不可并池**,上一轮那份 8 粒读数**已封口,W27 不会加厚它**。
W27 开的是**同一棵 `bots/` 代码上的新 45-id 家族**——
`git log 5d9b9927..origin/main -- bots/ game/` **exit 0 且空**,
且 W27 的树相对 W26 的 `b834e887` 在 `bots/`/`game/` 上**一行未改**
⇒ **被测代码与 W25/W26 逐字节相同,变的只有 arm 串(多一个 `fieldsip`)。**

---

## 三、载体供给(交给录像组/英雄组)

212 个计分局里六个 term 的出场数(armed 腿 / baseline 腿):

| term | armed | baseline |
|---|---|---|
| skeleton_king | 118 | 94 |
| crystal_maiden | 95 | 60 |
| lion | 84 | 73 |
| obsidian_destroyer | **78** | **34** |
| zuus | 69 | 85 |
| spirit_breaker | 64 | 93 |

⇒ **「这波没有载体」在 W27 上同样不成立。**
⚠️ **但帧通道仍是 1/16**:GH #308 第四波未裁 ⇒ 走登记路径 (A),`--rec-slots` 退回 1,
本波只传了 **30 个 `.dem`**(6/8/8/8)。
**`hero-22`/`hero-23`/`strategy-23` 的取帧仍建议从 W25 取**(`rec_slots 12`、145 个录像)。

---

## 四、本轮不发波:三闸逐条(章程步骤 4)

- **queue**:23 条 pending 里 **无一需要新 EC2** —— 14 条是 `ROUTED_ARCHIVE_SCAN`/`REDUMP`/
  `RIDESHARE`(明写零 EC2、零新波次),其余是 `REJECTED`(strategy-19/22)、
  `RECEIVED_NOT_SCHEDULED`(hero-9 / strategy-6)、`DEFERRED_PENDING_W3_RESOLVE`
  (strategy-7 / strategy-8)与 `strategy-5b`(`APPROVED_CONDITIONAL / wave W6`,早已过期未重排)。
  ⇒ **显式请求的 (i)(ii) 豁免本轮用不上。**
- **闸 (i) ✗** —— W27 起飞 `2026-08-30T00:18:47Z`,本轮 `03:3xZ` 只过 ~3.2h,
  **6h 闸解锁 `2026-08-30T06:18:47Z`。**
- **闸 (ii) ✗** —— **没有新东西可测**:`bots/`/`game/` 自 W27 的树起**一行未改**
  (`git log 5d9b9927..origin/main -- bots/ game/` **exit 0 且输出为空**;
  ⚠️ 按章程,**exit 0 才算「无漂移」,exit 128 是「不可比」**,本轮是前者);
  `test_set.md` 第 2 行仍是 **45 id / 396 字节**,与 W27 测的那串相同。
  且当前树+测试集的累计种子数 = **4**(45-id 家族只跑过 W27 一波),
  **< 8 ⇒ (ii) 的第二个分支本来是满足的**,但第一个分支(有变更)不满足;
  按章程 (ii) 是「或」⇒ **严格说 (ii) 由「累计种子数 4 < 8」满足**。
  **⇒ 真正卡住本轮的是 (i),一条足矣。**
- **闸 (iii) ✓** —— $68.17 ≤ $80。

**⇒ 按章程跳过启动。下一波 W28(45-id 家族第二波,把 4 粒加厚到 8 粒)在 `06:18:47Z` 之后。**

---

## 五、⭐ 顺手把上一轮立的那条纪律执行了:索引已刷新

上一轮的 (甲) 发现是「`seed_roster_index` 陈了,它把六小时前刚烧掉的种子当成从未用过献回来」。
本轮**在没有发波的情况下也跑了 `--build`**:**139 → 143 粒种子 / 277 run / 18313 局**,
W27 的 `1827/1828/1835/2103` 已折进索引。

**为什么不等到下一轮发波前再跑**:这条判据的失效方向是危险的那一侧(不报错、多给解、
多给的是已花过钱的种子),而 build 是增量的、秒级、只读 `analysis.json`、几分之一美分。
**把它挪到收割轮做,等于让「刷新」与「用它选种」不再是同一个动作** ——
下一轮发波时即使有人忘了 build,索引里也已经有 W27 那四粒。
⚠️ 但这**不是**把它从发波前动作里去掉:W28 发波前仍要 `--build`(W28 之前可能还有别的波落盘)。

⚠️ **窗口右移的后续仍欠**:W27 用的是 `[1801,2200]`(400 粒未用 / 50 掩码 / 5618 组解),
本轮未重算它被 W27 削掉一层后还剩多少 —— **W28 选种时按新纪律先 `--build` 再穷举即可**。

---

## 六、局数(铁律 7)

**(a) 上一波(W27)的最终有效局数**:
- **235 落盘 / 212 计分**,`unfinished 0`,`engine_natural 235/235`,暖场未带戳 **23** 局
- per-seed(ab / ba **不对称,已注明**):1827 `ab26/ba16`、1828 `ab39/ba19`、
  1835 `ab40/ba15`、2103 `ab38/ba19`
- `min_arm_depth 8`、`thin_arm_seeds []` ⇒ **无种子被排除**
- 四台全自毁、零抢占;`.dem` 上传 6/8/8/8 = **30 个**

**(b) 本轮在跑波次**:**无**(本轮不发波,S3 上零新增语料;W27 四个 run 前缀已收满)。

---

## 七、`W27_wave.json` 已回填

`iterations/reports/batch-desk/waves/W27_wave.json`:四台的
`status_code` / `update` / `ab` / `ba` / `arm_depth` / `dem_uploaded` 全部落位,
新增 `harvest` 节(含 `flags_used`「本轮没有 SKIP 行」、`stamp_check`、
`placement_result`、`wall_clock`、`carriers_scored_games`、`pooling_note`),
并移除 `_harvest_todo`。GH **#271** 的接力(起飞时一手写、收割时回填)第二次走完全程。

`iterations/queue.json`:**strategy-23**(`fieldsip` 搭车请求)
`pending → harvested_pending_verification`,`result` 写明读数、量程、
**以及 acceptance (3) 的那条约束:本波未做 situation 帧普查,不许把这份四量读数读成
「fieldsip 测过了没效果」。**

---

## 八、交棒

1. **⭐⭐ 下一轮本台 —— 发 W28**(45-id 家族第二波,闸 `2026-08-30T06:18:47Z` 解锁;
   发波前先 `seed_roster_index --build` 再掩码穷举;目标是把 4 粒加厚到 **8 粒可池化读数**,
   与 W25+W26 那份 44-id 8 粒池**分开登记**)。
2. **⭐⭐ 总监 —— 45-id 家族首份读数已备齐**(gpm +29.92,3/4,留一法全正,零排除);
   **44-id 串的 8 粒读数仍备齐可裁,本轮无新增语料改变它。**
3. **⭐⭐ 总监 —— GH #308 重裁第四次落空**。W27 按登记路径 (A) 退回 `rec_slots 1`,
   帧通道 **1/16**,本波只落 30 个 `.dem`;**代价第四次点名**,owner P1/P2 的条件 (a) 继续等。
   W28 若仍不裁继续走 (A)。
4. **⭐ 录像组/英雄组 —— W27 载体供给已点清**(§三):OD armed 78/baseline 34、
   WK 118/94、CM 95/60、lion 84/73、SB 64/93、zuus 69/85。
   **但帧通道 1/16 ⇒ `hero-22`/`hero-23`/`strategy-23` 的取帧仍从 W25 取。**
5. **⭐ 总监 —— 自检连续第四轮报 director `08-29T04:08Z→16:28Z` 12.3h 无工作单元**,
   而 #308 / #313 / #321 / #299 / #298 / #291 / #285 都等你裁。
6. 存量:**#207 `zusstatic` 第二十八波 armed**;**#218 后续第二十四轮**;
   **#282 连续第六波零 MISMATCH,第六次建议关闭**;**#295 建议关闭**;**#285 第七轮催**;
   **#321(本台上一轮开的选种链路两个静默失效)待裁**;#313/#290/#291/#298/#299 照旧;
   #217/#211/#225/#180/#171/#200/#181/载体门 PARTIAL/`stable-v*` tag/`campdanger`/§BL.4/#75 照旧。

---

## 九、铁律与合规

- **铁律 6**:`bots/` / `game/` **一行未改**(本轮只动报告、`waves/W27_wave.json`、
  `queue.json`、本章程);`core.hooksPath` 已上膛;
  **未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行要抄**;
  动态半(`tests/run_tests.lua`,~100min,GH #124)**未跑,且不声称**。
- **铁律 6 顺序条款(GH #290)**:本轮**先 push 再发表任何带引用的评论**。
- **铁律 11**:**MCP 未触发 `requires approval`**,未在任何权限提示上坐等。
- **泄漏两次**:开工 running/pending **空**;收尾 `--leak-only` 见 §十
  —— **本轮不发波,收尾判据就是「0 台在跑」。**

---

## 十、收尾泄漏检查

`check_costs.sh --leak-only`(零成本,完全不查花费):
**running/pending 实例 = 空,0 台在跑 ⇒ 零泄漏。**
本轮不发波,所以收尾判据是**「0 台在跑」**本身
(发波轮才是「在跑台数 == 本轮自发台数且 id 逐个对上」)。
常设成本只有 `ami-0a990a26d89c66547`。

## 十一、本轮 token 用量(铁律 8)

`TOKENS total_in=3,761,063 out=27,241 turns=38`

(统计时刻之后的收尾回合不计入,量级参考。)
