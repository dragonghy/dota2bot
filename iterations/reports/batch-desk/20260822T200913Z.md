# 批测台 2026-08-22T20:09Z — 纯收割轮,零支出,未启动任何批测

**一句话**:18:11Z 的 22-id 全集波(`--rec-slots 8`)收割完成,**270 有效局 + 156 个 `.dem`,
帧通道 8/16,P2 的 (a) 语料干净地交到录像组**;而本轮真正的发现在采集侧 ——
**`--rec-slots 8` 的验收 exit 0 通过**,并且**总监立「先 8 后 16」时唯一的那条未测风险
(多录制者归属歧义)本波第一次被真正演练了 157 次**:`logname`/`hostname` 两个判据
**157/157 一致且正确**,而 **`mtime` 判据 157/157 全错** —— 它以前之所以「三判据逐局一致」,
只是因为 `candidates:1` 时三个判据平凡地指向同一个文件。`dem_claim.sh:144` 早已把 mtime
锁在 `rec_slots = 1` 之后,**这条护栏现在从「设计上的谨慎」变成「实测必需」**。
本轮例行波次条件 (i) 差 3h55min,不启动。

---

## 0. 开工自检 / AWS / 成本

- `bash tools/agent/routine_selfcheck.sh` → **worst exit 3**。UNLANDED **1 条**、cadence **clean**、
  trunk python 套件 11 passed / 0 failed。
  唯一那条 UNLANDED 是 `4e8972f`(strategy 14:00Z 的 `fieldbuy`),**分支名自带
  `origin/discarded/strategy-20260822T140000Z-fieldbuy-duplicate`**,而 `fieldbuy` 本身
  已经在树上(本波 cand 串里就有它,见 §1.2)⇒ 极可能正是工具 LIMITS 说的
  「已改头换面落地的同一份工作」;总监 18:5xZ §AP.3 也已经就同一条做过一次抢救并
  **主动丢弃**。**不属批测台,只转述不接管,判决归总监。**
- `bash tools/batch_test/aws/session_setup.sh` → `arn:aws:iam::581436534924:user/dota2bot-agent`(受限用户,正确)。
- `check_costs.sh`:**MTD $18.741**(source `budgets/dota2bot-batch`,**免费**;forecast 23.139,
  budget limit 100.0,budget refreshed 2026-08-22T10:52:21Z)。
  **$18.741 < `COST_CONFIRM_AT`=$35 ⇒ 未花那 $0.01 调 CE**。
  与 18:06Z 逐位一致(18:11Z 那波 4 台 on-demand 尚未入账,计费滞后,不是泄漏 —— §5 四层复核 0 台)。
  **$45 围栏 / $90 刹车线 / $100 owner 批准线全部未触及**,未触发任何预算刹车。
- 开工 **0 台在跑**(18:11Z 那 4 台已跑完自毁)。

---

## 1. 收割:18:11Z 的 22-id 全集波(树 `05b5e42…`,`--rec-slots 8`)

### 1.1 下载与合并(标准路径:先分 run,再带**时分秒**前缀合并)

| run | analysis.json | .demclaim.json | `.dem`(dem21/) |
|---|---|---|---|
| `spot_20260822_181122_1_05b5e42…_17e770` | 73 | 40 | 40 |
| `spot_20260822_181125_1_05b5e42…_52a8a6` | 79 | 40 | 40 |
| `spot_20260822_181127_1_05b5e42…_2a5c28` | 72 | 40 | **39** |
| `spot_20260822_181130_1_05b5e42…_91f400` | 70 | 37 | 37 |
| **合计** | **294** | **157** | **156** |

**合并后 294 = 73+79+72+70,与逐 run 之和对账一致,一个不丢。**

### 1.2 事先登记的七条必查项(18:06Z §6.1),逐条

| # | 必查项 | 结果 |
|---|---|---|
| ① | 暖场局 `script_version` 必须是 `05b5e42` | ✅ **24/24 全是 `05b5e42`**(每 run 6 局),裸 SHA checkout 成功,**数据有效** |
| ② | 分 run 下载 + 时分秒前缀 + 对账 | ✅ 294 = 73+79+72+70 |
| ③ | cand 串逐字交叉验证,**`pullcamp` 必须 0 次** | ✅ 22 id 与 §AO.0 **一字不差**;`pullcamp` **0 次**、`wandlimbo`/`odaoe` **0 次** |
| ④ | `rec_slot_cost.py` 验收,**判据不许改、末尾不许接管道** | ✅ **真 exit 0 = 通过** → §3 |
| ⑤ | 三元组栏位 `analysis / demclaim / .dem` | ✅ **294 / 157 / 156** → §1.3 |
| ⑥ | 经济读数是 **22 杠杆合读**,不可归因单 id | ✅ 照做,见 §2 |
| ⑦ | 申报目的是 (a) 帧证据、重心 `stayfield2`;**读零不许自动记「本语料无表面」** | 记录在案,**帧判读不在本台**,已随 §6 交录像组 |

**S3 真实 cand 串**(从逐局 stamp 读出,不是转述我自己的启动命令):
```
l1trade,l5combo,midtp,suptp,tpcommit,tpdying,lf_rescue,teambrain,ownhalf,overchase,
fieldregen,wandbleed,capmono,cmrguard,tpdead,zusult,blinkflee,liondrainstop,
creeppull,stayfield,stayfield2,fieldbuy
```
= §AO.0 的 22 id,顺序一字不差。

### 1.3 三元组:`--rec-slots 8` 下**分母变了**,必须换着读

16:06Z 立的纪律是 `analysis / demclaim / .dem`,当时 `--rec-slots` 是 1 或 16。
**在 8 槽下,`analysis` 不再是 `.demclaim` 的正确分母** —— 只有 slot 1-8 录制,
slot 9-16 结构上不产生 `.dem`,也就没有 claim。正确的分母是**录制槽的局数**:

- `rec_slot_cost.py` 独立数出:**recording 157 / control 137**(合 294)。
- `.demclaim` = **157**,与录制局数 **157/157 完全相等** ⇒ **归属侧 100% 干净,一局都没漏认**。
- `.dem` = **156 < demclaim 157** ⇒ **1 局丢在上传侧**(run `181127_2a5c28`),
  按 16:06Z 的判别式这是**上传侧截断,不是归属侧问题**,读数不受影响,
  录像组少一局帧证据。

**⇒ 纪律补一句(免费,今后照用)**:三元组的第一栏在 `--rec-slots < 16` 时要写
**「录制局数(不是 analysis 总数)」**,否则 157/294 会被误读成 47% 的归属灾难。

### 1.4 局数(章程第 7 条固定栏位)

**上一波(= 本轮收割的这一波,18:11Z)最终有效局数**:

| seed | run | ab(radiant) | ba(dire) | 小计 | 暖场 |
|---|---|---|---|---|---|
| 888 | `…181122…_17e770` | 42 | 25 | 67 | 6 |
| 895 | `…181125…_52a8a6` | 41 | 32 | 73 | 6 |
| 896 | `…181127…_2a5c28` | 42 | 24 | 66 | 6 |
| 906 | `…181130…_91f400` | 42 | 22 | 64 | 6 |
| **合计** | | **167** | **103** | **270** | **24** |

**四种子两 wave 全齐,无饿死。** 预期 ≥260,实测 **270**,达标。
**ab/ba 不对称仍在且更重了**(167 : 103,radiant 打满 `--games 22` 配额的 42 局,
dire 22-32 仍撞 35min stall 上限)—— 与 20:11Z 的结论一致:**再提 `--games` 只会
加大不对称**,补 dire 要动 stall 上限(harness 侧,不自己改)。

**本轮在跑波次:无**(未启动,见 §4)。

---

## 2. 经济读数(条件 (b) 的粗粒度旁证,**不做显著性检验**)

`recover_verdict.py` 全量重算,**270 有效局**。每个指标都打基线腿:

| 指标 | delta(候选腿 − 基线腿) | comps_better |
|---|---|---|
| gpm | **−29.35** | 1/4 |
| xpm | **−27.38** | 0/4 |
| deaths | **+0.29** | 0/4 |
| last_hits | **−1.48** | 1/4 |

`suggested = hold_or_reject`。逐种子 gpm:**888 −39.69 / 895 +4.68 / 896 −31.23 / 906 −51.15**
(sd 23.15,SE 11.58)。

**两条纪律必须同时说出来(必查项 ⑥ + §AF.3)**:
- **这是 22 个 id 的合成效应,不可归因到其中任何一个** —— 含本波首次上机的
  `stayfield2` / `fieldbuy`。
- **§AF.3 已裁定「靠比较波间残差判单 id」这门手艺作废**。所以我**不**把 −29.35
  与 12:12Z 的 −29.87 并排当趋势或当「稳定」;并排只作背景,不构成任何结论。
  (唯一能说的中性观察:22-id 与 21-id 落在同一负残差带内。)

---

## 3. ⭐ `--rec-slots 8` 验收:**exit 0 通过**,而且**总监那条唯一的未测风险终于被演练了**

### 3.1 吞吐侧:通过

```
python3 tools/batch_test/soak/rec_slot_cost.py <本波 4 个 run 目录> \
    --baseline tools/batch_test/soak/rec_slot_baseline.json
```
**真 exit code = 0**(单独重定向到文件后取 `$?`,**末尾没有接管道**,合必查项 ④)。

- 语料:**294 局 / 4 run,recording 157 / control 137**,recorder set `[1..8]`(从 8 个 claim sidecar 读出)。
- 对照腿趋势(**只在 8 个非录制槽上拟合**):`timescale ~ 0.00955*slot + 1.7607`。
- 录制槽相对自己槽号的残差:**+5.6% ~ +7.7%,八个槽全是正的(= 录制槽比趋势更快)**。
- 对基线剖面(305 局 / rec_set `[1]`)做净比较,**已扣除对照槽自身的 box factor 0.993**:
  **16 个槽逐一比对,0 个录制槽超差**。
- 第二通道同向:**录制槽 19-20 局/槽,对照槽均值 17.12** ⇒ 那 156 次 `.dem` 上传
  (在 `wall_s` 之外)**没有吃掉任何一局**。

**按 §AL/§AN 的事先登记,exit 0 ⇒ 下一波上 `--rec-slots 16`。**
**一条诚实边界(不改判据,只标风险)**:16 槽**没有波内对照腿**(全部槽都在录),
它自己的验收只能跨波对本波这份剖面做 —— 建议收割 16 槽那波时把**本波并进基线**
(`rec_slot_cost.py` 收多个 run 目录),它与 16 槽那波除采集配置外完全同构。

### 3.2 ⭐ 归属侧:**「多录制者歧义从未被真正演练过」这条缺口,本波被填上了 —— 结果是一半好一半坏**

总监立「先 8 不直接上 16」时给的**唯一理由**是:此前 4 个 run 的 20/20 局 claim sidecar
虽然三判据(logname/hostname/mtime)逐局一致,但**那 20 局全是 `candidates:1`**
(池里只有一份 `.dem`)⇒ **歧义从未真正发生过**。

**本波 157 个 sidecar 里,`candidates ≥ 2` 的是 157/157**,分布
`2:1 / 3:3 / 4:3 / 5:18 / 6:32 / 7:58 / 8:42`(中位 **7**)。歧义**每一局都真的发生了**。逐局解析结果:

| 判据 | 与最终选择一致 | 读法 |
|---|---|---|
| `by_logname` | **157 / 157** | ✅ 现役 `method`,**157/157 全是 `logname`** |
| `by_hostname` | **157 / 157**(`hostname_hits` 恒为 1,唯一命中) | ✅ 独立第二判据,**与 logname 逐局同解** |
| `by_mtime` | **0 / 157** | ⛔ **每一局都指向别的局的 `.dem`** |

举一个具体的(run `181122`,slot 1,`candidates:6`):
`by_logname` = `by_hostname` = `…soak_20260822_181212_slot1.dem`(正确),
而 `by_mtime` = `…soak_20260822_181956_slot4.dem` —— **另一个槽、晚 7 分钟的另一局**。

**这不是新故障,是一条旧结论的作废**:「三判据逐局一致」这句话在 `candidates:1` 下是
**平凡真**(只有一个文件可选,三个判据当然指同一个)。**并发一开,mtime 立刻 0/157。**

**好消息(也是本轮最该传下去的)**:`tools/batch_test/soak/dem_claim.sh:144` 早就把 mtime
**锁死在 `[ "$rec_slots" = "1" ]` 之后**,8 槽下它结构上不可能被选中 ——
`method` 157/157 都是 `logname`。**那条护栏现在从「设计上的谨慎」升级为「实测必需」**:
若哪天有人为了「兜底」把 rec_slots 限制放开,后果就是每一局都认错人,而且
**`.dem` 数量、`analysis` 数量、三元组全部正常,静默到没有任何计数能报警**。
⇒ **建议在 `dem_claim.sh:144` 上留一条 `[reverse]` 源码钉子**(谁放开这个限制就转红);
harness 归 harness,**本台按章程不自己改**,已随 §6 上报。

**给录像组的直接结论**:本波那 **156 个 `.dem` 的归属是 100% 可信的,没有一局认错了人**
—— 这一次是**在歧义真正发生了 157 次的条件下**验出来的,比 12:12Z 那次强得多。

---

## 4. 启动决策:**不启动**

- **4a**:`queue.json` 无 status=`pending` **且需要开波**的请求。
  `hero-1` / `hero-2` 自述 **NO NEW WAVE NEEDED**(档案扫描,不花 AWS 钱);
  `strategy-1` = `done`;`strategy-2` = **`approved-as-rider`** —— 总监 §AP.1 明确
  **驳回了它的专波**,裁定它**搭 §AP.0 那一波顺路买**,所以它不是一条「要单独开波」的请求,
  也**不构成绕过例行节流的理由**。
- **4b 例行三条件**:
  - **(i) ❌ 不满足** —— 上一次例行波次启动 **18:11:22Z**,现 **20:09Z**,间隔 **1h58min**,
    距 6h 门槛差 **~4h02min**。**小时级差额**,按 16:10Z / 08:08Z / 16:06Z 先例即**不启动**
    (**不是 06:07Z 那种「差 2 分钟按立法目的照常启动」**;也不重演 18:06Z 那条
    「不会 sleep 的 sleep」——那是脚本 bug,不是先例)。
  - (ii) ✅ 满足(`pullcamp` 按 §AP.1 重新入集,22 → 23 id,是新东西)。
  - (iii) ✅ 满足($18.741 + ~$0.8 ≈ $19.5 ≤ $45 围栏)。
- **owner 优先项核对**:P1 球在**协同组(1)**、P2 球在**协同组** —— 两项当前都不在批测台。
  本轮不开波**对两项都无代价**:P2 要的 (a) 语料**正是我刚收割的这一波**
  (`stayfield2`/`fieldbuy` 首次 armed,156 个 `.dem`),球在录像组读帧;
  P1 的 `pullcamp` 回程读数要的那一波,配置已预置好(§4.1),只等 6h 线。

### 4.1 下一波预置(最早 **2026-08-23T00:11Z**)

| 项 | 值 | 依据 |
|---|---|---|
| cand 串 | **§AP.0 的 23 id**(§AO.0 的 22 id **加回 `pullcamp`**) | 总监 §AP.0;**不自行改被测集合** |
| 种子 | 888 / 895 / 896 / 906 | §AP.0 |
| `--rec-slots` | **16** | §AL/§AN 事先登记:本波 exit 0 ⇒ 升到 16(§3.1) |
| 拓扑 | 4 台 × 1 种子,c6i.4xlarge **on-demand**(64 vCPU 配额恰好 4 台) | 14:12Z 短波次别用 spot |
| 槽 / 看门狗 / games | 16 槽 / 2h / `--games 22` | 沿用;**`--games` 不再上调**(§1.4) |
| 树 | 上机时 `git ls-remote origin main` 取**全 40 位 SHA** | 章程 §5 |
| 核树 | `git fetch --depth 1 origin <全40位SHA>` **再** `git log <SHA>..HEAD -- bots/ game/`;**exit 128 = 不可比,不是无漂移** | 章程 §5(浅 clone 陷阱) |
| 顺带买 | `queue.json:strategy-2` 的三条臂内读数(连接率 / 开拉点离泉水中位 / 深营 poke 帧占比) | §AP.1 approved-as-rider |

**下一波的必查项(事先登记,收割时照做)**:① 暖场 `script_version` = 那一波钉的短 SHA;
② 分 run 下载 + 时分秒前缀 + 对账;③ cand 串逐字交叉验证,**`pullcamp` 这次必须出现 1 次**;
④ `rec_slot_cost.py` 验收(16 槽**无波内对照腿**,须把本波并进 `--baseline`,见 §3.1);
⑤ 三元组,**16 槽下第一栏 = 全部局数(录制局数 = analysis)**;
⑥ **`candidates` 分布 + 三判据逐局一致性再读一次**(16 并发下 `hostname_hits` 是否仍恒为 1
是新问题,8 槽下的 157/157 不自动外推);⑦ 经济读数是 **23 杠杆合读,不可归因单 id**。

---

## 5. 泄漏检查(四层 + spot 请求,全免费只读)

| 层 | 开工 | 收尾 |
|---|---|---|
| 实例(pending/running/stopping/stopped) | **0** | **0** |
| 游离卷(status=available) | 0 | 0 |
| 快照(self) | 1(`snap-0ad026b386c804288`,唯一常设成本) | 1 |
| EIP | 0 | 0 |
| spot 请求(open/active) | 0 | 0 |

**无泄漏。** 收尾走 `check_costs.sh --leak-only`(不查花费,零成本 —— 收尾那次要的是
「0 台在跑」,不是 MTD 数字)。

**S3 固定栏位**:`soak/` 前缀 **160**(16:06Z 是 156,+4 = 本波四个 run)、
`dem21/` 前缀 **12**、`unattributed/` **0**、off-roster **0**。
远端 main tip 开工时 `a27b436`。

---

## 6. 跨组

- 在既有 **GH #45** 下追评(**不新开**):交付本波读数 + 三元组 + `--rec-slots 8` 验收通过
  + **mtime 判据 157/157 全错**这条归属侧实测。
- **建议 harness 侧加一条 `[reverse]` 钉子**在 `dem_claim.sh:144` 的
  `[ "$rec_slots" = "1" ]` 上(§3.2)——**本台不自己改 harness**,已在 #45 里点名。
- `[batch] #70`、`[harness] #98`(已落地,建议总监确认后关闭)/`#95`/`#75`/`#33`、
  `[bug] #96` 仍开着,不重复开。

## 7. 验证

本会话**未改动任何 Lua**(改动仅 `iterations/` 下报告 / 章程 / queue.json),
且本容器**未装 `luacheck` / `lua5.1`** ⇒ 按铁律第 6 条立法目的,本轮无适用对象。
**本轮无估算项**(未启动、无费用预估)。

## 8. Token 用量

```
TOKENS total_in=5,758,480 out=46,106 turns=54
```
(到统计时刻为止;统计后的收尾回合(commit/push/章程定稿)不计入。)
