# 批测台 2026-09-02T15:15Z — 收 2613 补跑并把 W38 补成 4 粒;发 W39(55-id 家族第一波)

**一句话**:W38 的缺臂补上了(4 粒 / 211 局,gpm **+18.10**),W39 已按 55-id 起飞;
本轮真正的产出是 **GH #408 的按需补跑机把它自己的 `status_code` 弄丢了**,
而丢法是**结构性的**:按需机没有 SIR 通道,唯一的证据窗口只有 describe-instances 的 ~1 小时。

---

## 一、开工自检(铁律 10)

第一条命令**又误用管道**,自检自拒(**同形第十一次,又是当轮第一条命令**)。
工具自己打的复发计数仍是 `it has recurred 5x`,与本台报告序列记的次数**对不上**——
**没有任何东西把这两个计数器对起来过**,照上一轮据实并列,不选一个写。

改走重定向后拿到真判决:**`SELFCHECK_EXIT=3`**,`legs run 8`,
`FINDINGS (exit 3) : cadence trunk-red(python) trunk-red(lua)`,
`UNCERTIFIABLE (exit 2): none`。

- `unlanded_commits`:**OK**,可核验窗口内无未落地工作。
- **cadence 2 findings**:`batch-desk` 09-01T21:14Z→09-02T00:45Z **3.5h**;
  `strategy` 09-01T13:55Z→18:55Z **5.0h**。两处**与上一轮逐字相同**,是历史的洞。
  ⚠️ **但本轮有一个它还看不到的新洞,要自己登记**:本台上一份报告是 **12:15Z**,
  本轮是 **15:2xZ**,中间 **~3h**、**14:xxZ 那次触发没有留下任何报告**。
  这个洞不是记账问题,**它花掉了 §三的那个读数**(见下)。
- `stable-v1` / `stable-v2` 两个锚点:**EXISTS / PINNED / SHIPPED 全 ok**。
- 队列:**59 open**;`OTHER (routing/slot ruling still owed): 2` = **`strategy-37`(roshdist)**、
  **`strategy-38`(ckpush)**,两条都 `first_seen=unknown`。`strategy-36` 本轮**已被裁**(见 §四)。
- **trunk red(python)**:`tests/test_detector_source_constants.py` **FAIL**;
  `73 passed, 1 failed, 2 uncertifiable`。**未 `git stash` 重跑,故不声称 main 也红。**
- **trunk red(lua)**:`test_incoming_damage_callsite_census.lua` —— 「已发布的 43 现在读成 44」,
  与上一轮**同一条**(1 of 73 Lua 检测器文件红)。
- ⭐ **GH #420 本轮原样复发**:python 腿自己打 `2 uncertifiable` 并**点名**
  `tests/test_rc_wrapper.py` / `tests/test_selfcheck_lua_leg.py`,
  而汇总块打 **`UNCERTIFIABLE (exit 2): none`**。**只看汇总块的读者会得到「没有东西没人看过」。**
  #420 上一轮已开、**仍未裁**;本轮不重复开,只登记它**照原样复现**。

---

## 二、成本与围栏

九月 MTD **`$3.637`**,`budgets` 免费通道,快照 **`2026-09-01T17:58:37Z`** ——
**与前三轮同一张快照 ⇒ 连续第四轮无新围栏信息**。MTD `< $35` ⇒ **CE 复核未触发,那 `$0.01` 没花**。

**围栏算术**(章程 2(甲),逐波按它自己的界别取价):

| 项 | 取值 | 依据 |
|---|---|---|
| MTD | $3.637 | budgets(免费) |
| W38 四台 spot(含 86 秒就死的那台) | ~$0.60–0.64 | SIR 机时 2.576 h × $0.231–0.250(上一轮已登记) |
| 2613 按需补跑 + 自造失误机 | ~$0.60–0.63 | **S3 反解**机时 ~0.87–0.92 h × $0.673 + ~1 机分钟 |
| W39 预估(spot) | $0.90 | 章程常数(08-30 总监) |
| **合计** | **≈ $5.77** | ≤ **$80** ⇒ **闸 (iii) 过** |

最坏情形(W39 全程降级按需 $2.15)= **$7.02**,同样过。
**不跨任何 owner 可见告警档($50/$80/$100)⇒ 不欠解释行。**

**`$0.90` 自动失效条款:不触发** —— W38 的 spot 单波实测(机时反解)$0.60–0.64 < $0.90。
⚠️ 按需补跑机**不进这个常数**,它是按需价,不是 spot 单波价的证据。

---

## 三、⭐⭐⭐ GH #408 的第一个数据点丢了,而**丢法是结构性的**

上一轮把这件事明确交给本轮:「下一轮收割给它第一个完整数据点
(存活分钟 + `market: on-demand` + EC2 `StateReason.Code` + 增量账单)」。
**拿不到了。**

两条普查本轮**都是空的**,各自 exit 0:

```
ec2 describe-instances --filters Name=instance-state-name,Values=pending,running      -> 0 行
ec2 describe-instances --filters Name=instance-state-name,Values=terminated,shutting-down -> 0 行
```

- 补跑机 `i-0075962af14069424` 起飞 **12:18:43Z**,最后一份 `analysis.json` 上传 **13:10:53Z**,
  随即自毁(`validate_onspot.sh` 交完 verdict 立刻 terminate)。
- 本轮读数发生在 **15:2xZ**,**距终止 ~2 小时**,已远超 GH #375 的 **~1h** 窗口。

⛔ **`ec2_state_reason_code` 对这台机器永久不可恢复**,记 `UNRECOVERABLE`,
**不是「null 因为干净」,是「null 因为窗口关了」**——这两件事在 JSON 里长得一模一样,
所以 `W38_wave.json:rerun.status_code` 写的是那句话本身,不是一个 `null`。

**⭐ 结构性的那一半(这才是要交出去的东西)**:
**按需机没有 spot instance request**,而 SIR 正是让 spot 机的 `Status.Code` **活过**
describe-instances 窗口的那个通道(W37/W38 两轮都实测过:describe 空了,SIR 还在)。
⇒ **GH #408 让「容量码 ⇒ 直接上按需」的那条路,证据半衰期比它取代的 spot 路径严格更短**:
spot 留 SIR,按需**什么都不留**。它的 `status_code` **必须在终止后 ~1h 内落盘**。
而本台**没有一次触发落在那个窗口里**(12:15Z 那轮机器还在跑;14:xxZ 那次触发没有产出;
本轮 15:2xZ 已经晚了 2h)。**§一登记的那个 cadence 洞,花掉的就是这个读数。**

⇒ 本轮把这条交回总监(见 §八)。**本台不改 GH #408 的裁定**,只报它的运维形状。

---

## 四、W38 补齐:2613 补跑收割 + 4 粒全波读数

`recover_verdict.py <一个父目录,五个 run 前缀> "<54-id>"` ⇒ **`RV_EXIT=0`**。

**语料同一性(承重,先写)**:交给工具的串**不是工作树里的那一条**。
`test_set.md` 今晨已经动到 **55 id**(`slotpush` 入集,md5 `bfe60fcd…` / 485 字节),
照工作树取串会把语料**从未携带过**的候选集算进去。
从**发波那棵树**取回:`git show 7196468dad…:iterations/streams/test_set.md | sed -n 2p`
⇒ **54 id / 476 字节 / md5 `80392b258fcd214cf351231be61d15a4`**,与 `W38_wave.json:arm_md5` **逐字节相同**。

**输入**:`files_seen 235 / games_loaded 235 / unparseable 0 / source_dirs 4 / pooled_basename 覆盖 0`;
`scored_games 211`(= 235 − 24 局暖场,4 个产局 run 各 6 局)、`unfinished 0`。
第五个前缀(`4a60ad`,86 秒就死的那台)**仍是 0 文件**;自造失误机的前缀
(`..._23b452`)**在 S3 上根本不存在** ⇒ **没有东西需要排除**(上一轮预登记的那条排除义务,**查过再说的**)。

### 4.1 补跑机自身的两条断言(收割两件必做之一)

- **`script_version` 暖场断言 = PASS**:6 局暖场**全部** `7196468d`,即钉住 SHA 的 8 位前缀。
  **这一条本轮是承重的**:`origin/main` 现已是 `1a13b9df…`,且自钉点起 `bots/` **确实漂了**
  ⇒ 不钉 `--ref` 的补跑会测**另一棵树**,而它会被并进 2665/2761/2840 的池子里,**没有任何门会举手**。
- **armed 串断言 = PASS**:53 局计分游戏**全部**打 `mirror:<54-id>:s2613:<side>`,
  **0 处不匹配**;戳的分布 `radiant 37 / dire 16`,与工具算出的 `ab 37 / ba 16` 逐位一致。
- **采集配置断言 = PASS**(自造失误机的教训,**按语料查不按散文查**):补跑机 59 局跨 **slot 1..16**
  (每槽 3–4 局),与 W38 另外三台的 16 槽配置相同。

### 4.2 四量读数(**两分层由工具自打,未手算** —— 铁律 4(i-a)/(i-d))

| 量 | mean(4 粒) | ab 层 | ba 层 | side | 反号 | side_gt_arm | comps_better |
|---|---|---|---|---|---|---|---|
| gpm | **+18.10** | +124.47 | −88.27 | 106.37 | 是 | 4/4 | **3/4** |
| xpm | **+0.11** | +106.27 | −106.04 | 106.15 | 是 | 4/4 | 2/4 |
| deaths | **+0.20** | −1.63 | +2.04 | −1.84 | 是 | 3/4 | 1/4 |
| last_hits | **−0.57** | +18.22 | −19.37 | 18.80 | 是 | 4/4 | 2/4 |

**四量仍然全部 `sign_flip`**。按铁律 **4(i-c)**:每粒已 50/50 swap-average ⇒
反号 ⟺ `|side| > |arm|`,**恒等式不是诊断**。**登记(4(i-a)),不据此下任何结论。**

**逐粒(全部过 `arm_depth ≥ 8` 硬门,零排除)**:

| seed | ab / ba | arm_depth | gpm | xpm | deaths | last_hits | winrate_headroom |
|---|---|---|---|---|---|---|---|
| **2613(补跑)** | 37 / 16 | **22.34** | **+48.93** | +26.11 | −0.34 | +2.88 | **0.25** |
| 2665 | 38 / 20 | 26.21 | −6.41 | −13.74 | +0.24 | −3.06 | 0.075 |
| 2761 | 37 / 21 | 26.79 | +12.91 | −18.88 | +0.48 | −2.85 | 0.0 |
| 2840 | 29 / 13 | 17.95 | +16.97 | +6.97 | +0.44 | +0.74 | 0.0 |

`min_arm_depth 8`、**`thin_arm_seeds` 空**、**零排除**。
**未用 `--min-arm-depth` / `--allow-pooled-basenames` / `--allow-unparseable`
⇒ 本节没有任何一行「这是 SKIP 不是 pass」。**
`suggested: hold_or_reject`(章程:只是提示,promote 仍走三条件)。

**离散度**:gpm 逐粒 48.93 / −6.41 / 12.91 / 16.97 ⇒ **sd 22.94 / se 11.47**。
按 SS-CO.4 登记为**效应量读数**(SE 仍管排序与定量主张),
**不拿它给整波下「不够裁」的结论** —— 那句话按三条件写,或者交给总监。

⚠️ **补跑那粒是四粒里最大的一个正值(+48.93)**,而它是**唯一一粒在不同时刻、不同市场类型
(按需)、不同实例上跑出来的**。`--ref` 钉住了**树**,钉不住**时刻与机器**。
**登记这条,不解读**:本台不做判断分析。

### 4.3 winrate:**通道 DEGENERATE,不是读数**(SS-CT 强制)

`winrate_channel` = **`DEGENERATE`**;少数侧只赢 **11/235**(share **0.0468** < 0.20);
波级 side census **radiant 11 / dire 224**;`mean.winrate_headroom` = **0.0813**。
逐粒 headroom:**2613 `0.25`**、2665 `0.075`、**2761 `0.0`**、**2840 `0.0`**
⇒ 后两粒的 `winrate 0.500` **在算术上被逼成 0.500,与候选做什么无关**。

⇒ **`winrate 0.495` 与 `comps_better winrate 1/4` 本轮写成占位符,不得引作条件 (b) 的支持。**
⚠️ 补跑那粒把**唯一一点真 headroom** 带了进来(0.25),但**并池后通道仍是 DEGENERATE**。

`winrate_independent_of_gold` 打 **`235/235`**、`winner_by = {engine_natural: 235}`
⇒ GH #108/#352 桶名 bug **仍是修好的**。

### 4.4 收割义务对账

| 义务 | 状态 |
|---|---|
| 4(i-a) 两分层登记读数 | ✅ 工具自打,**未手算** |
| 4(i-d) 不许按局数并池 | ✅ 先每粒 swap-average,再跨 4 粒算术平均 |
| 4(i-c) 反号登记但不否决 | ✅ §4.2 |
| SS-CT winrate | ✅ §4.3,DEGENERATE 写成占位符 |
| **SS-DK.3 `slotarb` 负控** | ❌ **仍买不到** —— 补跑一台改变不了 schema,**GH #423 照旧挂着** |
| SS-DK.1 (v-b) `slotdust` 不是子集 | ✅ 登记,本轮未取任何读数 |
| SS-DK.5 不许并成 `0SLOT9` 按侧读数 | ✅ 全文未并 |
| 不许与 W35/W37 并池 | ✅ W38 只与**它自己**并(缺臂修复),不与别的家族并 |

### 4.5 ⭐ 一个新的读法陷阱:`reclaim_blind.py` 的 yield 数**不是这一波的最终产出**

`reclaim_blind.py --wave-json W38_wave.json` ⇒ **`RB_EXIT=0`**,
`VERDICT: not blinded -- the wave delivered 3 paired seed(s)`,`NEXT WAVE: spot`。

**这个 `3` 对它自己的问题是对的**(市场有没有把这一波弄瞎),
**但它读的是 `machines[]`,而补跑机按 GH #271 的写法住在 `rerun` 节** ⇒
修复之后这一波实际交付的是 **4 粒**,工具仍会一直打 `3`。
**失效方向是花钱那一侧**:哪一轮拿 yield 数去决定「要不要补跑」,就会**把已经补过的种子再补一次**。
本轮开 `[harness]` issue 交出去(见 §八),**本台不自己改 harness**。

---

## 五、W39 发波(55-id 家族第一波)

### 5.1 三闸

- **(i) 6h 节流:PASS,靠守卫** —— W38 起飞 `09:31:32Z` ⇒ 解锁 `2026-09-02T15:31:32Z`。
  发波块自带 `GATE-I` 守卫,**先扣住并逐条打 `GATE-I NOT OPEN`**,到点打 `GATE-I OPEN` 才发第一台。
  ⚠️ **2613 的按需补跑不消耗这道闸**(它是已飞波次的登记内修复,不是常规波),上一轮已登记,本轮照此执行。
- **(ii) 有新东西可测:PASS,走第一支** —— `test_set.md` 于 09-02T10:xxZ 把 **`slotpush` 入集**
  (54 → 55,§DN,GH #415,queue `strategy-36`)⇒ **armed 串真的变了**。第三支同样成立(发波前该家族 0 粒)。
- **(iii) 成本围栏:PASS** —— 见 §二,$5.77 ≤ $80。
- **(iv) 无欠收割:PASS,本轮亲自跑的** —— W38 的最后一条臂**就是本轮收的**(§四),
  收完才发波;`reclaim_blind.py` **RB_EXIT=0**、`NEXT WAVE: spot`。

### 5.2 ⭐ 卡 W39 的那件事**已经解掉了**(上一轮开的 GH #424)

GH #424 **已 CLOSED(13:21:27Z)**,总监追评逐字:「**批测台的时限解掉了**:W39 可以按 55-id 走」。
落地 commit **`d16227aa`**,已在 `origin/main`(本轮 `git merge-base --is-ancestor` 核过)。

⛔ **但它给收割方留了两条硬限定,写进 `W39_wave.json:harvest_obligations`,不写进散文**:
1. `outlatch` 的 (a) **必须两个分层各自登记**,且**两层反号按铁律 4(i-b) 读成噪声** ——
   这个估计量带的是**侧相关的混杂**(armed `slotpush` 扫 5/5,出厂 radiant 4/5、dire 1/5,
   否决从 dire 侧拿走的帧约是 radiant 的 5 倍),不只是侧相关的抽样;
2. **W38(54-id,无 `slotpush`)与 W39 起(55-id)的 `outlatch` (a) 不是同一个量,不许跨波比。**

### 5.3 四道门

- `tree_verified`:`git ls-remote origin main` = 本地 HEAD = **`1a13b9df15f23bef704fecc3828ee7e3eb21fc67`**(逐字节);
- `arm`:**55 ids / 485 字节 / md5 `bfe60fcdfbe466440213541c8f37a254`**,与 `test_set.md` 第 4 行散文逐字对上;
- `carrier_terms.py --arm "<55-id>"` ⇒ **exit 0**,`10 hero-scoped / 45 generic / 0 unresolved => 6 term(s)`;
- `check_armed_wiring.py --cand "<55-id>"` ⇒ **exit 0**,`all 55 armed ids wired on HEAD`;
- `seed_roster_index.py --build` ⇒ **exit 0**,**在选种之前**跑的;
- `seed_draft.py --assert-carrier-from-arm` ⇒ **`CARRIER_GATE ids=10 seeds=4 exit=0`**,**六项全 `satisfied=2`**。

⚠️ **一处自造的 exit 2,点名登记**(GH #412 的立案句正是「把自造的拒答读成真拒答」):
`carrier_terms.py` 第一次调用写成 `tools/batch_test/carrier_terms.py` ⇒
`No such file or directory`,真实路径是 **`tools/batch_test/soak/carrier_terms.py`**;
第二次写成 `--arm` 不带值 ⇒ `expected one argument`。**两次都是我的调用错误,不是工具对这棵树的拒答。**

### 5.4 选种:窗口 `[2601,3000]` 保留,右移**没有**触发

`372 available / 28 banked-or-drawn in window`;
**popcount 供给 `{0:50, 1:140, 2:141, 3:41}`**;**34 个 mask**;
**`BEST min-per-term = 2`、`slots = 12`、该层 `5` 个组合**。
预登记的右移条件是「**搜出 0**」,本轮搜出 **12** ⇒ **不触发,不提前右移、不放松 `≥2/term`**。

**但周期性机制仍在跑,而且这一轮更明显**:`popcount 4` 层**仍空**;
`popcount 3` **45 → 41**;该层组合数 **20 → 5**。**连续第三波卡在 `slots = 12` 这个下限。**
(GH #285 **第二十五轮未裁**。)

排除集 = 索引 banked ∪ **W35/W36/W37/W38 抽过的十六粒**(抽过就是抽过)。
选出 **2740 / 2877 / 2896 / 2914**,`--seed` 复核**四粒全 `no banked games`**。

⚠️ **⭐ 零成本次序偏好(pudge)本轮拿到 `0`** —— 最优层那 5 个组合的 mask 池里
**一个 pudge 载体都没有**(可用池整体有 57 粒带 pudge,但它们都不在这 5 个组合用到的 mask 里)。
W37 有 2 粒、W38 有 1 粒、**W39 是 0** ⇒ **`queue.json:strategy-26` / `rotscope` 本波拿不到任何 pudge 证据**。
这是**登记不是抱怨**:偏好从来只是最优层内部的 tiebreak,**永远不能放松 `≥2/term`**。

⚠️ **载体冗余仍薄**:六项**各恰好 2** ⇒ **掉任何一台,该项就到 1**。
W33/W35/W36/W37/W38 分别掉了 4/3/4/2/1 台。

### 5.5 采集配置:**一个字都没动**

id 集变了(54 → 55),所以按章程「**不许在同一波里同时改 id 集和采集配置**」,
`--slots 16 --hours 2 --rec-slots 8 --games 12`、4×1 拓扑、一次调用一个 `--az`,**与 W38 逐项相同**。
`rec_slots` 仍**押在 8**:抬到 16 的验收门按 GH #413 **结构上跑不了**(`rec_slot_cost.py:180`
在 claim sidecar 对 `rec_slots` 不一致时拒答),**拿旧基线当新基线等于事后改判据**。

### 5.6 起飞:四台全 `SPOT_RUN_EXIT=0`,**但 AZ 分散度是 GH #252 落地以来最差的一次**

守卫 `15:31:51Z` 打 **`GATE-I OPEN`**(此前逐条打 `GATE-I NOT OPEN`,共扣住 ~10 分钟)。

| seed | 请求 AZ | 实得 AZ | 实例 | SIR | run token | 容量拒绝 |
|---|---|---|---|---|---|---|
| 2740 | us-west-2a | **us-west-2c** | `i-022ceff8ec8ab5827` | `sir-h4afjhcm` | `724bad` | **2** |
| 2877 | us-west-2b | **us-west-2c** | `i-0447598090fb1e251` | `sir-c6b7kw6m` | `85aa25` | **1** |
| 2896 | us-west-2c | us-west-2c | `i-08f01bb61ccb1e91c` | `sir-fcj7khkn` | `39b063` | 0 |
| 2914 | us-west-2d | us-west-2d | `i-0b0d4f773e37dcdc9` | `sir-8c77hikp` | `fa631e` | 0 |

`InstanceLifecycle` 四台全 **`spot`**、全 `running`、**四个 SIR 互不相同**
(**这才是 owner spot-first DoD 要的证据,`run_id` 的 `spot_` 前缀不是**);四个 `RUN_TOKEN` 互不相同。
市场降级阶梯**一级都没下**,`AZ RING EXHAUSTED` **一行都没有**。

⛔ **但 4 台只落到 2 个 AZ(`2c`×3 / `2d`×1)。**
GH #252 的验收(**≥2 个不同**)过;**GH #256 的严格验收(4 个不同、无 re-aiming)不过**,
`! re-aiming inside the ring` **两行**(按 #256 这**不是降级**,但要点名「请求 X 实得 Y」)。

**⭐ 机制比计数值钱**:环走是**确定性的、单向的**(a→b→c→d)。
一旦全区容量紧张,**每一次改投都收敛到同一个「第一个还有容量的 AZ」**
⇒ **GH #252 买来的放置去相关,恰恰在它被买来对付的那个工况下被抵消**。
#256 已经抓到过这件事的**一跳版本**(改投落到刚清空的 AZ);**这是多跳版本,而且它把 3/4 挤到了一起。**

**暴露面是具体的,不是抽象的**:六个载体项**各恰好 2**,而 `2c` 那三台之间带着**全部六项**
⇒ **一次 `2c` 容量事件会把六项里的五项打到 ≤1、`obsidian_destroyer` 打到 0**。
那正是 W17 的形状(四台一个 AZ、一次事件带走全部)按 3/4 强度重演。
⇒ 本轮开 `[harness]` issue(见 §八),**本台不自己改 `spot_run.sh`**。

⚠️ **连续第六波被容量咬,而且这是第一次咬在起飞而不是飞行中**:
W33/W35/W36/W37/W38 分别被回收 4/3/4/2/1 台;W39 一台没掉,但**付了三次
`InsufficientInstanceCapacity` 才起飞**。读数交 **GH #408(仍零评论未裁)**,
**本台不重裁 owner 的 GH #158。**

### 5.7 局数(铁律 7)

- **(a) 上一波最终有效局数**:W38 = **4 粒 / 211 计分局**
  (2613 `37/16`、2665 `38/20`、2761 `37/21`、2840 `29/13`;
  ab/ba 合计 **141 : 70**,**高度不对称,已注明**)+ 24 局暖场(不算有效局)。
- **(b) 本轮在跑波次**:W39 起飞时 **0 局**(记录写于起飞时刻)。
  预期 **4 粒 × 12 局 × 2 腿 ≈ 96 计分局**(不含暖场),预计 `~16:1x–16:3xZ` 自毁。

---

## 六、泄漏检查

**收尾那次要的是「几台在跑」,不是 MTD 数字。**

- **进场时**(收割轮判据 = **0 台**,补跑机应已自毁):`pending,running` ⇒ **0 行**;
  `terminated,shutting-down` ⇒ **0 行**(**这正是 §三那个读数丢掉的地方**)。
- **发波后**(发波轮两条点名,零成本,`--leak-only` 只报「多出来的」、
  **「少一台」它永远不会举手**,所以这两条不能省):
  1. `soak-run` 标签 = **恰好四个值**,与 `W39_wave.json:machines[].run_id` **一一对上**
     (不多不少);
  2. `terminated,shutting-down` ⇒ **返回空**,**本波窗口内无 `Server.SpotInstanceTermination`**
     ⇒ 起飞后没掉臂。
- `describe-volumes`:**四卷全 `in-use`**(分别挂在那四台上),**无孤儿**。
- `check_costs.sh --leak-only` ⇒ **`LEAK_EXIT=0`**,列出的正好是这四台。
  常驻成本仅 AMI `ami-0a990a26d89c66547`。

---

## 七、铁律 6

`bots/` / `game/` **本轮一行未改**(改动只在 `iterations/`)。
静态半:`bash tools/agent/luacheck_gate.sh` ⇒ **`GATE_EXIT=0 CLEAN`**
(冷启自装 `lua-check`,`luacheck bots game: 0 warnings`);
`bash tools/agent/arm_push_gate.sh` ⇒ **exit 0**,`core.hooksPath=.githooks` 已上膛
⇒ 两次 `git push` 各自又跑一次同一道门。
**未用 `RULE6_BYPASS` ⇒ 本报告没有「SKIPPED, not passed」那一行。**
动态半(`lua5.1 tests/run_tests.lua`,~100 min,GH #124)**未跑,也不声称**。

另跑 `python3 tests/test_wave_gate_keys.py` ⇒ **exit 0,`151 checks, 0 failed`**
—— **W39 的四道闸键在起飞当轮就是齐的**(W38 是靠收割轮补写的,那次它抓到 2 个 FAIL 并点名批测台)。

---

## 八、交出去的(GitHub issue,按 GH #290 在 push 之后发)

1. **[batch] GH #408 的按需补跑路径:证据半衰期比它取代的 spot 路径严格更短** → 总监。
   见 §三。**建议的验收方式**:要么把「按需补跑机的 `StateReason.Code` 必须在终止后 1h 内落盘」
   写成**发波那一轮**的义务(而不是交给下一轮收割),要么承认这个字段对按需机是**尽力而为**,
   别再让下游以为它一定拿得到。
2. **[harness] `spot_run.sh` 的 AZ 环走是确定性单向的,容量紧张时会把整波挤回同一个 AZ** → 总监。
   见 §5.6。**这是 GH #256 的多跳版本**;建议的验收方式:环起点按调用随机,
   或**跳过本波已用过的 AZ**,验收就搭下一波的车看 `Placement.AvailabilityZone` 的不同值个数。
3. **[harness] `reclaim_blind.py` 的 yield 只数 `machines[]`,数不到 `rerun`** → 总监。
   见 §4.5。**失效方向是花钱那一侧**(可能重复补跑同一粒已修好的种子)。

---

## 九、queue.json(13 行)

- **十行**(`strategy-25/26/27/28/29/30/34/35` + `hero-24/25`):`result` 追加 **W38 全波 4 粒读数**
  (取代原 3 粒读数,并写明原节保留在哪);`rides_wave` 追加 **改搭 W39** 与**不可并池**。
  - `strategy-34/35` 另注:**SS-DK.3 负控仍买不到(GH #423)**,`slotarb` 条件 (a) 不可买。
  - **`strategy-28` 另加 SS-DN.6 两条硬限定**(`outlatch` 的 (a) 两分层分开登记、
    反号按 **4(i-b)** 读成噪声;**不许跨 54/55 波比**)。
  - **`strategy-26` 另加**:**本波零 pudge 载体**,收割时**不许把「没有 pudge 证据」写成负向读数**。
- **`strategy-36`**:`status` `pending` → **`running`**,首次真正搭上波(W39),
  带 GH #424 已裁的落地证据与起飞侧实况。
- **`strategy-31/32`**:**连续第三轮不搭任何波**,按铁律 9 **第三次显式交回总监**
  (掉棒正是铁律 9 立案的那个形状)。

---
## 十、铁律 11

**未触发任何 `requires approval`**;GitHub MCP 可用(`issue_read` #424 的正文与追评都读到了)。
**无空转等待**:闸 (i) 前那 ~17 分钟全部用在零成本工作上
(自检、成本、**收 W38 最后一条臂**、四道门、选种、W39 记录、报告草稿、queue 更新、静态门)。

---

## 十一、本轮工具读数汇总(evidence discipline:退出码都不经管道)

| 命令 | 退出码 | 读数 |
|---|---|---|
| `routine_selfcheck.sh`(第一次,管道) | **拒答** | `REFUSED ... stdout is a pipe`,**同形第十一次** |
| `routine_selfcheck.sh > file` | **3** | `legs 8`;`FINDINGS: cadence trunk-red(python) trunk-red(lua)`;`UNCERTIFIABLE: none` |
| `session_setup.sh` | 0 | `AWS ready: arn:aws:iam::581436534924:user/dota2bot-agent` |
| `check_costs.sh` | 0 | MTD `$3.637`,快照 `2026-09-01T17:58:37Z`(**连续第四轮同一张**) |
| `describe-instances`(进场,running) | 0 | **0 行** |
| `describe-instances`(进场,terminated) | 0 | **0 行** ⇒ §三那个读数丢了 |
| `recover_verdict.py <pool> "<54-id>"` | **0** | 235 seen / 235 loaded / 0 unparseable / 4 dirs;211 计分局;4 粒 |
| `reclaim_blind.py --wave-json W38` | **0** | `not blinded -- 3 paired seed(s)`;`NEXT WAVE: spot` |
| `carrier_terms.py`(第一次,**路径写错**) | **2** | `No such file or directory` —— **自造的拒答,不是工具拒答** |
| `carrier_terms.py --arm`(第二次,**漏值**) | **2** | `expected one argument` —— 同上 |
| `carrier_terms.py --arm "<55-id>"` | **0** | `10 hero / 45 generic / 0 unresolved => 6 term(s)` |
| `check_armed_wiring.py --cand "<55-id>"` | **0** | `all 55 armed ids wired on HEAD` |
| `seed_roster_index.py --build` | **0** | **在选种之前**跑的 |
| `seed_draft.py --assert-carrier-from-arm` | **0** | `CARRIER_GATE ids=10 seeds=4 exit=0`,**六项全 `satisfied=2`** |
| `seed_roster_index.py --seed 2740 2877 2896 2914` | **0** | 四粒全 `no banked games` |
| `spot_run.sh` ×4 | **0 / 0 / 0 / 0** | 四台全 spot、全 running;**两行 `re-aiming`**、三次容量拒绝 |
| `describe-volumes` | 0 | 四卷全 `in-use`,无孤儿 |
| `check_costs.sh --leak-only` | **0** | 恰好那四台 |
| `luacheck_gate.sh` | **0** | `bots game: 0 warnings`,**CLEAN** |
| `arm_push_gate.sh` | 0 | `core.hooksPath=.githooks` 已上膛 |
| `tests/test_wave_gate_keys.py` | **0** | `151 checks, 0 failed` |

⚠️ **三处 exit 2 全是我自己的调用错误**(一次自检误用管道、两次 `carrier_terms.py`),
**逐条点名而不是含糊带过** —— GH #412 的立案句正是「把自造的拒答读成真拒答」。

---

## 交棒

1. ⭐⭐⭐ **总监** — **GH #408 的按需路径证据半衰期**(§三,本轮新开 issue)。
   **第一个数据点已经永久丢了**,这不是补得回来的东西;要么把落盘义务前移到发波轮,要么承认它是尽力而为。
2. ⭐⭐⭐ **总监** — **AZ 环走确定性单向,容量紧张时把整波挤回同一个 AZ**(§5.6,本轮新开 issue)。
   **W39 现在就有 3/4 台在 `2c`,而六个载体项各只有 2 粒** ⇒ 一次 `2c` 事件会把五项打到 ≤1。
3. ⭐⭐⭐ **全体** — **W39 不可与 W38 / W35 / W37 并池**;收割照 `W39_wave.json:harvest_obligations` **十二条**,
   其中 **SS-DN.6 两条是本波第一次真的咬到**(`outlatch` 的 (a) 两分层分开登记、反号读 **4(i-b)** 噪声;不许跨波比)。
4. ⭐⭐ **总监** — **GH #408 仍零评论未裁**,**连续第六波被容量咬**(第一次咬在起飞)。
5. ⭐⭐ **总监** — **GH #418 未裁**(并池判据本身要返修);**GH #420 未裁**(汇总块少一个桶,本轮原样复发)。
6. ⭐⭐ **总监** — **GH #285 第二十五轮未裁**:`popcount 4` 层仍空、`popcount 3` **45 → 41**、
   最优层组合数 **20 → 5**、**连续第三波卡在 `slots = 12` 下限**。**削得比上一轮快。**
7. ⭐ **录像组** — **GH #423**(SS-DK.3 负控)**仍挂着**;`slotarb` 的条件 (a) 在帧级检测器做出来之前**不可买**,
   再发波也买不到。
8. ⭐ **总监/协同组** — `strategy-31/32` **连续第三轮不搭任何波**(铁律 9,第三次交回);
   `strategy-37`(roshdist)/ `strategy-38`(ckpush)**路由未裁**。
9. ⭐ **总监/英雄组** — `test_incoming_damage_callsite_census.lua` 的 43→44(与上一轮同一条);
   `tests/test_detector_source_constants.py` 红;`test_rc_wrapper.py` / `test_selfcheck_lua_leg.py` **UNCERTIFIABLE**
   (**而汇总块打 `none`,即 GH #420**)。
10. ⭐ 存量:**#388 的 W34 103 份 `.dem` 2026-09-22 到期(距今 20 天)** / #413 / #412 / #402 / #395 / #375 /
    按需常数第十轮 / #317 五条 / `strategy-5b` 第十六轮 / #290 / #313 / #207 / #271 / #364 / #398 / #399 /
    #382 / #383 / #218 / #367 / #295 / #329 / #321。

**下一轮本台 = 收割 W39**(四台预计 `~16:1x–16:3xZ` 自毁)。
闸 (i) 对下一次发波解锁于 **`2026-09-02T21:31:51Z`**。
**收割三件必做**:(a) `machines[].status_code` 走 **SIR** 通道、**SIR 词汇**(GH #412);
(b) 暖场 `script_version` 断言 = `1a13b9df…`;
(c) **若这一波需要按需补跑,它的 `StateReason.Code` 必须在那台机器终止后 ~1h 内落盘**
(§三:按需机没有 SIR,过期就永久没有了)。

---

## 本轮 token 用量(铁律 8)

```
TOKENS total_in=12,321,779 out=75,155 turns=84
```
(`python3 tools/agent/token_usage.py`,**TOK_EXIT=0**;这是「到统计时刻为止」的数字,
统计后的收尾回合不计入。)
