# 批测台 2026-08-30T06:19Z —— 发 W28(45-id 家族第二波)。三闸全过,四台 spot 全中,零降级。

**一句话**:W28 已于 `06:18:58Z–06:19:11Z` 起飞(种子 `1850/1938/2130/2142`,四个 AZ,全 spot)。
**它与 W27 同串同码 ⇒ 可并池** —— 收割后就是交棒里点名的那份 **45-id 家族 8 粒读数**。
本轮闸 (i) 的 `date -u` 守卫**真的拦了一次**:`06:16:43Z` 的第一次调用被自己的守卫按 exit 9 退回。

---

## 一、开工自检(前台跑,不接管道、不加 `timeout`、不用 `nohup &`)

- `unlanded_commits` **OK**;
- **cadence 1 finding**:**director `08-29T04:08Z → 16:28Z` 12.3h,连续第五轮同一个洞**;
- 未裁请求 **none / none**(开放 42;`UNKNOWN STATUS` 5 条按 GH #317 计为开放);
- `ORPHAN_PROPOSAL` **none**(`ROWLESS` 3 条:`aimguard`/`campvoid`/`campexit`,信息项);
- 过期等待 **无**;`stable-v1`/`stable-v2` **2/2 OK**(`EXISTS`/`PINNED`/`SHIPPED` 全绿);
- trunk python **53 passed / 0 failed / 0 uncertifiable**;fast Lua **34 个 detector 文件 0 失败**
  (⚠️ 工具自己声明这是 FAST SUBSET,不覆盖未打标签的测试);
- `worst exit 3`。**归因照抄工具自己那一节**(GH #267:这句话曾被手工归因写错过 22 小时):
  `FINDINGS (exit 3) = cadence`、`UNCERTIFIABLE (exit 2) = none`。
- `arm_push_gate.sh`:`core.hooksPath=.githooks`,**already set**。

## 二、成本

| 项 | 读数 |
|---|---|
| 开工 running/pending | **空**(W27 四台已于 `01:02–01:14Z` 自毁) |
| MTD(budgets,免费) | **$67.374** |
| CE 复核($0.01) | **$67.3735002094** —— **逐位一致,连续第二十七轮** |
| 快照时刻 | `2026-08-29T22:17:33Z` |

⚠️ **快照连续第三轮没刷新**:它与上一轮、上上一轮是**同一张**,而 **W27 整段(`00:18–01:14Z`)都在它之后**
⇒ 那 ~$0.80 **一分没进裸 MTD**。章程 (甲) 的算术照旧不省:

```
围栏 = 67.374 + W27 0.80 + W28 0.80 = $68.97  ≤ $80  ✓(余量 $11.03)
最坏(两波全程降级按需 $2.15) = $71.67       ≤ $80  ✓
```

刹车 $90 / owner 线 $100 **均未接近**(余量 $21.03 / $31.03);**本轮不跨任何新告警档**。
本轮现金 = CE $0.01 + EC2 ~$0.80 + 几分之一美分的 S3 GET。

## 三、收割:本轮无 verdict 可收(空过,不是跳过)

S3 `soak/` 最新仍是 **W27 的四个 run 前缀**(`spot_20260830_0018*`/`0019*`),自上一轮 `03:30Z` 全量重算之后
**零新增语料**。W27 的读数上一轮已封口(gpm **+29.92**、`comps_better` **3/4**、212 计分局、`thin_arm_seeds []`),
本轮不重复登记。**`recover_verdict.py` 未调用。**

## 四、发波前的四道自查

### 4.1 `seed_roster_index --build` —— 这条新纪律第一次接受实测,而它是个 no-op

```
[build] 277 run prefixes in S3, 277 already indexed, 0 to scan
[build] nothing new; index unchanged
```

**这正是上一轮那条纪律想要的形状**,值得写下来免得下一轮误读成「这条纪律没用」:
上一轮(收割轮)已经把 build 做过了,W27 那四粒当时就折进了索引(139 → 143 粒 / 277 run)。
所以本轮发波前的 build **本该是 no-op** —— 它是**冗余的那一道**,不是失效的那一道。
⚠️ **仍然不能从发波前动作里去掉**:上一轮把 build 挪到收割轮做的理由就是「让刷新与用它选种不再是同一个动作」,
而发波前这一道是兜底。本轮的 no-op 是**两道都在**时的正常读数。

### 4.2 选种:窗口 `[1801,2200]`,**本轮没有复发组合学耗尽**

```
range 1801-2200 | unused seeds 396 | rostered 352 | distinct masks 46
  supply cm 94 | lion 114 | od 72 | sk 128 | sb 82 | zuus 165   (/352)
  valid mask-combos (>= 2 carriers per term): 1438
BEST carrier slots=16 seeds=[1850, 1938, 2130, 2142]   (四粒全是 pop-4)
  max popcount in window: 4
```

- **1438 组合法解、最大 popcount 仍是 4** ⇒ 上一轮在 `[1600,1800]` 上记下的耗尽机制**这一轮没有复发**。
- ⚠️ **但机制没有消失**:本轮照旧挑走了「载体最富的四粒」(四个 pop-4),**每一波都在削掉窗口 popcount 最高的那一层**。
  窗口仍然是**必须跟着走的参数**,不是常量。
- 四粒**从未用过**:刷新后的索引对 `1850/1938/2130/2142` 一律 `no banked games`。

### 4.3 两道官方门

- **载体门** `seed_draft.py --assert-carrier-from-arm` **exit 0**(`ids=8 seeds=4`)。
  六 term 由 `carrier_terms.py` 从 **45-id 串机械推导**(8 hero-scoped / 37 generic / **0 unresolved**)——
  与 W27 **同一个 term 集**,如预期(`fieldsip` 是 generic)。
  每 term 载体:`skeleton_king` **4(FULL)**、`crystal_maiden`/`obsidian_destroyer` 各 **3**、
  `lion`/`spirit_breaker`/`zuus` 各 **2** ⇒ **16 个载体槽**。
  ⚠️ **诚实边界第六轮保留:「每 term ≥2」是本台自律不是门,官方门只要求 ≥1,GH #285 仍未裁。**
- **接线门** `check_armed_wiring.py --cand <45 串> --ref HEAD` **exit 0**:`all 45 armed ids wired on HEAD`。

### 4.4 `--rec-slots`:按登记分支**第五次**退回 1(GH #308 仍未裁)

重裁截止就是本波的闸 `06:18:47Z`。发波前核到:本 issue `get_comments` **只有本台自己的四条**
(`18:26:56Z`、`21:20:08Z`、`00:27:10Z`、`03:25:00Z`),**无总监评论**;无总监提交碰它。
⇒ **未裁**,按章程「不许事后改判据」执行路 **(A) = 1**。**帧通道 1/16,第五波。**
代价照旧点名:未裁的实测代价 ≈ **121 个录像/波**(W25 `rec_slots 12` 传 145 个,W26/W27 退回 1 只传 24/30 个)。

## 五、发波:配置、市场、放置

| seed | az | instance | SIR | run_id 尾 | 起飞 |
|---|---|---|---|---|---|
| 1850 | us-west-2a | `i-0f37ed0041eaca3b3` | `sir-6nkfha3n` | `990f5c` | `06:18:58Z` |
| 1938 | us-west-2b | `i-06d88875005df1130` | `sir-h1qfk8dq` | `90463d` | `06:19:03Z` |
| 2130 | us-west-2c | `i-06676c7c8de85d8f8` | `sir-b1mzkeyq` | `e706a3` | `06:19:07Z` |
| 2142 | us-west-2d | `i-0531992fa655fc6a8` | `sir-93n7ghhq` | `8fffe9` | `06:19:11Z` |

- **阶梯停在第 1 级**:`c6i.4xlarge` spot **四台全中** —— 无 `InsufficientInstanceCapacity`、
  无 `re-aiming inside the ring`、无 `AZ RING EXHAUSTED` ⇒ **未到 `c6a`、更未到 `--on-demand`,零降级**。
  **owner 的「Spot 优先」(GH #158)连续第六波原样成立。**
- **证据以 `describe-instances` 为准,不用 `spot_` 前缀**:四台全 **`spot`** + `c6i.4xlarge` + `running`,
  **四个 AZ 互不相同**;四条 SIR 全 **`active`/`fulfilled`** 且 `LaunchedAvailabilityZone` 与请求一一对上;
  四个 `soak-run` 尾 token 两两不同(#98 `RUN_TOKEN` 生效)。
- **#252 通过**;**#256 第六波拿满**(一次 AZ 事件的暴露面 = 1 粒);
  **#282 第六波四条 `requested == actual`、零 `PLACEMENT MISMATCH`**。
- **配置**:pin **显式 40 位 `f015321ea2926ace44aa8a003b3d165122ededf2`**(**不写 `main`**),
  `git ls-remote origin main` == 本地 `origin/main` == 该 sha,**且在发波那个 shell 块里又核了一次**;
  `--slots 16 --rec-slots 1 --hours 2`、`--validate "<45-id 串> <seed> --games 12"`;`--dry-run` 先行且干净。
- **未传 `--on-demand`/`--cand-ref`、未用任何 `--allow-*`/`--min-arm-depth`/`--allow-short-watchdog`
  ⇒ 本轮没有任何一行「这是 SKIP 不是 pass」要抄。** 波次预算闸未拒发。

### ⭐ 闸 (i) 的 `date -u` 守卫本轮真的拦了一次

守卫**写在发波那个 shell 块里**(不是写进推算)。第一次调用 `06:16:43Z` 被自己退回:

```
REFUSED: gate (i) not yet unlocked (2026-08-30T06:16:43Z < 06:18:47Z)   exit 9
```

第二次 `06:18:58Z` PASS。**这条纪律至此拦过两次早发**(上一次在 W26 那轮),保留。

### 三闸逐条

- **(i) ✓** W27 起飞 `00:18:47Z` + 6h = `06:18:47Z`,发波时 `06:18:58Z`(守卫见上)。
- **(ii) ✓ 但只由第二个分支满足,这一点要写清楚**:`bots/`/`game/` 与 W27 的 pin **逐字节相同**
  (`git diff 5d9b9927..f015321 -- bots/ game/` 空),`test_set.md` 第 2 行也**逐字节相同**
  ⇒ **第一分支(有新代码或新串)不成立**;当前树+测试集的累计种子数 = **4(仅 W27)< 8**,
  章程 (ii) 是「**或**」⇒ 由第二分支满足。
  ⚠️ 顺带核清一处**可能被误读的提交**:总监 `04:12Z` 的 `837bc31e` 确实碰了 `test_set.md`,
  但它做的是 GH #317(queue status 词汇),**不是 arm 串**。
- **(iii) ✓** $68.97 ≤ $80(最坏 $71.67 也过)。

### ⭐ 排波事实(与上两轮相反,这次是**可以**并池)

W27 与 W28:**同一个 45-id / 396 字节 arm 串**,**同一棵逐字节相同的 `bots/`/`game/`**
⇒ **可并池**。收割 W28 之后,45-id 家族就有交棒里点名的那份 **8 粒可池化读数**。
⚠️ **它与 W25+W26 那份 8 粒池必须分开登记** —— 那份是 **44-id / 387 字节**串,已于 W26 封口。

## 六、局数(铁律 7)

- **(a) 上一波(W27)最终有效局数**(上一轮实测,原样转载):落盘 **235** / 计分 **212** /
  `unfinished 0` / `winner_by engine_natural 235/235` / 暖场 23;
  `min_arm_depth 8`、`thin_arm_seeds []`(**无种子被 THIN-ARM 排除,无人要点名**);
  四台全 `instance-terminated-by-user`(自毁,零抢占),`.dem` 6/8/8/8 = **30 个**。
  per-seed:1827 ab26/ba16(`arm_depth 19.81`)、1828 ab39/ba19(`25.55`)、
  1835 ab40/ba15(`21.82`)、2103 ab38/ba19(`25.33`)。
- **(b) W28 本轮预期**:4 粒 × 2 腿 × `--games 12` = **96 局带戳**,外加每台暖场若干
  (逐字节同码的 W25/W26/W27 分别落盘 196/199/235)。
  发波后 ~1 分钟四个 run 前缀尚无 `analysis.json`,**这是开机中(≈12 分钟)的预期值,不是异常**。

## 七、泄漏检查(两次)

- **开工**:running/pending **空**;
- **收尾** `--leak-only`:**4 台在跑,全部本轮自发,instance id 逐个对上,没有第五台 ⇒ 零泄漏**。
  ⚠️ 发波轮的判据不是「0 台在跑」,是「**在跑台数 == 本轮自发台数且 id 逐个对上**」。

## 八、交棒

1. **⭐⭐ 下一轮本台 —— 收割 W28**(四个 run 前缀见 §五;先分 run 下载再带前缀合并,**从不 flatten**;
   `mean` 旁抄 `min_arm_depth`/`thin_arm_seeds`;`W28_wave.json` 待回填)。
   **收完立刻做 W27+W28 的 8 粒并池读数** —— 同串同码,这是本家族第一份可池化读数;
   **与 W25+W26 那份 44-id 8 粒池分开登记**。**W29 的 6h 闸解锁 `2026-08-30T12:18:58Z`。**
2. **⭐⭐ 总监 —— 两份读数都备齐可裁**:44-id 串的 8 粒池(gpm +32.25、6/8)与 45-id 家族的
   W27 首份(gpm +29.92、3/4、留一法全正、零排除)。`suggested: promote` **只是提示**,
   三条件的 (a) 帧核验不在本台。
3. **⭐⭐ 总监 —— GH #308 重裁第五次落空**,W28 按 (A) 退回 `rec_slots 1`、帧通道 1/16,
   **代价第五次点名**;owner P1/P2 的条件 (a) 继续等。W29 若仍不裁继续 (A)。
4. **⭐ 录像组/英雄组 —— W28 载体供给**(按草案):sk 4/4、cm 3、od 3、lion/sb/zuus 各 2;
   **但帧通道仍 1/16 ⇒ `hero-22`/`hero-23`/`strategy-23` 的前置门取帧仍从 W25 取**(`rec_slots 12`、145 个录像)。
5. **⭐ 总监 —— 自检连续第五轮报你 `04:08Z→16:28Z` 12.3h 无工作单元**,
   而 #308/#313/#321/#299/#298/#291/#285 都等你裁。
6. 存量:**#207 `zusstatic` 第二十九波 armed**;**#218 后续第二十五轮**;
   **#282 连续第六波零 MISMATCH,第七次建议关闭**;**#295 建议关闭**;**#285 第八轮催**;
   **#321 待裁**;#313/#290/#291/#298/#299 照旧;
   #217/#211/#225/#180/#171/#200/#181/载体门 PARTIAL/`stable-v*` tag/`campdanger`/§BL.4/#75 照旧。

## 九、铁律自查

- **铁律 6**:`bots/`/`game/` **一行未改**(本轮只写报告/章程/波次记录/queue);
  `core.hooksPath` 已上膛;**未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行**;
  动态半(#124,~100min)**未跑且不声称**。
- **铁律 6 顺序条款(GH #290)**:本轮**先 push 再发表带引用的评论**,草稿过 `claim_precheck.sh`。
- **铁律 11**:**MCP 未触发 `requires approval`**(`issue_read` 正常返回)。
- **铁律 1**:本轮花钱在围栏内,三闸逐条留痕。

## 十、token 用量

`TOKENS total_in=4,472,910 out=32,036 turns=39`

(注:这是"到统计时刻为止"的数字,统计之后的收尾回合——push 与两条 GitHub 追评——不计入。)

## 十一、本轮已发表的评论(先 push 后发表,GH #290)

| issue | 评论 id | 内容 |
|---|---|---|
| #308 | `5467138095` | 重裁第五次落空;新信息 = **W28 与 W27 可并池 ⇒ 下一轮 (C) 的语料第一次变厚**,但两份都是 `rec_slots 1` 下买的,加厚的是对照腿不是帧证据 |
| #282 | `5467139612` | 第六波零 MISMATCH;新信息 = 本波四台是在闸守卫**拒绝过一次之后**发出的,重试路径没丢 AZ 约束(正是 #256 的立案形状);第七次建议关闭,并请总监裁定是否需要等一次真实容量失败 |

两条草稿发表前 `claim_precheck.sh` 均 **exit 0 / `clean` / 本地领先 `origin/main` **0** 个 commit**
(`CITATIONS sources 1 refs 563 ... resolved on trunk 3 refused 0`)。
push 顺序照 GH #290 执行:**先 `git push` 再发表**,两次 push 各跑一次铁律 6 静态门,
**`luacheck bots game: 0 warnings` 两次全绿,未用 `RULE6_BYPASS`**。
