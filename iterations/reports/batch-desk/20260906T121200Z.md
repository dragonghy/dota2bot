# 批测台 2026-09-06T12:12Z — 不发波轮:外来负载停了,而 $50 已经越线;闸 (iii) 的 CERTIFIED 是本轮的头号发现

**一句话**:上一轮预言的「$50 会被别人的算力撞破」本轮**已成事实**(诚实 MTD **`$54.673`**,
其中 **57% 不是批测**);⭐ **但外来那条作业本轮停了**(两台 `r7a.16xlarge` 都已 `closed`,
末台 `09:58:36Z`,账号内**零实例**);⛔ **而 `wave_fence.py` 本轮打 `CLEAR` 并且把 `pending $0.000`
标成 `CERTIFIED ... a reading, not a default`** —— **恰恰因为那两台已经终止了**。
上一轮交给总监的修法(「把全账号 running 实例 × 现货价计进 pending」)**在本轮救不了这个 CLEAR**,
因为本轮该被计进去的钱**没有一分是 running 的**。

---

## 一、自检(铁律 10)

⚠️ **开工的自检命令第 9 次(同形第 27 次)误用管道**,脚本逐字自拒:
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked`,
并自陈「**evidence discipline 3;已复发 5 次,每次都是当轮第一条命令**」。
**那个 `EXIT=2` 是 REFUSED 不是通过。** 上一轮交棒 ④ 写的是「重定向不管道、放后台给足 ≥45min」——
**管道那一半本轮又犯了**;⭐ **另一半照办了**:改重定向 + 后台(未设 `timeout`,不重演上一轮的 `EXIT=124`),
真码见第八节收尾补记(**读脚本自打的 `selfcheck worst exit` 那一行,不读 harness 报的码**)。

⭐ **「工作树红 = main 红」这个结构性论证本轮同样成立**:
`git diff --stat origin/main -- bots/ game/ tests/ tools/` 为空(本会话改动全在 `iterations/` 下)
⇒ 两个命题在本轮是同一个,**不需要 `git stash`**。

## 二、⭐⭐⭐ 成本:$50 已经越线,而越线的钱大部分不是批测的

### 2.1 工具的读数(原样,不改写)

`AWS_SETUP_EXIT=0`(`arn:aws:iam::581436534924:user/dota2bot-agent`),`COSTS_EXIT=0`。
`check_costs.sh` ⇒ 九月 MTD **`$39.328`**(免费 `budgets`),
**`budget refreshed 2026-09-06T06:02:46Z`** —— ⚠️ **与上一轮(09:12Z)逐位相同、刷新时点也逐位相同**
⇒ **上一轮交棒 ① 的判据落在「没翻过 09-06」那一侧**,故按它指定的方法**重建一遍再判**。
因 ≥`$35` 脚本自动付 `$0.01` 用 CE 复核,逐位复现 `39.3278311289`。

### 2.2 ⛔ 头号发现:`CLEAR` 这次是被 `CERTIFIED` 背书的,而背书的机制正是它的盲区

裸跑工具(`wave_fence.py --planned 1.10`,`FENCE_TOOL_EXIT=0`)逐字打出:

```
budget filters   : none  <- so every instance in the account does land in this budget, whoever owns it
accrual check    : CERTIFIED (0 accruing instances account-wide, read this run) -- pending $0.000 is a reading, not a default.
actual (MTD)     : $39.328   <- re-read this run, never cached
pending waves    : $0.000   (launched, may not be in MTD yet)
projected total  : $40.428
headroom         : $9.572 after this wave
WAVE_FENCE: CLEAR (exit 0)
```

⭐⭐⭐ **这一行 `CERTIFIED (0 accruing instances account-wide, read this run)` 是真的,而结论是错的。**
Ruling 4 要求「pending 的那个零必须可证」,工具证它的办法是**读此刻还在 accruing 的实例**;
本轮账号内确实**一台都没有**(第五节)。⛔ **但今天已经花掉、尚未落账的 `$15.345`
全部来自已经 `terminated` 的机器** —— 钱**先发生、再落账**,而实例**先消失**。
⇒ **「零台在跑」与「零元未落账」是两个命题,工具把前者当后者的证据。**

⚠️ **这直接反驳上一轮交给总监的那条修法建议**(09:12Z 交棒 ②:
「建议 `wave_fence.py` 把全账号 running 实例 × 同 AZ 实测现货价 × 已跑机时计入 pending waves」)——
那条修法**在上一轮成立**(当时外来机确实在跑),**在本轮失效**:本轮该被计进去的
`$14.245` 属于两台**已终止**的机器,running 集合是空的。
⇒ **正确的量不是「还在跑的机时」,是「本账期内已发生但尚未出现在 CE 里的机时」**,
它的证据不在 `describe-instances` 而在 **spot instance request 的 `CreateTime`/`Status.UpdateTime`**
(SIR 行在实例消失后仍留了一段时间——本轮就是靠它拿到的)。
⚠️ **诚实边界:SIR 行也会过期**,所以这条修法同样有窗口(与 GH #556 同族)。

### 2.3 把今天补回去(实测机时 × 实测同 AZ 现货价,不是估)

`describe-spot-instance-requests`(全账号,`us-west-2`,**2 条**,两条都已 `closed`):

| SIR | 实例 | 机型/AZ | Create | Status.UpdateTime | `Status.Code` |
|---|---|---|---|---|---|
| `sir-rmaqhkvp` | `i-0a7d4c7fbeb3a6712` | `r7a.16xlarge` / `us-west-2d` | `02:10:35Z` | `08:25:24Z` | `instance-terminated-by-user` |
| `sir-ctifj6bp` | `i-0114b249d00ad956e` | `r7a.16xlarge` / `us-west-2d` | `08:24:02Z` | `09:58:36Z` | `instance-terminated-by-user` |

同 AZ 现货价实测(`describe-spot-price-history`,`r7a.16xlarge`/`us-west-2d`):
`02:00Z` 与 `06:00Z` 两档都是 **`$1.8209/h`**,`10:00Z` 起 `$1.8140/h`
(两台都在 `09:58Z` 前结束 ⇒ **全程用 `$1.8209`**)。

- `i-0a7d4c7fbeb3a6712`:**6.2469 h** × `$1.8209` = **`$11.3751`**
- `i-0114b249d00ad956e`:**1.5761 h** × `$1.8209` = **`$2.8699`**
- 外来小计 = **`$14.2450`**;加本台 W50(章程常数 `$1.10`,GH #454)⇒ **pending `$15.345`**

⇒ **诚实 MTD ≈ `$39.328 + $15.345` = `$54.673`** ⇒ **`$50` 档已越线约 `$4.67`**。

把这个数喂回同一个工具(**不手判,让闸自己说**),`FENCE_HONEST_EXIT=3`:

```
pending waves    : $15.345
projected total  : $55.773
operative ceiling: $50.00   = min(fence, brake)
WAVE_FENCE: THROTTLED (exit 3) -- do not launch. Headroom was $-4.673, this wave needs $1.100.
```

⇒ **本轮不发波,决定性理由是闸 (iii)。**

### 2.4 归因:付费 CE 一次(`$0.01`),按机型拆开,逐位对得上

`ce get-cost-and-usage`(09-01→09-07,MONTHLY × `INSTANCE_TYPE`):

| 机型 | 金额 | 归属 |
|---|---|---|
| `c6i.4xlarge` | **`$18.5054`** | **本台**(批测机型) |
| `c7a.16xlarge` | **`$17.1086`** | **外来**(`final-table-trainer`,09-05 那台) |
| `NoInstanceType` | `$3.7138` | 存储/传输等,未拆 |
| **合计** | **`$39.3278`** | 与 `budgets` **逐位相同 ⇒ 拆分完备** |

加上未落账的 09-06:外来 `r7a` `$14.245`、本台 W50 `$1.10` ⇒ **诚实全月**:

- **本台批测 ≈ `$19.61`(36%)**
- **外来 `final-table-trainer` ≈ `$31.35`(57%)** ← `c7a $17.109` + `r7a $14.245`
- 未拆 `NoInstanceType` `$3.71`(7%)

⇒ **越线的那笔钱里,大头不是批测的**;而 owner 收到的第一封 `$50` ACTUAL 告警邮件
**抬头仍是 `dota2bot-batch`**(工具自打 `budget filters : none`,根因 GH #515)。

### 2.5 ⭐ 好消息也要登记:外来那条作业本轮停了

上一轮的算术是「`$1.8209/h` = `$43.7/天`,一天多撞 `$100`」。**本轮那个前提不再成立**:
两台 `r7a` 都已 `closed`(末台 `09:58:36Z`,距本轮读数 ~2.3h),
账号内 `describe-instances` **零 Reservation**(第五节)⇒ **外来燃烧率现在是 `$0/h`**。
⚠️ **但这不撤销已经花掉的钱**:`$50` 是**累计**门槛不是速率门槛,已越的 `$4.67` 不会退回来。
⚠️ **也不保证不复发**:09-06 那两台是「上一台终止 33 秒后起下一台」的修复重试循环
(上一轮已登记),**本轮只是这一轮没有第三台**。

## 三、闸 (i):UNLOCKED —— 所以本轮的阻塞**确实**只是钱

`wave_throttle.py`(**不带管道**,`THROTTLE_EXIT=0`):锚点 W50 末台 `2026-09-06T03:20:34Z`
⇒ `unlock 2026-09-06T09:20:34Z`,`now 12:16:26Z`,**`margin +10552s past unlock`**,
**`UNLOCKED (exit 0)`**(另打了 Y1 那条候选并按 ruling 3 取最晚者)。
⇒ ⭐ **与上两轮不同:节奏闸本轮不挡了。** 本轮不发波**只因为闸 (iii)**,
即 **`campgrade` 独占波(#480/#241)连续第五轮欠着,而欠的理由连续第二轮是钱不是遗忘**。

## 四、收割:本轮无新数据(且这是查过的,不是默认的)

S3 `soak/` 最新四个前缀就是 W50 那四台
(`spot_20260906_0320*_b73358bc0f764fd4a22171da2ae4556a0d5d38f7_{295740,e56ba0,f2182d,eff51c}`),
**已由 06:14Z 那轮 4/4 全量收割完毕**(187 局计分、gpm `+13.76`、comps 2/4);
`validation/` 最新对象是 07-23 的历史物。本轮零实例、零新 run ⇒ **无收割义务,不重复收割**。

## 五、泄漏检查:dota2bot 侧干净,而本轮的证据强度比往轮高一档

⭐ **本轮做的是全账号、正确区域、不带过滤器的普查**(GH #515 订正后的写法,防 09-06 那次
「区域决定了问题」与 09-05 那次「过滤器决定了问题」):

- `describe-instances --region us-west-2`(`running,pending`)⇒ **空**;
- ⭐ **再放宽到全状态** ⇒ `Reservations` **0 条** —— **账号内一台实例都没有**。
- ⚠️ **空输出的处置**:先用「全状态」这条**更宽的查询**验证空不是查询坏了
  (若查询坏了,全状态那条也该报错而不是返回 0 条),再采信。
- 常驻仍只有 AMI `ami-0a990a26d89c66547`。

⇒ **本台无泄漏、W50 四台早已自终**;⇒ **外来那台也确实不在了**
(⚠️ 这一次「已不在」有 SIR 的 `closed` + `Status.UpdateTime` 撑着,
**不是** 09-05 那次靠错过滤器读出来的「已不在」)。
⛔ **本台对外来资源未做任何处置也不打算做**(它们本轮已自行终止;即便还在跑,
终止别项目的算力破坏性且越权,归 owner)。

## 六、交棒

1. ⭐⭐⭐ **owner —— `$50` 已经越线(诚实 `$54.673`),而 57% 不是批测的。** 两条路都不改本台工具:
   给 `dota2bot-batch` 加 cost filter(⚠️ 走 `Project` 标签路线**必须先给批测机器补 `Project` 标签**,
   否则 fence 恒读 0,**一个永远打 CLEAR 的闸比现在更坏**),或给 `final-table-trainer` 单开预算。
   ⭐ **本轮的新事实**:外来负载**已停**(第 2.5 节)⇒ 决策不再紧急,但**已越的线不会自己退回**。
   **已推手机通知。**
2. ⭐⭐⭐ **总监 —— `wave_fence.py` 的 accrual check 有一个结构性盲区,且它上一轮拿到的修法建议堵不住。**
   见第 2.2 节:`CERTIFIED (0 accruing instances)` 本轮为真而结论为假;
   正确的量是**本账期已发生但未落账的机时**(证据在 SIR 不在 `describe-instances`),
   不是「还在跑的机时」。本轮开 issue(编号见第八节)。
3. ⭐⭐ **总监(承前两轮未结)** —— W50 的 promote/reject:gpm `+13.76` / comps 2/4,
   **均值由单粒 `6072` 扛着(去掉后 `−21.53`)**,请连离散度一起裁;
   winrate 第九次 `DEGENERATE`(W50 是 `engine_natural 211/211`,**独立于金钱**)该结案。
4. ⭐⭐ **本台 —— `campgrade` 独占波(#480/#241)连续第五轮欠着**,
   ⭐ **本轮闸 (i) 已 UNLOCKED ⇒ 唯一阻塞是闸 (iii)**;预算一拆开或 owner 批准越线,
   **下一轮第一件事就是它**。
5. ⭐ **总监 —— `$1.10` 常数(GH #454)本轮无新数据点**(未发波);
   (乙2) 机时判据未触发。

## 七、下一轮本台

1. **先跑 `check_costs.sh` 看 `budget refreshed` 有没有翻过 09-06** —— 翻过了就**用工具的数**;
   没翻过就**照第 2.3 节的方法重建**(全账号 SIR + 同 AZ `describe-spot-price-history`)。
   ⚠️ **重建时查的是 SIR 不是 running 实例**(第 2.2 节:本轮的钱全在已终止的机器上)。
2. 重算后仍 > `$50` **且** owner 未拆预算 / 未批准 ⇒ **继续不发波**,
   **不要因为工具打 `CLEAR` 就发**(本轮它就打了 `CLEAR`)。
3. 落在 `$50` 内、或交棒 1 落地 ⇒ **第一件事发 `campgrade` 独占波**(已欠五轮);
   发波当轮就把 `machines[].sir` / `create` / `launched_at` 写进波次记录(GH #556 / #544),
   并在**写完记录之后**复跑 `test_wave_gate_keys.py` 与 `test_wave_throttle.py`。
4. 发波前**最后一条命令**仍是 `wave_throttle.py`(**动作时点跑,不是决定时点**)。
5. ⚠️ **开工第一条命令重定向不管道、放后台给足 ≥45min** —— 管道那一坑**本轮第 9 次**踩。

## 八、铁律 6 / 11 与收尾读数

- `bots/` / `game/` / `tests/` / `tools/` **本会话一行未改**(改动 = `iterations/` 下的报告与本节)。
- **铁律 11**:全程无 `requires approval`、**无空转等待**;唯一的「等」是自检在后台跑,
  本轮**没有为它停下**。
- CE 付费调用本轮 **2 次**(`$0.02`):一次是 `check_costs.sh` 因 ≥`$35` 自动复核,
  一次是第 2.4 节按机型归因 —— **章程允许的「确认贴近围栏的读数」**,
  而 owner 的决策正压在归因上。
- 动态半(GH #124)**未跑不声称**。

## 九、收尾补记(自检真码 + 铁律 6 读数 + 发表)

### 9.1 自检真码 —— ⚠️ 读脚本自打的那一行,不读 harness 报的码

后台任务完成通知说 **`exit code 0`**,⛔ **那是末尾 `echo` 的码不是自检的码**(章程点名过的坑)。
脚本自己打的是:

```
legs run              : 10
FINDINGS (exit 3)     : cadence queue-rulings owed-executions
UNCERTIFIABLE (exit 2): trunk-red(python)
NOT RUN (inside a leg): tests/test_rc_wrapper.py tests/test_selfcheck_lua_leg.py
selfcheck worst exit: 3
```

**`3` = 有发现,不是没跑成。** 十条腿全部真跑过。逐条:

- ⛔ **`trunk-red(python)` 本轮 `UNCERTIFIABLE`** ⇒ **python 侧 trunk 红不红,本台本轮答不了**。
  ⚠️ 照抄腿自己的边界:`This line is NOT a pass, and it is NOT evidence that trunk is red`。
  根因是 `5a0` 腿超预算:`84 file(s) ... in 120.1s (budget 120s)`,连带 4 条 `UNC` 空过
  ——⭐ 工具自己写明「墙钟分不开『集合变大』与『容器变慢』,要跨轮读 `NOTE` 行」。
- ⭐ **`trunk health (fast Lua detectors)`:`84 tagged detector file(s), 0 failures`** ⇒ **Lua 侧绿**;
  ⚠️ 边界照抄:**`FAST SUBSET, not the full suite`**,未打标签的测试不在覆盖内。
- `NOT RUN` 那两个的理由逐字都是 **`no lua5.1 on PATH`**(GH #171 同族,时序产物:
  自检在更后面的腿里才装解释器)——**登记,不自改 harness**。
- `cadence`:**2 条 `GAP cadence strategy`** ⇒ **不是本台**。
- `owed-executions`:registry 共 6 行,点名本台的仍是
  **`campgrade_wave_type_premise_reading`(GH #480 / #241)—— 连续第五轮欠着**,
  ⭐ 而本轮欠的理由是**钱**(闸 (iii)),不是遗忘:闸 (i) 本轮已 `UNLOCKED`(第三节)。
- `queue-rulings`:**`RIDESHARE (§BB.4: rule this round): 1`** ——
  ⚠️ **本台不裁,但连续第三轮指出同一处规则冲突**:owner 的 **P4.2 已废止并压过 §BB.4**
  (冻结期唯一合法裁定是 `FROZEN-HOLD`)⇒ **工具还在引一条已被废止的条款**,交总监。

⭐ 按第一节的结构性论证(`git diff --stat origin/main -- bots/ game/ tests/ tools/` 为空),
上面这些「工作树」读数**在本轮就是 `main` 的读数**。

### 9.2 铁律 6 读数

静态门两次 `git push` **各自跑了同一个 gate 并各自自打**
**`GATE_EXIT=0  CLEAN (iron rule 6 static half passed)`**
(`luacheck bots game: 0 warnings`;门自己装的 luacheck:`installed luacheck (it was missing; apt package lua-check, bounded)`);
推送门 `ARM_HOOK_EXIT=0`;**未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行**。
**`PUSH1_EXIT=0`**(`claude/great-sagan-awlwnp`,新分支)/
**`PUSH2_EXIT=0`**(`0244fe68..25bc149f  HEAD -> main`,**未被拒,无需 rebase**)。
动态半(GH #124)**未跑不声称**。

### 9.3 发表(GH #290 顺序:先 push 再发)

两处发表**都在 `25bc149f` push 之后**,发表前跑 `claim_precheck.sh`:
**`PRECHECK_EXIT=0`** —— `local commits not on origin/main: 0`,
`paths cited 2 / resolved on trunk 2 / refused 0`,`clean` / `OK to publish`。

- **GH #565 新开 `[harness]`**(第 2.2 节:`wave_fence.py` 的 accrual check 把「零台在跑」
  证成「零元未落账」;**并说明上一轮交给总监的修法建议本轮堵不住**;
  验收 = 下一轮 `budget refreshed` 仍未翻天时,工具应自己计入当天已终止实例的机时,
  取不到证据时应打 **`UNCERTIFIABLE` 而不是 `CERTIFIED`**)。
- **GH #515 追评**(comment `5559180518`:`$50` 已越线的全套算术、按机型的归因表、
  ⭐ **外来负载已停**的 SIR 证据、给 owner 的两条路)。

### 9.4 收尾泄漏复检

**`LEAK_EXIT=0`** —— 本轮未发波,`describe-instances` **全状态 0 条 Reservation**
(⇒ 既无本台泄漏,也无外来机在跑),常驻仍只有 AMI `ami-0a990a26d89c66547`。
⚠️ **不发波轮要的是「本台零台在跑」,而本轮连账号侧也是零** —— 这是 09-05 以来第一次。

## 十、本轮 token

`TOKENS total_in=5,565,112 out=36,026 turns=45`(到统计时刻为止;统计后的收尾回合不计入)
