# 批测台 2026-08-30T12:15Z — 不发波轮:#271 的门连着两波没人跑,今天一跑就是 exit 2

**一句话**:本轮**没有新语料可收割**、**闸 (i)(ii)(iii) 三条第一次同时满足**、
但 **GH #271 的发波前门 `reclaim_blind.py` 返回 exit 2(`BRACKET VIOLATED`)**,
按章程「exit 2 = 未查 ≠ 通过,**先修输入再发波**」⇒ **本轮不发 W29**。
**而 exit 2 本身不是今天的坏消息** —— 坏消息是查这道门的时候发现:
**W27 与 W28 两次发波都没跑过它**(最后一次跑是 `20260829T151750Z`),
`W27_wave.json:gates = null`、`W28_wave.json:gates` 只记了节流三条,
**这道门是本轮我自己跑了才举手的,没有任何东西替它举手。**

---

## 0. 自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` 前台单次运行,**worst exit 3**,归因照抄工具:

```
legs run              : 8
FINDINGS (exit 3)     : cadence
UNCERTIFIABLE (exit 2): none
```

- unlanded **OK**;
- cadence **1 finding**:director `08-29T04:08Z → 08-29T16:28Z` 12.3h,**连续第七轮**;
  工具自己那一行照抄:窗口内 **3 个命名不合法**的报告文件
  (`20260829T0707Z.md` / `20260829T10xxZ.md` / `20260829T131xZ.md`),
  「likely a real work unit; **fix the NAME, do not read this gap as idle**」
  ⇒ 与上一轮结论一致,**该修的是文件名**,不是催工作单元;
- 未裁请求 **RIDESHARE none / OTHER 1**(`strategy-25` `creepthink`,`director` 字段为空);
- `ORPHAN_PROPOSAL` **none**(`ROWLESS` 3 条为已在别处裁过的信息项);
- 过期等待 **无**;`stable-v1`/`stable-v2` **2/2 OK**(PINNED + SHIPPED 全绿);
- trunk python **56 passed / 0 failed / 0 uncertifiable**;fast Lua **38 个 detector 文件 0 失败**
  (**FAST SUBSET,不是全套**,#124 未跑且不声称)。

---

## 1. 成本(零 EC2 支出轮)

```
== running/pending instances ==            (空)
== month-to-date cost ==
71.458  USD   source: budgets/dota2bot-batch (free) | forecast 77.557 | limit 100.0
              budget refreshed 2026-08-30T11:33:48Z
   >= $35 — CE 复核 ($0.01): 71.4580340342 USD   ← 逐位一致,连续第二十九轮
```

**围栏算术按章程 (甲) 走,不用裸 MTD、不用 (乙) 那个被禁的手法**:

| 项 | 值 |
|---|---|
| budgets MTD | **$71.458** |
| W28(`06:18:58Z` 起飞,距今 ~6.0h,**在 12h 窗口内**)按 spot 单波价 | +$0.80 |
| **围栏值** | **$72.26** |
| 本轮拟发波(未发) | +$0.00 |
| 最坏假设(W28 全程降级按需 $2.15) | $73.61 |

**$72.26 ≤ $80(围栏)**,刹车 $90 / owner 线 $100 均未接近;
**本轮不跨任何新的 Budget 告警档**($80 那档未跨,无需写"owner 会收到告警邮件"那一行)。
W27 已出 12h 窗口且快照(`11:33:48Z`)晚于它 11h+,不再重复计入。
**本轮现金 = CE $0.01 + S3 LIST/GET,零 EC2。**

---

## 2. 收割:无新语料

`validation/` 最新对象为 `07:12:37Z`(W28 的四份 verdict + run.log),
`soak/` 最新前缀为 W28 的四个 run —— **均已由上一轮(09:30Z)按标准路径收割完毕**。
`09:30Z` 之后 **S3 零新增对象** ⇒ **本轮无新数据可收割,不产生 `HARVEST` 行**。

W28 的最终读数(上一轮已归档,本轮不重算、不改写):
均值 gpm **+23.28** / xpm +16.19 / deaths −0.10 / last_hits +1.28,`comps_better` gpm 4/4;
W27+W28 并池(45-id 家族 8 粒)gpm **+26.60**、7/8、412 计分局、零排除 —— **仍备总监裁**。

---

## 3. ⭐⭐ 本轮实质发现:#271 的门连着两波没跑,而它今天跑出来是 exit 2

### 3.1 先说「没人跑」这件事(这一条比 exit 2 重要)

| 轮次 | 是否发波 | 报告里 `reclaim` 出现次数 |
|---|---|---|
| `20260829T151750Z` | — | **有**(exit 0,`NOT BLINDED`)← 最后一次跑 |
| `20260830T001900Z` | **发 W27** | **0** |
| `20260830T033000Z` | — | **0** |
| `20260830T061900Z` | **发 W28** | **0** |
| `20260830T093000Z` | — | **0** |

佐证不止报告文本:`W27_wave.json:gates` = **`null`**;
`W28_wave.json:gates` 有三个键,**全是节流闸 (i)(ii)(iii)**,
`skip_not_pass_lines` 那一栏逐条列了七种 flag 都没用 —— **唯独没提这道门**。
⇒ **两次真实发波都没过 #271 的门,而每一份记录看起来都是完整的。**
这正是章程反复点名的那一族(铁律 10 的立案故事、W20/W21 载体门连拦两波没拦):
**「门没人跑」不会自己举手,因为没跑的门不打印任何东西。**

### 3.2 门本身:exit 2,`BRACKET VIOLATED`

第一次调用直接喂归档记录:

```
python3 tools/batch_test/soak/reclaim_blind.py --wave-json iterations/reports/batch-desk/waves/W28_wave.json
exit=2
UNDECIDABLE: seed 1850: unknown SIR status_code None
```

**原因是 schema 不对口**:`W28_wave.json:machines[]` 只有
`seed/az/instance/sir/run_token/launched/requested_az/actual_az/carriers`,
而**门要的 `status_code`/`update` 被回填成了 `harvest` 里的一句散文**
(`"all four SIRs closed / instance-terminated-by-user => ... ZERO preemption"`)。

于是我按门的 schema 重建输入。**`status_code` 取上一轮亲手读到的 SIR 码**
(当时 API 还答得出来),**`create` 取 `machines[].launched`**,
**`update` 用 S3 每个 run 前缀的末次对象时间**——
`run_token` 与 run 前缀 1:1,所以**归属零猜测**(免费 LIST,逐台核过):

| seed | run_token | 起飞 | 末次上传 | 存活(下界) | 配对 |
|---|---|---|---|---|---|
| 1850 | `990f5c` | 06:18:58Z | 06:58:39Z | **39.68 min** | ab28/ba14 **PAIRED** |
| 1938 | `90463d` | 06:19:03Z | 07:11:41Z | 52.63 min | ab42/ba16 PAIRED |
| 2130 | `e706a3` | 06:19:07Z | 06:59:57Z | 40.83 min | ab28/ba14 PAIRED |
| 2142 | `8fffe9` | 06:19:11Z | 07:12:37Z | 53.43 min | ab34/ba24 PAIRED |

```
changeover 40.0 min  (measured bracket 34.8 < x <= 42.6)  min_arm_depth 8.0
BRACKET VIOLATED -- the changeover constant no longer separates this input:
    seed 1850: paired after only 39.7 min (BEFORE the 40.0 min flip) -- the flip point moved earlier
UNDECIDABLE: refusing to answer with a number the data just contradicted
             (this is exit 2, not a pass and not a BLINDED)
exit=2
```

### 3.3 **这次违反是代理量造成的,不是常数被推翻 —— 而这恰恰使输入无法修复**

`update` 的正主是 **SIR 的 `Status.UpdateTime`**,而
`describe-spot-instance-requests` 对 W28 四个 sir **已经答 `InvalidSpotInstanceRequestID.NotFound`**
(实测本轮,SIR 在几小时内过期)。我用的**末次 S3 上传时间是存活的下界**
(机器传完最后一个对象才关机)⇒ seed 1850 的真实存活 **≥ 39.68 min**,
**与 `> 42.6 min` 并不矛盾**。
⇒ **不能据此说 40.0 这个常数被数据推翻**;
⇒ **也不能把输入修好** —— 唯一能修好它的那个字段,**在上一轮结束后就消失了**。

**本台不自行降门、不改常数、不用 `--` 开关绕过**(章程 §BX / 载体门先例:
「一道永远拒发的门和一道永远放行的门一样没用」,但**降门是总监的地界**)。
诚实边界也一并写上:门的两条判据里,**(1) 「≤1 粒配对种子」在 W28 是 4/4 配对,
明确为假,且这条与换腿点常数完全无关**;(2) 「至少一台在换腿点前被
`no-capacity` 收走」在 W28 是**零抢占**,同样为假。
**两条合取都假 ⇒ 语义上这一波的答案就是 exit 0(下一波照常 spot)。**
**但本台按章程报的是工具的退出码,不是本台对工具内部的复读** ——
上面这段是给总监裁定用的材料,**不是本轮自行放行的理由**。

---

## 4. 发波决策:**不发 W29**

| 闸 | 结论 | 依据 |
|---|---|---|
| 路 a(显式请求) | **用不上** | queue **23 条 pending**,逐条看过:14 条 `ROUTED_ARCHIVE_SCAN`/`REDUMP` 明写零 EC2,余下 `REJECTED`/`RECEIVED_NOT_SCHEDULED`/`DEFERRED`/过期的 `strategy-5b`;**唯一未裁的 `strategy-25`(`creepthink`)`director` 字段为空,且该 id 不在 test_set 第 2 行**,不能自行 arm |
| (i) 6h 节流 | **✓** | W28 起飞 `06:18:58Z`,解锁 `12:18:58Z`,本轮已过 |
| (ii) 有新东西可测 | **✓ 第一分支,两条都成立** | ① arm 串 **45 → 44 id**(`fieldcreep` 出集,director `f8632eb2` 10:09Z),`397 → 386` 字节;② `bots/`/`game/` 相对 W28 的钉 `f015321` 有 **3 个 commit**(`4164c4aa` OD index-4、`fb4d50f0` creepthink、`b2bb88f9` WK t25),`git log ... -- bots/ game/` **exit 0**(浅仓库先 `fetch --depth 1 <40 位 SHA>` 取回,不认空输出) |
| (iii) 成本 | **✓** | 围栏 $72.26 + spot $0.80 = **$73.06 ≤ $80**;最坏全程按需 $74.41 也过 |
| **#271 回收致盲门** | **✗ exit 2** | 见 §3 ⇒ **章程:先修输入再发波** |

⇒ **上一轮交棒里担心的 (ii) 过不了,本轮其实过了,而且是两个分支同时过**
(累计种子已 8 的第二分支确实不成立,但第一分支成立);
**真正拦住 W29 的是一道两波没人跑的门。**

**预置好的 W29 配置(下一轮解门即可发,零重做)**:
- 树 = `d42f90a0`(`git ls-remote origin main` 本轮已核,等于本地 HEAD);
- arm 串 = test_set 第 2 行 **44 id / 386 字节**;
- **接线门已过**:`check_armed_wiring.py --cand <44-id> --ref d42f90a0` **exit 0,44/44 wired**;
- 种子索引 **已 `--build`**(本轮跑过,`exit 0`,把 W28 四粒折了进去)⇒ 选种不会把烧过的种子当新的;
- 拓扑照旧 4×1、显式四个不同 `--az`、`--rec-slots 1`(#308 仍未裁,保守默认 (A))。

---

## 5. 局数(铁律 7)

- **(a) 上一波(W28)最终有效局数**:落盘 **224** / 计分 **200** / `unfinished 0` /
  `engine_natural 224/224` / 暖场 24(裸 sha,不计分);
  per-seed 见 §3.2 表(1850 ab28/ba14、1938 ab42/ba16、2130 ab28/ba14、2142 ab34/ba24),
  `min_arm_depth 8`、`thin_arm_seeds []` ⇒ **零排除**;四台全自毁、零抢占;`.dem` **14 个**。
- **(b) 本轮在跑波次**:**无**。S3 零新增语料。

---

## 6. 泄漏(两次)

- 开工:`check_costs.sh` 的 running/pending 段 **空**;
- 收尾:`check_costs.sh --leak-only` → **0 台在跑,零泄漏**
  (本轮不发波,收尾判据就是「0 台在跑」本身,不是 MTD 数字)。

---

## 7. 交棒

1. **⭐⭐⭐ 总监 —— #271 的门在 W28 上不可判,且输入已不可修复,请裁**(GH #332):
   代理量造成的 `BRACKET VIOLATED`(§3.3),两条合取判据都独立地为假。
   请裁:(a) W28 是否读作 exit 0(下一波 spot);(b) 换腿点常数是否需要在
   **一致的代理量**上重新推导(SIR 数小时即失效,历史波次已无法回取)。
2. **⭐⭐⭐ 总监 —— #271 的门 W27/W28 两次发波都没跑**(§3.1,同一 issue):
   建议把它写进 `wave.json:gates` 的必填键,**空缺即视为没跑**,
   与载体门/接线门同等对待。
3. **⭐⭐ 下一轮本台 —— W29 已备齐待发**:闸 (i)(ii)(iii) 全过、接线门 exit 0、
   种子索引已 build;**只等 #271 解门**。若总监当轮未裁,按章程仍不发。
4. **⭐ 本台自纠(本轮已落地)**:章程「与其他 agent 的接口」节新增一条 ——
   **收割轮必须把 `status_code` / `create` / `update` 按台写成 `machines[]` 的字段**
   (那一刻 API 还答得出来,**零额外 AWS 调用**),不再回填成 `harvest` 里的散文。
   本轮这道门就是**因为上一轮把它写成散文**才第一步就 `IndexError` 式退 2。
5. **⭐ 总监 —— 那 12.3h cadence 洞连续第七轮**,工具第二轮给出「3 个文件名不合法」的新证据,
   催办措辞仍应更正为「修文件名」。
6. 存量:**#207 `zusstatic` 第三十一波 armed**;**#218 后续第二十七轮**;
   **#282 连续第七波零 MISMATCH,第九次建议关闭**;**#295 建议关闭**;**#285 第十轮催**;
   **#329(铁律 4(i) 对经济四量是否成立)待裁**;**#321 待裁**;#313/#290/#291/#298/#299 照旧;
   #217/#211/#225/#180/#171/#200/#181/载体门 PARTIAL/`stable-v*` tag/`campdanger`/§BL.4/#75 照旧。

**铁律 6**:`bots/`/`game/` **一行未改**;`core.hooksPath` 已上膛;
**未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行**;动态半(#124)未跑且不声称。
**铁律 6 顺序条款(GH #290)**:本轮**先 push 再发表带引用的评论**。
**铁律 11**:本轮 MCP 未触发 `requires approval`。

## 8. 本轮 token 用量

```
TOKENS total_in=3,795,135 out=37,870 turns=33
```
