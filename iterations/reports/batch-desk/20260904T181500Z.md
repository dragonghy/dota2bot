# 批测台 2026-09-04T18:15Z — 发 W47 = 62-id 家族第二波(补到 8 粒)

**一句话**:本轮真正的产出不是那四台机器,是**两件在闸上量出来的边界**——
① **载体门的 term 集在 armed 串一字未动的情况下自己变了**(7 → 8,`rotscope` 认到了
pudge),而变它的是**总监 16:1xZ 落地的工具**,不是本台的任何选择;
② **上一轮本台自己提议的那条规则,在发波轮上是空的**:闸 (iv) 对自己刚起飞的波
**按构造 `exit 2`**,因为它要的字段在收割前**诚实地全是 null**。

---

## 一、自检(铁律 10)

⚠️ 开工第一条命令**又误用管道**(`| tail -40`),脚本自拒:
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.`
**第 N 次**——⛔ **本台仍不给这个复发编号**:章程 00:15Z 写「第二十二次」、12:15Z 写
「第 7 次」、脚本自己写 `recurred 5x`,**三个数互不相容**(上一轮已交总监定名,未裁)。

改重定向重跑:**`EXIT=124`(裸读 `$?`)= 超时,不是 0 也不是 3**。
400s 上限用尽在最后一条腿 `=== trunk health (python test suite) ===` 上,
**该腿本轮没有读数**。⚠️ 按 GH #171 的措辞纪律:**`124` 不是通过**,
**trunk 的 python 那一侧本轮没人看过**,本台**不声称**它红或绿。

**跑完的腿**(超时前已落盘):
- 锚点 `stable-v1`(`roamstale`,`6db5921c5945`)/ `stable-v2`(`creeppull,pullbeat`,
  `136a332fdea4`)**`EXISTS/PINNED/SHIPPED` 全 ok**。
- `unlanded_commits`:**无 OFF-TRUNK**(本地 HEAD `15b0d9fd` == `origin/main`,当场
  `git ls-remote` 核过)。
- `owed executions`:仍 **1 行 `OWED campgrade_wave_type_premise_reading`**
  (GH #480 / #241,executor = 批测台**排 `campgrade` 独占波的那一轮** + 录像组)。
  **`campgrade` 独占波此刻仍不存在,本波不是它**,本轮不认领。
- `orphan admission proposals`:23 节扫过,`ORPHAN_PROPOSAL` **none**,
  `ROWLESS` 3(`aimguard` / `campvoid` / `campexit`,信息性)。
- `expired waits`:6 份章程 / 75 个已结 id,**live 块内无过期等待**。
- `UNKNOWN STATUS`(GH #317 词汇漂移)**21 行**,与上一轮同形。

---

## 二、成本(闸 (iii))

- 九月 MTD **`$14.981`**(免费 `budgets describe-budgets`,`refreshed 2026-09-04T11:29:50Z`)。
  MTD `< $35` ⇒ CE 未触发,**没花 `$0.01`**。
- ⚠️ **这是连续第三轮读到逐位相同的同一张快照**,而它比 W46 起飞(`12:31Z`)还早 1h
  ⇒ **按构造不含 W46**;距 W45 关机 ~6.5h,**仍在 4.3–11.3h 滞后带内** ⇒ **也不能假定含 W45**。
- **围栏(铁纪律甲,逐波按自己的市场取价)**,本轮**改用 W46 账单侧实测上界 `$1.02`
  给两条 spot 波计价,不用章程常数 `$0.90`** —— 理由是 GH #454 的自动失效条款
  **上一轮已经触发**而总监尚未重裁,取高值的失效方向是**安全那一侧**:

  `14.981 + W44 按需 2.15 + W45 spot 0.90 + W45 按需补跑 1.10 + W46 spot 1.02 + W47 本波 spot 1.02 = $21.15`
  vs `$45 / $80 / $90 / $100` ⇒ **闸 (iii) 通过**,九月未及第一档 $50 告警,**不触发任何刹车**。
  (若照旧用 `$0.90` 计两条 spot 波则是 `$20.93`;**两个数都过**,登记高的那个。)
- ⛔ **未用被禁的「MTD 增量对得上波费 ⇒ 该波已计入」手法**(铁纪律乙)。
- **(a) W44 对账第六次顺延**:快照仍在滞后带内,按构造结不了。

---

## 三、收割:本轮零收割

S3 `soak/` 前缀**自 W46 的四个 run 之后没有新对象**(`awsx s3 ls` 逐条核过,最新四个
是 `..._123113/_123117/_123122/_123127`,全是 W46 自己的)。W46 **上一轮已收干净**
(四粒全过 `arm_depth` 门、`RV_EXIT=0`、四台全自毁)。⇒ **本轮无新数据可收割,
无 verdict 可写,无 queue 行可翻动。**

**上一波实测局数(铁律 7 (a))**:W46 语料 **230** 份 `analysis.json`(48/56/62/64),
**计分 206**,差额 24 = 每台 6 局暖场。

---

## 四、⭐⭐ 头号发现:载体门的 term 集自己动了,而 armed 串一字未动

发波前照章程跑闸 (ii) 的载体腿:

```
CT_EXIT=0
CARRIER_TERMS derived from 62 armed ids: 13 hero-scoped, 49 generic, 0 unresolved => 8 term(s)
TERMS chaos_knight,crystal_maiden,lion,obsidian_destroyer,pudge,skeleton_king,spirit_breaker,zuus
```

**W46 在同一个 62-id 串上登记的是 `12 hero-scoped / 50 generic ⇒ 7 term`,没有 pudge。**
串逐字节相同(`62 / 550 / c7e1f92c…`,本轮**当场重算未抄散文**),`bots/mode_roam_generic.lua`
自 W46 起**一行未动**。差在**工具**:

- **总监 16:1xZ 落地 GH #473 甲**(commit `6ea528a8`,`tools/batch_test/soak/carrier_terms.py`)
  —— 「载体门看不见『域是一个英雄、文件是 generic』的 id」。
- 于是 **`rotscope` 现在解析成 `hero pudge`**(`via bots/mode_roam_generic.lua:1039`),
  此前被算作 generic。**第 8 个 term 就是这么来的。**

**为什么这条要写进报告而不是一笔带过**:
① **W46 的 7-term 读数是在旧工具下取的,和本波的 8-term 读数不能逐 term 对比** ——
两次「min-per-term = 2」说的不是同一句话;
② **它把选种的解空间收紧了一个量级,而且是可量化的**:8 个 term 每个要 ≥2 载体 ⇒
一副 4 粒的牌需要 **≥16 个 term 槽**,而窗口 `[4601,5000]` 里最好的四粒**最多只能供 19 个**
(popcount 分布 `{0:26, 1:105, 2:145, 3:91, 4:31, 5:1, 6:1}`)。对 popcount ≥3 的 **124 粒**
做穷举(**C(124,4) = 950 万**四元组),**可行解只有 471 个**。
**这不是耗尽——门过了**——但它是**下一个 term 出现时该盯的那个数**。

---

## 五、发波:W47 = 62-id 家族第二波

### 5.1 闸 (ii):**第三支**成立,而且这一支不依赖那个未裁的分叉

| 支 | 判据 | 本轮 |
|---|---|---|
| (一) armed 串变了 | 当场重算 `62 / 550 / c7e1f92c739f8f8dc0ee0c9484288628` | **不成立**(与 W46 逐字节相同) |
| (二) 树变了且对被测配置非惰性 | 见下 | **按实测不成立** |
| (三) 该家族累计计分种子 < 8 | 62-id 家族现 **4 粒**(W46) | ⭐ **成立,且是决定性的那一支** |

**(二) 是量出来的不是断言的**:`git log efa7ba70..HEAD -- bots/ game/`(`LOG_EXIT=0`,
**裸读不过管道**)只给**一个** commit —— `da79f6bc`(协同组 13:55Z,`midsupyield` / GH #489),
`game/` 零 diff。整个 diff 是 `bots/FunLib/jmz_func.lua` 的 59 行,两处改动:

- **(a)** `J.HasAvailableSupportResponder` 加 `hBuilding` 参数 + 一条距离合取项。
  **它的唯一调用点是 `jmz_func.lua:8489`,而那一行坐在 `J.IsSoakCandidate('midsupyield')` 门内**
  (本台亲手 grep 整个 `bots/`:**一个调用点 + 一处注释提及,再无其他**),
  而 **`midsupyield` 不在 62-id 串里**。
- **(b)** `J.ShouldTpSupportTowerFight` 里的字面量 `3500` 换成 `J.TP_RESPONSE_FAR_FLOOR`,
  该变量在 **`8332` 行文件加载期赋值 = 3500** ⇒ **门外唯一的改动是一次保值替换**,
  运行期取值在任何调用之前就已完成。

⇒ **树漂移对 armed 配置惰性。** 顺带:**GH #490 那条 trunk 红正是这次替换造成的**
(`test_detector_source_constants.py` 要 `GetUnitToUnitDistance( bot, building ) > <数字>`,
现在那里是个常量名)——**本台只登记这个因果,不改 `bots/` 也不改判据**。

⚠️ **GH #363/#449 那个老分叉第五次上门**(「按 armed 串家族」vs「按树对象哈希」):
本轮又是**串不动而树动**。**但它这次不咬**——第三支的成立**与取哪种读法无关**,
两种读法都同意「有东西可买」。分叉**仍然欠一个裁定**。

### 5.2 选种

`seed_roster_index.py --build` **在选种之前**跑(`ROSTER_EXIT=0`):S3 345 个 run 前缀、
341 已索引、**新扫 4 个**(正是 W46 自己那四个)⇒ 索引 **209 粒种子 / 41 英雄**。
**不 build 就会把 W46 刚烧掉的 4209/4401/4470/4252 当「从未用过」献回来。**

窗口**右移**到 `[4601,5000]`(W46 在 4201–4600),**400 粒全未用**。
按第四节的解空间,选定 **4723 / 4678 / 4763 / 4950**;
`pudge` 平局判据同 W45/W46(**4 粒里 3 粒**抽到 Pudge,维持 `rotscope` 条件 (a) 的帧级
通道可执行;GH #472 / queue `strategy-26`)——**这是门自己解空间内部的免费平局判据,
不是门的改动,也不是新 term**。

### 5.3 五道程序化门(全部本轮亲手跑,裸读退出码,不沿用散文)

| 门 | 命令 | 退出码 | 读数 |
|---|---|---|---|
| (ii) 载体 term | `carrier_terms.py --arm-file test_set.md --arm-line 2` | `CT_EXIT=0` | 62 id = 13 hero / 49 generic / **0 unresolved** ⇒ **8 term**(见第四节) |
| (ii) 载体覆盖 | `seed_draft.py 4723 4678 4763 4950 --assert-carrier-from-arm "$ARM"` | `CARRIER_GATE_EXIT=0` | ids=13 seeds=4;逐 term 载体数 cm 3 / pudge 3 / ck 2 / lion 2 / od 2 / sk 2 / sb 2 / zuus 2,**min-per-term = 2,8 个 term 全满足** |
| 布线 | `check_armed_wiring.py --cand "$ARM"` | `ARM_EXIT=0` | **62 个 armed id 全部 wired on HEAD** |
| (iv) reclaim-blind | `reclaim_blind.py --wave-json W46_wave.json` | `RB_EXIT=0` | `4 paired of 4` / `0 reclaimed before the flip` / `not blinded` / **`NEXT WAVE: spot`** —— **本轮重跑,不是沿用上一轮散文** |
| 铁律 6 静态半 | `bash tools/agent/luacheck_gate.sh` | `LUACHECK_EXIT=0` | 冷启自己装上 luacheck(apt 包名 `lua-check`),`luacheck bots game: 0 warnings`,`GATE_EXIT=0 CLEAN` |

**闸 (iv) 一处诚实边界**:W46 四行全打 `[ab/ba only (no arm_depth given)]` ⇒
**depth-checked 那条路径没被走到**,因为 `W46_wave.json:machines[]` 有 `ab`/`ba` 没有
`arm_depth`。**W47 的记录已把 `arm_depth` 作为键预置进去**,并写进 `harvest_obligations`。

**闸 (iii)** 见第二节;**步骤 4a 队列检查**:`queue.json` 74 行(25 pending),
**没有任何 pending 行申请一个会被本波挤掉的专波**(逐行扫「独占 / solo / dedicated /
专波」,命中的行要么 `done`/`harvested`,要么自己写明「搭车不申请专波」)。
全集波**本来就是 owner 2026-08-22 定的默认波型**。**本轮不翻动任何 id 的 queue 状态。**

### 5.4 闸 (i):被行使了**四次**,拦了四次,第五次才开

章程上一轮手打的解锁时刻是 `18:31:15Z`。**本轮没有读它,读的是工具那一行** ——
而工具锚在**锚定波最后一台**上,给的是 **`18:31:30Z`**,比手打的数**晚 15 秒**。
⇒ **照手打的数发波会真的破闸。**(上一轮已写「读工具那一行,不要读这个手打的数」,
本轮兑现了,而且兑现得有代价可言。)

四次拦截,全部 `exit 3`,全部裸读:`18:13:57Z`(短 1053s)、`18:26:47Z`(短 283s)、
**`18:29:13Z`(短 137s,这次是在启动脚本内部,脚本 `exit 8`,一台都没发)**、
`18:29:45 / 18:30:15 / 18:30:45 / 18:31:15Z`(脚本内 30s 轮询,仍 3),
**`18:31:45Z` → `WAVE_THROTTLE: UNLOCKED (exit 0)`**,第一台 `18:31:48Z` 出。

⭐ **那 ~18 分钟没有空转**(铁律 7/11):自检、成本、收割核查、五道门、选种(950 万四元组
的穷举)、波次记录、`test_wave_gate_keys.py` **全部在等待窗口内做完,零成本**;
真正的「等」只是启动脚本里对同一道闸的 30s 轮询,**不是绕过**。
顺带:工具对本轮新写、`machines[]` 还空着的 `W47_wave.json` 打
`skipped W47 (no machines[] -- opened no window)` ⇒ **没上机的波不开窗,按 schema 结构成立**。

### 5.5 起飞

四次独立调用,每次显式一个 `--az`,
`--slots 16 --hours 2 --rec-slots 8 --ref 15b0d9fd…(40 位) --validate "<62-id 臂串> <种子> --games 12"`
—— 采集配置与 W39–W46 **逐字相同**。

| 种子 | 实例 | AZ 请求→实得 | `run_id` 尾 | `LaunchTime` | `SPOT_RUN_EXIT` | SIR |
|---|---|---|---|---|---|---|
| 4723 | `i-067c5f462a7d2933d` | 2a → **2a** | `b8fa5b` | 18:31:48Z | `0` | `sir-ivtqk2fq` |
| 4678 | `i-0f9b588e467660385` | 2b → **2b** | `1bfa3d` | 18:31:50Z | `0` | `sir-97qfgvrm` |
| 4763 | `i-0f6fb96cd27e0b01f` | 2c → **2c** | `b30fe4` | 18:31:53Z | `0` | `sir-zh3zjean` |
| 4950 | `i-008ac3bde84e299cb` | **2d → 2b(re-aimed)** | `9782b8` | 18:32:27Z | `0` | `sir-5fczhafq` |

- **市场**:`describe-instances` 的 `InstanceLifecycle` = **`spot`,四台全核过**。
  `run_id` 的 `spot_` 前缀是硬写字符串,**不是证据**。
- **自毁**:`--hours 2` 看门狗 + `shutdown-behavior=terminate` + **one-time** spot 请求。
  四个 SIR 起飞时全 `active / fulfilled`。
- **预期局数**(铁律 7 (b)):~96 计分局(4 粒 × 12 局 × 2 腿)+ 暖场局(不计分)。
  起飞时 S3 零对象;**14 分钟内 `OBJ_COUNT=0` 是预期值不是症状**。

---

## 六、⭐⭐ 第二个发现:GH #252 本波失败,而失败的形状是 GH #460 点名要在 spot 波上重看的那个

```
! launch in us-west-2d failed -- InsufficientInstanceCapacity (reached max retries: 4)
! re-aiming inside the ring -> us-west-2a
! launch in us-west-2a failed -- InsufficientInstanceCapacity (reached max retries: 4)
! re-aiming inside the ring -> us-west-2b
! PLACEMENT MISMATCH requested=us-west-2d actual=us-west-2b re-aimed=yes
```

- **GH #252 验收**(四个 AZ 互不相同 且 requested == actual 四台全对)**本波失败**:
  实得 AZ 是 **2a / 2b / 2b / 2c ⇒ 3 个去重**,一处 placement mismatch。
- **GH #256** 的 `re-aiming inside the ring` **触发两次**;`AZ RING EXHAUSTED` **未出现**。
- **GH #460 (丙) 提议的 `AZ SPREAD COLLAPSED` 判据(实得 AZ 去重数 < 2)未达成**(是 3)。
- ⭐⭐ **本波真正该登记给 #460 的是这一条,而它比拓扑论证硬**:
  **`us-west-2a` 在 `18:31:48Z` 成功交付了第 1 台,39 秒后的 `18:32:2xZ` 对第 4 台
  「没有 spot 容量」。** ⇒ 这是**一波之内的容量收紧**,**不是 AZ 列表陈旧**,
  也不是环设计不对。`InsufficientInstanceCapacity` 本波计数 **2**(W46 是 **0**,W43 是 6)。
- ⚠️ **诚实边界**:上一轮的 W44 按需波曾被明写「不许当成 #460 的反证」;
  **对称地,本波也不构成 #460 的正证** —— 它是**一次 spot 波上的实测事件**,
  归因(环的分散度下限该定多少)**是总监的活**。

---

## 七、⭐⭐ 第三个发现:上一轮本台自己提议的那条规则,在发波轮上是空的

上一轮(15:15Z)本台把「**写记录的那一轮必须自己把闸跑一遍**」提给总监(GH #487),
本轮**照做**——对 W47 自己刚填完的记录跑闸 (iv):

```
RB_SELFCHECK_EXIT=2
UNDECIDABLE: seed 4723: unknown SIR status_code None
```

⭐ **这个 `exit 2` 是按构造的,不是记录写坏了。** 闸 (iv) 问的是「这一波的**采集**有没有
被回收致盲」,它要的 `status_code` / SIR 终态 `UpdateTime` / 逐台腿数,**在收割前根本
不存在**;起飞那一刻它们**诚实地全是 null**,而工具**拒答而不是猜**——这正是对的行为。

⇒ **那条规则对收割轮成立,对发波轮空洞。** 发波轮能做、且本轮做了的是两件:
① 对**上一波**跑闸 (iv)(`RB_EXIT=0` on W46)——**那本来就是闸 (iv) 的用途**;
② 对**新写的记录**跑 `tests/test_wave_gate_keys.py`(**`WGK_EXIT=0`,241 checks 0 failed**)
—— 于是记录的**形状**在写的当刻被验了,即使它的**内容**此刻还验不了。
**已把这条边界写进 `W47_wave.json:gate_iv_self_verification` 并追评 #487**,
好让总监裁的是一条**能被执行**的规则。

---

## 八、泄漏:干净

- **起飞前**:`pending,running` 查询返回**空** ⇒ 四台全是本轮有意启动的。
- **起飞后点名(章程步骤 6,两半都跑,零成本)**:
  (1) `pending,running` 的 `soak-run` 标签 **恰好四个,两两不同**,与 `machines[].run_id`
  逐一对上(`b8fa5b / 1bfa3d / b30fe4 / 9782b8`)——**不多(无泄漏)不少(无掉臂)**;
  (2) `terminated,shutting-down` 返回**空**。⚠️ 这个空是**「还没有任何一台死掉」**
  (波次 ~1 分钟大),**不是「记录过期了」**(GH #375 的 ~1h 窗口)。
- `describe-volumes`:**四个卷,各自 `in-use` 挂在这四台上**,**无游离根卷**。
- 四个 SIR 全 `active / fulfilled`。**常驻成本仍只有 AMI**(`ami-0a990a26d89c66547`)。

---

## 九、预登记(写在任何数据落地之前)

1. **胜负通道**:已连续**五**波有数据的波 `DEGENERATE`(W41/W42/W44/W45/W46;W43 沉默不计)。
   W46 少数侧 `28/230 = 0.1217`。W47 若第六次 `DEGENERATE`,**`winrate` 与
   `comps_better` 的 winrate 行一律不得写成读数**(GH #352 / §CT),并**必须打逐粒侧别普查**;
   若读到 `RECOVERED`(少数侧 ≥20%,#352 事先登记的验收),**那本身就是本波的发现**。
   ⚠️ **本波按构造买不到「换种子能不能救」的答案**:W41 同时换了 id 家族、种子窗口和树,
   横扫**反而恶化一个数量级**,那条路已被排除;本波只右移了种子窗口,**第六次
   `DEGENERATE` 是预期而非信息**。
2. **GH #454**:W47 又是**纯 spot** 波。**若它跑完**,收割轮必须**当场**把账单侧单波成本
   对常数结账(方法同 W46:逐台运行时长 × 该 AZ spot 费率),并明写自动失效条款
   **触发/不触发**。⭐ **本波额外带一条常数该知道的事实:两个 AZ 完全没有 spot 容量**
   —— 与 W46「四个 AZ 里三个顶穿 `$0.231–0.250` 价带」是同一个市场紧张的两种表现。
3. **并池**(`W47_wave.json:pooling_claim`,四个数落地之前写):本台的**读数**是
   **W47 与 W46 可并成 8 粒 62-id 家族**(串逐字节相同;唯一的 `bots/` commit 按第五节
   的实测对 armed 配置惰性)。**朴素的「树对象哈希」判官会说不能并** —— 那正是
   GH #363/#449 的分叉,**第五次上门仍未裁**。**决定权在总监,不在本台。**
4. **co-armed caveat**(原样承自 W46):trunk 红在
   `tests/test_coarmed_attribution_register.lua`(`creepthink > campbind` /
   `pullcad > campbind` / `pullthink > campbind` 三条未登记合取)⇒ **家族级读数照常成立**,
   但**任何拿本波说 `creepthink`/`pullcad`/`pullthink` 逐 id (a) 的话,测的是
   `outer AND campbind`**。GH #486 在协同组/总监手上;本台不改入集节、不撤 id。

---

## 十、铁律 6 / 11 / 队列

- **`bots/`/`game/` 本会话一行未改**(改动全在 `iterations/`)。
- 静态门 **`LUACHECK_EXIT=0`**(`luacheck bots game: 0 warnings`,`GATE_EXIT=0 CLEAN`),
  **未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行要抄**。
- **动态半(GH #124,~100 min)未跑,不声称**。
  ⚠️ 自检的 python trunk 腿**超时(`EXIT=124`)⇒ 本轮无读数,既不声称红也不声称绿**。
- **铁律 11**:本轮未遇到 `requires approval`;**无空转等待**(见 5.4)。
- **队列**:本轮不翻动任何 id 的 queue 状态。

---

## 十一、交棒

1. ⭐⭐⭐ **总监(GH #454)** —— **常数重裁仍欠**(上一轮 W46 实测 `$0.959–$1.022 > $0.90`,
   区间两端都超)。**本轮新增一条该一并考虑的事实:两个 AZ 完全没有 spot 容量**,
   即价带陈旧与容量紧张是同一件事的两面。本轮围栏已**先行按 `$1.02` 计价**(安全侧)。
2. ⭐⭐⭐ **总监(GH #487)** —— **本台修正自己上一轮的提议**:「写记录的那一轮必须
   自己跑一遍闸」**对收割轮成立、对发波轮按构造空洞**(闸 (iv) 的输入在收割前诚实为 null,
   工具正确地 `exit 2`)。请裁**能执行的那一版**:发波轮跑「对上一波的闸 (iv) + 对新记录的
   `test_wave_gate_keys.py`」,收割轮跑「对本波的闸 (iv)」。
3. ⭐⭐⭐ **总监(GH #460)** —— **本波是你要的那个 spot 波,而且给了硬事实**:
   `us-west-2a` **39 秒内从可用变成无容量**(18:31:48Z 交付第 1 台 → 18:32:2xZ 拒绝第 4 台),
   `InsufficientInstanceCapacity` ×2,**GH #252 验收失败**(实得 AZ 3 个去重 + 一处
   placement mismatch),(丙) 的 `AZ SPREAD COLLAPSED` 判据**未达成**(3 ≥ 2)。
   (甲)(乙)(丙)(丁) 四件仍全部有效。
4. ⭐⭐ **总监(GH #473 甲的下游)** —— **载体门 term 集在 armed 串不动的情况下从 7 变 8**
   (`rotscope` → pudge)。两件请裁:① **W46 的 7-term 读数与本波的 8-term 读数不可
   逐 term 对比**,历史效率台账要不要标一条界?② **选种解空间实测收紧到 471/950 万**,
   第 9 个 term 出现时 4 粒牌可能真的解不出来 —— 要不要现在就定「解空间 < N 时怎么办」?
5. ⭐⭐ **总监(GH #363/#449)** —— **闸 (ii) 的老分叉第五次上门**(串不动而树动)。
   本轮**不咬**(第三支独立成立),但 `pooling_claim` 已预登记:**W47 能否与 W46 并成
   8 粒,取决于这个未裁的分叉。**
6. ⭐⭐ **协同组/英雄组(GH #490)** —— `tests/test_detector_source_constants.py` 的 trunk 红,
   **本轮查清了因果**:是 `da79f6bc` 把 `J.ShouldTpSupportTowerFight` 里的字面量 `3500`
   换成了常量名 `J.TP_RESPONSE_FAR_FLOOR`,而检测器的正则要一个数字。
   **本台不改 `bots/` 也不改判据**,只交因果。
7. ⭐⭐ **协同组(GH #486)** —— co-armed 登记册仍红在 `campbind`;**W47 是第二波为它花钱的波**。
8. ⭐ **总监** —— **W44 对账第六次顺延**(快照仍在滞后带内);
   **管道误用计数器仍欠定名**(三个数互不相容)。
9. ⭐ **章程行文** —— 步骤 3 的收割前缀写 `validation/` 而实际在 `soak/`(上一轮已点名,
   本轮复现:`validation/` 递归 ls 仍是 0 对象)。**本台只点名不擅改步骤节。**
10. ⭐ **存量**:#480/#241(`campgrade` 独占波仍不存在)/ #473 乙 / #472 / #427 / #408 /
    #420 / #449 / #446 / #290 / `strategy-5b`(⚠️ 它的 `midsupyield` 今天刚被修,
    但**未入集、不在 62-id 串里**,本波测不到它)/ **#388 的 W34 103 份 `.dem`
    2026-09-22 到期(18 天)**。

**下一轮本台 = 收割 W47**(预计 ~19:30–20:30Z 落地),按
`W47_wave.json:harvest_obligations` 九条逐条做,重点:
`machines[].ab/ba` 填**每台自己的整数**、**这一波要补上 `arm_depth`**、
`status_code`/`update` 走 SIR、臂串**从发波树重算**、
从 **`soak/`** 前缀收(不是 `validation/`)、**GH #454 当场结账**、
四量两分层读数用工具自打的 `strata` **不手算、不按局加权**。
**闸 (i) 下次解锁 ≈ `2026-09-05T00:32:27Z`(锚定本波最后一台)——
但下一轮仍要读 `wave_throttle.py` 那一行,不要读这个手打的数。**

---

## 十二、本轮 token

```
TOKENS total_in=7,814,676 out=50,659 turns=59
```

(统计时刻之后的收尾回合不计入;量级参考。)
