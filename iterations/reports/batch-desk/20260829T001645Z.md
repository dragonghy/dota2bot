# batch-desk 2026-08-29T00:16:45Z — 发 W23(树 `72a8cf75`、43-id 全集、种子 `1021/1295/1343/1551`)

**一句话**:⭐⭐ **本轮是三件验收同时通过的一波** —— 四台 `c6i` spot 一次即中、
**四个 AZ 互不相同(#256 首次通过)**、`requested=/actual=` **四条全打且全相等
(#282 的判据首次可执行,且首次执行就通过)**;⭐ 选种把**六个 hero-scoped id
的载体数从 W22 的「三个只有 1 粒」全部抬到 ≥2 粒**,这是对 W22「84 个计分局
对三个 id 买到零读数」(GH #285)在**本台职权内**的直接对策。
另立 **GH #289**(trunk RED,strategy 的过期等待格,非本台文件)。

- 波次:**W23**,树 pin **`72a8cf754e99683e6472f404cfe6c8bbec7617c3`**(显式 40 位,不写 `main`)
- 市场:**全 spot,阶梯第 1 级一次即中,未用 `--on-demand`、未用 `c6a`**
- 支出:本波预估 **~$0.80**(spot)/ 保守 ~$2.15(全程降级按需);另 CE 复核 $0.01

---

## 一、自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` worst exit **3**。
两锚点 `stable-v1`/`stable-v2` 的 `EXISTS/PINNED/SHIPPED` **全 ok**;`unlanded_commits` 无。

⭐ **本轮 exit 3 的来源与前几轮不同,而且多出一处 trunk 上的**(退出码来源由工具自报,#267):
`FINDINGS = cadence stale-waits trunk-red(python)`、`UNCERTIFIABLE = none`。

- ⭐ **trunk RED(python)**:**19 checks, 1 failed**;整棵树判 `TRUNK RED`。
  `tests/test_stale_waits.py`:
  ```
  FAIL: a charter's LIVE 当前状态 waits on an admission ruling that already landed:
    [('strategy.md', 2569, '**下一格**:等总监重裁 `campexit`;…')]
    -- fix the charter line, do not loosen this test
  ```
  判据成立:`campexit` **已在 43-id 全集里**(test_set.md 第 2 行末位,W21/W22 都 armed 过)。
  **不是本台的文件** ⇒ 已立 **GH #289** 交棒,附时间线:
  18:16Z 轮 50/0 → 21:15Z 轮 51/0 → **本轮 50/1** ⇒ 回归窗口 **≤1 轮**。
  诚实边界:这个 RED **不挡 push**(`.githooks/pre-push` 的铁律 6 静态门是 luacheck,
  不是这条 python 腿),本轮 `bots/`/`game/` 一行未改,**W23 发波不受它影响**。
- cadence:`strategy` 01:24Z→10:35Z **9.2h,连续第五轮逐位同值(不是新洞)**;
  上轮那处 `director` 4.2h 洞本轮**已不在**。
- `replay-check` **连续第五轮未被点名**。
- 快 Lua 检测器 **18 文件 0 失败**(上轮 17,**+1**;**FAST SUBSET,不是全套**)。
- 并发会话检查(GH #180):`ListAgents` 本机**无其他会话** ⇒ 本轮无抢同一份活的风险。

## 二、成本

`check_costs.sh`:MTD **$63.046**(`budgets/dota2bot-batch`,免费路径),
读数 ≥$35 自动 CE 复核($0.01):**$63.0463391803** ⇒ **逐位一致,连续第二十轮**。
快照 **`2026-08-28T22:26:40Z`** —— ⭐ **本轮前进了**(前三轮卡在 `08-28T14:20:52Z`);
forecast/limit **88.194 / 100**。

MTD 自上轮 $61.719 → $63.046,**+$1.327**。⚠️ 按章程 §2(乙),**禁止**用
「MTD 增量对得上波费 ⇒ 该波已计入」这个手法(它已两次给出错误结论)——
这里**只登记数字,不作归因**。

**围栏算术**(§2 甲:MTD + Σ 近 12h 已发波次 × 该波市场类型单价;**保守不去重**):

| 口径 | 算式 | 值 | 闸 (iii) $80 |
|---|---|---|---|
| W23 走 spot(实际) | 63.046 + W22 0.80 + W23 0.80 | **$64.65** | 通过,余量 $15.35 |
| W23 全程降级按需(保守) | 63.046 + W22 0.80 + W23 2.15 | **$66.00** | 通过,**余量 $14.00** |

**两种口径都过闸**(§2 甲的对价)。刹车 **$90 未接近**;
**本轮不跨任何新告警档**(已跨 $50,下一档 **$80**)⇒ **owner 不会因本台收到新的 Budget 邮件**。
本轮 AWS 现金支出 = CE 复核 **$0.01** + W23 一波 ~$0.80。

## 三、收割:为空(不是没做,是没有对象)

`s3://dota2bot-batch-results-4924/soak/` 最新四个前缀仍是 W22 那四台
(树 `2f20fe82`,尾 token `80d801`/`bc7f45`/`af08aa`/`daf92f`),**21:15Z 那轮已定案**
(128 落盘 / 84 计分 / 2 粒配对 / gpm −49.67 / `min_arm_depth 8` / `thin_arm_seeds []`);
此后无新 run,`validation/` 无新对象(最新仍停在 07-23)。
⇒ `queue.json` **无 status/result 需更新**(W23 是例行波,不对应任何队列请求)。

## 四、回收致盲(#271)exit 0 ⇒ 本波照常 spot

在 W22 那份**已落盘的一手 `wave.json`**(659 B,上轮按建议存进了 run 目录)上重跑:

```
changeover 40.0 min  (measured bracket 34.8 < x <= 42.6)  min_arm_depth 8.0
  seed 975   22.1 min  instance-terminated-no-capacity  ab10/ba0   NO-PAIR
  seed 976   21.2 min  instance-terminated-no-capacity  ab10/ba0   NO-PAIR
  seed 977   43.7 min  instance-terminated-by-user      ab28/ba14  PAIRED
  seed 978   44.4 min  instance-terminated-by-user      ab28/ba14  PAIRED
yield      : 2 paired seed(s) of 4 machine(s)
attribution: 2 machine(s) reclaimed before the flip
VERDICT: not blinded -- the wave delivered 2 paired seed(s).
NEXT WAVE: spot (the default; GH #158 unchanged).
```

exit **0**,无 `BRACKET VIOLATED` ⇒ **W23 默认 spot**。
⭐ 上轮登记的那条危险侧边界(「反推的输入永远自洽、不会自己举手」)**本轮实地闭合了一次**:
同一份读数,这次的输入是**一手落盘文件**而不是上一份报告的存活表。

## 五、三条闸:全部满足 ⇒ 发波

**§4a 无适用对象**(22 条 pending 逐条查 `director`:`ROUTED_ARCHIVE_*` 15 / `REJECTED` 2 /
`RECEIVED_NOT_SCHEDULED` 2 / `DEFERRED_PENDING_W3_RESOLVE` 2;唯一 `APPROVED_CONDITIONAL`
的 `strategy-5b` 是 `director.wave=W6` **历史波次**;**无 `director` 字段的 pending 为空**)⇒ 走 §4b。

- **(i) 6h 闸通过,+7 秒**。锚点 = W22 发波循环 `2026-08-28T18:16:38Z` ⇒ 解锁 `00:16:38Z`。
  块内 `date -u` 守卫**原样复用,本轮连续拦了 13 次**
  (`00:12:25Z` 起每 20s 一行 `NOT UNLOCKED, refusing to launch`,最后一次 `00:16:25Z`,
  差 13 秒),第 14 次 **`00:16:45Z PASS (+7s)`**。
- **(ii) 满足,两条都成立**:
  (a) `git fetch --depth 1 origin 2f20fe82…`(全 40 位)后
  `git log 2f20fe82..HEAD -- bots/ game/` **exit 0**(不是浅克隆的空输出)且输出为空
  ⇒ `bots/`/`game/` 与 W22 **同树**;`test_set.md` 有 `e80a72ef`(director 19:03Z)一笔,
  但**第 2 行 arm 串逐字节未变**。
  (b) 因此本条靠**第二子句**成立:当前 tree+测试集的**累计计分种子 = 2(只有 W22 那两粒) < 8**。
  ⭐ **这一条本轮特意分开写**:若只看第一子句,「同树 + 同串」看起来像「没有新东西可测」,
  而实际情况恰恰相反 —— W22 因回收只买到 2 粒,**语料是薄的,不是够的**。
- **(iii) 通过**,见 §二(两种口径都过)。

## 六、两道门(先落盘再取 `$?`)

- **接线门 exit 0**:`all 43 armed ids wired on 72a8cf754e99683e6472f404cfe6c8bbec7617c3`。
- **载体门 exit 0**(#276 机械推导 `--assert-carrier-from-arm "$ARM"`,**不是手打词表**):
  `CARRIER_TERMS derived from 43 armed ids: 6 hero-scoped, 37 generic, 0 unresolved => 5 term(s)`
  `TERMS crystal_maiden,lion,obsidian_destroyer,spirit_breaker,zuus`
  `CARRIER_GATE ids=6 seeds=4 exit=0`

### ⭐ 六之二、选种:六个 hero-scoped id 的载体数全部 ≥2(对 GH #285 的直接对策)

上轮交棒①点名「**选种优先照顾 `aimguard`/`liondrainstop`/`odaoe`**,它们 W22 买到零局,
若再挂单粒种子,一次回收就是**连续两波零读数**」。本轮据此**自己选种**
(不是无条件 `--find`,也不是沿用总监为 W22 预选的那组),判据是**每个词至少两粒载体**:

| id | 词 | W22 载体数 | **W23 载体数** | W23 carriers |
|---|---|---|---|---|
| `aimguard` | spirit_breaker | **1**(975) | **3** | 1295,1343,1551 |
| `liondrainstop` | lion | **1**(975) | **2** | 1021,1295 |
| `odaoe` | obsidian_destroyer | **1**(975) | **3** | 1295,1343,1551 |
| `cmrguard` | crystal_maiden | 1(978) | **2** | 1021,1551 |
| `zusstatic` | zuus | 3 | **2** | 1021,1343 |
| `zusult` | zuus | 3 | **2** | 1021,1343 |

**六个全部 `PARTIAL` 且 satisfied ≥2**(W22 是三个 satisfied=1)。
⇒ **本波任何单台被回收,都不会有任何一个 hero-scoped id 归零** —— 它只会变薄。
这正是 W22 事故(975 一粒扛三个 id,被回收后那三项**与没发这一波逐字节相同**)在
**本台职权内**能做的那一半;另一半(收割侧的门)是 **GH #285**,仍在总监处。

⚠️ **诚实边界**:`zusstatic`/`zusult` 的载体数从 3 **降到 2**(为了给三个稀缺词腾位置),
这是一次**自觉的再分配**,不是退步 —— 但 `#207` 那条「`zusstatic` 连续第十八波空转」的
计数器仍然只数「armed 过几波」,**不数买到几局**(上轮交棒②已请总监裁这件事)。

## 七、市场与放置:⭐⭐ 三件验收同时通过

**发波循环**:四次独立调用,每次 `--count 1` + **显式一个 `--az`**,四次四个不同
(`2a`/`2b`/`2c`/`2d`),调用间隔 4–5 秒。

**阶梯第 1 级 `c6i.4xlarge` spot 一次即中,四台全中**:
**无一条 `InsufficientInstanceCapacity`、无一条 `re-aiming`、无一条 `AZ RING EXHAUSTED`**
⇒ **没走到第 2 级(`c6a`),更没走到第 3 级(`--on-demand`)**,本波**全 spot**。
(与上一波 W22 恰好相反:那一波两粒在四个 AZ 全无容量、靠 `c6a` 才救回。)

以 `describe-instances`(独立证据源,非脚本自述)为准:

| 种子 | 实例 | 机型 | Lifecycle | 请求 AZ | **实得 AZ** | run_id 尾 token | LaunchTime |
|---|---|---|---|---|---|---|---|
| 1021 | `i-0cc3f47e411d145c0` | c6i.4xlarge | **spot** | 2a | **2a** | `9e514b` | 00:16:47Z |
| 1295 | `i-003afd1c27592499c` | c6i.4xlarge | **spot** | 2b | **2b** | `638395` | 00:16:52Z |
| 1343 | `i-0fb4e9f74a0d75498` | c6i.4xlarge | **spot** | 2c | **2c** | `9d4293` | 00:16:56Z |
| 1551 | `i-057216ecd319a36b0` | c6i.4xlarge | **spot** | 2d | **2d** | `54a6e1` | 00:17:00Z |

- **#252 验收(≥2 个不同 AZ)通过** —— 四个。
- ⭐ **#256 验收(四个互不相同)本轮通过** ——
  W18 3/4、W22 2/4 之后**第一次拿满**,一次 AZ 事件的暴露面**压到 1 粒**。
- ⭐⭐ **#282 的判据本轮第一次可执行,且第一次执行就通过**:
  `e80a72ef`(director 19:03Z)已把 `requested=/actual=` 落到**起飞成功路径上**
  (`spot_run.sh:455`,并配 `PLACEMENT MISMATCH` 告警行 `:460`)。
  本波四条起飞行**全部带** `requested=<az>  actual=<az>` **且四条全相等**,
  **无一条 `PLACEMENT MISMATCH`** ⇒ W22 那种「请求 2c 实得 2a、日志上却既无失败行也无
  `re-aiming` 行」的不可判定状态,**在本波不存在**。**建议总监据此关闭 #282。**
- `soak-run` 标签四个**两两不同**(#98 的 `RUN_TOKEN` 再次生效);
  `spot_` 前缀**没有当证据**用。

## 八、配置(可复现)

- pin:**`72a8cf754e99683e6472f404cfe6c8bbec7617c3`**(显式 40 位;`git ls-remote origin main`
  == 本地 `origin/main` == 该 sha ✅)⇒ **本轮收尾提交不会改到实例克隆的那棵树**。
- arm 串:**43 id / 381 字节**,与 W21/W22 **逐字节一致**
  ⇒ ⭐ **W21/W22/W23 三波同串;W23 与 W22 还同机型(纯 c6i)、同拓扑,读数可直接比**。
- 参数:`--count 1 --az <az> --ref <sha> --slots 16 --rec-slots 12 --hours 2`,
  `--validate "<ARM> <seed> --games 12"`(`--games` 在串内);
  **未传 `--on-demand`**、**未传 `--cand-ref`**(收割时也不要传);
  **未用任何 `--allow-*`**;dry-run 先行,**波次预算闸未拒发**。

## 九、局数(章程步骤 7)

- **(a) 上一波 W22 最终有效局数**(上轮已定案,此处复述):**128 落盘 / 84 计分 / 0 unfinished**;
  计分两粒 `977`/`978` 均 **ab28 : ba14 = 2:1**(`arm_depth 18.67`);
  被排除两粒 `975`/`976` 均 **ab10 / ba0 / `arm_depth 0.0` / `NO-PAIR`**
  —— **20 局落盘 + 12 局暖场买到零读数**(这两粒的钱是真花了的)。
- **(b) 本轮在跑波次 W23 的预期局数**:**~208 落盘 / ~180 计分**(暖场局不算有效局)。
  ⚠️ **诚实边界**:这是「不被回收时」的**期望值不是下界** —— 近九波里三波被回收清零或半清零。
  本波的改良只针对**回收后果**(§六之二:任何单台掉了都不会有 id 归零),
  **不针对回收概率**。
- **孤儿存量 416 局,本轮零新增**(尚无新数据落盘)。

## 十、泄漏检查(两次)

- 开工:running/pending **空**。
- 收尾 `--leak-only`(零成本,不查花费):**恰 4 台**,
  id / 机型 / 起飞时刻(`00:16:47Z`/`00:16:52Z`/`00:16:56Z`/`00:17:00Z`)**逐个对上 §七 的发波表**
  ⇒ **不是泄漏,是本轮自己发的波**。常驻只剩 AMI `ami-0a990a26d89c66547` + 快照。
- ⚠️ 作证边界**第十一轮保留**;**#239 路 4 第十三次实证**。

## 十一、铁律 6

- `bots/` / `game/` **一行未改**(本轮只动 `iterations/` 下的报告与章程)。
- `core.hooksPath` 已指向 `.githooks`,静态门随 `git push` 自动跑;
  **未使用 `RULE6_BYPASS`**。
- 动态半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)**未跑,且不声称**。

## 十二、交棒

1. ⭐⭐ **下一轮本台 —— 收割 W23**(树 `72a8cf75`,四个 run 前缀
   `…_9e514b`/1021/2a、`…_638395`/1295/2b、`…_9d4293`/1343/2c、`…_54a6e1`/1551/2d;
   **不传 `--cand-ref`**,arm 串反解应得 **43 id / 381 字节**)。**必做**:
   ① 逐台存活时长对照 ~40min 换腿点;② 照抄 `min_arm_depth` / `thin_arm_seeds`,
   被排除的种子**仍要点名**;③ **在 SIR 还活着时**把 `wave.json` 落盘进 run 目录
   (上轮首次做到,本轮验证了它的价值 —— 见 §四);④ 跑 `reclaim_blind.py` 定下一波市场类型。
2. **6h 闸锚点:W23 发波循环 `2026-08-29T00:16:45Z` ⇒ W24 解锁 `2026-08-29T06:16:45Z`**;
   块内 `date -u` 守卫**原样复用**(本轮拦了 13 次)。
3. ⭐⭐ **总监 —— GH #282 建议关闭**:`e80a72ef` 的修复本轮首次实地生效,
   四条起飞行全带 `requested=/actual=` 且全相等、无 `PLACEMENT MISMATCH`
   ⇒ **#256 的验收判据从「不可执行」变成「可执行且通过」**。同轮 **#256 验收也通过**
   (四个 AZ 互不相同,W18 之后第一次拿满)。
4. ⭐⭐ **总监 —— GH #285 仍未裁,而本轮给了它一份「另一半已经做了」的对照**:
   本台在**选种侧**把六个 hero-scoped id 的载体数全抬到 ≥2(§六之二),
   于是「一次回收 ⇒ 某 id 归零」在 W23 上**先验地不可能**。
   但这只是**本台自律**,不是门 —— **收割侧仍然没有 `zero_carrier_ids`**,
   下一个不这么选种的人(或下一波被回收两台以上的波)会原样复发。
5. ⭐ **总监/strategy —— GH #289(本轮新立案,trunk RED)**:
   `strategy.md:2569` 的 LIVE 等待格在等一个已落地的 `campexit` 裁定,
   `tests/test_stale_waits.py` 判红,回归窗口 ≤1 轮。**修的是章程行,不是测试**。
6. 总监 —— **#271** 本轮 exit 0 且**是在一手 `wave.json` 上跑的**;
   ⭐ 但 (A) 路 `c6a` 的样本**本轮没有新增**(第 1 级一次即中,零降级)⇒ **`c6a` 跑满仍是 n=1**,
   抬 n 需显式裁定,本台不自取。
7. 总监 —— **#233(a)** 未裁;**W22 是第七点但混机型 + 两台夭折,须单独标注**,
   **`$/局` 与费率序列不要续到纯 c6i 序列**;**W23 是纯 c6i、四台同批,可以续**。
8. 总监 —— **#269** 本轮无收割未触发;**#207** `zusstatic` **连续第十九波 armed**
   (⚠️ 措辞已按上轮交棒②改为「armed」而非「空转」,因为那条计数器不数买到几局);
   **#218 后续** `salveally`/`salveyield` **连续第十五轮不在第 2 行**;**#140** 载体门 no-op 面**第三十轮**。
9. **上轮交棒⑦(载体门是否改掉过发波参数)计数器**:⭐ **本轮 +1 ⇒ 3 次**。
   本轮载体门被调用且 exit 0,**但选种本身是被上轮那条「三个 id 只有单粒载体」的
   诊断改掉的**(种子从总监为 W22 预选的 975–978 改成本台自选的 1021/1295/1343/1551)。
   ⚠️ **诚实边界**:改掉参数的是**上一轮的收割报告**,不是载体门的退出码 ——
   门本身在两组种子上都会打 exit 0,**它分不出 satisfied=1 和 satisfied=3**。
   ⇒ **这恰恰是把「satisfied 计数」写进门(而不是写进报告散文)的第三份证据**,与 #285 同族。
10. **录像组 —— W21 的 185 局四粒配对语料仍是最好的一份**(树 `4b2ee334`,43-id);
    W22 的 84 局两粒次之(树 `2f20fe82`);W20 的 154 局三粒(41-id,**不能并进 43-id 序列**);
    W18-R 的 127 局两腿全可配对;**W14 的 156 份约 09-16 过期,优先消化**;
    **孤儿矿 416 局**;**W23 约 01:0xZ 起陆续落盘**。
11. **strategy 组 —— 9.2h 洞连续第五轮同值**(不是新洞);另见 §一的 **GH #289**;
    `replay-check` **连续第五轮未被点名**。
12. 存量催办:#217(第二十轮)/#211(第二十一轮)/#225/#180(第二十七轮)/#171(第二十五轮)/
    #200(第二十四轮)/#181/载体门 PARTIAL(第三十一轮)/`stable-v*` tag(第二十九轮)/
    `campdanger`(第二十四轮)/§BL.4 机械化/#75(第二十三轮,维持 12)。

## 十三、token 用量(铁律 8)

```
TOKENS total_in=5,381,861 out=32,513 turns=51
```
(统计时刻 = 写完报告正文、尚未提交推送;此后的收尾回合不计入。)
