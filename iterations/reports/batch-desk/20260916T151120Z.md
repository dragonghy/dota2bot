# 批测台 2026-09-16T15:11Z —— 刹车第三十四轮持有;零发波、零收割、零泄漏

**一句话**:MTD **`$91.448`** / 戳 **`2026-09-16T11:07:38Z`** —— **与上一轮(12:13Z)两量逐位相同**
⇒ ⛔ **本轮没有新的支出测量**(快照冻结),headroom 对 `$90` = **`−$1.448`**,
闸 `WAVE_FENCE: THROTTLED (exit 3) -- brake` ⇒ 连续第三十四轮零发波;GH #779 owner **第二十五轮零表态**。

**⭐⭐⭐ 本轮头号产出在 §三,不在成本那几行**:上一轮(12:13Z)立案的 `dem21/` 订正,
**在它的第一个可检验点上完全闭合了 —— 字节数与对象数两侧都逐位对上**
(Δ `11,035,155,647 B` / Δ `769` 个对象,**全桶那一侧的 Δ 与 `dem21/` 那一侧逐位相同**)。

**⛔⛔ 而同一份读数带来一条必须当场钉死的陷阱,方向是「让下一轮把一条正确的订正读成错的」**:
本轮**实测**的 `dem21/` 是 **`117.02 GB ⇒ $0.090/天`** ——
**与 09-15T00:14Z 那个被上一轮判为「投影值不是测量值」的数,两级都逐位相同**。
⭐ **上一轮那条订正仍然成立**;两个数今天相等**不是互证,是时间差恰好走完了**。
📌 平时抓这类缺陷靠的是「两个数对不上」,**这一次那条线索没有了** —— 判别子只能是**时间戳**(§三.3)。

---

## 一、成本(RULING 48 三段式)

**`零 EC2 / 零 CE / S3 读取 0 个对象(出网未计价)`** —— 本轮**一次 `s3 cp` / `s3 sync` 都没跑**,
`.dem` / `analysis.json` 一个没下。S3 调用**全部是 LIST**(三次 `s3 ls --recursive`:
`validation/` 一次、`dem21/` 一次、全桶一次)。
⚠️ 照已登记的诚实边界:LIST 请求在 `<N>` 里恒为 0 ⇒ **`<N>` 只会低估真实 S3 用量、永不高估**。

`AWS_SETUP_EXIT=0`(`arn:aws:iam::581436534924:user/dota2bot-agent`)。
`check_costs.sh`(**`COSTS_EXIT=0`**,⛔ 未走管道)逐字:

```
91.448	USD
   source: budgets/dota2bot-batch (free) | forecast 102.657 | budget limit 100.0 | budget refreshed 2026-09-16T11:07:38Z
   accrual probe (GH #779): alpha = 0 accruing instance(s) account-wide; beta = 0 wave record(s) after 2026-09-15T23:49:38Z
   stamp unknown (fresh container) -- alpha and beta are both zero -- a frozen stamp over an idle account opens no blind spot
   >= $35, but headroom to the $90.00 brake is $-1.448
   < $1.10 (cheapest wave) — CE confirmation SKIPPED, NOT passed (GH #801).
   Nothing can launch at this MTD, so confirming it buys nothing.
   Resumes by itself as soon as headroom >= $1.10.
```

⚠️ **这是 SKIP 不是 pass**;⛔ 欠条 `gh801_confirm_headroom_first_live_read` **仍未结清**
(headroom 为负,走的还是 SKIP 分支);⛔ **未用 `COST_CONFIRM_AT=999` 自救**,用默认值跑。

**⭐ 快照冻结,而这一条要按交棒 ⑤(e) 的规矩写**:MTD `$91.448` 与戳 `2026-09-16T11:07:38Z`
**与上一轮(12:13Z)两量逐位相同** ⇒ ⛔ **本轮不算任何空转速率**
(⑤(e) 逐字:速率只用间隔 ≥ 11.3h 的两个端点算)——
**两个端点是同一张快照,不是一个短段,是零段。** ⛔ 不动 `$0.40–0.92/天` 带、⛔ 不动 9/30 区间。

## 二、闸 (iii)(`--planned 1.10`)

**`FENCE_EXIT=3`**。逐字:

```
actual (MTD)     : $91.448   <- re-read this run, never cached
pending waves    : $0.000   (launched, may not be in MTD yet)
planned          : $1.100
projected total  : $92.548
fence            : $100.00   <- lowest ACTUAL alert not yet crossed (Ruling 1)
brake            : $90.00   (owner's line, not derived here)
operative ceiling: $90.00   = min(fence, brake)
A launch at this instant would put MTD past the $90.00 BRAKE line.
WAVE_FENCE: THROTTLED (exit 3) -- brake, not fence. Stop and report to the owner; do not wait it out.
```

RULING 5/6/7 三行各自照抄:
`accrual scope    : 17 region(s) read [...]  <- COMPLETE: every region this account has enabled` /
`wave accrual     : records after 2026-09-15T23:49:38Z (= 2026-09-16T11:07:38Z - 11.3h lag, clock from budget snapshot)` /
`accrual check    : CERTIFIED (0 accruing instances account-wide, read this run) -- pending $0.000 is a reading, not a default.`
⭐ **钟没有降级**(`clock from budget snapshot`,不是容器时钟)。
`crossing registry: 0 record(s) ..., none in force` + `1 retired record(s) archived in _retired`(RULING 15 传导正确)。
⛔ 未传任何 `--no-*` / `--pending` / `--director-crossing` ⇒ **无一行「这是 SKIP 不是 pass」**。

⚠️ 照登不藏:`$90`–`$100` 之间在 AWS 侧**仍可花**(`$100` 告警 `state=OK`),**挡住它的是 owner 这条线不是 AWS**。
⇒ **连续第三十四轮,不发波的原因是闸不是本台的处置。**

## 三、⭐⭐⭐ 工作单元:上一轮的 `dem21/` 订正在第一个可检验点闭合 —— 两侧逐位

### 三.1 一手读数(⛔ 不靠一个数自证:本台 `awk` 求和 + 服务端 `--summarize`)

| 量 | 上一轮 12:13Z | **本轮 15:11Z** | Δ |
|---|---|---|---|
| `dem21/` 对象 | `6,056` | **`5,287`** | **`769`** |
| `dem21/` 字节 | `128,059,945,202` | **`117,024,789,555`** | **`11,035,155,647`** |
| 全桶对象 | `81,197` | **`80,428`** | **`769`** |
| 全桶字节 | `178,057,062,942` | **`167,021,907,295`** | **`11,035,155,647`** |

⭐⭐ **两个 Δ 逐位相同(字节与对象都是)** ⇒ **过去 ~3h 里全桶唯一的变化就是 `dem21/` 少掉的那一批**,
**别处零新增、零删除** —— 顺带是「零发波、零收割、零上传」的第三条独立证人(前两条是 §四 与 §五)。

⭐⭐ **那一批逐位等于上一轮登记的 08-25 档**:上一轮 §(3.3) 登记该档 **`11.04 GB`**
(本轮 Δ = `11,035,155,647 B` = **`11.04 GB`**),§(3.2) 登记「现存最早是 08-25 的 **769 个**」
(本轮 Δ = **`769`** 个)⇒ **两条独立的核对各自命中,一条对字节一条对个数。**

**闭环算术**(上一轮 §(3.3) 的逐日表全量 `134.47 GB`):
`134.47 − 6.45`(08-24 档,09-14 到期)`− 11.04`(08-25 档,09-15 到期)`= 116.98`,
本轮实测 **`117.02`**,差 **`0.04 GB`** —— **正是上一轮自己已经识别过的那个进位**(它当时在 `134.47 − 6.45 = 128.02` vs 实测 `128.06` 上同样是 `0.04`)。
⇒ ⭐ **两轮读数在「逐档过期」这个假设下完全对齐,⛔ 不需要第三个解释。**

### 三.2 ⭐ 逐日表逐位复核:四档命中,说明两轮量的是同一台仪器

本轮逐日直方图(`awk` 按 GiB 汇总后换算成十进制 GB)对上一轮登记值:

| 档 | 到期日 | 上一轮登记 | 本轮实测 | |
|---|---|---|---|---|
| 08-25 | 09-15 | `11.04 GB` | **不在桶里** | ⭐ 本轮扫掉 |
| 08-26 | **09-16(今天)** | `12.01 GB` | **`12.00 GB`** | ⚠️ **已到期仍在桶里** |
| 08-27 | 09-17 | `14.81 GB` | **`14.81 GB`** | 逐位 |
| 08-28 | 09-18 | `12.42 GB` | **`12.42 GB`** | 逐位 |
| 08-29 | 09-19 | `10.50 GB` | **`10.50 GB`** | 逐位 |

⇒ **三档逐位、一档差 `0.01`(进位)** ⇒ 本台的 GiB→GB 换算复现了上一轮那张表,
**两轮不是各说各话,是同一台仪器的两次读数**。

**清扫滞后,n=3,⛔ 仍不写成常数**:
- 08-24 档(09-14 到期):09-16T12:13Z 已不在 ⇒ 滞后 **≤ ~2 天**(窗口太宽,⛔ 钉不住);
- ⭐ **08-25 档(09-15 到期):12:13Z 在、15:11Z 不在** ⇒ 滞后落在 **`[21h, 24h]`**
  —— **这是迄今唯一被钉进 ~3h 窗口的一次**;
- 08-26 档(**今天**到期):15:11Z **仍在** ⇒ 滞后**至今 ≤ 15h,还在走**。

⚠️ **这是观察不是故障判定**,⛔ 不主张 lifecycle 坏了;⛔ **n=3 且只有一个被紧确界住,不足以把「约一天」写成常数。**

### 三.3 ⛔⛔ 必须当场钉死的陷阱:`117.02` 在两处出现,epistemic 地位相反

- 09-15T00:14Z 章程 `:12214` 登记:`dem21/` **`117.02GB`** ⇒ **`$0.090/天`**
  —— 上一轮 §三 判定它是**投影值**(`134.47 − 17.49`),⛔ **不是测量值**;
- 本轮 15:11Z **实测**:`117,024,789,555 B` = **`117.02 GB`** ⇒ 同口径(`$0.023/GB·月 ÷ 30`)= **`$0.090/天`**。

⭐⭐ **两级都逐位相同(尺寸和每日成本),而它们回答的不是同一个问题。**

**上一轮那条订正仍然成立,理由是算术不是立场**:那个投影值预言的是「lifecycle 追上之后桶会剩多少」,
而**存储费按每一天实际存着的收**。09-15 到 09-16T12:13Z 之间桶里真实存着的是 **`128.06 GB`**,
登记值 `117.02` 在那整段时间里**低报 ~8.6%–13%**。
⇒ **投影值的内容今天兑现了,它的时点晚了约一天半,而计费只认时点。**

⛔⛔ **失效方向(为什么必须写在这里而不是脚注):**
下一轮或下个月有人翻章程,会在 `:12214` 和本条目里读到**同一个 `117.02` 和同一个 `$0.090/天`**,
最省事的结论是「**订正搞错了,12:13Z 那轮的 `128.06` 才是异常**」—— **那个结论是错的**,
而**平时抓这类缺陷靠的线索(两个数对不上)这一次不存在**。
⇒ **唯一的判别子是时间戳**:`117.02` 在 09-15 是**投影**、在 09-16T15:11Z 是**测量**,
中间差的那件事(08-24 与 08-25 两档被扫掉)**正是那个投影已经预支掉的**。
📌 与章程反复点名的「一个真命题被用来满足另一个问题」同族,**只是这一次两个答案在数值上重合了**
⇒ 本台的建议仍是交棒 ②(总监未裁):成本行注明 `measured` / `projected`,
⭐ **本轮给了那条建议一个它先前没有的论据** —— 单靠数值,这两者在今天**不可区分**。

### 三.4 存储/天重算(同口径 `$0.023/GB·月 ÷ 30`)

| 量 | 上一轮实测 12:13Z | **本轮实测 15:11Z** | Δ |
|---|---|---|---|
| `dem21/` | `128.06 GB` / `$0.098` | **`117.02 GB` / `$0.090`** | `−$0.008/天` |
| 全桶 | `178.06 GB` / `$0.137` | **`167.02 GB` / `$0.128`** | `−$0.009/天` |

⛔ **不据此动任何区间**:`$0.009/天` 是空转速率量级的 ~1–2%,而那条带的不确定性来自速率样本本身
(且**本轮快照冻结,连一个新速率点都没有**,见 §一)。
⭐ 上一轮的**结论**(「存量这条路总共只值 `$0.09/天` 且在自走完,速率上能动的只有 egress」)
**不受影响且本轮被加强**:存量正在按档自己走掉,**本台一次都没动过它**。

### 三.5 ⛔ 订正交棒 ⑤(g) 自己的预测(它的减数写错了,不是它的方法)

⑤(g) 逐字预测下一轮全桶是 `178.06 − 12.01 − 14.81` —— 它**默认 08-25 那档已经走了**,
而上一轮自己的 §(3.5) 明写它**还在**(`11.04 GB`,到期 09-15)。
实际走掉的是 **08-25**(`11.04`),**不是** 08-26 ⇒ 实测 `167.02 = 178.06 − 11.04`。
**修正后的下一轮预期**(⛔ 是预期不是读数,下一轮请照 (f)/(g) 实测):
- 若 08-26 档(`12.00`,今天到期、15:11Z 仍在)在下一轮前扫掉 ⇒ 全桶 **≈ `155.0 GB`**;
- 09-17 之后再加 08-27 档(`14.81`)⇒ **≈ `140.2 GB`**;
- ⛔ **若实测不是这两个量级,先核自己这一轮的数是不是投影值**(⑤(g) 这半句是对的,原样保留)。

## 四、收割:无可收(⛔ 「没得收」不是「漏收」)

`s3 ls validation/ --recursive`:**`S3VAL_EXIT=0`**、**`OBJ_COUNT=592`**,与已登记的 592 **逐位相同**
⇒ 零新 verdict、零下载。最新对象按 **RULING 47 取法**(`--recursive | sort | tail -1`,⛔ 不是裸 `| tail`):
`2026-09-12 10:18:26      15187 validation/lf_rescue+27ids-d177b87026dd_20260912_1018_run.log` ⇒ **仍是 W69**。
`recover_verdict.py` **未跑**。
`alpha = 0` 台在计费、`beta = 0` 条波次记录(cutoff `2026-09-15T23:49:38Z`)、`pending waves : $0.000`
且逐字 `CERTIFIED ... is a reading, not a default` ⇒ 无在飞波次、无未收割 run。
⛔ **交棒 ⑤(c) 本轮不触发**(`beta = 0`,故 `soak/` 未单独列)。
⚠️ `--ledger` 接线**本轮又未被行使**(没有波可收)⇒ 欠条 `games_ledger_cross_wave_accounting` **仍 OWED**,
⛔ 未为回填下载任何对象。
⭐ **§三.1 给了这一节一条独立旁证**:全桶 Δ 与 `dem21/` Δ 逐位相同 ⇒ `validation/` 在过去 3h **一个字节都没动**。

**局数(铁律「报告必须带局数」)**:(a) 上一波次 = **W69**(`20260912_1018`),自 09-12 起无新波
⇒ 本轮无新的 per-seed / per-side 局数可报,⛔ 不重抄历史数字冒充本轮读数;
(b) 本轮在跑波次 **0 个** ⇒ 实时进度 **N/A**。

## 五、泄漏:五条独立路径全零(⛔ 不合并成一句)

① `check_costs.sh` 自带「running/pending instances」区块 —— **空**;
② `describe-instances` 五态(pending/running/shutting-down/stopping/stopped)、**不加 tag 过滤**
⇒ **`INST_EXIT=0`**、**`LINES=0`**;
③ 账户级 **17 区 `COMPLETE`**、闸独立复核 `alpha = 0 accruing instance(s)`、`CERTIFIED`;
④ AMI 仍 `ami-0a990a26d89c66547`(`dota2bot-batch-20260718`)**一张**(常设成本,非泄漏);
⑤ 闸的 `pending waves : $0.000`,且**是读数不是默认值**(逐字)。
⚠️ 本轮**未**买账单侧那第六条(逐日 `EC2 - Compute`),理由同 GH #801 四行(headroom 为负,买不到决策)。

**`bots/Customize/soak_side.lua` 在本容器不存在**(现跑 `ls` ⇒ `No such file or directory`,
**`SOAKSIDE_LS_EXIT=2`**)—— 交棒 ⑤(b) 的发波前检查,本轮零成本先做掉;⛔ 下轮仍要重做(容器局部)。
⭐ 与 GH #848 / #856 并读:这是**第六个**说那条红是容器局部遗留文件、**不是 trunk 红**的证人。

## 六、⛔ 浅克隆纪律(交棒 ⑤(f))

开工即跑:`git rev-parse --is-shallow-repository` ⇒ **`true`**,`git rev-list --count HEAD` ⇒ **`50`**。
⇒ **本报告不含任何「`git log` 顶是 X」式的历史读数。**
⛔ 本轮**未**核 GH #850(没有新的一手现场,且上一轮刚按**文件内容**核过并逐字保留了交棒 ⑦(d));
⛔ 不执行、⛔ 也不退休该交棒,**逐字结转**。

## 七、GH #779:owner 第二十五轮零表态

本轮现读(`mcp__github__issue_read`,一次调用):`state=open`、**`comments=17`**、
**`updated_at 2026-09-16T06:22:00Z`** —— **三个值与上一轮逐位相同**,
而那第 17 条**是本台 09-15 自己发的** ⇒ ⛔ **不得读成 owner 有动作**。
三条路(维持 `$90` / 给预算加过滤器 / 一次性解禁到某个数)**一条都还没选**。⛔ 本台无权自裁、不预选。

⛔ **本轮未在 #779 追评,也未推 owner 通知** —— 理由是判据不是省事:
已登记的通知/追评判据是「MTD 与预算戳有没有动」,**本轮两量逐位冻结**(§一)
⇒ **零新支出测量、零新算术** ⇒ 一条评论只会稀释 owner 那张桌子。
本轮的实质产出(§三)是**本台自己的记账闭合**,不是 owner 要定夺的事。

## 八、铁律 11 / 开工自检

**铁律 11**:MCP 可用(`issue_read` 一次,`requires approval` **零次**),**零空转**。

**⛔ 自捉(方向对本台不利,照登):交棒 ⑤(a) 的字面模板第十一轮仍未照抄。**
第一条命令是 `bash tools/agent/routine_selfcheck.sh 2>&1 | tail -40`,**带管道**,当场被拒,逐字:

```
REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.
This is evidence discipline 3; it has recurred 5x, every time as the first command of the round.
```

第二条改成 `timeout 400 bash ... > /tmp/sc.log 2>&1`,**又被拒一次**,逐字:

```
REFUSED: routine_selfcheck.sh is running under `timeout`; exit 2, nothing checked.
REFUSED: an ancestor process is `timeout`, so this run would be KILLED mid-leg rather than finishing.
```

第三次 `nohup ... > /tmp/sc.log 2>&1 &` 跑起来了(pid **1577**)。
⇒ ⭐ **与上一轮逐字同形:两道自卫腿各自工作了,不工作的仍是本台的第一条命令** —— **十一轮十次**。
📌 `exit 2 =「没跑成」不是「没发现」` ⇒ 前两次什么都没检查。
⚠️ 脚本自陈计数仍是 `5x`(连续第四轮读到 `5x`)⇒ 计数器不跨容器,⛔ 本台只登记不解释。
⭐ **等待用的是上一轮立的修法**(`ls /proc/1577`,**等 PID 不等命令串**)⇒ ⛔ **本轮零个永不退出的等待壳**,
上一轮 §八 第三条自捉的那个形状**没有复发**。

**开工自检:⛔ 写本节时仍在运行**(`/proc/1577` 存活,日志 568 行,停在 `=== trunk health (python test suite) ===` 之后)
—— 那是 **python trunk 腿正在跑**,⛔ **不是「它没跑完」**;「tail 里没有」不是「它没产生」。
⇒ ⛔ **本节不写 `SELFCHECK_EXIT`、不写 trunk 绿也不写 trunk 红**;**腿级判词见补记**。
已跑到并可照抄的两条:`push gate armed (core.hooksPath=.githooks)` /
`unlanded work (pushed to a branch, never landed)` 区块**空**。
⚠️ 上一轮实测该自检跑了 **>40 分钟**(章程写的是「约 20s」)—— 本轮至此已 **>40 分钟**,
⛔ 本台**不归因**(容器慢 vs 检测器集长大,`wall clock cannot separate` 那条限度仍成立),**只登记这个对照点**。

**本轮 `bots` / `game` / `tests` / `tools` 一行未改** —— 只动 `iterations/` 的报告与章程。

## 九、交棒

1. ⭐⭐⭐ **owner —— 刹车第三十四轮持有,GH #779 第二十五轮零表态。** 三条路一条都没选。
   ⛔ 本台无权自裁、不预选。⛔ **本轮未推通知、未追评**:MTD 与预算戳**逐位冻结**,零新事实(§七)。
   ⚠️ 并读章程 `:12101` 已立的算术:**MTD 单调上升 + brake 是常数 ⇒ owner 表态前,
   九月剩余天数没有任何算术路径发出下一波**。
2. ⭐⭐ **总监 —— 交棒 ②(成本行注明 `measured` / `projected`)本轮获得一条新论据,请裁。**
   §三.3:本轮**实测**的 `117.02 GB / $0.090per天` 与 09-15 那个**投影值两级逐位相同**
   ⇒ **单靠数值,投影与测量今天不可区分**;⛔ 平时那条线索(两数不符)这一次不存在。
   **这不是重复上一轮的请求,是它先前缺的那个论据。**
3. ⭐⭐ **总监 —— §三 的闭合读数请收**:两侧 Δ 逐位(`11,035,155,647 B` / `769` 个对象),
   逐日表四档逐位复核,清扫滞后 **`[21h, 24h]`**(⭐ 迄今唯一被钉进 ~3h 窗口的一次)。
   ⛔ **n=3 且只有一个紧确界,本台不把「约一天」写成常数。**
4. ⭐ **总监 —— `s3_attribution_gap_remeasure_under_ruling48` 触发判据请重裁(连续第九轮未获回应)。**
   ⛔ **本轮仍读不到分母**:闸自己打的 cutoff 是 `2026-09-15T23:49:38Z`,
   而第一个干净的裁定后整日是 **09-16** ⇒ **最早可读仍是 `~09-17T11:20Z`(明天)**。
   状态仍 **OWED**,⛔ 本台不自行改欠条(先例 GH #33)。
5. **总监/协同组 —— GH #850 仍未落地**,交棒 ⑦(d) **逐字结转**(⛔ 本轮未重核,见 §六)。
   GH #852 / #856 本轮**未追评**(无新的一手现场,⛔ 不拿无新信息的评论稀释)。
6. **下一轮本台**:
   (a) ⭐ **开工第一条命令**:`nohup bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1 &`
       —— ⛔ **无管道、⛔ 无 `timeout`,一次到位**(十一轮十次未做到);等它用 `ls /proc/<pid>`,**等 PID 不等命令串**;
   (b) `soak_side.lua` 发波前检查**重做**(容器局部);
   (c) 若某轮 `beta ≠ 0`,`soak/` 必须列;
   (d) ⑦(d) 逐字结转;
   (e) 空转速率只用间隔 **≥ 11.3h** 的两个端点算;**快照冻结的轮次一个点都不算**(本轮就是);
   (f) 任何「`git log` 顶是 X」式的读数,先打 `git rev-parse --is-shallow-repository`;
   (g) ⭐ **按 §三.5 的修正预期核 `dem21/`**:08-26 档(`12.00`)扫掉则全桶 ≈ `155.0 GB`;
       09-17 后再减 08-27 档(`14.81`)≈ `140.2 GB`。⛔ 不合则先核自己这一轮的数是不是投影值;
   (h) ⭐ **新增**:引用章程 `:12214` 的 `117.02` 时**必须带时间戳**(§三.3)——
       那个数在 09-15 是投影、在 09-16T15:11Z 是测量,**数值不区分,时间戳区分**;
   (i) 刹车解除(headroom ≥ `$1.10`)时按规格发 P4.1 标尺波、**收割必带 `--ledger`**,
       并抄 GH #801 四行**恢复后**的样子(那才结清 `gh801_confirm_headroom_first_live_read`)。

## 十、铁律 9

**P4.1 upstream 标尺波连续第三十四轮欠**,⛔ **唯一阻因是闸 (iii) `exit 3`(预算)**,
**不是本台未认领、也不是排期。**

## 十一、token 用量

见补记(由 `tools/agent/token_usage.py` 在收尾时现跑)。

---

## 补记(push 之后写,GH #290 顺序)

**(A) 铁律 6 三条腿,逐字照抄**(`.githooks/pre-push`,开工自检已上膛 `core.hooksPath=.githooks`)。
⛔ **未用 `RULE6_BYPASS` / `--no-verify` / `core.hooksPath=/dev/null`** ⇒ **无一行「这是跳过不是通过」**。

分支 push(`PUSHBR_EXIT=0`,`* [new branch] claude/great-sagan-odo6pp`):

```
luacheck bots game: 0 warnings
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 127 ran, 0 findings, 0 uncertifiable, 47.7s
lua gate: SKIPPED BY SCOPE -- this push touches no bots/game/tests path.
```

⚠️ **两条限定照抄不藏,⛔ 都不是「全绿」**:
- `lua gate` 那行**自己就声明了**它是什么:`This is a SCOPE decision, not a pass and not a skip of a
  gate that had something to say (GH #624)`,并逐字列出 `2 path(s) offered, none of which can move a
  Lua test's answer` ⇒ **本轮的「lua gate 没红」不是一条关于测试体的陈述**(与上一轮那次真跑 389 个
  测试体、744.1s 的读数**不可比**)。
- `py gate` 的 `127 ran` 里有 **3 条靠 fail-open 捡进来的**,逐字
  `3 new test(s) not in the manifest were run anyway, costing 1.40s: tests/test_carry_item_issue_state.py
  tests/test_citation_audit_forward_ref.py tests/test_lua_gate_budget_backstop.py`
  ⇒ `=> the hook's REAL cost this run is 47.71s against a 90.0s budget.`
  ⛔ 本台**未**动 manifest 成员资格(不是本台的域,GH #616 约束 1 要求按实测秒数登记)。

`main` push(`PUSHMAIN_EXIT=0`,逐字 `23492bcc..3d468a34  HEAD -> main`)走的是 **memo 复用路径**,逐字:

```
RULE6_MEMO=REUSE  a green three-leg reading taken 2026-09-16T15:16:30Z on THIS EXACT tree.
  This is a REUSE, not a skip: all three legs really ran, on
  HEAD^{tree}=89feb4854e5820e855534bff1b930b05d8542b37 with origin/main=23492bcc21cd43ea108fd0e3f4bc4848efa3306b
  and a clean working tree.
```

**独立复核(⛔ 不靠 push 回显)**:`git rev-parse HEAD` = `git ls-remote origin main` =
**`3d468a34c1d01e0f7759d927240b0f07fc581761`**。

**⭐⭐ (B) 上一轮 §十 那条假红竞态本轮未复发 —— 而这不是它被修好了的证据。**
上一轮**同一条路径上两次**被 `stale_write_census.py` / `test_call_form_census.py` 的裸 `open()` 拒掉
(根因:开工自检的 `test_lua_corpus_stability.py` 往真实工作树短暂写 `bots/Customize/soak_side.lua`)。
本轮**两次 push 都一次过**,`py gate: 0 findings`。
⚠️⚠️ **诚实边界,写在结论旁边而不是脚注里**:本轮两次 push 时开工自检**都还在跑**(`/proc/1577` 存活)
⇒ **写方仍然在场** ⇒ ⛔ **这一次绿不能读成「树是安静的」,只能读成「这一次没撞上那个毫秒级窗口」**。
📌 这正是上一轮 (10.5) 登记的失效形状:**窗口是毫秒级 ⇒ 间歇 ⇒ 不复现时看起来像「上次那轮手滑」**。
⇒ **GH #856 仍然立着**,⛔ 本轮**未追评**(一次「这次没撞上」不是新的一手现场),
⛔ **不主张**那 10 个同形状候选里任何一个被修好。

**(C) 开工自检**:写本节时仍在推进(日志 `568 → 608` 行,`/proc/1577` 存活)
⇒ **腿级判词见补记之二**;⛔ 在拿到之前不写 `SELFCHECK_EXIT`、不写 trunk 绿也不写 trunk 红。

## 十一、token 用量(本节替换正文占位)

`TOKENS total_in=3,560,838 out=31,115 turns=33`(统计后的收尾回合不计入)。

⭐ **对照上一轮**:`31,354,219 in / 101,157 out / 163 turns` ⇒ 本轮 **回到本台常态区间**
(近几轮 `3.6–3.9M in / 35–38k out / 34–35 turns`)。
**归因可指认**:上一轮那个数的大半来自**本台自己造的 7 个永不退出的等待壳**(`pgrep -f` 自指),
本轮按上一轮立的修法**等 PID 不等命令串**(`ls /proc/1577` / `while [ -d /proc/1577 ]`)
⇒ **零个自指等待壳** ⇒ ⭐ **那条修法在它的第一个使用点上就把回合数打回了常态**,
这是它有效的**第一份读数**,⛔ 不是一般性结论(n=1)。
