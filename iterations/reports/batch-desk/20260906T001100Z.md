# 批测台 2026-09-06T00:11–00:4xZ — 收割 W49(4/4 干净,零抢占);本轮不发波(闸 (i) THROTTLED)

**一句话**:W49 四粒全部落地、零抢占、gpm **+11.70**;但本轮真正该被读的是账单 ——
**上一轮预言的「headroom 被别人的项目吃掉」已经落账了**:`c7a.16xlarge` 从零变成 **$7.995**,
是本轮 MTD 全部涨幅的来源,而本台自己的 `c6i.4xlarge` 那一行 **逐位没动**。

本轮 AWS 支出:**$0.01**(一次 CE 分组查询,取的是硬证据)。未发波。

---

## 一、自检(铁律 10)

- ⭐ **上一轮交棒 ④ 本轮照办**:开工第一条命令**重定向不管道、放后台**
  (`bash tools/agent/routine_selfcheck.sh > …/sc.log 2>&1`)。⚠️ 但**第一条命令仍然踩了**:
  第一次调用写成了 `... | tail -60`,脚本**自拒**并逐字点名
  (`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked` /
  自陈「evidence discipline 3,已复发 5 次,每次都是当轮第一条命令」)——
  这是本台**第八次**。那个 `EXIT=2` 是 **REFUSED,不是通过**。
  第二次改重定向 + 后台,真码见第九节(收尾补记)。
- 本会话对 `bots/` / `game/` / `tests/` / `tools/` **一行未改**,改动全在 `iterations/` 下。

## 二、成本与围栏(闸 (iii))

`AWS_SETUP_EXIT=0`(`arn:aws:iam::581436534924:user/dota2bot-agent`)。
`COSTS_EXIT=0`(经 `tools/agent/rc.sh` 取裸码,不经管道)。

| 量 | 读数 |
|---|---|
| 九月 MTD | **`$27.814`**(免费 `budgets`,`refreshed 2026-09-05T22:33:08Z`) |
| 上一轮同一读数 | `$19.597` ⇒ **涨 `$8.217`** |
| forecast | `150.822`(**forecast 不是读数、不进围栏**,只作旁证) |
| budget limit | `$100.00` |

**⭐⭐⭐ 本轮花 `$0.01` 把这 `$8.217` 拆开了**(章程允许:确认近刹车读数 / 需要分项)。
`ce get-cost-and-usage --group-by INSTANCE_TYPE`(09-01→09-06):

| 机型 | 本轮 | 上一轮 | 差 | 归属 |
|---|---|---|---|---|
| `c6i.4xlarge` | `16.5068` | `16.50675` | **+0.00005** | **本台** |
| `c7a.16xlarge` | **`7.9950`** | **`0`(账单上没有这一行)** | **+7.995** | **不是本台**(`final-table-trainer`) |
| `NoInstanceType` | `3.3127` | `3.0905` | +0.222 | 混合(EBS/传输) |
| **合计** | **`27.814`** | `19.597` | +8.217 | — |

合计与 `budgets` 的 MTD **逐位相同** ⇒ 拆分是完备的,没有第四项。

⛔⛔ **两条要写进结论的**:

1. **GH #505 的「燃烧率是本台的」那部分,从本轮起不再成立。**
   上一轮它还成立(账单上当时**没有** `c7a` 这一行)。本轮 MTD 的**全部**增量
   (`$8.217` 中的 `$7.995` + 大部分 `NoInstanceType`)**都不是批测花的**;
   本台自己那一行 `16.5068` 与上一轮**逐位相同**,即 W48 / Y1 / W49 **三波都还没落账**
   (章程写死的 4.3–11.3h 滞后)。
2. **围栏的失效方向仍是「以为还有余量」**,而现在它是**已发生的**不是前瞻的。

**闸 (iii) 读数**(工具现读,不抄常数;本轮虽不发波,这个数是第七节escalation 的量具):
`FENCE_EXIT=0` `--planned 1.02 --pending 3.13`(W48 spot `$1.02` + Y1 按需 `$1.09` + W49 spot `$1.02`)
⇒ projected `$31.964`,fence `$50.00`,brake `$90.00`,operative ceiling `$50.00`,
**headroom `$18.036`**;三档 ACTUAL 告警 `$50/$80/$100` **全部 NOT yet crossed**。
**裸 headroom(不发波、不计 pending)= `$50 − $27.814` = `$22.186`。**

⚠️ **`$0.90` 单波成本常数的自动失效条款,第四轮连续点名仍未重裁**(W46 实测 `$0.959–$1.022` 已越过)。
本台照旧在波次记录里登记**高的那个数 `~$1.02`**。

## 三、⛔ 一个本轮自己犯的读数错误,当场抓住并登记

收尾泄漏检查的**头三条 ad-hoc 查询用了 `--region us-east-2`**,而 `tools/batch_test/aws/aws.env`
写的是 **`AWS_REGION=us-west-2`**。三条查询各自返回**空**,而空看起来像答案:

- 「账号上没有 running 实例」← 假
- 「那台外来机已经不存在(`InvalidInstanceID.NotFound`)」← 假

改到 `us-west-2` 重跑,**全部翻转**(见第七节)。

⭐ **这与上一轮那个缺陷同形不同因**:上一轮是**过滤器**决定了问题
(`dota2bot-*` 的空回答的是「本台有没有泄漏」不是「那台还在不在」),本轮是**区域**决定了问题。
共同的形状是**「空输出被当成读数」**,而两次的伪装物不同 ⇒
**「查询的作用域必须和问题的作用域对齐」是比「别信空输出」更根的那条**。
`check_costs.sh` 本身**没有**这个毛病(它用 `$AWS_REGION`),中招的是本台手打的那三条。

## 四、⛔ 闸 (i) 本轮开局是 UNCERTIFIABLE,原因是上一轮的波次记录少写了一个键

`wave_throttle.py` 读 `machines[].launched_at`;**W49 的波次记录只写了 `launch_time`**。

```
UNCERTIFIABLE: W49 machine 0 (seed 5404) has no launched_at
gate (i) DID NOT RUN. That is not a pass -- do not launch on it.
WAVE_THROTTLE: UNCERTIFIABLE (exit 2)
```

**键名史(逐波实测)**:W47 `launched_at` ✅ / W48 **两个键都有** ✅ / W49 **只有 `launch_time`** ❌。
⇒ W48 那一轮的「两个都写」是**冗余掩护**,W49 这一轮把冗余那一份丢了,**而没有任何东西举手**
—— 直到下一轮(本轮)要用它。

**处置**:从**同一份记录自己的 `launch_time`** 逐机器复制出 `launched_at`(脚本复制,**不是手打**;
`launch_time` 本身来自发波时的 `describe-instances`),并写下 `launched_at_source` 说明这是收割期回填。
回填后闸 (i) **真的跑起来了**:

```
family candidate : W49  last machine 2026-09-05T21:20:05Z -> unlock 2026-09-06T03:20:05Z  <- ANCHOR
family candidate : Y1   last machine 2026-09-05T15:16:56Z -> unlock 2026-09-05T21:16:56Z
slate spread     : 8s        window: 6h
now              : 2026-09-06T00:15:49Z
margin           : 11056s SHORT of unlock
WAVE_THROTTLE: THROTTLED (exit 3) -- do not launch. Wait 11056s.
```

⭐ **失效方向是安全的**(exit 2 拒绝、不放行),**但它把闸变成了一次性的**:
没有工具写 `launched_at`(`grep -rn launched_at tools/` 除 `wave_throttle.py` 外**零命中**),
波次记录是本台每轮手写的 ⇒ **同一个漏写下一轮还会发生**。
已开 **GH #544 `[harness]`**(第八节),本台按章程**不自改 harness**。

## 四之二、⭐⭐⭐ 而且第四节那个漏写**有人举过手** —— 两条检测器在 `main` 上红着,都点名 W49,都归本台

自检的 python 腿本轮 **7 red**(上一轮 5 red),新增的两条恰好是:

| 检测器 | 红在哪 | 归属 |
|---|---|---|
| `tests/test_wave_throttle.py` | W49 记录缺 `machines[].launched_at`(第四节那件事) | **本台** |
| `tests/test_wave_gate_keys.py` | W49 的 `gates` 用了裸罗马数字键 `i/ii/iii/iv`,而 W47/W48 用的是长名(`(i) >=6h routine-wave throttle` 等)⇒ 5/258 检查全部点名 W49 | **本台** |

⭐ **这推翻了第四节那句「没有任何东西举手」**:举手的东西**存在且早就写好了**,
`test_wave_gate_keys.py` 的红字自己写着「Owner: the batch desk (it is the only seat that knows
whether the gate ran)」。**没被读到的原因是时序** —— 上一轮的自检跑在**写波次记录之前**,
而那一轮之后再没人跑过 python 腿。⇒ **这不是「缺检测器」,是「检测器和它要看的产物在同一轮里顺序反了」**,
与铁律 10 立案时那个「工具没人跑」同族。

**本轮修法(两条都归本台,当轮修完)**:

1. `launched_at` 从同一份记录的 `launch_time` 脚本复制(第四节)⇒ `test_wave_throttle.py` **55 checks, 0 failed**。
2. `gates` 的四个键改回**长名**,**值逐字照搬,一个字没编** ——
   `test_wave_gate_keys.py` 的红字自己写着「Do NOT invent a value to turn this green;
   that is the exact failure this file was written against」。
   顺带修正一处**归位错误**:上一轮写在 `iv` 里的那段其实是 **reclaim-blind 那一半**
   (`RB_EXIT=0 against W48_wave.json`),`inputs -- no harvest owed` 那一半在上一轮**只写在散文里**
   (报告第三节「收割:无对象」)⇒ 按 W48 那一轮的同一诚实措辞补键:
   **写明这是收割期补的、不是回填一个通过**。
   ⚠️ **这是同一形状连续第二波**(W48 一模一样地需要这次补键)⇒ **不是一次性失手**,已进 GH #544。
   ⇒ `test_wave_gate_keys.py` **263 checks, 0 failed**。

⭐ **按第一节那个结构性论证**(工作树在 `bots/ game/ tests/ tools/` 上与 `origin/main` 逐字节相同),
**这两条本来就是 `main` 的红,现在是 `main` 少了两条红** —— 修的是波次记录(`iterations/` 下),
`tests/` 一行未动。

**其余 5 条 python 红不是本台的**,逐名登记交总监:
`test_bots_walk_farm_only`(8 checks / 2 failed:一个 `tests/` walk 没排除 `bots/Customize/soak_*.lua`,
外加一条 `io.popen` 静态解析不了)、`test_detector_source_constants`、`test_lua_corpus_stability`、
`test_mutstand_restore_trap`、`test_wkqdmg_domain`。
⚠️ **这 5 条的名字与上一轮那 5 条不同**(上一轮:#497 / #514 ×2 / #506·#501 / `test_wkqdmg_domain`)
⇒ **红的集合在动,不是同一批红一直挂着**,不能按「老面孔」略过。

## 五、⭐ 收割 W49(61-id 家族第一波,4 粒完整)

**臂串收割期重算**(义务第 3 条,不抄散文):
`git show 066219d6:iterations/streams/test_set.md` 第 2 行 ⇒
**61 ids / 540 bytes / md5 `824ec2842e234693cb3c4094f73d6a14`** —— 与波次记录 `arm_bytes`/`arm_md5`
**逐字节相同**。

**取数**:`s3://dota2bot-batch-results-4924/soak/<run>/`(走 `soak/` **不走** `validation/`),
四个 run **各自下到独立子目录**(GH #225 禁扁平化)⇒ 每个 run 48 个 `.analysis.json`,
`files_seen 192 / games_loaded 192 / source_dirs 4 / unparseable 0 / pooled_basename_dirs_overridden 0`。
`RV_EXIT=0`。

### 5.1 逐机器(`machines[]` **零 null**,义务第 1 条)

| seed | instance | ab | ba | arm_depth | survival | status_code |
|---|---|---|---|---|---|---|
| 5404 | `i-090caadb7b7f89fd5` | 29 | 13 | 17.95 | 42.53 min | `instance-terminated-by-user` |
| 5502 | `i-0b09b8b9cfcd5129c` | 28 | 14 | 18.67 | 46.35 min | `instance-terminated-by-user` |
| 5574 | `i-010c6ec51f7e8cf87` | 28 | 14 | 18.67 | 55.17 min | `instance-terminated-by-user` |
| 5705 | `i-0acdcb2ded174213a` | 27 | 15 | 19.29 | 44.45 min | `instance-terminated-by-user` |

⚠️ **`status_code` 和 `survival_min` 这一轮不是从 `describe-instances` 来的,来不了**:
收割时点(发波后约 3h)EC2 **在任何状态下都已经查不到这四台**
(`tag:Name=dota2bot-*` 全状态查询 ⇒ **空**;GH #375 那个约 1h 的 terminated 窗口)。
唯一还活着的死因记录是 **spot instance request**:
`describe-spot-instance-requests` ⇒ 四条 `closed` / `Status.Code=instance-terminated-by-user`,
`survival = Status.UpdateTime − CreateTime`。
⭐ **`instance-terminated-by-user` = 自毁路径正常触发,不是 `Server.SpotInstanceTermination`**
⇒ **本波零抢占**。四台 42.5–55.2 min 自己关机,**远在 2h 看门狗之内**。

### 5.2 效应量(义务第 4 条:两层**读数**都登记,工具自打不手算)

| 量 | mean(每粒 swap-average 后跨种子算术平均) | ab 层 | ba 层 | side | comps_better |
|---|---|---|---|---|---|
| gpm | **+11.70** | +58.07 | −34.66 | +46.37 | 3/4 |
| xpm | +2.09 | +16.13 | −11.96 | +14.05 | 3/4 |
| deaths | +0.02 | −0.54 | +0.59 | −0.56 | 3/4 |
| last_hits | −1.27 | −5.77 | +3.23 | −4.50 | 2/4 |

⭐ **四个量全部 `sign_flip=true`**。按铁律 4(i-c):这四个是**侧偏已消除**的估计量
(每粒 50/50 swap-average),反号 ⟺ `|side| > |arm|` 是**恒等式不是诊断**,
**不是否决理由**,只说这一波抽到的阵容侧偏比效应大。**登记(4(i-a)),不当结论读。**
并池**未按局数加权**(4(i-d));两层读数取自工具自己的 `strata.<m>.ab/.ba`,**未手算**。

### 5.3 深度与排除(义务第 6 条)

`min_arm_depth = 8`(门限常数);**实测四粒 17.95 / 18.67 / 18.67 / 19.29**,
最浅的一粒也是门限的 **2.24 倍**。`thin_arm_seeds = []`,
**`excluded_seeds = NONE` —— 没有任何一粒的局是花了钱又扔掉的。**

### 5.4 winrate(义务第 5 条)

`winrate_channel = **DEGENERATE**`,**连续第八波**(W41/W42/W44/W45/W46/W47/W48/**W49**)。
少数侧只赢了 **4 / 192** 局(share `0.0208 < 0.20`),`mean.winrate_headroom = 0.018`。
⇒ 按 W49 波次记录**发波前就写死的**预登记条款:
**`winrate` 与 `comps_better.winrate` 不得写成读数,且不再逐波复议** ——
作为「该通道对本 harness **结构性无信息**」交总监(接 W48 那一轮已达成的同一条件)。

### 5.5 局数 vs 预期

`scored_games = 168`,`unfinished = 0`;预登记预期是 **~96**(4 粒 × 12 × 2)。
⇒ **实际多交付 75%**(每粒 42 局计分,ab 27–29 / ba 13–15,而不是 24)。
**登记为读数不作解释**:预期那个数把 `--games 12` 读成「每腿 12 局」,实测说那不是这个旗标买到的东西。
(方向是好的:同样的钱买到更多局。但**它同时意味着 `expected_games` 这个预登记量在过去若干波一直是错的**,
若哪一波真的少交付,拿它当分母会读不出来 —— 交总监。)

### 5.6 `suggested` 与并池

`suggested = hold_or_reject`。⚠️ **这是工具 gpm-only 的提示、门限是 `>5 gpm`,而 mean gpm 是 +11.70**
⇒ 提示**不是**由效应量驱动的(它还要求 `complete` 等其他条件)。
**促/退由总监裁**(GH #510 同形),条件 (a) 录像确认**仍欠**。
并池:**W49 自成一族 4 粒**,与发波前的预登记逐字一致(61 ≠ 63 ≠ 62,三个 md5 全不同)。

## 六、闸 (iv)(给 W50 的输入)

`RB_EXIT=0`:

```
yield      : 4 paired seed(s) of 4 machine(s)
attribution: 0 machine(s) reclaimed before the flip
VERDICT: not blinded -- the wave delivered 4 paired seed(s).
NEXT WAVE: spot (the default; GH #158 unchanged).
```

四粒全部 `PAIRED [depth-checked]`,changeover 常数 40.0 min,四台存活 42.5–55.2 min 全在其上。
⚠️ **第一次调用因为缺 `survival_min` 得 `exit 2` = UNDECIDABLE**;
`exit 2` 是**没跑成,永远不是「没瞎」**(与上一轮同一条纪律)。

## 七、⛔⛔ 泄漏:本台干净;外来机**没有走**,而且**已经落账了**

**dota2bot 侧干净**:`tag:Name=dota2bot-*` 在 `us-west-2` **全状态**查询 ⇒ **空**
(四台已自毁并过了 EC2 的保留窗口);`check_costs.sh` 的 running/pending 表 ⇒ **空**;
常驻仍只有 AMI `ami-0a990a26d89c66547`。

⛔ **不带过滤器、在正确区域的查询**(第三节那个更正之后):

```
i-0ce7ddf08ea24cc7f  c7a.16xlarge  running  2026-09-05T05:02:24.000Z  spot
                     Name=fft-cache-v1-attempt3   Project=final-table-trainer
```

**同一个 InstanceId、同一个 LaunchTime、仍然 running** ⇒ 到本轮已连跑 **约 19.2 小时**
(上一轮记的是 16.3h)。spot request `sir-ra3qg27m` 仍 `active / fulfilled`。

**⭐ 上一轮只能说「已发生未落账约 $16.4–19.7」;本轮它落账了,是 `$7.995`。**
(两者不矛盾:CE 的 09-01→09-06 窗口本身也有落账滞后,`$7.995` 是**已落账的那一部分**,
按 `c7a.16xlarge` spot 价带 `$1.008–1.210/h` × 19.2h ⇒ 已发生总额约 **`$19.4–23.2`**,
即**还有约 `$11–15` 在路上**。)

**算术(交 owner,本台不自裁)**:

- 裸 headroom **`$22.186`**(= `$50` operative ceiling − MTD `$27.814`);
- 外来机单机烧 **`$24–29/天`**;
- ⇒ **headroom 约 `0.6–0.9 天`见底**,而且**在路上那 `$11–15` 落账时会一次性吃掉一半以上**。
- **(甲)** 届时闸 (iii) 会对**本台**打 `THROTTLED`,而本台九月自己只花了 `$16.5`、发一波只要 `~$1.02`
  ⇒ **读起来像批测超支,实际不是**。
- **(乙)** owner 收到的第一封 `$50` ACTUAL 告警邮件,**抬头是 `dota2bot-batch`,而它指着错的项目**。

根因已量出(GH #515):`CostFilters = null` ⇒ **账号级预算不是项目级预算**。
**建议(两条都不动本台任何工具)**:给 `dota2bot-batch` 加 cost filter(按 `Project` 标签或机型),
或给 `final-table-trainer` 单开预算。

⛔ **本台未做任何处置**:带别人的项目标签,终止既越权又不可逆;**不主张它不该跑**。

## 八、本轮 issue 动作

- **GH #544 `[harness]`(新开)** —— 波次记录的 `launched_at` 键会静默丢失,闸 (i) 因此打 UNCERTIFIABLE(第四节)。
- **GH #528(追评,升级)** —— 从「前瞻」变成「已落账 `$7.995` + 仍在跑 19.2h」;
  同时**撤回上一轮那句「已不在」的更正之更正**:它上一轮就没走,本轮也没走。
- 未关闭任何 issue。

## 九、交棒

1. ⭐⭐⭐ **owner / 总监 —— GH #528 + #515 本轮从「前瞻」变成「账单上的一行」**:
   `c7a` 已落账 `$7.995`,还有约 `$11–15` 在路上,headroom `0.6–0.9 天`见底。
   **本台无法自解**:加 cost filter 或另开预算是 owner 侧的动作。
2. ⭐⭐⭐ **总监 —— GH #544**:`launched_at` 漏写会让闸 (i) 下一轮再次 UNCERTIFIABLE。
   最便宜的修法是让 `wave_throttle.py` 接受 `launch_time` 作为别名(或加一个波次记录 schema 检查)。
3. ⭐⭐ **总监 —— `$0.90` 单波成本常数的自动失效条款,第四轮连续点名仍未重裁。**
4. ⭐⭐ **录像组 —— 61-id 家族条件 (a) 仍欠**(W49 的四粒已在 S3,`.dem` 可取)。
5. ⭐⭐ **总监 —— W49 促/退**:gpm **+11.70** / comps 3/4,四粒深度全部 ≥17.95,零抢占,零排除种子;
   winrate 通道 **DEGENERATE 第八波**,按预登记**不再逐波复议**,请作为结构性结论收掉。
6. ⭐ **总监 —— `expected_games` 这个预登记量偏低约 75%**(第 5.5 节):
   它现在**不能**当「少交付」的分母用。
7. ⭐ **总监 —— GH #513** 仍 `open`;本轮串未动过所以未用上,下一轮串若不动就又要手做。
8. ⭐ **本台 —— `campgrade` 独占波仍不存在**(#480/#241),那一棒仍欠着。

**下一轮本台** = ① **发波 W50**(闸 (i) 解锁于 **`2026-09-06T03:20:05Z`**,
但**不要读这个数,读 `wave_throttle.py` 打的那一行**);
② ⚠️ **发波前先看第七节**:headroom 可能已被外来机吃到 `THROTTLED`,
**而那个 `THROTTLED` 不是本台超支** —— 若真被挡,把这一节的算术贴给 owner,不要自行放行;
③ 闸 (iv) 已备好:`NEXT WAVE: spot`;
④ 开工第一条命令**重定向不管道、放后台**(本轮**第八次**踩管道那个坑,已当场被脚本拒绝)。

## 十、收尾补记(自检真码 + 铁律 6 读数)

**自检真码(重定向不管道、后台跑满)**:**`EXIT=3`**,`legs run 10`,
**`UNCERTIFIABLE (exit 2): none`**(十条腿本轮都真跑过),
`FINDINGS: queue-rulings owed-executions trunk-red(python)`。
**3 = 有发现,不是没跑成。**

- ⭐ **`trunk-red(lua)` 本轮消失了**:上一轮是 `RED test_corpus_scale.lua`
  (`tests/test_staybottle_inflight_regen.lua:223` 的字面量语料规模断言,本台上一轮开的 **GH #538**),
  本轮 **`84 tagged detector file(s), 0 failures`** ⇒ **#538 已被协同组修掉**(git log 可见),
  上一轮那一棒**落地了**。
- `trunk-red(python)`:**`93 passed, 7 failed, 2 uncertifiable`**。
  其中 **2 条是本台的、本轮已修绿**(第四之二节);其余 5 条逐名交总监(同上)。
  ⭐ 按第一节的结构性论证,**这些都是 `main` 的红**。
- `queue-rulings` 腿:**`RIDESHARE (§BB.4: rule this round): 2`**。⚠️ **本台不裁**,
  但**再次指出上一轮已经指出过的规则冲突**:owner 的 **P4.2 明写它废止并压过 §BB.4**,
  冻结期唯一合法裁定是 `FROZEN-HOLD` ⇒ 自检那句 `§BB.4: rule this round`
  **是工具还在引一条已被废止的条款**,连续第二轮登记交总监。
- `owed executions`:**含本台仍欠的 `campgrade_wave_type_premise_reading`**(#480/#241)。
  另有 `p2_pair_arm_stayattr_staysrc`(GH #532 / OWNER_PRIORITIES P2)——
  **这是一条否决权不是待办**,它每轮报 OWED 是为了在**排 P2 取证波的那一轮**被读到;
  本轮不排 P2 波,登记不动作。
- `cadence` 腿本轮**无 finding**(上一轮有 `GAP cadence director`)。
- `unlanded commits` / `expired waits` / `stable-v1/v2 锚点` / 三条 promote atom:**全 ok**。
- 边界照抄:`REFUSED refs 802 of 827`(浅克隆);
  `NOT RUN (inside a leg): tests/test_rc_wrapper.py tests/test_selfcheck_lua_leg.py`。

**铁律 6 读数**:见下方收尾行(静态门 / 推送门 / 两次 push 的真码)。
**动态半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)本轮未跑,故不声称。**

**铁律 6 / 推送门读数**:
- 静态门 **`GATE_EXIT=0  CLEAN`**(`luacheck bots game: 0 warnings`;门自己装的 luacheck:
  `installed luacheck (it was missing; apt package lua-check, bounded)`)。
- 推送门上膛 `ARM_HOOK_EXIT=0`(`core.hooksPath` 指向 `.githooks`)。
- **未使用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行。**
- 两次 `git push` 的真码见下(各自会再跑一次同一个 gate)。

## 十一、Token 用量

`TOKENS total_in=8,172,747 out=51,300 turns=70`(到统计时刻为止;统计之后的收尾回合不计入)。
