# 批测台 2026-08-25T00:19Z —— 收割 W7 + 发波 W8(owner P1 的取证波,球在本台手里)

**一句话**:W7 收割完成(302 局 / 278 计分 / unfinished 0,`hold_or_reject`),
**W8 已发波** —— 总监 18:56Z 批准 `pulllane`+`towerfear` 入集(成员串 26 → 28),
**owner P1 DoD 第 3 步(买拉野连接率的取证波)本轮由本台执行**,连续两轮的封锁彻底解除。

---

## 1. ⭐ 真发现①:W7 是 stable-v2 作为基线腿的第一份语料,读数是 `hold_or_reject`

`recover_verdict.py` 全量重算(4 个 run 池化,**302 局 / 278 计分 / unfinished 0**),
armed = 26-id 头部成员串,钉树 `03fb378`:

| 量 | 池化均值 | comps_better |
|---|---|---|
| gpm | **−7.29** | 1/4 |
| xpm | **−12.66** | 0/4 |
| deaths | +0.12 | 1/4 |
| last_hits | −0.83 | 0/4 |
| **winrate** | **0.491**(中性 0.5) | 1/4 |

`suggested` = **`hold_or_reject`**。逐种子(每台一个种子,ab/ba 两层局数并列):

| seed | ab | ba | gpm | xpm | winrate |
|---|---|---|---|---|---|
| 888 | 42 | 29 | **+0.20** | −9.80 | 0.468 |
| 895 | 42 | 27 | −0.73 | −4.07 | **0.569** |
| 896 | 38 | 28 | **−15.31** | −18.14 | 0.476 |
| 906 | 41 | 31 | −13.32 | −18.63 | 0.450 |

**读法边界(铁律 2 + 计量三条)**:这是**粗粒度**读数,不做显著性检验。
条件 (b) 问的是「对胜负有没有明显负面影响」—— **winrate 0.491 / 278 局,离中性 0.9pp**,
逐种子 0.450–0.569 的散布**远大于**这 0.9pp ⇒ 本台的读法是
**「(b) 上看不出明显负面,也看不出正面」**,不是「负面」。
gpm −7.29 同理:随机抽签 SD ≈ 600 GPM/局,镜像草稿消掉大部分但 4 个种子的
−15.31 ~ +0.20 散布已经把 −7.29 罩住。**这份语料的价值在帧证据,不在这五个数**。

**诚实边界**:`winner_by` = `economy_10min_cap` **301** / `engine` **1**
⇒ 胜负判定几乎全部来自 10 分钟经济上限的代理量,**只有 1/302 局的胜负独立于金钱**。
`winrate` 与 `gpm` 因此**不是两个独立读数**,不要当交叉验证用。

## 2. ⭐ 真发现②:三、四例归属分歧到货,**全落在 slot 1**,GH #152 的前缀机制被钉死

必查项 ⑦(235 份 sidecar):`method` logname **235/235**、
`by_logname == by_hostname` **233/235(分歧 2)**、`candidates` 3–12(众数 11)。

两例分歧**点名到 tag**(§AH.2:不因「预期会分歧」而放过):

| run | tag | `by_logname` | `by_hostname` |
|---|---|---|---|
| `…_a0d128` | `20260824_182203_slot1` | `…_slot1.dem` | `…_slot`**`10`**`.dem` |
| `…_2b3d86` | `20260824_183801_slot1` | `…_slot1.dem` | `…_slot`**`12`**`.dem` |

**这推翻了上一轮那条软化说法的一半。** 12:17Z 据 W4/W5 的 466/466 把机制改述为
「~0.14% 的**顺序**竞态,不是确定性错配」。现在:

- **对的一半**:确实不是每局必现(slot 1 本波约 20 局,2 局分歧)。
- **错的一半**:它**不是顺序竞态,是共存竞态,而且域是结构性的**。
  **三例分歧(1/236 + 0/466 + 2/235 = 3/937)无一例外全是 `slot1` 撞 `slot1{0,1,2}`**,
  与 #152 开篇「只有 slot 1,且只在 `rec_slots ≥ 10` 时」逐字吻合。
  W4/W5 的 466/466 不是反例,是那两波 slot 1 恰好没撞上共存的 slot1X。

**期望值第二次修正(请按这条登记)**:不是 100%,也不是「≈100% 的随机竞态」,而是
**「slot 2–12 恒 100%;slot 1 在 `--rec-slots ≥ 10` 下以池共存概率分歧」**。
**任何非 slot 1 的分歧都是新缺陷,必须点名。** 已追评 GH #152。
`hostname_hits` 一栏保持作废(短路使其结构上 ∈ {0,1})。
**本波零错误归属**:235/235 `method=logname`,主判据全对。

## 3. ⭐ 真发现③:本波 `.dem` 零丢失,GH #147 的尾局竞态没有复发

逐 run `.dem` 数 **60/59/56/60 = 235**,与 sidecar 数**逐位相同** ⇒
**尾局丢失 0/4 run**,累计 **1/701**。不撤 #147(机制仍在:`--validate` 语义是
verdict 上传后立即 terminate),但它的发生率比 12:17Z 那句「每 run 至多 1 局」暗示的更低。

## 4. ⭐ 真发现④:上一轮登记的「slot 1 净值单调走深」趋势本轮被证伪,撤回

`--rec-slots 12` **第六次验收 exit 0**(`0 recording slot(s) beyond tolerance`,
302 局,box factor **1.025**)。但上一轮那条「⚠️ 不等它撞线就报」的趋势**反了**:

| 波次 | W3 | W4 | W5 | **W7** |
|---|---|---|---|---|
| slot 1 净值 | −3.3% | −3.2% | −4.0% | **−2.4%** |

**单调走深不成立,撤回「会撞 5% 线」的预期。** 仍然成立的是
**slot 1 是唯一处于赤字的录制槽**(slot 2–12 净值 +3.6% ~ +6.0% 全为正),
其结构性理由(`soak_loop.sh:170` 的扁平 `replays/` 镜像只留 slot 1 ⇒ 多一次上传)不变。
⇒ **这条不再需要 harness 加急**,已在交棒里降级。

## 5. ⭐ 真发现⑤:verdict 文件名 266 字节 > 255 上限,收割路径必然失败(新开 GH #167)

`s3 cp --recursive validation/` 对 W7 的 **4 个 verdict 全部 `[Errno 36] File name too long`**,
一个字节都没落地。armed 串 **232 字节** + 后缀 = basename **266** > `NAME_MAX` **255**。
**这是单分量上限,不是 `PATH_MAX`,换短目录不能绕过。**
成员串单调变长(26 → **28**),W8 的 basename 已 **~284 字节**。

**影响面**:S3 侧 key/内容完好,**无数据损失**;`recover_verdict.py` 标准收割路径
**不受影响**(读 `soak/<run>/*.analysis.json`,文件名短);受影响的是「拉自产 verdict 做对账」。
本轮绕法:逐个 `s3 cp <key> <短名>`。**踩到的坑**:`--recursive` 的失败混在进度输出里,
`| tail` 之后极易读成「跑完了」——本轮第一次就这么误读,`ls` 出来 0 个文件才发现。

## 6. 实例自产 verdict 第三次对账:仍不完整,标准收割路径第四次被坐实

| seed | 自产 ab/ba | 全量重算 ab/ba | gpm 自产 → 重算 |
|---|---|---|---|
| 888 | 42 / **27** | 42 / **29** | +3.07 → **+0.20** |
| 895 | 42 / 27 | 42 / 27 | −0.73 → −0.73 ✅ |
| 896 | 38 / 28 | 38 / 28 | −15.31 → −15.31 ✅ |
| 906 | 41 / 31 | 41 / 31 | −13.32 → −13.32 ✅ |

**1/4 个 run 少 2 局,且又是 888 的 ba 腿、又是 −2**(W4 那次是 888 ba 25 vs 27)。
**但 n=2,本台不宣称「888 特异」** —— 机制是 GH #147 那条尾局竞态
(verdict 在末尾几局落地前就算完并上传),哪个 run 中招取决于谁最后收波。
⇒ **`recover_verdict.py` 作为标准收割路径第四次被坐实,自产 verdict 只当免费早期预览。**

---

## 7. 启动决策:**发波 W8** —— 三条节流全过,且封锁本轮真的解除了

- (i) 距上一波 18:12:41Z ⇒ **6.1h** ≥ 6h ✅
- (ii) 树变了(`03fb378` → `aadd993`)**且**成员串变了(26 → **28**),
  当前树+成员串累计种子 **0 < 8** ✅
- (iii) 纪律(甲)照办:$36.245 + **$1.48(W7,6.1h 前发,在 12h 窗口内必须加计)**
  + $1.48(W8)= **$39.21 ≤ $45** ✅。**未越过 owner 的 $50 批准档。**

### 7.1 ⭐ 决定性变化:总监把 owner P1 的球交到本台手里

总监 18:56Z(GH #164)**批准 `pulllane` + `towerfear` 入集,成员串 26 → 28**,
并在交棒表里逐字写明:

> **批测台** | 成员串已是 **28**,`pulllane`/`towerfear` **下一波全集自动带上,零 AWS 增量,不请求专波**。
> **owner P1 责任链** | DoD 第 2 步(总监批准入 armed 集)**本轮完成**;球移到**批测台(第 3 步:买连接率的取证波)**。

⇒ 这不是「可以发」,是**点名要本台发**。`queue.json` 的 `strategy-11`/`strategy-12`
已由总监改成 `approved_pending_wave`。**上一轮那五条不发的理由,本轮逐条复核**:

| 上一轮的理由 | 本轮状态 |
|---|---|
| ① 基线错(promote 未上 main) | **已解除**(18:15Z 起 promote 在 main;本轮另核实 `refs/heads/stable-v1`/`stable-v2` **远端已建**,总监 18:56Z 报告里那句「仍未创建」已过期) |
| ② W6(`strategy-5b`)条件未兑现 | **仍成立** ⇒ **本波编号 W8,W6 继续留给 `strategy-5b`** |
| ③ `strategy-7/-8` 的 `DEFERRED_PENDING_W3_RESOLVE` | 仍未由总监宣布解锁,**本台不代宣**,两者不在成员串 ⇒ 带不上 |
| ④ 单发 `strategy-9` = 重复采购 | 不适用,本波是全集波 |
| ⑤ 总监缺席时不得单方面插波 | **不适用** —— 总监在场且**点名交办**(§AW.1:先例保护的是缺席情形) |

### 7.2 发波参数(全部事先登记)

- armed = test_set.md 头部成员串 **28 逐字**;接线门 **28/28 WIRED exit 0** @ `aadd993`
- 载体门 `--assert-carrier "crystal_maiden,zuus,lion,obsidian_destroyer"` **exit 0**:
  cm/zuus **FULL 4/4**;lion/odaoe **PARTIAL 仅 s896**(另三个种子是波内零腿,**该新规仍未裁**)
- **`pullcamp`/`pulllane` 是角色门,`--assert-carrier` 表达不了 ⇒ 按先例读作 no-op,附发牌表作正面证据**:
  4 个种子 × 2 边 **全部有 pos4 + pos5 辅助位**(共 16 个辅助槽),分母按构造齐备
- `hero-6`(WK)正面证据:**s895 dire pos1 / s906 radiant pos3 = `skeleton_king`**
- 钉树 **`aadd9938cc2f04cc9e8e8746fc6f9019f468c040`**(发波时刻 `ls-remote` 真 tip)
- 4 台 × 1 种子(**888/895/896/906,与 W7 同种子**)、`--on-demand`(GH #158 未裁前的保守默认)、
  `--slots 16`、**`--rec-slots 12`**(本波只动 id 集合、不动采集配置)、`--hours 2` + terminate、`--games 22`
- run_id 尾 token `364fb1`/`ae722d`/`6a2b32`/`c1eace`,发波 **00:16:28/32/37/41Z**
- **唯一性双保险**:四次调用各跨整秒(间隔 4/5/4 秒)+ `soak-run` 标签 **4 值两两不同**,
  `describe-instances` 只回 **4 台**,`InstanceLifecycle` 全 `None`(= 按需,与 `--on-demand` 一致)

### 7.3 ⭐ 预登记(可证伪):W7 是 W8 的 26-id 对照

W8 与 W7 **种子相同、采集配置相同**,armed 串只差 **`pulllane` + `towerfear`** 两个 id;
两波之间的树差(`03fb378` → `aadd993`)只有 **未 arm 的 gated id**(`lionhexaoe`/`alchrage`)
与文档/报告提交 ⇒ **行为上惰性**。
**⇒ W7 可作为 W8 的 26-id 对照腿,跨波差 ≈ 这两个 id 的效应。**
**诚实边界(必须与结论同时呈现)**:这是**跨波**比较,08-23 已实测「开拉点离泉水距离」
这类量的**波间自噪声 2,012u 与效应同量级**;所以此路只用于**帧证据侧的定性对照**
(连接率有没有动),**不许**用来给 gpm/winrate 这类经济量下结论 —— 那些仍只看波内 ab/ba 两层。

---

## 8. 局数(owner 2026-08-19 要求的固定节)

- **(a) 上一波 W7 最终有效局数**:**278 计分 + 24 暖场 = 302**,`unfinished` **0**。
  逐种子 ab/ba:888 **42/29**、895 **42/27**、896 **38/28**、906 **41/31**
  (**ab/ba 不对称**,radiant 波普遍多 10–14 局;这是既有形态,不是本波异常)。
- **(b) 本波 W8 实时进度**:三次读数 +2.5 分钟(00:18:56Z)、+4.8 分钟(00:21:20Z)、
  **+6.5 分钟(00:23:11Z)四个 run 前缀均未出现**。
  **这不是异常** —— S3 前缀要等第一个对象落地才存在,W7 的首局落在 **+9.1 分钟**,
  本波三次读数**都还在那个时刻之前**。
  预期 **~275 ± 25 有效局**;**预计自毁 ≈ 00:50Z**(0.550 h/台的账单侧读数,不是 02:16Z 看门狗)。
  **⚠️ 给下一轮的核对项**:若下轮开工时 `soak/` 仍是 **188**(四个前缀一个都没出现),
  那才是真异常 —— 说明 boot/clone/部署三段里有一段没走通,应去读
  `validation/…_run.log` 与实例控制台输出定位,**不要直接重发**。

## 9. 成本

- MTD **$36.245**(`budgets` refreshed **2026-08-24T21:12:20Z**;脚本按 $35 门限自动 CE 复核
  **$36.2450323784** 逐位吻合),forecast **64.471** / limit **100.0**,**三线全未触及**。
- 本轮 CE 花费 **$0.01**(脚本自动复核那一次)。
- **纪律(乙)照办**:未用「MTD 增量对得上波费」做任何推断。
  较上轮 **+$0.039** 而窗口内有 W7 一波($1.48)—— 这**不是**「W7 只花了 4 分钱」,
  是 `budgets` 快照停在 **21:12Z** 而 W7 的 EC2 行**滞后未到货**(与 08-24 实测的 4.3–11.3h 滞后一致)。
- **围栏算术(甲)**:$36.245 + $1.48(W7) + $1.48(W8) = **$39.21**,$45 围栏余 **$5.79**。
  ⚠️ **给下一轮的提醒**:再发一波就是 **$40.69**,**余量只够 ~1 波**;
  越过 $45 前必须停下来,越过 **$50** 需要 **owner 批准**。

## 10. 泄漏检查

四层 + spot 请求,**开工**:实例 **0** / 卷 0 / 快照 1 / EIP 0 / spot 请求 0 ⇒ 无泄漏
(W7 四台已按 `--validate` 语义完成即自毁,verdict 落 18:43–18:45Z)。
**收尾**:实例 **4 = 本波四台逐个对上,无第五台**,其余层不变 ⇒ **无泄漏**。
**本轮有活波次 ⇒ 收尾判据是「恰好 4 台且逐个对得上」,不是「0 台在跑」;
下一轮收尾判据回到「0 台在跑」。**

## 11. 固定栏位

| 栏位 | 读数 |
|---|---|
| `soak/` | **188**(持平 —— W8 四个前缀要等首局落地才出现,见 §8(b)) |
| `dem21/` | **40**(36 → 40,W7 四个 run,预期) |
| `unattributed/` | **0** |
| `validation/` 最新 | **2026-08-24T18:45:05Z**(W7 四个自产 verdict) |
| 远端 main tip | **`aadd9938cc2f04cc9e8e8746fc6f9019f468c040`**(= W8 所测树) |
| 远端 stable 引用 | **`stable-v1` = `6db5921`、`stable-v2` = `136a332` 均已存在**(tag 仍为空) |

## 12. 验证

本会话 `bots/` / `game/` **逐字未动**(改动仅 `iterations/` 下)⇒ 铁律 6 无适用对象;
容器无 `luacheck` / `lua5.1`,**不声称跑绿过 Lua 全量**。
`queue.json` 实测 `json.load` ok 且按原 `indent=2` 回写 ⇒ `git diff --stat` **9 增 9 删**,无重排噪声。
**开工自检 worst exit 3**:UNLANDED **9**(仍是总监那条已由 cherry-pick 落地的旧链,
按 18:15Z 的判读**不当掉棒**:内容已在 main 上,自检点的是换过的 SHA)、
trunk python **18/0**、cadence **6 个 finding**(全部是 08-24 那次停摆的残影,五条流现均已恢复)。

---

## 13. 铁律 9 的交棒

1. **⭐ 录像组 —— W8 收割后的 (a) 核验,这是 owner P1 的取证波**:
   `strategy-11`(`pullcamp`+`pulllane`)的**连接率**必须明显高于 post-fix 的 12.1%/9.9%,
   两个估计量都报;跟随小野→最近线兵中位必须下降;`poke_episodes` 不得塌向 0。
   **ab/ba 两层分别给读数,计数类不报中位数**(GH #148 计量三条)。
   同波另两份:`strategy-12`(`towerfear`,含**不许省的反向判据** 死亡数不许上升 / 塔下总撤退不许塌零)、
   `strategy-9`(`pullcad`)与 `hero-6`(WK 双树枝,s895/s906 有载体)的 W7 语料
   (`dem21/spot_20260824_1812*/`,**235 个 `.dem`**,跨 run 统计一律用 run 前缀限定 tag)。
2. **⭐ 录像组 —— W6 解锁棒(第四轮催)**:`strategy-5b` 卡的事件轴量级,**零 AWS**,
   语料在已交付的 `.dem` 里。**这是唯一还挡着 W6 的东西**,W8 之后队列就轮到它。
3. **总监 —— 载体门 PARTIAL 的惰性种子 = 波内零腿**(第三轮催,本波 lion/odaoe 又踩在上面)。
4. **总监 —— 按需 vs spot 的方法学裁定(GH #158)**:$45 围栏只剩 **$5.79 ≈ 1 波按需**,
   这条现在开始真的卡排期了。裁 (B)/(C) 无需 harness 改动,本台下一波即可执行。
5. **总监 —— `strategy-7`/`strategy-8` 的 `DEFERRED_PENDING_W3_RESOLVE`**:
   W3 的 resolve 事实上已落地,**本台不代宣解锁**,请正式 resolve 并排期。
6. **`[harness]` 棒**:**新开 GH #167**(verdict 文件名 266 > 255,收割路径必失败,
   成员串 28 后 ~284 字节,只会更糟);**GH #152 追评**(分歧域钉死在 slot 1,期望值第二次修正);
   **GH #147 降级**(本波 `.dem` 零丢失,累计 1/701);
   **slot 1 净值单调走深那条撤回**(§4,不再需要加急)。

---

## 14. 本次会话 token 用量

```
TOKENS total_in=4,587,755 out=37,234 turns=40
```

(铁律 8:这是**到统计时刻为止**的数字,统计后的收尾回合——本节改写、章程插入、
commit/push——不计入,当量级参考足够。)
