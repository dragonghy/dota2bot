# batch-desk 2026-09-18T03:13:44Z — 刹车第四十六轮持有;⭐⭐ 本轮把「历史 winrate 读数的复查面有多大」量成了一个数:**S3 存档侧是 0/185**

零发波、零收割欠、零泄漏。工作单元 = 上一轮交棒 ② 与 ④(b)。

---

## 〇、开工自检(铁律 10)

⚠️ **管道自卫第 38 次在当轮第一条命令上生效**,连续第八轮同形,⛔ 不辩解 ——
逐字 `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.`
⚠️ **第二跑又踩了超时坑**(`timeout 900`),脚本第二道自卫当场拦下,逐字
`REFUSED: routine_selfcheck.sh is running under `timeout`; exit 2, nothing checked.`
⇒ **两道坑各踩一道,与上一轮交棒 ⑤ 的字面模板不符**;第三跑才照模板走
(`nohup … > /tmp/sc.log 2>&1 &`,⛔ 无管道、⛔ 无 `timeout`)。

写作时 python 腿仍在跑 ⇒ **收尾三行横幅未打 ⇒ `SELFCHECK_EXIT` 本轮未知**,
⛔ **不读成通过**、⛔ **也不空转等它**(真码见「补记」)。已跑完腿实读:

- `LUA GATE COVERAGE disk 511 | push gate 346 | 开工自检 leg 138 | known_red 5`;
  `UNCOVERED 114 of 511 (22%)` `{'no_manifest_row': 51, 'timed_out': 60, 'too_slow': 3}`。
- **`UNCOVERED SET GREW -- 2 file(s)`**:
  `NEW UNCOVERED no_manifest_row tests/test_fieldsip_transfer_receiving_site.lua`
  (**连续第三轮同一个文件**,`fieldsip_transfer` 家族第九次,修复路径仍卡 GH #783)、
  `NEW UNCOVERED too_slow tests/test_lion_w_fight_seed.lua`(**本轮新出现**)。
- ⭐ `NOW COVERED tests/test_fieldsip_atom_pricing.lua`(1 个文件离开未覆盖集)。
- `PY GATE COVERAGE disk 151 | push gate 131 | leg 151`、`UNCOVERED 0 of 151 (0%)`;
  `out-of-gate rows with a written reader: 17 of 17`。

⛔ 本轮 `bots`/`game`/`tests`/`tools` **一行未改** ⇒ 以上无一条由本轮引入,⛔ 本台不代修。

---

## 一、预算(闸 (iii) 现跑,⛔ 未传任何 `--no-*`)

`FENCE_EXIT=3`(**连续第四十六轮是闸拒绝,不是本台的处置**),逐字:

```
actual (MTD)     : $92.163   <- re-read this run, never cached
pending waves    : $0.000   (launched, may not be in MTD yet)
planned          : $1.100
projected total  : $93.263
fence            : $100.00
brake            : $90.00   (owner's line, not derived here)
operative ceiling: $90.00   = min(fence, brake)
A launch at this instant would put MTD past the $90.00 BRAKE line.
WAVE_FENCE: THROTTLED (exit 3) -- brake, not fence. Stop and report to the owner; do not wait it out.
```

RULING 5/6/7 三行各自照抄,⭐ **钟没有降级**(无 `WAVE_FENCE CLOCK :` 降级行):

- `accrual scope : 17 region(s) read [...] <- COMPLETE: every region this account has enabled`
- `wave accrual : records after 2026-09-17T08:41:13Z (= 2026-09-17T19:59:13Z - 11.3h lag, clock from budget snapshot)`
- `accrual check : CERTIFIED (0 accruing instances account-wide, read this run) -- pending $0.000 is a reading, not a default.`

`crossing registry: 0 record(s) ... none in force` + `1 retired record(s)`;`budget filters : none`;
告警 `$50 [ALARM]` / `$80 [ALARM]` / `$100 NOT yet crossed [OK]`
(⚠️ 照登:`$90`–`$100` 在 AWS 侧仍可花,挡住的是 owner 的线)。

**⭐⭐ 快照连续第三轮冻结**:MTD `$92.163` / 戳 `2026-09-17T19:59:13Z`,
与前两轮(09-17T21:28Z、09-18T00:24Z)**两量逐位相同**
⇒ **零新支出测量**(⛔ 不等于「没花钱」)⇒ **一个速率点都不贡献**,
⛔ 不动 `$0.40–0.92/天` 带、月末兜底日 `2026-09-27T00:00Z` 原样结转。
**headroom 对 `$90` = `$-2.163`**(与前两轮逐位相同,⛔ 不得读成趋势 —— 是同一张快照的直接后果)。

GH #801 四行整块照抄(**SKIP 不是 pass**):

```
   >= $35, but headroom to the $90.00 brake is $-2.163
   < $1.10 (cheapest wave) — CE confirmation SKIPPED, NOT passed (GH #801).
   Nothing can launch at this MTD, so confirming it buys nothing.
   Resumes by itself as soon as headroom >= $1.10.
```

欠条 `gh801_confirm_headroom_first_live_read` 仍 **OWED**;⛔ 未用 `COST_CONFIRM_AT=999` 自救。
`alpha = 0` / `beta = 0` 本轮实读;`stamp unknown (fresh container) -- alpha and beta are both
zero -- a frozen stamp over an idle account opens no blind spot`;
`forecast 118.091` 与戳一同冻结,⛔ 不解释不采信。

---

## 二、收割:零欠

- `validation/` **592 对象**(`VAL_EXIT=0`)—— 与前两轮**逐位相同**;
  最新对象按 RULING 47 取法仍是 `lf_rescue+27ids-d177b87026dd_20260912_1018_run.log`
  (`2026-09-12 10:18:26`)= **W69**。
- `soak/` **72,079 键**(`SOAK_EXIT=0`)—— 逐位相同;最新键 `20260912_100538_slot6.analysis.json`。
- **在跑波次:无。** `recover_verdict.py` 未跑 ⇒ **没得收,不是漏收。**

---

## 三、工作单元(上一轮交棒 ② + ④(b))

### 3.1 交棒 ② — GH #184 零表态,⛔ 按交棒不重复发表证据表

`issue_read` 现读:**`state: open`**、**`comments: 3`**、`updated_at 2026-09-18T00:36:03Z`。
三条评论分别是 08-26T00:20Z / 08-26T06:21Z(本台第二、三次复现)与
**09-18T00:36:02Z(= 上一轮本台自己那条)** ⇒ **总监/owner 零表态,评论数与戳自上一轮未动。**

⇒ 按交棒 ② 严格执行:**本轮只登记戳与评论数,⛔ 不重复发表同一份证据表**(与 #779 同规格)。
⚠️ **#184 已 open 24 天**,而它的验收是**零 AWS 成本**的(语料已在 S3)。
连带:交棒 ③(加厚真局侧别,~4,700 GET)**以总监裁定为前提,本轮零表态 ⇒ ⛔ 未做。**

### 3.2 交棒 ④(b) — 缺陷修没修:**没修**(trunk 现读)

```
tools/batch_test/soak/analyze_log.py:96   natural_end = fort is not None
tools/batch_test/soak/analyze_log.py:102  winner_by = "engine_natural"
```

⇒ #184 §1 那一行**逐字未动**;`referee_surrender`(§4 建议的新桶名)在
`tools/batch_test/soak/` 下**仍一处都不存在** ⇒ **§4 建议修法仍未落地**。
⇒ **交棒 ④(b) 的前提成立**:刹车解除后第一波的 `winrate` 仍是裁判伪影。

### 3.3 ⭐ 顺带核实的一件**反向**的事:`DEGENERATE` 闸本身是好的,⛔ 不要连坐它

上一轮交棒 ④(b) 预判「`DEGENERATE` 横幅会正确地打出来,但打出来的原因不是它以为的那个」。
**本轮逐行核实,这句话的前半对,而且闸比预期结实:**

- 守卫在 `recover_verdict.py:548-567`,判据 = **物理侧别少数侧占比 < 0.20**;
  界后语料 272/272 全 dire ⇒ `share = 0.0000` ⇒ **必然 `DEGENERATE`**。
- 横幅逐字自带禁令:`MUST NOT be cited as rule 2(b) support until the channel recovers.`
- ⭐ **它挂在正确的条件上**:外层是 `if any("scored_games" in r for r in rows):`(:524),
  注释逐字 `The census hangs off "a wave was counted", NOT off "a winrate survived the gate"`
  ⇒ **一波全是薄种子也照样出普查**,⛔ 闸不会把自己的证据一起带走。

⇒ **结论要分成两半写,别混**:判据的**拦截行为**是对的(会拦住引 2(b));
判据的**归因**是「语料被横扫」,而真因是裁判伪影 ⇒ ⛔ 修 #184 §1 时**不要顺手改这个闸**,
它在没修之前是唯一还站着的那道门。

### 3.4 ⭐⭐ 本轮真正新的一个数:**历史复查面 = S3 存档侧 0/185**

上一轮在 #184 下问了总监三件,第 2 件是「**历史读数**:界后所有引用过 winrate 的 (b) 判定」,
并明确 ⛔ 本台不点名、不重开。**本轮不点名任何一条判定,改去量那个面本身有多大。**

取法(⛔ 不是抽样,是**全量**):`validation/` 592 对象里 `2026-08-26` 之后的
`*.verdict.json` **全部 185 个**,逐个 GET 后做**键普查**(不是找某个字段,是数所有字段):

| 键 | 出现 |
|---|---|
| `cand` / `cand_ref` / `contrast` / `seeds` / `per_seed` / `mean` / `comps_better` / `suggested` | **185 / 185** |
| `mean.gpm` / `mean.xpm` / `mean.deaths` / `mean.last_hits` | **185 / 185** |
| `min_arm_depth` / `thin_arm_seeds` / `per_seed.arm_depth` / `per_seed.scored` | **160 / 185** |
| **任何带 `winrate` 的键**(顶层或嵌套) | **⭐⭐ 0 / 185** |

⇒ **界后 18 天里,S3 存档的 verdict 从来没有装过一个 winrate 数** ——
没有 `winrate`、没有 `winrate_channel`、没有 `winrate_headroom`、没有 `winrate_side_census`、
没有 `winrate_independent_of_gold`。

**机制(逐行核实,⛔ 不是推测)**:存档侧那个 verdict **不是** `recover_verdict.py` 写的,
是 `validate_onspot.sh:130-165` 里一段**内联 heredoc 的第二份独立实现**写的,
它只实现四个经济量。winrate 通道(连同 #352 的 `DEGENERATE` 闸、#696 的 headroom、
#184 §5 的独立性披露)**只存在于 `recover_verdict.py`**,而后者在**本地收割**时才跑。

**这个数有两半,两半都要写:**

- **(甲) 令人放心的那半,而且它是本轮的主要产出**:**存档侧从未向任何读者提供过一个
  可引的 winrate** ⇒ 总监那第 2 件事的复查面**不在 `validation/` 里**,
  在**报告**里。这把一个看起来无界的复查,界定成了「翻本台自己的波次报告」。
- **(乙) ⛔ 但它不是「所以没事」**:§CT 那条常设义务(每份 verdict 报告照抄
  `winrate_channel` 与 `mean.winrate_headroom`)**在存档侧无法满足** ——
  那两个字段在 185/185 里都不存在。历史上它是靠本地跑 `recover_verdict.py` 满足的
  (语料自证:章程 `8115` / `9249` / `9522` 三处波次记录都抄出了具体数)。
  ⚠️ **风险点在下一波**:若哪一轮改从 S3 verdict 去满足 §CT,读到的是**字段缺失**,
  而 `recover_verdict.py:506` 自己的注释逐字警告过这个形状 ——
  `"0/0" on purpose: an absent key reads as "nothing to report", which is the one
  thing a wave with no measurable seed does not mean.`
  ⇒ **缺失会被读成「没什么可报的」,而那正是它唯一不意味着的东西。**

**⛔ 三条不主张(自划边界)**:
1. ⛔ **不主张这是缺陷**。两份实现分工是既有设计,且 `suggested` 两边**逐字同规则**
   (gpm + deaths),`recover_verdict.py:49-51` 注释逐字说明这是**故意**的:
   `suggested` deliberately still reads gpm only -- changing the automated promote hint is
   a policy change, not a side effect of adding a metric (test_set.md AS.4)`。
   ⇒ 本台一度以为两边 `suggested` 会分叉,**逐行比对后自证否定,当轮撤回**(证据纪律 4:
   ⛔ 不许用「结论对得上」代替「理由对」)。
2. ⛔ **不点名**任何 promote / (b) 判定,⛔ 不重开已关档裁定,
   ⛔ 未改 `state.json` / `test_set.md` / `OWNER_PRIORITIES.md` / `queue.json`。
3. ⛔ **`160/185`(#269 三字段)不作解读** —— 它只说 #269 落地在界后纪元**之内**,
   ⛔ 本台未去对那 25 个的落地日,不主张边界在哪天。

**⛔ 本轮未在 #184 追评**(理由见 §七),该发的内容已在此登记,棒交下一轮。

---

## 四、局数

本月无新波次;本轮**未读 `analysis.json`**(⛔ 0 局),读的是 **185 个 `verdict.json`**。

---

## 五、成本(铁律 1 三段式)

**零 EC2 / 零 CE / S3 读取 `~264` 个对象(出网未计价)** ——
`s3 ls --recursive` 2 次(592 + 72,079 键 ≈ **79 LIST**,不下载)
+ `verdict.json` GET **185**(≈ 0.15 MB)。
**本轮新增 EC2 计费 `$0.00`**;`check_costs.sh` 本轮**未买 CE**(GH #801 SKIP,见 §一)。

---

## 六、泄漏:五条独立路径全零(⛔ 不合并成一句)

1. `check_costs.sh` 自带 `running/pending instances` 区块 **空**;`COSTS_EXIT=0`。
2. `describe-instances` 五态(pending/running/shutting-down/stopping/stopped),
   **不加 tag 过滤** ⇒ `INST_EXIT=0` / **`LINES=0`**。
3. `describe-spot-instance-requests` `State==open||active` ⇒ `SIR_EXIT=0` / **`SIR_LINES=0`**。
4. 闸 (iii) 账户级 **17 区 COMPLETE** + `CERTIFIED (0 accruing instances account-wide, read this run)`。
5. AMI 仍 `ami-0a990a26d89c66547  dota2bot-batch-20260718` **一张**(常设成本,非泄漏);快照无新增。

---

## 七、发表纪律

**⛔ 本轮零发表**(零 issue、零评论),理由**逐条对照上一轮交棒 ②**:

- 交棒 ② 逐字要求「若零表态,⛔ 不重复发表同一份证据表,改为**只登记戳/评论数是否动过**」。
  本轮 #184 零表态 ⇒ **照办**。
- ⚠️ **本台自问过一次该不该破例**:§3.4 那个 `0/185` **不是**上一轮那份证据表的重发,
  它是**新测量**,而且直接收窄了本台自己向总监提的第 2 问。
  **仍然选择不发**,理由是保守侧:第一条评论发出**才 2.6 小时**、尚无人读,
  此刻追第二条长评论**增加的是噪声不是信息**,而这个数**不会过期**(语料是存档的)。
  ⇒ **登记在本报告 §3.4 + 章程,棒交下一轮**:总监一旦表态,**连同这个数一起答**;
  若 #184 继续零表态,按报告节奏升级(见交棒 ②)。
- ⇒ 本轮**未跑** `claim_precheck.sh`(⛔ 无草稿可检)——
  **⛔ 这是「没有可发表的东西」,不是「检查通过」。**

---

## 八、通知判据(自设,09-16T18:15Z 收窄的四条)

逐条现读:MTD `$92.163` **< `$100`** ✗;`alpha = 0` ✗;headroom `$-2.163` **< `$1.10`** ✗;
今天 `2026-09-18` **< `2026-09-27`** ✗ ⇒ **四条一条未中 ⇒ ⛔ 本轮不推送。**

⭐ 与上一轮的**偏离已纠正**:上一轮按「量具级缺陷」破例推了一次并自报偏离,
交总监裁该不该加第 ⑤ 条 —— **本轮零表态 ⇒ 判据仍是那四条 ⇒ 严格照它执行。**
本轮 §3.4 虽是新测量,但它**不改变任何一条阈值、不需要 owner 做任何决定**
(刹车僵局的球在 GH #721 的 owner 侧,已推送过两次)⇒ **第 47 次「刹车仍持有」对 owner 是零信息。**

---

## 九、没做的事(⛔ 逐条)

⛔ 未发波(闸 (iii) exit 3);⛔ `bots`/`game`/`tests`/`tools` **一行未改**
(⚠️ **想过**给 `analyze_log.py:96` 收紧 `natural_end`,**否掉** —— 判据与拦截点是总监的事,
先例 GH #33「不自己改 harness」,且 #184 §4 已给两版待选,本台**不代选**);
⛔ 未做交棒 ③ 的 ~4,700 GET 加厚(前提是总监裁定,本轮零表态);
⛔ 未重开任何已关档 promote 裁定;⛔ 未点名受影响的 (b) 判定;
⛔ 未改 `state.json` / `test_set.md` / `OWNER_PRIORITIES.md` / `queue.json`;
⛔ 闸 (i)/(ii)/(iv) **未跑**(发波轮的闸;(iii) 已 exit 3)—— **未跑不记成通过**。

---

## 十、交给下一轮本台的棒

1. **闸 (iii) 照跑**;`headroom ≥ $1.10` 的那一轮 GH #801 会**自己**恢复付费复核,
   ⛔ 不用 `COST_CONFIRM_AT=999`。**快照已连续三轮冻结** ⇒ 先读 `budget refreshed` 戳,
   **戳没变 ⇒ 比对零信息,登记「不可得」**,⛔ 没到点不要编速率。
2. ⭐⭐⭐ **GH #184 若有总监回应 → 连同本轮 §3.4 的 `0/185` 一起答**(那条收窄了本台自己提的第 2 问);
   若**仍零表态**,⛔ 继续不重发证据表,只登记戳/评论数 —— 但**本轮起开始数轮次**:
   本轮是零表态第 **1** 轮,**连续 12 轮零表态按铁律 9 的节奏升级并指名下一步**。
3. ⭐⭐ **刹车解除后的第一波**(P4.1 或例行全集串),四件照旧 + 本轮新加第 (e) 件:
   (a) verdict **现场照抄** `winrate_channel` 与 `mean.winrate_headroom`(§CT);
   (b) 照抄之前先确认 #184 §1 修没修 —— **本轮现读:没修**;
   (c) 收割必带 `--ledger iterations/games_ledger.jsonl --ledger-wave W<N>`;
   (d) 抄 GH #801 四行**恢复后**的样子(结清 `gh801_confirm_headroom_first_live_read`);
   (e) ⭐ **§CT 那两个字段只能从本地 `recover_verdict.py` 拿,⛔ 不要去 S3 的 `verdict.json` 里找** ——
   界后 **185/185** 一个都没有(§3.4),而**缺失会被读成「没什么可报的」**
   (`recover_verdict.py:506` 自己的注释就是这个警告)。
4. ⭐ **检索这道工序保留**:`grep -rn '<关键词>' iterations/reports/` + `mcp__github__search_issues`,
   **两条都要**。⭐ 本轮它又起作用了一次,方向相反:检索 + 逐行比对让本台**当轮撤回**了
   一个已经成形的「两份实现的 `suggested` 分叉了」的结论(§3.4 不主张 1)。
5. ⚠️ **开工第一条命令照抄字面模板**:`nohup bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1 &`
   (⛔ 无管道、⛔ 无 `timeout`)。**本轮管道坑第 38 次 + 超时坑同轮各一次,连续第八轮同形。**
   ⇒ **下一轮 = 第 39 次**;⛔ 不要先写命令再想模板,**先抄模板**。
6. ⚠️ `NEW UNCOVERED tests/test_fieldsip_transfer_receiving_site.lua` **连续第三轮同一个文件**
   (卡 GH #783)+ **本轮新增** `tests/test_lion_w_fight_seed.lua`(`too_slow`);⛔ 本台不代修。

---

## 十一、补记(第一次 push 之后按 GH #290 顺序回填)

见文末。

## 十二、Token 用量(铁律 8)

见文末。
