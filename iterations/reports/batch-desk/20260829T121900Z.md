# 批测台 2026-08-29T12:19Z —— **发波轮:W25 起飞**(第一波 44-id 串;`skeleton_king` 是新载体 term)

**一句话**:上一轮交棒 ⑤ 说的「不到闸不发波,W25 解锁 `12:16:51Z`」在本轮到期,
守卫连拦 8 次后于 **`12:16:54Z`(+3 秒)**放行,四台 spot 分四个 AZ 全中、零降级;
本波是 **`odbuild`+`wkqdmg` 入集后的第一波**,载体 term 从 5 个变成 **6 个**
(新增 `skeleton_king`),于是**选种不能沿用 W24** —— W24 的四粒在新 term 上只有 **1 粒载体**。

---

## 一、自检(铁律 10)

`routine_selfcheck.sh` **worst exit 3 / `UNCERTIFIABLE: none`**。三条发现**全是 cadence**
(director `08-28T13:07Z→17:20Z` 4.2h、director `08-29T04:08Z→now` 8.0h、
replay-check `00:55Z→05:30Z` 4.6h),**无一条是本台的**。
其余七条腿干净:`unlanded_commits` OK、`stable-v1/v2` 两个锚点 EXISTS/PINNED/SHIPPED 全 ok、
python 套件 52 passed / 0 failed、快腿 26 个检测器文件 0 failures、
un-ruled queue 请求 0(RIDESHARE none / OTHER none)、无过期 admission wait。

## 二、成本与围栏(步骤 2)

- **MTD $64.561**(`budgets/dota2bot-batch` 免费读数,budget refreshed **`06:58:07Z`**);
  ≥$35 触发的 **$0.01** CE 复核 **$64.5613714472** —— **与 09:13Z 那轮逐位一致**。
  **这是本轮唯一的非 EC2 支出。**
  ⚠️ 按滞后铁纪律**不做**「MTD 增量对得上波费 ⇒ 已计入」的反推:budget 快照停在 `06:58:07Z`,
  而 **W24 四台跑到 `07:12Z`** ⇒ W24 至少有一部分**必然不在这个数里**。
- **围栏算术(甲式,逐波按自己的界别取价)**:
  `$64.561 + W23(00:16Z, spot, $0.8) + W24(06:18Z, spot, $0.8)` = **$66.16**。
  (W23 起飞距本轮发波 12.01h,**卡在 12h 窗口边缘上**;本台**把它算进去**,取保守侧。)
- **(iii) 闸**:两种市场类型都要过 ——
  spot 估价 `$66.16 + $0.8` = **$66.96**;**全程降级按需**的最坏估价 `$66.16 + $2.15` = **$68.31**。
  两者都 **≤ $80** 围栏 ⇒ **通过**,发波后余量(spot 估)**$13.04**。
- **本轮未跨任何 owner 可见告警档**($50 早已跨,下一档 **$80** 未跨)⇒ **没有「本轮跨过 $X」那一行要写**。
  刹车 **$90** 未近,owner 批准线 **$100** 未近。
- 开工 running/pending 实例 **空**;常驻只有 AMI `ami-0a990a26d89c66547` + 快照。

## 三、收割:**本轮无新 verdict 可收**

S3 `validation/` 最新四份仍是 W24 的 `l1trade+42ids-607245e9b39f_20260829_0620xx_*.verdict.json`
(`06:57–07:10Z` 落盘),**已在 09:13Z 那轮全量重算并归档**;`soak/` 最新的四个 run 前缀也仍是
W24 的 `spot_20260829_0618xx_*`。**自 09:13Z 起零新增语料 ⇒ 本轮收割步骤空过,不是跳过。**

## 四、⭐ 本轮的实质变化:测试集 42 → 44,载体 term 5 → 6

`test_set.md` 第 2 行在 **`10:xxZ`** 变了:总监同轮双双入集 **`odbuild`(§CC/§CF)**
与 **`wkqdmg`(§CD/§CF)**,42 → **44 id / 387 字节**。
(同在 10:xxZ 提出的 **`fieldsip`(§CE)未裁** ⇒ **不在本波串里**。)

- **接线门 exit 0**:`all 44 armed ids wired on b51bac77e8fe7f47b076a2b3ea3c32ec24ab2097`;
  两个新 id 各自单点:`odbuild → bots/BotLib/hero_obsidian_destroyer.lua:67`、
  `wkqdmg → bots/BotLib/hero_skeleton_king.lua:608`。
- **载体门(#276 机械推导)exit 0**:
  `CARRIER_TERMS derived from 44 armed ids: 8 hero-scoped, 36 generic, 0 unresolved => 6 term(s)`,
  `TERMS crystal_maiden,lion,obsidian_destroyer,skeleton_king,spirit_breaker,zuus`。
  **`skeleton_king` 是 W25 才出现的新 term**(W23/W24 都是 5 term / 6 hero-scoped)。
  **term 未手写**,全部从 arm 串推。

## 五、⭐ 选种:**新 term 逼着换种子**——W24 的四粒在 `skeleton_king` 上只有 1 粒载体

先拿 W24 的 `1601/1603/1633/1641` 过新门:官方门确实 **exit 0**(门只要求 ≥1 粒),
但读逐条 verdict 会看到 **`wkqdmg` `satisfied=1 carriers=1633`** ——
**一粒载体 = 那一台一旦被回收,`wkqdmg` 这一波直接归零**,不是变薄。
这正是本台从 W23 起的自律(「每个 term ≥2 粒载体」)要挡的形状,而**官方门挡不住它**。

于是在 `1600–1780`(181 粒)内自己重搜:先把 `obsidian_destroyer`(29 粒)与
`spirit_breaker`(29 粒)两个最稀缺 term 的载体取并集(54 粒)——
**4 个槽位要同时喂饱两个各需 2 粒的稀缺 term,候选种子必落在这个并集里**,
枚举 54C4 得 **1027 组**满足「六个 term 全 ≥2」,按「min 优先、总载体数次之」取
**`1603 / 1633 / 1664 / 1770`**(min 2,总 17,是分布最均匀的那一组):

| term | 满足粒数 | 载体种子 |
|---|---|---|
| crystal_maiden (`cmrguard`) | 3 | 1603, 1664, 1770 |
| lion (`liondrainstop`) | 3 | 1603, 1633, 1770 |
| obsidian_destroyer (`odaoe`, **`odbuild`**) | 3 | 1603, 1633, 1664 |
| **skeleton_king (`wkqdmg`)** | **3** | 1633, 1664, 1770 |
| spirit_breaker (`aimguard`) | 2 | 1633, 1770 |
| zuus (`zusstatic`, `zusult`) | 3 | 1603, 1633, 1664 |

官方门复核:`CARRIER_GATE ids=8 seeds=4 exit=0`,八条全 `PARTIAL` 但**最低的一条是 2**
⇒ **任何单台被回收都不会让任何 id 归零,只会变薄。**
⚠️ **诚实边界原样保留(第三轮)**:「≥2 粒」仍是**本台自律不是门** ——
下一个不这么选种的人会原样复发本节开头那个 `satisfied=1`,而门会照样放行。**#285 仍未裁。**

## 六、市场与放置:三件验收连续第三波全过,零降级

四次独立调用、每次显式一个 `--az`(2a/2b/2c/2d),间隔 ~8s;`--dry-run` 先行且干净。
**阶梯停在第 1 级**:`c6i.4xlarge` spot 四台全中 —— 无 `InsufficientInstanceCapacity`、
**无 `re-aiming inside the ring`**、**无 `AZ RING EXHAUSTED`** ⇒ 未到 `c6a`,更未到 `--on-demand`。

以 `describe-instances` 为准(不拿 `spot_` 前缀当证据),四台全 `spot` + `c6i.4xlarge` + `running`:

| 种子 | AZ | instance | SIR | run_id 尾 token | LaunchTime |
|---|---|---|---|---|---|
| 1603 | us-west-2a | `i-0047c1877ba838d48` | `sir-dav7j52m` | `6df84c` | `12:16:56Z` |
| 1633 | us-west-2b | `i-02de99455e48036ab` | `sir-21ifjqen` | `b1386e` | `12:17:05Z` |
| 1664 | us-west-2c | `i-06f242a31cc1ad7e1` | `sir-e537jfvq` | `a29ed3` | `12:17:13Z` |
| 1770 | us-west-2d | `i-0ddbc6603b57326ce` | `sir-zksfgbjq` | `ecbb41` | `12:17:22Z` |

- **#252 通过**(四台四个 AZ);**#256 连续第三波拿满**(四 AZ 互不相同,一次 AZ 事件的暴露面 = 1 粒);
- **#282 连续第三波可执行且通过**:四条起飞行全带 `requested=/actual=` 且**四条全相等**,
  **无一条 `PLACEMENT MISMATCH`**;
- 四个 `soak-run` 标签两两不同(#98 的 `RUN_TOKEN` 生效)。

## 七、配置

- pin **显式 40 位 `b51bac77e8fe7f47b076a2b3ea3c32ec24ab2097`**(**不写 `main`**)
  ⇒ 本轮收尾提交**不改实例克隆的树**。发波前 `git ls-remote origin main` == 本地 `origin/main`
  == 该 sha,**发波前一分钟又核了一次**(铁律:实例 launch 时 clone `origin/main`)。
- **arm 串 44 id / 387 字节**(W21–W23 是 43/381,W24 是 42/372 ⇒ **读数不可并进 W24 序列**)。
- `--count 1 --az <az> --ref <sha> --slots 16 --rec-slots 12 --hours 2`,
  `--validate "<ARM> <seed> --games 12"` —— 与 W24 逐项同构,只有种子和 arm 串不同。
- **未传 `--on-demand` / `--cand-ref`**、**未用任何 `--allow-*`**、**未用 `--min-arm-depth`**、
  **未用 `--allow-short-watchdog`**(波次预算闸未拒发)
  ⇒ **本轮没有任何一行「这是 SKIP 不是 pass」要抄。**

## 八、局数(铁律 7)

- **(a) 上一波(W24)的最终有效局数**(09:13Z 那轮实测,原样转载):
  落盘 206 局(带戳 183 / 暖场 23)、完成 203、**计分 180**、`unfinished 3`;
  逐 seed `ab/ba`:1601 **39/19**、1603 **28/15**、1633 **27/13**、1641 **29/13**
  (`ab:ba = 2.05:1`,四粒同向,dire 腿被 stall 截断的老形状);
  `min_arm_depth 8` / `thin_arm_seeds []` ⇒ 四粒全 `scored`,最薄 1633 = **17.55**。
- **(b) 本轮在跑的 W25 实时进度**:发波后 ~2 分钟,四个 `soak/<run_id>/` 里
  `analysis.json` **各 0 份** —— **这是开机中(启动 + 拉仓库 + 生成 pool ≈ 12 分钟)的预期值,不是异常**。
- **本波预期局数(启动型报告按章程写预期)**:4 粒种子 × 2 腿 × `--games 12` = **96 局带戳**,
  外加每台暖场若干(W24 同构配置实测落盘 206 / 计分 180)。

## 九、泄漏检查(步骤 6)

开工那次:running/pending **空**。
收尾那次(`--leak-only`):**4 台在跑,全部是本轮 W25 自己发的四台**,instance id 与 §六表格逐个对上,
**没有第五台** ⇒ **零泄漏**。⚠️ 注意本轮的收尾读数**不是「0 台在跑」** —— 章程那句「要的是 0 台」
写给的是**纯收割轮**;发波轮的正确判据是「在跑的台数 == 本轮自己发的台数,且 id 逐个对上」。
四台的 `--hours 2` 看门狗 + `instance-initiated-shutdown-behavior=terminate` +
`--validate` 完成即自毁三条自毁路径全在。

## 十、⭐ 回收致盲(#271):`waves/W25_wave.json` 已在 SIR 存活时落盘

写于 **`12:19Z`**,彼时四个 SIR 全 `active/fulfilled`(**一手读数,非事后反推** ——
W23 就是这样丢掉整份 SIR 记录的)。含四台的
`seed / az / instance_id / sir / run_id / create / requested_az / actual_az / sir_state_at_launch`,
以及留空待收割回填的 `status_code / update / ab / ba / arm_depth`,
和本波特有的 `carrier_terms` / `carrier_gate` / `rideshares` 三个键。
收割那轮直接 `reclaim_blind.py --wave-json` 即可。

## 十一、搭车请求(§4a)

`queue.json` 里 **`hero-22`(`odbuild`)与 `hero-23`(`wkqdmg`)** 在 `10:xxZ` 拿到
`ROUTED_RIDESHARE / ADMITTED` 裁定(上一轮它们的 `director` 还是 null = 未裁)。
本波**同时带两个载体** ⇒ 两条**当场搭车,零 EC2 增量**,已把 `status` 从 `pending` 改成 `running`
并在 `result` 里写明波号、树、载体门读数。

**⚠️ 收割那轮必须先读两条各自 `acceptance` 的前置门,不许直接读成「测过了没效果」**:
- `hero-22`:先跑 `tools/batch_test/behavioral/skill_point_stall.py`。**OD 若仍在 STALL 表里,
  零读数一律标 `UNINTERPRETABLE` 并退回**(OD 停在 6 点就走不到那四个被修的下标)。
- `hero-23`:若该波 WK 的**等级分布压根没到 10**(t10 天赋把 dot 时长翻倍,而 turbo 局 ~20 分钟),
  读数同样按 `UNINTERPRETABLE` 退回,并把等级分布一起交出去。

## 十二、三条闸(§4)逐条

- **§4a**:33 条 `pending`/`running` 逐条查 `director`。`ROUTED_ARCHIVE_*`(零 EC2)/
  `RECEIVED_NOT_SCHEDULED`(**不许发**)/ `DEFERRED_*` / `REJECTED` 一律不构成发波请求;
  `strategy-5b` 的 `director.wave=W6` 是**历史波号**;所有 `APPROVED*` 的 id 已在 44-id 串里搭车且
  `status=running`。**新变化**:`hero-22`/`hero-23` 本轮已裁,但裁的是 **ROUTED_RIDESHARE
  = 搭车、零 EC2 增量、不申请专波** ⇒ **仍不构成「发一个新的付费波」的请求** ⇒ 走 **§4b**。
- **(i) 通过,+3 秒**:锚点 W24 发波循环 `12:16:51Z` 解锁;块内 `date -u` 守卫**连拦 8 次**
  (`12:14:14Z`–`12:16:34Z`),第 9 次 **`12:16:54Z PASS`**。
- **(ii) 通过,而且这次两条路都成立**:arm 串 42 → 44(被测配置真的变了),
  **且**树也变了 —— `2d1024ee..HEAD` 在 `bots/`/`game/` 上有 **2 个 commit**
  (`f11e1e26` 英雄组、`351389eb` 协同组的 gated `fieldsip`),`test_set.md` 上有 3 个。
  ⚠️ 按纪律**认 exit code 不认空输出**:`git rev-parse 2d1024ee` 在本容器里**解得开**
  (浅边界内),`git log` **exit 0** ⇒ 这是真的「有漂移」,不是 shallow clone 的空输出。
- **(iii) 通过**,见 §二,两种市场估价都过,保守余量 **$13.04**。

---

## 交棒

1. **⭐⭐ 下一轮本台 —— 收割 W25**。`waves/W25_wave.json` 已落盘待回填;
   走标准路径 `recover_verdict.py` 从 S3 逐局全量重算(**不用实例自产 verdict**),
   报告里 `mean` 旁边照抄 `min_arm_depth` / `thin_arm_seeds`,被 `THIN-ARM` 排除的种子**要点名**。
   **W26 的 6h 闸解锁 `2026-08-29T18:16:54Z`。**
2. **⭐⭐ 录像组 / 英雄组 —— W25 是 `odbuild` 与 `wkqdmg` 的取证波**,两条的前置门见 §十一,
   **先读前置门再读数**。载体供给:OD 3 粒(1603/1633/1664)、WK 3 粒(1633/1664/1770)。
3. **⭐ 总监 —— `#285` 仍未裁(第三轮)**:本轮 §五给出了它至今最锋利的一个样本 ——
   官方载体门对 W24 那四粒**在新 term 上 exit 0**,而那一组的 `wkqdmg` **只有 1 粒载体**,
   一台回收就归零。**「≥2 粒」是本台自律不是门**,这就是 #285 立案句的实测形状。
4. **⭐ 总监 —— 上一轮交上去的三条仍未裁**:`[harness] #298`(`recover_verdict.py:370`
   数错桶,吃掉铁律 2(b) 唯一的独立证人)、`[batch] #299`(铁律 4(i) 补作用域)、
   `reclaim_blind.py` 的 bracket 余量侵蚀(实测应收窄为 `(34.8, 40.6]`,余量剩 36 秒)。
5. **⭐ 协同组/总监 —— W24 判读仍欠**(gpm −43.12 / winrate 0.455 / 0-4;
   `#298` 修正后 winrate 是 203 局全 `natural_end` 的独立读数,不是 gpm 回声)。
   **W25 与 W24 的 id 集不同(44 vs 42),读数不可并序列。**
6. 存量催办:#207 `zusstatic` **第二十三波 armed**;#218 后续**第十九轮**不在第 2 行;
   #285/#291 仍未裁;**#282 连续第三波无 MISMATCH,建议关闭**;
   #217/#211/#225/#180/#171/#200/#181/载体门 PARTIAL/`stable-v*` tag/`campdanger`/§BL.4/#75 照旧。

**铁律 6**:`bots/`/`game/` **一行未改**;`luacheck_gate.sh` **exit 0 / 0 warnings**
(容器冷启,脚本自己装的 `lua-check`);`core.hooksPath=.githooks` 已上膛;
**未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行**;动态半(~100min,#124)**未跑且不声称**。
**MCP 未触发 `requires approval`。**

---

## Token 用量(铁律 8)

`TOKENS total_in=4,170,977 out=32,829 turns=40`

(统计时刻为收尾提交前;此后的收尾回合不计入。)
