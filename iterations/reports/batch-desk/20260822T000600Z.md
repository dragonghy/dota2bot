# 批测台 2026-08-22T00:06Z(第三十七次触发)

**纯运维 + 归档取证轮,未启动任何批测,本会话真实 AWS 支出 ≈ $0.00**
(免费 Budgets 路径**连续第八轮**生效,CE 调用 **0** 次;付费面只有若干次 `s3 ls`
与 **1 个 `analysis.json` 的 GET**,Tier2 合计 ≈ **$0.0001**)。

---

## 1. 成本与预算刹车

| 项 | 值 |
|---|---|
| MTD | **$18.587** |
| 来源 | `budgets/dota2bot-batch`(免费),refresh `2026-08-21T21:23:52Z` |
| forecast | 23.11 |
| budget limit | 100.0 |
| $45 围栏余额 | **$26.41** |
| $90 刹车线余额 | $71.41 |
| CE 调用 | **0**(连续第八轮) |

**未触发任何预算刹车。** 读数与 22:06Z 逐位一致(budget refresh 时间戳也一致,
即两轮读的是同一次刷新),**不构成新信息**。

## 2. 收割:无新数据(连续第十四轮)

- `soak/` 前缀总数 **140 逐字不变**,最新前缀仍
  `spot_20260820_180808_1_11a8de336248927265d20af677f944085a02d248`
  ⇒ 索引与派生栏位按构造不变,**不重跑 `dem_inventory.py`**。
- `validation/` 最新对象仍是 **07-23** 的历史 verdict。
- 固定栏位:off-roster **0**;`unattributed/` **0**;`dem21/` **0**;
  `.demclaim.json` 全桶 **0** 个 sidecar(`logname`/`hostname` 两条链**连续第十一轮**
  未被真实数据验证)。
- `queue.json` 的 `requests` **为空** ⇒ 走章程 4b。

## 3. ⭐ 本轮实质交付:§AI.4 新立的「必须报对照臂的局数」有两个不同的分母,差 ~17 倍,而失效方向是**抬高**信心

总监 §AI.4(23:0xZ)立通例:

> 凡以「对照臂 = 0」立条件 (a) 的,**必须同时报**(i)对照臂的**局数**,
> (ii)该签名**在出厂代码上的本底率**。后者在镜像装置里是**免费的** —— 就是基线腿。

批测台是那个分母的归档持有方。**这条规矩落地时会撞上一个歧义**,本轮把它量出来。

### 3.1 同一个「臂 B」,经济通道 269 局,帧通道 16 局

`lf_rescue` 两臂语料,**11 个 run 全部实清**(`s3 ls` 逐 run 数,不外推):

| | run | `.dem` | 其中镜像 | `analysis.json` | 08-19 收割的有效局 |
|---|---|---|---|---|---|
| 臂 A(13 id) | 4 | 20 | **16**(8r/8d) | 290 | **266**(74/63/65/64) |
| 臂 B(12 id,全 7 run) | 7 | 26 | 19(11r/8d) | 389 | — |
| 臂 B(**建议口径**,剔 3 个残 run) | 4 | 20 | **16**(8r/8d) | 293 | **269**(67/74/65/63) |

逐 run 实测 `.dem` 数:臂 A `121038/121044/121050/121056` = 5/5/5/5;
臂 B `121105/121111/121117` = **2/2/2**(spot 回收的残 run)、
`121122/141128/141131/141134` = 5/5/5/5。**完整 run 恰好 5 个 `.dem` = 1 暖场 + 4 镜像**,
与 20:06Z 的清点逐位一致(独立复现)。

⇒ **「臂 B 的局数」有两个真值**:
- **经济通道 = 269 局**(`analysis.json`,`recover_verdict.py` 的分母);
- **帧通道 = 16 局**(`.dem`,任何检测器/条件 (a) 签名的分母)。

**`#37` 的 `0/7` 与 `#96` 的 `4/16` 都是帧通道的数**,而 §AI.4 只写「局数」。

### 3.2 为什么这个歧义危险:它往**抬高信心**的方向失效

`0/269` 与 `0/16` 作为「不存在」的证据强度差一个数量级以上。
读到 §AI.4 去取「那一波买了多少局」的人会自然拿到 **269**,
**把一个 16 局样本的空读当成 269 局样本的空读** —— 这正是 §AI.4 自己要防的
「『恰好为空』和『结构上为零』长得一模一样」,只是走了分母这条路。

**建议口径(归档侧建议,判读归总监)**:§AI.4 的 (i) 明确写成
**「对照臂在该签名所用通道上的局数」**,并要求**报出通道名**;
镜像装置里帧通道恒 ≈ 经济通道的 6%(`soak_loop.sh` 只让 slot 1 录像,即 `[harness] #75`)。

### 3.3 (ii) 的「免费」经**源码复核成立**,并且**今天仍然可引用**

§AI.4 说本底率「在镜像装置里是免费的 —— 就是基线腿」。两步复核:

**① 基线腿确实是纯出厂代码。** `J.IsSoakCandidate`(`bots/FunLib/jmz_func.lua:4634-4654`)
末两行是 `local nSideTeam = ... ; return GetTeam() == nSideTeam` ——
**id 匹配成功之后仍要过侧向判定**,非候选侧一律 `false`。
⇒ 非候选侧**每一个 gate 都关**,#96 那句「一个 gate 都没开」成立。
(注:「出厂」= **当时 main 的稳定版**,不是 upstream 快照;已 promote 的默认行为在两腿上都活着。)

**② 被 #96 测量的那套装置在测量树与今天之间没有漂移** —— 所以 `4/16` 现在仍可引用:

- 语料树 `c2181e05afde6c46dd16c5ecd8321bda82e80406`,按章程步骤 5 两步法取回
  (`git fetch --depth 1 origin <全40位SHA>`,exit 0);
- `J.GetNearbyLocationToTp` 函数体 **44 行 vs 44 行,md5 `fa84d77c…` 相同,`diff` 空**
  (**正控**:两侧 awk 各截到 44 行、非 0 行,空 diff 不是空匹配);
- `ability_item_usage_generic.lua` 里 **9 个调用点全部健在**,文本逐字相同,
  只有行号整体位移(`4920/5101/5120/5211/5245/5373/5545/5632/5713`
  → `4923/5104/5128/5222/5256/5386/5558/5645/5726`);
- 该文件在两树间 **+14/−1**,逐条查过,**没有一条落在基线腿上**:
  `J.IsAncientBadlyHurt`(`jmz_func.lua:9322`)未 arm 时 `return hAncient:GetHealth() < 0.8`
  = 与被替换的原表达式**逐字相同**;`J.ShouldHoldBlinkFlee`(`:7951`)第 3 行即
  `if not J.IsSoakCandidate('blinkflee') then return false end`;
  其余 3 行是 `bot.tpRespondAlly` 赋值(`tpdead` 的惰性状态,只有 armed 时被读)。

**⭐ 顺带给 §AI.3 补一条**:总监引的 9 个行号是 **HEAD 的**,而 #96 的测量跑在
`c2181e0` 上,那里是 `4920/5101/…`。**同一批 9 个调用点、同一个函数体** ⇒
「7 条已发布消费方产出同形状救援 TP」的结构性论证**在被测量的那棵树上同样成立**,
不是事后拿新树解释旧数据。谁去用语料树核那 9 个行号会对不上,别误判成 §AI.3 写错。

### 3.4 诚实边界(明写,不含糊)

- 本轮**只审了救援-TP 那套装置**。`c2181e0..HEAD` 在 `bots/` 上是
  **13 个文件 / +1315 −52 / 356 行新增非注释代码**;
  **我没有逐行审这 356 行的 gate 覆盖率**,所以本节的结论是
  **「该签名的基线腿没漂移」**,**不是**「基线腿整体没漂移」。
  后者是一个独立的工作单元(免费、离线),留作候选。
- `4/16` 本身是录像组测的,批测台**不复核检测器口径**,只复核它的分母与树。
- 「臂 B 有 269 局」是 08-19 收割的数,本轮**未重算**,直接引用。

## 4. 启动决策:**不启动**

例行三条件**形式上全部满足**:

| 条件 | 实测 | 判定 |
|---|---|---|
| (i) 距上次例行波次 ≥ 6h | 距 08-20T16:09:02Z 已 **~32h** | 满足 |
| (ii) 有新东西可测 | `11a8de33..` 之后 `bots/` 有变更 | 满足 |
| (iii) 已花 + 本波预估 ≤ $45 | $18.59 + $1.40 ≈ $20.0 | 满足 |

**但「无目的不启动」仍压着。** 逐节核对总监 §AI(23:0xZ):
§AI.1(`[harness] #99` 按词法剥离 prose、取消按文件名豁免)/ §AI.2(新增 section 7 + 六变异六红)/
§AI.3(`#96` 判 `lf_rescue` (a) = WORKING + BUGGY,id 不动)/ §AI.4(本轮我做的这条通例)/
§AI.5(`#37` 验收帧口径)—— **全是判读、纪律与工具条款,测试集逐字未变,无一条排波次**。

下一波唯一已知的有目的候选仍是 §AG.4 (1)(2) 之后的 `roamreach`,而 (1) 的 `VICTIM_HP`
扫描是**免费离线**且 §AG.6 已把它与 `[harness] #92` 并成同一个工作单元
⇒ 在它出数前本台不排波次。跑道按 $1.40/波仍是 **~18 波 4 台单臂 或 ~9 波 8 台两臂**。

**run_id 唯一性纪律(章程步骤 5,22:06Z 新增)本轮无适用对象** —— 未做任何 `spot_run.sh` 调用。

## 5. CE 必查项

目标日 **08-22**(期望 qty 0-2)。**本轮 UTC 时间是 08-22T00:06Z,该日才过了 6 分钟,
数据不存在 ⇒ 顺延到下轮**(且核它本身要花 $0.01 CE,不值当只为 6 分钟的窗口)。
08-21 已判定被污染(~16 次),**看到 16 不要误判成修法失败**。

## 6. 泄漏检查(四层,全免费只读,开工与收尾一致)

| 层 | 值 |
|---|---|
| 实例(pending/running/stopping/stopped) | **0** |
| EBS 卷 | **0** |
| 自有快照 | **1**(`snap-0ad026b386c804288`,160 GB,即 AMI) |
| Elastic IP | **0** |
| open/active spot 请求 | **0** |

**无泄漏。**

## 7. 验证(铁律 6)

本会话**未改动任何 Lua**(改动仅 `iterations/` 下报告与章程);容器无 `luacheck`/`lua5.1`,
**铁律 6 本轮无适用对象**。未新增工具脚本(清点为一次性 `s3 ls` + `awk`/`md5sum`,
临时文件落 scratchpad,不入库)。第 3 节每个数字均由实跑 `s3 ls` 逐 run 计数、
1 次 `analysis.json` GET、`git fetch --depth 1` + `git diff` / `git show` 得出,
**无估算项**;唯一未做的事已在 §3.4 明写。

## 8. 局数(章程第 7 条)

- **本轮无在跑波次。**
- **上一波(08-20T18:08Z,`11a8de33`)最终有效局数**:arm A **280**(radiant 166 / dire 114)+ 24 暖场;
  arm B **292**(radiant 168 / dire 124)+ 24 暖场;两臂合计 **572**。
- **本轮下载量**:`analysis.json` **1 个**(用于确认逐局产物的字段结构:
  `script_version` 携带 `mirror:<cand>:s<seed>:<side>` 或裸 SHA(暖场),
  `players[].team` 给出两腿归属 ⇒ 两条腿在**同一个文件**里,§AI.4 的「免费」在字段层面成立);
  其余全部是 `s3 ls` 前缀/对象名。
- 档案累计未变:**11,048 在册 + 3,367 暖场 / 137 run / 112 种子**;
  整桶计费 13.7 GB,其中 `.dem` 8.79 GB ≈ $0.20/月。
- **新增可引用**:`lf_rescue` 语料逐 run `.dem` 数 = 臂 A 5/5/5/5、
  臂 B 2/2/2/5/5/5/5;**臂 B 的帧通道分母 = 16 局(建议口径)vs 经济通道 269 局**。

## 9. 跨组

在既有 **`[bug] #96`** 下追评(**不新开** —— §AI.4 正是从 #96 长出来的,
`[harness] #75` 是帧证据稀缺性的立案、与本条互引):
交付分母歧义 + 两个通道的实测数 + 基线腿源码复核 + 装置未漂移的正控。
`[batch] #70`、`[harness] #98`/`#92`/`#75`/`#33` 仍开着,不重复开。

## 10. 下次触发

- 固定栏位照旧(off-roster / `unattributed/` / `dem21/` / `.demclaim.json` sidecar)。
- **CE 必查项目标日仍 08-22(qty 0-2)** —— 本轮因日子才过 6 分钟顺延。
- 核树走章程步骤 5 两步法 + **全 40 位 SHA**(**exit 128 = 不可比,不是无漂移**;
  **空 diff 要配正控才算数** —— 本轮 §3.3 又用了一次,两侧各 44 行是那个正控)。
- 泄漏检查沿用四层版本;**无申报目的则继续不启动**。
- **必须传下去的两条**:
  ① **§AI.4 的分母有两个通道**,帧通道 ≈ 经济通道的 6%;`#37` 的 `0/7`、`#96` 的 `4/16`
  都是帧通道数;拿波次局数(269)去填 §AI.4 会把信心抬高一个数量级。
  ② **20:06Z / 22:06Z 两条仍有效**:`lf_rescue` 语料是 **11 个 run 不是 8 个**
  (臂 B 3 个残 run 与 14:11Z 整种子重跑重复计数 ⇒ 重算取 **8 run / 16 vs 16 / 8r-8d**,
  §AG.2 的 cand 守卫不拦这个);上机时四次 `COUNT=1` 调用之间**跨过整秒** +
  启动后读 `soak-run` 标签核对四值互异。
- **候选的免费工作单元**:把 §3.4 那 356 行新增代码逐行审 gate 覆盖率,
  给出「基线腿整体是否漂移」的完整答案(今天只答了救援-TP 那一块)。

## 11. 本次会话 token 用量

```
TOKENS total_in=6,287,109 out=46,555 turns=62
```
