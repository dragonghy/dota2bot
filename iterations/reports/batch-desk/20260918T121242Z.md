# batch-desk 2026-09-18T12:12:42Z — 刹车第四十九轮持有

**零发波、零收割欠、零泄漏;`bots`/`game`/`tests`/`tools` 一行未改。**

⭐⭐ **本轮实质:GH #291 的「经济修正分支不可达」从源码论断变成了存档实测 ——
本台自己归档的 W24–W69 共 3,173 局里,`economy_25min_cap` 桶是 `0` 局,
而聚合器现读是一张不截断的全直方图 ⇒ 它有一个也会显出来。**

---

## 一、开工自检(铁律 10)

⚠️ **第一条命令又踩了管道坑,同形第十例**,逐字
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.` +
`it has recurred 5x, every time as the first command of the round.`
第二跑改 `timeout` 又被拒,逐字
`REFUSED: routine_selfcheck.sh is running under \`timeout\`; exit 2, nothing checked.`
+ `This has recurred 3 rounds running.`
⇒ **第三跑才用对**(`nohup … > /tmp/sc.log 2>&1 &`,⛔ 无管道、⛔ 无 `timeout`)。
⛔ **归因是姿势不是工具**,且上一轮已把取法钉死、本台**仍然没有一次到位** ——
⚠️ 上一轮交棒 ⑥ 逐字写的就是「开工第一条命令 `nohup … &`,**一次到位**」,本轮**没做到**。

首行逐字 `push gate armed (core.hooksPath=.githooks); \`git push\` now runs the rule 6 static gate`;
`OK: no unlanded work in the certifiable window.`

**⛔ 自检真码本轮不可得,并且是实证不是推断**:收尾回读输出文件 **645 行**,停在
`=== trunk health (python test suite) ===`,**收尾三行横幅一个都没打**;
按上一轮补的取法**再 `pgrep` 一次** ⇒ **进程仍在**(`pgrep -c -f routine_selfcheck` = 3)
⇒ ⛔ 无 `selfcheck worst exit`,**不写、不读成通过**;**未跑完的腿这轮没人看过**。
按铁律 7/11 ⛔ 不空转等它。

---

## 二、预算 —— ⭐ 快照冻结轮,零新支出测量

`AWS_SETUP_EXIT=0`(容器无 CLI,自装 `aws-cli/1.46.1`;身份逐字
`AWS ready: arn:aws:iam::581436534924:user/dota2bot-agent` + `(restricted batch-runner user — correct)`)。
`COSTS_EXIT=0`(⛔ 未走管道)。

闸 (iii) `FENCE_EXIT=3`(**连续第四十九轮是闸拒绝,不是本台的处置**),逐字:

```
actual (MTD)     : $92.292   <- re-read this run, never cached
pending waves    : $0.000   (launched, may not be in MTD yet)
planned          : $0.000
projected total  : $92.292
fence            : $100.00   <- lowest ACTUAL alert not yet crossed (Ruling 1)
brake            : $90.00   (owner's line, not derived here)
operative ceiling: $90.00   = min(fence, brake)
A launch at this instant would put MTD past the $90.00 BRAKE line.
WAVE_FENCE: THROTTLED (exit 3) -- brake, not fence. Stop and report to the owner; do not wait it out.
```

RULING 5/6/7 三行照抄:
`accrual scope    : 17 region(s) read [...]  <- COMPLETE: every region this account has enabled`
`wave accrual     : records after 2026-09-17T19:40:29Z (= 2026-09-18T06:58:29Z - 11.3h lag, clock from budget snapshot)`
`accrual check    : CERTIFIED (0 accruing instances account-wide, read this run) -- pending $0.000 is a reading, not a default.`
⭐ **钟没有降级**(`clock from budget snapshot`,不是 fallback 措辞);⛔ 未传任何 `--no-*` / `--pending` / `--director-crossing`。

**⭐ 本轮是快照冻结轮**:MTD `$92.292` / 戳 `2026-09-18T06:58:29Z`,与上一轮(09:17Z)**两量逐位相同**
⇒ **零新支出测量**(⛔ 不等于「没花钱」)⇒ 按交棒 ⑤(e) **一个速率点都不贡献**,
⛔ 不动 `$0.28–0.92/天` 带、⛔ 不收窄。

**月末数逐字结转,⛔ 未重算**(锚点是同一张快照,重算只会得到同一个数):
`$100` 兜底日 **`2026-09-26T16:03Z`**,距今 **8.2 天**。
**headroom 对 `$90` = `$-2.292`**(与上一轮逐位相同,本轮**可以**这么说)。
`alpha = 0` / `beta = 0` 本轮实读;`forecast 115.334` ⛔ 不解释不采信;
`crossing registry: 0 record(s) ... none in force` + `1 retired record(s)`;`budget filters : none`。

**GH #801 四行整块照抄**(**默认值**跑,⛔ 未用 `COST_CONFIRM_AT=999` 自救):

```
   >= $35, but headroom to the $90.00 brake is $-2.292
   < $1.10 (cheapest wave) — CE confirmation SKIPPED, NOT passed (GH #801).
   Nothing can launch at this MTD, so confirming it buys nothing.
   Resumes by itself as soon as headroom >= $1.10.
```

**SKIP 不是 pass**;欠条 `gh801_confirm_headroom_first_live_read` **仍 OWED**,⛔ 本台不自行结清。

---

## 三、收割 —— 零欠

`validation/` **592 对象**、`soak/` **72,079 键**(均与前六轮逐位相同)。
最新对象按 RULING 47 取法仍是 `lf_rescue+27ids-d177b87026dd_20260912_1018_run.log`(`2026-09-12 10:18:26`)= **W69**。
在跑波次:**无**。`recover_verdict.py` 未跑(**没得收,不是漏收**)。

⭐ **顺带结清上一轮自己挂的一处不一致**:上一轮登记「同一对键数两轮得出 `79` 与 `74` 两个数」。
本轮在**逐位相同**的两个键数上独立重算:`ceil(592/1000) + ceil(72079/1000)` = `1 + 73` = **74**
⇒ **74 复现**。⛔ 仍不主张 `79` 错(那一轮的算法没留下),只登记「74 连续两轮由同一算术得出」。

---

## 四、⭐⭐ 实质:GH #291 的不可达性,存档侧实测 `0 / 3,173`

### 4.1 立案理由

上一轮交棒 ④(b) 要本轮现读「裁判伪影修没修」。**现读:没修** ——
`referee_surrender` 在 `tools/batch_test/soak/` 下仍 **0 处**;
`analyze_log.py:96` 逐字 `natural_end = fort is not None` 未动。
⇒ 刹车解除后第一波的 `winrate` 仍是裁判伪影。

**本轮多走一步**:这条此前一直是**源码论断**(GH #291,2026-08-29 立,**至今 0 条评论、20 天无人回应**),
而本台手上有一份**零成本的存档语料**可以独立检验它 —— `iterations/reports/batch-desk/waves/`(在仓库里,⛔ 不碰 S3)。

### 4.2 机制(源码现读,`analyze_log.py:94-116`)

```
fort = next((t for t in base.get("towers", []) if str(t.get("building","")).endswith("_fort")), None)
natural_end = fort is not None
winner_by = "engine"
if natural_end:                                          # ← 先问这一支
    winner_by = "engine_natural"
elif ... and dur_min >= cap_min - 0.5:
    winner_by = f"economy_{int(cap_min)}min_cap"          # ← 合同分支
```

裁判在 cap 上强制结束时**会把遗迹推掉** ⇒ `towers` 里出现 `_fort` ⇒ `natural_end` 为真
⇒ **第一支恒被取走,合同分支对「被 cap 强制结束的局」结构性不可达**。

### 4.3 存档侧实测

取 `iterations/reports/batch-desk/waves/` 全部 67 个文件,递归取出每一处 `winner_by` 桶表:

| 量 | 现测 |
|---|---|
| dict 值的桶表 | **20 张** |
| **全部 20 张里出现过的 distinct 桶键** | **`['engine_natural']` —— 只有这一个** |
| 去重(剔除 `W28`/`W66W67` 两个 pooled 派生表)后的主表 | 17 张 |
| **主表总局数** | **3,173 局,100% `engine_natural`** |
| 另有散文一处(`W25W26_pooled`) | 逐字 `engine_natural 347/347 -- none of these are the analyze_log.py econ_winner rewrite` |

⇒ **`economy_25min_cap` 与 `economy_forcewin_recovery` 在整份存档语料里各 `0` 局。**

语料界别已核:最早的 `W24_wave.json` 现读日期 **`2026-08-29`**,而 cap 10→25 界线是 **`2026-08-25`**
⇒ **W24–W69 全部在界后**,⛔ 不含界前局。

### 4.4 ⭐ 把最危险的那条诚实边界关掉了

**本来朝危险侧失效的假设**:如果那 20 张表是「只印头号桶」的摘要,
那么一个非零的少数桶**会隐身**,`0/3,173` 就是**虚高**,方向对本台的结论有利 —— 正是必须先证伪的那一侧。

**现读证伪**(`recover_verdict.py:532-533`):

```python
by[a.get("winner_by") or "unknown"] = by.get(a.get("winner_by") or "unknown", 0) + 1
v["winner_by"] = by
```

⇒ 这是一张**逐局全直方图**:无截断、无 top-N、无过滤,连缺值都单开一个 `unknown` 桶。
**只要有一局被判成 `economy_25min_cap`,那个键就会出现在表里。**
⇒ **`0/3,173` 站得住**:不是「没印出来」,是**没有发生过**。

### 4.5 于是撞出来的那一下

`state.json:1214` 给的语义逐字:`engine_natural` …**「its share IS the natural-end rate」**。
⇒ 任何读这 20 张表的人读到的是 **「100.0% 的局以遗迹被真实推掉告终」**。
而 00:24Z 那轮测到的是 **界后 95.8% 的局由裁判在 cap 上强制结束**。
**两者不能同真**,而 §4.2 的机制正好说明是哪一个是伪影。

⚠️ **诚实边界四条**:
① 本轮**未重测** 95.8%(那是 00:24Z 轮的读数,本轮**引用不重算**),⇒ 本节证的是「存档侧 100% 且合同分支 0 局」,
**不是**「95.8% 这个数本身正确」;
② 20 张表是**波次级**聚合,⛔ 未逐局回读 `analysis.json`(那要 S3 出网,正是把 MTD 推过线的那一项)
—— 但 §4.4 已证聚合器不丢桶,**这条边界因此不朝危险侧失效**;
③ ⛔ **不点名任何 promote / 条件 (b) 判定引过这些表** —— 那是上一轮 `15 份 / 5 天` 那条线在管的事,本轮不并案;
④ ⛔ 本台**不改 harness**(`tools/` 一行未动),GH #291 归总监/harness 域。

---

## 五、跨组

- **GH #291**(`[harness]`,open,**0 条评论,20 天**)—— §4 是它的**独立存档侧证据**,
  把「源码读出来不可达」升级成「3,173 局实测 0 次,且聚合器有一次也会显」。
  本轮**已按 GH #290 顺序:先 push,后发表**(见 §八)。
- **GH #184**(交棒 ②)—— 现读 `state: open` / `comments: 3` / `updated_at 2026-09-18T00:36:03Z`,
  三值与前三轮逐位相同。按交棒 ② **只登记戳/评论数,⛔ 不重复发表同一份证据表**。
- **GH #779** —— 现读 `state: open` / **`comments: 19`** / `updated_at 2026-09-17T09:43:51Z`。
  ⚠️ **逐条核过 19 条的作者与时间:全部是本台自己历轮发的**(最后一条即本台 09-17T09:43:51Z 那条)
  ⇒ **owner 第三十轮零表态**。⛔ 本轮未追评(无新预算事实,§二 是冻结轮)。
- **GH #721** —— 现读 `state: open` / `comments: 2` / `updated_at 2026-09-10T21:31:57Z`,三值冻结。

---

## 六、局数

本月无新波次;本轮**未读 `analysis.json`、未读 `verdict.json`**(⛔ 0 局)。
§4 的 3,173 局是**仓库里已归档的历史读数**,⛔ 不是本轮新测的局。

---

## 七、成本(铁律 1 三段式)

**零 EC2 / 零 CE / S3 读取 `74` 个对象(出网未计价)** ——
`s3 ls --recursive` 2 次(`ceil(592/1000) + ceil(72079/1000)` = `1 + 73` = **74 LIST**),**不下载任何对象**。
**本轮新增 EC2 计费 `$0.00`**;`check_costs.sh` 未买 CE(GH #801 SKIP 分支)。
⭐ **§四那份实质产出一个 S3 对象都没碰**(语料在仓库里,纯本地聚合)。

---

## 八、泄漏(五条独立路径全零,⛔ 不合并成一句)

① `check_costs.sh` 自带 `== running/pending instances ==` 区块**空**(`COSTS_EXIT=0`);
② `describe-instances` 五态(`pending,running,shutting-down,stopping,stopped`)**不加 tag 过滤**
⇒ `INST_EXIT=0` / **`LINES=0`**;
③ `describe-spot-instance-requests State==open||active` ⇒ `SIR_EXIT=0` / **`SIR_LINES=0`**;
④ 闸 (iii) 账户级 **17 区 COMPLETE** + `CERTIFIED (0 accruing instances account-wide, read this run)`;
⑤ AMI 仍 `ami-0a990a26d89c66547 / dota2bot-batch-20260718` 一张(常设成本非泄漏),快照无新增。

---

## 九、通知判据(四条逐条现读)

| 判据 | 现读 | 中? |
|---|---|---|
| MTD ≥ `$100` | `$92.292` | ✗ |
| 泄漏 | `alpha = 0`,五路全零 | ✗ |
| 刹车解除(headroom ≥ `$1.10`) | `$-2.292` | ✗ |
| `09-27T00:00Z` 仍无表态(兜底) | 今天 `2026-09-18`,兜底日 `2026-09-26T16:03Z`,**距今 8.2 天** | ✗ |

⇒ **四条一条未中 ⇒ ⛔ 本轮不推送。**
⭐ 与前几轮同样严格:§四 虽是本轮实质产出,但它**不改任何阈值、不需要 owner 做任何决定**
(它是 harness 域,球在总监/GH #291)⇒ **第 49 次「刹车仍持有」对 owner 是零信息**。
⚠️ **交下一轮**:兜底日 `2026-09-26T16:03Z` 距今 8.2 天,第四条一旦够到**推送就该发**,⛔ 不要等到那天才开始算。

---

## 十、没做的事

⛔ 未发波(闸 exit 3);⛔ `bots`/`game`/`tests`/`tools` 一行未改;
⛔ 未重开任何已关档裁定;⛔ 未改 `state.json`/`test_set.md`/`OWNER_PRIORITIES.md`/`queue.json`;
⛔ 未自行结清 GH #801 欠条;⛔ 未收窄速率带(冻结轮,一个点都没贡献);
⛔ 未重测 95.8%;⛔ 未逐局回读 `analysis.json`(要 S3 出网);
⛔ 未修 harness(GH #291 归总监域);
⛔ 闸 (i)/(ii)/(iv) **未跑**(发波轮的闸;(iii) 已 exit 3)——**未跑不记成通过**。

**铁律 9**:`OWNER_PRIORITIES.md` **P4.1 upstream 标尺波连续第四十九轮欠**,
⛔ 唯一阻因是闸 (iii) `exit 3`(预算),**不是本台未认领,也不是排期**。

---

## 十一、交棒

- ① ⭐⭐ **owner —— 刹车第四十九轮持有,GH #779 第三十轮零表态**(19 条评论逐条核过,全是本台自己发的)
  ⇒ 球仍在 owner:提高预算线 / 给预算加过滤器 / 接受本月剩余批测全停;
- ② ⭐ **总监 —— GH #291 请收**:§四把它从源码论断升级成 `0/3,173` 的存档实测,
  并把「聚合器会不会丢桶」这条朝危险侧的边界**关掉了**(`recover_verdict.py:532` 是全直方图);
- ③ **总监 —— GH #184 连续第四轮零表态**,三值逐位冻结,本台按交棒 ② 只登记不追评;
- ④ **总监 —— 上一轮交棒 ①③④⑤ 本轮未获回应,逐字保留**,⛔ 本台不自行退休任何一行;
- ⑤ **下一轮本台**:开工第一条命令 `nohup … > /tmp/sc.log 2>&1 &`(⛔ 无管道、⛔ 无 `timeout`,**一次到位**)
  —— 本轮**第三跑才对**,同形第十例,⛔ 不许再错;
  刹车解除(headroom ≥ `$1.10`)时按规格发 **P4.1 标尺波**,并把 #801 那四行**恢复后**的样子抄进报告。

---

## 十二、token 用量

见本节末尾「补记」(按铁律 8 于收尾时跑 `tools/agent/token_usage.py` 贴入)。
