# 批测台 2026-08-27T18:25Z(第 N+2 次触发)

**一句话**:**W17-R 与 W17 同样归零** —— 补跑的四台又全落 `us-west-2b`,存活 **~17.5 min**(比 W17 的 27.5 min 更短),
64 局**全是 radiant 单腿孤儿**,`per_seed` 有 4 粒但 **`ba_games` 全为 0 ⇒ `mean: {}`**,零可用种子;
两波合计 **$0.79 买到 0 个计分种子**,池子**连续第三轮卡在 4 粒**。
⭐ **但 GH #252 已被总监落地**(`f1115f3d`,15:59Z):`spot_run.sh` 现在有 `--az`,`aws.env` 有 `AZ_LIST`。
本轮 **W18 是它的第一次实测,验收条件通过**(4 台落在 **3 个不同 AZ**)——
**同一波也暴露出残留缺口**:`--az` 固定失败时回退成「让 EC2 选」,把一台送回了刚清空两波的 `us-west-2b`,
分散度从 4/4 掉到 3/4。已立 **GH #256**。

---

## 1. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` — **worst exit 3**(有发现,非未核验):

| 腿 | 结果 |
|---|---|
| push gate 上膛 | `core.hooksPath=.githooks` **已 armed** |
| unlanded work | `OK: no unlanded work in the certifiable window`(⚠️ shallow clone,462 refs 里 **421 条 REFUSED/不可核验**,比上一轮的 418 又多 3 条) |
| 报告节奏 | **1 处洞**:`replay-check` 08-27T07:19Z→13:28Z **6.1h**(与上一轮同一处、同一数值)——**不是本台的** |
| 未裁队列请求 | RIDESHARE **1**(`hero-20`,pending prio=3)/ OTHER **0**;total open **37**(上一轮 36) |
| stable-v* 锚点 | `stable-v1` / `stable-v2` **两个都 ok**(EXISTS/PINNED/SHIPPED 全绿) |
| trunk python 套件 | **44 passed / 0 failed / 0 uncertifiable**(上一轮 43,**+1**) |
| trunk 快 Lua 检测器 | **12 文件 0 失败**(⚠️ FAST SUBSET,不是全套) |

---

## 2. 成本与预算闸

`check_costs.sh`:MTD **$50.671**(budgets,免费)= CE **$50.6709276212** **逐位一致(连续第十轮)**。

⚠️ **仍是同一张快照**,`budget refreshed 2026-08-26T23:46:06Z` **逐位未动**(与上一轮同一张)⇒
**W15-R / W16 / W16-R / W17 / W17-R 一台都没进去**,滞后**照旧朝危险侧失效**。围栏因此不用裸 MTD:

| 项 | 金额 | 口径 |
|---|---|---|
| MTD(budgets,滞后) | $50.671 | 免费读数 |
| W15-R(00:16Z,按需) | $1.69 | 已登记 |
| W16(06:16Z,spot) | $0.80 | 已登记上界 |
| W16-R(09:14Z,spot) | $0.190 | 账单侧实测 |
| W17(12:20Z,spot,**整波回收**) | $0.479 | 账单侧实测 |
| **W17-R(15:14Z,spot,整波回收)** | **$0.313** | **本轮账单侧实测**,见 §3.3 |
| **W18(18:20Z,spot 4 台)** | **$2.24** | 本轮**按按需口径**保守预估,见 §5.5 |
| **围栏合计** | **$56.38** | **≤ $60 ⇒ 闸 (iii) 满足** |

- **$50 档的书面解释(章程 §4b(iii) 配套义务,续记)**:MTD 早在前几轮已越过 $50,
  **本轮 budgets 读数逐位未动 ⇒ 无新增跨档**。本轮新增 **W17-R 实测 $0.313(零产出)+ W18 预估 $2.24**。
  刹车线 **$90** 未接近;owner 批准线 **$100**。
- 常驻资源只有 AMI `ami-0a990a26d89c66547` + 快照。

---

## 3. ⭐⭐ 主产出之一:W17-R 收割 —— 与 W17 同样归零

### 3.1 先查 SIR(上一轮交棒第 1 条点名的前置动作)

`describe-spot-instance-requests`(CreateTime ≥ 14:00Z):

| SIR | 实例 | AZ | Code | Create | Update |
|---|---|---|---|---|---|
| `sir-7b6qkwgq` | `i-04ab17618729dd4e3` | us-west-2b | `instance-terminated-no-capacity` | 15:14:19Z | **15:33:57Z** |
| `sir-p6zfhxvm` | `i-0ac674ab4654b964f` | us-west-2b | `instance-terminated-no-capacity` | 15:14:23Z | **15:31:57Z** |
| `sir-tt5zg9gn` | `i-0fe6d12c62731ee1c` | us-west-2b | `instance-terminated-no-capacity` | 15:14:28Z | **15:31:57Z** |
| `sir-4n5fhtiq` | `i-094bb86ed818d494c` | us-west-2b | `instance-terminated-no-capacity` | 15:14:33Z | **15:31:57Z** |

**四台全部被回收,存活窗 ~17.5 min —— 比 W17 的 27.5 min 还短。**
⇒ 上一轮交棒里那句「若再度整波回收,`per_seed` 会再次为 `[]`,**那时不要把 4 粒读数当成 8 粒的检验结果**」
**本轮原样兑现,并照此执行**(§3.4)。

### 3.2 收割读数

四个 run 目录(**每 run 独立子目录再指向父目录**,GH #225 的 R2 拒答保护;**不传 `--cand-ref`**,本波是 vs_stable 单臂):

```
recover_verdict.py <w17r 父目录> "<40-id / 354 字节串>"
  input: files_seen 64 / games_loaded 64 / source_dirs 4 / unparseable 0 / pooled_basename_dirs_overridden 0
  per_seed: 918 ab10/ba0 | 921 ab10/ba0 | 924 ab11/ba0 | 926 ab10/ba0
  mean: {}          comps_better: {}
  contrast=vs_stable  cand_ref=null  suggested: hold_or_reject
```

**⚠️ 与 W17 的形状差一格,结论相同**:W17 的 `per_seed` 是**空表**;W17-R 的 `per_seed` **有四项**,
但**每一项 `ba_games` 都是 0** ⇒ 没有一粒种子跑到第二腿 ⇒ `mean` 仍为空。
**「per_seed 非空」这次不等于「有可用种子」** —— 记下这条读法,免得下轮被表面形状骗过去。

### 3.3 单波成本(账单侧实测)

`us-west-2b` spot **$0.2557/h** + $0.005 IPv4 = $0.2607/h;逐台存活窗求和 **1.2014 h**:

| 实例 | 存活 | h |
|---|---|---|
| `i-04ab…` | 15:14:19→15:33:57 | 0.3272 |
| `i-0ac6…` | 15:14:23→15:31:57 | 0.2928 |
| `i-0fe6…` | 15:14:28→15:31:57 | 0.2914 |
| `i-094b…` | 15:14:33→15:31:57 | 0.2900 |

`1.2014 × 0.2607` = **$0.313**(上一轮预估 $0.869 ⇒ **实测只有预估的 36%,因为跑不满**)。
**买到零计分种子。** W17 + W17-R 合计 **$0.79 / 0 粒**。

### 3.4 8 粒池化:预登记**连续第二轮**答不了

- W17-R 计分种子 **0 粒** ⇒ 并进 `d9585a29` 树的池子后,**分子逐位不变**。
- 因此本轮**不重跑**那次池化(重跑会得到与上一轮**逐位相同**的输出,是纯支出无信息):
  池化读数仍是上一轮登记的 **`gpm −40.22 / xpm −19.09 / deaths +0.28 / last_hits −1.89 / winrate 0.499`,
  `comps_better` 各 1/4(last_hits 0/4、winrate 2/4),`scored_games 200`,4 粒**。
  **诚实边界**:这一段是**算术推论**(0 粒进分子 ⇒ 读数不动),**不是本轮的一次新执行**。
- ⇒ 上一轮 §3.4 的预登记(「n 翻倍 ⇒ SE 应从 ±77 降到 ±55,看 ab 层是否稳住」)
  **连续第二轮无法执行**;**既非证伪亦非证实,是语料没到手**。本台**不**拿 4 粒读数冒充 8 粒的检验。
- ⛔ **孤儿存量再长**:W15 的 30 局 + W16 里 896 的 27 局 + W17 的 128 局 + **W17-R 的 64 局** = **249 局单腿孤儿**,至今未并进任何池。

---

## 4. ⭐⭐ 主产出之二:GH #252 已落地,W18 是它的首次实测

### 4.1 修复确认(不是本台做的,是总监 15:59Z 落的)

`f1115f3d director 15:59Z: GH #252 落地 —— 而 issue 建议的「第 N 台取第 N 个 AZ」在 4×1 下恒等于不修`

- `spot_run.sh` 新增 `--az`(单值固定 / 逗号列表轮转)、`--no-az-spread`,`AZ_SPREAD=1` 为默认;
- `aws.env` 新增 `AZ_LIST=us-west-2a,us-west-2b,us-west-2c,us-west-2d`(注释注明四个 AZ 已核验 `available`);
- 总监的加注很到位:**4×1 拓扑下「第 N 台取第 N 个 AZ」恒等于不修**(四次独立调用里 N 恒为 1),
  所以实现改成了**每进程随机起点 + 显式 `--az` 保证确定性**。本台按后者用。

### 4.2 W18 起飞后的实际落位

| 种子 | 请求 AZ | **实际 AZ** | 实例 | `InstanceLifecycle` | launch |
|---|---|---|---|---|---|
| 918 | `us-west-2a` | **us-west-2a** ✅ | `i-0259ffdc874167606` | **spot** | 18:20:00Z |
| 921 | `us-west-2c` | **us-west-2b** ❌ | `i-05e52b9104584c699` | **spot** | 18:20:05Z |
| 924 | `us-west-2d` | **us-west-2d** ✅ | `i-0bfbc256216fbefbf` | **spot** | 18:20:23Z |
| 926 | `us-west-2b` | **us-west-2b** ✅(有意) | `i-09f116202274284ed` | **spot** | 18:20:27Z |

- **#252 的验收条件通过**:`Placement.AvailabilityZone` 取到 **3 个不同值 ≥ 2** ✅,**零额外支出**(搭本波的车验的)。
- **四台全 `spot`**,`run_id` 的 `spot_` 前缀**没有**被当证据 ✅;降级阶梯(`--on-demand`)一级未用。
- ⚠️ **残留缺口(→ GH #256)**:921 那次调用 `spot_run.sh` 打印 `az=<ec2 chose>`,耗时 **18s**(其余三次 4–5s),
  形状与「按 `us-west-2c` 试 → 容量失败 → **去掉 AZ 约束**重试」一致,而 EC2 选的正是
  **刚刚连清两波的 `us-west-2b`**。**回退的默认目的地是被点名的那个 AZ** ⇒ 分散度 4/4 → 3/4,
  且掉的方向恰好最坏。工具本身**是诚实的**(它明说了固定没生效),问题在回退策略不在可观测性。

### 4.3 为什么本轮 926 有意放进 `us-west-2b`

不是疏忽,是选择:**满格分散**(一台一个 AZ)让「一次 AZ 事件最多损失 1 粒种子」——
这正是章程 §5「回收处置」预设的形状。替代方案(把 926 也放 2a)会让 2a 的一次事件损失 **2 粒**,
尾部更差。代价是 926 大概率再输一次;**上界是 1 粒,不是整波**。
(921 被回退送进 2b 之后,2b 实际承载 **2 粒** —— 那是 §4.2 的缺口造成的,不是本条选择。)

---

## 5. W18 发波纪律(三条闸逐条)

### 5.1 闸 (i) 6h 节流 —— **先打钟点,没有写死 epoch 抢跑**

例行锚点 = **W17 首台 `12:19:57Z`**(W17-R 是补跑,**不作锚点** —— 沿用上一轮已写死的裁法)
⇒ W18 解锁 **`2026-08-27T18:19:57Z`**。
`date -u` 读到 **18:11:18Z** 时还差 **518s**,轮询到 **18:20:00Z** 才发第一台。

### 5.2 闸 (ii) 树可比 —— 仍 pin `d9585a29`,理由第三轮成立且更强

远端 tip 已是 `fc682de9`;**本波不测 tip**。理由与前两轮同一套:
**池子仍是 4 粒,缺的是种子不是新树**;发 tip 会把池子清零重来。
`git cat-file -t d9585a29…` = `commit`(可达)✅。
**诚实边界**:tip 上的天赋定价 commit(`fc682de` 等)**本波仍测不到** —— 这是**连续第三轮**记在账上的权衡,不是遗漏。

### 5.3 两道门(发波前)

| 门 | 调用 | 结果 |
|---|---|---|
| **接线门** | `check_armed_wiring.py --cand <40串> --ref d9585a29…` | **exit 0**,`all 40 armed ids wired on d9585a29…` |
| **载体门** | `seed_draft.py 918 921 924 926 --assert-carrier 'crystal_maiden,zuus,lion,obsidian_destroyer,skeleton_king'` | **exit 0**(`CARRIER_GATE terms=5 seeds=4`) |

载体剖面与前两轮**逐字复现**:CM/zuus FULL 4/4;`skeleton_king` PARTIAL 2(918/924);
`lion` PARTIAL 1(921);`obsidian_destroyer` PARTIAL 1(926)。
角色门 / 等级门 id 的载体门仍 **no-op**(缺口 **GH #140**,**第二十一轮**)。

> ⚠️ **本轮一个操作教训**:接线门脚本路径是 `tools/batch_test/check_armed_wiring.py`,
> **不是** 上一轮报告里写的 `tools/batch_test/soak/check_armed_wiring.py`。
> 按旧路径调用会 `No such file`,而如果那次调用被 `| tail` 接着,`$?` 读到的是 **`tail` 的 0**,
> **一道没跑成的门会读成 exit 0 通过**。本轮已改成先落盘再取 `$?`。**下轮照此写。**

### 5.4 启动纪律

`--dry-run` 先行(ref / `slots=16` / `rec_slots=12` / `watchdog=2h` / `region=us-west-2` / `az ring` 全对上,
**波次预算闸未拒发**);**未传 `--on-demand`**、**未传 `--cand-ref`**;四次调用之间隔 2s。
拓扑 4 台 × 1 种子;`--games 12`;自毁 + `shutdown-behavior=terminate` + 2h 看门狗。
`--rec-slots 12`(GH #75 **第十三轮维持 12**)。

### 5.5 单波预估

`4 × 0.8328 h × $0.673`(**按需口径**)≈ **$2.24**。
按 GH **#233 (a)** 未裁,本台保守默认**一律按最坏情形记账,跑成 spot 算省下来的**;
实际若如愿应落在 **~$0.87**,$1.37 的差额是这条默认的已知代价。
**新数据点(续 §3.3)**:W17-R 实测 $0.313,即**回收波跑不满使「按需口径」偏离到 7 倍**,请总监一并裁。

---

## 6. 局数(铁律 §7)

- **(a) 上一波次定案 —— W17-R**:**64 落盘 / 0 计分**(4 粒各 ab10/10/11/10,**ba 腿全 0**)/ **零可用种子**。
  上一轮预期 **~208 落盘 / ~180 计分** ⇒ **落盘 −69%、计分 −100%**。**原因是整波回收,不是抖动**(§3.1)。
- **`d9585a29` 树池子**:**415 落盘 / 200 计分 / 4 粒**(与上一轮 351/200/4 相比,**落盘 +64、计分 +0**)。
  ba 占比(计分)= 61/200 = **0.305**,逐位未动(W17 / W17-R 均未参与该分母)。
- **(b) W18 在跑**:4 台 × 1 种子 × `--games 12` × 2 腿,预期 **~208 落盘 / ~180 计分**。
  **诚实边界**:W17 与 W17-R **连着两波**证明这只是期望值不是下界;本波 4 台里**仍有 2 台在 `us-west-2b`**(§4.2)。

---

## 7. ⭐ GH #256 立案(本轮唯一新立案)

**`[harness] #252 后续:--az 固定失败时回退成「让 EC2 选」,当场把实例送回被点名的 AZ(W18 实证 2/4)`**

请求:把 `--az` 固定失败的回退从「放弃 AZ 约束」改成「**走 ring 里的下一个 AZ**」,
走完一圈仍失败才退回「让 EC2 选」,并打成**显式告警行**而非与正常路径同形的 `az=<ec2 chose>`。
次要:`us-west-2b` 今天两次整波清零,**是否临时移出 `AZ_LIST` 归总监裁**(本台不自改共享配置)。
**验收零额外支出**:下一波 4 台取到 4 个互不相同的 AZ,且容量失败时日志有「ring 内改投」行。

**价格因素已排除**:`describe-spot-price-history`(18:00Z 前 30 min)四 AZ 无尖峰
(2a $0.2227 / 2c $0.2334 / 2d $0.2467 / 2b $0.2557)⇒ 纯容量事件。
**事前仍查不到**:`GetSpotPlacementScores` 对 `dota2bot-agent` 是 `UnauthorizedOperation`(#252 请求 (2) 未落地)。

---

## 8. 泄漏检查(两次)

- **开工**:`check_costs.sh` 的 running/pending 段 **空** —— W17-R 四台**已被回收终止**(§3.1),**无泄漏**。
- **收尾**:`describe-instances` **恰 4 台在跑**(本轮自己发的 W18,id 与 §4.2 逐个对上,全 `running`/`spot`),
  常驻只剩 AMI + 快照 —— **不是泄漏**。

⚠️ **保留作证边界(连续第五轮)**:读到 N 台**只能**证明「此刻在跑的是哪几台」,
**不能**证明「过去这一轮没漏过」。路 4(SIR)本轮**第四次**补上部分回溯
(它正是 §3.1 那张表的来源,**且再次是唯一能说清「四台是被回收不是自终」的证据源**),
**但其保留窗口仍未知,不构成完整回溯审计**。

---

## 9. 铁律 6

- 静态门 `bash tools/agent/luacheck_gate.sh` 由 `.githooks/pre-push` 在 push 时自动执行,
  `core.hooksPath` 开工自检已上膛;**未使用 `RULE6_BYPASS`**。
- 动态半(`lua5.1 tests/run_tests.lua`,~100 min,GH #124)**未跑,也不声称**。
- **本轮 `bots/` 与 `game/` 一行未改**(改动只有 `iterations/` 下的报告与章程)。

---

## 10. 交棒

1. **⭐ 下一轮本台 —— 收割 W18**,四个 run 前缀
   `…_998ecd`(918/2a)/ `…_f43908`(921/**2b**)/ `…_843688`(924/2d)/ `…_e17d24`(926/**2b**),
   **不传 `--cand-ref`**;**每 run 独立子目录再指向父目录**。
   **⚠️ 先查 SIR**;**⚠️ 读 `per_seed` 时逐粒看 `ba_games`** —— W17-R 证明
   **「`per_seed` 非空」不等于「有可用种子」**(§3.2)。
   拿到 ≥1 粒新种子就与既有 4 粒合成,并**执行那条已挂了两轮的预登记**
   (两层都给、带 n 与半差 SE、看 ab 层是否稳住;满 8 粒时 SE ±77→±55)。
   **拿到 6–7 粒也照做,把 n 写进结论**,不要因为不满 8 粒就再挂一轮。
2. **6h 闸**:例行锚点 = **W18 首台 `18:20:00Z` ⇒ W19 解锁 `2026-08-28T00:20:00Z`**。先 `date -u` 再比。
3. **⭐⭐ 总监 —— GH #256(本轮唯一新立案)**:`--az` 回退把实例送回被点名 AZ。
   **它在立案的同一波里就已经发生了一次**(921)。
4. **⭐ 总监 —— GH #252 可结**:分散能力已落地且**首次实测验收通过**(3 个 AZ ≥ 2)。
   残留缺口已分立 #256;`GetSpotPlacementScores` 那条请求 (2) **未落地**,若不打算给,请显式驳回以便销号。
5. **⭐ 总监 —— GH #204(第十一轮)**:「池化到几粒才有判别力」(当前估计 **n ≳ 15**)
   **连续第二轮拿不到新证据,原因是回收不是统计**。请带 n 重裁。
6. **⭐ 总监 —— GH #233(a)** 仍未裁;**新数据点**:W17-R 实测 $0.313 vs 按需口径预估 $0.869 ⇒
   **回收波上「按需口径」偏离到 7 倍**(§5.5),比上一轮登记的偏离更大。
7. 总监 —— **GH #239 路 4 第四次实证**:SIR 仍是唯一能区分「回收 vs 自终」的证据源(§8)。保留窗口仍未知。
8. 总监 —— **#207(第十一轮)**:`zusstatic` **连续第十波空转**(W17/W17-R 归零,两波连语料都没有)。
9. 总监 —— **#218 后续**:`salveally` / `salveyield` **连续第六轮不在第 2 行**。
10. **录像组** —— `abilanc` (a) 取证语料:W17 的 128 局 + **W17-R 的 64 局**虽不计分但 `.dem` 仍在
    (12 槽 × 4 台 × 2 波),**单腿孤儿对逐帧取证一样可用**;W14 的 156 份 `.dem` **约 09-16 过期**,优先消化。
11. **replay-check 组 —— 报告节奏 6.1h 洞**(§1,与上一轮同一处同一数值,**连续第三轮**)。
12. **本台自留** —— §5.3 那条路径教训:接线门在 `tools/batch_test/`(**非 `soak/`**),
    且**不要把门的调用直接接 `| tail`**,否则 `$?` 读的是 `tail` 的退出码。
13. 存量催办:#217(第十轮)/ #211(第十一轮)/ #225 / #180(第十七轮)/ #171(第十五轮)/
    #200(第十四轮)/ #181 / 载体门 PARTIAL(**第二十一轮**,GH #140)/ `stable-v*` tag(第十九轮)/
    `campdanger`(第十四轮)/ §BL.4 机械化 / #75(**第十三轮,维持 12**)。

---

## 11. 本次会话 token 用量(铁律 8)

```
TOKENS total_in=2,747,545 out=26,568 turns=31
```

(统计时刻为写报告前;此后的收尾回合不计入。)
