# 批测台 2026-09-13T15:17Z — 持有第十轮;⭐⭐⭐ 头号:刹车会自己跳,不等 owner

**一句话**:刹车第十轮在力,闸 (iii) 连续第三轮自己拒绝(`FENCE_SPOT_EXIT=3`,差 `$0.23`);零发波、零收割欠、零泄漏;本轮新增计费 **`$0.03`**(三次 CE,其中**一次是本台浪费的**,§四如实登记)。

⭐⭐⭐ **头号(本轮第一条真正的新测量)**:上一轮交棒 2 说的那个判据条件**本轮成立了** —— `budget refreshed` 戳**真的变了**(`07:29:49Z` → `14:36:55Z`)。于是这一轮的比对有证明力,而它买到的不是「没长」,是:
**MTD 在零 EC2 活动的 30 小时里涨了 `+$0.2133`,headroom 从 `$1.083` 掉到 `$0.870`。**
⇒ **「等 owner 表态」不是成本中性的**:围栏在**不发任何波**的情况下也在单调收紧,
**`$90` 刹车会在 9 月 30 日之前自己跳掉**(下界 0.6 天 / 上界 7.5 天,§三),
⇒ **owner 的决策窗口不是「九月还剩 17 天」,是「几天」**;窗口关掉之后,连「提高天花板」这条路都要先解释一笔与批测无关的存量支出。

⭐⭐ **第二**:本月第一个零批测日(09-13)的逐日表里,**本台自己的成本计量是最大的一条支出** ——
`$0.0400` CE 调用 / `$0.0752` 当日已发布总额 = **53.1%**,且 **EC2 一行都没有**。
⇒ 本台本轮**当场自行止血**(操作动作,非 harness 改动,§五)。

---

## 一、成本(现跑)

- `AWS_SETUP_EXIT=0`(容器已有 `aws-cli/1.46.1`;身份 `arn:aws:iam::581436534924:user/dota2bot-agent`)。
- `COST_EXIT=0`;MTD **`$89.130`**;`≥$35` 自动 CE 复核 **`89.129790035`**。
- ⭐ **`budget refreshed 2026-09-13T14:36:55Z`** —— **与上一轮(`07:29:49Z`)不是同一张快照**,间隔 7.12h。
  ⇒ 这是本月**第一次**满足上一轮交棒 2 立的那个判据(「由**戳变了**认出来,⛔ 不由总额没变认出来」),
  故本轮的 MTD 比对**有**证明力,与前两轮**没有**证明力的同形比对**不是一回事**。
- 诚实 cutoff `14:36:55Z − 11.3h` = **`2026-09-13T03:18:55Z`**(闸 (iii) 自己打的那一行)。

**跨快照增量(本轮唯一有证明力的那条)**:

| 读数 | 上一轮 12:14Z(快照 `07:29:49Z`) | 本轮 15:17Z(快照 `14:36:55Z`) | 差 |
|---|---|---|---|
| MTD | `$88.916522` | `$89.129790` | **`+$0.213268`** |
| headroom(对 `$90` 刹车) | `$1.083` | **`$0.870`** | **`−$0.213`** |

⭐ **而这 30 小时里 EC2 一分没花**:最后一台机器是 W69(`2026-09-12T09:26:31Z` 起飞,2h 看门狗),
本轮 `describe-instances` 六态**零行**(§六)。⇒ **这笔增量按构造不是批测支出。**

## 二、闸(裸读,逐字)

**(i) `THROTTLE_EXIT=0`**:`anchor wave : W69 (4 machine(s))` / `last machine up : 2026-09-12T09:26:31Z  <- the anchor` / `slate spread : 7s` / `unlock : 2026-09-12T15:26:31Z` / `now : 2026-09-13T15:17:41Z` / **`margin : +85870s past unlock`**。
⚠️ **(i) 开着不构成许可**(连续第五轮点名):它是时间函数会自愈,(iii) 只随 MTD 单调收紧 —— 而 §一 现在给了这句话一个**量**:(iii) 每 7 小时收紧 `$0.21`。

**(iii) 裸跑 `FENCE_BARE_EXIT=0`**:`actual (MTD) : $89.130` / `pending waves : $0.000` / `planned : $0.000` / `projected total : $89.130` / `fence : $100.00` / `brake : $90.00` / `operative ceiling: $90.00 = min(fence, brake)` / **`headroom : $0.870 after this wave`**;`accrual scope : 17 region(s) read <- COMPLETE`;`accrual check : CERTIFIED (0 accruing instances account-wide, read this run) -- pending $0.000 is a reading, not a default.`;`crossing registry: 0 record(s) ... none in force`;`budget filters : none`。

**(iii) `--planned 1.10`(最便宜的一档)⇒ `FENCE_SPOT_EXIT=3`**,逐字:
`projected total : $90.230` / `A launch at this instant would put MTD past the $90.00 BRAKE line.` / **`WAVE_FENCE: THROTTLED (exit 3) -- brake, not fence. Stop and report to the owner; do not wait it out.`**
⇒ ⭐ **差 `$0.230`**;上一轮差 `$0.017`。**差额在扩大,不在缩小** —— 同 §一。

⛔ **未用** `--no-accrual-check` / `--no-crossing-file` / `--director-crossing` / `--snapshot-instant`;**未传** `--pending` ⇒ **无任何「这是 SKIP 不是 pass」行**。
**闸 (ii)/(iv) 未评估也不声称跑过**((iii) 已 exit 3,走不到发波决策)。
⇒ **本轮不发波是闸拒绝**,不是本台处置(与上一轮、上上轮同)。

## 三、⭐⭐⭐ 零批测日的逐日表:刹车会自己跳

`awsx ce get-cost-and-usage --granularity DAILY --group-by SERVICE`(`CE_DAILY_EXIT=0`,**裸读**),
十三天求和 **`89.12979`** 与 §一 的 MTD **逐位相同** ⇒ 语料同一性先钉死再读。

**09-13(本月第一个零批测日,已发布部分)**逐字:

```
2026-09-13  0.07536
    AWS Cost Explorer                        0.040000
    Amazon Route 53                          0.000209
    Amazon Simple Storage Service            0.035151
```

⭐ **三件事一起读出来**:
1. **EC2 一行都没有** —— 零批测日的形状是干净的,增量**不是**批测。
2. **`AWS Cost Explorer $0.04` = 当日已发布总额的 53.1%** ⇒ **本月第一个零批测日,最大的一条支出是本台自己查成本。**
3. 剩下的是**存量**(S3 存的历史录像 + Route53),它**每天都收**,与发不发波无关。

**还剩几天(两个端点都给,⛔ 不取中点冒充精度)**:
- **上界 7.5 天**:只数本台自己的 CE 调用。09-08~09-12 五天的 `AWS Cost Explorer` 行 = `0.14 / 0.14 / 0.13 / 0.08 / 0.09`,均值 **`$0.116/天`** ⇒ `0.870 / 0.116 ≈ 7.5 天` ⇒ 约 **09-21** 跳。
- **下界 0.6 天**:按本月**活动最低那天**(09-10,EC2-Compute 仅 `$0.599`)的存量口径 —— S3 `1.157` + EC2-Other `0.187` + Route53 `0.0005` + CE `0.13` = **`$1.475/天`** ⇒ `0.870 / 1.475 ≈ 0.6 天` ⇒ 约 **09-14** 跳。

⚠️ **诚实边界(为什么本轮只给区间不给点估计)**:09-13 那一行是**部分发布**的(cutoff `03:18:55Z`),
它的 `$0.0352` S3 既可能是「一天 `$0.25` 的 14%」也可能是「一天 `$1.16` 的 3%」,**本轮分不出**;
⛔ **不许**把 `0.0352 / 0.1375` 这种外推写成读数(那正是本章程反对的、`$3.05` cap-25 外推那一类)。
⭐ **钉死它的那一轮**:09-13 是**完整**零批测日,其逐日行在 `2026-09-14T02:00Z` 之后第一次完整可得
(= 上一轮交棒 3 指定的那个测点,**本轮未到点,未编**)。
⇒ **两个端点都落在 09-30 之前**,这一条**与区间宽度无关**,是本节唯一进结论的句子。

## 四、本轮花了 `$0.03`,其中 `$0.01` 是本台浪费的(如实登记)

三次 CE,逐条:
1. `check_costs.sh` 自动复核 `$0.01` —— **非自选**(`MTD ≥ $COST_CONFIRM_AT` 触发)。
2. `check_costs.sh --ce` `$0.01` —— ⛔ **浪费**。它回的是 `--granularity MONTHLY`(`89.129790035`),
   与第 1 次**逐位相同的同一个量**;本台当时要的是**逐日表**,而 `--ce` **不提供逐日**。
3. `awsx ce ... --granularity DAILY --group-by SERVICE` `$0.01` —— **这一次才买到 §三**。
⇒ ⭐ **教训(下轮照抄,免得再付一次)**:逐日/逐服务**直接调 `ce get-cost-and-usage --granularity DAILY`**,
⛔ **不要指望 `check_costs.sh --ce`** —— 它只是把同一个月度总额换个来源再打一遍。
⇒ 本轮 EC2 支出 **`$0.00`**;新增计费合计 **`$0.03`**。

## 五、⭐⭐ 本台当场自行止血(操作动作,⛔ 非 harness 改动)

**做法**:`COST_CONFIRM_AT=999 bash tools/batch_test/aws/check_costs.sh`。
**当场验过,`COST_FREE_EXIT=0`**,逐字 `89.13 USD  source: budgets/dota2bot-batch (free) | ... | budget refreshed 2026-09-13T14:36:55Z`
⇒ **同一个 MTD、同一张快照戳、`$0.00`**。

**为什么这不是抄近路(三条,缺一条就不该做)**:
1. **两者是同一个量**:本台 09-12T18:20Z 轮已证 `check_costs.sh:48-55` 的复核调的是
   `ce get-cost-and-usage --granularity MONTHLY`,与预算 `ActualSpend` **是同一个量**,且 **CE 并不领先那张快照**(GH #603)。
2. **复核的立法目的在本轮不成立**:那 `$0.01` 是为了「**在发波决策靠近围栏之前**确认读数」;
   而 (iii) 已 `exit 3`,**本轮根本没有可确认的发波决策** —— 花钱确认一个用不上的数字,买到的是零。
3. **恢复条件写死,⛔ 不是永久关掉**:**任何一轮只要 `headroom ≥ $1.10`(即发波真的成为可能),
   那一轮就照付 `$0.01` 复核**。⇒ 省的是「确认一个不能行动的数字」,不是「确认一个要行动的数字」。

⚠️ **这是本台自己的调用方式,⛔ 没有改任何脚本的默认值**(`CONFIRM_AT=${COST_CONFIRM_AT:-35}` 一字未动)。
**永久修法交 harness/总监**(§九交棒 ③):`check_costs.sh` 在「MTD 已越过刹车线」时自动复核,
**失效方向是危险的那一侧** —— 它在最该省钱的时候恰好每轮固定掏钱,且 12 轮/天 ⇒ `$0.116/天`(§三实测),
**占掉零批测日过半的支出**。

## 六、泄漏:四条独立路径全零(⛔ 不合并成一句)

1. `check_costs.sh` 的 `running/pending` 区块**空**;
2. `describe-instances`(**不加 tag 过滤**,五态 `pending,running,shutting-down,stopping,stopped`)⇒ **`[]`**,`INST_EXIT=0`;
3. `describe-volumes` ⇒ **`[]`**,`VOL_EXIT=0`(零孤儿 EBS);
4. 闸 (iii) 自带账户级普查 `accrual scope : 17 region(s) read <- COMPLETE` + `CERTIFIED (0 accruing instances account-wide, read this run)`。

常驻仍只有 AMI `ami-0a990a26d89c66547`。⛔ **#515/#528 立案理由一字未变**:`budget filters : none`。
⭐ 顺带:§三 的逐日表给了泄漏检查一条**账单侧**的独立佐证 —— 09-13 EC2 行**不存在**,
与路径 2 的 `[]` **两个独立来源同向**。

## 七、收割:零欠

`awsx s3 ls s3://dota2bot-batch-results-4924/validation/` **`S3VAL_EXIT=0`**(⛔ 未走管道读码),
**592 对象**,与上一轮基线**逐个相同** ⇒ 无 W69 之后的新数据;W68/W69 前几轮均已收割,**本轮未新增任何 verdict**。

## 八、queue / 铁律 9 优先项

`OWNER_PRIORITIES.md:99-101` **4.1 upstream 标尺波**仍挂「当前球:批测台」。
**本轮仍未认领,原因是钱不是排期** —— 它同样要过闸 (iii),而 (iii) 本轮 `exit 3`。
⭐ **owner 一旦说「发」,本台下一轮发的就是它**(逐字「排在任何 arm 波之前」),⛔ 不是普通 arm 波。
⚠️ **而 §三 给这一条加了一个以前没有的限定**:标尺波也要 `$1.10`,
**若刹车先自己跳掉,它在九月就彻底没有算术路径了** —— 不是「排队等」,是「窗口关了」。
`queue.json` 119 条,pending 项**全部堵在同一个闸上**,⛔ 本轮不逐条重列(与前九轮同形,无新测量)。

## 九、本会话未碰代码

⛔ **`bots` / `game` / `tests` / `tools` 一行未改** ⇒ **无一条 trunk red 由本轮引入**;
产物只有 `iterations/` 下的本报告与章程条目。`origin/main` 的红族属 GH #624/#705/#751/#774,⛔ 本台不改不立新案。

## 十、自查(铁律 10 / 11)

- ⚠️ **管道坑第 27 次,仍是当轮第一条命令**(`routine_selfcheck.sh 2>&1 | tail -60`),
  脚本自卫逐字 `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.`
  并自己点名 `it has recurred 5x, every time as the first command of the round`。
  ⚠️ **工具拦住了 ≠ 这轮没犯**(连续第 27 轮同一位置)。
- ⚠️ **超时坑同轮第二次,而上一轮交棒 4 逐字点的就是它**:第二跑给了 `timeout 900`,
  被自己砍断在 `=== trunk health (fast Lua detectors) ===` 那条腿上,**`EXIT=124`** ⇒ ⛔ **没跑完不是通过**。
  ⇒ **同一条交棒连续第三轮在同一轮里被违反了两次。**
  第三跑按交棒**后台 `nohup` + 无 `timeout`**重发,真码见「补记」。
- **本轮「每个读了退出码的命令是否走管道」自查**:`AWS_SETUP_EXIT` / `COST_EXIT` / `COST_FREE_EXIT` /
  `THROTTLE_EXIT` / `FENCE_BARE_EXIT` / `FENCE_SPOT_EXIT` / `CE_DAILY_EXIT` / `S3VAL_EXIT` /
  `INST_EXIT` / `VOL_EXIT` **十个退出码全部重定向裸读,零管道**。
- **铁律 11**:零 `requires approval`,零空转(自检后台跑,成本/闸/收割/泄漏并行推进)。

---

## 交棒

1. ⭐⭐⭐ **owner —— 刹车第十轮持有,而本轮变的是题目本身**:前九轮问的是「三条路选一条」,
   本轮量到的是**不选也会有结果,而且是最差那个** —— headroom `$0.870`,零批测的情况下
   **`$90` 在 09-30 之前自己跳**(区间 0.6–7.5 天,两端都在九月内,§三)。
   ⇒ 本轮已按 Routine 通知路径推到手机/邮箱(第二次);
   ⛔ **按上一轮交棒 1,本轮只在 #779 补一条「有新测量」的评论,不堆同形的第九条**。
2. ⭐⭐⭐ **下一轮本台 —— 零批测日测点,`2026-09-14T02:00Z` 之后第一次完整可得**:
   09-13 的**完整**逐日行给出「一波都不发时这条线一天涨多少」,**把 §三 的 0.6–7.5 天区间收成一个数**。
   ⚠️ **先读 `budget refreshed` 戳**(上一轮交棒 2,本轮第一次兑现):戳没变 ⇒ 比对零信息,登记「不可得」;
   ⛔ 没到点/戳没变**不要编**。
3. ⭐⭐ **harness/总监 —— `check_costs.sh` 在刹车之上仍每轮自动掏 `$0.01`**(§五)。
   实测:`$0.116/天`,**占本月第一个零批测日已发布支出的 53.1%**,
   失效方向是危险的那一侧(最该省钱时固定花钱)。建议:`CONFIRM_AT` 的触发条件加一条
   「**且 headroom 仍够发最便宜的一波**」。⛔ **本台不自行改脚本**,本轮只用环境变量自救并写死恢复条件。
4. ⭐ **下一轮本台 —— 省钱的调用方式**(§四):`COST_CONFIRM_AT=999` 起跑 `check_costs.sh`;
   **恢复条件**:任何一轮 `headroom ≥ $1.10` 就照付那 `$0.01`。
   逐日表**直接** `awsx ce get-cost-and-usage --granularity DAILY`,⛔ **不要用 `check_costs.sh --ce`**(它只给月度总额,本轮为此白花 `$0.01`)。
5. ⚠️ **开工第一条命令第 27 次踩管道坑、同轮第二次踩超时坑**,两条都是上一轮交棒点过名的。
   **下一轮 = 第 28 次**:自检**一律** `nohup … > 文件 2>&1 &` 起,正文**不给它任何 `timeout`**,收尾**从文件读 `EXIT=`**。
6. ⛔ **本条目的 `$89.130` / `headroom $0.870` / `budget refreshed 14:36:55Z` / S3 `592 对象` /
   闸 (i) 锚点 `2026-09-12T09:26:31Z` 全部作废**,围栏/闸/成本/收割/泄漏**一律当轮现跑**。
7. ⭐ **若 owner 说「发」**:下一波是 `OWNER_PRIORITIES.md` **4.1 标尺波**
   (`stable(当前 main,gate 全关) vs upstream 74727e4a`,不 arm 任何 candidate),排在任何 arm 波之前;
   ⚠️ **它同样要过闸 (iii)**,且按 §三 那扇门可能先自己关上。
