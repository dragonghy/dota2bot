# 批测台 2026-09-17T18:06:17Z

**刹车第四十三轮持有;零发波、零收割欠、零泄漏。**
**⭐⭐ 本轮实质:上一轮交下来的「整桶普查」把四十二轮的收割盲区量到了见底(恰好一波,已读),
而同一次普查顺手撞出一件更大的事 —— 胜负这个读数在镜像波上是饱和的:侧别决定 99.2% 的胜负,
铁律 2 条件 (b) 的 swap-average 估计量被恒等式钉在 0.50,对任何候选都成立。已发 GH #883。**

---

## 一、预算与闸(零发波)

`check_costs.sh` 实读:

- MTD **`$92.001`** / 戳 **`2026-09-17T13:24:58Z`** / forecast `118.091` / budget limit `100.0`
- ⚠️ **戳与上一轮(15:19:57Z 轮)逐位相同** ⇒ **本轮无新速率点**,既有带 `$0.40–0.92/天`
  原样结转,越线日与兜底日 `2026-09-27T00:00Z` 不动。⛔ 不据一个未动的戳宣布任何速率结论。
- headroom **`$-2.001`** < 刹车 `$90.00` ⇒ **闸 (iii) 不通过,零发波。**
- `alpha = 0` accruing instance(s) account-wide;`beta = 0` wave record(s) after `2026-09-17T02:06:58Z`(本轮实读)
- 无在跑/pending 实例;AMI 仍 `ami-0a990a26d89c66547` 一张。

**GH #801 四行整块照抄(SKIP 不是 pass):**

```
   >= $35, but headroom to the $90.00 brake is $-2.001
   < $1.10 (cheapest wave) — CE confirmation SKIPPED, NOT passed (GH #801).
   Nothing can launch at this MTD, so confirming it buys nothing.
   Resumes by itself as soon as headroom >= $1.10.
```

欠条 `gh801_confirm_headroom_first_live_read` 仍 **OWED**;⛔ 未用 `COST_CONFIRM_AT=999`。

## 二、收割:整桶普查(上一轮交棒 (二) 的结清)

上一轮发现「`validation/` 的 592 是一个前缀的数、不是桶的数」,交棒要求本轮起数整桶前缀。已做。

**整桶 `s3 ls --recursive` 实读 `79,889` 键,与上一轮逐位相同 ⇒ 全桶零新对象**
(比「`validation/` 592 未变」强:后者只覆盖一个前缀)。

```
  72079  soak        4748  dem21       1987  replays      592  validation
    306  behavioral     8  tools          6  calibration     1  rs
    161  run_20260905_151656_main_vs_upstream-baseline
      1  run_20260905_151230_main_vs_upstream-baseline
```

**⭐ 盲区见底:`aws_run.sh`(ref 式链路 B)在本桶历史上只留下 `2` 个根级前缀,两个都是 09-05、
两个都已被上一轮读过。** ⇒ 那个四十二轮没人看见的收割盲区,**内容恰好是一波,不是一批未知的波**;
**收割欠仍是零**,并且这次是**普查**出来的,不是抽样出来的。
⛔ 不再需要为「还有多少没收割的 aws_run 波」留悬念 —— 答案是零。

## 三、⭐⭐ 工作单元:胜负读数在镜像波上是饱和的(GH #883)

普查确认 bot 是活的之后,顺着查「09-05 那种空波会不会也发生在现役 soak 链路上」,
结果是**没有**(链路 A 完全正常),但撞出另一件事。

### 3.1 先证伪「链路 A 也空」

最新 soak 波(09-12 09:26)逐局实读:GPM **870–2799**、人头 **40–51**、等级 **19–30**、
`duration_s` 均值 **1558s**(22–28 分,Turbo 正常)⇒ **bot 是活的,#881 的空波是链路 B 专有的。**
另核对 stamp 结构健康:每台 **50 mirror-stamped + 6 warm-up**,warm-up 携带 git SHA `52ea2be4`
被 `recover_verdict.py` 正确排除 ⇒ **无静默丢局**。

### 3.2 然后撞出的事

两波互相独立的镜像波、6 粒种子、**247 计分局**:**dire 胜 245 = 99.2%**。

| 种子 | ab 层 (cand=radiant) | ba 层 (cand=dire) | `arm=(r_ab+r_ba)/2` |
|---|---|---|---|
| 13019 | 1/35 = 0.029 | 15/15 = 1.000 | **0.5143** |
| 13027 | 0/31 = 0.000 | 18/19 = 0.947 | **0.4737** |
| 13034 | 0/36 = 0.000 | 14/14 = 1.000 | **0.5000** |
| 13052 | 0/36 = 0.000 | 14/14 = 1.000 | **0.5000** |
| 12542 | 0/26 = 0.000 | 13/13 = 1.000 | **0.5000** |
| 12939 | 0/8 = 0.000 | 0/0 = 无 | 不可算(缺臂) |

两个分层照铁律 4 §CL (i-a) **全登记**;跨种子按 (i-d) **先每粒 swap-average 再算术平均**
(⛔ 未按局加权)= **0.4976**,SD ≈ **0.023**。

**为什么是缺陷不是「测出了中性」:**

1. **`arm ≈ 0.50` 是恒等式。** 侧别决定胜负的概率 ≈ 1 ⇒ 对**任何**候选 `r_ab→0`、`r_ba→1`
   ⇒ `arm→0.5`。灾难性候选与极好候选给出**同一个 0.50**。⇒ 铁律 2 (b) 被**自动、无条件**满足。
   **一条永远不会失败的闸不是闸。**
2. **⭐ 饱和把自己伪装成高精度。** 现行读法(§CL (i-c) + #269 深度门)以 arm 的跨种子离散度
   判精度;这里 SD ≈ 0.023,**紧得像一个测得极准的零** —— 而它紧正因为每粒都被钉在同一个恒等式上。
   ⇒ **现有精度判据在这个失效模式下朝危险那侧失效:把饱和读成可信。**
3. **⛔ 不是阵容强弱**(这是本轮花力气钉死的那一条)。波 A 四粒草稿**互不相交**、已逐一实读
   (13019 `axe/cm/lion/luna/storm` vs `jugg/wk/veno/warlock/zuus`;13034 `centaur/drow/lion/ogre/qop`
   vs `cm/dp/lina/sniper/sb` 等)⇒ **四份完全不同的草稿,dire 一律 ~100% 胜**。
   阵容强弱应让不同草稿偏向不同侧;它没有。
4. **⛔ 不是没打完/没打起来。** 224 局全 `winner_by=engine_natural` / `natural_end=True` /
   `cap_min=25.0`;经济近似平分 `econ_winner` **dire 119 / radiant 105**,`winner != econ_winner`
   占 **45.5%** ⇒ **经济是硬币,胜负不是**。种子 13027 上 radiant 平均净领先 **+30,600** 金仍 54/56 负。

⚠️ 与 `AGENTS.md`「Radiant 侧偏 ≈ +1.5k gold」**方向相反**:radiant 拿到钱,dire 拿到胜。

**诚实边界:** ⛔ 只测到事实,**未测到机制**,本台不裁;⛔ 只覆盖 09-12 两波 6 粒种子,
**未回溯历史全部波次**;⛔ **经济四量不受本条影响**(5 人均值、连续量,未饱和),本条只针对 winrate。

**交出去的下一棒(GH #883,⛔ 本台不定夺):** (1) 跨波普查(纯 `analysis.json` 读,零 EC2);
(2) winrate 饱和守卫(`max(side_share) ≥ 0.95` ⇒ 判 UNINFORMATIVE,不得用于满足 (b));
(3) 在 (b) 恢复可测前,promote 决策不引用 winrate 那一行。

## 四、局数(铁律/章程步骤 7)

- **(a) 上一波次最终有效局数**:本月无新波次。本轮所读 09-12 两波实测 ——
  波 A(`spot_20260912_0926*`,4 台/4 粒):**200 计分局 + 24 暖场**(每台 50+6,逐台一致);
  ab 层合计 138 局、ba 层合计 62 局(**ab/ba 严重不对称,dire 腿被截断** —— 与既有
  `thin_arm_seeds`/`arm_depth` 现象一致,本轮未新增诊断)。
  波 B(`spot_20260912_0326*`,抽 2 台):47 计分局(26+13 与 8+0;后者缺 ba 臂)。
- **(b) 本轮在跑波次进度**:**无**(零发波)。

## 五、成本(铁律 1 三段式)

**零 EC2 / 零 CE / S3 读取 `~330` 个对象(出网未计价)**

第三段明细:整桶 `s3 ls --recursive` **1 次**(79,889 键 ≈ 80 LIST,不下载);
`analysis.json` 全量小对象 GET ≈ **327**(波 A 4 前缀 × 56 = 224;波 B 2 前缀 ≈ 95;
verdict.json 1;run.log 1;另含首轮 8+8 抽样后被覆盖重下)≈ **2.1 MB**。
⭐ **单对象 ~6.5 KB,故本轮全程未触发上一轮那条「后缀 range 读」** —— 该方法仍对
`.dem`/大 log 有效,本轮不适用(小对象整取更便宜)。

**零泄漏**(收尾 `--leak-only` 实读 `LEAK_EXIT=0`,running/pending 区块空;
另 `alpha = 0` account-wide、`beta = 0`、AMI 一张、无 pending)。

## 六、没做的事

⛔ 未发波(headroom 负);⛔ `bots`/`game`/`tests`/`tools` **一行未改**
(想过直接给 winrate 加饱和守卫,**否掉**:判据与拦截点决定以后每一波的去留,
**裁定前造仪器是本仓库反复点名的顺序错误**,且 harness 不是本台归属 —— 与上一轮同一条理由);
⛔ 未回溯历史全部波次做跨波普查(已作为下一棒交出);⛔ 未删 09-05 两个前缀(仍是 #881 验收语料);
⛔ 未改 `OWNER_PRIORITIES.md`(owner 的文件);⛔ 未追评 #779(戳未动,零信息增量);
⛔ 未把本条并进 #881(**不同的失效**:#881 是 bot 没动,本条是 bot 正常而读数没信息;
合并会把两件事混成一件,已在 #883 交叉引用)。

## 七、开工自检

⭐ **本轮照抄了交棒模板:`nohup … > /tmp/sc.log 2>&1 &`**(⚠️ 管道坑第 35 次仍在当轮第一条命令上
被自卫挡住 —— 逐字 `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.`;
第二跑带 `timeout` 又被第二条自卫挡住,逐字 `REFUSED: … running under 'timeout'; exit 2, nothing checked.`
⇒ **两条自卫都生效了,第三跑才对**)。

写报告时自检仍停在 `=== trunk health (python test suite) ===` 腿上(`STILL RUNNING`)
⇒ ⛔ **本轮收尾时无真码,不空转等它,⛔ 不写 trunk 绿也不写 trunk 红。**
收尾前已到手的分腿实读:
`LUA GATE COVERAGE disk 507 | push gate 342 | 开工自检 leg 135 | known_red 6`,
`UNCOVERED 115 of 507 (23%) {'no_manifest_row': 53, 'timed_out': 60, 'too_slow': 2}`,
**`UNCOVERED SET GREW -- 3 file(s)`**(`test_campbind_poke_real_frame.lua` 第二轮、
`test_dusttower_dive_guard.lua` 第八轮、`test_fieldsip_transfer_receiving_site.lua` 第六轮),
同时 `NOW COVERED tests/test_fieldsip_atom_pricing.lua` **1 个**。
对照上一轮 `disk 506→507` / `push gate 341→342` / `UNCOVERED 115→115`:
⇒ **缝宽未再增(115 = 115),但三个老面孔仍无自动读者。**
`PY GATE COVERAGE disk 149 | push gate 130 | 开工自检 leg 149`,`UNCOVERED 0 of 149 (0%)`,
`out-of-gate rows with a written reader: 17 of 17`,`uncovered set unchanged -- OK`。

## 八、发表纪律

GH **#883** `[batch]` 已发。发表前按 GH #290 跑 `claim_precheck.sh` 实读
**`PRECHECK_EXIT=0`** / `local commits not on origin/main: 0` /
`NO CITATIONS -- nothing to certify, nothing to strand.  OK to publish.`
⛔ 未用任何 bypass。

## 九、手机通知

**推了一条。⚠️ 与刹车四判据的关系写清:四条本轮一条不成立**
(MTD `$92.001 < $100` / `alpha = 0` / headroom `$-2.001` / 兜底日 `09-27` 未到)
⇒ **不是按那四条推的**,⛔ 也不据此放宽它们(它们量的是刹车,这条不量刹车)。
推的理由是另一类事实:**铁律 2 的三条 promote 条件之一(b)在现有语料上恒真**,
而它是**全队每一次 promote 都要过的那道闸** —— 这件事影响的不是这一轮,是**已经做过的和将要做的每一次 promote 决策**。

## 十、Token 用量

```
TOKENS total_in=3,714,796 out=38,937 turns=36
```

(统计时刻之后的收尾回合不计入。)
