# 批测台 2026-09-02T03:30Z —— 发波轮:W37 = **52-id 家族第二波**,并且**把 GH #271 那道门从我们自己手里救了回来**

**一句话**:本轮最该被读的不是发波,是 **`reclaim_blind.py` 连续两轮的 `exit 2` 里,这一轮那次是我们自己写的** ——
W36 的收割轮把 `machines[].status_code` 写成了 EC2 `StateReason.Code` 词汇,而工具读的是 SIR `Status.Code` 词汇;
换回去之后**同一份输入、同一个工具**立刻给出 `RB_EXIT=0 / not blinded / NEXT WAVE: spot`。
**一个响亮的错答案和一个静默的错答案一样贵,只是它更像真的**:两轮的报告行长得一模一样
(「`RB_EXIT=2`,不是 pass,按预登记保守默认走 spot」),而上一轮那次是真的答不了(`BRACKET VIOLATED`),
这一轮那次不是。立案 **GH #412**。

---

## 0. 开工自检(铁律 10)

第一条命令**又误用了管道**,自检自己拒跑并打
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked`
(**管道守卫第 12 个外部验收点、同形状第八次** —— 每次都是当轮第一条命令)。
改走 `tools/agent/rc.sh` 重跑后拿到**真判决**:**`RC_EXIT=3`(有发现)**,
**连续两轮「无自检总判决」的记录到此中断**(前两轮分别是 600s 超时 / 管道)。

`exit sources`(GH #267 4b,工具自打,不手工归因):`legs run 8`,
`FINDINGS (exit 3) = cadence + trunk-red(python) + trunk-red(lua)`,`UNCERTIFIABLE (exit 2) = none`。

- **trunk RED(lua)**:`test_incoming_damage_callsite_census.lua` —— **GH #394 第五次复现,仍未修**
  (「published 43 是 grep 行数,现读 44」)。**不是本台的活**,点名交总监/英雄组。
- **trunk RED(python)**:`illumove_pairs:HP_CUT is classified (UNREGISTERED)` 1 FAILED。
  ⚠️ **这一条直接坐在本波的语料通道上** —— `illumove` 是本波 armed 的 52 个 id 之一
  (queue `strategy-29`),交总监/协同组。
- **两条 RED 的诚实边界照抄工具自己那句**:「Whether main is red too is NOT established by this line:
  re-run after `git stash`」—— **本轮没跑那一步,故不声称 main 也红**。

---

## 1. 成本(闸 (iii))

| 量 | 读数 |
|---|---|
| 九月 MTD | **`$3.637`**(`budgets describe-budgets`,**免费通道**) |
| 快照时刻 | `2026-09-01T17:58:37Z` —— **与上一轮同一张,本轮无新围栏信息** |
| forecast / limit | `150.867` / `100.0`(开月外推,**不构成刹车信号**) |
| CE 复核 | **未触发**(MTD `$3.637` < `$35` 的 `COST_CONFIRM_AT`)⇒ **那 `$0.01` 没花** |
| 在跑资源 | `describe-instances` pending/running **空**;常驻仅 AMI `ami-0a990a26d89c66547` |

**围栏算术**(章程 §2 甲,近 12h 内已发波次按各自界别取价):
`3.637 + W36 实测 ~$0.5(约 2.2 机时)+ W37 预估 spot 0.90` = **`$5.04`** ≤ **$80** ⇒ **闸 (iii) PASS**。
**最坏口径**(W37 全程降级按需 $2.15)= `$6.29`,**同样过**。
**不跨任何 owner 可见告警档($50 / $80 / $100)⇒ 本轮不欠跨档解释行**;刹车 $90、owner 线 $100 都不接近。
**本轮现金支出 = W37 一波(~$0.90 预估),CE `$0.01` 未花。**

---

## 2. 收割:本轮**没有收割对象**(闸 (iv) 输入已清)

`s3://dota2bot-batch-results-4924/soak/` 最新八个前缀全是 W35(2 个)与 W36(4 台 + 2 台补跑),
**W36 已由上一轮全量收割**(`RV_EXIT=0`,读数见 `20260902T004500Z.md`),`validation/` 无新 verdict。
`describe-instances` pending/running **空** ⇒ 无在跑波次。**⇒ 闸 (iv) 满足,本轮是纯发波轮。**

### 2.1 ⭐⭐⭐ 但本轮把上一轮的一笔**记录债**还了:`reclaim_blind.py` 的 exit 2 是我们自己造的

第一次调用(输入 = `W36_wave.json`,未改):

```
RB_EXIT=2
UNDECIDABLE: seed 2745: unknown SIR status_code 'Server.SpotInstanceTermination'
             (known: instance-terminated-by-user, instance-terminated-no-capacity)
```

`W36_wave.json` 自己的 `_note` 写着「The harvesting round **MUST** write `machines[].status_code` …
`reclaim_blind.py` reads them」,而那一轮写进去的是 `describe-instances` 的 **`StateReason.Code`**:

| seed | 写进 `status_code`(EC2 词汇) | 同一行已有的 `sir_status_code`(SIR 词汇) |
|---|---|---|
| 2745 | `Server.SpotInstanceTermination` | `instance-terminated-no-capacity` |
| 2838 | `Client.UserInitiatedShutdown` | `instance-terminated-by-user` |
| 2850 | `Client.UserInitiatedShutdown` | `instance-terminated-by-user` |
| 2922 | `Server.SpotInstanceTermination` | `instance-terminated-no-capacity` |

**W33 / W35 写的都是 SIR 词汇,W34 四台全 `null`** ⇒ 这条写入约定**没有守卫**,已经漂过两种形态。

**处置(零成本)**:`status_code` 从**该记录自己的第一手 `sir_status_code`** 回填
(与 GH #408 的表逐字一致),EC2 的值移到新键 `ec2_state_reason_code`,并写 `status_code_correction_note`。
⚠️ **诚实边界,写在文件里也写在这里**:这**不是新测量** —— 本轮 `describe-spot-instance-requests`
**返回空**(六个 SIR 已过期),正因如此更正只能取自记录、不能取自 AWS。

回填后**同一个工具、同一份输入**:

```
RB_EXIT=0
  seed 2745   1.6 min  instance-terminated-no-capacity  ab0 /ba0   NO-PAIR
  seed 2838  57.5 min  instance-terminated-by-user      ab42/ba16  PAIRED  [depth-checked]
  seed 2850  54.2 min  instance-terminated-by-user      ab31/ba12  PAIRED  [depth-checked]
  seed 2922   5.4 min  instance-terminated-no-capacity  ab0 /ba0   NO-PAIR
yield      : 2 paired seed(s) of 4 machine(s)
attribution: 2 machine(s) reclaimed before the flip
VERDICT: not blinded -- the wave delivered 2 paired seed(s).
NEXT WAVE: spot (the default; GH #158 unchanged).
```

**为什么这条值得单独立案(失效方向)**:工具**响了**(exit 2,不是静默通过),
但**它响出来的那句话把归因指向了外面**。上一轮的 exit 2 理由是 `BRACKET VIOLATED`(W35 的换挡常数
被 2986 的 37.2 min 配对证伪)——**那是真的答不了**;本轮的理由是 `unknown SIR status_code`
——**那是我们自己写坏的**;而**两轮在报告里长得一模一样**。于是一道**本来能给出裁定**的门
连续两轮被记成「市场未定」,而 **GH #408 正在拿「补跑机制在容量紧张下无效」向总监要一条裁定,
那条裁定的输入之一今天本来就读得出来**。⇒ **GH #412**(三条验收方式,本台不代裁;
明确**不推荐**「工具自动回落到 `sir_status_code`」,那会把写入约定的漂移变成静默可容忍)。

**结论与保守默认同向**(仍是 spot)⇒ W36 的市场选择事后看没选错 —— **但那是运气,不是这道门给的。**

### 2.2 ⭐⭐ 另一笔掉了两轮的债本轮**去还了,然后撞上一条永久拒答** —— §DA.2 的 baseline 重建**按设计跑不出来**

总监 §DA.2(GH #308)判 **(C) = `--rec-slots 8` + 重建 baseline**,写明「先把 W25 的 196 局并进
`rec_slot_baseline.json`,再用**原判据**跑验收门」。W36 那一轮**既没执行也没豁免**(理由是章程边界 (3):
不在同一波里同时改被测 id 集合与采集配置,而 W36 是 52→54)。**本波边界 (3) 不成立**
(armed 串与 W35 **逐字节相同**)⇒ 本轮去还这笔债。

语料先下齐(**零 EC2,只有几分之一美分的 S3 GET**):

| 语料 | run 数 | `.analysis.json` | `.demclaim.json` |
|---|---|---|---|
| 现行 baseline `spot_20260822_0610*` | 4 | **305** | 有 |
| W25 `spot_20260829_1217*` | 4 | **196** | 有 |
| 合计 | 8 | **501** | **165** |

`501 = 305 + 196`,与 §DA.2 写的「196 局」逐位对上。然后:

```
python3 tools/batch_test/soak/rec_slot_cost.py /tmp/recslot/*/ --emit-baseline /tmp/new_baseline.json
EMIT_EXIT=2
CANNOT CERTIFY: claim sidecars disagree about rec_slots: [1, 12]
  denominator: sidecar slots=[1..12]
```

**`exit 2` 是拒答不是通过**,而且**拒答发生在读完 sidecar 之后**(denominator 是工具自己打的)
⇒ **不是「文件没下全」那一类问题**。根因是契约:`rec_slot_baseline.json` 按定义是
**REC_SLOTS=1 profile**(文件自己写着 `rec_slots: 1, rec_set: [1]`),而 **W25 是 `--rec-slots 12`**
(`W25_wave.json:rec_slots=12`,sidecar 独立佐证 slots 1–12),`rec_slot_cost.py:180` 是
**五条永久 REFUSALS 之一**。**与 2026-08-22T22:06Z 那次 16-of-16 同族:设计不是语料。**

⭐ **而这恰恰是 §DA.3 自己诊断出的缺陷的另一面**:§DA.3 证明 slot 1 的 −7.4% 不是当前配置的代价,
是 **baseline 里 slot 1 太快**(`rec_set=[1]` 让它独占 SourceTV 带宽,**和其余 15 格不是同一种物理量**)。
要修它,slot 1 的 baseline **必须**来自一份「slot 1 不独占」的语料,也就是 W25 这种多录制者语料。
⇒ **裁定的目的正确,而唯一能实现它的动作被工具的 profile 契约挡住:
`--emit-baseline` 只生产它自己承认的那一种 baseline,而 §DA.3 证明的正是那一种有毛病。**

**本轮处置(保守默认,不自行改判据、不自行改 harness)**:`--rec-slots` **保持 8**
(裁词的第一半不依赖重建,W37 已按 8 发出);**不跑验收门** —— 没有合法的重建 baseline,
跑出来的任何数都是拿旧 baseline 冒充新 baseline,**是「事后改判据」的镜像,本台不生产这种读数**。
立案 **GH #413**(三条取舍,本台不代裁;明确**不推荐**「只对 slot 2+ 判」,那是把缺陷改成不看)。

---

## 3. 局数(owner 2026-08-19 要求)

**(a) 上一波(W36)最终有效局数**(取自上一轮 `RV_EXIT=0` 的收割,本轮未重算):
`2838 ab42/ba16`(`arm_depth 23.17`)、`2850 ab31/ba12`(`arm_depth 17.30`),
**2745 / 2922 各 ab0/ba0**(起飞 1.6 / 5.4 分钟即被 `no-capacity` 回收,补跑机同秒再被回收)⇒
**101 局计分 / 113 局载入 / 2 粒入账,#269 深度门零粒排除**。

**(b) 本轮在跑波次(W37)**:见 §5;**启动型报告写预期局数** ——
`4 粒 × 12 局 × 2 腿 ≈ 96 局计分`(**暖场局不算有效局**)。

---

## 4. 发波前四道准备(全部在闸 (i) 解锁**之前**跑完,零 EC2 支出)

| 门 | 读数 |
|---|---|
| `tree_verified` | `git ls-remote origin main` = 本地 `HEAD` = **`ada4717f3b99eaed25e80d746f07d66ca13ea174`**,逐字节相同(章程步骤 5) |
| `arm` | **52 ids / 459 字节 / md5 `c5af3b11deb9c024983c22afb90d9a7d`**(取自 `test_set.md:2`) |
| `carrier_terms.py --arm` | **`CARRIER_EXIT=0`** —— `10 hero-scoped / 42 generic / 0 unresolved => 6 term(s)`,`TERMS crystal_maiden,lion,obsidian_destroyer,skeleton_king,spirit_breaker,zuus`,与手写六项**同一集合**(GH #276 想要的收敛,§DH.2 修完后第二次成立) |
| `seed_draft.py --assert-carrier-from-arm` | **`CARRIER_GATE ids=10 seeds=4 exit=0`**,六项**全 `satisfied=2`** |
| `seed_roster_index.py --build` | **`EXIT=0`**(章程活规则:**选种之前**必须 build,否则把上一波刚烧掉的种子当成未用过) |
| 铁律 6 静态半 | `luacheck_gate.sh` ⇒ **`GATE_EXIT=0 CLEAN`**(冷启自己装 `lua-check` 后 `bots game: 0 warnings`);`arm_push_gate.sh` ⇒ `core.hooksPath=.githooks` 已上膛 |

⚠️ `check_armed_wiring.py` 的路径:章程/上一轮写的是 `tools/batch_test/soak/check_armed_wiring.py`,
**真实路径是 `tools/batch_test/check_armed_wiring.py`**(第一次调用 `EXIT=2 No such file`)。
**exit 2 不是通过** —— 见 §5.4 的实跑读数。

---

## 5. W37 发波

### 5.1 三道闸

| 闸 | 判定 |
|---|---|
| **(i) ≥6h** | **PASS,而且守卫又真的拦了一次**。W36 起飞 `2026-09-01T21:31:10Z` ⇒ 解锁 **`2026-09-02T03:31:10Z`**。本轮 `03:12:55Z` 触发,发波块第一次调用在 `03:17:18Z` 被自己的守卫按 **`exit 9`** 退回并打 `GATE-I NOT OPEN: now=03:17:18Z unlock=03:31:10Z`。**未取任何例外**;这 ~14 分钟花在零成本准备上(成本、收割核对、§2.1 的更正与立案、四道门、选种),**不是空转**。 |
| **(ii) 有新东西可测** | **PASS,但走的是第三支,不是第一支 —— 这一格必须写清楚。** |
| **(iii) 预算** | **PASS**,见 §1(`$5.04` / 最坏 `$6.29` ≤ `$80`)。 |
| **(iv) 输入已清** | **PASS**,见 §2。`reclaim_blind` 本轮 **`RB_EXIT=0`**(§2.1),**不再是 UNDECIDABLE**。 |

#### 5.1.1 ⭐⭐ 闸 (ii) 的读法:`bots/` 变了,**但这一波与 W35 是同一个配置**

`test_set.md:2` 的 52-id 串 md5 `c5af3b11…` 与 **W35 逐字节相同**(54 → 52 由 §DH 出集后回到原串)。
两步核法(章程步骤 5,`fetch --depth 1` 后再 `git log`,**认退出码不认空输出**):

```
git fetch --depth 1 origin e81ba0ae…(W35 树)  exit=0
git log e81ba0ae..HEAD -- bots/ game/         exit=0, 3 commits
   87bc6b33 [strategy] slotdust …
   0b468372 strategy: gated slotarb …
   da63008e [strategy] 落地 gated hpbool …
```

⛔ **朴素读法「`bots/` 变了 ⇒ 闸 (ii) 第一支通过」在这里是错的,而且错在危险的一侧**
(章程 08-29 那条先例):**这三个 commit 落地的 `hpbool` / `slotarb` / `slotdust` 三个 id
一个都不在 armed 串里** ⇒ 按门的定义它们**惰性**,本波被测配置与 W35 **行为上逐字节等价**。
把它当「新配置」的代价不是零:那样 **W35 的 2986 那一粒就不能并池**,4 粒新种子白买。

⇒ **闸 (ii) 实际走第三支「当前 tree+测试集的累计种子数 < 8」**:
52-id 配置目前累计 **1 粒**(W35 四台只有 2986 存活,45 局)≪ 8。
**这一支同时是买这一波的实质理由,也是并池的许可证**:W37 的 4 粒与 W35 的 2986 **互不相同**
⇒ 收割时 **可并池到 5 粒**(仍 < 8,家族还要再买)。

### 5.2 选种:窗口 `[2601,3000]` 的 **popcount-4 层已被上一波吃光**,本轮**恰好卡在最低可行层**

```
window [2601,3000]   20 banked-or-drawn / 380 unused
popcount supply: {3: 49, 2: 141, 1: 140, 0: 50}   distinct masks: 36
BEST slots = 12   mask-combos in that layer: 17
```

排除集 = 索引 banked ∪ **W35+W36 抽过的八粒**(`2658/2774/2963/2986` + `2745/2838/2850/2922`,
**抽过就是抽过**,即使 W35 那三粒产出零局、索引如实标成未用过)。

⚠️⚠️ **`popcount 4` 这一层从 `{4:3}` 变成了空** —— 上一波那四粒里有两粒是 popcount 4。
这正是章程 08-30 点名的那个**周期性机制**:「每一波都挑载体最富的四粒,于是每一波都在削掉窗口
popcount 最高的那一层」。**后果是本轮 `BEST slots = 12` 恰好等于下限**(6 term × 2 = 12 槽)
⇒ **只有完美划分才行,而且是最后一层完美划分**。
**GH #285 的右移触发条件(搜出 `0`)仍未到,故本轮不右移、不放松自律(第二十二轮未裁)**;
但**下一波在 `[2601,3000]` 内极可能直接搜出 0** —— 这不是预言,是上面那张 popcount 表的算术。
**预登记:下一轮若搜出 0,按章程处置右移到 `[3001,3400]`,不降 `≥2/term` 的自律。**

同层 17 个 mask 组合里加**零成本次序偏好**(pudge 载体,服务 queue `strategy-26` / `rotscope`)⇒ 选中
**2673 / 2735 / 2752 / 2874**,其中 **2673 与 2735 两粒带 pudge**(上一波只有 1 粒)。

| 粒 | 六项载体 |
|---|---|
| 2673 | crystal_maiden, spirit_breaker, zuus (+pudge) |
| 2735 | crystal_maiden, spirit_breaker, zuus (+pudge) |
| 2752 | lion, obsidian_destroyer, skeleton_king |
| 2874 | lion, obsidian_destroyer, skeleton_king |

**每一项恰好 2 ⇒ 六项全 `satisfied=2`,`CARRIER_GATE exit=0`。**
⚠️ **诚实边界**:「恰好 2」意味着**任何一台被回收,那一项就掉到 1** —— 而 W33/W35/W36 连续三波
分别丢了 3/3/4 台。**这一波的载体冗余是本台记录里最薄的一次**,归因是选种池而不是市场。

### 5.3 起飞

**四台全 `SPOT_RUN_EXIT=0`,而且这是本台记录里最好的一次放置。**

| seed | AZ 请求 / 实得 | instance | run_token | SIR | 容量拒绝 |
|---|---|---|---|---|---|
| 2673 | 2a / **2a** | `i-0c24d6881c00d9f61` | `84f4fc` | `sir-3kffjbbm` | **0** |
| 2735 | 2b / **2b** | `i-0f3c1ebca2ff89df0` | `a57e1e` | `sir-w4ifkkvn` | **0** |
| 2752 | 2c / **2c** | `i-05aafd271394099ad` | `83e4fa` | `sir-txsfj22m` | **0** |
| 2874 | 2d / **2d** | `i-0c5437a9fa7c44e73` | `26717d` | `sir-niqfg18m` | **0** |

- **市场按证据核对**(`run_id` 的 `spot_` 前缀是硬写字符串,**不是证据**):`InstanceLifecycle` **四台全 `spot`**、四台全 `running`、**四个 SIR 互不相同**。**容量降级阶梯一级都没下。**
- **AZ 分散 = 4/4 互不相同,请求即实得** ⇒ **GH #252(≥2)通过,GH #256 的严格验收(四个互不相同)也通过**。四份日志里**一行 `! re-aiming inside the ring` 都没有**,`!! AZ RING EXHAUSTED` 也没有,**`InsufficientInstanceCapacity` 零次**。
- ⚠️ **这是关于此刻 us-west-2 容量的读数,不是对 GH #408 的反驳**:连续三波被 `no-capacity` 打掉 3/3/4 台,**一次干净起飞不注销那条**。
- `run_id` 唯一性:`Tags[?Key==soak-run]` **四个值两两不同**,与 `machines[].run_id` **逐一对上**(独立于本会话 transcript 的证据源)。

### 5.3.1 ⚠️⚠️ 一条**本轮自己造成的**偏离,写在这里而不是留在日志里:`--ref` 没钉

本波**没有传 `--ref <sha>`**,于是 `spot_run.sh` 的 `REF` 取了默认字面量 `main`,
run_id 变成 `spot_<ts>_1_**main**_<token>` —— 而 W35/W36 的 run_id 里带的是 **40 位 SHA**。

**测量本身不受影响**:`git ls-remote origin main` 在**发波前**、`03:32:04Z`、`03:32:30Z` **三次**
都等于 `ada4717f3b99eaed25e80d746f07d66ca13ea174` ⇒ tip 在整个起飞窗口内没动过,
实例的 `--ref main` clone 解析到的就是核对过的那棵树。

**但归档的 run_id 从此不再记录「测的是哪棵树」** —— 审计链**静默地变弱了,没有任何东西举手**。
这与本台反复点名的那一族同形(失效不报警),只是这次的作者是本台自己。

**权威通道还在,并作为收割义务登记**:每局 `analysis.json` 的 `script_version`
在暖场局里就是**裸树 SHA**。⇒ **收割轮必须先读一局暖场的 `script_version` 并断言
它等于 `ada4717f…`,然后才并池。** **下一次发波必须传 `--ref <40 位 sha>`。**
(没有为此杀波重发:树已三次核对一致,重发的代价是 `$0.90` + 配额等待 ~2m40s,买不到任何测量上的改善。)

⭐ **同轮后续,把代价说完整**:本轮自己的报告+章程 commit `0a930645` 在 **~03:35Z** 推上 main ——
**在四台起飞之后,但可能在某台开机 clone 完成之前**。**这个歧义正是不钉 `--ref` 的代价。**
**它不影响测量**:`git diff ada4717f..0a930645 -- bots/ game/` **为空**(该 commit 只动 `iterations/`)⇒
两种解析测的是**逐字节相同的 bot 代码**。**收割判据现在就写死,不留到那时临时发挥**:
暖场 `script_version` 是 **`ada4717f…` 或 `0a930645…` 都可并池;任何第三个 SHA 不行**,
必须先对 `bots/ game/` 做 diff 才能并。

### 5.4 `check_armed_wiring.py`

`WIRING_EXIT=0` —— **`all 52 armed ids wired on HEAD`**。

⚠️ **但第一次调用是 `EXIT=2 No such file or directory`**:章程与上一轮报告写的路径是
`tools/batch_test/soak/check_armed_wiring.py`,**真实路径没有 `soak/` 这一段**
(`tools/batch_test/check_armed_wiring.py`)。**`exit 2` 不是通过** —— 这一格若照抄旧路径、
又不看退出码,读到的会是「门跑过了」。本轮按真实路径重跑才拿到 `exit 0`。

---

## 6. 泄漏检查

**CLEAN。** 发波轮的收尾按章程跑**两条**,不是一条:

1. **波次点名**(`describe-instances` pending/running 的 `soak-run` 标签):
   **恰好四个值**,与 `W37_wave.json:machines[].run_id` **一一对上,不少一个、不多一个**。
2. **回收普查**(`terminated,shutting-down`):**空** —— 截至 `03:32:30Z` 本波窗口内
   **没有任何 `Server.SpotInstanceTermination`**。

`check_costs.sh --leak-only` ⇒ `LEAK_EXIT=0`,列出的四台正是本波四台;
`describe-volumes` 四卷全 `in-use`(四台的根卷),**无孤儿卷**;常驻仅 AMI `ami-0a990a26d89c66547`。

⚠️ **这一步存在的理由照抄本台 09-01 自己立的那句**:`--leak-only` **只报「多出来的」,
「少一台」它永远不会举手**。terminated 实例在 API 里只留约 1 小时(GH #375)⇒
若本波在 `~04:3xZ` 之后掉台,**下一轮读不到**,结算通道只剩 `describe-spot-instance-requests`,
而按 **GH #412** 它的 `Status.Code` 才是该写进 `machines[].status_code` 的那个词汇。

---

## 7. 交棒

① ⭐⭐⭐ **总监 —— GH #412(本轮新立)**:`reclaim_blind.py` 连续两轮 `exit 2`,
**这一轮那次是我们自己的记录写坏的**;回填后 `RB_EXIT=0 / not blinded / NEXT WAVE: spot`。
**GH #408 要的那条裁定,输入之一今天已经读得出来了。**

② ⭐⭐⭐ **总监 —— GH #408 仍未裁**(创建于 00:23Z,零评论):补跑机制在容量紧张下无效,
**4 粒门槛在当前市场下事实上够不着**。本轮起飞干净(0 容量拒绝、4/4 AZ)**不注销它**。
本轮按预登记保守默认走 spot;而**这一轮不是「拒答不是判决」那条路** —— 是 `RB_EXIT=0` 自己说的 spot。

③ ⭐⭐⭐ **总监 —— GH #413(本轮新立)**:§DA.2 的 baseline 重建**按工具设计不可执行**
(`--emit-baseline` 对 rec_slots 混合语料 `1 vs 12` 永久拒答)。
**裁定的目的正确,唯一能实现它的动作被 profile 契约挡住。** 三条取舍已列,本台不代裁。

④ ⭐⭐ **总监 —— GH #285 第二十二轮未裁,而这一轮它离咬人只剩一波**:
`[2601,3000]` 的 popcount-4 层**已空**,`BEST slots = 12` **恰好等于下限**。
**下一波极可能直接搜出 0**(是算术不是预言)。本轮已预登记处置:右移到 `[3001,3400]`,**不放松 ≥2/term**。

⑤ ⭐⭐ **本台下一轮 —— 收割 W37 时两件必做**:
(a) 先读一局暖场的 `script_version`,**断言 = `ada4717f…`**(§5.3.1 的 `--ref` 偏离);
(b) 回填 `machines[].status_code` 用 **SIR 词汇**(GH #412),EC2 的值放 `ec2_state_reason_code`。
**下一次发波必须传 `--ref <40 位 sha>`。**

⑥ ⭐⭐ **总监/协同组 —— 自检 python 那条 RED 直接坐在本波语料通道上**:
`illumove_pairs:HP_CUT is classified (UNREGISTERED)`,而 `illumove` 是本波 armed 的 52 个之一
(queue `strategy-29`)。**诚实边界**:未做 `git stash` 重跑,**不声称 main 也红**。

⑦ ⭐ **总监/英雄组 —— GH #394 第五次复现未修**(`test_incoming_damage_callsite_census.lua`)。

⑧ ⭐ **总监 —— §DC.3 (甲) 的债(`illumove`×`illureal` 不正交)本轮找到家了**:
两条 id 都在本波 armed 串里,**W37 就是它下一次能被还的地方**(W36 那一族已解散)。
**登记为 W37 的收割义务。**

⑨ ⭐ **总监/协同组 —— `strategy-31`/`strategy-32`(`tormself`/`immguard`)本波不搭车**
(§DH.3 出集,**退回不是 reject**),`queue.json` 已写明。**它们现在不搭任何波** ——
按铁律 9 这就是接力棒会掉的形状,**在此显式交出**:需要总监给一条载体路径,否则这两条永远停在原地。

⑩ ⭐ 存量:**#388 的 W34 103 份 `.dem` 2026-09-22 到期(距今 20 天)** / #402 / #395 / #375 /
按需常数第六轮 / **#285 第二十二轮未裁** / #317 五条 / `strategy-5b` 第十三轮 / #398 / #399 /
#382 / #383 / #290 / #313 / #207 / #218 / #367 / #295 / #252 / #256 / #282 / #329 / #321 / #271。

**下一轮本台 = 收割 W37**(闸 (i) 于 **`2026-09-02T09:31:28Z`** 解锁;四台预计 `~04:2x-05:3xZ` 自毁)。

---

## 8. 本轮 token 用量

`TOKENS total_in=11,955,806 out=65,669 turns=81`

(铁律 8:这是**到统计时刻为止**的数字,统计后的收尾回合不计入。)
