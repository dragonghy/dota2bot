# 批测台 2026-09-11T03:13Z — W64 收割轮 + 两台按需补跑

**一句话**:W64 四台里**两台在起飞 11.2 分钟后被同一次容量事件带走**(同 AZ、**同一秒**),
收到 2 粒配对种子(gpm **−10.75**,comps_better gpm **1/2**);按 GH #408 的预先登记处置
**发两台按需补跑机买回那两粒**;⭐ 而本轮最贵的发现不是这些 ——
**闸 (i) 第一次跑就 `exit 2`,原因是 W64 自己的发波记录把机器时刻写在了闸读不到的键上**。

---

## 一、⭐⭐⭐ 头号发现:发波记录写了 `launch_time`,而闸 (i) 读的是 `launched_at`

本轮第一次跑 `wave_throttle.py`,逐字:

```
UNCERTIFIABLE: W64 machine 0 (seed 10890) has no launched_at
gate (i) DID NOT RUN. That is not a pass -- do not launch on it.
WAVE_THROTTLE: UNCERTIFIABLE (exit 2)
```

**对照**:W63 的 `machines[]` 每台**同时带** `launched_at` 与 `launch_time`(两者逐字相同);
W64 的**只带 `launch_time`**。于是:

- **闸 (i) —— 五道闸里唯一管「花钱节奏」的那道 —— 在 W64 落地之后结构上无法给出裁决**。
  它不会误放行(`exit 2` 不是通过,这是对的),但它会**把下一轮卡死在一个看不出原因的
  `UNCERTIFIABLE` 上**,而那一轮的人要先去读 schema 才知道少了什么。
- **上一轮的 py gate 抓到过同族的另一半**(`gate_iv_reclaim` 少了 `_blind`、
  「不欠收割」留在顶层没进 `gates`),当场红、当场改。**这一半没被抓到**,
  因为 `tests/test_wave_gate_keys.py` 检的是 `gates` 下面的键,
  **不检 `machines[]` 下面的键** —— 而闸 (i) 恰恰只读后者。
- ⇒ **这正是 GH #624 那条腿的立案形状,只是换了个字段**:
  红(这里是 `exit 2`)由**下一个开工的组**发现,几小时后,作者已经走了。
  本轮之所以当场发现,**是因为本轮恰好是同一个座位、而且恰好要用那道闸**。

**处置(改记录,不改门)**:把 `launched_at` 写进 W64 的 `machines[]`,
取值**逐字抄发波轮自己记的那个时刻**,**不取 AWS**。
⚠️ 这一句是有分量的:seed 10900 的 SIR `CreateTime` 是 `00:21:16Z`,
而发波记录写的是 `00:21:17Z` —— **取晚的那个**,因为闸 (i) 的锚点是末台上机时刻,
**晚 = 解锁更晚 = 波更少 = 保守侧**。⛔ 没有为了让门变绿去 `REQUIRED_GATES` 加别名。

修完重跑,闸 (i) 正常作答:

```
anchor wave      : W64 (4 machine(s))
last machine up  : 2026-09-11T00:21:17Z  <- the anchor (GH #469 ruling 1)
slate spread     : 22s
unlock           : 2026-09-11T06:21:17Z
now              : 2026-09-11T03:16:45Z
margin           : 11072s SHORT of unlock
A launch at this instant BREACHES gate (i) by 184m32s.
WAVE_THROTTLE: THROTTLED (exit 3) -- do not launch. Wait 11072s.
```

⇒ **本轮不发例行波次**。已交 **GH #738 `[harness]`**。

---

## 二、AWS 引导 + 成本

- `session_setup.sh` `AWS_SETUP_EXIT=0` ⇒ `AWS ready: arn:aws:iam::581436534924:user/dota2bot-agent`
  (容器无 AWS CLI,脚本自己装的 `aws-cli/1.46.1`)。
- `check_costs.sh` `COSTS_EXIT=0`:**MTD `$78.253`**,`budget limit 100.0`,
  `forecast 182.287`,`budget refreshed 2026-09-10T19:46:38Z`。
  读数 ≥ `$35` ⇒ 按章程自动花 **$0.01** 用 CE 复核,得 `78.2534452025`,**吻合**。
- ⚠️ **MTD 与上一轮(00:15Z)逐位相同 `$78.253`,预算快照时刻也一字未动
  (`2026-09-10T19:46:38Z`,已滞后 ~7.5h)** ⇒ W63 与 W64 两波**都还没进 MTD**。
  这正是闸 (iii) 本轮点名两波的原因(§四)。
- **常驻成本**:仍只有 AMI `ami-0a990a26d89c66547`,无其他。

---

## 三、收割:W64(标准路径全量重算)

`recover_verdict.py` 走标准路径,**四个 run 各自下载到自己的子目录**
(`/tmp/pool/{53db33,8d88d3,db97c7,452004}`),⛔ 未 `cp` 进同一目录(GH #225)。
**逐 run 落盘数与 S3 侧逐个相等:63 / 0 / 0 / 48 = 111**,零 `[Errno 36]`。
臂串当轮现算:**37 id / 335 字节 / md5 `b525d51d4b4957e0e40f22f203aea641`**,与发波时逐位相同。
`RECOVER_EXIT=0`,`unparseable 0`,`pooled_basename_dirs_overridden 0`,
**`min_arm_depth 8`**,**`thin_arm_seeds []`**。

| 种子 | ab/ba | `arm_depth` | gpm (ab / ba) | xpm | deaths | last_hits | 计分 |
|---|---|---|---|---|---|---|---|
| 10890 | 40/17 | 23.86 | **+12.15** (−68.18 / +92.49) | +10.23 | +0.17(更差) | −1.59 | 57 |
| **10891** | **0/0** | — | — | — | — | — | **零局,见下** |
| **10892** | **0/0** | — | — | — | — | — | **零局,见下** |
| 10900 | 28/14 | 18.67 | **−33.64** (−299.17 / +231.90) | −22.82 | +0.54(更差) | +0.10 | 42 |

⚠️ **被排除的种子必须点名,而 10891/10892 连「被排除」都不是**:它们**根本没进 `per_seed`** ——
不是 `THIN-ARM`,也不是 W63 那种 `NO-PAIR`(ab26/ba0),而是**一局都没产**。
两台在起飞 **11.2 分钟**后被容量回收,**早于第一局跑完**。
**它们花的钱是真的**(2 × 0.187 机时),买到的是零。本轮已发补跑机买回(§五)。

跨种子(**先每粒 swap-average、再跨种子算术平均**;⛔ 未按局数加权 —— 铁律 4 (i-d);
四量由工具自己打,未手算):

| 量 | mean | comps_better | ab 层 | ba 层 | side | sign_flip |
|---|---|---|---|---|---|---|
| gpm | **−10.75** | 1/2 | −183.68 | +162.19 | −172.94 | true |
| xpm | −6.29 | 1/2 | −105.23 | +92.64 | −98.93 | true |
| deaths | +0.36(更差) | 0/2 | +1.68 | −0.97 | +1.33 | true |
| last_hits | −0.74 | 1/2 | −8.77 | +7.29 | −8.03 | true |

**铁律 4 (i-c) 照条文读**:四量的 `side_gt_arm` 都是 `2/2`、四个 sign_flip 都 true ——
这是**侧偏已消除的估计量**,**反号不是否决理由,是恒等式**(|side| > |arm|),
**登记但不当诊断**。`suggested: hold_or_reject`。

⚠️ **两粒种子的波,定量主张不写**。按 §CO.4:这一波对三条件的 (b) 能说的只有
「gpm −10.75 / deaths +0.36,两粒里一粒更好」—— **这不是一个能承重的粗粒度读数**,
条件 (a) 的瓶颈一字未变。**不建议任何 promote/reject。**

### 3.1 `winrate` 两条必抄的读数(GH #352)

- **`winrate_channel: DEGENERATE`**,**`mean.winrate_headroom: 0.0`**。逐字 stderr:
  `GH #352 winrate channel DEGENERATE: minority side won 0 of 111 finished games
  (share 0.0000 < 0.20). The winrate below is bounded to 0.5 +/- 0.0 and MUST NOT be
  cited as rule 2(b) support until the channel recovers.`
- ⇒ **`winrate 0.5` 与 `comps_better.winrate 0/2` 本波是占位符不是读数**,不写进任何结论。
- ⭐ **`winrate_independent_of_gold: "111/111 games"`** —— GH #108/#352 那个桶名 bug
  (W31 打 `0/222` 而真值 `222/222`)**在活路径上确认已修**。
  本台 08-29T09:13Z 立的逐波复发登记**可以正式停了**。

### 3.2 ⭐ 结清 `gh696_winrate_measurable_first_live_read`(欠在本轮)

逐字:

```
GH #696 winrate comps_better: 2 of 2 seed(s) had headroom 0 and could not vote yes
(seeds 10890,10900). The pooled fraction 0/2 counts them; `comps_better.winrate_measurable`
0/0 does not. A promote bar above 0/2 is unreachable on this corpus.
```

**这条提示第一次在活路径上打出来,而且它打的正是它要防的那件事**:
`comps_better.winrate` 的 `0/2` 分母里**两粒都是不可能投赞成票的**,
`winrate_measurable` 的 `0/0` 才是诚实的那个分数。

### 3.3 ⭐ 结清 `overchase_new_body_first_reading`

`overchase` 在本波 37-id 臂串内 ⇒ **W64 是新函数体的第一份波次读数**
(`test_set.md §FZ.5` / `state.json:overchase_KEPT_20260908`)。
**登记为「已读」,读数就是上面的波级读数**;⛔ **本波无法把它归因到单个 id**
(37 个 id 一条臂),这一行是登记不是归因。

### 3.4 ⚠️ 一处诚实边界:`games_loaded 111` vs `scored_games 99`

差 **12 局 = 6 局/台 × 2 台** —— 与上一轮(15:14Z)立案的「`recover_verdict.py` 每波静默丢
6 局/台」**逐台同量**,本轮第二次复现。**本轮不重开 issue**(已立案),只登记复现。

---

## 四、闸 (iii) 围栏 —— 本轮两波要覆盖,不是一波

第一次跑(未传 `--pending`)按 RULING 6 `exit 2`,**而且点名了两波**:

```
wave accrual     : records after 2026-09-10T08:28:38Z (= 2026-09-10T19:46:38Z - 11.3h lag, clock from budget snapshot)
  un-accrued?    : W64 last machine up 2026-09-11T00:21:17Z  <- inside the lag window
  un-accrued?    : W63 last machine up 2026-09-10T12:18:45Z  <- inside the lag window
  must cover     : 2 wave(s) listed above (W64, W63). This tool does not price them; check your sum covers each.
```

⭐ **`clock from budget snapshot`** —— GH #692 的 epoch 解析修法**继续成立**,
本轮**未**用 `--snapshot-instant`,时钟没有降级。
⭐ **`accrual scope : 17 region(s) read ... <- COMPLETE: every region this account has enabled`**
(RULING 5 的多区域腿,`COMPLETE` 不是 `SCOPE-LIMITED`)。

⚠️ **上一轮 `must cover` 只点 W63 一波,本轮点两波** —— 因为**预算快照 7.5 小时没动**,
cutoff 还停在 `08:28:38Z`,于是 W63 **没有滑出窗口**,W64 又滑了进来。
**这正是 RULING 6 (3) 那一行要防的形状**:`$3.250` 漏掉 W60 就是漏了一波。
⇒ `--pending 2.20`(W64 `$1.10` + W63 `$1.10`,均取 GH #454 的覆盖档常数)。

`--planned 1.25`:两台按需补跑机。章程的按需常数给**半波** `$2.15/2 = $1.075`,
机时路线给 `0.93 h/台 × 2 × $0.673 = $1.252`。⭐ **取大的那个申报**,失效方向朝安全侧;
⛔ 两条路线**各自独立登记,没有相乘去互相校对**(那正是章程 (丙) 否掉的「极大值之积」)。

```
actual (MTD)     : $78.253   <- re-read this run, never cached
pending waves    : $2.200   (launched, may not be in MTD yet)
planned          : $1.250
projected total  : $81.703
  ACTUAL alert   : $50.00   crossed         AWS state=ALARM
  ACTUAL alert   : $80.00   NOT yet crossed AWS state=OK
  ACTUAL alert   : $100.00  NOT yet crossed AWS state=OK
fence            : $80.00   <- lowest ACTUAL alert not yet crossed (Ruling 1)
brake            : $90.00   (owner's line, not derived here)
operative ceiling: $80.00   = min(fence, brake)
DIRECTOR CROSSING: ceiling $85.00 by ruling GH#721/director-20260910, expires 2026-09-30T23:59:00+00:00
headroom         : $3.297 after this wave
WAVE_FENCE: CLEAR (exit 0) -- gate (iii) passes ONLY BECAUSE OF RULING GH#721/director-20260910.
The derived fence $80.00 was NOT met; the ceiling that was met is $85.00, and it is a ruling with
an expiry, not a reading. Do not copy either number forward; re-run the tool.
```

`crossing registry: ruling GH#721/director-20260910 IS IN FORCE (read from
/home/user/dota2bot/iterations/director_rulings.json, not from a flag). It is applied below.`
—— 本台**一个 flag 都没传**(RULING 11 的注册表供的)。
**`projected` 不跨任何新的 ACTUAL 告警档**(`$80.00` 仍 `NOT yet crossed`;
`projected` 是投影不是 ActualSpend)⇒ **owner 本轮不会因此收到新告警邮件**。

---

## 五、发了什么:两台按需补跑机(不是波次)

**授权链,三条各自独立**:

1. **GH #408 的判据是 SIR 码,不是「市场看起来紧张」的印象** ——
   两台被回收机的 `Status.Code` **都是 `instance-terminated-no-capacity`**
   ⇒ 补跑机**直接 `--on-demand`**,不走 (1)(2)(3) 阶梯。
   **围栏两条都在界内**:每粒种子只补跑一次(两粒都是第一次)、
   每波最多 2 台按需补跑(**本轮恰好 2 台 = 顶到上限**;
   ⇒ 若本波再多一台被回收,那一粒**留给下一波,不许连发**)。
2. **GH #498:补跑机不受闸 (i) 的 6h 窗口约束。**
   ⚠️ **仍在动作时点跑了闸 (i) 并如实登记**:`THROTTLE_AT_LAUNCH_EXIT=3`
   —— **记录,不是遵守**;豁免条款才是它合法的理由,不是「闸没跑」。
3. **闸 (iii) 照常适用**,在发波脚本内部、紧挨 `spot_run.sh` 之前重跑
   (GH #469 绑动作不绑决定):`FENCE_AT_LAUNCH_EXIT=0`。

⭐⭐ **树钉,这一条差点被漏掉**:`origin/main` 已经比 W64 的钉
`34ea0edd` **前进 2 个 commit,而且动的是 `bots/`**
(`hero_arc_warden.lua` +27−?、`mode_team_roam_generic.lua` +84−?,共 `+100−11`)。
实例 launch 时 clone 的是 ref ⇒ **不显式钉住,补跑机测的就是另一棵树,却挂在 W64 的名下**。
故显式传 `--ref 34ea0edd750b23f9450f0f4bbb351065cb2ab6b4`,
并在脚本第一条语句核验该 sha **可解析且是 `origin/main` 的祖先**(不是则 `exit 9`)。
`PIN_OK 34ea0edd750b23f9450f0f4bbb351065cb2ab6b4 (ancestor of origin/main ae8b00d5)`。

**配置与 W64 逐字节同**:`--slots 16 --rec-slots 8 --hours 2 --games 12`
(`--rec-slots 8` 显式传,W51 就是吃默认被杀的那波)。
两次 `--dry-run` 先行,`DRYRUN_10891_EXIT=0` / `DRYRUN_10892_EXIT=0`,
**`spot_run.sh` 的波次预算闸没打 `REFUSED`** ⇒ 未传也不需要 `--allow-short-watchdog`。
AZ 每次调用显式点一个、两次不同(GH #252 的发波纪律);
⛔ **这不是「补跑打散 AZ」那条被 GH #408 否掉的缓解手段**,只是点名纪律。

| 种子 | instance | run_id | AZ | `InstanceLifecycle` | 上机 |
|---|---|---|---|---|---|
| 10891 | `i-0bcc97464e10626a7` | `spot_20260911_031953_1_34ea0edd…_6f1d01` | us-west-2a | **`null` = 按需** | 03:19:56Z |
| 10892 | `i-0409f3a0062f31507` | `spot_20260911_031956_1_34ea0edd…_82390f` | us-west-2d | **`null` = 按需** | 03:19:58Z |

⚠️ **市场证据是 `InstanceLifecycle` 实读 `null`,不是 run_id 的 `spot_` 前缀**
(那是 `spot_run.sh` 硬写的字符串,与市场类型无关)。
按 GH #408 第二半,两行都已写 `"market": "on-demand"`;`status_code` 留空待收割时
读 **EC2 `StateReason.Code`**(按需机没有 SIR)。

---

## 六、机时、成本、GH #454 两条腿(**不触发也报**)

SIR 全部可读(`state=closed`),⇒ ⭐ **本波存活是一个数,不是 GH #699 的区间** ——
`harvest_notes (b)` 要的正是这个。

| 种子 | SIR `Status.Code` | Create → Update | 机时 |
|---|---|---|---|
| 10890 | `instance-terminated-by-user` | 00:20:55 → 01:17:34 | **0.9442 h** |
| 10891 | `instance-terminated-no-capacity` | 00:21:12 → **00:32:26** | 0.1872 h |
| 10892 | `instance-terminated-no-capacity` | 00:21:14 → **00:32:26** | 0.1867 h |
| 10900 | `instance-terminated-by-user` | 00:21:16 → 01:12:38 | **0.8561 h** |

- **(乙1) 不触发**。总机时 **2.1742 h**。即便取本账号观测带的最高费率
  (W46 那次紧张隐含 ~`$0.275/h`),2.1742 h 也只到 **`$0.598`**(含 IPv4 `$0.005/h` 行);
  常态带 `$0.231–0.250/h` ⇒ **`$0.513–0.554`**。远低于 `$1.10`。
  ⚠️ **诚实边界:这是费率带的上界估计,不是已结算的账单侧读数**(当日 CE 未结算)。
  **本波机时只有常态的 ~58%**(2.17 h vs 四台 × ~0.93 h ≈ 3.7 h),
  **因为两台只活了 11 分钟** —— 也就是说 (乙1) 这次**不可能**触发,
  ⭐ **而这正是 GH #454 (乙) 立案的那句话**:抢占把实测成本压回线下,
  **抢占越频繁,按钱写的判据越不会被触发**。本波是它的又一个实例。
- **(乙2) 不触发**。**自终止**机器(`instance-terminated-by-user`)的机时均值
  = (0.9442 + 0.8561)/2 = **0.9001 h/台** ≤ `1.00`。
  ⛔ **两台被回收机不进这个均值**(规则自己的措辞);若强行并进去得 `0.5436`,
  **那正是 (乙2) 被写出来要绕开的那个向下偏置**。

**本轮支出**:两台按需补跑(申报 `$1.25`)+ CE `$0.01`。

---

## 七、⚠️ 两台在**同一秒**、**同一个 AZ** 被带走 —— 给 GH #403 的一条读数

10891 与 10892 的 SIR `Status.UpdateTime` **都是 `00:32:26Z`**,两台**都在 us-west-2c**
(10891 是发波时从 2b 重瞄过去的)。
⇒ **同 AZ 的两台,被同一次容量事件、在同一秒带走。**

- 这是 GH #403(种子间对容量事件是否独立)**方向明确的一条正面证据**:
  **同 AZ 不独立**。
- ⛔ 但它**不给 #403 收案**,理由和本台 W59 自己的发现是同一条:
  「4/4 AZ 分散」**不是**种子间独立性的证据,反过来「同 AZ 同秒」也只说明
  **同 AZ 内相关**,**没有**说明跨 AZ 独立。两个命题不是一回事。
- ⭐ **可操作的一点**:W64 发波时 2b 报 `InsufficientInstanceCapacity` 被重瞄到 2c,
  于是**四台跑在 3 个互异 AZ(2c 两台)**,而**恰好就是那挤在一起的两台一起死了**。
  ⇒ **重瞄(re-aim)把 AZ 分散悄悄削掉了一级,而削掉的那一级正好是后来出事的那一级。**
  这一条登记在此,交总监决定要不要变成一条发波期的硬约束
  (例如「重瞄后若与已用 AZ 重合则继续走环」)。**本台不自行改 harness。**

---

## 八、泄漏检查

- 发波**前**:`check_costs.sh` 的在跑清单为空;`wave_fence.py` accrual 腿
  `CERTIFIED ZERO (0 accruing instances account-wide)`,`accrual scope : 17 region(s) read ... COMPLETE`。
- 发波**后**:同一条腿**全账号 17 region** 读到**恰好 2 台**,**全是本轮有意启动的**:
  ```
  accruing       : i-0bcc97464e10626a7 c6i.4xlarge [running] us-west-2 launched 2026-09-11T03:19:56.000Z project=(untagged)
  accruing       : i-0409f3a0062f31507 c6i.4xlarge [running] us-west-2 launched 2026-09-11T03:19:58.000Z project=(untagged)
  ```
  **无第三台**;常驻仍只有 AMI `ami-0a990a26d89c66547`。
- ⛔ **GH #515/#528 的立案理由一字未变**:仍是
  `budget filters : none <- so every instance in the account does land in this budget, whoever owns it`,
  两台也照旧 `project=(untagged)`。

---

## 九、队列 / §4a

`queue.json` **40 条 pending**。**无一条要求本轮发波**:
唯一带 `APPROVED_CONDITIONAL` 的 `strategy-5b`(`midtp,suptp,midsupyield`)
`director.wave = W6` —— **排期早已过去 62 波**,且那三个 id **不在当前 37-id 臂串内**;
其余 `APPROVED*` 均为 `ROUTED_ARCHIVE_SCAN`(不请求 EC2)。
⇒ **§4a 无适用对象**;§4b 例行波次被闸 (i) 挡住(§一)。

---

## 十、闸 (iv) 与 (ii)

- **闸 (iv)** `RECLAIM_EXIT=0`,发波前亲手跑在 W64 自己的记录上:
  `yield : 2 paired seed(s) of 4 machine(s)` / **`attribution: 2 machine(s) reclaimed before the flip`**
  (⭐ **是一个数,不是 W63 那种 `0..1` 区间** —— 因为本轮按 `harvest_notes (b)`
  把 SIR `CreateTime`/`UpdateTime` 写回了 `machines[]`)/
  `VERDICT: not blinded -- the wave delivered 2 paired seed(s).` / `NEXT WAVE: spot`。
- **闸 (ii)** ⚠️ **家族深度现在 9 粒配对种子**(W62 4 + W63 3 + W64 2)⇒ **≥ 8,第二肢不再成立**。
  ⇒ **W65 必须靠第一肢**(树漂移**且与臂串交集非空**)才够格。
  上一轮已经记过:新落的 5 个 id 与 37-id 臂串**交集为空**;
  本轮新增的两个 commit 动的是 `hero_arc_warden.lua` / `mode_team_roam_generic.lua`,
  **同样需要下一轮现查交集,不得继承本结论**。

---

## 十一、铁律 6 读数(三条腿)+ 推送

`ARM_HOOK_EXIT=0`(`push gate armed (core.hooksPath=.githooks, already set)`)。
本轮共三次推送(收割+补跑 / test_set.md 登记 / 自检读数),每次 `push` 钩子三条腿**逐次都跑**,
三行读数每次相同:

```
luacheck bots game: 0 warnings
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 95 ran, 0 findings, 0 uncertifiable, ~35s
lua gate: SKIPPED BY SCOPE -- this push touches no bots/game/tests path.
  This is a SCOPE decision, not a pass and not a skip of a gate that had something to say (GH #624).
```

⚠️ **`SKIPPED BY SCOPE` 不是 pass**:本会话确实没动那三棵树,
**但 trunk 上那 `4 of 87` 条 lua 红仍在**(§十三)。
**未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行。**
动态半(~100min,GH #124)**未跑,不声称**。

`PUSH1_EXIT=0` / `PUSH2_EXIT=0`(`ae8b00d5..5ffdf0e9 HEAD -> main`)、
`PUSH3_EXIT=0` / `PUSH4_EXIT=0`(`5ffdf0e9..be2bb0a8`)、
`PUSH5_EXIT=0` / `PUSH6_EXIT=0`(`be2bb0a8..4b74d0db`)——**三次均一次通过,无一次被拒**。

⭐ **GH #290 的顺序照走了**:开 GH #738 之前先 push;
§GR 落地后把草稿存文件跑 `claim_precheck.sh`,
第一次 **`PRECHECK_EXIT=3`**(`MISSING section test_set.md §GR` / `DO NOT PUBLISH YET`),
push 之后重跑 **`PRECHECK2_EXIT=0`** 逐字
`OK to publish: every citation resolves on origin/main.`

## 十二、铁律 11

本轮**未触发任何 `requires approval`**,**没有任何等待**。

## 十三、自检(铁律 10)

⚠️ **管道坑第 14 次**(连续十四轮同一位置、同为当轮第一条命令)——**又是工具拦的不是纪律拦的**:
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.`
改重定向 + 后台重跑。首次前台尝试 `timeout 400` **被超时切掉**(`SELFCHECK_EXIT=124`),
第二次改后台无短 `timeout`,**本轮跑完并取到真退出码**。

**`SELFCHECK_EXIT=3`**,逐字:

```
legs run              : 11
FINDINGS (exit 3)     : cadence owed-executions a-evidence-owed trunk-red(python) trunk-red(lua)
UNCERTIFIABLE (exit 2): none
```

⭐ **与上一轮的差别是一条 finding 被清掉了**:上一轮是
`cadence queue-rulings owed-executions a-evidence-owed trunk-red(python) trunk-red(lua)`,
本轮 **`queue-rulings` 不在里面了**(`un-ruled queue requests` 两腿都打 `none`,
`total open requests: 92`)—— 总监 §GQ 那一轮把四行的 `director` 字段连 `status` 一起写了。
**登记归属:不是本台做的。**

`UNCERTIFIABLE (exit 2): none` ⇒ **十一条腿这一轮都真的跑了**,没有哪一侧是没人看过的。

**其余读数**:`6 anchor(s) checked -- OK`;`FROZEN none` + `promote-atom constraints: OK`;
`cadence` 两个洞都在 **director**(`09-10T01:15Z→05:30Z` 4.2h、`09-10T12:50Z→16:22Z` 3.5h),
**不是本台的**;`FINDING: 5 armed id(s) with neither a verdict nor an owed row`(条件 (a) 腿,
本台不判)。

**trunk 两条红**:python `122 passed, 2 failed, 1 uncertifiable`;
lua **`4 of 87`**(`test_focus_mana_cost_consumer_census.lua`
`expected n=26 … got n=27` / `the control must sweep the same 173 frames, saw 176`、
`test_lion_ult_reserve_domain.lua` `the tree now holds 7 rank-2 Finger instants … not 2`)。
⭐ **两条都是「登记表形状」族**(GH #650/#705/#718/#585 同族:**语料多一份 fixture 就把写死计数的
断言顶红**,不是数据变了);⛔ 本会话 `bots`/`game`/`tests` **一行未改** ⇒ **无一条红由本轮引入**;
**未做 `git stash` 复跑,故不宣布「这是 main 的红」**(工具自己那句
`Whether main is red too is NOT established by this line` 逐字适用);
#718 已收进一张单子 ⇒ **不开重复 issue**。

---

## 十四、本会话改了什么

`bots/` / `game/` / `tests/` / `tools/` **一行未改** —— §一 的 harness 缺陷**本台不自己改**
(章程:批测台不改 harness,先例 GH #33),已交 **GH #738**。
改的只有:`iterations/reports/batch-desk/waves/W64_wave.json`(收割回填 + 补跑登记 +
`launched_at` 补键)、新增 `W64_verdict.json` / `W64_verdict.stderr.txt`、本报告、章程「当前状态」。

---

## 十五、交棒

1. ⭐⭐⭐ **下一轮本台 = 收割两台补跑机**(03:19:5xZ 起飞 + 2h 看门狗 ⇒ 约 `05:30Z` 后齐)。
   四条必做:① 按需机**没有 SIR**,码要读 EC2 **`StateReason.Code`** 写进 `rerun[].status_code`;
   ② 存活分钟 + **该台的增量账单**(GH #408 的三样验收);
   ③ `ab`/`ba`/`arm_depth` 回填 `rerun[]` **和** verdict;
   ④ ⭐ **合池时把两台补跑机的 run 目录各给一个子目录**,与 W64 原四目录一起喂
   `recover_verdict.py`(⛔ 别 `cp` 平)—— 合池后 W64 应达 **4 粒配对种子**。
2. ⭐⭐ **harness / 总监 —— §一 的 `launched_at` 缺键**(**GH #738**):
   `test_wave_gate_keys.py` 只检 `gates` 的键、不检 `machines[]` 的键,
   而闸 (i) 只读后者。**建议验收**:该 ratchet 增一条「每台 `machines[]` 必须带
   可解析的 `launched_at`」,并对既有波次文件回扫一遍。
3. ⭐⭐ **总监 —— §七 的重瞄削 AZ 分散**:重瞄后与已用 AZ 重合要不要继续走环。
4. ⭐⭐ **总监 / owner —— 九月余量 `$3.297`**,`$85` 之下最多约 **2 波 spot**;
   GH #721 另两条路(#528 那台机器去留、#515 给预算加 cost filter)**仍 open**。
   ⚠️ 该 `$85` 天花板 **`expires 2026-09-30T23:59:00+00:00`**。
5. ⭐ **总监 —— 管道坑第 14 次**,工具拦得住纪律拦不住;**这已不是提醒能解决的形状**。
6. ⭐ **闸 (ii) 第二肢已失效**(§十)—— W65 要发,得先证明树漂移与臂串有交集。

**下一轮本台**:① **收割补跑机**;② 闸 (i) 锚点仍是 W64 末台 `2026-09-11T00:21:17Z`
⇒ 解锁 `06:21:17Z`(**读工具打的那一行,不要抄这个数**);
③ ⚠️ **开工第一条命令重定向 + 后台 + 不设短 `timeout`**(第 15 次);
④ 围栏 / 闸 / 成本**一律当轮现跑**,本条所有数字作废。

详见 `iterations/reports/batch-desk/waves/W64_wave.json`
与 `iterations/reports/batch-desk/waves/W64_verdict.json`。

---

## 十六、本次会话 token 用量(铁律 8)

```
TOKENS total_in=5,685,243 out=47,001 turns=48
```

(统计时刻之后的收尾回合不计入。)
