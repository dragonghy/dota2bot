# 批测台 2026-09-04T21:15Z —— 收割 W47

**一句话**:**W47 有一半被 AWS 抢回去了。** 四台里两台死于
`instance-terminated-no-capacity`(21.6 min / 35.7 min),**只有 2 粒种子计分**;
这一波是为「把 62-id 家族做到 8 粒」而发的,**它交回来 6 粒**。
本轮零发波(闸 (i) 差 195 分钟),零新增 AWS 花费。

---

## 1. 自检(铁律 10)

⚠️ **开工第一条命令又误用管道** —— `routine_selfcheck.sh | tail` 被脚本**自己拒绝**
(`REFUSED: stdout is a pipe; exit 2, nothing checked`),并打出「这已经是第 5 次、
每次都是本轮第一条命令」。**那个 exit 2 不是通过**,重定向重跑。

**重跑读到的:`TRUNK RED — a python test is failing ON THE WORKING TREE.`**
(自检启动于本轮任何编辑之前 ⇒ 那时工作树 = `origin/main`,**这是 trunk 的红,不是本轮的红**;
本轮改动全在 `iterations/`,与这两个测试的域不相交。)

`PY_LOG` 裸读 `90 passed, 2 failed, 2 uncertifiable`,两条红:

| 测试 | 状态 | 备注 |
|---|---|---|
| `tests/test_detector_source_constants.py` | **红,第二轮** | `J.ShouldTpSupportTowerFight -> /GetUnitToUnitDistance\(\s*bot,\s*building\s*\)\s*>\s*(?P<n>[\d.]+)/: expected exactly 1 match, found 0` —— **上一轮已立 GH #490**,仍红 |
| `tests/test_bots_walk_farm_only.py` | **红,本轮新发现** | `tests/_mockscalar_sweep.lua:86` 的 walk 走进 `bots/Customize/` 而未排除 farm-only 开关 |

⛔ **第二条上一轮报告里没有** —— 上一轮自检的 python 腿 `EXIT=124` 超时,
明确写了「trunk 的 python 那一侧本轮没人看过」。**那一侧没人看的时候,这条红就在那里。**
两条都不是本台的代码,交棒见 §7。

**自检最终跑完(后台,~25 min):`EXIT=3`,`legs run 10`,`UNCERTIFIABLE none`。**
⇒ **十条腿本轮全部有人看过**(与上一轮 `EXIT=124` 超时、python 那一侧无人看过相反)。
`FINDINGS (exit 3): cadence owed-executions trunk-red(python)` —— **照工具自己的告诫,
不手工把三项都归到 cadence 上**(GH #267:那句手造归因曾错了 22 小时),逐项裸读:

- **`trunk-red(python)`** = 上表两条,**本台交棒 §7.3**。
- **`cadence`**:`GAP cadence hero` + `GAP cadence strategy`(24h 窗口 / >3.5h 阈值)——
  **不是批测台的洞**。按工具 LIMITS,洞也可能是那轮本来就没东西交,**报出来的是问题不是判决**。
- **`owed-executions`**:`OWED campgrade_wave_type_premise_reading`(GH #480/#241,
  executor **含批测台**:「排 `campgrade` 独占波的那一轮」)——
  ⚠️ **`campgrade` 独占波仍不存在,W47 不是它**,这一棒**本台仍欠着**,与上一轮读数一致;
  另一条 `fixture_mock_extrapolated_location`(GH #492)executor 是总监,不归本台。
- `unlanded_commits` **无 OFF-TRUNK**;`ORPHAN_PROPOSAL` **none**;
  `trunk health (fast Lua detectors)` **80 个检测器文件 0 失败**
  (⚠️ 工具自陈这是 **FAST SUBSET,不是全套**);
  腿内 **NOT RUN**:`tests/test_rc_wrapper.py`、`tests/test_selfcheck_lua_leg.py`
  —— **这两条本轮没人看过,不声称绿。**

## 2. 成本(闸 (iii))

九月 MTD **`$14.981`**(免费 `budgets`,`refreshed 2026-09-04T11:29:50Z`)。
⚠️ **连续第四轮读到逐位相同的同一张快照**,它比 W46 起飞还早 1h ⇒ **按构造既不含 W46 也不含 W47**。

围栏算术(近 12h 已发波次按各自界别计价,**W47 本轮已结出账单侧实测值,直接用实测**):

```
14.981 + W44 2.15 + W45 0.90 + W45补跑 1.10 + W46 1.02 + W47 0.7413 = $20.89
```

vs `$45 围栏 / $80 / $90 刹车 / $100 批准线` ⇒ **全部满足,未触发任何刹车**。
`check_costs.sh` `COSTS_EXIT=0`,`running/pending` **为空**,常驻成本仍只有 AMI
`ami-0a990a26d89c66547`。

### 2.1 ⭐ GH #454 结账:$0.90 这个常数**本波没触发失效条款,而它是为了错误的理由才没触发的**

逐台(SIR `CreateTime` → `Status.UpdateTime`,× 该 AZ 当时 spot 价):

| 种子 | AZ | 机时 | $/h | 账单侧 | 终态 |
|---|---|---|---|---|---|
| 4723 | us-west-2a | 0.3597 | 0.2959 | $0.1064 | **抢占** |
| 4678 | us-west-2b | 0.7433 | 0.2741 | $0.2037 | 正常自毁 |
| 4763 | us-west-2c | 0.5947 | 0.2963 | $0.1762 | **抢占** |
| 4950 | us-west-2b | 0.9300 | 0.2741 | $0.2549 | 正常自毁 |
| **合计** | | **2.6277** | | **$0.7413** | |

`$0.7413 < $0.90` ⇒ **自动失效条款本波不触发**。**⛔ 但不要把这读成「常数没问题」**:

- 本波实付价带 **$0.274–0.296/h**;`$0.90` 这个常数是在 **$0.231–0.250/h** 的价带上取的
  ⇒ **价带顶部已陈旧约 18%**,正是 GH #454 已经点名的那条根因,**这是它的第二个独立测量点**。
- 本波便宜**只因为两台被提前打死**。反事实:四台都跑到 4950 的 0.93h,
  `4 × 0.93 × ~0.285 = ~$1.06` ⇒ **常数照样会破**。
- **一个只有在波被抢占时才待在界内的常数,不在界内。**
- 每计分粒成本反而**变贵**:`$0.7413/2 = $0.371/粒` vs W46 的 `$1.02/4 = $0.255/粒`(**+45%**)。

## 3. 收割 W47(`RV_EXIT=0`,裸读重定向后的 `$?`)

**前缀**:`s3://…/soak/<run_id>/`(**不是 `validation/`**,义务 5)。
**语料**:162 个 `*.analysis.json`(16/48/36/62),
`files_seen 162 / games_loaded 162 / unparseable 0 / pooled_basename_dirs_overridden 0`。
**没用 `--allow-pooled-basenames`、没用 `--allow-unparseable`、没用 `--min-arm-depth`
⇒ 本轮没有任何「这是 SKIP 不是 pass」的行要抄。**
热身局**每台恰好 6 局**(与 W46 同),162 − 24 = 138 带戳,**计分 128**。

**臂串核对(本轮亲手从发波树重算,不抄散文)**:
`git show 15b0d9fd…:iterations/streams/test_set.md` 第 2 行 ⇒
**62 id / 550 字节 / md5 `c7e1f92c739f8f8dc0ee0c9484288628`** —— 与登记值、与 W46 **逐字节相同**。

### 3.1 ⭐⭐ 头号产出:一半的波被抢占

| 种子 | 腿 | `arm_depth` | 判定 | 死因 / 时刻 |
|---|---|---|---|---|
| 4723 | ab10 / ba0 | 0.00 | **NO-PAIR,不计分** | `instance-terminated-no-capacity` 18:53:23Z(**21.6 min**) |
| 4763 | ab29 / ba1 | 1.93 | **THIN-ARM,被 GH #269 深度门排除** | `instance-terminated-no-capacity` 19:07:34Z(**35.7 min**) |
| 4678 | ab28 / ba14 | 18.67 | PAIRED,计分 | `instance-terminated-by-user` 19:16:26Z |
| 4950 | ab33 / ba23 | 27.11 | PAIRED,计分 | `instance-terminated-by-user` 19:28:15Z |

**逐台腿数两条独立路径互证、逐位一致**(义务 1):
(1) `recover_verdict.py` 的 `per_seed ab_games/ba_games`;
(2) 直接从该前缀落盘文件的 `script_version` 戳 `<臂串>:s<种子>:<radiant|dire>` 重导
(每前缀恰好一粒种子、每局恰好一条腿、每台 6 局无戳热身)。**未留 null、未填并池值。**

**义务 6(被深度门排除的种子必须点名,它的局是花过钱的)**:
`min_arm_depth 8`,`thin_arm_seeds [{4763, ab29, ba1, depth 1.93}]` ——
**4763 的 30 个计分局付了钱、不进均值**;4723 的 10 局同理。

**按 `OWNER_PRIORITIES` 回收处置**:一台被抢占 = 该粒缺臂,**该粒配对差作废,整波不作废**。
两粒补跑是**下一轮**的活(本轮闸 (i) 未开,见 §5),**交棒已显式写下,见 §7**。

### 3.2 四量(n = **2 粒计分种子**;每粒先 swap-average,再跨种子取算术平均;⛔ 永不按局加权)

| | gpm | xpm | deaths | last_hits |
|---|---|---|---|---|
| 均值 | **−5.95** | **−8.68** | **+0.25** | **−2.15** |
| `comps_better` | **1/2** | 1/2 | 1/2 | 1/2 |

逐粒 gpm:`4678 −24.54` / `4950 +12.63`(**两粒反号**,极差 37.2)。
⚠️ **分母是 2 不是 4** —— 这一波的判定力比 W46 的 n=4 **更弱**,不是更强。
`suggested: hold_or_reject` —— **只是提示**;三条件判定归总监,且这是**家族级**读数,**永远不是逐 id 读数**。

### 3.3 两个分层的**读数**(铁律 4(i-a);取自工具自打的 `strata`,**未手算**)

| | ab | ba | side | `sign_flip` | `side_gt_arm` |
|---|---|---|---|---|---|
| gpm | −45.27 | +33.37 | −39.32 | true | 2/2 |
| xpm | −21.18 | +3.83 | −12.50 | true | 2/2 |
| deaths | +0.96 | −0.46 | +0.71 | true | 2/2 |
| last_hits | −18.55 | +14.27 | −16.41 | true | 2/2 |

**四量全部反号。按铁律 4(i-c) 这不是否决理由也不是诊断** —— 在 swap-average 过的估计量上
反号 ⟺ `|side| > |arm|`,那是**恒等式**:它只说这一波抽到的阵容侧偏大于效应,
**对 arm 测得多准零信息**。**登记是因为 (i-a) 要求登记,不是因为它裁了什么。**

### 3.4 胜负通道:**第六次 `DEGENERATE`,预登记的分支落地**

少数侧 **16 / 162 = 0.0988**(W46 是 0.1217,**更差了**)。
⇒ 按 W47 记录里**数据落地之前**就写好的预登记:
**`winrate`(0.489)与 `comps_better` 的 winrate 行(0/2)一律不写成读数**,
**不得**用作铁律 2(b) 的支持(GH #352 / `test_set.md` §CT)。

**逐粒侧别普查(预登记要求打出来)**:
`s4678 radiant 0 / dire 42`、`s4950 radiant 1 / dire 55`、`s4763(已排除)radiant 10 / dire 20`。
⇒ **两粒计分种子合起来 98 局里 radiant 只赢 1 局。**
如预登记所述,**第六次 DEGENERATE 是预期而非信息**(W41 同时换了 id 家族、种子窗口和树,
横扫反而恶化一个数量级)⇒ **原结论不动:要修的是横扫本身,不是估计量。**

## 4. 闸 (iv) 自核(`RB_EXIT=0`)

```
VERDICT: not blinded -- the wave delivered 2 paired seed(s).
yield      : 2 paired seed(s) of 4 machine(s)
attribution: 2 machine(s) reclaimed before the flip
NEXT WAVE: spot (the default; GH #158 unchanged).
```

**W46 欠的那条义务(`arm_depth` 缺失 ⇒ 深度门路径全程没被走到)本轮结清**:
四行里 **3 行**打 `[depth-checked]`,而且它**干了真活** —— 4763 的 `ab29/ba1`
正是被这条路径从 PAIRED 降级成 NO-PAIR 的。

### 4.1 ⭐ 顺带捡到一个标签缺陷(读源码之后才敢说)

第 4 行(种子 4723)仍打 `[ab/ba only (no arm_depth given)]`,**而 `arm_depth` 是给了的(0.0)**。
裸读源码 `tools/batch_test/soak/reclaim_blind.py:343`:

```python
if paired and machine.get("arm_depth") is not None:
```

`is not None` 这一半**是对的**(0.0 不会被真值判断吃掉);跳过是因为该机 **unpaired**,
深度对未配对的机器本来就无意义 ⇒ **分类是对的,只有打印出来的理由是假的**。
后果不是读数错,是**审计错**:拿日志核对记录的人会去补一个**已经填好的**字段。
**已开 issue,低优先级。**(登记这条的理由与本台一贯登记的同族:**一个填好的值被报成缺失**。)

## 5. 本轮不发波:闸 (i) 差 195 分钟

**读的是工具那一行,不是散文里手打的数**(`WT_EXIT=3`):

```
anchor wave : W47   last machine up: 2026-09-04T18:32:27Z <- the anchor (GH #469 ruling 1)
unlock      : 2026-09-05T00:32:27Z      now: 2026-09-04T21:17:02Z
margin      : 11725s SHORT -- A launch at this instant BREACHES gate (i) by 195m25s.
WAVE_THROTTLE: THROTTLED (exit 3) -- do not launch.
```

⇒ **卡在 (i),不是卡在钱、也不是卡在没东西测。不发波、不空转等待**(铁律 7/11)。

⚠️ **一个本轮不必回答、但下一轮就会咬人的问题,先摆到台面上**:
§3.1 的两粒**补跑**算不算受闸 (i) 管的「例行波」?本轮不用答(工具对任何发波都 THROTTLED),
但**下一轮 00:32Z 解锁后第一件事就是它**。交棒给总监裁,见 §7。

## 6. 泄漏:干净

- `pending,running` **返回空** ⇒ 四台全部已自毁,**没有该关没关的**;本会话未启动任何实例。
- `describe-volumes`:**无游离卷**。
- 四个 SIR 全 `closed`(两 `instance-terminated-by-user` / 两 `instance-terminated-no-capacity`)。
- ⚠️ 停机时刻与状态码**全部取自 SIR**,因为 EC2 记录已过 ~1h 窗口过期(GH #375)——
  这是「SIR 比 EC2 记录活得久」**本轮又被正着用了一次**。
- 常驻成本仍只有 AMI。

## 7. 队列与交棒

**`queue.json` 回填 `QUEUE_ROWS_CHANGED=11`**,diff **33 插入 / 24 删除**
(= 11 行 × 3 字段插入;24 = 11 `status` + 11 `rides_wave` + 2 已有的 `last_wave` ——
**逐位对得上,没有把真实改动埋进重排**;写回前已核对该文件是 `indent=2` 并 round-trip 验证)。
11 行 `running` → **`harvested_pending_verification`**,`last_wave` = **W47**,
`rides_wave` 改写为 W47 全文并**当行写明本波只交回 2/4 粒**。

⛔ **回填时撞到的事实要登记:这 11 行在本轮之前已经落后两波** —— 它们还写着 `W45`,
而 W46 与 W47 **都**载过这些 id。两个发波轮各自记的是「队列本轮零翻动」,
**中间没有任何东西举手**。(与铁律 9 掉棒同族。)
`strategy-31/32`(`tormself`/`immguard`)**第十三轮不搭波**(按构造不在臂串里),未改动。

**交棒**:

1. ⭐⭐⭐ **总监 —— 补跑裁定(新)**:W47 缺 2 粒(4723/4763),按 `OWNER_PRIORITIES`
   回收处置该补跑。**请裁补跑是否受闸 (i) 管**。00:32Z 解锁后这是本台第一件活,
   **没有裁定就按保守默认走**(= 当作受闸 (i) 管,解锁后才发)。
2. ⭐⭐⭐ **总监 —— GH #454 常数重裁仍欠,本轮给出第二个独立测量点**:
   实付价带 **$0.274–0.296/h** vs 常数所依据的 **$0.231–0.250/h**;
   **本波未触发失效条款只因为被抢占了两台**,反事实 ~$1.06 照样破界(§2.1)。
3. ⭐⭐⭐ **协同组/英雄组 —— trunk 两条红**:`test_detector_source_constants.py`
   (**GH #490,第二轮仍红**)+ **`test_bots_walk_farm_only.py`(本轮新发现,
   上一轮 python 腿超时期间它就已经红着)**。
4. ⭐⭐ **总监 —— spot 容量本身**:W47 发波时 `us-west-2a` 与 `us-west-2d`
   **零容量**(GH #252/#256/#460 已记),跑起来之后 **`2a` 与 `2c` 又各被抢占一台**。
   ⇒ **同一个区域在同一小时内,两次以不同方式表现出容量不足**。
   这已经不是单点噪声,是本台每波交付率的系统性风险(本波 **50%**)。
5. ⭐ **本台自己 —— `reclaim_blind.py` 标签缺陷**(§4.1),已开 issue。

**下一轮本台 = ①(00:32Z 后)补跑 4723/4763 两粒,spot 优先;② 若总监已裁则照裁定走。**
补跑落地后 62-id 家族才到 8 粒,`GH #363/#449` 的并池分叉(**第六次出现,仍未裁**)才有意义。

## 8. 铁律 6 / 11

- `bots/` / `game/` **本会话一行未改**(改动全在 `iterations/`)。
- 静态门:`bash tools/agent/luacheck_gate.sh` ⇒ **`GATE_EXIT=0 CLEAN`**,
  `luacheck bots game: 0 warnings`(容器缺 luacheck,门**自己装上**再跑:`apt` 包名 `lua-check`)。
  **未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行要抄。**
- 动态半(`tests/run_tests.lua`,~100 min,GH #124)**未跑,不声称**。
- `tests/test_wave_gate_keys.py` 对改后的 W47 记录**当场跑过**:`WGK_EXIT=0`,**241 checks 0 failed**
  (上一轮总监被请裁的那条「写记录的那一轮必须自己跑一遍闸」,**在收割轮是能执行的**)。
- **铁律 11**:本轮未触发任何 `requires approval`,**无空转等待**。

## 9. token 用量

`TOKENS total_in=5,171,572 out=39,297 turns=50`(统计时刻后的收尾回合不计入)。

## 10. 本轮开出的 issue 编号(push 之后才发,#290 顺序)

| # | 标题 | 收件 |
|---|---|---|
| **#497** | `[bug]` `tests/test_bots_walk_farm_only.py` 红在 trunk —— 上一轮它藏在一个超时的腿后面 | 协同组/英雄组 |
| **#498** | `[batch]` W47 一半被抢占:请裁「补跑受不受闸 (i) 管」+ us-west-2 spot 容量已是系统性交付风险 | **总监** |
| **#499** | `[harness]` `reclaim_blind.py` 对 unpaired 机器打「no arm_depth given」,而 `arm_depth` 是给了的(0.0) | 总监/本台 |
| **#454 追评** | W47 结账:第二个独立测量点,常数重裁仍欠 | **总监** |

⭐ **顺序合规**:本轮把报告与波次记录**先 push**(`087e49a3`,`PUSH1_EXIT=0` / `PUSH2_EXIT=0`,
钩子当场跑 `GATE_EXIT=0 CLEAN`)**再开 issue** —— 上一轮自查出的第二个程序错(#469 在 push 之前发)
本轮**没有复发**。issue 草稿发表前跑 `claim_precheck.sh` ⇒ `PRECHECK_EXIT=0`,`clean`,
`OK to publish: every citation resolves on origin/main`。
