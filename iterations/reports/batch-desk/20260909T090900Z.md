# batch-desk 2026-09-09T09:09–09:5xZ — 发波轮:W60 四台**按需**起飞

**一句话**:W60 四台 on-demand 起飞,四道闸全绿,市场类型是闸 (iv) 打的不是本台判的。
⭐⭐⭐ 头号产出不在这一波,在队列底下:**`strategy-5b` 的解锁条件早在 2026-08-25 就交卷了
(GH #174,至今零评论),而 `queue.json` 那一行从那天起一个字没动 —— 它排的是 W6,今天是 W60。**

---

## 一、成本(步骤 2)

`AWS_SETUP_EXIT=0`(`arn:aws:iam::581436534924:user/dota2bot-agent`);`COSTS_EXIT=0`。

- MTD **`$70.614`**(免费 `budgets`,`refreshed 2026-09-09T07:30:21Z`),≥$35 ⇒ 自动 `$0.01` CE 复核
  **`70.6143798247`**,**两源逐位一致**。
- `forecast $312.047` —— **原样登记,不据它决策**(它把一个 12 轮/天的容器时钟外推成月度)。
- 进场在跑实例 **0 台**(账号级,不按 tag 过滤)。
- ⚠️ 那张 `budgets` 快照**早于本轮 1.7 小时**(章程钉死的 EC2 滞后 **4.3–11.3h**)⇒ MTD 系统性偏低。
  **1.7h 是本台见过最新的一张快照**,但它仍然**不含 W59**(W59 末台 04:08Z 死,离快照 3.4h,
  在滞后窗内)。⛔ 未用被禁的「MTD 增量对得上波费 ⇒ 该波已计入」手法(章程 (乙))。
- 常设成本仍只有 AMI `ami-0a990a26d89c66547`。
- **本轮跨档情况:未跨任何新档。** `$50` 早已跨(`AWS state=ALARM`),`$80`/`$100` 未跨。

## 二、⭐⭐⭐ 队列底下那根掉了 54 波的接力棒(铁律 9 连带规则的教科书形状)

步骤 4a 的普查本来只是走个过场,这一轮它撞上了东西。

`queue.json:strategy-5b`(`bundle: midtp,suptp,midsupyield`,种子 888/895/896/906)现在**逐字**是:

```
"status": "pending"
"director": { "ruling": "APPROVED_CONDITIONAL", "wave": "W6", "at": "2026-08-23T17:xxZ", "ref": "test_set.md §AX.5" }
```

裁定第 ③ 条挂的条件是:**发波前必须给出 `J.ShouldTpSupportTowerFight` 的事件轴量级**
(每局塔防 TP 响应次数 / 其中核心占几次)。「拿到之前不排发波时刻,拿到之后 W6。」

**那个数在 2026-08-25T06:54Z 就交了。** GH **#174**(`[strategy] midsupyield 事件轴量级交卷 ——
AX.5 解锁条件已满足,W6 可排期`,**open**)带着 W8 语料 211 局宽扫 + 6 局逐帧,给出
ALL 口径 **2.70–2.87 次/局**(负控的门柱)、helper-shaped 口径 armed−baseline **两层同号 +0.603**、
以及杠杆唯一能动的那个集合「可让」**每局 0.15–0.41 次**,外加五处帧证据和一条自我更正
(15s 复活冷却子句)。

**然后什么都没发生:**

- GH #174 `created_at == updated_at == 2026-08-25T06:58:04Z` ⇒ **十五天零评论、零裁定**。
- `queue.json` 那一行**至今 `status: pending`、`director.wave: W6`**,条件字段没有被标成已满足。
- `midtp` / `suptp` / `midsupyield` **不在今天的 37-id 串里**(本轮逐字算过)。
- 从 W6 到 W60 —— **54 波** —— 每一轮都有一次步骤 4a 普查看见这一行,每一轮都把它读成
  「APPROVED_CONDITIONAL,条件未满足 ⇒ 不排」。**读法没错,前提错了,而前提十五天前就翻了。**

⭐ **这正是铁律 9 连带规则那句「修好不等于做完」的形状,只是掉棒的是上游**:
录像组把条件做完了、开了 issue、写了报告,但**没有任何一个动作把 `queue.json` 那一行推下去**,
于是接力棒落在两份文件之间的缝里 —— issue 说「球回总监」,队列说「条件未满足」,
**两边都自洽,合起来是一个静默的死循环**。失效方向照例是危险的那一侧:它不报错、不缺数据,
只是让一个已获批的杠杆**在每一轮都合法地不被排期**。

**本台不自行排期**(裁定权在总监,且 `director.wave` 归总监写)。本轮动作:在 GH #174 上追评点名,
并把它写进交棒 ①。**⛔ 未改 `queue.json`** —— 改那一行等于本台替总监做入集裁定。

其余非终态行普查(100 行):`ROUTED_ARCHIVE_SCAN` 15 / `APPROVED-SCAN` 家族 17 /
`RECEIVED_NOT_SCHEDULED` 2 / `DEFERRED_PENDING_W3_RESOLVE` 2 / `REJECTED` 2 /
`ROUTED_ARCHIVE_SCAN_CONDITIONAL` 1 / `ROUTED_ARCHIVE_REDUMP` 1 / `HOLD-DOMAIN-EMPTY` 1 /
`APPROVED_CONDITIONAL` 1(= `strategy-5b`)。**除 `strategy-5b` 外没有任何一行请求发波** ——
它们要么自称「零 EC2 归档扫描」,要么自称「不申请专波」,要么是驳回/挂起。
⇒ **步骤 4b(例行「测试版 vs 稳定版」)是 W60 的正确路径。**

## 三、收割:本轮无可收割

`validation/` 最新对象 **2026-09-07T22:18:50Z**(W53 家族),`soak/` 最新四个前缀是 W59 的
`spot_20260909_0322xx`,**已由 06:07Z 那一轮全量重算过**(`RV_EXIT=0`)。进场 0 台在跑。
⇒ **本轮零收割义务**,`harvest_owed_at_launch: NONE`,证据写进 `W60_wave.json`。

## 四、四道闸(全部在起飞前跑完,读数逐字登记)

### 闸 (i) 节流 —— `THROTTLE_EXIT=0`,`margin : +7s past unlock`

⭐ **闸跑在发波脚本内部**,GH #469 那句「闸绑的是动作不是决定」这一轮是**结构性**满足的:
同一个 `bash` 脚本先 `git ls-remote` 核树(不等就 `exit 9`)、再轮询闸、拿到 `exit 0` 之后
**在同一个进程里**进发波循环,**中间插不进任何东西**。

- 轮询 **21 次 / 30s 间隔**,自 09:13:2xZ 起。**前 20 次全是 `THROTTLED (exit 3)`**,
  margin 从 `595s SHORT` 单调走到 `23s SHORT` —— **这 20 次拒答逐条登记,不丢弃**。
- 第 21 次:`WAVE_THROTTLE: UNLOCKED (exit 0) -- gate (i) passes at this instant.`
  `GATE_I_UNLOCKED_AT=2026-09-09T09:23:17Z`,首台 `LaunchTime 09:23:20Z` ⇒ **闸与动作相隔 3 秒**。
- 锚点是 W59 末台 `2026-09-09T03:23:10Z`(`max`,GH #469 ruling 1)。
- ⚠️ **本台没有抄上一轮章程里那个手打的解锁时刻** —— 那个数(09:23:10Z)这次恰好对,
  但对不对不归本台判,归工具打的那一行判。

### 闸 (ii) 有新东西可测 —— 两条腿各自充分

1. **成员串动了**:39 → **37** id,354 → **335** 字节,md5 `433070c6` → **`b525d51d`**。
   **退集 `pulllane`、`pullthink`;入集 0 个** ⇒ **连续第四波纯退集零入集**(P4.2 冻结在入集侧仍成立)。
2. **树动了**:`66a8a15b` → **`f25680bc`**,五个 commit 碰了 `bots/`
   (`18e95747` / `4a388b65` / `9403c661` / `88d845a0` / `d5b5acce`)。
   ⭐ **按章程两步走读的,认退出码不认空输出**:先 `git fetch --depth 1 origin <40 位 SHA>`
   (`FETCH_EXIT=0`),再 `git log <SHA>..HEAD -- bots/ game/`(`LOG_EXIT=0`)。
   浅 clone 下 `exit 128 + 0 字节`**是不可比,不是无漂移**。

**接线**:`ARM_WIRING_EXIT=0`,逐字 `all 37 armed ids wired on HEAD`,无 `DO NOT LAUNCH` 行;
跑的是**交给实例的那 37 个 id 本身**,不是记住的串。
⚠️ **第一次调用 `exit 2`**(argparse:旗标是 `--cand` 不是 `--armed`)—— **`exit 2` 不是通过**,
上面的读数来自改正后的那次调用。

### 闸 (iii) 围栏 —— `FENCE_EXIT=0 CLEAR`,围栏**本轮现推不是抄的**

- `--planned 2.150` = **按需**单波常数(闸 (iv) 把这一波定成按需,`$1.10` 的 spot 常数在这里是错的操作数)。
- `--pending` **没传也不需要传**:accrual 那行逐字
  `CERTIFIED (0 accruing instances account-wide, read this run) -- pending $0.000 is a reading, not a default.`
  **未用 `--no-accrual-check`** ⇒ 无「SKIPPED, NOT CERTIFIED」行。
- 读数:`actual (MTD) $70.614` / `pending $0.000` / `projected total $72.764` /
  `fence $80.00` / `brake $90.00` / `operative ceiling $80.00` / **`headroom $7.236 after this wave`**。
- 告警档:`$50.00 crossed (AWS state=ALARM)`、`$80.00 NOT yet crossed`、`$100.00 NOT yet crossed`。
  **本波不跨任何新档。**
- ⚠️ **登记不裁决**:`$7.236` 的余量 = **3 波按需**或 **6 波 spot**。按当前 6h 闸每天最多 4 波的节奏,
  **$80 档可能在 24–36 小时内被撞上**,而**跨 $80 需要总监在当轮明确裁定**(章程 §2 (iii))。
  本台不预支那个裁定,只把倒数写进交棒 ②。

### 闸 (iv) BLINDED —— `RECLAIM_BLIND_EXIT=1`,**本轮在 W59 自己的记录上重跑**

用**具名 `--wave-json`**(位置参数会拿到 argparse 的 exit 2,那个码**永远不许事后读成 UNDECIDABLE**)。
逐字:

```
yield      : 1 paired seed(s) of 4 machine(s)
attribution: 3 machine(s) reclaimed before the flip
VERDICT: BLINDED -- yield <= 1 AND the loss is capacity.
NEXT WAVE: launch --on-demand (ONE wave), then revert to spot.
```

⇒ **W60 的市场类型是闸打的,不是本台判的,也不是对 owner GH #158 的背离** ——
`#158` 自己那句「除非没有 spot 的机器」正是 BLINDED 在量的东西。**只此一波**;W61 退回 spot,
除非它自己的闸 (iv) 另说。

**GH #591 第七轮**:闸头逐字仍 `changeover 40.0 min (measured bracket 34.80 < x <= 40.63,
margin above the constant 0.63 min)`。⭐ **W60 结构上也收窄不了它** —— 按需机不会被抢占,
这一波**不会产出任何 `reclaimed before/after the flip` 读数**。它不会自己收窄。

## 五、发波(步骤 5):W60 四台按需,4/4 AZ,零 re-aim

- **核树**:`git ls-remote origin main` 读**两次**、两次都等于 `f25680bcbed59113248284bdec39f624c24e525b`
  (一次在闸阶段,一次是发波脚本的**第一条语句**,不等就 `exit 9`)。
  传给实例的是 **40 位 SHA 不是 `main`** ⇒ 本轮自己的收尾 commit 改不动实例 clone 的那棵树。
- **dry-run 先行**:`DRYRUN_EXIT=0`(**裸读,不经管道**),计划行逐字 `plan: 1 x on-demand c6i.4xlarge`
  —— 这是 `--on-demand` 生效的**第一个独立确认**。波次预算闸未拒(12 局 / 16 槽装得进 2h 看门狗)。
- **采集配置逐项显式传齐**:`--slots 16 --hours 2 --rec-slots 8 --games 12`,与 W39–W59 逐字节相同。
  `--rec-slots 8` **显式传**(脚本默认是 1;W51 就是吃了这个默认在起飞 70 秒后被杀)。
- **四次独立调用,每次显式一个 `--az`,四个互不相同**;`SPOT_RUN_EXIT` 四个 **0**(**裸读**)。

| 种子 | 实例 | AZ 请求/实得 | run_id 尾 | LaunchTime |
|---|---|---|---|---|
| 9911 | `i-06b4c4aba46b61c97` | 2a / **2a** | `…_7689b5` | 09:23:20Z |
| 10079 | `i-0bb3cf07d2a3e7f85` | 2b / **2b** | `…_be701b` | 09:23:24Z |
| 10097 | `i-07de3698e808dcba9` | 2c / **2c** | `…_15755b` | 09:23:28Z |
| 10133 | `i-085094e48cd50d9d3` | 2d / **2d** | `…_7a68aa` | 09:23:33Z |

**成功是从 AWS 状态断言的,不是从退出码断言的**:`describe-instances` 四台 `running`、
`LaunchTime` 齐全、**`InstanceLifecycle` 四台全部缺省(渲染为 `None`)= 按需**;
`describe-spot-instance-requests` 的 `active` 普查**返回空** ⇒ 这一波**没有任何 SIR**,
与「按需」互为独立佐证。`instanceInitiatedShutdownBehavior` **逐台单独查**,四台全 `terminate`。
四个 `soak-run` 标签值读回来**两两不同**,与 `machines[].run_id` 一一对上。
⚠️ `run_id` 的 `spot_` 前缀是 `spot_run.sh` 里的硬写字符串,**与市场类型无关,不可当证据**
(Name 标签是 `dota2bot-soak-od-*`)。
**降级阶梯本轮未走**:零 `re-aiming inside the ring`、零 `AZ RING EXHAUSTED`、零
`InsufficientInstanceCapacity`。

⚠️ **4/4 的 AZ 分散这一波什么都不证明,而且本波是它最弱的一次检验**:按需机根本不受 spot 容量回收管辖,
W60 **在任一方向上都加不了 GH #403 的数据点**。写进 `W60_wave.json` 的 `az_spread`,免得收割轮
把一波安静的按需读成分散度的证据。

### 选种(章程步骤 5 的活规则)

`seed_roster_index.py --build` **跑到底才写下面的互斥断言**:`ROSTER_EXIT=0`,新扫 62 局有种 + 18 局暖场,
0 unusable / 0 fetch error,**257 粒**入索引,**W59 那四粒(9531/9532/9583/9839)已经在里面** ——
十万以下最大索引种子现在恰好是 `9839`。**不 build 就会把上一波刚烧掉的四粒当成「从未用过」献回来。**

窗口右移到 **`[9901,10300]`**,与索引的交集是**空集(查过,不是假设)**。
term-popcount 分布 `{0:43, 1:115, 2:140, 3:79, 4:21, 5:2}`。
先最大化**每 term 最小载体数**、再最大化总载体槽 ⇒ **9911 / 10079 / 10097 / 10133**,`(min 2, total 18)`。
⭐ **这个 (2,18) 是该窗口的全局最优,不是启发式的最好猜测**:`min≥3` 要 7 term 各被 ≥3 粒覆盖 = ≥21 槽,
而窗口内 popcount 上限是 5 ⇒ 四粒最多 20 槽,**算术上不可能**;给定 `min=2`,`18 = 5+5+4+4`
是窗口能给的最大总额且**已取到**。

**载体闸**:`CARRIER_GATE ids=9 seeds=4 exit=0`。term **从臂串机械推导**(`--assert-carrier-from-arm`,
GH #276),不手打:`9 hero-scoped, 28 generic, 0 unresolved => 7 term(s)`。
逐 term 载体数:`crystal_maiden 3 / lion 4 / obsidian_destroyer 2 / pudge 2 / skeleton_king 2 /
spirit_breaker 2 / zuus 3`。每个 id 的裁决至少 `PARTIAL`,日志 `grep -c EMPTY` = **0**。
term 集合与 W53–W59 **逐字节相同**,尽管臂串从 39 缩到 37 —— 因为退掉的两个都是 generic。

### 家族深度:**连续第五波开一个空家族**

`(37-id 串 b525d51d × 树 f25680bc)` 这个家族**以 0 粒配对种子开张**。机制不变也不是本台的选择:
**退集裁定落地比 6h 发波闸更快**(`pulllane`+`pullthink` 这一对就落在 W59→W60 这个窗口里),
于是没有任何家族攒得起跨波深度。**GH #646 已开(09-09T00:22Z),本轮追评加第五个数据点。**

## 六、局数(铁律:报告必须带局数)

- **(a) 上一波(W59)最终有效局数**:`files_seen 80 == games_loaded 80`,`scored_games 42`,
  `unfinished 0`。逐粒:9531 `ab28/ba14`(`arm_depth 18.67`,唯一计分粒)、9532 `ab10/ba0`(NO-PAIR)、
  9583 `ab0/ba0`(全损,9.1 分钟被回收)、9839 `ab10/ba0`(NO-PAIR)。
  `min_arm_depth 8` / `thin_arm_seeds []`。**被排除的三粒在这里点名,它们的局数是真花过钱的。**
- **(b) 本轮在跑波次(W60)进度**:起飞 `09:23:20–09:23:33Z`,写报告时**尚无 `analysis.json` 落盘**
  (开机 + 暖场约 12 分钟起步)。**预期局数 ~96 计分**(4 粒 × 12 局 × 2 腿)——
  ⚠️ **只作可比性登记,不许当交付率的分母**:近波实测 W49 168 / W51 214 / W52 214 / W55 110 /
  W56 204 / W57 92(两粒)/ W58 85(两粒)/ W59 42(一粒)。**暖场局不算有效局。**

## 七、GH #454 两条腿

- **(乙1) 第七轮欠着**,本轮**不重测**:这是发波轮,而 W59 到 06:07Z(末台死后 ~2.2h)
  在逐日 CE 里**分组仍为空**,章程钉死的 EC2 滞后是 4.3–11.3h。
- **(乙2) 本轮不适用**(W60 尚未终止,无自终机时可算)。
- ⭐ **W60 是把 (乙1) 一次性结清的最好机会**:按需有**已知牌价**(`$0.673/h`,章程常数)
  且**没有抢占** ⇒ 收割轮的价格侧估算与账单侧逐日行应当在滞后之内对得上,
  剩下的差额**不能再赖到 spot 定价上**。建议收割轮把 W55–W60 一次并清。
  ⚠️ 本轮**没有拿到活的牌价**:`pricing get-products` 对 `dota2bot-agent` 返回不可解析的空体,
  ⇒ `$0.673` 在 `W60_wave.json` 里**登记为常数不是读数**。
- ⭐ 顺带:**(甲) 的方向问题在 W60 上不会出现** —— 没有回收就没有 SIR/S3 两个钟的分歧。

## 八、泄漏(步骤 6)+ 发波轮的「波次点名」

见收尾补记(§十一)。

## 九、自检(铁律 10)

见收尾补记(§十一)。

## 十、交棒

1. ⭐⭐⭐ **总监 —— `strategy-5b` 的接力棒掉了 54 波**(见二)。条件在 GH #174(2026-08-25,
   **至今零评论**)已交卷,`queue.json` 那一行仍是 `pending` / `wave: W6`。
   **要的是一次裁定**:排期(给一个真的 `director.wave`)或改判(写明为什么不再排),
   两者都比现状好 —— 现状是**两份文件各自自洽、合起来静默死循环**。
   本台**不改那一行**(入集裁定权不在本台)。已在 GH #174 追评点名。
2. ⭐⭐ **总监 —— `$80` 告警档的倒数开始了**:发完 W60 余量 **`$7.236`** = **3 波按需 / 6 波 spot**。
   按 6h 闸每天最多 4 波,**24–36 小时内可能撞档**,而跨 $80 **需要总监当轮明确裁定**。
   本台不预支该裁定,只交倒数。
3. ⭐⭐ **总监/harness(GH #646)—— 家族深度第五个数据点**,本轮追评。
   机制未变:退集比发波闸快。
4. ⭐ **总监/harness(GH #591)—— 写死的 `40.0 min` 第七轮零信息**,且 **W60 结构上收窄不了它**
   (按需机不被抢占)。
5. ⭐ **本台下一轮 = 收割轮**,解锁 **`2026-09-09T15:23:33Z`**(⚠️ **不要抄,跑 `wave_throttle.py`**)。
   收割须知全部钉在 `W60_wave.json:harvest_instructions`,三条最要紧的:
   (a) 臂串**按发波树 `f25680bc` 的 test_set.md 第 2 行**现算,**不要读 `origin/main`**
   (GH #631,连续第三轮走在这条边上);
   (b) **这一波没有 SIR** —— 逐台读 EC2 `StateReason.Code`,**必须在收割轮读完**
   (终止实例约 1 小时后从 API 老化掉,GH #375);
   (c) 逐粒回填 `ab`/`ba`/`arm_depth` 到 `W60_wave.json`,那是让下一轮闸 (iv) 可判的唯一动作
   (缺腿数 = exit 2)。

## 十一、泄漏 + 发波轮的「波次点名」

`LEAK_EXIT=0`。全账号(**不按 tag 过滤** —— 预算也没过滤,GH #515/#528)`running,pending`
= **恰好 4 台**,全是 W60 自己的(`dota2bot-soak-od-1`)。无第五台、无孤儿卷、
无外来 `c7a`/`r7a`。常设成本仍只有 AMI `ami-0a990a26d89c66547`。

**发波轮的两条点名(章程步骤 6,零成本)**:

1. `soak-run` 值集合读回 **4 个,与 `W60_wave.json:machines[].run_id` 逐一对上** ——
   **没有多的也没有少的**(判据是「恰好等于意图」,不是「没有多的」)。
2. `terminated,shutting-down` 普查**返回空**。⚠️ **这个空这一轮只说明一件事**:
   W60 起飞 10 分钟内**四台都还活着,没掉臂**。它**对 W59 一个字都不证明**
   (W59 四台早已被 EC2 从 API 里老化掉,GH #375 的 ~1h 窗口),
   也**不是**「没有东西终止过」的证据。

## 十二、自检(铁律 10)

⚠️ **第一条命令又踩了管道**(`2>&1 | tail -60`)⇒ 工具**自己拒答**,逐字
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.` /
`it has recurred 5x, every time as the first command of the round`,而 harness 报的
`EXIT=0` 是 `tail` 的。**登记为那道自拒门第三轮连续起作用,不是登记为本台没踩。**

第二次改重定向**但又带了 `timeout 400`** ⇒ `EXIT=124`,**是本台自己砍的**(GH #514 的形状,
上一轮已自陈过一次,**本轮复发**)。第三次:后台 + 重定向 + **不设 `timeout`**。

**读得到的腿照登、读不到的不声称** —— 收尾时**仍卡在最后那条 python trunk 腿上**
⇒ ⛔ **trunk 的 python 一侧本轮既不声称干净也不声称红**。读到的腿:

- **推送门**:`push gate armed (core.hooksPath=.githooks, already set)`。
- **未落地扫描**:`OK: no unlanded work in the certifiable window`
  (12 条可判 ref;959/971 因浅 clone 无 merge-base 被判 **REFUSED = 不可判,不是干净**)。
- **报告节奏**:**1 finding** —— `GAP cadence strategy 09-08T10:51Z -> 09-08T14:31Z, 3.7h`
  (门槛 3.5h)。**报出来的是问题不是判决**,交总监。
- **稳定版锚点 6/6 OK**(stable-v1…v6,`EXISTS`/`PINNED`/`SHIPPED` 全绿)。
- **promote 时约束**:`184 distinct live gate id`、18 个 `PROMOTED` 注、
  **`FROZEN none`**(没有活 gate 点名一个已 promote 的 id ⇒ `pullcad` 陷阱本轮未复发)、
  5 个 co-promote 原子全 `ok`。
- **入集等待**:`no expired admission wait in any live 当前状态 block`(6 份章程 / 55 个已结 id)。

⭐ **一件顺带查实的事,与 §二 同源**:自检的 `owed executions` registry(§DR 立的那张表,
**专门收「没有任何一波、任何一个 id 行载得下」的接力棒**)本轮 **25 行**里
**没有 `strategy-5b` / `midsupyield`**。⇒ **那根棒连这张兜底的表都漏过去了**,
这正是它能安静掉 54 波的机制。**建议的可验收修法:给它补一行 registry**(执行方 = 总监排期)。

⭐ **本台自己的一行 registry 本轮结清**:`wave_fence_ruling4_first_live_run`
(GH #515,`executor=batch-desk(下一轮跑闸 (iii) 的那一次)`)要的是**活 AWS 路径的第一手读数** ——
本轮闸 (iii) 真跑了,accrual 行逐字
`CERTIFIED (0 accruing instances account-wide, read this run) -- pending $0.000 is a reading, not a default.`
**权限足够,没有 `accrual read : FAILED`,没有静默放行,`--no-accrual-check` 未用。**

## 十三、铁律 6 / 11

- **静态门**:`GATE_EXIT=0 CLEAN`,逐字 `luacheck bots game: 0 warnings` /
  `GATE_EXIT=0  CLEAN (iron rule 6 static half passed)`。
  **未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行。**
- **推送门**:`ARM_GATE_EXIT=0`。
- **动态半**(`lua5.1 tests/run_tests.lua`,~100min,GH #124):**未跑,不声称**。
- `bots/` / `game/` / `tests/` / `tools/` **本会话一行未改**(只写 `iterations/`,
  含 `seed_roster_index.py --build` 的产物)。
- **铁律 11**:全程无 `requires approval`、**零空转** ——
  等解锁那 10 分钟是发波脚本内部的**有界轮询**(21 次,每次都打读数),自检后台跑,本台没坐等。

## 十四、Token 用量

```
TOKENS total_in=8,251,666 out=47,950 turns=61
```

(统计时刻在收尾之前,之后的回合不计入;量级参考。)
