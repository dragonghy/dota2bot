# batch-desk 2026-09-13T06:15–06:4xZ — 持有轮第七轮

**一句话**:刹车第七轮在力(owner 在 GH #779 上仍零表态),**零发波、零收割欠、零泄漏**,本轮新增支出 EC2 **`$0.00`**、CE **`$0.01`**(`check_costs.sh` 的 `≥$35` 自动复核,非自选;本台**未**主动加买第二个 `$0.01`,理由见 §四)。

⭐⭐⭐ **头号:两道闸又都是绿的,而本轮找到了一条零成本的理由说明那个绿是靠一个「从来没有读数的项」撑起来的。** `wave_fence.py:983` 第一手读源码:`projected = actual + pending + planned` —— **三项里没有任何一项装每日常驻计费**(`NoInstanceType`:AMI/EBS、IPv4、跨区流量),而 `pending` 那一项按 Ruling 4/6 是**只从 EC2 实例/波次**认证出来的。本轮 `headroom : $0.208`,而本月**十二天里最小的一天** `NoInstanceType` 是 **`$0.4375`** ⇒ **`$0.208 < $0.4375`**:闸打绿所依赖的「今天的常驻计费 = $0」是一个**工具从未声明过的假设**,而且它连 `pending $0.000 is a reading, not a default.` 那种自我限定都打不出来 —— 因为那一项**根本没有对应的读数**。

⭐⭐ **第二:今天(09-13)是本月第一个零批测日,它正好是量那条常驻底线的自然实验** —— 按本台自己实测的滞后(≥19.9h),它大约要到 **09-14T02:00Z 之后**才在账单侧可见。那是第一次能回答「不发波这条线也照样被越过吗」的读数,本台已写进交棒。

⛔ **本轮不发波是本台的处置,不是闸挡的** —— 两道跑过的闸都是绿的,如实写在 §二。

---

## 一、成本(现跑)

- `AWS_SETUP_EXIT=0`。容器无 AWS CLI,脚本自装 `aws-cli/1.46.1 Python/3.11.15 botocore/1.43.62`;身份 `arn:aws:iam::581436534924:user/dota2bot-agent`(受限 IAM,正确)。
- `COST_EXIT=0`。MTD **`$88.692`**;`≥$35` 自动 `$0.01` CE 复核 **`88.6920614964`**(与快照逐位一致);`forecast 179.629` / `limit 100.0` **登记,不据以决策**。
- `budget refreshed 2026-09-13T01:19:49Z` —— 与上一轮(03:11Z)**同一张快照**,到本轮 `06:15Z` 已 **4.9h 未刷新**。⛔ 因此**不做任何 MTD 增量归因**(章程步骤 2 (乙)),也**不**把「MTD 没涨」读成「没花钱」。
- `== running/pending instances ==` 节**空**;常驻成本仍只有 AMI `ami-0a990a26d89c66547`(`dota2bot-batch-20260718`)。

## 二、闸(现跑,裸读退出码,逐字)

**闸 (i) `THROTTLE_EXIT=0`**:
```
anchor wave      : W69 (4 machine(s))
first machine up : 2026-09-12T09:26:24Z
last machine up  : 2026-09-12T09:26:31Z  <- the anchor (GH #469 ruling 1)
slate spread     : 7s
window           : 6h
unlock           : 2026-09-12T15:26:31Z
now              : 2026-09-13T06:15:26Z
margin           : +53335s past unlock
WAVE_THROTTLE: UNLOCKED (exit 0) -- gate (i) passes at this instant.
```
⚠️ **(i) 开着不等于可以发**(连续第六轮点名):(i) 是时间函数、会自愈;(iii) 只随 MTD 单调收紧。工具自己那句 `RE-RUN IT AT THE LAUNCH INSTANT: it is the action, not the decision, that the gate binds.` 一并登记。

**闸 (iii) `--planned 1.10`(⛔ 未传 `--pending`,理由见 §三)⇒ `FENCE_BARE_EXIT=0`**:
```
crossing registry: 0 record(s) ... none in force -- the gate runs at the DERIVED fence.
budget filters   : none  <- so every instance in the account does land in this budget, whoever owns it
accrual scope    : 17 region(s) read ... <- COMPLETE: every region this account has enabled
wave accrual     : records after 2026-09-12T14:01:49Z (= 2026-09-13T01:19:49Z - 11.3h lag, clock from budget snapshot)
accrual check    : CERTIFIED (0 accruing instances account-wide, read this run) -- pending $0.000 is a reading, not a default.
actual (MTD)     : $88.692   <- re-read this run, never cached
pending waves    : $0.000
planned          : $1.100
projected total  : $89.792
  ACTUAL alert   : $50.00   crossed   AWS state=ALARM
  ACTUAL alert   : $80.00   crossed   AWS state=ALARM
  ACTUAL alert   : $100.00  NOT yet crossed   AWS state=OK
fence            : $100.00   <- lowest ACTUAL alert not yet crossed (Ruling 1)
brake            : $90.00   (owner's line, not derived here)
operative ceiling: $90.00   = min(fence, brake)
headroom         : $0.208 after this wave
WAVE_FENCE: CLEAR (exit 0) -- gate (iii) passes with the fence DERIVED THIS RUN.
```
- ⛔ **未用** `--no-accrual-check` / `--no-crossing-file` / `--director-crossing` / `--snapshot-instant` ⇒ **无任何「这是 SKIP 不是 pass」行**。
- **闸 (ii) / (iv) 本轮未评估,也不声称跑过** —— 本轮不发波,走不到那两道门。⛔ 下一轮若发波,四道门**从头跑**。

## 三、⭐⭐⭐ 头号:那个绿靠一个没有读数的项撑着

### (甲)第一手读源码,不是转载

`tools/batch_test/soak/wave_fence.py:983` 逐字:

```python
projected = actual + pending + planned
```

三项的来源各是什么,本轮逐条核对:

| 项 | 来源 | 装不装每日常驻计费 |
|---|---|---|
| `actual` | budget 快照 `ActualSpend`(本轮戳 `01:19:49Z`) | 只装**快照时刻之前已落账**的 |
| `pending` | `certify_pending()`,Ruling 4/6:**数账户内 `ACCRUING_STATES` 的 EC2 实例 + cutoff 之后起飞的波** | ⛔ **不装** —— 它量的是波,不是钱 |
| `planned` | 操作员传的 `--planned`(本台传 `1.10`,即一波 spot 覆盖档) | ⛔ **不装** |

⇒ **「快照之后发生的、非实例的计费」落在三项之外**,既不在 `actual`(还没落账),也不在 `pending`(不是实例),也不在 `planned`(不是波)。

### (乙)本轮它是决定性的,不是理论洁癖

- 上一轮(03:11Z)第一手拉的九月逐日 CE:**十二天求和 `$88.6921` 与 MTD `88.6920614964` 逐位相同** ⇒ 那张快照覆盖 `09-01..09-12`,**09-13 对它的贡献是 `$0.00`**。
- 同一张表的 `NoInstanceType` 逐日:**`$0.4375` … `$1.7591`,中位数 `$0.8212`,十二天里低于 `$0.4375` 的有 0 天**。
- 本轮闸给的 `headroom : $0.208`。

⇒ **`$0.208` 比本月已观测到的最小的一天常驻计费还小一半**。即:闸判 `CLEAR` 的那一步,等价于**默认今天的常驻计费是 `$0`**,而今天已经走了 6.25 小时。

### (丙)⚠️ 诚实边界三条(其中一条削弱本节)

1. ⚠️ **`$0.4375` 不能当作今天的下界**:那十二天**每一天都有波次活动**,而 `NoInstanceType` 里含 IPv4/跨区流量这类**部分由波驱动**的项。**一个真正的零波日可能低于 `$0.4375`,本轮量不出来** —— 这正是 (丁) 那个自然实验要买的东西。所以本节成立的是**「有一个未建模项,且它的量级压过 `$0.208`」**,不是「今天一定越线」。
2. `actual` / `pending` 的逐日数字是**上一轮**第一手拉的(报告 `20260913T031100Z.md` §四),本轮**未重新付费复核** —— 标注为转载,不冒充本轮读数。
3. ⛔ 本节**不改 harness、不定新常数**(先例 GH #33),**不拿推导替代退出码**:本轮的授权读数就是 §二 的 `FENCE_BARE_EXIT=0`。

### (丁)⭐⭐ 归属:这不是新缺陷,是 GH #565 的第一个「改变决策」的实例

⛔ **本轮不开新 issue。** GH #565 立的就是这条(`accrual check 会把「零台在跑」证成「零元未落账」`),本轮加的是**它第一次落在刀口上**:此前每一轮 headroom 都远大于任何一天的常驻计费,那个缺口**藏在余量里看不见**;本轮 headroom `$0.208` **小于**它,于是同一个缺陷第一次能单独翻转一个发波决定。已作证据评论发在 **#565**。

## 四、为什么本轮没有加买第二个 `$0.01`

上一轮立的判据是「当轮 CE 复核 **> `$86.7576682213`** 才花第二个 `$0.01`」,那条判据服务的是 GH #454 (乙1),而 **(乙1) 上一轮已结清**。本轮唯一值得买的新读数是**今天(零波日)的常驻计费**,而按本台自己实测的滞后(**≥19.9h 且 ≤≈21.0h**,上一轮双边有界),**今天的账现在拉出来必然接近 `$0`,而那个 `$0` 是滞后不是事实** —— 花 `$0.01` 买一个必然被误读的读数,是本章程反对的那种支出。⇒ **不买,写进交棒等 09-14T02:00Z 之后再买。**

⭐ 顺带:§三 (乙) 的结论**不依赖**那次购买 —— 今天这一天还没过完,**它的常驻计费无论如何不可能已经在 `01:19:49Z` 的快照里**,所以 (甲)(乙) 的算术是 a fortiori 成立的。

## 五、收割

**零欠。** `s3://dota2bot-batch-results-4924/validation/` **592 个对象**,与上一轮登记的基线**逐个相同** ⇒ 无 W69 之后的新数据。W68/W69 均已收割完毕(前两轮)。本轮**未新增任何 verdict**。

## 六、泄漏

**零。** 四条独立路径:
1. `check_costs.sh` 的 `== running/pending instances ==` 节**空**;
2. 闸 (iii) 的账户级普查 `accrual scope : 17 region(s) read <- COMPLETE`(该账户启用的**每一个** region);
3. 同 `accrual check : CERTIFIED (0 accruing instances account-wide, read this run)`;
4. 常驻资源枚举只有 AMI `ami-0a990a26d89c66547` + 其快照。

⛔ **GH #515/#528 的立案理由一字未变**:`budget filters : none`。

## 七、queue / 优先项

- `queue.json` **117 条**:`pending 46`、`harvested_pending_verification 29`、`done 9`、`ruled_approved_scan 8`、`harvested 6`、`frozen_hold 5`、`running 3`、`ruled 3`、其余各 1。**无一条要求本轮发波**。
  ⚠️ 登记一条**给总监的观察,不自裁**:`running 3` 与本轮账户级 `CERTIFIED ZERO` **不相容** —— 没有任何实例在跑,却有三条请求挂在 `running`。⛔ 本台**不替它们改状态**(不是本台开的请求),只点名。
- `OWNER_PRIORITIES.md` **4.1 upstream 标尺波**(`stable(当前 main) vs upstream 74727e4a`,`aws_run.sh`,不 arm 任何 candidate)仍挂在**「当前球:批测台」**。⛔ 本轮**仍未认领,原因是钱不是排期**:它与任何 arm 波一样要过闸 (iii),而 §三 说明本轮的 `$0.208` 撑不住。**owner 一旦在 #779 说「发」,本台下一轮发的就是它**(4.1 逐字「排在任何 arm 波之前」),⛔ 不是普通 arm 波 —— 这一条本轮明确写进交棒,免得又出现 09-05 那种「发了但发错了波」。

## 八、本轮支出

| 项 | 金额 |
|---|---|
| EC2 | **`$0.00`** |
| CE(`≥$35` 自动复核 ×1) | `$0.01` |
| **合计** | **`$0.01`** |

## 九、`bots` / `game` / `tests` / `tools` 本会话一行未改

§三 的 harness 缺陷**本台不自己改**(先例 GH #33),已作证据交 **GH #565**。

## 十、铁律 11

本轮**未触发任何 `requires approval`**,无等待。`mcp__github__*` 可用。

---

## 交棒

1. ⭐⭐⭐ **owner —— GH #779 第七轮零表态,刹车第七轮持有。** 本轮加的**不是余量也不是时限,是一条反方向的约束**:上一轮告诉您「还剩恰好一波」;本轮 §三 说明那个「剩」是**闸不装常驻计费**算出来的,`headroom $0.208` 小于本月已观测到的**最小**一天的 `NoInstanceType`(`$0.4375`)。⚠️ **但请连着 §三 (丙)1 一起看**:那十二天都有波次,零波日可能更低,**本台没有把它算成「一定越线」**。三条选项不变:维持 `$90` / 给 budget 加成本分配标签过滤器(`budget filters : none`)/ 一次性解禁到某个数(并说明是否含外来支出,外来机型 ≈ MTD 的 **42.21%**)。
2. ⭐⭐⭐ **下一轮本台 —— 去买那个零波日读数。** 今天(09-13)是本月**第一个零批测日**;按实测滞后 ≥19.9h,**09-14T02:00Z 之后**花 `$0.01` 拉逐日 CE,第一次得到「不发波时这条线一天涨多少」。那个数**同时**回答 owner 那道题的 §2(「即便一波不发,`$90` 会不会在 09-30 前被越过」)和 §三 (丙)1 留下的洞。⛔ **快照/账单侧没推过就不要编**,按本轮同样方式登记「仍欠」。
3. ⭐⭐ **总监/harness —— GH #565 第一个「改变决策」的实例已评论**(§三)。建议的收窄方向:让 `projected` 里出现**第四项**(自快照戳起的常驻计费下界,按本月逐日 `NoInstanceType` 的最小值兜底),或者**至少**在 `headroom` 小于该量级时打一行限定 —— 现在它一个字都不打。⛔ 本台不改 harness、不定新常数。
4. ⭐⭐ **owner/总监 —— 若说「发」,下一轮发的是 `OWNER_PRIORITIES` 4.1 的 upstream 标尺波**,不是 arm 波(§七)。
5. ⭐ **总监 —— `queue.json` 有 3 条 `running` 而账户级零实例**(§七),本台只点名不改。
6. ⛔ **下一轮本台**:围栏/闸/成本/收割/泄漏**一律当轮现跑**,本报告的 `$88.692` / `headroom $0.208` / `budget refreshed 01:19:49Z` / S3 `592 对象` / 闸 (i) 锚点 `2026-09-12T09:26:31Z` **全部作废**。⚠️ 开工第一条命令**重定向 + 后台 + 不设短 `timeout`**(上一轮交棒记为**第 23 次**;本轮实际情况见 §十一)。

## 十一、自检与铁律 6 读数

(收尾时回填 —— 见文末补记)

## 十二、Token 用量

(收尾时回填)
