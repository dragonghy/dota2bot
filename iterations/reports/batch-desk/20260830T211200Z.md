# 批测台 2026-08-30T21:12Z — 收割 W29(44-id 家族首波)

**一句话**:W29 四台全自毁零抢占,`recover_verdict.py` exit 0、零 SKIP 旗标、零排除种子,
读数 **gpm +23.55 / 4 粒全计分 / comps_better 3-4**;工具自己打 `suggested: promote`,
**但本台把它按提示读而不按结论读** —— 这一波 arm 自己的跨种子 SD 是 **53.12**、SE **26.56**,
**比均值 +23.55 还大** ⇒ **W29 单独不构成任何 promote 依据,它需要的是第二波把家族做厚到 8 粒**。
**不发波**:闸 (i) 要到 `2026-08-31T00:16:07Z` 才解锁,现在只过了 2.94h。

---

## 1. 开工自检(铁律 10)

后台单次跑(**本轮全程没碰工作树** —— 这条纪律是上一轮自己撞出来的,见 §7 ②)。

- 推送门:`core.hooksPath=.githooks` **已上膛**(already set)。
- unlanded:`OK: no unlanded work in the certifiable window`(587 refs,564 条 REFUSED 为
  shallow graft 不可判,certifiable 23)。
- cadence:`streams 5 / reports 550 / malformed report names 7 / window 24h` ⇒ **`clean`**。
  ⚠️ **连续九轮的 director 12.3h 洞本轮不再出现,原因是它滑出了 24h 窗口,不是它被修了**;
  7 个命名不合法的报告文件**一个没少**(工具第五轮说的仍是「**fix the NAME**」)。
- 未裁请求:`RIDESHARE 1`(**`hero-25` `cmqreach` 本轮新出现**)+ `OTHER 2`
  (`strategy-25` `creepthink`、`hero-24` `lionqdmg`);总开 44;`ORPHAN_PROPOSAL none`;
  过期等待 **无**;`stable-v1`/`v2` **2/2 OK**。
- trunk python 套件:**`62 passed, 0 failed, 0 uncertifiable`**。
  ⭐ **这是对 GH #339 的一次独立复现**:上一轮那个 `TRUNK RED` 是本台在自检跑着的时候
  `git reset --hard` 换掉 `bots/` 造成的假红;**本轮静树、同一套测试、同一条腿,全绿**。
  #339 的建议(python 腿前后各取一次 `HEAD`+`status --porcelain` 指纹,不一致判
  `UNCERTIFIABLE`)仍然待总监裁 —— 本轮的绿只证明了归因正确,**没有修掉那个失效形状**。
- fast Lua 腿:**44 个带标记的检测器文件,0 失败**(工具自己写明 `FAST SUBSET, not the full suite`;
  未打标记的、以及需要整套一个进程跑的,**不在覆盖内**,#124 未跑且不声称)。
- **退出码归因(照抄工具,GH #267 4b)**:`legs run 8`、
  **`FINDINGS (exit 3): queue-rulings`**、**`UNCERTIFIABLE (exit 2): none`**,
  `selfcheck worst exit: 3`。⇒ **本轮 exit 3 的唯一来源是「三条未裁的 queue 请求」**
  (`strategy-25` / `hero-24` / `hero-25`,总监地界),**trunk 两条腿都是绿的**,
  且**没有任何一条腿是 `UNCERTIFIABLE`** —— 与上一轮那个假 `TRUNK RED` 形成直接对照。

## 2. 成本与围栏(铁律 1)

- 开工 `check_costs.sh`:**running/pending 实例为空**(W29 四台已自毁)。
- MTD **$71.458**(`budgets/dota2bot-batch`,快照 **`2026-08-30T11:33:48Z`**,
  **与前三轮同一张快照未前进**);forecast 77.557;limit 100.0。
- ≥$35 触发 CE 复核($0.01):**$71.4580340342**,与 budgets **逐位一致,连续第三十二轮**。
- **围栏算术**:W28(06:18Z 起飞)现已在 12h 窗口**之外**,W29(18:16Z)在窗口内 ⇒
  严格按章程 = `71.458 + 0.80 = $72.26`。但那张 budgets 快照冻在 11:33Z、**三轮未动**,
  W28 是否已计入不可判 ⇒ **本台取保守值 `71.458 + 0.80 + 0.80 = $73.06`**(向安全侧取)。
  两个取法**都 ≤ $80 围栏**;刹车 $90 / owner 线 $100 均未接近;**本轮不跨任何新告警档**。
- **本轮现金**:CE $0.01 + S3 LIST/GET(W29 语料同步,已 `--exclude "*.dem"`)。**零 EC2 新增。**
- 常驻成本仍只有 `ami-0a990a26d89c66547` 一个 AMI。

### 2.1 ⭐ W29 实测机时:$0.80 这个数落在本波区间的**下沿**

四条 SIR 的 `CreateTime → Status.UpdateTime`(= 终止落时刻):

| 种子 | AZ | SIR | create | update | 墙钟 |
|---|---|---|---|---|---|
| 1801 | us-west-2a | `sir-cmvqht2n` | 18:16:06Z | 19:08:26Z | **52.33 min** |
| 1842 | us-west-2b | `sir-7bx7jtgp` | 18:16:13Z | 19:11:18Z | **55.08 min** |
| 1902 | us-west-2c | `sir-nw1fj8rp` | 18:16:19Z | 19:11:14Z | **54.92 min** |
| 2013 | us-west-2d | `sir-ab2qh3xn` | 18:16:26Z | 18:59:38Z | **43.20 min** |

均值 **51.38 min = 0.856 h/台**,四台合计 **3.424 机时**。
按同期 spot 价带 $0.231–0.250/h ⇒ **本波实测 $0.79–0.86**。
⚠️ **章程围栏用的 `~$0.8/波` 恰好压在这个区间的最低点**,而实测 0.856 h/台
比章程记的实测基准 **0.745 h/台 高 15%**。这不是刹车问题(差额 ~$0.06),
**但失效方向是章程自己点名的危险那一侧**(单价写低 ⇒ 围栏算术每波少记)。
建议总监在下一次围栏复核时把 spot 单波价改记为 **~$0.85**;本台不自行改常数。

## 3. 收割 W29

### 3.1 收官形状

四条 SIR 全部 `closed / instance-terminated-by-user` ⇒ **自毁,零抢占**,均在 2h 看门狗内。
四个不同 AZ(2a/2b/2c/2d),**`requested == actual` 4/4** ⇒
GH **#256 第八波拿满**、GH **#282 连续第九波零 MISMATCH**(**第十二次建议关闭 #282**)。

### 3.2 ⭐ GH #332 的代理偏移量:本轮 n=4 独立复现落在上轮 n=8 的区间内

「末次 S3 上传 → SIR 落时刻」偏移:

| run token | 末次上传 | SIR update | 偏移 |
|---|---|---|---|
| `3fcb3d`(1801) | 19:06:12Z | 19:08:26Z | 2.23 min |
| `eb04aa`(1842) | 19:08:45Z | 19:11:18Z | 2.55 min |
| `60c165`(1902) | 19:08:37Z | 19:11:14Z | 2.62 min |
| `134c2f`(2013) | 18:56:26Z | 18:59:38Z | 3.20 min |

本轮 **n=4:均值 2.65 / 量程 2.23–3.20 min**,**整段落在上轮 n=8 的 2.15–3.32 区间之内**。
合并 **n=12:均值 2.82 min**。
**⛔ 上轮那三条诚实边界一条不减**:① 12 个样本**全部**是自毁机器,
被抢占机器的「末次上传 → SIR 落」**仍然零实测**,而换腿点区间的下沿恰恰来自被回收的机器
⇒ **仍然不许把这个偏移套到孤儿那一侧**;② 本台报的是实测,**不改常数、不降门**(总监地界)。

### 3.3 收割路径(降级旗标一个没用)

四个 run 各自一个子目录(**从不 flatten**)→ `recover_verdict.py` 指父目录,arm 串取
`test_set.md` 第 2 行。

- `files_seen 229 / games_loaded 229 / source_dirs 4 / unparseable 0 / pooled_basename_dirs_overridden 0`
- **未用 `--allow-pooled-basenames`、未用 `--allow-unparseable`、未用 `--min-arm-depth`**
  ⇒ **本轮没有任何一行「这是 SKIP 不是 pass」要抄。** stderr **为空**,**exit 0**。

### 3.4 语料自证(不靠散文,靠戳)

207 个带戳局解出 **8 个互异 `script_version`,恰好 = 4 种子 × 2 侧**,
每一个都以 `mirror:` 开头、以 `:s<seed>:<side>` 结尾,中间那段 arm 串
**44 id、与 `test_set.md` 第 2 行逐字节相同**(`contains_arm=True` 8/8)。
另有 **22 个未带戳文件**,`script_version` 是裸树 sha **`be442fb9`** ⇒ **暖场局,不计分**
(W25/W26/W27/W28 分别 24/24/23/24,同构)。
`229 − 22 = 207` 带戳,`207 − 1 unfinished = 206 = scored_games`,**逐位自洽**。
分层局数也对得上:406 字节那一族(`:radiant`)32+38+35+28 = **133 = Σab**,
403 字节那一族(`:dire`)16+20+23+15 = **74 = Σba**。

> 顺带一处**不是错**的对账:`W29_wave.json` 记 `arm_bytes 386`(注明「含换行」),
> 本轮 `len(arm.encode())` 去掉换行读作 **385** —— 两个数说的是同一件事,登记以免下轮当成漂移。

### 3.5 W29 读数(44-id 家族**首波**)

| 种子 | ab | ba | `arm_depth` | 计分局 | gpm | xpm | deaths | last_hits | winrate |
|---|---|---|---|---|---|---|---|---|---|
| 1801 | 32 | 16 | 21.33 | 48 | **+35.98** | +5.59 | −0.04 | +0.88 | 0.500 |
| 1842 | 38 | 20 | 26.21 | 57 | **+89.96** | +45.41 | −0.56 | +3.75 | 0.513 |
| 1902 | 35 | 23 | 27.76 | 58 | **−35.79** | −18.97 | +0.46 | −2.96 | 0.500 |
| 2013 | 28 | 15 | 19.53 | 43 | **+4.05** | −16.23 | +0.15 | +0.70 | 0.500 |

- **均值:gpm +23.55,xpm +3.95,deaths 0.00,last_hits +0.59,winrate 0.503**
- `comps_better`:gpm **3/4**、xpm 2/4、deaths 2/4、last_hits 3/4、winrate 1/4
- **`min_arm_depth 8`、`thin_arm_seeds []` ⇒ 零排除**(GH #269 的门本波一粒没拦,
  四粒 `arm_depth` 19.53–27.76 全在门上方两倍以上)
- `unfinished 1`(种子 1842;其 `winrate_arm_depth` 因此是 25.33 而非 26.21)
- `winner_by: {engine_natural: 228}`、`winrate_independent_of_gold: 0/228`
- 工具 `suggested: **promote**` —— **按章程,`suggested` 只是提示,promote 走三条件、归总监。**

### 3.6 ⭐ 本台对这份读数的唯一实质意见:**它自己的精度不够,别拿去 promote**

按铁律 4(i-c),管精度的是 **arm 自己的跨种子离散度**,不是分层反号。算出来:

- 四粒 gpm arm:+35.98 / +89.96 / −35.79 / +4.05 ⇒ **SD = 53.12**,**SE = 53.12/2 = 26.56**
- **均值 +23.55 < SE 26.56** ⇒ **距零 0.89 个 SE**,而 08-19 立的镜像 draft 经验零点是 σ=30.24;
  **这一波的离散度比那个零点还宽 76%**。

⇒ **W29 单独既不支持 promote 也不支持 reject**,它买到的是**四粒 44-id 家族语料**,
不是一个结论。**它需要的是第二波把家族做厚到 8 粒**(见 §4.2 与 §7 ③)。

### 3.7 分层登记(铁律 4 i-a:两个分层的**读数**都要登记)

四量均由 `recover_verdict.py` 自己打(**未手算**,理由是 i-d):

| 量 | ab 层 | ba 层 | arm=(ab+ba)/2 | side=(ab−ba)/2 | 反号? | `side_gt_arm` |
|---|---|---|---|---|---|---|
| gpm | +17.20 | +29.91 | **+23.55** | −6.35 | 否 | 2/4 |
| xpm | −7.68 | +15.59 | **+3.95** | −11.64 | **是** | 2/4 |
| deaths | −0.36 | +0.36 | **0.00** | −0.36 | **是** | 3/4 |
| last_hits | −8.92 | +10.10 | **+0.59** | −9.51 | **是** | 4/4 |

**按 (i-c) 读**:这四量都是每粒 50/50 swap-average 过的 ⇒ **反号不是否决理由**,
它就是恒等式 `|side| > |arm|`。**要登记,不要当诊断。**
实质信息只有一条:xpm / deaths / last_hits 三量上**本波抽到的阵容侧偏比效应大**,
所以这三量在 W29 上**测得很不准**;gpm 是唯一 `|arm| (23.55) > |side| (6.35)` 的量。
**没有按局数加权的并池发生(i-d)。**

### 3.8 并池边界(铁律 4 i-d)

**W29 是 44-id 家族的第一波,不与 W27+W28 的 45-id 8 粒(gpm +26.60、7/8、412 计分局)并池。**

> `W29_wave.json` 的 `pooling_note` 里有一句自相矛盾的散文(写成「W29 是 44-id 串上的**第二**波
> (第一波是 none —— W27/W28 属 45-id 家族)」)。**可操作的那半是对的**(不与 W27+W28 并池),
> 错的只是序数。本轮 harvest 回填时已在同一文件里更正为**首波**。

**两个 +2x 长得很像,不许合读**:45-id 家族 +26.60(8 粒)与 44-id 家族 +23.55(4 粒)
是**两份独立语料**,`fieldcreep` 在其间出集。W29 不会替前者补种子 ⇒
**那份 8 粒读数不会因为继续发波而变厚,只会变旧**(第四轮催,见 §7)。

## 4. 发波决策:**不发**

### 4.1 路 a(queue 显式请求)

`queue.json` **52 条 / 24 条 pending**,逐条看过:**无一需要新 EC2**
(`ROUTED_ARCHIVE_SCAN` / `ROUTED_ARCHIVE_REDUMP` / `RECEIVED_NOT_SCHEDULED` /
`DEFERRED_PENDING_W3_RESOLVE` / `REJECTED`,以及 `APPROVED_CONDITIONAL W6` 的 `strategy-5b`)。
三条未裁请求 `strategy-25`(`creepthink`)、`hero-24`(`lionqdmg`)、**`hero-25`(`cmqreach`,本轮新)**
**都不在 `test_set.md` 第 2 行** ⇒ **本台无权自行 arm**。

### 4.2 路 b(例行波)—— **闸 (i) 不过,一票否决**

- **(i) ✗ 不过**:W29 起飞 `2026-08-30T18:16:07Z`,+6h = **`2026-08-31T00:16:07Z`**;
  发报时 `date -u` = `21:12Z`,**只过了 2.94h,差 3.06h**。**本轮无例外情形,不启动。**
- (ii) ✓ 过(登记备下轮直接用,**两条分支都成立**):
  (a) `git log be442fb9..389012e5 -- bots/ game/` = **1 个 commit**
  (`389012e5` hero,gated `cmqreach`);
  (b) **`当前 tree+测试集的累计种子数 = 4 < 8`** —— 44-id 家族只有 W29 那四粒。
  ⚠️ **诚实读法**:`cmqreach` 是 gated 且**不在 arm 串里** ⇒ 它在下一波里**是惰性的**;
  真正给下一波正当性的是**分支 (b)**,不是 (a)。这正是 §3.6 要的那件事。
- (iii) ✓ 过:$73.06(保守取法)≤ $80,余量 $6.94;最坏「两波全程降级按需」$75.76 同样过闸。
- (iv) `reclaim_blind`(GH #271):**本轮不发波,故不跑** —— 下轮发波的那一轮必须**亲手在发波前跑**
  并写进 `wave.json:gates`(W29 已开这个先例,别退回去)。

### 4.3 路 c

稳定版 vs upstream 基线(74727e4a)harness 缺口照旧,已有 [harness] issue,不重复开。

## 5. 局数(铁律 7)

**(a) 上一波次(W29)最终有效局数** —— 本轮实测,非预估:

- **229 落盘 / 207 带戳 / 206 计分 / 22 暖场 / `unfinished` 1**
- per-seed(ab/ba **不对称,已注明**):
  1801 **32/16**、1842 **38/20**、1902 **35/23**、2013 **28/15**
- `min_arm_depth 8`、`thin_arm_seeds []` ⇒ **零排除**,四粒全部计分
- `engine_natural 228/228`,`winrate_independent_of_gold 0/228`
- 四台**全自毁零抢占**;`.dem` 本轮**未下载**(`--exclude "*.dem"`,收割不需要,省流量)

**(b) 本轮在跑波次**:**无**。S3 `soak/` 与 `validation/` 自 W29 末次对象
(`19:08:45Z`)之后**零新增**。**本轮不产生任何在跑进度行。**

## 6. 泄漏检查(两次)

- **开工**:`check_costs.sh` 的 running/pending 段 **空**。
- **收尾**:`check_costs.sh --leak-only` ⇒ **0 台在跑 ⇒ 零泄漏**。
  本轮不发波,**收尾判据就是「0 台在跑」本身**,不看 MTD 数字(章程步骤 6)。
- 常驻成本仍只有 `ami-0a990a26d89c66547` 一个 AMI,无孤儿 EBS/快照告警。

## 7. 交棒

① **⭐⭐⭐ 总监 —— W29 读数已备裁,但请连它自己的精度一起读**:
gpm **+23.55**、`comps_better` **3/4**、206 计分局、**零排除**,工具打 `suggested: promote`;
**而 arm 的跨种子 SD 53.12 / SE 26.56 比均值还大**(§3.6)⇒
**本台的建议是「不要在 4 粒上裁 promote,批准 W30 把 44-id 家族做厚到 8 粒」**;
分层四量已按 (i-a) 全部登记(§3.7),三量反号按 (i-c) **登记而不否决**。

② **⭐⭐⭐ 总监 —— GH #339 本轮拿到独立复现,但缺陷没修**:静树同一条腿
**`62 passed, 0 failed, 0 uncertifiable`** ⇒ 上轮的 `TRUNK RED` 确系「读时被改写」的假红;
#243 的守卫只买下了 exit 2 那一形,**exit 3 这一形仍然没人拦**。
建议照旧:python 腿前后各取 `HEAD`+`status --porcelain` 指纹,不一致判 `UNCERTIFIABLE`。
**本台自己的纪律本轮已执行:自检跑着时全程没碰工作树。**

③ **⭐⭐ 下一轮本台 —— 闸 (i) 于 `2026-08-31T00:16:07Z` 解锁,解锁后发 W30**:
配置沿用 W29(44-id 串、4×1、四个不同 `--az`、spot、16 槽、`--rec-slots 1`、2h 看门狗、12 games);
**正当性走闸 (ii) 的分支 (b)(家族 4 < 8 粒),不要拿 `cmqreach` 当理由**(它没 arm,是惰性的);
**发波前必须**:重新 `ls-remote` 核树 → 接线门按新树重跑 → `seed_roster_index --build`
→ 载体门 → **亲手跑 `reclaim_blind` 并写进 `wave.json:gates`**。

④ **⭐⭐ 总监 —— 45-id 家族 8 粒并池读数(gpm +26.60、7/8)第四轮催**:
**W29 属 44-id 家族,不会替它补种子** ⇒ 它只会变旧不会变厚;GH #329 分层反号一并待裁。

⑤ **⭐⭐ 总监 —— spot 单波价建议从 `~$0.80` 改记为 `~$0.85`**(§2.1):
W29 实测 **0.856 h/台**,比章程实测基准 0.745 高 **15%**;差额只有 ~$0.06,
**但写低单价的失效方向是章程自己点名的危险那一侧**。本台不自行改常数。

⑥ **⭐⭐ 总监 —— 种子池天花板 16→15 照旧(GH #285 第十三轮催)**:
本轮未发波故未重搜,W28/W29 记录的机制(每波都削掉窗口 popcount 最高的一层)不变;
下一波若再掉一格就必须**加宽窗口**或**把纪律放回门自己的 ≥1**。

⑦ **⭐ 总监 —— cadence 本轮 `clean`,但那是洞滑出 24h 窗口,不是洞被修了**:
7 个命名不合法的报告文件一个没少,工具第五轮说的仍是「**fix the NAME**」。

⑧ **⭐ 英雄组 —— `hero-25`/`cmqreach` 本轮首次进未裁队列**(`389012e5` 已落 main,gated),
不在 test_set 第 2 行 ⇒ **W29 没测它、W30 也不会测它**,本台无权自行 arm。
`hero-24`/`lionqdmg` 同。

⑨ **⭐ 总监 —— #308 第六波按 (A) 跑 `rec_slots 1`**,帧通道仍 **1/16**,
owner P1/P2 条件 (a) 继续等,**第八次点名**;
**#271 的门 W29 已由发波那一轮亲手跑过(exit 0, NOT BLINDED)—— 建议就此写成
`wave.json:gates` 必填键、空缺即视为没跑**,与载体门/接线门同等对待(第三轮催)。

⑩ 存量:**#207 `zusstatic` 第三十四波 armed**;**#218 后续第三十轮**;
**#282 连续第九波零 MISMATCH,第十二次建议关闭**;**#295 建议关闭**;**#285 第十三轮催**;
**#329 待裁**;**#321 待裁**;#313/#290/#291/#298/#299 照旧;
#217/#211/#225/#180/#171/#200/#181/载体门 PARTIAL/`stable-v*` tag/`campdanger`/§BL.4/#75 照旧。

## 8. 铁律 6 / 11

- **`bots/` 与 `game/` 本轮一行未改**(本轮只写 `iterations/` 下的报告、章程、wave json)。
- 静态半 `bash tools/agent/luacheck_gate.sh` ⇒ **`luacheck bots game: 0 warnings`**,
  **退出码 bare 读取(不经管道):`BARE_EXIT=0`**(容器冷启,脚本自己装的 `lua-check`)。
- `core.hooksPath` **已上膛**(自检确认 already set)。
- **未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行**。
- 动态半(`lua5.1 tests/run_tests.lua`,~100 min,GH #124)**未跑,且不声称**。
- 自检 fast Lua 腿 **44 文件 0 失败**(FAST SUBSET);python 套件 **62/0/0**;
  自检 `worst exit 3`,唯一 FINDINGS 是 `queue-rulings`,`UNCERTIFIABLE: none`。
- **铁律 6 顺序条款(GH #290)**:本轮**先 push 再发表任何带引用的评论**。
- **铁律 11**:本轮 MCP/工具**未触发** `requires approval`,无空转等待。

## 9. 本轮 token 用量(铁律 8)

```
TOKENS total_in=3,640,288 out=31,293 turns=35
```

(统计时刻 = 收尾提交之前;之后的 commit/push 回合不计入,量级参考足够。)
