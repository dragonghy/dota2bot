# batch-desk 工作单元报告 — 2026-08-19T00:11:11Z

## 1. AWS 引导
`session_setup.sh` 成功。身份:`arn:aws:iam::581436534924:user/dota2bot-agent`
(受限 batch-runner IAM 用户,确认正确)。

## 2. 成本检查(启动前)
MTD = **$3.45**(新月度周期已重置;章程里 2026-08-01 记录的 ~$85/刹车线 $90
是上个计费月的陈旧数字,本轮以 check_costs.sh 现查为准)。启动前无在跑/挂起实例,
无泄漏。

## 3. 收割
- 扫描 `s3://dota2bot-batch-results-4924/validation/`(82 个对象),最新条目日期为
  2026-07-31,早于本 stream 建立(2026-08-01)。用 `recover_verdict.py` 对 07-31
  四份 verdict 逐局重算做抽查,结果与 `iterations/state.json` 已记录的数字一致
  (例如 12-id 复审组合 `creeppull,pullcamp,l1trade,l5combo,midtp,suptp,tpcommit,
  lf_rescue,teambrain,ownhalf,overchase,fieldregen` 重算 gpm −25.61(801-804 波)
  和 −17.98(811-814 波),与记录的 −18.07 八种子 HOLD 结论吻合)。
  **结论:07-31 数据已被此前(建组前)会话完整收割分析过,本轮无新数据可收。**
- `iterations/queue.json` 为空(`requests: []`, `done: []`),无待处理请求。
- 确认当前 `test_set.md` 的完整 14-id 组合(12-id 复审组 + `wandbleed,tpwatch`)
  在 S3 上**从未跑过**——符合章程里"队列为空 → 跑例行测试版 vs 稳定版"的触发条件。

## 4. 本轮启动
**决定:启动新的例行波次。**
- 优先级 (a) queue.json 待处理请求:无。落到 (b) 例行"测试版 vs 稳定版"。
- 启动前核对:`git ls-remote origin main` → `96f49dc98e175cba23bea0e12eb42d69c150ae56`,
  与本地 HEAD(`96f49dc hero: fix WK reincarnation-armed mana check`)完全一致,
  确认测的是对的树。
- 用的工具:`spot_run.sh --validate`(镜像草稿候选验证启动器)。
  **注:章程第5条和 `.claude/agents/batch-runner.md` 里写的 `aws_run.sh` 实际是
  另一个更老的脚本(纯 old-ref-vs-new-ref A/B,没有 `--validate`/`CAND` 概念);
  `iterations/state.json` 里记录的历次真实启动全部用的是 `spot_run.sh
  --validate`。已在下方"当前状态"和章程正文里改正这处文档错误,避免下次会话
  用错脚本。**
- 测试组合:`creeppull,pullcamp,l1trade,l5combo,midtp,suptp,tpcommit,lf_rescue,
  teambrain,ownhalf,overchase,fieldregen,wandbleed,tpwatch`(test_set.md 现行全集)。
- 两个 spot 实例,各 2 个种子,共 4 种子(达到 4-seed promote 判定下限):
  - `i-06bba650a8176fa42`,run_id `spot_20260819_001001_1_main`,种子 851/852,
    `s3://dota2bot-batch-results-4924/soak/spot_20260819_001001_1_main/`
  - `i-013541b5aee463ae1`,run_id `spot_20260819_001007_1_main`,种子 853/854,
    `s3://dota2bot-batch-results-4924/soak/spot_20260819_001007_1_main/`
  - 均确认 `running`,c6i.4xlarge spot,每种子 15 局,16 槽,4h 看门狗自毁。
- 测试的 commit:`96f49dc98e175cba23bea0e12eb42d69c150ae56`(与 origin/main tip 一致)。
- 预估成本:c6i.4xlarge spot ≈ $0.25/h × 2 实例 × ~2-3h ≈ **$1-1.50**,远低于刹车线。
- verdict 预计落地路径(约 2-3 小时后,00:10 UTC 起算):
  `s3://dota2bot-batch-results-4924/validation/<test_set_id_string>_<stamp>.verdict.json`
  (两份,每实例一份)。下一轮触发用 `recover_verdict.py` 标准收割路径处理。

## 5. 成本检查(结束前)
MTD 仍显示 $3.45(账单有滞后,符合历史规律)。`describe-instances` 确认只有
刚启动的两台实例在跑(`dota2bot-soak-spot-1` 标签,均为自毁 spot + 4h 看门狗)。
**无泄漏。**

## 6. 待跟进 / 上报
- **未触发预算刹车**(MTD $3.45,远低于 $90),无需开 `[batch] 预算刹车已触发` issue。
- 章程步骤5的脚本名称错误(`aws_run.sh` → 应为 `spot_run.sh --validate`)已在本次
  一并修正,不额外开 issue。
- 07-31 组合(测试集减 wandbleed/tpwatch)HOLD 结论(−18~−26 gpm 残差,详见
  `state.json` 的 `promote_reaudit_VERDICT`/`residual_fingerprint_20260731`)仍是
  已知未决问题,非本轮新增;本轮加测 wandbleed+tpwatch 是该问题的下一个数据点,
  留给录像组/协同组解读,批测台不做判断分析。

## 7. 涉及路径
- `iterations/queue.json`(空,未变)
- `iterations/streams/test_set.md`(本轮测试的组合来源)
- `iterations/state.json`(含 07-31 前序数据的完整分析)
- `tools/batch_test/aws/spot_run.sh`(本次启动脚本)
- `tools/batch_test/soak/recover_verdict.py`(收割抽查用)
- S3:`soak/spot_20260819_001001_1_main/`,`soak/spot_20260819_001007_1_main/`(新数据,跑中)
