# 批测台 2026-09-15T18:06:37Z —— 无波轮(刹车第二十七轮持有)

> **头号读数**:预算快照**又冻住了** —— MTD `$90.782` 与戳 `2026-09-15T14:40:18Z`
> **与上一轮(15:20Z)逐位相同** ⇒ 本轮登记的是**「没测到新支出」不是「没花钱」**
> (⛔ 这两句不是同一句;06:13Z / 09:12Z / 12:15Z 三轮已按同一判据登记过)。
> 闸 (iii) `FENCE_EXIT=3`(**brake,不是 fence**)⇒ **零发波,第二十七轮**。
> 收割零欠(`validation/` 592 对象与上一轮逐位相同),泄漏五条路径全零。
> **本轮实质交付物**:RULING 48 采纳率的**第二个读数**(新窗口,闭窗,零 AWS)——
> `4/4` 采纳、`MULTIPLIABLE N = 48`、**`APPROX` 仍是 1 条且仍是同一条流** ⇒
> 上一轮交棒 ④ 的那处形式漂移**不是一次性的**。

---

## 一、成本(当轮现跑,⛔ 下轮不得抄作前提)

- `AWS_SETUP_EXIT=0` —— 容器无 CLI,自装 `aws-cli/1.46.1 Python/3.11.15 botocore/1.43.62`;
  身份逐字 `arn:aws:iam::581436534924:user/dota2bot-agent` + `(restricted batch-runner user — correct)`。
- `COST_EXIT=0`(⛔ **未走管道**,重定向到文件后 `echo $?`)。
- MTD **`$90.782`** / `forecast 186.702` / `budget limit 100.0` /
  **`budget refreshed 2026-09-15T14:40:18Z`**;**headroom 对 `$90` = `$-0.782`**。

⭐ **快照冻结(本轮的第一件事实)**:上一轮(15:20Z)读到的是 `$90.782` / 戳 `14:40:18Z`;
本轮 `18:06Z` 读到的是**同一个 MTD、同一个戳**。两轮之间过了 **2.77h**,而**预算侧一次都没刷新**
(最近一次刷新在本轮开工前 **3.44h**)。
⇒ ⛔ **本轮不得写「零支出」也不得写「支出没涨」** —— 本轮**没有拿到任何新的支出测量**。
📌 这与 00:14Z / 03:06Z 那两轮同型(当时冻在 `$90.373` / 戳 `20:27:33Z`),
那两轮的登记逐字是「没测」不是「没涨」,本轮照抄该口径。
⛔ **空转速率不据本轮更新**:上一轮登记的 `$0.40–0.92/天`(n=3 个空转日)原样保留,
本轮**没有产生第四个点**(两个端点都来自同一张快照,差是恒等的 0,不是一次测量)。

**GH #801 四行整块照抄**(⛔ **未用 `COST_CONFIRM_AT=999` 自救,用默认值跑**):

```
   >= $35, but headroom to the $90.00 brake is $-0.782
   < $1.10 (cheapest wave) — CE confirmation SKIPPED, NOT passed (GH #801).
   Nothing can launch at this MTD, so confirming it buys nothing.
   Resumes by itself as soon as headroom >= $1.10.
```

⚠️ **这是 SKIP 不是 pass。** 欠条 `gh801_confirm_headroom_first_live_read` **仍未结清** ——
它要的是「**该买**」那条分支的第一次真读数,而本轮 headroom 为负,走的还是 SKIP 分支。
⭐ 结清判据钉在最后一行(`Resumes by itself ...`),**恢复是自动的,不需要谁记得**。

accrual probe 两行照抄:

```
   accrual probe (GH #779): alpha = 0 accruing instance(s) account-wide; beta = 0 wave record(s) after 2026-09-15T03:22:18Z
   stamp unknown (fresh container) -- alpha and beta are both zero -- a frozen stamp over an idle account opens no blind spot
```

**成本三段式(RULING 48)**:**零 EC2 / 零 CE / S3 读取 0 个对象(出网未计价)**。
⛔ 无 `s3 cp` / `s3 sync`、⛔ 无 `.dem` / `analysis.json` 下载。
⚠️ **诚实边界照登(与上一轮同一条)**:本轮发了 **1 次 `s3 ls --recursive`**(LIST,592 个 key 落在
单页 1000 之内 ⇒ 1 个 LIST 请求)。**LIST 是一条没有被 `<N>` 计量的 S3 请求** —— `<N>` 数的是对象,
不是请求;⛔ 不把它写成零,也⛔ 不冒充成 `<N>`。

## 二、闸 (iii)(`--planned 1.10`)

`FENCE_EXIT=3`。逐字:

```
actual (MTD)     : $90.782   <- re-read this run, never cached
pending waves    : $0.000   (launched, may not be in MTD yet)
planned          : $1.100
projected total  : $91.882
fence            : $100.00   <- lowest ACTUAL alert not yet crossed (Ruling 1)
brake            : $90.00   (owner's line, not derived here)
operative ceiling: $90.00   = min(fence, brake)
A launch at this instant would put MTD past the $90.00 BRAKE line.
WAVE_FENCE: THROTTLED (exit 3) -- brake, not fence. Stop and report to the owner; do not wait it out.
```

RULING 5/6/7 三条行照抄:

```
accrual scope    : 17 region(s) read [...]  <- COMPLETE: every region this account has enabled
wave accrual     : records after 2026-09-15T03:22:18Z (= 2026-09-15T14:40:18Z - 11.3h lag, clock from budget snapshot)
accrual check    : CERTIFIED (0 accruing instances account-wide, read this run) -- pending $0.000 is a reading, not a default.
```

⭐ **钟没有降级**(`clock from budget snapshot`,不是容器时钟)⇒ RULING 7 (甲) 的 epoch 修法本轮仍有效。
⚠️ 但**本轮的 epoch 与上一轮逐位相同**(`03:22:18Z`),因为它由冻结的那张快照戳减 11.3h 得出 ——
**快照冻住时,accrual 窗口也跟着冻住**。今天这不开盲区(alpha 与 beta 都是 0),
⛔ **但这个耦合要记一笔**:一旦真发了波,冻结的戳会让 `beta` 的起点比实际晚。
⛔ 未传任何 `--no-*` / `--pending` / `--director-crossing` ⇒ **无一行「这是 SKIP 不是 pass」**。

⚠️ 照登不藏:`$90`–`$100` 之间在 AWS 侧**仍可花**(`$100` 告警 `state=OK`),
**挡住它的是 owner 那条线,不是 AWS。**

## 三、本轮实质交付:RULING 48 采纳率的第二个读数(零 AWS)

上一轮交棒 ③ 把工具交到本轮手里(逐字「下一轮改 `--since` 即可重跑」)。照办,**闭窗跑**
(⛔ 开窗会把本台自己这一轮算进去,工具自己会打那条 LIMIT):

`python3 tools/agent/ruling48_adoption.py --since 20260915T141315Z --until 20260915T175959Z`
⇒ `R48_EXIT=3`(findings,不是没跑成)。逐字:

```
window          : [20260915T141315Z, 20260915T175959Z]  4 report(s)
  OK_N           N=0      iterations/reports/batch-desk/20260915T152022Z.md
  OK_N           N=48     (APPROX) iterations/reports/replay-check/20260915T155328Z.md
  OK_N           N=0      iterations/reports/director/20260915T162016Z.md
  OK_N           N=0      iterations/reports/strategy/20260915T162524Z.md

adoption        : 4/4 report(s) carry a three-part cost line
declared objects: 48 across 1 report(s) that read anything
prose zeros     : 0 (said zero, wrote no numeral)
approximate N   : 1
MULTIPLIABLE N  : 48  <- one number a later reader can multiply by a unit price
```

**三条读法,分开写(⛔ 不合并)**:

1. **采纳率保住了**:新窗口 `4/4`,与上一窗口的 `10/10` 同向。**`prose zeros = 0`** ——
   RULING 48 禁的那句(「零支出」冒充第三段)**在这个窗口里一次都没再出现**。
2. ⭐ **`APPROX` 不是一次性的**:上一轮交棒 ④ 点的那处形式漂移(录像组写**近似值**进分子,
   当时是 `约 200`),**本窗口又出现一次**(`N=48` 带 `(APPROX)`),**而且是同一条流**。
   ⇒ 这不再是「一轮的笔误」,是**一条流稳定的写法**。工具自己的判词逐字:
   `an approximate N still multiplies, but the exact count was in the caller's own hands at write time.`
   ⛔ **本台不代该流改写法、不判它违规**(RULING 48 只禁「零支出」,没禁近似值)——
   ⇒ **交回总监**:要不要把「精确计数」收紧成 RULING 48 的补充条款,是裁定不是本台的活。
3. ⛔ **这仍然只是分子。** 工具自己打的两条 LIMIT 原样有效:分母在 **11.3h 滞后带**背后,
   窗口还在带内就**没有分母**,而「没有分母」**不是「没有缺口」**。
   ⇒ 欠条 `s3_attribution_gap_remeasure_under_ruling48` **本轮仍 OWED**,
   上一轮交棒 ② 指出的结构性问题(「三轮」窗口 8.1h **短于**它自己分母的 11.3h 滞后带,
   ⇒ 每次都在分母存在之前关闭)**本轮原样成立**,⛔ 本台不自行改欠条判据(写入权归总监)。

## 四、收割:零欠(⛔ 不是「漏收」)

`S3VAL_EXIT=0`。`validation/` **592 个对象**,与上一轮基线**逐位相同**。
**按 `LastModified` 排序**(⛔ 不按 key 名 —— 那正是 06:13Z 查出的「九轮用错的量」)的最新对象:

```
2026-09-12 10:18:26      15187 validation/lf_rescue+27ids-d177b87026dd_20260912_1018_run.log
```

⇒ 距今 **3.3 天**无新对象 ⇒ **零新对象、零下载,`recover_verdict.py` 未跑 —— 没得收,不是漏收。**

⚠️ **本轮未列 `soak/` 前缀,并且这是有意的,照登**:该前缀 **72,079 个对象** ⇒ 一次
`ls --recursive` 约 **73 个 LIST 请求**,而**它不可能有新数据** —— 连续 27 轮零发波,
且本轮 accrual probe 独立给出 `beta = 0 wave record(s)`。
⛔ **这是一次省下的请求,不是一次做过的检查**:如果哪一轮 `beta ≠ 0`,`soak/` 必须列。

## 五、泄漏:五条独立路径全零(⛔ 不合并成一句)

1. `check_costs.sh` 自带 `running/pending instances` 区块 —— **空**;
2. `describe-instances` 五态(pending/running/stopping/stopped/shutting-down)、**不加 tag 过滤**
   ⇒ `INST_EXIT=0`,**`LINES=0`**;
3. 账户级 **17 区 `COMPLETE`**,`alpha = 0 accruing instance(s) account-wide`;
4. AMI 仍 `ami-0a990a26d89c66547 / dota2bot-batch-20260718` **一张**(常设成本,非泄漏);
5. 闸的 `pending waves : $0.000`,且逐字 `CERTIFIED ... is a reading, not a default`。

⛔ **不合并** —— ①②③ 问「现在有没有机器」,④ 问「有没有常设资产」,⑤ 问「有没有已发未计的波」。
⚠️ 本轮**未**买账单侧那第六条(`EC2 - Compute` 逐日 = 0 那类),理由同 §一 的 GH #801 四行:
headroom 为负 ⇒ 没有可确认的发波决策 ⇒ 那 `$0.01` 买到的是零。

## 六、通知判据:本轮**不推**

上一轮(15:20Z)推了,理由逐字是「MTD 与预算戳**两量都动了** ⇒ 判据成立」。
**本轮两量逐位相同**(`$90.782` / `14:40:18Z`)⇒ **零新支出测量** ⇒ **按同一条判据不推**
(06:13Z / 09:12Z / 12:15Z 三轮的先例)。
⭐ **这是按写死的字面判据停手,不是按本台的判断** —— owner 手机上 3 小时前刚收到同一条事实,
再推一条内容相同的通知**只是噪声**。

⚠️ **月末外推照上一轮口径,区间不变也不收窄**:`$90.782 + 15 天 × $0.40–0.92/天 − $0.8`
⇒ 9/30 的 MTD 落在 **`$96.0 – $103.8`**(`dem21/` 09-22 自动过期省 `~$0.10/天 × ~8 天 ≈ $0.8`
已扣进**两端**)。⚠️ **空转日样本仍是 n=3**(本轮没加点,见 §一)
⇒ **区间宽是真实的不确定性不是排版**;⛔ 不外推到十月、⛔ 不改任何常数、
⛔ 不主张 `$100` 一定会/不会被越。

## 七、交棒

0. ⭐⭐ **总监/协同组 —— GH #839 `[harness]`(本轮新开)**:`py_gate` manifest 漂了 7/141、
   超 slack 5,**漂出去的两个是今天两条裁定自己的棘轮**(RULING 48 的 `test_ruling48_adoption.py`、
   RULING 57 的 `test_strata_paired_arm.py`)⇒ **立法当天它就不设防**。
   处方是工具自己打的(`python3 tools/agent/py_gate_measure.py`,逐字 `do not raise the slack`);
   ⭐ issue 里主张的那一半不是重测,是**「新落地的 python 测试如何默认进 manifest」**。
   ⛔ 本台未代修、未动 manifest、未调 slack。
1. ⭐⭐ **owner —— 刹车第二十七轮持有,GH #779 第十八轮零表态。** 本轮**无新事实**
   (快照冻结)⇒ **按判据未推**。球仍在 owner:**提高预算线,还是接受本月剩余时间批测全停。**
   ⛔ 本台无权自裁。
2. ⭐⭐ **总监 —— `s3_attribution_gap_remeasure_under_ruling48` 的触发判据请重裁**(上一轮交棒 ②
   逐字保留,本轮未获回应)。现行「三轮」窗口(8.1h)**短于它自己分母的滞后带(11.3h)**
   ⇒ 每次都在分母存在之前关闭,与流量无关。建议改成**日期判据**:
   第一个干净的裁定后整日 = **09-16**,最早可读 **~09-17T11:20Z**。⛔ 本台不自行改欠条。
3. ⭐ **总监 —— §三 的第二个分子读数请收**:闭窗 `[20260915T141315Z, 20260915T175959Z]`,
   `4/4` 采纳、`MULTIPLIABLE N = 48`、`prose zeros = 0`。
   **与上一窗口(`10/10` / `N=330`)合起来是两个独立窗口的采纳率,不是一个数的两次抄写。**
4. ⭐ **总监 —— §三.2 那处形式漂移现在有第二例,且是同一条流**(录像组的近似 `<N>`:
   上窗 `约 200`,本窗 `48 (APPROX)`)。**一次是笔误,两次是写法** ⇒
   请点一句:RULING 48 要不要补「`<N>` 必须精确计数」。⛔ 本台不代各流改写法、不判违规。
5. **总监 —— 上一轮交棒 ④⑤⑥ 本轮未获回应,逐字保留**,⛔ 本台不自行退休任何一行。
6. **下一轮本台**:(a) 开工第一条命令**重定向裸读**(⛔ 无管道、⛔ 无 `timeout`,一次到位)——
   **本轮第五轮仍未做到**,见 §八;(b) 刹车解除(headroom ≥ `$1.10`)时按规格发 P4.1 标尺波、
   **收割必带 `--ledger`**,并抄 GH #801 四行**恢复后**的样子(那是欠条
   `gh801_confirm_headroom_first_live_read` 的结清判据);(c) 若某轮 `beta ≠ 0`,**`soak/` 必须列**(§四)。

## 八、铁律 9 / 开工自检 / 闸 / ⛔ 本轮自捉

- **铁律 9**:P4.1 upstream 标尺波**连续第二十二轮欠**,⛔ **唯一阻因是闸 (iii) `exit 3`(预算)**,
  不是本台未认领也不是排期。
- ⛔⛔ **本轮自捉(方向对本台不利,照登)——上一轮交棒 ⑦ (a) 第五次未做到,而且是两次不同的犯法**:
  本轮对 `routine_selfcheck.sh` 的**第一次**调用走了 `| tail -40`(管道),脚本按
  evidence-discipline 3 **拒绝**并逐字打 `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.`
  (自陈「**已复发 5 次,每次都是当轮第一条命令**」);**第二次**改成重定向但加了 `timeout 400`,
  脚本**再次拒绝**,逐字 `REFUSED: an ancestor process is `timeout`; exit 2, nothing checked.`
  (自陈「**已连续 3 轮复发**」)。**第三次** `nohup ... > /tmp/sc.log 2>&1 &` 才真正跑起来。
  📌 **两次拒绝都是 exit 2 = 「没跑成」不是「没发现」** —— ⛔ 那两次**什么都没检查**,
  ⛔ 不得记成两次干净的自检。**这条拒绝腿自己在工作(它挡住了一个假读数);
  不工作的是本台的第一条命令。**
- **开工自检(跑完,腿级判词逐字)**:

```
legs run              : 13
FINDINGS (exit 3)     : cadence queue-rulings owed-executions lua-coverage trunk-red(python) trunk-red(lua)
UNCERTIFIABLE (exit 2): none
selfcheck worst exit: 3
```

  ⭐⭐ **本轮与上一轮在同一个位置分叉,而且分叉方向是「变红」不是「变绿」**:
  15:20Z 那轮 `trunk-red(python)` 是 **`UNCERTIFIABLE (exit 2)`**(腿没跑成),
  本轮它是 **`FINDINGS (exit 3)`** —— 而且本轮 `UNCERTIFIABLE (exit 2): none`。
  ⇒ 上一轮逐字写的「**读到 2 的那一轮,trunk 的那一侧这轮没人看过**」在本轮**被看过了**,
  看到的是**一条真红**:`137 passed, 1 failed, 3 uncertifiable`,失败的是 **`tests/test_py_gate.py`**。
- ⭐⭐ **那条红的内容是本轮第二件实质发现,已开 GH #839 `[harness]`**。检查 5f 逐字:
  `the manifest has drifted from the suite -- 7 of 141 on-disk tests are in no row, over the slack of 5,`
  `so each one runs fail-open on every push with its cost recorded nowhere.`
  `Re-measure (python3 tools/agent/py_gate_measure.py); do not raise the slack.`
  **Uncovered 名单里有两个是今天两条裁定自己的棘轮**:`tests/test_ruling48_adoption.py`
  (RULING 48 自陈信道那半 —— **本轮 §三 用的正是它的被测对象**)与
  `tests/test_strata_paired_arm.py`(RULING 57 验收 1 的棘轮)
  ⇒ **两条今天刚立的裁定,它们的棘轮现在都在 push 闸里 fail-open**。
  📌 这是 GH #624 立案形状**上移了一层**:#624 说「红由下一个开工的组发现」,
  本条说「**新落的棘轮默认不进闸,于是立法当天它就不设防**」;
  `slack = 5` 让**前五次漂移静默**,第六次才举手 ⇒ **举手的时刻与漂移的作者无关**。
  ⛔ 本台**未复现、未归因到某一次落地、未代修、未动 manifest、未调 slack**(章程:本台不写 harness 代码)。
  ⚠️ **⛔ 连带的诚实限制,写在本台自己身上**:`tests/test_ruling48_adoption.py` 正是 §三 那个工具的棘轮,
  而它**当前没有任何自动读者** ⇒ §三 的读数**是工具自陈的,本轮没有第二个东西给它背书**。
  ⛔ 不因此收回 §三,但也⛔ 不把它说成「经过闸的」。
- ⚠️ **`trunk-red(lua)` 是另一件事,不并进 GH #839**:仍是 `tests/test_fieldsip_atom_pricing.lua`
  (语料走查从 1021 → 1039 帧,四份普查要么一起 re-baseline 要么都不动)。
  自检自己写了 `Whether main is red too is NOT established by this line: re-run after git stash`
  ⇒ ⛔ 本台**不主张 main 也红**。
- **其余 FINDINGS 照登不归因**:`cadence` / `queue-rulings` / `owed-executions` / `lua-coverage`
  (后者逐字 `UNCOVERED SET GREW -- 3 file(s)`:`test_axe_cull_blade_mail.lua` /
  `test_dusttower_dive_guard.lua` / `test_fieldsip_transfer_receiving_site.lua`,
  另有 `NOW COVERED tests/test_fieldsip_atom_pricing.lua` 一条离开该集合)。
  ⛔ 本台未复现、不归因、不代修、不调成员资格。
- ⚠️ **`NOT RUN (inside a leg)` 三条照抄**:`tests/test_lua_gate.py` /
  `tests/test_luacheck_gate_soakswitch.py` / `tests/test_selfcheck_lua_leg.py`
  (逐字 `did NOT run -- this is not a pass and not a failure`)。
  py 腿内部另有 **9 条 `UNCERTIFIABLE`**,自带逐字 `9 check(s) did not run; this is NOT a pass.`

## 九、push 与闸读数

(push 时回填 —— 三条腿逐行照抄,⛔ 缺席的那行也要写出来。)

## 十、token 用量

```
TOKENS total_in=11,581,830 out=41,714 turns=89
```

⚠️ 这是**到统计时刻为止**的数字,统计后的收尾回合(两次 push + 本行)不计入。
⚠️ **`turns=89` 与 `total_in` 明显高于本台近轮的无波轮**,归因照登、不粉饰:
**大部分不是工作,是等** —— 开工自检的 python 腿在本容器上跑了约 20 分钟墙钟,
而本台在等它的过程中**反复轮询**(`pgrep` / `tail`),每次轮询都是一个完整回合。
📌 **下一轮本台的可执行改法**:等长任务用**一次阻塞**(`tail --pid=<PID> -f /dev/null`)
而不是 N 次轮询 —— 本轮最后两次等待正是这么做的,**那两次各只花一个回合**。
