# batch-desk 2026-09-01T00:35Z — 收割轮:W32 = 48-id 家族第一粒读数;**不发波**(闸 (i) 未解锁)+ 一个新的自检假红

**一句话**:W32 四台按需全部自毁干净、**231 局零丢失**收割完毕,48-id 家族首读
**gpm 均值 +35.12 / comps_better 3-4 / deaths −0.15**、`suggested: promote`;
本轮 **不发波**(例行闸 (i) 要到 `03:30:48Z` 才解锁);
**本轮两条**:① 自检的 `trunk-red(python)` 腿在**同一棵干净树上 20 分钟内给出两个不同答案**
(70/1 FAIL `test_rc_wrapper.py` → exit 3 ⇔ 71/0 → exit 2)—— ⚠️ **这不是新发现,是 GH #364 / #339 的又一次独立复发**
(发报前搜索命中,已按此改写,见 §5);② **按需单波常数 `~$2.15` 被 W32 实测顶到 `$2.22–2.30`**,失效方向是危险的那一侧。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1` — **worst exit 3**。
(第一次调用用了管道,脚本**自己拒跑**并打 `REFUSED: stdout is a pipe; exit 2, nothing checked`
—— 证据纪律 3 的那道护栏正常工作,已改成重定向重跑。)

`FINDINGS (exit 3) : cadence queue-rulings trunk-red(python)`;`UNCERTIFIABLE (exit 2): none`。

- **stable 锚点**:`stable-v1` / `stable-v2` 两个 **EXISTS / PINNED / SHIPPED 全 ok**。
- **过期 admission wait**:6 份章程、61 个已裁 id,**零过期**。
- **快 Lua 检测器**:63 个 tagged 文件、**0 失败**(快子集,不是全套)。
- **`trunk-red(python)`** → 见 §5,**本轮认定为假红**。

## 1. 成本与泄漏

| 项 | 读数 |
|---|---|
| `budgets` MTD | **$75.988**(limit $100,forecast $77.28)—— ⚠️ **这是八月的数**,`LastUpdatedTime` = `2026-08-31T21:01:57Z`,**尚未滚进九月** |
| CE 九月 MTD | **$0**(`2026-09-01` 行为空数组 —— 滞后,不是"零花费"的证据) |
| 开工 running/pending | **空** |
| 收尾 `--leak-only` | **`RC_EXIT=0`**,running/pending **空**,零孤儿 |
| 常驻成本 | 一个 AMI(`ami-0a990a26d89c66547`) |

**⭐ 本轮跨月**。九月的围栏按章程「围栏 = 下一个尚未跨过的 owner 可见 Budget ACTUAL 告警档」
**重置回 `$50`**(八月用的 `$80` 是因为 `$50` 在八月已跨)。刹车线 `$90`、owner 批准线 `$100` 不动。

**八月是否跨过 $80:按现有证据判"多半没有,但不能定论"。** 21:01Z 快照 `$75.988` + W32 实测
`$2.22–2.30` ≈ **`$78.2–78.3`**,低于 `$80`;但 `budgets` 对 EC2 有 4.3–11.3h 滞后(§甲),
21:01Z 那张快照本身可能还没含全 W31。**下一轮 `budgets` 滚进九月时,八月的终值会定格,届时补一行确认**;
若确实跨了 `$80`,按配套义务补写「本轮跨过 $80,原因是 X 波 × Y 元」那一行。

### 1.1 W32 单波成本 —— **`describe-instances` 这条路本轮已经走不通了**

上一轮交棒写的是「**趁 terminated 实例还没老化出去,早点读**」。本轮 `00:1xZ` 触发,
距 W32 自关(~22:2xZ)**2h 左右**,`describe-instances --filters state=terminated,shutting-down`
返回 **`[]`** —— 四台**全部老化出 API**。CE 那条路同时也走不通(见下)。
⇒ 改用**两条免费的旁证**:

**(甲) S3 对象时戳定界**(`LaunchTime` 来自上一轮报告记录的 `21:30:48–21:31:01Z`,
终点取该 run 前缀**最后一个对象**的写入时刻):

| run | 起 | 末对象 | 机时 |
|---|---|---|---|
| `…_3bf33d` | 21:30:48 | 22:23:28 | 0.878 h |
| `…_93546a` | 21:30:52 | 22:23:39 | 0.880 h |
| `…_d72e7e` | 21:30:57 | 22:23:34 | 0.877 h |
| `…_858e13` | 21:31:01 | 22:10:56 | 0.665 h |
| | | **合计** | **3.300 h**(+ 收尾上传/terminate 的尾巴,~3.37 h) |

按 `c6i.4xlarge` 按需牌价 `$0.673/h` ⇒ **compute $2.22–2.27**,加 EBS/VPC 分摊 ⇒ **本波 ≈ $2.22–2.30**。

**(乙) VPC 公网 IPv4 行反解(章程 §乙 指定的独立法)**:08-31 全天 VPC `$0.03492778`
÷ `$0.005/h` = **6.986 机时**,当天共 8 台(W31 四台 spot + W32 四台按需)。
减去 W32 的 3.300 h ⇒ W31 ≈ **3.69 h**(0.92 h/台),与 W29 实测 0.856 h/台 同量级 ⇒ **一致**。
⚠️ **这只是一致性检查不是独立证明**:W31 的机时是我从 VPC 总数**减出来**的,不是量出来的。

**(丙) 为什么 CE 这轮对不上账**:08-31 逐日 CE(`$0.01`,本轮唯一一次付费读数)给
EC2-Compute **`$1.4893`**;而 (甲)(乙) 两法各自算出的当天真实机时对应
W31 spot 3.69h×~$0.24 + W32 按需 3.30h×$0.673 = **`$3.11`**。
**CE 只含了不到一半** ⇒ 这正是章程 §甲 记的「EC2 4.3–11.3h 滞后」,**本轮又实测到一次**。
⚠️ **本轮因此没有动用 §乙 禁止的那个手法**(「MTD 增量对得上波费 ⇒ 该波已计入」),
结论走的是 (甲) 的定界 + (乙) 的一致性检查。

### 1.2 ⭐ 按需单波常数 `~$2.15` 被本波实测顶到 `$2.22–2.30`

W32 = **0.825 h/台**(3.300/4),而章程里那个 `~$2.15` 是 W10–W14 五次实测的
**0.745 h/台**。本波高 **+11%**,单波价高 **+3% 到 +7%**。

**方向是危险的那一侧**:围栏算术每记一条按需波就**少记 $0.07–0.15**,与「滞后让 MTD 系统性偏低」
**同向叠加** —— 与章程给 `$0.90` 写自动失效条款时点名的那个形状**逐字同族**。
⚠️ **但 `$0.90` 的自动失效条款按其字面不适用于本波**(那是 spot 常数,本波是按需)。
⇒ **本台按同一立法目的点名,请总监下一轮裁 `~$2.15` 要不要抬**(建议档:`$2.30`,
理由与 `$0.90` 取整数档同构 —— 覆盖全部账单侧实测点,而不是压在区间最低点)。
**本台不自行改常数。**

## 2. 收割:W32 = 48-id 家族第一粒读数

- 语料:`soak/spot_20260831_2130{48,52,57}_1_85bcbeb7…` + `…_213101_…`,四个 run 各自一个子目录
  (`recover_verdict.py` 的 fix (A) 递归走,**没有触发 R2 并池**,`pooled_basename_dirs_overridden: 0`)。
- **`files_seen 231 / games_loaded 231 / unparseable 0 / unfinished 0`**,`source_dirs 4`。
  其中 **207 局**带 48-id 的 `mirror:` 戳并计分,**24 局** `script_version` 是裸 commit `85bcbeb7`
  (无 `mirror:` 戳的排空局),按 `STAMP_SEED` 正则**自动出集**——**不是丢数据**。
- **`min_arm_depth: 8`,`thin_arm_seeds: []`,`thin_arm_winrate_seeds: []`** ——
  **四粒种子全部计分,零排除**(GH #269 硬门本波一个都没挡)。
  **未使用 `--min-arm-depth` 降门,故无「这是 SKIP 不是 pass」那一行;
  未使用 `--allow-pooled-basenames` / `--allow-unparseable`。**

### 2.1 四粒种子(`arm_depth` 与四量)

| seed | ab | ba | `arm_depth` | gpm | xpm | deaths | last_hits | scored |
|---|---|---|---|---|---|---|---|---|
| 2604 | 37 | 17 | 23.30 | **−52.50** | −23.51 | +0.36 | −3.73 | 54 |
| 2624 | 42 | 16 | 23.17 | **+59.18** | +30.94 | −0.48 | +2.69 | 58 |
| 2737 | 36 | 17 | 23.09 | **+11.50** | +8.31 | −0.16 | −0.63 | 53 |
| 2970 | 30 | 12 | 17.14 | **+122.30** | +46.90 | −0.33 | +0.57 | 42 |

### 2.2 波级读数

| 量 | `mean` | `comps_better` |
|---|---|---|
| gpm | **+35.12** | **3/4** |
| xpm | **+15.66** | **3/4** |
| deaths | **−0.15**(更少死 = 更好) | **3/4** |
| last_hits | −0.28 | 2/4 |
| winrate | ⛔ 见 §2.4 | ⛔ 见 §2.4 |

`suggested: "promote"`。**照旧:`suggested` 只是提示,promote 走三条件,裁定权在总监。**

**跨种子离散度(§CO.4 允许的定量描述)**:gpm SD **73.97**、SE **36.98**;xpm SD 30.54、SE 15.27。
⚠️ **按 §CO.4 本台不用它写「不够裁」** —— 那句话问的是效应量是否显著异于零,正是铁律 2
(owner 2026-08-01)废掉的那一步。按铁律 2(b) 的**粗粒度**尺子读:
**gpm +35.12 / xpm +15.66 / deaths −0.15 / comps_better 三个量各 3-4,没有任何一项负面**
⇒ **条件 (b) 本波买到了**。卡住 48-id 家族 promote 的**仍然是条件 (a)**(录像确认真的执行且行为正确)。

### 2.3 分层披露(计量四条 (i-a))

**两个分层的读数**(不是局数)——`strata`,由 `recover_verdict.py` 自己打,**未手算**((i-d)):

| 量 | `ab` | `ba` | `side` | `sign_flip` | `side_gt_arm` |
|---|---|---|---|---|---|
| gpm | +133.46 | −63.22 | +98.34 | **true** | 4/4 |
| xpm | +104.17 | −72.85 | +88.51 | **true** | 4/4 |
| deaths | −1.86 | +1.56 | −1.71 | **true** | 4/4 |
| last_hits | +2.80 | −3.35 | +3.07 | **true** | 4/4 |

**四量全部反号,`side_gt_arm` 全 4/4。按 (i-c) 这不是否决理由** —— 经济四量是每粒
50/50 swap-average 的**侧偏已消除**估计量,`反号 ⟺ |side| > |arm|` 是恒等式不是诊断,
它只说这一波抽到的阵容侧偏(≈ +98 gpm)比效应(+35)大,**对 arm 测得多准零信息**。
管精度的是 §2.2 那三行(`mean` + `comps_better` + #269 深度门),**三行都在**。
**登记完毕**(这是 (i-a) 的义务,不是 (i-b) 的判决)。

### 2.4 ⛔ `winrate` 通道:**DEGENERATE**(GH #352 / §CT)

工具自己打的横幅,原文抄录:

> `GH #352 winrate channel DEGENERATE: minority side won 10 of 231 finished games (share 0.0433 < 0.20). The winrate below is bounded to 0.5 +/- 0.0749 and MUST NOT be cited as rule 2(b) support until the channel recovers.`

- **`winrate_channel = "DEGENERATE"`**,**`mean.winrate_headroom = 0.0749`**(§CT 要求照抄的两个数)。
- `winrate_side_census` = radiant **10** / dire **221**,`winrate_minority_side_share` **0.0433**。
- 逐粒 headroom:2604 `0.1176` / 2624 `0.0938` / 2737 `0.0882` / **2970 `0.0000`**
  —— **2970 那粒是 42:0 的 dire 横扫,它的 `winrate 0.500` 在算术上被逼出来,与候选做什么无关。**
- ⇒ **本报告不把 `winrate` 与 `comps_better winrate` 写成读数。** 通道恢复的判据是 `== "RECOVERED"`。
- ⚠️ **不许读反**:经济四量那条路一个字都没坏,§2.2 的结论不受影响。

### 2.5 ⭐ GH #108/#352 桶名 bug 的**修复确认波**

`winrate_independent_of_gold` 本波打 **`"231/231 games"`**,`winner_by` = `{"engine_natural": 231}`。
改名前读者读 `engine`、真桶是 `engine_natural`,W31 因此打 `0/222` 而真值 `222/222`。
**W32 是章程说的"下一波",本波确认该字段打真值** ⇒ **本台 08-29T09:13Z 立的逐波复发登记,自本轮起停止。**

## 3. 发波决策:**不发**

| 闸 | 判定 |
|---|---|
| (a) `queue.json` pending | pending 全是 `ROUTED_ARCHIVE_SCAN`(hero-1/2/3/… 共十条,**零 EC2、零新波次**,归档扫描)⇒ **没有一条是发波请求** |
| (i) 距上一次例行波次启动 ≥ 6h | 上波 `2026-08-31T21:30:48Z` ⇒ 解锁于 **`2026-09-01T03:30:48Z`**;本轮 `00:2xZ`,**差约 3h05m** ⇒ **FAIL** |
| (ii) 有新东西可测 | 未评估(被 (i) 挡住,不必评估) |
| (iii) 成本 | 九月围栏 `$50`,MTD ≈ $0 ⇒ 余量充足,**非本轮的阻塞项** |

⇒ **本轮是纯收割轮,零 AWS 支出**(唯一支出是 §1.1(丙) 那次 `$0.01` 的 CE 复核)。
**这与上一轮的交棒预期一致**(上轮已写「下一轮本台 = 收割 W32,多半只收割不发波」)。

## 4. queue.json

**未改动**。W32 走的是章程 4(b) 例行波次路径,不对应任何 queue 请求;
十条 `ROUTED_ARCHIVE_SCAN` 是归档扫描,本轮没有执行其中任何一条(本轮工作单元已满),
它们的 status 保持 `pending` 不动。

## 5. ⭐⭐ 新发现:自检 `trunk-red(python)` 腿的**假红**

**同一棵树、同一个容器、相隔约 20 分钟,两个不同答案:**

| 何时 | 怎么跑 | 读数 | 退出码 |
|---|---|---|---|
| `00:1xZ` 自检内 | `routine_selfcheck.sh` 的 python 腿 | **70 passed, 1 failed, 1 uncertifiable**;`FAIL tests/test_rc_wrapper.py` | 该腿 **note 3** ⇒ 自检 worst **3**,横幅 **TRUNK RED** |
| `00:3xZ` 直接跑 | `bash tests/run_py_tests.sh` | **71 passed, 0 failed, 1 uncertifiable**;`tests/test_rc_wrapper.py` **PASS** | **2**(唯一非通过是 `test_selfcheck_lua_leg.py` UNCERTIFIABLE) |

**树的状态已核实**:`git status --short` **空**,`HEAD` = `cc6bf220d4a88e536c530acd287da408b950e840`,
`git ls-remote origin main` = **同一个 sha** ⇒ **工作树 == HEAD == origin/main**,
所以「re-run after `git stash`」这条自检自己给的复核路径在本例里**无事可做**,两次读的是**同一棵树**。

**`test_rc_wrapper.py` 单独重跑 3 次:3/3 EXIT=0,34 个 ok,零 FAIL。**
`tests/run_py_tests.sh` 里**没有超时机制**(grep `timeout|budget|SECS` 无命中),
⇒ 归因指向**负载/时序**:该文件测的是 `rc.sh` 的信号与长输出行为(含 `SIGTERM: exits 143`),
而自检那一趟同时在跑 120s 预算的 lua 腿、且整趟 **超过 600s**(被本会话的 600s 上限移到后台)。

**⚠️ 先更正一处:这条本台起初写成"新开 issue",发表前搜索发现它早已立案 —— GH #364**
(`[harness] tests/test_rc_wrapper.py 是 flaky 的:本轮在开工自检里制造了一次假 TRUNK RED(裸跑 7:1)`,
2026-08-31T12:30Z 由本台自己立)**与 GH #339**(`开工自检可以打出一个假的 TRUNK RED,而 GH #243 的守卫拦不到这一形`)。
**下面这三点因此不是立案理由,是把本轮读数摆进已有案子的位置**;发表走**追评 #364**,不新开。

**为什么这次的读数仍然值得单独交出去**:

1. **它落在 GH #243 修好的那条分支的旁边,不在里面。** #243 把 runner 的 **exit 2**
   (读不到输入)改判成 `UNCERTIFIABLE`,理由逐字写在 `routine_selfcheck.sh:322-327` 的注释里,
   连举的例子都同形(「one round read 39/2 where two later runs of the same tree read 41/0」)。
   本例 runner 给的是**真 `FAIL`**,走的是 `else` 分支 ⇒ **直接 `note 3` + `TRUNK RED`**,#243 的护栏够不着。
2. **失效方向是危险的那一侧。** 自检是**每个 stream 开工第一件事**,而
   `DECISIONS_NEEDED.md` §14 已记过「横幅打对了 TRUNK RED、三个 stream 照样落在上面,
   其中一个还发了付费波」。**一条会假红的横幅,训练的正是"横幅可以不信"** ——
   它不会让谁停下,它会让下一次**真红**没人停。
3. 它同时污染 **GH #267 的 exit 归因行**:本轮 `FINDINGS (exit 3): cadence queue-rulings trunk-red(python)`
   里那第三项是假的,而 #267 立案的原因恰恰是「手工归因错了 22 小时」。

**本台不改 harness**(章程 4(c) 同精神:批测台不自己动 harness)。
⇒ **新开 `[harness]` issue**,见 §7。

**顺带 —— GH #358 的第六个形状点**:`test_selfcheck_lua_leg.py` 本轮 **UNCERTIFIABLE**,
`NOTE 5a0 cost: ? file(s) in 120.1s (budget 120s)` —— **超预算 0.1s**,四条子断言 5a0/5a/5a2/5b 全没跑。
`?` 那个文件数本身也没打出来。

## 6. 铁律 6(push 前)

- `bots/` / `game/` **一行未改**(本轮改动全在 `iterations/` 下:一份报告 + 章程"当前状态")。
- 静态半:`bash tools/agent/luacheck_gate.sh` 由 `.githooks/pre-push` 在 push 时自跑;
  **未使用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」那一行需要抄录**。
- 动态半(`lua5.1 tests/run_tests.lua`,~100min,GH #124):**未跑,且本报告不声称它跑过**。
- **顺序条款(GH #290)**:本报告与 issue 草稿**先 push 再发表**,草稿过 `claim_precheck.sh`。

## 7. 交棒

1. **⭐⭐⭐ 总监 —— 48-id 家族第一粒读数到手(W32)。**
   `gpm +35.12 / xpm +15.66 / deaths −0.15`、`comps_better` 三个量 3-4、四粒种子零排除、
   `suggested: promote`、**条件 (b) 按铁律 2 粗粒度尺子已买到**。
   **卡的仍是条件 (a)** —— 需要录像组确认这 48 个 id 在实际对局里真的执行且行为正确。
   连带 **GH #363**(并池单位)第五轮点名:47-id 家族已冻结在 8 粒,48-id 现有 4 粒,
   **两族要不要并池,现在是一个可以当场裁的问题**(不再需要预测未来)。
2. **⭐⭐⭐ 追评 GH #364(不新开)—— `test_rc_wrapper.py` 假红的第二次独立复发**(§5)。
   **关键增量:同一个文件,相隔约 12 小时,在两棵不同的树上(12:30Z 那棵 / 本轮 `cc6bf22`)、
   两个不同的容器里,各自在开工自检那一趟里挂一次、裸跑全绿** ⇒
   #364 原文诚实边界写的「它证明的是这个红不可复现,不是这个测试在任何机器上都稳」,
   本轮把它推进了一格:**不稳的是 `test_rc_wrapper.py` 这一个文件,不是随机某个文件**。
   本轮读数:70/1 ⇔ 71/0;该文件单跑 **3/3 绿**;`run_py_tests.sh` **无超时机制**(grep 无命中)。
   **同时确认 #364 建议 1 仍未落地**:逐条输出还是在自检那一层被 `grep -E '^(FAIL|failed:|[0-9]+ passed)'` 滤掉,
   本轮**同样没能定位到具体哪条 check 挂了** —— 这已是该建议第二次因同一原因被需要。
   连带 **GH #339** 第 N 轮点名(exit 3 分支,#243 的护栏够不着)。
3. **⭐⭐ 总监 —— 按需单波常数 `~$2.15` 请重裁**(§1.2)。W32 实测 `$2.22–2.30`(0.825 h/台 vs 常数的 0.745),
   高估方向与 MTD 滞后**同向叠加**。建议 `$2.30`,与 `$0.90` 取整数档同构。
   ⚠️ `$0.90` 的自动失效条款按字面只管 spot,**本条是本台按同一立法目的主动点名**,不是该条款自动触发。
4. **⭐⭐ 新开 `[harness]`(已搜索,零命中,确为新案)—— 按需波的成本在「实例老化」与「CE 滞后」之间有一个读不到的窗口**(§1.1)。
   上一轮的交棒「趁 terminated 实例还没老化出去,早点读」**对一个 2 小时一触发的 Routine 不可执行**:
   本轮距波末 ~2h,`describe-instances` 已返回 `[]`,而 CE 同时只含不到一半。
   spot 波有 SIR `create→update` 兜底,**按需波没有任何兜底**。
   建议:`spot_run.sh` / watchdog 在自关前把 `LaunchTime` + 终止时刻写进该 run 的 S3 前缀,
   **让单波成本离线可重建**;零 EC2 增量。
5. **⭐⭐ 九月跨月已发生**(§1):围栏重置回 **`$50`**,`budgets` 尚未滚(仍读八月的 `$75.988`)。
   **下一轮补一行确认八月终值是否跨过 `$80`**(现估 `$78.2–78.3`,多半没跨,但滞后使其不能定论)。
6. **⭐ GH #352 / §CT 的修复确认波已到**(§2.5):`winrate_independent_of_gold` 打真值 `231/231`。
   本台的逐波复发登记**停止**。同时本波 `winrate_channel = DEGENERATE`,§CT 的两个数已照抄。
7. **⭐ GH #358 第六个形状点**(§5 末):自检 lua 腿 **120.1s / 预算 120s**,超 0.1s,四条子断言全没跑。
8. **⭐ 存量**(本轮只登记不另发评论):#371(上轮新开,`spot_run.sh` 失败时 exit 0)待裁;
   #218 第三十九轮;#367 待裁;#339 见上述 2;#295/#252/#256/#282 建议关闭;#329/#321 待裁;
   `UNKNOWN STATUS` 4 条(#317);`strategy-5b` 第六轮 / `strategy-27` 待裁;
   #313/#290/#291/#298/#299/#349/#350(#355)照旧;#207 停在第四十波 armed;
   #285 第十六轮;#308 第十一轮。**#364 本轮不是「登记」而是追评**(见上述 2)。
   自检报的 `cadence` 与 `queue-rulings` 两项**本轮未逐条展开**(工作单元已满,非新增形状)。

## 8. 铁律 11

本轮**未触发**任何 `requires approval` / 权限对话框;**无空转等待**。
GitHub MCP 若在发表 issue 时被拦,按铁律 11 立刻放弃并把 issue 正文留在 §5/§7 里,下轮补发 —— **发表结果见本节收尾追加行**。

## 9. 下一轮本台

**发波轮**:闸 (i) 于 `2026-09-01T03:30:48Z` 解锁。届时按 (ii) 判「有没有新东西可测」——
注意 W32 的 armed 串是 48-id,若 `test_set.md` 与 `bots/` 自 `85bcbeb7` 起无变更,
(ii) 第一肢不成立,要看第二肢(当前 tree+测试集累计种子数 < 8:48-id 家族现有 **4 粒** ⇒ **成立**)。
**选种前先 `python3 tools/batch_test/soak/seed_roster_index.py --build`**(增量索引;不 build 会把
W32 刚烧掉的 2604/2624/2737/2970 当成"从未用过"献回来)。
市场类型按 owner GH #158:**spot 优先**,没容量才降级按需;围栏取值 spot `$0.90` / 按需 `~$2.15`
(若总监按 §1.2 改了常数,用新的)。

## 10. 本轮 token 用量

```
TOKENS total_in=3,230,970 out=37,267 turns=34
```
(到统计时刻为止;统计后的收尾回合不计入。)

## 11. 铁律 6 收尾读数

```
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
luacheck bots game: 0 warnings
installed luacheck (it was missing; apt package lua-check, bounded)
```
**未使用 `RULE6_BYPASS`,故无「SKIPPED, not passed」行。** 动态半(#124)未跑,本报告不声称它跑过。
