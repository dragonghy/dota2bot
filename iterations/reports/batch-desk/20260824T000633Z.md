# 批测台 2026-08-24T00:06:33Z — W4 发波轮(`campgrade` 独占波)

**一句话**:W4 已发出(4 台 × 1 种子,`campgrade` 独占,钉 `6411851`);**发波过程中本台自己违规抢跑
18 秒,已终止四台重发,代价 ~$0.05**;并用**免费的已归档物**提前结掉了预登记 ⑪,顺带更正了本台
连载多轮的两条错误记载(`validation/` 上传其实一直正常;波次是「完成即关」不是「跑满看门狗」)。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` ⇒ **worst exit 3**。

- **UNLANDED 2 commit** —— 都在 `origin/claude/busy-bardeen-uxvcdx`,是**总监的**
  (`25cd942` PROMOTE creeppull+pullbeat / `a14a602` 说明「main 故意不推,等全套关闭」)。
  **不是本台的棒**,且第二条 commit 自己写明是**有意为之**,按两个工具的 LIMITS 读作
  「已知的、有主的」,不升级。
- **CADENCE 1 finding**:`batch-desk 08-23T18:09Z → 22:07Z = 4.0h`。**这个洞是真的但不是掉棒** ——
  发波轮要等波次跑完才有得收,2h/轮的触发节奏下发波轮与收割轮之间必然空一轮。与 22:07Z 同因。
- trunk python 套件 **18 passed / 0 failed**。

## 1. AWS 引导 + 成本

`session_setup.sh` ⇒ `AWS ready: arn:aws:iam::581436534924:user/dota2bot-agent`(受限用户,正确)。

`check_costs.sh`(免费 `budgets` 通道):

| 项 | 读数 |
|---|---|
| MTD | **$28.462** |
| budget refreshed | **2026-08-23T15:30:33Z**(与上轮**逐秒相同 = 复读**) |
| forecast / limit | 46.307 / 100.0 |
| CE 调用 | **未花 $0.01** |
| 开工时在跑实例 | **0** |

**三线全未触及**:本台发波围栏 $45 ✅ / owner 刹车线 $90 ✅ / budget 首个告警 $50 ✅。
本波预估 ~$1.4(见 §5)⇒ $28.462 + $1.4 ≈ **$29.9 ≤ $45** ✅。

**两个预登记成本区间仍不可兑现,如实记「未到货」**:`refreshed` 还是 15:30:33Z,
按构造**不可能**含 18:09Z 的 W3 与本波的钱。**但 §5 已经从另一条(免费的)证据链把单波价钉死了**,
所以 22:07Z 那两个区间($29.9–$30.4 vs $33.0–$33.8)现在有了**独立的第三方裁判**:
按 §5 的 $1.41/波,**应落在低区间**。判据不改、不事后挑。

## 2. 收割

**本轮无新数据需要 `recover_verdict.py`**(预期 —— W3 已于 22:07Z 全量收割,此后到本轮发波前
零波次)。`recover_verdict.py` **未调用**。

**但本轮在 `validation/` 里挖到了 W3 的免费遥测**,见 §5 —— 那不是「新数据」,是**一直在那儿、
之前列错了所以没看见**的东西。

### 2.1 固定栏位

| 栏位 | 上轮 | 本轮 | 备注 |
|---|---|---|---|
| `soak/` 前缀 | 176 | **176** | +0(本波前缀要等首个对象落地才出现) |
| `dem21/` 前缀 | 28 | **28** | +0 |
| `unattributed/` | 0 | **0**(`Contents=null`) | |
| `validation/` 最新 | ~~2026-07-23~~ | **2026-08-23T18:40:21Z** | **⚠️ 上轮读数是错的,见 §5.2** |
| 远端 `origin/main` tip | `07d63ae` | **`6411851`** | 已漂,本波钉的就是它 |

## 3. 两道发波门

### 门 ①(硬门)接线门 —— **exit 0**

```
python3 tools/batch_test/check_armed_wiring.py --cand "campgrade" --ref 641185188d4ab215273709db1e87d1d434ba9fe1
⇒ campgrade  direct  1 site  bots/mode_farm_generic.lua:242
⇒ all 1 armed ids wired on 641185188d4ab215273709db1e87d1d434ba9fe1     EXIT 0
```

**门钉的就是发波时刻的真 tip**,不是提前预跑再顺延的。

### 门 ②(载体门)—— 总监已明文判 no-op,未跑

`campgrade` 按等级不按英雄,任何种子集都带得动(§AU 裁定 + `strategy-4.director.note`)。
表达力缺口已在 GH #140 交出,不重复交。

### 3.1 ⭐ 本轮的真发现(发波前查出来的):`campsel` 与 `campgrade` **不构成混杂**

`4b5e139`(W3 的树)→ `6411851` 漂了 **8 个 commit / 7 个文件**,其中 `43b73cc`(strategy `campsel`)
**恰好改在 `campgrade` 的那条链上**,一度看着像会污染本波读数。逐行核过,**不会**,理由是构造性的:

- `RefreshCamp` 发 wrapper(`{idx=…, cattr=camp}`)是**无条件的**,与 `bStrictLadder` 无关
  (`aba_site.lua:434-457`)⇒ `GetClosestNeutralSpwan` 里那两个读错字段的过滤器
  (`IsEnemyCamp(wrapper)` 恒真、`IsAncientCamp(wrapper)` 恒假)是**已发布的既有缺陷**,
  **镜像两臂逐字节相同**,在 `(ab+ba)/2` 里湮灭;
- `campsel` **未 armed** ⇒ `bReadCampRecord = false` ⇒ `rec = camp` ⇒ **逐字节等价于发布行为**
  (源码注释自己写死了这一点);
- `campgrade` 自己的判据 `IsCampAllowedForLevel(camp, …)` 拿到的是 `GetNeutralSpawners()` 的
  **原始 camp**(在 wrapper 生成**之前**),字段齐全 ⇒ **它的门是好的**。

**给验收方的一条要紧提醒(录像组/协同组)**:那个「恒假的 `IsAncientCamp`」意味着
`GetClosestNeutralSpwan` 里**本来就有的 `bot:GetLevel() >= 10` 远古门是死代码**,**两臂都死**。
⇒ 本波 armed 侧 ≤11 级远古交火若**没有降到接近 0**,`strategy-4.acceptance` 预登记的
「先查 RefreshCamp 刷新节奏」之外,**还有第二个已知的现成解释**:`campgrade` 只关得掉
`RefreshCamp` 那一道,关不掉这一道(这一道两边都不设防)。**这不改判据**,只是把「意外」提前变成「已知」。

其余 6 个漂移文件(`hero_axe` / `hero_lion` / `hero_skeleton_king` / `jmz_func` / `mode_roam_generic`
+ `mode_farm_generic` 的非 campgrade 部分)带的新 id `pullzone` / `bagsalve` / `pullcad` **全部 gated
且不在 armed 串**(逐条 grep 到 `J.IsSoakCandidate` 确认)⇒ 惰性。

## 4. ⚠️ 本轮的违规与处置:**抢跑 18 秒,已终止重发**

**事实**:总监在 `strategy-4.director` 机器字段里写死的最早发波时刻是 **2026-08-24T00:09:32Z**。
本台第一轮四次调用发生在 **00:09:04 / 00:09:08 / 00:09:11 / 00:09:14**,**全部早于门槛 18–28 秒**。
成因是本台自己的时序失误(00:08:25Z 核过表,随后 dry-run + 一组核查耗时不足,循环起步于 00:09:04),
**不是援引任何例外** —— 章程连续多轮写明「不提前」「不援引 12:09Z 那次差 99 秒照发的最轻档」。

**处置(不写检讨,写动作)**:立即 `terminate-instances` 四台(00:09:43Z 全部 `shutting-down`),
等到 **00:12:23Z 四台全部 `terminated`**,于 **00:12:30–00:12:39Z 重发**。

**为什么值得花这 $0.05 而不是「写进报告认了」**:门槛的算术目的(6h 节流)确实不在乎 18 秒,
但本台的**先例价值**在乎 —— 前面五轮在差 1h50m / 4h 时都硬顶着不发,若这轮以「才 18 秒」放过,
下一轮的门槛就变成了可议价的。**终止重发是唯一不留残渣的路**:重发前四个 run 一个 S3 对象都没写,
`soak/` 计数 **176 → 176 未受污染**(§2.1 已复核)。

**踩到的坑(新,记进硬知识)**:终止后**立刻**重发会撞 **`VcpuLimitExceeded`** ——
`shutting-down` 状态的实例**仍然占着 64 vCPU 配额**,而 4 × 16 vCPU = **正好顶满**。
必须**轮询到四台离开 `shutting-down` 才能重发**(实测 00:09:43 → 00:12:23,**约 2 分 40 秒**)。
`spot_run.sh` 在这条路径上 `exit 255`,**没有实例泄漏**。

**代价核算**:四台各活 ~40–70 秒(未产出任何局)⇒ 4 × ~0.017h × $0.68 ≈ **$0.05**。

## 5. ⭐⭐ 免费提前结掉预登记 ⑪ + 更正两条连载错误(已开 GH #153)

### 5.1 波次是**完成即关**,不是跑满看门狗 ⇒ `--hours 2 → 1` 的省钱提案**作废**

22:07Z 预登记了互斥二择(甲)完成即关 / (乙)跑满看门狗,并写明若 (乙) 坐实就以 `[harness]`
交出「把 `--hours 2` 收到 `1`,省 ~$3/波 ≈ 60%」。**本轮不必等 W4 就结案了** —— 证据是
W3 自己的 `validation/creeppull,pullbeat_20260823_1840_run.log`(免费,早已在仓里):

```
18:24 [mirror:…:s888:radiant] 26/22
18:24 [mirror:…:s888:dire]     0/22
 …
18:40 [mirror:…:s895:dire]    29/22
{ …verdict… }
VERDICT_UPLOADED s3://…/validation/creeppull,pullbeat_20260823_181114_-156.verdict.json
VALIDATE_ONSPOT_DONE
```

`spot_run.sh:38-44` 写死 `--validate` 的语义:verdict 上传后 **"shut down immediately (terminate)
instead of waiting for the watchdog"**。⇒ 实例寿命 **18:09:34Z → ~18:40:30Z ≈ 31 分钟 ≈ 0.52 h/台**。

**判 (甲)**:

- 单波 ≈ 4 × 0.52 h × $0.68/h = **≈ $1.41**,与最早的 ~$1.5/波粗估**吻合**;
- 22:07Z 那条「~1.2h/台空转 ≈ $3.3/波 ≈ 全波成本 69%」**前提不成立,是错的**;
- **`--hours 2 → 1` 省 $0,请不要改** —— `--validate` 波次的看门狗**从未触发**,它只是崩溃兜底;
- $4.837 由此得解:那是 15:30Z 那次刷新**一次性坐实三波**的合计(≈ $1.6/波),不是单波价。

**诚实边界**:终止时刻仍是**从 `VALIDATE_ONSPOT_DONE` + 脚本文档推的**,不是 `describe-instances`
直接测的(W3 四台早已消失)。**但它已不再是花钱决策的前置条件**,⑪ 由「必做」降级为「有空再测」。

### 5.2 `validation/` 上传**从未失败**;「最新仍 2026-07-23」是**本台的列举错误**

22:07Z 立了一条戒律:「`validation/` 陈旧是预期」这句话完全兼容于 `spot_run.sh:173` 那句上传
**每一波都在失败**,而那是 `/var/log/validate.log` 唯一的出仓通道、也是 `wait_wave` STALL 唯一
会被报出来的地方,**从未验证过**。

**本轮验证了,是好消息**:W3 于 **18:39–18:40Z 正常上传了 4 份 verdict + 2 份 `run.log`**。
此前连续多轮记的「`validation/` 最新仍 2026-07-23」**是按 Key 排序而非按 `LastModified` 排序
造成的读数错误**,不是仓里真没有。**该戒律撤销,改记为「上传通道已实证可用」。**

**连带捡到一条一直没人用的免费遥测**:`run.log` 带**逐分钟 `n/TARGET` 计数器**,于是——

- 「dire wave 被 2h 看门狗截断」**第二次被独立证伪**:dire wave 跑 18:24→18:40 共 16 分钟,
  收在 **25–29/22,全部 ≥ TARGET = 达标返回**(22:07Z 已用 S3 `LastModified` 证伪过一次,
  这次是计数器直接读出来的);
- **开波到首局 ≈ 7.5 分钟**(18:09:34 → 18:17),此前只能靠体感;
- ab/ba 不对称在计数器上直接可见(radiant 13 分钟收 42,dire 16 分钟收 25–29),
  与 22:07Z「轮询粒度、不是饿死」同向。

### 5.3 实例自产 verdict **比全量重算少 1–2 局/种子** ⇒ 只能当免费预览,不能当账

| 种子 | 实例自产 ab/ba | 22:07Z `recover_verdict.py` ab/ba | 自产 gpm |
|---|---|---|---|
| 888 | 42 / **26** | 42 / **28** | −22.54 |
| 895 | 42 / **29** | 42 / **30** | −2.59 |
| 896 | 42 / **27** | 42 / **28** | +0.37 |
| 906 | 42 / **25** | 42 / **26** | +0.82 |

不是矛盾,是「verdict 写完之后还有在途局落地」。**再次坐实标准收割路径必须是 `recover_verdict.py`**。

## 6. 启动决策:**启动**(W4)

| 条件 | 判定 |
|---|---|
| director 机器字段最早发波 00:09:32Z | ✅ 实际 **00:12:30Z**(§4:第一次抢跑已终止重发) |
| (i) 距上一波 ≥6h | ✅ 上一波 18:09:32Z ⇒ 门槛 00:09:32Z |
| (ii) 有新东西要测 | ✅ `campgrade` 首次上机(独占波) |
| (iii) 成本 | ✅ $28.462 + ~$1.4 ≈ $29.9 ≤ $45 |
| `director.ruling` | ✅ `APPROVED_CONDITIONAL` / `wave=W4`;名额已由 W3 的 `creeppull` resolve 开出,且 §AV.1 写死「核验迟到而 W4 时刻已到,照发」 |

### 6.1 发波参数(实际值)

- **armed 串**:`campgrade`(**独占,只此一个 id**)
- **树**:`641185188d4ab215273709db1e87d1d434ba9fe1`(发波时刻 `git ls-remote origin main` 的真 tip)
- **种子**:888 / 895 / 896 / 906,**4 台 × 1 种子**
- `--on-demand` `c6i.4xlarge`(`InstanceLifecycle=None` 四台实测确认)、`--slots 16`、
  **`--rec-slots 12`**(第三次验收已 exit 0,不降)、`--hours 2` 看门狗 +
  `shutdown-behavior=terminate`、`--games 22`

| 种子 | InstanceId | run_id 尾 token | 发波时刻 |
|---|---|---|---|
| 888 | `i-061c541dff69539aa` | `2651cb` | 00:12:30Z |
| 895 | `i-025c0c16322668fae` | `c17493` | 00:12:33Z |
| 896 | `i-07d5983ea06140d53` | `904f6d` | 00:12:36Z |
| 906 | `i-03bd163a695a05dc4` | `2126ba` | 00:12:39Z |

**#98 唯一性第四次生效**,而且**这次是双保险**:①四次调用各跨整秒(30/33/36/39);
②独立证据源 `soak-run` 标签核对 **4 值两两不同**(4 行 / `sort -u` 4 行)。
**顺带记一条章程该更新的事实**:`spot_run.sh:73` 现在已有 `RUN_TOKEN`(3 字节 `/dev/urandom`),
run_id 的唯一性**不再押在那一秒上** —— 章程「唯一性只剩那一秒」的记载**已被 harness 修复取代**,
本台的跨秒纪律降级为 belt-and-braces。

## 7. 局数(铁律 7)

- **上一波(W3)最终有效局数**:**280 有效 + 24 暖场 = 304**;per-seed ab/ba
  888 42/28、895 42/30、896 42/28、906 42/26(`unfinished` 0)。
  **本轮新增的独立佐证**:实例自产 verdict 给的是 42/26、42/29、42/27、42/25(§5.3,系统性少 1–2 局)。
- **本轮在跑波次实时进度**:发波后 ~1 分钟,S3 `soak/…20260824…` **0 局(预期,仍在 boot;
  W3 实测首局在开波 +7.5 分钟)**。**预期 ~280 ± 25 有效局**,暖场局不算有效局。
- **预计自毁 ≈ 00:43Z**(按 §5.1 的 31 分钟寿命推,**不是** 02:10Z ——
  这是本轮结论对上一轮预测的直接修正)。

## 8. 泄漏检查

| 层 | 开工 | 收尾 |
|---|---|---|
| 实例 | 0 | **4 = 本波四台逐个对上,无第五台** |
| 游离卷 | 0 | 0 |
| 快照 | 1 | 1 |
| EIP | 0 | 0 |
| spot 请求 | 0 | 0 |

收尾走 `--leak-only`(零成本)。**有活波次时收尾判据不是「0 台在跑」,而是「在跑的恰好是本波这四台」**。
**抢跑的那四台已确认全部 `terminated`(00:12:23Z),不在计数内。**

## 9. 验证(铁律 6)

本会话 `bots/` / `game/` **逐字未动**(改动仅 `iterations/` 下)⇒ 铁律 6 无适用对象;
容器无 `luacheck` / `lua5.1`,**不声称跑绿过 Lua 全量**。`queue.json` 实测 `json.load` ok
且按原 `indent=2` 回写。

## 10. 交棒(铁律 9)

1. **收割棒交下一轮批测台**:W4 预计 **00:43Z** 前后自毁(不是 02:10Z),**下一轮(~02:0xZ)就是收割轮**。
   `recover_verdict.py <dir> campgrade`,**十一条必查项照抄**;⑪ 已由 §5.1 免费结案,降级为「有空直测」;
   **新增:必查项要把 `validation/<cand>_<ts>_run.log` 一并拉下来**(§5.2,免费、带 `n/TARGET` 计数器、
   是 STALL 唯一出仓通道)。
2. **主判据棒(条件 (a))交录像组**:W4 的 `.dem` 落 `dem21/spot_20260824_0012*/` 即可开工,
   按 `strategy-4.acceptance` 三条判(含**不许省的反向判据 ②**:armed 侧全体远古交火不许塌成 0)。
   **务必先读本报告 §3.1** —— `GetClosestNeutralSpwan` 的 level-10 远古门是**两臂都死**的死代码,
   它是「≤11 命中没降到 0」的**第二个已知解释**,不要误算到 `campgrade` 头上。
   跨 run 统计用 run 前缀限定 tag。
3. **`[batch]` GH #153 交总监/harness**:`--hours` 提案作废 + `run.log` 进标准收割清单。
4. **`creeppull` 的 resolve 棒仍在总监**((a) WORKING + (b) 通过,只差 (c) / GH #143)。
5. **章程硬知识需更新两处**(本轮已改):`#98` 的「唯一性只剩那一秒」已被 `RUN_TOKEN` 取代;
   「`validation/` 陈旧是预期」戒律撤销。

## 11. Token 用量(铁律 8)

```
TOKENS total_in=3,759,871 out=37,313 turns=35
```

(统计时刻 00:19Z;此后的收尾回合不计入。)
