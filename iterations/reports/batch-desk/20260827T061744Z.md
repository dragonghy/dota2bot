# 批测台 2026-08-27T06:17:44Z —— **发 W16(第一波全 spot 四台,零降级)**

**一句话**:6h 闸按 `date -u` 实打到点后放行,**W16 四台全部以 `InstanceLifecycle=spot` 起飞、
容量阶梯第一级就拿到、无一台降级按需** —— 这是 owner GH #158 拍板后的**第一波完整 spot 波**,
也正是交棒项②(给 spot 单波价买账单侧实测)一直在等的那种波。
连带:上一轮点名的 **trunk 两条红本轮全绿**,GH #210 收口后「机械取第 2 行」**第一次真的照做**(40 id/354 字节)。

---

## 一、开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` → **worst exit 3**(非阻断;3 = 有发现)。

| 腿 | 读数 |
|---|---|
| 推送门上膛 | `core.hooksPath=.githooks` 已设 |
| unlanded work | **OK**,可证窗口内无未落地工作(442 refs,406 因浅克隆 REFUSED —— 是「不可判」不是「干净」) |
| 报告节奏 | 1 处洞:**`strategy` 08-26T10:34Z → 14:23Z,3.8h**。**不是本台的**,登记交棒 |
| 未裁队列请求 | RIDESHARE none / OTHER none —— **零积压** |
| stable 锚点 | `stable-v1`(6db5921c)/ `stable-v2`(136a332f)**两个都 ok**(EXISTS/PINNED/SHIPPED) |
| **trunk python** | **41 passed / 0 failed** |
| **trunk 快 Lua 检测器** | **11 个文件 / 0 失败** |

**⭐ 上一轮(03:12Z)点名的两条红本轮全绿**:`test_selfcheck_lua_leg.py` 由 40p/1f → **41p/0f**;
`test_salveyield_arbitration.lua:450` 那个写死 `== 105` 的 fixture 计数由 1/9 失败 → **0 失败**。
⇒ **交棒项⑨(hero/strategy 组 trunk 两条红)可以销号**。本台不改别组的树,只报读数。

---

## 二、成本与围栏(铁律 1 / 章程步骤 2)

### 2.1 读数

| 量 | 值 | 来源 |
|---|---|---|
| MTD(免费) | **$50.671** | `budgets/dota2bot-batch`,快照 **2026-08-26T23:46:06Z** |
| MTD(CE 复核 $0.01) | **$50.6709276212** | 读数 ≥$35 自动复核 |
| 一致性 | **逐位相同,连续第六轮** | |
| forecast / limit | 76.999 / 100.0 | |
| 开工在跑实例 | **0 台** | running/pending 段空 |
| 常驻 | AMI `ami-0a990a26d89c66547` + 快照 | 唯一常驻成本 |

### 2.2 围栏算术(章程步骤 2 甲:**不用裸 MTD**)

**快照时刻 08-26T23:46:06Z 早于 W15-R 发波(00:15:43Z)⇒ W15-R 一台都没进去**;
本轮 MTD 与上一轮**同一张快照、逐位未动**。

```
围栏 = MTD $50.671
     + W15-R 实测 $1.69   (上一轮 S3 时间戳法算出,3 台 × 0.812 h × $0.673 + 收尾)
     + W16 预估 $2.25     (见下,按最坏情形按需价记账)
     = $54.61   ≤ $60 闸 (iii)   ✅
```

**W16 为什么按按需价预估**:GH **#233 (a)** 尚未裁 —— spot 优先规则下,**单波价从常数变成了随机变量,
而降级发生在闸 (iii) 判定之后**。本台裁定前的保守默认就是**一律按按需价预估(最坏情形),
跑成 spot 算省下来的**。取值 = 4 台 × **0.812 h/台**(W15-R S3 实测,是下界)× $0.673 + 收尾 ≈ **$2.25**。
**实际若如愿跑成 spot,应落在 ~$0.8**(章程值),差额 $1.45 是这条保守默认的已知代价。

### 2.3 $50 档的书面解释(章程步骤 2 (iii) 配套义务)

MTD 读数本轮**逐位未动**(同一张快照)⇒ **没有新增跨档**。但本台仍按义务把话写在这里,
让 owner 手上那封 **$50 Budget ACTUAL 告警邮件永远有一份对得上的书面解释**:

> 本月累计 $50 的构成是**例行「测试版 vs 稳定版」镜像批测波次**,单波界前 ~$1.48 / 界后按需 ~$2.15、
> spot ~$0.8,累计 W1–W15-R 共十余波。$50 是 Budget 的**第一档告警(一封邮件)**,
> **不是限额也不是冻结**;owner 当前批准档为 **$100**,⇒ $50–$100 这一段的钱**已经被批过**。
> 本台的自设围栏 **$60**、刹车线 **$90**、批准线 **$100** 均未动。

---

## 三、收割

**本轮无可收割对象。** 上一轮(03:12Z)已把 W15-R 三个 run 全量收割完毕
(`files_seen=176 / games_loaded=176 / source_dirs=3 / unparseable=0`,零静默丢局),
并与种子 906 合成了 37-id 全集迄今第一份完整 4 种子读数(gpm −19.96 / 218 计分局)。
S3 上自那以后**没有新的 verdict 落地**(本轮开工时 `soak/` 下无新前缀)。

⛔ **仍未并池的东西照录不误**:W15 那 **30 局孤儿单腿**至今没有并进任何池
(物理上要重下 W15 的 run 才做得到);四个经济量**未施铁律 4(i)**。

---

## 四、发波决策:W16 —— 三条闸全过,**放行**

### 4.1 三条闸(章程步骤 4b)

| 闸 | 判定 | 证据 |
|---|---|---|
| **(i) 距上一波 ≥6h** | ✅ **PASS** | 锚点 = W15-R 末台 `2026-08-27T00:15:53Z` ⇒ 解锁 `06:15:53Z`。**先打钟点再比**:`date -u` 实测 06:12:09Z 时**还差 223s**,轮询到 **06:15:57Z** 才放行,首台 launch **06:16:04Z**。**没有写死 epoch 常数抢跑** |
| **(ii) 有新东西可测** | ✅ **PASS** | tip `d9585a29` ≠ W15-R 树 `79f32c92`;**两步核法**:`git log 79f32c92..HEAD -- bots/ game/` **exit 0 且非空** ⇒ 真可比,不是空输出被读成无漂移。**5 个 commit 动了 `bots/`**(`87b32479` salveally / `624de021` ABILVALUE / `bc75ad2b` salveyield / `b49fe55f` t20/t25 / `40e9bf18` Lion t25) |
| **(iii) 已花 + 本波 ≤ $60** | ✅ **PASS** | **$54.61**(见 §2.2) |

### 4.2 队列不构成例外,也不构成阻碍

42 条请求逐条看过。**没有一条要求专波**:11 条 `ROUTED_ARCHIVE_SCAN`(零 EC2 归档扫描)、
2 条 `RECEIVED_NOT_SCHEDULED`(受理 ≠ 可发)、2 条 `DEFERRED_PENDING_W3_RESOLVE`、
2 条 `REJECTED`、1 条 `APPROVED_CONDITIONAL` 但 wave 写的是 **W6**(早已过去)。
⇒ §4a 的「显式请求不受 (i)(ii) 节流」**本轮仍无适用对象**,与上一轮结论一致。

**搭车的两条本轮上车**(`APPROVED_ADMITTED`,wave = 下一波全集):
`strategy-18`(`pullthink`,GH #186)、`strategy-21`(`bbfight`/`bbshort`,GH #208 族;
`bbrespawn` 按 strategy-22 `REJECTED` 正确排除)。两条 `pending → running`,queue.json 已更新。
另有 6 条(`strategy-10/13/14/15/16`、`hero-15`)自 W14 起已是 `running`,继续在集。

### 4.3 发波前两道门(章程硬知识)

| 门 | 调用 | 结果 |
|---|---|---|
| **接线门** | `check_armed_wiring.py --cand <40 id> --ref d9585a291a1f…` | **exit 0**,`all 40 armed ids wired`。新入的三个各自点得到落点:`bbfight`→`jmz_func.lua:10594`、`bbshort`→`:10633`、`pullthink`→`mode_roam_generic.lua:224`(2 处) |
| **载体门** | `seed_roster_index.py --seed 888 895 896 906` | **exit 0**,与 **W8–W15 逐字复现**:`crystal_maiden`/`zuus` **FULL 4/4**;`lion`/`obsidian_destroyer` **PARTIAL 仅 s896**;`skeleton_king` **PARTIAL s895/s906**。角色门/等级门 id 的载体门仍 **no-op**(缺口 GH #140,**第十八轮**) |

**⭐ arm 串:本轮第一次真的机械取第 2 行。** GH #210 上一轮由本台自撤并留言后,第 2 行现在是
**40 id / 354 字节**(`echo -n | tr ',' '\n' | wc -l` = 40,与接线门权威计数一致)。
W14/W15/W15-R 那条「不能机械取第 2 行」的临时纪律**已作废,本轮照新规执行,无异常**。

⚠️ **积压没清空,只是没长大**:本轮闸 (ii) 的 5 个新 commit 里,`salveally` / `salveyield`
两个 gated id **仍不在第 2 行**(与上一轮**同样的两个,没增没减**)。新 id 入集裁定继续积压,GH #218。

### 4.4 ⭐⭐ 市场类型:**四台全 spot,阶梯第一级就拿到,零降级**

owner GH #158 原话「**On demand和spot之间肯定优先用spot呀,除非没有spot的机器**」。
本波**未传 `--on-demand`**(`spot_run.sh` 默认即 spot),**容量降级阶梯一级都没用上**:

```
awsx ec2 describe-instances --instance-ids <四台> \
     --query 'Reservations[].Instances[].[InstanceId,InstanceLifecycle,InstanceType,State.Name]'
```
| 实例 | **InstanceLifecycle** | 机型 | 状态 |
|---|---|---|---|
| `i-0e3599276c1d36979` | **spot** | c6i.4xlarge | running |
| `i-04618f56342920b5c` | **spot** | c6i.4xlarge | running |
| `i-055f6a527640f312d` | **spot** | c6i.4xlarge | running |
| `i-03d371da6c7e47ae3` | **spot** | c6i.4xlarge | running |

**四个读数都是 `spot` 不是 `None`** —— 这是章程要的那份证据。
`run_id` 的 `spot_` 前缀是硬写字符串,**没有当证据用**。
`c6i.4xlarge` 一次拿满,**没有 `InsufficientInstanceCapacity`,没有换 `c6a.4xlarge`,没有降级按需**。

**⇒ 这正是交棒项②等的那种波:W15 只有一台跑满,W15-R 是按需时段。
若本波四台跑满零回收,下一轮就能用 S3 时间戳法给 `~$0.8` 这个 spot 单波价买到
**它至今一次都没有过的账单侧实测**。**在那之前围栏对 spot 继续用 `~$0.8` 并记住它未经实测。**

### 4.5 run_id 唯一性(章程步骤 5)

`RUN_TOKEN`(GH #98)已让唯一性独立于 wall-clock,跨整秒纪律降级为 belt-and-braces —— 但**独立证据源的核对保留**:
`soak-run` 标签四个值 **`5e1086` / `c1d1cf` / `8ca428` / `654032`,两两不同(distinct=4)**。
四次调用实测间隔 **5s / 5s / 4s**,均跨整秒。

### 4.6 W16 配置

| 项 | 值 |
|---|---|
| 波次 | **W16** |
| 树 | `d9585a291a1feeace13ac46a30aeb3ad3ba481cb`(**发波前 `git ls-remote origin main` 核对 = 本地 HEAD,逐位相同**) |
| arm 串 | test_set.md 第 2 行 **40 id / 354 字节**(机械取用) |
| 对比 | `contrast=vs_stable`(单臂;**未传 `--cand-ref`** ⇒ 收割时也不要传) |
| 拓扑 | **4 台 × 1 种子**(888/895/896/906,与 W7–W15 同种子) |
| 参数 | `--slots 16 --rec-slots 12 --hours 2 --games 12`,**无 `--on-demand`** |
| 市场 | **spot ×4,零降级**(§4.4) |
| 自毁 | `--hours 2` 看门狗 + 完成即关 + shutdown-behavior=terminate + spot 一次性请求 |
| run 前缀 | `spot_20260827_061604_1_main_5e1086`(s888)/ `…_061609_…_c1d1cf`(s895)/ `…_061614_…_8ca428`(s896)/ `…_061618_…_654032`(s906) |

**`--rec-slots 12` 不动**(总监 §AS.2 裁的 (C) 走 12,压过本台自己「停在 8」的保守默认;
留 slot 13-16 作对照腿,验收仍成立)。同一波内**只动被测树、不动采集配置**,08-11 那条边界继续有效。

---

## 五、局数(铁律:owner 2026-08-19 要求)

### (a) 上一波次(W15-R)最终有效局数 —— 已于上一轮定案,此处只复述

**176 落盘 / 159 计分 / unfinished 0 / 暖场 17**,三粒 ab/ba = **28/14、34/25、36/22**,
ba 占比 **0.352**。三台全部跑满零回收,分母同质,可与 W11–W14 的 0.306–0.325 序列并列。

### (b) 本轮在跑波次(W16)**预期**局数

本轮是**启动型报告**,按章程写预期。以 W15-R 的**每台实测**为基准外推
(58.7 落盘 / 53.0 计分 per 台,同 recipe、同 `--games 12`、同 `--hours 2`):

- **预期 ~235 落盘 / ~212 计分**,ba 占比预期落在 **0.31–0.36**。
- **诚实边界**:这条外推的基准来自**按需**时段的三台;spot 的中断风险让它**只是期望值不是下界**。
  一台被抢占 = **只作废那一粒种子的配对差**(处置已事先登记,owner 常设运维项),
  按容量阶梯补发该种子,**不整波作废**。
- 本轮开工时 S3 `soak/<run_id>/` 下**尚无对象**(实例 06:16Z 起飞,约 12 分钟开机),
  **实测进度由下一轮收割时报**。

---

## 六、泄漏检查(章程步骤 6)

- **开工**:`check_costs.sh` 的 running/pending 段**空**,0 台在跑。
- **收尾**:`--leak-only` 读到 **4 台在跑,逐个对得上 §4.6 的发波表**(全是本轮自己发的),
  常驻只剩 AMI + 快照。**这不是泄漏。**
- ⚠️ **本台自己要认的作证边界**(上一轮 §8 立):`--leak-only` 读到的数字只能证明
  **此刻**在跑的是哪几台,**不能**证明「过去这一轮没漏过」——
  终止实例约 1 小时后就从 `describe-instances` 消失,**它不报错,它返回空,而空会被读成「没有实例」**。

---

## 七、铁律 6(push 门)

- 静态半:`bash tools/agent/luacheck_gate.sh` → **exit 0**(0 warnings;容器缺 luacheck,脚本自装 `lua-check`)。
- `core.hooksPath` 已由开工自检上膛,**两条 `git push` 各跑一次该门**。
- **未使用 `RULE6_BYPASS`**。
- 动态半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)**未跑,也不声称** ——
  本轮 `bots/` / `game/` **一行未改**(改动只有报告 + 章程 + queue.json)。

---

## 八、交棒

1. **⭐ 下一轮本台 —— 收割 W16**。四个 run 前缀见 §4.6。收割用
   `recover_verdict.py`,**按 run 前缀分目录再池化**(GH #225,一 run = 一种子零交叉);
   **不要传 `--cand-ref`**(本波是单臂 `vs_stable`)。arm 串从 `script_version` 反解应得
   **40 id / 354 字节**,与 §4.6 逐字核对。
2. **⭐⭐ 下一轮本台 —— 交棒项②(spot 单波价的账单侧实测)第一次有料**:
   若 W16 四台跑满零回收,用 **S3 `LastModified` 法**(GH #239,免费、永不过期)
   给 `~$0.8` 买第一份实测,并写进围栏算术。**若有回收,只作废那粒种子并补跑,不整波作废。**
3. **6h 闸锚点:W16 末台 `2026-08-27T06:16:18Z` ⇒ W17 解锁 `2026-08-27T12:16:18Z`。**
   **先 `date -u` 再比,别写死 epoch 常数**(本轮就是这么做的:轮询到点才发)。
4. **✅ 可销号 —— 交棒项⑨(trunk 两条红)**:本轮自检 python 41p/0f、快 Lua 0 失败,**两条都绿了**。
5. 总监 —— **GH #233**(spot 优先的硬边界两条待裁)。**本轮新增第一手材料**:
   (a) 四台一次拿满 spot、零降级 ⇒ 「降级发生在判定之后」这一条**本波没有被触发**,
   但**规则仍然是随机变量**,本台继续用「按需价预估」的保守默认;
   (b) 「回收+补跑 = 一波的钱花两次」本波**尚无实例**,继续攒。
6. 总监 —— **GH #239**(两条静默失效 + S3 时间戳法进硬知识 + `--leak-only` 的作证边界)。
7. 总监 —— **#207(第七轮)** `zusstatic` 连续第六波空转,W16 仍在集,再空转就该裁是否退集。
8. 总监 —— **#204(第七轮)**,上一轮已买到横向证据(同波三粒 `D` = −197.06/+65.18/+113.51,极差 310 gpm)。
9. 总监 —— **#219 可结案**,连带读 #233。
10. 总监 —— **#218 后续**:`salveally`/`salveyield` **仍不在第 2 行**(与上一轮同样两个),新 id 入集裁定继续积压。
11. 录像组 —— `abilanc` 的 (a) 取证语料:W14 的 156 个 `.dem`(约 09-16 过期)+ W15 的 84 个 +
    W15-R 的三 run(共 483 对象)+ **W16 新增(`--rec-slots 12`,四台)**。
12. **strategy 组 —— 报告节奏 1 处洞**(08-26T10:34Z → 14:23Z,3.8h)。
13. 存量催办:#217(第六轮)/ #211(第七轮)/ #225 / #180(第十三轮)/ #171(第十一轮)/
    #200(第十轮)/ #181 / 载体门 PARTIAL(**第十八轮**)/ `stable-v*` tag(第十五轮)/
    `campdanger`(第十轮)/ §BL.4 机械化 / #75(第九轮,维持 12)。

---

## 九、本次会话 token 用量(铁律 8)

```
TOKENS total_in=2,917,266 out=25,224 turns=29
```

(统计时刻之后的收尾回合不计入 —— 提交/推送那几步不在这个数里,当量级参考。)
