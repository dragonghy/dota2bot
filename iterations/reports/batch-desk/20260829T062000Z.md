# 批测台 2026-08-29T06:20Z — 发 W24(**42-id**,新序列起点)+ GH #295(trunk RED,两个独立根因)

**一句话**:发 W24(树 `2d1024ee`、**42-id**、种子 `1601/1603/1633/1641`、四台 c6i spot 四 AZ),
**三件市场验收连续第二波全过**(阶梯第 1 级一次即中、四 AZ 互不相同、四条 `requested=/actual=` 全等);
⭐ **本轮两个主产出都是「字面读法会骗人」**:(a) armed 串**已由 43 降为 42**(总监 §CB 把 `campexit` 退集),
所以 **W24 不能并进 W21–W23 的 43-id 序列**,是新序列的第一粒;
(b) §4b(ii) 的「树变了」**字面成立但实质空洞** —— `bots/` 的三个新提交里 **55 行是纯注释、零行为**,
唯一的真改动 `odbuild` **gated 且不在 arm 串里 ⇒ 本波内恒 false**。
⭐ 另立 **GH #295**(trunk RED,非本台文件),其中一条**直接咬到本波**:`odaoe` 检测器在这棵树上抛异常。

---

## 一、自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh`,worst exit **3**,8 腿。
退出码来源自报(#267):**FINDINGS = `cadence` `trunk-red(python)` `trunk-red(lua)`;UNCERTIFIABLE = none**。

- **⭐ trunk 两侧同时红,且上一轮(03:17:49Z)两侧全绿 ⇒ 回归窗口 ≤1 轮。**
  python **50 passed / 2 failed**(上轮 51/0/0);fast Lua **1/21 文件失败**(上轮 18 文件 0 失败,
  **FAST SUBSET 不是全套**)。
  工作树干净(`git status` 空)、`HEAD == origin/main == 2d1024ee` ⇒ **这是 main 上的红,不是本地脏树**。
  三处失败、**两个独立根因**,**全部不是本台文件** ⇒ **立 GH #295**,详见 §六。
- **两锚点** `stable-v1` / `stable-v2`:`EXISTS` / `PINNED` / `SHIPPED` **三项全 ok**。
- `unlanded_commits.py`:**无**。
- cadence:`replay-check` `00:55Z→05:30Z` 4.6h、`strategy` `01:24Z→10:35Z` 9.2h(**连续第六轮同值,不是新洞**)、
  `director` 4.2h。
- 队列:`pending` **23** 条,`hero-22`(`odbuild`)`director` 字段 **EMPTY** —— 唯一一条未裁的,见 §五。
- 并发会话检查(#180):本机无其他会话。

## 二、成本与围栏(铁律 1)

- 开工 `check_costs.sh`:running/pending **空**(W23 四台已自行终止,见上轮 §四)。
- MTD **$63.046**(budgets 免费读数,快照 **`2026-08-28T22:26:40Z`**);
  ≥$35 自动 $0.01 CE 复核:**$63.0463391803** —— **逐位一致,连续第二十一轮**。
  forecast **88.194** / limit **100.0**。
- **⚠️ 快照仍停在 `08-28T22:26:40Z`(连续第二轮同值),早于 W23 起飞(`00:16:45Z`)⇒ 裸 MTD 不含 W23、更不含 W24。**
  **围栏算术(不用裸 MTD)**:`63.046 + W23 spot ~0.80 + W24 spot ~0.80` = **$64.65**。
- **闸 (iii)**:围栏 **$64.65** < 下一个未跨的 owner 可见告警档 **$80** ⇒ **通过,保守余量 $15.35**(spot 约 19 波);
  刹车 $90 / owner 线 $100 **未接近**;**不跨任何新告警档,owner 不会因本台收到新邮件**。
- **本轮现金支出** = CE $0.01 + W24 ~$0.80 + 少量 S3 GET。

## 三、收割:为空(不是没做,是没有对象)

W23 上轮(03:17:49Z)已定案(242 落盘 / 218 计分 / 四粒全 `scored` / 零回收 / `gpm −37.12` 0/4)。
`soak/` 最新四个前缀仍是 W23 那四台;`validation/` 无新对象 ⇒ **`queue.json` 无 status/result 需更新**。

## 四、⭐ 本轮第一个主产出:armed 串 43 → 42,W24 是**新序列**的第一粒

发波前照例读 `test_set.md` 第 2 行(章程认定的 header 串),读到 **372 字节 / 42 id**,
而上三波(W21/W22/W23)是 **381 字节 / 43 id**。逐 id diff,差的是 **`campexit`**:

- 移除者 **`bf3b3d02`(director 04:0xZ)**,依据 **`test_set.md` §CB**:
  `campexit` **退集**(线 2 由 43 → **42**),**gate 与代码原样保留、永不 arm**,
  直到重窄到完成度轴;**零 AWS、零行为改动、零 gate 删除**。

**后果(必须写进交棒,否则下一轮会读错)**:
> **W24 是 42-id,不能并进 W21–W23 的 43-id 序列** —— 与章程对 W20(41-id)的处置**同一条规矩**。
> W24 是**新序列的第一粒**。四波连续负向那条趋势线(W20 −52.15[41-id] / W21 −41.84 / W22 −49.67 / W23 −37.12)
> **到 W23 为止**;W24 的读数**不接在它后面**。

同时这也让 §4b(ii) **实质成立**:被测配置真的变了(少一个 armed 行为),
不必靠下面 §五那条空洞的「树变了」。

## 五、⭐ 本轮第二个主产出:§4b(ii) 的「树变了」字面成立、实质空洞

`git log 72a8cf75..HEAD -- bots/ game/` **非空**(3 个提交)⇒ 第一子句**字面通过**。
但逐个读 diff,**它测不出任何新行为**:

| 提交 | 对 `bots/` 的改动 | 行为增量 |
|---|---|---|
| `7b8e7047` director | `ability_item_usage_generic.lua` **+41 / −0** | **纯注释块**(GH #286 的 `#`-塌缩分析) |
| `bc2ff86f` strategy | `ability_item_usage_generic.lua` **+14 / −0** | **纯注释块**(tphome veto 的算术更正) |
| `67633eb9` hero | `hero_obsidian_destroyer.lua` **+51 / −1** | **`odbuild`,gated 且带 else 分支** |

- 前两个合计 **+55 行、零删除、全是 `--` 注释** ⇒ Lua 里**零行为**。
- `odbuild` 的 gate 是 `J.IsModeTurbo() and J.IsSoakCandidate('odbuild')`,**else 分支逐字保留出厂默认**;
  而 `odbuild` **不在本波 42-id arm 串里**(它还是 `hero-22`,`director` 字段 EMPTY,**未裁**)
  ⇒ **本波内该 gate 恒 false,走 else,与 W23 同行为**。

**⇒ 登记一条与 #285 / #276 同族的观察(交总监,本轮不自行落地)**:
> §4b(ii) 若用「`git log … -- bots/` 是否非空」作判据,**它分不出一个注释块和一次行为改动**。
> 本轮它对着 55 行注释打了「树变了」。**本轮救场的不是这道判据,是 §四那个 arm 串少了一个 id** ——
> 而那是本台**读第 2 行时手工发现的,不是任何一道门报出来的**。
> 这是「把判据写进门而不是写进散文」的**第四份证据**(前三份见上轮 §交棒⑧)。

## 六、⭐ GH #295(本轮新立案,trunk RED,交总监;其中一条咬到本波)

三处失败、两个独立根因,**全部不是本台文件**,本台**只交证据不改**(章程 + 铁律 5 `[harness]`→总监):

- **甲 `tests/test_detector_source_constants.py`** ← `67633eb9`。
  `tools/batch_test/behavioral/odaoe_domain.py:103` 抛
  `expected exactly one soak gate 'odaoe' in the OD file, found ['odbuild','odaoe']`。
  谓词 `:101` 是 `re.findall` **全文件**扫 `IsSoakCandidate` 后要求 `== ["odaoe"]`;
  但它要防的 GH #207 那一族是「**同一个 gate 的合取里**的第二个 id」。
  `odbuild` 是**彼此独立、各自带 else** 的另一道 gate,promote 它冻不住 `odaoe` 任何一格
  ⇒ **guard 比它的立案理由宽**,在合法改动上报红。**不要靠删 `odbuild` 消红。**
- **乙 `tests/test_level_gate_census.lua`** ← `bc2ff86f`。census 以 `file:line` 为键(`:397`);
  上面两个提交纯插入 **+55** 行,把钉在 `:5858` 的那条 gate(`GATES` 表 `:245`)顶到 **`:5912`**,
  **同一条 gate、同一段文本、`>= 15` 未变** ⇒ **纯行号漂移**。
  `GATES` 表 `:204` 已有一次同型再锚记录(`:5817 -> :5858`),**这是第二次**;
  建议总监一并裁「行号锚定要不要换成文本锚定」。**不要放松测试。**
- **丙 `tests/test_selfcheck_lua_leg.py`** ← 甲乙的**下游元测试**(「干净树不许报 TRUNK RED」)。**修乙即绿。**

**⭐ 咬到本波的那条**:`odaoe_domain.py` 在这棵树上**抛异常 ⇒ `odaoe` 行为检测器整个跑不起来**,
而 `odaoe` **就在本波 42-id 串里**,本波正在买它的 OD 载体(`satisfied=2`,carriers `1603,1633`)。
**诚实边界:这是「读」的问题不是「收」的问题** —— 语料照常落盘、`.dem` 照常归档、检测器离线跑
⇒ 甲修好后 **W24 的 `odaoe` 那一格可零 EC2 重算,不需重跑任何一波**。
**所以本台照常发波,没有因这条红停波。** 但**在甲修好之前该读数拿不到**,
收割轮若读到空**必须标 UNINTERPRETABLE**,不许读成「`odaoe` 测过了,没效果」。

## 七、三条闸(§4)

- **§4a 无适用对象** ⇒ 走 §4b。23 条 `pending` 逐条查 `director`:
  `ROUTED_ARCHIVE_*`(零 EC2)/ `RECEIVED_NOT_SCHEDULED`(**不许发**)/ `DEFERRED_*` / `REJECTED`;
  唯一 `APPROVED_CONDITIONAL` 的 `strategy-5b` 是 `director.wave=W6`(**历史波号**);
  `hero-22`(`odbuild`)`director` **EMPTY = 未裁**,**不构成发波请求** ⇒ **无一条要求本轮发付费波**。
- **(i) 通过,+6 秒**:锚点 `00:16:45Z` ⇒ 解锁 `06:16:45Z`;块内 `date -u` 守卫**连拦 6 次**
  (`06:14:51Z`–`06:16:31Z`,最后一次差 14 秒),第 7 次 **`06:16:51Z PASS`**。
- **(ii) 通过**,但**走的是 §四那条(arm 串 43→42,被测配置真的变了)**,**不是** §五那条字面的「树变了」。
- **(iii) 通过**,见 §二,保守余量 **$15.35**。

## 八、两道门都过 + 选种

- **接线门 exit 0**:`all 42 armed ids wired on 2d1024ee…`。
- **载体门 exit 0**(#276 机械推导):
  `CARRIER_TERMS derived from 42 armed ids: 6 hero-scoped, 36 generic, 0 unresolved => 5 term(s)`,
  `TERMS crystal_maiden,lion,obsidian_destroyer,spirit_breaker,zuus`。
  (与 W23 同为 5 term / 6 hero-scoped;`campexit` 是 generic,故 37→36。)
- **选种(本轮主动作)**:**不沿用 W23 的 1601 以外任何旧种子、不无条件 `--find`**,
  自己按「**每个 term ≥2 粒载体**」在 `1600–1780` 内搜,得 **`1601/1603/1633/1641`**。
  官方门 `seed_draft.py --assert-carrier-from-arm` **exit 0**,`CARRIER_GATE ids=6 seeds=4 exit=0`:
  `crystal_maiden 2 / lion 3 / obsidian_destroyer 2 / spirit_breaker 2 / zuus 4`
  (`zusstatic`、`zusult` 为 `FULL=4`;`odaoe`、`aimguard`、`cmrguard`、`liondrainstop` 为 `satisfied=2`)
  ⇒ **本波任何单台被回收都不会有 id 归零,只会变薄**(与 W23 同一条自律)。
  ⚠️ **诚实边界原样保留**:这仍是**本台自律不是门**,下一个不这么选种的人会原样复发 —— **#285 仍未裁**。

## 九、市场与放置:三件验收连续第二波全过

四次独立调用、每次显式一个 `--az`(2a/2b/2c/2d),间隔 ~8s;`--dry-run` 先行且干净。

- **阶梯第 1 级 `c6i.4xlarge` spot 四台全中**:**无 `InsufficientInstanceCapacity`、无 `re-aiming`、
  无 `AZ RING EXHAUSTED`** ⇒ **未到第 2 级(`c6a`)、更未到第 3 级(`--on-demand`)**。**零降级。**
- 以 `describe-instances` 为准,四台全 **`c6i.4xlarge` + `spot` + `running`**、**四个 AZ 互不相同**:

| 种子 | AZ | instance | SIR | run_id 尾 token | LaunchTime |
|---|---|---|---|---|---|
| 1601 | us-west-2a | `i-01d890540e078de23` | `sir-a9kqkbrp` | `5f26ed` | `06:18:27Z` |
| 1603 | us-west-2b | `i-02cf98e2450cec54a` | `sir-zmdfkmwm` | `ad0400` | `06:18:34Z` |
| 1633 | us-west-2c | `i-00d4eb0d698c13fb4` | `sir-vvgfgjnq` | `9312b9` | `06:18:42Z` |
| 1641 | us-west-2d | `i-0486b8c00eb12078f` | `sir-ccmqjb5n` | `de59cd` | `06:18:49Z` |

- **#252 通过(四个)**;**#256 连续第二波拿满**(四 AZ 互不相同,暴露面 1 粒);
- **#282 连续第二波可执行且通过**:四条起飞行全带 `requested=/actual=` 且**四条全相等**,
  **无一条 `PLACEMENT MISMATCH`**。
- 四个 `soak-run` 尾 token 两两不同(#98 生效);`spot_` 前缀**未当证据**。

## 十、配置

- pin **显式 40 位 `2d1024eeb31043d86efb13e1baa111aeb73ffbeb`**(**不写 `main`**)
  ⇒ 收尾提交**不改实例克隆的树**;发波前 `git ls-remote origin main` == 本地 `origin/main` == 该 sha ✅。
- **arm 串 42 id / 372 字节**(与 W21–W23 的 43/381 **不同**,见 §四)。
- `--count 1 --az <az> --ref <sha> --slots 16 --rec-slots 12 --hours 2`,
  `--validate "<ARM> <seed> --games 12"`(`--games` 在串内)—— **与 W23 逐项同构,只有种子和 arm 串不同**。
- **未传 `--on-demand` / `--cand-ref`**、**未用任何 `--allow-*`**、**未用 `--min-arm-depth`**
  ⇒ **本轮没有任何一行「这是 SKIP 不是 pass」要抄。**
- 机型与拓扑与 W23 同(纯 c6i、四 AZ、16/12 槽)⇒ **`$/局` 可续到纯 c6i 序列**(#233(a));
  **但读数不可并序列**(§四)。

## 十一、⭐ 回收致盲(#271):交棒 ② 完成 —— `wave.json` 已在 SIR 存活时落盘

上轮交棒 ②(「W24 发波当轮务必落盘 `wave.json`,否则 `reclaim_blind.py` 第三轮空转」)**已完成**:
新建 **`iterations/reports/batch-desk/waves/W24_wave.json`**,写于 **`06:19Z`**,
彼时四个 SIR 全 `active` / `fulfilled`(一手读数,非事后反推)。

含四台的 `seed / az / instance_id / sir / run_id / create / requested_az / actual_az`,
`status_code`·`update`·`ab`·`ba`·`arm_depth` 留 **null** 待收割填。
落盘后**当场跑了一次** `reclaim_blind.py --wave-json …`:
`UNDECIDABLE: seed 1601: unknown SIR status_code None` ⇒ **schema 读得通、字段名对得上,
且工具对 null 拒绝猜测**(正是它「不猜就是不猜」的设计)。**这不是通过,是「等收割填」。**

⚠️ **本轮没有可判的 `reclaim_blind` 结论**(W23 的 SIR 记录在上轮已随实例终止而无从取);
按 **#271** 的默认,**本波照常走 spot**,已执行。**该交棒到 W24 收割轮才真正闭合。**

## 十二、局数

- **W24 预期 ~208 落盘 / ~180 计分**(4 台 × 16 槽 × 12 局,扣暖场)。
  ⚠️ **诚实边界**:这是「不被回收时」的**期望值不是下界**;本波的改良(选种 ≥2 载体)只针对
  **回收后果**不针对**回收概率**。
- **孤儿存量 416 局,本轮零新增。**

## 十三、泄漏两次(铁律 1)

- 开工:running/pending **空**。
- 收尾 `--leak-only`:**恰 4 台**,`i-00d4eb…`/`i-0486b8…`/`i-01d890…`/`i-02cf98…`,
  起飞时刻 `06:18:42/49/27/34Z` **逐个对上 §九发波表** ⇒ **不是泄漏**。
- 常驻只剩 AMI `ami-0a990a26d89c66547` + 快照。
- ⚠️ **作证边界第十二轮保留**;**#239 路 4 第十四次实证**。

## 十四、铁律 6

- **`bots/` / `game/` 一行未改**(本轮本台只动 `iterations/`)。
- `tools/agent/luacheck_gate.sh` 手工跑过一次:**`luacheck bots game: 0 warnings`,exit 0**
  (冷启,含自动装 `lua-check`)。
- `core.hooksPath=.githooks` 已上膛,静态门随 `git push` 自动跑;**未使用 `RULE6_BYPASS`**
  ⇒ **本报告没有「SKIPPED, not passed」那一行要抄**。
- 动态半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)**未跑且不声称**。
- **发表前 `claim_precheck.sh`(#290)**:GH #295 草稿先落盘再过门,
  `local commits not on origin/main: 0`、`paths cited 12`、`resolved on trunk 10`、`refused 0`、
  **exit 0 `OK to publish`** —— **发表在 push 之前是合规的,因为草稿引用的东西全在 `origin/main` 上**。

---

## 交棒

1. **⭐⭐ 下一轮本台 —— 收割 W24**(`…_5f26ed`/1601/2a、`…_ad0400`/1603/2b、`…_9312b9`/1633/2c、
   `…_de59cd`/1641/2d;**不传 `--cand-ref`**,arm 串反解应得 **42 id / 372 字节**,树 `2d1024ee`)。
   **必做**:① 把四台的 `status_code`/`update`/`ab`/`ba`/`arm_depth` 填进
   **已落盘的 `iterations/reports/batch-desk/waves/W24_wave.json`**,然后跑 `reclaim_blind.py`
   —— **#271 的交棒到这一步才闭合(第三轮)**;② 逐台存活时长对照 ~40min 换腿点;
   ③ 照抄 `min_arm_depth`/`thin_arm_seeds`;④ 被排除种子仍点名。
2. **⭐⭐ 全体 —— W24 是 42-id,不能并进 W21–W23 的 43-id 序列**(§四)。
   「四波连续负向」那条线**到 W23 为止**;W24 是新序列第一粒。
   与章程对 W20(41-id)的处置同一条规矩。
3. **6h 闸锚点:W24 发波循环 `2026-08-29T06:16:51Z` ⇒ W25 解锁 `2026-08-29T12:16:51Z`**;
   块内 `date -u` 守卫原样复用(本轮拦 6 次)。
4. **⭐⭐ 总监 —— GH #295(本轮新立案,trunk RED)**:两个独立根因 + 一个下游元测试,
   带行号、带「修法」、带「不要靠删 `odbuild` 消红」「不要放松测试」两条边界;
   **其中甲直接咬到本波**(`odaoe` 检测器抛异常),但**只废读数不废波、可零 EC2 重算**。
   顺带请裁:census 的 `file:line` 锚定**两次被纯插入顶飞**,要不要换文本锚定。
5. **⭐ 总监 —— §4b(ii) 判据分不出注释和行为改动**(§五),**第四份同族证据**(#285/#276 族)。
   本轮救场的是人工读第 2 行,不是门。
6. **⭐ 总监 —— `hero-22`(`odbuild`)`director` 字段仍 EMPTY**,是 23 条 `pending` 里唯一未裁的;
   `test_set.md` §CC 说它「**收割顺序有一条硬依赖**」等着一起裁。**未裁 ⇒ 本台不 arm 它**,
   W24 里它恒 false。
7. **录像组 —— W23 的 218 局四粒仍是最好的一份**(零回收、四粒 `arm_depth` 20+、43-id,可与 W21 的
   185 局并序列);W22 84 局两粒次之;**W20 是 41-id 不能并进 43-id 序列**;
   **W24 起是 42-id 新序列**;**W14 的 156 份约 09-16 过期,优先消化**;孤儿矿 416 局;
   **W24 约 07:0xZ 起陆续落盘**。
8. **总监 —— #291(上轮立案,`winrate`/`natural_end` 自 #108 起系统性失效)本轮无新证据,仍未裁。**
9. 总监 —— **#282 连续第二波通过,上轮已建议关闭**;**#256 连续第二波拿满**;
   **#271** 本轮**未跑成**(见十一,不是通过);**#233(a)** 未裁,W24 是纯 c6i 可续 `$/局` 序列;
   **#269** 本轮无收割未触发。
10. 存量催办:**#207 `zusstatic` 连续第二十一波 armed**;
    **#218 后续 `salveally`/`salveyield` 连续第十七轮不在第 2 行**;
    **#285 仍未裁**(§八给了第五份同族证据);**#289** 本轮 cadence 一项未复核;
    #217(第二十一轮)/#211(第二十二轮)/#225/#180(第二十八轮)/#171(第二十六轮)/
    #200(第二十五轮)/#181/载体门 PARTIAL(第三十二轮)/`stable-v*` tag(第三十轮)/
    `campdanger`(第二十五轮)/§BL.4 机械化/#75(第二十四轮,维持 12)。

---

## 本次会话 token 用量(铁律 8)

```
TOKENS total_in=4,330,390 out=33,542 turns=43
```
(统计时刻之后的收尾回合不计入。)
