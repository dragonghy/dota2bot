# 批测台 2026-09-08T03:16–03:3xZ —— 发波轮:W55 起飞(4/4 spot,**首次四个不同 AZ**)

**一句话**:上一轮交棒指定的发波轮如期执行,四道闸**全部在起飞前跑完且全绿**(其中闸 (iv)
在 W54 自己的记录上**从 W53 的连续四轮 `exit 2` 恢复成 `exit 0`**),W55 四台 spot 起飞;
本轮最该记住的不是波,是**开工自检的 trunk red 从「一个实例」变成了「一个不收敛的循环」**
—— GH #596 点名的那条 `io.popen` 已被总监 `37483f4f` 登记,而**紧接着的下一个英雄组提交
`2f012664` 又带进同一形状的第二条**,于是同一道门在 24 小时内红了两次,中间只隔一次修复。

---

## 一、自检(铁律 10)

⚠️ **第一条命令第十九轮踩同族坑**:`… 2>&1 | tail -60` ⇒ 工具当场自拒
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked`,
harness 报的 `EXIT=0` 是 `tail` 的码。改重定向到文件后台重跑,**真退出码 `SELFCHECK_EXIT=3`**。

`exit sources` 节逐字:

```
legs run              : 10
FINDINGS (exit 3)     : cadence queue-rulings owed-executions trunk-red(python)
UNCERTIFIABLE (exit 2): none
NOT RUN (inside a leg): tests/test_rc_wrapper.py tests/test_selfcheck_lua_leg.py
selfcheck worst exit: 3
```

⛔ **`NOT RUN` 那两条不是通过**(腿内两个检查打了 `UNCERTIFIABLE`,`this is not a pass and
not a failure`)。`cadence` / `queue-rulings` / `owed-executions` 三项属别组,本轮不认领。

## 二、⛔ trunk python RED —— 同一道门 24 小时内第二次红,而这次的读数是「循环不收敛」

python 腿:`109 passed, 1 failed, 2 uncertifiable`,唯一 FAIL 逐字

```
FAIL  tests/test_bots_walk_farm_only.py
      8 checks, 1 failed  [218 commands executed, 20 unresolved]
      FAIL: a tests/ io.popen command cannot be resolved statically and is not on the hand-read
      list -- read it, then add it to UNRESOLVED_HAND_READ with the value its callers pass
      (do not delete this check): ["tests/test_lion_ult_reach.lua  ::  'ls ' .. dir .. ' 2>/dev/null'"]
```

**独立复现,在读者看到的那棵树上**:`git diff origin/main -- tests/ bots/` **为空**
(HEAD == origin/main == `7d960677dbc575ac8fce2712bd03742541195ff2`),单跑
`python3 tests/test_bots_walk_farm_only.py` ⇒ **`WALKFARM_EXIT=1`**,同一行逐字复现
⇒ **红在 main 上,不是本台工作树的伪影**,不需要 `git stash`。

⭐ **本轮的新读数不是「又红了」,是这两个 commit 的顺序**:

| commit | 做了什么 | 对这道门 |
|---|---|---|
| `37483f4f` director: trunk python half 1 red -> 0 red | 把 `test_lion_considere_earlyreturn_domain.lua` 登记进 `UNRESOLVED_HAND_READ`(现坐在 `tests/test_bots_walk_farm_only.py:107`) | 红 → 绿 |
| `2f012664` hero: Lion ConsiderR commits the finger 400u out of range | 新增 `tests/test_lion_ult_reach.lua`,**带进同一形状的第二条 `io.popen`** | 绿 → 红 |

⇒ GH #596 的**实例**被修好了,**形状**没有。计数一起走:`208 commands / 18 unresolved`
→ `218 / 20`。**「每次落地后由下一个读到的人登记」这条路本轮被自己证伪** —— 它不是没人做,
是做了之后立刻又被下一个提交打破。本台按边界**不写别组的 `tests/`**,读数与归因发在 #596 追评。

## 三、成本与围栏

- `AWS_SETUP_EXIT=0`,`AWS ready: arn:aws:iam::581436534924:user/dota2bot-agent`。
- `COST_EXIT=0`。MTD **`$66.033`**(免费 `budgets`,`budget refreshed 2026-09-08T00:43:06Z`),
  ≥$35 ⇒ 自动 `$0.01` CE 复核 **`66.0333539768`**,**两源逐位一致**。`forecast 166.167`。
- 常驻成本仍只有 AMI `ami-0a990a26d89c66547`(`dota2bot-batch-20260718`)。
- ⭐ **本波不跨任何告警档**:`$50.00 crossed (AWS state=ALARM)` 是本轮**之前**就已跨过的
  (owner 那封邮件早已发出),`$80.00` 与 `$100.00` 双双 `NOT yet crossed / state=OK`,
  投影 `$68.483` 够不着 `$80.00` ⇒ 章程的跨档披露行**本轮不欠**。这一句写下来是为了让下一个
  读者不必重新推导。

## 四、四道闸(全部在起飞前跑完)

| 闸 | 读数 |
|---|---|
| (i) 节流 | `THROTTLE_EXIT=0` `UNLOCKED`。决定时点 03:17:02Z(`margin +111s`),**动作时点 03:21:13Z 重跑**(`margin +362s`),是 `spot_run.sh` 循环前的最后一条命令(GH #469) |
| (ii) 有新东西可测 | 满足,**两条腿各自独立成立**:main 从 `0d62930` 走到 `7d96067`(`hero_lion.lua +179` / `jmz_func.lua +110` / `mode_retreat_generic.lua +20` / `test_set.md +181`);臂串 46 → 45(总监 §FX 退集)。换代 ⇒ 本家族累计种子 0,W55 是它的第一张 slate |
| (ii') 接线 | `ARM_WIRING_EXIT=0`,`all 45 armed ids wired on HEAD`,**无 `DO NOT LAUNCH` 行** |
| (iii) 围栏 | `WAVE_FENCE: CLEAR (exit 0)`,围栏**本轮现算**。`--pending 1.350`(**ASSERTED 分支**)/ `--planned 1.100` ⇒ 投影 `$68.483`,围栏 `$80.00`,`operative ceiling = min(fence,brake) = $80.00`,**余量 `$11.517`**。`accrual check : CERTIFIED ZERO (0 accruing instances account-wide)`。**未用 `--no-accrual-check` ⇒ 无「SKIPPED, NOT CERTIFIED」行** |
| (iv) reclaim-blind | **`RECLAIM_BLIND_EXIT=0`**(在 **W54 自己的记录**上跑,收割义务 ④ 就此结清)。逐字 `yield: 3 paired seed(s) of 4 machine(s)` / `attribution: 1 machine(s) reclaimed before the flip` / `VERDICT: not blinded` / **`NEXT WAVE: spot (the default; GH #158 unchanged)`** |

**`--pending 1.350` 的来源**(不是猜):W54 四台 spot 的账单侧结算 **`$0.7271`**(上一轮逐机按
各自 AZ 报价算的)+ 那台按需补跑机(机时下界 `0.7178 h` × on-demand 费率)≈ `$0.55–0.61`。
W54 起飞于 09-07T21:14Z(约 6h 前),而预算刷新于 09-08T00:43:06Z,**落在 EC2 的 4.3–11.3h
滞后窗口里** ⇒ 按条款 (乙) **不许**用「MTD 增量对得上波费」推断它已计入,一律当未计入申报。

**`--planned 1.100` 而不是 W54 用的 `0.85`**:W54 自己的结算表明,那一波之所以只花 `$0.7271`,
是因为一台机器 97 秒就被抢占;**归一化后假想的四台满波 ≈ `$0.958`**。本波四台全在四个不同 AZ,
被抢占的先验更低 ⇒ 用 (乙1) 常数 `$1.10` 报 planned 是**保守侧**。

### ⭐ 闸 (iv) 的一句边界(不含糊过去)

工具的表头行仍然写着 `changeover 40.0 min (measured bracket 34.80 < x <= 40.63, margin above
the constant 0.63 min)`。上一轮已经立案:**那个 `measured bracket` 是写死的历史常数,却长得像
对手上这一波的测量**(交棒 ① 给总监,GH #591 一线)。本轮的 `exit 0` **不构成**关于那个常数的
任何证据,两件事分开记。

## 五、树的核对(2026-07-23 那次险些测错树)

`git ls-remote origin main` 读**两次**(选种时 03:17Z 一次、发波循环前 03:21:13Z 一次,
脚本对漂移写死 `exit 9`),两次都是 **`7d960677dbc575ac8fce2712bd03742541195ff2`**,
且本地 `HEAD == origin/main`。`--ref` 传的是 **40 位 SHA 不是分支名**,四个 run_id 里逐字带着它。

## 六、臂串与选种

| 量 | 读数 |
|---|---|
| id 数 | **45** |
| 字节 | **408** |
| md5 | **`f4292f7bb9a5f112ed34af62cbe3c2a8`** |

三个数**本轮现算**(`split(',')` + md5,不是 `wc -l`),与 `test_set.md` 自己第 3 行的声明
**逐位一致**。与 W54 的 46-id 串相比 **−1 / +0**:一条 `退回出集`(总监 §FX,09-07T22:xxZ)。
⛔ **不是 reject**,gate 与代码逐字保留。**P4.2 入集冻结本轮继续成立:新增 0。**

⇒ **W55 与 W54 / W53 不同家族,不可并池**(串变了 **且** 树变了,两条各自充分)。
**发波时登记,不留到收割轮再判。**

- `carrier_terms.py --arm <45 串>` ⇒ `10 hero-scoped, 35 generic, 0 unresolved => 7 term(s)`,
  `TERMS crystal_maiden,lion,obsidian_destroyer,pudge,skeleton_king,spirit_breaker,zuus`。
  与 W54 **逐字相同**,而这是对的:退出的那个 id 是 generic,**generic 动不了 hero term**。
- `seed_roster_index.py --build --jobs 8`(选种**之前**,章程步骤 5)⇒ `ROSTER_EXIT=0`,
  索引 **241** 粒。⭐ **这一步本轮是有分量的**:重建前的索引最大种子只到 `7162`,
  **W54 的 7527/7529/7719/7789 四粒不在里面**;重建后它们在。
- 窗口右移到 **`[7901,8300]`**,**与 241 粒索引的交集是空集**(按集合求交,不逐粒看),
  四粒选中种子也都不在索引里。窗口内未用 **400** 粒,term-popcount 分布
  `{0:35, 1:101, 2:178, 3:69, 4:16, 6:1}`。
- 选中 **7921 / 7964 / 7992 / 8010**(在 60 粒最高 popcount 的未用种子里,极大化**每 term
  载体数的最小值**,同分比总数;**min 2 / total 18**,比 W54 的 min 2 / total 17 略好)。
- **闸(载体)**:`seed_draft.py --assert-carrier-from-arm`(term **由臂串导出,不手打**,GH #276)
  ⇒ **`CARRIER_GATE ids=10 seeds=4 exit=0`**。每 term 载体数:cm 2 / lion 4 / od 3 / pudge 2 /
  wk 3 / sb 2 / zuus 2 —— **每一条 ≥ 2**,与 W50/W52/W53/W54 同一条杆。

## 七、起飞与点名

采集配置 `--slots 16 --hours 2 --rec-slots 8 --games 12`,与 W39–W54 **逐字节相同**
(`--rec-slots 8` **显式传**,脚本默认是 1)。先 `--dry-run` 读计划行(`DRYRUN_EXIT=0`),再实发。
四次独立调用,一次一个 AZ:

| 种子 | 实例 | AZ(请求 → 实得) | LaunchTime | `InstanceLifecycle` | SIR / 状态 |
|---|---|---|---|---|---|
| 7921 | `i-0584e7811fc3909cf` | 2a → **2a** | 03:21:15Z | **`spot`** | `sir-a5hfmiij` active/fulfilled |
| 7964 | `i-07b5b4ae1438e756e` | 2b → **2b** | 03:21:17Z | **`spot`** | `sir-9fgfmsmk` active/fulfilled |
| 7992 | `i-0750576674308b467` | 2c → **2c** | 03:21:19Z | **`spot`** | `sir-iemzmq6h` active/fulfilled |
| 8010 | `i-0f0373967839c48ac` | 2d → **2d** | 03:21:22Z | **`spot`** | `sir-yz4fpiph` active/fulfilled |

- **四台全 `InstanceLifecycle=spot`,零按需** ⇒ owner GH #158「spot 优先」本波**零降级执行**。
  证据是 `InstanceLifecycle`,**不是 run_id 的 `spot_` 前缀**(那是硬写字符串)。
- ⭐ **AZ 分散度 4/4,四个请求全部实得,零 ring-walk —— 这是记录上第一次满分。**
  W54 是 3(一个 2b 请求落进了 2c)。**收割时要记住的后果**:本波没有任何一个 AZ 带两粒种子,
  **单个 AZ 容量事件只损失一粒**,而 W54 的 2c 带两粒。
- **SIR 四行在发波轮第一手读到**(GH #556:SIR 行 ~1–3h 就过期),`Status.UpdateTime` 四条
  同为 `03:21:35Z`,**已直接写进 `W55_wave.json` 的 `update` 键** ⇒ 即使收割轮迟到,
  闸 (iv) 要的那个读数**这一波丢不了**(GH #375 的形状在本波被提前堵死)。
- 四次 `spot_run.sh` 的退出码**裸读、不过管道**(W42 正是在这里把 `tail` 的码读成了脚本的),
  四个 `SPOT_RUN_EXIT=0`;但**成败以 `describe-instances` 为准**,已如上核对。
- 自毁:`spot_run.sh` self-terminate + 2h watchdog + one-time spot request(被抢占不会静默重启)
  + `instance-initiated-shutdown-behavior=terminate`。波预算守卫未拒绝(12 局 / 16 槽装得进 2h)。

## 八、收割:本轮无欠

W54 已在 00:12Z 轮全量收割(4/4,`RV_EXIT=0`)。S3 `soak/` 03:19Z 列表最新的就是 W54 的
五个 run 前缀,**之后没有新对象** ⇒ 无欠收割。起飞前 `describe-instances`(running,pending,
**账号级不按 tag 过滤**)返回**零行**,`check_costs.sh` 的同一节亦空。

## 九、泄漏检查

起飞后 `describe-instances --filters running,pending`(**不按 tag 过滤**)= **恰好 4 行**,
四行全是 W55 的,tag 全为 `dota2bot-soak-spot-1`,lifecycle 全 `spot`
⇒ **无第五台、无孤儿、无外来负载**。常驻成本仍只有那一个 AMI。

## 十、铁律 6 / 11

- `bots/` / `game/` / `tests/` / `tools/` **本会话一行未改**(只动 `iterations/` 下的报告、
  `W55_wave.json`、章程本节)。
- 静态门读数见文末。**未用 `RULE6_BYPASS`。**
- **铁律 11**:全程无 `requires approval`,无权限对话框,无 MCP 拒答。

---

## 交棒

1. ⭐⭐⭐ **总监 —— GH #596 该从「修实例」升级成「修形状」**。本轮的读数不是复发,是
   **不收敛**:`37483f4f` 把第一条 `io.popen` 登记好,**紧接着的 `2f012664` 就带进了第二条**,
   中间没有任何东西挡着。这正是 #596 正文里自己提的那句「同族是否该升成一条硬规则」的
   实证材料。本台按边界不写别组 `tests/`,读数与归因已发 #596 追评。
2. **总监 —— 闸 (iv) 的 `measured bracket` 常数**(上一轮交棒 ①,GH #591 一线)本轮**未动**。
   W54 的 `exit 0` 说明那条 W53 的 `exit 2` 是**输入特定**的,不是常数坏了;
   但「写死的历史常数长得像本波测量」这个缺陷**与它是否恰好通过无关**,仍然待裁。
3. **录像组 —— W55 的帧通道**:`--rec-slots 8`,四台 × 8 槽。本波臂串 45 id,
   载体分布见第六节(lion 4 粒最厚,cm/pudge/sb/zuus 各 2 粒是最薄的一档)。

**下一轮本台** = ① **收割轮**(W55,2h watchdog ⇒ 约 05:2xZ 之后全部落地);
② 收割义务五条见 `W55_wave.json:harvest_obligations`,其中**「不可并池」已在发波时登记**、
**`update` 键已在发波时填好**,收割轮不必再补;③ 闸 (i) 下次解锁按**末台** `03:21:22Z` 起算,
但**不要抄这个时刻,跑 `wave_throttle.py` 读它打的那一行**。

---

详见 `iterations/reports/batch-desk/waves/W55_wave.json`。

## 铁律 6 读数

- 静态门:**`GATE_EXIT=0  CLEAN`**,`luacheck bots game: 0 warnings`,门自己装的 luacheck
  (`installed luacheck (it was missing; apt package lua-check, bounded)`)。**未用 `RULE6_BYPASS`。**
- `tools/agent/arm_push_gate.sh` ⇒ `ARM_GATE_EXIT=0`(`core.hooksPath=.githooks, already set`)。
- 动态那半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)**本轮未跑,本节不声称它**。

## 本轮 token

`TOKENS total_in=7,064,040 out=45,901 turns=58`(到统计时刻为止;统计后的收尾回合不计入)。
