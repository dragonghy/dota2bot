# batch-desk 2026-09-11T15:14Z — 发波轮 W66(四台 spot 起飞,四道闸全绿)

**一句话**:W66 四台 spot 15:24:3xZ 起飞,**四道闸当轮现跑全绿**;⭐⭐⭐ 头号产出**不是**那一波,
是闸 (iv) 起飞前**根本打不开** —— 它要的四个码在 AWS 上**已经读不到了**(SIR 全账户零行),
只能从上一轮报告里抄;⭐⭐ 第二是 **W66 是连续第三个深度 0 的新家族**,而这一次串在**一天之内**动了两回。

---

## 〇、本轮支出与围栏一览(全部当轮现跑,不得抄作下轮前提)

| 项 | 读数 |
|---|---|
| `AWS_SETUP_EXIT` | `0`(容器无 CLI,脚本自装 `aws-cli/1.46.1`;身份 `arn:aws:iam::581436534924:user/dota2bot-agent`) |
| `COST_EXIT` | `0`,MTD **`$80.413`**,`budget refreshed 2026-09-11T13:49:53Z`,CE 复核 `80.4127597815` 逐位一致 |
| 本轮新增支出 | **一波 spot 申报 `$1.100`** + CE 复核 **`$0.01`** |
| 闸 (iii) | `FENCE_EXIT=0`,`projected $83.863`,`headroom $1.137` |
| 泄漏 | 发波前两条路径皆空;发波后**恰好 4 台**全是本轮的,无第五台 |

⚠️ **快照这次真的动了**(上一轮 `08:15:19Z` → 本轮 `13:49:53Z`),MTD `$79.547 → $80.413` = **+$0.866**。
⛔ **不得读成「W65 花了 $0.866」**:诚实 cutoff = `13:49:53Z − 11.3h` = **`2026-09-11T02:31:53Z`**,
而 W65 首台 `09:23:35Z` 远在其后 ⇒ **W65 整波仍一分未落账**。这 `$0.866` 里能确定的只有
「cutoff 之前发生的事」,本轮不给它归因。

---

## 一、⭐⭐⭐ 头号:闸 (iv) 起飞前打不开,而修好它的那四个数**今天已经不在 AWS 上了**

`reclaim_blind.py --wave-json .../W65_wave.json` 第一次跑 **`RECLAIM_BLIND_EXIT=2`**,逐字:

```
UNDECIDABLE: seed 10926: missing leg count 'ab'
```

`W65_wave.json:machines[]` 四行**只有起飞侧的字段**(seed / instance_id / az / lifecycle / launch_time),
**没有 `status_code`,也没有 `ab`/`ba`/`arm_depth`** —— 而这三样正是闸 (iv) 判「这一波瞎没瞎」的全部输入。

**回填做了,来源**故意**分成两半登记**(`W65_wave.json:gate_iv_backfill_20260911T15xxZ` 逐字):

- `ab` / `ba` / `arm_depth`:**程序化**读 `W65_verdict.json:per_seed[].ab_games/ba_games/arm_depth`
  (28/14、39/19、33/23、28/14)⇒ **第一手,机器读的**。
- `status_code` / `survival_min`:**抄自上一轮报告**`20260911T121500Z.md:145-147`
  (四台全 `instance-terminated-by-user`,机时 0.7158/0.9133/0.9781/0.7492h)。

⭐⭐ **为什么第二半只能抄** —— 本轮**真的去读了**,读到的是空的:

```
awsx ec2 describe-spot-instance-requests ... --output text   => SIR_EXIT=0, 0 行
```

**全账户零行**。W65 四台 `10:22Z` 前后自毁,本轮 `15:2xZ` 去读,**中间只隔约 5 小时**,
它们的 spot 请求就已经**从 API 里过期消失**。上一轮 12:07Z 还读得到(那正是它量出
`0.8391 h/台` 的来源),本轮读不到。

⇒ **一条可操作的硬结论,交总监/harness**:
**SIR 派生的一切读数(GH #454 的 (乙1)(乙2)、闸 (iv) 的 `status_code`、机时、抢占归因)
有一个「必须在**同一轮**或**紧邻下一轮**取走」的**保质期**,而这件事此前没有任何地方写着。**
本轮之前,大家默认「波次记录里没写没关系,想读随时能从 AWS 读回来」——
**这个默认今天被证伪**。失效方向是**危险的那一侧**:它不报错,`describe-spot-instance-requests`
返回 `exit 0` 和一个**空列表**,而「零行」与「零台被抢占」在 shell 里长得一模一样。
(与章程里「shallow clone 的空输出被读成无漂移」**同族**:空输出被读成好消息。)

⚠️ **诚实边界三条**:
1. 本轮**没有**测出那个保质期是多久,只测到「**5 小时后已经没了**」和「**~2.7 小时时还在**」
   (上一轮 12:07Z 读到了 09:23Z 起飞的机器)⇒ 区间是 `(2.7h, 5.0h]`,**不是一个常数**。
2. 回填进 `W65_wave.json` 的那两个字段**是转载不是重读**,后续任何人**无法**再从 AWS 核对它们;
   这一点写在记录自己里,不靠人记得。
3. 本轮**不主张** W65 的那四个码是错的 —— 上一轮是第一手读的,且四个码彼此自洽(零回收 + 满配对)。

**回填后重跑 `RECLAIM_BLIND_EXIT=0`**,逐字:
```
yield      : 4 paired seed(s) of 4 machine(s)
attribution: 0 machine(s) reclaimed before the flip
VERDICT: not blinded -- the wave delivered 4 paired seed(s).
NEXT WAVE: spot (the default; GH #158 unchanged).
```
⇒ **W66 的市场类型是「闸裁的 spot」,不是「读不到码的保守默认」。**

---

## 二、四道闸,逐道现跑

**(i) `wave_throttle.py`** —— 本轮跑了**两次,两次结论相反,而这正是它立法的那句话**:
- 决定时点 `15:15:16Z`:**`THROTTLE_EXIT=3`**,逐字 `margin : 508s SHORT of unlock` /
  `A launch at this instant BREACHES gate (i) by 8m28s.`
- **发波时点**(写进发波脚本,**排在第一条 `spot_run.sh` 之前的最后一条命令**):
  **`THROTTLE_AT_LAUNCH_EXIT=0`**,逐字 `anchor wave : W65 (4 machine(s))` /
  `unlock : 2026-09-11T15:23:44Z` / `now : 2026-09-11T15:24:06Z` / `margin : +22s past unlock` /
  `WAVE_THROTTLE: UNLOCKED (exit 0)`。
  ⭐ 脚本在 `exit != 0` 时 `exit 9` 不发波 —— **闸是代码里的分支,不是我读完记住的一句话**
  (三次历史突破 W12/W20/W44 都是「钟只读了一次,然后去做五分钟的门,回来直接发」)。

**(ii) 有新东西可测** —— **腿 1 成立,串变了**:W65 飞 **34 id / 309 B / md5 `7be691dc…`**;
本轮 `origin/main:test_set.md` 第 2 行当轮现算 **30 id / 266 B / md5 `7d4c638dab526c68d9404526a3195663`**。
两个 promote 原子干的:`liondrainstop`+`tpdeathbuy`(10:37:01Z,34→32)、
`zusult`+`zusboltdom` 作为**一个原子**(commit `2a82d6b`,32→30)。腿 2(树漂移)**没用上也不主张**。

**(iii) `wave_fence.py --planned 1.100 --pending 2.350`** —— **`FENCE_EXIT=0`**,逐字:
```
WAVE_FENCE: CLEAR (exit 0) -- gate (iii) passes on the fence DERIVED THIS RUN ($100.00);
ruling GH#721/director-20260910 was carried but NOT needed for this wave. Do not copy $100.00 forward; re-run the tool.
DIRECTOR CROSSING: ceiling $85.00 by ruling GH#721/director-20260910, expires 2026-09-30T23:59:00+00:00
accrual scope    : 17 region(s) read [ap-northeast-1, ap-northeast-2, ap-northeast-3, ap-south-1, ap-southeast-1, ap-southeast-2  +11 more]  <- COMPLETE: every region this account has enabled
accrual check    : CERTIFIED ZERO (0 accruing instances account-wide), but --pending $2.350 was given and is used as the larger claim.
budget filters   : none
```
`actual $80.413` / `pending $2.350` / `planned $1.100` / `projected $83.863` /
`fence $100.00` / `brake $90.00` / `operative ceiling $85.00` / **`headroom $1.137`**。
**未用** `--no-accrual-check`(无 SKIPPED 行);**未传** `--director-crossing`(那条裁定是工具**自己**
从 `iterations/director_rulings.json` 读的,不是本台声明的)。
`$50` 与 `$80` 两条 ACTUAL 告警**都已越线**(`state=ALARM`),`$100` 仍 `OK`。

**`--pending` 自推导,逐波从波次记录本身核过**(cutoff `02:31:53Z`):
W65 末台 `09:23:44Z` 在 cutoff 之后 ⇒ spot **`$1.10`**;
W64 两台**按需补跑机** `03:19:56Z`/`03:19:58Z` 也在 cutoff 之后 ⇒ **`$1.25`**;
W64 主波四台 `00:2xZ` 在 cutoff **之前** ⇒ 已落账,不重复计。合计 **`$2.350`**。
⚠️ 工具只点名 `must cover : 1 wave(s) listed above (W65).` —— **补跑那 `$1.25` 是本台手加的**,
因为普查读 `machines[]` 不读 `rerun[]`(**GH #742**,06:15Z 那轮开的)。
**失效方向**:照工具点名的清单付钱,本轮会**少报 `$1.25`**。

**(iv)** 见 §一,`RECLAIM_BLIND_EXIT=0`,`NEXT WAVE: spot`。

---

## 三、⭐⭐ 第二:连续第三个深度 0 的新家族,而这次串在**一天之内**动了两回

| 波 | 串 | 家族 |
|---|---|---|
| W62+W63+W64 | 37 id | 深度 7 粒 / 329 局 |
| W65 | 34 id | 深度 4 粒 / 222 局 |
| **W66(本轮)** | **30 id** | **深度 0(本波若全配对 = 4 粒)** |

⇒ **W66 不与任何已有家族并池**,收割轮**不得**写成「给谁再加四粒」。
⭐ 本台**不裁**这是好是坏 —— promote 让集合变小正是 owner P4.2 要的方向,
而它同时让**每一波都变成一个新家族的第一波**。**两件事都是真的,取舍不归本台。**
交总监登记:**自 W65 起,本台已连续三波在买深度 0 的读数**,
而「家族深度」正是本台多轮点名缺的那个量(GH #663 复制波问题仍未裁)。

---

## 四、发波详情(W66)

**树**:`git ls-remote origin main` = **`f663570a2e987f7d202886ccf180a36477a9c8c8`**。
每一台起飞前**逐台重核三条不变量**(不是裸 SHA,沿 W65 的口径):
`origin/main:bots` = `49d1703d2a61a935498f47e7cbd80f612c1bc290`、
`origin/main:game` = `f13b532c00e48f76ee4cabef97f85149bdc06ac8`、
`md5(test_set.md 第 2 行)` = `7d4c638dab526c68d9404526a3195663`;
四台**全部打出** `bots/game/arm invariants EQUAL`,任一不等脚本 `exit 9` 不发波。

**选种**:窗口 `[11090,11490)`。已用集合 = `iterations/data/seed_roster_index.json`
(本轮 `--build`,`ROSTER_BUILD_EXIT=0`,**277 粒 / 24684 局 / 417 run**)**并上**所有
`*_wave.json` 点过名的种子 = **291 粒**;窗口内 **400 粒全部未用**。
400 粒的阵容全部展开,按**「最大化每个 term 的最小载体数」**挑(与 W65 同一目标函数,
**不是**走到第一个通过的区块就停)。⇒ **11106 / 11190 / 11240 / 11395**。

**载体冗余**(6 个 hero-scoped term,全部 PARTIAL、**无一 ABSENT**):
`spirit_breaker 3 / crystal_maiden 3 / lion 3 / obsidian_destroyer 2 / pudge 2 / skeleton_king 3`,
**min = 2**。⛔ **它不买「种子之间对容量事件独立」**(本台 W59 自己的发现),
只买「掉一台 ⇒ 某个 term 退化成薄读数,而不是归零」。

**两道发波前的门**(都 bare 读,不过管道):
- `CARRIER_EXIT=0`,逐字 `CARRIER_TERMS derived from 30 armed ids: 6 hero-scoped, 24 generic, 0 unresolved => 6 term(s)` / `CARRIER_GATE ids=6 seeds=4 exit=0`
- `WIRING_EXIT=0`,逐字 **`all 30 armed ids wired on HEAD`**
- `--dry-run` 先跑:`DRYRUN_EXIT=0`,`plan: 1 x SPOT c6i.4xlarge`,**无 REFUSED 行**
  ⇒ `--allow-short-watchdog` **没用到也没传**。

**四台**(配置与 W39–W65 逐字节相同:`--count 1 --az <az> --slots 16 --rec-slots 8 --hours 2 --games 12`;
`--rec-slots 8` **显式传**,默认是 1,W51 就死在那个默认上):

| seed | instance | 请求 AZ | 实得 AZ | LaunchTime | lifecycle |
|---|---|---|---|---|---|
| 11106 | `i-05332647bf9030117` | 2a | **2a** | 15:24:30Z | `spot` |
| 11190 | `i-07182e2c2d41cd5a5` | 2b | **2c**(环内改投) | 15:24:47Z | `spot` |
| 11240 | `i-05b2b7621902b16b0` | 2c | **2c** | 15:24:53Z | `spot` |
| 11395 | `i-0fa41a7e767d61299` | 2d | **2d** | 15:24:58Z | `spot` |

**市场核对**:`describe-instances` 读回 `InstanceLifecycle = spot` **四台全是**
(`run_id` 的 `spot_` 前缀不是证据);四个 `soak-run` 标签值两两不同。
**起飞窗口** `15:24:30Z .. 15:24:58Z = 28s`(2b 的容量重试撑开的;W65 是 9s)。

⚠️ **AZ 分散 3/4,而丢掉的那一级正是 W64 出事的那一级(波次级告警)**:
2b 请求吃 `InsufficientInstanceCapacity`(`reached max retries: 4`),脚本按 GH #256
**环内改投** us-west-2c,逐字
`! dota2bot-soak-spot-1: re-aiming inside the ring -> us-west-2c` +
`! PLACEMENT MISMATCH requested=us-west-2b actual=us-west-2c re-aimed=yes`。
**这不是降级**(不是 `AZ RING EXHAUSTED`,四台仍全 spot),但结果是
**11190 与 11240 两台同在 us-west-2c** —— 而 W64 那次,挤在同一个 AZ 的两台
**被同一次容量事件在同一秒带走**。⇒ 本波对一次 2c 容量事件的暴露面是 **2 粒不是 1 粒**。
本台**不自行改 harness**;这条已在 W64 交过总监,本轮**第二次复现,加一条实测**。

**下一轮闸 (i) 锚点**:末台 `2026-09-11T15:24:58Z` ⇒ 名义解锁 **`21:24:58Z`**。
⛔ **别抄,跑 `wave_throttle.py`。**

---

## 五、收割:零欠,而且是查过不是假定

`s3api list-objects-v2 --prefix validation/` 按 `LastModified` 倒排,最新对象
`2026-09-11T10:19:33Z`,**全部属 W65 的四个 run 前缀**(`…_20260911_0925*`),
而 W65 已由 12:07Z 那轮**全量收割完毕**(`W65_verdict.json`,4 粒 / 222 局)
⇒ 最新对象**早于**那次收割 ⇒ 全部已收。**`harvest_owed = NONE`**。

---

## 六、⚠️ 铁律 6:`origin/main` 上有五条红,**不是本轮引入的**,而它们**都从 push 闸旁边走过去**

自检(铁律 10)当轮跑到的 trunk 红,**四条 python + 一条 Lua**:

| 红 | 立即原因 | 谁的改动 |
|---|---|---|
| `test_detector_source_constants.py` | 逐字 `item_purchase_generic: the tpdeathbuy arm is no longer \`gate then bDyingWithDoomedGold = botHP < X\`` | 总监 10:37Z promote `tpdeathbuy` |
| `test_carrier_terms.py` | `FAIL zusult -> zuus` / `FAIL liondrainstop -> lion` / `…is still hero-scoped off the tree (unarmed, so the arm string cannot speak for it)` | 总监两次 promote |
| `test_bots_walk_farm_only.py` | 四条新的 `tests/ io.popen` 命令不在 `UNRESOLVED_HAND_READ` 名单里 | 英雄组/协同组新测试 |
| `test_py_gate.py` | 逐字 `5c: the selected total (12.03s) fits the recorded budget (12.0s)` | 闸自己的预算被新成员挤爆 **0.03s** |
| `test_lion_ult_reserve_domain.lua`(Lua 快腿) | 逐字 `the tree now holds 7 rank-2 Finger instants (EXACT rank), not 2` | 语料变大 |
| `test_stayfield2_marginal_domain.lua`(Lua 快腿) | 逐字 `the negative-natural-bid count moved from 56 -- every "this guard suppresses" sentence about this file has to be re-read` | 语料变大 |

⛔ **一处本轮自己写错、当轮实测改正的读数(留档不删)**:初稿写「五条**全部在** `.githooks/pre-push`
的 `py_gate` manifest 里(逐条核过)」。**核的是 manifest,而闸选的不是 manifest 全集** ——
实测这次 push 的闸打出 `scope: 84 fast python ratchets (selected by measured seconds <= 3.0s, cumulative budget 12.0s)`
与 `NOT claimed here: 30 slower python tests (run by tests/run_py_tests.sh in 开工自检)`,
`96 ran, 1 findings`,**那唯一一条 finding 是本台自己的 `W66_wave.json`(见文末),
上面四条 python 红一条都没进闸**。
⇒ **改正后的结论比初稿更糟,不是更好**:它们**不会**挡住任何人的 push,
于是**除了开工跑自检的那个组,没有人会看见它们** —— 这正是 GH #616 立案句
(「推的人的闸里没有任何 Lua 测试 ⇒ 红由下一个开工的组几小时后发现」)在 **python 侧**的同构复发,
只是这一次连「下一个 push 的人被挡一下」这个兜底都没有。
⚠️ 成员资格**只看实测秒数不看文件名**是 GH #616 约束 1 明写的,**本台读 manifest 当作读闸就是踩了它**。

⭐ **这正是 GH #624 立那条腿时描述的结构性形状,而且是它的第五、六例**:
一条 census 断言写成「语料里 X 的集合**恰好等于**已被读过的那个集合」,
于是**任何组**落一个 promote / 新测试 / 新 fixture 都会把它顶红,
而红由**下一个开工的组**(今天是批测台)在几小时后发现。
⛔ 本台**不改**这五条断言 —— 它们分别属于总监(promote 的两条)、英雄组/协同组(hand-read 名单)、
harness(py_gate 预算 12.0s)。本轮**开 issue 交出去**(见 §八)。

⚠️ **诚实边界**:「非本轮引入」的依据是**本轮对仓库的唯一改动是两个 JSON 报告文件**
(`W65_wave.json` 回填 + 新建 `W66_wave.json`),它们**不可能**影响上述任何一条断言;
本台**没有**用 `git stash` 重跑来独立证明 main 自己也红(自检那行自己也说了这一点)。

**本轮 push 的实际读数**:见文末「铁律 6 三行」。

---

## 七、GH #454 两条腿:**本轮两条都无测量点,而且第一次是因为读不到而不是因为没到时候**

- **(乙1)**(账单侧实测单波成本 > `$1.10`):W65 未落账(§〇),**无测量点,不声称不触发**。
- **(乙2)**(自终止机机时均值 > `1.00 h/台`):**本轮读不到** —— SIR 全账户零行(§一)。
  上一轮已量到 W65 的 `0.8391 h/台`(不触发),**那是能拿到的最后一次读数**。
⇒ ⭐ **给下一轮的硬指令**:W66 的 SIR **要在收割轮当轮读走**,不要留到再下一轮。

---

## 八、本轮已发表 / 待发表

⚠️ 本轮 `mcp__github__*` **未调用**(铁律 11:无头 Routine 不在权限提示上坐等)。
以下两条**要开的 issue 写在这里**,下一轮补开(或由读到本报告的总监/主会话直接开):

1. **`[bug]` trunk red ×5,全部在 push 闸的 manifest 里,全部非本轮引入**(§六 那张表逐条)。
   建议受理方:promote 两条 → 总监;hand-read 名单 → 英雄组/协同组;`py_gate` 12.0s 预算 → harness。
2. **`[harness]` SIR 读数有保质期,而没有任何地方写着**(§一):
   `describe-spot-instance-requests` 在 W65 关闭后 **~5h 已返回零行**(2.7h 时还在),
   ⇒ GH #454 (乙1)(乙2)、闸 (iv) 的 `status_code`、机时与抢占归因**全部**依赖一个会过期的源,
   而过期的表现是 **`exit 0` + 空列表**(与「零台被抢占」逐字同形)。
   建议:(a) `spot_run.sh` 起飞后即把 SIR id 写进 `<W>_wave.json`;
   (b) 收割脚本在写 verdict 的同轮抓一次 SIR 快照;
   (c) `reclaim_blind.py` 对「输入缺 `status_code` 且 SIR 已不可读」**单独报一种诊断**,
   不要与「这一波瞎了」混成同一个 `exit 2`。

---

## 九、交棒

① ⭐⭐⭐ **总监/harness —— SIR 保质期**(§一 + §八.2):这是本轮唯一一条**会静默毁掉未来读数**的发现,
   且它已经**吃掉**了本轮 GH #454 (乙2) 的测量点。
② ⭐⭐⭐ **总监 —— 连续第三个深度 0 的新家族**(§三):promote 让集合变小(P4.2 要的)与
   「每波都是新家族第一波」是同一件事的两面;GH #663(复制波)仍未裁,本轮给它第三个实例。
③ ⭐⭐ **各组 —— trunk red ×5**(§六):五条全在 push 闸里,**下一个 push 的人会被挡**;
   本台本轮的处置写在文末「铁律 6 三行」。
④ ⭐⭐ **总监 —— AZ 环内改投第二次把分散削到 3/4**(§四),而 W64 上挤在一起的那两台
   正是一起死的那两台;本波 11190+11240 同在 2c。要不要把「改投后仍须互异 AZ」做成发波期硬约束,
   交总监;**本台不自行改 harness**。
⑤ ⭐ **下一轮本台 = 收割轮**:W66 预计 `~17:2xZ` 前后落地(2h 看门狗,实测通常 ~0.84 h/台)。
   收割时(a)**先读 SIR**,(b) 喂 `W66_wave.json:arm_string` 的 30-id 串**不要喂活树**
   (今天的串一天动了两回),(c) 四个 run **各下各的目录**(GH #225),(d) **不与任何家族并池**。
⑥ ⚠️ **owner —— `$50`/`$80` 两条告警都在 `ALARM`**(`$80.413`,`forecast 191.545`),
   `budget filters : none` ⇒ 外来支出仍不受本台任何闸约束(GH #721 三条路仍未落定)。
⑦ ⚠️ **自检管道坑第 13 次** —— 本轮**第一条命令又把 stdout 接了管道**,工具当场拒绝,逐字
   `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.` /
   `it has recurred 5x, every time as the first command of the round.`;改重定向后正常。

---

## 十、铁律 6 三行 + 自检(铁律 10)

**自检 `SELFCHECK_EXIT=3`**(后台跑完取到真码,约 40min),逐字:
`legs run : 12` / `FINDINGS (exit 3) : cadence queue-rulings owed-executions trunk-red(python) trunk-red(lua)` /
**`UNCERTIFIABLE (exit 2): none`** / `NOT RUN (inside a leg): tests/test_lua_gate.py tests/test_luacheck_gate_soakswitch.py tests/test_selfcheck_lua_leg.py`。
Lua 快腿:**`TRUNK RED -- 2 of 87 Lua detector file(s) failing`**(§六 那两条)。

⚠️ **本轮第一条命令又把 stdout 接了管道**,工具当场拒绝(**第 13 次**),逐字
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.` /
`it has recurred 5x, every time as the first command of the round.`;改重定向 + 后台后取到真码。
⚠️ 第二次尝试**前台 `timeout 400`** 又被切进后台 —— **这条腿要 `> 文件 2>&1` + 后台 + 不设短 timeout**。

**铁律 6 三行(第一次 push 尝试,`PUSH1_EXIT=1`)**:
- `luacheck bots game: 0 warnings` / **`GATE_EXIT=0  CLEAN (iron rule 6 static half passed)`**
- **`py gate: 96 ran, 1 findings, 0 uncertifiable, 40.1s`** ⇒ **`PUSH REFUSED -- iron rule 6 python half is RED`**
- `lua gate:` —— **没读到**,python 腿先拒绝,第三条腿没跑到。**不声称它绿。**

⭐⭐ **那一条 finding 是本台自己的,闸抓对了**:`tests/test_wave_gate_keys.py`,
`450 checks, 5 failed`,全部指着 `W66_wave.json` —— 本台把 `gates` 的五个值写成了**嵌套 dict**,
而该文件要的是**非空字符串**,并且漏了 `iv_inputs*` 这一格。逐字:
`FAIL W66's \`i_throttle\` is a non-empty string (a blank value is the absence this file exists to catch, wearing the key)`。
⛔ 处置**按它自己写的那条**:`add the key to the record with an honest value` —— 五格改成平铺字符串、
补上 `iv_inputs_harvest`,**原来的嵌套结构原样保留在新键 `gates_detail` 下**(不丢读数),
**没有**为了变绿编任何值,**没有**动 `REQUIRED_GATES` 的别名表。
⇒ **本轮没有用 `RULE6_BYPASS`,因此没有「SKIPPED, not passed」那一行要抄。**
(改正后的第二次 push 读数见下节回填。)

## 十一、本轮 token 用量

(见文末回填节。)
