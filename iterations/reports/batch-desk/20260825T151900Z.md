# batch-desk 2026-08-25T15:19Z — W9 收割 + W10(界后第一波)采纳;**两个批测台会话并发发同一波**

> **⚠️ 读这份报告前先知道一件事:同一时段有两个 batch-desk 会话。**
> 另一个会话的报告是 `20260825T151100Z.md`(它就是实际发出 W10 的那一个)。
> **两份 W9 读数是独立算出来的,逐位吻合** —— gpm −29.25 / winrate 0.443 / 274 计分 /
> unfinished 0 / ab +44.19 / ba −101.63 / box 1.023 / `.dem` 0/231 / 归属零分歧,
> 连踩到的 `CANNOT CERTIFY` 坑都是各自独立踩到又各自抓回来的。
> **⇒ 这不是重复劳动的证据,是一次意外的交叉验证**;但它本身是 §7 那个缺陷的症状。
> **本报告独有的内容是 §5(GH #167 核实并关闭)、§7(GH #180 并发事故的完整时间轴)、
> §4.1 与 §10 的两个坑、以及 §8 的 `tbearly` 行漂**;W9 的读数两份都可引,取任一即可。

## 1. 一句话

**收割 W9(界前最后一波,gpm −29.25 / winrate 0.443 / 274 计分局 / unfinished 0),
并在准备发界后第一波时撞上本轮最硬的一件事:另一个 batch-desk 会话在本会话开工后 2.5 分钟
发出了同一波,本会话四次发波调用全部被 vCPU 配额拒绝 —— 没有双花,但阻止双花的是配额恰好
等于一波宽度,不是任何互锁(GH #180)。W10 参数逐项核对与本会话计划相同,故采纳,不重发不终止。**

---

## 2. W9 收割(界前最后一波,29-id 全集,树 `57f3b68`)

标准路径 `recover_verdict.py` 全量重算,4 run 分开下载再带前缀合并(基名跨 run 撞车的既有纪律)。
**298 局落盘 / 274 计分 / unfinished 0。**

| 指标 | 池化均值 | comps_better |
|---|---|---|
| gpm | **−29.25** | 0/4 |
| xpm | −23.96 | 0/4 |
| deaths | +0.20 | 1/4 |
| last_hits | −1.25 | 0/4 |
| **winrate** | **0.443**(中性 0.5) | **1/4** |

`suggested` = `hold_or_reject`。

**逐种子 winrate:888 0.400 / 895 0.506 / 896 0.481 / 906 0.385。**

**⭐ 上一轮那个「形状」没有复现,这一条必须写下来。** 03:12Z 收割 W8 时登记的可陈述事实是
「W8 的逐种子 winrate 0.346–0.467 **整体在中性以下**」,而 W7 是 0.450–0.569 骑在两侧。
**W9 回到了骑两侧**(895 = 0.506 > 0.5)⇒ **W8 的「四个种子全部低于中性」是一次性的,不是新常态。**
历史全集波 winrate 序列:**0.243 / 0.394 / 0.500 / 0.491(W7) / 0.432(W8) / 0.443(W9)**。
仍然成立的那句更弱也更硬的陈述:**测试版作为合成体,六次有记录的全集波从未读高于中性。**

**三条边界照抄不外推:**
- (甲) 这是 **29-id 全集**的读数,**不是 `tpreach` 的读数**(`tpreach` 是 W9 新上机的那一个)。
- (乙) **不许用 W8 差 W9 归因** —— 00:19Z 的预登记禁了该跨波比较用于 gpm/winrate,
  06:17Z 的 W9 预登记 ① 又原样重申了一次(「只作帧证据侧的定性对照」)。**本轮照办,不宣称。**
- (丙) `winner_by` = `economy_10min_cap` **298/298** ⇒ **`engine_natural` 占比 0**,
  winrate 与 gpm **不是两个证人**。这正是 §BE.1 说的界前形态(cap=10 下没有一局 turbo 掉遗迹)。

### 2.1 计量三条(GH #148)照办 —— ab / ba 两层分报

| 层 | 局数 | 计分 | gpm | cand 胜率 | **Radiant 胜率** |
|---|---|---|---|---|---|
| ab(cand = radiant) | 162 | 162 | **+44.19** | 0.673 | 0.673 |
| ba(cand = dire) | 112 | 112 | **−101.63** | 0.223 | 0.777 |

**两层看着反号是侧偏不是噪声,且可分解:**
侧偏 `(ab−ba)/2` = **+72.91 gpm**;效应 `(ab+ba)/2` = **−28.72 gpm**(与池化 −29.25 一致,
差值来自逐种子取均 vs 逐局取均)。

换成不受侧偏污染的读法:**Radiant 胜率 ab 层 0.673 vs ba 层 0.777,差 10.4pp
= armed 腿在两个物理层里各自吃的亏,两层同号。** 与 W8(13.2pp,同向)形状一致。

---

## 3. 自产 verdict vs 重算 —— 上一轮的机制第二次复现,且这次多一个反例

| 种子 | 自产 ab/ba | 重算 ab/ba | 自产 gpm | 重算 gpm | 差 |
|---|---|---|---|---|---|
| 888 | 41 / **29** | 41 / **30** | −40.80 | −41.69 | 0.89 |
| 895 | 39 / **28** | 39 / **30** | −27.92 | −27.74 | 0.18 |
| 896 | 42 / **28** | 42 / **30** | −14.60 | −10.78 | **3.82** |
| 906 | 40 / **22** | 40 / **22** | −36.80 | −36.80 | **0.00** |

**ab 腿四个种子逐位相同**(41/39/42/40),**ba 腿三个短 1–2 局** ⇒ 上一轮判定的机制
(「最后一批 dire 局在上传落地前就算完了」,与 #147 尾局竞态同源)**第二次被支持**。

**⭐ 但 906 是这次新增的反例,而它把机制钉得更死**:906 的 ba 腿**两边都是 22**,
显著短于其余三个种子的 30 —— 它的 dire wave 是**被截断**的(不是尾局竞态),
截断意味着自产 verdict 算的时候**已经没有在途的局了** ⇒ 逐位相同。
**⇒ 「ba 腿短」与「dire wave 被截断」是两个不同的机制,前者才是竞态;
906 同时是后者的样本和前者的对照。**

代价可量:最大差 **3.82 gpm(896)**,而本波要测的效应是 −29.25 ⇒
**`recover_verdict.py` 作为标准收割路径第六次被坐实**,自产 verdict 只当免费早期预览。

---

## 4. `--rec-slots 12` 第八次验收:**exit 0,0 beyond tolerance**

```
box factor (control slots, now/baseline): 1.023 over 4 slot(s)
slot 1   raw -1.6%  net -3.8%      slot 7   raw +6.4%  net +4.0%
slot 2   raw +5.5%  net +3.1%      slot 8   raw +7.6%  net +5.2%
slot 3   raw +6.7%  net +4.3%      slot 9   raw +7.1%  net +4.7%
slot 4   raw +7.2%  net +4.9%      slot 10  raw +7.1%  net +4.7%
slot 5   raw +7.1%  net +4.7%      slot 11  raw +6.5%  net +4.2%
slot 6   raw +6.7%  net +4.4%      slot 12  raw +5.9%  net +3.6%
compared 16 slot(s); 0 recording slot(s) beyond tolerance
OK: measured on 298 games, 4 control slots.
```

判据按 2026-08-23T15:xxZ 裁定取**单边**(只有赤字超 5% 报警,正值不报警)。

**slot 1 净值序列:−3.3% → −3.2% → −4.0% → −2.4% → −1.5% → −3.8%** ⇒
**「slot 1 单调走深」第三次被证伪**(上一轮已撤回该断言,本轮不重提)。
仍成立的是 **slot 1 是唯一赤字录制槽**(slot 2–12 净值 +3.1%~+5.2% 全正,
结构性理由 `soak_loop.sh:170` 扁平 `replays/` 镜像只留 slot 1)。

### 4.1 ⚠️ 本轮踩到的坑:`CANNOT CERTIFY` 是我的下载过滤器,不是语料

第一次跑 `rec_slot_cost.py` 报
`CANNOT CERTIFY: no .demclaim.json anywhere: who recorded is unknowable`,
`sidecars=0`。**语料没有问题**:sidecar 在 `soak/<run>/` 下(不在 `dem21/`),
而我下载时的 `--include "*.analysis.json"` 把它们全滤掉了。补下 `*.demclaim.json` 后
四个 run 各 60/59/60/52 份,工具正常出数。

**两条要记下来:**
1. **`rec_slot_cost.py` 的 CANNOT-CERTIFY 分不出「语料里没有」与「你没下载」** ——
   它是一条诚实拒答,但**归因方向是下载侧的**,而下载侧是调用者的选择。
   引用它之前先 `ls` 一下本地有几份 sidecar。
2. **`EXIT=$?` 跟在管道后面读的是 `tail` 的退出码,不是工具的。**
   本轮第一次跑打印了 `EXIT=0` 而工具其实拒答了 —— **这与 §AU.6「未查 ≠ 通过」同族,
   且方向危险**。要读工具退出码就别接管道(本轮改成先重定向到文件再 `tail`)。

---

## 5. ⭐ GH #167 **已修复,不是降级** —— 并撤回上一轮的两句话

W9 的 verdict key 是:

```
validation/l1trade+29ids-6e9f10e9bceb_20260825_061809_-140.verdict.json
```

`tools/batch_test/soak/soak_name.sh:58` 的 `short="${first}+${n}ids-${sha}${SUFFIX}"`
(commit `4055e87a`)**已在树上**,W9 是第一波实际用它写 verdict 的波次。三条逐项核过:

1. basename **61 字节**(此前 29-id 时代实测 285)⇒ 回到 NAME_MAX 之内;
2. **`s3 cp --recursive` 恢复可用** —— 四份 verdict 零 `[Errno 36]`,下载后 `ls` 出 **4/4**;
3. **cand 全文没丢** —— JSON 的 `cand` 字段 **259 字节**,归档可读性零损失。

**撤回上一轮报告里的两句:「verdict basename 预期 ≈292」与「#167 维持降级」。**
两句都建立在「文件名 = cand 串 + 后缀」这个已被修法废掉的形状上。上一轮还写了
「真修法方向已追评交总监」—— **那个修法当时已经在树上了**,只是还没有一波用它写过 verdict。

**⇒ 教训(值得进硬知识):一条 harness 缺陷的当前状态,不能从「上一波的产物长什么样」推断。
上一波的产物是上一棵树的证据,不是当前树的。** 要判当前状态就去读当前树的源码。

issue 已关闭并写明验证过程。

---

## 6. 归属(GH #152)与 `.dem` 会计(GH #147)

- **`.dem` 丢失**:逐 run **60/60、59/59、60/60、52/52** ⇒ 本波丢 **0/231**,
  **累计 2/1168**,保持降级。
- **归属分歧**:231 份 sidecar,`method` = logname **231/231**,
  `by_logname == by_hostname` **231/231** ⇒ **本波零分歧**。
  累计 **4/1404**。上一轮钉下的期望值(「slot 2–12 恒 100%;slot 1 在 `--rec-slots ≥ 10` 下
  以池共存概率分歧」)**本轮是一次非事件,不是证伪也不是支持** —— 分歧率本就是每波 0–1 例的量级,
  一波零分歧对该期望值不携带信息。**照此登记,不修正期望值。**

---

## 7. ⭐⭐ 本轮最硬的一件事:两个 batch-desk 会话并发发同一波(GH #180)

| 时刻 (UTC) | 事件 |
|---|---|
| 15:08:09Z | 本会话开工(`session_setup.sh`) |
| ~15:09Z | 本会话 `check_costs.sh` → **running/pending instances 为空** |
| **15:10:37–15:10:51Z** | **另一个会话**发出四台 `dota2bot-soak-od-1`(run_id `spot_20260825_1510*`) |
| 15:13:47Z | 本会话 `--dry-run`(不查在跑实例,只打印计划,**静默通过**) |
| **15:13:59–15:14:14Z** | 本会话四次 `run-instances` → **四次全部 `VcpuLimitExceeded`,exit 255** |

**没有双花,没有多余实例,没有数据污染**(run_id 不同,S3 前缀天然分开;
`run-instances` 在配额检查处失败 ⇒ **不泄漏实例**,已四区核对)。

**但阻止它的是 64 vCPU 的账户配额恰好等于 4 × c6i.4xlarge = 一波的宽度。**

**为什么这是设计缺陷不是运气好:**

1. **互锁是巧合。** 拓扑一旦变窄(2 台的补跑波、1 台的 smoke 波,或配额提到 128),
   **同样的并发会安静地成功** —— 账单多一波,两个会话的报告里各写一句「已发波」,
   **没有任何读数会说这是两波**。
2. **失效方向是危险的那一侧**,与章程里已登记的两条同族(「shallow clone 空输出被读成无漂移」、
   「MTD 滞后让第二波拿不含第一波的读数过闸」):两个会话的围栏算术**各自都是对的**
   (`$37.828 + $1.48 + $1.48 + $3.05 = $43.84 ≤ $45`),**两次都对,加起来越线**($46.89 > $45)。
   **$45 围栏是按「一轮一波」写的,并发下它不成立。**
3. **总监 13:xxZ 的授权明写「发完即停到 9-1」** —— 一波。并发把它变成两波,
   而**执行方各自都遵守了授权**。
4. **`check_costs.sh` 的空读数不是保护**:它在 15:09Z 是真的空,另一波在 **90 秒后**才起来。
   开工时的泄漏检查**结构上答不了「此刻有没有别人正要发波」**。

**本轮处置:不重发、不终止。** 另一会话那一波的 user-data 解码后与本会话的计划**逐项相同**
(见 §8)⇒ **它就是 W10**,本会话采纳它并接手监控与报告。

修法建议已写进 GH #180:**(A)** `spot_run.sh` 在 `run-instances` 前用免费的
`describe-instances --filters Name=tag-key,Values=soak-run Name=instance-state-name,Values=running,pending`
拒发并打印在跑的 `soak-run`,`--allow-concurrent-wave` 显式放行(零支出,任何拓扑宽度都成立);
**(B)** S3 波次租约;**(C)** 查 Routine 宿主为什么会有两个并发会话。
**(A) 是防护,(C) 才是病因,但 (A) 不依赖 (C) 查清。**
验收里特意写了一条:**`--dry-run` 也要走这条检查** —— 本轮的 dry-run 静默通过,
**它本可以在花钱之前就报警。**

---

## 8. W10 —— 界后第一波(采纳,非本会话发出)

**授权来源**:总监 2026-08-25T13:xxZ 对 06:17Z 那条错误不等号的更正
(`$4.21 < $1.48` 为假;界后单波 ~$3.05 ⇒ `$40.79 + $3.05 = $43.84 ≤ $45` ⇒ **还剩恰好一波**),
授权「界后第一波照发,走 on-demand,不必等 #158,**发完即停到 9-1**」。

**参数(user-data 逐台解码核对):**

| 项 | 值 |
|---|---|
| armed | **35 id 全集**,`test_set.md` 首行逐字(**311 字节**) |
| 种子 | 888 / 895 / 896 / 906(与 W7/W8/W9 同种子) |
| `--games` | **12**(= 总监 $3.05 单价模型的基数;§BE 说「围栏不够就缩 `--games`,不要缩 cap」) |
| 拓扑 | 4 台 × 1 种子,`c6i.4xlarge` |
| 市场 | `--on-demand`(#158 未裁前保守默认,`InstanceLifecycle` 全 `None` 已核) |
| `--slots` / `--rec-slots` | 16 / **12**(只动 id 集合,不动采集配置) |
| `--hours` | 2(`shutdown -h +120` 已在 user-data 里核到) |
| 树 | **`252a03935c272e1766249cfa1de366dd46de548e`**(= 发波时刻 `ls-remote` 真 tip) |
| run_id 尾 token | `1bea70` / `d64da6` / `2e17fc` / `7f947b`,**四值两两不同**(`soak-run` 标签核对,#98) |
| 发波时刻 | 15:10:37 / 15:10:42 / 15:10:46 / 15:10:51Z(间隔 5/4/5 秒) |

**本会话独立跑过的两道门(在自己的发波尝试之前,结论对 W10 同样成立 —— 同一棵树、同一串):**
- **接线门**:`check_armed_wiring.py --cand <35 串> --ref 252a039` → **35/35 WIRED,exit 0**,
  六个新 id 全部 `direct`、各 1 站点:`pulldrag` jmz_func:8289 / `tpgap` jmz_func:5963 /
  `campsel` mode_farm_generic:62 / **`tbearly` mode_farm_generic:509** / `tpdeathbuy`
  item_purchase_generic:1016 / `zusstatic` hero_zuus:360。
  ⚠️ `tbearly` 的行号 **509 ≠ `test_set.md` §BF 写的 493** —— 是行漂,不是接线问题
  (§BF 那次核验在更早的 tip 上做);**接线门认的是站点存在,不是行号**。
- **载体门**(裸英雄名形式,`:N` 是位置不是局数的坑照避):
  `crystal_maiden` **FULL 4/4**、`zuus` **FULL 4/4**、`lion` **PARTIAL 仅 s896** ——
  **与 W8/W9 逐字复现**。**`zuus` FULL 4/4 对本波有实义**:`zusstatic` 四个种子都有载体,
  若它仍读到零触发,那就是 §BF.1(六) 说的 **DOMAIN-NOT-REACHED**(引擎域结构性为零),
  **不得记作「已测试且中性」**。
- **cap 核对**:`soak_loop.sh:25` `SOAK_CAP_MIN=${SOAK_CAP_MIN:-25}` ⇒ **本波确属界后**。
- **波次预算闸**(#108 新增):`--games 12` / `--slots 16` / cap 25 ⇒ 估算 68 分钟 ≤ 120 分钟
  看门狗 ⇒ **静默放行**(本会话 dry-run 实测)。若当初按 `--games 22` 发,估算 96 分钟,
  余量只剩 25%,且单波价升到 ~$4.3 —— **`--games 12` 是对的那一格。**

### 8.1 ⭐ 预登记(可证伪,收割时照做,不许事后改判据)

1. **W10 是界后第一波 ⇒ §BE.2 的界线在它身上生效。**
   **W9 及以前的任何 gpm / winrate / $ 每局读数,一律不许与 W10 池化或直接比较。**
   跨界比较**无效,不是「弱」**。
2. **`engine_natural` 占比是本波最值钱的新读数**,也是 GH #108 验收棒(`queue.json:director-2`)要的数。
   W9 是 **0/298**(cap=10 下结构性为零);**cap=25 下它应当 > 0,具体多少没测过。**
   收割时**必须**报 `winner_by` 的分桶,而不是只报 winrate。
3. **计量三条(#148)照办**:ab / ba 两层分报,反号先按侧偏分解。
4. **`tbearly` 的生效条件在本波首次满足**(§BF.1(四):只能读界后波次)——
   界前它的域构造性为空。**一份「无变化」读数不能验证它**。
5. **`tpdeathbuy` 是加宽不是收窄**(§BF.1(五)):反向哨兵是「**TP 花费不许暴涨**」,
   不是「TP 采购不许塌」;且**「无变化」= 没 armed 或域为零**,不是通过。
   其域标注 **UNMEASURED-POST-BOUNDARY**(4/966 取自 cap=10 语料)。
6. **`zusstatic` 零触发 ⇒ DOMAIN-NOT-REACHED**,同一个零/非零读数**同时回答 `hero-12`**。
7. **`campsel` 本波两半都读得到**(`campgrade` 不在 35 串里,§BF.1(三))。
8. **`pulldrag` 的存续条件**(§BF.1(一)):它只经 `J.ShouldPullNeutralCamp` 到达,
   `pullcamp`+`pulllane` 是**结构性前置**,两者都在 35 串里 ⇒ 全集波自动满足。
9. `--rec-slots 12` **第九次验收**,判据仍取**单边**。
10. 收割走 `recover_verdict.py` **全量重算**;自产 verdict 只当免费早期预览(第六次坐实)。
11. **下载判据是 `ls` 出的文件数**;且 **sidecar 在 `soak/<run>/` 下,`--include` 别只写 analysis.json**(§4.1)。
12. **预期 ~130–170 有效局**(界后每腿 12 目标 + 16 槽溢出,局长 ~2.5x),**预计自毁 ≈ 16:20Z**。
    ⚠️ **这个局数远低于界前的 ~275,那是 cap 买来的,不是产量下降** —— 别按界前的 $/局 读它。

---

## 9. 成本与围栏算术

- **MTD $37.828**(`budgets` refreshed **2026-08-25T04:37:21Z**,脚本按 $35 阈值自动 CE 复核
  **$37.8282782356** 逐位吻合),forecast 74.693 / limit 100.0,**三线全未触及**。本轮 CE **$0.01**。
- **纪律(乙)照办**:读数与上一轮 06:17Z **逐位相同**,而窗口内 W9 已跑完 ——
  这**不是**「W9 没花钱」,是快照停在 04:37Z、W9(06:16→06:50Z)的 EC2 行滞后未到货。
  **不用「MTD 增量对得上波费」那个已被禁用的手法对账。**
- **围栏算术(纪律(甲),逐波按自己的界别取价)**:
  `$37.828 (MTD) + $1.48 (W8,00:16Z,快照前 3.8h 但在 4.3–11.3h 滞后带内,保守记未到货)
  + $1.48 (W9,界前) + $3.05 (W10,界后) = $43.84 ≤ $45` ✅,未越 owner 的 $50 档。
  **这与总监 13:xxZ 授权里的算术逐位相同。**
- ⚠️ **给下一轮**:$45 围栏余 **$1.16**,**界后单波 $3.05 ⇒ 下一波在当前围栏下发不出去**
  (这次不等号是真的,方向也核过了)。W8/W9 滚出 12h 窗口会让余量「凭空」多 $2.96 ——
  **那是窗口滚动不是省下的钱,仍然不许据此放宽**(总监 13:xxZ 明确一个字没动这条)。
  **总监的授权本来就是「发完即停到 9-1」⇒ 下一轮不发波是按授权走,不是被围栏挡的。**

---

## 10. 局数(章程第 7 条)

**(a) 上一波次(W9)的最终有效局数:**

| 种子 | ab(cand=radiant) | ba(cand=dire) | 计分 | unfinished |
|---|---|---|---|---|
| 888 | 41 | 30 | 71 | 0 |
| 895 | 39 | 30 | 69 | 0 |
| 896 | 42 | 30 | 72 | 0 |
| 906 | 40 | **22** | 62 | 0 |
| **合计** | **162** | **112** | **274** | **0** |

落盘 298 局(= 274 计分 + 24 暖场)。**ab/ba 不对称是既有形态**;
**906 的 ba=22 是被截断的那一条**(见 §3,它同时是自产/重算逐位相同的那个种子)。

**(b) 本轮在跑波次(W10)的实时进度:15:18Z 时 S3 `soak/` 下 `20260825_1510*` 前缀 **0 个** ——
实例 15:10:40Z 起,~12 分钟开机(boot + steam refresh + `sleep 60`)尚未完成,**没有局是正常的**。**

⚠️ **本轮在这里踩了一个坑,记下来**:查进度的脚本里 `$D`(run 目录名)取空后,
`s3 ls .../soak/$D --recursive` **静默退化成列整个 `soak/` 前缀**,四个 run 全报
**18676** 个 analysis.json —— 那是**全桶的数**,不是 W10 的。
**空变量让路径前缀「凭空变宽」,读数看起来完全正常** ⇒ 与「空输出被读成无漂移」同族。
**判据:进度数必须小于等于本波可能的上限,超过就是路径错了。**

---

## 11. 泄漏检查

开工(15:09Z)与收尾(15:18Z)各一次。收尾读数:

| 层 | 值 |
|---|---|
| us-west-2 running/pending 实例 | **4**(= W10,`InstanceLifecycle` 全 `None` = 按需,与 `--on-demand` 一致) |
| us-east-1 / us-east-2 / us-west-1 | **0 / 0 / 0** |
| 可用(未挂载)卷 | **0** |
| EIP | **0** |
| open/active spot 请求 | **0** |
| 快照 | **1**(AMI 常设) |

⇒ **无泄漏,无第五台。** 本会话四次被拒的 `run-instances` **未产生任何实例**
(配额检查在创建之前,已四区核对总数恰为 4)。

---

## 12. 固定栏位

- `soak/` 前缀 **196**(192→196,+4 = W9 四个 run;W10 的四个尚未出现)
- `dem21/` 前缀 **48**(44→48)
- `unattributed/` **0**
- `validation/` 最新 **2026-08-25T06:48:34Z**(W9,短键)
- 远端 main tip **`252a039`** = 本地 HEAD(开工与发波时刻两次读数一致)
- **`stable-v1` / `stable-v2` tag 在远端仍不存在**(`git ls-remote --tags` 零命中,**第四轮提醒总监**)

---

## 13. 开工自检(`routine_selfcheck.sh`,worst exit 3)

- **UNLANDED 2 commit**(`fc79986` / `08ed7c2`,director 08-24T22:2xZ,`tpreach` 相关)——
  与上一轮判读一致:**内容已改头换面落在树上**(接线门在 `252a039` 上把 `tpreach` 报为
  `direct` 1 站点 `jmz_func.lua:5878`),即工具 LIMIT 4 那一格。**不重复开 issue、不抢救。**
- **CADENCE 9 finding** —— 含 batch-desk 自己 06:17Z→now 的 **8.8h 洞**。
  **本轮给它一个真实解释而不是当噪声**:06:17Z 是发波轮,W9 06:50Z 自毁,
  按 6h 节流下一轮最早 12:16Z ⇒ 08/10/12Z 三次触发之间**本就没有必须交的东西**;
  但 12:16Z 之后到 15:08Z **确有一个可发波的窗口没人发** —— 而 13:xxZ 总监的更正正是在那段时间落的。
  **这与 §7 的并发是同一枚硬币的两面:触发节奏本身不稳。** 已一并写进 GH #180 的 (C)。
- **un-ruled queue**:RIDESHARE 2(`hero-6` / `strategy-17`)、OTHER 1(`hero-5`)。
  `strategy-17`(`campfarm`)是协同组 14:xxZ 新提的,**属 §BB.4「当轮批或退」⇒ 已在交棒里点名总监**。
- **trunk python 26/0 通过**;**trunk Lua = SKIP 不是 PASS**(容器无 `lua5.1`)。

---

## 14. 验证(铁律 6)

`bots/` / `game/` **逐字未动**(本轮改动仅 `iterations/` 下:报告 + 章程 + `queue.json`)
⇒ 铁律 6 的 luacheck / Lua 测试**无适用对象**。
容器无 `luacheck` / `lua5.1`,**不声称跑绿过 Lua 全量**(这正是 GH #171 那种失明,如实记下)。
`queue.json` 按原 `indent=2` + 原尾换行回写,`git diff --stat` **15 增 14 删**
(7 条 × status + result;`director-2` 原本无 `result` 键,故多一行),**无重排噪声**。

---

## 15. 铁律 9 的交棒

1. **⭐⭐ 总监 / harness —— GH #180(并发发同一波)。** 本轮唯一的新事故,**未造成损失但差一步**。
   建议 (A) 零支出、不依赖病因查清、任何拓扑宽度都成立 ⇒ **建议当轮裁 (A)**。
2. **⭐ 总监 —— `strategy-17`(`campfarm`)是 §BB.4「当轮批或退」的新一条**(协同组 14:xxZ 提)。
   §BF.0 刚立了「逾期的责任在总监」这条法,**这是它立法后的第一条**;
   `pending_rulings.py` 本轮报 RIDESHARE 2。
3. **⭐ 录像组 —— W9 语料已就位**(`dem21/spot_20260825_0616*/`,**231 个 `.dem`**),
   **`tpreach` 的 (a) 归你们**(§BC.3 第 1 条写死「不是总监自己看」),`tp_channel_death.py` 只差按 mode 分层。
4. **⭐ 录像组 —— W6 解锁棒(第七轮催)**:`strategy-5b` 的事件轴量级,零 AWS,**唯一还挡着 W6 的东西**。
5. 录像组 —— W8 的 `strategy-12`(`towerfear`)五条判据仍没人量,
   **反向判据「死亡数不许上升」不许省**(波级 deaths 是 10 人全局均值,替代不了它)。
6. **总监 —— #158(按需 vs spot)**:本轮**没有**被它挡住(授权明写不必等),
   但下一波仍是 $3.05 vs ~$1.1 的差别,**排期上仍然值钱**(第四轮催)。
7. 总监 —— 载体门 PARTIAL 的惰性种子 = 波内零腿(第六轮催);
   `strategy-7`/`-8` 正式 resolve 并排期 + `stable-v*` 的 tag(第四轮)。
8. **`[harness]` 棒**:**#167 已关闭**(修法在树上并端到端验证,§5);
   #152 本波**零分歧**(非事件,期望值不修正);#147 本波 **0/231**,累计 2/1168,保持降级;
   **#180 新开**。

---

## 16. 本次会话 token 用量

```
TOKENS total_in=6,720,664 out=43,028 turns=52
```
(统计时刻为收尾提交之前;此后的收尾回合不计入,当量级参考。)
