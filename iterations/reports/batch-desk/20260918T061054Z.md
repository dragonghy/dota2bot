# 批测台 2026-09-18T06:10:54Z

**一句话**:刹车第四十七轮持有;零发波、零收割欠、零泄漏。⭐⭐ 本轮把上一轮交棒的「复查面在报告里」
量成了一个**有界的数**:界后纪元里引过 winrate 数字的报告共 **59** 份,其中**不带 `DEGENERATE` 横幅的
只有 25 份,且全部落在 2026-08-26 → 08-31 这 6 天里** —— 上界正是 `DEGENERATE` 横幅第一次出现的那一天。
⭐ 另一件:**管道坑连续八轮的同形本轮断了**(改用重定向到文件,自检正常起跑)。

---

## 1. 开工自检(铁律 10)

⭐ **本轮第一条命令就跑通** —— `bash tools/agent/routine_selfcheck.sh > <file> 2>&1`,
**既不是管道也不在 `timeout` 下** ⇒ 两道自卫**都没有触发**,逐字首行
`push gate armed (core.hooksPath=.githooks); `git push` now runs the rule 6 static gate`。
⚠️ **这不是工具变了,是调用姿势变了**:连续八轮的 `REFUSED: ... stdout is a pipe` 同形到此为止,
**取法钉在这里给下一轮直接抄**:`> 文件 2>&1`(重定向到普通文件 ≠ 管道),**⛔ 不要加 `timeout`**。

已跑完腿实读:
- `unlanded-commit scan`:`OK: no unlanded work in the certifiable window.`;
  ⚠️ 自述边界照抄 —— `shallow clone: YES` / `REFUSED refs: 1302 of 1325` / `certifiable refs: 23`
  ⇒ **「窗口内没有」不是「没有」**。
- `LUA GATE COVERAGE disk 512 | push gate 347 | leg 138 | known_red 5`、
  `UNCOVERED 114 of 512 (22%)` `{'no_manifest_row': 51, 'timed_out': 60, 'too_slow': 3}`;
  **`UNCOVERED SET GREW -- 2 file(s)`** → `NEW UNCOVERED no_manifest_row tests/test_fieldsip_transfer_receiving_site.lua`
  (**连续第四轮同一个文件**,`fieldsip_transfer` 家族,修复路径仍卡 GH #783)
  + `NEW UNCOVERED too_slow tests/test_lion_w_fight_seed.lua`(**与上一轮同两个**);
  ⭐ `NOW COVERED tests/test_fieldsip_atom_pricing.lua`(与上一轮同)。
- `PY GATE COVERAGE disk 151 | push gate 131 | leg 151`、`UNCOVERED 0 of 151 (0%)`。
- 欠条腿:`UNCERTIFIABLE gh801_confirm_headroom_first_live_read` **仍在册**(见 §2 末)。

⛔ 本轮 `bots`/`game`/`tests`/`tools` **一行未改** ⇒ 上面两条 `NEW UNCOVERED` **无一条由本轮引入**,⛔ 不代修。
收尾三行横幅见「补记」。

## 2. 预算(闸 (iii) `FENCE_EXIT=3` —— 连续第四十七轮是闸拒绝,不是本台的处置)

逐字照抄:

```
actual (MTD)     : $92.163   <- re-read this run, never cached
pending waves    : $0.000   (launched, may not be in MTD yet)
planned          : $0.000
projected total  : $92.163
fence            : $100.00
brake            : $90.00   (owner's line, not derived here)
operative ceiling: $90.00   = min(fence, brake)
A launch at this instant would put MTD past the $90.00 BRAKE line.
WAVE_FENCE: THROTTLED (exit 3) -- brake, not fence. Stop and report to the owner; do not wait it out.
```

RULING 5/6/7 三行照抄,⭐ 钟没有降级,⛔ 未传任何 `--no-*`:
- `accrual scope : 17 region(s) read [...] <- COMPLETE: every region this account has enabled`
- `wave accrual : records after 2026-09-17T08:41:13Z (= 2026-09-17T19:59:13Z - 11.3h lag, clock from budget snapshot)`
- `accrual check : CERTIFIED (0 accruing instances account-wide, read this run) -- pending $0.000 is a reading, not a default.`

`crossing registry: 0 record(s) ... none in force` + `1 retired record(s)`;`budget filters : none`;
`alpha = 0` / `beta = 0` 本轮实读。

**⭐⭐ 快照连续第四轮冻结**:MTD `$92.163` / 戳 `2026-09-17T19:59:13Z`,与前三轮(09-17T21:28Z、
09-18T00:24Z、09-18T03:13Z)**两量逐位相同** ⇒ **零新支出测量**(⛔ 不等于「没花钱」)
⇒ **一个速率点都不贡献**,⛔ 不动 `$0.40–0.92/天` 带、月末兜底日 `2026-09-27T00:00Z` 原样结转。
**headroom 对 `$90` = `$-2.163`**(四轮逐位相同,⛔ 不得读成趋势)。
⚠️ `check_costs.sh` 自打的一行照抄:`stamp unknown (fresh container) -- alpha and beta are both zero --
a frozen stamp over an idle account opens no blind spot`。
`forecast 118.091` 与戳一同冻结,⛔ 不解释不采信。

**GH #801 四行整块照抄**(用**默认值**跑,⛔ 未用 `COST_CONFIRM_AT=999` 自救):

```
   >= $35, but headroom to the $90.00 brake is $-2.163
   < $1.10 (cheapest wave) — CE confirmation SKIPPED, NOT passed (GH #801).
   Nothing can launch at this MTD, so confirming it buys nothing.
   Resumes by itself as soon as headroom >= $1.10.
```

**SKIP 不是 pass。** ⚠️ **欠条 `gh801_confirm_headroom_first_live_read` 本轮登记为仍 OWED,并附一个自问**:
章程写「结清判据钉在整块的最后一行(以 `Resumes by ...` 开头的那一行),它只有用默认值真跑一次才打得出来」,
而**那一行本轮(以及至少前三轮)都用默认值打出来了**,自检的欠条腿却仍读 `UNCERTIFIABLE`。
⛔ **本台不自行结清** —— 「打出来了」与「欠条该结」是两个命题,用前者代替后者正是证据纪律 4 禁的那一手
(结论对得上 ≠ 理由对);⇒ **交总监一句话裁**:要么判据其实是「headroom ≥ $1.10 那一轮真付了那 `$0.01`」
(那么名字里的 `confirm` 是承重词,本轮读法错),要么判据已满足而账本腿没跟上(那么是账本的洞)。
**两种都不需要 AWS 支出**,且**刹车解除前无法用第一种方式结清**。

## 3. 收割(零欠)

- `validation/` **592 对象**、`soak/` **72,079 键** —— 与前三轮**逐位相同**。
- 最新对象按 RULING 47 取法仍是 `lf_rescue+27ids-d177b87026dd_20260912_1018_run.log`
  (`2026-09-12 10:18:26`)= **W69**。
- 在跑波次:**无**。`recover_verdict.py` 未跑(**没得收,不是漏收**)。

## 4. 交棒执行

### 4.1 交棒 ② —— GH #184 零表态(只登记,⛔ 不重复发表证据)

现读 `state: open` / **`comments: 3`** / `updated_at 2026-09-18T00:36:03Z`
⇒ 与上一轮**逐位相同**,第三条仍是本台自己那条 ⇒ **总监/owner 连续两轮零表态**。
⚠️ #184 已 open **24 天**(created 2026-08-25T18:17:30Z),验收是**零 AWS 成本**的。
按交棒 ② 严格执行:**只登记戳/评论数**。
连带:交棒 ③(加厚真局侧别 ~4,700 GET)**以总监裁定为前提 ⇒ ⛔ 本轮未做**。

### 4.2 交棒 ④(b) —— 缺陷现读:**没修**

`analyze_log.py` 现读逐字未动:

```python
    natural_end = fort is not None
    winner_by = "engine"
    if natural_end:
        winner_by = "engine_natural"
    elif base.get("winner") is not None and econ_winner and dur_min >= cap_min - 0.5:
```

`referee_surrender` 在 `tools/batch_test/soak/` 下 **仍 0 处**(排除 `__pycache__`)
⇒ **#184 §4 建议修法仍未落地** ⇒ 刹车解除后第一波的 `winrate` 仍是裁判伪影。

## 5. ⭐⭐ 本轮新测量:复查面 = **59 份报告,其中 25 份无横幅,窗口只有 6 天**

上一轮把复查面从 `validation/` 赶到了「报告里」(存档侧 `0/185`,从未提供过一个可引的 winrate),
**但没给这一侧的数**。本轮补上,取法**全量不是抽样**:`iterations/reports/*/` 下所有
`YYYYMMDDTHHMMSSZ.md`,窗口 `2026-08-26` 起(界后纪元),共 **920** 份。

**判据(收紧版,只认可靠的绑定方向:数字**跟在**`winrate` 后 40 字符内)**:

| | 份数 |
|---|---|
| 界后引过 winrate 数字的报告 | **62** |
| 其中 `< 2026-09-17`(缺陷尚未被发现) | **59** |
| └ 报告里**有** `DEGENERATE` 横幅 | **34** |
| └ ⭐ 报告里**没有**任何 `DEGENERATE` 横幅 | **25** |
| 其中 `>= 2026-09-17`(缺陷已知) | 3 |

**⭐ 判别子在数据自己身上**:那 25 份的日期区间是 **`20260826` → `20260831`**,
而全库**最早**出现 `DEGENERATE` 的报告是 **`director/20260831T095500Z.md`** ——
**两个边界同一天**。⇒ 这 25 份不是散落各处的遗漏,是**一个连续 6 天的前置窗口**,
上界正是 #352 那道闸落地的那一天。分布:`batch-desk 24` + `director 1`,**其余三流 0**。

⇒ **两半都要写**:
- **(甲) 收窄的那半(本轮主要产出)**:总监第 2 问「历史 winrate 读数要复查多大一片」
  现在有一个**有界答案** —— 存档侧 `0/185`(上一轮)+ 报告侧 **25 份无横幅 / 6 天窗口**(本轮)。
  ⭐ 这与上一轮 §五 的反向核实**互相印证**:`DEGENERATE` 闸挂在 `"a wave was counted"` 上、
  界后 272/272 全 dire ⇒ `share 0.0000 < 0.20` ⇒ **必然触发** ⇒ 08-31 之后每一个被引的 winrate
  都站在一条自带禁令的横幅底下(`MUST NOT be cited as rule 2(b) support until the channel recovers.`)。
  **闸归因错、拦截对**(上一轮已立),本轮量出了**它实际拦住了 34/59**。
- **(乙) ⛔ 但不是「所以没事」**:那 25 份里的 winrate 数字**当时没有任何东西举手**,
  而它们正处在界后纪元最早的 6 天 —— 即 W10 之后、#352 之前,恰是 #184 §3 点名
  「界后所有 winrate 读数」那一段的**最前端**。

**诚实边界六条**:
① **这是正则启发式不是语义解析**:宽判据(数字在 `winrate` 前后 60–120 字符内)给 69/66/25,
  收紧判据给 62/59/25 —— **两个读数都登记**(铁律 4(iii):连切法一起登记)。
  宽判据的假阳已实见(`0.22 xpm −19.09 ... winrate` 这类,数字属于**邻近**指标);
  收紧判据**只认数字跟在 `winrate` 后**,⛔ 仍不保证每一个命中都是 winrate 本身。
② **「报告里有 `DEGENERATE`」是报告级读数,不是逐数字读数** —— ⛔ 本台**未**核对
  横幅与那个具体数字的相邻性;一份报告可以在 A 处带横幅、在 B 处引一个裸数字。
  **25 是「完全无横幅」的下界形状,34 不是「34 个数字被覆盖」。**
③ ⛔ **未核对那 25 份里的数字是不是界后语料**(一份报告可以复述界前的历史数)
  ⇒ **25 是复查面的上界,不是「25 个错数」**。
④ ⛔ **不点名**任何 promote / 铁律 2(b) 判定引过它们;⛔ **不重开**已关档裁定;
  ⛔ **不改** `state.json` / `test_set.md` / `OWNER_PRIORITIES.md` / `queue.json`。
⑤ 窗口起点 `2026-08-26` 取自 #184/§CT 的界后纪元定义,⛔ 本台未重新论证这个界。
⑥ 文件名不合规的报告(自检读到 `malformed report names 7`)**不在这 920 份里**,⛔ 未追。

**取法可复现**(零 AWS,纯本地):`iterations/reports/*/YYYYMMDDTHHMMSSZ.md` 全量正则
`winrate[^\n]{0,40}?([01]\.\d{2,4})` + `DEGENERATE` 的存在性,按文件名日期分层。

## 6. 局数与成本(铁律 1 三段式)

**(六.1) 局数**:本月无新波次;本轮**未读 `analysis.json`、未读 `verdict.json`**(⛔ 0 局)。

**(六.2) 成本**:**零 EC2 / 零 CE / S3 读取 `~79` 个对象(出网未计价)** ——
`s3 ls --recursive` 2 次(592 + 72,079 键 ≈ **79 LIST**,**不下载任何对象**)。
**本轮新增 EC2 计费 `$0.00`**;`check_costs.sh` 本轮**未买 CE**(GH #801 SKIP)。
⭐ 本轮是界后最便宜的一轮之一 —— §5 那个新测量**一个 S3 对象都没碰**(语料在仓库里)。

## 7. 泄漏(五条独立路径全零,⛔ 不合并成一句)

① `check_costs.sh` 自带区块**空**(`COSTS_EXIT=0`,`running/pending instances` 无行);
② `describe-instances` 五态(pending/running/shutting-down/stopping/stopped)**不加 tag 过滤**
   ⇒ `INST_EXIT=0` / **`LINES=0`**;
③ `describe-spot-instance-requests State==open||active` ⇒ `SIR_EXIT=0` / **`SIR_LINES=0`**;
④ 闸 (iii) 账户级 **17 区 COMPLETE** + `CERTIFIED (0 accruing instances account-wide, read this run)`;
⑤ AMI 仍 `ami-0a990a26d89c66547`(`dota2bot-batch-20260718`)**一张**,常设成本非泄漏,快照无新增。

## 8. 发表纪律

⛔ **本轮零发表**(零 issue、零评论)。
自问过该不该把 §5 发到 GH #184:**否** —— 上一轮那条评论发出至今 **5.6 小时、仍无人读**
(`comments` 未动),此刻追第二条长评论**增加的是噪声不是信息**,而这个数**不会过期**(语料在仓库里)。
⇒ **登记在本报告 §5 + 章程,棒交下一轮**:总监一旦对 #184 表态,**连同 `0/185` 与本轮的 `25/6 天` 一起答**。
⇒ 本轮**未跑** `claim_precheck.sh`(⛔ 无草稿可检)—— **⛔ 这是「没有可发表的东西」,不是「检查通过」。**

## 9. 通知判据(09-16T18:15Z 收窄的四条,逐条现读)

| 条 | 现读 | 中? |
|---|---|---|
| MTD ≥ `$100` | `$92.163` | ✗ |
| `alpha > 0` | `alpha = 0` | ✗ |
| headroom ≥ `$1.10` | `$-2.163` | ✗ |
| 日期 ≥ `2026-09-27` | 今天 `2026-09-18` | ✗ |

**四条一条未中 ⇒ ⛔ 本轮不推送。**
⭐ 与上一轮同样地严格:§5 虽是新测量,但**不改任何阈值、不需要 owner 做任何决定**
(刹车僵局的球在 **GH #721** 的 owner 侧,已推送过两次)⇒ **第 47 次「刹车仍持有」对 owner 是零信息**。
⚠️ 09-18T00:24Z 那轮自报的偏离、以及它交给总监的「要不要加第 ⑤ 条(量具级缺陷)」
**仍零表态** ⇒ 判据仍是这四条,严格照它执行,⛔ 本台不自行改判据。

## 10. 没做的事

⛔ 未发波(闸 exit 3);⛔ `bots`/`game`/`tests`/`tools` **一行未改**
(⚠️ **又想过**给 `analyze_log.py` 收紧 `natural_end`,**再次否掉** —— 判据与拦截点是总监的事,
先例 GH #33,且 #184 §4 已给两版待选,本台**不代选**);
⛔ 未做交棒 ③ 的 ~4,700 GET 加厚(前提是总监裁定);
⛔ 未重开任何已关档裁定;⛔ 未点名受影响的 (b) 判定;
⛔ 未改 `state.json`/`test_set.md`/`OWNER_PRIORITIES.md`/`queue.json`;
⛔ 未自行结清 GH #801 欠条;
⛔ 闸 (i)/(ii)/(iv) **未跑**(发波轮的闸;(iii) 已 exit 3)—— **未跑不记成通过**。

## 11. 交棒(给下一轮批测台)

1. **抄取法**:自检用 `> 文件 2>&1`,**⛔ 不用管道、⛔ 不加 `timeout`** —— 本轮实测这样就正常起跑。
2. **GH #184 仍只登记戳/评论数**;总监一旦表态,**把 `0/185`(存档侧)+ `25 份/6 天窗口`(报告侧)
   两个数一起答**,这是复查面的完整界定。
3. **GH #801 欠条**:带着 §2 末那个一句话的自问去问总监,⛔ 不自行结清。
4. 刹车预计 **2026-10-01** 预算重置后解除;解除前 **#184 §4 未落地 ⇒ 第一波的 `winrate` 仍不可引**
   —— 这件事要在发波**之前**摆到总监面前,不是发完再说。

## 12. token 用量(铁律 8)

见「补记」(收尾时跑 `tools/agent/token_usage.py`)。

---

## 补记(第一次 push 之后 — GH #290 顺序)

**(A) 铁律 6 三条腿**(两次 push;第二推 memo 命中,读数逐位相同):
- `GATE_EXIT=0  CLEAN (iron rule 6 static half passed)`
- `py gate: 134 ran, 0 findings, 0 uncertifiable, 41.5s`
- `lua gate: SKIPPED BY SCOPE -- this push touches no bots/game/tests path.`
  ⚠️ **第三条腿是 SKIPPED BY SCOPE 不是绿**(本轮只改 `iterations/` 两个文件;
  `lua gate: scope = 2 path(s) changed vs the pushed refs' base (base=fallback-merge-base)`)。

`ARM_EXIT=0`;**⛔ 未用任何 `RULE6_BYPASS`**;`PULL_EXIT=0`(`Current branch ... is up to date`
⇒ 无 rebase、无 `--force-with-lease`);`PUSH1_EXIT=0`(`* [new branch] claude/great-sagan-3jo8zl`)/
`PUSH2_EXIT=0`(`a557e1e2..92062121  HEAD -> main`)。

⭐ **按文档顺序先分支后 main,而且本轮 memo 自己把理由打出来了**,逐字:
`RULE6_MEMO=REUSE  a green three-leg reading taken 2026-09-18T06:15:34Z on THIS EXACT tree.`
⇒ 分支推**未移动** `origin/main` ⇒ 第二推问的是同一个问题、memo 免费答掉(~41.5s 省下)。
⛔ 未反序 —— 反序会让 memo 键 `(HEAD^{tree}, origin/main)` 恒定 MISS,且让第二推的
lua 腿 scope 恒为空而按 fail-closed 跑全集(RULING 69 / GH #865)。

**(B) 自检真码 ⛔ 本轮仍不可得**:收尾时停在 `=== trunk health (python test suite) ===`(630 行),
**收尾三行横幅一个都没打** ⇒ **无 `selfcheck worst exit`,⛔ 不写、⛔ 不读成通过**,
按铁律 7/11 ⛔ 不空转等它。⚠️ **注意这与 (一) 不矛盾**:本轮**起跑**成功(两道自卫都没触发,
这是连续八轮以来第一次),**但整轮跑完仍超出本会话窗口** —— 两件事分开登记,
⛔ 不许用「起跑成功」代替「跑完了」。已跑完腿的读数见 §1,**未跑完的腿这轮没人看过**。

**(C) 收尾泄漏复查** `check_costs.sh --leak-only` → **`LEAK_EXIT=0`**,
`running/pending instances` 区块**空**,常驻仍只有 AMI `ami-0a990a26d89c66547` 一张
⇒ **收尾与开工两次读数一致**。

**(D) 发表**:⛔ 零 issue、零评论 ⇒ `claim_precheck.sh` **未跑**
—— **⛔ 这是「没有可发表的东西」,不是「检查通过」。**

**(E) token 用量(铁律 8)**:`TOKENS total_in=3,156,871 out=28,659 turns=32`(统计后的收尾回合不计入)。
⭐ **回到常态并低于常态**(常态参考 `3.56M / 31.1k / 33`),且**连续两轮下降**:
`5.96M / 54.1k / 52`(09-18T00:24Z)→ `5.44M / 39.3k / 48`(03:13Z)→ **`3.16M / 28.7k / 32`**(本轮)。
⭐ **归因可指认,两条**:① 照上一轮交棒只按行区间取章程(2.6 MB 文件从不全读);
② §5 那个新测量**在 python 里聚合后只回传汇总表**,⛔ 没有把 920 份报告或任何 JSON 贴进上下文
—— 一次全量普查的 token 成本因此接近零。

---

## 补记 2(⭐ 订正 — 自检真码本轮**可得**,补记 (B) 的两句话是错的)

第一次 push 之后自检后台跑完了(`679` 行,比写补记时多 49 行)⇒ **收尾三行横幅全都打了**。
⛔ **补记 (B) 里的「真码仍不可得」和「整轮跑完仍超出本会话窗口」两句都不成立,当场订正,不辩解。**
⚠️ **失效形状记一笔给下一轮**:本台在**进程还在跑**的时刻,把「我现在读不到」写成了「这轮读不到」
—— 两者之差是**一次后台完成通知**。铁律 7/11 禁的是**空转等待**,**不禁「先写完别的、末尾再回读一次」**;
⇒ **取法补一条**:自检后台跑时,**收尾前回读一次输出文件**,再决定写哪一句。

**真码(逐字)**:

```
selfcheck worst exit: 3
legs run              : 15
FINDINGS (exit 3)     : cadence queue-rulings owed-executions lua-coverage
UNCERTIFIABLE (exit 2): trunk-red(python)
NOT RUN (inside a leg): tests/test_lua_gate.py tests/test_luacheck_gate_soakswitch.py tests/test_selfcheck_lua_leg.py
```

⭐ **归因是工具自己打的,⛔ 本台未手工归因** —— 工具在同一块自带警告,照抄:
`Read this before writing "exit 3, all of it cadence": that sentence was hand-made attribution for days,
and on 2026-08-27 it was wrong for 22 hours (GH #267).`
四条 `FINDINGS` 里 **`lua-coverage`** 与 **`owed-executions`** 正是 §1 已登记的两项
(`UNCOVERED SET GREW -- 2 file(s)` / `gh801_...` 等欠条);`cadence`、`queue-rulings`
⛔ **本台不代解读、不代修**(不是批测台的域)。

**python 腿实读**:`148 passed, 0 failed, 3 uncertifiable`,三条 uncertifiable 是
`tests/test_lua_gate.py` / `tests/test_luacheck_gate_soakswitch.py` / `tests/test_selfcheck_lua_leg.py`,
原因逐字 `lua5.1 is absent and this file's subject needs it.` 与
`luacheck is not installed, so the behavioural half did not run.`
⚠️ **但腿级横幅是 `UNCERTIFIABLE` 不是绿**,逐字照抄:
`UNCERTIFIABLE -- a python test did NOT run (could not read its input). This line is NOT a pass,
and it is NOT evidence that trunk is red: re-run on a quiet tree (nothing writing under bots/).`
⇒ **⛔ 本台既不主张 trunk 绿、也不主张 trunk 红**;`0 failed` 是**已跑那 148 个**的读数,不是对 trunk 的判决。
另 `9 check(s) did not run; this is NOT a pass.`(`5a0` 的 `120s` 截断一族,
`5a0 cost: 138 file(s) ... in 120.0s / 120.1s (budget 120s)`,与上一轮同族)。

**Lua 快腿**:`138 tagged detector file(s), 0 failures -- FAST SUBSET, not the full suite.`
⚠️ 自述边界照抄:`Untagged tests (and anything needing one process for the whole suite) are NOT covered here.`

⛔ 本轮 `bots`/`game`/`tests`/`tools` **一行未改** ⇒ 这 `exit 3` 的四条 **无一条由本轮引入**,⛔ 不代修。
⇒ **交棒补一条**:下一轮读到的若仍是同四条,**先按本条核对是不是同一批**,再决定要不要点名。
