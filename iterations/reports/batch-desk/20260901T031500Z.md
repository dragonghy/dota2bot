# 批测台 2026-09-01T03:15Z —— 发波轮(W33,50-id 家族首波)

> 与上一轮(00:35Z)交棒预期一致:本轮是**发波轮**。闸 (i) 于 `2026-09-01T03:30:48Z` 解锁,
> 发波块内的 `date -u` 守卫在 03:17:18Z **开火一次(exit 9,810s 未到)**,本台**没有绕过它**,
> 做完全部零成本准备后到点再发。

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh`,**第一次调用是经管道的**(`| tail -50`),
守卫**当场拒绝**并打:

```
REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.
... This is evidence discipline 3; it has recurred 5x, every time as the first command of the round.
SELFCHECK_EXIT=2  REFUSED (nothing was checked; this is NOT a pass)
```

**这是总监 §22 管道守卫的第四个外部验收点**,与上一轮同形:本轮第一条命令又踩了同一个坑,
守卫又当场拦住。此后本轮**所有读数一律走 `rc.sh` 或重定向**,报告里写的都是 `BARE_EXIT` / `RC_EXIT`。

重跑(重定向,真退出码可读):

- **push gate 已上膛**(`core.hooksPath=.githooks`)
- **unlanded work**:`OK: no unlanded work in the certifiable window`(25 条可核 ref)
- **cadence**:3 个 GAP(director 3.5h / replay-check 3.8h / strategy 3.6h),均**刚过 3.5h 阈值**
- **queue rulings**:`RIDESHARE: none` / `OTHER: none` ⇒ **本轮无待裁路由**;
  `total open 48`,`UNKNOWN STATUS 4`(#317 老四条,不变)
- **ORPHAN_PROPOSAL:none** ⇒ 上一轮点名的 `roamidle` 孤儿提议**已被总监入集消化**(见 §2)
- **stable 锚点**:v1/v2 的 EXISTS / PINNED / SHIPPED **全 ok**
- **trunk red(python)**:`FAIL tests/test_rc_wrapper.py` → `70 passed, 1 failed, 1 uncertifiable`
  → 横幅 `TRUNK RED` —— **经复核为假红,详见 §6**

## 1. 成本与泄漏(开工)

`check_costs.sh`(经 `rc.sh`,`RC_EXIT=0`):

- **running/pending 实例:空** ⇒ W32 四台按需**已全部自毁干净,零泄漏、零孤儿**
- **MTD `75.988` USD**,`source: budgets/dota2bat-batch (free)`,
  **`budget refreshed 2026-08-31T21:01:57Z`** —— 与上一轮**逐位相同**,
  **这仍然是八月的数,尚未滚进九月**
- 读数 ≥ $35 ⇒ 脚本自动花 `$0.01` 做 CE 复核,CE 九月 MTD 打 **`0 USD`**
  (**空 = 滞后,不是零花费的证据**;八月的滞后带 4.3–11.3h 在 §甲 有记录)
- 常驻资源只有 AMI `ami-0a990a26d89c66547`,无快照泄漏

### 1.1 九月围栏 = **$50**(跨月重置,本轮第一次实际使用)

`dota2bot-batch` 的 owner 可见 ACTUAL 告警档是 **$50 / $80 / $100**(限额 $100 的 50/80/100%)。
围栏 = **下一个尚未跨过的档**。八月里 $50 早已跨过,故当时用 $80;**九月从零开始 ⇒ 下一档是 $50**。
刹车线 $90 / owner 批准线 $100 **不动**。

**八月终值仍未定论**:`budgets` 快照停在 08-31T21:01:57Z(**比 W32 起飞还早**),
上一轮估的八月终值 ≈ **$78.2–78.3**(多半未跨 $80)。本轮的 `budgets` **仍未滚月**,
⇒ **这条确认再欠一轮**。若滚月后确已跨 $80,补写配套义务那一行(见章程步骤 4(b)(iii))。

## 2. 收割:**本轮无新数据**

`s3://…/validation/` 最新对象是 **08-31 22:23:47Z**(W32 的两份 run.log),
即**上一轮已经全量收割过的那一批**;`soak/` 也没有 22:2xZ 之后的新前缀。
⇒ **零收割工作**,queue.json 无 status=running 的请求需要回填。

### 2.1 ⭐ 补掉一根掉在地上的接力棒:W32 的 `harvest` 从未回填

`iterations/reports/batch-desk/waves/W32_wave.json` 在本轮开工时读到 **`harvested_at: null`**,
**而 W32 明明在 00:35Z 就被完整收割了**。GH #271 的规矩是「**起飞时写,收割时回填**」,
00:35Z 那一轮把读数写进了自己的报告,**却没有写回这个文件**。

**这不是数据丢了,是棒掉了** —— 而它掉在一个**会静默失效**的地方:
`reclaim_blind.py --wave-json` 读的就是这个文件里的**裸字段名**(`ab` / `ba` / `arm_depth` /
`status_code` / `create` / `update`),下一轮发波前的闸 (iv) 就靠它。

本轮已把 00:35Z 报告 §2.1(逐粒 ab/ba/arm_depth)与 §3(机时)**回填进该文件**,
并显式标注 `backfilled_at` 与来源。逐粒:

| seed | ab | ba | `arm_depth` | S3 时戳机时 |
|---|---|---|---|---|
| 2604 | 37 | 17 | 23.30 | 0.878 h |
| 2624 | 42 | 16 | 23.17 | 0.880 h |
| 2737 | 36 | 17 | 23.09 | 0.877 h |
| 2970 | 30 | 12 | 17.14 | 0.665 h |

**`survival_bound` 一律写 `lower` 不写 `exact`** —— S3 首末对象的定界只可能**偏小**,
这正是 `reclaim_blind.py` 的 `BOUND_LOWER` 存在的理由,也正是 GH #332 立案时点名的那种替换:
**把一个代理值填进一个以「测量」命名的字段**。

### 2.2 ⭐⭐ 闸 (iv) 的 `reclaim_blind.py` 在按需波上 **`RC_EXIT=2`(UNDECIDABLE)**

回填之后仍然跑不过,逐字:

```
=== reclaim-blind check (GH #271 ruling) -- wave W32 ===
changeover 40.0 min  (measured bracket 34.80 < x <= 40.63, margin above the constant 0.63 min)  min_arm_depth 8.0
UNDECIDABLE: seed 2604: unknown SIR status_code None (known: instance-terminated-by-user, instance-terminated-no-capacity)
RC_EXIT=2
```

**`2` 是本仓库的「没跑成」码,不是通过**(`rc.sh` 自己也把这行打出来了)。

原因是结构性的:**W32 是按需波,根本没有 spot request,也就没有 SIR `Status.Code` 可读**;
而 `reclaim_blind.py` 的 `KNOWN_CODES` 只有两个 **SIR** 码。
本台**拒绝**往 `status_code` 里填一个猜的 SIR 码把它变绿 —— 那正是 §2.1 末尾那条纪律。

**为什么这不阻断本轮发波**(理由是结构,不是绕过):

1. **按需实例在构造上不会被回收** —— reclaim-blind 这个问题在按需波上无从发生;
2. W32 **四粒种子全部配对**,最小 `arm_depth` **17.14 > 门槛 8**,
   `thin_arm_seeds []` ⇒ 这一波**没有被任何回收致盲**,读数是全的;
3. 该工具在能跑的情况下给下一波的建议本来就是 `NEXT WAVE: spot`(GH #158 的默认),
   **与本轮实际选择一致**。

⇒ 结论是「**这一波没被致盲,但这句话不是工具说的**」,已如实登记,并**追评 GH #375**(§7)。

## 3. 发波决策:**发**(W33)

| 闸 | 判定 | 依据 |
|---|---|---|
| (i) 距上波 ≥6h | **PASS(到点才发)** | W32 `2026-08-31T21:30:48Z` ⇒ 解锁 `03:30:48Z`;块内 `date -u` 守卫 03:17:18Z 开火 exit 9,到点后重跑 |
| (ii) 有新东西可测 | **PASS,且是对的理由** | armed 串 **48 → 50**:总监入集 `roamidle` + `outlatch`(commit `2770d1e7`);两者**都已 wired** |
| (iii) 预算 | **PASS,余量一个数量级** | 九月 MTD ≈ $0(围栏 $50);spot 估价 $0.90 ⇒ $0.90 ≤ $50。保守变体(把 12h 内的 W32 按 $2.30 也算进来,尽管那是**八月**的钱):$3.20 ≤ $50 |
| (iv) 输入齐备 | **PASS(带一条 UNCERTIFIABLE)** | 无欠收割;queue 十条 pending 全是 `ROUTED_ARCHIVE_SCAN`(零 EC2、零波次),**无发波请求**;`strategy-5b`(`APPROVED_CONDITIONAL`, wave=W6)仍卡在**它自己的条件 (3)**(事件轴量级未给)——**第六轮,本台不重裁**。`reclaim_blind` = **exit 2,见 §2.2** |

### 3.1 闸 (ii) 的细节(浅克隆陷阱按正确形状规避)

先 `git fetch --depth 1 origin 85bcbeb76af942ac5d7b9ccc997c42fc7ced02e1`(**缩写 SHA 取不回**,`fetch=0`),
再 `git log 85bcbeb7..HEAD -- bots/ game/` ⇒ **`BARE_EXIT=0` 且输出非空**(一个 commit:
`175c8b9d [strategy] outlatch`)⇒ **可比,不是空输出被读成无漂移**。

⭐ **注意 `roamidle` 的形状**:它的代码(`11b7e2e8`)在 **W32 的钉树上就已经在了**,
但当时**不在 armed 串里** ⇒ 两条腿都够不到它,是 W32 报告里登记的那个「$0 增量搭车项」。
本轮它**被入集**,于是**同一份代码第一次真的被测**。这正是 18:13Z 那一轮拒发时讲的道理的正面:
`bots/` 动过 **不等于** 有新东西可测;**armed 串动了才算**。本轮两条腿都成立。

## 4. 发波前四道门(全部不经管道读)

- `seed_roster_index.py --build` —— **`RC_EXIT=0`**,`297 run prefixes in S3, 293 already indexed, **4 to scan**`
  ⇒ W32 那四粒**已入账**(第六次连续执行固定的发波前 build)
- `carrier_terms.py --arm-file iterations/streams/test_set.md` —— **`RC_EXIT=0`**:
  50 ids ⇒ **10 hero-scoped / 40 generic / 0 unresolved ⇒ 6 term**,
  与 W27–W32 **同六项**(`roamidle` → `bots/mode_team_roam_generic.lua:651`、
  `outlatch` → `bots/mode_outpost_generic.lua:79`,**两者都解析为 generic,没有移动载体要求**)
- `seed_draft.py --assert-carrier-from-arm <50串> 2756 2790 2887 2938` —— **`RC_EXIT=0`**,
  `CARRIER_GATE ids=10 seeds=4 exit=0`;逐项载体
  **cm 3 / lion 2 / od 2 / wk 4 / sb 2 / zuus 3 ⇒ 16 载体槽**
  **诚实边界(第十一轮不变)**:「≥2/term」是**本台自律**不是门;
  官方门只要 ≥1(逐项报 `PARTIAL` 仍 exit 0)。**GH #285 第十六轮未裁。**
- `check_armed_wiring.py --cand <50串> --ref e84fe4d2…` —— **`RC_EXIT=0`**:
  `all 50 armed ids wired on e84fe4d216efdaac3b40966b1fb542d36816e1db`,
  含新入集的 `roamidle` / `outlatch` 两条 `direct`

**树核对**:`git ls-remote origin main` = `e84fe4d216efdaac3b40966b1fb542d36816e1db` = 本地 HEAD = 钉树;
**发波块内再核一次**(漂移则 exit 8),另有 armed id 数守卫(≠50 则 exit 7)。

### 4.1 选种:`[2601,3000]` 窗口,**天花板一波之内 18 → 16**

掩码穷举(GH #313 方法)跑在**刚 build 过的**索引上:

| 窗口 | banked/未用 | popcount | 有效 4-掩码组合 | BEST |
|---|---|---|---|---|
| 上一轮 [2601,3000] | 0 / 400 | `{5:2, 4:11, 3:56, 2:141, 1:140, 0:50}` | 3342 | **18** |
| **本轮 [2601,3000]** | **4 / 396** | `{4:9, 3:56, 2:141, 1:140, 0:50}` | 687 | **16** |

⭐ **章程点名的那个机制第四次被确认,而且这次供给表把它写得最直白**:
W32 拿走的四粒**恰好是 pop-5 两粒 + pop-4 两粒** ⇒ **pop-5 从 2 归 0,pop-4 从 11 掉到 9**。
每一波都挑载体最富的四粒,于是**每一波都削掉窗口最顶那层**;顶层一旦见底,天花板就一次掉一档。

**本轮不右移窗口**(与上一轮不同):BEST **16 ≥ 12**(6 term × 2)⇒ 自律仍能满足,
右移的触发条件没到。**诚实边界**:pop-4 还剩 9 粒,一波吃 4 ⇒ **这个窗口大约还能撑两波**,
**W35 前后会需要再右移一次**,而右移只是搬家不是修理。三选一仍挂在 **GH #285(第十六轮未裁)**。

选出 **2756 / 2790 / 2887 / 2938**,四粒**在任何历史波次中从未用过**。

## 5. 发波:**W33 起飞,四台 spot,零降级**

### 5.1 起飞记录

**闸 (i) 的守卫开火了一次,本台没有绕过它。**
本 Routine 在 **03:11Z** 触发,比解锁点 `03:30:48Z` **早约 20 分钟**。
发波块内的 `date -u` 守卫在 **03:17:18Z** 打 `GATE-I NOT UNLOCKED: 810s remaining` 并 **exit 9**。
本台**没有**援引 2026-08-19T06:07Z 那种「只差两分钟」的目的解释,
而是把余下的零成本准备(选种、四道门、W32 回填、报告草稿、评论草稿)全部做完,
到点后 **03:31:05Z** 重跑同一个脚本。**实际差额 17 秒,方向是解锁之后。**

四次独立调用,**每次显式一个 `--az`,四次四个不同**(#252 的发法):

| seed | AZ(请求=实得) | instance | run_token | 起飞 | SIR |
|---|---|---|---|---|---|
| 2756 | us-west-2a | `i-09c6ae0d5b352c701` | `e45160` | 03:31:05Z | `sir-n1tfgm8p` |
| 2790 | us-west-2b | `i-01e482da43780ce52` | `f3c64a` | 03:31:10Z | `sir-56qfj5mp` |
| 2887 | us-west-2c | `i-0a0ed5369ee235b3a` | `893bb1` | 03:31:15Z | `sir-ae1qhqkn` |
| 2938 | us-west-2d | `i-011eff50b3afac2a3` | `d39101` | 03:31:20Z | `sir-xfpqkesn` |

配置:`c6i.4xlarge` **spot**,`--slots 16`,`--rec-slots 1`(GH #308 分支 (A),**第十波**),
`--hours 2`,`--games 12`,钉树 `e84fe4d2…`。

### 5.2 市场:**级 (1) spot 一次全中,零降级**

**四台全部在阶梯第一级、第一次询问即中**,**零 `re-aiming inside the ring`、零 `AZ RING EXHAUSTED`**。
W32 那次「十波零降级记录中断」到此**重新起算(现为 1)**。

**证据是 `describe-instances` 的 `InstanceLifecycle`,四台全打 `spot`**;
另有四份 `fulfilled` 的 spot request 交叉印证。
**不是** run_id 的 `spot_` 前缀 —— 那是 `spot_run.sh:155` 硬写的字符串,与市场类型无关。

值得登记的对照:**十一小时前 W32 那次,c6i 与 c6a 两族 spot 池在四个 AZ 同时空了约 11 分钟**;
本轮同机型同区同 AZ 集合**一次全中** ⇒ 那次是**池子的瞬时排空**,不是结构性的容量退化。

### 5.3 ⚠️ GH #371:`spot_run.sh` 的退出码本轮**照旧不可信**

四次调用**全部打 `exit=0` 并打印 `launched`**,而 #371 立案句正是「失败时也这样打」。
⇒ 本台**没有**把它当证据,起飞用**带外**方式核实:
`describe-instances` 四台 `running` + `InstanceLifecycle=spot`,
`describe-spot-instance-requests` 四份 `fulfilled`。**#371 仍 open 未裁(第二轮)。**

### 5.4 唯一性与 AZ 分散(免费核对)

`soak-run` 标签四个值**两两不同**(`e45160`/`f3c64a`/`893bb1`/`d39101`,
取自 `describe-instances`,**独立于本会话自己的 transcript**);调用间隔 5s/5s/5s,全部跨整秒。
`Placement.AvailabilityZone` **四个互不相同** ⇒ **GH #252(≥2 个)与 GH #256(4 个且零 re-aiming)
的验收本轮双双满足**,而且这次是在 **spot** 上满足的(W18/W32 是在别的市场类型上)。

### 5.5 自毁与回收处置(事先登记)

四台均 `--instance-initiated-shutdown-behavior terminate` + `shutdown -h +120`,
**硬顶 `2026-09-01T05:31Z`**,按近波 0.75–0.92 h/台 的形态,预计 **04:15–04:30Z** 自关。

**本波是 spot ⇒ 回收是活的退出路径**(与 W32 不同)。按章程事先登记:
**一台被抢占 = 该种子缺臂,那一粒的配对差作废,不要整波作废**;
已落盘局由 `recover_verdict.py` 从 S3 重算,缺的那粒**再发一台补跑**(仍走同一阶梯)。

### 5.6 ⭐⭐⭐ 事先登记的回收处置**当轮就被用上了** —— 一台在 3 分 17 秒后被收回

**`i-09c6ae0d5b352c701`(seed 2756,us-west-2a)于 `03:34:25Z` 被 EC2 收回**,
起飞 **03:31:08Z**,存活 **3m17s**,**零局产出**。

| 证据源 | 读数 |
|---|---|
| `describe-instances` `StateReason.Code` | `Server.SpotInstanceTermination` |
| 同上 `StateTransitionReason` | `Service initiated (2026-09-01 03:34:25 GMT)` |
| `sir-n1tfgm8p` `Status.Code` | `instance-terminated-no-capacity` |
| 同上 `Status.Message` | `Your Spot instance was terminated because there is no Spot capacity available…` |

**处置照章程,当轮完成**:那一粒**缺臂作废、整波不作废**,**同一阶梯补发一台**:
**`i-029e65db1adae13f0`**,`run_token` `25b94f`,**us-west-2a,级 (1) c6i spot,第一次询问即中**,
`03:37:45Z` 起飞,`sir-t85qjqtm` `fulfilled`,`InstanceLifecycle=spot`。
⇒ **AZ 分散回到 4 个互不相同**,四台全部 `running`。
**无 `VcpuLimitExceeded`**:被收那台补发时已是 `terminated`(不是 `shutting-down`),
`4×16=64` 的配额天花板从未被争用。

⭐ **同一个 AZ 在 3 分 20 秒后就给了容量** ⇒ 这是**池子的瞬时排空**(与 W32 同形),
**不是 us-west-2a 的常态性容量失败**。按 GH #256 的裁定,**不把 `2b` 移出 `AZ_LIST`** 的同一条理由
在这里同样适用于 `2a`:移出会把 4 路分散变成 3 路,**反而抬高剩余种子之间的相关性**。

### 5.7 ⚠️ 但这一台是**偶然**被看见的 —— 收尾检查看不见「少一台」

**必须如实写下来**:§8.1 的收尾 `--leak-only` 跑在 **03:36Z**,它读出的是**健康**。
它没错 —— **`--leak-only` 只列 `pending,running`,只能报「多出来的」,「少一台」它不会举手**。
本台**从 W29 起就在报告里写过这句话**,但**那一直只是一条纪律,没有任何固定动作去执行它**。

这台是本台为了给 GH #375 的评论量一个「terminated 实例还能读多久」,
顺手跑了一次 `describe-instances --filters state=terminated,shutting-down` 才撞见的。
**靠顺手,不是靠门。** 若没顺手,W33 会**三条臂静静跑完**,
直到**下一轮收割**才在 `thin_arm_seeds` / `arm_depth` 上现形 —— 而那时补跑要多花一整轮,
且 terminated 记录**早已过了 API 的约 1 小时保留期**(GH #375),连归因都做不了。

⇒ **本轮把它从纪律变成动作,写进本台章程步骤 6**(自订,零成本,不需要总监裁定,
也不改 harness):**发波轮收尾固定跑两条查询** ——
(1) `pending,running` 的 `soak-run` 标签集合**逐一对上 `W<N>_wave.json:machines[].run_id`**
(少一个 = 掉了一臂,多一个 = 泄漏);
(2) `terminated,shutting-down` 里**出现落在本波窗口内的 `Server.SpotInstanceTermination`
就是本波掉了一臂**。**这一步必须在发波那一轮做,过期就读不到了。**

## 6. ⭐⭐ GH #364 的假红**第三次独立复发** —— 而本轮第一次拿到一个**具名的混淆源**

见 §0:自检的 python 腿打 `FAIL tests/test_rc_wrapper.py` / `70 passed, 1 failed` / 横幅 `TRUNK RED`。

**同一棵树、同一个容器、几分钟之内**单跑该文件:

```
rc.sh wrapper: all checks ok
RC_EXIT=0  (bare exit code of: python3 tests/test_rc_wrapper.py)
```

**11 条 check 全 `ok`。** 这已是该文件在自检那一趟挂、裸跑全绿的**第三次**
(前两次:08-31T12:30Z 那棵树、09-01T00:35Z 的 `cc6bf22`,均见 #364 与上一轮报告)。

⭐ **本轮的增量是一个可以被证伪的机制,不再是「负载/时序」这种说不清的归因**:
`test_rc_wrapper.py` 测的**正是 `rc.sh` 自己**(`SIGTERM: exits 143` 一类的时序行为),
而**自检的 python 腿在后台运行的整段时间里,本会话正在前台密集调用 `rc.sh`**
(本轮到那一刻为止:`check_costs` / `seed_roster_index` / `carrier_terms` ×2 / `reclaim_blind` /
`seed_draft` / `check_armed_wiring`)。
**被测对象与测试进程共享同一个宿主**,而它断言的是**时序**。

⇒ 建议加进 #364 的验收:**要么让自检的 python 腿在独占条件下跑,要么让 `test_rc_wrapper.py`
不去断言绝对时序**。可证伪性:**在没有并发 `rc.sh` 的容器里应当无法复现**。

**并且 #364 建议 1 仍未落地,本轮是它第三次被需要** —— 自检那层的
`grep -E '^(FAIL|failed:|[0-9]+ passed)'` 把逐条输出滤掉了,
所以**本轮同样不知道具体哪一条 check 挂了**。三轮下来,这个信息一次都没拿到过。

**失效方向仍是危险的那一侧**:`DECISIONS_NEEDED.md` §14 已记过「横幅打对了 TRUNK RED、
三个 stream 照样落在上面」——**一条会假红的横幅,训练的正是「横幅可以不信」**。
**本台不改 harness。**

## 7. 交棒

**顺序条款(GH #290)照做:先 push,再发表**;两份草稿各过一次 `claim_precheck.sh`。

1. **⭐⭐⭐ 总监 —— W33 收割后 50-id 家族第一粒读数到手**,连带 **GH #363**(并池与否)第六轮。
   48-id 家族**只有 W32 一粒**就冻结了(armed 串又动了),47-id 冻结在 8 粒 ⇒
   **「一个家族要不要并池」现在是一个当场可裁的问题,不需要预测未来**(与上一轮同论,数据更硬)。
2. **⭐⭐⭐ 总监 —— 闸 (iv) 的 `reclaim_blind.py` 在按需波上结构性跑不出结果**(§2.2)。
   已**追评 GH #375**(同一个缺失记录的第二个下游后果,**未新开案**)。
   它需要的信息(`InstanceLifecycle`)正是 #375 正文那条修法要落盘的东西 ⇒ **只需补一条验收**。
3. **⭐⭐⭐ 总监 —— 按需单波常数 `~$2.15` 仍待重裁**(上一轮 00:35Z 交出,本轮仍未见裁定)。
   W32 实测 **0.825 h/台** vs 常数依据的 0.745(**+11%**),单波 `$2.22–2.30`,建议 **`$2.30`**。
   **本台不自行改常数。**
4. **⭐⭐ GH #364 假红第三次复发,且第一次有可证伪的机制**(§6):
   `test_rc_wrapper.py` 断言的是 `rc.sh` 的**时序**,而自检的 python 腿与本会话前台密集的
   `rc.sh` 调用**并发**。已追评。**建议 1 第三次被需要,仍未落地。**
5. **⭐⭐ GH #285 第十六轮未裁**,本轮买到的数据继续变强:窗口天花板 **18 → 16**,
   pop-5 **2 → 0**、pop-4 **11 → 9**(**第四次机制确认**)。本轮**不右移**(BEST 16 ≥ 12),
   但 pop-4 只够再吃两波 ⇒ **约 W35 必须右移一次**。
6. **⭐⭐ GH #371 第二轮**:`spot_run.sh` 四次调用全打 `exit=0` + `launched`,本轮仍靠带外核实。
7. **⭐⭐ GH #308 第十一次点名**:`rec_slots` 仍为 1(分支 (A)),**第十波**;
   owner P1/P2 的条件 (a) 仍卡在这个 1/16 的帧通道上。
8. **⭐ GH #271 的执行缺口**:W32 的 `harvest` 回填**被上一轮漏掉**,本轮补(§2.1)。
   **不是数据丢了,是棒掉了**,而它掉在一个会静默失效的地方(闸 (iv) 读的就是那些裸字段名)。
   W33 的记录里已写死这条提醒。
9. **⭐ GH #358 本轮未复发**:自检的 Lua 腿跑完 **64 个检测器文件、0 失败**,未超时。
10. **⭐ 存量**:#218;#367 待裁;#295/#252/#256/#282 建议关闭;#329/#321 待裁;
    `UNKNOWN STATUS` 4 条(#317);`strategy-5b` **第六轮**;#313/#290/#291/#298/#299/#349/#350(#355)照旧;
    #207 停在第三十九波 armed。

### 7.0 ⭐ 本轮自订的一条动作(**不需要总监裁定**,写进本台章程步骤 6)

见 §5.7:**发波轮收尾固定跑「波次点名」两条查询**,把「`--leak-only` 看不见少一台」
从**一条纪律**变成**一个动作**。这属于本台自己的工作流,零成本、不改 harness、不动别组,
故**当轮落地不入 issue 流**;若总监认为该升级为 harness 改动(例如让 `check_costs.sh`
直接吃 `W<N>_wave.json`),那是 `[harness]` 的事,本台不自己改。

### 7.1 已发表

**顺序条款(GH #290)照做**:两次 push(`e84fe4d2..372ff09c`、`372ff09c..da0c98b7`)都在发表之前;
两份草稿各过一次 `claim_precheck.sh`,**均 `RC_EXIT=0` / `local commits not on origin/main: 0` /
`OK to publish: every citation resolves on origin/main.`**

- **GH #375 追评** `issuecomment-5488564682` —— 闸 (iv) 在按需波上 `RC_EXIT=2`
  (**同一个缺失记录的第二个下游后果,未新开案**),连带把「terminated 保留期」夹成
  `> 3 分钟 / < 2 小时`,并登记 §5.7 那条「靠顺手不是靠门」。
- **GH #364 追评** `issuecomment-5488567617` —— 假红第三次复发 + **首个可证伪的机制**
  (自检 python 腿与前台 `rc.sh` 并发,而该文件断言的正是 `rc.sh` 的时序),
  连带 #364 建议 1 第三次被需要、#339 / #267 的连带影响。

**#363 / #308 / #285 / #371 / `~$2.15` 重裁**本轮**只登记在报告与章程,未另发评论**
(同形重复点,从简;`~$2.15` 是上一轮已交出、仍待总监裁的一条)。

### 7.2 queue.json 变更

六条 **ADMITTED 搭车**请求的 id 都在本波 armed 串里 ⇒ 按章程步骤 5「启动后把请求标记 status=running」,
本轮把它们从 `pending` 改为 `running` 并写上本波的钉树/种子:
**`strategy-25`(creepthink)/ `hero-24`(lionqdmg)/ `hero-25`(cmqreach)/
`strategy-26`(rotscope)/ `strategy-27`(roamidle)/ `strategy-28`(outlatch)**。
十条 `ROUTED_ARCHIVE_SCAN`(零 EC2、零波次)与 `hero-26` **保持 pending**,本轮不动。

## 8. 局数(owner 2026-08-19 要求)

**(a) 上一波(W32)最终有效局数**:`files_seen 231 / games_loaded 231 / unparseable 0 / unfinished 0`;
**207 局带 50-id 前身(48-id)`mirror:` 戳计分,24 局是无戳排空局按 `STAMP_SEED` 自动出集**;
逐粒 ab/ba 见 §2.1 表(2604 37/17、2624 42/16、2737 36/17、2970 30/12),
`min_arm_depth 8`、`thin_arm_seeds []` ⇒ **四粒零排除**。

**(b) 本轮在跑波次(W33)**:**预期** 4 种子 × 12 局 × 2 腿 = **96 局标称**,
近波实测落 **200–240 已计分局**(W32 231 载入 / 207 计分;W31 222 / 198)。
发报时(03:3xZ)四台全部 `running`,S3 `soak/<run_id>/` 前缀计数为 **0** —— 属正常,首局需十余分钟。

## 8.1 收尾泄漏检查

`check_costs.sh --leak-only`(经 `rc.sh`,**`RC_EXIT=0`**):running/pending **恰为 W33 的四台**,
`i-09c6ae0d5b352c701` / `i-0a0ed5369ee235b3a` / `i-011eff50b3afac2a3` / `i-01e482da43780ce52`,
**无一孤儿**;常驻只有 AMI `ami-0a990a26d89c66547`。

⚠️ **本轮是发波轮 ⇒ 收尾要的不是「0 台在跑」**(那是不发波轮的判据),
**四台在飞且每一台都对得上名字**才是正确读数。

⚠️⚠️ **这次读数虽然是绿的,但它读不出 §5.6 那台已经没了** —— 它跑在 03:36Z,
当时正好是「三台在跑 + 一台已被收回」,而 `--leak-only` 只列 `pending,running`。
**这正是 §5.7 立的那条新动作要挡的东西。** 补发之后(03:37:45Z)的真实状态:

| instance | AZ | lifecycle | soak-run |
|---|---|---|---|
| `i-029e65db1adae13f0` | us-west-2a | spot | `…_25b94f`(**补发,seed 2756**) |
| `i-01e482da43780ce52` | us-west-2b | spot | `…_f3c64a` |
| `i-0a0ed5369ee235b3a` | us-west-2c | spot | `…_893bb1` |
| `i-011eff50b3afac2a3` | us-west-2d | spot | `…_d39101` |

**四个 `soak-run` 值与 `W33_wave.json` 逐一对上,零缺失、零多余。**

## 9. 铁律自查

- **铁律 6**:`bots/` / `game/` **一行未改**(本轮改动全在 `iterations/` 下)。
  **未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行需抄录**。
  动态半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)**未跑,也不声称**。
- **铁律 10**:自检 worst **exit 3**,findings = `cadence` + `trunk-red(python)`;
  **第二项经复核为假红(§6)**;`queue-rulings` 与 `ORPHAN_PROPOSAL` 本轮**均为 none**。
- **铁律 11**:未触发 `requires approval`,无空转等待。
  唯一的等待是闸 (i) 的 6h 节流,**由块内 `date -u` 守卫强制,不是坐等审批**。
- **铁律 2 计量四条**:本轮无新 verdict 读数 ⇒ (i-a)~(i-d) 无适用对象;
  W32 的分层披露已在上一轮报告登记。

## 10. 本轮 token 用量

```
TOKENS total_in=10,841,291 out=61,005 turns=79
```

(统计时刻之后的收尾回合不计入;量级参考。)
