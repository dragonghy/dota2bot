# 批测台 2026-09-04T00:15–00:2xZ

**发 W44:58-id 家族第二次首波,按需(GH #271 的 `BLINDED` 裁定),~$2.15。**
**本轮真正的产出是一件,而且是本台自己的错:闸 (i) 被突破 3 分钟,而它是五道发波闸里唯一一道没有可执行检查的 —— 它在第一次真正被考验的那一轮就失效了,失效方向是花钱那一侧,发波点名全绿。**

---

## 一、自检(铁律 10)

⭐ **开工第一条命令又误用管道**,自检当场自拒
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked`
(**同形第二十二次,又是当轮第一条命令**;工具自报仍是 `it has recurred 5x`,照上四轮据实并列)。
改重定向重跑,`timeout 330` 后被移到后台;**harness 给后台命令报的 `exit code 0` 又是末尾 `echo` 的码不是自检的**(读码陷阱第四轮原样复发),真码从脚本自打的那一行读:

```
legs run              : 9
FINDINGS (exit 3)     : cadence
UNCERTIFIABLE (exit 2): trunk-red(python)
selfcheck worst exit: 3
```

⇒ **有发现不是没跑成**。python 腿 **`84 passed, 0 failed, 2 uncertifiable`**
—— ⭐ **上两轮那两个 trunk 红(`test_chain_member_census.py` / `test_mutstand_restore_trap.py`)本轮已绿**(总监 `bd2dc9e2` 清掉)。
两个 `UNCERTIFIABLE`(`test_rc_wrapper.py` / `test_selfcheck_lua_leg.py`)**不是通过也不是失败**,照登记。
两锚点 `stable-v1`/`stable-v2` 的 `EXISTS`/`PINNED`/`SHIPPED` 全 ok;`OWED_EXECUTION: none`。

---

## 二、成本

九月 MTD **`$12.519`**(免费 `budgets describe-budgets`,快照 `refreshed 2026-09-03T22:56:31Z`)。
MTD `< $35` ⇒ **CE 未触发,没花那 `$0.01`**。进场 `pending,running` **为空**。

⚠️ **快照时刻辨析(不做「增量对得上 ⇒ 已计入」那个被章程禁用的手法)**:快照 22:56:31Z 比 W43 死亡时刻(18:32Z)晚 **4.4h**,**正压在章程 4.3–11.3h 滞后带的下沿** ⇒ **不能断定 W43 已计入**,围栏里照保守价另记一笔。

围栏(铁律 2 甲,逐波按**自己的市场**取价):
`12.519 + W43 spot 0.90 + W44 按需 2.15 = **$15.57**` ≤ **$80**。
**未跨任何 Budget ACTUAL 告警档**(九月尚未到 $50)。**本轮新增支出 ~$2.15。**

⚠️ 一条登记不主张的:`forecast 124.921` > 限额 100,但配置的告警是 **ACTUAL** 制(50/80/100% = $50/$80/$100),**forecast 不触发告警**,当前 MTD 距第一档还有 $37.5。

---

## 三、⛔⛔⛔ 头号发现:闸 (i) 被突破 3 分钟,而没有任何东西能举手(GH #469 已开)

### 事实

- W43 起飞 `2026-09-03T18:21:58Z` ⇒ 闸 (i)(≥6h)解锁 **`2026-09-04T00:21:58Z`**(**上一轮本台自己写进章程的时刻**)。
- W44 四台 `LaunchTime`:**`00:18:45` / `00:18:50` / `00:18:54` / `00:18:58Z`**。
- ⇒ **四台全早;最早的早 3m13s,最晚的早 3m00s。**

**证据源是 `describe-instances` 的 `LaunchTime`,即 AWS 自己的时钟** —— **不是 run_id 里的 `STAMP`**(那个来自本容器,与本会话 transcript 同源,按 §AH.2 不能拿来做排除性陈述)。两者本轮恰好一致,但**能定这个案的是前者**。

### 机制:不是记错了,是结构上没有东西会举手

本轮 `date -u` **只读了一次**,`00:13:47Z`,算出「解锁还有 8 分钟」;随后约 5 分钟的门(成本、树核对、`carrier_terms`、`seed_draft --assert-carrier-from-arm`、`check_armed_wiring`、`luacheck_gate`、`reclaim_blind`、`seed_roster_index`),**然后直接发波,再没把时钟对过第二次**。

| 闸 | 可执行检查 | 本轮读数 |
|---|---|---|
| (ii) 有新东西可测 | `seed_roster_index.py` + 树 diff | `ROSTER_EXIT=0` |
| (iii) 成本围栏 | `check_costs.sh` | `COST_EXIT=0` |
| (iv) inputs / reclaim-blind | `reclaim_blind.py` | `RB_EXIT=1` ⇒ `BLINDED` |
| 周边 | `carrier_terms.py` / `seed_draft.py` / `check_armed_wiring.py` / `luacheck_gate.sh` | `CT_EXIT=0` / `CARRIER_GATE exit=0` / `ARM_EXIT=0` / `LUACHECK_GATE_EXIT=0` |
| **(i) ≥6h 节流** | **没有。散文 + 人脑算术** | **突破 3 分钟,零告警** |

**闸 (i) 是五道里唯一一道靠「上一轮把时刻写进章程、这一轮记得去对」来执行的。**
它**在第一次真正被考验的那一轮失效**:此前每一轮解锁时刻都远在工作窗口之前(几小时),**这一轮第一次落在工作窗口内部**,于是「读一次时钟然后干活」第一次不够用。

⚠️ **失效方向是花钱那一侧**,而且**发波点名全绿**:四次 `spot_run.sh` 各自 `exit 0`(**裸读 `$?`,不经管道**)、四台 `InstanceLifecycle=None` 符合裁定、**四个 AZ 互不相同(#252 验收干净通过)**、run_id 两两不同、收尾点名恰好四台。
**没有任何一份既有产物记录了这次突破** —— 它只存在于 `W44_wave.json:gates` 那一格和 GH #469 里,**而那一格是本台手写的,不是任何工具打出来的**。

⚠️ **`tests/test_wave_gate_keys.py` 本轮 `WGK_EXIT=0`(`208 checks, 0 failed`),而闸 (i) 那一格写着 `BREACHED`** —— 它检查的是**记录完整性**(键在不在、值空不空),**不是闸的结论**。这不是它的缺陷(它是为 W27 的 `gates: null` 造的),但**要写明:它绿不代表闸过了。**

### 处置:未杀波,理由是算术不是偏好

1. 闸 (i) 保护的量是**月度支出**(立案原文「防预算烧穿:每 2h 一波则月成本 $180+」)。围栏 **$15.57** 对 **$80 / $90 / $100**。**这 3 分钟没有移动任何被保护的量。**
2. **晚 3 分钟发的同一波每道闸都完全合规** ⇒ 杀波买不回东西,还赔掉已投出的 ~$2.15。
3. 杀了重发撞章程另一条坑:`4 × 16 vCPU` **正好顶满 64 配额**,`shutting-down` 让重发吃 `VcpuLimitExceeded`(实测约 2m40s)⇒ **「改正后」那一波反而落地更晚**。
4. 对价是**登记**:GH #469 + `W44_wave.json:gates` + 本报告 + 章程。
   ⛔ **不主张**「这次没害处所以下次也可以」;处置追认交总监。

---

## 四、发波参数与门(全部本轮亲手跑,不沿用散文)

- **树**:`git ls-remote origin main` = **`67f9c41ee92480b3aa8b478fea0fd94db30c1e00`** = 本地 HEAD,`git diff --stat origin/main -- bots game tests tools .github .claude` **空**。
  **用 `--ref <40位SHA>` 显式钉死**(四次调用跨 ~13s,期间任何 push 都会让四台 clone 到不同的树,**而那不会有任何东西举手**)。
- ⭐ **树相对 W43 动了,不是逐字重发**:`git log 859e7ab3..HEAD -- bots/ game/` 给 **两个 commit**(`dc63d791` slotwait pid→slot;`0aa7dca3` 决策函数死常量)。
  **独立佐证**:`carrier_terms` 的行号跟着动(`ckpush` `hero_chaos_knight.lua:470` → **`:488`**)。
- **armed 串**:**58 id / 512 字节 / md5 `7009f6c512e1b6bdf514401f20178eca`**,**与 W43 逐位相同**(ids/bytes/md5 **本轮重算**,不抄散文)。
- ⭐ **闸 (ii) 的分叉第四次上门,又一次没被逼出裁定**:**串不动而树动**。
  两种读法**本轮一致同意 PASS**(串家族:58-id 家族 **0 粒计分种子**,因为 W43 一局没存;树哈希:两个 bots/ commit)⇒ **分叉不咬,仍欠总监一个裁定**。
- **选种**:⭐ `seed_roster_index.py --build` 在选种前跑,`ROSTER_EXIT=0`(332 前缀 / 0 新扫 / 索引仍 197 粒)。
  **沿用 W43 的四粒 `3426/3713/3749/3508`** —— **它们是真未用**:W43 存了 **0 份 analysis.json**,四粒**逐粒核对,索引里 0 命中**。
  重用保住了 W43 已验证过的载体覆盖,**让 W44 成为 W43 意图最干净的重发**(只动了树和市场两条轴)。
- **门(裸读,不经管道)**:`CT_EXIT=0`(`58 ids: 11 hero-scoped, 47 generic, 0 unresolved => 7 term(s)`)、
  **`CARRIER_GATE ids=11 seeds=4 exit=0`**(逐项 `sk 4 / zuus 4 / ck 3 / cm 2 / lion 2 / od 2 / sb 2`,**min-per-term 2**)、
  `LUACHECK_GATE_EXIT=0`(`0 warnings`)、`ARM_EXIT=0`(`all 58 armed ids wired on HEAD`)、
  **`RB_EXIT=1`**(`BLINDED` ⇒ `NEXT WAVE: --on-demand`)、**`WGK_EXIT=0`**(`208 checks, 0 failed`)。
- **采集配置** `--slots 16 --hours 2 --rec-slots 8 --games 12` **与 W39–W43 逐字相同**(id 集本轮也没动,所以那条约束不绑;不动仍是**刻意的** —— `rec_slot_baseline.json` 标定在 `c6i.4xlarge`+`rec_slots=8`)。
- ⭐ **上一轮交棒 ②b 的建议本轮执行了**:发完波**当场**跑 `test_wave_gate_keys.py`,**第一次就绿**(上一轮是发波三小时后才发现漏了一整道门)。

### 市场与 AZ

- **四台 `InstanceLifecycle=None` ⇒ 真按需**(`run_id` 的 `spot_` 前缀是 `spot_run.sh` 硬写的字符串,**不是证据**)。
- ⭐⭐ **四个 AZ 互不相同(2a/2b/2c/2d),四台全部落在自己请求的 AZ**,**没有一行 `re-aiming inside the ring`,没有一行 `AZ RING EXHAUSTED`** ⇒ **GH #252 验收干净通过**。
- ⭐ **`InsufficientInstanceCapacity` 全波 0 次**(W43 是 **6 次**,W43+补跑共 9 次)⇒ **这是 #271 那条 `BLINDED` 裁定买到东西最清楚的一个信号**。
- ⚠️⚠️ **但这四个 AZ 不构成对 GH #460 的反证,不许这样读**:**按需与 spot 是不同容量池**,本波说不了「spot 的 `--az` 环在压力下还会不会塌成一个幸存 AZ」——**那正是 W43 量到的东西**。#460 的**(甲)(乙)(丙)(丁)四件待裁全部仍然有效**,要在**下一个 spot 波**上才重新被考验。

---

## 五、⭐ 连掉三轮的零成本活,本轮做掉了:`rec_slot_baseline` 并入

上一轮交棒 ⑦(a)(**已连掉三轮,升为本轮第一件事**)。

- 下载**连 `.demclaim.json` sidecar 一起**(交棒 (e):只下 `*.analysis.json` 会 `exit 2` —— 工具的 recorder set 是**读**出来的不是**猜**出来的)。9 个 run 目录,`analysis`/`claim` 逐目录齐备。
- ⭐ **先做复现核验再动基线**:用**原来那 8 个** run 重跑 `--emit-baseline`,产物与已提交的 `rec_slot_baseline.json` **逐位相同(`IDENTICAL = True`,395 局)** ⇒ 工具确定性成立,**然后**才敢信它 9 个 run 的输出。
- 并入后:**395 → 453 局,8 → 9 run**;`rec_slots` 与 `rec_set` **逐字不变**。
- **逐槽 timescale 位移 −0.04% ~ −0.32%**,方向一致、量级极小。
  ⇒ **远在 5% 容差之内,按构造翻不动任何一个验收结论**(这是本次并入最该被读的一句:它改的是基线的精度,不是它的判决)。
- 录制槽残差 **−0.9% ~ +0.1%**,**8 个录制槽无一超差** ⇒ 「录制代价约等于零」这个结论在更厚的语料上仍然成立。

---

## 六、局数(铁律 7)

**(a) 上一波 W43:最终有效局数 = 0(整波报废)。**
四台 spot 在 `18:32:10Z` 同一秒被同一次 `us-west-2d` 容量事件带走(SIR 四条全 `instance-terminated-no-capacity`),存活 10.20/9.50/9.18/9.10 min,而首份 `analysis.json` 实测约在起飞后 ~14.5 min 落盘 ⇒ **每条腿都差 4–5 分钟没跑出第一局**。
⛔ **这不是一个「测了没效果」的波,是一个什么都没测的波。**

**(b) 本轮在跑波次 W44 实时进度:`00:24:53Z` 读数 = 4 台各 0 份 `analysis.json`,合计 0 局**(起飞后 6 分钟,**早于 ~14.5 min 的首份落盘时刻,0 是预期不是症状**)。
**启动型报告写预期局数:~96 计分局**(4 粒 × 12 局 × 2 腿),暖场局不算有效局。预计自毁 **~01:0x–01:2xZ**。

---

## 七、收割

**本轮无可收割数据**:进场 `pending,running` 为空;W42 及其补跑机已于 15:15Z 轮全量收割;**W43 零语料**。

⭐ **「读语料的门」与「读记录的门」本轮继续分开对待**(上一轮立的方法):
`reclaim_blind.py` 读的是**波次记录**,所以在零语料的 W43 上照跑,并给出本轮发波的市场裁定;
`rec_slot_cost.py` 读语料 —— 但**它这次有语料可读**(W42 补跑机的 58 局),所以第五节那件活成立。

---

## 八、泄漏 CLEAN(判据是「恰好等于意图的那四台」)

发波轮**固定两条点名**(`--leak-only` **只报「多出来的」,「少一台」永远不举手**):

1. `pending,running` 的 `soak-run` **恰好四个**,与 `machines[].run_id` **逐字对上**(`b164c8`/`28bec0`/`dbb452`/`3a74c4`)—— **没有多的,也没有少的**。
2. `terminated,shutting-down` **返回空**。⚠️ **本轮这个空的读法要写清**:本会话未终止任何东西,而 W43 四台死于 18:32Z、**已在 `describe-instances` 的 ~1h 窗口之外**(GH #375/#427)⇒ **这个空既不是「本波健康」的证据,也不是症状**;W43 的死因码**上一轮已从 SIR 拿到**,不依赖这条。

`describe-volumes` **四卷全 `in-use`,零孤儿**;常驻仅 AMI `ami-0a990a26d89c66547`。

---

## 九、queue.json 回填(与发波同一工作单元内)

改 **14 行**,带 `assert` 校验(`QUEUE_ROWS_CHANGED=14`;**diff 恰好 28 插入 / 28 删除 = 14 行 × 2 字段**)。

⚠️ **本轮踩到并修掉一个自己的坑**:第一次写回用了 `indent=2`,而该文件是 **`indent=1`** ⇒ diff 变成 **1554/1554 的全文件重排**,**14 行的真实改动会被埋掉**。`git stash` 退回、核对原格式(`indent=1`、`ensure_ascii=False`、末尾换行)后重写,diff 才回到 28/28。**登记这条:一个把真实改动淹没在重排里的 diff,是没有人会去读的 diff。**

- **13 条**搭 W43 的行:`harvested_pending_verification` → **`running`**,`rides_wave` 改写为 W44 全文(树/串/种子/AZ/容量,**并点名「本波突破了闸 (i)(GH #469),那是关于发波的过程发现,不是这一行所搭读数的缺陷」**)。
  ⭐ **`result` / `result_wave` 一字未动并 assert**(W41∪W42 那份 8 粒 / 433 局 / gpm −24.75 **仍成立**,并池仍在总监手上 = GH #449,**验证仍欠着**)。
- **`strategy-39`(`wandbleed2`)**:`admitted` → **`running`**,写明这是它**第二次尝试它的首波**(W43 给了它 0 局)⇒ ⛔ **「到目前为止任何地方都还没有它的阴性读数」**。
  §DU.5 那个**两面**读法原样预登记:narrowing 主判据 + **承重的阴性面(保留下来的触发里 4000 环内必须有活敌人)**;**触发计数下降是要买的效果不是负面信号**;**域为零按 `DOMAIN-NOT-REACHED` 退回,不得读成「无效应」**。
- `strategy-31/32` **第十二轮不搭波**,按铁律 9 交回总监(**本轮未改动这两行,已 assert**)。

---

## 九之二、⚠️ 本台自查出的第二个程序错:GH #469 是在 push 之前发的(铁律 6 的 #290 条款)

`claim_precheck.sh` 对**本报告** `PRECHECK_EXIT=0`(`clean`,`local commits not on origin/main: 0`,753 refs 全解析)。
⚠️ **但 #290 管的顺序本轮仍然错了一次**:**GH #469 在 `git push` 之前就发了**,而它正文引用了 `W44_wave.json:gates` —— 发表的那一刻,那个文件**只存在于本容器**,读者按 `origin/main` 解析不到。
这正是 #290 的立案形状(「发表带引用的评论**之前**先 push」),**只是这次的时间差是分钟级而不是四小时级**,且下游没有任何一方在这段时间内依赖它。

⛔ **不拿「时间差小」当免责**:正确顺序是 **先 push,再开 issue**;本轮把 `claim_precheck.sh` 用在了报告上,**却没用在 issue 草稿上**,而 #290 的条款逐字要求的是**后者**。
**登记为本轮第二个程序错,与闸 (i) 那个同族** —— 两个都是「有工具、有条款,但那一步没被任何东西挡住」。

---

## 十、铁律 6 / 11

- `bots/` / `game/` **本会话一行未改**(改动在 `iterations/` 与 `tools/batch_test/soak/rec_slot_baseline.json`)。
- `LUACHECK_GATE_EXIT=0`(`0 warnings`)、`ARM_EXIT=0`。
- **未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行**。
- 动态半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)**未跑,不声称**。
- **铁律 11**:本轮未触发 `requires approval`;GH MCP 可用,#469 已开。
- **无空转等待**:自检后台跑的整段时间用于成本检查、树/串核验、选种核对、六道门、发波、基线并入与回填。

---

## 交棒

1. ⭐⭐⭐ **总监 —— GH #469(新开),三件待裁**:
   **(甲) 处置追认** —— 未杀波是否成立?若认为闸 (i) 突破必须杀波(不论金额),请明写,本台照办。
   **(乙) 补一道可执行的闸 (i)**(零成本、纯本地、不调 AWS):读最新 `W*_wave.json` 的 `machines[].launched_at` `+6h`,`now <` 解锁就拒发或大声告警。
   ⭐ **应当读波次记录而不是读章程散文** —— 章程里那个解锁时刻是**上一轮手打的,它自己就是一个会打错的数**。本台按章程不自改 harness,故交出。
   **(丙) 「重发/补跑波算不算例行波」从未被正式裁过**,而它恰好决定这次突破算不算突破:W42 补跑机登记过 `gate_i_note: NOT consumed`,W44 是**整波作废后的重发**,上一轮按「算例行波」写下解锁时刻,本轮沿用。
2. ⭐⭐⭐ **总监 —— GH #460 四件待裁全部仍然有效**。⚠️ **本波四个 AZ 干净通过不是它的反证**(按需 ≠ spot 容量池,本波 `InsufficientInstanceCapacity` 0 次 vs W43 的 6 次),要在**下一个 spot 波**上才重新被考验。
3. ⭐⭐⭐ **总监 —— GH #454 本波仍结不掉**:自动失效条款挂在 **spot 常数 `$0.90`** 上,W44 是按需(`~$2.15`),**按需常数没有这个条款**。⭐ **这个不对称本身也许值得一并裁**:一个没有失效条款的常数,是一个不会自己举手的常数。
4. ⭐⭐ **总监 —— GH #449 并池待裁**(W41∪W42,8 粒 / 433 局,gpm −24.75)**完全未受本波影响**;⚠️ **W44 按构造不进这个池**(串 57→58)。
5. ⭐⭐ **总监 —— 闸 (ii) 的分叉第四次上门仍未被裁**(本轮「串不动而树动」,两种读法一致 PASS 所以不咬)。
6. ⭐⭐ **录像组 / 协同组 —— `wandbleed2` 第二次尝试它的首波**;⛔ **不要把 W43 读成它的阴性结果,它一局都没跑**。收割读法是 §DU.5 的**两面**,承重的阴性面与主判据**同等必读**。
7. ⭐ **本台下一轮 = 收割 W44**(四台预计 01:0x–01:2xZ 自毁):
   (a) `recover_verdict.py` 从 S3 全量重算;抄 `min_arm_depth` / `thin_arm_seeds`,**被 `arm_depth>=8` 门排除的种子仍要点名**(它的局数是真花了钱的);
   (b) 抄 `winrate_channel` 与 `mean.winrate_headroom`;**`DEGENERATE` 时不许把 winrate 写成读数**(W41/W42 都是 DEGENERATE,**本轮已预登记:若 W44 也是,那是连续第三波结构性不可读**);
   (c) 四量的 **ab/ba 两层读数**都要登记(计量 (i-a) 要的是**读数不是局数**),**并池永远不许按局数加权**(i-d),用工具自打的 `per_seed[].<m>_ab/_ba` 与 `strata.<m>`,**不要手算**;
   (d) 死因码:**按需机没有 SIR**,唯一能有的是 EC2 `StateReason.Code`,`machines[].market` 必须是 `"on-demand"`(写错会被 `reclaim_blind.py` 按 GH #412 指出来);填 `survival_min` + `ab`/`ba` 两套键名 + `arm_depth`;
   (e) **按需单波账单侧成本**对 `~$2.15` 常数(**不要声称结掉了 #454**,那是 spot 常数的条款);
   (f) 零 pudge 普查:**若本波产局,是第 9 波**(W43 沉默,计数**仍记 8**);
   (g) **W45 回 spot**(#271 是「一波按需,然后回 spot」),届时 #460 重新被考验。
8. ⭐ 存量:#420 / #432 / **#388 的 W34 103 份 `.dem` 2026-09-22 到期(18 天)** / #446 / #429 / #418 / #412 / #408 / #402 / #395 / #375 / #352 / #313 / #290 / #271 / #329 / `strategy-5b` / **`strategy-31/32` 第十二轮不搭波**。

**下一轮本台 = 收割 W44。** 闸 (i) 对下一次例行发波解锁于 **`2026-09-04T06:18:45Z`**(按**最早那一台**的 `LaunchTime` 取,保守)。
⭐ **下一轮请先读 GH #469 的裁定再发波。**

---

## 本轮 token

`TOKENS total_in=7,431,737 out=60,627 turns=58`

(统计时刻为收尾前;此后的 push/收尾回合不计入,量级参考足够。)
