# 批测台 2026-08-25T15:11Z — 收割 W9 + 发波 W10(**界后第一波**)

## 0. 一句话

W9(29-id 全集,界前)全量重算完成:**gpm −29.25 / winrate 0.443,comps 0/4**;
并发出 **W10 = cap25 界线之后的第一波**(35-id 全集,ref `252a0393`),
`director-2` 的 **(丙) 波次预算闸零成本验收通过**,`(乙)` 成本口径**独立复核通过并首次拿到第二条证据链**。

---

## 1. 收割 W9(29-id 全集,ref `57f3b68`,cap=10 ⇒ **界前**)

标准路径 `recover_verdict.py` 全量重算 4 run 池化:

| 量 | 池化 | comps |
|---|---|---|
| gpm | **−29.25** | 0/4 |
| xpm | −23.96 | 0/4 |
| deaths | +0.20 | 1/4 |
| last_hits | −1.25 | 0/4 |
| **winrate** | **0.443**(中性 0.5) | 1/4 |

`scored_games` **274** / 总局 **298** / `unfinished` **0**,`suggested` `hold_or_reject`。
逐种子 winrate:**888 0.400 / 895 0.506 / 896 0.481 / 906 0.385**。

### 1.1 ⭐ 真发现①:上一轮那个「形状」判断**本轮被证伪,而且是它自己预登记的方式**

03:12Z 报告在 W8 上写下的可陈述事实是:W7 的逐种子 winrate **骑在中性两侧**,
W8 的 **0.346–0.467 整体在中性以下** ⇒ 「测试版作为合成体,五次有记录的全集波从未读高于中性」。

**W9 的 895 读 0.506 > 0.5。** ⇒ 那句「整体在中性以下」**是一波的形态,不是趋势**;
W9 回到了 W7 的「骑在两侧」形状。历史全集波池化序列:
**0.243 / 0.394 / 0.500 / 0.491(W7) / 0.432(W8) / 0.443(W9)**。

**仍然成立、且这才是该带走的那句**:六次有记录的全集波,**池化 winrate 一次都没读到过 0.5 以上**
(最高 0.500,W3)。这是**池化**层面的陈述,与「逐种子是否骑两侧」是两回事 —— 上一轮把这两层
写在同一段里,本轮把它们拆开。

**边界照旧三条**:(甲) 这是 **29-id 全集**的读数,**不是任何单 id 的读数**;
(乙) **不许用 W8 差 W9 归因** —— 00:19Z 的预登记禁止该跨波比较用于 gpm/winrate,本轮照办;
(丙) `winner_by` = `economy_10min_cap` **298/298** ⇒ winrate 与 gpm **不是两个证人**。

### 1.2 计量三条(GH #148)照办 —— ab/ba 两层分报

| 层 | 计分局 | gpm | cand winrate | **Radiant winrate** |
|---|---|---|---|---|
| ab(cand=radiant) | 162 | **+44.19** | 0.673 | 0.673 |
| ba(cand=dire) | 112 | **−101.63** | 0.223 | 0.777 |

两层反号 ⇒ 先按侧偏分解:**侧偏 (ab−ba)/2 = +72.91 gpm**,**效应 (ab+ba)/2 = −28.72**
(对池化 −29.25 的差来自逐种子均值 vs 逐局均值,非矛盾)。
换成不受侧偏污染的读法:**Radiant 胜率 ab 层 0.673 vs ba 层 0.777,差 10.4pp
= armed 腿在两个物理层里各自吃的亏,两层同号**(W8 同法读 13.2pp,同号同量级)。

**逐种子分层里有一格值得点名**:**seed 888 的 ab 层 gpm −55.13、Radiant 胜率 0.366** ——
armed 在 radiant **且仍然输**,是四个种子八格里**唯一效应压过侧偏**的一格。

### 1.3 ⭐ 真发现②:自产 verdict 的 ba 腿短 —— 上一轮的「4/4 通病」修正为 **3/4**

| seed | ab 自产/重算 | ba 自产/重算 | gpm 自产/重算 |
|---|---|---|---|
| 888 | 41 / 41 | **29 / 30** | −40.80 / −41.69 |
| 895 | 39 / 39 | **28 / 30** | −27.92 / −27.74 |
| 896 | 42 / 42 | **28 / 30** | **−14.60 / −10.78** |
| 906 | 40 / 40 | **22 / 22** ✅ | −36.80 / −36.80 |

**ab 腿四个种子逐位相同**(与 W8 一致);**ba 腿 3/4 短 1–2 局,906 逐位吻合**。
⇒ 上一轮「不是某台特异,是收波顺序的通病」**方向对但过强**:906 是反例,
机制仍是「最后一批 dire 局上传落地前就算完了」,但**它并非每台必然发生**。
代价可量:**s896 gpm −14.60(自产)vs −10.78(重算),差 3.82**,池化差 0.78,
而本波效应 −29.25 ⇒ **`recover_verdict.py` 作为标准收割路径第六次被坐实**,
自产 verdict 只当免费早期预览。

### 1.4 预登记的三项验收(判据事先登记,未事后改)

- **`--rec-slots 12` 第八次验收 `exit 0`**:298 局,box **1.023**,`0 recording slot(s) beyond tolerance`。
  slot 1 净值 **−3.8%**,序列 −3.3 → −3.2 → −4.0 → −2.4 → −1.5 → **−3.8** ⇒
  **「slot 1 单调走深」保持撤回**(它涨回去了)。仍成立:**slot 1 是唯一赤字录制槽**
  (slot 2–12 净值 **+3.1%~+5.2%** 全正,结构性理由 `soak_loop.sh:170`)。
  判据按 08-23T15:xxZ 裁定**单边**(只有赤字超 5% 报警,正值不报警)。
- **GH #152 归属:231 份 sidecar,`method` logname **231/231**,`by_logname==by_hostname` **231/231**,
  `hostname_hits` 全 1 ⇒ **本波零分歧**。累计 **4/1404**(上一轮 4/1173)。
  期望值(「slot 2–12 恒 100%;slot 1 在 `--rec-slots ≥ 10` 下以池共存概率分歧」)是**概率性**陈述,
  一波零分歧**不构成反例**,不修正。
- **GH #147 `.dem` 丢失:0/231**。逐 run `.dem` 60/59/60/52 = **231**,claim sidecar 逐 run 逐位相同 = **231**,
  而录制槽(1–12)承载局数亦为 **231** ⇒ **三个数三方吻合,本波零丢失**。累计 **2/1168**,保持降级。

### 1.5 ⚠️ 一条自己踩了又自己抓回来的坑(留档,免得下轮重踩)

第一次跑 `rec_slot_cost.py` 报 `CANNOT CERTIFY: no .demclaim.json anywhere`,
同时 `s3 ls dem21/.../` 数 claim 得 **0** ⇒ **看起来像「sidecar 通道整条断了」的重大回归**。
**实际是本台自己的下载过滤器**:`--include "*.analysis.json"` 把 sidecar 排除了,
而 **sidecar 存在 `soak/<run>/` 不在 `dem21/`**。补下载后 231/231 全在。
**判据教训**:`rec_slot_cost.py` 的 `CANNOT CERTIFY` 是**拒答**(工具头列的五条 REFUSALS 之一),
**不是「农场没产出」** —— 与 §AU.6「未查 ≠ 通过」同族。**没有据此开 issue。**

---

## 2. 成本 —— 本轮把「MTD 滞后」从推断变成了**直接观测**

**`budgets` MTD $37.828,快照时刻 `2026-08-25T04:37:21Z`** —— 与上一轮(06:17Z)**逐位相同**,
且 CE 复核 **$37.8282782356** 亦逐位相同。快照已 **10.5 小时未刷新**。

### 2.1 ⭐ 真发现③:**08-25 一整天在 CE 里是空的** ⇒ W8/W9 未计入是**观测**不是推断

按纪律(乙)(禁用「MTD 增量对得上波费 ⇒ 该波已计入」),本轮花 **$0.01** 拉逐日逐服务 CE:

| 日 | EC2-Compute | EC2-Other | S3 | VPC | 合计可见 |
|---|---|---|---|---|---|
| 08-23 | 5.9103 | 0.2838 | 0.0657 | 0.0436 | ~6.30 |
| 08-24 | 4.4525 | 0.0771 | 0.0637 | 0.0334 | ~4.63 |
| **08-25** | **—** | **—** | **—** | **—** | **零行,一条都没有** |

⇒ **W8(00:16Z)与 W9(06:16Z)确定未计入 MTD**,这是**直接观测**(该日无任何行),
不再需要上一轮那种被禁的增量拟合。**上一轮「W7 已落地」那句用的正是被禁手法**,
本轮不沿用它,改用下面这条独立通道。

### 2.2 ⭐ 真发现④:$1.480/波 的界前基准**首次有了第二条独立证据链**

按纪律(乙)推荐的**与 EC2 行独立的 VPC/公网 IPv4 通道**反解 08-24 机时:

```
$0.0334 / $0.005 per-h = 6.68 台·小时
6.68 / 12 台(3 波 × 4 台) = 0.557 h/台      对账单侧钉死的 0.550 差 1.3%
回代:12 × 0.557 × $0.673 = $4.4956          对 CE 实测 $4.4525 差 1.0%
```

⇒ **0.550 h/台 与 $1.480/波 此前只有 EC2 行一条证据,现在有两条互不依赖的通道互证。**

### 2.3 围栏算术(纪律(甲):不用裸 MTD)

```
MTD $37.828  (CE 08-25 = $0.00 ⇒ W8/W9 确定不在内)
  + W8 $1.48 + W9 $1.48          = $40.79   已花或已承诺
  + W10 $3.05(界后新单价,§BE)   = $43.84  ≤ $45 围栏 ✅   余 $1.16
```

**三线全未触及**($45 自设围栏 / $50 owner 批准档 / $90 刹车线);forecast 74.693 / limit 100.0。
本轮 CE 支出 **$0.02**(check_costs 自动复核 ×2 各 $0.01;另加逐日拆解 1 次 —— 见下)。

> **⚠️ 本轮多花了 $0.01,如实记账**:`check_costs.sh` 在读数 ≥$35 时**自动**花 $0.01 用 CE 复核,
> 而本轮因会话中途挂起、开工与发波前**跑了两次** `check_costs.sh` ⇒ 自动复核触发两次。
> 加上纪律(乙)要求的逐日拆解一次,**本轮 CE 合计 $0.03**。
> **给下一轮**:同一轮内第二次 `check_costs.sh` 应当用 `--leak-only`(零成本),
> 收尾那次要的本来就是「0 台在跑」不是 MTD —— 章程步骤 6 已经这么写了,本轮开头那次是重复。

---

## 3. 发波 W10 —— **界后第一波**

**启动决策:发波,三条闸全过。**

- **(i) 6h 节流**:距 W9 发波 `06:16:33Z` 门槛 `12:16:33Z`,实际发波 **15:10:37Z** ⇒ 过闸 **2h54min** ✅
- **(ii) 有新东西可测**:`bots/` 自 W9 钉树 `57f3b68` 起有漂移(`252a0393`,GH #179),
  且当前树全集累计种子 **0 < 8** ✅
- **(iii) 成本**:**$43.84 ≤ $45** ✅(算式见 §2.3)

### 3.1 ⚠️ 浅克隆陷阱本轮**真的触发了**,靠 exit code 抓住

核漂移第一次跑 `git log 57f3b68..252a0393 -- bots/ game/` ⇒ **exit 128、stdout 0 字节**。
**空输出会被读成「无漂移」**(章程步骤 5 那条,往危险方向失效)。
按章程正解 `git fetch --depth 1 origin <全40位SHA>` **把两个 SHA 都取回**后重跑 ⇒ exit 0,
**漂移 1 个 commit**。**认 exit code 不认空输出,本轮兑现了一次。**

### 3.2 ⚠️ 一条差点写进报告的假发现(自查抓回)

rebase 冲突态下 grep `soak_loop.sh` 得到**两处** `SOAK_CAP_MIN` 默认值(`:-25` 与 `:-10`),
一度读作「cap25 被后一行影子覆盖 ⇒ owner 的 P3 根本没生效」这一重大缺陷。
**复核后为假**:那是 `git rebase` 进行中工作树的混合状态。在真 tip `252a0393` 上
`SOAK_CAP_MIN=${SOAK_CAP_MIN:-25}` **只有一处**;W9 钉树 `57f3b68` 上是 **`:-10`**。
⇒ **cap25 干净落地,W9 确为界前,W10 确为界后**,与 §BE 判断一致。**没有据此开 issue 或通知。**

### 3.3 发波参数

- **armed = `test_set.md` 第 2 行 35 id 逐字**(29 + §BF 六条新入集:
  `pulldrag`/`tpgap`/`campsel`/`tbearly`/`tpdeathbuy`/`zusstatic`),裸串 **311 字节**
- **接线门 35/35 WIRED,`exit 0` @ `252a0393`**(本台独立复跑,非照抄 §BF;
  六个新 id 全 `direct` 各 1 站点。注:`tbearly` 实测 `mode_farm_generic:509`,
  §BF 记的是 493 —— **行号漂移,站点存在,不影响判定**)
- **载体门 `exit 0`**,与 W8/W9 **逐字复现**:`crystal_maiden`/`zuus` **FULL 4/4**;
  `lion`/`obsidian_destroyer` **PARTIAL 仅 s896**;`skeleton_king` **PARTIAL s895/s906**
  (用**裸英雄名**形式 —— `:N` 是**位置**不是局数,W9 报告已留档)
- 钉树 **`252a03935c272e1766249cfa1de366dd46de548e`**(发波时刻 `ls-remote` 真 tip,已核对相等)
- 4 台 × 1 种子(**888/895/896/906**,与 W7/W8/W9 同种子)、`--on-demand`(#158 未裁前保守默认)、
  `--slots 16`、`--rec-slots 12`(只动 id 集合、不动采集配置)、**`--games 12`**、**`--hours 2`** + terminate
- **`--games` 由 22 降到 12**:章程「不够就缩 `--games` 或缩种子,**不要缩 cap**」。
  界后单价 2.06x,22 局/腿会把预估推到 ~$4.28/波 ⇒ **越 $45 围栏**;12 局/腿 = 闸内 68 min = $3.05 ⇒ 过闸
- run_id 尾 token `1bea70`/`d64da6`/`2e17fc`/`7f947b`,发波 **15:10:37/42/46/51Z**
- **唯一性双保险(#98)**:间隔 **5/4/5 秒**;`soak-run` 标签 **4 值两两不同**;
  `describe-instances` 只回 **4 台**、`InstanceLifecycle` 全 `None`(= 按需,与 `--on-demand` 一致)

### 3.4 ⭐ `director-2` 的 (丙) 验收 —— **本轮通过,零成本**(全部 `--dry-run`)

| 用例 | 期望 | 实测 |
|---|---|---|
| 总监点名的 852 形状:4 种子 12 局 @ 默认 3h | 拒发 | **REFUSED**,`~236 min needed / watchdog 180`,给出 `--hours 4`,**exit 1** ✅ |
| 本台 1 种子拓扑挤到 `--hours 1`(需 68 > 60) | 拒发 | **REFUSED**,给出 `--hours 2`,**exit 1** ✅ |
| `--allow-short-watchdog` 覆写 | 放行 | **exit 0** ✅ |

**(乙) 成本口径亦独立复核通过**:闸内模型 `g_need = 68 min ⇒ 1.13 h/台` 与总监外推**逐位一致**;
另有 §2.2 的 VPC 通道给出第二条独立证据。
**(1)(2)(3)(4) 四个数要等 W10 收割**(下一轮)⇒ `queue.json:director-2` 标 **running** 而非 done。

### 3.5 ⚠️ 交给总监的一条**先行警告**:`natural_end` 字段在 W9 语料里**根本不存在**

`director-2` 写「直接数 `winner_by == "engine_natural"`……`analyze_log` 新出 `natural_end` 布尔字段,
`recover_verdict.py` 的 `winner_by` 分布里现在会多这个桶,不用自己推」。
**W9 实测:298 局里带 `natural_end` 键的有 0 局**,`winner_by` 分布**只有** `economy_10min_cap` **298/298**。
⇒ 若 W10 语料同样不带该键,**验收项 (1) 按你给的取数路径取不到**。
W10 跑的是新树,**有可能**带上;**但请预先准备第二取数口径**,否则下一轮会卡在这一格。

### 3.6 W9 的**界前基线四件套**(为界后对照预先取好,免得下一轮无对照可比)

按 §BE(甲)「界后请在界后重取基线」——**这四个数是界前的那一半**,现在取好,下一轮直接对照:

| 量 | W9(cap=10,界前) |
|---|---|
| **自然结束占比** | `winner_by` **100% `economy_10min_cap`**;`natural_end` 键**不存在** |
| **均局长** | 均值 **10.99 min**,中位 10.90,范围 9.8–13.9;**>10min 占 98.3%**、>11 占 38.3%、>12 占 **4.7%**(铁律 4(ii):给占比不只给中位数) |
| **$/有效局** | $1.48 / 274 计分局 = **$0.0054/局** |
| **`avg_gpm` 分布** | 均值 **838.0**,中位 813,范围 614–1496;**< 510 线占 0.0%**、**< 300 线占 0.0%** |

**顺带一条对界后有用的机制观察**:W9 **298/298 局都有 fort 倒下**,而 `winner_by` **仍 100% 是
`economy_10min_cap`** ⇒ 「fort 倒了」在界前是**裁判到点终局的手法**,不是「机器人真赢了」。
这正是 `natural_end` 要区分的东西,也是为什么界后那个数**只有在新键存在时才有意义**。

---

## 4. 局数(章程步骤 7)

- **(a) 上一波(W9)最终有效局**:计分 **274** + 暖场 **24** = **298**,`unfinished` **0**。
  逐种子 ab/ba:888 **41/30**、895 **39/30**、896 **42/30**、906 **40/22**
  (**ab/ba 不对称是既有形态**;906 的 ba 腿 22 最短,亦是自产 verdict 唯一逐位吻合的那台)。
- **(b) 本轮在跑波次(W10)实时进度**:发波 15:10:37–51Z,**报告写作时 S3 `soak/<run_id>/` 尚无 analysis.json**
  (实例仍在开机+steam 刷新阶段,约 12 分钟)。**预期 ~120 ± 30 有效局**
  (界后局长 ~2.5x、`--games` 22→12 ⇒ 局数显著少于 W9 的 274,**这是预期不是异常**),
  **预计自毁 ≈ 16:20Z**。暖场局不算有效局。

## 5. 泄漏

开工、收尾各一次。**收尾四层区域全扫**:

| 区 | 实例 | 可用卷 | EIP | spot 请求 |
|---|---|---|---|---|
| us-west-2 | **4**(= 本波 W10,lifecycle 全 `None`) | 0 | 0 | 0 |
| us-east-1 / us-east-2 / eu-west-1 | 0 | 0 | 0 | 0 |

快照 **1**(AMI `ami-0a990a26d89c66547` 常设)⇒ **无泄漏,无第五台**。
W9 四台已于 06:48Z 前后自毁完毕(开工时区域实例 **0** 可证)。

## 6. 固定栏位

- `soak/` **196**(192→196,+4 = W9);`dem21/` **48**(44→48,+4 = W9);`unattributed/` **0**
- `validation/` 最新 **2026-08-25T06:48:35Z**(W9 四份 verdict 齐全)
- **⭐ GH #167 状态变化:verdict basename 已改短键**。W9 四份实测名形如
  `l1trade+29ids-6e9f10e9bceb_20260825_061809_-140.verdict.json`(**头 id + 计数 + 短哈希**)⇒
  **`[Errno 36]` 不再发生**,`s3 cp` 直接落盘成功,上一轮的 `s3api get-object` 绕法**本轮未被需要**。
  裸 cand 串仍 **311 字节 > 255**(35 id;29 id 时 259),但它**已不再出现在本地文件名里**。
  ⇒ **#167 的真修法方向(key 换短哈希、cand 全文进 JSON)看起来已经落地**;
  本台**只报观测不宣布关闭**(未核实是哪个 commit、也未核实 `soak/` 侧全部路径)。
- 远端 main tip **`252a0393`**
- **`stable-v1`/`stable-v2` tag 在远端仍不存在**(`git ls-remote --tags` 零命中,**第四轮提醒总监**)

## 7. 开工自检 / 验证

- **自检 worst exit 3**:UNLANDED 2 commit(`fc79986`/`08ed7c2`,director `tpreach`)——
  **接线门已独立证明其内容在树上**(`tpreach` direct @ `jmz_func.lua:5878`),
  即工具 LIMIT 4「改头换面落地」那一格 ⇒ **不重复开 issue、不抢救**;
  CADENCE 14 finding(08-24 白天五条流停摆残影 + 发波/收割轮之间的必然空轮);
  trunk python **25 passed / 0 failed**;**trunk Lua = SKIP 不是 PASS**(容器无 `lua5.1`)。
- **验证**:`bots/`/`game/` **逐字未动**(本轮改动仅 `iterations/` 下)⇒ 铁律 6 无适用对象;
  容器无 `luacheck`/`lua5.1`,**不声称跑绿过 Lua 全量**(GH #171 那种失明,如实记下)。
- `queue.json` 按原 `indent=2` 回写,`git diff --stat` **4 增 3 删**,无重排噪声。

## 8. 铁律 9 的交棒

1. **⭐ 总监 —— `director-2` 的 (丙) 已验收通过(§3.4),(1)(2)(3)(4) 随 W10 下一轮到货。**
   **但请先看 §3.5**:你给的取数路径依赖 `natural_end` 键,**该键在 W9 语料里 0/298 存在**;
   若 W10 也没有,验收项 (1) 取不到 —— **请预备第二口径**。
2. **⭐ 总监 —— GH #158(按需 vs spot)第四轮催,且本轮起后果具体化**:
   界后单价 2.06x ⇒ 本台已被迫把 `--games` 22 砍到 12 来过围栏(§3.3)。
   转 spot(~$0.54/波)可让局数**加回来**;不转则界后每一波都要在「局数」和「围栏」之间二选一。
   **裁 (B)/(C) 无需 harness 改动。**
3. **⭐ 录像组 —— W6 解锁棒(第七轮催)**:`strategy-5b` 的事件轴量级,零 AWS,**唯一还挡着 W6 的东西**。
4. **⭐ 录像组 —— `tpreach` 的 (a)**(§BC.3 写死由录像组独立核验,不是总监自己看):
   W9 语料已就位 `dem21/spot_20260825_0616*/`(**231 个 `.dem`**),`tp_channel_death.py` 只差按 mode 分层。
5. **录像组 —— W8 的 `strategy-12`(`towerfear`)五条判据仍没人量**(第三轮催),
   **反向判据「死亡数不许上升」不许省**(波级 deaths +0.20 是 10 人全局均值,替代不了它)。
6. **总监 —— `hero-6` / `hero-5` 的实质 resolve**(§BF.4 自己指名「留给下一轮」的两条,本轮即下一轮)。
7. **总监 —— 载体门 PARTIAL 的惰性种子 = 波内零腿(第六轮催)**;`stable-v*` 的 tag(第四轮)。
8. **`[harness]` 棒**:
   - **#167:verdict 短键疑似已落地**(§6),`[Errno 36]` 本轮未复现 —— **请确认并考虑关闭**,本台不代宣。
   - **#147:本波 0/231,三方吻合**(dem / claim / 录制槽承载局数),累计 2/1168,维持降级。
   - **#152:本波零分歧**,累计 4/1404,概率性期望值不修正。
   - **自产 verdict ba 腿短:由「4/4 通病」修正为 3/4**(§1.3),906 是反例 ⇒ 追评订正上一轮的过强表述。

## 9. Token 用量

```
TOKENS total_in=7,023,785 out=41,795 turns=56
```
