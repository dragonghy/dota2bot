# batch-desk 2026-09-18T18:05:55Z

**刹车第五十轮持有;零发波、零收割欠、零泄漏。⭐⭐ 本轮实质:把空转速率从「重叠窗口」重估成「不相交独立区块」,
结果是本台自己那条 `$0.28–0.92/天` 带**在高侧也是错的** —— 一个合规的 ≥11.3h 独立区块读 `$1.0157/天`,
于是 `$100`(owner 的限额本身,不是本台 `$90`)的最坏落点从 `09-26T17:47Z` 提前到 **`2026-09-25T23:16Z`**,
**早于本台自设通知判据第 ④ 条的兜底日 `09-27T00:00Z`** ⇒ 判据 ④ 本轮收紧到 `2026-09-25T00:00Z`。**

## 一、开工自检(铁律 10)

⚠️ **第一条命令又踩管道坑(同形第十一例)**,逐字
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.`
+ `This is evidence discipline 3; it has recurred 5x, every time as the first command of the round.`
⛔ **归因是姿势不是工具**,且上一轮交棒 ⑤ 已逐字钉死「一次到位」⇒ **本轮仍未做到**,照登不辩解。
第二跑用 `> /tmp/sc.log 2>&1`(⛔ 无管道、⛔ 无 `timeout`),被 harness 移入后台。

**⛔ 自检真码本轮尚未可得**,并且这句话按上一轮订正后的取法说:
回读输出 **646 行**,停在 `=== trunk health (python test suite) ===`,**收尾三行横幅一个都没打**;
`pgrep -af routine_selfcheck` 现读**进程仍在**(pid 516/1952)⇒ 按订正后的取法,
`pgrep` 活**只能推出「尚未」,永远推不出「跑不完」** ⇒ 本节写「尚未可得」,
**回读放在整轮最后一件事**(见「补记」)。⛔ 不空转等它(铁律 7/11)。

## 二、预算(闸 (iii),当轮现跑,⛔ 不得抄作前提)

`AWS_SETUP_EXIT=0`(容器无 CLI,自装 `aws-cli/1.46.1`;身份逐字
`arn:aws:iam::581436534924:user/dota2bot-agent` + `(restricted batch-runner user — correct)`)。

`python3 tools/batch_test/soak/wave_fence.py --planned 0` ⇒ **`FENCE_EXIT=3`**
(**连续第五十轮是闸拒绝,不是本台的处置**)。逐字:

```
actual (MTD)     : $92.462   <- re-read this run, never cached
pending waves    : $0.000   (launched, may not be in MTD yet)
planned          : $0.000
projected total  : $92.462
fence            : $100.00
brake            : $90.00   (owner's line, not derived here)
operative ceiling: $90.00   = min(fence, brake)
WAVE_FENCE: THROTTLED (exit 3) -- brake, not fence. Stop and report to the owner; do not wait it out.
```

RULING 5/6/7 三行照抄:
`accrual scope    : 17 region(s) read [...]  <- COMPLETE: every region this account has enabled` /
`wave accrual     : records after 2026-09-18T01:51:04Z (= 2026-09-18T13:09:04Z - 11.3h lag, clock from budget snapshot)` /
`accrual check    : CERTIFIED (0 accruing instances account-wide, read this run) -- pending $0.000 is a reading, not a default.`
⭐ 钟没有降级,⛔ 未传任何 `--no-*`;`budget filters : none`;
`crossing registry: 0 record(s) ..., none in force`。
三档 ACTUAL:`$50.00 crossed [ALARM]` / `$80.00 crossed [ALARM]` / `$100.00 NOT yet crossed [OK]`。

**headroom 对 `$90` = `$-2.462`**(上一轮 `$-2.292`)。

⭐ **快照解冻**:MTD 与戳(`$92.292` / `2026-09-18T06:58:29Z`)**两量都动了**
⇒ 这是**一个真段不是零段**。

## 三、⭐⭐ 实质:空转速率重估(不相交独立区块),以及本台那条带在**高侧**也是错的

### (1) 语料与口径

从本台历轮报告里把 `(budget 快照戳, MTD)` **全量**抽出(`wave_fence.py` 的
`= <戳> - 11.3h lag` 行 + `actual (MTD)` 行,⛔ 非抽样、⛔ 非外推),
取 W69 落账之后的全部 14 个点(`09-13T01:19:49Z $88.692` → 本轮 `09-18T13:09:04Z $92.462`)。
⛔ 本节**一个 AWS 调用都没花**(纯本地读仓库里的报告)。

### (2) ⭐ 缺陷:此前的速率点全是**共享端点的重叠窗口**

历轮登记的速率点(`$0.664` / `$0.724` / `$0.882` / `$0.582` / `$0.47` / `$0.2818` …)
都是**以当轮为右端点、向左取不同左端点**得到的。把本轮的同一张表打出来:

```
2026-09-13T01:19:49Z -> 131.821h  => $0.6864/day
2026-09-14T20:27:33Z ->  88.692h  => $0.5653/day
2026-09-16T20:07:54Z ->  41.019h  => $0.4628/day
2026-09-17T19:59:13Z ->  17.164h  => $0.4181/day        (本轮合规新点)
```

**它是单调的,而单调是恒等式不是发现**:嵌套窗口共享右端点 ⇒ 只要近段比远段慢,
所有嵌套均值**必然**单调下降。⇒ **这 12 个读数不是 12 个独立点,是 1 个右端点的 12 个嵌套平均**,
而嵌套平均**系统性地把两侧极值往中间收**。⛔ **被收掉的高侧极值,正是定 owner 越线日的那一个。**

### (3) 改用**不相交**区块(每块 ≥11.3h)⇒ 7 个独立点

左起贪心:

```
09-13T01:19:49Z -> 09-13T20:32:29Z  19.211h  +$0.813  => $1.0157/day
09-13T20:32:29Z -> 09-14T10:54:23Z  14.365h  +$0.353  => $0.5898/day
09-14T10:54:23Z -> 09-15T04:18:45Z  17.406h  +$0.711  => $0.9803/day
09-15T04:18:45Z -> 09-16T01:56:47Z  21.634h  +$0.434  => $0.4815/day
09-16T01:56:47Z -> 09-16T20:07:54Z  18.185h  +$0.668  => $0.8816/day
09-16T20:07:54Z -> 09-17T19:59:13Z  23.855h  +$0.492  => $0.4950/day
09-17T19:59:13Z -> 09-18T13:09:04Z  17.164h  +$0.299  => $0.4181/day
min $0.4181  max $1.0157  mean $0.6946
```

⭐ **分块方式不背锅 —— 右起贪心独立复算**(块界不同、点数同为 7):
`$1.0157 / $0.8710 / $0.5390 / $0.7814 / $0.5203 / $0.5242 / $0.4181`,`max` **逐位相同**。
⇒ **「有独立区块越过 `$0.92`」对两种分块都成立**(左起 2 个越、右起 1 个越)。

⇒ **带按算术扩成 `$0.28–1.02/天`**。⛔ **不收窄、不取代、不解释为「烧得快了/慢了」**;
⭐ **只扩不收**,与 09-18T09:17Z 那轮扩低侧是同一条规矩。

### (4) 越线日重算(⛔ 两个端点都登记,本台不挑一个)

从 `$92.462 @ 2026-09-18T13:09:04Z`,九月剩 **12.452 天**:

| 速率 | 9/30 落点 | `$100` 还有 | `$100` 兜底日 |
|---|---|---|---|
| `$0.28/天`(带下沿) | `$95.95` | 26.92 天 | 十月,九月不越 |
| `$0.92/天`(**旧**上沿) | `$103.92` | 8.19 天 | `2026-09-26T17:47Z` |
| **`$1.0157/天`(新上沿)** | **`$105.11`** | **7.42 天** | **`2026-09-25T23:16Z`** |

### (5) 诚实边界四条(⛔ 一条都不去掉)

① **`$1.0157` 那一块离 W69 最近,必须先问是不是 W69 污染**:W69 结束
`2026-09-12 10:18:26`(S3 最新对象戳),按闸自己的 11.3h 滞后模型,该块覆盖的记录窗口是
`[09-12T14:01Z, 09-13T09:14Z]` ⇒ **整段在 W69 结束之后** ⇒ 按该模型**不污染**。
⚠️ 但 `11.3h` 是**上界常数**不是实测尾长 ⇒ 更长尾的记录仍可能落进去。
⛔ **本台不因此丢弃该块**:丢掉它会把越线日**往后推**,而那是**危险那一侧**(显得更不紧急)。
② 7 个点由 14 个快照配出,**快照节奏由各轮触发时刻决定,不是等间隔采样** ⇒
区块长度 14.4–23.9h 不齐,⛔ 不做任何加权平均、⛔ 不报中位数(铁律 4 (ii))。
③ ⛔ **本节不回答「钱是谁花的」**:`USAGE_TYPE` 在原理上不回答这个
(`owed:s3_attribution_gap_remeasure_under_ruling48` 仍 OWED),本节只重估**多快**。
④ ⛔ 未外推十月、⛔ 未改任何常数、⛔ 未主张 `$100` 一定会/不会被越。

## 四、⭐ 自设通知判据第 ④ 条本轮**收紧**(披露不是建议)

09-16T18:15Z 收窄后的四条,第 ④ 条是 **`2026-09-27T00:00Z` 仍无表态则推**,
它当时的立法理由逐字是「按 §二 两个最长基线,此后 headroom 连一波都不够」——
即**兜底日要排在坏事发生之前**。

⇒ §三 (4) 把最坏越线日提前到 **`2026-09-25T23:16Z`**,**它现在排在 `09-27T00:00Z` 之前** ⇒
按旧判据,最坏情况下 **owner 的 `$100` 限额先被越,兜底通知后响** ——
兜底条款在它唯一要防的那个情形里**失效**。
⇒ **第 ④ 条改为 `2026-09-25T00:00Z`**(排在新最坏落点之前约 23h)。
⛔ 这是**让它更早响**不是更晚 ⇒ 方向是保守那侧;⛔ 其余三条一字未动;⛔ 不改 `$90`、不预选三条路。

## 五、收割:零欠

`validation/` **592 对象**、`soak/` **72,079 键**(与前七轮逐位相同);
最新对象仍是 `validation/lf_rescue+27ids-d177b87026dd_20260912_1018_run.log`
(`2026-09-12 10:18:26`)= **W69**。在跑波次:**无**(闸 `CERTIFIED 0 accruing`)。

## 六、局数

本月无新波次;本轮**未读 `analysis.json`、未读 `verdict.json`**(⛔ **0 局**)。
§三 用的是**仓库里已归档的历轮报告读数**,⛔ 不是本轮新测的局。

## 七、成本(铁律 1 三段式)

**零 EC2 / 零 CE / S3 读取 `74` 个对象(出网未计价)** ——
`s3 ls --recursive` 2 次 = `ceil(592/1000) + ceil(72079/1000)` = `1 + 73` = **74 LIST**,
**不下载任何对象**。本轮新增 EC2 计费 **`$0.00`**;`check_costs.sh` 未买 CE
(GH #801 SKIP,见下);⭐ §三 那份实质产出**一个 S3 对象都没碰**(纯本地聚合)。

GH #801 四行整块照抄(**默认值**跑,⛔ 未用 `COST_CONFIRM_AT`):
`>= $35, but headroom to the $90.00 brake is $-2.462` /
`< $1.10 (cheapest wave) — CE confirmation SKIPPED, NOT passed (GH #801).` /
`Nothing can launch at this MTD, so confirming it buys nothing.` /
`Resumes by itself as soon as headroom >= $1.10.`
⚠️ **SKIP 不是 pass**;欠条 `gh801_confirm_headroom_first_live_read` **仍 OWED**。

## 八、泄漏:五条独立路径全零(⛔ 不合并成一句)

① `check_costs.sh --leak-only` 自带区块**空**(`LEAK_EXIT=0`);
② `describe-instances` 五态(`pending,running,shutting-down,stopping,stopped`)**不加 tag 过滤**
⇒ `INST_EXIT=0` / **`LINES=0`**;
③ `describe-spot-instance-requests State==open||active` ⇒ `SIR_EXIT=0` / **`SIR_LINES=0`**;
④ 闸 (iii) 账户级 **17 区 COMPLETE** + `CERTIFIED (0 accruing instances account-wide)`;
⑤ AMI 仍 `ami-0a990a26d89c66547`(`dota2bot-batch-20260718`)一张(常设成本非泄漏)。

## 九、通知判据逐条现读

按**本轮收紧后**的四条(§四):
① MTD `$92.462` < `$100` ✗;② `alpha = 0` ✗;③ headroom `$-2.462` < `$1.10` ✗;
④ 今天 `2026-09-18` < 新兜底日 `2026-09-25T00:00Z`(距今 **6.5 天**)✗
⇒ **四条一条未中 ⇒ ⛔ 本轮不推手机通知**。

⭐ **同样严格地问一次该不该破例**:§三 是本轮实质产出且**就是预算轴**,不像上一轮的「main 红」是别的轴。
**答:否**,两条理由:① **它不改变 owner 的选项集** —— #779 那三条路(维持 `$90` / 加过滤器 / 一次性解禁)
一条都没因为「最坏日提前 18 小时」而增减,而 owner 已**三十一轮零表态**,
在未答的同一道题上再推一条不改选项的通知 = 噪声(先例:09-16T18:15Z 自捉「恒真式判据」);
② **兜底条款已经替它兜住了** —— §四 把 ④ 提前到 `09-25T00:00Z`,**排在新最坏落点之前**,
⇒ 这件事不会因为本轮沉默而无声消失。**但它够格发 issue 评论**(新论据、同一 issue),见 §十。

## 十、跨组

- **GH #779** 现读 `state=open` / `comments: 19` / `updated_at 2026-09-17T09:43:51Z` 三值与上一轮逐位相同
  ⇒ **owner 第三十一轮零表态**(上一轮已逐条核过 19 条全是本台自己发的,本轮不重核)。
  ⭐ **本轮追评**(判据:§三 是**新论据**不是同一结论的复述 —— 带的上沿被独立区块证伪、
  `$100` 最坏日提前到 `09-25T23:16Z`),按 GH #290 顺序**先 push 后发表**。
- **GH #291** 上一轮已发表本台的 `0/3,173` 存档实测(20 天来第一条评论),⛔ 本轮不追评、不关闭。
- **GH #894**(上一轮开的 `[harness]` trunk 红)⇒ 见「补记」的自检回读。
- **GH #184** / **GH #721** 本轮⛔ 未读(无新事实,读了也只是重复登记冻结值),⛔ 不写成「已核对」。

## 十一、没做的事(⛔ 一条不省)

⛔ 未发波(闸 (iii) `exit 3`);⛔ `bots`/`game`/`tests`/`tools` 一行未改;
⛔ 未重开已关档裁定;⛔ 未改 `state.json`/`test_set.md`/`OWNER_PRIORITIES.md`/`queue.json`;
⛔ 未自行结清 #801 欠条;⛔ **未收窄速率带**(只按算术扩了高侧);
⛔ 未改 `$90` 刹车线、未预选 #779 三条路;⛔ 未逐局回读 `analysis.json`(要 S3 出网);
⛔ 未修 harness;⛔ 闸 (i)/(ii)/(iv) **未跑**(发波轮的闸;(iii) 已 exit 3)——**未跑不记成通过**。

**铁律 9**:`P4.1` upstream 标尺波**连续第五十轮欠**,⛔ 唯一阻因是闸 (iii) `exit 3`(预算)。

## 十二、交棒

1. ⭐⭐⭐ **owner —— 刹车第五十轮持有,GH #779 第三十一轮零表态**;球仍在 owner:
   提高预算线 / 给预算加过滤器 / 接受本月剩余批测全停。**本轮新事实**:`$100` 最坏落点
   `2026-09-25T23:16Z`(不是上一轮的 `09-26T17:47Z`)。⛔ 本台无权自裁。
2. ⭐⭐ **总监 —— 请裁「重叠窗口速率点」这个形状是否该升格成通则**:§三 (2) 的失效机制
   (嵌套窗口共享端点 ⇒ 单调是恒等式、极值被系统性收掉)与铁律 4 §CL (i-c)/(i-e)
   「反号是恒等式不是诊断」**同型**;本台只在自己的预算轴上改了取法,⛔ 未自行立通则。
3. ⭐ **总监 —— GH #291 已收到本台的存档侧实测**(上一轮),修法归 harness 域,本台不代修。
4. **总监 —— 上一轮交棒 ③④(GH #184 零表态、更早各轮未获回应的行)逐字结转**,⛔ 本台不自行退休任何一行。
5. **下一轮本台**:
   (a) ⛔ **开工第一条命令不许再带管道**(同形第十一例,本轮第二跑才对);
   (b) 收尾回读自检:`pgrep` **活 ⇒ 只许写「尚未可得」**,**死 + 无横幅 ⇒ 才可写「本轮不可得」**;
       核对 `FINDINGS` 时**连 `UNCERTIFIABLE` 那一行一起核**(桶间迁移才看得见);
   (c) 速率端点仍 ≥11.3h,且**按不相交区块取,⛔ 不再登记共享右端点的嵌套窗口点**;
   (d) 通知判据按 §四 **收紧后**的四条执行(④ = `2026-09-25T00:00Z`);
   (e) 刹车解除(headroom ≥ `$1.10`)时按规格发 **P4.1 标尺波**,收割必带 `--ledger`,
       并把 GH #801 那四行**恢复后**的样子抄进报告。

## 十三、token 用量(铁律 8)

```
TOKENS total_in=2,311,131 out=29,084 turns=25
```

参考上一轮 `5,750,774 / 37,520 / 45`(那是它补记 2 之后的收尾读数)⇒ 本轮**降约 60%**。
归因一条:**章程按 `tail -180` 取尾、⛔ 未按猜的行号取大段**(上一轮交棒点名的那次浪费),
§三 的全量抽取在 python 里聚合后**只回传汇总表**(30 份报告一份都没贴进上下文)。
⚠️ 这是**统计时刻**的数,其后的收尾回合(push / 发表 / 补记)不计入。

## 补记(两次 push 之后,GH #290 顺序)

### (A) 铁律 6 三条腿(两次 push,第二推 memo 命中)

`luacheck bots game: 0 warnings` / `GATE_EXIT=0  CLEAN (iron rule 6 static half passed)` /
`py gate: 133 ran, 0 findings, 0 uncertifiable, 39.2s` /
`lua gate: scope = 2 path(s) changed vs the pushed refs' base (base=fallback-merge-base)` /
`lua gate: SKIPPED BY SCOPE -- this push touches no bots/game/tests path.`
⚠️ **第三条腿是 SKIPPED BY SCOPE 不是绿**(本轮只改 `iterations/` 两个文件)⇒ ⛔ 不记成「Lua 测试通过」。
⚠️ **py gate 本轮 `133 ran`,上一轮 `134 ran`** —— ⛔ 本台**未查**少的那一个是什么,只登记差异。
`ARM_EXIT=0`(`push gate armed (core.hooksPath=.githooks, already set)`);**⛔ 未用任何 `RULE6_BYPASS`**;
`PULL_EXIT=0`(`Current branch claude/great-sagan-wh5r3b is up to date.` ⇒ 无 rebase、无 `--force-with-lease`);
`PUSH1_EXIT=0`(`* [new branch] claude/great-sagan-wh5r3b`)/ `PUSH2_EXIT=0`(`50b2b99b..4c3811c8  HEAD -> main`)。
⭐ **按文档顺序先分支后 main**,memo 逐字
`RULE6_MEMO=REUSE  a green three-leg reading taken 2026-09-18T18:20:10Z on THIS EXACT tree.`,⛔ 未反序(RULING 69)。
⭐ 落地不靠 push 回显:`git merge-base --is-ancestor HEAD origin/main` ⇒ **真**。

### (B) 发表:GH #779 第三十二条评论

`issues/779#issuecomment-5734351705`,带署名脚注,按 GH #290 **两次 push 都在发表之前完成**。
发表前 `claim_precheck.sh` ⇒ **`PRECHECK_EXIT=0`**,逐字 `local commits not on origin/main: 0`。
⚠️ **但要照实读它的中间行**:`REFUSE  zero citations extracted from 1 source(s)` ⇒
本条评论**没有任何 `tests/` / `bots/` / `state.json:<key>` / `§XX` 引用**,
结论行是 `NO CITATIONS -- nothing to certify, nothing to strand.  OK to publish.`
⇒ ⛔ **这是「没有可查的东西」,不是「查过且干净」** —— 与 09-18T09:17Z 那轮「零 issue ⇒ 未跑」是同一条诚实边界的另一侧。

### (C) 收尾泄漏复查

`check_costs.sh --leak-only` ⇒ **`LEAK_EXIT=0`**,`running/pending instances` 区块**空**,
常设成本仍只有 `ami-0a990a26d89c66547` 一张 ⇒ 与开工两次读数一致。

### (D) ⭐⭐ 自检回读:仍「尚未可得」,而**取法本身本轮又差点被自己骗过去**

回读 **686 行**(正文那次 646 行,**还在长**),停在 `=== trunk health (fast Lua detectors) ===`,
**收尾三行横幅一个都没打** ⇒ ⛔ 无 `selfcheck worst exit`,**不写、不读成通过**。

⚠️⚠️ **本轮的新教训,和上一轮那条是同族的第三例**:第一次判活死用的是
`pgrep -c routine_selfcheck`,它回 **`0`** 并附一行警告
`pattern that searches for process name longer than 15 characters will result in zero matches`
⇒ **「零匹配」在这里的意思是「问法不对」,不是「进程没了」**。
按上一轮订正后的取法,`0` + 无横幅**恰好构成可以写「本轮不可得」的那一组条件** ——
⛔ **而它是假的**:改用 `pgrep -af routine_selfcheck` 现读 **pid 516 / 19249 仍在**。
⇒ **取法再补一条(与上一轮那条并列,不取代)**:判活死**只用 `pgrep -af`**,
⛔ **`pgrep -c <名字>` 在名字 >15 字符时恒零**;并且**先看有没有 warning 行再看数字**。
📌 与铁律 1 §RULING 48、铁律 4 §CL (i-a) **同型**:一个真命题(命令退出 0、输出是 0)
被拿去回答**另一个**问题(进程还在不在),**而两者之差正好是会让本台写下假结论的那一段**。

### (E) ⭐ 但 python 那条腿本轮跑完了,读数在:**`main` 仍是红的,且仍是 GH #894 那一条**

逐字 `149 passed, 1 failed, 3 uncertifiable` / `failed: tests/test_bots_walk_farm_only.py` /
`TRUNK RED -- a python test is failing ON THE WORKING TREE.`
工具自带的自我限制 `Whether main is red too is NOT established by this line` **本轮仍被外部条件满足**:
`git status --porcelain` 为空、`git rev-parse HEAD` 与 `git ls-remote origin main` 同为
`4c3811c84d8a725cda700fe7cb2c85a83a15b3f5` ⇒ **工作树就是 main** ⇒ 可升格成**「main 红」**
(⛔ 这一步是本台补的外部证据,不是工具说的)。
⚠️ 与上一轮(`147 passed, 1 failed, 3 uncertifiable`)相比:**通过数 +2、失败的仍是同一个文件**
⇒ **GH #894 立案约 9 小时后仍未修**。⛔ 本台**不代修**(先例 GH #33)、⛔ **未在 #894 追评**
(无新事实:归因与修法上一轮已写全,再发一条只是稀释)。
另 Lua 检测器腿 `5a0` 现读 `UNCERTIFIABLE`,`138 file(s) [discovered -- the leg never reached its count line] in 120.0s (budget 120s)`
⇒ ⛔ **未跑不记成通过**。

### (F) 通知判据复核(收尾重问一次)

四条**预算**判据仍全未中(MTD `$92.462` < `$100` / `alpha = 0` / headroom `$-2.462` / 今天 < `2026-09-25T00:00Z`);
新轴「main 红」与上一轮同题、同答:**否** —— 路由是 issue(铁律 5)且 **#894 已在**,
下一个开工的组必然读到;⛔ 不推送。

### (G) token 用量(取代 §十三 的数)

`TOKENS total_in=4,034,813 out=41,118 turns=38`。
⚠️ §十三 那个 `2,311,131 / 29,084 / 25` **不是错的是早的**(统计时刻的读数),**两个都留着**,
差值就是 push + 发表 + 本补记这一段的成本。

## 补记 2(自检跑完了;⛔ 订正正文 §一 与补记 (D) 的「尚未可得」)

**(i) 真码到手,而拿它的姿势本身又是一课。** harness 的后台任务通知说
`completed (exit code 0)` —— ⛔ **那不是自检的退出码**:本轮第二跑的命令是
`bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1; echo "EXIT=$?"; tail -50 /tmp/sc.log`,
**复合命令的退出码是最后那个 `tail` 的**。真码在输出文件第 1 行的 `EXIT=` 里:**`EXIT=3`**。
📌 这与本轮开工第一条命令踩的管道坑(`REFUSED: ... the exit code your harness reports is the READER's`)
**是同一条铁律的两面**:一次是管道、一次是命令串,**都让一个「成功」冒充另一个问题的答案**。
⇒ **取法再补一条**:后台任务的 `completed (exit code N)` **只在整条命令就是那一个程序时**才是它的码;
**凡命令串里还有 `echo`/`tail`,必须回读文件里的 `EXIT=` 行**。

**(ii) 真码逐字**(`/tmp/sc.log` 现读 **698 行**,收尾三行横幅**全打了**):

```
legs run              : 15
FINDINGS (exit 3)     : cadence queue-rulings owed-executions lua-coverage trunk-red(python)
UNCERTIFIABLE (exit 2): none
NOT RUN (inside a leg): tests/test_lua_gate.py tests/test_luacheck_gate_soakswitch.py tests/test_selfcheck_lua_leg.py
selfcheck worst exit: 3
```

⭐ 归因是工具自己打的,⛔ 本台未手工归因(GH #267 警告照抄)。

**(iii) ⭐ 按上一轮交棒 ⑤(b) 核对:`FINDINGS` 与 `UNCERTIFIABLE` 两行一起核。**
五条 **`cadence queue-rulings owed-executions lua-coverage trunk-red(python)` 与上一轮逐字相同**,
且 `UNCERTIFIABLE: none` **也与上一轮相同** ⇒ **本轮无桶间迁移**
(上一轮正是靠一起核才看见 `trunk-red(python)` 从 `UNCERTIFIABLE` 升进 `FINDINGS`;
本轮它**停在 FINDINGS 里没动** —— 与补记 (E) 的「#894 未修」是同一件事的两处读数)。
`NOT RUN (inside a leg)` 三个文件亦与上一轮逐字相同。

**(iv) ⛔ 订正**:正文 §一 与补记 (D) 写的「尚未可得」在**写下的那一刻是对的**
(横幅当时确实没打、进程确实还在),**现在被跑完的这一份取代**。
⭐ 值得记的是:**上一轮订正后的取法本轮经受住了考验** —— 若按补记 (D) 那个假的 `pgrep -c` 读数
写成「本轮不可得」,本节就**不会存在**,而 `selfcheck worst exit: 3` 会在本轮**彻底丢失**。
⇒ 「`pgrep` 活只许写尚未 / 回读放在整轮最后」这条规矩,本轮**第一次真的救回了一个读数**。

**(v) 通知判据**:四条预算判据仍全未中;`selfcheck worst exit: 3` 的五条与上一轮逐字相同
⇒ ⛔ **无新事实,不推送**。
