# 批测台 2026-08-31T18:13Z — 不发波轮(第三轮),但闸 (ii) 第一肢**第一次为真,而且第一次不管用**

**一句话**:`bots/` 三轮来第一次真的动了(`mode_roam_generic.lua` +43/−1),
闸 (ii) 第一肢**按字面为真** —— 可是那处改动**在 W32 的两条腿里都被 gate 关着**
(`rotscope` 不在 armed 的 47-id 串里),所以 W32 仍会是 W31 的**逐位重复**。
**本轮因此拿到了 GH #363 三轮以来第一个带价签的判据**:一个 (乙) 会放行、
而它放行的那一波**在算术上无法测量触发它的那处改动**的现场边界。
**不发 W32;零 EC2 支出;唯一付费调用是 `check_costs.sh` 自带的 $0.01 CE 复核。**

---

## 1. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh > sc.log 2>&1; echo EXIT=$?` ⇒ **裸退出码 3**。
(第一次尝试走 `| tail -40`,被总监 §22 的管道守卫**当场拒绝**并自称
`REFUSED: … exit 2, nothing checked … this is NOT a pass` —— **§22 的第二个外部验收点**,
守卫工作正常,本台改重定向重跑拿到真读数。)

```
legs run              : 8
FINDINGS (exit 3)     : cadence queue-rulings trunk-red(python) trunk-red(lua)
UNCERTIFIABLE (exit 2): none
selfcheck worst exit  : 3
```

四条 finding 逐条落位见 §5–§7。

---

## 2. 成本与围栏

`bash tools/agent/rc.sh bash tools/batch_test/aws/check_costs.sh` ⇒ **`RC_EXIT=0`**。

| 项 | 读数 |
|---|---|
| running/pending 实例 | **空** |
| MTD(budgets,免费路) | **$75.092**,budget 快照 `2026-08-31T10:32:38Z`,forecast **$77.28**,limit $100 |
| CE 复核($0.01,读数 ≥ `$COST_CONFIRM_AT`=$35 自动触发) | **$75.0922198225** — **逐位一致,连续第三十八轮** |
| 常驻成本 | 仍只有一个 AMI(`ami-0a990a26d89c66547`) |

**围栏算术(条款 (甲):`MTD + Σ(近 12h 内已发波次 × 该波单价)`)**

- W31 起飞 **06:17Z**,距今 **11h56m** —— **刚好越出实测滞后带 4.3–11.3h 的上沿**,
  即它现在**可能**已经进了 MTD。**本台仍照记**(条款 (甲) 的失效方向是「滞后让 MTD 系统性偏低」,
  重复计一次朝安全侧,漏计一次朝危险侧)。
- W30 起飞更早,同样照记。

⇒ **围栏 = 75.092 + 0.90 (W30) + 0.90 (W31) = `$76.892` ≤ `$80`**;
含预估 W32(spot,`~$0.90`)为 **`$77.792` ≤ `$80`** ⇒ **闸 (iii) PASS**(本轮不是它 binding)。

- **明确未用条款 (乙)**(「MTD 增量对得上波费 ⇒ 该波已计入」)—— 该手法已两次给出错误结论。
- **本轮不跨任何 owner 可见告警档**($50 已跨且已有书面解释;下一档 $80 未跨);
  刹车 $90 / owner 档 $100 均远。
- **单波成本自动失效条款不适用**:本轮无新波;W31 已于上一轮结算 **$0.794–0.859 ≤ $0.90**。
- ⭐ **登记(与上一轮同,时限更近)**:今天是 **08-31 18:13Z**,MTD 是月度量,
  **约 5h47m 后翻月归零**。⇒ `$80` 围栏本月**不会** binding;未来数周真正的约束**是闸 (i)(ii),不是钱**。
  **常数与档位一律不动。**

---

## 3. 收割:零新数据,零重算,零重复计费

- `s3://dota2bot-batch-results-4924/soak/` 最新四个前缀**就是 W31 的四台**
  (`spot_20260831_0617{21,26,31,35}_1_1dd5705f…_{731a21,fde133,b5b4b9,c44eb5}`),**无更新的前缀**。
- `validation/` 最新对象仍停在 **2026-07-23**。
- W31 已于 **12:30Z** 全量收割完毕(`222 loaded / 198 scored / unfinished 0 / 零排除`)。

⇒ **本轮零重算、零重复下载、零重复计费。**

**`reclaim_blind.py`(GH #271,市场类型的输入)当轮在 W31 记录上重跑** —— `RC_EXIT=0`:

```
changeover 40.0 min  (measured bracket 34.80 < x <= 40.63, margin 0.63 min)  min_arm_depth 8.0
  seed 2375  43.2 min  instance-terminated-by-user       ab28/ba14  PAIRED [depth-checked]
  seed 2393  54.9 min  instance-terminated-by-user       ab39/ba16  PAIRED [depth-checked]
  seed 2403  52.8 min  instance-terminated-no-capacity   ab36/ba10  PAIRED [depth-checked]
  seed 2444  55.2 min  instance-terminated-by-user       ab40/ba15  PAIRED [depth-checked]
yield      : 4 paired seed(s) of 4 machine(s)
attribution: 0 machine(s) reclaimed before the flip
VERDICT: not blinded    NEXT WAVE: spot (the default; GH #158 unchanged).
```

⇒ **市场类型建议 `spot` 再次留给下一轮**(本轮未发波)。
顺带登记一次读数事故的**未遂**:第一次裸调 `reclaim_blind.py` 不带 `--wave-json` 拿到 **exit 2**,
`rc.sh` 自己打了那行「2 是本仓的 could-not-run 码,argparse 也用 2 答缺参 —— **不是通过**」,
所以没有被读成「工具说没问题」。

---

## 4. ⭐⭐⭐ 发波决策:不发 W32,binding 的仍是闸 (ii) —— 但这次的形状是新的

### 4.1 三条闸的读数

| 闸 | 读数 | 结论 |
|---|---|---|
| (i) 距上一波 ≥6h | W31 起飞 06:17Z,现 18:13Z ⇒ **11h56m** | **PASS** |
| (iii) 围栏 | `$77.792 ≤ $80`(§2) | **PASS** |
| (ii) 有新东西可测 | 见 §4.2 / §4.3 | **FAIL** |

### 4.2 第一肢:**三轮来第一次按字面为真,而且这次的「真」是可比的真**

```
$ git log --oneline 1dd5705f..HEAD -- bots/ game/        # rc=0
7afb8380 [strategy] gated rotscope: the Pudge block's continuous attack is guarded by a variable it does not read
$ git diff --stat 1dd5705f..HEAD -- bots/ game/          # rc=0
 bots/mode_roam_generic.lua | 44 +++++++++++++++++++++++++++++++++++++++++++-
 1 file changed, 43 insertions(+), 1 deletion(-)
```

**浅克隆假阴陷阱本轮不适用,但理由与前两轮不同,要写清楚**:本容器**是** shallow
(`is-shallow-repository`=true,50 commit),`git fetch --depth 1 origin 1dd5705f` 甚至
**报了 `couldn't find remote ref`**(缩写 SHA 取不回)。**但这不重要** —— 章程钉的陷阱形状是
**「exit 128 + 0 字节输出被读成无漂移」**,而本轮的读数是 **rc=0 且输出非空**:
非空输出本身就证明 `1dd5705f` 在本地解析成功(`git rev-parse` 复核得全 40 位
`1dd5705f43a6bb10f1071464db32035199388141`)。**认退出码不认空输出,本轮两者同向。**

`test_set.md:2`(armed 串)**逐字节不变**:两侧 md5 同为 `24d3096a90c8256482f3cb47b2282f99`,
**47 id / 414 字节**。

### 4.3 ⭐⭐⭐ 而第一肢的「真」**买不到任何东西** —— 这是算术,不是判断

那 43 行加的是一条 **gated** 修复,gate id 是 **`rotscope`**,而 **`rotscope` 不在 armed 的 47-id 串里**
(§4.2 已证该串逐字节不变;`grep rotscope` 于该行 ⇒ **NOT IN LINE2**)。

改动的实际形状(全 diff 已逐行读过,`bots/mode_roam_generic.lua` 是**唯一**被碰的产品文件):

```lua
if botName == 'npc_dota_hero_pudge' then
    local rotscope = J.IsModeTurbo() and J.IsSoakCandidate('rotscope')
    local Rot = bot:GetAbilityByName('pudge_rot')
    if Rot:GetToggleState() then
        ...
        if rotscope and J.IsValid(botTarget) then      -- 新增,armed 才走
            bot:ActionQueue_AttackUnit(botTarget, false)
        end
    end
    if not rotscope then                                -- 出厂那条语句,原地未动
        bot:ActionQueue_AttackUnit(botTarget, false)
    end
end
```

**W32 的两条腿逐条代入**:

- **armed 腿** = 47-id 串 armed ⇒ `J.IsSoakCandidate('rotscope')` 为**假**(串里没有它);
- **baseline 腿** = 全 gate 关 ⇒ 同样为**假**。

⇒ **两条腿都走 `if not rotscope then …` 那一支**,发的是**出厂那条命令、同一个变量
(文件级 `botTarget`)、同一个位置**;新增块一次都不进。多出来的只有每 Pudge 帧一次
**无副作用**的谓词求值。

> **⇒ W32 若按现行 47-id 串发,是 W31 的逐位重复。第一肢为真,却一分钱的信息都买不到。**

### 4.4 ⭐⭐⭐ 这正是 GH #363 三轮以来第一个**带价签**的判据

- 前两轮交给总监的是**频率**(298 个 run 前缀 ⇒ 29 棵树 / 28 个边界,**25/28 = 89.3%**
  的边界上 `bots/` 有变更)加一条**包含恒等式**(`count_乙 ≤ count_甲` ⇒
  `{count_乙≥8} ⊆ {count_甲≥8}` ⇒ (乙) 挡不住任何 (甲) 挡不住的东西)。
- **本轮拿到的是一个现场边界,两种读法在它上面给出相反答案,而且哪一个对是可判定的**:
  - 读法 **(甲)**(armed 串家族为并池单位):串逐字节不变 ⇒ 计数仍是 **8** ⇒ `8<8` 假 ⇒ **不发波**。
  - 读法 **(乙)**((树, 串) 对为并池单位):树变了 ⇒ 计数重置回 **4** ⇒ `4<8` 真 ⇒ **发波**。
  - **而 §4.3 已证:那一波在算术上无法测量触发它的那处改动。**
    ⇒ 在这个边界上,(乙) 会花 **$0.90** 买一份 W31 的逐位副本。

**本台明确不夸大**:这**不是** (乙) 在一般情形下错误的证明,它是**一个** (乙) 空放行、
而 (甲) 恰好答对的边界。但它是三轮里第一个**有价签**的点,而且它暴露的东西比 (甲)/(乙) 之争更基本:

> ⭐ **(甲) 与 (乙) 都不看那处 diff 在两条腿里是否可达。**
> 本台顺带交出第三种读法 **(丙)**:「**armed 串变了,或 `bots/` 里发生了在本波某条腿上可达的变更**」——
> 它在本边界上**因为正确的理由**答对(改动不可达 ⇒ 不发),在 (甲)/(乙) 分歧的其余边界上也有定义。
> **本台不裁,只把它摆上桌** —— 三选一比二选一更该由总监一次定完。

### 4.5 代价一字不变地重复(第三轮)

那 8 粒(`pooled_47id` gpm **+20.77** / sd 18.58 / SE 6.57 / 6-8)**横跨一次活的**
Silencer 崩溃守卫改动(`074d3e9c`,按 #346 崩溃守卫不走 gated-fix ⇒ 未 gate ⇒ 活的)。

> **要么** (甲) 成立、并池合法、家族已厚到 8 ⇒ 不发波;
> **要么**并池不合法 ⇒ 那个已交总监**五轮**的 `+20.77` 本身需要重述。
> **两者不能同时要。本台不裁。**

### 4.6 ⭐⭐ 最便宜的解锁路径,以及它自己也堵着:`rotscope` 是一条 ORPHAN_PROPOSAL

把 `rotscope` **arm 进串**,闸 (ii) 第一肢就**因为正确的理由**成立,W32 就买得到真信息,
增量成本 **$0**(搭车,不申请专波)。**但入集是总监的活,本台不自行改串**,而且这条路本身也堵着 ——
开工自检的孤儿提议腿点名:

```
=== orphan admission proposals (test_set.md sections with no queue row) ===
proposal sections scanned: 11
ORPHAN_PROPOSAL (§CG.5: open a queue request row, then rule it): 1
  §CU   id=rotscope       no queue request row at all
```

⇒ 协同组落了 **§CU / GH #368** 的入集提议,**但没开 queue 行**,于是**连正常的裁定通道都没打开**。
交棒见 §9 ①②。

### 4.7 队列侧(闸 (i)(ii) 的豁免通道):无一条可发

53 条 / `pending` **26** 条,**无一条同时持 `APPROVED*` 且待发**。

- `strategy-5b`:`APPROVED_CONDITIONAL` + `wave=W6`,**第四轮点名不自行改判** ——
  裁定条件 ③ 明写「发波前必须给出事件轴量级(每局塔防 TP 响应次数 / 其中核心占几次),
  **拿到之前不排发波时刻**」,该数至今未见 ⇒ 不可发。
- `hero-26`(英雄组 08-31T13:59Z 入):`director` 字段仍**为空** ⇒ 未裁,**按 4a 不发**
  (自检 `OTHER (routing/slot ruling still owed): 1` 点的就是它,**第二轮**)。
- `UNKNOWN STATUS` 4 条(#317):`strategy-17` / `hero-22` / `hero-23` / `strategy-23`,词汇漂移,照旧。

---

## 5. GH #364(`test_rc_wrapper.py` flaky):**本轮拿到迄今最干净的一组对照**

| 跑法 | 读数 |
|---|---|
| 开工自检的 python 腿 | **FAIL `tests/test_rc_wrapper.py`**;`70 passed, 1 failed, 1 uncertifiable` |
| 裸跑 `python3 tests/test_rc_wrapper.py` × 5 | **5/5 `BARE_EXIT=0`** |
| **同一个 runner** 独立跑 `bash tests/run_py_tests.sh`(经 `rc.sh`) | **`71 passed, 0 failed, 1 uncertifiable`** |

⭐ **第三行是本轮的新东西**:上一轮证伪了「管道形状」假说,本轮进一步证伪
**「runner 不同」**假说 —— **同一个 `run_py_tests.sh`,自检里 1 failed、独立跑 0 failed**。
⇒ 差异既不在管道也不在 runner,**flaky 成立**,方向仍是「朝吵闹那一侧失效」。
**#364 的第一条建议(让 `run_py_tests.sh` 留档 FAIL 的逐条输出)第三次被现场证明必要** ——
本轮同样无法知道它为什么红。

---

## 6. GH #358(`test_selfcheck_lua_leg.py`):**第四个实测点,形状仍是 `=2`**

自检腿级横幅打 `UNCERTIFIABLE (exit 2): none`,**但那是腿级**;python 腿**内部**照旧:

```
uncertifiable: tests/test_selfcheck_lua_leg.py
UNCERTIFIABLE 5a0: the leg finishes inside 120s [it did not on THIS container --
  and wall clock cannot separate a grown discovery set from a slow container:
  the same 50 files measured 99.5s here and 133.3s on the batch desk (GH #358)]
… 9 check(s) did not run; this is NOT a pass.
```

**9 条未跑,120s 撞顶** —— 与前三轮同形。**不要把腿级 `none` 读成 #358 好了。**

---

## 7. ⭐⭐ 本轮新出现的 Lua trunk red,**是真发现,不是假阳** —— 已开 GH `[hero]`

```
FAIL: test_corpus_scale.lua :: [detector] no test pins the live corpus size with an equality
  tests/test_corpus_scale.lua:177: 1 assertion(s) compare a literal to the current
    fixture count (107). That is the GH #106 / GH #127 defect …
    tests/test_axe_bkb_supply_staged_frame.lua:548: assert(c.frames == 107, string.format(
```

**新在本轮**:该行由英雄组 **16:51Z** 的 `21b2ec9f`(付 GH #357 第 3 行)落地,
**晚于 15:15Z 那轮自检** ⇒ 上一轮看不到它。语料现为 **107** 个 fixture,断言写死 **107**。

**与上一轮的 #367 不同型,要分开读**:#367 是**假阳**(存在性探针被棘轮读成存在性声称);
**本条是真阳**。作者在同一处写了理由,而那个理由**只要下界**:

> `-- Two-sided ON PURPOSE … a FALL to zero here has to raise a hand rather than
>  read as "no supply". A zero from an empty predicate and a zero from an empty
>  corpus are the same integer …`

**「零和有料是同一个整数」这个担忧,`c.frames >= 107`(或 `> 0`)已经全部买到**;
**上半边(`<= 107`)一分钱不多买**,却**恰好重建** GH #106/#127 那个缺陷:
**落第 108 个 fixture 的那一刻它变红,而它所度量的东西一点没变**。
`tests/corpus_scale.lua` 已备好三个正确件:`ratchet()` 逐 fixture 求和、
`universal()` 作「全都如此」的声称、`corpus()` 拿尺寸本身。
同一处的 `assert(c.axe == 28, …)` 同型,一并列出。

**本台只立案不改测试**(那是英雄组的文件),已开 issue,见 §9 ③。

---

## 8. 局数(owner 2026-08-19 要求的固定节)

- **(a) 上一波 W31 最终**:`222 loaded / 198 scored / unfinished 0 / 零排除`,逐粒
  **2375 ab28/ba14**、**2393 ab39/ba16**、**2403 ab36/ba10**、**2444 ab40/ba15**
  (**ab–ba 不对称已按计量条 (i-a) 逐粒登记**;四粒全 `PAIRED [depth-checked]`,
  `min_arm_depth` 门 8.0 无一粒被 `THIN-ARM` 排除)。
- **(b) 本轮在跑波次**:**无**。S3 与 `analysis.json` 零新增。

---

## 9. 交棒

1. **⭐⭐⭐ 总监 —— GH #363 第三轮点名,本轮带来第一个有价签的判据(§4.4)。**
   一个现场边界:(乙) 放行的那一波在**算术上**无法测量触发它的那处改动。
   连带**同轮**裁 `+20.77` 是否合法(§4.5,两者是同一问题的两面)。
   **并请一并考虑第三种读法 (丙)**(「armed 串变了,或 `bots/` 有在某条腿上**可达**的变更」)——
   它在本边界上因为正确的理由答对。**上一轮本台说过「第三轮仍无裁定就直接盖 (甲) 章或明确指示按 (乙) 发」,
   本轮仍不自行改判**,但请注意这已是**连续第三个不发波轮**,而闸 (i)(iii) 三轮全 PASS。
2. **⭐⭐⭐ 协同组 + 总监 —— `rotscope` 是 ORPHAN_PROPOSAL(§4.6)。**
   协同组:按 §CG.4/§CG.5 补开 `queue.json` 行。总监:裁它入不入集。
   **这是本轮最便宜的解锁路径** —— arm 进串后闸 (ii) 第一肢**因为正确的理由**成立,
   增量 AWS 成本 **$0**(搭车)。
3. **⭐⭐ 英雄组 —— 新开 `[hero]` issue:`test_axe_bkb_supply_staged_frame.lua:548`
   用等式钉住语料尺寸(§7)**,是本轮 Lua trunk red 的**唯一**成因,且是**真阳**;
   建议改 `>=` / 用 `tests/corpus_scale.lua` 的 `ratchet()`,作者要的下界一条不丢。
4. **⭐⭐ GH #364 —— 第三/四个新实测点 + 第二条假说被证伪(§5)**:同一个 runner,
   自检里 1 failed、独立跑 0 failed ⇒ 不在 runner 上。第一条建议第三次被证明必要。
5. **⭐⭐ GH #358 —— 第四个实测点(§6)**,9 条未跑、120s 撞顶,形状不变;
   **提醒不要把腿级 `UNCERTIFIABLE: none` 读成它好了。**
6. **⭐⭐ #308 第十三次点名**:帧通道仍停在**第八波** 1/16 —— **本轮未发波,波计数不前进**,
   只有按轮计的催次前进(两者不要混)。`--rec-slots` 按分支 (A) 保持 1。
   这仍是 owner P1/P2 条件 (a) 的唯一瓶颈。
7. **⭐⭐ W30/W31 promote 判断第六轮点名**(前五轮未见裁定)—— 卡住 44/45/47 三个家族的
   **从来是条件 (a)**,不是数据厚度。
8. **⭐⭐ #285 第十八轮催**;**⭐ `strategy-5b` 第四轮点名**(条件 ③ 的事件轴量级仍未见);
   **⭐ `hero-26` 第二轮**(`director` 字段空)。
9. **⭐ §22 管道守卫第二个外部验收点**(§1):本轮第一条命令又是那个形状,守卫当场拒绝,工作正常。
10. **⭐ 存量**:#207 仍停在**第三十九波 armed**(未发波,波计数不前进);#218 第三十七轮;
    #367(上一轮开的假阳棘轮)待裁;#295/#252/#256/#282 建议关闭;#329/#321 待裁;
    `UNKNOWN STATUS` 4 条(#317);#313/#290/#291/#298/#299/#349/#350(#355)照旧。
11. **⭐ cadence 三个洞**(自检):director 09:55→13:26Z(3.5h)、replay-check 13:00→16:45Z(3.8h)、
    strategy 04:20→07:55Z(3.6h)。按工具自己的 LIMIT,**报出来的是问题不是判决**。

---

## 10. 泄漏检查

- **开工**:`check_costs.sh` 的 running/pending 小节 **空**。
- **收尾**:`rc.sh … check_costs.sh --leak-only` ⇒ **`RC_EXIT=0`**,小节 **空**。
- 本轮**未发任何实例**,故无起飞记录可逐 id 对照 —— **要的是「0 台在跑」,本轮就是 0**。
- 常驻成本仍只有一个 AMI。

---

## 11. 铁律 6

- `bots/` / `game/` **一行未改**(本轮改动全在 `iterations/` 下)。
- 静态半 `bash tools/agent/luacheck_gate.sh` ⇒ **裸退出码 0 / `luacheck bots game: 0 warnings`**
  (**不经管道读**:`gate > log 2>&1; echo $?`)。容器缺 luacheck,**gate 自己装上了**
  (`installed luacheck (it was missing; apt package lua-check, bounded)`)。
- `core.hooksPath` 已由开工自检上膛(`push gate armed (core.hooksPath=.githooks)`)。
- **未用 `RULE6_BYPASS` ⇒ 本报告无「SKIPPED, not passed」行。**
- 动态半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)**未跑且不声称**。
  python 那套**跑了**:`71 passed / 0 failed / 1 uncertifiable`,**`RC_EXIT=2` 不是通过**,
  uncertifiable 是 `test_selfcheck_lua_leg.py` / GH #358(§6)。
- **顺序条款(#290)**:**先 push 再发表**;每份草稿各过一次 `claim_precheck.sh`。读数见 §11 附。

---

## 12. 本轮 token 用量

```
TOKENS total_in=3,195,467 out=35,856 turns=29
```

(统计时刻之后的收尾回合不计入 —— 提交、push、发表 issue 各若干回合;量级参考足够。)
