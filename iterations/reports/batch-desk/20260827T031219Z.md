# 批测台 2026-08-27T03:12Z — 收割 W15-R;本轮不发波(闸 (i) 未解锁)

**一句话**:W15-R 三粒补跑全部跑满并自毁,159 计分局零丢局,与 906 合成
**37-id 全集迄今第一份完整 4 种子读数:gpm −19.96 / 0-or-1-of-4,四个经济量一致为负**;
本轮闸 (i) 还差 3h05m,**不发波**;顺带发现 **GH #210 已被总监解掉**(test_set 第 2 行
现在是 **40 id / 354 字节**,`abilanc` 已入),W16 的接线门已零成本预检 exit 0。

---

## 1. 开工自检(铁律 10)

`routine_selfcheck.sh` worst exit **3**(非阻断,发现项不属本台):

| 腿 | 读数 |
|---|---|
| stable-v* 锚点 | 两个全 ok(EXISTS / PINNED / SHIPPED) |
| trunk python | **40 passed / 1 failed**(`tests/test_selfcheck_lua_leg.py`)|
| 快 Lua 检测器 | **1/9 失败**(`test_corpus_scale.lua`:`test_salveyield_arbitration.lua:450` 把 fixture 数写死成 `== 105`,正是 GH #106/#127 那个形状)|
| unlanded | OK(可核验窗口内无未落地工作)|
| 未裁队列请求 | none |
| cadence | 1 处洞在 `strategy`(10:34Z→14:23Z,3.8h)——**不是本台的** |

⚠️ 上面两条红都在**本轮之前就红了**,且都属于 hero/strategy 组的树
(`test_salveyield_arbitration.lua` 是 01:20Z `bc75ad2b` 带进来的)。本台不改别组的测试,
**交棒里点名**(见 §8 ⑨)。注意自检自己那句话:「main 是否也红**不由这一行确立**」。

## 2. 成本

`check_costs.sh`:MTD **$50.671**(budgets,刷新 08-26T23:46:06Z),CE 复核
**$50.6709276212** —— **逐位一致,连续第五轮**。预测 $76.999,Budget 限额 $100。

### 2.1 围栏算术(章程 §2 甲:MTD + 近 12h 内已发波次 × 该波单价)

MTD 快照 23:46:06Z 早于 W15-R 发波(00:15:43Z)⇒ **W15-R 一台都没进去**。

```
围栏 = $50.671 (MTD) + W15-R 按需 3 台 $1.69 (本轮实测,见 §5) = $52.36
```

上一轮用的是 $1.61(= 3/4 × $2.15 的比例外推),**本轮实测把它抬到 $1.69**(+5%)。
$52.36 ≤ $60 ⇒ 闸 (iii) **过**。但**闸 (i) 不过**(§4),所以本轮不发波,余量原样留给 W16。

### 2.2 §4b(iii) 的配套义务(跨 $50 的书面解释)

MTD 已在上一轮越过 $50 一档,**owner 会/已收到 Budget 告警邮件**。本轮 MTD 逐位未动
($50.671 与上一轮同一张快照),**本轮没有新增跨档**,但把账再写一遍以便对账:
08-26 一天四波(03:10Z + W13 + W14 三波按需各 ~$2.15 + W15 一波 spot),该日 EC2-Compute
实测 **$4.086**;08-25 EC2-Compute **$7.271**。**$50–$100 这一段是 owner 已批过的预算**,
告警邮件是一档 ACTUAL 提醒,不是限额也不是冻结。

## 3. 收割 W15-R(本轮主产物)

标准路径 `recover_verdict.py` 从 S3 逐局数据全量重算,**按 run 前缀分目录再池化**(GH #225):

```
pool/fc61ad  48 analysis  ← spot_20260827_001541_1_79f32c92…_fc61ad   种子 888
pool/21e0a8  64 analysis  ← spot_20260827_001546_1_79f32c92…_21e0a8   种子 895
pool/7b6b1f  64 analysis  ← spot_20260827_001550_1_79f32c92…_7b6b1f   种子 896
```

**一 run = 一种子,零交叉**(逐文件 `script_version` 反解核对过)。
`files_seen=176 / games_loaded=176 / source_dirs=3 / unparseable=0 /
pooled_basename_dirs_overridden=0`,**176 = 48+64+64 逐个对上 S3 对象数,零静默丢局**。
`contrast = vs_stable`(单臂,未传 `--cand-ref`,与发波形态一致)。

### 3.1 arm 串核对

从落盘语料 `script_version` 反解:**37 id / 328 字节**
(`mirror:` 7 + 串 328 + `:s888:radiant` 13 = 348 ✓),**与 W15/W15-R 发波记录逐字相同**。

### 3.2 W15-R 三粒(主读数,预登记的「三个补跑 run 单独池化」)

| seed | ab | ba | gpm | xpm | deaths | last_hits | winrate | 计分 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 888 | 28 | 14 | **−14.98** | −15.23 | +0.16 | −0.59 | 0.500 | 42 |
| 895 | 34 | 25 | **−23.90** | −14.65 | +0.29 | −2.20 | 0.475 | 59 |
| 896 | 36 | 22 | **−53.46** | −32.22 | +0.37 | −3.68 | 0.505 | 58 |
| **均值** | | | **−30.78** | **−20.70** | **+0.27** | **−2.16** | **0.493** | **159** |

`comps_better`:gpm **0/3**、xpm 0/3、deaths 0/3、last_hits 0/3、winrate 1/3。
`unfinished` **0**。`suggested = hold_or_reject`。
`winner_by` = `engine_natural` 176/176;`winrate_independent_of_gold` **0/176 局**
⇒ 本波 winrate 是金钱信号的**符号粗化读数**,不是独立证人(工具自己的 LIMITS)。

### 3.3 与 906 合成 4 种子(**必须注明出处**,预登记条款)

906 来自 **W15 的 `ffdcb6` run(spot,08-26T21:14Z 时段)**;888/895/896 来自
**W15-R 的三个按需 run(08-27T00:15Z 时段)**。**同树(`79f32c92`)、同 arm(37 id/328 字节)、
不同波次时段**。

| seed | 出处 | gpm | xpm | deaths | last_hits | winrate | 计分 |
|---|---|---:|---:|---:|---:|---:|---:|
| 906 | W15 `ffdcb6`(spot)| +12.52 | +4.52 | +0.26 | −1.19 | 0.513 | 59 |
| 888 | W15-R `fc61ad`(按需)| −14.98 | −15.23 | +0.16 | −0.59 | 0.500 | 42 |
| 895 | W15-R `21e0a8`(按需)| −23.90 | −14.65 | +0.29 | −2.20 | 0.475 | 59 |
| 896 | W15-R `7b6b1f`(按需)| −53.46 | −32.22 | +0.37 | −3.68 | 0.505 | 58 |
| **4 种子均值** | | **−19.96** | **−14.40** | **+0.27** | **−1.92** | **0.498** | **218** |

`comps_better`(4 种子):gpm **1/4**、xpm **1/4**、deaths **0/4**、last_hits **0/4**、
winrate **2/4**(0.513 / 0.505;888 的 0.500 是**正中性,不计入 better**)。

**这是 37-id 全集迄今第一份完整 4 种子读数。** 判读留给协同组/总监 —— 本台只登记:
四个经济量**方向一致为负**,winrate **贴着中性**(0.498,4 种子);
上一轮把 906 的 +12.52 圈进「单种子 ±30 = 纯噪声」是对的,**它在 4 种子里被另外三粒淹掉了**。

⛔ **W15 那 30 局孤儿单腿 radiant 局没有并进任何池**(预登记条款,并腿会让 888/895/896 的
radiant 腿混两个时段而 dire 腿只有一个)。本轮下载只取三个补跑 run 目录,**物理上做不到并进**。
⛔ 四个经济量**未施铁律 4(i) 两层符号检验**(GH #204 已复现,见 §3.4)。
`zusstatic` 仍 **UNDETERMINED**(GH #207,**连续第五波空转**);`abilanc` **UNREAD 不是 SILENT**。

### 3.4 ⭐ GH #204 第六次复现 —— 三粒**同时**两层反号,而 |D| 三粒**各不相同**

| seed | ab | ba | **E = (ab+ba)/2** | **D = (ab−ba)/2** |
|---|---:|---:|---:|---:|
| 888 | −212.04 | +182.09 | **−14.98** | **−197.06** |
| 895 | +41.28 | −89.08 | **−23.90** | **+65.18** |
| 896 | +60.04 | −166.97 | **−53.46** | **+113.51** |

**三粒全部两层反号 ⇒ 机械施 4(i) 会把整波判成「噪声」**,而 `E` 三粒同号且量级 15–53。
两层反号在这里仍然只是 **|D| > |E|** 的算术后果。

**本轮比上一轮多买到一件事:`D` 的「逐种子近乎常数」这次是横着看出来的。**
上一轮的证据是**纵向**的(同一粒 906 跨 W13/W14/W15,`D` 稳在 +142.29/+138.81/+121.22,
而 `E` 从 −138.2 翻到 +12.52);本轮是**横向**的 —— 同一波、同一树、同一 arm、同一时段,
三粒种子的 `D` 是 **−197.06 / +65.18 / +113.51**,**连符号都不同**,极差 **310 gpm**。
`D` 跟着**阵容**走而不跟着腿走,这正是 #204 的论点形状,而且 **888 是迄今第一粒负 `D`**。
⇒ 一个「跟着种子走、跨种子能翻符号、量级压过效应」的量,**不是效应的证人**。

## 4. 本轮发波决定:**不发**

- **闸 (i) 不满足**:6h 锚点 = W15-R 末台 `2026-08-27T00:15:53Z` ⇒ 解锁 **06:15:53Z**;
  **先打钟点**(`date -u` = 03:10:52Z),**还差 3h05m**。⇒ **不发波,本轮到此为止。**
  (上一轮登记的坑照做了:不手写 epoch 常数,现打钟点再比。)
- 闸 (ii) **满足**(不构成放行):远端 tip **`a5e467d0`** ≠ W15 树 `79f32c92`,
  `bots/`/`game/` 有 **4 个 commit**(`salveally` 87b32479、ABILVALUE 624de021、
  `salveyield` GH #237 bc75ad2b、GH #238 b49fe55f)。核法按章程走两步:
  先 `git fetch --depth 1 origin <全40位SHA>` 再 `git log`,**exit 0 = 真的可比**,不是空输出。
- 闸 (iii) **满足**:围栏 $52.36 ≤ $60(§2.1)。
- **队列不构成例外**:22 条 pending 逐条看过,**没有一条要求专波** ——
  11 条 `ROUTED_ARCHIVE_SCAN`(零 EC2)、3 条 `RECEIVED_NOT_SCHEDULED`(受理≠可发)、
  2 条 `DEFERRED_PENDING_W3_RESOLVE`、2 条 `REJECTED`、
  2 条 `APPROVED_ADMITTED`(明写「搭下一波全集,不申请专波」)、
  1 条 `APPROVED_CONDITIONAL` 但 wave 写的是 **W6**(早已过去)。
  ⇒ 章程 §4a 的「显式请求不受 (i)(ii) 节流」**本轮无适用对象**。

## 5. ⭐ 新登记:单波机时可以从 **S3 时间戳**测,这条路**不会过期**

交棒项②(spot 单波成本实测)上一轮卡在「CE 没结清」。本轮先试了两条老路,**两条都死了**:

1. **CE 逐日**:08-27 **一行都没有**(发波已 3h);08-26 的行与上一轮**逐位相同**
   (EC2-Compute $4.0857 / VPC $0.030122 = 6.02 IPv4 机时,16 台 ⇒ 0.377 h/台 vs 章程 0.745)
   ⇒ **过了 24h 仍是 `est` 且没动**,结清与否本身**不可判**。
2. **`describe-instances` 反解机时**:**返回 0 条**。终止实例约 **1 小时**后就从该 API
   消失 —— 08-26 那 16 台、以及本波三台(02:xx 结束),**在本轮触发时证据已经没了**。
   ⚠️ 这是一条**静默失效**:它不报错,它返回空,**而空会被读成「没有实例」**
   ——与章程里「shallow clone 的空输出被读成无漂移」**完全同族**。

**第三条路成立,而且便宜、免费、不过期**:`s3 ls --recursive` 的 **LastModified**。
逐局文件是**跑完就传**的,所以 `首个对象 → 末个对象` 夹出实例的有效工作窗口,
再加发波时刻就是机时。S3 对象**永久留存**(录像组依赖它),⇒ **任何一轮都能回溯任何一波**。

| run | seed | 发波 (LaunchTime) | 首对象 | 末对象 | 发波→末对象 | 对象数 |
|---|---|---|---|---|---:|---:|
| `fc61ad` | 888 | 00:15:43Z | 00:28:08Z | 00:56:14Z | **0.675 h** | 132 |
| `21e0a8` | 895 | 00:15:48Z | 00:31:18Z | 01:08:12Z | **0.873 h** | 175 |
| `7b6b1f` | 896 | 00:15:53Z | 00:31:27Z | 01:09:13Z | **0.889 h** | 176 |

- **实测 0.812 h/台**(三台均值,**尚未含末次上传后到自毁的收尾**,故是**下界**)。
  章程现行值 **0.745 h/台**(W10–W14 账单侧)⇒ 本波**至少高 9%**。
- W15-R 单波成本 = 3 × 0.812 h × $0.673 = **$1.64**,加收尾按 ~1.5 min/台 ⇒ **≈ $1.69**。
  上一轮的比例外推 $1.61 **偏低 5%**(朝**危险**那一侧 —— 围栏算术会少记)。
- ⚠️ **888 这台早收工 12 分钟**(0.675 h vs 0.87/0.89),对象数 132 vs 175/176,
  ba 腿只跑到 **14 局**(另两粒 25/22)。**不是抢占**(按需,且自毁正常),
  是**波内 ab/ba 分配不均**的老形状被这一粒放大了。登记,不判读。

**⇒ 交棒项②改写**:方法已经有了(上表),**缺的只是一波「四台跑满、无回收」的 spot 波**。
在那之前围栏对 spot 继续用 `~$0.8`,并**记住它至今一次都没被实测过**。

## 6. ⭐ GH #210 已解 —— 但解它的是总监,不是本台,而且**没人告诉过本台**

`test_set.md` 第 2 行现在是 **40 id / 354 字节**(权威计数来自接线门自己的输出;
本台第一次用 `echo -n | tr | wc -l` 数成 39,**那是少一个换行的经典差一**,已更正)。

- `abilanc` **已入**(总监 b75afa17,GH #196)⇒ **本台连喊五轮的 #210 不再成立**:
  第 2 行与三波落盘语料**不再打架**。
- 另外新增 **`bbfight` / `bbshort` / `pullthink`**(总监 b5320112 落 GH #218 的裁定;
  `bbrespawn` 按 strategy-22 的 `REJECTED` **正确地排除在外**)。
- ⇒ **W16 起「机械逐字取第 2 行」重新是对的**。W14/W15/W15-R 那条
  「不能机械取第 2 行」的临时纪律**到此作废**,别再照抄。

**零成本预检(为 W16 省一轮)**:接线门
`check_armed_wiring.py --cand <第2行40id> --ref a5e467d0…` ⇒ **exit 0**,
`all 40 armed ids wired on a5e467d0…`(新三个:`bbfight` → `jmz_func.lua:10594`、
`bbshort` → `:10633`、`pullthink` → `mode_roam_generic.lua:224`,后者 2 处调用)。

⚠️ **新 id 入集的积压没有清空,只是换了一批**:本轮闸 (ii) 读到的 4 个新 commit 里
又有 **`salveally`(87b32479)与 `salveyield`(GH #237)**两个 gated id,**都不在第 2 行**。

## 7. 局数(铁律,章程 §7)

**(a) 上一波(W15-R)最终实测**:
- 落盘 **176** analysis / **计分 159** / **unfinished 0** / 暖场 **17**(6+5+6)。
- 逐 seed ab/ba:888 = **28/14**、895 = **34/25**、896 = **36/22**。
- ba 占比 **0.352**(56/159)。**可与 W11–W14 的 0.306–0.325 序列并列** ——
  本波三台**全部跑满、零回收**,分母与那四波是同一种东西
  (⚠️ 与之相对,W15 的 0.356 **不可并列**,它只有一台跑满)。
- 上一轮 W15-R 的预期是「~155 落盘 / ~138 计分」⇒ **实测 176/159,双双高于预期**。

**(b) 本轮在跑波次**:**无**(本轮不发波,§4)。

## 8. 泄漏检查(两次)

- **开工**:`check_costs.sh` 的 running/pending 段 **空**(W15-R 三台已自毁完毕)。
- **收尾**:`check_costs.sh --leak-only` ⇒ **0 台在跑**(收尾要的是这个数,不是 MTD)。
  常驻只剩 **AMI `ami-0a990a26d89c66547` + 快照**,与历轮一致。无游离实例。

## 9. 铁律 6

- 静态门 `bash tools/agent/luacheck_gate.sh` ⇒ **exit 0**,`luacheck bots game: 0 warnings`
  (容器缺 luacheck,**脚本自装 `lua-check`**)。`core.hooksPath` 已由 `arm_push_gate.sh` 上膛
  (`already set`)⇒ 本轮两条 `git push` 各自再过一次同一道门。**未使用 `RULE6_BYPASS`。**
- 动态半(`lua5.1 tests/run_tests.lua`,~100 min,GH #124)**本轮未跑**,本台**不声称**它。
  本轮改动只有报告 + 章程(markdown),`bots/`/`game/` **一行未改**。

## 10. 交棒

1. **下一轮本台 —— 发 W16**。解锁 **`2026-08-27T06:15:53Z`**(先 `date -u` 再比,别写死 epoch)。
   arm 串**直接取 test_set 第 2 行**(40 id / 354 字节),**接线门已预检 exit 0 on `a5e467d0`**;
   市场类型按 owner 拍板走 **spot 优先 + 容量降级阶梯**,并读 `InstanceLifecycle` 作证据。
2. **下一轮本台 —— 交棒项② 重新定义**:方法已备(§5 的 S3 时间戳表),
   等**第一波四台跑满、无回收的 spot 波**即可落地首个 spot 单波成本实测。
3. **总监 —— GH #210 可结案**(§6:第 2 行已 40-id,`abilanc` 已入,与语料不再打架)。
4. **总监 —— 已开 `[batch]` GH #239(§5)**:把「终止实例 ~1h 后从 `describe-instances`
   消失、且**返回空而不是报错**」登记为一条静默失效(与 shallow-clone 空输出并列),
   并把 S3 时间戳法写进章程硬知识。**连带一条本台自己要认的话**:`--leak-only` 读到 0 台
   只能证明「此刻没有在跑的」,**不能**证明「过去这一轮没漏过」。
5. **总监 —— GH #233**(spot 优先的第一个硬边界,两条待裁:(a) 闸 (iii) 单波价现在是随机
   变量而降级发生在判定之后;(b) 回收+补跑 = 一波钱花两次)。**本轮新增一条支持材料**:
   W15-R 实测 $1.69 > 外推 $1.61,说明**比例外推朝危险侧偏**,(a) 的「一律按按需价预估」更稳。
6. **总监 —— GH #207(第六轮)** `zusstatic` 仍 UNDETERMINED,**连续第五波空转**。
7. **总监 —— GH #204(第六轮)**,本轮是**第一份横向证据**(同波三粒 `D` 连符号都不同,§3.4)。
8. **总监 —— GH #219 可结案**(owner 已选转 spot),连带读 #233。
9. **hero/strategy 组 —— trunk 两条红**(§1):`tests/test_selfcheck_lua_leg.py` failed;
   `test_salveyield_arbitration.lua:450` 把 fixture 数写死成 `== 105`(GH #106/#127 的形状,
   由 01:20Z `bc75ad2b` 带进来)。**本台不改别组的树,只点名。**
10. **总监 —— #218 后续**:`salveally` / `salveyield` 两个新 gated id 不在第 2 行,
    **新 id 入集裁定继续积压**(只是换了一批,§6 末)。
11. **录像组 —— `abilanc` 的 (a) 取证**:W14 的 156 个 `.dem`(约 09-16 过期)+
    W15 的 84 个 + **W15-R 新增语料**(三 run 共 483 对象,含 `.dem`)。
12. **存量催办**:#217(第五轮)/#211(第六轮)/#225/#180(第十二轮)/#171(第十轮)/
    #200(第九轮)/#181/载体门 PARTIAL(第十七轮)/`stable-v*` tag(第十四轮)/
    `campdanger`(第九轮)/§BL.4 机械化/#75(第八轮,维持 12)。

## 11. Token 用量(铁律 8)

```
TOKENS total_in=3,997,663 out=36,489 turns=39
```

(到统计时刻为止;统计后的收尾回合不计入。)
