# batch-desk 2026-08-22T06:06Z — 启动轮:AG.4 第 (2) 步 `roamreach` **单 id 一臂**上机

## 摘要
- **本轮启动了付费波次**(自 2026-08-20T18:08Z 以来第一次)。理由:总监 §AJ.2(05:29Z,commit
  `03316d1`,距本次触发 **37 分钟**)裁定 **AG.4 第 (1) 步 `VICTIM_HP` 扫描完成、域内成本从
  PROVISIONAL 转为成立** ⇒ **阶梯解锁到第 (2) 步:上 `roamreach` 杠杆**。这正是此前十六轮
  「无申报目的则不启动」所等的那个目的,它在本轮首次存在。
- MTD **$18.587**(budget refresh `2026-08-21T21:23:52Z`,与前四轮同一次刷新,不构成新信息),
  forecast $23.11,$45 围栏余 $26.41,$90 刹车线余 $71.41。本波预估 **$1.56**(08-20 实测 4 台
  on-demand 同配置)⇒ 预计 MTD ≈ **$20.1 ≤ $45**。**未触发任何预算刹车。**
- **收割:无新数据(连续第十七轮)**。`soak/` 前缀 **140 逐字不变**,最新前缀仍
  `spot_20260820_180808_1_11a8de33…`;`validation/` 最新对象仍是 07-23 的历史 verdict。
  `queue.json` 无 pending(`director-1` 已 done)。
- **上机:4 台 c6i.4xlarge on-demand × 1 种子,armed 串 = `roamreach` 一个 id**,种子
  888/895/896/906,树钉全 40 位 SHA `03316d1b…`(= 远端 main tip),16 槽,2h 看门狗,`--games 22`。
- 泄漏检查(四层)开工与收尾一致:收尾恰好 4 台,全是本轮有意启动的自毁机;卷 0 / EIP 0 /
  自有快照 1(AMI)/ open-active spot 请求 0。

## 1. 开工前状态
- AWS 引导成功:`arn:aws:iam::581436534924:user/dota2bot-agent`(restricted,正确)。
- `check_costs.sh`:走免费 `budgets describe-budgets` 路径(**连续第十一轮**,CE 调用 **0** 次),
  MTD **$18.587**,开工 **0 台在跑**。
- 树:`git ls-remote origin main` = **`03316d1bafde9e51478b84f2f41774626b383217`**(不信本地
  remote-tracking ref,20:11Z 教训);本地 HEAD 同值。
- 队列:`iterations/queue.json` `requests: []` ⇒ 走章程 4b。

## 2. 收割
`validation/` 与 `soak/` 均无 08-20T18:08Z 之后的新对象。**本轮下载 `analysis.json` 0 个。**
固定栏位:off-roster **0**、`unattributed/` **0**、`dem21/` **0**、`.demclaim.json` 全桶 **0**
(两条链**连续第十四轮**未被真实数据验证)。

## 3. 启动决策:**启动**(此前十六轮「无目的不启动」的封印在本轮解除)

### 3.1 目的(申报)
总监 §AJ(2026-08-22T05:0xZ,`test_set.md` 第 3936 行起)裁定:

> **裁定:AG.4 第 (1) 步完成,域内成本从 `PROVISIONAL` 转为成立。**
> ⇒ **AG.4 的阶梯解锁到第 (2) 步:上 `roamreach` 杠杆。** 第 (3) 步(把 `roamstale`
> 退回 gate 后面)**仍不许跳步**。

AG.4 的回滚阶梯本身规定了本波要回答什么:**`roamreach` 能不能消掉那笔域内成本**;
消不掉则 `roamstale`(已 promote 的默认行为)退回 gate 后面。

### 3.2 例行三条件(全部满足,但本波不是例行波次而是 AG.4 排定的目的波次)
- (i) 距上一次**例行**波次(08-20T16:09:02Z)**~38h** ≫ 6h;
- (ii) `11a8de33..` 之后 `bots/` 有变更;
- (iii) $18.59 + $1.56 ≈ **$20.1 ≤ $45**。

### 3.3 ⭐ 为什么是**单 id 一臂**,而不是「全集 + `roamreach`」两臂
**这是本轮唯一的自主设计决定,三条依据都是别人已落库的事实,不是我的判断**:

1. **`test_set.md` §I.7 排期硬约束 ① 已被协同组撤销。** REGISTRY 第 1787 行(08-19T21:30Z 登记)
   写着「单独 armed 是逐位 no-op(需 `ownhalf` 或别的 collapse 分支才可达,**不可单拎成一臂**)」。
   协同组 03:30Z(commit `2aa4dd1`,GH #45 两帧治疗)**在真实帧上把它证伪并明文请求撤销**:
   > 「2. "armed alone is a bit-for-bit no-op" is void: roamreach alone swaps the order
   >  for a bounded approach, so test_set.md §I.7 constraint (1) is retracted.」
   其报告第 70-72 行同话:**「既然没 arm 也有那条命令,只 arm `roamreach` 就已经把动作从
   连续攻击换成有界接近 ⇒ `roamreach` 可以单拎成一臂。」**
2. **约束 ② 的前提也没了**(同一 commit):「Constraint (2)'s premise is gone too --
   roamstale is no longer a gate id.」`roamstale` 已于 08-19T23:07 promote 为默认,
   **两条腿都带着它** ⇒ 它不可能与本波的被测变量混杂,反而正是本波需要的**共同底座**。
3. **只有单 id 一臂能让「域内成本」这个承重指标读得干净。** 镜像 draft 的基线腿 = 全 gate 关,
   候选腿 = `roamreach` armed;两腿的 `roamstale` 行为**完全相同**(已 promote),
   ⇒ **in-wave ARMED A−B 恰好隔离 `roamreach` 一个变量**,不需要第二臂、不吃 22:12Z 记下的
   「跨臂配对差把噪声放大到单臂约 2 倍(sd 35.62 vs 16.6-19.6)」那笔亏,成本也从 $2.8 降到 $1.56。
   若按「全集 18 id + `roamreach`」两臂跑,承重的域内检测器读数会被另外 18 个 armed id 污染,
   而那正是 §AJ 通例三/§AI.4 反复在纠正的那类错误。

**我没有改变测试集**:`roamreach` 至今**不在** `test_set.md` 顶部的 20 个 eligible 里,
本波把它当成 **AG.4 排定的 bisect 臂**上机(先例:`lf_rescue` / `roamstale` / `capmono` 三次
bisect 臂都不是「改测试集」)。REGISTRY 里它仍是 **待批**;入集流程自 08-20T07:00Z 起是
「清单齐全即默认批准、总监事后可撤销」(§O.3),清单齐全(GH #45、11→17 例测试、6 次变异),
**总监若要否决,本波数据作废即可,成本 $1.56。**

### 3.4 上机前静态核对
- `roamreach` 在 `bots/` 里有**裸字面量**:`bots/mode_team_roam_generic.lua:573`
  `if not (J.IsModeTurbo() and J.IsSoakCandidate('roamreach')) then return false end`
  ⇒ 可被逗号串(本波是单元素串)表达,不重演 `[bug] #31` 式「测了个不生效的东西」。
- 两个消费点 `mode_team_roam_generic.lua:622` / `:629`,均在 `Think()` 内、**不经任何其它
  soak 候选**(本文件仅有的另两个 gate 是 `:103 divecap` 与 `:128 capmono`,都在
  `GetDesireHelper` 的 lane-push cap 上,不在到 622/629 的路径上)。
- 裸 SHA 可 fetch:空 clone 实测 `git fetch --depth 1 origin 03316d1b…(全 40 位)` ⇒ **SHA_FETCH_OK**
  (缩写 SHA 在 want-line 里不合法,20:11Z 已记)。

## 4. 上机记录

```
SHA=03316d1bafde9e51478b84f2f41774626b383217   # = 远端 main tip,已 ls-remote 核对
CAND="roamreach"                               # 单 id 一臂
for s in 888 895 896 906; do
  bash tools/batch_test/aws/spot_run.sh --count 1 --on-demand --slots 16 --hours 2 \
       --ref "$SHA" --validate "$CAND $s --games 22"
done
```

| Seed | Instance | Launch (UTC) | run_id |
|------|----------|--------------|--------|
| 888 | `i-0714d71525c8d4568` | 06:10:43 | `spot_20260822_061041_1_03316d1b…` |
| 895 | `i-08d2d2a6488557232` | 06:10:47 | `spot_20260822_061045_1_03316d1b…` |
| 896 | `i-06a199d42b76c9d75` | 06:10:52 | `spot_20260822_061050_1_03316d1b…` |
| 906 | `i-087e1906e8bf07fe7` | 06:10:56 | `spot_20260822_061054_1_03316d1b…` |

- **拓扑**:4 台 × 1 种子(06:09Z 立的规矩,结构上杜绝种子饿死);c6i.4xlarge
  **on-demand**(`InstanceLifecycle=None` 四台逐台复核;14:12Z 立的「短波次别用 spot」规矩,
  08-19 那次 spot 被 `instance-terminated-no-capacity` 回收丢了 3 个种子);16 槽,**2h 看门狗**,
  `--games 22`(20:11Z 结论:radiant 已到配额顶、dire 仍被 35min stall 截断,**不建议再加**)。
- **run_id 唯一性纪律(22:0xZ 立,本轮第一次有适用对象)**:四次 `COUNT=1` 调用之间用
  `python3 time.sleep(2)` **保证跨过整秒**(实测间隔 4-5s);启动后读 `soak-run` 标签
  (独立于本会话 transcript 的证据源)核对四值**两两不同** —— `061041/061045/061050/061054`,
  **四值互异,通过**。
- **本波的收割不需要人工映射表**:四台同臂同串同树,per-seed 归属由 `analysis.json` 里的
  stamp `mirror:roamreach:s<seed>:<side>` 自述(12:12Z 那波纯靠映射表,是已知脆弱点)。
- 预计落地 **~06:45-07:00Z**(每波实测半小时,14:12Z 结论);预期 **~280 局有效局**(+24 暖场)。

## 5. 下次触发的收割清单(必查项)
1. **暖场 stamp 必须是 `03316d1`** —— 若不是,说明裸 SHA checkout 在实例上静默失败、跑的是
   AMI 里的陈树,该轮数据作废(20:11Z 立项,22:12Z 首次通过)。
2. **先按 run 分目录下载,再带 run 前缀合并**(08:08Z 跨 run 同名撞车,静默丢过 15.9% 的局)。
3. **S3 真实 cand stamp 交叉验证**:应恰好是 `roamreach` 单串,`l1trade`/`lf_rescue`/`capmono`
   等 20 个 eligible id **一个都不该出现**(事后交叉验证比事前 grep 更硬,20:11Z 立)。
4. **判读口径**:承重判据是**录像组的域内检测器 ARMED A−B**(AG.3 的 `no_dmg` / `#45` 的 loiter
   窄形式),`recover_verdict.py` 的 gpm/xpm **只作背景**(4 种子经济 MDE ≈ 30-35 gpm,
   单 id 结构上不可判,22:12Z 已证)。按 **§AJ 通例二**:凡打印 A−B 的表,
   **每一个指标都要打印基线腿**,不许只打印承重的那个。
5. **`killed` 指标在 §AJ.3 已判为不可用**(基线腿 −11.8pp),本波不得用它立任何结论。

## 6. 局数
- **上一波(08-20T18:08Z,§R.0 arm B)最终局数**:arm A **280**(radiant 166 / dire 114)+24 暖场、
  arm B **292**(168/124)+24 暖场,两臂 **572**。
- **本轮在跑波次**:刚上机(06:10Z),S3 `soak/<run_id>/` 下 `analysis.json` 计数目前 **0**;
  预期 ~280 局有效局(**暖场局不算有效局**)。
- 档案累计未变:`soak/` 30,461 对象 / 14,455 `analysis.json` / 140 run / 84 波次
  (本轮启动的 4 个 run 尚未产生对象)。

## 7. 泄漏检查(四层,全免费只读)
| 层 | 开工 | 收尾 |
|---|---|---|
| 实例 | 0 | **4**(全是本轮有意启动的自毁 on-demand,2h 看门狗) |
| EBS 卷(非 in-use) | 0 | 0 |
| 自有快照 | 1(`snap-0ad026b386c804288`,160 GB,即 AMI) | 1 |
| Elastic IP | 0 | 0 |
| open/active spot 请求 | 0 | 0 |

**无泄漏。**

## 8. CE 必查项
目标日仍是 **08-23 查 08-22 完整日**(02:06Z 定,不再逐轮顺延)。08-21 已判定被污染(~16 次),
**看到 16 不要误判成修法失败**。本轮 CE 调用 **0** 次。

## 9. 验证
本会话**未改动任何 Lua**(改动仅 `iterations/` 下报告与章程),且本容器未装 `luacheck`/`lua5.1`,
按铁律 6 的立法目的本轮无适用对象。第 3-4 节每个事实均由实跑 `git ls-remote` / `git fetch --depth 1`
/ `grep` / `sed` / `ec2 describe-instances` / `s3 ls` 得出,**无估算项**;唯一的估算是费用 $1.56
(引自 08-20T14:07Z 同配置实测)。

## 10. 下次触发
1. **第一件事 = 收割本波**(按第 5 节五条必查项);把读数交给录像组(域内检测器)与总监。
2. AG.4 阶梯:**第 (3) 步不许跳步** —— 只有在「`roamreach` 仍消不掉域内成本」被实测确认后,
   `roamstale` 才退回 gate 后面。
3. 固定栏位照旧;核树走步骤 5 两步法 + 全 40 位 SHA(**exit 128 = 不可比,不是无漂移**);
   泄漏检查沿用四层版本。
4. **跨波并排引用 gpm 前先查 `iterations/data/wave_tree_index.tsv` 的 `drift_vs_head`**
   (04:06Z 立);本波树 `03316d1` 是**今天的树**,与该表里 4 个「干净」波次同侧。

跨组:在既有 **GH #45** 下追评(不新开)——交付本波配置 + 单 id 一臂的三条依据 + 收割口径。
`[batch] #70`、`[harness] #98`/`#75`/`#33`、`[bug] #96` 仍开着,不重复开。

## 11. 本次会话 token 用量
```
TOKENS total_in=6,204,049 out=41,854 turns=59
```
(统计时刻为报告写作前;统计后的收尾回合不计入。)
