# batch-desk 2026-09-12T12:11–12:5xZ — 收割轮 W69 + ⛔ 预算刹车触发(零发波)

**一句话**:W69 四粒全配对干净收割(224 局,零回收零补跑),四个经济通道
**0/4 全数为负且不是噪声区**;本轮**不发波** —— 围栏 `exit 3` 打在 **`$90` 刹车**上
(projected `$90.058`),工具逐字要求「Stop and report to the owner」,已照办。
⭐⭐⭐ 头号产出**不是**那份 verdict,是零成本从本波自己语料里读出来的一件事:
**dire 赢 221/224,而两条腿的 dire 胜率逐乎相同(99.3% / 98.4%)⇒ 胜负通道量的是地图不是臂**,
这正是铁律 2(b) 的胜负半边连续十波结构性缺席的**机制解释**。

---

## 〇、上一轮交棒执行情况

| # | 交棒 | 本轮 |
|---|---|---|
| 1 | 收割 W69,分子目录下载,cand 传完整臂串 | ✅ 见 §三 |
| 2 | ⭐⭐⭐ `status_code`/`ab`/`ba`/`arm_depth`/`survival_min` **写进 `machines[]` 而非散文**,且**必须当轮做完** | ✅ **第一手**,见 §五;闸 (iv) 首跑即 `exit 0` |
| 3 | 闸 (i) 别抄,现跑 | ✅ `THROTTLE_EXIT=3` |
| 4 | 成本/围栏一律现跑;**越 `$90` 转纯收割并开 issue** | ✅ 见 §二,**条件已触发** |
| 5 | W69 深度 0,不并池;读数取 `strata` | ✅ 见 §四 |
| 6 | 开工第一条命令重定向+后台+不设短 timeout | ⚠️ **第 19 次踩管道**,见 §八 |
| 7 | `origin/main` 四条 python 红不是本台引入 | ✅ 未改一行 `bots`/`game`/`tests`/`tools` |
| 8 | 发波脚本改 `tee` 不用 `tail` | — 本轮未发波,未触及该脚本 |

---

## 一、成本(当轮现跑,⛔ 下轮不得抄作前提)

`AWS_SETUP_EXIT=0`(容器无 CLI,脚本自装 `aws-cli/1.46.1`;身份 `arn:aws:iam::581436534924:user/dota2bot-agent`)。
`COST_EXIT=0`;MTD **`$86.758`**,`budget refreshed 2026-09-12T12:08:45Z`
(⭐ **快照是活的,距本轮读数仅 ~3 分钟**,不是上两轮那种冻住的戳);
`≥$35` 自动 `$0.01` CE 复核 **`86.7576682213`** 逐位一致;`forecast 209.009` / `limit 100.0` 原样登记不据以决策。

诚实 cutoff = `12:08:45Z − 11.3h` = **`2026-09-12T00:50:45Z`**。⇒ W68(`03:26Z`)与 W69(`09:26Z`)
**都在 cutoff 之后,两波都还没落账**;W67(`09-11T21:25Z`,按需)在 cutoff 之前,已落账。
⛔ 因此**不得**用「MTD 从 `$83.945` 涨到 `$86.758`」反推任何一波的价钱。

本轮新增支出 = **CE 复核 `$0.01`,未起任何实例**。

## 二、⛔⛔ 闸 (iii) 打在刹车上 —— 本轮不发波,并按工具指令上报 owner

`wave_fence.py` 跑了两次,**两次都登记**:

1. 裸跑 `--planned 1.10` ⇒ **`FENCE_EXIT=2`**,逐字
   `UNCERTIFIABLE: nothing is accruing at this instant, but 2 wave(s) above launched inside the 11.3h ActualSpend lag, so pending=$0.000 is NOT certifiable (Ruling 6).`
   ⭐ **没读成通过**。工具自己点名两波(`W69`/`W68`),与本台的 cutoff 推导**独立一致**。
2. 补 `--pending 2.20 --planned 1.10`(两波 spot × `$1.10`)⇒ **`FENCE_EXIT=3`**:

```
actual (MTD)     : $86.758   <- re-read this run, never cached
pending waves    : $2.200   (launched, may not be in MTD yet)
planned          : $1.100
projected total  : $90.058
fence            : $100.00   <- lowest ACTUAL alert not yet crossed (Ruling 1)
brake            : $90.00   (owner's line, not derived here)
operative ceiling: $90.00   = min(fence, brake)
A launch at this instant would put MTD past the $90.00 BRAKE line.
WAVE_FENCE: THROTTLED (exit 3) -- brake, not fence. Stop and report to the owner; do not wait it out.
```

`crossing registry: 0 record(s) …, none in force`(RULING 15 的退休仍然传导正确,天花板是工具自 derive 的 `$90`);
`budget filters : none`;`accrual scope : 17 region(s) read <- COMPLETE`;
`accrual check : CERTIFIED ZERO (0 accruing instances account-wide), but --pending $2.200 was given and is used as the larger claim.`
**未用** `--no-accrual-check`(无 SKIPPED 行)、**未传** `--director-crossing`。

⇒ **超出刹车 `$0.058`**。⛔ **本台不自行削减、不改线、不「等一轮再说」** —— 工具逐字写着
`do not wait it out`,这一条正是为「围栏只随 MTD 单调收紧、不会自愈」写的。**已按铁律开 issue 并推 owner。**

⚠️ **必须与之并排读的一条事实(第四轮同题)**:`budget filters : none`,而 09-08 与 09-10 两轮都第一手量到
本月 MTD 里 **外来 `c7a.16xlarge` + `r7a.16xlarge` = `$37.4355` ≈ 47.8%**(且只存在于 09-05/09-06 两天,
此后一分未涨)。⇒ **咬住本台的这条 `$90` 里有接近一半不是本台的支出**。⛔ 本台不据此主张放宽任何线 ——
这是 owner 的决定(GH #721 三条路仍未落定),本台只把两个数摆在一起。

## 三、收割 W69 —— 四粒全配对,本台在册最干净的一波之一

**语料同一性先钉死**:cand **传完整 27-id 臂串,不是 `cand_name`**(上一轮 §四 记的静默空读数坑)。
钉得住不靠那句话:语料里每一局计分游戏的 `analysis.json:script_version` 就是
`mirror:<该 27-id 串>:s<seed>:<side>`,**程序化核对过**,与 `W69_wave.json:arm_string` 逐字相同。

⭐ **上一轮弄丢的四个 `run_id` 全部找回,且不可恢复性被证否**:
`s3 ls soak/` 取到四个 `20260912_0926*` 前缀,seed↔run 由各 run 的 `script_version` 戳**独立复原**
(与丢掉的那行无关):

| seed | run |
|---|---|
| 13019 | `spot_20260912_092622_1_main_896712` |
| 13027 | `spot_20260912_092624_1_main_0128b9` |
| 13034 | `spot_20260912_092627_1_main_006854` |
| 13052 | `spot_20260912_092629_1_main_b920b0` |

**下载**:四个 run **各进各的子目录**再把父目录交给工具(GH #225),⛔ 全程无 `cp` 平铺;
逐 run 落盘数与 S3 逐个相等 **120/120 × 4 = 480**,零 `[Errno 36]`。

`RECOVER_EXIT=0`;工具自证 `files_seen 224 == games_loaded 224`、`source_dirs 4`、
`unparseable 0`、**`pooled_basename_dirs_overridden 0`**、`min_arm_depth 8`、
**`thin_arm_seeds []`**、`unfinished 0`、`scored_games 200`、`winner_by {"engine_natural": 224}`。

⚠️ **一个只在收尾核对时才没变成错误的细节留档**:本台第一次抽样读 `script_version` 时抽到的是
**每 run 前 6 局的热身局**(戳是裸 SHA `52ea2be4` 而非 `mirror:…`),差一点据此断言「本波的戳不是
mirror 形式」。24 局(4×6)热身局被 cand 过滤器正常排除(`224 loaded` → `200 scored`)。
**教训与管道坑同族:一次抽样不是一次普查**;改成 `collections.Counter` 全量普查后形状立刻清楚。

## 四、读数(⛔ 登记,不是裁定;本台不做 promote/reject 判断)

四粒 gpm:`13019 −28.84` / `13027 −12.91` / `13034 −34.24` / `13052 −19.01`。
波级 `mean`:**gpm −23.75 / xpm −17.56 / deaths +0.46 / last_hits −3.01**;
`comps_better`:**gpm 0/4、xpm 0/4、deaths 0/4、last_hits 0/4**、winrate 1/4;
`suggested: hold_or_reject`。

**铁律 4 (i-a) 两层读数**(取工具自己的 `strata.<m>`,⛔ **未手算**):

| 量 | ab | ba | side | sign_flip | side_gt_arm |
|---|---|---|---|---|---|
| gpm | −22.05 | −25.45 | +1.70 | false | 4/4 |
| xpm | −25.93 | −9.18 | −8.38 | false | 4/4 |
| deaths | +0.53 | +0.40 | +0.06 | false | 4/4 |
| last_hits | −6.02 | −0.01 | −3.00 | false | 4/4 |

⭐ **本波四个量一个反号都没有**(近几波少见):两个分层**同号同向**。
**(i-c)** 四个量都是每粒 50/50 swap-average 的估计量;`side_gt_arm 4/4` 是恒等式不是诊断,**登记不作否决**。
**(i-d)** 全程**先每粒 swap-average、再跨种子取算术平均**,⛔ 无一处按局数加权
(本波 ab/ba 严重不等 —— `35/15`、`31/19`、`36/14`、`36/14` —— 正是 (i-d) 防的那个形状)。

⭐⭐ **离散度必须登记,因为本波不在 W65 那个被拒绝的区间里**(W65 被拒的理由是 `|均值| < SE/2`):

| 量 | mean | sd | SE(n=4) | \|mean\|/SE |
|---|---|---|---|---|
| gpm | −23.75 | 9.59 | 4.80 | **4.95** |
| xpm | −17.56 | 5.29 | 2.64 | **6.64** |
| deaths | +0.46 | 0.17 | 0.09 | **5.30** |
| last_hits | −3.01 | 1.32 | 0.66 | **4.58** |

⇒ **四个通道各自都在自己标准误的 4.5 倍以上,且四粒种子在四个通道上全部同号(0/4 ×4)**。
⛔ **本台仍不裁定**,但按本台自己的登记标准,这**不是**「噪声,不写进结论」那一类读数,
和 W65「|均值| 不足 SE 一半」是相反的情形 —— 交总监。
⛔ **归因边界**:27-id **bundle**,本读数**不能**指认其中任何单个 id;
硬知识里 `lanefix` 的教训(fixture 全清、聚合强负)正是这个形状。

⛔ **家族:W69 是 27-id 串第一波,深度 0 → 4**。不与 W68(29-id)、不与 W66+W67(30-id)并池。

⛔ **胜负通道 `DEGENERATE`**(stderr 逐字 `minority side won 3 of 224 finished games (share 0.0134 < 0.20)`)
⇒ **W69 不能给它那 27 个 id 里任何一个结清铁律 2(b) 的胜负半边**。
GH #696:`13034`/`13052` 两粒 headroom 0 投不了 yes,`winrate_measurable 1/2` vs 池化 `1/4`;
⛔ `winrate_measurable` **未被拿去过任何门**。

## 五、⭐⭐⭐ 上一轮头号交棒执行完毕 —— 而这次闸 (iv) 首跑就开

`status_code` / `ab` / `ba` / `arm_depth` / `survival_min` **全部第一手读到并写进 `W69_wave.json:machines[]`**
(不是只写进散文 —— 那正是上一轮把闸 (iv) 堵成 `exit 2` 的原因)。

`SIR_EXIT=0`,**四条记录全在**,读取时距各自终止 **约 1h55m–2h**,稳在老化窗口内
(上一轮量到的远端是 ~5h–5h30m 已返回 `[]`)。⇒ **本轮的值是第一手,不带 W68 那种
`BACKFILLED … NOT re-read first-hand` 的标注**。

| seed | instance | status_code | 存活 | 机时 |
|---|---|---|---|---|
| 13019 | `i-023af1cbf9c998329` | `instance-terminated-by-user` | 52.80 min | 0.8800 |
| 13027 | `i-0a802c6f7687025dd` | `instance-terminated-by-user` | 50.82 min | 0.8469 |
| 13034 | `i-07eaed0571a85d81d` | `instance-terminated-by-user` | 51.12 min | 0.8517 |
| 13052 | `i-0594672d686a7556f` | `instance-terminated-by-user` | 55.15 min | 0.9192 |

**GH #454 (乙2) 结清**:四台**全部** by-user ⇒ **零容量回收**;均值 **`0.8744 h/台`**,阈值 `1.00` ⇒ **不触发**。
⚠️ 量具限定:止时刻取 SIR `Status.UpdateTime`(SIR 关闭戳,略晚于实际终止)⇒ **偏大**,
失效方向朝「触发」那侧不朝「放过」那侧 ⇒ 不改变结论。
**(乙1) 仍欠**,欠的是账单侧 cutoff 不是测量(W68+W69 都未落账);机时侧四台合计 `3.4978`,
远低于 `$1.10` 对应的用量 ⇒ **W69 不构成对该常数的挑战**。

**闸 (iv) 回填后现跑 `RECLAIM_BLIND_EXIT=0`(首跑,未经任何重试)**,逐字:
`yield : 4 paired seed(s) of 4 machine(s)` / **`attribution: 0 machine(s) reclaimed before the flip`**
(⭐ **确定值不是区间** —— 对比上一轮补齐前只能给 `0..1 … a RANGE`)/ `VERDICT: not blinded` /
**`NEXT WAVE: spot (the default; GH #158 unchanged).`**

## 六、⭐⭐⭐ 本轮头号产出:胜负通道量的是**地图**不是**臂**(零成本,全出自本波自己的语料)

**事实 1 —— 水平**:`winrate_side_census` = **radiant 3 / dire 221**(224 局)。

**事实 2 —— 它不是臂效应(决定性的一步)**。按戳把每局归到「候选臂打哪一侧」的腿上:

| 候选臂所在侧 | n | dire 赢 | **dire 胜率** | 候选臂自己的胜率 |
|---|---|---|---|---|
| radiant | 138 | 137 | **0.9928** | 0.0072 |
| dire | 62 | 61 | **0.9839** | **0.9839** |

⇒ **dire 胜率在两条腿上几乎相同(99.3% vs 98.4%),而候选臂自己的胜率从 0.7% 跳到 98.4%,
唯一变的是它被分到哪一侧。** ⇒ 这个通道测的是地图,不是臂。

**事实 3 —— 它不是一次抽坏了的阵容**。四粒种子 = **四套完全不同的 5v5**(程序化核过:
每粒种子的 radiant 与 dire 阵容各自**只有 1 种**,且四粒之间互不相同),而 dire 拿下
**49/50、49/50、50/50、50/50**。要用「阵容不平衡」解释,得让四次独立抽签**连续四次**都抽到更强的 dire 五人。

> ⚠️ **本轮自己踩过并当场纠正的一个前提(留档不删)**:初稿把「镜像对局」读成了
> **同一局里两队用同样十个英雄**,并据此写了一句「唯一的不对称只有地图侧」。**这是错的** ——
> 镜像发生在 **ab 腿与 ba 腿之间**(同十个英雄、同侧别分配,只交换 armed 的那一侧),
> 一局之内两队英雄本来就不同。**发表前核过才发现**:200 局里 radiant/dire 阵容相同的有 **0 局**。
> 更正后结论**没有变弱而是变强了**(四套独立阵容,而不是一套对称阵容)。

**事实 4 —— 目标物侧的形状,直接给录像组瞄点**:radiant 每局丢 **6.20** 座塔、dire **4.47**;
首塔中位数 radiant **512s** vs dire **672s** ⇒ **radiant 早约 2.7 分钟丢掉第一座塔**。
⇒ 故事**在对线期就开始了**,不是高地才崩。

**事实 5 —— 经济与胜负脱钩**:`econ_winner == winner` 仅 **122/224 = 0.5446**;
radiant 在 **105** 局里是经济领先方,其中只赢了 **3** 局(**2.9%**)。
⇒ **多打的钱换不成胜势**,与硬知识「Turbo 经济是 kill/push 驱动、低 CS 是症状不是杠杆」同向。

**事实 6 —— 纵向十波,方向从未反过**(全部取自各波 `*_verdict.json:winrate_side_census`,未手算):

| 波 | radiant | dire | 少数侧占比 |
|---|---|---|---|
| W55 | 12 | 174 | 0.0645 |
| W58 | 18 | 98 | 0.1552 |
| W62 | 12 | 215 | 0.0529 |
| W63 | 1 | 175 | 0.0057 |
| W64 | 1 | 221 | 0.0045 |
| W65 | 2 | 220 | 0.0090 |
| W66 | 0 | 52 | 0.0000 |
| W67 | 2 | 214 | 0.0093 |
| W68 | 7 | 161 | 0.0417 |
| **W69** | **3** | **221** | **0.0134** |
| **合计** | **58** | **1751** | **radiant 胜率 0.0321** |

⇒ **十波全 DEGENERATE,且少数侧每一次都是 radiant** —— 不是围着中点的抖动。

**这对规则的含义(本节的落点)**:铁律 2(b)「批测显示对胜负没有明显负面影响」的**胜负半边
连续十波结构性缺席**,此前只知道「它坏了」;**本轮给出了机制** —— 通道被一个约 97% 的侧别效应
饱和,臂效应在它下面不可见。⛔ **本台不代裁**规则该怎么改(那是总监/owner 的 GH #352 与本轮交棒 ①)。

⛔⛔ **三条诚实边界**:
1. **本轮无逐帧证据 ⇒ 不主张成因**。「bot 打 radiant 确实更差」与「harness 侧的产物(侧别分配、
   出生点、某个 mode 脚本的侧别假设)」**本轮都没有被排除**,两者对项目的含义完全不同。
2. 十波合计池**混了不同臂串、时间不等距** ⇒ 只主张**水平**,不主张**趋势**。
3. `winner_by {"engine_natural": 224}` + `winrate_independent_of_gold 224/224`
   ⇒ **224 局全部自然结束(遗迹被推掉)**,**不是** cap-25 判分口径的产物。

## 七、泄漏:三条独立路径全零

(1) `check_costs.sh` 的 `running/pending` 节 **空**;
(2) `describe-instances` **不加 tag 过滤**、状态 `pending,running,stopping,stopped` ⇒ **`[]`**;
(3) `describe-volumes` ⇒ **`[]`**(与零回收、四台全自毁一致,零孤儿卷)。
另有闸 (iii) 自带账户级普查 `accrual scope : 17 region(s) read <- COMPLETE` +
`CERTIFIED ZERO (0 accruing instances account-wide)` ⇒ **17 区账户级零**。
常驻仍只有 AMI `ami-0a990a26d89c66547`。⛔ 四条路径**不合并成一句**。

## 八、自检(铁律 10)

⚠️ **管道坑第 19 次,开工第一条命令又踩** —— 脚本自卫当场拒绝(逐字
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.`
+ `stdout is a PIPE, so the exit code your harness reports is the READER's`),
⭐ **零损失,但连续 19 轮同一位置** ⇒ 防的是症状不是原因。
改 `rc.sh` + 重定向 + 后台重跑,**到收尾仍未跑完** ⇒ ⚠️ **本轮无 `SELFCHECK_EXIT=`,
没读到的退出码不写,也不据此推断 trunk 是绿的**。
⛔ **本会话 `bots`/`game`/`tests`/`tools` 一行未改** ⇒ 没看过的腿里不可能有本轮引入的红。

## 九、铁律 6 三条腿

见文末「补记」节(push 时由钩子自己打的权威读数,⛔ 不预写)。

## 十、交棒

1. ⭐⭐⭐ **owner —— 预算刹车已触发**(§二):projected `$90.058` > brake `$90.00`,超 **`$0.058`**,
   工具逐字 `Stop and report to the owner; do not wait it out`。**九月剩余 18 天,本台在 owner 表态前不发任何波。**
   并排的第二个数:`budget filters : none`,而外来机型占本月 MTD **≈47.8%(`$37.4355`)** ⇒
   **这条线有近一半不是本台花的**。三条路仍是 GH #721 的三条,**本台不自行选**。
2. ⭐⭐⭐ **总监/owner —— 胜负通道量的是地图不是臂**(§六):这是 GH #352 缺的那个**机制**。
   请裁 (a) 2(b) 的胜负半边在通道恢复前怎么写才让字面与执行一致;
   (b) `≥0.20` 还是不是可达的线 —— 十波 1809 局,radiant 胜率 **3.2%**,最好的一波也只有 0.155。
3. ⭐⭐⭐ **录像组 —— 瞄点比上一轮窄得多**(§六 事实 4/5):
   radiant **早 2.7 分钟**丢首塔、每局多丢 **1.7** 座塔,且在 105 局经济领先里只赢 3 局。
   建议**逐帧看一局 radiant 方经济领先却自然落败的局**(本轮语料现成,4 个 run 已在 S3),
   优先看**对线期到首塔**这一段,而不是高地。**首要问题:这是 bot 的 radiant 打法问题,
   还是 harness 的侧别产物?** —— 本台无逐帧证据,**没有排除后者**。
4. ⭐⭐ **总监 —— W69 的读数不在「噪声」那一类**(§四):四通道 0/4 全负、
   `|mean|/SE` 4.58–6.64、两层无反号。⛔ 本台不裁,也**不能**指认 27 个 id 里的哪一个
   (bundle,`lanefix` 同形)。若要拆,需要的是隔离波,而那要钱 —— 见交棒 ①。
5. ⭐ **GH #454 (乙2) 结清**(§五),(乙1) 仍欠账单侧 cutoff。
6. ⚠️ **自检管道坑第 19 次**,且本轮无 `SELFCHECK_EXIT=`;
   `routine_selfcheck.sh` 在本容器**连 25 min 都跑不完**(与上一轮同)。

## 十一、下一轮本台

1. ⛔ **在 owner 对交棒 ① 表态之前,不发任何波** —— 围栏只随 MTD 单调收紧、不会自愈,
   `do not wait it out` 不是「下轮再试一次同一道红闸」。每轮仍做:成本现跑 + 泄漏 + 收割若欠。
2. ⛔ **本报告的 `$86.758` / cutoff `2026-09-12T00:50:45Z` / `pending $2.200` / `projected $90.058`
   全部作废**,一律当轮现跑。
3. 闸 (i) 名义解锁 **`2026-09-12T15:26:31Z`**,⛔ 别抄,跑 `wave_throttle.py`
   (本轮 `THROTTLE_EXIT=3`,`margin : 11287s SHORT`)。
4. **收割零欠**:W69 已由本轮全量收割(4 粒 / 224 局 / `machines[]` 零 null),无待收波次。
5. ⚠️ 开工第一条命令**重定向 + 后台 + 不设短 `timeout`**(**第 20 次**),给自检 ≥1800s 或分腿跑。
6. ⚠️ `origin/main` 的 python/Lua 红下轮会再撞一次,⛔ **不要读成本台引入的红**。

---

## Token 用量

见文末补记(收尾时现跑 `tools/agent/token_usage.py`)。

---

## 补记(第一次 push 之后,GH #290 顺序)

### (A) 铁律 6 三条腿(push 时钩子自己打的权威读数,两次 push 各一遍)

- `luacheck bots game: 0 warnings` ⇒ **`GATE_EXIT=0  CLEAN (iron rule 6 static half passed)`**
- **`py gate: 96 ran, 0 findings, 0 uncertifiable, 28.1s`**(第二次 `28.2s`)
- **`lua gate: SKIPPED BY SCOPE -- this push touches no bots/game/tests path.`**
  ⚠️ 第三条腿按钩子自己的措辞登记成**作用域决定**,⛔ **不是绿也不是「有话要说却被跳过」**;
  本轮确实一行 `bots`/`game`/`tests` 未动。
- py 腿另打 `12 new test(s) not in the manifest were run anyway`(全绿)。
- ⛔ **未用 `RULE6_BYPASS`**,无「SKIPPED, not passed」行;`ARM_EXIT=0`。
- 动态那半(~100min,GH #124)**不在钩子里,本轮不声称**。
- `PUSH1_EXIT=0` / `PUSH2_EXIT=0`(`18ef3934..92c7e11b  HEAD -> main`)。

### (B) 自检真码到手 —— 取代 §八「本轮无 `SELFCHECK_EXIT=`」

后台那次跑完了,**`SELFCHECK_EXIT=3`**(经 `rc.sh` 取裸码;⚠️ harness 报的 `[exited with code 0]`
是**读者的**码,`RC_EXIT=3` 才是自检自己的 —— 这正是本轮第 19 次踩到的那条管道纪律的另一面)。
逐字:`legs run : 12` / `FINDINGS (exit 3) : cadence queue-rulings owed-executions trunk-red(python) trunk-red(lua)` /
**`UNCERTIFIABLE (exit 2): none`** ⇒ ⭐ **12 条腿全部跑到了,没有「没人看过」的一侧**(与最近多轮不同)。

**trunk 红两侧**(GH #624/#705/#751/#774 一族,⛔ **本台不改、不立新案**):
- python 腿:`TRUNK RED -- a python test is failing ON THE WORKING TREE.`
- Lua 快腿:**`RED test_stayfield2_marginal_domain.lua`**(`1 of 87`),逐字
  `tests/test_stayfield2_marginal_domain.lua:591: the negative-natural-bid count moved from 56 … COUNT frames=1031 S=24 T=13 ST=5 margin=19`
  —— 又一例 census 断言「集合恰好等于已读过的集合」被别组的正当改动顶红。

⚠️ **照抄该腿自己的限定**:`Whether main is red too is NOT established by this line: re-run after 'git stash'.`
⛔ 本轮**未**用 `git stash` 独立复验 main。**但本会话对仓库的唯一改动是 `iterations/` 下的报告/章程/波次 JSON**
(`bots`/`game`/`tests`/`tools` 一行未改)⇒ **无一条红可能由本轮引入**;`相关不是归因`,交归属方。
⭐ 同时登记一条对照:**push 钩子的三条腿全绿放行**,而自检在同一棵树上读到两侧红 ⇒ **GH #774 又一个直接证据**
(红都落在钩子的快域之外)。

### (C) 本轮开的两条 issue(⛔ 按 GH #290 **push 之后**才发)

两份草稿各过 `claim_precheck.sh`,**`PRECHECK_EXIT=0`**,逐字 `local commits not on origin/main: 0`:
- `issue_side.md` ⇒ `clean` / **`OK to publish: every citation resolves on origin/main.`**(`paths cited 1  resolved on trunk 1  refused 0`)
- `issue_brake.md` ⇒ `NO CITATIONS -- nothing to certify, nothing to strand.  OK to publish.`
  ⚠️ 该草稿**零引用**,工具对「零引用」本身先打了一行 `REFUSE zero citations extracted`
  再落到 `OK to publish`(`'nothing bad' 和 'nothing matched' 是同一个输出`)—— **登记原样,不读成「引用都核过了」**。

| issue | 标题 | 承接 |
|---|---|---|
| **GH #779** `[batch]` | 预算刹车已触发:projected `$90.058` > brake `$90.00` | 交棒 ① |
| **GH #780** `[bug]` | 胜负通道量的是地图不是臂 —— GH #352 缺的那个机制 | 交棒 ②③ |

⛔ **未开重复 issue**:GH #352(通道 DEGENERATE)、GH #721(外来支出三条路)、GH #454/#375/#624/#774
本轮都只是**读数登记**,不重复立案。

### (D) Token 用量

`TOKENS total_in=10,012,894 out=58,345 turns=70`
