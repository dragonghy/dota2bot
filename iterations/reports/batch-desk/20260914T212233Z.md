# 批测台 2026-09-14T21:22:33Z — 刹车第二十轮持有;本轮第一次给空转支出归了因

**一句话**:MTD `$90.373`(越 `$90` 刹车线),**零发波、零收割欠、零泄漏,`bots`/`game` 一行未改**;
本轮的实质工作单元是花 `$0.01` 拉逐日 × 逐服务 CE,**把「零实例却还在涨」归了因 —— 烧的是 S3 不是 EC2**,
并顺带**当轮自我更正**了推给 owner 的一个作废的拍板问题(`dem21/` 的 lifecycle 本来就开着)。

---

## 一、成本(当轮现跑,⛔ 下轮不得抄作前提)

- `AWS_SETUP_EXIT=0`。容器无 CLI,自装 `aws-cli/1.46.1 Python/3.11.15 botocore/1.43.62`;
  身份逐字 `AWS ready: arn:aws:iam::581436534924:user/dota2bot-agent` + `(restricted batch-runner user — correct)`。
- `COST_EXIT=0`(⛔ **未走管道**,重定向后裸读)。
- MTD **`$90.373`** / `forecast 196.979` / `budget limit 100.0` /
  `budget refreshed 2026-09-14T20:27:33Z`;**headroom 对 `$90` = `$-0.373`**。
- 探针行逐字:`accrual probe (GH #779): alpha = 0 accruing instance(s) account-wide; beta = 0 wave record(s) after 2026-09-14T09:09:33Z`
  + `stamp unknown (fresh container) -- alpha and beta are both zero -- a frozen stamp over an idle account opens no blind spot`。

**GH #801 四行整块照抄**(与 `RULE6_BYPASS` 同规格;⛔ **未用 `COST_CONFIRM_AT=999` 自救,用默认值跑**):

```
   >= $35, but headroom to the $90.00 brake is $-0.373
   < $1.10 (cheapest wave) — CE confirmation SKIPPED, NOT passed (GH #801).
   Nothing can launch at this MTD, so confirming it buys nothing.
   Resumes by itself as soon as headroom >= $1.10.
```

⚠️ **这是 SKIP 不是 pass** ⇒ `check_costs.sh` 那条路径上的 MTD 未经 CE 复核。
(⭐ **但本轮 MTD 事实上被 CE 独立对上了** —— 见 §三,那是另一条路径、另一个理由付的钱,
逐日合计 `$90.3726` 与 budgets 的 `$90.373` **逐位相同**。⛔ 不把这当成 #801 的自动复核已发生。)

**通知判据:本轮推。** 两条**同时成立**:戳 `15:57:27Z` → **`20:27:33Z`**(换了张)**且**
MTD `$90.005` → **`$90.373`**(+`$0.368` / 4.5h)⇒ 增量比对有证明力。
已推 owner 手机/邮箱 **两条**(第二条是当轮自我更正,见 §四),并发 GH #779 第十二条评论
(`issues/779#issuecomment-5670989166`)。

---

## 二、闸 (iii) —— `wave_fence.py --planned 1.10`

`FENCE_EXIT=3`。⛔ 未传任何 `--no-accrual-check` / `--pending` / `--director-crossing`
⇒ **本轮无一行「这是 SKIP 不是 pass」**。

裁决三行逐字:

```
projected total  : $91.473
operative ceiling: $90.00   = min(fence, brake)
A launch at this instant would put MTD past the $90.00 BRAKE line.
WAVE_FENCE: THROTTLED (exit 3) -- brake, not fence. Stop and report to the owner; do not wait it out.
```

**RULING 5 / 6 / 7 三条结清判据行,各自照抄**(⛔ 只抄 `accrual check :` 不算):

```
accrual scope    : 17 region(s) read [ap-northeast-1, ap-northeast-2, ap-northeast-3, ap-south-1, ap-southeast-1, ap-southeast-2  +11 more]  <- COMPLETE: every region this account has enabled
wave accrual     : records after 2026-09-14T09:09:33Z (= 2026-09-14T20:27:33Z - 11.3h lag, clock from budget snapshot)
accrual check    : CERTIFIED (0 accruing instances account-wide, read this run) -- pending $0.000 is a reading, not a default.
```

⭐ **钟没有降级**(`clock from budget snapshot`,不是 fallback 措辞)⇒ RULING 7 (甲) 的
epoch 修法在本账号上**本轮仍然有效**,那扇窗确实锚在预算快照上。
另:`crossing registry: 0 record(s) ... none in force`,`1 retired record(s)`(GH#754/RULING 15)——
逐字自带「`none in force` 不是 `none ever issued`」。
`budget filters : none <- so every instance in the account does land in this budget, whoever owns it`。

三条告警状态照登:`$50.00 crossed [ALARM]` / `$80.00 crossed [ALARM]` / `$100.00 NOT yet crossed [OK]`。
⚠️ **照登不藏**:`$90`–`$100` 之间在 AWS 侧**仍可花**,**挡住它的是 owner 这条线不是 AWS**。

---

## 三、⭐⭐ 本轮实质工作单元:花 `$0.01` 把「空转还在涨」归了因

### 3.1 为什么本轮付这 `$0.01`,而上一轮明确没付

⛔ **这不是推翻 GH #801。** #801 掐掉的是 `check_costs.sh` 在 headroom < `$1.10` 时
**对发波决策的自动复核** —— 理由是「一波都发不了 ⇒ 没有可确认的发波决策」,那条理由本轮**仍然成立**,
所以那条路径照旧打 SKIP(§一)。**本轮付钱买的是另一个问题:归因。**
章程步骤 2 自己留的口子逐字是「要逐日/逐服务拆解时显式 `--ce`」,而立案事实是新的:

- 连续两轮在**零实例**的账户上读到**非零**增量:+`$0.147` / 5.05h(09-14T15:57Z),
  再 +`$0.368` / 4.5h(本轮);
- `forecast 196.979` 对着 `$100` 硬线;
- 上一轮明写「本轮未做 MTD 增量归因(要花 `$0.01` 逐日 CE,而 headroom 已为负)」⇒ **已经推迟过一轮**。

**一次请求,`$0.01`**(DAILY × GroupBy=SERVICE 同在一次请求里,⛔ 没有分两次买)。

### 3.2 读数(`ce get-cost-and-usage`,09-01 → 09-15,DAILY,GroupBy=SERVICE)

| 日期 | 合计 | EC2-Compute | S3 | EC2-Other | CE | Route53 | VPC |
|---|---|---|---|---|---|---|---|
| 09-05 | 26.3977 | 25.4860 | 0.2121 | 0.5170 | 0.0400 | 0.0006 | 0.1421 |
| 09-06 | 18.4114 | 17.6808 | 0.1670 | 0.3625 | 0.1200 | 0.0006 | 0.0806 |
| 09-09 | 6.7158 | 5.0823 | 1.1328 | 0.3114 | 0.1400 | 0.0006 | 0.0487 |
| 09-10 | 2.0879 | 0.5989 | 1.1575 | 0.1871 | 0.1300 | 0.0005 | 0.0138 |
| 09-11 | 6.8677 | 5.1105 | 1.2606 | 0.3550 | 0.0800 | 0.0006 | 0.0610 |
| 09-12 | 2.5014 | 1.5777 | 0.5508 | 0.2506 | 0.0900 | 0.0006 | 0.0317 |
| **09-13** | **0.9185** | **—(0)** | **0.6599** | 0.1378 | 0.1200 | 0.0007 | — |
| **09-14**(20.5h) | **0.3997** | **—(0)** | **0.3893** | — | 0.0100 | 0.0004 | — |

**本月累计(逐服务)**:
`Amazon Elastic Compute Cloud - Compute` **76.7465** ·
`Amazon Simple Storage Service` **6.7664** ·
`EC2 - Other` **4.1154** ·
`AWS Cost Explorer` **1.0300** ·
`Amazon Route 53` **1.0087** ·
`Amazon Virtual Private Cloud` **0.7055** ⇒ **`90.3726`**。
⭐ 与 budgets 的 `$90.373` **逐位对上**(两条独立来源同值)。

### 3.3 三条结论,逐条带限度

1. ⭐⭐ **09-13 与 09-14 两天 `EC2 - Compute` 逐字为 0** ⇒ **没有漏掉的机器**。
   **这条比 `describe-instances` 强,而且强在 RULING 6 点名的那个地方**:
   RULING 6 立案句是「一台自终止的机器,对 `describe-instances` 与 ActualSpend **同时不可见**
   —— 钱先发生、机器先消失、账最后到」,所以波间普查对**本台自己的花费**从来什么都没证。
   **但滞后带过去两天之后的逐日 CE 看得见** ⇒ 本读数**独立于普查**地确认了
   「把 MTD 推过 `$90` 的不是本台的波」(末台 EC2 是 W69,`2026-09-12T09:26:31Z`,
   而 09-12 的 EC2-Compute `$1.5777` 正是它,09-13 起归零)。
   ⚠️ **限度**:这只对**滞后带已经过去**的日子成立;它**不能**用来给今天的波背书,
   那正是 RULING 6 要防的形状。⛔ 不把这条推广成「普查可以不跑了」。
2. ⭐ **空转期的支出 97.4% 是 S3**(09-14:`$0.3893 / $0.3997`)。
   S3 日费轨迹:月初 `~$0.11/天` → 09-09~09-11 `~$1.2/天` → 现在 `~$0.4-0.66/天`。
3. ⚠️⚠️ **但「S3 = 存量」这一步没有证据,⛔ 不下这个结论。**
   全桶现测 **184.51 GB / 81,897 对象**(`s3 ls --recursive --summarize`,
   逐字 `Total Objects: 81897` / `Total Size: 184510458615`),
   按 S3 Standard `$0.023/GB-月` 折算**纯存储只有 `~$4.24/月 = ~$0.141/天`**,
   而实测 S3 是 `$0.39-0.66/天` ⇒ **存储解释不了其中的三分之二到四分之三**。
   剩下那部分是请求数 / 传输 / 存储类别 / 别的,**这次读数说不了,⛔ 不猜**。
   (要拆就得再买一次 `GroupBy=USAGE_TYPE` 的 CE;**本轮不买** —— 它买不到任何本轮要做的决定,
   而 headroom 已为负。**下一轮若 owner 要削 S3,这是第一笔该花的 `$0.01`。**)

   ⭐ **一条本来会朝危险侧失效、本轮被关掉的限度**:`s3 ls` 只列**当前版本**,
   若开了版本控制,历史版本在计费却**不出现在这个字节数里** ——
   失效方向是「看起来比实际便宜」,与「shallow clone 的空输出被读成无漂移」同族。
   现测 `get-bucket-versioning`(`VER_EXIT=0`)**返回空** ⇒ 版本控制从未开启
   ⇒ **这条限度是被关掉的,不是被忽略的**。

### 3.4 前缀分布(完整列举,⛔ 非抽样)

| 前缀 | 大小 | 对象数 | 占字节 |
|---|---|---|---|
| `dem21/` | **134.51 GB** | 6,756 | **72.9%** |
| `replays/` | 27.84 GB | 1,987 | 15.1% |
| `soak/` | 13.52 GB | 72,079 | 7.3% |
| `run_20260905_151656_main_vs_upstream-baseline/` | 8.53 GB | 161 | 4.6% |
| `tools/` `behavioral/` `validation/` `rs/` `calibration/` 等 | < 0.1 GB | 914 | < 0.1% |

⚠️ 顺带更正一处**本章程散文里的数**:上一轮 §三乙 把 `dem21/` 写成「103 份 `.dem`」,
现测是 **6,756 个对象 / 134.51 GB**。(103 大概是 `.dem` 本身的份数,而该前缀下还有
每局的 `.analysis.json` / `.demclaim.json` / `.log.gz` 等伴生对象。⛔ 本台未逐类清点,
**不主张** 103 这个数错,只登记「前缀总量不是 103 个对象」。)

---

## 四、⛔ 一条本台**没有**做、并且建议**谁都不要**做的事(当轮自我更正,方向对本台不利,照登)

推给 owner 的**第一条**通知里,本台把「要不要提前删 `dem21/` 省日费」写成了一个**需要 owner 拍板的问题**。
**那个问题是作废的。** 当轮现测 bucket lifecycle 配置(`LC_EXIT=0`),**已经有一条开着的规则**:

```
ID=dem21-expire-21d   Filter.Prefix=dem21/   Expiration.Days=21   Status=Enabled
```

⇒ 那 134.51 GB **会在 ~2026-09-22 自己过期**,与本台上一轮 §三乙 记的 09-22 日期**吻合**
⇒ **不需要任何人拍板**;提前手删只买到约 7 天 × `~$0.10/天` ≈ **`$0.7`**,**却是不可逆操作**
⇒ ⛔ **不做、不建议、也已把这个问题从 owner 面前收回**(第二条推送)。

⭐ **为什么要专门写这一节而不是悄悄改掉**:第一条推送已经发到 owner 手机上了,
而它请求的是一个**不可逆动作**的授权。**一个基于本台没查清的前提发出去的授权请求,
如果 owner 回了「删吧」,不可逆的那一步就是本台的错误买的。**
⇒ 发现即当轮推第二条更正,⛔ 不等下一轮、⛔ 不只写进报告。
**本台未触碰任何 S3 数据。**

---

## 五、收割 / 泄漏(四条独立路径 + 本轮新增第五条,⛔ 不合并成一句)

**收割:零欠。** `S3VAL_EXIT=0`,`validation/` **592 对象**,与上一轮基线**逐位相同**;
最新对象仍是 `tpcommit+46ids-24c30245be01_20260907_2218_run.log`(`2026-09-07 22:18:52`)
⇒ 零新对象、零下载,`recover_verdict.py` 本轮**未跑**(**没有可收的东西,不是漏收**)。
`queue.json` 无可发请求(闸 (iii) `exit 3`,发不出)。

**泄漏:五条独立路径全零。**
① `check_costs.sh` 自带 `running/pending` 区块 **空**;
② `describe-instances` 五态(`pending,running,shutting-down,stopping,stopped`)
**不加 tag 过滤** ⇒ **零行**(`INST_EXIT=0`,`LINES=0`);
③ 闸 (iii) 账户级普查 `CERTIFIED (0 accruing instances account-wide)`,`17 region(s) <- COMPLETE`;
④ AMI 仍只 `ami-0a990a26d89c66547`(`dota2bot-batch-20260718`)一张 —— **常设成本,非泄漏**;
⑤ ⭐ **本轮新增账单侧**:09-13 / 09-14 两天 `EC2 - Compute` = 0(§三.1)——
这一条**独立于 ①②③**,因为那三条读的都是「现在有没有机器」,而它读的是「过去两天有没有花过机器的钱」。

---

## 六、给 owner 的那条(⛔ 本台无权自裁,已推送 + 已发 #779)

按 `$0.40-0.92/天` 的空转速率,9/30 的 MTD 落在 **`$96.0 – $104.3`**
(16 天 × 速率;`dem21/` 09-22 过期后省 `~$0.10/天` × ~8 天 ≈ `$0.8` 已扣进下界)
⇒ ⭐ **接下来 16 天一波不发,也未必守得住 `$100` 硬线**;
即使 owner 现在批准,余量也只够**约一波**(`$1.10`)就撞 `$100`。

⚠️ **诚实边界**:**n=2 个空转日**(09-13 全天 `$0.9185`、09-14 的 20.5h `$0.3997`),
两天本身差近 2 倍 ⇒ 区间宽是**真实的不确定性不是排版**;⛔ **不外推到十月**、
⛔ 不据此改任何常数、⛔ 不主张 `$100` 一定会/一定不会被越。

**需要 owner 表态的是**:是否提高预算线 / 是否接受本月剩余时间批测全停。⛔ 不是删录像(§四)。

---

## 七、铁律 9(owner 优先项)

`OWNER_PRIORITIES.md` **P4.1 upstream 标尺波:连续第十九轮欠。**
⛔ **唯一阻因是闸 (iii) `exit 3`(预算),不是本台未认领,也不是排期。**
常设运维项(Spot 优先)本轮无适用面 —— 零发波。

---

## 八、⛔ 本会话 `bots/` `game/` 一行未改。

---

## 九、开工自检与铁律 6

**⚠️ 管道坑第 31 次**:本轮**第一条命令**又走了管道
(`routine_selfcheck.sh 2>&1 | tail -60`),脚本**当场自卫拦下**,逐字:

```
REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.
```

⇒ **零损失,但拦住它的又一次是工具不是纪律**;第二跑改 `> /tmp/sc.log 2>&1` 裸读,
⭐ **本轮没有再踩超时坑**(⛔ 未带 `timeout`)—— 上一轮交棒 ⑦ 的「一次到位」**仍未做到**
(仍是第二跑才对),但**两道坑里踩了一道不是两道**,如实登记不辩解。
此后每个读退出码的命令一律重定向后裸读,零管道。

**自检读数(写作时 Lua 腿仍在跑,见文末补记)**:python 腿 **`131 passed, 1 failed, 3 uncertifiable`**;
`TRUNK RED -- a python test is failing ON THE WORKING TREE.`,其自带限定逐字
`Whether main is red too is NOT established by this line`。
⚠️ **本轮工作树 = 干净树**(未改 `bots`/`game`,报告文件尚未写入时读的),
但**本台未用 worktree 独立复跑**(上一轮已就同族两条红开过 GH #825 并在净树一手复现)
⇒ ⛔ **不主张这 1 failed 与上一轮的三条红是同一批**,也⛔ 不重开 issue;
⚠️ 上一轮 3 条红 / 本轮 1 failed + 3 uncertifiable,**数变了**,**归因交总监**。

**铁律 6 三条腿**:见文末补记(push 时现跑)。

**铁律 11**:MCP 本轮**可用**(#779 评论发出,`issuecomment-5670989166`),
零 `requires approval`、零空转(自检后台跑,成本/闸/收割/泄漏/CE 归因并行推进)。

---

## 十、交棒

1. ⭐⭐ **owner —— 刹车第二十轮持有,GH #779 第十五轮零表态**;本轮**有新事实**
   (§三 归因 + §六 的 `$96-104` 区间)⇒ 已推送两条。**球在 owner**:
   提高预算线,还是接受本月剩余时间批测全停。
2. ⭐ **总监 —— §三.1 那条读数请收**:逐日 CE 在滞后带过去之后**能**看见自终止机器的钱,
   ⇒ 它是 RULING 6 那个结构性盲区的**事后**补偿instrument。
   ⚠️ **本台不主张它能替代普查**(它对「今天这一波」无效,那正是 RULING 6 的域)——
   请总监裁:**要不要把「滞后带外的逐日 CE 归因」写成一条定期读数**,以及**谁付那 `$0.01`**。
3. **总监 —— §三.3 的 S3 缺口**:`~$0.14/天` 存储 vs `$0.39-0.66/天` 实测,差的那部分**未归因**。
   下一步是 `GroupBy=USAGE_TYPE` 的一次 CE(`$0.01`);⛔ 本台本轮未买(买不到本轮的决定)。
   **要不要买、什么时候买,请总监或 owner 点一句。**
4. **总监 —— §三.4 的前缀清点更正**(`dem21/` 不是 103 个对象而是 6,756 个 / 134.51 GB);
   ⛔ 本台不改 `owed_executions.json`(写入权归总监,先例 GH #33)。
   ⚠️ 上一轮交棒 ②(乙)那条 `hero27_dem21_deadline_scan` 的倒计时**仍然有效**:
   现测 lifecycle 规则确认 **09-22 自动删除**,距今 **8 天**。
5. **总监 —— 上一轮交棒 ①③④⑤⑥ 本轮未获回应,逐字保留**,⛔ 本台不自行退休任何一行。
6. **下一轮本台**:开工第一条命令 `nohup … > /tmp/sc.log 2>&1 &`(⛔ 无管道、⛔ 无 `timeout`,
   **一次到位**);刹车解除时(headroom ≥ `$1.10`)按规格发 P4.1 标尺波,
   并把 #801 那四行**恢复后**的样子抄进报告 —— 那才是它第一次走「该买」分支。

---

## 十一、Token 用量

`python3 tools/agent/token_usage.py`(`TOK_EXIT=0`),最后一行原样:

```
TOKENS total_in=3,667,466 out=35,373 turns=34
```

⚠️ 这是「到统计时刻为止」的数字,统计后的收尾回合(push + 补记)不计入。

---

## 补记(push 之后,GH #290 顺序)

### (A) 铁律 6 三条腿(三次 push 逐字一致)

```
luacheck bots game: 0 warnings
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 88 ran, 0 findings, 0 uncertifiable, 11.6s   (PUSH3 为 11.7s)
lua gate: SKIPPED BY SCOPE -- this push touches no bots/game/tests path.
```

`ARM_EXIT=0`(`push gate armed (core.hooksPath=.githooks, already set)`)。
⛔ **未用 `RULE6_BYPASS` ⇒ 本轮无「这是跳过不是通过」行。**
⚠️ **`lua gate` 那一行是作用域判定不是绿灯** —— 本轮只改 `iterations/`,
它**一个测试体都没执行**,⛔ 不记成「Lua 测试通过」。

### (B) ⚠️ push 竞态一次

`PUSH1_EXIT=0`(会话分支 `[new branch]`);`PUSH2_EXIT=1`,逐字
`! [rejected]  HEAD -> main (non-fast-forward)` ⇒ `git pull --rebase origin main`
(`REBASE_EXIT=0`,**无冲突**)后 `PUSH3_EXIT=0`(`d185747d..b49810b4`);
会话分支 `--force-with-lease` 对齐(`PUSH4_EXIT=0`)。
⭐ **落地独立复核不靠 push 回显**:`git merge-base --is-ancestor HEAD origin/main` ⇒ **真**。

### (C) ⭐ 自检跑完,同轮补上正文 §九 当时读不到的真码

正文 §九 写作时 Lua 腿仍在跑,只登记了 python 腿。**真码**:

```
selfcheck worst exit: 3
legs run              : 13
FINDINGS (exit 3)     : cadence queue-rulings owed-executions lua-coverage trunk-red(python)
UNCERTIFIABLE (exit 2): none
```

python 腿 `131 passed, 1 failed, 3 uncertifiable`;
Lua 快腿 `99 tagged detector file(s), 0 failures -- FAST SUBSET, not the full suite.`
⭐ `UNCERTIFIABLE (exit 2): none` ⇒ `trunk-red(python)` 是**真红不是没跑完**。
⚠️ ⛔ **未把 exit 3 归给任何单一条腿**(GH #267 那句防滥读提醒照抄)。

⭐ **比上一轮少了 `unlanded`**:上一轮那三条(协同组 `d92ba19`/`bb70c8d`/`862dfdb`,已开 GH #823)
本轮**不再被点名**。⚠️ ⛔ **本台不主张它们"已落地"** —— 该腿自带的三条限度(patch-id 只排除逐位相同的重做 /
改头换面的同一份工作分不开 / `shallow clone` 只覆盖 graft 点以上 3 天)意味着**「不再被点名」有多种成因**;
本轮**未逐条现跑 `merge-base` 复核**(上一轮跑了),⇒ **只登记读数变化,不改 #823 的状态。**

### (D) ⭐⭐ 正文 §九 那句「不主张与上一轮三条红是同一批」,现在**能**说准了 —— 同轮补,方向对本台不利照登

正文写作时只有计数(上一轮 3 failed / 本轮 1 failed)、没有名字,故本台当时**只敢**写
「数变了,归因交总监」。跑完后名字在 `PY_LOG` 里,**逐条对上**:

| 上一轮(18:07Z)三条红 | 本轮 | 处置 |
|---|---|---|
| (乙) `test_lua_corpus_stability.py` | ⛔ **仍 `FAIL`** | **同一条,已在 GH #825**,⛔ 不重开 |
| (甲) `test_call_arity_census.py` | **不在 `FAIL` 列表**(⇒ 在 131 passed 里) | ⚠️ **「变绿了」是读数,「被谁怎么弄绿的」不是** —— 该行要的本来就可能是一个 `ALLOWLIST`/`ROUTED` 裁决词而非改代码;⛔ **本台不主张它被修好**,也⛔ 不据此关 #825 的任何一半 |
| (丙) `test_selfcheck_lua_leg.py` | **`UNCERTIFIABLE`**(逐字 `did NOT run -- this is not a pass and not a failure`) | ⛔ **不是变绿** —— 它在案于 `owed_executions.json:selfcheck_lua_leg_4c2_red`(`executor=总监自己`),状态**未知不是已修** |

⇒ **本轮唯一的 trunk 红是一条已立案的旧红**,⛔ 无新红、⛔ 不新开 issue。
⚠️ 另两条 `UNCERTIFIABLE`(`test_lua_gate.py` / `test_luacheck_gate_soakswitch.py`)**不下判断**。
⭐ **闸仍然绿得对**:三次 push 逐字 `py gate: 88 ran, 0 findings`
⇒ `test_lua_corpus_stability.py` **不在那 88 条里** —— **GH #624/#616/#806 立案形状的又一例**
(缺陷在**成员资格**,不在闸的执行)。⛔ 本台不自行调整闸的成员资格。

### (E) 通知与评论

owner 手机/邮箱 **两条**(第二条是 §四 的当轮自我更正,收回一个作废的不可逆动作授权请求);
GH #779 第十二条评论 `issues/779#issuecomment-5668510897` 之后的新一条:
**`issues/779#issuecomment-5670989166`**。
**铁律 11**:MCP 本轮可用,零 `requires approval`、零空转。
