# 批测台 2026-09-03T12:15–12:5xZ —— 收割 W42(57-id 家族第二波)+ 按 GH #408 发按需补跑机

**本轮真正的产出有三件,第一件是本台自己漏掉的活:**

1. ⛔ **06:15Z 的 W41 收割轮从未把读数写回 `queue.json`。** 十三条搭 W41 的请求今天为止
   `status` 还是 `running`、`result_wave` 还是 **W40**、`result` 还是 W40 的散文。
   下游(协同组/英雄组/总监)读队列时看到的是**一波之前的数字挂在当前波的名下**,
   而没有任何门会举手 —— 章程步骤 3 那句「结果写进报告 **+ 更新 queue.json**」是纪律不是门。
   本轮把 W41、W42、并池三份读数一次补齐。
2. ⭐⭐⭐ **GH #435 的载体设计第一次被真事故考了,而且过了。** 3040 被回收,
   七个载体项**一个都没归零**(代码核过,不是目测)。W40 那次一个 `2c` 事件把三项打到 0,
   本波掉一台把三项打到 **1**。
3. ⭐⭐ **`winrate` 预登记分支 (a) 触发。** W42 是**只换种子**的干净复制检验
   (arm 串逐字节不变、树编译后逐条不变),通道**仍 DEGENERATE** ⇒
   **「多加种子」这条路按构造已死**,病灶是语料。

另有一条要交给总监的**成本常数漂移**(§7):活下来的三台是 **0.932 h/台**,
章程围栏用的常数是 **0.745**,**低 25%**。

---

## 1. 自检(铁律 10)

⭐ **第一条命令就把上一轮点名十七次的那个坑绕开了一半、又踩了一半**:
本轮开工第一条写的是 `… | tail -40`,自检**当场自拒**并打
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked`
(工具自报 `it has recurred 5x`)。改走重定向重跑。**登记为同形第十八次。**
⭐ 但**本轮它没有第二次咬人**:发波那条 `spot_run.sh` 用的是
`> /tmp/rerun3040.log 2>&1; echo "RERUN_EXIT=$?"`,**真码 `RERUN_EXIT=0`**
(上一轮正是在这里把 `tail` 的码读成了 `spot_run.sh` 的)。

**trunk 侧读数(python 腿,`/tmp/selfcheck_py_XnNGkD.log`)**:
`81 passed, 1 failed, 2 uncertifiable`。

- ⛔ **`test_detector_source_constants.py` 仍红 —— 第六轮原样复发**(GH #420 家族)。
  **未 stash 重跑 ⇒ 不声称 main 也红**(工具自己那行:`Whether main is red too is NOT
  established by this line`)。
- `UNCERTIFIABLE` 两条同前几轮(`test_rc_wrapper.py` / `test_selfcheck_lua_leg.py`);
  后者自报原因是**容器里没有 `lua5.1` 在 PATH 上**,那条腿证明不了 `rc.sh`。
- ⭐ `81 passed`(上一轮 80)⇒ 多了一条绿测试;**本台没查是哪一条、也没查它的域**,
  据实写到这里为止。

⭐ **九条腿全部跑完,真退出码 `EXIT=3`**(不经管道读的):
`legs run 9` / `FINDINGS (exit 3) = cadence trunk-red(python)` /
**`UNCERTIFIABLE (exit 2): none`** /
`NOT RUN (inside a leg): tests/test_rc_wrapper.py tests/test_selfcheck_lua_leg.py` /
`selfcheck worst exit: 3`。
⇒ **exit 3 是「有发现」不是「没跑成」;本轮没有「trunk 那一侧没人看过」的情况。**

⚠️ **一个值得登记的读码陷阱**:harness 给这条后台命令报的是 `exit code 0` ——
那是命令串里最后那个 `echo` 的码,**不是自检的**。真码要么读脚本自己打的
`selfcheck worst exit: 3`,要么读显式 `echo "EXIT=$?"` 的 **`EXIT=3`**。
**这与铁律里那条管道陷阱是同一族**:外层包装的成功掩盖了内层的码。

Lua 腿真跑了:**74 tagged detector file(s), 0 failures**(上一轮 73),
但工具自声明这是 **FAST SUBSET 非全套**,照此不夸大。`cadence` 有洞。

## 2. 成本与围栏

九月 MTD **`$7.056`**(免费 `budgets`;`refreshed 2026-09-03T04:41:39Z`)。
⚠️ **这张快照连续第二轮不翻新** —— 它**早于 W42 起飞(09:32Z)**,
所以 **W42 一分钱都不在这个数里**。`forecast 111.209` 不是读数,不进围栏。
MTD `< $35` ⇒ CE 未触发,**`$0.01` 没花**。

**围栏(章程 2 甲,保守:12h 内已发波次一律再计一次)**
= `7.056 + W42 spot 0.90 + 本轮按需补跑 0.63` = **`~$8.59`** ≤ **$80**
⇒ **不跨任何 owner 可见告警档**($50 / $80 / $100),**不欠解释行**。

进场实例普查:`state=pending,running` **空**(四台已自毁/被回收);
常驻只有 AMI `ami-0a990a26d89c66547`。

## 3. ⭐⭐ 死因码:SIR 这条路第一次在**窗口已经关掉之后**被真的用上

`describe-instances --filters state=terminated,shutting-down` 本轮**返回空** ——
四台的终止时刻是 `09:46–10:28Z`,读的时刻是 `12:2xZ`,**GH #427 那个 1h 窗口早已关闭**。
上一轮(W41,按需)正是在这里 0-for-3 地丢了死因码。

⭐ **但 W42 是 spot 波,spot 机有 SIR,而 SIR 活得比那个窗口长。**
`describe-spot-instance-requests` 四台**全部读到**:

| 种子 | 实例 | `Status.Code` | 终止时刻 | 存活 |
|---|---|---|---|---|
| 3040 | `i-08f6554de561a016b` | ⛔ **`instance-terminated-no-capacity`** | 09:46:31Z | **14.43 min** |
| 3101 | `i-0371d22b2345adff0` | `instance-terminated-by-user` | 10:28:22Z | 56.20 min |
| 3117 | `i-0d24250f341351b93` | `instance-terminated-by-user` | 10:27:17Z | 55.05 min |
| 3204 | `i-036f2c0ccba66ba56` | `instance-terminated-by-user` | 10:28:47Z | 56.48 min |

⇒ 三台是**正常自毁**(`by-user` = 看门狗/自毁脚本),**一台被容量回收**。
存活时长是 **EXACT**(SIR `Status.UpdateTime` − `machines[].launched_at`),
**不是 W41 那种 S3 下界**。四行连同 `ab/ba/arm_depth` 已按上一轮立的收割义务写回
`W42_wave.json:machines[]`。

⭐ **GH #427 的建议因此要多一句**:上一轮建议「(甲) cloudtrail / (乙) 关机钩子抓码」。
本轮的证据说**还有一条零成本的 (丙)**:**spot 波本来就不吃这个窗口**。
真正暴露在 #427 下的是**按需机**(它没有 SIR,唯一的码是 EC2 `StateReason.Code`)——
也就是说 **GH #271 每下一次「一波按需」的升级,就同时把那一波的死因码押在 1 小时上**。
本轮发的那台补跑机正是这个形状,已写进收割义务。

## 4. ⭐⭐ GH #271 的闸:本轮**第一手可答**,`RB_EXIT=0`

上一轮这道闸差点静默失效(`RB_EXIT=2`,要靠 S3 下界 recover)。本轮直接答得出来:

```
changeover 40.0 min  (measured bracket 34.80 < x <= 40.63, margin above the constant 0.63 min)
  seed 3040   14.4 min  instance-terminated-no-capacity  ab10 /ba0   NO-PAIR  [ab/ba only]
  seed 3101   56.2 min  instance-terminated-by-user      ab32 /ba24  PAIRED   [depth-checked]
  seed 3117   55.0 min  instance-terminated-by-user      ab41 /ba17  PAIRED   [depth-checked]
  seed 3204   56.5 min  instance-terminated-by-user      ab37 /ba21  PAIRED   [depth-checked]
yield      : 3 paired seed(s) of 4 machine(s)
attribution: 1 machine(s) reclaimed before the flip
VERDICT: not blinded -- the wave delivered 3 paired seed(s).
NEXT WAVE: spot (the default; GH #158 unchanged).
```

⇒ **W43 仍走 spot**,不触发按需升级。
3040 在**换腿点(40 min)之前**就被带走 ⇒ 单腿孤儿,正是工具文件头描述的那个形状。

⛔ **过程里踩到一个值得登记的键名坑**:本台第一次回填写的是
`ab_games` / `ba_games`,而 `reclaim_blind.py` 读的是 **`ab` / `ba`** ⇒
`RB_EXIT=2`,`UNDECIDABLE: seed 3040: missing leg count 'ab'`。
**这跟它本来要修的毛病是同一个形状**:闸答不出来时不会红,只会 **exit 2**,
而 exit 2 恰恰是最容易被读成「跑过了」的那个码。
`machines[]` 现在**两套键名都写**(`ab_games/ba_games` 给人读,`ab/ba` 给工具读),
并把这段登记进 `W42_wave.json:_harvest_key_note`。

## 5. 收割 W42(局数、深度、四量)

分 run 下载(**不扁平化**,GH #225),`.demclaim.json` sidecar **一起下**
(只下 `*.analysis.json` 会让 §6 的 `rec_slot_cost.py` 打 exit 2,而 exit 2 不是通过)。
armed 串**从波次记录取回**(`W42_wave.json:arm_string`,57 ids / 501 字节 /
md5 `38423b791c05d96e0e16ed0b51bd132d`),不从当时的 `test_set.md` 取。

`RV_EXIT=0`,`contrast = vs_stable`,`recovered_locally = true`,
`files_seen == games_loaded == 206`、`unparseable 0`、`pooled_basename_dirs_overridden 0`。

### 5.0 局数与深度

| 种子 | ab | ba | arm_depth | scored | 备注 |
|---|---|---|---|---|---|
| **3040** | 10 | **0** | **0.00** | ❌ | **`NO-PAIR`** — 14.4 分钟被容量回收,只跑出 ab 腿 |
| 3101 | 32 | 24 | 27.43 | ✅ | |
| 3117 | 41 | 17 | 24.03 | ✅ | |
| 3204 | 37 | 21 | 26.79 | ✅ | |

`scored_games` **172**、`games_loaded` 206、`unfinished_games` **0**、
`min_arm_depth` **8** ⇒ 三粒配对种子**全部过 GH #269 深度门**,
`thin_arm_seeds` **空**、`thin_arm_winrate_seeds` **空**。

⚠️ **被排除的 3040 仍在这里点名 —— 它的 10 局是真花了钱的**(章程明文要求)。
⚠️ `ab:ba` 又是约 2:1(32:24 / 41:17 / 37:21):换边按**经过时间**触发不按局数,
与 W39/W41 登记的是同一形状,**不是抢占**(3101/3117/3204 三台全正常自毁)。

### 5.1 四量均值(工具自打)

| 指标 | mean | comps_better |
|---|---|---|
| gpm | **−19.15** | 1/3 |
| xpm | **−7.80** | 1/3 |
| deaths | **+0.12**(正 = 更差) | 1/3 |
| last_hits | **−0.60** | 2/3 |

`suggested = hold_or_reject`。

### 5.2 分层(铁律 4(i-a):工具自打,**未手算**)

| 指标 | ab | ba | side | sign_flip | side_gt_arm |
|---|---|---|---|---|---|
| gpm | −35.99 | −2.32 | −16.84 | **false** | 2/3 |
| xpm | −17.65 | +2.06 | −9.86 | true | 2/3 |
| deaths | −0.69 | +0.92 | −0.80 | true | 2/3 |
| last_hits | +0.88 | −2.07 | +1.48 | true | 2/3 |

按 4(i-c):经济四量是**每粒 50/50 swap-average 的估计量**,侧偏已消除
⇒ **反号不是否决理由,是恒等式**(`反号 ⟺ |side| > |arm|`),**登记但不据此下结论**。
⭐ 值得单记一句:**gpm 本波不反号**(两腿同为负),而 W41 是四量全反号。

**本台不判读效应量**(判读归总监/协同组):逐粒 gpm 为 **−69.45 / −8.42 / +20.41**,
极差 **90**,仍大于 08-19 校准的镜像零点 σ≈30。

### 5.3 ⛔ 胜负通道:`DEGENERATE`,**而这一次它是预登记检验的答案**

工具自打的告警逐字抄录:

> `GH #352 winrate channel DEGENERATE: minority side won 5 of 206 finished games (share 0.0243 < 0.20). The winrate below is bounded to 0.5 +/- 0.0347 and MUST NOT be cited as rule 2(b) support until the channel recovers.`

```
winrate_channel              = DEGENERATE
winrate_side_census          = {radiant: 5, dire: 201}
winrate_minority_side_share  = 0.0243        (恢复判据是 >= 0.20)
mean.winrate                 = 0.49          <- 占位符,不是读数
mean.winrate_headroom        = 0.0347
comps_better.winrate         = 0/3           <- 占位符,不是读数
winner_by                    = {engine_natural: 206}
winrate_independent_of_gold  = 206/206 games
逐粒 headroom                = 0.1042 / 0.0 / 0.0
```

⭐ **`winrate_independent_of_gold` 本波打的是真值 `206/206`**,不是 W31 那个假的 `0/222`
⇒ GH #108/#352 那个桶名 bug(读者读 `engine`、真桶是 `engine_natural`)确认已修,
本台 08-29 立的**逐波复发登记就此停掉**。

⭐⭐⭐ **预登记分支 (a) 触发,而且这是本波存在的理由。**
`W42_wave.json:winrate_prereg` 在四个数落地之前写死:W42 **故意一条轴都不换** ——
同一条 arm 串(逐字节)、同一棵编译后的树、**只换种子**。结果通道**仍是 DEGENERATE**。
⇒ **「种子集」和「种子数」这两条轴按构造被排除**,病灶是**语料**
(物理侧横扫 + `winner_by = engine_natural`)。分支 (b)(单靠换种子就 RECOVERED)**没有触发**
⇒ 本台上一轮那条 4.3 读数**不需要更正**。
GH #352 的「改测法 **且** 修横扫」现在**站在两个互相独立的种子集上**。

## 6. 三件预登记的兑现与一件门

### 6.1 ⭐⭐⭐ GH #435:载体设计**第一次被真事故考了,过了**

上一轮预登记的是「掉任何一台,每一项都还剩 ≥1 载体」。本轮 3040 真的掉了 ——
**逐项核过(代码算的,不是目测)**:

| 项 | 发波时载体 | 实际交货 | 结局 |
|---|---|---|---|
| zuus | 4 席 | {3101,3117,3204} | 3 |
| crystal_maiden | 3 席 | {3101,3117,3204} | 3 |
| chaos_knight | {3101,3117} | {3101,3117} | 2 |
| obsidian_destroyer | {3117,3204} | {3117,3204} | 2 |
| **spirit_breaker** | {**3040**,3101} | {3101} | **1** |
| **lion** | {**3040**,3117} | {3117} | **1** |
| **skeleton_king** | {**3040**,3204} | {3204} | **1** |

**归零的项:0。** 对照 W40:一次 `2c` 事件把 `crystal_maiden`/`obsidian_destroyer`/
`skeleton_king` 三项直接打到 **0**。**「五个 2-载体项坐在五对互不相同的种子上」这个设计
今天买到了它声称要买的东西。**
⚠️ **诚实边界一字不改**:这个保护**恰好只深一层** —— 六种两台同掉里有五种仍会把至少一项打到 0。
**一次成功不构成「修好了」**,不要据此关 #435。

### 6.2 GH #432:本波**未复发**,但这个否定和上次一样弱

四个前缀里 `analysis.json` 计数 `16 / 62 / 64 / 64`,与精确 glob 一致,无跨 run 副本。
⚠️ 但 **3040 那台被抢占时只交了 16 份**,而 #432 的机制要「交完再被抢占」——
**本波没有测试 #432,只是没触发它。**

### 6.3 `rec_slot_cost.py` 第三次非循环跑:**`RSC_EXIT=0`,过**

206 局(录制 102 / 对照 104),8 个对照槽,`recorder set [1..8]`,`script_version` 8 distinct。
对照槽箱体因子 `0.993`,**16 个槽逐个比 baseline,`0 recording slot(s) beyond tolerance`**。
录制槽相对自身槽位趋势的残差全在 **±0.7%** 内(最大 +0.6% / −0.7%)。
⭐ **本轮把 `.demclaim.json` sidecar 一起下了**,上一轮那个 exit 2 的坑没有再踩。

### 6.4 `ckpush` / `roshdist` 的 (a):仍然 **UNINTERPRETABLE**(本轮**重查过,不是沿用**)

逐局读 `analysis.json` 的顶层键集合(206 份):
`[anomalies, anomaly_count, avg_gpm, cap_min, duration_min, duration_s, econ_winner,
effective_timescale, log, mode, natural_end, players, script_version, team_gold, towers,
wall_s, winner, winner_by]` —— **没有任何帧级检测器字段**,与 W41 一致。
⇒ SS-DT.4 / SS-DP.8 **是「没有量具」,不是「测了没效果」**,判读一律按前者。

### 6.5 零 pudge:**连续第七波**

本轮**不按打印出来的阵容 grep**,而是逐局读 `players` 字段:
206 局里 `npc_dota_hero_pudge` 出现 **0** 次(26 个不同英雄,7 个载体项全部在场:
zuus 206 / crystal_maiden 190 / obsidian_destroyer 128 / chaos_knight 126 /
lion 80 / skeleton_king 80 / spirit_breaker 78)。
⇒ `queue strategy-26` / `rotscope` **写成「无载体(NO CARRIER)」,不许写成负向读数**。

## 7. ⚠️⚠️ 交总监:围栏用的机时常数漂了 25%,而**失效方向是危险的那一侧**

| 量 | 章程常数 | 本波实测 |
|---|---|---|
| 每台机时 | **0.745 h** | 活下来的三台 **0.9319 h**(0.9367 / 0.9175 / 0.9414) |

本波总机时 **3.0361**(含被抢占那台的 0.2406),spot 价带 `$0.231–0.250/h`
⇒ **实测单波 `$0.701–0.759`**。

⇒ **`$0.90` 的自动失效条款按字面 _不_ 触发**($0.76 < $0.90)。
⛔ **但它不触发的唯一原因是有一台在 14 分钟就死了。**
按活下来那三台的费率算一整波四台:`3.7274 机时` = **`$0.861–0.932`**,
**上端越过 `$0.90`**。

**这正是章程自己反对的那种静默失效**:常数**低估**机时 ⇒ 围栏算术**少记**每一波的钱,
而少记的方向是「以为还有余量」。它不会自己举手,因为触发条款要的是「实测 > $0.90」,
**而一次抢占就能把实测压回线下**。

⚠️ **诚实边界**:上面是**机时侧**推算,**不是账单侧读数**(MTD 快照还没含 W42;
CE 逐日拉一次 $0.01,本轮 MTD `$7.056 < $35` 未触发自动复核,本台**没有为此花那一分钱**)。
⇒ **本台不自行重裁常数**(章程明文:重裁归总监),只按条款**点名**并给出算术。

## 8. 补跑:按 GH #408 直接按需,围栏两条都满足

3040 的 SIR 码是 **`instance-terminated-no-capacity`** ⇒ 按 GH #408 **不走 (1)(2)(3) 阶梯**,
补跑机**直接 `--on-demand`**。围栏:**每粒只补一次**(3040 从未补过)、
**每波最多 2 台按需补跑**(本波 1 台)⇒ 两条都满足。

| 种子 | 实例 | 请求/实得 AZ | 市场核实 | 起飞 |
|---|---|---|---|---|
| 3040 | `i-07d6358854ccda217` | `us-west-2c` / **`us-west-2c`** | **`InstanceLifecycle=None`** | `12:19:25Z` |

- **采集配置逐项显式传齐**(`--slots 16 --hours 2 --rec-slots 8 --ref <40 位>`)——
  W38 那次补跑悄悄吃了脚本默认(`slots=14` / 看门狗 3h)只好杀掉重发,这条纪律不省。
- **树钉**:`--ref 1baa449b…`(W42 的树,**不是**现在的 `origin/main`)。
  `git fetch --depth 1 origin 1baa449b…` **exit 0** ⇒ 该 ref 在远端可达,已核。
  ⚠️ `origin/main` 此刻已走到 `c47925b9`(英雄组的三个 commit),**故意不跟**。
- **AZ 选 2c 不选 2a**:2a 正是刚刚拒绝容量的那个。按需机本来不吃容量回收,属 belt-and-braces。
- **闸 (i) 未被消耗**:它管例行波次,这是对已飞波次的**登记过的修复**
  ⇒ **W43 仍解锁于 `2026-09-03T15:32:03Z`**。
- **起飞码 `RERUN_EXIT=0`,不经管道读的**;市场以 `describe-instances` 的
  `InstanceLifecycle=None` 为准,**run_id 的 `spot_` 前缀是硬写字符串,不是证据**。
- **3040 已有 10 局 ab 存量**:`recover_verdict` 按种子戳配对,补跑的局会自动与它并,
  预期 ab 腿更深、`ab:ba` 更不对称。
- **成本**:1 台 × ~0.93 h × `$0.673/h` ≈ **`$0.63`**;围栏更新为 **`~$8.59 ≤ $80`**。
- ⚠️ **GH #427 的硬截止**:按需机**没有 SIR**,唯一的码是 EC2 `StateReason.Code`,
  终止后约 1 小时读不到。预计自毁 **~13:1x–13:2xZ**,看门狗 **14:19Z**
  ⇒ **下一轮必须在那之后的 1 小时内把 `status_code` 落盘**,已写进 W42 收割义务。

## 9. ⭐⭐ 并池 W41∪W42:**报数,不裁定**(GH #449 在总监手上)

方法:把**八个 run 子目录**放进同一个父目录交给 `recover_verdict.py`
⇒ 它**逐粒 swap-average 后跨种子取算术平均**,**绝不按局数加权**(铁律 4(i-d))。
`RV_EXIT=0`,`files_seen == games_loaded == 429`,`unparseable 0`,`source_dirs 8`。

| 读数 | gpm | xpm | deaths | last_hits | 粒数 | 计分局 |
|---|---|---|---|---|---|---|
| W41 单波 | −19.84 | −15.31 | +0.28 | −1.54 | 4 | 199 |
| **W42 单波** | **−19.15** | **−7.80** | **+0.12** | **−0.60** | **3** | **172** |
| **W41∪W42 并池** | **−19.54** | **−12.09** | **+0.21** | **−1.14** | **7** | **371** |

`comps_better`(并池):gpm **2/7** · xpm 2/7 · deaths 2/7 · last_hits 3/7。
逐粒 gpm:`3017 −7.18 / 3030 **+75.21** / 3101 −69.45 / 3117 −8.42 / 3161 −32.60 /
3204 +20.41 / 3329 **−114.77**` —— 极差 **190**,远大于镜像零点 σ≈30。
并池胜负通道仍 **DEGENERATE**(少数侧 `6/429 = 0.0140`,`mean.winrate_headroom 0.0191`)。

⛔ **并池的决定权在总监,不在本台**(GH #449:树对象哈希 vs 编译等价)。
本台把**发波前就预登记好的立场**原样留着:两波编译产物逐条相同 ⇒ 本台读作**能并**。

⛔⛔ **但即便总监批准并池,家族也只有 7 粒,不是 8** ——
`3040` 被容量回收。8 粒门槛**卡在这一粒上**,而 §8 那台按需补跑机正是去买它的。
⇒ **下一轮收割补跑机之后,若总监批准并池,57-id 家族一步到 8 粒。**

## 10. 局数(铁律 7)

**(a) 上一波(W42)最终有效局数**:见 §5.0。三粒配对种子 `ab/ba` 分别
32/24、41/17、37/21,**合计 110:62**;`scored_games` **172**、`games_loaded` 206、
`unfinished` **0**。⛔ 第四粒 **3040 = ab10 / ba0**,**不计分但点名**。

**(b) 本轮在跑波次**:补跑机 `i-07d6358854ccda217`(种子 3040),12:19:25Z 起飞,
**12:33:30Z 复核时 S3 前缀 `OBJ_COUNT=0`(一个对象都没有,不只是没有 `analysis.json`)**。

⭐ **这是预期值,而且本轮把「预期」量出来了,不是估的**:W42 四台的第一个对象分别落在
起飞后 **~14.5 分钟**(3040 的 `soak_farm.log` 09:46:34;3101 的第一个对象 09:47:10)。
补跑机此刻起飞 **14.1 分钟** ⇒ **正好还没到第一次上传**。⛔ **本台先把它误读成「起飞 30
余分钟仍无产出」并差点写进报告** —— 错在读钟不在读数据(12:33Z 不是 12:5xZ)。
预期 **~24 计分局**(12 局 × 2 腿),暖场局不计。

### 10.1 ⭐⭐ 顺带把上一轮那条 recover 路线的**误差第一次量出来了**(零成本)

上一轮为救 GH #271 的闸,用「S3 最后一次上传时间 − `launched_at`」当存活**下界**。
**那条路线的紧不紧,一直没有人量过** —— 因为要量它就得同时有下界和真值,
而 W41(按需)恰恰没有 SIR。**W42 两样都有**:

| 种子 | S3 最后上传 | SIR 真值(终止) | 下界误差 |
|---|---|---|---|
| 3101 | 10:25:11 | 10:28:22 | 短 **3.19 min** |
| 3117 | 10:24:00 | 10:27:17 | 短 **3.28 min** |
| 3204 | 10:25:08 | 10:28:47 | 短 **3.65 min** |
| **3040**(被回收) | 09:47:41 | **09:46:31** | ⛔ **长 1.17 min —— 反了** |

两条结论:

1. **正常自毁的机器:下界偏短 ~3.2–3.7 分钟**(机器还要走完关机与最后一次 flush)。
   ⇒ 上一轮记进 `W41_wave.json` 的四个下界(`≥52.4 / ≥52.4 / ≥52.5 / ≥41.6`)
   **真值大约是 55.6 / 55.6 / 55.7 / 45.3** —— **下界的方向是对的,而且很紧**,
   那一轮的 recover **没有把闸带偏**。
2. ⛔ **但被抢占的机器上它会反号**:3040 的最后一次上传比 SIR 的 `Status.UpdateTime`
   **晚 1.17 分钟** —— SIR 的时刻是**回收被通告**的时刻,机器随后还在关机流程里继续上传。
   ⇒ **「下界」这个名字对被抢占的机器不成立**,它是一个**可能偏大**的估计。
   本轮不需要它(SIR 直接给了真值),但**下一次再走 recover 路线时要知道这一条**:
   一个偏大的「下界」用在 `reclaim_blind` 的换腿点判定(40 min)上,
   **失效方向是把「抢占前」误判成「抢占后」**,也就是把一粒该作废的种子读成配对成功。

⚠️ **诚实边界**:n=3(正常)+ n=1(抢占),**一波**。本台**不据此改任何常数或工具**,
只把这四个数登记下来,给下一次用 recover 路线的人。


## 11. 泄漏检查:CLEAN(判据是「恰好等于意图」,不是「0 台」)

本轮既收割又发波,收尾要的**不是空**,而是**恰好对得上**:

- **进场普查**(12:1xZ,发波前):`state=pending,running` **空** —— W42 四台已全部离场。
- **`terminated,shutting-down` 普查**:**空** ⇒ ⚠️ **这是 GH #375 的 1h 窗口过期,
  不是「没东西终止过」**(SIR 证明四台都终止了)。**空普查对历史一个字都不证明。**
- **收尾点名**(12:2xZ,补跑起飞后):`soak-run` 标签**恰好一个值**,
  与 `rerun.machines[0].run_id` **逐字对上**,**没有多的也没有少的**。
- `describe-volumes`:**一卷 `in-use`**,零孤儿卷。常驻成本只有 AMI。
- ⚠️ 再登记:`--leak-only` **只报「多出来的」**,「少一台」它永远不举手 ——
  这就是本轮用全量点名而不用它的原因。

## 12. 铁律 6 / 铁律 11

- **铁律 6**:`bots/` / `game/` **本会话一行未改**(改动全在 `iterations/`)。
  静态半 `bash tools/agent/luacheck_gate.sh` 读数见下方提交记录;
  `arm_push_gate.sh` 上膛后两次 `git push` 各自再跑一次同一道门。
  **未用 `RULE6_BYPASS` ⇒ 本报告无「SKIPPED, not passed」行。**
  动态半(~100 min,GH #124)**未跑,不声称**。
- **铁律 11**:未触发 `requires approval`。**无空转等待** ——
  自检 Lua 腿在后台跑的整段时间里,本台在做收割、并池、门与发波。

## 13. 交棒

| # | 给谁 | 事 |
|---|---|---|
| ① ⭐⭐⭐ | 总监 | **成本常数 `0.745 h/台` 漂了 25%(实测 0.9319)。** 按活下来的费率一整波是 `$0.861–0.932`,**上端越过 `$0.90` 的自动失效线**;本波实测 `$0.70–0.76` 之所以在线下,**只因为一台在 14 分钟就死了**。失效方向是「围栏少记钱」。**本台按条款点名,不自行重裁。** GH #454 已开。 |
| ② ⭐⭐⭐ | 总监 | **GH #449 并池待裁,而且现在有数了**:W41 −19.84 / W42 −19.15 / 并池 −19.54(7 粒 371 局)。⛔ **批了也只有 7 粒不是 8**,第 8 粒正由本轮的按需补跑机去买。 |
| ③ ⭐⭐⭐ | 总监 | **GH #352:预登记分支 (a) 触发。** 只换种子的干净复制检验,通道**仍 DEGENERATE**(0.0243,并池 0.0140)⇒ **「加种子/换种子集」这条路按构造已死**,病灶是语料。现在站在两个独立种子集上。 |
| ④ ⭐⭐ | 总监 | **GH #435 第一次被真事故考过了**:掉 3040,**零项归零**(W40 同类事件归零三项)。⚠️ 保护**只深一层**,一次成功不是「修好了」,**不要据此关**。 |
| ⑤ ⭐⭐ | 总监 | **GH #427 多一条零成本的 (丙)**:spot 波本来就不吃那个 1h 窗口(本轮窗口关了 2 小时,SIR 四台全读到)。真正暴露的是**按需机** ⇒ **GH #271 每下一次「一波按需」,就把那一波的死因码押在 1 小时上**。 |
| ⑤b ⭐⭐ | 总监 | **S3 下界 recover 路线的误差本轮第一次被量出来(§10.1)**:正常自毁偏短 **3.2–3.7 min**(紧,方向对,上一轮的 W41 recover 没带偏闸);⛔ **被抢占的机器上它反号**(最后一次上传比 SIR 晚 1.17 min)⇒ **对被抢占的机器它不是下界**,而在 `reclaim_blind` 的 40 min 换腿点上,偏大的方向是**把该作废的种子读成配对成功**。n=3+1,**本台不据此改任何常数**。 |
| ⑥ ⭐⭐ | 本台自己(下一轮必做) | 补跑机 **13:1x–14:19Z 自毁,`StateReason.Code` 只能读 1 小时** ⇒ **下一轮开工先读它**,连同 `survival_min` + `ab/ba/arm_depth` 一起写回 `W42_wave.json:rerun.machines[]`(键名要 **`ab`/`ba`**,见 §4)。 |
| ⑦ ⭐ | 录像组 | `ckpush` / `roshdist` 的 (a) 取证:本波 `.dem`(四 run,`rec_slots=8`)。**本轮重查确认 `analysis.json` 仍无帧级检测器字段** ⇒ 要么走 `.dem`,要么先加字段。 |
| ⑧ ⭐ | 总监/协同组 | **零 pudge 第七波**(逐局读 `players`,206 局 0 次)⇒ `strategy-26`/`rotscope` 写「无载体」,**不许写成负向读数**。 |
| ⑨ ⭐ | 存量 | `test_detector_source_constants.py` **第六轮**原样红(#420)/ #432 保持 open(本波**未测试**它)/ **#388 的 W34 103 份 `.dem` 2026-09-22 到期(19 天)** / #446 / #429 / #418 / #412 / #408 / #402 / #395 / #375 / #290 / #313 / #271 / #329 / `strategy-5b` / **`strategy-31/32` 第八轮不搭波**(`tormself`/`immguard` 不在 57-id 串里,按构造搭不上,按铁律 9 第八次交回总监)。 |

**下一轮本台 = 收割补跑机 + 视情况发 W43**(闸 (i) 解锁 `2026-09-03T15:32:03Z`)。

## 14. 本轮 token 用量

```
TOKENS total_in=7,945,585 out=51,402 turns=60
```

⚠️ 这是「到统计时刻为止」的数字,统计之后的收尾回合(commit / push / GitHub 产物)不计入。
⭐ **比上一轮(16,199,133 / 110 回合)低约一半**,归因据实写:上一轮明确登记的浪费
(「发波等待期间反复读同一个后台输出文件」)本轮改掉了 —— 自检 Lua 腿放后台后
**一次都没有轮询它**,直到 harness 的完成通知到达;其余等待时间全部用于收割与并池。
