# 批测台 2026-08-23T02:06Z — 收割轮(§AR.0 的 24 id 全集波)

**一句话**:00:11Z 那波四台全部跑完并自毁,**278 有效局 + 23 暖场 = 301** 全量收割完毕;
24 id 合读 **gpm −42.49 / xpm −41.74 / deaths +0.38 / last_hits −2.35,四指标全 0/4**,
`suggested = hold_or_reject`;`--rec-slots 8` 验收**第二次 exit 0 通过**;
**158 个 `.dem`** 落在 `dem21/<run>/`(不在 `soak/` 下,见 §5 的口径更正),
owner 优先项 P1/P2 的 (a) 语料**已备齐,球在录像组**。本轮**未启动新波次**(4b(i) 节流未到),
零新增支出,泄漏四层全清。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` worst exit **3**:

- **UNLANDED 2 条**:`271ecba` / `13ab459`(均 2026-08-23T01:43:04Z,`[strategy] itemtrip`,
  在 `origin/claude/dreamy-feynman-tmvbay` 上,未落 `origin/main`)。**不属批测台,只转述不接管。**
- cadence **clean**;trunk python **12 passed / 0 failed**。
- 工具自述的 LIMITS 照旧:shallow clone ⇒ 271 个 ref 里 246 个不可判定,graft 点
  2026-08-22T16:45:45Z 以下一律拒答。

**owner 优先项核对**(`iterations/OWNER_PRIORITIES.md`):P1 球在协同组(1)、P2 球在协同组,
**两项当前都不在批测台**。本轮的收割**正面推进两项**:P1/P2 都卡在条件 (a)「真实对局里核验到,
带帧证据」,而这一波的 158 个 `.dem` 就是那份语料(§5)。

## 1. AWS 引导 + 成本

```
bash tools/batch_test/aws/session_setup.sh
→ AWS ready: arn:aws:iam::581436534924:user/dota2bot-agent  (restricted batch-runner user — correct)
```

MTD **$23.578**(免费 `budgets/dota2bot-batch`,forecast 23.139,limit $100,
budget refreshed 2026-08-22T21:26:04Z)。≪ `COST_CONFIRM_AT=$35` ⇒ **未花 $0.01 调 CE**。
$45 围栏 / $90 刹车 / $100 owner 线全未触及。

**注意一处计费滞后**:读数与 00:06Z 逐位相同($23.578),而 00:11Z 那四台 on-demand 已经跑完 —— 
budget 的 refresh 时刻(08-22T21:26Z)**早于本波启动**,所以本波的 ~$1.5 **尚未入账**。
这与 20:09Z→22:06Z 那次(+$4.837 补进 18:11Z 的四台)是同一个已知现象,**不是泄漏**;
下一轮读数应当看到它进来。**预期下轮 MTD ≈ $25.1。**

## 2. 收割:四个 run 全齐,301 局

四台实例在 02:06Z 已全部**不在任何非 terminated 状态**(自毁齐备,无需人工终止)。

| run | seed | analysis.json | demclaim.json | dem21 `.dem` |
|---|---|---|---|---|
| `spot_20260823_001127_1_cde1d6c4…_3fb2ec` | 888 | 77 | 40 | 40 |
| `spot_20260823_001131_1_cde1d6c4…_590502` | 895 | 71 | 39 | **38** |
| `spot_20260823_001136_1_cde1d6c4…_febb54` | 896 | 77 | 40 | 40 |
| `spot_20260823_001140_1_cde1d6c4…_c3ba4a` | 906 | 76 | 40 | 40 |
| **合计** | | **301** | **159** | **158** |

**必查项 ①(暖场 stamp)**:23 局 `script_version` 全是 `cde1d6c`,**23/23 ✅**,无一局串到别的树。
**必查项 ②(分 run 下载 + 带前缀合并,一个不丢)**:见 §2.1 —— **本轮真的丢了,靠这条查了出来。**

### 2.1 ⭐ 必查项 ② 当场兑现:裸合并静默吃掉 22 局,时分秒前缀救回

第一次我图省事跑了 `cp */*.analysis.json all/`(**不带前缀**),`ls all | wc -l` = **279**,
而逐 run 之和是 **301** ⇒ **22 局被同名覆盖,零报错、零告警**。这正是 `#95` 那条
「同秒开局撞 basename 跨 run」的实例(全桶已知 21 个撞名 basename,本波又添)。

**后果不是「少了 22 局」而是「读数变了」**——两份 verdict 并排:

| | 裸合并(279 局) | 带前缀合并(301 局,**正确**) |
|---|---|---|
| seed 895 gpm | −29.20 | **−31.38** |
| seed 896 gpm | −32.17 | **−38.32** |
| mean gpm | −40.51 | **−42.49** |

888 / 906 两个种子逐位不变(没撞上),被污染的是 895 / 896。**⇒ 必查项 ② 不是形式主义,
它今天挡下了一次会被当成真读数发出去的错误。** 逐 run 单独跑 `recover_verdict.py`
与带前缀合并**逐位一致**,互为交叉验证。

## 3. 经济读数(条件 (b) 的粗粒度旁证,**不做显著性检验**)

`recover_verdict.py`,**278 有效局**(暖场 23 局不计):

| 指标 | delta(候选腿 − 基线腿) | comps_better |
|---|---|---|
| gpm | **−42.49** | **0/4** |
| xpm | **−41.74** | **0/4** |
| deaths | **+0.38** | **0/4** |
| last_hits | **−2.35** | **0/4** |

`suggested = hold_or_reject`。逐种子 gpm:**888 −55.24 / 895 −31.38 / 896 −38.32 / 906 −45.03**
(sd 10.35,SE 5.17)。**四个种子同号**,没有一个是正的。

**必查项 ⑥,两条纪律一起说**:

- **这是 24 个 id 的合成效应,不可归因到其中任何一个** —— 含本波首次上机的 `fieldcreep`
  与本波重新入集的 `pullcamp`。
- **§AF.3 已裁定「靠比较波间残差判单 id」这门手艺作废**。所以 −42.49 与 18:11Z 的 −29.35、
  12:12Z 的 −29.87 **不并排当趋势、不当「恶化」**。唯一能说的中性观察:24-id 与之前的
  22/21-id 都落在同一负残差带内,**且本波是四指标同时 0/4 的第二波**(18:11Z 那波 gpm 与
  last_hits 各有 1/4)。这句只是描述,不是判定 —— **判定归总监。**

**一处口径提醒(免得下一个人误用)**:把全部 278 局不分 wave 直接池化取两腿均值,得到的是
gpm −25.12 / xpm −28.60 / deaths +0.30 / last_hits −1.27。**这组数不作数** —— 本波
radiant wave 167 局 : dire wave 111 局,池化没有抵消 Radiant 侧偏(≈ +1.5k 金,CLAUDE.md),
`recover_verdict.py` 的「两 wave 各自取均再平均」才是抵消过的那一个。表里的 −42.49 是后者。

**ab/ba 不对称仍在**(167 : 111):radiant wave 每个种子都打满 `--games 22` 的 42 局配额,
dire wave 25-29 局仍撞 35min stall 上限。**与 20:09Z / 18:06Z 结论一致:再提 `--games`
只会加大不对称,补 dire 要动 stall 上限(harness 侧,本台不自己改)。**

## 4. 必查项 ③:被测集合逐字交叉验证 —— 24/24 通过

S3 真实 cand 串 **与 `test_set.md:5047`(§AR.0)逐字节相同**,`count = 24`:

```
l1trade,l5combo,midtp,suptp,tpcommit,tpdying,lf_rescue,teambrain,ownhalf,overchase,
fieldregen,wandbleed,capmono,cmrguard,tpdead,zusult,blinkflee,liondrainstop,
creeppull,pullcamp,stayfield,stayfield2,fieldbuy,fieldcreep
```

- `pullcamp` **出现 1 次 ✅**(上一波是必须 0 次,本波方向反过来 —— 重新入集后的第一波);
- `fieldcreep` **出现 1 次 ✅**(首次上机);
- `wandlimbo` **0 次 ✅**、`odaoe` **0 次 ✅**(eligible 但不可 arm)。

## 5. ⭐ 必查项 ⑤ + 一处会骗人的口径更正:`.dem` 不在 `soak/` 下

**我第一次数 `.dem` 数到了 0**,四个 run 的 `soak/<run>/` 里一个 `.dem` 都没有 ——
差一步就写成「帧通道整波挂了」的红色告警。**实际不是**:

- `soak/<run>/` 只放 `*.analysis.json` / `*.demclaim.json` / `*.log.gz`(该 run 117 json + 77 gz);
- 录像走 **`dem21/<run>/`**(会过期的那个前缀,`soak_loop.sh` 的既定行为,章程
  「扁平 `replays/` 镜像仍只留 slot 1,其余录制走会过期的 `dem21/`」写过),
  本波 **158 个 `.dem`**;
- 上一波(18:11Z)报表里那句「`.dem` 156」数的也是 `dem21/`,**不是** `soak/`。

**⇒ 立一条口径:数 `.dem` 一律数 `dem21/<run>/`;在 `soak/<run>/` 下 `grep '\.dem$'` 恒为 0,
而这个 0 长得跟「SourceTV 整波没录上」一模一样。** 顺带:`grep '\.dem$'` 还会漏掉
`.dem.gz`,数的时候不要锚 `$`。

**三元组(必查项 ⑤:`--rec-slots 8 < 16` ⇒ 第一栏分母是「录制局数」不是 `analysis`)**:

```
analysis 301(278 有效 + 23 暖场)
录制局数 159 (slot 1-8;slot 9-16 是 142 局,结构上不录、也就没有 claim)
demclaim 159  → 归属侧 159/159 = 100% 干净
.dem     158  → 1 局丢在上传侧(run …_590502:39 claim / 38 dem)
```

不换分母的话 `159/301` 会被误读成 53% 的归属灾难。**上传侧丢 1 局与上一波(157 claim /
156 dem)是同一形状、同一量级,读数不受影响。**

## 6. ⭐ 必查项 ⑦:8 槽下的归属歧义**这次真的被演练了**,而 mtime 判据 **0/159 全错**

`159` 个 claim sidecar 逐个读:

| 项 | 读数 |
|---|---|
| `method` | **logname 159/159** |
| `by_hostname` 与 `by_logname` 一致 | **159/159** |
| `by_mtime` 与 `by_logname` 一致 | **0/159**(**全部不一致**) |
| `hostname_hits` | **恒 1**(159/159) |
| `rec_slots` | 恒 8(159/159) |
| `candidates` 分布 | **8:53 / 7:56 / 6:34 / 5:11 / 4:4 / 3:1** —— **没有一局是 1** |

**这一格是本轮最值钱的免费读数。** 总监 08-22 裁 8 槽不裁 16 槽时,写下的唯一未测风险是
「这 20 局都是 `candidates:1`(池里只有一份 `.dem`),**多录制者的歧义从未被真正演练过**」。
**本波 159 局 candidates 全部 ≥3(众数 7-8),歧义每一局都在发生,而 `logname` 与
`hostname` 仍然 159/159 一致、`hostname_hits` 恒 1 ⇒ 那条未测风险现在测过了,通过。**

同时,`dem_claim.sh:144` 那条 `[ "$rec_slots" = "1" ]` 的 mtime 护栏 **第二次被实测证明必需**:
8 槽下 `by_mtime` **159/159 全错**(20:09Z 那波是 157/157 全错)。**若有人为「兜底」放开它,
后果是每一局都认错人,而 `.dem` 数 / `analysis` 数 / 三元组全部正常,静默到没有任何计数会报警。**
建议仍是在 `dem_claim.sh:144` 上加一条 `[reverse]` 源码钉子(**本台按章程不自己改 harness**,
已在 GH #45 点名,本轮在 GH #70 下续报)。

**给录像组**:本波 158 个 `.dem` 的归属 **100% 可信,没有一局认错人**,且这次是在
**歧义真正发生了 159 次的条件下**验出来的。

## 7. 必查项 ④:`--rec-slots 8` 验收 **exit 0 通过**(第二次)

```
python3 tools/batch_test/soak/rec_slot_cost.py <本波 4 个 run 目录> \
    --baseline tools/batch_test/soak/rec_slot_baseline.json
```

**真 exit code = 0**(单独重定向到文件后取 `$?`,**末尾没有接管道**,合必查项 ④),
`compared 16 slot(s); 0 recording slot(s) beyond tolerance`,`OK: measured on 301 games,
8 control slots`。box factor 1.000。

吞吐侧结论与 20:09Z 同向且更干净:**8 个录制槽的残差全部为正**(+6.5% ~ +8.2%,
`+ = recorder FASTER than trend`,对照腿拟合 `timescale ~ 0.00913*slot + 1.7804`),
**局数侧同向**:录制槽 19-20 局 vs 对照槽均值 17.75(**+1.25 ~ +2.25 局**)。
**⇒ 8 路并发 SourceTV 的吞吐代价仍然测不出来,连符号都是反的。**

**一处操作纪律(踩了才知道)**:只下 `*.analysis.json` 时该工具 **exit 2**
(`CANNOT CERTIFY: no .demclaim.json anywhere`,`sidecars=0`)。**这个 exit 2 与 12:12Z
那次 16 槽的 exit 2 是完全不同的两件事**(那次是结构性无对照腿,永久拒答;这次只是我少下了
一类文件),**都印成 exit 2,凭 exit code 分不开,必须读那行 die 文本。**
⇒ 收割时 `--include "*.demclaim.json"` 与 `--include "*.analysis.json"` **必须一起下**。

**阶梯照 22:06Z 的裁定不动**:通过**也不自动上 16 槽**(16-of-16 结构上无对照腿 ⇒
`rec_slot_cost.py:218-220` 按构造 exit 2)。三条路 (A) 停 8 / (B) 上 16 弃验收 /
(C) 走 12 **仍待总监裁**;在他改阶梯之前,本台保守默认 **(A) 停在 8**。
**本轮给这道选择题添了一份新证据**:§6 那条「多录制者歧义未演练」的顾虑 —— 也就是当初
「先上 8 不直接上 16」的**唯一**技术理由 —— **已经在 8 槽上被证伪了**。它不构成上 16 的
充分条件(验收侧的结构性拒答与它无关,是另一回事),但**(C) 走 12 这条路上的归属侧风险
现在有实测背书**。判定仍归总监。

## 8. 必查项 ⑧ / ⑨:交录像组,批测台不越界

- **⑧ `strategy-2` 的三条臂内读数**(连接率 / 开拉点离泉水中位 <9,403u / 深野营 poke 帧占比
  + 免费正控):这三条是**帧级检测器**的手艺,按章程「不做判断分析」由录像组出数。
  语料已备:**158 个 `.dem`,帧通道 8/16**,`pullcamp` 选点修法 `01ab933` 在被测树内。
- **⑨ §AR.3 甲 `fieldcreep` 的每一次否决命中必须写清打他的是野怪还是小兵**:同上,
  归录像组。**只报「触发了 N 次」不算买到 (a)** —— 那个数在总监 §AR.1 的两个世界里指向
  相反结论。**§AR.3 乙的退出条件同时生效**:若 camp 啃人的帧确实出现而 `fieldcreep`
  一次没点亮,**这个 id 立刻出 armed 集**并退回协同组重估修法。

## 9. 启动决策:**不启动**

- **4a(queue.json 显式请求)**:5 条 pending/running 里**没有一条需要为它开波** ——
  `strategy-1` = done;`hero-1`/`hero-2`/`hero-3`/`hero-4` **四条全部自述
  `NO NEW WAVE NEEDED`**(档案侧扫描,不花 AWS 钱);`strategy-2` 本轮由这波交付,
  已在 queue.json 里置 **done** 并附读数与下一棒。
- **4b(例行波次三条节流)**:
  - **(i) 不满足** —— 上一次例行波次启动 **00:11:22Z**,现 **02:06Z**,间隔 **1h55min**,
    差 6h 门槛约 **4h05min**,**小时级差额**。按 22:06Z / 20:09Z / 16:10Z / 08:08Z / 16:06Z
    的先例即不启动(**不是 06:07Z 那种「差 2 分钟按立法目的照常启动」**)。
  - (ii) 满足(被测树刚跑完,`bots/` 无新漂移需要立刻测);(iii) 满足($23.578 + ~$1.5 ≤ $45)。
- **owner 优先项核对**:P1/P2 球都在协同组与录像组,**本轮不开波对两项零代价** ——
  两项要的 (a) 语料**正是本轮刚收割的这 158 个 `.dem`**,球在录像组读帧,再开一波只会
  重复买同一份证据。

**下一波最早 2026-08-23T06:11:22Z**,预置(除非总监另有裁定):§AR.0 的 **24 id 不变**、
种子 888/895/896/906、4 台 × 1 种子 on-demand、16 槽、**`--rec-slots 8`**(§7 的保守默认)、
2h 看门狗、`--games 22` **不再上调**(§3 的不对称原因在 stall 上限不在配额);
核树两步走 `git fetch --depth 1 origin <全 40 位 SHA>` → `git log <SHA>..HEAD -- bots/ game/`
(**exit 128 = 不可比,不是无漂移**;**短 SHA 会 `couldn't find remote ref`**)。

**下一波必查项 8 条**(承接,方向有变的单列):
① 暖场 stamp 全同树;
② **分 run 下载 + 带时分秒前缀合并,对账一个不丢** —— **本轮它真的抓到了 22 局(§2.1),
   这条从「形式」升级为「实测必需」**;
③ S3 真实 cand 串与当时的 §x.0 逐字交叉验证;
④ `rec_slot_cost.py` **判据不许改、末尾不许接管道、认 exit code 不认空输出**;
   **【新】下载时 `*.demclaim.json` 必须与 `*.analysis.json` 一起下,否则 exit 2 是
   「我少下了文件」而不是「结构性拒答」,两者同码不同因(§7)**;
⑤ 三元组第一栏分母是**「录制局数」**;**【新】`.dem` 数 `dem21/<run>/`,不数 `soak/`,
   不锚 `\.dem$`(§5)**;
⑥ 经济读数是 24 杠杆合读,不可归因单 id,不与前波并排当趋势(§AF.3);
⑦ `candidates` 分布 + 三判据一致性再读(**本轮已在真歧义下通过,下一波是复核不是首证**);
⑧ 录像组的 ⑧/⑨ 两条帧级交付是否已回流(若两波都没回流,在 GH #109 下点名)。

## 10. 泄漏检查(四层 + spot 请求,全免费只读)

| 层 | 开工 | 收尾 |
|---|---|---|
| 非 terminated 实例 | **0** | **0** |
| 游离(available)卷 | 0 | 0 |
| 快照 | 1(`snap-0ad026b386c804288`,160GB,唯一常设成本) | 同 |
| EIP | 0 | 0 |
| open/active spot 请求 | 0 | 0 |

**无泄漏。** 00:11Z 那四台**全部自毁完毕,无需人工终止**。

**固定栏位**:`soak/` **164**(00:06Z 是 160,+4 = 本波四个 run)、`dem21/` **16**(+4 同)、
`unattributed/` **0**、`validation/` 最新条目仍是 2026-07-23(标准收割路径走
`recover_verdict.py`,陈旧是**预期**)、远端 main tip 开工 `3c6f020`。

## 11. 跨组

- **不新开 issue**。在 **GH #70**(拥有归属核对 / demclaim 三判据这条线的 issue,
  00:06Z 的 24 id 静态核对也在它下面)下追评 §6 的真歧义实证 + §5 的 `.dem` 位置口径。
- `[batch] #70`、`[harness] #98`/`#95`/`#75`/`#33`、`[bug] #96`、
  `[strategy] #117`/`#116`/`#119`/`#109` 仍开着,不重复开。
- **给总监的两件待裁**:(a) §3 的四指标 0/4(第二波)是否触发对 24 id 测试集的处置;
  (b) §7 的 rec-slots 阶梯 (A)/(B)/(C) —— 本轮为 (C) 添了归属侧实测背书。
- **给录像组的抬头**:本波语料 **158 `.dem` / 帧通道 8 of 16 / 归属 100% logname**,
  在 `s3://dota2bot-batch-results-4924/dem21/spot_20260823_0011*/`;
  owner P1(`creeppull`/`pullcamp` 选点修法后第一波)与 P2(`stayfield`/`stayfield2`/
  `fieldbuy` + 首次带 `fieldcreep`)的 (a) 都要从这里读。

## 12. 验证(铁律 6)

本会话**未改 Lua**(改动仅 `iterations/` 下的报告 / 章程 / `queue.json`),
容器无 `luacheck` / `lua5.1`,**铁律 6 无适用对象**。
**本轮无估算项**:§2-§7 全部是命令输出或源码行号级读数;唯一的推断是 §1 的
「$1.5 尚未入账」,已标为预期并给出可证伪的下轮读数($25.1)。

## 13. 本次会话 token 用量

`python3 tools/agent/token_usage.py` 最后一行原样:

```
TOKENS total_in=6,287,746 out=50,545 turns=71
```

(统计时刻之后的收尾回合——commit / push / GH #70 追评——不计入。)
