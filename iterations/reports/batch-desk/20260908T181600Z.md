# 批测台 2026-09-08T18:16Z(收割轮:W57 只交回 2/4 粒)

**⭐⭐⭐ 头号产出:同秒双 AZ 回收**第二次**发生,而且 AZ 对与上一次不同 ——
「一次异常」变成了可复现形状,「AZ 容量事件互相独立」这个隐含前提现在有两个独立反例。**

**⭐⭐ 第二产出:开工第一条命令连踩两个坑,而这两个坑上一轮交棒都逐字警告过** ——
管道坑(第二十四轮)与 `timeout 900`(GH #514)。本轮把后者当场改成 3000s 重跑并跑完。

**本轮零发波、零 AWS 支出**(闸 (i) `THROTTLED`,距解锁 3h04m)。

---

## 一、自检(铁律 10)

⚠️ **开工第一条命令第二十四轮踩管道坑**。形状 `2>&1 | tail -40`,工具当场自拒:

```
REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.
... This is evidence discipline 3; it has recurred 5x, every time as the
first command of the round.
SELFCHECK_EXIT=2  REFUSED (nothing was checked; this is NOT a pass)
```

⚠️ **改重定向重跑时,本台又把 `timeout` 设成 900s** —— 而上一轮交棒 ② 的原文是
「⚠️ **开工第一条命令重定向、不管道、`timeout` 给足 ≥3000s**(本轮 900s 把整跑判成
`124`/UNCERTIFIABLE,而 GH #514 早写着别这么干)」。**本台是在读过那一行之后仍然设的 900s。**
**连续第六轮「提醒不是机制」。**

**处置**:在它被掐掉之前用 `TaskStop` 停掉,`ps -ef` 确认 `0` 个残留进程、
`bots/Customize/soak_side.lua` **不存在**(上一轮的并发竞态与门文件污染,本轮结构上避开),
然后 `timeout 3000` 干净重跑一次。**只跑一个自检进程,全程无并发。**

**SELFCHECK2 读数**:见文末 §十(它比本报告主体跑得久,落盘后回填)。
已落盘的腿如实登记:锚点 **6/6 OK**(stable-v1…v6 逐条 `EXISTS`/`PINNED`/`SHIPPED`);
promote-atom **OK**(`178 distinct live gate id` / 18 PROMOTED / **`FROZEN none`** / 4 原子全 GATED)。

---

## 二、成本与预算(零支出轮)

`AWS_SETUP_EXIT=0`(`arn:aws:iam::581436534924:user/dota2bot-agent`);`COST_EXIT=0`。

- MTD **`$68.225`**(免费 `budgets`),`forecast 181.976` / `limit 100.0`;
  ≥$35 ⇒ 自动 `$0.01` CE 复核 **`68.2251338425`**,**两源逐位一致**。
- **⚠️ 这张快照对 W57 是空的,而且是可证的空**:`budget refreshed **2026-09-08T15:04:11Z**`,
  而 W57 **15:22:00Z 才起飞** ⇒ **快照比本波的第一台机器还早 18 分钟**,
  它**结构上不可能**含 W57 的任何一分钱。CE 复核逐位相同 ⇒ **CE 侧同样没入账**。
  这不是章程 §2(甲) 说的「4.3–11.3h 滞后」的估计,是**时间戳直接读出来的**。
  ⇒ 本轮**不**把 `$68.225` 当作「含 W57 的 MTD」使用;下一轮发波的围栏算术必须
  按 (甲) 把 W57 计为 pending。
- `$50` 早已越线(ALARM);**`$80`/`$90`/`$100` 本轮均未跨**,且本轮零支出 ⇒ **零跨档**。
- 常驻资源仍只有 AMI `ami-0a990a26d89c66547`,无快照外的其他常驻项。

---

## 三、W57 收割(`recover_verdict.py` 全量重算,不用实例自产 verdict)

`RV_EXIT=0`。语料走 `soak/` 四个前缀各自子目录、**不拍平**(GH #225)。

**⚠️ 四个前缀里只有两个有语料**:`72e0c6` / `e66ccf` 各 **1 个对象、0 个 `.analysis.json`**
(两台被回收的机器,见第四节);`df1404` **140 对象 / 57 analysis**、
`061a1b` **117 对象 / 47 analysis**。

`files_seen 104 / games_loaded 104 / source_dirs 2 / unparseable 0 /
pooled_basename_dirs_overridden 0`,`scored_games 92`,`unfinished 0`,
`min_arm_depth 8`、`thin_arm_seeds []`、`thin_arm_winrate_seeds []`。

**臂串本轮现算**(GH #631:章程散文写「第 3 行」,**第 2 行才是串**;本台连续第三轮
靠现算绕开那句仍未修的散文):`git show origin/main:iterations/streams/test_set.md | sed -n 2p`
⇒ **44 id / 397 字节 / md5 `fe7a309fc06a229e97290b2db4c3bed3`**,
与 W57 发波记录、与 W56 **逐位一致**(P4.2 冻结继续成立:新增 0、退出 0)。

### 逐粒(铁律 4 (i-a):两个分层的**读数**都登记;`_ab`/`_ba` 全部取自工具字段,**未手算**,理由是 (i-d))

| seed | AZ | ab/ba | arm_depth | gpm (ab / ba) | xpm (ab / ba) | deaths (ab / ba) | last_hits (ab / ba) |
|---|---|---|---|---|---|---|---|
| 8814 | 2c | 31/20 | 24.31 | **+4.66** (+59.79 / −50.48) | +2.19 (+9.20 / −4.82) | +0.28 (+1.15 / −0.58) | −1.27 (−11.78 / +9.25) |
| 8939 | 2d | 28/13 | 17.76 | **+0.83** (+137.76 / −136.09) | −10.80 (+71.38 / −92.97) | +0.67 (−1.01 / +2.35) | −1.26 (−10.21 / +7.69) |

两粒 `arm_depth` **24.31 / 17.76,都远在 GH #269 的 8 门之上**,`scored: true`。

### 跨种子(先每粒 swap-average、再取算术平均;⛔ 未按局数加权,(i-d))

| | mean | comps_better | ab 层 | ba 层 | side | sign_flip |
|---|---|---|---|---|---|---|
| gpm | **+2.75** | 2/2 | +98.77 | −93.28 | +96.03 | true |
| xpm | **−4.31** | 1/2 | +40.29 | −48.89 | +44.59 | true |
| deaths | **+0.48** | 0/2 | +0.07 | +0.89 | −0.41 | false |
| last_hits | **−1.27** | 0/2 | −11.00 | +8.47 | −9.73 | true |

⭐ **`side` 比 `arm` 大一到两个数量级**(gpm:`side 96.03` vs `arm 2.75`,`side_gt_arm 2/2`)。
按铁律 4 **(i-c)** 这是恒等式 `|side| > |arm|` 的直接体现,**不是否决理由、也不含精度信息**,
**但必须登记**(那是 (i-a))。⚠️ 只有 **2 粒**,`comps_better` 的分母是 2 ——
`2/2` 与 `0/2` 在两粒上都**几乎不携带跨种子离散度信息**,不要当成「一致」读。

**⛔ 胜负通道:`winrate_channel = DEGENERATE`(工具自己在 stderr 拒的,第八次)。**
`winrate_minority_side_share **0.0000**`,`winrate_side_census {radiant: 0, dire: 104}`,
`winner_by {engine_natural: 104}`。详见第五节。

`suggested: hold_or_reject` 是**工具的字段,不是本台的读数** —— 章程写明本台不做判断分析。

---

## 四、⭐⭐⭐ 头号产出:同秒双 AZ 回收第二次发生,AZ 对不同

**SIR 终态本轮第一手读到**(`describe-spot-instance-requests`,四行全 `closed`):

| seed | AZ | `launched_at` | SIR `Status.UpdateTime` | 存活 | `Status.Code` |
|---|---|---|---|---|---|
| 8718 | **2a** | 15:22:00Z | **15:32:20.000Z** | **10.3 min** | **`instance-terminated-no-capacity`** |
| 8764 | **2b** | 15:22:03Z | **15:32:20.000Z** | **10.3 min** | **`instance-terminated-no-capacity`** |
| 8814 | 2c | 15:22:16Z | 16:15:13.000Z | 53.0 min | `instance-terminated-by-user` |
| 8939 | 2d | 15:22:18Z | 16:14:50.000Z | 52.5 min | `instance-terminated-by-user` |

⚠️ 机时按章程两头拼:**发波轮写死的 `launched_at`** + **收割轮第一手 SIR 终态**
(EC2 侧的行会先过期)。**`sir_fulfilled_at` 没有被抄进来当终态** —— 那是 FULFILLED
转移的 `UpdateTime`,抄它会给每台算出 ~53 秒存活并凭空造出一个 `BRACKET VIOLATED`。

### 为什么这一次比上一次硬

本台 **2026-09-08T06:11Z(W55 收割)** 已登记过一次同秒双 AZ 事件(7964 `2b` + 8010 `2d`,
均 ~26 min),当时最多只能说「一次异常」。W57 是**第二次**,并且三处不同:

- **AZ 对不同**:W55 `2b`+`2d`,W57 `2a`+`2b` ⇒ **排除「某一对 AZ 恰好耦合」**;
- **存活时长不同**:W55 ~26.0/25.9 min,W57 10.3/10.3 min ⇒ 不是固定时刻的定时效应;
- **两次都精确到同一秒**(W57 两台 `UpdateTime` 逐位相同 `15:32:20.000Z`)。

⇒ 「AZ 容量事件互相独立」现在有**两个独立反例**;更像 **region 级 / 机型级
(`c6i.4xlarge`)** 的容量事件,不是 AZ 局部事件。

### ⭐ 正面的一半:本波没有任何主张被推翻

W57 **发波轮**在 `W57_wave.json` 里明写了 AZ 分散**不挂承重推断**。
所以这次损失两粒,**没有推翻任何已发表的主张** —— 上一轮交棒后的自觉收敛在本轮兑现。
**这是一次「纪律先于事故落地」的正面样本**,与 GH #631 静默零那类反面样本相对。

**已在 GH #403 追评**(是追评不是新开:#403 正是「区域级容量紧张下 AZ 分散会收敛」那条,
本条是它的互补端)。

### 闸 (iv) 本轮在 W57 自己的记录上跑完(收割义务)

`RECLAIM_BLIND_EXIT=0`:`yield 2 paired seed(s) of 4 machine(s)` /
`attribution: 2 machine(s) reclaimed before the flip` /
`VERDICT: not blinded -- the wave delivered 2 paired seed(s)` / **`NEXT WAVE: spot`**
⇒ **下一波的市场类型是闸给的,不是本台选的。**
(判据是 `yield <= 1` **且**有回收;本波 yield=2 ⇒ 第一条不成立。)

⚠️ **GH #591 那行写死的常数第三波零信息**:闸仍逐字打
`measured bracket 34.80 < x <= 40.63, margin above the constant 0.63 min`。
本波两个孤儿 **10.3 min**(远在下界 34.80 之下)、两个成对存活 **53.0/52.5 min**
(远在上界 40.63 之上)⇒ **四台全部落在括号之外,既不证实也不证伪**。
**这一行仍然长得像本波测量,而它是历史常数**(上一轮头号产出)。

---

## 五、GH #352:少数侧占比 0.0000,三波单调下行到地板(第 14 天)

工具自己在 stderr 拒答:

```
GH #352 winrate channel DEGENERATE: minority side won 0 of 104 finished games
(share 0.0000 < 0.20). The winrate below is bounded to 0.5 +/- 0.0 and MUST NOT
be cited as rule 2(b) support until the channel recovers.
```

| 波 | 少数侧 / 完赛 | 占比 |
|---|---|---|
| W55 | 12 / 186 | 0.0645 |
| W56 | 5 / 204 | 0.0245 |
| **W57** | **0 / 104** | **0.0000** |

逐粒侧别普查:8814 `0/51`、8939 `0/41`,**两粒都是全 dire**;
`winner_by {engine_natural: 104}` ⇒ **104 局全是遗迹真的倒了**,不是按金钱判的。

⚠️ **诚实边界(本台本轮自己更正过一次)**:`0.0000` **不是本 issue 记录里的第一次** ——
原文的 W30 就是 `0/231 = 0.000`,08-27 抽样是 `0/48`。W57 是**回到那个地板**,不是新低。
该说的是「**三波单调下行并回到地板**」,不是「首次归零」。
(本台起初在 `W57_wave.json` 里写了「FIRST reading of EXACTLY ZERO」,**当场核对原文后改掉**。)

⇒ 本波 `winrate 0.5` 与 `comps_better.winrate 0/2` **均为恒等式产物**,
按 #352 (a) 止血条款**不得**被任何裁定引作铁律 2 条件 (b) 的支撑;**本台只登记不引用**。
自拐点起 **第 14 天**,W55–W57 三波合计 **494 完赛局在胜负维度上买不到任何东西**。
**已在 GH #352 追评**并重提上一轮未被裁的建议(升级为「该 harness 结构性无信息」)。

---

## 六、本波成本(账单侧欠账如实登记,不结算)

机时(两头拼,见第四节):**总计 2.1017 机时**
(8718 `0.1722` / 8764 `0.1714` / 8814 `0.8825` / 8939 `0.8756`)。

- **GH #454 (乙2) 机时腿:未触发。** 只取**「跑完自终」**的两台(被回收的两台
  **按条款构造排除** —— 它们正是该判据要绕开的那部分):
  均值 **`0.8790 h/台`** < 阈值 `1.00 h/台`。
- **本波 spot 计算估算 ≈ `$0.5673`**(逐台 `机时 × 该台发波时报价`:
  `0.1722×0.2782` + `0.1714×0.2594` + `0.8825×0.2817` + `0.8756×0.2585`)。
  ⚠️ **这是估算不是结算,且只含 spot 计算行** —— 不含 `DataTransfer-Out`、
  `PublicIPv4`、请求费。W56 的可见账单里那三项合计 ~$0.163,量级不可忽略。
- **GH #454 (乙1) 账单侧本轮无法结清**,理由在第二节:手上的 CE/budgets 快照
  (`refreshed 15:04:11Z`)**早于 W57 起飞 18 分钟**,含零。
  ⇒ **(乙1) 本轮登记为「无法读」,不是「未触发」。** 欠给下一轮。
  (按估算 `$0.5673` 远低于 `$1.10`,但**一个不含账单的数不许拿去满足账单侧判据** ——
  这正是章程 §2(乙) 禁掉的那个手法的近亲。)

---

## 七、收割义务与 queue/issue

- **S3 复扫**:`soak/` 下 `20260908` 前缀**恰好 12 条** = W55 四条(`032113`…)+
  W56 四条(`092131`…)+ W57 四条(`152158`…);前八条上两轮已重算,
  本轮重算 W57 四条(其中两条空)。`validation/` 最新对象停在 **09-07 22:18Z**,
  **无新对象** ⇒ **本轮无欠收**。
- **queue.json 未改**:全集例行波不对应单条请求;本轮零发波,无新请求要登记。
- **本轮开的 issue:0 新开**,两条追评(GH #403、GH #352),理由见第四/五节。
  两份草稿发表前跑 `claim_precheck.sh`,读数见第九节。

---

## 八、泄漏检查

开工 `check_costs.sh` 的 `running/pending` **为空** —— W57 四台**全部已终止**
(两台自终、两台被回收,SIR 四行全 `closed`,第四节逐行)。
本轮**零发波**,故无「起飞后复查」这一步;收尾再次确认账户级无在跑实例。
常驻仅 AMI `ami-0a990a26d89c66547`。**`final-table-trainer` 连续第四轮不在;
`fft-cache-v1-attempt3`(GH #528)本轮亦不在。**
⛔ **GH #515/#528 立案理由不变**:`CostFilters=null`,围栏仍读一个本台不控制的账号级数字。

---

## 九、铁律 6 / 11

- 静态门:见 §十(与自检一并回填)。**未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行。**
- 动态半(GH #124,~100min)**未跑,不声称**。
- `bots/` / `game/` / `tests/` / `tools/` **本会话一行未改**;只动 `iterations/` 下的
  报告、`W57_wave.json`、本章程「当前状态」节。
  ⭐ **本轮全程未创建 `bots/Customize/soak_side.lua`** —— 上一轮那条「照 traceback
  把门文件建出来 ⇒ 本地 armed 全部 44 个 candidate」的错误修复链,本轮结构上未再触发
  (无并发自检、未跑 `carrier_terms.py`,收割轮不需要载体闸)。
- **铁律 11**:全程无 `requires approval`,MCP GitHub 读写正常。**零空转** ——
  唯一的等待是自检自己的 ~18 分钟,期间本台在并行做收割,不是坐等。

---

## 十、自检 #2 与静态门

**`SELFCHECK2_EXIT=3`**,`legs run 10`,逐字:

```
FINDINGS (exit 3)     : cadence queue-rulings owed-executions
UNCERTIFIABLE (exit 2): trunk-red(python)
NOT RUN (inside a leg): tests/test_selfcheck_lua_leg.py
selfcheck worst exit: 3
```

- **FINDINGS 三项全属总监范围**(cadence / queue-rulings / owed-executions):
  按 GH #267 **只登记不处置、不做手工归因**。
- ⛔ **`trunk-red(python)` 本轮是 `UNCERTIFIABLE`,不是「已修复」**。
  python 腿的元测试在 120s 内没跑完,工具逐字:
  `UNCERTIFIABLE 5b/5c/5d/5e/5f/5g … the clean run did not finish inside 120s --
  these four did NOT run; 5a in particular would pass vacuously`,以及
  `UNCERTIFIABLE -- a python test did NOT run (could not read its input).
  This line is NOT a pass, and it is NOT evidence that trunk is red`。
  ⇒ **上一轮开的 GH #637(`origin/main` python 腿红,归因 `f7c77b9f` 的 +13 行)
  本轮无法确认是否仍红** —— 既不能说它修好了,也不能说它还红。
  ⚠️ 它给的补救建议(「在安静的树上重跑,`bots/` 下没有东西在写」)**本轮本来就满足**:
  全程单进程自检、`bots/` 一行未改、无 `soak_side.lua`。**所以这次超时不是树不安静造成的。**
- ⭐ **Lua 腿干净**:`86 tagged detector file(s), 0 failures`,
  **且全程没有 `soak_side.lua already exists` 那一行** —— 与上一轮被门文件污染的 Lua 腿
  形成对照,反证第九节「本轮未创建门文件」。

**静态门(铁律 6 前半)**:`ARM_EXIT=0`(`core.hooksPath=.githooks, already set`);
**`GATE_EXIT=0  CLEAN`**,`luacheck bots game: 0 warnings`。
**未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行。**
⚠️ 静态门**刻意排在自检之后跑**:它要读整个 `bots/`,而自检 Lua 腿会反复建删
`bots/Customize/soak_side.lua` —— 上一轮的门文件竞态正是这个形状,本轮结构上错开。

---

## 交棒

① ⭐⭐⭐ **总监 / harness(GH #403 追评)——「四 AZ 分散 ⇒ 单 AZ 事件只损失一粒」
现在有两个独立反例**(W55 `2b+2d`、W57 `2a+2b`,两次都精确到同一秒,AZ 对不同、
存活时长不同)。请裁:是否把这句话从收益预期里正式删掉,以及冗余预算要不要
按「一波可能损失两粒」重排。

② ⭐⭐⭐ **总监 / 录像组(GH #352 追评)——第 14 天,三波 494 局零少数侧**。
占比 W55 `0.0645` → W56 `0.0245` → W57 **`0.0000`**(回到地板,非新低)。
上一轮建议的「升级为该 harness 结构性无信息」**仍未裁**,本轮第二次提出。

③ ⭐⭐ **总监 / harness(GH #591)——那行写死的 `bracket 34.80 < x <= 40.63` 第三波零信息**。
W57 四台全部落在括号之外(10.3 / 10.3 / 53.0 / 52.5 min)。W56 已给过第一份反例。
它仍每波逐字打印,**仍长得像本波测量**。

④ ⭐⭐ **总监 / harness ——「提醒不是机制」连续第六轮**。本轮开工一条命令踩两个坑:
管道坑**第二十四轮**,以及 `timeout 900`(GH #514)——**后者是在读过上一轮逐字警告
之后仍然设的**。⇒ 建议把 `timeout` 下限与「不许管道」做进 `routine_selfcheck.sh`
的调用侧(它已经会拒管道;`timeout` 那一半还没有对应的门)。

⑤ ⭐⭐ **总监 / harness —— GH #631 的引信仍未修**(章程 §2 散文「第 3 行」,
实为第 2 行),本台**连续第三轮**靠现算 md5 绕开它。

⑥ ⭐ **本台下一轮 = 发波轮**。解锁 **`2026-09-08T21:22:18Z`**(⚠️ **不要抄,
跑 `wave_throttle.py` 读它自己打的那一行**)。发波前必须:
(a) **围栏算术把 W57 计为 pending** —— MTD `$68.225` 那张快照**早于 W57 起飞 18 分钟**,
结构上含零(第二节);(b) **回补 (乙1)**:届时 CE 应已含 W57,拉一次逐 `USAGE_TYPE`
把本波账单侧读数结清(第六节欠账);(c) 市场类型**用闸 (iv) 本轮打的 `NEXT WAVE: spot`**。

⑦ ⭐ **存量**(未变):#420 / #432 / **#388 的 W34 103 份 `.dem` 2026-09-22 到期(14 天)** /
#460 / #454 / #449 / #446 / #429 / #418 / #412 / #408 / #402 / #395 / #375 / #352 /
#329 / #313 / #290 / #271 / **#33** / #515 / #528 / #527 / #636 / #637 /
`strategy-5b` / **`strategy-31/32` 第十三轮不搭波**。

⑧ ⭐ **owner —— 本轮零支出、零跨档**。MTD `$68.225`(两源逐位一致,**但不含 W57**),
W57 估算 `~$0.57`(spot 计算行,非结算)。`$80` / `$90` / `$100` 均未跨。

---

## 十一、本轮 token 用量

`TOKENS total_in=6,914,500 out=40,895 turns=61`

(章程注:这是统计时刻的数字,统计后的收尾回合不计入。)
