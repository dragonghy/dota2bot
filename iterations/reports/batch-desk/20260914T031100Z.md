# batch-desk 2026-09-14T03:11Z — 持有第十四轮

**一句话**:零发波、零收割欠、零泄漏,本轮 AWS 新增计费 **`$0.00`**(EC2 `$0.00` + CE `$0.00`)。

⭐⭐⭐ **头号(本轮唯一的新**支出侧**测量,且它是一条判据缺陷不是一条花费事实):`budget refreshed` 戳
`2026-09-13T20:32:29Z` 连续第三轮逐位相同,冻结已达 `6.65h` —— 而本台**两条**写死的判据
(买 `$0.01` 逐日 CE、推 owner 通知)**第一个合取项都是「戳变了」** ⇒ 快照一冻,两条判据
**同时被焊死在关位**。要命的是被焊死的那条析取项正是「**MTD 越过 `$90`**」:
`MTD` 只能**经由这张快照**读到 ⇒ **最该被看见的那个事件,恰好在读它的通道断掉时变得不可见。**
⛔ **本轮不据此自行放宽任何一条判据**(见 §四/§五),只把它连同量到的数交给总监。

⭐⭐⭐ **并列头号(非本台的活,但它挡着所有组,所以本台抓到就点名):`origin/main` 现在是红的 ——
`tests/test_pending_rulings.py`(`810 checks, 1 failed`:BORN-DONE 行数 **7 > ceiling 6**),
而该测试**在铁律 6 的第二条腿(`py_gate`)上** ⇒ **每个组的下一次 push 都会被拒,而红不是它们造成的。**
归因指向 `c17e0ad8`(总监 `02:00Z` 那轮),⛔ 本台不代修、⛔ 不抬 ceiling,只点名 ⇒ §八bis / 交棒 ⑦。
⭐ 这是 GH #624 的立案形状第五例,而本轮的不同之处是它在 `1h11min` 内被下一个开工的组抓到。**

⭐⭐ **第二:上面那条「盲」本轮的决策代价实测为零,这是本台不去自救的理由,不是借口。**
闸 (iii) 已红、刹车已咬,本台**本来就发不了任何波** ⇒ 就算 MTD 真在冻结期里越了 `$90`,
**本台这一轮会做的事一模一样**。⇒ 花 `$0.01` 去解盲买到的是**零个改变的决策**,
与 GH #801 那条新分支自己打的那句 `Nothing can launch at this MTD, so confirming it buys nothing`
**逐字同构**。

---

## 一、⚠️ 本轮 MTD 是连续第三次「零信息读数」,必须先说否则后面每个数都读错

`COST_EXIT=0`(⛔ 未走管道,重定向到文件后裸读)。四个数**与 21:10Z、00:06Z 两轮逐位相同**:

```
89.505  USD
   source: budgets/dota2bot-batch (free) | forecast 181.993 | budget limit 100.0 | budget refreshed 2026-09-13T20:32:29Z
```

| 读数 | 21:10Z | 00:06Z | **本轮 03:11Z** |
|---|---|---|---|
| MTD | `$89.505` | `$89.505` | **`$89.505`** |
| forecast | `181.993` | `181.993` | **`181.993`** |
| limit | `100.0` | `100.0` | **`100.0`** |
| `budget refreshed` | `2026-09-13T20:32:29Z` | 同 | **同** |

⇒ **读的是同一张快照,第三次。** 余量 **`$0.495`** 因此**不是「6.65 小时没涨」,是「6.65 小时没测」** ——
⛔ 不得读成「支出曲线停了」,⛔ 不得读成「缺口停止扩大」。
`forecast 181.993` **原样登记不据以决策**。

⭐ **冻结时长本轮创新高**:上一张快照(`14:36:55Z`)被 15:17Z / 18:13Z **两轮**读过、跨度约 `3.6h`;
这一张已被**三轮**读过、跨度 **`6.65h`**(`20:32:29Z → 03:11:36Z`)。**这是本轮唯一的新支出侧测量**(另一条新测量在 §八bis,那条不是支出侧的)。

那四行逐字(**它是 SKIP 不是 pass**,⛔ 本轮 MTD 只有 budgets 一个来源):
```
>= $35, but headroom to the $90.00 brake is $0.495
< $1.10 (cheapest wave) — CE confirmation SKIPPED, NOT passed (GH #801).
Nothing can launch at this MTD, so confirming it buys nothing.
Resumes by itself as soon as headroom >= $1.10.
```
⭐ `COST_CONFIRM_AT` **未设**(默认值跑) ⇒ 走的是 GH #801 那条新分支,**恢复条件写在脚本里不在人的记忆里**。

`AWS_SETUP_EXIT=0`:容器无 CLI,自装 `aws-cli/1.46.1 Python/3.11.15 botocore/1.43.62`;
身份逐字 `arn:aws:iam::581436534924:user/dota2bot-agent` + `(restricted batch-runner user — correct)`。
常驻成本仍只有 AMI `ami-0a990a26d89c66547`(`dota2bot-batch-20260718`),无快照行。

## 二、闸

**(iii) 围栏/刹车 —— `--planned 1.10` ⇒ `FENCE_EXIT=3`**(连续第六轮是**闸拒绝**,不是本台的处置):
```
actual (MTD)     : $89.505      pending waves : $0.000
planned          : $1.100       projected     : $90.605
fence $100.00 / brake $90.00 / operative ceiling $90.00
WAVE_FENCE: THROTTLED (exit 3) -- brake, not fence. Stop and report to the owner; do not wait it out.
```
缺口 **`$0.605`**,⛔ **与上一轮逐位相同,而那是「同一张快照」的直接后果,不得读成任何趋势**。
告警档:`$50` **crossed / AWS state=ALARM**、`$80` **crossed / AWS state=ALARM**、`$100` `NOT yet crossed / OK`
⇒ **owner 侧两封预算告警邮件均已发出**,不需本台代转。
⛔ 未用 `--no-accrual-check` / `--no-crossing-file` / `--director-crossing` / `--snapshot-instant` / `--pending`
⇒ **报告里无任何「SKIP 不是 pass」行**;⭐ 无 `WAVE_FENCE CLOCK` 限定行(`clock from budget snapshot`)
⇒ **GH #692 epoch 修复仍生效**。`crossing registry: 0 record(s) ... none in force`。

**(i) 节流 —— `THROTTLE_EXIT=0`**:锚 **W69**(4 台,`first 09-12T09:26:24Z` / `last 09-12T09:26:31Z`,
`slate spread 7s`),`unlock 2026-09-12T15:26:31Z`,`margin +128795s`。
⚠️ **开着不构成许可**(连续第九轮点名;工具自带 `RE-RUN IT AT THE LAUNCH INSTANT:
it is the action, not the decision, that the gate binds`)。

**(ii)/(iv) 本轮未评估,也不声称跑过**(闸 (iii) 红 ⇒ 任何波都发不出去,评估它们买不到决策)。

## 三、⭐⭐ 那 `$0.01` 本轮没花 —— 按上一轮写死的字面判据,不是按本台的判断

上一轮交棒 ② 逐字:「**两条判据都要过**:`budget refreshed` 戳**变了** **且** 时点在 `02:00Z` 之后」,
并逐字写死了本轮的处置:「**下轮若戳仍未变,照样不许买**」。

| 判据 | 本轮 | |
|---|---|---|
| 戳变了 | `20:32:29Z` 第三次逐位相同 | **✗** |
| 时点 > `2026-09-14T02:00Z` | `03:11:36Z` | ✓ |

**合取式 ⇒ 不买。** 逐日 CE 表本轮**未拉**,本报告**不含任何 09-13/09-14 逐日读数**。
⚠️ **零批测日(09-13)完整行的欠条仍欠** —— 它今天第一次「时点可得」,却卡在戳这一条上。

⚠️ **本台必须把这条判据的前提说清楚,因为它本轮第一次被压到了受力点上。**
该判据的理由逐字是「戳没变 ⇒ **逐日表也是旧的一张**」。⛔ **这个前提至今只有一次共动观测**:
21:10Z 那轮戳从 `14:36:55Z` 换到 `20:32:29Z`,同时 09-13 行从 `$0.07536` 涨到 `$0.4504`。
**一次共动分不开「两个 API 联动」与「两个 API 各自独立地在那 6.5h 里都动了」** ——
而 `budgets` 与 `ce get-cost-and-usage` 是**两个独立 API**,逐日表并不取自那张快照。
⛔ 这与本章程步骤 2 **(乙)** 亲自禁掉的那个手法(「MTD 增量对得上波费 ⇒ 该波已计入」,
它已两次给出**方向相反**的错误结论)**是同一个动作**:拿单次吻合当耦合的证明。
⛔ **本台不据此自行放宽判据、本轮一分未花**;这条交总监(§五 交棒 ②)。

## 四、⭐⭐⭐ 通知本轮没推,以及本轮量到的那条判据缺陷

**判据**(21:10Z §八 逐字记下的那条):
> `budget refreshed` 戳变了 **且**(headroom 跨过 `$1.10` **或** 零批测日完整行第一次可得 **或** MTD 越过 `$90`)。

**本轮实际**:戳**未变** ⇒ **第一个合取项就不成立** ⇒ **不推**;顺带三个析取项也一条不中
(headroom `$0.495` 未跨 `$1.10`;完整行未取得,§三;MTD `$89.505` 未越 `$90`)。
⭐ 这是对上一轮自报偏离的**连续第二轮**守字面:**总监尚未裁**,无裁定期间取**保守默认 = 守原文**,
⛔ 不自行放宽、⛔ 不拿「内容有没有新东西」做替代判据。
⛔ **同理未在 GH #779 发第 12 条评论**(立的是「有新测量才发」;本轮那条新测量是**判据侧**的,
见下,它该去总监不该去 owner 的刹车帖)。

**⭐⭐⭐ 但本轮量到了这条判据的一个结构性缺陷,登记如下(⛔ 是交棒,不是本台的自我授权)**:

1. 判据是**合取式**,第一个合取项是「**戳变了**」。
2. 三个析取项里最要紧的一条是「**MTD 越过 `$90`**」。
3. **MTD 只能经由那张快照读到**(本轮 §一:CE 那条旁路也被 §三 那条判据的同一个合取项关着)。
4. ⇒ **快照一冻,「MTD 越过 `$90`」这条析取项在算术上不可达** ——
   不是「暂时没发生」,是「**发生了也读不到**」。
   **判据的失效条件(读不到 MTD)与它要抓的事件(MTD 越线)走同一条通道**,
   与 `pullcad`「promote 一个 id 会静默冻死引用它的 gate」**同族**:门在,但被结构性绕开。
5. ⛔ **它不是假想**:戳已冻 **6.65h / 三轮**,而余量只有 **`$0.495`**。

⭐⭐ **本台不自救,理由是算术不是纪律(§二 的第二条)**:闸 (iii) 已红、刹车已咬,
本台**本轮本来就发不了任何波** ⇒ **冻结期里 MTD 有没有越 `$90`,对本台这一轮的行动零影响**。
解盲能买到的**改变的决策数 = 0**。⇒ **按 GH #801 自己那句话,这笔钱不该花**;
两条判据即使都焊死在关位,**本轮的实际代价也是零**。
⚠️ **但这个「代价为零」是刹车期的性质,不是判据的性质** —— 刹车一解除,同一条缺陷立刻变成
「发波前读不到 MTD」,那时它就不是零代价了。**这正是要在刹车期里就把它交出去的原因。**

## 五、收割:零欠

`S3VAL_EXIT=0`(⛔ 未走管道)。`s3://dota2bot-batch-results-4924/validation/` **592 对象**,
与上一轮**逐个相同**;最新对象仍是 `tpcommit+46ids-24c30245be01_20260907_2218_run.log`(`2026-09-07 22:18:52`)
⇒ **零新对象、零下载、零 S3 支出**,⛔ 不重算已收波次。

`queue.json` **122 条**:`pending 48` / `harvested_pending_verification 29` / `ruled_approved_scan 11` /
`done 9` / `harvested 6` / `frozen_hold 5` / `delivered-and-consumed 4` / **`running 3`** / `ruled 3` /
`returned_uninterpretable 1` / `rejected 1` / `registered 1` / `open 1`。
`running 3` 仍是 `strategy-31` / `strategy-32` / `hero-27`(上一轮已查实:**没有一条需要一台机器**,
与账户级 `CERTIFIED ZERO` 并不矛盾,是 `running` 一词同时承载「有实例在跑」与「某组分析进行中」)
⇒ ⛔ 本轮**不重复点名**,该不该拆词仍在总监手上。
**零条 pending 在申请专波** ⇒ 本轮无任何被队列拉动的发波需求。

## 六、泄漏:四条独立路径全零(⛔ 不合并成一句)

| # | 路径 | 读数 |
|---|---|---|
| ① | `check_costs.sh` 自带 `running/pending instances` 区块 | **空**(⭐ 直接用它自带这段,未为取它重跑) |
| ② | `describe-instances` 五态(`pending,running,shutting-down,stopping,stopped`)**不加 tag 过滤** | **零行**,`INST_EXIT=0` |
| ③ | `describe-volumes` 全账户 | **零行**,`VOL_EXIT=0` |
| ④ | 闸 (iii) 账户级普查 | `accrual scope : 17 region(s) read <- COMPLETE`(账户已启用的每个区域)+ `accrual check : CERTIFIED (0 accruing instances account-wide, read this run) -- pending $0.000 is a reading, not a default.` |

⛔ **#515/#528 的立案理由一字未变**:`budget filters : none` ⇒ **账户里每一台实例都落进这个 budget,不论归谁**。

## 七、铁律 9(owner 优先项)

`OWNER_PRIORITIES.md:99/:139` 至今逐字带着「**4.1 upstream 标尺波(最高优先,批测台下一轮就发
…… 必须发标尺波,排在任何 arm 波之前);当前球:4.1 批测台**」,而 `DECISIONS_NEEDED.md:436–441`
(总监 **2026-09-05T19:xxZ**,GH #535)已裁 **P4.1 = 部分完成**,且**「排在任何 arm 波之前」的排序条款已释放**。
⇒ **条文与裁定不一致仍在**(上一轮 §七 已登记,本轮**未见任何改动**)。
⛔ **本台不代改 `OWNER_PRIORITIES.md`**(owner 原话:只有主会话维护),⛔ 不宣布 P4.1 已完成
(裁的是**部分完成**;(A)/(B) 完成定义的拍板**至今未收到 owner 回复**)。
⚠️ 顺带:本轮**不再自报「未认领 4.1」** —— 上一轮已查实那是**按已释放的那半条文背了一条不存在的欠账**;
且闸 (iii) 红 ⇒ 标尺波与 arm 波**一样发不出去**。

## 八、自检

⭐ **本轮第一条命令就踩中管道坑(第 30 次)**,脚本自卫当场拦下、零损失,逐字
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.` + `SELFCHECK_EXIT=2 REFUSED`。
⚠️ **上一轮交棒 ⑦ 的「开工第一条命令一次到位」本轮仍未做到**(⛔ 不辩解)——
第二跑用了重定向但**带了 300s 前台超时**,被搬进后台;⭐ **但这次没再重跑第三次**,
而是直接读它的重定向日志 ⇒ **比上一轮少一步,仍不是一步**。

**已跑完的腿(⛔ 后台跑完才读,不预写;真码见补记)**:
- `9 anchor(s) checked -- OK`(含 `stable-v9 ids=zusult,zusboltdom`,`EXISTS/PINNED/SHIPPED` 全 ok)
- `promote-atom constraints: OK` —— `scanned: 224 distinct live gate id(s), 19 carrying a PROMOTED note`,
  `FROZEN none`,5 atom 全 GATED
- `INVERSE-GATE CENSUS  live gate ids 219  armed 26` / `FROZEN 0  COUPLED 1  UNRESOLVED(armed) 0`
  ⭐ **活 gate 218 → 219**,armed **仍 26**;`COUPLED` 仍是 `fieldsip`(挂在 `buyband`/`buydeep`/`buyring`/
  `buytower`/`fieldbuy`/`stayfield`/`stayfield2` 下),`no armed id hangs under an unarmed gate -- OK`
- `LUA GATE COVERAGE  disk 452 | push gate 322 | 开工自检 leg 90 | known_red 9`,
  `UNCOVERED 114 of 452 (25%)  {'no_manifest_row': 51, 'timed_out': 61, 'too_slow': 2}`
  ⚠️ `UNCOVERED SET GREW` —— **1 个新文件**:`NEW UNCOVERED  no_manifest_row  tests/test_dusttower_dive_guard.lua`
  (协同组 `dusttower`)。⭐ 上一轮点名的两个里,`tests/test_cm_r_retreat_crowd.lua` **本轮已不在新增名单**;
  `dusttower` **连续第二轮**没有任何自动读者会跑它。
  ⛔ **本台不代登记、不新开 issue** —— 修复路径已被 **GH #783** 立案(登记一条新 Lua 测试的唯一被许可动作
  re-measure 会清空 `known_red` 赦免名单),**GH #804 / #806** 在同一条缝上,**再开就是重复立案**。

### 八bis、⭐⭐⭐ `main` 现在是红的,而红的那条测试**在 push 闸里** —— 即**每个组的下一次 push 都会被拒**

自检 python 腿本轮是 **FINDING 不是 UNCERTIFIABLE**(上一轮它**没跑成**,⛔ 那一侧上一轮没人看过),
逐字 `failed: tests/test_pending_rulings.py` + `TRUNK RED -- a python test is failing ON THE WORKING TREE.`

**⛔ 本台没有停在自检那一行上,而是**单独直跑该测试拿第一手读数**(`TPR_EXIT=1`,⛔ 未走管道)**:
```
810 checks, 1 failed
FAIL: 7 live registry rows read BORN-DONE (a_evidence_pulldrag, a_evidence_tpgap,
gh692_epoch_clock_first_live_read, gh699_materiality_gate_first_live_read,
instrument_ownhalf_a_evidence, lua_gate_baseline_e2e, promote_bar_paired_seed_depth)
but the ceiling is 6 -- a criterion that has never been observed to say anything but
DONE cannot be retired on that reading; fix the criterion, or record the OWED reading
taken at ruling time
```

**⭐ 「是不是 main 也红」本台是证出来的,不是推出来的**(自检那行自己声明 `Whether main is red
too is NOT established by this line`):`git status --short` 全树**只有一行** ——
`?? iterations/reports/batch-desk/20260914T031100Z.md`(**未跟踪**),**零个已跟踪文件被改**;
且 `git ls-remote origin main` = `22f7539307…` = **本地 HEAD** ⇒ **被测的那些文件与 `origin/main` 逐位相同**
⇒ **这条红就在 `origin/main` 上。**

**⭐ 归因(是点名不是判决)**:`git log -- iterations/owed_executions.json` 的最新一条是
**`c17e0ad8` 「director 2026-09-14T02:00Z: the re-measure ran, and the round's product is that it
must NOT land」**(结清 `gh806_lua_manifest_remeasure`)⇒ **那一轮把 BORN-DONE 行数从 6 顶到 7**,
棘轮当场红。⚠️ **⛔ 本台不主张那一轮做错了**(该轮的产物恰恰是「**不要**落地那份 manifest」,
理由是受控实测);本台主张的只有一件事:**红留在了 main 上,而写红的人已经走了。**

⚠️ **`tests/test_pending_rulings.py` 在 `tools/agent/py_gate_manifest.json` 里** ⇒ 它**在铁律 6 的第二条腿上**
⇒ **下一个 push 的组会被拒**,而红**不是它造成的**。**这正是 GH #624 的立案形状,数到第五例,
本轮的特殊之处是它被下一个开工的组(本台)在 `1h11min` 内抓到,而不是几小时后。**
⛔ **本台不代修**:那行自带的两条出路(`fix the criterion` / `record the OWED reading taken at ruling
time`)**都是裁定动作**,属总监;⛔ **尤其不把 ceiling 从 6 抬到 7** —— 棘轮存在的理由就是逼那条判据被修,
抬顶就是把它拆掉。⛔ **本台不新开 issue**(GH #624 已是这条缝的立案,见交棒 ⑦)。

## 九、交棒

① ⭐⭐⭐ **owner —— 刹车第十四轮持有,GH #779 第十一轮零表态。**
⚠️ **本轮零新支出测量**(快照第三次逐位相同)⇒ **既未推通知也未发第 12 条评论,判据如此**(§四)。
三条选项逐字不变,⛔ 本台不建议、不预选、不自行改线,**已挂 `DECISIONS_NEEDED.md` §15,不重复立条**:
维持 `$90` / 给 `dota2bot-batch` 加成本分配标签过滤器(`budget filters : none`,MTD 的 **41.8%** 是
外来机型 `c7a.16xlarge`+`r7a.16xlarge`,扣掉后本台九月 MTD ≈ **`$52.07`**)/ 一次性解禁到某个数
(并说明是否含外来支出)。

② ⭐⭐⭐ **总监 —— 本轮的新东西全在这里,请一次裁三条(它们是同一条缝的三个面)**:
   - **(甲) §四 那条结构性缺陷**:通知判据的合取项「戳变了」**让析取项「MTD 越过 `$90`」在算术上不可达**
     (MTD 只能经由那张快照读到)。戳已冻 **6.65h / 三轮**,余量 `$0.495`。
     ⚠️ 本轮实际代价为零**只因为刹车已咬**(发不了波 ⇒ 解盲改变零个决策);**刹车一解除它就不是零代价**。
   - **(乙) §三 那条前提**:「戳没变 ⇒ 逐日 CE 表也是旧的一张」**只有一次共动观测**,
     而 `budgets` 与 `ce` 是**两个独立 API**;拿单次吻合当耦合的证明,正是本章程步骤 2 **(乙)** 亲自禁掉的手法。
     ⇒ 请裁:该不该给这条判据加一个**与戳无关的旁路**(例如「戳冻结 ≥ N 小时 **且** 余量 < 一个最便宜波」时,
     允许花 `$0.01` 走 CE 直读 MTD)。⛔ 本台不自拟阈值、不自行改判据。
   - **(丙) 21:10Z 交棒 ④ 那条仍欠裁定**(通知判据放宽一项 vs 本台守字面)。
     **本轮是连续第二轮守字面且结果正确**(零新支出测量,推了就是同形投递)⇒ **不紧急,但 (甲) 让它变得更值得一裁**:
     若按 (甲) 加旁路,这条大概率一并解决。

③ ⭐⭐ **下一轮本台 —— 判据一字不改地继续执行,并把戳的冻结时长当成一个要登记的量**:
   买 `$0.01` 仍要**两条都过**(戳**变了** 且 时点 > `02:00Z` —— 后者从本轮起永远成立,
   **实际上只剩戳这一条**);推通知仍要**合取式**过。
   ⚠️ **戳若再冻一轮,冻结时长 ≥ 9.5h,请在报告里把它作为 (甲) 的第二个数据点登记**(不是作为放宽的理由)。

④ ⭐ **总监 —— `queue.json` 的 `running` 词表歧义**(上一轮已点名并查实结论反转):
   三条 `running` 没有一条需要机器 ⇒ 与 `CERTIFIED ZERO` 不矛盾。请裁该不该拆词,⛔ 本台不代改状态。
   ⚠️ `hero-27` 自带逐字「GH #388 的 W34 语料 `dem21/` **2026-09-22 自动删除**」⇒ **距今 8 天**而它仍 `running`。

⑤ ⭐ **owner/总监 —— 刹车解除的收益仍比「跑一个 id」大得多**(逐字不变,列在收益侧不作为催促):
   `queue.json` 开放约 111 条,**零条在申请专波**,二十几条搭车行逐字是「任一常规波次」/「下一波例行全集串」
   ⇒ **一发例行全集串,它们一次全部上机**。

⑥ ⚠️ **协同组 —— `tests/test_dusttower_dive_guard.lua` 连续第二轮 `NEW UNCOVERED`**;
   红了只会被**下一个开工的组**发现(GH #624/#806 的立案形状),修复路径卡在 **GH #783**。⛔ 本台未开新 issue。

⑦ ⭐⭐⭐ **总监(急,且它挡着所有组)—— `origin/main` 红在 `tests/test_pending_rulings.py`,而该测试在 push 闸第二条腿上**(§八bis)。
   读数已证到 `origin/main`(工作树**零个已跟踪文件**被改 + 本地 HEAD == 远端 tip),归因指向 **`c17e0ad8`**
   (总监自己 `02:00Z` 那轮结清 `gh806_lua_manifest_remeasure`,把 BORN-DONE 行数顶到 **7 > ceiling 6**)。
   两条出路**都是裁定动作**(`fix the criterion` / `record the OWED reading taken at ruling time`),
   ⛔ 本台不代修、⛔ **尤其不抬 ceiling**(棘轮就是为逼那条判据被修而设,抬顶等于拆掉它)。
   ⚠️ **在它被修好之前,每个组的下一次 push 都会被第二条腿拒**;本轮本台自己那次 push 的真码见补记。

⑧ ⚠️ **下一轮本台 —— 开工第一条命令一次到位**(重定向 + 后台 + **无 `timeout`**)。
   本轮**两步**(上一轮三步),仍不是一步。

## 十、铁律 11 与改动面

**零 `requires approval`、零空转**(自检后台跑,成本 / 闸 / 收割 / 泄漏 / 铁律 9 并行推进)。
⛔ **本会话 `bots` / `game` / `tests` / `tools` 一行未改** ⇒ **无一条 trunk red 由本轮引入**。
⭐ **每个读了退出码的命令全部重定向到文件后裸读,零管道**
(`AWS_SETUP_EXIT` / `COST_EXIT` / `FENCE_EXIT` / `THROTTLE_EXIT` / `S3VAL_EXIT` / `INST_EXIT` / `VOL_EXIT`)。
⭐ **报告序列按 `ls | sort` 取得,⛔ 未用 `ls -t`**(git clone 的 mtime 全同)。
远端核对照做:`git ls-remote origin main` = `22f7539307fa7b4f3d2f36e0104cb8c0965a601c` = **本地 HEAD**,零漂移。

## 十一、Token 用量(铁律 8)

```
TOKENS total_in=4,325,290 out=37,370 turns=40
```
(统计时刻之后的收尾回合不计入。)
