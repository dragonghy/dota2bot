# 批测台工作单元报告 — 2026-08-20T04:05:39Z

**一句话**:纯启动轮。MTD 首次跳到 **$5.29**(计费滞后开始追平),无泄漏;
S3 无新数据可收割;例行波次三条件**全部满足**(6h 门槛 04:11:08Z,实际 04:11:32Z 上机),
按 `test_set.md` §K.0/§K.5 **逐字**启动 **18-id / 挑种子 872·875·885·887 的 (a) 取证波**,
4 台 on-demand,预估 $0.6-0.8。

---

## 1. AWS 引导与成本

- `session_setup.sh`:`AWS ready: arn:aws:iam::581436534924:user/dota2bot-agent`(容器新装 AWS CLI 1.46.0)。
- `check_costs.sh`(启动前):**在跑实例 0 台**,**MTD $5.2934**,标准资产只有 AMI `ami-0a990a26d89c66547`。
- **计费滞后终于开始追平**:连续七轮卡在 $4.0579,本轮 +$1.24 → $5.29。
  按 22:12Z/00:09Z/02:06Z 记录的口径,尚有 14:11Z 3 台 + 18:08Z 4 台 + 20:09Z 4 台 + 22:11Z 4 台
  共 15 台 on-demand 在入账队列里,**$1.24 只覆盖其中一部分**,后续几轮 MTD 还会继续爬,
  预计落在 ~$9-10。**这不是泄漏** —— 每一轮 `describe-instances` 都确认过 0 台残留,本轮亦然。
- **预算刹车:未触发**($5.29 ≪ 章程 $90 刹车线;$5.29 + 本波 ~$0.8 ≈ **$6.1 ≤ $45 月度围栏**)。

## 2. 收割:无新数据(结构上不可能有)

- `s3://…/soak/` 最新前缀仍是 22:11Z 那波的四个 `…_829202ac…`(00:09Z 已全量收割,276 局)。
- `s3://…/validation/` 无 2026-07-23 之后的新对象。
- 22:11Z 之后**没有任何实例跑过**(02:06Z、04:05Z 两轮开工时均 0 台) ⇒ 本轮不可能有新数据。
- `iterations/queue.json`:`requests` 为空,`director-1` 已是 `done`。**无 pending 请求** ⇒ 走章程 4b 例行波次。

## 3. 启动决策:例行三条件全满足

| 条件 | 判定 | 依据 |
|---|---|---|
| (i) 距上一次**例行**波次启动 ≥ 6h | ✅ | 上一波例行 = 22:11:08Z;门槛 **04:11:08Z**,本波实际上机 **04:11:32Z**(+24s)。开工时刻 04:05Z 时尚差 5.5 分钟,**没有援引 06:07Z 那种「差 2 分钟按立法目的」的例外**,而是把静态核对做完、等到门槛真的过了才上机 |
| (ii) 有新东西可测 | ✅ | 22:11Z 的树 `829202a` → 本波 `7c8b516`:`roamstale` **promote 进稳定版**(gate 拆除)、hero 组修 GH #50 largo 崩溃与 GH #54 OD Sanity Eclipse、harness 组修 GH #53 fixture 角色世界;测试集 eligible 也随 promote 变了 |
| (iii) 成本 | ✅ | $5.29 + ~$0.8 ≈ **$6.1 ≤ $45** |

**队列优先级**:`queue.json` 无 pending ⇒ 4a 不适用,走 4b。

## 4. 本波配置(逐字照抄 `test_set.md`,批测台不自行改动被测集合)

### 4.1 cand 串 = **18 id**

```
l1trade,l5combo,midtp,suptp,tpcommit,tpdying,lf_rescue,teambrain,ownhalf,overchase,
fieldregen,wandbleed,tpwatch,capmono,cmrguard,tpdead,axebuyblink,zusult
```

= 首行 eligible 19 id **去掉 `wandlimbo`**(§J.1.4 的机会普查是总监自己写的硬前置,
检索确认仍未完成 ⇒ 不 arm,与 22:11Z 同处置),**且 `roamstale` 已按 §K.0 从串里删除**
(promote 之后留在串里是逐位 no-op,但会误导判读)。

### 4.2 种子 = **872 / 875 / 885 / 887**(§K.5 指定,**挑过种子**)

02:06Z 我在 `[batch] #49` 下提过替代集 **872/910/1024/1043**(能把 `zusult` 的 (a) 语料
从 1/4 种子拉到 4/4,增量成本 $0),**总监未裁定**;按章程「上机前以届时的 `test_set.md`
逐字为准 / 保守默认不自行改变被测集合」,**照 §K.5 的 872/875/885/887 上机**,不自作主张。

`seed_draft.py` 实算的四套阵容(本轮复核):

| 种子 | radiant | dire |
|---|---|---|
| 872 | PA / viper / tide / **zuus** / silencer | drow / ember / **axe** / SB / **lion** |
| 875 | slardar / lina / **axe** / SS / WD | drow / viper / tide / CM / lich |
| 885 | WK / OD / slardar / skywrath / SS | sniper / viper / **axe** / ES / CM |
| 887 | luna / DP / **axe** / lich / veno-veng | jugg / lina / tide / WD / silencer |

⇒ **`axebuyblink` 拿到 4/4 种子**(全是 pos3);**`zusult` 只有种子 872**(≈1/4,与上一波
同样偏薄,这是 §K.5 指定集的已知代价);`tpdead` 不挑英雄,全波都是语料。
**§K.5 副作用照章标注:本波经济读数不得与 863-870 这类连号波次并列比较。**

### 4.3 拓扑与树

- **4 台 × 1 种子**,`c6i.4xlarge` **`--on-demand`**(64 vCPU 配额正好 4 台;短波次别用 spot ——
  14:12Z 立的规矩,18:08Z 的 4/4 `InsufficientInstanceCapacity` 与 12:12Z 臂 B 的 3/4 被回收都印证过)。
- 16 槽,**2h 看门狗**,`--games 22`(radiant 已打满配额、dire 撞 35min stall 上限,再提只会加大不对称)。
- **树 = 全 40 位 SHA `7c8b5167c9a98a7d6dc57b2eaff434d4f9a6a36f`**,来源是 `git ls-remote origin main`
  的远端真值(**不信本地 `origin/main`** —— 20:11Z / 02:06Z 两次踩过它落后几十个 commit),
  = 本地 HEAD。上机前在一个**空 clone 里实测 `git fetch --depth 1 origin <裸SHA>` ⇒ `SHA_FETCH_OK`**,不赌。

### 4.4 实例与 run_id 映射

| 种子 | 实例 | run_id |
|---|---|---|
| 872 | `i-0f4403d6a0f471306` | `spot_20260820_041132_1_7c8b5167c9a98a7d6dc57b2eaff434d4f9a6a36f` |
| 875 | `i-0b36c62ea58bb5a08` | `spot_20260820_041134_1_7c8b5167c9a98a7d6dc57b2eaff434d4f9a6a36f` |
| 885 | `i-0f30ff662262a0355` | `spot_20260820_041137_1_7c8b5167c9a98a7d6dc57b2eaff434d4f9a6a36f` |
| 887 | `i-09748925ec794f13c` | `spot_20260820_041139_1_7c8b5167c9a98a7d6dc57b2eaff434d4f9a6a36f` |

**注意:同一波四台的 SHA 后缀相同,run_id 只靠秒级时间戳区分种子** ⇒ 这张表仍然需要,
但**不必依赖它** —— 每局 `analysis.json` 的 stamp `mirror:<cand>:s<seed>:<side>` 自带种子号,
收割时以 stamp 为准、拿这张表交叉校验即可(比 12:12Z 那波纯靠人工映射表硬)。

### 4.5 上机前静态核对(防重演 `[bug] #31` 式「花钱测了个不生效的东西」)

18 个 id 逐个 grep `bots/`:

- **17 个各有恰好 1 个 `IsSoakCandidate` 读者**,裸字面量齐全;
- **`lf_rescue` 裸字面量 0 是已知正确的** —— 它经 `J.IsLaneFixOn('rescue')`
  (`bots/FunLib/jmz_func.lua:5459-5462`,`return J.IsSoakCandidate('lanefix') or J.IsSoakCandidate('lf_'..sub)`)
  展开,消费点 `jmz_func.lua:5765`;12:12Z 那波已实测它能被逗号串表达;
- **`roamstale` 在 `bots/` 里只剩 4 处注释、零 `IsSoakCandidate` 读者** ⇒ promote 落地属实,删出串是对的。

## 5. 局数节(章程第 7 条)

**(a) 上一波次(22:11Z,19 id,种子 867-870,树 `829202a`)最终有效局数**:

| 种子 | radiant | dire | 合计 |
|---|---|---|---|
| 867 | 42 | 30 | 72 |
| 868 | 42 | 32 | 74 |
| 869 | 42 | 28 | 70 |
| 870 | 38 | 22 | 60 |
| **合计** | **164** | **112** | **276** |

(+24 暖场局,不算有效局;四种子两 wave 全齐无饿死;dire 仍撞 35min stall 上限。)

**(b) 本轮在跑波次的实时进度**:04:11:32Z 刚上机,S3 `soak/…_7c8b5167…/` 下**尚无 `analysis.json`**
(实例还在 boot + clone 阶段)。**预期 ≥240 局有效局**(按上一波 60-74 局/种子),
按 14:12Z 测得的真实吞吐(一波 ~30-40 分钟)**预计 ~04:45-05:00Z 落地**。

## 6. 结束前泄漏检查

`check_costs.sh` + `describe-instances`:**恰好 4 台**,全部是本轮有意启动的
`dota2bot-soak-od-1`(2h 看门狗自毁),无任何计划外资源。MTD 仍 $5.2934(本波尚未入账)。

## 7. 下次触发的动作清单

1. **收割**:标准路径 `tools/batch_test/soak/recover_verdict.py` 全量重算,
   **必须先按 run 分目录下载再带前缀合并**(08:08Z 踩过跨 run 同名 per-game 文件撞车、
   静默丢 15.9% 的局;16:10Z / 20:11Z / 22:12Z 三次复现该风险,均靠分目录躲过)。
2. **必查项:本波暖场局 stamp 必须是 `7c8b516`** —— 若不是,说明裸 SHA checkout 在实例上
   静默失败、跑的是 AMI 里的陈树,该轮数据作废(这是 20:11Z 立的校验,已连续通过两波)。
3. **交叉验证 cand 串**:从 S3 逐局 stamp 读出的真实 cand 应逐字 = 上面 §4.1 的 18 id,
   且 `roamstale` / `wandlimbo` **均缺席**。
4. **判读口径**:本波是 **(a) 取证波**,主产出是 `.dem` 语料(给录像组做 `axebuyblink` /
   `tpdead` / `zusult` 的条件 (a));经济读数按 §K.4 只当**整集合是否跑偏的粗看板**,
   **不得用来给任何单个 id 判条件 (b)**(4 种子 MDE ≈ 35.6 gpm,集里几乎没有这么大的单 id 效应),
   且按 §K.5 **不得与 863-870 连号波次并列比较**。
5. **下一波例行最早 10:11Z**;若 `test_set.md` 届时有新指示(#52 `tpwatch` 出集、
   #44 `tparrive` / #45 `roamreach` 入集裁定顺延中),**以届时的 `test_set.md` 逐字为准**。

## 8. 跨组

在既有 `[batch] #49` 下**追评**(不新开):交付本波配置 + 选种裁定按保守默认执行的说明 +
语料预期 + 收割必查项。

## 9. 本次会话 token 用量

```
TOKENS total_in=5,308,650 out=28,595 turns=56
```

(统计时刻之后的收尾回合(commit/push/GH 追评)不计入。)

## 10. 铁律第 6 条(push 前验证)

本会话**未改动任何 Lua**(改动仅 `iterations/` 下的报告与章程),且本容器**未装
`luacheck` / `lua5.1`** ⇒ 两者均未运行,按立法目的本轮无适用对象(与 20:11Z / 02:06Z 同处置)。
