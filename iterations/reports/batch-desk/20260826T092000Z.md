# batch-desk 2026-08-26T09:20Z — W13 发波(界后第四波),外加一条**发波前置查出来的结构性零腿**

一句话:例行三闸全过,W13 已发(4 台,树 `a883d7c6`,36-id 全集,种子 888/895/896/906);
**本轮的实质产出不是那一波,是发波前置核查在 armed 串里查出 `zusstatic` 是一条结构性零腿**
—— 它的合取依赖 `zusbind` 不在串里,而接线门对这种死法**按构造看不见**(GH #207)。

---

## ① 自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` → **worst exit 3(有发现,非阻断)**。

- `unlanded_commits.py`:**OK,certifiable 窗口内无未落地工作**(40 条可证 ref;
  366/406 因浅 clone 无 merge-base 被 REFUSED —— 按工具自己的 LIMITS,这是**拒答不是通过**)。
- `citation_audit.py` cadence:3 处洞,**其中一处是本台自己的**
  —— `batch-desk 08-25T06:17Z → 15:11Z 共 8.9h`。另两处是 replay-check(3.8h / 3.7h)。
- `stable-v1` / `stable-v2` 两个锚点 **EXISTS / PINNED / SHIPPED 全 ok**。
- trunk 健康:python 32 passed 0 failed;快 Lua 检测器 8 文件 0 失败
  (`lua5.1` 缺失时自装,实测符合 §GH #171 更正后的描述)。
- 队列:`RIDESHARE(§BB.4 本轮该裁)1 条 —— strategy-20 / abilanc`。**归总监,本台只转达。**

## ② 成本 / 围栏算术(铁纪律(甲),逐波按界别取价)

`check_costs.sh`(开工):

| 项 | 值 | 依据 |
|---|---|---|
| MTD(budgets,免费) | $41.182 | 刷新 **2026-08-26T02:13:56Z** |
| MTD(CE,自动 $0.01 复核,因 ≥$35) | **$43.4628** | **与上一轮不同:上一轮两者「逐位一致」于 $41.182,本轮 CE 高出 $2.28** |

⚠️ **`budgets` 那张快照的刷新时刻(02:13:56Z)与上一轮**逐位相同** ⇒ 它这 7 小时没有前进,
而 CE 前进了 $2.28。**取大者(CE $43.463)作围栏基数**,这是保守方向。

- 12h 窗口(现在 09:13Z ⇒ 回看到 08-25T21:13Z)内的波次:
  - **W11** `21:10:16Z` —— 严格说**在窗外 3 分钟**;**照样计入**(铁纪律(甲)的立法理由是
    「滞后让 MTD 系统性偏低」,卡边界的那一波往危险方向失效)。
  - **W12** `03:10:52Z` —— 窗内。
  - **W13** 本波 —— 窗内。

| 项 | 值 |
|---|---|
| 基数(CE) | $43.463 |
| W11 / W12 / W13 各 +$2.15(界后**在用**单价) | +$6.45 |
| **围栏值** | **$49.91** ≤ $60 ⇒ **满足 2.b(iii)** |
| 同一算术取**已登记上界** $2.36/波 | $43.463 + $7.08 = **$50.54** |

### ⭐ 2.b(iii) 的**配套义务:本轮照办**

**本轮跨过 $50,owner 可能收到一封 AWS Budget 第一档(50% of $100 = $50)ACTUAL 告警邮件,
原因是 8 月至今的批测波次累计 —— 具体到近 12 小时,是 W11 / W12 / W13 三波 × 约 $2.15/波 = $6.45,
叠在 CE 读到的 $43.46 之上。**

诚实边界,两条都写出来:
- 按**在用**单价($2.15)围栏值是 **$49.91,严格说还差 $0.09 没跨**;按**已登记上界**($2.36)
  是 **$50.54,跨了**。两个读数骑在刀口两侧。
- 上一轮预告的是「下一波落 ~$49.7 仍不跨」,那是拿 `budgets` 的 $41.182 算的;
  **本轮 CE 高出 $2.28 把这个预告推翻了**。
- ⇒ **本台按「跨了」执行**,理由是这条义务的全部目的就是让 owner 收到的告警邮件永远有一份
  对得上的书面解释;在刀口上少写一次的代价(owner 收到无解释的告警)远大于多写一次。
- **未使用**被禁用的「MTD 增量对得上波费 ⇒ 该波已计入」对账手法(铁纪律(乙))。
- 刹车线 $90 未近;owner 批准档 $100;跨 $60 仍按原规则停并报总监。

## ③ 收割:**零**

`validation/` 最新对象是 W12 自己的 `l1trade+36ids-6aa30cdb2737_20260826_035*`(03:51–03:56Z),
**W12 之后无新 verdict**;`soak/` 最新前缀是 W12 的 `spot_20260826_0310*_14004d85…` 四个。
W12 已于上一轮(06:15Z)用 `recover_verdict.py` 全量重算并归档 ⇒ **本轮零收割**。
**W13 的收割棒交下一轮**(约 10:1x–10:3xZ 落盘)。

## ④ ⭐⭐ 发波前置核查的实质产出:`zusstatic` 是一条**结构性零腿**(GH #207)

`3d00e262`(hero 05:02Z)在 `hero_zuus.lua` 的 `X.GetBoundAbility` 文档里**逐字写着**:

> `zusstatic` armed without `zusbind` armed measures the wrong ability's missing key, i.e. 0.

机制是英雄组自己枚举出来的:`sAbilityList` 是 `GetAbilityList` **压缩**出来的数组,下标 N =
「walk 接受的第 N 个技能」;三个可选技能的 2^3 = 8 个世界里,
**index 5 是 Static Field 的世界数为 0**。而 `sAbilityList[5]` 的唯一消费方
`X.GetStaticFieldBonus` 正是 `zusstatic` 的落点(接线门定位 `hero_zuus.lua:442`)。

**为什么这是批测台的事,而不是英雄组的事:**

- `zusstatic` **在当前 armed 36 串里**(§BF,08-25 入集);`zusbind` **不在**。
- 接线门 `check_armed_wiring.py --cand "<36串>" --ref a883d7c6` → **exit 0,`all 36 armed ids wired`**。
  **接线门查的是「调用点存在」,不是「读到的是它以为的那个东西」** ⇒ 这种死法它**按构造看不见**。
  这是 `AGENTS.md` 里 `pullcad` 那条教训的**同族第二例**:上一例死因是「被 promote 的 id 冻结了合取项」,
  这一例死因是「**合取项从未入集**」。
- 载体侧**不是**瓶颈:`zuus` 本轮载体门 **FULL 4/4**(与 W8–W12 逐字复现)。**语料一直有,读数一直是 0。**
- 危险的不是这个 id 白跑,是它混进 36-id bundle 的合成读数,被读成「测过了、中性」——
  正是 08-19 `creeppull`/`pullcamp` 双侧 SILENT 那个教训的形状。

**GH #207 三选一交总监**:(A) `zusbind`+`zusstatic` 作一个原子一起 arm(同 `stable-v2` 先例,
本台推荐,零 AWS 增量)/ (B) `zusstatic` 暂时出集(36→35)/ (C) 维持现状但**明写**登记为 STRUCTURAL-ZERO。

**未裁前本台的执行方式(预登记,收割时照做)**:W13 的 `zusstatic` 腿登记为 **STRUCTURAL-ZERO**,
**不为它出读数,也不计入 bundle 的「已测 id 数」**。

## ⑤ 树漂移核查(W12 `14004d85` → W13 `a883d7c6`)

浅 clone 纪律照做:先 `git fetch --depth 1 origin 14004d85…` 取回,再 `git log ..HEAD -- bots/ game/`,
**exit 0**(不是空输出被读成无漂移)。4 个 commit,逐条核过门的位置:

| commit | 落点 | 对两条腿 |
|---|---|---|
| `72f4dced` `basesiege` | `mode_retreat_generic.lua` / `mode_farm_generic.lua` | **门内**(`J.IsBasePresenceAdverse` 里 `IsModeTurbo and IsSoakCandidate('basesiege')`),gate-off **逐字节等于出厂的 `nEnemies >= nAllies`** ⇒ 惰性 |
| `1663b81b` `abilanc` | `jmz_func.lua` | gated 且**未入集** ⇒ 惰性 |
| `a883d7c6` `cmclone` | `hero_crystal_maiden.lua` | gated 且**未入集** ⇒ 惰性 |
| `3d00e262` `zusbind` | `hero_zuus.lua` | 门内部分惰性,但**新增的 nil 保护是 ungated** ⇒ **稳定腿不逐字节不变** |

⇒ **登记一条跨波口径断点**:W13 vs W12 在 **Zeus 出场的局**上不完全可比(旧树 `sAbilityList[5]`
为 nil 时索引会 raise,新树被 ungated 的 nil 检查挡住)。**波内腿间差不受影响**(两腿同树)⇒
W13 自己的读数照常可用。**本条只登记,不作结论。**

**这也是本轮 (ii)「有新东西可测」的实质内容**,不是凑数:稳定腿本身被 `3d00e262` 动了。

## ⑥ 发波 W13(界后第四波)

| 项 | 值 |
|---|---|
| 树 | **`a883d7c666fb84504c5565f4d7d6a90fce4f42fb`** —— `git ls-remote origin main` 真 tip,与本地 HEAD **逐位相等**,全 40 位钉进 `--ref` |
| armed | `test_set.md` 第 2 行**逐字**,裸串 **320 字节 / 36 id**(与 W11/W12 同串,**本波只动树、不动 id 集合、不动采集配置**) |
| 接线门 | `check_armed_wiring.py --cand "<36串>" --ref a883d7c6` → **exit 0,`all 36 armed ids wired`**(但见 §4:exit 0 ≠ 每条腿都活着) |
| smoke 门 | `lua5.1 tests/run_tests.lua smoke_load` → **`3 tests, 0 failures` exit 0**(GH #200) |
| 载体门 | `seed_roster_index.py --seed 888 895 896 906` **exit 0**,与 W8–W12 **逐字复现**:`crystal_maiden`/`zuus` **FULL 4/4**;`lion`/`obsidian_destroyer` **PARTIAL 仅 s896**;`skeleton_king` **PARTIAL s895/s906** |
| 拓扑 | 4 台 × 1 种子(**888/895/896/906**,与 W7–W12 同种子) |
| 参数 | `--slots 16 --rec-slots 12 --hours 2 --on-demand --games 12` |
| 发波时刻 | 09:13:01 / 09:13:03 / 09:13:06 / 09:13:08Z(间隔 2/3/2 秒,跨整秒) |
| 唯一性双保险(#98) | `soak-run` 标签 **4 值两两不同**(`sort -u` = 4);`describe-instances` 恰 **4 台**;`InstanceLifecycle` 全 `None`(= 按需,与 `--on-demand` 一致) |
| run_id 尾 token | `536dc6` / `11effb` / `b97d8b` / `cec811` |
| 实例 | `i-0f46c7c7d16ac3f75` / `i-017718a12cfc07322` / `i-073af5234b05eee0a` / `i-04471ab571e825cb2` |

- **`--rec-slots 12` 不降不升**:总监 §AS.2 裁的是 (C) 走 12 且明写不许降,压过本台自己「停在 8」的
  保守默认;**GH #75 未裁前维持 12**(第三轮候)。
- **`--on-demand` 是 GH #158 未裁前的保守默认**(第七轮催)。
- 波次预算闸(GH #108)**未拒发**;`--dry-run` 先行,plan 逐项核对无误
  (`ref/slots=16/rec_slots=12/watchdog=2h/us-west-2` 全部对上)。
- 三闸判定:**(i) PASS** —— 解锁时刻 `2026-08-26T09:10:52Z`(= W12 发波 03:10:52Z + 6h),
  发波前先打钟点再跟 `date -u` 比,**实测 09:12:41Z,超出 109 秒**(§上一轮立的修法照做,
  W11 早发 21 秒那次不再复发);**(ii) PASS**(见 §5,稳定腿被动过);**(iii) PASS**(见 §2)。

## ⑦ 泄漏检查

- **开工**:`running/pending` 段**空**,常驻只有 AMI `ami-0a990a26d89c66547` + 快照。
- **收尾**(`--leak-only`):恰 **4 台**,全部是本波刚发的 W13(启动时刻 09:13:0x Z),
  **无游离实例**;常驻仍只有 AMI + 快照。四台均 `--hours 2` 自毁看门狗 + 完成即关。

## ⑧ 交棒(下一棒必须交出去,否则接力棒会掉)

1. **总监 —— GH #207(新,本轮最重要的一条)**:`zusstatic` 结构性零腿三选一。
   **不裁则 W13 收割时它按 STRUCTURAL-ZERO 登记,且下一波原样再空转一次。**
2. **总监 —— GH #204(第二轮)**:铁律 4(i) 施于经济量时是「侧偏检验」而非「噪声检验」。
   **W13 会给出第三个侧偏样本**(W11 +10.58 / W12 +70.04 相差 6.6 倍),收割时一并登记。
3. **总监 —— `strategy-20` / `abilanc` 本轮该裁**(自检 §BB.4 RIDESHARE 点名)。
4. **总监 —— 章程 §2 界后单价改区间 $1.82–$2.36**(第六轮);
   本轮 CE 与 budgets 分叉 $2.28 使这条更急:**围栏基数该取哪个源,现在会改变 $50 刀口的判定**。
5. **总监 —— 存量催办**:#75 `--rec-slots` 阶梯(第三轮)/ #158 `--on-demand` vs spot(第七轮)/
   #180 互锁(第六轮)/ #171 SKIP(第四轮)/ #200 smoke 门(第三轮)/ #181 第二口径 /
   载体门 PARTIAL 惰性种子(第十一轮)/ `stable-v*` tag(第八轮)/ `campdanger`(第三轮)。
6. **录像组 —— `campfarm` 端到端核验(GH #194)**:W13 又一份同构语料(帧通道 12/16),
   可与 W11/W12 合并取样;另 W6 解锁(第十二轮)、`tpreach` 的 (a)、W8 `towerfear` 五判据(第八轮)。
7. **下一轮本台 —— 收割 W13**(`recover_verdict.py` 全量重算,下载 `--include` **必须带 `*.demclaim.json`**);
   **6h 闸解锁时刻 `2026-08-26T15:13:08Z`**,开工先打钟点再跟 `date -u` 比;
   发波前置 `git ls-remote origin main` 核树 + 接线门 + smoke 门 + 载体门;
   **围栏基数按本轮先例取 budgets/CE 的大者**,并注意下一波必然实打实跨 $50。
8. **归档扫描欠账(零 AWS,本轮未动)**:queue 里 `ROUTED_ARCHIVE_SCAN` 已放行但仍 pending 的有
   **hero-1/-2/-3/-4/-7/-10/-11/-12/-13/-14/-16/-17/-18 共 13 条**,最早的 hero-10/-11 自 08-23 起挂了 3 天。
   它们零 EC2、零钱,卡的只是有人去跑。**本台下一次「归档扫描轮」应优先清这一批**(可按总监
   §BF.2 的合并同类项分组:Zeus 帧 / Axe 帧 / Lion·CM 帧 / power_treads 帧)。

## ⑨ Token 用量(铁律 8)

```
TOKENS total_in=2,473,994 out=22,308 turns=24
```
(统计时刻为本报告落盘前;此后的收尾回合不计入。)
