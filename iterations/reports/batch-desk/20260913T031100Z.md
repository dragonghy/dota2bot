# batch-desk 2026-09-13T03:11–03:4xZ — 持有轮第六轮

**一句话**:刹车第六轮在力(owner 在 GH #779 上仍零表态),**零发波、零收割欠、零泄漏**,本轮新增支出 EC2 **`$0.00`**、CE **`$0.03`**(`check_costs.sh` 的 `≥$35` 自动复核 1 次 + 本台主动付费 2 次,理由见 §四/§六)。

⭐⭐⭐ **头号:上一轮预言的那次「毫无保留的放行」本轮如期发生了,一字不差 —— 而钱恰好在同一次刷新里落了账。** 于是这次放行**实质上是对的**,但它对不对**与那道闸无关**:W69 从围栏账本上掉出的时刻是 `2026-09-12T20:44:31Z`,账单侧拿到这两波是在 `00:13Z`(CE 活查询仍逐位缺席)与 `01:19:49Z`(快照带着钱一起刷新)之间 ⇒ **中间有 ≈4.6 小时,任何一次预算刷新都会打出同一句无保留的 `CLEAR`,而那 `$2.20` 是真的不在账上**。⛔ **本轮没有被这个洞咬到,靠的不是闸,是那张快照恰好冻了 13.2 小时** —— 一个缺陷替另一个缺陷挡了一次。

⭐⭐ **第二:GH #454 (乙1) 欠了 15 轮,本轮结清**,花 `$0.01` 拿到本台**有史以来第一条真正的账单侧 `c6i.4xlarge` 行**(此前全部实测点都是「价带 × 机时」的估算)。

⭐⭐ **第三(顺带查出来的,比前两条更影响 owner 那道题):九月余量 `$1.308` 小于本月**每一天**的 `NoInstanceType` 行,中位数 `$0.8212`** ⇒ **`$90` 刹车线大概率会在 09-30 之前被越过,即便本台一波都不再发。**

---

## 一、成本(现跑)

- `AWS_SETUP_EXIT=0`。容器无 AWS CLI,脚本自装 `aws-cli/1.46.1 Python/3.11.15 botocore/1.43.62`;身份 `arn:aws:iam::581436534924:user/dota2bot-agent`(受限 IAM,正确)。
- `COST_EXIT=0`。MTD **`$88.692`**;`≥$35` 自动 `$0.01` CE 复核 **`88.6920614964`**;`forecast 179.629` / `limit 100.0` **登记,不据以决策**。
- ⭐⭐ **快照活了**:`budget refreshed 2026-09-13T01:19:49Z` —— 上一轮(及此前连续四轮)读到的是 `2026-09-12T12:08:45Z`,**冻了 13.2h**。本轮是**六轮来第一张新快照**。
- MTD 相对那五个逐位相同的读数上涨 **`+$1.9343932751`**(`88.6920614964 − 86.7576682213`)。⛔ **本台不据此对任何一波做归属**(章程步骤 2 (乙) 明令:「MTD 增量对得上波费 ⇒ 该波已计入」这个手法已两次给出错误结论)。归属走 §四 的逐日 CE + 机时反解,那是**另一条推导**。
- 常驻成本仍只有 AMI `ami-0a990a26d89c66547`(`dota2bot-batch-20260718`)。

## 二、闸(现跑,裸读退出码,逐字)

**闸 (i) `THROTTLE_EXIT=0`**:
```
anchor wave      : W69 (4 machine(s))
last machine up  : 2026-09-12T09:26:31Z  <- the anchor (GH #469 ruling 1)
slate spread     : 7s
unlock           : 2026-09-12T15:26:31Z
now              : 2026-09-13T03:14:04Z
margin           : +42453s past unlock
WAVE_THROTTLE: UNLOCKED (exit 0) -- gate (i) passes at this instant.
```
⚠️ **(i) 开着不等于可以发**(连续第五轮点名):(i) 是时间函数、会自愈;(iii) 只随 MTD 单调收紧。

**闸 (iii) `--planned 1.10`(⛔ 本轮未传 `--pending`,理由见 §三)⇒ `FENCE_BARE_EXIT=0`**:
```
crossing registry: 0 record(s) ... none in force -- the gate runs at the DERIVED fence.
budget filters   : none  <- so every instance in the account does land in this budget, whoever owns it
accrual scope    : 17 region(s) read ... <- COMPLETE: every region this account has enabled
wave accrual     : records after 2026-09-12T14:01:49Z (= 2026-09-13T01:19:49Z - 11.3h lag, clock from budget snapshot)
accrual check    : CERTIFIED (0 accruing instances account-wide, read this run) -- pending $0.000 is a reading, not a default.
actual (MTD)     : $88.692   <- re-read this run, never cached
pending waves    : $0.000
planned          : $1.100
projected total  : $89.792
  ACTUAL alert   : $50.00   crossed   AWS state=ALARM
  ACTUAL alert   : $80.00   crossed   AWS state=ALARM
  ACTUAL alert   : $100.00  NOT yet crossed   AWS state=OK
fence            : $100.00   <- lowest ACTUAL alert not yet crossed (Ruling 1)
brake            : $90.00   (owner's line, not derived here)
operative ceiling: $90.00   = min(fence, brake)
headroom         : $0.208 after this wave
WAVE_FENCE: CLEAR (exit 0) -- gate (iii) passes with the fence DERIVED THIS RUN.
```
- ⛔ **未用** `--no-accrual-check` / `--no-crossing-file` / `--director-crossing` / `--snapshot-instant` ⇒ **无任何「这是 SKIP 不是 pass」行**。
- **闸 (ii) / (iv) 本轮未评估,也不声称跑过** —— 本轮不发波(§七),走不到那两道门。

## 三、⭐⭐⭐ 头号:预言兑现,而兑现的方式比预言更值钱

上一轮(`00:13Z`)§三 (丁) 写下的是一个**可证伪的预言**:一旦预算快照刷新且戳晚于 `2026-09-12T20:44:31Z`,W68/W69 会一起从 `must cover` 掉出,闸 (iii) 会打出 `CERTIFIED ... pending $0.000 is a reading, not a default.` + `WAVE_FENCE: CLEAR (exit 0)`,且 **`zero qualified` 与 `WAVE_FENCE CLOCK` 两句限定语一句都不打**(两者的打印条件都是 `clock_source != "budget snapshot"`)。

**本轮实测:逐字兑现。** §二 那份读数里 `un-accrued?` 一行都没有,`must cover` 整节消失,两句限定语**一句都没打**。

### (甲)但这次的放行**实质上是对的** —— 而它对,不是那道闸的功劳

钱在同一次刷新里落了账(§四 第一手证据)。⇒ **本轮不需要本台自补 `--pending 2.20`**,上一轮交棒 ③ 的那条开工必读(「快照戳若晚于 `20:44:31Z`,`--pending` 必须由本台按波次记录抬进来」)**本轮被一条更强的证据取代**:不是「抬不抬进来」的判断题,而是「那笔钱现在能不能在账单侧被看见」的测量题,答案是**能**。⛔ 抬进 `$2.20` 会**重复计账**。

### (乙)⭐⭐⭐ 真正的产出:危险窗口第一次被**两边都钉住**

| 时刻 | 事件 | 证据 |
|---|---|---|
| `2026-09-12T09:26:31Z` | W69 末台起飞 | `W69_wave.json:launch_window` |
| `2026-09-12T20:44:31Z` | **W69 从围栏账本掉出**(= 末台 + 11.3h,时钟是快照戳) | 上一轮第一手读源码(`wave_fence.py:739/889/1248`) |
| `2026-09-13T00:13Z` | 账单侧**仍然没有**这两波 | 上一轮 CE 活查询 `86.7576682213`,第五个逐位相同的读数 |
| `2026-09-13T01:19:49Z` | 快照刷新,**带着钱一起** | 本轮 `budget refreshed` + MTD `$88.692` |

⇒ **`20:44:31Z` 到 `≈01:19Z` 之间约 4.6 小时,任何一次预算刷新都会打出本轮这句无保留的 `CLEAR`,而那 `$2.20` 真的还不在账上。** 本台没被咬到的唯一原因是**那张快照恰好冻了 13.2 小时** —— ⛔ **一个缺陷替另一个缺陷挡了一次,这不是纵深防御,是运气**。

### (丙)顺带把滞后常数的实测下界再抬一截

W68 用量在 `≈2026-09-12T04:17Z` 结束(末个 S3 对象 `04:15:50Z`),账单侧第一次看得见它是 `≤2026-09-13T01:19:49Z`,而 `00:13Z` 的活查询还看不见 ⇒ **真实滞后 ≥ 19.9h,且 ≤ ≈21.0h**。围栏的 `ACCRUAL_LAG_MAX_HOURS = 11.3` 源码自称 *"the upper end of the documented ActualSpend lag"* / *"The MAX is the load-bearing end"* ⇒ **那个「上缘」被顶穿 ≈76%,而这一次是两边都有界的实测,不是下界。**

⛔ 本台**不改 harness、不定新常数**(先例 GH #33),**也不拿推导替代退出码**:本轮的授权读数就是 §二 的 `FENCE_BARE_EXIT=0`。已作 **GH #603 第六代证据**评论。

## 四、⭐⭐ GH #454 (乙1) 欠 15 轮,本轮结清

**判据是读数不是时刻**(上一轮所立):当轮 CE 复核 **> `$86.7576682213`** 才值得花第二个 `$0.01`。本轮复核 **`88.6920614964`**,**是** ⇒ 花之,一次 `--granularity DAILY --group-by INSTANCE_TYPE`(`CE_DAILY_EXIT=0`):

```
2026-09-12 : c6i.4xlarge $1.576733 + NoInstanceType $0.562348 = $2.139081
2026-09-13 : $0.0000   (本日尚未出账,与 §三 (丙) 的 ~20h 滞后一致)
```

**那一天只有 W68(`03:26:12Z`)与 W69(`09:26:31Z`)两波**,都是 4 台 spot `c6i.4xlarge`,都整个落在 09-12 的 UTC 日里,当日无其他本台波次。

**归属不靠「增量对得上」,靠费率反解**(章程步骤 2 (乙) 指定的那条路):
- 机时第一手(SIR,`W68/W69_wave.json:machines[]`):W68 `2.9023h` + W69 `3.4978h` = **`6.4001h`**;
- `$1.576733 / 6.4001h` = **`$0.246361/h`**,**落在有据可查的 spot 价带 `$0.231–0.250/h` 之内**(且靠近上沿)。
- ⇒ 这条 c6i 行确实是这两波;**这是结构性核对,不依赖 MTD 增量的大小**。

**单波账单侧实测(按机时分摊)**:

| 波 | 机时 | 仅算力(c6i 行) | 含当日 `NoInstanceType`(**上界**) |
|---|---|---|---|
| W68 | 2.9023h | **`$0.7150`** | `$0.9700` |
| W69 | 3.4978h | **`$0.8617`** | `$1.1691` |

**(乙1) 判定:按章程自己的口径 NOT TRIGGERED。** 章程 (甲) 列的实测点(`$0.7413` W47、`$0.776–0.840` W48、`$0.70–0.76` W49)都是**价带 × 机时**、即**仅算力**口径 ⇒ 同量比较用左列,两波都在 `$1.10` 之下。
⚠️ **右列登记但不据以裁定**:W69 的上界 `$1.1691` **高于 `$1.10`**,而这正是 **GH #743** 那道未裁的口径题(「波 + 补跑」总额该不该拿去比一个按四台 spot 标定的覆盖档)的同一条缝。⛔ **本台当时没裁,本轮也不裁。**

## 五、⭐⭐ 新发现:`$1.10` 覆盖档从来没有包含 `NoInstanceType` 那条行

09-12 当天 `NoInstanceType = $0.562348`,占当日账单 **26.3%**。本台的围栏算术**一直只按 `$1.10/波` 记算力**。
⚠️ 但**不能**就此把这条行整条算成波费:它**不跟着 c6i 行走**(09-10:c6i `$0.5984` 而 `NoInstanceType` `$1.4894`),里头含常驻 AMI/EBS 快照、IPv4、跨区流量。本月唯一的零批测日是 09-13,而它**今天还没出账** ⇒ **纯常驻日基线本轮量不出来**,⛔ 因此本台**不定这条行的波内份额、不改常数**。

**能说死的只有方向**:凡有份额是波驱动的,围栏就**系统性少记**,失效方向是**危险的那一侧**,与 §三 的滞后**同向叠加**。⇒ 交总监,与 GH #743 同一条缝,**不新开 issue**。

## 六、⭐⭐ 把 owner 那道题重新称一遍(本轮零成本的第二次 CE 买到的)

`CE_SEPT_EXIT=0`(第二个 `$0.01`,`Start=2026-09-01 End=2026-09-14 DAILY`)。**自洽核对:十二天逐日求和 `$88.6921`,与 MTD `88.6920614964` 逐位相同** ⇒ 这张表就是那条线本身,不是它的近似。

| 日 | 合计 | 构成 |
|---|---|---|
| 09-01 | 4.4708 | NoInstanceType 1.5350 / c6i 2.9358 |
| 09-02 | 4.1982 | 0.5255 / 3.6728 |
| 09-03 | 4.1160 | 0.4375 / 3.6786 |
| 09-04 | 5.8004 | 0.5795 / 5.2210 |
| **09-05** | **26.3977** | 0.9117 / 3.8529 / **c7a.16xlarge 21.6331** |
| **09-06** | **18.4114** | 0.7306 / 1.8784 / **c7a 1.6474 + r7a.16xlarge 14.1550** |
| 09-07 | 4.1133 | 0.6545 / 3.4587 |
| 09-08 | 3.3738 | 1.1319 / 2.2419 |
| 09-09 | 6.7158 | 1.6351 / 5.0807 |
| 09-10 | 2.0879 | 1.4894 / 0.5984 |
| 09-11 | 6.8677 | 1.7591 / 5.1086 |
| 09-12 | 2.1391 | 0.5623 / 1.5767 |
| 09-13 | 0.0000 | (未出账) |

**(甲) 外来支出**:`c7a.16xlarge $23.2805 + r7a.16xlarge $14.1550 = $37.4355` = **MTD 的 42.21%**,**只存在于 09-05 / 09-06 两天,此后一分未涨**(第六轮同题,本轮是逐日第一手而非转载)。`budget filters : none` ⇒ **咬住本台的这条 `$90` 里四成以上不是本台的支出**。扣掉后本台自己的 MTD ≈ **`$51.26`**。

**(乙) ⭐⭐ 本轮新增、且改变问题形状的那条算术**:
```
到 $90 刹车线的余量 = $90.00 − $88.6920614964 = $1.3079
本月 NoInstanceType 逐日读数 = $0.4375 … $1.7591,中位数 $0.8212,十二天没有一天低于 $0.4375
```
⇒ **`$1.308` ≈ 1.6 个中位日的非实例计费。本台一波不发,`$90` 也大概率在 09-30 之前被越过。**
⇒ **owner 那道题因此不再是「要不要花掉最后 `$1.10`」,而是「这条线本月内会被本台控制不了的计费越过,三条路要选一条」。** ⛔ 本台**不建议、不预选、不自行改线**。

## 七、⛔ 本轮零发波 —— 而这一轮的「不发」第一次不是闸拒绝的

**必须如实写清:四道闸里跑过的两道本轮都是绿的**(闸 (i) `exit 0`;闸 (iii) `exit 0`,`headroom $0.208 after this wave`)。**不发波是本台的处置,不是闸的拒绝**,两条独立理由:

1. **GH #779 第六轮零表态,而这一波会吃掉九月剩余余量的 84%**(`$1.10` of `$1.308`)。刹车立案轮的处置逐字是「⛔ owner 对交棒 ① 表态前不发任何波」,**并且工具当时逐字写着 `do not wait it out`**。本轮闸变绿的机制恰恰是**时间过去了、保守的 pending 估算兑现成更小的真值**(`$2.20` 记 vs `$1.9344` 实际入账,保守 `$0.266`,而当初越线量只有 `$0.058`)⇒ **把它读成「许可」,实质就是把「再等一轮同一道红闸」等成了绿,正是那句话禁的动作**。
2. **§六 (乙)**:九月余量小于 1.6 个中位日的非实例计费 ⇒ 这 `$1.10` 花下去,月内几乎必然以**没有余量、也没有 owner 裁定**的状态越线。

⚠️ **反方向的论点本台也照登,不藏**:预算按月重置,`$1.308` 不花就在 10-01 作废;若 owner 的答案是三条里的任何一条,这一波都不违反它。**所以本台把它做成一道可以一句话回答的题**(见 §九 交棒 ①):**owner 说「发」,下一轮触发就发;不说,本台继续持有。**

## 八、收割与泄漏

**收割:零欠(现扫)。** `awsx s3 ls s3://<bucket>/validation/ --recursive` 重定向后裸读 ⇒ **`S3VAL_EXIT=0`**,**592 对象**,与上一轮**逐个相同**;当日最新对象仍是 **`2026-09-12 10:18:26Z`**(`lf_rescue+27ids-d177b87026dd_20260912_1018_run.log`,W69 末个对象)⇒ **自上一轮零新对象**,W68/W69 均已全量收割。⛔ **本轮不重算已收波次**,零下载、零 S3 支出。

**泄漏:四条独立路径全零(⛔ 不合并成一句)。**
1. `check_costs.sh` 的 `running/pending` 段 —— **空**;
2. `describe-instances`(**不加 tag 过滤**,`pending,running,shutting-down,stopping,stopped`)⇒ **零行**,`INST_EXIT=0`;
3. `describe-volumes` ⇒ **零行**,`VOL_EXIT=0`;
4. 闸 (iii) 自带账户级普查 ⇒ `accrual scope : 17 region(s) read <- COMPLETE` + `CERTIFIED (0 accruing instances account-wide, read this run)`。

常驻仍只有 AMI `ami-0a990a26d89c66547`。

## 九、交棒

1. ⭐⭐⭐ **owner —— GH #779,一句话就能答的版本**:九月还剩**恰好一波** spot(`$1.10` 对 `$1.308` 余量)。**说「发」,本台下一轮触发就发;不说,继续持有。** 并请连着看 §六 (乙):**这条线本月大概率会被本台控制不了的计费越过**(余量 < 1.6 个中位日的 `NoInstanceType`),以及 §六 (甲):**MTD 的 42.21% 是外来机型**,`budget filters : none`。三条选项不变:维持 `$90` / 给预算加成本分配标签过滤器 / 一次性解禁到某个数(并说明是否含外来支出)。
2. ⭐⭐⭐ **总监/harness —— GH #603 第六代证据,危险窗口第一次两边都钉住**(§三 乙):`20:44:31Z → ≈01:19Z` 约 4.6h 内任何一次刷新都会静默放行,本轮没被咬到**靠的是另一个缺陷**。滞后实测 **≥19.9h 且 ≤≈21.0h**,顶穿 `11.3h` 约 76%。⛔ 本台不改常数。
3. ⭐⭐ **总监 —— GH #454 (乙1) 已结清**(§四),并请一并看它引出的两条口径题:(a) W69 的**含 `NoInstanceType` 上界 `$1.1691` 高于 `$1.10`** 而仅算力 `$0.8617` 低于,选哪条口径 = **GH #743** 那道未裁的题;(b) §五 —— `$1.10` 覆盖档**从来不含** `NoInstanceType` 行(09-12 占当日 26.3%),少记方向是危险那侧。⛔ 本台两条都不自裁,**不新开 issue**。
4. ⭐ **下一轮本台**:① ⛔ **围栏/闸/成本一律当轮现跑**,本条目的 `$88.692` / `headroom $1.308` / `budget refreshed 01:19:49Z` / cutoff `2026-09-12T14:01:49Z` **全部作废,不得抄作前提**;② **收割零欠**,S3 基线 **592 对象**;③ 闸 (i) 锚点仍 W69 末台 `2026-09-12T09:26:31Z`(早已解锁)⇒ ⚠️ **(i) 绿不是许可**;④ **若 owner 说发**:`--pending` 按当轮波次记录现算(⛔ **不要再抬 W68/W69 的 `$2.20`,它们已落账**,§四),闸 (ii)/(iv)/接线/载体四道门本轮**未跑过,下轮要从头跑**;⑤ ⚠️ 开工第一条命令**重定向 + 后台 + 不设短 `timeout`**(**第 23 次**;本轮仍在同一位置踩,见 §十)。

## 十、自检(铁律 10)与铁律 9/11

- ⚠️ **管道坑第 23 次,仍是当轮第一条命令**:`routine_selfcheck.sh 2>&1 | tail -40`,脚本自卫逐字 `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.`,并自己点名 `it has recurred 5x, every time as the first command of the round`。**当场重定向重跑,零损失,但连续 23 轮同一位置。**
- **本轮每一个读了退出码的命令是否走管道的自查**(上上轮交棒所立):`AWS_SETUP_EXIT` / `COST_EXIT` / `THROTTLE_EXIT` / `FENCE_BARE_EXIT` / `S3VAL_EXIT` / `INST_EXIT` / `VOL_EXIT` / `CE_DAILY_EXIT` / `CE_SEPT_EXIT` / `WGK_EXIT` —— **全部重定向到文件后裸读,零管道**。
- **自检真码**:见本报告「补记」节(⛔ 后台跑完才读,**不预写**)。
- **铁律 9**:`OWNER_PRIORITIES.md` 开工已读,**无属于本台的新未完成项**;P4.1 标尺波按前几轮结论**不是欠棒**(Y1 已于 09-05 交付、排序条款已释放),⛔ 未代改本文件。
- **铁律 11**:本轮零 `requires approval`、**零空转**(自检后台跑,成本/闸/收割/泄漏并行推进)。
- ⛔ **本会话 `bots`/`game`/`tests`/`tools` 一行未改** ⇒ **无一条 trunk red 由本轮引入**。本轮唯一改动的代码外产物是两份波次记录的 `gh_454_leg_b1` 节(§四),**棘轮当场跑过**:`WGK_EXIT=0`,`760 checks, 0 failed`。
