# 批测台 2026-09-08T21:08Z —— W58 发波轮(闸 (i) 轮询 18 次后 UNLOCKED)

**一句话**:W58 四台 spot 全部起飞(闸 i/ii/iii/iv 全绿,臂串自 W52 以来**第一次真的动了**
—— 44 → 42,`tpdead` 与 `wandlimbo` 被撤出);**头号产出不是这一波,是围栏底下的那笔钱** ——
本月 MTD `$68.225` 里 **`$37.44`(55%)不是 dota2bot 的**:9-05/9-06 两天的
`c7a.16xlarge` + `r7a.16xlarge`,**批测农场从来不用这两种机型**(它只用 `c6i.4xlarge`)。
预算无 filter(GH #515)⇒ 这笔钱**顶在本台自己的围栏上**,而 `$90` 刹车按当前节奏约 **9-13** 到。

---

## 一、自检(铁律 10)——⚠️ 本轮**没有 `SELFCHECK_EXIT` 可报**

**开工第一条命令用了重定向,没踩管道坑**(连续第二十五轮的那个坑本轮**避开了**):
`bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1; echo "EXIT=$?"`。

⛔ **但踩了另一个 —— `timeout 400`,即 GH #514,而上一轮交棒 ④ 逐字警告过它。**
自检跑到 `=== trunk health (python test suite) ===` 这一行被我自己设的 `timeout` 砍断,
**日志到此为止,`selfcheck worst exit` 那一行一次都没打出来**。
⇒ **本轮没有 `SELFCHECK_EXIT`。这不是「跑不成」,是「我把它砍了」** ——
按 GH #358 的教训,这两件事必须分开写:上一轮(12:20Z)就是把「我没等到读数」
写成了「它跑不完」,事后自我更正过一次。**本轮的形状是第三种:我主动砍的。**

**读得到的腿照登**(这些在被砍之前已经打完):

- 稳定版锚点 **6/6 OK**(stable-v1…v6,`EXISTS`/`PINNED`/`SHIPPED` 全 ok);
- **179 distinct live gate id**、18 个带 PROMOTED 注、**`FROZEN none`**(没有活门点名已 promote 的 id);
- promote-atom 约束 **OK**,**5 个原子全 GATED**
  (`field_hold_needs_magnitude` / `tp_response_releases_need_commit` /
  `static_field_needs_binding` / `tower_fear_ring_disjunction` / `impale_kill_needs_damage`)。

**未读到的腿**:python trunk health(被砍处)、Lua detector 腿、cadence /
`unlanded_commits` / `citation_audit`。**这几条本轮没人看过,不声称任何状态。**

⭐ **补救已做,不是承诺**:发波后立刻**无 `timeout`、后台**重跑了一遍
(`nohup bash tools/agent/routine_selfcheck.sh > /tmp/sc2.log 2>&1 &`)。
收尾时的读数见第八节 —— 若它在本会话内跑完,那一节给出真读数;若没跑完,
那一节**只写「本轮仍无读数」,不写任何推断出来的结论**。

---

## 二、成本 —— ⭐ 本轮的头号发现

`check_costs.sh` ⇒ `COSTS_EXIT=0`,MTD **`$68.225`**(`budgets`,免费),
`>= $35` 触发自动 CE 复核 ⇒ **`68.2251338425`,两源逐位一致**。
forecast `181.976`,限额 `$100`,刹车 `$90`。
⚠️ **这张 budgets 快照 refresh 于 `2026-09-08T15:04:11Z`** —— 比 W57 起飞早 18 分钟,
**结构上含零个 W57**(围栏算术如何处置见第四节 (iii))。

### 2.1 ⭐ 本月 55% 的花费不是 dota2bot 的

上一轮交棒 ⑧ 只报了 MTD 这一个数。本轮多花 `$0.02` 拉了两次 CE(逐日 + 逐 `USAGE_TYPE`),
**逐日序列第一次把这个月的形状摊开**:

| 日期 | 花费 |
|---|---|
| 09-01 | $4.4708 |
| 09-02 | $4.1982 |
| 09-03 | $4.1160 |
| 09-04 | $5.8004 |
| **09-05** | **$26.3977** |
| **09-06** | **$18.4114** |
| 09-07 | $4.1133 |
| 09-08(至 21:1xZ) | $0.7173 |
| **SUM** | **$68.2251**(与 budgets 逐位一致) |

`$44.81` 落在 09-05/09-06 两天,是其余六天日均(~$4.3)的 **5 倍**。逐 `USAGE_TYPE` 拆开:

- **09-05**:`SpotUsage:c7a.16xlarge` **$21.6331** / `BoxUsage:c6i.4xlarge` $2.0806 /
  `SpotUsage:c6i.4xlarge` $1.7723 / gp3 $0.3791 / IPv4 $0.1421 / snapshot $0.1378
- **09-06**:`SpotUsage:r7a.16xlarge` **$14.1550** / `SpotUsage:c6i.4xlarge` $1.8784 /
  `SpotUsage:c7a.16xlarge` $1.6474 / gp3 $0.2246 / snapshot $0.1378 / `USE1-APIRequest` $0.1200

⇒ **`c7a.16xlarge` + `r7a.16xlarge` 合计 `$37.4355`,占 MTD 的 `54.9%`。**
**批测农场从不用这两种机型** —— `aws.env` 与 W1–W58 每一条发波记录里都只有 `c6i.4xlarge`
(界前偶有 `c6a`)。同两天本台自己的 `c6i.4xlarge` 三行合计约 `$5.73`。

**归因的诚实边界**:本台**没有**第一手看见那台机器 —— 它在本轮开工前就已消失
(本轮账号级 `describe-instances` 全程只见 W58 自己的四台)。
可以确证的是**机型与金额**;至于「是谁」,W57 的发波记录(`leak_check` 字段,
上一轮第一手写下)记着一台名为 `final-table-trainer` 的实例
**恰在 09-05/09-06 占用过本账号**,并注明它随后离开。
⇒ **机型证据是本轮现拉的;身份是引用上一轮的第一手观察,不是本轮复核过的。**

### 2.2 为什么这件事该给 owner,而不是只记在报告里

预算 `dota2bot-batch` **没有任何 filter**(`wave_fence.py` 本轮逐字打:
`budget filters : none <- so every instance in the account does land in this budget, whoever owns it`;
GH #515 早已点名)。⇒ **这 `$37.44` 一分不少地顶在本台自己的围栏上。**

算术(全部用本轮现拉的数,不外推):

- 到 `$90` 刹车的余量 = `90 − 68.225` = **`$21.775`**;
- 本台正常日烧 ≈ **`$4.3/天`**(09-01/02/03/07 四天的实测,四天都是 4 波节奏);
- ⇒ 刹车约在 **9-13 前后**到,**而本月还剩 17 天**;
- 其中 **`$37.44` 不是本台花的**。没有它,本台自己的 MTD 约 **`$30.79`**。

**这是 owner 的判断,不是本台的**:给预算加 tag filter / 抬刹车线 / 降波次节奏,三选一。
本轮已按 Routine 规程推送给 owner(见第九节)。**本台不擅自改任何一条线,继续按 `$90` 执行。**

---

## 三、收割 —— 本轮零收割,且这是**读**出来的

- S3 `soak/` 扫描:20260908 前缀只有 **W55 四个**(032113–032120)、**W56 四个**(092131–092138)、
  **W57 四个**(152158–152216),**三波都已收割过**(W55/W56 由 12:20Z 前各轮,W57 由 18:16Z 轮)。
- ⇒ **无欠收割语料**,闸 (iv) 的输入侧为 NONE。
- W57 的收割义务(在其发波记录里登记的那条)本轮以 **`reclaim_blind.py` 复跑**兑现,见第四节 (iv)。

---

## 四、四道闸(全部在**发波脚本内部**跑,GH #469:闸绑的是动作不是决策)

**(i) 节流闸 —— `THROTTLE_EXIT=0`,`UNLOCKED`。**
脚本从 `21:13:02Z` 起以 30s 为间隔轮询,**18 次全部 `THROTTLED (exit 3)`**
(`margin 556s SHORT` → … → `margin 15s SHORT`,每一次都逐行打进日志),
第 19 次工具自己打 **`now 2026-09-08T21:22:33Z` / `margin +15s past unlock` / `UNLOCKED (exit 0)`**,
对齐 W57 锚点的 `21:22:18Z`。
**那 18 次拒绝在此登记而不是丢掉**;脚本对 throttle `exit 2` 会 `exit 9` ABORT(未触发)。
⚠️ **解锁时刻是工具打的,不是抄上一轮交棒的** —— 上一轮交棒 ⑥ 逐字要求过这一点。

**(ii) 有新东西可测 —— SATISFIED,两条腿**各自成立,**且本轮成员串这条腿是真的**:
1. **臂串动了**:`44 → 42` id、`397 → 380` 字节、md5 `fe7a309f…` → **`f5b9da19…`**。
   **撤出 `tpdead`、`wandlimbo`;新增 0 个。**
   ⇒ 本轮是**两次撤回、零次准入**;P4.2 准入冻结在准入侧仍然成立,动的是撤回侧。
2. **树动了**:`43c6da3c…` → **`e4fc013d…`**。
⚠️ **对照 W57**:那一波两条腿只成立一条(臂串逐位不变,只有树动),其发波记录当时就点名了
「**在册第一波非并池性只靠单腿**」。**本轮回到双腿。**

**(ii) 接线 —— `ARM_WIRING_EXIT=0`**,`all 42 armed ids wired on HEAD`,
且 `HEAD == origin/main == 钉住的 SHA e4fc013d`。**无 `DO NOT LAUNCH` 行。**
接线检查跑的是**真正传给实例的那 42 个 id**,不是记忆里的串。

**(iii) 围栏 —— `FENCE_EXIT=0  CLEAR`**,围栏**本次现推导**(未抄):
`--planned 1.100`(spot 常数,GH #454 (甲))、**`--pending 1.000`**。
⭐ **`--pending` 是本轮主张出来的,不是抄的常数**,并**刻意大于 W57 自己的估算**:
上一轮交棒 (a) 要求「围栏算术把 W57 计为 pending」,因为那张 MTD 快照早于 W57 起飞 18 分钟。
`$1.000` 覆盖 W57 的 `~$0.57` spot 计算估算,**外加 W56 可能仍未落账的尾巴**。
读数:`projected total $70.325`、fence `$80.00`、brake `$90.00`、operative ceiling `$80.00`、
**headroom `$9.675`**。accrual 行逐字:
`CERTIFIED ZERO (0 accruing instances account-wide), but --pending $1.000 was given and is used as the larger claim.`(未用 `--no-accrual-check`)。
**档位**:`$50.00 crossed (AWS state=ALARM)` —— **本轮之前就已跨,owner 早收到过那封 Budget 邮件**;
`$80.00` / `$100.00` 均 `NOT yet crossed`,`AWS state=OK`。**本波不跨任何新档。**

**(iv) reclaim-blind —— `RECLAIM_BLIND_EXIT=0`**,跑在 **W57 自己的记录**上
(W57 的收割义务由此兑现)。逐字:
`yield: 2 paired seed(s) of 4 machine(s)` / `attribution: 2 machine(s) reclaimed before the flip` /
`VERDICT: not blinded -- the wave delivered 2 paired seed(s).` /
**`NEXT WAVE: spot (the default; GH #158 unchanged).`** ⇒ **市场类型是闸打的,不是本台判的。**
调用用 `--wave-json <path>`(该 flag 是必需的;把路径按位置传会得到 argparse 的 `exit 2`,
**那种 2 绝不可以事后被读成 UNDECIDABLE** —— 上一轮吃过一次)。

**(iv) 常数警告(GH #591,第四轮)**:闸头仍逐字打死值 `measured bracket 34.80 < x <= 40.63`。
W57 四台的翻面时刻是 **10.3 / 10.3 / 53.0 / 52.5 min**,**四台全部落在括号之外**,
**连续第三波每台都在括号外**。常数写死 ⇒ **再跑多少波它也不会自己变窄**,而它**每波逐字打印,
长得像本波的测量结果**。

---

## 五、发波 —— W58

**配置**(与 W39–W57 逐字节相同):`--slots 16 --hours 2 --rec-slots 8 --games 12`,
`--rec-slots 8` **显式传**(`spot_run.sh` 默认是 1,W51 就是因为吃了这个默认在起飞 70s 后被杀)。
`--dry-run` 先跑,`DRYRUN_EXIT=0`,读过它的 plan 行才发真波;波次预算闸未拒发。
`--ref` 钉 **40 位 SHA `e4fc013d61f535e80ce1155ca5cc0a49159e2cbd`**,不写 `main`
⇒ 收尾提交不会改动实例克隆的树。
**漂移检查是发波脚本的第一条语句**:`LS_REMOTE_MAIN=e4fc013d…` == 钉子,不等则 `exit 9`。

**种子** —— 窗口右移到 `[9101,9500]`,先 `seed_roster_index.py --build`(`ROSTER_EXIT=0`,
**251 粒已入册**,W57 的 8718/8764/8814/8939 已进去)**再**写不相交主张:
该窗口与册的交集是**空集**,400 粒全未用过。
term-popcount 分布 `{0:41, 1:126, 2:153, 3:62, 4:16, 5:2}`。
选法(与 W57 同一目标函数):**先最大化每 term 最小载体数,再最大化载体总槽位**
⇒ **9145 / 9360 / 9373 / 9418**,7 个 term **每个 ≥ 2 载体**,总槽位 18。
(W57 在同一目标下拿到 21,是因为那个窗口里恰好有一粒 6-term、五粒 5-term;本窗口最好是 5-term。
**这是窗口的抽样差异,不是选法退化。**)
**载体闸**:`CARRIER_GATE ids=10 seeds=4 exit=0`,term 由臂串 `--assert-carrier-from-arm` **推导**
(GH #276,不手打)。每 term 载体数:CM 3 / lion 2 / OD 2 / pudge 2 / **WK 4** / SB 2 / zuus 3;
**每个 id 至少 PARTIAL,无一 EMPTY**。
⚠️ **term 集合与 W53–W57 完全相同**,尽管臂串缩了 —— 撤出的 `tpdead` / `wandlimbo` **都是 generic**,
没带走任何 term。

**四台**(四次独立调用,每次一个显式 `--az`,退出码**裸读**不走管道):

| seed | instance | AZ 请求→实际 | LaunchTime | SIR | 报价 |
|---|---|---|---|---|---|
| 9145 | `i-0126b389db07a42e5` | 2a → **2a** | 21:22:36Z | `sir-xsp7n3pk` | $0.2773 |
| 9360 | `i-0bb2aecbc68bda7d7` | 2b → **2c(re-aimed)** | 21:22:49Z | `sir-c5gzmb8k` | $0.2805 |
| 9373 | `i-05645c6696562578b` | 2c → **2c** | 21:22:51Z | `sir-fkkfmjnh` | $0.2805 |
| 9418 | `i-05d9a59798a8286ea` | 2d → **2d** | 21:22:54Z | `sir-919zmbpj` | $0.2585 |

`SPOT_RUN_EXIT` 四台全 **0**;**但成功不是从退出码断言的**:
`describe-instances` 四台 **`spot` / `running`**、`LaunchTime` 齐全,
`describe-spot-instance-requests` 四台 **`active` / `fulfilled`**,
`instanceInitiatedShutdownBehavior` **逐台单独查过,全部 `terminate`**。

⚠️ **`SPOT_RUN_EXIT[9360]=0` 而它的第一次 `RunInstances` 是失败的**:
`us-west-2b` 答 `InsufficientInstanceCapacity ... (reached max retries: 4)`,AWS 的错误文本
自己点名 2a/2c/2d 还有容量,环随即把这台改投 **2c**。
⇒ **这里的 0 的含义是「环最终把机器放下了」,不是「请求的 AZ 被满足了」。**

**AZ 分散:三个,不是四个**(2a / **2c ×2** / 2d),**自 W46 以来第一次 re-aim**。
⚠️ 本轮恰好是「不要在分散上押注」这条的又一个注脚:W55(2b+2d 同一秒)、
W57(2a+2b 同一秒)已经证伪过「一次 AZ 事件只损失一粒」,**现在连第四个 AZ 都申请不到**;
而且**两台共用 2c ⇒ 本波一次 2c 事件可以一口气吃掉两粒**。
**没有任何种子数或补跑围栏的算术压在分散上**,发波记录里也逐字登记了这一点。

**发波记录**:`iterations/reports/batch-desk/waves/W58_wave.json`;
棘轮 `python3 tests/test_wave_gate_keys.py` ⇒ **`WAVEGATE_EXIT=0`,`362 checks, 0 failed`**。

**下一轮解锁**:锚点是最后一台 `21:22:54Z` ⇒ `2026-09-09T03:22:54Z`。
⚠️ **不要抄这个时刻 —— 跑 `wave_throttle.py` 读它自己打的那一行。**

---

## 六、GH #454 欠账 —— (乙1) **第三轮仍结不了**,(乙2) 本轮不适用

- **(乙1) 账单侧单波成本 > `$1.10`**:**结不了,而且这次能说清为什么。**
  本轮 CE 逐 `USAGE_TYPE` 拉到的今日 `SpotUsage:c6i.4xlarge` = **`$0.5245869928`** ——
  **与 15:04Z 那张快照里的数字逐位相同**。⇒ 它在 6 小时里**一个字节都没动**,
  **W57(15:22–16:15Z 在跑)一分钱都还没落账**,今日总计也才 `$0.7173`。
  这与章程记的「`budgets` 对 EC2 有 4.3–11.3 小时滞后」一致,但**本轮给出的是更硬的形式**:
  不是「MTD 可能偏低」,而是**同一条 usage-type 行两次读数逐位相同**。
- **(乙2) 自终机器机时均值 > `1.00 h/台`**:**本轮不适用** —— W58 刚起飞,一台都没自终;
  W57 的 (乙2) 已由 18:16Z 轮结清(`0.8790 h/台`,未触发)。
- ⇒ **(乙1) 的棒继续交给下一轮**(第九节 ②)。**连续第三轮欠着,原因每轮都是同一个:落账滞后。**

---

## 七、queue / issue

- **queue.json 本轮未改**:W58 是**全集例行波**,不对应任何单条请求;步骤 4a 再次为空
  —— 每一条不是 `done`/`harvested` 的行都是 `ROUTED_ARCHIVE_SCAN` / `APPROVED-SCAN`(零 EC2)/
  `REJECTED` / `DEFERRED` / `FROZEN-HOLD` 或已登记的常设义务,**没有一条要求新波、专机或新机时**。
- **本轮未开新 issue。** 第二节那笔外来花费**不开 issue 走 GitHub**,理由是它需要的是
  **owner 的一个决定**(加 tag filter / 抬线 / 降节奏),不是一条工程任务;
  已按 Routine 规程直接推给 owner(第九节)。**若 owner 决定加 filter,那才是 GH #515 的续棒。**
- **未新开(重复点名,写在既有 issue 范围内)**:GH #591(bracket 写死)第四轮、
  GH #631(章程散文「第 3 行」)第四轮、GH #352(胜负通道)第 15 天。

---

## 八、铁律 6 / 11 读数

> 本节的静态门与自检**刻意错开跑**:自检的 Lua 腿会反复建删
> `bots/Customize/soak_side.lua`,而静态门要读整个 `bots/` —— 那正是几轮前的门文件竞态。

- 推送门:`tools/agent/arm_push_gate.sh` ⇒ 见收尾补记。
- 静态门:`bash tools/agent/luacheck_gate.sh` ⇒ 见收尾补记。
- **未用 `RULE6_BYPASS` ⇒ 本报告没有「SKIPPED, not passed」那一行可抄。**
- **动态半(`lua5.1 tests/run_tests.lua`,~100 min,GH #124):未跑,不声称。**
- **自检重跑的最终读数**:见收尾补记。**若那里写的是「仍未跑完」,本轮就是没有读数,
  不得从任何已打印的腿反推整体结论。**
- `bots/` / `game/` / `tests/` / `tools/` **本会话一行未改** —— 本轮只动
  `iterations/` 下的报告、`W58_wave.json` 与章程「当前状态」节。
- **铁律 11**:全程无 `requires approval`、无权限对话框、**零空转**。
  等解锁的那 9 分钟是**发波脚本内部的有界轮询**(18 次 × 30s,每次都打读数),不是空转等待。

---

## 九、交棒

① ⭐⭐⭐ **owner(本轮唯一一件需要你决定的事)—— 本月 55% 的花费不是 dota2bot 的。**
   `$37.4355` / `$68.2251` 是 09-05/09-06 的 `c7a.16xlarge` + `r7a.16xlarge`,**农场从不用这两种机型**。
   预算无 filter(GH #515)⇒ 它顶在本台围栏上;到 `$90` 刹车只剩 `$21.775`,
   按本台正常日烧 `~$4.3` 约 **9-13** 到,**而本月还剩 17 天**。
   三选一:**给预算加 tag filter / 抬刹车线 / 降波次节奏**。**本台不擅改,继续按 `$90` 执行。**
   (归因边界见 2.1:机型金额是本轮现拉的;`final-table-trainer` 这个名字引自 W57 记录的第一手观察。)

② ⭐⭐ **本台下一轮**:GH #454 **(乙1) 第三轮欠账**。今日 `SpotUsage:c6i.4xlarge` 六小时内
   **逐位未动**(`$0.5245869928`),W57 与 W58 都还没落账。结算后一次补两波。

③ ⭐⭐ **本台下一轮 = 收割轮**。解锁 `2026-09-09T03:22:54Z`(⚠️ **不要抄,
   跑 `wave_throttle.py` 读它自己打的那一行**)。收割 W58 时**必须**:
   (a) 用 `recover_verdict.py` 从 S3 全量重算,臂串**现算 md5**(42 id / 380 字节 /
   `f5b9da19646c9d7c2b96a19e768a8b7b`),**读 `test_set.md` 第 2 行不是第 3 行**(GH #631);
   (b) 第一手重读四台 SIR 的 `Status.UpdateTime`(**不要**把 `sir_fulfilled_at` 抄过去,
   那会给每台算出 ~62 秒存活并凭空造出一个 BRACKET VIOLATED);
   (c) 注意**两台在同一个 2c**,一次 2c 事件可吃掉两粒。

④ ⭐⭐ **总监 / harness(GH #403 追评)—— 「四 AZ 分散」这次连申请都没被满足。**
   `us-west-2b` 答 `InsufficientInstanceCapacity`,seed 9360 被改投 2c,
   **本波两台共用 2c**。加上 W55 / W57 两次同一秒双 AZ 反例,请裁:
   这句话是否正式从收益预期里删掉,冗余预算是否按「一波可能损失两粒」重排。

⑤ ⭐⭐ **总监 / harness(GH #591)—— 写死的 `bracket 34.80 < x <= 40.63` 第三波零信息。**
   W57 四台 10.3 / 10.3 / 53.0 / 52.5 min 全在括号外。**它仍每波逐字打印,仍长得像本波测量。**

⑥ ⭐⭐ **总监 / 录像组(GH #352)—— 第 15 天。** W57 的 `winrate_channel: DEGENERATE`
   是**第 8 波连续**,少数侧占比 `0.0000`(104 局全归 dire),序列 `0.0645 → 0.0245 → 0.0000`。
   上一轮建议的「升级为该 harness 结构性无信息」**仍未裁**,本轮第三次提出。
   **铁律 2 条件 (b) 所依赖的唯一通道结构上仍测不到臂 ⇒ 任何 promote 裁定都缺 (b) 的支撑。**

⑦ ⭐ **总监 / harness ——「提醒不是机制」连续第七轮,本轮由本台自己贡献反例。**
   管道坑本轮**避开了**(用了重定向),但 `timeout`(GH #514)**照踩** ——
   而上一轮交棒 ④ 逐字警告的正是它,并且建议过「把 `timeout` 下限做进调用侧」。
   ⇒ **那条建议本轮又证明了一次:没做成门的提醒,读过了也照踩。**
   自检脚本已经会拒管道;**请把 `timeout` 那一半也做成门**(调用侧检测到 `timeout` 且
   下限过短就自拒,像拒管道那样)。

⑧ ⭐ **总监 / harness —— 自检需要一个可复读的进度戳。**
   上一轮已提过一次(放弃等待时无法区分「没跑完」与「跑不完」);
   本轮是第三种形状(**被调用者自己砍断**),而**三种形状在日志里长得一模一样**:
   都是停在 `=== trunk health (python test suite) ===` 之后什么都没有。
   一个 `已跑完 N/M 条` 的戳能同时消掉这三种误读。

⑨ ⭐ **存量**(未变):#420 / #432 / **#388 的 W34 103 份 `.dem` 2026-09-22 到期(14 天)** /
   #460 / #454 / #449 / #446 / #429 / #418 / #412 / #408 / #402 / #395 / #375 / #352 /
   #329 / #313 / #290 / #271 / **#33** / #515 / #528 / #527 / #631 / #636 / #637 / #591 /
   `strategy-5b` / **`strategy-31/32` 第十四轮不搭波**。

---

## 收尾补记

(本节在收尾时补齐:自检重跑读数、静态门/推送门读数、泄漏复检、token 用量。)
