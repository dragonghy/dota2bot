# 批测台 2026-08-29T15:17Z —— 收割 W25:**五连负之后第一次转正**(+40.16 gpm)

**工作单元类型:纯收割轮**(W26 的 6h 闸 `18:16:54Z` 才解锁,本轮**不发波、零 EC2 支出**)。

---

## 一、开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` — **worst exit 3**,8 腿全跑。

- `FINDINGS (exit 3) = cadence`;**`UNCERTIFIABLE (exit 2) = none`** —— 没有"这轮没人看过"的腿。
- ⭐ **trunk 两侧都绿了**:python **52 passed / 0 failed / 0 uncertifiable**、fast Lua **28 个检测器文件 0 失败**。
  06:20Z 那轮 trunk 两侧同时红(python 50/2、fast Lua 1/21)是 **GH #295** 的立案读数;
  **本轮复测两侧全绿 ⇒ #295 的回归已被修掉**,建议总监核一眼后关闭。
- 锚点:`stable-v1` / `stable-v2` 三项 `EXISTS/PINNED/SHIPPED` **全 ok**;`unlanded_commits` 无。
- ⚠️ cadence 三个洞,最大的一个是 **director `08-29T04:08Z → now` 11.0h**
  (另:director `08-28T13:07Z→17:20Z` 4.2h、replay-check `08-29T00:55Z→05:30Z` 4.6h)。
  总监 11 小时没交工作单元,而本轮恰好有**三件事要它裁**(见六),点名一下。

## 二、成本与泄漏(开工)

- 开工 `describe-instances` running/pending —— **空**。
- MTD **$66.19**(`budgets/dota2bot-batch`,免费);≥$35 触发 CE 复核 **$66.1902331785** —— 逐位一致,**连续第二十二轮**。
- forecast 80.585,budget limit 100.0。⚠️ **budget 快照刷新于 `2026-08-29T12:39:06Z`** ——
  在 W25 起飞(12:16Z)之后、自毁(13:00–13:12Z)之前,**裸 MTD 未必含 W25 全额**。
- **围栏 = 66.19 + W25 尾款 ~$0.5 ≈ $66.7** ⇒ **闸 (iii) $80 通过,保守余量 $13.3**;
  刹车 $90 未接近;$50 档已过、**下一档 $80 本轮不跨**。
- **本轮现金 = CE $0.01,无 EC2**(不发波)。

## 三、⭐⭐ W25 收割 —— 主产出

44-id 全集 vs stable,树 `b51bac77`,四粒种子 1603/1633/1664/1770,**四台全部自毁**
(`instance-terminated-by-user`,13:00:13Z–13:11:44Z),**零抢占**。
`recover_verdict.py ./pool "<44-id 串>"`,196 个 per-game 文件全部解析成功
(`unparseable 0`、`pooled_basename_dirs_overridden 0`),**172 局计分**、24 局是未打戳的暖场局、`unfinished 0`。

```
mean:  gpm +40.16   xpm +25.57   deaths -0.30   last_hits +1.72   winrate 0.560
comps_better:  gpm 3/4   xpm 3/4   deaths 3/4   last_hits 2/4   winrate 2/4
min_arm_depth 8   thin_arm_seeds []   winner_by: engine_natural 196/196
suggested: promote
```

逐种子(ab = radiant armed,ba = dire armed):

| seed | ab gpm | n | ba gpm | n | (ab+ba)/2 | arm_depth | winrate |
|---|---|---|---|---|---|---|---|
| 1603 | −18.58 | 31 | +108.17 | 15 | **+44.80** | 20.22 | 0.500 |
| 1633 | +388.86 | 29 | −97.52 | 13 | **+145.67** | 17.95 | 0.703 |
| 1664 | +29.84 | 27 | +57.44 | 15 | **+43.64** | 19.29 | 0.537 |
| 1770 | +76.91 | 28 | −223.84 | 14 | **−73.47** | 18.67 | 0.500 |

### 3.1 ⭐ 这是 W20 以来第一次转正,但**不能并进那条序列**

W20 −52.15(41-id)、W21 −41.84、W22 −49.67、W23 −37.12(43-id)、W24 −43.12(42-id)、
**W25 +40.16(44-id)**。**id 集合逐波不同,按章程与铁律 3 不可池化成一条序列** ——
所以"五连负后转正"是**符号事实**,不是一条趋势线的拐点。

### 3.2 ⭐⭐ 真正硬的证据不是波均值,是**两粒共享种子**

W24 与 W25 共享 **seed 1603 与 1633**。同一粒种子 = 同一份镜像 draft = 两边同样那 10 个英雄,
是本项目噪声最低的一种比较:

| seed | W24 (42-id) | W25 (44-id) | Δ |
|---|---|---|---|
| 1603 | −62.51 | +44.80 | **+107.30** |
| 1633 | −48.55 | +145.67 | **+194.21** |
| 共享均值 | −55.53 | +95.23 | **+150.76** |

**两粒共享种子同向、同号、量级都大。** 这比 4 粒种子的波均值(见 3.3 的诚实边界)结实得多。

### 3.3 诚实边界:波均值本身**分辨不出零**

逐种子离散度极大:sd **89.58**、SE **44.79** ⇒ **mean/SE = 0.90**。
留一法:

```
drop 1603 -> +38.61    drop 1633 -> +4.99    drop 1664 -> +39.00    drop 1770 -> +78.03
```

**去掉 1633 这一粒,+40.16 塌成 +4.99** —— 波均值的绝大部分由单粒种子扛着。
⇒ **本台的结论不是"测试版变好了 40 gpm"**,而是:
① 波均值方向翻正、`suggested` 首次给出 `promote`;② 共享种子那条**低噪声**读数强烈同向。
两条合起来值得下游认真查,**但单靠 W25 一波不构成 promote 依据**。

### 3.4 铁律 4(i) 分层读数 —— 并**按 GH #299 的范围读**

```
POOLED gpm:  ab = +119.25   ba = −38.94   mean = +40.16
逐腿反号 3/4 粒(1664 两腿同为正号,是唯一不反号的)
```

按 4(i) 字面("两层反号 = 噪声"),W25 该被判噪声。**本台不这样读**,理由是本台自己在
**GH #299**(09:13Z 立,**至今未裁**)已经算清楚的:镜像波的**逐腿经济读数不是 contrast 量**,
`ab = 效应 + 侧偏`、`ba = 效应 − 侧偏`,**反号正是这个设计的预期签名**;W24 16/16 + W23 14/16 = 30/32,
`P(X≥30|Binom(32,0.5)) ≈ 1.2e-7`,**是结构不是噪声**。W25 的 3/4 与之同族。

⭐ **本轮给 #299 补第三份同号残差**:`(ab−ba)/2` = **W25 +79.10(实测)**、
**W24 +68.22(本轮实测,且本轮复算的 W24 池化 ab/ba 与归档读数 +25.10/−111.34/−43.12 逐位一致
—— 这同时是本轮方法学的自校验)**、W23 +63.5(由 #299 数字推得)。**三波同号同量级**,方向与已登记的 Radiant 侧偏一致。
按铁律 4(iii),四粒种子太少,**仍不登记为效应量**。

> **⚠️ 顺带更正 #299 的一处标注**:它写 `(ab-ba)/2 = +136 (W24) / +127 (W23)`,
> 但那两个数是 **(ab−ba) 未除 2**;除 2 后是 **+68.22 / +63.5**。
> **论证与结论不受影响,印出来的数字大了一倍**,已在 #299 追评更正,免得日后被引用。

### 3.5 代码面的事实(**本台不做归因**,只交事实)

`2d1024ee`(W24 树)→ `b51bac77`(W25 树)在 `bots/`/`game/` 上只有两个提交:

- `f11e1e26` hero 08:08Z — `bots/BotLib/hero_skeleton_king.lua` **+63**(`wkqdmg`)
- `351389eb` strategy 10:43Z — `bots/FunLib/jmz_func.lua` **+129**(`fieldsip`)

而 **`fieldsip` 未被总监裁定入集,不在 W25 的 44-id 串里 ⇒ 本波恒 false、走出厂默认、行为上惰性**。
`odbuild` 的代码在 W24 树里就有,但**不在 W24 的 42-id 串里**。

⇒ **W24 → W25 之间真正换了状态的杠杆恰好两个,且都属英雄组:`odbuild`、`wkqdmg`。**
这是少见的干净归因面。**归因本身归录像组/策略组**(章程),本台只交这个事实。

### 3.6 搭车前置门 —— `hero-23` 的那道**已经过了**

上轮交棒 ② 要求两个搭车 id 先过前置门再读数。本台顺手把 WK 那道算了(免费,数据在手):

```
skeleton_king       ARMED n=56 mean_lvl 24.36  min 19  max 29   pct_level>=10 = 100.0%
skeleton_king       base  n=70 mean_lvl 22.96  min 17  max 29   pct_level>=10 = 100.0%
obsidian_destroyer  ARMED n=71 mean_lvl 24.24  min 20  max 29   pct_level>=10 = 100.0%
obsidian_destroyer  base  n=59 mean_lvl 22.69  min 18  max 27   pct_level>=10 = 100.0%
```

**`hero-23` 的"WK 等级分布须到 10"前置门:100% 通过,不必退回** —— 读数可解释。
`hero-22` 的 `skill_point_stall.py`(OD 仍在 STALL 表则标 `UNINTERPRETABLE`)**仍须英雄组自己跑**,本台不代跑。
⚠️ 两个英雄 armed 腿都比 baseline 高约 **1.5 级**;**这不是归因** —— 赢得多的一方全队都升得快,
该读数**不能**单独当作这两个 id 有效的证据。

## 四、两道登记在册的验收门

**(a) `reclaim_blind.py --wave-json` — exit 0,NOT BLINDED**

```
changeover 40.0 min (bracket 34.8 < x <= 42.6)   min_arm_depth 8.0
  1603 54.8min ab31/ba15 PAIRED   1633 43.1min ab29/ba13 PAIRED
  1664 45.1min ab27/ba15 PAIRED   1770 45.4min ab28/ba14 PAIRED
yield: 4 paired of 4     attribution: 0 reclaimed before the flip
NEXT WAVE: spot (GH #158 unchanged)
```

**(b) `rec_slot_cost.py` — exit 1,⚠️ 有一个录制槽超差**

W25 用 `--rec-slots 12`(即 08-22 那三条路里的 (C),留 slot 13-16 作对照腿 —— 验收因此**结构上成立**,
不像 16/16 那样必然 exit 2)。box factor(对照槽 now/baseline)**1.097**,以下均已扣除:

```
slot 1   net −7.4%   <-- DEFICIT beyond --tolerance 5%      slot 2..12  net −1.8% ~ +0.5%
slot 13  +0.6%   slot 14  −0.2%   slot 15  +0.1%   slot 16  −0.5%      (对照腿)
compared 16 slot(s); 1 recording slot beyond tolerance
recorder 局数 12~13,对照槽 12.50 均值 —— 局数通道无损失
```

**登记在册的失败分支是「exit 1 ⇒ 退回 `--rec-slots 1`」,本台照办**:
**W26 的默认值定为 `--rec-slots 1`**,除非总监在 `18:16:54Z` 之前另裁。
判据是**事先登记**的,章程写明"不许事后改判据",所以本台**不自行改判**。

但**逐槽表按章程要贴出来由总监重裁**,而这张表的形状值得它看:
**12 个录制槽里 11 个在 ±2% 内,唯一超差的是 slot 1**,而 slot 1 的 baseline(1.952)
恰恰是 baseline 剖面里 1–12 号槽的**最高值** —— 因为 baseline 的 `rec_set` 就是 `[1]`,
章程自己记过"**录制槽是 12/12 波里最快的那一槽**,残差 +8.2%"。
⇒ 这个"赤字"更像是 **slot 1 从当年的异常快回到大盘**,而不是 12 路并发新增的代价。
**这是事后观察,本台不拿它推翻事先登记的门**,交总监裁。
⚠️ 代价面要提醒:退回 1 会把帧通道从 **12/16 砍回 1/16**,而 **owner P1/P2 两项都卡在
条件 (a) 的帧证据上** —— 退回 1 直接伤 owner 优先项,这一点请总监连带权衡。

## 五、收尾泄漏检查

`check_costs.sh` 的 running/pending 段 **空**;四台 W25 的 SIR 全 `closed / instance-terminated-by-user`。
**本轮不发波 ⇒ 收尾判据就是章程给纯收割轮写的那句「0 台在跑」,已满足,零泄漏。**

## 六、交棒

1. **⭐⭐ 英雄组 —— W25 是 `odbuild`+`wkqdmg` 的取证波,数据已就绪 —— 已立 GH #309。**
   `hero-23`(WK)前置门本轮已代验:**100% 到 10 级,通过**,可直接读。
   `hero-22`(OD)**仍须自己先跑 `skill_point_stall.py`**;OD 若仍在 STALL 表,按上轮裁定标 `UNINTERPRETABLE` 退回。
2. **⭐⭐ 录像组 —— 共享种子 1603/1633 的行为差分**(事实面同在 GH #309)。W24→W25 在这两粒上 +107.30 / +194.21 gpm,
   同 draft、同设计,**是本项目能拿到的最低噪声对照**;换状态的杠杆只有 `odbuild`/`wkqdmg` 两个。
   请做行为差分定位那 150 gpm 从哪来(**归因归你们,本台不做**)。
3. **⭐⭐ 总监 —— `rec_slot_cost` exit 1 的重裁,须在 `18:16:54Z`(W26 闸)之前 —— 已立 GH #308**(含 (A)/(B)/(C) 三条路)。
   不裁则 W26 按登记分支走 `--rec-slots 1`,帧通道 12/16 → 1/16,伤 owner P1/P2。四、(b) 有逐槽表与代价权衡。
4. **⭐ 总监 —— GH #299 第三轮催,本轮补了第三份同号残差 + 一处数字更正**(已追评);#295 建议核后关闭(trunk 两侧已绿,已追评)。
5. **⭐ 总监 —— 自检 cadence 报你 11.0h 无工作单元**,而本轮有三件事等你裁(3、4 及上轮的 #298/#291)。
6. 存量催办:**#207 `zusstatic` 第二十四波 armed**;**#218 后续 `salveally`/`salveyield` 第二十轮不在第 2 行**;
   **#282 连续第四波零 `PLACEMENT MISMATCH`,再次建议关闭**;#217/#211/#225/#180/#171/#200/#181/
   载体门 PARTIAL/`stable-v*` tag/`campdanger`/§BL.4/#75 照旧。

## 七、铁律 6

`bots/` / `game/` **一行未改**(本轮只动报告、章程、`waves/W25_wave.json`)。
`luacheck_gate.sh` **exit 0 / 0 warnings**(容器冷启,脚本自装 `lua-check`)。
`core.hooksPath` 已上膛,两条 `git push` 各自过了一次静态门;**未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行**。
动态半(`tests/run_tests.lua`,~100min,GH #124)**未跑,且不声称**。
**MCP 未触发 `requires approval`。**

## 八、Token 用量(铁律 8)

```
TOKENS total_in=4,337,761 out=36,532 turns=45
```
(统计时刻之后的收尾回合不计入。)


## 九、本轮发表(GH #290 顺序:先 push 后发表)

两条 `git push` 均 exit 0、`luacheck bots game: 0 warnings`,
发表前 `claim_precheck.sh` 对两份草稿均 **exit 0**(`local commits not on origin/main: 0`)。

- **新立 GH #308** `[batch]` — `rec_slot_cost` exit 1 重裁请求(deadline `18:16:54Z`)
- **新立 GH #309** `[hero]` — W25 取证波读数 + 共享种子归因面 + WK 前置门已代验
- **GH #299 追评** — 第三份同号残差 + `(ab-ba)/2` 数字更正
- **GH #295 追评** — trunk 两侧已绿,建议关闭
