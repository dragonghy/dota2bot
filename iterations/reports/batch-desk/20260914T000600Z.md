# batch-desk 2026-09-14T00:06–00:4xZ — 持有第十三轮

**一句话**:零发波、零收割欠、零泄漏,本轮 AWS 新增计费 **`$0.00`** —— ⭐ **本轮是刹车期里第一轮「两笔该省的钱都真的省下了」**:`$0.01` 的 CE 读没买(判据不满足),通知没推(判据不满足),**两条都是按上一轮写死的字面判据停手,不是按本台的判断**。

⭐⭐⭐ **头号:本轮 MTD 是一次「零信息读数」,而这件事必须先说,否则后面每个数都会被读错。** `budget refreshed` 戳 **`2026-09-13T20:32:29Z`**,与上一轮 `21:10Z` **逐位相同** ⇒ **读的是同一张快照**。MTD `$89.505`、余量 `$0.495` 因此**不是「3.6 小时没涨」,是「3.6 小时没测」** —— ⛔ 不得读成「非算力那条曲线停了」。

⭐⭐ **第二:本台本轮对 §八 那道悬案的处置是「守字面」。** 上一轮自报偏离了写死的通知判据并把该不该放宽交给了总监(交棒 ④),总监**尚未裁**。无裁定期间的保守默认是**守原文**:本轮戳未变 ⇒ 合取式第一项就不成立 ⇒ **不推通知**。⛔ 本台未自行放宽,也未拿「内容确实有新东西」当例外 —— 本轮恰恰**没有**新东西。

⭐⭐ **第三(⚠️ 新发现,不是成本题):`OWNER_PRIORITIES.md` 的 P4.1 条文与总监 09-05 的裁定不一致,而本台已经按过期的那半报了多轮「未认领」。** 详见 §七。

---

## 一、成本(现跑)

- `AWS_SETUP_EXIT=0`(容器无 CLI,自装 `aws-cli/1.46.1 Python/3.11.15 botocore/1.43.62`;身份 `arn:aws:iam::581436534924:user/dota2bot-agent`,逐字 `(restricted batch-runner user — correct)`)。
- `COST_EXIT=0`,⛔ **未走管道读码**(重定向到 `/tmp/cost.log` 后裸读):

```
89.505	USD
   source: budgets/dota2bot-batch (free) | forecast 181.993 | budget limit 100.0 | budget refreshed 2026-09-13T20:32:29Z
   >= $35, but headroom to the $90.00 brake is $0.495
   < $1.10 (cheapest wave) — CE confirmation SKIPPED, NOT passed (GH #801).
   Nothing can launch at this MTD, so confirming it buys nothing.
   Resumes by itself as soon as headroom >= $1.10.
```

- MTD **`$89.505`**;`forecast 181.993` / `limit 100.0` **登记不据以决策**。
- ⚠️ **那四行是 SKIP 不是 pass** ⇒ 本轮 MTD **没有经过 CE 复核**,只有 budgets 一个来源。
- ⭐ **戳逐位未变**(`20:32:29Z`,上一轮同)⇒ **本轮 MTD 与上一轮之间的「增量」是 `$0.000`,而那个零是「同一张快照读两次」的零,不是测量。** 上一轮 §一 立的判据(戳换张才有证明力)本轮落在**否定的一侧**。
- 常驻成本仍只有 AMI `ami-0a990a26d89c66547`(`dota2bot-batch-20260718`)。

## 二、闸(裸读退出码,逐字)

**(i) `THROTTLE_EXIT=0`**:
```
anchor wave      : W69 (4 machine(s))
last machine up  : 2026-09-12T09:26:31Z  <- the anchor (GH #469 ruling 1)
slate spread     : 7s
unlock           : 2026-09-12T15:26:31Z
now              : 2026-09-14T00:14:36Z
margin           : +118085s past unlock
WAVE_THROTTLE: UNLOCKED (exit 0) -- gate (i) passes at this instant.
```
⚠️ **(i) 开着不构成许可**(连续第八轮点名);工具自带限定照抄:`RE-RUN IT AT THE LAUNCH INSTANT: it is the action, not the decision, that the gate binds.`

**(iii) `--planned 1.10` ⇒ `FENCE_EXIT=3`**:
```
crossing registry: 0 record(s) ... none in force -- the gate runs at the DERIVED fence.
budget filters   : none  <- so every instance in the account does land in this budget, whoever owns it
accrual scope    : 17 region(s) read [...+11 more]  <- COMPLETE: every region this account has enabled
wave accrual     : records after 2026-09-13T09:14:29Z (= 2026-09-13T20:32:29Z - 11.3h lag, clock from budget snapshot)
accrual check    : CERTIFIED (0 accruing instances account-wide, read this run) -- pending $0.000 is a reading, not a default.
actual (MTD)     : $89.505   <- re-read this run, never cached
pending waves    : $0.000
planned          : $1.100
projected total  : $90.605
  ACTUAL alert   : $50.00   crossed         AWS state=ALARM
  ACTUAL alert   : $80.00   crossed         AWS state=ALARM
  ACTUAL alert   : $100.00  NOT yet crossed AWS state=OK
fence            : $100.00   <- lowest ACTUAL alert not yet crossed (Ruling 1)
brake            : $90.00   (owner's line, not derived here)
operative ceiling: $90.00   = min(fence, brake)
A launch at this instant would put MTD past the $90.00 BRAKE line.
WAVE_FENCE: THROTTLED (exit 3) -- brake, not fence. Stop and report to the owner; do not wait it out.
```
⇒ **连续第五轮是闸拒绝,不是本台的处置**。余量 `$0.495` < 最便宜一波 `$1.10`,差 **`$0.605`** —— ⚠️ **与上一轮逐位相同,而那是 §一 那条「同一张快照」的直接后果,⛔ 不得读成「缺口停止扩大」。**
- ⛔ **未用** `--no-accrual-check` / `--no-crossing-file` / `--director-crossing` / `--snapshot-instant` / `--pending` ⇒ **无任何「这是 SKIP 不是 pass」行**。
- ⭐ 本轮**没有** `WAVE_FENCE CLOCK ... a claim, not a reading` 那行(快照戳由工具自读,`clock_source = budget snapshot`)⇒ **GH #692 epoch 修复仍在生效**。
- ⭐ **`$80` 档现在是 `crossed / AWS state=ALARM`**(09-13T18:13Z 那轮还是 `OK`)⇒ **owner 侧的第二封预算告警邮件已经发出**,不再需要本台代为转达该档。
- **(ii)/(iv) 本轮未评估,也不声称跑过** —— (iii) 红,走不到那两道门。

## 三、⭐⭐ 那 `$0.01` 本轮**没有**花(⛔ 自证,不自赦)

上一轮交棒 ② 写死了两条**都要过**的判据,逐字:
> `budget refreshed` 戳**变了**(否则拿到同一张,别花那 `$0.01`)**且**时点在 `02:00Z` 之后(否则拿到的仍是下界,销不了欠条)。

**本轮实际**:戳 **未变**(`20:32:29Z` 逐位相同)✗;时点 `00:14Z` **在 `02:00Z` 之前** ✗ —— **两条都不过**。
⇒ **不买。** 逐日 CE 表本轮**未拉**,`CE_DAILY` 本轮**不存在**,⛔ 本报告不含任何 09-13/09-14 的逐日读数。

⭐ **这一条值得单独记一笔**:上一轮买那 `$0.01` 时只过了两条中的一条(戳变了、时点没到),自报「欠条仍欠」;**本轮两条都不过,于是一分没花**。⇒ **判据本身这一轮是活的**:它挡下的正是「反正闲着,顺手再拉一次」那种支出,而 §三(上一轮)已量到**本台自己的 CE 调用曾占某一整日成本的 53.1%**。⛔ 本台仍不把自己从那条曲线上摘出去。

⚠️ **欠条不销,判据与时点不变**:零批测日(09-13)的**完整行**仍要 `2026-09-14T02:00Z` 之后才第一次可得。

## 四、⭐⭐ 通知本轮**没有**推(§八 悬案的处置)

上一轮交棒 ① 的判据逐字:
> `budget refreshed` 戳变了 **且**(headroom 跨过 `$1.10` **或** 零批测日完整行第一次可得 **或** MTD 越过 `$90`)。

**本轮实际**:戳**未变** ⇒ **合取式第一项就不成立**,三个析取项无需再看(顺带:三项也**一条都不满足** —— headroom 仍 `$0.495`、完整行仍未可得、MTD 仍 `$89.505`)。
⇒ **不推通知。**

⭐ **这是对上一轮 §八 / 交棒 ④ 的直接处置**:上一轮偏离了这条判据并把「该放宽还是该守字面」交给了总监,**总监本轮尚未裁**。无裁定期间本台取**保守默认 = 守原文**,⛔ 不自行放宽、⛔ 不拿「内容有没有新东西」做替代判据(本轮恰恰**没有**新东西:§一 同一张快照、§五 零新对象)。**交棒 ④ 仍然欠着一个裁定**,但它**本轮没有咬人**。

⛔ **同理未在 GH #779 发第 12 条评论**:上一轮立的是「**有新测量才发**」,本轮**零新测量**(戳未变)⇒ 发出去就是同形投递。

## 五、收割:零欠(现扫)

`S3VAL_EXIT=0`(⛔ 未走管道读码)。`validation/` **592 对象**,与上一轮基线**逐个相同**;最新对象仍是 `tpcommit+46ids-24c30245be01_20260907_2218_run.log` ⇒ **自上一轮零新对象**,W68/W69 早已全量收割,本轮**未新增任何 verdict**,零下载、零 S3 支出。⛔ 不重算已收波次。

## 六、泄漏:四条独立路径全零(⛔ 不合并成一句)

1. `check_costs.sh` 的 `running/pending instances` 区块 —— **空**(⭐ 直接用它自带这段,**未为取它重跑一次**);
2. `describe-instances` 五态(`pending,running,shutting-down,stopping,stopped`,**不加 tag 过滤**)⇒ **零行**,`INST_EXIT=0`;
3. `describe-volumes` ⇒ **零行**,`VOL_EXIT=0`(零孤儿 EBS);
4. 闸 (iii) 自带账户级普查 ⇒ `accrual scope : 17 region(s) read <- COMPLETE` + `accrual check : CERTIFIED (0 accruing instances account-wide, read this run)`。

常驻仍只有 AMI `ami-0a990a26d89c66547`。⛔ **#515/#528 立案理由一字未变**:`budget filters : none`。

## 七、⭐⭐ 铁律 9:P4.1 的条文与裁定不一致(新发现)

`OWNER_PRIORITIES.md:99` 至今逐字写着:
> **4.1 upstream 标尺波(最高优先,批测台下一轮就发;⛔ … 下一批测台轮**必须发标尺波,排在任何 arm 波之前**,再空一轮即按铁律 9 写进 DECISIONS_NEEDED 向 owner 说明)**

而 `DECISIONS_NEEDED.md:436–441`(总监 **2026-09-05T19:xxZ**,GH #535)已裁:
> - P4.1 = **部分完成**(胜率 + 局数买到,gpm 未买到);
> - 「标尺波排在任何 arm 波之前」的排序条款**本轮释放** —— 理由不是够了,是**再发一遍同一条路,按构造买回来的还是同样的三分之二**,每次 ~$1.09;
> - ⛔ 因此**上面那句「每周跑一次标尺波」暂缓执行**:先做量具(让记分板非零),再谈把曲线连起来。

⇒ **排序条款已于 09-05 释放,而 `OWNER_PRIORITIES.md` 里它一字未动**,`:139` 仍写「当前球:4.1 批测台」。
⚠️ **后果是本台自己承担的**:上一轮(及此前数轮)报告逐字写「**4.1 仍挂『最高优先 / 批测台下一轮就发』,本轮仍未认领**」—— 那是**按已被释放的那半条文**自报未认领。**实质上没有做错事**(条款释放了,不发标尺波是对的;何况 (iii) 红,发什么波都发不出去),**但台账上背了一条不存在的欠账**,而铁律 9 的健康巡检正是按「优先项零推进轮数」升级的。
⛔ **本台不代改 `OWNER_PRIORITIES.md`**(owner 原话:该文件只有主会话维护),⛔ 也不宣布 P4.1 已完成 —— 总监裁的是**部分完成**,且 (A)/(B) 那个完成定义的拍板**至今未收到 owner 回复**(默认按 (A) 的实质推进但不改文件)。**⇒ 交棒 ④。**

## 七bis、⭐ 队列:刹车解除那一刻欠的是**什么**(本轮现扫,零成本)

`queue.json` **121 条请求;按铁律 5 / GH #317 的口径(只有 `done`/`rejected` 算已关)已关 10 条 ⇒ 开放 111 条**。按「**要不要新开一台机器**」重新切一刀:

- **零条请求在申请专波。** 开放行全部落在三类:(a) **零 EC2 只读归档扫描**(`ROUTED_ARCHIVE_SCAN` / `APPROVED-SCAN`,`hero-2/30..70` 那一长串同一次遍历);(b) **搭车**(`ROUTED_RIDESHARE` / `APPROVED_ADMITTED`,措辞逐字是「**任一常规波次**(搭车,零 EC2 增量,零新波次)」/「下一波例行全集串」);(c) **冻结/驳回**(`FROZEN-HOLD`,P4.2 armed > 20;`REJECTED`;`HOLD-DOMAIN-EMPTY`)。
- ⇒ **刹车解除时本台欠的不是某一条点名的波,是「下一波例行全集串」本身** —— 一发,(b) 类那二十几条 `harvested_pending_verification` / `ADMITTED` 一次全部上机。⭐ **这条对交棒 ① 有用**:owner 那三选一的收益不是「跑某一个 id」,是**一次解开二十几条搭车行**。
- ⛔ 本台**未改任何一行 status**,也**不代总监裁**任何一条。

⭐ **交棒 ⑤ 本轮可以从「`running 3`」升级成点名**:三条 `status=running` 的行是 **`strategy-31`**、**`strategy-32`**(均 `ROUTED_RIDESHARE / ADMITTED`,搭车行,成员串 52→54)与 **`hero-27`**(`ROUTED_ARCHIVE_SCAN`,**零 EC2 归档扫描**)。⇒ ⚠️ **与账户级 `CERTIFIED ZERO` 其实并不矛盾**:三条没有一条需要一台机器(两条是搭车登记、一条自述零 EC2),**`running` 在这里记的是「某个组的分析在进行中」,不是「有实例在跑」**。⇒ **连续第五轮报的这条「不相容」,本轮查实是词表歧义不是泄漏**(泄漏另有四条独立路径在 §六,全零)。⛔ 本台仍不代改状态,但**把「不相容」改述为「`running` 一词在本队列里同时承载两种语义」**交总监。

⚠️ **顺带一条有硬时限的**:`hero-27` 的 `director.wave` 自带逐字 **「有硬时限:GH #388 的 W34 语料 `dem21/` 2026-09-22 自动删除」** ⇒ **距今 8 天**,而它仍是 `running`。⛔ 非本台的活(归档扫描),**点名不代做**。

## 八、自检(铁律 10)

⭐ **本轮开工第一条命令一次到位了一半**:仍先踩了**管道坑第 29 次**(`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.` —— 脚本自卫当场拦下,零损失),第二跑给了 `timeout 400` 又被砍(`EXIT=124`,⛔ 没跑完不是通过),**第三跑才用上 `run_in_background` + 重定向 + 不设 `timeout`**,并且**用 `wait`/后台任务等它,⛔ 未用命令名做 `pgrep` 轮询**(上一轮 §十 那个「四个等待者全在等自己」的坑本轮未复发)。
⇒ **上一轮交棒 ⑥ 要求「一次到位」,本轮仍未做到**(第 29 次同形)。⛔ 不辩解。

⭐ **本轮按 09-13T03:11Z 查实的结论,把 push 排在自检跑完之后**(自检跑着时 push 会把 `test_py_gate_hook.py` 5b 顶成假红)。

**真码与 findings 见「补记 (B)」**(⛔ 后台跑完才读,**不预写**)。

**已跑完的腿的第一手读数(报告定稿时已出的部分)**:
- `promote-atom constraints: OK`(5 个 atom 全 GATED);
- `INVERSE-GATE CENSUS  live gate ids 218  armed 26 / FROZEN 0  COUPLED 1  UNRESOLVED(armed) 0`;`no armed id hangs under an unarmed gate -- OK`(⭐ 活 gate 数 216 → **218**,armed 仍 26);
- `9 anchor(s) checked -- OK`(stable-v1…v9 的 EXISTS/PINNED/SHIPPED 全 ok);
- ⚠️ **`UNCOVERED SET GREW` —— 2 个新文件没有任何自动读者会跑**,逐字:
  ```
  LUA GATE COVERAGE  disk 451 | push gate 322 | 开工自检 leg 88 | known_red 9
    UNCOVERED (no automatic reader runs these): 115 of 451 (25%)  {'no_manifest_row': 52, 'timed_out': 61, 'too_slow': 2}
      NEW UNCOVERED  no_manifest_row    tests/test_cm_r_retreat_crowd.lua
      NEW UNCOVERED  no_manifest_row    tests/test_dusttower_dive_guard.lua
  ```
  两者分别是昨晚 **22:57Z 英雄组 `cmrflee`**(`546f7e31`)与 **22:45Z 协同组 `dusttower`**(`570ac13d`)随改动落的新测试。⛔ **本台不代登记、不新开 issue** —— 修复路径本身已被 **GH #783** 立案(「登记一条新 Lua 测试的唯一被许可动作 re-measure 会清空 `known_red` 赦免名单」),另有 **GH #804**(lua_gate fail-open 无成本上界)在同一条缝上;**再开一条就是重复立案**。**⇒ 交棒 ⑥,点名不代裁。**

## 九、交棒

① ⭐⭐⭐ **owner —— 刹车第十三轮持有,GH #779 第十一轮零表态。⚠️ 本轮没有任何新测量,因此本台既未推通知也未发第 12 条评论(判据如此)。** 三条选项逐字不变:维持 `$90` / 给 `dota2bot-batch` 加成本分配标签过滤器(`budget filters : none`,MTD 的 41.8% 是**外来机型** `c7a.16xlarge`+`r7a.16xlarge`,扣掉后本台九月 MTD ≈ `$52.07`)/ 一次性解禁到某个数(并说明是否含外来支出)。⛔ 本台不建议、不预选、不自行改线。**已在 `DECISIONS_NEEDED.md` §15 挂着,不重复立条。**

② ⭐⭐ **下一轮本台 —— 零批测日完整行,欠条仍欠,而下一轮是第一个可能销它的轮次**:`2026-09-14T02:00Z` 之后可得,下次触发(~`03:00Z`)在其之后。⚠️ **两条判据仍都要过**:戳**变了** **且** 时点在 `02:00Z` 之后。**本轮两条皆不过,一分未花;下一轮若戳仍未变,照样不许买。**

③ ⭐⭐ **总监 —— 上一轮交棒 ④ 仍欠裁定**(通知判据该放宽一项还是本台守字面)。**本轮本台守了字面,且守字面的结果是正确的**(零新测量,推了就是同形投递)⇒ **这条不再紧急,但仍未决**;下一轮若戳换张而三个析取项仍一条不中,它会立刻再次咬人。

④ ⭐⭐ **主会话 / 总监 —— §七 的条文不一致**:`OWNER_PRIORITIES.md:99/:139` 仍带着 09-05 已被释放的「标尺波排在任何 arm 波之前 / 当前球:4.1 批测台」,而 `DECISIONS_NEEDED.md:436–441` 已裁 P4.1 **部分完成 + 排序条款释放**。⛔ 本台不代改该文件。**顺带**:该处 (A)/(B) 的完成定义拍板至今未收到 owner 回复。

⑤ ⭐ **总监 —— `queue.json` 的 `running 3`:本轮从「点数」升级成「点名」,并且结论反转。** 三条是 `strategy-31` / `strategy-32`(搭车登记行)与 `hero-27`(**自述零 EC2** 的归档扫描)—— **没有一条需要一台机器** ⇒ 与账户级 `CERTIFIED ZERO` **并不矛盾**,此前四轮报的「不相容」**本轮查实是词表歧义**:`running` 在本队列里同时承载「有实例在跑」与「某个组的分析在进行中」两种语义。请裁该不该拆词(⛔ 本台不代改状态)。**⚠️ 同时带出一条有硬时限的**:`hero-27` 自带逐字「GH #388 的 W34 语料 `dem21/` **2026-09-22 自动删除**」⇒ **距今 8 天**而它仍 `running`,⛔ 非本台的活,点名不代做。

⑤bis ⭐ **owner/总监 —— §七bis:刹车解除的收益比「跑一个 id」大得多。** 开放队列里**零条在申请专波**,(b) 类搭车行有二十几条,措辞逐字是「任一常规波次」/「下一波例行全集串」⇒ **一发例行全集串,它们一次全部上机**。这可以直接并进交棒 ① 那三选一的收益侧。

⑥ ⚠️ **总监 / 英雄组 / 协同组 —— §八 的 `UNCOVERED SET GREW`**:`tests/test_cm_r_retreat_crowd.lua`(英雄组 `546f7e31`)与 `tests/test_dusttower_dive_guard.lua`(协同组 `570ac13d`)**没有任何自动读者会跑**,红了只会被**下一个开工的组**发现(GH #624/#806 的立案形状)。修复路径本身卡在 **GH #783**。⛔ 本台未开新 issue(重复立案)。

⑦ ⚠️ **下一轮本台 —— 开工第一条命令**:`routine_selfcheck.sh` **重定向 + `run_in_background` + 不设 `timeout`**,**一次到位**。本轮**仍是分三步才做对**(管道坑第 29 次 + 超时坑第五次),与上一轮**完全同形**。

## 十、铁律 11 与改动面

- **铁律 11**:**零 `requires approval`**、**零空转**(自检后台跑,成本/闸/收割/泄漏/铁律 9 并行推进)。
- ⛔ **本会话 `bots`/`game`/`tests`/`tools` 一行未改** ⇒ **无一条 trunk red 由本轮引入**。产物只有 `iterations/` 下的本报告与章程「当前状态」条目。
- **本轮每一个读了退出码的命令是否走管道的自查**:`AWS_SETUP_EXIT` / `COST_EXIT` / `THROTTLE_EXIT` / `FENCE_EXIT` / `S3VAL_EXIT` / `INST_EXIT` / `VOL_EXIT` —— **全部重定向到文件后裸读,零管道**。
- **报告序列按文件名排序取得**(`ls | sort`),⛔ **未用 `ls -t`** —— 上一轮 §十 那条「git clone 的 mtime 全同,`ls -t` 对仓库文件无意义」的教训本轮已落实,基线一次就对(上一轮是 `21:10Z`)。
