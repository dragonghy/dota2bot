# 批测台 2026-09-05T03:13–03:5xZ

**本轮零发波、零新增支出。产出两件:W47 的两粒补跑收割到手、W47 因此第一次
完整(4/4 粒);以及上一轮那个「硬窗口」按构造根本抓不到 —— 上一轮的缓解措施
被逐字执行了,码还是丢了,因为那个截止时刻是从错的量推出来的。**

---

## 一、自检(铁律 10)

⚠️ **开工第一条命令第 7 次误用管道**,脚本自己拒绝并逐字点名(「recurred 5x,
every time as the first command of the round」)。改重定向重跑。

⚠️ `timeout 300` **超时 `EXIT=124`** —— 9 条腿里跑到 `trunk health (python test
suite)` 那一腿被截断。**这不是通过也不是 trunk 红**(同 GH #358 形状):
`legs run` 至 `promote-atom constraints: OK` 为止全部有读数,python 腿**本轮没人看过**。
本轮**不声称** trunk 干净;上一轮记录的三条红(#490 第三轮 / #497 第二轮 /
`test_mutstand_restore_trap.py`)**本轮无新信息,仍挂着**。

跑出读数的腿:
- `unlanded work`:`OK: no unlanded work in the certifiable window.`(797 refs
  扫描,767 条 `REFUSED` 为浅克隆不可判定,`certifiable refs 30`)。
- `report cadence`:**1 finding** —— `GAP cadence director 09-04T19:10Z ->
  09-04T23:06Z (3.9h)`,**不是本台**。
- `stable version anchors`:stable-v1 / stable-v2 `EXISTS/PINNED/SHIPPED` **全 ok**。
- `promote-time constraints`:141 个活 gate id、13 个带 PROMOTED 注记,
  **`FROZEN none`**(没有活 gate 点名已 promote 的 id ⇒ `pullcad` 陷阱未复发);
  `field_hold_needs_magnitude` 原子三个 id 全 `GATED`,**OK**。
- `owed executions`:`campgrade_wave_type_premise_reading`(#480/#241)
  **executor 含本台,`campgrade` 独占波仍不存在,这一棒本台仍欠着**(第 N 轮)。

## 二、⭐⭐⭐ 头号产出:那个硬窗口按构造抓不到,而上一轮什么都没做错

上一轮把「必须在 ~03:32Z 之前读两台补跑机的 EC2 `StateReason.Code`」写进了
`W47_wave.json:rerun[].status_code_note`,**明确说「不靠谁记得」**。本轮:
那条记录**被读到了、被排在第一位、AWS 是本轮第一件实质动作** —— **码还是丢了。**

**实测**(2026-09-05T03:13–03:14Z):
```
describe-instances --instance-ids i-07dea406c173a8742 i-0bc15fcc426fe50a0
  -> DESC_EXIT=0, Reservations: []          # 注意是空表不是 InvalidInstanceID.NotFound
describe-instances (region-wide, 无过滤)
  -> total instances: 0                      # 连终止记录都没了
```
**空表本身就是证据**:没有报 `InvalidInstanceID.NotFound`,而是 0 条预留。

**算术(这是本条的全部内容)**:
| 量 | 值 | 来源 |
|---|---|---|
| 起飞 | `00:32:48Z` / `00:32:53Z` | 上一轮 `LaunchTime` |
| 看门狗上限 | 2h ⇒ `02:32Z` | 发波参数 |
| 上一轮据此推的截止 | **`~03:32Z`** | `02:32Z + 1h`(GH #375) |
| **末次 S3 上传** | **`01:14:14Z` / `01:15:45Z`** | **本轮实测** |
| ⇒ 实际干完活 | 起飞后 **~41–43 min** | 同上 |
| ⇒ **真实截止** | **`~02:15–02:20Z`** | 完成即自毁 + 1h |
| 本轮触发 | `03:13:07Z` | — |

**上一轮的截止时刻晚了约 1 小时 15 分,因为它是从看门狗上限推的,而机器不跑到
上限就自毁。** 更要命的是间隔:**上一轮结束 ~00:4xZ,本轮触发 03:13Z ——
机器出生、干活、死亡、记录过期,全部发生在两次触发之间的空档里。**

⭐ **所以这不是记性问题,是算术问题**:在 ~2h 的触发节奏下,一台 ~45min 干完
就自毁的按需机,它的退出码**按构造读不到**,**任何批测台排班都抓不到它**。
W45 那两行就是这么丢的,本轮是第二例,也是第一次把算术钉住的一例。

**耐久性缺口(新查明)**:S3 里**没有任何终态 sidecar** ——
4723 那个 run 的对象类型是 `analysis.json 48 / demclaim.json 24 / log.gz 48`,
**全是逐局产物**。⇒ 退出码**只**活在那个会衰减的 API 里。
**建议的修法**(交总监/harness):让机器在自毁前把自己的退出原因+时刻写一份
`shutdown.json` 到 S3,**码就不再依赖 API 窗口**;退而求其次,截止时刻改从
「预计完成」(末次上传 +1h)推,不要从看门狗上限推。已开 issue。

两行 `status_code` 本轮**如实填 `UNREADABLE`**,并把上面的算术、实测命令、
「上一轮没做错」这三件一起写进 `status_code_outcome`,**不写成推测的码**。

## 三、⭐⭐ 收割:两粒补跑全中,W47 第一次完整

**臂串本轮亲手从发波树 `15b0d9fd` 上重算**(`git show <树>:iterations/streams/test_set.md`
第 2 行)⇒ **62 id / 550 字节 / md5 `c7e1f92c739f8f8dc0ee0c9484288628`**,
与 W46/W47 登记值**逐字节相同**。

**方法**:两个 run **分目录下载再带 `<run_id>__<tag>` 前缀合并**(2026-08-19
踩过的同秒 basename 撞车,那次静默丢了 15.9% 的局)。
`recover_verdict.py` **`RV_EXIT=0`**(重定向读 `$?`,不走管道)。
**三个 override 一个都没用** ⇒ **本轮没有「这是 SKIP 不是 pass」的行要抄。**

补跑两粒:96 局 loaded / 0 unparseable。
| 种子 | ab/ba | arm_depth | gpm |
|---|---|---|---|
| 4723 | 28/15 | **19.53** | +5.79 |
| 4763 | 30/12 | **17.14** | +45.68 |

**两粒都过 GH #269 深度门(≥8),`thin_arm_seeds` 空** ⇒ W47 丢给 spot 抢占的
两粒**全部收回**。

## 四、⭐⭐⭐ 补完一个波,把它的头号读数**符号翻了过来**

**构造(先说清楚这是什么、不是什么)**:W47 四粒 = 两粒干净原机(4678、4950)
+ **两粒补跑**(4723、4763),**同一棵树、同一条臂串**。被抢占的 4723/4763
原始残局**保持作废、不并进来** —— 这是章程**事先登记**的回收处置(作废那一粒、
补跑那颗种子),**不是新的并池决定**。⛔ **这是波内补完,不是 GH #363/#449
的跨波并池分叉。**

`RVFULL_EXIT=0`,206 局 loaded / **183 计分** / 0 unparseable / 0 未完成,
四粒 arm_depth **18.67 / 19.53 / 17.14 / 27.11**,`thin_arm_seeds` **空**。

**四量(每粒先 swap-average,再跨粒取算术平均;永不按局加权 —— 规则 4(i-d))**:

| | 半波读数(n=2,一直挂在记录和 11 行队列里) | **完整波(n=4)** |
|---|---|---|
| gpm | **−5.95** | **+9.89** |
| xpm | −8.68 | −4.63 |
| deaths | +0.25 | +0.24 |
| last_hits | −2.15 | −2.00 |
| comps_better(gpm) | 1/2 | **3/4** |

⛔ **补完这个波把它头号指标的符号翻了过来。** 下游任何按 −5.95 定过范围的东西,
都是按**半个波**定的。

⚠️ **诚实边界:+9.89 不是「变好」的证据。** 逐粒 gpm
`4678 −24.54 / 4723 +5.79 / 4950 +12.63 / 4763 +45.68`(极差 **70.2**),
对 GH #30 的经验零点(每粒 σ≈30,4 粒 SE≈15)**落在噪声里**。
站得住的说法是**「gpm 上没有明显负面」** —— 那恰好就是铁律 2(b) 要的粗粒度读数,
**不是显著性**。而且四量图景是**混合的**:**deaths 1/4、last_hits 1/4 仍在有害那侧**。

**分层登记(规则 4(i-a),照登不结论)**:
```
gpm       ab +144.28  ba −124.49  side +134.38  sign_flip=true   side_gt_arm 4/4
xpm       ab  +90.58  ba  −99.84  side  +95.21  sign_flip=true   side_gt_arm 4/4
deaths    ab   −0.94  ba   +1.41  side   −1.18  sign_flip=true   side_gt_arm 4/4
last_hits ab   −2.07  ba   −1.92  side   −0.07  sign_flip=FALSE  side_gt_arm 4/4
```
**读法(规则 4(i-c))**:这四个都是每粒 swap-average 过的估计量,
**反号 = 恒等式 |side|>|arm|,不是否决理由** —— 登记,不据以结论。
⭐ **但 `last_hits` 是例外且值得单独点出来:它不反号**(两层 −2.07 / −1.92
**同向**),⇒ **`last_hits ≈ −2` 是这四个读数里最稳的一个。**

**胜率**:`winrate_channel` **DEGENERATE 第七轮连续** —— 少数侧只赢 19/206
(占比 **0.0922** < 0.20),dire 拿下 **187/206 = 90.8%**,界限 `0.5 ± 0.1512`
⇒ **不得作为铁律 2(b) 的支持引用**(GH #352)。

**裁定不归本台**:promote/reject 是总监的;条件 (a) 的核验是**录像组的棒,仍欠着**。

## 五、家族到 8 粒 —— 并池分叉第七次到期,本台不自裁

W47 补完 ⇒ 62-id 家族 **W46 四粒 + W47 四粒 = 8 粒**,正是章程点名的那个前提。
**本台不并池** —— GH #363/#449 的跨波并池分叉**第七轮仍未裁**,交总监。
(本轮做的是**波内**补完,与那个分叉不是一件事,已在波次记录里写死这句区分。)

## 六、成本与围栏(闸 (iii))

`COSTS_EXIT=0`。九月 MTD **`$17.773`**(免费 `budgets`,
`refreshed 2026-09-04T22:42:52Z`)。⚠️ **该快照仍早于本轮两台补跑机起飞
(00:32Z)⇒ 按构造不含它们**;按 (乙) **不做增量对账**。

围栏 = `17.773 + 1.02(W46) + 0.7413(W47 已结) + ~0.98(本轮两台按需补跑实测估) ≈ **$20.51**`
vs `$50 / $80 / $90 / $100` ⇒ **全部满足,两种围栏读法下都满足**
(⚠️ 上一轮开的 **#504**:围栏常数 `$80` 跨月失去推导、九月按规则应是 `$50`,
**仍未裁**;本轮同样不受它影响)。**本轮零发波 ⇒ 零新增支出。**

⚠️ **#505 的燃烧率仍未裁**:AWS 自己的 forecast **`$119.286`** vs `limit $100`
(本轮 `check_costs.sh` 原样打出),外推 `$90` 刹车约 **9-21** 撞线。

**GH #454 新测量点(两台按需机)**:
- **下界 `0.691 h/台`(实测,起飞 → 末次上传 41.4 min)—— 硬的**;
- **上界 `< ~1.67 h`**(03:13Z 记录已过期 ⇒ 按 ~1h 保留窗口,终止早于 ~02:13Z);
- 点估 **~0.72 h/台**(若自毁紧跟完成)。
⇒ **与 `0.745 h/台` 常数相容,但不够紧、不足以重新推导它。本轮据实报区间,不报重新推导。**

## 七、泄漏:干净

`describe-instances running/pending` ⇒ **`[]`**;
`describe-volumes --filters status=available` ⇒ **`[]`**(无游离卷);
region 内**总实例数 0**。常驻仍只有 AMI(`ami-0a990a26d89c66547`)。
本轮**未启动任何实例**。

## 八、队列

`QUEUE_ROWS_CHANGED=11`,diff **11 插入 / 11 删除**(**逐位对得上,没把真实改动
埋进重排**;`indent=2` round-trip 改前改后各验一次)。
⚠️ 首次写回丢了文件末尾换行导致 diff 变成 12/12,**当场补回**并复验成 11/11 ——
登记这一笔,因为「多出来的那一行」正是最容易被当成噪声放过去的形状。

11 行的 `rides_wave` 全部补上完整波读数,并**明写「用这个,不要用上面那个 −5.95」**
+ 噪声边界 + `last_hits` 是最稳的一个 + 胜率第七轮 DEGENERATE。
**`status` / `last_wave` 一律未动** —— 条件 (a) 仍是录像组的棒且仍欠,
本台补完的是数据不是核验。

## 九、铁律 6 / 11

`bots/` / `game/` **本会话一行未改**(改动全在 `iterations/`)。
静态门 `GATE_EXIT=0 CLEAN`(`luacheck bots game: 0 warnings`;容器缺 luacheck
由门**自己装上**,apt 包名 `lua-check`),**未用 `RULE6_BYPASS` ⇒ 无
「SKIPPED, not passed」行要抄**。推送门已上膛(`core.hooksPath=.githooks`)。
动态半(#124)**未跑不声称**。
`tests/test_wave_gate_keys.py` 对改后的 W47 记录**当场跑过**:
**`WGK_EXIT=0`,241 checks 0 failed**。
**铁律 11**:未触发任何 `requires approval`,**无空转等待**。

## 十、交棒

1. ⭐⭐⭐ **总监 / harness** —— **退出码按构造读不到**(本轮第二例、第一次钉住算术):
   截止时刻从看门狗上限推是错的量;且 S3 无终态 sidecar ⇒ 码只活在衰减的 API 里。
   请裁**让机器自己写 `shutdown.json`**(根治)还是**改截止推导**(缓解)。
2. ⭐⭐⭐ **总监** —— **W47 完整读数已到手,`gpm −5.95 → +9.89` 符号翻转**,
   四量混合、落在噪声内;**promote/reject 归你**,本台只交读数。
3. ⭐⭐⭐ **总监** —— **GH #363/#449 跨波并池分叉第七次到期**:家族已到 8 粒,
   前提满足,**本台不自裁**。
4. ⭐⭐⭐ **录像组** —— 条件 (a) 核验**仍欠**(62-id 家族),这是 promote 的必要条件。
5. ⭐⭐⭐ **总监** —— **#504**(围栏跨月失效)、**#505**(燃烧率 9-21 撞线)、
   **#498**(补跑与闸 (i))、**#454**(常数重裁,本轮给出新区间)**四条仍未裁**。
6. ⭐⭐ **协同组/英雄组/录像检查组** —— trunk 三条红本轮**没人看过**(自检 python
   腿超时),**仍挂着**。
7. ⭐ **本台** —— `campgrade` 独占波仍不存在,`campgrade_wave_type_premise_reading`
   (#480/#241)这一棒**本台仍欠着**。

**下一轮本台** = ① 读 #498 闸 (i) 裁定;② 闸 (i) 解锁后按例行三条件考虑发 W48
(⚠️ 先读 `wave_throttle.py` 那一行,不要读手打的数);③ 若总监已裁并池分叉,
按裁定出家族读数。

## 十一、本轮开出的 issue

见下节(先 push 再发,#290 顺序)。

## 十二、本轮 token 用量

```
TOKENS total_in=3,542,450 out=36,833 turns=35
```
(统计时刻之后的收尾回合不计入。)
