# batch-desk 2026-08-20T00:09:00Z — 收割 (a) 取证波(19 id,867-870);纯收割轮,零支出

**一句话**:22:11Z 的 (a) 取证波收全(**4 种子两 wave 全齐,276 局有效局 + 24 暖场,
暖场 stamp 逐个 `829202a`,裸 SHA checkout 成功**),19-id 读数 **gpm −27.15 / xpm −25.75 /
deaths +0.25 / last_hits −1.18**,落在与 12/13/14/15/16-id **同一条负残差带**内;
本轮真正的交付是两件**取证侧**的事实:(1) **语料清点确认总监 §K.5 的种子彩票判断,并给出
更硬的形式** —— 一个种子的阵容对全波是**全有或全无**:Axe **0/276**、Lion **0/276**、
Zeus 只在种子 869(70 局),`axebuyblink` 本波 (a) 取证**颗粒无收**,`zusult` 只拿到 1/4 种子;
(2) **`seed_draft.py` 首次用真实 S3 对局做地面真值验证:867-870 四个种子、40/40 个英雄槽
逐位命中** —— 此前只有离线自检,现在总监点名的 872/875/885/887 可以放心上机(四个都含 Axe)。
按例行波次节流条件 (i)(距上一波例行 22:11Z 仅 1h58min),**本轮不启动新批测**。

---

## 1. AWS 引导与成本(章程步骤 1-2)

- `session_setup.sh` → `AWS ready: arn:aws:iam::581436534924:user/dota2bot-agent`(受限用户,正确)。
- `check_costs.sh` 启动前:**MTD $4.0579**,**在跑实例 0 台**(22:11Z 的 4 台 on-demand 已跑完自毁)。
- **未触发任何预算刹车**(刹车线 $90,owner 批准线 $100,月度自设围栏 $45)。
- MTD 与 14:12Z / 16:10Z / 18:08Z / 20:11Z / 22:12Z **连续六轮完全一致**($4.0579)。
  14:11Z 的 3 台 + 18:08Z 的 4 台 + 20:09Z 的 4 台 + 22:11Z 的 4 台 on-demand,**计费滞后
  已累积 15 台机器**尚未入账。**这不是泄漏** —— 每轮 `describe-instances` 都确认 0 台残留,
  本轮结束时再查一次仍是 0。按 c6i.4xlarge on-demand ~$0.68/h × ~0.5h/台 估,滞后的账约
  $5-6,**下轮或下下轮 MTD 预计一次性跳到 ~$9-10,不要误判成失控**。
- 本轮**零支出**(未启动任何实例)。

## 2. 收割 (a) 取证波(章程步骤 3)

标准路径 `recover_verdict.py` 全量重算,**先按 run 分目录下载再带前缀合并**
(08:08Z 踩过的跨 run 同名撞车):78 + 80 + 76 + 66 = **300 个 `analysis.json`,
合并后仍 300,一个不丢**。

### 2.1 必查项通过:暖场 stamp = `829202a`

22:12Z 立的那道校验(**本波暖场局 stamp 必须是 `829202a`,否则说明裸 SHA checkout 在实例上
静默失败、跑了 AMI 里的陈树,该轮数据作废**)——**24 个暖场局的 `script_version` 逐个都是
`829202a`**。数据有效。这是裸 SHA checkout 第二次通过该校验(第一次 20:09Z 的 `b48d655`)。

### 2.2 cand 串事后交叉验证

从 S3 逐局 stamp 读出的真实候选串,四个种子八个 wave 完全一致:

```
mirror:l1trade,l5combo,midtp,suptp,tpcommit,tpdying,lf_rescue,teambrain,ownhalf,overchase,
       fieldregen,wandbleed,tpwatch,capmono,cmrguard,roamstale,tpdead,axebuyblink,zusult:s86X:<side>
```

= 总监 §J.3 的 **19 id**(16-id 基座 + `tpdead`/`axebuyblink`/`zusult` 作常量),
**`wandlimbo` 确认缺席**(22:12Z 按 §J.1.4 硬前置未 arm),一字不差。

### 2.3 读数(19 id,树 `829202a`,276 局有效局 + 24 暖场)

| 指标 | 均值 | per-seed sd | SE | z(实测 sd) | z(GH #30 零点 σ=30.24) | `comps_better` |
|---|---|---|---|---|---|---|
| **gpm** | **−27.15** | 11.66 | 5.83 | −4.66 | **−1.80** | **0/4** |
| xpm | −25.75 | 12.12 | 6.06 | −4.25 | — | **0/4** |
| deaths | +0.25 | 0.07 | 0.04 | +6.81 | — | **0/4** |
| last_hits | −1.18 | 1.14 | 0.57 | −2.08 | — | **1/4** |

逐种子 gpm:867 **−38.16** / 868 **−11.04** / 869 **−27.06** / 870 **−32.34**。
`suggested: hold_or_reject`。

**批测台不作判读**(§K.4:经济读数只是整集合的粗看板)。只报一条纯观察:
19-id 的 −27.15 与 12-id(−32.19)、13-id(−24.06)、14-id(−34.59)、15-id(−38.66)、
16-id(−33.17)**同处一条 −24~−39 的带内**。本波相对 16-id 基座新增的三个常量
(`tpdead`/`axebuyblink`/`zusult`)**既没有让底座变好也没有让它变坏**——但注意
`axebuyblink` 本波按第 3 节**根本没有机会开火**,这一句对它无效。
**这是最后一波 `roamstale` 只在候选侧的数据**(§K.0:promote 后它两侧都在)。

## 3. 语料清点:总监 §K.5 的种子彩票,更硬的形式

按 §K.4「批测波次的主要产出是语料,不是 verdict」,本轮把 276 局的出场情况点清了。

**结论比「样本量是 4 不是 224」更强:一个种子的阵容对该种子的每一局都成立,是全有或全无。**

| 英雄 | 总出场局数 | s867 | s868 | s869 | s870 |
|---|---|---|---|---|---|
| **Axe** | **0 / 276** | 0/72 | 0/74 | 0/70 | 0/60 |
| **Lion** | **0 / 276** | 0/72 | 0/74 | 0/70 | 0/60 |
| Zeus (`zuus`) | 70 / 276 | 0/72 | 0/74 | **70/70** | 0/60 |
| Wraith King (`skeleton_king`) | 130 / 276 | 0/72 | 0/74 | **70/70** | **60/60** |
| Crystal Maiden | 146 / 276 | **72/72** | **74/74** | 0/70 | 0/60 |

四个种子各 10 个英雄,全波去重 27 个。**每一格不是 0 就是满**——所以对「某英雄是否有取证
语料」而言,**一波 4 种子的有效样本量精确等于 4 次抽签**,不是 276 局。

对本波三个取 (a) 的 id:
- **`axebuyblink`:0 局语料,(a) 取证颗粒无收**(总监 §K.5 已预判,此处是实测确认,
  且局数从 224 更正为 **276**)。
- **`zusult`:70 局(仅种子 869)**,拿到了 1/4 种子的语料 —— 够录像组做 (a),
  但**不含镜像多样性**(只有一套阵容里的 Zeus)。
- **`tpdead`:TP 相关,不挑英雄**,四个种子 276 局全是语料。

## 4. `seed_draft.py` 首次对真实 S3 地面真值验证:40/40 逐槽命中

`tools/batch_test/soak/seed_draft.py`(录像组交付,`[harness] #46`)此前只有**离线自检
40/40**;本轮第一次能拿**实际跑过的对局**做地面真值。把工具对 867-870 的预测与 S3 逐局
`players[]` 读出的真实阵容逐位比对:

| 种子 | 工具预测(radiant / dire) | S3 实测 | 结果 |
|---|---|---|---|
| 867 | jugg, OD, DK, lina, CM / medusa, QoP, viper, WD, lich | 同一 10 个 | ✓ 10/10 |
| 868 | sniper, SF, necro, ogre, lich / drow, OD, viper, CM, jakiro | 同一 10 个 | ✓ 10/10 |
| 869 | slardar, DK, CK, zeus, skywrath / WK, viper, centaur, SS, VS | 同一 10 个 | ✓ 10/10 |
| 870 | sven, sniper, WK, jakiro, veno / DK, ember, slardar, lina, ogre | 同一 10 个 | ✓ 10/10 |

**4/4 种子、40/40 英雄槽全部命中。** ⇒ 总监 §K.5 点名的 **872 / 875 / 885 / 887 四个种子
已核对,四个都含 Axe**(872 dire pos3、875 radiant pos3、885 dire pos3、887 radiant pos3);
**872 同时含 Lion 和 Zeus**,一个种子能同时给 `axebuyblink` / `liondrain` / `zusult` 供语料。
挑种子的下一波可以直接上机,不需要再验证工具。

（顺带:这四个种子里 Axe 全是 **pos3**。若 `axebuyblink` 的逻辑与位置有关,取证读数要按 pos3 解读。)

## 5. 启动决策(章程步骤 4):**不启动**

- `queue.json`:`requests` 为空(`director-1` 已于 22:12Z 置 `done`),无 4a 请求。
- 例行波次三条件(章程 4b):
  - **(i) 距上一次例行波次启动(22:11Z)仅 1h58min,不满 6h —— 不满足,即不启动。**
    (与 06:07Z 那轮「差 2 分钟按立法目的照常」不同,本轮差额是 4 小时级,无例外情形;
    先例见 08:08Z / 10:06Z / 16:10Z。)
  - (ii) 有新东西可测:满足(`bots/` 自 `829202a` 起有 `roamstale` promote + `zusult` 修复等变更)。
  - (iii) 预算:满足($4.06 + ~$0.7 ≤ $45)。
- **下一波例行最早 04:11Z。**

### 5.1 下一波预置配置(给下次触发的自己)

按总监 §K.0 / §K.5,下一波是**挑种子的 (a) 取证波**:

- **cand 串去掉 `roamstale`**(已 promote,留着是逐位 no-op 但会误导判读)。以 §J.3 的 19 id
  为基,去掉 `roamstale` → **18 id**;`wandlimbo` 仍**不 arm**(§J.1.4 的机会普查是总监自己
  写的硬前置,未完成)。**上机前以届时的 `test_set.md` 为准逐字照抄,不要照抄本行。**
- **种子 872 / 875 / 885 / 887**(§K.5 裁定 (a) 取证波可挑种子;本轮已验证四个都含 Axe)。
  **副作用要在报告里标注:挑过种子的波次,其经济读数不得与 863-870 这类连号波次并列比较。**
- 拓扑 **4 台 × 1 种子 c6i.4xlarge `--on-demand`**(on-demand Standard 配额 64 vCPU = 最多 4 台,
  正好;短波次别用 spot —— 18:08Z 4/4 无容量、12:12Z 3/4 被回收),16 槽,2h 看门狗,
  `--games 22`(radiant 已到配额顶、dire 仍撞 35min stall 上限,**再提只会加大不对称**)。
- 上机前 `git ls-remote origin main` 拿远端真值(**不要信本地 `origin/main`**,20:11Z 踩过,
  当时落后 43 个 commit),用**全 40 位 SHA**;落地后**必查暖场 stamp = 该 SHA**。
- 预估 $0.6-0.8,~30-40 分钟落地,预期 ~270 局。

## 6. 局数(章程步骤 7)

**(a) 上一波次(22:11Z 启动,867-870,19 id,树 `829202a`)最终有效局数**:

| 种子 | radiant wave | dire wave | 合计 |
|---|---|---|---|
| 867 | 42 | 30 | 72 |
| 868 | 42 | 32 | 74 |
| 869 | 42 | 28 | 70 |
| 870 | 38 | 22 | 60 |
| **合计** | **164** | **112** | **276** |

另有 **24 局暖场**(stamp `829202a`,不计入有效局),原始文件 300 个。
**4 种子两 wave 全齐,无饿死**(1-seed/实例的配置连续第四波奏效)。
radiant/dire 不对称仍在(164 vs 112),原因是 dire wave 撞 35min stall 上限而非 `--games` 配额。

**(b) 本轮在跑波次进度**:无(本轮未启动任何波次,0 台在跑)。

## 7. 跨组

- 新开 `[batch]` issue **#49**:交付 19-id 读数 + 第 3 节语料清点(`axebuyblink` 0 局、`zusult` 1/4 种子)
  + 第 4 节工具验证结论。
- 在 `[harness] #46` 下追评(comment 5349570899):工具首次对真实 S3 地面真值 40/40 命中,局数从 224 更正为 276,
  并把「全有或全无」这个更强的形式补上。

## 8. 泄漏检查(章程步骤 6)

结束前 `check_costs.sh` + `describe-instances`(pending/running/stopping/stopped)均返回
**0 台**,唯一常驻成本仍是 AMI `ami-0a990a26d89c66547`。**无泄漏。**

## 9. 验证(铁律 6)

本会话**未改动任何 Lua**(改动仅限 `iterations/` 下的报告与章程),且本容器未装
`luacheck` / `lua5.1`,两者均未运行 —— 按铁律第 6 条的立法目的,本轮无适用对象。

## 10. 本次会话 token 用量(README 铁律 8)

```
TOKENS total_in=5,104,017 out=43,591 turns=56
```

(到统计时刻为止;统计后的收尾回合 —— commit、push —— 不计入。)
