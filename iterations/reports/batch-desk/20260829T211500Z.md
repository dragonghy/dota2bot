# 批测台 2026-08-29T21:15Z — 收割 W26 + **首份 8 粒种子并池读数**

**工作单元**:AWS 引导 → 成本/围栏 → 收割 W26 → **W25+W26 并池成 8 粒种子出读数**
(上一轮交下来的 ⭐⭐ 第一棒)→ 泄漏检查。**本轮不发波**(闸 (i) 未到,见 §5)。

---

## 一、开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` —— **本轮跑了两次,第一次的读数作废,原因写在下面。**

- **第一次作废**:我把它写成 `timeout 300 bash … | tail -60`,**输出 0 字节**,
  而任务通知报 `exit code 0`。那个 0 是**管道末端 `tail` 的**退出码,不是自检的;
  `timeout 300` 又在自检的 python 腿跑完前就把它砍了。⇒ **一个被超时砍掉的自检,
  在通知里长得和一个干净通过的自检一模一样。**
  这与上一轮记下的 `nohup cmd &` 教训**是同一族**(壳的退出码冒充被测者的退出码),
  换了个形状复发:上一轮的形状是 `&`,本轮的形状是 **管道 + `timeout`**。
  **给全队的收口写法**:后台跑自检就用工具自己的 `run_in_background`,
  **重定向到文件而不是接管道**,并且**只信文件里那行横幅**,不信任务通知的 exit code。
- **第二次(有效)**:`> selfcheck.log 2>&1`,不接管道,不加 `timeout`。读数:
  - `unlanded_commits`:**OK,certifiable window 内无未落地工作**(27 个可核验 ref)。
  - `citation_audit` cadence:**2 findings**,均为报告节奏空洞 ——
    **director `08-29T04:08Z → 16:28Z` 12.3h**(与上一轮报的是同一个洞,**仍未闭合**),
    replay-check `00:55Z → 05:30Z` 4.6h。
  - 未裁队列请求:**RIDESHARE none / OTHER none**,开放请求 40 条。
  - 过期等待:**无**(6 份章程、58 个已结 id)。
  - `stable-v1` / `stable-v2` 锚点:**2/2 OK**(EXISTS / PINNED / SHIPPED 全绿)。
  - trunk 健康(python 套件):**53 passed, 0 failed, 0 uncertifiable** —— **全绿**。
  - trunk 健康(fast Lua detectors):**31 个打标检测器文件,0 失败**
    (工具自己声明这是 FAST SUBSET,不是全套 —— 照抄不加工)。
  - **`selfcheck worst exit: 3`**,`legs run 8`;**exit sources 腿的归因是工具打的不是我手写的**:
    `FINDINGS (exit 3) = cadence`,**`UNCERTIFIABLE (exit 2) = none`**
    ⇒ **exit 3 全部来自节奏空洞,trunk 两侧本轮都被看过了。**
    (这条归因以前手写过、并在 2026-08-27 错了 22 小时,GH #267;本轮照抄工具输出。)
  - **顺带结清上一轮的悬案**:上一轮那次并发自检报 `51 passed, 2 failed`
    (`test_call_form_census.py`、`test_detector_source_constants.py`)并打了 `TRUNK RED`,
    当时归因为"我自己起了两个并发进程,不是 trunk"。**本轮单进程干净跑出 53/53
    ⇒ 那个归因是对的,trunk 从未红过。**

---

## 二、成本与围栏(章程步骤 2)

- `check_costs.sh`:**running/pending 空**;常驻只有 AMI `ami-0a990a26d89c66547` + 快照。
- **MTD $66.19**,来源 `budgets/dota2bot-batch`(免费);读数 ≥ `$COST_CONFIRM_AT`
  自动花 **$0.01** 用 CE 复核:**$66.1902331785**,**逐位一致(连续第二十四轮)**。
- ⚠️ **快照仍是 `2026-08-29T12:39:06Z` —— 与上一轮同一张,九小时没刷新。**
  它在 W25 起飞(12:17Z)之后、W25 自毁(13:00–13:11Z)之前,**更在 W26 整段之前**
  ⇒ 章程 (甲):裸 MTD 不可直接用。**注意 CE 复核也逐位等于 budgets**,
  这不是"两个独立证人对上了",**是两个证人都在同一条滞后曲线上** —— 复核买到的是
  "budgets 没读错",不是"这个数是当前真值"。
- **围栏算术**(逐波按自己的界别取价):

  | 项 | 金额 |
  |---|---|
  | MTD(budgets,CE 复核一致) | $66.19 |
  | W25(12:17Z 起飞,近 12h 内,**spot**) | + $0.80 |
  | W26(18:17Z 起飞,近 12h 内,**spot**) | + $0.80 |
  | **围栏值** | **$67.79** |

- **本轮不发波** ⇒ 围栏值只作登记。$67.79 ≤ **$80** 围栏,余量 **$12.21**;
  刹车 **$90** 未接近,owner 批准线 **$100** 未接近。
  **本轮未跨任何 owner 可见告警档**($50 早已跨,$80 未跨)⇒ 无"本轮跨过 $X"那一行。
- 本轮现金:**CE 复核 $0.01 + S3 取数(可忽略)**,**零 EC2**。

---

## 三、收割(章程步骤 3):W26 干净落地,4/4 计分

四台全部 `instance-terminated-by-user`(**自毁,零抢占**),
`describe-spot-instance-requests` 读到的收官时刻 `19:00:10Z – 19:11:37Z`,
即在 2h 看门狗内跑完了 12 局/腿。**放置:4/4 requested == actual,4 个不同 AZ,
无 re-aiming、无 AZ RING EXHAUSTED、无降级** —— GH **#282 连续第四波零 MISMATCH**。

收割走标准路径 `recover_verdict.py`(逐局全量重算,不用实例自产 verdict):
199 个 per-game 文件,8 个独立目录,`pooled_basename_dirs_overridden: 0`。

| 种子 | ab | ba | `arm_depth` | 计分局 | gpm | xpm | deaths | last_hits | winrate |
|---|---|---|---|---|---|---|---|---|---|
| 1698 | 31 | 14 | 19.29 | 45 | +28.53 | +16.05 | −0.28 | +5.51 | 0.461 |
| 1733 | 32 | 13 | 18.49 | 45 | +11.69 | +13.45 | −0.07 | −0.34 | 0.500 |
| 1743 | 30 | 12 | 17.14 | 42 | +58.60 | +46.65 | −0.65 | +1.95 | 0.533 |
| 1747 | 31 | 12 | 17.30 | 43 | −1.45 | −11.79 | +0.02 | −1.22 | 0.565 |

- **均值:gpm +24.34,xpm +16.09,deaths −0.25,last_hits +1.47,winrate 0.515**
- `comps_better`:gpm **3/4**、xpm 3/4、deaths 3/4、last_hits 2/4、winrate 2/4
- **`min_arm_depth` = 8,`thin_arm_seeds` = []** —— **本波无一粒被 THIN-ARM 排除**
  (最薄的 1743 是 17.14,离门 8 有一倍以上余量)。**无种子需要点名。**
- `scored_games` 175,`unfinished` 0,`winner_by` = `engine_natural` 199/199,
  warmup 未打戳 24 局(与 W25 逐局同数,同构)。
- **未用任何降级旗标**:`--allow-pooled-basenames` / `--min-arm-depth` / `RULE6_BYPASS`
  一个都没用 ⇒ **本报告里没有"这是 SKIP 不是 pass"那一行要抄。**

`iterations/reports/batch-desk/waves/W26_wave.json` 已回填(`status_code`/`update`/
`ab`/`ba`/`arm_depth`/`dem_uploaded` + `placement` + `verdict`)。

---

## 四、⭐⭐ 并池读数:**这个实验室第一次拿到 8 粒种子的单树读数**

前六波做不了这件事,不是因为没想到,是因为**没有两波跑在同一棵可池化的树上**。
W26 是特意为此选的:四粒**从未用过**的种子 + **与 W25 逐字节相同的 arm 串**。

**池化合法性(不靠散文,靠语料自证)**:395 个 per-game 文件的 `script_version` 戳
解出**唯一一个** arm 串,`ids=44 / bytes=387 / sha256[:12]=f0e03819073c`;
两棵树 `b51bac77` 与 `b834e88` 之间碰 `bots/` 的两个 commit 剥掉 Lua 注释后逐字节相同
(W26_wave.json:`tree_note`)。**⇒ 读的是 test_set.md 之外的独立证据。**

| | W25(4 粒) | W26(4 粒) | **并池(8 粒)** |
|---|---|---|---|
| gpm 均值 | +40.16 | +24.34 | **+32.25** |
| xpm 均值 | +25.57 | +16.09 | **+20.83** |
| deaths | −0.30 | −0.25 | **−0.27** |
| last_hits | +1.72 | +1.47 | **+1.60** |
| winrate | 0.560 | 0.515 | **0.537** |
| `comps_better` gpm | 3/4 | 3/4 | **6/8** |
| 计分局 | 172 | 175 | **347** |
| `min_arm_depth` / `thin_arm_seeds` | 8 / [] | 8 / [] | **8 / []** |

### 4.1 并池买到的到底是什么(这才是重点)

**不是那个更小的点估计。** W25 自己登记的诚实边界是:`mean/SE = 0.90`,
且**去掉种子 1633 后从 +40.16 塌到 +4.99** —— 也就是说 **W25 的效应符号压在一粒种子上**。

并池之后:

- `mean/SE`:**0.90 → 1.48**(gpm);xpm 1.43;winrate 距 0.5 亦 1.43 SE。三个量同阶。
- **留一法 8 个读数全部为正**,区间 **+16.05 … +47.35**;
  最坏的那个仍然是 drop-1633,但它现在是 **+16.05 而不是 +4.99**。
- **⇒ 结论符号不再依赖任何单一种子。** 这是第二波真正买到的东西。

留一法全表:drop 1603 → +30.46 | **1633 → +16.05** | 1664 → +30.62 | 1698 → +32.78 |
1733 → +35.19 | 1743 → +28.49 | 1747 → +37.07 | 1770 → +47.35。

### 4.2 铁律 4(i):ab / ba 两层读数(**两层都给,不给就不算数**)

| 量 | ab(armed 在天辉) | ba(armed 在夜魇) | 均值 (ab+ba)/2 | 残差 (ab−ba)/2 |
|---|---|---|---|---|
| gpm | +126.57 | −56.96 | **+34.80** | +91.77 |
| xpm | +103.34 | −59.33 | **+22.00** | +81.34 |
| deaths | −1.35 | +0.74 | **−0.31** | −1.04 |
| last_hits | +11.72 | −7.76 | **+1.98** | +9.74 |
| winrate | 0.109 (n=239) | 0.963 (n=108) | **0.536** | — |

四个经济量**全部两层反号**。按 GH **#299**,这在镜像设计下是**预期签名而不是噪声旗**
(残差本身就是侧偏),且残差**跨 W23/W24/W25/W26 同号同阶**。
⚠️ 一处必须说清的算术:上表的 **+34.80 是按局加权的池化值**,
而 §4 表里的 **+32.25 是种子均值** —— 两者不同是因为各粒种子局数不等,
**登记量是种子均值 +32.25**(铁律 4(iii):效应量连切法一起登记)。

### 4.3 ⚠️ 新发现:**铁律 2(b) 的证人在这份语料里几乎没有量程**

347 局里 **夜魇赢 317 局(91.4%)**、天辉 30 局(8.6%),
而**天辉在两层里都握着更多团队金钱**(armed 在天辉时 +15805,armed 在夜魇时 +7276)。
`winner_by` = **`engine_natural` 347/347** —— 这些**不是** `analyze_log.py` 的
`econ_winner` 改写,是真的推掉了遗迹。

- 于是 winrate 的两层被钉在 **0.109 / 0.963**:swap-and-average 数学上仍然成立,
  但**一个 armed 效应要去推动一个已经贴到 0/1 边界 4–11 个百分点的概率**。
- 池化 winrate **0.537,距中性 1.43 SE**,与 gpm 同阶、同号 ⇒ **它没有和经济读数打架**;
  但它**不是铁律 2(b) 所设想的那个独立证人**,因为它的量程几乎被侧别吃光了。
- **另有一个更粗但独立的证人对得上**:两层的团队金钱差
  **15805 − 7276 = 8529 金/局,方向与 gpm +32.25 一致** ——
  经济读数在 winrate 之外仍有一个二次佐证。
- ⇒ **本条交总监**(§6 第 2 项)。**本台不判读,只报量程。**

并池归档:`iterations/reports/batch-desk/waves/W25W26_pooled.json`。

---

## 五、要不要发新波(章程步骤 4):**不发,卡在闸 (i)**

- (a) `queue.json`:**无任何需要新 EC2 的 pending 请求**。40 条开放请求里,
  pending 的全是归档扫描 / 重导出 / 搭车(`ROUTED_ARCHIVE_SCAN` /
  `ROUTED_ARCHIVE_REDUMP` / `ROUTED_RIDESHARE`),**零 EC2 增量**。
- (b) 例行波三条件:
  - **(i) ✗ 不满足** —— 上一次例行波 **W26 起飞 `2026-08-29T18:17:02Z`**,
    本轮 21:15Z 只过了 **~2.96h**,6h 闸解锁在 **`2026-08-30T00:17:02Z`**。
  - (ii) ✓ 满足(fieldsip 已于 18:54Z 入集,arm 串 44 → 45,是新东西);
  - (iii) ✓ 满足($67.79 + ~$0.8 = $68.59 ≤ $80)。
  **⇒ 三条件未全满足,按章程跳过启动。不满足的是 (i)。**
- (c) 稳定版 vs upstream 基线:harness 缺口照旧,已有对应 issue,本轮不动。

**顺带一条给下一轮本台的排波事实**:W27 的 arm 串将是 **45 id**(fieldsip 入集),
**与 W25/W26 的 44 id 串不同** ⇒ **W27 不能并进本轮这份 8 粒池**。
本轮这份 8 粒读数是 44-id 串上**已经封口**的读数,不要指望 W27 把它加厚。

---

## 六、交棒

1. **⭐⭐ 总监 —— 44-id 全集串的 8 粒种子读数已备齐,可以裁了。**
   gpm **+32.25**(6/8)、xpm +20.83(6/8)、deaths −0.27(6/8)、winrate 0.537;
   `min_arm_depth` 8、`thin_arm_seeds` **[]**;**留一法 8/8 为正**,最坏 +16.05。
   这是本实验室**第一份**单树单串 8 粒读数,也是 44-id 串上**封口的**那一份(见 §5 末)。
   `suggested: promote` **只是提示**,promote 仍走三条件 —— 条件 (a)(帧核验)不在本台。
2. **⭐⭐ 总监 —— 铁律 2(b) 的量程问题(§4.3)**:347 局 91.4% 夜魇赢、
   而天辉两层都更富且 `engine_natural` 347/347。请裁 2(b) 在这种饱和语料里怎么读
   (本轮开 issue,`[harness]` 前缀)。
3. **⭐ 总监 —— GH #308(rec_slots)重裁**仍欠,而本轮把**代价量出来了**:
   W25 在 `rec_slots 12` 上传 **145 个 `.dem`**,W26 按登记路径 (A) 退回 `1` 只上传 **24 个**;
   并池 169 个录像里 **86% 是 W25 的**。**这一波的立案目的正是载体取证**(hero-22/hero-23),
   ⇒ 未裁 #308 在这一波上的实测代价 ≈ **121 个录像**。W27 若仍不裁,继续按 (A) 走 1。
4. **⭐ 录像组/英雄组 —— 载体供给已点清,取帧请从 W25 取。**
   并池语料里 OD 出场 **armed 129 / baseline 133**,WK **armed 111 / baseline 145**
   —— **供给充足,"没有载体"这个退回理由本波不成立**。
   ⚠️ **但 end-level 答不了 hero-23 的前置门**:OD/WK 四组的终局等级 **100% ≥ 13**
   (均值 22.5–24.6),而 `wkqdmg` 订正后的域是**英雄 2–12 级、13 级起逐字节 no-op** ——
   **终局等级不是施法时等级**,这个普查既不能证实也不能证伪那道门,
   它要的是**逐次施法的帧**(`.dem`,不是 `analysis.json`)⇒ 见上一条,**从 W25 取**。
5. **存量**(照抄未闭合项):#207 `zusstatic` 第二十六波 armed;#218 后续第二十二轮;
   #282 **连续第四波零 MISMATCH,第四次建议关闭**;#295 建议关闭;#285 第五轮催;
   #313 选种剪枝;#290/#291/#298/#299 照旧;#217/#211/#225/#180/#171/#200/#181/
   载体门 PARTIAL/`stable-v*` tag/`campdanger`/§BL.4/#75 照旧。
6. **⭐ 总监 —— 自检仍报你 `04:08Z → 16:28Z` 12.3h 无工作单元**(连续第二轮报同一个洞)。

**下一轮本台 = 6h 闸 `2026-08-30T00:17:02Z` 之后发 W27(45-id 串,spot,四个 AZ),
并按 §5 末把 W27 与本轮 8 粒池分开登记。**

---

## 七、铁律 6 两道门

- `bots/` / `game/` **一行未改**(本轮只动 `iterations/` 下的报告/归档/章程/队列)。
- `core.hooksPath` 已上膛;**未用 `RULE6_BYPASS`** ⇒ **无「SKIPPED, not passed」行要抄**。
- 静态半 `bash tools/agent/luacheck_gate.sh`:见下方收尾读数。
- 动态半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)**未跑,也不声称**。
- **铁律 6 顺序条款(GH #290)**:本轮**先 push,再发表带引用的评论**。
- **MCP 未触发 `requires approval`。**

---

## 八、本轮已发表的 GitHub 产物(**push 之后才发,GH #290 顺序条款**)

发表前把草稿存成文件跑了 `bash tools/agent/claim_precheck.sh`:
**`local commits not on origin/main: 0` / `resolved on trunk 1` / `refused 0` / `clean` /
`OK to publish` ⇒ exit 0。**

- **新开 GH #316 `[harness]`** —— §4.3 那条:铁律 2(b) 的胜负证人在饱和语料里没有量程。
  请裁三件事:2(b) 怎么读、要不要照 #269 的形状给 winrate 装"量程门"、
  91.4% 的侧别倾斜本身要不要立案(后者不属本台)。
- **GH #308 追评** —— rec_slots 重裁的**实测代价**:W25 `12` 传 145 个 `.dem`,
  W26 `1` 传 24 个,**≈ 121 个录像**,而 W26 的立案目的正是载体取证;
  并附本轮的载体普查(供给充足,但终局等级 100% ≥13 ⇒ `analysis.json`
  **结构上答不了** `wkqdmg` 的 2–12 级前置门)。**两条棒已指向 W25,没有掉。**
- **GH #282 追评** —— 连续第四波零 PLACEMENT MISMATCH(W23/W24/W25/W26 均 4/4),
  #252 落地以来零错投、#256 的两种行四波未打出过,**第四次建议关闭**。

**MCP 全程未触发 `requires approval`。**

---

## 九、Token 用量(铁律 8)

```
TOKENS total_in=6,702,579 out=50,354 turns=59
```

(到统计时刻为止;统计后的收尾回合不计入。)
