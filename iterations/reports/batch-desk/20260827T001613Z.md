# 批测台 2026-08-27T00:16Z — 收割 W15(**首波 spot 被回收 3/4**)+ 发 W15-R 补跑(**容量阶梯走到第 3 档**)

**一句话**:owner 拍板后的第一波 spot(W15)**四台里三台在开波 20 分钟内被 `instance-terminated-no-capacity` 回收**,
只剩种子 906 完整(gpm **+12.52**,按本台自己登记的「单种子 ±30 = 纯噪声」纪律**不足以下任何结论**);
本轮按章程 §5 预登记的回收处置补发那三粒种子,**`c6i` spot 与 `c6a` spot 双双 `InsufficientInstanceCapacity`,
阶梯走到第 3 档按需**才发出去。AWS 支出:本轮 $0.02(CE 两次)+ 补跑 ~$1.61(按需 3 台)。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` → **worst exit 3**(有发现,非阻断)。

| 腿 | 读数 |
|---|---|
| push gate 上膛 | ok(`core.hooksPath=.githooks`,`arm_push_gate.sh` 报 already set) |
| unlanded work | **OK,可核验窗口内无未落地工作**(certifiable refs 37) |
| cadence | **1 finding**:`strategy` 08-26T10:34Z → 14:23Z **3.8h** 洞。**不是本台的**(本台 21:15Z → 00:16Z = 3.0h) |
| 队列未裁请求 | RIDESHARE none / OTHER none;total open requests 35 |
| `stable-v1` / `stable-v2` 锚点 | 两个全 **ok**(EXISTS / PINNED / SHIPPED) |
| trunk python | **40 passed, 0 failed**(上一轮 39,再上一轮 37 —— **连续四轮在涨**) |
| 快 Lua 检测器 | 9 个 tagged 文件,**0 失败**(**FAST SUBSET,不是全量套件**) |

---

## 1. 成本与预算闸

- `check_costs.sh`:MTD **$50.671**(budgets,刷新 `2026-08-26T23:46:06Z`),
  ≥ $35 触发自动 CE 复核($0.01)→ **$50.6709276212**,**逐位一致(连续第四轮)**。
  forecast $76.999,budget limit $100。
- **开工泄漏检查**:`running/pending` 段**空**;常驻只有 AMI `ami-0a990a26d89c66547` + 快照。

### 1.1 ⭐ 跨 $50 的配套义务 —— **这一轮是真的跨过去了,不是围栏口径跨的**

上一轮(21:15Z)写的那份解释是**围栏值**触到 $50.71 时的预防性说明,当时实际 MTD 还是 $43.46。
**本轮实际 MTD = $50.671,已经越过 Budget 的第一档 ACTUAL 告警线**,owner 会(或已经)收到一封
AWS Budget 邮件。按章程 §4b(iii) 抬高到 $60 时立下的对价,把对得上的账写在这里:

> **8 月 MTD $50.67。跨过 $50 这一档的原因是 08-26 一天内跑了四波批测:
> 03:10Z、09:13Z(W13)、15:14Z(W14)三波按需各约 $2.15,21:14Z(W15)一波 spot。
> 08-26 单日 EC2-Compute 实测 $4.086(CE 逐日,该日行可能尚未结清)。
> 本轮再补发 3 台按需(约 $1.61)完成 W15 缺的三粒种子。
> 刹车线 $90 与 owner 批准线 $100 均未接近;$60 自停线尚有余量。**

- **围栏算术**(章程 §2 甲:`MTD + Σ(近 12h 内已发波次 × 该波的单波价)`):

  | 项 | 值 |
  |---|---|
  | MTD | $50.671 |
  | W14(08-26 15:14Z,**按需**) | +$2.15 |
  | W15(08-26 21:14Z,**spot**) | +$0.80 |
  | **W15-R(本轮补跑,3 台**按需**)** | +$1.61 |
  | **围栏合计** | **$55.23** |

  **≤ 自停线 $60 ⇒ 闸 (iii) 满足。** 注意余量已经只剩 **$4.77**,按需口径**不够再发一波完整波次**
  (~$2.15 尚可,但下一轮的 MTD 会把 08-26 那四波逐步吃进来)。**下一轮开工时闸 (iii) 很可能自己关上。**

---

## 2. 收割 W15 —— ⭐ 首波 spot,**四台里三台被回收**

### 2.1 回收的证据(不是推断)

`describe-spot-instance-requests`(独立于本台自己的 transcript 的证据源):

| 实例 | 种子 | Status.Code | UpdateTime |
|---|---|---|---|
| `i-098339231685bf8ec` (`7f0cc4`) | 888 | **`instance-terminated-no-capacity`** | 21:34:33Z |
| `i-04181a85ecceb8d94` (`552d6b`) | 895 | **`instance-terminated-no-capacity`** | 21:35:36Z |
| `i-04e54ddbb182ffe27` (`4f267b`) | 896 | **`instance-terminated-no-capacity`** | 21:34:33Z |
| `i-0778591d606865f5b` (`ffdcb6`) | 906 | `instance-terminated-by-user`(**自毁,正常收尾**) | 22:11:41Z |

**三台在发波后 19m38s–20m58s 内同时被回收**(21:34:33Z 两台同秒 + 21:35:36Z 一台)——
同秒回收两台说明这是**容量池整体收紧**,不是单机事件。

### 2.2 落盘语料

| run | 种子 | `*.analysis.json` | mirror 局(计分) | 裸 sha 暖场局 | 末次上传 |
|---|---|---|---|---|---|
| `7f0cc4` | 888 | 16 | **radiant 10 / dire 0** | 6 | 21:35:38Z |
| `552d6b` | 895 | 16 | **radiant 10 / dire 0** | 6 | 21:36:55Z |
| `4f267b` | 896 | 16 | **radiant 10 / dire 0** | 6 | 21:35:48Z |
| `ffdcb6` | 906 | 64 | **radiant 38 / dire 21** | 5 | 22:08:11Z |
| **合计** | | **112** | **59 计分(仅 906)+ 30 孤儿单腿** | 23 | |

**三台被回收的种子全部只跑完 radiant 腿 ⇒ 缺臂 ⇒ 按章程 §5 预登记处置,那三粒的配对差作废,
不整波作废。** 30 局孤儿单腿局**留在 S3**(录像组仍可用),但**不进任何配对读数**。

**收割纪律照做**:四个 run **分目录下载 + 按 run 前缀重命名再池化**(GH #225 的坑),
`--include "*.analysis.json" --include "*.demclaim.json"`,池内 196 个文件、112 个 analysis
**逐个对得上 S3 的计数,零静默丢局**。

### 2.3 verdict(`recover_verdict.py`,判据一字未改)

arm 串从落盘语料的 `script_version` 反解出来,**37 id / 328 字节,与 W15 发波记录逐字相同**。

```
python3 tools/batch_test/soak/recover_verdict.py <pool> "<37-id 串>"
```

| 种子 | ab(radiant 腿) | ba(dire 腿) | gpm | xpm | deaths | last_hits | winrate | 计分局 |
|---|---|---|---|---|---|---|---|---|
| 888 | 10 | **0** | — | — | — | — | — | **PARTIAL,排除** |
| 895 | 10 | **0** | — | — | — | — | — | **PARTIAL,排除** |
| 896 | 10 | **0** | — | — | — | — | — | **PARTIAL,排除** |
| **906** | 38 | 21 | **+12.52** | **+4.52** | +0.26 | −1.19 | 0.513 | **59** |
| **mean** | | | **+12.52** | +4.52 | +0.26 | −1.19 | 0.513 | 59 |

`comps_better` gpm 1/1 · xpm 1/1 · deaths 0/1 · last_hits 0/1 · winrate 1/1;
`unfinished 0`;`winner_by` 全 `engine_natural`(112);`suggested = hold_or_reject`。

> **⚠️ 本台对这份读数的正式登记:「1 种子 = 不可读」。**
> 08-19T08:08Z 本台自己立的纪律是 **「单种子 gpm 差 ±30 属于纯噪声,不得据以对任何 id 下结论」**,
> **+12.52 落在那条线以内**。这不是「W15 转正了」——**W15 目前没有可判读的经济读数**,
> 补跑落地前它就是一份 1 种子的噪声样本。

### 2.4 ⭐ 同一粒种子 906 的跨波翻转 —— 给 GH #204 的第五份证据

| 波 | 种子 906 的 `E`(gpm) | 种子 906 的 `D=(ab−ba)/2` |
|---|---|---|
| W13 | — | **+142.29** |
| W14 | **−138.2** | **+138.81** |
| **W15** | **+12.52** | **+121.22** |

- **`D` 稳如常数**(+142 / +139 / **+121**),`E` 在同一粒种子上从 **−138** 翻到 **+12.5**。
  这正是 GH #204 的论点形状:`D` 是**阵容不对称**(逐种子固定),`E` 才是效应,
  而 `E` 的逐种子噪声幅度远大于任何被测效应。
- **机械施铁律 4(i) 于 gpm 的又一个反例**:本波 906 的 `ab = +133.74` / `ba = −108.70`
  **反号 ⇒ 按字面判「噪声」**,而两层平均给出的 `E` 是 +12.52 —— 两层反号在这里
  **只是 `|D| > |E|` 的算术后果**,不是噪声的证据。
- 顺带:三粒被回收种子的单腿 `ab` 分别是 **−368.02 / +75.62 / +46.94**,
  **单腿读数极差 444 gpm**,再次说明单腿数不能当读数用(登记,不判断)。

### 2.5 预登记读法的执行(§4.3 五条,逐条交代)

1. **`zusstatic` = `UNDETERMINED`** —— GH #207 **第五轮**未裁,**连续第四波空转同一条腿**。
2. **`abilanc` = `UNREAD` 不是 `SILENT`** —— acceptance 指定的 `campfarm_target.py` 要**逐帧字段**,
   而 `soak/<run>/` 只有 `*.analysis.json` / `*.demclaim.json` / `*.log.gz`。**GH #211 路由错配的第二个实证受害者**。
3. **按 run 前缀重命名再池化** —— 照做,零丢局(见 §2.2)。
4. **四个经济量不施 4(i) 两层符号检验** —— 照做,两种切法都登记(见 §2.4)。
5. **本波是第一份 spot 语料,额外登记有无回收** —— **有,3/4 被回收**,见 §2.1。**处置照预登记执行**,见 §3。

---

## 3. W15-R 补跑 —— ⭐ 容量阶梯**第一次真的走到第 3 档**

章程 §5 的回收处置是**事先登记**的:「缺的那粒种子再发一台(仍走上面的阶梯)补跑」。
本轮执行它,**并且这是阶梯写进章程以来第一次被真正用到**。

### 3.1 阶梯逐档实测(错误码 + 试过的机型 + 最终市场类型,按章程必须写进报告)

| 档 | 机型 / 市场 | 时刻(UTC) | 结果 |
|---|---|---|---|
| **1** | `c6i.4xlarge` **spot** | 00:13:38 / 00:13:54 / 00:14:12 | **三次全部 `InsufficientInstanceCapacity`**(`reached max retries: 4`,"There is no Spot capacity available that matches your request") |
| **2** | `c6a.4xlarge` **spot** | 00:14:36 / 00:14:55 / 00:15:12 | **三次全部 `InsufficientInstanceCapacity`**(同一错误文本) |
| **3** | `c6i.4xlarge` **按需**(`--on-demand`) | 00:15:41 / 00:15:46 / 00:15:50 | **三台全部发出** |

**⇒ 本轮 us-west-2 的 `c6i`/`c6a` 4xlarge spot 池是干的。** 这与 §2.1 的回收
(21:34Z 同秒回收两台)是**同一件事的两个面**:回收发生在 ~21:35Z,到 ~00:14Z(约 2h40m 后)
**池仍然没恢复**。**owner 的「spot 优先」规则本身没被违反 —— 阶梯就是为这一刻写的。**

### 3.2 落地参数

| 项 | 值 |
|---|---|
| 树 | **`79f32c921aca924ba61b26217c693816afaba754`**(**故意钉 W15 的树,不是当前 remote tip**) |
| 为什么不追 tip | 补跑要与 W15 已落盘的 radiant 腿**同树**才可配对。当前 `git ls-remote origin main` = **`e6978d10`** ≠ W15 树,**追 tip 会让同一粒种子的两条腿跑在两棵树上** |
| armed | **37 id / 328 字节**,从 W15 落盘语料的 `script_version` **反解**得到,与发波记录逐字相同 |
| 种子 | **888 / 895 / 896**(缺臂的那三粒;906 已完整,**不重跑**) |
| 参数 | `--slots 16 --rec-slots 12 --hours 2 --games 12`(与 W15 逐字相同) |
| 实例 | `i-0526d2649285f7bdb` / `i-00149a6ac7fb484f6` / `i-0ceb09a6ef9395f50` |
| **市场类型的证据** | `InstanceLifecycle` **三台全 `None` = 按需**(阶梯第 3 档;**不是** `spot`)。`run_id` 的 `spot_` 前缀按章程**不作证据** |
| LaunchTime | 00:15:43 / 00:15:48 / 00:15:53Z,**三台全 `running`** |
| 唯一性(#98) | `soak-run` 标签 **3 值两两不同**:`…_fc61ad` / `…_21e0a8` / `…_7b6b1f`;发波间隔 5/4 秒,**全部跨整秒** |
| vCPU 配额 | 发波前 `describe-instances` **零台在跑** ⇒ 3 × 16 = 48 ≤ 64,无 `VcpuLimitExceeded` 风险 |
| 预计落地 | ~01:15–02:15Z(2h 看门狗 + 完成即关) |

### 3.3 四道门(发波前,全绿)

| 门 | 命令 | 结果 |
|---|---|---|
| 接线门 | `check_armed_wiring.py --cand "<37串>" --ref 79f32c92…` | **exit 0**,`all 37 armed ids wired on 79f32c92…` |
| smoke 门 | `lua5.1 tests/test_smoke_load.lua` | **exit 0**(**零输出 + exit 0**,不是零输出被读成通过) |
| 静态门(铁律 6) | `bash tools/agent/luacheck_gate.sh` | **exit 0**,`luacheck bots game: 0 warnings`;容器缺 luacheck,**脚本自装 `lua-check`** |
| 载体门 | `seed_roster_index.py --seed 888 895 896` | **exit 0**;`zuus` 3/3、`crystal_maiden` 3/3、`lion`/`obsidian_destroyer` 仅 s896、`skeleton_king` 仅 s895 —— 与 W8–W15 **逐字复现** |
| `--dry-run` 先行 | `ref` / `slots=16` / `rec_slots=12` / `watchdog=2h` / `us-west-2` **全部对上**;波次预算闸(GH #108)**未拒发** |

### 3.4 ⭐ 补跑的**预登记读法**(下一轮收割照做,不许事后改判据)

1. **主读数 = 三个补跑 run 单独池化**(`fc61ad`/`21e0a8`/`7b6b1f`),得到种子 888/895/896 的
   完整配对差;与 906 的 W15 读数**合并成 4 种子**时,**必须注明 906 来自 `ffdcb6`、
   另三粒来自补跑 run**(同树同 arm,但**不同波次时段**)。
2. **不要把 W15 三个被回收 run 的 30 局孤儿 radiant 局并进补跑池** —— 那会让 888/895/896 的
   radiant 腿混入两个时段的语料而 dire 腿只有一个时段,**腿不对称就是新的偏置**。
   要登记合并口径就**两种都登记**(4(iii)),但**主读数取补跑 run 单独池化**。
3. **仍不施 4(i) 于四个经济量**(GH #204,本轮第五次复现)。
4. `zusstatic` **UNDETERMINED**、`abilanc` **UNREAD**(判据同 §2.5)。
5. **本波是按需,不是 spot** ⇒ **不作为 spot 单波成本的实测样本**(见 §4)。

---

## 4. ⚠️ 交棒项 ②(spot 单波成本实测)—— **本轮做不成,而且原因比「账单还没结」更硬**

上一轮交棒要求:「登记第一份 spot 单波成本实测,别让一个没被实测过的数继续当围栏算术的输入」。
**本轮的结论是:W15 根本不能当那个样本,而且以后也不能。**

- **账单侧还没结**:CE 逐日拉到 08-27 —— **08-27 一行都没有**;08-26 行是
  EC2-Compute **$4.0857** / VPC **$0.030122**(= **6.02 个公网 IPv4 机时**)。
  08-26 当天有**四波共 ~15–16 台**,6.02 机时 / 16 台 = **0.377 h/台**,
  与章程钉的 **0.745 h/台** 差一半 ⇒ **该日行明显尚未结清**(该日 UTC 刚过去 16 分钟)。
- **更硬的那条原因**:**W15 有 3/4 的台在 20 分钟就被回收**(§2.1),
  一台跑满 ~57 分钟。**这一波的台·小时天生就是完整波次的一小半** ⇒
  **就算账单结清,W15 也测不出「一个完整 spot 波要多少钱」**,只能测出
  「一个被回收 3/4 的 spot 波要多少钱」。
- **⇒ 交棒项重新指向:章程 §2 的 spot 界后价 `~$0.8/波` 仍是推算,
  等的是第一波「四台全部跑完、无回收」的 spot 波。** 在那之前围栏算术继续用 `~$0.8`
  (它对**被回收的**波是**高估**,朝安全侧失效;对完整波尚未验证)。

---

## 5. 局数(章程 7)

- **(a) 上一波(W15)最终有效局数**:**112 局落盘 / 59 计分 / unfinished 0**。
  ab/ba 逐种子 = **10/0(888)、10/0(895)、10/0(896)、38/21(906)** ⇒ 计分 **38/21**,
  **ba 占比 0.356**。裸 sha 暖场局 **23 局不算有效局**。
  **⚠️ 这一波的 ba 占比不可与 W11–W14 的 0.306–0.325 序列并列** —— 那四波是四台跑满,
  本波只有一台跑满,**分母不是同一种东西**。
- **(b) 本轮在跑波次(W15-R)预期局数**:3 台 × 1 种子 × `--games 12` × 2 腿,
  暖场局不算有效局。按 W11–W14 的实测形状(每台 ~52 局落盘 / ~46 计分),
  预期 **~155 局落盘 / ~138 计分**。

---

## 6. 泄漏检查(两次)

- **开工**:`running/pending` 段**空**;常驻只有 AMI `ami-0a990a26d89c66547` + 快照。
- **收尾**(`--leak-only`,零成本、不查花费):恰 **3 台**,全 `c6i.4xlarge`,
  LaunchTime **00:15:43–00:15:53Z**,Name `dota2bot-soak-od-1` ⇒
  **逐个对得上 §3.2 的发波表,无游离实例**。W15 的四台**全部已消失**(3 台被回收 + 1 台自毁)。
  常驻仍只有 AMI + 快照。三台均 **`--hours 2` 看门狗 + 完成即关**;
  **按需台没有回收风险,但也没有 spot 的一次性请求保护 —— 看门狗是唯一的自毁路径**。

---

## 7. 交棒(下一棒必须交出去,否则接力棒会掉)

1. **本台下一轮 —— 收割 W15-R**。三个 run 前缀:
   `spot_20260827_001541_1_79f32c92…_fc61ad` / `…_001546_…_21e0a8` / `…_001550_…_7b6b1f`。
   **读法按 §3.4 五条预登记,不许事后改**;收割仍**按 run 前缀重命名再池化**(GH #225)。
   **⚠️ 闸 (iii) 提醒**:本轮围栏已到 **$55.23**,自停线 $60 只剩 **$4.77**,
   **下一轮很可能因成本闸自己关上而不能发 W16** —— 那不是故障,是章程在起作用,
   报告里如实写「(iii) 不满足」即可。
2. **本台下一轮 —— 6h 闸的锚点**。本台**保守取补跑末台 `2026-08-27T00:15:53Z` 为锚**
   ⇒ **W16 解锁 `2026-08-27T06:15:53Z`**(不取 W15 末台 21:14:55Z 那个更早的锚 ——
   补跑虽然属于 W15,但**它确实花了钱**,而 (i) 的立法目的写的就是防预算烧穿)。
   **先打钟点 `date -u` 再比**(21:15Z 那轮登记的坑:手写 epoch 常数会**朝提前放行那一侧**失效)。
3. **⭐ 总监 / owner —— 已开 `[batch]` [GH #233](https://github.com/dragonghy/dota2bot/issues/233):spot 优先规则遇到了它的第一个硬边界。**
   owner 08-26 拍板「Spot 优先,没容量才 on-demand」之后的**第一波 spot 就被回收 3/4**,
   ~2h40m 后 `c6i`+`c6a` 两种机型的 spot 池**仍然是干的**。
   **规则本身没坏,阶梯照设计工作了** —— 但两条后果要裁:
   (a) **围栏算术的单波价现在是个随机变量**:同一条「发一波」的动作可能 $0.8(spot 跑满)、
       可能 $0.3(spot 早回收,但**数据也废了**)、可能 $2.15(降级按需)。
       本台目前的保守做法是**按实际市场类型逐波取价**,但**降级发生在发波那一刻**,
       围栏判定却在**发波之前** ⇒ 建议裁一条:**闸 (iii) 一律按按需价预估**(最坏情形),
       跑成 spot 算省下来的。本台在裁定前的保守默认就是这条。
   (b) **回收 + 补跑 = 一波的钱花两次**。W15 实际花掉的是「一波 spot(3/4 作废)+ 3 台按需补跑」。
       若 spot 池持续干,「spot 优先」在**期望成本**上可能反而更贵。建议总监登记这条,
       攒 2–3 波实测后再回看 owner 的拍板是否需要加一条「池干时直接走按需」的旁路。
4. **总监 —— GH #210(第五轮)**:`test_set.md` 第 2 行**仍是 36-id**,而 W14/W15/W15-R
   **三波落盘语料都是 37-id**。机械逐字取第 2 行会**无声地把 `abilanc` 踢出测试集**。
5. **总监 —— GH #207(第五轮)`zusstatic` 三选一**。不裁则**连续第四波空转同一条腿**。
6. **总监 —— GH #204(第五轮)**:本轮给出**同一粒种子 906 跨波 `E` 从 −138 翻到 +12.5、
   而 `D` 稳在 +121~+142** 的直接证据(§2.4),这是「`D` 是阵容不对称、`E` 才是效应」
   最干净的一份复现。
7. **总监 —— GH #219 可结案**(owner 已选「转 spot」);但请连带读第 3 条 (a)(b)。
8. **录像组 —— `abilanc` 的 (a) 取证**:W14 的 156 个 `.dem`(`dem21/`,**约 09-16 过期**);
   **W15 新增语料**:`ffdcb6` 的 48 个 demclaim + 三个被回收 run 各 12 个 = **84 个**,
   其中 30 局是**孤儿单腿局**(不进配对读数,但**帧证据完全可用**)。
9. **总监 —— GH #218(第三轮)** `bbrespawn`/`bbfight` 路由;顺带 `salvepool`/`bbshort` 仍不在测试集里,
   **新 id 入集裁定继续积压**。
10. **存量催办**:#217(第四轮)/ #211(第五轮)/ #225 / #180(第十一轮)/ #171(第九轮)/
    #200(第八轮)/ #181 / 载体门 PARTIAL(第十六轮)/ `stable-v*` tag(第十三轮)/
    `campdanger`(第八轮)/ §BL.4 机械化 / #75(第七轮,`--rec-slots` 维持 12)。

---

## 8. 本轮 AWS 支出

| 项 | 金额 |
|---|---|
| CE 复核(`check_costs.sh` 自动,MTD ≥ $35) | $0.01 |
| CE 逐日逐服务(交棒项 ② 的成本实测尝试) | $0.01 |
| W15-R 补跑(3 台 × `c6i.4xlarge` 按需 × ~0.745h × $0.673/h) | **~$1.51–1.61** |
| **合计** | **~$1.53–1.63** |

spot 发波尝试(6 次)**全部 `InsufficientInstanceCapacity`,零实例创建,零成本**。

---

## 9. Token 用量(铁律 8)

```
TOKENS total_in=5,142,339 out=37,084 turns=46
```

(统计时刻为收尾前;此后的提交/推送回合不计入,量级参考足够。)
