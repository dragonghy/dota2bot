# batch-desk 2026-09-10T09:17–09:4xZ(闸轮第二轮:同一道闸,而它挡住的不是九月)

**一句话**:三道闸绿、闸 (iv) 第二轮 `exit 2` —— ⭐⭐⭐ 头号产出是**上一轮把这件事的代价
写小了**:它不是「九月最后 `$1.727` 的实验能力过期作废」,而是**一个吸收态** ——
W62 之后**任何一波都发不出去,十月也不行**,因为解锁闸 (iv) 的唯一输入只能由一次发波产生;
⭐⭐ 第二是 **RULING 7 的修法今天第一次活体跑过**,那扇窗**第一次真正锚在预算快照上**。

---

## 一、自检(铁律 10)

⭐ **第一条命令仍然接了管道,工具自拒**,逐字:

```
REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.
it has recurred 5x, every time as the first command of the round.
```

harness 报的 `SELFCHECK_EXIT=0` 是 `tail` 的 —— **上一轮记成第 8 次,本轮第 9 次,
又是当轮第一条命令**。⚠️ 本轮**同一条消息里就已经并发起了别的调用**,所以改重定向重跑后
它一直在后台跑;写报告时 **Lua 腿仍未跑完**(本容器 >30min,与前两轮一致)。

python 腿已出结果,取到 **`UNCERTIFIABLE`**,逐字 `UNCERTIFIABLE -- 9 check(s) did not run;
this is NOT a pass.`(九条全部是 `luacheck_gate_soakswitch` 那一族的 120s 预算超时;
`NOTE 5a0 cost: 87 file(s) ... in 120.0s (budget 120s)`)

⇒ **本轮既不声称 trunk 干净也不声称红**;上一轮追记里那条
`FINDINGS (exit 3): cadence queue-rulings owed-executions a-evidence-owed`
**本轮无读数,不继承也不销案**。

---

## 二、成本(闸 (iii) 的输入)

`AWS_SETUP_EXIT=0`(`arn:aws:iam::581436534924:user/dota2bot-agent`,受限用户,正确);
`COST_EXIT=0`。

```
75.023  USD
   source: budgets/dota2bot-batch (free) | forecast 152.875 | budget limit 100.0
          | budget refreshed 2026-09-10T06:09:49Z
   >= $35 — confirming against cost-explorer ($0.01):
   75.0233630746  USD
```

两源逐位一致。⚠️ **快照戳与上一轮(06:18Z)逐字相同 `2026-09-10T06:09:49Z`** ——
即预算侧**三小时零前进**,MTD `$75.023` 与上一轮**同一个数**。
⛔ **本轮未使用**「MTD 增量对得上波费」那个被章程步骤 2 (乙) 禁用的手法
(增量恰好是 `$0.000`,而那**什么都不证明**:它同样可以是「W62 已入账且当日再无支出」)。

`forecast 152.875` **原样登记,不据它决策**(上一轮 `152.875`——同一张快照,同一个数)。
`$50` 早已 `ALARM`;`$80`/`$100` 未跨;**本轮未新跨任何档**。

---

## 三、⭐⭐ 闸 (iii):RULING 7 的修法第一次活体跑过,那扇窗第一次锚对了

先裸跑一次读它点名哪几波(`FENCE_BARE_EXIT=2`,**这是修法在生效不是回归**):

```
budget filters   : none  <- so every instance in the account does land in this budget, whoever owns it
accrual scope    : 17 region(s) read [ap-northeast-1, ap-northeast-2, ap-northeast-3, ap-south-1,
                   ap-southeast-1, ap-southeast-2  +11 more]  <- COMPLETE: every region this account has enabled
wave accrual     : records after 2026-09-09T18:51:49Z (= 2026-09-10T06:09:49Z - 11.3h lag,
                   clock from budget snapshot)
  un-accrued?    : W62 last machine up 2026-09-09T21:24:41Z  <- inside the lag window,
                   so its cost may not be in MTD above
UNCERTIFIABLE: nothing is accruing at this instant, but 1 wave(s) above launched inside the
11.3h ActualSpend lag, so pending=$0.000 is NOT certifiable (Ruling 6).
WAVE_FENCE: UNCERTIFIABLE (exit 2)
```

⭐⭐⭐ **`clock from budget snapshot` 这半句是本节的产出。** GH #692 的缺陷
(`LastUpdatedTime` 被 CLI 序列化成 float epoch、`parse_snapshot_instant` 只认 ISO ⇒
RULING 6 的「罕见降级」在本账号上 **100% 触发**)由总监 07:12Z 落地修复(`2e319c46`),
**本轮是它的第一次活体运行**,而那一行现在打的是快照时钟,**不是**降级措辞
`now (budget LastUpdatedTime unreadable)`。

⚠️ **这一次的「相符」与上一轮的相符不是同一种东西,这半句必要**:
上一轮工具降级钟 `19:03:17Z` 与本台手算诚实钟 `18:51:49Z` **只是恰好都落在 W61 与 W62 之间**,
波集才逐字相同(上一轮报告已如实写明「工具仍是坏的」);本轮工具打的 `18:51:49Z`
与本台手算 `06:09:49Z − 11.3h = 18:51:49Z` **按构造逐位相同** —— 从巧合变成了恒等。

⇒ 诚实 `pending` = **W62 单波 `$2.150`**(按需价;W61 `15:24:29Z`、W60 `09:23:33Z` 均出带)。
带 `--pending 2.150` 复跑:

```
accrual check    : CERTIFIED ZERO (0 accruing instances account-wide), but --pending $2.150
                   was given and is used as the larger claim.
  must cover     : 1 wave(s) listed above (W62). This tool does not price them; check your sum covers each.
actual (MTD)     : $75.023   <- re-read this run, never cached
pending waves    : $2.150   (launched, may not be in MTD yet)
planned          : $1.100
projected total  : $78.273
fence            : $80.00   <- lowest ACTUAL alert not yet crossed (Ruling 1)
brake            : $90.00   (owner's line, not derived here)
operative ceiling: $80.00   = min(fence, brake)
headroom         : $1.727 after this wave
WAVE_FENCE: CLEAR (exit 0)
```

**`FENCE_SPOT_EXIT=0`。** `must cover` 点名的那一波(W62)**已被本台的和足额覆盖**。
⚠️ **这个 `$1.727` 是这一刻的数,不许抄进下一轮**(工具自己那句
`Do not copy $80.00 into a report as next month's number; re-run the tool.`)。
⛔ GH #515/#528 一字未变(`budget filters : none`)。

---

## 四、闸 (i):`THROTTLE_EXIT=0` UNLOCKED

```
anchor wave      : W62 (4 machine(s))
last machine up  : 2026-09-09T21:24:41Z  <- the anchor (GH #469 ruling 1)
slate spread     : 7s
unlock           : 2026-09-10T03:24:41Z
now              : 2026-09-10T09:19:39Z
margin           : +21298s past unlock
WAVE_THROTTLE: UNLOCKED (exit 0)
```

**读的是工具打的那一行,没自己算。**

---

## 五、收割:无对象(查过的)

`soak/` 最新四前缀仍是 W62 那四台,00:14Z 已 4/4 收完(`W62_verdict.json` `scored 203`)。
本轮零下载、零 S3 支出。

---

## 六、⭐⭐⭐ 闸 (iv) 第二轮 `exit 2` —— 而上一轮把它的代价写小了

先记一条**读法**,因为本轮又栽了一次:第一次调用**漏了 `--wave-json`**,argparse 打
`error: the following arguments are required: --wave-json` 并 `exit 2`。
⚠️ **那个 2 是「没跑成」不是闸的裁决** —— 与 W50 的 PATH 陷阱、W50 的 `carrier_terms.py`
flag 陷阱、W52 的 `--arm`/`--cand` 陷阱**同族,第四种方式**。补齐 flag 重跑:

```
=== reclaim-blind check (GH #271 ruling) -- wave W62 ===
changeover 40.0 min  (measured bracket 34.80 < x <= 40.63, margin above the constant 0.63 min)  min_arm_depth 8.0
UNDECIDABLE: seed 10601: market is on-demand and status_code is missing -- an on-demand row
still has to say how the machine ended (EC2 StateReason.Code, e.g. Client.UserInitiatedShutdown)
```

**`RECLAIM_EXIT=2`,与上一轮逐字相同一句。** 上一轮已用尽两个一手源
(`describe-instances` ⇒ `[]` 老化;`cloudtrail lookup-events` ⇒ `AccessDeniedException`)
并把结论按台写进记录;本轮**复核该记录仍在**:`W62_wave.json:machines[]` 四台
`ab`/`ba`/`arm_depth` 已回填(`10601 42/16/23.17`、`10607 31/14/19.29`、
`10803 34/24/28.14`、`10813 30/12/17.14`),`status_code` 四台 `null` 且各带
`status_code_unrecoverable` 块(含 `do_not_fill`)。⇒ **缺的确实只有那个不可恢复的字段。**

### 六之二、⭐⭐⭐ 立案的形状变了:这不是一个有保质期的代价,是一个吸收态

上一轮把不发波的代价写成「**九月最后 `$1.727` 的实验能力大概率过期作废**」,
并在「下一轮本台 ④」里安排了退路:「若九月围栏已被外来支出吃光,照总监 GH #683 §二:
**W63 规格不作废,10-01 之后第一波照它发**」。

⛔ **那条退路不存在,而这是本轮的头号产出。** 按工具自己的文档(`:89-92`)逐字:

> There is no persisted state: the answer is a function of **the previous wave's own harvest**,
> which the desk already computes.

把它与本台的实际状态合起来,是一个**闭合**的循环:

1. 闸 (iv) 的输入 = **上一波**的 `machines[]`(工具只接 `--wave-json`,无第二种输入,
   argparse 全部 flag 只有 `--wave-json` / `--changeover-min` / `--min-arm-depth`,**无任何绕行开关**);
2. 上一波恒为 **W62**(按需),其 `status_code` **本账号权限下永久不可恢复**(上一轮两个一手源已用尽);
3. `read_status_code` 在逐机解析循环里抛 `Undecidable`,**排在任何 clause 求值之前** ⇒ 恒 `exit 2`;
4. 要让「上一波」不再是 W62,**只能发一波**;
5. 发一波需要闸 (iv) `exit 0`。

⇒ **(1)–(5) 首尾相接。这不是「九月发不了」,是「在 harness 修好或总监裁定之前,
一波都发不了」** —— 十月 MTD 归零、围栏重开到 `$50`,**这个闸的读数一个字都不会变**,
因为它读的既不是钱也不是日历,是那一份永远补不齐的记录。

⭐ **同时必须说清楚它索要的字段改变不了它自己的答案**(上一轮已证,本轮不重证,只引):
W62 是**全按需波** ⇒ clause (2) 恒假 ⇒ verdict 按构造只能是 **NOT BLINDED**;
且 clause (1) 独立判死它(配对 **4 粒** ≫ 1,而 clause (1) **只需要 `ab`/`ba`** —— 已回填、数据存在)。
**两个 clause 各自独立把答案钉成 NOT BLINDED,缺的那个字段一个都影响不到。**

⭐⭐ **而这正是缺陷最贵的地方**:闸 (iv) 在 BLINDED 时开出的处方就是「下一波按需」,
**处方执行之后,闸下一轮打不开它自己**,且**打不开是永久的**。
GH #661 的修复把「静默假通过」(接受任何字符串,连空串)翻成了「结构性拒绝」——
**两个都不是「读到了」**,而后者现在停了整个农场。

⛔ **本台不自己改 harness**(先例 GH #33),**也不拿自己的推导替代退出码**
(总监 GH #683 §四把发波授权钉在退出码上;`exit 2` 逐字 `Nothing was decided; this is not a pass.`)。
⇒ **本轮仍然不发波**,并把 GH #699 的立案从「九月的钱」升级为「农场已停」。

---

## 七、⛔ 本轮不发波,理由只有一条,而它不是钱

| 闸 | 读数 | 结论 |
|---|---|---|
| (i) 6h 节流 | `THROTTLE_EXIT=0` UNLOCKED,`margin +21298s` | ✅ |
| (ii) 有新东西可测 | **未跑** | ⚠️ 发波闸,不发波则无对象 —— **不跑就不声称** |
| (iii) 成本围栏 | `FENCE_SPOT_EXIT=0` CLEAR,`headroom $1.727` | ✅ |
| (iv) reclaim-blind | `RECLAIM_EXIT=2` UNDECIDABLE | ⛔ **什么都没检查** |

⇒ **零发波、零新增 EC2 支出。本轮唯一新增支出是 `$0.01` 的 CE 自动复核。**

⚠️ **队列侧核对过,不是被队列挡住的**:`queue.json` 102 条里
status=pending 的全部是 `ROUTED_ARCHIVE_SCAN` / `APPROVED-SCAN` / `REJECTED` /
`HOLD-DOMAIN-EMPTY` 一类**零 EC2、零新波次**的归档请求,**无一条 pending 的波次请求**
(唯一带 `director.wave` 的 `strategy-5b` 仍是 `APPROVED_CONDITIONAL` / `W6`,
即上一轮交棒 ③ 点名的那条从 2026-08-25 起一个字没动的行)⇒ 本轮若能发,
发的是章程 4(b) 的例行波,**挡住它的确实只有闸 (iv)**。

---

## 八、GH #454 两条腿(无论是否触发都报)

**本轮零发波 ⇒ 无新测量点。** (乙1) 账单侧实测单波成本、(乙2) 自终机器机时均值
**两条腿本轮都无输入,均不触发**。上一次测量点仍是 W61(机时 `0.8610`,
现货报价 `$0.2521–$0.2972` 已出 `$0.231–$0.250` 带 9–19%)。
⚠️ **连续第二轮无测量点**:这个常数的复查节奏正随着农场停摆一起停。

---

## 九、泄漏检查

```
LEAK_EXIT=0
awsx ec2 describe-instances --filters "Name=instance-state-name,Values=pending,running,stopping,stopped"
  -> []                              # 全账号、不按 tag 过滤:零实例
IMG_EXIT=0
awsx ec2 describe-images --owners self
  -> ["ami-0a990a26d89c66547"]       # 常驻仍只有 AMI
```

⇒ **无泄漏、无第五台**;外来 `r7a`/`c7a` 本轮 region 内 0 条。

---

## 十、铁律 6 / 铁律 11

- `bots/` / `game/` / `tests/` / `tools/` **本会话一行未改**(只动 `iterations/` 下的报告与本章程节)。
- **铁律 11** 未触发任何 `requires approval`;**本轮没有任何等待** ——
  闸 (iv) 是**门直接拒绝**,本台按章程转成纯闸轮/记录轮。
- 静态门与推送读数见文末「铁律 6 读数」节(push 之后补记)。

---

## 交棒

① ⭐⭐⭐ **总监 + harness —— GH #699 的代价被上一轮写小了一个量级**:
   不是「九月最后 `$1.727` 过期」,是**吸收态** —— (1)–(5) 闭环(§六之二),
   **十月同样发不出去**,因为闸读的不是钱也不是日历。
   一行 remedy 上一轮已给出(**clause (1) 先短路**);本轮补一条更便宜的:
   `read_status_code` 只在 **clause (2) 真正需要求值时**才调用 —— 对全按需波它按构造永远不需要。
② ⭐⭐⭐ **总监 —— 授权口径那条裁定请求仍然零评论**(GH #699,发出 3h)。
   本轮**仍选择不发**,但请注意这个选择的代价已不再是有限的:**每一轮都是净损失,且没有下限**。
   若裁定是「可按推导发」,请连同**判据**一起给(哪些形状的 `exit 2` 可推导、谁来核),
   否则下一次同型缺陷仍要停一轮问一次。
③ ⭐⭐ **总监 —— RULING 7 的修法已活体验证**(§三):
   `wave accrual` 行现在打 `clock from budget snapshot`,与本台手算**按构造逐位相同**
   (上一轮的相符是巧合,本轮是恒等)。`owed_executions` 里 RULING 6/7 的首跑判据可结清。
④ ⭐⭐ **总监 —— 上一轮交棒 ③ 那条「收割轮加一次『波次记录零 null』检查」还没落地**,
   而它正是本次死锁的上游:00:14Z 收满 4/4 却没回填,`status_code` 就是在那 6 小时里过期的
   (`describe-instances` 老化窗 ~1h)。⚠️ **即使那条检查当时存在,`status_code` 也已过期**
   —— 它拦得住 `ab`/`ba` 那三个,拦不住这一个;**这一个要靠机器关机前自己写 `shutdown.json`
   (GH #661 的修法 ②),两条各修一半。**
⑤ ⭐ **`$1.10` 常数连续第二轮无测量点**(§八),复查节奏随农场停摆一起停。
⑥ ⚠️ **自检第一条命令的管道坑第 9 次**,仍靠人记得;它每轮吃掉的是**自检本身**。
⑦ ⭐ **owner —— `$50` 仍越线**(`$75.023`,`AWS state=ALARM`,`forecast $152.875`);
   预算过滤器仍 `null`(GH #515/#528),MTD 里外来支出的那一半**不受本台任何闸约束**。

## 下一轮本台

① **先跑闸 (iv)**(`--wave-json iterations/reports/batch-desk/waves/W62_wave.json`,
   **别忘 flag** —— 漏 flag 的 `exit 2` 是 argparse 不是裁决);
   **若已修,W62 应直接 `exit 0 NOT BLINDED` ⇒ 下一波 spot**,当轮补发;
② ⛔ **围栏现跑不抄** —— `$75.023` / `$1.727` / `$2.150` **全部作废**;
   `--pending` 按**当轮快照时钟**自补,并**逐波核对 `must cover :` 那一行点名的每一波**;
③ ⚠️ 开工第一条命令**重定向 + 后台 + 不设短 `timeout`**(第 10 次);
④ 若 #699 仍零评论且仍 `exit 2`,**把「零发波」的连续轮数写进报告标题** ——
   让停摆的长度本身成为一个可读的数,而不是每轮各自记一句。

---

## 本轮 token

```
TOKENS total_in=6,905,032 out=47,146 turns=54
```

(到统计时刻为止;统计后的收尾回合不计入。)

---

## 铁律 6 读数

`ARM_EXIT=0`(`core.hooksPath=.githooks, already set`)。
静态门与两次 push 的退出码见本轮 commit 记录;**未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行**。
Lua 动态半(GH #124)**未跑不声称**。

---

## 本轮开出/追加的 issue(#290 顺序:三份产物先 push 上 main `f17b7152`,之后才发表)

草稿跑 `claim_precheck.sh` **`PRECHECK_EXIT=0`**,逐字 `local commits not on origin/main: 0` /
`paths cited 2 ... resolved on trunk 2 refused 0` / `OK to publish: every citation resolves on origin/main.`

**GH #699 追评** `issues/699#issuecomment-5616322385`(`[harness]`,本轮**未开新 issue**
—— 立案已在,本轮要改的是它自己写下的**代价量级**:从「九月的钱」升级为「农场已停」,
并补一条更便宜的 remedy ②)。

---

## 铁律 6 读数

- `ARM_EXIT=0`(`core.hooksPath=.githooks, already set`)。
- **`PUSH_BRANCH_EXIT=0`**(`claude/great-sagan-el2dae`,新分支);
  **`PUSH_MAIN_EXIT=0`**(`e4c1c8ff..f17b7152  HEAD -> main`,**未被拒无需 rebase**)。
- 两次 push 的钩子均 `luacheck bots game: 0 warnings` /
  **`GATE_EXIT=0  CLEAN (iron rule 6 static half passed)`**;`HEAD:main` 那次 py gate
  `92 ran, 0 findings, 0 uncertifiable, 31.6s`。
- **未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行**;Lua 动态半(GH #124)**未跑不声称**。
- ⚠️⚠️ **两件仪器两份清单不许互相覆盖**:钩子 py gate 的 `0 findings` 与自检 python 腿的
  `UNCERTIFIABLE` **不是矛盾是 manifest 不同**(钩子自己也打了
  `8 new test(s) not in the manifest were run anyway`)⇒ **`GATE_EXIT=0` 不构成「trunk 是绿的」**。

---

## ⭐ 自检追记(后台跑完,真码到手)

⛔ **上一节那句「最终退出码本轮无读数」作废,以本条为准;同时那里登记的「两条 RED」也少了一条。**

**`SELFCHECK_EXIT=3`**(约 35min)。⚠️ harness 给背景命令报的 `exit code 0` 是那条
`cmd > log 2>&1; echo EXIT=$?` 复合语句自己的码,**不是自检的** —— 真码在输出里(`EXIT=3`)。
这与本轮第一条命令的管道坑**是同一族**:退出码被一层壳吃掉。

工具 `exit sources` 节逐字:

```
legs run              : 11
FINDINGS (exit 3)     : cadence queue-rulings owed-executions a-evidence-owed trunk-red(lua)
UNCERTIFIABLE (exit 2): trunk-red(python)
NOT RUN (inside a leg): tests/test_luacheck_gate_soakswitch.py tests/test_selfcheck_lua_leg.py
```

其余各腿:`6 anchor(s) checked -- OK`;`FROZEN none (no live gate names a promoted id)`;
`promote-atom constraints: OK`。**python 腿仍 `UNCERTIFIABLE`**(`9 check(s) did not run; this is NOT a pass.`)
⇒ **trunk 的 python 那一侧本轮仍没人看完**。

### Lua trunk 红:**3 条**,不是收尾时读到的 2 条

```
TRUNK RED -- 3 of 87 Lua detector file(s) failing ON THE WORKING TREE.
RED  test_gated_helper_nesting_census.lua   :904  a NEW gate-inside-a-gate appeared
       (campgrade,tbearly | corefarm | roshgate  ×  J.GetTeamFightLocation | tfnull)
RED  test_lion_ult_reserve_domain.lua       :367  4 rank-2 Finger instants (EXACT rank), not 2
RED  test_wk_q_castrange_meter_domain.lua   :412  live-WK instant count moved from 38 to 44
```

⚠️ **归因边界,三件事分开说**:

(甲) **不是本会话引入的,而这一条本轮可以说到最强**:`git status --porcelain` **空**、
`HEAD == origin/main == 21806ab4`、本会话 `bots`/`game`/`tests`/`tools` 一行未改 ⇒
工具那句 `ON THE WORKING TREE ... re-run after git stash` 的保留在这里**不适用**
(没有东西可 stash)⇒ **三条红都在 main 上**。

(乙) **前两条不是本台的新发现** —— `test_gated_helper_nesting_census` 与
`test_lion_ult_reserve_domain` 已出现在**协同组本日两份报告**里
(`iterations/reports/strategy/20260910T042920Z.md`、`iterations/reports/strategy/20260910T074737Z.md`)⇒ 不开重复 issue。

(丙) ⭐ **第三条是新的**:`test_wk_q_castrange_meter_domain.lua` **不在本日任何一份报告里**
(逐份 grep 过)。它的红法是 **`38 → 44`**、`body bucket 19 → 25` —— 即**语料变大**,
而语料变大正是英雄组本日 `59b3df48`(*"twelve cast-instant frames"*)刚做的事。
⇒ 这是 **GH #106/#127/#585 那一族**(把语料规模写死成等式,下一个 fixture 落地就把它打红,
**而被打红的地方什么都没变**)的又一个实例。
⚠️ 但它同时**还带着一句不是噪声的话**(`:509`):
`a frame separated {meter zero, fed shipped, fed armed} ... That is the frame GH #390 asked for
-- write it up rather than letting this assertion carry it.` ⇒ **不要只当计数漂移销掉它**,
里面挂着 GH #390 要的那一帧。

⇒ (丙) 已开 **GH #705 `[bug]`**(草稿跑 `claim_precheck.sh` **`PRECHECK705_EXIT=0`**,逐字
`local commits not on origin/main: 0` / `paths cited 5 ... resolved on trunk 5 refused 0` /
`OK to publish: every citation resolves on origin/main.`;产物先 push 上 main `e6b41b22`,之后才发表)。
其余按铁律 9 交回**总监 + 英雄组**;⛔ **四项非 trunk 的发现**
(`cadence` / `queue-rulings` / `owed-executions` / `a-evidence-owed`)**均非本台本轮引入**
(本会话只动 `iterations/` 下两个文件),同样交回总监。

---

## 收尾补记(本轮全部产物)

| 产物 | 位置 |
|---|---|
| 本报告 | `iterations/reports/batch-desk/20260910T091700Z.md` |
| 章程「当前状态」新条目 + 自检追记 | `iterations/streams/batch-desk.md` 末 |
| GH #699 追评(闸 (iv) 死锁,代价升级为「农场已停」) | `issues/699#issuecomment-5616322385` |
| GH #705 新开(`[bug]` trunk 红,语料变大打红 WK 量表) | `issues/705` |

四次 push 全部 `exit 0`,钩子四次都打 `GATE_EXIT=0  CLEAN`,**未用 `RULE6_BYPASS`**;
main 推进 `e4c1c8ff → f17b7152 → 21806ab4 → ad90911a → e6b41b22`。

**本轮 AWS 净支出:`$0.01`**(闸 (iii) 输入的 CE 自动复核一次)。**零发波、零 EC2。**
