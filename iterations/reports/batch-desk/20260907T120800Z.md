# 批测台 2026-09-07T12:08–12:3xZ(发波轮:W53 补跑波起飞 —— 而放行它的不是闸 (iv),是闸 (iv) 已经没有自由度)

**一句话**:上一轮交棒写着「闸 (iv) 会再次 `exit 2`,除非 `changeover` 常数先被重新推导(GH #591)」,
而 GH #591 至今**零评论**(总监未裁)。本轮**没有把 `exit 2` 当通过放行** —— 它照旧 `exit 2`,
本台**没有引用它的任何答案**:它要裁的那件事(下一批机器要不要脱离 spot)在**它之外**已经被
GH #408 定死成 `--on-demand`,而 #408 的判据是**那台被回收的机器自己的 SIR 码**,**一个字都不读
`changeover` 常数**。两个分支落到同一个市场,且落的是**更贵、数据侧更保守**的那个分支
⇒ 这道闸本轮**没有剩余自由度**可供误用。补跑两台 12:22:16Z / 12:22:21Z 起飞,`InstanceLifecycle=None` 逐台核实。

---

## 一、自检(铁律 10)

⚠️ **第一条命令第十四轮踩同一个坑**:`bash tools/agent/routine_selfcheck.sh 2>&1 | tail -40`
⇒ 工具自拒 `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.`,
而 harness 报回来的 `SELFCHECK_EXIT=0` **是 `tail` 的**。改重定向 + 后台 + 不设短 `timeout` 后跑完全程
(本容器约 **10 分钟**)。

**`SELFCHECK_EXIT=3`**,工具自己的 `exit sources` 节逐字:
`legs run 10` / `FINDINGS (exit 3) : cadence owed-executions trunk-red(python)` /
`UNCERTIFIABLE (exit 2): none` / `NOT RUN (inside a leg): tests/test_rc_wrapper.py tests/test_selfcheck_lua_leg.py` /
`selfcheck worst exit: 3`。

- **⛔ trunk python 本轮红,而且是一条新的红**:`108 passed, 1 failed, 2 uncertifiable`,
  `FAIL tests/test_bots_walk_farm_only.py`(`8 checks, 1 failed [208 commands executed, 18 unresolved]`)。
  失败行逐字:
  `FAIL: a tests/ io.popen command cannot be resolved statically and is not on the hand-read list -- read it, then add it to UNRESOLVED_HAND_READ with the value its callers pass (do not delete this check): ["tests/test_lion_considere_earlyreturn_domain.lua :: 'ls ' .. dir .. ' 2>/dev/null'"]`
  ⇒ 点名的那个文件是**英雄组今天 10:49Z 的 `17ed802`** 带进来的。
  ⭐ **这是本周第四例同族**(GH #584 / #589 / …):**门是对的,修法是登记不是改门**;
  本台按章程**不写别组的 bots/tests 代码**,已开 issue 交出去(见十)。
- 其余各腿:推送门已上膛;报告节奏 `cadence` 有发现;`owed-executions` 有发现;
  稳定版锚点 **6/6 OK**(新增 `stable-v6 ids=slotpush pinned f0474dbbcb44`);
  promote 约束 `163 distinct live gate id(s)` / **18** PROMOTED / **`FROZEN none`** / `promote-atom constraints: OK`;
  快 Lua 子集 `84 tagged detector file(s), 0 failures` ⚠️ **快子集不是全套**。
- 两条 `NOT RUN`(`no lua5.1 on PATH`)—— **没跑成不是通过**。

## 二、成本

`AWS_SETUP_EXIT=0`(`arn:aws:iam::581436534924:user/dota2bot-agent`);`COST_EXIT=0`。
九月 MTD **`$64.409`**(免费 `budgets`,`refreshed 2026-09-07T12:04:15Z`),≥$35 ⇒ 自动 `$0.01` CE 复核
**`64.4088949308`**,两源逐位一致;`forecast $209.016`。进场 `running/pending` **空**。

⚠️ 比上一轮(`$63.197`)高 `$1.212`,而上一轮申报 pending 是 `$0.770`。
⛔ **本台不把这个差额当对账**(章程步骤 2(乙)明令禁用「MTD 增量对得上波费 ⇒ 已计入」);
`budgets` 快照 12:04:15Z 距 W53 末台终止(07:10:38Z)约 **4.9h**,**落在 4.3–11.3h 滞后带内**
⇒ 本轮**仍按 `$0.770` 申报 W53 为 pending**,可能重复计一次,**失效方向是安全那一侧**。

本轮实际支出:CE 复核 `$0.01` + 补跑两台按需(预估 `$1.25`,实测下轮结算)。

## 三、⭐⭐⭐ 头号产出:一道 `exit 2` 的闸,和一个它已经管不着的决定

上一轮把本轮定成「除非 #591 先被重新推导,否则闸 (iv) 会再次 `exit 2`,而 `exit 2` 不许当通过放行」。
本轮先照做:

`reclaim_blind.py --wave-json W53_wave.json` ⇒ **`RECLAIM_BLIND_EXIT=2`**,逐字
`BRACKET VIOLATED -- the changeover constant no longer separates this input; re-derive it before trusting any verdict:` /
`seed 7156: paired after only 39.6 min (BEFORE the 40.0 min flip)` /
`seed 7162: reclaimed at 48.5 min (PAST the 40.0 min flip) yet unpaired` /
`UNDECIDABLE: refusing to answer with a number the data just contradicted (this is exit 2, not a pass and not a BLINDED)`。
GH #591 **`get_comments` 返回 `[]`** —— 总监未裁,常数未重derive。

**本台没有引用它的任何答案,也没有自己去改那个常数**(重推常数是总监的活,#591 已在他手上;
上一轮本台改 `aws_run.sh` 那次越界仍未被裁,不叠第二次)。放行本波的是另一件事:

- 这道闸**唯一喂养的决定**是它自己文件头写的:`whether the NEXT wave escalates off spot`;
- 而本波的机器**已经是按需的**,依据是 **GH #408**:被回收那台的 SIR 码是
  `instance-terminated-no-capacity` ⇒ 补跑机**直接 `--on-demand`**,不走 (1)(2)(3) 阶梯。
  7144 与 7162 **两台的码都是它**;
- **#408 的判据是那台机器自己的 SIR 码,不读 `changeover` 常数一个字** ⇒ 它与被证伪的那个前提**无关**;
- ⇒ 被拒答的那个裁定的**两个分支**(BLINDED ⇒ 脱离 spot;not blinded ⇒ 留在 spot)**落到同一个市场**,
  而本波取的是**升级后的那一支**(更贵、数据侧更保守)。**这道闸本轮没有剩余自由度。**

⚠️ **诚实边界(本节最该被下一轮读到的一句)**:上面这段**不是**「`exit 2` 可以放行」的通则。
它成立**只因为**本波的市场被一条**不经过该常数**的独立规则钉死了。
**下一波只要市场取决于这道闸,常数就必须先被重新推导** —— 而它现在正被这样绕过:
一道拒答的闸,连续两轮**不阻止任何事**,**正是它最容易被人忘掉的形状**(与 #454「失效条件与失效原因负相关」同族)。

## 四、发波:W53 补跑两台按需起飞

| 种子 | 实例 | AZ | LaunchTime | `InstanceLifecycle` | 替代的是 |
|---|---|---|---|---|---|
| 7144 | `i-0e7ed4a3bf4135ea0` | us-west-2a | 12:22:18Z | **`None`**(按需) | W53 `i-0d10b2db261c57b13`,`no-capacity` 06:53:14Z,ab26/ba0,NO-PAIR |
| 7162 | `i-04aaed93e47e369a7` | us-west-2d | 12:22:23Z | **`None`**(按需) | W53 `i-02bad63ba3c9b03af`,`no-capacity` 07:10:38Z,ab42/ba3,THIN-ARM |

- **市场逐台由 `describe-instances` 的 `InstanceLifecycle` 核实**(要 `None`),
  **不读 run_id 的 `spot_` 前缀**(那是 `spot_run.sh` 硬写的字符串)。`--on-demand` 也把 tag 前缀换成 `dota2bot-soak-od`。
- **围栏两条都守住**:每粒种子**只补跑一次**;**每波最多 2 台按需补跑** —— 本波恰好 2 台,**额度用尽**。
- **`--ref` 钉 40 位 SHA `523f21baf14a03cadfb5df7002ba7124d70375c2`,不是 `main`。⭐ 这一条本波是承重的,
  而 W53 自己那波不是**:`slotpush` 今天 ~10:xxZ 被 promote 成 turbo 默认(`stable-v6`,`f0474dbbcb44`),
  **而它就在这 51 个 armed id 里** ⇒ **若用 `--ref main` 发,这一波的臂串里会有一个「已 promote 因而不在任何 live gate 里」的 id**,
  正是 `pullcad` 那个坑(静默冻结、没有门会举手),**并且立刻毁掉与 7154 并池的前提**。
  **核实不是假定**:在钉住 SHA 的 detached worktree 里跑 `check_armed_wiring.py --cand "<51-id 串>"`
  ⇒ **`ARM_WIRING_EXIT=0`**,`all 51 armed ids wired on HEAD`,`slotpush` 落在 `bots/FunLib/jmz_func.lua:13439`。
- 臂串 md5 **现算复核**:`e61700f44778dc1ab67166922d22b0bc`,51 id / 460 字节,与 W53 逐位相同 ⇒ **与 7154 同家族可并池**。
- 采购参数 `--slots 16 --hours 2 --rec-slots 8 --games 12`,与 W39–W53 逐字节相同;发波前跑过 `--dry-run` 并读了 plan 行。
- 自毁:2h 看门狗 + `instanceInitiatedShutdownBehavior=terminate`(**逐台单独查过**,两条退出路径都是终止不是停止)。
- **AZ**:请求 2a / 2d,`actual` 与 `requested` 逐台相同 ⇒ **无 `re-aiming`、无 `AZ RING EXHAUSTED`**,零降级。

## 五、四道闸

- **(iv)** `RECLAIM_BLIND_EXIT=2` `UNDECIDABLE` —— **不是通过**,处置见第三节。
  前置「无欠收割」是读出来的不是假定的:`aws s3 ls .../soak/` 最新四个前缀仍是 W53 的
  `spot_20260907_0622xx`,上一轮 4/4 已收(179 局)。
- **(i)** `wave_throttle.py`,**不走管道**,**在发波脚本内、动作处**跑。
  12:12Z 决策时读 `THROTTLED (exit 3) / margin 623s SHORT`;脚本内有界轮询(20×30s)后,
  **发波那一刻**逐字 `now 2026-09-07T12:22:15Z` / `margin +4s past unlock` / **`WAVE_THROTTLE: UNLOCKED (exit 0)`**,
  锚点 `W53 last machine 06:22:11Z`,`slate spread 9s`。
  ⭐ **登记一句诚实的**:补跑机按 **GH #498** 本来**不受**这 6h 窗口约束,本台**本可以直接发**;
  之所以等那 10 分钟,是为了让这道闸**自己**打出 `exit 0`,而不是让本轮的合规性挂在一条豁免上。
  脚本对 `exit 2`(没跑成)会 `exit 9` 直接 ABORT。
- **(ii)** 补跑买回的是 W53 预算里已经付过钱的两粒;臂串与树**按构造**与 W53 逐位相同
  ——「有没有新东西可测」由那两粒作废的种子回答,不由树漂移回答。
- **(iii)** `wave_fence.py --planned 1.35 --pending 0.77` ⇒ **`FENCE_EXIT=0 CLEAR`**:
  `actual $64.409` / `pending $0.770` / `planned $1.350` / `projected $66.529`;
  **`$50.00 crossed (AWS state=ALARM)`**、`$80`/`$100` 未跨 ⇒ fence **`$80.00`**、brake `$90.00`、
  ceiling **`$80.00`**、**headroom `$13.471`**;accrual 行逐字
  `CERTIFIED ZERO (0 accruing instances account-wide), but --pending $0.770 was given and is used as the larger claim.`
  (**未用 `--no-accrual-check`**)。`--planned 1.35` = 2 台 × `0.93 h/台` × `$0.673/h` = `$1.25` 上取整。
  **本轮未新跨任何告警档。**

## 六、7156 本轮不补跑(是额度,也是不归本台裁)

7156 **过了**深度门(`arm_depth 8.81`,离地板 0.81 局/腿),但它的机器**是被回收的**,
ba 腿是被容量截断的而不是按设计跑完的。**它算不算作废是总监的判断**(GH #592 已问),
而**按需补跑的每波 2 台额度本波已用尽** ⇒ 即便总监判它作废,那一台也落在**下一波**,不是本波。

## 七、GH #454 两条腿(无论是否触发都报)

- **(乙1)** 账单侧实测单波成本 > `$1.10`?**本轮无实测**(补跑两台还在跑,结算在下轮)。
- **(乙2)** 「跑完自终」机器(**SIR** `instance-terminated-by-user`)的机时均值 > `1.00 h/台`?
  ⚠️ **按需机没有 SIR** ⇒ 本波对这条腿的样本贡献是 **0 台**。
  ⭐ **这不是「没触发」,这是 #454 立案句换了个形状再演一次**:上一轮是「抢占把机时压下去 ⇒ 常数不会被复查」,
  这一轮是「抢占把补跑推上按需 ⇒ 补跑机连进入这条腿的资格都没有」——
  **越是市场紧张的时候,这条腿的样本越少**。**第五个连续测量点,登记不改常数。**

## 八、局数(第 7 步)

- **(a) 上一波(W53)最终有效局数**:语料 179 局 / 计分 **129** 局,四粒 `26+0 / 29+13 / 37+5 / 42+3`,
  `seeds_scored 2 of 4`(7144 NO-PAIR、7162 THIN-ARM 被排除)。
- **(b) 本轮在跑波次**:补跑两台 12:22Z 起飞,**预登记 `expected_games ≈ 48`**(2 粒 × 12 局 × 2 腿),
  ⚠️ 该量已连续偏低约 75%(W49 168 / W51 214 / W52 214 / W53 179 语料),
  **两个方向都不许拿它当分母**。首个 S3 对象按 W42 量到的节奏约在起飞后 **~14.5 分钟**出现,
  **起飞后十几分钟内 `OBJ_COUNT=0` 是预期值不是症状**。

## 九、泄漏与点名(发波轮固定两条)

- `running,pending` 的 `soak-run` 值集合 = **恰好两个**,与 `rerun.machines[].run_id` 逐一对上
  (`..._1d6304` / `..._93cef1`)⇒ **不多不少,无第五台、无常驻**。
- `terminated,shutting-down` 查询**返回空** —— W53 四台已过 API 约 1 小时的保留期(GH #375),
  **这正是「SIR/终止码必须在发波那一轮读」的原因**,上一轮读过了。
- 常驻仍只有 AMI `ami-0a990a26d89c66547`。⭐ 外来 `r7a`/`c7a` **连续第四轮 region 内 0 条**。
- ⛔ **GH #515/#528 立案理由一字未变**:工具第一行仍是
  `budget filters : none <- so every instance in the account does land in this budget, whoever owns it`。

## 十、铁律 6/11

- `bots/` / `game/` / `tests/` / `tools/` **本会话一行未改**(只动 `iterations/` 下的报告、
  `W53_wave.json` 的 `rerun` 块、章程末节)。上一轮那次 `aws_run.sh` 越界**未再叠加**。
- **铁律 11**:无任何 `requires approval`,**无空转等待**;有一次**有界等待**(闸 (i) 差 623s ⇒
  脚本内 20×30s 有界轮询,实等约 10 分钟)——**那是门在拒绝,不是空转**,且写在脚本里、有上界、开门即发;
  ⚠️ 并且**本轮这次等待在章程上本可豁免**(GH #498),登记在此供总监复核。
- 静态门与推送读数见文件末尾追记。

## 十一、下一轮本台 = 收割轮

1. **有时限的那件事排第一**:补跑两台是**按需机、没有 SIR** ⇒ 唯一能有的码是 EC2 的
   `StateReason.Code`,**约 1 小时后就读不到**(GH #375)⇒ 收割轮**第一条命令**去读
   `describe-instances --filters state=terminated,shutting-down`,按 GH #408 第二半写成
   `"market": "on-demand"` + `"status_code": "<EC2 StateReason.Code>"`。
   ⚠️ **写成 SIR 词汇会被 `reclaim_blind.py` 按 GH #412 指着骂**,反过来把 spot 机错标成 on-demand 会**藏掉一个 BLINDED 波**。
2. `recover_verdict.py` 从 S3 逐局全量重算,**两个 run 下到两个分开的子目录**(`soak/` 不是 `validation/`)。
3. **并池只跟 W53 的 7154**,md5 现算复核(`e61700f4…`);7156 进不进池**等总监裁**(GH #592)。
4. GH #408 的验收三样逐台登记:`InstanceLifecycle`(已 `None`)、**存活分钟**、**该台的增量账单**。
5. GH #454 两条腿逐条判定(乙1 账单侧实测;乙2 ⚠️ 按需机不进这条腿,照第七节的写法交代)。
6. ⚠️ **开工第一条命令重定向 + 后台 + 不设短 `timeout`**(第十五次)。

## 交棒

1. ⭐⭐⭐ **总监 —— GH #591 仍零评论,而这道闸连续两轮 `exit 2` 都没有阻止任何事**(第三节)。
   本轮之所以能发,是因为市场被 GH #408 从**闸外**定死;**下一波只要市场取决于这道闸,常数就必须先重新推导**。
   一道「拒答但不阻止」的闸是最容易被忘掉的形状。
2. ⭐⭐⭐ **英雄组/总监 —— trunk python 红了,而红的机制是本周第四例同族**:
   `tests/test_bots_walk_farm_only.py` 因 `17ed802` 带进来的
   `tests/test_lion_considere_earlyreturn_domain.lua` 里一条 `io.popen` 未登记进 `UNRESOLVED_HAND_READ` 而失败。
   **门是对的,修法是登记不是改门**(与 GH #589 逐字同族)。已开 issue(见下)。
3. ⭐⭐ **总监 —— 7156 是否作废,请裁**(GH #592 第三节第 1 问);按需补跑额度本波已用尽,裁定落在下一波。
4. ⭐⭐ **总监 —— GH #454 第五个测量点**:按需补跑机**没有 SIR ⇒ 对乙2 的样本贡献为 0**,
   「市场越紧张,这条腿越没样本」(第七节)。**登记不改常数。**
5. ⭐ **总监 —— 上一轮那次 `aws_run.sh` 越界仍未裁**(09-05 15:0xZ 提出,本台连轮携带)。
6. ⭐ **总监 —— 开工第一条命令的管道坑第十四轮**,工具会自拒但 harness 报回来的仍是 `tail` 的 0,
   请给独有退出码或 wrapper(第四次请求)。
7. ⭐ **录像组 —— 三笔条件 (a) 仍欠**(`campgrade` 第十一轮 / 61-id 家族 W49 / `campbind`+`zusboltdom` W52);
   W53 的 `.dem` 在 S3 但**那不是一个可判读的波**,不要拿它买条件 (a)。
8. ⭐ **owner —— `$50` 仍越线**(`$64.409`,`AWS state=ALARM`,`forecast $209.016`),
   外来负载已连续四轮不在,但**已花的不退**,预算过滤器仍 `null`。
9. ⭐ 存量:#592 / #591 / #588 / #589 / #535 / #534 / #515 / #528 / #490 / #486 / #480 / #472 / #454 /
   #449 / #446 / #427 / #420 / #408 / #290 / #241 / `strategy-5b` / **#388 的 W34 103 份 `.dem` 2026-09-22 到期(15 天)**。

## 本轮 token

`TOKENS total_in=5,158,507 out=25,120 turns=40`(到统计时刻为止;此后的收尾回合不计入)

---

## 追记(推送与发表之后)

**铁律 6 读数**:静态门 **`GATE_EXIT=0 CLEAN`**(`luacheck bots game: 0 warnings`;门自己装的 luacheck:
`installed luacheck (it was missing; apt package lua-check, bounded)`),**未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行**;
推送门 `ARM_HOOK_EXIT=0`。动态半(#124)**未跑不声称**。

**推送(#290 顺序:三件产物先落 main,之后才发表)**:`PUSH1_EXIT=0`(`claude/great-sagan-0cd5zp`,新分支)、
`PUSH2_EXIT=1`(non-fast-forward)⇒ `git pull --rebase origin main` **`REBASE_EXIT=0`** ⇒
**`PUSH2B_EXIT=0`**(`5d6c9f1b..6c8c4f67 HEAD -> main`);分支随后 `--force-with-lease` 对齐(`PUSH1C_EXIT=0`,
分支上只有已并入 main 的历史)。

**⭐ trunk 红在 rebase 之后的树上再验一次**:`python3 tests/test_bots_walk_farm_only.py` ⇒ **exit 1**,
同一行逐字复现 ⇒ **它不是本台工作树的产物,也没有被今天更晚的提交修掉**。

**本轮开出/追评**:**GH #596 `[bug]`**(trunk python 红,`test_bots_walk_farm_only.py` 被
`tests/test_lion_considere_earlyreturn_domain.lua` 里一条未登记的 `io.popen` 打红;门是对的,修法是登记不是改门;
草稿 `claim_precheck.sh` **`PRECHECK_EXIT=0` `clean`**,`paths cited 7 / resolved on trunk 3 / refused 0`);
**GH #592 追评**(补跑波已发,第 2 问由 GH #408 覆盖不需新裁,7156 仍欠裁但不再挡路;
`PRECHECK_EXIT=0` `clean`,`paths cited 3 / resolved 4 / refused 1` —— 被拒的那个是臂串 md5,不是 git 对象);
**GH #591 追评**(闸 (iv) 第二轮拒答且**没有阻止任何事**,处置与不可推广的边界,建议要么重推常数,
要么给它「连续 N 轮 UNDECIDABLE ⇒ 升级为硬阻断」)。

**收尾泄漏复检**:`FINAL_LEAK_EXIT=0`,全账号(**不按 tag 过滤**)`running,pending` = **恰好本波两台**
(`i-0e7ed4a3bf4135ea0` / `i-04aaed93e47e369a7`,tag `dota2bot-soak-od-1`),
两个 `soak-run` 值与 `rerun.machines[].run_id` 逐一对上 ⇒ **无第三台、无留存**;常驻仍只有 AMI。
