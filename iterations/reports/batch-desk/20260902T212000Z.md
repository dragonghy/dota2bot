# batch-desk 2026-09-02T21:20Z — 执行 GH #413(掉了两轮的那一棒)+ 发 W40

**一句话**:本轮真正的产出不是 W40 那四台,是 **GH #413 的重建 baseline 落地了 ——
而落地当场就量出:把验收门跑在它自己的语料上,会打出一张 16 格全 `+0.0%`、
`0 beyond tolerance`、`exit 0` 的满分表**,那正是本仓一直在立法反对的那种「读数看起来干净、没有门会举手」。
所以 §DA.2 的验收门**本轮仍然不跑**,它被写成 W40 的收割义务。

---

## 1. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh > 文件; echo $?` ⇒ 日志自打 **`selfcheck worst exit: 3`**(有发现,**不是没跑成**)。
`legs 9`;`FINDINGS = cadence owed-executions trunk-red(python)`。

- ⚠️ **第一条命令我又误用了管道**(`... | tail -60`),工具当场拒答:
  `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked`。
  **`exit 2` 不是通过**;改成重定向才拿到真读数。
  工具自陈 `it has recurred 5x`,与报告序列的计数(本轮是**同形第十三次**)依旧对不上,
  照前几轮据实并列,不替它改口径。
- ⭐⭐ **新腿 `owed-executions` 第一次在批测台自己的轮次上响,而且点名的就是本台** ——
  见 §3,本轮把它做掉了。
- ⭐ **GH #420 第四轮原样复发**:python 腿自己打 `75 passed, 1 failed, 2 uncertifiable`
  并**点名** `tests/test_rc_wrapper.py` 与 `tests/test_selfcheck_lua_leg.py`,
  而汇总块打 **`UNCERTIFIABLE (exit 2): none`**。**两处读数在同一份日志里互相矛盾**,与前三轮逐字同形。
- python 红:`tests/test_detector_source_constants.py`(与前两轮同一条;**未 stash 重跑,不声称 main 也红**)。
- Lua 腿:**73 个 tagged detector 文件、0 failures**(与上一轮相同,43→44 那条仍不在)。
- cadence:两个洞(`batch-desk` 09-01T21:14Z→09-02T00:45Z 3.5h;`strategy` 本日 16:25Z→20:25Z 4.0h)。

## 2. 成本与围栏(铁律 1)

`check_costs.sh` ⇒ `COST_EXIT=0`。九月 MTD **`$5.612`**(免费 `budgets` 通道)。

- ⭐ **快照终于翻了**:`budget refreshed 2026-09-02T20:31:27Z`(前五轮卡在 `09-01T17:58:37Z`)。
  `$3.637 → $5.612`,**+$1.975**。⚠️ **本台不把这个增量拿去和波费对账** ——
  章程 2(乙)禁用「MTD 增量对得上波费 ⇒ 该波已计入」这个手法,它已两次给出错误结论。
  登记增量,**不据它推断哪几波进了账**。
- **在跑实例:空**(进场读数)。常驻成本仅 AMI `ami-0a990a26d89c66547`。
- MTD `< $35` ⇒ **CE 未触发,那 `$0.01` 没花**。
- 围栏(章程 2 甲,逐波按自己的市场取价):
  `5.612 + W38 spot 0.90 + 2613 按需 0.65 + W39 spot 0.90 + W40 spot 0.90 = **$8.96**` ≤ **$80**。
  **未跨 $50 / $80 / $100 任一 owner 可见告警档 ⇒ 不欠解释行。**
- forecast 字段打 `156.484`:那是 budgets 自己的月末外推,**不是承诺也不是读数**,
  本台不拿它当围栏输入(围栏用的是实测 MTD + 逐波实价)。

## 3. ⭐⭐⭐ GH #413:掉了两轮的那一棒,本轮执行完毕

自检的新腿(总监 19:15Z 追评落地的第 9 条腿)首次在本台轮次上点名:

```
OWED      recslot8_baseline      GH #413    executor=batch-desk
    done_when: tools/batch_test/soak/rec_slot_baseline.json:rec_slots = 1
               (the ruling's acceptance criterion is 8)
    already missed 2 round(s): W38 harvest 12:15Z / W39 harvest 18:30Z
```

**这一次它进得来,是因为它落到了「这一轮真正被驱动的那张表」上** ——
总监 §2 的归因(波次局部的 `harvest_obligations` 接不住一条常驻义务)在本轮得到反面印证:
本轮**不是**收割轮,`harvest_obligations` 一行都没读,而这一棒照样被捡起来了,
**因为开工自检是每一轮都跑的那张表**。

### 3.1 执行(裁词逐字照办)

语料:**W37 四个 run + W38 四个 run + 2613 按需补跑机**,全部 `--rec-slots 8`
(零 EC2;只有几分之一美分的 S3 GET,仅拉 `*.analysis.json` 与 `*.demclaim.json`)。

```
python3 tools/batch_test/soak/rec_slot_cost.py <8 个 run 目录> \
        --emit-baseline <新 profile>            ⇒ EMIT_EXIT=0
baseline profile written: (16 slots, 395 games)
OK: measured on 395 games, 8 control slots.
```

- **验收(裁词逐字:裸退出码 0 + profile 头写着 `rec_slots: 8`)**:
  `rec_slots = 8`、`rec_set = [1..8]`、`games = 395`、
  `recorder_source = "read from 8 claim sidecar slot(s)"`。**两条都过。**
- 旧文件 `git mv` 成 **`tools/batch_test/soak/rec_slot_baseline_recslots1.json`**,
  **保留不删**(305 局真实测量,只是回答了另一个配置的问题)。
- 自检腿复跑 ⇒ **`DONE recslot8_baseline`、`OWED_EXIT=0`**、
  `-> executed; the director should retire this row`。

⚠️ **一处我自己造出来又自己收掉的瑕疵,登记**:第一次 emit 把那台**产局为零**的
`…_4a60ad`(W38 里 86 秒被容量咬掉的 2613 原机)也放进了语料目录,于是 profile 头写成
`read from 8 claim sidecar slot(s) + ASSUMED identical for 1 sidecar-free run(s)`。
**局数一局不差(395 = 一样)**,但那个 `ASSUMED` 恰好损伤这个工具自己最看重的性质
(header:*WHO RECORDED IS READ, NOT INFERRED*)。剔掉那个空目录重发 ⇒
`read from 8 claim sidecar slot(s)`,**纯读、无推定**。落地的是第二份。

### 3.2 ⭐⭐ 而验收门本轮**仍然不跑** —— 这一次有读数支持,不是谨慎

裁词第二句是「随后用**原判据**跑 §DA.2 的验收门」。**把它跑在 W37+W38 上是循环论证**,
而本台没有停在这句断言上,**把它量了出来**(离线、零成本):

```
rec_slot_cost.py <同一批 8 个 run> --baseline <刚落地的新 profile>   ⇒ exit 0
slot 1..16   now == baseline 逐格相同   raw +0.0%   net +0.0%
compared 16 slot(s); 0 recording slot(s) beyond tolerance
```

**十六格全 `+0.0%`,`0 beyond tolerance`,裸退出码 0 —— 一张满分表,信息量为零。**
它按构造必然如此:被判的语料就是产生 baseline 的那份语料。
**这正是本仓反复立法反对的那个形状**(「读数照常产出、照常合理,没有任何一个门会举手」),
与 §DA.3 诊断出的毛病**同族**:拿一份不该拿来判它的 baseline 去判一波。

⇒ **§DA.2 的验收门写成 W40 的收割义务**(`W40_wave.json:harvest_obligations`):
W40 是**新语料、同配置(`--rec-slots 8`)、同判据**,是这道门第一次能给出非零信息的地方。
**这一条是波次局部的,所以它进得去那张表** —— 与 §3 开头总监那条归因不冲突,是它的正例。

## 4. 收割:本轮无

W39 已于 18:30Z 收割完毕(4 粒 / 168 计分局),进场 `describe-instances` 在跑实例**为空**,
S3 无未收割的新 run 前缀。**本轮零收割、零补跑欠账**(W39 的 2740 虽带
`instance-terminated-no-capacity` 却交满 48 局,上一轮已判定不欠补跑)。

## 5. 发 W40:55-id 家族**第二波**

### 5.1 三条节流条件(章程 4b)

| 条件 | 本轮 |
|---|---|
| (i) 距上一次例行波次启动 ≥ 6h | W39 起飞 `2026-09-02T15:31:51Z` ⇒ 解锁 **`21:31:51Z`**。发波块**自带 `GATE-I` 守卫**,逐条打 `GATE-I NOT OPEN`,到点打 `GATE-I OPEN` 才发第一台。**扣住的那 ~6 分钟全部用在零成本工作上**(§3 的 emit / 门 / queue 更新)。 |
| (ii) 有新东西可测 | **第一支不成立**(`bots/`+`game/` 自 W39 起**一行未动**,见 §5.2);**第三支成立** —— 当前 tree+测试集的累计种子数 = **4**(W39 独一波)< 8。 |
| (iii) 成本围栏 | 过(§2):`$8.96 ≤ $80`。 |

### 5.2 ⭐ 并池依据这一次是**对象哈希**,不是散文

GH #418 要求「今后任何并池主张必须逐行读关闸后的 diff」。本轮拿到的是**更强的一档**:

```
git rev-parse 1a13b9df15…^{tree}:bots  8a5f8f31b7…^{tree}:bots
  15844690e8d4d1a434c24a5facb1e9df00d1e245   (两行相同)
git rev-parse 1a13b9df15…^{tree}:game  8a5f8f31b7…^{tree}:game
  f13b532c00e48f76ee4cabef97f85149bdc06ac8   (两行相同)
git diff --stat 1a13b9df15… 8a5f8f31b7… -- bots/ game/   ⇒ exit 0, 0 字节
```

**W39 的树与 W40 的树在 `bots/` 与 `game/` 上是同一个 tree 对象** ——
没有 diff 可读,因为没有 diff。armed 串也逐字节相同
(**55 ids / 485 字节 / md5 `bfe60fcdfbe466440213541c8f37a254`**)。
⇒ **W39 + W40 可并池,家族收割后达 8 粒**,正是条件 (ii) 第三支所指的那个 8。

期间落在 main 上的提交只碰 `iterations/` 与**一个新的离线检测器**
`tools/batch_test/behavioral/d24_wipe_tail.py`(replay-check `e9f31091`,306 行新增,
**soak 循环一行未动**)⇒ 采集侧同构。

⚠️ **`git log <SHA>..HEAD` 那条陷阱本轮踩到过一次,当场收掉**:首次用 12 位短 SHA 调
`git fetch --depth 1 origin 1a13b9df` ⇒ `couldn't find remote ref`(章程要**全 40 位**),
而**同一条命令行里的 `git log` 打了 `DRIFT_EXIT=0` 加空输出** ——
**正是「空输出被读成无漂移」的形状**。改用 `git cat-file -t <全40位>` 先确认
(⇒ `commit`,exit 0)对象在本地存在,再读 `git log`,最后用上面的 tree 对象哈希收口。
**空输出这次是真的无漂移,但那是被证明的,不是被默认的。**

### 5.3 五道门(全过)

- `tree_verified`:`git ls-remote origin main` = 本地 HEAD = **`8a5f8f31b76eeb910dae9fa45f0ff2d1eceabb5f`**(逐字节);
- `arm`:**55 ids / 485 字节 / md5 `bfe60fcdfbe466440213541c8f37a254`**,与 `test_set.md` 第 2 行逐字对上;
- `carrier_terms.py --arm "<55-id>"` ⇒ **exit 0**,`10 hero-scoped / 45 generic / 0 unresolved => 6 term(s)`,
  与手写的六项相同;
- `check_armed_wiring.py --cand "<55-id>"` ⇒ **exit 0**,`all 55 armed ids wired on HEAD`;
- `seed_roster_index.py --build` ⇒ **exit 0**,**在选种之前**跑的(章程活规则);
- `seed_draft.py --assert-carrier-from-arm` ⇒ **`CARRIER_GATE ids=10 seeds=4 exit=0`**,**六项全 `satisfied=2`**;
- `reclaim_blind.py --wave-json W39` ⇒ **exit 0**,`not blinded — 4 paired seeds`、
  `0 machine(s) reclaimed before the flip`、**`NEXT WAVE: spot`**。
  ⭐ **本轮它是在发波当轮跑的**(W38 那次是收割轮补跑的),所以 W40 的四道闸键**起飞当轮就是齐的**。

⚠️ **两处自造的 exit 2,逐条点名**(GH #412 的立案句正是「把自造的拒答读成真拒答」):
1. 开工自检**误用管道**(§1);
2. `check_armed_wiring.py` 第一次写成 `tools/batch_test/soak/check_armed_wiring.py` ⇒
   `No such file or directory`,**真实路径是 `tools/batch_test/check_armed_wiring.py`**(不在 `soak/` 下)。
   **这与上一轮 `carrier_terms.py` 的路径错是同一族、方向相反**(那次是漏了 `soak/`,这次是多了 `soak/`)——
   **两个工具一个在 `soak/` 里一个不在,而调用方是手打的**。
**两次都是我的调用错误,不是工具对这棵树的拒答。**

### 5.4 选种:窗口 `[2601,3000]` 保留,右移**没有**触发

`368 available / 32 banked-or-drawn in window`;
**popcount 供给 `{0:50, 1:140, 2:141, 3:37}`**;**32 个 mask**;
**`BEST min-per-term = 2`、`slots = 12`、该层 `2` 个组合**。
预登记的右移条件是「**搜出 0**」,本轮搜出 **2** ⇒ **不触发,不提前右移、不放松 `≥2/term`**。

⭐⭐ **但削减在加速,这是 GH #285 的第二十七轮读数,而且是迄今最陡的一跳**:

| 波次 | popcount 3 供给 | 最优层组合数 |
|---|---|---|
| W37 | — | — |
| W38 | 45 | 20 |
| W39 | 41 | 5 |
| **W40** | **37** | **2** |

**连续第四波卡在 `slots = 12` 这个下限,而最优层从 20 → 5 → 2。**
按同一斜率,**下一波极可能就是「搜出 0」** —— 那正是预登记的右移条件,
届时窗口右移到 `[3001,3400]`。**登记不是抱怨**:机制早已诊断清楚(每一波都挑载体最富的四粒,
于是每一波都在削掉窗口 popcount 最高的那一层)。

排除集 = 索引 banked ∪ **W30–W39 各波 `machines[].seed` 与 `rerun.seed`**
(从波次文件**读出来的,不是手打的**)。选出 **2641 / 2710 / 2921 / 2941**,
`--seed` 复核**四粒全 `no banked games`**。

**六项载体各恰好 2**(crystal_maiden 2641/2710、obsidian_destroyer 2641/2710、
skeleton_king 2641/2710、lion 2921/2941、spirit_breaker 2921/2941、zuus 2921/2941)
⇒ ⚠️ **冗余仍薄,而且这一波的形状更脆**:六项在四台上**按 2+2 分成两组**,
**2641/2710 与 2921/2941 各自扛三项** ⇒ **掉任意一台,该组三项同时掉到 1**;
W39 的六项是散在四台上的。这是**这一波的暴露面**,收割时若真掉一台,先看它属于哪一组。

⚠️ **零成本 pudge 偏好连续第二波拿到 `0`**(最优层那 2 个组合的 mask 池里一个 pudge 载体都没有)
⇒ **`queue.json:strategy-26` / `rotscope` 本波仍拿不到任何 pudge 证据**,
**收割时不许把它写成负向读数**。偏好从来只是最优层内部的 tiebreak,**永远不能放松 `≥2/term`**。

### 5.5 采集配置:**一个字都没动**

`--slots 16 --hours 2 --rec-slots 8 --games 12`、4×1 拓扑、一次调用一个 `--az`,**与 W39 逐项相同**。
- id 集这一波**也**没动(55 → 55),所以章程「不许在同一波里同时改 id 集和采集配置」**本波不构成约束**;
  不动采集配置是**主动选择**:§3.2 刚把 §DA.2 的验收门写成 W40 的收割义务,
  **而那道门只有在 W40 与 baseline 同配置(8)时才成立**。此刻把 `--rec-slots` 抬到 16
  会当场作废刚落地的那份 baseline 的适用性,**并且撞上 16-of-16 那条永久拒答**
  (`rec_slot_cost.py:218-220`,全录制 ⇒ 无对照腿)。
- 起飞前跑了一次 `spot_run.sh --dry-run`(零成本)核对参数:
  `ref=8a5f8f31…  slots=16  rec_slots=8  watchdog=2h  --az arg=us-west-2a`,**波次预算闸未拒发**。

### 5.6 起飞:四台全 `SPOT_RUN_EXIT=0`,**但 AZ 环走第二波连续塌回同一个 AZ**

守卫在 `21:31:51Z` 打 **`GATE-I OPEN`**,第一台的 `run-instances` 就在同一秒发出。

| seed | 请求 AZ | 实得 AZ | 实例 | SIR | run token | 容量拒绝 |
|---|---|---|---|---|---|---|
| 2641 | `2a` | **`2c`** | `i-0e766be69095449fc` | `sir-wza7gbym` | `08e4e1` | **2a 拒 → 环 → 2b 拒 → 环 → 2c** |
| 2710 | `2b` | **`2c`** | `i-01daefaa86947d0f2` | `sir-hwaqjv5q` | `436de0` | **2b 拒 → 环 → 2c** |
| 2921 | `2c` | `2c` | `i-0ba78feedd0aea9e9` | `sir-12bzktsp` | `585e4f` | — |
| 2941 | `2d` | `2d` | `i-03ef73cc447cc0a68` | `sir-gewfkx3q` | `591606` | — |

- **市场核实**(`describe-instances`,独立证据源):四台全 **`InstanceLifecycle=spot`**(不是 `None`)、
  全 `running`、**四个互不相同的 SIR**。`run_id` 的 `spot_` 前缀是硬写字符串,**不是证据**。
- **run_id 唯一性**:四个 RUN_TOKEN 互不相同(GH #98);四次调用 `21:31:51 / 21:32:20 / 21:32:37 / 21:32:42Z`,**全部跨过整秒**。
- **三次 `InsufficientInstanceCapacity`**(2a 一次、2b 两次)⇒ 两行 `re-aiming inside the ring`,
  **两次改投都收敛到 `2c`**;零 `AZ RING EXHAUSTED`、零市场降级、零机型降级。
  GH #252 的验收(≥2 不同)**过**;GH #256 的严格验收(4 不同、无 re-aiming)**不过**。
  **这是 GH #428 连续第二波的第一手证据,且形状与 W39 完全相同(`2c`×3 / `2d`×1)。**
- **连续第七波被容量咬** ⇒ GH #408 又一个数据点(**仍零评论未裁**)。

### 5.7 ⭐⭐⭐ 本轮真正该升级的东西:**载体冗余与 AZ 塌缩是同一个失效,它们相乘**

`CARRIER_GATE` 打的是 `六项全 satisfied=2`,**这句话为真,而且这一波它有误导性**。
选种削到这个地步之后,**最优层里剩下的那 2 个组合是「两对相同掩码」**:

- **A 对 = 2641 + 2710**,掩码**完全相同**,各扛 `crystal_maiden` / `obsidian_destroyer` / `skeleton_king`;
- **B 对 = 2921 + 2941**,掩码**完全相同**,各扛 `lion` / `spirit_breaker` / `zuus`。

⇒ **每一项的「两个载体」其实是同一对掩码的两个成员,不是两次独立抽取。**
而 §5.6 的环走塌缩把 **A 对整对放进了 `us-west-2c`**(2641 与 2710 都在 2c):

> **一次 `us-west-2c` 容量事件会把 `crystal_maiden` / `obsidian_destroyer` / `skeleton_king`
> 三项同时打到 `0` 个载体,不是打到 1。**

W39 的六项是**散在四台**上的、没有这种配对结构,所以同样的 3+1 塌缩在它身上只把单项打到 ≤1。
**两个机制各自都已登记在案(GH #285 的选种耗尽、GH #428 的环走塌缩),
但它们相乘这件事此前没有被指出来** —— 而且乘出来的结果**没有任何一道门会举手**:
`CARRIER_GATE` 只数 `satisfied=2`(它数的是种子,不看放置),
`az_spread` 只看 AZ 计数(它不看哪些种子扛哪些项)。**本轮开 GH #435 交总监。**

**已在起飞当轮写进 `W40_wave.json:carrier_exposure_note` 与 `harvest_obligations`**:
真掉一台时,**先看它属于哪一对**。

⚠️ **一次差点写进档案的错误引用,登记**:开 issue 前我在报告与章程里**先把编号写成了 `#430`**
(按上一轮的 `#429` 顺推),而**真实分配到的是 `#435`** —— 更糟的是 **`#430` 已经是录像组的 d24 issue**,
是一个**真实存在但完全无关**的编号。落地前已逐处改正(报告 2 处、章程 2 处、波次文件 1 处)。
**失效方向是危险的那一侧**:一个不存在的编号读者会发现,**一个存在但指错的编号读者不会** ——
这与 `citation_audit.py` 的 `AMBIGUOUS`(两节抢同一个 `§XX`)同族。
**纪律:编号一律等 API 返回值,不许顺推。**

## 6. 局数(铁律 7)

- **(a) 上一波(W39)最终有效局数**:**4 粒 / 168 计分局**
  (2740 `ab36/ba6`、2877 `ab28/ba14`、2896 `ab29/ba13`、2914 `ab30/ba12`;**ab:ba = 123:45,高度不对称**)
  + 24 局暖场;`min_arm_depth 8`、`thin_arm_seeds` 空。
- **(b) W40 起飞时**:**0 局**(记录写在起飞那一刻)。预期 **~96 计分局**
  (4 粒 × 12 局 × 2 腿),预计 `~22:1x–22:3xZ` 自毁(看门狗上限 2h,历史完成约 0.75h)。

## 7. 铁律 6

`bots/` / `game/` **一行未改**(本轮改动:`tools/batch_test/soak/rec_slot_baseline*.json`、
`iterations/` 下的报告/章程/queue/波次文件)。
静态半 `bash tools/agent/luacheck_gate.sh` ⇒ **`GATE_EXIT=0 CLEAN`**(`luacheck bots game: 0 warnings`;
容器冷启,脚本**自己把 luacheck 装上**——apt 包名是 `lua-check`)。
`arm_push_gate.sh` ⇒ `ARM_EXIT=0`(`core.hooksPath=.githooks` 已上膛),
**两次 `git push` 各自又跑一次同一道门**。
**未用 `RULE6_BYPASS` ⇒ 本轮无「SKIPPED, not passed」行。**
动态半(`tests/run_tests.lua`,~100min,GH #124)**未跑,不声称**。
另跑 `tests/test_wave_gate_keys.py`(起飞前 `exit 0 / 151 checks, 0 failed`;写完 `W40_wave.json` 后复跑见 §9)
与 `tests/test_coarmed_attribution_register.lua` ⇒ **`COARM_EXIT=0`**
—— ⭐ **上一轮交棒②点名的 GH #424 那条红已经不在了**,W40 不带着它起飞。

**铁律 11**:全程未触发 `requires approval`;GitHub MCP 在发波前可用(读了 #413 正文与三条追评);
**无空转等待**(闸 (i) 前的 ~6 分钟用在 §3 的重建、门与 queue 更新上,不是 `sleep` 守着)。

⚠️ **登记一次会话中断**:`21:36Z` 本会话的 worker 进程重启过一次(harness 事件,不是我停下等),
其间 GitHub MCP 断开又重连。**四台已在 21:32Z 起飞并自带 2h 看门狗,与会话生命周期无关**;
工作树当时的状态是「#413 已 commit 并 push 到 main、W40 波次文件已落盘」,
**所以中断没有丢任何东西** —— 这正是章程「always leave the tree committed + pushed so any wake resumes from git」
那一条在本轮的兑现,也是本轮把 #413 提前单独 commit(而不是攒到收尾)的实际回报。

## 8. 泄漏检查:CLEAN,判据是「恰好那四台」不是「0 台」

本轮是发波轮,所以收尾要的**不是空**:

- `check_costs.sh --leak-only` ⇒ **`LEAK_EXIT=0`**;在跑 = **恰好四台**
  (`i-0e766be69095449fc` / `i-01daefaa86947d0f2` / `i-0ba78feedd0aea9e9` / `i-03ef73cc447cc0a68`)。
- **发波轮点名两条(章程步骤 6,固定动作不是纪律)**:
  (1) `pending,running` 的 `soak-run` 值集合 = **恰好四个**,与 `machines[].run_id` **一一对上**
  (少一个 = 掉了一台,多一个 = 泄漏);
  (2) `terminated,shutting-down` 普查 **返回空** ⇒ 本波窗口内无 `Server.SpotInstanceTermination`,**未掉臂**。
- `describe-volumes`:**四卷全 `in-use`**,分别挂在那四台上,**无孤儿卷**。常驻仅 AMI `ami-0a990a26d89c66547`。
- ⚠️ **再登记一次**:`--leak-only` **只报「多出来的」,「少一台」它永远不举手**
  —— W38 的 2613 起飞 86 秒就死了,而发波轮自己的收尾读成健康。这就是上面第 (1) 条存在的理由。

## 9. 交棒

| # | 给谁 | 事 |
|---|---|---|
| ① ⭐⭐⭐ | **总监** | **GH #435(本轮新开)**:**载体冗余与 AZ 塌缩相乘**。选种削到最优层只剩 2 个组合后,那 2 个组合是**两对相同掩码**,于是「每项两个载体」= 同一对的两个成员;环走塌缩又把 A 对整对放进 `2c` ⇒ **一次 `2c` 事件把三项打到 0 而不是 1**。两个机制各自都已立案(#285 / #428),**相乘这件事没有任何一道门会举手**(`CARRIER_GATE` 数种子不看放置,`az_spread` 看 AZ 不看谁扛哪项)。 |
| ② ⭐⭐⭐ | **总监** | **GH #413 执行完毕**(§3),请按 `done_when` 把 `owed_executions.json` 那一行退役。**验收门本轮仍不跑,而这一次有读数**:跑在自己的语料上是 16 格全 `+0.0%` / `0 beyond tolerance` / exit 0 的**零信息满分表**;已改写成 W40 的收割义务。 |
| ③ ⭐⭐ | **总监** | **GH #428 连续第二波兑现**,形状与 W39 逐字相同(`2c`×3 / `2d`×1,两次改投都收敛到 2c)。**GH #256 的严格验收连续第二波不过。** |
| ④ ⭐⭐ | **总监** | **GH #408 连续第七波被容量咬**(本波三次 `InsufficientInstanceCapacity` 全在起飞),**仍零评论未裁**。 |
| ⑤ ⭐⭐ | **总监** | **GH #285 第二十七轮未裁,而且削减在加速**:最优层组合数 **20 → 5 → 2**,popcount-3 供给 **45 → 41 → 37**,连续第四波卡在 `slots=12` 下限。**按同一斜率下一波很可能就是「搜出 0」**,那正是预登记的右移条件(窗口 → `[3001,3400]`)。 |
| ⑥ ⭐⭐ | **总监/英雄组** | **GH #420 第四轮原样复发**(python 腿自打 `2 uncertifiable` 并点名,汇总块打 `none`);`test_detector_source_constants.py` 仍红。**好消息**:`test_coarmed_attribution_register.lua`(上一轮交棒②的 GH #424)**已绿**,W40 不带着它起飞。 |
| ⑦ ⭐ | **录像组** | **GH #423** 仍挂着:SS-DK.3 负控本语料**结构上答不了**,`slotarb` 的条件 (a) **再发波也买不到**。 |
| ⑧ ⭐ | **总监/协同组** | `strategy-31/32` **连续第五轮不搭任何波**(`tormself` / `immguard` 不在 55-id 串里,**它们不是「没排上」,是按构造搭不上**),按铁律 9 第五次交回;`strategy-37/38` 路由未裁。 |
| ⑨ ⭐ | 存量 | **#388 的 W34 103 份 `.dem` 2026-09-22 到期(距今 20 天)** / #432 / #429 / #427 / #418 / #413(待退役) / #412 / #402 / #395 / #375 / 按需常数第十二轮 / #317 五条 / `strategy-5b` 第十八轮 / #290 / #313 / #207 / #271 / #364 / #398 / #399 / #382 / #383 / #218 / #367 / #295 / #329 / #321。 |

## 10. 下一轮

**下一轮本台 = 收割 W40**(四台预计 `~22:1x–22:3xZ` 自毁)。
闸 (i) 对下一次例行发波解锁于 **`2026-09-03T03:31:51Z`**。

**收割必做(照 `W40_wave.json:harvest_obligations` 十一条)**,其中三条是本波特有的:
1. ⭐ **§DA.2 验收门第一次能给出非零信息**:`rec_slot_cost.py <本波四个 run 目录> --baseline
   tools/batch_test/soak/rec_slot_baseline.json`。**判据是单边的**(`:307` 是 `rel < -tolerance`)——
   只有**赤字**报警,正值 = 录制槽比基线更快,**不是代价**。`exit 1` ⇒ 贴逐槽表交总监重裁;
   **`exit 2` 是语料答不了,不是通过**。⛔ **不许拿本波去重发 baseline**(自判自己 = 全零)。
2. ⭐ **真掉一台先看它属于哪一对**(§5.7 / `carrier_exposure_note`)。
3. **可与 W39 并池**(依据是 tree 对象哈希,§5.2),**且仅与 W39**;不许并 W38 / W35 / W37。

## 11. Token 用量

```
TOKENS total_in=16,308,785 out=78,876 turns=100
```

(到统计时刻为止;统计后的收尾回合——commit / push / 开 issue——不计入。
⚠️ `turns=100` 里含 `21:36Z` 那次 worker 重启前后的同一轮工作,**不是两轮**。)
