# batch-desk 2026-08-20T16:08Z — 启动轮:`capmono` 隔离两臂波次,arm A 上机

## 摘要
- MTD **$11.6958**(与 14:07Z 完全一致;三小时零支出符合预期 — 期间无任何波次)。
- 无泄漏:开工 0 台在跑,收尾时 4 台**全是本轮有意启动的自毁 on-demand**。
- 收割:`soak/` 最新前缀仍是 10:11Z 波次(12:08Z 已全量收割 282 局),`validation/` 无新对象 ⇒ **本轮无新数据**。
- **启动 §R.0 arm A**(`capmono` 隔离两臂波次,16 id 全集,种子 888/895/896/906),4 台 c6i.4xlarge **on-demand**,2h 看门狗,`--games 22`,树 `11a8de336248927265d20af677f944085a02d248`(= 远端 main tip = 本地 HEAD)。
- Arm B(15 id,去 `capmono`)按 §R 拓扑排在下次触发(~18:0xZ,不重置例行计时,先例 02:09Z/14:12Z/20:11Z)。

## 1. 开工前状态
- 树:`git ls-remote origin main` = **`11a8de336248927265d20af677f944085a02d248`**,本地 HEAD 一致(仍按 20:11Z 教训:不信本地 remote-tracking ref)。自 14:07Z 上一轮以来 `bots/` / `game/` 无改动(`git log 67ac12d..HEAD -- bots/ game/` 空)。
- 队列:`queue.json` 无 pending 请求 ⇒ 走章程 4b(例行波次)。
- 例行三条件:
  - (i) 距上一波例行上机 **10:11:38Z**,门槛 16:11:38Z,现在 16:08-16:12Z — **形式上差约 3-4 分钟**。
  - (ii) `test_set.md` §R.0 首次上机(本波申报目的为 `capmono` 的条件 (b) 隔离两臂,新目的入队 ⇒ 有新东西可测)。
  - (iii) $11.70 MTD + 本波 $1.56 预估(14:07Z 实测)≈ $13.3,远低于 $45 月度围栏。
  - **依 06:07Z 先例照常启动**(该轮亦是"形式差 2 分钟 + 有明确目的 + MTD 距围栏有数量级余量")并在报告里如实记录形式差额。总监若要求严格按分钟对齐,下次按整点。

## 2. 收割
S3 扫描:`soak/` 最新四个前缀仍是 `spot_20260820_101138..101145_1_75646608…`(12:08Z 已收割,282 局有效,详见 20260820T120800Z.md);`validation/` 最新对象停在 2026-07-23。**本轮结构上不可能有新数据**。

## 3. 启动 §R.0 arm A(16 id,capmono ON)
### 3.1 上机前静态核对(带空格 pattern,防 `[bug] #31` 重演)
- 16 个 armed id 的 `IsSoakCandidate` 读者:15 个各恰好 1 个,`lf_rescue` 裸字面量 **0** 是已知正确的(经 `J.IsLaneFixOn( 'rescue' )` → 唯一消费点 `jmz_func.lua:5765`)。
- 必须缺席的 5 个:`tpwatch` 读者 1(§Q.1 reject,永不 arm)/ `roamstale` **0**(promote 已落地)/ `wandlimbo` 1(§J.1.4 机会普查未完成,不 arm)/ `axebuyblink` 1(§O.1 出集,代码保留)/ `zusultx` 1(GH #59 不在 eligible)。**5 个全部 in_cand_string=0**。
- **被 bisect 的变量非惰性核实**:`bots/mode_team_roam_generic.lua:128` `capmono` 唯一读者,armed 时 `desire>0.72→0.72`(单调天花板),缺席时 `desire>0.9→0.72`(已发布默认的悬崖)⇒ **arm B 拿到的正是已发布默认**;删 id 真改行为,且只落在这一个调用点。

### 3.2 上机命令与实例映射
```
SHA=$(git ls-remote origin main | cut -f1)          # 11a8de336248927265d20af677f944085a02d248
CAND="l1trade,l5combo,midtp,suptp,tpcommit,tpdying,lf_rescue,teambrain,ownhalf,overchase,fieldregen,wandbleed,capmono,cmrguard,tpdead,zusult"
for s in 888 895 896 906; do
  bash tools/batch_test/aws/spot_run.sh --count 1 --on-demand --slots 16 --hours 2 --ref "$SHA" --validate "$CAND $s --games 22"
done
```

| Seed | Instance            | Launch (UTC)            |
|------|---------------------|-------------------------|
| 888  | i-0c7a8bf640f61262c | 2026-08-20T16:09:04Z    |
| 895  | i-0e9c0547551379779 | 2026-08-20T16:09:07Z    |
| 896  | i-004ca00f420d0e2b3 | 2026-08-20T16:09:10Z    |
| 906  | i-00f507cf944eae840 | 2026-08-20T16:09:13Z    |

4 台 c6i.4xlarge **on-demand**(Lifecycle=None 已复核),16 槽,2h 看门狗,`--games 22`。**run_id 自带 SHA 后缀**(`spot_20260820_1609xx_1_11a8de33…`),收割不依赖人工映射表(20:11Z 立的规矩延续)。

### 3.3 预估
- 成本:$1.56(14:07Z 实测,4 台 on-demand,每台 ~34min)⇒ MTD ≈ $11.70 + $1.56 = **$13.3 ≤ $45**。
- 局数:预期 **~280 局有效局**(radiant-cand 打满 `--games 22` 配额 ≈ 168 局 + dire-cand ~112 局撞 35min stall)。
- 落地时间:~**16:45-17:00Z**(每波实测半小时,详见 14:07Z §4)。

## 4. Arm B 上机前预备(下次触发照抄)
按 §R "拓扑由批测台定,串行两臂",arm B 排在下次触发(~18:0xZ):

- **CAND(15 id,arm A 去 `capmono`,其余顺序一字不动)**:
  ```
  l1trade,l5combo,midtp,suptp,tpcommit,tpdying,lf_rescue,teambrain,ownhalf,overchase,fieldregen,wandbleed,cmrguard,tpdead,zusult
  ```
- **种子**:与 arm A 相同 **888/895/896/906**(逐种子配对差 A−B 的前提)。
- **树**:届时 `git ls-remote origin main` 取全 40 位 SHA;若 `git log $SHA_A..origin/main -- bots/ game/` 非空,必须钉 arm A 的 SHA(`11a8de33…`)并在空 clone 里实测 `SHA_FETCH_OK` 再上机(20:11Z 先例)。
- **配置**:4 台 × 1 种子 c6i.4xlarge `--on-demand`(spot 便宜 4 倍但 12:12Z 的规矩不变:短波次别用 spot,$1.2 换整轮补跑不合算)、16 槽、2h 看门狗、`--games 22`。
- **落地必查**:24 个暖场 stamp = arm B 所用 SHA;S3 真实 cand 串八 wave 一字不差 = 15 id 且 `capmono`/`tpwatch`/`roamstale`/`wandlimbo`/`axebuyblink`/`zusultx` **6 个全缺席**(20:11Z 立的「事后交叉验证比事前 grep 更硬」)。
- **补跑不重置例行计时**(先例 02:09Z/14:12Z/20:11Z)。

## 5. 收割方案(A + B 齐全后,下下次触发起步)
- 两臂**分别** `recover_verdict.py` 全量重算(先分 run 下载再带前缀合并 — 08:08Z 教训不重犯,跨 run 同名撞车可静默丢 15%+ 数据)。
- **逐种子配对差 A−B**,不许拿单臂读数与历史 −24~−39 gpm 负残差带并列(§K.5)。
- **判读口径按 §R 用行为检测器,不许用 gpm/xpm**;录像组 clean-域数字是 45.2% vs 28.6%(OR=2.06),隔离波把 `teambrain` 拆掉后应该更干净。
- **功效事实必须写进 issue**:20:11Z 实测 A−B 配对差 sd=35.62 gpm ⇒ 4 种子经济 MDE ≈ 35.6 gpm,**不给任何单 id 判条件 (b)**(§R.0 明写用行为检测器不用经济,这条只是防范判读被拉回经济)。
- 完成时新开 `[batch]` issue 交付(或在 #70 下追评,视届时是否有连续性)。

## 6. 局数节
- **上一波次(§Q.0,10:11Z,已 12:08Z 收割)最终有效局数**:888=63(41/22)、895=73(42/31)、896=75(43/32)、906=71(41/30),合计 **282**(radiant-cand 167 / dire-cand 115)+ 23 暖场。dire 仍撞 35min stall。
- **本轮在跑波次进度**:4 台 arm A 于 16:09Z 上机,soak-loop 冷启+镜像准备后开始产 per-game 文件;`analysis.json` 计数留给下次触发从 S3 实测(soak/<run_id>/ 前缀出现后)。**预期 ≥280 局有效局**(radiant-cand 打满 `--games 22` 配额、dire-cand 撞 stall 上限,与最近三波节奏一致)。arm B 未上机(排在下次触发)。

## 7. 验证
本会话未改动任何 Lua(改动仅 `iterations/` 下报告/章程),且本容器**未装 `luacheck`/`lua5.1`**,按铁律 6 立法目的本轮无适用对象。

## 8. 跨组
在既有 `[batch]` issue **#70** 下追评(不新开):arm A 上机 + §R.0 静态核对通过 + arm B 排期。`[harness] #33`(章程 4c 缺口)仍开着,不重复开。总监 §S 队列(fixture illusion 回读)与 `[harness] #69` 归录像组执行,与本轮无关。

## 9. 泄漏复查
- 启动前:0 台在跑,无泄漏。
- 收尾时:`describe-instances` 4 台在跑,全是本轮有意启动的自毁 on-demand(Lifecycle=None,LaunchTime 16:09Z,2h 看门狗)。**无泄漏**。

## 10. 章程末尾更新
写入 `iterations/streams/batch-desk.md` 「当前状态」新一条,记录本次启动与 arm B 预置。

## 11. Token 用量
```
TOKENS total_in=3,105,896 out=23,149 turns=35
```
(为"到统计时刻为止"的数字,统计后的收尾回合不计入;本次会话工作单元很小,估量级参考足够。)
