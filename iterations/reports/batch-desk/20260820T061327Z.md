# 批测台 2026-08-20T06:13:27Z — 纯收割轮(零支出,未启动任何批测)

## 0. 一句话

04:11Z 的 (a) 取证波四种子全齐收全(18 id,挑种子 872/875/885/887,树 `7c8b516`,
291 局有效局):gpm **−39.65**,四指标全 **0/4**,`hold_or_reject`;
**挑种子的目的达成 —— `axebuyblink` 的取证语料从上一波的 0 局变成 291 局(4/4 种子)**;
例行波次 6h 节流未到(下一波最早 **10:11Z**),本轮不启动,零支出。

## 1. AWS 成本与泄漏(章程步骤 2 / 6)

- `session_setup.sh` 引导成功:`arn:aws:iam::581436534924:user/dota2bot-agent`(受限用户,正确)。
- **MTD `$5.2934350747`** —— 与 04:05Z 那轮**逐位完全一致**。04:11Z 的 4 台 on-demand 尚未入账。
  计费滞后仍在:14:11Z 3 台 + 18:08Z 4 台 + 20:09Z 4 台 + 22:11Z 4 台 + 04:11Z 4 台
  = **19 台机器里只有一部分入了账**(04:05Z 那次 +$1.24 是滞后首次追平)。
  **这不是泄漏** —— 本轮开工前与收尾时 `describe-instances` 均 **0 台在跑**。
- **未触发任何预算刹车**:$5.29 ≪ $90 刹车线,亦远低于 $45 月度围栏。
- 常驻成本只有 AMI `ami-0a990a26d89c66547` + 快照,与历轮一致。

## 2. 收割:04:11Z 的 (a) 取证波(章程步骤 3)

### 2.1 标准路径与防坑

四个 run(`spot_20260820_041132/041134/041137/041139_1_7c8b5167…`)**先分 run 下载
再带前缀合并**(08:08Z 踩过的跨 run 同名 per-game 文件撞车,那次静默丢了 15.9% 的局):
79 + 80 + 76 + 80 = **315 个 `analysis.json`,合并后仍 315,一个不丢**。
然后 `tools/batch_test/soak/recover_verdict.py` 全量重算。

### 2.2 三项必查项(04:05Z 立的)全部通过

| 必查项 | 结果 |
|---|---|
| 暖场局 stamp 必须是 `7c8b516` | **24/24 通过** —— 裸 SHA checkout 成功,**连续第三波通过该校验**,数据有效 |
| S3 真实 cand 串逐字 = 18 id,`roamstale`/`wandlimbo` 缺席 | **通过**,八个 wave 的 stamp 一字不差:`l1trade,l5combo,midtp,suptp,tpcommit,tpdying,lf_rescue,teambrain,ownhalf,overchase,fieldregen,wandbleed,tpwatch,capmono,cmrguard,tpdead,axebuyblink,zusult` |
| 先分 run 下载再带前缀合并 | 通过(见 2.1) |

补充:**4 种子 × 2 wave 全齐,无饿死**(1 种子/实例的拓扑连续第六波奏效)。

### 2.3 读数

```
mean:  gpm -39.65 | xpm -36.26 | deaths +0.33 | last_hits -1.75
comps_better: gpm 0/4 · xpm 0/4 · deaths 0/4 · last_hits 0/4     suggested: hold_or_reject
per-seed gpm: 872 -33.44 | 875 -53.77 | 885 -27.16 | 887 -44.24
              sd 11.76, SE 5.88  →  实测 z = -6.74;按 GH #30 经验零点 σ=30.24 换算 z = -2.62
per-seed 局数: 872 40/33=73 | 875 41/33=74 | 885 40/30=70 | 887 42/32=74   (radiant/dire)
```

**判读纪律(批测台不判读)**:
- **§K.5 副作用照章标注:本波是挑种子波次,经济读数不得与 863-870 这类连号波次并列比较。**
  所以本轮**不主张**「−39.65 比上一波 −27.15 更差」——两波种子集不同,差值里混了阵容抽签,
  不可归因。同理也不主张「落在同一条负残差带内」是新证据。
- 4 种子 MDE ≈ 35.6 gpm(22:12Z 实测的配对差功效事实)⇒ **不得给任何单 id 判条件 (b)**。
  本波主产出是 `.dem` 语料,经济读数只当整集合粗看板(§K.4)。
- 判读/promote-reject 归总监与协同组。

## 3. 本轮真正的交付:挑种子的收益兑现了,且「全有或全无」第三次坐实

对 291 局逐局阵容清点(S3 地面真值,不是工具预测):

| 英雄 | 872 | 875 | 885 | 887 | 合计 |
|---|---|---|---|---|---|
| Axe | 73 | 74 | 70 | 74 | **291 / 291(4/4 种子)** |
| Zeus (`zuus`) | 73 | 0 | 0 | 0 | 73(1/4) |
| Lion | 73 | 0 | 0 | 0 | 73(1/4) |
| Wraith King | 0 | 0 | 70 | 0 | 70(1/4) |
| Crystal Maiden | 0 | 74 | 70 | 0 | 144(2/4) |

每一格**不是 0 就是满**,「一个种子 = 一套阵容、对该种子每一局全有或全无」**第三次实测坐实**;
本波 25 个不同英雄(§K.5 预估 25,命中)。

**收益**:上一波连号种子 867-870 给 `axebuyblink` 的取证语料是 **0 局**(#49 的发现);
本波挑种子后是 **291 局、4/4 种子、全 pos3** —— #46 提的选种工具在真实批次上第一次
兑现了它承诺的东西。录像组 05:05Z 已用这批语料出了 `axebuyblink` 的 (a) WORKING 判定
(GH #56),链路走通。

**已知代价(诚实报告,§K.5 自己写明的)**:`zusult` 只拿到 **1/4 种子 / 73 局 / 单套阵容**。
02:06Z 我在 `[batch] #49` 下提过替代种子集 **872/910/1024/1043**(axe 4/4 不损失、
zuus 4/4、lion 4/4,增量成本 $0),**总监未裁定**,故 04:11Z 按章程「以届时 `test_set.md`
逐字为准 / 不自行改变被测集合」上了 §K.5 指定集。**本报告再提请一次**:若 `zusult` 还要
条件 (a) 取证,下一波换这个种子集是白拿 4 倍语料;不换则 `zusult` 仍是 1 套阵容。

## 4. 启动决策:不启动(章程步骤 4)

- `queue.json`:`requests` 为空(`director-1` 已 `done`)⇒ 走章程 4b 例行波次判定。
- 例行三条件:
  - **(i) 不满足** —— 上一次例行波次上机 **04:11:32Z**,门槛 = +6h = **10:11Z**;
    本轮开工 06:10Z,**差约 4 小时**。属小时级差额,**无 06:07Z 那种「差 2 分钟按立法目的」
    的例外情形**(先例:00:09Z 差 4h / 02:06Z 差 2h / 10:06Z 差 2h / 16:10Z 差 2h,均不启动)。
  - (ii) 满足(`bots/` 自 `7c8b516` 后有 hero 组 GH #50/#56 相关改动)。
  - (iii) 满足($5.29 + ~$0.7 ≤ $45)。
- ⇒ **本轮零支出。下一波例行最早 10:11Z。**
- 章程 4c(稳定版 vs upstream 基线 `74727e4a`)缺口:`[harness] #33` 已立案且仍开着,不重复开。

## 5. 局数(章程步骤 7,固定节)

- **(a) 上一波次最终有效局数**:04:11Z 波次 **291 局有效局 + 24 局暖场**,
  radiant **163**(40/41/40/42)/ dire **128**(33/33/30/32)。
  **ab/ba 不对称仍在**:radiant 四种子基本打满 `--games 22` 配额,dire 仍撞 35min stall 上限;
  **再提 `--games` 只会拉长 radiant、加大不对称**,补 dire 要动 harness 侧 stall 上限(#33 域外)。
- **(b) 本轮在跑波次**:无(未启动)。

## 6. 下一波(≥10:11Z)预置配置

**上机前以届时的 `test_set.md` 逐字为准**(本轮 05:00Z 的 §N.0 明写测试集逐字未变,
且 `#45` `roamreach` / `#44` `tparrive` / `#52` `tpwatch` 出集 / `#55` `defstale` /
`#56` `axebuyblink` 代价 / `#58` `defclose` 六项裁定都在顺延 —— 集合届时可能变)。

沿用已被反复验证的配置:
- 拓扑 **4 台 × 1 种子** c6i.4xlarge **`--on-demand`**(on-demand Standard 配额 64 vCPU
  正好 4 台;短波次别用 spot —— 18:08Z 4/4 拿不到容量、12:12Z 被回收 3/4 的先例)。
- 16 槽,**2h 看门狗**,`--games 22`。
- 树用 **`git ls-remote origin main` 取的全 40 位 SHA**,**不信本地 `origin/main`**
  (20:11Z/02:06Z 两次踩过:本轮 `git fetch` 又打出 `+ 46d381d...fa3a332 (forced update)`,
  本地 remote-tracking ref 再次落后几十个 commit)。
- 上机前 18(或届时 N)个 id 逐个 grep 静态核对(防 `[bug] #31` 式「测了个不生效的东西」;
  `lf_rescue` 裸字面量命中 0 是已知正确的,它经 `J.IsLaneFixOn('rescue')` 展开)。
- 落地后必查:暖场 stamp = 该 SHA;S3 真实 cand 串逐字;先分 run 下载再带前缀合并。
- 选种若要覆盖 Axe+Zeus+Lion 三英雄,用 **872/910/1024/1043**(见 §3,待总监裁定)。

## 7. 验证(铁律 6)

本会话**未改动任何 Lua**(改动仅 `iterations/` 下的报告与章程),且本容器**未装
`luacheck` / `lua5.1`**(`which` 两者均空)。按铁律第 6 条的立法目的,本轮无适用对象。

## 8. 跨组

在既有 `[batch] #49` 下**追评**(不新开):交付本波读数 + 语料清点兑现 + `zusult` 选种提请。

## 9. Token 用量

```
TOKENS total_in=4,478,916 out=39,615 turns=51
```

(统计时刻为收尾提交前;此后仅剩 commit/push 两步。)
