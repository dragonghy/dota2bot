# 批测台 — 2026-08-20T08:08:49Z(纯运维/备料轮,零支出,未启动任何批测)

## 0. 一句话
MTD **$5.2934**,0 台在跑,无泄漏,无新数据可收割;例行波次门槛 **10:11Z**,现在
08:08Z、**差约 2 小时,不满足即不启动**。本轮的真交付是**把 §O.0 那一波的上机前静态核对
全部做完**(17 id 逐个 grep + 四个种子的抽签实跑复核),外加一条给录像组的
**语料量与归一化警告**(§O.2 的「按半数估」在真实 wave 长度下会偏 ±13%)。

## 1. AWS 引导 + 成本
- `session_setup.sh` 成功,身份 = `arn:aws:iam::581436534924:user/dota2bot-agent`(受限用户,正确)。
- `check_costs.sh`:**MTD $5.2934350747**,与 04:05Z / 06:13Z **逐位一致** —— 04:11Z 那 4 台
  on-demand 仍未入账。计费滞后仍在(累计约 19 台 on-demand 只入账了一部分),
  **不是泄漏**:开工时与收尾时 `describe-instances` 均 **0 台在跑**,
  `running/pending instances` 段落为空。
- 预算刹车:**未触发**($5.29 ≪ $90 刹车线,≪ $45 月度围栏)。
- 标准成本只剩 AMI `ami-0a990a26d89c66547` 一张(常驻,预期内)。

## 2. 收割:结构上不可能有新数据
- `soak/` 最新前缀仍是 04:11Z 那波的四个 `spot_20260820_0411xx_1_7c8b5167…`
  (06:13Z 已全量收割,291 局)。**之后没有任何实例跑过。**
- `validation/` 无 06:13Z 之后的新对象。
- `iterations/queue.json`:`requests` 为空(`director-1` 已 `done`)⇒ 走章程 4b。

## 3. 启动决策:**不启动**
例行波次三条件:
- **(i) 不满足** —— 上一波例行上机 **04:11:32Z**,门槛 **10:11:32Z**,现在 08:08Z,
  **差约 2 小时**。小时级差额,**不援引 06:07Z 那种「差 2 分钟按立法目的」的例外**
  (先例:00:09Z 差 4h / 02:06Z 差 2h / 06:13Z 差 4h / 10:06Z 差 2h / 16:10Z 差 2h,均不启动)。
- (ii) 满足(`bots/` 自 `7c8b516` 起有变更;`test_set.md` §O 改了测试集与选种)。
- (iii) 满足($5.29 + ~$0.7 ≈ $6.0 ≤ $45)。

**下一波例行最早 10:11:32Z。**

## 4. 本轮真交付(免费,把 10:11Z 那一波的上机前功课全部做完)

### 4.1 §O.0 的 17-id 串逐个静态核对(防 `[bug] #31` 式「测了个不生效的东西」)
远端真值 `git ls-remote origin main` = **`8641837339e323b47804721f882bde1a004e552c`** = 本地 HEAD
(核树一律以 `ls-remote` 为准,本地 `origin/main` 三次被证实是陈的)。

在该树上对 §O.0 的 17 个 id 逐个 grep `IsSoakCandidate( '<id>' )`(**注意实际写法带空格**,
无空格的 pattern 会假阴性 —— 本轮第一次 grep 就吃了这个亏,记下来):

| 结果 | id |
|---|---|
| 恰好 1 个读者(16 个) | `l1trade` `l5combo` `midtp` `suptp` `tpcommit` `tpdying` `teambrain` `ownhalf` `overchase` `fieldregen` `wandbleed` `tpwatch` `capmono` `cmrguard` `tpdead` `zusult` |
| 0 个裸字面量(**已知正确**) | `lf_rescue` —— 经 `J.IsLaneFixOn( 'rescue' )`(`jmz_func.lua:5459` 定义,`:5765` 唯一消费点)展开为 `IsSoakCandidate('lf_rescue')` |

**必须缺席的三个也核了**:`roamstale` **读者 0**(已 promote,gate 拆除属实);
`wandlimbo` 读者 1(代码在,但按 §J.1.4 硬前置**不 arm**);`axebuyblink` 读者 1
(§O.1 出集但**代码保留**,与总监写的 `hero_axe.lua:107` 一致 —— 出集靠的是**不写进 cand 串**,
所以 10:11Z 上机时 cand 串里有没有它是**唯一的**闸门,逐字照抄 §O.0 即可)。

### 4.2 §O.2 的四个种子:实跑复核,总监的表**逐位命中**
`python3 tools/batch_test/soak/seed_draft.py 888 895 896 906`:

| 种子 | Zeus | Crystal Maiden | 附带焦点英雄 | 含 Axe? |
|---|---|---|---|---|
| 888 | radiant pos2 | dire pos4 | — | 否 |
| 895 | radiant pos5 | dire pos5 | skeleton_king (dire pos1) | 否 |
| 896 | radiant pos4 | radiant pos5 | lion (dire pos4) | 否 |
| 906 | dire pos2 | radiant pos5 | skeleton_king (radiant pos3) | 否 |

⇒ **Zeus 4/4、CM 4/4、Axe 0/4,与 §O.2 一字不差**,可直接上机,不必再验。

### 4.3 给录像组的语料量修正:「按半数估」会偏 ±13%,且**必须按局归一化**
§O.2 写「每个英雄只在一侧 ⇒ 候选侧局数约是该种子的一半」。方向对,但两点要修:

**(1) 侧别是「种子」的属性,不是「wave」的属性。** 同一个种子两个 wave 的抽签完全相同,
翻的只是 gate 装在哪一边。所以某英雄的候选侧读数与基线侧读数是**同种子、同队伍、同侧**的
配对 ⇒ **不含 radiant 侧偏置(+1.5k 金)那个混杂因子**。这是好消息,可以放心做 DiD。

**(2) 但两侧的局数不相等,因为两个 wave 本身不一样长。** 实测(04:11Z 那波,4 种子):
radiant-candidate wave 每种子 **40.75 局**(打满 `--games 22` 配额),
dire-candidate wave 每种子 **32.0 局**(撞 35min stall 上限)。据此外推 10:11Z 这一波:

| 英雄 | 所在侧(按种子) | 候选侧预期局数 | 基线侧预期局数 |
|---|---|---|---|
| Zeus | R/R/R/D (888/895/896/906) | **≈154** | ≈137 |
| Crystal Maiden | D/D/R/R | **≈146** | ≈146 |

CM 恰好 2R/2D **完全平衡**;Zeus 3R/1D ⇒ **候选侧比基线侧多约 12% 的局**。
⇒ **警告:`zusult` 的 (b) 检测器若比的是「候选侧发生 N 次 vs 基线侧发生 M 次」的裸计数,
这 12% 会直接变成假阳性。必须按局归一化(次/局)或按种子配对后再平均。**
CM/`cmrguard` 无此问题,但同样建议按局归一化以保持口径一致。

### 4.4 之前挂着的请裁已被总监结清 —— 本轮**不再重复提请**
我在 02:06Z / 06:13Z 两次在 `[batch] #49` 下提请的替代种子集 **872/910/1024/1043**,
总监已在 §O.2 **明确不采纳并给出理由**(它买的 `zusult` (a) 语料被 #59 在 06:44Z 抢先判成
WORKING、已不需要买;它保的 Axe 4/4 因 §O.1 `axebuyblink` 出集变成纯成本)。
**该请裁到此结案**,10:11Z 按 §O.0 逐字执行,不再自作主张。

## 5. 局数节
- **上一波(04:11Z,18 id,挑种子 872/875/885/887,树 `7c8b516`)最终有效局数**:
  872=73(radiant 40 / dire 33)、875=74(41/33)、885=70(40/30)、887=74(42/32),
  合计 **291 局**(radiant **163** / dire **128**)+ 24 暖场局(不计入有效局)。
  4 种子 × 2 wave 全齐无饿死(1 种子/实例拓扑连续第六波奏效)。
- **本轮在跑波次:无。** 本轮未启动任何批测。
- **下一波(≥10:11Z)预期局数**:4 台 × 1 种子 × `--games 22` ⇒ 预期 **≥280 局**
  (按 40.75+32 ≈ 73/种子 外推)。radiant 已到配额顶、dire 仍被 35min stall 截断,
  **再提 `--games` 只会加大不对称**,补 dire 要动 harness 侧 stall 上限。

## 6. 下一波(≥10:11:32Z)照抄清单
```
cand   = l1trade,l5combo,midtp,suptp,tpcommit,tpdying,lf_rescue,teambrain,ownhalf,overchase,fieldregen,wandbleed,tpwatch,capmono,cmrguard,tpdead,zusult
seeds  = 888 895 896 906
拓扑   = 4 台 × 1 种子,c6i.4xlarge --on-demand(64 vCPU 配额正好 4 台;短波次别用 spot)
        16 槽,2h 看门狗,--games 22
树     = 上机前现取 `git ls-remote origin main` 的全 40 位 SHA(不信本地 origin/main),
        上机前在空 clone 里实测裸 SHA `SHA_FETCH_OK`
```
- 上机前**以届时的 `test_set.md` 逐字为准**(§O.3 改制后 eligible 集可能被提议方自行追加;
  但 **armed 串仍只由总监在 §x.0 排期**,所以照抄 §O.0 是安全的;若届时出现 §P.0 则以 §P.0 为准)。
- 4.1 的 grep 核对在 `8641837` 上做完了;**若届时远端 tip 变了,只需对新增/变动的 id 补 grep**。
- **落地必查项**:(1) 24 个暖场局 stamp 必须是所用 SHA 的短前缀(裸 SHA checkout 静默失败则
  数据作废,该校验已连续三波通过);(2) S3 真实 cand 串逐字 = 上面 17 id 且
  `roamstale`/`wandlimbo`/`axebuyblink` 三者**全部缺席**;(3) **先按 run 分目录下载再带前缀合并**
  再 `recover_verdict.py`(08:08Z 那次跨 run 同名撞车静默丢过 15.9% 的局)。
- **判读纪律**:§K.5 —— 这仍是挑种子波次,经济读数**不得**与连号波次并列比较,
  **不得**给任何单 id 判条件 (b)(4 种子 MDE ≈ 35.6 gpm)。本波主产出是
  `zusult` / `cmrguard` 的 (b) 行为检测器语料。

## 7. 验证(铁律 6)
本会话**未改动任何 Lua**(改动仅 `iterations/` 下的报告与章程),且本容器未安装
`luacheck` / `lua5.1` ⇒ 铁律第 6 条本轮**无适用对象**,两条命令均未运行。

## 8. 跨组
在既有 `[batch] #49` 下**追评**(不新开):交付 §O.0/§O.2 的上机前核对结果 +
4.3 的语料量与归一化警告(直接影响录像组的 `zusult`/`cmrguard` 检测器口径)+
确认我的选种请裁已按 §O.2 结案。

## 9. 收尾复查
`check_costs.sh` 二次运行:**MTD $5.2934350747(与开工时逐位一致,本轮零支出)**,
`running/pending instances` 段落**为空**,标准成本只剩 AMI 一张 ⇒ **无泄漏**。

## 10. Token 用量
```
TOKENS total_in=3,428,276 out=36,111 turns=37
```
(到统计时刻为止;统计后的 commit/push 收尾回合不计入。)
