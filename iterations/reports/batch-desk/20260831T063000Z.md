# 批测台 2026-08-31T06:30Z —— 发波轮:W31 起飞(47-id 家族第二波,四台四 AZ 全部一次到位)

**一句话**:闸 (i) 06:16:31Z 解锁、四道门全过,**W31 四台 spot 于 06:17:21–06:17:35Z 起飞**
(种子 2375/2393/2403/2444,树钉 `1dd5705f`,47-id 串一字未动);
⭐⭐⭐ 但本轮真正的产物是发波之外的一条:**铁律 10 的自检从今晨 04:23Z 起会在每一个流的每一次触发上打
`TRUNK RED` —— 而它是对的**:总监 `074d3e9c` 落地的 120s 预算断言撞上了一条**实测 133.3 秒**的腿,
**其中 78.4 秒来自两个文件**(已开 **GH #358**)。

---

## 1. 自检(铁律 10)

后台单次运行,**裸退出码不经管道**(`EXIT=$?` 直接写进日志文件)。归因照抄工具(GH #267 4b):

```
legs run              : 8
FINDINGS (exit 3)     : trunk-red(python)
UNCERTIFIABLE (exit 2): none
selfcheck worst exit: 3
```

- **python 腿:66 passed / 1 failed / 0 uncertifiable**,唯一红的是 `tests/test_selfcheck_lua_leg.py`
  (上一轮 03:25Z 是 65/0/0;两个新文件 `test_cmqreach_domain.py`/`test_fixture_creeps.py` 于 05:02Z 落地,
  分母 65 → 67)。**根因见 §5,不是 bot 逻辑红**。
- **Lua 检测器腿:50 个 tagged 文件、0 failures**(上一轮 48;新增的正是 05:02Z 落地的
  `test_cm_creep_reach_real_frame` / `test_cm_q_creep_aoe_reach`,实测 0.36s + 0.45s,**便宜**)。
- unlanded `OK`;cadence `clean`(7 条 `SKIPPED-IN-STREAM`,均为历史命名);
  未裁 queue 请求 **none**;`ORPHAN_PROPOSAL none`;`ROWLESS` 3(§BR/§BT/§BV,informational);
  **`UNKNOWN STATUS` 4 条**(GH #317 词汇漂移,均 `ruled=True`,第 N 轮照旧);
  `stable-v1`/`v2` **2/2 OK**。

### 1.1 ⭐⭐ 顺带买到的一件:同一台容器上,`unlanded_commits.py` 的 `OK` 有两个完全不同的域

上一轮(03:25Z §4.7)在 `claim_precheck.sh` 上钉死了浅克隆坑,并给所有 stream 留了一句
「容器默认浅克隆,任何跨越数日的 `git log` 区间计数在 `--unshallow` 之前都不可信」。
本轮**在铁律 10 自己的工具上复现了同一根因**,而且形状更值得登记 —— 因为它**不打任何横幅**:

| | 浅克隆(自检实际跑的那次) | `git fetch --unshallow` 之后(本轮复跑) |
|---|---|---|
| trunk | `origin/main (50 commits)` | `origin/main (1621 commits)` |
| certifiable refs | **25** of 602 | **602** of 602 |
| commits examined | **0** | **16** |
| 年龄下限 | 被 graft point 顶到 `2026-08-30T16:15:45`(`--days 3` 的窗口里**丢了两天**) | 窗口按 `--days 3` 兑现 |
| 结论行 | `OK: no unlanded work in the certifiable window.` | `OK: no unlanded work in the certifiable window.` |

**两次一字不差**。工具没说谎(限定语 `in the certifiable window` 就写在那句话里),但那半句
**承担了全部信息量而没有任何视觉重量**:检查了 **0** 个 commit 与检查了 16 个,读者拿到同一行 `OK`。
**失效方向是安全那一侧**(读成"没漏活"),所以它不会自己举手 —— 与 `claim_precheck` 那次
(把 trunk 上的 commit 报成不在 trunk,读成危险)是**同根异向**的一对。
本轮真值恰好也是 clean,**所以这次什么都没漏 —— 那是运气,不是工具**。
建议(不是本台的活):浅克隆下把结论行降级为 `UNCERTIFIABLE`(exit 2),或工具自己先 `--unshallow`。
**已连同 §5 一起写进 GH #358 的第 2 节。**

---

## 2. 成本与预算闸

| 项 | 读数 |
|---|---|
| 开工 running/pending | **空**(零遗留) |
| MTD(`budgets describe-budgets`,免费) | **$73.90**,快照 `2026-08-31T04:55:37Z`,forecast 77.101,限额 100.0 |
| CE 复核($0.01,脚本因 ≥$35 自动触发) | **$73.8997453436** —— 与 budgets **逐位一致,连续第三十五轮** |
| 常驻成本 | 仍只有一个 AMI(`ami-0a990a26d89c66547`),零快照增量 |

**围栏算术(滞后条款 (甲),逐波按自己的界别取价)**:

```
$73.90 (MTD) + $0.90 (W30,00:16Z 起飞,在近 12h 内 ⇒ 保守假定尚未进快照)
             + $0.90 (W31 本波,spot)
           = $75.70  ≤  $80(围栏 = 下一个未跨过的 owner 可见 Budget ACTUAL 告警档)
```

- 快照 `04:55:37Z` 晚于 W30 自毁(~02:16–02:29Z)**约 2.5 小时**,按 §2 实测的 4.3–11.3h 滞后
  **仍在不可信区间内**,故按 (甲) 照记 W30 一波价。**未使用条款 (乙) 那个手法**
  (「MTD 增量 $1.065 ≈ 一波价 ⇒ W30 已计入」)—— 那正是章程两次点名禁用的推理。
- **本轮不跨任何告警档**($80 未跨),故**不欠**「跨档解释行」。刹车 $90 / owner 线 $100 未接近。
- **单波成本自动失效条款**:W30 账单侧实测 $0.824–0.892 ≤ $0.90 ⇒ **未触发**,常数不动。

---

## 3. 收割

**本轮无待收割波次**。W30 已于 03:25Z 收割完毕(231 局、scored 207、unfinished 0、四粒全过深度门
零排除、gpm +18.34、`suggested: promote`),读数与交棒见
`iterations/reports/batch-desk/20260831T032500Z.md`。本轮**未重复收割,未重复计费**。

**唯一在 W30 记录上新跑的工具是 `reclaim_blind.py`**,因为它是本波市场类型的**输入**
(GH #271:降级阶梯由「回收致盲」触发),按「立论现场必须跟着数据走」的纪律
**当轮重跑而不是引用上一轮的那次**:

```
BARE_EXIT=0(不经管道),stderr 空
changeover 40.0 min (measured bracket 34.80 < x <= 40.63, margin above the constant 0.63 min)
min_arm_depth 8.0
  2204 PAIRED [depth-checked] ab36/ba16   2214 PAIRED ab27/ba15
  2286 NO-PAIR (instance-terminated-no-capacity) ab0/ba0
  2315 PAIRED ab41/ba15                   2286(补发) PAIRED ab37/ba20
yield      : 4 paired seed(s) of 5 machine(s)
attribution: 1 machine(s) reclaimed before the flip
VERDICT: not blinded -- the wave delivered 4 paired seed(s).
NEXT WAVE: spot (the default; GH #158 unchanged).
```

⇒ **W31 走 spot 是这条读数点的,不是习惯**。

---

## 4. 发波:W31

### 4.1 四道闸(全部有独立证据,不是自述)

| 闸 | 判据 | 结果 |
|---|---|---|
| (i) 距上波 ≥6h | W30 起飞 `00:16:31Z` ⇒ 解锁 `06:16:31Z` | **PASS**,首次调用 `06:17:21Z`,**余量 50 秒**;由发波块内的 `date -u` 守卫强制(不满足 exit 9),不靠手看 |
| (ii) 有新东西可测 | `git log 0d58ce09..HEAD -- bots/ game/` | **PASS,2 个 commit**:`074d3e9c`(总监裁 GH #346 + Silencer 本体落地)、`083ad814`(协同组 pullcad 注释-only + ratchet) |
| (iii) 预算 | 围栏 $75.70 ≤ $80 | **PASS**(§2) |
| (iv) 输入无欠账 | 无待收割;queue 无本波专属 `APPROVED` 排期 | **PASS**;`reclaim_blind` 见 §3 |

**闸 (ii) 有一处必须写明的方法学**:这条 `git log` 区间**先 `git fetch --unshallow` 才跑**。
上一轮已证浅克隆会把区间计数算错(22 vs 23),而**本轮的区间起点 `0d58ce09` 恰好在 50-commit 浅历史里**,
所以浅树上它**也能给出答案** —— 但那是运气。unshallow 后的读数才是本表引的那个。

`test_set.md` 第 2 行**一字未动**(47 ids / 414 bytes,与 W30 逐字节相同)⇒
**本波换的是树,不是测试集**。

### 4.2 起飞记录(第一手,写于四台 `describe-instances` 读作 running/spot 之时)

| 种子 | AZ(请求→实得) | 实例 | SIR | run token | 起飞 | 载体 |
|---|---|---|---|---|---|---|
| 2375 | 2a → **2a** | `i-0efb4f0359b92cb13` | `sir-kj4zjwkq` | `731a21` | 06:17:21Z | cm, lion, od, zuus |
| 2393 | 2b → **2b** | `i-04ef0b9835397607f` | `sir-p8bfhbgq` | `fde133` | 06:17:26Z | cm, lion, wk, zuus |
| 2403 | 2c → **2c** | `i-0e8b937752d64c739` | `sir-2afzgtnn` | `b5b4b9` | 06:17:31Z | cm, od, wk, sb |
| 2444 | 2d → **2d** | `i-035ac2979fa03df7c` | `sir-7sjqh66p` | `c44eb5` | 06:17:35Z | cm, lion, wk, sb |

- **市场类型:四台全 `InstanceLifecycle=spot`**(`describe-instances` 证据,**不是** run_id 的 `spot_` 前缀)。
  阶梯停在第 1 级,**无 `--type c6a` 备选、无 `--on-demand`**。
  **第九波连续零市场类型降级**,owner 的 Spot 优先裁定(GH #158)继续成立。
- **AZ:4 请求 / 4 到位 / 4 互异**。stderr 里**既无** `re-aiming inside the ring`
  **也无** `AZ RING EXHAUSTED` ⇒ **GH #252 验收(≥2 互异)与 GH #256 验收(4 互异且零 re-aiming)本波双双满足**,
  且这是 **W18 以来第一波每个 AZ 都一次就给**的波。对照 W30:2a 报 `InsufficientInstanceCapacity`、
  环内改投 2b。**结论只到这里:今晨容量不紧张。**(GH #256 的两个计数器:本波**零降级、零 re-aiming**。)
- **run_id 唯一性**:四个 `soak-run` 标签值两两不同(`describe-instances`,独立于本会话 transcript 的证据源);
  调用间隔 5s / 5s / 4s,均跨整秒。GH #98 的 `RUN_TOKEN` 本来就让唯一性不依赖秒分辨率。
- 配置:`--slots 16 --rec-slots 1 --hours 2`,`--games 12`,四台均
  terminate-on-shutdown + 2h 看门狗 + **一次性(非 persistent)spot 请求**。

### 4.3 `--rec-slots` 仍是 1 —— 第八波按登记分支 (A),**第九次点名**

本轮**直接读了 issue** 而不是引上一轮的结论:GH #308 仍 `open`,`updated_at 2026-08-30T06:24:56Z`,
5 条评论,**无总监裁定**。章程写明「不许事后改判据」⇒ `rec_slots` 保持 **1**。
**帧通道连续第八波 1/16**,owner P1/P2 的条件 (a) 继续等在这上面。

### 4.4 选种:窗口 `[2201,2600]` 未右移,但**天花板一波掉了两格(18 → 16)**

`seed_roster_index --build` 先跑(exit 0,第四波连续执行这条固定纪律),然后按 GH #313 的掩码穷举法枚举:

| | W30 选种时 | **本轮** | 变化 |
|---|---|---|---|
| 已用 / 未用 | 0 / 400 | **4 / 396** | W30 烧掉 4 粒 |
| popcount 5 | **2** | **0** | **−2(清零)** |
| popcount 4 | 8 | **6** | −2 |
| 互异掩码 | 50 | 47 | −3 |
| 合法四元组(≥2/term) | 5014 | **1209** | −76% |
| **最优载体格** | **18** | **16** | **−2** |

- **这是有记录以来单波跌幅最大的一次**:旧窗口 `[1801,2200]` 的三点趋势线是 **16 → 15 → 14,一波一格**;
  新窗口**一波两格**。原因在供给表上是直读的:**W30 拿走了该窗口仅有的两粒 pop-5 与两粒 pop-4**。
- ⇒ 章程点名的机制**第二次坐实,而且是在一个新窗口里、以两倍速率**:
  **一个新窗口的顶层在绝对数量上比每一刀的深度还薄**,所以右移买到的是**约两波的余量**,不是修复。
  **GH #285 第十四轮催**,该裁的仍是三选一:改自律(退回门自己的 ≥1)/ 改窗口策略(固定右移 or 滚动)/
  改选择规则(不总取最富的四粒)。
- **本波四粒**:`2375 / 2393 / 2403 / 2444`,`seed_roster_index --seed` 逐粒 **`no banked games`**
  (对**已刷新**的索引)⇒ 与历史任何一波零重叠,并池前提成立。

### 4.5 两道门(裸退出码,不经管道)

- **载体门**:`seed_draft.py --assert-carrier-from-arm "<47-id>" 2375 2393 2403 2444` ⇒ **BARE_EXIT=0**,
  `ids=10 seeds=4`。逐 term:`crystal_maiden 4 / lion 3 / skeleton_king 3 / obsidian_destroyer 2 /
  spirit_breaker 2 / zuus 2` = **16 个载体格**。
  **诚实边界(第九轮,一字不改)**:「每 term ≥2」是**本台自己的自律,不是门**;
  官方门只要求 ≥1(它报 `PARTIAL` 仍 exit 0)。
- **接线门**:`tools/batch_test/check_armed_wiring.py --cand "<47-id>" --ref 1dd5705f…` ⇒ **exit 0**,
  `all 47 armed ids wired on 1dd5705f…`。本轮**只跑一次**(树漂移守卫未触发,无需重钉重跑)。
- **波次记录门**:`tests/test_wave_gate_keys.py` 在本轮**真的拦了一次** —— 第一版 `W31_wave.json`
  漏写 `gates.iv_reclaim_blind`,**BARE_EXIT=1 / `13 checks, 1 failed` 点名该键**;补上后 **14 checks, 0 failed**。
  这正是 GH #350(现 **#355**)那条 schema 断言要买的东西,**本轮是它第一次真的挡住一次疏漏**。

### 4.6 局数(铁律 7 / owner 2026-08-19)

- **(a) 上一波(W30)最终有效局数**:`files_seen/games_loaded 231`、**scored 207**、`unfinished 0`、
  `source_dirs 4`(被抢占那台 0 个 analysis.json,**裸 0 写出,未悄悄丢**)、深度门**零排除**。
  逐粒(ab/ba/合计):`2204 36/16/52`、`2214 27/15/42`、`2286 37/20/57`、`2315 41/15/56`。
- **(b) 本波(W31)实时进度** @ `06:32:12Z`(起飞后 ~15 分钟,**暖场局不算有效局**):
  `2375` **8** / `2393` **2** / `2403` **0** / `2444` **2** 个 `analysis.json`。
  四台**全部已在产局或正在排空首局**。**预期**:4 粒 × 12 局 × 2 臂 = 96 局标称;
  近几波全集波实测落在 200–240 loaded(W30:231 loaded / 207 scored)。
- 预计自毁:若沿 W30 的 ~0.92 机时剖面,**07:12Z–07:20Z**;看门狗硬上限 **08:17Z**。

---

## 5. ⭐⭐⭐ 本轮最重要:铁律 10 从今晨起对每一个流打 `TRUNK RED`,而它**打得对**

### 5.1 现象

自检 python 腿唯一的红是 `tests/test_selfcheck_lua_leg.py`,单跑复现(**BARE_EXIT=1**),
`43 checks, 1 failures, 8 uncertified`。唯一的 FAIL 是:

```
FAIL  5a0: the leg finishes inside 120s -- it is a 开工 check, and a discovery set
           that outgrows the budget must fail here rather than hang
```

其余 **8 条 UNCERTIFIABLE 全是它的级联**(clean run 与 reddened run 都没在 120s 内跑完 ⇒ 5a/5a2/5b/5c/5d/5e/5f/5g **一条都没真跑**)。

### 5.2 归因(不是 bot 逻辑红,也不是测试坏了)

`BUDGET_S = 120` 与检查 `5a0` 由总监 **`074d3e9c`(2026-08-31T04:23:56Z)** 落地 ——
**晚于上一轮批测台自检(03:25Z,当时 65/0/0)**。⇒ **本轮是这条断言第一次被跑,它立刻就响了。**

本台实测了它断言的那个量(50 个 tagged 文件,逐个 `lua5.1 tests/run_tests.lua <basename>`,与 leg 同构):

```
TOTAL 133,340 ms over 50 files      (第二次独立计时:2m12.494s,逐位同阶)
```

**133.3 秒 > 120 秒预算 ⇒ 断言为真。** 而代价高度集中:

| 文件 | 耗时 | 落地 |
|---|---|---|
| `tests/test_blinkflee_scope_ruling.lua` | **39,468 ms** | `8785bdd0` 2026-08-29,tag `[ratchet]` |
| `tests/test_fieldsip_magnitude.lua` | **38,970 ms** | `351389eb` 2026-08-29,tag `[ratchet]` |
| 其余 48 个合计 | 54,902 ms | — |

⇒ **两个文件吃掉 78.4 秒(58.8%);去掉它们,整条腿 54.9 秒,预算内还剩一倍余量。**
第三名 `test_pullcad_throttled_duty` 只有 6.3 秒,**断层极大**。

**这正是 `routine_selfcheck.sh` 自己的 header 预言过的那件事**(逐字):
> LIMIT -- the runtime is a property of TODAY'S members, not of the tag. Four files in `tests/`
> take >25s just to LOAD (their top-level sweeps run at require time) … **None of them is tagged
> today. If one ever is, 开工 gets that cost silently and every trigger pays it.**
> … **If the count below stops being seconds, move the slow file's sweep behind a function
> instead of dropping it from the set.**

两个**新的** `[ratchet]` 文件在 08-29 走进了这个集合,**开工从那天起就在静静地付这笔钱**;
04:23Z 的预算断言只是**第一次把它变成可见的**。**修法 header 已经写好了:把顶层 sweep 挪到函数里,不要退集。**

### 5.3 为什么这条必须当轮交出去而不是记一笔

- 铁律 10 的自检是**每个流每次触发的第一件事**。从今晨起它对**所有五个流**返回 **exit 3 / `TRUNK RED`**,
  而这条红**与 bot 逻辑无关**。
- `routine_selfcheck.sh` 自己写过:「a false TRUNK RED is worse than a slow selfcheck —
  it is the thing that teaches people to ignore the line」。**本轮这条红不是假的**,
  但**一条与被测对象无关、且每轮必现的红,消耗的是同一份注意力预算**。
- 本台**不写 bot 代码也不改 harness**(章程边界),故只交读数与定位:**已开 GH #358**。

---

## 6. 泄漏检查(收尾)

`check_costs.sh --leak-only`(零成本,不查花费)。**本轮是发波轮,收尾要的不是「0 台在跑」而是「恰好 4 台且逐个对得上」**:

```
i-0e8b937752d64c739  c6i.4xlarge  06:17:33Z  dota2bot-soak-spot-1
i-0efb4f0359b92cb13  c6i.4xlarge  06:17:24Z  dota2bot-soak-spot-1
i-035ac2979fa03df7c  c6i.4xlarge  06:17:38Z  dota2bot-soak-spot-1
i-04ef0b9835397607f  c6i.4xlarge  06:17:28Z  dota2bot-soak-spot-1
```

**恰好 4 台,逐个 id 对上 §4.2 的起飞记录,零陌生实例 ⇒ 零泄漏。**
沿用 W29 那轮买到的纪律:**`--leak-only` 只报「多出来的」,「少一台」它不会举手**,所以这里是
**逐个 id 对起飞记录核对的**,不是看条数。常驻成本仍只有一个 AMI。

---

## 7. 交棒

1. **⭐⭐⭐ 总监 —— GH #358(本轮新开)**:铁律 10 的自检**从 04:23Z 起对每个流每次触发返回 exit 3**;
   断言是对的,腿实测 **133.3s / 50 文件**,**78.4s 集中在两个 08-29 落地的 `[ratchet]` 文件**
   (`test_blinkflee_scope_ruling` 39.5s、`test_fieldsip_magnitude` 39.0s);去掉两者 **54.9s**。
   修法 `routine_selfcheck.sh` 的 header 已写好(**把顶层 sweep 挪到函数里,不要退集**)。
   同一 issue 第 2 节附 `unlanded_commits.py` 的浅克隆等价 `OK`(§1.1)。
2. **⭐⭐⭐ 总监 —— GH #352(胜负通道横扫)仍是上一轮交出的最大一件**,本轮无新证据也未回退:
   **重裁前任何裁定不得再引 `winrate ≈ 0.5` 作为铁律 2 条件 (b) 的支撑**;§CO.4 里 W29 的
   `winrate 0.503` 那条支撑待撤回。**W31 收割时本台会照旧数赢家**(零额外支出),给拐点后的第三个点。
3. **⭐⭐ 总监 —— W30 的 promote 判断仍归你**(gpm +18.34 / comps 3-4 / 零排除 / `suggested: promote`),
   且条件 (b) 的胜负侧因 #352 空缺 —— 这一条**上一轮已交,本轮未见裁定,第二轮点名**。
4. **⭐⭐ 总监 —— GH #285 第十四轮催,趋势线现在是四个点且换了斜率**:
   旧窗口 16→15→14(一波一格),新窗口 **18→16(一波两格)**。右移只买到约两波余量。
5. **⭐⭐ 总监 —— 家族碎片化第四轮点名**:45-id **8 粒**(gpm +26.60、7/8,#329/#332 待裁)、
   44-id **4 粒**(W29,冻结)、47-id **4 粒 → W31 收割后 8 粒**。
   **47-id 这块本轮起在变厚**(这是三轮点名以来第一块),另两块按章程的「默认波次 = 全测试集」**永远冻结**,
   除非总监明确指定重钉某串。
6. **⭐ 总监 —— GH #308 第九次点名**:仍无裁定,`rec_slots` 第八波按登记分支 (A) = 1,帧通道 1/16;
   owner P1/P2 的条件 (a) 卡在这里。**#271 建议写成 `wave.json:gates` 必填键(第五轮催)** ——
   顺带报喜:**#355 那条 schema 断言本轮第一次真的挡住了一次疏漏**(§4.5)。
7. **⭐ 总监 —— GH #256 本波双验收通过**(4 AZ 互异、零 re-aiming、零降级),**建议连同 #252 一并关闭**;
   **#282 连续第十二波零 UNEXPLAINED,第十五次建议关闭**。
8. **⭐ 存量**:**#207 `zusstatic` 第三十七波 armed**;**#218 后续第三十三轮**;**#295 建议关闭**;
   **#329 / #321 待裁**;**`UNKNOWN STATUS` 4 条(#317)建议一次性归一到文档化词表**;
   #313/#290/#291/#298/#299/#349/#350(#355)照旧。
9. **⭐ 下一轮本台 = 收割 W31**(闸 (i) 解锁 **`2026-08-31T12:17:21Z`**;四台预计 07:12Z–07:20Z 自毁,
   硬上限 08:17Z ⇒ **多半是纯收割轮**)。收割时必做:回填 `machines[]` 的
   `status_code`/`create`/`update`/`ab`/`ba`/`arm_depth` **六个裸名**(#355);数赢家喂 #352;
   跑 `reclaim_blind --wave-json` 定下一波市场类型。

---

## 8. 铁律自查

- **铁律 6**:`bots/` / `game/` **一行未改**(本轮只写 `iterations/` 下的报告/章程/波次记录)。
  静态半 `bash tools/agent/luacheck_gate.sh` ⇒ 见 §8.1 的裸退出码。
  `core.hooksPath` 已由自检上膛。**未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行可抄**。
  动态半(`lua5.1 tests/run_tests.lua` 全量,~100min,GH #124)**未跑,且不声称**。
- **铁律 6 顺序条款(GH #290)**:本轮**先 push,再发表任何带引用的评论/issue**;
  GH #358 的草稿先过 `claim_precheck.sh`(容器已 `--unshallow`,故不会复现上一轮那个假 OFF-TRUNK)。
- **铁律 11**:MCP/工具**未触发** `requires approval`,无空转等待。
- **铁律 2 / 4**:本轮**未产出任何 armed/baseline 对比读数**(纯发波),故无四量/分层义务;
  §3 引用的 W30 读数一律指回 03:25Z 报告,未重算、未改写。
- **AWS 支出纪律**:本轮花费 = 一波 spot(围栏记 $0.90)+ 一次 CE 复核 $0.01;
  未跨任何告警档,刹车线未接近。

### 8.1 静态门读数

```
$ bash tools/agent/luacheck_gate.sh > gate.log 2>&1 ; echo "BARE_EXIT=$?"
BARE_EXIT=0
installed luacheck (it was missing; apt package lua-check, bounded)
luacheck bots game: 0 warnings
```

容器冷启缺 luacheck,**门自己把它装上了** ⇒ GH #205 要防的那条「没 luacheck 就跳过」本轮未发生。
退出码 **0 = 干净**(2 = 没跑成 / 3 = 有警告),**裸读,不经管道**。

## 9. 本轮 token 用量

`python3 tools/agent/token_usage.py` 最后一行,原样:

```
TOKENS total_in=8,513,929 out=48,661 turns=67
```

(到统计时刻为止;统计后的收尾回合不计入。)
