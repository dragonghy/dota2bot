# 批测台 2026-08-28T00:20Z — 挂了四轮的 n=8 预登记**执行完毕**:那个「−40」一路走到 −14.36,而**在 n=4 上它曾经越过 |t|=2**;pin 到此为止,W19 回 tip

**一句话**:W18-R 两台**全须全尾**(127 落盘 / 115 计分 / unfinished 0),**三波以来第一次一波买满两粒**,
`d9585a29` 池子到达预登记的 **n=8** ⇒ 主读数 **gpm −14.36,逐粒均值 SE ±14.01,|t| = 1.03**,
且**四个量的 ab 层与 ba 层全部反号**(n=6 时唯一同号的 `deaths` 在 n=8 上也翻了)——
按铁律 4(i) **一条都不写进结论**。同一把切法回看序列:**n=4 时 |t| = 2.07**(它当时越过了 |t|=2),
n=6 → 1.10,n=8 → 1.03,**效应量 −40.22 → −20.28 → −14.36 单调朝零走**。
预登记问题既已答完,pin 的理由(上一轮原话「缺的是种子不是新树」)就用尽了 ⇒
**W19 不再 pin,回 `origin/main` tip `3110f323`**(四轮未测的天赋定价 / `abil1st` / `aimguard` 一并进语料)。

---

## 1. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` — **worst exit 3(非阻断)**:

- `stable-v1` / `stable-v2` 两锚点 **ok**(EXISTS / PINNED / SHIPPED 三项全过);
- unlanded commits:**OK**(certifiable window 内无未落地工作;shallow clone,426/472 refs 不可证,照旧);
- **cadence 1 处洞:`replay-check` 08-27T07:19Z → 13:28Z = 6.1h** —— **同一处、同一值,连续第五轮**。**不是本台的。**
- 未裁队列:RIDESHARE **none**、OTHER **none**(上一轮的 `hero-21` 已被总监裁掉),open requests 37;
- trunk **45 passed / 0 failed / 0 uncertifiable**(上一轮 44,**+1**);快 Lua 检测器 **15 个文件 0 失败**(上一轮 13,**+2**)。
  ⇒ 上一轮交棒项④「trunk 两条红」**保持全绿**。

---

## 2. 成本(铁律 1 / 章程 §2)

| 项 | 值 |
|---|---|
| `budgets` MTD | **$50.671** |
| CE 复核($0.01,脚本在 ≥$35 时自动做) | **$50.6709276212** — **逐位一致,连续第十二轮** |
| budgets 快照时刻 | ⚠️ **`2026-08-26T23:46:06Z`** —— **与上三轮同一张、逐位未动** |
| forecast / limit | 76.999 / 100.0 |
| 刹车线 $90 | 未接近 |
| 开工 running/pending | **空**(W18-R 两台已自终) |

**⚠️ 滞后照旧朝危险侧失效(连续第四轮同一形状)**:快照时刻不动 ⇒ **W15-R / W16 / W16-R / W17 / W17-R / W18 / W18-R 一台都没进这个 MTD**。
围栏算术因此**不用裸 MTD**(章程 §2 甲):

```
围栏 = 50.671 + W15-R 1.69 + W16 0.80 + W16-R 0.190 + W17 0.479 + W17-R 0.313
                + W18 实测 0.613 + W18-R 实测 1.25
     = $56.01
再加 W19:  spot ~$0.96  ⇒ $56.97   ≤ $60  ⇒ 闸 (iii) 过
           若全程降级按需 ~$2.49 ⇒ $58.50 ≤ $60  ⇒ 仍过
```

**两种市场类型下 (iii) 都过**,所以本轮**不需要**为市场类型另立分支。
**本轮 budgets 读数逐位未动 ⇒ 无新增跨档**($50 档的书面解释已在 08-27T15:15Z 报告 §2 立卷,不重复计次)。

**⚠️ 要交给下一轮的算术**:发完 W19 后围栏值 **$56.97–58.50**。**再发一波(spot ~$0.96)= $57.9–59.5,勉强在线内;
两波必然破 $60**。也就是说 **(iii) 这道闸最迟在下下轮开始咬人**,而刹车线是 $90、owner 批准档是 $100 ——
**闸 (iii) 的 $60 会先于任何真实预算风险停掉例行波次**。这不是本台能改的数(总监 08-25 从 $45 抬到 $60),
**本轮把它作为可预期事件登记,交棒 ⑤ 请总监提前裁**,免得又变成「静默停摆」。

---

## 3. ⭐ 主产出之一:收割 W18-R —— 三波以来第一次**一波买满**

### 3.1 前置动作:实例终态

`describe-instances --instance-ids i-04a0789046bd97674 i-05098c54c2b779dfd` ⇒ **`[]`**。
两台 21:18Z 起飞、约 22:12Z 收尾,到本轮开工已超过约 1 小时的保留窗 ⇒ **从 `describe-instances` 里消失**。
**这正是上一轮预告的那条**:「本轮 W18-R 是按需,没有 SIR ⇒ 下轮这条证据通道不存在」。**兑现,照预告改用落盘局数判断。**

⚠️ **同时把上一轮那条作证边界再钉一次**:`[]` **不是**「没有实例」的证据,是「查不到了」——
`--leak-only` 的空输出与此同族,**它不报错,它返回空,而空会被读成没有**。

### 3.2 收割读数(`--cand-ref` 未传,`contrast=vs_stable`)

两个 run 各自独立子目录再指向父目录(GH #225 的 R2 保护);**W18 的 `f43908` / `e17d24` 孤儿目录按上一轮预登记未放入**:

```
recover_verdict.py ./w18r "<40 id / 354 字节>"
  input: files_seen 127 / games_loaded 127 / source_dirs 2 / unparseable 0 / pooled_overridden 0
  scored 115 / unfinished 0
```

| 种子 | ab | ba | gpm | xpm | deaths | last_hits | winrate | scored |
|---|---|---|---|---|---|---|---|---|
| 921 | 41 | 16 | +0.58 | +3.29 | 0.00 | +1.06 | 0.512 | 57 |
| 926 | 41 | 17 | +6.22 | −7.30 | +0.15 | −1.04 | 0.500 | 58 |
| **单波均值** | | | **+3.40** | **−2.00** | **+0.07** | **+0.01** | **0.506** | **115** |

`comps_better` gpm 2/2、xpm 1/2、deaths 0/2、last_hits 1/2;`suggested hold_or_reject`。
**两粒都完整(ab 与 ba 两腿都有),这是 W16 之后第一次。**

### 3.3 单波成本(S3 时间戳法;实例已查不到)

| run | 起飞 | 末个 S3 对象 | 跨度 |
|---|---|---|---|
| `…_109abf`(921) | 21:18:47Z | 22:11:55Z | 0.886 h |
| `…_4aa0a5`(926) | 21:18:51Z | 22:12:40Z | 0.897 h |

按需 $0.673/h + IPv4 $0.005 = **$0.678/h**,末个对象后再留 ~2 min 关机 ⇒ 机时 **1.849 h** ⇒ **$1.25**。
上一轮预估 $1.010 ⇒ **实测是预估的 124%**。
**诚实边界**:这是**时间戳法**不是账单法,末对象到真正终止之间的那一段是**估的**;
`describe-instances` 已查不到,SIR 对按需不存在,**这一波没有第二个独立通道**。

### 3.4 ⭐ 一个该进章程的费率更正

章程写死的按需实测机时是 **0.745 h/台**(W10–W14)。但那五波里**混着被回收的台**。
**只看跑完自终的台**:W18 的 918 = **0.909 h**、924 = **0.920 h**(上一轮 SIR 表),W18-R 两台 ≈ **0.89 / 0.90 h**
⇒ **跑满一台是 ~0.90 h,不是 0.745 h**。
**0.745 是「含回收台」的均值,拿它给一波全须全尾的波做围栏预估会系统性低估约 20%** ——
方向与「滞后让 MTD 偏低」**同向**,即**朝危险侧**。本轮围栏算术已改用 **0.92 h/台**估 W19。
**这条与已作废的 $3.05 外推是镜像**:那次是高估 52%(朝安全侧,静静少发波),这次是低估 ~20%(朝危险侧)。

---

## 4. ⭐⭐ 主产出之二:挂了四轮的 n=8 预登记,**本轮执行完毕**

池 = W16 三粒(888/895/906)+ W16-R 一粒(896)+ W18 两粒(918/924)+ **W18-R 两粒(921/926)**,
每 run 独立子目录再指向父目录 ⇒ `source_dirs 8`、`pooled_overridden 0`、
`files_seen 464 / games_loaded 464 / unparseable 0`、`scored 417`、`unfinished 0`、`suggested hold_or_reject`。
⛔ **不可与 W15-R 池化**(那是 37 id / `79f32c92` 树);⛔ **孤儿目录一律未放入**。

### 4.1 逐粒(主读数)+ 两层分层(铁律 4(i) 要求的形状)

| 种子 | ab/ba | gpm(ab 层 / ba 层 / 折中) |
|---|---|---|
| 888 | 30/12 | −203.81 / +135.27 / **−34.27** |
| 895 | 39/19 | +83.87 / −70.43 / **+6.72** |
| 896 | 29/13 | +159.16 / −250.38 / **−45.61** |
| 906 | 41/17 | +16.95 / −192.39 / **−87.72** |
| 918 | 35/18 | −176.23 / +174.33 / **−0.95** |
| **921** | 41/16 | +121.54 / −120.38 / **+0.58** |
| 924 | 34/15 | −127.48 / +207.73 / **+40.13** |
| **926** | 41/17 | −116.92 / +129.35 / **+6.22** |

| 量 | n=8 均值 | 逐粒均值 SE | ab 层 | ba 层 | 两层同号? |
|---|---|---|---|---|---|
| **gpm** | **−14.36** | **±14.01** | −30.37 | **+1.64** | ⛔ **反号** |
| xpm | −4.03 | ±8.42 | +5.66 | −13.71 | ⛔ **反号** |
| **deaths** | **+0.10** | ±0.12 | **−0.04** | **+0.24** | ⛔ **反号(n=6 时是同号)** |
| last_hits | −0.51 | ±0.88 | +0.13 | −1.15 | ⛔ **反号** |

`comps_better`:gpm **4/8** / xpm 4/8 / deaths 3/8 / last_hits 2/8 / winrate 4/8;winrate 均值 **0.503**。

### 4.2 结论(逐条按预登记的判据写)

1. **主读数与零不可分**:gpm −14.36,|t| = **1.03**。
2. **铁律 4(i):四个量全部两层反号 ⇒ 全部读作噪声,一条都不写进结论。**
   **n=6 时唯一同号的 `deaths`(+0.01 / +0.20)在 n=8 上翻成 −0.04 / +0.24。**
   即**上一轮那条「只有 deaths 同号」的观察,是 6 粒的抽样位置,不是效应。**
3. **⭐ 同一把切法(逐粒均值 SE)的三点序列 —— 这是本轮最该被记住的一行**:

   | n | 均值 gpm | SD | SE | \|t\| | 要 \|t\|=2 需要的 n |
   |---|---|---|---|---|---|
   | 4 | −40.22 | 38.83 | ±19.42 | **2.07** | 3.7 |
   | 6 | −20.28 | 45.03 | ±18.38 | 1.10 | 19.7 |
   | **8** | **−14.36** | **39.63** | **±14.01** | **1.03** | **30.5** |

   **在 n=4 上这个读数越过了 |t|=2。** 如果当时按「越过就当效应」处置,买到的是一个
   **加四粒后掉到 |t|=1.03、效应量缩掉 64%** 的东西。**这是本项目自己语料上的一个 |t|=2 假阳性实例**,
   与铁律 2「小改动不做显著性检验」是**同向的证据**,值得存档。
4. **判别力要求恶化**:n ≳ 20(上一轮登记)→ **n ≳ 31**。按每波 4 粒算,还要 **~6 波**。

### 4.3 ⚠️ 一条自查:上一轮的预登记里有一个**没登记切法**的数

18:25Z 那一轮登记的预测原文是「n 翻倍 ⇒ SE 应从 **±77** 降到 **±55**」。
但**同一把切法(逐粒均值 SE)在 n=4 上算出来是 ±19.42**,不是 ±77 —— 也就是说
**±77 出自另一种切法,而那种切法没被登记下来**,于是这条预测**今天无法核验**。
**铁律 4(iii)(「登记过的效应量,连用哪种切法得到的一起登记」)在本台自己的登记上被违反了一次。**
本轮起,本台登记的每个效应量都写明切法(§4.1 表头已照做:「逐粒均值 SE」)。
**这条是自查不是甩锅**:它正是 4(iii) 立法时预言的那个形状。

---

## 5. W19 发波

### 5.1 三条闸

- **(i) 6h 例行节流:过。** 例行锚点 = W18 首台 **08-27T18:20:00Z** ⇒ 解锁 **08-28T00:20:00Z**;
  `date -u` 实读(不写死 epoch),本波在解锁后发出。
- **(ii) 有新东西可测:过,而且这是本轮的主动作。** 见 §5.2。
- **(iii) 成本:$56.97(spot)/ $58.50(全按需)≤ $60 ✅**(§2)。

**§4a 无适用对象**(逐条扫过 queue.json 45 条 / pending 22 条):
唯一带 `APPROVED*` 的仍是 `strategy-5b`(`director.wave` = **W6**,早已过去),
其余是 `ROUTED_ARCHIVE_SCAN`(零 EC2)/ `RECEIVED_NOT_SCHEDULED` / `DEFERRED_PENDING_W3_RESOLVE` / `REJECTED`。
**没有任何一条在等本台发波。** ⇒ 走 §4b 例行波。

### 5.2 ⭐ 本轮的判断:**pin 到此为止**

上一轮 pin `d9585a29` 的理由是逐字的一句:**「缺的是种子不是新树」**。
**那句话今天不成立了** —— 预登记的 n=8 **本轮执行完毕并给出了结论**(§4)。
现在缺的**不是种子**,是**总监对「要不要按每波 4 粒继续买到 n ≳ 31」的裁定**(交棒 ④,已挂两轮)。
在没有裁定的情况下,本台的保守默认**不是**第五轮单方面续 pin,而是**章程 §4b 的例行规则:测当前 tree**。

三条支撑:
1. **续 pin 的代价在累积**:`d9585a29` 之后落地的天赋定价(CM t20/t25、Zeus t20/t25、Axe t20)、
   `abil1st`、`aimguard`、四处 getter 的 override 修正 —— **连续四轮记账为「本波测不到」**,再 pin 就是第五轮。
2. **池子不会被销毁**:8 粒的逐局数据全在 S3,总监若裁「继续买到 n≳31」,**随时可以接着买接着池化**。
   本轮做的是**不再单方面替总监先买**。
3. **未 gate 的 commit 是两腿同时动的**:天赋定价这类改动同时进入 candidate 腿与 stable 腿,
   镜像对比仍是 cand−stable;**pin 老树等于两腿一起跑四轮前的代码**。

### 5.3 两道门(发波前)

| 门 | 调用 | 结果 |
|---|---|---|
| 接线门 | `check_armed_wiring.py --cand <40串> --ref 3110f323…` | **exit 0** `all 40 armed ids wired on 3110f323…` |
| 载体门 | `seed_draft.py 928 930 932 935 --assert-carrier 'crystal_maiden,zuus,lion,obsidian_destroyer,skeleton_king'` | **exit 0** `CARRIER_GATE terms=5 seeds=4 exit=0` |

**载体门本轮回到 exit 0**(上一轮是本台第一次在 exit 1 上放行,那是补跑波被动继承的剖面)。
逐项覆盖(**PARTIAL 是常态,要写出来**):`skeleton_king` **3/4**(928/930/935)、
`zuus` **2/4**(928/932)、`lion` **2/4**(930/932)、`crystal_maiden` **2/4**(928 pos4 / 935 pos5)、
`obsidian_destroyer` **1/4**(930)。**焦点五全部有载体**,SK 供给从 2/8 的池比一次提到 3/4。
角色门 / 等级门 id 的载体门仍 **no-op**(缺口 **GH #140**,**第二十三轮**)。

### 5.4 树核对(章程 §5 第一句)

`git ls-remote origin main` = **`3110f323ead440674cef721a4797352366516d29`**,与本地 HEAD **逐位相同**;
`--ref` **显式钉这个 40 位 sha**(不写 `main`),所以本轮收尾提交报告**不会**改变实例克隆到的树。

### 5.5 发波记录

| 项 | 值 |
|---|---|
| `--dry-run` 先行 | ✅ `ref` / `slots=16` / `rec_slots=12` / `watchdog=2h` / `us-west-2` / `az ring` 全对上,**波次预算闸未拒发** |
| 拓扑 | **4 台 × 1 种子**(928 / 930 / 932 / 935),`--games 12`,`--slots 16`,`--rec-slots 12`(GH #75 **第十五轮维持 12**) |
| 未传 `--cand-ref` | ✅ 单臂 `contrast=vs_stable`,**收割时也不要传** |
| 未传 `--on-demand` | ✅ **默认 spot**(owner GH #158「优先用 spot」);**四台全部 `InstanceLifecycle=spot`**,已 `describe-instances` 核实 |
| `--az` | **四次调用四个不同**(2a / 2b / 2c / 2d),按 GH #252 |
| launch | 00:20:03Z / 00:20:11Z / 00:20:32Z / 00:20:39Z(间隔 8s / 21s / 7s,均跨整秒) |
| 单波预估 | **$0.96** = 4 × **0.92 h**(§3.4 更正后的跑满机时)× ($0.2557 spot + $0.005 IPv4) |

| 种子 | 请求 AZ | 实际 AZ | 实例 | run 前缀 | 生命周期 |
|---|---|---|---|---|---|
| 928 | 2a | **2a** | `i-07709a6af60c7999b` | `…_002003_…_1a45f5` | spot |
| 930 | 2b | ⚠️ **2c**(环内改投) | `i-04eb4eff2ae392a33` | `…_002011_…_6c47b6` | spot |
| 932 | 2c | **2c** | `i-08ad359a2e51651f0` | `…_002032_…_5fcfc2` | spot |
| 935 | 2d | **2d** | `i-062d5203b12f6a556` | `…_002039_…_db92df` | spot |

**降级阶梯:第 1 级即成功,未用到第 2/3 级。** 但**环内改投触发了一次**,原文:

```
! dota2bot-soak-spot-1: launch in us-west-2b failed -- InsufficientInstanceCapacity ...
  There is no Spot capacity available that matches your request.
! dota2bot-soak-spot-1: re-aiming inside the ring -> us-west-2c
```

### 5.6 ⚠️ **#256 的代价第二次被计量:这次是 1 个 AZ**

环从**请求 AZ 的下一个**开始走(2b → 2c),与 #256 裁定逐字一致 ✅。
**但后果是 #252 的分散被削掉一格**:四次调用点了四个不同 AZ,**落地只有 3 个不同**
(2a / **2c** / **2c** / 2d,两台并置在 2c)。

- **上一轮**:#256 的回退把 921 送回 2b,代价 = **1 粒种子**;
- **本轮**:#256 的环内改投把 930 送进 2c,代价 = **1 个 AZ 的分散度**
  ⇒ 若 2c 发生一次容量事件,**一次事件会带走 2 粒而不是 1 粒**,`#252` 的「一次事件最多损失 1 粒」上界**本波不成立**。

**这是同一个机制第二次在账单/语料侧留下可计量的痕迹,两次方向一致:环内改投买到了「不丢实例」,
卖掉的是「实例落在哪」。** 本台**不自改机制**(那是 #256 的裁定),只把第二个数据点交上去(交棒 ⑥)。
**诚实边界**:代价目前是**风险敞口**不是已实现损失 —— 2c 这一波若不出事,它就是零。

---

## 6. 局数(铁律 §7)

- **(a) 上一波次定案 —— W18-R**:**127 落盘 / 115 计分 / unfinished 0 / 暖场 12**,**有效种子 2 粒**,**孤儿 0 局**。
  上一轮预期 ~104 落盘 / ~90 计分 ⇒ **落盘 +22%、计分 +28%**(两台都跑满,且按需机时比预估长)。
- **`d9585a29` 树池子(封盘)**:**464 落盘 / 417 计分 / 8 粒**(上一轮 337/302/6 ⇒ **+127 / +115 / +2 粒**)。
  ba 占比(计分)= 132/417 = **0.317**(W11–W18 区间 0.304–0.325,**在带内**)。
- ⛔ **孤儿存量**:W15 30 + W16(896)27 + W17 128 + W17-R 64 + W18 32 = **281 局单腿孤儿**,至今未并进任何池。
- **(b) W19 在跑**:4 台 × 1 种子 × `--games 12` × 2 腿,预期 **~230 落盘 / ~205 计分**(按 W18-R 每台 63.5/57.5 外推)。
  **诚实边界**:spot 的中断风险让这只是**期望值不是下界**;W17/W17-R 两波各 0 粒就是这条边界的实例。

---

## 7. 泄漏检查(两次)

- **开工**:`check_costs.sh` 的 running/pending 段 **空** ⇒ W18-R 两台已自终,**无遗留**。
- **收尾**:`check_costs.sh --leak-only` **恰 4 台**(`i-07709a6af60c7999b` / `i-04eb4eff2ae392a33` /
  `i-08ad359a2e51651f0` / `i-062d5203b12f6a556`),**逐个对得上 §5.5 的发波表,全是本轮自己发的,全 `running`**。
  常驻只剩 AMI `ami-0a990a26d89c66547` + 快照 —— **不是泄漏**。
- ⚠️ **作证边界(第三轮保留)**:`--leak-only` **不能**证明「过去这一轮没漏过」——
  终止实例约 1h 后从 `describe-instances` 消失,**它不报错,它返回空,而空会被读成「没有实例」**。
  本轮 §3.1 的 `[]` 就是同一条边界的又一次实地兑现。

---

## 8. 铁律 6

- **静态半**:`bash tools/agent/luacheck_gate.sh` → **exit 0**,`luacheck bots game: 0 warnings`
  (容器缺 luacheck,脚本自装 apt 包 **`lua-check`**)。`core.hooksPath` 已由开工自检上膛,
  **未使用 `RULE6_BYPASS`**。
- **动态半**(`lua5.1 tests/run_tests.lua`,~100 min,GH #124):**未跑,且不声称跑过**。
  本轮 **`bots/` / `game/` 一行未改**(改动只有:本报告 + 章程「当前状态」节 + GH #204/#256 两条评论)。

---

## 交棒

1. **⭐ 下一轮本台 —— 收割 W19**:四个 run 前缀见 §5.5,**每 run 独立子目录再指向父目录**(GH #225),
   **不传 `--cand-ref`**(单臂 `vs_stable`),arm 串反解应得 **40 id / 354 字节**。
   ⚠️ **W19 在新树 `3110f323` 上,不可与 `d9585a29` 的 8 粒池化** —— 这是新的一串,从 n=4 起算。
2. **6h 闸锚点**:W19 首台见 §5.5 ⇒ **W20 解锁 = 首台时刻 + 6h**。**先 `date -u` 再比,别写死 epoch。**
3. **⭐⭐ 总监 —— GH #204(第十三轮,本轮有决定性证据)**:n=8 执行完毕。
   (a) 主读数 |t|=1.03,四量两层全反号;(b) **n=4 上曾越过 |t|=2 的假阳性实例**(§4.2 第 3 条表);
   (c) 判别力要求 n ≳ 31,按每波 4 粒还要 ~6 波(spot 约 $6)。
   **请裁:这个 n 还买不买;若买,是否值得为它继续 pin 一棵越来越旧的树。**
4. **⭐ 总监 —— 闸 (iii) 的 $60 最迟下下轮咬人**(§2 末尾算术)。刹车 $90 / owner 档 $100,
   **$60 会先于任何真实预算风险停掉例行波次**。请提前裁,别又变成静默停摆。
5. **⭐ 总监 —— 章程费率更正请复核**(§3.4):`0.745 h/台` 含回收台,**跑满一台实测 ~0.90 h**,
   低估约 20% 且**朝危险侧**。本轮围栏已改用 0.92 h/台。
6. **⭐ 总监 —— GH #256 的代价第二个数据点**(§5.6):环内改投把 930 从 2b 送进 2c ⇒
   **四次调用四个 AZ,落地只有 3 个不同**,`#252` 的「一次事件最多丢 1 粒」上界本波不成立。
   上一轮代价 = 1 粒种子,本轮代价 = 1 个 AZ 的分散度。**已在 #204 之外单独追评到 #256。**
   请连同交棒 ③ 一并裁「环内改投要不要在 `--az` 被显式点名时改成失败而非改投」。
7. **⭐ 总监 —— 本台自查一条 4(iii) 违规**(§4.3):`±77` 的切法未登记,预测无法核验。
8. 总监 —— **#207(第十三轮)** `zusstatic` **连续第十二波空转**;
9. 总监 —— **#218 后续** `salveally`/`salveyield` **连续第八轮不在第 2 行**;
10. 总监 —— **#233(a)** 未裁(新数据点:W18-R 实测 $1.25 vs 预估 $1.010 = 偏离 1.24 倍,**方向与 W18 的 3.7 倍相反**);
11. 录像组 —— `abilanc` (a) 取证:**W18-R 的 127 局 `.dem` 两腿全可配对**(比 W18 的 102/154 更好用);
    W18 154(102 可配对)+ W17 128 + W17-R 64 单腿孤儿仍可逐帧;**W14 的 156 份约 09-16 过期,优先消化**;
12. replay-check 组 —— 报告节奏 **6.1h 洞**(**连续第五轮**,同一处同一值);
13. 存量催办:#217(第十二轮)/ #211(第十三轮)/ #225 / #180(第十九轮)/ #171(第十七轮)/
    #200(第十六轮)/ #181 / 载体门 PARTIAL(第二十三轮)/ `stable-v*` tag(第二十一轮)/
    `campdanger`(第十六轮)/ §BL.4 机械化 / #75(第十五轮,维持 12)。

---

## Token 用量(铁律 8)

```
TOKENS total_in=6,669,502 out=42,986 turns=59
```
(统计时刻之后的收尾回合不计入。)
