# batch-desk 2026-09-10T06:18–06:5xZ(闸轮:三道闸绿、一道闸**永久**打不开)

**⭐⭐⭐ 头号产出:上一轮宣布的「九月没有下一波了」本轮自己到期了 —— 围栏重新打开
($0.292 → $2.827 headroom),而波仍然没发,挡住它的换成了闸 (iv):
`reclaim_blind.py` 对 W62 打 `exit 2 UNDECIDABLE`,索要一个本账号里
**两个一手源都已经不存在**的字段 —— 而那个字段**按该工具自己的文档改变不了它自己的答案**。**

**⭐⭐ 第二产出:GH #661 的修复把一对「互相掩护的缺陷」换成了一个死锁。**
修之前:按需波的终止码读不到 + 分支接受任何字符串 ⇒ **静默假通过**。
修之后:分支只接受白名单 ⇒ 按需波**永远无法通过**,因为白名单里的值在闸跑的时刻**已经不存在**。
**失效方向从「假通过」翻成「结构性拒绝」——两个都不是「读到了」。**

---

## 一、自检(铁律 10)

⚠️ **本轮第一条命令又把 stdout 接了管道**,工具当场自拒,逐字:

```
REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.
it has recurred 5x, every time as the first command of the round.
SELFCHECK_EXIT=2  REFUSED (nothing was checked; this is NOT a pass)
```

—— 上一轮把自己记成第 7 次,**本台本轮是第 8 次,又是当轮第一条命令**。
拒绝门**有效**(没让 reader 的 `0` 冒充通过 —— harness 那一行确实报了 `SELFCHECK_EXIT=0`,
那是 `tail` 的),**但连续八轮拦不住这个动作本身**。改重定向 + 后台后重跑。

⚠️ **收尾时 Lua 检测器腿仍未跑完**(整份自检在本容器 >30min,与前几轮一致)。
python 腿本轮取到的是 **`UNCERTIFIABLE`**,逐字:

```
UNCERTIFIABLE -- 9 check(s) did not run; this is NOT a pass.
UNCERTIFIABLE -- a python test did NOT run (could not read its input). This line is NOT a pass,
  and it is NOT evidence that trunk is red: re-run on a quiet tree (nothing writing under bots/).
```

⇒ **本轮不声称 trunk 干净,也不声称 trunk 红。** 那一侧本轮没人看完。
(上一轮 `SELFCHECK_EXIT=3` 抓到的 `test_detector_source_constants.py:508` 那条红是否仍在,
本轮**无读数**,不继承也不销案。)

---

## 二、成本与闸 (iii):⭐ 上一轮的「九月已封盘」自己到期了

`AWS_SETUP_EXIT=0`(`arn:aws:iam::581436534924:user/dota2bot-agent`);`COST_EXIT=0`。

MTD **`$75.023`**(免费 `budgets`,**`refreshed 2026-09-10T06:09:49Z`**),≥$35 ⇒ 自动 `$0.01`
CE 复核 **`75.0233630746`**,两源一致。`forecast $152.875`。

### 为什么围栏重新打开了(这是本轮能发波的那一半)

上一轮(03:11Z)的快照戳是 `2026-09-09T20:23:37Z`,MTD `$74.308`,
pending 记 **W62+W61+W60 = `$5.400`** ⇒ projected `$79.708` ⇒ **`exit 3` THROTTLED**,
并据此写下「九月在 `$80` 围栏下已经没有下一波了」。

**本轮快照前进了 9.77h**(`20:23:37Z` → `06:09:49Z`)。按 11.3h 滞后带的保守边缘现算:

```
budget snapshot  : 2026-09-10T06:09:49+00:00
honest cutoff    : 2026-09-09T18:51:49+00:00   (snapshot - 11.3h)
```

| 波 | `machines[].launched_at` 末台 | 相对诚实 cutoff | 判定 |
|---|---|---|---|
| W60 | `2026-09-09T09:23:33Z` | 早 **9.5h** | 带外 ⇒ **已入账** |
| W61 | `2026-09-09T15:24:29Z` | 早 **3.5h** | 带外 ⇒ **已入账** |
| W62 | `2026-09-09T21:24:41Z` | **晚 2.5h** | 带内 ⇒ **计 pending** |

⇒ 诚实 `pending = W62 单波 = $2.150`(按需名义价)。**W60/W61 那 `$3.250` 出带即释放。**

⭐ **本轮 #692 的降级时钟没有翻转裁决,而这句话是核过的不是假设的**:
工具的降级 cutoff `2026-09-09T19:03:17Z` 与本台的诚实 cutoff `18:51:49Z`
**都落在 W61(15:24:29Z)与 W62(21:24:41Z) 之间** ⇒ 两个时钟点名的波集**逐字相同**。
上一轮咬人的那个翻转本轮**按算术不成立**,**但工具仍然是坏的**(#692 未落地)。

⛔ **本台不使用「MTD 增量对得上波费 ⇒ 该波已入账」那个手法**(章程步骤 2 (乙) 明令禁用)。
上面的判定**只用滞后带**,不用 `$74.308 → $75.023` 那 `$0.715` 的增量去反推是哪几笔。

### 闸 (iii) 两种市场类型各跑一次,**都 CLEAR**

```
FENCE_SPOT_EXIT=0   --planned 1.10 --pending 2.150
  actual (MTD) $75.023 / pending $2.150 / planned $1.100 / projected $78.273
  fence $80.00 / brake $90.00 / operative ceiling $80.00 / headroom $1.727 after this wave
  WAVE_FENCE: CLEAR (exit 0)

FENCE_OD_EXIT=0     --planned 2.15 --pending 2.150
  projected $79.323 / headroom $0.677 after this wave
  WAVE_FENCE: CLEAR (exit 0)
```

RULING 6 要求原样抄的三行:

```
wave accrual     : records after 2026-09-09T19:03:17Z (= 2026-09-10T06:21:17Z - 11.3h lag, clock from now (budget LastUpdatedTime unreadable))
  un-accrued?    : W62 last machine up 2026-09-09T21:24:41Z  <- inside the lag window, so its cost may not be in MTD above
  must cover     : 1 wave(s) listed above (W62). This tool does not price them; check your sum covers each.
  zero qualified : the wave half above ran on now (budget LastUpdatedTime unreadable), so this zero is not an accrual-complete one (Ruling 6).
WAVE_FENCE CLOCK : the wave-accrual window above was anchored to now (budget LastUpdatedTime unreadable), which is LATER than the budget snapshot and therefore a NARROWER window. Quote this line in the round's report.
```

⚠️ **`headroom $1.727` / `$0.677` 是本轮这一刻的数,不许抄进下一轮** —— 围栏是时间的函数。
⛔ GH #515/#528 立案理由一字未变,工具第一行仍是
`budget filters : none <- so every instance in the account does land in this budget, whoever owns it`。

---

## 三、闸 (i) 节流:UNLOCKED

`THROTTLE_EXIT=0`,**读工具打的那一行不自己算**:

```
anchor wave      : W62 (4 machine(s))
last machine up  : 2026-09-09T21:24:41Z  <- the anchor (GH #469 ruling 1)
unlock           : 2026-09-10T03:24:41Z
now              : 2026-09-10T06:18:43Z
margin           : +10442s past unlock
WAVE_THROTTLE: UNLOCKED (exit 0)
```

---

## 四、收割:无对象(查过的,不是默认的)

`awsx s3 ls s3://dota2bot-batch-results-4924/soak/` 最新四个前缀是
`spot_20260909_2124{32,34,36,39}_1_a91efb69…` = **W62 那四台**,00:14Z 那轮已 4/4 收完
(`W62_verdict.json`:`scored 203 局`、`seeds_scored 4/4`)。**本轮无新语料。**

---

## 五、⭐⭐⭐ 闸 (iv):`exit 2`,而这一次它**修不好**

```
RECLAIM_BLIND_EXIT=2
=== reclaim-blind check (GH #271 ruling) -- wave W62 ===
changeover 40.0 min  (measured bracket 34.80 < x <= 40.63, margin above the constant 0.63 min)  min_arm_depth 8.0
UNDECIDABLE: seed 10601: market is on-demand and status_code is missing -- an on-demand row still has to say how the machine ended (EC2 StateReason.Code, e.g. Client.UserInitiatedShutdown)
```

### 五之一、先修能修的那一半:W62 的收割义务上一轮掉了

`W62_wave.json:machines[]` 的 **`ab`/`ba`/`arm_depth`/`status_code` 全是 null** ——
而 00:14Z 那一轮**确实**把四粒全收了并写进了 `W62_verdict.json`。
⇒ **收割数据落在 verdict 里,没有回填进波次记录**,而波次记录正是闸 (iv) 读的那份。
(章程收割义务逐字:「`machines[]` 的 `ab`/`ba`/`arm_depth`/`status_code` **不许留 null**」。)

本轮从 `W62_verdict.json:per_seed[]` 回填,附 `_backfill_source` 注明出处:

| seed | ab | ba | arm_depth |
|---|---|---|---|
| 10601 | 42 | 16 | 23.17 |
| 10607 | 31 | 14 | 19.29 |
| 10803 | 34 | 24 | 28.14 |
| 10813 | 30 | 12 | 17.14 |

### 五之二、修不了的那一半:两个一手源本轮都读过,都没有

⛔ **本台不猜一个终止码去让闸过。** 两个源逐个读:

```
awsx ec2 describe-instances --instance-ids i-046fe13456914381a i-08fd4fef0e94a0993 \
     i-0dd115a91af3cf747 i-0a951b0024605351d
  -> EC2_EXIT=0   []          # 空:终止约 8h 后,实例已从 describe-instances 老化掉(~1h,GH #375)

awsx cloudtrail lookup-events --lookup-attributes ...
  -> CT_EXIT=255  AccessDeniedException: User .../dota2bot-agent is not authorized to
     perform: cloudtrail:LookupEvents because no identity-based policy allows the action
```

⇒ **W62 的 `status_code` 在本账号权限下永久不可恢复。**
波次记录里因此写的是 `status_code: null` **加一个 `status_code_unrecoverable` 块**
(记下上面两条读数 + 一句 `do_not_fill`),**让后来的轮次能把「读不到」与「还没做」分开** ——
留一个裸 null 会被下一轮读成「上一轮偷懒」,而它其实是「证据不存在了」。

回填后**再跑一次,仍然 `exit 2`,同一句话** ⇒ 缺的确实只有那个不可恢复的字段。

### 五之三、⭐⭐⭐ 立案的形状:它索要的字段**改变不了它自己的答案**

按 `reclaim_blind.py` **自己的文档和自己的代码**:

- BLINDED 需要**两个 clause 同时成立**(`:28-33`):(1) 该波配对种子 **≤ 1**;
  (2) 至少一台在 changeover 之前被 **`instance-terminated-no-capacity`** 回收。
- clause (2) 的唯一取值是 SIR 词汇;而 `read_status_code` 对**按需行**上的 SIR 词汇
  **明确拒绝**(`:313-325`,理由是「按需实例没有 spot request,该记录自相矛盾」)。
- 工具自己把这句话打在输出里(`:501-506`):按需机 *"can never satisfy the attribution
  clause, because an on-demand row is refused if it carries a SIR code at all"*。
- 头部散文更直白(`:91`):*"an on-demand wave cannot satisfy clause (2), so it always
  hands the next wave back to spot."*

⇒ **W62 是全按需波 ⇒ clause (2) 按构造恒假 ⇒ 无论 `status_code` 是什么,verdict 都是 NOT BLINDED。**

⭐ **而且 clause (1) 本轮也已经独立地判死了它**:W62 配对种子 **4 粒**(远 > 1),
而 clause (1) **只需要 `ab`/`ba`** —— 那两个字段**本轮已经回填、数据是存在的**。
**两个 clause 各自独立地把答案钉成 NOT BLINDED,而缺的那个字段一个都影响不到。**

**缺陷是求值顺序**:`read_status_code` 在**逐机解析循环**里
(`:470-486` → `classify_machine` → `:303`),`Undecidable` 在**任何 clause 被求值之前**
就 `return 2`。若 clause (1) 先短路(yield ≤ 1?),W62 会直接 `exit 0 NOT BLINDED`,
**根本不需要那个字段**。

⚠️ **本台不自己改 harness**(章程/先例 GH #33),已交 issue。

---

## 六、⛔ 本轮不发波,理由只有一条,而它不是钱

闸 (i) ✅ UNLOCKED、闸 (iii) ✅ CLEAR(两种市场类型都 clear)、
闸 (iv) ⛔ **`exit 2` = 什么都没检查**。

⭐ **本台**推导出**了答案(五之三,两条独立的 clause),但**不拿推导替代退出码**。
理由是章程自己的措辞:总监 GH #683 §四 把发波授权**钉在退出码上**;
`exit 2` 逐字「**nothing was decided; this is not a pass**」。
**一个自己推导出来的通过,和一个工具打出来的通过,在授权上不是同一个东西** ——
这正是上一轮 §一 量到的那件事(披露给人看、退出码给授权用)的镜像:
**这次是本台手里有正确的理由,而门没开;不许因为理由正确就自己开门。**

⇒ **零发波、零新增 EC2 支出。本轮唯一新增支出是 `$0.01` 的 CE 复核。**

⚠️ **闸 (ii) 本轮未跑**:它是发波闸(臂串家族/深度),不发波则无对象。**不跑就不声称。**

⚠️ **诚实边界**:九月围栏眼下容得下 **一波 spot**(`headroom $1.727 after`)或
**一波按需**(`$0.677 after`);这个余量**随外来支出入账而缩**(外来 = MTD 的 50.4%,GH #515),
**不保证下一轮还在**。

---

## 七、泄漏检查

```
LEAK_EXIT=0
awsx ec2 describe-instances --filters "Name=instance-state-name,Values=pending,running,stopping,stopped"
  -> []          # 全账号、不按 tag 过滤:零实例
awsx ec2 describe-images --owners self
  -> ["ami-0a990a26d89c66547"]     # 常驻仍只有 AMI
```

⇒ **无泄漏、无第五台;外来 `r7a`/`c7a` 本轮 region 内 0 条。**

---

## 八、铁律 6 / 铁律 11

- `bots/` / `game/` / `tests/` / `tools/` **本会话一行未改**
  (只动 `iterations/` 下的报告、`W62_wave.json` 回填、章程本节)。
  ⛔ §五之三 的 harness 缺陷**本台不自己改**(先例 GH #33),已交 issue。
- **铁律 11** 未触发任何 `requires approval`;**本轮没有任何等待**。
- 静态门 / push 读数见文末补记。

---

## 九、交棒

1. ⭐⭐⭐ **总监 + harness —— 闸 (iv) 在任何按需波之后结构性死锁**(§五之三)。
   这不是「W62 这一份记录坏了」,是**每一次按需波都会复发**,而按需波正是闸 (iv)
   自己在 BLINDED 时开出来的处方 ⇒ **闸开出的处方会让闸下一轮打不开自己**。
   一行remedy:**clause (1) 先短路**(yield ≤ 1 为假 ⇒ 直接 NOT BLINDED),
   `status_code` 只在 clause (2) 真要被求值时才索取。已开 issue。
2. ⭐⭐⭐ **总监 —— 授权口径需要一条裁定**:当闸 (iv) `exit 2` 而**该波按构造只有一个可能答案**
   时,本台可否按推导发波?本轮本台**选择了不发**(§六),但那让九月最后 `$1.727`
   的实验能力**大概率过期作废**。**这是一个授权问题不是技术问题,本台不自裁。**
3. ⭐⭐ **总监 —— 收割义务掉棒一例**(§五之一):00:14Z 那轮把 W62 收进了 verdict
   却没回填波次记录,而**闸 (iv) 读的是波次记录** ⇒ 掉的那一棒**恰好是下游闸的输入**。
   本轮已回填 `ab`/`ba`/`arm_depth`。建议:收割轮跑一次「波次记录零 null」检查。
4. ⭐⭐ **总监/owner —— 上一轮「九月已封盘」的结论本轮到期**(§二):
   那个结论在它自己的快照上正确,但**默认了快照不会前进**;快照一走,
   `$3.250` 的 pending 出带即释放。**「围栏关了」是一个有保质期的读数,不是一个状态。**
5. ⭐ **总监 —— `$0.90 → $1.10` 常数本轮无新测量点**(本轮零发波),两条腿都不触发。
6. ⚠️ **自检第 8 次栽在同一个管道上**(§一),八轮了。

---

## 十、下一轮本台

1. **仍是发波轮**,但**先看闸 (iv) 是否已被修好**(交棒 1)。若已修,
   W62 应直接 `exit 0 NOT BLINDED` ⇒ **下一波 spot**。
2. ⛔ **围栏现跑不抄** —— **不许抄本报告任何一个数**(`$75.023` / `$1.727` / `$2.150` 全部作废),
   `--pending` 按**当轮快照时钟**自补,不得只覆盖工具 `must cover :` 点名的那几波(#692 未落地)。
3. ⚠️ **开工第一条命令重定向 + 后台 + 不设短 `timeout`**(第 9 次提醒)。
4. 若九月围栏已被外来支出吃光,**照总监 GH #683 §二:W63 规格不作废,10-01 之后第一波照它发**
   (⚠️ 重置后天花板是 `$50` 不是 `$80`)。

---

## 十一、本轮开出的 issue(#290 顺序:先 push 再发表)

**GH #699 `[harness]`** —— 闸 (iv) 在任何按需波之后结构性死锁。
草稿跑 `claim_precheck.sh`,逐字:

```
PRECHECK_EXIT=0
local commits not on origin/main: 0
paths cited 6  hashes audited 0 of 4 hex tokens (anchored)  resolved on trunk 4  refused 0
clean
OK to publish: every citation resolves on origin/main.
```

发表顺序:三份产物先 push 上 main(`9bf853b4..62b77c53`),**之后**才发 issue。

## 十二、铁律 6 读数

- `ARM_EXIT=0`(`core.hooksPath=.githooks, already set`)。
- 两次 push 的钩子均 `luacheck bots game: 0 warnings` /
  **`GATE_EXIT=0  CLEAN (iron rule 6 static half passed)`**;
  `push origin HEAD:main` 那次 py gate **`91 ran, 0 findings, 0 uncertifiable, 31.3s`**。
- **未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行**。
- Lua 动态半(GH #124)**未跑,不声称**。
- `PUSH_BRANCH_EXIT=0`(`claude/great-sagan-nzij6w`,新分支)/
  `PUSH_MAIN_EXIT=0`(`9bf853b4..62b77c53  HEAD -> main`,**未被拒,无需 rebase**)。
- ⚠️⚠️ **两件仪器两份清单,不许互相覆盖**:钩子 py gate `0 findings` 与自检 python 腿的
  `UNCERTIFIABLE` **不是矛盾,是 manifest 不同** ⇒ **`GATE_EXIT=0` 不构成「trunk 是绿的」**。
  钩子自己也打了 `7 new test(s) not in the manifest were run anyway`。

## 十三、⭐ 自检追记(后台跑完之后,真码到手)

**`SELFCHECK_EXIT=3`**(后台约 45min 跑完 —— 比 §一 写稿时预计的「跑不完」要好)。
⛔ **§一 里那句「Lua 腿未跑完、本轮不声称」到此作废**,以本节为准。
工具自己的 `exit sources` 节逐字:

```
=== exit sources (GH #267 4b) ===
legs run              : 11
FINDINGS (exit 3)     : cadence queue-rulings owed-executions a-evidence-owed
UNCERTIFIABLE (exit 2): trunk-red(python)
NOT RUN (inside a leg): tests/test_luacheck_gate_soakswitch.py tests/test_selfcheck_lua_leg.py
selfcheck worst exit: 3
```

(工具自带 GH #267 的归因警告,本台**只登记来源不做归因**。)

逐腿:

- **Lua 检测器腿跑完了,而且是绿的**:`87 tagged detector file(s), 0 failures`
  + 工具自己的限定 `-- FAST SUBSET, not the full suite.`
  ⇒ **trunk 的 Lua 那一侧本轮真被看了一遍**(⭐ 上一轮 `test_detector_source_constants.py:508`
  那条红**在这个子集里已不复现**;但那是 python 侧的测试,见下条 —— **不据此销案**)。
- **python 腿仍是 `UNCERTIFIABLE`**,逐字
  `UNCERTIFIABLE -- 9 check(s) did not run; this is NOT a pass.` /
  `a python test did NOT run (could not read its input) ... it is NOT evidence that trunk is red`
  ⇒ **trunk 的 python 那一侧本轮仍然没人看完**,`test_detector_source_constants.py:508`
  那条红**既不继承也不销案**。
- 稳定版锚点 `6 anchor(s) checked -- OK`;`FROZEN none (no live gate names a promoted id)`;
  `promote-atom constraints: OK`。

⚠️ **`exit 3` 是「有发现」不是「没跑成」**,四项发现
(`cadence` / `queue-rulings` / `owed-executions` / `a-evidence-owed`)**均非本台本轮引入**
(本会话只动 `iterations/` 下三个文件),按铁律 9 交回总监。

## 十四、本轮 token

```
TOKENS total_in=5,051,452 out=42,009 turns=44
```

(到统计时刻为止;统计后的收尾回合不计入。)
