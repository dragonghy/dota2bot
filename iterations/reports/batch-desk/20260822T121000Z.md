# 批测台 2026-08-22T12:10Z(第四十二次触发)—— 收割轮 + owner P1 第 3 棒启动轮

**一句话**:10:10Z 的 19-id 全集波收割完毕(264 有效局,gpm **−26.01**,四指标 0/4);
**`--rec-slots 8` 的事先登记验收 exit 0 = 通过 ⇒ 本波按登记上 16**;
按总监 §AN.0 启动 **21-id 全集波**(+`stayfield` +`pullcamp`),这是 **owner 优先项 P1 的第 3 棒**
(`queue.json:strategy-1`,总监 §AN.3 已批准)。本轮最硬的新发现:多录制者归属歧义
**第一次被真正演练**,`by_mtime` 判据 **147/147 全错**,而 `dem_claim.sh:144` 的
`rec_slots=1` 闸门正好挡住它 —— 闸门是承重的,这是实证,不是推演。

---

## 1. 成本与预算刹车

| 项 | 读数 |
|---|---|
| MTD(`budgets describe-budgets`,免费) | **$18.741** |
| forecast | 23.139 |
| budget limit | 100.0(章程口径,非 $50) |
| budget refreshed | 2026-08-22T10:52:21Z |
| 开工时在跑实例 | **0**(10:10Z 那波已全部跑完自毁) |

$18.74 < `$COST_CONFIRM_AT`($35)⇒ **未花 $0.01 调 Cost Explorer**。
距 $45 自设围栏、$90 刹车线、$100 owner 批准线均有大余量。
本波预估 ~$1.6 ⇒ $18.74 + 1.6 ≈ **$20.3 ≤ $45**,启动条件 (iii) 满足。
**未触发任何预算刹车。**

---

## 2. 收割:10:10Z 波(19-id 全集,树 `19b90b9`,`--rec-slots 8`)

### 2.1 下载与对账(章程必查项 ②)

先分 run 下载、**前缀取时分秒不取 SHA**、合并后与逐 run 计数之和对账:

| run | analysis.json |
|---|---|
| `spot_20260822_100932_1_19b90b9…` | 71 |
| `spot_20260822_100937_1_19b90b9…` | 72 |
| `spot_20260822_100941_1_19b90b9…` | 74 |
| `spot_20260822_100946_1_19b90b9…` | 71 |
| **逐 run 之和** | **288** |
| **合并后实测** | **288** ✅ 一个不丢 |

(上一轮真丢过 3 局,这条对账是那次的产物。)

### 2.2 事先登记的必查项,逐条裁定

| # | 必查项 | 结果 |
|---|---|---|
| ① | 暖场 stamp 必须是 `19b90b9` | ✅ **24/24 全是 `19b90b9`**,树 checkout 未静默失败,数据有效 |
| ② | 分 run 下载 + 前缀 + 对账 | ✅ 288 = 288(见 2.1) |
| ③ | S3 真实 cand stamp = 19 id 一字不差;`creeppull` 在串里;`wandlimbo`/`odaoe` 一个不出现 | ✅ 逐字核对通过(串见下),`creeppull` **在第 19 位**,`wandlimbo`/`odaoe` **0 次出现** |
| ④ | `rec_slot_cost.py` 验收,三个 exit code 三种处置 | ✅ **exit 0 = 通过** ⇒ 下一波上 16(见 §3) |
| ⑤ | 全集波经济读数是 19 id 的**合成效应**,不可归因到任何单个 id(含 `creeppull`) | 已遵守:下表不打任何单 id 归因 |
| ⑥ | `blinkflee`/`liondrainstop` 读到 0 次不构成 SILENT(§V.5);`killed` 不可用(§AJ.3) | 登记在案,归口录像组 |

S3 实测 cand 串(19 id):
```
l1trade,l5combo,midtp,suptp,tpcommit,tpdying,lf_rescue,teambrain,ownhalf,overchase,
fieldregen,wandbleed,capmono,cmrguard,tpdead,zusult,blinkflee,liondrainstop,creeppull
```

### 2.3 verdict(标准路径 `recover_verdict.py` 全量重算)

**264 有效局 + 24 暖场局 = 288。四种子两 wave 全齐,无饿死。**

| seed | ab(radiant) | ba(dire) | gpm | xpm | deaths | last_hits |
|---|---|---|---|---|---|---|
| 888 | 41 | 24 | −15.01 | −27.56 | +0.23 | −1.99 |
| 895 | 42 | 24 | −16.36 | −23.94 | +0.06 | −1.30 |
| 896 | 42 | 26 | −45.06 | −39.43 | +0.42 | −1.94 |
| 906 | 42 | 23 | −27.62 | −43.13 | +0.38 | −3.02 |
| **均值** | | | **−26.01** | **−33.52** | **+0.27** | **−2.06** |

`comps_better`:gpm **0/4**、xpm **0/4**、deaths **0/4**、last_hits **0/4**;
`suggested = hold_or_reject`。

逐种子 gpm 的 sd = **13.90**,SE = 6.95 ⇒ z = **−3.74**;
按 GH #30 经验零点 σ=30.24 换算 z = **−1.72**。

**判读归总监/协同组**,批测台只报数。纯观察一句(不构成归因):19-id 落在与
14/13/12/15/16-id 同一条负残差带内(−24 ~ −39),**加了 `creeppull` 之后既没消失也没显著恶化**。
按 §AF.3,多-id 波次之间的跨波经济比较已整门作废,上面这句只作背景,不作证据。

---

## 3. ⭐ `--rec-slots 8` 事先登记验收:**exit 0 = 通过**

按章程末节登记的命令原样执行(未改判据):

```
python3 tools/batch_test/soak/rec_slot_cost.py <本波 4 个 run 目录> \
    --baseline tools/batch_test/soak/rec_slot_baseline.json
```

**结果:`compared 16 slot(s); 0 recording slot(s) beyond tolerance` / `OK: measured on 288 games, 8 control slots.` / exit 0。**

### 3.1 波内自控腿(选 8 不选 16 的那个理由,兑现了)

对照槽(9-16,同机同时不录)拟合趋势 `timescale ≈ 0.01044*slot + 1.7955`;
录制槽(1-8)对**自己槽号**的残差:

| 录制槽 | n | 实测 | 趋势预测 | 残差 |
|---|---|---|---|---|
| 1 | 19 | 1.935 | 1.806 | **+7.1%** |
| 2 | 20 | 1.948 | 1.816 | **+7.2%** |
| 3 | 19 | 1.948 | 1.827 | +6.6% |
| 4 | 20 | 1.959 | 1.837 | +6.6% |
| 5 | 20 | 1.961 | 1.848 | +6.2% |
| 6 | 20 | 1.972 | 1.858 | +6.2% |
| 7 | 20 | 1.991 | 1.869 | +6.5% |
| 8 | 20 | 1.989 | 1.879 | **+5.8%** |

**+ = 录制槽比趋势更快。8 个录制槽全部比对照趋势快 5.8-7.2%。**
第二通道同向:录制槽完成 **19-20 局**,对照槽均值 **16.25 局**(+2.75 ~ +3.75)——
即 8 个并发 SourceTV 的 `.dem` 上传**没有吃掉任何一局**。

⇒ **「16 个并发 SourceTV 争用是超线性的」这个担心,在 8 并发这一档没有出现任何苗头。**

### 3.2 跨波对照(对 `rec_slot_baseline.json`,已扣 box factor 1.017)

16 个槽 net 全落在 **−2.5% ~ +6.1%**,**0 个录制槽超差**。

### 3.3 处置(按登记)

**exit 0 ⇒ 下一波 `--rec-slots 16`。已在本轮 §5 的波次里执行。**
总监 §AM.4 已事先裁定「同波同时动 id 集合与 rec-slots 在这里无害」,
故本波 19→21 id 与 8→16 rec-slots 同时改,不构成阻挡。

**诚实边界(必须传下去)**:**16 录制槽 = 没有波内对照腿了**。下一轮的验收
只能走跨波比较(`--baseline`),§3.1 那种自控证据这一波起拿不到。

### 3.4 顺带完成总监交办:把 08:10Z 并进基线

总监指示「08:10Z 那波收割后应把它并进基线」。已做:

```
rec_slot_cost.py <06:10Z 4 run> <08:10Z 4 run> --assume-rec-slots 1 \
    --emit-baseline tools/batch_test/soak/rec_slot_baseline_merged.json
```
新剖面:**16 槽 / 624 局 / 8 run**(旧剖面 305 局 / 4 run),已入库。
**注意**:该语料无 `.demclaim.json`(rec-slots 1 时代),`--assume-rec-slots 1` 是
**显式声明的假设,不是读数** —— 工具原样打印,未冒充实测。旧剖面文件保留未删。

---

## 4. ⭐ 本轮最硬的新发现:多录制者归属歧义第一次被演练,`by_mtime` 全错

`--rec-slots 8` 让 `.demclaim.json` 与 `dem21/` **第一次有真实数据**
(此前连续第十五轮为 0 = 从未被真实数据验证过)。147 个 sidecar 逐个解析:

| 项 | 读数 |
|---|---|
| sidecar 总数 | **147** |
| `method` | `logname` **147/147** |
| `hostname_hits` | `1` **147/147** |
| `candidates`(池里同时有几份 `.dem`) | **3-8**(8:48 局、7:41、6:29、5:18、4:10、3:1)|
| `by_logname == by_hostname` | **147/147 一致** |
| `by_logname == by_mtime` | **0/147 —— 全部不一致** |

**总监此前记录的事实「三种判据逐局一致」在 8 录制者下不再成立,需要更正。**
它在 rec-slots 1 时代成立是**平凡成立**(`candidates:1`,三个判据看的是同一份文件);
现在池里有 3-8 份,`by_mtime`(取最新)**每一局都指向别人的局**——
经常是别的槽、有时差好几分钟(例:`…101022_slot1` 的 claim,mtime 判据指向
`…101132_slot8.dem`)。

**这不是 bug,是一道闸门被证明承重**:`tools/batch_test/soak/dem_claim.sh:144`
```
elif [ "$rec_slots" = "1" ] && [ -n "$by_mtime" ]; then
```
mtime 回退**被结构性限定在 rec_slots=1**。所以多录制者下它根本不会被采用。
**本轮把「如果没有这道闸门会怎样」量化了:147/147 错误归属。**

**对上 16 的影响评估**:`logname` 是**结构匹配**(`.dem` 文件名里字面带着本局的
时间戳与槽号),不是启发式,**判别力不随池子变大而衰减**;`hostname_hits=1`
在 8 并发下仍是 147/147 唯一命中。⇒ 上 16 在归属正确性侧没有已知风险。
下一轮仍应复核这三个计数(池子会涨到 16)。

---

## 5. 启动:21-id 全集波 —— owner 优先项 P1 第 3 棒

### 5.1 启动依据(为什么本轮可以启动)

- **章程 4a**:`queue.json` 有 pending 的 **`strategy-1`**(priority 1)。
- **铁律 9**:这是 **owner 优先项 P1** 的第 3 棒,凌驾于 issue 流。
- **总监 §AN.3 明文批准**:「`queue.json:strategy-1` 的波次请求(seeds 888/895/896/906)
  **本节即为其批准**;批测台按 §AN.0 的 21 id 串排下一波即可,**不必另等**」。
- **例行 6h 节流不适用**:队列显式请求不受 (i)(ii) 约束,只受成本约束(§4a),
  成本条件已满足(§1)。距上一波(10:10Z)仅 2h,若按例行波次论本不该开 —— **本波不是例行波次**。

### 5.2 组合(逐字照抄总监 §AN.0,本台未做任何自主选择)

**21 id**(§AM.0 的 20 id 逐字不变 + `pullcamp`):
```
l1trade,l5combo,midtp,suptp,tpcommit,tpdying,lf_rescue,teambrain,ownhalf,overchase,
fieldregen,wandbleed,capmono,cmrguard,tpdead,zusult,blinkflee,liondrainstop,creeppull,
stayfield,pullcamp
```
`wandlimbo` 仍不 armed(§J.1.4 机会普查是硬前置,**连续第十一轮无人做**);
`odaoe` 按 §AB.1 仍不进串。

**上机前 21 个 id 逐个 grep**:20 个在 `bots/` 里有裸字面量;
**`lf_rescue` 命中 0 是已知正确的**(经 `J.IsLaneFixOn('rescue')` → `J.IsSoakCandidate('lf_'..sub)`)。
新入集两个:`stayfield` 3 处、`pullcamp` 6 处,均真实存在。

### 5.3 树(两步法 + 正控,认 exit code 不认空输出)

- `git ls-remote origin main` 真值 = **`177a2d176774d29affcc8b4765c59695cf0d5d3a`**(不信本地 remote-tracking ref)。
- 上一波树 `19b90b9` 先 `git fetch --depth 1 origin <全40位SHA>` 取回(浅仓库边界外可取),
  再 `git log 19b90b9..HEAD -- bots/ game/` ⇒ **exit 0 且非空**:**1 个 commit `c081e74`**。
- **正控**:同区间不限路径 **13 个 commit** ⇒ 区间真实,不是空区间伪影。
- **漂移的行为学后果:正是本波要测的东西。** `c081e74` = 协同组 07:30Z 的
  **`pullcamp` 触发器修复**(死条件:触发器要求先看见自己动作才会产生的状态)。
  这不是需要排除的污染,是 §AN.0 指定的被测变量。
- **树钉全 40 位 SHA `177a2d17…`,不用 `--ref main`**(避免波次进行中远端再动)。

### 5.4 上机

4 台 c6i.4xlarge **on-demand**(`InstanceLifecycle=None` 逐台复核),
**一台一种子**,**16 槽**,**`--rec-slots 16`**,**2h 看门狗**,**`--games 22`**。

| seed | instance | run_id |
|---|---|---|
| 888 | `i-01931db8f4910899a` | `spot_20260822_121209_1_177a2d17…` |
| 895 | `i-0f803ca0b88b55908` | `spot_20260822_121214_1_177a2d17…` |
| 896 | `i-01374db9b488c9a66` | `spot_20260822_121219_1_177a2d17…` |
| 906 | `i-07544a41d40f2dbdd` | `spot_20260822_121224_1_177a2d17…` |

**run_id 唯一性纪律(本轮第四次有适用对象,通过)**:四次 `COUNT=1` 调用之间
`time.sleep(2.6)`,**实测间隔 5s/5s/5s**;启动后读**独立证据源** `soak-run` 标签,
**四值互异(distinct=4)**。

预估 **$1.6**,预计 **~12:45-13:00Z 落地**,预期 **~290 局**。

### 5.5 下次触发的收割必查项(七条)

1. **暖场 stamp 必须是 `177a2d1`** —— 不是则裸 SHA checkout 在实例上静默失败,该轮作废。
2. 先分 run 下载再带前缀合并,**前缀取时分秒不取 SHA**,合并后与逐 run 之和**对账**。
3. S3 真实 cand stamp 应恰好等于 §5.2 那 21 id 串**一字不差**;`stayfield`/`pullcamp`
   **必须在串里**(两者首次上机);`wandlimbo`/`odaoe` 一个都不该出现。
4. **`--rec-slots 16` 验收**:`rec_slot_cost.py <4 run> --baseline
   tools/batch_test/soak/rec_slot_baseline_merged.json`(**用本轮新并的 624 局剖面**)。
   **16 录制槽没有波内对照腿**,只能跨波比;exit 0 = 站住,exit 1 = 退回 8 并贴逐槽表,
   **exit 2 = 拒答不是通过**。
5. **复核 §4 的三个计数**(`method`/`hostname_hits`/`candidates`):池子会涨到 16,
   `logname` 与 `hostname` 是否仍 100% 一致是上 16 的归属正确性判据。
   **`by_mtime` 预期继续全错,这是正常的、被闸门挡住的,不要报成回归。**
6. **全集波经济读数是 21 个 id 的合成效应,不可归因到任何单个 id**(含 `pullcamp`/`stayfield`);
   凡打 A−B 的表每个指标都要打基线腿。
7. `blinkflee`/`liondrainstop` 读到 0 次不构成 SILENT(§V.5);`killed` 不可用(§AJ.3)。

**主判据不在批测台**:P1 第 4 棒是**录像组**按 §AN.4 口径读帧 ——
`pullcamp` 与 `creeppull` **分开数**,且 `pullcamp` 要跟到落地
(**出发 / 抵达 / 抵达时营地空(空跑) / 真拉成** 四段分开)。
`stayfield` 按 §AM.5,除触发计数外**必须数 stay 之后 30 秒内的死亡率**。

---

## 6. 泄漏检查(四层 + spot 请求,全免费只读)

| 层 | 开工 | 收尾 |
|---|---|---|
| 在跑实例 | **0** | **恰好 4 台**,全是本轮有意启动的自毁 on-demand |
| 游离卷(available) | 0 | **0** |
| 快照 | 1(`ami-0a990a26d89c66547`,唯一常设成本) | **1** |
| EIP | 0 | **0** |
| open/active spot 请求 | 0 | **0** |

**无泄漏。** 收尾那次走 `--leak-only`(零成本,不查 MTD —— 同轮第二次读 MTD 从来没带过信息)。

---

## 7. 局数(章程第 7 条固定栏位)

- **上一波次(10:10Z,19-id)最终有效局数**:**264**(+24 暖场,合计 288)。
  per seed / per side(ab=radiant / ba=dire,**不对称,dire 仍被 35min stall 截断**):
  888 = 41/24、895 = 42/24、896 = 42/26、906 = 42/23。
  radiant **167** / dire **97**。radiant 已到 `--games 22` 配额顶。
- **本轮在跑波次实时进度**:刚上机,S3 `soak/<run_id>/` 里 `analysis.json` 计数 **0**;
  预期 **~290 局**(暖场局不算有效局)。
- 本轮下载 `analysis.json`:10:10Z 波 **288** + 基线合并语料 **624** = **912**。
- 档案累计:`soak/` 前缀 **148 → 152**(+4 run),波次 **86 → 87**。

## 8. 固定栏位

| 项 | 读数 |
|---|---|
| off-roster | **0**(`seed_roster_index.py --verify`:112/112 seeds match,0 mismatch。**边界**:该索引覆盖 137 run,本轮未跑昂贵的 `--build`,10:10Z 的 4 个 run 未必已并入索引)|
| `unattributed/` | **0** |
| `dem21/` | ⭐ **首次非 0** —— 10:10Z 波的 4 个 run 前缀均已落数据 |
| `.demclaim.json` | ⭐ **首次非 0** —— 147 个(详见 §4)|
| `queue.json` | `strategy-1` **pending → running**(本轮波次即为其执行);`hero-1` 仍 pending(见 §9)|

## 9. `hero-1` 请求的处置(本轮未做,已说明)

`queue.json:hero-1` 自称 **NO NEW WAVE NEEDED**,是一次**归档语料扫描**
(WK `wkqaim` 的域大小普查:level ≥ 7、Q 可用、568u 内 ≥2 敌人、且 nearest ≠ lowest health 的
帧/episode/局计数)。它不花 AWS 的钱,但需要在 11048 局归档语料上跑检测器 ——
**本轮工作单元已满**(收割 + 验收 + 基线合并 + 启动 + 归属核查),按「工作单元要小」
不硬塞。**下一轮若无收割冲突,优先做它**(它已挂 1 轮)。
提醒记录在案:该请求自带一条 bench note —— fixture loader 的 nearby-hero 顺序
在 2026-08-22 之前是**字母序**,任何早于该修复的工具读数都得重读。

## 10. 验证(铁律 6)

本会话**未改动任何 Lua**;改动为 `iterations/` 下的报告与章程、`queue.json`,
以及新增数据文件 `tools/batch_test/soak/rec_slot_baseline_merged.json`(生成物,非 Lua)。
本容器**未装 `luacheck`/`lua5.1`**,两者均未运行 —— 按铁律 6 立法目的本轮无适用对象。

本报告 §1-§8 每个数字均由实跑得出:`budgets describe-budgets`、`s3 ls`/`s3 cp`
(912 个 `analysis.json` + 147 个 `.demclaim.json` 逐个解析)、`recover_verdict.py`、
`rec_slot_cost.py`(验收 + `--emit-baseline`)、`seed_roster_index.py --verify`、
`git ls-remote`/`fetch --depth 1 <全40位SHA>`/`log`/`grep`、
`ec2 describe-instances`(含 `soak-run` 标签)/`describe-volumes`/`describe-snapshots`/
`describe-addresses`/`describe-spot-instance-requests`。
**唯一估算项是本波费用 $1.6**(引自同配置历史实测)。

## 11. 跨组

在既有 **GH #45** 下追评(**不新开**):交付 19-id 读数、rec-slots 8 验收通过、
§4 的 `by_mtime` 更正、以及 21-id 波已上机的通知。
`[batch] #70`、`[harness] #98`/`#75`/`#33`、`[bug] #96` 仍开着,不重复开。

## 12. 必须传下去的四条

1. **`--rec-slots 16` 起没有波内对照腿了** —— 验收只能跨波,用本轮新并的
   `rec_slot_baseline_merged.json`(624 局),**exit 2 是拒答不是通过**。
2. **`by_mtime` 在多录制者下 147/147 全错,是正常的**;它被 `dem_claim.sh:144` 的
   `rec_slots=1` 闸门挡住。**下一轮看到它继续全错,不要报成回归**;要看的是
   `logname == hostname` 是否仍 100%。
3. **owner「默认波次 = 全集」是常设指示**,默认串取 `test_set.md` 最新 §x.0
   (现为 §AN.0 的 21 id),**不由本台自选组合**。
4. **P1 第 3 棒的球已交出**:波次在跑,**第 4 棒是录像组**按 §AN.4 口径读帧
   (`pullcamp`/`creeppull` 分开数,`pullcamp` 跟到「出发/抵达/空跑/真拉成」四段)。
   接力棒不许掉在这里。

## 13. 本次会话 token 用量

见文末追加。
