# 批测台工作单元报告 — 2026-08-19T12:12Z

**类型:启动轮(`lf_rescue` bisect,同树两臂对照)。无新数据可收割。**

## 1. AWS 引导

`bash tools/batch_test/aws/session_setup.sh` → 新容器无 AWS CLI,脚本自行装好
(aws-cli/1.46.0),身份核对通过:

```
AWS ready: arn:aws:iam::581436534924:user/dota2bot-agent
(restricted batch-runner user — correct)
```

之后一律走 `awsx` 包装器。

## 2. 成本与泄漏检查(启动前)

`check_costs.sh`:

| 项 | 值 |
|---|---|
| MTD 花费 | **$3.4651**(与 10:07Z 那轮完全一致——中间两轮零支出) |
| 在跑/pending 实例 | **无** |
| 常驻成本 | 仅 AMI `ami-0a990a26d89c66547` + 其快照 |

刹车线 $90 / owner 批准线 $100,MTD 距离两条线都有数量级余量,**未触发任何
预算刹车**。无泄漏。

## 3. 收割

- `s3://dota2bot-batch-results-4924/validation/` 最新对象仍是 07-23 的历史件;
- `s3://dota2bot-batch-results-4924/soak/` 最新前缀仍是 `spot_20260819_060935_1_main`
  (855-858 那一波,已在 08:08Z 轮用 `recover_verdict.py` 全量收割完毕)。

**08:08Z 之后没有任何新对象落地(中间的 10:07Z 轮按节流条款没启动波次),
本轮无新数据可收割。** `iterations/queue.json` 的 `requests` 仍为空数组,
没有 pending/running 请求需要改状态。

## 4. 启动决策

queue.json 空 → 走章程 4(b) 例行波次,三条件检查:

| 条件 | 判定 |
|---|---|
| (i) 距上一次例行波次启动 ≥ 6h | **满足**。上一波 06:09Z,本轮 12:10Z 启动,间隔 6h01min(10:07Z 那轮因差 2h 未启动,把窗口让到了本轮整点之后) |
| (ii) 有新东西可测 | **满足**。`test_set.md` 自 `ce2c5df` 后成员变更(`cmrguard` 退出、`capmono` 入集),`bots/` 有 `[bug] #31` 修复(`bLaneKillCommit` + cap exemption)、`capmono`、`cmrguard` 收窄等改动 |
| (iii) 当月已花 + 本波预估 ≤ $45 | **满足**。$3.4651 + ~$3.4 ≈ **$6.9**,远低于 $45 月度围栏 |

三条件全过 → 启动。**本波内容完全按总监 11:10Z 在 `test_set.md` §C 定死的
两臂定义执行**(该节明确"覆盖 batch-desk 10:07Z 的建议"):`lf_rescue` 的
**同树两臂 bisect**,不是与历史 -34.59 的跨波比较。

### 上机前的静态核对(避免重演 `[bug] #31` 那类"测了个不存在/不生效的东西")

1. **远端树核对**:`git ls-remote origin main` = `d6bfa08` = 本地 HEAD。
   实例 clone 的就是要测的树。**核对通过。**
2. **13 个 id 全部在 `bots/` 里真实存在**(逐个 grep):`fieldregen`/`wandbleed`/
   `capmono` 走裸 `J.IsSoakCandidate('<id>')`;`l1trade`/`l5combo`/`midtp`/
   `suptp`/`tpcommit`/`teambrain`/`ownhalf`/`overchase`/`tpwatch` 走各自模块里的
   字面量;**被 bisect 的 `lf_rescue` 没有裸字面量**——它经
   `J.IsLaneFixOn('rescue')`(`jmz_func.lua:5385-5388`)展开成
   `J.IsSoakCandidate('lf_' .. sub)`,即 `lf_rescue`,消费点在
   `jmz_func.lua:5691`。**被测变量确实可被逗号串表达,不是空转。**
3. **逗号 bundle 解析路径核对**:`J.IsSoakCandidate`(`jmz_func.lua:4598-4618`)
   对含 `,` 的 `conf.cand` 做 `gmatch('[^,]+')` 逐项匹配,且再叠一层
   `GetTeam() == 侧`。两臂只差一个 id,其余 12 个在两臂里逐字相同。

### 本波配置

| 项 | 值 |
|---|---|
| 臂 A(13 id) | `l1trade,l5combo,midtp,suptp,tpcommit,`**`lf_rescue`**`,teambrain,ownhalf,overchase,fieldregen,wandbleed,tpwatch,capmono` |
| 臂 B(12 id) | 同 A **去掉 `lf_rescue`** |
| 种子 | 859 / 860 / 861 / 862 —— **两臂用同一组种子**,draft 逐局配对,A−B 才是配对差 |
| 拓扑 | **8 台 × 1 种子**(4 种子 × 2 臂),沿用上一波证明有效的 1-seed/实例(结构上杜绝种子饿死) |
| 机型 | c6i.4xlarge **spot** ×8,全部一次拿到容量,无需降级 on-demand |
| slots | 16 |
| 看门狗 | 3h(`shutdown -h +180` + `instance-initiated-shutdown-behavior=terminate` + one-time spot) |
| games | **`--games 22`**(上一波 15,dire 侧 80 局 vs radiant 149 明显被截断) |
| 树 | `main` @ `d6bfa08` |

### 启动结果(全部 `running`)

**臂 A(含 `lf_rescue`)**

| 种子 | instance id | run_id(S3 前缀 `soak/`) |
|---|---|---|
| 859 | `i-09dd54aa8d220363b` | `spot_20260819_121038_1_main` |
| 860 | `i-04450cb6f8c6ebf9d` | `spot_20260819_121044_1_main` |
| 861 | `i-05d253b15d37d1f86` | `spot_20260819_121050_1_main` |
| 862 | `i-0530f89f182421df8` | `spot_20260819_121056_1_main` |

**臂 B(不含 `lf_rescue`)**

| 种子 | instance id | run_id(S3 前缀 `soak/`) |
|---|---|---|
| 859 | `i-086d3dd950d013cae` | `spot_20260819_121105_1_main` |
| 860 | `i-0d5eb91cd745fadc2` | `spot_20260819_121111_1_main` |
| 861 | `i-0d1a396a812b6ecde` | `spot_20260819_121117_1_main` |
| 862 | `i-010ec40dede14e7e3` | `spot_20260819_121122_1_main` |

**run_id 不编码臂和种子**,映射只存在于上面两张表(和本章程"当前状态"节)
——下次收割**必须**按此表把 run 归臂,不要靠时间戳猜。verdict 对象名里的
`<cand>` 串本身区分两臂(13-id 串 vs 12-id 串),可以交叉校验。

预估花费:8 台 × ~1.5-2h × ~$0.25/h ≈ **$3-4**。预计 verdict 约
**2026-08-19 13:40-14:40 UTC** 落地。

## 5. 结束前泄漏复查

`describe-instances`(pending+running)恰好 **8 台**,全部 `spot` / `running`,
全部是本轮有意启动的自毁实例,**无多余实例,无泄漏**。

## 6. 局数(章程第 7 条固定节)

**(a) 上一波次(`l1xpsoak` solo,种子 855-858)最终有效局数** —— 已完结,
数字来自 08:08Z 轮的 `recover_verdict.py` 全量重算:

| 种子 | 有效局数 |
|---|---|
| 855-858 合计 | **229**(另 23 局暖场,不计入) |
| 按侧拆分 | radiant **149** / dire **80** —— 明显不对称,dire wave 被 35min stall 上限截断,这正是本波把 `--games` 从 15 提到 22 的理由 |

**(b) 本轮在跑波次实时进度**:12:10-12:11Z 启动,写报告时 8 个
`soak/spot_20260819_1210*|1211*_1_main/` 前缀下 `analysis.json` 计数**均为 0**
(实例仍在开机 / 拉 repo / steamcmd 刷游戏文件阶段,首局落地通常在启动后
20-30min)。**预期局数**:8 台 × 2 wave × ≥22 局 ≥ **350 局有效局**
(每臂 ≥175),暖场局不算。

## 7. 交给下一轮/其他组

- **下次触发**:用 `tools/batch_test/soak/recover_verdict.py` 标准路径**分两臂
  分别**收割(每臂 4 个 run),再算 **A−B 配对差**。
  **不许拿单臂读数去和历史 -34.59 比**(总监 §C 明确)。
- **收割操作坑(08:08Z 轮踩过,本波 8 台风险更高)**:同波次多实例的 per-game
  文件名会跨 run 撞车(秒级同时启动),直接 `s3 cp` 到同一目录会**静默丢局**
  (上次丢了 15.9%)。**必须先按 run 分目录下载,再带前缀合并。**
- **判读噪声底**(GH #30):per-seed gpm delta σ≈30,4-seed 均值 SE≈15。A−B 是
  配对差,噪声底应更低,但在拿到本波实测前**不要假设它更低**。
- 本轮未开新 issue:queue 为空、无异常、无泄漏、无 harness 阻塞
  (`[harness] #33` 仍挂着,是章程 4c 的已知缺口,不在本波路径上)。

## 7b. 验证(铁律第 6 条)

本轮**未改动任何 `bots/` / `game/` 下的 Lua**,改动仅为两个 markdown 文件
(本报告 + 章程"当前状态"节)。附带记录:本容器**未安装 `luacheck` / `lua5.1`**
(`command not found`),故两项检查无法执行;因无 Lua 改动,不构成风险,
但下一个需要改 Lua 的会话要先自行装好工具链再动手。

## 8. 本次会话 token 用量(铁律第 8 条)

```
TOKENS total_in=3,213,790 out=17,198 turns=41
```
