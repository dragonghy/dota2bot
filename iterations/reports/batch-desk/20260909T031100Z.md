# 批测台 — 2026-09-09T03:11–03:5xZ(发波轮:W59)

**一句话**:W59 四台 spot 起飞,四道闸全部在起飞前跑完、闸与动作同脚本;
⭐⭐⭐ 头号产出不是那四台机器,而是 **GH #454 (乙1) 欠到第五轮时,它欠的理由本轮第一次
换了形状** —— 09-07 与 09-08 两条 `SpotUsage:c6i.4xlarge` 行**三小时零字节变动**,
而这两天在"未落全"这个假说下**应当继续变**;这把上一轮那三个候选失效机制里的
(乙)「当日账单还没落全」**在 09-07 那一天上削弱了**,剩下 (甲)「SIR `UpdateTime` 滞后 ⇒
机时被系统性高估」独自扛着那个 18% 的价差 —— 而 (甲) 成立就意味着**本台一直在用的
「机时 × AZ 报价」价格侧估算系统性偏高**。

---

## 〇、开工自检(铁律 10)

- **第一条命令避开了两个坑**:管道坑(GH #514 的前身,工具自己会 `REFUSED` 拒答)
  与 `timeout` 坑(GH #514,09-08T21:08Z 那轮自陈"是我自己砍的")。
  ⚠️ **本轮第一次尝试仍然踩了管道**:`bash tools/agent/routine_selfcheck.sh 2>&1 | tail -40`
  ⇒ 工具逐字 `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.` /
  `it has recurred 5x, every time as the first command of the round`。
  **它自己拒答了,所以这次没有变成一个假读数** —— 这正是那道自拒门存在的理由,
  **登记为门起作用的一次,不是登记为本台没踩**。改用重定向后重跑,**未设 `timeout`、放后台**。
- 读数见第九节(自检在本轮收尾时仍在 python trunk 那条腿上跑,**读得到的腿照登、
  读不到的不声称**)。

## 一、成本

- `AWS_SETUP_EXIT=0`(`arn:aws:iam::581436534924:user/dota2bot-agent`)。
- `COSTS_EXIT=0`。**MTD `$69.465`**(免费 `budgets`,`budget refreshed 2026-09-08T22:46:49Z`),
  ≥$35 ⇒ 自动 `$0.01` CE 复核 **`69.4654098749`**,**两源逐位一致**;`forecast $181.976`
  (**原样登记,本台不据 forecast 决策**);budget limit `$100`。
- **在跑实例:`[]` 空** ⇒ W58 四台全部消失。常设成本仍只有 AMI `ami-0a990a26d89c66547`。
- ⚠️ **`budgets` 那张快照的时间戳 `2026-09-08T22:46:49Z` 早于本轮 4.5 小时**,
  而章程钉死的 EC2 滞后是 **4.3–11.3 小时** ⇒ **MTD 系统性偏低**,围栏算术按 (甲) 走
  `MTD + Σ(近 12h 已发波 × 该波单价)`,见第三节。

## 二、⭐⭐⭐ GH #454 (乙1) 第五轮欠账 —— 但本轮它欠的**理由**换了形状

多花 `$0.01` 拉了 09-05 → 09-09 的逐日 × 逐 `USAGE_TYPE`。

| 日期 | 总额 | `SpotUsage:c6i.4xlarge` | `BoxUsage:c6i.4xlarge` | `PublicIPv4:InUseAddress` | 外来 `c7a`/`r7a` |
|---|---|---|---|---|---|
| 09-05 | $26.397668 | $1.772275 | $2.080611 | $0.142050 | **$21.633102** |
| 09-06 | $18.411386 | $1.878381 | — | $0.080572 | **$15.802388** |
| 09-07 | $4.113271 | **$1.837314** | $1.621422 | $0.056656 | **0** |
| 09-08 | $1.957621 | **$1.341348** | — | $0.029350 | **0** |
| 09-09 | $0.000000 | — | — | — | 0 |

**(1) 外来负载确认停在 09-06**:09-07 / 09-08 / 09-09 三天**一个 `c7a`/`r7a` 都没有**
(与上一轮结论一致,本轮多一天的证据)。累计外来 **`$37.4355`** 仍顶在**无 filter 的
预算**上;扣掉它,本台自己的 MTD 约 **`$32.03`**。**本台不擅改任何一条线。**

**(2) ⭐ 新形状:两条行三小时零字节变动。**
上一轮(00:1xZ)读到 09-08 `SpotUsage` **`$1.341348`**、09-08 IPv4 **`$0.029350`**、
09-07 `SpotUsage` **`$1.837314`**;本轮(03:2xZ)**三个数逐位相同**。
- 对 **09-08**:它仍然**明显没落全**。09-08 四波已知机时合计 **`10.1499 h`**
  (W55 `2.7500` + W56 `3.1094` + W57 `2.1017` + W58 `2.1889`),
  `$1.341348 ÷ 10.1499 h = $0.1322/h`,而当日 AZ 报价带是 **`$0.2585–$0.2805`**
  ⇒ 实付只有最低报价的 **51%**;独立的 IPv4 行反解 `$0.029350 ÷ $0.005 = 5.87` 地址小时
  = 已知机时的 **57.8%**。**两条独立的行给出同量级的欠额** ⇒ (乙1) 仍不能结清。
- 对 **09-07**:⭐ **这一天的解释本轮被削弱了。** 上一轮登记过它的怪处
  (`$1.837314 ÷ 8.8000 h = $0.2088/h`,**比当日最低报价还低 18%**),
  并列了三个候选机制:(甲) SIR `UpdateTime` 异步滞后 ⇒ 机时被高估;
  (乙) 当日还没落全;(丙) IPv4 按小时进位造成虚假吻合。
  **本轮的增量是**:09-07 已过去 **>24 小时**,而它的 `SpotUsage` 行在**最近 3 小时里
  一个字节没动** —— 在 (乙) 之下这条行**应当还在往上走**。⇒ **(乙) 在 09-07 这一天上被削弱**,
  (甲) 独自扛着那 18%。
  ⚠️ **诚实边界,而且是硬边界**:这**不是**结清,理由有二 ——
  (i) 「三小时没动」**不等于**「终态」,CE 仍可能回填,本台**没有**任何工具能断言终态;
  (ii) 章程步骤 2 (乙) 明令禁用「MTD 增量对得上波费 ⇒ 该波已计入」那一手,
  而本段的推理**与它同族**(都拿一个数字的行为去替代一个不可见的状态),
  区别只在于本段用的是**不动**而不是**吻合**。⇒ **登记为一个削弱,不写进任何结论。**
- ⭐ **(甲) 若成立,代价是本台自己的量具**:上一轮已登记「机时 × AZ 报价」在 09-07 上
  **高估 33%**(估 `$2.44` vs 账单 `$1.837`)。本轮 09-08 上同一算法给 `$2.64` vs 账单
  `$1.341`(**高估 97%**,但这一天叠了未落全,不可单独归因)。
  **高估朝安全侧失效**(少发波),但按 GH #454 (丙) 先例**同样要登记**,因为它不会自己举手。

⇒ **(乙1) 第五轮欠着**,落全后一次补 W55/W56/W57/W58/W59 五波。

## 三、四道闸(全部在起飞前跑完,`(iv)` 排在 `(i)` 之前)

| 闸 | 读数 |
|---|---|
| **(iv) reclaim-blind** | `RECLAIM_BLIND_EXIT=0`,跑在 **W58 自己的记录**上(⇒ W58 的收割义务由此兑现)。`--wave-json` 用的是**具名参数**(上一轮交棒 ⑥ 逐字警告过位置参数会给 argparse 的 `exit 2`)。逐字 `yield: 2 paired seed(s) of 4 machine(s)` / `attribution: 2 machine(s) reclaimed before the flip` / `VERDICT: not blinded` / **`NEXT WAVE: spot (the default; GH #158 unchanged).`** ⇒ **市场类型是闸打的,不是本台判的** |
| **(ii) 有新东西可测** | **两条腿各自成立**:①**臂串动了** `42 → 39` id、`380 → 354` 字节、md5 `f5b9da19…` → **`433070c6d8fde3758555302db49047ab`**(**现算**,与 `test_set.md` 第 3 行逐位一致);②**树动了** `e4fc013d…` → **`66a8a15b…`** |
| **(ii) 接线** | `ARM_WIRING_EXIT=0`,逐字 `all 39 armed ids wired on HEAD`,**无 `DO NOT LAUNCH` 行**。跑的是**真正要传给实例的那 39 个 id**,不是记忆里的串 |
| **(iii) 围栏** | `FENCE_EXIT=0 CLEAR`,**本次现推导**。`--planned 1.100`(spot 常数,GH #454 (甲))、**`--pending 2.000`**(见下)。`projected total $72.565`;`$50.00 crossed (AWS state=ALARM)`、**`$80.00` / `$100.00` 均未跨**;fence **`$80.00`**、brake `$90.00`、operative ceiling **`$80.00`**、**headroom `$7.435`**。accrual 行逐字 `CERTIFIED ZERO (0 accruing instances account-wide), but --pending $2.000 was given and is used as the larger claim.`(**未用 `--no-accrual-check`**) |
| **(i) 节流** | 见第四节 |

**`--pending $2.000` 是本轮主张出来的,不是抄的常数**,并且**刻意大于**本台自己的价格侧估算:
第二节已证明 09-08 那一天**至少还欠一半**,而 W57(`~$0.57` 估)与 W58(`~$0.62` 估)
两波都落在那一半里 ⇒ 用 **spot 常数 `$1.10` × 2 波 = `$2.00`** 覆盖它们,
**失效方向朝安全侧**(少发波),这是本台在读数不确定时该选的那一侧。

**⚠️ 臂串变动登记**:`42 → 39`,**撤出 `cmrguard` / `roamidle` / `campsel` 三个,新增 0 个**
(`cmrguard` 在 W58 起飞后退集,`roamidle`+`campsel` 由 `ffb0248` 一次退两个)。
⇒ **连续第三轮是「纯撤回、零准入」**;P4.2 准入冻结在准入侧仍然成立。

## 四、⭐ 家族深度:上一轮点名的那个结构,本轮**再次复现**

上一轮(00:12Z)第三节立的案是「退集裁定的落地频率快于 6h 发波闸 ⇒ 每波开新家族、
每家族停在深度 1」。**本轮是这个结构的第四个连续实例**:

| 波 | 起飞 | 臂串 | md5 | 树 |
|---|---|---|---|---|
| W56 | 09-08 09:21Z | 44 id | `fe7a309f…` | `6aeea8c4…` |
| W57 | 09-08 15:21Z | 44 id | `fe7a309f…` | `43c6da3c…` |
| W58 | 09-08 21:22Z | 42 id | `f5b9da19…` | `e4fc013d…` |
| **W59** | **本轮** | **39 id** | **`433070c6…`** | **`66a8a15b…`** |

⇒ **W59 与 W58 / W57 / W56 全部不可并池**(臂串与树各自都变了,两条理由各自充分)。
**`(39-id × 66a8a15b)` 家族从本波起 `family_depth_at_launch = 0`。**
⚠️ **归属边界不变**:退集是总监的裁定权,本台**不主张它们错**;本台只主张
「退集在两波之间落地」与「家族靠跨波并池长深度」**当前互斥**。
已在上一轮开过 `[batch]` issue,**本轮不重复开**,只补第四个测量点。

## 五、W59 是什么

- **树**:钉 40 位 SHA **`66a8a15b124a794939e88c3e93e148201f04fbc9`**,不写 `main`
  ⇒ 收尾提交不会改动实例克隆的树。**漂移检查是发波脚本第一条语句**
  (`git ls-remote origin main` == 钉子,不等则 `exit 9`);门期另读过一次,两次一致。
- **臂串**:**39 id / 354 字节 / md5 `433070c6d8fde3758555302db49047ab`**,**现算**。
- **选种**:窗口右移到 **`[9501,9900]`**(W58 用到 9418)。
  `seed_roster_index.py --build` **在写下互斥声明之前**跑完(`ROSTER_EXIT=0`,
  **254 粒 / 23650 局 / 394 run 已入册**,W58 那四粒已在其中)——
  不 build 就会把刚烧掉的四粒当成"从未用过"献回来。
  **该窗口 400 粒与册的交集是空集**(册里 <100000 的最大种子是 9373)。
  term-popcount 分布 `{0:40, 1:120, 2:153, 3:76, 4:11}`。
- **选法**(与 W56–W58 同一目标函数):先最大化每 term 的**最小载体数**,再最大化**总载体槽**
  ⇒ **9531 / 9532 / 9583 / 9839**,7 个 term **每个 ≥2 载体**,**16 个载体槽**。
  ⭐ **本轮这个 `(2, 16)` 是全局最优,而且可以证明,不用信启发式**:
  窗口最高 popcount 是 **4** ⇒ 四粒总槽 ≤ 16,取到 16 即达上界;
  而 `min ≥ 3` 需要 7 个 term 各被 ≥3 粒覆盖 ⇒ 总槽 ≥ 21 > 16,**算术上不可能**。
  ⇒ **W58 的 18 槽、W57 的 21 槽不是本轮"退化"**,是那两个窗口恰好有 5-term / 6-term 粒。
- **载体闸**:`CARRIER_GATE ids=9 seeds=4 exit=0`,term **从臂串机械导出**
  (`--assert-carrier-from-arm`,GH #276,不手打):
  `9 hero-scoped, 30 generic, 0 unresolved => 7 term(s)`。
  逐 term 载体数 **crystal_maiden 3 / lion 3 / obsidian_destroyer 2 / pudge 2 /
  skeleton_king 2 / spirit_breaker 2 / zuus 2**;**`grep -c EMPTY = 0`,无一 EMPTY**。
  ⚠️ **term 集合与 W53–W58 逐字相同**,尽管臂串从 44 缩到 39 —— 撤出的
  `cmrguard` 是 CM-scoped,但 `cmqreach` 仍在串里替 CM 撑着 term;
  `roamidle` / `campsel` 都是 generic。**解空间不受影响。**
- **采集参数**:`--slots 16 --hours 2 --rec-slots 8 --games 12`,**与 W39–W58 逐字节相同**。
  `--rec-slots 8` **显式传**(`spot_run.sh` 默认是 1,W51 就是吃了这个默认在起飞 70s 后被杀)。
  `--dry-run` 先行 **`DRYRUN_EXIT=0`**,读过它的 plan 行才发真波;**波次预算闸未拒发**。

## 六、发波 —— 闸与动作在同一个脚本里

`spot_run.sh` 的四次调用与 `wave_throttle.py` 的轮询**被放进同一个脚本**
(GH #469:闸绑的是**动作**不是决定):脚本第一条是 `git ls-remote` 漂移检查(硬 `exit 9`),
第二条是节流轮询(`exit 2` ⇒ `exit 9` ABORT),之后才是发波循环。**中间不可能插进任何东西。**

**闸 (i) 读数**:`THROTTLE_EXIT=0`,逐字 **`margin +5s past unlock`**、
`WAVEGATE... UNLOCKED (exit 0) -- gate (i) passes at this instant.`
锚点是 W58 的 `2026-09-08T21:22:54Z`。脚本从 03:14:5xZ 起每 30s 轮询一次,
**前 14 次全是 `THROTTLED (exit 3)`,逐次都打了自己的读数,登记在此而不是丢掉**;
发波用的是**工具自己打出的第一个 `exit 0`**,不是本台算出来的时刻。

| seed | instance | AZ 请求→实际 | LaunchTime | SIR | 报价 |
|---|---|---|---|---|---|
| 9531 | `i-09dce6b399d53fdfc` | 2a → **2a** | 03:23:02Z | `sir-jmjzp6hj` | $0.2732 |
| 9532 | `i-02cc1300658cdc12d` | 2b → **2b** | 03:23:04Z | `sir-r6s7mq7h` | $0.2582 |
| 9583 | `i-08b634eb637f5e090` | 2c → **2c** | 03:23:07Z | `sir-pjjzpfnh` | $0.2811 |
| 9839 | `i-0a488e9b954c65ada` | 2d → **2d** | 03:23:10Z | `sir-sqjfp5ah` | $0.2602 |

`SPOT_RUN_EXIT` 四台全 **0**,**退出码裸读不走管道**(W42 正是在这里把 `tail` 的码读成了脚本的);
**但成败不从退出码断言**:`describe-instances` 四台 **`spot` / `running`** + `LaunchTime` 齐全,
`describe-spot-instance-requests` 四台 **`active` / `fulfilled`**,
`instanceInitiatedShutdownBehavior` **逐台单独查过,全部 `terminate`**。
⭐ 与 W58 不同的是,本轮**没有任何一台的第一次 `RunInstances` 失败** ⇒ 那四个 0
与 AWS 侧的状态**说的是同一件事**(W58 的 `SPOT_RUN_EXIT[9360]=0` 说的只是"环最终把机器放下了")。

**⭐ AZ 分散 4/4,且四个 AZ 全部按请求给足、零 re-aim、零 `InsufficientInstanceCapacity`**
—— 自 W55 以来第一次满分,也**直接回答了上一轮交棒 ④(GH #403)**「四 AZ 分散这次连申请
都没被满足」。⚠️ **但它不改变任何承重的东西**:W55(2b+2d 同一秒)与 W57(2a+2b 同一秒)
**已经正面证伪过「一次 AZ 事件只损失一粒」**,所以 4/4 分散**不是**种子间独立性的证据。
**没有任何种子数或补跑围栏的算术压在分散上**,这是设计,不是疏漏。

**槽位间隔 8 秒**(W58 是 18s),是在册最紧的一次 ⇒ 下一轮解锁锚点 `2026-09-09T09:23:10Z`。
⚠️ **不要抄这个时刻 —— 跑 `wave_throttle.py` 读它自己打的那一行。**

**发波记录**:`iterations/reports/batch-desk/waves/W59_wave.json`。
棘轮 `python3 tests/test_wave_gate_keys.py` ⇒ **`WAVEGATE_EXIT=0`,`373 checks, 0 failed`**。
⭐ **但它第一次跑是红的,而红的正是本台自己**:本台把闸 (ii) 那个键写成了
`gate_ii_new_signal`,而别名表要的是 `['new_to_test','new_to_be_tested']`
⇒ `FAIL W59 records gate (ii), something new to test ...`。
**这就是那个棘轮存在的全部理由 ——「一次静默改名」**,而它**当场抓到了**;
改回 `gate_ii_new_to_test` 后 373/0。**登记为门起作用的一次,不是登记为本台没写错。**

## 七、泄漏

`LEAK_EXIT=0`。全账号(**不按 tag 过滤** —— 预算本身也没有 filter,GH #515/#528)
`running,pending` = **恰好 4 台,且四台全是 W59 自己的**,全部 `spot`。
**无第五台、无孤儿、无外来负载**(region 内 `c7a`/`r7a` 自 09-06 起 0 条,见第二节)。
常设成本仍只有 AMI `ami-0a990a26d89c66547`。
⛔ **GH #515/#528 的立案理由一字未变**:`wave_fence.py` 第一行仍逐字打
`budget filters : none <- so every instance in the account does land in this budget, whoever owns it`。

## 八、胜负通道 / 收割义务

- **收割义务**:`harvest_owed_at_launch = NONE`。S3 `soak/` 前缀扫描确认 09-08 的
  **十六个**前缀(W55 `032113–032120`、W56 `092131–092138`、W57 `152158–152216`、
  W58 `212233–212252`)**全部已被前面几轮 `recover_verdict.py` 重算过**,无未收割语料。
  W58 的那一份收割义务本轮以 `reclaim_blind.py` 复跑兑现(第三节 (iv))。
- **步骤 4a 复核**:`queue.json` **98 条里没有任何一行要求新波次、专机或新机时**
  (`pending` 的 hero-*/strategy-* 逐条自己写着 `NO NEW WAVE NEEDED` / `NO WAVE` /
  `零 EC2、零新波次`,或是搭车项)。⇒ 本轮走章程 (b) 的例行"测试版 vs 稳定版"。
- **GH #352(胜负通道)**:本轮是发波轮,**不产生新的 `winrate` 读数**,不登记新的
  `DEGENERATE` 计数。上一轮的少数侧占比序列 `0.0645 → 0.0245 → 0.0000 → 0.1552`
  仍是最新读数,**铁律 2 条件 (b) 结构上仍无支撑**(第 16 天)。

## 九、自检读数(铁律 10)

自检**在本轮收尾时仍在跑**(无 `timeout`、后台、重定向)。**读得到的腿照登,读不到的不声称;
最终 `SELFCHECK_EXIT` 见文末追记。**

- **锚点腿**:6/6 OK(`stable-v1` … `stable-v6`,`stable-v6 ids=slotpush` `PINNED f0474dbbcb44`)。
- **promote 约束腿**:`181 distinct live gate id` / **18 个 `PROMOTED` 注** / **`FROZEN none`**
  (没有活体 gate 指名一个已 promote 的 id ⇒ `pullcad` 陷阱本轮未复发);
  promote 原子 **5/5 全 GATED**。
- **⛔ python trunk RED**:`112 passed, 2 failed, 2 uncertifiable`,
  `failed: tests/test_bots_walk_farm_only.py tests/test_carrier_terms.py`。
  工具逐字 `TRUNK RED -- a python test is failing ON THE WORKING TREE.` 并自己补一句
  `Whether main is red too is NOT established by this line: re-run after git stash.`
  ⚠️ **本台不靠推断补这一句**:本会话 `bots/` / `game/` / `tests/` / `tools/` **一行未改**,
  开工时 `git status` 干净且 `HEAD == origin/main == 66a8a15b` ⇒ **工作树就是 main**。
- **⛔ Lua 检测器腿 RED**:`test_coarmed_attribution_register.lua`。
- **两条 `UNCERTIFIABLE`**:元测试的 4 个 run **没在 120s 内跑完**,工具逐字
  `9 check(s) did not run; this is NOT a pass.` ⇒ **那 9 项本轮没人看过,不声称。**

### ⭐⭐⭐ 三条红里有两条是同一个缺陷族,而触发它的是一次**正常的退集**

| 文件 | 断言 | 逐字失败行 |
|---|---|---|
| `tests/test_carrier_terms.py:73` | `check(len(ids) >= 40, ...)` | `FAIL test_set.md line 2 parsed as an arm string (39 ids)` |
| `tests/test_coarmed_attribution_register.lua:501` | `assert(n >= 40, ...)` | `parsed only 39 armed id(s) ...; the set was 45 on 2026-08-29` |

两条测试的**立案句**都写着「解析不能静默截断」,**实现出来的却是「这个集合永远不许小于 40」**
⇒ **任何一次退集都能把它们打红,而它们要量的那个东西一点没变。**
触发它的是 `ffb0248 director: roamidle and campsel returned out of the armed set (41 -> 39)`
—— **退集是总监的裁定权,本台不主张它错**;主张的是**没有任何一方的流程写着要检查另一方**。
⭐ **而这两条断言分不出来的两种情况,本轮恰好可以现场分开**:第 2 行现算是
**39 id / 354 字节 / md5 `433070c6…`**,与第 3 行散文**逐位一致** ⇒ **不是截断,集合真的是 39。**
这是 **GH #584**(`test_carrier_hero_guard.py` 硬编 `chaos_knight`,被 `ckpush` 的 promote 打红)
与 `test_corpus_scale.lua`(硬编语料规模)之后,**同族的第三、第四个实例,四天之内**。

**第三条红不同族,一并登记不混为一谈**:`tests/test_bots_walk_farm_only.py`
(`8 checks, 1 failed [230 commands executed, 27 unresolved]`)逐字点名新落地的
`tests/test_blind_a_roamidle_campsel.lua` 里一条 `io.popen` 静态解析不了,
**处置是照它自己给的话把那条命令加进 `UNRESOLVED_HAND_READ`**。

## 十、交棒

① ⭐⭐⭐ **总监 —— trunk 三条红,其中两条是同族的「写死活体登记表规模」**(见第九节),
   被一次正常退集(`42 → 39`)打红,本周第三、第四个实例。本轮开 `[bug]` issue,
   并给出可验收的拆法(截断这一半 vs 规模这一半分开断言;规模那半用
   `tests/corpus_scale.lua` 的 `ratchet()`/`universal()`/`corpus()`,不写死整数)。
② ⭐⭐⭐ **总监 —— GH #454 (乙1) 欠到第五轮,但本轮它欠的理由第一次是量化的**(见第二节):
   09-08 的 `SpotUsage` 与 `PublicIPv4` **两条独立的行三小时零字节变动**,
   分别给出「已落 51%」与「已落 57.8%」。⭐ 同时 **09-07 那个 18% 价差的三个候选机制里,
   (乙)「当日没落全」被削弱了**,剩 (甲)「SIR `UpdateTime` 滞后 ⇒ 机时被高估」独自扛着 ——
   **若 (甲) 成立,本台一直在用的「机时 × AZ 报价」价格侧估算系统性偏高**(09-07 高 33%),
   高估**朝安全侧失效**,但按 GH #454 (丙) 先例**必须登记**,因为它不会自己举手。
   ⚠️ 削弱**不是**结清,理由写在第二节 (2) 的诚实边界里。
③ ⭐⭐ **总监/harness(#403)—— 上一轮那件事本轮自愈了,但不要据此收案**:
   W59 四个 AZ **全部按请求给足、零 re-aim**,是自 W55 以来第一次 4/4。
   **一次给足不改变 W55/W57 已证伪的那件事**(同秒双 AZ 事件),
   所以「分散」仍然**不能**被写进任何承重算术里。请裁的是**后者**,不是前者。
④ ⭐⭐ **总监/harness(#591)—— 写死的 `40.0 min` 第五轮零信息**:闸头仍逐字打
   `measured bracket 34.80 < x <= 40.63, margin above the constant 0.63 min`,
   **那是存档常数却长得像本波测量**;上一轮已把它收紧到距上界 **38 秒**。
⑤ ⭐⭐ **总监 —— 家族深度封死结构的第四个连续测量点**(见第四节):
   W56/W57/W58/W59 四波、四个不同的 `(臂串 × 树)` 家族,**每个都停在深度 1**。
   上一轮已开 `[batch]` issue,本轮**不重复开**,只补测量点。
⑥ ⭐ **录像组(#352)—— 三笔条件 (a) 仍欠着**(`campgrade` / 61-id 家族(W49) /
   `campbind`+`zusboltdom`(W52)),语料都在 S3。胜负通道第 16 天,
   **铁律 2 条件 (b) 结构上仍无支撑**。
⑦ ⭐ **owner —— `$50` 仍越线**(MTD `$69.465`,`AWS state=ALARM`),
   其中 **`$37.4355`(55%)不是本项目花的**且**已停在 09-06 不再增长**;
   扣掉它本台自己约 `$32.03`,离 `$90` 还有 `$58`。**预算过滤器仍 `null`,本台不擅改。**
⑧ ⭐ **本台下一轮 = 收割轮**,解锁 `2026-09-09T09:23:10Z`(**不要抄,跑 `wave_throttle.py`**)。
   收割须知:(a) 臂串**收割那一刻现算**(⚠️ 本轮 39 id / `433070c6…`,而**退集节奏
   快于发波闸**,到收割时 `origin/main` 第 2 行很可能又变了 —— 上一轮正是在这里
   差点拿一个机器从没跑过的臂串重算,**钉的是发波树 `66a8a15b` 的第 2 行**,GH #631);
   (b) **第一手重读四台 SIR `Status.UpdateTime`**,⛔ **不要抄 `sir_fulfilled_at`**
   (那条路会给每台算出 ~62 秒存活并凭空造出 `BRACKET VIOLATED`);
   (c) `recover_verdict.py` **四个前缀各自子目录不压平**(GH #225);
   (d) GH #454 **两条腿无论是否触发都要报**。
⑨ ⭐ **本台 —— `rec_slot_baseline` 并入仍欠着**(零成本),本轮未做。

## 十一、铁律 6/11

- **静态门**:`bash tools/agent/luacheck_gate.sh` ⇒ **`GATE_EXIT=0 CLEAN`**,
  逐字 `luacheck bots game: 0 warnings`。**未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行。**
- **推送门**:`ARM_GATE_EXIT=0`(`push gate armed (core.hooksPath=.githooks, already set)`)。
- **动态半**(`lua5.1 tests/run_tests.lua`,~100min,GH #124):**未跑,不声称**。
- **本会话 `bots/` / `game/` / `tests/` / `tools/` 一行未改**。改动只在:
  本报告、`waves/W59_wave.json`、`iterations/data/seed_roster_index.json`
  (`seed_roster_index.py --build` 的产物,闸 (ii) 选种腿要求它先重建)、本章程"当前状态"节。
- **铁律 11**:全程**无任何 `requires approval`**,**零空转** ——
  等解锁那 8 分钟是**发波脚本内部的有界轮询**,15 次每次都打自己的读数;
  自检放后台不设 `timeout`,本台没有坐等它。
- **`PUSH1_EXIT` / `PUSH2_EXIT`** 见文末追记。

## 十二、token 用量

`TOKENS total_in=11,286,869 out=58,018 turns=84`(到统计时刻为止;统计后的收尾回合不计入)。

---

## 追记(03:4xZ,收尾之后)

- **自检最终读数:`SELFCHECK_EXIT=3`**(逐字 `selfcheck worst exit: 3`,`legs run 10`)。
  `FINDINGS (exit 3) : cadence owed-executions trunk-red(python) trunk-red(lua)`
  —— `cadence` / `owed-executions` **归因属总监范围,本台只登记不做手工归因**(GH #267);
  `trunk-red(python)` / `trunk-red(lua)` 即第九节那三条。
  `UNCERTIFIABLE (exit 2): none`;
  `NOT RUN (inside a leg): tests/test_luacheck_gate_soakswitch.py tests/test_selfcheck_lua_leg.py`
  ⇒ **这两项本轮没人看过,不声称。**
  ⚠️ **差分**:上一轮同一行是 `cadence owed-executions`,**本轮多出两条 trunk-red**;
  上一轮的 `UNCERTIFIABLE: trunk-red(python)` **本轮变成了确定的 FINDING**
  ⇒ 那条腿上一轮"没跑成",本轮**跑成了,而且是红的**。
- ⛔ **一条必须写下来的读数陷阱,本轮当场发生**:harness 给这条后台命令报的是
  **`[exited with code 0]`**,而**那是命令链末尾 `tail` 的码,不是自检的**。
  自检自己写下来的是 `EXIT=3` 与 `selfcheck worst exit: 3`。
  **⇒ 本节引的是脚本自己打的两处,不是 harness 报的那个 0**(evidence-discipline 3,
  与开工第一条命令被工具拒答的那次是同一条纪律的两面)。
- **本轮开出的 issue**(#290 顺序:三件产物先 push 上 main **`dbed90f6`**,之后才发表;
  草稿跑过 `bash tools/agent/claim_precheck.sh` ⇒ **`PRECHECK_EXIT=0`**、`clean`、
  `paths cited 6 / resolved on trunk 6 / refused 0`、**`local commits not on origin/main: 0`**):
  **GH #650 `[bug]`** —— 两个测试用写死的 `>= 40` 给活体臂串把门,一次正常退集(42→39)
  就打红;GH #584 缺陷族的第三、第四个实例。
- **推送读数**:**`PUSH1_EXIT=0`**(`claude/great-sagan-1jm3xq`,新分支)/
  **`PUSH2_EXIT=0`**(`66a8a15b..dbed90f6  HEAD -> main`,**未被拒、无需 rebase**)。
  ⭐ 提交发生在发波**之后**,而 `--ref` 钉的是 **`66a8a15b`** ⇒ **四台实例克隆的仍是发波那棵树**,
  本轮的收尾提交进不去(这正是不写 `main` 而写 40 位 SHA 的理由)。
