# 批测台 2026-09-13T18:13Z — 持有第十一轮

**一句话**:零发波、零收割欠、零泄漏、**本轮 AWS 新增计费 `$0.00`**(EC2`$0.00` + CE`$0.00`)。
⭐⭐⭐ 头号是一条**零成本的结清**:GH #801 的自动复核新分支**第一次在真实 budgets 读上被执行**,
四行整块如实打出,欠条 `gh801_confirm_headroom_first_live_read` 的针脚本轮落到本台章程里。
⭐⭐ 第二:本轮**第一次出现「三道读数全部与上一轮逐位相同」**,而这**不是**一条独立确认 ——
`budget refreshed` 戳未变 ⇒ 同一张快照被读第二次(上一轮交棒 ② 立的判据当场生效,**挡住了**一次本会误报为「没长」的比对)。

---

## 一、成本(现跑,`$0.00`)

`AWS_SETUP_EXIT=0`(容器无 CLI,自装 `aws-cli/1.46.1`;身份 `arn:aws:iam::581436534924:user/dota2bot-agent`,
逐字 `(restricted batch-runner user — correct)`)。

`COST_EXIT=0`,⛔ **未走管道读码**,⛔ **未用 `COST_CONFIRM_AT=999`**(见 §二):

```
89.13	USD
   source: budgets/dota2bot-batch (free) | forecast 181.993 | budget limit 100.0 | budget refreshed 2026-09-13T14:36:55Z
```

- MTD **`$89.130`**;`forecast 181.993` / `limit 100.0` **登记不据以决策**。
- ⚠️ **`budget refreshed 2026-09-13T14:36:55Z` 与上一轮(15:17Z)是同一张快照**,已 **3.61h** 未刷新
  ⇒ ⛔ **本轮 MTD 是同一张快照被读第二次,不是第二次测量** ⇒ ⛔ **不做任何 MTD 增量归因**(章程步骤 2 (乙))。
- ⭐ 这条判定**正是上一轮交棒 ② 立的判据**(「由 `budget refreshed` **戳变了**认出来,
  ⛔ 不由『CE 总额没变』认出来」)。本轮是它**第一次挡住一次会出错的比对**:
  MTD、CE 复核值、S3 对象数**三条全部与上一轮逐位相同**,朴素读法会得出「三小时零增长」——
  而真相是**这三小时根本没有被测量过**。⇒ 登记为「无读数」,不是「零增长」。

## 二、⭐⭐⭐ 头号:GH #801 第一次在真实读上执行,欠条结清(零成本)

上一轮(15:17Z §五/§六)本台用 `COST_CONFIRM_AT=999` 临时自救;总监同轮按 GH #801 把它**落进脚本默认行为**,
并明令 ⛔ **不要再用 `COST_CONFIRM_AT=999` 自救**(那条路省的钱一样,但**恢复条件只活在人的记忆里**)。
**本轮按新章程用默认值真跑**,四行整块逐字(与 `RULE6_BYPASS` 那行同规格,照抄):

```
   >= $35, but headroom to the $90.00 brake is $0.870
   < $1.10 (cheapest wave) — CE confirmation SKIPPED, NOT passed (GH #801).
   Nothing can launch at this MTD, so confirming it buys nothing.
   Resumes by itself as soon as headroom >= $1.10.
```

⚠️ **这是 SKIP 不是 pass** —— 本轮 MTD **没有**经过 CE 复核,`$89.130` 只有 budgets 一个来源。
⭐ **结清判据**:`owed_executions.json` 的 `done_when` 钉的是针脚串
`Resumes by itself as soon as headroom` 出现在 `iterations/streams/batch-desk.md`
(立行当日 `grep -c` = **0**,且 `COST_CONFIRM_AT=999` 那条路**打不出它**)——
本轮已随「当前状态」节整块抄入 ⇒ **针脚落地**。
⛔ **本台不自行把该条移进 `retired`**:欠条的肯定半(「它跑得对不对」)逐字归总监
(`mention is not correctness`),本台只交付被执行这一半。

⭐ 顺带一条对总监有用的第一手:该分支立行时的自述是「**新分支在真实 budgets 读上一次都没被执行过**」
(总监未花 AWS 的钱,`$89.130`/`$0.870` 全抄自本台已付费读数)。**本轮是它的首次真实执行**,
且**打出的 headroom `$0.870` 与总监抄写的值逐位相同** ⇒ 该分支在真实读上的行为与六个变异体覆盖的纯逻辑一致。
⛔ 这是 **n=1 次执行**,不主张任何等价性(GH #692 立案教训:覆盖不是等价)。

**本轮 CE 支出因此是 `$0.00`** —— 对比:09-08~09-12 实测本台 CE 均值 `$0.116/天`,
且在本月第一个零批测日占已发布支出的 **53.1%**。

## 三、闸(裸读,逐字)

**(i) `THROTTLE_EXIT=0`**:
```
anchor wave      : W69 (4 machine(s))
last machine up  : 2026-09-12T09:26:31Z  <- the anchor (GH #469 ruling 1)
slate spread     : 7s
unlock           : 2026-09-12T15:26:31Z
now              : 2026-09-13T18:13:43Z
margin           : +96432s past unlock
```
⚠️ **(i) 开着不构成许可**(连续第六轮点名);另抄工具自带限定
`RE-RUN IT AT THE LAUNCH INSTANT: it is the action, not the decision, that the gate binds.`

**(iii) 裸跑 `FENCE_BARE_EXIT=0`**:`actual (MTD) : $89.130` / `pending waves : $0.000` /
`planned : $0.000` / `projected total : $89.130` / `fence : $100.00` / `brake : $90.00` /
`operative ceiling: $90.00 = min(fence, brake)` / **`headroom : $0.870 after this wave`** /
`WAVE_FENCE: CLEAR (exit 0)`。

**(iii) 带最便宜一波 `--planned 1.10` ⇒ `FENCE_PLANNED110_EXIT=3`**,逐字:
```
A launch at this instant would put MTD past the $90.00 BRAKE line.
WAVE_FENCE: THROTTLED (exit 3) -- brake, not fence. Stop and report to the owner; do not wait it out.
```
⇒ **连续第三轮是闸拒绝,不是本台的处置**。余量 `$0.870` < 最便宜一波 `$1.10`,差 **`$0.230`**。
⛔ 未用 `--no-accrual-check`、未传 `--director-crossing` / `--pending`。

## 四、为什么本轮没买那个 `$0.01`

上一轮交棒 ② 指定的测点是**零批测日完整那一行**(「一波都不发时这条线一天涨多少」,
把 0.6–7.5 天的区间收成一个数)。按本台实测滞后(**≥19.9h 且 ≤≈21.0h**),它
**`2026-09-14T02:00Z` 之后**才第一次完整可得;本轮 `18:13Z` **在其之前**。
且 §一 刚证明**快照本身未刷新**,而本台 09-12T18:20Z 已证 `check_costs.sh` 的复核调的是
`ce --granularity MONTHLY`、与预算 `ActualSpend` **同量且 CE 并不领先快照**
⇒ 现在拉逐日表**必然拿到与上一轮逐位相同的一张**,买的是一个**必被误读的读数**。
⇒ ⛔ **没花**,登记「仍欠,判据与时点不变」。

## 五、收割:零欠

`S3VAL_EXIT=0`(⛔ 未走管道读码)。`validation/` **592 对象**,与上一轮基线**逐个相同**,
当日最新对象仍是 W69 末个(`2026-09-07` 之后无新增 run.log;W68/W69 已于 09-13 03:11Z / 06:15Z 两轮全量收割)
⇒ **无 W69 之后的新数据,本轮未新增任何 verdict**,零下载、零 S3 支出。
⛔ 不重算已收波次。

## 六、泄漏:四条独立路径全零(⛔ 不合并成一句)

1. `check_costs.sh` 的 `running/pending instances` 区块 **空**;
2. `describe-instances` 五态(`pending,running,stopping,stopped,shutting-down`,**不加 tag 过滤**)⇒ **`[]`**,`INST_EXIT=0`;
3. `describe-volumes` ⇒ **`[]`**,`VOL_EXIT=0`(零孤儿 EBS);
4. 闸 (iii) 自带账户级普查:`accrual scope : 17 region(s) read <- COMPLETE: every region this account has enabled`
   + `accrual check : CERTIFIED (0 accruing instances account-wide, read this run) -- pending $0.000 is a reading, not a default.`

常驻仍只有 AMI `ami-0a990a26d89c66547`(`dota2bot-batch-20260718`)。
⛔ **#515/#528 立案理由一字未变**:`budget filters : none  <- so every instance in the account does land in this budget, whoever owns it`。

## 七、GH #779:第十轮零表态 —— 本轮**不发评论、不发通知**,并说明理由

现拉 #779 全部评论:**9 条,全部是本台自己发的**(15:12Z/18:20Z/21:13Z/00:13Z/03:11Z/06:15Z/09:08Z/12:14Z/15:17Z),
**零 owner 表态、零 reaction**(⛔ `author_association: OWNER` 只说明评论走 owner 的 token,不可读成已答)。

⭐ **本轮按上一轮交棒 ① 的逐字约束执行**:「**有新测量才发评论,没有就只在报告里登记**」。
§一 已证本轮**在成本线上没有新测量**(同一张快照)⇒ **不发第十条同形评论**。
⭐ **同一条逻辑本轮首次延伸到通知路径**:12:14Z / 15:17Z 两轮已把三条选项推过手机/邮箱,
而本轮读数与 15:17Z **逐位相同** ⇒ **再推一次是同形投递,不是处置**,与「往 #779 堆同形评论」同族失效。
⇒ **本轮不推通知**,并把下一次推送的判据写死在交棒 ①。

**owner 那道题的状态(不变,不重复前九条的推导)**:余量 `$0.870`,最便宜一波 `$1.10`,
⇒ 九月剩余 17 天无算术路径;不选的默认结果是「九月一波都不发」,
包含 `OWNER_PRIORITIES.md` 4.1 那条逐字「排在任何 arm 波之前」的 upstream 标尺波。

## 八、铁律 9 优先项

`OWNER_PRIORITIES.md:93-103` **4.1 upstream 标尺波**仍挂「最高优先 / 批测台下一轮就发」,
**本轮仍未认领,原因是钱不是排期** —— 它同样要过闸 (iii),而 (iii) 对 `$1.10` 本轮 `exit 3`。
⭐ **owner 一说「发」(且刹车线动了),本台下一轮发的就是它**,⛔ 不是普通 arm 波。

`queue.json` **120 条**(`pending 46`、`harvested_pending_verification 29`、`ruled_approved_scan 11`、
`done 9`、`harvested 6`、`frozen_hold 5`、`delivered-and-consumed 4`、`running 3`、`ruled 3`、余各 1),
**无一条要求本轮发波** —— pending 项全部堵在同一道闸上,⛔ 与前十轮同形,不逐条重列。
⚠️ **不自裁的观察再登记一次**(连续第三轮):`running 3` 与账户级 `CERTIFIED ZERO` **不相容**
(没有实例在跑却有三条挂 `running`),⛔ 本台不替它们改状态,只点名交总监。

## 九、本会话改动面

⛔ **`bots`/`game`/`tests`/`tools` 一行未改** ⇒ **无一条 trunk red 由本轮引入**。
产物只有 `iterations/` 下的本报告与章程「当前状态」条目(后者同时是 §二 欠条的针脚投递)。

## 十、自查

- ⭐ **管道坑第 27 次,仍是当轮第一条命令**,脚本自卫当场拦下,逐字
  `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.`,
  并自带 `it has recurred 5x, every time as the first command of the round.`
  **当场重定向重跑,零损失** —— 但**连续 27 轮同一位置**,拦它的一直是工具不是纪律。
- ⚠️ **超时坑第三次,而本轮只犯了一半**:第二跑给了 `timeout 400`,又被砍在
  `=== trunk health (python test suite) ===` 那条腿上 ⇒ ⛔ **没跑完不是通过**。
  ⭐ **与前两轮不同的是本轮当场改正**:立刻改用 `run_in_background` 重跑、**不设任何 `timeout`**
  (正是第 24/25/26 次交棒逐字要求的做法),读数见下。
- 已跑完的腿实读:`promote-atom constraints: OK`;
  `INVERSE-GATE CENSUS  live gate ids 214  armed 26 / FROZEN 0  COUPLED 1  UNRESOLVED(armed) 0`;
  `no armed id hangs under an unarmed gate -- OK`。
- **铁律 11:零 `requires approval`、零空转**(自检后台跑,成本/闸/收割/泄漏并行推进)。
- ⛔ 本轮无任何 `RULE6_BYPASS` 行(见「补记」)。

---

## 交棒

① ⭐⭐⭐ **owner —— GH #779 第十轮零表态,刹车第十一轮持有。本轮的变化是「本台停止同形投递」**:
   评论与通知**都不发**(理由 §七:无新测量)。
   **下一次推送的判据写死**:`budget refreshed` 戳变了 **且**(headroom 跨过 `$1.10`
   **或** 零批测日完整行第一次可得 **或** MTD 越过 `$90`)。三者之一,否则只在报告登记。

② ⭐⭐ **下一轮本台 —— 零批测日测点**:`2026-09-14T02:00Z` 之后第一次完整可得,
   把 15:17Z 那个 **0.6–7.5 天**区间收成一个数。
   ⚠️ **先读 `budget refreshed` 戳**:戳没变 ⇒ 逐日表也是旧的一张,**别花那 `$0.01`**(§四 本轮即如此)。

③ ⭐⭐ **总监 —— GH #801 欠条 `gh801_confirm_headroom_first_live_read` 的执行半已交付**:
   针脚串已随「当前状态」落入 `batch-desk.md`;首次真实执行的 headroom `$0.870`
   与裁定文书抄写值**逐位相同**。⛔ 肯定半(`mention is not correctness`)与 `retired` 迁移**归总监**,
   本台未自行移动该条。

④ ⭐ **总监 —— `queue.json` 的 `running 3`** 与账户级 `CERTIFIED ZERO` 连续第三轮不相容,本台不代改。

⑤ ⚠️ **下一轮本台 —— 开工第一条命令**:`routine_selfcheck.sh` **重定向 + `run_in_background` + 不设 `timeout`**,
   一次到位(本轮仍是分两步才做对,第 27 次)。

---

## 补记(push 之后,GH #290 顺序)

**(A) 铁律 6 三条腿**(两次 push 逐字相同):`luacheck bots game: 0 warnings` /
**`GATE_EXIT=0  CLEAN (iron rule 6 static half passed)`** /
**`py gate: 101 ran, 0 findings, 0 uncertifiable, 43.8s`**(自带 `scope: 84 fast python ratchets`;
自带 `NOT claimed here: 30 slower python tests ... and the Lua dynamic half (GH #124)`;
⭐ 自带 `17 new test(s) not in the manifest were run anyway`,含 `tests/test_check_costs_confirm.py`
—— 即 GH #801 那 35 checks/六变异体,**本轮随 push 顺带跑过且 0 findings**) /
**`lua gate: SKIPPED BY SCOPE -- this push touches no bots/game/tests path.`**
⚠️ 自带逐字 `This is a SCOPE decision, not a pass and not a skip of a gate that had something to say (GH #624).`
⇒ **第三条腿是作用域判定,不是绿**。`ARM_EXIT=0`(`push gate armed (core.hooksPath=.githooks, already set)`)。
⛔ **未用 `RULE6_BYPASS` ⇒ 无任何「SKIPPED, not passed」行**。动态半(GH #124)不在钩子里也不被它声称。

**(B) push**:`PUSH1_EXIT=0`(`[new branch] claude/great-sagan-2dok6u`);
`PUSH2_EXIT=1` 被拒(`remote contains work that you do not have locally`)⇒
`git pull --rebase origin main` **`REBASE_EXIT=0`**(`d861f72f..2ad917cb`,`Rebasing (1/1)` 成功)⇒
重推 **`PUSH2_EXIT=0`**(`2ad917cb..58c22886  HEAD -> main`)。⭐ 本轮**有**推送竞态,一次 rebase 化解。

**(C) ⛔⛔ 开工自检本轮 UNCERTIFIABLE —— 没跑完不是通过(如实登记)**。
两次运行**都没打出收尾横幅**,`grep -c SELFCHECK_EXIT /tmp/sc2.log` = **0**,
两次都停在 `=== trunk health (python test suite) ===` 这条腿上。
⇒ **本轮 trunk 的那一侧没人看过**(铁律 10 (ii) 逐字:读到 2 的那一轮,trunk 的那一侧这轮没人看过)。
⛔ 本台**不把已跑完的腿读成自检通过**。

**已跑完的腿实读(这些是有效的)**:
- `ARM` = `push gate armed`;
- **`unlanded-commit scan`**:`OK: no unlanded work in the certifiable window.`
  ⚠️ 自带限定照抄:`shallow clone : YES` / `REFUSED refs : 1126 of 1150 (uncertifiable...)` /
  `certifiable refs : 24` ⇒ **这个 OK 的作用域是 24 个 ref,不是 1150 个**;
- **`report cadence + published citations`**:**`2 finding(s)`** ——
  `GAP cadence director  between: 09-13T09:00Z -> 09-13T13:14Z  4.2h`;
  `GAP cadence replay-check  between: 09-13T12:59Z -> 09-13T16:30Z  3.5h`。
  ⛔ **报出来的是问题不是判决**(工具 LIMITS:那一轮可能本来就没东西可交),本台只登记不代裁;
  ⭐ **两条都不是批测台**(本台 03:11/06:15/09:08/12:14/15:17/18:13 六轮无洞);
- `promote-atom constraints: OK`(`scanned: 219 distinct live gate id(s), 19 carrying a PROMOTED note`;`FROZEN none`);
- `INVERSE-GATE CENSUS  live gate ids 214  armed 26 / FROZEN 0  COUPLED 1  UNRESOLVED(armed) 0  other 213`;
  `no armed id hangs under an unarmed gate -- OK`;
- `stable version anchors (铁律 3)`:`9 anchor(s) checked -- OK`。

**(D) ⭐⭐⭐ 新坑,而且是本台修上一个坑修出来的(交下一轮 + 总监)**。
第 24/25/26 次交棒逐字要求「**不设短 `timeout`**」,本轮照做改用后台重跑 ——
**但没有先把第一次那个被 `timeout 400` 移进后台、仍然活着的进程收掉**。
实测形状:`ps` 同时有**两个 `bash tests/run_py_tests.sh`**(etime `06:25` 与 `01:19`)
在同一棵树上并发跑,各自 fork `lua5.1 tests/run_tests.lua ...`。
⚠️ **自检自己就警告过这个形状**,第一次运行的 Lua 腿逐字打过:
`and it is NOT evidence that trunk is red: re-run on a quiet tree (nothing writing under bots/).`
⇒ **「短 `timeout`」被修好了,代价是换成了「并发跑」**,而并发这条**污染读数而不是截断读数**,
比被截断**更难发现**(截断会少一段,污染会给出一段看起来完整的假读数)。
⇒ **下一轮的正确做法是三件而不是两件**:(1) 重定向(⛔ 不走管道);(2) `run_in_background`,⛔ 不设 `timeout`;
**(3) 起跑前先确认没有上一次的 `routine_selfcheck.sh` / `run_py_tests.sh` 还活着,有就先收掉。**
⛔ 本台不改 harness(先例 GH #33),仅交棒;**本条与 GH #205/#171 同族**(工具在,但执行方式让它说不出真话)。

**(E) 本轮无 AWS 支出发生在 push 之后**;`bots`/`game`/`tests`/`tools` 仍一行未改。

## 十一、本次会话 token 用量(铁律 8)

```
TOKENS total_in=3,140,382 out=24,227 turns=28
```
⚠️ 统计时刻之后的收尾回合(本节与第二次 push)不计入,量级参考足够。
