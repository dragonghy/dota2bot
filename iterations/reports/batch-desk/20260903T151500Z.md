# 批测台 2026-09-03T15:15–15:5xZ

**收 GH #408 补跑机 → W42 补成完整 4 粒 → 57-id 家族并池到 8 粒。本轮零发波、零新增支出。**

**本轮真正的产出是两件,都不是那八个数**:
① **GH #427 的 1 小时窗口对「按需补跑机」不是「容易错过」,是按构造读不到** —— 本轮亲手撞上并量化了它;
② **57-id 家族本轮买到了它的第 8 粒 ⇒ 闸 (ii) 的第三支自己熄火了**,而另外两支经测量都不成立 ⇒ **W43 不发**。

---

## 一、自检(铁律 10)

⚠️ **开工第一条命令又误用管道**,自检当场自拒:
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.`
**同形第十九次,又是当轮第一条命令。** 工具自报仍是 `it has recurred 5x`,与报告序列对不上,照上轮据实并列。

改走重定向重跑(`> /tmp/sc2.log 2>&1; echo "EXIT=$?"`)。

⛔ **第一次重跑用了 `timeout 300`,`EXIT=124`** —— 超时被杀在 `=== trunk health (python test suite) ===` 那一腿。
**124 不是通过,也不是发现**;当轮改用 `timeout 1500` 后台重跑。

⚠️ **本轮自己读错了一次钟,登记**:我一度按「背景任务已跑 35 分钟仍停在 python 腿」判断它挂死,
并据此去查进程树。实际 `date -u` 显示当时距开工只有 **6 分钟** —— 进程树本身也自证在推进
(`test_selfcheck_lua_leg.py` → `lua5.1 tests/_fieldsip_sweep.lua`)。
**错在读钟不在读数据**,与上一轮「把补跑机 14.1 分钟时 `OBJ_COUNT=0` 误读成症状」**同形第二次**。
⇒ 本台以后凡是要声称「某个东西跑太久」,先 `date -u`,不靠会话内的时间感。

最终读数见文末「自检收尾」节。

---

## 二、成本与围栏

- 九月 MTD **`$7.056`**(免费 `budgets` 通道;快照 `refreshed 2026-09-03T04:41:39Z`)。
  ⚠️ **快照时刻早于 W42 起飞(09:32Z)和补跑机起飞(12:19Z)** ⇒ 两者**都不在这个读数里**,滞后照章程 2(甲) 处理。
- **MTD `< $35` ⇒ CE 未触发,本轮没花那 `$0.01`。**
- 进场 `running/pending` 实例:**空**。
- ⚠️ **增量只登记不对账**:章程 2(乙) 禁用「MTD 增量对得上波费 ⇒ 该波已计入」,它已两次给出错误结论。

**围栏算术(章程 2 甲,逐波按自己的市场取价,近 12h 内的波全部重记)**:
`7.056 + W41 按需 2.15 + W42 spot 0.90 + 补跑机按需 0.65 = **$10.76** ≤ $80`
⇒ **不跨任何 owner 可见告警档,不欠解释行。**
`forecast 111.209` 是 budgets 自己的外推,**不是读数,不进围栏**。

**本轮新增支出 = $0**(未发波;只有几分之一美分的 S3 GET)。

---

## 三、⭐⭐⭐ GH #427:1 小时窗口对按需补跑机是**按构造读不到**,不是「容易错过」

上一轮交棒第 ⑥ 条写的是「**本台下一轮必做** —— 补跑机自毁后 `StateReason.Code` 只能读 1 小时,**开工先读它**」。
本轮**开工第三条命令就去读了**,结果:

| 读法 | 结果 |
|---|---|
| `describe-instances --instance-ids i-07d6358854ccda217` | `[]`(**exit 0**,不经管道) |
| 全状态普查 `pending,running,shutting-down,terminated,stopping,stopped` | `[]`(**exit 0**) |

**这个码永久没了**,而且**不是本台懈怠**:

- 补跑机 12:19:25Z 起飞,S3 最后一个对象 **13:11:40Z** ⇒ 正常自毁 ~13:15Z;
- ⇒ #427 的 1h 窗口 **~14:15Z 关闭**;
- ⇒ 本 Routine 的**下一次触发是 15:15Z**,**比窗口关闭晚整整一小时**。

⇒ **一个 2 小时 cadence 的 Routine,永远读不到一台「周期中段自毁」的机器的 1 小时窗口。**
这不是纪律问题,是 **cadence × 窗口宽度**的算术。

⛔ **而且这次没有第二个信源**:上一轮 W42 的四台是 **spot,有 SIR**,`describe-spot-instance-requests` 四台全读到;
**按需机没有 SIR**,`StateReason.Code` 是它唯一的码。
⇒ 上一轮交棒第 ⑤ 条(「GH #271 每下一次『一波按需』,就把那一波的死因码押在 1 小时上」)
**一轮之后就兑现了**,而且兑现在**代价最高的那种机器上**(补跑机 = 专门为救一粒种子花的钱)。

**已按 GH #427 / #271 交总监**(见文末交棒)。本台不自行改窗口或改 cadence。

**存活时间只能走下界路线**:`52.25 min`(S3 最后上传 − `launched_at`,`survival_bound='lower'`)。
上一轮实测过这条路线在**正常自毁**上偏短 `3.19 / 3.28 / 3.65` 分钟 ⇒ 真值约 **55.4–55.9 min**,
与「正常自毁、没撞看门狗」一致。
⚠️ 上一轮同时实测到这条路线在**被抢占**的机器上**反号**(偏大 1.17 min);
**该警告在此不适用** —— 这台是按需、且没被抢占。**边界写清楚,不含糊带过。**

---

## 四、⭐⭐⭐ 补跑成功:W42 补成完整 4 粒

`RV_EXIT=0`(不经管道)。`files_seen == games_loaded == 264`、`unparseable 0`、`source_dirs 5`。
⚠️ 5 个 run 各自独立子目录(GH #225 并池形式),`3040` 与 `3040r` **分属两个目录** ⇒ R1/R2 均无从触发。

**种子 3040:`ab10 / ba0` `arm_depth 0.00` `NO-PAIR`(出集) → `ab44 / ba18` `arm_depth 25.55` `SCORED`。**
那 10 局 banked 游戏按种子戳自动并入,与上一轮 `banked_games_note` 的**预登记完全一致**。

**W42 完整 4 粒**(`scored_games 234`、`unfinished 0`、`min_arm_depth 8`、`thin_arm_seeds` 空):

| 量 | 均值 | comps_better |
|---|---|---|
| gpm | **−29.66** | 1/4 |
| xpm | **−16.15** | 1/4 |
| deaths | **+0.21** | 1/4 |
| last_hits | **−1.44** | 2/4 |

`suggested = hold_or_reject`。逐粒 gpm:3040 **−61.18** / 3101 −69.45 / 3117 −8.42 / 3204 **+20.41**。

**分层(工具自打,未手算 —— 4(i-d))**:gpm `ab +11.44 / ba −70.77 / side +41.11`,**`sign_flip` 从 3 粒时的 `false` 翻成 4 粒时的 `true`**。
按 **4(i-c)**:这是侧偏已消除的估计量上的**恒等式**(`|side| > |arm|`),**登记但不据此下结论、也不据此折价读数**。

⚠️⚠️ **一处必须登记的修复产物,不要当成普通读数**:
**3040 的 `gpm_ab = +153.75`、`gpm_ba = −276.11`。**
它的 ab 腿是**两台机器的混合**(被抢占那台的 10 局 + 补跑机的局),而另外三粒每条腿都出自**同一台机器**。
`arm_depth 25.55` 诚实地过了 #269 的门,**但「两台拼出来的一粒」和「一台跑出来的一粒」不是同一种对象**。
**本台不裁,交总监。**

---

## 五、⭐⭐⭐ 57-id 家族并池到 **8 粒**(上一轮说「第 8 粒正由补跑机去买」——买到了)

`RV_EXIT=0`、`games_loaded 487`、`source_dirs 9`、`scored_games 433`、`min_arm_depth 8`、`thin_arm_seeds` 空。
**逐粒 swap-average 后跨种子算术平均,绝不按局数加权(4(i-d))。**

| 量 | 8 粒并池 | comps_better | 上一轮 7 粒 |
|---|---|---|---|
| gpm | **−24.75** | 2/8 | −19.54 |
| xpm | **−15.73** | 2/8 | −12.09 |
| deaths | **+0.24** | 2/8 | +0.21 |
| last_hits | **−1.49** | 3/8 | −1.14 |

逐粒 gpm:−7.18 / **+75.21** / **−61.18**(新) / −69.45 / −8.42 / −32.60 / +20.41 / **−114.77**,**极差 190**,远高于 σ≈30 的单粒噪声底
⇒ **本台不判读效应量。**

⛔ **这份读数取代上一轮那份 7 粒的**(第 8 粒把并池 gpm 从 −19.54 拉到 −24.75)。
**13 条搭波的 queue 行本轮已同步改写**(见七),否则下游会继续读一个已经不存在的数。

**并池合法性本身仍是 GH #449,在总监手上。本台报数不裁定。**

---

## 六、⭐⭐ GH #352:并池**加深**退化,不是缓解 —— 分支 (a) 拿到第二个独立确认

`winrate` 通道在每一种切法下都是 **`DEGENERATE`**:

| 语料 | 少数侧占比 | headroom |
|---|---|---|
| W42 三粒(上一轮) | 0.0243 | 0.0347 |
| W42 四粒(本轮,+58 局新语料) | **0.0265** | 0.0399 |
| W41∪W42 八粒(487 局) | **0.0164** | 0.0237 |

⭐⭐ **局数翻倍把占比从 0.0243 压到 0.0164 —— 方向是恶化。**
上一轮用「只换种子」证伪了**种子集**这条路;本轮用「同语料加倍局数」证伪了**局数/种子数**这条路。
⇒ **`winrate` 的病灶是语料本身(单侧横扫),加语料只会加深它。** GH #352 的「改测法 **且** 修横扫」现在站在**三个互相独立**的证据上。

工具横幅逐字抄录:
> `GH #352 winrate channel DEGENERATE: minority side won 8 of 487 finished games (share 0.0164 < 0.20). The winrate below is bounded to 0.5 +/- 0.0237 and MUST NOT be cited as rule 2(b) support until the channel recovers.`

⇒ 本报告**没有任何一处**把 winrate 当作铁律 2 条件 (b) 的支持。
`winrate_independent_of_gold` 打真值 **`264/264`** 与 **`487/487`**(桶名 bug 确已修)。

---

## 七、⛔ 上一轮点名的「漏掉的活」本轮**没有复发**:13 条 queue 行已同步

上一轮第一条就是「06:15Z 那轮从未把读数写回 `queue.json`」。本轮**在收割的同一个工作单元内**改完:
13 条 `rides_wave` 含 W41 且 `status=harvested_pending_verification` 的行,
`result_wave` / `result` 全部换成**本轮的 4 粒 + 8 粒并池读数**,并显式写明 **supersedes 上一轮的 7 粒数字**、
**winrate 不可引用**、**并池待裁 GH #449**。写入带 `assert`,改动行数自打 **13**。

`strategy-31/32` **第九轮不搭波**(`tormself`/`immguard` 仍不在 57-id 串里,按构造搭不上)⇒ 按铁律 9 继续交回总监。

---

## 八、⭐⭐⭐ W43 **不发**:闸 (ii) 三支**全部**不成立(逐支测量,不是推定)

| 支 | 判据 | 本轮测量 | 成立? |
|---|---|---|---|
| 一 | armed 串变了 | `origin/main` 上 line 2 = **57 id / 501 字节 / md5 `38423b791c05d96e0e16ed0b51bd132d`**,与 W42 **逐字节相同**(md5 当场重算,非抄) | ❌ |
| 二 | 树变了且**对被测配置**有效 | `bots/` 树 `783dc234` → `1927ef47`(**哈希确实变了**),三个 commit,逐个查 ⇒ **全部惰性**(下表) | ❌ |
| 三 | 家族不足 8 粒 | **本轮刚补到 8 粒 / 433 计分局** | ❌ |

**第二支的逐个测量**(这是本轮花时间最多的一处,不能靠「gated 所以没事」一句带过):

1. `bots/mode_farm_generic.lua`(+52):新块整体在 `J.IsModeTurbo() and J.IsSoakCandidate('arbheart')` 之下,
   而 **`arbheart` 不在 armed 串里**(当场 grep 过)⇒ 未 arm 即不进。
   ⚠️ **但同一 commit 还改了门外的一行**:`else` → `elseif preferedCamp == nil`。
   **这一行必须自己证,不能算进 gated 里**:因为上文 `local old = preferedCamp`,
   且未 arm 时**两者之间没有任何东西修改 `preferedCamp`** ⇒ `preferedCamp == nil` ⟺ `old == nil`
   ⇒ 未 arm 时与原 `else` **逐分支等价**。✅
2. `bots/BotLib/hero_zuus.lua`(+62):新函数 `ZeusSupportAghsFirst` 只在
   `J.IsModeTurbo() and J.IsSoakCandidate('zeusaghs5')` 下被调用,**`zeusaghs5` 不在 armed 串里**。✅
3. `bots/BotLib/hero_tinker.lua`(±30,commit `f0c3d375`):**这是唯一一处门外的真改动**
   (`J.Utils.GetItem('item_x')` → `J.Utils.GetItem(bot, 'item_x')`,8 处)。
   英雄组的 commit 声称这 8 处「全部结构性不可达」。**本台不采信也不复核那个声称** ——
   ⭐ **改用一个本台自己能量的判据:tinker 在本语料里出现过几次?**
   逐局读 487 份 `analysis.json` 的 `players` 字段 ⇒ **`npc_dota_hero_tinker`: 0 / 487**。
   ⇒ **无论那个声称对不对,这处改动都不可能影响这个家族的任何读数。**
   ⚠️ **诚实边界**:「tinker 在 487 局里 0 次」只说**这份语料**不受影响,
   **它一个字都没有验证英雄组「结构性不可达」的声称**。那个声称仍归英雄组/总监。

`game/` 树 `f13b532c` **逐字节未动**。

⇒ **三支全灭 ⇒ 闸 (ii) 不满足 ⇒ 本轮不发 W43。**
(顺带:闸 (i) 解锁于 **15:32:03Z**,本轮做发波决定时尚未解锁,**但决定不是由闸 (i) 做出的** —— 是闸 (ii)。)

### ⚠️⚠️ 同时必须交出去的代价:这正是 GH #363/#449 那个老分叉,第三次

第二支的现行判据在实践里被写成「**树对象哈希**」,而**树对象哈希本轮确实变了**。两种读法给出相反答案:

- **(甲) 按 armed 串家族** ⇒ 家族已厚到 8 粒、树改动对被测配置全惰性 ⇒ **不发**(本台所取)。
- **(乙) 按树对象哈希** ⇒ 树变了 ⇒ 家族重置回 0 粒 ⇒ **发**。

⭐ **本轮这个分叉比 47-id 那次更容易裁,理由是算术**:
若按 (乙) 发一波,那一波**在算术上无法测量触发它的那三个改动** ——
两个 gated id 未 arm(测不到),tinker 在语料里 0 次(测不到)。
**一个「无法测量触发它的改动」的波次,买不到任何东西,只花 ~$0.90。**
⇒ 本台按**保守默认**(不花钱的那一侧)取 (甲),**并把这个分叉连同上面这条算术一起交总监**,请与 GH #449 一并裁。

---

## 九、其余门与登记

- **`reclaim_blind.py --wave-json W42_wave.json` ⇒ `RB_EXIT=0`**:
  `changeover 40.0 min (measured bracket 34.80 < x <= 40.63, margin 0.63 min)`、
  `yield 3 paired of 4`、`attribution 1 machine reclaimed before the flip`、
  `VERDICT: not blinded`、**`NEXT WAVE: spot`** ⇒ **W43 起飞时仍走 spot**(owner GH #158 默认不变)。
  ⭐ 上一轮踩的键名坑本轮**未复发**:回填时 `ab`/`ba` 与 `ab_games`/`ba_games` **两套键名都写**。
- **铁律 6 静态半:`bash tools/agent/luacheck_gate.sh` ⇒ 真码 `LUACHECK_GATE_EXIT=0`**,
  自打 `luacheck bots game: 0 warnings` / `GATE_EXIT=0 CLEAN`(容器缺 luacheck,脚本自己装上了)。
  **未用 `RULE6_BYPASS` ⇒ 本报告无「SKIPPED, not passed」行。**
  `arm_push_gate.sh` ⇒ `ARM_EXIT=0`,`core.hooksPath=.githooks` 已上膛。
  **动态半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)未单独跑,不声称。**
- **`bots/` / `game/` 本会话一行未改**(改动全在 `iterations/`)。
- **零 pudge 第八波**:逐局读 `players`,**0 / 487** ⇒ `strategy-26` / `rotscope` 本波仍写「无载体」,
  **不许写成负向读数**。
- **`ckpush` / `roshdist` 的条件 (a)**:本轮**未重查**(上一轮已确认 487 份 `analysis.json` 顶层无帧级检测器字段
  ⇒ **UNINTERPRETABLE(没有量具),不是「测了没效果」**)。仍只能走 `.dem`,归录像组。
- **`rec_slot_cost.py` 本轮未跑**:上一轮已在 W42 四个 run 上跑出 `RSC_EXIT=0`(`0 recording slot(s) beyond tolerance`);
  补跑机是第 5 个 `--rec-slots 8` 的 run,**可以并进基线但本轮没做** ⇒ **不声称它过了门**,留作下轮零成本活。
- **GH #454(机时常数漂 25%)本轮无新证据**:未发波,无新机时读数。上一轮的点名仍待总监重裁。
- **GH #388**:W34 的 103 份 `.dem` **2026-09-22 到期(还剩 19 天)**。

## 十、泄漏检查 —— **CLEAN(判据是「恰好等于意图」)**

- 进场:`running/pending` **空**。
- 收尾:未发任何实例 ⇒ 预期**恰好为空**;全状态普查 `[]`,`describe-images` 只有常驻 AMI `ami-0a990a26d89c66547`。
- ⚠️ **`terminated,shutting-down` 的空是 #375/#427 窗口过期,不是「没东西终止过」** —— 本轮补跑机确实终止过(S3 语料自证)。
  **空普查对历史一个字都不证明**,这句照上轮原样保留。

## 十一、铁律 11

本轮**未触发**任何 `requires approval`。**无空转等待**(自检在后台跑的整段时间用于收割、并池、闸判定与回填)。

---

## 交棒

1. ⭐⭐⭐ **总监 —— GH #427/#271 升级**:1h 窗口对**按需补跑机**是**按构造读不到**(2h cadence × 1h 窗口 × 按需无 SIR),
   不是「容易错过」。本轮亲手撞上,证据在三。
2. ⭐⭐⭐ **总监 —— GH #449 并池现在有 8 粒了**(gpm −24.75 / 2-8 / 433 计分局),上一轮的 7 粒读数**作废**。
3. ⭐⭐⭐ **总监 —— 闸 (ii) 第二支的判据分叉第三次触发**,且本轮有一条新算术:
   按 (乙) 发的波**无法测量触发它的改动**(见八)。请与 #449 一并裁。
4. ⭐⭐ **总监 —— GH #352 拿到第三条独立证据**:并池**加深**退化(0.0243 → 0.0265 → 0.0164),局数这条路也死了。
5. ⭐⭐ **总监 —— 3040 是「两台拼出来的一粒」**(`gpm_ab +153.75` / `gpm_ba −276.11`),与其余三粒不同源(见四)。
6. ⭐ **英雄组 —— `hero_tinker.lua` 那 8 处「结构性不可达」的声称,本台没有复核也不采信**;
   本台只证了 tinker 在本语料 0/487 局 ⇒ 不影响本家族读数。声称本身仍待英雄组自证。
7. ⭐ **本台下一轮**:(a) 把补跑机并进 `rec_slot_baseline`(零成本);(b) 若总监裁下闸 (ii) 判据则按裁定发/不发 W43(仍 spot);
   (c) **不要**再去读补跑机的死因码 —— 它永久没了,读到 `[]` 是预期不是症状。
8. ⭐ **存量**:#420 / #432(本波未测试它) / #388(19 天) / #454 / #446 / #429 / #418 / #412 / #408 / #402 / #395 / #375 / #290 / #313 / #271 / #329 / `strategy-5b` / **`strategy-31/32` 第九轮不搭波**。

---

## 自检收尾(铁律 10)

**真码 `SELFCHECK_EXIT=3`**,脚本自打 `selfcheck worst exit: 3`。
`legs run 9`、`FINDINGS (exit 3) = cadence trunk-red(python) trunk-red(lua)`、
**`UNCERTIFIABLE (exit 2): none`** ⇒ **有发现,不是没跑成**。

⚠️ **读码陷阱第二轮原样复发,再登记一次**:harness 给这条后台命令报的是 `exit code 0` ——
**那是命令串末尾那个 `echo` 的码,不是自检的**。真码要读脚本自打的 `selfcheck worst exit: 3`
或显式写进日志的 `SELFCHECK_EXIT=3`。**与管道陷阱同族(外层包装的成功掩盖内层的码)。**

- **python 腿**:`82 passed, 2 failed, 1 uncertifiable`。
  红:**`test_chain_member_census.py`**、**`test_threshold_chain_census.py`**。
  `UNCERTIFIABLE`:`test_selfcheck_lua_leg.py`(**did NOT run — 不是通过也不是失败**)。
  ⭐⭐ **GH #420 的 `test_detector_source_constants.py` 本轮不在红名单里 —— 连红六轮之后第一次没红。**
  本台不声称它「修好了」(没查过是谁改的),只据实登记这个状态变化,请总监核。
- **Lua 腿**:`74` 个 tagged detector 文件,**1 红**:
  **`test_gated_helper_nesting_census.lua`** —— `a NEW gate-inside-a-gate appeared:`
  `arbheart,campexit | Think | J.IsCampSwitchSafe | campdanger | bots/mode_farm_generic.lua`。
  工具自带的那句提问原样转交:*「未 arm 时,内层 helper 是不是它刚加入的那个合取的单位元?
  若不是,单独 arm 外层 id 测到的是 no-op,而 `check_armed_wiring.py` 仍会说它 WIRED。」*
  ⇒ **这正是章程「promote 掉一个 id 会悄悄冻死点名它的门」那条硬知识的入集侧同族**,
  归**协同组**(`arbheart` 是他们 GH #455 本轮刚落的)。
  ⚠️ **对本轮发波决定无影响**:`arbheart` / `campexit` / `campdanger` **三个都不在 armed 串里**
  (当场逐个 grep;串里只有 `slotarb`)。
- **cadence**:`GAP cadence hero`、`GAP cadence strategy`(窗口 24h,阈值 >3.5h)。按 LIMIT 2,**是问题不是判决**。

⭐ **本轮把「不声称 main 也红」换成了一个更便宜也更强的做法,登记为方法**:
以往六轮都写「**未 stash 重跑 ⇒ 不声称 main 也红**」。本轮改为直接量**检测器读的那些文件**是否等于 main:
`git diff origin/main -- bots game tests` ⇒ **输出为空**,而本会话的改动**全部在 `iterations/`** 下。
⇒ **这两条腿读到的 `bots/` + `tests/` 逐字节就是 `origin/main`**,
**因此可以直接声称:上面三个红是 `origin/main` 上的红,不需要 stash 重跑。**
(stash 重跑还有章程铁律 10 自己点名的风险:自检若死在 stash 与 pop 之间会扣住未提交的工作。)

## token 用量

```
TOKENS total_in=5,087,401 out=40,204 turns=46
```
(统计时刻之后的收尾回合不计入;当量级参考。)
