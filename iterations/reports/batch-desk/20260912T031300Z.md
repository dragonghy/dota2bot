# 批测台 2026-09-12T03:13Z — 发波轮 W68

**〇、定位**:**发波轮**。四道闸全部现跑、全部过,**W68 四台 spot 全部起飞并经 `describe-instances` 核实**,泄漏发射前后各查一次均零。
⭐⭐⭐ **头号产出不是那四台机器,是 `py_gate.py` 在 push 前拦下了本台自己刚写的波次记录** —— 记录里 gate 块的键名写成 `gates_at_launch_instant`,棘轮逐字打 `FAIL W68 has a 'gates' object (null/absent is 'not run', never a pass)`。⛔ **修法是改键名不是加别名**:该测试文件自己写着「Do NOT invent a value to turn this green; that is the exact failure this file was written against」,而加别名正是它说的「a silent rename skips 的那个 acknowledgement」。**这是闸第一次抓到本台的登记而不是本台的动作。**
⭐⭐ **第二是一条 trunk-red,它在 `origin/main` 上、由昨夜的 promote 造成、而且按构造推的人看不见**(§九)。

---

## 一、成本(当轮现跑,⛔ 不得抄)

`AWS_SETUP_EXIT=0`(`arn:aws:iam::581436534924:user/dota2bot-agent`,受限 IAM)。
`COST_EXIT=0`。MTD **`$83.035`**,`budget refreshed 2026-09-11T21:41:17Z`,CE `$0.01` 复核 **`83.0348379061`** 逐位一致;`forecast 191.545` 原样登记,**不据以决策**。

⚠️ **快照又冻住了**:`21:41:17Z` 这个刷新戳与上一轮(00:11Z)读到的 **完全相同**,MTD 也与上一轮 **`$83.035` 逐位相同**。⇒ 本轮 MTD **一分钱没动**,而中间隔着 W67 整波。
**诚实 cutoff** = `21:41:17Z − 11.3h` = **`2026-09-11T10:23:17Z`**。

| 波次 | 最后一台起飞 | 在 cutoff 之后? | 市场 | 计入 MTD? |
|---|---|---|---|---|
| W66 | 2026-09-11T15:24:58Z | 是 | spot | **否**,按 ~$1.10 计 pending |
| W67 | 2026-09-11T21:25:21Z | 是 | 按需 | **否**,按 ~$2.15 计 pending |

⇒ Σpending = **`$3.25`**。⛔ **未用「MTD 增量对得上波费 ⇒ 已计入」那个手法**(步骤 2 乙明令禁用,它已两次给出错误结论)。

`$50` 与 `$80` 两条 `ACTUAL` 告警**均已越线**(AWS state=`ALARM`);`$100` 未越(state=`OK`)。GH #515/#528 那条「budget 过滤器为 `null` ⇒ 账户里**所有**实例都落进这个预算,不管是不是 dota2bot 花的」**本轮一字未变**,`wave_fence.py` 自己逐字打 `budget filters : none <- so every instance in the account does land in this budget, whoever owns it`。

---

## 二、四道闸(全部当轮现跑,闸 (i)(iii) 另在发波脚本内部紧挨 `spot_run.sh` 再跑一次)

**闸 (i) 节流 —— 本轮它真的挡过一次,然后真的放行。**
决策时刻(03:13:32Z)`THROTTLE_EXIT=3`,逐字 `margin : 709s SHORT of unlock` / `A launch at this instant BREACHES gate (i) by 11m49s.`,锚点 `W67 last machine up 2026-09-11T21:25:21Z` ⇒ 解锁 `2026-09-12T03:25:21Z`。
⇒ 本台**没有**因此判零发波,而是把发波脚本挂到解锁后 14 秒起跑。动作时刻 `THROTTLE_EXIT_AT_LAUNCH=0`,逐字 `now : 2026-09-12T03:25:35Z` / `margin : +14s past unlock`。
⭐ **GH #469 的「闸绑的是动作不是决定」本轮是实打实用上了的**,不是抄一句:决策时刻那次 `exit 3` 与动作时刻那次 `exit 0` **是同一个工具在同一轮给的两个不同答案**。

**闸 (ii) 有新东西可测 —— 过在第一析取支,而且这一支是「测试集变了」。**
W66/W67 的臂串 `arm_md5 7d4c638dab526c68d9404526a3195663`(30 id);本轮 `origin/main` 现读 **`33047ce53c029f3901e5f63eab872ecb`(29 id / 257 字节)**,与总监在 `test_set.md` 第 3 行登记的 md5 逐位相同。差值 = `tpcommit` 被 RULING 19 promote 出集。
⇒ **W68 是 29-id 家族第一波,深度 0**。⛔ **不与 W66+W67 的 30-id 池并池** —— 总监 RULING 19 逐字写了这一条。

**闸 (iii) 围栏 `FENCE_EXIT=0`。** 逐字:
`limit $100.00` / `actual (MTD) $83.035` / `pending waves $3.250` / `planned $1.100` / `projected total $87.385` /
`fence $100.00 <- lowest ACTUAL alert not yet crossed (Ruling 1)` / `brake $90.00 (owner's line, not derived here)` /
**`operative ceiling: $90.00 = min(fence, brake)`** / **`headroom : $2.615 after this wave`**。
⚠️ 工具要求逐字转录的那行钟表免责声明:`WAVE_FENCE CLOCK : the wave-accrual window above was anchored to snapshot instant ASSERTED by the operator (--snapshot-instant) -- a claim, not a reading.`
另:`accrual scope : 17 region(s) read ... <- COMPLETE: every region this account has enabled`,`accrual check : CERTIFIED ZERO (0 accruing instances account-wide)`,但因 `--pending $3.250` 更大而取 pending。

**闸 (iv) 市场 `BLIND_EXIT=0`。** 对 W67 跑 `reclaim_blind.py`,逐字 `VERDICT: not blinded -- the wave delivered 4 paired seed(s).` / **`NEXT WAVE: spot (the default; GH #158 unchanged).`**
⭐ 值得记一句:W67 四台按需的 `status_code` 全是 unread,但工具自己打了 `immaterial : all 256 extension point(s) agree, so the verdict below does not rest on the unread field(s). A gate may not demand a datum its own answer does not depend on (GH #699).` ⇒ **上一轮立的 GH #759(收割义务可以写成不可满足的)本轮拿到了工具侧的正面回应**:它没有因为那四个洞而拒答。

---

## 三、W68 发波(四台 spot,**成败不从退出码断言**)

- `--dry-run` 先行:`DRYRUN_EXIT=0`,逐字 `plan: 1 x SPOT c6i.4xlarge` / `ref=main slots=14 rec_slots=1 watchdog=2h region=us-west-2`。**波次预算闸未打 `REFUSED`** ⇒ **未用** `--allow-short-watchdog`。
- 四次 `spot_run.sh --count 1 --hours 2 --validate "<29-id 串> <seed> --games 12"`,四个 `SPOT_RUN_EXIT=0`。
- ⭐ **市场从 `InstanceLifecycle` 读,不从 run_id 的 `spot_` 前缀读**(那是硬写字符串,与市场无关,GH #158):四台 `describe-instances` 全部 **`spot`** ⇒ 闸 (iv) 裁的 spot 确实落地。
- 四台 `State=running`;`instanceInitiatedShutdownBehavior` **逐台单独查**(不从整批推断)全为 **`terminate`**;watchdog `--hours 2`。

| seed | instance | AZ | LaunchTime |
|---|---|---|---|
| 12924 | `i-0f239f5fc29a97021` | us-west-2a | 03:26:05Z |
| 12888 | `i-08e9cb09ec2581e6b` | us-west-2b | 03:26:07Z |
| 12939 | `i-0d372a0b1795fb447` | us-west-2d | 03:26:10Z |
| 12542 | `i-04c24c4a18ee03576` | us-west-2b | 03:26:12Z |

**slate spread 7s**(与 W67 并列最紧)。**AZ 分散 3/4**(2a/2b/2b/2d)—— 不是 4/4:az 环随机起点落在 `us-west-2c`(GH #252),而 c 没填上。
**树**:`ref_pin = 2186d6a2fe696ec24c101618c3b4e4044e1a9c15` = **发波前现跑 `git ls-remote origin main`**。⭐ **这一条本轮不是形式**:容器 HEAD 当时是 `36bf349`,**落后 origin/main**;实例 clone 的是 `origin/main`,所以臂串与树都从**远端**读(`git show origin/main:...`),不从工作树读。这正是章程「批测实例 clone origin/main,发波前先核远端 tip」那条近失事故的形状。
预计 ~04:15–05:30Z 落 verdict,**下一轮收割**。

---

## 四、收割:本轮无新数据(这是闸 (iv) 前半的输入)

S3 `validation/` 最新对象 `2026-09-11 22:22:08`(W67 末粒 verdict),**上一轮 00:11Z 已全量收割**(`W67_verdict.json` / `W66W67_pooled.json` 均在盘上)。此后零新对象 ⇒ **无收割欠账**,W68 不是压在一波没读完的波上发的。

---

## 五、泄漏(发射前 + 发射后,两次)

- **发射前**:账户级 `describe-instances`(us-west-2,**不加 tag 过滤**,状态 `pending,running,stopping,stopped`)⇒ **`[]`**(`LEAK2_EXIT=0`);`describe-volumes` ⇒ **`[]`**(`LEAK3_EXIT=0`)。
- **发射后**:同一查询 ⇒ **恰好本轮有意启动的那 4 台**,无一多余。
- 常驻成本仅 AMI `ami-0a990a26d89c66547`。
- ⭐ 按上一轮交棒第 5 条,泄漏检查**直接用 `describe-instances`**,**未**为这个块重跑 `check_costs.sh` ⇒ 省下上一轮多花的那 `$0.01`。

---

## 六、GH #454 两条腿

- **(乙1) 第 14 轮欠着,而且按它自己的判据本轮**依然**不该结清**:它要账单侧实测单波成本,而本轮 cutoff = `2026-09-11T10:23:17Z`,**盖不住 W66(15:24Z)也盖不住 W67(21:25Z)**,更盖不住 W68。⛔ 不用「MTD 增量对得上波费」反推(步骤 2 乙禁用)。
- **(乙2) ⭐ W68 是它九轮来第一个「按构造可满足」的测点。** W67 四台按需,`status_code` 按 GH #699 在收割时**永久 null** ⇒ 结构上给不出机时均值;**W68 四台是 spot,SIR 词汇适用**,只要在 GH #375 那个 ~1h 老化窗口内读到码,(乙2) 就能算。已写进 `W68_wave.json:harvest_obligations (a)(b)`。

---

## 七、铁律 6 三条腿(push 前现跑,⭐ 三行读数)

- `GATE_EXIT=0`(`luacheck_gate.sh`),逐字 `luacheck bots game: 0 warnings` / `GATE_EXIT=0 CLEAN (iron rule 6 static half passed)`。
- `py gate: 96 ran, 0 findings, 0 uncertifiable, 26.3s` ⇒ **`PY_GATE_EXIT=0`**(⚠️ 第一次跑是 **`exit 3`**,见 §九;这里是修好之后的读数)。
- `lua gate: 322 fast Lua ratchets` ⇒ **`LUA_GATE_EXIT=0`**。
- ⛔ **未用 `RULE6_BYPASS`**,三条腿都真的跑了。
- 动态那半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)**不在钩子里、本轮也未跑**,本报告**不声称**它。

---

## 八、自检(铁律 10)—— ⚠️ `EXIT=124`,**这不是通过**

`routine_selfcheck.sh` 两次都**没跑完**:第一次 400s 超时,第二次后台 900s **仍然 `EXIT=124`**,死在 `=== trunk health (fast Lua detectors) ===` 这一腿上。
⇒ **Lua 检测器那一侧本轮没人看过**,与 `exit 2` 同义:**不是干净,是未核验**。
⚠️ 另:`UNCERTIFIABLE tests/test_selfcheck_lua_leg.py (did NOT run -- this is not a pass and not a failure)`。
⭐ **管道坑本轮第 17 次登记,而且本台又踩了一次**:开工第一条命令接了 `| tail -60`,工具当场 `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.`(它自己说这已复发 5 次、每次都是当轮第一条命令)。改重定向后才拿到真码。**上一轮交棒第 6 条逐字预警过这件事,本台照样踩** ⇒ 说明那条交棒**写在报告里不够**,它需要一个跑得起来的东西挡着。
跑完的腿读数(截至超时):anchors `8 anchor(s) checked -- OK`;promote-atom `constraints: OK`;inverse gate census `FROZEN 0 / COUPLED 1 / UNRESOLVED(armed) 0`,`no armed id hangs under an unarmed gate -- OK`。

---

## 九、⭐⭐ 本轮的第二件产物:一条在 `origin/main` 上的 trunk-red,由昨夜的 promote 造成,而推它的人按构造看不见

自检的 python 腿打 **`TRUNK RED`**,`122 passed, 3 failed, 1 uncertifiable`。在**已 rebase 到 `origin/main`(`2186d6a2`)的树上**逐个复跑,三条全部复现:

| 测试 | 退出码 | 失败逐字 |
|---|---|---|
| `tests/test_stale_waits.py` | 1 | **`FAIL: member string parsed to only 29 ids -- line 2 looks truncated`**(`49 checks, 1 failed`) |
| `tests/test_carrier_terms.py` | 1 | `FAIL zusult -> zuus` / `FAIL liondrainstop -> lion` / `FAIL zusult is still hero-scoped off the tree (unarmed, so the arm string cannot speak for it)` / 同 `liondrainstop` / `FAIL W20: liondrainstop 1/4 (as #276 predicted)`(`65 checks, 5 failed`) |
| `tests/test_detector_source_constants.py` | 1 | 见 `/tmp` 日志,同族 |

⭐⭐⭐ **`test_stale_waits.py` 这条是 GH #624 立案形状的第五例,而且是最干净的一例**:它断言成员串是 30 id,**RULING 19 把 `tpcommit` promote 出集、串变成 29**,于是这条 census 当场变红 —— **红的成因是一次完全正确的 promote**,不是谁写坏了代码。而按构造:
- 三条**都在 `py_gate_manifest.json` 里**(第 82 / 152 / 432 行),
- 但三条**都不在钩子那 84 条「快棘轮」选择域内**(`py_gate.py` 逐字 `NOT claimed here: 30 slower python tests`),
⇒ **推 promote 的那一轮,钩子对它全绿**;红要等**下一个开工的组**跑全量自检才看得见 —— 本轮就是那个下一个组,而作者已经走了。**这正是 #624 说的「红由下一个开工的组发现,几小时后,作者已经走了」,只是这次触发它的不是新 gate,是一次 promote。**
⛔ **本台不修**(章程:不写 bot 代码、不做判断分析;且 `test_stale_waits.py` 的正确修法是把硬编码的 30 换成从 `test_set.md` 现读,那是总监的登记口径问题)。已按铁律 5 开 issue(§十一)。

---

## 十、⭐ 本轮闸抓到的是本台自己的登记(而不是本台的动作)

`py_gate.py` 第一次跑 **`exit 3`**,逐字 `FAIL W68 has a 'gates' object (null/absent is 'not run', never a pass)` —— 本台把波次记录的闸块键名写成了 `gates_at_launch_instant`。
第二次跑仍 `exit 3`,逐字点名三个缺键:`ii_new_to_test` / `iv_inputs` / `iv_reclaim_blind`(本台原写 `ii_testset` / `iv_market`,**把闸 (iv) 的两个半边压成了一个键**)。
⛔ **两次都按键名改,没加别名**:`tests/test_wave_gate_keys.py` 自己写着 `If instead the desk renamed a gate on purpose, the fix is one alias in REQUIRED_GATES above, which is the acknowledgement a silent rename skips.` —— 本台**不是**故意重命名,是写错了,所以加别名会把一次笔误伪装成一次决定。
⭐ 顺带被这条逼出来的一个**真实缺口**:原记录把闸 (iv) 写成单键 `iv_market`,意味着**闸 (iv) 前半(inputs / 有无收割欠账)本台原本根本没登记**。补登记后它是有内容的(§四),但**如果没有这条棘轮,这个洞会以「记录看起来很完整」的样子过去**。

---

## 十一、本轮 issue(⭐ push 之后才发,GH #290 顺序)

**新开 GH #765 `[harness]`** —— `test_stale_waits.py` 把成员串 id 数硬编码成 30,被 RULING 19 的 promote 顶红;它在 `py_gate_manifest.json` 里但落在钩子的快选择域外,所以 promote 那一轮钩子全绿、红留给下一个组。建议:改成从 `origin/main:test_set.md` 第 2 行现读 id 数,或把它移进钩子的快域。附本轮逐字读数与复跑证据。
**并附带** `test_carrier_terms.py` / `test_detector_source_constants.py` 的 `zusult`/`liondrainstop` hero-scoped 两条(同一次自检打出,归英雄组)。

✅ **MCP 可用,本轮已发出**:`GH #765`(`https://github.com/dragonghy/dota2bot/issues/765`)。
发表前按 GH #290 顺序:**先 push 再发**,草稿跑 `claim_precheck.sh` ⇒ `PRECHECK_EXIT=0`,逐字 `local commits not on origin/main: 0` / `paths cited 12 ... resolved on trunk 11 refused 0` / `OK to publish: every citation resolves on origin/main.`

---

## 十二、⚠️ 给总监的一条:围栏在 9 月 12 日就只剩 `$2.615`

本波之后 `projected total $87.385` vs `operative ceiling $90.00`。⇒ **本月还剩 18 天,而围栏只够再发约一波 spot(~$1.10)**。
⛔ **本台不自行改刹车线也不自行降级**,只登记两件事实:
1. 刹车 `$90` 是 owner 的线,`fence $100` 是派生的,`min` 取 `$90`;
2. **预算过滤器是 `null`**(`wave_fence.py` 逐字),而 09-08 那轮已量到 **本月 MTD 里 55% 不是 dota2bot 花的** ⇒ 咬住本台的这条线,有一半以上不是本台的支出。
**这两件事合起来是一个可以裁的问题,不是一句抱怨。** 交总监。

---

## 十三、交棒(下一轮本台)

1. ⭐ **收割 W68**(四粒 spot,seeds 12924/12888/12939/12542)。⛔ **先按 run 分四个子目录下载再带前缀合并**,不要 `s3 cp` 进同一目录(GH #225 那个静默丢 15.9% 的坑)。
2. ⭐⭐ **抢在 GH #375 的 ~1h 老化窗口内读四台的 SIR `Status.Code`** —— 这是 (乙2) 九轮来第一个按构造可满足的测点(§六)。读不到就如实写 unread,**不许编**。
3. **闸 (i) 名义解锁 `2026-09-12T09:26:12Z`**(⛔ 别抄,跑 `wave_throttle.py`,并在发波脚本内部紧挨 `spot_run.sh` 再跑一次)。
4. **并池纪律**:W68 是 **29-id 家族深度 0**,⛔ **不与 W66+W67 的 30-id 池并池**(RULING 19)。收割时按铁律 4:(i-a) **两层读数都登记**(是读数不是局数),(i-d) **先每粒 swap-average 再跨种子算术平均**,⛔ 永不按局数加权。
5. ⛔ **本条目的 `$83.035` / cutoff `10:23:17Z` / `headroom $2.615` / 解锁 `09:26:12Z` 全部作废,不得抄作前提** —— 围栏、闸、成本**一律当轮现跑**。
6. ⚠️ **开工第一条命令:重定向 + 后台 + 不设短 `timeout`**(**第 17 次**登记,本轮本台又踩了一次 —— 见 §八)。并且 `routine_selfcheck.sh` 在本容器 **900s 跑不完**,请直接给它 ≥1200s 或分腿跑。
7. ⛔ **本条目的 issue 号已当轮回填(GH #765),无待补项。**

---

## 十四、Token 用量

`TOKENS total_in=5,731,412 out=36,411 turns=52`(到统计时刻为止,此后收尾回合不计入)
