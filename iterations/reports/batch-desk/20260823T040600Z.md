# 批测台 2026-08-23T04:06Z — 静默轮:无可收、不启动、零支出;把下一波的前置核对提前买了

**一句话**:6h 节流差 **2h04min**,S3 自 02:06Z 起**逐位未动**,四层泄漏检查全清,
MTD **$23.578** 未动;本轮的实际产出是**把 06:11Z 那一波的启动前置核对全部提前做完**
(树漂移、27 个 gate 调用点、树可启动性),外加一条从中掉出来的 [harness] 欠账(**GH #131**)。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` — **worst exit 0**,三项全清:

| 项 | 结果 |
|---|---|
| unlanded work | `OK: no unlanded work in the certifiable window`(trunk 50 commits,275 refs 扫过,251 条因浅克隆无 merge-base 而 **REFUSED**,certifiable 24) |
| report cadence + citations | `clean`(streams 5 / reports 248 / 窗口 24h) |
| trunk python 测试 | **13 passed, 0 failed** |

对比:02:06Z 那轮 worst exit 3(两条 `itemtrip` 的 UNLANDED,在
`origin/claude/dreamy-feynman-tmvbay` 上)。**那两条本轮不再报,因为它们落地了** ——
`5ee98e6` / `d779a75` 现在都在 `origin/main` 里(见 §3 的漂移表)。**检测器点名 → 下一轮落地**,
这是铁律 10 立的时候想要的那条闭环,记一笔。

---

## 1. AWS 引导 + 成本(章程步骤 1-2)

`session_setup.sh`:AWS CLI 本容器缺失,脚本自己 pip 装上
(`aws-cli/1.46.0 Python/3.11.15 botocore/1.43.62`),身份核到
`arn:aws:iam::581436534924:user/dota2bot-agent`(受限批测用户,正确)。

`check_costs.sh`(免费 `budgets` 路径):

```
MTD 23.578 USD | forecast 23.139 | budget limit 100.0
budget refreshed 2026-08-22T21:26:04Z
```

**$45 围栏 / $90 刹车 / $100 owner 批准线,三线全未触及。** 读数
`23.578 ≪ COST_CONFIRM_AT=$35` ⇒ **未花 $0.01 调 CE**。

### 1.1 ⚠️ 上一轮那条「可证伪的预期」——**本轮证伪不了,而且原因值得写下来**

02:06Z 我写过:「本波 ~$1.5 尚未入账,**可证伪的预期:下轮 MTD ≈ $25.1**」。
本轮读数 **$23.578**,看上去像是**证伪了**。**它没有,它连检验都没做成。**

判据是 `budget refreshed` 那个时刻:**02:06Z 与本轮 04:07Z 都是
`2026-08-22T21:26:04Z`,逐秒相同**。⇒ 我这两轮读到的**是同一份快照**,
不是两次独立读数。一个没有刷新过的数字**不可能**承载「这两小时里有没有入账」的信息。

**⇒ 纠正我自己上一轮的登记方式**:预期该挂在 **refresh 时刻推进**上,不该挂在轮次上。
重述成可检验的形式:**下一次读到 `budget refreshed` 晚于 2026-08-23T00:11Z(本波启动时刻)时,
MTD 应 ≈ $25.1;在那之前,MTD 逐位不变是「没有新数据」而不是「没有花钱」。**

这与章程步骤 6 那条已立的纪律同源(「收尾那次要的是 0 台在跑,不是 MTD 数字;
22:18Z→06:11Z 连续五轮 MTD 逐位一致,同轮第二次读数从来没带过任何信息」)——
本轮把它推进一格:**跨轮的 MTD 一致同样可能是零信息,判据是 refresh 时刻,不是轮次差。**

---

## 2. 收割(章程步骤 3):**无可收**

S3 固定栏位,与 02:06Z **逐位一致**:

| 栏位 | 04:07Z | 02:06Z | 说明 |
|---|---|---|---|
| `soak/` 前缀数 | **164** | 164 | 00:11Z 那四个 run 是最后四个 |
| `dem21/` 前缀数 | **16** | 16 | 同上 |
| `unattributed/` | **0** | 0 | |
| `validation/` 最新条目 | 2026-07-23 | 2026-07-23 | 标准路径走 `recover_verdict.py`,**陈旧是预期** |

`soak/` 最新四个前缀仍是 `spot_20260823_0011{27,31,36,40}_1_cde1d6c4…`,
02:06Z 已全量收完(**278 有效局 + 23 暖场 = 301**)。**本轮没有任何新数据落地,
不存在「漏收」的可能。**

---

## 3. 启动决策(章程步骤 4):**不启动**,只卡 (i)

### 4a — 队列:5 条 pending,**无一需要为它开波**

| id | 自述 | 本轮处置 |
|---|---|---|
| `hero-1` / `hero-2` / `hero-3` / `hero-4` | 均自述 **NO NEW WAVE NEEDED**(档案侧扫描) | 不动 |
| `strategy-3`(`itemtrip`) | **NO DEDICATED WAVE** —— 请求并进下一个多 id 波 | **前提未成立,见 §3.2**,已写进 result |
| `hero-5`(CM 靴子) | **ungated,搭任何一波** | **已确认下一波自动覆盖**,已写进 result |

### 4b — 例行波次节流三条

| 条 | 判定 | 数字 |
|---|---|---|
| (i) 距上一波 ≥ 6h | ❌ **不满足** | 上一波 **00:11:22Z**,现 **04:07Z**,间隔 **3h56min**,差门槛 **2h04min** |
| (ii) 有新东西可测 | ✅ 满足 | `bots/` 漂移 **4 个 commit**(见 §3.1) |
| (iii) 已花 + 本波预估 ≤ $45 | ✅ 满足 | $23.578 + ~$1.5 ≈ **$25.1** |

**2h04min 是小时级差额,按 22:06Z / 20:09Z / 16:10Z / 08:08Z / 16:06Z 的先例不启动**;
**明确不援引 06:07Z 那条「差 2 分钟按立法目的照发」** —— 那条的适用面是分钟级。

**下一波最早 2026-08-23T06:11:22Z。**

### 3.1 两步法核树(章程步骤 5,**exit code 认真读**)

```
git fetch --depth 1 origin cde1d6c4f3b83b0b75904575eae1f4a599ac65be   -> exit 0（全 40 位,不用短 SHA）
git log --oneline cde1d6c4…..HEAD -- bots/ game/                       -> exit 0（真答案,不是那个会被误读成「无漂移」的 128)
```

自上一波被测树以来 `bots/` 漂移 **4 个 commit**:

```
9fa4898 hero 02:00Z: CM's pos_5 buys arcane boots, and Boots of Bearing had to leave with the tranquils
d779a75 [strategy] itemtrip closeout: the full-suite delta, and the one failure in it that was mine
5ee98e6 [strategy] itemtrip: the healthy trip home, contested by an item-mode file that installs nothing until armed
66c81d9 hero 21:48Z: Lion t10 flips to +20 move speed, and t15 is a decision not a skip
```

本地 `HEAD` = 远端 `origin/main` tip = **`68d0ea2`**(`ls-remote` 已核),工作树 clean。

### 3.2 ⚠️ `itemtrip` 的搭车前提**尚未成立** —— 球在总监,写在这里免得掉棒

`strategy-3` 的申请书写「please fold `itemtrip` into the next multi-id wave」。
**但它现在不在下一波的串里,而且这不是我能改的**:

- `test_set.md:4` 的 `itemtrip` 是 **2026-08-23T01:2xZ 的协同组入集提议**,措辞是「请总监特别看这一条」;
- `test_set.md:271` 自己写死:**「下一波要 armed 的串永远在最新一节的 §x.0 里」**;
- 现行 **§AR.0**(`test_set.md:5095`,总监 08-22T23:0xZ 裁定)是 **24 id,不含 `itemtrip`**。

**⇒ 06:11:22Z 那一波若届时总监仍未批准入集,发出去的 24 id 串里没有 `itemtrip`,
`strategy-3` 的条件 (a) 这一波买不到。** 按章程「不自己动测试集」,本台**不代批**,
只把棒明确交出去(README 铁律 9 的连带规则),已写进 `queue.json:strategy-3.result`。

**顺带记一条口径(不是本轮的新发现,是既有必查项 ③ 的又一次兑现)**:
`test_set.md:2` 那行 header 串是 **26 id**,比 §AR.0 多 `wandlimbo` 与 `odaoe` 两个。
历轮必查项 ③ 正是为此存在(「S3 真实 cand 串与 §x.0 逐字节相同 + `wandlimbo` 0 次 / `odaoe` 0 次」),
**已知且有护栏**。记在这里只因为 header 在**第 2 行**、§x.0 在**第 5095 行** ——
新会话最先读到的是错的那个。

### 3.3 启动前置静态核对(提前买掉下一轮的活):**27/27 通过**

对 `test_set.md:2` 的 26 元 header 串 **加** 提议中的 `itemtrip`,逐个核到**活的 gate 调用点**:

- **26/26** header 串 id 全部命中(每个 1 处);
- `lf_rescue` 字面量 **0**,走 `jmz_func.lua` 的 `J.IsLaneFixOn('rescue')` **拼接**形式,
  命中 1 —— **这是已知正确的**,判据是「字面量命中 **或** 落在 8 个 lf sub 里」
  (`chase/mana/recover/rescue/revive/salve/support/threat`),**这条早在 GH #70 §5 就写过,
  不是本轮的发现**;
- `itemtrip`:命中 **1 处**,`bots/mode_item_generic.lua:65`,`J.IsSoakCandidate('itemtrip')`。

### 3.4 树可启动性:**可以启动**,但结论要挂对判据

英雄组 `7d14c23` 报了「**main is red in 9 files**(31 个断言点)」。这句话对启动决策**要害**,
所以我读了它的正文而不是标题:

- 9 个红**先于本轮存在**(在 `origin/main` 08db68e 的 worktree 上逐个复跑,计数逐位相同),
  属 **GH #127 / #106** 的 corpus-size 等值家族(`expected 100 fixtures, got 101` 一类),
  另有 `test_wk_reincarnation_mana_gate` / `test_relicguard_siege_gate` 两个不属该家族;
- **142 个文件里 133 个绿,含 `test_smoke_load 2/2`**,`luacheck 0 warnings`。

**`test_smoke_load` 绿才是启动的判据** —— 它是「每个英雄文件在 mock 下加载得了」的那一条,
加载期崩溃在 bot VM 里是**静默**的(`CLAUDE.md`:`error in error handling` 吞掉全部错误文本)。
**⇒ 9 个红是行为断言不是加载失败,不构成启动阻塞。** 经验旁证:上一波跑在同样带这些红的树上,
产出了 301 局。

---

## 4. ⭐ 本轮唯一的真发现:smoke 覆盖是**碰巧**的,不是结构给的(**GH #131**,[harness])

§3.4 顺着读下去掉出来的。`tests/test_smoke_load.lua` 两个用例的结构**不对称**:

- 英雄侧用 `io.popen('ls bots/BotLib')` **自动发现** ⇒ 随树生长;
- core 侧(`:70-84`)是一个**硬编码 11 元字面量数组** ⇒ **不发现任何东西**。

按文件清单量:引擎按固定名加载的顶层 `bots/*.lua`(排掉被文件名禁用的 `--mode_item_generic.lua`)
共 **27 个**,core 列表覆盖 **11 个**,**16 个不在列表里**(13 个是 mode 文件:
assemble ×2 / defend_tower ×3 / push_tower ×3 / outpost / secret_shop / side_shop / team_roam / ward,
外加 `FretBots.lua`、`mode_retreat_generic_wip.lua`、**`mode_item_generic.lua`**)。

**为什么这不是「16 个都在跑,没事」**:那 15 个继承件已在几百局里跑过,能加载是实测过的;
**风险在「列表不随树生长」这个结构本身,而它刚刚被兑现了一次** ——
`bots/mode_item_generic.lua` 是**本项目历史上新增的第一个 mode 文件**(`5ee98e6`),
**不在 core 列表里**。它有加载覆盖,是因为协同组**自己**写了
`tests/test_itemtrip_wasteful_trip.lua:130` 的 `pcall(dofile, MODE)`(armed / 未 armed 两条路都盖,14 例绿),
另有三处也 dofile 它。**⇒ 覆盖是有的,但是碰巧的。下一个往 `bots/` 加引擎加载件、
又没顺手写 dofile 用例的人,拿到的是零加载覆盖 —— 而后果在批测里长的样子不是一条报错,
是这一波的读数悄悄变了。**

**这一条还正好压在 `itemtrip` 自己的安全论证上**:那个文件的全部安全性押在
「未 armed 时一个 `GetDesire` 都不装 ⇒ 未 armed 的一局逐字节等于今天这一局」。
这句话为真的**前提是文件本身加载成功**;加载期就炸的话,**两条腿一起没了 item mode,
镜像 A/B 的差分读数看不出来**。

**诚实边界(写正文不写脚注)**:本条**只量了覆盖清单,没量任何一个文件是否真的加载得了** ——
本容器**没有 `lua5.1` 也没有 `luacheck`**(历轮固定栏位),我**跑不了** smoke test。
⇒ 这是**覆盖面缺口**,**不是**「有文件加载失败」的告警,两者不可混读。
相邻但不同的两条已开着的:**#124**(整套 suite 一个进程跑不完,3000s 上限打死)是**运行方式**问题;
**#127 / #106**(既有 9 个红)是**既有红**。本条既不是那两条,也不依赖它们先修。
修法(发现式 core 列表 + 显式 skip 表)写在 #131 里,**按章程本台不自己改 harness**。

**不阻塞 06:11Z 那一波**:`test_smoke_load 2/2` 绿、`itemtrip` 未 armed 且不在 §AR.0 串里、
该文件已被四处 dofile 覆盖。

---

## 5. 局数(章程步骤 7,owner 2026-08-19 要求)

**(a) 上一波次(`spot_20260823_0011xx`,§AR.0 的 24 id 全集,种子 888/895/896/906)的最终有效局数**
—— 02:06Z 已收割定案,本轮无新数据,**原样转录**:

| 项 | 数 |
|---|---|
| 有效局 | **278**(+ 23 暖场 = **301**) |
| 逐 run `analysis` | 77 / 71 / 77 / 76 |
| 侧别 | radiant **167** : dire **111**(dire 仍撞 35min stall 上限,不对称是已知结构) |
| 三元组 | `analysis 301 / 录制局 159 / demclaim 159 / .dem 158`(8<16 槽 ⇒ 第一栏分母是**录制局数**;`.dem` 数 `dem21/<run>/` **不数** `soak/`) |

**(b) 本轮在跑波次的实时进度:无在跑波次**(实例 0 台,见 §6)。

---

## 6. 泄漏检查(章程步骤 6:四层 + spot 请求,全免费只读)

开工与收尾各查一次,**逐层相同**:

| 层 | 开工 | 收尾 |
|---|---|---|
| 非终止实例(pending/running/stopping/stopped/shutting-down) | **0** | **0** |
| 游离卷(status=available) | **0** | **0** |
| 快照(self) | **1** — `snap-0ad026b386c804288`(160GB,唯一常设成本) | 同 |
| EIP | **0** | **0** |
| open/active spot 请求 | **0** | **0** |

**无泄漏。** 00:11Z 那四台在 02:06Z 已全部自毁完毕,本轮无需任何人工终止。
收尾那次按章程走 `--leak-only`(**不查花费,零成本**),要的是「0 台在跑」这个事实,
不是第二个 MTD 数字(§1.1)。

---

## 7. 验证(铁律 6)

本会话**未改任何 Lua** —— 改动只在 `iterations/` 下(本报告、章程、`queue.json`)。
容器无 `luacheck` / `lua5.1`(已复核:`which lua5.1 luacheck lua` 三个都空),
**铁律 6 无适用对象**。

**本轮的推断项只有一条**,已在 §1.1 标明并改成可检验形式:
「本波 ~$1.5 尚未入账」现在挂在 `budget refreshed` 时刻上,不再挂在轮次上。
其余全部是命令输出或行号级源码读数。

---

## 8. 跨组

- **新开 GH #131**([harness],§4)—— 搜过 `search_issues` 无既有条目拥有这条线,
  且 #124 / #127 / #106 都不是它,故新开而非追评。
- **给总监一件待决(时限性的)**:`itemtrip` 入集与否,**在 06:11:22Z 之前**要有结论,
  否则 `strategy-3` 的条件 (a) 这一波买不到(§3.2)。
- **02:06Z 交出去的两件仍待裁**,本轮无新增证据,不重复施压:
  (a) 四指标 0/4 第二波是否触发对 24 id 测试集的处置;(b) rec-slots 阶梯 (A)/(B)/(C)。
- 仍开着:`[batch] #70`、`[harness] #131`(新)/`#98`/`#95`/`#75`/`#33`/`#124`、
  `[bug] #96`、`[strategy] #117`/`#116`/`#119`/`#109`、`#127`/`#126`/`#120`。

**owner 优先项核对**:P1(拉野)与 P2(低血不回家)的球都在**协同组与录像组**。
两项要的 (a) 语料**正是 02:06Z 刚收割的那 158 个 `.dem`**(帧通道 8/16,归属实测 159/159 可信),
**本轮不开波对两项零代价** —— 再开一波只会重复买同一份证据。
唯一的例外是 §3.2:P2 那一族的 `itemtrip` 卡在入集批准上,**那是总监的一步,不是波次的一步**。

---

## 9. 下一轮(最早 2026-08-23T06:11:22Z)的活 —— 启动,必查项 8 条

**预置**(除非总监改):种子 888/895/896/906、4 台 × 1 种子 on-demand、16 槽、
**`--rec-slots 8`**(阶梯照裁定停在 8,通过也不自动上 16)、2h 看门狗、`--games 22` 不再上调。

**串**:**§AR.0 的 24 id**;**若总监在 06:11Z 前批准 `itemtrip` 入集,则按新的 §x.0 走**(见 §3.2)。

1. **启动时刻不早于 06:11:22Z**;四次调用之间**保证跨过一个整秒**(run_id 唯一性押在那一秒上,`[harness] #98`),
   启动后用免费的 `describe-instances` 核 `soak-run` 标签四值**两两不同**。
2. **核树两步法**:`git fetch --depth 1 origin <全 40 位 SHA>` → `git log <SHA>..HEAD -- bots/ game/`;
   **exit 128 = 不可比,不是无漂移**。
3. **启动前静态核对 gate 调用点** —— §3.3 已提前做完 **27/27**,若串未变可直接引用,
   **但树若又漂移则必须重做**(判据:字面量命中 **或** 落在 8 个 lf sub 里)。
4. **暖场 stamp 必须 100% 等于被测树 SHA**。
5. **S3 真实 cand 串与 §x.0 逐字节比对** + `count` 核对 + `wandlimbo` / `odaoe` **各 0 次**
   (header 串比 §x.0 多这两个,见 §3.2)。
6. **收割合并必须带时分秒前缀**(02:06Z 实测:裸合并静默丢 22 局**并改变了两个种子的读数**);
   下载时 `*.demclaim.json` **必须与** `*.analysis.json` **一起下**(否则 `rec_slot_cost.py` exit 2 同码不同因,
   要读 die 文本才分得开)。
7. **`.dem` 数 `dem21/<run>/` 不数 `soak/`**(`soak/` 下恒为 0,长得跟「整波没录上」一模一样),
   **且不锚 `\.dem$`**(会漏 `.dem.gz`)。
8. **`fieldcreep` 的 (a) 按 §AR.3 甲**:每次否决命中必须写清打他的是**野怪还是小兵**,
   只报次数**不算买到 (a)**;§AR.3 乙的退出条件同时生效。**这条交录像组,本台不越界。**

---

## 10. 本次会话 token 用量(铁律 8)

```
TOKENS total_in=3,426,886 out=31,811 turns=33
```

(到统计时刻为止;统计后的收尾回合——commit/push——不计入。
本轮为**静默轮**:无 AWS 支出,无实例启动,无收割。)
