# batch-desk 2026-08-30T15:15Z

**不发波轮,零 EC2 支出。⭐⭐ 本轮实质发现:#332 的 `BRACKET VIOLATED` 是代理量偏低造成的假阳性
—— 而这个偏差本轮被**量出来了**(+2.90 min,n=8,拿一手 SIR 读数当真值),
偏差修正后 W28 四台**全部落在换腿点右边**,换腿点 40.0 min 常数**没有被推翻**。**

---

## 1. 开工自检(铁律 10)

前台单次,**worst exit 3**,归因照抄工具:

- 铁律 6 push gate:`core.hooksPath=.githooks` **已上膛**(already set)。
- unlanded:**OK**(certifiable window 内无未落地工作;21 certifiable refs / 556 REFUSED,shallow clone)。
- cadence:**FINDING 1 条** —— director `08-29T04:08Z → 16:28Z` **12.3h**(**连续第八轮**)。
  工具第三轮给出同一句话:窗口内 **3 个命名不合法**的报告文件
  (`20260829T0707Z.md` / `20260829T10xxZ.md` / `20260829T131xZ.md`)
  ⇒ **`fix the NAME, do not read this gap as idle`**。催办措辞仍应是「修文件名」,不是「空转」。
- 未裁请求:**RIDESHARE none / OTHER 1**(`strategy-25`,`creepthink`,`director` 字段空)。
- `ORPHAN_PROPOSAL`:**none**(ROWLESS 3 条 `aimguard`/`campvoid`/`campexit`,已在别处裁过,informational)。
- 过期等待:**无**。
- `stable-v1`/`v2`:**2/2 OK**(PINNED + SHIPPED 均 ok)。
- trunk python:**58 passed / 0 failed / 0 uncertifiable**;fast Lua:**40 个 tagged 检测器文件,0 失败**
  (**FAST SUBSET,not the full suite;#124 动态半未跑且不声称**)。
- **exit sources(GH #267 4b,照抄工具自己的归因,不手编)**:`legs run 8`、
  `FINDINGS (exit 3) = cadence`、`UNCERTIFIABLE (exit 2) = none` ⇒ **selfcheck worst exit: 3**。

## 2. 成本(闸 iii)

- 开工 running/pending 实例:**空**。
- MTD **$71.458**(`budgets`,快照 `2026-08-30T11:33:48Z`);读数 ≥ $35 ⇒ 脚本自动花 $0.01 用 CE 复核:
  **$71.4580340342**,与 budgets **逐位一致**(**连续第三十轮**)。
- 按 (甲) 记未入账波次:**W28 已出 12h 窗口**(起飞 `06:18:58Z`,快照 `11:33Z` 晚它 5.2h,
  但本轮已距起飞 8.9h;上一轮已按 $0.80 计入且快照未变)⇒ 保守起见仍计:
  **围栏 = 71.458 + 0.80 = $72.26 ≤ $80,余量 $7.74**。
  最坏情形(W28 全程降级按需 $2.15)= **$73.61**,同样过闸。
- 刹车 $90 / owner 线 $100 **均未接近**;**本轮不跨任何新告警档**。
- **本轮现金 = CE $0.01 + S3 LIST/GET + 若干免费 `describe-*`,零 EC2。**

## 3. 收割:无新语料 ⇒ 本轮不产生 `HARVEST` 行

- `validation/` 末次对象 `l1trade+45ids-2b581faac0c3_20260830_0712_run.log` @ **`07:12:37Z`**;
- `soak/` 末四个前缀即 W28 的四个 run(`…_990f5c`/`…_90463d`/`…_e706a3`/`…_8fffe9`),
  末次对象同为 **`07:12:37Z`**;
- ⇒ **09:30Z 那轮已按标准路径(`recover_verdict.py` 从 S3 逐局全量重算)收割完毕,之后 S3 零新增。**
- W27+W28 的 45-id 家族 **8 粒并池读数**(gpm **+26.60**、**7/8**、**412 计分局**、零排除、
  留一法全正、mean/SE 2.70)**仍备总监裁**,本轮不重算、不改动。

## 4. ⭐⭐ 本轮实质工作:把 #332 的 (b) 从「无法判断」变成「量得出来」

上一轮把 W28 的 `BRACKET VIOLATED` **假设**成代理量造成的假阳性(末次 S3 上传是存活的**下界**),
但当时只能说「≥39.68,与 >42.6 并不矛盾」——**那是一个不等式,不是一个读数**。
本轮把那个偏差**测出来了**。

### 4.1 先确认:两个 AWS 侧来源都已死,而且**朝相反方向失效**

| 来源 | 本轮实测 | 失效形状 |
|---|---|---|
| `describe-spot-instance-requests --spot-instance-request-ids sir-6nkfha3n` | `InvalidSpotInstanceRequestID.NotFound` | **响** —— 报错,不会被误读 |
| `describe-instances --instance-ids i-0f37ed0041eaca3b3` | **`{"Reservations": []}`,exit 0** | **哑** —— 空列表 + 退出码 0 |

⚠️ **新登记一条危险侧观察**:`describe-instances` 对**已经不存在**的实例
**不报错、退出码 0、返回空列表**。任何「数一下这波有几台被 `no-capacity` 收走」的脚本,
在实例记录过期之后会**照常输出 0**,而 0 恰好就是「零抢占」的正确答案的样子。
**这与「shallow clone 的空输出被读成无漂移」、「没跑的门不打印任何东西」是同一族**:
**空输出在语义上与好消息同形。**

### 4.2 偏差是可以量的:W24/W25 同时有一手 SIR 读数和可免费重取的 S3 时间

W24/W25 的 `machines[]` 有 **`create` + SIR 一手 `update`**,且有 `run_id` ⇒ 可与 S3 末次上传逐台配对。
八台全部 `instance-terminated-by-user`:

| 波 | seed | SIR 存活 | S3 代理存活 | 偏差 |
|---|---|---|---|---|
| W24 | 1601 | 54.17m | 51.78m | **2.38m** |
| W24 | 1603 | 42.75m | 39.68m | **3.07m** |
| W24 | 1633 | 40.63m | 38.48m | **2.15m** |
| W24 | 1641 | 42.58m | 39.68m | **2.90m** |
| W25 | 1603 | 54.80m | 51.48m | **3.32m** |
| W25 | 1633 | 43.15m | 40.27m | **2.88m** |
| W25 | 1664 | 45.13m | 41.95m | **3.18m** |
| W25 | 1770 | 45.40m | 42.10m | **3.30m** |

**n=8,偏差 min 2.15 / max 3.32 / 均值 2.90 min,量程只有 1.17 min。**
物理意义直白:机器传完最后一个对象之后,还要跑完关机流程,SIR 才落 `UpdateTime`。

### 4.3 把偏差加回 W28

| seed | S3 代理 | +2.90 修正 | vs 换腿点 40.0 | 配对 |
|---|---|---|---|---|
| 1850 | 39.68m | **42.58m** | **ABOVE** | ab28/ba14 PAIRED |
| 1938 | 52.63m | 55.53m | ABOVE | ab42/ba16 PAIRED |
| 2130 | 40.83m | 43.73m | ABOVE | ab28/ba14 PAIRED |
| 2142 | 53.43m | 56.33m | ABOVE | ab34/ba24 PAIRED |

**四台全部落在换腿点右边 ⇒ `BRACKET VIOLATED` 消失。**
更值得记一笔的是**落点**:seed 1850 修正后 **42.58 min**,而历史最短配对存活是
**42.60 min**(W21 seed 995)——**两者相差 0.02 min**。
即:W28 不但没有推翻 `(34.8, 42.6]` 这个区间,它**几乎正好落在区间的那条边上**,
是对该边界的一次独立复现,不是对它的反例。

### 4.4 ⛔ 诚实边界(这几条决定了本台**不能**据此自行放行)

1. **偏差只在 `instance-terminated-by-user` 上测过(8/8 自毁)。**
   被抢占的机器收到的是 ~2 分钟通知然后被强拿走,「末次上传 → SIR 落时间」的关系
   **没有任何一台的实测**。而区间的**下沿 34.8 min(最长孤儿,W19-R)恰恰来自被回收的机器**
   ⇒ **不许把 +2.90 这个自毁偏差套到孤儿那一侧去**,否则等于用一种机器的常数去移另一种机器的边界。
2. 本节做的是**修输入**,不是**改常数**,也不是**降门**。三者都是总监的地界(§BX / 载体门先例)。
3. 门的两条合取判据在 W28 上**本来就都独立地为假**(4/4 配对、零抢占),
   语义上答案是 exit 0 —— 但**本台报的是工具的退出码,不是自己对工具内部的复读**。
   本节只是把「为什么它退 2」从猜测变成读数,**裁定权不变**。

⇒ **本轮仍然不发 W29。** 材料已备齐,已追评到 GH #332。

## 5. 不发波(逐条记账)

- **路 a 用不上**:queue **50 条 / 23 条 pending** 逐条看过,**无一需要新 EC2**
  (14 条 `ROUTED_ARCHIVE_SCAN`/`REDUMP` 明写零 EC2;余下 `REJECTED` 2 / `RECEIVED_NOT_SCHEDULED` 2 /
  `DEFERRED_PENDING_W3_RESOLVE` 2 / 过期的 `strategy-5b`);唯一未裁的 `strategy-25`(`creepthink`)
  **不在 test_set 第 2 行**,不能自行 arm。
- **节流闸**:(i) ✓(W28 起飞 `06:18:58Z`,6h 闸已于 `12:18:58Z` 解锁,现已过 8.9h);
  (ii) ✓(`bots/` 相对上一轮的钉 `d42f90a0` 有新 commit,见 §6);(iii) ✓($72.26 ≤ $80)。
- **真正拦住 W29 的仍然是 #271 那道门** —— 总监 `20260830T130354Z` 报告 §120 明写
  「**GH #332 / #271 …(拦着 W29)未裁 —— 本轮工作单元已满**」,并把它列为下一轮第 1 优先。
  **本台按章程「exit 2 = 未查 ≠ 通过」继续不发,不自行放行。**

## 6. ⚠️ 上一轮交棒的「W29 已备齐,零重做」**本轮已失效**(本台自查)

上一轮把 W29 钉在树 `d42f90a0`。本轮 `git ls-remote origin main` = **`ac6ec401`**:

- `d42f90a0..ac6ec401` 共 **19 个 commit**;
- 其中 **`2b356024`(hero 13:55Z,`abilityASBonus`)动了 `bots/`**。

⇒ 上一轮那句「零重做」**不再成立**:接线门是钉着 `--ref` 跑的,树一动就必须重跑。
**本轮已重跑并刷新**:

```
python3 tools/batch_test/check_armed_wiring.py --cand <44-id> --ref ac6ec401…
exit=0   all 44 armed ids wired on ac6ec401bdca894f8304827354c492adf71b6290
```

**顺带更正一处路径**:章程与历次交棒写的是
`tools/batch_test/soak/check_armed_wiring.py`,**该路径不存在**(本轮实测 `Errno 2`);
真实路径是 **`tools/batch_test/check_armed_wiring.py`**(无 `soak/`)。
照交棒抄的那一行会 exit 2,而 **exit 2 在这里长得像「门跑了」**——
与本轮 §4.1 登记的哑失效同族。已在下方交棒点名。

**W29 现状(刷新后)**:树 `ac6ec401`(`ls-remote` 已核 = 本地 HEAD)、arm 串 **44 id / 386 字节**
(与上一轮逐字节相同,`test_set.md` 第 2 行未变)、接线门 **exit 0(44/44)**、
种子索引 W28 四粒已折入(本轮无新波,无需再 `--build`)、
拓扑 4×1 + 显式四个不同 `--az` + `--rec-slots 1`(#308 未裁,保守默认 (A))。

## 7. 局数(铁律 7)

- **(a) 上一波次 W28 最终**:**224 落盘 / 200 计分**,`unfinished 0`,`engine_natural 224/224`,暖场 24
  (暖场不算有效局);per-seed `ab/ba` = 1850 **28/14**、1938 **42/16**、2130 **28/14**、2142 **34/24**;
  `min_arm_depth 8`、`thin_arm_seeds []` ⇒ **零排除**;四台全自毁、**零抢占**;
  `.dem` **14 个**(3/4/3/4)。
- **(b) 本轮在跑波次**:**无**。S3 零新增,无实时进度可报。
- **本轮无 `HARVEST` 行**(无新语料可收割,见 §3)。

## 8. 泄漏检查(两次)

- 开工:`check_costs.sh` 的 running/pending 段 **空**。
- 收尾:`check_costs.sh --leak-only` ⇒ **0 台在跑,零泄漏**。
  (本轮不发波,收尾判据就是「0 台在跑」本身,不是 MTD 数字。)

## 9. 交棒

1. **⭐⭐⭐ 总监 —— #332 的 (b) 本轮有读数了,请连同 (a)(c) 一并裁**(已追评 GH #332):
   S3 代理相对一手 SIR 的偏差 **+2.90 min(2.15–3.32,n=8)**;W28 四台修正后全在换腿点右边;
   seed 1850 修正后 **42.58m** 与历史最短配对 **42.60m** 相差 0.02m ⇒
   **换腿点常数不需要重推,需要的是给代理量登记一个偏移**。
   **⛔ 但 §4.4 那三条边界务必一起读** —— 偏差只在自毁机器上测过,
   而区间下沿 34.8m 来自**被回收**的机器,**不许把这个偏移套过去**。
2. **⭐⭐⭐ 总监 —— #271 的门 W27/W28 两次发波都没跑**(上一轮 ② 未落地,总监 `20260830T130354Z` §125-126
   自己也记了「本轮未落地」):建议把 `reclaim_blind` 写成 `wave.json:gates` 的**必填键、空缺即视为没跑**,
   与载体门/接线门同等对待。**第二轮催。**
3. **⭐⭐ 下一轮本台 —— W29 已按新树 `ac6ec401` 刷新完毕**(接线门 exit 0/44),
   只等 #271 解门;总监当轮仍未裁则按章程继续不发。
   ⚠️ **发波前必须再核一次 `ls-remote`** —— 本轮的教训就是隔一轮树就动了(19 个 commit,1 个动 `bots/`)。
4. **⭐⭐ 总监/全队 —— 章程里 `check_armed_wiring.py` 的路径是错的**(多了一层 `soak/`),
   照抄会 exit 2 而 **exit 2 在这里长得像门跑过了**。真实路径 `tools/batch_test/check_armed_wiring.py`。
   本台已在本报告 §6 更正,但**章程正文的那几处引用建议由总监统一改**(跨章程,非本台地界)。
5. **⭐ 总监 —— 12.3h cadence 洞连续第八轮**,工具第三轮给出「窗口内 3 个命名不合法的报告文件,
   fix the NAME」⇒ 催办措辞应改为「修文件名」,不是「空转」。
6. 存量:**#207 `zusstatic` 第三十二波 armed**;**#218 后续第二十八轮**;
   **#282 连续第七波零 MISMATCH,第十次建议关闭**;**#295 建议关闭**;**#285 第十一轮催**;
   **#329 待裁**;**#321 待裁**;#313/#290/#291/#298/#299 照旧;
   #217/#211/#225/#180/#171/#200/#181/载体门 PARTIAL/`stable-v*` tag/`campdanger`/§BL.4/#75 照旧。

## 10. 纪律自查

- **铁律 6**:`bots/`/`game/` **一行未改**(本轮只写报告 + 章程);`core.hooksPath` 已上膛;
  **未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行**;动态半(#124)**未跑且不声称**。
- **铁律 6 顺序条款(GH #290)**:本轮**先 push 再发表带引用的评论**(#332 追评在 push 之后)。
- **铁律 11**:MCP 未触发 `requires approval`,GitHub 读写走 `mcp__github__*` 正常。
- **铁律 4**:本轮无新读数产生,§3 的并池读数原样转述、未重算、未加减结论。
- **AWS 花钱**:仅 CE $0.01(脚本按 ≥$35 规则自动复核),**零 EC2**。

## 11. 本轮 token

```
TOKENS total_in=3,838,054 out=28,425 turns=34
```

(统计时刻之后的收尾回合不计入,量级参考足够。)

