# 批测台 2026-08-27T21:20Z — W18 收割:三波以来第一次买到种子,#252 的分散在账单侧兑现;W18-R 触到降级阶梯第 3 级(首次)

**一句话**:W18 四台里 **2 台跑完自终、2 台被 `us-west-2b` 的容量事件回收** ——
**#252 的 AZ 分散把「整波归零」压成「丢 2 粒」**,这是连续三波(W17 0 粒 / W17-R 0 粒)之后
**第一次有可用种子**;挂了两轮的池化预登记**本轮执行,n=6**,结论是**主读数与零不可分**
(gpm −20.28,逐粒均值 SE ±18.38);补跑 W18-R 时 **c6i / c6a 两种机型、四个 AZ 全部无 spot 容量**,
**降级阶梯第 3 级(`--on-demand`)自立条以来首次真正用上**,同时 **GH #256 的两种新 stderr 行首次实地触发**。

---

## 1. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` — **worst exit 3(非阻断)**:

- `stable-v1` / `stable-v2` 两锚点 **ok**(EXISTS / PINNED / SHIPPED 三项全过);
- unlanded commits:**OK**(certifiable window 内无未落地工作);
- **cadence 1 处洞:`replay-check` 07:19Z → 13:28Z = 6.1h** —— **与上一轮同一处、同一值,连续第四轮**。**不是本台的。**
- 未裁队列:RIDESHARE **1**(`hero-21`,§BB.4 本轮该裁),OTHER none,open requests 37;
- trunk **44 passed / 0 failed / 0 uncertifiable**(与上一轮同值);快 Lua 检测器 **13 个文件 0 失败**(上一轮 12,+1)。

---

## 2. 成本(铁律 1 / 章程 §2)

| 项 | 值 |
|---|---|
| `budgets` MTD | **$50.671** |
| CE 复核($0.01,脚本在 ≥$35 时自动做) | **$50.6709276212** — **逐位一致,连续第十一轮** |
| budgets 快照时刻 | ⚠️ **`2026-08-26T23:46:06Z`** —— **与上两轮同一张、逐位未动** |
| forecast / limit | 76.999 / 100.0 |
| 刹车线 $90 | 未接近 |

**⚠️ 滞后照旧朝危险侧失效(连续第三轮同一形状)**:快照时刻不动 ⇒ **W15-R / W16 / W16-R / W17 / W17-R / W18 一台都没进这个 MTD**。
所以围栏算术**不用裸 MTD**(章程 §2 甲):

```
围栏 = 50.671 + W15-R 1.69 + W16 0.80 + W16-R 0.190 + W17 0.479 + W17-R 0.313
                + W18 实测 0.613 + W18-R 预估 1.010(按需)
     = $55.77  ≤ $60   ⇒ 闸 (iii) 过
```

**本轮 budgets 读数逐位未动 ⇒ 无新增跨档**($50 档的书面解释已在 08-27T15:15Z 报告 §2 立卷,本轮不重复计次)。

---

## 3. ⭐⭐ 主产出之一:收割 W18 —— 三波以来第一次买到种子

### 3.1 先查 SIR(上一轮点名的前置动作,照做)

| 种子 | 实例 | AZ | Status.Code | Update | 存活 |
|---|---|---|---|---|---|
| **918** | `i-0259ffdc874167606` | **2a** | **`instance-terminated-by-user`** | 19:14:36Z | **0.909 h** ✅ 跑完自终 |
| 921 | `i-05e52b9104584c699` | **2b** | ⛔ `instance-terminated-no-capacity` | 18:33:29Z | 0.219 h |
| **924** | `i-0bfbc256216fbefbf` | **2d** | **`instance-terminated-by-user`** | 19:15:36Z | **0.920 h** ✅ 跑完自终 |
| 926 | `i-09f116202274284ed` | **2b** | ⛔ `instance-terminated-no-capacity` | 18:47:27Z | 0.449 h |

**读法**:`by-user` = 看门狗/自毁正常收尾(**不是回收**);`no-capacity` = 被 EC2 收走。
**回收的两台恰好是 `us-west-2b` 那两台,`2a`/`2d` 两台全须全尾** —— 这正是 #252 设计要买的形状。

### 3.2 ⭐ #252 的分散在账单侧兑现(本轮唯一的正面结论)

W17(四台全 2b)与 W17-R(四台全 2b)各买到 **0 粒**;W18 把四台摊到 **3 个 AZ**,同一个 `2b` 事件
**只带走了落在 2b 的那 2 粒**。**「一次 AZ 事件最多损失 1 粒」的上界没能完全兑现,原因不是 #252 失效,
是 #256 的回退把 921 送回了 2b**(§4.2 上一轮已立案)——**如果 921 落在它请求的 `2c`,这一波会是 3 粒。**
**#256 的代价因此是可计量的:恰好 1 粒种子。**

### 3.3 W18 单波读数(`--cand-ref` 未传,`contrast=vs_stable`)

`files_seen 154 / games_loaded 154 / source_dirs 4 / unparseable 0`,`scored 102`,`unfinished 0`。

| 种子 | ab | ba | gpm | xpm | deaths | last_hits | winrate |
|---|---|---|---|---|---|---|---|
| 918 | 35 | 18 | **−0.95** | +9.99 | −0.15 | −0.18 | 0.500 |
| 921 | 6 | **0** | — 缺臂 | | | | |
| 924 | 34 | 15 | **+40.13** | +38.19 | −0.33 | +3.64 | 0.515 |
| 926 | 26 | **0** | — 缺臂 | | | | |

⛔ **921 / 926 的 32 局单腿孤儿不并进任何池**(沿用 W15-R / W16-R 立的预登记读法)。
**本轮新记一条**:W18-R 补跑的是**同两粒种子号**,所以 **孤儿目录与补跑目录同号** ——
把它们一起丢进池子会让 921/926 的 ab 腿**分属两个时段两台机器**,`ab/ba` 配平被单侧灌水,
**而 `recover_verdict.py` 照样会印一个数**。下轮收割 W18-R 时**只放补跑目录,不放 W18 的 `f43908`/`e17d24`**。

---

## 4. ⭐⭐ 主产出之二:挂了两轮的池化预登记,本轮在 **n=6** 上执行

池 = W16 三粒(888/895/906)+ W16-R 一粒(896)+ **W18 两粒(918/924)**,
**每 run 独立子目录再指向父目录**(GH #225 的 R2 保护)⇒ `source_dirs 6`、`pooled_overridden 0`、
`files_seen 337 / games_loaded 337 / unparseable 0`、`scored 302`、`unfinished 0`、`suggested=hold_or_reject`。
⛔ **不可与 W15-R 池化**(那是 37 id / `79f32c92` 树)。

### 4.1 逐粒(主读数)

| 种子 | ab/ba | gpm | 半差 (ab−ba)/2 |
|---|---|---|---|
| 888 | 30/12 | −34.27 | −169.54 |
| 895 | 39/19 | +6.72 | +77.15 |
| 896 | 29/13 | −45.61 | +204.77 |
| 906 | 41/17 | −87.72 | +104.67 |
| **918** | 35/18 | **−0.95** | **−175.28** |
| **924** | 34/15 | **+40.13** | **−167.61** |

**逐粒均值(主读数,带 n 与 SE —— 预登记要求的形状)**:

| 量 | n=6 均值 | 逐粒均值 SE | ab 层 | ba 层 | 两层同号? |
|---|---|---|---|---|---|
| **gpm** | **−20.28** | **±18.38** | −41.26 | **+0.69** | ⛔ **反号** |
| xpm | −4.70 | ±11.40 | +3.08 | −12.49 | ⛔ 反号 |
| deaths | +0.11 | ±0.16 | +0.01 | +0.20 | 同号 |
| last_hits | −0.69 | ±1.15 | +2.79 | −4.16 | ⛔ 反号 |

`comps_better`:gpm **2/6** / xpm 3/6 / deaths 3/6 / last_hits 1/6 / winrate 3/6;winrate 均值 **0.502**。

### 4.2 结论(三条,逐条按预登记的判据写)

1. **主读数与零不可分**:gpm −20.28,|t| = 20.28/18.38 = **1.10**。
   **上一轮登记的 −40.22(n=4)在加两粒后移了 +19.9 gpm** ——
   **那个「−40」不是稳住了的效应,是 4 粒的抽样位置**。本轮把它降级为登记值,不作结论。
2. **铁律 4(i):gpm / xpm / last_hits 三个量的 ab 层与 ba 层反号 ⇒ 读噪声,不写进结论。**
   只有 `deaths` 两层同号(+0.01 / +0.20),而它的均值 +0.11 也只有 0.7 SE。
3. **⭐ 判别力条件更新(GH #204 要的那个 n)**:半差 sd 在 n=6 上是 **169.56**(n=4 时估的是 155)。
   `169.56/√n < 40` ⇒ **n ≳ 18**,比上一轮估的 **n ≳ 15 更差,不是更好**。
   **按每波净产 2 粒的当前实测速率,那是 6 波以上** —— 请总监据此重裁 §4(i) 的施加粒度。

---

## 5. W18-R 发波(补跑;⭐ 降级阶梯第 3 级首次用上)

### 5.1 三条闸

- **(i) 6h 例行节流:本波不受约束** —— 例行锚点 = W18 首台 `18:20:00Z` ⇒ **W19 解锁 `2026-08-28T00:20:00Z`**,
  `date -u` 读到 **21:13Z 时还差约 3.1h**。**本波是补跑不是例行波**,沿用 W15-R / W16-R / W17-R 的逐字先例
  (§4b(i) 的立法对象是例行波次);**保守起见仍不把补跑取作下一次的例行锚点**(与上一轮同裁法)。
- **(ii) 树可比:仍 pin `--ref d9585a29…`(第四轮)**。远端 tip 已是 **`313f5679`**;
  `git cat-file -t d9585a29…` = `commit` ✅。缺的是种子不是新树,发 tip 会把 6 粒池子清零重来。
  **诚实边界**:tip 上的天赋定价与 `abil1st` 等 commit **本波仍测不到**,连续第四轮记账。
- **(iii) 成本**:$55.77 ≤ $60 ✅(§2)。

**§4a 无适用对象**:queue.json 45 条里 pending 22 条,逐条扫过 —— 唯一带 `APPROVED*` 的是 `strategy-5b`
(`director.wave` 写 **W6**,早已过去),其余是 `ROUTED_ARCHIVE_SCAN`(零 EC2)/ `RECEIVED_NOT_SCHEDULED` /
`DEFERRED_PENDING_W3_RESOLVE` / `REJECTED`。**没有任何一条在等本台发波。**

### 5.2 两道门 —— ⚠️ 载体门 **exit 1**,本台照实登记后放行

| 门 | 调用 | 结果 |
|---|---|---|
| 接线门 | `tools/batch_test/check_armed_wiring.py --cand <40串> --ref d9585a29…` | **exit 0** `all 40 armed ids wired`(先落盘再取 `$?`,没接 `\| tail`) |
| 载体门 | `soak/seed_draft.py 921 926 --assert-carrier 'crystal_maiden,zuus,lion,obsidian_destroyer,skeleton_king'` | ⚠️ **exit 1** |

**exit 1 的成因是子集算术,不是新信息**:`crystal_maiden` / `zuus` FULL 2/2、`lion` PARTIAL(921)、
`obsidian_destroyer` PARTIAL(926)、**`skeleton_king` ABSENT 0/2**。而 W18 发波时**同一道门在四粒上是 exit 0**,
SK 的两个载体**正是已经落地的 918 / 924**。补跑的两粒**不是本台挑的**,是被 2b 拿走的那两粒,
**恢复的正是原定剖面,不引入新倾斜**。
**要登记的后果说清楚**:这两粒**不给 SK 系 id 提供任何新语料**;8 粒池子里 SK 的供给仍是 2/8(918/924),
而 CM/zuus 是 8/8。**这是本台第一次在 exit≠0 的载体门上放行,理由写在这里备总监复核。**
角色门 / 等级门 id 的载体门仍 **no-op**(缺口 **GH #140**,**第二十二轮**)。

### 5.3 ⭐⭐ 容量降级阶梯:三级全走过,**首次走到第 3 级**

| 步 | 动作 | 结果 |
|---|---|---|
| (1) | `c6i.4xlarge` spot,显式 `--az`(921→`2a`,926→`2d`) | ⛔ **两次调用各走完四个 AZ,全 `InsufficientInstanceCapacity`** |
| (2) | 同参数 `--type c6a.4xlarge` spot | ⛔ **同样四个 AZ 全无容量** |
| (3) | `spot_run.sh --on-demand`(仍自毁 + 2h 看门狗) | ✅ **两台起飞** |

**这是本条阶梯立条以来第一次真正走到第 3 级** —— 前几轮的 spot 失败都发生在**运行期回收**,
这一轮是**起飞期容量**,是 owner GH #158「除非没有 spot 的机器」那个例外条件的**首次成立**。
错误码原文(EC2 自己给的建议里连「换个 AZ」都给不出来,四个 AZ 互相推荐):
`There is no Spot capacity available that matches your request` / `We currently do not have sufficient
c6i.4xlarge capacity in the Availability Zone you requested`。

### 5.4 ⭐ GH #256 的两种新 stderr 行 **首次实地触发**(验收:搭本波的车,零额外支出)

上一轮请求的行为**逐字出现了,而且是两种行都出现**:

```
! dota2bot-soak-spot-1: launch in us-west-2a failed -- ... InsufficientInstanceCapacity ...
! dota2bot-soak-spot-1: re-aiming inside the ring -> us-west-2b      <- 环内改投(不是降级)
! dota2bot-soak-spot-1: re-aiming inside the ring -> us-west-2c
! dota2bot-soak-spot-1: re-aiming inside the ring -> us-west-2d
!! dota2bot-soak-spot-1: AZ RING EXHAUSTED (us-west-2a us-west-2b us-west-2c us-west-2d)
   -- falling back to EC2-chosen placement, which is where #256's correlation comes back.
   Treat this line as a wave-level warning.                          <- 真降级 / 波次级告警
```

- **环内改投**:两次调用各 3 次(921 从 `2a` 起走 `2b`→`2c`→`2d`;926 从 `2d` 起走 `2a`→`2b`→`2c`)——
  **环从请求 AZ 的下一个开始走,不是从环首**,与 #256 的裁定逐字一致 ✅
- **`AZ RING EXHAUSTED`**:两次调用各 1 次,**且这一次不是「回退到 2b」而是整个区域都没有容量**,
  回退后的 EC2-chosen 放置**同样失败** ⇒ **本轮它是如实告警,不是 #256 描述的那个坏回退**。
- ⇒ **#256 的两条行为(环内改投 + 波次级告警)本轮全部实证通过**,请总监据此收口。

### 5.5 发波记录

| 项 | 值 |
|---|---|
| `--dry-run` 先行 | ✅ `ref` / `slots=16` / `rec_slots=12` / `watchdog=2h` / `us-west-2` / `az ring` 全对上,**波次预算闸未拒发** |
| 拓扑 | **2 台 × 1 种子**(921 / 926),`--games 12`,`--rec-slots 12`(GH #75 **第十四轮维持 12**) |
| 未传 `--cand-ref` | ✅ 单臂 `contrast=vs_stable`,**收割时也不要传** |
| 传了 `--on-demand` | ⚠️ **是**,理由见 §5.3(阶梯第 3 级),**这是例外不是默认** |
| 实例 | `i-04a0789046bd97674`(921,**us-west-2a**)/ `i-05098c54c2b779dfd`(926,**us-west-2d**) |
| `InstanceLifecycle` | **`None` × 2 = 按需**(与 §5.3 的降级一致;`run_id` 的 `spot_` 前缀**没有当证据**) |
| AZ 分散 | **2 台 2 个不同 AZ** ✅(#252 的形状,2 台拓扑下的满格) |
| `soak-run` 标签 | 两个值 **`…_109abf` / `…_4aa0a5`**,**两两不同** ✅ |
| launch | 21:18:47Z / 21:18:51Z |
| 单波预估 | **$1.010**(2 × **0.745 h** 实测按需机时 × $0.673 + IPv4)——用的是 W10–W14 的**实测**费率,**不是已作废的 $3.05 外推** |

---

## 6. 局数(铁律 §7)

- **(a) 上一波次定案 —— W18**:**154 落盘 / 102 计分 / unfinished 0 / 暖场 52**;
  **有效种子 2 粒**(918 ab35/ba18、924 ab34/ba15),**孤儿 32 局**(921 ab6、926 ab26)。
  上一轮预期 ~208 落盘 / ~180 计分 ⇒ **落盘 −26%、计分 −43%**,**差额全部由 2b 的两台回收解释**。
- **`d9585a29` 树池子**:**569 落盘 / 302 计分 / 6 粒**(上一轮 415/200/4 ⇒ **落盘 +154、计分 +102、粒数 +2**)。
  ba 占比(计分)= 94/302 = **0.311**(W11–W18 区间 0.304–0.325,**在带内**)。
- **孤儿存量**:W15 30 + W16(896)27 + W17 128 + W17-R 64 + **W18 32** = **281 局**,至今未并进任何池。
- **(b) W18-R 在跑**:2 台 × 1 种子 × `--games 12` × 2 腿,预期 **~104 落盘 / ~90 计分**。
  **按需 ⇒ 无回收风险**,这一波的预期值比前四波都更接近下界(唯一残余风险是看门狗到点前跑不完第二腿)。

---

## 7. 泄漏检查(章程 §6)

- **开工**:`check_costs.sh` 的 running/pending 段 **空** —— W18 四台已全部离场(两台自终、两台被回收)。
- **收尾**:`check_costs.sh --leak-only`(零成本)⇒ **恰 2 台**,`i-04a0789046bd97674` / `i-05098c54c2b779dfd`,
  **正是本轮自己发的 W18-R**,id 逐个对上,全 `running`。常驻只剩 AMI `ami-0a990a26d89c66547` + 快照 —— **不是泄漏**。
- ⚠️ **作证边界保留(连续第六轮)**:SIR 能回溯已消失的 spot 实例(#239 路 4 **第五次实证**,本轮靠它区分
  「自终 2 台 vs 回收 2 台」),**但保留窗口仍未知**。**且注意:本轮 W18-R 是按需,没有 SIR** ——
  下轮收割 W18-R 时**这条证据通道不存在**,要靠 `describe-instances` 的 `StateReason` / 落盘局数判断。

---

## 8. 铁律 6

`bots/` 与 `game/` **一行未改**(本轮只写报告 / 章程 / queue)。静态门由 `.githooks/pre-push` 自动跑
(`core.hooksPath` 由开工自检上膛),**未使用 `RULE6_BYPASS`**。
动态半(`lua5.1 tests/run_tests.lua`,~100 min,GH #124)**未跑,也不声称跑过**。

---

## 9. 交棒

1. **⭐ 下一轮本台 —— 收割 W18-R**:run 前缀 `…_109abf`(921)/ `…_4aa0a5`(926),
   **每 run 独立子目录**、**不传 `--cand-ref`**;
   **⚠️ 千万不要把 W18 的 `f43908`/`e17d24` 两个孤儿目录一起放进池**(§3.3,同种子号会灌单侧 ab 腿);
   收满即为 **8 粒**,**照做 §4 的同一套读法**(逐粒均值 + n + SE,两层都给都只登记)。
   **按需波没有 SIR**,判存活要换证据源(§7)。
2. **6h 闸**:例行锚点仍 = **W18 首台 `18:20:00Z` ⇒ W19 解锁 `2026-08-28T00:20:00Z`**(W18-R 是补跑,**不作锚点**)。先 `date -u` 再比。
3. **⭐⭐ 总监 —— GH #256 可结**:两种新 stderr 行**本轮首次实地触发并逐字符合**(§5.4);
   同时 §3.2 给出了 #256 缺口的**代价计量:恰好 1 粒种子**(921 若落在请求的 `2c`,W18 会是 3 粒)。
4. **⭐ 总监 —— GH #204(第十二轮,本轮**有**新证据)**:预登记在 **n=6** 上执行完毕;
   **判别力条件从 n ≳ 15 恶化到 n ≳ 18**(半差 sd 155 → 169.56),且 gpm 主读数 |t| = 1.10。
   **请带 n 重裁 §4(i) 的施加粒度**,并裁一句:**每波净产 2 粒的速率下,要不要继续买这个 n**。
5. **⭐ 总监 —— #233(a)** 仍未裁;本轮新数据点:**W18 实测 $0.613**(路 4,逐台按 AZ 现价求和 + IPv4),
   vs 上一轮按需口径预估 **$2.24** ⇒ **偏离 3.7 倍**,方向仍是「朝安全侧失效、不会自己举手」。
6. **⭐ 总监 —— 载体门首次 exit≠0 放行**(§5.2):补跑子集使 `skeleton_king` ABSENT。
   请裁:**补跑波的载体门该不该按其父波的四粒读**;若不该,本台下次遇到同形状就不发。
7. 总监 —— **#207(第十二轮)** `zusstatic` **连续第十一波空转**(W18 终于有语料,但该 id 的载体仍未出现)。
8. 总监 —— **#218 后续**:`salveally` / `salveyield` **连续第七轮不在第 2 行**。
9. 总监 —— **#239 路 4 第五次实证**(本轮是它唯一能区分自终 vs 回收的场合);保留窗口仍未知。
10. 录像组 —— `abilanc` (a) 取证:**W18 的 154 局 `.dem` 已落盘(12 槽 × 4 台),其中 102 局是双腿可配对的**,
    比前两波的纯孤儿语料更好用;W17 的 128 局 + W17-R 的 64 局单腿孤儿仍可逐帧取证;
    **W14 的 156 份约 09-16 过期,优先消化**。
11. replay-check 组 —— 报告节奏 **6.1h 洞**(**连续第四轮,同一处同一值**)。
12. 存量催办:#217(第十一轮)/ #211(第十二轮)/ #225 / #180(第十八轮)/ #171(第十六轮)/
    #200(第十五轮)/ #181 / 载体门 PARTIAL(**第二十二轮**)/ `stable-v*` tag(第二十轮)/
    `campdanger`(第十五轮)/ §BL.4 机械化 / #75(第十四轮,维持 12)。

---

## 10. 本次会话 token 用量(铁律 8)

```
TOKENS total_in=4,267,977 out=32,068 turns=39
```
(统计时刻之后的收尾回合不计入。)
