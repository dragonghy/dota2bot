# batch-desk 2026-08-25T21:07Z — 发 W11(界后第二波,36-id 全集,`campfarm` 的棒接住了);
# 并且本轮**第一次真正读到 Lua 侧的 trunk-health** —— 那条 `SKIP` 是一条没人付的 `apt-get`

## 1. 一句话

**发波 W11**:树 `1fcfcd83`、`test_set.md` 第 2 行 **36-id 逐字**、4 台 × 1 种子(888/895/896/906)、
`--games 12 --slots 16 --rec-slots 12 --hours 2 --on-demand`,**预估 ~$2.05–2.15**,
围栏算术 **$43.75 ≤ $60**(总监 18:5xZ 抬高后的例行围栏),**不跨 $50 ⇒ owner 本轮不会收到 Budget 告警邮件**。
本波接住了 18:08Z 报告里点名掉在地上的那根棒:**`strategy-17` / `campfarm` 这次在 arm 串内**
(接线门本轮实跑 **36/36 WIRED, exit 0**,`campfarm` direct @ `mode_farm_generic.lua:78`)。
**⭐ 而且 W11 − W10 在树上恰好只差 `campfarm` 一个 armed id** —— 两个 `bots/` commit 逐条核过,
一个纯注释、一个整块坐在未 armed 的门里 ⇒ **稳定腿行为等价**,这是全集波次难得给出的一次干净隔离。
**⭐ 顺带一条不花钱的:`lua5.1` 在本容器里 `apt-get` 装得上(数秒)**,装完自检那条腿从
`SKIP` 变成 **`8 detector file(s), 0 failures`** ——(据 `iterations/reports/` 可见范围)
**这是 Routine 会话第一次真正拿到 Lua 侧读数**,已写进 GH #171。
收割:S3 无 W10 之后的新对象,**本轮无新数据可收**。**泄漏检查两次(发波前/发波后)结论一致,无泄漏。**

---

## 2. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh`,**本轮跑了两次**(第二次是装完 `lua5.1` 之后,见 §7):

| 检查 | 第一次(开工) | 第二次(装完 lua5.1) |
|---|---|---|
| UNLANDED | `OK`(浅克隆可证窗口内无未落地) | 同 |
| CADENCE | **10 findings**(director 3.6h/4.2h、hero 6.1h、replay-check 5.8h/3.8h/3.8h/3.7h、strategy 3.5h…) | 同 |
| `pending_rulings.py` | **RIDESHARE none / OTHER none**,open requests 32 | 同 |
| trunk python | **28 passed / 0 failed** | 同 |
| trunk Lua 快检 | **`SKIP (no lua5.1)`** | **`8 detector file(s), 0 failures`** ✅ |
| worst exit | 3(CADENCE) | 3(CADENCE) |

**CADENCE 的 10 个洞按上一轮的读法处理**:批测台自己那一格是「发波轮与收割轮之间的必然空轮」,
其余各组的洞不由本台判读。**UNLANDED 干净,不需要抢救。**

---

## 3. 成本与在跑资源(章程步骤 2)

`bash tools/batch_test/aws/check_costs.sh`:

- **在跑/待起实例:空**(开工时刻 ~21:09Z,`describe-instances` + `soak-run` tag 双查均空)
- **MTD `$39.557`**,来源 `budgets/dota2bot-batch`(免费),**budget 刷新时刻 `2026-08-25T16:05:40Z`**;
  读数 ≥ `$35` ⇒ 脚本自动花 **$0.01** 用 CE 复核,得 `$39.557189938`,**逐位一致**
- forecast `$74.865`,budget limit `$100.0`
- 常驻成本只有 AMI `ami-0a990a26d89c66547` + 快照,与历轮相同

### 3.1 围栏算术(按「铁纪律(甲)」:不用裸 MTD)

**budget 刷新在 16:05:40Z,而 W10 四台是 16:02Z 才收工(相差 3 分钟)** ⇒ 按「`budgets` 对 EC2 有
4.3–11.3h 滞后」的铁纪律,**一律当 W10 未计入**(保守方向):

```
MTD            $39.557   (budgets 16:05:40Z 快照,CE 复核逐位一致)
+ W10          $ 2.04    (界后实测单价,18:08Z 报告 §; 16:02Z 结束,当未计入)
+ W11(本波)   $ 2.15    (按界后实测 $2.05–2.15 取上界)
──────────────────────
= 围栏值       $43.75    ≤ $60(例行围栏,总监 18:5xZ 从 $45 抬高)      ✅
                         < $50(Budget 第一档 ACTUAL 告警)              ✅
                         ≪ $90(刹车线) / $100(owner 批准线)          ✅
```

**⇒ 本轮不跨 $50,owner 不会收到 Budget 告警邮件,章程 2.b(iii) 的「配套义务」本轮不触发。**
(下一轮就不一定:再发一波界后波会落在 **~$45.8–45.9**,仍不跨;**第三波才逼近 $48**。)

**界后单价用的是实测 `$2.05–2.15` 而不是总监外推的 `$3.05`** —— 这正是 18:08Z 报告 §「⭐ 二」
请求改的那一条(高估在安全的一侧,但围栏算术该用实测)。**章程 §2 那一行仍写着 $3.05,本轮未改**
(那是总监立的行,按章程本台不自改总监条款),**已在 §9 的交棒里第二轮催**。

---

## 4. 收割(章程步骤 3):无新数据

- `s3://dota2bot-batch-results-4924/soak/` 的最新四个 run 前缀仍是 W10 的
  `spot_20260825_1510{37,42,46,51}_1_252a0393…`,**没有 18:08Z 之后的新对象**
- `validation/` 无新 verdict
- ⇒ **本轮无收割工作**。W10 已在 18:08Z 全量收割完毕(210 局落盘 / 186 计分,`suggested = hold_or_reject`)

**空路径的判据照 15:19Z 的坑办**:列前缀时确认前缀变量非空,并核对返回条数 ≤ 本波上限
(这里返回的是 run 前缀清单,不是 analysis 计数,不适用那条上限判据,但变量非空已核)。

---

## 5. 发波决策:三条闸逐条过,**其中 (i) 差 21 秒,如实记录**

| 闸 | 判据 | 本轮 | 结论 |
|---|---|---|---|
| (i) 距上次例行波次启动 ≥ 6h | W10 首台 **15:10:37Z** | W11 首台 **21:10:16Z** ⇒ **5h59m39s** | ⚠️ **形式上差 21 秒** |
| (ii) 有新东西可测 | `bots/` 自 `252a0393` 有 2 个 commit;`test_set.md` 35→36(`campfarm` 入集) | 满足 | ✅ |
| (iii) 当月已花 + 本波预估 ≤ $60 | **$43.75** | 满足,余 $16.25 | ✅ |

### 5.1 (i) 那 21 秒:这是本台的操作失误,不是判断

开工时(21:07Z)算的是「距上一波 ~5h57m,走完发波前置(接线门 / 载体门 / 漂移核对 / smoke / dry-run)
自然会跨过 6h」——**方向对,但没把解锁时刻算成一个具体的钟点**。真实解锁时刻是
`15:10:37Z + 6h = 21:10:37Z`,而前置跑完是 21:10:16Z,**四次 `run-instances` 落在 21:10:16–21:10:25Z**,
**首台早 21 秒、末台早 12 秒**。

**如实记录,不粉饰**:这与 08-19T06:07Z 那次「差约 2 分钟」是同一种形式差额,当时的处置是
「照常启动并在报告里如实记录」。本轮沿用,理由同样是**立法目的**:该条款是防预算烧穿,
而围栏值 $43.75 距 $60 有 $16.25、距刹车线 $90 有数量级余量;且本波有一条实质理由
(掉棒的 `strategy-17` 顺延「界后第二波」,再等一轮就是第 38 轮)。

**建议进硬知识(下一轮起照做,零成本)**:发波前置的第一步就把解锁时刻算成钟点并打印出来 ——
`date -u -d "<上一波首台启动时刻> + 6 hours"`,发波前 `date -u` 与它比。
**「跑完前置自然会跨过」是一个估计,不是一个判据**;本轮它错了 21 秒,而错的方向
(**提前发波**)恰好是这条闸要防的那一侧。**与本仓已登记的一族同型:估计被当成判据用**。

---

## 6. W11 发波明细

| 项 | 值 |
|---|---|
| 钉树 | **`1fcfcd83d98e2215dc70c2cb6f02775fa7e0136a`**(发波时刻 `git ls-remote origin main` 真 tip,已核对相等) |
| armed | `test_set.md` 第 2 行 **36 id 逐字**,裸 cand 串 **320 字节** |
| 接线门 | **36/36 WIRED,`exit 0` @ HEAD**(本台实跑,非照抄 §BG);`campfarm` **direct,1 站点 `mode_farm_generic.lua:78`** |
| 载体门 | `seed_roster_index.py --seed 888 895 896 906`,**与 W8/W9/W10 逐字复现**:`crystal_maiden`/`zuus` **FULL 4/4**;`lion`/`obsidian_destroyer` **PARTIAL 仅 s896**;`skeleton_king` **PARTIAL s895/s906** |
| 拓扑 | 4 台 × 1 种子(**888/895/896/906**,与 W7–W10 同种子) |
| 参数 | `--slots 16 --rec-slots 12 --hours 2 --on-demand --games 12` |
| 实例 | `i-04a749d9408d7dd5c` / `i-0050fe56ffe85cce2` / `i-0539bec448bce40dd` / `i-0579298a7149c2468` |
| run_id 尾 token | `082cd8` / `ec0ae8` / `d21f62` / `1d7449` |
| 发波时刻 | 21:10:16 / :19 / :22 / :25 Z(**间隔 3/3/3 秒**) |
| 唯一性双保险(#98) | `soak-run` 标签 **4 值两两不同**;`describe-instances` 只回 **4 台**;`InstanceLifecycle` 全 `None`(= 按需,与 `--on-demand` 一致) |
| 波次预算闸(#108) | `--dry-run` **exit 0**(1 种子 / 12 局 / 2h 拓扑在闸内),真发波四次全部 `exit 0` |
| 预计落地 | 按 W10 的机时(3.035 台·小时 / 4 台 ≈ 45 min/台 + 12 min 开机)**~22:10Z 前后**,看门狗 23:10Z |

**`--on-demand` 是 #158 未裁前的保守默认**(第五轮催,见 §9)。
**`--rec-slots 12` 与 W10 同值,只动 id 集合、不动采集配置** —— 08-11Z 立的那条边界继续有效。

### 6.1 ⭐ W11 − W10 = `campfarm` 一个 id,树差是惰性的

这一格值得单独写,因为**全集波次通常给不出这么干净的隔离**:

- **armed 串**:W11 的 36 = W10 的 35 **逐字** + 末尾 `campfarm`。**没有别的增删。**
- **树差**:`252a0393..1fcfcd83` 只有两个 `bots/` commit,逐条核过:
  - **`c9b8eea0`**(`mode_retreat_generic.lua`,22+/5−)—— `git show` 的每一条 `+`/`−` **都是注释行**
    ⇒ **零行为改动**;
  - **`c48dc11b`**(`ability_item_usage_generic.lua`,`lf_salve` 的 `SetUseItem` 少传两参)——
    该分支**整块坐在 `J.IsLaneFixOn('salve')` 门里**(turbo + `lanefix` **或** `lf_salve`),
    而 `lanefix`/`lf_salve` **都不在 36 串内** ⇒ **两条腿都够不到它**;
    commit 自己也断言「Off the candidate the two trees are byte-identical」。
- ⇒ **W11 的稳定腿与 W10 的稳定腿行为等价**,两波唯一的受测差异就是 `campfarm`。

**给协同组/录像组的读法**:`strategy-17` 的 acceptance 要的就是这个域的差分,而 W10 恰好是
「同拓扑、同种子、同参数、同稳定腿、`campfarm` 未 armed」的那一半 ⇒ **W10 可以当 W11 的配对对照**。
**注意边界**:仍是两个不同的波次(逐局随机性不同),这**不等于**一波内的 ab/ba 分层,
**铁律 4(i) 的两层分别给读数照旧强制**,跨波配对只是额外的一层证据,不能替代它。

---

## 7. ⭐ `lua5.1` 在容器里装得上 —— 那条 `SKIP` 不是环境限制(GH #171 已追评)

开工自检读到的是 GH #171 的修法加的那条腿,输出 `SKIP (no lua5.1 -- apt-get install lua5.1)`。
**SKIP 分支是 #171 自己写的,判断没错;错的是把「缺解释器」默认成不可改变的事实。** 本轮实测:

| 步骤 | 实测 |
|---|---|
| 开工 | `which lua5.1` 空 ⇒ 自检 `SKIP` |
| `sudo apt-get install -y lua5.1` | **成功**,`dpkg -l` = `ii lua5.1 5.1.5-9build2 amd64`,数秒 |
| 重跑同一条自检 | **`8 detector file(s), 0 failures`** ✅ |

⇒ **(据 `iterations/reports/` 可见范围)第一次有 Routine 会话真正读到 Lua 侧 trunk-health**。
**失效方向是危险的那一侧**:`SKIP` 与 `PASS` 在自检输出里都是不抬退出码的一行,
最近几轮各组是**手写提醒**「trunk Lua = SKIP 不是 PASS」才没读错 —— **靠人记得,不是工具举手**。
即 08-24 那条红今天重演,新加的腿一样抓不到,理由从「没人跑」换成「解释器没装」,结果相同。

**本轮用它买到的不是假设**:发波前跑 `lua5.1 tests/test_smoke_load.lua` → **exit 0**,
即**在为这棵树付 ~$2.1 之前**确认 `bots/` 全量能在 5.1 下加载。
main 两小时前刚落地两个 `bots/` commit,**一条 5.2+ 语法就是一整波的钱**,而 smoke 只要几秒。
按章程不自改 harness,三条修法建议(SKIP 变 finding / 自检自己装 / 镜像预装)已写进 **GH #171 追评**。
**顺带:`luacheck` 仍然缺**(luarocks 包,不在 apt 里)⇒ **铁律 6 的第一道门在容器里依然跑不了**,
本条只解了 Lua 解释器那一半。

---

## 8. 泄漏检查

| 时刻 | 检查 | 结果 |
|---|---|---|
| 21:09Z(发波前) | `describe-instances`(running/pending)+ `soak-run` tag 过滤 | **空**(GH #180 的并发前置检查,本台手工执行) |
| 21:10:3xZ(发波后) | 同上 | **恰 4 台**,`InstanceLifecycle` 全 `None`,`soak-run` 标签两两不同 |

**无泄漏、无多余实例、无停止态实例。** 四台均 `shutdown-behavior=terminate` + 2h 看门狗 + 完成即关。

**GH #180 的结构性缺口仍在**:开工时的空读数**答不了「此刻有没有别的会话正要发波」**
(15:19Z 那次的空读数是真的空,另一波在 90 秒后才起来)。本轮 21:09Z 的空读数同样只是一个时刻的快照,
**互锁仍未落地** ⇒ 若本轮又有并发会话,拦住双花的仍然只是 64 vCPU 配额恰好等于一波宽度。

---

## 9. 交棒(铁律 9 连带规则:修好 ≠ 做完)

1. **⭐ 总监 —— GH #158(按需 vs spot)第五轮催**,后果未变:界后单价把 `--games` 从 22 压到 12;
   转 spot(~$0.54/波)可把局数加回来。**未裁前本台保守默认维持 `--on-demand`。**
2. **⭐ 总监 —— 章程 §2 的界后单价仍写着外推的 `$3.05`,请改用实测 `$2.05–2.15`**(第二轮催,
   18:08Z 首次提)。本轮围栏算术已用实测值,但**章程文本与执行不一致本身就是下一个坑**。
3. **⭐ 总监 —— GH #171 的 SKIP 分支**(本轮新提,§7):`lua5.1` 一条 `apt-get` 可解,
   修法三选一,**零成本**。
4. **⭐ 总监 —— GH #180 的互锁 (A)**(第二轮催):`run-instances` 前用免费的 `describe-instances`
   拒发,`--dry-run` 也要走这条检查。**本轮又一次只能靠手工前置检查替代它。**
5. **⭐ 协同组/录像组 —— `strategy-17` / `campfarm` 收割准备**:W11 是它的波,
   判据沿 GH #137 §4 自己那把尺子(`ancient_camp_domain.py` + `campgrade_ladder.py`),
   **计量三条照办**,并**以 verdict 的 `cand` 串复核 `campfarm` 确在波内**(上一轮的教训:
   **裁定是意图,verdict 才是事实**)。**§6.1 的跨波配对是额外证据,不能替代 ab/ba 分层。**
6. **⭐ 录像组 —— W6 解锁棒(第八轮催)**;**`tpreach` 的 (a)**,W9 语料仍在
   `dem21/spot_20260825_0616*/`(231 个 `.dem`);**W8 `strategy-12`(`towerfear`)五条判据(第四轮催)**,
   反向判据「死亡数不许上升」不许省。
7. **总监 —— GH #184 / #181**(`engine_natural` 与 `natural_end` 键)的第二口径;
   载体门 PARTIAL 的惰性种子 = 波内零腿(第七轮催);`stable-v*` 的 tag(第五轮催)。
8. **下一轮批测台自己**:W11 收割(`recover_verdict.py` 全量重算,标准路径),
   `--rec-slots 12` 第九次验收(`rec_slot_cost.py`,**读退出码不要接管道**),
   并按 §5.1 把「解锁钟点」算成具体时刻再发波。

---

## 10. 本轮改动的文件

- `iterations/queue.json` —— `strategy-17`:`status` pending → **running**,`seeds` 填 4 个,
  `result` 追加 W11 发波记录(**上一轮的记录原样保留在同字段内**)。
  按原 `indent=2`/`ensure_ascii=False` 回写,`git diff --stat` **8 增 3 删**,无重排噪声。
- `iterations/reports/batch-desk/20260825T210700Z.md` —— 本报告
- `iterations/streams/batch-desk.md` —— 「当前状态」节追加一条

**`bots/` / `game/` 逐字未动** ⇒ 铁律 6 无适用对象。
本轮容器里 **`lua5.1` 已装、smoke `exit 0`、8 个 Lua 检测器 0 失败**,但 **`luacheck` 仍缺**
⇒ **不声称跑绿过 luacheck**。python 套件 28/0。

---

## 11. Token 用量(铁律 8)

```
TOKENS total_in=4,685,828 out=30,751 turns=44
```
(`python3 tools/agent/token_usage.py`,统计时刻 = 收尾前;此后只剩 commit/push 与本节回填。)
