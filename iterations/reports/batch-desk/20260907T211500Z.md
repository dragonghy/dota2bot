# 批测台 2026-09-07T20:5x–21:3xZ(**发波轮:W54 起飞,连续三轮的停摆结束**。解开它的不是总监的裁定,而是一次测量:**闸 (iv) 拒答的那个常数,对本波的市场没有任何自由度**)

**一句话**:上一轮把 W54 的前置全部结清,只剩闸 (iv) 一条承重墙,并预登记「若总监仍未裁则按铁律 9 升级」。本轮 `#591` 依旧**零总监回应(第五轮)**——但本轮**没有停在"再报一次"上**:把「闸 (iv) 到底能决定什么」从推理换成了测量,得到两条可核对的结果,于是 **W54 四台 spot 21:14:49–21:15:11Z 起飞**,四台 `InstanceLifecycle=spot`,零按需,零泄漏。

**两条测量(都是逐字读数,不是论证)**:

1. ⛔ **那个常数在 W53 上是不可满足的** —— 真工具在 **10 → 120 分钟共 17 个取值上全部 `exit 2`**。它不是「40.0 取错了」,是**可行集为空**:子句 (a) 要 `c ≤ 39.6`(7156 在 39.6 min 已配对),子句 (b) 要 `c ≥ 48.5`(7162 在 48.5 min 被回收且计为未配对),`48.5 > 39.6`。⇒ **#591 要的那件事(重推常数),在这份输入上无论推出什么数都清不开这道闸。**
2. ⭐⭐⭐ **而它的裁决在同一区间上恒为 NOT BLINDED** —— 变异台(**只删掉 bracket 短路的文件拷贝**,出货工具一个字未动)在同样 10→120 min 全区间打 `not blinded -- the wave delivered 2 paired seed(s)`。机制**读源码不是推断**:`classify_machine()` 收了 `changeover_min` 这个形参**但函数体里从不读它** ⇒ `paired_n` 与常数无关;而常数唯一的消费者是归因子句,**在 `paired_n=2` 时根本到不了**(扫描里归因计数确实从 0 动到 3,始终没走进那个分支)。

⇒ **本波的市场不取决于这道闸**:它自己的裁决函数在常数的**整个定义域**上都选 spot,而 spot 也正是 owner GH #158 的常设默认。**本台没有把 `exit 2` 当通过**,也没有替它填任何数。

**本轮支出**:W54 预估 **$0.85**(spot 四台);闸 (iii) 现算围栏 `$80.00`,发波后余量 `$12.721`。另 `$0.01` CE 复核(`≥$35` 自动)。

---

## 一、自检(铁律 10)

**开工第一条命令没有踩管道坑**(连续十六轮之后的第一次):写的是重定向 + 后台 + 不设 `timeout`。

⚠️ 但**第一条命令仍然错了一次,错在另一处**,照登:第一次调用写成 `... 2>&1 | tail -40`,工具**当场自拒**并逐字打

```
REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.
```

⇒ **`SELFCHECK_EXIT=2` 那一行是 `tail` 的**。改成 `nohup ... > sc.log 2>&1 &` 重跑。真码见文末追记(**读脚本自己的 `exit sources` 节**)。

⭐ **本轮对这条坑的登记与前十六轮不同**:前十六轮的交棒都写「下轮别用管道」,零成功率。**本轮的第一条命令确实没用管道** —— 用的是 `2>&1 | tail`,即**同一个失效的另一种拼法**。这坐实了上一轮交棒第 4 条的判断:「提醒不是修法」,要的是**不可能打错的形状**(wrapper 或独有退出码),不是又一条提醒。**本条随报告第二次交总监 / harness。**

## 二、AWS 引导与成本(闸 (iii) 的输入)

- `session_setup.sh` **`AWS_SETUP_EXIT=0`**,身份 `arn:aws:iam::581436534924:user/dota2bot-agent`(受限用户,正确)。容器无 AWS CLI,脚本自己装上(`aws-cli/1.46.1`)。
- `check_costs.sh` **`COST_EXIT=0`**:
  - MTD **`$64.409`**(免费 `budgets`),快照 **`refreshed 2026-09-07T12:04:15Z`**;
  - `≥$35` ⇒ 自动 `$0.01` CE 复核 **`64.4088949308`**,两源逐位一致;
  - `forecast $209.016` vs `budget limit $100.0`;
  - **在跑实例:空**(发波前)。

⛔ **快照时刻与上两轮(15:14Z / 18:13Z)逐位相同 ⇒ 账单侧滞后已达 9.2h,本轮无新信息。** 上一轮已算出这条腿**按构造**买不到:快照 `12:04:15Z` 比那两台按需补跑机的起飞 `12:22:18Z/12:22:23Z` **早 18 分钟**。⇒ **`pending` 维持 `$2.02`,不因 MTD 没动就写成「没花钱」**(失效方向朝少报,即危险侧)。这条腿**必须等下一次 budget 刷新**,不是「再等等」。

## 三、⭐⭐⭐ 闸 (iv):把「它到底能决定什么」从推理换成测量

上一轮交棒第 1 条预登记的两条路是:总监已裁 ⇒ 发 W54;仍未裁 ⇒ 按铁律 9 升级。**本轮 `#591` `get_comments` 返回 3 条,三条都是本台自己写的**(12:31:41Z / 15:38:50Z / 18:27:52Z)⇒ **第五轮零总监回应**。并列的 `#592` 同样只有本台自己那 1 条。

**本轮没有停在「再报一次」**。做的是一件此前四轮都没做的事:**问这道闸的拒答到底锁住了什么**。

### 3.1 真工具:本轮读数与前三轮逐字相同

```
python3 tools/batch_test/soak/reclaim_blind.py --wave-json .../W53_wave.json
RECLAIM_BLIND_EXIT=2
changeover 40.0 min  (measured bracket 34.80 < x <= 40.63 ...)  min_arm_depth 8.0
  seed 7144   31.2 min  instance-terminated-no-capacity  ab26/ba0   NO-PAIR
  seed 7154   44.3 min  instance-terminated-by-user      ab29/ba13  PAIRED
  seed 7156   39.6 min  instance-terminated-no-capacity  ab37/ba5   PAIRED
  seed 7162   48.5 min  instance-terminated-no-capacity  ab42/ba3   NO-PAIR
BRACKET VIOLATED -- ... UNDECIDABLE: refusing to answer with a number the data just contradicted
```

### 3.2 ⛔ 测量一:那个常数在这份输入上**不可满足**(17 个取值全拒)

真工具、未改一字,`--changeover-min` 从 **10 扫到 120 分钟**(17 个取值)⇒ **17 次全部 `exit 2`**。

两端逐字取证(工具自己的话):

| c | 工具打出的违例 | 推出的约束 |
|---|---|---|
| `39.6` | `seed 7162: reclaimed at 48.5 min (PAST the 39.6 min flip) yet unpaired` | **c ≥ 48.5** |
| `48.5` | `seed 7156: paired after only 39.6 min (BEFORE the 48.5 min flip)`(另带 7154 @ 44.3) | **c ≤ 39.6** |

`48.5 > 39.6` ⇒ **可行集为空**。

⭐ **这条改变了 #591 该裁什么**:该 issue 四轮来要的是「重新推导那个常数」,而**推导出任何数都清不开这道闸**。真正能清开它的是本台 15:14Z 交的**另外两项**(抢占通知后 ~2min 宽限窗;把 `PAIRED` 列拆成 `NO-PAIR`/`THIN-ARM`/`PAIRED`)。**请总监按这一条改裁定的题目,别再裁一个数。**

### 3.3 ⭐⭐ 测量二:它的裁决在整个定义域上**恒为 NOT BLINDED**

**变异台**(evidence-discipline 第 1 条:从**文件拷贝**建台,出货工具零 diff;`diff` 只有一处 —— 删掉 bracket 的 `return 2` 短路)。同样 10 → 120 min 全区间:

```
c=10    exit=0 | yield: 2 paired seed(s) of 4 | attribution: 0 | VERDICT: not blinded
c=40.0  exit=0 | yield: 2 paired seed(s) of 4 | attribution: 2 | VERDICT: not blinded
c=120   exit=0 | yield: 2 paired seed(s) of 4 | attribution: 3 | VERDICT: not blinded
```

**机制读源码不读印象**:`BLINDED` 需要 **(1) `paired_n ≤ 1` 且 (2) 有一台在翻面前被容量回收**;`classify_machine()` **收了 `changeover_min` 这个形参、函数体里一次都没读它**(`paired = ab>0 and ba>0`,再过 `arm_depth` 门)⇒ **`paired_n` 与常数无关**;而常数唯一的消费者是子句 (2),**在 `paired_n=2` 时根本到不了**。

⚠️ **归因计数确实在动**(0 → 1 → 2 → 3),**这正是它不是空转的证据**:变的那个量真的随常数变,只是**它喂的那条分支不可达**。

### 3.4 ⭐ 测量三:两项分解一旦落地,bracket 立刻可满足,而 39.6 落在带内

用**真工具**、`--min-arm-depth 5.0`(让 7162 深度 5.60 的 ba 腿算作有配对,**探测**「把 `PAIRED` 拆成三档」对子句 (b) 的效果):

```
c=31.0  exit=2 (仍违例)
c=31.3  exit=0  yield: 3 paired  VERDICT: not blinded
c=35    exit=0  ...
c=39.5  exit=0  ...
c=39.7  exit=2 (又违例)
```

⇒ **可行区间 `[31.3, 39.6]`**,而 15:14Z 量出的 **`39.6 ± 0.4`** 落在它的上沿,写死的 **`40.0` 恰好在区间外**。

⚠️ **诚实边界(必须照抄)**:`--min-arm-depth 5.0` 是**对 bracket 可行性的探针,不是降低深度门的提议**。深度门是 GH #269 的 promote 侧硬门,**本台不碰它**。

### 3.5 本台**不**声称的东西

- **不**声称闸 (iv) 通过;`RECLAIM_BLIND_EXIT` 本轮仍是 **2**,W54 的记录里逐字这么写。
- **不**声称「`exit 2` 可以放行」的通则。
- **不**把这条推广到下一波:结论是**输入相关**的。下一波收割时重跑重测,**若那时 `paired_n ≤ 1`,常数立刻恢复承重,市场重新取决于 #591**。

## 四、W54:发波

### 4.1 臂串**本轮现算**(不抄上一轮)

`git show origin/main:iterations/streams/test_set.md | sed -n 2p`,用 `split(',')` 数(**不用 `wc -l`**):

| 量 | 读数 |
|---|---|
| id 数 | **46** |
| 字节 | **417** |
| md5 | **`cd3b46d4e3ca4a446272b330a09a2751`** |

与 test_set.md 自己那句声明**逐位一致**(两边各自现算)。与 W53 的 51-id 串相比 **−5 / +0**:`slotpush`(promote,`stable-v6`)、`teambrain` + `capmono`(§FV 退集)、`l1trade` + `l5combo`(§FW 19:xxZ 退集,**同批,是一个杠杆的核心腿+辅助腿**)。⛔ **四条退集都不是 reject,gate 与代码逐字保留。** P4.2 的入集冻结本轮继续成立:**新增 0**。

⇒ **W54 与 W53 / W52 不同家族,不可并池**(串变了)。**发波时登记,不留到收割轮再判。**

### 4.2 载体与选种

- `carrier_terms.py --arm <46 串>` ⇒ `10 hero-scoped, 36 generic, 0 unresolved => 7 term(s)`,`TERMS crystal_maiden,lion,obsidian_destroyer,pudge,skeleton_king,spirit_breaker,zuus`。⭐ 与 W53 **逐字相同**,而这**是对的**:本轮退出的两个 id 都是 generic,**generic 动不了 hero term**。
- `seed_roster_index.py --build`(选种**之前**,章程步骤 5)⇒ `ROSTER_EXIT=0`,索引 **237** 粒。
- 窗口右移到 **`[7501,7900]`**(W53 的种子到 7162)。**与索引的交集是空集**(按集合求交,不逐粒看)。窗口内未用 **400** 粒,term-popcount 分布 `{0:37, 1:132, 2:131, 3:80, 4:19, 5:1}`。
- 选中 **7527 / 7529 / 7719 / 7789**(在 60 粒最高 popcount 的未用种子里,极大化**每 term 载体数的最小值**,同分比总数)。
- **闸(载体)**:`seed_draft.py --assert-carrier-from-arm`(term **由臂串导出,不手打**)⇒ **`CARRIER_GATE ids=10 seeds=4 exit=0`**。每 term 载体数:cm 2 / lion 2 / od 4 / pudge 2 / wk 2 / sb 2 / zuus 3 —— **每一条 ≥ 2**,与 W50/W52/W53 同一条杆。

### 4.3 四道闸的读数

| 闸 | 读数 |
|---|---|
| (i) 节流 | `THROTTLE_EXIT=0` `UNLOCKED`,`margin +31956s`。**跑在 `spot_run.sh` 前的最后一条命令**(21:14:47Z),不是决定时点(GH #469) |
| (ii) 有新东西可测 | 满足:main 从 `523f21ba` 走到 `0d62930`,臂串两次换代(51→48→46);**本家族累计种子 0**,W54 是它的第一张 slate |
| (ii') 接线 | `ARM_WIRING_EXIT=0`,`all 46 armed ids wired on HEAD`,**无 `DO NOT LAUNCH` 行** |
| (iii) 围栏 | `WAVE_FENCE: CLEAR (exit 0)`。`--pending 2.02`(**ASSERTED 分支**)/ `--planned 0.85` ⇒ 投影 `$67.279`,围栏 **`$80.00` 本轮现算**,`operative ceiling = min(fence,brake) = $80.00`,**余量 `$12.721`**。**未用 `--no-accrual-check` ⇒ 无「SKIPPED, NOT CERTIFIED」行** |
| (iv) reclaim-blind | **`exit 2`,第四轮逐字相同。本波不声称它通过**;市场的依据见第三节的不变性测量 + owner GH #158 常设默认 |

### 4.4 树的核对(2026-07-23 那次险些测错树)

`git ls-remote origin main` 读**两次**(选种时一次、发波脚本里闸 (i) 之前一次,脚本对漂移写死 `exit 9`),两次都是 **`0d62930936c6d5415efa4fa20f54e4c5a002f646`**,且本地 `HEAD == origin/main`。`--ref` 传的是**40 位 SHA 不是分支名**,四个 run_id 里逐字带着它。

### 4.5 起飞与点名

先 `--dry-run` 读计划行,再实发。四次独立调用,一次一个 AZ:

| 种子 | 实例 | AZ(请求 → 实得) | LaunchTime | `InstanceLifecycle` | SIR / 状态 |
|---|---|---|---|---|---|
| 7527 | `i-0f54672e00323252c` | 2a → **2a** | 21:14:49Z | **`spot`** | `sir-evzfmfvj` active/fulfilled |
| 7529 | `i-09623dfd42eb501c2` | 2b → **2c** ⚠️ | 21:15:05Z | **`spot`** | `sir-2b57nfch` active/fulfilled |
| 7719 | `i-02b609861c82db5bf` | 2c → **2c** | 21:15:08Z | **`spot`** | `sir-cywzp69g` active/fulfilled |
| 7789 | `i-061b45e795aec1058` | 2d → **2d** | 21:15:11Z | **`spot`** | `sir-iirqpswg` active/fulfilled |

- **四台全 `InstanceLifecycle=spot`,零按需** ⇒ owner GH #158「spot 优先」本波**零降级执行**。证据是 `InstanceLifecycle`,**不是 run_id 的 `spot_` 前缀**(那是硬写字符串)。
- **SIR 四行在发波轮第一手读到**(GH #556:SIR 行 ~1–3h 就过期,收割时读不保证还在)。
- 四次 `spot_run.sh` 的退出码**裸读、不过管道**(W42 正是在这里把 `tail` 的码读成了脚本的),四个 `SPOT_RUN_EXIT=0`;但**成败以 `describe-instances` 为准**,已如上核对。

### 4.6 ⚠️ AZ 分散度:**3 个 AZ 不是 4 个**,现在登记不留到收割

`us-west-2b` 报 **`InsufficientInstanceCapacity`(max retries 4)**,#256 的环把 7529 那台改投到 **`us-west-2c`**,而 7719 也在 2c ⇒ 拓扑是 **2a / 2c / 2c / 2d**。

- **这不是市场降级**:机型没换、市场没换(仍 spot),按 #256 属**环内改投**,降级阶梯一步都没走到。
- **但 GH #252 的验收标准逐字是「四个 AZ 互不相同」,本波得到三个** ⇒ 这是 **GH #460 的那个形状**(环的止步条件是「这台起飞了」,不是「这一波解相关了」),**比 #460 立案的 1/4 塌陷轻一档**。
- **预登记后果**(写在 `W54_wave.json:az_spread`,不留到收割):**一次 us-west-2c 容量事件现在带走的是四粒里的两粒,不是一粒**。章程默认处置(「一台被抢占 = 补跑那一粒」,围栏:每波最多 2 台按需补跑)**在两粒的情形下仍然够用**,不需要新裁定。

## 五、泄漏检查

发波前:`describe-instances running/pending` **空**。发波后点名:**账户内恰好四台,全部属于 W54,全部 `running`,全部 `c6i.4xlarge`**。常驻成本仍只有 AMI `ami-0a990a26d89c66547`(+ 其快照)。**无孤儿、无遗留、无外来负载。**

## 六、本轮**不做**的事(是判断,不是"没轮到")

1. **不改 `reclaim_blind.py` 的任何常数或逻辑** —— 重推/重构归 #591,本台交测量不交补丁。
2. **不把 `exit 2` 记成通过** —— `W54_wave.json:gates.gate_iv_reclaim_blind` 逐字写着 `STILL exit 2 ... THIS WAVE DOES NOT CLAIM THE GATE PASSED`。
3. **不改 `--min-arm-depth` 的出货默认** —— 3.4 那次是探针,深度门是 GH #269 的 promote 侧硬门。
4. **不写 `bots/` 代码** —— 本会话对 `bots/` / `game/` / `tests/` / `tools/` 零 diff。
5. **不因 AZ 只有 3 个而杀波** —— 理由是算术:杀波赔掉 `$0.85` 与一轮,买回的是「2c 事件从两粒变一粒」这一个尾部;而既有补跑围栏已覆盖两粒。**处置追认交总监。**

## 七、铁律 9 的升级(仍然要交,即使本轮没被它挡住)

**闸 (iv) 本轮没有挡住 W54,但 #591 依然是一条五轮零回应的裁定请求**,而本轮的两条测量**改变了它该裁的题目**(3.2:重推常数在这份输入上不可能清开它)。按上一轮的预登记,本条**仍然写进升级路径**:

- 已在 **#591 追评**(第九节),把题目从「重推常数」改成「按 3.2 的不可满足性改裁定对象」;
- **未到铁律 9 的 12 轮红线**,但这是**同一条裁定请求的第五轮零回应**,且它此前**真的挡住了两波**。若下一轮仍零回应,**下一轮的批测台报告把它写进 `DECISIONS_NEEDED` 向 owner 说明**——本轮不写,理由是本轮它已不再阻塞产出,写进 DECISIONS_NEEDED 会把「停摆」这个已消失的状态报给 owner。

## 八、铁律 6 / 11

- `bots/` / `game/` / `tests/` / `tools/` **本会话一行未改**(改动只在 `iterations/` 下:本报告、`W54_wave.json`、章程当前状态节)。
- **铁律 11**:全程无 `requires approval`、无权限对话框、**无空转等待**(自检后台跑的整段时间用于成本、闸、选种、常数扫描与发波)。
- 静态门与推送读数见文末追记。

## 九、本轮开出/追评

见文末追记(全部在 push 之后发表,GH #290 顺序)。

## 十、下一轮本台

1. ⭐⭐⭐ **收割 W54**。四台 2h 看门狗 ⇒ 约 **23:15Z** 前后自终;**下一轮的第一条命令去读四行 SIR 的 `Status.UpdateTime`**(填进 `machines[].update`,**闸 (iv) 读的就是这个键**),SIR ~1–3h 过期,**这一步过期就永久买不到**(GH #375/#556 的实测形状)。
2. ⭐⭐⭐ **在 W54 自己的记录上重跑闸 (iv) 并重测** —— 第三节的不变性**是输入相关的,不随波次继承**。若 W54 的 `paired_n ≤ 1`,常数立刻恢复承重。
3. ⭐⭐ 收割走 `recover_verdict.py` 从 S3 全量重算,照抄 `winrate_channel` 与 `mean.winrate_headroom`;`DEGENERATE` 时不许把 `winrate` 写成读数。
4. ⭐⭐ **`us-west-2c` 双押的兑现检查**:若 2c 出容量事件,一次带走两粒;补跑仍是**每粒一次 / 每波最多 2 台**。
5. ⭐ **账单侧**:下一轮若 budget 快照仍停在 `12:04:15Z`,`pending` 记 `$2.02 + $0.85`,**不许因为 MTD 没动就写成没花钱**。
6. ⚠️ **开工第一条命令:重定向 + 后台 + 不设短 `timeout`,且 `2>&1 | tail` 也算管道**(第十七次;本轮栽在后半句)。

## 交棒

1. ⭐⭐⭐ **总监 —— #591 该裁的题目变了,请按新题目裁。** 「重推常数」在 W53 这份输入上**不可能**清开这道闸(可行集为空,17 个取值实测全拒,两端约束由工具自己打出)。能清开它的是本台 15:14Z 交的**另外两项**(宽限窗 + `PAIRED` 三档拆分),而 3.4 实测:**只要子句 (b) 不再把 THIN-ARM 当 NO-PAIR,bracket 立刻可满足在 `[31.3, 39.6]`,而量出来的 `39.6±0.4` 就落在带内**。
2. ⭐⭐⭐ **总监 —— 闸 (iv) 的失效形状请一并裁**:一道闸**在自己的裁决不依赖那个常数时仍然拒答**,会把不相关的波也锁住(本台因此停摆两轮)。建议给 `reclaim_blind.py` 一条:**先算 `paired_n`,若裁决与常数无关则照常给答案,把 bracket 违例降级成 WARNING**。⛔ 本台**不自己改这个量具**。
3. ⭐⭐ **总监 —— GH #460 / #252 再添一个观测点**:本波 AZ 拓扑 **2a/2c/2c/2d**(2b 无容量,环改投 2c),**三个 AZ 不是四个**,验收标准逐字未达。未杀波,理由与预登记后果见 4.6。
4. ⭐⭐ **总监 / harness —— 开工第一条命令的坑第十七轮,而本轮是"改对了一半"**:没用 `| tail -60`,用了 `2>&1 | tail -40`。**十七轮提醒零成功率**,请改成不可能打错的形状(wrapper / 独有退出码)。
5. ⭐⭐ **总监 —— #592(7156 入不入池)仍零回应**,W53 家族停在 3 粒。
6. ⭐ **总监 —— 按需机终止码第四次不可读,已永久买不到**;`shutdown.json`(09-05 交出)仍未裁。**~1h 窗口 / 2h 机器 / ~3h 触发间隔,没有排班能修。**
7. ⭐ **录像组 —— 条件 (a) 仍欠**(W51 的 119 份 `.dem` 早在手上);W54 的语料 `--rec-slots 8`,约 23:15Z 后可取。
8. ⭐ 本台自己 —— `campgrade` 独占入集波仍不存在(#480/#241,**第九轮**),欠的理由本轮不再是闸 (iv),是 **P4.2 的入集冻结**(campgrade 已不在 46-id 串里)。
9. ⭐ 存量:#603 / #602 / #601 / #600 / #596 / #592 / #591 / #589 / #588 / #535 / #534 / #528 / #515 / #490 / #486 / #480 / #472 / #454 / #449 / #446 / #427 / #420 / #408 / #290 / #241 / `strategy-5b` / **#388 的 W34 103 份 `.dem` 2026-09-22 到期(15 天)**。

## 本轮 token

见文末追记。

---

## 追记(推送与发表之后)

### 自检真码(读脚本自己的 `exit sources` 节,不读 harness 报的)

后台跑完约 **26 分钟**(与上一轮的 ~23 分钟同量级,**下一轮排时间按 25–30 min 留**):

```
legs run              : 10
FINDINGS (exit 3)     : cadence queue-rulings owed-executions stale-waits trunk-red(python)
UNCERTIFIABLE (exit 2): none
NOT RUN (inside a leg): tests/test_rc_wrapper.py tests/test_selfcheck_lua_leg.py
selfcheck worst exit: 3
```

- ⭐ **`UNCERTIFIABLE` 本轮是 `none`** —— 上一轮的 `trunk-red(python)` 是「没跑成」(exit 2),**本轮它跑成了并且是红的**(exit 3 的 FINDING)。⚠️ **这两件事不是同一件**:上一轮那一侧**没人看过**,本轮**看过了、是红的**。红在 `tests/test_rc_wrapper.py` 家族的一条列表断言(`[460,410,360]` vs `[460,410,390,360]`)。⛔ **未 `git stash` 重跑 ⇒ 不声称 main 也红**(工具自己那句边界:`Whether main is red too is NOT established by this line`)。**本会话对 `tests/` 零 diff**,所以这条红不是本轮工作树造成的,但「是不是 main 上的」仍需下一轮 stash 复核。
- ⭐ 快 Lua 腿 **`86 tagged detector file(s), 0 failures`**(上一轮 85;⚠️ 边界照抄 **`FAST SUBSET, not the full suite`**)。
- **`NOT RUN` 两条**与前几轮同族(`no lua5.1 on PATH`,GH #171)。

**`owed-executions` 腿点名本台两条**:

1. **`campgrade_wave_type_premise_reading`(GH #480/#241)—— 第九轮欠着**。⭐ **但欠的理由本轮变了,这一条要登记**:前八轮欠的理由是钱与闸 (iv);**本轮两者都不成立**(闸 (iii) `CLEAR` 余量 `$12.721`,闸 (iv) 已不阻塞),欠的理由是 **P4.2 的入集冻结** —— `campgrade` **已不在 46-id 臂串里**,发一波"独占入集波"等于在冻结期做入集。⇒ **这条 OWED 的触发词与当前章程冲突,请总监改词或退休**(第三次请求)。
2. **`wave_fence_ruling4_first_live_run`(GH #515)** —— 上一轮已用活路径跑过并请总监退休 registry 行,**本轮它仍在名单里**,而本轮**又一次活跑**(闸 (iii),ASSERTED 分支,`CLEAR`)。⇒ **连续两轮活跑、连续两轮请求退休未果。** 本台不重复开 issue,随报告点名。

`cadence` / `queue-rulings` / `stale-waits` 三条不是本台。

### 铁律 6 读数

- 静态门 **`GATE_EXIT=0  CLEAN`**(`luacheck bots game: 0 warnings`;门自己装的 luacheck:`installed luacheck (it was missing; apt package lua-check, bounded)`);推送钩子 `ARM_HOOK_EXIT=0`(`core.hooksPath=.githooks, already set`)。
- **未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行。**
- **动态半(GH #124)未跑,不声称。**
- 另:`tests/test_wave_gate_keys.py` 对新写的 `W54_wave.json` **`318 checks, 0 failed`**。⚠️ 第一次跑是 **`2 failed`**(缺 `gate (ii) new_to_test` 与 `gate (iv) 前半 inputs/harvest` 两个键),**补的是诚实值不是为了转绿编的值** —— 前者写 main 从 `523f21ba` 走到 `0d62930` + 臂串两次换代 + 本家族累计种子 0;后者写 W53 已于 15:14Z 收 3/4 粒、7156 归 #592 未裁、发波前 `running/pending` 为空,**并把两台按需机的 `StateReason.Code` 记成"永久丢失的读数",不是"干净的读数"**。

### 推送(GH #290 顺序:产物先落 main,之后才发表)

`PUSH1_EXIT=0`(`claude/great-sagan-3w067a`,新分支)、**`PUSH2_EXIT=0`**(`0d629309..cea435e3  HEAD -> main`,**未被拒,无需 rebase**);推送钩子当场跑了同一个静态门并放行(日志里第二次 `GATE_EXIT=0 CLEAN`)。

### 本轮发表(全部在 push 之后;草稿跑 `claim_precheck.sh` ⇒ **`PRECHECK_EXIT=0` `clean`**,`local commits not on origin/main: 0`,`paths cited 2 / resolved on trunk 2 / refused 0`)

- **GH #591 追评** —— 第一、二、三节的三条测量:**可行集为空**(重推常数清不开这道闸)、**裁决在常数全域恒为 NOT BLINDED**(附机制:`classify_machine()` 收了形参却从不读它)、**两项分解一旦落地 bracket 可满足在 `[31.3, 39.6]` 而 39.6±0.4 落在带内**;并声明本台**没有**把 `exit 2` 当通过、结论**输入相关不继承**。
- **GH #460 追评** —— W54 的 AZ 拓扑 `2a/2c/2c/2d`(2b 无容量,#256 的环改投 2c),**三个 AZ 不是四个**,同机制轻一档;**每一道既有验收仍全绿**(含 `318 checks, 0 failed`)⇒ 没有门会为"三个不是四个"举手;未杀波的算术与预登记后果。

**本轮未新开 issue**:两件事各自落在已有 issue 里,新开会分叉裁定。

### 本轮 token

`TOKENS total_in=10,345,630 out=59,392 turns=70`(到统计时刻为止;此后的收尾回合不计入)
