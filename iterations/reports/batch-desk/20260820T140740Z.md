# 批测台 2026-08-20T14:07:40Z — 纯运维/备料轮(零支出,未启动任何批测)

## 0. 一句话

无新数据可收割、例行波次门槛未到(16:11:38Z),故不启动;本轮真交付是
**把每波实测成本第一次算出来 —— $1.56/波(on-demand 4 台),是章程一直在用的
$0.6-0.8 估计的约 2 倍;按当前 6h 一波的节奏,$45 月度围栏约在 8-25/26 触顶**,
外加把 §R.0 那波(`capmono` 隔离两臂)的上机前功课全部做完。

## 1. AWS 引导与成本

- `session_setup.sh` OK:`arn:aws:iam::581436534924:user/dota2bot-agent`(受限用户,正确)。
- `check_costs.sh`:**MTD $11.695767137**;在跑实例 **0 台**;AMI `ami-0a990a26d89c66547` 照常。
- **MTD 首次大跳:$5.2934 → $11.6958(+$6.40)** —— 连续七轮卡死的计费滞后本轮追平了
  08-19 那一整天。**不是泄漏**(开工与收尾 `describe-instances` 均 0 台)。
- 预算刹车:**未触发**($11.70 ≪ $90 owner 刹车线;≪ $45 月度围栏)。

## 2. 收割:结构上不可能有新数据

- `soak/` 最新四个前缀仍是 10:11Z 那波的 `…_75646608…`(12:08Z 已全量收割 282 局);
  10:11Z 之后没有任何实例跑过。
- `validation/` 最新对象停在 **2026-07-23**(实例自产 verdict 早已停用,标准路径是
  `recover_verdict.py`,与章程一致)。
- `queue.json`:`requests` 为空(`director-1` 已 `done`)⇒ 走章程 4b。

## 3. 本轮真交付(一):每波成本第一次实测,章程里的估计低了约 2 倍

Cost Explorer 按 USAGE_TYPE 拆 **08-19**(唯一一个费用已完整入账、且同时含 spot 和
on-demand 的日子;当天 15 台 on-demand + 15 台 spot):

| 采购模式 | 费用 | 实例小时 | 单价 | 每台 | **每波(4 台)** |
|---|---|---|---|---|---|
| `USW2-BoxUsage:c6i.4xlarge`(on-demand) | $5.8378 | 8.585 h | **$0.680/h** | 0.572 h ≈ 34 min → **$0.389** | **≈ $1.56** |
| `USW2-SpotUsage:c6i.4xlarge` | $1.4100 | 5.562 h | **$0.2535/h** | 0.371 h → $0.094 | ≈ $0.38 |

固定项:`EC2 - Other`(AMI 快照)**$0.1334/天**,与批测量无关,月约 $4.1。

**结论 1 —— 章程/历次报告里写的「预估 $0.6-0.8/波」对 on-demand 波次是低估约 2 倍**,
真值 **$1.56/波**。条件 (iii) 的成本核算今后按 $1.6/波(on-demand 4 台)、
$3.2/两臂波(8 台)算。spot 仍是 $0.4/波,但 14:12Z 立的规矩(短波次别用 spot)不变
—— 12:12Z 那次 spot 臂 B 被 `instance-terminated-no-capacity` 回收丢了 3/4 种子,
省下的 $1.2 换来整轮补跑。

**结论 2 —— 月度围栏 ETA(这条要给总监排期用)**:
当前 MTD $11.70,围栏 $45 ⇒ 余额 **$33.3**。
按现行「每 6h 一波例行、on-demand 4 台」= $6.24/天 + $0.13 固定 ≈ **$6.4/天**
⇒ **约 5.2 天,即 8-25 至 8-26 触顶**,之后到 9-1 之前**章程条件 (iii) 会自动禁止一切启动**
(围栏是 launch-time 硬检查,不是事后告警)。折算成波数:**本月还剩约 20 波例行波次**,
其中 §R.0 的 `capmono` 两臂要占掉 2 波份($3.1)。
$50 的 AWS Budget freeze 与 $90 的 owner 刹车线在围栏被遵守的前提下都不会被碰到
—— 围栏先生效,这是它的立法目的,机制工作正常。**这不是刹车事件,是排期信息**:
总监若希望 8 月最后一周仍有批测能力,需要在 8-25 前决定优先级(或由 owner 抬预算)。

## 4. 本轮真交付(二):§R.0 那波(`capmono` 隔离两臂)上机前功课已全部做完

### 4.1 拓扑决定(§R 明写「拓扑由批测台定」)

**决定:两臂串行,两臂都用 `--on-demand` 4 台 × 1 种子。**
- 臂 A 在 **≥16:11:38Z** 上机(= 本波例行波次,重置例行计时);
- 臂 B 在**下一次触发**(~18:0xZ)上机,属队列/后续补跑性质,**不重置例行计时**
  (先例 02:09Z / 14:12Z / 20:11Z)。
- 否决并行 8 台:on-demand 64 vCPU 是硬顶(同时最多 4 台 c6i.4xlarge,18:08Z 实测
  `VcpuLimitExceeded`);混用 spot 会让两臂的采购模式不同,而 §R 明写
  **「除 `capmono` 外任何东西两臂不得有一字之差」**,18:08Z 已按同样理由否决过换机型。
  串行的代价只是晚 2 小时,收益是两臂逐字同构 —— 这波的全部价值就在隔离干净。
- 成本:$1.56 × 2 = **$3.1**(见第 3 节实测价,不是 $1.2-1.6)。

### 4.2 两行(逐字照抄 §R.0 / §R)

```
臂 A(16 id) l1trade,l5combo,midtp,suptp,tpcommit,tpdying,lf_rescue,teambrain,ownhalf,overchase,fieldregen,wandbleed,capmono,cmrguard,tpdead,zusult
臂 B(15 id) l1trade,l5combo,midtp,suptp,tpcommit,tpdying,lf_rescue,teambrain,ownhalf,overchase,fieldregen,wandbleed,cmrguard,tpdead,zusult
种子(两臂共用) 888 895 896 906
```
臂 B = 臂 A 删掉第 13 项 `capmono`,其余顺序一字不动(顺序保持一致很要紧:落地后要用
S3 逐局 stamp 的真实 cand 串做交叉验证「臂 B 串 = 臂 A 串去掉 `capmono`」,20:11Z 立的
「事后交叉验证比事前 grep 更硬」)。

### 4.3 静态核对(带空格 pattern,08:08Z 立的规矩)

树 `67ac12d03cfccc68cc770d7f11c07c19038f5416` = `git ls-remote origin main` 远端真值 = 本地 HEAD
(**仍不信本地 remote-tracking ref**,20:11Z/02:06Z/06:13Z 三次踩过)。

- 16 个 armed id:**15 个各恰好 1 个 `IsSoakCandidate` 读者**;
  **`lf_rescue` 裸字面量 0 是已知正确的**(经 `J.IsLaneFixOn( 'rescue' )`,
  `jmz_func.lua:5459` → 唯一消费点 `:5765`)。
- 必须缺席的五个:`tpwatch` 1(§Q.1 reject,代码保留永不 arm)、`roamstale` **0**
  (promote 落地属实)、`wandlimbo` 1(§J.1.4 机会普查未做,不 arm)、`axebuyblink` 1
  (§O.1 出集,代码保留)、`zusultx` 1(GH #59 引入,不在 eligible 集)。
  ⇒ **出集/不 arm 唯一的闸门就是「不写进 cand 串」**,逐字照抄即可。
- 新申请的 `retnear`/`towerreach`/`defclose`/`defstale`/`defnum`/`esaftershock`
  按 §R **一个都不加**。

### 4.4 被 bisect 的变量非惰性核实(防 `[bug] #31` 重演)

`capmono` 的**唯一读者**在 `bots/mode_team_roam_generic.lua:128`:

```lua
if J.IsModeTurbo() and J.IsSoakCandidate('capmono') then
    if desire > 0.72 then return 0.72 end
    return desire
end
if desire > 0.9 then return 0.72 end  -- soft ceiling(= 已发布默认的悬崖)
```

⇒ **臂 B 拿到的正是已发布默认那条悬崖**,臂 A 把它变成单调天花板。**删掉 id 真的会改行为,
不是空转**;且改动只落在这一个调用点,隔离干净。

**给录像组的一条口径提示(与 §R.3 推论 ② 直接相关)**:`capmono` 不是「每局 <1 次的动作」,
它是**帧级的欲望天花板**,有效带宽是 **desire ∈ (0.72, 0.9]** —— 在臂 A 这些帧被压到 0.72
(会输给已 promote 的 `lanesurv` 撤退 0.75),在臂 B 它们原样通过。**被扣下的动作不产生事件,
只能用对照臂反推**(§R.3 推论 ②)——而这波的臂 B **在构造上就是那个对照臂**,
所以口径是**逐种子配对 + 按局归一化**地比两臂「team-roam 动作被选中」的率,不是任何单臂计数。
12:08Z 的教训照旧适用:**两侧局数不等的程度必须逐波实测,不许外推**(上波 Zeus 候选侧 +23.8%)。

### 4.5 上机命令模板(臂 A,16:11:38Z 之后)

```bash
SHA=$(git ls-remote origin main | cut -f1)   # 现取,全 40 位
for s in 888 895 896 906; do
  bash tools/batch_test/aws/spot_run.sh --count 1 --on-demand --slots 16 --hours 2 \
    --ref "$SHA" --validate "<臂A 16-id 串> $s --games 22"
done
```
臂 B 同上、cand 换 15-id 串、种子相同;**若届时 `git log $SHA..origin/main -- bots/ game/` 非空,
臂 B 必须钉臂 A 的那个全 40 位 SHA**(20:11Z 先例),并在空 clone 里实测 `SHA_FETCH_OK` 再上机。

### 4.6 落地必查项

1. 暖场局 stamp 必须 = 所用 SHA(裸 SHA checkout 静默失败则该轮作废;已连续四波通过);
2. S3 真实 cand 串逐字 = 上述两行,且 `tpwatch`/`roamstale`/`wandlimbo`/`axebuyblink`/`zusultx`
   五个全部缺席,且**臂 B 串 = 臂 A 串去掉 `capmono`**;
3. **先分 run 分目录下载再带前缀合并**再 `recover_verdict.py`(08:08Z 那次跨 run 同名撞车
   静默丢过 15.9% 的局);
4. 判读:`capmono` 的 (b) **按 §R 用行为检测器,不许用 gpm/xpm**;4 种子经济 MDE ≈ 35.6 gpm,
   **不得给任何单 id 判条件 (b)**;挑种子波次的经济读数**不得与 863-870 连号波次并列比较**(§K.5)。

## 5. 启动决策:不启动

- `queue.json` 无 pending ⇒ 走章程 4b 例行波次。
- 三条件:(ii) 满足(树 `7564660`→`67ac12d` 有 hero/strategy 组多个 commit);
  (iii) 满足($11.70 + $1.6 ≈ $13.3 ≤ $45);
  **(i) 不满足** —— 距上一波例行上机 10:11:38Z,门槛 **16:11:38Z**,现在 14:0xZ,**差约 2 小时**。
  小时级差额,**不援引 06:07Z 那种「差 2 分钟按立法目的」的例外**;先例
  00:09Z / 02:06Z / 06:13Z / 08:08Z / 10:06Z / 12:08Z / 16:10Z 全部照此处置。
- **下一波例行最早 16:11:38Z**,内容 = 第 4 节的臂 A。
- 章程 4c 缺口 `[harness] #33` 仍开着,不重复开。

## 6. 局数

- **上一波(10:11Z,已于 12:08Z 收割)最终局数**:888=63(radiant-cand 41 / dire-cand 22)、
  895=73(42/31)、896=75(43/32)、906=71(41/30),合计 **282** 有效局
  (radiant-cand 167 / dire-cand 115)+ 23 暖场。dire 波动大(22-32),撞 35min stall 上限;
  **再提 `--games` 只会加大不对称**,补 dire 要动 harness 侧。
- **本轮在跑波次**:无(开工与收尾均 0 台)。
- **下一波预期**:臂 A ≥280 局,臂 B ≥280 局。

## 7. 泄漏检查

开工时 `check_costs.sh` 与收尾时 `describe-instances` 均确认 **0 台在跑,无泄漏**;
唯一常驻成本是 AMI 快照 $0.1334/天。

## 8. 验证(铁律 6)

本会话**未改动任何 Lua**(改动仅 `iterations/` 下的报告与章程),且本容器未安装
`luacheck`/`lua5.1` ⇒ 按立法目的本轮无适用对象。

## 9. 跨组

新开 `[batch]` issue:每波成本实测(估计低 2 倍)+ $45 围栏 ETA(约 8-25/26)+
§R 拓扑决定(两臂串行 on-demand)+ `capmono` 帧级口径提示。

## 10. Token 用量

```
TOKENS total_in=3,747,647 out=42,204 turns=38
```
(统计时刻为止;之后的收尾回合未计入。)
