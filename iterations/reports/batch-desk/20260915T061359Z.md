# 批测台 2026-09-15T06:13:59Z —— 刹车第二十三轮持有

**一句话**:零发波、零收割欠、零泄漏,`bots`/`game` 一行未改,**本轮新增计费 `$0.00`**。
⭐⭐⭐ 本轮的实质工作单元有两件,都指向 RULING 45:
**(甲) 它批准的那 `$0.01` 已经在 00:14Z 花过了 ⇒ 本台⛔不第二次买**,改为把欠条按交付结清;
**(乙) 它要的第二半交付物(「谁产生、削多少」)本轮用免费仪器推进了一格,
并且把 00:14Z 的 `~28%` 向对归因不利的方向订正成 `23–26%`。**

---

## 一、成本(当轮现跑,⛔ 下轮不得抄作前提)

- `AWS_SETUP_EXIT=0`(容器无 CLI,自装 `aws-cli/1.46.1 Python/3.11.15 botocore/1.43.62`;
  身份逐字 `arn:aws:iam::581436534924:user/dota2bot-agent` + `(restricted batch-runner user — correct)`)。
- `COST_EXIT=0`(⛔ 未走管道,重定向后裸读)。

| 量 | 上一轮(03:06Z) | **本轮** | 变化 |
|---|---|---|---|
| MTD | `$90.373` | **`$90.569`** | **+`$0.196`** |
| `budget refreshed` 戳 | `2026-09-14T20:27:33Z` | **`2026-09-15T04:18:45Z`** | **+7.85h,戳解冻** |
| forecast | `196.979` | `186.702` | −10.3 |
| headroom 对 `$90` | `$-0.373` | **`$-0.569`** | 更深 |

⭐ **戳本轮解冻了** —— 上一轮与 00:14Z 连续两轮读到同一张冻结快照(`$90.373` / `20:27:33Z`),
⛔ 那两轮登记的是「没测」不是「没涨」。本轮拿到新快照,**那 7.85h 真实增量是 `+$0.196`**
(≈ `$0.60/天` 的瞬时速率,落在已登记的 `$0.40–0.92/天` 区间内,⛔ 不据单点改任何常数)。

**GH #801 四行整块照抄(⛔ 未用 `COST_CONFIRM_AT` 自救,用默认值跑)**:

```
   >= $35, but headroom to the $90.00 brake is $-0.569
   < $1.10 (cheapest wave) — CE confirmation SKIPPED, NOT passed (GH #801).
   Nothing can launch at this MTD, so confirming it buys nothing.
   Resumes by itself as soon as headroom >= $1.10.
```

⚠️ **这是 SKIP 不是 pass。**

**accrual probe 三行照抄**:
`alpha = 0 accruing instance(s) account-wide; beta = 0 wave record(s) after 2026-09-14T17:00:45Z` /
`stamp unknown (fresh container) -- alpha and beta are both zero -- a frozen stamp over an idle account opens no blind spot`。

---

## 二、闸 (iii) —— `python3 tools/batch_test/soak/wave_fence.py --planned 1.10`

`FENCE_EXIT=3`。⛔ 未传任何 `--no-*` / `--pending` / `--director-crossing`
⇒ **无一行「这是 SKIP 不是 pass」**。逐字:

```
actual (MTD)     : $90.569   <- re-read this run, never cached
pending waves    : $0.000   (launched, may not be in MTD yet)
planned          : $1.100
projected total  : $91.669
fence            : $100.00   <- lowest ACTUAL alert not yet crossed (Ruling 1)
brake            : $90.00   (owner's line, not derived here)
operative ceiling: $90.00   = min(fence, brake)
A launch at this instant would put MTD past the $90.00 BRAKE line.
WAVE_FENCE: THROTTLED (exit 3) -- brake, not fence. Stop and report to the owner; do not wait it out.
```

RULING 5/6/7 三条行各自照抄:
`accrual scope : 17 region(s) read [...] <- COMPLETE: every region this account has enabled` /
`wave accrual : records after 2026-09-14T17:00:45Z (= 2026-09-15T04:18:45Z - 11.3h lag, clock from budget snapshot)` /
`accrual check : CERTIFIED (0 accruing instances account-wide, read this run) -- pending $0.000 is a reading, not a default.`
⭐ **钟没有降级**(逐字 `clock from budget snapshot`)⇒ RULING 7 (甲) 的 epoch 修法本轮仍有效。
`crossing registry: 0 record(s) ... none in force` + `1 retired record(s)`;`budget filters : none`;
告警 `$50 [ALARM]` / `$80 [ALARM]` / `$100 NOT yet crossed [OK]`
(⚠️ 照登:`$90`–`$100` 在 AWS 侧**仍可花**,挡住它的是 owner 这条线不是 AWS)。

⇒ **连续第二十三轮,不发波的原因是闸不是本台的处置。**

---

## 三、⭐⭐⭐ RULING 45 (甲):批准的那 `$0.01` 已经花过了 ⇒ 本台⛔ 不第二次买

**`iterations/owed_executions.json:gh779_s3_egress_usage_type_attribution` 本轮读作 OWED,
而它要的东西已经在 `iterations/reports/batch-desk/20260915T001414Z.md` §3.2 交付了。**

### 3.1 时间线(两条戳,⛔ 不是推测)

| 时刻 | 事件 |
|---|---|
| 2026-09-14T21:22Z | 本台交棒 ③ 问:那次 `GroupBy=USAGE_TYPE` 的 CE 买不买 |
| **2026-09-15T00:14Z** | **本台(另一轮)自行买了。** 逐字 `CE_EXIT=0`,`DAILY × GroupBy=USAGE_TYPE × Filter=SERVICE:S3` **同在一次请求里**,`$0.01` |
| **2026-09-15T04:1xZ** | **总监 RULING 45 落地:「买,买一次,仅此一次」** |

⇒ **裁定是在交付物已经落地之后到的。** 裁定自己的理由段逐字引用了
「S3 那条线 `$0.3893/天`,其中 egress `$0.3873`(99.5%)」——
**那两个数正是 00:14Z 那次购买的产物** ⇒ 裁定是在**已经读过结果**的情况下批准了这次购买。

### 3.2 ⛔ 为什么不再买一次

裁定正文自己写着 **`⛔ 不批准第二次`**。按 `trigger` 字面(「批测台的下一个工作单元」)
机械执行,本轮就会掏出**第二个** `$0.01` 去取**同一批行**,
而 MTD 此刻已在刹车线**之上** `$0.569`。⇒ **执行字面会违反裁定正文。**

⚠️ **本台的不买理由必须说清,因为它与被明文禁止的那个理由长得像但不是同一个**:
`trigger` 逐字禁止的是「**以发不出波为由顺延**」(拿预算约束当工作量豁免)。
**本台不是顺延,是宣告已执行** —— 两者的判别式是「交付物在不在」:
它在,在 00:14Z 报告 §3.2 的表里,本轮逐行复核过。⛔ 本台不主张这两个理由可以互换。

### 3.3 交付物照登(⛔ 不是转述,是 00:14Z 那次购买的原始读数)

`ce get-cost-and-usage`,09-08 → 09-15,DAILY,`SERVICE=S3`,`GroupBy=USAGE_TYPE`:

| 日期 | S3 合计 | **DataTransfer-Out-Bytes** | TimedStorage-ByteHrs | Requests-Tier1 | Tier2 |
|---|---|---|---|---|---|
| 09-08 | 0.6156 | **0.4653** | 0.1248 | 0.0230 | 0.0025 |
| 09-09 | 1.1328 | **0.9789** | 0.1285 | 0.0231 | 0.0023 |
| 09-10 | 1.1575 | **1.0150** | 0.1333 | 0.0072 | 0.0020 |
| 09-11 | 1.2606 | **1.0959** | 0.1369 | 0.0250 | 0.0029 |
| 09-12 | 0.5508 | **0.3962** | 0.1411 | 0.0120 | 0.0015 |
| **09-13**(零实例全天) | 0.6599 | **0.5188** | 0.1380 | 0.0024 | 0.0007 |
| **09-14**(零实例) | 0.3893 | **0.3873** | —(未出) | 0.0016 | 0.0004 |

**裁定要的「前三名及各自 `$/天`」(七天均值 = 合计 ÷ 7)**:

1. **`USW2-DataTransfer-Out-Bytes`** — 七天合计 **`4.8574`(84.2%)** ⇒ **`$0.694/天`**;
   **零实例日 09-14 单日 `$0.3873` = 当天 S3 的 99.5%**
2. `USW2-TimedStorage-ByteHrs` — 合计 `0.8026`(13.9%) ⇒ **`$0.115/天`**
3. `USW2-Requests-Tier1` — 合计 `0.0943`(1.6%) ⇒ **`$0.0135/天`**
4. (第四)`USW2-Requests-Tier2` — 合计 `0.0122`(0.2%) ⇒ **`$0.0017/天`**

⇒ 七天合计 **`5.7665`**。

**USAGE_TYPE 归因已交付。**

⚠️ **限度两条,⛔ 不放宽**:(a) 七天均值把**有批测的日子**(09-08→09-12)和**零实例日**
(09-13/09-14)混在一起,⇒ **`$0.694/天` 不是空转速率**,空转速率是 09-14 那一行的 `$0.3873`;
(b) `TimedStorage` 在 09-14 逐字**未出现**(不是 0,是那天的行没出) ⇒ ⛔ 不把 `$0.115/天`
当成 09-14 的存储费,存储那条的最近一个真读数是 09-13 的 `$0.1380`。

---

## 四、⭐⭐ RULING 45 (乙):「谁产生、削多少」—— 免费仪器推进一格,并订正本台自己一个数

### 4.1 「哪一类流量」——已确定

`USW2-DataTransfer-Out-Bytes`,零实例日 **`$0.3873/天` = 当天 S3 的 99.5%**;
按 `$0.09/GB` 折算 ⇒ **≈ 4.30 GB/天被从桶里拉到公网,在没有任何机器活着的日子里**。
桶完全私有(00:14Z 四项逐条读:PAB 四项 `true` / `NoSuchBucketPolicy` / ACL 只有 owner)
⇒ **拉它的必然是带凭证的自己人**,⛔ 排除公开桶被白嫖(那一支若成立处置是安全事件,方向完全不同)。

### 4.2 「谁产生」—— ⭐ 本轮新做的:逐轮实计,不用「8×8」外推

00:14Z 那轮的 `~28%` 是**估算**(逐字「8 份 ≈ 0.16 GB ≈ `$0.014`/轮,该流 8 轮/天」)。
**本轮改为把 replay-check 09-14 当天每一轮报告里自己写下的份数逐条读出来**(⛔ 非抽样,⛔ 非外推):

| replay-check 轮次(09-14) | 自报 `.dem` 份数 | 自报体积 |
|---|---|---|
| 00:51:58Z | 8 | ~174 MB |
| 03:52:46Z | **1** | 21.4 MiB |
| 09:49:21Z | **9** | ~200 MiB |
| 13:12:32Z | 8 | ~180 MB |
| 15:48:49Z | 8 | — |
| 18:45:00Z | 8 | — |
| 21:47:58Z | 8 | — |
| **09-14 合计** | **50 份** | — |

⇒ **09-14 实际是 50 份,不是外推假定的 64 份(8×8)。**
按本台全量列举的 `dem21/` 均值 **19.9 MB** ⇒ **0.995 GB ⇒ `$0.0896/天`**;
按 replay-check 自己报告折算出的 **~22 MB/份**(174/8、200/9、180/8 三处一致)⇒ **1.10 GB ⇒ `$0.099/天`**。

⭐⭐ **⇒ replay-check 占 09-14 出网的 `23%–26%`,不是 `~28%`。**
⚠️ **这个订正的方向对「把钱归因掉」是不利的**(未归因从 72% 涨到 **74%–77%**,约 **`$0.288–0.298/天`**),
**照登不藏**。⛔ 本台不因为方向不利就取区间上沿。

**其余四个 stream 09-14→09-15 的报告本轮逐份扫过**(`hero` 10 份 / `strategy` 10 份 /
`director` 10 份 / `batch-desk` 10 份):`.dem` 的出现**全部是散文引用**
(如 hero 引本台 W34 归档扫描的「102 份」、strategy 引 fixture 取自真实 `.dem` 帧、
director 引 GH #388 的 W34 语料)—— **没有一份记录了一次下载**。

⚠️⚠️ **这条证据的失效方向必须点名,而且它朝危险那侧**:本方法读的是**各流自报**,
⇒ **一个下载了但没写进报告的流,对本方法完全隐形**,而 00:14Z 已经立案过
「读 S3 在各流账里记成免费 ⇒ 没有任何一条记账把它加起来」这个集体错觉
⇒ **「报告里没写」恰恰是那个错觉的预期表现,⛔ 不能读成「没下载」。**
⇒ **74%–77% 未归因** ⛔ 不点名、不分摊、不猜。

⭐ **结构性限度(这一条不是本轮的不足,是仪器的性质)**:
`USAGE_TYPE` 回答的是**流量是哪一类**,它**在原理上永远不回答是谁拉的**。
⇒ **再买多少次 CE 都到不了「谁」** —— 那需要 S3 服务端访问日志(00:14Z 现测
`BLOG_EXIT=0` 且返回空 ⇒ **从未开启**;开启本身有成本、是配置变更)或 CloudTrail 数据事件(贵得多)。
⛔ **本台未擅自开启任何一个**;这正是 00:14Z 交棒 ② (b) 的问题,**至今未获裁定**。

### 4.3 「削多少」—— 数,⛔ 不含建议

裁定逐字 `⛔ 仍不建议、不预选任何一条线`。本台只摆算术(本月剩余按 **16 天**算,09-15→09-30):

| 情形 | 日费 | 本月剩余 16 天 | 相对「什么都不做」省下 |
|---|---|---|---|
| 什么都不做(09-14 实测) | `$0.3997` | `$6.40` | — |
| **只削掉唯一已归因的那一块**(replay-check 的 `.dem`) | `$0.300–0.310` | `$4.80–4.96` | **`$1.43–1.59`** |
| 出网**全部**归零(上界,⛔ 现实中做不到) | `~$0.14` | `$2.24` | **`$4.16`** |

⭐ **一条可执行面的量级刻度**:**最便宜的一波是 `$1.10`** ⇒
**只削掉那唯一已归因的一块、削满本月剩余,省出来的钱约等于一波。**
⚠️ **诚实边界三条,⛔ 一条都不去掉**:
(a) 出网不可能真归零 —— **录像组的活就是看录像,丙是削不是关**;
(b) 上表第二行只覆盖 **23%–26%**,**剩下 74%–77% 的可削性未知**(连是谁都不知道,见 §4.2);
(c) 速率样本仍是 **n=2 个空转日**(`$0.9185` / `$0.3997`,差近 2 倍),
⛔ 不外推十月、⛔ 不改任何常数。

### 4.4 9/30 落点(按本轮新 MTD 重算)

`$90.569` + 15.8 天 × `$0.40–0.92/天` ⇒ **`$96.9 – $105.1`**。
⇒ ⭐ **接下来半个月一波不发,也未必守得住 `$100` 硬线**;⚠️ n=2,区间宽是真实的不确定性不是排版。

---

## 五、⚠️⚠️ 本轮独立发现:`validation/` 那条「最新对象」读数,九轮都是用错的量得到的

**计数这条腿是对的,判断「有没有新结果」靠的也是它** —— 但**贴在它旁边的那句「最新对象」是错的**。

- 本轮 `S3VAL_EXIT=0`,`validation/` **592 对象**,与上一轮**逐位相同** ⇒ **零新对象、零收割欠**(这条不变)。
- **但**:自 2026-09-14T00:06Z 起**连续九份报告**(`20260914T000600Z` / `031100Z` / `061300Z` /
  `091100Z` / `150800Z` / `180741Z` / `212233Z` / `20260915T001414Z` / `030639Z`)都写
  「最新对象仍是 `tpcommit+46ids-24c30245be01_20260907_2218_run.log`(`2026-09-07 22:18:52`)」。
- **本轮两跑对照**(⛔ 都未走管道):
  - `tail -3`(即 `s3 ls` 的原始**键序**)⇒ `...tpcommit+46ids-24c30245be01_20260907_2218_run.log  2026-09-07 22:18:52`
  - `sort | tail -1`(**时间序**)⇒ **`lf_rescue+27ids-d177b87026dd_20260912_1018_run.log  2026-09-12 10:18:26`**
- ⇒ **真正的最新对象比被登记的那个晚 5 天。** `s3 ls --recursive` 按**键名字典序**输出,
  而 `tpcommit…` 的键在字典序上恰好排在最后 ⇒ **九轮报的是「字典序最后一个键」,不是「最新对象」。**

**⭐ 为什么这值得单独立一节(而不是一个笔误)** —— 它与铁律 4 §CL (i-a)
「**读数不是局数**,#329 之前六轮是用错的量满足的」**逐字同型**:
一个量长得像被问的那个量,于是**九轮独立地用它满足了一条检查**,没有人举手。

**⚠️ 失效方向朝危险那侧**:本轮它无害,因为**对象数没变**(592 = 592),
**而对象数才是真正在把关的那条腿**。但设想计数腿因任何原因读不出来、
或有人只看那一行 ⇒ **一个 09-12 落地的新 verdict 会被一句「最新对象仍是 09-07」盖住**,
读起来像「什么都没来」。⇒ **它是一条冗余读数,而冗余读数写错时不会立刻疼。**

**⛔ 本轮明确不主张的三件事**:
1. ⛔ **不主张漏收了任何东西** —— 当场独立复核:`iterations/reports/batch-desk/waves/W69_verdict.json`
   与 `W69_wave.json` **都存在**,`state.json` 里 `lf_rescue` 出现 **67 次**
   ⇒ **09-12 那个 verdict 早已被收割**,**收割欠仍然是零**。
2. ⛔ **不改那九份历史报告**(⛔ 不改写档案)。
3. ⛔ **不自行改本章程散文里的读数配方**(编排权在总监,先例 GH #33)——
   只登记 + 开 issue,⇒ 见交棒 ②。

---

## 六、收割 / 泄漏

**收割:零欠。** `S3VAL_EXIT=0`,`validation/` **592 对象**与上一轮逐位相同
⇒ 零新对象、**零下载**、**零由本轮产生的 S3 出网**;`recover_verdict.py` **未跑**
(**没得收,不是漏收**);⛔ 不重算已收波次。
**局数**:无波在跑 ⇒ 进度 **N/A 不是 0 局**;距上一波(W69,末台 `2026-09-12T09:26:31Z`)起飞 **68.8h**。

**泄漏:五条独立路径全零(⛔ 不合并成一句)**:

1. `check_costs.sh` 自带 `running/pending` 区块 **空**;
2. `ec2 describe-instances` 五态(`running`/`pending`/`stopping`/`stopped`/`shutting-down`)
   **不加 tag 过滤** ⇒ **零行**(`INST_EXIT=0`,`LINES=0`);
3. `ec2 describe-volumes` **全账户不过滤** ⇒ **零行**(`VOL_EXIT=0`,`VOL_LINES=0`);
4. 闸 (iii) 账户级普查 `CERTIFIED (0 accruing instances account-wide)` + `17 region(s) <- COMPLETE`;
5. **账单侧**:§3.3 的逐日表里 09-13/09-14 两天 **`EC2 - Compute` 逐字不出现**
   ⇒ 过去两天没花过机器的钱。⚠️ **限度照抄不放宽:只对滞后带已过去的日子成立,
   ⛔ 不能给今天的波背书**(RULING 6 的域)。

AMI 仍只 `ami-0a990a26d89c66547`(`dota2bot-batch-20260718`)一张 —— **常设成本,非泄漏**。

---

## 七、铁律 9(Owner 优先项)

`OWNER_PRIORITIES.md` **P4.1 upstream 标尺波** —— **连续第二十二轮欠**,
⛔ **唯一阻因是闸 (iii) `exit 3`(预算)**,不是本台未认领也不是排期。
棒已在 `DECISIONS_NEEDED.md §15` 与 GH #779,⛔ 不重复立条。
常设运维项(Spot 优先)零发波 ⇒ 无可执行面。

**RULING 44(`hero27_dem21_deadline_scan` 的 fallback 线改 `2026-09-17`)**:
⚠️ 本轮读到,⛔ **本行的 executor 不是本台**(逐字表态方是 hero 组)⇒ 本台不代表态、不代扫描;
本台只登记:距那条新 fallback 线还有 **2 天**,且 §RULING 44.1 那份「滚动过期」的算术
(08-26→08-29 四档 **49.74 GB** 在 **09-16 → 09-19** 之间走完)意味着
**明天起 W34 语料开始成批消失**。

---

## 八、⛔ 本会话 `bots`/`game`/`tests`/`tools` 一行未改

⇒ **无一条 trunk red 可能由本轮引入**;产物只有 `iterations/` 下的本报告与章程条目。

---

## 九、自检(铁律 10)

⭐ **上一轮交棒 ⑥ 的「一次到位」本轮兑付了两件**:第一条命令**未走管道**(重定向后裸读)、
**未设 `timeout`**;⚠️ **但第一跑仍被脚本自卫拦下** —— 逐字
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.`
(**管道坑第 33 次**,因为第一跑把 `tail` 接在了后面),
第二跑改 `> /tmp/sc.log 2>&1` 后裸读 ⇒ 零损失,**如实登记不辩解**。

写作时 python 腿仍在跑、**无收尾横幅** ⇒ **`SELFCHECK_EXIT` 本轮正文未知**,
⛔ **不读成通过**、⛔ **也不空转等它**(铁律 11)。真码见补记。

**已跑完腿实读**:
- `LUA GATE COVERAGE disk 469 | push gate 325 | 开工自检 leg 107 | known_red 7`;
  `UNCOVERED 114 of 469 (24%)` `{no_manifest_row: 52, timed_out: 60, too_slow: 2}`;
  `NOW COVERED tests/test_fieldsip_atom_pricing.lua`(**离开 uncovered 集 1 份**);
  `UNCOVERED SET GREW -- 2 file(s)`:`test_dusttower_dive_guard.lua`(**第六轮**)、
  `test_fieldsip_transfer_receiving_site.lua`(**第四轮**)⇒ ⛔ **不重复开 issue**(已在案)。
  ⭐ 与 00:14Z 对照:`disk 459→469` / `leg 102→107` / **`UNCOVERED 115→114`** ⇒ 缝**窄了一格**。
- `no armed id hangs under an unarmed gate -- OK`。

---

## 十、铁律 11

零 `requires approval`、零权限对话框、**零空转**(自检后台跑,成本/闸/收割/泄漏/§三§四 并行推进)。
⭐ 除 §九 自己点名的那一处外,每个读退出码的命令一律**重定向后裸读**。

---

## 十一、交棒

1. ⭐⭐⭐ **owner —— 刹车第二十三轮持有,GH #779 第十八轮零表态。本轮有两个新输入**:
   (a) 戳解冻后的真实增量 **+`$0.196`/7.85h**,MTD **`$90.569`**,9/30 落点 **`$96.9–$105.1`**;
   (b) **选项丙第一次有了可执行的量**:只削掉唯一已归因的那块 ⇒ 本月剩余省 **`$1.43–1.59`** ≈ **一波**;
   出网全归零(上界,做不到)⇒ 省 **`$4.16`**。
   ⛔ 本台**不建议、不预选**任何一条线。**已推 owner 一条。**
2. ⚠️⚠️ **总监 —— §五 的读数配方缺陷**:`s3 ls --recursive | tail` 是**键序**不是**时间序**,
   九轮报告据此登记了一个错的「最新对象」。⛔ 本台**未改章程散文、未改历史报告**。
   请裁:配方是否改成 `sort | tail -1`(或显式写「字典序最后一个键」),
   以及这九轮的登记要不要在档案里挂一条更正。**已开 `[batch]` issue**(见补记)。
3. ⭐⭐ **总监 —— RULING 45 的 `trigger` 与它自己的 `⛔ 不批准第二次` 本轮撞了**(§3.1/§3.2):
   裁定在交付物落地**之后**到达,按 `trigger` 字面执行会掏第二个 `$0.01`。
   ⛔ 本台未买,已按交付结清(§3.3 写下 `USAGE_TYPE 归因已交付`)。
   请核:(a) 前三名读数是否确在报告里(裁定 `done_when_note` 指定的退休复核);
   (b) **更一般的那条**:「裁定晚于交付」这个形状要不要在 `owed_executions.json` 里
   有一个通用的处置(本台这次是自行判断的,⛔ 不应由执行方自己裁)。
4. ⭐ **总监 —— 00:14Z 交棒 ②(b) 仍未回,而它现在卡住了 RULING 45 的后一半**:
   `USAGE_TYPE` **在原理上**不回答「谁拉的」(§4.2 末),⇒ 那 74%–77% 要归因
   **只能**开 S3 服务端访问日志(现测从未开启,**自身有成本、是配置变更**)或买 CloudTrail 数据事件。
   ⛔ **本台未擅自开启任何一个**,请裁。
5. ⚠️ **总监 —— 00:14Z 交棒 ② (a) 仍未回**:各流报告里的「零支出」是否必须改成
   「零 EC2 / 零 CE,S3 出网 ~`$X`」。§4.2 本轮**又一次**踩到它:
   **本台只能读各流自报,而自报里 S3 读取记成免费** ⇒ 归因方法对「下了没写」完全隐形。
6. ⚠️ **总监 —— 上一轮交棒 ①③④⑤⑥ 与 00:14Z 交棒 ④(章程散文里 `wave_fence.py` 路径错)
   本轮仍未获回应,逐字保留**,⛔ 本台不自行退休任何一行。
7. **下一轮本台**:开工第一条命令 **`bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1`
   然后单独一条 `tail`** —— ⛔ 不在同一条里接管道(本轮第 33 次就是这么来的)、⛔ 不设 `timeout`。
   刹车解除(headroom ≥ `$1.10`)时按规格发 P4.1 标尺波,
   并把 GH #801 那四行**恢复后**的样子抄进报告(那才是它第一次走「该买」分支)。

---

## 十二、Token 用量

(见文末补记)
