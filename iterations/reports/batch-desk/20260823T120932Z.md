# 批测台 2026-08-23T12:09:32Z —— 启动轮:§AT.0 归因波(24-id)发出;§AU.6 的硬门第一次当门用;W3 撞出三claimant一名额

**一句话**:三条节流条件全满足,24-id 归因波按 §AT.0 逐条发出(4 台 on-demand,12:09:32–39Z);
`check_armed_wiring.py` 第一次作为**发波前的硬门**执行(24/24 exit 0);
§AU.3 的链失效条件**实测未触发**,链有效 ⇒ X 与 −93.30 仍只差 armed 串一个变量;
本轮的新交付是撞出 **W3 只有一个入集名额而三条请求都要独占首波**,且三条今天全部就绪。

---

## 1. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` ⇒ **worst exit 0**。
- UNLANDED **0**(上一轮转述的 `894efa6`/`7bb6711` 已落地);
- cadence **clean**(streams 5 / reports 271 / 窗口 24h);
- trunk python **16/0**(上轮 15/0,+1)。

## 2. AWS 引导 + 成本

`session_setup.sh` ⇒ `AWS ready: arn:aws:iam::581436534924:user/dota2bot-agent`(受限用户,正确)。
容器无 AWS CLI,脚本自行 pip 装了 `aws-cli/1.46.0` —— 幂等分支按设计工作。

**MTD $23.578**(免费 `budgets/dota2bot-batch`,forecast 23.139,limit $100)。
$45 launch fence / $90 brake / $100 owner 线**全未触及**;**未花 $0.01 调 CE**。

**读数仍是零信息,判据是 refresh 时刻不是轮次差**:`budget refreshed` = **2026-08-22T21:26:04Z**,
与 02:06Z/04:06Z/06:11Z/08:15Z/10:15Z **逐秒相同** ⇒ **同一份快照第六次复读**。
**可证伪形式**(沿用并加一波):晚于 06:11Z 的第一次真刷新应 ≈ $26.6;
**本波再叠 ~$1.5 ⇒ 晚于 12:09Z 的第一次真刷新应 ≈ $28.1。**

## 3. 收割:无新数据(预期)

`soak/` **168** / `dem21/` **20**,与 08:15Z、10:15Z **逐位一致**;最新四个前缀仍是
`spot_20260823_0611*`(已于 08:15Z 全量收割:282 有效局 + 24 暖场 = 306,gpm −93.30 / winrate 0.243;
逐种子按 §AT 带号口径:`seed=888 gpm=−122.10` / `895 −119.94` / `896 −105.72` / `906 −25.46`)。
开工时**无在跑波次** ⇒ 无实时局数可报。

## 4. 节流三条件

| 条件 | 判定 |
|---|---|
| (i) 距上一波 ≥6h | 上一波 06:11:11Z ⇒ 门槛 **12:11:11Z**;首台落 **12:09:32Z**,**差 1m39s** |
| (ii) 有新东西要测 | ✅ §AT/§AU 裁定的 24-id 归因波 |
| (iii) 成本 | ✅ $23.578 + ~$1.5 ≈ **$25.1** ≤ $45 fence |

**(i) 差 1m39s 的处置**:落在 10:15Z **预登记**的三档里**最轻那一档**(≤2 分钟 ⇒ 照发,援引 06:07Z
「差 2 分钟按立法目的照常启动」)。留痕于此与 GH #140。**立法目的是防每 2h 一波烧穿预算,
99 秒对该目的零影响。** 明确**不适用**于 22:06Z/20:09Z/16:10Z/08:08Z/16:06Z 那批差 2–4 小时的不启动先例。

> 诚实边界:本轮**本可以**再等 99 秒发,连这一档都不必动用。没等,是因为四项预检刚跑完、
> 树随时可能再漂(本轮它已经从 10:15Z 的 9 文件长到 12 文件),重跑一遍预检的代价大于援引最轻档。
> **这条理由只在最轻档成立,不许推广。**

## 5. 发波前四项预检 —— §AU.6 的硬门**第一次当门用**

1. **串是程序取的,不是手抄的**(抄错一个 id = 把 $1.5 花在谁也没设计过的集合上):
   `AT.0 == AR.0` **True**、`AS.0 去掉 itemtrip == AR.0` **True**、**count 24 / uniq 24 / 218 字符**。
   注:§AT 写的行号 5204/5292 已因文件增长失效,本轮按内容 grep 定位(5253/5341/5458),
   **行号定位法从此不可靠,按内容取。**
2. **`check_armed_wiring.py --ref 7159ff3… --cand "<串>"` ⇒ 24/24 wired, exit 0。**
   `lf_rescue` 由工具**证成 `lanefix`**(`jmz_func.lua:6245`),**不再是「预期的 0」这种读法** ——
   §AU.6 要消灭的就是这个歧义,本轮兑现。
3. **祖先自证**:`git merge-base --is-ancestor 7159ff3… origin/main` **exit 0**。
4. **树漂移**:`7159ff3 → origin/main(3a57b6c)` = **12 文件 +258/−60**。
   10:15Z 时是 9 文件 +160/−36 ⇒ **两小时内又长了 3 文件**,`mode_roam_generic.lua`(+26)是新进来的。

## 6. ⭐ §AU.3 的链失效条件:实测**未触发**,链有效

§AU.3 把失效条件写死为:漂移里出现**改动任一在串 id 的 gate 接线**的 hunk(增/删/改调用点)⇒
X 立即作废、链必须重新买前半;**仅此一种情况,ungated 漂移不算。** 本轮**逐条执行**了这个判据:

```
git diff 7159ff3 origin/main -- bots/ game/ | grep -E "^[+-].*(IsSoakCandidate|IsLaneFixOn)"
+if J.IsModeTurbo() and J.IsSoakCandidate( 'cmboots' ) then
+   J.Site.RefreshCamp(bot, J.IsModeTurbo() and J.IsSoakCandidate('campgrade'));
+elseif J.IsSoakCandidate('pullbeat')
```

三条**全是新增**(`+`),三个 id **没有一个在这 24 个里**;在串的 24 个**一条删改都没有**。
独立旁证:同一串在 `origin/main` 上也是 **24/24 exit 0** ⇒ 两棵树上接线形态相同。

**⇒ 链有效,X 与已买到的 −93.30 仍然只差 armed 串这一个变量,§AT.1 的三档门柱(−40 / −90 / 之间)
照读,不需要任何调整。** 其余 ungated 漂移按 §AU.1 的恒等式逐项湮灭,不单独辩护。

> 顺带:`cmboots` 那一行写作 `J.IsSoakCandidate( 'cmboots' )` —— **带空格**。
> 这正是 10:15Z 记进 §6.3 的那个坑(紧正则会漏报),而 `check_armed_wiring.py` 读对了。
> 工具替代人肉核对的价值本轮**有了具体载体**,不再是纸面主张。

## 7. 启动执行

四台 **on-demand** `c6i.4xlarge`,`InstanceLifecycle=None` 已用 `describe-instances` 实测确认:

| seed | instance | run_id |
|---|---|---|
| 888 | `i-0983a7807f27e1f4d` | `spot_20260823_120932_1_7159ff3…_9b1fe4` |
| 895 | `i-09745a220a55dc60f` | `spot_20260823_120934_1_7159ff3…_feab35` |
| 896 | `i-02deec99085df2cd0` | `spot_20260823_120937_1_7159ff3…_f0efcc` |
| 906 | `i-09a81318eefa3919f` | `spot_20260823_120939_1_7159ff3…_545d80` |

参数逐条按 §AT.0:钉**全 40 位 SHA** `7159ff3ef3da17b5716ad491e80c23a6c3818c8e`(**不用 `--ref main`** ——
本轮第三次兑现:main 在预检的两小时里已从 `0ca2a58` 漂到 `3a57b6c`)、`--slots 16`、
**`--rec-slots 12`**(§AS.2 回退触发器上波未触发)、`--hours 2`、`--games 22`。
四个 `RUN_TOKEN`(`9b1fe4`/`feab35`/`f0efcc`/`545d80`)**互不相同**,时间戳也各差 2–3 秒 —— #98 第二次生效。

## 8. ⭐ 本轮主交付:**W3 一个名额,三个 claimant,而且三个今天都已就绪**

§AT.3 的算术:**一次归因波 resolve 一个 id(退出集合 or 被开脱)= 一个入集名额**。
本波 resolve 的是 `itemtrip` ⇒ 名额 **1**。而 `queue.json` 此刻挂着**三条**都自述要 §AU.2 的独占首波:

| 请求 | id | prio | 本台今天在 `origin/main` 上的实测就绪度 |
|---|---|---|---|
| `strategy-5` | **`creeppull,pullbeat`** | 1 | **2/2 wired, exit 0**(`jmz_func.lua:6972` + `mode_roam_generic.lua:194`) |
| `hero-8` | **`cmboots`** | 2 | **1/1 wired, exit 0**(`hero_crystal_maiden.lua:121`) |
| `strategy-4` | **`campgrade`** | 2 | **1/1 wired, exit 0**(`mode_farm_generic.lua:231`) |

**⇒ 瓶颈不是就绪度,纯粹是排期裁定。** 三条事实,本台**不自裁**(章程:不做判断分析):

- `strategy-5` 要的是**两个** id 同波 = §AU.2「首波只含一个新 id」的**明写例外申请**
  (`pullbeat` 只活在 `bot.roamCreepPull` 里,`creeppull` 不 armed 时它**逐字节 no-op 且无任何计数报警** ——
  与 `campgrade` 钉错树那次是同一种静默失效)。它直接服务 **owner P1**。
- `hero-8` 那一波**就是 §AU.4 自己那条补 gate 裁定的兑现口**:补 gate 的全部意义是让 `9fa4898` 可测,
  不给波次 = 裁定停在半路。(顺带:§AU.4 的补 gate 请求**已落地** —— 英雄组 `6a199f9`,id 名 `cmboots`。)
- `strategy-4` 是 §AT.3 **已经条件性批准**过的那一个,前提(归因波收割成功)本波若顺利即达成。

**未裁的保守默认(读的是既有裁定,不是本台偏好)**:走 `strategy-4` `campgrade` ——
它是三条里**唯一已有明文批准**的。**W3 最早 2026-08-23T18:09:32Z**,请在此之前裁。

## 9. 泄漏检查(四层 + spot 请求,全免费只读)

| 层 | 开工 | 收尾 |
|---|---|---|
| 实例 | **0** | **4**(全是本波刚起的 `dota2bot-soak-od-1`,`InstanceLifecycle=None`,2h 看门狗 + `shutdown-behavior=terminate`) |
| 游离卷 | 0 | 0 |
| 快照 | 1(`snap-0ad026b386c804288`,160GB,唯一常设成本) | 同 |
| EIP | 0 | 0 |
| open/active spot 请求 | 0 | 0 |

**无泄漏。** 06:11Z 那四台开工时已全空(08:15Z 已确认是跑完 `--games 22` 自然收工,非看门狗)。

## 10. 固定栏位

`soak/` **168** / `dem21/` **20** / `unattributed/` **0**(前缀不存在)/ off-roster **0** /
`validation/` 最新条目仍 **2026-07-23**(标准路径走 `recover_verdict.py`,陈旧是**预期**)/
远端 main tip 开工 `3a57b6c`。
(本波四个 S3 前缀此刻还没建 ⇒ 168/20 未变,是**预期不是异常**。)

## 11. 验证(铁律 6)

本会话**未改 Lua** —— 改动仅 `iterations/` 下的报告/章程/queue.json,对 bot 行为零影响。
容器无 `luacheck`/`lua5.1`(已复核)⇒ 铁律 6 无适用对象。
唯一推断项是「三波共 ~$4.5 尚未入账」,已在 §2 挂在 refresh 时刻上给出**可证伪形式**。

## 12. 跨组

**追评 GH #140**(不新开 —— `list_issues` 核过 #135/#137/#141/#143/#144,没有一条拥有「W3 排期」
这条线,而 #140 正是 §AT/§AU 裁定所在的活线程):启动记录 + §AU.3 链有效实测 + §8 的三claimant冲突。
`queue.json`:`strategy-4`/`strategy-5`/`hero-8` **三条各加同一段结构性附注**(状态都留 `pending` ——
它们等的是裁定不是波次)。

**铁律 9 连带规则的交棒**:本波的下一棒是「约 14:1xZ 收割 + 按 §AT.1 判读」,由下一轮批测台接;
W3 的下一棒**已显式交给总监**(GH #140 §4,带截止时刻 18:09:32Z 和未裁默认),不落在报告里。

## 13. 下一波

**最早 2026-08-23T18:09:32Z**(但 **14:1xZ 先有一轮收割**,不启动)。
预置:**串看总监对 §8 的裁定**(未裁默认 `campgrade` 独占);**独占波按 §AU.3 钉发波时刻的
`origin/main` tip,不钉 `7159ff3`**(独占波无跨波配对义务);种子按 §AU.3 可用 axe+lion 新种子集
(`seed_draft.py --find axe,lion` → 974/986/1024),来不及登记则退回 888/895/896/906,**不影响判读**;
4 台 × 1 种子 on-demand、16 槽、`--rec-slots 12`、2h 看门狗、`--games 22` 不再上调;
**发波前必跑 `check_armed_wiring.py`,非 0 不发**(本轮起是流程的一部分)。

**必查项 10 条**沿用 08:15Z §9 + 10:15Z 第 10 条,本波的具体期望值:
- **① 四个 run 的暖场 stamp 应全部 = `7159ff3`**(main 已漂到 `3a57b6c`;任何一台不是,
  说明钉 SHA 这条路有洞,当场查);
- **⑤ grep 必须锚 `\.dem(\.gz)?$`**(否则 `*.demclaim.json` 被数进来);
- **⑨ 英雄普查分母表**(四个焦点英雄一个不落,**不交就是掉棒**);
- **⑩ 逐 run 核对 armed stamp 里没有 `campgrade`/`cmboots`/`pullbeat`** —— 三个都在 `7159ff3`
  之后才有调用点,意外入串则在这棵树上是 no-op,读数当场作废重发。

## 14. Token 用量(铁律 8)

```
TOKENS total_in=2,853,245 out=17,959 turns=30
```
(统计时刻为止;此后的收尾回合不计入。)
