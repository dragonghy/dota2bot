# 批测台 2026-09-14T15:08–15:5xZ(持有第十八轮)

**一句话**:零发波、零收割欠、零泄漏,本轮 AWS 新增计费 **`$0.00`**(EC2 `$0.00` + CE `$0.00`)。
工作单元 = **`gh779_accrual_keyed_ce_bypass` 欠条落地**(executor 逐字 `batch-desk`,零 AWS 成本)。
⭐⭐⭐ 头号:**新落地的那道判据,在本轮的真账号上第一次跑就走了「不买」那条分支** ——
`α=0 / β=0` ⇒ 冻结的戳不产生盲区 ⇒ `$0.00`。这正是裁定要买的行为,而**它是被现跑证明的,不是被设计声称的**。

---

## 一、成本(现跑)

`AWS_SETUP_EXIT=0`(容器已有 `aws-cli/1.46.1 Python/3.11.15 botocore/1.43.62`;身份逐字
`arn:aws:iam::581436534924:user/dota2bot-agent` + `(restricted batch-runner user — correct)`)。
`COST_EXIT=0`(⛔ 未走管道读码)。

- MTD **`$89.858`**;`forecast 196.979` / `budget limit 100.0`;
  `budget refreshed 2026-09-14T10:54:23Z`。
- **headroom 对 `$90` 刹车 = `$0.142`**。
- ⚠️ **本轮读数与上一轮(12:15Z)逐位相同**,戳也逐位相同 ⇒ **快照自 10:54:23Z 起又冻住**,
  跨度 ≈ 4.2h。⛔ **因此本轮没有「新支出测量」**,见 §六(通知判据)。

**GH #801 那四行整块照抄**(与 `RULE6_BYPASS` 同规格):

```
   >= $35, but headroom to the $90.00 brake is $0.142
   < $1.10 (cheapest wave) — CE confirmation SKIPPED, NOT passed (GH #801).
   Nothing can launch at this MTD, so confirming it buys nothing.
   Resumes by itself as soon as headroom >= $1.10.
```

⚠️ **这是 SKIP 不是 pass** —— 本轮 MTD **没有**经过 CE 复核,`$89.858` 只有 budgets 一个来源。
⛔ **未用 `COST_CONFIRM_AT=999` 自救**(章程步骤 2 明禁)。

---

## 二、闸(裸读,逐字)

**(i) `THROTTLE_EXIT=0`**:`anchor wave : W69 (4 machine(s))` /
`last machine up : 2026-09-12T09:26:31Z  <- the anchor (GH #469 ruling 1)` /
`slate spread : 7s` / `unlock : 2026-09-12T15:26:31Z` / `margin : +171700s past unlock`。

**(iii) `--planned 1.10` ⇒ `FENCE_EXIT=3`** —— **连续第十七轮是闸拒绝,不是本台的处置**:

```
actual (MTD)     : $89.858   <- re-read this run, never cached
pending waves    : $0.000   (launched, may not be in MTD yet)
planned          : $1.100
projected total  : $90.958
fence            : $100.00   <- lowest ACTUAL alert not yet crossed (Ruling 1)
brake            : $90.00   (owner's line, not derived here)
operative ceiling: $90.00   = min(fence, brake)
A launch at this instant would put MTD past the $90.00 BRAKE line.
WAVE_FENCE: THROTTLED (exit 3) -- brake, not fence.
```

**RULING 5/6/7 的三行按规矩逐字抄**(⛔ 只抄 `accrual check :` 不算):

- `accrual scope    : 17 region(s) read [...] <- COMPLETE: every region this account has enabled`
- `wave accrual     : records after 2026-09-13T23:36:23Z (= 2026-09-14T10:54:23Z - 11.3h lag, clock from budget snapshot)`
  ⇒ **`clock from budget snapshot`**,不是 `clock from now (...)` ⇒ GH #692 的 epoch 修复本轮仍在力。
- `accrual check    : CERTIFIED (0 accruing instances account-wide, read this run) -- pending $0.000 is a reading, not a default.`

**`crossing registry:`(RULING 11 要求每轮抄一行)**:
`0 record(s) at .../iterations/director_rulings.json, none in force -- the gate runs at the DERIVED fence.`
⭐ **本轮比上一轮多出后半句,而它正好回答了上一轮交棒 ④**:
`1 retired record(s) archived in "_retired" (most recent GH#754/director-20260911 (RULING 15)) -- "none in force" is not "none ever issued"; why a ceiling went away is recorded there, not missing.`
⇒ 上一轮登记的「`$85` 那条读作不在册,本台不代填、不追问是到期还是被撤」**现在有答案了,而且是工具自己给的**:
它被**退休并归档**,原因写在 `_retired` 里。⛔ 本台仍不代填、不复活,只登记这条棒可以收了。

---

## 三、⭐⭐⭐ 工作单元:`gh779_accrual_keyed_ce_bypass` 落地

**为什么是它**:`executor` 逐字 `batch-desk(批测台)`;`trigger` 逐字**「下一次发波之前」**,
且自带一句 ⛔ **「不许等到发波那一轮再写」**,理由写在行里 ——
**它读起来永远不急**(刹车红着的每一轮后果都真的是零),而它到期的那一刻(第一波起飞)
恰好是**最没空写代码**的一刻。⭐ **与 `hero27` 同族:刹车挡不住它,它一分 AWS 钱不花**
⇒ ⛔ 不得以闸 (iii) 红为由顺延。

### 3.1 判据(总监 2026-09-14T09:5xZ 裁定第 2 条,原样落码)

```
budget refreshed 戳 FROZEN
  AND ( (α) 活体普查读到非零  OR  (β) 有波次记录落在 11.3h lag 窗口内 )
⇒ 才允许花 $0.01 走 CE 直读 MTD
(α)(β) 皆零 ⇒ 冻结不产生盲区 ⇒ 不买
```

**三个要件逐条落地并各带一个分离性测试**(§3.3):非零普查 / 11.3h 窗口 / 皆零则不买。

### 3.2 产物

- **新** `tools/batch_test/aws/accrual_probe.py`(退出码 `0 = 该买 / 1 = 不买 / 2 = 没跑成`)。
- **改** `tools/batch_test/aws/check_costs.sh`:在 GH #801 的 skip 分支里问这道判据;
  `2` 与 `0` 都走付费(见下),`1` 保持 #801 原样的四行 SKIP。
- **改** `tests/test_check_costs_confirm.py`:35 checks → **59 checks**。

**⛔ 11.3h 与普查都不在本台重打一份**:`read_wave_accrual()` 与 `read_accruing_instances()`
都从 `wave_fence.py` 导入 —— **那就是闸 (iii) 自己**,于是仓里只有一个
「这笔钱可能还没落进 MTD」的定义,而不是第二份会**朝便宜那侧**漂的拷贝。
钉在 `8h`(AST 断言:代码里没有 `11.3` 这个浮点字面量,也没有人给那个常量名赋值)。

**⛔ 一个字都没挂到「冻结时长 ≥ N 小时」上**(裁定明禁)。「冻结」在这里是**比较**:
本轮的戳 vs 本探针上次见过的戳。理由不是口味 —— **冻结时长正是本台四轮前自己撤回的那个量**,
把一个已撤回的读数放进触发器位置,是 `pullcad` 同族的错误。钉在 `8g`。

**失效方向与刹车那条对齐**:普查读不出来 ⇒ **付费**,因为
**一个读不出来的仪器永远不许把复核静音**。逃生门 `COST_NO_ACCRUAL_PROBE=1` 自打
`NOT CONSULTED ... that is a SKIP, not a finding of 'nothing accruing'`(钉在 `8e`)。

### 3.3 ⭐⭐ 变异台:两个存活的变异体各自暴露了一条写错的断言

⛔ **不是「测试写完就绿」那种交付**。每个变异体都按证据纪律第 2 条处理
(**变异体存活时先怀疑断言,不是怀疑变异体**),两次都是**断言错了**:

| 变异体 | 首跑 | 处置 |
|---|---|---|
| M1 always-bypass | 逮住(8 条) | — |
| M2 never-bypass | 逮住 | — |
| M3 忽略戳 | 逮住(`8c`) | — |
| **M4 scope-blind** | ⛔ **存活** | 见下 (甲) |
| **M5 忽略 (β)** | ⛔ **存活** | 见下 (乙) |
| M5b 忽略 (α) | 逮住 | — |
| M6 丢掉 epoch 强制 | 逮住(12 条) | — |
| **M7 新容器静默不买** | ⛔ **存活** | 见下 (乙) |
| M8 删掉「皆零不买」 | 逮住(10 条) | — |

**(甲) M4 存活 ⇒ 发现一条真缺陷,不只是一条弱测试。**
`read_accruing_instances()` 在**每个区域都失败**时**不抛异常** —— 它返回**空列表 + 不完整 scope**,
在调用点与「账号是空闲的」**逐字节同形**。读成零就走进「皆零不买」,
而那恰好是账号可能正在**看不见地烧钱**的那一次。
⇒ 探针新增一道 scope 守卫:**「scope 内为零」不是「为零」**,判 `UNCERTIFIABLE` 并付费。
⚠️ **我的 `8d` 第一版是错的**:它喂的是**非法 JSON**,那会在 `parse_instances` 里抛异常、
走通用异常分支 `exit 2` ⇒ **把 scope 守卫整条删掉仍然全绿**。
**到达守卫的是「调用失败」不是「返回畸形」**,改成失败之后 M4 被逮住。

**(乙) M5 与 M7 存活 ⇒ 两条支路从来没有被走过。**
- **M5**:(β) 那条析取支**一个测试都没碰过**(其余全在驱动 (α))。
  补 `8j`(窗口内有波 ⇒ 付费)+ `8k`(同一条波挪到窗口外 ⇒ 不付费)——
  `8k` 是**防止 `8j` 靠「只要有个 waves 目录就过」蒙混**的那一半。
  为此在 `check_costs.sh` 加了 `COST_WAVES_DIR` 测试缝:否则 (β) 只能靠
  **真的在过去 11.3h 里发过一波**才能到达,即整条支路不可测。
- **M7**:新容器(无历史戳)那条路没测过,**而每一个 Routine 会话都是新容器**。
  补 `8l`(无戳 + α 非零 ⇒ 付费,且打印 `fresh container` 而不是谎称 `FROZEN`)
  与 `8m`(无戳 + 账号空闲 ⇒ **仍不付费**)——
  `8m` 是**防止「未知=冻结」变成一扇后门**、让每个会话的第一轮都买一次读数的那一半。

**收尾复跑:9 个变异体全部被逮住;`diff` 确认变异台已从文件拷贝还原干净(`RESTORED_CLEAN`)。**

### 3.4 顺带修掉一个已经红了的门

`--help` 的 `sed -n '2,43p'` 在本轮改动**之前**就已经随 header 增长而失准,
`7b: --help actually prints the usage block` 当场红 ⇒ 改为 `2,71p`。
⭐ **这条是那个测试自己抓的**,而它的注释里写着上一次同样的事(`2,26` 停在半个 header 里)
⇒ **同形第三次**,已在源码注释里点名该断言,让下一个改 header 的人被测试挡住而不是靠记性。

### 3.5 真账号上第一次现跑(零成本)

```
   accrual probe (GH #779): alpha = 0 accruing instance(s) account-wide; beta = 0 wave record(s) after 2026-09-13T23:36:23Z
   stamp unknown (fresh container) -- alpha and beta are both zero -- a frozen stamp over an idle account opens no blind spot
```

⇒ 走「不买」分支,`CE $0.00`。⭐ **注意这一行同时是 (乙) 的现场佐证**:
戳读作 `unknown (fresh container)`,而**皆零条款先答**,所以新容器**没有为自己的新鲜付费**。
`cutoff 2026-09-13T23:36:23Z` = 快照 `10:54:23Z − 11.3h`,逐位对得上。

---

## 四、收割零欠 / 泄漏四条独立路径全零(⛔ 不合并成一句)

**收割**:`awsx s3 ls .../validation/ --recursive` **`S3VAL_EXIT=0`**(⛔ 重定向后裸读码),
**592 对象**与上一轮基线**逐位相同**,最新对象仍是
`tpcommit+46ids-24c30245be01_20260907_2218_run.log`(`2026-09-07 22:18:52`)
⇒ **零新对象、零下载、零 S3 支出**;W68/W69 前几轮均已收割,**本轮未新增任何 verdict**,
`recover_verdict.py` 未跑。

**泄漏**:
① `check_costs.sh` 自带 `running/pending instances` 区块**空**;
② `describe-instances` 五态、**不加 tag 过滤** ⇒ **零行**(`INST_EXIT=0`);
③ 闸 (iii) 自己的账户级普查 ⇒ `CERTIFIED (0 accruing instances account-wide)`,**17 个区域 COMPLETE**;
④ AMI 仍只有 `ami-0a990a26d89c66547 / dota2bot-batch-20260718` 一张(常设成本,非泄漏)。

---

## 五、⛔ 铁律 6 三条腿 + trunk 红(第一手,非转述)

`ARM_EXIT=0`;`GATE_EXIT=0  CLEAN (iron rule 6 static half passed)` / `luacheck bots game: 0 warnings`;
`PY_GATE_EXIT=0` —— `py gate: 87 ran, 0 findings, 0 uncertifiable, 10.8s`。
⛔ **未用 `RULE6_BYPASS`** ⇒ 本报告无「SKIPPED, not passed」行。

**⚠️ 一条要登记的作用域事实**:本轮写的那个测试
`tests/test_check_costs_confirm.py` 在 `py_gate_manifest.json` 里是
`"in_gate": false, "reason": "over_cumulative_budget"`(实测 1.611s → 本轮 **2.38s**,因为多了 24 条断言)
⇒ **推我这笔改动的闸不会跑我这笔改动的测试**。
⭐ 这正是 GH #624/#616/#806 反复立案的那个形状,**本轮是它的一个新实例,且方向对本台不利,照登**。
⛔ 本台**不自行调整闸的成员资格**(那要重测,而总监的 `lua_gate_baseline_e2e` 行已明令
「⛔ 不许把『在慢容器上量』当成本行的执行」,同族约束)。已交棒。
**代偿**:本台**另跑了认领它的那一半** —— `tests/run_py_tests.sh` 里
`PASS  tests/test_check_costs_confirm.py`。

**⛔⛔ `origin/main` 有两条 python trunk 红,⭐ 均非本轮引入,且本台第一手复核而不是转述**:
自检 python 腿打 `TRUNK RED -- a python test is failing ON THE WORKING TREE.`,
它自带的限定逐字是 `Whether main is red too is NOT established by this line: re-run after git stash.`
⇒ **本台用 `git worktree add /tmp/trunkcheck origin/main` 独立复跑了这条限定**
(⛔ 不用 `git stash`:自检若死在 stash 与 pop 之间会扣住未提交的工作):

| 测试 | 工作树 | **`origin/main` 净树** | 归属 |
|---|---|---|---|
| `tests/test_pending_rulings.py` | `837 checks, 1 failed` | **逐字相同** | 总监(queue 行) |
| `tests/test_selfcheck_lua_leg.py` | `49 checks, 1 failures, 9 uncertified` | **逐字相同** | 总监([harness]) |

- 红 1 逐字:`FAIL: rows in the OTHER bucket that declare a rideshare in their own prose -- §BB.4's 'rule it this round' cannot fire for these: [('hero-82', ['零 EC2'])]`
- 红 2 逐字:`4c2: the uncertifiable path does NOT raise 3 -- an absent interpreter is not a failing test`

⇒ **两条在净 `origin/main` 上逐字复现 ⇒ 本轮 `bots`/`game` 一行未改,无一条 trunk red 由本轮引入。**
worktree 已 `--force` 移除(`WT_REMOVED=0`),工作树未被触碰。

---

## 六、通知判据:本轮**不推**,判据如此

上一轮(12:15Z)已就 **`$89.858` / headroom `$0.142`** 推过 owner 通知并发过 GH #779 评论。
本轮 MTD 与戳**两个量都与上一轮逐位相同**(快照自 `10:54:23Z` 冻住)
⇒ **零新支出测量** ⇒ ⛔ **既不推通知也不发新评论**,与 06:13Z / 09:11Z 两轮同一条判据。
⚠️ **重复投递同一个读数会稀释下一次真读数的信号**,这正是本台 18:13Z 立「停止同形投递」的理由。
**三条选项逐字不变**(维持 `$90` / 给 `dota2bot-batch` 加成本分配标签过滤器 ——
`budget filters : none`,外来机型占 MTD 的 41.8% / 一次性解禁到某个数并说明是否含外来支出);
已挂 `DECISIONS_NEEDED.md §15`,⛔ 不重复立条。

---

## 七、铁律 9 优先项

`OWNER_PRIORITIES.md` **P4.1 upstream 标尺波**连续第十八轮欠,
⛔ **唯一阻因是闸 (iii) `exit 3`(刹车),不是本台未认领** ——
`$0.142 < $1.10`,它同样要过 (iii)。棒已在 `DECISIONS_NEEDED.md §15` 与 GH #779 上,本轮不重复。

---

## 八、⛔ 本会话 `bots`/`game` 一行未改

产物:`tools/batch_test/aws/accrual_probe.py`(新)、`tools/batch_test/aws/check_costs.sh`、
`tests/test_check_costs_confirm.py`,以及 `iterations/` 下的本报告与章程条目。

---

## 九、自查

⚠️ **管道坑第 30 次**:开工第一条命令仍是 `routine_selfcheck.sh 2>&1 | tail -60`,
脚本自卫逐字 `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.`
(自带 `it has recurred 5x, every time as the first command of the round.`)。
⚠️ **并且紧接着踩了第二道**:改成重定向之后**带了 `timeout`**,脚本第二次自卫,逐字
`REFUSED: routine_selfcheck.sh is running under timeout; exit 2, nothing checked.`
+ `Measured 2026-09-14: EXIT=124 ... the whole fast-Lua detector leg never ran`。
⇒ **上一轮交棒 ⑧ 的三件事,(1)(2) 本轮各犯一次,(3) 做到了**(起跑前确认过没有上一次的自检活着)。
⭐ **两次都零损失,因为拦住它们的是工具不是纪律** —— 这正是那两道自卫存在的理由;
第三次才用对:`nohup bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1 &`。
⭐ **此后每一个读退出码的命令都重定向到文件后裸读,零管道**
(`AWS_SETUP_EXIT`/`COST_EXIT`/`COST2_EXIT`/`THROTTLE_EXIT`/`FENCE_EXIT`/`S3VAL_EXIT`/
`INST_EXIT`/`GATE_EXIT`/`PY_GATE_EXIT`/`TPR_TRUNK_EXIT`/`TSL_TRUNK_EXIT`)。

**铁律 11**:零 `requires approval`、零权限对话框、零空转(自检与慢 python 套件都后台跑,
成本/闸/收割/泄漏/工作单元并行推进)。

---

## 十、交棒

① ⭐⭐⭐ **owner —— 刹车第十八轮持有,GH #779 第十四轮零表态**;
⚠️ 本轮**零新支出测量** ⇒ 既未推通知也未发新评论(判据见 §六)。三条选项逐字不变。
⛔ 本台不建议、不预选、不自行改线。

② ⭐⭐ **总监 —— 验收 `gh779_accrual_keyed_ce_bypass`**:`done_when` 是
`path_contains_all(check_costs.sh, ["accrual","accruing"])`,**本轮两串均已出现**
(⚠️ 该判据自带 LIMIT 逐字「买的是『脚本里出现了 accrual 这条腿』,**不是**『这条腿写对了』」)
⇒ **对不对请读 diff**,三个要件在 §3.1,分离性测试与变异台在 §3.3。
⭐ 请特别看 **(甲)**:落地过程中发现 `read_accruing_instances()` 在**全区域失败**时
返回空列表而非抛异常 ⇒ 「scope 内为零」会被读成「为零」。本台在**探针侧**加了守卫,
⛔ **没有改 `wave_fence.py`** —— 闸 (iii) 自己是否也有同一个读法,**归总监**,本台不碰扛钱的那条路径。

③ ⭐⭐ **总监 —— `origin/main` 两条 python trunk 红,本台已第一手在净树上复现**(§五表格),
均非本台产物且本台不代修(先例 GH #33):
`hero-82` 那条是 queue 行的措辞与 §BB.4 的冲突;`4c2` 是 [harness]。
⚠️ **两条都不在 py gate 的作用域里** ⇒ 推它们进来的那次闸是绿的且是对的
—— **GH #624/#616/#806 的立案形状,本轮第三例**。

④ **总监 —— 上一轮交棒 ④ 可以收了**:`crossing registry` 本轮多打的后半句自己回答了它
(`$85` 那条 = **RULING 15 / GH#754,已退休并归档在 `_retired`**,不是丢失)。§二。
⛔ 本台不代填、不复活。

⑤ **总监 —— py gate 成员资格**:`tests/test_check_costs_confirm.py` 因本轮加的 24 条断言
从 1.611s 涨到 2.38s,仍 `in_gate: false / over_cumulative_budget`(§五)。
⛔ 本台不自行调整成员资格;若要重测,请注意 `lua_gate_baseline_e2e` 行那条
「⛔ 不许把『在慢容器上量』当成本行的执行」对 python 侧同样适用(本容器 `5a0` 仍在 120s 预算上截断)。

⑥ **总监 —— 06:13Z / 09:11Z 的历史交棒(冻结时长定级、(乙)(丙) 两条欠裁、`w55_record` 卡在
`done_when: manual`)本轮未收到回应,逐字保留。**⛔ 本台不自行退休任何一行。

⑦ **下一轮本台**:1. ⛔ owner 对 GH #779 表态前不发任何波;
2. 开工第一条命令 **`nohup … > /tmp/sc.log 2>&1 &`** —— ⛔ 不走管道、⛔ 不加 `timeout`
(本轮 (1)(2) 又各犯一次,见 §九);
3. 通知/评论判据不变:**戳变了且 MTD 变了**才推;
4. 若刹车解除,`gh779_accrual_keyed_ce_bypass` 本轮已落地 ⇒ 发波那一轮**不必再写代码**,
   但请**现跑一次**并把探针那两行抄进报告(它到那时才第一次走「该买」分支)。

---

## 十一、token 用量

```
TOKENS total_in=13,928,675 out=61,021 turns=84
```
⚠️ 这是「到统计时刻为止」的数字,统计后的收尾回合(commit/push/补记)不计入。

---

## 补记(push 之后,GH #290 顺序)

**(A) 铁律 6 三条腿 —— push 时钩子自打的权威读数**(三次 push,逐字一致):

```
luacheck bots game: 0 warnings
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 87 ran, 0 findings, 0 uncertifiable, 10.6s
lua gate: 375 ran, 0 findings, 0 uncertifiable, 8 unanswered, 7 known-red, 570.8s
```

⛔ **未用 `RULE6_BYPASS`** ⇒ 本报告**没有**「SKIPPED, not passed」那一行。
⭐ **lua gate 本轮是真跑不是作用域判定**(上一轮是 `SKIPPED BY SCOPE`,因为那轮没碰
`bots/game/tests` 路径;本轮改了 `tests/` ⇒ 375 个测试真跑了 570.8s)。
`8 unanswered` / `7 known-red` 是它自带的两栏,⛔ 不读成 findings。

**(B) ⚠️ push 竞态一次,如实登记**:`PUSH2_EXIT=1`,逐字
`! [rejected]  HEAD -> main (non-fast-forward)` —— 协同组的 `94242ade` 在本台跑闸的
~10 分钟里落了 main。按章程 `git pull --rebase origin main`(`REBASE_EXIT=0`,`Rebasing (1/1)` 无冲突)
后重推 `PUSH3_EXIT=0`(`94242ade..bf3a067d HEAD -> main`),
会话分支随后 `--force-with-lease` 对齐(`PUSH4_EXIT=0`,`7934405f...bf3a067d`,仅本台自己的分支)。
⭐ **落地已独立复核,不靠 push 的回显**:`git merge-base --is-ancestor bf3a067d origin/main` ⇒ **真**,
其时 `origin/main` 已被别的组推到 `9fe7bf9b`(即本台的 commit 在 main 的历史里,而不是 main 的尖端)。

**(C) 慢 python 套件(`tests/run_py_tests.sh`,闸外那 47 条所在的那一半)**:
`PASS  tests/test_check_costs_confirm.py` ⇒ **本轮写的测试在认领它的那条腿上跑过且绿**
(§五 登记的「推我这笔改动的闸不会跑我这笔改动的测试」的代偿)。
全套两条 `FAIL` 逐字就是 §五 表格里那两条(`test_pending_rulings.py` / `test_selfcheck_lua_leg.py`),
**与净 `origin/main` 逐字相同** ⇒ **无一条由本轮引入**。

**(D) 自检(铁律 10)**:本工作单元收尾时**仍未打出收尾横幅**
(`grep -c "selfcheck worst exit" /tmp/sc.log` = **0**,停在 `trunk health (fast Lua detectors)` 腿上)
⇒ ⛔ **`SELFCHECK_EXIT` 本轮未知,不读成通过**,⛔ 也不空转等它。
⭐ 但它最有价值的那条读数已落地**并被本台独立复核**:python 腿的 `TRUNK RED` 由
`git worktree` 在净 `origin/main` 上逐字复现(§五),**不是孤证**。
⚠️ **harness 的后台任务通知若报 `exit code 0`,那不是自检的退出码** —— 同形第三次,⛔ 下轮不许照抄。
`cadence` 腿本轮 **8 条 GAP**(`director`×2 / `replay-check`×2 / `strategy`×3 …),
⭐ `grep -cE "^GAP +cadence batch-desk"` = **0** ⇒ **无一条是批测台**。

---

## ⛔⛔ 补记 (E) —— 自检跑完了,**同轮更正补记 (D) 自己**

(D) 说「`SELFCHECK_EXIT` 本轮未知」——**那只在当时为真**。自检在补记 (A)–(D) push 之后跑完,
**有真读数**,逐字:

```
legs run              : 13
FINDINGS (exit 3)     : unlanded cadence queue-rulings owed-executions lua-coverage trunk-red(python)
UNCERTIFIABLE (exit 2): none
NOT RUN (inside a leg): tests/test_lua_gate.py tests/test_luacheck_gate_soakswitch.py
selfcheck worst exit: 3
```

⭐ **更正方向对本台不利(未知 → 有发现),照登。** ✅ (D) 里「不把没跑完的腿读成通过」
「不空转等它」两句未被推翻。
⭐ **`UNCERTIFIABLE: none`** ⇒ 与上一轮判开:本轮 `trunk-red(python)` 是**真红不是没跑完**,
与 §五 本台在净 `origin/main` worktree 上的第一手复现**独立同向**。

### ⭐⭐ 与上一轮相比多出来的那条:`unlanded`

上一轮 FINDINGS 是 `cadence queue-rulings owed-executions lua-coverage trunk-red(python)`,
**本轮头一个是 `unlanded`** —— 那正是铁律 10 立条时的那个形状(08-22 hero 整棵树留在会话分支上,
检测器点名点得到它,没人跑,10:00Z 花整整一轮从零重做同样的 8 个文件)。

**三条,全部是协同组的,⛔ 无一条是批测台的**(`origin/claude/intelligent-hopper-2s810x`,均 `2026-09-14T14:40:41Z`):

| commit | 标题 |
|---|---|
| `d92ba19` | `strategy: the recover-TP branch fires on a destination it never chose (gated 'tpstale')` |
| `bb70c8d` | `strategy: backfill this round's real push-gate readings into the report` |
| `862dfdb` | `strategy: prove the tpstale stand's restore with sha256sum, per the house rule` |

⭐ **本台没有转述这个读数,而是当场独立复核**:`git fetch origin main` 之后
`git merge-base --is-ancestor <c> origin/main` 对三条**逐条现跑** ⇒ **三条都 `STILL UNLANDED`**
(此时 `origin/main` 已是 `1c87208c`,即含本台本轮两笔)。
⚠️ **诚实边界,照抄工具自己的 LIMIT 不加强**:`CLAIMS-LANDED : 0`,且工具**按 patch-id 排除了 rebase 双胞胎**
—— 但那只排除**逐位相同**的重做,**不排除改头换面之后已经落地的同一份工作**。
⚠️ 特别值得协同组自己看一眼的是 `bb70c8d`:main 上**有一条同名 commit `b71e2998`**
(本台 rebase 时正好经过它)⇒ **标题相同而 patch-id 不同**,两种可能(重做时内容变了 / 真的掉了一块)
**本台分不开,也不替它分**。
⚠️ 另:该腿自报 `shallow clone : YES`、`REFUSED refs : 1160 of 1184`、
`REFUSED below : 2026-09-14T01:08:02+00:00 (65a5892)` ⇒ **它只覆盖 graft 点以上的 3 天**,
**「只报出三条」不等于「只有三条」**。

⇒ **交棒 ⑧(新增)**:**协同组 / 总监 —— 这三条请自查是掉棒还是已改头换面落地。**
⛔ 本台**不代 cherry-pick**(那是别组的工作树与别组的判断),只把读数交出去。
⭐ 协同组同轮的 main 上有一条 commit 标题逐字是 `record GH #821 and the two main-push races`
⇒ **它自己已经知道本轮撞了两次 push 竞态**,这三条很可能就是那两次竞态的残留。

### 顺带一条零成本读数,交给总监(⛔ 本台不退休任何行)

`selfcheck_recursive_fork_amplifier` 那一行要求「谁退休它,谁必须同轮贴两个读数」,其 (i) 是
**跑完一次完整自检之后** `pgrep -c -x -f 'bash tools/agent/routine_selfcheck.sh'`。
本台本轮正好满足那个前提条件,现读 = **`0`**。
⛔ **这不是退休依据**:该行 `executor` 是总监,`done_when` 钉的是
`tests/test_selfcheck_pipe_guard.py` 里的 `killpg`(本轮仍 **0** 命中),
而且该行自己写明「**冷容器上的绿不算**」。⇒ 只作为一个**日期戳过的读数**登记,供总监裁时取用。

**⇒ 已开 GH #823 `[strategy]`** —— `三条 0NEXT13 的 commit 推了但没落 main(intelligent-hopper-2s810x),疑为本轮两次 push 竞态的残留`。
⭐ 三条限度(patch-id 只排除逐位相同的重做 / `bb70c8d` 与 main 上 `b71e2998` **同名不同 patch** /
shallow clone 只覆盖 graft 点以上 3 天)**一条不漏地写进了 issue 正文**,⛔ 没有把「问题」写成「判决」。
⛔ 本台不代 cherry-pick。**铁律 11:MCP 本轮可用,零 `requires approval`。**
