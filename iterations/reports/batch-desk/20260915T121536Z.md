# batch-desk 2026-09-15T12:15:36Z — 刹车第二十五轮持有;把三周没写出去的那本台账**接到收割链上**

**一句话**:零发波、零收割欠、零泄漏;⭐⭐⭐ 本轮的工作单元不是成本那几行,是欠条
`games_ledger_cross_wave_accounting` —— 它掉了三周,**而掉的原因不是谁失职,是它的载体一直是散文**
(不属于任何一波、任何一个 id ⇒ 任何一张波次局部的收割清单里**都没有行可以缺席**)。
⇒ 本轮**换载体**:`recover_verdict.py --ledger`,收割这一步本来就把每个逐局文件解析了一遍,
现在它顺手把行写出去。⛔ **欠条本身仍是 OWED,本报告不记成已结清**(针脚要一局九月的比赛,
而九月逐局数据只在 S3 上)。

---

## 一、成本(当轮现跑,⛔ 下轮不得抄作前提)

`AWS_SETUP_EXIT=0`(容器无 CLI,自装 `aws-cli/1.46.1 Python/3.11.15 botocore/1.43.62`;
身份逐字 `AWS ready: arn:aws:iam::581436534924:user/dota2bot-agent` + `(restricted batch-runner user — correct)`)。
`COST_EXIT=0`(⛔ 未走管道,重定向后裸读)。

MTD **`$90.569`** / `forecast 186.702` / `budget limit 100.0` / `budget refreshed 2026-09-15T04:18:45Z`;
**headroom 对 `$90` = `$-0.569`**。

⚠️ **戳与 MTD 与上两轮(06:13Z / 09:12Z)逐位相同 ⇒ 快照已连续第三次读到同一张**
(冻结跨度现测 ≥ **7.9h**,04:18:45Z → 12:15Z)。⛔ **本轮登记的是「没测」不是「没涨」** ——
步骤 2 乙禁用「MTD 没动 ⇒ 没花钱」的反推,本轮照禁。⚠️ 这张快照**不覆盖** 04:18:45Z 之后的任何支出。

**GH #801 四行整块照抄(⛔ 未用 `COST_CONFIRM_AT=999` 自救,用默认值跑)**:

```
   >= $35, but headroom to the $90.00 brake is $-0.569
   < $1.10 (cheapest wave) — CE confirmation SKIPPED, NOT passed (GH #801).
   Nothing can launch at this MTD, so confirming it buys nothing.
   Resumes by itself as soon as headroom >= $1.10.
```

⚠️ **这是 SKIP 不是 pass。** `accrual probe (GH #779)` 两行照抄:

```
   accrual probe (GH #779): alpha = 0 accruing instance(s) account-wide; beta = 0 wave record(s) after 2026-09-14T17:00:45Z
   stamp unknown (fresh container) -- alpha and beta are both zero -- a frozen stamp over an idle account opens no blind spot
```

**成本三段式(铁律 1 / RULING 48,本台第 2/3 轮)**:
**零 EC2 / 零 CE / S3 读取 0 个对象** —— 本轮只发 **LIST**(`s3 ls`,两次),
⛔ 无 `s3 cp`、⛔ 无 `s3 sync`、⛔ 未下载任何 `.dem` 或 `analysis.json`。
⚠️ 照 RULING 48 的口径,LIST 请求本身不是「读取对象」,故 `<N>` 记 **0**;
⛔ 这不等于「本轮 S3 一分钱没花」(LIST 请求自身计费 + 桶的存储持续计费),
**只等于本轮没有把任何对象的字节拉出 AWS**。

## 二、闸 (iii) —— 不发波的原因是闸,不是本台的处置

`python3 tools/batch_test/soak/wave_fence.py --planned 1.10` ⇒ **`FENCE_EXIT=3`**,逐字:

```
actual (MTD)     : $90.569   <- re-read this run, never cached
pending waves    : $0.000   (launched, may not be in MTD yet)
projected total  : $91.669
fence            : $100.00
brake            : $90.00   (owner's line, not derived here)
operative ceiling: $90.00   = min(fence, brake)
A launch at this instant would put MTD past the $90.00 BRAKE line.
WAVE_FENCE: THROTTLED (exit 3) -- brake, not fence. Stop and report to the owner; do not wait it out.
```

RULING 5/6/7 三行各自照抄:

```
accrual scope    : 17 region(s) read [...]  <- COMPLETE: every region this account has enabled
wave accrual     : records after 2026-09-14T17:00:45Z (= 2026-09-15T04:18:45Z - 11.3h lag, clock from budget snapshot)
accrual check    : CERTIFIED (0 accruing instances account-wide, read this run) -- pending $0.000 is a reading, not a default.
```

⭐ **钟没有降级**(epoch 解析正常)⇒ RULING 7 (甲) 的修法本轮仍有效。
`crossing registry: 0 record(s) ... none in force` + `1 retired record(s)`;`budget filters : none`;
告警 `$50 [ALARM]` / `$80 [ALARM]` / `$100 NOT yet crossed [OK]`。
⚠️ 照登不藏:`$90`–`$100` 之间在 **AWS 侧仍可花**,**挡住它的是 owner 这条线不是 AWS**。
⛔ 未传任何 `--no-*` / `--pending` / `--director-crossing` ⇒ **无一行「这是 SKIP 不是 pass」**。

⇒ **连续第二十五轮,零发波。**

## 三、收割 —— 零欠

`awsx s3 ls s3://dota2bot-batch-results-4924/validation/ --recursive`:**`OBJ_COUNT=592`**。
⭐ **与已登记的 592 逐位相同 ⇒ 无新 verdict ⇒ 收割欠为零。**(把关的是**对象计数**,RULING 47。)

**最新对象按 RULING 47 规定的取法读**(`--recursive | sort | tail -1`,⛔ 不是裸 `| tail`):

```
2026-09-12 10:18:26      15187 validation/lf_rescue+27ids-d177b87026dd_20260912_1018_run.log
```

⇒ 与 W69(09-12 收割)一致,**本轮无新数据**。

## 四、工作单元:欠条 `games_ledger_cross_wave_accounting` —— **换载体,不是再喊一次纪律**

### 4.1 现场(开工现测,不是转述)

`iterations/games_ledger.jsonl` **391 行**,`run_prefix` 最新 `soak/run_20260719_1353`;
`grep -oE '"game_id":"[0-9]{6}' | sort -u` 唯一值 **`202607`**。
同期 W62–W69 八波产出 **~1300 计分局** ⇒ **跨波的机器可读局数账从来没有存在过。**

⭐ **失效形状比一般的静默更隐蔽:没有台账也写得出效率台账。** 写的人当场从八个
`W6*_verdict.json` 里拼局数 —— 拼得出来,所以**没有人被挡住**;代价是**分母由每次拼的人当场决定**
(某周八个文件恰好同用 `scored_games`,那是运气不是保证),`$/有效局` 因此**三周不可比**。
📌 W35 记「棒掉了」、W36 记「棒在但没人捡」并主动不升级 —— **两次重申,两次都没改变结果**,
因为它**不属于任何一波、任何一个 id**,于是任何一张波次局部的收割清单里**都没有行可以缺席**:
逐条自审可以条条全中,台账照样是空的。

### 4.2 落地

**(甲) `tools/batch_test/soak/recover_verdict.py` 新增 `--ledger <path> [--ledger-wave W<N>]`。**
收割这一步**本来就**把每一个逐局 `analysis.json` 解析进了 `loaded`;现在它把行写出去。
⇒ 这根棒**没有地方可以掉**。

**(乙) `tools/batch_test/soak/append_ledger.py` 抽出 `row_from_analysis_obj(a, game_id, run_prefix)`**,
收割链复用**同一份行 schema**。⛔ 不许两份拷贝 —— 两份会漂移,而**行的形状中途变过的台账**
正是本欠条要消灭的那种不可比。

**(丙) ⭐⭐ 行的身份是 `(run_prefix, game_id)`,不是 `game_id`。**
逐局文件名 `<YYYYmmdd_HHMMSS>_slot<N>` **不带 run 标记**,4×1 波同秒起飞、slot 节奏相同
⇒ **同名跨 run 是常态不是意外**(GH #225 在文件系统层实测 **208 → 188**)。
按 `game_id` 去重会把那次丢失**原样搬进台账**,而且**照样静默**(台账没有期望局数可比)。
⚠️ `game_id` 字段本身**不变**(仍是裸 tag)⇒ 历史行与任何对它的 grep 照常可读。

**(丁) `$/有效局` 该读哪个字段**:每行新带 **`finished`**(= `winner ∈ {radiant,dire}`,
与 verdict 的 `unfinished` **同一谓词**)。
⚠️ **它不是 verdict 的 `scored_games`** —— 后者更窄(只数活过 `NO-PAIR` / `THIN-ARM` 的种子里的完赛局),
**两者相差真实局数**。⛔ **本台不替总监选定终局口径**:行里同时带 `seed` / `arm_side` / `cand` / `wave`,
⇒ **两种分母都能被事后重算**。而「每周静默地选一个、每周选的不一样」正是本条要终结的缺陷。

**(戊) 被 GH #225 拒绝的语料一行都不写**(写在所有 refusal 之后)。
一份收录了 verdict 拒收的局的台账,是**第二本互相矛盾的账**,比没有账更糟。

**(己) 无 `--ledger` 则一行不写**、verdict 里也不多一个 `ledger` 节 —— 接线**没有**让工具开始碰
调用方没点名的文件。

### 4.3 证据(⛔ 不是「测试绿了」)

`tests/test_verdict_ledger_write.py`:**20 checks, 0 failed**,全部驱动**真脚本**
(⛔ 无第二份 row schema —— 自带拷贝的测试会在两份同错时通过)。

**变异台(`evidence-discipline` 第 1/2 条:从文件副本恢复,⛔ 不用 `git stash`)——
四只全杀,零幸存**:

| 变异 | 预期红 | 实测 |
|---|---|---|
| M1 删掉 ledger 写入 | case 1 | **KILLED**(exit 1) |
| M2 按 `game_id` 单独去重 | case 3 | **KILLED** —— 逐字 `all 40 games land despite the collision (got 20)` |
| M3 `finished` 改读 `econ_winner`(永不为 null) | case 4 | **KILLED** —— 逐字 `(got 20/20)` |
| M4 把写入挪到 #225 refusal **之前** | case 5 | **KILLED** —— 逐字 `a refused harvest wrote no ledger rows at all` |

`BASELINE (unmutated): exit 0, 0 FAIL line(s)` / `RESTORED: exit 0, 0 FAIL line(s)` / `SURVIVORS: none`。
⚠️ **M4 的击杀带附带红**(该变异体把 `v["ledger"]` 自报行一并剥掉了,故 case 1 的两条自报检查也红)
—— ⛔ 不藏:**case 5 那一条自己单独红**,那才是被买到的断言。
⭐ 恢复后 `md5sum` 与副本**逐位相同**(`1c002e57…` / `9f5a0f4b…`),工作树未被变异残留污染。

**回归**:verdict 家族八个测试全绿 ——
`test_verdict_strata.py`(17)/ `test_verdict_arm_depth.py` / `test_verdict_ledger_write.py`(20)/
`test_verdict_name_length.py`(16)/ `test_verdict_pool_lossless.py` / `test_verdict_winrate.py` /
`test_verdict_winrate_channel.py` / `test_verdict_winrate_comps.py`,**各自 `EXIT=0`**(⛔ 逐个重定向后裸读,零管道)。

### 4.4 ⛔ 欠条仍是 **OWED** —— 本轮**没有**结清它

针脚逐字是 `path_contains_all` / `"game_id":"202609`。**现测仍 0 次命中。**
原因**不是**接线没做,是**九月的逐局数据只在 S3 上,本地只有聚合**
(`W69_harvest.json` 现读:`files_local_vs_s3: 120/120 per run, 4 runs` —— 那些本地目录在早已销毁的容器里;
仓库里的 `W6*_verdict.json` 只有 `per_seed` / `mean`,**没有逐局行**)。

⛔ **本台未为回填而下载**,理由是算术不是懒:RULING 49 (乙) 正让 RULING 48 的 `<N>` 跑满三轮再重测归因缺口,
而**为回填拉几百个对象会直接污染那次测量**(本轮恰是第 2/3 轮)。
⚠️ 欠条自己也写着「**不要求补录历史**,只要求从下一次收割起收割的局进账」。
⇒ **针脚由刹车解除后的第一次收割自己打上;线已经接好,不再依赖谁记得。**

## 五、泄漏检查

启动前后 `check_costs.sh` 的 `== running/pending instances ==` 节**为空**;
`accrual probe` 独立复核 `alpha = 0 accruing instance(s) account-wide`(17 个区域全读)。
常驻成本仍只有 AMI `ami-0a990a26d89c66547`(`dota2bot-batch-20260718`)+ 其快照。
⇒ **零泄漏,零该终止而未终止的实例。**

## 六、通知判据:**本轮不推**

上一轮(09-14T21:22Z)已就 `$90.569` / 归因 / 区间向 owner 推过**两条**,并在 GH #779 留评。
本轮 MTD 与预算戳**两量逐位相同** ⇒ **零新支出测量**、owner 面前的二选一**没有新事实**
⇒ ⛔ 既不推通知也不发新评论(与 06:13Z / 09:11Z / 09:12Z 同判据)。
⚠️ **重复投递同一读数会稀释下一次真读数的信号**,这正是 18:13Z 立「停止同形投递」的理由。
⚠️ 本轮的实质产出(§四)是**本队内部的工程债**,不是 owner 需要在手机上处置的事。
三条选项逐字不变(提高预算线 / 接受本月剩余时间批测全停 / 09-15T00:14Z 找到的第三条:
先把 S3 出网这个真燃烧点按住)。

## 七、铁律 9

P4.1 upstream 标尺波**连续第二十轮欠**,⛔ 唯一阻因是闸 (iii) `exit 3`(预算),
**不是本台未认领,也不是排期**。

## 八、⛔ `bots` / `game` 一行未改

本轮改动只在 `tools/batch_test/soak/`(2 个 py)、`tests/`(1 个新 py)、`iterations/`(章程 + 本报告)。

## 九、自查

⚠️ **管道坑第 32 次** —— 本轮**第一条**命令又走了管道(`routine_selfcheck.sh … | tail -40`),
脚本自卫当场拦下,逐字 `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.`
+ `SELFCHECK_EXIT=2  REFUSED (nothing was checked; this is NOT a pass)`。**零损失,拦住它的是工具不是纪律。**
⭐ **上一轮交棒 ⑥ 要的「一次到位」仍未做到**(仍是第二跑才对);
⭐ 但**第二跑一次过**(重定向 + 裸读,⛔ 未带 `timeout`)—— 上一轮那道「改了重定向又带上 `timeout`」的坑本轮**没踩**。
此后每一条读退出码的命令一律重定向后裸读,**本轮零管道退出码**。

**开工自检**:`bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1` 跑满全程(Lua 腿慢,收尾时仍在跑,
最终读数见**补记 C**)。写报告时已读到的两条:
- **python 腿 `135 passed, 0 failed, 3 uncertifiable`** —— ⭐ 上一轮是 `131 passed, **1 failed**, 3 uncertifiable`。
  ⚠️ **「那条红不在 FAIL 列里」是读数,「被谁怎么弄绿的」不是** ⇒ ⛔ 本台**不主张** `test_lua_corpus_stability.py`
  已被修好、⛔ 不据此关 GH #825 的任何一半;⛔ 本轮**未用 worktree 独立复跑**净 `origin/main`,
  故**不主张**这三条 `UNCERTIFIABLE`(`test_lua_gate.py` / `test_luacheck_gate_soakswitch.py` /
  `test_selfcheck_lua_leg.py`)与上一轮是同一批。逐字限定照抄:`did NOT run -- this is not a pass and not a failure`。
- **Lua 快腿 `RED test_fieldsip_atom_pricing.lua`**(`tests/test_fieldsip_atom_pricing.lua:400/562`:
  语料走查从 1021 帧变成 1039 帧、flask 门下从 944 变成 961)。
  ⭐ **这正是上一轮 §F 那条**:它**不在 push 闸的成员资格里**(在开工自检快腿里),
  ⇒ **推它的闸绿得对** —— GH #624 / #616 / #806 立案形状的又一例。
  ⛔ **本台不代修**(语料棘轮属英雄组/协同组),⛔ **不自行调整闸成员资格**,⛔ 不重开 issue。

**铁律 11**:零 `requires approval`、零空转(自检后台跑,成本 / 闸 / 收割 / 泄漏 / 工作单元并行推进)。
⛔ 本轮**未调用任何 `mcp__github__*`** —— 不是被拒,是**本轮没有该发的 issue**:
欠条 `games_ledger_cross_wave_accounting` 自带 ⛔「**不开新 GH issue**」(逐字:同一根棒换个地方放),
而它的全部新意就是**换载体** —— 本轮做的正是那件事。

## 交棒

1. **owner —— 刹车第二十五轮持有,GH #779 第十六轮零表态**;本轮**零新支出测量** ⇒ ⛔ 未推未评(§六)。
   球仍在 owner:提高预算线 / 接受本月剩余批测全停 / 先按住 S3 出网。
2. ⭐⭐ **总监 —— 验收 §四,并裁一句 `$/有效局` 的终局口径**。
   欠条 `done_when` 的 note 逐字要求「谁退休这一行,谁必须在同一轮里写下 `$/有效局` 该读哪一个字段,
   一个字段名,不许留空」。⛔ **本台不退休这一行**(针脚未命中,§4.4),因此**不行使那个命名权**;
   ⭐ 但两个候选现在都**在行里可重算**:`finished`(宽,= 完赛局)与 verdict 的 `scored_games`
   (窄,= 活过 `NO-PAIR`/`THIN-ARM` 的种子里的完赛局),**两者相差真实局数** ⇒ 请总监点一个。
3. **总监 —— 欠条 `games_ledger_cross_wave_accounting` 的状态请勿改动**:仍 **OWED**。
   ⛔ 本台不改 `owed_executions.json`(写入权归总监,先例 GH #33)。
   ⚠️ **注意它现在的失效模式变了**:此前是「没人写」,现在是「**线接好了但九月没有波可收**」——
   ⇒ 若刹车延续到 10 月,针脚 `"game_id":"202609` 将**永远不可能命中**(九月过完了),
   **那时它需要的是改针脚,不是再催一次本台**。请总监预先裁一句。
4. **总监 —— §九 的 Lua trunk 红 `test_fieldsip_atom_pricing.lua` 与三条 `UNCERTIFIABLE`**:
   ⛔ 本台未在净树复现、⛔ 不代修、⛔ 不归因;只登记读数变化(上一轮 1 failed → 本轮 0 failed)。
5. **总监 —— 上一轮交棒 ②③④⑤ 本轮未获回应,逐字保留**,⛔ 本台不自行退休任何一行。
6. **下一轮本台**:开工第一条命令 `bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1`
   (⛔ 无管道、⛔ 无 `timeout`,**一次到位** —— 这是第三轮欠这一条);
   刹车解除(headroom ≥ `$1.10`)时按规格发 P4.1 标尺波,**收割命令必须带
   `--ledger iterations/games_ledger.jsonl --ledger-wave W<N>`**(章程步骤 3 已写死),
   并把 GH #801 那四行**恢复后**的样子抄进报告 —— 那才是它第一次走「该买」分支。

## 补记 C — 开工自检跑完(正文 §九 当时读不到的真码,同轮补)

⚠️ **真退出码不是通知里那个 0** —— 通知报的是外壳(`… ; echo EXIT=$?; tail …`)里 `tail` 的码;
自检自己的码在输出文件的 `EXIT=` 行里,读作 **`EXIT=3`**。逐字照抄:

```
legs run              : 13
FINDINGS (exit 3)     : cadence queue-rulings owed-executions lua-coverage trunk-red(lua)
UNCERTIFIABLE (exit 2): trunk-red(python)
NOT RUN (inside a leg): tests/test_lua_gate.py tests/test_luacheck_gate_soakswitch.py tests/test_selfcheck_lua_leg.py
selfcheck worst exit: 3
```

⛔ **未把 exit 3 归给任何单一条腿**(工具自带的防滥读提醒照抄:
`Read this before writing "exit 3, all of it cadence": that sentence was hand-made attribution
for days, and on 2026-08-27 it was wrong for 22 hours (GH #267)`)。

⭐⭐ **正文 §九 那条读数要按这一行收紧,方向对本台不利,照登**:
`trunk-red(python)` 本轮读 **`UNCERTIFIABLE (exit 2)`,不是 finding** ——
⇒ **python 那侧的 trunk 这轮没人看过**(铁律 10 逐字:`SKIP`/`UNCERTIFIABLE` **不是通过**)。
⛔ 因此「`135 passed, 0 failed`」**不许**被读成「python trunk 干净了」:那三条
`NOT RUN` 正是把这条腿抬成 UNCERTIFIABLE 的原因。⛔ 本台**不主张** GH #825 的任何一半可以关。

`trunk-red(lua)` 是**真 finding**:`test_fieldsip_atom_pricing.lua`(语料走查 1021→1039 帧)——
⭐ 与正文 §九 一致,**它不在 push 闸成员资格里** ⇒ 推它的闸绿得对(GH #624/#616/#806 立案形状)。
`lua-coverage` finding 现读:**`UNCOVERED 114 of 472 (24%)`**,且本轮**集合又长了两个**
(`test_dusttower_dive_guard.lua` / `test_fieldsip_transfer_receiving_site.lua`,均 `no_manifest_row`);
⭐ 同时 `NOW COVERED tests/test_fieldsip_atom_pricing.lua` —— ⚠️ **正是上面那条真红**,
⇒ **它今天能被看见,恰恰因为它刚被纳入自检快腿**。⛔ 本台不代修、⛔ 不调成员资格(归总监/英雄组)。
⭐ **`unlanded` 不在 findings 里**;⚠️ ⛔ 但本台**不主张**任何人的工作「已落地」——
该腿自带三条限度,「不再被点名」有多种成因,本轮**未逐条现跑 `merge-base` 复核**。

## 补记 D — push 之后(GH #290 顺序):三条腿的读数、一次竞态、一条新交棒

**(A) 铁律 6 三条腿,逐字**(三次 push 读数一致,此处抄落地那一次):

```
luacheck bots game: 0 warnings
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 92 ran, 0 findings, 0 uncertifiable, 11.9s
lua gate: 388 ran, 0 findings, 0 uncertifiable, 8 unanswered, 6 known-red, 581.0s
```

`ARM_EXIT=0`(逐字 `push gate armed (core.hooksPath=.githooks, already set)`);
⛔ **未用 `RULE6_BYPASS`** ⇒ 无「跳过不是通过」行。
⭐ **本轮 `lua gate` 那行是真跑不是作用域判定** —— 与上一轮不同(那轮只改 `iterations/`,
`HEAD:main` 读 `SKIPPED BY SCOPE`),本轮 commit 含 `tests/test_verdict_ledger_write.py`
⇒ **两个 ref 的 push 都跑满了全腿**,`py gate` 的清单里也逐字点名了它。

**(B) ⚠️ push 竞态一次,如实登记**:第一次 `HEAD:main` 得
`! [rejected]  HEAD -> main (non-fast-forward)`(`PUSH2_EXIT=1`)⇒
`git pull --rebase origin main`(`REBASE_EXIT=0`,两个 commit,无冲突)后
`PUSH4_EXIT=0`,逐字 `544baea7..b90b9c2e  HEAD -> main`。
⭐ **落地独立复核不靠 push 回显**:`git merge-base --is-ancestor HEAD origin/main` ⇒ 真,
`git ls-remote origin main` = `b90b9c2e` = 本地 HEAD。

**(C) `RULE6_MEMO=REUSE` 第一次在本台的读数里出现**,自带限定逐字
`This is a REUSE, not a skip: all three legs really ran` ——
它出现在同一棵树上的第二次 push(push 路径每次尝试跑两遍钩子)。
⛔ 不记成「第二次也跑了三条腿」,**记成「第二次复用了第一次的绿」**。
⚠️ 被 rebase 改过树之后,memo **不再适用**,于是重跑了全腿(581s)—— 这正是它该有的行为。

**(D) ⭐ 新交棒(总监 / 英雄组)——闸自己点名的一行,本台不代办**:

```
1 baselined test(s) are GREEN again -- take them off the baseline so they start refusing again:
    tests/test_axe_t15_payoff.lua
  python3 tools/agent/lua_gate_measure.py --set-known-red <file>
```

⭐ 上一轮它还在 `7 known-red` 里,本轮 `6 known-red` ⇒ **它绿了**。
⛔ **本台不改 baseline**(成员资格/赦免名单的编排权归总监),
⚠️ 也**不主张**它「被修好了」—— 绿是读数,归因不是。
⚠️ 闸对那六条的限定逐字照抄:`⛔ this list is not an exemption and it is meant to SHRINK.`

**(E) 给总监欠条 `push_hook_duration_exceeds_stream_push_interval` 的三个新读数**:
本轮三次全腿分别 **586.6s / 580.2s / 581.0s**,逐字 `the hook's REAL cost this run is 581.00s
against a 300.0s budget`(**1.94 倍**),`8 new test(s) exceeded the hook budget and were EXCLUDED`。
⚠️ **本台一轮内跑了三次全腿**(分支 / main 被拒 / rebase 后重推)⇒ **一轮 ~29 分钟花在闸上**,
而流的触发间隔是 2h;⛔ 本台未改 `tools/agent/lua_gate.py`(编排权归总监),
⛔ 未用 `--no-verify` 绕过(闸从来不是挡路的那个,绕过只会丢读数)。

**铁律 11**:全程零 `requires approval`、零空转。

## Token

`TOKENS total_in=6,909,894 out=47,194 turns=52`(统计后的收尾回合不计入)。
