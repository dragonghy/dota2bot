# 批测台 2026-09-09T18:20Z — W61 收割:**全损**

**一句话**:W61 的四台机器在起飞后 **2.6–7.9 分钟**内被 spot 容量回收殆尽,
**零局、零 verdict、零配对种子**;闸 (iv) 因此第一次打出 **BLINDED**,
**W62 必须按需发一波**。本轮零发波(闸 (i) 差 185 分钟),新增支出 `$0.01`。

---

## 一、成本

| 量 | 读数 | 来源 |
|---|---|---|
| MTD | **`$71.130`** | `budgets describe-budgets`(免费),`check_costs.sh` `COSTS_EXIT=0` |
| CE 复核 | `71.1297550729` | 脚本按 `≥$35` 规则自动花 `$0.01` 复核,两源逐位一致 |
| 围栏 | `$80.00` | `wave_fence.py` **本轮现推**(`$50` 已跨 `state=ALARM`;`$80`/`$100` 未跨) |
| 刹车 / owner 线 | `$90` / `$100` | 章程,工具只应用不推导 |
| 本轮新增支出 | **`$0.01`** | 只有那一次 CE 复核 |
| W61 残值 | **~`$0.11`** 模型侧 | 23.92 机分 = `0.3986 h` × 四台,`$0.28/h` 价带 |

⚠️ **滞后必须写明**:那张 budgets 快照 `refreshed 2026-09-09T11:20:58Z`,
**结构上含零个 W61**(15:23Z 起飞),而 W60(09:23Z)落在章程记的 4.3–11.3h
滞后窗内、**大概率也没进去**。`$71.130` 是**偏低**的读数,方向是危险的那一侧。

⭐ **未变的大头**:上一轮定位的「MTD 里 `54.9%` 不是本项目的」(09-05/09-06
两天的 `c7a.16xlarge` + `r7a.16xlarge` spot,农场从不用这两种机型)本轮**未再复查**
(不花第二次 CE),仍顶在本台围栏上。已在 owner 侧挂着,本台不擅改任何一条线。

## 二、收割 W61 —— 全损,四条独立证据

**结论:0 局、0 verdict、0 配对种子,四台无一例外。**

1. **SIR 行(第一手,本轮读到,harvest 须知 (b))** —— 四台全是
   `Status.Code = instance-terminated-no-capacity`,`Status.Message` 逐字
   `Your Spot instance was terminated because there is no Spot capacity available that matches your request.`

   | seed | SIR | create | update | 存活 |
   |---|---|---|---|---|
   | 10208 | `sir-44ifpf4j` | 15:23:43Z | 15:31:35Z | **7.87 min** |
   | 10212 | `sir-ngp7p8tj` | 15:23:48Z | 15:26:26Z | **2.63 min** |
   | 10390 | `sir-ca87mgjk` | 15:24:17Z | 15:31:35Z | **7.30 min** |
   | 10526 | `sir-h5vzpcyj` | 15:24:28Z | 15:30:35Z | **6.12 min** |

2. **S3 逐 run 计数(不是「看最新对象时间戳」推断)** —— `validation/` 下匹配
   `20260909_1523` / `20260909_1524` 的对象数**各为 0**;`validation/` 最新对象仍是
   `2026-09-09T10:22:37Z`(W60 的)。
3. **`soak/` 下 W61 只留了三个 `soak_farm.log`**,零 `analysis` / 零 `demclaim`。
   第四台(152346 / seed 10212,2.63 分钟就死)**连日志都没来得及传**。
4. **机器自己的日志(seed 10208)** —— 它**一路起到了最后一步**:clone 到钉死的树
   (`252a5781 hero: record the round's publication readings and token usage`)、
   `soak_pool.lua` 41 英雄、`plain_deploy.sh` 完成、16 槽农场起来(`SOAK_REC_SLOTS=8`)、
   `validate_onspot.sh` **带着完整 37-id 臂串和 seed 10208 已经发出去了**,然后:
   `SPOT INTERRUPTION: {"action":"terminate","time":"2026-09-09T15:33:35Z"}`。
   ⭐ 那个时刻比 SIR 的 `UpdateTime 15:31:35Z` **晚 2 分钟**,正是 spot 的标准两分钟
   预告 ⇒ **两个读数互相印证,不是互相矛盾**。
   顺带:这份日志里的臂串是 37 个 id 的**第三次独立推导**(闸时的 test_set.md 第 2 行、
   发波脚本自己的复算、现在是机器自己的命令行)。

**每台回填**:`ab=0 / ba=0 / arm_depth=0.0`(**各自的整数,按须知 (c) 零填,不留 null、
不填池化值**),外加 `sir`/`create`/`update`/`survival_min`/`status_code`。

## 三、闸 (iv):第一次打出 BLINDED —— **W62 按需**

`reclaim_blind.py --wave-json W61_wave.json` ⇒ **`RECLAIM_BLIND_EXIT=1`**,逐字:

```
yield      : 0 paired seed(s) of 4 machine(s)
attribution: 4 machine(s) reclaimed before the flip
VERDICT: BLINDED -- yield <= 1 AND the loss is capacity.
NEXT WAVE: launch --on-demand (ONE wave), then revert to spot.
```

⭐ **这是一次裁定,不是一个默认** —— 与 W61 自己的市场类型形成正对照:W61 写的是
「保守默认走 spot,**并明写这一行是默认不是判据**」(闸 (iv) 当时 exit 2)。
本轮 exit **1**,读源码确认 `1 = BLINDED`(`2` 才是 UNDECIDABLE,`0` 是 not blinded)⇒
**W62 的市场类型是闸打出来的**。
**下一轮务必传 `--on-demand`,且只此一波,之后回 spot。**

## 四、AZ 塌缩:上一轮登记的暴露**当天就兑现了一半**(诚实边界在后半句)

W61 起飞时登记过:请求 2a/2b/2c/2d,**实得 2a/2b/2a/2a**,三次
`InsufficientInstanceCapacity` + 两次 `re-aiming inside the ring`,
四粒里**三粒落在 us-west-2a**。本轮的死亡时刻表:

- **10208(2a)与 10390(2a)在同一秒 `15:31:35Z` 一起死** —— 这正是 #664 预测的
  「一次 2a 容量事件同时拿走多粒」的相关性形状,**当天兑现**。
- ⛔ **但不许把它读成全损的原因**:10212 在 **2b**,而且**死得最早**(15:26:26Z),
  10526(2a)死在 15:30:35Z。⇒ 本波是**跨 AZ 的普遍容量短缺**,
  **AZ 分散度即使是 4+0 也救不了这一波**。
  本条登记为 **#664 的实测佐证**,**不是**「AZ 塌缩导致了全损」。

## 五、GH #454 (乙1)/(乙2):一个欠着,一个**结构上不可算**

- **(乙1)** 第 **10** 轮欠着。W55–W61 仍拆不开日 `USW2-SpotUsage` 行;
  本波 `~$0.11` **即使拆开也小到分不出来**。
- **(乙2)** ⛔ **本波不可算,而理由是条款自己的措辞**:它取
  **自终机器**(`instance-terminated-by-user`)的机时均值,**被回收的台按条款原文剔除**。
  W61 **零台自终** ⇒ 均值取在空集上。
  ⚠️ **上面那个 `0.0997 h/台` 是被回收台的数字,绝不可拿去和 `1.00 h/台` 的阈值比** ——
  那样比会得到「远低于阈值 ⇒ 常数不用复查」,而这恰恰是 #454 (乙) 立案时点名的
  **「抢占越频繁,常数越不会被复查」**那个反向机制。本波是该机制的**教科书样本**。

## 六、并池:裁定**在法律上不变,在事实上落空**

总监 `test_set.md §GH` 裁定 W60+W61 按「可达程序等价」key 并池、家族深度 2、至多 8 粒。
**key 仍然成立**(臂串逐字相同、树差全在 `hrparity` 后面不可达)——
但 **W61 交出 0 粒** ⇒ 池 = W60 的 4 粒,**家族深度仍是 1 波 / 4 粒**。
**自 W52 以来第一次 8 粒配对读数没有发生。**
⚠️ 这不改变任何裁定,只是把它的分子清零;下一次同臂串的波仍可与 W60 并池。

## 七、闸 (i)、闸 (iii)、本轮不发波

- **闸 (i)**:`THROTTLE_EXIT=3`,逐字 `margin : 11102s SHORT of unlock` /
  `A launch at this instant BREACHES gate (i) by 185m02s`,unlock `2026-09-09T21:24:29Z`
  (**锚点 W61 末台,不要抄这个时刻,下轮跑工具**)。⇒ **本轮结构上不许发新波**。
- **闸 (iii)**(本轮只作信息登记,没有发波):`FENCE_EXIT=0 CLEAR`,
  `--planned 2.150`(按需,因为闸 (iv) 已裁 W62 按需)`--pending 2.200`
  (W60+W61 各按登记价 `$1.10` 取较大声明,虽然 W61 实际只烧了 `~$0.11`),
  `actual $71.130` / `projected $75.480` / `fence $80.00` / **`headroom $4.520`**;
  `accrual CERTIFIED ZERO (0 accruing instances account-wide)`,未用 `--no-accrual-check`。
- ⛔ **补跑豁免本轮不适用,理由是规则不是偏好**:章程 GH #498 的豁免给的是
  **「一条预先登记的处置被执行」**(`rerun` 块)。`W61_wave.json` **在发波时没有登记任何
  `rerun` 块** ⇒ 没有可执行的既有处置,补跑就会是**一个新的花钱决定**,而那正是闸 (i) 管的东西。
  **更硬的一条在前面**:总监 `§GH` 对 W61 的起飞**明确「不予追认」**(闸 (ii) 三条腿全不成立)⇒
  **补跑一个未获授权的波,是把同一笔未授权支出再买一次**。两条各自独立成立。

## 七之二、先例:**W43 是同型全损,本轮处置与它逐条相同**(独立得出后才发现)

`2026-09-03T21:15Z` 那轮收 W43:**整波报废、零局数据**,四台同样全被
`instance-terminated-no-capacity` 回收,同样死在跑出第一局之前。
它当轮的处置理由**三条**,在 W61 上**逐条重新成立**:
① 章程默认「一台被回收 = 补跑那粒」**预设其余粒还活着,在 4×1 全灭下这个前提是假的**;
② **GH #408 补跑围栏是每波最多 2 台按需,而全灭需要 4 台** ⇒ **围栏按构造不够用**;
③ **闸 (i) 锁着**(W43 锁到 00:21:58Z,W61 锁到 21:24:29Z)。
⇒ 本轮 §七 的两条(无 `rerun` 预登记、总监不予追认)是**新增的**理由,不是替代;
**四条独立成立,同一结论。**

⚠️ **同时继承 W43 一条「不主张」**:AWS 对**被 AWS 自己在第一小时内中断的 spot**
有不计费政策,W61 四台**全部**符合该形状 ⇒ **若成立本波花费为 `$0`**。
**本台没从账单上读到过,因此不声称**;围栏一律取保守非零值(`~$0.11` 模型侧 /
登记价 `$1.10`)。

## 七之三、queue.json:**本波零行需要回填,这是查过的不是假设的**

W43 那轮最大的坑是「**一个报废的波在 queue 里长得很像一个「测过了」的波**」。
本轮逐行查过:
- 全表 `status=running` 只有 **3 行**:`strategy-31`(`tormself`)、`strategy-32`
  (`immguard`)、`hero-27`(`savemana`)。
- 前两行**按构造搭不上车** —— `tormself` / `immguard` **都不在 W61 的 37-id 臂串里**
  (逐字核对过那串);第三行是 `ROUTED_ARCHIVE_SCAN`,**零 EC2**。
- 全表**没有任何一行**的文本提到 `W61` 或 `W60`。发波轮报告 §十 也自述
  「**发波轮不改 queue**」。
⇒ **没有一行被 W61 置成 running,因此没有一行需要被改回、也没有一行会被误读成
「测过了没效果」。本波不需要 W43 那句 `NOT A NEGATIVE READING` 的回填。**
⚠️ 顺带:`strategy-31/32` 的 `rides_wave` 文本**仍停在 `NOT riding W43`**(已过 18 个波次),
**本台不改别组的行**,按铁律 9 连同「仍搭不上车」一起交回总监(§十)。

## 八、泄漏检查:零,**而且这个零被证明过不是一次失败的调用**

- 区域 `us-west-2` **取自 `aws.env`**(`AWS_REGION=us-west-2`),不是手打。
- `describe-instances --filters state=running,pending` ⇒ `LEAK_EXIT=0`,**count=0**。
- ⭐ **正对照**(上一次同族事故:2026-09-06 那轮把三个空的 us-east-2 答案读成「没有实例」):
  同一条 EC2 API 路径上跑 `describe-spot-instance-requests` ⇒ `CTL_EXIT=0`,**SIR count=4**。
  ⇒ 调用通,凭据对,**那个 0 是读数不是失败**。
- W61 的四个 instance-id 直查 `describe-instances` ⇒ **空**,即四台已**整个**老化出
  `describe-instances`(起飞后 ~2.8h)。这**顺带给两件事补了第一手证据**:
  (a) harvest 须知 (b)「SIR 不会永远活着,必须在收割轮读完」——**本轮不读就永远读不到了**;
  (b) 章程 owed 行 `ondemand_termcode_read_time` 的 (甲):`StateReason.Code` 的老化窗
  **确实**关在收割轮之前。
- **收尾复查**(发完 issue 之后再读一次):`FINAL_LEAK_EXIT=0`,`running,pending` 仍为 **0**;
  正对照同样重跑 `CTL_EXIT=0` **SIR count=4**。⇒ **进场零、收尾零,全程零泄漏。**
- ⚠️ 一如既往:**零泄漏 ≠ 本月没有外来支出**(见 §一)。

## 九、量具缺陷登记(小,不影响本轮任何裁定)

`reclaim_blind.py` 对四行都打了 `[ab/ba only (no arm_depth given)]`,
而这份记录**给了** `arm_depth: 0.0`。读源码(`:387`)确认它用的是
`machine.get("arm_depth") is not None`(**不是** falsy 判断,所以**不是**零值坑),
真正原因是该行 `paired=False` ⇒ 根本没走到深度检查。
⇒ **判定正确(未配对的行本就该排除),但打印出来的理由是假的**。
本台**不自改 harness**,已按铁律 5 交出去(见 §十)。

## 十、交棒(本轮已发表的 issue:**GH #671 / #672 / #673**,评论 **#664**)

1. ⭐⭐⭐ **总监 / 下一轮本台 —— W62 按需,这是闸打的不是猜的**(§三)。
   一波,之后回 spot。围栏还剩 `headroom $4.520`(按 `--planned 2.150` 算)⇒
   **按需波贴着线,发之前必须当轮现跑 `wave_fence.py`,不要抄本报告的数**。
2. ⭐⭐⭐ **总监 —— W61 未获追认却已全损,请裁「这粒钱怎么记」**:
   本台的读法是**不补跑**(§七),但 W61 的四粒种子(10208/10212/10390/10526)
   **一局没跑就作废了**,而它们已从名册里被划掉。请裁它们是否退回可用窗口。
3. ⭐⭐ **GH #664(AZ 环)—— 当天就有了实测佐证**(§四),带诚实边界。
4. ⭐⭐ **[harness] `reclaim_blind.py` 的 `no arm_depth given` 对给了 `arm_depth` 的行也照打**(§九)。
5. ⭐ **GH #454 (乙1) 第 10 轮欠着;(乙2) 本波结构上不可算**,且本波正是 (乙) 立案理由的样本(§五)。
6. ⭐ **并池分子清零**(§六):裁定不变,8 粒读数没到手。
7. ⭐ `queue.json:strategy-5b` 本轮无进展(纯收割轮不改 queue);`strategy-31/32` 仍搭不上车、
   `rides_wave` 文本停在 `NOT riding W43`,按铁律 9 交回总监(§七之三)。
8. ⭐⭐ **[bug] 英雄组 / 总监 —— `test_call_arity_census.py` 的 trunk 红是真的,
   而被它点名的代码是对的**(§十一):普查没有「可选参数」这一格,把
   `X.axe_IsBattleHungerFresh` 刻意的 `fExtra` 默认值读成缺参。**此前几轮该腿都没跑成,
   所以这行没人看过。** 修普查(加可选参数概念)还是改调用点,请总监裁。

**下一轮本台 = 发波轮(W62,按需)**,解锁 `2026-09-09T21:24:29Z`
(**不要抄,跑 `wave_throttle.py`**)。

## 十一、自检(铁律 10)—— **第三次才跑完,`worst exit 3`,而它查出了一条没人看过的 trunk 红**

⛔ **如实登记两次失败**:第一条命令踩了管道坑并被脚本**当场拒绝**
(`REFUSED: stdout is a PIPE ... exit 2, nothing checked`);改重定向后**我自己给的
400s 上限把它掐了**(`EXIT=124`)—— **超时不是通过,也不是「trunk 是红的」**。
第三次 `timeout 1500` 后台重跑,**跑完了**。

⛔ **读码陷阱照旧复发,本轮照旧只采信工具自己那一行**:harness 给后台任务报
`[exited with code 0]`,而**脚本自己打的是 `selfcheck worst exit: 3`**,我自己 echo 的
`SELFCHECK_RAW_EXIT=3` 与之一致 ⇒ **本轮读数是 3(有发现),不是 0。**

⚠️ **一处自我更正,因为它是一句会误导下一轮的话**:中途我据 `ps` 判定
`tests/test_selfcheck_lua_leg.py` **挂死**并据此推论「一条腿没有秒表 ⇒ 铁律 10 每轮结构性作废」。
**那个推论是错的**:该测试只是**慢**,后来自己返回并被如实记成
`UNCERTIFIABLE tests/test_selfcheck_lua_leg.py (did NOT run -- this is not a pass and not a failure)`,
整条腿跑完。`run_py_tests.sh` 确实没有 per-test 超时(那是真的),但**本轮没有证据说它导致过作废**,
**不作为发现登记**。前两次失败的原因是**我自己给的上限太短**,不是工具。

**跑完的读数,逐字**:

```
116 passed, 1 failed, 1 uncertifiable
failed: tests/test_call_arity_census.py
TRUNK RED -- a python test is failing ON THE WORKING TREE.
legs run              : 10
FINDINGS (exit 3)     : cadence owed-executions trunk-red(python)
UNCERTIFIABLE (exit 2): none
NOT RUN (inside a leg): tests/test_selfcheck_lua_leg.py
selfcheck worst exit: 3
```

Lua 腿 **`87 tagged detector file(s), 0 failures`**(**`FAST SUBSET, not the full suite`**)。
六个 stable 锚点 `EXISTS`/`PINNED`/`SHIPPED` **全 ok**;promote-time 约束 `FROZEN none`、
5 个 co-promote 原子全 ok(`185 distinct live gate id(s), 18 carrying a PROMOTED note`);
`no expired admission wait in any live 当前状态 block (6 charter(s), 55 settled id(s))`。
`REFUSED refs: 959 of 985 (uncertifiable, mostly stale session branches ...)` —— 照抄不解读。

### ⭐⭐ trunk-red(python) 查到底了:**红是真的,而被红的那段代码是对的**

`tests/test_call_arity_census.py` 逐字:

```
bots/BotLib/hero_axe.lua  X.axe_IsBattleHungerFresh  UNDER passed 2 declares 3
```

查证(读源码,不是推测):
- `bots/BotLib/hero_axe.lua:956` 声明三参 `( hTarget, tCandidates, fExtra )`,
  **第三个参数是 `213cc903`(英雄组上一轮,GH #562 `axebhrecast`)加的**;
- 三个调用点里 `:1247` 传三个,**`:1215` 与 `:1267` 只传两个** ⇒ 普查如实报 UNDER;
- ⭐ **但 `fExtra` 是有意的可选参数**:它只被透传给
  `X.axe_HasHungerAlternative`(`:990`),而那里逐字写着
  **`and ( fExtra == nil or fExtra( npcOther ) )`** —— **nil 被显式处理**。

⇒ **两句都成立,缺一不可**:① 按普查自己的判据 **trunk 确实是红的**(它 exit 非零,
`worst exit` 因此是 3);② **两个两参调用点是正确的,不是 bug** —— 普查**没有
「可选参数」这个概念**,把一个刻意的默认值读成了缺参。**不是英雄组的缺陷,是普查的判据缺一格。**

⚠️ **这条红此前没人看过,而原因可查**:上一轮报告里 python 腿是
`UNCERTIFIABLE (exit 2): trunk-red(python)`(**没跑成**)⇒ 那一轮**没有人**读到过这行;
本轮是该腿跑完后**第一次**读到它。**「没跑成」与「跑完了是红的」在报告里长得很像,
而它们中间隔着一整条证据链** —— 这正是铁律 10 自己 `SKIP/UNCERTIFIABLE 不是通过`
那一条要防的形状。

⇒ 已按铁律 5 交出去(§十 ⑧)。**本台不改英雄组的代码,也不改普查。**

## 十二、Token 用量

```
TOKENS total_in=6,286,961 out=42,306 turns=47
```

(统计时刻 `2026-09-09T18:28:30Z`,之后的收尾回合不计入。)
