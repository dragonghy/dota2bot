# 批测台 2026-09-17T15:19:57Z — 刹车第四十二轮持有;零发波、零收割欠、零泄漏

**⭐⭐ 本轮的实质:P4.1 不是「没发过」,是 2026-09-05 发过一次、跑完 80 局、
并且那 80 局里一个人头都没有。** 预算解禁不足以让 P4.1 产出读数 ——
下一波在同样条件下会再空一次,而且**这次会空得没有声音**。

---

## 〇、开工自检(铁律 10)

⚠️ **管道坑第 34 次,且仍是当轮第一条命令**。第一跑逐字:

```
REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.
SELFCHECK_EXIT=2  REFUSED (nothing was checked; this is NOT a pass)
```

⛔ 那个 `SELFCHECK_EXIT=0` 是**管道读者的码**,不是自检的码(evidence discipline 3)。
第二跑改重定向裸读(`> /tmp/sc.log 2>&1; echo "EXIT=$?"`),**仍未带 `nohup`**
⇒ 400s 后被挪后台(交棒 ⑥(b) 字面模板**第十八轮未照抄**)。写报告时仍在跑
⇒ ⛔ **本轮无 `SELFCHECK_EXIT` 真码**,⛔ 不空转等它,⛔ **不写 trunk 绿也不写 trunk 红**。

---

## 一、AWS 引导 + 成本(闸 (iii))

`session_setup.sh` → `AWS_SETUP_EXIT=0`,`AWS ready: arn:aws:iam::581436534924:user/dota2bot-agent`
`(restricted batch-runner user — correct)`。⚠️ 本容器无 AWS CLI,脚本自装
(`installed via 'pip3': aws-cli/1.46.1`)。

`check_costs.sh`,`COSTS_EXIT=0`,逐字:

```
92.001	USD
   source: budgets/dota2bot-batch (free) | forecast 118.091 | budget limit 100.0 | budget refreshed 2026-09-17T13:24:58Z
   accrual probe (GH #779): alpha = 0 accruing instance(s) account-wide; beta = 0 wave record(s) after 2026-09-17T02:06:58Z
   stamp unknown (fresh container) -- alpha and beta are both zero -- a frozen stamp over an idle account opens no blind spot
   >= $35, but headroom to the $90.00 brake is $-2.001
   < $1.10 (cheapest wave) — CE confirmation SKIPPED, NOT passed (GH #801).
   Nothing can launch at this MTD, so confirming it buys nothing.
   Resumes by itself as soon as headroom >= $1.10.
```

⭐ **戳解冻了 —— 连续三轮冻结之后的第一个新速率点**:

| 轮次 | 戳 | MTD |
|---|---|---|
| 上一轮(09-17T12:24Z 报) | `2026-09-17T03:46:42Z` | `$91.809` |
| 本轮 | `2026-09-17T13:24:58Z` | `$92.001` |

Δ = `$0.192` / `9.6378h` ⇒ **`$0.478/天`**,**落在既有带 `$0.40–0.92/天` 之内**
⇒ ⛔ 不收窄那条带(一个点不收窄一条带),越线日与兜底日 `2026-09-27T00:00Z` **原样结转**。
⛔ GH #801 四行整块照抄如上,**SKIP 不是 pass**,欠条
`gh801_confirm_headroom_first_live_read` 仍 **OWED**(⛔ 未用 `COST_CONFIRM_AT=999` 自救)。

**闸 (iii) 判决:MTD `$92.001` > 刹车 `$90.00`,headroom `$-2.001` ⇒ 零发波(第四十二轮)。**

---

## 二、收割:零欠(但计量口径本轮被纠正一次)

`validation/` 前缀 **592** 个对象,与 09-15 / 上一轮登记**逐位相同** ⇒ 无新 verdict
⇒ 未跑 `recover_verdict.py`。RULING 47 两个读数分开写:最新对象(`sort|tail`)
`lf_rescue…_20260912_1018`;字典序最后键(裸 `tail`)`tpcommit…_20260907_2218`。

⚠️ **口径更正(不影响结论,影响以后的可比性)**:此前各轮登记的 `OBJECT_COUNT=592`
是 **`validation/` 一个前缀**的数,不是桶。整桶实测 **79,889**,分布:

```
72079 soak | 4748 dem21 | 1987 replays | 592 validation | 306 behavioral
  161 run_20260905_151656_main_vs_upstream-baseline
    8 tools | 6 calibration | 1 run_20260905_151230_main_vs_upstream-baseline | 1 rs
```

⛔ **一次自捉**:本轮第一条 `s3 ls` 用了**猜的**桶名 `dota2bot-batch-results`
(`NoSuchBucket`,`S3LS_EXIT=255`),真名在 `aws.env` 里是 `dota2bot-batch-results-4924`。
那次失败的 LIST **照样登记**(见 §五)。

---

## 三、⭐⭐ 工作单元:P4.1 的 09-05 那一波跑完了,而它一个人头都没有

### (甲) 它存在,而且全队的账本看不见它

上表倒数第四、第五行两个前缀就是 P4.1 的形状 ——
`main_vs_upstream-baseline`,2026-09-05。之所以四十二轮没人提起它:
**收割账本只数 `validation/` 前缀**,而 `aws_run.sh` 的产出落在**桶根下自己的 run 目录**。
⇒ 📌 与铁律 1 §RULING 48 同型:**一个只覆盖部分语料的计数被当成了全量计数**。

- `run_20260905_151230_…`:**只有 `batch_run.log`**(256,396 B)⇒ 一次夭折的首发。
- `run_20260905_151656_…`:**161 个对象 = `batch_run.log` + fwd 40 局 + rev 40 局**
  ⇒ ⭐ **两条 swap 腿都跑满了**(40/40),不是被 12h 看门狗截断的半波。

### (乙) 它当时的失败被读成「传输问题」,而传输只是第一层

`batch_run.log` 实读:`git fetch origin main upstream-baseline` **成功**
(`* [new branch] upstream-baseline -> origin/upstream-baseline`),
`run_batch.sh -n 40 -j 16 -t 4` 正常起跑,然后**每一局**:

```
tools/batch_test/run_batch.sh: line 68: .../game_13.log: No such file or directory
[game 13] finished (0 log lines)
FileNotFoundError: ... '/opt/results/.../fwd/game_13.log'
[game 13] parse failed — inspect .../fwd/game_13.log
```

⇒ 80 个 `game_N.json` **全部 0 字节**。这一层**已经修好了**:`run_batch.sh:70-75`
现在有 stdout 回退,注释逐字点名本案
(`Y1 2026-09-05: cp silently failed, every .log was absent, all 80 game JSONs came back 0 bytes`)。

⛔ **但那是传输层的解释,不是这一波为什么没有读数的解释。**

### (丙) 逐局普查(80/80,不是抽样):一个人头都没有

`.log.stdout` 每个 ~100 MB(全波 ~7.9 GiB)。⛔ 不下载全量 ——
**signout 记分板在文件尾部**,于是用 **S3 后缀 range 读**(`--range bytes=-262144`)
把 80 局的尾部各取 256 KB(合计 20 MB),逐局解析:

```
games with signout: 80 / 80 | missing: []
player rows per game (gpm): [10]
ALL GPM zero: True | ALL KDA 0/0/0: True | ALL level 1: True
max GPM seen anywhere: 0 | max level: 1 | max kills anywhere: 0
duration s: min 2847 max 3391 mean 3089
fwd: n=40 team0(radiant) wins=25 team1(dire) wins=15
rev: n=40 team0(radiant) wins=21 team1(dire) wins=19
```

**800 个选手行,无一例外:Level 1、GPM 0、KDA 0/0/0、Actual Player Damage 0、LastHit 0。
全波最大人头数 = 0。** 局长 2847–3391s(均值 **51.5 游戏分钟**)后有一方基地被推掉。

⇒ **英雄一个动作都没做过;那 80 局是小兵推了 50 分钟推完的。**

### (丁) 不是「没抽到英雄」——英雄抽到了

头部 512 KB 实读:`PR:SetSelectedHero` **20 行**(ursa / phantom_lancer / razor /
arc_warden …,随机池不是焦点池),`DOTA_GAMERULES_STATE_` 走到 `PRE_GAME`。
⇒ 英雄实体存在、draft 正常,**然后没有任何 AI 驱动它们**。

⚠️ **诚实边界**:本台**只测到「英雄零动作」这个事实**,⛔ **未测到机制**。
最自洽的假说是 A/B 的 bots 树没有真正接管(`bots_ab_new`/`bots_ab_old`),
而 `AGENTS.md` 早就登记了「引擎错误处理器坏掉、Lua 报错文本全被
`error in error handling` 吃掉」⇒ **这一类失败在设计上就是无声的**。
⛔ 本台不宣布机制,交总监/harness。

### (戊) ⭐⭐ 已落地的那个修复,把响的失败改成了哑的失败

`parse_log.py` **没有活性守卫**。它只有两条 note,逐字:

```
"no winner found — game likely did not finish"
"no Match signout scoreboard — incomplete game or format change"
```

**空波两条都不触发**(它有 winner,也有 10 行记分板)。唯一会动的是
`mode_guess`(avg_gpm 0 ≤ 510 ⇒ 判 `"normal"`),而同一段注释自己说
`treat this as a sanity flag, not ground truth` —— **一个被设计成可以忽略的旗**。

⇒ **09-05 那天,空波表现为 80 个 0 字节文件(响亮)。今天同样的条件下,
stdout 回退会产出 80 个格式完好、字段齐全、全是 0 的 JSON(无声)。**
`recover_verdict.py` 的空检查(`:84`,`both legs non-empty`)问的是**有没有行**,
不是**行里有没有内容** ⇒ 空波照样过闸。
📌 这正是本仓库反复立法的那个形状:**一个真命题(文件非空)被用来满足另一个问题
(这一波测到了东西吗)**。

### (己) 对 P4.1 的结论

P4.1 要的三样是**胜率 + gpm 差 + 局数**。这份语料:

- **gpm 差:恒等于 0**,⛔ 不是「主干与 upstream 打平」,是**两边都没打**。
- **胜率**:两个分层按铁律 4 (i-a) **全登记**(fwd team0 25/40 = 62.5%;
  rev team0 21/40 = 52.5%)。⛔ **不合成 arm** —— 不是因为反号(没反号),
  而是**被估的量在构造上是空的**:唯一的行动方是小兵,于是这两个数量的是
  **侧偏本身**(合计 radiant 46/80 = **57.5%**,与「Radiant 侧偏 ≈ +1.5k gold」同向)。
  ⭐ 顺带:它是一次**意外的纯侧偏标定**,但 ⛔ 本台不把它登记成标定
  (局长 51 分钟、零英雄行动,与真实 Turbo 局不同分布)。
- **局数**:80 真实存在,但**测的不是 bot**。

⇒ **P4.1 仍然欠着,而欠的原因从今天起是两个不是一个。**
⛔ **解禁预算不足以交付 P4.1**:下一波在同样条件下会再空一次。
⇒ 建议的顺序(⛔ 本台不定夺,已交总监/harness):**先补活性守卫,再发波**。
守卫是廉价的(一局 GPM 全 0 / 全场 0 人头 / 全场 Level 1,任一即 FATAL),
而且**有零成本的验收现场** —— 这 80 局就是它的 fixture 语料。

---

## 四、没做的事(逐条)

- ⛔ **未发波**(闸 (iii) `$-2.001`)。
- ⛔ **未改任何代码**:`bots`/`game`/`tests`/`tools` **一行未改**。
  想过直接给 `parse_log.py` 加活性守卫,**否掉**:守卫的判据(阈值?FATAL 还是 note?
  拦在 `run_batch.sh` 还是 `recover_verdict.py`?)会决定以后每一波的去留,
  **裁定前造仪器正是本仓库反复点名的顺序错误**;且本台不是 harness 的归属组。
- ⛔ **未下载全量 7.9 GiB**(那是 ~`$0.72` 出网,而 headroom 是负的)。
- ⛔ **未删除**那两个 09-05 前缀(它们现在是活性守卫的验收语料)。
- ⛔ **未在实例上验证**任何结论。

---

## 五、成本(铁律 1 三段式)

**零 EC2 / 零 CE / S3 读取 `89` 个对象(出网未计价)**。

⭐ 本轮第三段**不是零,而且不小**,逐条数出来:

| 读 | 次数 | 字节 |
|---|---|---|
| `batch_run.log` 全量 | 1 | 937,671 |
| 尾部 range 抽样(512 KB) | 7 | 3,670,016 |
| 头部 range(512 KB) | 1 | 524,288 |
| 逐局普查尾部(256 KB × 80) | 80 | 20,971,520 |
| **合计 GET** | **89** | **≈ 26.1 MB** |

另登记**不下载的调用**(⛔ 不写成零):`s3 ls --recursive` **2 次**
(1 次 `NoSuchBucket` 失败 + 1 次成功列 79,889 键 ≈ 80 个 LIST 请求)。

⭐ **方法学上值得留下的一条**:整波 7.9 GiB 里,**判决所需的信息全在每个文件最后 256 KB**
⇒ 后缀 range 读把「判断一波是不是空的」从 **~`$0.72`** 压到 **~`$0.002`**,
**降低约 350 倍**。⇒ 建议把它做成收割期的标准动作(已写进 §三(己) 的建议)。

---

## 六、泄漏检查:零

五条独立路径:(1) `check_costs.sh` 自带的 running/pending 区块**空**;
(2) `alpha = 0` accruing instance(s) **account-wide**,本轮实读;
(3) `beta = 0` wave record(s);(4) AMI 仍 `ami-0a990a26d89c66547` **一张**;
(5) `pending waves : $0.000`。

---

## 七、交棒

1. **(总监 / harness)** §三 的活性守卫 —— 判据与拦截点归属待裁,验收语料现成
   (`s3://dota2bot-batch-results-4924/run_20260905_151656_main_vs_upstream-baseline/`)。
   已开 issue(见 §八)。
2. **(总监)** P4.1 的完成定义需要更新:它现在有**两个**阻因,预算只是其一。
   ⛔ 本台不改 `OWNER_PRIORITIES.md`(owner 的文件)。
3. **(本台下一轮)** 闸 (iii) 仍会是 `exit 3`(除非 10-01 归零)。
   ⭐ **下一轮起收割要数整桶前缀,不只数 `validation/`**(§二 口径更正)。
4. **(本台下一轮)** 开工自检:`nohup … &` 字面模板**第十九次**待照抄。

---

## 八、GitHub

已开 **GH #881 `[batch]`**(见下节 push/precheck 读数)。
⛔ **未追评 GH #779**(本轮有新速率点,但它落在既有带内,零信息增量)。
⛔ **未追评 GH #877**(上一轮那条 P4.1 启动口令的 issue)—— §三 是**不同的阻因**,
合并进去会把两件事混成一件;已在新 issue 里交叉引用。

---

## 九、push 与闸读数(铁律 6,三条腿)

顺序照 RULING 69:**先分支、后 main**,且**两次 push 都排在发表 issue 之前**(GH #290)。
`arm_push_gate.sh` → `ARM_EXIT=0`;`git pull --rebase origin main` → `PULL_EXIT=0`
(`up to date`)。⛔ **未用任何 `RULE6_BYPASS`。**

第一推(分支,`PUSH1_EXIT=0`):

```
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 130 ran, 0 findings, 0 uncertifiable, 47.9s
lua gate: scope = 2 path(s) changed vs the pushed refs' base (base=fallback-merge-base)
lua gate: SKIPPED BY SCOPE -- this push touches no bots/game/tests path.
```

第二推(`HEAD:main`,`PUSH2_EXIT=0`,`473cdb8f..6e3334fe`):

```
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 130 ran, 0 findings, 0 uncertifiable, 47.9s
lua gate: SKIPPED BY SCOPE -- this push touches no bots/game/tests path.
```

⭐ **RULING 69 的正序代价本轮实测为零**:两推都是 `SKIPPED BY SCOPE`(本轮 markdown-only,
未碰 `bots/game/tests`),⛔ **没有出现反序那一轮的 `lua 411 ran … 639.7s`**。

**发表 issue 前过闸**(GH #290):`claim_precheck.sh` → **`PRECHECK_EXIT=0`**,逐字
`local commits not on origin/main: 0` / `paths cited 5 … resolved on trunk 4 refused 0` /
`clean` / `OK to publish: every citation resolves on origin/main.`
⭐ 与上一轮不同,本轮**一次过**(上一轮首跑 `exit 3`,浅克隆答不了裸 SHA 的祖先关系;
本轮引用里**没有裸 SHA**,用的是分支名与 S3 URI)。

⚠️ **一处自捉,已更正并重推**:报告与章程初稿把新 issue 预写成 **`#879`**,
实际创建回来是 **`#881`** ⇒ **两处引用在 push 之后、被任何人读到之前改正**。
📌 登记原因:**预测一个尚不存在的编号,本身就是一条 `claim_precheck.sh` 在原理上查不出的引用**
(它解析 `tests/…`/`bots/…`/`state.json:<key>`/`§XX`,**不解析 issue 号**)——
与 §三(戊) 同型:**闸绿不等于引用对**。

---

## 十、开工自检:⭐ 真码到手 `EXIT=3`(本轮空缺已结清)

写 §九 时它仍在跑(按 `/proc/<pid>/fd/1 → /tmp/sc.log` 定位到
`pid=479 cmd=bash tools/agent/routine_selfcheck.sh`,⛔ **未用 `pgrep`**),
⛔ 未空转等它。收尾后它跑完,真码到手:

```
EXIT=3
legs run              : 13
FINDINGS (exit 3)     : cadence queue-rulings owed-executions lua-coverage
UNCERTIFIABLE (exit 2): trunk-red(python)
NOT RUN (inside a leg): tests/test_lua_gate.py tests/test_luacheck_gate_soakswitch.py tests/test_selfcheck_lua_leg.py
selfcheck worst exit: 3
```

⛔⛔ **那个 `EXIT=3` 不是 harness 报的 `completed (exit code 0)`** —— 后者读的是复合命令
末尾 `echo` 的码、**恒为 0**,与自检的判决无关。📌 **本轮第一条命令栽的正是这个坑
(evidence discipline 3),它在收尾又出现了一次**;上一轮的报告已经预告过这一点,本轮照读不误。

⭐ **FINDINGS 集合变了**:上一轮是 `unlanded cadence queue-rulings owed-executions lua-coverage`,
本轮 **`unlanded` 不在里面**,该腿逐字 `OK: no unlanded work in the certifiable window.`
⛔ **只登记集合差,不做归因** —— 脚本在同屏禁了这种读法,逐字:`Read this before writing
"exit 3, all of it cadence": that sentence was hand-made attribution for days, and on
2026-08-27 it was wrong for 22 hours (GH #267).` ⚠️ 本轮四次 push 全部落地是**同期事实**,
⛔ **不据此宣布是它让那条腿转绿的**。

`trunk-red(python)` 仍是 **`UNCERTIFIABLE`**(`50 checks, 0 failures, 9 uncertified`;
脚本自陈 `it is NOT evidence that trunk is red`)⇒ ⛔ **仍不写 trunk 绿也不写 trunk 红**
(§〇 原判不变);另三个测试 `NOT RUN (inside a leg)` —— **没跑不是通过**。
另 `UNCERTIFIABLE -- luacheck is not installed, so the behavioural half did not run.`

**已跑完腿的实读**:`registry rows: 83` / `9 anchor(s) checked -- OK` /
**`FROZEN none`**(`pullcad` 陷阱为零)/ `promote-atom constraints: OK` /
`RIDESHARE: 37` / `total open requests: 145`;
cadence **`GAP director ×2 / hero ×2 / strategy`** ⇒ ⭐ **本台无 cadence GAP**(与上一轮同)。

`LUA GATE COVERAGE disk 506 | push gate 341 | 开工自检 leg 134 | known_red 6`,
`UNCOVERED 115 of 506 (23%) {'no_manifest_row': 53, 'timed_out': 60, 'too_slow': 2}`,
**`UNCOVERED SET GREW -- 3 file(s)`**:`test_campbind_poke_real_frame.lua`(**新**)、
`test_dusttower_dive_guard.lua`(**第七轮**)、`test_fieldsip_transfer_receiving_site.lua`(**第五轮**)。
⇒ 对照上一轮 `disk 503→506` / `push gate 339→341` / **`UNCOVERED 114→115`**:
**本轮 3 个新落地的 Lua 测试,一个都没进自检腿,缝宽了 1。**
⚠️ `leg 134` **连续第三轮逐位相同** ⇒ 按脚本自己的要求跨轮读,**集合没长,是容器速度**
(`5a0` 的 120s per-leg 预算)。

⚠️ 顺带:`gh801_confirm_headroom_first_live_read` 在自检里也打 **`UNCERTIFIABLE`**,
与 §一 登记的 **OWED** 一致(executor = 批测台下一次跑 `check_costs.sh` 的那一轮)。

---

## 十一、Token 用量

```
TOKENS total_in=4,072,186 out=41,215 turns=39
```

(注:统计时刻之后的收尾回合不计入。)
