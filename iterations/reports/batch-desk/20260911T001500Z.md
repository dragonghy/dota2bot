# 批测台 2026-09-11T00:15–00:2xZ(**发波轮 W64** —— 九月第一次靠一条裁定过闸 (iii))

**一句话**:闸 (iii) 从上一轮的 `THROTTLED` 变绿,**不是因为 MTD 降了(它反而涨了 $0.406),
而是因为总监按 GH #721 裁了 `$85` 的 operative ceiling**,且这条裁定**由闸自己从
`iterations/director_rulings.json` 读出来**(RULING 11),本台一个 flag 都没传。
四道闸 + 两道门全绿,**W64 四台 spot 全部起飞**,泄漏零。

---

## 一、AWS 与成本

- `session_setup.sh`:`AWS_SETUP_EXIT=0`,`AWS ready: arn:aws:iam::581436534924:user/dota2bot-agent`
  (容器无 AWS CLI,脚本自己装了 `aws-cli/1.46.1`)。
- `check_costs.sh`:`COSTS_EXIT=0`。**MTD `$78.253`**(免费 `budgets` 路径),
  `forecast 182.287`、`budget limit 100.0`、**`budget refreshed 2026-09-10T19:46:38Z`**。
  读数 ≥ `$35` ⇒ 脚本按章程自动花 **`$0.01`** 用 CE 复核,得 **`78.2534452025`**(吻合)。
- 起飞前 `running/pending` 为空;常驻仍只有 AMI `ami-0a990a26d89c66547`。
- **本轮支出**:EC2 一波(覆盖档 `$1.10`)+ CE `$0.01`。

⚠️ **上一轮的 `$77.847` → 本轮 `$78.253`,涨 `$0.406`,而本台上一轮零 EC2 支出。**
这与上一轮 GH #721 的立案读数同向:**MTD 里有不是本台花的成分**。本轮不重复那次归因
(逐实例对账要按 `ResourceId` 拆 CE,更贵且权限未知),只登记增量。

---

## 二、四道闸(按上一轮交棒 ② 的顺序:先跑不会自愈的那道)

### 闸 (iii) 预算围栏 —— `FENCE_EXIT=0`

**先跑它,因为闸 (i) 会自己变绿而它不会。** 第一次运行(不传 `--pending`)按 RULING 6
返回 **`UNCERTIFIABLE (exit 2)`**,逐字:

```
UNCERTIFIABLE: nothing is accruing at this instant, but 1 wave(s) above launched inside the
11.3h ActualSpend lag, so pending=$0.000 is NOT certifiable (Ruling 6).
```

⭐ **这正是 `wave_fence_ruling6_first_live_read`(GH #683)欠的那次活路径读数** ——
W63 末台 `2026-09-10T12:18:45Z` 落在滞后窗 `2026-09-10T08:28:38Z` 之后,
于是「什么都没在跑」**不等于** `pending=$0`。传本台自己的 `--pending 1.10` 后重跑得 `exit 0`:

```
actual (MTD)     : $78.253   <- re-read this run, never cached
pending waves    : $1.100   (launched, may not be in MTD yet)
planned          : $1.100
projected total  : $80.453
fence            : $80.00   <- lowest ACTUAL alert not yet crossed (Ruling 1)
brake            : $90.00   (owner's line, not derived here)
operative ceiling: $80.00   = min(fence, brake)
DIRECTOR CROSSING: ceiling $85.00 by ruling GH#721/director-20260910, expires 2026-09-30T23:59:00+00:00
headroom         : $4.547 after this wave
```

**结论行逐字照抄(它自己要求的)**:

```
WAVE_FENCE: CLEAR (exit 0) -- gate (iii) passes ONLY BECAUSE OF RULING GH#721/director-20260910.
The derived fence $80.00 was NOT met; the ceiling that was met is $85.00, and it is a ruling with
an expiry, not a reading. Do not copy either number forward; re-run the tool.
```

**RULING 11 的注册表行(每次都打,本轮是它的第一次活路径读数)**:

```
crossing registry: ruling GH#721/director-20260910 IS IN FORCE (read from
/home/user/dota2bot/iterations/director_rulings.json, not from a flag). It is applied below.
```

⭐ **`--director-crossing` 三个 flag 一个都没传**,裁定是闸自己读出来的。
⇒ 一次性结清三根棒:`gh721_crossing_ruling_first_live_read`、`gh729_standing_ruling_registry`、
`wave_fence_ruling6_first_live_read`。

**时钟披露(工具要求原样登记)**:

```
WAVE_FENCE CLOCK : the wave-accrual window above was anchored to snapshot instant ASSERTED by
the operator (--snapshot-instant) -- a claim, not a reading.
```

断言值 `2026-09-10T19:46:38Z`,取自 `check_costs.sh` 的 `budget refreshed` 字段。
⇒ 这同时是 `gh692_epoch_clock_first_live_read` 欠的活路径读数。

⚠️ **跨档配套义务**:本波**不跨任何新告警档** —— `$50` 早已跨过(`AWS state=ALARM`),
`$80` 仍 `NOT yet crossed`(projected `$80.453` 是**投影**不是 ActualSpend)。
⇒ **本轮 owner 不会因为这一波收到新的 Budget 告警邮件**。
但 `headroom $4.547` ⇒ **`$85` 之下最多还有约 4 波**,九月余量本轮起进入个位数。

### 闸 (i) 节流 —— `THROTTLE_EXIT=0`,且在**动作**时点重跑

独立运行(00:16:15Z):`anchor wave : W63 (4 machine(s))`、`unlock 2026-09-10T18:18:45Z`、
`margin : +21450s past unlock`。
**发波脚本内部紧挨四条 `spot_run.sh` 之前再跑一次**(GH #469:闸绑的是动作不是决定):
`THROTTLE_AT_LAUNCH_EXIT=0`、`margin : +21728s past unlock`,零轮询。

### 闸 (ii) 有没有新东西可测 —— 满足,**但走的是第二肢,这一条要分开写**

`bots/` 自 W63 的钉 `cfa966e3` 以来**有 6 个 commit / 5 文件 / +345-2**,
字面上第一肢成立。**但本台不拿它当理由**:这 6 个 commit 落的 5 个新 id
(`lionqfight` / `zusarcexec` / `anyhero` / `lvlany` / `tombhp`)与 37-id 臂串的**交集为空**,
⇒ 它们在**两条腿上都不 armed**,本波一帧都测不到。
**拿树漂移当「有新东西可测」在这里是空的。**
⇒ 实际成立的是第二肢:**当前树 + 测试集的累计配对种子 7 < 8**(W62 四粒 + W63 三粒)。

### 闸 (iv) 回收致盲 —— `RECLAIM_EXIT=0`,下一波 spot

在 W63 自己的记录上**发波前亲手跑**:

```
yield      : 3 paired seed(s) of 4 machine(s)
attribution: 0..1 machine(s) reclaimed before the flip -- a RANGE, because 4 machine(s) carry an unread field
immaterial : all 16 extension point(s) agree, so the verdict below does not rest on the unread field(s).
VERDICT: not blinded -- the wave delivered 3 paired seed(s).
NEXT WAVE: spot (the default; GH #158 unchanged).
```

⭐⭐ **GH #699 的物质性机制本轮第一次在活路径上救了场,而且救得看得见**:
W63 **四台全部** `survival was not read`,旧条文下这就是 `exit 2`;
新条文把缺失字段当成一个**带名字的洞**,在它所有可能取值上各算一遍,16 个延拓点一致
⇒ 给出裁定,并把归因打成**一个区间 `0..1`** 而不是一个数。
⇒ 结清 `gh699_materiality_gate_first_live_read`。
⚠️ **但区间就是代价**:W64 收割时要把 SIR 的 `CreateTime`/`UpdateTime` 写回 `machines[]`,
否则 W65 的归因还是区间(已写进 `W64_wave.json:harvest_notes(b)`)。

---

## 三、并池:**可并**,而 tombhp 那一半的理由在抽签池里不在 gate 里

W64 与 W62+W63 **可并池**,家族深度 **7 → 若四粒全配对可到 11**。逐条核过 5 个新 id:

| id | 未 armed 时 | 判定 |
|---|---|---|
| `lionqfight` | helper `return true` ⇒ 原合取项不变 | 干净 no-op |
| `zusarcexec` | helper `return true` ⇒ 原合取项不变 | 干净 no-op |
| `anyhero` | 落到原来的 `return false` | 干净 no-op |
| `lvlany` | helper `return tHeroes[1]:GetLevel() < nLevel`(含 nil 守卫) | 干净 no-op |
| `tombhp` | ⚠️ **不是 no-op** —— `IsSoakCandidate('tombhp')` 排在合取链中间,未 armed 时**整个分支变成死代码**,而此前它可达 | 见下 |

⭐⭐⭐ **`tombhp` 不断池,而理由不在代码里**:该分支的外层守卫是 Undying 墓碑僵尸的 modifier,
而 **`undying` 不在 `tools/batch_test/soak/hero_pool.txt`**(41 个英雄,`grep` rc=1,
**本轮重新读过,没有继承上一轮的结论**)⇒ 该 modifier 在本语料任何一局都不可能出现
⇒ 外层守卫恒假 ⇒ 该分支**两条腿上都不可达**。
**可达性可以由抽签池关闭,不是只能由 gate 关闭。** 这复现 2026-09-10T21:18Z 那一轮在
同一份 delta 上的结论,而它的**承重前提本轮是现读的**。

⚠️ **边界**:这一半主张的是「**按抽签池构造不可能**」,严格强于「gated」,但它**完全依赖**
`hero_pool.txt` 不含 `undying` —— 这正是 **GH #726** 要求做成断言的那一条。
若哪天 Undying 进池,`tombhp` 分支会同时变成可达**并且**让已钉的波与新波**静默断池**。

---

## 四、两道门

- **载体门 `CARRIER_EXIT=0`**(**derived,不是手写**,GH #276):
  `CARRIER_TERMS derived from 37 armed ids: 9 hero-scoped, 28 generic, 0 unresolved => 7 term(s)`、
  `CARRIER_GATE ids=9 seeds=4 exit=0`。
  ⭐ **这道门本轮真的拦了一次**:第一组候选 `10900-10903` **`exit 1`**,
  `odaoe term=obsidian_destroyer verdict=ABSENT carriers=none` ⇒ 换选种。
  ⚠️ **预登记的诚实边界**:四个 id 只吊在**一粒**种子上 ——
  `aimguard`(spirit_breaker,仅 10892)、`rotscope`(pudge,仅 10900)、
  `zusboltdom` 与 `zusult`(zuus,仅 10891)。**那一台被回收 ⇒ 这几个 id 本波读数归零而非变薄。**
  逐项覆盖:spirit_breaker 1 / pudge 1 / zuus 1 / crystal_maiden 2 / lion 2 /
  obsidian_destroyer 2 / skeleton_king 3。
- **接线门 `WIRING_EXIT=0`**:`all 37 armed ids wired on HEAD`(先落盘再取 `$?`,不接管道)。

**选种**:`10890 / 10891 / 10892 / 10900`。程序化扫遍**所有** `*_wave.json`
(154 个互异整数,最大 10889)排除历史用种 ⇒ 四粒可证未用过。

---

## 五、发波 W64

**树核对是发波脚本的第一条语句**(在任何花钱动作之前):
`REMOTE_MAIN=34ea0edd750b23f9450f0f4bbb351065cb2ab6b4` == `PIN=…34ea0edd…`,不等则 `exit 9`。
`--dry-run` 先行:`DRYRUN_EXIT=0`、`plan: 1 x SPOT c6i.4xlarge`,
**spot_run.sh 的波次预算闸没有打 `REFUSED`** ⇒ 未传也不需要 `--allow-short-watchdog`。

配置 `--count 1 --az <az> --slots 16 --rec-slots 8 --hours 2 --validate "<ARM> <seed> --games 12"`,
与 W39–W63 逐字节相同。`--rec-slots 8` **显式传**(默认是 1,W51 就是吃了默认被杀的那一波)。

| seed | instance | run_id | 请求 AZ | 实际 AZ | lifecycle |
|---|---|---|---|---|---|
| 10890 | `i-0720743124a0a4ca2` | `spot_20260911_002053_1_main_53db33` | 2a | 2a | **spot** |
| 10891 | `i-0004aa27a167c1dff` | `spot_20260911_002056_1_main_8d88d3` | 2b | **2c(re-aimed)** | **spot** |
| 10892 | `i-0f888f79d3c971b59` | `spot_20260911_002112_1_main_db97c7` | 2c | 2c | **spot** |
| 10900 | `i-0c69da87845ae432c` | `spot_20260911_002114_1_main_452004` | 2d | 2d | **spot** |

`LAUNCH_START=00:20:53Z` → `LAUNCH_END=00:21:17Z`(slate spread 24s)。

⚠️ **本轮有一次降级可报,不是四台直给**:us-west-2b 报
`InsufficientInstanceCapacity ... (reached max retries: 4)`,环内重瞄到 2c,脚本自己打了
`PLACEMENT MISMATCH requested=us-west-2b actual=us-west-2c re-aimed=yes`。
**降级阶梯第 1 级成功,无 `AZ RING EXHAUSTED`。** ⇒ 实际是 **3 个互异 AZ 跑 4 台**(2c 两台)。
⚠️ 按本台 W59 自己的发现,**AZ 分散不是种子间对容量事件独立的证据**(W59 是 4/4 分散仍丢 3/4,
其中两台在**不同 AZ 的同一秒**)—— **这一行不得用来关 GH #403**。

**市场类型的证据是 `InstanceLifecycle`**(四台全 `spot`,`describe-instances` 实读),
**不是 run_id 的 `spot_` 前缀**(那是硬写字符串,W62 的四台按需机也带它)。

**收割落地** `iterations/reports/batch-desk/waves/W64_wave.json`(发波同轮写,
`machines[]` 的 `status_code`/`create`/`update`/`ab`/`ba`/`arm_depth` 按设计留 null 待收割回填)。

---

## 六、收割与队列

- **收割:本轮不欠。** W63 已由上一轮收割(`W63_verdict.json` 在库,`machines[]` 四台齐全);
  S3 `validation/` 最新对象 `2026-09-10T13:02:15Z`(W63 自己的),**无更新对象**。
  ⚠️ 注意 `awsx s3 ls --recursive` **按 key 名排序不按时间**,要 `sort -k1,2` 才看得到真的最新。
- **queue.json 102 行**:`pending` 42 条,**无一条要求波次/专机/新机时**。
  40 条带 `director` 字段的全是 `ROUTED_ARCHIVE_*` / `APPROVED-SCAN` / `REJECTED` /
  `RECEIVED_NOT_SCHEDULED` / `DEFERRED_*` / `HOLD-DOMAIN-EMPTY`;唯一 `APPROVED_CONDITIONAL`
  的 `strategy-5b` 仍写 `director.wave=W6`(历史波次)。
  两条**无** `director` 字段的 `pending`(`hero-55`/`hero-56`)逐条读过,
  **都自述零 EC2、不请求发波**(一条是归档只读重扫,一条是 fixture 入集裁定请求)。
  ⇒ **§4a 无适用对象**,走 §4b 例行波。

---

## 七、泄漏检查:**零**

`wave_fence.py` 的 accrual 腿(**全账号 17 个已启用 region,region-complete,比
`check_costs.sh` 的单区读数强**)在发波后读到**恰好 4 台**,全是本波的:

```
accruing : i-0720743124a0a4ca2 c6i.4xlarge [running] us-west-2 launched 2026-09-11T00:20:55Z
accruing : i-0004aa27a167c1dff c6i.4xlarge [running] us-west-2 launched 2026-09-11T00:21:12Z
accruing : i-0f888f79d3c971b59 c6i.4xlarge [running] us-west-2 launched 2026-09-11T00:21:14Z
accruing : i-0c69da87845ae432c c6i.4xlarge [running] us-west-2 launched 2026-09-11T00:21:17Z
```

⇒ **无第五台**,外来 `r7a`/`c7a` 本轮 region 内 0 条;常驻仍只有 AMI `ami-0a990a26d89c66547`。
⛔ **GH #515/#528 的立案理由一字未变**:工具第一行仍是
`budget filters : none  <- so every instance in the account does land in this budget, whoever owns it`,
四台也照旧打 `project=(untagged)`。

---

## 八、铁律 10 / 11

- **铁律 10 自检**:⚠️ **管道坑第 13 次** —— 本轮第一条命令仍然接了 `| tail`,
  **又是工具拦下来的不是纪律拦下来的**:`REFUSED: routine_selfcheck.sh stdout is a pipe;
  exit 2, nothing checked.` / `SELFCHECK_EXIT=2  REFUSED (nothing was checked; this is NOT a pass)`。
  **「工具拦住了」不等于「这轮没犯」。** 改用重定向 + 后台重跑,拿到真码:
  **`SELFCHECK_EXIT=3`**,`legs run : 11`,**`UNCERTIFIABLE (exit 2): none`**,
  `FINDINGS (exit 3) : cadence queue-rulings owed-executions a-evidence-owed
  trunk-red(python) trunk-red(lua)`。
  两条 trunk 红:`trunk-red(python)` = **`120 passed, 2 failed, 3 uncertifiable`**;
  `trunk-red(lua)` = **`4 of 87` Lua detector 文件**,其中
  `test_focus_mana_cost_consumer_census.lua`(`expected n=26 … got n=27`、
  `the control must sweep the same 173 frames, saw 176`)与
  `test_lion_ult_reserve_domain.lua`(`the tree now holds 7 rank-2 Finger instants … not 2`)。
  ⚠️ 该腿另有 9 项 `UNCERTIFIABLE` 的**自测**(120s 上限没跑完),工具自己写着
  `the same 50 files measured 99.5s here and 133.3s on the batch desk (GH #358)`
  —— **那是对自测的限定,不是对上面两条红的限定**。
  ⭐ **两条都是「登记表形状」族**(GH #650/#705/#718/#585 同族):**语料多了一份 fixture
  就把写死计数的断言顶红**,不是数据变了。⛔ 本会话 `bots/`/`game/`/`tests/` **一行未改**
  ⇒ **无一条红由本轮引入**;未做 `git stash` 复跑,故**不宣布「这是 main 的红」**。
  **不开重复 issue**(#718 已把这一族收进一张单子)。
- **铁律 11**:本轮**没有任何工具返回 `requires approval`**,**没有任何等待**。

---

## 九、交棒

1. ⭐⭐⭐ **下一轮本台 = 收割 W64**(`…_53db33` / `…_8d88d3` / `…_db97c7` / `…_452004`,
   树 `34ea0edd`,臂串 37 id / 335 字节 / md5 `b525d51d4b4957e0e40f22f203aea641`)。
   **必做四条,已写进 `W64_wave.json:harvest_notes`**:
   (a) 趁 SIR 还在读 `Status.Code`;
   (b) ⭐ **把 `CreateTime`/`UpdateTime` 也写回 `machines[]`** —— W63 四台全缺它,
   闸 (iv) 只能给区间;(c) `ab`/`ba`/`arm_depth` 回填**本文件**不只是 verdict;
   (d) 走 `recover_verdict.py` 全量重算,照抄 `min_arm_depth`/`thin_arm_seeds`,
   被排除的种子**仍要点名**。
2. ⭐⭐ **下一轮本台 —— 三根棒在 W64 收割那一轮到期**:
   `gh696_winrate_measurable_first_live_read`(下一次 `recover_verdict.py`)、
   `overchase_new_body_first_reading`(`overchase` 在本波 armed,**这是新函数体的第一份波次读数**)、
   `gh454_cost_constants_rerule` 的两条腿((乙1) 账单侧单波 > `$1.10`、
   (乙2) 自终机机时均值 > `1.00 h/台`)——**两条都不触发也要报**。
3. ⭐⭐ **总监/owner —— 九月余量进入个位数**:`headroom $4.547 after this wave`
   ⇒ `$85` 之下**最多还有约 4 波**。而 MTD 在本台上一轮零 EC2 支出的情况下
   仍涨了 `$0.406`,**与 GH #721 立案的那条「MTD 里约 55% 不是本台花的」同向**。
   GH #721 的另外两条路(#528 那台机器的去留、#515 给预算加 cost filter)**仍 open**。
4. ⭐ **harness/总监 —— GH #726 仍 open**:本轮的并池结论**再一次**完全依赖
   「`undying` 不在 `hero_pool.txt`」。这是连续第二轮把同一个未加断言的前提当承重墙。
5. ⭐ **总监 —— 开工第一条命令的管道坑第 13 次**,连续十三轮同一位置、同为当轮第一条命令,
   仍靠工具自己拦。工具拦得住,纪律拦不住 ⇒ 这已经不是提醒能解决的形状。
6. ⭐ **总监 —— trunk 两条红仍在**(python + lua),来源都不是本台;
   族属 GH #718 那张单子,本轮**不开重复 issue**。

**下一轮本台**:① **收割轮**(W64 起飞 00:21Z + 2h 看门狗 ⇒ 约 `02:30Z` 后数据齐);
② 闸 (i) 锚点 = W64 末台 `2026-09-11T00:21:17Z` ⇒ **解锁 `06:21:17Z`**
(**读工具打的那一行,不要自己算**);③ ⚠️ **开工第一条命令重定向 + 后台 + 不设短 `timeout`**(第 14 次);
④ 围栏/闸/成本**一律当轮现跑**,本报告所有数字作废。

---

## 十、铁律 6 读数(三条腿)与**推送门抓到的是本轮自己的缺陷**

`ARM_HOOK_EXIT=0`(`core.hooksPath=.githooks, already set`)。
**未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行。**

⭐⭐⭐ **第一次 `git push` 被拒,而红的是本轮自己写的产物 —— 这正是 GH #616 那条腿的立案形状。**
`PUSH1_EXIT=1`,`py gate: 95 ran, 1 findings`,逐字:

```
FAIL  W64 records gate (iv) first half, inputs -- no harvest owed under a `gates` key
      matching one of ['inputs', 'harvest']
FAIL  W64 records gate (iv) second half, GH #271 reclaim-blind check under a `gates` key
      matching one of ['reclaim_blind']
```

**诊断**:两道闸**都真的跑了**(`RECLAIM_EXIT=0`;收割不欠也现查过 S3),
但我把它们写在了棘轮点不到的名字上 —— 回收那半叫 `gate_iv_reclaim`(少了 `_blind`),
不欠收割那半根本没放进 `gates` 而是留在顶层 `harvest_owed_at_launch`。
**⇒ 改的是记录不是门**:`gate_iv_reclaim` → `gate_iv_reclaim_blind`,
并把「不欠收割」的实据(W63_verdict.json 在库 + `machines[]` 非空 + 本轮 S3 重扫)
写成 `gate_iv_inputs_harvest`。⛔ **没有编造任何值,也没有去 `REQUIRED_GATES` 加别名**
——工具自己写着「Do NOT invent a value to turn this green; that is the exact failure this
file was written against」,而本轮确实不是有意改名。
`tests/test_wave_gate_keys.py` 复跑 **`428 checks, 0 failed`** 后重推。

⭐ **顺带买到一条方法教训,已写进 `W64_wave.json:gates.gate_iv_inputs_harvest`**:
`awsx s3 ls --recursive` **按 key 名排序不按时间**。本轮第一次扫 S3 读 `tail`,
看起来最新只到 `09-08`,**而真正的最新是 W63 自己的 `09-10T13:02:15Z`**;
要 `sort -k1,2` 才对。**读一份没排序的清单的尾巴,正是「一波没被收割而当轮报告说干净」的做法。**

三条腿最终读数:

```
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)      # luacheck bots game: 0 warnings
py gate: 95 ran, 0 findings, 0 uncertifiable, 28.5s
lua gate: SKIPPED BY SCOPE -- this push touches no bots/game/tests path.
```

`PUSH1_EXIT=0`(`claude/great-sagan-hkbgo1`,新分支)/ `PUSH2_EXIT=0`
(`34ea0edd..cc9cb4a4  HEAD -> main`,**未被拒,无需 rebase**)。
动态半(GH #124,~100min)**未跑,不声称**。
⚠️ `lua gate` 那行是 **SKIPPED BY SCOPE 不是 pass** —— 本会话 `bots/`/`game/`/`tests/`
确实一行未改,所以它无可跑,但**这不是一条「Lua 是绿的」的陈述**
(第八节那 `4 of 87` 条红仍在 trunk 上)。

---

## 十一、本轮 token 用量

`TOKENS total_in=9,974,715 out=54,853 turns=67`(到统计时刻为止;统计后的收尾回合不计入)。

**收尾复查**:四台 `InstanceLifecycle=spot` 仍 `running`(`describe-instances` 实读),
2h 看门狗自毁,**无第五台**。
