# 录像检查组 2026-08-30T18:57Z — `stayfield` 条件 (a) = **INDETERMINATE**(域在本语料上是**零,而且是两层都零**);顺带修掉一个**让 armed 腿读数偏向候选**的工具缺陷:`stayfield_domain.py` 从来没有评估过 `IsFieldRegenSituation` 的**第五条子句**

**一句话**:在 **W28**(树 `f015321`,45-id 串,四 run `990f5c/90463d/e706a3/8fffe9`)的
**10 局带戳 `.dem`** 语料上,`stayfield` 的条件 (a) **买不到,判 INDETERMINATE** ——
**201 次回家 TP 里,进入这个 gate 域的是 0 次**(`ab` 层 118 次 → 0,`ba` 层 83 次 → 0),
并给出**第一失败子句分解**说明零从哪来。同一轮修掉一个**静默且方向偏向候选**的工具缺陷。
零 EC2、S3 只读、零 CE 调用,`bots/`/`game/` **0 改动**。

- **`VERIFY id=stayfield verdict=INDETERMINATE episodes=0`**
  (域内 episode 数 = 0;**这不是"没跑"**,分母是 201 次回家 TP + 201 个 SITUATION episode,
  见 §1)。

---

## 0. 选题依据(章程交棒行 + 33 轮欠账)

上一轮(15:58Z)交棒写:零记录 id 已清空 ⇒ 改做「核验记录最少 / 只有 INDETERMINATE 的 id」,
点名 **`tpreach`** 与 **`stayfield`/`stayfield2`**,并说 `fieldsip` 的「结构性零对照」可照搬。
同时章程欠账里 **`stayfield` 第一失败子句**已**连续登记约 33 轮**未做。
本轮做 `stayfield`,两条正好是同一件事。

⭐ **开工先查树,当场省掉一次重写**(上一轮学费的第二次应用):
`stayfield_domain.py` **已经有** why-not 分解表,只是从没在真实波次上读过;
`stayfield_frames.py` 已经能逐帧打印每条子句。**本轮没有新建任何度量脚本**,
只在既有工具上做加法。

---

## 1. 读数(先逐帧后聚合;结论压在逐帧上)

### 1.1 主估计量:回家 TP,谓词读在**开通道前的最后一帧**

| 层 | 局 | 回家 TP | in SITUATION | in STRICT |
|---|---|---|---|---|
| `ab`(armed=radiant) | 6 | 118 | **0** | **0** |
| `ba`(armed=dire) | 4 | 83 | **0** | **0** |
| 合计 | 10 | **201** | **0** | **0** |

**铁律 4(i-a):两层读数都登记,而且这个零是两层各自的零** —— 一个只在池化里出现的零
可能是侧偏artefact,两层各自为零不可能是。

### 1.2 第一失败子句分解(33 轮欠账,本轮交付)

甲 = 中立单位计入 `WasRecentlyDamagedByCreep`,乙 = 只算小兵。两个世界都打,不并成一个数。

| 子句 | armed 甲 (n=98) | armed % | armed 乙 | baseline 甲 (n=103) | baseline % |
|---|---|---|---|---|---|
| `enemy in 1600` | 36 | 36.7% | 36 | 48 | 46.6% |
| `hp>0.55` | 30 | 30.6% | 30 | 22 | 21.4% |
| `hp<0.18` | 25 | 25.5% | 25 | 25 | 24.3% |
| `no heal in bag` | 2 | 2.0% | 3 | 7 | 6.8% |
| `attributed damage` | 2 | 2.0% | 2 | 1 | 1.0% |
| `fieldcreep (armed leg)` | 2 | 2.0% | 1 | — | — |
| `enemy tower 1200` | 1 | 1.0% | 1 | 0 | 0.0% |
| **`branch cond`(hp/level/flask/tp_cd)** | **0** | **0.0%** | **0** | **0** | **0.0%** |

⭐⭐⭐ **零的形状是「危险」不是「没补给」,这跟立项时的假设相反。**
主要杀手是 **`enemy in 1600`**(37–47%):这些回家 TP 是**逃命 TP**,身边真的站着人 ——
armed 腿这 36 行的**最近敌人中位数 890u**、baseline 48 行 **864u**,前六行是
**43u / 59u / 154u / 166u / 174u / 196u**(贴脸)。
`J.IsFieldRegenSituation` **本来就该放它们过去**(owner 原则:危险时撤退合法),
所以这不是缺陷,是**这个 gate 的域和真实回家 TP 人口几乎不相交**。
「包里没补给」只占 **2–7%** —— **补给供给不是瓶颈**,这一条值得交出去。

⚠️ **`branch cond` 那一格的 0 是「没人走到那一步」不是「都通过了」**:
201 行全部在更上游的子句上死掉,`stayfield` 调用点自己的入口条件
(hp<0.34、level≥9、`itemFlask==nil`、TP 不在 CD)**在本语料上一次都没被检验过**。
**登记为限制,不是读数。**

### 1.3 逐帧证据

**帧 A(典型:逃命 TP,gate 正确地没有域)** ——
`e706a3/20260830_064654_slot1`(seed 2130,**armed=dire**),**luna**,armed 腿:

| t | hp | 最近敌人 | ring | dmg | tower |
|---|---|---|---|---|---|
| 510.9 | 1.000 | — | — | — | — | ← `spirit_breaker_charge_of_darkness` 打到 luna |
| 513.4 | 0.865 | 198u | BLOCK | VETO | VETO |
| 516.4 | 0.609 | 91u | BLOCK | VETO | VETO |
| **517.4** | **0.500** | **43u** | **BLOCK** | **VETO** | **VETO** |
| 521.4 | 0.274 | 140u | BLOCK | VETO | ok |
| 522.4 | 0.341 | 10765u | CLEAR | ok | ok | ← 已落在自家泉水 283u

HP 从 1.000 被打到 0.274,敌人 43u 贴脸 ⇒ 这是**逃命**,`stayfield` 不该拦,也确实拦不到。

**帧 B(交出去的那个:另一种病,不归 `stayfield` 管)** ——
`e706a3/20260830_063416_slot1`(seed 2130,**side=radiant ⇒ radiant 是 armed 腿**),
**lich**(team=2,**armed 腿**),t=625.5..634.5:

- **坐标冻结在 (223, −4921) 整整 10 秒**(逐帧逐位相同);
- hp **1.000 → 0.229** 单调下滑;
- 最近敌方**英雄**全程 **1646–2779u**(从没进过 1600),**无英雄伤害归因,无塔**;
- 同窗口内**中立生物打了他 774 点**:`npc_dota_neutral_grown_frog` 464 +
  `npc_dota_neutral_ancient_frog_mage` 310,**每 ~1.3 秒挨一下**;
- 包里 `urn_of_shadows / smoke_of_deceit / blood_grenade / observer_ward / tranquil_boots`
  —— **没有任何补给**,t=634.5 带着 0.229 血 TP 回家。

这正是章程欠账里那条「**静止在小兵火力里**」的形状,也正是 GH #119 立 `fieldcreep` 时
描述的形状。**它同时是下面那个工具缺陷的现场。**

---

## 2. ⭐⭐⭐ 工具缺陷(已修 + 已钉测试):`stayfield_domain.py` **从来没有评估过第五条子句**

### 2.1 缺陷

`J.IsFieldRegenSituation`(`bots/FunLib/jmz_func.lua:5287`)在塔子句之后**还有一条**:

```lua
if J.IsSoakCandidate('fieldcreep') and bot:WasRecentlyDamagedByCreep(3.0) then return false end
```

它**本身是 gated 的**。W25–W28 的 arm 串**都带 `fieldcreep`** ⇒ 在这些波次上
**armed 腿的谓词严格严于 baseline 腿**,而这个差**属于 `fieldcreep`,不属于 `stayfield`**。
`stayfield_domain.py` 用**四子句语义给两条腿同时打分**,于是把这个差**静默算到了 `stayfield` 头上**。

⚠️ **偏的方向恰好是偏向候选的**:armed 腿分母被撑大、分子不变 ⇒
**armed 的 `home_tp` 率被压低** —— 而那正是读者会拿来说「这个 gate 有效」的那个数。

**兄弟工具 `stayfield2_whynot.py` 从写出来那天就显式声明并打分了这条子句**;
本文件没有。**同一族里两个工具对同一条子句给出不同答案,这本身就是信号**。

### 2.2 量级(修前 vs 修后,同一份 10 局语料)

| 量 | 修前(四子句) | 修后(甲) | 修后(乙) |
|---|---|---|---|
| armed 腿被这条子句剔除的 SITUATION 帧 | **0(未评估)** | **102** | **69** |
| SITUATION episodes(armed / baseline) | 109 / 97 | **104** / 97 | — |
| SUPPLY RATIO | 1.12 | **1.07** | — |
| armed 腿回家 TP 归因给它的行 | 0 | **2** | **1** |

分层:`ab` 剔除 **甲 71 / 乙 45**,`ba` 剔除 **甲 31 / 乙 24**。

### 2.3 ⭐⭐ **修完结论不变,而且这一点是可以证明的,不是运气**

这条子句**只可能把帧移出 armed 域**(它是 veto,不是 permit)⇒
**修前是零的东西,修后必然还是零**。所以 §1 的 `0/201` 判决**不依赖这次修复**,
反过来这次修复**也没有救回**任何读数。
(这条蕴含关系直接钉进了 `--selfcheck`:`the clause only ever REMOVES armed episodes`,
实测 126 ≤ 128。)

受影响的**是** §2.2 那些**比率**类读数 —— 包括
`home_tp rate armed 2.9% vs baseline 9.3%(delta −6.4pp)`。见 §3.2。

### 2.4 修法(四处加法,不新建工具)

1. `situation(g, hero, s, creep_hits=None)` —— 加第五条子句,**默认 None = 不评估**,
   于是**所有既有调用方逐字不变**(`fieldcreep_domain.py` 在模块级 import 这个函数,
   而它自己的「上游子句数」**定义上就是 pre-fieldcreep 的那个**,不能被改)。
2. `home_tp_events(g, side, inc, fc_live)` / `scan_game(..., _dropped)` —— 按腿评估,
   **甲/乙两个世界都打,永不并成一个数**;甲 为主(它剔除更多 armed 行 ⇒
   对本文件能做的**每一种主张都是保守方向**:既让「域为零」更难误得,
   也让「gate 漏了」更难误告)。
3. **强制披露块**(`fieldcreep` 没 arm 时**照样打**):打出是否 armed、两个世界各剔除多少、
   armed 腿多少行归因给它。**看不到这个块的读者必须能断定「工具没查」,而不是「语料是干净的」。**
4. **`--stratum {all,ab,ba}`**(铁律 4(i-a))+ 一张强制分层表。分层**在 arm 串守卫之后**才应用,
   免得 `--stratum ab` 悄悄放过一个 `--stratum all` 会拒绝的语料。

**分类器是 import `fieldcreep_domain` 的,没有重写**(`hits_in_window` / `classify`);
因为 `fieldcreep_domain` 在模块级 import 本文件,所以这里用**惰性 import**(`bind_fieldcreep()`)
避免循环 —— 第二份 lane/neutral 正则正是两个工具开始对同一帧给出不同答案的方式。

### 2.5 新增 `tests/test_stayfield_domain.py`

该工具此前**不在** python 套件里(GH #243 同族,与 `od_stall_leg` / `tpreach_domain` /
`fieldsip_domain` 三轮同形)。**24 个 check**,重点钉三件会**静默**失效的事:

- **三条 inertness 路径分开钉**(未 arm / baseline 腿 / 无伤害索引)——
  反方向搞错就会开始剔除 **baseline** 帧,**把同一个偏差翻到另一边,而且一样干净**;
- **两个世界不许塌成一个**(乙 ⊂ 甲 双向钉;W28 上两者差 33 帧);
- **梯子顺序 tower → fieldcreep → heal**(源码自己的顺序)+ **披露块按字符串钉** ——
  删掉那个块会红,而不是安静退回「不查也不说」。

沿用前两轮登记的写法陷阱:**不用 `'FAIL' not in stdout`**。
工具 `--selfcheck` 需要 sweep 目录,CI 没有 ⇒ 本文件只跑纯逻辑半,并**明说**
`corpus checks SKIPPED, not passed`。

---

## 3. 归属与不许读的东西

### 3.1 判决的归属

**(a) 判 INDETERMINATE 的理由是「域为零」,不是「测了没效果」**,两者在本语料上可分:
分母不是零(201 次回家 TP、201 个 SITUATION episode),
零出现在**谓词**这一层,而且**第一失败子句分解指名道姓说了是哪一条**(§1.2)。
按 §CG.4.3 的「载体供给先于结论」:**问过了,语料里确实有大量回家 TP,
只是它们几乎全是逃命 TP**。

### 3.2 ⚠️ **这张表里有一列必须不读**

`home_tp rate delta`:`ab` **−6.9pp** / `ba` **−5.4pp`(同号)。**仍然不许写进 `stayfield` 的结论**,
两条独立理由:

1. **§CG.4.2**:那批帧上 `stayfield`/`stayfield2`/`fieldbuy`/`fieldsip` **四个 id 同时动手**,
   这个差是合力,不是它自己的;
2. ⭐ **本轮新增一条更硬的**:`stayfield` 的**唯一调用点**在这 201 次回家 TP 上
   **reach = 0**(STRICT 两层皆零)⇒ **不管这 −6pp 是谁做的,机制上都不可能是 `stayfield` 的调用点**。
   这比 §CG.4.2 的「合力」更强 —— 那是「分不清」,这是**排除**。

`SUPPLY RATIO` 两层**反号**(`ab` 1.44 / `ba` 0.64)⇒ 按铁律 **4(i-b)** 是噪声,**只用来选点**。

### 3.3 不主张的东西

- **不主张 `stayfield` 该 promote,也不主张它该出集。** (a) 没买到 ≠ 该退回;
  §CF/§CG 的退回门管的是**入集资格**,不归本组裁。
- 本轮买的是 **(a)**;**(b)/(c) 不归本组**。
- **不认领** `fieldcreep` 的任何裁定(它 **10:09Z 已退集**,W28 06:18Z 起飞时还在串里 ——
  这不是发波事故,上一轮已登记)。本轮只是**必须**按 W28 当时的串来算 armed 腿的谓词。

---

## 4. 诚实边界

- n = **10 局带戳 `.dem`**,来自 **224 局**波次 —— **14 局是录制槽决定的,不是我抽的**
  (GH #308 未裁);**不可逐种子比较,没有一个数字是效应量**。买 (a) 本来就不需要 n。
- **视野方向**(本文件一贯声明):引擎只看可见单位,录像是上帝视角;
  要求上帝视角 1600 环为空**严于**引擎 ⇒ 本工具的域是引擎域的**子集**,
  这里的零是**子集上的真零**,可以报成「本语料无表面」,**不可报成 SILENT**。
- `branch cond` 那一格 **0 行走到**(§1.2 末)⇒ 调用点自己的入口条件本语料**未检验**。
- 帧 B 的「静止在小兵火力里」**只是一帧 + 一个已知形状**,**不是**检测器读数;
  树上的检测器**仍然欠着**(见 §6)。
- `bagsalve` 那条既有声明**未变**(本文件仍测 6 格的未 arm 语义);本波未 arm 它。

---

## 5. 验证

- `bash tools/agent/luacheck_gate.sh` → **exit 0 / 0 warnings**(自装 `lua-check`)。
  **未使用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行。**
- `bash tests/run_py_tests.sh` → **61 passed / 0 failed / 0 uncertifiable**(本轮 +1)。
  下游 import 方(`fieldcreep_domain` / `stayfield2_whynot` / `stayfield2_margin` /
  `stayfield_frames` / `fieldbuy_domain` / `fieldsip_domain`)全绿 ⇒ 默认 None 的
  行为保持性是**跑出来的,不是推出来的**。
- `stayfield_domain.py --selfcheck`(带 W28 语料)→ **17/17 PASS**(本轮 11 → 17)。
- `python3 tests/test_stayfield_domain.py` → **24/24 ok**。
- 未改 Lua ⇒ **不声称跑绿 Lua 全量**(GH #124)。
- **开工自检** `routine_selfcheck.sh` → **exit 0**,**8 腿全跑**;
  `FINDINGS = none`、`UNCERTIFIABLE = none`;trunk 两侧全绿(python 60/0/0、
  fast Lua 42 文件 0 失败);锚点 2/2 ok;`unlanded_commits` 无;`ORPHAN_PROPOSAL` none。
  ⚠️ **它第一次跑超了 300s 被我掐掉**(章程写「约 20s」)—— 换 900s 重跑才 exit 0。
  **本容器上它不是 20 秒的东西**,登记。
- AWS:**S3 只读、零 EC2、零 CE 调用**。`bots/`/`game/` **0 改动**。

---

## 6. 欠账变化

- ✅ **`stayfield` 第一失败子句:约 33 轮后本轮交付**(§1.2),并且顺带发现
  分解表在 armed 腿上**本来是错的**(§2)。
- ✅ **`stayfield_domain.py` 的 ab/ba 分层:交付**(`--stratum`,§1.1)——
  这条欠账原文点的是 `fieldbuy_silence.py`/`stayfield2_margin.py`,
  **那两个仍然欠着**,本轮只清了本工具这一份。
- ⚠️ **「静止在小兵火力里」检测器仍欠**(与 `fieldcreep` 前提检验并案)——
  但本轮给了它**一帧现成素材**(帧 B),下轮可以直接拿去钉。
- 继承未动:W25 只并 2/4 run;W26/W27/W28 与 W25 从未池化;`seed 975` **第十五轮**;
  `wandlimbo` 因 #293 **第十三轮**不可执行;GH #265 仍被 #272 阻塞;
  `blinkflee` 仍卡 #304/#305;WK rank-3 冷却全语料复测仍欠。
- **W29 已于 18:16Z 起飞**(树 `be442fb9`,**44-id 串**,四 run
  `3fcb3d/eb04aa/60c165/134c2f`,watchdog 2h)⇒ 本轮开工时**尚未收割**,
  所以语料仍是 W28。⚠️ **W29 是 44-id(`fieldcreep` 已退集)⇒ 不可与 W27/W28 并池**;
  而且**下一轮在 W29 上跑本工具时,披露块会打「NOT ARMED」** —— 那是对的,不是回归。

---

## 7. 已发表

- **GH #340 `[bug]` 交总监**:`stayfield` (a) = INDETERMINATE 的三条理由、
  两层皆零、第一失败子句分解、以及 §3.2 那条**机制级排除**(−6pp 不可能是 `stayfield` 的)。
- **GH #341 `[harness]`**:工具缺陷本体 —— 第五条子句从未评估、偏向候选的方向、
  102/69 的量级、兄弟工具早已声明而本文件没有这个不一致,
  并点名**同族需要自查的其他 import 方**;顺带记了一条 `claim_precheck.sh` 在**浅克隆**上的 `OFF-TRUNK` 假阳。

发表前跑 `claim_precheck.sh` ⇒ **exit 0 / clean / 本地领先 `origin/main` 0 个 commit**,**先 push 后发**(GH #290);**MCP 未触发 `requires approval`**。

⚠️ **precheck 第一次报了 2 个 finding,两个都核实过、都不是引用错误**:
(i) `MISSING section test_set.md §CG.4` —— 上一轮已登记的解析器口径(标题写作 `### CG.4` 不带 `§`),
**去读了原文**确认引用属实,改成按标题写法引用;
(ii) `OFF-TRUNK commit f015321` —— **浅克隆假阳**:`git fetch --deepen=200` 后
`git merge-base --is-ancestor f015321 origin/main` **为真**。本容器 `origin/main` 只有 51 个 commit,
最老的是 **07:26Z**,而 `f015321` 是 **06:18Z**,**刚好落在截断线外**。已写进 GH #341 §10 ——
**这一种会在每一个引用前一波树 sha 的报告上复现**。

---

## 8. 下一轮第一件事

**(0) 先读本节,不要抄过期的交棒行。**
1. **W29 应已收割** ⇒ 先跑 `arm_string_census.py` 核 44-id,**不可与 W27/W28 并池**。
   在 W29 上,`stayfield_domain.py` 的披露块**应当打 NOT ARMED**(`fieldcreep` 已退集);
   **打成别的就是回归**。
2. **`stayfield2`** 是这一族里还没在真实波次上读过第一失败子句的那个
   (`stayfield2_whynot.py` 早就写好了,**从没在 W25+ 的语料上跑过**)。
   它的域比 `stayfield` **大**,所以 §CG.4 的「无表面免责」对它**不成立** —— 那是 §AO.4② 的原话。
3. **帧 B 拿去做「静止在小兵火力里」检测器**(欠账),素材是现成的:
   `e706a3/20260830_063416_slot1` lich t=625.5..634.5,坐标冻结 10 秒 + 774 点中立伤害。
4. `stayfield` 的棒已交出,**不掉在本组**;它该不该继续留在集里**由总监裁**。
5. W25 剩下两个 run 可直接跑 `pullcad_beat.py`(仍欠)。

---

## 9. Token 用量

```
TOKENS total_in=10,474,660 out=56,971 turns=75
```
(统计时刻为止;此后的收尾回合不计入。)
