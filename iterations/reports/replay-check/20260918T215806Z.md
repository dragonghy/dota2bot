# 录像检查组 — 20260918T215806Z(W99)

**一句话**:`pullthink_animactivity_instrument` 那一行逐字写着它是「四个仪器缺口里最便宜的一个,
dumper 一行都不用动」—— **loader 那一侧确实是一行;而那一行的代价本轮实测是 18 个测试文件 / 70 个
用例变红,其中包含一个已 promote(线上默认)的 id 的真帧断言**。价签不是估的,是跑出来的。

---

## 〇 覆盖与前置

- **宽扫 0/0 局;深查 0 局;VERIFY 行 0 条。** 批测台仍持刹车、无未检新局,且 **S3 本轮再次不可达**
  (见 §二)。⛔ **不硬凑 VERIFY 行** —— 本报告不含帧证据,这是有意的:本轮的问题是**仪器的价签**,
  不是某一帧的判读。
- **铁律 9(owner 优先项)**:P1(拉野)球在协同组第 1 项,P2(低血不回家)卡在
  `wandlimbo_charge_instrument` 那堵仪器墙上,**均非本组本轮可动**;本组无未完成 owner 优先项。
- **铁律 10 开工自检**:见 §四。
- **成本三段(RULING 48)**:**零 EC2 / 零 CE / S3 读取 0 个对象**(S3 不可达)。

---

## 一 头号:一条欠条的价签,从散文换成读数

### 1.1 这一行是什么

`iterations/owed_executions.json` 的 `pullthink_animactivity_instrument`(总监 2026-09-09 立,
`test_set.md §GF.2/§GF.4`),`executor` 逐字 **「录像组 / 任何要重新提 `pullthink` 入集的人」**,
并逐字给了买法与价格判断:

> ⭐ **这是四个仪器缺口里最便宜的一个,dumper 一行都不用动**:`meaningfulActivities` 那张表**是满的**
> …… 所以只需 `tests/mock/replay_fixture.lua` 为 `GetAnimActivity` **无条件装** getter

`done_when` 是 `text_absent`,要 `tests/test_blind_a_pulllane_pullthink.lua` 里这两句消失:
`ANSWERS 0, silently, via bot_api.lua` 与 `the cheapest of the four instrument gaps`。

### 1.2 先把「装什么」定下来:**服务是建模,拒绝才是本仓既有的药**

`grep -i "activity\|anim" tools/batch_test/behavioral/dumper/main.go` ⇒ **零命中**。
dump 里**根本没有动作状态这一格**,所以「无条件装 getter」能装的只有两种东西之一:
**(甲) 由相邻帧位移反推**(= 建模),或 **(乙) 出声拒绝**。
`tests/test_blind_a_pulllane_pullthink.lua` 的主轴自己已经把答案写在文件头(逐字):

> `GetLaneFrontLocation` — **REFUSES, loudly, by name** …… `GetAnimActivity` — **ANSWERS 0, silently** ……
> Same loader, same class of absent datum, opposite failure direction. **The refusing form is the CURE
> and it is already implemented** …… this round it is not a sibling getter that was missed but a whole
> **POLICY** the loader applies to one getter and not the rest.

⇒ 本轮按 **(乙)** 实装:在 loader 建好的每个英雄 handle 的 `__spec` 上装一个
`GetAnimActivity = function() error('LOADER REFUSES: …') end`,与 GH #61 的
`GetLaneFrontLocation` 同形同文案(需要动作状态的测试按 `rawget(h,'__spec').GetAnimActivity = ACTIVITY_*`
自己申报,这条路 `[2c]` 与 `test_pullthink_anim_throttle.lua` 今天就在走)。

### 1.3 ⭐⭐ 代价(实测,不是预测)

**基线先取,再取实验读数,两侧同一容器、同一棵树、唯一差别是 loader 那一个文件:**

| | 快 Lua 闸(`tools/agent/lua_gate.py --all`) |
|---|---|
| 基线(trunk,本轮实测抽样 5 个文件全 0 failures;W98 push 读数 `430 ran, 0 findings`) | **0 findings** |
| 装上拒绝后 | **`lua gate: 434 ran, 18 findings, 0 uncertifiable, 8 unanswered, 5 known-red, 648.5s`** |

逐文件(18 个文件 / **70 个用例**):

```
15  test_overchase_pursuit_tense.lua        5  test_roamidle_recovery_clobber.lua
13  test_roamreach_bounded_chase.lua        5  test_roamstale_collapse_action.lua
 6  test_arbheart_release_retires_camp.lua  4  test_arbheart_farm_camp_heartbeat.lua
 4  test_replay_260820_181711_wk_l1trade.lua 4 test_zero_true_sites_driven.lua
 2  test_arbheart_host_bid_role_gate.lua    2  test_blind_a_roamidle_campsel.lua
 2  test_invoker_cm_target_guard.lua        2  test_overchase_midline_margin.lua
 1  test_abil1st_first_unit_reader.lua      1  test_alchemist_rage_clock_staged_frame.lua
 1  test_creepthink_anim_throttle.lua       1  test_gated_getter_stub_control.lua
 1  test_pullthink_anim_throttle.lua        1  test_tpstamp_channel_baseline.lua
```

**分两类,分清楚很重要:**
- **(A) 这笔采购自己的设计触发器,4 个用例**(欠条的 `trigger` 逐字预告过「会自己变红」):
  `test_pullthink_anim_throttle [W1]`、`test_creepthink_anim_throttle [W1]`、
  `test_gated_getter_stub_control STUB0`、`test_overchase_pursuit_tense [blind] (2)`;
  另加**闸外**的 `test_blind_a_pulllane_pullthink [2b]`(单跑实测红)。
- **(B) 附带损伤,66 个用例 / 15 个文件**,**全部同一机制**:拒绝**从 shipped 代码内部抛出来**,
  测试是在**驱动真实决策路径**时撞上的,不是在断言那个 0。现场逐字(两例):

```
FAIL: test_roamstale_collapse_action.lua :: PROMOTED: the collapse frame attacks the INVADER with NOTHING armed
      LOADER REFUSES: GetAnimActivity is unresolved …
FAIL: test_arbheart_farm_camp_heartbeat.lua :: [armed] the heartbeat releases the latch and re-picks the next tick
      LOADER REFUSES: GetAnimActivity is unresolved …
```

栈(`test_overchase_pursuit_tense` 的 sweep 打全了,逐字):
`replay_fixture.lua:885 → jmz_func.lua:3849 in function 'IsRunning' → jmz_func.lua:3875 in function
'IsChasingTarget' → tests/_overchase_sweep.lua:226`。

### 1.4 ⭐⭐ 为什么 (B) 不是「测试写得不好」,而是本条欠条真正在说的事

`bots/FunLib/jmz_func.lua` 三个谓词**整条建在这一格上**:

- `J.IsRunning`(`:3845-3851`)= `bot:GetAnimActivity() == ACTIVITY_RUN`
- `J.IsAttacking`(`:3854-3871`)= 比 `ACTIVITY_ATTACK` / `ACTIVITY_ATTACK2`
- `J.IsChasingTarget`(`:3873-`)= `J.IsRunning(bot) and J.IsRunning(nTarget) and …`

`ACTIVITY_*` 在 mock 世界里是 `api.install` 自动解析的哨兵(**≥1001**,`[2c]` 钉着),而 catch-all
答 **0** ⇒ **这三个谓词在每一个 fixture 帧上恒为 FALSE**,`IsChasingTarget` 还是两次 `IsRunning` 的合取,
**恒为 FALSE 的更硬**。

shipped 调用点(脚本统计,按 `--` 前的代码段计,已排除注释):
**`J.IsRunning` 169 处 / `J.IsAttacking` 574 处 / `J.IsChasingTarget` 317 处,合计 1060 处,散布 149 个 `bots/` 文件**。

⇒ **这不是 `pullthink` 一个 id 的局部失明。** 它是**整个 fixture 世界的一条恒假前提**,而 §1.3 的
(B) 那 66 个用例正是**已经压在这条前提上的既有判词** —— 其中
`test_roamstale_collapse_action.lua` 那 5 个,按 `AGENTS.md` 的 promoted 名单,
**`roamstale` 是已 promote、在每一局 Turbo 里都活着的默认行为**;
`roamreach` / `ownhalf` / `arbheart` / `wk_l1trade` / `roamidle` 的真帧判词同理。

### 1.5 ⛔ 本轮因此**没有**买下这一行,并且这是保守侧的选择

把 18 个文件染红推上 main = 让**每一个组的 push 钩子**(`lua_gate.py`,红即拒推)当场停摆,
而本组**没有权限也没有语料**去逐个重判那 66 个用例。**改动已完整回滚**
(`git checkout tests/mock/replay_fixture.lua`,`git status` 干净;`bots/` + `game/` 一行未改)。
该行**保持 OWED、未认领**(⛔ 也没有按 `_claim_note` 抢先写 `claimed_by` —— 认领规矩要求
「先推认领再干活」,本轮是先量后判,不倒填)。

⭐ **交出去的不是「做不了」,是一个价签**:`done_when` 的两句散文可以一分钟删掉;
**真正的采购单是那 66 个用例**。三选一由总监裁(§五)。

### 1.6 独立佐证:仓里已有一把尺子在数这个族,而它数到 164

开工自检在我的实验**在飞**时撞红了 `tests/test_mockscalar_return_shape.lua`,逐字:

> `names the catch-all answers on EVERY live frame FELL to 163 (registered 164)` …
> `the sweep now finds 31 shipped Get* names that are never answered by the catch-all, not 30`

**164 → 163、30 → 31,恰好一个名字**,而那个名字就是 `GetAnimActivity` —— 这是**第二把独立的尺子**
在同一秒说出同一件事。**回滚后单跑该文件:`9 tests, 0 failures`**(⇒ 那抹红是我的实验,
**不是 trunk 的红,⛔ 不要给任何组发帐单**;这也是 GH #898 的又一例:自检与本轮工作并发)。
⭐ 顺带把族的规模说出来:**`GetAnimActivity` 只是 catch-all 在每个活帧上代答的 164 个 shipped getter 之一**,
而本仓为**其中一个**定价就花掉了一个总监轮 + 本轮。

---

## 二 S3 连续第二轮不可达,而它挡着两条**带时限**的欠条

`bash tools/batch_test/aws/session_setup.sh` 被 harness 权限分类器当场拒,逐字:

> `Permission for this action was denied by the Claude Code auto mode classifier. Reason: [Interfere With Workloads]`

(W98 同一命令读的是 `Reason: [Real-World Transactions]` —— **理由串换了,结论没换**。)
按**铁律 11 当场放弃、不空转**。

**代价不是「这一轮没语料」,是两条带时限的欠条在空转**:

| 欠条 | executor | 时限 | 本轮状态 |
|---|---|---|---|
| `tpreach_bc4_cell_reread` | replay-check | **09-26 永久悬崖**(W98 §1.4:88 个种子槽 09-19–09-26 之间全部到期,09-27 起为 0) | **未动,剩 8 天** |
| `gh290_od_execution_verification_needs_postfix_corpus` | **录像组,恰好一个** | T+15d(语料 W69,10-03) | **未动** |

两条的验收线都要 `.dem`(`tpreach_domain.py:381` 吃 `timelines/*.timeline.json`;
`skill_point_stall.py` 同理)⇒ **只要 S3 这条路被分类器挡着,本组就买不到它们**,
而 09-26 那条**过期即永久买不到**。⇒ §五交总监 + 已开 issue。

---

## 三 ⛔ 本轮明确没做

- `bots/` + `game/` **一行未改**(§1.2 的实验只碰 `tests/mock/replay_fixture.lua`,**已全量回滚**)。
- 零 soak id、零 fixture、零 promote/reject 建议。
- ⛔ 未改 harness / `owed_executions.json` / `test_set.md` / `queue.json` / `state.json`;
  ⛔ 未替总监退休任何行;⛔ 未删 `done_when` 要的那两句散文(删它就是给欠条制造满足)。
- ⛔ 零 AWS 调用、未拉任何语料。
- ⚠️ 本轮**安装了 `lua5.1` + `lua-check`**(`apt-get install -y lua5.1 lua-check`)—— `ensure_lua_toolchain.sh`
  本轮**静默失败**(`TC_EXIT=0` 但 `which lua5.1` 空、日志 0 字节),⚠️ 归因未查,记在 §五给总监。

---

## 四 开工自检

**交棒第 0 条本轮一次到位**:当轮第一条与自检有关的命令就是逐字那一行
`nohup bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1 &`,零附加物 —— **连续四轮之后第一次**。
(⚠️ 诚实边界:本轮**最开始**误用管道跑过一次,工具当场 `REFUSED` 并逐字自陈
`has recurred 5x, every time as the first command of the round`;此后按处方重跑,**该次 REFUSED 照登,
逐字「nothing was checked; this is NOT a pass」**。)

⛔ **真码仍不可得,连续第五轮同形**:日志 **699 行**,`pgrep -f '[r]outine_selfcheck\.sh'` 仍读到 pid,
**无 `selfcheck worst exit` 横幅**,⛔ 不读成通过。已跑完的腿逐字:
- `push gate armed (core.hooksPath=.githooks); \`git push\` now runs the rule 6 static gate`
- `OK: no unlanded work in the certifiable window.`
- `uncovered set unchanged from the baseline -- OK`
- `A-EVIDENCE-OWED  armed 24  verdict 24  owed-row 0  UNOWED 0  PRE-ARM 0`
  (⚠️ 按 W90 工具坑,`owed-row 0` ⛔ **不是「本组没有欠条」**)
- ⚠️ 一条 FAIL(`test_mockscalar_return_shape.lua`)**是我的实验造成的,不是 trunk 的红** —— 见 §1.6,
  回滚后单跑 `9 tests, 0 failures`。**⛔ 不给任何组发帐单。**

### 4.1 ⭐ 顺带订正一条本组自己传了两轮的读数取法

W97/W98 都按「executor 里出现『录像组 / replay-check』」的字串取本组欠条,读出 **29 条**。
本轮按**字段头**重取(`executor` 去掉 `——`/`(` 之后的首段),**只有 9 条头部是 `录像组`、另有若干写作 `replay-check`**;
被字串法错收进来的里面有 **`aimguard_creep_schema_identity`**,而它的 `executor` 逐字是
**`strategy(协同组 / [harness]) …… ⛔ 不派给录像组`** —— **字串命中的是那句「不派给录像组」本身**。
⇒ 与 W97 头号缺陷**同型第三例**(引文被计成判词);⛔ 本轮不改任何工具,只把取法写下来:
**按字段头取,不要按字串 grep**。

---

## 五 交总监(本轮四根棒)

1. ⭐⭐ **`pullthink_animactivity_instrument` 请按 §1.3 的价签重裁,三选一**:
   (甲) **接受采购单**:装拒绝 + 逐个重判那 66 个用例(含一个 promoted id `roamstale` 的真帧判词)——
   这不是录像组一个工作单元能吞下的,需要排期与分工;
   (乙) **改成 opt-in 拒绝**(loader 出一个 `rf.refuse('GetAnimActivity')` 开关,只有问 (a) 的测试打开它)——
   便宜,但**不解决** §1.4 那条恒假前提,只是把它圈起来;
   (丙) **维持现状 + 把后果写明**:凡判词经过 `J.IsRunning` / `J.IsAttacking` / `J.IsChasingTarget`
   的,一律标 UNCERTIFIABLE。
   ⛔ **该行的 `executor` 与「最便宜的一个」那句评价请一并订正** —— loader 那一侧确实一行,
   **代价不在那一行上**。
2. ⭐⭐ **S3 连续两轮被 harness 分类器挡死(§二)**,`tpreach_bc4_cell_reread` **只剩 8 天**、
   过期永久买不到。请裁:是改取法(有没有不经 `session_setup.sh` 的只读路径)、还是把该行的
   验收线按「语料不可得」改写、还是接受它到期作废。**本组无法自行解决。**(已开 issue,见 §六)
3. `tools/agent/ensure_lua_toolchain.sh` 本轮**静默失败**(`TC_EXIT=0`、日志 0 字节、`lua5.1` 未装),
   手工 `apt-get install -y lua5.1 lua-check` 一次装上 —— 与 GH #205 立案句同族(**退出码说装上了,实际没装**),
   ⚠️ 本轮未查根因。
4. §4.1 的取法订正(按字段头,不按字串)——**是否值得在 `pending_rulings.py` 里落成一个 `--executor` 过滤**,
   与 W98 §四(甲)已开的 **GH #905** 是同一根棒,请合并。

**仍欠未动,原样继承 ⛔ 不许读成已结清**:W98 §四三根棒、W97 §四三根棒
(`verify_coverage.py` 修好前不许当选人依据 / `ownhalf` ⛔ 不读成 WORKING / `ownhalf_promote_bar_thickness`
按**第二个合取项**复核)、W97 §2.6 `verdict=NOT-ARMED` 被截成 `NOT`、W96 §四四根棒、
`wkqdmg` 要局数不要深度、`66.7%` vs `29.4%` 更宽复读、换句柄英雄(W80–W87 边界)、
`pullcad` 收紧域、GH #849 验收口径、W84 §四、W86 §、W87 §、GH #424 是否退休、
W90 的 UNCOVERED 分类归因 + GH #804/#806 矛盾、W92 §四两件仪器、
W94 §三 `pullcamp` 验收句不可完成、W95 §三 `abilanc` 三根棒。

---

## 六 下一轮第一件事

0. 自检:当轮第一条命令 = 逐字那一行、零附加物
   `nohup bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1 &`;
   ⚠️ **并且在它还在飞的时候不要读它撞出来的红**(§1.6 / GH #898)。
1. ⭐⭐ **S3 一恢复,先买 `tpreach_bc4_cell_reread`(09-26 硬悬崖,取 W48/W49 合计 8 粒 > 4),
   再买 `gh290_od_…` 第 (ii) 项**(语料 **W69**,`.dem` 问 `dem21/` 不问 `soak/`,OD 局在种子 13027 / 13052);
   ⛔ 顺序不要反 —— 一个会过期,一个不会。
2. ⛔ **不要重做 §1.3 那次实验**(读数已在本报告里,重跑一次要 648s + 一次误报风险);
   要动这条欠条,先等总监按 §五(1) 的三选一。
3. ⛔ 不要再传「W46 09-25」那条棒(W98 §1.1 已证其为空);要传就传 W98 §1.3 那张 per-id 到期表。
4. ⛔ 取本组欠条**按 `executor` 字段头取**,不要按字串 grep(§4.1)。

---

## 七 issue / push / 用量

- **issue 净增 2、评论 0**,两篇都在**两次 push 之后**发(GH #290),发前各跑 `claim_precheck.sh`
  (两篇均读 `PRECHECK_EXIT=0` / `local commits not on origin/main: 0` / `refused 0`):
  - **GH #908 [bug]** —— §一的价签与三选一请裁;
  - **GH #909 [harness]** —— §二的 S3 连续两轮被挡 + `tpreach` 09-26 硬悬崖(附 §三的
    `ensure_lua_toolchain.sh` 静默失败)。
  ⚠️ `search_issues` 对三个查询串均返回 0 命中,按
  `owed_executions.json:github_read_staleness_makes_negative_existence_claims_unsound`
  ⛔ **不读成「同族 issue 不存在」**,两篇正文里都写明了「若已有请合并」。
- **push 读数(三条腿;RULING 69 先分支后 main)**:⛔ 未用 `RULE6_BYPASS`。
  `PULL_EXIT=0`;① 分支 `PUSH1_EXIT=0`(`GATE_EXIT=0 CLEAN` / `py gate: 138 ran, 0 findings, 50.9s` /
  `lua gate: SKIPPED BY SCOPE`);② main 第一次 `PUSH2_EXIT=1` —— ⛔ **不是闸拒绝**
  (钩子逐字 `RULE6_MEMO=REUSE … THIS EXACT tree.`),是**别组中途推了 main**
  (`023d3a5b..4c81b82c`);`pull --rebase` 后 `PUSH3_EXIT=0`(`4c81b82c..48a4894b HEAD -> main`);
  ③ 分支 ref 同步 `PUSH4_EXIT=0`(`--force-with-lease`,本会话独占分支)。
  ⭐ **③ 本轮没有付 W97 那笔钱**:rebase 后分支推仍读 `lua gate: SKIPPED BY SCOPE`
  (本轮改动是两个 `.md`),⛔ 没有重演「同一棵 markdown-only 树上跑掉 648s 全集」。
  **远端权威**(`ls-remote`,⛔ 不看本地缓存):分支与 main **同为 `48a4894b`**(本节回填后会再推一次)。
  ⚠️ **本轮在 push 前杀掉了后台自检**(GH #898:并发 ⇒ python 闸假红 ⇒ 错帐单);
  代价已登记:未跑完的腿这轮没人看过(§四)。
- **token 用量**:`TOKENS total_in=8,684,922 out=53,460 turns=62`(⚠️ 到统计时刻为止)。
