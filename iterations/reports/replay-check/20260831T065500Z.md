# replay-check 2026-08-31T06:55Z — 买 `lionqdmg` 的条件 (a):域**非空**,但 (3) 是**十局一个 episode**;逐帧那一个 episode 是**教科书式的漏杀**,而机制归因只排除掉了一半

**语料**:W30(树 `0d58ce09`,**47-id**,`arm sha1=9420af39`,四 run
`89e581`/`6eb30e`/`69e067`/`4ecca6`,**10 局带戳** + 4 局暖场)。
零 EC2、S3 只读、零 CE 调用,`bots/`/`game/` **0 改动**。

**`VERIFY count this round = 1`**(`lionqdmg`)。上一轮交棒行第 (1) 条点名的就是它
(「lion 四格填得满,是本家族里语料最好的一条;**不要再顺延一轮**」)。已连续两轮
`VERIFY count = 0`,本轮回到 1。

⚠️ **排序提醒**:章程「当前状态」节的历史条目**不按时间排**,`tail` 读到的不一定最新。
本轮追加在文件末尾。

---

## 0. 交棒行核对

| 交棒项 | 本轮 |
|---|---|
| (1) `lionqdmg` 条件 (a) | ✅ **本轮主线**,见 §2–§5 |
| (2) `cmqreach` 不要再用同类语料重买 | ✅ **没重买**(英雄组 05:02Z 已认领 GH #354 §5 的 fixture 路线) |
| (3) 若总监采纳反对角线(GH #353) | ⛔ 未裁,本轮照常打四格 |
| (4) `idletrip_domain.py` 在 W30 上跑 | ⛔ **顺延**(见 §7 欠账) |

**W31 已 06:17Z 起飞**(树 `1dd5705`,run `731a21`/`fde133`/`b5b4b9`/`c44eb5`)⇒
开工时未收割,语料仍是 W30。

`arm_string_census.py`(喂 `…/analysis`)四个 run **全 MATCH**,`arm sha1=9420af39`,
`14 = 10 stamped + 4 warmup`,`lionqdmg` **在串里**。**不可与 W29(44-id)并池。**

---

## 1. 这条 id 的方向决定了条件 (a) 能买到什么

`bots/BotLib/hero_lion.lua:546` `X.GetImpaleKillDamage`:

```
shipped = hAbility:GetAbilityDamage()            -- 结构性 0(lion_impale 不声明顶层 AbilityDamage)
armed   = hAbility:GetSpecialValueInt('damage')  -- 105/170/235/300
```

⇒ **这是一次加宽,不是收窄** —— 与同族的 `wkqdmg`(纯 `math.min` 收窄)**方向相反**。
加宽的那一半**在出厂腿上根本不存在**,所以条件 (a) 不能像收窄那样「从发生过的施法上读」;
而在 armed 腿上,`X.ConsiderQ` 后面**八条分支也都返回 `BOT_ACTION_DESIRE_HIGH`**
⇒ 一次观测到的施法**不可归因**到斩杀分支。

**能量、也正是 `queue.json:hero-24` 要的,是「域」**:斩杀循环今天会不会被**走到**,
以及走到时 armed 的伤害声明够不够得着一个活着的敌人的血条。**这两件事在两条腿上都是
反事实,因此四格都有意义,而且 baseline 腿上的读数信息量更大**(armed 腿上真开了火的
帧会**因为敌人死掉而从域里消失**,这是本 id 特有的、会自己吃掉自己域的形状)。

**交付**:`tools/batch_test/behavioral/lionqdmg_domain.py`(新)。

---

## 2. 四格读数(铁律 4(i-a):每个头条数字四格全打;4(ii):只报均值与占比,不报中位数)

### (1) 载体分母 —— **10/10 局都有 Lion**

| 格 | 局 | Lion 帧 | 存活帧 |
|---|---|---|---|
| ab/armed | 3 | 5079 | 4619 |
| ab/baseline | 3 | 4648 | 4084 |
| ba/armed | 3 | 4914 | 4654 |
| ba/baseline | **1** | 1712 | 1521 |

**四格填得满**(上一轮 GH #353 数的 `lion 3/3/3/1`,逐位复现)。

### (2) 域:斩杀循环**被走到**(Q 就绪 + `nCastRange+200` 内有活着的敌方英雄)

| 格 | ready 帧 | 每局 | 占存活帧 | ready episodes |
|---|---|---|---|---|
| ab/armed | 218 | 72.67 | 0.047 | 73 |
| ab/baseline | 214 | 71.33 | 0.052 | 82 |
| ba/armed | 193 | 64.33 | 0.041 | 78 |
| ba/baseline | 40 | 40.00 | 0.026 | 20 |
| **合计** | **665** | — | — | **253** |

⇒ **域非空,而且远非稀缺**。**总监的 `UNINTERPRETABLE` 退回门(§CO)不适用**
(它的触发条件是「语料里没有『Q 就绪且射程内有活着的敌方英雄』的帧」——
本语料有 665 帧 / 253 个 episode);申请书 (甲) 的 **DOMAIN-NOT-REACHED 也不适用**;
(丁)(解析吃掉技能行)同样不适用 —— 技能行逐帧读得到,rank/cd/mp 都在。

### (3) armed 的声明够得着一个活着的敌人的血条 [占 (2)]

`J.WillMagicKillTarget` 的估值最后要过 `GetActualIncomingDamage`,而语料**没有魔抗也没有
回血**⇒ 这一格**报两遍**:`raw`(申请书验收 (3) 的字面口径,`hp ≤ dmg×(1+amp)`)与
`mr25`(再乘基础 25% 魔抗的 0.75,**更贴近真谓词、也更紧**)。**两者都忽略回血,所以两者都是上界。**

| 格 | amp | raw | 占比 | mr25 | 占比 |
|---|---|---|---|---|---|
| ab/armed | 0% | 3 | 0.014 | **0** | 0.000 |
| ab/armed | 15% | 4 | 0.018 | **0** | 0.000 |
| ab/armed | 20% | 4 | 0.018 | **0** | 0.000 |
| ab/baseline | 0/15/20% | 0 | 0.000 | **0** | 0.000 |
| ba/armed | 0% | 0 | 0.000 | **0** | 0.000 |
| ba/armed | 15% | 1 | 0.005 | **0** | 0.000 |
| ba/armed | 20% | 3 | 0.016 | **0** | 0.000 |
| ba/baseline | 0/15/20% | **2** | 0.050 | **2** | 0.050 |

**池化(仅供量级,不是效应量)**:`raw` 0%→**5** / 15%→**7** / 20%→**9** 帧;
`mr25` 三档**都是 2 帧**。

**episodes(本组的既定读法,比帧数值钱)**:`mr25` 的 **kill episodes = 0 / 0 / 0 / 1**
⇒ **十局语料一共 1 个 episode。**

### 邻近阶梯 —— 「差得远」还是「差一点」

(占 (2) 的帧里,射程内最软的敌人血量在 mr25 声明的 k 倍以内;**k=1.0 就是上表的 mr25 行**)

| 格 | ready | ≤1.0× | ≤1.5× | ≤2.0× | ≤3.0× |
|---|---|---|---|---|---|
| ab/armed | 218 | 0 | 4 | 7 | 17 |
| ab/baseline | 214 | 0 | 0 | 3 | 9 |
| ba/armed | 193 | 0 | 1 | 3 | 19 |
| ba/baseline | 40 | **2** | 2 | 2 | 5 |

⇒ **不是「差一点」**:665 个 ready 帧里,连放宽到 **3 倍**声明也只有 50 帧(7.5%)。
斩杀循环被走到得很勤,**但它面前站的人几乎从来不是残血**。

### (4) 本 id **独有**的覆盖 [**只有上界,没有下界**]

`IsGoingOnSomeone` / `IsRetreating` 读 `bot:GetActiveMode()`,**录像不带** ⇒ 一律**假设为假**,
这**抬高**本格、**偏袒**本 id。`IsInTeamFight(bot,1200)` 数的是 1200 内 `BOT_MODE_ATTACK`
的队友,mode 那一半不可见,**但「1200 内至少 2 个队友」是必要条件** ⇒ 队友 < 2 的帧上
该谓词**确定为假**。于是两个上界:`u_lvl`(只用 level<15)与 `u_tf`(再排除可能的团战)。

| 格 | amp | kill(mr25) | u_lvl | u_tf |
|---|---|---|---|---|
| ba/baseline | 0/15/20% | 2 | **2** | **2** |
| 其余三格 | 0/15/20% | 0 | 0 | 0 |

⇒ 那**唯一**的 episode **两个上界都通过**(Lion 7 级 < 15;1200 内队友 1 个 < 2
⇒ `IsInTeamFight` 确定为假)。**但 1 个 episode 判不了 (丙)** —— 见 §4。

---

## 3. ⭐ 先逐帧,再聚合:那**一个** episode 是什么样子

`4ecca6/20260831_005639_slot1`(seed 2286,**side=dire ⇒ dire 是 armed 腿**,
Lion 在 **team 2 = radiant ⇒ baseline 腿**)。**Lion 主体 `idx=1243` 已核**
(另两个 `idx` 1554/1648 都在 t=370.5 才出现且几乎全是 hp=0 的帧;身份锁按最早出现取,
取到的是 1521 个存活帧那一个)。

70 秒逐帧,只留骨架:

| t | Lion hp/mp | Q rank/cd | SB hp | d(Lion,SB) | 事件 |
|---|---|---|---|---|---|
| 400.8 | — | — | — | — | **`lion_impale` 施法**(⇒ Q 进 12s 冷却) |
| 402.1 / 403.8 | — | — | — | — | `lion_voodoo` → SB;`lion_mana_drain` → SB |
| 405.5 | 734 / 499 | 3 / **7.4** | 422 | 587 | 追击开始,Lion 平 A |
| 408.5 | 721 / 508 | 3 / **4.6** | 369 | — | `DAMAGE lion→SB 61` |
| 410.5 | 692 / 515 | 3 / **2.6** | 185 | — | `DAMAGE lion→SB 51/53` |
| 412.5 | 691 / 522 | 3 / **0.6** | 149 | — | 还差 0.6 秒 |
| **413.5** | **699 / 526** | **3 / 0** | **102** | **847** | ⭐ **Q 就绪,SB 102 血,在 870 的圈内** |
| **414.5** | 673 / 528 | 3 / 0 | 108 | 868 | ⭐ 仍在圈内(圈边 870) |
| 415.5 | 636 / 533 | 3 / 0 | 74 | **1522** | SB 已跑出圈 |
| 416.6 | — | — | 81 | — | **`ITEM spirit_breaker item_flask`**(大药) |
| 424.5 | 714 / 593 | 4 / 0 | **373** | — | SB 回满脱身 |
| 480.3 | — | — | — | — | Lion 的**下一次** `lion_impale` |

**armed 腿会在 t=413.5 开火**:rank 3 ⇒ KV 235,`mr25` 声明 176 ≥ **102**;
即便再减去 `HealthBack/MagicResistReduce`(SB 回血按 ~5/s 计约 33)也还有 143 ≥ 102。
**出厂腿一枪没放**:`lion_impale` 的下一次施法在 **480.3**,中间 67 秒空白,
而 Q 从 412.8 起**全程可用、蓝量 526 起步、目标在射程内**。

⇒ **这是一次货真价实的漏杀,并且后果被录下来了**:SB 喝大药、回到 373 血、跑掉。
**这一条是 outcome 层面的证据,不依赖分支归因。**

### 3.1 机制归因:**排除掉一半,另一半排除不掉**

**(甲) 能排除的:`J.ShouldConserveManaInLane` 不是解释。**
`X.ConsiderQ` 开头第二行就是它,为真则整个 `ConsiderQ` 返回 0(**斩杀分支也一起死**)。
但 `jmz_func.lua:6568` 第一行是 `if not J.IsLaneFixOn('mana') then return false end`,
而 **47-id 串里没有任何 lanefix id**(串里只有 `lf_rescue`)⇒ 该谓词在本波**恒假**。
**这是机制级排除,不是「分不清」。**

**(乙) 排除不掉的:「攻击」分支的 mode 谓词。**
`X.ConsiderQ` 的「攻击」分支只要 `J.IsGoingOnSomeone(bot)` 且 `botTarget` 在
`nCastRange+300` 内就会放 Q,**它在斩杀分支下游、但它不需要残血**。
而 **t=400.8/402.1/403.8 的三连(impale + hex + drain 全打在 SB 身上)说明
13 秒前这个谓词是真的**。⇒ **若它在 413.5 仍为真,出厂腿本来也会放 Q,
那这一帧就不是本 id 独有的覆盖。**

**能说的只有一个方向的推断**:413.5 时 Q 完全可放、目标在 1170 内,而**没有施法** ⇒
要么 mode 已经不是 attack/roam/gank,要么 `J.GetDelayCastLocation` 返回了 nil。
**这缩小了范围,但不是判决** —— `GetDelayCastLocation` 返回 nil 的那一支同样成立,
而它与 mode 无关。**所以 §2 的 (4) 那两个「2」是上界,本 episode 也不例外。**

---

## 4. 裁定:条件 (a) = **域已买到,效应域 NEGLIGIBLE**;按预登记的 (乙) 处理

申请书**开工前**就把反向读法写死了,本轮**逐字命中其中一条**:

> **(乙) 若 (2) 非零而 (3) 极小,正确结论是本改动无效应、不是本改动无害,
> 应直接不入集,别买波次。**

读数正是这个形状:**(2) = 665 帧 / 253 episode / 10 局**,**(3) = 2 帧 / 1 episode**
(mr25,三档法术增强完全相同)。⇒

- **`lionqdmg` 的域被走到得很勤,但它能改变的帧几乎不存在。**
- 按 (乙),**本组的建议是不为它单独买波次**;它已经在 W30 的 47-id 串里,
  **是否出集不归本组裁**(退集门在总监)。
- **(丙) 判不了**:它问的是「(3) 的帧是不是几乎都被别的分支覆盖」,
  而 (3) 只有 1 个 episode,且 §3.1 (乙) 说明连**这一个**的归因都没闭合。
  **不要把「u_tf = 2」读成「已证明是独有覆盖」。**
- ⚠️ **不写「域空」,不点 `state.json:lionqdmg_20260830` 的任何 not-reached 条款**
  —— 域**不空**,不空的证据是 665 帧。

**本组既不支持也不反对 `lionqdmg` 的 promote**:条件 (b)/(c) 不归本组,
条件 (a) 的答案是「域在,效应域可忽略」,**这两件事不是同一句话**。

---

## 5. 法术增强三档:验收 (3) 要的三栏都在,而**真正的缺口不是档位**

**三档对读数几乎无差别**:`mr25` 三档都是 2 帧;`raw` 5 → 7 → 9 帧。
⇒ **本语料里,「哪一档可达」不是这份读数的瓶颈**(它连 1 个 episode 都改不动)。

总监 §CO 加注要的「哪一栏真实可达由英雄组给事实」**仍然没有交付**(hero 组
08-30T16:51Z 之后的三轮 —— 22:56Z / 01:55Z / 05:02Z —— 都在别的轴上)。
按上一轮交棒行的指示,**本轮按 0% 档先报并明写缺口**,同时把本组能观测到的那一半交出去:

- ⭐ **665 个 ready 帧里,Lion 包里有法术增强**物品**的帧数 = 0(四格全 0)。**
  (口径:`kaya` 系 / `ethereal_blade` / `aghanims_shard` / `ultimate_scepter`;
  `aether_lens` **不算**,它给的是施法距离。)
  ⇒ **在本语料里,15%/20% 两档没有任何物品来源。**
- `lion_to_hell_and_back` 的 KV(`tests/mock/special_value_shapes.lua:276`)声明
  `spell_amp = 20`,**但同一块里还有 `duration = 90` 与 `damage_window = 3.0`**
  ⇒ 它是一个**有触发条件的限时增益,不是一条常开被动**。
  逐帧确实看到 `lion_innate_to_hell_and_back` 以 level 1 常驻技能栏,
  **但本仓没有它的触发条款**,所以 **20% 那一栏能不能在一局里到手,本组答不了** ——
  **这仍然是英雄组的事实**,本轮只是把它从「未知」缩小到「不是物品给的,只能是这个内在技能给的」。

---

## 6. 验证(退出码**全部未经管道**)

| 门 | 读数 |
|---|---|
| `tools/agent/luacheck_gate.sh` | **exit 0 / 0 warnings**(容器缺 luacheck,gate 自己装的);**未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行可抄** |
| `tests/run_py_tests.sh` | **67 passed / 1 failed / 0 uncertifiable**(本轮 +1 个测试文件);失败的是 `tests/test_selfcheck_lua_leg.py`,见下 |
| `lionqdmg_domain.py --selfcheck` | **29/29 PASS,exit 0**,并打 `corpus checks SKIPPED, not passed` |
| `tests/test_lionqdmg_domain.py`(新) | **exit 0**;43 个 check |
| 变异台 | **6/6 全杀**,还原走文件副本 + `sha256sum -c` **每次 OK** |
| Lua 全量 | **未改 Lua ⇒ 不声称跑绿 Lua 全量**(GH #124) |

**变异台的六个**(每一个都是「会静默失效」的那种):
M1 `BASE_MAGIC_RESIST_MULT` 0.75→1.0(魔抗被悄悄丢掉,`mr25` 塌成 `raw`,
**问题从『能不能杀』变成『不算魔抗能不能杀』而数字照样好看**)/
M2 episode 间隔从「上一帧」改成「本段起点」(长追击被并成一个 episode,**域被压小,
恰好是有利于本轮否定结论的方向**)/ M3 蓝量门恒用 rank-1 花费 / M4 +600 天赋无条件折进射程 /
M5 死掉的敌人算作目标 / M6 冷却子句删除。

**开工自检**:**exit 3**,八腿全跑;`FINDINGS = trunk-red(python)`、`UNCERTIFIABLE = none`;
稳定版锚点 2/2 ok;`unlanded_commits` 无;`ORPHAN_PROPOSAL` none。
⚠️ **第五次登记:它在本容器上不是章程写的「约 20s」** —— 180s 前台被移到后台,
实测跑完远超 300s。**连续五轮不成立,章程该改了。**

### 6.1 那个 python 红:**是本容器的预算发现,不是本轮引入的**

`tests/test_selfcheck_lua_leg.py` 唯一的失败是
**`5a0: the leg finishes inside 120s`** —— 快速 Lua 腿在本容器**跑不进 120 秒预算**,
连带 8 个 check 打 `UNCERTIFIABLE`(它们依赖那次没跑完的干净树运行)。
**这不是断言红,是同一个「本容器比章程假设慢」的家族**(与上面第五次登记同源)。
**本轮两个新文件都是 python,不进快速 Lua 腿的发现集**;为把「不是我的」变成读数而不是推论,
把两个文件挪开后单独复跑了这条测试 —— **结果见 §6.2**。
⚠️ 开工自检当时还报了 `tests/test_rc_wrapper.py` 红,**本轮全量复跑它是绿的**
⇒ **那一条不稳定,登记备查,不据此下任何结论。**

### 6.2 干净树复跑:**逐位相同 ⇒ 不是本轮引入的,这是读出来的不是推出来的**

把本轮两个新文件**挪出工作树**后单独复跑 `tests/test_selfcheck_lua_leg.py`:

```
43 checks, 1 failures, 8 uncertified        # 与带着两个新文件时逐位相同
CLEAN_TREE_EXIT=1                           # 未经管道
```

⇒ **失败与 uncertified 的构成完全一致**,**归因闭合**:本轮两个新文件与它无关。
两个文件事后**已还原**(`git status` 复核,仍是两个 `??`)。
⚠️ 但**这不等于 `origin/main` 是绿的** —— 它只说「本容器上,有没有我的文件都一样红」。
**「未改 Lua」也不是免责**:红的是**预算**不是断言,而预算是容器给的。

---

## 7. 诚实边界

- n = **10 局带戳**,**14 局是录制槽决定的,不是我抽的**(GH #308 未裁);
  **本报告没有一个数字是效应量**;计数类量的**侧偏未消**(铁律 4(i-b))⇒
  **四格之间的差不可读成 armed−baseline 差分**。
- **一切计数都是上界**:视野(引擎的 `GetNearbyHeroes` 受战争迷雾,本语料全知)、
  `IsFullyCastable` 的沉默/破坏子句不可见、`GetActualIncomingDamage` 的魔抗与回血缺失。
  **上界对本轮的否定结论是安全方向,对任何肯定结论都不是** —— 引用时必须一起引。
- 非主体 `idx` 的英雄实体(幻象等)**被身份锁排除在敌人列表之外**;这是保守选择,
  但**是一个选择**,登记。
- **`lionqdmg` 的 promote / 出集,本组既不支持也不反对**;退集门在总监。
- §3.1 (乙) 的分支归因**没有闭合**,§2 的 `u_lvl`/`u_tf` 是上界,**不是「独有覆盖已证」**。
- 「20% 档可达吗」**本报告不主张**,只主张「本语料里没有任何物品来源」。

---

## 8. 欠账变化

- ✅ **交棒 (1) 交付**:`lionqdmg` 条件 (a),连续两轮 `VERIFY count = 0` 的局面打断。
- ✅ 交棒 (2):`cmqreach` 没有重买同类语料。
- ⛔ **交棒 (4) `idletrip_domain.py` 在 W30 上跑:顺延**(本轮全部预算给了 `lionqdmg`
  的逐帧;两波数字本来也不可并池)。
- ⛔ `stayfield2_whynot.py` 仍待**下一个 44-id 波**(W30 是 47-id,W31 未收割)。
- 继承未动:`fieldbuy_silence.py`/`stayfield2_margin.py` 的 ab/ba 分层;
  「静止在小兵火力里」检测器(素材 `e706a3/20260830_063416_slot1` lich t=625.5..634.5);
  `pullcad_beat.py` 在 W25 剩两 run;W25 只并 2/4 run;W26–W28 与 W25 从未池化;
  `seed 975` **第十九轮**;`wandlimbo` 因 #293 **第十七轮**不可执行;
  GH #265 仍被 #272 阻塞;`blinkflee` 仍卡 #304/#305;WK rank-3 冷却全语料复测仍欠。

---

## 9. 下一轮第一件事

**(0) 先读章程「当前状态」节的最后一条,不要抄过期的交棒行。**

1. **W31(树 `1dd5705`,run `731a21`/`fde133`/`b5b4b9`/`c44eb5`)应已收割** ⇒
   先跑 `arm_string_census.py`(**喂 `…/analysis` 子目录** —— 喂 sweep 根目录会打
   `0 games` 且 `exit 0`,与「全部 MATCH」在退出码上不可区分),核串长与 sha1,
   **不可与 W30(47-id / `9420af39`)并池**。
   ⚠️ 若 W31 仍是 47-id 且 sha1 相同,`lionqdmg_domain.py` **可以在它上面独立再跑一遍**
   ——**不是并池,是第二次独立读数**;本轮的 1-episode 结论最需要的就是这个。
2. ⭐ **`lionqdmg` 的棒已交出**(GH issue 见 §10),**不掉在本组**。
   若总监要 (丙) 的答案,**唯一的路是 fixture**:钉 `4ecca6/20260831_005639_slot1`
   **t=413.5**,在真实帧上驱动**真的** `X.ConsiderQ`,把 `IsGoingOnSomeone` 与
   `GetDelayCastLocation` 的返回值**读回来** —— 那正是 §3.1 (乙) 排除不掉的那一半。
   这条路与英雄组 05:02Z 给 `cmqreach` 走的是**同一条**。
3. `idletrip_domain.py` 在最新波上照常打四格(不核 gate,跨串可跑,两波不可并池)。
4. `stayfield2_whynot.py` 等下一个 **44-id** 波。
5. `pullcad_beat.py` 在 W25 剩两 run 上仍欠。

---

## 10. 已发表

**先 push 后发(GH #290)**:`9ed14f35` 落 `origin/main` 之后才发;
`claim_precheck.sh` 读 **exit 0 / clean / 本地领先 `origin/main` 0 个 commit**
(`paths cited 4 / resolved on trunk 4`);**MCP 未触发 `requires approval`**。

- **GH #359 `[hero]` 新开**(`lionqdmg` 条件 (a) 的全文裁定:三个退回门都不适用 +
  四格 + 邻近阶梯 + 10 行逐帧表 + §4 那条**机制级排除与排除不掉的那一半** +
  §5 「本语料 15%/20% 没有任何物品来源」+ 交棒两件)。
- ⛔ **没有为「开工自检超预算」另开 issue** —— 批测台 06:30Z 已开 **GH #358**
  并给了根因(78.4 秒来自两个文件),本轮的干净树复跑(§6.2)只是同一件事的第三次独立确认,
  **按铁律 7 的节制不重开也不追评**;需要时它在本报告 §6.1/§6.2 里。

---

## 11. 本次会话 token 用量(铁律 8)

```
TOKENS total_in=11,024,182 out=76,897 turns=76
```

(到统计时刻为止;统计后的收尾回合不计入。)
