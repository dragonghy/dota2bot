# 录像检查组工作单元 — 2026-08-20T18:36Z(第二十二次触发)

**对象:批测台 16:09Z(arm A)+ 18:08Z(arm B)的 `capmono` 隔离两臂波次。
执行章程「下一轮优先」第 1 条 —— `capmono` 的隔离条件 (a) 核验,
即 14:37Z 报告 §8.1 明确申请、等了六轮的那次隔离波。**

**宽扫 32/32 镜像有效局(arm A 16 + arm B 16,8 个暖场局剔除),806 个 clean 域帧;
逐帧深查 6 局。** 超过章程 6 局下限。

---

## 0. 一句话结论

**`capmono`:SILENT(隔离后)** —— 准确措辞是**「检测器分辨不出」,不是「capmono 坏了」**。
teambrain 被两臂同时 armed 控住之后,隔离 **DiD = −7.9pp ± 9.3(|t| 0.84)**,
逐种子 DiD 符号 **2/4**(掷硬币)。**设计预期的「+拒扑」签名没有重现;
14:37Z 的 +16.6pp 在隔离设计下不成立。条件 (a) 隔离证明未通过 ⇒ 不支持 promote。**

**本轮真正的资产不是这个判决,是副产品**:arm B 里 capmono 是关着的,
所以 arm B 的内差是一条**实测零通道** —— 它给出了**这个行为检测器的经验零点
σ ≈ ±15pp/种子,4 种子 MDE ≈ ±21pp**。这是 GH #30(经济通道 σ≈30 gpm)的行为通道版本。

---

## 1. 隔离设计(这次是真隔离,14:37Z 那次不是)

| | arm A | arm B |
|---|---|---|
| run | `spot_20260820_160902/160904/160908/160911_1_11a8de33…` | `spot_20260820_180759/180802/180805/180808_1_11a8de33…` |
| cand | 16 id,**含 `capmono`** | 15 id,**去 `capmono`**,其余 15 个顺序一字不动 |
| 种子 | 888/895/896/906 | 同 888/895/896/906 |
| 树 | `11a8de33` | 同 `11a8de33`(批测台钉 SHA,`main` 已漂移到 `71b510c`) |

**关键**:`teambrain`(regroup 造脱离)、`ownhalf`/`overchase`/`l1trade`/`l5combo`
(反向,加 commit)**在两臂都 armed** ⇒ 在 DiD 里对消。14:37Z 那次是单臂 13-id 全 armed,
结构上无法把 +16.6pp 单独归给 capmono —— 当时报告 §4 自己写下了这条限制,本轮补上。

**侧别正交验证:通过。** 种子 888 四局(跨两臂)从 `.dem` 的 `snapshots[].team` 直读花名册
完全一致(T2: spirit_breaker/sven/viper/witch_doctor/zuus;T3: crystal_maiden/drow_ranger/
jakiro/lina/tidehunter),`armed_team` 随 stamp 后缀翻转 2/2/3/3;两臂同种子选人一致,
仅差 capmono 一个 id。**按 run 分目录下载 + 逐目录 4 timelines 对拍**(章程 16:46Z 的同名
跨 run 撞车坑,本轮兑现:8 个目录各 4 局 = 32,零静默覆盖)。

---

## 2. 四格 retreat_frac + DiD(14:37Z 的 clean 域口径逐字沿用)

clean 域 = HP∈[0.42,0.50] + 队友在 1200u 内挨打/低血 + **最近敌人在 850–1500u(非贴脸)**
+ t≤600s + 尸体帧(`hp_pct<=0.01`)剔除。850u 下界是承重条款:>850u 保证 `lanesurv`
爆发撤退不独立触发,所以这里的撤退才可归因到 team-roam 被 cap。

**帧级**(806 帧):

| 格子 | retreat/机会帧 | retreat_frac |
|---|---|---|
| armA-armed | 82/196 | **41.8%** |
| armA-base | 112/225 | **49.8%** |
| armB-armed | 76/180 | **42.2%** |
| armB-base | 86/205 | **42.0%** |

- arm A 内差(armed−base)= **−7.9pp**(设计预期是 **+**)
- arm B 内差 = **+0.3pp**(capmono OFF ⇒ ≈0,**一次成功的阴性对照**)
- **DiD = −8.2pp**

**去重**(同 actor ≥6s):armA 33/74=44.6%、armA-base 47/87=54.0%、armB 28/77=36.4%、
armB-base 32/78=41.0% ⇒ armA 内差 −9.4pp、armB 内差 −4.6pp、**DiD_dedup = −4.8pp**。符号一致。

**逐镜像方向**:armA radiant −4.5 / dire −13.4(两方向同为负但量级不同);
armB radiant −0.6 / dire −2.7。

---

## 3. 决定性的一段:arm B 是一条实测零通道

```
armA 逐种子内差: −1.3  −3.5  −23.1  +1.4   mean −6.62  SD 11.15  SE 5.57
armB 逐种子内差: −13.2 −9.3  +18.2  +9.3   mean +1.27  SD 14.99  SE 7.50  ← capmono OFF!
DiD(种子配对) = −7.89pp   SE 9.34   |t| 0.84
逐种子 DiD:  +11.9  +5.8  −41.3  −8.0   →  2/4 为负
```

**capmono 关着的那条臂,逐种子照样抖 ±18pp。** 即:

> **该检测器 4 种子经验零点 ≈ ±15pp/种子(SE ≈ 7.5pp),MDE ≈ ±21pp。−7.9pp 落在噪声里。**

**推论(对 14:37Z 的更正)**:那次的 **+16.6pp(n=62/63,单臂、teambrain 未控)
落在本轮实测的零通道散布之内** —— 它从一开始就不构成信号。`capmono` 的条件 (a)
至今**没有**被任何隔离证据支持。

`armA s896` 的 −23.1pp **不是单局离群**:逐局 3/4 局同向(162821 armed 3/9 vs base 10/16;
163337 2/8 vs 8/16;162322 2/7 vs 3/3)—— 是种子级效应,但另外三个种子不复现。

---

## 4. 逐帧证据(硬规则:结论落到帧;含诚实反例)

**① 真 capmono 形状(armA-armed)**:`armA_160902/20260820_161810_slot1` t=327
spirit_breaker 43% HP,tide 45% 在 972u、ally sven 60% 在 1036u。从 (4890,−5726) 撤向
自家 ancient,`dAnc` 10816→10026,**HP 40%→48%,脱离+回血,0 死亡**。形状干净。

**⑤ 但同形在 capmono OFF 侧同样普遍(致命的一条)**:`armB_180759`(capmono **关着**)
里 sven vs CM@1334u、viper vs jakiro@1330u、CM vs sven@1149u 与 ① 的形状**一模一样**。
⇒ 这个"拒扑"行为**归既有 `lanesurv`/撤退逻辑,不是 capmono 独有**。
**只看 armed 侧的漂亮帧会把已发布行为记成新 id 的功劳** —— 这正是单臂读数的系统性陷阱。

**③ 诚实反例(分类器噪声)**:`armA_160904/20260820_163418_slot1` t=39 slardar 44% 被判
retreat,但 t=43–46 dragon_knight 扑到 207u、ally venomancer 94u —— 2v2 乱斗里的**瞬时侧移**
(离开 zuus 转打 DK),HP 最终爬到 51%。**不是"拒绝扑杀",是团战 swirl 里逐帧二分的噪声。**

**④ armed 却没撤(COMMIT,符合设计)**:`armA_160902/…161810` t=116 sven 41–45% HP、
tide **60–66%** 在 465–902u,全程贴脸对拼没撤。敌人 60%+ 非"够得着的击杀"⇒ raw 出价
大概不在悬崖区,capmono 不介入。**capmono 是极窄门,不是每帧都翻**(与 14:37Z 一致)。

**② 镜像不固定对局走势**:同 seed/同 draft/同 side 的两局到对线中期完全发散(同一 SB
同一时刻,一局在下路撤退、镜像局在上路打 jakiro/drow)⇒ **逐英雄同时刻跨局对拍无效**。

**⑥ 分类器可信度批核**:全部 armA-armed dedup retreat 结局 = **18 回血 / 15 平 / 0 死亡**;
armA-base = 30 回血 / 16 平 / 1 死。⇒ 判出的 retreat 确实是"脱离+存活/回血"的真形状,
**不是死亡逃窜**,分类器可信。

---

## 5. 一条被排除的解释(观测域是对的,不是采样缺陷)

806 帧里 **713 帧(88.5%)在 t≤480、100% 在 t≤600**。子代理初判这是"观测窗没覆盖
capmono 活跃区"(理由:team-roam 主要在对线后活跃)—— **我核了代码,这条不成立,方向反了**:

`_divecap_CapForLanePush` 的 cap **就住在** `if J.IsInLaningPhase() or J.IsPushing(hBot) then`
里面(`bots/mode_team_roam_generic.lua:112`),而 `J.IsInLaningPhase()` 的 Turbo 地板
= **480s**、soft end = 600s(`jmz_func.lua:8973-8985`,本波 `c2` 未 armed)。
⇒ **88.5% 落在 t≤480 恰恰说明域选对了**,这**加强**而不是削弱 SILENT 结论。

**真正没覆盖的窄缝(如实登记)**:cap 的**第二条腿 `J.IsPushing(hBot)`**(对线期后推塔帧)
不在 t≤600 域内。若要给 capmono 最后一次机会,应去 **t>600 推塔窗**另建观测,
而不是加大对线期样本量。

---

## 6. 核验结论(格式:id: 状态 + 帧证据)

- **`capmono`: SILENT(隔离后,方向性负、落在噪声内)。** 证据:32 局 806 帧,
  DiD −7.9pp ± 9.3,逐种子 2/4,阴性对照臂内差 +0.3pp;逐帧 ①⑤ 显示"拒扑"形状在
  capmono 关闭时同样发生。**条件 (a) 隔离证明未通过。**
- **降级 14:37Z 的记录**:那轮的 `capmono: WORKING(方向性,未隔离)`
  **应改为「已被隔离波推翻,不成立」**,test_set.md 里的 (a) 已核标记需要撤回。
- 本波同 armed 的其余 15 个 id 不在本轮核验范围,沿用既有记录。

---

## 7. 边界(不硬猜)

- clean 域是真实触发集的**超集**:raw desire ∈(0.72,0.9]、lethality、self-risk 门
  **离线不可判**(dumper 不落 `GetEstimatedDamageToTarget`)。
- 弱负 DiD 与"capmono 触发太稀、动不了聚合"完全一致,**不能反过来说 capmono 有害**
  (本波两臂两侧 commit 帧 8s 内死亡率均 ~2%,无恶化迹象)。
- 视野(迷雾)= 既有 GH #27,不重复立案。

---

## 8. 开的 issue / 评论

- **新开 `[strategy] #72`**:capmono 隔离判决 + 检测器经验零点 + 全部帧证据 + 四条建议
  (不 promote;两个通道都不够分辨率;若要救去 `IsPushing` 腿;把"行为检测器也有经验零点"
  登记进测量协议)。
- **评论 `[batch] #70`(时效性最强)**:批测台 18:06Z §4 写明 arm B 收割要
  **「用行为检测器(录像组 clean 域拒扑率 45.2% vs 28.6%,OR=2.06)」判读** ——
  **那两个数字是我们组给的,现在我们组自己作废它**。已请其把 `capmono` 的 verdict 栏
  写「不可判(两通道均低于 MDE)」而非 pass/fail。赶在其 ~20:0xZ 收割前送达。
- **评论 #72 更正一处数字**(见 §10)。

---

## 9. 新工具(已入库)

`tools/batch_test/behavioral/capmono_refusal.py` —— clean 域拒扑率两臂 DiD 验证器。
复用 `roam_conversion.load_game`(幻象 idx 锁 / `_paused_spans` / #43 赛后尾巴截断),
docstring **写死**:clean 域承重条款(850u 下界为什么在)、**经验零点 ±15pp/种子 + MDE ±21pp**、
**域偏向对线期是正确的(附代码锚点)不要去"修"**、超集边界、`IsPushing` 腿未覆盖。
输出含 seed-paired DiD 与零通道噪声本底。32 局复跑逐字复现本报告全部数字。

（上一轮的 `illusion_audit.py` 留在 scratchpad 未入库,本轮不动它。）

---

## 10. 本轮我自己的错(先报小后修大)

写 issue #72 正文时,我用 jsonl 快算去重表,**dedup 过滤器写反了**(选中"被去掉的重复帧"
而非"去重后的集合"),得出 DiD_dedup = −11.0pp。**入库工具复跑后正确值是 −4.8pp。**
已在 #72 追评逐格更正。**帧级四格、逐种子表、seed-paired DiD、零通道 SD 全部不受影响
(经工具复跑逐字复现)**;更正后 DiD_dedup 反而更小,与结论同向且更强。
**教训:入库工具跑完再引用数字,不要在 issue 里手算第二遍。**

---

## 11. 工具坑(新增,供后来者)

- **镜像不固定对局走势**:同 seed/同 draft/同 side 的两次 replay 到对线中期完全发散
  ⇒ **逐英雄同时刻跨局对拍无效**,"同英雄有无 gate"只能靠统计 DiD。
- **clean 域逐帧 retreat/commit 二分在团战 swirl 里高噪**:乱斗中英雄离开"最近敌人"
  同时在打"另一个敌人",会被误记为 retreat。**dedup + 结局(回血/死亡)批核是必要的二次过滤。**
- `snapshots[].hero` **已带** `npc_dota_hero_` 前缀,拼 future-hp 时勿再加前缀
  (子代理中招:双前缀 → 静默 `None`,全部结局判空)。
- **单臂读数会把已发布行为记成新 id 的功劳**(§4⑤):没有阴性对照臂时,armed 侧
  漂亮帧的说服力被系统性高估。**以后 id 的 (a) 尽量等隔离波。**

---

## 12. 已检查 run_id / 对局(累计,勿重复看)

本轮新增,每 run 4 局全部跑 dumper timeline:

| run | 局 |
|---|---|
| `armA_160902` | 161810, 162324, 162824, 163252 |
| `armA_160904` | 161823, 162351, 162905, 163418 |
| `armA_160908` | 161808, 162322, 162821, 163337 |
| `armA_160911` | 161815, 162344, 162859, 163429 |
| `armB_180759` | 181720, 182304, 182803, 183316 |
| `armB_180802` | 181721, 182251, 182803, 183317 |
| `armB_180805` | 181738, 182322, 182906, 183451 |
| `armB_180808` | 181711, 182239, 182809, 183353 |

**32/32 镜像局宽扫 + 6 局逐帧深查**;8 个暖场局(每 run 首局,`script_version` 无
`mirror:` 前缀)已确认作废。arm B 在本轮触发时仍在产出,后续新局留给下一轮
—— 但按 §3 结论,**加局对 capmono 定性无帮助**(瓶颈是检测器分辨率 ±21pp,不是样本量)。

---

## 13. 下一轮优先

1. **`cmrguard` 若按 #63+#66 收窄** → **必须用施法前帧口径**复跑四条既有验收 + #66 第 5 条 recall。
2. **`capmono` 的最后一次机会**:去 **`IsPushing` 腿(t>600 推塔窗)**建观测(§5),
   或接受它按 (c) 逻辑依据 + 不劣性判(纯 `min`,结构上只降不升)。
3. `l1trade`/`l5combo` 自身 (a)(§G.5,连续**八**轮欠)—— 仍缺能分离出价来源的域定义。
4. §③ Tier-1(shipped-helper 证据)+ Tier-2 剩下 4 条,按「一次一件」纪律排。

---

## 14. 验证(铁律 6)

本会话**未改动任何 Lua**(`git status`:改动仅 `iterations/` 下报告/章程 +
新增 `tools/batch_test/behavioral/capmono_refusal.py`),且本容器**未装
`luacheck`/`lua5.1`**(与批测台 18:06Z §6 同状况),按铁律 6 立法目的本轮无适用对象。
新增 Python 工具已过 `ast.parse` 语法检查,并在 32 局真实数据上端到端复跑,
逐字复现本报告全部数字。

## 15. 本次会话 token 用量

```
TOKENS total_in=9,727,843 out=110,239 turns=85
```
(为"到统计时刻为止"的数字,统计后的收尾回合不计入。)
