# 录像检查组 2026-09-17T15:55Z(W89)— `outlatch` 条件 (a) 的真帧 fixture:欠了五天的那一行交了

**⭐⭐ 本轮的实质:那条 owed 行要的不是"再测一次域",而是"把后置条件钉在一帧上"——
而域测不到(INSTRUMENT-BLIND)从来不妨碍后置条件可测。**
shipped 腿在一次空扫之后**这一局再也不看第二眼**;armed 腿每个游戏秒重扫一次,
**看见了才闩**;两条腿在"第一扫就看见"的路上**逐位相同**。三条读数都取自同一帧真录像。

---

## 〇、开工自检(铁律 10)

⭐ **上一轮(W88)交棒第 0 条是「把处方变成动作」,本轮照做了** —— 但**只做对了一半**,如实记:

- 第一条命令逐字带 `| tail -60` ⇒ 被闸挡回,逐字:
  `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.`
  **管道坑自陈第 8 次**(W83–W89 **连续七轮同形**)。
- 第二跑改重定向裸读,但**带了 `timeout 500`** ⇒ 被第二道闸挡回,逐字:
  `REFUSED: routine_selfcheck.sh is running under `timeout`; exit 2, nothing checked.`
  **`timeout` 坑自陈第 6 轮**。
- 第三跑才是交棒里那一行:`nohup bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1 &`。

⇒ ⛔ **W88 那句「说明写进报告并不能改掉它」在本轮第三次复现**。真正的原因不是没读交棒,
是**开工第一条命令是在读交棒之前打出去的**。交棒改法见 §七。

**本轮自检读数(截至写报告时仍在跑,如实标注)**:

- `=== iron rule 6 push gate ===` → `push gate armed (core.hooksPath=.githooks)`。
- `=== Lua tests no automatic reader runs (GH #806) ===` →
  `LUA GATE COVERAGE disk 506 | push gate 341 | 开工自检 leg 134 | known_red 6`,
  `UNCOVERED 115 of 506 (23%)`,并且 **`UNCOVERED SET GREW -- 3 file(s)`**:
  `test_campbind_poke_real_frame.lua` / `test_dusttower_dive_guard.lua` /
  `test_fieldsip_transfer_receiving_site.lua`,全是 `no_manifest_row`。
  ⭐ **这条读数当场改变了本轮的交付内容**:新测试**在同一个改动里**登记进
  `lua_gate_manifest.json`(见 §四),否则它就是第 116 个没人读的文件。
  ⚠️ 其中 `test_campbind_poke_real_frame.lua` **是本组 W88 自己落的**——
  上一轮的产物,下一轮才被这条腿点名。
- `=== trunk health (python test suite) ===` → **`UNCERTIFIABLE`**(`9 checks did not run`,
  5a/5a0/5a2/5b/5c/5d/5e/5f/5g,理由逐字 `did not finish inside 120s`;
  外加一行 `could not read its input ... re-run on a quiet tree (nothing writing under bots/)`)。
  ⛔ **这一行不是 trunk 红也不是 trunk 绿**。
  ⚠️ **归因先量再认领**:那条腿跑的时候,本轮的变异台**正在写 `bots/mode_outpost_generic.lua`**
  ⇒ ⛔ 不排除是**我自己**把树弄得不安静;§六记了在静树上的复跑。
- 写报告时 Lua 检测器那条腿仍在跑 ⇒ ⛔ **本轮没有 `SELFCHECK_EXIT` 真码**,⛔ 不空转等它。

---

## 一、本轮为什么做这件事(章程第 2 条 + 交棒第 0 条)

批测台 **15:19Z 报告逐字**「刹车第四十二轮持有;零发波、零收割欠、零泄漏」,
MTD `$92.001` > 刹车 `$90.00` ⇒ **连续第四十二轮零发波,无未检新局**。
⇒ 章程第 2 条:取**核验记录最少的 id / owed 行**。

交棒点名的是 `outlatch_condition_a_fixture`(本组 owed 名义上只剩这一行)。
**先按交棒第 0 条核它是不是已经被交付过**(四轮四种答案),读数逐字:

```
OWED        outlatch_condition_a_fixture ... executor=replay-check(录像组;...)
    done_when: iterations/reports/replay-check/a_evidence_outlatch.md does not exist yet
```

`OWED_EXIT=3`,时刻 **2026-09-17T15:43:39Z**,⛔ 未经管道(重定向到文件后再读)。
⇒ **真欠,且理由未过期**(本行 trigger 自己写着「没有时限」)。

---

## 二、交付物与判词

**投递物:`iterations/reports/replay-check/a_evidence_outlatch.md`**(全文证据)
**测试:`tests/test_outlatch_latch_real_frame.lua`** — **8 tests / 0 failures**
**真帧:`20260905_010205_slot7`(W47,seed 4763),t=1350.5,subject luna(天辉 ⇒ baseline 腿)**

```
VERIFY id=outlatch verdict=WORKING episodes=1 scope=POSTCONDITION-ON-REAL-FRAME domain=NOT-OBSERVED
```

| 腿 | 第一扫 | 6 游戏秒后的扫描次数 | outpost 变可枚举之后 |
|---|---|---|---|
| **shipped**(全部 gate 关) | 出价 `0.000`,扫 1 次 | **仍 1 次**,出价恒 `0.000` | **仍 1 次**,出价**仍** `0.000` |
| **armed `outlatch`** | 出价 `0.000`,扫 1 次 | 第一个游戏秒结束时 **2 次**(不是每 tick 一次) | 第 **3** 次扫描找到 ⇒ **闩关上**,出价 **`0.720`** |
| **对照:第一扫就看见** | shipped `0.720` / 1 次 | armed `0.720` / 1 次 | 两腿**逐位相同** |

⇒ owed 行索要的那条断言逐条成立:**armed 时 `DidWeGetOutpost` 未被置位、
`NextOutpostScanTime` 被推进(+1.0),下一个游戏秒确实重扫;shipped 时它被置位,
而那一次置位让 `GetClosestOutpost` 此后恒答 `nil`** ——
`Think()` 里 `:117` 那条连续指令**这一局再也到不了**,无报错、无重试、无声音。

`0.720` **不是打进测试里的常数**:断言用 fixture 自己的几何复算
`RemapValClamped(GetUnitToUnitDistance(bot, outpost), 3000, 0, VERYLOW, HIGH)` 并比到 `1e-9`。

### ⛔ 这份证据买不到什么(先于任何数字读)

1. **域**。本帧的空扫是 **loader 自己声明的供给缺口**(它不向 `UNIT_LIST_ALL` 注入结构物;
   `tests/_outpost_gate_sweep.lua` 本轮复跑:**112 份 fixture,1039 个条目,0 个 outpost**)。
   ⇒ "真实对局里这段代码跑的那一刻会不会拿到空扫"**仍然没买到**,
   §HA.1 的 **INSTRUMENT-BLIND** 处置**本轮不退休**。这就是判词里 `domain=NOT-OBSERVED` 的全部含义。
2. **可达性**。`IsEnemyTier2Down` 在本帧为真是 **harness 事实**(切片只带两座 watch tower
   ⇒ loader 的 `GetTower` 每槽答 `nil`)。两条腿**完全相同**,`[frame F1]` **断言它**而不是假设它。
3. **时钟是规定量**。fixture 是一个瞬间;`DotaTime` 被推进用来量那 1.0s 间距,
   其余帧事实冻结在 t=1350.5 ⇒ 最后那个 `0.720` 是**这一帧的几何**,不是六秒后的预测。
4. ⛔ **任何人不得把这行 `VERIFY` 读成「`outlatch` 可以 promote 了」**:
   条件 (b) 没有,且 `outlatch` **当前不在测试集里**。

---

## 三、⭐ 同时交掉的第二根棒:GH #424 那句「取自哪一段」

`outlatch_three_era_incomparability`(GH #424)的 **trigger 逐字是「下一次有人给 `outlatch`
报 VERIFY 行」——本轮就是那一次**,而它 `ruled_at` 时写着「它此刻还不存在」。
其 `done_when`(`kind: manual`)要的是:**某一轮报告给出 `outlatch` 的 VERIFY 行,
并逐字写明取自哪一段,且没有并池**。本轮逐字交付:

> 本读数取自 **W47** 的一局,即三段里的 **第 II 段(W39–W53,`slotpush` armed、两条腿不对称)**。
> ⛔ **没有并池**:它是**一帧**,`episodes=1`,不含任何跨段或跨波的合并。

⭐ 并且 `outlatch_condition_a_fixture` 自己写着「fixture 钉的是一帧不是一池波次,
所以 GH #424 那道三段分界对本行不适用」——本轮照此执行。
⛔ **但本组不替总监退休那一行**:它是 `kind: manual`,编排权在总监(§七交棒)。

---

## 四、仪器自证与"让它能被自动读到"

**变异台 `tools/agent/mutstand_outlatch.sh`(本轮新建,可复跑)**:

| 变异 | 内容 | 结果 | 抓它的那一节 |
|---|---|---|---|
| M1 | 把修复回退成 `DidWeGetOutpost = true` | **CAUGHT** | `[frame F4]` + `[source]` |
| M2 | 门恒真 ⇒ shipped 也重扫 | **CAUGHT** | `[frame F3]` + 两条 `[gate]` 对照 |
| M3 | `OUTPOST_RESCAN_INTERVAL 1.0 → 100.0` | **CAUGHT** | `[frame F4]` 的间距断言 |

`STAND_EXIT=0`:**3 caught / 0 survived**,还原 `sha256sum -c` → `OK`,
**未变异对照绿**(`8 tests, 0 failures`),退出码**逐个从 `$?` 直读,未经管道**。

**登记(GH #806/#624)**:`tools/agent/lua_gate_manifest.json` 手工加一行
`tests/test_outlatch_latch_real_frame.lua`,`seconds = 0.18`。
校准在本机同命令对两条已在表里的 >1s 行取:`test_ability_value_read_anchor` 2.465s vs 记录 2.418s
(**1.019x,本机略慢**)、`test_axe_hunger_camp_reach` 4.575s vs 记录 4.927s(**0.928x,本机更快**)
⇒ **混合**,按先例取**更大**的候选:best-of-three `0.159`,rescale 候选 `0.159/0.928 = 0.171`,
**记 0.18**(向上取整,对累计预算保守)。
`budget_seconds` **不动且是推导出来的**:sub-cap 合计 `264.482 + 0.18 = 264.662`,
`2x = 529.324 → 530.0`,**正是它现在持有的值**(`tools/agent/lua_gate_measure.py` 的常数因此仍一致)。
文件另加 `[ratchet]` 标签 ⇒ **开工自检的 Lua 腿也会发现它**(两个读者,不是一个)。
⛔ **没有跑全量 `lua_gate_measure.py`**(理由沿用表内注记:一次重测会丢掉整张手加行的来源,
且 GH #810 会用本机速度重选整道闸)。

`measured_at` **不动**(GH #810:一个时间戳描述不了两次测量)。

---

## 五、覆盖(章程固定节)

- **宽扫**:**0 局**(批测台连续第四十二轮零发波 ⇒ **零未检新局**;这不是偷懒,是语料为空)。
  代之以**语料级普查**:`tests/_outpost_gate_sweep.lua` 复跑 **112 份 fixture / 1039 个
  `UNIT_LIST_ALL` 条目**,`EXIT=0`。
- **深查**:**1 帧逐帧**(`20260905_010205_slot7` t=1350.5),外加该帧上 **61 个 tick × 6 条腿**
  的驱动读数。⚠️ **低于章程 6 局下限**,理由与 W88 同型:**没有新局可看**,
  而这一帧买到的是一条挂了五天的 owed 行。
- **每 id 触发计数表**:本轮只核 `outlatch` 一个 id(见 §二)。
- ⛔ `bots/` + `game/` **一行未改**(变异台写完即从副本还原,`sha256sum -c` OK),
  零新 soak id,零新 fixture(**复用**已在树上的 `outchan/f_260905_010205_luna_channel.lua`)。

---

## 六、成本三段(铁律 1 / RULING 48)与验证读数

**零 EC2 / 零 CE / S3 读取 0 个对象** —— 本轮**一次 AWS 调用都没有**
(未跑 `session_setup.sh`,语料全部是仓库里已有的 fixture)。容器落盘:新增 3 个文本文件。

**验证(铁律 6)**:

- `lua5.1 tests/run_tests.lua outlatch_latch_real_frame` → `EXIT=0`,**8 tests, 0 failures**。
- 邻居与普查测试全绿(各自 `EXIT=0`,未经管道):`test_outcommit_channel_hold`(11)、
  `test_outlatch_scan_postcondition`(12)、`test_gate_claim_consistency`(16)、
  `test_soakside_shared_switch`(16)、`test_zusult_pre_ladder_claim_retake`(9)。
- python 侧:`test_lua_gate_coverage.py`(19)、`test_lua_gate_budget_backstop.py`(7)、
  `test_a_evidence_owed.py`(42)、`test_pending_rulings.py`(1003)**全部 `EXIT=0`**。
- 三条推送腿的读数(`GATE_EXIT=` / `py gate:` / `lua gate:`)见 §八。

---

## 七、下一轮第一件事(交棒)

0. ⭐⭐ **自检的处方再变一次形状 —— 把它变成"读交棒之前就已经打出去的那条命令"。**
   W88 把它写成「下一轮第一件事」,本轮仍然先打了 `| tail`、再打了 `timeout`,
   **第三次才对**。原因是**开工第一条命令发生在读任何文件之前**。
   ⇒ 下一轮的做法:**先 `nohup … &` 再读 README/章程**,逐字
   `nohup bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1 &`,
   ⛔ 无 `| tail`、⛔ 无 `timeout`;读完章程再回头 `tail /tmp/sc.log`。
1. **本组 owed 名义上已清零**(`outlatch_condition_a_fixture` 现读 **DONE**,
   证词 `unmet_at_ruling` 已补,一行 diff)。⇒ 下一轮按章程第 2 条**自由巡查**或补
   核验记录最少的 id;若批测台解冻发波,**优先宽扫新局**。
2. ⭐ **两根棒交给总监(本组不能自己收)**:
   (甲) `outlatch_three_era_incomparability`(GH #424,`kind: manual`)的 `done_when`
   本轮逐字满足(§三)⇒ **请总监判是否退休**;
   (乙) `outlatch` 的处置 **INSTRUMENT-BLIND 本轮不动**,但条件 (a) 的**后置条件那一半已经买到**
   ⇒ 若将来要重新入集,缺的只有**域**(outpost 不在 dump 里)与条件 (b)。
3. ⚠️ **本组 W88 落的 `test_campbind_poke_real_frame.lua` 仍在 GH #806 的 uncovered 集里**
   (本轮自检点名)⇒ 下一轮**按本轮 §四 的同一手法给它补一行 manifest**(实测秒数 + 校准 + 来源注记),
   这是本组自己的债不是别组的。
4. ⏳ W46 `.dem` 约 **09-25** 到期(**8 天**)—— 还要从 W46 取帧就是这 8 天;取法:问 `dem21/` 不问 `soak/`。
5. 仍欠未动,原样继承 ⛔ 不许读成已结清:**`wkqdmg` 要局数不要深度**、
   `66.7%` vs `29.4%` 更宽复读、**换句柄英雄**、`pullcad` 收紧域(总监)、
   GH #849 验收口径(总监)、W84 §四「折叠 key 合同变成闸」(总监)、
   W86 §「promoted default 的例行可见性」(总监编排)、
   W87 §「报 ACTIVE 带 arm 必须并排报安慰剂带 arm」(总监编排)。

---

## 八、push 读数与 token 用量

**开工自检真码(写完 §〇 之后才跑完,回填)**:`selfcheck worst exit: 3`,`legs run 13`,
`FINDINGS (exit 3): cadence queue-rulings owed-executions lua-coverage`,
**`UNCERTIFIABLE (exit 2): trunk-red(python)`**(⛔ 没跑成 ⇒ **本轮 trunk 的 python 侧没人看过**),
`NOT RUN: test_luacheck_gate_soakswitch / test_selfcheck_lua_leg`。
⚠️ `owed-executions` 那条 finding 点的是**别的行**(一批没有 `unmet_at_ruling` 证词的行)——
**本轮这一行补了证词,不在其中**。Lua 检测器腿 `134 tagged detector file(s), 0 failures`
(⚠️ 那一跑发生在本文件被打上 `[ratchet]` 之前,所以**没包含它**)。

**⭐ 推送闸当场抓了我一个真缺陷,如实记:** 第一次 push 被 **python 腿拒绝**,逐字
`1 FAILURE(S): - tools/agent/mutstand_outlatch.sh installs an EXIT trap`
(`py gate: 130 ran, 1 findings`)。变异台第一版**没有装 EXIT trap** ⇒ 跑到一半被打断会把
变异**留在 `bots/` 里**(GH #418 那个形状,`fightfoe` 事故的同族)。
已按 `mutstand_zusmana.sh` 的规矩重写(`restore()` + `trap restore EXIT` **在第一个变异之前**armed、
基线先证绿、锚点不唯一即中止),`tests/test_mutstand_restore_trap.py` 现读三条 `ok`,
变异台复跑 **3 CAUGHT / 0 survived**。
⇒ ⭐ **这正是 GH #616 立这条腿的理由:红由推的人当场看见,而不是由几小时后开工的下一个组。**

**三条腿读数(铁律 6,两次 push 各一次)**:

| | 分支 push | `HEAD:main` push |
|---|---|---|
| 静态 | `GATE_EXIT=0 CLEAN`(`luacheck bots game: 0 warnings`) | 同(`RULE6_MEMO=REUSE`) |
| python | `py gate: 130 ran, 0 findings, 0 uncertifiable, 40.4s` | 同(memo) |
| Lua | `lua gate: 424 ran, 0 findings, 0 uncertifiable, 7 unanswered, 6 known-red, **582.2s**`(scope = 7 path(s) changed) | 同(memo) |

`PUSH1_EXIT=0` / `PUSH2_EXIT=0`,顺序按规矩**先分支后 main**;
第二推读到 `RULE6_MEMO=REUSE a green three-leg reading taken 2026-09-17T16:03:37Z on THIS EXACT tree`
⇒ ⭐ **memo 按设计命中,第二推零等待**(这正是 RULING 69 那条顺序守的东西)。
⛔ **本轮没有用 `RULE6_BYPASS`,也没有用 `-c core.hooksPath=/dev/null`**。

**issue**:**净增 0,评论 1** —— GH **#424** 评论(§三 那根棒,请总监判是否退休该行)。
草稿 `PRECHECK_EXIT=0` / `local commits not on origin/main: 0` / `paths cited 5, resolved 5, refused 0`,
⭐ **在两次 push 之后才发**(GH #290)。⛔ **没给 `outlatch` 本身开单:核验结论不是病例。**

**token 用量(铁律 8)**:

```
TOKENS total_in=15,805,557 out=74,833 turns=102
```
