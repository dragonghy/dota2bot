# replay-check 2026-09-13T00:4x–01:2xZ — `campfarm` 的 fixture 层:交棒单要的那条断言**结构上买不到**(判别子在生成器源码里),但同一帧买到了**更该买的东西** —— #137 的 maxHP 机制在这一帧上**两个真英雄都不成立**,而路径上藏着**第四个远古阈值(9)**

```
VERIFY id=campfarm verdict=INDETERMINATE episodes=1
```

**段位声明(GH #424)**:语料只有 **W69**(27-id 家族,`arm_md5` 未复核,本轮不引跨波读数)。
本轮**不并池任何波次**,也**不引** W63 / W68 / W66+W67。所有读数出自**单局单帧**:
`spot_20260912_092624_1_main_0128b9 / 20260912_094042_slot1`,t=546.0。

**一句话**:上一轮(21:2xZ)交棒单第 (1) 条点名「`campfarm` 的 fixture(`--t 546.0 --hero sniper`)——
**唯一没试过的层**」。这一层做出来了,**而它要的那条断言买不到**——理由不在语料量,在
`make_fixture.py` 自己的源码里。⭐ **但同一帧把一件更重的事买了下来**:GH #137 立项时的
承重机制(「maxHP 农夫必然挑中远古」)**在这一帧上对两个真英雄都不成立**,而追下去发现
这条路径上有**第四个远古阈值,写在 `IsValidCreep` 里,值是 9**,任何 campfarm 头注都没提过它。

---

## 〇、覆盖(⚠️ 本轮低于章程下限,如实登记)

| 层 | 数字 |
|---|---|
| **宽扫** | **0 新局** —— `awsx s3 ls soak/` 最新四前缀仍是 `0926{22,24,27,29}_1_main_*` = **W69**,**连续第四轮无新波次**;W69 的 12/12 宽扫已由 09-12T18:54Z / 21:2xZ 两轮完成,本轮不重跑 |
| **深查(逐帧)** | **1 局**(`0128b9/094042`,承重 episode 逐帧穷尽)—— ⚠️ **章程下限是 6,本轮未达** |

⚠️ **未达下限,理由写在这里而不是省略**:本轮工作单元是交棒单点名的 **fixture 层**(源码层 + 单帧层),
不是语料层;而**同一份章程的工具坑第一条**(09-12 新踩)逐字禁止**再为 `campfarm` 买语料**
(「⛔ **不要再为 `campfarm` 买语料**」,理由是 MECHANISM 第一格要 50+ 局)。
**两条要求在无新波次的一轮里方向相反**,本轮选了交棒单点名的那条,并把缺口显式交给下一轮(见 §六)。

**AWS**:只读(`s3 ls` ×2、`s3 cp` 一份 23MB `.dem` + 一份 7KB analysis.json、dumper 缓存 HIT 未重建);
**零 EC2 / 零 CE / 零支出**;`AWS_SETUP_EXIT=0`、`DUMPER_EXIT=0`。

---

## 一、⛔ 交棒单要的那条断言:**结构上买不到**,判别子在生成器自己的源码里

交棒单(21:2xZ 报告 §3.4 末)逐字要的是:

> `make_fixture.py …094042…timeline.json --t 546.0 --hero sniper`,
> 断言 armed 世界交给 `J.Site.FindFarmNeutralTarget` 的表里**没有远古小兵**

**买不到,而且不是语料量问题。** `tools/batch_test/replayscope/make_fixture.py:474-478` 自己写着:

> Lane/neutral creeps at the instant. The dumper samples these on their own
> (coarser) interval and each sample carries **POSITION AND TEAM ONLY -- no
> entity id, no name, no health** (dumper/main.go: creepSnaps). So the block
> below is the whole truth available about creeps, and everything a test
> wants beyond position+team it has to declare for itself.

`FilterFarmNeutrals` 读的**唯一**谓词是 `IsAncientCreep()`,而它**不在这条流里**。
⭐ **本轮在这份 timeline 上第一手复核**:`tl_094042_slot1.json` 的 **135,137** 条 creep 采样,
键集合逐条是 `{t, team, x, y}`,一条都没有别的。⇒ 「这次 sweep 里哪几只是远古」
**在 fixture 落盘那一刻就被丢掉了**。

⭐ **同一段注释自己点名了同族先例**:「That is why **GH #354 section 5's "pin a fixture on the gap
frame" could not be built**: the datum the question is about was dropped at fixture-write time.」
—— 与 `campvoid_escape.py` 头注的「NEITHER CONJUNCT OF THE GATE CAN BE EVALUATED ON THIS
CORPUS」、与 `aimguard` 的「creep 流只有 `t,team,x,y`」**是同一条结构性拒绝的第三、第四例**。

**处置**:不写那条断言,改为把**拒绝本身钉成断言**。新测试的
`[limit] the fixture carries NO creep identity` 逐条检查 `fx.creeps` 的键集合,
**在 dumper 哪天开始发身份的那一天变红**,于是这条拒绝**会自己退休**,而不是留在散文里被下一轮当第一手源引用
(那正是工具坑第一条记的失效形状)。

⚠️ **本轮登记的新判别子(建议进工具坑)**:
**「给 X 钉一帧」这类交棒,动手前先问一句「X 的那个谓词读的字段,在 `make_fixture.py` 的
生成块里有没有?」** —— 判别子极便宜(一次 `grep -n 'neutral\|creep' make_fixture.py`,
本轮 30 行内命中),而它拦住的是**一整轮的 fixture 工作**。
⭐ 这条与 09-11T16:11Z 立的「先读工具头部 LIMITS」是同一族,但**换了一个文件**:
那条读的是**检测器**的 LIMITS,这条要读的是**生成器**的 LIMITS——
上一轮的交棒单读了前者、没读后者,于是点名了一个买不到的层。

---

## 二、⭐⭐⭐ 头号产出:#137 的承重机制,在这一帧上**两个真英雄都不成立**

### 2.1 #137 立项句

`tests/test_campfarm_ancient_target.lua` 与 `aba_site.lua:615` 头注逐字:

> For a maxHP farmer -- **viper, naga_siren, huskar**, or anyone holding
> **bfury/maelstrom/mjollnir/radiance** -- that is not a corner case but the
> **ROUTINE outcome**: an ancient creep has the most health on the field, so it
> is exactly the one `GetMaxHPCreep` returns.

### 2.2 主体(sniper)两条都不满足 —— 逐条现读,**全是 fixture 自带的真数据**

- `npc_dota_hero_sniper` **不在** `ConsiderFarmNeutralType` 的 13 个键里
  (现读:templar_assassin / sven / drow_ranger / phantom_lancer / naga_siren / viper /
  huskar / phantom_assassin / medusa / luna / tidehunter / nevermore / dragon_knight);
- t=546.0 的**真背包**:`blade_of_alacrity, belt_of_strength, magic_wand, power_treads,
  wraith_band, flask, faerie_fire`(7 格)—— **四件 maxHP 物品一件都没有**;
- ⇒ `FindFarmNeutralTarget` 一路落到 `targetCreep or GetMinHPCreep(creepList)`,
  即**纯最低血量挑选**;
- 它的提前返回 `HasArmorReduction` 在这一帧**结构性死**:它读的四个 modifier 来自
  TA 的 meld / Slardar 的 amplify damage / medallion / solar crest,而**本局阵容没有 TA、没有 Slardar**
  (radiant: bristleback, skeleton_king, skywrath_mage, sniper, warlock;
  dire: obsidian_destroyer, ogre_magi, spirit_breaker, sven, viper),
  **全局第一件 solar crest 出现在 t=1191.5**(ogre_magi,**晚 645 秒**),**medallion 全局零命中**。

⇒ **远古小兵是这个主体的目标选择最不可能返回的东西,不是最可能的。**

### 2.3 ⭐⭐ 对照组本来是要复现 #137 的,**结果它也拒绝了远古** —— 这才是本轮最该读的一行

#137 第一个点名的 maxHP 农夫是 **viper**,而 viper **就在本局阵容里**(dire,`player_id=5`),
所以对照**不需要替身主体**(替身正是 `test_replay_004757_veno_ancient.lua` 抱怨过的那件事)。
把同一份混合营地表交给这一帧的真 viper:

- `ConsiderFarmNeutralType['npc_dota_hero_viper']()` 现跑 = **`maxHP`**(分支没走错);
- 它的真背包同样**没有**四件 maxHP 物品(wraith_band / magic_wand / wraith_band /
  enchanted_mango / ring_of_health / power_treads / clarity)⇒ 它走 maxHP **靠的是名字表**;
- **然而它返回的仍是普通小兵**;
- **只把远古交给它,它返回 `nil`** —— 在它那里远古**根本不是候选**。

原因见 §三:**这个 viper 是 9 级**。

⇒ **这一帧上两个真英雄都拒绝远古,理由却是两条不同的、相差一级的**。
⇒ **#137 的 maxHP 机制需要一个恰好 10 或 11 级的 maxHP 农夫** —— 一个**两级宽的窗口**,
正是本组 campfarm 表一直在读的那条 **10..11 带**(这是本轮第一次从**机制侧**独立推出这个带,
而不是从工具的分带参数里继承它)。**本局唯一的 maxHP 农夫差一级。**

---

## 三、⭐⭐ 第四个远古阈值:`IsValidCreep` 里的 **9**,任何 campfarm 头注都没写过

`bots/FunLib/utils.lua:1135-1140`:

```lua
function ____exports.IsValidCreep(target)
    if target == nil then return false end
    return ____exports.IsValidUnit(target) and target:GetHealth() < 5000
       and not target:IsHero()
       and (GetBot():GetLevel() > 9 or not target:IsAncientCreep())
end
```

`GetMinHPCreep` 与 `GetMaxHPCreep` **都**逐个 creep 调它 ⇒ **每一个读者各自又带了一道远古闸,键在 9。**

**于是这条路径上现在有四个不同的远古阈值:**

| 值 | 位置 | 管谁 |
|---|---|---|
| **9** | `utils.lua:1139` `IsValidCreep` | **目标选择的每一次 creep 校验**(本轮新发现) |
| 10 | `mode_farm_generic.lua` 两处 `nNeutrals[1]` 子句 | 首个 creep 的放行问句 |
| 12 | `aba_site.lua:427` `ANCIENT_MIN_LEVEL` / 营地梯子 | `campfarm` 的过滤器、`campgrade` |
| **无** | 1000u `neutralCreeps` 分支、`Action_AttackUnit(nNeutrals[1], true)`(`:992`) | 什么都不管 |

⭐ **这是 #137 §1 第 2 条建议的直接延伸**:该 issue 已经点名「`RefreshCamp` 的 `<=11`
与 `GetClosestNeutralSpwan` 的 `>=10` **这两个数字得先对齐一个**」。
**本轮把「两个」变成「四个」**,并且新发现的那个(9)**恰好是决定本帧读数的那一个**
—— 主体 10 级 `10 > 9` 放行、对照 9 级 `9 > 9` 假 ⇒ 拦死。
**同一条 if,相差一级,给出相反的世界。**

⚠️ **归属**:这是源码结构问题,**本组不改 bot 代码**,追评 #137 交协同组(见 §五)。

---

## 四、逐帧:t=546.0 那 12 次右键,**在 armed 世界里没有任何 wrapper 内的消费者能发出**

### 4.1 事件流(逐条现读,`.dem` → dumper → 本轮自建 timeline)

| 时刻 | 事件 |
|---|---|
| 540.9 | sniper 施放 `sniper_shrapnel`(落在**普通营**上) |
| **541.2** | sniper **打死** `npc_dota_neutral_centaur_outrunner`(金 + 经验入账) |
| **545.1** | `npc_dota_neutral_centaur_khan` 死 ⇒ **普通营清空** |
| **546.0** | **sniper → `npc_dota_neutral_prowler_acolyte`,`dota_unknown`,115 伤害** ← **本帧** |
| 546.9 | 第一条落在远古身上的 shrapnel tick(sniper 自己 5.1 秒前对**另一个营**的 AoE) |
| 547.0 | bristleback 的 quill spray 溅到两只 prowler |
| **548.3** | **全局第一条 prowler → 英雄**的伤害 |

⭐ **前置激怒判别子(上一轮登记的那条)本轮独立复核,两个计数都是零**:
t=546.0 **之前**,以 prowler 为 **target** 的伤害行 **0 条**;以 prowler 为 **actor** 的伤害行 **0 条**。
⇒ 546.0 那一击**严格早于**任何还手,**也早于**该判别子警告的两种污染形状
(自己的 AoE 在 546.9、队友的 quill 在 547.0,**都在它之后**)。

⚠️ **prowler 是远古营**:本轮不靠地图记忆断定 —— 仓内既有先例逐字写过
(`tests/test_replay_004757_veno_ancient.lua`:「`npc_dota_neutral_prowler_shaman`
(a Prowler **ancient**)」)。

### 4.2 ⭐ 由此得到的收窄(**这是本轮对 09-10「命令流不在 dump 里」那堵墙的实际进展**)

**armed 腿已第一手确认**:`analysis.json` 的
`script_version = mirror:…,campfarm,…:s13027:**radiant**`,主体 team 2 = radiant ⇒ **armed**。

在 t=546.0 那一瞬,**普通营已于 545.1 清空** ⇒ sweep 里能返回的中立单位**只剩远古**。
`FilterFarmNeutrals` 的头注**自己声明了**这种情况的后果:

> when the only creeps in the sweep are ancient ones, **the armed list is EMPTY**, and the
> farm block then takes its own existing "nothing here" path

**armed 时那张表是空的**,于是 wrapper 下游**三个读者一个都发不出这一击**:
目标选择拿到空表返回 `nil`、两处 `[1]` 子句没有 `[1]`、
`Action_AttackUnit(nNeutrals[1], true)`(`:992`)也没有 `[1]`。
**三条都已在新测试里跑成断言,不是推理。**

⇒ **这一击来自 `NeutralFarmList` 之外的消费者。**
而「wrapper 之外还有消费者」**不是本轮的猜测** —— `mode_farm_generic.lua` 自己的
GH #265 头注逐字写着:这个文件读中立单位走**两个**引擎调用,
`bot:GetNearbyNeutralCreeps` 的 **3 个站点一个都没被包**。

⛔ **仍判 INDETERMINATE,理由未变**:命令流不在 dump 里(GH #521 同族),
**离线说不出是哪个消费者**。本轮**收窄了消费者集合,没有点名消费者**。
⚠️ 另:`.dem` 不带 bot mode ⇒「当时是不是 `BOT_MODE_FARM`」照旧不可答
(与 `test_replay_212636_tide_ancient.lua` 同一条边界)。

---

## 五、产物与量具

### 5.1 新增两份仓内文件(本轮**唯一**的树上代码改动,均在 `tests/`)

- `tests/fixtures/f_20260912_094042_sniper_546.lua` —— `make_fixture.py` 生成,
  `MF_EXIT=0`,`units=10, buildings=38, creeps=9, modifiers=18, recent_damage=13`;
- `tests/test_replay_094042_sniper_ancient.lua` —— **12 个 case,12 PASS / 0 FAIL**。

⚠️ **`bots/` `game/` 一行未改**(本组不改 bot 代码)。

### 5.2 ⭐ 变异台:**4/4 杀死,0 存活,0 NO-OP**,台子本身可复现

按 `evidence-discipline` 规则 1,**从文件副本恢复,不从 git 恢复**;
脚本 `mutstand.sh` 全程落 scratchpad(**零新增仓库工具文件**);
退出码 `MUTSTAND_EXIT=0` **裸读未经管道**。

| 变异 | 改了什么 | 结果 |
|---|---|---|
| M0 | 无(基线) | pass=12 fail=0 |
| **M1** | `FilterFarmNeutrals` 永不丢弃 | **杀** — 两条 `[gate]` 红 |
| **M2** | `IsValidCreep` 的 `> 9` 改 `> 8` | **杀** — `[source] 第四个阈值` + `[control] viper` 红 |
| **M3** | 给 sniper 加一条 `maxHP` 名字表条目 | **杀** — `[frame+source] minHP` + `[source] sniper 不在表里` 红 |
| **M4** | `GetMinHPCreep` 改成取最高血 | **杀** — `[frame+source] minHP` 红 |

⭐ **M4 只杀一条,而那是对的**:viper 那条在 M4 下仍绿,因为 9 级时远古**根本不是候选**,
最高血写法也只能返回普通小兵 —— **测试文件自己的正文预先解释了这一点**,
不是事后找补(§2.3 与 §三 是同一条因果)。
**恢复后 `cmp` 逐字节核对**:`SITE restored OK` / `UTILS restored OK`。

### 5.3 量具纪律

- **零新增仓库工具文件**;两个探针全程 scratchpad,只读 timeline JSON,
  **没有重新实现任何既有谓词**;
- ⭐ **一处 vacuous-pass 被自己的正控制拦下**:`[frame] 主体不持有四件 maxHP 物品`
  这条若只走 `HasItem`,会被 **replay_fixture 的 NAMESPACE 洞**放行
  (该 loader 头注自报:114 个 fixture 物品名里 **24 个**从不以 `item_<name>` 出现在 `bots/` 里,
  **而 `bfury` 恰好是那一族**)⇒ 本轮改为**在 dump 原始字符串上**逐条检查,
  并**另加一条正控制**(`item_power_treads` 必须被真谓词看见),
  否则那条否定断言**在空世界上也会绿**;
- ⛔ **未登记进 `lua_gate_manifest.json`,是故意的**:**GH #783** 逐字说明
  「登记一条测试 = 清空 `known_red` 赦免名单 = 让下一个人的 push 被 9 条既存红挡下」。
  ⇒ 本轮**不碰那个 manifest**,新测试留在快棘轮之外,**并在此声明它因此不受钩子保护**。

### 5.4 ⚠️ 一处本轮自己制造的假红,如实登记

跑同族测试时 `test_replay_212636_tide_ancient` / `test_abilanc_ancient_selector`
各报一次 `soak_side.lua already exists`。**那是本轮自己的并发**:
我把开工自检重新挂到后台,它的 Lua 腿与我的手跑撞在同一个全局 inode 上
(**正是 GH #417 立的那条护栏,它工作正常**)。自检窗口过后**两条各自复跑全绿**
(9/0 与 13/0)。⚠️ **不是 trunk red,不点名任何组**;
**教训归本轮**:别在手跑 Lua 测试的同时后台挂自检。

---

## 六、交棒(下一轮第一件事)

1. ⛔⛔ **撤回上一轮交棒单第 (1) 条**:`campfarm` 的「表里没有远古」断言**不要再做**,
   判别子是「`make_fixture.py` 的生成块里有没有那个字段」(§一),答案是**没有**,
   且**本轮已把这条拒绝钉成会自己退休的断言**。
2. ⭐ **还本轮欠的深查局数**(本轮 1/6)。**但不要为 `campfarm` 买**(工具坑第一条仍然有效);
   按章程工作流第 2 条挑**核验记录最少且语料能买**的 id 补课。
3. ⛔ **别重跑本轮三条**:135,137 条 creep 采样的键集合(已第一手复核)、
   本局无 TA/无 Slardar/首件 solar crest 在 t=1191.5(已复核)、
   4/4 变异台(已跑,台子在 scratchpad,要重跑请重建而不是引用本报告的数字)。
4. ⭐ **两条新判别子进工具坑**:(i) **钉帧交棒动手前先读生成器 `make_fixture.py` 的 LIMITS**,
   不只读检测器的;(ii) **fixture 上的物品否定断言必须配一条正控制**,
   否则 NAMESPACE 洞会让它在空世界上也绿。
5. ⭐ **等协同组对 §三(四个远古阈值)的处置** —— 已追评 #137,**本组不自改 bot 代码**。

---

## 七、本轮 issue

**先搜后开**:`list_issues` 直读最近 12 条(⚠️ 章程已登记 `search_issues` 的语义检索
在本仓对这一族**不可靠**,本轮不依赖它);`campfarm` 的归属 issue 现读确认是 **#137**
(上一轮补记已查明「读数属于 #137 而不该新开 issue」)。

**净增 0 条新单,1 条追评 #137**:fixture 层结构性拒绝 + 第四个远古阈值(9)+
maxHP 机制的两级窗口 + t=546.0 消费者收窄。

---

## 八、铁律 6 / 开工自检 / token

（收尾回填，见本文件末尾追加节。）
