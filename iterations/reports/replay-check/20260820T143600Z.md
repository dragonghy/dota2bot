# 录像检查组 2026-08-20T14:36Z(第二十次触发)

## 一句话

执行「下一轮优先」第 1 条 —— **`tpdead` 的首次条件 (a) 核验**(GH #37 rec 3,
armed 已多波、核验记录一直是 0)。跨三个含 `tpdead` 的波次 + 一个不含它的对照臂
合并语料,用 `tp_attribution.py` 的落点归属法钉出 50 次可归属救援 TP,收窄到
**tpdead 唯一的触发条件**(被救友军在 12s 承诺窗内死亡),逐帧核验响应者是否被
**从 DEFEND 地板上释放**。结论:**`tpdead` = WORKING(条件 (a) 成立,帧级正归属;
强度:强,但非镜像隔离)**。顺带在大得多的样本上**重新证实 #37 的效果侧缺陷**
(50 次归属救援里 20 次友军当场死 = 40%,落点→友军中位 2006u)。**未开新 issue**
(tpdead 正是 #37 rec 3;无新缺陷)——评论 #37 交付。

## 宽扫 X / 深查 Z / 每 id 触发计数

- **宽扫:63/64 局**(16 个 run × 4 局;`_101143` 首扫因 dumper 二进制 `Permission
  denied` 掉 1 局,单独重跑补齐 → 实际 64/64)。三波含 `tpdead`(`_2211**` 种子867-870、
  `_0411**` 种子872/875/885/887、`_1011**` 种子888/895/896/906,均 17-id 全 armed
  cand 串含 `tpdead`)+ 一波对照(`_1211**` lf_rescue bisect,cand 串**不含** `tpdead`,
  种子859-862)。`sweep_summary.md` 15 个通用检测器两组无可归因粗粒度塌陷。
- **深查:20 次归属救援全部逐帧还原**(横跨约 15 局),其中 4 局手工逐帧精读
  (3 ON 帧 + 1 OFF 帧,见 §2)。超过章程 6 局下限。
- 触发计数(本轮唯一被逐帧核验的 armed id):
  | id | 语料 | 触发(归属) | 状态 |
  |---|---|---|---|
  | `tpdead` | 50 次可归属救援(ON 34 / OFF 16),其中 20 次友军在窗内死(ON 17 / OFF 3) | ON 侧 17 次真触发场景 | **WORKING(a)** |

## 1. 侧别/归属方法(可复现)

- **落点归属**(#37 立的法):`J.GetNearbyLocationToTp` 确定性(最近存活己方塔 +575
  朝友军偏移),落点与预测吻合 ≤600u 才算 `lf_rescue` 的真触发。50/93 casts 通过。
- **ON/OFF 判定不读 stamp 字面量**:直接从 `games_manifest.jsonl` 的 cand 串读
  `'tpdead' in cand`,三波含、一波不含,逐 run 打印核对(§宽扫)。
- **tpdead 的触发条件收窄**:tpdead 只在「被 stamp 的救援友军 `not IsAlive()`」时
  释放地板(`jmz_func.lua:7184`)。所以只有「归属救援 + 友军在 tc..tc+12 内死」
  的 episode 才可能触发它 → 20 次(ON 17,OFF 3)。

## 2. 逐帧核验(先逐帧)

判据:tpdead 释放地板后,响应者不再被 DEFEND 钉向 `tpRespondLoc`(= 友军施法时
位置)。**关键的正交控制**:只在「门本会继续 hold」的帧上判(即 respLoc 1600 内仍有敌人)——
否则地板自带的「respLoc 无敌人→return nil」子句也会释放,tpdead 不可归因。

**ON 帧 1 — `spot_20260820_041134…/20260820_042009_slot1` t=508.3(radiant=armed)**
pos3 核心 **HP 1.00** 开救援,友军 512.3 死(4038u 外)。承诺窗到 520.3。死后每一帧
respLoc 1600 内**都有 1 敌(PIN-HOLDS)**,但响应者到 respLoc 的距离
4267→5589 **单调远离**。满血英雄在门本会 hold 的情况下反向离开 = 地板被释放。
非低血撤退(满血)、非敌人消失(敌在)⇒ **只剩 tpdead**。

**ON 帧 2 — `spot_20260820_101138…/20260820_103128_slot1` t=281.2(dire=armed)**
pos4 **HP 0.96**,友军 284.7 死。死后 respLoc 有 2 敌(PIN-HOLDS)直到 292.5,响应者
到 respLoc 距离 3079→5537 单调远离,且 **291.5 又开了一发 TP**(tp_cd 0→30.1)——
彻底脱离。最干净的一帧。

**ON 帧 3(诚实反例) — `spot_20260820_041137…/20260820_042607_slot1` t=222.7**
pos4 **HP 0.66**,友军 226.7 死。响应者死后反而 TOWARD respLoc(898→118)约 5s 才离开。
tpdead 应在 226.7 释放,但**释放 ≠ 强制撤退**:normal-mode 见 respLoc 尚有 1 敌 + 自身
0.66 血,选择继续交战。这是**释放静默**的合法帧,如实标注为不可归因,不作反证。

**OFF 帧 — `spot_20260819_121056…/20260819_123012_slot1` t=568.4(dire=armed,#37 立案帧)**
tpdead **未 armed**。DP(p3)为 DK 开救援,DK 573.1 死(响应者 574.4 才落地)。之后
**整窗**(574→580)响应者在 respLoc 5900u 外**站桩不动**(disp +42),respLoc 内敌人
2→3 一直在(PIN-HOLDS)。这正是 #37 记载的「把健康英雄钉在离战场 5.9k 的空地上 12 秒」——
tpdead 缺席时地板照钉。

## 3. 聚合(后聚合,仅佐证方向,`tpdead_release.py`)

settled 窗口(已落地帧,过滤 1Hz 落地跳变):
| 组 | n | 中位 settled_disp | moved_AWAY(>400u) |
|---|---|---|---|
| **ON(tpdead armed)** | 17 | +99u | **7/17** |
| **OFF(tpdead 未 armed)** | 3 | −58u | **0/3** |

ON 侧远离榜首**全是满血/近满血响应者**(0.93-1.00)在 respLoc 仍有敌时移开 1300-2200u,
既非低血逃窜也非敌人消失;OFF 侧 3 次全部 hold(disp −242..+42)。方向与设计一致。
3 个 ON 的 TOWARD 帧 = §2 帧 3 类的释放静默(release≠强制退),如实登记不作反证。
**resp_died_15s = 0/50** ⇒ 释放无新伤害。

## 4. 对 #37 的重新证实(大样本)

50 次归属救援(vs #37 当时 11 次):**友军在 15s 内死 20 次 = 40%**,落点→友军(施法时)
中位 **2006u**、max 5858u。#37 的核心机制诊断(门算 4s 的仗、派的人要走 8s)在 4-5 倍
样本上稳住。**tpdead 只清扫尸体之后的 12s 钉住,不改「TP 本就不该发」**——#37 rec 1/2
(落点距离门 / 把落点距离喂回生存窗)仍是更高价值的未落地修复。已评论 #37。

## 5. 可测性(与总监 §I.8.3 / #34 §4 一致)

tpdead 触发频次 ≈ **每局 0.3-0.4 次**(20 次窗内死 / ~15 局的 ON 侧摊薄)。对照 #30
经验零点 σ≈30 gpm,**(b) 在 gpm / 死亡率上不可测,verdict 若 null 既非无效也非无害**;
条件 (a) 的答案在这里 = 执行了且方向正确。**(b) 须用行为检测器**:建议
「友军死后响应者 4s 内脱离 respLoc(≥400u)率」,即 `tpdead_release.py` 的 moved_AWAY 指标
(本轮 ON 41% vs OFF 0%,须待镜像隔离波次做净差)。

## 6. 边界(如实标注)

- **非镜像隔离**:三 ON 波 vs 一 OFF 臂是不同种子/draft,不是镜像配对;ON 侧 17 id
  全 armed(含 tpcommit/lf_rescue/tpdying 同族)。机制确定性 + 帧对照支撑归因,但
  「隔离证明」要等一个 tpdead 单臂 bisect(与 §A' 的 TP 族排期约束一致)。
- ON 侧「友军死率 50% vs OFF 19%」**不作发现**:样本量/draft 混淆(ON 局数多),
  只报合并 40%。
- `GetEstimatedDamageToTarget` 不落 replay → nVsMe / WillAllySurvive 门离线不可判,
  50 归属是真实施法集但机会集是超集(#37 §8 既有边界)。
- dumper 1Hz:落地帧滞后一拍,已用「≥1.5s past cast + settle radius」过滤跳变。

## 已检查 run_id / 局

- 含 tpdead:`_221108/221112/221117/221122`(种子867-870)、
  `_041132/041134/041137/041139`(872/875/885/887)、
  `_101138/101140/101143/101145`(888/895/896/906)—— 各 4 局镜像有效(暖场自动跳过)。
- 对照(不含 tpdead):`_121038/121044/121050/121056`(859-862)各 4 局。
- 合计 64 局宽扫;20 次窗内死救援逐帧;暖场局按 script_version 无 `mirror:` 自动作废。

## 新工具(已提交)

`tools/batch_test/behavioral/tpdead_release.py` —— tpdead 条件 (a) 验证器:复用
`tp_attribution.scan` 落点归属 → 收窄到「友军窗内死」→ settled 窗口 respLoc 位移
+ pin-hold(enemy near respLoc)正交门。内置 1Hz 落地跳变过滤、release≠retreat 的
诚实边界 docstring。下一轮 tpdead 单臂 bisect 落地直接复用做净差。

## 下一轮优先

1. **`tpdead` 单臂 bisect 若上机** → `tpdead_release.py` 直接做镜像净差(隔离证明)。
2. **`tpdead` 的 (b)**:moved_AWAY 率行为检测器(不许用 gpm/死亡率,#34 §4)。
3. `cmrguard` 若按 #63+#66 收窄,**必须用施法前帧口径**复跑四条验收 + #66 recall。
4. `l1trade`/`l5combo` 自身 (a)(§G.5,连续**七**轮欠,需能分离出价来源的域定义)。

## Token 用量

```
TOKENS total_in=8,802,775 out=95,633 turns=79
```
