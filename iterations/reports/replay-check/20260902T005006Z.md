# 录像检查组 — W36 首检:`illumove` 条件 (a) 在独立波上**方向复现**,而 W35 的「完全分离」**不复现**

**工作单元**:W36 收官后按章程「下一轮第一件事 (1)」执行 —— 宽扫 arm 串,`illumove` 仍在集
⇒ 把 18:49Z 那张 `starved%` 四格重打一遍(条件 (a) 的复现)。**本轮不碰胜负/经济,不花 AWS 的钱,不改 `bots/`。**

---

## 0. 开工自检(铁律 10)

⚠️ **第一条命令第十二次踩证据纪律 3**(`… | tail -40`),脚本自打:

```
REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.
SELFCHECK_EXIT=2  REFUSED (nothing was checked; this is NOT a pass)
```

**护栏第十二次生效,那次不是通过。**「开工模板内建 `rc.sh`」已交出去五轮,**本轮第六次登记、形状完全相同**。
改重定向后台跑(`nohup … > log 2>&1`),**到收尾仍未跑完(>55 分钟)——第十九次登记「不是章程写的『约 20s』」**(GH #358)。
⇒ **本轮不存在「自检总判决」**;已跑完的腿原样登记:

- stable 锚点 **ok**(`d8ecde5e04a1..136a332fdea4: bots/ game/ byte-identical`,2 anchors);
- **cadence GAP: batch-desk / strategy**(本组无洞);
- `rc_wrapper` 变异台 **2 项 `UNCERTIFIABLE`**(`tests/test_rc_wrapper.py` / `tests/test_selfcheck_lua_leg.py`,
  与前两轮逐字相同 —— **没跑成不是通过**);
- **python trunk-red(working tree)** —— 日志自带诚实边界「main 是否也红这一行并未确立」,**本轮没跑 stash 复核,故不声称**;**GH #364 已在,只记复现**。

**已有 #358 / #364 / #383 / #394,本轮不新开。**

---

## 1. 语料同一性先钉死(不靠信任)

| 项 | 读数 |
|---|---|
| W36 计分 run | `…_9fb8e2`(seed 2838)、`…_3c7e26`(seed 2850) |
| `.dem` 位置 | **`dem21/<run>/`**(32 / 24 个),`sweep_run.sh:52-59` 自动回退 —— 章程 §工具坑那条继续生效 |
| 宽扫 | `arm_string_census.py --declared <54-id 串>` **BARE_EXIT=0**,两目录 **MATCH**,arm sha1 `3143409a` |
| 串校验 | 交给工具的串 md5 **`74273b3ce5f58fcb50de769a700ab074` / 477 字节**,与 `W36_wave.json` 起飞时写下的 `arm_md5`/`arm_bytes` **逐字节相同** |
| `illumove` / `illureal` | **两条都在 54-id 串里** ⇒ 本波确实 armed 它们 |
| 覆盖 | **宽扫 44/44 局**(56 − 12 暖场);**`unparseable=0`** |
| 分层 | 9fb8e2 `radiant:18 / dire:8`;3c7e26 `radiant:10 / dire:8` ⇒ **ab、ba 两层都非空** |

**验证(退出码全部裸读,无管道)**:`session_setup.sh` **BARE_EXIT=0**;`get_dumper.sh` **BARE_EXIT=0**
(cache **HIT** `46fe9c6a2b084f9b`);`sweep_run.sh` ×2 **BARE_EXIT=0**;`arm_string_census.py` **BARE_EXIT=0**。

---

## 2. 逐帧(**先逐帧后聚合**,独立于任何工具)

`9fb8e2/20260901_213402_slot7`,seed 2838,**`side=radiant`**;`skeleton_king` 是 **team 2 = radiant ⇒ armed 腿**。
`modifier_illusion` ADD ×2 在 **t=1108**,两条幻象流 `idx1805` / `idx2574` 同在 **t=1108.8** 出现
(**owner `idx1351` 从 t=−63.2 就在**,即 GH #176 的出生时刻判别子把三条流分得干净)。

1 Hz 手读,`dx` 是各自与自己上一帧的位移(节选,全表 65 帧):

```
   t        A idx1805        dx   |  B idx2574        dx   | ohp
 1108.8  ( -1522,  1029)     -    | ( -1687,  1110)     -   | 1.00
 1109.8  ( -1490,   779)   252    | ( -1800,   845)   288   | 1.00
 1110.8  ( -1231,  1004)   343    | ( -1779,   419)   427   | 1.00
 ...     (两只连续同量级走 11 帧)
 1120.8  (   344,   403)     0    | (   231,   556)   348   | 1.00   ← starved(单帧)
 1121.8  (   344,   403)     0    | (   231,   556)     0   | 1.00   ← neither(两只都停)
 ...
 1138.8  (  2268,  1895)   428    | (  2229,  1950)     0   | 0.89   ← starved,方向反过来
 1139.8  (  2268,  1895)     0    | (  2229,  1950)     0   | 0.90
 ...
 1148.8  (  3246,  2441)    12    | (  2876,  2468)   372   | 0.90   ← starved
 1149.8  (  3246,  2441)     0    | (  3134,  2541)   268   | 0.90   ← starved(连跑 2 帧,本 episode 最长)
 1150.8  (  3627,  2396)   384    | (  3518,  2638)   396   | 0.91
```

**这条 armed 腿现场的形状**:65 个配对帧里,**最长的一段「一只走一只钉死」只有 2 秒**,
而且**方向来回换**(1120.8 是 A 停、1138.8 是 B 停、1148.8 又是 A 停)——
**不是 #378 §1 那种「一只长期独占句柄」**。对照 18:49Z 那轮 baseline 腿的现场
(`e12d0d/…_slot7` necrolyte **整整 12 秒逐帧逐位不变**),是两种不同的形状。

⚠️ **本节只描述一条 armed 腿的现场,不构成腿间断言。** 腿间读数在 §3。

---

## 3. 普查(在逐帧**之后**):四格重打,**方向复现**

工具:**`tools/batch_test/behavioral/illumove_pairs.py`(本轮新建并入仓)**。
18:49Z 那轮的普查是**临时脚本,随容器蒸发**,于是本轮本来要「按报告重建」——
**这次把它写进树里**,以后复现是**重跑而不是重推导**。`--selfcheck` **BARE_EXIT=0 / 9 of 9 / 8456 rows**;主表 **BARE_EXIT=0**。

```
stratum/leg       eps  frames    both%  starved%  neither% longest_starve
ab/armed          238    3607    43.8%     11.9%     44.2%          11 s
ab/baseline        92    1390    27.8%     20.5%     51.7%          14 s
ba/armed           67    1216    41.4%      8.7%     49.9%          14 s
ba/baseline       119    1720    26.0%     30.2%     43.8%          19 s
```

**铁律 4(i-a)**:两个分层的**读数**都登记在上表(不是局数)。
**铁律 4(i-b)**:这是**计数类、侧偏未消除**的量 ⇒ 判据是「两层反号 = 噪声」。
**这里两层同号**:`starved%` armed **11.9% / 8.7%**,baseline **20.5% / 30.2%**;
`both%` 同样同号(armed 43.8/41.4 vs baseline 27.8/26.0)。
**读数跟着 arm 腿走,不跟着物理侧走**(armed 在 ab 里是 radiant、在 ba 里是 dire,两层给同一个方向)。
⛔ **四格之间一次差也没作**。

**排除项都登记**:`unknown dx`(没有同间隔前驱)**523 帧,一律不打分** ——
把缺失 dx 填 0 会**制造** `starved`,即朝着结论的方向偏;`>2 只同时存活` **104 帧**,按 max/min 打分,在此披露。

### 3.1 混杂控制:同一个英雄跨两条腿,**10/10 同向**

比池化百分比硬得多的一张表(两条腿各 ≥30 帧的英雄):

| 英雄 | armed `starved%` / n | baseline `starved%` / n |
|---|---|---|
| drow_ranger | **6.6%** / 1160 | 26.6% / 685 |
| juggernaut | **14.5%** / 1722 | 25.3% / 740 |
| medusa | **5.7%** / 739 | 18.7% / 900 |
| queen_of_pain | **14.1%** / 177 | 38.6% / 132 |
| death_prophet | **21.4%** / 252 | 31.9% / 116 |
| obsidian_destroyer | **8.8%** / 34 | 36.9% / 111 |
| lich | **3.3%** / 30 | 70.4% / 115 |
| tidehunter | **6.7%** / 75 | 20.8% / 125 |
| skywrath_mage | **4.4%** / 45 | 16.3% / 43 |
| phantom_assassin | **0.0%** / 32 | 8.6% / 70 |

**10/10 个英雄 armed `starved%` < baseline。**(`skeleton_king` armed 7.8%/153 vs baseline 40.0%/20,
baseline 腿只有 20 帧,未计入这张 ≥30 帧的表。)

### 3.2 ⭐ 归因:删掉 `illureal` **整个域的上界**,读数纹丝不动

W35 那轮 §DC.3(甲) 的归因是靠「交集帧 **0/435**」买到的。**W36 上交集不再是 0**(见 §4),
所以归因**不能再靠同一句话**,必须**证明删掉重叠后结论还在**。加了 `--exclude-lowhp`
(在**两条腿上**都删掉 owner hp < 0.40 的全部帧,即 `illureal` 域的整个上界)后重跑:

```
ab/armed        11.9% -> 11.8%      ab/baseline     20.5% -> 20.7%
ba/armed         8.7% ->  8.8%      ba/baseline     30.2% -> 30.6%
```

**四格每一格移动 ≤ 0.4 个百分点,方向与大小都不变** ⇒ §3 的读数**可以归给 `illumove` 一条**。
(路径唯一性那一半继承 18:49Z:54 个 armed id 里只有 `illumove`/`illureal` 落在 `minion_lib/illusions.lua` 上。)

### 3.3 ⛔ **W35 的「episode 级完全分离」不复现 —— 这是本轮的更正**

18:49Z 报的是:armed **15/15** 个 episode 最长饿死连跑 ≤3 s、baseline **8/8** ≥4 s,**完全分离**。
W36 的 **516 个 episode**(W35 是 23 个)上,这条**不成立**:

| 格 | n | 最长饿死连跑 ≥4 s 的 episode |
|---|---|---|
| ab/armed | 238 | **19 个**(最大 11 s) |
| ba/armed | 67 | **3 个**(最大 14 s) |
| ab/baseline | 90 | 11 个(最大 14 s) |
| ba/baseline | 115 | 21 个(最大 19 s) |

两条腿的分布**大幅重叠**:armed 腿有 11 s / 10 s / 8 s 的 episode,baseline 腿有大量 0 s 的 episode。
**「15/15 vs 8/8」是 n=23、单粒种子、单套阵容上的小样本形状,不是这条 id 的性质。**
与上一轮「96% → 67%」「464 → 283 → 124 秒/局」同族:**形状是真的,分离度不是。**
⇒ **本轮把可引用的口径收窄成:分层百分比 + 同英雄跨腿 10/10,不再引用「完全分离」。**

---

## 4. `illureal`:W35 的 `DOMAIN-EMPTY` **是语料的性质,不是这条 id 的性质**

18:49Z 那轮:armed 腿全语料只有 **3 帧**满足「主人活着且 HP<40%」(幻象存活帧 1,106)⇒ 判 `DOMAIN-EMPTY`。
**W36 上同一个上界不是 3 也不是 0:**

```
DC.3(甲) 交集上界:配对帧中 owner hp < 0.40 的帧数
   ab/armed       有 owner hp 的帧 3850   其中 hp<0.40: 124
   ba/armed       有 owner hp 的帧 1283   其中 hp<0.40:  28
   ab/baseline    有 owner hp 的帧 1483   其中 hp<0.40:  30
   ba/baseline    有 owner hp 的帧 1840   其中 hp<0.40:  51
```

armed 腿合计 **152 / 5,133 = 3.0%**,W35 是 **3 / 1,106 = 0.27%** —— **约 11 倍**,而语料只大了 4.6 倍。
`撤退` 与 `非强势` 两个合取项仍不可观测 ⇒ **152 是上界不是域**。

⛔ **本轮不给 `illureal` 下新裁定**(既不撤销 `DOMAIN-EMPTY`,也不改判):
买到的只是一句**边界更正** —— 「域为空」这一读数**必须绑定语料**,W35 的 3 帧不能被引用成这条 id 的普遍性质。
已作为追评交回 GH #381。

---

## 5. ⭐ 新工具坑(**22.6 倍的静默污染**,与 GH #176 同族)

**幻象死后,快照流不停** —— 坐标冻在死亡那一格,`hp_pct` 归 0,**一路发到本局最后一帧**。
本轮 44 局全语料实测:

```
幻象(copy)流的帧:  hp>0 (真实存活)      18,496
                    hp<=0(死后冻结)     417,678      ← 22.6 倍
最坏的单条流:20260901_213429_slot8  obsidian_destroyer idx=1602  存活 19 帧 / 死后 1,206 帧
```

**任何按 idx 建幻象帧表而不先过 `hp_pct > 0` 的检测器,会有 95.8% 的「帧」是一只冻死的单位** ——
而冻结坐标**恰好制造 `starved` / `idle` 那个形状**,即**朝着结论的方向**污染。
这与 GH #176 ① ② 是同一族(名字不是实体键、插值 hp 不是生死),但**判别子是第三个**:
**不是名字、不是插值,是「实体流在它死后仍然被发」**。`illumove_pairs.py` 的 `episodes()` 先过 `hp_pct>0`,
所以本轮读数不受影响 —— **但这是设计时就防住的,不是运气**。已写进本章程 §工具坑。

---

## 6. 核验结论行

```
VERIFY id=illumove verdict=WORKING episodes=305
VERIFY id=illureal verdict=INDETERMINATE episodes=0
```

- `illumove` = **WORKING**,证据:两分层同号(`starved%` armed 11.9/8.7 vs baseline 20.5/30.2)+
  同英雄跨腿 **10/10** + 归因对删掉 `illureal` 全域**不敏感**(≤0.4pp)+ §2 的 armed 腿逐帧现场。
  `episodes=305` = armed 腿的 episode 数(238 + 67)。
  ⚠️ **这是条件 (a) 的复现,不是效应量,也不是条件 (b)。**
- `illureal` 仍是 `INDETERMINATE`(§DC.3(乙) 词表里的 `DOMAIN-EMPTY`):本波域上界 152 帧,
  **仍然没有一帧被证明呈现诱饵形状**;本轮**没有为它逐帧核过任何一帧**,所以 `episodes=0` 而不是 152。

---

## 7. 条件 (b) 本波**买不到**(不是没查)

批测台 00:45Z 已经把 W36 的胜负通道判成 **DEGENERATE**(GH #352,`minority side won 2 of 113`,
`share 0.0177 < 0.20`),工具自打拒绝横幅、`winrate 0.464` 被语料限死;经济四量 **n=2 且两粒符号相反**
(`comps_better` 四项全 `1/2`)。⇒ **`illumove` 的条件 (b) 仍然欠着**,而且
**54-id 家族已被 §DH 解散(54 → 52),这一波再没有下一波可以加深**。
下一次同时 armed `illumove` 的波收割后,第一件事仍然是重跑 `illumove_pairs.py`(现在是重跑,不是重建)。

---

## 8. 诚实边界

- **没有一个数字是效应量**;本轮不碰胜负/经济,买的只是条件 (a)。
- **armed 腿 = 54 个 id 全部 armed**,不是隔离实验;归因靠两条论证(路径唯一性 + §3.2 删域不敏感),不靠隔离。
- **44 局、2 粒种子、2 套阵容**;⛔ **不可与 W29–W35 并池**(不同 arm 串家族、不同树)。
- **`starved%` 的三个阈值(100u / 20u / 1 Hz)是本工具自己的操作定义,没有任何 gate 读它们**;
  它们照抄 18:49Z 的手读口径,以便两波可比。
- **两层的 episode 数不等**(ab 238/92、ba 67/119)⇒ 铁律 4(i-d),**不可作差、不可按局数或按 episode 数加权**。
  顺带登记一个**物理侧**形状:radiant 共 357 个 episode、dire 共 159 个 —— **这是符的分布,不是任何 id 的效应**。
- **本轮无变异台**:核心读数直接读 `snapshots` 的坐标与 `hp_pct`,不经过任何被检工具的判据;
  聚合半由 `--selfcheck`(9 项)承担,§2 的现场逐帧逐位可读、不依赖阈值。
- **未改 `bots/` 或 `game/` 任何一行**(新增的只有本组自己的检测器)⇒ **不声称跑绿 Lua 全量**(GH #124)。
- **未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行可抄。**

---

## 9. 欠账变化

- ✅ **章程「下一轮第一件事 (1)」结案**:W36 宽扫 + `starved%` 四格重打完成,
  **且这次工具入了仓**(`illumove_pairs.py`),下轮是重跑不是重建。
- ✅ **`illumove` 条件 (a) 在独立波上复现**(方向 + 同英雄跨腿 10/10)。
- ⛔ **更正**:W35 的「episode 级完全分离」**不复现**,该口径**收回**(§3.3)。
- ⛔ **更正**:`illureal` 的 `DOMAIN-EMPTY` **必须绑定语料**(W36 上界 152 帧,不是 3)(§4)。
- ⛔ `illumove` **条件 (b) 仍欠**,且 54-id 家族已解散(§7)。
- ⛔ 章程「下一轮第一件事 (2)」**§4.3 那张 2×2 表在有 WK 的语料上重打**:本波**有** `skeleton_king`
  (`213402_slot7` 等)—— **本轮未做,顺延一轮**(时间给了四格与归因)。
- ⛔ 「下一轮第一件事 (3)」看 harness/总监接不接 GH #405:**本轮未查,顺延一轮**。
- 继承未动:§5 宽扫表按 ab/ba 重打;`tp_out` 那 311 条;`stayfield2_whynot.py` 待下一个 44-id 波;
  `pullcad_beat.py` 在 W25 剩两 run;`unk` 那一列**第十二轮**;窗口常数只读未量;
  W33 0.748 与 W32 0.401 合并成案;`fieldbuy_silence.py`/`stayfield2_margin.py` 分层;
  「静止在小兵火力里」检测器;W25 只并 2/4 run;W26–W28 与 W25 从未池化;`seed 975` **第三十三轮**;
  `wandlimbo` 因 #293 **第三十一轮**;GH #265 被 #272 阻塞;`blinkflee` 卡 #304/#305;
  WK rank-3 全语料复测;载体侧别提案 GH #389;探针入仓待总监(GH #405 新欠账 b)。

**新欠账**:(a) W36 的 44 局 timeline **随容器回收**(重跑 sweep 实测约 30 分钟 + 约 900 MB 下载,
**别当成现成资产**);(b) §3.3 收回「完全分离」后,**episode 级还有没有一个可辩护的口径**待想清楚
(候选:`starved%` 的 episode 级分布分位数,而不是最大值);(c) §5 那条 22.6 倍污染
**只写进了本组章程** —— 若别组的检测器也按 idx 读幻象,要主动点名。

---

## 10. 下一轮第一件事

**(0) 先读章程「当前状态」最末一条,不要抄过期的交棒行。**
1. ⭐ **§4.3 那张 `will_reincarnate` 2×2 表在有 WK 的语料上重打**(顺延一轮,**别再掉**)——
   W36 语料里 `skeleton_king` 在场,但 timeline 已随容器回收,要重跑 sweep(约 30 分钟)。
2. **看 GH #405 / #390 / #381 / #389 有没有回音**;没接就追评,不要再顺延。
3. **§5 宽扫表按 ab/ba 重打**(零新素材,`all_findings.jsonl` 每条带 `side`)—— 已连续两轮顺延。
4. 下一个同时 armed `illumove` 的波:**重跑 `illumove_pairs.py`**(不是重建),并回答新欠账 (b)。

---

## 11. 已发表

**先 push 后发表**(GH #290):报告与章程先落 `main` **`80b2d3b1`**
(`git push -u origin claude/lucid-pascal-a5sydf` **BARE_EXIT=0**;`git push origin HEAD:main` **BARE_EXIT=0**,
钩子在两次 push 上各跑一遍铁律 6 静态门,均 `GATE_EXIT=0 CLEAN`)。
两份草稿各过一次 `claim_precheck.sh`,**全部 BARE_EXIT=0** / `clean` / `OK to publish: every citation resolves on origin/main`。

- GH **#378** 追评 `issuecomment-5502712287` —— `illumove` 条件 (a) 方向复现(四格 + 同英雄跨腿 10/10 +
  t=1108.8..1172.8 帧证据 + 删域不敏感的归因),**并收回 §10 那轮的「episode 级完全分离」口径**;附 22.6 倍读数陷阱。
- GH **#381** 追评 `issuecomment-5502714441` —— `illureal` 的 `DOMAIN-EMPTY` **必须绑定语料**
  (W35 3/1,106 vs W36 152/5,133),**不撤销也不改判**,建议域按每份语料重新预登记。

**未新开 issue**(铁律 7):自检时长归 #358、python trunk-red 归 #364、Lua trunk-red 归 #394,
**一律只记复现不重开**;§5 那条读数陷阱是本组自己的读法纪律,已写进章程 §工具坑。

---

## 12. Token 用量

```
TOKENS total_in=6,568,892 out=46,663 turns=52
```

(统计时刻为止;此后的收尾回合不计入。)

**铁律 6 静态半(GH #205/#213)**:`bash tools/agent/arm_push_gate.sh` **BARE_EXIT=0**;
`bash tools/agent/luacheck_gate.sh` **BARE_EXIT=0** —— `installed luacheck (apt 包名 lua-check)` /
`luacheck bots game: 0 warnings` / `GATE_EXIT=0 CLEAN`。**未用 `RULE6_BYPASS`。**
动态半(`lua5.1 tests/run_tests.lua`,~100 分钟,GH #124)**未跑,也不声称** —— 本轮未改 `bots/`/`game/` 任何一行。
