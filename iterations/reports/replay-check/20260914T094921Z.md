# replay-check 2026-09-14T09:49Z — 第七十四轮

**一句话**:交棒第 (3) 条交货了 —— §四 那张**一局**的表跑满了一整波的一个 8 局切片,
`80/80` 具身体**无例外**都有分钟级停摆(span 中位数 **547.5 s**);而跑满之后浮出来的
**比停摆本身硬得多的读数**是:**天赋摄取是一个按英雄取值的常数,8/8 局逐局复现** ——
`nevermore` / `obsidian_destroyer` / `vengeful_spirit` **在 8/8 局里终局 0 天赋**
(24/24 具身体),`nevermore` 最高打到 **30 级**仍然 0 天赋;`skeleton_king` 的第一个天赋
迟到 **18–20 级**。**34/80 具身体在英雄 10 级(第一个天赋进队头的那一级)一个点都没花。**

⭐⭐ 并列:**`obsidian_destroyer` 在 8/8 局里都是全场最差**,终局只有 **5–6** 个能力账面点
(最高等级 19–25),`span` 中位数 **1279.5 s ≈ 21 分钟**,**8 局零例外**。

---

## 一、覆盖与切法(先登记,再看数)

- **宽扫 8 局**(达到章程 6 局下限),第 9 局用作**标定**(见 §二)。
  语料仍是 W40 = `dem21/spot_20260902_214528_1_8a5f8f31…_ab6c0d/`;
  连续第**十三**轮无新波次(S3 `soak/` 最新仍 `spot_20260912_0926*` = W67)。
- **⚠️ 与交棒的偏差,明写**:上一轮第 (3) 条写的是「**新波次一到**,把同一段读法跑满整波」。
  本轮没等新波次,而是**在现有这一波里跑满了一个 8 局切片**。理由:十三轮无波,
  「跑满整波」这句话的两个条件里**只有「新」这一个买不到,「满」这个买得到**;
  而这一格的价值(把 n=1 抬成 n=8)**不依赖新**。⛔ 代价如实登记:
  **这 8 局与上一轮那 1 局同属一波、同一棵树(`8a5f8f3`)**,所以本节所有数字是
  **一波之内的复现**,不是跨波复现。
- **本轮读的 8 局与上一轮那 1 局坐标不重叠**(本轮 `2146xx` 批的 slot1–8,
  上一轮是 `220100_slot6`)⇒ ⛔ 不是重跑。
- `DUMP_EXIT=0` **8/8**(外加标定局 1/1),unparseable **0/9**。

**切法(铁律 4(iii),引本报告任何数必须连这段一起引)**:

| 量 | 定义 |
|---|---|
| body | `(英雄名, 该名字第一次出现的 idx)`。按名字取会把幻象/分身并进来,**方向朝着把停摆读长**。 |
| spend | 能力账本上涨的那一帧。基线沿用 `levelup_build_meter.py`:能力取开局前等级(天生技能已是 1 级,**不算点**)、天赋取 **0**、神杖/魔晶后发放取到手时等级。 |
| `span` | **§四 那一列的真身**:上一次 spend 之后的**第一个活帧** → 下一次 spend 之前的**最后一个活帧**。`hp_pct>0` **只裁两端,不打断中间**;中途死掉的那段秒数**照算**。 |
| `strict` | 保守孪生:**死亡打断窗口**。恒 ≤ `span`,二者之差就是尸体时间。 |
| `points` | 能力**等级账面**之和,**不是花掉的点数**(见 §二 LIMIT 2)。 |
| `talents` | 终局账本里等级 ≥1 的天赋数。 |

工具落地为 `tools/batch_test/behavioral/skillstall_wave_meter.py`(切法与三条 LIMIT 逐字写在模块 docstring 里)。

```
VERIFY id=skillstall verdict=NOT-ARMED episodes=80
```
(`episodes=80` = **8 局 × 10 具 pre-horn 身体**。⚠️ 与上一轮 `episodes=10`(一局的活体英雄数)、
再上一轮 `episodes=6`(干净形墙条数)**都不是同一个量,别混引**。`skillstall` 这个 id 在 W40 的
树里根本不存在,所以 NOT-ARMED 不等于「armed 了没触发」。)

## 二、⭐ 先标定尺子,再用它(9/10 逐位复现,第 10 行差 24 s 未解释)

上一轮 §四 那张表**没有随附脚本**,所以本轮第一件事是**在它读过的那一局上把尺子校准**
(`ab6c0d/20260902_220100_slot6`,为标定单独下了 1 个 `.dem`)。

- ⭐ **`span` 复现 10 行里的 9 行,逐位相同**:vengeful `665.0` / nevermore `613.0` /
  death_prophet `302.0` / crystal_maiden `742.0` / witch_doctor `801.0` /
  skeleton_king `460.0` / skywrath `619.0` / bristleback `433.0` / shadow_shaman `615.0`。
- ⚠️ **第 10 行不复现**:obsidian_destroyer 本轮读 **1163.0**,上一轮 **1139.0**,
  差 **24 s(2.1%)**,**没解释**。⛔ 登记为残差,不动结论(OD 两种读法下都是全场最长)。
- ⭐⭐ **标定顺手改正上一轮一句措辞**:上一轮 §四 的散文写「只统计 `hp_pct>0` 的活帧」,
  读起来像「尸体时间被排除」。**逐帧看,不是** —— 活帧过滤**只裁窗口两端**,
  中途的死亡既不打断窗口、其秒数也照算(否则 9 行复现不了,`strict` 那一列就是真排除掉的样子:
  OD `1163.0 → 381.0`)。⇒ 本报告两列都给。
- ⛔ **上一轮的 `终局技能点` 那一列复现不了**,10 行里 **5 行**对不上
  (vengeful 15 vs 16、witch_doctor 14 vs 15、skeleton_king 15 vs 16、
  bristleback 15 vs 17、shadow_shaman 15 vs 17),而且**试过的规则都不是它**:
  +天赋、+后发放(神杖/魔晶)、`levelup_build_meter.py` 的合帧 `n_abil_pts`
  (它读 bristleback 15 / nevermore 15 / OD 5)**都对不上**。
  ⇒ **两份 points 列不可并池**,引哪份就说哪份。
- ⛔ **另一处更正(上一轮自己的散文 vs 自己的表)**:上一轮正文写「四个英雄终局 0 天赋」,
  但它**同一节的表里只有三行是 0**(OD / vengeful / nevermore),本轮在同一局上复读也是 **3**。
  按表不按散文。

## 三、⭐⭐ 主结果:天赋摄取是**按英雄取值的常数**,不是随机停摆

8 局 × 10 英雄,每格 8 个样本:

| 英雄 | 终局天赋(8 局) | 账面点 | 最高等级 | `span` 中位数 |
|---|---|---|---|---|
| obsidian_destroyer | **0 ×8** | 5–6 | 19–25 | **1279.5** |
| vengeful_spirit | **0 ×8** | 14–15 | 23–25 | 685.5 |
| nevermore | **0 ×8** | 23(账面,实花 15) | 24–**30** | 556.0 |
| shadow_shaman | 1 ×8 | 12–14 | 20–26 | 609.5 |
| witch_doctor | 1 ×8 | 13–15 | 20–26 | 581.5 |
| crystal_maiden | 1 ×8 | 13–15 | 21–27 | 544.5 |
| skywrath_mage | 1 ×8 | 12–14 | 19–26 | 517.5 |
| skeleton_king | 1 ×8 | 13–15 | 20–28 | 303.0 |
| bristleback | 1 ×7, 2 ×1 | 12–14 | 19–27 | 530.0 |
| death_prophet | 2 ×8 | 14–15 | 23–28 | 344.0 |

**除 bristleback 外,天赋数在 8 局里逐局相同**(零方差)。这不是「有时候忘了点」,
**是按英雄取值的确定行为**。

**帧证据(`20260902_214620_slot1`,nevermore idx=1352,15 个 spend 帧全列)**:

```
#1  t=-59.9  herolvl=1   shadowraze1/2/3 -> 1
#9  t=376.1  herolvl=9   requiem1
#10 t=504.1  herolvl=11  frenzy1        <- 10 级那一级一个点都没花
#15 t=1333.1 herolvl=24  requiem3        <- 之后到终局(27 级)再无 spend
```

同一局 `vengeful_spirit` idx=1243 逐字同形:`#9 herolvl=9` → `#10 herolvl=11`,
`#13 herolvl=15`(第二个天赋档位)也是能力。

**波级复核(80 具身体逐具)**:

- **英雄 10 级那一级零消费**:`nevermore` **0/8 局**有花点、`obsidian_destroyer` **0/8**、
  `vengeful_spirit` **0/8**、`skeleton_king` **0/8**;**合计 34/80 具身体在 10 级一点没花**。
- **第一个天赋到手的英雄等级**:bristleback / death_prophet / skywrath / witch_doctor = **10 级**;
  crystal_maiden / shadow_shaman = 10 或 11 级;**`skeleton_king` = 18 或 20 级(迟到 8–10 级)**;
  nevermore / OD / vengeful = **从来没有**。
- ⇒ 「10 级那一级不花点」与「这一局拿不到天赋 / 天赋大幅迟到」**在 8 局里同进同出**,
  且**第一个天赋进队头的等级恰好就是 10**(`aba_skill.lua:217` 的槽位规则,上一轮已逐字转写)。
  这与 GH #799 验收 3 那条 fixture 钉住的形状(**队头没被训练就离开、后面那项被消费**)
  是同一族,**本轮把它从 1 局 1 英雄抬到 8 局 4 英雄**。

⛔ **不点名分支**:上一轮已证明 `:395 generic_hidden` 与 `:402 still-try-it` 两条都还活着,
本轮没有新的判别证据,**不替它们选**。

## 四、停摆本身(§四 跑满 8 局)

```
== wave: 8 games, 80 bodies
span_s    min 257.0  median 547.5  max 1398.0
strict_s  min 141.0  median 336.5  max 834.0
  span >= 300 s : 74/80 (92.5%)    strict >= 300 s : 50/80 (62.5%)
  span >= 600 s : 28/80 (35.0%)    strict >= 600 s :  8/80 (10.0%)
  span >= 900 s :  8/80 (10.0%)    strict >= 900 s :  0/80 (0.0%)
  0 talents at end : 24/80 (30.0%)
```

- **80/80 具身体都有 ≥257 s 的停摆**,零例外 —— 上一轮「这局十个英雄全部停摆」在
  **另外 8 局里逐局复现**。
- ⚠️ 按铁律 4(ii)(整数/小值域计数不报中位数):`talents` 那一列**报的是分布不是中位数**
  (§三 的表逐格给了 8 局取值)。`span`/`strict` 是连续量,中位数随分位表一起给。
- ⚠️ **`span >= 900 s` 的 8 具,8 具全是 obsidian_destroyer**(8/8 局)。
  这条尾巴**不是尾巴,是一个英雄**。
- ⛔ **LIMIT(逐字重复模块 docstring)**:`span` 是**闲置的下界,不是意图的证据** ——
  一个手上没有未花点的英雄同样会读出长 `span`。**必须与 `points`/最高等级并读**:
  它咬人的地方在于**跨过这段停摆等级还在涨**(点存在且被攥着)。OD 是最干净的那一例:
  终局 19–25 级、账面 5–6 点。

## 五、⛔ 侧别与分层(铁律 4(i-a))

本节的量**不是 armed/baseline 对比**,是**同一棵树上的全员普查**:每局 10 具身体
= 两队各 5 人**由构造包含两侧**,`skillstall` 在 W40 不存在(§一)。
⇒ 4(i-b)/(i-c) 的反号判据在这里无对象;**(i-a) 的披露义务照做**:
本轮没有 ab/ba 分层读数可登记,因为**没有腿**。
⛔ 谁要把这些数拿去和某个 armed 波次比,**必须自己重新分层**,不能直接减。

## 六、issue

- **净增 1**:开 `[bug]` issue 一条(波级证据 + 帧证据 + 建议验收),正文见下轮报告回填的编号。
  理由:GH #799 已被关成 `completed`(本组连续第四轮点名它两条评论逐字写着别关),
  ⛔ **本组不碰 issue 状态、不 reopen**,所以新证据落在**新 issue** 上,并在正文里指回 #799。
- ⛔ 本轮**没碰任何 issue 的状态**。

## 七、AWS / 代码边界

- `AWS_SETUP_EXIT=0`,`arn:aws:iam::581436534924:user/dota2bot-agent`。
  **只读 S3(9 个 `.dem`,约 200 MiB),零 EC2、零 Cost-Explorer 请求、零支出。**
- ⛔ **`bots/` 与 `game/` 一行未改,零新 soak id。**
  本轮改动仅:`tools/batch_test/behavioral/skillstall_wave_meter.py`(新)、
  本报告、章程「当前状态」节。

## 八、下一轮第一件事

1. ⛔ **别重做本轮任何一项**;尺子已进仓(`skillstall_wave_meter.py`),切法与三条 LIMIT 在 docstring 里。
2. ⭐⭐ **最便宜的下一格是把 §三 钉成 fixture**:帧已选好 ——
   `20260902_214620_slot1` nevermore idx=1352,`t=376.1`(herolvl 9,spend #9)与
   `t=504.1`(herolvl 11,spend #10)。两帧之间**英雄等级 9/10/11 逐帧读到、点在手、0 个死帧**
   (⛔ 「天赋合法」是 Dota 档位规则,**不是读数** —— `GetHeroLevelRequiredToUpgrade()` 离线买不到),
   钉的是「10 级那一级零消费」。⚠️ 新 fixture **必须落进子目录**
   (`tests/fixtures/skillstall/` 已是先例),且**递归 glob 的 census(`slotdust`)照样会抓到**,
   按闸的话登记 vocabulary 与 `nMissing`(上一轮教训)。
3. ⚠️ **OD 的 24 s 残差**(§二)没解释;**便宜**,重跑标定局时顺手查一次窗口边界即可,
   ⛔ 但**不值得为它单独下 `.dem`**。
4. ⚠️ 09-13T16:30Z 的 **n=1 复现连续第六轮挂账**,只需一局,等新波次。
5. ⛔ 引本报告任何数**必须连 §一 的切法表一起引**;两份 points 列**不可并池**(§二)。

## 九、开工自检(真码)

⛔ **管道门第 30 次,又是第一条命令** —— `SELFCHECK_EXIT=2 REFUSED (nothing was checked;
this is NOT a pass)`,脚本自己拒绝(stdout 是管道,读到的是 `tail` 的码)。重定向重跑取真码。
真码与 finding 见【收工回填】(自检本轮跑了 40 分钟以上,与分析并行)。

已读到的两条,先登记:
- `TRUNK RED -- a python test is failing ON THE WORKING TREE`,横幅逐字写着
  「Whether main is red too is NOT established by this line」⇒ ⛔ **不读成 main 红**。
  ⚠️ 本轮对 `bots/` 零写入,但**并行确实存在**(自检跑着的同时下 9 个 `.dem` + 9 次 dump)
  ⇒ **连续第四轮**记同一句:更稳妥是等自检跑完。
- `LUA GATE COVERAGE … UNCOVERED 115/455 (25%)`,`NEW UNCOVERED no_manifest_row`
  两条(`test_dusttower_dive_guard.lua` / `test_fieldsip_transfer_receiving_site.lua`)
  —— **GH #806 那笔欠条,不是本组的活、非本轮引入**,只登记。
- `test_selfcheck_lua_leg` 的 **5a\* 九条 UNCERTIFIABLE**(120s 超时)⇒ **那一侧这轮没人看过**。

## 十、铁律 6 三条腿

见【收工回填】(裸读,逐字抄)。

## 十一、token

见【收工回填】。

---

## 【收工回填】push 落地读数、issue 编号、闸的三条腿

**落地**:`origin/main` `249f9000..66f23ea0`,`PUSH_MAIN_EXIT=0`;
session 分支 `claude/lucid-pascal-rau2iv` 同点,`PUSH_BRANCH_EXIT=0`。
⛔ 全程**未用 `RULE6_BYPASS`**,**零 rebase、零 403**。

**铁律 6 三条腿(裸读,两次 push 逐字相同)**:
```
luacheck bots game: 0 warnings
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 86 ran, 0 findings, 0 uncertifiable, 11.3s
lua gate: SKIPPED BY SCOPE -- this push touches no bots/game/tests path.
```
⚠️ **`SKIPPED BY SCOPE` 是范围判定不是通过**(本轮只动 `tools/` 与 `iterations/`)。
动态半(GH #124,~100 min)**未跑、不声称**。

**issue**:`GH #817` —— `[bug] 天赋摄取是按英雄取值的常数:3 个英雄 8/8 局终局 0 天赋,
34/80 具身体在英雄 10 级零消费(波级,带帧证据)`,**发在两次 push 之后**(GH #290 顺序)。
`PRECHECK_EXIT=0`(`paths cited 5 / resolved on trunk 5 / refused 0`)。
⛔ 本组**没碰任何 issue 的状态**,**没有 reopen #799**。

⭐ **precheck 当场抓到一条,值得记**:草稿原文把批测那棵树的 commit 当锚点引了,
`claim_precheck.sh` 判 **OFF-TRUNK**(`PRECHECK_EXIT=3`,`resolves on origin/claude/admiring-hawking-0xutf6`)
—— 那串 hash **不在 `origin/main` 上**,读者查不到。改成「run 名里带的那串 commit」并**把这件事写进正文**后
`PRECHECK_EXIT=0`。这正是 #290 立那道门要挡的东西。

⭐ **发布前又砍掉两句买不到的话**(证据纪律 4):
草稿原写「天赋已合法(`GetHeroLevelRequiredToUpgrade()=10`)」—— **那个句柄本轮一次都没读过**
(dumper 看不见未点的天赋,上一轮已立),改成「Dota 的档位规则,不是本轮的读数」;
「没有死亡」则**补了一次真读**:窗口 `t∈[376.1, 504.1]` 内 **0 个死帧**,
且英雄等级 **9/10/11 逐帧出现**(10 级是读到的不是插值)。

## 【收工回填】开工自检(真码,⛔ 更正本节的前一版)

⛔ **更正**:本节先前写「没取到真码,自检 70 分钟未打横幅」。**它随后跑完了**,真码在手,
那句话作废 —— 这里登记的是**跑完的读数**。(⚠️ 它总共跑了约 **80 分钟**,与分析全程并行。)

```
EXIT=3                      # 真码;第一跑踩管道门第 30 次:SELFCHECK_EXIT=2 REFUSED,没跑成不是通过
legs run              : 13
FINDINGS (exit 3)     : cadence queue-rulings owed-executions lua-coverage trunk-red(python)
UNCERTIFIABLE (exit 2): none
NOT RUN (inside a leg): tests/test_lua_gate.py tests/test_luacheck_gate_soakswitch.py tests/test_selfcheck_lua_leg.py
selfcheck worst exit: 3
```

逐条归属(⛔ **一条都不是本组的活,也没有一条是本轮引入的**,只登记):
- **`trunk-red(python)`**:`tests/test_pending_rulings.py` 红,失败行逐字
  `FAIL: real owed row 'hero27_dem21_deadline_scan' is missing trigger`(817 checks, 1 failed)
  —— 这是**欠条表少一个 trigger**,指向总监那张表,**不是录像组的**。
  横幅自己写着「Whether main is red too is NOT established by this line: re-run after `git stash`」
  ⇒ ⛔ **不读成 main 红**。⚠️ 本轮对 `bots/` 零写入,但**并行确实存在**
  (自检跑着的同时下 9 个 `.dem` + 9 次 dump)⇒ **连续第四轮**记同一句:更稳妥是等自检跑完再开工。
- **`lua-coverage`**:`LUA GATE COVERAGE  disk 455 | push gate 323 | 开工自检 leg 92 | known_red 8`,
  `UNCOVERED 115/455 (25%)`,`NEW UNCOVERED no_manifest_row` **两条**
  (`test_dusttower_dive_guard.lua` / `test_fieldsip_transfer_receiving_site.lua`)
  —— **GH #806 那笔欠条**。
- **`cadence` / `queue-rulings` / `owed-executions`**:沿用既有归属,非本组。
- **Lua 检测器腿**:`92 tagged detector file(s), 0 failures` —— ⚠️ 它自称
  **`FAST SUBSET, not the full suite`**,⛔ **不等于「Lua 侧是绿的」**。
- ⚠️ **`test_selfcheck_lua_leg` 的 5a\* 九条 UNCERTIFIABLE**(120s 预算,实测
  `92 file(s) ... in 120.1s`)⇒ **自检自己那条腿这轮没人看过**;
  脚本逐字:`this is NOT a pass`。

⭐ **另一条教训,连续第二轮**:收尾时我先按「70 分钟没横幅」判它没跑完并写进了报告 ——
**认横幅不认进程计数是对的,但「还没打横幅」也不是「不会打」**。⛔ 结论应当等到
**进程真的退出**(任务通知 + 真码),不是等到我失去耐心。

## 【收工回填】token

`TOKENS total_in=8,593,742 out=65,033 turns=66`(到统计时刻为止;统计后的收尾回合不计入。
本轮**零 `requires approval`**,零权限坐等)。
