# replay-check 2026-09-12T12:54Z — W69 首检:`bbfight` / `bbshort` 的 (a),外加一枚把 GH #208 的两种读法分开的真实买活

**一句话**:W69(27-id,224 局,四粒)全新语料首检。买了 `bbfight` 的**前提**
(它的「结构性零」第一次是**量出来的**不是断言的:30 个域内死亡,跨度 70.8–76.3s,
上界 ≤77.0s,**80s 阈值一次都没被碰到**),买了 `bbshort` 的**域**(开出的 [45,60) 带
66 次死亡,armed 腿上 **5 次**走到了终端 rung 的几何全满足、**0 次买活**)。
⭐⭐⭐ **头号产出不是这两条,是顺带捡到的一枚 n=1 但判别力满格的帧**:
本波语料里**唯一**的一次买活(`0128b9/20260912_100236_slot1` ogre_magi L22,
死 1492.5 → 复活 1512.5)发生在 e≈18–20s,而那一级的复活时长在本语料里是 60.5–68.1s
⇒ **按 GH #208 登记的「getter 已经是剩余时间」读法,那一帧的 `nRemainingRespawnTime`
是 20.5–32.1,`< 40` 那道门必然 return,这次买活在结构上不可能发生**;
按另一种读法是 40.5–50.1,门开。**两个区间不重叠。**
⇒ **这枚帧只与「getter 返回完整时长」相容**,也就是说**出厂那行 `R − e` 是对的,
而 `bbrespawn` armed(返回不衰减的裸 getter)会是回归**。
⛔ **n=1,登记为强单例证据 + 建议的钉帧,不是裁定。**

⭐ 顺带把 GH #208 §7 点名交给本组的那根棒还了(「若语料里出现过围高地帧,请顺手报一声」):
**出现了**,而且 §4 登记的依赖链前提在本语料上不成立(见 §5)。

**段位声明(GH #424)**:单独一波 **W69**(27-id,`family_depth_before` 0 → 4),
**未与 W68(29-id)/ W66+W67(30-id)并池**。

---

## 〇、覆盖(章程口径)

- **宽扫 12/12 局**,四个 run 各 3 局带 `mirror:` 戳(各 4 份 `.dem`,各 1 份暖场被
  `sweep_run.sh` 自动跳过),**unparseable 0**,四个 `sweep_complete.json` 全部写出
  (`dem_found 4 / swept 3 / skipped 1 / unparseable 0 / exit_code 0` ×4)。
  ⭐ 与上一轮不同,**本轮是 `sweep_run.sh` 自己的口径**,不是自建探针。
- **深查(逐帧)7 局**:`006854/094015`、`006854/095057`、`006854/100140`、
  `0128b9/094042`、`0128b9/100236`、`b920b0/095331`、`b920b0/100514`
  (达章程 ≥6 下限)。另 `896712/095226` 在 §5 的围高地普查里逐帧读过。
- **语料**:659 个死亡跨度 / 588 个干净复活 / 41 个原地复活(WK 38 + aegis 3)/
  30 个被截断(丢弃,不 clamp)。
- ⚠️ **`sweep_run.sh` 收的是 S3 前缀不是本地目录**:本轮第一次把本地目录喂进去,
  它照跑、照打 `0 games`、**exit 0**(章程「空字符串当目录」那条坑的**同族变体**,
  见 §7)。

```
VERIFY id=bbfight verdict=INDETERMINATE episodes=30
VERIFY id=bbshort verdict=INDETERMINATE episodes=5
```
(`episodes` 口径:`bbfight` = **进入域**(level>24)的死亡次数;`bbshort` = **armed 腿上
走到终端 rung 几何全满足**的次数。两个都**不是** release 次数 —— release 全语料 **1** 次,
且 §4 证明它**不归这两个 id**。)

---

## 一、量具与纪律

- 量具是**既有的** `tools/batch_test/behavioral/bbfloor_domain.py`(W58 那条坑:
  **动手前**跑了 `ls tools/batch_test/behavioral/ | grep -i -E "^bb|buyback"`,
  唯一命中就是它)。`--selfcheck` **57 PASS / 0 FAIL,exit 0**。
- 本轮新写的只有一个**判别用探针**,**全程落 scratchpad,零新增仓库文件**
  (`bb_ancient_rung.py`:读 `bbfloor_domain.py --dump-rows` 的行 + 时间线,
  算终端 rung 的几何)。
- liveness 一律走 `roam_conversion.load_game()` / `is_dead()`(章程 W66 坑:
  **不许 `g['dead'].get(raw_name)`**),因此幻象锁 idx(GH #176)、赛后冻结尾巴
  也一并由它处理。
- 探针的两处**故意放宽**,方向都朝「超集」:(i) 丢掉 `X.GetNumEnemyNearby` 的
  `time_since_seen < 1.0` 视野项(dump 无视野,GH #27);(ii) 窗口同时给**宽**
  (`e ≤ R−floor`)与**严**(`e ≤ (R−floor)/2`,出厂 `R−2e` 读法)两个读数。
  **超集里的零才是真的零。**

---

## 二、`bbfight`:它的前提第一次是量出来的

消费点 `bots/ability_item_usage_generic.lua:645-657`,门 `J.BuybackFightRespawnFloor()`
(`jmz_func.lua:13792`):armed `80*0.75 = 60`,unarmed `80`。

**域(level > 24 的死亡):30 次,跨 11 局,两腿都有(armed 12 / baseline 18)。**
30 个跨度:**min 70.8 / median 75.8 / max 76.3**;**上界跨度 max 77.00**。
**越过 unarmed 的 80s 阈值:0(上界口径也是 0)。**

⇒ **GH #215 头注那句「turbo 复活天花板 75s ⇒ `remaining > 80` 是结构性零」的结论成立,
而且现在是从 659 个真实复活量出来的,不是从两条引擎事实推出来的。**
⚠️ **但那句话的一个数要改**:头注写「差 5 秒」(80 − 75.0)。实测最保守的上界是 **77.00s**
⇒ **实测余量是 3.0s 不是 5.0s**。76.3 这个观测值本身**不能**用来否证 75.0 ——
量具头注自己写了每个跨度是 ≤1 个采样 + DEATH 事件滞后(p95 0.7s / max 3.9s)的**高估**
⇒ 76.3 与真值 77.0 与真值 75.0 **相容**;能被否证的是 ≥77.0 以上的天花板。
**结论没变,理由的那个数变了**(与本章程「结论对,理由错了一条」同族)。

**执行:30 次域内死亡,0 次买活。** ⛔ **不判 SILENT**:门之上还有
`if not bot:HasBuyback() then return end`(`:614`,金钱/CD),门之下还有
`J.GetTeamFightLocation(bot) ~= nil` —— **两条在 dump 上都不可判**。
armed 腿的 12 次 fight-level 死亡里,**自家遗迹 3000u 内一个活敌人都没有过**
(baseline 腿有 2 次有,见 §3 表),所以连终端 rung 那条替代路径也不在。
**记 BUGGY 就是把「不知道」洗成「有罪」**(与 W68 `arbheart`、W45 `zusult` 同族)。

---

## 三、`bbshort`:域买到了,armed 腿上 5 次走到终端 rung,0 次买活

消费点 `:639-641`,门 `J.BuybackShortRespawnFloor()`(`:13841`):armed `60*0.75 = 45`,
unarmed `60`。它读的是 **`nFullRespawnTime`**,开出的带是 **[45,60)**。

**域:ladder 级(level>15)且跨度落在 [45,60) 的死亡 66 次**
(leg:armed 30 / baseline 36;物理侧:radiant 41 / dire 25)。

那条带开出来以后,**下游只有两级能真的调 `ActionImmediate_Buyback()`**:
(1) `bbfight` rung —— 要 level > 24,而 level>24 的英雄复活时长 ~75s ≥ 60,
**unarmed 也过得了 `bbshort` 的门** ⇒ 这条路**不在 `bbshort` 开出的带里**;
(2) 终端遗迹 rung(`:659-674`)—— 要 `remaining ≥ 40` **且**
`X.GetNumEnemyNearby(ancient) > 0` **且** 那个数 `≥ J.GetNumOfAliveHeroes(false)`。

探针量的就是 (2) 的几何:

| kind | n | 宽窗达成 | 严窗(`R−2e`)达成 | armed 宽/严 | baseline 宽/严 |
|---|---|---|---|---|---|
| band [45,60) | 66 | 10 | 7 | **5 / 4** | 5 / 3 |
| fight (level>24) | 30 | 2 | 2 | **0 / 0** | 2 / 2 |

⭐ **承重帧(armed 腿,`bbshort` 开的门是它走到这里的唯一原因)**:
`006854/20260912_094015_slot1` lion **L20**,armed_side=radiant,自家遗迹 `(-5920,-5352)`,
`t_death=1220.9`,跨度 **57.6s**(level-20 中位 57.1 ⇒ **完整复活,没买活**):

```
t=1226.5  e=5.6s  rem(R-2e)=46.4  enemies@anc=2 [death_prophet 2688, crystal_maiden 2444]  alive_allies=2  RUNG=FIRES
t=1227.5  e=6.6s  rem(R-2e)=44.4  enemies@anc=2 [death_prophet 2623, crystal_maiden 2152]  alive_allies=2  RUNG=FIRES
t=1228.5  e=7.6s  rem(R-2e)=42.4  enemies@anc=2 [crystal_maiden 2050, death_prophet 2623]  alive_allies=2  RUNG=FIRES
t=1229.5  e=8.6s  rem(R-2e)=40.4  enemies@anc=2 [crystal_maiden 2004, death_prophet 2628]  alive_allies=2  RUNG=FIRES
```

**连续 4 个采样帧(≈4 秒,1Hz 采样下是上百个 think tick)整条合取全真**,
**而且是在最严的 `R−2e` 读法下**;unarmed 的 60 floor 会在 `nFullRespawnTime=57.6 < 60`
当场 return ⇒ **这四帧是 `bbshort` 独占的**。**没有买活。**

另一枚(4 个敌人):`b920b0/20260912_095331_slot1` ogre_magi **L19**,`t_death=1191.6`,
跨度 52.9,`t=1197.5` e=5.9s rem=41.1,enemies@anc=3(bristleback 2546 / nevermore 2905 /
crystal_maiden 2730)、alive_allies=3 ⇒ 合取全真,**也没有买活**。

⛔ **判 INDETERMINATE 不判 SILENT**,理由和 §2 同一条:剩下没被排除的合取项恰好**两条,
都不在 dump 里** —— `bot:HasBuyback()`(金钱/CD)与 `time_since_seen < 1.0`(视野,GH #27)。
⭐ **但域已经不是问题了:`bbshort` 的 (a) 现在只差一个字段**(见 §6 新开的 [harness] 单)。

⚠️ 铁律 4(i-a) 两层登记:band 带的 leg 计数 armed 30 / baseline 36,
物理侧 radiant 41 / dire 25;`bbfight` 域 radiant armed 5 / base 13(δ −8)、
dire armed 7 / base 5(δ +2)—— **两层反号**;`bbshort` band radiant armed 24 / base 19(δ +5)、
dire armed 6 / base 17(δ −11)—— **两层反号**。
⇒ **(i-b):这四个都是侧偏未消除的计数量,反号 = 噪声,一个都不写进结论。**
本报告的结论**没有一条**建立在 armed−baseline 的计数差上,全部建立在**结构 + 逐帧**。

---

## 四、⭐⭐⭐ 全语料唯一的一次买活,把 GH #208 的两种读法分开了

`0128b9/20260912_100236_slot1` ogre_magi **L22**,armed_side=dire,team=3(dire),
`t_death=1492.5`,跨度 **20.0s**(level-22 中位 64.3)⇒ **这是买活,不是复活**,
`jumped=True` 且 > `REINCARNATION_MAX_S` ⇒ 不是 WK/aegis 原地复活。
**全语料 659 个跨度里,买活签名只有这一个。**

**它从哪条 rung 出来的 —— 三条独立排除,把上面那条 rung 关死**
(`J.IsAncientBadlyHurt` 那条坐在 `bbshort` 的门**之上**,必须先排掉):
1. `bbancient` **不在 W69 的 27-id 串里** ⇒ unarmed ⇒ 谓词是 `ancient:GetHealth() < 0.8`
   (**绝对血量**),而 dire 遗迹 `t=1509.5` 读 `hp=4500 hp_pct=1`;
2. 即使 armed(`J.GetHP() < 0.8`),`hp_pct = 1.0` 仍然为假;
3. 即使血量项过了,它还要 1500u 内 `> 1` 个敌人 —— 实测 `t=1508.5..1512.5`
   **每一帧都是 0**。

⇒ **只剩终端遗迹 rung**,而它压在 `if nRemainingRespawnTime < 40 then return end` 下面。
逐帧读几何(自家遗迹 `(5528,5000)`):

```
t=1502.5 e=10.0s enemies@anc=1 [skeleton_king 2962] alive_allies=2   -> enemy >= ally 假
...(e=10..17s 恒为 1 vs 2,假)
t=1510.5 e=18.0s enemies@anc=2 [bristleback 2975, skeleton_king 2133] alive_allies=2  -> 真
t=1511.5 e=19.0s enemies@anc=3 [...] alive_allies=2                                   -> 真
t=1512.5 e=20.0s 复活
```

⇒ **买活指令发生在 e ∈ [18, 20]。** 本语料 level-22 的干净跨度(n=27,剔除这一行本身):
**min 60.5 / p10 61.0 / median 64.3 / max 68.1**。把两种读法代进去:

| 读法 | e=18.0 的 `nRemainingRespawnTime` | e=20.0 | `< 40` 那道门 |
|---|---|---|---|
| **A**(GH #208 登记:getter 已是剩余 ⇒ 表达式 = `R − 2e`) | **24.5 .. 32.1** | **20.5 .. 28.1** | **必 return,买活不可能发生** |
| **B**(getter 是完整时长 ⇒ 表达式 = `R − e`) | **42.5 .. 50.1** | **40.5 .. 48.1** | **过,买活可以发生** |

**两个区间在整个 R 取值范围上都不重叠,分界恰好是那个 40。**
⇒ **这枚帧只与读法 B 相容。** 而读法 B 下:
- 出厂那行 `bot:GetRespawnTime() - (DotaTime() - fDeathTime)` **算的是对的剩余时间**;
- `bbrespawn` armed(`return hBot:GetRespawnTime()`)会返回一个**永不衰减的完整时长**
  ⇒ 三个门的窗口**永远不关** ⇒ **arm 它是回归,不是修复**;
- GH #208 §1.1 那张「出厂窗口被砍半」的表,**方向反了**。

⛔ **n=1。** 而且 R 是从同语料同等级分布借的(这一行自己因为买活量不到 R)。
**登记为强单例证据,不是裁定。** 判别子(便宜且结构性)有两条:
(i) **钉帧**:`make_fixture.py` 取这一局 `t=1510.5`,断言 `X.GetRemainingRespawnTime()`
在该帧的返回值落在 [40,50) 还是 [20,32) —— 一次 fixture 就结清 #208;
(ii) 再等一波语料,数第二个买活(本波 224 局只出了 1 个 ⇒ 频率约 **1/659 次死亡**,
要第二例得等好几波)。
⇒ **(i) 是活路,(ii) 不是。**

**顺带把归属钉死**:这次买活 **`leg=armed`,但它不归 `bbshort` 也不归 `bbfight`** ——
L22 的完整复活时长 60.5–68.1s **≥ 60**,`bbshort` 的门 armed/unarmed **都过**;
level 22 **不 > 24**,`bbfight` rung 根本不求值。⇒ **出厂默认终端 rung 的功劳。**
(章程 4a 归属纪律:一个差分不许同时记到几个 id 名下。)

---

## 五、⭐ 还 GH #208 §7 交给本组的那根棒:围高地帧出现了,而 §4 的依赖链前提在本语料上不成立

#208 §7 逐字:「**录像组**:无。唯一一句:**若 GH #108 的 cap=25 语料里出现过「围高地」帧,
请顺手报一声**」。#208 §4 据以主张 `:574` 守遗迹 rung「到不了」的登记是:
「29 份带遗迹的 fixture、5 条时间线、**58 个遗迹快照全部 hp = 1.0**;批测裁判 `forcewin`
在 `SOAK_CAP_MIN` 结束每一局 ⇒ **没有一局录到围高地**」。

**本语料(12 局 / 24 个遗迹)实测**:

- **12 / 24 个遗迹的 `hp_pct` 掉到过 0.8 以下,全语料最低 `hp_pct = 0`**
  (遗迹**真的被推掉了**;与批测台本波 `winner_by {"engine_natural": 224}` 一致 ——
  **这一波不是 forcewin 结束的**);
- **159 帧**有 **>1 个活着的敌方英雄站在某个遗迹 1500u 内**;
- **`bbancient` rung 的完整几何(`hp_pct < 0.8` **且** 1500u 内 >1 敌 **且** 0 友)
  在本语料上成立 1 帧**:
  `896712/20260912_095226_slot1`,**dire 遗迹 `t=1469.5`,`hp_pct = 0.147`,
  1500u 内 5 个敌人、0 个友军**。

⇒ **`bbancient` 的域在 turbo 语料里非空,只是极小(12 局 1 帧)。**
⛔ 本组不主张入集、不主张 promote —— 只是把 #208 §4 里那条「录不到围高地」的
**前提**按棒还回去:**它在 W69 上不成立**,所以建立在它上面的「本轮不提入集」的
理由需要总监重读(**裁定本身可能仍然对,但要换一条理由**)。
⚠️ 同时注意 §4 的**另一半仍然成立且更硬**:unarmed 的 `ancient:GetHealth() < 0.8` 是
**绝对血量**,4500 血的遗迹要跌到 <1 点血才为真 ⇒ **不 arm `bbancient` 的话,
那条 rung 依然是结构性零**,和录不录得到围高地无关。

---

## 六、本轮 issue(先搜后开)

语义检索两次:`dumper 缺买活可用性/金钱字段` **0 命中**;
`bbshort bbfight 买活 floor 执行核验` 命中 **#222 / #215 / #246 / #218(closed)/ #5**。
⇒ **天花板实测那条不新开(#246 已在案)**;净增 **1** 条新单 + **3** 条追评。

- **新开 [harness]**:dumper snapshot 缺买活可用性(或金钱)字段 ⇒ `bbshort` / `bbfight`
  的 (a) 各自只差这一个字段;带 §3 的 lion 承重帧与 §2 的 30 次域内死亡做验收锚。
  **与 GH #662 同族**(`move_speed` / `visible_to_team` 卡 `tpgap`),形状逐字相同:
  **域买到了、几何买到了、卡在一个引擎 getter 没落进 dump**。
- **追评 #208**:§4 的读法判别(**头等**)+ §5 的围高地棒。
- **追评 #215**:天花板实测(30 次域内死亡,余量 3.0s 不是 5.0s)+ 0 次 release。
- **追评 #222**:armed 腿 5 次走到终端 rung 全满足、0 次买活 + 承重帧。

---

## 七、工具坑(本轮新踩 / 复现)

- ⛔ **`sweep_run.sh` 的第一个参数是 S3 前缀,不是本地目录。** 喂本地目录进去它会拿
  `aws s3 ls <本地路径>` 去列(顺带打一行无关的 `ListBuckets AccessDenied`),
  **`found 0 .dem` → `swept=0` → exit 0 → 照写 `sweep_summary.md`**。
  判别子**极便宜**:看 `sweep_complete.json` 的 `dem_found`,不是看退出码。
  **与章程记的「空字符串当目录」是同一个洞的另一个入口**(那次是 `arm_string_census.py`)。
  ⭐ 可迁移的一句:**这一族工具的「跑成了」信号不在退出码里,在它自报的分母里。**
- ⭐ **GH #761 第四次独立复现,又是下载当场**:`20260912_092714_slot1.dem` 在
  `…_896712` 与 `…_0128b9` **两个 run 下同名**。已先建分目录再下载(GH #225),
  `ls … | xargs -n1 basename | sort | uniq -d` 当场打出。**并池前必跑。**
- ⭐ **`roam_conversion.death_spans(tl)` 要两个参数**(`tl, real_idx`),而
  `real_idx` 的正确构造(幻象锁 idx + 赛后尾巴切除)**已经在 `load_game()` 里**。
  自写帧读件**一律走 `load_game()`**,不要自己拼 `frames` 再调 `death_spans`。
- ⚠️ **自检的 Lua 腿本轮又跑了 >20 分钟**(上一轮实测 >45min,章程文档写「约 20s」)。
  本轮判别子按章程用对了:**不是看 `pgrep`,是看它在跑哪个子进程** ——
  `lua5.1 tests/_stayfield2_margin_sweep.lua`(上一轮是 `_midsupfar_sweep.lua`,
  **不是同一个**)⇒ **在跑,不是卡死**。GH #358 已在案,不重开。

---

## 八、诚实边界

- **没有一个数字是效应量。** 本报告**零**结论建立在 armed−baseline 的差分上。
- `bbfight` / `bbshort` 的 (a) **都没买到**,两条都卡在 `bot:HasBuyback()`;
  `bbfight` 还额外卡 `J.GetTeamFightLocation`。**INDETERMINATE 不是 SILENT。**
- §4 是 **n=1**,且 R 借自同语料同等级分布(60.5–68.1),不是这一行自己的 R。
  **相容/不相容的判断对整个区间都成立**,但它仍然是一个例子。
- §5 的 159 帧用的是**位置**不是**视野**(dump 无视野,GH #27),
  `X.GetNumEnemyNearby` 的 `time_since_seen < 1.0` 一律被放宽 ⇒ **所有敌人计数是超集**。
- 跨度是**高估**(≤1 采样 + DEATH 事件滞后 p95 0.7s):对天花板claim 是安全方向,
  对下界 claim 不是,所以本报告**不从跨度主张任何下界**。
- **W69 不与 W68 / W66+W67 并池**;本波所有读数**带段号 W69**。
- `bbrespawn` / `bbancient` **都不在 W69 的 arm 串里** ⇒ 本波两腿跑的都是它们的出厂形态。

---

## 九、下一轮第一件事

1. ⭐⭐ **#208 的钉帧是本组能自己做完的最高价值一件事**:
   `0128b9/20260912_100236_slot1` `t=1510.5` ogre_magi ——
   `make_fixture.py <timeline> --t 1510.5 --hero ogre_magi`,断言
   `X.GetRemainingRespawnTime()` 落 [40,50) 还是 [20,32)。**一次就结清 #208 的读法之争。**
   ⛔ 但要先确认 fixture 能不能供出 `GetRespawnTime()`(#208 §3 自己记着
   「dump 里没有 respawn 字段」)—— **如果供不出,这条路也断,那就并进新开的 [harness] 单。**
2. ⛔ **别重跑本轮四条排除**:`bbancient` 三条独立排除 / `bbfight` 天花板 /
   `bbshort` 下游只有两级 / 那次买活不归这两个 id。
3. ⭐ 新判别子进工具坑:**这一族工具的「跑成了」看分母不看退出码**;
   **自写帧读件走 `load_game()` 不走 `death_spans()`**。
4. 盯新开的 [harness] 单 + #208 / #215 / #222 / #246 / #662 / #770 / #511 / #775。
5. ⭐ **单波读数不是跨波读数**;`bbfight`/`bbshort` 的读数**永远带段号 W69**。
6. ⚠️ W69 已收割且批测台本轮**预算刹车触发、零发波**($90.058 > $90)⇒
   **下一轮大概率没有新语料**,按章程第 2 条转核验记录最少的 id
   (W69 串里 1 条 VERIFY 的还有:`aimguard`(**无专用量具**)、`campfarm`、
   `campvoid`、`creepthink`、`fieldsip`、`fieldbuy`、`stayfield`、`stayfield2`、`pullcad`)。

---

## 十、验证与读数

- **AWS**:只读 S3(1 次 `s3 ls soak/`、4 次逐 run `s3 ls --recursive` 计数、
  `sweep_run.sh` 自己的 12 份 `.dem` + `analysis.json` 下载),
  **零 EC2 / 零 Cost Explorer / 零支出**。`AWS_SETUP_EXIT=0`
  (`arn:aws:iam::581436534924:user/dota2bot-agent`)。
- **树上改动**:仅本报告 + 本章程;`bots/` `game/` `tests/` `tools/` **一行未改**;
  **探针全落 scratchpad**。
- **`bbfloor_domain.py --selfcheck`**:`57 PASS / 0 FAIL`,`SELFCHECK_EXIT=0`(裸读,无管道)。

### 开工自检

`selfcheck worst exit: 3` / `legs run 12` /
`FINDINGS (exit 3): cadence queue-rulings owed-executions trunk-red(python) trunk-red(lua)` /
`UNCERTIFIABLE (exit 2): none` /
`NOT RUN (inside a leg): tests/test_lua_gate.py tests/test_luacheck_gate_soakswitch.py tests/test_selfcheck_lua_leg.py`
—— ⚠️ **腿内那三条这轮没人看过,不是通过。**

trunk 红逐条:python `test_carrier_terms.py`(5/65 checks 红,点的是 `zusult -> zuus` /
`liondrainstop -> lion` 的 hero-scoped 归属)、`test_detector_source_constants.py`、`test_stale_waits.py`;
Lua `test_stayfield2_marginal_domain.lua`(87 个里 1 个,`:591` 负自然出价计数从 56 动了)。
**`bots/` `game/` `tests/` `tools/` 本轮一行未改 ⇒ 四条都不是本轮增量。**
⭐ **与上一轮对照两处变化,都朝好的方向**:上一轮红的 `test_bots_walk_farm_only.py`
**本轮不红了**(有人修掉了);上一轮那条「`test_tpreach_domain.py` 在 py 闸里红过但自检名单里没有」的
**不一致本轮没有复现**(本轮 py 闸两次都是 0 findings,自检的 python 红名单里也没有它)。

⚠️ **自检实测 >20 分钟**(章程文档写「约 20s」,GH #358 已在案,不重开)。
⭐ 第一条命令又撞了管道门(脚本自打 `REFUSED: stdout is a pipe`,**护栏再次生效,那次不是通过**),
改成 `> /tmp/sc.log 2>&1; echo EXIT=$?` 后跑成 —— **`EXIT=3`,裸读**。

### 铁律 6 三条腿(两次 push 读数一致)

```
luacheck bots game: 0 warnings
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 96 ran, 0 findings, 0 uncertifiable, 32.0s   (push main 那次 32.2s)
lua gate: SKIPPED BY SCOPE -- this push touches no bots/game/tests path.
```
⚠️ **末行是范围判定,不是通过**(闸自己也打了这句:`This is a SCOPE decision, not a pass`,GH #624)。
**未用 `RULE6_BYPASS` ⇒ 没有「SKIPPED, not passed」那一行可抄。**
动态半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)**未跑,不声称**。
`arm_push_gate.sh` `ARM_EXIT=0`;`PUSH_BRANCH_EXIT=0`;`PUSH_MAIN_EXIT=0`
(`b4b848bd..b3a61564`,**一次过,无 non-ff,无重推**)。

### 发表顺序(GH #290)

`claim_precheck.sh` 发布前跑:`PRECHECK_EXIT=0` / `local commits not on origin/main: 0` /
`paths cited 4  resolved on trunk 4  refused 0` ⇒ **先 push 后发表**。

### 本轮 issue(回填号)

- **新开 [harness] GH #781** —— dumper 缺买活可用性字段,`bbshort`/`bbfight` 的 (a) 各差这一个字段。
- **追评 GH #208** `issuecomment-5646049865` —— 读法判别(头等)+ 围高地棒。
- **追评 GH #215** `issuecomment-5646052335` —— 天花板实测(余量 3.0s 不是 5.0s)+ 30 域内 0 release。
- **追评 GH #222** `issuecomment-5646055563` —— armed 腿 5 次走到终端 rung + 承重帧。

### token

`TOKENS total_in=11,517,671 out=74,812 turns=80`
(注:这是统计时刻为止的数字,统计后的收尾回合不计入。)
