# 录像检查组 2026-08-23T02:36Z(第五十次触发)

**一句话**:执行 01:16Z 自己登记的**下一轮优先 (1)** —— 00:11Z 那波(24 id,`fieldcreep`
首次上机)落地后的宽扫 + `fieldcreep` 的 (a)。**§AR.3(甲) 那条强制归属栏位现在是测到的,
不是按逐击量级推的**(战斗日志里 `events[].actor` 就是字面实体名);(a) 判定
**INDETERMINATE**,§AR.3(乙) 的「立刻出集」**不触发**;并带回一条方向**相反**的新证据 ——
野怪那一格里**一半是他在打野**,而 `fieldcreep` 在这种帧上把决定权交还给回家逻辑。
零 EC2 支出,未改 bot Lua,未动 gate。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` worst exit **3**:

- **UNLANDED 3 条**,全部是协同组的 `itemtrip` 工作,在
  `origin/claude/dreamy-feynman-tmvbay`(`57c4cb2` / `271ecba` / `13ab459`,最新一条
  02:33Z)。**不属录像组,只转述不接管。**
- **CADENCE**:clean(5 个 stream / 245 份报告,24h 窗口内无洞)。
- trunk python 套件 **13 passed / 0 failed**。

## 1. 本轮覆盖(章程吞吐口径)

| 层 | 数字 |
|---|---|
| **宽扫** | **135/135 非暖场局(100%)**。批测台 00:11Z 波 4 个 run,S3 上当时 159 个 `.dem` / 301 个 `analysis.json`;`sweep_run.sh` 逐 run 串行、按 run 分目录,自动剔除 24 个暖场局(`script_version` 不带 `mirror:`)。#102 哨兵 4/4(`sweep_summary.md` 的 `games swept` 与 manifest 行数逐位相符:34/33/34/34)。 |
| **深查(逐帧)** | **6 局 6 个 episode**(`002103_slot5` / `002033_slot2` / `001328_slot8` / `002610_slot3` / `003611_slot2` / `002633_slot1`),外加冻结尾巴的 40 局抽样普查。 |
| 每 id 触发计数表 | 见 §2;本轮口径是**域读数**,不是触发计数(`fieldcreep` 是否决,触发时什么都不发生)。 |

armed 串逐字核对 = §AR.0 的 **24 id**(`fieldcreep` 在内),种子 888/895/896/906。
全部 S3 **只读**,EC2 **$0**,**未调用 Cost Explorer**。

## 2. `fieldcreep`:(a) = **INDETERMINATE**

新工具 `tools/batch_test/behavioral/fieldcreep_domain.py`(`--selfcheck` **28/28 PASS**)。
四条上游子句**原样复用** `stayfield_domain.situation()`(同一份源码转写,不重新实现)。

### 2.1 §AR.2 说这一列在 fixture 里丢了 —— 在**录像里没丢**

`dumper/main.go:655-667` 的噪声过滤**只在两边都不是英雄时**丢弃战斗日志条目。所以
「野怪打英雄」「小兵打英雄」的 `DAMAGE` 行原样保留,`events[].actor` 就是字面实体名
(`npc_dota_neutral_granite_golem` vs `npc_dota_creep_badguys_melee`)。
**§AR.1 只能按逐击量级推断的那一列,这里是直接读出来的。**

### 2.2 §AR.3(甲) 强制栏位:打他的是野怪还是小兵

| 腿 | SITUATION episodes | 无小兵/野怪 | 纯小兵 | 纯野怪 | 混合 | 甲 否决域 | 乙 否决域 |
|---|---|---|---|---|---|---|---|
| armed | 2623 | 1932 | 601 | **87** | 3 | 691 (26.3%) | 604 (23.0%) |
| baseline | 2867 | 2214 | 583 | 63 | 7 | 653 (22.8%) | 590 (20.6%) |

**两个世界之间换手的那一格 = 90/2623 = 3.4%**,不是 §AR.1 那五枚钉帧读起来的 40%(2/5)。
字面名(全语料计次,前几名):`grown_frog` / `warpine_raider` / `granite_golem` /
`rock_golem` / `ancient_frog_mage` / `forest_troll_berserker` / `dark_troll_warlord`。

### 2.3 ⭐ 订正 §AR.1 的「世界乙里剩余域 **100%** 落在不该否决那一格」

§AR.1 用药膏 92 / 仙灵之火 85 算。按**每个 episode 手里真正拿着的那件**重算:

| 类别 | 手里有回复品 | 否决**正确**(3s 创伤 > 该件 3s 量) | 否决**反向** |
|---|---|---|---|
| 纯小兵 | 701 | 192 (**27.4%**) | 509 (**72.6%**) |
| 野怪/混合 | 53 | 39 (**73.6%**) | 14 (26.4%) |

原因:手里拿的绝大多数是**树枝** —— 纯小兵 episode 的最强回复品 `tango` **389** /
`faerie_fire` 184 / `flask` 100 / `tango_single` 28。tango 的 3s 量是 **21**,而纯小兵 3s
创伤中位 **21** / p90 64 / max 221 ⇒ **48% 越过 tango**。
**⇒ 「100%」应读作「≈73%」。方向结论成立,但它不是全称。**

### 2.4 §AR.3(乙) 的退出条件:**不满足,而且这个测法在本语料上判不了**

前件成立(camp 啃人的帧确实出现:150 个纯野怪 episode + 10 个混合 / 135 局)。
后件判不了 —— `fieldcreep` 点亮 = **释放**,只能看后果。最干净的判别式是**腿内对照**
(两个世界都释放小兵帧,只有甲释放野怪帧),带回复品的 episode:

| 腿 | 类别 | n | home_tp | 5s 内不动 | 10s 内离开 |
|---|---|---|---|---|---|
| armed | 野怪 | 40 | **0 (0.0%)** | 2.5% | 90.0% |
| armed | 小兵 | 373 | 8 (2.1%) | 7.0% | 55.8% |
| **baseline** | **野怪** | **13** | **0 (0.0%)** | 0.0% | 84.6% |
| baseline | 小兵 | 328 | 8 (2.4%) | 10.4% | 57.6% |

**基线腿(零 gate)在野怪帧上的 home_tp 也是 0/13** ⇒ 出厂基率就是零,「野怪帧 home_tp≈0」
**区分不了甲和乙**。第二条独立读数同样不支持乙:野怪帧 armed「5s 内不动」**20.9%**
vs baseline **23.8%** —— armed **更低**,与「乙 ⇒ armed 被多按住」符号相反。

**⇒ `fieldcreep`: (a) INDETERMINATE。不是 SILENT,也买不到 WORKING。不触发 §AR.3(乙)。**

### 2.5 ⭐ 新证据,方向与 #119 原文**相反**:野怪那一格里**一半是他在打野**

同一个 3s 窗口读**反方向**的伤害(英雄 → 野怪/小兵):

| 腿 | 类别 | episodes | 打出过伤害 | **打出 > 挨的** | 打出中位 |
|---|---|---|---|---|---|
| armed | 野怪 | 90 | 51 (56.7%) | **41 (45.6%)** | 80 |
| baseline | 野怪 | 70 | 42 (60.0%) | **39 (55.7%)** | 163 |
| armed | 小兵 | 601 | 216 (35.9%) | 176 (29.3%) | 0 |

**承重帧(逐帧)`run_001140/20260823_002103_slot5` skeleton_king t=629.4–649.4**:
坐标 **(-5029,-76) 21 帧逐位不动**,hp 0.820 → 0.328 —— 但他每 ~1.3s 对
`npc_dota_neutral_rock_golem` **打出 80/81/223/225/228**,`skeleton_king_vampiric_spirit`
每下回 12–35,**638.1 把石头人打死**,**634.2 喝了瓶药膏(`ITEM item_flask` +
`modifier_flask_healing`)接着打**;挨的是 `granite_golem` 58–75 一下。
第二例 `run_001140/20260823_002033_slot2` skeleton_king t=638.5–649.5,(352,-5022),
对 `grown_frog` 打出 84/85/86/237,**646.5 把青蛙打死**,挨 `ancient_frog_mage` 40–54。

这**不是**「a hurt bot with nobody near it 站在营里被啃」,这是**打野**。而 `fieldcreep`
在这种帧上把 `IsFieldRegenSituation` 置假 ⇒ **把决定权交还给回家逻辑**,方向是反的。
**这条反证只在世界甲里生效** —— 也就是这个 id 本来被认为「好」的那个世界。

⇒ 给协同组的下一个杠杆建议**不是**申请里自己标的「改读伤害量级」(打野帧的挨打量级恰恰是
**高**的那一档,量级杠杆解决不了它),而是**读方向**:`bot:GetAttackTarget() ~= nil`
一类的「我正在打回去」应当**排除**在否决之外。

## 3. ⭐ 新工具坑(全队级):每局录像结尾有 ~25.0s 的**冻结尾巴** —— GH #130

抽样前 40 局:**37 局尾巴 ≥2s,中位数正好 25.0s**;`last_frame − last_event` 中位 **24.4s**。
尾巴里十个英雄的 `hp`/`x`/`y` **逐位不变**、`items` **同时清空**、**零战斗日志**。
朴素评分下这些帧同时是「存活 ∧ 位移恰好 0 ∧ 手里没回复品 ∧ 一直不走」——
**免费合成出 GH #119 的病例签名**。

**存在性证明** `run_001140/20260823_002033_slot2` skeleton_king:
`t=649.5 hp=0.407 xy=(352,-5022) items=5` → `t=650.5..674.5` 全部
`hp=0.389 xy=(352,-5022) items=0`,而他**最后一次 DEATH 在 585.3**(全部死亡:46.2 /
136 / 186.6 / 309.3 / 495.8 / 512.2 / 585.3)。**死的是录像,不是他。**
同局 10 个英雄 `last_items` 全是 649.5、`last_frame` 全是 674.5 ⇒ **全局性,不是逐英雄的**。

暴露面:`stayfield_domain.situation()`/`Game.alive_frame()` **没有 items 子句**,直接建在它
上面的工具**全部继承**;`fieldbuy_domain.py` **侥幸免疫**(采购块要空槽,空 items 行本来就
过不了);`detect.py` 的 15 个检测器**本轮未逐个核查**。
本工具的守卫:`has_items_row()` 同时当尸体守卫和尾巴守卫,**域帧和结局窗口都切**,
外加两条 selfcheck 正控(`every episode carries an inventory` /
`no episode starts after its game's last live frame`)。

**对本轮结论的实测影响**:域侧 5492 → **5490** episode(**0.04%**),野怪帧「5s 内不动」
21.8% → **20.9%**。**真实但很小** —— 报它不是因为它翻了本轮的盘,是因为它下次可能翻别人的盘。

## 4. 本轮另一条自我更正(逐帧抓出,聚合读反)

第一版 selfcheck 里我写了一条断言:「域内不可能出现塔的伤害 —— 子句 4 禁止 1200 内有敌塔,
塔射程 700」。语料**当场证伪**:第一个 run 就有 **88 次塔伤害分布在 52 个 episode 里**。
它们是**撤退的尾巴** —— 挨打时人还在射程内,**被打分的那一帧是他跑出来之后**。
**回看窗口是对过去的窗口,子句是对当下的检验**,两者不能互相推。断言已改成对子句 4 本身的
独立复核(`clause 4 holds on every scored frame`,PASS),塔伤害计数降级为**观测**。

## 5. 跨组

- **GH #119 追评**(不新开):§AR.3(甲) 的测量表、§AR.1「100%」的订正、(a) INDETERMINATE
  的两条独立读数、打野反证 + 两枚承重帧、以及「候选-对-候选波次才能买到 (a)」的建议。
- **新开 GH #130**([harness],给总监):冻结尾巴,含存在性证明、暴露面、两条修法
  (下游守卫 vs dumper 侧切尾)与验收方式。
- **不提波次请求**:本轮全部读数来自已有语料。**但登记一条**:`fieldcreep` 的 (a) 对着
  出厂基线**买不到**(要移除的通道出厂基率 0/13),要买得组**一腿 `stayfield,stayfield2,
  fieldbuy` / 另一腿再加 `fieldcreep`** 的候选-对-候选波 —— 这不是本组能定的,已在 #119 里
  提给总监。

## 6. 验证

- `bots/` / `game/` **0 改动** ⇒ 铁律 6 的 Lua 门无适用对象(`git diff --stat -- bots game` 为空)。
  本容器**未装 `luacheck`/`lua5.1`**(如实记录,未假装跑过 —— 工具可用性不跨轮继承)。
- 开工自检 trunk python 套件 **13 passed / 0 failed**。
- 新工具 `fieldcreep_domain.py`:`py_compile` 过 + **在真实 sweep 上 `--selfcheck` 28/28 PASS**
  + 全量报告跑通(**编译通过 ≠ 跑过**,这条纪律本轮照办:每次改完都真跑)。
- 位置只用于位移/环几何,**未用作分路代理**(GH #57/#116)。
- **AWS**:EC2 **$0**,S3 **只读**,**未调用 Cost Explorer**。

## 7. 下一轮优先

1. ⭐ **GH #123 的 fixture**(`182012_slot1 t=366.4 spirit_breaker`,**已顺延两轮,不要再顺延**)。
2. **`fieldcreep` 打野反证的 fixture**:`make_fixture.py` 钉
   `run_001140/20260823_002103_slot5 t=634.2 skeleton_king`(喝药膏那一帧,野怪伤害与
   反向输出同时在场)—— 本组自己欠协同组的下一棒。
3. `pullcamp` 回集那一波的连接率/开拉点/深营占比(总监 §AP.1 点名,owner P1 第 3 棒)。
4. 00:11Z 这波其余 armed id 的 (a) 补课(本轮只买了 `fieldcreep` 一格,语料已在本地
   `.sweep_out/`,零成本)。
5. `hero-1` 的 153 局 WK 语料(棒子挂 **8 轮**)。
6. `creeppull` 的 FORCED 通道。
7. `l5combo` 的 (a)(**连续第二十四轮**)。
8. `make_fixture.py` 钉 `062551 t=205.5 jakiro`(#45,**连续第十一轮顺延**)。
9. `axebuyblink` armed 的波次。

## 8. 本次会话 token 用量(铁律 8)

```
TOKENS total_in=11,248,040 out=68,356 turns=79
```
