# 录像检查组 2026-08-21T09:00Z(第二十九次触发)—— `blinkflee` 的 (a) 早就录在归档里了,不用再开波次买

**一句话**:批测台 08:06Z 无新波次(MTD $18.2767,滞后补账落地),按章程第 2 条转向补课,执行的是我自己上一轮登记的「下一轮优先 (4)」(`axe_blink_domain.py` 的跳跃选择审计,等含 Axe 的语料)。**含 Axe 的语料一直都在** —— `axebuyblink` armed 的 `spot_20260820_0411xx` 四个 run。审计结论是**干净但靠余量**;更值钱的是顺带买到的三件事:**`blinkflee` 的条件 (a) 在这份归档语料上已经可以定案(5 个真实事件、5/5 反事实 HOLD、5/5 事后没死)**,**`axebuyblink` 的 (a) 独立复现为 7/8 vs 0/8**,以及**跳刀在 Turbo 里只占 11.7% 的对局时长、中位到手时刻是全局的 84%** —— 这个供给瓶颈,而不是站位,才是 `axeblink` 域稀疏的主因。

**零 AWS EC2 支出;未改任何 Lua(`.lua` 改动数 = 0);未开新 issue,评论 3 条(#74 / #56 / #69)。**

---

## 0. 覆盖数字(章程固定节)

**宽扫 20/20 局**(4 个 run 的全部 `.dem`,178MB S3 GET,全部重下重跑 dumper `-interval 0.5`;16 局 mirror 有效 + 4 局暖场,**暖场局本轮故意不排除** —— 它们是 gate-OFF 的对照,§3 用到了)。
**深查 11 次跳刀施法全部逐帧还原**(= 本语料真实施法总数,100% 覆盖不抽样)+ **5 次撤退腿的前后 8s 帧序列重建** + **1 次跳刀→TP 串联的完整事件流还原** + **20 局同名多实体普查(7537 个非主实体快照)**。

对象:`spot_20260820_041132/041134/041137/041139_1_7c8b5167…`,种子 872/875/885/887,
armed 串含 `axebuyblink`(18 个 id)。

### 每 id 触发计数 / 核验状态(本轮涉及的)

| id | 状态 | 本轮证据 |
|---|---|---|
| `axebuyblink` | **WORKING**(独立复现,分母更大) | armed 侧 7/8 局持刀,baseline 侧 0/8(Fisher p≈0.0014) |
| `axeblink` | **DOMAIN-NOT-REACHED**(维持) | A 段 0/11;漏斗 167/63/21/0 与英雄组 03:48Z **逐位复现** |
| `blinkflee` | **NOT-YET-EXECUTED**;但 (a) 的**反事实**本轮做完 | 5 次撤退腿,门 5/5 会 HOLD,5/5 事后 15s 内没死 |
| 认证工具 `axe_blink_domain.py` | **审计通过(靠余量,非构造)** | 剔除全部非主实体后漏斗逐位不变;最近一次活着的同名实体离本体 **55u** |

---

## 1. 为什么做这条(接上一轮)

上一轮(06:46Z)登记的「下一轮优先 (4)」是:`axe_blink_domain.py` 的落点选择器有一个未被证伪的残余风险 —— 它用「窗口 `[t-0.5, t+2.0]` 内最大相邻位移」定位跳刀落点,**如果窗口里混进 TP 落地或死亡瞬移,argmax 会选错,承重谓词就在错误的地点上算**。当时的结论是「1609xx/1807xx 那 40 局里 Axe 出现 0 次 ⇒ 本语料无法证伪,登记为『需含 Axe 的语料』」。

**本轮先纠正那个登记的隐含前提**:「需含 Axe 的语料」不是要等新波次 —— 归档里 `axebuyblink` armed 的那一波(0411xx)Axe **四个种子全在阵容里**,而且是唯一一波 Axe 真有刀的。零支出就能做。

---

## 2. 审计结论:**干净,但靠的是余量,不是构造**

新工具 `tools/batch_test/behavioral/axe_jump_pick_audit.py`:对每一次 `item_blink` 施法,列出窗口内**全部**相邻步长(不只是胜出者)、runner-up 的余量、胜出步长是否超过跳刀物理上限(1400,= 施法距离 1200 + 余量)、窗口是否与 `modifier_teleporting` 读条区间(精确取自 `MODIFIER_ADD/REMOVE`,非启发式)或死亡区间相交,以及**全局 >1400 的步长离任何施法窗口最近有多少秒**。

**11 次施法,10 次窗口完全干净,1 次被标记复查,复查后判为「标记正确、选择正确」。**

被标记的那次 `041134/20260820_042539_slot1` **t=548.7**:读条区间 549.1→554.1 与窗口 [548.2, 550.7] 相交,但 **TP 落地的 14728u 步长发生在 554.0→554.5,在窗口结束之后 3.3 秒** ⇒ argmax 选中的 1273u(548.5→549.0)是真跳刀。逐帧见 §3。

**方向性(写进 docstring,免得被引用反)**:污染只会让承重谓词落在泉水/塔边这类没有敌人的地点 ⇒ **只会低估 `A_in_domain`,不会高估**。当前处置 DOMAIN-NOT-REACHED 建立在 A = 0/11 上,所以污染即使发生也不会救回这个处置。

### 2.1 同名多实体(#69 的第二个消费面)—— **本轮真正的风险在这里**

`axe_blink_domain.py:176` 按名字取快照、**没锁 `idx`**。语料本底:**10/20 局存在同名多实体,非主实体快照 7537 个,其中 `hp > 0` 的 889 个**(能穿过所有现有 `hp > 0` 过滤器)。两个必须分开报的群体:

- **真幻象**:成对出现、n 与跨度逐位相同、HP 与本体同阶(axe 1960 / viper 1484 / slardar 1587 / death_prophet 1596);
- **低血壳**:单个、`hp_max` 3–18、几乎全帧 `hp == 0`(shadow_shaman 7 局全属此类)。

**验证方式**:把非主实体(判据 = 最早出现时刻,本体从 −75s 赛前就在)全部剔掉重跑整个漏斗,**每一个数逐位不变**:`A 0/11`、`B 167/63/21/0`、`B3 {1:106, 2:48, 3:13} = 36.5%`、`episodes 25/12/48.0%`、`corpse dropped 132`。⇒ **英雄组 03:48Z 公布、总监已写进 `state.json` 的数不需要更正。**

**但这是余量,不是构造**,三条:
1. 全语料里**活着的**非主实体离本体最近一次 **55u**,发生在有 5 次跳刀的那一局(`041137/20260820_043124_slot1` t=325.9,enemy 侧 shadow_shaman 壳 hp=8)。**55u 深在落点判据的 315 环内**;没中招只因为 Axe 的 5 次施法在 t∈[492,613],离它 166 秒。
2. **幻象带道具**:death_prophet 幻象快照带 5 件,**Axe 幻象最多也带到 5 件** ⇒ `holds` 的 `blink_dagger in items` **不是保护**。(我第一版以为它是,取的是恰好为空的中位帧 —— 自查纠正。)
3. 唯一有 Axe 幻象的那一局(`042023`)Axe 恰好**没买刀** ⇒ 该路径在本语料上**未被测试**。

已评论 **#69**(同根因、同一条「只认名字不认实体」),含具体修法。**未开新 issue**:根因已有 issue,再开是重复。

---

## 3. 顺带买到的第一件:**`blinkflee` 的条件 (a) 可以定案了**

我自己在 **#74**(08-20 20:49Z)里写过:`blinkflee` 的 (a) 在 §U.0 那一波**结构上买不到**(撤退腿 0.063 次/局,armed 侧期望 0.5 次/波),并给了选种指标「载体首次持有 < 500s」。**满足该指标的语料就是本轮这份**:

| | §U.0 那一波 | **本语料(0411xx)** |
|---|---|---|
| 载体 | ES / OD,恒在 dire | **Axe,4/4 种子** |
| 首次持有 | 554–640s | **472–630s**(中位 552s) |
| 真实施法 | 5 次 / 32 局 | **11 次 / 20 局** |
| **撤退腿** | **2 / 32 局 = 0.063 次/局** | **5 / 16 mirror 局 = 0.31 次/局** |
| 撤退腿(只算有刀那侧) | — | **5 / 8 局 = 0.63 次/局** |

### 3.1 五次撤退腿 + `J.ShouldHoldBlinkFlee` 反事实重放

谓词一律在**施法前最后一帧**评估(#66 §0 口径);`WasRecentlyDamagedByAnyHero(2.0)` 用 `DAMAGE/CRITICAL_DAMAGE` + `actor_hero == true` 精确判定。

| # | 局 / t | HP | 最近**活**敌 | 前 2s 英雄伤害 | 后 8s 英雄伤害 | 15s 内死 | 门 |
|---|---|---|---|---|---|---|---|
| 1 | `043120` t=**655.0** | **0.99** | tidehunter 329(.89)、silencer 347(.80) | 无 | 297(7 次) | 否(最低 .844) | **HOLD** |
| 2 | `042539` t=**548.7** | 0.84 | drow_ranger 758(1.00) | 无 | **0** | 否 | **HOLD** |
| 3 | `042539` t=**625.5** | 0.88 | crystal_maiden 908(.79) | 无 | 66(2 次) | 否 | **HOLD** |
| 4 | `043124` t=**529.6** | 0.84 | skeleton_king 339(.63) | 无 | **0** | 否 | **HOLD** |
| 5 | `043124` t=**555.2** | 0.81 | skeleton_king 843(.41),**5 敌里 3 个已死** | 无 | **0** | 否 | **HOLD** |

**5/5 HOLD,5/5 事后 15s 内没死,后 8s HP 最低 0.81**(唯一例外 #1 从 .99 掉到 .844)。
**诚实报出最弱的一个**:#1 两个敌人在 350u 内、随后吃了 297 —— 这是五个里唯一值得辩护的,但他全程没掉到 84% 以下。
**最干净的浪费是 #5**:81% 血、对面只剩 2 个活人、最近的是 41% 血的 SK 在 843u,他朝自家远古跳了 1139。

### 3.2 新机制:**跳刀和 TP 串起来,刀白烧**(#2 帧)

```
548.4 ABILITY  axe -> drow_ranger  axe_battle_hunger   (敌 758u,Axe 84% 血)
548.7 ITEM     axe                 item_blink          ← 撤退腿 (-6402,4822)->(-6352,3550),1273u,cos(→自家远古)=+0.999
549.1 ITEM     axe                 item_tpscroll       ← 0.4 秒后按 TP
549.1 MODIFIER_ADD  axe -> axe     modifier_teleporting
      549.4-553.9 battle_hunger 每 0.5s 对 drow 跳 12 点(人已在地图另一头)
554.1 MODIFIER_REMOVE modifier_teleporting
554.5 快照 (4998,-5821) ← 对面下路一塔,位移 14728u
```

**那 1273u 是 TP 无论如何都要走的路的一部分** ⇒ 15s CD 换零收益。这是 #71 形状之外**额外**的一层:不只是「烧刀走路」,是**烧刀走一段 TP 已经覆盖的路**。
**n=1(5 次撤退腿里只有这 1 次串了 TP),只作机制登记,不作速率主张。**

### 3.3 ⚠️ 排期约束:**`blinkflee` 的可测性挂在 `axebuyblink` 上**

baseline 侧 8 局 Axe **一次都没摸到刀** ⇒ 那 8 局对 `blinkflee` 的贡献**恒为零**。而 #60 已把 `axebuyblink` 移出测试集(采购队列冻结)。两件事合起来:**今后的波次里 Axe 不会有刀,`blinkflee` 的 (a) 会退回「结构上买不到」。**
⇒ 建议(决定权在总监):**`blinkflee` 的 (a) 就用这份归档语料定案,别再排波次买它**;若仍要上机,必须与一个「能把刀送到 Axe 手里」的 id 同臂,否则读到的 0 次不构成任何结论(#34 / #28 / #68 同一陷阱)。

建议钉帧(替换 #74 §1.5 建议 3 的那一对,同目的更强载体;两对一起钉更好 —— 跨两英雄两波次):
- **必须 HOLD**:`spot_20260820_041137_1_…/20260820_043124_slot1` **t=555.2** axe;
- **必须放行**:`spot_20260820_041139_1_…/20260820_042612_slot1` **t=573.2** axe(进攻腿,cos=−0.996,**HP 0.33**,落点距 juggernaut 183u —— 同时压住 HP 子句下边界与分支归属)。

已评论 **#74**。

---

## 4. 顺带买到的第二件:**`axebuyblink` (a) 复现,但持刀只占 11.7% 的对局时长**

| Axe 所在侧 | 局数 | 有刀 | 持刀时长/总时长 |
|---|---|---|---|
| **armed** | 8 | **7** | **624s / 5333s = 11.7%** |
| baseline | 8 | **0** | 0s / 5334s = 0.0% |

7/8 vs 0/8,Fisher 精确 **p ≈ 0.0014**。方向与量级与 #56 的 5/6 vs 0/6 一致。

**诚实边界补充**:4 局暖场(gate 全关)里**有 1 局** Axe 也买到了刀(`041139/20260820_041230_slot1` t=472.2,全语料最早)⇒ **shipped build 不是结构上买不到刀,只是很少**(1/4 暖场 vs 7/8 armed)。写「shipped 从不持刀」会略过头。

**本轮新增的那个数**:中位首次持刀 **552s**,中位「全局已过去的比例」**0.84**;三局的刀在最后 4% 时间到手(0.96/0.96),其中**两局一次都没用出去**。

**排除「局太短」解释**:12 局无刀局中位局长 **667s**,比 8 局有刀局的 **656s** 还**长** ⇒ 买不到刀与局长无关,与 #60 的采购阶梯有关。

**对 `axeblink` 的因果重排(建议英雄组/总监采纳)**:`axeblink` 的域只在持刀时存在,而持刀 = **11.7% 的时长且集中在最后 1/6** ⇒ **任何「持刀才咬合」的门,占空比上界就是 11.7%,与站位无关**。英雄组 03:48Z 把处置从「结构性为空」拉回「活着但稀疏」我逐位复现、结论不动;但**「稀疏」的主因看起来是供给而不是站位**。

已评论 **#56**。

---

## 5. 自查纠正(本轮我自己踩的两个)

1. **「t=555.2 时视野里零敌人」是帧查找伪影,不是事实。** 我第一版用 `t-0.2` 找前帧,而 0.5s 采样下 555.0 / 555.2 两个时刻都不存在快照,回退也没命中 ⇒ 敌人列表为空被我读成「全场没人」。**正确读数是 SK 在 843u、41% 血、5 敌 3 死** —— 改成「取 ≤ 施法时刻的最后一帧」后重算了全部 5 行。**教训:空列表要先分清「查询没命中」和「集合真的为空」**,前者在稀疏采样下比后者常见。
2. **「Axe 幻象不带道具」是取样伪影。** 我看的是中位帧恰好为空,推出「`blink_dagger in items` 结构上保护了 B/B3」。取全部幻象帧的最大值后是 **5 件** ⇒ 该保护不存在。**教训:要断言「某字段恒为空」必须取全序列的极值,不能看一帧。**

---

## 6. 工具坑(新增,已同步进章程)

- **`axe_blink_domain.py:176` 按名字取快照、没锁 idx**;10/20 局有同名多实体,889 个非主实体快照是**活的**,能穿过 `hp > 0`。判「主实体」用**最早出现时刻**(本体从 −75s 赛前就在),别用 `hp_max` 或帧数(低血壳的帧数能到 656)。
- **幻象快照带道具(实测最多 5 件)** ⇒ 任何「按 `items` 含某道具」的筛法都不构成对幻象的过滤。
- **dumper 的 CLI 是位置参数 + 写 stdout**(`behav-dump -interval 0.5 <in.dem> > out.json`),**没有 `-in/-out`**。照抄 `-in` 会 20/20 局全部静默 FAIL(本轮踩,`sweep_run.sh:86` 是权威用法)。
- **0.5s 采样下按固定偏移(如 `t-0.2`)查前帧会大量落空** ⇒ 一律用「≤ 目标时刻的最后一帧」。

---

## 7. 验证

- 未改任何 Lua(`git diff --name-only` 的 `.lua` 计数 = **0**)⇒ 铁律第 6 条本轮无适用对象;且本容器**未装 `luacheck` / `lua5.1`**(fresh container,`which` 两者均空),**如实记录未假装跑过**。
- Python 侧:`python3 -m py_compile tools/batch_test/behavioral/axe_jump_pick_audit.py` 通过;新工具在 20 局语料上跑通并复现报告全部数字。
- 保真度自检:`axe_blink_domain.py` 在本轮独立重下重跑的语料上**逐位复现**英雄组 03:48Z 登记的全部数字(167/63/21/0、`{1:106,2:48,3:13}`、36.5%、25/12/48.0%、corpse 132、A 0/11)。
- **AWS 支出**:EC2 **$0**;S3 GET 20 个 `.dem` ≈178MB(出网 ≈$0.016);**未调用 Cost Explorer**(批测台 08:06Z §4 刚把这条 $0.01/次 的开销点出来,本轮不加码)。

---

## 8. 已检查清单更新

`spot_20260820_041132/041134/041137/041139_1_7c8b5167…` 共 **20 局**(16 mirror + 4 暖场),**跳刀维度首检完毕**(此前只有英雄组按 `axeblink` 域看过,且只用了 18 局)。

---

## 9. 下一轮优先

1. **`l5combo` 的 (a)** —— 仍是「别用同一批语料换切法」,等去掉它的 bisect 臂(连续第三轮登记,瓶颈没变)。
2. **`cmrguard` 若按 #63+#66 收窄** → 必须用**施法前帧口径**复跑四条验收 + #66 recall。
3. **`liondrainstop` 的 (a)** —— #74 §2 已给出修正口径(域内频道先按跨度 ≥2.0s 筛,并与零通道 4:2 并排报);等 §U.0 那波上机。
4. **`axe_blink_domain.py` 锁 idx 之后的复跑** —— 若英雄组采纳 #69 的建议,复跑一次确认漏斗仍逐位不变(本轮已给出「应当不变」的预期,改完要验)。
5. **OUT 带作为零通道的分辨率问题**(每种子 ~15 episode,SD 24.5)—— 引用 DiD 前先量对照带自己的 n(连续登记,未消)。

---

## 10. Token 用量

```
TOKENS total_in=17,215,554 out=135,218 turns=132
```
(统计时刻为止;此后的收尾回合未计入。)
