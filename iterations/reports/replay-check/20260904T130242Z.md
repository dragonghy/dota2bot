# 录像检查组 2026-09-04T13:02Z — `odaoe` 的条件 (a):三个判别子全部在**证明失效的那条腿上**复现

**一句话**:交棒第 (4) 件事(`odaoe` 连续第六轮零 `VERIFY`)这轮真的排上了 ——
36 局全部有 OD、两条腿都非空,**但买回来的不是 WORKING,是一次证伪**:
我为它造的三个判别子里最硬的那个(**不含任何伤害模型、不含视野假设的纯几何判别子**)
在 armed 腿读 5、在**「gate 可证明失效」的 baseline 腿读 5**(占比 18% vs **36%**,
baseline 还更高)。⇒ **`X.ConsiderSanitysEclipse` 的源码解释不了这个英雄在录像里的施法行为**,
在这一点被查清之前,armed 腿上的任何一发大招**都不能记到 `odaoe` 名下**。

`VERIFY id=odaoe verdict=INDETERMINATE episodes=0`

---

## 〇、本轮覆盖(章程吞吐要求)

**宽扫 36/36 局;深查 6 局逐帧 + 137 发大招逐发判定 + 5 帧完整还原;每 id 触发计数表见 §6。**

| 语料 | run | 种子 | `.dem` | 扫过 | 跳过暖场 | `unparseable` |
|---|---|---|---|---|---|---|
| W45 | `…_fdfe8d` | 3954 | 24 | **18** | 6 | **0** |
| W45 | `…_d5b1b8`(3968 补跑) | 3968 | 24 | **18** | 6 | **0** |

两台都是上一轮交棒点名的**未扫 run**,并且是 W45 里仅有的两个 **OD 载体**未扫机
(载体 `{3968, 3954}`,3968 的原机 `b6eb6a` 上一轮已扫,本轮补它的**补跑机**)。
**36 局全部有 OD**,`leg` 全部可判(0 局缺 side 戳)。

零 EC2、零发波、S3 只读、零 CE 调用。**未改 `bots/`/`game/` 任何一行。**

重扫命令(timeline 随容器回收):

```bash
bash tools/batch_test/aws/session_setup.sh
bash tools/batch_test/behavioral/sweep_run.sh \
  s3://dota2bot-batch-results-4924/soak/spot_20260904_063045_1_27165f8c67bb14fcef5eb04e424054f0551eb1e6_fdfe8d/
bash tools/batch_test/behavioral/sweep_run.sh \
  s3://dota2bot-batch-results-4924/soak/spot_20260904_071255_1_27165f8c67bb14fcef5eb04e424054f0551eb1e6_d5b1b8/
```

---

## 一、先说堵点:`odaoe` 的量具**这一轮之前跑不起来**(GH #478 的第 7 个文件)

`tools/batch_test/behavioral/od_eclipse_aoe_domain.py` —— 就是那份被逐字抄进
`hero_obsidian_destroyer.lua:620-636` 的入集前核验(「30 帧 = 10 episodes / 7 of 9 games」)
的产出者 —— 在本轮语料上 **`TypeError: 'NoneType' object is not iterable`,整轮零输出**。

```
od_eclipse_aoe_domain.py:321   ult = next((a for a in s["abilities"] if a["name"] == ULT), None)
od_eclipse_aoe_domain.py:419   ... for a in [next((a for a in s["abilities"] if a["name"] == ULT), None)]
```

**实测**:36 局里 **5 局**的真身 OD 实体带 `abilities: null` 帧;其中 **1 局**
(`20260904_064713_slot1`,7 帧,**7 帧全是活人**)会走到 `:321` ⇒ **1/36 局
(2.8%)的语料把 36 局的读数全部清零**。这正是 #478 立案句的形状:
**它不产生错数,它产生一个不存在的读数**。

**⭐ 而这个文件不在 #478 的表上,漏掉它的机制值得单独记一笔**:#478 的普查
grep 的是 `for a in s['abilities']` 和 `{a['name']: a for a in s['abilities']}`
两种**单引号、直接迭代**的写法;这里是**双引号 + `next(<生成器>)`**。
按同一把尺子重新普查,漏网的是 **2 个文件 / 3 个点**:

| 文件 | 行 | 在 #478 表上? |
|---|---|---|
| `od_eclipse_aoe_domain.py` | `:321`、`:419` | **否** |
| `od_ult_gate.py` | `:117` | **否** |

⇒ **#478 建议的验收方式必须是解析,不能是 grep** ——「按形状找形状」的普查
天然漏掉它没想到的那种形状,而这次漏掉的恰好是**扛着 `odaoe` 条件 (a) 的那一个**。
已追评 #478(见 §7)。

**本轮的处置(不是 `or []`)**:`odaoe_liveness.py` 把缺失帧**显式记账**
(`missing_ability_frames` / `..._alive`),从分子和分母**同时**移出,
**绝不读成「大招没准备好」** —— 那个方向把缺失帧记成真阴性,而它**偏向让 armed 腿好看**
(#478 修复说明里点名的那个方向)。

---

## 二、腿的判定(这一步错了,整张表会换腿而看上去照样合理)

单臂 `J.IsSoakCandidate`(`jmz_func.lua:5022-5025`)先匹配 id 再比 `GetTeam()`,
⇒ **baseline 腿上 `odaoe` 可证明失效**。W45 每局 OD **只在一边**(逐局核过:
滤掉幻象后每局恰好 1 个 OD 真身实体),且**种子决定阵容 ⇒ OD 的队伍在一个种子内恒定**,
而 `side` 在 run 内交替。⇒

- 3954:OD 恒 dire ⇒ `side=dire` 的局是 armed(7 局),`side=radiant` 的是 baseline(11 局);
- 3968:OD 恒 radiant ⇒ `side=radiant` 是 armed(12 局),`side=dire` 是 baseline(6 局)。

**两个分层都非空,而且同一个 OD、同一套阵容各出一条腿** —— 这是铁律 4(i) 要的配对读法。
四格 **7/11/12/6**,不压在反对角线上(对照 §CL 里 CM 的 0/198/120/0)。

---

## 三、逐帧(硬规则:先逐帧后聚合)

### 3.1 先给最像 WORKING 的那一帧 —— 它确实很硬

`…_fdfe8d/20260904_065937_slot8`(种子 3954,`side=dire` ⇒ **armed**),**t=353.5**,
快照恰在施法瞬间(**dt=0.0**,无采样滞后):

- OD hp 1286/1286、mp 851、大招 lvl1 `cd=0`;`t=354.5` 快照 `cd=139.3` ⇒ **确实放了**。
- 700 内 **4 个敌方英雄**:pudge 342u / juggernaut 475u / skeleton_king 591u / lina 600u。
- **出厂环路的两个合取项同时为假**:
  - `J.CanKillTarget`:裸伤(**按 0% 魔抗算的上界**)376 / 412 / 427 / 383,
    对手血量 1077 / 980 / 698 / 1075 ⇒ **四个全杀不掉,余量近 3 倍**;
  - 队友环 `#nInRangeAlly >= #nTargetInRangeAlly`:OD 侧 **2**(OD + dragon_knight,
    1200 内),四个目标各自 **4** ⇒ **2 >= 4 为假**。
- 实际命中 **4 个敌方英雄**(334 / 342 / 330 / 134)+ 4 个小兵。

按源码读,`X.ConsiderSanitysEclipse` 只剩 gated 那一个出口 ⇒ 看上去就是 `odaoe` 生效。
**§3.2 说明为什么这一帧仍然不能当条件 (a) 买下来。**

### 3.2 证伪:在 gate **可证明失效**的腿上,出厂代码做了它做不到的事

`…_fdfe8d/20260904_065910_slot5`(种子 3954,`side=radiant` ⇒ **baseline**),**t=1211.8**:

- OD 独自一人:**1200 内零队友**(最近的活队友 lion 2455u,skywrath 1819u 且**已死**)
  ⇒ `#nInRangeAlly` ≤ 1,而 juggernaut / pudge 互为队友 ⇒ 目标侧 ≥ 2 ⇒ **队友环为假**;
- 伤害侧:全场**任意距离**上没有一个敌方英雄可杀 —— juggernaut hp 1858 / pudge hp 2628 /
  lina 1902 / skeleton_king 3169,而按施法瞬间的蓝量回推(`t=1212.4` mp 1574 + 300 蓝耗
  = 1874)裸伤上界只有 803(实际打出 juggernaut 556 / pudge 354,与之相符);
- 1300 内**零幻象**;`AbilityUsageThink → BotBuild.SkillsComplement`(`ability_item_usage_generic.lua:8661-8668`)
  逐条查过,`X.SkillsComplement` 是这个大招在 `bots/` 下**唯一**的施法路径。

**⇒ 出厂环路的两个合取项都为假,gated 分支在这条腿上失效,可是大招放了。**

### 3.3 于是造第三个判别子:**纯几何,不含伤害模型、不含视野假设**

源码事实:出厂分支 `return BOT_ACTION_DESIRE_HIGH, enemyHero:GetLocation()`
(`hero_obsidian_destroyer.lua:561`)—— **圆心永远压在某个敌方英雄身上**;
gated 分支可以返回**两人中点**(`:687`)。
⇒ **「≥2 个英雄受害者,而任何以敌方英雄为圆心的圆都罩不住他们」= 出厂分支做不到的事**。
魔抗、`GetSpecialValue*`、迷雾、幻象、蓝量采样滞后 —— 一个都不进这个判据。

读数(容忍 60u 位移):

| 腿 | 多英雄命中 | **非英雄圆心** | 占比 |
|---|---|---|---|
| armed | 28 | **5** | 17.9% |
| baseline | 14 | **5** | **35.7%** |

**baseline 更高。** 五帧分别是 `063232_slot7 t=756.4`(3 人)、`064705_slot5 t=451.4`、
`064712_slot4 t=1481.7`、`064717_slot8 t=1507.1`、`065910_slot5 t=1026.8`(3 人)。

### 3.4 另外两个判别子(都不如 §3.3 硬,一并登记)

| 判别子 | armed | baseline | 说明 |
|---|---|---|---|
| 队友环封死 + AoE 谓词真 | **10** / 83 发 | **1** / 54 发 | 不含伤害模型,但含视野与幻象假设 |
| 裸伤(0% 魔抗上界)全员杀不掉 + AoE 真 | **17** | **4** | 含伤害模型 ⇒ 被 §3.2 直接反驳 |
| **纯几何(§3.3)** | **5** / 28 | **5** / 14 | **唯一无模型的,读数相等** |

**三个判别子给出三种方向不一致的图景**,而最硬的那个把差异抹平并反超。
**这正是「先逐帧后聚合」的用处**:只看第一行(10 : 1)会写出一个 WORKING。

---

## 四、裁定

**`VERIFY id=odaoe verdict=INDETERMINATE episodes=0`**

- **不是 WORKING**:§3.1 那一帧虽硬,但 §3.2 证明「出厂谓词的重建**是不完整的**」,
  而 §3.1 的全部说服力**恰恰来自那个重建**。同一把尺子在证明失效的腿上给出假阳性,
  就不能拿它给 armed 腿签字。
- **不是 SILENT**:域在两条腿上都到得了(armed 65 episode / baseline 44 episode),
  armed 腿 22/65 的 episode 后面 2 秒内有真施法。
- **不是 BUGGY**:没有任何一帧证明 gated 分支做了错的事。
- **不是 `DOMAIN-NOT-REACHED`**:36 局全有 OD,castable 帧 armed 12449 / baseline 11286。

**⇒ 堵点从「语料里没有 OD」(上一轮的诊断)转移到「出厂谓词不可重建」。
这是第一次给 `odaoe` 的六轮空白写出可执行的下一步。**

---

## 五、交付物

- `tools/batch_test/behavioral/odaoe_liveness.py`(只读离线,零 AWS):
  按腿拆分的域读数 + 施法归类 + **§3.3 的纯几何判别子** + GH #478 缺失帧显式记账。
  `--selfcheck` **21 checks / 0 failures**。
- `tests/test_odaoe_liveness.py` **30 checks / 0 failures**。
- **变异台 7 变异 7 CAUGHT / 0 SURVIVED**(树外 `cp` 还原,`sha256sum -c` OK,
  还原后基线 GREEN;每次运行前后清 `__pycache__`)。
  ⭐ **M5 第一版 SURVIVED,是断言的问题不是护栏的问题**:变异体往圆心表里
  偷偷塞一个字面量 `(0,0)`,而我最初的用例**全都长在原点附近**,那个假圆心
  与真圆心重合 ⇒ 看不见。补一对**跨原点**的受害者((0,+400) / (0,−400),
  中点恰是 (0,0))才把它逼出来 —— **一个假圆心只有在「只有它罩得住全部受害者」
  的形状上才可见**。据实登记这一次返工。

---

## 六、宽扫触发计数(选点用,不构成结论)

`sweep_run.sh` 的 15 个通用检测器,36 局合计(candidate 侧 / baseline 侧):
明细见两个 run 目录下的 `sweep_summary.md`。本轮深查选点**不是**从这张表来的 ——
是从交棒的第 (4) 条(`odaoe` 载体种子)来的,表只用于确认两台机器都没有解析失败
(`unparseable` 0/0)。

---

## 七、本轮开的 issue / 追评

- **GH #478 追评([harness])**:补第 7 个文件 `od_eclipse_aoe_domain.py:321,:419`
  与 `od_ult_gate.py:117`;给出「grep 形状漏掉了它自己没想到的形状」这条机制,
  以及 1/36 局把 36 局读数清零的实测。
- **新 issue([hero],给英雄组)**:`X.ConsiderSanitysEclipse` 的录像可见行为
  与源码不符 —— baseline 腿 `065910_slot5 t=1211.8` 帧证据 + §3.3 的 5:5,
  并给出建议的验收方式(把 `GetSpecialValueFloat('base_damage')` /
  `('damage_multiplier')` / `GetCastRange()` 三个 KV 读数按 `efa7ba7`
  (Lion/Zeus 同族)改由 getter 驱动并钉一个 fixture,或说明第四个真实入参是什么)。

---

## 八、下一轮第一件事

1. **等 §7 那个 [hero] issue 的回音** —— `odaoe` 的条件 (a) 在出厂谓词可重建之前买不到,
   **不要再扫同类语料**(和 `cmqreach` 上一轮的处置同形)。
2. **W45 另外三个 run(`b6eb6a` 已扫 / `8e7e5e` 0 局 / `d3d158` 52 局)** ——
   `d3d158`(种子 3927)**未扫**,`8e7e5e` 银行 0 局无录像。
3. **接 GH #467 的 `slotwait` 条件 (a)**(⚠️ 冷却读 dumper 的 `abilities[].cd`,
   **注意会是 `None`** —— 本轮又添一个受害者,见 §1)。
4. 查 **#477 / #470 / #474 / #482 / #483** 回音;#477 点名的重 dump
   (`W44/…_b164c8/20260904_003453_slot8` t=404.4)仍是本组的球,
   **W44 录像约 09-25 过期**。
5. `cmqreach` 的建议钉帧(上一轮给出 `…_b6eb6a/20260904_064534_slot7` t=764.5)
   **fixture 仍未做**。

**欠账**:36 局 timeline 随容器回收(§0 有重扫命令);§3.1/§3.2 两帧的 fixture **未做**
(建议帧已写进 §7 的 issue);`d3d158` 未扫;#419 第 15 轮 / #421 第 14 轮仍零评论;
继承未动那批不变。

---

## 九、验证(裸读,无管道)

- `session_setup.sh` **0**(`arn:…:user/dota2bot-agent`)。
- `sweep_run.sh` ×2 **全 0**;`sweep_complete.json` 各 `unparseable 0`、`swept 18`。
- `odaoe_liveness.py --selfcheck` **0**(21/0);真语料全 36 局 **0**。
- `tests/test_odaoe_liveness.py` **0**(30/0);**变异台 7/7 CAUGHT / 0 SURVIVED**;
  树外还原 `sha256sum -c` **OK**;还原后基线 **GREEN**。
- `od_eclipse_aoe_domain.py` 单局 **0**,全语料 **1(`TypeError`)** —— §1 的实测。
- `luacheck_gate.sh` **见 §9.1**;`arm_push_gate.sh` 见 §9.1。
  **未改 `bots/`/`game/` ⇒ 不声称 Lua 全量(GH #124)。**
- ⛔ **证据纪律 3 第二十七次踩**:本轮第一条命令又写了管道,
  `routine_selfcheck.sh` 当场自拒并打 `SELFCHECK_EXIT=2 REFUSED`(**不是通过**);
  它自己把两条正确写法印了出来,损失一次往返。
  ⚠️ **同轮第二次读码陷阱**:改成重定向那次超时被移到后台,
  harness 完成通知报 `exit code 0` —— **那是末尾 `echo` 的码**,不是脚本的。
  自检读数见 §9.1。

### 9.1 门与自检

- **铁律 6 静态半** `bash tools/agent/luacheck_gate.sh` **`GATE_EXIT=0 CLEAN`**,
  `luacheck bots game: 0 warnings`,**本容器冷启、脚本自己装了 `lua-check`**。
  **未用 `RULE6_BYPASS` ⇒ 没有「SKIPPED, not passed」那一行可抄。**
- `bash tools/agent/arm_push_gate.sh` **0**(`core.hooksPath=.githooks`)。
- **动态半**(`lua5.1 tests/run_tests.lua`,~100min,GH #124)**未跑,也不声称** ——
  本轮未改 `bots/`/`game/` 任何一行,新增的两个文件都是 Python。
- **开工自检**:`selfcheck worst exit: **3**`(有发现),`legs run: 9`,
  `FINDINGS (exit 3): cadence owed-executions trunk-red(python) trunk-red(lua)`,
  `UNCERTIFIABLE (exit 2): none`,`NOT RUN (inside a leg): tests/test_rc_wrapper.py
  tests/test_selfcheck_lua_leg.py`(容器无 `lua5.1` ⇒ 不是通过也不是失败)。
  - `trunk-red(python)` = `test_bots_walk_farm_only.py`,**批测台 GH #476,不重复认领**;
  - `trunk-red(lua)` = `test_coarmed_attribution_register.lua`
    (`creepthink`/`pullcad`/`pullthink` > `campbind` 三条新合取),
    **批测台 12:15Z 报告已立案交协同组/总监,不重复认领**;
  - `OWED campgrade_wave_type_premise_reading`(GH #480 / #241)
    `executor=batch-desk(排 campgrade 独占波的那一轮) + 录像组(读数)` ——
    **`campgrade` 此刻还不存在、W46 也不是它的波**,本组的读数这一轮**无处可买**,
    登记不认领;
  - `GAP cadence strategy` 不归本组。
  ⚠️ **第六次登记:自检在本容器不是章程写的「约 20s」** ——
  本轮那条超时被移到后台,**120s 预算的 5a0 腿没跑完**(76 个文件 / 120.1s)。

---

## 十、Token 用量

```
TOKENS total_in=12,451,389 out=68,451 turns=85
```

(统计时刻之前的数字;其后的收尾回合不计入。)
