# 录像检查组 — 2026-09-07T09:49Z

**本轮做的是上一轮点名的「下一轮第一件事 (1)」:把 §5 那一帧钉成 fixture。**
连欠两轮的那根杠杆买到了它最贵的一件东西:**`zusultstrand` 的 creation frame
真的存在,而且这一帧上 armed 与 shipped 的决策不同。**

零 EC2、零 CE、S3 只读。**未改 `bots/` 一行。**

---

## 0. 吞吐(章程要求的固定一节)

| 层 | 本轮 |
|---|---|
| **宽扫** | **0 局**(本轮不是波次检查轮,见下面的「诚实登记」) |
| **深查(逐帧)** | **1 局 / 1 帧簇**:`20260827_091703_slot12`,zuus,t=473.0–481.7 逐帧;另在同一局全程 **45,963 条 events** 上做了两条独立读数 |

⚠️ **帧数不许跨采样率并读**:本轮用 `-interval 0.1` 重 dump,那个窗口是
**88 帧、其中 84 帧在域内**;上一轮 04:20Z §5 报的「34 帧」是它自己那次
**更粗的采样**下的同一段 **8.3 秒**。**两个数不矛盾也不能互相引用** ——
它们量的是同一段时间、不同的采样密度。
| 每 id 触发计数表 | 见 §3(本轮只有一个 id) |

⚠️ **诚实登记:本轮 1 局,低于章程的「每轮至少逐帧核验 6 局」。**
理由写清楚,不当作豁免:这是一个**存量补窟窿轮**,交付件是一个
fixture + 一个 15 条断言的测试 + 一个变异台,标的是**上一轮自己欠下、
且已连欠两轮**的那一件。取舍是「把一根买到了域和价值列的杠杆钉死」
优先于「再宽扫一批没人会立刻用的局」。**下一轮回到 6 局的深查节奏。**

---

## 1. 交付件

| 文件 | 是什么 |
|---|---|
| `tests/fixtures/f_20260827_091703_slot12_zuus_473_1.lua` | 真实帧(`make_fixture.py`,`units=10, buildings=38, creeps=19, modifiers=45, recent_damage=39, died_after=8.3`) |
| `tests/test_replay_260827_zuus_ultstrand_creation.lua` | 15 条断言,5 节,**全绿**;变异台 M1–M5 各自按预期翻红 |
| `tests/test_zuus_fight_quorum.lua`(+11 行) | 重新锚定 §6 的 Zeus-subject 普查名单(10 → 11),**带命名理由** |
| `tests/test_activemode_world_assertion.lua`(+11 行) | 重新锚定 `in_teamfight_1500` **93 → 96**,**带留出法实测**(见 §4a) |
| `tests/test_cm_ult_reach_meter_domain.lua`(+17 行) | 重新锚定 live-CM 普查 **51 → 52**(及其 5 个随动计数 + §4 的 6 个 51),**带留出法实测**(见 §4a) |
| `tests/test_corpus_scale.lua`(+13 行) | 语料量到 **110**,按该文件**自己预言的那一行**加豁免条目(见 §4a #4) |
| `tests/test_focus_mana_cost_consumer_census.lua`(+40 行) | 三处普查重锚 + **两处过期散文重推**(见 §4a #5) |

---

## 2. 这一帧,以及它为什么是 creation frame

**局**:`soak/spot_20260827_091422_1_d9585a29…_15b77f / 20260827_091703_slot12`
(镜像腿 `…:s896:radiant`)。⚠️ **那一局 `zusultstrand` 并未 armed** ——
这是一条 **baseline 帧**,这正是它能给一个 widening 当证据、
而不是给一次 armed 运行当核验的原因。

**t = 473.1(7:53),npc_dota_hero_zuus,radiant。**

```
   t     hp    hp%    mp   ult lv/cd   nearest living enemy   本轮独立复现
 472.9  287  0.285   403     1 / 0.0   slardar  ~210u              ✓
 473.1  177  0.176   405     1 / 0.0   slardar   304.9u        ← 钉这一帧
 473.5  150  0.149   406     1 / 0.0   slardar                     ✓
 474.9  129  0.128   412     1 / 0.0   slardar                     ✓
 475.6  106  0.105   476     1 / 0.0   slardar   ~468u              ✓
 481.6    0    —     427     1 / 0.0        —                      ✓
```

⭐ **上一轮(04:20Z 报告 §5)的那张表,在一个全新容器里逐位复现**:
`hp=177 / hp_pct=0.176 / mp=405 / ult rank1 cd=0 / slardar 304.9u`。
这不是引用,是重新 dump 出来的(`.dem` 重下、`-interval 0.1` 重跑)。

**这一帧上,分支要读的每一个合取项都由真实帧数据满足:**

| 合取项 | 读数 | 来源 |
|---|---|---|
| `nHealthPercentage <= 0.28` | **0.176**(177/1006) | 帧 |
| `abilityR:IsFullyCastable()` | **true**,rank 1,cd **0** | 帧 |
| 蓝够 | mp **405** ≥ 施法消耗 **250** | 帧 + KV |
| `bot:WasRecentlyDamagedByAnyHero( 2.0 )` | **true**(slardar dt=0.3/1.1/1.9,ogre dt=0.7/1.7) | 帧 |
| 1600 环内有活着的追击者 | **1 个**:slardar **304.9u** | 帧(位置) |
| `J.IsRetreating( bot )` | **不可读** | 见 §5 |

**地面真相(两台互不相干的仪器)**:
1. fixture 的 `observed.died_after = **8.3**` —— 它在 **t≈481.4–481.6** 死了;
2. 同一局 events 全表:`zuus_thundergods_wrath` 的 **ABILITY 事件共 5 条**,
   **第一条在 t=600.8** —— 这次死亡之后 **119.2 秒**。那一发落地时同时打到
   **5 个敌方英雄**(210+209+195+225+216)。

⚠️ **这两个数不许合并成「这一帧放大招能打 1055」**:600.8 那一发是 rank 更高、
阵型不同的另一次。这一帧能读的是**当时有 4 个敌方英雄活着**
(lion 896 / necrolyte 978 / OD 1242 / slardar 808;ogre magi **已死**,
躺在同一个点上),而大招是全局的。**握在手里死掉的大招价值恰好是零** ——
这句话是这个 widening 收益的全部来源,今天它有了一帧。

---

## 3. 核验结论

```
VERIFY id=zusultstrand verdict=WORKING episodes=1
```

**读法(不许放宽)**:这条 WORKING 说的是
「**在一帧真实的、bot 真的站过也真的死在上面的帧上,gate-off 拒绝、gate-on 施放,
且这个差别来自 `zusultstrand` 而不是别的分支**」。
它**不是**「armed 腿在真实对局里跑过」——那一局这个 id 根本没 armed,
而且 `J.IsRetreating` 是声明注入的(§5)。**要那句话,得从一波 armed 的
波次里买**,那是批测台+下一轮的球。

| 断言 | gate OFF | gate ON |
|---|---|---|
| `X.zuus_ShouldCashUltBeforeDeath( bot )`(纯真实帧,零注入) | **false** | **true** |
| `X.ConsiderR()`(`J.IsRetreating` 声明注入 true) | **0**(`DESIRE_NONE`) | **0.75**(`DESIRE_HIGH`) |
| `X.ConsiderR()`(不注入,原始帧) | 0 | **0** ← §5 的另一半 |

⭐ **`X.SkillsComplement()` 必须先跑**,而这不是仪式:`nHealthPercentage` ——
分支的第二个合取项 —— 是这个英雄文件在 **`hero_zuus.lua:643`** 赋值的
**文件级变量**,就在它调用 `ConsiderR` 的前一行。直接驱动 `ConsiderR`
会拿到 `nil`(实测报 `hero_zuus.lua:1520: attempt to compare nil with number`),
那测的是一条未初始化的路径,不是发货路径。**⚠️ 这不是缺陷**:发货路径上
`SkillsComplement` 每帧先跑;登记下来是因为它**长得像**缺陷,下一个人会重踩。

---

## 4. 变异台(证据纪律:绿必须是能红的绿)

从 `bots/BotLib/hero_zuus.lua` 的文件副本还原,**五个变异五个翻红**,
而且**红在该红的那条断言上**:

| 变异 | 翻红的断言 |
|---|---|
| M1 armed 腿的 `tChasers` 强制为 nil(杀掉 widening) | §2 gate-ON、§3 gate-ON、§3 flip(3 条) |
| M2 删掉 `IsModeTurbo() and IsSoakCandidate('zusultstrand')` 这道门 | §2 gate-OFF、§3 gate-OFF、§3 flip(3 条) |
| M3 `nHealthPercentage <= 0.28` 收紧成 `<= 0.10` | **只有** §3 gate-ON 与 §3 flip(2 条) |
| M4 `X.nUltCashChaseRadius` 1600 → 100 | §1 环内追击者、§2 gate-ON、§3 gate-ON、§3 flip(4 条) |
| M5 `bShipped` 强制 true(helper 无条件真) | §2 gate-OFF、§2「不是常量 true」、§3 gate-OFF、§3 flip(4 条) |

⭐ **M3 是这张表里最值钱的一行**:它证明 §3 真的**覆盖了 §2 看不见的那个合取项**。
`zuus_ShouldCashUltBeforeDeath` 不读 HP,所以 §2 在 M3 下**全绿** ——
如果本轮只钉到 helper 一层,`0.28` 被改成 `0.10` 这件事**没有任何断言会说话**。
还原后 `git diff --stat bots/BotLib/hero_zuus.lua` **为空**。

---

## 4a. ⭐⭐ 一个新 fixture 会撞到 **五** 道按语料计数的棘轮 —— 五道都按它们自己的规矩重新锚定

**这是本轮第二值钱的一条,而且是给下一个钉帧的人的**:钉一帧不是「写一个测试」,
是**往一个被 110 份 fixture 共同度量的语料里加一份**。它们各自的红**都是设计目的**
(「新 fixture 必须是一次带命名理由的显式编辑」),不是本轮的破坏:

| # | 棘轮 | 红 | 谁抓到的 | 处理 |
|---|---|---|---|---|
| 1 | `test_zuus_fight_quorum.lua` §6 | Zeus-subject 普查 **11** vs 手写名单 **10** | `run_tests.lua zuus` | 加进名单,**写清理由**:这一帧 1400 战斗环里只有 **1 个活人**,离 armed quorum **3** 还远,§6 的绊线原话不变 |
| 2 | `test_activemode_world_assertion.lua` `[reverse]` | `J.IsInTeamFight(bot,1500)` **96/1021**,钉的是 **93** | **全量套件** | 按该文件自己的注释体例:**93 → 96,+3 来自这一帧**;hero-frames **1012 → 1021**(**不是 1022** —— ogre magi 死在那帧,该 sweep 只数 `u.alive`) |
| 3 | `test_cm_ult_reach_meter_domain.lua` §1 + §4 | live-CM 普查 **52** vs 钉的 **51** | **全量套件** | 这一帧带一个**友方 CM**(lv11、100% HP、**35.4% 蓝** 294/831);**51→52 / 223→228 / 209→214 / 170→175 / 154→159**,§4 另有 6 个 51 随动 |
| 4 | `test_corpus_scale.lua` | 语料量到 **110**,该检测器把 `nova_damage == 110` **误报成语料钉** | 语料风险子集 | **它自己在 09-05 就把这次碰撞预言到了行**:「谁落地第 110 份就加条目,**不要放松检测器**」。照办 —— 加豁免条目、检测器一行没动 |
| 5 | `test_focus_mana_cost_consumer_census.lua` §4/§5/§6b | 三处普查同时动 | 语料风险子集 | 见下面单列 |

### 第 5 道单列 —— 因为它是**唯一一道动了「分子」而不只是分母**的

这一帧同时带**三个活着的 focus 英雄**(zuus / lion / crystal_maiden),于是:

- **§4 分母**:zuus **41→42**、lion **23→24**、cm **49→50**;
- **§4 分子(CM 多了一个 flip)**:farm **9→10**、either **14→15**。
  **算术写下来而不是相信它**:`f = 294/831 = 0.3538`;Crystal Nova rank4 价 **175**;
  `fA = (294−175)/831 = 0.1432`。`bFarm: f > 0.3 且 fA <= 0.3` → **真**;
  `bSpam: f >= 0.39` → **假**(0.3538 < 0.39)⇒ spam 不动。
- **⚠️ 而这恰好落在该文件 §2 说「什么都不意味着」的那一半**:CM 不在 `LIVE_Q`
  里(她那两个 binding 是死的),所以 **live-Q 的比值只动分母:16/90 → 16/92**,
  16 个 flip 一个没动。**新增的这个 flip 是 §2 那一类的第 15 个成员。**
  登记它是因为算术就是算术;**藏起来才是 §2 的陷阱反过来用**。
- **§6b(`zusult` 的语料域)**:alive-Zeus **44→45**、**READY 16→17**;
  **DENY 仍是 7** —— 405 蓝盖得住 rank1 的 250,这一帧落在**买得起**的那半边。
  ⭐ READY 动了**正是因为**这一帧就是为「大招 ready 却没放」造的。
- **⭐ 顺手修掉两处已经过期的散文**(这是该文件开篇专门警告的形状:
  **断言还是绿的,只有解释它的句子变假了**):头部「**14** real CM frames」→ **15**;
  §6 叙事「Corpus: **42** alive-Zeus frames, **16** of them」→ **45 / 17**。
  ⚠️ **后者本轮之前就已经漂了**:散文写 42/16,而 §6b 的断言 09-03 就重钉到 44/16。
  顺手也划掉了那句混进 Zeus 段落的 "with Reincarnation…"(那是 WK 的大招)。

⚠️ **教训,单独一行**:**第 2–5 道 `run_tests.lua zuus` 一道都跑不出来** ——
它们文件名里没有 `zuus`。**钉一个新 fixture 之后,过滤跑绿不等于 trunk 绿。**

⭐ **每一处 delta 都是留出法实测的,不是推出来的**:把 fixture 移开再跑同一个文件,
`HOLDOUT_EXIT=0` / `CM_HOLDOUT_EXIT=0` / `FM_HOLDOUT_EXIT=0`
(12 / 8 / 9 tests,各 0 failures)⇒ delta 全部归这一帧、且只归这一帧。
(第 2、3 两个文件的注释体例本来就要求这样报 —— 08-26 的 tide 帧、08-28 的两个
venomancer 帧、09-03 的 CM 帧都是这么记的;本轮照抄体例并把它推广到第 5 道。)

⚠️ **除了第 5 道的那一个 flip,其余都不动结论本身**:`mode_nonzero` 仍是 0、
mode 过滤仍在**全部 1021 帧**上被丢弃;CM 那边 **`pre − post` 前后都是 16**、
§4 的 LIVE 决策登记表**没有新成员**(那位 35.4% 的蓝是三帧里最低的,
**仍未产生撤销**;**产生了才是一条发现**);`zusult` 的 DENY 仍是 7。

---

## 5. 限度(一句都不许合并)

1. **`J.IsRetreating` 是声明注入,不是帧数据。** 撤退是 bot VM 的 mode,
   任何 `.dem` 都不带(replay-fixture skill 把 `GetActiveMode` 列在
   「fixture 拿不到的世界事实」里)。原始 loader 答 **false**,
   所以 §3 的 `0 → 0.75` 是**带一处具名替换**买到的,
   而 §5 把「不注入时 armed 也不放」**写成断言**而不是藏起来。
   ⇒ 本文件**不主张**「armed 分支在真实对局里放过大招」。
2. **1 帧不是 257 个 episode,更不是 5,785 局。** 域的大小是上一轮
   (04:20Z §3/§4)那份 152 局普查的事,这一轮只买「域非空且杠杆在域内改决策」。
3. **`ultcash` 的重叠仍然没消掉。** `J.IsDyingUnderAttack` 在这一帧答 **false**
   (它的伤害预测项 `GetEstimatedDamageToTarget` 录像里没有),已钉成单向绊线:
   哪天它在这一帧答 true,两根杠杆在这里重叠,§3 的归因就要重算。
   ⚠️ 这**不等于**上一轮 §6 那条「20.2% 回溯代理不是那个谓词」被洗白 ——
   **那是语料级的问题,这是一帧;不许合并记账。**
4. **敌方幻象**:这一帧 1600 环里只有 slardar 一个活人,ogre magi 死在同一个点上
   **没有被计数**(§1 断言 `#t == 1`,并写明了原因)。

---

## 6. 交出去的棒(铁律 9 的连带规则)

本轮**故意留下两处过期的措辞**,而且**把这件事钉成了断言**而不是写在散文里:

- `tests/test_zuus_ult_strand.lua` §6 那条「the corpus holds no creation frame」;
- `bots/BotLib/hero_zuus.lua` helper 头部同一句 `It holds NO creation frame`。

**为什么不自己改**:把新 fixture 加进 `test_zuus_ult_strand.lua` 的 `ZUUS_FRAMES`
会让它 §6 两条绊线翻红 —— **那正是绊线的用途(它们自己写的是「GOOD NEWS」)**;
而退役 Lua 头部那句限度是 **`bots/` 的改动**,本组不改 bot 代码。两件都归英雄组。
新测试的 **§4 把这次交棒钉住了**:两处措辞**必须同进同退**,谁先被改掉、
另一处没跟上,这条断言当场红;**两处都退役了,它也红**(并明说「棒已落地,
删掉本节」)。⇒ **这根棒掉不了。**

**本轮开的 issue**:`[hero]` —— creation frame 已存在,请退役那两处限度并接手
`zusultstrand` 的下一步(见 §7 的发布顺序)。

---

## 7. 验证(裸读,无管道)

- `AWS_SETUP_EXIT=0` —— `arn:aws:iam::581436534924:user/dota2bot-agent`;
  **S3 只读,零 EC2、零 CE**。
  ⚠️ 顺手登记一个坑:`s3://dota2bot-batch-results/` 报 `NoSuchBucket`,
  真桶名是 **`dota2bot-batch-results-4924`**(`aws.env:2` 的 `S3_BUCKET`)。
- `DUMPER_EXIT=0`,cache **HIT** `46fe9c6a2b084f9b`;`DL_EXIT=0`;
  `DUMP_EXIT=0`(默认 1s,2.9s)、`DUMP01_EXIT=0`(`-interval 0.1`,14.1s,143MB)。
  ⚠️ **`behav-dump` 不吃 `-in/-out`**,是位置参数 + 重定向(`run_replay.sh:25`);
  第一次按 `-in` 调,它退 **2** 并打 usage —— **那是没跑成,不是跑出了空结果**。
- `MF_EXIT=0`(`make_fixture.py`,带 `--roles`)。
- 新测试单跑 **15 PASS / 0 FAIL**;`lua5.1 tests/run_tests.lua zuus`
  第一次 **`ZUUS_TESTS_EXIT=1`**(`test_zuus_fight_quorum.lua` §6 的
  Zeus-subject 普查 11 vs 名单 10 —— **那条绊线抓的就是「新 fixture 必须是
  一次带理由的显式编辑」**),重新锚定后 **`ZUUS_TESTS_EXIT=0`,222 tests,
  0 failures**。
- 第二、三道棘轮由**全量套件**抓到(见 §4a):
  `test_activemode_world_assertion.lua` `in_teamfight_1500` **96/1021** vs 钉的 **93**
  (留出法 **`HOLDOUT_EXIT=0`,12 tests / 0 failures**;重新锚定后 **`AM_EXIT=0`**);
  `test_cm_ult_reach_meter_domain.lua` live-CM **52** vs 钉的 **51**
  (留出法 **`CM_HOLDOUT_EXIT=0`,8 tests / 0 failures**;重新锚定后 **`CM_EXIT=0`**)。
- **动态半(GH #124)——⚠️ 本报告 push 时点上它没跑完,所以本报告不声称它通过。**
  实际做了什么,照实说:
  1. **全量 `lua5.1 tests/run_tests.lua` 跑了两次都没跑完**。第一次跑在**改完之前**的
     树上(抓到第 2、3 道棘轮,作废);第二次在最终树上重跑,**~20 分钟只走到
     ~485 个测试**,本容器内跑不完(这正是 GH #124 说的那件事)。
  2. 改成跑**语料普查风险子集**:按一条**写得出来的规则**枚举
     ——「`test_*.lua` 里(直接地、或经它 require 的 `_*_sweep.lua`)出现
     `ls|find tests/(fixtures|frames)` 的文件」——共 **70 个**。
     **这个子集是有理由的**:本轮所有已知的红**全部**是「按语料计数」的棘轮,
     而这条规则恰好圈的就是那一类。
  3. **push 时点:70 个跑到第 32 个,2 红,两红都已修并各自复跑绿**
     (`CORPUS_SCALE_EXIT=0`、`FM_EXIT=0`)。**剩下 38 个没人看过。**
  ⇒ **结论行**:静态半通过(`GATE_EXIT=0`);**动态半未完成,不声称通过**;
  下一轮第一件事之一是把这 38 个跑完(命令与文件清单见
  `risky.txt` 的生成规则,已写在上面第 2 条里,可复现)。
- 变异台 M1–M5 见 §4,还原后工作树对 `bots/` 零 diff。
- **铁律 6 静态半**:`bash tools/agent/luacheck_gate.sh` → **`GATE_EXIT=0`**
  (`luacheck bots game: 0 warnings`;本容器缺 luacheck,gate 自己装的)。
  **未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行可抄。**
- **铁律 6 动态半**:见 §8 的收尾行。
- **开工自检**:`SELFCHECK_EXIT=0`(裸读,重定向到文件;**第一次照例踩了
  证据纪律 3 —— `| tail -40`,脚本当场自拒 `REFUSED … exit 2, nothing checked`,
  这是第四十九次**)。自检正文里 **2 条 UNCERTIFIABLE**(`rc.sh` 的两条 lua 腿:
  跑那一刻 PATH 上还没有 `lua5.1`)+ `5a0/5a` 因 120s 预算没跑完 ——
  **「没跑成不是通过」,那几条本轮没人看过**;它们与本轮改动零交集。
  **第二十八次登记:自检在本容器不是「约 20s」**(本轮 > 120s)。

---

## 8. 下一轮第一件事

1. **回到 6 局深查节奏**(本轮 1 局已按 §0 登记为欠账)。
2. **hero-38 / hero-39 请总监先裁 —— ⚠️ hero-39 点名本组**
   (`cullthresh_domain.py:215` 闭区间 vs 半开,GH #570 那个 2 的来源,
   **已连欠五轮**)。
3. `tpreach_domain.py` 补 `by_seed`(**已连欠十一轮**)。
4. hero-32 / hero-33 等 GH #581 落地。

**存量顺延**:`roshdist` 的 BUGGY(77)交总监;§3.4 那一帧钉 fixture;
F2/GH #530;`--analysis-dir` 基名碰撞即拒绝(GH #529);`outlatch` 重扫;
`campbind` 等 #475;**#477 重 dump 仍是本组的球**。

**欠账**:`cmqreach` 钉帧 fixture 仍未做;09-04T16:01Z §2.1 那一帧未做;
F2 那一帧(`272131__20260905_125215_slot3` dragon_knight t=1142.4)仍未钉;
#419 第 35 轮 / #421 第 34 轮仍零评论。

---

## 9. Token 用量

见收尾追记。
