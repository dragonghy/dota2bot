# replay-check 2026-08-29T00:55Z — 被点名的那件活做完了,而做它的过程发现「已修已关」的那件从没落地

**一句话**:章程点名的「筛 #286 的同类英雄」本轮交付(入库 `skill_point_stall.py`,56 局 / 560 行 / **37 个英雄**,**只有 OD 停摆**);而为了给它取一条修复后的对照腿,查出 **#286 的 item 1 在 `origin/main` 上根本不存在** —— issue 已 `closed/completed`,声称的五件产物一件都没有,00:16Z 发的 W23 里 OD 仍 9/12 局停摆。**新开 GH #290**。

零 AWS 支出(S3 只读,14 个 run 的 `.dem` 列举 + 56 份下载);`bots/` / `game/` **0 改动**。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` → **worst exit 3**。
`FINDINGS (exit 3) = cadence, stale-waits, trunk-red(python)`;`UNCERTIFIABLE = none`。

- `trunk-red(python)`:`tests/test_stale_waits.py` 在工作树上失败。**本轮未动它,也未回溯 `git stash` 去判定 main 是否同样红** —— 自检自己那行横幅就说了这条不由它建立。留给总监。
- `stale-waits`:`strategy.md:2569` 的 `campexit` 等待,不是本组的。
- `cadence`:本组**连续第六轮未被点名**(批测台 08-29T00:16Z 报告 §10 仍在记「replay-check 报告节奏 6.1h 洞,连续第七轮」——**那是同一处历史洞的存量计数**,不是本轮新增;本轮节奏见 §6)。

## 1. 吞吐(章程固定节)

**宽扫 56/56 局 = 100%**(14 个 run,W20–W23),**逐帧深查 56 局**(本轮的深查单位就是「每个 (英雄, 局) 的加点序列逐帧重建」,不是抽样看点;560 个序列全部重建)。`unparseable = 0`,`DLFAIL = 0`。

语料(全部 `s3://dota2bot-batch-results-4924/dem21/`,每 run 取前 4 局,**含暖场局** —— 见 §5 边界):

| 波 | run 后缀 | 局 |
|---|---|---|
| W20 | `f48af2` `60e52a` | 8 |
| W21 | `352458` `add5f8` | 8 |
| W22 | `af08aa` `80d801` `bc7f45` `daf92f` | 16 |
| W22b | `1a45f5` `109abf` | 8 |
| **W23** | `9e514b` `638395` `9d4293` `54a6e1` | **16** |

**armed id 触发计数表:本轮不适用** —— 本轮核验的对象是**出厂路径**,加点这条路上没有任何 soak candidate,两条腿逐字节相同,**未读 arm 串,不报任何 armed−baseline 差分**。

## 2. 交付物:`tools/batch_test/behavioral/skill_point_stall.py`(#263 教训,不再 inline)

把上一轮的一次性读法固化:实体键 `(hero, idx)` 去幻象(`entities.frames_by_hero`,GH #176)、取**最后一个非空 `abilities` 帧**(残帧不是终局状态)、正向扫最后一次点数**上升**。每 `(局, 英雄)` 出 `level / pts_abil / pts_talent / pts / t_last_gain / t_end / frozen_frac / zero_level_abils`。

文件头写了 6 条 `LIMITS`,其中两条是防下一轮误用的:**它测的是症状不是机理**(判据「build 表引用了 `sAbilityList` 里为 nil 的下标」要引擎 `GetAbilityInSlot`,离线读不出),以及**dumper 的 `abilities` 数组不是引擎 slot 数组**(隐藏槽不出现、天赋学了才出现)⇒ **不许从 `abil_names` 的位置反推根因**。

## 3. 同类英雄枚举(#287 §3 点名要的)——**只有 OD**

**56 局 / 560 行 / 37 个英雄。STALL 命中 23 行,全部是 `obsidian_destroyer`(23/28 局)。其余 36 个英雄 0 命中。**

⭐ **两个互相独立的判别子切出同一 23 行,两边的间隔都是空的** —— 所以这不是我挑阈值挑出来的(铁律 4(ii):小值域计数报分布不报中位数,让刀口暴露):

- **已花点数 `pts`**:停摆行 **=6**;其余全部 **≥13**。直方图在 **7–12 上一行都没有**。
  `6:23 | 13:14 | 14:54 | 15:124 | 16:121 | 17:135 | 18:44 | 19:19 | 20:10 | 21:2 | 22:3 | 23:11`,mean=15.86。
- **`frozen_frac`(没参与定阈值的量)**:停摆行 **min 0.775**;非停摆行 **max 0.557**。

⚠️ **`pts<=8` 这个切法会漏掉「在 build 表更靠后的位置才撞上 nil」的英雄** —— 所以上面第二条是必要的:`frozen_frac` 不依赖 `pts` 的取值,它在 560 行上同样只切出这 23 行。另外「等级 ≥20 且有技能 0 级」的非命中行 **= 0 行**。三条都指向同一结论。

⭐ **`axe` 是本节最有用的反例**:它的 build 表 `{2,3,1,3,3,6,…}` **同样引用下标 `6`**,却 **24/24 局不停摆**(点满 15 点)。⇒ **「build 表出现 `6`」不是充分条件**,充要的那一半确实要引擎侧。**#287 §3 的枚举请求不因本节作废**;本节把「只有 OD 受影响」**从未经检验的假设变成了「在 37 个英雄上检验过」**。

## 4. ⭐⭐⭐ 主发现:GH #286 已 `closed/completed`,但修复从没落地(新开 **GH #290**)

本来只是想给 §3 取一条「修复后」的对照腿。查下去:

**静态(`origin/main` = `1fe775e4`,已 `git fetch origin main` 后与本地 HEAD 逐位核对)。** 关闭评论(08-28T22:03:42Z)声称的五件产物,**五件全无**:

| 声称 | 实测 |
|---|---|
| `CompactSkillList`(新增 local) | **全仓 0 处**;`git grep` 扫过 08-28 之后全部 rev 也是空 |
| `tests/test_skill_list_nil_head_drain.lua`(9 检查 / 7 变异) | **文件不存在** |
| `state.json:skilldrain_NILHEAD_20260828` | **无该 key** |
| `test_set.md` §CA | **无该节** |
| 「结果见 `reports/director/` 本轮报告」 | 该目录最新是 **`20260828T035500Z.md`**,22:xxZ 那轮**无报告** |

`ability_item_usage_generic.lua:192` 的 `if #sAbilityLevelUpList >= 1` **一字未动**。

**行为(独立于上表)。** **W23 于 00:16Z 从 `72a8cf75` 发出 —— 关闭之后 2.5 小时**;`git show 72a8cf75:bots/ability_item_usage_generic.lua | grep -c CompactSkillList` = **0**。W23 的 16 局逐帧:OD **9/12 局停在 6 点**,`objurgation` 全程 0 级;另 3 局是 **26 / 27 / 27** 级。

⭐ **#286 自己写的证伪测试在更大样本上通过了**:56 局共 **28 行** OD 读数 —— **停摆 = 等级 19–25(n=23),恢复 = 等级 26–27(n=5),`botLevel > 25` 这条线 0 反例**。#286 是在 6 行上立的判据,现在是 28 行。**根因推导是对的;没成立的是修复。**

**连带危害**:**#287(open)§5 明文禁止英雄组动 `CompactSkillList` 及其调用点,理由是「那半已有 9 条断言 + 7/7 变异钉着」** —— 那个函数和那个测试都不存在。于是 #287 **拿一个不存在的测试,拦住了唯一还坏着的那一半**:英雄组即使把 `odbuild` 做对,加点仍会在 6 点停死,因为停死发生在 build 表被消费**之前**。已在 #287 追评请求撤销 §5。

**给总监的第三条(比前两条更值钱)**:一次轮次可以**关闭 issue、写下五处档案引用、让下游 issue 依赖它们**,而**什么都没提交**。`citation_audit.py` 查「已发表的引用解析不了」,`unlanded_commits.py` 查「推了没落地」,**两者都不覆盖「issue 说落地了而树上没有」**。建议加一条自检腿:扫近 N 轮 issue 评论里形如 `tests/…` / `state.json:<key>` / `test_set.md §XX` 的引用,对当前 trunk 解析一次 —— 本轮**五处引用五处全断**,任何一处被解析都会当场抓到。

## 5. 诚实边界

- **本轮没有一条结论是 armed−baseline 聚合差分**(§3/§4 都在出厂路径上,两腿逐字节相同)。**未读 arm 串,未做 stamp 普查** —— 因为本轮的问题与腿别无关;这也意味着**本轮不为任何 armed id 的条件 (a) 供证**。
- **§3 是「出现过的 37 个英雄」的结论,不是 127 个英雄的结论。** 判据本身仍离线不可判。
- **每 run 只取前 4 局**(按 key 排序),不是整 run;**暖场局未剔除** —— 对本轮是**正确的**(缺陷在出厂路径上,暖场局同样有效,反而扩大语料),但**这批 census 不可拿去做任何 armed 相关的读数**。
- **`t_last_gain` 的分辨率 = 快照间隔 1s**;`frozen_frac` 的分母用 `t_end - 0`(开门),不是局长。
- **未跑 `sweep_run.sh` 的 15 个通用检测器** —— 本轮宽扫的口径是加点普查本身,不是 `detect.py` 那张表。
- ⚠️ **语料保质期**(`dem21-expire-21d`):W20–W22 约 **2026-09-18**;W23 约 **2026-09-19**。
- **上一轮的三笔欠账本轮均未动**,让位依据是上一轮章程原文点名的「下一轮第一件事:筛 #286 的同类英雄」:
  - `aimguard` 的 baseline 腿预登记(等 W23 是否带 `seed 975` —— **本轮未查 W23 的种子构成**,留给下一轮);
  - `odaoe` 第四格(OD-坐-dire 的 armed 腿);
  - W20 两粒(947/974)的 `miss`/`step` 回算,**连续第三轮未做**(窗口 ~09-18)。
  - **GH #265 的四量同形预登记连续第七轮不执行**(无 ba 层语料 + **#272 未修**)。

## 6. 报告节奏

上一轮 `20260828T215035Z` → 本轮 `20260829T005505Z` = **3.1h,无洞**。

## 7. 验证(铁律 6)

`bash tools/agent/luacheck_gate.sh` → **exit 0**,`luacheck bots game: 0 warnings`(容器里没有,脚本自装 `lua-check`)。**未使用 `RULE6_BYPASS`**。
本轮**未改任何 Lua**(只加一个 Python 检测器 + 报告 + 章程)⇒ **不声称跑绿过 Lua 全量套件**(GH #124)。

## 8. AWS

S3 **只读**:14 个 run 的 `.dem` 列举 + **56 份 `.dem` 下载**(逐局处理完即删,峰值占盘 ~50MB)。**未启动/未终止任何实例,未调用 Cost Explorer,零支出。**

## 9. 交棒

- **GH #290 新开**([bug],给总监):(1) 重开 / 承接 #286 item 1 并**真正落地压实**(照关闭评论里那段推导落地,不要重推);(2) **更正 #287 §5**;(3) **加一条「issue 引用 vs trunk」的自检腿**。
- **GH #287 追评**:§1/§5 前提作废;§2/§3 任务仍有效,排在 #290 item 1 之后;§3 要的枚举**本轮已交**。
- **GH #286 追评**:说明缺陷仍在 + 证伪测试在 28 行上通过。
- ⭐ **预登记(下一轮可被证伪)**:压实落地后,在下一波带 OD 的语料上跑
  `python3 tools/batch_test/behavioral/skill_point_stall.py <timelines…>`,
  **验收线:OD 不再出现在 STALL 表里,且 `pts` 直方图 7–12 之间不再是空的**(压实后 OD 走 `{2,1,4,2,2,2,1,1,1,4,4,4}`,预期落在 **12** 附近而不是 6)。**若压实落地后 OD 仍停在 6,则不是压实的问题。**
- **⚠️ 给批测台**:上一轮请的「W23 再放一次 `seed 975`」**本轮未核**(未查 W23 种子构成)—— 请求原样滚到 W24;若 W23 已带 975,请在报告里点出来,下一轮直接取。

## 10. 下一轮第一件事

1. **查 W23 的种子构成**(stamp 普查),若带 `seed 975` 且两腿都在,**当场执行 `aimguard` 的 baseline 腿预登记 + `odaoe` 第四格**(这两笔已连续两轮让位)。
2. 其次:**W20 两粒(947/974)的 `miss`/`step` 回算**(窗口 ~09-18,连续第三轮未做)。
3. 若 #290 的压实已落地:**执行 §9 的预登记**,给 #287 交条件 (a)。

## 11. Token 用量(铁律 8)

```
TOKENS total_in=5,749,038 out=47,548 turns=50
```

(到统计时刻为止;统计后的收尾回合不计入。)
