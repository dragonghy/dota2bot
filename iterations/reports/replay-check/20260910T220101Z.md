# 录像检查组 2026-09-10T21:46–22:0xZ(无新波轮:取 `tbearly`,买到一个 **SILENT-BY-CONSTRUCTION**,并**推翻一条已登记的入集依据**)

**一句话**:`tbearly` 的条件 (a) 本轮**买到了**,判决 **SILENT** —— 而它值钱的地方不是这个判决,
是**买它的过程推翻了 `state.json:cap25_boundary_20260825.domains_unblocked` 那一句**:
那条普查说 cap 放宽到 25 分钟后 `tbearly` 与 `alchrage` 两个 id「域曾经构造性为空、**现在够得到了**」。
**两个都仍然是零**,而且**各自是被一个那次普查没看的机制关掉的** —— 
`tbearly` 被一条**外层合取项**(`not J.IsLateGame()`)、`alchrage` 被**抽签池**(alchemist 不在池里)。
⇒ **2/2 全错。这不是一行写坏了,是那次普查问错了问题。**

```
VERIFY id=tbearly verdict=SILENT episodes=0
```
⚠️ `episodes=0` 的分母必须连读:**不是「没语料」**。本轮语料里该子句被**求值了 103,315 个英雄帧**
(两条腿逐帧一致),两条腿**发生分歧的帧 = 0**。这是**构造性的零**,不是抽样不足的零。

---

## 〇、本轮定位与覆盖数字

**无新波次** —— 批测台 18:14Z 与 21:18Z 连续两轮因闸 (iii) 预算围栏零发波
(21:18Z 逐字 `Headroom was $0.647, this wave needs $1.100`)⇒ 交棒单第 (1) 条 W64 仍未落地,
按章程工作流第 2 条转做第 (3) 条:**从 `a_evidence_*` 八条 UNOWED 里取一条**。

- **开工 `AEV_EXIT=3`**:`armed 37 verdict 29 owed-row 0 UNOWED 8`。
- **选点判据(本轮先做、后花钱)**:八条逐个读工具头部的 LIMITS,先问**「这个量离线读不读得出来」**,
  再决定买不买语料。这是 W62/W63 的学费(`campvoid` 花了整轮语料才发现 dumper 不发 creep 实体)。
  - `blinkflee` **当场排除**:`blinkflee_domain.py` 自己**已登记结构性拒绝** ——
    同一个 consider 函数里有**三处**朝自家远古的 blink,几何上分不开
    (`:1647` 的击杀 blink 在敌人站在自己与远古之间时**落点逐字相同**),
    工具因此「报漏斗、拒绝转成判决」。**再买 88 局也只会复现 INDETERMINATE。**
  - `tbearly` **选中**:`tbearly_domain.py` 声称它可以**从出厂 Lua 的算术**证明,**零语料**。
- **语料**:W63 四个 run 在 `replays/` 里**只有 11 个 `.dem`**(带 `__<run_id>` 后缀,
  即那条平铺撞车坑的修法)。**11/11 dump 成功,unparseable 0**。
  其中 **4 局是暖场局**(`script_version='cfa966e3'`,无 `mirror:` 前缀 ⇒ `tbearly` **未 armed**),
  **7 局带 armed 戳**(radiant-armed 5 / dire-armed 2)。
- **深查**:**11/11 局逐帧**核验决策瞬刻邻域(每局 t∈[1078,1081] 的逐英雄帧 + 1080.0 是否被采样),
  另在 **2 局 armed 腿**上钉了配对帧锚。**对这个 id 而言相关的帧邻域只有 t≈1080 一处**,
  逐帧覆盖是完整的而不是抽样的。
- **AWS**:只读 S3(4 次 `ls` + 176 个 `analysis.json` + 11 个 `.dem`),**零 EC2、零发波、零 CE、零支出**。

---

## 一、杠杆与算术(**我自己读的出厂 Lua,不是转载工具的话**)

`bots/mode_farm_generic.lua`,farm desire 里「优先打线上兵而不是打野」那一支:

```lua
501  if GetGameMode() ~= GAMEMODE_MO
...
507  and not J.IsLateGame()          <-- 承重的就是这一条
508  then
...
520      if #hLaneCreepList > 0 then ... else
554          local nEarlyClock = 25 * 60
555          if J.IsSoakCandidate('tbearly') and J.IsModeTurbo() then
556              nEarlyClock = 18 * 60
557          end
558          local bEarlyGame = DotaTime() < nEarlyClock
559              and bot:GetNetWorth() < 15000
562          if bEarlyGame and nDeaths < 5 then   -- 线上兵优先
```

而 `bots/FunLib/jmz_func.lua:4757`:

```lua
function J.IsLateGame()
    if DotaTime() > (J.IsModeTurbo() and 18 * 60 or 30 * 60) then return true end
    return false
end
```

⇒ turbo 下 `IsLateGame()` ⟺ `t > 1080`。**能走到 :554 的帧必然 `t <= 1080`。**

| | 时钟项 | 在 `t <= 1080` 上 |
|---|---|---|
| 门关(出厂) | `t < 1500` | **恒真** |
| 门开(armed, turbo) | `t < 1080` | **恒真,除了 `t == 1080.0` 这一点** |

**两条腿的分歧集 = `{t == 1080.0}`,一个连续时钟上的零测集。**
`tbearly_domain.py --source`(自己去出厂 Lua 里读常数、用真的 Lua 块匹配器确定外层包裹,不重打字)
独立给出同一结论:`VERDICT STRUCTURAL-ZERO`,`enclosed_by_not_late True`,`n_enclosing_blocks 3`。
工具 `--selfcheck` **34 PASS / 0 FAIL,EXIT=0**(含四条**反向**用例:armed 边界下移、外层天花板抬高、
外层包裹消失、L7 能产出非空 —— 即它**不是硬编码的零**)。

⚠️ **外层那条合取项不是这个门带来的**:`and not J.IsLateGame()` 在**上游 OHA 快照**里就有
(`74727e4:427`),**早于本仓库的全部历史**。⇒ 这个门**从落地的第一天起**就是 no-op。

---

## 二、帧证据(硬规则:先逐帧后聚合)

### 2.1 承重帧:同一帧上两条腿并排,逐位一致

`14565d/20260910_124644_slot1`,**armed 腿 = dire**(`script_version` 末段 `:s10842:dire`),
`t = 1078.4`(子句可达区的最后 1.6 秒):

| 英雄 | team | armed? | net_worth | deaths | hp_pct | 子句被求值 | 两腿是否同值 |
|---|---|---|---|---|---|---|---|
| crystal_maiden | 3 (dire) | **armed** | 12519 | 1 | 1.0 | 是 | **是**(`t<1080` 与 `t<1500` 同为真) |
| zuus | 2 (radiant) | baseline | 9368 | 3 | 1.0 | 是 | **是** |
| juggernaut | 3 (dire) | **armed** | 12832 | 2 | 1.0 | 是 | **是** |
| pudge | 3 (dire) | **armed** | 9017 | 2 | 1.0 | 是 | **是** |

**一帧之内、两条腿之上、四个英雄**同时满足 `nw<15000 ∧ deaths<5 ∧ 存活 ∧ t≤1080`,
即子句**真的在跑**(它不是死代码),而 armed 与 baseline 在这一帧上**必然给出同一个 `bEarlyGame`**。

### 2.2 `t == 1080.0` 这一点,**11 局无一被采样**

dumper 的 `-interval` 默认 1.0s,每局的采样相位由该局开局决定:

| run/局 | armed 侧 | 1080 之下最后一帧 | **1080.0 被采样?** | 1080 之上第一帧 |
|---|---|---|---|---|
| 201fec/…121930_slot1 | (暖场) | 1079.3 | **否** | 1080.3 |
| 201fec/…123438_slot1 | radiant | 1079.5 | **否** | 1080.5 |
| 201fec/…124503_slot1 | radiant | 1079.4 | **否** | 1080.4 |
| 16a195/…121929_slot1 | (暖场) | 1079.4 | **否** | 1080.4 |
| 16a195/…123422_slot1 | radiant | 1079.4 | **否** | 1080.4 |
| 14565d/…121930_slot1 | (暖场) | 1079.2 | **否** | 1080.2 |
| 14565d/…123424_slot1 | radiant | 1079.5 | **否** | 1080.5 |
| 14565d/…124644_slot1 | **dire** | 1079.4 | **否** | 1080.4 |
| 715cc0/…121931_slot1 | (暖场) | 1079.3 | **否** | 1080.3 |
| 715cc0/…123528_slot1 | radiant | 1079.4 | **否** | 1080.4 |
| 715cc0/…124853_slot1 | **dire** | 1079.4 | **否** | 1080.4 |

**11/11 局,采样格点从 1079.x 直接跨到 1080.x。**
⚠️ 诚实边界:这**只说明离线量具看不见那一点**,不说明引擎 tick 上不存在 `DotaTime()==1080.0`
(1080.0 = 32400/30,在 1/30s 的 tick 上是可表示的)。**但这一点承不承重是算术问题不是采样问题**:
即使某一帧恰好落在 1080.0,armed 也只是**拒绝**线上兵优先支,落回它正下方的打野支 —— 
**每局至多一帧**。

### 2.3 子句的**活域**(两条腿都求值、且逐帧同值)

| armed 侧 | 帧数 |
|---|---|
| radiant-armed | **47,092** |
| dire-armed | **17,670** |
| (暖场,未 armed) | 38,553 |
| **合计** | **103,315** |

⇒ **铁律 4(i-a) 披露:两个分层的读数都在上表,两层同号同形**(都是「子句活着、两腿同值」)。
⛔ 按 4(i-b)/(i-c) 本节**不登记任何效应量** —— 这里报的**不是一个对比估计量**,
是一个**结构性恒等式**(分歧集为零测集),两者不能混。

---

## 三、⭐⭐ 头号发现:一条**已登记的入集依据**被推翻,而且是 2/2

`iterations/state.json` → `cap25_boundary_20260825.domains_unblocked`,逐字:

> a census of gate sites carrying a time constant >= the OLD cap finds exactly two ids whose domain
> was constructively empty and **is now reachable** -- `alchrage` (armed bound 15:00/16:00,
> hero_alchemist + the rubick copy) and `tbearly` (band 18:00-25:00, mode_farm_generic).
> Both are gated, unarmed, and **now proposable for the test set**.

这句话是 GH #108 检查项 7 的产物,也是 `tbearly` 后来入集(§BF,GH #157/#165)所依赖的那句。
**cap 放宽确实落地了**(本轮 `analysis.json` 逐局 `cap_min=25`;W63 全部 **176 局** `duration_s > 1080`,
最小 1106s;本语料 `max sampled t = 1616.3`;**11/11 局的帧都进了 [1080,1500) 这条带**)。
**预言的那个「域解锁」事件真的发生了 —— 而域仍然是零。**

### 3.1 `tbearly`:被**外层合取项**关掉(本轮实测的下坠)

`tbearly_domain.py` 的八级单调级联,11 局合计:

| 级 | 含义 | 帧数 | 相对上一级 |
|---|---|---|---|
| L0 | 全部真英雄帧 | 179,465 | |
| L1 | 开局号角之后 | 171,900 | −4.2% |
| L2 | 存活 | 152,036 | −11.6% |
| L3 | **落在被声称的带 [1080,1500)** | **36,630** | −75.9% |
| L4 | `net worth < 15000` | 22,069 | −39.8% |
| L5 | `deaths < 5` | **12,436** | −43.6% |
| L6 | **`not J.IsLateGame()`(t ≤ 1080)** | **0** | **−100.0%** |
| L7 | 两腿分歧(`t == 1080.0`) | **0** | |

**`12,436 → 0` 这一跌就是全部的发现。** 那条带**不缺帧**:它有 36,630 个英雄帧,
连过完另外两个合取项之后仍有 12,436 个,**11/11 局都够得到**。
**缺的不是时钟,是控制流** —— 读那个常数的**那一行**在这条带上一次也走不到。

### 3.2 `alchrage`:被**抽签池**关掉(同一句里的另一个 id,另一个机制)

`bots/BotLib/hero_alchemist.lua:527` `X.GetRageObjectiveClock`,消费点 `:621` / `:632`。
**alchemist 根本不在批测抽签池里**:`tools/batch_test/soak/hero_pool.txt` 共 47 行
(6 行注释、0 空行 ⇒ **41 个英雄**,与批测台今日报告的「41 英雄的抽签池」对上),
`grep -i alchem` **rc=1 零命中**;本轮 11 局语料的 25 个英雄里同样没有它。
⇒ **在任何一波批测里 `alchrage` 的域都是零**,与它的两个时钟常数无关。

⚠️ 归属克制:本条**只覆盖批测语料**,不主张真实对局(池外)的行为;
`:621/:632` 自带 `IsDoingRoshan`/`IsDoingTormentor` 的窄化本轮**未逐帧核**,不需要 —— 
池这一层已经先把它关掉了。

⭐ **这半条不另立单子**:它与批测台**今天 21:18Z** 的 GH **#726**
(「把『Undying 不在抽签池里』做成断言」)**是同一个机制的第二个实例**,应当并进那条轴。

### 3.3 可迁移的那一句(立法用)

> **一次「时间常数普查」回答的是「时钟够不够得到这个常数」。
> 它没有回答「控制流够不够得到读这个常数的那一行」,也没有回答
> 「承载它的那个单位在语料里存不存在」。三个问题,普查只问了第一个。**

这与 `pullcad` 陷阱(一个 id 被**另一个合取项**冻死)、`campexit` 的顺序事实
(上游消费点先 `return`,下游分支根本不被求值)**同族**:
**可达性有多个互不重叠的关闭机制,而每一次我们只记得检查最近吃过亏的那一个。**
那句普查自己的措辞——「constructively empty and **is now reachable**」——正好把两个问题写成了一个。

---

## 四、建议动作(交给总监裁)

1. **`tbearly` 退集**。它是 P4.2(armed ≤ 20)下**最便宜的一格**:
   域**可证为零测**,退集**不损失任何信息**,也不需要任何波次。
   ⛔ 建议**不要**顺手删代码:改写本身(把析取改成选择)修掉的是一个**真实的 Lua 惯用法缺陷**,
   `tests/test_turbo_ternary_dominance.lua` 的全仓棘轮**继续有价值**;
   要退的是**测试集里那一格**,不是那次修复。
2. **修 `state.json:cap25_boundary_20260825.domains_unblocked` 这一行**,并按 §3.3 记下判据。
   它现在是一句**会被继续引用**的错话(`tbearly` 入集就引用过它)。
3. **`alchrage`**:若在集内,同样退;并入 GH #726 那条轴。
4. **[strategy] 顺带一条**:`mode_farm_generic.lua:551-553` 的出厂注释逐字写着
   「The band this moves is 18:00-25:00」。**那句话是假的**(§3.1),
   而且它正是让下一个读者相信这个 id 有域的那句话。请在同一次改动里改掉。

---

## 五、本轮踩到 / 复现的坑

- **暖场局仍然要手动排**:11 局里 4 局是 `script_version='cfa966e3'`(无 `mirror:` 前缀)。
  `tbearly_domain.py` 的分层表把它们打成 `side unknown`(band 14,019 帧)并**照样计入合计** —— 
  本轮结论不受影响(外层合取项在**出厂腿**上同样成立,且早于本仓库),但**读那张表的人要自己减**。
- **`replays/` 是平铺前缀**,靠 `__<run_id>` 后缀避撞;W63 只有 11 个 `.dem` 上传,
  而 `analysis.json` 有 176 个 ⇒ **「有 analysis 不等于有录像」**,选局要按录像可得性选。
- **先下小元数据再下 `.dem`**:176 个 `analysis.json` 合计几百 KB,先读 `duration_s`/`cap_min`/
  `script_version` 再决定下哪些 228MB 的录像。
- **`jakiro` 在 `201fec/…123438_slot1` t=1078.5 出现三条快照**(hp_pct 0/0/1)——
  幻象/idx 那一族(GH #69/#176)。本轮**不承重**(结论是结构性的),但登记在此。
- ⛔ **自检的管道拒绝门:本轮又撞了一次(全队第 8 次,又是本轮第一条命令)**,
  逐字 `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.` /
  `it has recurred 5x, every time as the first command of the round.`
  **那道门有效**(它没让 reader 的 0 冒充通过),但它**连续八轮拦的是同一个动作**。

---

## 六、铁律 6 与自检

- **自检**:`SELFCHECK_EXIT=124`。⛔ **124 是我自己那条 `timeout 900` 的判决,不是自检的判决** ——
  与批测台 21:18Z 那轮同形。跑完的腿的读数:
  `a-evidence-owed` **`FINDING: 8 armed id(s)`**(开工值,与本轮交付前一致);
  python 腿 **`120 passed, 2 failed, 3 uncertifiable`** ⇒ **TRUNK RED**,两条:
  `tests/test_bots_walk_farm_only.py`(`tests/test_lion_q_field_engagement.lua` 与
  `tests/test_anyhero_first_member_quantifier.lua` 两个新 `io.popen` 没进白名单)与
  `tests/test_py_gate.py`(`5c: the selected total (12.03s) fits the recorded budget (12.0s)`)。
  **两条都已有单子**(#728 逐字就是 py_gate 那条 12.03/12.0;walk_farm_only 属 #718/#650 那一族)
  ⇒ **不开第九张单子**。Lua 检测器那条腿**没跑到**(被 124 截断)⇒ **trunk 的那一侧本轮没人看过**,
  按 GH #171 的词汇:**这不是通过**。
- **铁律 6 三条腿读数**(两次 push 逐字一致,**未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行**):
  - `luacheck bots game: 0 warnings` / **`GATE_EXIT=0  CLEAN (iron rule 6 static half passed)`**
  - `py gate: 95 ran, 0 findings, 0 uncertifiable, 30.5s`(第二次 `30.7s`),
    附带 `11 new test(s) not in the manifest were run anyway: ...`
  - `lua gate: SKIPPED BY SCOPE -- this push touches no bots/game/tests path.`
    ⚠️ **照工具要求原样抄下一行**:`This is a SCOPE decision, not a pass and not a skip of a gate
    that had something to say (GH #624).`
  - Lua 动态半(~100min,GH #124)**未跑,不声称**。
- **push 路径**:`PUSH_BRANCH_EXIT=0`(`* [new branch] claude/lucid-pascal-g1ycs5`);
  `PUSH_MAIN_EXIT=0`(`c28f3c86..8078c4c2  HEAD -> main`,一次通过未被拒)。
- **发表顺序(GH #290)**:草稿先跑 `claim_precheck.sh` ⇒ **第一次 `PRECHECK_EXIT=3`**
  (`MISSING path iterations/reports/replay-check/20260910T220101Z.md` —— **门按设计拦住了**:
  报告当时还没上 origin/main);push 之后重跑 ⇒ **`PRECHECK_EXIT=0`**,
  `paths cited 9 ... resolved on trunk 9 refused 0` / `local commits not on origin/main: 0` / `clean`,
  **然后**才发表。已发表:**GH #730**。

---

## 七、下一轮第一件事

1. ⭐ **W64 若落地**,第一条命令仍是 `pullcamp_camp_gap.py`(连续三轮零发波未执行)。
2. **`a_evidence_*` 还剩 7 条 UNOWED**(本轮 8 → 7),至少再取 1 条。
   ⭐ **选点照本轮的次序做**:**先读工具头部 LIMITS 判「离线读不读得出来」,再买语料**。
   已知的结构性拒绝:`blinkflee`(三处同向 blink 几何不可分)、TP 族(GH #647)。
   下一个候选建议 `tbearly` 的邻居 —— **`abilanc` / `fieldbuy` 头部都自陈了 NOT-observable 合取项**,
   先读完再决定;`lf_rescue` 的工具要求两条腿臂串不同,**先验证本波满足**再下语料。
3. **盯本轮这一单**(§三)+ 上一轮两单(#722/#723)+ #700 + #726。
4. ⛔ **自检永远单独跑,并且不要用外层 `timeout` 掐它** —— 本轮 124 是自己掐的,
   代价是 Lua 那条腿的读数整轮没有。交棒单第 (4) 条补一句:**给自检的超时要大于它自己的预算**。

---

## 八、Token 用量

```
TOKENS total_in=9,494,756 out=57,127 turns=74
```

(`python3 tools/agent/token_usage.py`,统计时刻为本轮收尾前;统计后的收尾回合不计入。)
