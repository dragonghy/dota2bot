# 录像检查组 2026-09-10T18:2x–18:5xZ(无新波轮:取 `a_evidence_*` 欠账一条 = `campvoid`)

**一句话**:`campvoid` 的条件 (a) **买不到,而且不是因为语料不够** —— 它用来替代触发级证据的那个后果代理(`escape`)
在逐帧下是**从营地旁边全速走过去**,**对照腿以 100% 的比例产出同一个几何形状**,而对照腿上这个门**结构上开不了火**。

```
VERIFY id=campvoid verdict=INDETERMINATE episodes=120
```

---

## 〇、本轮定位与交棒单执行情况

上一轮(16:01Z)交棒单第 (1) 条是「**W64 落地后**第一条命令就是 `pullcamp_camp_gap.py`」。
**W64 没有落地**:批测台 18:14Z 那轮是**零发波轮**(闸 (iii) 预算围栏,`Headroom was $1.053, this wave needs $1.100`,
见 `iterations/reports/batch-desk/20260910T181400Z.md`)。⇒ 按章程「每次触发的工作流」第 2 条
(没有新批次 → 挑执行核验记录最少的 id 补课),本轮转做交棒单第 (3) 条:
**`a_evidence_*` 的 9 条 UNOWED 里取一条**,取的是 `campvoid`。

**取它的理由**:它是章程 §4a 唯一逐字写了「(a) 只能按触发级逐帧买」的两个 id 之一,且树上已有专用工具
(`campvoid_escape.py`,判别子先跑:`ls tools/batch_test/behavioral/ | grep -iE 'campvoid|campexit|campfarm'` 三个命中,W58 学费)。

| 层 | 覆盖 |
|---|---|
| **宽扫** | **64/64 局**(W63 四个 run;88 个 `.dem` − 24 个暖场局;四个 run 逐个读到 `unparseable 0`) |
| **深查** | **7 局逐帧**(5 局 armed + **2 局 baseline 阴性对照**),章程下限 ≥6 局 **达标** |
| **新波** | **0**(W63 已于上一轮宽扫过;本轮为补课轮,宽扫是为本 id 重建 timeline) |

**AWS**:只读 S3(2 次 `ls` + 88 局下载),**零 EC2、零发波、零 CE、零支出**。

---

## 一、`campvoid` 条件 (a):INDETERMINATE,三条独立理由

`campvoid`(GH #265)包住 `bots/mode_farm_generic.lua` 里的一个读数:`campfarm` armed 时清空了低等级 bot
在远古营的攻击名单,而**未过滤的 presence 名单仍然数着那些远古**,于是「没东西打、也没理由走」——
`campvoid` 让 `#nNeutrals == 0` 能翻真,把线兵出口**打开**。杠杆是单调的:只能开,不能关。

**armed 确认**:`campvoid` 在 W63 的 arm 串里。证据取自**每一局自己的 `script_version` 戳**
(`sweep_run.sh` 逐局打出 `cand=...,campvoid,... seed=10842 side=radiant`),不是从 `test_set.md` 推的。

### (1) 触发级 (a) 在本语料上**结构性买不到** —— 这是工具自己写的,不是本轮的结论

`campvoid_escape.py` 头部逐字:dumper **不发 creep 实体**(`dumper/main.go` 只保留至少一方是英雄的战斗日志行,
且没有 creep 快照类型)⇒ `GetNearbyNeutralCreeps()` 与 `GetNearbyLaneCreeps(900)` **两个合取项离线都读不出来**。
⇒ 章程 §4a 要的那种触发级证据(「那一帧 `#nNeutrals` **因过滤**变成 0 + 小兵出口**真的被走了**」)
**在当前 dumper 下不可能买到**,与 GH #647 的 TP 家族同族(「要么走纯结构窗口,要么等 dumper 加字段」)。

### (2) 它用来替代的后果代理(`escape`),逐帧下不成立

**这是本轮的新结论**,详见 §二。**对照腿产出同一个形状**,而对照腿上 `campvoid` gate-OFF、**结构上开不了火**。

### (3) 铁律 4(i-b):两层反号 ⇒ 噪声,不写进结论

`escape` 是**侧偏未消除的计数类量**。r=800 上两个物理分层**反号**:

| radius | ab/armed | ab/baseline | ba/armed | ba/baseline |
|---|---|---|---|---|
| 400 | **1** | 0 | 0 | 0 |
| 600 | **2** | 0 | 0 | 0 |
| 800 | **4** | 0 | 0 | **2** |

r=800 反号(ab +4 / ba −2)⇒ 按噪声处理。r=400 与 r=600 的 ba 层是 `0 vs 0`,**零信息,不是支持** ——
一个从不动的对照证明不了它被用过(W62 `ownhalf` 那句话的第三次现场)。

⇒ 三条合起来:**INDETERMINATE**,不是 WORKING 也不是 SILENT。
⛔ **本轮不主张 `campvoid` 是坏的**:上面没有一条说这个门做错了事,说的是**没有一个量具能回答它有没有做事**。

---

## 二、逐帧:被记成 `escape` 的九条,是「路过」

**硬规则:先逐帧后聚合。** 下面的百分比只用来说明**逐帧看到的那个形状有多普遍**。

W63 全部 **9 条 escape**,跨 **7 局**、**两条腿**、**两个远古营**,形状**逐条相同**:
`d_cam` 单调降到一个最小值再单调升(一个 **V**),速度**全程 250–500 u/s 不落地**,box 内**没有一帧静止**。

⛔ **承重帧在对照腿上**(`campvoid` gate-OFF):

`14565d/20260910_124637_slot5` **skeleton_king lvl10**,营地质心 `(-4857, 28)`:

```
 t=586.5 d_cam=2215 v=  0     ← 进场前
 t=590.5 d_cam=2131 v=419
 t=592.5 d_cam=1336 v=516
 t=594.5 d_cam= 673 v=492  <<EPISODE
 t=595.5 d_cam= 583 v=288  <<EPISODE
 t=596.5 d_cam= 691 v=432  <<EPISODE
 t=600.5 d_cam≈1900 v=421     ← 继续走,一帧未停
```

t=601.2 那次白打 `npc_dota_creep_badguys_melee`(`inflictor=dota_unknown`,+84 金)发生在**离营地 ~1500 u 处**。
同腿第二例 `715cc0/20260910_124823_slot6` chaos_knight lvl11 t=594.4→600.4:`717→609→758→509→70→257→685`,
v=250–456,**同一个 V**。

armed 腿逐条同形,举两条:
- `201fec/20260910_122040_slot8` spirit_breaker t=616.3–619.3 `379→73→428→759`,v=307–501;
- `14565d/20260910_123424_slot1` **zuus 与 venomancer 在同一局同一瞬间**(t=771.5–773.5)各记一条 ——
  **两个盟友结伴走过同一个营地记成两条独立 episode**,且哪一个被记下**取决于半径**
  (r=400 只有 zuus 够 3 帧,r=600 只有 venomancer 与 spirit_breaker)⇒ **episode 之间并不独立**。

**聚合(r=600,全部 120 条 episode)**:

| cell | n | box 内出现过 v<100 u/s | 单调 V(一次性穿过) |
|---|---|---|---|
| ab/armed | 59 | 14 (23.7%) | 54 (**91.5%**) |
| ab/baseline | 27 | 1 (3.7%) | 27 (**100%**) |
| ba/armed | 18 | 6 (33.3%) | 14 (77.8%) |
| ba/baseline | 16 | 3 (18.8%) | 16 (**100%**) |

全体 **109/120 = 90.8% 一次性穿过**;min-speed 中位 **268 u/s**;只有 **20.0%** 曾低于 100 u/s。
⭐ **两个 baseline cell 都是 100%** —— 这个形状**不是门造出来的**。

**根因**:episode 定义里**没有驻留(dwell)谓词**。工具头部已逐帧钉死过三种假阳性(普通营地交战记成 stuck /
跨图 TP 记成 escape / 法术溅射记成 escape)并为此加了可达上限与「必须是白打」两道闸,
**第四种没被盖住:全速路过**。而 GH #265 拍到的那个僵局形状是「19 秒内六次穿过仇恨半径、自身零伤害」——
**一个滞留形状**,不是一次穿过。

---

## 三、⚠️ 本轮自己中的一发(判别子极便宜,我第一遍跳过了)

第一版速度脚本**没走 `entities.frames_by_hero`**,于是 chaos_knight 读出 `5849 u/s / 位移 11697 u`,
**看着像工具把一次跨图 TP 记成了 escape**(即工具头部假阳性 #2 的复发)。

**那是我的脚本的产物,不是工具的**:CK 是幻象英雄,该过滤器在本语料里丢掉 **88 个 chaos_knight 实体**;
走过滤器之后三条 CK episode 全部是 §二 那个规矩的 V。**工具是对的,量它的人是错的。**

⭐ **可迁移**:与 W58「写 `<id>_domain.py` 前先 `ls | grep <id>`」同族 ——
**判别子一秒钟,跳过它就会去追一个不存在的因**,而且这一次追的方向是**去指控一个正确的工具**。
`campvoid_escape.py --selfcheck` **`CV_SELFCHECK_EXIT=0`(20/20 PASS)**,本轮开工时就跑过,
**那本该是我不去指控它的第一个理由**。

---

## 四、另一条(与本 id 无关,顺手量到的):`a_evidence_route.py` 的语料读数 29/40 是瞎的

取 `campvoid` 时,九条 UNOWED **逐条同一个数** `waves 10 last W58` —— **那个整齐本身就是判别子**。

```
wave records total : 40
parseable          : 11  ['W39','W40','W41','W51'..'W58']
SILENTLY SKIPPED   : 29  ['W24'..'W38','W42'..'W50','W59','W60','W61','W62','W63']
```

`arm_string` 是批测台**有的轮次写、有的轮次不写**的可选字段,而 **W59 起连续五波都没写**
⇒ `last_wave` **结构性冻结在 W58**,只要发波记录模板不变**永远不会前进**。
而工具的拒绝闸是**全有全无的**(`waves_parsed == 0` 才 `return 2`),**11/40 照样 exit 0**。

**真值可零成本恢复,两个独立来源**:(i) W60–W63 四条记录的 `arm_md5` 与 `test_set.md` 第 2 行**今天的 arm 串逐位相同**
(`b525d51d4b4957e0e40f22f203aea641`);(ii) 每一局自己的 `script_version` 戳(本轮宽扫逐局读到)。

**失效方向**:今天错的是**低估**(良性侧),但已被引用 —— GH #540 的总监评论(comment 5600262893)逐字写着
「每条都在 10 波的 arm 串里活过(**最近 W58**)」。⛔ **危险侧已装好未发**:`classify()` 把 `waves == 0`
判成 `NO-CORPUS`(「从没在任何一波里活过」)⇒ **W59 之后才入集的 id 会被判成 (a) 买不到**。
本轮现算:今天 `NO-CORPUS 0` **不是闸拦住的,是 W58 之后只有退集没有入集**
(W58 的 42 条 ⊇ 今天的 37 条,`current − W58 = ∅`)⇒ **P4.2 的入集冻结正在替这个缺陷挡子弹。**

---

## 五、本轮的 issue(净增 2 条,两份草稿各跑一次 `claim_precheck.sh`)

先搜后开(GH #675 学费):`campvoid` / transit / arm_string 三组关键词 + 逐条扫 40 条最新 open issue,
**无同题单**。两份草稿 precheck 均 `EXIT=0` / `local commits not on origin/main: 0` / `clean`;
按 GH #290 **先 push 产物,再发表**。

1. **`[bug]`** —— `campvoid_escape.py` 的 `escape` 列量的是路过(§二)。
   ⭐ 与 **GH #700**(「请给『域内计数 + 带对照』类检测器强制配一条 placebo 带」)**同族**,
   而本轮就是那条 placebo 自己走出来的:**对照腿以 100% 比例产出同一个几何形状** ⇒ **这是 #700 那条 ask 的验收语料**,不是又一张单子。
2. **`[harness]`** —— `a_evidence_route.py` 语料读数 29/40 瞎,拒绝闸只在 40/40 全瞎时才响(§四)。

---

## 六、条件 (a) 台账更新

`campvoid` 本轮取得判决(`VERIFY` 行) ⇒ `a_evidence_owed.py` 的 **UNOWED 9 → 8**
(开工读数 `A-EVIDENCE-OWED armed 37 verdict 28 owed-row 0 UNOWED 9`,`AEV_EXIT=3`)。
⛔ **不开 `owed_executions.json` 登记行**:登记行是「没有人认领时发生的事」,本轮**认领并交付了判决**,
再补一行等于让登记表自己结自己的案。

**剩余 8 条 UNOWED**:`abilanc` `blinkflee` `fieldbuy` `lf_rescue` `tbearly` `tpcommit`(DELIVER)/
`aimguard` `overchase`(MENTION)。

---

## 七、铁律 6 与自检

(收尾时填写,见文末补记)

---

## 八、下一轮第一件事

1. ⭐ **W64 若落地,第一条命令仍是 `pullcamp_camp_gap.py`**(上一轮交棒单第 (1) 条,本轮因零发波未执行;
   n=3 仍是那条结论唯一的软肋,工具全在树上,零 AWS);
2. **`campfarm` 的触发级逐帧**(章程 §4a 与 `campvoid` 同族;`campfarm_target.py` 在树上)——
   ⚠️ 注意 §二 的形状**同样适用于 `campfarm` 的营地 episode**,先给它配驻留谓词再读数;
3. `a_evidence_*` 还剩 **8 条 UNOWED**,**至少再取 1 条**(连续第二轮有取);
4. 盯本轮两张单子 + GH #700(本轮给了它验收语料)。

---

## 九、Token 用量

(收尾时填写)
