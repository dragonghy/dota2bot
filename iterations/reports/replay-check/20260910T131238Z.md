# 录像检查组 2026-09-10T12:31–13:12Z — `pullcamp` 的条件 (a):机制 WORKING,选点 BUGGY

**一句话**:本轮回到宽度(**宽扫 88/88 局,W62 四个 run 全覆盖,unparseable 0**;深查 **6 局**逐帧),
把连续四轮欠着的 `a_evidence_*` 队列取了一条 —— **`pullcamp`**(同时是 owner **P1 第 3 棒**、
`a_evidence_owed.py` 的 **11 条 UNOWED 之一**)。读数分两半,而**两半的方向相反**:
**拉野这套机械在真实对局里跑通了**(逐帧看到走位→仇恨→扛伤→回拖→接线),
**而它挑的营地有 38.5% 结构上不可能连上兵线**。

```
VERIFY id=pullcamp verdict=BUGGY episodes=35
```

⚠️ `episodes=35` 是**归属修正后**的 armed 腿 poke episode 数(工具原样打 39,见 §5.1),
不是帧数;`BUGGY` 指的是**执行了但行为不正确**,不是 SILENT —— 执行那一半的证据在 §3。

⭐ 另有一条与本单元无关但当场撞上的:**main 今天上午被三个连续工作单元各弄红一条 ratchet**,
其中一条是**本组自己 09:54Z 那轮的**,本轮已修并已 push(§6),开了 **GH #711**。
抓到它的判别子比三条红更该被读:**上一轮的「假红」结论是用一个空跑的 A/B 买的**。

---

## 0. 覆盖(章程固定节)

| 层 | 数字 |
|---|---|
| 宽扫 | **88/88 局**(W62 四个 run:`…364764` 26、`…7eb1ba` 18、`…53d28c` 26、`…5313f5` 18),`unparseable=0`,四份 `sweep_complete.json` 齐 |
| 深查(逐帧) | **6 局**(5 局 armed 腿 + **1 局 baseline 腿做阴性对照**),全部可复跑 |
| 每 id 触发计数表 | 通用检测器 13 条按 **ab/ba 分层**重打,见 §2;`pullcamp` 专用域扫描见 §3–§4 |
| AWS | 只读 S3(4 次 `ls` + 88 局 `.dem`/`analysis` 下载),**零 EC2、零发波、零 CE、零支出** |

W63 12:18Z 起飞,本轮开工时**零局落地**,故本轮语料仍是 W62(与上一轮同一批,但**问的是另一个 id**)。

## 1. 为什么是 `pullcamp`(三条线在这一条 id 上重合)

1. **铁律 9 / owner P1**:P1 DoD 第 3 条逐字是「一个带申报目的的批测波次买到条件 (a):
   录像组在真实对局里核验到勾线/拉野实际执行(WORKING,帧证据)」。
2. **章程交棒第 (4) 条**:`a_evidence_*` 队列**连续四轮未取**,自检 `FINDINGS` 里的
   `a-evidence-owed` 点的就是本组的义务。`a_evidence_owed.py` 本轮读
   **`armed 37 / verdict 26 / owed-row 0 / UNOWED 11`**,`pullcamp` 是 11 条中的一条,
   且工具给它点名了三把现成的尺子(`pullcamp_domain.py` / `pullcamp_frames.py` / `pullcamp_quality.py`)。
3. **它的 verdict=0 是记法的产物,不是没人看过**:2026-08-23T09:09Z 本组就把它记成 WORKING,
   但那在 `VERIFY` 行约定(2026-08-30)之前,散在散文里 ⇒ `verify_coverage.py` 读作 0。
   **而这不只是补一行字**:那次读数取自 **283 局的 21-id 波**,之后
   `pulllane`(§4 贴路子句)与 `campbind`(戳点绑定)**双双退集**,`pullcamp` 的实际行为
   已经不是当时那一个(test_set.md 第一节逐字警告过:`tLanePath = nil` ⇒
   `J.IsCampBesideLane` 首行 `return true` ⇒ 逐字节 no-op)。**W58 及更早不与本读数并池。**

⚠️ **先读档案再动手**(W58 学费):`ls tools/batch_test/behavioral/ | grep pullcamp` 三把尺子全在,
本轮**一把新的都没写**;新增的那一个(§5.2)是**通用的分层器**,不是第四把拉野尺。

## 2. 宽扫层:13 条通用检测器,按 ab/ba 分层(铁律 4(i))

`sweep_strata.py`,88 局,**ab(armed=radiant)53 局 : ba(armed=dire)35 局**
(GH #686 的失衡在本波读作 **1.51**,与上一轮四个 run 的 2.25/1.57/1.00/1.57 一致)。

| detector | r_ab | r_ba | **arm** | side | neg/n |
|---|---|---|---|---|---|
| `tp_under_threat` | −1.134 | −1.294 | **−1.214** | +0.080 | 4/4 |
| `missed_cs_at_tower` | −0.255 | −0.184 | **−0.219** | −0.036 | 4/4 |
| `laning_past_midline_death` | −1.295 | +0.781 | −0.257 | −1.038 | 4/4 FLIP |
| `overextend_alone` | −0.675 | +2.253 | +0.789 | −1.464 | 0/4 FLIP |
| `died_with_ult_ready` | +0.456 | +0.084 | +0.270 | +0.186 | 1/4 |

**只有前两行两层同号**(4(i-b):FLIP 行是侧偏未消除的估计量,登记不结论)。
⛔ **LIMIT 1 逐字适用**:一波 arm 的是 37 个 id,`arm` 是**整束读数**,
不是任何单条 id 的效应量 —— 上表**不能**用来给 `pullcamp` 定价。

## 3. `pullcamp` 的执行那一半:**WORKING**,而且两个物理侧各有一次教科书拉野

`pullcamp_domain.py --selfcheck --any-camp`,88 局,**11 条先验全 PASS**
(泉水间距 18475、营地复现 worst 66u、两腿局数 440:440、拉野窗 duty 0.500、
28 个营地由 t∈[60,66] 首次刷新推出);位移物理守卫 **max 1405u(上限 1980)**,
345/6406 域内帧按 TP/死亡窗剔除。

| metric | armed | baseline |
|---|---|---|
| poke episodes | **39**(归属修正后 **35**,§5.1) | 7 |
| drag episodes | **16** | 4 |
| connect_own episodes | **3** | 1 |
| strict 域 episode / 局 | 5.80 | 4.64 |

**分层重打**(`domain_rows_strata.py`,每粒种子先 swap-average、再跨种子算术平均,
**绝不按局加权** —— 4(i-d)):

| 签名 | seed 10601 | 10607 | 10803 | 10813 | **arm 均值** | sd | 同号 | 按局加权会读成 |
|---|---|---|---|---|---|---|---|---|
| poke | +0.292 | +0.565 | +0.385 | +0.279 | **+0.380** | 0.132 | **4/4** | 0.359 |
| drag | +0.174 | +0.305 | −0.038 | +0.162 | **+0.151** | 0.142 | 3/4 | 0.142 |
| connect_own | +0.028 | +0.045 | −0.038 | +0.071 | **+0.027** | 0.047 | 3/4 | 0.025 |

**poke 那一行八个分层读数无一为负**(4(i-a) 两层都登记:`r_ab` 0.333/0.273/0.231/0.273,
`r_ba` 0.250/0.857/0.538/0.286)⇒ **执行差分是真的,且不是侧偏**。

### 3.1 帧证据(一),radiant-armed:`…7eb1ba/20260909_212625_slot7` silencer(pos5)

```
t=224  d(camp) 568   ← 走进营地
t=226  d(camp)  82   hp 1.00→0.94   DAMAGE 森林巨魔/狗头人 → silencer   ← 仇恨拉上
t=228  d(camp) 265   hp 0.85        ← 往家走
t=229  d(camp) 499   6 只小野在 700 内,foll→wave 1651
t=232  d(camp)1046   hp 0.76        foll→wave  507   d(wave)  50   ← ⭐ 接上了
t=235                               foll→wave   79   DAMAGE silencer→狗头人(在兵线上)
```
**走位→仇恨→扛伤(−24pp)→回拖→把小野交进自家兵线**,全程 1800 圈内 **0 个敌人**,0 死亡。
开戳时刻 `226 % 60 = 46` ∈ [35,50],**落在设计的 :42 拉野刻上**。

### 3.2 帧证据(二),dire-armed:`…5313f5/20260909_215241_slot3` lion(pos5)

```
t=137.5 d(camp) 231   6 只小野
t=138.5 d(camp)  77   hp 1.00→0.93  DAMAGE 狗头人→lion,lion 反手 impale   ← 仇恨
t=141.5 d(camp) 706   4 只跟着       foll→wave 1161
t=143.5 d(camp)1053   2 只跟着       foll→wave  224   d(wave) 292   ← ⭐ 接上了
```
开戳 `138.5 % 60 = 18.5` ∈ [5,20],**落在 :12 那一刻**。
⇒ **两个物理侧各一次完整拉野**,4(i-a) 在帧一级也满足。

⚠️ **窗口本身不是强证据**:两个拉野窗合起来占时钟的 **50%**(工具的 duty selfcheck 读 0.500),
所以「戳在窗内」几乎是白给的。**能定案的是形状**(接近→受伤→回拖→交线),不是时刻。

## 4. `pullcamp` 的行为正确性那一半:**BUGGY** —— 38.5% 的拉野结构上不可能连上

`pullcamp_quality.py`(armed 腿 39 个 poke episode):

- **WHERE**:开戳点离自家泉水 **中位 10820u**(全图 18876u),**>9000u 的 25/39 = 64.1%**;
  离敌方泉水更近的 **0/39**(「绝不进敌方野区」那条 scoped non-goal 成立)。
- **COST**:开戳后 15s 掉血 **中位 12pp / p90 28 / max 53**;掉到 35% 以下 **2/39**;
  20s 内死亡 **0/39**(邻近计数,不是归因)。⇒ **比 2026-08 那两波便宜得多**(当时中位 19pp、1.7% 死亡)。
- **CONNECT**:两种估计量**一致** —— **4/16 拖拽 = 25.0%**;
  跟随小野离最近线兵最近处 **中位 1225u**。
- **EDGE CONTROL(章程要求的边界对照)**:跟随小野**离营地最远只走 中位 826 / p90 1048 / max 1120u**
  ⇒ **低连接率是拖拽本身的物理上限,不是估计量看漏了**。

### 4.1 ⭐ 本轮最该被下游读到的一条:营地离**任何**一条兵线路径的垂距

用 `tools/agent/pullcamp_lane_geometry.py` 的地图(**68 个 fixture 的塔坐标逐个一致**)对
每个 armed poke episode 的营地算**到三条兵线的最小垂距**(取最小 ⇒ **对「bot 被分到哪条路」不敏感,
是下界**):

| | 中位 | p90 | max | **>1200u(`IsCampBesideLane` 对任何一条路都会拒)** |
|---|---|---|---|---|
| armed | **1200** | 2334 | 2914 | **15/39 = 38.5%** |
| baseline | 1200 | 2173 | 2914 | 2/7 |

**跟随小野的最大行程是 1120u**(§4 的 EDGE CONTROL,与 GH #117 立 `PULL_CAMP_LANE_GAP=1200`
时那个「观测到的最长拖拽 1170u」逐位同量级)⇒ **那 15 次拉野在开戳的瞬间就已经不可能连上**。
它们不是更差的拉野,**是不是拉野**,而每一次要价约 12–25pp 血。

### 4.2 ⛔ 而同一张表**反对**「把 `pulllane` 原样重新入集」

三次连上的 episode,它们营地的**最小垂距是 `1268 / 1268 / 1200`** ——
**两次在 1200 之上**。`IsCampBesideLane` armed 之后会**拒掉那两次**(垂距取的是下界,
真实垂距只会更大 ⇒ 结论是单向的)。
GH #117 的总监裁定**预先登记过这个失效形态**逐字:「把它们过滤掉,`pulllane` 就从
『丢掉不可能的拉野』变成『丢掉所有拉野』,而那个读数与 SILENT 逐字节相同」。
**本语料显示的正是那一侧**:armed 之后 39 → 24 次 poke(−38.5%),而 3 次连接里丢掉 2 次。

⚠️ **n=3,别当成常数标定**:这一条只主张**「常数没有被本语料证成」**,
不主张「1200 太小」应改成多少。**能定案的路是加大 n,不是加大常数**。

### 4.3 帧证据(三),不可能的拉野:`…5313f5/20260909_214027_slot5` crystal_maiden(pos5)

```
t=64.4  d(camp)1182  hp 0.94      最近线兵 2010u
t=75.4  d(camp) 238             DAMAGE 小野→CM          ← 仇恨
t=76.4  d(camp) 118  hp 0.89
t=79.4  d(camp) 578  hp 0.78      最近线兵 2460u  跟随小野→线兵 2376u
t=81.4  d(camp) 860  hp 0.73      最近线兵 2917u  跟随小野→线兵 2764u
```
**整段里最近的线兵从没进过 2400u**,而小野最多走 1120u ⇒ **这次拉野从第一帧起就没有出口**;
账单是 **−25pp 血**、17 秒的对线缺席。

### 4.4 帧证据(四),缰绳断在半路:`…7eb1ba/20260909_214054_slot6` silencer

```
t=282.5 d(camp) 121  hp 0.96 → t=284.5 hp 0.79   ← 扛伤
t=287.5 d(camp)1104  4 只跟着  foll→wave 1067
t=288.5 d(camp)1370  1 只跟着  foll→wave 1067   ← 只剩 1 只,缰绳到头
t=291.5              3 只      foll→wave 1032
```
**跟随集合在 1032–1067u 处停住**,与 §4 的 `max 1120u` 是同一堵墙。

## 5. 两条仪器级发现(都便宜,都可迁移)

### 5.1 `POKE` 把「在营地边打敌方小兵」记成了戳营

`pullcamp_domain.py` 的 POKE = 「AT_CAMP 时对**非英雄**单位造成伤害」,而 `dmg_creep`
**同时含线兵**。逐帧查:`…53d28c/20260909_220414_slot6` pudge **连续四个 episode**
站在营地 149u 处**与敌方远程小兵对拼**(`DAMAGE pudge→npc_dota_creep_goodguys_ranged`),
**一次都没碰过小野**。全语料归属:**armed 39 → 35 条带真小野目标,4 条纯线兵,且四条同属这一局一个英雄**;
**baseline 7 → 7 全带小野**。⇒ 修正后的读数是 **35 : 7**,方向不变,量级动约 10%。

**判别子是现成的、别再发明**:`campbind_poke.py` 早就写着「目标名必须
`npc_dota_neutral_*`,**不是 `dmg_creep`,那个把线兵混进来**」。
本轮把这段量出来的事实**写进 `pullcamp_domain.py` 的 POKE 条目**(不改算法,
否则此前所有波的读数不可比),下一个读者不必再推一遍。
与 W45 `zusult`、GH #491 同族:**「域内计数」在没有归属之前不是读数**。

### 5.2 新工具 `domain_rows_strata.py`:任何 `<id>_domain.py` 的 rows 都能按 ab/ba 重打

`sweep_strata.py` 只服务宽扫表(`all_findings.jsonl`),而**买条件 (a) 的是各 id 的域工具**,
它们一律只打一张**池化**的 `armed vs baseline` 表 —— 那正是 4(i-a) 禁止单独读的东西。
新工具吃域工具**已经写好的 rows 文件**(`episodes()` / `load_sweep()` 直接 import,
不重写,避免两边对「什么是一个 episode」漂移),按种子分层、swap-average、跨种子算术平均,
并**把「按局加权会读成多少」一起打出来**(本轮 0.380 vs 0.359,系数 0.205)。

- 钉在 `tests/test_domain_rows_strata.py`(6 组 19 条断言,`TEST_EXIT=0`,**不经管道测的退出码**)。
- **变异台两发全中**:M1 删掉 `- baseline` 减法 ⇒ **12 条 FAIL**;
  M2 把 swap-average 换成按局加权 ⇒ 断言当场读出 **1.25**(文档里预言的那个数)。
  两次 `RESTORED_EXIT=0`。
- ⭐ **第 3 组用例在第一版上就抓到了本文件自己的语料缺陷**:两条腿用了同一个英雄名,
  于是 `episodes()`(按 (sweep, game, hero) 归并)把两腿合成一条链,
  「加了 baseline episode」**读不出任何变化** —— 与 W62 `ownhalf` 变异台那条
  「**一个从不动的对照,证明不了它被用过**」是同一句话,这次是**在写测试时**踩到的。

## 6. 撞上的:main 今天上午红了三条,一条是本组的(已修已 push)

开工自检(12:31Z,干净树 ⇒ **工作树红就是 main 红**)报三条:

| 红 | 落它的 commit | 组 |
|---|---|---|
| `tests/test_cm_ult_reach_meter_domain.lua` | `31a9bf1e` | **本组 09:54Z** |
| `tests/test_corpus_scale.lua` | `2cb556d9` | 协同组 10:36Z |
| `tests/test_bots_walk_farm_only.py` | `980b6df0` | 英雄组 11:02Z |

本组那条已修:`f717bd86` —— `instants 65→66 / handles 285→289 / trained 266→269 /
pre 209→212 / post 193→196`,**该节存在的意义那个数没动:212−196 = 16,第六次**。

⛔ **它上一轮就该修,没修的原因是一个空跑的 A/B**:上一轮把 fixture 移走/放回各跑一次,
两次都读 `EXIT=0`,于是把红判成并发写树的假红。
**`lua5.1 tests/test_X.lua` 只定义那张 `tests` 表就退出,一条断言都不执行**;
能打出红的只有 `lua5.1 tests/run_tests.lua <basename>`(自检 `routine_selfcheck.sh:566` 跑的那条)。
本轮实测:直接跑**无输出 EXIT=0**,走 runner **2 failures EXIT=1**。
⇒ **上一轮的「无罪」不是判断错了,是量具结构上答不出「有罪」。**
这条已写进 `tests/test_cm_ult_reach_meter_domain.lua` 的注释里(不靠记忆传)并开了 **GH #711**。

⚠️ 顺带修正本组上一轮的一句归因:那 7 条「假红」里**至少有 1 条是真的**(就是本组自己这条),
其余 6 条本轮**未复验**,不认领也不辩护 —— 判别子已交出,谁复跑谁知道。

## 7. 本轮的 issue

- **GH #711 `[bug]`(新开)**:三条 trunk red + §6 的判别子;两条交棒(协同组 `test_corpus_scale`、
  英雄组 `test_bots_walk_farm_only`)各带验收命令。发前 `claim_precheck.sh` 读
  `PRECHECK_EXIT=0` / `local commits not on origin/main: 0` / `OK to publish`。
- **GH #712 `[bug]`(新开,交总监)**:owner **P1 第 3 棒交付** + 请裁 P1 DoD 第 4 条;
  含 §4.2 那条**反对原样重新入集 `pulllane`** 的读数,以及 #475 §5.2 提过、至今未改的
  `OWNER_PRIORITIES.md` P1「当前球在:协同组(1)」**已陈**一行。

## 8. 门(铁律 6)

- `bots/` + `game/` **一行未改**(本轮改的是 `tests/` 与 `tools/`)。
- `luacheck bots game: 0 warnings` ⇒ **`GATE_EXIT=0`**,**未用 `RULE6_BYPASS`**;
  py gate `92 ran, 0 findings`(逐字限定 `84 fast python ratchets`)。
- `lua5.1 tests/run_tests.lua test_cm_ult_reach_meter_domain.lua` = **0 failures**;
  `python3 tests/test_domain_rows_strata.py` = **0 failed**。
- 动态全量套件(~100min,GH #124)**未跑,不声称**。
- 自检本轮 **`EXIT=124`(900s 超时)** —— 拿到的是**部分**结果:锚点 6/6 OK、`FROZEN none`、
  5 个原子全 GATED、trunk 三条红(§6);Python 腿的 9 条子检查打 `UNCERTIFIABLE`。
  ⚠️ **归因是本台负载**(四个并行 sweep 占满 4 核),**与上一轮同型**;
  ⇒ 章程交棒第 (5) 条要扩写:**别把自检和任何重活(变异台 / 宽扫)放在同一段时间跑**。

## 9. 下一轮第一件事

1. ⭐ **W63 的局(12:18Z 起飞)落地后先跑宽扫** —— 那是 37-id 串的第二波,
   `pullcamp` 的 §4.1 垂距分布**可以直接复算**(工具已在树上,零新代码),
   把 `>1200u = 38.5%` 与 `连接营地 1268/1268/1200` 这两个数**在独立语料上复现或推翻**。
   n=3 是本轮唯一真正的软肋。
2. **把 §5.2 的分层器推广**:`stayattr_domain` / `slotwait_domain` / `tbearly_domain` 等
   同族域工具的 rows 都能直接喂进去,**一条命令补上它们全部欠着的 4(i-a)**。
3. **`a_evidence_*` 队列还剩 10 条 UNOWED**(`abilanc` `blinkflee` `campfarm` `campvoid`
   `fieldbuy` `lf_rescue` `tbearly` `tpcommit` `aimguard` `overchase`)——
   本轮取了 1 条,**连续四轮零取的记录到此为止**;下一轮至少再取 1 条。
4. 盯 **GH #711 / #712**;#686(失衡)本轮读作 53:35 = 1.51,与上一轮一致,不另发言。

## 10. token 用量

```
TOKENS total_in=24,983,587 out=87,584 turns=153
```
(统计时刻为止;此后收尾回合不计入。)
