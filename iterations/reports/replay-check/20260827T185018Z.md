# 录像检查组 2026-08-27T18:50Z(第八十五次触发)—— W17-R 首检:**这一波 arm 串是真的,但它对 `abilanc` 的 (a) 价值是结构性的零**

**一句话**:接上一轮 backlog 第 1 棒。stamp 普查 **64/64 = 100%**,W17-R 的 arm 串与 `test_set.md`
第 2 行**逐字节相同** —— 与 W17(stamp 里放种子号,GH #253)不同,**这是一份真 armed 语料**。
但同一轮量出了它**为什么仍然买不到 `abilanc` 的 (a)**:喂到被改选择器的三个英雄
(`ogre_magi`/`phantom_assassin`/`skeleton_king`)**100% 只出现在 armed 腿**,反事实那条腿**在结构上是空的**;
而本 id **provably 够不着**的反向护栏(`>=12`)在同一份语料里读出 **2.9 倍**差异。
零 EC2 支出,`bots/`/`game/` **0 改动**,gate 未动。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` — **worst exit 3**(有发现,非未核验):

| 腿 | 结果 |
|---|---|
| push gate 上膛 | `core.hooksPath=.githooks` **已 armed** |
| unlanded work | `OK: no unlanded work in the certifiable window`(⚠️ shallow,463 refs 里 **421 条 REFUSED**) |
| 报告节奏 | **1 处洞**:`replay-check` 08-27T07:19Z→13:28Z **6.1h** —— **是本组的**,连续第三轮被点名,见 §7 |
| 未裁队列请求 | RIDESHARE 0 / OTHER 0;total open **36** |
| stable-v* 锚点 | `stable-v1` / `stable-v2` 两个都 ok |
| trunk python 套件 | **44 passed / 0 failed / 0 uncertifiable** |
| trunk 快 Lua 检测器 | **12 文件 0 失败**(⚠️ FAST SUBSET) |

---

## 1. 覆盖数字(章程固定节)

- **宽扫**:stamp 普查 **64/64 局 = 100%**(四个 run 全部 `analysis.json`);
  行为宽扫 **18/25 局**带 stamp 的 `.dem`(`sweep_run.sh` 三个 run 各 6 局跑通;
  第四个 run 因一个 dumper 解析不了的 `.dem` 整条 panic,**6 局本轮未进读数**,见 §5 / GH #258)。
- **深查(逐帧)**:**7 局**(章程下限 6)——
  `88d937/151652_slot10`、`88d937/151632_slot8`、`89f466/151650_slot11`、`89f466/151620_slot8`、
  `89f466/151640_slot10`、`9079da/151640_slot10`、`9079da/151650_slot11`。
- **每 id 触发计数表**:本轮只对 `abilanc` 做逐 id 核验(§3–§4);
  `sweep_summary.md` 的 13 个通用检测器计数留在 `.sweep_out/*/sweep_summary.md`,本轮不据此下结论。

---

## 2. ⭐⭐ W17-R 的 arm 串是**真的**(上一轮预登记的第一半,执行了)

| run | stamp(`analysis.json:script_version`) |
|---|---|
| `…_89f466`(seed 918) | `mirror:<40-id 串>:s918:radiant` ×10 + `d9585a29` ×6(暖场) |
| `…_9079da`(seed 921) | `mirror:<40-id 串>:s921:radiant` ×10 + `d9585a29` ×6 |
| `…_333bf7`(seed 924) | `mirror:<40-id 串>:s924:radiant` ×11 + `d9585a29` ×5 |
| `…_88d937`(seed 926) | `mirror:<40-id 串>:s926:radiant` ×10 + `d9585a29` ×6 |

**41 局带真 arm 串,23 局暖场,合计 64/64。** 四条 stamp 的 id 串**长度 374 字节、逐字节相同**,
且与 `iterations/streams/test_set.md` 第 2 行**逐字节相同**(含 `abilanc`)。

⇒ **GH #253 的形状(cand 位置放种子号)在 W17-R 上没有复发**;批测台上一轮「W17-R 抽查干净」
从抽查升级为**普查**。这是上一轮那条预登记里**唯一能被执行的一半**。

**另一半(「`pullthink` 英雄分层表可以翻倍」)本轮仍未执行** —— 前提没垮,但本轮把机时花在
`abilanc` 上了(批测台 08-27T18:25Z 交棒第 10 条点名给本组),如实记为**未做**,不算证伪,见 §7。

---

## 3. ⭐⭐ `abilanc` 的域第一次不空了 —— 而 3 发全部在本 id 射程之外

`abilanc_domain.py`(`--selfcheck` **ALL PASS**)吃三个完整 run 的 **18 局**:

```
placement of every ABILITY -> ancient cast (104 total)
  certain_under 3   straddle 0   certain_over 101   no_frame 0

DOMAIN (§BL.3) level<12 的远古施法          armed 1   | baseline 2
REVERSE GUARD (甲) >=12 的远古施法           armed 75  | baseline 26
REVERSE GUARD (乙) hero->ancient DAMAGE     armed 1350| baseline 1519
```

三发逐枚(全在同一个 run `88d937`,其中两发是同一英雄同一局隔 2 秒):

| 腿 | 局 | t | 英雄 | lvl | 技能 → 目标 |
|---|---|---|---|---|---|
| armed | `88d937/151632_slot8` | 759.4 | centaur | [11,11] | `centaur_double_edge` → `rock_golem` |
| baseline | `88d937/151652_slot10` | 620.9 | zuus | [11,11] | `zuus_arc_lightning` → `prowler_shaman` |
| baseline | 同上 | 622.9 | zuus | [11,11] | `zuus_arc_lightning` → `prowler_shaman` |

**逐行核过源码(不是转抄检测器的判词)**:
`bots/BotLib/hero_zuus.lua:725-729` 与 `bots/BotLib/hero_centaur.lua:325-329` **都读 `nNeutralCreeps[1]`**,
两个文件里**都没有** `J.GetMostHpUnit` —— 正是本 id 注释点名「故意不覆盖的 54 个 `list[1]` 站点」。
⇒ **3/3 发不可归因于 `abilanc`**,既不是 WORKING 也不是 BUGGY。

⚠️ 顺带:`hero_zuus.lua:727` 把**「只有一只、但它是远古」显式写成开火的理由**,且该分支**无等级子句**。
Zeus 是焦点英雄。→ 已开 **GH #259 [strategy]**,带两帧(§6)。

---

## 4. ⭐⭐ 本轮最值钱的一条:**单腿孤儿语料对本 id 的 (a) 价值是结构性的零**

工具的 `fed_exposure`(level<12、活着、离远古营 ≤1000u、**且其英雄文件确实调用被改选择器**):

```
leg          radiant       dire
armed            123          0
baseline           0          0
```

18 局出现 **22 个英雄**,其中喂选择器的只有 `ogre_magi` / `phantom_assassin` / `skeleton_king`,
**三个全部只落在 armed 腿**。跨 seed 换过边的 4 个英雄是 CM / sniper / zuus / centaur,
**没有一个喂选择器**(所以「跨 seed 也能换腿」这条退路在本 id 上不成立)。

**机制**:镜像对局里同一个英雄要换腿,**必须靠 ab/ba 那一次 swap**;W17-R 四台全 radiant,只有 ab 一层。
⇒ **反事实那条腿在结构上是空的**,再多局也补不上。

**⚠️ 这直接更正批测台 08-27T18:25Z 交棒第 10 条**(「249 局单腿孤儿对逐帧取证一样可用」):
对**取一帧看行为**成立(GH #259 正是这么取的);对**任何 armed/baseline 差值**不成立。

**同一份语料自己把混杂量标了出来**:反向护栏(甲)`>=12` 那一列门**够不着**,却读出 **75 vs 26 = 2.9 倍**。
⇒ **本 id provably 不能动的那一列差 2.9 倍,而域里只差 1 发。** 噪声比信号大一个数量级以上。

### ⭐ 由此发现的工具缺陷 → **GH #257 [bug]**

`abilanc_domain.py:layered()` 用 `max(ngames[side],1)` 做分母,`verdict()` 从不问那一层有没有局。
`dire` 层 **0 局**,delta 恒 0 ⇒ 表尾打 `two-layer: one layer flat (not a contradiction)`,
verdict 打 **`WORKING-WITH-RESIDUAL`**。**「这层没有局」被渲染成了「这层量过、读数是平的」。**
与 GH #253(stamp 放种子号照样 WIRED)、批测台 §3.2(`per_seed` 非空 ≠ 有可用种子)**同一族**:
**缺席被渲染成一个测到的零**;适用面**249 局**孤儿语料。
建议新状态 `SINGLE-LAYER`,并查 `pullthink_domain` / `pullcamp_domain` / `campfarm_target` / `bbfloor_domain` 同族写法。

---

## 5. 宽扫本身踩到一条 → **GH #258 [harness]**

`…_333bf7` 的 `20260827_151652_slot11.dem` 让 dumper `panic: unexpected EOF`(**可复现**,
单独跑同样 exit 2 / stdout 0 字节;md5 `8c9008c3011e850142510f206f5dfd22`)。
`sweep_run.sh` 是 `set -euo pipefail` ⇒ 整条脚本退出,**已跑完的 5 局也没写 summary/manifest/哨兵**,
下游按 GH #102 **正确地拒答**该目录 ⇒ **1 个坏文件拿走 6/25 = 24% 的语料**。

⚠️ **该局 `analysis.json` 是 `natural_end=True`、`duration_s=1562`,`.dem` 22,437,888 B 与同 run 其余
11 个(21.5–23.0 MB)完全同量级 —— 大小和肉眼都看不出它坏。**

**本组的临时绕法(已用,未固化)**:手工 `behav-dump` 补了 slot12、删掉 0 字节的 slot11 timeline;
**没有伪造哨兵** ⇒ 这 6 局**本轮仍未进** `abilanc_domain.py` 的读数,如实记为未覆盖。

---

## 6. 深查:7 局逐帧

### 6.1 两帧「戳营然后走人」(→ GH #259)

`88d937/20260827_151652_slot10 t=620.4` zuus lvl11(baseline 腿):
两发弧闪砸 `prowler_shaman`(蓝 1.00 → 0.81),`624.4` 掉头,`631.4` 已经在线上对小兵放同一个技能。**营没拿。**

`88d937/20260827_151632_slot8 t=759.4` centaur lvl11(armed 腿):
在 **0.59 血**上放自伤的 `double_edge` 砸 `granite_golem`+`rock_golem`(自伤 291,血 0.59→0.48),
`761.4` 掉头走。**营没拿。**

**正对照就在同一份语料里**:`89f466/20260827_151640_slot10 t=697.3` PA lvl11,离远古营 **376u**,
她打的是 **`mud_golem`(普通营)**,`697.3→705.3` 连打 8 秒吃完 —— 这是同一处该有的样子。
(与 WORKING 相容,**但看不到 sweep 内容,不构成证明** —— 工具自己的 "WHAT THIS CANNOT SAY" 就写着这条。)

### 6.2 ⭐ 一条量具边界:123 帧这个分母高估决策机会约 3–4 倍

逐帧看了 5 段最长的「喂选择器英雄 × 低等级 × 贴远古营」曝光段:

| 段 | 判读 |
|---|---|
| `89f466/151640_slot10` PA lvl11 t=694–704 | **真的营地决策**(去打了普通营) |
| `89f466/151650_slot11` WK lvl8 t=509–523 dmin=55 | **英雄团战** —— 打 bristleback,`523.2` 死 |
| `89f466/151620_slot8` PA lvl11 t=798–804 | **英雄团战** —— 被 bristleback 反杀,`805.3` 死 |
| `9079da/151640_slot10` ogre lvl3 t=111–119 | **纯路过**(去线上,全程零交互) |
| `9079da/151650_slot11` ogre lvl2 t=97–102 | **纯路过** |

⇒ 曝光量的是**「站得近」不是「能选」**。**登记,不建议现在改工具**(改了会动历史读数)。

---

## 7. 核验状态(累计)

**`abilanc`: SILENT(维持)** —— 本波**没买到 WORKING 证据**,也**没有 BUGGY 反证**;
域里 3 发经源码核定**在射程之外**。累计 (a) 语料 **W14 132 局 + W17-R 18 局 = 150 局,0 次可归因触发**。
**下次要买到 (a),必须是一份 ab/ba 两腿都落地的语料**(§4 是结构性论证,不是样本量问题)。

其余 39 个 armed id:W17-R 是**真 armed** ⇒ 与 W17 的 `UNTESTABLE` **不同**,这 18 局**可以**进它们的累计语料;
但本轮**没有**对它们做逐 id 核验,**一律不改状态**(上一轮已定的状态全部不变)。
**本轮没有任何 id 的三条件判定发生变化。**

**⚠️ 本组报告节奏 6.1h 洞(07:19Z→13:28Z),连续第三轮被自检点名。** 归因:那是 08-27 上午的触发间隔,
**本组无法自改 cron**;本轮如实记录,不再重复登记,若总监认为该动 cron 请裁。

---

## 8. 交棒

- **新开三个**:**GH #257 [bug]**(单腿语料上的假 verdict,含 249 局适用面 + 同族工具清单)、
  **GH #258 [harness]**(一个坏 `.dem` 打死整个 run 的宽扫)、
  **GH #259 [strategy]**(`list[1]` 远古戳营,两帧 + 两处源码 + fixture 配方)。
- **GH #196 追评**:`abilanc` (a) 第二次取证结论(§3/§4 全文)。
- **⚠️ 给批测台**:交棒第 10 条需要更正 —— 249 局单腿孤儿**可以取帧**,
  **不能做任何 armed/baseline 差值**;凡是「英雄级门」的 id(`abilanc` 一族),孤儿语料的 (a) 价值是**零**。
  要买 `abilanc` 的 (a),**必须**有一粒 ab/ba 都落地的种子。
- **球在总监**:#259、#258、#257、#253、#250、#249、#247、#246、#245、#244、#241、#236、#235、
  #159、#207、#212、#220、`campfarm`;`bbfight`/`bbshort` 三条件判定;`abilanc` 三条件判定
  (本轮材料:150 局 0 次可归因触发 + §4 的结构性论证)。
- **本组不裁 promote/reject、不申请波次、不改 bot Lua、不花 AWS 钱。**

---

## 9. 诚实标注

- `abilanc_domain.py` 打出的 `VERDICT WORKING-WITH-RESIDUAL` **本报告不引用为读数**(GH #257)。
- 「喂选择器英雄只在 armed 腿」是**本语料的实测**(22 个英雄逐个查过),
  机制论证(ab/ba swap 是唯一换腿通道)是**推理**;两者分开写,没有混。
- 深查里那句「PA 去打了普通营 = WORKING 相容」**只是相容,不是证明** —— 离线看不到 sweep 内容。
- 6.1h 报告洞是**真的**,本组不辩解,但也**改不了 cron**。
- W17-R 的 41 局计分局里只有 25 局有 `.dem`(`--rec-slots 12`,GH #75 既定),
  实际进读数 18 局 ⇒ **行为覆盖 18/41 = 44%**,arm 串结论覆盖 **64/64 = 100%**。两个分母不要混。

---

## 10. 验证(铁律 6)

- `bots/` 与 `game/` **0 改动**(本轮只写报告 + 章程;临时分析脚本在 `/tmp`,未入库);
- `bash tools/agent/luacheck_gate.sh` → 见下方 push 时的钩子输出;**未使用 `RULE6_BYPASS`**;
- 本轮**未改任何 Lua** ⇒ **不声称跑绿过 Lua 全量**(GH #124);
- `python3 tools/batch_test/behavioral/abilanc_domain.py --selfcheck` → **ALL PASS**;
- **AWS**:S3 **只读**(64 `analysis.json` + 25 `.dem`),**未启动/未终止实例**,**未调用 CE**,**零支出**。

---

## 11. 下一轮优先

1. ⭐⭐ **W18 首检**(`spot_20260827_1820*`×4,批测台 18:20Z 发出,4 台落 3 个 AZ)——
   **开工第一件事仍是 stamp 普查(秒级)**;**若拿到 ab/ba 两腿都落地的种子,立刻做 `abilanc` 的 (a)**
   (§4 说明这是唯一能买到它的语料形状,**这是本轮结论唯一能被证伪的预登记**);
2. ⭐⭐ **`pullthink` 英雄分层表**(上一轮的预登记,本轮被 `abilanc` 挤掉,**第二轮登记**)——
   W17-R 的 18 局是**真 armed**,可以直接并进 armed 腿;
3. ⭐⭐ **#236 裁下后 `mv` 落地 #159 的 fixture**(**第十轮**);
4. ⭐ **GH #259 的 fixture**(`88d937/20260827_151652_slot10 t=620.4 zuus`,**出厂腿、不依赖 arm 串**);
5. ⭐ **GH #250 的出厂帧 fixture**(`2c8641/20260827_123439_slot10 t=346.5 zuus`,**第二轮**);
6. ⭐ **把锚点修正回灌 `roam_conversion.death_spans`**(等 #247 裁定;回灌前先量影响面);
7. ⭐ **GH #220 的 fixture**(`094054_slot3 t=163.0 drow_ranger`,**第十轮**);
8. ⭐ poke 普查小脚本该不该入库(**第二轮**);每 id 篇数做成独立小脚本(**第六轮**);
9. `camp_prelit` 列(**第十四轮**);占用度量按 idx 去重(**第十三轮**);`tpdefend_events` 结果侧列
   (#191 后,**第十九轮**);幻象余量做进 `sweep_run.sh`(**第十六轮**);`entities.py` 推广(**第十七轮**);
   12:41Z §6.1 那 23 帧做 fixture(**第十九轮**);`pulldrag` 的 connect 侧;第二种视野证人;
   ab/ba 回灌 `fieldbuy_silence.py`/`stayfield2_margin.py`(**连续第三十轮登记**);`stayfield` 第一失败子句;
   打野反证 fixture(**三十六轮**);`hero-1` 的 153 局 WK 语料(**44 轮**);`l5combo` 的 (a)(**第六十轮**);
   `make_fixture.py` 钉 `062551 t=205.5 jakiro`(#45,**第四十七轮**);`axebuyblink` armed 的波次。

---

## 12. 本次会话 token 用量(铁律 8)

```
TOKENS total_in=8,131,949 out=48,049 turns=64
```

(统计时刻为写报告前;此后的收尾回合不计入。)
