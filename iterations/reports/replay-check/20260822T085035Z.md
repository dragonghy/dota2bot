# 录像检查组 2026-08-22T08:50Z(第四十一次触发)

**类型:预登记表重跑 + 一个新缺陷。** 批测台 08:11Z 收割完 `roamreach` 单 id 波次
(282 有效局),本组按 06:55Z **预登记口径**在收割完毕的全 16 局上重跑 §3.3 表;
过程中逐帧核验 owner P2 的铁证帧时,发现 `tp_home_wasteful` 检测器
**没有目的地谓词**,P2/#110 钉的那一帧不成立。

**宽扫 16/16 局;深查 6 局**(逐帧重建:2 条新增的基线侧长尾 + P2 帧 +
替换帧 + jakiro 回家往返 + 42 个 TP 命中的落点逐个重建)。
**零 EC2 支出,未改 bot Lua,未动 gate。**

---

## 0. 语料与守卫

4 run `spot_20260822_0610{41,45,50,54}_1_03316d1bafde9e51478b84f2f41774626b383217`,
种子 888/895/896/906,**20 `.dem` − 4 暖场 = 16/16 有效局**(上一轮只有 13 局,
本波当时仍在跑)。

- 按 run 分目录下载;`sweep_run.sh` **严格串行**(#102);`-interval` **默认 1.0s**。
- **#102 完成度哨兵 4/4 通过**:`games swept: 4` == `games_manifest.jsonl` 行数 4。
- 16/16 局 stamp = `mirror:roamreach:s<seed>:<side>` **单串**,无其它 id。
- **新增的 3 局是**(S3 LastModified 06:40-06:42Z,晚于上一轮 06:36Z 的快照):
  `20260822_063631_slot1`(s895)、`20260822_063559_slot1`(s896)、
  `20260822_063722_slot1`(s888)—— **三局全是 dire-armed**,这一点在 §1.2 承重。
- **同名跨 run 撞车**:`20260822_062004_slot1` 同时是 061041/s888 与 061045/s895。
  按 run 分目录 ⇒ scanner 不受影响;**我自己的一个临时脚本踩了它**,见 §4。
- **侧别缺口(沿用 06:55Z 登记)**:本波只 arm 一个 id,没有第二个「效果显眼且与本维度
  无关」的 id 做正交交叉验证,侧别**只**来自 stamp + `{radiant:2,dire:3}`。

---

## 1. 预登记表重跑:`roamreach`

口径**逐字未改**(06:55Z 预登记):band `[700,1600]`、`≥4` 帧、阈值 `9.0s`、
`-interval 1.0s`、idx 锁(最早出现时刻)。`--selfcheck` 通过(353 episode)。

### 1.1 承重列变强

```
ARMED      n=173  /局 10.81 | dur 均 3.73 max  8.0 | >=6s 16 | >=9s 0 | 尾部位移均 453 | 仍在追 67.6% | 转化 30.1%
baseline   n=180  /局 11.25 | dur 均 3.83 max 12.0 | >=6s 18 | >=9s 5 | 尾部位移均 462 | 仍在追 72.8% | 转化 33.9%
供给比 armed/base = 0.96 (173 vs 180)
```

**≥9s 零伤害追击:候选 0 / 基线 5**,超几何单尾 **p = 0.0335**
(06:55Z 部分语料 0:3、p≈0.115 ⇒ 补齐后跨过 0.05)。候选最长 8.0s,基线 12.0s。

阈值扫描**单调**(供给近平衡):

| 阈值 | armed : base | 比 |
|---|---|---|
| ≥5s | 26 : 32 | 0.81 |
| ≥6s | 16 : 18 | 0.89 |
| ≥7s | 7 : 13 | 0.54 |
| ≥8s | 3 : 6 | 0.50 |
| **≥9s** | **0 : 5** | **0.00** |

⇒ 与工具 docstring 的机制预言一致:armed **不减少追击次数**(供给 0.96、`dur` 均值
3.73 vs 3.83 几乎不动),**只截断尾巴**。「有限命令 vs 连续命令」该有的签名。

### 1.2 承重列穿过侧别分层(因为新增 3 局全是 dire-armed,必须查)

| 分层 | 局 | n armed:base | 供给比 | ≥6s | **≥9s** |
|---|---|---|---|---|---|
| radiant-armed | 8 | 94 : 82 | 1.15 | 6 : 8 | **0 : 2** |
| dire-armed | 8 | 79 : 98 | 0.81 | 10 : 10 | **0 : 3** |

**两个分层同号** ⇒ 不是 radiant 侧偏置(+1.5k 金)的产物。
注意供给比在分层里**不稳**(1.15 / 0.81),总体才平衡到 0.96 —— 引用分层率时要带上。

### 1.3 ⭐ 打自己脸的一条:`≥6s`/`≥7s` 列在 16 局上是噪声,不可引用

06:55Z 我登记过「反号项:≥6s 候选 16 : 基线 11」。补 3 局后同一列变成 **16 : 18**——
**armed 一条没加,baseline 加了 7 条**。分层看更极端:

- dire-armed **旧 5 局**:≥6s **10 : 3**(强反号)
- dire-armed **新 3 局**:≥6s **0 : 7**(强同号)

同侧、同批种子、同一个 scanner,**3 局就能让这一列翻号**。
⇒ **`≥6s`/`≥7s` 两列不可判,双方都不要引用**。承重的只有 `≥9s`,
它能读是因为它数的是**分布尾部的存在性**(armed 上界 8.0s 是硬截断),不是一个比例。
**纪律(新登记)**:同一个扫描里不同阈值的列统计性质不同,不能一起引用。

### 1.4 判定:`roamreach` **WORKING(倾向),证据未闭合**(定级不变,数字更硬)

- **支持**:≥9s 全在基线侧(p=0.0335)、两分层同号、供给 0.96、阈值扫描单调、机制一致。
- **不闭合**:(i) 域是**经验的**(快照无施法距离,`_roamreach_ThreatReach` 离线不可重建),
  episode 池了所有「走向敌方英雄却零伤害」的原因,armed 只碰 team_roam 子集
  ⇒ **效应被稀释,零也不等于 SILENT**;(ii) n=5 的尾部事件,888 独占 3;
  (iii) 侧别无正交佐证;(iv) 批测台经济四指标全 null(gpm +2.00,z=+0.42),
  按事先登记是预期(单 id MDE 30-35 gpm),**既不支持也不反对**。

**交接 AG.4 第 (3) 步**(前置 = 「实测确认 `roamreach` 仍消不掉域内成本」):
本轮读数是**削顶了(≥9s 归零)但没有消掉(次数不减)**。按字面满足「仍消不掉」,
但**削顶是真的** —— 请总监按这个更细的读数裁定,别只读「消不掉」三个字。

### 1.5 两条新增的基线侧缺陷帧(都在新补的 3 局里,可直接当 #45 的形状样本)

- `20260822_063631_slot1` **zuus → slardar 391.5-402.5(11.0s,3,300u)**:
  Zeus **全程 hp=1.00**、对 slardar **零伤害**,slardar 边跑边把血从 0.54 回到 0.66;
  身边队友 0-1 人。
- `20260822_063722_slot1` **witch_doctor → drow_ranger 598.4-608.4(10.0s,3,573u)**:
  WD **全程 hp=1.00**、零伤害、`conv=0`;drow 在 609.4 被**别人**打死,
  WD 之后还朝尸体位置走了 6 秒。(该 episode 前 5s 有一次 13,101u 的 TP 落地,
  **在 episode 之外**,不污染 band 内测量,登记备查。)

---

## 2. ⭐ 新缺陷:`tp_home_wasteful` 没有目的地谓词(GH **#111**)

### 2.1 代码事实

`detect.py:187 d2_tp_home_wasteful` 的判据只有三条:TP 频道存在、
`0 < hp_pct < 0.55`、最近敌人 `>= 1400u`。**没有任何一行读 TP 的落点。**
而它的 `desc` 写 `"should regen in lane, not TP home"`,名字叫 `tp_home_wasteful`。
⇒ 实际测的是「**低血 + 身边无敌人时按下的任意 TP**」,含**向前/侧翼**的进攻性 TP。

### 2.2 实测(42 个命中逐个重建落点)

落点 = 频道后 8s 内首个相对按下位置位移 >1500u 的快照;
自家泉水 = 本队英雄在最早 3s 的位置质心;idx 锁。

| | n | 离自家泉水中位 | hp 均值 | hp≥0.40 |
|---|---|---|---|---|
| 落在自家泉水(<1200u) | **22** | ~110u | 0.191 | **0/22 (0%)** |
| 落在别处 | **18** | **~10,000u** | 0.424 | **13/18 (72%)** |
| 频道被打断(8s 内无跳变) | 2 | — | — | — |

两群体血量几乎不相交,Fisher 单尾 **p = 7.1e-07**。机制说得通:8% 血回泉水,
~50% 血往前 TP 支援 —— `WASTE_HP_PCT = 0.55` 把后一整类扫了进来。

- **正控**:22 个 HOME 命中读出的离泉距离 51-228u(22 次独立确认)
  ⇒ 泉水质心估计本身是对的,NOT-HOME 那侧的 ~10,000u 不是标定误差。
- **已排除复活伪影**:42 个命中里只有 1 个在按下前 25s 内有本人 DEATH 事件
  (`061952` ogre_magi 601.9,而它恰好是两个「频道被打断」之一)。
  22 个 HOME 命中**没有一个**是复活跳泉水。

### 2.3 直接后果:owner P2 / #110 的铁证帧作废(**owner 原则不受影响**)

P2 与 #110 标题都钉 `…061050…/20260822_063559_slot1` t=636 Slardar 46%。逐帧:

```
  t      slardar(x,y)     hp    最近敌人
 628.4 ( -1162,  -699)  0.65   1520u zuus
 631.4 ( -1188,  -531)  0.44   1219u zuus          <- 决策前 3s 掉了 20% 血
 636.4 (  -101,  -128)  0.47   1485u tidehunter    <- 频道帧(636.4/637.4/638.4 坐标逐位不变)
 639.4 ( -5557,  5350)  0.48   7332u zuus          <- 落点
```

Slardar 是 **dire(team 3)**,本局 dire 泉水实测 **(+6902,+6420)**(t=-73.6 花名册帧)。
落点离自家泉水 **12,646u** ⇒ 这是一次**打上路的前压 TP**,落地后还朝中路走了 20s。
**他没有回家。** 另外「无任何危险」也不成立(决策前 3s 掉 20% 血,敌人 1,046-1,485u,
只差 85u 没被 `SAFE_RADIUS=1400` 拦下)。
该命中 `on_candidate_side: true`,但本波 armed 串是 `roamreach` 单 id、与 TP 无关
⇒ **稳定版出厂行为**,不是任何 armed id 的锅(这点 P2 判断正确)。

**要强调**:owner 的原则(Turbo 里低血无危险不该回家)**完全没被否掉** ——
同一份语料里有 **22 个货真价实的回家 TP**。作废的只是**这一帧**作为它的证据,
而 P2 完成定义第 1 条要求「钉 10:36 帧做 fixture」—— 照钉会把 fixture 钉在前压 TP 上。

### 2.4 建议的替换铁证帧(已逐帧核验,四条都比原帧硬)

`…061041…/20260822_063722_slot1`(s888,dire armed)**`lina` t=349.1,hp=0.32**:

```
  t      lina(x,y)      hp   最近敌人           队友<1500  身上回复品
 343.4 (  -747,  -542) 0.50   6476u spirit_breaker  0   faerie_fire, empty_bottle, magic_wand
 347.4 (  -541,  -465) 0.31   6696u spirit_breaker  0   faerie_fire, empty_bottle, magic_wand
 349.4 (  -498,  -210) 0.32   6677u spirit_breaker  0   <- 频道帧(349.4/350.4/351.4 逐位不变)
 352.4 (  6928,  6372) 0.34  11808u viper           0   <- 落点 = dire 泉水(实测 (6902,6420),距 33u)
 361.4 (  5694,  5603) 1.00  10661u sven            0
```

1. 落点**确实是自家泉水**(33u);2. **连续 8 秒最近敌人 ≥6,400u**,0 队友在 1500u 内
⇒「无危险」前提为真;3. **反事实就在她包里**(`faerie_fire` + `empty_bottle`);
4. 代价可量化:决策 349.1 → 再次离开泉水 369.4 = **20.3s**,t=365.4 时离最近敌人
还有 8,968u ⇒ 实际脱离战场 **~30s**(Turbo 一局才 ~20 分钟)。

`make_fixture.py --t 349.1 --hero lina`;决策帧取 **≤349.1 的最后一帧**
(章程「按固定偏移查前帧会落空」)。

### 2.5 给 P2 决策侧 id 的免费事实

22 个真·回家 TP 的随身回复品普查:**21/22 带 `magic_wand`/`magic_stick`,
5/22 tango,5/22 clarity,4/22 faerie_fire,`flask`(大药)**0/22**。**
⇒ owner 说的「买大药」在当前 buy list 下**没有可用对象**。决策侧 gate 单独上,
很可能只是把 bot 钉在线上干耗。P2 把它与 `fieldregen`/`wandbleed` 编成
「野区续航一族」同波取证是对的,但**补给侧(至少让 pos4/5 常备 flask)
可能才是承重的那一半**。建议决策侧 id 把「身上有可用回复品」作为拦截的**前置**。

### 2.6 建议的修法与验收(已写进 #111)

1. 加**目的地谓词**:落点距自家泉水 <1200u 才算 home;不满足的另记
   (`tp_forward_lowhp`)或丢弃,**别继续用现在的名字报数**。
2. **凡引用过 `tp_home_wasteful` 计数的轮次都要标注口径** —— 含本组 06:55Z 宽扫表的
   「候选侧 14:23」(按本轮比例约 43% 不是回家)。**我不替它重算**(另一份语料),
   只登记它需要重算。
3. 验收:修完在本语料重跑,HOME 组应为 22(±2 个频道打断),NOT-HOME 组应消失。

---

## 3. 宽扫层(16/16 局,选点用,不下结论)

| 检测器 | 候选侧 | 基线侧 |
|---|---|---|
| sandwiched_walk | 69 | 80 |
| overextend_alone | 68 | 66 |
| laning_past_midline_death | 44 | 43 |
| missed_cs_at_tower | 43 | 39 |
| tp_under_threat | 42 | 35 |
| return_to_death_spot | 34 | 38 |
| unpunished_tower_dive | 32 | 34 |
| idle_while_ally_dies | 18 | 26 |
| **tp_home_wasteful** | **18** | **24** |
| died_with_ult_ready | 14 | 11 |
| lowhp_limbo | 12 | 8 |
| deep_solo_death | 8 | 5 |
| enemy_overchase_unpunished | 2 | 1 |

06:55Z 标记的三项「候选侧偏高」(`lowhp_limbo` 12:7、`died_with_ult_ready` 12:6、
`deep_solo_death` 6:3)在补齐 16 局后变成 12:8 / 14:11 / 8:5 —— **差距全部收窄**,
与 §1.3 同一教训(个位数差在 16 局上不可判)。**不开 issue。**
`tp_home_wasteful` 的 18:24 现在**按 §2 的口径整体存疑**,不作侧别解读。

---

## 4. 本轮自己踩的坑(写进工具坑)

**同名跨 run 撞车会咬到分析层,即使宽扫层是干净的。**
`20260822_062004_slot1` 同时属于 061041(s888)与 061045(s895)。
`sweep_run.sh` / scanner 都是**逐 run** 跑的,不受影响;
但我第一版的回复品统计脚本建了一个**全局 `basename → 单个 timeline 文件`** 映射,
于是那两条记录读到了**对臂的 timeline**,输出 `hp=0.00`、`frm=0`。
**抓住它的是逐字段自检**(`hp=0.00` 与检测器自己的 `0 < hp_pct < 0.55` 守卫矛盾)——
本组第三次靠自检挡下建在错语料上的结论。按 run 重跑后修正:
dragon_knight 0.16 / venomancer 0.18,**§2.2 的 42 行落点表本来就是逐 run 跑的,不受影响**。
**纪律:任何按 basename 索引 timeline 的临时脚本都必须带 run 维度**,
章程里那条「必须按 run 分目录下载」只覆盖了下载,没覆盖**分析**。

---

## 5. 核验状态(本轮变动项)

| id | 状态 | 证据 |
|---|---|---|
| `roamreach` | **WORKING(倾向),未闭合**(定级不变,数字更硬) | 16/16 局;≥9s 0:5,p=0.0335;两分层同号;供给 0.96;§1.5 两条新帧证据 |

其余测试集 id 本波**未 arm**(单 id 波次),无新核验记录。
`l5combo` 的 (a) **连续第十五轮**未买到,仍请总监按 §AF.3 先例直接裁定降级。

---

## 6. 跨组

- **新开 GH #111** `[harness]`:`tp_home_wasteful` 无目的地谓词(§2 全文)。
  这是一个**新的检测器缺陷类别**(既不是 #78 的采样格点、也不是 #69 的实体身份,
  而是**判据缺了一条它名字承诺的维度**),现有 issue 无覆盖 ⇒ 不算重复开。
- **#110 追评**:P2 铁证帧作废 + 替换帧 + 0/22 大药那条事实;**球仍在协同组**。
- **#45 追评**:§1 全表 + AG.4 第 (3) 步的细读数交接给总监。

---

## 7. 验证

- `.lua` 改动数 = **0** ⇒ 铁律 6 的 Lua 门无适用对象;本容器**未装 `luacheck`/`lua5.1`**
  (**如实记录未假装跑过**)。
- 未新增工具脚本(全部为一次性 `python3 -`,数据落 scratchpad 不入库);
  `roamreach_chase_tail.py` **未修改**,`--selfcheck` 通过(353 episode,band 与长度不变量)。
- #102 完成度哨兵 4/4;泉水质心带 22 次正控;落点判据对「频道被打断」显式记成第三类,
  不硬塞进任一组。
- **AWS**:EC2 **$0**,S3 ≈ **$0.02**(20 `.dem` + 20 `analysis.json` + 若干 `s3 ls`),
  **未调用 Cost Explorer**。

---

## 8. 已检查清单(本轮新增)

`spot_20260822_0610{41,45,50,54}_1_03316d1…` 的 **16/16 局全部**
(上一轮 13 局的超集;新增 `063631`/`063559`/`063722`)。
该波次 `.dem` 侧**现已 100% 检查完毕**。
批测台 08:11Z 启动的全集波次(`spot_20260822_0810{28,33,38,43}_1_12ef3de…`,
18 id armed)触发时尚无可用 `.dem`,**留给下一轮 —— 那是自 08-20 以来第一波全集波次,
优先级最高**(owner 07:23Z 指示:录像组选局核验优先用全集波次的局)。

## 9. 下一轮优先

1. **全集波次 `spot_20260822_0810xx`(18 id armed)的宽扫 + 深查** —— owner 指示的
   合成行为观察,且是 14 个「核验记录最少」id 唯一的取证机会;
2. `make_fixture.py` 钉 `20260822_062551_slot1 t=205.5 jakiro` 离线重放 `Think()`
   (#45 的闭合路径,**连续第二轮顺延**,免费离线);
3. `l5combo` 的 (a)(**连续第十六轮**,请总监直接裁定降级);
4. 05:05Z 缺口:另三个消费方(`tpdead_release`/`tpdying_landing`/`null_leg_occupancy`)
   的 idx 锁 delta(免费离线,**连续第三轮顺延**);
5. `axe_blink_domain.py` 等 `axebuyblink` armed 的波次(**0810xx 那波也没有它**,继续等);
6. 引用任何注册读数前核对它的口径参数(`-interval`、阈值、`ATTRIB_U`);
   **本轮新增**:同一扫描里不同阈值的列不能一起引用(§1.3)。

## 10. 本会话 token 用量

```
TOKENS total_in=12,717,045 out=127,768 turns=101
```
(统计时刻为收尾前;此后仅剩章程更新与 commit/push 两回合,不计入。
本轮为「预登记表重跑 + 一个新缺陷的全量落点重建」,42 个命中逐个重建 + 6 局逐帧,
量级正常偏高;主要成本在 16 局 sweep 的 dumper 输出与三处逐帧视图。)
