# 录像检查组 2026-08-23T10:54Z(第五十三次触发)— `creeppull` 的 (a):触发在跑、动作能成,可是「兵线跟过来」这件事一次超量都没有

**一句话**:执行章程「下一轮优先 (1)」= `creeppull` 的特异签名。那个签名一半是空转
(`poke` 本来就是 `pull` 的合取项),另一半写对之后(`inflictor == 'dota_unknown'` = 真普攻)
把比值从 1.34× 收窄到最窄切片 **1.63×** —— **确实存在 armed 特异的超量**;
但新量的**效果侧**(敌方兵线质心朝我方泉水的位移)在**五个切片、四个位移分层里 armed 全部 ≤ baseline**,
超量 **= 0**。机制在帧上是清楚的:**一次普攻中位只买到 2.2 秒的小兵追击,而 bot 中位拖 587u
(实测走到 1380–1460u)—— 拖拽的后半程没有兵线跟着**。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` → **worst exit 0**:UNLANDED **0 条**
(certifiable window 内;271/294 refs 因浅克隆不可判定被 REFUSE,工具既有边界);
cadence + citations **clean**;trunk python 测试 **15 passed / 0 failed**。

## 1. 本轮做什么、为什么是它

- `iterations/OWNER_PRIORITIES.md`:P1 球在协同组(第 1 棒),P2 球在协同组 —— **本组无未完成的
  owner 优先项**,但 P1 完成定义第 3 条(录像组在真实对局里核验勾线/拉野 WORKING)
  **只兑付了 `pullcamp` 那一半**(09:09Z),`creeppull` 那一半还欠着。
- 批测台 10:15Z 报告:**本轮无新波次**(节流门 12:11:11Z 未到,`soak/` 仍是 168 个前缀)
  ⇒ 按章程第 2 条走**补课**,目标就是章程末尾登记的「下一轮优先 (1)」。
- 容器是全新的,上一轮的 `.sweep_out/` 不存在 ⇒ 重新宽扫一个 run(S3 只读,零 EC2)。

## 2. 吞吐(章程固定栏位)

- **宽扫**:`spot_20260823_061111_1_7159ff3...89ea04`,**54/54 非暖场局全扫**
  (60 个 `.dem`,6 局暖场/未打戳按 `sweep_run.sh` 规则跳过),
  `sweep_summary.md` 15 个通用检测器计数表已产出。
- **深查(逐帧)**:**7 局**(章程下限 6):
  `062503_slot11` viper / `063011_slot1` lina / `061930_slot12` viper /
  `062509_slot7` viper / `061936_slot11` sven / `063000_slot3` tidehunter /
  `062455_slot4` zuus。
- **每 id 触发计数**:本轮只对 `creeppull` 做逐 id 核验(见 §5);宽扫的 15 个通用检测器
  计数在 `.sweep_out/.../sweep_summary.md`,与 09:09Z 那两波同量级,无新异常。

## 3. ⭐ 先把上一轮登记的那个「下一步」拆开:一半是空转

09:09Z 登记的下一步是「先量有多少 core_pull episode 伴随本人对该敌方英雄的一次普攻」。

**空转的一半**:`creeppull_domain.py:339` 写的是
`pull = bool(drag >= DRAG_U and keep and poke)` —— **`poke` 是 `pull` 的合取项**,
所以「伴随任意伤害」的答案**构造性地是 100.0%(692/692)**,两条腿都一样。

> **新纪律(登记进章程)**:**判别式不能是度量自己已经要求的子句。**
> 提出一个新签名之前,先回去看被比较的那个量的定义里有没有它。

**对的一半,而且它比提出者想的更重要**:`poke` 收的是**任意 hero→hero 伤害**,
语料里它大量被**被动 / DOT**满足:

| inflictor(该局 hero→hero DAMAGE 前几名) | 性质 |
|---|---|
| `dota_unknown` 269 | **真普攻** |
| `witch_doctor_maledict` 180 / `jakiro_dual_breath` 124 / `crystal_maiden_frostbite` 120 | 技能 |
| **`viper_corrosive_skin` 99** | **挨打才触发的反伤被动** |
| **`lina_slow_burn` 77 / `viper_poison_attack` 61** | **DOT,几秒前那一下留下的** |
| **`zuus_static_field` 55** | **施法触发** |

**存在性证明**:`20260823_063011_slot1` lina t=47.4(armed 腿),整段对 zuus 的伤害
**只有 `lina_slow_burn` 的 DOT 在跳**,她一个攻击指令都没下,却拿到了 `pull` 帧。
`20260823_061930_slot12` viper t=100.4 同形状,靠 `viper_corrosive_skin` —— 那是**他被打**才触发的。

**armed 腿 47.0% 的 pull episode 里没有任何一次真普攻。**

## 4. ⭐ 三条轴的读数(工具 `creeppull_specificity.py`,`--selfcheck` 8/8 PASS)

54 局,5,105 个 episode(core 2,869)。

### 4.1 轴 0b:真普攻收窄 —— **比值随切片变窄而单调上升**

| 切片(core) | armed | baseline | 比值 |
|---|---|---|---|
| 任意 pull episode | 396 | 296 | 1.34× |
| + 真普攻 | 210 | 151 | 1.39× |
| STRICT(单敌人分支,匹配 Lua 门) | 126 | 99 | 1.27× |
| **STRICT + 真普攻** | **75** | **46** | **1.63×** |

- **1.34× 在两个独立 run 上复现**(09:09Z 213 局读 1.31×,本轮 54 局读 1.34×);
- 分母仍然是平的(每局每腿 core 域 episode:armed 26.83 vs base 26.30);
- **⇒ armed 特异的超量是真的**,但离 `pullcamp` 的 10.4× 差一个量级:**门关着照样做 46 次**。

### 4.2 轴 1:节奏从来没有持续过 —— **连续 pull 帧的最长游程 = 2,两条腿都没有一个 3**

| 游程长度 | armed | baseline |
|---|---|---|
| 1 | 293 | 225 |
| 2 | 103 | 71 |
| **3** | **0** | **0** |
| ≥4 | 0 | 0 |

调用点(`mode_roam_generic.lua:185-198`)的节奏是「攻击一拍(≥1.2s 才允许下一次)+ 其余帧走 retreat」;
**要续上仇恨只能回头走进攻击距离,而那正好抵消拖拽**。5,105 个 episode 里这个矛盾一次都没被解开。

### 4.3 ⭐ 轴 2(判决所在):效果侧的超量 = **0**

新量 = **敌方兵线质心朝我方泉水的位移**(锚在开拉那一帧的兵线位置;`WAVE_R=1500` 防止把
下一波兵换进来 —— selfcheck 里有专门一条钉这个)。`pull` 的三个子句**没有一个**看兵线自己动没动。

| 切片 | armed 中位 | baseline 中位 |
|---|---|---|
| 全部 core episode | 15.5 (n=1334) | 15.5 (n=1356) |
| ≥1 pull 帧 | 70.0 (n=370) | **88.0** (n=288) |
| run ≥ 2 | 71 (n=95) | **130** (n=71) |
| STRICT pull | 70 (n=121) | 67 (n=99) |
| 真普攻 pull | 70 (n=193) | **98.5** (n=146) |

**五个切片 armed 全部 ≤ baseline。** 再按 bot 自己的净向家位移分层(控住「armed 走得更远」这个混杂):

| 净位移分层 | armed(n, 中位 follow) | baseline(n, 中位 follow) |
|---|---|---|
| [−∞,0) | n=69 med=−48 | n=45 med=−81 |
| [0,300) | n=98 med=63 | n=78 med=64 |
| [300,600) | n=105 med=92 | n=92 med=**122** |
| [600,∞) | n=98 med=137 | n=73 med=**206** |

**四个分层同方向。** armed 腿把核心拖得更远(run≥2 净位移中位 **587 vs 434**),
**兵线并没有更跟**。

> 诚实边界:单个切片的中位数差(71 vs 130,n=95/71)本身是噪声级的,**本报告不主张
> 「armed 更差」**;主张的是**更弱也更稳的那一条 —— 五个切片 + 四个分层里 armed 没有一次超量**。

### 4.4 ⭐ 轴 3:机制 —— 一次普攻只买到 2.2 秒

离线唯一能读的「小兵真的转过来了」= 战斗日志里 **非中立小兵 → 本 bot** 的 DAMAGE 行
(章程 08-23 条:非英雄 actor 在 timeline 的 events 里是完整的)。

| 量(core,含 ≥1 次真普攻的 pull episode) | armed | baseline |
|---|---|---|
| episode 数 | 198 | 144 |
| 对目标英雄的普攻次数(中位) | **2.0** | 2.0 |
| 只普攻 **1 次**的 episode 占比 | **47.5%** | 45.8% |
| 最后一次普攻后小兵还打我多久(中位) | **2.2 s** | 1.55 s |
| 该尾巴 = 0(小兵一次都没转过来) | **26.8%** | 34.0% |

小兵移速 ~325 u/s ⇒ **2.2 秒的追击最多值 ~700u**,而 bot 中位拖 587u、实测走到 1380–1460u。
**⇒ 拖拽的后半程是没有兵线跟着的空走。**

## 5. 逐帧(7 局;硬规则:先逐帧后聚合)

**(甲)反例 —— 拖了 1380u,兵线反着走(`20260823_062509_slot7`,armed=radiant,viper pos3)**

```
205.5 (-5945,4616) hp0.53  DAM jakiro v=56                      <- 普攻 1
206.5 (-5945,4616) hp0.54  DAM jakiro v=55 (+predator/poison)   <- 普攻 2(最后一次)
207.5 (-5973,4491) −126u   badguys_ranged >                      <- 小兵开始打他
208.5 (-6023,4310) −184u   badguys_ranged >                      <- 小兵最后一次打他
209.5 (-6217,4059) −264u   只剩 viper_poison_attack 的 DOT
210.5 (-6212,3824) −233u
211.5 (-6204,3500) −322u
212.5 (-6199,3244) −255u   合计往家 ~1380u,follow = **−104u**
```
**仇恨 208.5 就断了,他又走了 4 秒。** 开局那两帧还在挨敌方一塔(`badguys_tower1top >` 202.5/204.5)。

**(乙)正例 —— 教科书勾线(`20260823_062455_slot4`,armed=radiant,zuus pos2)**

```
93.5  (-704,-502)  hp0.92  DAM lina v=49        <- 普攻
95.5  (-1142,-782) −397u   badguys_ranged >     <- 小兵跟过来
98.5  (-1512,-1130) −60u   badguys_ranged >
99.5  (-1655,-1275)−203u   badguys_ranged >
100.5 (-1879,-1555)−355u   合计 ~1460u,follow = **+749u**
```
**门是好的、动作也能成。** 代价也在这一帧里:zuus 血 0.93 → 0.54,中单被小兵啃着走了 1460u。

**(丙)域是**超集**,前三名「最像拉」的 episode 有两个根本不是拉** —— 这条要单记,
因为它决定了聚合读数怎么解释:
- `062503_slot11` viper t=129.5:**2v1**(jakiro + drow_ranger 同时打他),
  hp 1.00 → 0.36,`viper_corrosive_skin` 的反伤替他满足了 `poke`。
  Lua 门在 `#tEnemyHeroes > 1` 就 return 了(viper 是远程,melee-vs-2-ranged 分支不适用)
  ⇒ **这一帧真实的门是关的**,是域的超集把它放进来的。
- `063000_slot3` tidehunter t=332.4:一场 1v1 打架,**t=335.4 放了大招 Ravage**,
  hp 0.87 → 0.34。也被记成 pull 帧。
- `061936_slot11` sven t=293.5:**纯振荡**(dFount +88/+68/+28/−6/−24/−55/−46/−282/+77/…),
  12 秒净位移 217u,`follow=−1` —— 就是 §4.2 那个「回头再打一下」的矛盾的样子。

⇒ 报告里所有结论都同时给了 **STRICT 切片**的读数,原因就在这里。

## 6. ⭐ 顺带修掉一个 fatal(工具侧,同一族的第二次)

`creeppull_domain.py` 的位移护栏是 `550 * DRAG_S`,注释写 `~550 u/s with every haste in the game`
—— **和 09:09Z 修掉的 `pullcamp_domain.WALK_CAP` 是同一条已被删除的游戏规则**,同样是裸 `sys.exit(2)`:
语料里只要有一次全速向家行走,整个工具产不出任何读数。

按同一份实测(879,106 个干净 1s 步长;五个毫无位移技能的英雄同为 **660 u/s**)改成 `WALK_CAP = 660.0`。
本轮实测 `max clean drag = **1177 u** (cap 1980)`,未触发 —— **这一条之所以还没引爆,是因为这个护栏读的是
投影不是位移**,投影天然比 u/s 界更宽容;已把这层原因写进注释,免得下次有人以为它被验证过。
技能位移/跳刀仍留在护栏外(earthshaker 1597、PA 1146、SB 1142、jugg 983),护栏还有牙。

**纪律沿用**:注释写「every X in the game」的常数,先问它是哪一年的游戏 —— **而且要问全目录**,
09:09Z 只修了自己手上那一个。

## 7. `creeppull` 的 (a) 判读(章程三标签)

**`creeppull` = WORKING(执行)/ 效果未兑现。** 拆开写,免得被读成 SILENT 或全盘 BUGGY:

| 面 | 判读 | 证据 |
|---|---|---|
| 触发 | **在跑** | 最窄切片 STRICT+真普攻 75:46 = **1.63×**,比值随切片变窄单调上升;1.34× 跨两个 run 复现 |
| 动作 | **能成** | `062455_slot4` zuus t=93.5→100.5,小兵一路追,拖 1460u,`follow=+749u` |
| 效果 | **未兑现** | 五切片 + 四分层,armed 相对 baseline 的兵线位移超量 **= 0** |
| 机制 | **拖拽 ≫ 仇恨** | 一次普攻中位买 2.2s 追击(26.8% 尾巴 = 0),bot 中位拖 587u |

**⇒ owner P1 完成定义第 3 条对 `creeppull` 这一半:执行 ✓、「行为正确」✗(未证)。
按铁律 2,(a) 尚未买到,不建议现在 promote。** 建议下一步是 #143 的**提前收手**版
(距最后一次普攻 >~2.5s 就停止拖拽,保守、无新引擎调用),再买一次 (a)。

## 8. 跨组

- **新开 GH #143 `[strategy]`**:拖拽比仇恨活得久 —— 机制 + 甲/乙两帧 + 两条修法
  (续仇恨 / 提前收手)+ **建议钉帧 `20260823_062509_slot7 --t 207.5 --hero viper`**
  (第一次「仇恨已断但仍在拖」的判定帧),**正例保护帧 `20260823_062455_slot4 --t 93.5 --hero zuus`**
  (修法不得把这一类关掉)。
- **GH #109 追评**(owner P1 主线,不新开):§3 的判别式更正 + §4 三条轴 + §7 的三标签判读
  + §6 的 fatal 修复。
- **不提波次请求**(全部读数来自已有语料;下一次 (a) 取证等 #143 落地后再提)。

## 9. 验证(铁律 6)

- `bots/` / `game/` **0 改动**,gate 未动(`git status`:仅 `tools/batch_test/behavioral/` 两个文件);
- `bash tests/run_py_tests.sh` → **15 passed / 0 failed**(观测,改动前后各跑一次);
- `creeppull_specificity.py --selfcheck` → **8/8 PASS**;`creeppull_domain.py --selfcheck` 全 PASS;
- 容器**无 `lua5.1` / `luacheck` 且本轮未改任何 Lua** ⇒ 铁律 6 的 Lua 两条无适用对象;
- 位置一律走 `seed_draft.positions_for_game`,**未用 `team_slot%5+1`**;英雄名 canon 化后再比;
- 采样间隔 = dumper **默认 1.0s**(creeps 3.0s,按 `CREEP_STALE=1.6` 取 ≤t 的最后一帧);
- **AWS**:EC2 **$0**,S3 **只读**(一次 `s3 ls` + 60 个 `.dem` 下载),**未调用 Cost Explorer**。

## 10. 下一轮优先

1. ⭐ **12:11Z 那波落地后的宽扫**(批测台预登记会发)+ 各 armed id 的 (a) 补课;
2. ⭐ **同一份 54 局语料的零成本补课**:`stayfield` / `stayfield2` / `fieldbuy` /
   `fieldcreep` / `itemtrip` 都是首次或第二次 armed(语料已在本地 `.sweep_out/`);
3. **打野反证的 fixture**:钉 `002103_slot5 t=634.2 skeleton_king`(**已顺延三轮**);
4. #136 的魔棒普查跑满另外两波(#139 已给出真正机制,核验它修完之后 40/40 变没变);
5. §3.4「整族沉默」在新语料上的复现率;
6. `hero-1` 的 153 局 WK 语料(棒子挂 **11 轮**);
7. `l5combo` 的 (a)(**连续第二十七轮**);
8. `make_fixture.py` 钉 `062551 t=205.5 jakiro`(#45,**连续第十四轮顺延**);
9. `axebuyblink` armed 的波次。

## 11. 本次会话 token 用量

```
TOKENS total_in=11,733,171 out=65,491 turns=84
```
(统计时刻为止;此后的收尾回合不计入。)
