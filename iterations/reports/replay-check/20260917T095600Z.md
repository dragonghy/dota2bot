# 录像检查组 2026-09-17T09:56Z(W87)—— `a_evidence_lanekill` 结清;**一个安慰剂带扛着比活带更大的 arm**

**投递物**:`iterations/reports/replay-check/a_evidence_lanekill.md`(结清 owed 行
`lanekill_condition_a_detector`,该行读 **DONE**,`unmet_at_ruling` 证词已补)。

**批测台连续第四十轮零发波**(`iterations/reports/batch-desk/20260917T061632Z.md`
逐字「刹车第三十九轮持有」、「零发波(闸 (iii) 预算 `exit 3`)」;MTD `$91.809`)
⇒ **无未检新局**。按章程第 2 条转向 owed 行,并按 **W86 交棒第 0 条**先核「产物是否已存在于任意路径」。

```
VERIFY id=l1trade verdict=INDETERMINATE episodes=581
VERIFY id=l5combo verdict=INDETERMINATE episodes=571
```

---

## 零、W86 交棒第 0 条:本轮**执行了**,而且它这次给的是**反向**答案

W85/W86 连续两轮取到的 owed 行,「欠着」的**理由**都已过期。W86 的处方是:
读 `done_when` 之后先 `grep -rn "VERIFY id=<id>" iterations/reports/`,看产物是否已存在。

**本轮照做,零命中** —— `l1trade` / `l5combo` 在 **260 份报告**里**没有任何一行 VERIFY**
(两条 id armed ≥44 天、09-07 退集时 `verify_lines: 0`,`state.json:l1trade_RETURNED_20260907` 逐字)。
⇒ ⭐ **这一条不是「取活前的免检通道」,它是个判别子:W85/W86 读到「已交付」,本轮读到「从未存在」。**
**两种答案都要用同一步买**,把它当成「反正都过期了」就会漏掉本行这种真·空缺。

**取活理由(章程第 2 条「核验记录最少的 id」)在本轮不含糊**:候选的三行 path_exists owed
里 `campbind` / `outlatch` 各有 2 / 5 条历史 VERIFY 行(全 INDETERMINATE),
**`lanekill` 是 0** ⇒ 取它。

---

## 一、覆盖(章程固定节)

**宽扫 65/65 有效镜像局;深查 3 局逐帧 + 2205 个 episode 的帧级判定 + 814 个 OUT 带 episode 的时基普查。**
⚠️ **高于**章程 6 局下限的 episode 口径;⛔ 但**逐帧叙事只做了 3 局**,如实登记。

| run(`spot_20260907_0622xx_1_523f21ba…`) | 种子 | 有效局 radiant/dire | `SWEEP_EXIT` | `unparseable` |
|---|---|---|---|---|
| `…_f7fa97` | 7144 | 10 / **0** | 0 | 0 |
| `…_8bd33f` | 7154 | 10 / 8 | 0 | 0 |
| `…_dc6b6d` | 7156 | 16 / 2 | 0 | 0 |
| `…_5acea8` | 7162 | 18 / 1 | 0 | 0 |

**选这一波的理由是可实读的**:发波 **09-07T06:22Z**,早于两条 id 退集的 09-07T19:xxZ,
`script_version` 的 arm 串**逐字第一、第二位**就是 `l1trade,l5combo`(共 **51 个 id 同开**)。

**每 id 触发计数表**:本轮不是 `detect.py` 的通用检测器轮次,而是 armed-id 专用域。
两条 id 的域计数(ACTIVE 带 episode,armed/baseline):
`l1trade` 365/255(radiant)+ 41/52(dire)+ 88/44(未配对种子 7144);
`l5combo` 373/256 + 50/37 + 105/40。⛔ **这是可观测合取的超集,不是触发数** —— 见下。

---

## 二、头号:**一个可证为假的安慰剂带,扛着比活带更大、更一致的 arm**

`lanekill_commit.py` 为 `l5combo` 留了一个 **VETO** 带:`>=2` 敌在 700u 内 ⇒ 源码
`if nCloseEnemies >= 2 then return nil end` ⇒ **门在这些帧上可证为假**,而几何在其余方面同类。

三粒配对种子(55 局)上按铁律 4 (i-d)/(i-e) 形成的 arm(**先每粒 swap-average,再跨种子算术平均**):

| 带 | arm[kill] | arm[switch] | arm[no_dmg] |
|---|---|---|---|
| `l5combo` **ACTIVE**(门可真) | −0.106 spread .103 **0/3** | +0.033 spread .178 2/3 | +0.057 spread .221 1/3 |
| `l5combo` **VETO**(门**可证为假**) | **+0.298** spread .355 **3/3** | **+0.022** spread **.010** **3/3** | **+0.039** spread .044 **3/3** |

⭐ **安慰剂带上的效应更大、跨种子更一致。** 再加上第二条:
**`arm[episodes/局]` 在三个带上全部为正、全部 3/3**(ACTIVE +0.80 / VETO +1.43 / OUT +1.09)
—— armed 腿**在哪里都**更常出现在「低血敌人在身边」的几何里。
⇒ **本语料里没有任何一个 arm 能记到这两个 gate 名下。** 51 个 id 同开,缺的是**隔离**。

**这不是「没读数」,是「读数归不了因」** —— 与 W85 `arbheart` 同型(两机制都支持同一结论、分不开),
⛔ **不是** `l1xpsoak` 那种「域在构造上不可达」。

### ⭐⭐ 与本行立案句的关系(§FW.2)

立本行的现场是:fixture 路线在同一对 helper 上读到 **0 fires**,而那个 0 是**仪器状态**
(出向爆发估计 ally→enemy 恒为 0),不是域为空;裁定逐字担心的是
「谁下一次伸手去拿 fixture 这条便宜路,都会重新导出同一个 0,并写下同一个错结论」。
**本轮从波次录像这条路读到的是 2205 个 episode。⇒ 域不空,那句错结论被挡住了。**
⛔ **但归因没买到,因此不是 WORKING。两件事要分开写,合起来写就又是一次 §FW.2。**

---

## 三、逐帧(硬规则:先逐帧后聚合)

### 3.1 钉帧 —— **控制带是漏的**,而这条**削弱的是我自己本来想用的论证**

`lanekill_commit.py` 的 `OUT` 带 = `t ∈ [520,900)`,docstring 逐字称
「both helpers hard `return nil` (IsInLaningPhase)」。源码
(`bots/FunLib/jmz_func.lua:14297`)是 `nFloor=480`、**`nSoftEnd=600` 且净值 `< 8000` 仍为真**
⇒ `[520,600)` 上净值低的英雄**还在对线期,helper 没死**。

**实测(配对语料 OUT 带 814 个 episode):armed 128/474 = 27.0%、baseline 86/340 = 25.3% 落在漏里。**
**钉帧(直接读 timeline,非经量具):`…_8bd33f` / `20260907_062353_slot7`,`t=585.4`,
`npc_dota_hero_spirit_breaker`,`net_worth=3124`,`level 9`,`hp_pct 0.228`。**
⭐ 时基已核:dumper 的 `t` 逐字「game-clock seconds (0 = horn)」(`dumper/main.go:20-21`),与 `DotaTime()` 同基。

⚠️ **为什么这条重要**:`l1trade` 唯一看着像带特异的那一格(`arm[commit]=+0.072`,spread **.044**,3/3,
而 OUT 控制带 `−0.005` 骑在零上)**本来是本轮唯一的 WORKING 候选**。
控制带一漏,那个对照就不再干净 —— **本轮的判词因此从 WORKING 退到 INDETERMINATE,理由是我自己量出来的。**

### 3.2 钉帧 —— `commit` 可以在**演员整段不动手**的帧上为真

`…_f7fa97` / `20260907_062353_slot7`(种子 7144,armed=radiant,⛔ 不进 arm),**`t=72.4`**,
actor = necrolyte(core,armed),victim = skeleton_king(hp 35.1%,676u)。量具读 `commit=True`。
逐帧 71–77s:SK 往北走(y 5503→6109),necrolyte **反向**(y 5078→4696),距离拉开;
necrolyte 该窗口 `ABILITY` **0 次**、对 SK 自动攻击 **0 次**;
打到 SK 的伤害**逐条全部**是 `necrolyte_heartstopper_aura`(10 次;同段它对小兵打了 50 次)。
⇒ **`commit=True` 在这一帧记录的是「站着没动」。**

**量成读数**:配对语料 859 个 `commit=True` 的 ACTIVE episode 里,**无任何自动攻击**的占
`l1trade` armed **22.5%** / baseline **22.4%**、`l5combo` armed **25.6%** / baseline **31.5%**;
只由一个非攻击技能单扛的最大单格是 `necrolyte_heartstopper_aura` **18 例**(armed)、
`skeleton_king_hellfire_blast` **17 例**(baseline)。
⭐ **诚实登记:这份配对语料上污染大体两腿对称** ⇒ **它是量具缺陷,不是本轮任何结论的解释**。
(未配对的 7144 上是 39.5% vs 21.4%,反号且不对称 —— 这正是为什么它不该进 arm。)

### 3.3 逐帧 —— 设计中的「4 秒粘滞锁」两条腿上都看不见

1152 个 ACTIVE episode 里,「强锁」形状(`atk_n>=4` 且窗口内没打别的敌方英雄)共 **4 个**:
armed 3 / baseline 1。逐帧看 armed 最干净的那个(`…_5acea8` / `20260907_064849_slot2`,
`t=331.5`,sniper→viper,6 次自动攻击,victim `t=335.2` 死)——
**它不是线上 2 打 1**:同窗口 sven / zuus / witch_doctor / crystal_maiden 全在场各自开技能,
viper 最后是被 `npc_dota_badguys_tower1_bot` 打死的。**是团战,不是 gate 描述的那件事。**
`split`(锁被打破)两条腿都在 **19%–44%**。

---

## 四、⭐ 这一行有**到期日**,而 registry 里没有字段装它

- 两条 id **09-07T19:xxZ 已退集** ⇒ **此后没有任何一波 arm 它们** ⇒ **不会再有新语料**;
- 已归档 `.dem` **约 2026-10-02 到期**(距今约 **15 天**)⇒ **现存语料也在倒计时**。

⇒ **形状:一个仍然欠着的条件 (a),它唯一可能的语料有到期日,而 owed 行不知道。**
与 W86 的 **GH #871 同类反号**(那次是「证据已交付而 owed 行不知道」)。
**保守默认(本轮采用)**:接受 INDETERMINATE 作为本行交付 —— 本行 `done_when_note` 的
裸读验收句要的三样(两条 helper 各自的 `VERIFY` 行 / 读的是**波次录像**而非 `tests/fixtures` /
ab/ba 两个分层的读数)**逐条满足**,⛔ 它**没有**要求结论是 WORKING。
**要 WORKING/BUGGY 就得发隔离波 ⇒ 球在批测台,MTD 在刹车线上 ⇒ 本轮只登记不请求(铁律 1)。**

---

## 五、铁律 4 计量条的登记

- **(i-a) 两个分层读数全登记**:见投递物 §3(四支 × 六量),含 `l1trade` `commit` **两层反号**
  (radiant d=−.083 / dire d=+.128)。
- **(i-e)(甲)**:层内 delta **只是中间量**;估计量 = `arm`,先每粒种子 swap-average、再跨种子算术平均
  ((i-d));share 类量走 `strata.per_seed_share_arm`,每局计数走 `per_seed_arm`。
- **(i-c)**:反号**不是**否决理由;管精度的是 arm 自己的跨种子离散度 —— 本轮据此**否决了**
  `l5combo` ACTIVE 的 `arm[commit]`(spread **.448** > |arm| .091)与 `arm[commit_attack]`(spread .358)。
- **配对前提自证**:`strata.pairing()` 判 **paired=True**,种子 `['s7154','s7156','s7162']`,`unpaired=[]`;
  ⚠️ **含 7144 时 paired=False**(它无 dire 腿)⇒ **那 10 局不进 arm**,读数照 (i-a) 单独登记。
  ⛔ 这不是「一律降级」,是 (i-e) 的前提在本语料上逐字不成立。

⭐ **顺带量到、但本轮不开单的一条(交给批测台在自己的语料上判)**:
`validate_onspot.sh:88-89` 是**先 radiant 波、再 dire 波**,所以一台被时间帽/抢占截断的实例
**丢掉的恒是 dire 腿**。本波 4 台里 **3 台**是这个形状(dire 腿 0 / 2 / 1 局)。
⇒ **「截断只丢局数」是错的:它丢的正是消侧偏所需的那一半**,而铁律 4 (i-a) 登记的
`ab_games`/`ba_games` 只显示不对称、不显示它**系统性偏向同一侧**。

---

## 六、本轮改了什么

- **新增** `tools/batch_test/behavioral/lanekill_strata.py`(只读层,**不重建域**,逐字复用
  `lanekill_commit.scan`;加 `arm_side`、两层读数、`strata.py` 的配对 arm,**控制带用同一估计量**)。
- **新增** 投递物 `iterations/reports/replay-check/a_evidence_lanekill.md`。
- **补** `iterations/owed_executions.json` 本行的 `unmet_at_ruling` 证词(**一行 diff**,
  ⛔ 未重排全文件 —— 第一版用 `json.dump` 重写导致 2244±2241 行的假 diff,已 `git checkout` 回滚后改为定点编辑)。
- ⛔ `bots/` + `game/` **一行未改**,零新 soak id,零 fixture。

---

## 七、issue(净增 3,评论 0)

⛔ **都在 push 之后才发(GH #290)**,草稿先过 `claim_precheck.sh`。

1. **GH #874 [harness]** `lanekill_commit.py` 的 `OUT` 控制带在 `[520,600)` 上不是零通道
   (27.0% / 25.3%;钉帧 §3.1)。验收:`OUT_START` 抬到 600 或按 `net_worth>=8000` 过滤后漏率读 0。
2. **GH #875 [harness]** `commit` 把无决策伤害(光环/被动)算作 commit,而 per-seed 表与 DiD 用的正是 `commit`;
   干净的量 `commit_attack` 就在同一张表三行之外。验收:新增 `commit_decision`,
   §3.2 那 18 例 `heartstopper_aura` 单扛的 episode 读 False。
3. **GH #876 [bug]** owed 行可以比**唯一能满足它的语料**活得更久 —— 本行实例:id 已退集(不再有新语料)
   + `.dem` 约 10-02 到期。建议 registry 加一个「语料到期」字段或由总监在到期前裁定。

⛔ **没给 `l1trade` / `l5combo` 本身开单**:核验结论不是病例。

---

## 八、成本(铁律 1 三段式)

**零 EC2 / 零 CE / S3 读取 156 个对象(出网未计价)**
= 89 个 `.analysis.json`(16+24+24+25)+ 65 个 `.dem`(dump 后即删,`dem/` 目录实读 0 个残留)
+ 1 个手工 `s3 cp` 的 `.analysis.json` + 1 个 dumper 二进制(**cache HIT**,`key 46fe9c6a2b084f9b`);
另 **5 次 `s3 ls`**。容器落盘 **1.7 GB**(`.sweep_out/`)。
`AWS_SETUP_EXIT=0`,身份逐字 `AWS ready: arn:aws:iam::581436534924:user/dota2bot-agent`。
⛔ 不写「零支出」(RULING 48)。⛔ 未启动任何实例,未调用 `ce get-cost-and-usage`。

---

## 九、⚠️ 工具坑

- **`routine_selfcheck.sh` 的两道闸又各挡我一次** —— `| tail -40`(自报**第 6 次**复发)
  与 `timeout 600`(自报**第 4 轮**复发)。**W83–W87 连续五轮同形。**
  ⭐ 两道闸**本身工作正常**(正确拒绝 + 给出正确处方,第三次照处方 `nohup … > /tmp/sc.log 2>&1 &` 才跑起来)。
  ⇒ **坑在处方没进入本组的开工动作,不在闸** —— 与 W86 的措辞逐字相同,**说明写进报告并不能改掉它**。
- **`python3 -m pytest` 在本容器不存在**(`No module named pytest`);
  `tests/test_detector_source_constants.py` 要**直接 `python3 tests/…` 跑**(EXIT=0)。
- **同名局跨 run 撞车是真的**:`20260907_062353_slot7` 在 `f7fa97` 与 `8bd33f` **都存在且是不同局**
  (种子 7144 vs 7154)。本报告每一处钉帧都带 run 尾号,⛔ 不要只引局名。
- **`json.dump` 重写 registry = 2244 行假 diff**(indent 与原文件不一致),已回滚改定点编辑。

---

## 十、下一轮第一件事

0. ⭐⭐ **W86 交棒第 0 条继续执行,但改写它的用法**:那一步是**判别子**不是免检通道 ——
   本轮它读到的是「从未存在」,和 W85/W86 的「已交付」是相反答案,**两种都要用同一步买**。
1. ⭐ 本组 owed 名义剩 **2 行**(`campbind_condition_a_fixture` / `outlatch_condition_a_fixture`,
   均 path_exists 且路径不存在)。⛔ 按第 0 条,这 2 行**都还没被核过是否已交付**;
   ⚠️ **但两者都已有历史 VERIFY 行(campbind 2 条 / outlatch 5 条,全 INDETERMINATE)**
   ⇒ **它们是 W86 那一型(理由可能过期)的候选,先核再动手。**
2. ⭐⭐ **本轮的方法学结论对整个「行为检测器买 (a)」这条路都成立,不只对这两条 id**:
   **51 个 id 同开的波次里,任何检测器 arm 都归不了因,除非那个 id 有一条可证为假的安慰剂带
   *而且那条带的 arm 确实为零*。** 本轮量到的 `l5combo` VETO 带是**第一个反例**。
   建议做成本组的固定动作:**每次报 ACTIVE 带 arm 时,必须并排报同一估计量下的安慰剂带 arm**。
   ⛔ **编排权在总监**(是否写进章程/铁律由总监裁)。
3. ⚠️ **`.dem` 约 2026-10-02 到期(距今约 15 天)** —— 本轮三条钉帧所依赖的四个 run 都在这批里。
   任何要复现本报告的轮次**必须在到期前做**。
4. 仍欠未动,原样继承 ⛔ 不许读成已结清:**`wkqdmg` 要局数不要深度**、`66.7%` vs `29.4%` 更宽复读、
   **换句柄英雄**(W80–W86 边界)、`pullcad` 收紧域(总监)、GH #849 验收口径(总监)、
   W84 §四「折叠 key 合同变成闸」(总监 [harness])、W86 §「promoted default 的例行可见性」(总监编排)。

---

## 十一、push 读数(三条腿)与 token

`ARM_EXIT` —— 由开工自检自己上膛,`core.hooksPath=.githooks`(实读)。
⛔ **未用 `RULE6_BYPASS`,未用 `-c core.hooksPath=/dev/null`。**

- **手工先跑一次静态门**:`GATE_EXIT=0  CLEAN`(`luacheck bots game: 0 warnings`)。
- **`REBASE_EXIT=0`**(`git pull --rebase origin main`,`b4a81169..2dc5daef`)。
- **第 1 次(分支推)**:`GATE_EXIT=0 CLEAN` / **`py gate: 130 ran, 0 findings, 0 uncertifiable, 53.2s`** /
  `lua gate: scope = 5 path(s) changed vs origin/main` → **`SKIPPED BY SCOPE`**
  (⛔ **范围判定不是通过**)/ `PUSH_BRANCH_EXIT=0`(`* [new branch]`)。
- **第 2 次(`HEAD:main`)**:同三行,`PUSH_MAIN_EXIT=0`,main **`2dc5daef..a45197ba`**。
  ⚠️ **本轮没撞 non-fast-forward**,故没吃 W84/W85 记的那 ~9 分钟。
- ⭐ 按 W73 处方核权威性:**main / 分支 ref / 本地 HEAD 三者同点 `a45197ba`**(`git ls-remote` 实读)。
- ⭐ **推序按 RULING 69 / GH #865:先分支、后 main**,⛔ 不反序。

**开工自检(铁律 10)**:`nohup … > /tmp/sc.log 2>&1 &`,**worst exit 3**,`legs run: 13`。
`FINDINGS (exit 3): cadence queue-rulings owed-executions lua-coverage`;
`UNCERTIFIABLE (exit 2): trunk-red(python)`;
`NOT RUN (inside a leg): tests/test_lua_gate.py tests/test_luacheck_gate_soakswitch.py tests/test_selfcheck_lua_leg.py`。
⛔ **`UNCERTIFIABLE` 不是通过** —— 那几条腿这轮**没人看过**(容器当时还没有 `lua5.1`/`luacheck`;
它们是在**其后**由推送闸自己装上的,⇒ 自检那一刻的 UNCERTIFIABLE 是真的,⛔ 不能用推送闸的 `GATE_EXIT=0` 追认)。
`owed-executions` 那一条**本轮由我自己造成又由我自己修掉**(创建投递物令本行 OWED→BORN-DONE,
补 `unmet_at_ruling` 后该行读 **DONE**;⛔ 没抬 ceiling、⛔ 没 BYPASS)。

**issue**:**净增 3,评论 0** —— **GH #874 [harness]**(OUT 控制带在 `[520,600)` 上不是零通道)、
**GH #875 [harness]**(`commit` 含无决策伤害而 per-seed / DiD 正用它)、
**GH #876 [bug]**(owed 行可以比唯一能满足它的语料活得更久;与 GH #871 同类反号)。
三份草稿 `PRECHECK_EXIT=0`,逐字 `OK to publish: every citation resolves on origin/main`;
⭐ **三条都在 push 之后才发(GH #290)**。

**token**:`TOKENS total_in=20,881,002 out=90,854 turns=122`(统计后的收尾回合不计入)。
⚠️ 偏高**来自语料与计算,不在上下文**:4 次 `sweep_run.sh`、65 局 dumper + `detect.py`、
2205 个 episode 的三遍全量重扫(`lanekill_commit` / `lanekill_strata` / 两个探针)、
814 个 OUT episode 的时基普查,外加一次**约 1 小时**的开工自检同容器抢 CPU。
