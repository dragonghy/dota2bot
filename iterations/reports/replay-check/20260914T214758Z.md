# replay-check 2026-09-14T21:47Z — 交棒第 (2) 条交货:自由等级修正喂回 `ability_exhaustion_split.py`

**一句话**:修正落地,**#822 的 `PROVEN 229` 与 `OD-INDETERMINATE 18` 逐位不变**(如预告,
由构造),`banked<0` 那 35 行按预告变了 —— **但变的方向是个陷阱:6 行不是被修好,是滑进了
一个写着「tiers exactly account」的桶里**。本轮因此多交了一件交棒没要的东西:
**一个不看符号的连锁检测器 `is_linked()`**,它把那 35 行原样捞回来。

```
VERIFY id=skillstall verdict=NOT-ARMED episodes=282
```
(`episodes=282` = W40 八局 18–22 带内的 skipped-alive **行数**。⚠️ 铁律 4(iii) 连切法登记:
与 09-14T18:45Z 的 `80`(1 级自由等级,按身体计)、15:48Z 的 `282`(**同值同量**,同一张表)
不是同一个量的有:`1778`/`80`/`10`/`6`。本轮与 15:48Z 那个 `282` **是同一个量**,
这正是它能当尺子用的原因。)

---

## 一、覆盖与语料

- **宽扫 8/8 局**(达章程 6 局下限)+ **深查 2 具身体逐帧走账本**(slot1 nevermore idx=1352 /
  slot1 VS idx=1243)+ **80 具身体的自由等级与连锁性普查**。
- 语料仍 W40(`dem21/spot_20260902_214528_1_8a5f8f31…_ab6c0d/` 的 `2146xx` 批 slot1–8),
  与前三轮**同波同树**(`8a5f8f3`)= **波内复现**。读的量(带内 282 行的 banked 分桶)
  与 15:48Z 同量 ⇒ **这一轮是一次受控的前后对比,不是重跑**。
- 连续第 **十七** 轮无新波次:S3 `soak/` 最新仍 `spot_20260912_0926*`(W67)。
- `DL_EXIT=0` 8/8,`DUMP_EXIT=0` 8/8,unparseable 0/8。`AWS_SETUP_EXIT=0`,
  **只读 S3(8 个 `.dem`),零 EC2 / 零 CE / 零支出**。
- ⛔ **`bots/`/`game/` 一行未改,零新 soak id。**

## 二、⭐ 先标定尺子,再动它

改动前用**未修改的工具**跑同一 8 局,逐位复现 #822 已发表的读数:

```
skipped-alive rows in band  282
  PROVEN                    229
  banked>0 无 basic gap      18
  banked==0                   0
  banked<0 (LIMIT A)         35
```

改动后(`banked = 等级 − (可见和 − free) − 已达档位`):

```
  PROVEN                    229   ← 逐位不变(交棒预告:由构造)
  banked>0 无 basic gap      18   ← 逐位不变(OD free=0)
  banked==0                   6   ← 其中 LINKED 6 行
  banked<0 (LIMIT A)         29
  on a LINKED body, any sign 35   ← 新检测器,与修正前的 LIMIT A 集合**逐位相同**
```

**方向就是全部论证**:`free ≥ 0` ⇒ `banked` 只会变大 ⇒ **PROVEN 行永不被撤回**,
#822 的 229 当时是下界、现在仍是下界。173/282 行的身体带自由等级;
**0 行缺 1 级窗口**(缺了也只按 `free:=0` 记,仍是下界,不是猜)。

## 三、⛔⛔ 本轮最贵的一条:修正**换掉**了一个检测器,而换法是静默的

35 行里 **6 行滑到了 `banked==0`**。那个桶的标签是「tiers exactly account」——
**读起来像「账平了」,实际上是「仍然虚高,只是虚高的量恰好等于缺口」**。
连锁身体的虚高**随每一点增长**(3 点 → 9 个可见等级),而 `free` 只在 1 级买下 2 个
⇒ 修正**买不回**连锁,只把它推近零点。**不加处理的话,这次修正就是拿一个响的检测器
换了一次静默的误标,而且恰好换在它修不了的那批身体上。**

**判别子从帧上买,不用民俗**(slot1,逐帧):

| 身体 | 帧 | 账本 | 读法 |
|---|---|---|---|
| nevermore idx=1352 | `t=-66.9` lvl 1 | `[]` | 1 级、还没花点 |
| | `t=-59.9` lvl 1 | `shadowraze1/2/3 = 1/1/1` | **1 点发出、3 个等级站着** ⇒ free≥2 |
| | `t=82.1` lvl 3 | `1/2/3 → 2/2/2` | **三行同时动** ⇒ 连锁 |
| | `t=168.1` lvl 5 | `→ 3/3/3` | 同上,虚高在长 |
| VS idx=1243 | `t=-68.9` lvl 1 | `revenge 1` | 花点**之前**就站着 |
| | `t=-59.9` lvl 1 | `+ wave_of_terror 1` | 动的是 wave ⇒ revenge 是**天生的** |
| | 全局 | `revenge` 此后**再不动** | 天生行的指纹 |

⇒ **`is_linked(body)` = 1 级站着的行里,后来还动的 ≥ 2 个。**
天生行后来**不动**(VS `revenge`、WD `gris_gris`、SK `innate_vampiric_spirit`、
BB `prickly`、SS `fowl_play`,八局逐位相同,各只有**一行**后来动 = 买的那行);
连锁行**整组同步动**。八局普查:**35 行判 LINKED,全部 nevermore**,
**等于该带内 nevermore 的全部行数**,且 **PROVEN / INDETERMINATE 里一行都没有**。

## 四、顺带两条(都在 `tools/`,都不改判读)

1. **身体选择改成「活得最久」**(LIMIT E)。上一轮在 `free_rank_census.py` 写下
   「按 idx 取是必要不充分」,但**没回头改这个文件** —— 它当时用的是「名字第一次出现的 idx」。
   ⭐ **本轮实测:W40 八局 80/80 具,两种取法取到同一具**,所以**本轮任何数都没动**;
   改它是为了下一份语料(幻象先出现的那份)。⛔ **这条 80/80 是本轮量的,不是测试断言的**
   —— 测试用合成幻象分开的是**规则**,语料分不开规则,所以语料也认证不了规则。
2. **同一份语料连跑三次,每英雄表的并列顺序会变**:`bodies()` 走的是 `set`,
   str 哈希逐进程随机 ⇒ `shadow_shaman` 与 `bristleback`(同为 27)换位,
   **像个发现其实不是**。已改 `sorted()` + 稳定的并列键;**连跑三次逐位相同**,
   前 8 行计数与改前**逐位相同**。

## 五、验收与变异台

- 新测试 `tests/test_exhaustion_free_correction.py`(`[ratchet] [bug]`,20 条断言,**实测 0.028s**)。
  ⚠️ 它**不在** `py_gate_manifest.json` 里(该文件头注写着 **do not hand-edit; re-measure**),
  所以它按 GH #616 的 fail-open 走:**每次 push 都会跑,记在 0.03s 的未计量额度里**,
  下一次 `py_gate_measure.py` 重测会把它选进去(远低于 3.0s 的 per-test cap)。
- **变异台 6/6 全杀**,每次**从树外副本还原 + `sha256sum -c` 打 OK**:
  M1 `free` 少减 1 / M2 `free` 读 1 级窗口的 **min** 而非 max / M3 连锁阈值 2→1 /
  M4 `banked` 去掉 `− free`(回到 #822 的算术)/ M5 `bodies` 改回 `min(idx)` /
  M6 缺窗口时谎报 `(0, True)`。
- ⛔⛔ **变异台自己先骗了我一次,这条比六个变异体都可迁移**:
  **M2 与 M3 第一轮都「活下来」了,而它们其实根本没被执行。**
  两者都是**等长替换**(`max`→`min`、`2`→`1`),文件 size 不变、改动与还原在同一秒内,
  于是 `__pycache__` 里的 `.pyc` **被判定为仍然新鲜**,跑的是旧字节码。
  M1(删了 ` - 1`,size 变了)照常红 —— **所以失败是沉默且有选择性的**。
  ⇒ **纪律:变异台每跑一个变异体前 `rm -rf __pycache__`(或 `python3 -B`)**,
  否则「变异体存活」与「变异体没跑」**在输出上逐字相同**,而后者会被写成
  「断言不够强」并去弱化断言 —— 正好是 evidence-discipline 第 2 条警告的反方向。
  本轮按第 2 条先查了断言:M2 的存活**另有一半是真的** —— 合成身体每级两帧、账本相同,
  `min == max`,**语料恰好同意错规则**。补上**花点前那一帧**(真实形状:VS `t=-68.9`
  只有 revenge)后,M2 在 pycache 修好前就已经该红了。两半都记下来。

## 六、issue

**净增 0**。1 条评论发到 **GH #822**(本组自己那条),内容 = §二 的前后对比表 + §三 的判别子。
⛔ 用 `add_issue_comment`,**不碰 `issue_write`**;按 GH #290 **发在两次 push 之后**。

## 七、下一轮第一件事

1. ⛔ **别重做本轮任何一项**:`min(idx)`、corpus-max、pycache 假存活三个具名失败都已进头注释/本报告。
2. ⭐⭐ **最便宜的下一格:`is_linked()` 现在能把 nevermore 那 35 行从判读里干净剔除,
   于是「nevermore 到底停摆没有」第一次**可以单独问** —— 用 `free` 做等差修正跑不动它
   (连锁虚高随点数长),但**「连锁组的可见等级 ÷ 组大小」**是一个可买的量,**零新波次**。
3. ⚠️ #822 §源码算术 第 3 点那个「23 = 23」巧合**仍等人证伪,证伪前不许引用**。
4. ⚠️ OD 那 24 s 残差**连续第五轮挂账**;09-13T16:30Z 的 n=1 复现**连续第十轮挂账**。
5. ⛔ 钉任何帧之前先把该 issue 的评论读完(本轮 #822 读到 **1 条评论**,见 §六 前的复核)。
6. ⛔ 引本轮任何数**必须连切法一起引**;每英雄那张表**只作选点不作结论**(LIMIT C)。

## 八、token 用量

(收尾时回填)
