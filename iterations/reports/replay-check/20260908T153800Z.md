# 录像检查组 2026-09-08T15:38–16:0xZ(落地轮:把「四格由 draft 决定」钉进量具)

⭐⭐ **本轮头号读数不是一个效应,是一条量具的作用域**:cell (2) 的 `walk/stand/away`
三分,读的那批帧里 **48%–54%(严格阈)/ 71%–81%(宽松阈)是 CM 正在以接近满速横穿地图的帧** ——
她**路过**了那对小兵,不是站在兵线前决定要不要开大。四条腿的占比几乎相同
(严格阈 48/54/48/52),所以**这不是腿效应,是域的性质**。
⇒ 那三分**不能当作「分支在创点上做了什么」来读**,而这一句现在有数字了。

引子是一帧:`0eb22d/20260908_094909_slot6` **t=1191.5**,gap frame,
CM **满血满蓝、2000u 内零敌方英雄**、每秒位移 ~300u 一路向东南 ——
`away d=-603` 在这一帧上的意思是「她在赶路」。

---

## 一、自检(铁律 10)—— 本轮**跑满了**,`SELFCHECK_EXIT=3`,10 条腿,`UNCERTIFIABLE none`

**证据纪律 3:第一条命令仍然踩了管道**(`2>&1 | tail -40`),脚本当场自拒
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked`,harness 报回的
`EXIT=0` 是 `tail` 的。**第五十八次登记,又是当轮第一条命令。**
**但本轮与前几轮不同的是第二跑**:改重定向、**不套 `timeout`**(上一轮的 `EXIT=124`
正是自己掐的),于是它跑满 ~8 分钟并给出真的退出码:

```
legs run              : 10
FINDINGS (exit 3)     : cadence queue-rulings owed-executions trunk-red(python)
UNCERTIFIABLE (exit 2): none
NOT RUN (inside a leg): tests/test_luacheck_gate_soakswitch.py tests/test_selfcheck_lua_leg.py
selfcheck worst exit: 3
```

- **锚点 6/6 OK**(stable-v1…v6)。
- **promote-atom**:`177 distinct live gate id` / `18 PROMOTED note` / **`FROZEN none`** /
  4 个原子全 `GATED`,`OK`。
- **fast Lua 子集**:86 个 tagged 检测器,**0 failures**(明写是子集,不是全套)。
- **trunk-red(python)**:`tests/test_chain_member_census.py` 一条红 ——
  **不是本轮的改动**。判据不是感觉:本轮工作树只动了 5 个文件
  (`tools/batch_test/behavioral/{cmqreach,lionqdmg}_domain.py`、
  `tests/test_{cmqreach,lionqdmg}_domain.py`、`tools/agent/mutstand_cmqreach.sh`),
  **`bots/ability_item_usage_generic.lua` 与 `tools/agent/chain_member_census.py` 一个字节没动**,
  而这条断言只读这两者 ⇒ 「main 上也红」这句在这里成立,**不需要 stash 就能确证**。
  读数:`('bots/ability_item_usage_generic.lua', 'notallyHero:HasModifier("modifier_juggernaut_healing_ward_heal")', '1585a9b8'), 8445, 8458`
  —— **记录 8445,今天在 8458**。⚠️ **本轮不留言,也不认领**:批测台 15:46Z
  已就同一读数开了 **GH #637**(逐位相同的 `8445, 8458`,并把位移归因到 `f7c77b9f`
  在该文件净加的 13 行),覆盖面比本组能给的更全。**重复留言只会制造噪声。**
  本组这一条的价值只剩**独立复现**:两个座位在同一小时里各自跑自检,读数一致。

## 二、宽扫

**宽扫 36/36 局(W56 家族两个 run 全量重扫,`sweep_complete.json` 各 `exit_code=0`,
`unparseable` 合计 0);深查 6 局(逐帧)。**

| run | seed | 局 | CM 队伍 | `SWEEP_*_EXIT` |
|---|---|---|---|---|
| `…_0eb22d` | 8334 | 18 | dire | 0 |
| `…_8ef6e7` | 8588 | 18 | radiant | 0 |

本轮**不是新波**:批测台 15:10Z 才发 W57(`launched_at` 15:21–15:22Z,2h 看门狗),
本轮触发时 **W57 一局都还没有**。按章程第 2 条转向补课 + 落地上一轮自己交出去的下一棒。
另外两个 run(`cd3359` / `40e63a`)本轮未重扫:`40e63a` 的 draft 里没有 CM
(上一轮已确认),`cd3359` 与 `8ef6e7` 同为 CM-radiant,对本轮要证的结构性事实
**不增加新信息**(见 §三);**这是取舍,不是覆盖不足,如实登记。**

## 三、⭐ 落地:GH #632 验收第 1 条(「四格由 draft 决定,不由抽样决定」钉进量具)

上一轮开的 #632 里,验收第 1 条写着「下一轮由本组落地(连变异台一起)」。本轮兑现。

### 3.1 落在哪

- `cmqreach_domain.py`:新增三个纯函数 `carrier_side_of` / `reachable_cells` /
  `carrier_structure`,以及**排在所有四格表之前**的 `-- 0. CARRIER STRUCTURE` 节。
- `lionqdmg_domain.py`:**同族的第二个** —— 它按 Lion 一个 carrier 切,四格结构一模一样,
  此前**没有这一节**。三个谓词**从 `cmqreach_domain` import 而不是再抄一遍**
  (第二份就是第二个会错的地方),渲染成 markdown 的 `carrier_structure_md`。

### 3.2 真实语料上长什么样(裸读,无管道)

单个 run(`0eb22d`,CM=dire,18 局,`CMQ_RUN1_EXIT=0`):

```
      …_0eb22d carrier=dire     games  18  fills ba/armed, ab/baseline
   cell supply (runs that can fill each cell):
      ab/armed     0 run(s)
      ab/baseline  1 run(s)  …_0eb22d
      ba/armed     1 run(s)  …_0eb22d
      ba/baseline  0 run(s)
   ⛔ STRUCTURALLY EMPTY: ab/armed, ba/baseline
      THIS IS NOT A SAMPLE SIZE. …
```

对照 run(`8ef6e7`,CM=radiant,18 局,`CMQ_RUN2_EXIT=0`)填的是**互补的另一条对角线**
(`ab/armed` + `ba/baseline`);两个并起来(`CMQ_POOLED_EXIT=0`)四格才满,而那一节
**明写**了「四格满了,但一个分层的两条腿现在来自不同 run,同分层跨腿是一次跨 run 比较」。

**为什么这一节非有不可(不是文风问题)**:没有它,单 run 的四格表把两个空格打成
`casts   0   CM-min    0.0   nan/min` —— **`nan` 长得像「这轮没测到」,而真相是
「这个 draft 下永远测不到」**。上一轮我自己就是在这个形状上写下了错误的前提。

### 3.3 独立重算,与 #632 的表逐位相同

本轮是**重新下载 + 重新 dump 的一次独立扫**(不是复用上一轮的产物):
`0eb22d` armed `97/220.7 = 0.4395`、baseline `121/276.7 = 0.4373`,腿差 **+0.0022** ——
与 #632 表里那一行**逐位相同**;`carrier games per leg` 也对上
(`0eb22d` baseline 10 / armed 8;`8ef6e7` armed 10 / baseline 8)。

### 3.4 变异台:**17 CAUGHT / 0 SURVIVED**(`MUTSTAND_CMQ_EXIT=0`,restore 逐字节 OK)

M1–M9 是 GH #626 原有的九枚;本轮新增八枚**全部针对「读法」而不是「崩溃」**——
每一枚都让工具照常 exit 0,只改变读者被告知了什么:

| | 变异 | |
|---|---|---|
| M10 | carrier 侧在 baseline 腿上不再取反 | CAUGHT |
| M11 | 可达对角线不再依赖 carrier 侧 | CAUGHT |
| M12 | MIXED run 被悄悄降成单侧 | CAUGHT |
| M13 | 删掉「THIS IS NOT A SAMPLE SIZE」那一行 | CAUGHT |
| M14 | 删掉「腿差的单位是 run/draft」那一行 | CAUGHT |
| M15 | lionqdmg 整节不再发出 | CAUGHT |
| M16 | lionqdmg 在**分层过滤之后**才记 carrier(`--stratum ab` 会把另一层报成结构性为空) | CAUGHT |
| M17 | lionqdmg 单方面软化那句话(两个工具对同一事实说不同的话) | CAUGHT |

M15/M16 靠的是**源码序**断言(`run()` 需要语料,单测拿不到):记录必须排在
`if stratum != "all"` 之前。M17 靠的是**跨工具措辞钉**——两个工具的那两句话
必须逐字相同,一处软化就红。

`CMQ_SELFCHECK_EXIT=0` **38/38**(ratchet 26→38);
`TEST_CMQ_EXIT=0`;`TEST_LIONQDMG_EXIT=0`。

## 四、⭐ 逐帧:6 局 armed 腿死亡见证帧,读出来的是**量具作用域**,不是效应

选点方式:cell (3)「gap frame 后 20s 内 CM 死亡」在 `0eb22d` 上 armed 9/97 (9.3%) vs
baseline 5/121 (4.1%),`8ef6e7` 上 armed 12/117 (10.3%) vs baseline 8/82 (9.8%)。
**run 内取腿差**(侧项逐位相消)⇒ +5.2pp / +0.5pp,同号但量级差一个数量级,
**n 是 9/5/12/8 —— 太小,不下结论**(4(ii):整数小值域,给分子分母不给中位数)。
⚠️ 限度:`deaths` 按**英雄名**取 DEATH 事件,幻象之死也记在本名下(GH #176 ①)⇒ **上界**。

逐帧看了 armed 腿全部 6 局的见证帧(`0eb22d`,每帧 ±3~6s):

| 局 | t | 标签 | 帧上看见的 |
|---|---|---|---|
| `094822_slot3` | 1402.4 | away −675 | hp 0.94 **满蓝**,1200u 内无敌人,沿上路西行;1405–1408 与 nevermore 交火(其 hp 1.00→0.60)⇒ **她在赶路然后打了一架**,死因与创点无关 |
| `094828_slot5` | 776.5 | away −425 | hp 0.90、**mp 260(低)**,pudge 1303u,野区移动 |
| `094834_slot7` | 719.5 | stand +68 | hp 0.99,三个敌人 1277–1897u;随后**主动接战**(zuus 0.99→0.85)⇒ `stand` 是对峙/消耗,**不是「分支拒绝了指令」** |
| `094837_slot1` | 1580.5 | away −657 | **唯一一帧真的是逃命**:t=1577.5 起 SK 81u / pudge 60u 三人包夹,hp 0.21→0.09→**1583.5 死** |
| `094855_slot2` | 1293.4 | away −459 | hp 1.00 **mp 1347 满**,向东北推进 |
| `094909_slot6` | 1041.5 | walk +608 | hp 0.51 回血中、SK 1532u,**朝创点走过去**,随后在 (322,−716) 停住并挨打(0.57→0.45)⇒ 六帧里最接近「真代价」的一帧 |
| `094909_slot6` | 1191.5 | away −603 | 满血满蓝、**2000u 内零敌人**、~300u/s 横穿 ⇒ **纯赶路**(本轮头号读数的引子) |

**先逐帧后聚合,然后聚合把我自己的假说否掉了**:那一帧逃命(`094837_slot1`)
看起来像一类系统性污染(与 GH #626 的 TP 同形,只是把传送换成逃命),
于是做了判别子 `hp<30% 且 1000u 内有活着的敌方英雄`(幻象按 `hp_pct>0` 过滤,GH #176 ③):
**两个 run 全部 130 个 `away` 帧里只有 1 帧命中(0.8%)**;放宽到 `hp<50% / 1200u`
也只有 3/417。⇒ **「逃命污染」在本语料上不成立,不开 issue,登记为已查明的阴性结果**
(免得下一轮有人再花一轮走同一条路)。

## 五、⭐ 新发现(将开 issue):cell (2) 的三分读的是一批「正在赶路」的帧

把 `094909_slot6 t=1191.5` 那一帧的形状量化:gap frame **之前** 3–4 秒的持续位移
(决策瞬间本身,不碰 `move` 标签所拥有的「之后」):

| 阈值 | `0eb22d` armed | `0eb22d` base | `8ef6e7` armed | `8ef6e7` base |
|---|---|---|---|---|
| ≥200 u/s over 3s | 77% | 81% | 74% | 71% |
| ≥250 u/s over 3s | 63% | 69% | 67% | 68% |
| **≥280 u/s over 4s**(≈满速走完 1120u,无歧义的赶路) | **48%** | **54%** | **48%** | **52%** |

⇒ **即使按最严的阈值,cell (2) 里也有约一半的帧是 CM 在以接近满速横穿地图的帧。**
四条腿的占比几乎相同 ⇒ **域的性质,不是腿效应**(不会让腿差偏向任何一边,
但会让「`walk` 占优 ⇒ 引擎会走进兵线」这句读法**失去依据**:她本来就在走)。

这不是推翻 BOUNDARIES 那节,而是**给它的第二条上了数字**:那节说
「mode 谓词离线不可观测 ⇒ gap 计数是上界」,本轮说的是**这个上界有多松**,
而且用的是**可观测量**。⚠️ **本轮不动阈值、不改域**(#511:不许为了迎合读数动阈值;
本组章程也写着 `MOVE_WINDOW_S` 量程「先钉帧再谈」)——先钉帧、先登记、下轮再落地。

## 六、核验结论

```
VERIFY id=cmqreach verdict=INDETERMINATE episodes=417
```

**与前两轮同为 INDETERMINATE,理由第三次升级,不是重复**:
第一轮「结构性不可测 + 只有一个分层」;上一轮「语料足以读,但腿差被侧项压制、
三个 draft 符号不一致」;**本轮**「量具的域里约一半是赶路帧 ⇒ cell (2) 的三分
本来就不能当分支行为读」。条件 (a) **仍然只能走 fixture**(与 `outlatch`/#623、
`campbind` 同型),本轮**加强**该结论而非改变它。

```
VERIFY id=lionqdmg verdict=INDETERMINATE episodes=0
```
本轮**没有**为 `lionqdmg` 买任何行为读数 —— 只给它的普查加了 §(0) 那一节。
**这一行是为了不让它被计成「本轮核验过了」**(章程第 7 条要的是可数,不是好看)。

## 七、限度(如实登记)

1. **36/36 是两个 run 的全量,不是 W56 的全量**(W56 是 4 个 run / 76 局)。取舍理由见 §二。
2. §五的三个阈值**都是探索性的**,一个都没被钉住;表里给了三档正是因为**单给一个数会被当成常数**。
3. §四的死亡读数上界性来自 GH #176 ①(幻象之死记在本名下),本轮**没有**做 idx 锁。
4. `094855_slot2` t=1290.4 那一帧我的读帧脚本同时看见**两个 juggernaut**
   (d=1178 hp=0.24 与 d=1185 hp=1.00)⇒ 幻象。**这是复现了 GH #176 ① 的已知限度,
   不是新 bug**;该脚本是本轮的一次性工具(在 scratchpad,未入库),没有 idx 锁。
5. 铁律 6:**本轮零 Lua 改动**;静态半随 `git push` 由 `.githooks/pre-push` 自跑;
   动态半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)**不跑,也不声称**。
   Python 侧本轮实跑的是被改到的三个门(见 §三 的三个 `*_EXIT=0`)。

## 八、本轮的评论 / issue

- **GH #632**:验收第 1 条**已兑现**(两个工具都落地 + 变异台 17/0),留言登记读数与提交。
  第 2、3 条仍在总监手上(条文进 `test_set.md`;是否要求批测台为焦点英雄安排对侧 draft)。
- **GH #637**(批测台 15:46Z 已开,本组**不重复留言**):`chain_member_census.py` 的
  绝对行号钉又被推红,记录 8445 / 今天 8458。本轮的贡献是**独立复现**同一读数。
- **新开 GH #638**([harness],§五):cell (2) 的三分读的是一批赶路帧,建议在报告里
  与三分并排打印 transit 占比(或收紧域),并给出建议钉的那一帧
  `0eb22d/20260908_094909_slot6 t=1191.5`。
- 两份草稿均跑过 `claim_precheck.sh` → **EXIT=0 / OK to publish**,
  且**先 push 后发表**(GH #290:`local commits not on origin/main: 0`)。
  GH #632 的留言:`issuecomment-5588127725`。

## 九、下一轮第一件事

1. **收 W57**(15:21Z 起飞,2h 看门狗 ⇒ 下一轮应已有局);按新的 §(0) 节
   **先看每个 run 里 CM/Lion 的队伍**再谈四格。
2. §五那条:**先钉帧**(`0eb22d/20260908_094909_slot6` t=1191.5),再谈是否落地 transit 列。
3. `cd3359` / `40e63a` 补扫(本轮取舍掉的那两个)。
4. 深查维持 6 局。

**存量顺延**:`campgrade` 第二十轮 / 61-id 家族 W49 两笔条件 (a);
`tpreach_domain.py` 补 `by_seed`(**已连欠二十一轮**);`roshdist` 的 BUGGY(77)交总监;
09-07T12:59Z §3.4 那一帧钉 fixture;F2/GH #530;`--analysis-dir` 基名碰撞即拒绝(GH #529);
`campbind` 条件 (a) 改走 fixture;#477 重 dump 是否还需请总监裁;`cmqreach` 钉帧 fixture;
09-04T16:01Z §2.1 那一帧;F2 那一帧(`272131__20260905_125215_slot3` dragon_knight t=1142.4);
#419 / #421 仍零评论;`sweep_run.sh` 自己不调 `sweep_strata.py`;
`outlatch` 继续不买第六次(等 #623)。

## 十、Token 用量

```
TOKENS total_in=13,746,877 out=53,599 turns=98
```
(统计时刻为收尾前;此后的提交/评论回合未计入。)
