# replay-check 2026-08-30T07:10Z — `hero-20`:重生触发频率与触发时的敌人数(归档扫描)

**本轮做的是章程尾部编号 (0) 的那一件事**,不是自选题。总监 01:05Z 裁定
(`test_set.md §CH`,`queue.json:hero-20`,`director.executor = replay-check`)
已悬 2 轮,本轮结清。

零 EC2、零新局、零 CE 调用;S3 只读;`bots/` / `game/` **0 改动**。

---

## 0. 一句话结论

**WK 的 t25 定价(留 [7] `..._wraith_king_10`,否 [8] `..._wraith_king_4`)在语料上被坐实,
而且是从"推断"变成了"实测"** —— 但源码里那条自认的 honest bound 有**两处事实是错的**,
必须改:语料里**有** 25 级的 WK(49/71 局),窗口长度**不用**从一局推(实测 n=49)。
另外**论证里三个条件中"900 内有敌人"那条几乎不做功**(触发时 P(≥1)=95.0%),
真正的稀缺项是**触发本身**。

---

## 1. 覆盖数字(章程固定节)

| 层 | 数字 |
|---|---|
| 载体分母(analysis.json) | W17 + W17-R 共 **192 局**,其中 **WK 英雄-局 96** |
| 宽扫(逐帧表全解析) | **71 / 72** 局有 .dem 的 WK 局(1 局 dumper 崩溃,见 §6) |
| 深查(逐帧还原) | **8 局**(章程下限 6):见 §4,每一局都逐帧列了坐标/血量/cd/事件 |
| 触发 episode | **261**(A 识别子),WK 死亡总数 632 |

---

## 2. 识别子:预登记的那个用不了,`acceptance_amendment` 的候选也用不了,第三个可以

`acceptance` (2) 点名 `modifier_skeleton_king_reincarnation`。
`director.acceptance_amendment` (甲) 已在 109 份 fixture 上实测它 **0 次**,
(丙) 给出 `cd` 上升沿作为**线索但明说未证**,(丁) 立硬条款:
**任何识别子得 0 次一律记 `METHOD-FAILED`,不得读成「触发罕见 ⇒ 定价坐实」。**

**(丁) 本轮没有触发,因为语料给了 261 次触发。**但两个候选识别子的命运值得写下来:

- **(甲) `modifier_skeleton_king_reincarnation`:71 局 / 632 次 WK 死亡上仍然 0 次。**
  这不是 fixture 的偶然,是这个名字在当前版本里根本不出现。**修正到此为止。**
- **(丙) `cd` 由 0 变正的上升沿:在真语料上两个方向都错,同一个机制造成的。**
  - **假阴性,`20260827_122211_slot9` t=982.3。**真触发在那儿(ABILITY_TRIGGER +
    DEATH + slow 同在 982.3),但**它前一帧的 `cd` 读 0.5 不是 0**
    (982.0 cd=0.5 → 983.0 cd=**149.7**),`prev <= 0` 的写法整个漏掉。
  - **假阳性,`20260827_123454_slot3` t=452.5。**`cd` 走 0 → **4.3** → 3.1 → 2.3 →
    1.1 → 0.3 → 0,**没有死亡、没有触发**。同期
    `modifier_skeleton_king_reincarnation_scepter_active` **452.0 加、456.5 减** ——
    那 4.3s 是**它**的计时器骑在同一个技能句柄上。
  - ⚠️ **`cd_len` 字段救不了这一条:该技能的 `cd_len` 恒读 4.3**,从来不是真冷却的
    110/150(见 §5 的工具坑)。

**实际使用的识别子 —— ⚠️ 它不是"新立"的,它一直在 trunk 上:**

```
events[] type == 'ABILITY_TRIGGER'
         actor == npc_dota_hero_skeleton_king
         inflictor == 'skeleton_king_reincarnation'
```

它是**战斗日志的事件**,带 0.1s 时间戳 ⇒ **不受 (戊) 那条「必须是 ≥1s 持续态」的约束**,
`wkqdmg`(GH #310)输掉的那条分辨率,这条不输。

### ⭐⭐⭐ 本轮最该被读的一条:这个识别子在 trunk 上躺了 9 天,两轮取证都没找到它

我把工具写完、命名 `wk_reincarn_domain.py` 去提交时,git 说那是一次**修改**不是新增 ——
**同名文件已经在树上**,是英雄组 `8e535741`(08-21)为**另一个门** `wkreincarnmp` 写的
预检工具。我已把它**原样恢复**(`git checkout HEAD --`,零 diff),本轮的工具改名
`wk_reincarn_trigger_domain.py`。

**但真正的发现是那个文件的注释:**`wk_reincarn_domain.py` 的 section C 头
(**`:50-59`**)**已经写着**这两件事:

> C. outcome side -- WK deaths, split by whether Reincarnation actually fired
> (an `ABILITY_TRIGGER` event whose inflictor is `skeleton_king_reincarnation`;
> note that a DEATH event is emitted either way, and that
> `modifier_skeleton_king_reincarnation_scepter_active` is **NOT** the trigger
> -- it also appears with the ability unlearned)

⇒ **正确识别子、以及"`_scepter_active` 不是触发标记"这条否定结论,08-21 就在 trunk 上了。**
而 08-30 的 `acceptance_amendment` **从 109 份 fixture 重新推导了同一对事实**
((乙) 排除 `_scepter` 变体、(丙) 猜 `cd` 上升沿),**并且推歪了**(§2 的两个反例)。

**这不是谁的锅,是检索面的洞**:请求方按 **fixture** 找识别子,总监按 **fixture** 复核,
**没有人 grep `tools/batch_test/behavioral/` 里同一个技能名**。
`grep -rn skeleton_king_reincarnation tools/` 当场就能命中。
**与 GH #171(`lua5.1` 那条腿)、#205(luacheck)同族:不是缺工具,是没人查已有的那一份。**
⇒ 已在 §9 的 issue 里向总监提一条**取证前置动作**(找识别子先 grep `tools/`,再动 fixture)。

**三识别子交叉核验(工具自己打印,不是散文):**

| | 计数 | 与 A 一致 |
|---|---|---|
| A `ABILITY_TRIGGER` 事件 | **261** | — |
| B 冷却跳到**满长**(修正后,阈值 60s) | **261** | **261 / 261** |
| C **原地复活**(纯位置+血量,与技能系统无关) | 264 | 260 / 261 |

**>>> METHOD OK(门识别子 B 在 261 次上逐次一致)。**
C 是**行为侧独立旁证**不是第二个 ground truth;它的 1 个假阴 + 4 个假阳**都已逐帧定因**:

- **假阴 `20260827_123452_slot10` t=1374.2**:站起来后**一帧内被位移 1583u**
  (超过 1500u 门限 83u;同帧 pudge 在 slow 名单里)。
- **假阳 `20260827_122056_slot2` t=1365.2**:5.5s 后原地(77u)满血复活,
  **而重生正在冷却中**(cd 97.9 → 92.7,无重置)⇒ 不是重生(aegis 型)。

---

## 3. 读数(acceptance 逐条)

### (1) 载体分母 —— **`DOMAIN-NOT-REACHED` 分支没有命中**

批测台 08-27T18:25Z 预判「到 25 级为 0 局很可能命中」。**实测相反:**

- WK 英雄-局 **96**(192 局语料);**到 20 级 96/96 = 100%**;**到 25 级 60/96 = 62.5%**。
- 有 .dem 可逐帧的 71 局里:**到 25 级 49 局**,最高 30 级,平均峰值 25.06。
- 局时长 22–27 分钟(均值 1544s),全部 `natural_end`。

**t25 窗口(WK 到 25 级 → 该局最后一帧),n=49:均值 208s(3.5 分钟),min 32s,max 453s。**
t20 窗口 n=71:均值 418s(7.0 分钟),min 67s,max 759s。

### (3) 主读数 —— 触发帧上的敌方英雄数(均值 + 分布,**不报中位数**,铁律 4(ii))

全语料 261 次触发,**触发前一采样帧**:

| 半径 | 均值 | 分布 | P(≥1) | P(≥2) |
|---|---|---|---|---|
| **900u**([8] 的半径) | **2.046** | 0:13 1:72 2:88 3:67 4:20 5:1 | **95.0%** | 67.4% |
| **600u**(出厂 `slow_radius`) | **1.257** | 0:60 1:101 2:77 3:19 4:4 | 77.0% | 38.3% |
| **900−600([8] 相对 [7] 的毛收益)** | **0.789** | 0:108 1:106 2:41 3:6 | **58.6%** | — |

**触发后一采样帧**逐格同号同量级(900u 均值 2.011,600u 1.257,差 0.755);
**相位敏感度 |pre−post| 在 900u 上 208/261 次为 0**,均值 0.234 ⇒
**结论不靠采样相位**(这正是 `wkqdmg` 输掉的那一条,这里过了)。

**仪器标定(不需要相信位置读数):**出厂重生把
`modifier_skeleton_king_reincarnate_slow` 打在 `slow_radius` 内的所有人身上,
**引擎自己报了它的 600u 答案**:均值 **1.245**(位置法 1.257,逐位可比);
逐次一致率 pre **76.6%** / post **85.1%**,不一致处**位置法系统性偏高一格**,
成因见 §4 的 `123442_slot12`。

### (4) 每局触发次数 + 按等级分层 —— **这一格才是 t25 的定价依据**

- 全局:**3.676 次/局**,分布 1:1 2:7 3:23 4:24 5:15 6:1,**P(一局≥1)=100%**。
- 分层(触发时 WK 的等级):

| 等级带 | 触发数 | 900u 均值 | 600u 均值 | 900−600 | P(900≥1) |
|---|---|---|---|---|---|
| <20 | 174 | 1.989 | 1.184 | 0.805 | 96.0% |
| 20–24 | 67 | 2.239 | 1.448 | 0.791 | 95.5% |
| **≥25** | **20** | **1.900** | **1.250** | **0.650** | **85.0%** |

**⭐ 分母要按"到得了 25 级的局"算,不是按全部局:**
49 局到了 25 级,其中 **30 局(61.2%)在 25 级后一次都没触发**,18 局触发 1 次,1 局 2 次
⇒ **0.408 次 / 到 25 级的局**。

**⇒ [8] 在它真正付钱的窗口里的期望毛收益 = 0.408 × 0.650 ≈ 0.27 个"多打到的敌人"/局。**
这是**毛**的:源码第二条论证(1.6s 晕比 `reincarnate_time = 3` 早 1.4s 过期,
而它顶掉的 4s 减速在那一刻还剩 1 秒)**还没从这 0.27 里扣**。

### (5) 腿间对比

**未做腿间对比。**本条主体按 acceptance 是**单侧描述统计**;且 W17/W17-R 整波
spot 回收不计分、`script_version` 有 47/192 是裸 sha(暖场),孤儿腿对单侧统计无损。
**因此铁律 4(i) 的 ab/ba 两层读数本轮不适用,是明写不是省略。**

### (6) 预登记的第三种结局 —— **不成立**

「900 与 600 的分布几乎相同 ⇒ 半径优势是纸面的」:实测
**mean(900) − mean(600) = 0.789**(≥25 层 0.650),**不是 ~0**。
**半径优势是真的,只是小**;定价被坐实靠的是**频率**那一格,不是这一格。

---

## 4. 深查:逐帧证据(8 局)

**支点帧一 —— [8] 在整个 ≥25 语料里最好的那一帧,`20260827_151700_slot12` t=1529.4,WK 28 级。**
WK 在 (−2724,−2471),血 0.060 → 0.000。三名敌人**全部落在 600–900 的环里**:
crystal_maiden **648u**、zuus **671u**、drow_ranger **764u**(post 帧);**600 内一个都没有**,
**引擎的减速名单是空的**(`ENGINE slowed: nobody`)。
⇒ 这一帧出厂重生赔付 **0**,[8] 会打到 **2–3 人**。**49 局里这样的帧出现 1 次。**

**支点帧二 —— 模态形状,`20260827_122201_slot8` t=1406.6,WK 27 级。**
nevermore **121u**、venomancer **131u**、viper **243u**,三人**全在 600 内**;
引擎减速名单 `['viper','nevermore','venomancer']`,**与位置法逐人相同**。
⇒ 900 计数 == 600 计数 == 3,**[8] 在这一帧多打到的人是 0**。
**≥25 的 20 次触发里,900>600 的只有 13 个"人次",7 次触发的增量为 0。**

**支点帧三 —— 标定不一致的成因,`20260827_123442_slot12` t=1516.7,WK 27 级。**
pre 帧 (1516.5) bristleback 距 **44u** 但 `hp=0.00` 判死 ⇒ 位置法 n900=0;
post 帧 (1517.5) 他在 **710u** 处 `hp=0.98`,**引擎把他放进了减速名单**。
⇒ **不一致来自 pre 帧的生死判定,不是距离**;600u 一格**以引擎的答案为准**,
位置法的 900u 读数按 **±1** 读。这也说明 §3 的 900u 若有偏,**偏的方向是低估**。

**另五局**(`122226_slot11` 8 次死亡全序列、`122211_slot9`、`123452_slot10`、
`122056_slot2`、`123454_slot3`、`122046_slot1`)的逐帧轨迹见 §2 的识别子核验,
每一条都附了坐标 / 血量 / `cd` / 事件三行对读。

---

## 5. 给源码的四条更正(已开 GH issue,见 §7)

`bots/BotLib/hero_skeleton_king.lua:382-417` 那段 t25 论证:

1. **`:412` 「no frame in this repo shows a Wraith King at 25」—— 现在是假的。**
   96 个 WK 英雄-局里 **60 局到 25 级**;逐帧语料里 **49/71**。
2. **`:413` 「窗口长度只由 ONE post-cap 局推得」—— 现在有 n=49 的实测:均值 208s(3.5 min)。**
   顺带:GH #235 那局的 23:02 / 26 级**落在实测分布里**,不是异常点。
3. **`:391` 「[8] 需要 WK 死在 900 内有敌人处」这一条几乎不做功。**
   触发时 P(900 内 ≥1)=**95.0%**(≥25 层 85.0%)。三个条件里做功的是**触发本身**
   (0.408 次 / 到 25 级的局),第三条几乎必然满足。**论证的结论对,理由的权重排错了。**
4. **`:391` / `:50` 的 rank-3 冷却 120s:帧上读 ~110s。**
   逐帧实测 rank1 **179**、rank2 **149–150**、rank3 **109–110**(n=3,仅深查 5 局)。
   ⚠️ **这一条是指示性的不是判决**:样本小,且未排除冷却缩减装备。
   **它不改本轮结论** —— 频率那一格是**实测出来的**,不是用冷却建模出来的。

**结论方向:定价维持 [7]。**本组**不主张**改这两行的取舍;只主张把上面四条事实写对。

---

## 6. 工具坑(章程追加,见章程末)

- **`skeleton_king_reincarnation` 的 `cd` 字段被一个 4.3s 的辅助计时器共用**
  (`modifier_skeleton_king_reincarnation_scepter_active`),而 **`cd_len` 恒读 4.3**,
  从来不是真冷却 110/150/180。**"cd 由 0 变正"两个方向都会错**;
  要用就用**幅度阈值**(本工具 `B_MIN_CD = 60`)。
- **`snapshots[].abilities` 在开门前的帧上是 JSON `null` 不是缺键** ⇒
  `s.get('abilities', ())` 的默认值**永远不会生效**,必须写 `s.get('abilities') or ()`。
  本轮第一次宽扫因此 72 局全崩,0 条记录。
- **`pkill -f` 又一次匹配到发起它自己的 `bash -c`(exit 144)** —— 章程已记过 2 次,
  本轮第 3 次踩。**收尸只用 PID**:`pgrep -a -f` 看清楚再 `kill <pid>`。
- 1 局 dumper 崩溃(`20260827_151652_slot11`,manta panic),**不静默**:71/72,
  失败局已点名。

---

## 7. 交出去的棒(铁律 9 连带规则)

本条的下一棒**不在本组**:总监裁词写明「英雄组在本条上的下一棒 = 读数落地后重开
`..._wraith_king_4` 与 `_10` 的定价」。**本轮已把读数交出去**,见 §9 的 issue。

---

## 8. VERIFY 行(章程第 7 条)

**本轮没有可写的 `VERIFY id=<gate id>` 行,而且这不是漏写。**
`hero-20` 是总监指派的**归档测量**,`queue.json` 与裁词都写明**无 gated id**
(「零 EC2、零新局、无 gated id」)——它不是任何 gate 的条件 (a) 核验。

⇒ **本周本轮完成的 gate-id 执行核验数 = 0**,原因如上,**不是散文里数不出来**。

⚠️ **同时这暴露了 VERIFY 模式的一个洞,交总监**:枚举
`WORKING|BUGGY|SILENT|INDETERMINATE` **没有一格容得下"总监指派的非 gate 测量"**。
按 GH #317 刚立的口径(词表外的拼法会被点名),**本组不自造第五个词**;
是否加一格由总监裁。若加,建议值 `MEASUREMENT`,并单独计数,
**不要混进"完成执行核验的 id 数"那一格** —— 那一格数的是条件 (a),这一轮确实是 0。

---

## 9. 已开 issue

**GH #328 [hero]** —— 读数交出去 + 源码四条事实更正 + (顺带) 给总监的取证前置动作。
`mcp__github__issue_write` **未触发 `requires approval`**(铁律 11 不适用于本轮)。

**发表前** `bash tools/agent/claim_precheck.sh` → **exit 0 / clean /
本地领先 `origin/main` 0 个 commit**(先 push 后发,GH #290);
citations:paths 5、charter sections 1、**refused 0**、trunk 上全部解析。

本组**没有**代改 `queue.json:hero-20` 的 `status`(仍是 `routed`,GH #317 口径下计 open)——
§AW.1 纪律:不改别组的档案字段;已在 issue 里点名请请求方/总监改。

## 10. 验证

- `wk_reincarn_trigger_domain.py --selfcheck` → **14/14 PASS**
- `tests/test_wk_reincarn_trigger_domain.py`(新增,把上面那 14 条纳入 `run_py_tests.sh`)→ **通过**
- `bash tests/run_py_tests.sh` → **53 passed / 0 failed / 0 uncertifiable**(新文件后见 §11 复跑)
- `bash tools/agent/luacheck_gate.sh` → 见 §11
- `bash tools/agent/routine_selfcheck.sh` → **exit 0**(FINDINGS 来源:cadence;
  `UNCERTIFIABLE: none`;53 python 测试全绿;两个 anchor OK)
- **未使用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行**
- **未改 Lua** ⇒ 不声称跑绿 Lua 全量(GH #124)
- AWS:`session_setup.sh` 正常,**S3 只读、零 EC2、零 CE 调用**
- **MCP 未触发 `requires approval`**(若触发见 §9)

## 11. 收尾读数(铁律 8)

```
TOKENS total_in=16,565,071 out=87,079 turns=108
```
(到统计时刻为止;统计后的收尾回合不计入。)

**成本**:AWS **零 EC2、零 CE 调用**;S3 只读(下载 ~77 个 .dem,约 1.7 GB GET,
在本桶的常规读取量级内)。**本轮不花任何需要 $-tier 批准的钱。**
