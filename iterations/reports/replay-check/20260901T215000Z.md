# replay-check 2026-09-01T21:50Z — 交棒「`will_reincarnate` 提案跟到底」结案（顺延四轮后）：引擎布尔在第三波第三棵树上 **1077/1077**,而 `true` **不是 WK 专属**

**工作单元**:章程「下一轮第一件事」第 (4) 条 —— `will_reincarnate` 提案**第四轮顺延**,
「别再掉」。本轮把它跟到底:先查 harness 有没有接(**没有**),再**自己重做一次独立测量**
把提案从「3 局 132 次」抬到「三波三树、两套阵容」,然后把 issue 开出去。

**语料**:W35 run `e12d0d`(`spot_20260901_153142_1_e81ba0ae…_e12d0d`,树 `e81ba0ae`)
的**全部 21 个 `.dem`**(本轮自己下载、自己重跑探针);其中 **16 局**是 sweep 认定的
非暖场对局(5 局 `script_version=e81ba0ae` 未打 `mirror:` 戳 ⇒ 暖场,自动跳过)。
零 EC2、S3 只读、零 CE 调用、`bots/`/`game/` **0 改动**、既有工具 **0 改动**。

⚠️ 排序提醒:章程「当前状态」节历史条目不按时间排;本条对应的章程条目**追加在文件末尾**。

---

## 1. 结论行

```
（本轮不核任何 gate id，因此没有 VERIFY 行）
FOLLOWED-UP  proposal=will_reincarnate  harness_uptake=NONE  ->  GH issue 开出
MEASURED     wr_present = 1077 / 1077 hero deaths   (W35, 21 games, tree e81ba0ae)
MEASURED     wr_true    = 13    （全部是 aegis 持有者；本波阵容里没有 skeleton_king）
MEASURED     LocationX/Y present = 0 / 1077          （第三波复现上一轮的 0/132）
MEASURED     heuristic-vs-engine on 824 spans: TP=9 FP=0 FN=0 TN=815（完全一致）
MEASURED     duration 半边单独用: FP=2（两次 pudge 买活）; position 半边单独用: FP=0 FN=0
```

**一句话**:上一轮说「引擎自己带着那个布尔」,本轮说的是**它带得比我们以为的更宽**——
`true` 不是「WK 用了大招」,是「**这次死亡之后他会原地回来**」,**aegis 也算**;
而我们那个用两个常数拼出来的启发式,在 824 次死亡上**恰好**与它一字不差,
**恰好**的原因是位置那一半独自扛完了全部分离度,**时长那个常数一次都没派上用场**。

---

## 2. 先做的第一件事:harness 有没有接?**没有。**

- `grep -rn "WillReincarnate\|will_reincarnate" tools/` ⇒ 只在本组自己的报告里出现,
  **`tools/batch_test/behavioral/dumper/main.go` 里一个字都没有**(event 结构体仍是
  `T/Type/Actor/Target/Inflictor/Value/ValueName/ActorHero/TargetHero`,`main.go:124-147`)。
- `bbfloor_domain.py:427-429` 的合取式**逐字未动**:
  `span < REINCARNATION_MAX_S and jumped is False`,`REINCARNATION_MAX_S = 8.0`(`:98`)。
- GitHub 语义检索(`dumper will_reincarnate boolean field …`)**0 命中**;
  当前 open issue 从 #386 到 #401 逐条看过,**没有一条是这件事**。

⇒ 提案自 2026-09-01T09:51Z 提出后**四轮无人认领**,**而它从来没有被写成 issue**——
只写在本组报告的 §3 和章程的「新欠账」行里。**这就是它掉了四轮的机制**:
铁律 5 说协作总线是 GitHub issue,而这一条**从头到尾没上过总线**。本轮补上。

---

## 3. 逐帧(先逐帧后聚合;这一格是本轮所有结论的锚)

`e12d0d/20260901_154652_slot3`,`obsidian_destroyer`(**dire = baseline 腿**;
本节不做任何腿间断言),实体 `idx=1353`(直方图 `{1353: 1674, 2357: 1111, 2477: 1111}`
—— 另两个 idx 是幻象,**已按 idx 隔离**)。

```
t= 1462.4 hp= 1703 xy=( 3619.0, 2928.3) aegis=True
t= 1467.4 hp=  231 xy=( 3534.8, 2864.9) aegis=True
t= 1470.4 hp=  129 xy=( 2966.4, 2059.1) aegis=True
t= 1471.4 hp=   21 xy=( 2966.4, 2059.1) aegis=True    ← 最后一个活着的采样
   —— 1471.5 combat log: DEATH  zuus -> obsidian_destroyer (zuus_arc_lightning)
                          引擎 WillReincarnate = TRUE
t= 1472.4 hp=    0 xy=( 2966.4, 2059.1) aegis=False   ← 尸体;aegis 在死亡那一帧被消耗
t= 1473.4 hp=    0 xy=( 2966.4, 2059.1) aegis=False
t= 1474.4 hp=    0 xy=( 2966.4, 2059.1) aegis=False
t= 1475.4 hp=    0 xy=( 2966.4, 2059.1) aegis=False
t= 1476.4 hp=    0 xy=( 2966.4, 2059.1) aegis=False   ← 尸体五帧逐位不动
t= 1477.4 hp= 3012 xy=( 3260.1, 1661.3) aegis=False   ← 复活;离死点 494u
```

三件事同时成立,**互相独立**:

1. **引擎布尔**:`WillReincarnate=true`(探针读 `CMsgDOTACombatLogEntry`)。
2. **背包证人**:`aegis` 在他包里连续 **138 帧**(t=1334.4 → 1471.4),
   **在 hp 归零那一帧消失**。这不是推断,是逐帧的物品串。
3. **启发式**:`span=5.9s < 8.0`、`jumped=False` ⇒ `in_place=True`。

⇒ 这一格上三个证人**说同一句话**。下一节把它铺到全部 824 次死亡。

**顺带登记(不下裁定)**:t=1469.4 他 `MODIFIER_ADD modifier_teleporting` + `ITEM item_tpscroll`
(hp≈113/3226 = **3.5%**),1471.5 死亡时 `modifier_teleporting` 被移除——**低血读条 TP 被打断**。
这与 owner 优先项 P2 同族,但**本例是 26 级 aegis 持有者在团战里**,
**不满足 P2「无危险」的前提**,所以**不算 P2 的病例**,只登记形状。

---

## 4. 聚合(在逐帧之后)

### 4.1 引擎布尔的覆盖率:1077/1077(第三波、第三棵树)

| 局数 | 英雄死亡 | `WillReincarnate` 有值 | 其中 `true` | `LocationX/Y` 有值 |
|---|---|---|---|---|
| 21(含 5 局暖场) | **1077** | **1077 (100%)** | **13** | **0** |

跨轮累计(**三波三树,但绝不并池做效应量**——这里只并「字段在不在」这一个二值事实):

| 波 | 树 | 局 | 英雄死亡 | 有值 | 出处 |
|---|---|---|---|---|---|
| W32 `93546a` | `85bcbeb7` | 1 | 45 | 45 | 09-01T09:51Z 报告 §3 |
| W33 `25b94f` | `e84fe4d2` | 2 | 87 | 87 | 同上 |
| **W35 `e12d0d`** | **`e81ba0ae`** | **21** | **1077** | **1077** | **本轮** |
| **合计** | 3 棵树 | **24** | **1209** | **1209 (100%)** | |

`LocationX/Y` 同样在第三波复现为 **0/1077** ⇒ 上一轮「专用服务器不填这两个字段」
**不是那一局的偶然**。

### 4.2 ⭐ 上一轮那句「true 全是 skeleton_king」是**语料造成的**,不是字段的性质

W35 这粒种子的十个英雄里**没有 skeleton_king**
(radiant: obsidian_destroyer / sven / phantom_assassin / ogre_magi / pudge;
dire: necrolyte / zuus / bristleback / lion / dragon_knight),
而 `WillReincarnate=true` 仍出现 **13 次**,分布在 **sven / phantom_assassin /
ogre_magi / obsidian_destroyer** 四个英雄上,**全部在 t≥898s 的中后期**。

**九次(16 局非暖场那一侧)逐条查了背包**:

| 局 | 英雄 | t_death | span | 死前一帧有 aegis | 尸体那一帧还有 aegis |
|---|---|---|---|---|---|
| `153317_slot6` | sven | 984.4 | 5.7 | ✅ | ❌ |
| `153337_slot8` | sven | 1056.1 | 6.0 | ✅ | ❌ |
| `154627_slot2` | sven | 898.4 | 6.1 | ✅ | ❌ |
| `154643_slot8` | ogre_magi | 1358.1 | 5.4 | ✅ | ❌ |
| `154647_slot7` | sven | 1236.2 | 6.3 | ✅ | ❌ |
| `154651_slot6` | ogre_magi | 982.8 | 5.6 | ✅ | ❌ |
| `154652_slot3` | obsidian_destroyer | 1471.5 | 5.9 | ✅ | ❌ |
| `155839_slot1` | phantom_assassin | 1257.1 | 5.4 | ✅ | ❌ |
| `155845_slot2` | phantom_assassin | 1069.5 | 6.0 | ✅ | ❌ |

⇒ **`WillReincarnate` 读作「他会原地回来」而不是「他是 WK」。**
`bbfloor_domain.py` 的文档串**本来就写着**「**Aegis** does the same thing for whoever
carries it」——本轮做的是**把那句话量出来**,并且指出:**正因为它不止 WK,
用英雄名或用 `wk=` 那一列去近似它都是错的,而引擎布尔天然覆盖两者。**

### 4.3 启发式 vs 引擎布尔:824 次死亡,零分歧;而**分离度全部来自位置那一半**

`bbfloor_domain.py <sweep> --dump-rows` 在 16 局非暖场对局上给出 **824** 条 span;
探针在同样这 16 局上给出 **824** 次英雄死亡;
按 `(局, 英雄, |Δt|≤3s)` 配对 **824/824 全部配上,0 条落空**
(⇒ `death_spans` 的取数与引擎的死亡表**逐条一一对应**,这本身是一次交叉验证)。

以引擎布尔为真值:

| 判据 | TP | FP | FN | TN |
|---|---|---|---|---|
| 现行合取 `span<8.0 and jumped is False` | 9 | **0** | **0** | 815 |
| 只用时长 `span < 8.0` | 9 | **2** | 0 | 813 |
| 只用位置 `jumped is False` | 9 | **0** | **0** | 815 |

- 时长那一半单独用时的两个 FP 是 **`154601_slot5` pudge t=1281.3 span=1.2 jumped=True**
  和 **`154643_slot8` pudge t=1362.1 span=2.4 jumped=True** —— **两次买活**,
  正是 `bbfloor` 文档串说「folding every short span into this bucket would hide the one
  outcome this tool exists to look for」的那个东西。**那句话在本语料上是对的。**
- 位置那一半单独用**在本语料上就是完整答案**:`REINCARNATION_MAX_S = 8.0`
  这一轮**一次都没有改变过任何一行的判定**。
- 两族的实际间隔:`in_place` 的 span 全部落在 **[5.4, 6.3]**(aegis 5s 复活 + 1 Hz 上估),
  最近的一条「干净复活」是 **9.4s**(1 级)。**中间 3.1s 是空的**,阈值 8.0 坐在里面。

⇒ **本轮不把这读成「启发式有 bug」**——它在 824 条上一条没错。
读成的是**两句更准的话**:
(i) 那个合取式**当前由 `jumped` 独自承担**,`REINCARNATION_MAX_S` 是**未被行使的冗余**;
(ii) 冗余不等于安全——上一轮已实测到 1 级真实复活 **7.10s < 8.0**,
本轮又实测到尸体单步可达 **6401 u/s**(09-01T09:51Z §2.1,`jumped` 的 1500u 门槛的尺子问题)。
**两个常数各自都有一条已知的、被量过的、朝错误方向的边界;而引擎已经把答案发过来了。**

---

## 5. 诚实边界

- **没有一个数字是效应量。** 本轮**不下任何 `VERIFY id=…` 裁定**,不核任何 gate,
  **不要数进「本周完成执行核验的 id 数」**。
- **不做任何 armed−baseline 断言。** 本轮的 824 条 span 里 `radiant 590 / dire 234`、
  `armed 490 / baseline 334`,**两层局数与行数都不等**;按铁律 4(i-d),
  这种语料**不可作差、不可按局数加权**。9 条 `in_place` 落在 `armed 7 / baseline 2`,
  **那是 aegis 的分布不是任何 id 的效应**,不写进任何结论。
- **1077 vs 824**:1077 是 21 局(含 5 局暖场)的引擎死亡数;824 是 16 局非暖场的 span 数。
  两者在**同样的 16 局**上是 824 = 824,**跨局数比较没有意义**。
- **一粒种子一个阵容**(W35 只剩 `e12d0d` 一台,另三台 15:42:25Z 同瞬被 spot 回收,GH #398)。
  本波阵容没有 WK ⇒ 本轮**没有测到任何一次真正的 Reincarnation**,
  「true 覆盖 WK」这一半**继承自上一轮的 13 例**,不是本轮买的。
- **「位置那一半独自够用」是本语料的读数**,不是定理:它在 824 条上成立,
  在下一局一次「尸体漂过 1500u」上就可能不成立(那正是 `anchor_slip` 的欠账)。
- **探针未入仓**(活在容器 scratchpad 里,~110 行 Go,复刻 `get_dumper.sh` 的同一份
  manta `class.go` 补丁);**本轮未改 dumper、未改 `bbfloor_domain.py`、未改任何工具**。
  ⇒ **本轮无变异台**:核心读数(引擎布尔、背包物品串、坐标)**不经过任何被检工具的判据**,
  直接读 combat log 与 `snapshots`;交叉验证由 §4.3 的 824/824 配对与 §4.2 的背包第三证人承担。
- 未改 Lua ⇒ **不声称跑绿 Lua 全量**(GH #124)。

---

## 6. 验证(退出码全部裸读,无管道)

- `session_setup.sh` **BARE_EXIT=0**(`arn:aws:iam::581436534924:user/dota2bot-agent`)
- `go mod init` + `go get manta@v1.5.0` **BARE_EXIT=0**;manta `class.go` 补丁 `patched`;
  `go build ./probe` **BARE_EXIT=0**
- 探针 21 局 **21/21 exit 0**(`ALL_DONE fail=0`)
- `sweep_run.sh`(W35 `e12d0d`)**BARE_EXIT=0**:21 个 `.dem`、**5 局暖场跳过**、
  **16 局 swept**、`unparseable=0`
- `bbfloor_domain.py <sweep> --dump-rows` **BARE_EXIT=0**(824 行)
- **未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行可抄**

---

## 7. 开工自检

- ⚠️ **第一条命令第十一次踩证据纪律 3**(`… | tail -60`),脚本自打
  `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked`
  —— **护栏第十一次生效,那次不是通过**。「开工模板内建 `rc.sh`」已交出去四轮,
  **本轮第五次登记、形状完全相同**(建议归总监:改 `routine_prompts.md` 的开工行本身)。
- 改重定向后台跑:**第十八次登记「不是章程写的『约 20s』」**(GH #358)——
  本轮跑到收尾**仍未结束**(>30 分钟,卡在 Lua 检测器腿)。
- 已跑完的腿:iron rule 6 push gate 上膛 ok;`unlanded_commits` ok;cadence + citations 已跑;
  queue 场 ok;orphan proposals ok;expired waits ok;**stable 锚点 ok**。
- ⚠️ **python trunk-health:`UNCERTIFIABLE`**(`tests/test_rc_wrapper.py`、
  `tests/test_selfcheck_lua_leg.py` **没跑成**,不是红也不是绿)+ 一条
  `TRUNK RED -- a python test is failing ON THE WORKING TREE`。**已有 GH #364,不新开。**
- ⚠️ `rc.sh` 变异台腿 **2 项 `UNCERTIFIABLE`**(`no lua5.1 on PATH`)——
  与上一轮**逐字相同**(上一轮已登记「腿级退出码没抬到 2」这处不一致)。
- ⚠️ **trunk-red(lua):`test_incoming_damage_callsite_census.lua`(published 43 → 实读 44)**
  —— **GH #394 已在**,本组**只记复现,不重开不重报**。

---

## 8. 欠账变化

- ✅ **交棒「`will_reincarnate` 提案跟到底」结案**(顺延四轮后):harness 未接 ⇒ 已开 issue;
  并且**证据从 132 抬到 1209**,外加一条上一轮没有的性质(`true` ⊋ WK)。
- ✅ **新买到的交叉验证**:`death_spans` 与引擎死亡表在 16 局上 **824/824 一一对应**。
- ⛔ `illumove` 条件 (b) 仍欠(等下一个同时 armed 它的波;**W36 于 21:31Z 起飞**,本轮无产物)。
- ⛔ `illureal` `DOMAIN-EMPTY` 已退回总监(GH #381),等回音。
- ⛔ `wkqdmg`「击杀确认分支产不产 Q 施法」等 GH #390 回音。
- **新欠账**:
  (a) **W35 那 16 局的 timeline 与 21 个 `.dem` 随容器回收** —— 要复用得重跑
      `sweep_run.sh`(实测约 4 分钟)+ 重新下载(约 450 MB)。**别当成现成资产。**
  (b) **探针要不要入仓**:本轮是第二次从零重建它(上一轮建过一次、随容器蒸发)。
      入仓 ~110 行 Go 就再也不用重建 —— 但**它属于 harness 的目录**,交总监定。
  (c) **`REINCARNATION_MAX_S` 的「冗余」结论只在一波上成立**,
      下一波若语料里有 WK,应把 §4.3 那张 2×2 表**原样重打一遍**(零新方法)。
- 继承未动:§5 宽扫表按 ab/ba 重打;`tp_out` 那 311 条;`stayfield2_whynot.py` 待下一个 44-id 波;
  `pullcad_beat.py` 在 W25 剩两 run;`unk` 那一列**第十一轮**;窗口常数只读未量;
  W33 0.748 与 W32 0.401 合并成案;`fieldbuy_silence.py`/`stayfield2_margin.py` 分层;
  「静止在小兵火力里」检测器;W25 只并 2/4 run;W26–W28 与 W25 从未池化;
  `seed 975` **第三十二轮**;`wandlimbo` 因 #293 **第三十轮**;GH #265 被 #272 阻塞;
  `blinkflee` 卡 #304/#305;WK rank-3 全语料复测;载体侧别提案 GH #389。

---

## 9. 下一轮第一件事

0. **先读章程「当前状态」节最末一条,不要抄过期的交棒行。**
1. ⭐ **W36(21:31Z 起飞,三台 spot)收官后第一件事**:宽扫 arm 串
   (`arm_string_census.py` **喂 `…/analysis` 子目录**,`--declared` 用**发波当时**的串)。
   若 `illumove` 再次在集 ⇒ **第一件事是把上一轮 §4 那张 `starved%` 四格重打一遍**(条件 (b))。
2. **本轮的 §4.3 那张 2×2 表在有 WK 的语料上重打一遍**(新欠账 c,零新方法)。
3. **看 harness/总监接不接本轮开的 issue**;接了就等他们落地,**没接不要再顺延第五轮**——
   直接在 issue 上追一条,或按新欠账 (b) 请总监裁定探针入仓。
4. `will_reincarnate` 这一棒**到此为止不再顺延**(已上总线)。

---

## 10. 已发表

见本文件同轮提交后的追加(**先 push 后发表**,GH #290;草稿过 `claim_precheck.sh`)。

---

## 11. Token 用量

见收尾追加。
