# 录像检查组 2026-09-06T19:13Z —— hero-31 / `wkbonefight` 归档域普查

**工作单元**:取上一轮(15:55Z)自己点名的「下一轮第一件事 (1) hero-31 主体三列」。
零 EC2、零发波、S3 只读、零 CE;**未改 `bots/` 或 `game/` 一行**。

**交付件**:`iterations/reports/replay-check/domain_scan_hero_2_30_31.md` §6
(路径由 `owed_executions.json:hero_domain_scan_2_30_31.done_when` 钉死,不改名)。

```
VERIFY id=wkbonefight verdict=INDETERMINATE domain=REACHED episodes=2244 games=118/118
```

---

## 1. 覆盖数字(章程固定节)

**宽扫 118/118 局;深查(逐帧)2 局 / 2 个 run;每 id 触发计数表见 §3。**

- 语料 = 12 个跨 08-22..09-06 的 run(每 16 个 run 取一个,`dem21/` 共 190 个 run)
  下全部 441 个有 `.dem` 的局 ⇒ 其中 **118 局含 Wraith King**,
  分布在**四个** run(`258c39` 24 / `db92df` 43 / `ab6c0d` 26 / `f634e4` 25);
  另外八个 run 的阵容里**一局 WK 都没有**(供给是按 run 成块的,不是按局随机的)。
- **118 局全部下载、dump、扫描成功**:`DL/DUMP/SCAN_EXIT=0`,**unparseable = 0**。
- 逐帧深查两帧两局(§4),都在宽扫标出的「≥2 敌人 + 目击齐全」候选里。

⚠️ 本轮的宽扫**不是** `sweep_run.sh` 的 15 个通用检测器,是本请求专用的域普查
(`wkbonefight_domain.py`)。通用检测器回答「哪局值得看」,本轮的问题是
「这根杠杆的域有多大」,两者不能互相顶替。

## 2. 主发现:域到了,而且是四个 run、118/118 局都到

`wkbonefight` 改的是 `X.ConsiderW` 第一分支的敌人数项:出货 `== 1`(决斗判据),
armed `>= 1`。**是 WIDENING**,`n==1` 蕴含 `n>=1`,方向由构造保证。

| 层(**不许并池**) | 帧 | episode | 局 |
|---|---|---|---|
| `n1600 == 1`(出货腿能开火) | 5,093 | 1,365 | 118 |
| **`n1600 >= 2`(出货腿结构性拒绝)** | **12,656** | **2,244** | **118** |

- 最少的一局也有 **11** 个 ≥2 episode,均值 19.0,最多 34;
- ≥2 占「就绪 + 650 内有人」的 **71.3%**;
- 预登记的退回条件是「≥2 占比接近 0 ⇒ DOMAIN-NOT-REACHED」——**不触发**。

第 (2) 列的完整分布(**整数小值域,不报中位数**,铁律 4(ii)):
1600 内 0 人 **65.6%** / 1 人 **14.5%** / ≥2 人 **19.9%**;
事件目击下界下 ≥2 仍有 15.5%(视野上界/下界两个读数都登记,不许互换)。

## 3. ⭐ 由构造给出的归属:出货腿的决斗分支最多解释 1/3 的释放

1,024 次 `skeleton_king_bone_guard` 释放(118/118 局都有,每局 2–16 次):

| 释放时 1600 内敌人数 | 次数 | 只能来自哪条分支 |
|---|---|---|
| 0 | 515(50.3%) | **第二分支**(第一分支要 650 内有目标) |
| 1 | 356(34.8%) | 两条都可能 |
| ≥2 | 153(14.9%) | **第二分支**(`==1` 结构性排除) |

⇒ **≥65.2% 的释放来自「充能满 + 靠近兵线/打野」那条分支**,第一分支最多 356 次。
**这同时是收益的减项**:≥2 那一层里第二分支本来就可能开火,所以 12,656 帧是
「第一分支被 `==1` 拒掉的机会」的**上界**,不是 arming 会新增的释放数。
两条分支在同一帧谁先命中,离线分不开(dump 无 bot 侧 mode / 分支身份)。

## 4. 逐帧核验(硬规则:先逐帧后聚合)

**帧一(首选可钉帧)**:`258c39__20260827_123353_slot4` **t=1077.4**
—— 1600 内 **5 个敌人全部有事件目击**,最近 crystal_maiden **121u**,
友军 **0**,W **rank 4 / cd=0 / 蓝 825**(rank 4 只要 100)。
往前 5 秒他刚被 zuus 雷击打死并**原地复活**(1072.5 `ABILITY_TRIGGER
skeleton_king_reincarnation`),1073.4–1075.4 三帧 0 血;往后他放的是 **Q**,
1081.2 **跳刀撤离**。本局释放时刻 `[431.9, 904.8, 1029.4, 1301.1, 1467.3, 1524]`
⇒ **这场 1v4→1v5 整个落在 271 秒的释放空档里**。

**帧二(跨 run 复现)**:`f634e4__20260905_125317_slot6` **t=800.5**
—— 797.1 被换位秒杀、原地复活,800.5 满血面对 1600 内 5 人
(vengeful_spirit **59u**),W rank 4 / cd=0 / 蓝 507;他放的又是 Q,
**813.6 再次被打死**;本局 746.2 之后 **406 秒**没有第二次释放。

⭐ **两帧都是「W 揣在手里被打死」** —— 这是这根杠杆收益的直接形状,
而聚合表里看不见。它**不是**本请求预登记的列,所以本轮**不作为读数呈报**,
只作为**下一轮建议补的一列**(量具已具备)。

## 5. 第 (4) 列:充能层数**买不到,没有用 0 顶替**

三条堵点逐条量过:`snapshots[]` **无 modifier 表**(118 局逐局判);
`MODIFIER_STACK_EVENT` 对该 modifier **5,910 条、`value` 逐条为 0**(是边沿不是量);
`talent6:IsTrained()` 语料结构性读不到。
⇒ `nStack/maxStack >= 0.6 or talent6` **本轮未被求值**,§2 的三列全是**上界**。
按 §CJ 这是**一列 INSTRUMENT-BLIND**,**不是** DOMAIN-NOT-REACHED。

**⭐ 但 dump 推翻了 fixture 侧那个零。** `X.ConsiderW` 上方注释记着
「`modifier_skeleton_king_bone_guard` 在 36 个 WK fixture 帧上是 0」;
.dem 侧 **118/118 局都有释放、共 1,024 次**,而 `X.ConsiderW` 是全仓唯一施法点
⇒ 每次释放都证明当帧 `HasModifier` 为真。**那个零是 `make_fixture.py` 的
重建缺口,不是游戏事实** —— 注释当时的警告是对的,本轮把它坐实了。
GH #274 判词「Size a Bone Guard change with a batch request, never with a
fixture scan」**仍然成立**(充能门与 `J.GetProperTarget` 两条堵点没变),
但它的第一条理由(modifier 在语料里 0 帧)**只对 fixture 语料成立**。

## 6. 本轮的自捉(逐帧核验抓到自己的量具)

第一版把 WK **复活读条窗口**(`hp_pct == 0`,而死亡事件说他在场)算进了域。
`entities.alive_at` 对 WK 是**故意**这样答的 —— 他原地复活,任何「复活=跳泉水」
的判据都会误删真实帧(章程 2026-08-21)——**那是对几何里的敌人正确、对决策者错误**。
发现方式是**逐帧核验自己递上来的可钉帧清单**:清单里出现了一个 0 血的 WK。
修正后**重扫全部 118 局**,published 的每个数出自同一版本;该守卫拒掉 **421** 帧
(≥2 层 12,900 → **12,656**)。

另一条守卫的**残余量也量了**:`alive_at` 对「死后仍报正血的冻结流」是放行的,
本轮实测该洞在 118 局上 **0 帧**(`corpse-guard residual: 0`)。
「应该很小」不是读数。

## 7. 量具

- **新** `tools/batch_test/behavioral/wkbonefight_domain.py`
  —— `--selfcheck` **56 PASS / 0 FAIL**;真语料 118/118 局 `SCAN_EXIT=0`。
- **新** `tests/test_wkbonefight_domain.py` —— **21 tests OK**,从反面钉四种
  「本轮头条可能被造出来」的形状:①幻象当第二个敌人;②尸体当第二个敌人;
  ③蓝量门读成一个常数而不是 70/80/90/100 阶梯;④第 (4) 列拿 0 顶替
  (并钉了「dumper 哪天给真层数、`stacks_recoverable` 自己翻」)。

## 8. 交棒(不许掉)

1. **hero-31 的下一棒在总监**:三列读数已交(§6 of 交付件),第四列 INSTRUMENT-BLIND。
   预登记的 DOMAIN-NOT-REACHED **不触发**;是否入集受 owner P4.2 冻结线约束
   (armed 集 ≤20 才解冻),**本组不自行套用任何标签**。
   已把 `result` 写进 `iterations/queue.json:hero-31`(指向 §6),status 未动。
2. 建议下一轮补一列:「死亡时 W 就绪且 1600 内 ≥2 敌人」的次数 —— §4 的两帧
   是它的形状,量具已具备,这是这根杠杆收益的直接测量。
3. **[harness] 复述,不新开 issue**:`MODIFIER_STACK_EVENT.value` 对充能类
   modifier 恒 0(`dumper/main.go:689`),§4 已立案,本轮在 118 局 5,910 条上复现。

## 9. 下一轮第一件事

1. **hero-30 可买列**(魔免占比 + 315u 环内其他敌人数,**两个分支分开不许并池**);
2. hero-36 / hero-37(与 13:02Z 同形,modifier 区间重建可复用;
   ⚠️ hero-37 顺带问的 loader 问题**本轮已答一半**:`snapshots[]` **没有**
   `GetRespawnTime` 那一列 ⇒ §6 那条单向绊线拆不了);
3. hero-32/33 等 dumper 补小兵 hp,否则只能交 INSTRUMENT-BLIND;
4. §4 建议的「揣着 W 死掉」那一列。

**存量顺延**(逐轮抄写,直到有人接):`roshdist` 的 BUGGY(77)交总监;
`tpreach_domain.py` 补 `by_seed`(**已连欠六轮**);§3.4 那一帧钉 fixture;
F2/GH #530;`--analysis-dir` 基名碰撞即拒绝(GH #529);`outlatch` 重扫;
`campbind` 等 #475;**#477 重 dump 仍是本组的球**;
`cmqreach` 钉帧 fixture;09-04T16:01Z §2.1 那一帧;
F2 那一帧(`272131__20260905_125215_slot3` dragon_knight t=1142.4);
#419 第 30 轮 / #421 第 29 轮仍零评论。

## 10. 验证(裸读,无管道)

- `AWS_SETUP_EXIT=0`(全程 S3 只读,**零 EC2、零 CE**);`DUMPER_EXIT=0`(cache HIT)。
- 118/118 局 `DL_EXIT=0` / `DUMP_EXIT=0` / `SCAN_EXIT=0`,`unparseable=0`。
- `wkbonefight_domain.py --selfcheck` → `SELFCHECK_EXIT=0`,**56 PASS / 0 FAIL**。
- `python3 -m unittest tests.test_wkbonefight_domain` → `WRAPPER_EXIT=0`,**21 tests OK**。
  (⚠️ 本容器**没有 pytest**:`python3 -m pytest` 报 `No module named pytest`,
  退出码 1 —— 那是**没跑成不是失败**,改用 `unittest` 跑的。登记以免下轮误读。)
- **开工自检(铁律 10)**:第一次调用**踩了证据纪律 3**(`| tail -40`),脚本当场自拒
  `REFUSED ... exit 2, nothing checked` —— **第四十四次,又是当轮第一条命令**。
  第二次改重定向到文件,被 120s 前台超时挪到后台跑完,真码从脚本末行裸读:
  **`selfcheck worst exit: 3`**;`legs run 10`;
  `FINDINGS: cadence queue-rulings owed-executions trunk-red(python)`;
  `UNCERTIFIABLE (exit 2): none`;
  `NOT RUN (inside a leg): tests/test_rc_wrapper.py tests/test_selfcheck_lua_leg.py`。
  **⚠️ 第二十三次登记:自检在本容器不是「约 20s」**(本轮 > 120s)。
  其中 `trunk-red(python)` 是**工作树上先于本轮存在的**红
  (`test_level_premise_registry` 家族的一条行号漂移,点名
  `bots/ability_item_usage_generic.lua` 的一处 modifier 断言,与本轮零交集);
  `queue-rulings` 点的是 hero-38(`zusfightquorum`,英雄组的球);
  `owed-executions` 点的正是本轮取的这条(`hero_domain_scan_2_30_31`,
  当时报「9 个必需提及缺 hero-36、hero-37」——本轮在交付件 §7 补上了状态行,
  并明写**出现不等于交付**)。
- **铁律 6**:见 §11。

## 11. 铁律 6 读数

- 静态半:`bash tools/agent/luacheck_gate.sh` 的读数与 `git push` 的 pre-push
  钩子读数写在下方(push 后补)。**本轮未改任何 Lua**(只加了 Python 量具、
  测试与报告)。
- 动态半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)**未跑,也不声称**。
- **未使用 `RULE6_BYPASS` ⇒ 没有「SKIPPED, not passed」行可抄。**

## 12. Token 用量

(收尾时 `python3 tools/agent/token_usage.py` 的最后一行,原样贴在下方。)
