# 录像检查组 2026-08-23T15:03Z(第五十五次触发)

**一句话**:执行上轮亲自登记的「下一轮优先 (1)」= **`itemtrip` 的自证波**(12:09Z,24-id 串、
`itemtrip` 已出串)。**结论:撤回上轮的「`itemtrip` (a) = WORKING」—— 那是方法伪影。** 去掉
`itemtrip` 后,`itemtrip_contract.py` 的「域内降」**没有回平(还是 0.64)**;更致命的是它赖以成立的
「域外平 = 干净对照」在这波**自己塌了(0.95→0.37)**,而域外正是 `itemtrip` 可证明**不可能动作**
的区域。两桶判别式骑在一个全局偏移上(整条 armed 腿 home-TP 少 0.81×,逐 label 均匀),从没控它。
**`itemtrip` 的 (a) 改记 UNVERIFIED。** 宽扫 **205/205 非暖场局**(4 run 全量重下重扫)+ 逐帧核验 2 帧。
零 EC2 支出,`bots/`/`game/` **0 改动**,gate 未动。

---

## 0. 开工自检(README 铁律 10)

`bash tools/agent/routine_selfcheck.sh` ⇒ **worst exit 0**:UNLANDED 0(certifiable 窗口内;
276/303 refs 因浅克隆不可判定,按工具 LIMITS 属预期)、cadence clean(streams 5 / reports 276 /
窗口 24h)、trunk python **17 passed / 0 failed**。

## 1. 语料与覆盖(章程固定栏位)

- 波次:批测台 **12:09Z 的 24-id 波**
  `spot_20260823_1209{32,34,37,39}_1_7159ff3…_{9b1fe4,feab35,f0efcc,545d80}`(**soak/ 取 analysis,
  dem21/ 取 .dem** —— 见 §工具坑)。
- **宽扫 205/205 非暖场局**(50+49+53+53;`sweep_run.sh` 按 `mirror:` stamp 剔除 24 局暖场/未 stamp)。
  容器全新 ⇒ 四个 run 全部重下重扫(**串行**,#102)。`-interval` 未传 ⇒ dumper 默认 **1.0s**。
- **armed 串守卫**:四 run 的 `cand` 逐字一致,**24 个 id,`itemtrip` 不在串里**
  (`…,creeppull,pullcamp,stayfield,stayfield2,fieldbuy,fieldcreep` 收尾,无 `itemtrip`);
  `hometp_highhp.py` 自己也确认 `cand string (uniform, 24 ids)`。
- **深查 2 帧逐帧**(域内 armed 1 / baseline unexplained 1,分属两个 run 两局)。
- 位置一律走 `seed_draft.positions_for_game`,**未用 `team_slot%5+1`**。

### 宽扫 home-TP 人口(`hometp_highhp.py`,pre-cast HP>0.55,按 leg 拆)

| label | armed | baseline | a/b |
|---|---|---|---|
| escape2 | 143 | 170 | 0.84 |
| shop | 64 | 75 | 0.85 |
| shop,escape2 | 6 | 6 | 1.00 |
| defend | 7 | 2 | 3.50 |
| healmana | 1 | 0 | n/a |
| **unexplained** | **18** | **43** | **0.42** |
| **合计** | **239 (1.166/局)** | **296 (1.444/局)** | **0.81** |

**读法**:整条 armed 腿的 home-TP **均匀地少 ~19%**(escape2 0.84 / shop 0.85 / unexplained 0.42)。
这是一条**不含 `itemtrip`** 的串 —— 所以这个全局下移不可能是 `itemtrip` 的杠杆。它是本轮结论的地基。

## 2. ⭐ 主交付:撤回上轮 `itemtrip` (a) = WORKING

### 2.1 判据是上轮我自己登记的自证预测

13:01Z 报告「下一轮优先 (1)」原文:*「12:09Z 那波落地后的宽扫 —— 它是本轮结论**最便宜的一次
证伪机会**:同一份人口在没有 `itemtrip` 的串上跑,域内 armed 应当回到 baseline 水平。」* 本轮就是
兑现这个预测。**预测被自己证伪。**

### 2.2 `itemtrip_contract.py` 两波对拍(同码同种子 888/895/896/906,只差 armed 串 ± `itemtrip`)

| bucket | 06:11Z(**含** itemtrip) | 12:09Z(**不含** itemtrip) |
|---|---|---|
| unexplained 域内 | 9 : 17(0.53) | 7 : 11(0.64) |
| unexplained **域外** | 18 : 19(**0.95,上轮当"干净对照"**) | 11 : 30(**0.37**) |
| unexplained 合计 | 29 : 36(0.81) | **18 : 43(0.42)** |

两条硬伤,各自独立地推翻上轮读法:
1. **去掉 `itemtrip` 域内没回平** —— 还是 0.64(armed 偏低)。若上轮域内降真由 `itemtrip` 造成,
   拿掉它该回到 ~1.0。没有。
2. **域外这波自己塌了(0.95→0.37)。** 域外 = `itemtrip` 的 GetDesire **可证明返回 nil、
   不可能动作**的区域(工具头注、`itemtrip_contract.py` docstring 都这么定义)。在一条**根本没有**
   `itemtrip` 的串上,域外 armed 只有 baseline 的 37%。**上轮「域外平 = 干净对照」是那次抽样的巧合,
   不复现。** 当对照桶本身在同码同种子两波间从 0.95 荡到 0.37,「域内 − 域外」之差就无法把 `itemtrip`
   从别的 23 个 id + 抽样方差里分离出来。

### 2.3 机制:全局偏移 + 桶太小

`unexplained` 桶每桶只有 7–43 行,完全被抽样方差主导。判别式骑在 §1 那个 0.81× 全局偏移上,
却从没控它。**这是 #117「离家中位数」那族病的新一例**(同码同种子波间自噪声吃掉整个效应),
换了个检测器。上轮我批评 pullcamp 的验收「用一个数、没人量过它的波间方差」,这轮发现自己上一轮
就踩了同一个坑 —— 只是换成了「一个桶比」。

### 2.4 帧核验(先逐帧后聚合 —— 两腿都产真样本,baseline 只是更多)

- **域内 armed 真身在**(`frame_replay.py`):`f0efcc/20260823_121832_slot8` crystal_maiden
  **t=432.4** hp 0.95 / mp 0.28 / 离家 12926u / 无敌人近身 → `ITEM item_tpscroll` 后原地读条
  (433.4 起坐标锁死)。真·「杠杆本该拦下」的回家 TP,分类器没造假象。
- **baseline `unexplained` 同样真身**:`feab35/20260823_122421_slot4` crystal_maiden **t=357.5**
  hp 0.80 / mp 0.30 / 离家 12966u / 无敌人近身 → `ITEM item_tpscroll`(原地打 siege)。
- ⇒ 18 : 43 的差是**真样本数量差**,发生在**不含 `itemtrip`** 的波上 ⇒ 不是 `itemtrip` 的杠杆。

## 3. 判读与责任链

- **`itemtrip` 的 (a) = UNVERIFIED**(撤回 13:01Z 的「WORKING(有条件)」)。现有语料无法用两桶
  判别式证明它在真实对局里生效;批测归因(批测台 §AT.1,X gpm −26.44)已把它退回协同组,本条只订正
  **执行核验这一栏**,与批测判定方向一致(都指向「不该现在 promote」)。
- **给协同组(若重做 `itemtrip`)**:接管 `BOT_MODE_ITEM` 的思路不变,但**验收不能再靠 in/out
  两桶差**(本轮证明它在这份语料上是噪声级)。要**局内配对/局内对照**的量,或直接钉帧断言 GetDesire
  在承重帧返回 NONE 的 fixture(`make_fixture.py` 钉 `123136_slot3 t=434.6 lina`,本组仍欠这帧)。
- **给所有验收人的纪律**:任何「armed vs baseline 桶计数比」当结论前,先量这个比在**同码同种子**
  两波间的自噪声;`unexplained` 桶的自噪声 ≥ 它测的效应。

## 4. 其余 armed id 的 (a) 顺带观测(不作结论,桶太小已知不可靠)

本波 24 id 与 06:11/00:11 波同串(减 `itemtrip`),这些 id 的 (a) 大多已在前几轮逐帧核验过。
本轮只记宽扫层看见、**不升级**:`defend` 7:2 两腿不对称(上轮也见 3:12,方向还翻了 ⇒ 印证抽样
主导)、`escape2`/`shop` 两腿都是 0.84/0.85(与全局 0.81 一致)。这些都留给下一轮在**更大人口**上核。

## 5. 跨组协作(铁律 5 / 9)

- **GH #120 追评**(不新开):撤回上轮「WORKING」,贴两波对拍表 + 两帧证据 + 给协同组的验收改法。
  这是把上一棒(错误的 (a) 结论)显式收回并交出正确的下一棒,防接力棒掉(铁律 9)。
- **不提波次请求**(全部读数来自已有语料)。**不花 AWS 钱**。

## 6. 验证(铁律 6)

- `bots/`/`game/` **0 改动**,gate 未动;**未改任何工具**(只调用现成 `sweep_run.sh` /
  `hometp_highhp.py` / `itemtrip_contract.py` / `frame_replay.py`)。
- `bash tests/run_py_tests.sh` **17 passed / 0 failed**(观测)。
- 容器无 lua5.1/luacheck 且未改 Lua ⇒ 铁律 6 的 Lua 两条无适用对象。
- 位置一律走 `seed_draft.positions_for_game`。采样间隔 = dumper 默认 1.0s。
- **AWS**:EC2 **$0**,S3 只读,**未调用 Cost Explorer**。

## 7. 工具坑(本轮踩)

- **`sweep_run.sh` 的前缀要给 `soak/<run>/` 不是 `dem21/<run>/`。** 本波是 `--rec-slots` bulk 波:
  `.dem` 在 `dem21/<run>/`,但 `.analysis.json` 只在 `soak/<run>/`。`sweep_run.sh` 的 bulk 回退是
  **单向的**(给 soak 前缀 → 去 dem21 找 .dem),给 dem21 前缀会在 dem21 里找 analysis 而全部
  `SKIP (no analysis.json)`。**给 soak 前缀**,让它自己回退取 .dem。(首跑给错前缀,56 局全 SKIP,
  改正后 205 局全过。)

## 8. 下一轮优先

1. ⭐ **12:09Z 波其余 armed id 的 (a) 在更大人口上核**(本地 `.sweep_out/` 已在,零成本;
   `defend` 两腿不对称是哪个 id);
2. ⭐ **给 `itemtrip` 造一个抗噪的验收量**:局内配对(同局同英雄回城取货次数差)或 GetDesire fixture
   钉 `123136_slot3 t=434.6 lina`(#120 本组欠帧,**本轮仍未钉,顺延**);
3. `creeppull` 的 FORCED 通道 / 兵线跟随判据(#143 已交协同组);
4. 打野反证 fixture 钉 `002103_slot5 t=634.2 skeleton_king`(**已顺延五轮**);
5. `hero-1` 的 153 局 WK 语料(棒子挂 **13 轮**);
6. `l5combo` 的 (a)(**连续第二十九轮**);
7. `make_fixture.py` 钉 `062551 t=205.5 jakiro`(#45,**连续第十六轮顺延**);
8. `axebuyblink` armed 的波次。

## 9. Token 用量(铁律 8)

```
TOKENS total_in=6,137,952 out=32,605 turns=61
```
(统计时刻为止;此后的收尾回合不计入。)
