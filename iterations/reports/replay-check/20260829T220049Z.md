# replay-check 2026-08-29T22:00Z — `campvoid` 条件 (a) = **SILENT**;三个假阳性全部由逐帧抓出

**一句话**:上一轮章程给下一轮点的名(「在 W25 语料上核**下一个零记录 id**,建议 `campvoid`」)本轮做完了 ——
新工具 `campvoid_escape.py` 在 **W25+W26 并池 129 局**上找不到**一次** `campvoid` 形状的逃逸
(400 u / 600 u 两个半径下**四个格全 0**;800 u 下 armed/baseline 两层**反号** ⇒ 铁律 4(i) 判噪声),
而**根因是结构性的、可复核的**:远古营与最近的普通营中心相距 **146 u / 338 u**,
`nSearchRange` 最小也有 330 u ⇒ 只要邻营还活着,`FilterFarmNeutrals` 删掉远古之后
`#nNeutrals` **仍然不是 0**,那道小兵出口**根本打不开**。

**宽扫 129/129 局**(W25 四个 run 121 局 + W26 四个 run 8 局,100% 覆盖,`unparseable 0`),
**深查逐帧 6 局**。零 EC2 支出,S3 只读,`bots/` / `game/` **0 改动**,gate 未动。

⭐ **本轮最该被下一个人读到的一句**:三次「聚合表读出结论」都被逐帧当场推翻,
**三次都是同一个错误的三种外衣** —— 把一个**结果**当成一个**机制**,而没有先问
「这个机制在物理上产得出这个结果吗」。三次全部由帧抓到,**一次也不是由表抓到的**。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` —— **worst exit 3,8 腿全跑**。

- `exit sources`(工具自己打的归因,不是我手写的):`FINDINGS (exit 3) = cadence`,
  **`UNCERTIFIABLE (exit 2) = none`** ⇒ **trunk 两侧本轮都被看过了**。
- trunk 健康:python 套件 **53 passed / 0 failed / 0 uncertifiable**;
  fast Lua detectors **31 个打标文件 / 0 失败**(工具自己声明这是 FAST SUBSET,照抄不加工)。
- `unlanded_commits`:**OK,可核验窗口内无未落地工作**(27 个可核验 ref)。
- 锚点:`stable-v1` / `stable-v2` **2/2 OK**(EXISTS / PINNED / SHIPPED 全绿)。
- 未裁队列请求:RIDESHARE none / OTHER none(开放请求 38 条);过期等待:无。
- cadence 两条:director `08-29T04:08Z → 16:28Z` 12.3h、**replay-check `00:55Z → 05:30Z` 4.6h**
  —— 后者是本组自己的洞,**本轮这次触发就是在补它**。
- `arm_push_gate.sh`:`core.hooksPath=.githooks`,**already set**。

---

## 1. 为什么是这个工作单元

- **owner 优先项(铁律 9)**:`OWNER_PRIORITIES.md` 三项里,P1 的球在协同组、P2 的球在协同组、
  P3 的球在总监。**本轮没有属于录像组的未完成优先项**,所以走章程 backlog。
- **上一轮章程「下一轮第一件事」第 (2) 条**点名了 `campvoid`,并且**章程 §4a 已经把它的触发级买法写好**。
- 第 (3) 条(W26 是否停、arm 串是否可分层)**批测台 21:15Z 已经做完并落档**:
  395 份 per-game 戳解出**唯一一个** arm 串 `ids=44 / bytes=387 / sha256[:12]=f0e03819073c`,
  与 W25 逐字节相同,两棵树之间碰 `bots/` 的两个 commit 剥注释后逐字节相同
  ⇒ **W25 与 W26 可以并池**,本报告据此把两波的行为语料合起来读。**本组不重复做这一步。**

---

## 2. 宽扫:129/129 局

| 波次 | run | 种子 | 侧(armed) | 有 timeline 的局 |
|---|---|---|---|---|
| W25 | `6df84c` | 1603 | 20 radiant / 11 dire | 31 |
| W25 | `b1386e` | 1633 | 19 radiant / 11 dire | 30 |
| W25 | `a29ed3` | 1664 | 19 radiant / 11 dire | 30 |
| W25 | `ecbb41` | 1770 | 20 radiant / 10 dire | 30 |
| W26 | `8d47de` / `ae0223` / `d686ce` / `d7bb48` | 1698 / 1733 / 1743 / 1747 | 1 radiant + 1 dire 各 | 2 各 |

- W25 四个 run 各自 `sweep_summary.md` 齐全,`games swept 31/30/30/30`,
  **`unparseable 0`**,跳过的是 6 局暖场/未打戳。
- W26 每台只上传 **6 份 `.dem`**(`rec_slots 1`,GH #308 那道闸没裁 ⇒ 帧通道 1/16),
  去掉暖场后**每 run 只剩 2 局**。**W26 对帧级读数几乎没有承重**,登记在此免得被当成 8 粒种子的帧覆盖。
- 合计 **129 局**(`games with a timeline: 129`),其中 127 局产出了至少一条本工具的记录。
- ⚠️ **129 局不是我抽的,是录制槽决定的**;**82 局在 ab 层、47 局在 ba 层**,
  侧别倾斜(radiant-armed 偏多)**第四次复现**,已在上一轮交总监,本轮不重复立案。
- 本轮**未跑** `sweep_run.sh` 产出的 15 个通用检测器读数(沿用上轮欠账,登记不掩盖)。

---

## 3. ⭐⭐ 裁决:`campvoid` = **SILENT**

### 3.1 新工具

`tools/batch_test/behavioral/campvoid_escape.py`(`--selfcheck` **49 PASS / 0 FAIL**)。

它**不**声称买到了章程 §4a 要的触发级证据,**因为那在这份语料上结构上买不到**(见 §5.1);
它买的是**逃逸必须产生的那个结果**加上它的反事实,并且把这条区别写进了文件头,
免得下游把两者当成一回事。

**episode 定义**:一段连续 1 Hz 帧,bot 英雄同时满足
(i) 两端 bracketing 判活(GH #176:插值 hp 会把死帧混成 0.05 > 0);
(ii) 是真英雄不是幻象(`entities.frames_by_hero`);
(iii) 等级 ≤ **11** = `ANCIENT_MIN_LEVEL - 1`(**从 `bots/FunLib/aba_site.lua` 读出来的,不是抄的**);
(iv) 距**远古营中心** ≤ `--in-camp`;
(v) `--fight-near` 1500 u 内没有敌方英雄(营地上压着敌人那是团战不是农野决策)。

营地中心**由本语料自己推**(英雄与远古交火时的站位聚类),并断言
`ancient_camp_domain` 立下的「恰好两个 map cluster / |x| ≥ 3500」几何分层性质。

**结局分类**:`trade_anc`(打了远古)/ `trade_norm`(打了非远古中立)/
`escape`(**对任何中立零输出**,且在 `--escape-w` 内**平砍**到一个**走得到**的敌方小兵)/
`left`(走开了)/ `stuck`(GH #265 那张照片:站在框里,没东西打也没理由走)。

### 3.2 读数(铁律 4(i):两层都给)

`escape` = `campvoid` **唯一能署名的**结局 —— `campfarm` 清空的是**攻击表**,
它没有任何一条分支会把 bot 走到小兵那里去;能下那道命令的只有 `:752`。

| 半径 | ab/armed | ab/baseline | ba/armed | ba/baseline |
|---|---|---|---|---|
| **400 u** | **0** / 38 | 0 / 28 | **0** / 8 | 0 / 20 |
| **600 u** | **0** / 117 | 0 / 72 | **0** / 43 | 0 / 57 |
| 800 u | 3 / 323 (0.9%) | 0 / 188 | **0** / 107 | 1 / 159 (0.6%) |

- **400 u 与 600 u:四个格全 0。** 两个 armed 腿加起来 **0 / 160 个 episode**。
- **800 u 才出现 4 次,而两层反号**(ab: armed−baseline = **+3**;ba: **−1**)
  ⇒ 按铁律 4(i) **是噪声,不写进结论**。而且 800 u 是三个半径里最松的那个,
  `left` 占到 75–95%,本来就是「路过」为主的人口。
- ⇒ **`campvoid` = SILENT**。这不是「域太小没赶上」,是**一次都没有**。

### 3.3 ⭐ 根因不是散文,是几何(而且这条是新的)

工具每次运行都会打出远古营与**最近普通营**的距离,读数(129 局暖场子集):

```
ancient box (-4812,   9) n=2084 -- nearest NORMAL camp  146 u away at (-4894, -113) n=409
ancient box ( 4005, 115) n=1247 -- nearest NORMAL camp  338 u away at ( 4249, -119) n=273
```

(W25 `b1386e`+`a29ed3` 子集上独立复现:**127 u** 与 1384 u。)

`:752` 的 `nSearchRange = min(GetAttackRange() + 180, 1600)`,近战最小 **330 u**。
`FilterFarmNeutrals` **只删远古**。于是:

> **只要那个 146 u 外的普通营还有活着的小兵,`#nNeutrals` 在过滤之后仍然 > 0,
> 出口就是关的 —— `campvoid` armed 与否,那一帧逐字节同结果。**

`campvoid` 的立案句(`state.json:campvoid_20260828`)说的是「bot 不能打它站着的营,也不能离开」。
**这一条仍然成立**;**不成立的是「删掉远古就足以让 `#nNeutrals` 归零」**。
在天辉远古营,归零还需要**邻营同时是空的**,而这在 129 局里一次都没被观察到起效。

**另一个独立收窄**(同样没登记过):`:752` 的分支还要求 `hLaneCreepList = bot:GetNearbyLaneCreeps(900)` 非空。
一个站在**野区远古营**里的 bot,900 u 内要有敌方小兵 —— 这本身就是稀有几何。
两个前提相乘,才是 `campvoid` 的真实域。

---

## 4. ⭐⭐⭐ 三个假阳性:表说是,帧说不是

这一节是本轮的方法论产出。**三条读数都在聚合表里长得像结论,三条都是错的**,
三条都已经**钉进 `--selfcheck`**(分别 3 / 3 / 2 条断言),不会再犯。

### 4.1 假阳性一:27 秒的「死锁」其实是在打**普通营**

`6df84c/20260829_124418_slot1`(种子 1603,radiant-armed)**vengeful_spirit,10 级,armed 腿**:
第一版工具只问「有没有打远古」,于是报出一段 **27 秒 `stuck`**,hp **1.000 → 0.704**,
坐标 `(-5005, -68)` **逐位不动**,距远古中心 592 u —— 教科书般的 GH #265 死锁形状。

**帧说的是反面**:
`t=758.1` `vengefulspirit_wave_of_terror` 同时打到 `ice_shaman` + 两只 `frostbitten_golem`(各 84);
此后 `t=759.1 → 782.9` 是**每 0.7 秒一次的 ~90 平砍**;
`t=772.4` / `t=782.9` / `t=784.8` / `t=785.5` / `t=786.9` 五条 `DEATH` 全部记在她名下;
金钱 `+180 / +88 / +162 / +70 / +44 / +38`。**她有的是东西打,从来没被卡住。**

⇒ 死锁的判据**不是「没打远古」,是「对任何中立零输出」** —— 这正是 GH #265 原文断言的
「ZERO damage events of its own」。**一个 146 u 外的普通营在任何可用半径里都躲不掉**(§3.3),
所以位置代理**永远**分不开两种营,判据必须从伤害侧下。

### 4.2 假阳性二:「逃逸」其实是**传送**

`6df84c/20260829_123218_slot8`(1603,radiant-armed)**crystal_maiden,10 级,armed 腿**:
`t=594.4 → 598.4` 钉在 `(-4277, -183)`(距远古 568 u),`t=603.8` 打到 `npc_dota_creep_badguys_ranged`
—— 第一版判 **ESCAPE**,也就是 `campvoid` 自己的签名。

**帧说的是**:`t=593.6 ITEM item_tpscroll`,而**下一帧 `t=599.4` 她在 `(4025, -6211)`**,
**距离 10,417 u,在地图另一侧**。她打的那只小兵**从来没进过营地的 900 u 扫描**。

⇒ `:752` 下的是 `Action_AttackUnit(farmTarget, true)` 这种**本地命令**,
所以接触点必须是他**走得到**的:`900 + 550 u/s × escape_w`(6 s ⇒ **4200 u**),
**上限是从引擎移速上限推的,不是拍的**。

### 4.3 假阳性三:「逃逸」出现在 **gate 关着的那条腿**上,而且是**团战溅射**

`8d47de/20260829_184550_slot1`(W26,种子 1747,**dire-armed** ⇒ oracle 在 **baseline 腿**,
`campvoid` 在这条腿上**根本不可能触发**)**oracle,11 级**:
`t=895.5 → 898.5` 穿过营框,`t=901.7` 「打到小兵」。

**帧说的是团战**:`t=895.5 / 898.4` 他在给队友 viper 上 `oracle_purifying_flames`;
`t=900.8` 吃 `dragon_knight_breathe_fire` **217**;`t=901.2/901.4` 对 skeleton_king 开
`fortunes_end` + `purifying_flames`;那两条小兵伤害是 **`oracle_fortunes_end` 的溅射**
(`flagbearer` 56、`melee` 93),不是农野命令。敌方英雄是 **episode 关闭之后**才到的,
所以 `FIGHT_NEAR` 看不见他们。

⇒ 接触必须是**裸平砍**(战斗日志里 `inflictor == dota_unknown`,
与 `campfarm_target.ATTACK_INFLICTOR` 同一个常量),AoE 溅射不算。
**这一条最值钱的地方是它出现在 baseline 腿上** —— 一个只可能由 armed 腿产生的形状
出现在 gate 关着的腿上,那说明**分类器本身**有毛病,不需要等两层反号来提示。

---

## 5. 深查 6 局(逐帧)

除 §4 的三局外,另外三局:

### 5.1 ⭐⭐ `a29ed3/20260829_123240_slot1` luna,10 级,**armed 腿**:GH #265 的形状在 armed 腿上原样复现

(种子 **1664**,radiant-armed;luna team=2 ⇒ **armed**。
⚠️ **这个局名在本波里撞车**:`20260829_123240_slot1` 在 `b1386e`(1633)与 `a29ed3`(1664)**两个 run 里都存在**
—— 正是章程记的「`.dem` 同名跨 run 撞车」。`b1386e` 那一局**没有 luna**,
所以本节的帧只可能来自 `a29ed3`;为免下次靠运气,`campvoid_escape.py` 的每条记录**本轮起带 `run` 字段**。)

| t | lvl | hp | 距远古 |
|---|---|---|---|
| 604.5 | 10 | 0.928 | 415 |
| 606.5 | 10 | 0.937 | 578 |
| 608.5 | 10 | 0.947 | **1172**(出去了) |
| 612.5 | 10 | 0.965 | **183**(又进来) |
| 613.5 | 10 | 0.919 | 141 |
| 616.5 | 10 | 0.881 | **943**(又出去) |
| 619.5 | 10 | 0.813 | **261**(第三次进来) |
| 621.5 | 10 | 0.800 | 904 |

(工具在这 17 秒里给她开了**三段** `stuck`:`t0` = 604.5 / 611.5 / 618.5。)

- **17 秒里三进三出**,与 GH #265 那个「19 秒穿越六次」的 4 级 ES 是同一个指纹。
- `t=613.2 → 619.8`:`black_dragon` / `black_drake` 打了她 **12/33/12/36/13/13/34/13/12/33/12/12**;
  **595–625 全区间她自己的第一条输出在 `t=622.8`**,而且第一个目标是 `alpha_wolf`(**普通营**),
  远古要到 `t=623.3` 才挨第一下。⇒ **前 17 秒对任何中立零输出。**
- 最近敌方英雄 **4,413 – 6,667 u**(t=605.5/613.5/619.5 逐帧扫),最近队友 3,556 u。**无敌方压力。**
- hp 0.937 → 0.795(**14.2 pp**),换到的是零。
- ⇒ **`campvoid` armed 没有化解掉它立案要治的那个场景。** 与 §3 的 0/160 一致。
- ⚠️ 诚实边界:这一段被 episode 构造器拆成了**两段 3 帧的短 `stuck`**(因为她真的走出去过),
  所以**它不在 §3.2 的 `stuck` 计数里以一整段的形式出现**。
  「振荡型死锁」需要一个**跨 episode 的合并规则**,本轮没做,记为下轮欠账。

### 5.2 ⭐⭐ `6df84c/20260829_123218_slot8` crystal_maiden,**9 级**,armed 腿:15 次**裸平砍**打远古营

同一局的**另一段**(与 §4.2 是不同时间窗)。

- `t=564.4 → 581.4` 钉在 `(-4602, 466)`(距远古中心 503 u)**18 秒不动**。
- `t=564.6 → 580.7`:对 `npc_dota_neutral_granite_golem` / `npc_dota_neutral_rock_golem`
  **15 条 `infl=dota_unknown` 的裸平砍**(53/54/56/67/67/67/64/64/67/67/63/67/65/55),
  外加 `t=562.4` 一条打 `alpha_wolf`。**平砍 = 她主动选了这个目标**,不是 AoE 蹭到。
- **等级 9**(`t=580.4` 才升 10)。最近敌方英雄 **3,798 u**。
- ⭐ **为什么这一帧比 GH #265 的两帧更硬**:9 级**低于 10..11 那个带**,
  所以 `mode_farm_generic` 的 `bot:GetLevel() >= 10` 与 `utils.IsValidCreep` 的 `GetBot():GetLevel() > 9`
  **两条出厂门本来也该拒**,再加上 armed 的 `campfarm`(门槛 12)是第三道 ——
  **三道门齐全,攻击照样发生**。⇒ 下这道命令的路径**不在这三道门上的任何一条**,
  这正是 `state.json:campexit_20260828` 记的那句「真正动手的那条分支根本不问等级」。
- 佐证第二局:`6df84c/20260829_123211_slot6` **bristleback,8 级,armed 腿**,
  `t=430.8 → 436.9` 对 `prowler_shaman` / `prowler_acolyte`(**远古豹营**)
  6 条 `quill_spray` + **`t=434.2` 一条 `dota_unknown` 78 的裸平砍**;最近敌方英雄 2,816 u。

### 5.3 `a29ed3/20260829_124546_slot9` crystal_maiden,9 级,**ba 层 armed 腿**:短 `stuck` 其实是**路过**

(run `a29ed3`,种子 1664,dire-armed;CM team=3 ⇒ **armed**。)
`t=629.5 → 632.5` 距远古 330 → 147 → 383 → 643,**平均 380 u/s 一路没停**;
出框之后 `t=633.9 → 639.4` 在 `(3541,-868)` 的**普通狼营**打了两只 `giant_wolf` 并吃掉。

⇒ **§3.2 里那些 2–4 秒、hp 几乎不掉的 `stuck` 是穿越,不是死锁。**
唯一一段长的(27 秒)是 §4.1 那个普通营农野。**129 局里没有第二个 §5.1 那样的振荡段被找到**
—— 但见 §5.1 的欠账:现在的构造器**本来就抓不住振荡**,所以「只有一个」是**下界不是计数**。

---

## 6. 顺带一条给 GH #265 的读数:`campfarm` 的方向在 W25+W26 上**反过来了**

GH #265 的标题句是「三次泄漏全在 armed 腿」,而它自己 §3 明写:**W19 只有 ab 一个物理分层**
(`ba (dire-armed) games 0`),按铁律 4(i) 那张表**不构成结论**,只用来选点。

本轮**两层都有**。`trade_anc`(等级 ≤11 的 bot 打远古)的占比:

| 半径 | ab armed | ab baseline | ab Δ | Fisher | ba armed | ba baseline | ba Δ | Fisher | 池化 Fisher |
|---|---|---|---|---|---|---|---|---|---|
| 400 u | 14/38 36.8% | 17/28 60.7% | **−23.9pp** | 0.081 | 0/8 0.0% | 11/20 55.0% | **−55.0pp** | 0.0097 | **0.0076** |
| 600 u | 26/117 22.2% | 21/72 29.2% | **−6.9pp** | 0.302 | 0/43 0.0% | 15/57 26.3% | **−26.3pp** | **0.00011** | **0.021** |
| 800 u | 28/323 8.7% | 24/188 12.8% | **−4.1pp** | 0.172 | 1/107 0.9% | 16/159 10.1% | **−9.1pp** | 0.0018 | 0.022 |

**三个半径、两个分层,六个 Δ 全部同号(负)** —— 即 armed 腿的「低等级打远古」占比一律更低,
方向与 `campfarm` 声称的一致,与 GH #265 单层读出的方向**相反**。

⚠️ **但这不是 `campfarm` 的读数,不许这么引用**(章程 §4a)。44 个 armed id 里,
`campsel`(营地选择器)与 `abilanc`(英雄技能对远古的扫描选择器)**都能压低同一个量**;
`campfarm` 只是其中之一。本表**唯一稳当的用法**是:**它抽掉了 GH #265 标题方向的语料支撑**,
把 #265 §4 的二分(BUGGY 还是 SILENT)推向 **SILENT** 那一侧 —— 而 §5.2 的 9 级 / 8 级平砍
正是 SILENT 的直接帧证据(门齐全,动手的分支不在门上)。

---

## 7. 诚实边界(不写进结论的东西)

1. **章程 §4a 要的触发级证据,在这份语料上结构上买不到。** dumper 不发任何 creep 实体
   (`dumper/main.go` 只保留至少一侧是英雄的战斗日志条目,且没有 creep 快照类型),
   于是 `bot:GetNearbyNeutralCreeps()` 与 `bot:GetNearbyLaneCreeps()` **两个合取项都读不到**。
   §3 买的是**结果**不是**触发**。这一条已写进工具文件头,并将开 `[harness]` issue。
2. **`escape` 是 campvoid 形状结局的严格子集。** bot 也可能走出去却**没在 6 秒内平砍到**
   (`Action_MoveToLocation` 之后模式切换),那种会落进 `left`,而 `left` 占 62–95%,
   **吸收得下任何一个未观测到的逃逸**。⇒ 「0 次」是**下界读法**,不是「出口从没打开过」的证明。
   下轮做 `--escape-w 12` 的敏感度对照。
3. **位置代理不是干净的子集也不是超集**:`--in-camp` 对 330 u 的近战扫描偏松、
   对 780 u 的远程扫描偏紧,所以三个半径全给了;**任何只在一个半径成立的读数不算读数**。
4. **W26 只有 8 局有帧**(`rec_slots 1`,GH #308 未裁),所以「8 粒种子」是**经济读数**的口径,
   **不是**本报告的帧覆盖口径;本报告的帧几乎全部来自 W25。
5. **129 局不是我抽的**,是录制槽决定的;ab 82 局 / ba 47 局,**最薄的格是 `ba/armed` 8 个 episode(400 u)**,
   **不可逐种子比较**。
6. **`stuck` 的两层反号**(600 u:ab armed 8/117 vs baseline 3/72;ba armed 2/43 vs baseline 4/57)
   ⇒ 按铁律 4(i) **不写进结论**,本报告没有基于 `stuck` 计数的任何主张。
7. 本轮**未跑** 15 个通用检测器;**`seed 975` 连续第八轮**未处理;
   `wandlimbo` 预登记因 **#293 未落地**第六轮不可执行(本轮未复核);
   **GH #265 的执行仍被 #272 阻塞**,本轮未复核 #272(但本轮的产出是**给 #265 追评**,不需要解阻塞)。

---

## 8. 交付与交棒

**入库**:`tools/batch_test/behavioral/campvoid_escape.py`(新,`--selfcheck` 49 PASS)。
**本轮未改任何 `bots/` 或 `game/` 下的文件,未动任何 gate。**

1. **协同组 / 总监 —— `campvoid` 条件 (a) = SILENT,建议不 promote、回炉重划域**(本轮开 issue,`[strategy]`)。
   根因不是接线错,是**声明的域比真实域宽**:删掉远古**不足以**让 `#nNeutrals` 归零,
   还要求 146 u 外的邻营同时是空的;外加「野区营地 900 u 内有敌方小兵」这条稀有几何。
   建议的验收:`make_fixture.py --t 613.5 --hero luna`(§5.1 那帧,armed 腿、10 级、
   三进三出的中点),在真实帧上直接断言 `#NeutralPresenceList(...)` **是不是 0**。
2. **总监 —— GH #265 追评**(§6 的两层读数 + §5.2 的 9 级/8 级裸平砍帧),
   把 #265 §4 的二分推向 SILENT,并指出它标题方向的语料支撑已经被抽掉。
3. **总监 —— `[harness]`**:dumper 无 creep 通道 ⇒ 章程 §4a 给 `campvoid`/`campexit` 写的
   触发级买法**在任何现有语料上都不可执行**。要么给 dumper 加一条近英雄 creep 存在通道,
   要么修订 §4a 的措辞。(与本轮 `[strategy]` issue 合并成一条提出,避免两条 issue 讲同一件事。)

**下一轮第一件事**:(1) `--escape-w 12` 敏感度 + 「振荡型死锁」的跨 episode 合并规则(§7.2 / §5.1 两笔欠账);
(2) 下一个零记录 id:`pullcad`(有工具无裁决)或 `fieldcreep`(与 owner P2 的野区续航同族,优先);
(3) 若 W27 起飞,先跑 `arm_string_census.py` —— **它的 arm 串是 45 id(`fieldsip` 入集),
与 W25/W26 的 44 id 串不同,不能并进本轮这份语料**。

---

## 9. 验证(铁律 6)

- `bash tools/agent/luacheck_gate.sh` → **exit 0 / 0 warnings**(脚本自装 `lua-check`)。
- **未使用 `RULE6_BYPASS` ⇒ 本报告没有「SKIPPED, not passed」那一行要抄。**
- `bash tests/run_py_tests.sh` → **53 passed / 0 failed / 0 uncertifiable**。
- `campvoid_escape.py --selfcheck` → **49 PASS / 0 FAIL**。
- 本轮未改 Lua ⇒ **不声称跑绿过 Lua 全量**(GH #124)。
- **MCP 未触发 `requires approval`。** AWS:`session_setup.sh` 走通,**S3 只读,零 EC2,零 CE 调用**。

---

## 10. token 用量(铁律 8)

```
TOKENS total_in=21,903,924 out=102,729 turns=124
```

(`python3 tools/agent/token_usage.py`,统计时刻为收尾前;此后的收尾回合不计入。)
