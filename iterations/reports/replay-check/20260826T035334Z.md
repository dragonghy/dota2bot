# 录像检查组 2026-08-26T03:53Z —— #197 的两个反例:一个被自己的大招解释掉,fixture 已钉

**一句话**:接自己上一轮交出的 backlog 第 1 棒 —— 给 GH #197 的两个 `campfarm` 反例
做 fixture 裁决。**新 fixture `f_212636_tide_ancient.lua` + 9 条测试全绿**,答掉 #197 §3
的第 1、2 问:**门本身没坏**(armed 腿 L10 上,真的 `FilterFarmNeutrals` 确实把两只远古
剔掉、只留普通野,且换到 dire 腿/无 soak 文件时按恒等返回)。第 3 问(当时的 active
mode)**任何 fixture 都答不了**,但**逐帧把它从外面夹住了**:例 ① 那个营地在**整局里的
第一次战斗事件,就是这个 bot 自己的大招 Ravage**(t=614.7,比被举报的那一下**早 12.8 秒**),
所以 #197 §1 用来排除「引擎自动还手」的那句「此前一帧都没挨过打」**在帧上不成立**——
它只看了「打到主角身上的伤害」,而且 episode 窗口从 t=624.5 才开。例 ② 同型但更弱:
远古雷兽营**整局第一次战斗是 t=642.8 它打 sven 的队友 spirit_breaker**,比被举报的
668.1 **早 25.3 秒**。

⇒ **两个反例都不是「门在那一帧没生效」的证据**,`campfarm` 的 (a) 读数
(**domain-too-small,不是 SILENT-because-broken**)**不因这两帧改变**。
球仍在总监:`campfarm` 的三条件判定。

零 EC2 支出(S3 只读),`bots/`/`game/` **0 改动**,gate 未动。

---

## 1. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh`:UNLANDED **0**(certifiable window)、
python 套件 **31 passed 0 failed**、`stable-v1`/`stable-v2` 锚点 EXISTS/PINNED/SHIPPED
全 ok、queue 33 open(RIDESHARE 1 条待总监裁)。CADENCE 5 个洞(3.5–8.9h),含本组
自己两个(08-25T06:54→10:42、15:38→19:18),与前几轮同型,不升级。
Lua 侧起手 `SKIP (no lua5.1)`(GH #171 那条**第三轮**未裁);本轮 `apt-get install lua5.1`
数秒装上,后面所有 Lua 测试都是真跑的。

`iterations/OWNER_PRIORITIES.md`:P1 球在协同组、P2 球在协同组、P3 球在总监 ——
**本轮没有属于录像组的未完成优先项**,故按本组 backlog 第 1 条执行。

## 2. 本轮范围:为什么不是宽扫

批测台 03:15Z 报告:**W12 于 03:10:52Z 首台实发**(树 `14004d85`,36-id 全集,
seed 888/895/896/906)。本轮开工 03:38Z ⇒ **S3 上 W11 之后没有任何新对象**,
**没有可宽扫的新语料**。按章程「没有新批次:补课或自由巡查」,做 backlog 第 1 条
(上一轮登记:「**#197 的 fixture** —— 唯一能把那两个反例从『没解释掉』变成结论的东西」)。

- **宽扫 0/0 局**(无新语料,不是跳过)
- **深查 2 局逐帧**(#197 点名的两局,两侧事件流全量重放):
  `..._d21f62 / 20260825_212636_slot7`(例 ①)、`..._082cd8 / 20260825_212624_slot5`(例 ②)
- **fixture 1 枚 + 测试 9 条**

## 3. ⭐⭐ 例 ① 被它自己的大招解释掉了

`spot_20260825_211022_..._d21f62 / 20260825_212636_slot7`,tidehunter,armed 腿
(`analysis.json: script_version = mirror:...,campfarm:s896:radiant`,主角 team 2 = radiant),
`mode = turbo`。**逐帧(事件流原文,非聚合)**:

| 时刻 | 事件 | 意义 |
|---|---|---|
| 612.6 | `ABILITY tidehunter_gush -> ogre_magi` | 团战开始,**营地上打团** |
| 613.0 | `ABILITY tidehunter_ravage` | 大招 |
| **614.7/614.8** | **`DAMAGE ravage -> granite_golem / mud_golem / rock_golem ×2,各 193`** | **这是该营地整局第一次战斗事件,施加者就是主角本人**,比被举报的那一下早 **12.8 s** |
| 613.7 / 615.0 | GOLD 134 + XP 728 / GOLD 180 + XP 668 | 两个人头(slardar、ogre_magi);**L9 → L10 在 t=614.5** |
| 615.9 | `ABILITY anchor_smash` | 又一次 AoE 落在营地上 |
| 617.5→621.5 | (−5518,1539) → (−6156,2226) | 走开 |
| **622.4** | **`MODIFIER_ADD rock_golem -> tidehunter modifier_ancient_rock_golem_weakening`** | 折返,重新进营地光环 |
| 623.5 | `DAMAGE rock_golem -> zuus 21` | **营地在打主角的队友** |
| 623.6–626.6 | luna(L14)连续普攻 + 月刃 | **队友已经在打这个营** |
| **627.1** | **`DAMAGE tidehunter -> rock_golem 102`(无 inflictor = 普攻)** | **被举报的那一下** |
| 629.8 | `DAMAGE granite_golem -> tidehunter 24` | #197 说的「首次挨打」 |

**#197 §1 的推理错在哪**:它写「此前一帧都没挨过打 ⇒ 不是被营地咬了之后的引擎还手」。
那句话**只看了打到主角身上的伤害**,而且 episode 窗口**从 t=624.5 才开**,把主角自己
12.8 秒前的大招 AoE 关在窗口外了。真实状态是:决策瞬间这是**刚打完团的战场**,
营地已经和这支队伍交火 4 秒(且第一下是主角自己造成的),**引擎自动索敌完全在牌桌上**。

**顺带一条反向证据**:`mode_farm_generic.lua:764-782` 的**防抢野分支**
(`J.GetAlliesNearLoc(targetFarmLoc, 800)` + `J.IsFarming` + `J.IsAttacking`)——
t=627.5 时 **luna 336u、zuus 343u,两人都在这个营 800u 环内且都在打**,
⇒ 如果真是打野路径在下令,它会**把主角推离这个营**,而不是推向它。

## 4. 例 ②:同型但更弱,**部分**解释

`..._082cd8 / 20260825_212624_slot5`,sven,armed 腿(`campfarm:s888:radiant`,sven team 2 = radiant)。

- **远古雷兽营整局第一次战斗事件 = t=642.8 `big_thunder_lizard -> spirit_breaker`
  (sven 的队友)**,比被举报的 668.1 **早 25.3 s**;650.2 它又打 spirit_breaker 41,
  650.3–651.6 敌方 drow_ranger 用 `multishot` 连续轰这个营。
- 660.3–663.5 sven 打的是**团战**:`gods_strength`(大)→ `storm_bolt -> drow_ranger`
  → `warcry` → `mask_of_madness`,drow 用 `wave_of_silence` 把他**沉默+击退**(至 667.3)。
- **665.8 `sven_great_cleave -> centaur_khan 209 / centaur_outrunner 244`** ——
  他对中立单位的第一下伤害是**普攻半人马时的被动溅射**,**不是选择**。
- 667.5 drow 已退到 **1054u**、668.5 出 1400u 窗口 ⇒ **敌人跑了**,主角站在
  115–247u 的中立堆里。**668.1 普攻 `big_thunder_lizard 252`**,**668.5 雷兽回打他 23**。
- **诚实边界**:与例 ① 不同,sven 对**这个营**的第一下就是 668.1 那记普攻(他之前的
  溅射打的是隔壁半人马营)。所以这里只能说**「冷启动地从打野列表里挑中远古」不是自然读法**
  (营地已被两队打了 25 秒、0.4 秒后就还手),**不能像例 ① 那样直接判掉**。
  **离线仍分不开下令方** —— 这条能力边界照原样保留。
- **没开 [strategy] issue**:一度想报「开着大招的 sven 丢下 0.41 血的 drow 去打野」,
  逐帧一看**不成立** —— 他 663.3–667.3 被沉默击退,解沉默时 drow 已在 1054u 外撤退中。
  **证据不支持的指控不开 issue。**

## 5. ⭐ 方法学:AoE/被动溅射误算的第三、四例

上一轮(01:07Z §"opened 不等于他选的")抓到 `zuus_static_field` 会**先于**主角落下第一下。
本轮又两例,**同族但机制不同**:
- `tidehunter_ravage` / `tidehunter_anchor_smash`:**主动 AoE**,把整个营地写进战斗史;
- **`sven_great_cleave` / `luna_moon_glaive`:被动、无 ABILITY 事件,只在 DAMAGE 的
  `inflictor` 里露头。**

`campfarm_target.py` 的 `opened_by` 按 inflictor 分类,**这两类都会正确落到 `ability`**
(`--selfcheck` 已有 `opened_by: a bounce is an ability` 这条),**所以工具没读错**。
**真正的缺口在 episode 窗口**:`attacked` 为真的那一下,可能落在一个**早已被点燃的营**上。
⇒ 新 backlog 条目:给 `campfarm_target.py` 加一列
**`camp_prelit`(episode 首帧之前 N 秒内,该营与任一方英雄有无伤害往来)**;
本轮两例分别会读出 **12.8 s / 25.3 s**,四个 armed/baseline 命中都该带上这一列再解读。

## 6. 交付:fixture 与 9 条测试

- **`tests/fixtures/f_212636_tide_ancient.lua`**(`make_fixture.py --t 627.5 --hero tidehunter
  --roles <analysis.json>`;units=10、buildings=38、modifiers=43、recent_damage=12)
- **`tests/test_replay_212636_tide_ancient.lua`** —— **9 tests, 0 failures**。
  **一个 `J.*` 都没打桩**:`IsSoakCandidate` / `IsModeTurbo` 都跑真实函数体,
  gate 用**真的 `bots/Customize/soak_side.lua`**(写 → 跑 → 删,同 `test_aegis_grouping.lua`)。
  - `[frame]` 域:`fx.time == 627.5`、`bot:GetLevel() == 10`、`ANCIENT_MIN_LEVEL`
    **从源码读**( = 12)、`IsModeTurbo() == true`、armed 时 `IsSoakCandidate('campfarm') == true`
    且 `bot:GetTeam() == 2`(= 本波 armed 侧)。
  - `[gate]` armed L10:**两只远古被剔、且只有它们被剔**(泥人留下 = 阴性对照),返回新表;
    armed L12:**按恒等返回同一张表**。
  - `[control]` **两条反证**:① 把 soak 文件写成 `side='dire'` —— 同一帧、同一份野怪表,
    **列表必须一字不动**(防「其实是等级单独在剔」);② 无 soak 文件 —— 恒等返回。
  - `[frame]` 三条从 fixture 自身读出的证据:营地光环在主角身上已 **elapsed 5.1 s**;
    `rock_golem` 在 **dt=4.0 s** 前打过一个英雄(zuus 的 `recent_damage`);
    **两个队友在营地 800u 内、1600u 内零个活着的敌人**(slardar 665u / ogre_magi 877u 都是尸体)。
  - `[source]` gate 仍是 `IsModeTurbo() and IsSoakCandidate('campfarm')`、
    **恰好一个 soak id**(`pullcad` 那条教训写成断言)、**三个扫描点都还走 `NeutralFarmList`**。
- **声明的构造输入**(GH #61,写在文件头而不是藏在 loader 里):野怪那一半是**替身** ——
  fixture 上 `GetNearbyCreeps()` 恒答 `{}`(GH #100),所以喂给过滤器的三只野怪是**本文件按
  timeline t=626.5 采到的营地(距主角 197/220u 两只远古 + 一只泥人)构造的**。
  ⇒ **第 1 问测的是过滤器,不是端到端的下令链**。第 3 问(active mode)**.dem 里没有,
  任何 fixture 都答不了**,照旧留空,由 §3/§4 的帧证据从外面夹。

## 7. 连带:四个语料计数测试的分母

新增一枚 fixture 会动到四处**语料规模**断言(不是几何常量)。按各文件自己的惯例
(带日期 + 增量原因的注释)更新,**没有放宽任何断言**:
- `tests/test_write_only_local_census.py`:`carrier_fixtures` 61 → **62**;
  `towers == 22` 等几何常量**未动**。
- `tests/test_activemode_world_assertion.lua`:`in_teamfight_1500` 86 → **89**
  (hero-frames 966 → **974**,不是 976 —— 那一帧 slardar / ogre_magi 是**尸体**,
  这个 sweep 只数 `u.alive`);世界断言本身(`mode_nonzero == 0`、过滤器仍被忽略)**未动**。
- `tests/test_focus_innate_index_anchor.lua`:crystal_maiden 50 → **51**、lion 22 → **23**、
  zuus `frames` 50 → **51** / `with` 47 → **48**;axe 与 skeleton_king **不在那一帧,未动**。
- `tests/test_itemdesire_world_assertion.lua`(四处):`crash_total` 211 → **213**、
  `crash_3325` 33 → **35**(`:2597` 未动 ⇒ **没有出现第三个崩溃点**)、
  `alive_H` 563 → **571**、有/无 buildings 块的切分 61/43 → **62/43**(fallback 侧不动)。
  ⚠️ **这一处不是纯记账**:`rest5_H` 211 → **218**(那 8 个英雄里 7 个满足其余五个操作数);
  `outer_and_H` **不动**(该帧最高级是 luna/lina 的 14,**没人到 15**),所以 7 个全部落在
  `sole_blocker_H`(207 → **214**)那一侧。
  **这条要写进章程**:先失败的断言会让后面的断言不执行 —— 第一次跑只报了 4 条,
  改完再跑才露出第 5 条(`rest5_H`)。**加 fixture 那一轮要反复跑到零失败为止,
  不能看第一次的失败清单就以为改全了。**

## 8. 交棒

- **GH #197 追评**:两个反例的帧证据 + fixture 结论(**门没坏**;例 ① 判掉,例 ② 部分),
  并建议总监**据此关闭**(本组不裁 promote/reject,只交证据)。
- **GH #137 追评**:`campfarm` 的 (a) 读数不因这两帧改变;新增 `camp_prelit` 列的必要性。
- **球仍在总监**:`campfarm` 的三条件判定。
- **本组不裁 promote/reject、不申请波次、不改 bot Lua、不花 AWS 钱。**

## 9. 验证

- `bots/` / `game/` **0 改动** ⇒ 铁律 6 的 luacheck 一条无适用对象(容器无 luacheck,
  **不声称跑过**)。
- Lua:`lua5.1 tests/run_tests.lua 212636_tide_ancient` **9 tests / 0 failures**;
  `... campfarm` **16 / 0**;`... test_corpus_scale` **8 / 0**;
  `... test_activemode_world_assertion` **13 / 0**;`... test_focus_innate_index_anchor` **13 / 0**;
  `... test_itemdesire_world_assertion` **25 / 0**。
  **清扫**:对 **31 个会枚举 `tests/fixtures/` 的测试文件**逐个跑,收尾一轮
  **零失败**(第一轮 3 个文件红、修完再扫又露出第 4 个文件、再修才全绿 —— 见 §7 那条)。
  **全量 Lua 套件在容器里跑不完(≈100 min,GH #124),不声称跑绿过全量。**
- Python:`bash tests/run_py_tests.sh` **31 passed / 0 failed**。
- **AWS**:S3 **只读**(2 个 `.dem` + 2 个 `analysis.json` + dumper 缓存命中),
  **未启动/未终止任何实例**,**未调用 Cost Explorer**。

## 10. 下一轮优先

1. ⭐ **W12 首检**(03:10Z 发波,下一轮应有语料)—— 宽扫 + `campfarm` 之外的 armed id 核验;
2. ⭐ **`camp_prelit` 列**(本轮 §5 立的,两例读数 12.8 s / 25.3 s 已在手);
3. ⭐ `tpdefend_events.py` 加结果侧列(**#191 落地后**,第六轮登记);
4. ⭐ 幻象出生下界 vs 检测器域上界的余量做成 `sweep_run.sh` 常规输出(**第三轮**);
5. ⭐ `entities.py` 推广到 #176 §3 其余检测器(**第四轮**);
6. ⭐ 12:41Z §6.1 那 23 帧做 fixture(**第六轮**);7. ⭐ `pulldrag` 的 connect 侧;
8. ⭐ 第二种视野证人(**连续第十轮顺延**);
   ab/ba 回灌 `fieldbuy_silence.py`/`stayfield2_margin.py`(**连续第十七轮登记**);
   `stayfield` 第一失败子句;打野反证 fixture(**二十三轮**);`hero-1` 的 153 局 WK 语料(**31 轮**);
   `l5combo` 的 (a)(**第四十七轮**);`make_fixture.py` 钉 `062551 t=205.5 jakiro`(#45,**第三十四轮**);
   `axebuyblink` armed 的波次。

## 11. Token 用量

```
TOKENS total_in=16,754,038 out=64,199 turns=101
```

(到统计时刻为止;统计后的收尾回合不计入。)
