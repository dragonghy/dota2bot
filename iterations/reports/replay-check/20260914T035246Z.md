# replay-check 2026-09-14T03:52Z — 第七十三轮

**一句话**:交棒第 (3) 条那一格交货了 —— #799 **验收 3 的 fixture 落地并会咬人**(两个变异体全杀);
而买它的过程里**逐帧推翻了上一轮自己那条「承重」判决** —— 那一局的 bristleback
**19→24 级是一次 12376 的 XP 跳变、0.5 秒后就死了、躺了 71 秒**,
⇒ 「23 级假、24 级真」**在这局根本没有 23 级的活帧可读**,而「等级到了」与「复活了」
**预测同一个时间戳**,本局分不开。

⭐⭐ **并列头号(免费捡到的,域比本格大得多)**:同一条时间线上**十个英雄全部**有
**分钟级的「活着但一点不花」停摆**,中位数 **615 秒**;OD 最狠 **1139 秒**(19 分钟)
且终局只有 **6** 个技能点。停摆**不是 bristleback 的怪癖,是这局的普遍状态**。

---

## 一、覆盖(⚠️ 低于章程下限,如实登记不粉饰)

- **宽扫 0 局 —— 故意不跑**:连续第**十二**轮无新波次。批测台 03:11Z 报告确认零发波
  (MTD `$89.505` 第三次逐位相同,闸 (iii) 已红、刹车已咬)。语料仍 W40。
- **深查 1 局**(`ab6c0d/20260902_220100_slot6`),**低于章程的 6 局下限**。
  理由是上一轮交棒逐字指定了这一格(「验收 3(fixture)是现在最便宜的一格,帧已选好」),
  且逐字写着 ⛔ 别再为这格下 `.dem`。**不拿别的局凑数**。
  代偿:这 1 局做的是**全员 × 全程**的逐帧读(1663 帧 × 14 具身体,见 §四),
  不是单帧读,覆盖的英雄数是 10 不是 1。
- `DUMP_EXIT=0`,`MF1_EXIT=0`/`MF2_EXIT=0`,unparseable 0。

```
VERIFY id=skillstall verdict=NOT-ARMED episodes=10
```
(`episodes=10` = 本局**逐帧核到底的活体英雄数**。⚠️ 按铁律 4(iii) 连切法登记:
与上一轮 `id=skillstall episodes=6`(干净形墙条数)、再上一轮 `episodes=4`(OD 基线腿局数)
**都不是同一个量,别混引**。`skillstall` 在 W40 语料里不存在,故 NOT-ARMED 不是「没触发」。)

## 二、⭐ 交付物:#799 验收 3 的 fixture(会咬人,已做变异台)

- `tests/fixtures/skillstall/f_ab6c0d_bristleback_skillwall_650.lua`(立墙)
- `tests/fixtures/skillstall/f_ab6c0d_bristleback_skilldrain_1085.lua`(塌墙)
- `tests/test_skillstall_wall_frame.lua`(5 节,`[ratchet]`)

钉住的正是验收 3 那句话(**队头不可升级时后续项仍被消费**),读数逐字:

| 帧 | 英雄等级 | 技能点 | 天赋 | 已消费队列项 |
|---|---|---|---|---|
| `t=650.4` | 14 | 13 `{goo3,quill4,bristle4,warpath2}` | **1** | **14** |
| `t=1085.4` | 24 | 14 `{goo4,quill4,bristle4,warpath2}` | **1** | **15** |

⇒ 跨过这堵墙**技能点 +1 而天赋数逐位不动** ⇒ **队头(按 `GetSkillList` 算术 = 槽 15 = T2)
没被训练就离开了队列,而它后面那一项被消费了**。这就是验收 3。

- **队列算术已按源码复核**(`aba_skill.lua` `X.GetSkillList`:`i>=10 and (i%5==0 or 能力用尽)`)
  ⇒ `[a1..a9, T1, a10..a13, T2, a14, a15, T3..T8]`,13 能力 + 1 天赋恰好吃掉槽 1..14。
- ⛔ **T2 的身份是源码算术 + 台账,不是对 T2 的直接读数**(dumper 看不见未点的天赋,
  `test_fixture_talent_blindness.lua`)。测试第 3 节断的是**台账**,不断 T2 的身份。
- **变异台(证据纪律 2)**:M1 把天赋差断成 `==1` → 第 3 节红;M2 把塌墙帧技能点断成 13 → 第 2 节红;
  **两个变异体全部被杀**,从文件副本恢复后 `5 tests, 0 failures`。

## 三、⛔⛔ 自我推翻:上一轮那条「承重」判决买不到,本轮撤回

上一轮(00:51Z)写:「**承重的是墙自己塌掉那一帧** …… `IsHidden()` 不随 `botLevel` 变
⇒ **23 级假、24 级真,翻号的只能是等级需求那一项**」。**逐帧不成立,两条独立理由**:

1. **这局根本没有 23 级的活帧。** `t=1012.2` 一次 **XP value=12376** 把它从 19 直接抬到 24
   (`XP` 事件原样);19→24 是**一步**,中间没有任何一级停留过。
2. **「到 24 级」与「复活」在本局完全混淆。** `t=1012.7` 被 nevermore 杀死(`DEATH` 事件),
   `t=1013.4 … 1083.4` **每一帧 `hp_pct=0.000`(躺了 71 秒)**,`t=1084.4` 复活,
   **`t=1085.4` 花点** —— 即**复活后的第一个活帧的下一帧**。
   「等级需求在 24 翻真」与「死人不 think,活过来第一帧就花」**预测同一个时间戳**。

⇒ **#799 验收 1 仍然欠着,而且欠得比上一轮以为的多**:上一轮认为只剩「等级需求」一个幸存者,
实际**两个幸存者都还在**。

**另一处不成立的推理**:上一轮以「`skillstall` 门本语料 NOT-ARMED」排除了 `generic_hidden` 分支。
**这不成立** —— 那个门在**终局 `else` 里面**(`ability_item_usage_generic.lua:~415`),
而 `generic_hidden` 是**另一条 `elseif`**(:395),门对它没有任何作用。
按源码,`botLevel<=25` 能把未训练的队头挪走的分支有两条,**都还活着**:
- `:395 elseif abilityName == 'generic_hidden'` → 摘掉队头**并立刻** `ActionImmediate_LevelAbility(nextAbility)`;
- `:402 elseif not IsHidden() and botLevel >= required`(“still try it”)→ 对天赋发一次可能失败的升级后摘掉。

观测到的双拍(`t=1085.4` goo 3→4,`t=1086.4` warpath 2→3)**两条都解释得通**。
⛔ 本轮**不点名分支**,只把「两条都活着」钉进测试头注释。

## 四、⭐⭐ 免费捡到的大结果:这局十个英雄**全部**在长时间停摆

同一条时间线,按 `(hero, idx)` 键取(W71 那条坑:一个名字五具身体 —— 本局
`nevermore` 3 具、`shadow_shaman` 3 具,幻象/分身已被 idx 分开),只统计 **`hp_pct>0` 的活帧**:

| 英雄 | idx | 最高等级 | 终局技能点 | 终局天赋 | **活着的最长零消费段** |
|---|---|---|---|---|---|
| obsidian_destroyer | 1462 | 24 | **6** | 0 | **1139.0 s** |
| witch_doctor | 1389 | 23 | 15 | 1 | 801.0 s |
| crystal_maiden | 1352 | 23 | 14 | 1 | 742.0 s |
| vengeful_spirit | 1243 | 24 | 16 | 0 | 665.0 s |
| skywrath_mage | 153 | 25 | 15 | 1 | 619.0 s |
| shadow_shaman | 1535 | 24 | 17 | 1 | 615.0 s |
| nevermore | 1279 | 24 | 23 | 0 | 613.0 s |
| skeleton_king | 1426 | 26 | 16 | 1 | 460.0 s |
| bristleback | 1498 | 28 | 17 | 2 | 433.0 s |
| death_prophet | 1316 | 24 | 15 | 2 | 302.0 s |

**十个英雄零例外**,中位数 **615 秒**。OD 的 **1139 秒 / 终局 6 点**是 GH #286 那一族
(nil 空洞顶到队头)的又一现场。⚠️ 这是**一局**的读数,不是波次结论;
但它把 #799 的价值论证(条件 (c))从「攥着几个点」抬到「**每个英雄半局不加点**」。

## 五、⛔ 踩到并拆掉的两个坑(都会反复踩,写全)

1. **⛔⛔ `rf.load` 会把全局 `print` 换成 no-op** —— 自写的内联 runner 因此
   **一个字都不打,而且退出码 0**,长得和干净通过**一模一样**。
   本轮第一次跑新测试得 `TEST_EXIT=0` **零输出**,差点当成通过。
   判别子极便宜:`io.stderr:write` 能出、`print` 不能出。
   ⭐ **正确做法就是仓库既有约定**:文件 `return tests`,由 `tests/run_tests.lua` 驱动(它不走 `print`)。
   已把这条写进测试文件尾部注释。
2. **⛔⛔ 新 fixture 落进 `tests/fixtures/` 平级目录会顶红别人的 census(GH #624 形状)。**
   实测平级时顶红**三条**(不是两条 —— 第三条是 开工自检 的 Lua 腿替我抓到的):
   `test_cm_ult_reach_meter_domain.lua:429` **`expected 70, got 72`**、
   `test_cm_cmqreach_transit_frame.lua:321` **「2 talent rows on some hero, not 1」**、
   `test_axe_t15_in_domain.lua:527` **「some hero unit now carries 2 special_bonus entries」**;
   移进 `tests/fixtures/skillstall/` 子目录后**三条全部回绿**(两个方向都实测过才写这行)。
   原因:95 个测试用 `ls tests/fixtures`、47 个用 `tests/fixtures/*.lua`,**两种 glob 都是平的**;
   `tests/fixtures/outchan/` 是既有先例。
   ⛔ **不要反过来「修」成抬 census 数字**:415 个测试读这份语料,覆盖它们的是 ~100 分钟那套(GH #124),
   **一个工作单元内验证不了**。这两帧是**为一个缺陷钉的一对**,不是样本材料,不该动任何人的分母。
   ⚠️ **连带更正**:本轮 开工自检 的 Lua 腿打出的那三条红(两条 CM + 一条 axe),
   **是我自己在飞的 fixture 造成的瞬态,不是 trunk 的红** —— 移目录后三条已逐条复测回绿
   (`axe_t15_in_domain` / `cm_cmqreach_transit_frame` / `cm_ult_reach_meter_domain` 各 `EXIT=0`)。
3. **⛔⛔ 子目录挡得住平 glob,挡不住递归 glob —— 第二道 census 是 push 闸替我抓到的。**
   移进子目录后 push 仍被拒:`tests/test_slotdust_dust_arbitration.lua`
   「the fixture item vocabulary changed size: **124**」。它用的是
   `grep -rho … tests/fixtures/`(**递归**),所以子目录对它无效。
   实测两个方向确认是我的:带我的 fixture `EXIT=1`,移走 `EXIT=0`。
   **按闸自己的话登记(「census 要你登记,那是它在工作不是在坏」)**:
   vocabulary `117 → 124`(新增 7 个名字),**且这次 `nMissing` 真的涨了 `23 → 25`** ——
   两个不解析的是 `greater_critical` / `gris_gris`,都是**bots/ 从来不提的 dumper 实体类名**
   (Daedalus / Gris-Gris),属于该上限自己注释里说的「良性那一半」,不是命名空间分歧。
   ⭐ **明写不吸收**:那条上限的全部职责就是**不许悄悄涨**,所以 7 个新名字与两个新洞
   逐字写进了它的注释。
4. (登记,非本轮引入)开工自检 报 `NEW UNCOVERED no_manifest_row tests/test_dusttower_dive_guard.lua`
   —— **不是本组的活**,只登记。本组新测试**已自己上闸**:
   `tools/agent/lua_gate_manifest.json` 加一行(`in_gate: true, fast, 0.232s`,实测),
   **手工加一行而不是重跑 `lua_gate_measure.py`**(后者会重写整份 453 行清单,不是本组该动的范围)。

## 六、issue

- **净增 0**。**GH #799 追一条评论**(撤回上一轮的验收-1 判决 + 交付验收 3 + §四 的十英雄读数)。
  ⛔ 本组**不碰 issue 状态**(#799 已被 21:52Z 关成 `completed`,连续第三轮点名它两条评论逐字写着别关)。

## 七、AWS / 代码边界

- `AWS_SETUP_EXIT=0`,`arn:aws:iam::581436534924:user/dota2bot-agent`。
  **只读 S3(1 个 `.dem`,21.4 MiB),零 EC2、零 CE 请求、零支出。**
- ⛔ **`bots/` 与 `game/` 一行未改,零新 soak id。**
  改动仅:`tests/test_skillstall_wall_frame.lua`(新)、`tests/fixtures/skillstall/*`(新 2)、
  `tools/agent/lua_gate_manifest.json`(+5 行)。

## 八、下一轮第一件事

1. ⛔ **别重做本轮任何一项**;fixture 已进仓、已上闸,别写第二遍。
2. ⭐⭐ **#799 验收 1 现在是「两个幸存者」不是一个**(§三)。
   **离线仍买不到**(第八例「加局无救」)⇒ ⛔ 别再为这格下 `.dem`。
   要买只有两条路:真实对局里读一次天赋槽的 `GetHeroLevelRequiredToUpgrade()`/`IsHidden()`,
   或者让英雄组在 mock 里给天赋槽真句柄。
3. ⭐ **§四 那张表最便宜的下一步**:它是**一局**的。新波次一到,把同一段读法跑满整波,
   就能把「每个英雄半局不加点」从一局读数抬成波次读数 —— 这是 #799 条件 (c) 最硬的那块。
   读法已在本报告 §四 写全(活帧 + `(hero,idx)` 键),**不需要新工具**。
4. ⚠️ 09-13T16:30Z 那个 **n=1 复现连续第五轮挂账**,只需一局,等新波次。
5. ⛔ 引 §四 的数时**连「活帧 + 按 idx 分身」这个切法一起引**(铁律 4(iii));
   按名字取会把幻象算进去,方向朝着把停摆读长。

## 九、开工自检(真码)与那 6 条 Lua 红的归属

`TRUE_SELFCHECK_EXIT=3`(⛔ 第一跑又踩管道门 —— 脚本自己拒绝并打
`SELFCHECK_EXIT=2 REFUSED (nothing was checked; this is NOT a pass)`,重定向重跑才拿到真码),
`legs run 13`,`UNCERTIFIABLE: none`,
`FINDINGS: cadence queue-rulings owed-executions lua-coverage trunk-red(python) trunk-red(lua)`。

⭐⭐ **`trunk-red(lua)` 那 6 条,5 条是我自己的瞬态,不是 trunk 红。**
自检跑的时候我的 fixture 还平铺在 `tests/fixtures/`。**两个方向各跑一遍**(平铺 / 移进子目录):

| 测试 | 平铺 | 移进子目录 | 归属 |
|---|---|---|---|
| `alchemist_rage_clock_staged_frame` | 红 | **绿** | 我的,瞬态 |
| `axe_bkb_supply_staged_frame` | 红 | **绿** | 我的,瞬态 |
| `axe_t15_in_domain` | 红 | **绿** | 我的,瞬态 |
| `cm_cmqreach_transit_frame` | 红 | **绿** | 我的,瞬态 |
| `cm_ult_reach_meter_domain` | 红 | **绿** | 我的,瞬态 |
| `aimguard_target_axis` | **绿** | **绿** | ⚠️ **两边都绿,归不到我头上** |

⚠️ **第 6 条 `aimguard_target_axis` 单独拎出来跑两个布局都是绿的** ⇒ 它在自检里的红
**既没被我的改动解释,也没被确立为 trunk 红**(可能是顺序/状态相关)。**只登记,不下结论**,
交给拥有它的组看一眼。⛔ 别把它读成「trunk 的 Lua 侧是红的」——
自检自己那行也写着 `Whether main is red too is NOT established by this line`。

(`trunk-red(python)` 与 `lua-coverage` 沿用批测台 03:11Z 的归属:
`test_pending_rulings.py` BORN-DONE 行数 7 > ceiling 6,指向总监 `c17e0ad8`;
`no_manifest_row` 那条是 GH #806 的欠条。**都不是本组的活,只登记。**)

## 十、铁律 6 三条腿(裸读,⛔ 未用 `RULE6_BYPASS`)

```
luacheck bots game: 0 warnings
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 86 ran, 0 findings, 0 uncertifiable, 13.9s
lua gate: 371 ran, 0 findings, 0 uncertifiable, 6 unanswered, 8 known-red, 607.0s
```
`PUSH_BRANCH_EXIT=0`。动态半(GH #124,~100min)**未跑、不声称**。

⛔ **本轮被闸拒绝过两次,两次都记下来**:
1. **第一次是我自己造成的假红** —— 我 `pkill` 掉多余的并发 `lua_gate.py` 时
   **连 push 钩子正在跑的那条腿一起杀了**,得 `gate exit 143`(SIGTERM),
   闸如实报 `could NOT RUN … that is not a pass`。**闸没错,是我手抖。**
   ⭐ 教训:push 钩子跑的时候**不要 `pkill -f` 任何和闸同名的进程**。
2. **第二次是真红,而且是闸替我抓到的**(见 §五.3 的 `slotdust` 递归 glob)。

## 十一、token

`TOKENS total_in=14,374,184 out=67,537 turns=94`(统计时刻为止;零 `requires approval`)。
