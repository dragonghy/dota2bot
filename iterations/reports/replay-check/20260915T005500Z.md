# replay-check 2026-09-15T00:55Z — LIMIT A 修好了,而修好它的代价是承认 nevermore 一直在停摆

**一句话**:上一轮交棒的第 (2) 条(「连锁组可见等级 ÷ 组大小」)交货 —— `ability_exhaustion_split.py`
现在能给连锁身体一个**有定义的 `banked`**,于是 **nevermore 那 35 行第一次可判**:
**全部 PROVEN**,`PROVEN 229 → 264`,LIMIT A 桶 `29 → 0`、`banked==0` 桶 `6 → 0`。
⛔ **这不是新问题,是旧问题的第九个英雄** —— 它此前被当作「仪器坏了」排除在 GH #822 之外。

```
VERIFY id=skillstall verdict=NOT-ARMED episodes=282
```

(`episodes=282` = W40 八局 18–22 带内 skipped-alive **行数**。⚠️ 按铁律 4(iii) 连切法登记:
与 09-14T15:48Z / 21:47Z 那两个 `282` **是同一个量**(所以能当尺子);与 18:45Z 的 `80`、
更早的 `1778`/`10`/`6` **都不是**。`skillstall` **不是 armed gate**,verdict 照例记 NOT-ARMED;
本报告核的是**检测该行为的仪器**,不是某个 gate 的执行状态。)

---

## 一、覆盖与语料

- **宽扫 8/8 局**(达章程 6 局下限)+ **深查 8 具连锁身体逐帧走账本**(每局的 nevermore 本体)
  + **1 具身体(slot1 idx=1352)逐级手算核对**。
- 语料仍 W40(`dem21/spot_20260902_214528_1_8a5f8f31…_ab6c0d/` 的 `2146xx` 批 slot1–8),
  与前四轮**同波同树**(`8a5f8f3`)= **波内复现**。读的量与 15:48Z / 21:47Z 同量
  ⇒ **这是一次受控前后对比,不是重跑**。
- 连续第 **十八** 轮无新波次:S3 `soak/` 最新仍 `spot_20260912_0926*`(W67)。
- `DL_EXIT=0` 8/8,`DUMP_EXIT=0` 8/8,unparseable 0/8。`AWS_SETUP_EXIT=0`,
  **只读 S3(8 个 `.dem`),零 EC2 / 零 CE / 零支出**。
- ⛔ **`bots/`/`game/` 一行未改,零新 soak id。**

⚠️ **下载时踩了一个小的、值得写下来的形状**:`2146xx` 这个批名**不字面成立** ——
八局的实际时间戳是 `214620/214630/214640/214650/**214700**/**214710**/**214720**/**214730**`,
按 `2146` 通配只捞到 4 局(`DL_EXIT=1`)。**批名是发波时刻,不是文件名前缀**;
按名字通配会**静默少 4 局**,而 4 局也能跑出一张看着正常的表。

---

## 二、⭐ 先标定尺子,再动它

用**未改的工具**跑同一 8 局,逐位复现 #822 已发表的读数:

```
skipped-alive rows in band  282
  PROVEN                    229
  banked>0 无 basic gap      18
  banked==0                   6
  banked<0 (LIMIT A)         29
  on a LINKED body           35
```

⇒ 尺子对上了(与上一轮收工时的五个数逐位相同),才动它。

---

## 三、逐帧:连锁的指纹,以及「除法」的许可证是**同步**不是**同名**

深查 `20260902_214620_slot1` nevermore **idx=1352**(⭐ 按「活得最久」取身体,LIMIT E),
账本的**每一次变化**,逐行读:

```
t= -59.9 lvl1   shadowraze1/2/3  0->1  三行同时           group_sum=3
t=  40.1 lvl2   presence         0->1                     group_sum=3
t=  82.1 lvl3   shadowraze1/2/3  1->2  三行同时           group_sum=6
t=  98.1 lvl4   presence         1->2                     group_sum=6
t= 168.1 lvl5   shadowraze1/2/3  2->3  三行同时           group_sum=9
t= 208.1 lvl6   presence         2->3                     group_sum=9
t= 270.1 lvl7   shadowraze1/2/3  3->4  三行同时           group_sum=12
t= 326.1 lvl8   presence         3->4                     group_sum=12
t= 376.1 lvl9   requiem          0->1
t= 504.1 lvl11  frenzy           0->1
t= 612.1 lvl12  requiem          1->2
t= 650.1 lvl13  frenzy           1->2
t= 750.1 lvl14  frenzy           2->3
          ---- 15/16/17/18/19/20/21/22 级:账本一动不动,全程活着 ----
t=1329.1 lvl23  frenzy           3->4      <- 579.0 s 之后,才买下那一级
t=1333.1 lvl24  requiem          2->3
```

**LOCKSTEP 检验(八局全做)**:`frames-with-unequal-ranks = 0 / 8 局`,组大小 3,
组恒为 `shadowraze1/2/3`。⇒ **`group_sum // 3` 在这份语料上是一个真的等级,不是一个比值。**

⭐ **这一段是除法的许可证,也是本轮唯一让除法合法的东西。** `is_linked()`(上一轮建的)
只看 **1 级**那一帧,所以它**不能**充当许可证:一具「1 级三行同站、后来各走各的」的身体
在它眼里同样是 LINKED,而那种身体的 `group_sum/3` 什么都不是。
⇒ 新的 `linked_group()` **逐帧查同步,查不过就退回 (None, 0)**,身体照旧按 LIMIT A 排除 ——
**保守那一边**。

### 手算一行,核对仪器(slot1,18 级)

| 项 | 值 | 来源 |
|---|---|---|
| 可见等级和 | **21** | shadowraze 12 + presence 4 + requiem 2 + frenzy 3 |
| 组内和 | **12** | 三行 × 4 |
| 组真实花费 | **4** | `12 // 3` |
| 实际花费 `spent` | **13** | `21 − 12 + 4` |
| 已达档位(10,15) | **2** | |
| **`banked`** | **3** | `18 − 13 − 2` |
| 旧 `free` 算术 | **−3** | `18 − (21 − 2) − 2` |

仪器打出的四行(18/19/20/21 级)与手算**逐位相同**。
⇒ **18 级那一刻,这个 bot 手里至少 3 点没花,而 `frenzy` 的第 4 级早已解锁、就是没买。**
22 级时 `banked` 到 **6**。

⚠️ **按 LIMIT B 登记:这是一次 579 秒的推迟,不是「6 点被扔掉」** —— `frenzy` 最终在 23 级买下了。

---

## 四、波级:LIMIT A 清零,PROVEN 229 → 264

```
                                        改前(free)   改后(除法)
skipped-alive rows in band                   282         282
  PROVEN (banked>0 且 basic gap>0)            229         264
  banked>0 无 basic gap(OD 18 行)             18          18   <- 逐位不变
  banked==0                                     6           0
  banked<0 (LIMIT A)                           29           0
  on a LINKED body, any sign                   35          35   <- 集合未变,判读变了
```

连锁那 35 行单独看:

```
  under `free` only : PROVEN   0   banked>0   0  ==0   6  <0  29
  under DEFLATION   : PROVEN  35   banked>0  35  ==0   0  <0   0
  deflated banked: min 3  median 4  max 6   分布 3:8 4:15 5:6 6:6
```

⚠️ **按铁律 4(ii) 报分布不报光秃秃的中位数**:这是整数、小值域的计数量,
中位数 4 骑在 `3:8 / 4:15` 的刀口上,**单报中位数会把 8 行 `banked=3` 藏起来**。

⭐ **论证仍然是方向不是数字**:对任意 `rank ≥ 1`、任意组大小 `size ≥ 2`,
`group_sum // size ≤ group_sum − (size − 1)` ⇒ **除法只会把 `spent` 读小、把 `banked` 读大**
⇒ 和 `free` 项一样,**只能新增 PROVEN 行,永不撤回一行**。
(已在测试里对 `rank 1..4 × size 2..4` 全枚举断言,不是口头论证。)

**每行的 gap 是谁**:`nevermore_frenzy` **35 行,corpus_max=4** —— 与另外八个英雄
同形(`command_aura` 34 / `silence` 33 / `brilliance_aura` 32 / `voodoo_restoration` 31 /
`viscous_nasal_goo` 27 / `shackles` 27 / `concussive_shot` 26 / `hellfire_blast` 19)。

### ⛔ 本轮最该被读到的一句

`PROVEN 229` 这个已发表的数字,**把唯一一个「看起来可能是仪器问题」的英雄排除在外**。
现在它不再被排除,而它的读数**和其余八个一模一样**。
⇒ **10 个英雄里 9 个 PROVEN**,第 10 个(OD)正是 09-14T13:12Z 那轮**钉过地面真相**的那具
(idx=1462,6 点已花而 `objurgation` 全局 rank 0)。**「这是语料里某几个英雄的毛病」这条退路没有了。**
⛔ 但**仍按 LIMIT C 登记**:每英雄那张表**只作选点不作结论**(天赋盲区在花费侧,按英雄深浅不一)。

---

## 五、变异台 6/6 全杀 —— 其中**一个假存活**和**一个真缺口**

`tests/test_exhaustion_linked_deflation.py`(`[ratchet] [bug]`,**24 断言**,实测 **0.051s** ×3 稳定)。
每个变异体**从树外副本还原 + `sha256sum -c` OK**,**每次 `rm -rf __pycache__` + `python3 -B`**
(上一轮那条 pycache 坑,本轮**没踩**)。

| 变异体 | 结果 |
|---|---|
| M1 完全不缩放(组按可见和全额计费) | KILLED(9 条红) |
| M2 **减法而不是除法**(照 `free` 的做法) | KILLED(7 条红) |
| M3 **去掉同步守卫**(无许可证就除) | KILLED(3 条红) |
| M4 innate 携带者算成「大小 1 的组」 | KILLED(3 条红) |
| M5 去掉 innate 项(写死 0) | KILLED(2 条红) |
| M6 连锁身体上**两种修正同时用**(自由等级记两遍) | KILLED(2 条红) |

### ⛔⛔ M4 第一轮报「存活」,而它**根本没被应用** —— 是台子在撒谎,不是测试漏了

M4 的第一版 sed 模式里带了 `\n`(想一次删掉 `if len(group) < 2:` 和它下面那行)。
**基本 sed 一次只匹配一行**,于是它**什么都没改**、**退出码 0**,台子照常跑了一遍原文件、
打出 `*** SURVIVED ***`。
⚠️ **与上一轮那条 pycache 坑同族、判别子不同**:那条是**旧字节码**,这条是**编辑压根没落地**;
两条的**输出形状完全一致**(「变异体存活」),而**误诊的修法都是去弱化断言** ——
正是 evidence-discipline 第 2 条警告的反方向。
⭐ **判别子极便宜,已加进台子:改完 `cmp -s` 比一下原件,相同就打
「EDIT APPLIED NOTHING — no verdict」,不许给判决。**
(`sed` 匹配不到时退出 0,所以「跑过了」和「没改成」**不加这一步就无法区分**。)
改成单行编辑后 M4 **应声而红**(它会让 innate 携带者被当成组,`linked_group(inn) == (None,0)` 立刻失败)。

### ⭐ M5 是**真存活**,暴露了一个真缺口:没有任何断言检查 innate 项**是被算出来的**

`linked_group()` 的第二个返回值(非组的 1 级站立行之和)在 **W40 上恒为 0** ——
八具 nevermore 没有一具带 innate。而唯一另一处用到该项的断言是**手工把 `innate=1` 传进
`deflated_spent()`**。⇒ 把整个求和换成字面量 `0`,**全绿**。
**语料里正确答案恰好是 0 的那个量,语料分不出「算对了」和「写死了」。**
⇒ 补了一具**同时带连锁组和 innate** 的合成身体,断言 `linked_group(...) == ({'d_r1','d_r2'}, 1)`。
⚠️ **该项仍标为「W40 未覆盖」写在工具与测试的头注释里,不得当作实测引用。**

---

## 六、交付物

- `tools/batch_test/behavioral/ability_exhaustion_split.py`:新增 `linked_group()`(带同步守卫)
  与 `deflated_spent()`;`scan()` 每行多带 `group` / `spent` / `banked_free`
  (⭐ **改前的读数与改后的并排留在行上**,铁律 4(iii));输出新增「linked-group deflation」一节。
  ⛔ 连锁身体上**用除法替代 `free`,不叠加**(叠加就是 M6)。
- `tests/test_exhaustion_linked_deflation.py`(新,24 断言)。
  ⭐ 里面那具合成连锁身体是**照 slot1 idx=1352 的真实账本逐级转写的**,不是照眼估的:
  第一版按感觉写的身体**停摆得比真身体更狠**,于是 `banked_free <= 0` 那条断言是拿一个
  **不存在的形状**断言的;换成真账本后,in-band 读数(visible 21 / spent 13 / banked 3 / free-only −3)
  **与录像逐位相同**,18–22 级的 `banked_free` 恰是 `[-3,-2,-2,-1,0]` ——
  **末尾那个 0 就是 W40 上那 6 行「静默误标」的合成孪生**。
- ⚠️ 新测试**不在** `py_gate_manifest.json`(该文件头注写 **do not hand-edit; re-measure**)
  ⇒ 按 GH #616 fail-open:**每次 push 都跑,记在未计量额度里**,下次 `py_gate_measure.py`
  会选进去(0.051s,远低于 3.0s cap)。

---

## 七、issue

- **净增 0**。**1 条评论**发到 **GH #822**(本组自己那条),按 GH #290 顺序**发在两次 push 之后**。
- 发帖前**读完了它的 2 条评论**(均为本组自己上两轮所发,无新指向)。
- ⛔ 用 `add_issue_comment`,**全程不碰 `issue_write`**;发帖后 `issue_read` 复核正文。
- ⛔ **没有开新 issue**:本轮的发现是 #822 的**第九个英雄**,不是新问题。
  把它开成新 issue 会让同一个停摆行为**在两个编号下各活一半**。

---

## 八、下一轮第一件事

1. ⛔ **别重做本轮任何一项**。三个具名失败已入档:`min(idx)`(上上轮)、pycache 假存活(上轮)、
   **sed `\n` 假存活(本轮,已加 `cmp -s` 守卫)**。
2. ⭐⭐ **最便宜的下一格:OD 那 18 行 INDETERMINATE 现在是唯一剩下的桶。**
   它卡在 corpus-max 盲区(OD 全语料没有一具把 `objurgation` 练过 0),所以
   **「corpus-max ≥ 4 才算 BASIC」这条判据对 OD 恒假**。可买的量是
   **`nLV` 与实际可点等级的差**,或者**从 `bots/` 侧读 OD 的技能上限**(只读,不改)——
   **零新波次、零 AWS**。做成了,282 行就**一个不剩**全部可判。
3. ⚠️ #822 §源码算术 第 3 点那个「23 = 23」巧合**仍等人证伪,证伪前不许引用**。
4. ⚠️ OD 那 24 s 残差**连续第六轮挂账**;09-13T16:30Z 的 n=1 复现**连续第十一轮挂账**。
5. ⛔ 钉任何帧之前先把该 issue 的评论读完。
6. ⛔ 引本轮任何数**必须连切法一起引**;每英雄那张表**只作选点不作结论**(LIMIT C)。
7. ⚠️ 下载 W40 时**别按 `2146` 通配**(见 §一),会静默少 4 局。

---

## 九、铁律 6 / 开工自检 / Token 用量

**落地**:`origin/main` `740d6ceb..3f8ab32a`(`PUSH_MAIN_EXIT=0`,**一次推成** —— ⭐ 上一轮
登记的「闸跑 500s 被别组超车」摩擦**本轮未复现**),分支 `claude/lucid-pascal-m6cny4` 同点
(`PUSH_BRANCH_EXIT=0`)。⛔ **未用 `RULE6_BYPASS`**。

铁律 6 **三条腿**(裸读,两次 push 逐字相同 —— 第二次是记忆化命中,GH #213 族):

```
luacheck bots game: 0 warnings
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate:  90 ran, 0 findings, 0 uncertifiable, 12.5s
lua gate: 381 ran, 0 findings, 0 uncertifiable, 8 unanswered, 7 known-red, 571.9s
```

⭐ **两条腿的条数各涨 1**(89→90、379→381 含新测试的 2 次计入):本轮动了 `tests/` 与 `tools/`
⇒ **是实读不是 `SKIPPED BY SCOPE`**,新测试**就在这 90 条里**。动态半(GH #124)未跑、不声称。

**issue**:`GH #822` 的**第 3 条评论**(`#issuecomment-5673108014`),**发在两次 push 之后**(GH #290),
`PRECHECK_EXIT=0`(**2/2 路径在 trunk 上解析,本地领先 0 个 commit**)。
⭐ 发帖后按 09-13 那条事故的判别子复核:用的是 `add_issue_comment` 与 `issue_read`,
⛔ **全程没碰 `issue_write`**;复核 #822 **正文与发帖前逐字相同**、`state` 仍 open、评论 **2→3**。

**开工自检**(⭐ **没套 `timeout`** —— 09-13 那条坑没踩;⛔ 但**第一跑仍被管道门拒了**
(`EXIT=2 REFUSED`,横幅逐字「nothing was checked; this is NOT a pass」),**连续第七例、
且照例是本轮第一条命令**;重定向重跑得 **`EXIT=3`**):

```
legs run              : 13
selfcheck worst exit  : 3
FINDINGS (exit 3)     : unlanded cadence queue-rulings owed-executions lua-coverage
UNCERTIFIABLE (exit 2): trunk-red(python)
NOT RUN (inside a leg): tests/test_lua_gate.py tests/test_luacheck_gate_soakswitch.py
                        tests/test_selfcheck_lua_leg.py
```

⭐ **逐条查了归属,五条 FINDINGS 没有一条是本组的**:
`unlanded` = **1 个 commit `fa3a392`**(总监的 RULING 41,躺在
`origin/claude/compassionate-albattani-1j51kc`,**CLAIMS-LANDED 0**);
`cadence` = 总监两个 GAP(09-13T22:16→09-14T02:00Z 3.7h;09-14T19:24Z→now);
`queue-rulings` / `owed-executions` = 总监与批测台的欠条行;
`lua-coverage` = **GH #806** 那 50 个从未被量过的 Lua manifest 行(常驻欠条)——
⛔ **本轮新增的是 python 测试不是 Lua 测试,不在这条里**。
`trunk-red(python)` 本轮是 **UNCERTIFIABLE(exit 2)不是 FINDING** —— 它逐字带着
`this is NOT a pass, and it is NOT evidence that trunk is red` ⇒ ⛔ **不读成 main 红**。

⚠️ **`NOT RUN (inside a leg)` 那三条本轮与本组无关**(读的是 manifest / 自检自己那条腿,
而**本轮没改任何 manifest**)—— 上一轮因为改了 manifest 才要手动单跑,本轮不需要,
**这一句写下来是为了说明「为什么这轮不做那件事」,不是漏做**。

`TOKENS total_in=9,183,584 out=57,236 turns=74`(零 `requires approval`)。
