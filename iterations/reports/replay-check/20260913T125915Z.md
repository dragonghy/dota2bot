# 录像检查组 2026-09-13T12:59Z — `ownhalf` 补课:判别子移植落地,`closed` 在本band上被逐帧拆开

**一句话**:上一轮欠下的第 (3) 件事(把 `punish`/`rundown` 判别子移植给 `ownhalf`)**本轮做完了**,
代价是 **`closed` 这个量在 own-half 带上当场作废** —— 81 局实测:armed 腿 3,795 个 `closed` episode 里
**只有 26.7% 是真正的「进去打了」**,**40.2% 的推进是对手供给的**,**12.7% 里 bot 自己在往后退**;
而且**两腿的分解比例不同**(bot-driven armed 56.2% vs baseline 51.1%)⇒ **这个混杂不在 armed−baseline 里抵消**。
新量 `punish` 的 DiD 是 `ownhalf` 迄今**第一个两层同号且越过自己 placebo 的读数**(ab **+6.3** / ba **+1.2**,
placebo ab +1.5 / ba −0.4),**但 placebo 自己的跨run离散度与效应同量级(单run最大 +9.9)**,
⇒ 结论仍是 **INDETERMINATE**,堵点从「没有量」换成「量有了,精度不够,而加局被刹车挡着」。

```
VERIFY id=ownhalf verdict=INDETERMINATE episodes=7088
```
(7088 = W67 全 81 局 **armed 腿 own-half 带**的 episode 数。⚠️ 与此前三轮登记的 `episodes=0`
**不是同一个量**:那三轮是「专用仪器尚未存在」的 0,不是「域为空」——域不空早在 09-10 就测过。
按铁律 4(iii),切法与量一起登记。)

---

## 一、覆盖(章程固定节)

**宽扫 81/81 局(W67 四台全收,逐字引各 `sweep_complete.json`)**:

| run | dem_found | swept | skipped | unparseable | exit_code |
|---|---|---|---|---|---|
| `…_65af1b` | 25 | 19 | 6 | 0 | 0 |
| `…_2a2de3` | 31 | 26 | 5 | 0 | 0 |
| `…_d2a2fd` | 24 | 18 | 6 | 0 | 0 |
| `…_71f624` | 24 | 18 | 6 | 0 | 0 |
| **合计** | **104** | **81** | **23** | **0** | **0** |

⭐ **免费的复现检查**:这张表与上一轮报告 §一**逐格相同** ⇒ 语料与 dumper 未漂
(GH #666 的那种跨轮对不上,在本轮的宽扫计数上没有出现)。

**深查逐帧 6 局**(下限 6 达标),**证人按确定性规则挑,不按极值挑**:
先按 run 名排序取两台(`d2a2fd` / `71f624`),每台按局名排序取前 3 局,每局取
**armed 腿 own-half 带第一条 `closed` episode**,外加(若存在)该局**第一条 `bot_adv<0`** 的。
⚠️ **为什么又是 W67**:连续第八轮无新波次(批测台第九轮持有刹车,GH #779);**跨波不并池**。

---

## 二、先逐帧:六条帧证人(判别子在真实帧上对不对)

命令(可复跑,只读):
```
python3 tools/batch_test/behavioral/ownhalf_domain.py \
  /tmp/sw_<run>/timelines/<game>.timeline.json --side radiant --trace <bot>:<enemy>:<t0>
```

| # | 局(armed=radiant) | 对 | t0 | d0→dmin | bot_adv | enemy_adv | 判别子 | 逐帧读到的事 |
|---|---|---|---|---|---|---|---|---|
| ① | `d2a2fd/…212717_slot8` | centaur vs pudge | 84.8 | 1419→233 | **+682** | +583 | **punish** | 88.8 帧 `hit=>`,pudge `hp_e` 1.00→0.87;bot 自己走进去并先落伤害 ⇒ **标对了** |
| ② | `d2a2fd/…214152_slot2` | skywrath vs PA | 54.5 | 604→156 | **−182** | +634 | **rundown** | 55.5/56.5 帧 `hit=<` 单向,`hp_b` 0.93→0.70;**bot 在后退挨打,而 `closed` 记它一次合围** |
| ③ | `d2a2fd/…212707_slot7` | skywrath vs PA | 59.6 | 1568→754 | +432 | +460 | 两者皆否 | 窗内(59.6–63.6)**零伤害事件**;第一次 `>` 在 **64.6 = t0+5.0**(见 §四) |
| ④ | `71f624/…212708_slot7` | viper vs pudge | 87.6 | 991→716 | **+375** | −41 | **punish** | 91.6 起连续 `>`,pudge `hp_e` 0.79→0.61;对手在退,bot 在追 ⇒ **标对了** |
| ⑤ | `71f624/…212718_slot8` | jakiro vs CK | 54.7 | 1310→534 | **−214** | +991 | 两者皆否 | 窗内零伤害;CK 推进 991u,jakiro 退 214u。第一次 `>` 在 **59.7 = t0+5.0** |
| ⑥ | `71f624/…214111_slot5` | jakiro vs CK | 50.5 | 719→194 | **−179** | +723 | 两者皆否 | **互伤**(54.5 起 `><`),jakiro `hp_b` 0.78→**0.04**;`rundown` 因 `hit_enemy` 为真而不成立 —— **这正是设计:互殴不冒充「被单方面追死」** |

⭐ **承重的不是「① 读成 punish」**,是 **②⑤⑥ 三条 `closed` 为真的帧上,bot 的自身位移是负的**
—— 同一个旧量把它们和 ① 记成同一件事。上一轮在姊妹仪器上正是这处把两条帧读反的。
⭐ **③⑤ 还暴露了窗口刀口**:两条都是 **t0+5.0** 落的第一刀,`punish`(4s)一律拒 ⇒ 见 §四。

---

## 三、再聚合:`closed` 的分解(81 局,两腿分开)

| 腿 | episodes | closed | 其中 bot-driven | 其中 enemy-driven | 其中 bot_adv<0 | 其中 punish |
|---|---|---|---|---|---|---|
| armed | 7,088 | 3,795 | 2,133 (**56.2%**) | 1,526 (**40.2%**) | 481 (12.7%) | 1,014 (**26.7%**) |
| baseline | 7,397 | 3,726 | 1,903 (**51.1%**) | 1,655 (**44.4%**) | 511 (13.7%) | 784 (21.0%) |

⛔ **两腿的分解不同(56.2% vs 51.1%)是结论的关键**:若两腿分解一致,混杂会在 armed−baseline 里
抵消,`closed` 还能当读数用;**实测不一致** ⇒ **凡引用过 own-half 带 `closed` 的读数(含上一轮报告里
`ownhalf` 的 `closed` 行)都不该再被当作「合围率」引用。** 四台逐台读数同向(见 `/tmp/oh_*.txt` 的
`WHAT closed IS MADE OF` 节,已写进仪器本身,下一轮不必重算)。

**DiD(ownhalf − nearmiss),每台一行,跨台取算术平均,⛔ 不按局数加权(铁律 4(i-d))**:

| run | ab punish | ba punish | ab closed | ba closed |
|---|---|---|---|---|
| 65af1b | +4.8 | +3.9 | +9.7 | +5.1 |
| 2a2de3 | +4.1 | −0.7 | +5.4 | −5.3 |
| d2a2fd | +3.5 | +2.1 | +15.0 | +2.7 |
| 71f624 | +12.9 | −0.6 | +5.8 | +1.1 |
| **均值** | **+6.3** | **+1.2** | +9.0 | +0.9 |
| **placebo 均值**(shipped−nearmiss,杠杆已知缺席) | **+1.5** | **−0.4** | +2.4 | +0.9 |

- **`punish`**:两层**同号为正**,且两层都越过自己的 placebo ⇒ 这是 `ownhalf` 第一个非空签名。
- **`closed`**:ba 层 **+0.9 恰好等于自己的 placebo +0.9** ⇒ **不是读数**(这与 §三 的分解互为佐证)。
- **`rundown`**:ab −0.4 / ba +0.9,**两层反号 ⇒ 按 4(i-b) 当噪声,不进结论**(计数类、侧偏未消除)。
- ⚠️ **别把 +6.3 当成定案**:placebo 自己的**跨台离散度与效应同量级**
  (ab 逐台 −2.3 / −0.3 / −1.5 / **+9.9**;ba +5.5 / −3.2 / +1.2 / −5.3)——
  **一台的读数在这把尺子上不可信,四台的均值才是能引的最小单位**,而 4 台就是 W67 的全部。

---

## 四、窗口敏感性(已做成常驻读数,不是一次性数字)

六条证人里 **两条(③⑤)的第一刀恰好落在 t0+5.0s**,`punish` 的 4s 窗一律拒。
其规模已实测并写进仪器:armed 腿 closed+bot_driven 的 episode 里 **18.6%** 的第一刀落在 **(4s, 8s]**。
⇒ 新增 `punish_wide`(8s)**只作敏感性**,`punish`(4s)仍是登记量。

**结论对窗口稳健**:`punish_wide` 的 DiD 均值 **ab +8.7 / ba +2.3**(placebo ab +2.0 / ba −2.0),
与 `punish` 同号同量级 ⇒ **那 18.6% 没有改变方向**,只让效应略大。
⛔ **登记一句**:两列同时打出来是为了让刀口可见(4(ii)),**不是给读者挑大的那个**。

---

## 五、本轮改了什么(只动量具与测试,零 bot 行为改动)

- `tools/batch_test/behavioral/ownhalf_domain.py`:移植 `attribute()` / `dealt()` / `died_in()`,
  新增 episode 字段 `bot_adv`/`enemy_adv`/`bot_driven`/`enemy_driven`/`hit_enemy`/`hit_by_enemy`/
  `bot_died`/`enemy_died`/`punish`/`rundown`/`punish_wide`;报告表加 `punish`/`rundown` 两列、
  加「`closed` 由什么构成」与「窗口敏感性」两节;**trace 加 `hp_b`/`hp_e`/`hit` 三列**
  (上一轮头号更正的直接产物:**没被打出来的列,就是被读者脑补的列**)。
  ⚠️ `punish`/`rundown` 追加在 DiD 行**末尾**,`ownhalf_across_runs.py:32` 的前缀正则**照常命中**
  (已在源码处写下「可追加不可重排」的约束)。
- `tests/test_ownhalf_attribution.py`(新):**21/21**。A 反打 / **B 被追死(负控制)** / C 站桩 /
  **D 走过去一次没打中 ⇒ `bot_driven` 真而 `punish` 假** / **E 这些控制走的是候选门不是出厂门** /
  **F t0+5s 的刀:`punish` 拒而 `punish_wide` 收**。
  ⭐ 承重断言仍是**「`closed` 在 A 与 B 上都为真而判别子把它们分开」**。
- **邻近测试复跑**:`tests/test_ownhalf_placebo.py` exit=0、`tests/test_domain_rows_strata.py` 6/0、
  `tests/test_detector_source_constants.py` exit=0、`tests/test_overchase_attribution.py` exit=0。
- ⛔ **`bots/` 与 `game/` 一行未改**;零新 soak id;armed 串 / `queue.json` / `test_set.md` 未动。

---

## 六、issue

**净增 1 条:GH #800**([bug] 给总监,正文含 §二 六条帧证人 + §三 分解表 + 复现命令),**零追评**。
发帖后立刻 `issue_read` 复核:标题 / 正文**逐字未变**,`state` open,`created_at 2026-09-13T13:10:54Z`。
⛔ **全程未碰 `issue_write(method='update')`**(工具坑第二条:那个改的是正文本身;
本轮发帖用 `issue_write(create)`,发完立刻 `issue_read` 复核)。
**未开的两条已登记理由**:
- 窗口刀口 **不另开** —— 本轮已在仪器里做成常驻两列并钉进 F 控制,没有需要别组决定的事;
- §二⑥ 那条「jakiro 从 0.78 掉到 0.04 全程没脱离」看着像行为问题,但**一条帧不够立案**,
  也可能落在已出厂的 `deathzone`/`vsafe` 域里 ⇒ **不开**,写进下一轮 backlog(先量频率再立案)。

---

## 七、AWS

**只读 S3**(4 个 run 前缀,104 个 `.dem` + `.analysis.json`),**零 EC2 / 零 Cost Explorer / 零支出**。
`AWS_SETUP_EXIT=0`(`arn:…:user/dota2bot-agent`)、`DUMPER_EXIT=0`(**cache HIT**,未重建)、
四次 `sweep_run.sh` 各 `exit_code=0`、`unparseable 0/104`。

---

## 八、开工自检

⚠️ **管道门第 27 次,又是本轮第一条命令**(脚本自己拦下:`REFUSED: routine_selfcheck.sh stdout is a
pipe; exit 2, nothing checked.` / `this is NOT a pass`)。第二跑重定向取真码:**`EXIT=3`**。

```
legs run              : 12
FINDINGS (exit 3)     : cadence queue-rulings owed-executions trunk-red(python)
UNCERTIFIABLE (exit 2): none
NOT RUN (inside a leg): tests/test_lua_gate.py tests/test_luacheck_gate_soakswitch.py tests/test_selfcheck_lua_leg.py
```

- `trunk-red(python)` 逐字是 **`FAIL tests/test_py_gate_hook.py`**(`py_gate_hook: 10 checks, 6 failed`;
  `125 passed, 1 failed, 3 uncertifiable`)。
- ⚠️ **同一棵树上单跑该文件是绿的:`10 checks, 0 failed`, exit=0** ⇒ 这条红**只在自检腿的上下文里出现**。
  失败文本里那一行逐字说钩子看到的是 **`a clean working tree`**,即那条腿自己的树态与断言预期不同。
  **不新开 issue**:同一文件上一轮已由 GH #795 点名;本轮把「腿内红 / 单跑绿」这个形状补登记。
- ⚠️ **本轮自己制造了一处并发**:自检在后台跑时,前台同时挂着 2 条 sweep(满载 CPU)。
  ⛔ 章程已有同族教训(「别在手跑测试的同时后台挂自检」)。**下一轮:自检跑完再起 sweep。**
- 腿内三条 `NOT RUN` ⛔ **不是通过**。

---

## 九、铁律 6 三条腿(push 之后回填)

两次 push,钩子自打的权威读数**逐字相同**:

```
luacheck bots game: 0 warnings
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 100 ran, 0 findings, 0 uncertifiable, 31.1s
lua gate: 363 ran, 0 findings, 0 uncertifiable, 8 known-red, 400.2s
```

- ⛔ **未用 `RULE6_BYPASS`** —— 无任何「SKIPPED, not passed」行;`ARM_EXIT=0`
  (`core.hooksPath=.githooks, already set`)。三条腿都**真跑了**,无 `SKIPPED BY SCOPE`。
- ⭐ **py gate 99 → 100 ran**,多出来的正是本轮新增的 `tests/test_ownhalf_attribution.py`
  (manifest 外的新测试**照跑**);lua gate **363 ran**,**8 条 known-red**
  (上一轮 9 条,少的那条是别组清的,**不是本轮动的**)。
- ⚠️ 本轮**没有**新增 known-red、**没有**动赦免名单、**也没有**往
  `tools/agent/lua_gate_manifest.json` 登记新测试 ⇒ **声明:本轮新测试不受钩子保护。**
- `PUSH_BRANCH_EXIT=0`、**`PUSH_MAIN_EXIT=0`**(`fdf5461c..0a094627 HEAD -> main`,
  无 non-ff,**未 rebase**)。动态半(GH #124)未跑、不声称。
- ⭐ **顺序(GH #290)本轮是对的**:issue **发在两次 push 之后**,发帖前
  `claim_precheck.sh` **`PRECHECK_EXIT=0`**(逐字 `paths cited 7 … resolved on trunk 6 refused 0` /
  `clean` / `OK to publish: every citation resolves on origin/main.`)。

---

## 十、下一轮第一件事

1. ⛔ **别重跑**:81 局宽扫、六条帧证人、§三 分解表、四台 DiD/placebo —— 都在本报告里,
   且「`closed` 由什么构成」与「窗口敏感性」两节**已长在仪器里**,下轮跑一次就有。
2. ⭐⭐ **`ownhalf` 的 (a) 现在缺的是精度不是量具**:4 台是 W67 的全部,而 placebo 的跨台离散度
   与效应同量级 ⇒ **在新波次到来前,再往 `ownhalf` 的 (a) 上花语料是「加局无救」那一格的第四例**
   (前三:`creepthink` / `campvoid` / `overchase`)。等刹车解除或等一条**专门 arm `ownhalf`** 的波。
3. ⭐ **判别子还有第三个可移植的落点**:凡是「域内计数 + 距离缩小当后果」的仪器都该带这两半;
   本轮普查过 `tools/batch_test/behavioral/*.py`,**只有这两把用 `d0−dmin` 语义的 `closed`**
   (`towerfear_domain.py` 的 `closed` 是 episode 终止原因,**同名不同物,别误移植**)。
4. ⚠️ **别引本轮任何单台读数**(见 §三 最后一条):能引的最小单位是四台均值。

---

## 十一、Token 用量

```
TOKENS total_in=11,115,996 out=64,178 turns=78
```
(统计时刻在收尾回填之前,其后回合不计入;量级参考。⭐ 本轮真烧了语料:104 个 `.dem` 下载 +
81 局宽扫,**四次 sweep 两两并行**约 30 分钟墙钟 —— **不是空转**,且比上一轮串行四次省了约一半墙钟。
四次等待全走后台,**零 `requires approval`**(铁律 11)。)
