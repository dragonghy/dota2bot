# 录像检查组 2026-09-07T15:53Z —— W52 首检:两笔条件 (a) 一买一空,而当轮最贵的一行是「宽扫表的头条读数有 78% 是侧偏」

**一句话**:W52(第一次被本组检的波)47 局两粒种子上,`zusult`+`zusboltdom`
**买到条件 (a)(WORKING,帧证据齐)**;`campbind` **买不到,而且不是没触发,是它的
可判读面在这套语料里恒空(INDETERMINATE)**;顺带量到宽扫表头条
`died_with_ult_ready`「armed 160 vs baseline 85」按 ab/ba 拆开后
**arm=+0.814、side=+2.957** —— 看着像 1.9 倍回归的那个数,**四分之三是 Radiant 偏置**。

- 零 EC2、零 CE、零 AWS 花费(**只读 S3**:两个 run 的 `soak/` + `dem21/`)。
- **未改 `bots/` 一行,未改 `tests/` 一行**。本轮只写 `iterations/` 下的报告与章程。

---

## 一、吞吐(章程固定节)

```
宽扫 47/47 局(W52 的 2/4 个 run 全量;另 1 局 dumper 读不动,单独登记)
深查 7 局(章程下限 6;上两轮分别是 1 局与 0 局,本轮按章程「下一轮回到 6 局」补回)
```

| run | 种子 | stamped 局 | 已扫 | warmup 跳过 | unparseable |
|---|---|---|---|---|---|
| `..._212ea5` | 6743 | 24 | 24 | 6 | 1(`20260907_004858_slot3`)|
| `..._7e6283` | 6858 | 23 | 23 | 6 | 0 |

未扫:`..._af2874`(6877)、`..._0e6ccf`(6907)—— 下一轮补,以便把 arm 读数从 2 粒
种子抬到 4 粒(现在 `sd` 只有 2 个自由度,见第四节)。

**深查 7 局**(逐帧,全部用 `pullcamp_frames.py` / 直读 timeline 事件流):
`20260907_003649_slot4`、`20260907_003641_slot2`、`20260907_010053_slot5`、
`20260907_010037_slot2`、`20260907_002328_slot7`、`20260907_004833_slot2`、
`20260907_010129_slot4`。

---

## 二、VERIFY 行(机器可读)

```
VERIFY id=zusult      verdict=WORKING       episodes=11
VERIFY id=zusboltdom  verdict=WORKING       episodes=6
VERIFY id=campbind    verdict=INDETERMINATE episodes=0
```

`episodes` 的口径逐条写在下面各节;`campbind` 的 0 **不是 SILENT**,理由见第三节。

---

## 三、`campbind`:INDETERMINATE —— 可判读面恒空,不是没触发

### 3.1 读数(`campbind_poke.py`,47 局 2 粒种子,`--selfcheck` 4/4 PASS)

单向证明的定义(工具自带,不是本轮发明的):**出厂代码只能戳最近的那个营地**,
所以「armed 腿上一次仇恨落在**非最近**营地」是出厂分支造不出来的东西。反向不对称,
落在最近营地的一戳记 UNDECIDED。

```
=== IN-WINDOW (60 <= t <= 360):唯一能跑拉营分支的窗口 ===
leg        pokes  ambig  non_nearest   nearest  unattributed
armed         52      9            0         4             5
baseline      14      4            0         3             1
```

**两条腿的 in-window `non_nearest` 都是 0。** 全语料 13 条 `non_nearest`
(armed 6 / baseline 7,**量级相同 ⇒ 那就是工具自己的假阳率**)全部落在窗口外
t=680.9 ~ 1530.6。

### 3.2 逐帧核验两条 armed 的 `non_nearest`(证明它们不在分支域内)

- **`20260907_002328_slot7` zuus t=798.0**(13:18,armed,pos4):
  t=798–804 **整整七秒站在原地**打食人魔营,`hp=1.00→0.92`,
  `d(wave)=3327` 且 `foll->wave` 从 3738 起**在变大**(离兵线更远)。
  营地朝他移动是**他在打野、野怪追他**,不是拉营。**t=798 ≫ T_HI=360。**
- **`20260907_004833_slot2` silencer t=1435.8**(23:56,armed,pos5):
  `hp=1.00` 全程,用 `silencer_glaives_of_wisdom` 连杀 `giant_wolf`/`alpha_wolf`,
  `d(wave)=2873`。同上,**打野**。

⇒ 这两条正是工具 docstring 里「WHY THE WINDOW MATTERS」那条教训在**第二份语料上的复现**
(W46 上是 t=1191..1481 的五条)。**登记为工具假阳,不进任何结论。**

### 3.3 逐帧核验四条 in-window 的模糊戳(拉营族真的在跑,但那不是 `campbind` 的读数)

- **`20260907_003649_slot4` t=132.5,armed,silencer pos5** —— 一次**教科书拉营**:
  t=124.5 从 (3498,−2605) 走向 kobold 营 (3963,−5070),t=131.5 到位(`d(camp)=338`),
  t=132.5 `DAMAGE silencer->kobold [dota_unknown]`(右键,**一戳**),
  随后 t=133.5–138.5 野怪反打(`neut≤700u` = 5),他向自家塔走,
  **`d(wave)` 2735 → 996**。⚠️ 但他 t=127.5 时距第二个营 (4592,−3719) 只有约 760u
  **没有戳**,而戳的是最近的那个 ⇒ 按工具规则 **UNDECIDED**(armed 与出厂都能产生)。
- **`20260907_003641_slot2` t=278.5,armed,silencer pos5** —— 同形状,
  戳 fel_beast/ghost 营,`hp 1.00→0.89`,营跟着走,但 `d(wave)` 只从 1619 收到 1802/2081,
  **拉营质量一般**;t=285.5 原地喝净化药水。
- **`20260907_010053_slot5` t=322.5,baseline,silencer pos5** ——
  **出厂腿上同样在拉营**:戳 warpine 营,`neut≤700u` = 8,`d(wave)` 2335 → 178,
  代价是 `hp 0.60→0.45`(gnoll 毒)。⇒ **出厂腿不是"不拉营"**,只是频次低得多(见 3.4)。
- **`20260907_010037_slot2` t=353.4,baseline,silencer pos5** ——
  **`hp=0.25` 时**先喝小蓝瓶(flask),`hp=0.39` 走进 warpine 营开打并挨打,
  t=357 起 1800 环内已有敌方英雄;t=360.4 交 `global_silence` 才脱身。
  ⚠️ **出厂腿的低血打野**,`pullcamp` 在这条腿上是关的,所以**没有 armed id 可记账**;
  登记为观察,不开 issue(本轮没有把它做成检测器的预算)。

### 3.4 拉营**族**(不是 `campbind`)的条件 (a):买到了,而且四格同号

按章程 4a 的归属纪律,下面这张表是 `pullcamp`+`pullcad`+`pulldrag`+`pullthink`
**四者合力**的读数,**不许记到任何单个 id 头上**:

```
=== IN-WINDOW 戳营普查,per 种子 x 分层 x 腿(铁律 4(i-a)) ===
seed   str  leg         pokes   ambig  atmark_hp>=.5  non_nearest
6743   ab   armed          16       2             12            0
6743   ab   baseline        3       1              0            0
6743   ba   armed          10       4              8            0
6743   ba   baseline        5       2              0            0
6858   ab   armed          20       1             18            0
6858   ab   baseline        6       1              3            0
6858   ba   armed           6       2              6            0
6858   ba   baseline        0       0              0            0
```

**四格全部 armed > baseline,无反号**;窄化到「窗口内 + 行军刻度 + hp≥0.50」后
armed 12/8/18/6 vs baseline 0/0/3/0。⇒ **拉营族 WORKING**(族级,非 id 级)。

### 3.5 结论与交棒

`campbind` 只能在「≥2 个活营同时在 1400u 内、且计划营 ≠ 最近营」的帧上咬到东西。
本语料 **47 局里符合前半条(窗口内 + 模糊)的戳一共 13 次(armed 9 / baseline 4),
分布在 9 局**;后半条(计划营 ≠ 最近营)**一次都没出现**。
⇒ **这不是接线问题,是频次问题**:按 ~0.19 次/局的可判读面,
一波 200 局的例行波期望产出的**可判定**帧数仍是个位数,而其中要求「计划≠最近」
再砍一个数量级。**继续用例行波买 `campbind` 的 (a) 是买不到的**。
本轮开 issue 请总监裁:改用 fixture 钉帧(`make_fixture.py` 冻结一帧
`bot.roamCampPull ≠ 最近营` 的真实帧,直接在 Lua 侧断言 `J.GetCampPullPokeTarget`
返回计划营内的那只)——**这是本组唯一能在合理成本内交付的 (a) 路径**。

---

## 四、`zusult` + `zusboltdom`:买到条件 (a),WORKING

### 4.1 门的读数(`zusult_gate.py`,47 局中 47 局有 Zeus)

```
=== 门域内漏放,per 种子 x Zeus 物理侧 x 腿(铁律 4(i-a)) ===
seed   zeus_on  leg        games    opp_frm  in_domain   per100
6743   radiant  armed         17         75          0      0.0
6743   radiant  baseline       7         45          1      2.2
6858   dire     armed          5         24          0      0.0
6858   dire     baseline      18        122         10      8.2

TOTAL armed     games=22 opp=99  in_domain=0  (0.0/100)
TOTAL baseline  games=25 opp=167 in_domain=11 (6.6/100)
```

**⭐ 这个读数的侧偏是自己消掉的,而且不是靠 swap-average 算式**:
镜像波里**英雄→队伍在一粒种子内是死的**(本轮 47 局逐局复核:6743 的 Zeus 恒 team=2,
6858 的 Zeus 恒 team=3)⇒ **一粒种子内「Zeus armed」与「Zeus 在某个物理侧」完全共线**。
但两粒种子的共线方向**相反**:6743 是 Radiant-Zeus-armed,6858 是 Dire-Zeus-armed。
**armed 腿在两个物理侧上都读 0,baseline 腿在两个物理侧上都 >0** ⇒ 效应在侧翻转下存活。

若真实率是 baseline 的 6.6/100,99 个 armed 机会帧上的期望漏放约 6.5 次,观测 0 次;
**这是效应不是抽样**(泊松尾 ≈ 1.5e-3)。

### 4.2 归属:哪一半是 `zusboltdom` 的

⚠️ **先说清楚合力**:`X.zuus_ShouldSaveManaForUlt` 的**整个门**都挂在
`zusult`/`zusultx` 后面(`hero_zuus.lua:464`:`if not bPostSpend and not
J.IsSoakCandidate('zusult') then return false end`),而 W52 的 armed 串里
`zusult` 与 `zusboltdom` **同时 armed**、`zusultx` 与 `zusboltcap` **都不 armed**。
⇒ 上表的 0 vs 11 是**两者合力**,`baseline` 那一侧是**根本没有门**。

分解靠 baseline 漏放的**消费者拆分**,那是工具自己打的:

```
base  in_domain via: considerW2=6  considerW=0  ambiguous=1  Q=4
armed in_domain via: considerW2=0  considerW=0  ambiguous=0  Q=0
```

- `Q=4` 与 entity-W 那部分,是 `zusult` **单独就能管**的域。
- **`considerW2=6` 正是 `zusult` 单独管不着的那 6 次** —— GH #47 的豁免让
  kill-AoE 分支**不上报目标**,门对 nil 目标 inert。
  **这不是推理,是量过的**:W45 上 `zusult` 单 armed 时,armed 腿读 **8.0/100**、
  baseline **8.6/100**,而那 7 发**全部**是 hero level 7–9 的点施法 ⇒ 全走 `ConsiderW2`
  (章程「工具坑」§W45)。**`zusboltdom` 就是为这 6 次写的**,本轮它们 → 0。
  ⚠️ **这一步是跨波论证**(W45 语料 ≠ W52 语料、树也不同),**按跨波登记**,
  不冒充同波分解;要同波分解,得发一粒 `zusult` 单 armed 的对照腿。

### 4.3 反证:分支没有被整体关掉(过宽的排除)

`zusboltdom` 的设计承诺是「**只**决定这一 bid 要不要交给储备门看,不动它是否开火、
不动落点」。逐局普查闪电波的**目标形态**(点施法 = kill-AoE 分支签名):

```
seed   zeus_on  leg        games      point     entity    pt/game
6743   radiant  armed         17        468        160      27.53
6743   radiant  baseline       7        203         38      29.00
6858   dire     armed          5        124         49      24.80
6858   dire     baseline      18        539        112      29.94

TOTAL armed    games=22 point=592 (26.91/局)  entity=209
TOTAL baseline games=25 point=742 (29.68/局)  entity=150
```

armed 腿仍然**每局点施法 26.9 发**(出厂 29.7),两粒种子同号、幅度约 −9%。
⇒ **分支照常开火,只在门域内被摁住** —— 承诺兑现,不是「Zeus 不放 W 了」。

### 4.4 帧证据(逐帧,`20260907_010129_slot4` baseline,t=339.2)

直读事件流:

```
t=339.2 ABILITY       npc_dota_hero_zuus -> dota_unknown          [zuus_lightning_bolt]
t=339.2 DAMAGE        npc_dota_hero_zuus -> npc_dota_hero_luna    [zuus_static_field]
t=339.2 DAMAGE        npc_dota_hero_zuus -> npc_dota_hero_luna    [zuus_lightning_bolt]
t=339.2 MODIFIER_ADD  npc_dota_hero_zuus -> npc_dota_hero_luna    [modifier_stunned]
```

`ABILITY` 的 target 是 **`dota_unknown` = 点施法**,不是实体施法 —— 这正是
`FindAoELocation` 那条 kill-AoE 分支的签名。工具读到的其余字段:
`tgt_hp=0.63`(healthy,≥0.60 地板)、`zeus_hp=1.00`(不在撤退)、`mp=187`、
`Rlvl=1`(大招已练、CD=0、**买不起**)、`zlvl=6`。
**`zlvl=6 < 10` ⇒ 按章程 §W45 的判别子,消费者是 `ConsiderW2` 而非 `ConsiderW`
(天赋最早 10 级,`ConsiderW` 的实体路径 10 级以下结构上不可能)** ——
`via=considerW2` 是**被判别子认证的**,不是工具的默认值。
⇒ 这一帧就是 GH #477 立案时描述的那件事,**在 W52 的出厂腿上原样复现**;
armed 腿上 22 局 99 个机会帧里**一次都没有**。

---

## 五、⭐ 当轮最贵的一行:宽扫表的头条读数,四分之三是侧偏

`sweep_summary.md` 的池化表(两个 run 合计)长这样:

| detector | total | candidate-side | baseline-side |
|---|---|---|---|
| died_with_ult_ready | 370 | 236 | 134 |

读成「armed 腿多死 76%」是**铁律 4(i-b) 现场**。按 `sweep_strata.py` 重打:

```
SWEEP STRATA  games ab(armed=radiant)=35  ba(armed=dire)=12  seeds paired=2
detector               hit_ab hit_ba |     r_ab     r_ba |      arm     side     sd neg/n
died_with_ult_ready       274     96 |   +3.771   -2.143 |   +0.814   +2.957  0.601 0/2  FLIP
tp_under_threat           254     83 |   -1.956   -0.871 |   -1.414   -0.542  0.193 2/2
lowhp_limbo                47     16 |   -0.487   -0.314 |   -0.401   -0.086  0.111 2/2
overextend_alone          893    277 |   -5.721   +3.386 |   -1.167   -4.553  2.853 1/2  FLIP
```

- **`died_with_ult_ready`**:`side=+2.957` 对 `arm=+0.814` —— 池化表里那个刺眼的
  1.76 倍,**约 78% 是 Radiant 偏置**。而且**这两个 run 的分层本身就是 35:12 的偏配**
  (6743 是 17:7,6858 是 18:5),池化 = 按局加权 = 铁律 4(i-d) 明令禁止的那个手法。
  按 4(i-c),FLIP **不是**对 `arm` 的否决(`FLIP ⟺ |side|>|arm|` 是恒等式);
  `arm=+0.814` 两粒种子**同号**(`neg/n 0/2`),但 `sd=0.601` 只有 2 个自由度 ⇒
  **登记为待观察,不写进任何裁定**。下一轮补 6877/6907 两粒把自由度抬到 4。
- **`tp_under_threat` `arm=−1.414`、`sd=0.193`、`neg/n 2/2`、无 FLIP** ——
  本表**唯一四格同号又低离散**的读数,armed 腿每局少 1.4 次「威胁下 TP」。
  同族的 `lowhp_limbo` `arm=−0.401`、`sd=0.111`、`2/2`。
  ⚠️ 仍受 LIMIT 1/2 约束:**检测器计数不是行为,也不是任何单 id 的效应量**;
  W52 armed 串有 51 个 id,这两条只说「测试版整体在这两个形状上更少犯」。

**可迁移的一句(交给总监,也交给下一个读宽扫表的人)**:
`sweep_run.sh` 写出来的 `sweep_summary.md` **是池化表**,而池化表在偏配分层上
**恒等于 4(i-d) 禁止的按局加权**。`sweep_strata.py` 2026-09-02 就建好了,
但**宽扫脚本自己不调它** ⇒ 每一个只看 `sweep_summary.md` 的读者,
拿到的是一份**预先算好的铁律违例**。本轮开 [harness] issue 请把 strata 表
并进 `sweep_summary.md`(或至少在表头打一行「这是池化表,不可单独引用」)。

---

## 六、开工自检 / 铁律 6

- **自检**(重定向,裸读):`SELFCHECK_EXIT=3`,`legs run 10`;
  `FINDINGS (exit 3): cadence queue-rulings owed-executions trunk-red(lua)`;
  `UNCERTIFIABLE (exit 2): trunk-red(python)`;
  `NOT RUN: tests/test_rc_wrapper.py tests/test_selfcheck_lua_leg.py`
  —— **那两条本轮没人看过,不是通过**。
  ⚠️ **第三十次登记:自检在本容器不是「约 20s」**(本轮约 9 分钟)。
- **trunk Lua 红 4/84,先于本轮存在,已有 issue**:
  `test_gated_helper_nesting_census`(`pgchannel|tpwatch` 新嵌套)、
  `test_stayfield2_marginal_domain`、`test_stayfield_callsite_domain`、
  `test_stayfield_hp_window_reach`。**工作树 `git status` 空** ⇒ 是 trunk 的红,不是本轮的。
  **已由 GH #601 覆盖(2026-09-07T15:39Z 开)**,本组不重复开 issue,登记转总监。
  `cadence`/`queue-rulings`/`owed-executions` **不归本组解读,登记转总监**。
- **铁律 6 静态半**:见文件末尾追记。
  **动态半(GH #124)本轮不跑也不声称** —— 本轮 `bots/`、`tests/`、`tools/` 一行未改,
  改动全部在 `iterations/` 下的 markdown。
- **证据纪律 3**:⛔ **当轮第一条命令又踩了管道**(`routine_selfcheck.sh 2>&1 | tail -40`),
  脚本当场自拒 `REFUSED … exit 2, nothing checked`,harness 报回来的是 `tail` 的 0。
  改重定向后才拿到真退出码 3。**这是本组连续第五十次踩、且第 N 次踩在当轮第一条命令上**;
  批测台已第十四轮请求给它一个独有退出码或 wrapper(其 12:08Z 报告交棒 6),
  **本组本轮再附议一次**。

---

## 七、开出 / 追评

1. **[strategy] 新 issue** —— `campbind` 的条件 (a) 不可能从例行波买到(第三节 3.5):
   47 局 2 粒种子,可判读面 0;请总监裁改走 fixture 钉帧路径。
2. **GH #477 追评** —— `zusboltdom` 的条件 (a) **买到了**(第四节全文,含帧证据与
   跨波归属的边界)。
3. **GH #475 追评** —— `campbind` 读作 **INDETERMINATE 而非 SILENT**,理由与 1 同。
4. **[harness] 新 issue** —— `sweep_summary.md` 是池化表,应并入 `sweep_strata.py`
   的分层读数(第五节)。
5. **总监** —— GH #601 的 4 条 trunk Lua 红,本组独立复现登记(第六节),不另开。

---

## 八、下一轮第一件事

1. **补扫 W52 剩下两个 run**(`af2874`/6877、`0e6ccf`/6907),把 `sweep_strata` 的
   `sd` 自由度从 2 抬到 4 —— 第五节那两个读数(`died_with_ult_ready` 的 +0.814、
   `tp_under_threat` 的 −1.414)现在**都卡在自由度上**,不是卡在效应量上。
2. 深查维持 6 局(本轮 7 局,已回到节奏)。
3. **存量顺延**(逐轮携带,未做):`campgrade` 第十二轮 / 61-id 家族 W49 两笔条件 (a);
   `tpreach_domain.py` 补 `by_seed`(**已连欠十三轮**);`roshdist` 的 BUGGY(77)交总监;
   09-07T12:59Z §3.4 那一帧钉 fixture;F2/GH #530;`--analysis-dir` 基名碰撞即拒绝
   (GH #529);`outlatch` 重扫;**#477 重 dump** —— ⭐ **本轮以另一种方式交付了它要的东西**
   (门域读数 + 帧证据),重 dump 是否还需要请总监裁;`cmqreach` 钉帧 fixture;
   09-04T16:01Z §2.1 那一帧;F2 那一帧(`272131__20260905_125215_slot3`
   dragon_knight t=1142.4);#419 / #421 仍零评论。

---

## 九、本轮 token

(见文件末尾追记。)

---

## 追记(推送与发表之后)

**铁律 6 读数**:静态门 **`GATE_EXIT=0 CLEAN`**(`luacheck bots game: 0 warnings`;
门自己装的 luacheck:`installed luacheck (it was missing; apt package lua-check, bounded)`),
**未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行可抄**;推送门 `ARM_HOOK_EXIT=0`。
**动态半(GH #124)本轮不跑也不声称** —— `bots/`/`tests/`/`tools/` 一行未改。

**本轮 token**:`TOKENS total_in=8,878,557 out=54,515 turns=65`(到统计时刻为止;
此后的收尾回合不计入)。

**推送**:见下方补记。
