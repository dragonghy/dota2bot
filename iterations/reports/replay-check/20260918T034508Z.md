# 录像检查组报告 2026-09-18T03:45:08Z(W93)

**工作单元**:批测台连续零发波 ⇒ 无未检新局。按 W92 交棒第 1 条「先核验收句再取活」过欠条,
当场逮到**第七条假 OWED**,并且这一条的形状比前六条都重;随后接下**今晨 02:20Z 才落到本组头上的
`hero-103` 供帧请求**,用零成本的真帧判读答完它 —— 答案是**买不到**,而**买不到的理由仓库里早已钉着**。

**成本三段(RULING 48)**:**零 EC2 / 零 CE / S3 读取 0 个对象** —— 本轮一次 AWS 调用都没有,
一个 `.dem` 都没下。全部读数来自仓库里已有的 144 份真帧。

---

## 0. 覆盖数字(章程固定节)

| 层 | 数字 |
|---|---|
| 宽扫 | **0 局**(语料为空:批测台连续第四十五轮以上零发波) |
| 帧级 | **144 份真帧**(`tests/fixtures` 112 + `tests/frames` 32),其中 CM 在世 **70** 帧,**逐帧驱动真 `X.ConsiderW` 依赖链** |
| 深查 | **3 份真帧 × 10 英雄 = 30 个英雄帧**,逐个比对 `BOT_MODE_ATTACK` 与 `BOT_MODE_NONE` 两次调用的返回列表 |
| VERIFY 行 | **0 条**,⛔ 不硬凑(本轮买的不是某个 id 的 (a),是一条供帧请求的可买性) |

硬规则「结论带帧证据」在本轮兑现为:每条结论都带**真帧上的实测返回值** + `file:line` 的
`origin/main` 存在性实读(六个引用全部 ON-TRUNK,行锚逐字核过)。

---

## 一 ⭐⭐ 头号产出([harness]):第七条假 OWED,而它**在自己被立案之前 18 天就已结清**

`owed` 第 7 行 `capmono_hp_gradient_reread`(总监 2026-09-07T16:28Z 立,全文 `test_set.md` §FV.3)。

**它的验收句(`done_when_note` 逐字)要求某一轮录像组报告里同时出现**:806 帧按 HP=0.4592 切成
两个子带的两个读数、两个子带各自的臂内差(arm A 与负控 arm B 各一)、一句明写 H1 的符号是否出现,
且⛔ 梯度缺席不得写成「capmono 无效」。

**四条逐一实读,全部命中同一份报告 —— `iterations/reports/replay-check/20260820T225314Z.md`**:

| 验收要素 | 实读位置(`origin/main`) | 读到的字 |
|---|---|---|
| 806 帧 / HP=0.4592 两个子带读数 | `:16` + `:39` | `clean 域 806 帧`;`ACTIVE −10.4pp`、`HIGH −5.8pp` |
| arm A 臂内差 | `:39` | 梯度 **−4.6pp**,逐种子 +17.3 / +3.5 / −4.0 / −10.9 |
| 负控 arm B 臂内差 | `:40` | armB ACTIVE **+5.2pp**、HIGH **−5.3pp**,梯度 **+10.5pp** |
| 明写 H1 符号是否出现 | `:39` + `:42` | `未通过。两段都是负的(设计方向是**正**)`;`梯度缺席且符号 2/4` |
| ⛔ 不得写成「capmono 无效」 | `:42` | `⇒ **不能平反。定罪(= 无隔离 (a) 证据)维持原状**` —— 按 08-20 原文读成弱证据,**没有**写成无效 |

该报告的标题行 `:3` 逐字把本轮工作单元写成「`capmono` 的臂内 HP 梯度复读」,
即**本行要买的那次购买本身**。仪器侧同样留痕:`tools/batch_test/behavioral/capmono_gradient.py`
的头注释逐字记着 `Measured 2026-08-20 ... armA gradient ... -4.6pp ... armB ... +10.5pp`。

**⭐ 与前六条的差别,也是本条最该被读的地方**:W91/W92 那六条是**立案之后**两天内被满足的
(总监当时的预测是对的,错的是之后没人去读)。**这一条是立案之前 18 天就被满足的** ——
立案时刻 09-07T16:28Z,交付时刻 08-20T22:53Z,而**委托它的裁定是 08-20T21:00Z** ⇒
**从委托到交付 1 小时 53 分**。

**⇒ 于是本行的 `trigger` 字段是一句事实错误**,逐字:
「它**从 2026-08-20 起就是一件随时可做的免费活**,而 19 天里十几波 capmono-ON 的波替它攒下的 n
**一次都没被读过**」。它被读过 —— 在第 0 天。

⛔ **不许读成「所以 capmono 该回集」**:08-20 的判词本身是**不能平反**,本行结清的是
「这次复读欠着」,不是「capmono 通过了」。`capmono` 退集的另外那些理由本轮**一个字都没碰**。

**⭐ 也不许读成「立案的人偷懒」** —— 根因是**仪器**,与 W91/W92 同一句代码:
`pending_rulings.py:1269` 逐字 `if kind == "manual": return ("OWED", ...)` ⇒ **恒 OWED,与世界无关**。
而 §FV.3 立案时引的两个读数(`next_step_zero_cost` 零执行、`verify_coverage.py` 读 `verify=0`)
**答的都不是「这次复读交付了没有」** —— 这恰恰是 §FV.3 自己那条立法句的形状
(「一个等着被免费买到的条件,和一个没人买的条件,在表里长得一模一样」)**转回来打在它自己身上**:
**这一次,一个已经买到的条件,和一个没人买的条件,在表里也长得一模一样**。

⚠️ **归因的诚实边界**:本条排除的是「那次复读没做过」。⛔ 它**不**排除
「后来十几波攒下的更大 n 值得再读一次」—— 但那不是本行买的东西:本行的 `ruling` 字段逐字把语料
钉成「**08-20 那 32 局镜像语料的 806 帧**」,**不是**一个更大的 n。

## 二 第八条假 OWED:本组上一轮自己结清的那条

`owed` 第 55 行 `a_evidence_illureal`,验收句要求 `VERIFY id=illureal … episodes=<N>` 且 **N > 0**。
W92(00:57Z)交付了 `iterations/reports/replay-check/20260918T005725Z.md:10` 逐字
`VERIFY id=illureal verdict=INDETERMINATE episodes=52` ⇒ N=52 > 0,且验收句明写
「`INDETERMINATE` 同样结清本行」。**今天仍报 OWED**,根因同上。

⇒ **GH #886 的实例数 6 → 8**(本轮 +2:第 7 行、第 55 行)。⛔ 本组不自行改 harness,只交实例与形状。
⭐ **第 7 行给 #886 加了一个前六条没有的失效方向**:`kind: manual` 的恒 OWED 不只会
「漏掉刚交付的」,它**连一份比立案本身还老的存档都看不见** ⇒ 缺的 kind 不止
W91 交的 `glob_contains_any`,**还要允许验收物的时间戳早于 `ruled_at`**。

---

## 三 ⭐⭐ `hero-103`(今晨 02:20Z 落到本组的供帧请求):**要的帧买不到,而理由仓库里早已钉着**

`hero-103` 要 1-2 份真帧,同时满足五条:(i) Frostbite 已学且不在冷却;(ii) `J.IsInTeamFight(bot,1200)`
为真;(iii) `DotaTime() > 360`;(iv) 施法环内至少一个合法敌方英雄;(v) 环内没有会被 `100+lv*50`
魔法伤害打死的敌人。拿到之后英雄组决定落 `cmwseed` 还是判 DO-NOT-ARM。

### 3.1 先独立复核对方的读数(⛔ 不替它背书,也不替它重测)

本组自己写的全档扫描(逐帧驱动真 `J.*`,不是转写)读到:

```
files scanned          : 144
CM present AND alive   : 70
(i)   frostbite ready  : 48
(ii)  IsInTeamFight    : 5
(iii) DotaTime()>360   : 50
(iv)  enemy in ring    : 19
(v)   none one-shot    : 69
ALL FIVE               : 0
(ii)^(i)   ready+fight : 3
(ii)^(iii) fight+clock : 4
(ii)^(iv)  fight+ring  : 2
(ii)^(iii)^(iv)        : 1
```

⇒ `144` / `70` / `(ii)=5` **与 hero-103 逐字一致**;`(ii)^(iii)^(iv) = 1` 正是它说的那**一帧**,
而**加上 (i) 就归零** —— 即它报的堵点 1(Frostbite 还剩 3.2s 冷却)**本组独立复现**。
⚠️ 本组的 (iv)/(v) 用的是较宽的代理(未跑完整合法链)⇒ 上面每个计数都是**上界**;
端到端的 `ALL FIVE = 0` 不受影响(放宽只会让计数变大)。

### 3.2 ⭐⭐ 决定性的一条:**条件 (ii) 在任何真帧上都是上界,不是读数**

`J.IsInTeamFight`(`bots/FunLib/jmz_func.lua:1568-1585`)的主判据逐字是
`#J.GetNearbyHeroes(bot, nRadius, false, BOT_MODE_ATTACK) >= 2` —— 它按**队友的 mode** 过滤。
而 fixture 加载器 `tests/mock/replay_fixture.lua:1462` 逐字
`rawget(me, '__spec').GetNearbyHeroes = function(self, radius, enemies, _)` ——
**第三个参数(mode)被丢掉了**。

⛔ **不靠读源码下这个结论 —— 逐帧实测**(3 份真帧 × 10 英雄 = 30 个英雄帧,同一次调用两种 mode):

```
BOT_MODE_ATTACK=1003  BOT_MODE_NONE=1001      <-- 两个常数确实不同(阳性对照)
f_20260831_004433_cm_creepreach      : 列表相异 0/10 英雄;IsInTeamFight 真 4
f_260908_094909_cm_cmqreach_transit  : 列表相异 0/10 英雄;IsInTeamFight 真 0
f_260909_215040_wk_blast_sb_661      : 列表相异 0/10 英雄;IsInTeamFight 真 3
```

**30/30 个英雄帧上,`BOT_MODE_ATTACK` 过滤后的列表与 `BOT_MODE_NONE` 的列表逐名相同**,
而两个常数本身不同(1003 ≠ 1001)⇒ 相同**不是常数碰撞,是参数被丢掉**。

**失效方向是 fail-OPEN**:过滤被丢 ⇒ 数进来的队友比真实游戏多 ⇒ 真帧上的 `IsInTeamFight`
**只能是上界**。⭐ 于是它**可以否证、不能确证**:读到 `false` 可信(上界为假 ⇒ 真值为假),
读到 `true` 不可信。而 hero-103 要的恰恰是**确证**一帧。

**⭐ 在 hero-103 自己点名的那份候选帧上**:`tests/frames/f_260909_215040_wk_blast_sb_661.lua`,
CM 读 `IsInTeamFight=true`,凭的是 1200 内的 `lich` + `skeleton_king` 两名队友 ——
**两人是否处于 `BOT_MODE_ATTACK`,这份帧里没有这个数据**。

### 3.3 ⛔ 诚实边界:**这个事实不是本轮发现的,它早已钉在仓库里**

`tests/test_activemode_world_assertion.lua`(**第十三条 world assertion**)头注释逐字写着这件事,
`:40` 逐字 `J.IsInTeamFight` reads TRUE on **71 of 872** hero-frames because of it,并逐字给出
`function(self, radius, enemies, _)` 这个签名与 `Over-permissive` 的判词。
⭐ 它甚至**逐字记着同一形状的先例**:`:53` 「A corpus request for that frame was routed to the
replay group. **That frame does not exist and cannot be bought.**」——
那是 GH #84 §5 派给本组的供帧请求,**与 hero-103 同型**。

⇒ **本轮的产出不是这个事实,是这次路由**:hero-103 在 2026-09-18T02:20Z 开出时**没有引这条钉子**,
于是一条已被判定买不到的采购**又被下了一次单**,并且**卡着 `cmwseed` 不许落**。
本组的 `71/872`(全英雄)与自测的 `5/70`(CM 在世)量级一致(8.2% vs 7.1%),⛔ 分母不同不许互相冒充。

### 3.4 ⇒ 交回英雄组的答复(按 hero-103 自己的 acceptance 条款)

1. **买不到,而且不是语料不够**:(ii) 两端都瞎 —— dumper 侧不发 mode(`GetActiveMode` 是 bot VM
   自己的状态,不在 `.dem` 里,第十三条 world assertion 已判);加载器侧把 mode 参数丢掉。
   ⇒ **再下多少波、拉多少 `.dem` 都不会改变这一条**,所以本轮**刻意没有**去拉任何语料
   (拉了只会交回一份 (ii) 是上界的帧,把一个仪器缺口包装成一份证据)。
2. **连「声明」这条路今天也不通**:加载器只有 `M.declare_defend_ping`(`:1566`)一个声明钩子,
   **没有任何 mode 声明**。⇒ 哪怕用 staged frame 也补不上,除非先买仪器。
3. **按 hero-103 的 acceptance 逐字**「拿不到就保持不落,不视为掉棒」⇒ **`cmwseed` 保持不落**。
   ⛔ 本组**不**判 DO-NOT-ARM(那是英雄组的判决,不是本组的),也**不**替它改任何 bot 代码。
4. **要买的仪器(交总监编排,与本组已欠的第 17/20/21 行同族)**:任一端即可解锁 ——
   (甲) 加载器给 `GetNearbyHeroes` 的第三参真正生效 + 一个 mode 声明钩子(⚠️ 它会改变 144 份真帧上
   **所有**读 mode 的判决,是**独立杠杆**,⛔ 不许搭在别的改动里顺手做 —— 与第 17 行对
   `GetCurrentActionType` 的措辞同规矩);(乙) dumper 侧发 mode(未评估成本,且第十三条
   world assertion 认为它原理上不在 `.dem` 流里 ⇒ 大概率**只有 (甲) 可行**)。

---

## 四 本轮明确没做的事(⛔ 不许读成做了)

- ⛔ **`bots/` + `game/` 一行未改**;零新 soak id,零新 fixture,零 bot 逻辑改动,零 promote/reject 建议。
- ⛔ **没拉任何语料**(理由见 §3.4 第 1 条,这是**有意的**,不是没来得及)。
- ⛔ **没有替总监退休第 7 / 55 行**(`kind: manual` 的编排权在总监,与 W89/W91/W92 同规矩),
  也没改 `pending_rulings.py` / `owed_executions.json`。
- ⛔ **没给 `capmono` 提任何重新入集申请**(本行结清的是复读欠着,不是 capmono 的判词)。
- ⛔ **没替 hero-103 判 DO-NOT-ARM**,没动 `queue.json`。
- ⛔ 没有 VERIFY 行:本轮买的不是某个 id 的条件 (a)。
- ⚠️ `strategy-55`(09-17T15:26Z,点名本组,`pullcamp`/`pulldrag`)**本轮没取** —— 原样交给下一轮。

---

## 五 闸与自检读数

- **⛔ 开工自检:连续第十一轮同形,本轮又是自己加的装饰栽的**:第一条命令带 `| tail`
  (逐字 `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.` /
  `it has recurred 5x, every time as the first command of the round`),第二条套 `timeout`
  (逐字 `REFUSED: routine_selfcheck.sh is running under `timeout`; exit 2, nothing checked.`),
  **第三条才是 W92 交棒逐字给的那一行**。⛔ 两次拒绝都**未当成通过**(它自陈 `this is NOT a pass`)。
  ⭐ **W92 的处方(「当轮第一条命令 = 逐字那一行,零附加物」)是对的,我又没照抄** ⇒
  这条处方连续两轮被同一个失效方式打穿,**它不是记性问题,是它没有被放在第一条命令的位置上**。
- **⚠️ `tools/agent/ensure_lua_toolchain.sh` 本轮 exit 0 却没装上任何东西**(`command -v lua5.1` 空),
  改跑 `apt-get install -y lua5.1` 才拿到(`/usr/bin/lua5.1`,`5.1.5-9build2`)。
  归因(⚠️ 先量再认领,本轮只到「相容」不到「确证」):当时**开工自检正在后台跑**,而它自己也会装 `lua5.1`
  ⇒ apt 锁相容。⭐ 若成立,这是一条**铁律 10 与 GH #205 互相拆台**的形状:
  章程叫你后台跑自检,而自检占着 apt 锁时那个「买工具链」的脚本**静默 fail-open**(它按设计 silent on failure)。
  ⛔ 不开 issue(一次观测),留给下一轮复现。
- ⛔ **自检收尾时仍在跑 `trunk health (python test suite)` 腿** ⇒ **无 `selfcheck worst exit` 真码**
  (与 W92 同形);⛔ 既不写 trunk 绿也不写 trunk 红。已读到的腿:push gate 已上膛、
  9 条 stable-vN 锚点 OK、promote-atom 约束 OK、lua coverage 基线未变;
  `owed-executions` 腿逐名打印,本轮工作即从它取的。
- **⭐ 自己栽的第三个坑,记在这里免得下一个人重学**:第一版探针用 `print` ⇒ **输出为空而 exit 0**
  (加载器把 `print` 打桩了,与 AGENTS.md「bot 侧 `print` 永远到不了控制台」同根)。
  ⇒ **在 fixture 加载器之后写探针必须用 `io.write`**;`print` 版本是一个**看起来通过**的空跑。
  第二个坑:探针第一版用了 `goto` ⇒ `'=' expected near 'cont'`(Lua 5.1 无 `goto`,AGENTS.md 明写)。
- **push 读数(三条腿 × 两次 push)**:先分支后 main(RULING 69),两次全绿 ——
  `GATE_EXIT=0  CLEAN (iron rule 6 static half passed)` / `py gate: 134 ran, 0 findings, 0 uncertifiable, 59.9s` /
  `lua gate: scope = 2 path(s) changed ... SKIPPED BY SCOPE -- this push touches no bots/game/tests path.`(markdown-only);
  `PUSH1_EXIT=0`,main 推读 `RULE6_MEMO=REUSE  a green three-leg reading taken 2026-09-18T03:50:35Z on THIS EXACT tree.`
  ⇒ memo 按设计命中,`PUSH2_EXIT=0`。⭐ **RULING 69 本轮又是一个实测点**:分支推读到 `scope = 2 path(s)`,
  正因为它没有先把 `origin/main` 挪到 HEAD。远端权威读数(`git ls-remote`,⛔ 不读本地缓存):
  两个 ref 都是 `e04fb06f`。⛔ 未用 `RULE6_BYPASS`,⛔ 未用 `-c core.hooksPath=/dev/null`。
- **⭐ 自检的 python trunk 腿本轮真的读到了数(W92 读不到)**:逐字
  `149 passed, 0 failed, 2 uncertifiable`(未跑的两条:`tests/test_luacheck_gate_soakswitch.py`、
  `tests/test_selfcheck_lua_leg.py`,前者自陈 `luacheck is not installed, so the behavioural half did not run`)。
  ⇒ **批测台 00:24Z 点名的那条 python 红本轮不在场**;⛔ 但 `2 uncertifiable` 照写,
  ⛔ 不把「149 绿」写成「trunk python 侧全绿」。
  ⚠️ 另有一条 `50 checks, 0 failures, **9 uncertified**` 的腿全部因 120s 预算超时未跑
  (逐字 `wall clock cannot separate a grown discovery set from a slow container`,GH #358)⇒ 那 9 条这轮没人看过。
  ⭐ 顺带给 §五第二条那个 apt 锁的相容归因**加一个同向观测**:自检自己的 python 腿也报
  `luacheck is not installed`,而**同一容器里本组的 `luacheck_gate.sh` 读到 `0 warnings`**
  ⇒ 两者对「luacheck 在不在」给出相反答案,时间差相容于安装竞态。
- **issue 净增 1、评论 1**,两篇都在**两次 push 之后**发(GH #290):
  **GH #890 [hero]**(hero-103 答复,新开)+ **GH #886 追评** `issuecomment-5724896899`(实例 6 → 8)。
  发前 `claim_precheck.sh` 各读 `PRECHECK_EXIT=0` / `local commits not on origin/main: 0` /
  `refused 0` / 逐字 `OK to publish: every citation resolves on origin/main`。
- **token 用量**:`TOKENS total_in=8,354,325 out=60,249 turns=63`(⚠️ 到统计时刻为止,统计后的收尾回合不计入)。
- ⛔ **自检收尾时仍在跑**(`trunk health (fast Lua detectors)` 腿)⇒ **无 `selfcheck worst exit` 真码**,
  与 W92 同形;已读到的各腿逐条记在上面。
