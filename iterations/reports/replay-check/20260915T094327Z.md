# 录像检查组 2026-09-15T09:43Z —— (丁) 那一格买到了(42 帧,上轮是 0),**但它没把 verdict 抬起来**;同一趟 #830 在**第二波独立语料**上以**更强的形式**复现

**一句话**:上一轮交棒点名的「最便宜的下一格 —— 补『dire 队 + gate ON』的 selector-fed 暴露」
**做成了**:`spot_20260912_0326xx` 波的 dire-arm 局给出 **42 帧**(上一轮该格 **0 帧**)。
⛔ **但 `abilanc` 仍判 `INDETERMINATE`,而且理由变了** —— 不是「没暴露」,是
**配对的 gate OFF 腿这一波也是 0 次**:可达域在这波上**两条腿全空**。
⭐ 同一趟买到一条更硬的东西:**GH #830 在这波上复现,且是更强的形式** ——
本波 DOMAIN 的**整个分子(1 次里的 1 次,100%)**是一个**射程外**英雄(`skywrath_mage`,
`grep -c GetMostHpUnit` = **0**),于是工具打的 `WORKING` 与可达读法的 `EMPTY-DOMAIN`
**不是量级之差,是 verdict 类别之差**。
⛔ **零 EC2、零 CE**;**S3 读取 13 个 `.dem` + 13 个 `.analysis.json` + 若干列目录(出网未计价)**。
`bots/` + `game/` **一行未改**,零新 soak id。

---

## 〇 本轮的覆盖数字(章程固定节)

- **宽扫**:**9/9 局**(W `spot_20260912_0326{03,05,08,10}` 四台的全部 armed 局),
  `SWEEP_EXIT=0` × 4,每台 `games swept: 3 (skipped 1 warmup/unstamped, unparseable 0)`,
  第四台 c9aad7 `games swept: 0`(它只录到 1 个暖场 `.dem`)。
- **深查(逐帧)**:**9 局**全部重算 fed-exposure(按物理队 × gate 四格);
  **3 具身体钉到逐帧账本**(`skywrath_mage` idx=1534、`sven` idx=1497、`ogre_magi` idx=1534),
  **49 次施法逐条判可达性**,**2 条正控制**各自钉到具体事件行。
- **本轮两层俱全**:6 局 radiant-arm / 3 局 dire-arm。
  ⛔ **但三粒种子里只有一粒供货**(见 §三.3)。

**切法(铁律 4(iii),与每一个数一起引)**:W `spot_20260912_0326{03,05,08,10}` 四台;
每台 50 局 stamped + 6 局暖场,**`.dem` 只存在于 `slot1`**(a6cf27/1fb832/192177 各 4 个 =
1 暖场 + 3 armed,c9aad7 只有 1 个暖场)⇒ 可逐帧样本 **9 局**,不是 200 局;
seeds **12924 / 12888 / 12542**;body = `(hero, idx)`;
营地聚类由 `games[:WARMUP_GAMES=10]`(= 全部 9 局)学,读到 **2 个 ancient cluster**
(`(-4747,-66)` 与 `(4131,-40)`,覆盖 92.9%);等级用 bracket 不插值;
`R_SWEEP_MAX=1000`;`ANCIENT_MIN_LEVEL=12`(从 Lua 读,不是本文件写死的)。

---

## 一 语料:这波从没人检过,`.dem` 在 `soak/` 下(不是 `dem21/`)

`iterations/reports/replay-check/` 全文检索 `20260912_032603`:**0 次命中**
(上一轮章程逐字点名它「仍未检」)。本轮取它。

⭐ 上一轮登记的那个「会骗人的读数」(`soak/` 下 `.dem` 为 0 要先看 `dem21/`)**本轮不适用**:
这波是 `REC_SLOTS=1` 波,`.dem` 就在 `soak/<run>/` 下。⛔ 仍然按 `grep -c "\.dem$"` 数
(不带 `$` 会把 `.demclaim.json` 数进去),实测 soak 4/4/1/4,dem21 0/0/0/0。

**arm 串**:四台的 stamped 局逐字带 `abilanc`,29 ids;side 与 seed 从
`script_version` 的尾两段读(`mirror:<ids>:s<seed>:<armside>`)。
⚠️ 与上一波不同,**这波每台机器内部两个 side 都有**,但**只有 `slot1` 被录像**,
于是可逐帧的 9 局是 6 radiant-arm + 3 dire-arm。

---

## 二 帧证据(硬规则:先逐帧后聚合)

### 2.1 ⭐ (丁) 那一格:dire 队 + gate ON,**42 帧暴露、0 次域内施法**,并带两条正控制

`20260912_040320_slot1`(run `a6cf27`,seed **12924**,`side=dire` ⇒ **dire 队是 armed 腿**)。

**`sven`,W71 判别子 `sorted({idx})` = `[1497]`(长度 1,不需消歧,但查了)**:

```
t=  350.5 lvl= 5 hp_pct=1.000 x=  4910 y=  -423 dist_to_camp=  869
t=  351.5 lvl= 5 hp_pct=1.000 x=  4470 y=  -449 dist_to_camp=  532
t=  352.5 lvl= 5 hp_pct=1.000 x=  4176 y=  -481 dist_to_camp=  444
...
t=  744.5 lvl=11 hp_pct=0.954 x=  4040 y=   104 dist_to_camp=  170   <-- 最近 170u
t=  746.5 lvl=11 hp_pct=0.963 x=  3280 y=   101 dist_to_camp=  862
```

**23 帧**档次线下、活着、在远古营 1000u 内(等级 5/6/9/11,最近到 **170u**)。
这个窗口里他放了 **11 次技能**,`target` 全是英雄或 `dota_unknown`(自增益),
**一次都没打到中立单位**;`DAMAGE -> ancient` = **0**。

⭐ **正控制**:同一具身体**跨过档次线后照样打远古** ——
`{"t": 1054.1, "type": "ABILITY", "actor": "npc_dota_hero_sven", "target": "npc_dota_neutral_black_dragon", "inflictor": "sven_storm_bolt", ...}`,
该时刻 **lvl bracket [17,17]、hp_pct=1.000**;整局 `DAMAGE -> ancient` **26 次**,
首次 t=1054.2。⇒ 档次线下那个 0 **不是**「这具身体不碰远古」。

**`ogre_magi`,`sorted({idx})` = `[1534]`(长度 1)**:**19 帧**(等级 1/4/6,最近 **416u**),
窗口内 **18 次施法全部指向英雄**,`DAMAGE -> ancient` = **0**。
⭐ **正控制**:`{"t": 889.5, ... "target": "npc_dota_neutral_granite_golem", "inflictor": "ogre_magi_ignite"}`
(**lvl [17,17]**)与 `{"t": 1400.2, ... "target": "npc_dota_neutral_prowler_shaman", "inflictor": "ogre_magi_ignite"}`
(**lvl [23,23]**);整局 `DAMAGE -> ancient` **91 次**,首次 t=890.4。

⇒ **这正是工具头注预言的 WORKING 长相**:档次线下的远古施法没有,档次线上的照旧。
**而这一格上一轮是 0 帧** —— 交棒点名要买的东西买到了。

### 2.2 ⛔ 但配对的 gate OFF 腿**这一波也是 0 次** —— 于是这一格**不能判决**

本波**唯一**一次档次线下远古施法:

```
{"t": 710.2, "type": "ABILITY", "actor": "npc_dota_hero_skywrath_mage", "target": "npc_dota_neutral_prowler_shaman", "inflictor": "skywrath_mage_arcane_bolt", "value": 0, "actor_hero": true, "target_hero": false}
```

`1fb832/20260912_034024_slot1`,`side=radiant` ⇒ 施法者所在的 **team 3 (dire) 是 baseline 腿**。

⭐⭐ **W71 判别子第三轮救场**:`sorted({idx})` = **`[628, 1534, 2315]`,长度 3**。
按名字取第一具会读到 `idx=628`(`lvl=6 hp_pct=0.000`,一具**死体**)。
**活的施法者是 `idx=1534`**:

```
t=  709.5 lvl=10 hp_pct=1.000 team=3 x= -3929 y=   356  dist_to_camp=  920
t=  710.5 lvl=10 hp_pct=1.000 team=3 x= -4006 y=   269  dist_to_camp=  813   <-- arcane_bolt
t=  711.5 lvl=10 hp_pct=1.000 team=3 x= -3952 y=   196  dist_to_camp=  837
```

活着、等级恒 **10**(`lvl_lo=lvl_hi=10`,不是插值)、距远古营 **813u < 1000**。
事件与身体都是真的 —— ⛔ **但施法者在 gate 射程外**(下 §三.1)。

---

## 三 ⛔ 为什么本组仍不采纳工具打的 `WORKING`

工具输出(9 局):

```
=== placement of every ABILITY -> ancient cast (49 total)
  certain_under       1
  straddle            0
  certain_over       48
  no_frame            0

DOMAIN (§BL.3) -- casts at an ancient by a hero below level 12
  arm side    games     armed  baseline     delta
  radiant         6    0/0.000    1/0.167    -0.167
  dire            3    0/0.000    0/0.000     0.000
  pooled          -         0         1   two-layer: one layer flat (not a contradiction)
=== VERDICT  WORKING
  armed leg is empty where baseline is not
```

### 3.1 ⭐⭐ GH #830 复现,而且**比立案那波更强**:本波分子**100% 不可达**

`grep -c GetMostHpUnit bots/BotLib/hero_skywrath_mage.lua` = **0**。
共享文件 `ability_item_usage_generic.lua` 的唯一选择器点仍是 item consider
(本轮实测 `generic_sites_are_items()` = `['ItemDesire']`,item 施法在战斗日志记 `ITEM` 不记 `ABILITY`)。
⇒ **arm 了 `abilanc`,`skywrath_mage_arcane_bolt` 那一次照样发生。**

| 切法 | DOMAIN armed | DOMAIN baseline | verdict |
|---|---|---|---|
| 现状(池化全部施法) | 0 | **1** | `WORKING` |
| 只留射程内(#830 建议的修法) | 0 | **0** | **`EMPTY-DOMAIN`** |

⛔ **这两个不是同一个结论的两种精度,是工具自己词表里两个不同的 verdict**
(`verdict()` 的 `EMPTY-DOMAIN` 逐字写着「empty domain is its own answer, not a silence」)。
立案那波(09-12 09:26)#830 把 baseline 从 **5** 改成 **3**,结论类别没变;
**本波它把结论从「核验通过」改成「这波什么都没测到」**。

**全波可达性普查(49 次施法)**:

```
placement        leg        reachability   n
certain_over     armed      FED            8
certain_over     armed      UNREACHABLE   21
certain_over     baseline   FED            7
certain_over     baseline   UNREACHABLE   12
certain_under    baseline   UNREACHABLE    1
```

**34/49 = 69% 不可达**(立案那波是 54%)。逐英雄:

| 英雄 | 施法数 | `grep -c GetMostHpUnit` | 射程内? |
|---|---|---|---|
| `zuus` | 16 | **0** | ⛔(它走 `J.GetFirstUnit`,归 `abil1st`) |
| `slardar` | 9 | **0** | ⛔ |
| `centaur` | 6 | **0** | ⛔ |
| `skywrath_mage` | 3 | **0** | ⛔ |
| `phantom_assassin` | 6 | ≥1 | ✅ |
| `ogre_magi` | 6 | 3 | ✅ |
| `skeleton_king` | 2 | ≥1 | ✅ |
| `sven` | 1 | ≥1 | ✅ |

### 3.2 ⚠️ 反向护栏(甲)**两波反号** —— 这是「它在这个规模上什么都证明不了」的第二个独立证据

| 波 | 池化 `certain_over` armed vs baseline | 只留射程内 |
|---|---|---|
| 09-12 **09:26**(上轮) | 13 vs 18 = **−27.8%** | 4 vs 10 = **−60%** |
| 09-12 **03:26**(本轮) | **29 vs 19 = +52.6%** | **8 vs 7 = +14.3%** |

⛔ **不要把 #830 的修法卖成「它会让护栏(甲)更严」** —— 上一波 fed 切法把读数推向不通过,
**本波推向更宽松的通过**。两波方向相反 ⇒ 这是**噪声不是系统偏**,
`fed` 修法的正当性来自**分子归属**(#830 §1),**不来自它让哪条护栏变紧**。
⭐ 这一条是本轮**新**的:立案那天只有一波,反号看不出来。

**反向护栏(乙)**(hero→ancient DAMAGE,不得塌到 0):radiant 709 vs 673,dire 410 vs 296。
两层都没塌。

### 3.3 ⛔ 三粒种子里只有一粒供货

按物理队 × gate 的 fed under-tier 暴露(**这是本轮的主读数**):

| 物理队 | gate | fed 暴露帧 | 射程内档次线下远古施法 |
|---|---|---|---|
| **dire** | **ON** | **42**(sven 23 + ogre_magi 19) | **0** |
| **dire** | OFF | 29(sven 26 + ogre_magi 3) | **0** |
| radiant | ON | 16(skeleton_king) | 0 |
| radiant | OFF | 6(skeleton_king) | 0 |

⛔ **这 93 帧全部来自 seed 12924 的三局**(run `a6cf27`)。
seed **12888** 与 **12542** 的 fed under-tier 暴露 **各为 0 帧** —— 它们不供货。
⇒ `comps_better` 无从谈起,**n 有效 = 1 粒种子**,与上一轮相同的病。

### 3.4 结论

(丁) 那一格的**暴露**买到了(42 帧,上轮 0),**两条正控制干净**,方向与 WORKING 一致。
⛔ **但条件 (a) 仍然没买到**,理由**换了一个**:
不再是「armed 腿没站到过营地」,而是 **「配对的 baseline 腿这一波一次都没犯过」**。
一个可达域两条腿全空的波,**对这个 id 沉默,不对它背书**。

```
VERIFY id=abilanc verdict=INDETERMINATE episodes=0
```

(`episodes=0` = **射程内**的 baseline 域事件数;工具那张表的 **1** 不是这个量 —— 它是射程外那次。)

⭐ **可迁移的那一句**:**暴露不是证据**。上一轮把「该格 0 帧」诊断成堵点、
本轮把它填到 42 帧,而 verdict 一动不动 —— 因为**分母大了,分子的两边仍然都是 0**。
下一次再遇到「补暴露就能判」的交棒,先问一句:**配对那条腿这波会不会也是 0?**

---

## 四 ⭐⭐ 下一轮最值钱的那一格:09-11 21:25 那波有 **104 个 `.dem`**,比迄今任何一波多一个数量级

顺手查 `dem21/` 时撞见的(**登记下来,这是本轮最值钱的副产物**):

| run | `soak/` 下 `.dem` | `dem21/` 下 `.dem` |
|---|---|---|
| `spot_20260911_212512_…_65af1b` | 0 | **25** |
| `spot_20260911_212514_…_2a2de3` | 0 | **31** |
| `spot_20260911_212517_…_d2a2fd` | 0 | **24** |
| `spot_20260911_212519_…_71f624` | 0 | **24** |
| `spot_20260911_152456_1_main_8d8c4b` | 0 | **24** |
| `spot_20260911_1524{27,33,51}` | 0 | **0** |

⇒ 21:25 那一波共 **104 局**可逐帧,而**本轮与上一轮各只有 9 / 12 局**。
抽查 `20260911_214200_slot15.analysis.json`:`script_version` 逐字以
`mirror:tpcommit,lf_rescue,…` 开头,**30 个 id,`abilanc` 在内** ⇒ **这波对 `abilanc` 能说话**。
⚠️ 它不是 `main` 树,是 `6b933bacf447197782a8b6200eb74546c81e5cf8` ——
引它的读数时**必须连这个 commit 一起引**。
⚠️ 它是 bulk 录像波(`slot15` 这种),`.dem` 在 `dem21/`(21 天过期),
`sweep_run.sh:50-57` 自己带这个回退,**不用改命令**;但 **21 天窗口**意味着
**约 2026-10-02 之后它就没了** —— 这是一条有到期日的资源。

⇒ **下一轮第一件事就该是它**:104 局能把 §3.3 的「n 有效 = 1 粒种子」和
§3.4 的「baseline 腿全空」**同时**解决,而且**零 EC2、零 CE、不需要新波次**。

---

## 五 本轮的限制(引本轮任何数字都要一起引)

- **LIMIT A**:9 局,3 粒种子,**其中只有 1 粒供 fed under-tier 暴露**。
  任何按 arm/baseline 的比较在本波上 **n 有效 = 1**。
- **LIMIT B**:可达性只判**文件级**(`hero_<name>.lua` 里有没有 `GetMostHpUnit`),
  与 `fed_exposure` 的分母同一个谓词。**昂贵的那一半**(文件里**哪个技能**的 consider
  用了选择器)本轮只对 §2.1 的两具身体读了源码,**没有逐英雄读**。
  ⇒ 表里的 ✅ 是「文件够得着」,不是「这次施法一定走那条码」。
- **LIMIT C**:营地聚类的 2 个 cluster 是从**本波 9 局**学的;
  `R_SWEEP_MAX=1000` 是 `campfarm_target` 的常数,不是 `abilanc` 自己的射程。
- **LIMIT D**:§3.2 的两波对比里,两波的英雄池不同(镜像草稿只在波内镜像),
  ⇒ **不能**把 +52.6% 与 −27.8% 读成「同一个量变了号」,只能读成
  **「这条护栏的读数在波与波之间不稳定」**。
- **LIMIT E**:#830 未修,本轮的 `FED/UNREACHABLE` 拆分是**手算**的
  (`selector_sites()` + 逐英雄 `grep`),不是工具打的。
  ⇒ 修好 #830 后应当**能复现本节每一个数**,这是一条可事后复核本轮结论的检查。
- **LIMIT F**:本轮**没有**碰 #257 的两条残项(分层闸口量错了量),也没有复核它们;
  那两条仍挂在 #257 下。

---

## 六 开的 issue

| # | 动作 | 内容 |
|---|---|---|
| **#830** | **评论,不开新号** | 第二波独立语料上的复现,**且是更强的形式**:本波分子 1 次里 1 次(100%)不可达 ⇒ `WORKING` → `EMPTY-DOMAIN` **换了 verdict 类别**;全波不可达率 **69%**(立案波 54%);⚠️ 并**撤回一个可能的误读**:护栏(甲)的 fed 切法**两波反号**,#830 的正当性**不来自**「它让护栏变紧」。 |

⛔ **净增 0**。⭐ 归属纪律:这是 #830 正文那条论证的**同一条缺陷的第二个现场**,
开新号会让下一个人以为有两道不同的闸。⭐ 发帖前读完 #830 正文 + 全部评论(**0 条**)。

---

## 七 铁律 6 / 开工自检 / 成本

(收工回填,见本文件末尾与章程「当前状态」节。)

**AWS**:`AWS_SETUP_EXIT=0`。成本三段(总监 RULING 48):
**零 EC2 / 零 CE / S3 读取 13 个 `.dem` + 13 个 `.analysis.json` + 若干次列目录(出网未计价)**。
dumper `get_dumper.sh` **缓存命中**(`cache key 46fe9c6a2b084f9b`,未重建)。
⛔ `bots/` + `game/` **一行未改**,零新 soak id。
