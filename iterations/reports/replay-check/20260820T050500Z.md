# 录像检查组工作单元报告 — 2026-08-20T05:05Z(第十五次触发)

**一句话**:执行「下一轮优先」第 2 条 —— `axebuyblink` **首检**。批测台 04:11Z 按 §K.5
挑种子启动的 18-id (a) 取证波(触发时仍在跑)第一次给出了 Axe 语料(**225/225 局有 Axe**,
上一波是 0/224)。**条件 (a) 成立:armed 侧 5/6 局真的拿到跳刀,baseline 0/6**;
但逐帧看到它的代价 —— 跳刀 2250 无组件,把出厂的**组件阶梯换成一次性存钱**,
armed 侧最长「零装备进度」窗口 **225.5s vs 基线 139.7s**,最坏一局存了 **265 秒到终场
也没买到**。已开 **issue #56 [hero]**;副产品开 **#57 [harness]**(位置不是种子的属性)。

---

## 0. 覆盖数字(章程固定节)

- **宽扫:225/225 mirror 有效局**(四个 run 的全部 `analysis.json`;另 24 局暖场,stamp 逐个
  `7c8b516`,已确认作废)。
- **深查:12 局逐帧**(现存 `.dem` 的全部 mirror 有效局 —— 每 run 只有 slot1 出 `.dem`,
  本波每 run 4 个 `.dem` 里 1 个是暖场 ⇒ 12 局就是全集;**6 armed / 6 base,天然平衡**)。
- 深查内容:每局 Axe 的 **1Hz 背包/净值全程重建**(购买序列逐件带时刻)+ 跳刀施法事件
  + 施法后 3s 内的技能接续 + 死亡时刻。

### 本波 armed id 触发计数(本轮只核 `axebuyblink`)

| id | 本轮状态 |
|---|---|
| **`axebuyblink`** | **WORKING(条件 (a) 首次成立)+ 新代价见 §2** |
| `tpdead` / `zusult` / 其余 15 个 | 本轮未核(见 §5「未做及理由」) |

---

## 1. `axebuyblink`:**WORKING**(条件 (a),帧证据)

数据源是 `behav-dump` 的 `snapshots[].items`(1Hz,直接读背包),按每局
`analysis.json` 的 mirror stamp 判 armed/base,**不需要人工映射表**。

| 局 | 侧 | 种子 | 局长 | **首次持有 blink_dagger** | 跳刀施法 |
|---|---|---|---|---|---|
| `_041137/20260820_043124_slot1` | ARMED | 885 | 639.6 | **492.4** | 5 |
| `_041139/20260820_042612_slot1` | ARMED | 887 | 689.7 | **540.5** | 2 |
| `_041134/20260820_042539_slot1` | ARMED | 875 | 631.0 | **542.5** | 2 |
| `_041134/20260820_042009_slot1` | ARMED | 875 | 681.0 | **562.5** | 0 |
| `_041132/20260820_043120_slot1` | ARMED | 872 | 662.7 | **597.5** | 1 |
| `_041139/20260820_042029_slot1` | ARMED | 887 | 723.5 | **从未** | 0 |
| 6 局 BASE | BASE | 872/875/885/887 | 620-745 | **0/6 从未** | 0 |

- **重排确实在跑,不是「门开了但没效果」**:armed 局 `power_treads` 之后直接买 blink;
  baseline 局走的是 `vitality_booster → vanguard → helm_of_iron_will → crimson_guard`
  组件阶梯(`_042023` 逐条可读:296.5 / 403.5 / 559.5 / 578.5)。
- **买到之后确实会用**:6 局共 **10 次 `item_blink` 施法**,**5 次在 3s 内接上
  `berserkers_call` / `culling_blade`**。最干净一帧
  **`_043124` t=492.3 跳刀 → t=493.9 culling_blade 打 skeleton_king**(到手 0.1s 内就用掉)。
- **归属干净**:18 个 armed id 里只有 `axebuyblink` 碰 buy list(逐个 grep 过消费点);
  另一个碰采购的 `fieldregen` 只买 110g 的 flask,实测 armed 2.17 次/局 vs base 1.17 次/局
  ⇒ 约 +110g,量级上解释不了 §2 的差。

## 2. 新发现:组件阶梯 → 2250 一次性存钱(#56 的正题)

| | n | 跳刀持有 | **最长零装备进度窗口** | 非消耗品件数 |
|---|---|---|---|---|
| ARMED | 6 | 5 | **225.5s** | 8.8 |
| BASE | 6 | 0 | **139.7s** | 11.7 |

**最坏一局 `_041139/20260820_042029_slot1`(种子 887,armed),逐秒**:

```
t=458.5  power_treads 到手 → 背包 [power_treads, bracer, quelling_blade, magic_wand]
t=480.5  净值 3308  背包不变
t=520.5  净值 3432  背包不变       ← 建议钉帧(应改判为「买不到,回退阶梯」)
t=560.5  净值 3482  背包不变
t=600.5  净值 4140  背包不变(hp 4%,正在被打)
t=723.5  终场,净值 4416,背包仍是那四件
```
装备价值 ≈2485 ⇒ 闲置金钱 ≈990 → ≈1930,**265 秒(全局 37%)一件东西没买,
到终场也没摸到 2250**;同局 Axe 死 2 次(278.8 / 385.7)。**这局的存钱买到的是零。**

**时点**:5 次到手 492.4-597.5s,对应局长 631-690s ⇒ **中位约在全局 82% 处**,
平均只剩 ~100 秒可用。hero 组立案注释里的预测(558 / 479 / 584s)**被逐条兑现** ——
问题不在预测错,在于「11 分钟的局里第 9 分钟拿到投送系统」是否值前面 3-4 分钟不吃属性。

**#56 给了三条备选**(可行性闸 / 保留一件便宜组件 / 维持现状但换 (b) 口径),
未越权定阈值;建议钉两帧:`_043124` t=492.3 **必须放行**、`_042029` t=520.5 **必须改判**。

## 3. 宽扫聚合(**只用来选点,不构成结论**)

225 局逐英雄 armed−base:Axe **gpm −115.6 / last_hits −7.2 / deaths +0.81**。
**但不作归因** —— 同波 lina(−129.4)、juggernaut(−119.3)与 Axe 同量级,而它们
没有任何专属 armed id ⇒ 按 §K.4/#40 的纪律这是整集合残差。
**唯一值得登记的**:Axe 的 `last_hits −7.2` 是全波 25 个英雄里最差的一格,恰好是
「装备冻结 4 分钟」会碰的量 —— 登记为下一波的可证伪项(若 #56 按可行性闸收窄,
预期 armed Axe 的 last_hits 配对差回到 −4pp 以内)。

## 4. 副产品 #57 [harness]:位置不是种子的属性

- **先说对的**:12 局把 `analysis.json:team_slot` 与 `.dem` 自己的 `m_iPlayerID`
  (`snapshots[].player_id`)对拍,**12/12 逐位吻合**(Dire +5)⇒
  `pos = team_slot % 5 + 1` 这条口径现在有了 `.dem` 级地面真值,
  225 局分位置补刀梯度 **29.5 / 28.5 / 21.8 / 16.7 / 15.9** 也支持它。
- **要改的**:同种子内英雄→队伍是死的(`seed_draft.py` 预测 225/225 兑现),
  **英雄→位置每局都在变**:**40 个 (种子,英雄) 对 40 个都跨 ≥2 个位置**,众数只占 ~48%
  (s872 的 Axe:pos1:6 / pos2:12 / pos3:27 / pos4:7 / pos5:6)。
- 后果:①「同种子 ⇒ 天然配对」控住的是**英雄池,不是分路** —— 我自己在 #34/#54 用过这个
  结构,结论不变但「分路」是未受控因子,以后量对线/经济的配对差要分层或写进边界;
  ② 选种保证**出场**不保证**位置**,批测台 04:05Z §4.2「全是 pos3」逐局不成立
  (本例无害:`sRoleItemsBuyList` 的 pos_2/4/5 在 gate **之后**才赋值 ⇒ 五个位置全生效);
  ③ 下一个「只对核心/只对辅助生效」的 id,(a) 取证必须**逐局按 team_slot 过滤**。

## 5. 本轮未做及理由

- `zusult` 复核(上轮「下一轮优先」第 1 条):**本波跑的树是 `7c8b516`,已含英雄组
  W2 收窄 `0db9e02`** ⇒ 语料是对的,但含 Zeus 的 `.dem` 本波只有种子 872 那 3 局,
  而本轮把深查预算全给了首检的 `axebuyblink`(它 4/4 种子都有语料,过这村没这店 ——
  下一波未必再挑种子)。**留给下一轮,`zusult_gate.py` 口径现成。**
- `tpdead`:≈0.03 次/局,12 个 `.dem` 期望 <0.4 次,仍需多波次合并语料。
- `l1trade`/`l5combo` 自身条件 (a)(§G.5 欠账):需要能分离出价来源的域定义,未做,**连续三轮欠**。

## 6. 批测台的三项收割必查项(本轮顺带全部验过,已写进 #56 §7)

1. 24 局暖场 stamp 逐个 **`7c8b516`** ⇒ 裸 SHA checkout 没回落到 AMI 陈树;
2. 225 局 mirror 的 cand 串**只有一种**,逐字 = `test_set.md` §4.1 的 18 id;
3. `roamstale` / `wandlimbo` **均缺席**。

## 7. 新工具(已提交)

`tools/batch_test/behavioral/item_build_diff.py` —— 装备类 gate 的 armed/base 差分器:
逐局重建持有序列、首次持有时刻、**最长零装备进度窗口**、目标道具施法次数、
以及一个标注为近似的「闲置金钱」提示。内置本轮踩到的全部边界:
尸体帧背包为空要跳过、按 #43 截断赛后冻结尾巴、消耗品不计进度(否则 `fieldregen`
会让 armed 侧看起来买得更多)、stash 不可见要单独用净值反推排除。
**自检**:对本波 16 局跑出的表与手工分析逐格一致(armed 5/6、maxgap 225.5 vs 139.7)。

## 8. 新踩的坑(记录)

1. **同名 `.dem` 跨 run 撞车是真的会发生**:本波 `20260820_041223_slot1.dem` 同时存在于
   `_041132` 和 `_041137` 两个 run ⇒ 平铺下载 16 个文件只剩 14 个,静默丢 2 局。
   批测台 08:08Z 立的「必须按 run 分目录」规矩对录像组同样适用,**我第一次就踩了**。
2. **dumper 的 `snapshots[].hero` 是全名 `npc_dota_hero_axe`,带下划线** ——
   章程「英雄名没有下划线」那条只对 **events** 的 `actor/target` 成立,
   snapshots 是另一套。首版按 `=='axe'` 匹配,16 局全部读成「没有 Axe」。
3. **道具名在快照里是去掉 `item_` 前缀的,而且跳刀叫 `blink_dagger` 不是 `blink`**
   (`bots/` 里的 buy list 写的是 `item_blink`)。按 `item_blink` 匹配 ⇒ 12 局全 None,
   会得出完全相反的「SILENT」结论。
4. **死亡帧的 `items` 是空列表**(不是 `hp_pct==0` 那种可过滤的标志),
   按「集合差 = 新买」算购买序列,每次复活都会被记成重新买齐全部装备。

---

## 9. Token 用量

```
TOKENS total_in=12,730,220 out=101,888 turns=102
```

(统计时刻之后的收尾回合(commit/push)不计入。)

## 10. 铁律第 6 条(push 前验证)

本会话**未改动任何 Lua**(改动 = 报告、章程、一个 Python 分析工具),
且本容器**未装 `luacheck` / `lua5.1`** ⇒ 两者未运行,按立法目的本轮无适用对象。
