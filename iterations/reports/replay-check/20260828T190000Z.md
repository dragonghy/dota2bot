# 录像检查组 2026-08-28T19:00Z —— W22 首检:三波买不到的域这次打开了,而打开它的那局里躺着第二处同族缺陷

**一句话**:`seed 975` 进了 W22,`spirit_breaker` **三波以来第一次真的在场**(6/6 局),
`aimguard` 的消费点**第一次被观测到执行**(14 次,逐次归属到位);
但判词仍买不到 —— 这个 id 构造上单向,**只能在 armed 对 baseline 的缺口里显形**,而 dire 腿此刻尚未开始。
顺带在同一个函数里撞到 **GH #283**:一处**没有 gate、没有 id、是出厂默认行为**的跨种守卫,
实测「进入即取不到目标」,在 6 局里**一次充能也没打出来**。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` — **worst exit 3(非阻断,唯一那处不是本组的)**:

- `stable-v1` / `stable-v2` 两个锚点:`EXISTS` / `PINNED` / `SHIPPED` **全 ok**。
- `unlanded_commits.py`:**无**推了没落地的树。
- cadence:1 处洞 —— **`strategy` 08-28T01:24Z → 10:35Z,9.2h**(与前几轮逐位同值,同一个洞被反复点名);
  **`replay-check` 连续第四轮未被点名**。
- 未裁队列:RIDESHARE **none** / OTHER **none**;总开放请求 **37**。
- trunk 健康:python **50 passed / 0 failed / 0 uncertifiable**;快 Lua 检测器 **17 个文件 0 失败**
  (**FAST SUBSET,不是全套**)。
- 退出码来源工具自报(#267):`FINDINGS (exit 3) = cadence`、`UNCERTIFIABLE (exit 2) = none`。

## 1. 覆盖数字(章程固定节)

| 层 | 数字 |
|---|---|
| **宽扫 A(stamp 普查)** | **W22 全波 64/64 份 `analysis.json` = 100%**(四个 run 各 6 暖场 + 10 stamped) |
| **宽扫 B(行为)** | **`af08aa`(seed 975)6/6 局**,`skipped 6 暖场`,**`unparseable=0`**,`exit_code 0` |
| **深查(逐帧)** | **6 局全部**:162 次充能逐次读目标;**14 个野怪充能瞬间逐次归属**;3 s 网格 creep 流 **3133 帧**重建;SB 实体流逐流核 |
| 实体层普查 | 6 局 × 10 个英雄名,与离线 draft 逐个相同 |

⚠️ **首检不是终检**:读数止于发波(18:16Z)后 **+22 min**,**40 个 stamped 局全部 `:radiant`**。
按批测台 §4 的 ~35–40 min 换腿点,**dire 腿此刻尚未开始,不是「没有 dire 腿」**
(W21 首检写过同一句话,并被那一波的终态证实)。

**arm 串三方核对**:`test_set.md` 第 2 行 = 局内 stamp = **43 id / 381 字节 / sha1 `0a601389`**,`IDENTICAL`。
seed→run 映射:**`975 = af08aa`**、`976 = daf92f`、`977 = 80d801`、`978 = bc7f45`。

## 2. ⭐⭐⭐ `aimguard`:域打开了,站点执行了,判词仍差一条腿

### 2.1 域(三波以来第一次)

上一轮点名 `seed 975` 是唯一解锁物,批测台 W22 采纳了它。
**实体层证实(不拿 draft 名单当证据)**:`af08aa` 六局逐 snapshot 建实体名集合,
**`spirit_breaker` 6/6 局在场**,十个名字与离线 draft 逐个相同。
离线复核 `seed_draft.py --assert-carrier spirit_breaker,obsidian_destroyer 975` → **exit 0**
(`spirit_breaker` radiant pos4 / `obsidian_destroyer` dire pos2)。

⇒ **W19 未入集 / W20 0/180 / W21 0/185 的「域缺失」到此结束。**

### 2.2 站点被走到了:14 次,逐次归属

6 局里 SB 共 **162 次** `spirit_breaker_charge_of_darkness`,打在**野怪**上的 **14 次**。
全仓只有两处返回野怪当充能目标:

- **B** = `hero_spirit_breaker.lua:299` `nNeutralCreeps[2]`(`aimguard` 的站点)
- **C** = `:309` `nNeutralCreeps[3]`(**另一处缺陷,见 §3**)

C 要打出非 nil 目标,必须**同一瞬间** `#nEnemyLaneCreeps >= 3`。逐次核算这 14 个瞬间的
「SB 周围 R 内敌方兵线小兵数 / 野怪数」:**14/14 在 R=600 内敌方兵线小兵数为 0**
⇒ **C 在每个半径上被排除,14 次全部归 B。**

其中 3 次目标是 `ancient_frog`、周围 R=600 内野怪 **22–24** 只 —— 正对上
`#nNeutralCreeps >= 2 and nNeutralCreeps[1]:IsAncientCreep()` 那条析取支。

⇒ **这是 `aimguard` 立案(GH #262)以来,它的消费点第一次被观测到真的执行。**

### 2.3 但判词买不到,原因是结构性的,不是语料不够

`J.CanBeAttackedPair` 按 GH #262 的设计**构造上单向**:armed 只能在出厂谓词上**多加一个合取**
(`J.CanBeAttacked(hTarget)`),只能**扣下**一次本会发生的充能,**永远不会造出新的**。
⇒ 它的效果在读数上**只能表现为 armed 腿相对 baseline 腿的缺口**,单腿上不可见。
SB 站 **radiant**,而本轮 40 个 stamped 局全 `:radiant` ⇒ 现有 6 局**全是 armed 腿**。

**核验状态:`aimguard` = 域已 present / 站点已执行(14 次,armed 腿)/ 判词 pending baseline 腿。**
**既不是 WORKING,也不再是 SILENT。**14 次是**真实域的下界**,
**不能**用来说「armed 的那个合取没扣下任何东西」。

### 2.4 预登记(下一轮唯一可被证伪的读数)

W22 dire 腿落地后,seed 975 的 dire-armed 局里 SB 是 **baseline** 腿。按 §2.3 的单向性:

> **baseline 腿的「野怪充能次数 / 局」应 ≥ armed 腿。**
> 若 baseline **低于** armed,单向性不成立 ⇒ **先审读法(归属或分母),再审 id**。

按铁律 4(i) ab / ba 两分层各自给读数;按 4(ii) 这是**整数小值域计数** ⇒
**只报均值 + 每局分布,不报中位数**。

## 3. ⭐⭐⭐ 新开 [bug] GH #283:同一个函数里的第二处同族缺陷,**没有 gate,是出厂行为**

`bots/BotLib/hero_spirit_breaker.lua:302-309`,紧接 `aimguard` 那个 `if` 的 `end`(:300)之后:

```lua
local nEnemyLaneCreeps = bot:GetNearbyLaneCreeps(nRadius, true)
if nEnemyLaneCreeps ~= nil and #nEnemyLaneCreeps >= 3 and nLocationAoE.count >= 3
then
    if J.CanBeAttacked(nEnemyLaneCreeps[1])
    then
        return BOT_ACTION_DESIRE_HIGH, nNeutralCreeps[3]   -- :309
    end
end
```

三条:(i) **跨种守卫** —— 问的是兵线小兵,返回的是野怪(**比 #262 那处更远**:#262 至少是同一张表的兄弟);
(ii) **`#nNeutralCreeps` 在这条路径上不受任何约束**(管张数的 `if` 在 :300 就闭合了)⇒ 野怪 <3 时目标是 **nil**;
(iii) **`nNeutralCreeps` 本身可能是 nil**(:288 的 `~= nil` 说明作者知道),:309 无保护索引 ⇒ Lua 错误,
而引擎错误处理器是坏的 ⇒ **完全静默**。

**帧证据一:两个条件在地图几何上近乎互斥。** 6 局按 3 s 网格重建
(`a/b` = 满足「lane ≥3 **且**野怪 <3」/ 满足「lane ≥3」):

| R | 250 | 300 | 400 | 500 | 600 |
|---|---|---|---|---|---|
| 合计 | **163/163** | **236/236** | **317/317** | **409/410** | **492/495** |

⇒ **只要 :303 的 `#nEnemyLaneCreeps >= 3` 成立,`nNeutralCreeps[3]` 就几乎必然是 nil**(R≤400 是 100%)。
兵线小兵挤在线上、野怪待在野区,两者不在同一个几百单位的圆里。
**不是「偶尔踩到边界」,是「这条分支只要被走到就取不到目标」。**
`bash_radius` 是引擎 special value,离线读不到 ⇒ **不猜常数**,给跨半径的形状;每个半径同号。

**帧证据二:该分支在 6 局里一次也没打出充能**(§2.2 的 14/14 全归 B)。
即 C 的可读合取项进入了 163–492 次,**产出 0 次充能**。

**代价不止是没打出充能**:`X.SkillsComplement:114-126` 里 desire 是 `DESIRE_HIGH`(不是 0)⇒
可能**白交一次 Bulldoze**(有 CD 的自身增益)、充能落空、然后 **`return` 把这一帧的
`ConsiderBulldoze` / `ConsiderNetherStrike` / `ConsiderPlanarPocket` 全部跳过**。**净损失,没有正收益的一侧。**

建议:`nNeutralCreeps[3]` 几乎肯定是**笔误**,原意应是 `nEnemyLaneCreeps[3]`;
但按铁律 2/3 它是**行为改动**,要走 gated 流程(建议新 id `sbwave`),**不许顺手改**。
fixture 建议钉 §3 表里 R=250 下 lane≥3 且野怪=0 的那一族帧。
⚠️ **语料保质期**:`s3://…/dem21/spot_20260828_182047_1_2f20fe82…_af08aa/`,
`dem21/` 有 **21 天过期规则**(`dem21-expire-21d`)⇒ **约 2026-09-18 前取**。

## 4. 工具:三个脚本**入库**(不留 scratchpad)

章程里「脚本留 scratchpad 未入库」已被登记过两轮(#263 那次同类教训),本轮不再重复:

| 文件 | 作用 |
|---|---|
| `tools/batch_test/behavioral/sb_charge_target_domain.py` | :309 nil-target 的**必要条件域**,跨半径 |
| `tools/batch_test/behavioral/sb_neutral_charge_attrib.py` | 野怪充能的 **B / C 分支逐次归属** |
| `tools/batch_test/behavioral/sb_bulldoze_orphan.py` | **测过、判为不可判别、登记留证**(见下) |

⭐ **一条被否掉的读法,写进代码而不是只写进报告**:Bulldoze「前置但无后续充能」的孤儿信号
本轮实测 **5 孤儿 / 1 配对**,看起来很像 nil 目标的签名 —— 但 `X.ConsiderBulldoze()` **自己也会放 Bulldoze**,
帧表里没有任何量能说一次施法来自哪个调用点 ⇒ **高孤儿率在两种世界里都是预期读数,不可判别**。
该读数**已丢弃,GH #283 不依赖它**。脚本入库时在文件头写了 `LIMITS` 横幅,**防止下一轮把这个数捡回去当证据**。

三个脚本都守了章程的踩坑纪律:实体流按 `(hero, idx)` 建键、只留开门前(t<0)采样过的流(GH #176 ——
`182234_slot7` 的 `spirit_breaker` 确实有 **3 条同名流**);生死用**两端 bracketing 采样**,不用插值 `hp_pct`;
冷却读**施法前最后一帧**,不用上升沿。

## 5. 诚实边界

- **本轮没有一条结论是 armed−baseline 聚合差分**(单层语料)。§3 之所以单腿可判,是因为
  **C 是出厂默认行为,两条腿上完全相同**;§2.2 的归属只用「lane 小兵数」一个可读量,
  不吃 `IsFarming`/`IsAttacking`/`nLocationAoE` 三个离线读不到的合取项 —— 那三项对 B 和 C **同样**是前提,
  归属只在 B 与 C 之间做二选一。
- §3 的域表是**必要条件域**,是真实触发集的**严格超集**:它**能证伪**(为 0 就没事),**不能证实频率**。
- armed 腿 = **43 id 同时开** ⇒ 不报任何聚合差值。
- **GH #265 的四量同形预登记连续第五轮不执行**:本轮无 ba 层,且 **#272 仍未修**。
- **W20 两粒(947/974)的 `miss`/`step` 本轮仍未回算** —— 上一轮登记的欠账。
  本轮查清了回算的**可行性**:W20 的 `.dem` **不在 `soak/` 下**(那里 0 份),而在
  **`dem21/spot_20260828_0616{32,45}_…_{f48af2,60e52a}/` 各 44 份**(seed 947 = `f48af2`、974 = `60e52a`,
  由 stamp 普查确定)。`dem21/` **21 天过期** ⇒ 回算窗口到 **约 2026-09-18**,不急但别忘。
  本轮把这一项让位给 `aimguard`,依据是上一轮章程原文:「**若 `seed 975` 已进波,则 `aimguard` 的 (a) 第一次可买,优先做它**」。
- 报告节奏 15:46Z → 19:00Z = **3.2h,无洞**。

## 6. 交棒

- **GH #283 新开 [bug]**(分派 **[hero] 英雄组**;gate/入集由总监裁)。
- **GH #276 追评**:载体门这一粒**正面兑现**;但 `spirit_breaker` 是**被点名塞进来的**,
  不是被 terms 机械推出来的 —— 两处推导陷阱仍待落实。
- **GH #262 追评**:域打开 + 站点 14 次 + §2.4 预登记。
- **⚠️ 给批测台**:W22 dire 腿请照常跑满;`seed 975` **一粒同时解锁两笔欠账** ——
  `aimguard` 的第一个载体,**以及** `odaoe` 缺的那条反证腿(975 把 OD 放 **dire**,
  此前三粒带 OD 的种子全落 radiant,是 p≈0.125 的巧合)。
- **⚠️ 给总监**:(1) `aimguard` **不再是 SILENT**,但**也不是 promote 的材料** —— 判词要等 baseline 腿;
  (2) **#272 请排在 #270 之前**(连续第五轮,仍是 #265 的唯一阻塞项);
  (3) GH #283 是**出厂缺陷**,不在任何测试集里,**不占 armed 名额**。
- **球在总监**:#283、#276、#272、#270、#265、#264、#263、#262、#261、#259、#258、#257、#253、#250、
  #249、#247、#246、#245、#244、#241、#236、#235、#159、#207、#212、#220、`campfarm`;
  `bbfight`/`bbshort` 与 `abilanc` 的三条件判定。
  **本组不裁 promote/reject、不申请波次、不改 bot Lua、不花 AWS 钱。**

## 7. 验证

- `bots/` / `game/` **0 改动**(本轮只新增 3 个 `tools/batch_test/behavioral/*.py`)。
- `bash tools/agent/luacheck_gate.sh` → **exit 0**,`luacheck bots game: 0 warnings`
  (容器里没有 luacheck,脚本自装 `lua-check` 后跑)。**未使用 `RULE6_BYPASS`**。
- `tools/agent/arm_push_gate.sh` → `core.hooksPath=.githooks`,已上膛。
- 本轮**未改任何 Lua** ⇒ **不声称跑绿过 Lua 全量**(GH #124)。
- **AWS**:S3 **只读**(64 份 `analysis.json` + 12 份 `.dem`),**未启动/未终止实例**,**未调用 CE**,**零支出**。

## 8. 下一轮第一件事

1. ⭐⭐⭐ **W22 终检** —— 先跑 stamp 普查确认 dire 腿落地;两腿都在就**当场执行 §2.4 的预登记**
   (seed 975 的 dire-armed 局里 SB 是 baseline 腿)。**这是本轮唯一能被证伪的预登记。**
2. ⭐⭐ 同一波里把 **`odaoe` 的侧别反证**做掉(975 把 OD 放 dire,armed/baseline 与 W20/W21 三粒**反向**)。
3. ⭐ **W20 两粒(947/974)的 `miss`/`step` 回算** —— 语料位置已查清(§5),窗口到 ~09-18。
4. ⭐ GH #283 的 fixture(R=250 下 lane≥3 且野怪=0 的帧);#265 的两条 fixture;
   #236 裁下后 `mv` 落地 #159 的 fixture(**第十四轮**);GH #262 的 fixture(**第三轮**);
   GH #259(**第五轮**);GH #250 的出厂帧(**第六轮**);`pullthink` 英雄分层表(**第六轮**);
   ab/ba 回灌 `fieldbuy_silence.py`/`stayfield2_margin.py`(**第三十四轮**);
   `l5combo` 的 (a)(**第六十四轮**);`axebuyblink` armed 的波次。

## 9. Token 用量

```
TOKENS total_in=8,371,247 out=56,126 turns=71
```
(到统计时刻为止;统计后的收尾回合不计入。)
