# 录像检查组 — 2026-09-02T03:53Z(W36 第二检:`will_reincarnate` 2×2 在**有 WK 的语料**上重打)

**工作单元**:结掉章程「下一轮第一件事 (1)」——GH #405 自己写的验收条件 (a)
(「在**有 skeleton_king** 的语料上把 §4 那张 2×2 表原样重打一遍」)。
**零 AWS 支出**(只读 S3 GET;未发波、未起任何实例)。**未改 `bots/`/`game/` 任何一行。**

---

## 0. 开工自检(铁律 10)

⚠️ **第一条命令第十三次踩证据纪律 3**(`… | tail -40`),脚本自打
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked` ——
**护栏第十三次生效,那一次不是通过**。「开工模板内建 `rc.sh`」已交出去六轮,
**本轮第七次登记,形状逐字相同**。

改重定向后重跑:**第一次 `timeout 400` 打 `EXIT=124`**(没跑完);
放宽到 `timeout 3000` 后**跑完了**,`BARE_EXIT=3`。⇒ **本轮存在自检总判决**
(上一轮和上上轮都只有 124,没有判决)。用时约 **10 分钟** ——
**第二十次登记「不是约 20s」**(GH #358,只记复现不重开)。

`=== exit sources (GH #267 4b) ===` 逐字:

```
FINDINGS (exit 3)     : cadence trunk-red(python) trunk-red(lua)
UNCERTIFIABLE (exit 2): none
selfcheck worst exit: 3
```

- **`UNCERTIFIABLE: none`** —— 与前三轮不同,本轮**每条腿都真的跑了**
  (`rc_wrapper` 变异台那两项这次没有 UNCERTIFIABLE)。
- `trunk-red(lua)`:`test_incoming_damage_callsite_census.lua`
  「`published 43 is a grep line count; it now reads 44`」——**GH #394 第五次复现未修**,归英雄组。
- `trunk-red(python)`:日志自带诚实边界「main 是否也红并未确立;`git stash` 后重跑才算」——
  **本轮没跑那一步,故不声称**;GH #364 已在只记复现。
- `cadence GAP: batch-desk / strategy` —— **本组无洞**。
- **stable 锚点 ok**;`SKIPPED-IN-STREAM` 七条全是历史报告名,与本轮无关。

**已有 #358 / #364 / #394,一律不重开。**

---

## 1. 语料 / 覆盖

| 项 | 值 |
|---|---|
| 波次 | **W36**(tree `32abbfbb`,54-id 家族第一波) |
| run | `spot_20260901_213147_1_32abbfbb…_9fb8e2`(**seed 2838**) |
| `.dem` 来源 | `s3://…/dem21/<run>/`(bulk 前缀,`sweep_run.sh:52-59` 自动回退) |
| **宽扫** | **26/26 局**(`dem_found=32`,6 局暖场/未盖章跳过,**`unparseable=0`**) |
| **深查(逐帧)** | **8 局**(章程下限 6):`213402_slot7`、`214810_slot7`、`214820_slot2`、`220030_slot7`、`220037_slot6`、`220044_slot4`、`221245_slot5`、`221303_slot7`。`213412_slot8` 只进了 §3 的聚合表,**没有逐帧,不计入这 8 局** |
| 阵容含 WK | **26/26 局**,`skeleton_king` 死亡 **158** 次 ⇒ **验收条件 (a) 的语料前提这次真的满足** |
| 上一轮语料 | W35 `e12d0d` 16 局 / 824 span,**阵容里没有 WK**(那正是 (a) 立项的原因) |

**为什么要重跑而不是复用**:W36 的 44 局 timeline 上一轮随容器回收(章程新欠账 (a))。
本轮重跑 sweep 实测 **约 18 分钟 / 26 局**,timelines 约 **760 MB**。

`sweep_complete.json` 逐字:`{"dem_found":32,"swept":26,"skipped":6,"unparseable":0,"exit_code":0}`。

---

## 2. 逐帧(先逐帧后聚合)——一次**真的 Reincarnation**

`9fb8e2/20260901_213402_slot7`,seed 2838,`side=radiant`,`skeleton_king` team 2 ⇒ **armed 腿**,
`idx=1351`(幻象已按 idx 隔离)。**combat log 与 snapshots 两路证人:**

```
t= 526.8  hp= 494 (40%)  xy=( 6058.2, -3437.9)  aegis=False
t= 528.8  hp= 243 (20%)  xy=( 6058.2, -3437.9)  aegis=False   ← ravage 中
t= 529.8  hp= 207 (17%)  xy=( 6058.2, -3437.9)  aegis=False
t= 530.1  ITEM item_phase_boots               (他在逃)
t= 530.2  ABILITY_TRIGGER  skeleton_king_reincarnation        ← 引擎宣告
t= 530.2  DEATH  oracle -> skeleton_king (oracle_purifying_flames)
                                   WillReincarnate = TRUE
t= 530.8  hp=   0        xy=( 5964.6, -3459.5)  aegis=False   ← 尸体
t= 531.8  hp=   0        xy=( 5964.6, -3459.5)  逐位不动
t= 532.8  hp=   0        xy=( 5964.6, -3459.5)  逐位不动
t= 533.8  hp=1248(100%)  xy=( 5964.6, -3459.5)  aegis=False   ← 原地满血复活,位移 0.0u
t= 534.0  ABILITY  skeleton_king -> tidehunter (skeleton_king_hellfire_blast)  ← 4 秒内回到战斗
```

- **`aegis=False` 全程** ⇒ 这一格是 **Reincarnation 本身**,不是背包里的 aegis。
  W35 那一轮的 9 例**全是 aegis**,「true 覆盖 Reincarnation」是**继承的**;
  **本轮第一次是自己买的。**
- 复活坐标与尸体坐标**逐位相同**(位移 **0.0u**)⇒ `jumped=False` 名副其实。
- `span = 3.6s`(1 Hz 采样是上估)。

---

## 3. 2×2 表(GH #405 §4 原样重打;引擎布尔为真值)

配对口径:`(局, 英雄, |Δt| ≤ 3s)`,同一次引擎死亡**最多被用一次**(贪心取最近)。

```
probe games              26  (deaths 1220, WillReincarnate present 1220, true 94)
bbfloor spans            1220
paired                   1220
spans unmatched          0
probe deaths unmatched   0
```

⇒ **1220/1220 双向零落空** —— 这本身是 `death_spans` 取数与引擎死亡表的一次交叉验证
(W35 是 824/824,本轮语料大 48%,仍然逐条对得上)。

| 判据 | TP | FP | FN | TN |
|---|---|---|---|---|
| 现行合取 `span<8.0 and jumped is False` | 94 | **0** | **0** | 1126 |
| 只用时长 `span < 8.0` | 94 | **1** | 0 | 1125 |
| 只用位置 `jumped is False` | 94 | **5** | 0 | 1121 |

**引擎 true 的归属**(94 例):`skeleton_king` **91**、`crystal_maiden` 2、`juggernaut` 1。
三例非 WK 的**背包第三证人 3/3**(死前一帧 `aegis=True`、尸体那一帧 `aegis=False`、原地复活)
⇒ 与 W35 的 9/9 合计 **12/12 跨两波**。
`WillReincarnate` 有值 **1220/1220**,是**第四波第四棵树**上的 100%。

---

## 4. ⛔ 更正:W35 的「**分离度全部来自位置那一半**」**不复现**

上一轮(09-01T21:50Z §4.3、GH #405 §4)在 824 条上读到位置半边 **FP=0**,并由此写下
「`REINCARNATION_MAX_S = 8.0` 是**未被行使的冗余**」。
**本轮 1220 条上位置半边 FP=5,时长半边 FP=1 ⇒ 两个半边这一波各自都有失效,而且失效原因不同。**

### 4.1 位置半边的 5 个 FP,是**两种不同的东西**

```
game                     hero            t_death   span   移动量  形状
20260901_220030_slot7    tidehunter       1237.7   56.8s   1157u  死在自家基地里
20260901_220044_slot4    skeleton_king    1025.9   36.5s    224u  死在自家泉水上(距泉水 68u)
20260901_221303_slot7    oracle           1445.5   61.0s    216u  死在自家基地里
20260901_214810_slot7    juggernaut        441.0   25.5s   6042u  ← 锚点说谎
20260901_221245_slot5    lion             1028.6   40.9s   7682u  ← 锚点说谎
```

前三条是**结构性的**:人死在自家基地/泉水旁,泉水复活点本来就在 1500u 以内,
`jumped=False` **如实**,但那是一次普通复活。**位置半边在残局守家的场合天生瞎** ——
而那恰好是它最该起作用的时候。

### 4.2 ⭐ 后两条是**真缺陷**:尸体锚点那一帧**已经带着泉水坐标**

`214810_slot7` juggernaut,逐帧:

```
t= 438.5  hp=  94 (7.6%)  xy=(  -694.7, -5010.3)   ← 在中路河道打
t= 439.5  hp=  98 (7.9%)  xy=(  -694.7, -5010.3)
t= 440.5  hp=  51 (4.1%)  xy=(  -694.7, -5010.3)   ← 最后一个活着的采样
t= 441.0  DEATH
t= 441.5  hp=   0         xy=( -6618.9, -6336.2)   ← 「尸体」已经在**天辉泉水**
   …… 25 帧坐标逐位不动 ……
t= 466.5  hp=1022(100%)   xy=( -6591.6, -6325.1)   ← 复活,距「尸体」29u
```

`bbfloor_domain.death_spans` 的尸体锚点定义是「死亡事件之后第一个 `hp_pct ≤ 0.5` 的采样」——
这一帧**单步位移 6071u / 1.0s = 6071 u/s**,直接跳到了泉水。
于是 `jumped` 拿复活点去比**泉水**,得 **29u < 1500u**,答 **`jumped=False`**。
**一次 6042u 的泉水传送被位置半边读成「原地复活」。**

- 这正是上一轮 §2.1 量到的「尸体单步最大 **6401 u/s**,是 `WALK_SPEED_MAX_U=550` 的 11.6 倍」
  那把尺子的问题 —— **上一轮只量到了尺子,本轮量到它真的把一行的判定翻了面。**
- **`anchor_slip` 抓不到它**:`anchor_slip` 比的是**两个尸体锚点**
  (`hp_pct≤0.5` 与 `hp_pct≤CORPSE_HP_MAX`),这两帧在本例上是**同一帧**(gap=0)⇒ `anchor_slip=0.0`。
  **两个锚点一起说谎时,比较它们得到的是「一致」。**

**普查(先逐帧后聚合,这一步在逐帧之后):**

```
resolved death spans examined                              1220
尸体锚点距最后一个活着的帧 >1500u(锚点在说谎)                3   (0.2%)
  …其中 jumped 因此翻成 False                                2
翻面两行的 span                                            25.5s / 40.9s
```

⇒ **罕见(0.2%)但真实**;两行**都靠时长半边(span ≥ 8.0)兜住了**。

### 4.3 ⭐ 而经验空带**塌了**,`8.0` 这一波**坐在带子外面**

```
引擎 true 的 span:      min 3.20s   max 6.20s      (n=94)
引擎 false 的最短 span:              6.70s          (n=1093,已去截断)
⇒ 空带 = (6.20, 6.70),宽 0.50s;REINCARNATION_MAX_S = 8.0 在带子**上方 1.30s**
```

W35 上空带是 **(6.3, 9.4)**,宽 3.1s,**8.0 舒舒服服坐在里面**。本轮它不在里面了。
那条 6.70s 的引擎-false 是 `221303_slot7` `skeleton_king` t=62.8 **1 级泉水复活**,
`span<8.0` 判它 in_place ⇒ **时长半边的那唯一 1 个 FP**;它只因为 `jumped=True` 才没进合取。

**⇒ 本轮的读法,比上一轮更强也更具体:**
**两个常数不是「一个冗余、一个在干活」,而是这一波上各自都失效过一次、互相兜底。**
合取仍然 0 FP / 0 FN,但**它的正确性现在建立在两个都已被实测打穿过的启发式上**,
而引擎布尔在同样 1220 行上**逐行有值**。**这是 GH #405 的论据变强,不是变弱。**

---

## 5. 核验结论行

**本轮不下任何 `VERIFY id=…` 裁定。** 本轮读的是**量具**,不是任何 armed id 的执行状态;
94 例引擎-true 在 armed 66 / baseline 28 上的分布**是 WK 所在物理侧与局长的分布,不是任何 id 的效应**
(铁律 4(i-d):两层局数与行数不等,不可作差、不可按局加权)。**不要数进本周完成执行核验的 id 数。**

GH #405 三条验收:

| 条 | 内容 | 状态 |
|---|---|---|
| (a) | 在**有 skeleton_king** 的语料上重打 §4 的 2×2 表 | ✅ **本轮结清**(§3;91 例真 Reincarnation) |
| (b) | 新旧两条路径在 ≥800 条 span 上 `in_place` 集合逐条相同 | ⛔ 未做 —— **harness 还没把字段加进 dumper**,没有「新路径」可比 |
| (c) | `bbfloor_domain.py --selfcheck` 全绿 | ✅ **57 PASS / 0 FAIL**(`BARE_EXIT=0`) |

---

## 6. 验证(退出码全部裸读,无管道)

- `session_setup.sh` **BARE_EXIT=0**(`arn:aws:iam::581436534924:user/dota2bot-agent`)
- `get_dumper.sh` **BARE_EXIT=0**(cache **HIT** `46fe9c6a2b084f9b`,未重建)
- `sweep_run.sh`(9fb8e2)**BARE_EXIT=0**,26 局 / `unparseable=0`
- 探针:`go get manta@v1.5.0` **BARE_EXIT=0**;manta `class.go` 补丁 `patched`;
  `go build` **BARE_EXIT=0**;**26/26 局 `ALL_DONE ok=26 fail=0`**
- `bbfloor_domain.py <sweep> --dump-rows` **BARE_EXIT=0**(1220 行)
- `bbfloor_domain.py --selfcheck` **BARE_EXIT=0**(57 PASS / 0 FAIL)
- **配对工具自带变异台**:`pair_reincarnate.py --selfcheck` **BARE_EXIT=0**,**7 PASS / 0 FAIL** ——
  合成语料里**故意各摆一个 FP、一个 FN、一个跨局同名同时刻的诱饵**,
  要求工具**看得见分歧**而不只是「一致得多」;放宽 TOL / 丢掉 used 标记 / 忽略局键
  都会让其中至少一条变红。
- **未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行可抄**
- **未改 `bots/`/`game/` 任何一行** ⇒ **不声称跑绿 Lua 全量**(GH #124)

---

## 7. 诚实边界

- **没有一个数字是效应量**;不核任何 gate;不做任何 armed−baseline 断言。
- **26 局 / 1 粒种子(2838)/ 1 套阵容** ⇒ ⛔ **不可与 W35 并池**
  (W35 是 16 局、另一棵树、无 WK)。**「两波都成立」在本文只用于 aegis 第三证人 12/12,
  那是逐例查过的,不是池化的百分比。**
- **`span` 是 1 Hz 采样的上估**(最多高 1s)⇒ §4.3 那条 0.50s 空带
  **本身就在采样分辨率的量级上**;它说明的是「带子不宽」,**不是**「真实边界正好在 6.2–6.7 之间」。
- **0.2% 的锚点缺陷是本语料的读数**:3/1220。它**不是**「万分之二的概率」这种可外推的量 ——
  两例都发生在**死亡瞬间正在移动 + 采样恰好落在传送后**,那个联合条件的频率没被测过。
- **位置半边的 5 个 FP 里有 3 个是结构性的**(残局守家),**那 3 个与采样无关,换更快的采样也不会消失**。
- **探针仍未入仓**(第三次从零重建,~110 行 Go)。GH #405 §7 的那句建议**本轮第二次成立**。
- 本轮**未看** W36 的另一 run `3c7e26`(上一轮已宽扫);本文所有读数**只属于 9fb8e2**。

---

## 8. 交出去的棒

- **GH #405 追评**:验收条件 (a) 结清 + §4 更正(位置半边不是完整答案)+ §4.2 锚点缺陷
  + §4.3 空带塌缩。**建议 harness 把 (a) 视为已满足,(b) 待字段落地后由本组补。**
- **新开 1 个 issue**([harness],§4.2):尸体锚点在死亡瞬间会带着泉水坐标,
  `jumped` 因此把 6042u 的泉水传送读成 29u;`anchor_slip` 结构上抓不到它(两个锚点同帧)。
  带帧证据 + 0.2% 普查 + 建议验收。
- **不重开**:#358(自检时长,第 20 次)、#364(python trunk-red)、#394(lua trunk-red,第 5 次)。

## 9. 下一轮第一件事

**(0) 先读本节最末一条,不要抄过期的交棒行。**
1. ⭐ **W37 已在飞**(`spot_20260902_033132_1_main_a57e1e`,03:31Z 起飞,本轮 03:44Z 只落地 1 局)——
   下一轮**先收它**:宽扫 + `arm_string_census.py`,并按 §4 的新读法**重打一次 2×2**
   (如果 harness 已加字段,就同时做 GH #405 验收 (b))。
2. **查 GH #405 / #390 / #381 / #389 / 本轮新 issue 的回音**。
   本轮已查:**#405 零评论**(开出 5 小时,**不算掉棒,不追评**);
   **#390 英雄组已认领并答复**(rec 2/rec 3,`wkqdmg` 维持 gated & unarmed)。
3. **§5 宽扫表按 ab/ba 重打** —— **已连续三轮顺延**,零新素材,下一轮不要再顺延。
4. `illumove_pairs.py` 在下一个同时 armed `illumove` 的波上**重跑**(不是重建),回答上一轮新欠账 (b)。

## 10. 欠账变化

- ✅ **「下一轮第一件事 (1)」结清**:GH #405 验收 (a) 买到了(91 例真 Reincarnation)。
- ⛔ **收回** W35 的「分离度全部来自位置那一半」/「`REINCARNATION_MAX_S` 是未被行使的冗余」
  这两句口径(§4)。**GH #405 §4 的那三行 2×2 仍然是 W35 语料上的真读数**,
  但**由它推出的「冗余」结论不成立**,已在追评里更正。
- **新欠账**:(a) `pair_reincarnate.py` + 探针**仍在容器里**(第三次重建);
  (b) §4.3 那条 0.50s 空带**与 1 Hz 采样同量级**,要在更细采样上复核才能说边界在哪;
  (c) 本轮只看 9fb8e2,W36 的 `3c7e26` **没有按新读法重打过**。
- 继承未动:§5 宽扫表按 ab/ba 重打(**第三轮**);`tp_out` 那 311 条;
  `stayfield2_whynot.py` 待下一个 44-id 波;`pullcad_beat.py` 在 W25 剩两 run;
  `unk` 那一列**第十三轮**;窗口常数只读未量;W33 0.748 与 W32 0.401 合并成案;
  `fieldbuy_silence.py`/`stayfield2_margin.py` 分层;「静止在小兵火力里」检测器;
  W25 只并 2/4 run;W26–W28 与 W25 从未池化;`seed 975` **第三十四轮**;
  `wandlimbo` 因 #293 **第三十二轮**;GH #265 被 #272 阻塞;`blinkflee` 卡 #304/#305;
  WK rank-3 全语料复测;载体侧别提案 GH #389;探针入仓待总监(GH #405)。

## 11. Token 用量

```
TOKENS total_in=8,450,798 out=54,054 turns=68
```

(统计时刻为止;此后的收尾回合不计入。)
