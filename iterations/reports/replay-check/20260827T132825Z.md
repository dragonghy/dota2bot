# 录像检查组 2026-08-27T13:28Z(第八十二次触发)

**一句话**:接上一轮 backlog 第 1 棒把 **`pullthink` 首检**做完了,而本轮真正值钱的不是它的读数,
是**它的验收线本身裁不了它** —— GH #186 §5 那条「戳营帧后一秒位移 &lt;50u 的占比降到 &lt;15%」有一个
**与 arm 无关的巨大本底**(baseline 腿 **57.1%**,全语料最长的零位移串就在那条腿上),
而池化后的 −14.3pp **过不了英雄分层**(N≥21 的三个英雄 +9.1 / −4.0 / −11.4pp)。
`pullthink` 判 **UNRESOLVED((a) NOT BOUGHT)**,**不是 WORKING 也不是 SILENT**。

**宽扫 337/337 计分局 = 100%**(12 个 run:W16 4 台 + W16-R 1 台 = **162 局**;
W15 4 台 + W15-R 3 台 = **175 局**)+ **深查 8 局逐帧**(章程下限 6)。
零 EC2 支出,`bots/`/`game/` **0 改动**,gate 未动。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` — **worst exit 3**(有发现,非未核验):

| 腿 | 结果 |
|---|---|
| push gate 上膛 | 已 armed |
| unlanded work | `OK: no unlanded work in the certifiable window` |
| 报告节奏 | **2 处洞**:本组 07:19Z→now **5.3h**;`strategy` 08-26T10:34Z→14:23Z **3.8h**(与前两轮同一处) |
| 未裁队列请求 | RIDESHARE **0** / OTHER **0** |
| stable-v* 锚点 | `stable-v1` / `stable-v2` **两个都 ok**(EXISTS/PINNED/SHIPPED 全绿) |
| trunk python 套件 | **42 passed / 0 failed / 0 uncertifiable** |
| trunk 快 Lua 检测器 | **12 文件 0 失败**(⚠️ FAST SUBSET) |

---

## 1. 本轮选题与语料

上一轮的下一轮优先第 (1) 条:**`pullthink` 首检** —— W16 三个首次有语料的 id 里唯一没看的那个。
W17 于 12:20Z 起飞,本轮**没有新对局**可检 ⇒ 走章程工作流第 2 条(补课)。

| | 波次 | 树 | arm 串 | 计分局 |
|---|---|---|---|---|
| 处理组 | W16(`0616*`×4)+ W16-R(`091422`) | `d9585a29` | **40 id**,含 `pullcamp` **和** `pullthink` | **162** |
| 对照组 | W15(`2114*`×4)+ W15-R(`0015*`×3) | `79f32c92` | **37 id**,含 `pullcamp`,**不含** `pullthink` | **175** |

`.sweep_out/` 在本容器里是空的(上一轮的产物不跨会话),12 个 run 全部重新宽扫,
`sweep_complete.json` 12/12,`timelines/` 合计 **337**。

### 1.1 ⭐ 为什么对照必须跨波取,不能用同波的 baseline 腿

`bot.roamCampPull` 只在 `J.ShouldPullNeutralCamp` 返回非 nil 时存在,而它开门第二句是
`if not J.IsSoakCandidate( 'pullcamp' ) then return nil end`(`jmz_func.lua:8250`)。
**baseline 腿一个 id 都没 armed ⇒ `roamCampPull` 每帧 nil ⇒ `pullthink` 两个站点都够不着**;
baseline 腿的戳营帧是**顺路打野**,不是拉野(`pullcamp_domain` 的头注早把这条记成负结果并留着)。
⇒ 同波 armed/baseline **不共享 domain**,那个对比不是效应估计。

**对照的代码同一性已核(不是假设)**:

```
git diff --quiet 79f32c92 d9585a29 -- bots/mode_roam_generic.lua   # → IDENTICAL
git diff 79f32c92 d9585a29 -- bots/FunLib/jmz_func.lua | grep '^@@'
#   → 单个 hunk:@@ -10683,6 +10683,116 @@ function J.SalveSelfMissingFloor
```

该 hunk 里 grep `PullNeutralCamp|LanePullDragTarget|IsLanePullSafe|roamCampPull|pullcamp|pulldrag|pullcad|pulllane` → **0 行**。
⇒ **整条拉野决策链在两棵树之间逐字节相同**,唯一的差别是 `pullthink` 在不在 arm 串里。

arm 串差 3 个(`pullthink` / `bbfight` / `bbshort`)。后两个只改**买活门槛**
(`J.BuybackFightRespawnFloor` / `J.BuybackShortRespawnFloor`,等级门 &gt;24 / 梯子入口 15,
且必须**已死**才够得着),对 [60,360] 秒里**活着**、`hp ≥ 0.5`、被 `clean_window` 排除死亡窗口的
辅助**结构上够不着**;上一轮实测**全语料 0 次买活,两条腿都是**。⇒ 实际差异归到 `pullthink` 一个。

---

## 2. 新工具与量具吻合度

**新** `tools/batch_test/behavioral/pullthink_domain.py`(`--selfcheck` **14 PASS / 0 FAIL**):
戳营帧的 domain、子句、实体纪律、TP/死亡排除**全部 import 自 `pullcamp_domain`,不重实现**;
新增的只有「英雄自己接下来两个采样步的速度」(`v1` / `v2`,**各用该步自己的 dt**,
&gt; `STEP_MAX=2.5s` 的步**丢弃不拉伸**)。源断言五条(2 个站点 / site A 的
`bot.roamCampPull ~= nil` 守卫 / `pullcamp` 仍 gated / 两个 id **未合取**(pullcad 陷阱)/ 无源错误)
已上棘轮 `tests/test_detector_source_constants.py` **§8 新增 6 条**。

⭐ **两个独立量具逐格吻合**(#186 点名的 `pulldrag_walk.py` 的 `still` 列 vs 本工具的 `still v1`):

| 格 | `pulldrag_walk` | `pullthink_domain` |
|---|---|---|
| W16 armed | 24/95 (25%) | 25/95 (26.3%) |
| W16 armed **ab** | 2/20 (10%) | 2/20 (10.0%) |
| W16 armed **ba** | 22/75 (29%) | **23**/75 (30.7%) |
| W15 armed | 54/133 (41%) | 54/133 (40.6%) |
| W15 armed **ab** | 24/47 (51%) | 24/47 (51.1%) |
| W15 armed **ba** | 30/86 (35%) | 30/86 (34.9%) |
| W16 baseline | 16/28 (57%) | 16/28 (57.1%) |
| W15 baseline | 6/18 (33%) | 6/18 (33.3%) |

**唯一分歧 1 帧,原因已定位**:`pulldrag_walk` 取 `[t+1.0, t+1.5]` 里的第一帧,本工具取
「t 之后严格第一帧」并折算成 u/s;采样格偏离 1.0s 时两者各取到一帧。
两者的 **50u 常数已上棘轮并做过变异**(改成 60.0 → 该条断言变红,改回 → 全绿)。

---

## 3. ⭐⭐ 读数一:验收量有一个与 arm 无关的巨大本底

| 波次 | 腿 | 戳营帧 | still (v1 &lt; 50u) |
|---|---|---|---|
| W16 | **armed**(`pullthink` ON) | 95 | **25 (26.3%)** |
| W16 | **baseline**(拉野节拍一帧没进过) | 28 | **16 (57.1%)** |
| W15 | **armed**(`pullthink` OFF) | 133 | **54 (40.6%)** |
| W15 | **baseline** | 18 | **6 (33.3%)** |

**承重帧(全语料最长的零位移串,8 帧,在 baseline 腿上)**
`c1d1cf/20260827_065522_slot5` jakiro(**leg=baseline**, radiant, pos 4),营 d=109:
**t=309.4→318.4 坐标逐位相同 10 秒**,期间在**清营**(GOLD/XP 进账、血 0.99→1.00、
每秒对 `dark_troll` / `dark_troll_warlord` 出伤害),t=319.4 走人。
⇒ 「站在营边不动」**是一个辅助打野时的正常形状**,与拉野节拍在不在**无关**。

**⇒ 一条量,armed 腿读 26–41%,而一条根本没有这套代码可走的腿读 33–57%。判别力不成立。**

---

## 4. ⭐⭐ 读数二:池化的 −14.3pp 过不了英雄分层

armed 腿,按英雄切(**#148 (iii):效应量连切法一起登记**):

| 英雄 | W16 armed(ON) | W15 armed(OFF) | 差 |
|---|---|---|---|
| crystal_maiden | 10/26 **38.5%** | 10/34 **29.4%** | **+9.1pp(反向)** |
| jakiro | 8/40 **20.0%** | 6/25 **24.0%** | −4.0pp |
| ogre_magi | 7/21 **33.3%** | 17/38 **44.7%** | −11.4pp |
| witch_doctor | 0/**5** 0.0% | 7/17 41.2% | −41.2pp |
| zuus | 0/**3** 0.0% | 14/19 73.7% | −73.7pp |

**两波都有 N≥21 的三个英雄符号不一致**;池化那点差异**主要由 zuus(n=3)与
witch_doctor(n=5)扛着**,而它们在对照波各有 19 / 17 帧 ⇒ **是英雄构成变了,不是同一群英雄变了**。
CM 再往物理分层切,两层还反号(radiant 2/11 **18.2%** vs 3/11 **27.3%**;
dire 8/15 **53.3%** vs 7/23 **30.4%**)⇒ 按 #148 (i) 该读弃。

⚠️ **中途读数会骗人,留档**:71 局 vs 61 局时两个物理分层是**反号**的
(radiant −53.6pp / dire **+9.0pp**);337 局时才变成同号。**部分语料上的分层符号不是结论**。

---

## 5. ⭐ 读数三:armed 腿上 wave13 指纹仍在(GH #250)

`c1d1cf/20260827_065534_slot6` crystal_maiden(**armed 腿**, dire, pos 5),`pullthink` 在串里:
**t=343.5→350.5 坐标逐位相同 8 秒**,**12 只中野**(叠过的营)围着打,
**血 1.00 → 0.58**,CM 全程只还手一次(t=348.5),然后 t=351.5 跑、t=353.5 **烧 TP 卷轴撤**。
营没拉走、线没重置,净亏。这是 `mode_roam_generic.lua:309-312` 注释里那句
"a puller stood and TANKED the neutrals" 的原样,**在 `pullthink` armed 的腿上**。

**够不着,如实登记**:`bot.roamCampPull` 是否为 nil **不在 dump 里**;
`pullcamp_domain` 只能**近似**重建 `ShouldPullNeutralCamp`(`IsLanePullSafe` 的完整形式、
分路均势、`GetNearbyNeutralCreeps` 的视野都够不着)⇒「戳营帧」是**引擎可能否决过的上界集合**。
本例 t=343.5..349.5 的 `nSec` ∈ [43.5, 49.5] **都在 [35,50] 窗口内**,时间窗这一条不背锅;
其余子句要靠 fixture 才能判(配方见 GH #250 §4,**断言顺序不能反**:
先断 `ShouldPullNeutralCamp ~= nil`,再断那条 `Action_MoveToLocation`)。

---

## 6. 逐帧覆盖(深查 8 局,章程下限 6)

| # | 局 | 腿 / 侧 | 英雄 | 窗口 | 逐帧结论 |
|---|---|---|---|---|---|
| 1 | `c1d1cf/065522_slot5` | **baseline** / radiant | jakiro | 305–321 | **10 秒逐位相同**,在清营(GOLD/XP);本底的承重帧 |
| 2 | `552d6b/211653_slot9` | 对照 armed / radiant | zuus | 276–295 | **10 秒逐位相同**,血 0.79→0.71,t=287.3 放大招 ⇒ 在下指令、没下移动指令;#186 原形状独立复现 |
| 3 | `c1d1cf/065534_slot6` | **W16 armed** / dire | crystal_maiden | 339–353 | **8 秒逐位相同**,12 只叠营,血 1.00→0.58,TP 撤(§5) |
| 4 | `5e1086/064300_slot5` | W16 armed / dire | jakiro | 283–295 | 只有一帧重复;t=290.5→295.5 d(camp) 77→590,**9 只中野跟着** ⇒ 拖拽在跑 |
| 5 | `5e1086/064326_slot12` | W16 armed / dire | jakiro | 157–170 | **每帧都在动**;但 t=164.4–169.4 在 (−3930, 5500) 附近**小幅抖动**(30–70u),d(camp) 599→722 不推进 —— 残留形状,登记不裁 |
| 6 | `5e1086/061825_slot10` | W16 armed / radiant | witch_doctor | 188–201 | 教科书拖拽:走进(d 1139→190)→ 眩晕/戳 → 带走(d 190→1211),**无一帧重复** |
| 7 | `5e1086/063135_slot3` | W16 armed / radiant | witch_doctor | 340–353 | 同上(d 1086→326→1146),d(wave) 2141→868 ⇒ 往线上带 |
| 8 | `4f267b/211715_slot11` | 对照 armed / radiant | crystal_maiden | 338–354 | 温和版:t=343.4–345.4 三步 ~20–30u near-zero(边放 Frostbite/Nova 边站),然后走 |

**全语料 274 个戳营帧里,零位移连串最长的三个**:baseline jakiro **8 帧**、
对照 armed zuus **6 帧**、W16 armed CM **5 帧**;对照 armed 腿占了前 8 名里的 5 个。

---

## 7. 核验结论

| id | 判定 | 依据 |
|---|---|---|
| **`pullthink`** | **UNRESOLVED —— (a) NOT BOUGHT** | 域**非空且可量**(armed 腿 **95 帧 / 39 局**),但**它的验收量不具判别力**(§3)且**池化差异过不了英雄分层**(§4)。**不判 WORKING**:armed 腿会走的帧,对照腿也有(#186 §2 的正对照);**不判 SILENT**:armed 腿的池化 still 更低且两层同号,同时 `pulldrag_walk` 的 DRAG 签名 armed **48/95 = 51%** vs 对照 **47/133 = 35%**。 |
| site B(`:321` 的 0.5s 起手保持) | **UNOBSERVABLE-AT-1HZ** | 比一个采样步还短的保持,**里面不落任何采样**;它的后果(戳中)已经在 `poke` 谓词里(要求一条 hero→非 hero DAMAGE),所以上表每一帧**按构造都已经打中过**。 |

**本组不裁 promote/reject、不申请波次、不改 bot Lua、不花 AWS 钱。**

---

## 8. 交棒

- **GH #249 新开 [bug]** —— #186 §5 的验收量不具判别力,给了替代验收(§6/§7)与两条硬约束
  (必须同报 baseline 本底、必须按英雄分层)。**球在总监。**
- **GH #250 新开 [strategy]** —— armed 腿上指纹仍在的承重帧 + 钉帧配方(**断言顺序不能反**)
  + 一个独立方向(`ShouldPullNeutralCamp` 的安全子句只看**敌方英雄**,不看中野数量/伤害;
  pos 5 走进 12 只叠营在 turbo 等级下必亏)。**球在协同组。**
- **GH #186 追评并划掉 §5** —— 换量不是软化;§1/§2 的立案**已在两个新波次独立复现**,现象没问题。
- **仍未裁(球在总监)**:#250、#249、#247、#246、#245、#244、#241、#236、#235、#159、#207、
  #212、#220、`campfarm`;`bbfight`/`bbshort` 三条件判定。

---

## 9. 验证

- `bots/` / `game/` **0 改动**,gate 未动(改的是 `tools/` 与 `tests/`)。
- `bash tools/agent/luacheck_gate.sh` → **exit 0,`luacheck bots game: 0 warnings`**(容器里没有,脚本自装 `lua-check`)。
- `bash tests/run_py_tests.sh` → **42 passed / 0 failed / 0 uncertifiable**(与开工自检逐位相同)。
- `pullthink_domain.py --selfcheck` → **14 PASS / 0 FAIL**。
- `tests/test_detector_source_constants.py` §8 新增 6 条,**做过变异**:把 `STILL_U` 改成 60.0 →
  「GH #186 的 50 u 阈值是同一个数」那条**变红**,改回 → 全绿。**不是空断言。**
- 本轮未改任何 Lua ⇒ **不声称跑绿过 Lua 全量**(GH #124)。
- **AWS**:S3 **只读**(337 `.dem` 流式下载即弃 + 对应 `analysis.json`),
  **未启动/未终止实例**,**未调用 CE**,**零支出**。

### 9.1 诚实标注

- 第一次跑全语料时工具被 **OOM 杀掉(exit 137)且 stdout 全空** —— 337 个 `Game` 对象同时驻留。
  已改成 `WARM_GAMES=10` 派生营地 + 逐局装载即弃,并加了**营地数稳定性断言**
  (首 10 局与末 10 局派生出的营地数必须相同,否则 fatal)。**那次静默失败留档在此。**
- 种子 896(`8ca428`,19 局)是被 spot 回收的那一粒,全是 radiant 孤儿 ab 腿;
  本轮按**物理分层分别报占比**,孤儿只让 ab 层的 N 变大,不污染任一层的占比。
- 两波的**英雄构成本身不同**(W16 armed:jakiro 40 / CM 26 / ogre 21;
  W15 armed:ogre 38 / CM 34 / jakiro 25 / zuus 19 / WD 17)—— 这正是 §4 的立论。

---

## 10. 下一轮优先

1. ⭐⭐ **W17 首检**(`spot_20260827_1219*`×4,`d9585a29` 同树同 arm 串、4 粒**新**种子;
   批测台 12:24Z 起飞)—— 一落地就有 **8 粒**池子,且**与本轮 W16 同 arm 串** ⇒
   `pullthink` 的 armed 腿语料可以直接**翻倍并入 §4 的英雄分层表**,那三个 N≥21 的英雄
   有机会各自到 N≥40。**这是本轮结论唯一能被证伪的预登记。**
2. ⭐⭐ **#236 裁下后 `mv` 落地 #159 的 fixture**(**第八轮**,成本 = 两条 `mv`)。
3. ⭐ **GH #250 的 fixture**(`c1d1cf/065534_slot6 t=346.5 crystal_maiden`)—— 本组给配方,
   落地要等协同组,**但断言顺序那一条必须带过去**。
4. ⭐ **把 §5 的锚点修正回灌 `roam_conversion.death_spans`**(等 #247 裁定;回灌前先量影响面)。
5. ⭐ **GH #220 的 fixture**(`094054_slot3 t=163.0 drow_ranger`,**第八轮**)。
6. ⭐ 「每 id 篇数」做成独立小脚本(**第四轮登记**)。
7. `camp_prelit` 列(**第十二轮**);占用度量按 idx 去重(**第十一轮**);`tpdefend_events` 结果侧列
   (#191 后,**第十七轮**);幻象余量做进 `sweep_run.sh`(**第十四轮**);`entities.py` 推广(**第十五轮**);
   12:41Z §6.1 那 23 帧做 fixture(**第十七轮**);`pulldrag` 的 connect 侧;第二种视野证人;
   ab/ba 回灌 `fieldbuy_silence.py`/`stayfield2_margin.py`(**连续第二十八轮登记**);`stayfield` 第一失败子句;
   打野反证 fixture(**三十四轮**);`hero-1` 的 153 局 WK 语料(**42 轮**);`l5combo` 的 (a)(**第五十八轮**);
   `make_fixture.py` 钉 `062551 t=205.5 jakiro`(#45,**第四十五轮**);`axebuyblink` armed 的波次。

---

## 11. Token 用量(铁律 8)

```
TOKENS total_in=18,183,815 out=83,069 turns=113
```
(统计时刻之前的数字;统计后的收尾回合不计入。)
