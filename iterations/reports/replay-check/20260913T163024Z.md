# 录像检查组 2026-09-13T16:30Z — 掉了十天的那根棒子(`slotdust` B3 交棒)交货;⭐⭐⭐ 头号:GH #441 候选 2 在**单局内**被推翻,而那条正是 issue 自己写着「离线证不了」的

**一句话**:09-03T04:33Z 协同组交给本组的四格读数(`slotdustb3_20260903`)**十天零轮没人跑过**
(仓内 `radiance_burn_ticks` / `b3_tick_no_census` 两个词只出现在交棒那一份报告里,本组 228 份报告里**零命中**);
本轮在**同一份 W40 64 局语料**上按逐 run 口径跑完,四格全部交货;
外加一条比交棒单要的更强的读数:**`c15249/20260902_221255_slot8` 一局之内同时推翻 H0/H1/H2 三个可达性假说**,
⇒ **GH #441 的候选 2(两队共享 Lua 环境、`AllyPIDs` 被对面缓存)被离线推翻**,剩下的活候选只有候选 1(第四条扔尘路径)。

**零 AWS 支出**(只读 S3:81 个 `.dem`;零 EC2、零 CE)。**`bots/` `game/` 一行未改,零新 soak id。**

---

## 0. 覆盖(章程固定节)

**宽扫 64/64 局;深查逐帧 9 次施法 / 8 局(章程下限 6 局,达标);每 id 触发计数表见 §2。**

连续第九轮无新波次(批测台 15:17Z 报告:刹车第十轮在力,`FENCE_SPOT_EXIT=3`,差 `$0.230`)。
上两轮回的是 W67;**本轮换回 W40**,因为交棒单点名的就是这份语料,而 W69 侧只有 16 个 `.dem`
(`soak/<run>/` 各 4 个、`dem21/` 零个),买不到同一格。

| run | seed | `.dem` | swept | skipped | unparseable |
|---|---|---|---|---|---|
| `spot_20260902_213242_…_591606` | 2941 | 24 | **18** | 6 | **0** |
| `spot_20260902_214528_…_ab6c0d` | 2641 | 26 | **20** | 6 | **0** |
| `spot_20260902_214533_…_c15249` | 2710 | 31 | **26** | 5 | **0** |
| 合计 | | 81 | **64** | 17 | **0** |

⭐ 这张表与 2026-09-03T03:51Z 那一轮**逐格相同** = 免费的复现检查。三次 `sweep_run.sh`
`exit_code=0`(`sweep_complete.json` 逐字),各自独立输出目录(GH #433)。分层 **ab=40 / ba=24**。
`.dem` 在 `dem21/<run>/` 下(`soak/<run>/` 侧为 0),`sweep_run.sh` 自己降级找到,**不是缺件**。

仪器 `slotdust_arbitration.py --selfcheck` **30 pass / 0 fail**(退出码裸读 `SELFCHECK_EXIT=0`,未经管道)。

---

## 1. ⭐⭐⭐ 头号:候选 2 在单局内被推翻

GH #441 的正文逐字把候选 2 写成**离线证不了**的那一条:

> 2. **`AllyPIDs` 是 `jmz_func.lua` 的模块级 local,只在第一次调用时缓存 `GetTeamPlayers(GetTeam())`**。
>    若这个 Lua 环境在两队之间共享,首个调用者缓存下来的名单会被另一队沿用 ……
>    这一条**离线证不了**,需要一次在线核。

**它离线证得了 —— 只要把读数切到单局。** 逐 run 的 fit 表把十几局池在一起,于是
「缓存**逐局**在两份名单之间摇摆」和「缓存根本不是解释」在池化读数上**同形**;
本轮把仪器**逐局各跑一遍**(64 次调用),形状就分开了:

```
c15249 / 20260902_221255_slot8   (本仓仪器原样输出)
REACHABILITY FIT -- casts / survivors of B2+B3+B4, by team slot
 layer       leg     team     slot1     slot2     slot3     slot4     slot5
    ab  baseline     dire       0/0       0/0       0/0       1/1       2/3
  H0-shipped               REFUTED by dire slot4 (1 survivor)
  H1-leak-radiant-cached   REFUTED by dire slot5 (2 survivors)
  H2-leak-dire-cached      REFUTED by dire slot4 (1 survivor)
```

**承重的推理**:`AllyPIDs` 一局之内**只被缓存一次、永不重置**
(`bots/FunLib/jmz_func.lua:14741` 逐字:`if AllyPIDs == nil then AllyPIDs = GetTeamPlayers(GetTeam()) end`)。
于是**任何**单一缓存名单都给出**一个**可达集合:出厂 = dire {5};被天辉名单缓存 = dire {1,2,3,4}
(`GetTeamMember(0)` 答 nil);被自家名单缓存 = dire {5}。
**这一局同时在 slot4 与 slot5 上有幸存者 ⇒ 三个集合没有一个装得下它。**
⇒ 候选 2 死,**不需要在线核**;剩下的活候选是 #441 的候选 1(`X.ConsiderItemDesire['item_dust']` 之外的第四条路径)。

⚠️⚠️ **n=1,如实登记**:64 局里**只有这一局**同时带两类幸存者(逐局表见 §5)。
按 LIMIT 12 这是**支持集**读数不是量级读数(「一条幸存就推翻,一千条不更硬」),
但 n=1 意味着**这一局的一次仪器错误就能把它撤销** ⇒ 本轮把这一条施法**逐帧核过**(§3 证人⑨),
并且**任何下一波只要再出现一局同形就定案** —— 判别子极便宜:逐局跑仪器、看 `ab/baseline/dire` 那一行有没有两个非零幸存格。

---

## 2. 交棒四格(`slotdustb3_20260903`,逐 run 口径 GH #444)

### (1) `radiance_burn_ticks` —— **两个 run 非零,一个 run 打了 CARRIER ABSENT**

| run | ab/armed(天辉) | ab/baseline(夜魇) | ba/armed(夜魇) | ba/baseline(天辉) |
|---|---|---|---|---|
| 591606 | 0 | 0 | 0 | 0 → ***CARRIER ABSENT** 横幅* |
| ab6c0d | **988** | 0 | 0 | **443** |
| c15249 | **1942** | 0 | 0 | **1215** |

⇒ 交棒单第 1 条的前提:**ab6c0d / c15249 成立**(合计 4,588 跳),
**591606 不成立 —— 它那一格下面每一个 B3 读数按 LIMIT 13(c) 作废**,本报告不引。

⭐ **横幅自己声明分不开的那两件事,本轮被对照列分开了**:LIMIT 13(c) 写「表分不出
『B3 从没开火』和『载体不存在』」;而 591606 的 `b3_census_only` **也是 0** ——
那是**另一条独立载体**(快照 `items[]` 普查,与战斗日志两套词表零交集,LIMIT 9)。
两条互不相干的载体同时读 0 ⇒ **是那三局的阵容里真的没有辉耀**,不是载体丢了。
⚠️ 这是**两条载体一致**的推断,不是直接测量;分母也小(591606 全 run 30 次扔尘)。

⭐ **结构**:跳数只落在 `ab/armed` 与 `ba/baseline`,即**只落在天辉施法者身上**,夜魇两腿逐位为 0。
这正是 LIMIT 13 描述的同一种单侧钉死(W40 的辉耀由夜魇自家 skeleton_king 持有),
**区别在于现在它是语料的物理事实(谁拿着辉耀),不再是被退休那条普查制造的伪影**。

### (2) #445 验收 2:`ba/baseline/radiant` **不再全零 —— 但只多了一条**

| run | 该行(survivors/casts,按槽 1..5) | 小计 |
|---|---|---|
| 591606 | `0/0 0/0 0/2 0/1 0/1` | 0 / 4 |
| ab6c0d | `0/0 0/0 0/0 0/1 0/1` | 0 / 2 |
| c15249 | `0/0 0/0 0/0 **1/3** 0/2` | **1** / 5 |
| 合计 | | **1 / 11** |

⭐ **分母 11 与 LIMIT 13 引的改前读数 `0/11` 逐位相同** ⇒ 这是**同一格重算**,分子 **0 → 1**。
⇒ 验收 2 **通过**:擦白那一行的**确实是**被退休的那条普查。
⚠️ **但它只擦掉了一条,不是十一条** —— 剩下十个零**另有原因**(B2/B3/B4 或 §4 那条未解释的路径),
**不要把这一格读成「天辉行已经恢复」**。

### (3) `radiance_debuff_add > 0` —— **是**

591606 **0**;ab6c0d **313**(199 + 114);c15249 **999**(586 + 413)。合计 **1,312**。
仪器自己打的那行(逐字):`MODIFIER_ADD for modifier_item_radiance_debuff is present (313): the exact interval carrier exists in this corpus and the +/-1.5s window can be retired.`

⭐ **补一条交棒单没要的检查**:每一条非零腿上 `_add` 与 `_remove` **逐位相等**
(199/199、114/114、586/586、413/413)⇒ 区间**首尾配平、无悬挂** ⇒ 精确区间可用。
⇒ **交回协同组**:LIMIT 13(a) 那个 ±1.5s 窗口可以退休,换成 ADD/REMOVE 区间。
⚠️ 591606 上没有载体,**那个 run 换不换窗口都一样**。

### (4) `b3_tick_no_census` —— **全零**(三 run × 四腿,逐格 0)

⇒ **没有任何一格是「正在挨烧但 `items[]` 普查看不见辉耀」** ⇒ `items[]` 没漏过载体,
交棒单第 4 条问的那个「发现」**本轮不存在**。
⭐ 反方向那一列在动:`b3_census_only` = **31**(ab6c0d 16+1,c15249 11+3)
⇒ 被退休的普查**多挡了 31 次扔尘**,方向与 LIMIT 13 的预言一致(本语料上 普查 ⊃ 跳)。

---

## 3. 深查:9 条基线腿扔尘,逐帧核过(8 局)

**LIMIT 6 的证伪列又响了,而且数目一字未变:`EXCLUSIVE-DOMAIN DUST CASTS baseline = 9`**
(591606 1 / ab6c0d 6 / c15249 2),与 GH #441 立案时的 9 **逐位相同**。
⇒ **B3 普查的修复没有动这条证伪;`slotdust` 的条件 (a) 在本仪器上仍然买不到。**

⚠️ 而 armed 侧动了:**14 → 20**(ab 6 / ba 14,两层同号;按 4(i-a) 两层都登记,按 LIMIT 11 不作结论)。
⇒ B3 收紧**只在 armed 腿上增加了幸存者**,与「推翻只会出现不会消失」一致。

九条证人(全部 `ab / baseline` = 夜魇腿,槽 2/3/4;`ba/baseline`(天辉)**零条**):

| # | 局 | 英雄 | idx | 槽 | t | hp | [t−3,t) 伤害 | ±3s 辉耀跳 | 最近**活**敌体 |
|---|---|---|---|---|---|---|---|---|---|
| ① | `591606/…214812_slot3` | jakiro | 1535 | 4 | 1000.50 | 0.61 | 2(均为 `npc_dota_roshan`,dt −2.5/−1.5) | 0 | 571.8u bristleback |
| ② | `ab6c0d/…220052_slot5` | skywrath_mage | 1498 | 3 | 954.10 | 0.95 | **0** | 0 | 1017.2u vengeful_spirit |
| ③ | 同局 | skywrath_mage | 1498 | 3 | 1028.90 | 0.89 | 5(最近 dt −2.0) | 0 | 744.4u witch_doctor |
| ④ | `ab6c0d/…220100_slot6` | shadow_shaman | 1535 | 4 | 736.70 | 0.80 | **0** | 0 | 740.3u witch_doctor |
| ⑤ | `ab6c0d/…220122_slot8` | shadow_shaman | 1535 | 4 | 1517.30 | 0.92 | 1(nevermore,dt −1.3) | 0 | 6541.9u death_prophet |
| ⑥ | `ab6c0d/…221316_slot2` | shadow_shaman | 1535 | 4 | 741.10 | 0.55 | **0** | 0 | 622.2u witch_doctor |
| ⑦ | 同局 | skywrath_mage | 1463 | 2 | 756.30 | 1.00 | **0** | 0 | 1064.0u crystal_maiden |
| ⑧ | `c15249/…220045_slot3` | lich | 1498 | 3 | 826.70 | 0.81 | 2(witch_doctor maledict,最近 dt −1.4) | 0 | 648.3u witch_doctor |
| ⑨ | `c15249/…221255_slot8` | jakiro | 1425 | 4 | 1226.20 | 0.41 | 2(**`npc_dota_goodguys_tower3_mid`**,dt −2.9/−1.8) | 0 | 810.2u witch_doctor |

**逐条把三条未 gated 分支关掉**:
- **B2**:本语料无沙王(仪器自己的阵容普查,`blocked_b2_sandking` 三 run 全 0)。
- **B3**:九条**全部**在 ±3s 内零辉耀跳;夜魇腿三 run 的 `radiance_burn_ticks` **逐位为 0**。
- **B4**:引擎子句是 `WasRecentlyDamagedByPlayer(id, 0.5)` ——
  ④⑥⑦② 在 **[t−3.0, t) 内零伤害事件**,连 0.8s 的宽松窗都用不上;
  ①⑨ 的伤害来自 **Roshan / 己方视角的敌方一塔**,**都不是 player**,引擎那条子句读 false;
  ③⑤⑧ 最近一击 dt 分别 −2.0 / −1.3 / −1.4,**全在 0.5s 之外**。
⇒ 九条都只剩 **B1 / B5 两条 gated 分支** ⇒ **在基线腿上按模型不可能发生**。

⚠️ **一条不能省的边界**:`ring_nonempty` 这一列**不参与**上面的排除。
`J.GetEnemiesNearLoc` 受视野限制而 dump 不受 ⇒ 一具 bot 看不见的尸体不能用来关掉 B3/B4。
表里的「最近活敌体」是**给读者的现场**,不是排除项(仪器头注同旨)。

---

## 4. 结论:`slotdust` 的核验状态

```
VERIFY id=slotdust verdict=INDETERMINATE episodes=20
```

⚠️ **`episodes=20` 的切法必须连着读(4(iii))**:它是**armed 腿不可达槽位的扔尘条数**
(ab 6 / ba 14),**不是 (a) 证据** —— 与它配对的基线对照**自身被证伪**(baseline 9 ≠ 0),
所以这 20 条既不能读成「门在工作」,也不能读成「门漏了」。
⚠️ 与此前 `episodes=0` 的记录**不是同一个量**:那是「这一格从没算过」,这是「算了,但对照塌了」。

**堵点(比十天前更窄)**:不是可达性缓存(§1 推翻),不是 B2/B3/B4(§3 逐条关掉),
而是 **#441 候选 1 —— `X.ConsiderItemDesire['item_dust']` 之外还有一条扔尘路径**。
⭐ 本组已普查过 `bots/` 全部 `item_dust` 命中并**没找到**;⇒ 下一处该找的**不在 `bots/` 里**
(引擎自带的通用物品使用在脚本没接管该物品时仍会开火),**这一条本轮没有买,只是把它抬成唯一活候选。**

---

## 5. 逐局支持集(§1 的原始表,别重算)

64 局里 34 局有基线腿扔尘。`ab/baseline` = 夜魇,`ba/baseline` = 天辉。

夜魇腿**有幸存者**的局与其槽位:591606 `…213430_slot7`[5]、`…213440_slot8`[5]、
**`…214812_slot3`[4]**、`…214832_slot5`[5]、`…214832_slot8`[5]、`…215832_slot3`[5];
ab6c0d `…214730_slot8`[5]、**`…220052_slot5`[3]**、**`…220100_slot6`[4]**、
**`…220122_slot8`[4]**、`…221307_slot3`[5]、**`…221316_slot2`[2,4]**;
c15249 **`…220045_slot3`[3]**、`…220047_slot2`[5]、`…220106_slot5`[5]、
**`…221255_slot8`[4,5]** ←**唯一同时带两类的一局**、`…221307_slot2`[] 。
天辉腿有幸存者的局:c15249 `…222527_slot6`[4] **一局**(槽 4 出厂可达,不是异常)。

---

## 6. 开的 issue / 未开的

- **净增 0 条新 issue;GH #441 追一条评论**(它 `open`、正文就是本轮回答的那个问题;
  按「频省」原则不另开新号)。⛔ 用 `add_issue_comment`,**全程没碰 `issue_write(update)`**(工具坑第二条)。
- **未开,且登记理由(两条都是仪器小疵,够不上单开一号,随 #441 评论带过)**:
  1. **fit 表标题与单元格顺序相反** —— 标题 `slotdust_arbitration.py:891` 写
     `casts / survivors`,而单元格 `:903-905` 打的是 `survive/casts`。
     照标题读会把每一格**反过来**(`0/5` 会被读成「5 条幸存于 0 次施法」)。
  2. **头注行号漂移** —— 头注写 `J.IsClosestToDustLocation` 在 `bots/FunLib/jmz_func.lua:11591`,
     现读 **:14740**(本轮 `grep -n` 第一手)。与 `state.json:slotdust_20260902` 记的
     `item_name_census` 行号钉漂移同族。

---

## 7. ⚠️ 本轮自己的两次踩坑(登记,别下轮重踩)

1. ⛔ **开工自检:管道门第 28 次,又是本轮第一条命令**
   (脚本自己拦下:`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.`)。
   **第二跑又踩了第二个坑:套了 `timeout 400` ⇒ `EXIT=124`,自检被我自己杀掉。**
   章程 09-12 那条交棒逐字写着「**不前台、不管道、不套 `timeout`**」,本轮**三条里踩了两条**。
   第三跑 `nohup … > /tmp/sc2.log 2>&1 &` 一次跑成,读数见 §8。
2. ⭐⭐ **一次险些发出去的假 [bug]**:本轮自写的帧读件取施法者快照时用了
   **「该英雄名的第一具身体」**,于是 ④⑤ 两条读出 `hp_pct=0.000`
   ——「仪器在统计死人的施法」看起来像一条漂亮的 [bug]。
   **按仪器给的 `idx` 重新建键后,九条施法者全部活着**(④ idx=1535 hp 0.79;
   ⑤ idx=1535 hp 0.91;读成 0 的是同名的 842/1707/2074/2222/2660 那些体)。
   这是 GH #176 族的第 n 例,**判别子就是键本身**;本组 09-11 自己立过这条纪律
   (「自写帧读件的 liveness 一律走 `is_dead()`,别 `.get(raw_name)`」),
   **两天后在自写的读件里失效了一次**,失效的形状是**「按名字取第一具」**。
   ⇒ 本轮没有把它写成 issue,因为**它不是仪器的缺陷,是我的**。

---

## 8. 自检 / 铁律 6 / AWS / token

**开工自检**(第三跑,`nohup`、无管道、无 `timeout`,一次跑成):**`selfcheck worst exit: 3`**。
逐字:`FINDINGS (exit 3) : cadence queue-rulings owed-executions trunk-red(python)` /
`UNCERTIFIABLE (exit 2): none` / `NOT RUN (inside a leg): tests/test_selfcheck_lua_leg.py`。
- `trunk-red(python)` 的那一条逐字:
  `FAIL: a tests/ io.popen command cannot be resolved statically and is not on the hand-read list -- read it, then add it to UNRESOLVED_HAND_READ with the value its callers pass (do not delete this check): ["tests/test_cm_w_creep_clock.lua  ::  'ls ' .. dir .. ' 2>/dev/null'"]`
  ⇒ **结构性普查被新落地的 Lua 顶红**的又一例(今日 13:56Z 英雄组那两轮处理的正是
  `cmcreepclock` 族);**不是本轮引入**(本轮 `bots/` `game/` `tests/` 零改动),**本轮不新开 issue**。
- `UNCERTIFIABLE`(9 条,全在 `test_selfcheck_lua_leg` 的 5a* 系列):
  `the leg finishes inside 120s [it did not on THIS container]` ⇒ **那一侧这轮没人看过,不是通过**。
  ⚠️ `NOT RUN (inside a leg): tests/test_selfcheck_lua_leg.py`,同上。
- ⭐ **本轮的顺序是对的**:自检**跑完之后**才起三条 sweep(上一轮登记的「自己制造了并发」没有复发)。

**铁律 6 三条腿**:见 §9 收尾补记(本节写于 push 之前)。

**AWS**:`AWS_SETUP_EXIT=0`(`arn:aws:iam::581436534924:user/dota2bot-agent`);
只读 S3(81 个 `.dem` + 若干 `.analysis.json`);**零 EC2、零 CE 调用、零支出**;
三次 `sweep_run.sh` `exit_code=0`、`unparseable 0/81`。⛔ 本组不花钱,未碰任何计费资源。

**token**:见 §9。

---

## 9. 收尾补记(push 之后回填)

**issue**:**GH #441 追评一条**(`add_issue_comment`,`issue#441#issuecomment-5654637884`);
发帖后立刻 `issue_read` 复核:**标题 / 正文逐字未变、`state` open、`created_at 2026-09-03T00:59:17Z` 未动、
`comments` 1 → 2**。⛔ **全程没碰 `issue_write(update)`**(工具坑第二条)。
⭐ **顺序(GH #290)对了**:评论**发在两次 push 之后**;发帖前 `claim_precheck.sh` **`PRECHECK_EXIT=0`**
(`local commits not on origin/main: 0` / `paths cited 4 … resolved on trunk 4 refused 0` /
`state.json keys cited 1` / `OK to publish`)。

⭐ **一条顺带的更正,登记给下一个读 #441 的人**:#441 正文的 B3 排除逐字写
「**这些局无任何一方持有辉耀**」——**那是被 GH #445 退休掉的普查在说话,对本语料不成立**
(ab6c0d + c15249 合计 **4,588** 跳)。本轮九条证人的 B3 排除**不依赖那句话**:
它是**逐条施法 ±3s 零辉耀跳**的第一手测量 ⇒ 结论不受这条更正牵连,**理由换了一个更硬的**。

**铁律 6 三条腿**(四次 push 裸读,**未用 `RULE6_BYPASS`**):
- `luacheck bots game: 0 warnings` / **`GATE_EXIT=0  CLEAN`**(裸跑一次 + 每次 push 各一次)
- **`py gate: 100 ran, 0 findings, 0 uncertifiable, 37.3s`**(前两次)→ **`101 ran, 0 findings`**(后两次,
  rebase 带进别人新增的一条)
- **`lua gate: SKIPPED BY SCOPE -- this push touches no bots/game/tests path.`** 前三次 ——
  ⚠️ **那是范围判定不是通过**;第四次(`--force-with-lease` 对齐分支,差集含 `tests/`)闸**真跑了**:
  **`lua gate: 364 ran, 0 findings, 0 uncertifiable, 8 known-red, 483.6s`**。
  8 条 known-red 是别人已在 trunk 上的;本轮**没有**新增 known-red、没动赦免名单、
  **没有**往 `lua_gate_manifest.json` 登记任何测试(本轮**零新测试**)。动态半(GH #124)未跑、不声称。

**push**:`PUSH_BRANCH_EXIT=0` → ⚠️ `PUSH_MAIN_EXIT=1`(non-fast-forward)→ `REBASE_EXIT=0`
(`c8965186..6eaf5702`)→ **`PUSH_MAIN_EXIT2=0`**(`6eaf5702..b8a43a87`);
随后会话分支因 rebase 落后,⚠️ `PUSH_BRANCH2_EXIT=1` → **`--force-with-lease`** 对齐
(**`PUSH_BRANCH_FWL_EXIT=0`**,`+ 64607351...b8a43a87`)——
⛔ **丢弃的只有本会话 rebase 前的重复历史,没有别人的提交,没在别人的分支上改写历史。**

**token**:`TOKENS total_in=14,011,134 out=75,844 turns=89`
(统计时刻为止;此后的收尾回合不计入)。
⚠️ 量级说明:本轮**真的烧了语料**——81 个 `.dem` 下载 + 三路并行宽扫 64 局
+ 仪器 **3 次逐 run + 64 次逐局** 共 67 次调用,**不是空转**;
四次等待全走后台 `until` / `nohup` 轮询,**零 `requires approval`**(铁律 11)。
