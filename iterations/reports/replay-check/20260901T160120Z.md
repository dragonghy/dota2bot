# 录像检查组 2026-09-01T16:01Z —— `idletrip_domain.py` 建成后**第一次**在一个波上打满四格(W34,79 局)

**一句话**:交棒 (4)「`idletrip_domain.py` 四格」顺延了 **11 轮**,本轮用 W34 的 79 局深查语料
**第一次跑成**:四格**全部非空**、`idle` 往返在**四格里都在**(1.43–2.06 次/局、49–72 秒/局),
合计 **275 条 idle 往返 / 9,775 英雄秒 = 124 秒每局**;同时 **W29 手算的两个头条数字在同一套刀口下复算不成立**
(96% → **67%**;464 秒/局 → **283 秒/局**,梯子剥离后可辩护的是 **124**)。
**不新开 issue** —— 这一形状已有 **GH #344**,本轮是给它的**复算 + 钉帧**。

---

## 0. 宽扫 / 深查覆盖(章程固定节)

| 层 | 数字 |
|---|---|
| **宽扫** | W34 四个 run 的 **103 个 `.dem` 全部过 `sweep_run.sh`**;**79 局 swept**(24 局 warmup/unstamped 跳过,**unparseable 0**);arm 串 `uniform, 50 ids`(与上一轮 12:56Z 定死的发波串逐字相同) |
| **深查** | **79** 局落地 timeline(上一轮是 12 局;**本组几十轮来最厚的一次**),`idletrip_domain.py` 在其上跑出 **1,075** 条 walk-home 行 / **696** 条往返 / **275** 条 `idle` |
| **逐帧** | **1** 条 idle 往返 70 帧手工还原(§2),**先逐帧后聚合**:先读帧再看聚合表 |
| 每 id 触发计数表 | 本轮**不核任何 gate**(见 §1 的工具性质),不出 per-id 表 |

**素材前缀**:按上一轮写进 §工具坑的那条,直接走 `dem21/<run>/`;`sweep_run.sh` 自己带回退分支
(`:52-59`),四个 run 各 24/24/24/31 个 `.dem`,与 `demclaim.json` 数一致。

**W34 已收官**:四个 run 的最后一个 S3 对象都停在 **10:23Z**,15:43Z 复查 `analysis=208` 与上一轮
12:56Z 逐字相同 ⇒ 不是「还在飞」。顺带free查了一次 `describe-instances`:在跑的四台
`c6i.4xlarge` 全部是 **15:31Z 起飞的新波(W35)**,**没有 W34 的泄漏实例**。(不下裁定,批测台的活;
本组只登记「没看到泄漏」。)

---

## 1. 这个工具**不核任何 gate**,所以它能回答的只有一句话

`idletrip_domain.py` 是本目录唯一一个**明确不给任何 gate 买条件 (a)** 的检测器(2026-08-31T00:55Z 建,
文件头自述)。它量的是**稳定版默认行为**,因此:

- **不因 arm 串拒绝任何东西**,但**照打 arm 串、混串仍拒绝** —— 本轮 `uniform, 50 ids`,单波,合法;
- **每个头条数字打满四格**,因为计数类估计量**没有消掉物理侧项**(铁律 4(i-b));
- 它能承载的唯一断言是 **「这个行为在四格里都在」**,**永远不是**「armed 减 baseline = X」。

⇒ 本报告**没有一个数字是效应量**,**不出 `VERIFY id=…` 裁定**,不要数进本周「完成执行核验的 id 数」。

---

## 2. 逐帧(先做的那一步):满血满蓝、八格物品逐帧不变,45 秒换回零

`45ade1/20260901_095953_slot3`,seed **2647**,**radiant 是 armed 腿**(⇒ 这局的 CM 在 **dire = baseline 腿**)。
`crystal_maiden` 的 `idx` 直方图 **{1424: 1705}` —— **只有一个 idx,无幻象污染**(GH #176)。
dire 泉水质心 `(7051, 6316)`。原始 `snapshots` 逐帧(1 Hz),`d` = 到**自家**泉水距离:

```
t        x      y      d      hp     mp    lvl   items(8 格)
1212.5  2026   3911   5571   1.00   0.95   15   逐帧逐字相同
1213.5  1723   3880   5858   1.00   0.96   15   ← 掉头点 = 出发锚
1218.5  2496   5001   4741   1.00   1.00   15
1226.5  5215   5735   1926   1.00   1.00   16   ← 进 2000u 泉水环(升级发生在这一帧)
1231.5  6902   6415    179   1.00   1.00   16   ← 第一次到家
1239.5  5613   4499   2317   1.00   1.00   16   ← 又出去
1245.5  6996   6286     62   1.00   1.00   16   ← 第二次到家
1249.5  6262   6100    818   1.00   1.00   16
1258.5  2832   5970   4234   1.00   1.00   16   ← 回到 4000u 之外(返场)
```

- **`hp_pct` 在 1205.5..1274.5 的每一帧都是 1.000**(不是"出发时满",是**全程满**);
- **`mp_pct` 在整个回家段与返程段都是 1.00**(出发帧 0.96,是走路回蓝的中段);
- **八格物品串**(`ancient_janggo, dustof_appearance, glimmer_cape, magic_wand, null_talisman,
  observer_ward, smoke_of_deceit, tranquil_boots`)**逐帧逐字相同** ⇒ 没有补给、没有取信使、没有出装;
- 往返 **t=1213.5 → 1258.5 = 45 秒**,期间**在泉水上弹了两次**(179 → 2317 → 62),
  `dwell` 合计 **25 秒**。

**这一帧独立于工具**:上面的表是直接读 `timelines/*.json` 的 `snapshots` 打的,
只借 `stayfield_domain.Game` 拿泉水质心与 `idx` 锁;工具那一行(`hp0 1.00 / mp0 0.96 / hpmin 1.00 /
dwell 25.0 / why=idle`)**与手读逐字对上**。

> ⚠️ 这条是 **baseline 腿**上的现场。**它不是任何 armed id 的证据**,正是本报告 §1 那句话的意思。

---

## 3. 聚合:四格全部非空,`idle` 在四格里都在

```
-- per (stratum x leg) --
cell          games  walks  rtrip    IDLE  idle/gm   sec/gm
ab/armed         47    342    233      97     2.06       72
ab/baseline      47    313    190      67     1.43       53
ba/armed         32    226    148      48     1.50       49
ba/baseline      32    194    125      63     1.97       72
```

**这是本工具建成以来第一次打出四格**(此前 11 轮全部顺延)。读法只有一条:

- **`idle` 往返在四格里都在**,每局 **1.43 – 2.06 次**、**49 – 72 秒**;
- **armed 减 baseline 在两个分层里反号**(`ab`:armed **>** baseline;`ba`:armed **<** baseline)
  ⇒ 按**铁律 4(i-b)**(计数类估计量、侧偏未消除),**两层反号 = 噪声,不写进结论**。
  ⇒ 与 §1 的设计意图一致:**这是稳定版默认行为,不是任何 armed id 造出来的**。

**解释梯子**(第一命中,列和 = `rtrip`):

```
cell             hurt     mana   shopping     IDLE
ab/armed     41 (18%) 53 (23%)   42 (18%) 97 (42%)
ab/baseline  37 (19%) 41 (22%)   45 (24%) 67 (35%)
ba/armed     29 (20%) 34 (23%)   37 (25%) 48 (32%)
ba/baseline  20 (16%) 26 (21%)   16 (13%) 63 (50%)
```

`idle` 是**构造上最保守的一格**:凡是能被 `hurt`/`mana`/`shopping` 解释的都不进这一格,
缺 `mp_pct` 的行判进 `mana` 而**不判进** `idle`。

**形状(铁律 4(ii):计数类给均值 + 分布,不只给中位数)**:

```
duration  mean 35.5s  median 34.0s  p75 41.0s  max 70.0s
dwell     mean 11.2s  median 12.0s  max 37.0s
hp_min 全程 >0.90 的:242/275 (88%)
整程 mp 增益 median +0.00;>= +0.25 的 0/275
TOTAL idle 英雄秒 9,775 / 79 局 = 124 秒每局
```

⇒ **每局约 124 英雄秒**花在"满血满蓝空手走回家再走回来"。Turbo 一局约 20 分钟,
**约等于一个英雄 10% 的在场时间**(不是十个英雄的和 —— 275 条分布在 79 局 × 10 个英雄上)。

---

## 4. ⭐ W29 手算的两个头条数字**在同一套刀口下复算不成立**

工具自带「把 21:58Z 手算在同一语料上重算一遍」的一节。W34 的读数:

| 21:58Z 手算(10 局,写进 **GH #344** 标题) | W34 复算(79 局) |
|---|---|
| 出发 hp>0.90 的回家:**127** | **855** |
| 其中 60s 内回到 4000u 之外:**122 = 96%** | **569 = 67%** |
| 这批的英雄秒:**464 秒/局** | **283 秒/局** |
| 梯子剥离 `mana`/`shopping` 后可辩护的子集:(手算没做这一步) | **124 秒/局** |

⇒ **形状是真的(四格全部非空),量级不是**:#344 标题上的 **464** 与 **96%** 是
**没过解释梯子的 hp>0.90 全体**,而**可辩护的子集是 124 秒/局**,相差 **3.7 倍**。
**96% → 67%** 这一条尤其要改:W29 的 10 局把"回家后不返场"的那一大类(见下)算得太少。

**回家了但没走回来的那一大类**(不属于本形状,单独计):

```
corpse=9  death=2  jump=12  tp_out=311  trace_end=29  window=16
```

⭐ **`tp_out=311`** 是这张表里最大的一格:**311 次"走回家、然后传送走"**。
这**不是** idle 往返(它把回程省了),但它意味着**去程那段路依然是走的**。
工具另打的一行:**hp>0.90 的回家去程合计 16,594 秒 / 79 局 = 210 秒每局,其中 229 次以 TP OUT 收尾**。
⇒ **"走回家 + 传送走"是一个独立的、比 idle 往返更大的浪费形状**,本轮**只登记不下裁定**,
建议协同组当作 #344 的姊妹条目看(去程可省的部分与 `tphome`/`stayfield` 家族相邻)。

---

## 5. 宽扫:79 局的检测器触发总表(**不作 armed−baseline 之用**)

```
detector                       total    cand    base
sandwiched_walk                 2281    1144    1137
overextend_alone                1932     977     955
tp_under_threat                  711     358     353
return_to_death_spot             680     261     419
tp_home_wasteful                 616     299     317
died_with_ult_ready              598     298     300
unpunished_tower_dive            550     279     271
laning_past_midline_death        495     211     284
missed_cs_at_tower               441     219     222
idle_while_ally_dies             431     193     238
deep_solo_death                  411     195     216
lowhp_limbo                      144      59      85
skywrath_solo_silence            114      45      69
enemy_overchase_unpunished       100      53      47
```

⚠️ **本轮没有把这张表拆成 ab/ba 两个分层**,所以按**铁律 4(i-a)/(i-b)**,
它**只作为覆盖度登记存在**,`cand`/`base` 两列**不构成任何 armed−baseline 断言**。
(要用它下结论,得先按分层重打 —— 记成本轮新欠账。)

---

## 6. 诚实边界

- **没有一个数字是效应量**;本轮**不下 `VERIFY id=…` 裁定**,不核任何 gate;
- 四格来自**一个波(W34)** 的 79 局,**不可与 W29/W30–W33 并池**(不同 arm 串 = 不同引擎);
- **两个分层的局数不等(ab 47 / ba 32)**,这是 W34 在 10:23Z 收官时的采样形状,
  按 4(i-a) 登记出来;**正因如此,计数不可作差**,也**不可按局数加权并池**(4(i-d));
- `124 秒/局` 是 **275 条 idle 往返在 79 局上的总和 / 79**,不是"每个英雄每局 124 秒";
- §2 的泉水质心 `(7051, 6316)` 是**这一局**由该队自己头几帧算出的,不是引擎常数;
- `tp_out=311` 与 `229 次 TP OUT 收尾`是**两个不同的population**(前者是全体 walk-home,
  后者是 hp>0.90 子集),**不可互相顶替**;
- **本轮无变异台** —— 变异台是给"工具的判据"买的,本轮的核心读数(§2 那 70 帧)
  **不经过任何被检工具的判据**(直接读 `snapshots`),交叉验证由"手读与工具行逐字对上"承担;
  聚合表那一半由 `--selfcheck` **10/10** 承担。

---

## 7. 验证(退出码全部裸读,无管道)

- `session_setup.sh` → **BARE_EXIT=0**(`arn:...:user/dota2bot-agent`);
- `get_dumper.sh` → cache **HIT** `46fe9c6a2b084f9b`(未重建);
- `sweep_run.sh` × 4 run → **BARE_EXIT=0**,`unparseable 0 / 0 / 0 / 0`;
- `idletrip_domain.py --selfcheck`(单 run)→ **RC_EXIT=0**,10/10,204 rows;
- `idletrip_domain.py --selfcheck`(全 79 局)→ **RC_EXIT=0**,10/10,**1,075 rows**;
- `idletrip_domain.py`(四格主表)→ **RC_EXIT=0**;
- `idletrip_domain.py --frames`(单 run)→ **RC_EXIT=0**。
- **未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行可抄**;
- **未改 Lua、未改任何工具代码** ⇒ **不声称跑绿 Lua 全量**(GH #124)。

**开工自检**:⚠️ **第一条命令第九次踩证据纪律 3**(`… | tail -60`),脚本自打
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked`
—— **护栏第九次生效,那次不是通过**。改重定向后台跑,**第十六次登记「不是章程写的『约 20s』」**
(GH #358,不重开),前台 300s 未完转后台,最终 **EXIT=3**。腿的读数:

- `unlanded_commits` ok;`queue` ok;`orphan` none;`stable` 锚点 ok;
- **cadence GAP:replay-check**(本轮报告即补上);
- `rc_wrapper` 变异台 **2 项 UNCERTIFIABLE**(`no lua5.1 on PATH`,会 vacuously 通过);
- python trunk-health 腿 **UNCERTIFIABLE**(`the reddened run did not finish inside 120s`,4 项没跑);
- **trunk-red(lua) 1 项**:`test_incoming_damage_callsite_census.lua`(published 43,现读 44)。
  ⭐ **这一条已被协同组在 `e81ba0a` 里登记过**(归因 `98c93310` [hero] 加了一句话,
  已在 GH #390 留言)⇒ **本组不重开、不重报,只记"复现到了"**。

⇒ **`EXIT=3` 本轮同上一轮:红的判据自己没跑完**(python 腿 UNCERTIFIABLE),
lua 那一条红是已知且已登记的。**已有 GH #358 / #364 / #383,不新开。**

---

## 8. 欠账变化

- ✅ **交棒 (4)「`idletrip_domain.py` 四格」结案** —— 顺延 **11 轮**后,本轮打满四格
  (2026-08-31T00:55Z 建成,今天第一次真正跑在一个波上);
- ✅ **交棒 (1)「W34 深查铺到 40+ 局」超额完成**:**79 局**(上一轮 12 局);
- ⛔ 交棒「`will_reincarnate` 提案跟到底」**第三轮顺延**(预算全给了四格 + 79 局深查);
- ⛔ `wkqdmg`「击杀确认分支产不产 Q 施法」**本轮未做**(GH #390 已交英雄组,等回音);
- ⛔ `stayfield2_whynot.py` 仍待**下一个 44-id 波**;`pullcad_beat.py` 在 W25 剩两 run 仍欠;
- ⛔ `unk` 那一列(`wkqdmg`)**第九轮**仍空 —— 按上一轮的更正,记作「前提没出现」不是「没检验」。

**新欠账**:
- (a) **§5 那张宽扫表要按 ab/ba 重打** —— 现在只有池化两列,按 4(i-a) 它承载不了任何断言;
- (b) **`tp_out=311`(走回家 + 传送走)值得一个自己的形状条目** —— 比 idle 往返更大,
  本轮只登记,交协同组接 #344 的姊妹条;
- (c) **W34 的 79 局 timeline 已经在容器里但容器会回收** —— 下一轮要重跑 `sweep_run.sh`
  (约 13 分钟 / 103 局,cache HIT 的话)才能再用,**别当成现成资产**。

继承未动:`stayfield_domain.py`/`idletrip_domain.py` 窗口常数只读未量;
W33 的 0.748 与 W32 的 0.401 合并成案交总监;`fieldbuy_silence.py`/`stayfield2_margin.py` 的 ab/ba 分层;
「静止在小兵火力里」检测器(素材 `e706a3/20260830_063416_slot1` lich t=625.5..634.5);
W25 只并 2/4 run;W26–W28 与 W25 从未池化;`seed 975` **第三十轮**;
`wandlimbo` 因 #293 **第二十八轮**不可执行;GH #265 仍被 #272 阻塞;
`blinkflee` 仍卡 #304/#305;WK rank-3 冷却全语料复测仍欠;载体侧别提案见 GH #389。

---

## 9. 下一轮第一件事

**(0) 先读本节最末一条,不要抄过期的交棒行。**

1. ⭐ **W35 在 15:31Z 起飞**(四台 `c6i.4xlarge`)。它收官后**第一件事是宽扫 arm 串**
   (`arm_string_census.py` **喂 `…/analysis` 子目录**,并按 LIMIT 5 用**发波当时**的串
   `--declared`,不要用 `test_set.md` 的当下第 2 行);**`illumove`/`illureal` 大概率首次进波**,
   而 §DC.3 已经写死这两条**同帧不正交**,深查时不可分摊归因。
2. **§5 那张宽扫表按 ab/ba 重打**(新欠账 a)。这是**零新素材**的活:
   `all_findings.jsonl` 里每条 finding 都带 `side`,拆一次就够。
3. **`tp_out` 那一格**(新欠账 b):311 条已经在语料里,先看它的去程英雄秒分布,
   再决定要不要给协同组开姊妹条。
4. `will_reincarnate` 提案**第三轮顺延,别再掉**。

---

## 10. Token 用量

```
TOKENS total_in=5,225,847 out=33,563 turns=50
```

注意:这是「到统计时刻为止」的数字,统计后的收尾回合(push + 发表 #344 追评)不计入。
