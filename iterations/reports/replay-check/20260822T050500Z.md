# 录像检查组 2026-08-22T05:05Z(第三十九次触发)

**一句话**:做上一轮登记的「下一轮优先 (3)」= 把 `TL.at()` 的假死伪影从**原语层**推进到
**结论层** —— 它流到 `tp_attribution.gate()` 的**三个**判据,`#96` 注册的四格表在锁上 idx
后**四格全动**(`B_armed 4:B_base 4` → **2:6**,ARMED A−B `+0.750` → **+0.938**),
但 `#96` 的处置结论**不变而且更硬**;同时 **NULL 腿变得更糟**(Fisher p 0.2258 → **0.0434**)。
零 EC2 支出,未改 bot Lua,未动 gate。

---

## 0. 本轮定位

批测台 04:06Z 报告:`soak/` 前缀总数 **140,连续第十六轮不变**,零新增 `.dem`
⇒ 按章程第 2 条走既定优先项,而不是等新局。

上一轮(04:05Z)在 §4 明确写了诚意边界:「**只量了原语,未逐路径追它在 `scan` 内部流到
哪个判据 ⇒ 不声称 `#96` 的 4/16 本底率被推翻或偏向**」,并把它挂成「下一轮优先 (3)」。
本轮就是兑现这句话。它有价值是因为 **`lf_rescue_null_channel.py`(承载 `#96` 四格表的那个
工具)直接 `import tp_attribution as TA` 并调用 `TA.scan`** —— 缺陷与已发布读数之间没有隔离层。

## 1. 覆盖数字(章程固定节)

**宽扫 32/32 局**;**深查:64 条腿全帧双世界配对重扫(32 局 × armed/base,不抽样)
+ 1,003,548 次 `TL.at()` 调用逐点分歧记录 + 3 组逐帧锚点还原 + 4 次完整表级重算。**

语料 = `lf_rescue` bisect(`#96` 自己的语料):
臂 A `spot_20260819_121038/121044/121050/121056_1_main`(13 id,含 `lf_rescue`),
臂 B `spot_20260819_121122/141128/141131/141134_1_main`(12 id),种子 859-862,
40 个 `.dem` → 4 局暖场经 stamp 作废 ⇒ **16 vs 16 有效镜像局**。
`sweep_run.sh` **严格串行**(#102),按 run 分目录,`-interval` **默认 1.0s**。
臂守卫在打印任何数字之前从各局自己的 stamp 读出 `A−B = {lf_rescue}`、`B−A = ∅`、12 个共享 id。

**armed id 触发计数表**:本轮是**单 id 纵深**(`lf_rescue`)而非横扫,
逐 id 计数沿用 04:05Z 表,无新增局故无变化。

## 2. ⭐ 阳性对照第一次是 FAIL,而 FAIL 是对的

按章程 10:51Z 丙条,先在已注册读数上证明探针会响。我原本把 `#96` prose 的
「**26 casts / 16 attributed**」当锚点,legacy 侧读出 **27 / 17** ⇒ 报警。拆开是两件事:

- **17 是我的错**:阈值默认写成 1500,而 `#96` 的注册值是
  `lf_rescue_null_channel.ATTRIB_U = **1000**`。改回 1000 后 attributed **逐位复现 16**。
- **27 不是我的漂移**:今天在 **`lf_rescue_null_channel.py` 自己身上**重跑,它也读 **27**
  (`A_armed`;另三格 A_base 2 / B_armed 7 / B_base 6)。

⇒ **prose 里的 26 在当前仓库 + 当前语料上不可复现**,与 08-21T22:05Z 登记的
「`#37` 的 20/11 不可从仓库内任何产物复现」是**同一类**(工具晚于结论进仓库),
只是这次差 1 而不是差 6。**跨轮引用 cast 数一律用 27。**

**纪律修正**:因此本审计的硬对照改成**跨工具**而不是对 prose ——
legacy 模式必须与 `lf_rescue_null_channel.collect` 在全部 **64 条 (局, 腿) 记录**上逐格相同
(casts / attributed / opportunity episodes),不符 `exit 2`。这条对照三次运行全通过,
**也正是它让「锁 vs 不锁」的差值可以被读成锁的效应而不是别的东西**。

## 3. ⭐ 它流到哪三个判据(逐调用点,标签从源码现读)

| tp_attribution.py 调用点 | calls | 分歧 | legacy 假尸体 | legacy 假活人 | 位置误差中位数 |
|---|---|---|---|---|---|
| `alive(tl.at(f, t)) and dist(...)`(divers 环) | 728,277 | 10,582 | 6,398 | 372 | 2,699u |
| `rs = tl.at(R, t)`(pass A 施法者) | 1,504 | 63 | 45 | 1 | 3,180u |
| `asn = tl.at(A, t)`(pass A 友军) | 4,940 | 197 | 154 | 0 | 3,499u |
| `cand = tl.at(R, t + dt, tol=0.6)`(落点追踪) | 114 | 1 | 1 | 0 | 593u |
| `a_land = tl.at(A, land["t"])` | 41 | 2 | 1 | 0 | 7,049u |
| `asn = tl.at(A, t, tol=0.6)`(pass B) | 217,000 | 7,496 | 5,873 | 36 | 3,335u |
| `rs = tl.at(R, t, tol=0.6)`(pass B) | 51,672 | 1,353 | 1,123 | 0 | 3,521u |
| **合计** | **1,003,548** | **19,694 (1.96%)** | **13,595** | **409** | — |

原语层单向性复现(33:1 假尸体,与 04:05Z 在另一份语料上的 143:1 同向、量级不同)。

## 4. ⭐ 帧证据:三种机制,符号并不一致

**(甲) 副本把「正在杀人的那个敌人」藏起来了。**
`20260819_143059_slot1` t=488.4,友军 venomancer `hp_pct=0.063`(**同一帧的 DEATH 事件
actor 就是 chaos_knight**)。chaos_knight 有 **6 个副本**:合并映射返回 `idx=1819`,
距垂死友军 **1,844u 且 hp=0**;**本体 `idx=1280` 距他 62u、活着**。
⇒ divers legacy **2** / locked **3**,门是 `1<=n<=2` ⇒ 这条「前提全真的救援 TP」锁后**消失**。
(视野上下文:另有 crystal_maiden 369u、tidehunter 91u 两个真 diver,所以这不是边界抖动,
是**第三个人被隐身了**。)

**(乙) 副本让跨图救援者看起来就站在原地。**
`20260819_122930_slot1` t=456.5,施法者 lich、友军 lina(`hp_pct=0.325`)。
lich 本体 `idx=1534` 在 `(−6228, 3146)`;合并映射返回副本 `idx=2186` 在 `(−2806, −1842)`,
**误差 6,049u**。门要求 `dist > 3500` ⇒ legacy 把这条真救援判成「他本来就在旁边」而丢掉;
锁后进表**且落点归因成立**(该局 casts 3→4、attributed 1→2)。

**(丙) 副本的血被当成友军的血,凭空造出垂死友军。**
`20260819_143611_slot1` t=476.0,友军 chaos_knight:合并映射返回 `idx=2406`
**hp_pct=0.289**(过 `<0.35` 门);**本体 `idx=1462` 是 0.648**(该门本应不过)。
该局同帧另有一条 lock-only 的 t=674.1 进表 ⇒ attributed 净 2→1。

**⇒ 原语层是 33:1 单向,判决层不是单向的**:锁后 casts 在四格里 **3 格升 1 格降**
(A_armed 27→31、A_base 2→3、B_base 6→8、**B_armed 7→6**)。
**别把原语的符号搬到结论上** —— 这是本轮最容易犯的错,也是上一轮明确不肯替它下的结论。

## 5. ⭐ `#96` 四格表:四格全动,结论不变

同语料同工具,只换身份原语(`lf_rescue_null_channel.py` **逐字重跑**):

| cell | 注册值(按名合并) | idx 锁 |
|---|---|---|
| A_armed | 16 | **17** |
| A_base | 0 | 0 |
| B_armed | **4** | **2** |
| B_base | **4** | **6** |
| **ARMED A−B** | +0.750 /局 SE 0.306 \|t\| 2.45 | **+0.938 /局 SE 0.213 \|t\| 4.39** |
| **NULL A−B**(该读零) | −0.250 SE 0.102,Fisher p=0.2258 | **−0.375 SE 0.072,Fisher p=0.0434** |
| 有 ≥1 归因的局(ARMED) | A 10 vs B 4,p=0.0290 | **A 10 vs B 2,p=0.0091** |
| 供给比 armed A:B(#92 栏位) | 1.06 | **1.13** |
| opportunity episodes(A_armed) | 432 | **479** |

**读法**:

1. `#96` 的核心处置(撤回 `#37` 的「臂 B 读零 ⇒ 独占签名」措辞、改判
   `WORKING(差分证实)+ BUGGY(效果)`、id 不动)**成立且更硬** ——
   出厂本底锁后是 **6/16 = 0.375 次/局**,比 armed-off 那条腿的 **2/16 = 0.125 还高**。
2. **但注册数字要改**:`4:4`、「本底 0.25 次/局」、`+0.750` 三个数都是合并世界的产物,
   建议重注册为 **2:6 / 0.375 次/局 / +0.938**。
3. **NULL 腿变糟,不是变好**:从「不显著非零」变成 **p=0.0434**。
   ⇒ **不应该**因为 ARMED 的 |t| 从 2.45 涨到 4.39 就上调 (a) 的可引用强度;
   两件事同时动且方向相反。本轮**未**重算 01:45Z 那条「构成可归因 pp」分解,
   故**对 NULL 的机制不下任何结论**。
4. **34 / 64 条 (局, 腿) 记录**至少有一个读数移动;episode 身份 churn:
   锁独占 8 条 cast、legacy 独占 2 条(不是简单的「多认几条」)。

## 6. 交付

- **`tp_attribution.py` 落地 idx 锁**:`primary_idx()`(最早出现时刻口径)+
  `IDX_LOCK = True` 默认开 + `--legacy-merge-by-name` 逃生舱(复现 08-22 前的读数)+
  无 `idx` 字段时 `sys.exit` 而不是静默降级。
  `frames` 网格改从本体帧构建 —— **先量后改**:32/32 局副本贡献 **0 个独有时间戳**,
  pass B 循环域不变;注释里写明这是**该语料的读数,不是 dumper 的性质**。
- **`tools/batch_test/behavioral/tp_attribution_idxlock_audit.py`**(只读,新)。
  守卫:臂守卫 + `sweep_summary.md` 完成度哨兵(#102),**两次变异测试均 `exit 1`**;
  `--selfcheck` 跨工具逐格对照 `exit 2`。**零第二实现**:两个世界都经 `TA` 自己的
  `TL.__init__` 构建(靠临时翻 `TA.IDX_LOCK`),`primary_idx` 直接 re-export `TA` 的。
- **调用点标签从源码现读**:第一版硬编码 `{163: 'passA rs ...'}`,落地 idx 锁把每个调用点
  推后 **60 行**,整张表当场变成 `(unlabelled)`。已改成读 `TA.__file__` 的对应行 + 所属函数名。

## 7. 工具坑(新登记两条)

- **登记一个「注册读数」当阳性对照时,必须把它的参数一起登记。** `#96` 的
  「16 attributed」只在 `ATTRIB_U = 1000` 下成立,我按 1500 跑读出 17 并差点当成锁的效应。
  与 08-21T10:51Z 的 `-interval` 那条同族:**数字 + 它的口径参数是一个原子,拆开引用会读出假差异。**
- **硬编码源码行号的检测器标签会在自己修的那次提交里失效。** 本轮修 `tp_attribution.py`
  推后 60 行,审计表自己变成 `(unlabelled)` —— 而它**不报错**,只是安静地不再标注。
  凡按行号索引源码的表,一律现读源码行。

## 8. 跨组

- **`[harness] #69` 追评**(§6 正是它派给本组的活,**不新开**):三判据表 + 三帧证据 + 交付清单。
- **`#96` 追评**:四格表 delta + 建议处置(**只改数字、不改结论**),请总监裁定。

## 9. 验证

- `.lua` 改动数 = **0** ⇒ 铁律 6 的 Lua 门无适用对象。本容器**未装 `luacheck` / `lua5.1`**
  (fresh container,**如实记录未假装跑过**)。
- `py_compile`(两个改动文件)通过;`--selfcheck` 跨工具 64/64 通过;
  臂守卫变异 `exit 1`;哨兵守卫变异 `exit 1`;`tp_attribution.py` 两种模式 CLI 冒烟通过;
  另三个消费方(`tpdead_release` / `tpdying_landing` / `null_leg_occupancy`)import 冒烟通过。
- **三次独立运行(落地前 / 落地后 / 标签改造后)第 2-4 节逐字节相同。**
- **AWS**:EC2 **$0**;S3 GET 40 `.dem`(≈350MB)+ 40 `analysis.json` + 8 次 `s3 ls` ≈ **$0.02**;
  **未调用 Cost Explorer**。

## 10. 诚意边界

- 只锁了**快照侧**。#69 §1/§6 的**战斗日志侧**(`actor`/`target` 是裸英雄名)**一个字没动** ——
  `deaths[]`、`ally_died_15s`、`resp_died_15s` 仍是名字口径。
- `tp_attribution` 的另外三个消费方会**跟着一起变**,本轮**没有测量它们的 delta**,
  不替它们声称任何事。
- 锁后的数字是「身份正确的读数」,不是「真值」:`GetEstimatedDamageToTarget` 仍不在录像里,
  pass A/B 仍是真实触发的**超集**,这一点与 `#96` 原文相同。

## 11. 已检查

上述 8 run 共 **32 局**(`lf_rescue` bisect 语料的第三个逐帧维度:
`#96` 的差分、`null_leg_occupancy` 的占用、本轮的身份)。无其它新对局。

## 12. 下一轮优先

1. `l5combo` 的 (a),仍等去掉它的 bisect 臂(**连续第十三轮**,请总监按 §AF.3 先例
   **直接裁定降级**,不要再顺延);
2. **量另外三个消费方的 idx 锁 delta**(`tpdead_release` / `tpdying_landing` /
   `null_leg_occupancy`)—— 本轮把锁落地了但只测了一个消费方,**免费离线**,
   这是本轮唯一主动留下的缺口;
3. `axe_blink_domain.py` **改为等 `axebuyblink` armed 的波次**(「含 Axe」已证不是充分条件);
4. #102 落地后按新哨兵重扫三份语料;
5. `cmrguard` 按 #63+#66 用**施法前帧**口径复跑四条验收 + #66 recall;
6. 引用帧级率前先核对 `-interval`;**引用任何注册读数前先核对它的口径参数**(本轮新增)。

## 13. Token 用量

```
TOKENS total_in=14,902,900 out=116,996 turns=115
```
