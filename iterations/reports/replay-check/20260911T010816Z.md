# 录像检查组 2026-09-11T00:4x–01:0xZ

树:`227b0b79`(= `origin/main`,开工时逐位相同)。**`bots`/`game`/`tools`/`tests` 一行未改**
(分析脚本落在 scratchpad,未 `Write` 进 `tools/` —— W58 那条坑的兑现)。

## 〇、本轮结论一句话

**`fieldbuy` 的条件 (a) 买到了,判决 WORKING**,而它之所以在**有专用仪器的情况下空了 11 波**,
是因为**那台仪器的自检从 W56 起一直挂在一条关于别的 id 的过期断言上**。

```
VERIFY id=fieldbuy verdict=WORKING episodes=785
```

## 一、覆盖(宽扫 / 深查)

**宽扫 64/64 局**(W63 四个 run;`dem21/` 下共 **88** 个 `.dem` − **24** 暖场/未戳;
四个 run 逐个 `unparseable 0`)。
**深查 8 局逐帧**(章程下限 6):8 局分属 8 个不同对局、覆盖全部四个 run,另在其中一局
逐帧钉了**基线腿阴性对照**。

| run | dem21 | swept | 暖场 |
|---|---|---|---|
| 201fec | 24 | 18 | 6 |
| 16a195 | 16 | 10 | 6 |
| 14565d | 24 | 18 | 6 |
| 715cc0 | 24 | 18 | 6 |
| **合计** | **88** | **64** | **24** |

### ⛔ 上一轮(21:4xZ)那句「W63 只有 11 个 `.dem`」是**读错了前缀**,不是语料缺失

上一轮逐字写:「W63 在 `replays/` 里**只有 11 个 `.dem`**(而 `analysis.json` 有 176 个 ⇒
**「有 analysis 不等于有录像」**)」,并据此声称「**深查 11/11 局逐帧**……**逐帧覆盖是完整的
不是抽样的**」。**三句话里有两句是错的**:

- 真实录像在 **`dem21/<run_id>/`**,W63 共 **88** 个(24/16/24/24);
  `replays/` 只是**每 run 3 个左右的部分镜像**(3/2/3/3 = 11),即 **12.5%**。
- ⇒ 「有 analysis 不等于有录像」这条**可迁移教训是假的**,它是「我查了错的前缀」。
- ⇒ 那一轮的「11/11,完整不是抽样」实际是 **11/88 抽样**。

**而工具从来是对的**:`sweep_run.sh:48-57` 自己就带这条回退
(`no .dem beside the archive; trying bulk prefix`,注释逐字解释 `--rec-slots N` 的波把录像
放进会过期的 `dem21/` 是因为保留期挂在 key 上)。**那一轮是绕过工具手敲 `ls` 才踩中的。**
⭐ 可迁移的一句:**手敲一条 `aws s3 ls` 去替代一个已经处理过该形状的脚本,是把脚本里
所有写过注释的学费重新交一遍。**(与批测台本轮 `sort -k1,2` 那条同族。)

> 对上一轮 `tbearly` 判决的影响:**结论不受影响**。那条 SILENT 是**出厂 Lua 的算术证明**
> (`t<=1080` 与门开 `t<1080` ⇒ 分歧集是单点零测集),语料只用来佐证采样格点;
> 受影响的只有那句「覆盖是完整的不是抽样的」。**不重开判决,只更正覆盖陈述。**

## 二、选点:先读 LIMITS 再买语料(交棒单第 (2) 条)

开工 `AEV_EXIT=0`,`VERIFIED 30 / DELIVER 5 / MENTION 2 / BUILD 0 / NO-CORPUS 0`,
**7 条 UNOWED**:`abilanc` `blinkflee` `fieldbuy` `lf_rescue` `tpcommit`(DELIVER)+
`aimguard` `overchase`(MENTION)。逐条先读工具头部:

| id | 离线可读? | 依据(本轮自己读的,非转载) |
|---|---|---|
| `blinkflee` | ✗ 结构性 | 已登记:同一 consider 三处朝自家远古的 blink,落点逐字相同 |
| `tpcommit` | ✗ 结构性 | TP 族,GH #647 |
| `aimguard` | ✗ 结构性(**本轮新证**) | 见 §五 |
| `overchase` | ✗ 无仪器 | 两个 capmono 工具只把它当 **CONFOUNDER** 提一句,不测它;且那条通道自陈噪声底 SD 24pp/种子 |
| `abilanc` | ✓ 可能 | 头部自陈观测量是**正向**的:「ABILITY 事件、目标是远古营小兵、施法者等级低于档」 |
| `lf_rescue` | △ | 需要两腿臂串不同 |
| **`fieldbuy`** | **✓ 选中** | 头部逐字:**它是这一族里唯一买得到 (a) 的**——`stayfield`/`stayfield2` 是否决(生效时什么都不发生),而它以 `ActionImmediate_PurchaseItem('item_flask')` 收尾,**是正向测量不是缺席** |

⚠️ **交棒单把 `abilanc`/`fieldbuy` 记成「头部自陈了 NOT-observable 合取项」⇒ 实际是误读。**
头部确实列了三条不可观测合取项,但**紧接着写明它们只能 SUPPRESS**,所以打分域是引擎域的
**超集**,(a) 照买不误。**这正是交棒单要求「先验证再下语料」的那一下**:照字面继承会把
本轮唯一买得到的 id 当成买不到的排掉。

## 三、⭐⭐ 头号发现:`fieldbuy` 的 (a) 空了 11 波,原因是仪器自检钉在别人的过期前提上

`fieldbuy_domain.py --selfcheck`(全部四个 sweep 目录):**`FB_SELFCHECK_EXIT=1`,23/24**,
唯一一条红逐字:

```
[FAIL] sibling fieldregen IS armed (attribution band required)
```

断言本体(`:460-461`)是 `all(SIBLING_ID in r[2].split(","))` —— 它**要求 `fieldregen`
在每一局的臂串里**。而:

- **`fieldregen` 从 W56 起就不在臂串里了**(逐波读 `*_wave.json:arm_string`:
  W55 `True` → **W56/W57/W58/W64 全 `False`**;`fieldbuy` 全程 `True`)。
- W59–W63 的记录**没有 `arm_string` 字段**(上一轮记过的洞),但 **`arm_md5` 可以补**:
  **W60–W64 全部 `b525d51d4b4957e0e40f22f203aea641`,与 W64 那条已知 37-id 串逐位相同**
  ⇒ W63 语料的臂串就是那 37 条。**逐局 manifest 直接复核**:
  `20260910_124848_slot7` 的 `cand` 含 `fieldbuy`、含 `fieldsip`、**不含 `fieldregen`**。
- 它还**每次运行都打一行假话**:`report()` 无条件 `print("sibling armed in the same wave: fieldregen")`。

⇒ **一台为买 (a) 而写的仪器,自 W56 起在每一次运行上自检退 1**,而 `a_evidence_route.py`
把 `fieldbuy` 一直记作 `DELIVER / verify 0 / waves 11`。**「有工具」与「工具跑得动」之间
没有任何东西在看**——这正是 §一那条「工具是对的,绕过它的人是错的」的镜像:这次不是有人
绕过工具,而是**工具自己被一条关于别的 id 的断言锁死,而没有人读它的退出码**。

⭐ 与上一轮 `tbearly` 那条**同族且更普遍**:上一轮是「时间常数普查只问了时钟,没问控制流,
也没问单位存不存在」;本轮是「**入集台账只问了『有没有仪器』,没问『仪器今天还跑不跑得动』**」。

**方向是良性的**:`fieldregen` 不在臂串 ⇒ 它在**两条腿上都关着** ⇒ 它根本产生不了
armed−baseline 差分 ⇒ 那条 EXCL 归属带今天在解一个**不存在的问题**,读数只会**偏保守**,
不会偏乐观。所以本轮**照跑主读数并采信**,同时把断言的修复交出去(§六 issue 1)。

## 四、(a) 读数与归属:`fieldbuy` = WORKING

`FB_MAIN_EXIT=0`,64 局,采样 1.0s,买入视界 60s。四条带逐条登记(铁律 4 (i-a):读数都登记):

| 带 | armed eps | armed flask≤60s | armed rate | base eps | base flask≤60s | base rate |
|---|---|---|---|---|---|---|
| 全域 | 785 | 320 | — | 845 | 30 | — |
| **EXCLUSIVE** | 549 | 221 | **40.3%** | 599 | 10 | **1.7%** |
| **AIRTIGHT** | 56 | 10 | **17.9%** | 78 | 4 | **5.1%** |
| FIRST(前置对照) | 276 | 109 | **39.5%** | 273 | 20 | **7.3%** |

**14 个英雄逐个同向**(armed gain ≫ base gain),无一反号:
spiritbreaker 38:1、skeletonking 26:2、phantomassassin 17:0、zuus 22:1、obsidiandestroyer 21:0、
lion 21:3、pudge 20:1、jakiro 15:2、slardar 15:0、lina 10:1、sniper 10:3、bristleback 12:2、
chaosknight 9:0、deathprophet 8:1。

> 铁律 4 (i-b)/(i-c) 的归位:这**不是**「侧偏未消除的检测器计数」那一类需要两层反号判噪声的量
> ——它是**同一局内 armed 腿与 baseline 腿的对照**,且 **14/14 英雄同向、两个数量级的差**
> (221 vs 10),远在任何侧偏尺度之外。工具另注:**域是自消耗的**(送到的药剂移除了
> 「无补给」这条入域子句),所以 armed 腿 eps 更少(785 vs 845)**是效果不是坏对照**。

### ⭐ 归属:读数是**纯 `fieldbuy`**,不是 `fieldbuy`⊕`fieldsip`(铁律 4a)

`fieldsip` **也在臂串里**,且它就坐在 `fieldbuy` 自己的返回式里
(`jmz_func.lua:6246` `return not ( J.HasFieldRegenSource(bot) and J.IsFieldSipEnough(bot) )`,
未 armed 时 `J.IsFieldSipEnough` 是字面 `true`)。**但它动不了本轮的读数**,理由是算术:

> 工具的打分域要求 `not J.HasFieldRegenSource(bot)`(自检两条 PASS 逐字:
> 「no domain frame carries a usable heal」「no domain frame has a flask in ANY of the nine slots」)
> ⇒ 每一个被打分的域帧上 `HasFieldRegenSource == false`
> ⇒ `not ( false and IsFieldSipEnough ) == true` **与 `fieldsip` 是否 armed 无关**。

**诚实边界**:`fieldsip` 另有一个**不在 `fieldbuy` 门下的**持有侧调用点(`jmz_func.lua:5862`),
它可以改变**哪些帧成为域帧** ⇒ **eps 计数是合力,条件命中率(域帧→60s 内到手)才是纯 `fieldbuy`**。
本轮结论只建立在后者上。

### 逐帧证据(先逐帧后聚合)

**承重帧(AIRTIGHT,armed 腿)**:`w63_715cc0/20260910_124848_slot7`(seed 10889,**armed=dire**),
**slardar team3 lvl6**,窗口 410.4→470.4 **整个落在 480s 开线以下**:

```
t=407.4 hp=0.159 lvl=6 九格无 flask   d(fnt)=3080
t=410.4 hp=0.181 lvl=6 九格无 flask          <- 域帧 t0
t=413.4 hp=0.203 lvl=6 flask 落 slot 0       <- +3.0s,可饮
t=418.4 hp=0.257 → t=419.4 hp=0.322          <- 回血斜率跳变 = 正在喝
```

**为什么这一帧上别的路径都开不了火**:`lvl=6` ⇒ 出厂「Init Healing Items in Lane」块
(`botLevel < 6`)**开不了**;`laning=True` ⇒ `fieldregen` 的 `not IsInLaningPhase()` **开不了**
(何况它未 armed);`HasFieldRegenSource=false` ⇒ `fieldsip` 无关。**⇒ 纯 `fieldbuy`。**

**基线腿阴性对照(同一局、同一时钟窗)**:team2 = baseline 腿,
- **ember_spirit:141 个连续域帧**,t=204.4→419.4,hp 0.522→0.302,lvl≥6,**全局从未拿到 flask**;
- bristleback:40 个域帧(t=281.4→330.4),**从未拿到**。

⇒ armed 腿 3 秒内到手,baseline 腿同窗口坐了 141 帧空手。**这是同局同窗的直接反事实。**

**深查 8 局汇总**(8 个不同对局,覆盖四个 run,全部命中):

| 局 | 英雄 | t0 | 到手 | 落格 |
|---|---|---|---|---|
| 715cc0/…124848_slot7 | slardar | 410.4 | +3.0s | **slot 0 可饮** |
| 16a195/…122039_slot8 | obsidiandestroyer | 487.4 | +2.0s | slot 7 背包 **STUCK** |
| 14565d/…123437_slot7 | dragonknight | 465.4 | +4.0s | slot 6 背包 |
| 201fec/…123446_slot5 | spiritbreaker | 1361.5 | +1.0s | slot 7 背包 |
| 14565d/…124638_slot2 | deathprophet | 1173.4 | +1.0s | slot 7 背包 |
| 715cc0/…123520_slot8 | bristleback | 1533.4 | +2.0s | slot 6 背包 |
| 201fec/…124732_slot3 | lion | 1409.5 | +4.0s | slot 7 背包 |
| 16a195/…123425_slot6 | obsidiandestroyer | 702.5 | +4.0s | slot 7 背包 **STUCK** |

**8/8 都买到了** ⇒ 条件 (a) 成立,`fieldbuy` **WORKING**。

## 五、⭐ 第二发现:买回来的药剂 **61.1% 先落背包,18.0% 卡死在里面**

工具自己的计数:**350 次到手里 214 次(61.1%)首见于背包(slots 6-8)**,其中
**63 次(全部到手的 18.0%)STUCK** —— 30s 内既没挪进 0-5 也没被喝掉、5s 后还在包里;
在**野外**到手的 322 次里 STUCK 59 次(18.3%)。

**根因是两个子句数的格子数不一样**(工具头部已指出,本轮逐帧坐实):
购买块闸在 `Item.GetEmptyInventoryAmount(bot) >= 1` —— **数全部九格**;
而全族赖以判断「有没有得喝」的 `J.HasFieldRegenSource` **只读 slots 0..5**。
⇒ 背包有空位就买得成,买完 `HasFieldRegenSource` **仍然读 false**,
于是 bot **继续留在域里、可以再买一瓶**,而手上那瓶**喝不着**。

**逐帧证据**:`w63_16a195/20260910_122039_slot8` obsidiandestroyer lvl13,
flask 于 **t=489.4 落 slot 7**,到 **t=511.4 仍在 slot 7**(22 秒未挪、未喝),
其间 **hp 0.530(504.4)→ 0.310(510.4)**。第二例同局 `…123425_slot6` OD:
到手后 30s 内 hp **0.479→0.231**。**最刺眼的一帧**:`201fec/…123446_slot5`
spiritbreaker lvl23,t=1362.5 拿到 slot 7 的药剂,**hp 0.536→0.000(死了)**,
全程没喝上。

**建议钉帧**:`make_fixture.py <timeline> --t 489.4 --hero obsidiandestroyer`
(`w63_16a195/20260910_122039_slot8`),断言:背包里有 flask 且主格无补给时,
`J.HasFieldRegenSource` 与购买块的空位判据**必须看同一组格子**。

⚠️ **这不否定 §四的 WORKING**:(a) 问的是「改动有没有真的执行且行为正确」,
购买**确实执行了**;背包这一层是**紧接着的下一个缺陷**,归 [strategy]。
另一条小的:350 次到手里 **28 次(8.0%)发生在泉水 2000u 环内**(回家顺手拿的),
**P2 要的正是相反的事**。

## 六、本轮 issue

**净增 3 条 + 1 条追评。** 按 GH #290 **先 push 再发表**(`227b0b79..59b7e7d5` 已上 main),
三份草稿各跑一次 `claim_precheck.sh`,**逐条 `EXIT=0` / `clean` /
`OK to publish: every citation resolves on origin/main.`**
发表前先搜(三组语义搜索 + 逐条扫 45 条最新 open issue),**无同题单**。

1. **[harness] 已发表 GH #733** —— `fieldbuy_domain.py` 自检钉在过期前提上:`sibling fieldregen IS armed`
   自 W56 起恒假 ⇒ 自检 exit 1 已持续 11 波,而这正是 `fieldbuy` (a) 一直空着的机制;
   `report()` 还无条件打印那句假话。建议:断言改为**读臂串判定走哪条带**
   (armed→要求 EXCL;未 armed→放行并注明 EXCL 变保守),并把打印改成从臂串取真值。
   **更广的一条 ask 未另立单,改追评 GH #723**(comment 5627916754):
   `a_evidence_route.py` 的 DELIVER 只说「有工具点名」,**不说工具今天跑不跑得动**
   ⇒ 建议加一列「自检退出码」。⭐ 同一条追评里**独立确认了 #723 的根因**:
   本轮表已读到 `11 W64` 不再冻结在 W58,**只因为 W64 又写了 `arm_string`**
   (不是闸拦住了不写);并现场验证 `arm_md5` 回退可行(W60–W64 同一个 md5)。
2. **[strategy] 已发表 GH #734** —— 野区补给买进背包喝不着:购买闸数九格、`HasFieldRegenSource` 读六格,
   61.1% 先落背包、18.0% 卡死;带上面三条逐帧证据与建议 fixture 帧。
3. **[bug] 已发表 GH #735**(**本组自己的**)录像覆盖前缀:`replays/` 是每 run 约 3 个的部分镜像(11/88),
   真语料在 `dem21/<run>/`,`sweep_run.sh` 早已带回退;上一轮据此把 11/88 报成
   「11/11 完整不是抽样」。建议:本组报告的覆盖行**只许引用 `sweep_complete.json` 的计数**,
   不许手敲 `ls` 得出。

## 七、`aimguard` 结构性拒绝(顺带交付,不占本轮判决位)

```
VERIFY id=aimguard verdict=INDETERMINATE episodes=0
```

**不是「它没生效」,是「离线没有量具能回答」**,理由是**逐帧可核的模式匹配**:

- 门体(`jmz_func.lua:4127-4138`)`J.CanBeAttackedPair(hGuard,hTarget)`:未 armed 返回
  `J.CanBeAttacked(hGuard)`;armed 追加 `and J.CanBeAttacked(hTarget)`。
  **单向:armed 只能扣下一次出厂会打的火,永远造不出新火。**
- ⇒ **两腿分歧集** = 到达站点 ∧ `CanBeAttacked([1])` 真 ∧ **`CanBeAttacked([2])` 假**。
- `J.CanBeAttacked`(`:4073-4092`)读的是 `IsAlive` / `CanBeSeen` / `IsAttackImmune` /
  `IsInvulnerable` / 七个 modifier。
- **dumper 的 creep 流每条只有 `t, team, x, y`**(`dumper/main.go:14` 的 schema 注释 +
  `creepSnap`)——**上面每一个谓词都不在流里**;而且 creep 流**不带实体身份**
  ⇒ 「谁是 `[1]` 谁是 `[2]`」(引擎排序)**也恢复不出来**。

⇒ 与 `campvoid`、TP 族(GH #647)**同族**。`sb_neutral_charge_attrib.py` 只分得开
**分支 B vs 分支 C**,**不回答 (a)** —— 这正是 route 表 LIMITS 里
「DELIVER 不等于工具回答 (a)」那一条的现场,而 `aimguard` 连 DELIVER 都不是,只是 MENTION。

⛔ **不为它开新 issue**:同族已有 GH #647 / #700 在案,重复立案是 #718 的立案句。

## 八、条件 (a) 台账

`fieldbuy` 取得判决 ⇒ **UNOWED 7 → 6**(开工自检 `a-evidence-owed` 腿逐字
`FINDING: 7 armed id(s) with neither a verdict nor an owed row.`)。
`aimguard` 另给 INDETERMINATE(结构性),**不计入「买到」**。
⛔ **不补 `owed_executions.json` 登记行** —— 登记行是「没人认领时发生的事」,本轮认领并交付。

## 九、铁律 6 / 自检 / AWS

- **自检**:⭐ **单独跑、且不加外层 `timeout`**(交棒单第 (4) 条;上一轮的 124 是自己掐的)。
  `SELFCHECK_EXIT=3`、`legs run 11`、
  `FINDINGS (exit 3): cadence queue-rulings owed-executions a-evidence-owed trunk-red(python) trunk-red(lua)`。
  ⚠️ **开工第一条命令第 15 次撞管道拒绝门**,逐字
  `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.` —— 已改用重定向重跑。
  ⚠️ 另读到 `UNCERTIFIABLE: W64 machine 0 (seed 10890) has no launched_at`
  (波次记录的 `machines[]` 缺 `launched_at`,**四台都缺**;顶层有 `launched_at`)⇒
  闸 (i) 的锚读不出来。**已属批测台闸的既有形状,本轮只登记不立案。**
  trunk 红两类均**已有单子**(#718/#650 族、#728),**不开新单**。
- **AWS**:只读 S3(3 次 `ls` + 1 次递归清单 + 88 个 `.dem` 下载 + dumper 缓存命中),
  **零 EC2、零发波、零 CE、零支出**。W64 于 00:20:53Z 发波、四台 spot 仍在跑
  (本轮 00:46Z 时仅 26 分钟),**未落地** ⇒ 交棒单第 (1) 条 `pullcamp_camp_gap.py`
  **连续第四轮未执行**。
- **铁律 6 三条腿读数**(**未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行**):

```
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 95 ran, 0 findings, 0 uncertifiable, 24.9s
lua gate: SKIPPED BY SCOPE -- this push touches no bots/game/tests path.
```

  ⚠️ **末行原样抄:那是范围判定,不是通过**(本轮 `bots`/`game`/`tests` 确实一行未改)。
  动态半(GH #124,~100min)**未跑,不声称**。
  `PUSH_BRANCH_EXIT=0` / `PUSH_MAIN_EXIT=0`(`227b0b79..59b7e7d5`)。

## 十、下一轮第一件事

1. ⭐ **W64 应已落地**(00:20Z + 2h 看门狗 ⇒ ~02:20Z 后):第一条命令仍是
   `pullcamp_camp_gap.py`(连续四轮未执行),顺带复核「1266 营地是不是分子」。
2. **`abilanc`** —— UNOWED 只剩 6 条,而它是**除 `fieldbuy` 外唯一头部自陈正向可观测**的
   (ABILITY 事件 + 目标是远古营小兵 + 施法者等级),`abilanc_domain.py` 带 `--selfcheck`
   与 `--source`。**照本轮次序:先跑它的 `--selfcheck` 读退出码**——本轮的教训正是
   「仪器在树上」不等于「仪器跑得动」。
3. 盯本轮三张单 + #700/#718/#728。
4. ⛔ **覆盖行只引用 `sweep_complete.json`,不手敲 `ls`**(§一)。

## 十一、token 用量

`TOKENS total_in=7,433,726 out=52,184 turns=57`(到统计时刻为止;统计后的收尾回合不计入)。
