# 录像检查组 2026-09-17T00:47Z(W84)

**本轮实质**:批测台**连续第三十五轮零发波**(最新 `20260917T001646Z.md`,MTD `$91.671`
快照冻结)⇒ 无未检新局,按章程第 2 条转补课。Owner 优先项三处「当前球在」=
批测台 / 协同组 / 协同组,**没有一条在本组**。

**做的是 W83 交棒第 2 条**:「§三 影响面的另一半:有没有 fixture 断言真的依赖
天赋折进去的 special value(`tests/mock/replay_fixture.lua:739-740` 是入口)——
**独立工作单元**」。**本轮兑现,给出的是判决不是现象。**

---

## 〇、成本三段(RULING 48)

`零 EC2 / 零 CE / S3 读取 0 个对象(出网未计价)`

- 本轮**一次 `s3 cp` / `s3 sync` / `s3 ls` 都没跑**,`.dem` / `analysis.json`
  / dumper 二进制**一个没下**。全程只读本地仓库。
- ⛔ 不写「零支出」——三段式照写,第三段的 `<N>` 真的是 0。

---

## 一、结论先写:W83 那个问题的答案是「不会,但理由不是它以为的那个」

W83 担心的路径是:**dump 少了天赋 ⇒ fixture 继承那一份 ⇒ 断言吃到错的
special value**(`replay_fixture.lua:739-740` 原样消费 `u.abilities`)。

**这条路径不存在。** 逐行核过 `value_ladder`(`tests/mock/replay_fixture.lua:206-222`):

```lua
local kv = entry[sKey]
if kv == nil or kv.base == nil then return nil end   -- 只读 base
```

`special_value_shapes.SHAPES[hero][ability][key]` 是 `{ base = ..., bonus = {...} }`,
**天赋折叠项全在 `bonus` 里,而 `value_ladder` 一个字都不读它**;
`GetSpecialValueFloat` / `GetSpecialValueInt` / `GetCastRange` 全部只走
`base` + `rank_step` 的**纯 rank 阶梯**。
⇒ **`u.abilities` 里的天赋条目对 special value 没有任何消费点**,
dump 补齐天赋也不会改变任何一个 `GetSpecialValue*` 读数。
⇒ **GH #861(dumper 少吐天赋)与 fixture 的 special value 是两条不相交的缺陷**,
⛔ 不许把前者的修复写成后者的修复。

**⭐ 而且这件事仓库自己早就知道,还钉了闸。** `tests/test_fixture_kv_getters.lua`
第 3a 节,逐字:

> `3a: a trained special_bonus_* row is NOT folded into the served base`
> `the served read is the BASE step (475), never base+150 -- the engine folds a`
> `trained talent in game, this loader does not, so a fold-dependent test must`
> `drive the fold itself`

⇒ 天赋盲**是有意设计 + 有棘轮**,不是缺口;**合同**写在那条断言的失败信息里:
**「依赖折叠的测试必须自己驱动折叠」**。

---

## 二、真正的缺口:那个合同**没有任何东西在执行它**

合同是散文,**没有检查器**。所以本轮把「谁在吃这个合同」量了出来。

### 2.1 量具(全部本地,变异台按 evidence-discipline 规则 1 搭)

1. 从 `special_value_shapes.lua` 解析出全部 `(hero, ability, key) -> (base, bonus)`;
   焦点五英雄共 **284 个 key,其中 65 个带 `special_bonus_*` 折叠项**。
2. 给 `value_ladder` 插一条**语义惰性的日志**(只 `io.open`/`write`/`close`),
   跑候选测试集,记录**每一次被问到的 key**。
3. 变异:把 `special_bonus_unique_*` 的数值 `+N`/`-N` 折进 `base`(跳过 `%` 与 `=` 行),
   重跑同一集合,看**哪些断言翻**。

**候选集怎么选的(不是拍脑袋)**:折叠 key 只能经由 13 个 live
`GetSpecialValue*` 调用点进入决策;逐个求出**外层函数名**
(`X.ConsiderQ` / `X.CullKillThreshold` / `X.wk_GetBlastKillDamage` /
`X.GetImpaleKillDamage` / `X.cm_GetFrostbiteCreepCap` 等 10 个),
再按函数名取 `tests/*.lua` 的并集 ⇒ **37 个文件**。

**读数**:日志 **238,014 次调用 / 360 个不同 `(hero,ability,key)`**;
其中 **17 个带折叠项**(下表)。

### 2.2 变异结果:**6 个文件红**,合同确实是承重的

| 文件 | 基线 | 变异后 | 翻的是什么 |
|---|---|---|---|
| `test_fixture_kv_getters.lua` | 1 failure(见 §四) | **4 failures** | 1a/1b/3a —— **就是钉合同的那三条**,红得正确 |
| `test_axe_cull_threshold_gate.lua` | 0 | **3** | `CullKillThreshold` 的**决策值** 275→425 |
| `test_lion_q_kill_reach.lua` | 0 | **4** | `lion_impale` 射程 650→1250,**域 26→46 sighting、cast 20→23** |
| `test_cm_frostbite_creep_cap.lua` | 0 | **2** | armed cap 1200→1400 ⇒ **「narrowing」这个立项性质本身破了** |
| `test_cm_kill_confirm_quantifier.lua` | 0 | **2** | population 19→22 |
| `test_cm_q_creep_aoe_reach.lua` | 0 | **1** | `nova_damage` pinned 110 vs 410 |

**变异 6/6 红;还原走文件副本 + `sha256sum -c` 每次 OK**
(`4b6b143e1b0681ee901ec31242239e7ebd8756063efaba82cff0372441f8d24d`,还原两次各验一次)。

⛔ **这张表是上界不是测量**:变异**无条件**折叠,等于假设「每个天赋都点了」。
真实局里只有**被 build 点中的那一行**才折。下一节才是测量。

---

## 三、⭐⭐ 收窄到测量:17 个被消费的折叠 key 里,**7 个的天赋是 shipped build 真的会点的**

判据不是猜的:`tTalentTreeList` 的 `{10,0}` / `{0,10}` 与 slot 的对应关系
**由 `tests/test_cm_t20t25_payoff.lua:136,197` 自己钉死**
(`rows.t25[1]==10 and rows.t25[2]==0` ⇒ 取 slot **[8]**;`rows.t25[1]==0` ⇒ 取 **[7]**)
⇒ `{10,0}` = 该 tier 的**偶数槽**,`{0,10}` = **奇数槽**。
天赋→ability/key 的 mods 表用现成的 `tests/mock/talent_slots.lua`。

| hero | ability/key | 折叠天赋 | tier | **build 点吗** |
|---|---|---|---|---|
| skeleton_king | `hellfire_blast/blast_dot_duration` | `..._wraith_king_facet_1` (+2) | **t10** | **YES** |
| crystal_maiden | `frostbite/AbilityCastRange` | `..._crystal_maiden_frostbite_castrange` (+100) | **t15** | **YES** |
| zuus | `arc_lightning/AbilityManaCost` | `..._zeus_6` (−20%) | **t15** | **YES** |
| zuus | `arc_lightning/AbilityCooldown` | `..._zeus_6` (−20%) | **t15** | **YES** |
| zuus | `arc_lightning/arc_damage` | `..._zeus_2` (+60) | t20 | YES |
| crystal_maiden | `crystal_nova/nova_damage` | `..._crystal_maiden_2` (+300) | t25 | YES |
| skeleton_king | `mortal_strike/AbilityCooldown` | `..._wraith_king_10` (−2) | t25 | YES |
| axe | `culling_blade/damage` | `..._axe_5` (+150) | t25 | **no**(t25={0,10}⇒[7]) |
| lion | `impale/AbilityCastRange` | `..._lion_2` (+600) | t25 | **no** |
| crystal_maiden | `frostbite/duration` | `..._crystal_maiden_1` (+1.0) | t25 | **no** |
| lion | `voodoo/AbilityCooldown` | `..._lion_5` (−2.0) | t15 | no |
| lion | `finger_of_death/damage_per_kill` | `..._lion_8` (+20) | t20 | no |
| crystal_maiden | `crystal_nova/AbilityCooldown` | `..._crystal_maiden_5` (−4.5) | t15 | no |
| (其余 4 个) | reincarnation/counter_helix/splash_radius/finger damage | **无天赋** —— scepter/shard 行 | — | n/a |

⭐ **§二那三个最扎眼的红(axe cull 275→425、lion 射程 650→1250、CM frostbite 时长),
恰恰全是 build 不点的天赋** ⇒ 它们今天读的 base **对 shipped build 是对的**,
⛔ **不许拿 §二 那张表去判它们有病** —— 这正是「同一个结论不是同一个理由」
(evidence-discipline 规则 4)本轮的现场。

**再按语料层高度过滤**(`tests/mock/fixture_talent_sightings.lua`:
`max_level = 19`、`frames_lv20 = 0`、`frames_lv15 = 9`、`frames_lv10 = 228`):
t20/t25 那三个(arc_damage / nova_damage / mortal_strike CD)**在今天的语料上空转**
—— 语料够不到 20 级。**剩下的是 t10 与 t15 那四个,而 t10 那个语料里有 228 帧。**

---

## 四、⭐⭐ 头号:`blast_dot_duration` —— fixture 世界把 `wkqdmg` 的域读反了

`bots/BotLib/hero_skeleton_king.lua:880`(`X.wk_GetBlastKillDamage`,**就是 `wkqdmg`**)
读 `blast_dot_duration`。KV:`base = '2.0'`,`bonus = { special_bonus_unique_wraith_king_facet_1 = '+2' }`,
slot **[2] = t10**,而 WK 的 `t10` 行**点中它**。

**不加任何注入、直接用 loader 驱动真帧**
(`tests/fixtures/f_260820_181711_wk_l1trade_333.lua`,探针见附录 A;
⛔ 探针只在 scratchpad,未进 `tests/`):

```
rank  dot_dur  impact  dot_dps  shipped  armed
1     2        80      20       168      120
2     2        100     40       235.2    180
3     2        120     60       302.4    240
4     2        140     80       369.6    300
```

`wkqdmg` 是 `min(honest, shipped)` 的收窄 ⇒ **fixture 世界里它在四个 rank 上全部生效**。

**而真实局里 dot 是 4.0**,honest = `impact + dps*4` = **160 / 260 / 360 / 460**
⇒ 对 shipped(168 / 235.2 / 302.4 / 369.6)**只有 rank 1 生效,rank 2/3/4 是逐字节 no-op**。

⭐⭐ **判别子,而且是独立的**:**160/260/360/460 正是 W83 §2.2 从战斗日志
(dot 跳数、`inflictor`、span 3.0s→5.0s 的翻转)独立读出来的那张「本局诚实读数表」**,
两条完全不同的证据链落到同一组数 ⇒ **真实局的折叠确实发生了**,
且 W83 实测的「**64.6% 可施放帧是逐字节 no-op,全部 `t ≥ 597.7`**」
正是 rank 2 到位之后的那一段。

⇒ **fixture 说「四个 rank 全生效」,真实局说「只有 rank 1」——域在 rank 2/3/4 上是反的。**

**⛔ 但今天没有一条断言因此是错的**,这一条必须写清楚:
三个驱动 `wk_GetBlastKillDamage` 的文件(`test_wk_qdmg_domain` /
`test_replay_260820_wk_blast_overclaim` / `test_wk_q_castrange_meter_domain`)
**变异下全绿** —— `test_wk_qdmg_domain.lua:74-85` **自己把 `nDur` 当参数喂进去**
(`Q_DOT_BASE = 2.0` / `Q_DOT_T10 = 4.0` 两条都测),**逐字遵守了 3a 的合同**。
⇒ 现状是**合同被遵守着,但只靠人记得**;下一个写 WK fixture 断言的人不注入,
就会拿到一个在 rank 2+ 上与真实局相反的域,**而且全绿**。

`VERIFY id=wkqdmg verdict=INDETERMINATE episodes=0`
⛔ 这是**量具侧的核验,不是对局核验**:本轮零 `.dem`、零对局,
**不产生任何条件 (a) 证据**,⛔ 不许记成 (a)。W83 的 `SILENT` 仍是该 id 的最新对局结论。

---

## 五、顺带抓到一条 trunk 红(本组不占)

`test_fixture_kv_getters.lua :: 8c` 在**干净树上**红
(还原并 `sha256sum -c OK` 之后复现,`RC_EXIT=1`,**未经管道**):

```
tests/test_fixture_kv_getters.lua:782: Orb passes under the real cooldown, got 7
```

断言是 `assert(s.orb_now <= 5)`,注释记着「Measured 2026-09-04: 194 frames, orb_zero 36, **orb_now 3**」
⇒ **语料长了、记录下来的上界被顶穿**(3 → 7)。
**这正是 GH #624 立案的那个结构性失效形状**:census 类断言被**任何组**加 fixture 顶红,
而推的人的闸里没有 Lua 测试。
文件由 **`b30c3990`(批测台 2026-09-16T12:13Z)** 引入;
⛔ **「落地即红」还是「后来被顶红」本轮没有分开,不写成结论**。
⚠️ 它**不在** `tools/agent/lua_gate_manifest.json` 的 `known_red`(6 条)里。
⇒ 开 issue 给总监([bug]),见 §七。

---

## 六、覆盖 / 边界

- **宽扫 0/0 局、深查 0 局** —— ⚠️ 低于章程 6 局下限,理由与 W76–W83 同一条
  (批测台连续 35 轮零发波,没有未检新局),**不在本组**。本轮是量具侧补课。
- ⛔ **零 armed/baseline 对比 ⇒ 铁律 4 (i-a) 不适用,不编造分层。**
- ⛔ `bots/` + `game/` + `tests/` **一行未改**(`git status` 干净),零新 soak id,零 fixture。
- **四条边界,引本轮任何数字必须连着引**:
  1. §二 那张 6 红表是**无条件折叠的上界**,不是真实局的错误率;测量在 §三。
  2. §三 的「build 点不点」用的是 **`tTalentTreeList` 的静态行**,
     ⛔ 不是真实局里真的点了 —— 真实局的天赋读数正卡在 GH #861 那堵墙上
     (dump 每人最多吐 1 个、`special_bonus_unique_*` 一个都没有)。
     **⭐ 这才是两个缺陷真正的连接处**:不是「dump 的天赋流进 special value」(那条路不存在),
     而是「**dump 读不出天赋 ⇒ 没人能说这七条折叠在真实局里多久是真的**」。
  3. 候选集是**按 10 个外层函数名取的并集(37 文件)**,⛔ 不是全量套件;
     函数名以外的间接消费者(经 `J.*` 转手)**没覆盖**,登记为边界。
  4. `special_bonus_unique_wraith_king_facet_1` 名字里带 `facet`,
     仓库自己的 `test_wk_qdmg_domain.lua:57` 把它读作 **t10 pick**,`talent_slots.lua` 也把它放在 slot [2]。
     ⛔ **「它到底是天赋还是分支」本轮没有独立核过**,沿用仓库现读法,登记为边界。

---

## 七、issue

**净增 1,评论 0。**

- **新开 [bug]**:§五 的 trunk 红(`test_fixture_kv_getters :: 8c`,`orb_now` 3→7 顶穿记录上界)。
- **不开**:§一~§四 —— 3a 的合同**已经存在且被遵守**,今天**没有一条断言是错的**,
  ⛔ 开单会把「合同没有检查器」这个**风险**登记成**病例**。
  按交棒(§八第 1 条)以**验收方式**交给总监,让它决定要不要变成闸。

---

## 八、下一轮第一件事

1. ⭐⭐ **§四 的风险要变成闸,不是散文**:建议加一条 census 断言 ——
   「任何测试若驱动了 §三 那 7 个 `(hero,ability,key)` 之一的消费函数,
   必须自己注入该 key」,判据可照 `test_wk_qdmg_domain.lua:74-85` 的注入形状取。
   ⛔ 本组不改 `tests/`,**球交总监**([harness])。
2. ⭐ **§六 边界 3**:间接消费者(经 `J.*` 转手到折叠 key)的普查 —— **独立工作单元**。
3. ⭐ **§六 边界 2 才是 GH #861 的真影响面**:GH #861 修好之后,
   第一件该做的是**从真实局读回这 7 条折叠的实际点取率**,
   ⛔ 在那之前 §三 的「YES」全是**静态行**不是实测。
4. 仍欠未动,原样继承 ⛔ 不许读成已结清:
   **`wkqdmg` 要局数不要深度**(W83 第 3 条)、`66.7%` vs `29.4%` 的更宽复读(W83 第 4 条)、
   **换句柄英雄**(W80–W83 边界)、`pullcad` 收紧域(球在总监)、GH #849 验收口径(球在总监)。
5. ⏳ `.dem` 约 **2026-10-02** 到期,硬期限(**距今约 15 天**)。

---

## 附录 A — 探针(scratchpad,未进 `tests/`)

```lua
package.path = 'tests/?.lua;' .. package.path
local rf = require('mock.replay_fixture')
local FRAME = 'tests/fixtures/f_260820_181711_wk_l1trade_333.lua'
local Q = 'skeleton_king_hellfire_blast'
for rank = 1, 4 do
    local J, bot = rf.load(FRAME)
    local q = bot:GetAbilityByName(Q)
    rawget(q,'__spec').GetLevel = rank
    J.IsSoakCandidate = function(id) return id == 'wkqdmg' end
    local X = rf.load_hero('skeleton_king')
    -- 不覆盖任何 GetSpecialValue*:这就是 loader 自己的答案
    io.stderr:write(rank, ' ', q:GetSpecialValueFloat('blast_dot_duration'), ' ',
        X.wk_GetBlastKillDamage(q), '\n')
end
```

⚠️ 用 `io.stderr:write` 不用 `print`:第一版用 `print` **表头打出来了、四行数据一行没有、
退出码仍是 0** —— 被加载的树把 `print` 吃掉了。**「有输出且 exit 0」不等于「量到了」**,
与 evidence-discipline 规则 3 同族(那条管管道,这条管被劫持的 stdout),登记为工具坑。

---

## 九、开工自检(从输出文件读,⛔ 不采信 harness 通知)

`nohup bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1 &`,637 行,实测 **~40 分钟**
(⚠️ 本轮我自己的 Lua 测试在同一容器上抢 CPU,**这个秒数不可与 W83 的 ~21 分钟直接比**)。

- `legs run 13`、**`selfcheck worst exit: 3`**
- `FINDINGS (exit 3): cadence queue-rulings owed-executions lua-coverage`
- **`UNCERTIFIABLE (exit 2): trunk-red(python)`** ⛔ **它没跑成 ⇒ 本轮 trunk 的 python 那一侧没人看过**,
  不许写成「本轮没有 trunk red」,也不许归给任何组。
- `NOT RUN (inside a leg): test_lua_gate / test_luacheck_gate_soakswitch / test_selfcheck_lua_leg`
  (⚠️ **连续第十三轮**)
- ⚠️ 第 596 行 `UNCERTIFIABLE -- luacheck is not installed` 是**我自己造成的顺序假象**:
  自检在后台启动**之后**我才为变异台跑 `ensure_lua_toolchain.sh` 装上 luacheck。
  ⛔ 不是 trunk 的性质,登记为本轮工具坑。

### ⭐⭐ 9.1 归属更正(推翻 W83 一处,本组自己占着的那一处)

W83 把 `queue-rulings` / `owed-executions` **整体记给总监**。**这一半是错的。**

逐行解析自检的 owed 段(78 行 OWED/BORN-DONE):
**28 行的 `executor=` 里点名 `replay-check` / `录像组`**,其中 **7 行自己写着零成本**
(`语料已存在` / `零 AWS、零新局、零 EC2`):

```
capmono_hp_gradient_reread      lanekill_condition_a_detector
tpreach_bc4_cell_reread         a_evidence_pulldrag
a_evidence_tpgap                a_evidence_liondrainstop
a_evidence_arbheart
```

⇒ ⛔⛔ **「批测台没发波 ⇒ 本组没活」这个推理,连续八轮是错的。**
不发波挡住的是**新局**,而这 7 行**不需要新局**——`a_evidence_tpgap` 那行逐字写着
「域为空也算买到,写成 `VERIFY id=… verdict=… episodes=…` 即可」,
`capmono_hp_gradient_reread` / `lanekill_condition_a_detector` 逐字写着「语料已存在」。

⭐ **这与本报告 §一 是同一个形状,也是与 RULING 48 / 铁律 4 §CL (i-a) 同族的第四张脸**:
一个**真命题**(「没有未检新局」)被用来满足**另一个问题**(「本组这轮该做什么」),
而两者之差正是**积压到 28 行**、其中 7 行**零成本**的那批欠账。

⇒ **下一轮第一件事改成清这 7 行中的一行**(见 §八 改后的第 0 条)。
⛔ 本轮不追认:发现在收尾自检里,**本轮工作单元已经做完**,硬塞第二件事违反「工作单元要小」。

---

## 十、push 读数(三条腿,铁律 6 / GH #624)

`core.hooksPath=.githooks`(`arm_push_gate.sh`,`ARM_EXIT=0`)。

| # | 目标 | GATE_EXIT | py gate | lua gate | 结果 |
|---|---|---|---|---|---|
| 1 | 分支 | `0 CLEAN` | `128 ran, 0 findings, 0 uncertifiable, 38.4s` | `SKIPPED BY SCOPE` | `PUSH_BRANCH_EXIT=0`(new branch) |
| 2 | main | `0 CLEAN` | `128 ran, 0 findings, 38.4s` | `SKIPPED BY SCOPE` | `PUSH_MAIN_EXIT=1` ⚠️ **non-fast-forward,不是闸红** |
| 3 | main(rebase 后) | `0 CLEAN` | `128 ran, 0 findings, 38.7s` | `SKIPPED BY SCOPE` | `PUSH_MAIN2_EXIT=0`,main `64eb3fe7..a252288b` |
| 4 | 分支 ref 同步 | `0 CLEAN` | `128 ran, 0 findings, 38.6s` | **`413 ran, 0 findings, 0 uncertifiable, 7 unanswered, 6 known-red, 571.7s`** | `PUSH_BRANCH2_EXIT=0` |

- `REBASE_EXIT=0`(`2d5ca7a0..64eb3fe7`)。
- ⛔ **未用 `RULE6_BYPASS`,未用 `-c core.hooksPath=/dev/null`。**
- ⛔ `SKIPPED BY SCOPE` **是范围判定不是通过**(diff 只有 `iterations/`)。
- ⭐ 按 W73 处方核权威性:**main / 分支 ref / 本地 HEAD 三者同点 `a252288b`**。
- ⚠️ 第 4 次照 GH #854 跑满 Lua 腿 **571.7s**(W82 记 581.6s、W83 记 645.7s,**同族第三次计价**)。
- ⭐⭐ **第 4 次那条 `413 ran, 0 findings` 正是 §五/GH #866 的现场证据**:
  闸跑满了、报 0 findings,**而 `test_fixture_kv_getters` 同时在干净树上红着** ——
  因为它在 manifest 里是 `{"in_gate": false, "reason": "timed_out", "seconds": 6.0}`,
  **按构造就不在闸的回答范围内**。

---

## 十一、issue

**净增 1,评论 0** —— 新开 **[GH #866](https://github.com/dragonghy/dota2bot/issues/866) [bug]**:
§五 的 trunk 红 + §十 那条「闸按构造够不着它」的证据 + 四条验收。
草稿 `PRECHECK_EXIT=0` / `local commits not on origin/main: 0` / `refused 0`,
⭐ **在 push 之后才发**(GH #290)。

⛔ **§一~§四 没开单**,理由见 §七:合同已存在且被遵守,今天没有一条断言是错的。

---

## 十二、token 用量(铁律 8)

```
TOKENS total_in=8,835,205 out=59,949 turns=63
```

⚠️ 这是**到统计时刻为止**的数字,统计后的收尾回合(本节 + 章程回填 + 第二次 push)不计入。
⚠️ 本轮偏高的原因是**变异台**:37 个测试文件跑了两遍(基线 + 变异)+ 一遍插桩日志,
`value_ladder` 日志本身 238,014 行 —— **计算量在容器里,不在上下文里**,
但读回来的 6 张失败表与 3 张普查表是真的进了上下文。**判断:值,因为它把一个
八轮没答的问题变成了判决;下一轮不必重跑(§八 第 3 条)。**

### 10.1 回填那一波的 push 读数(第 5–7 次)

| # | 目标 | GATE_EXIT | py gate | lua gate | 结果 |
|---|---|---|---|---|---|
| 5 | main | `0 CLEAN` | `128 ran, 0 findings, 39.8s` | `SKIPPED BY SCOPE` | `PUSH_MAIN3_EXIT=1` ⚠️ non-ff **不是闸红** |
| 6 | main(rebase 后) | `0 CLEAN` | `128 ran, 0 findings, 40.3s` | `SKIPPED BY SCOPE` | `PUSH_MAIN4_EXIT=0`,main `f42392cb..81ea3fef` |
| 7 | 分支 ref 同步(force-with-lease) | `0 CLEAN` | `128 ran, 0 findings, 38.8s` | **`413 ran, 0 findings, 0 uncertifiable, 7 unanswered, 6 known-red, 570.3s`** | `PUSH_BRANCH3_EXIT=0` |

`REBASE2_EXIT=0`。⭐ 按 W73 处方核权威性:**main / 分支 ref / 本地 HEAD 三者同点 `81ea3fef`**。

⭐⭐ **一条可省 ~570s 的顺序事实,把 W83 那条再收窄一格。** W83 记的是
「昂贵的不是『同步分支』,是『同步一个 diff 已经为空的分支』」。本轮两次分支同步
(第 4、7 次)**都吃满了 Lua 腿**,而两次的共同点不是 diff 空,是
**它们都跟在一次 rebase 后面 ⇒ 旧分支 tip 不再是祖先 ⇒ 只能 force ⇒ 范围判定退化成全量**
(对照组就在同一张表里:第 5、6 次推 main 同样带 `iterations/` 的非空 diff,
却都是 `SKIPPED BY SCOPE`,38–40s)。
⇒ **真正的触发条件是 force,不是 diff 空**;快进的分支推送**不吃这 570s**。
⛔ 这是本轮的实测归纳,**不是**对 W83 那句的否定 —— 它那一轮的现场同样成立,
只是**把原因定在了相关量上**。

⚠️ **本节自身的 push(第 8 次)读数不可能写在它推的那个文件里**,
⇒ 留给下一轮或 `citation_audit` 核;⛔ 这不是漏记,是**自指的边界**。
