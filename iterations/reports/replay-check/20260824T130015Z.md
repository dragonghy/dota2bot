# 录像检查组 2026-08-24T13:00:15Z(第六十一次触发)—— W5 `cmboots` 的条件 (a)

**一句话**:W5(`cmboots` 独占波,03:10Z 发、12:17Z 才被补记收割)是**全新未检语料**,
本轮把它整波扫完并给出 (a) = **WORKING**;更要紧的是 (a) 这次不止买到「买了没有」,
还第一次买到了**「用了没有」** —— `.dem` 事件流能看见 Replenish 的每一次激活,
而 CM 源码注释里写着这条「语料完全看不见(GH #100)、属论证不属实测」。
零 EC2 支出,`bots/`/`game/` **0 改动**,gate 未动。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` worst exit **3**:

- **UNLANDED 9 commit**,其中 **总监的 `creeppull`+`pullbeat` PROMOTE 仍在支线**
  (`origin/claude/busy-bardeen-uxvcdx` / `-u45ms4`)。**本轮独立复核并确认:**
  `git grep 'creeppull' origin/main -- bots/FunLib/jmz_func.lua` 仍打到
  `jmz_func.lua:7125  if not J.IsSoakCandidate( 'creeppull' ) then return nil end`
  ⇒ **owner P1 的 promote 到此刻仍未生效**。批测台 12:17Z 已在报,**本组不重复开 issue**,
  只把「第二个独立读数」记在这里:棒子在总监。
- cadence 六个 finding(五条流全部在报,08-24 02:05Z 之前集体停摆约 10 小时)。本组上次触发 00:59Z。
- trunk python **18 passed / 0 failed**。

## 1. 覆盖(章程吞吐要求)

| 层 | 数字 |
|---|---|
| **宽扫** | **233/233** 有 `.dem` 的局(W5 四 run 全量,**按 run 分目录**);297 局有 `analysis.json`,24 局暖场 ⇒ **有效 209 局** |
| **深查(逐帧)** | **8 局**(3 run、4 seed、两个物理层、两条腿) |
| 波次 | W5 = `spot_20260824_0310{28,31,33,36}_1_5a694f5…_{a6cb45,a2b6a9,1d9ae5,a3fa29}` |
| armed 串 | `cmboots` 独占(273 局 stamp 全部 `mirror:cmboots:s<seed>:<side>`,暖场 24 局全 `5a694f5`) |

**踩到并处理了「`.dem` 同名跨 run 撞车」**:233 个 tag 里 **22 组撞名**;
下载与解析全程按 run 分目录 + `(run, tag)` 二元组做主键 ⇒ 没丢局。

**W4 剩余 ~32 局的补扫(上轮登记的优先 (1))本轮没做** —— W5 是**整波未检的新语料**,
按章程第 1 条优先于历史补课。诚实登记,顺延一轮。

## 2. ⚠️ 先纠正我自己的一个会读反结论的分组错误

`pos = analysis.json:team_slot % 5 + 1` **不等于** bot 脚本里的 `sRole`。

按 `team_slot` 分组,seed 888 有 **12/70 局** 显示「CM 在 pos_5」,而 `cmboots` 只改 pos_5 的买表
⇒ 看起来直接推翻批测台 §2.3「seed 888 是**结构性零腿**」那条(它是本桌第一个波内噪声底的论据)。
**逐帧一看是我错了**:888 的 CM 全程走 `item_priest_outfit`(`urn_of_shadows` / `mekansm` /
`headdress` / `chain_mail` 链),**两条腿都原生持秘法鞋**。帧例
`a6cb45/20260824_033613_slot11`(**armed** 腿):`boots_of_speed` 82.5 → `sobi_mask` 172.5 →
`ring_of_basilius` 198.5 → `wizard_hat` 252.5 → `arcane_boots` **351.5** → `urn_of_shadows` 393.5
→ `mekansm` 590.5。pos_5 表的 `blood_grenade` 首项在 100.5 才出现且没有 `null_talisman`。

⇒ **批测台的「结构性零腿」读法逐帧成立**,而且现在它不再是工具断言(`seed_draft.py` 的位置众数),
而是**帧证据**。见 §4 R9:888 的两条腿在 Replenish 次数、收到次数、`mp<100` 占比上**逐项相同**。

**章程该加一句**(#57 的加强):位置不能按种子推,**也不能按 `team_slot` 读**;
唯一可靠观测的是 gate 的**队伍子句**(`jmz_func.lua:4654`,`GetTeam() == armed side`)。
以下全部按「CM 所在队 == armed 侧」分组,角色不猜。

## 3. 所有权:GH #144「验收第一条」兑现

| | n | 持过 tranquil | 持过 arcane | 持过 boots_of_bearing |
|---|---|---|---|---|
| CM 在 **armed** 队 | **106** | **0(0.0%)** | 106(100%) | **0** |
| CM 在非 armed 队 | 103 | 72(69.9%) | 31 | 1 |

**分两层(铁律 4(i)),两层同号且都是 0**:`armed_side=radiant` **0/60**、`armed_side=dire` **0/46**。
非 armed 腿的 31 局 arcane 里 **30 局是 seed 888**(§2);**剩 1 局是真残余 ⇒ GH #156**。

**合成路径逐帧对上**(同 seed 896 的一对):

- **ARMED** `1d9ae5/20260824_031904_slot7`:`boots_of_speed` t=247.5 → `sobi_mask` 270.5 →
  `ring_of_basilius` 329.5(吃掉 sobi_mask)→ `wizard_hat` 355.5 →
  **`arcane_boots` t=428.5,同帧消耗 `boots_of_speed + ring_of_basilius + wizard_hat`**。
- **非 armed** `1d9ae5/20260824_033503_slot9`:`boots_of_speed` 223.4 → `wind_lace` 264.4 →
  **`tranquil_boots` 285.4**;`belt_of_strength + wind_lace + headdress` → `ancient_janggo` 464.4。

⚠️ **一个看起来像副作用、其实不是的读数**(差点写成结论):全语料 armed 腿
`ring_of_basilius` **+88.2pp**、`sobi_mask` **+75.8pp**、`wizard_hat` **+57.7pp**(两层同号)。
**这三个就是当前版本秘法鞋的合成件**。组件只有在**装配期间**停留在栏位里才会被 1Hz 快照看见,
所以「组件占比」量的是**装配时长**,不是「买表被改动了别的地方」。
`energy_booster` 反而一次都没进差异表 —— 因为它根本不是这一版的合成件。
**教训:组件占比差异在读成行为差异之前,必须先拿一帧确认合成关系。**

## 4. ⭐ 执行:第一次买到「用了没有」

CM 源码注释(`hero_crystal_maiden.lua:86-91`)写着本案立论靠 Replenish,
「而语料完全看不见(GH #100)—— **argued, not measured**」。
`.dem` **事件流看得见**:

```
events[] type=='ITEM'          actor=<hero>  inflictor='item_arcane_boots'
events[] type=='MODIFIER_ADD'  target=<hero> inflictor='modifier_item_arcane_boots'
```

每局均值,**分两层报 + 平衡估计量**(铁律 4(i) + 08-23T21:00Z 的修正:反号先算平均再判):

| 量(每局) | armed=radiant 层 | armed=dire 层 | **平衡** | 去掉 888 |
|---|---|---|---|---|
| CM 自放 Replenish | 3.200 vs 2.317 | 3.826 vs 0.000 | **+2.355** | **+2.988** |
| CM 收到 Replenish | 2.067 vs 1.067 | 2.000 vs 0.000 | **+1.500** | **+2.000** |
| CM 技能施放数 | 37.40 vs 36.55 | 37.11 vs 33.88 | **+2.04** | +1.80 |
| 存活帧 `mp<100` 占比 | .091 vs .108 | .087 vs .141 | **−3.5pp** | −5.3pp |
| 存活帧平均 `mp_pct` | .653 vs .610 | .645 vs .569 | **+5.9pp** | +8.9pp |

**五行两层全部同号。**

- **阴性对照**:**队友**放的 Replenish 次数,平衡 **−0.38 且两层反号**(+1.05 / −1.81)
  ⇒ 装置没有整体漂移,上面五行不是「armed 侧什么都多」。
- **SILENT 率 2/106**(armed 腿一局都没放的只有 2 局,`radiant` 层 1 局 seed 896、`dire` 层 1 局 seed 895)。
- **`mp<100` 是严格下界**:离线没有 per-ability 蓝耗表(charter 2026-08-20 的成本),
  100 低于 CM 任何等级的任何一个技能 ⇒ 真实的「有就绪技能却付不起」比例只会更高。**当地板读,不当比率读。**
- **她自己那双不是冗余的**:非 armed 腿队友每局放 2.8–4.4 次,而 CM **收到 0.00–1.07 次**(1200 半径,站位远)。
  帧例 `1d9ae5/033503_slot9`:队友 lina、zuus 放了 **11 次**,CM **收到 0 次**。
- **R9 零腿自检**:seed 888 两条腿 —— 自放 4.67 vs 4.50、收到 2.00 vs 2.07、`mp<100` .098 vs .084。
  **逐项相同**,与「结构性零腿」一致(§2)。

## 5. ⚠️ 代价(建议一并写进 (c) 的登记)

- **鞋子完成推迟**:arcane 中位 **394–420s** vs tranquil **313–344s**(两层同号,**+70~85s**)。
  单局对读 428.5s vs 285.4s = **+143s**。
- **战鼓 `ancient_janggo` 完成率 −21.9pp**(两层同号 −17 / −27);单局 564.5s vs 464.4s = **+100s**。
  1300g vs 925g 的 **375g** 差价,在 10 分钟封顶的 Turbo 里直接推掉下一件。
- **死亡数两层反号**(−0.73 / +0.08)⇒ 按铁律 4(i) **不读**。
- 这不是反对 promote,是让 resolve 有完整账本:**买到的是蓝,卖掉的是一件装备的进度**。

## 6. 结论

**`cmboots` 条件 (a) = WORKING**,三层都有帧证据:
所有权(106/106 arcane、0/106 tranquil、0/106 bearing)+ 激活(自放 +2.4/局,SILENT 2/106)
+ 立论机制(`mp<100` −3.5pp、平均蓝 +5.9pp、技能数 +2.0/局,五行两层同号)。
(b) 批测台 12:17Z 已判通过。**(c) 与 resolve/promote 在总监,本组不裁。**

## 7. 跨组交付

- **GH #144 追评**:(a) = WORKING 全文 + §2 的分组更正 + §5 的代价登记。
- **GH #100 追评**:物品层有**第二个证人**(`.dem` 事件流,结果侧);
  本 issue 的口径决定价值不减(过程侧仍然黑),但「物品层完全不可观测」的转述过宽了。
  建议英雄组把 `hero_crystal_maiden.lua:86-91` 那句注释改成范围限定的说法。
- **GH #156 新开([bug],交总监)**:非 armed 腿 1/103 局漏成秘法鞋,承重帧
  `a2b6a9/20260824_032512_slot8` t=237.4,领头嫌疑是 `tEarlyBoots` 这条不读角色买表的鞋子通路
  (若属实则不是 gate 泄漏,而是所有改鞋买表候选都带 ~1% 对照腿污染),
  附反向可能(文件作用域求值 + `tSoakSideCache`)与它为什么变弱(armed 腿 0/106 反向泄漏)。
- **不申请波次,不花 AWS 钱。**

## 8. 工具交付(本轮新写,留在 scratchpad,未进仓)

三个一次性脚本(`extract_cm.py` / `extract_cm2.py` / `cmboots_exec.py`)。
**诚实登记:本轮没有把它们收进 `tools/batch_test/behavioral/`**,因为其中两个是 `cmboots` 专用的。
**值得进仓的是第三个能力**:「从 `events[]` 读某件主动物品的激活次数 + 收益者」是**通用**的,
任何主动物品候选(urn、glimmer、force staff、微光)都能用同一段。
**下一轮优先做这件**(见 §10 (2))。

## 9. 验证(铁律 6)

`bots/` / `game/` **0 改动** ⇒ 铁律 6 的 Lua 两条无适用对象
(容器无 `luacheck` / `lua5.1`,**不声称跑绿过 Lua 全量**)。
开工自检 trunk python **18 passed / 0 failed**。
**AWS**:`session_setup.sh` ⇒ `arn:…:user/dota2bot-agent` ✅;
S3 **只读**(`dem21/` + `soak/` 下载),EC2 **$0**,**未调用 Cost Explorer**。

## 10. 下一轮优先

1. ⭐ **W4 剩余 ~32 局补扫**(上轮登记,本轮为新语料让路,**顺延一轮**)。
2. ⭐ **把「主动物品激活计数」做成通用检测器进仓**(`tools/batch_test/behavioral/`),
   本轮已实证它能回答 `.dem` 侧的 (a) —— 这是本组第一次买到「用了没有」,别让它留在 scratchpad 里。
3. ⭐ **ab/ba 分层 + 平衡估计量回灌 `fieldbuy_silence.py` / `stayfield2_margin.py`**
   —— **连续第五轮登记、连续第五轮没做**(本轮又只把纪律用在了新脚本上)。
4. `pullbeat` 差分波(#149)落地后的 (a),验收量用 `FLIP` 拼法。
5. `stayfield`(TP 腿)在 205 局上补第一失败子句分解。
6. 打野反证 fixture 钉 `002103_slot5 t=634.2 skeleton_king`(**已顺延十一轮**)。
7. `hero-1` 的 153 局 WK 语料(棒子挂 **19 轮**);`l5combo` 的 (a)(**连续第三十五轮**);
   `make_fixture.py` 钉 `062551 t=205.5 jakiro`(#45,**连续第二十二轮顺延**);`axebuyblink` armed 的波次。

## 11. Token 用量(铁律 8)

```
TOKENS total_in=8,537,506 out=64,245 turns=67
```
