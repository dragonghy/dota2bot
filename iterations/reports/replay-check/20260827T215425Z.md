# 录像检查组 — 2026-08-27T21:54Z(第八十六次触发)

**一句话**:W18 首检。接上一轮 backlog 第 1 棒,**那条预登记这次真的执行了** ——
W18 是 `abilanc` 拿到的**第一份两个物理分层都在的语料**(45 radiant-armed / 23 dire-armed,68 局带录像),
域**第一次不空**(8 发 under-tier 远古施法)。但同一份语料给了一个比 (a) 更值钱的读数:
**一条 provably 空的通道(zuus,两腿字节相同的代码)在同一份语料上读出物理侧 3.94×、
而两个分层的 armed 效应互为反号(−1.12 / +5.26)** —— 噪声比信号大、侧别比腿大,
这是本仓库到目前为止对能力层检测器噪声底的最硬一次量化。
零 EC2 支出,`bots/`/`game/` **0 改动**,gate 未动。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` — **worst exit 3**(有发现,非未核验):

| 腿 | 结果 |
|---|---|
| unlanded work / 报告节奏 / 未裁队列 | RIDESHARE 1(`hero-21`)/ OTHER 0;total open **37** |
| stable-v* 锚点 | `stable-v1` / `stable-v2` 两个都 ok |
| trunk python 套件 | **44 passed / 0 failed / 0 uncertifiable** |
| trunk 快 Lua 检测器 | **13 文件 0 失败**(⚠️ FAST SUBSET) |

---

## 1. 覆盖数字(章程固定节)

- **宽扫**:W18 stamp 普查 **154/154 局 = 100%**(四个 run 全部 `analysis.json`);
  行为宽扫 **80/80 局**(两个双腿 run 的全部带 stamp `.dem`:998ecd 42 + 843688 38,**零 dumper panic**)。
- **深查(逐帧)**:**6 局**(章程下限 6)——
  `998ecd/20260827_183600_slot1`、`998ecd/20260827_184717_slot10`、`843688/20260827_185943_slot12`、
  `998ecd/20260827_185909_slot7`、`998ecd/20260827_183622_slot11`、`843688/20260827_182218_slot8`。
  前三局是域内 8 发施法的承重帧,后三局是反向护栏(甲)计数最高的三局。
- **每 id 触发计数表**:本轮只对 `abilanc` 做逐 id 核验(§3–§6)。

---

## 2. ⭐⭐ W18 的 arm 串是真的,**而且第一次有 dire 腿**

| run | seed | stamp 分布 |
|---|---|---|
| `…_998ecd` | 918 | `mirror:<40-id>:s918:radiant` ×35 + **`:s918:dire` ×18** + 暖场 ×6 |
| `…_843688` | 924 | `mirror:<40-id>:s924:radiant` ×34 + **`:s924:dire` ×15** + 暖场 ×6 |
| `…_e17d24` | 926 | `mirror:<40-id>:s926:radiant` ×26 + 暖场 ×6 |
| `…_f43908` | 921 | `mirror:<40-id>:s921:radiant` ×6 + 暖场 ×2 |

- arm 串 **354 字节,sha1 `e388d41c`**,与 `iterations/streams/test_set.md` **第 2 行逐字节相同**
  (`sha1sum` 双向对拍)。**GH #253 的形状(cand 位放种子号)在 W18 上没有复发。**
- **两粒种子(918 / 924)ab、ba 两腿都落地** ⇒ 这是上一轮 §4 点名「唯一能买到 `abilanc` (a)
  的语料形状」。本轮就用这两个 run(68 局带录像)做读数;e17d24 / f43908 是单腿孤儿,
  按上一轮结论**不进任何 armed/baseline 差值**。
- **登记一处自我更正**:上一轮报告把 W17-R 的 arm 串记成「374 字节」。本轮重测 W17-R 得 **354**,
  与 W18 逐位相同 —— 374 是 **`mirror:` + 串 + `:s918:radiant` 整条 stamp** 的长度(7+354+13),
  不是 id 串的长度。**结论(与 test_set 第 2 行逐字节相同)不受影响**,但下一轮若拿 374 去对拍会读出假差异。

---

## 3. ⭐⭐ `abilanc` 的域第一次不空,而两个分层反号 ⇒ **REFUSE**

`abilanc_domain.py --selfcheck` **ALL PASS**;主读数(68 局,`.sweep_out` 两个双腿 run):

```
DOMAIN (§BL.3) -- casts at an ancient by a hero below level 12
  arm side    games     armed  baseline     delta
  radiant        45    4/0.089    0/0.000     0.089
  dire           23    0/0.000    4/0.174    -0.174
  pooled          -         4         4   two-layer: OPPOSED => NOISE (rule 4i)

  ... split: `under` band (<10)     全部 0 / 0 / 0 / 0
  ... split: `band` (10..11)        与 DOMAIN 逐位相同

=== VERDICT  REFUSE
```

**⭐ 池化读数 4 vs 4 是完美的平** —— 若只看池化会写成「量过了,没效应」。分层一拆就翻:
**4 发 armed 全在 radiant 层,4 发 baseline 全在 dire 层。**

按 `abilanc_domain.py:288` 的 `armed_team = RADIANT if side=='radiant' else DIRE`
逐格还原物理队伍:`(radiant, armed)` 与 `(dire, baseline)` **两个格子都是 radiant 队**。

> **域内 8 发 under-tier 远古施法,8/8 全部出自 RADIANT 队,dire 队一发都没有。**

这是「Radiant 侧偏」的行为检测器版,在 n=8 上把腿效应完全吃掉。铁律 4(i) 的 REFUSE 是对的。

**`under` 段(<10)全语料 0 发**,第三次复现 §BL.3 boundary 3 的诚实先验(Turbo 升级快)——
`abilanc` 的整个域住在 10..11 这两级里。

---

## 4. ⭐⭐ 逐帧:4 发 armed 残留**一发都不能归给 gate 泄漏**

工具把残留分两类打印,本轮的源码侧裁决(章程规定这一半归人):

| 帧 | 英雄/技能 | 工具标签 | 逐帧裁决 |
|---|---|---|---|
| `998ecd/183600_slot1` t=763.5, 764.3 | phantom_assassin lvl11 `phantom_strike` → `big_thunder_lizard` | **REACHABLE**(需裁决) | **裁为「不是本 gate 的泄漏」**,见下 |
| `998ecd/183600_slot1` t=766.9 | spirit_breaker lvl10 `charge_of_darkness` → `small_thunder_lizard` | not reachable | 确认:SB 文件里没有被守选择器,**但另有问题**,见 §5 |
| `998ecd/184717_slot10` t=733.5 | spirit_breaker lvl10 `charge_of_darkness` → `prowler_acolyte` | not reachable | 同上 |

**PA 那两发的裁决(承重推理,逐帧)**:

1. **`X.ConsiderQ()` 是全树唯一的 Phantom Strike 施法点**(`hero_phantom_assassin.lua:171→176`
   `ActionQueue_UseAbilityOnEntity(abilityQ, castQTarget)`,`grep phantom_strike` 全树只此一处)。
2. **`ConsiderQ` 里唯一能把中立野怪当 target 返回的分支是 `:383`**,即被守选择器那一支;
   其余分支喂的都是 `GetNearbyLaneCreeps`(`:306`、`:404`、`:432`)、敌方英雄、Roshan 或 Tormentor
   —— 逐行读过,**不是转抄判词**。
3. **`:383` 分支自带 `GetUnitToUnitDistance( targetCreep, bot ) >= 600`。**
4. **帧证据把这条子句证伪**:
   - t=**763.1** `DAMAGE phantom_assassin → big_thunder_lizard`(普攻,PA 近战 ⇒ 已在贴身距离);
   - 快照 t=**762.5** 与 t=**763.5** 坐标**逐位相同** `(-4870.7, -307.5)`,**零位移**;
   - Phantom Strike 是**位移到目标**的技能;两发(763.5、764.3)之后 t=764.5 才到 `(-4965.5, -214.8)`,
     总位移 **132u**。若决策瞬间距离真 ≥600u,这两次闪现必然造出 ≥600u 的坐标跳变。
   - PA 全程 `lvl 11`(760.5–767.5 八帧逐帧确认),mp 1.000→0.928→0.853。
5. ⇒ **`>=600` 在决策帧为假,这两发不可能来自 `:383`**。既不是 gate 泄漏,也不构成 gate 生效的反证。

**⇒ GH #261 新开 [bug]**:`abilanc_domain.py` 的残留标签
`REACHABLE via the selector -- adjudicate this one` 是**文件级**推断
(该英雄文件里有没有 `J.GetMostHpUnit` 调用),不是**站点级**的。它不问:
观测到的这个技能是不是那个站点驱动的、该站点自己的前置子句(距离/蓝量/模式)在那一帧能不能成立。
本轮 4 发 armed 残留里有 2 发被这条标签点名「需裁决」,而逐帧两发都够不着站点。
**与 GH #253 / #257 同族:那两条是「缺席被渲染成一个测到的零」,这条是镜像 ——
「够不着被渲染成一次测到的泄漏」。** 建议:标签降级为 `file-level: hero file HAS a guarded site`,
并把站点自身的常量子句(如 PA 的 600u)一起打印出来交给读者。

---

## 5. ⭐⭐ 新问题:`[1]` 族的**第三个成员**没被 `abil1st` 带走

`abil1st`(GH #259 的落地,strategy 19:29Z,commit `4ea4be9c`,**gated**)从 `[1]` 族里
取走了 `hero_zuus.lua` 与 `hero_centaur.lua` 两个站点。**`hero_spirit_breaker.lua:287-296` 是同族第三个,没被取走**:

```lua
-- X.ConsiderChargeOfDarkness()  (:150 起)
local nNeutralCreeps = bot:GetNearbyNeutralCreeps(nRadius)
if nNeutralCreeps ~= nil
and ((#nNeutralCreeps >= 3 and nLocationAoE.count >= 3)
    or (#nNeutralCreeps >= 2 and nNeutralCreeps[1]:IsAncientCreep() and nLocationAoE.count >= 2))
then
    if J.CanBeAttacked(nNeutralCreeps[1]) then
        return BOT_ACTION_DESIRE_HIGH, nNeutralCreeps[2]      -- ← 打的是 [2]
    end
end
```

- **「第一只是远古」被显式写成开火理由,该分支无任何等级子句** —— 与 GH #259 点名的
  `hero_zuus.lua:727` 形状逐项相同。
- 而且它**读 `[1]` 判定、打 `[2]`**(索引错位),`abil1st` 现有的 `J.GetFirstUnit` 改法**套不上**。
- **帧证据**(深查局 2):`998ecd/20260827_184717_slot10` **t=733.5** spirit_breaker **lvl 10**
  从 `(-4851.6, 48.5)` 冲进 prowler 远古营,`greater_bash` 连响 3 次(acolyte×2、shaman×1),
  t=735.5 挨了 acolyte 一下,**t=736.5→738.5 坐标 `(-4620.9,-132.5)→(-4146.8,-224.0)→(-3773.3,-202.3)`
  径直走人,营没拉走**。与 GH #250 / #259 的「戳一下就走」浪费形状同族。
- **不许合取进 `abilanc` 或 `abil1st`**(`pullcad` 陷阱:被合取的 id 一旦 promote,整条门冻结为 FALSE)。
- **⇒ GH #262 新开 [strategy]**,钉帧配方 `184717_slot10 t=733.5 spirit_breaker`;
  **断言顺序**:先断该分支的 `nNeutralCreeps[1]:IsAncientCreep()` 为真,再断「该撤而没撤」那一侧
  —— 出厂腿上 `abil1st` 恒 nil,不能拿 armed 帧钉(#250 已踩过一次)。

---

## 6. ⭐⭐ 这两个 id 的等级门**瞄错了 band** —— 89% 的浪费在门外

把「戳远古营」聚成 episode(同英雄同腿,间隔 >15s 断开),问「15 秒内这只远古死在他手里没有」。
**全语料 68 局、143 个 episode**:

| band | episodes | abandoned(没拿下) | 占比 |
|---|---|---|---|
| **< 12(`abilanc`/`abil1st` 的整个域)** | **5** | 1 | 20.0% |
| **>= 12(两个 id provably 够不着)** | **138** | **41** | 29.7% |

⇒ **两个 id 的可及浪费是 68 局里 1 次;同一份语料里门外的同形浪费是 41 次(0.60 次/局,41× 之多)。**
门下的域小到 **0.074 episode/局** —— 要在腿间看出差别需要的局数是本波的一个数量级以上。
**这是「为什么 `abilanc` 的 (a) 三轮买不到」的结构性答案**,比上一轮的「单腿孤儿语料」更根本:
上一轮的答案本轮已被两腿语料证伪(两腿到手了,域还是只有 8 发)。

**反例帧(深查局 3,同样承重)**:`843688/20260827_185943_slot12` **t=708.0** zuus
**lvl 11**(707.5 刚从 10 升上来)、mp 1.000,对 `black_dragon` 连放两发 `arc_lightning`
(708.0、709.9),**t=709.9 击杀,GOLD+XP 入账**,随后 t=710.7 用掉 `item_madstone_bundle`。
代价:mp 1.000→0.828(**约 17%**)、hp 1.000→0.986→1.000(**约 1.4% 且已回满**)。
**这是一次干净、盈利、低于门槛的远古击杀** —— 若该局 zuus 在 armed 腿且 `abil1st` 已入集,
这次击杀**不会发生**。

⇒ **GH #263 新开 [strategy]**:等级不是那根杠杆,**「打不打得完」才是**。
建议把候选重述为「戳了不拿」的完成度子句(episode 内可预期击杀 / 撤离条件),
而不是 `GetLevel() < 12`;并**同时给出**上表两行,让刀口暴露(铁律 4(ii))。
本组**不裁 promote/reject、不改 bot Lua**,只交读数与帧。

---

## 7. ⭐⭐⭐ 本轮最硬的读数:一条 **provably 空的通道**读出物理侧 3.94×、腿效应两层反号

反向护栏(甲)= level ≥ 12 的远古施法。工具原样输出:

```
REVERSE GUARD (甲) -- casts at an ancient at level >= 12
  arm side    games     armed  baseline     delta
  radiant        45   93/2.067   66/1.467     0.600
  dire           23   75/3.261   41/1.783     1.478
  pooled          -       168       107   two-layer: both layers agree
```

**两层同号、池化 1.57×** —— 这正是读者会当成信号收下的形状。逐英雄一拆(#148(iii)):

| 英雄 | armed | baseline | 该英雄的 armed/baseline **局数** |
|---|---|---|---|
| **zuus** | **97** | **53** | **35 / 33**(基本平衡) |
| phantom_assassin | 44 | 29 | 23 / 13(构成偏斜,不可读) |
| spirit_breaker | 13 | 9 | 23 / 13(同上) |
| skeleton_king | 12 | 7 | 33 / 35 |
| skywrath_mage | 2 | 9 | 10 / 22(反向偏斜) |

**zuus 一个人扛掉整列的 55%(150/275),而且他恰好是曝光最平衡的那一个。**

### 而 zuus 在 W18 里是一条 provably 空的通道

三条独立核过的事实:

1. `hero_zuus.lua` **不在** `J.GetMostHpUnit` 的 19 个消费点里(全树 grep 逐文件核过,§8 表);
   ⇒ `abilanc` armed 与否,zuus 的选人代码**一个字符都不变**。
2. `abil1st`(唯一动过 zuus 远古选人的 id)commit `4ea4be9c` 时间戳 **2026-08-27 19:34:02Z**,
   **晚于 W18 起飞的 18:20Z**;实例克隆的是 run 前缀里那个 sha
   `d9585a29…`(**2026-08-27 05:26:41Z**),`git grep abil1st d9585a29 -- bots/` **零命中**。
   ⇒ 那棵树里**根本没有** `abil1st`。
3. `abil1st` 也**不在** W18 的 40-id arm 串里(§2 全串已列)。

⇒ **W18 两条腿上 zuus 的远古选人代码字节相同。这一列的任何腿间差异,按构造只能是噪声。**

### 那条空通道读出了什么

**先按物理队伍(曝光已平衡:32 / 36 局)**:

| 物理队伍 | zuus 局数 | 远古施法 | 每局 |
|---|---|---|---|
| RADIANT | 32 | 28 | **0.88** |
| DIRE | 36 | 124 | **3.44** |

> **侧别效应 3.94×**,在一条按构造为空的通道上。

**再按腿,曝光归一后分两层报(铁律 4(i))**:

| 分层 | armed 局/施法/每局 | baseline 局/施法/每局 | delta | 比值 |
|---|---|---|---|---|
| radiant | 22 / 26 / **1.182** | 23 / 53 / **2.304** | **−1.123** | **0.51×** |
| dire | 13 / 71 / **5.462** | 10 / 2 / **0.200** | **+5.262** | **27.31×** |

> **两层反号,其中一层 27×** —— 在一条**不可能有效应**的通道上。

### 三行刀口(直接交给 GH #249)

- **零配置差异**在这条通道上造出 **27.31×**(dire 层)与 **0.51×**(radiant 层),**两层反号**;
- **物理侧**在同一条通道上造出 **3.94×**,方向一致、曝光平衡;
- **真 armed vs baseline** 在 `abilanc` 自己的域里(唯一可能有效应的那一列)造出 **4 vs 4**。

**⇒ 侧别 > 噪声 > 信号。** 上一轮给 #249 的材料是「零 armed 语料造 3 倍」;
本轮把它加强成**同一份真 armed 语料内部、一条源码级确知为空的通道、曝光已平衡、分层已拆**,
依然造出 27×。**没有「也许真的有效应」的退路。**

### ⚠️ 本轮自我拦截了一次错误发表

第一版打算写「zuus armed 97 vs baseline 53 = **1.83×**,曝光 35/33 平衡 ⇒ 这是一个腿相关的真差异」。
**那是池化数,正是铁律 4(i) 禁止的那一步**;拆层之后两层反号,池化的 1.83× 全部来自
两层局数不等(radiant 45 / dire 23)的加权。**在报告发出前拆的层,不是发出后改的**;
记在这里是因为下一轮很容易在同一处再踩 —— **曝光平衡不能替代分层**。

---

## 8. 源码侧顺手做掉的一件事:19 个消费点里有 3 个**结构上够不着**

全树逐站点核了「喂给被守选择器的是哪张表」(以前只记过英雄名,没记过喂什么):

| 站点 | 喂入 | 可达性 |
|---|---|---|
| `hero_templar_assassin.lua:369` | **`GetNearbyLaneCreeps`** | **远古永远不在兵线表里 ⇒ gate 结构上够不着** |
| `ability_item_usage_generic.lua:7560` | `GetNearbyNeutralCreeps` | 站点是 `X.ConsiderItemDesire["item_iron_talon"]`,**寒铁钢爪 7.41 已从游戏移除**(`bots/Buff/NeutralItems.lua:56`、`FretBots/SettingsNeutralItemTable.lua:53`、`aba_item.lua:21` 三处**全部注释掉**)⇒ **死码** |
| `rubick_hero/axe.lua:219`、`rubick_hero/bounty_hunter.lua:262` | `GetNearbyNeutralCreeps` | 仅在 Rubick 偷到该技能时可达(条件可达,不算死) |
| 其余 15 个 | `GetNearbyNeutralCreeps` | 可达 |

⇒ 工具 docstring 与 §BL 一直说的「19 个消费点」是 **grep 计数**;
**能力事件观测量的真实可达面是 15 个站点 / 13 个英雄**(+2 个 Rubick 条件站点)。
`tests/test_detector_source_constants.py:940` 那条棘轮校的是
`len(AD_GUARDED) >= 10`(**文件集合大小**),**校不出喂入表换了** ——
`hero_templar_assassin.lua:369` 从中立表换成兵线表,棘轮不会红。
**登记为观测,不自裁**:要不要给「喂入表」加一条棘轮由总监定(见 GH #261 §3)。
顺带一提:这条也让工具 `generic_sites_are_items()` 那个承重断言比它自己声称的**更安全** ——
那个 ITEM 站点不只是 ITEM,它还是死码。

---

## 9. 核验状态

| id | 本轮状态 | 依据 |
|---|---|---|
| **`abilanc`** | **SILENT**(第三次取证) | 域非空(8 发)但两层反号 ⇒ 工具 verdict **REFUSE**;4 发 armed 残留逐帧裁决后**零发可归因于 gate**(2 发 SB 不在被守路径、2 发 PA 够不着站点的 600u 子句);**没有观测到任何一次 gate 生效或泄漏**。累计 (a) 语料 **W14 132 + W17-R 18 + W18 68 = 218 局,0 次可归因触发**。 |
| 其余 39 个 armed id | 状态不变 | 本轮未做逐 id 核验 |

**本轮没有任何 id 的三条件判定发生变化。** 本组不裁 promote/reject。

---

## 10. 诚实标注

- **上一轮的预登记这次执行了,而且被数据反号证伪的是它的隐含前提**:
  「拿到两腿语料就能买到 `abilanc` 的 (a)」。两腿到手了(§2),(a) 仍然没买到,
  原因换成了**域只有 8 发 / 5 个 episode**(§6)。**这算证伪,而且是有信息量的一种。**
- **`e17d24` / `f43908` 两个单腿 run(154 局中的 40 局)本轮只进 stamp 普查,不进任何差值读数** ——
  上一轮 §4 已立的规矩,本轮遵守。
- 行为读数覆盖 **80/154 = 52%**(双腿两个 run 的全部带 stamp `.dem`),
  arm 串结论覆盖 **154/154 = 100%**。**两个分母不要混。**
- **W18 在本轮取数时可能仍在飞**(dire 腿局数明显少于 radiant 腿,且 `f43908` 只有 8 局)。
  §2 的 stamp 分布是**一次快照**,不是终局计数;若批测台收割后局数变化,§3/§7 的每局率需重算。
  **但两层反号与 3.94× 侧别效应不依赖终局计数**(它们已在曝光归一后成立)。
- §6 的 episode 聚类用 **15s 间隔 + 「同英雄 15s 内击杀该远古」**,是本轮临时写的口径,
  **没有做成带棘轮的工具,留在 `/tmp` 未入库** —— 与上一轮 poke 普查小脚本同一个待决问题
  (**第三轮登记**,见下一轮第 5 条)。
- §7 的「zuus 是空通道」建立在三条源码/时间戳事实上(§7),**逐条核过**;
  但它**不能**排除**间接**通道(armed 腿的别的 id 改变了 zuus 的走位,使他更常路过远古营)。
  正因如此才同时给了**物理侧 3.94×** —— 间接通道解释不了「同一腿内 dire 队比 radiant 队多 3.94×」。
- 报告节奏:上一轮 18:50Z → 本轮 21:54Z = **3.1h**,在 2h cron 的正常抖动内,本轮无洞。

---

## 11. 交棒

- **GH #261 新开 [bug]** — 残留标签是文件级不是站点级(§4);
- **GH #262 新开 [strategy]** — `[1]` 族第三个成员 `hero_spirit_breaker.lua:287-296`(§5);
- **GH #263 新开 [strategy]** — 等级门瞄错 band,41 vs 1(§6);
- **GH #249 追评** — §7 全文(本组给它的最强材料);
- **GH #196 追评** — `abilanc` (a) 第三次取证,含 §3/§4/§6/§8。

**⚠️ 给总监(与 `abil1st` 入集裁定直接相关)**:`abil1st` 19:34Z 刚落地(gated),
它继承的正是 §6 量出的那个 band。**在给它开波之前**,§6 的两行(5 episode vs 138 episode)
与 §7 的噪声底(空通道 27×)一起说明:**这份形状的波次买不到它的 (a)**。
本组不申请也不反对波次,只把读数交出去。

**⚠️ 给批测台**:W18 是**第一份两腿都落地的镜像语料**(918 / 924 两粒种子),
上一轮交棒第 10 条要的就是这个形状 —— **有效,请继续**;
但 `e17d24`(926)/ `f43908`(921)仍是单腿孤儿,且 `f43908` 只有 8 局。

**球在总监**:#263、#262、#261、#259、#258、#257、#253、#250、#249、#247、#246、#245、
#244、#241、#236、#235、#159、#207、#212、#220、`campfarm`;
`bbfight`/`bbshort` 与 `abilanc` 的三条件判定。
**本组不裁 promote/reject、不申请波次、不改 bot Lua、不花 AWS 钱。**

---

## 12. 验证

- `bots/` / `game/` **0 改动**(本轮只写报告 + 章程;episode 脚本在 `/tmp` 未入库);
- `bash tools/agent/luacheck_gate.sh` → **exit 0,`luacheck bots game: 0 warnings`**
  (容器里没有 luacheck,脚本自装 `lua-check`);
- `bash tests/run_py_tests.sh` → **44 passed / 0 failed / 0 uncertifiable**(与开工自检逐位相同);
- `python3 tools/batch_test/behavioral/abilanc_domain.py --selfcheck` → **ALL PASS**;
- **未使用 `RULE6_BYPASS`**;
- 本轮未改任何 Lua ⇒ **不声称跑绿过 Lua 全量**(GH #124);
- **AWS**:S3 **只读**(W18 154 `analysis.json` + 80 `.dem`;W17-R 一个 run 的 16 `analysis.json` 复测),
  **未启动/未终止实例**,**未调用 CE**,**零支出**。

---

## 13. 下一轮优先

1. ⭐⭐ **W19 首检**(`spot_20260827_2118*`)—— **开工先跑 stamp 普查(秒级)**,
   **重点看有没有 dire 腿**;若又是两腿语料,**把 §6 的 episode 表并进去**
   —— 「等级门可及浪费 5 vs 门外 138」这条**只在一波上量过,是本轮结论唯一能被证伪的预登记**;
2. ⭐⭐ **§6 的 episode 口径做成带棘轮的入库工具**(**第三轮登记**,与 poke 普查小脚本同一决**;
   绕过棘轮的工具本身需要一条棘轮**);
3. ⭐⭐ **#236 裁下后 `mv` 落地 #159 的 fixture**(**第十一轮**);
4. ⭐ **GH #262 的 fixture**(`998ecd/20260827_184717_slot10 t=733.5 spirit_breaker`,
   **出厂腿、不依赖 arm 串**);
5. ⭐ **GH #259 的 fixture**(`88d937/151652_slot10 t=620.4 zuus`,**第二轮**);
6. ⭐ **GH #250 的出厂帧 fixture**(**第三轮**);
7. ⭐ **`pullthink` 英雄分层表**(**第三轮登记**,W17-R 18 局 + W18 68 局都是真 armed,可并腿);
8. ⭐ 锚点修正回灌 `roam_conversion.death_spans`(等 #247);**GH #220 的 fixture**(**第十一轮**);
   每 id 篇数独立小脚本(**第七轮**);
9. `camp_prelit` 列(**第十五轮**);占用度量按 idx 去重(**第十四轮**);`tpdefend_events` 结果侧列
   (**第二十轮**);幻象余量做进 `sweep_run.sh`(**第十七轮**);`entities.py` 推广(**第十八轮**);
   12:41Z §6.1 那 23 帧做 fixture(**第二十轮**);`pulldrag` 的 connect 侧;第二种视野证人;
   ab/ba 回灌 `fieldbuy_silence.py`/`stayfield2_margin.py`(**连续第三十一轮登记**);
   `stayfield` 第一失败子句;打野反证 fixture(**三十七轮**);`hero-1` 的 153 局 WK 语料(**45 轮**);
   `l5combo` 的 (a)(**第六十一轮**);`make_fixture.py` 钉 `062551 t=205.5 jakiro`(#45,**第四十八轮**);
   `axebuyblink` armed 的波次。

---

## 14. 本次会话 token 用量(铁律 8)

```
TOKENS total_in=9,280,558 out=69,393 turns=71
```

(统计时刻为报告收尾前;此后的 commit/push 回合不计入。)
