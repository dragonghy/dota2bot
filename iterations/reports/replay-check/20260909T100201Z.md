# 录像检查组 2026-09-09T10:02Z — 欠了五轮的补扫付清;transit 钉帧**costed but NOT landed**

**一句话**:`cd3359`/`40e63a` 两个 run 的补扫做完了(**40/40 局,0 不可解析**),
`cmqreach` 第三个 draft 的读数买到了;而欠了五轮的那个「transit 钉帧」**本轮第一次
被量了价** —— 它不是一条一分钟的命令,是一个 **6 个文件 / 9 条断言**的入集工作单元,
其中一条是英雄组的 hero-10 判读。**本轮不落地它**,把量好的价交出去。

| | |
|---|---|
| 宽扫 | **40/40 局**(`cd3359` 18/18 + `40e63a` 22/22;52 `.dem`,12 暖场跳过,**0 不可解析**,`SWEEP_EXIT_*=0` ×2) |
| 深查 | **11 局逐帧**(`cd3359` 全部 12 个 cell-(3) 命中帧,跨 10 局 + 钉帧那一局 `0eb22d/…_094909_slot6`) |
| 核验 | `VERIFY id=cmqreach verdict=INDETERMINATE episodes=158` |
| 开的 issue | 1 条 `[hero]`(见 §六) |
| 花钱 | **$0**(只读 S3;无实例) |

---

## 一、补扫:两个 run 都在,`.dem` 不在 `soak/` 而在 `dem21/`

**第一件差点写错的事,登记在最前面**:`soak/<run>/` 下 **四个 W56 run 全是 0 个 `.dem`**
(只有 `.analysis.json` / `.demclaim.json` / `.log.gz`)。按这个读数,本轮的第一版结论是
「语料被清掉了,补扫结构性不可能」——**那是错的**:`.dem` 在**桶顶层的 `dem21/<run>/`**
下,`sweep_run.sh:48-55` 自己写着这件事(逐字:「retention there is carried by the key」)。
判别子极便宜:**`awsx s3 ls s3://<bucket>/`** 一条,看见 `PRE dem21/` 就够了。
⇒ **与本章程「动手前先读工具的文件头」同族**:这次是「先读工具怎么找语料,再断言语料没了」。

| run | S3 `dem21/` | 扫到 | 暖场跳过 | 不可解析 |
|---|---|---|---|---|
| `…_cd3359` | 24 | **18** | 6 | **0** |
| `…_40e63a` | 28 | **22** | 6 | **0** |

两个 run 的 `sweep_summary.md` 都在,`load_sweeps` 的 #102 完整性哨兵(`games swept` 必须
等于 manifest 行数)对两个 run 都放行。

### 1.1 `40e63a`:工具**拒答**,这正是它该做的

```
=== cmqreach domain (queue.json:hero-25, cell (2) is the ruling cell) ===
games with a CM carrier: 0

UNINTERPRETABLE -- no CM carrier in this corpus.  Per the §CO
return gate, hand back the carrier distribution, do NOT read this
as 'tested, no effect'.
```
`CMQ_40E63A_EXIT=2`。上一轮是**没重扫、按 draft 推断**「`40e63a` 里没有 CM」;
本轮 **22 局实扫**独立坐实,而且工具给的是 exit 2 的拒答而不是一个零 ——
即 09-08T15:38Z 报告 §3.2 那条「`nan` 长得像『这轮没测到』,真相是『这个 draft 下永远测不到』」
的**正面样本**:这一次量具自己把这件事说出来了。

## 二、`cmqreach` 第三个 draft:cell (3) 的符号在这里翻了

`cd3359`(CM=radiant ⇒ 只有 ab/armed 与 ba/baseline 两格可填,§(0) 的结构性空格,不是样本量):

```
ab/armed     gap  94   walk 57  stand 12  away 24  unscored 1 (tp 1 / no-snap 0)
ba/baseline  gap  64   walk 33  stand 10  away 19  unscored 2 (tp 1 / no-snap 1)
gap frames 158 / ready frames 3731;centre distance on gap frames: mean 960u (734..1150)
```

cell (3)(gap frame 后 20s 内 CM 死亡),**三个 draft 并排**:

| run | armed | baseline | 腿差 |
|---|---|---|---|
| `0eb22d`(前轮) | 9/97 = 9.3% | 5/121 = 4.1% | **+5.2pp** |
| `8ef6e7`(前轮) | 12/117 = 10.3% | 8/82 = 9.8% | **+0.5pp** |
| **`cd3359`(本轮)** | **7/94 = 7.4%** | **5/64 = 7.8%** | **−0.4pp** |

⇒ **三个 draft 上 +5.2 / +0.5 / −0.4,第三个换号**。按铁律 4(i-b),这是**侧偏未消除的
计数量**(每个 run 的 CM 只在一个物理侧),**不写进结论**;登记它的价值在于:
前两个 draft 的同号(尤其 `0eb22d` 的 +5.2pp)在第三个 draft 上没有复现,
**「armed 腿死得多」这条读法本轮拿不到第三票**。分子分母照 4(ii) 全给,不报中位数。

## 三、⭐ 逐帧(先逐帧后聚合):cell (3) 的 12 个命中帧,10 个的死亡在**十几秒和一次横穿之外**

`cd3359` 全部 12 个 cell-(3) 命中帧,逐帧读(每帧:hp / mp / 最近**活着**的敌方英雄距离 /
帧前 4s 位移速度 / 上游 `movement` 给的标签 / 死亡还有多久):

| 局 | 腿 | t | hp | 最近敌英雄 | 帧前4s | 标签 | 死亡在 |
|---|---|---|---|---|---|---|---|
| `092323_slot7` | armed | 971.1 | 1.00 | 1537u | **2235 u/s** ⚠️ | stand | 19.6s |
| `092333_slot8` | armed | 1026.1 | 1.00 | 1471u | 344 u/s | walk | 20.0s |
| `093609_slot8` | armed | 1402.5 | 1.00 | 1284u | 248 u/s | stand | 15.9s |
| `093623_slot5` | armed | 931.4 | **0.68** | 1472u | 104 u/s | walk | **4.2s** |
| `093639_slot2` | armed | 1151.5 | 0.87 | 1027u | 137 u/s | walk | 19.4s |
| `094800_slot8` | armed | 800.4 | 1.00 | **3140u** | 204 u/s | walk | 10.4s |
| `094800_slot8` | armed | 1208.4 | 1.00 | **3594u** | 302 u/s | walk | 11.3s |
| `094834_slot7` | base | 593.5 | 0.53 | 959u | 284 u/s | walk | 19.6s |
| `094834_slot7` | base | 938.5 | 0.53 | 956u | 140 u/s | walk | 17.0s |
| `094834_slot7` | base | 1220.5 | 1.00 | 1407u | 321 u/s | away | 17.0s |
| `094843_slot2` | base | 617.5 | **0.37** | 805u | 49 u/s | unscored | **1.6s** |
| `094847_slot4` | base | 1226.5 | 1.00 | **3666u** | 338 u/s | stand | 7.8s |

**读出来的**:12 个里 **6 个 hp=1.00、8 个 hp≥0.87**;**10 个的死亡在 7.8–20.0s 之后**。
只有 **2 帧**的死亡近到可以和这个瞬间谈因果(4.2s / 1.6s),**而这两帧恰好就是最低血的
两帧**(0.68 / 0.37)—— 即 cell (3) 里真正「帧与死亡有关」的那一小撮,**是被血量挑出来的,
不是被 gate 挑出来的**。三帧的最近敌方英雄在 **3140 / 3594 / 3666u** 之外。

### 3.1 ⚠️ 一个当场抓到的量具污染(**我自己那把尺子的**,不是上游的)

`092323_slot7` 那一行的 **2235 u/s** 高于任何英雄的跑速 ⇒ 帧前 4s 里有一次**传送落地**。
上游 `movement()` 只对**帧后**窗口做了 TP 防护(GH #626),**帧前窗口没有任何东西挡着** ——
而 transit 这个量恰恰读的是帧前。实测边界(`cd3359`,158 个 gap 帧):
**`fast4`=88,其中帧前 4s 触到 TP 通道的 2 个,速度 >450 u/s 的 2 个** ⇒ 污染 **~2.3%**,
56% 修正后是 **54%**,**结论不动**。⇒ **给 GH #638 的落地条款补一句:transit 列必须把
`tp_crosses(t-W, t)` 也排掉,否则一次传送会被记成一次冲刺。**

## 四、聚合(在逐帧之后):transit 占比拿到了第三个 draft,并多了一个更硬的量

复用上游 `cmqreach_domain` 的域(`import` 它的 `load_sweeps`/`Game`/`gap_frames`,
**不重写**——本章程 09-09 那条工具坑),只加两个可观测量:

| run/腿 | gap | **≥280u/s(4s)** | ≥250u/s(3s) | **2000u 内无活敌英雄** | cell3 |
|---|---|---|---|---|---|
| `cd3359` ab/armed | 94 | 53 = **56%** | 59 = 63% | 74 = **79%** | 7 |
| `cd3359` ba/baseline | 64 | 35 = **55%** | 43 = 67% | 40 = **62%** | 5 |
| **合计** | 158 | 88 = **56%** | 102 = 65% | 114 = **72%** | 12 |

- **≥280u/s 这一栏,第三个 draft 给出 56%/55%**,与 09-08 那张表的
  `0eb22d` 48%/54%、`8ef6e7` 48%/52% **同形**:**四条腿到六条腿,占比几乎不随腿变**
  ⇒ 这是**域的性质,不是腿效应**(它不会把腿差推向任何一边,但会让
  「`walk` 占优 ⇒ 引擎会走进兵线」这句读法失去依据:她本来就在走)。
- **新的那一栏更硬**:gap 帧里 **72%** 的瞬间,**2000u 内一个活着的敌方英雄都没有**。
  ⛔ 79% vs 62% 这个腿差**不写进结论**(4(i-b):侧偏未消除的计数,且两条腿不共享局)。

## 五、⭐ 头号读数:那个「钉一帧」的欠账,本轮第一次被量了价 —— 它是一个入集工作单元

**帧本身买到了,而且比上一轮描述的更硬**(`0eb22d/20260908_094909_slot6`,t=1191.5,
CM **idx 1534,全局只有这一个 idx ⇒ 无幻象歧义**,GH #176 ①):

| t | x,y | hp | mp | 帧步长 | 最近活敌英雄 |
|---|---|---|---|---|---|
| 1187.5 | 451,1049 | 1.00 | 0.93 | 180u | sk 4108u |
| 1189.5 | 1137,830 | 1.00 | 0.96 | 288u | nevermore 3610u |
| **1191.5** | **1841,686** | **1.00** | **0.99** | **288u** | **nevermore 2790u** |
| 1193.5 | 2389,345 | 1.00 | 1.00 | 275u | nevermore 2232u |

**帧前 4s 净位移 1437u(≈359 u/s,接近满速直线)**。上一轮记的「满血满蓝、2000u 内零敌人、
~300u/s 横穿」**逐条复现并收紧**。

**而这一帧比上一轮知道的多一件事**,它才是本节存在的理由:**同一帧也是一个 cell-(3) 命中**。
时间线逐字:
```
t=1203.5  与 nevermore 第一次交火(钉帧后 12.0s)
t=1208.8  nevermore 击杀 crystal_maiden(钉帧后 17.3s)
```
⇒ **cell (3) 把这次死亡记在了一个「满血、满蓝、最近敌人 2790u、正在以满速横穿地图」的帧上,
中间隔着 12 秒和约 3000u 的路程。** 20s 窗口跨过了一整次横穿;这个关联**不是关于分支的证据**。
`make_fixture.py` 生成的 fixture 自己带着这条 ground truth(`died_after=17.3`,
`observed.burst` **空** —— 钉帧后 5s 内没有任何敌方英雄碰到她)。

### 5.1 ⛔ **本轮不落地这个 fixture**,以及量好的价

第一版把它写成 `tests/fixtures/f_260908_094909_cm_cmqreach_transit.lua` + 一个 4 case 的
测试(**4/4 通过**,含一条判别子:2500u 内 0 个敌方英雄、3000u 内恰好 1 个 ⇒ 证明加载器
真在读真实坐标,不是空表恒真)。**随后铁律 10 自检的 fast Lua 腿点名了它** ——
`tests/fixtures/` 里的一个文件**同时是两样东西**(一个可加载的世界 + **语料的一份样本**),
`tests/frames/README.md` 逐字写着这件事和它的价。实测(把文件挪出/挪回各跑一遍,
`WITHOUT_EXIT=0` / `WITH_EXIT=0`,读的是 diff 不是退出码):

- 放进 **`tests/fixtures/`**(入集):**4 个文件 / 5 条断言**变红 ——
  `test_alchemist_rage_clock_staged_frame`(t=1191.5 落进 armed band,该文件
  「语料自己还做不到」的主张不再成立)、`test_axe_bkb_supply_staged_frame`(BKB 槽 0→2)、
  `test_axe_t15_in_domain`(**天赋面 cap 1→2**,death_prophet 一个单位带两条 `special_bonus`;
  该断言自己的话:「**这个界该被重新取,而不是重新陈述**」)、`test_cm_ult_reach_meter_domain`(52→53)。
- 放进 **`tests/frames/`**(**只 staged 不入集**,即 README 的正规做法):
  上面四个**全部转绿**,但**另外 6 个文件 / 9 条断言**变红 —— 它们**枚举 `tests/frames/` 目录本身**:

  | 文件 | 动了什么 | 谁的 |
  |---|---|---|
  | `test_cm_ult_reach_meter_domain` ×2 | live-CM instants 52→53(并带 `nMode/nGoing/nRetreat` 三个 ==52) | hero |
  | `test_cm_w_lane_band` §1 | 「3 of 52 CM frames」的分母 | hero |
  | **`test_wk_level_supply_horizon` ×2** | **树里 12 级以上的 WK 槽 4→5,而新的这个是 level 20 且 `alive`,`max_mp=735`** | **hero(见下)** |
  | `test_wk_q_castrange_meter_domain` ×2 | live-WK instants / body bucket | hero |
  | `test_wk_q_lane_reach` §1 | 「6 of 37 WK frames」的分母 | hero |
  | `test_zuus_jump_landing_reach` §1 | 「5 of 10 in-ring sightings」 | hero |

  ⚠️ 这 6 个**不是**靠读 README 那张「已付」表找出来的 —— README 自己的告诫逐字:
  「**这张表不是那些扫描的集合,是有人记得的那个集合**」,并要求 `rg -l 'tests/frames' tests/`。
  本轮照做了,而且**没有停在 grep 上**:是把文件挪进挪出**各跑一遍**、读 diff 得到的这 9 条。

**为什么不硬付**:9 条里至少一条**不是计数是判读** ——
`test_wk_level_supply_horizon` 的新槽是**树里第二个 12 级以上、活着、池子可引用的 WK**
(level 20,`hp 2142/2575` 活体,**`max_mp=735`**)。README 记着上一次这种槽出现时的结论:
**735 > 出厂 Roshan 600 的蓝量地板**,而 queue `hero-10` 的立项句是「那些帧里最大池是 459,
即满蓝也在 600 以下」。**n=1 变 n=2,是英雄组的判读不是我的**;README 也逐字说过
「为了让套件变绿而对别人组的读数做快速编辑」那一次**被整体回滚了 15 个文件**。
⇒ 本轮把**价**交出去(§六的 issue),**树上零改动**,fixture 不留在仓库里。
**复现命令留在这里,下一棒不用重新找帧**:
```
awsx s3 cp s3://dota2bot-batch-results-4924/dem21/spot_20260908_092131_1_6aeea8c4b3467b0d40dbe39d21333b12d86ff145_0eb22d/20260908_094909_slot6.dem .
$(bash tools/batch_test/behavioral/get_dumper.sh) 20260908_094909_slot6.dem > tl.json
python3 tools/batch_test/replayscope/make_fixture.py tl.json --t 1191.5 --hero crystal_maiden \
  --roles 20260908_094909_slot6.analysis.json -o tests/frames/f_260908_094909_cm_cmqreach_transit.lua
```

**⇒ 这条欠账的形状本轮变了,这才是进展**:五轮里它一直被当成「跑一条 `make_fixture.py`」;
它实际是「**一个带 9 条断言价目的入集/staging 工作单元,其中一条是 hero-10 的判读**」。
**没被量过价的欠账,会一直看起来像一分钟的活。**

## 六、核验结论 / issue

```
VERIFY id=cmqreach verdict=INDETERMINATE episodes=158
```
**第四轮 INDETERMINATE,理由第四次升级**:①结构性不可测+单分层 → ②腿差被侧项压制 →
③域里约一半是赶路帧 → ④**本轮**:第三个 draft 让 cell (3) 的符号翻了(+5.2 / +0.5 / −0.4),
而 cell (3) 命中帧逐帧看下去,**12 个里 10 个的死亡在十几秒和一次横穿之外**。
条件 (a) 仍然**只能走 fixture**,而 §五量出了那条路的**实际价钱**。

**本轮开的 issue(1 条)**:**GH #659** `[hero]` —— 把 §5.1 那张 9 条断言的价目表交出去,并点名其中
`test_wk_level_supply_horizon` 里那条**不是计数是判读**的:树里第二个活体、池子可引用、
`max_mp=735 > 600` 的 12 级以上 WK 槽,直接关系 queue `hero-10` 的立项句。
建议的验收:要么英雄组在同一工作单元里付掉 9 条并落地这枚 frame,要么明确回「不值得,
`cmqreach` 的 (a) 另想办法」——**两种都行,悬着不行**(这条欠账已经五轮)。

⛔ **不为本轮的 transit 读数单开 issue**:GH #638 已经在(09-08 本组开的),
本轮的两条新料(第三 draft 的 56%/55%、以及 §3.1 的 **transit 列必须排掉帧前 TP 通道**)
已按铁律 5 追评在 **GH #638**(`issuecomment-5600161425`)。
两份草稿均先 push 后发表,`claim_precheck.sh` 各跑一次:
`PRECHECK2_EXIT=0` / `PRECHECK638_EXIT=0`,`local commits not on origin/main: 0`。
⭐ 第一次 precheck 是 **EXIT=3**,点名的是复现命令里的 `-o tests/frames/f_…lua` ——
那是**输出路径不是引用**,工具分不开;改写成 `/tmp/` 输出并写明「本轮树上没有这个文件,是有意的」后转 0。
**这是那道门的一个真阳性形状**:一条「我打算创建的文件」在读者眼里与「我引用的文件」长得一样。

## 七、限度(如实登记)

1. **40/40 是这两个 run 的全量,不是 W56 的全量**(W56 是 4 个 run);另两个 run
   (`0eb22d`/`8ef6e7`)09-08 已扫过,本轮**没有重扫**,只单独 dump 了钉帧那一局。
2. §四的 transit 是**我的一次性 scratchpad 脚本**(`import` 上游的域,**未入库**),
   它的帧前窗口**没有** TP 防护;污染已实测为 ~2.3% 并在 §3.1 登记,**不是零**。
3. §二 的三个 draft 之间**不并池**(§GE.5;而且按铁律 4(i-d),**任何并池都不许按局加权**)。
4. §三 的 `deaths` 按英雄名取 DEATH 事件(GH #176 ①:幻象之死记在本名下)⇒ **上界**;
   本轮**没有**做 idx 锁 —— 只有 §五的钉帧那一局做了(idx 1534,全局唯一)。
5. 深查 **11 局**,达到章程 6 局下限;宽扫 40/40。
6. **铁律 6**:本轮 **`bots/`/`game/`/`tests/`/`tools/` 一行未改**,改动只有本报告与章程本节。
   静态门随 `git push` 由 `.githooks/pre-push` 自跑,读数见 §八;**无 `RULE6_BYPASS`**;
   **动态半(~100min,GH #124)未跑,不声称。**

## 八、自检(铁律 10)与门

- ⭐ **第一条命令又踩了证据纪律 3 的形状,而工具自己拒答了**:
  `bash tools/agent/routine_selfcheck.sh 2>&1 | tail -60` ⇒ 逐字
  `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.` /
  `it has recurred 5x, every time as the first command of the round`
  (harness 报的 `EXIT=0` 是 `tail` 的)。**登记为那道自拒门第四轮起作用,不是登记为本台没踩。**
- ⛔ **第二次改成 `> file` 重定向,但带了 `timeout 600` ⇒ `EXIT=124` 是本台自己砍的**
  (GH #514 形状,批测台上一轮刚自陈过一次,**本轮本台复发**)。
  ⇒ **本轮没有工具自己的 `SELFCHECK_EXIT`**,不从已打印的腿反推整体结论。
- 读得到的腿照登:稳定版锚点 **6/6 OK**;promote 时约束 **184 个 live gate id / 18 个
  `PROMOTED` 注 / `FROZEN none`**(`pullcad` 陷阱未复发)/ 5 个 co-promote 原子全 `ok`;
  python 腿 **113 passed, 2 failed, 2 uncertifiable**
  (`test_stale_waits.py` + `test_threshold_chain_census.py`;后者的失败信息里点名的是
  `batch-desk.md` 的 LIVE 当前状态节,**属总监/批测台,不重开 issue**),
  元测试 **49 checks, 0 failures, 9 uncertified**(逐字 `this is NOT a pass`)。
- ⭐ **fast Lua 腿本轮的红全部是本台自己造成的,并已还原**:自检跑到 Lua 腿时
  `tests/fixtures/` 里正躺着那枚未定案的 fixture ⇒ 4 个文件报红并**逐字点名了它**。
  移走后 A/B 复跑 **0 红**(§5.1)。**收尾时工作树对 `bots`/`game`/`tests`/`tools` 零改动。**
- `luacheck` 静态门:见 push 时 `.githooks/pre-push` 的输出(本轮零 Lua 改动)。

## 九、下一轮第一件事

1. **盯 GH #659 的回音**;若英雄组答「不付」,把「`cmqreach` 的 (a) 走 fixture
   这条路结构性太贵」这句话本身交总监,作为 `cmqreach` 的退集/改判归因。
2. **补追评 GH #638**(§3.1 的帧前 TP 排除条款 + 第三 draft 的 56%/55%)。
3. W59/W60 收割后按新语料重跑 —— **但不得与 W58/W56 并池**(§GE.5)。
4. 继续「verify=0 但 narrat>0」补票(`pullcamp` narrat=6、`campfarm` narrat=7);
   **动手前先 `ls tools/batch_test/behavioral/ | grep <id>` 并读文件头与常量段。**

## 十、token

`TOKENS total_in=11,462,840 out=57,128 turns=80`(到统计时刻为止;统计后的收尾回合不计入)。
