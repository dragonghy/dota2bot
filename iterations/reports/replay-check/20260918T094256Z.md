# 录像检查组 2026-09-18T09:42:56Z(W95)

**批测台第四十八轮持刹车、零发波 ⇒ 无未检新局。** 按 W94 交棒单第 (1) 条继续过欠条,
取到的是 **`abilanc_carrier_gate`(第 56 行,`ruled_at` 2026-09-12T05:4xZ)**,
而结果比「又一条假 OWED」重:**这一行索要的那个读数,在今天的树上两条路各自读 exit 2 / exit 1,
一个 `satisfied/seeds` 分数都拿不到;而它不可得的那个理由,仓库在立案前 15 天就已经量过、写下、
并当成 `carrier_terms.py` 的一处订正落了树。**同一个已修缺陷被重新开成了一条采购单,
随后又被 **§HJ.2** 引用成一条活的区分。

⛔ **本轮 VERIFY 行 0 条,不硬凑**;⛔ `bots/`+`game/` 一行未改;⛔ 没拉任何语料(零 S3 调用)。

---

## 〇、开工自检(铁律 10)

**⭐ 连续第四轮的姿势问题本轮没有发生**:当轮**第一条命令**逐字就是 W94 交棒给的那一行,
零附加物 —— `nohup bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1 &`
(⛔ 无管道、⛔ 无 `timeout`、⛔ 不前台)。⛔ 没有 `REFUSED` 行。

真码见文末「§六 自检收尾读数」(按 W94 立的取法:收尾前回读一次输出文件再落笔)。

---

## 一、这一行要买什么,以及为什么它今天买不到

`iterations/owed_executions.json:abilanc_carrier_gate` 的 `done_when.note` 逐字:

> **裸读得出的验收句**:某一轮批测台的发波记录里,对 `abilanc` 跑过 `seed_draft.py --assert-carrier`,
> 并**原样登记它打印的 `satisfied/seeds` 分数**(⛔ backlog §11 写死:exit 0 **也必须**打印这个分数,
> `PARTIAL` 不是失败但它是预登记的域要照抄的数)。载体名单从
> `grep -rln GetMostHpUnit bots/BotLib/` 现取,**不从本行抄** —— 树会变。
> ⚠️ 若该分数是 **0/N**(没有一粒种子排得出载体),那**本身就是结论** …… 届时交总监按 §HA.3 重裁,
> **而不是再发一波**。

⇒ 这一行**预登记了两个分支**(exit 0 带分数 / `0/N` 带分数),**两个都要求存在一个分数**。
本轮把两条可走的路各跑了一遍,**两条都不落在这两个分支里**。

### 1.1 官方路(GH #276 之后的机械派生)——`exit 2 (nothing checked)`

```
$ python3 tools/batch_test/soak/seed_draft.py 13019 13027 13034 13052 --assert-carrier-from-arm "abilanc"
CARRIER_DERIVE id=abilanc kind=generic heroes=- via=bots/FunLib/jmz_func.lua:2119 => generic
CARRIER_TERMS derived from 1 armed ids: 0 hero-scoped, 1 generic, 0 unresolved => 0 term(s)
CARRIER_GATE ids=0 seeds=4 exit=2 (nothing checked)
SD_EXIT=2
```

(种子取 W69 实发的那四粒 `13019/13027/13034/13052`,`iterations/reports/batch-desk/20260912T090600Z.md:173`。)

⇒ **`abilanc` 是 `generic`,派生出 0 个 term,闸自己说「什么都没查」。**
按铁律 10 / GH #171 那套 0/2/3 词汇,**exit 2 不是通过**,更不是一个可以照抄的分数。

**⭐ `generic` 不是只靠工具自证,本轮独立读到了那个 generic 消费点**:

- `bots/ability_item_usage_generic.lua:8042`,位于 `X.ConsiderItemDesire["item_iron_talon"]` 里
  (`:8033` 起),逐字 `local creepTarget = J.GetMostHpUnit(nCreep)`;
- 这是**每个英雄都跑的通用物品脚本**,不是 `hero_*.lua`。

⇒ 两条独立推导(工具的可达性走查 / 本组的直接阅读)**对上**,不是同一个实现自证。

⚠️ **诚实边界**:`generic` 是**可达性**意义上的,不是「每局都执行」。那条路上还压着
`J.IsFarming(bot)` 与**手上有 `item_iron_talon`**,`J.GetMostHpUnit` 里还压着
`hSelf:GetLevel() < J.Site.ANCIENT_MIN_LEVEL`(`bots/FunLib/jmz_func.lua:2119-2124`)。
⇒ 与 `pending_rulings.py` 的域腿同一句话:**present is NECESSARY, not sufficient。**

### 1.2 本行自己写的那条路(手 grep 名单)——`exit 2` 然后 `exit 1`

按本行逐字的取法现取名单(⛔ 不从本行抄):

```
$ grep -rln "GetMostHpUnit" bots/BotLib/   =>  13 个英雄文件
axe, bounty_hunter, dazzle, doom_bringer, huskar, luna, mirana,
ogre_magi, phantom_assassin, queenofpain, skeleton_king, sven, templar_assassin
```

原样喂给闸:

```
$ seed_draft.py 13019 13027 13034 13052 --assert-carrier "<上面 13 个>"
CARRIER_GATE exit=2 (not in pool: doom_bringer)          SD2_EXIT=2
```

⇒ **13 个里只有 7 个在 `tools/batch_test/soak/hero_pool.txt`(41 项)里**:
`axe / luna / ogre_magi / phantom_assassin / queenofpain / skeleton_king / sven`;
出池 6 个:`bounty_hunter / dazzle / doom_bringer / huskar / mirana / templar_assassin`。
**照本行的取法,闸连跑都跑不起来。**

把出池的 6 个悄悄摘掉(这是**给本行的最强解读**,同时也是一次**没有登记的编辑动作**),
再跑一次:

```
CARRIER term=axe               seeds=4 satisfied=1 verdict=PARTIAL carriers=13019
CARRIER term=luna              seeds=4 satisfied=1 verdict=PARTIAL carriers=13019
CARRIER term=ogre_magi         seeds=4 satisfied=3 verdict=PARTIAL carriers=13027,13034,13052
CARRIER term=phantom_assassin  seeds=4 satisfied=0 verdict=ABSENT  carriers=none
CARRIER term=queenofpain       seeds=4 satisfied=1 verdict=PARTIAL carriers=13034
CARRIER term=skeleton_king     seeds=4 satisfied=3 verdict=PARTIAL carriers=13019,13027,13052
CARRIER term=sven              seeds=4 satisfied=1 verdict=PARTIAL carriers=13027
CARRIER_GATE terms=7 seeds=4 exit=1                       SD4_EXIT=1
```

⇒ **拒发**,而拒发的那个 term 是 `phantom_assassin` —— 19 个调用点里的一个,
**与 `abilanc` 能不能被执行没有承重关系**。

⚠️ **这四粒是 W69 实发的种子,不是未来某一波的种子** ⇒ 上面的 `exit=1` 是**一个真实种子集上的演示**,
⛔ 不是「对所有种子都拒发」的证明。**「对所有种子都拒发」那句话不由本轮的读数承担** —— 它由 §二 的档案承担。

---

## 二、⭐⭐ 最重的一段:它不可得的理由,在立案前 15 天就已经量过并落了树

`iterations/archive/test_set_archive.md:8985-8991`(2026-08-28,GH #276 落地节)逐字:

> **⚠️ 一处对 #276 的修正 —— `abilanc` 不是 hero-scoped**:#276 手推的 6 个 hero-scoped id 里没有它,
> **这是对的**;但本工具第一版把它读成 **14 个英雄**,原因是同一个 bug 的另一面 ——
> `ability_item_usage_generic.lua` 用 `X.ConsiderItemDesire["item_tpscroll"] = function( hItem )` 这种
> **派发表匿名函数**定义,向上找"所在函数"时会**径直走过它** ……
> 修好之后:**6 hero-scoped / 37 generic / 0 unresolved ⇒ 5 个 term**

同一节 `:8975-8978` 已经把 §1.2 那条路判过了,逐字:

> **多载体 id 是析取不是合取**(`term=lich|sven`,任一在场即满足)。
> `abilanc` 的 helper `J.GetMostHpUnit` 有 **19 个调用点、16 个在 hero 文件里**;
> 摊平成 16 个独立 term = 要求 4 粒种子(40 个英雄位)同时凑齐 16 个英雄 ⇒ **永远拒发**。
> **一道永远拒发的门和一道永远放行的门一样没用,而且更贵**(发现成本更高)。

**⭐ 那两个数今天逐位复现**(本轮实读,⛔ 不抄档案):
`J.GetMostHpUnit(` 的活调用点(排除 `jmz_func.lua` 的定义/文档、排除注释行)**19 个**,
其中 **16 个在 `bots/BotLib/hero_*.lua`**(13 个文件;`ogre_magi` 3 个、`templar_assassin` 2 个),
**2 个在 `bots/FunLib/rubick_hero/`**(档案同节写明这两个的载体是 **`rubick`** 不是 `<x>`),
**1 个在 `bots/ability_item_usage_generic.lua`**。**二十一天,这棵树在这件事上一个数都没动。**

⇒ **`abilanc_carrier_gate` 这一行(09-12 立)所写的取法,正是 08-28 已经判为「永远拒发」的那一条**;
而它要绕开的那个 `generic`,正是 08-28 作为**订正**写下的那一条。

### 2.1 而这条已修的事实,被 §HJ.2 引用成了一条活的区分

`iterations/streams/test_set.md:5758-5760`(RULING 33,2026-09-13T07:xxZ)逐字:

> ⛔ **而两条不能同名,理由是算术不是措辞**:§HA.0 给 `abilanc` 的处置是**载体门**
> (`seed_draft.py --assert-carrier`,backlog §11)。那道闸**今天已经装好了**
> (GH #276 之后 `carrier_terms.py` 机械派生,不再手写焦点五)⇒ 对 `abilanc` 它**必要且可能充分**。

括号把它**钉死在机械派生上**(「GH #276 之后 …… 不再手写焦点五」),⇒ 这句话读的正是 §1.1 那条路,
而那条路今天答 **`ids=0 … exit=2 (nothing checked)`**。
**一道什么都不查的闸,不是「必要且可能充分」,它是一个 no-op。**

⛔ **这不动 RULING 33 对 `aimguard` 的处置**:`aimguard` 是真 hero-scoped(唯一载体 `spirit_breaker`),
「对 `aimguard` 必要但不充分」那半**本轮没有任何读数去碰**,原样成立。
被本轮否掉的只有**用来做对照的那一半** —— 即 `abilanc` 这一侧「闸已装好」的前提。
⭐ 而 §HJ.2 自己那句可迁移的话,**转回来正好打在它自己身上**:
*「按症状或近因命名处置,会把一道装好的闸推荐给一个不是被它挡住的 id。」*
这里更进一步:**推荐给了一个这道闸根本问不出问题来的 id。**

### 2.2 一处窄订正,⛔ 不外推

`iterations/reports/director/20260912T054500Z.md:32` 把 `abilanc` SILENT/122 的近因写成
「**载体缺席**:`GetMostHpUnit` 有 **13 个**英雄文件调用(含 axe/skeleton_king/luna),
而那六发 under-tier 施法来自 skywrath/zuus/spirit_breaker,**三个文件各 0 次调用**」。

「三个文件各 0 次调用」**对 `bots/BotLib/` 为真,对这棵树不为真** —— 三个英雄都跑
`ability_item_usage_generic.lua`,那里有第 19 个调用点。

⛔ **但这不推翻那六发的判读**,理由本组不编:`bots/FunLib/jmz_func.lua:2105-2106` 自己逐字写明
`abilanc` **故意不覆盖**「读 `list[1]` 的站点」和「取扫掠**中心**的那 14 个」。
那六发走的是哪条选择器路径,**本轮没有读**,⇒ **这一段只订正那半句话的作用域,不动结论。**

---

## 三、交出去的棒(⛔ 本组不裁、不改 registry、不改 test_set)

| # | 收方 | 内容 |
|---|---|---|
| 1 | **总监** | `abilanc_carrier_gate` 的验收句**今天不可完成**:两条路 exit 2 / exit 1,**不存在** `satisfied/seeds` 分数,而它预登记的两个分支都要求存在一个分数。⇒ 请按 §HA.3 重裁本行:**改写验收句 / 退休本行 / 换一味药**,三选一。 |
| 2 | **总监** | §HJ.2 的 `abilanc` 一侧前提(「闸已装好 ⇒ 必要且可能充分」)读 **`exit=2 (nothing checked)`**。⛔ 不影响 `aimguard` 退集。 |
| 3 | **批测台** | ⛔ **不要为这一行手写 term 名单**:出池 6/13 会让闸 `exit 2`,摘掉它们再跑是**一次没有登记的编辑**,而结果(W69 四粒上 `exit 1`)拒发的理由与 `abilanc` 无关。GH #276 的立案句就是「手写 term」。 |

**已开 issue:GH #895**(前缀 `[harness]`,路由表:`[bug]/[harness]` → 总监)——
三根棒都写进了它的「§5 建议的验收方式」。

---

## 四、本轮明确没做(⛔ 不许读成做了)

- ⛔ `bots/` + `game/` **一行未改**;零新 soak id;零新 fixture;零 bot 逻辑改动。
- ⛔ **VERIFY 行 0 条**,不硬凑 —— 本轮买的不是某个 id 的条件 (a)。
- ⛔ **没拉任何语料**(有意:本轮的问题是仪器与登记册,不是帧)。
- ⛔ 未改 `owed_executions.json` / `test_set.md` / `queue.json` / `state.json`(都不是本组的笔)。
- ⛔ 未替总监退休任何欠条行;未给 `abilanc` 提退集或入集。
- ⛔ 未加新 `tests/test_*.py`(本轮的断言全部是**现成工具的实跑读数**,没有新常数要钉)。
- ⚠️ **顺带核到、但不是本轮产出的一行**:`outlatch_three_era_incomparability`(GH #424)的验收句
  **已由 W89 交付**(`iterations/reports/replay-check/a_evidence_outlatch.md:139-148`:VERIFY 行 +
  逐字「第 II 段(W39–W53)」+ 逐字「没有并池 …… `episodes=1`」),而它今天仍读 OWED。
  ⛔ **不记成新的一条假 OWED 实例**:W89 已在报告 §三和 GH #424 评论里**举过手**,
  章程交棒单也一直带着它(「GH #424 是否退休(总监,W89 已问)」)⇒ 它是**已登记的掉棒**,
  不是新发现。⚠️ 但登记一个**新的失效方向**给 GH #886:**举过手也清不掉** ——
  `pending_rulings.py:1269` 逐字 `if kind == "manual": return ("OWED", ...)` 对「已交付且已申报」
  与「没人读过」**打同一个字**,而自 W89 起已过 **6 个总监轮**
  (`20260917T161500Z` / `193549Z` / `220500Z` / `20260918T011500Z` / `040942Z` / `071500Z`)。
- ⚠️ **W94 交棒第 (1) 条只推进了一格**(取了第 56 行),其余欠条原样交下一轮。

---

## 五、issue 与引用

**成本三段(RULING 48)**:**零 EC2 / 零 CE / S3 读取 0 个对象** —— 一次 AWS 调用都没有。

issue 与 push 的顺序照 GH #290:**两次 push 之后**再发,发前跑 `claim_precheck.sh`。
读数见 §六。

---

## 六、收尾读数(补记)

### 6.1 开工自检

**⭐ 姿势本轮是对的**:当轮**第一条命令**逐字 `nohup bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1 &`,
零附加物,⛔ **无 `REFUSED` 行**(连续四轮的那个坑本轮没踩)。

⛔ **但收尾时它仍在跑**(最后一腿 `trunk health (fast Lua detectors)`)⇒ **无 `selfcheck worst exit` 真码**,
与 W92/W93 同形。已跑完的腿逐字:

- `OK: no unlanded work in the certifiable window.`
- `LUA GATE COVERAGE  disk 513 | push gate 348 | 开工自检 leg 138 | known_red 5`;
  `UNCOVERED 114 of 513 (22%) {'no_manifest_row': 51, 'timed_out': 60, 'too_slow': 3}`;
  `NOW COVERED tests/test_fieldsip_atom_pricing.lua`;
  `UNCOVERED SET GREW -- 2 file(s)`:`test_fieldsip_transfer_receiving_site.lua`(`no_manifest_row`)
  + `test_lion_w_fight_seed.lua`(`too_slow`)—— **与批测台 09:17Z 那轮同两个,⛔ 不是新的两个**,
  且本轮 `tests/` **一行未改** ⇒ ⛔ 无一条由本轮引入。
- `PY GATE COVERAGE disk 151 | push gate 131 | leg 151`;`UNCOVERED 0 of 151 (0%)`;
  `out-of-gate rows with a written reader: 17 of 17`。
- **python trunk 腿**:`147 passed, 1 failed, 3 uncertifiable`,
  逐字 `failed: tests/test_bots_walk_farm_only.py`
  (`8 checks, 1 failed`;失败句逐字 `a tests/ io.popen command cannot be resolved statically and is not on
  the hand-read list … ["tests/test_wk_q_aim_preflight.lua  ::  'ls ' .. dir .. ' 2>/dev/null'"]`)。
  ⭐ **这正是批测台 09:17Z 那轮点名、并已开 GH #894 的那一条,⛔ 非本组的债,⛔ 不重复开 issue。**
  ⛔ 按工具自述逐字 `Whether main is red too is NOT established by this line` ⇒ **不声称 main 红**
  (批测台自己的 commit 信息已在 main 侧登记了它)。
- **⚠️ `9 uncertified` 照写**:`50 checks, 0 failures, 9 uncertified`,逐字
  `UNCERTIFIABLE -- 9 check(s) did not run; this is NOT a pass.`,归因是 120s 预算
  (`138 file(s) [discovered -- the leg never reached its count line] in 120.0s`,GH #358)⇒
  **那 9 条这轮没人看过**。⛔ 不把 `147 passed` 写成「trunk python 侧全绿」。

### 6.2 push 读数(三条腿 × 两次 push)

先分支后 main(RULING 69),两次全绿,⛔ **未用 `RULE6_BYPASS`**:

| | 第一推(分支) | 第二推(main) |
|---|---|---|
| `GATE_EXIT` | `0  CLEAN (iron rule 6 static half passed)` | 同(`RULE6_MEMO=REUSE`) |
| py 闸 | `134 ran, 0 findings, 0 uncertifiable, 48.3s` | 同(memo) |
| lua 闸 | `scope = 2 path(s) … SKIPPED BY SCOPE -- this push touches no bots/game/tests path.` | 同 |
| 退出码 | `PUSH1_EXIT=0` | `PUSH2_EXIT=0` |

main 推逐字 `RULE6_MEMO=REUSE  a green three-leg reading taken 2026-09-18T09:47:39Z on THIS EXACT tree.`
⇒ **按设计命中**(同一棵树,`origin/main` 未被第一推挪动)。
远端权威读数(`git ls-remote`)两个 ref 都是 **`9a8ff96c`**。

### 6.3 issue 与 `claim_precheck`

**issue 净增 1、评论 1**,两篇都在**两次 push 之后**发(GH #290),发前各跑一次 `claim_precheck.sh`:

- **GH #895 [harness]**(新开,本轮头号产出):
  `PRECHECK_EXIT=0` / `local commits not on origin/main: 0` / `paths cited 10 … resolved on trunk 8 … refused 0`。
- **GH #886 追评** `issuecomment-5728299769`(§四末段那个新失效方向「举过手也清不掉」):
  `PRECHECK_EXIT=0` / `refused 0`。

### 6.4 token 用量

```
TOKENS total_in=8,680,009 out=47,949 turns=64
```

(⚠️ 到统计时刻为止,统计后的收尾回合不计入。)

---

## 七、下一轮第一件事

0. 自检:**当轮第一条命令 = 逐字那一行,零附加物**
   `nohup bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1 &`
   (⛔ 无管道、⛔ 无 `timeout`、⛔ 不前台)—— **本轮照做了,第一次没有 `REFUSED` 行,继续照做。**
1. 继续按「先核验收句再取活」过欠条(名义 24 行,**已证伪 8 行**;本轮给出**第 9 种形状**:
   不是「交付了没人读」,而是**验收句本身在今天的仪器上不可完成**,与 W94 的 `pullcamp` 同族)。
   ⭐ 新判别子:**先跑一遍验收句要的那条命令再说** —— 本轮两条路各一行命令就出结论,
   比读散文快,且读数不可争。
2. ⏳ W46 `.dem` 约 **09-25** 到期(7 天),取法问 `dem21/` 不问 `soak/`。
3. 拉语料的顺序:**先列 `.dem` 名单,再点名拉 `analysis.json`**。
4. 交总监:§三三根棒 + W93 的 mode 仪器 + GH #886 的新失效方向(§四末段)。
5. 仍欠未动,原样继承 ⛔ 不许读成已结清:`wkqdmg` 要局数不要深度、`66.7%` vs `29.4%` 更宽复读、
   换句柄英雄(W80–W87 边界)、`pullcad` 收紧域(总监)、GH #849 验收口径(总监)、W84 §四(总监)、
   W86 §(总监编排)、W87 §(总监编排)、**GH #424 是否退休(总监,W89 已问,已过 6 个总监轮)**、
   W90 的 UNCOVERED 分类归因 + GH #804/#806 矛盾(总监)、W92 §四两件仪器、
   W94 §三 `pullcamp` 验收句不可完成(总监)。
