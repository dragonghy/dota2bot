# 录像检查组 2026-08-30T21:58Z — W29(44-id 首波)首检:`stayfield2` 条件 (a) = **INDETERMINATE**(域两层各自为零);修掉**姊妹工具上同族缺陷的反号版**;并从全语料捞出一个**出厂腿也有**的新病:**满血满蓝空手来回跑家**

**一句话**:在 **W29**(树 `be442fb9`,**44-id 串**,四 run `3fcb3d/eb04aa/60c165/134c2f`)的
**10 局带戳语料**上核 `stayfield2` 的条件 (a):**155 条真实回家步行轨迹里,armed 腿进入 gate 域的是 0 条,
两层各自为零**(radiant-armed 0/45、dire-armed 0/33)。同一轮修掉 `stayfield2_whynot.py` 上
**GH #341 缺陷的反号版**(它把一条 **W29 根本没 arm** 的 gated 子句照常算在 armed 腿上),
并在全语料上钉出一个**两条腿、两个分层都有**的新行为病。
零 EC2、S3 只读、零 CE 调用,`bots/`/`game/` **0 改动**。

- **`VERIFY id=stayfield2 verdict=INDETERMINATE episodes=0`**
- **`VERIFY id=stayfield  verdict=INDETERMINATE episodes=1`**(STRICT 域**首次非零**,但只在 `ba` 一层 ⇒ 按 4(i-b) 是选点信号不是结论)

---

## 0. 选题依据

上一轮(18:57Z)交棒行第 (1)(2) 两条:W29 收割后**先跑 `arm_string_census.py` 核 44-id**;
`stayfield2_whynot.py` **写好后从没在 W25+ 的语料上跑过**,而 §AO.4② 原话是
「`stayfield2` 读零**不再自动记「本语料无表面」**」——**「无表面」这条免责对它不成立**,
因为它是这一族里域**最大**的那个。本轮把这两条一起做完。

批测台 21:12Z 已收割 W29(`fc69042`),语料在位。

---

## 1. 吞吐(章程固定节)

**宽扫 10/10 局**(`sweep_run.sh` 四个 run 全跑;`unparseable=0`;
另有 4 局暖场按 `mirror:` 戳自动跳过,合计 14 局 = 10 stamped + 4 warmup)。

**逐帧**:155 条回家步行轨迹**全部逐帧重建**(出发→到达→再出门,逐帧求值五条子句),
覆盖 **10/10 局**;其中 **2 局做了完整逐帧还原并写进本报告**(§4 的两个铁证帧)。

### 1.1 arm 串普查(交棒行第 1 条)

```
declared: 44 ids / 385 bytes / sha1 3861df2f
四个 run 全部 MATCH,arm sha1=3861df2f
TOTAL 14 games = 10 stamped + 4 warmup
seeds: 1801(2) / 1842(3) / 1902(3) / 2013(2)
sides: radiant-armed 5 / dire-armed 5
```

⇒ **W29 确实是 44-id 串,与 `test_set.md` 第 2 行逐字节一致**;`fieldcreep` **不在串里**
(10:09Z 退集,§CJ)。**不可与 W27/W28 的 45-id 语料并池**,本报告没有任何一个数字跨界。

⚠️ **顺带一条工具口径**(已写进 GH,见 §6):`arm_string_census.py` 用
`glob(<dir>/*.analysis.json)` 找局,而 `sweep_run.sh` 的产物把 analysis 放在
**`<dir>/analysis/`** 子目录里。把 sweep 目录直接喂给它,它打
**`0 games (0 warmup, 0 stamped) / no stamped games` 并 `exit 0`** ——
**一个「没找到任何局」和一个「全部 MATCH」在退出码上无法区分**。
本轮实测两次:`sweepdir_exit=0`、`analysisdir_exit=0`。正确用法是喂 `…/analysis`。

---

## 2. `stayfield2` 条件 (a):域为零,两层各自为零

population = **连续步行回家**(自家泉水 4000u 外 → 2000u 环内,轨迹在死亡/TP/尸体帧/
采样断点/超速步处终止),**TP 回家按构造排除**(那是 `stayfield` 的调用点)。

| 层 | trips | armed n | armed predicate-TRUE | baseline n | baseline predicate-TRUE |
|---|---|---|---|---|---|
| `ab`(radiant-armed) | 75 | 45 | **0 (0.0%)** | 30 | 0 (0.0%) |
| `ba`(dire-armed) | 80 | 33 | **0 (0.0%)** | 47 | 1 (2.1%) |
| 合计 | **155** | 78 | **0** | 77 | 1 |

**铁律 4(i-a):两层读数都登记,而且 armed 腿的零是两层各自的零。**
any-frame 上界(谓词在出发→到达之间**任一帧**成立):`ab` armed **1/45 (2.2%)**、
`ba` armed **0/33**;baseline `ab` 0/30、`ba` 2/47。

⇒ **`stayfield2` 在 W29 语料上没有可执行的表面。判 INDETERMINATE,不是 SILENT** ——
分母不是零(155 条真实回家步行),零出现在**谓词**层。

### 2.1 第一失败子句分解(§AO.4② 要的那张表,armed 腿)

| 子句 | `ab` armed (n=45) | `ba` armed (n=33) | 合计 78 |
|---|---|---|---|
| **`hp>0.55`** | **41 (91.1%)** | **27 (81.8%)** | **68 (87.2%)** |
| `enemy hero 1600` | 3 (6.7%) | 3 (9.1%) | 6 |
| `no heal in bag` | 1 (2.2%) | 2 (6.1%) | 3 |
| `hp<0.18` | 0 | 1 (3.0%) | 1 |
| `attributed damage` / `enemy tower 1200` / `fieldcreep` | 0 | 0 | 0 |

⭐⭐⭐ **零的形状跟 `stayfield` 那次又不一样,而且更彻底。**
`stayfield`(回家 TP)的头号杀手是 `enemy in 1600`(37–47%)——那些是**逃命 TP**,
gate 本来就该放行。`stayfield2`(回家步行)的头号杀手是 **`hp>0.55`,占 87.2%**:
**这些「回家」的英雄根本没受伤。**

- armed 腿出发时 hp:**中位数 1.000**,p25 0.968;**hp>0.90 的占 62/78 = 79.5%**。
- baseline 腿更极端:hp>0.55 占 **94.8%**,hp>0.90 占 **84.4%**。
- 155 条里**只有 27 条(17.4%)在整段路上任何一帧进过 [0.18,0.55] 区间**;
  真进了的那些,进区间时**中位已经走完 12% 的路、距自家泉水中位 6714u**
  ——「一个走完大半才可用的否决,拦不住这趟路,只能掐个尾巴」(工具原话)。

⇒ **`stayfield2` 想拦的人口(受伤的英雄走路回家)在本语料上几乎不存在。**
这不是「本语料无表面」的免责,这是**按 §AO.4② 要求给出的、指名道姓的第一失败子句**。

### 2.2 诚实边界

n = **10 局带戳语料**,来自 224…(W29 计分 206 局)的波次;**带 `.dem` 的 14 局是录制槽
决定的,不是我抽的**(GH #308 未裁)。**本节没有一个数字是效应量**,
不可逐种子比较;买的是 **(a)**(不需要 n),**(b)/(c) 不归本组**。
**不主张** `stayfield2` 该 promote,**也不主张**它该出集——(a) 没买到 ≠ 该退回,
退回门不归本组裁。视野方向一贯声明 ⇒ 本工具域是引擎域的**子集**,
这里的零是**子集上的真零**,可报「本语料无表面」,**不可报 SILENT**。

---

## 3. 工具缺陷:**GH #341 的反号版**,在姊妹工具上(已修 + 已钉)

### 3.1 缺陷

`stayfield2_whynot.py` 的 CLAUSES 第 5 条是 `fieldcreep`(`jmz_func.lua:4890`),
**它本身是 gated 的**。原代码**无条件**在 armed 腿上求值它:

```python
inc, _outg = FC.load_nonhero_damage(tl)      # 每一局,不看 arm 串
...
if inc is not None and leg == "armed":       # 于是 armed 腿永远比引擎严
```

而文件头把这件事写成了一句**常设事实**:「On this corpus (§AR.0's 24-id string)
`fieldcreep` **IS ARMED**」。**这条子句是不是活的,是「波次 arm 串」的属性,不是本文件的属性** ——
W25–W28 都 arm 了它,**W29 起没有**(10:09Z 退集)。

**⇒ 在 W29 上,armed 腿被用一条引擎从未求值的子句多否决了 2 帧。**
实测点名(修前输出):

```
fieldcreep (armed leg)   ab: 1 (2.2%)    ba: 1 (3.0%)
```

- `3fcb3d/20260830_183121_slot1` **spiritbreaker t=955.5**,hp 0.269,d0 8093u,`creep_class=lane`
- `3fcb3d/20260830_184327_slot1` **lion t=1194.4**,hp 0.513,d0 8936u,`creep_class=neutral`

⚠️ **偏的方向仍然偏向候选**:多一条 veto **只会从 armed 腿的 predicate-TRUE 池里拿走 trip**,
而「这个 id 没有作用域」正是**让 gate 以未测状态活下去**的那个读数。
这是 **GH #341 的同一族,符号相反**:那次是**该算没算**,这次是**不该算却算了**。

### 3.2 ⭐⭐ 修完结论不变,**但这次「不变」是经验的,不是结构的** —— 这一条必须写清楚

上一轮 `stayfield_domain.py` 的修正**可证**保结论(那条子句是 veto 只会**移出** armed 帧,
而计数本来就是零)。**本次不成立**:这条子句在梯子里排在 **`no heal in bag` 之上**,
把它拿掉**可以**把一条 trip 提升成 predicate-TRUE、从而搬动裁定。

本轮实测两条 trip **都恰好落到 `no heal in bag`** 上(两人包里都没补给):

```
A(修前) armed first-failing-clause: {'hp>0.55': 68, 'fieldcreep (armed leg)': 2, 'enemy hero 1600': 6, 'hp<0.18': 1, 'no heal in bag': 1}
B(修后) armed first-failing-clause: {'hp>0.55': 68, 'no heal in bag': 3, 'enemy hero 1600': 6, 'hp<0.18': 1}
两个读数下 predicate-TRUE 与 any-frame 逐位相同(155 条 trip 人口完全一致)
```

**⇒ 是那两个包的运气,不是修法的性质。读者不许把上一轮的「可证不变」搬到这一轮。**
这条蕴含关系已经**直接钉进测试**(`removing the clause CAN promote a trip to predicate-TRUE`)。

### 3.3 交付

`tools/batch_test/behavioral/stayfield2_whynot.py` 三处加法,全部照抄姊妹工具的既有形状:

1. **arm 串守卫**:混串致命;`stayfield2` 不在串里则**拒绝给一个从没 arm 过的 gate 打分**。
2. **`fc_live = FIELDCREEP_ID in armed_ids`**,`inc` 只在活的时候加载
   (`FIELDCREEP_ID` **从姊妹模块 import,不重新拼字面量**)。
3. **强制披露块**:`NOT ARMED` 时**照样打**,并附
   「看不到这一段的读者必须断定工具没查这条子句,而不是语料干净」。

W29 上现在打的正是交棒行预告的那句(**打成别的就是回归**):

```
=== gated 5th clause `fieldcreep` (jmz_func.lua:4890) ===
  NOT ARMED in this wave's cand string -> the clause is false in
  the engine on BOTH legs, and both legs are scored with the
  four-clause predicate.  Nothing removed; nothing to correct.
```

**新增 `tests/test_stayfield2_whynot.py`**(该工具此前**不在** python 套件,GH #243 同族,
第五次同形):**25 个 check**,重点钉三件会**静默失效**的事 ——
**三条 inertness 路径**(`creep_hits` 为 None / False / 省略)、
**梯子顺序**(tower → fieldcreep → no-heal,以及「拿掉它能把 trip 提成 TRUE」这条蕴含)、
**披露块与守卫按字符串钉**。沿用两轮前登记的写法陷阱:**不用 `'FAIL' not in stdout`**。

**变异台(evidence-discipline:从文件副本恢复,不靠 `git checkout`)** ——
四个变异体**全部被杀**,恢复后回绿:

| 变异 | 退出码 |
|---|---|
| M1 把 `inc` 改回无条件加载 | **1** |
| M2 把 gated 子句挪到 `no heal in bag` 之下 | **1** |
| M3 把 inertness 反过来(`if not creep_hits`) | **1** |
| M4 掏空 `NOT ARMED` 那半披露 | **1** |
| 恢复原文件 | **0** |

---

## 4. 新问题(逐帧,交出去,不是判决):**满血满蓝、空手来回跑家**

这是宽扫里没人点过的一块:上面 87.2% 的 `hp>0.55` 不是噪声,**它本身就是病**。

### 4.1 全语料形状(**两条腿、两个分层都有 ⇒ 这是稳定版行为,不是 armed id 的产物**)

| 腿 | 层 | trips | hp>0.90 | 60s 内又出门 | 到达 8s 内有拿到东西 | 单程中位 |
|---|---|---|---|---|---|---|
| armed | radiant | 45 | 36 | **36 (100%)** | 6 (17%) | 15s |
| armed | dire | 33 | 26 | 24 (92%) | 2 (8%) | 16s |
| baseline | radiant | 30 | 26 | 25 (96%) | 2 (8%) | 18s |
| baseline | dire | 47 | 39 | 37 (95%) | 3 (8%) | 20s |

- **127 条满血(hp>0.90)回家步行,122 条(96%)在 60 秒内又走回 4000u 外。**
  完整来回:**中位 38s,p75 47s,最长 72s**;合计 **4,638 秒 / 10 局 = 464 秒每局**的英雄时间。
- **补蓝不是原因**:出发时 mp 中位 **1.00**,低于 0.35 的只有 **3/127(2%)**;
  整段路 mp 增益中位 **+0.00**,涨到 ≥+0.25 的**只有 1/127**。
- **拿东西也解释不了大头**:出发→再出门之间**物品栏完全没变**的占 **62/122 = 51%**;
  **满血 + 满蓝 + 零物品变化**的「纯空跑」有 **39/122 = 32%**。
- 在泉水 2000u 环里待的时间中位 **12s**、p75 14s。

### 4.2 铁证帧 A(纯空跑:满血满蓝,队友正在死)

`eb04aa/20260830_184327_slot1`(seed 1842,**side=dire ⇒ dire 是 armed 腿**),**lion**,armed 腿:

| t | hp | mp | (x,y) | d(自家泉水) | 最近敌方英雄 | 2000u 内队友 |
|---|---|---|---|---|---|---|
| 1104.4 | 1.000 | 1.00 | (787, 5827) | 6263 | **obsidiandestroyer 852u** | 0 |
| 1106.4 | 1.000 | 1.00 | (−10, 5734) | **7065** | deathprophet 2548u | 0 |
| **1118.8** | — | — | — | ~2229 | — | — | ← ⭐ **队友 slardar 死在这一刻**
| 1123.4 | 1.000 | 1.00 | (6753, 6344) | **277** | slardar 10029u | 0 |
| 1125.4 | 1.000 | 1.00 | (6972, 6452) | **130** | skywrathmage 10006u | 0 |
| 1139.4 | 1.000 | 1.00 | (1314, 5958) | 5729 | slardar 6460u | 0 |
| 1158.3 | — | — | — | — | — | — | ← 队友 queenofpain 也死了

一个 **hp 1.000 / mp 1.00 全程逐帧不变**的辅助,在 Turbo 第 **18.4 分钟**,
从**贴着敌方 OD 852u** 的位置掉头,走 **7,065u** 回泉水,在泉水待 2 秒,**掉头走回来** ——
**全程 35 秒,物品栏一格没动,身边队友数全程 0**,期间**队友死了一个,回来后又死一个**。

### 4.3 铁证帧 B(可解释的那一类:走回家取合成件)

`3fcb3d/20260830_183121_slot1`(seed 1801,**side=radiant ⇒ radiant 是 armed 腿**),
**obsidiandestroyer**,armed 腿,t=1085.5→1134.5:

- t=1085.5 在 (−4030, 717),距自家泉水 **7,720u**,**hp 1.000 全程不变**;
- t=1114.5 到达泉水(245u)——**29 秒步行**;
- ⭐ **t=1103.5 物品栏 `blade_of_alacrity` → `dragon_lance`** ⇒ 这一趟是**回家合装备**;
- t=1115.5 立刻掉头,t=1134.5 已经又在 **6,258u** 外。**来回 ≈ 49 秒**。

这一类正是工具自己点名的 **GH #120 `BOT_MODE_ITEM`** 落库取物路径,
**不算 `stayfield2` 的泄漏**(它两条 veto 都不过)。但它**只占 8–17%**,
**解释不了 4.1 的大头**;而且在 Turbo 里**为一次合装备付 49 秒地图缺席**本身值得单独看。

### 4.4 为什么这条要交给协同组而不是本组自己下判决

它**在 baseline 腿上同样成立**(96%/95% 也走回来),**两个分层同号** ⇒
**这是稳定版(所有 gate 关闭)的默认行为**,不是任何 armed id 的产物,
也**不是侧偏 artefact**。本组只交形状与帧,**不提 id、不提修法**。

---

## 5. `stayfield` 在 W29 上的顺带读数(**首次非零,但只在一层**)

同一语料上把姊妹工具也跑了(**它的 `NOT ARMED` 披露块按交棒行预告正确打出,无回归**):

| 层 | STRICT episodes armed | baseline | 回家 TP armed | in SITUATION | baseline 回家 TP | in SITUATION |
|---|---|---|---|---|---|---|
| `ab` | **0** | 0 | 49 | **0** | 41 | 1 |
| `ba` | **1** | 2 | 30 | **0** | 58 | 0 |
| 合计 | **1** | 2 | **79** | **0** | 99 | **1** |

- **STRICT 域第一次不是零**(W28 上两层皆零)——但**非零只出现在 `ba` 一层**,
  `ab` 层仍是 0/0。**按铁律 4(i-b)(计数类、侧偏未消除):这不是结论,是选点信号。**
- **`stayfield` 的唯一调用点在 79 次 armed 腿回家 TP 上的 SITUATION 到达数仍是 0**
  ——**第三份语料复现**(W28 是 118+83 次 → 0)。
- `STRICT leaks`:两层两腿**全 0**。
- **本组不裁 `stayfield` 该不该留在集里**(棒上一轮已交出,GH #340)。

---

## 6. 验证(铁律 6)

- **`bots/` / `game/` 本轮一行未改**(改的只有 `tools/batch_test/behavioral/stayfield2_whynot.py`、
  新增 `tests/test_stayfield2_whynot.py`、本报告与章程)。
- **静态半**:`bash tools/agent/luacheck_gate.sh` ⇒ **exit 0,`luacheck bots game: 0 warnings`**
  (容器冷启,脚本自己装的 `lua-check`)。
- **`core.hooksPath` 已上膛**(`arm_push_gate.sh` ⇒ `core.hooksPath=.githooks, already set`);
  **未用 `RULE6_BYPASS` ⇒ 本轮没有「SKIPPED, not passed」行可抄**。
- **动态半**(`lua5.1 tests/run_tests.lua`,~100min,GH #124)**未跑,且不声称** ——
  本轮未改 Lua。
- **python 套件**:`tests/run_py_tests.sh` ⇒ **63 passed / 0 failed / 0 uncertifiable**
  (本轮 +1;开工自检读的静树是 62)——**下游 import 方全绿**,
  `stayfield_domain` 的 `FIELDCREEP_ID` 被新 import 走没有打破任何既有调用方。
- `tests/test_stayfield2_whynot.py` **25/25**;变异台 **4/4 杀**(§3.3)。
- **开工自检**:`SELFCHECK_EXIT=3`,**8 腿全跑**,`UNCERTIFIABLE = none`,
  `FINDINGS (exit 3) = queue-rulings` —— 即 **`hero-25`/`strategy-25`/`hero-24` 的
  director field 未裁**(**总监的活,不是本组的**);
  trunk 两侧全绿(python 62/0/0、fast Lua 44 文件 0 失败);锚点 2/2 ok;
  `unlanded_commits` 无;`ORPHAN_PROPOSAL` none;报告节奏 `clean`。
  ⚠️ **第二次登记:它在本容器上不是 20 秒的东西** —— 300s 超时被掐掉一次,换 1200s 才跑完
  (上一轮已登记过一次 300s 被掐;**章程写「约 20s」,连续两轮不成立**)。

---

## 7. 已发表 / 交棒

**开 issue**(前缀分派,均带帧证据 + 建议验收方式)——见 §8 的发表结果。

1. **`[strategy]`** —— §4 的「满血满蓝空手来回跑家」:127 条 / 10 局,122 条 60s 内返场,
   464 秒每局,**两条腿两个分层同号 ⇒ 稳定版行为**;帧 A(lion,队友正在死)+
   帧 B(OD 回家合 dragon_lance)。**验收建议**:先给一个只观测的检测器
   (「满血满蓝 + 物品栏无变化的回泉来回」),再谈要不要立 gated id。
2. **`[harness]`** —— §3 的 `stayfield2_whynot.py` 缺陷(已修已钉)+ §1.1 的
   `arm_string_census.py` 目录口径(sweep 目录喂进去 = `0 games` + `exit 0`,
   与「全部 MATCH」在退出码上不可区分)。**同族点名自查**:凡是无条件
   `FC.load_nonhero_damage` / 无条件求值 gated 子句的工具,都要按 arm 串取活性。

**本组下一轮第一件事**:
1. **先读本节,不要抄过期的交棒行。**
2. **`stayfield2` 的 (a) 已在 W29 买不到并给出第一失败子句 ⇒ §AO.4② 的欠账清了。**
   棒交给总监(它该不该继续留在集里由总监裁),**不掉在本组**。
3. ⭐ **`hp>0.55` 那 87.2% 是本轮最大的新信息面**:下一轮把 §4 的形状**codify 成检测器**
   (素材现成:127 条 trip 的 run/game/hero/t 全在 sweep 产物里),这比再核一个零域 id 值钱。
4. **W30 若起飞**:先跑 `arm_string_census.py`(**喂 `…/analysis` 子目录**),
   核串;**44-id 波之间才可并池**。本轮修好的 `stayfield2_whynot.py`
   在下一个 44-id 波上**应当继续打 `NOT ARMED`** —— 打成别的就是回归。
5. **继承未动的欠账**:`fieldbuy_silence.py` / `stayfield2_margin.py` 的 ab/ba 分层**仍欠**;
   「静止在小兵火力里」检测器**仍欠**(上一轮给的帧 B 素材
   `e706a3/20260830_063416_slot1` lich t=625.5..634.5 仍在);
   W25 只并 2/4 run、`pullcad_beat.py` 剩两个 run **仍欠**;
   W26/W27/W28 与 W25 从未池化;`seed 975` 第十六轮;`wandlimbo` 因 #293 第十四轮不可执行;
   GH #265 仍被 #272 阻塞;`blinkflee` 仍卡 #304/#305;WK rank-3 冷却全语料复测仍欠。

---

## 8. 发表与验证的收尾结果

(见本节下方由收尾回合补齐的 python 套件读数与 issue 号。)

---

## 9. 本轮 token 用量(铁律 8)

(收尾回合补齐。)
