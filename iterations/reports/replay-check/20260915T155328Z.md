# replay-check 2026-09-15T15:53Z — 最后一份将过期的语料扫完;`odaoe` 四格里**缺了三轮的那一格**(OD 坐 dire 的 **armed** 腿)本轮填上,判 `WORKING`

**一句话**:把 `dem21` 里唯一没人看过的那一波(`…8d8c4b`,09-11 15:24)宽扫 **18/18 局、`unparseable=0`**;
它的抽签把 `obsidian_destroyer` **18/18 局固定在 dire**,而两条腿都在(dire-armed 7 局 / radiant-armed 11 局)
⇒ 这正是 GH #54 从 **2026-08-28 起连续点名、`seed 975` 没能交付**的那一格。
读数 **armed 腿 `armed_only` 11 次(5 局) vs baseline 腿 0 次(11 局 / 66 次施法)**,
11 次逐帧核完,**10 次全场 ±2s 内没有任何英雄死亡**;baseline 腿的 10 次同形候选**10/10 都有英雄死在 ±2s 内**
—— 那 10 次正是宽松闸关掉的,**闸不是漏斗,它在本语料上 10/10 咬住、0/11 误咬**。

```
VERIFY id=odaoe verdict=WORKING episodes=11
```

（`episodes` 口径:**armed 腿上 `odaoe_domain.py` 判 `armed_only` 的施法次数**,宽松读法;
严格读法 armed 14 / baseline 10,**不承重**,理由见 §2.2。）

---

## 〇、覆盖数字(章程固定节)

**宽扫 18/18 局;深查 11 局;每 id 触发计数表见 §1.2。**

| 项 | 值 |
|---|---|
| 波次 | `spot_20260911_152456_1_main_8d8c4b`(单 run / 单粒种子 **11395**) |
| 树 | `main`(stamp 里的 commit 位逐字是 `mirror:<30 个 id>:s11395:<侧>`) |
| `.dem` | **24**,暖场 6(`script_version=f663570a`,裸 sha)⇒ 计分局 **18** |
| 宽扫 | **18/18**,`unparseable=0`、`skipped=6`(全是暖场) |
| 分层 | radiant-armed **11** 局 / dire-armed **7** 局 |
| 深查(逐帧) | **11 局**(armed 5 + baseline 6),章程下限 6 |
| `.dem` 到期 | `dem21` ≈ **2026-10-02** ⇒ ⭐ **本轮之后,这批到期资源里没有未扫的波次了** |

上一轮(12:53Z)交棒单第 (5) 条逐字点名的就是这一波:「**这批到期资源里唯一还没被看过的**」。已结清。

---

## 一、宽扫

### 1.1 抽签(一切读法的前提)

seed 11395 的十个英雄(逐字读 `analysis.json:players`):

```
radiant: dragon_knight  ember_spirit  spirit_breaker  bristleback  pudge
dire   : obsidian_destroyer  sniper  centaur  lion  jakiro
```

⭐ **逐局重建 `game.teams`(不拿 draft 名单当证据):`obsidian_destroyer` 在 18/18 局 = team 3 = dire。**
这是 **GH #835 的第五个独立 run 上的复现**(镜像波只换 gate 不换阵容;上一轮是四个 run × 81 局)。

⛔ **顺带两条 EMPTY-DOMAIN,登记但不发 VERIFY 行**(它们是**语料的性质**,不是 id 的判决):
本波无 `skeleton_king` ⇒ `wkqdmg` 在本语料上**域为空**;无 `crystal_maiden` ⇒ `cmqreach` 同。
**下一轮不要拿这一波去补这两个 id 的课。**

### 1.2 检测器表,按铁律 4(i-e) 读

`sweep_strata.py` 本来就按 (i-c)/(i-e) 算(`arm=(r_ab+r_ba)/2`,按**局率**不按计数,
per-seed swap-average 在前)。**本波只有一粒种子 ⇒ `sd=0.000`、`neg/n` 恒为 `0/1` 或 `1/1`,
`arm` 自己的跨种子离散度在本波上不可测** —— 下表是**登记的读数,不是结论**。

| detector | hit_ab | hit_ba | r_ab | r_ba | **arm** | side | |
|---|---:|---:|---:|---:|---:|---:|---|
| overextend_alone | 255 | 157 | −3.364 | +13.286 | **+4.961** | −8.325 | FLIP |
| deep_solo_death | 52 | 30 | +0.545 | +0.571 | **+0.558** | −0.013 | |
| enemy_overchase_unpunished | 13 | 14 | +0.273 | +0.571 | **+0.422** | −0.149 | |
| sandwiched_walk | 393 | 265 | +2.636 | −1.857 | **+0.390** | +2.247 | FLIP |
| missed_cs_at_tower | 59 | 37 | +0.636 | −0.143 | **+0.247** | +0.390 | FLIP |
| unpunished_tower_dive | 108 | 73 | −0.909 | +0.143 | **−0.383** | −0.526 | FLIP |
| return_to_death_spot | 95 | 54 | −1.727 | +0.857 | **−0.435** | −1.292 | FLIP |
| tp_home_wasteful | 93 | 57 | −1.182 | +0.143 | **−0.519** | −0.662 | FLIP |
| tp_under_threat | 127 | 63 | −1.545 | +0.429 | **−0.558** | −0.987 | FLIP |
| lowhp_limbo | 17 | 15 | −0.273 | −1.000 | **−0.636** | +0.364 | |
| laning_past_midline_death | 55 | 29 | −1.545 | −1.000 | **−1.273** | −0.273 | |
| died_with_ult_ready | 64 | 23 | −4.364 | +1.571 | **−1.396** | −2.968 | FLIP |
| idle_while_ally_dies | 49 | 42 | −0.273 | −2.571 | **−1.422** | +1.149 | |

⭐ **一条跨波佐证**:`overextend_alone` 的 arm 在**另一波完全不同的语料**(09-11 21:25,四 run / 四粒 / 81 局,
上一轮报告 §三)上池化读作 **+4.571/局**;本波(单 run / 单粒 / 18 局)读作 **+4.961/局**。
**两波的分层、种子、树都不同,同号且同量级。** 按 (i-e) 这两个数都是 arm(阵容项逐项抵消过),
所以并排是合法的;但**两波各自的粒内离散度都还没有** ⇒ 只登记,不下结论。
⚠️ 上一轮那个数是**池化演示**(「存在一个数」的演示,不是那个数),本轮这个是单粒 ⇒ **两者都不是最终读法**。

⚠️ **登记一条工具措辞的欠账(未开 issue,不改变任何数)**:`sweep_strata.py` 的输出脚注仍逐字写着
`r_ab / r_ba ... 4(i-b): opposite signs are noise, do not conclude from it`。
按 RULING 54 / 铁律 4(i-e),**有配对结构的层对里 `r_ab`/`r_ba` 是中间量不是估计量**,(i-b) 不管它们。
**工具算的是对的**(它下一行就写「FLIP is not a veto on `arm`」),**错的只有那半句引条**。
本组不动别组正在读的工具的判据,只登记;建议下轮由总监按 #835 统一改脚注。

---

## 二、深查:`odaoe` —— GH #54 从 08-28 起点名三轮的那一格

### 2.1 为什么是这一格

GH #54 的历轮评论(全部读过,11 条)把四格摆得很清楚:

| | OD 坐 radiant | OD 坐 dire |
|---|---|---|
| **armed 腿** | ✅ W20 s947 / s974、W21 s1138 | ⛔ **一直空着** |
| **baseline 腿** | ✅ 同上 | ✅ W22 s975(6 局) |

08-28T21:50Z 的交棒逐字:「**OD-坐-dire 的 *armed* 腿仍然买不到** …… 最后一格要等 975 拿到完整两条腿的一波」;
08-29T06:55Z 又写「⚠️ **反向(OD 在 dire)至今零证据** —— 解锁物仍是 `seed 975`,**请求连续第三轮滚下去**」。

⭐ **本波不需要 s975**:seed 11395 把 OD 放 dire,而**两条腿都落地了**(dire-armed 7 局 = OD 在 armed 腿)。
**那一格由另一粒种子填上了。**

### 2.2 读数(`odaoe_domain.py`,`--selfcheck` 本轮 **41 PASS / 0 FAIL**)

| 腿 | 局 | 施法 | 施法/局 | **armed_only** | /局 | 严格读法 |
|---|---:|---:|---:|---:|---:|---:|
| **armed**(OD 在 dire-armed 局) | 7 | 47 | 6.71 | **11** | **1.57** | 14 |
| **baseline**(OD 在 radiant-armed 局) | 11 | 66 | 6.00 | **0** | **0.00** | 10 |

**承重的是宽松读法**(工具文件头逐字:`Every load-bearing number in the report is the GENEROUS one;
the strict one is printed beside it so the size of the gap stays visible`)。
⛔ **严格读法那一列 baseline = 10,按构造是矛盾** —— 它正是「1 Hz 快照的陈旧 HP 低估了出厂循环」那条,
**方向恰好朝着讨好本 id 的那一边**,所以它不承重。

⭐ **域不是空的,两腿可比**:`covered>=2` 的施法 armed **14 次 = 2.00/局**、baseline **15 次 = 1.36/局**;
其中 `killable==0` 的 armed **14**、baseline **10**。
⇒ baseline 腿**确实走到了同一个几何形状十次**,只是每一次都被宽松闸关掉。

### 2.3 ⭐⭐ 逐帧:11 次 armed_only,10 次全场 ±2s 无英雄死亡;baseline 的 10 次同形候选 **10/10 有**

读件纪律(章程坑逐条):实体按 **`lock_real_entities` 锁的 `idx`** 取(⚠️ 5 局里 OD 有 **3 具同名身体**,
如 `155327_slot4` 的 `[398, 1388, 2309]`,锁定真身 `1388`);生死走 **`roam_conversion.is_dead()`**(DEATH 事件,
不是 `hp_pct`);位置与蓝量取**施法前最后一帧**;施法用 **ABILITY 事件**定位(11/11 逐条命中,不是冷却上升沿)。

**承重帧(armed 腿,整行未手挑字段,原件 `/tmp` 不入库,复现命令见 §5)**:

```
[armed] 20260911_155257_slot7 t=760.5  ABILITY_event=YES  OD idx=1460 (bodies [1460]) lvl=21 ult_lvl=2 mp=1774 hp_pct=1
      in-range dragon_knight    idx=1316 dist=653.9 hp=2046 mp=747 -> shipped_dmg_upper=533.1 kills=False dead=False
      in-range ember_spirit     idx=1243 dist=662.5 hp=1373 mp=795 -> shipped_dmg_upper=518.7 kills=False dead=False
      in-range spirit_breaker   idx=1352 dist=657.6 hp= 919 mp=773 -> shipped_dmg_upper=525.3 kills=False dead=False
      HERO deaths within +-2s: NONE
```

`shipped_dmg_upper = 0.75 × (base + |mp_OD − mp_target| × 0.4)`,**平 25% 魔抗、忽略一切魔抗装 ⇒ 上界**。
三个敌人的血量是该上界的 **1.7–3.8 倍**,**不在任何建模余量之内**;
⇒ 出厂单体循环的 `CanKillTarget` 三问全否 ⇒ **它没有出口**;而 `covered=3` ⇒ 门开 ⇒ **只可能是门放的**。

另三条同型的非 thin 帧:`155257_slot7 t=1089.1`(ult 3,DK/pudge/SB,血量是上界的 2.8/3.0/2.8 倍)、
`155327_slot4 t=1174.7`(ult 2,ember/pudge/SB,3.7/2.9/2.8 倍)、`155327_slot4 t=1434.6`(ult 3,BB/ember/SB,4.2/4.4/4.5 倍)。

**⭐ 正控制(这是本轮最值钱的一格,它量的是仪器不是 id)**:

| 腿 | 同形候选(`covered>=2` 且 `killable==0`) | 其中 ±2s 内**有**英雄死亡 |
|---|---:|---:|
| armed | 11(判 `armed_only`) | **1/11** |
| baseline | 10(全判 `shipped_explains`) | **10/10** |

baseline 那 10 次逐条:`153944_slot3` t=907.7→BB 死于 908.3;t=1119.5→ember 死于 **1119.5(同刻)**;
`154019_slot8` t=972.8→DK **同刻**、ember 974.3;t=1575.4→DK 1575.1;`154027_slot6` t=208.4→SB **同刻**;
t=839.4→pudge **同刻**;t=1348.0→ember 1347.9;`154035_slot5` t=1438.4→SB **同刻**、ember 1439.1;
`154037_slot1` t=1251.9→DK **同刻**;`155224_slot3` t=799.5→DK 800.5。

⇒ **宽松闸(「±2s 内死掉的敌人,在决策那一刻大概率在 `CanKillTarget` 的答案里」)不是一个能吞掉一切的漏斗**:
它在 baseline 腿上 **10/10 咬住**,在 armed 腿上 **0/11 咬住**(armed 那唯一一次死亡
`155326_slot8 t=886.4 → dragon_knight 887.2`,**DK 不在该帧的施程内**,所以闸正确地没有闭合)。
**一个近似的可信度,来自它在应该开火的那一侧真的开火。**

### 2.4 ⚠️ 几何判别子在本语料上读 0,**那不是反证,是它按构造看不见**

`od_eclipse_offcentre.py`(源码派生的那把更强的尺)本轮:

```
== s11395_armed:   20 multi-hero casts | shipped-explainable 17 | OFF-CENTRE ONLY 1 (5.0%) | neither(range) 2
     off-centre: 155326_slot8 t=289.1 miss=256 step=265 INSIDE-NOISE
== s11395_baseline:15 multi-hero casts | shipped-explainable 12 | OFF-CENTRE ONLY 0        | neither(range) 3
```

⇒ **clear 离心 armed 0/20、baseline 0/15**,而历史三粒的 armed 腿是 **9/101 = 8.9% clear**。

⛔ **不要把这读成「本波 `odaoe` 没生效」。** 逐帧算过 11 次 armed_only 的**敌人最大两两间距**
(离心判据的预算是 `RADIUS+SLACK = 560`):

```
155255_slot5 t= 387.3  305.6   155255_slot5 t=1085.6  389.9   155256_slot2 t=1228.1  239.3
155256_slot2 t=1439.6  193.8   155257_slot7 t= 760.5  524.3   155257_slot7 t=1089.1  379.2
155257_slot7 t=1291.2  219.6   155326_slot8 t= 289.1  815.9   155326_slot8 t= 886.4  373.3
155327_slot4 t=1174.7  339.9   155327_slot4 t=1434.6   94.6
```

**11 个里 10 个 ≤ 560** ⇒ **一个以敌人为心的 500 圆本来就罩得住** ⇒ 离心判别子对它们**结构性判 `shipped-explainable`**,
**它根本不在被问的那个问题里**。唯一 >560 的那个(`155326_slot8 t=289.1`,间距 815.9)**正是**它标出来的那一条,
而它自己按 08-28 立的余量规矩判 `INSIDE-NOISE`(miss 256 < step 265)——**判得对**。

⭐ **可迁移的那一句(与章程「先读工具头部 LIMITS」同族,换了一个方向)**:
**两把尺给出不同的零,先问「这两把尺问的是不是同一个问题」,再问哪一把错了。**
这里两把都没错:离心尺证的是**圆放在没人站的地方**,可杀性尺证的是**出厂循环没有出口**;
armed 分支的候选集**同时包含每个敌人自己的位置**,所以它完全可以放出一个「圆心压在敌人身上」的 armed-only 施法
—— 那种施法**离心尺按定义看不见**。
⇒ **`od_eclipse_offcentre.py` 的 0 只在命中集散开时才是信息**;本波的团战形状太密,它在这一格上没有话语权。

### 2.5 ⭐ 顺带退役一条挂了 18 天的效度限定:这一份 OD **会加大招**

08-28T21:50Z 那条评论逐字:「**本 id 历轮读数,全部取自一个不会加大招的 OD**」(21 次施法里 20 次 `ult_lvl=1`,
根因 GH #286 技能加点停摆),并给总监留了一句「**`odaoe` 若要进 promote 讨论,先落 #286**」。

本波 armed 腿 47 次施法的大招等级直方图:**`{1: 16, 2: 24, 3: 7}`**,OD 英雄等级跨 **6–26**。
⇒ **本语料的 OD 正常加点,2/3 级大招占 66%。** 那条效度限定**对本轮读数不适用**。
⛔ **这不是「#286 已修」的判决**(本组没查那个 issue 的代码,也没跨波复核),
**只是「本波的 OD 不在那个形状里」**——一个语料级事实,归属清楚。

---

## 三、本轮不做的事(免得下一轮当未了结的发现)

- ⛔ **零 bot Lua 改动**,零新 soak id,零 fixture(`git status --short` 全程只有 `iterations/` 下的文件)。
- ⛔ **不裁 promote/reject**;`odaoe` 的 (b)(c) 不在本组。(c) 由 GH #54 §4 早已给出。
- ⚠️ #257 的两条残项本轮**没碰也没复核**;OD 那 24 s 残差**连续第十一轮挂账**;
  09-13T16:30Z 的 n=1 复现**连续第十六轮挂账**。
- ⚠️ 上一轮交棒第 (3) 条(历史 `arm_side` 反号读数按 (i-e) 全部重算)**本轮只做了本波这一张表**,
  历史报告的回算**没做**,继续挂账。

---

## 四、issue

- **1 条评论**发到 **GH #54**(见收工回填的评论号)。发帖前读完正文 + **全部 11 条评论**;
  发帖用 `add_issue_comment`,事后 `issue_read` 复核;⛔ 全程不碰 `issue_write`。
- **净增 0**:没有新问题需要开单——本轮的发现是**把一个已点名三轮的证据格填上**,归属在既有 issue 里。
  §1.2 那条工具脚注欠账建议由总监按 #835 统一处理,**本组不另开单**(先搜过重,`sweep_strata` 脚注/`(i-b)` 措辞 0 命中)。

## 五、复现

```bash
bash tools/batch_test/aws/session_setup.sh
nohup bash tools/batch_test/behavioral/sweep_run.sh \
  s3://dota2bot-batch-results-4924/soak/spot_20260911_152456_1_main_8d8c4b/ > /tmp/sweep.log 2>&1 &
OUT=tools/batch_test/behavioral/.sweep_out/spot_20260911_152456_1_main_8d8c4b
python3 tools/batch_test/behavioral/sweep_strata.py "$OUT"
# od_games.json: 每局 {run, game, leg, od_team},leg 由 game.teams 的 OD 物理队 vs stamp 侧别推
python3 tools/batch_test/behavioral/odaoe_domain.py --games od_games.json --timelines DIR --frames
python3 tools/batch_test/behavioral/od_eclipse_offcentre.py s11395_armed <armed 腿 7 份 timeline>
```

## 六、成本(RULING 48 三段)

**零 EC2 / 零 CE / S3 读取 24 个 `.dem`(约 530 MB)+ 24 个 `.analysis.json` + 若干次列目录(出网未计价)**。
⛔ **不写「零支出」**。dumper `get_dumper.sh` **缓存命中**(未重建)。`AWS_SETUP_EXIT=0`。

## 七、token 用量

见 §8.5。

---

## 八、收工回填(2026-09-15T16:2xZ)

**落地 `origin/main` `5c3728bf..ba43bb8f`**;分支 `claude/lucid-pascal-pao3zo` **同点**
(`git ls-remote origin` 逐字读 —— 两条 ref 都是 `ba43bb8f31cf0e17dfe89b9240c3813634fd6ae8`;
⭐ 按章程 W73 那条坑,**问的是远端,不是 `git status` 缓存的那份副本**)。
⛔ **全程未用 `RULE6_BYPASS`**。

### 8.1 铁律 6 三条腿 —— ⚠️ **本轮被 py 闸拒过一次,那是 GH #629 的第二次实测复发**

推送一共四次,逐字读数:

| # | 命令 | 结果 |
|---|---|---|
| 1 | `git push -u origin <branch>`(rebase 前) | `PUSH_BRANCH_EXIT=0`;`GATE_EXIT=0 CLEAN` / `py gate: 92 ran, 0 findings` / `lua gate: SKIPPED BY SCOPE` |
| 2 | `git push origin HEAD:main` | `PUSH_MAIN_EXIT=1` —— **`non-fast-forward`**(批测台的 `8f610896..5c3728bf` 先到)⇒ `git pull --rebase origin main` **`REBASE_EXIT=0`** |
| 3 | `git push origin HEAD:main`(rebase 后) | ⛔ **`PUSH_MAIN_EXIT=1`,`py gate: 93 ran, 1 findings`,`FAIL tests/test_tpreach_domain.py (exit 1)`,`PUSH REFUSED -- iron rule 6 python half is RED`** |
| 4 | `git push origin HEAD:main`(**同树、零改动、原样重推**) | ✅ `PUSH_MAIN_EXIT=0`;`GATE_EXIT=0 CLEAN` / **`py gate: 93 ran, 0 findings, 0 uncertifiable, 13.1s`** / `lua gate: SKIPPED BY SCOPE` |
| 5 | `git push -u origin <branch> --force-with-lease`(rebase 后同步分支) | `PUSH_BRANCH_EXIT=0`;`GATE_EXIT=0 CLEAN` / `py gate: 93 ran, 0 findings, 0 uncertifiable, 13.1s` / **`lua gate: 390 ran, 0 findings, 0 uncertifiable, 8 unanswered, 6 known-red, 598.3s`** |

⭐ 第 5 次那条 **Lua 腿真的跑了 390 个测试(598.3s)**,不是范围跳过 —— 因为分支推在 main 之后,
`git diff --name-only origin/main...HEAD` 为空,而钩子把**空列表当成「跑全部」**(它自己的 fail-closed 设计)。
**这是本轮唯一一次三条腿全都真跑并全绿的读数。**
动态半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)**未跑、不声称**。

⚠️ **第 3 次那个红不是本轮造成的**:本轮 `tests/` + `tools/` + `bots/` + `game/` **一行未改**
(`git status --short` 全程只有 `iterations/` 下那两个文件)。
**同一棵树上六次读数、红一次**,详见发到 **GH #629** 的评论;本轮**主动排除了三条**成因
(管道那一族 / 自己跟自己并发 24/24 全绿 / `routine_selfcheck.sh` 不动工作树 —— 它的 `git stash` 只在 `printf` 里)。
⭐ **新的一格**:立案轮的现场是「子进程什么都没打」,**本轮它打了 11 条 PASS 才断**,
且 `no check in the battery FAILed` 读 **ok** ⇒ **它不是断言失败退出的,是在第 12 条前后消失的**。

### 8.2 开工自检

⭐ 逐字用了章程那一行(⛔ **没套 `timeout`**);⚠️ **第一条命令误接了管道**,被它自己挡住:
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked` ——
**那是「一行都没跑」,不是「跑了一半」**;改用 `> /tmp/sc.log 2>&1` 后一次跑成,退出码从**日志最后一行**读。

`legs run 13`,**`selfcheck worst exit: 3`**,
`FINDINGS (exit 3): queue-rulings owed-executions lua-coverage trunk-red(lua)`,
`UNCERTIFIABLE (exit 2): trunk-red(python)`。

⭐ **逐条查了归属,本组这轮一条都不占**:
- ⭐ **`cadence` 不在名单里**(上一轮是 `GAP cadence replay-check`)—— 本组的报告节奏这轮是干净的;
- `trunk-red(lua)` = `test_fieldsip_atom_pricing.lua`(`:400` 的 `1039/1021` + `:562` 的 `961/944`),**GH #814 那一族**,
  与前两轮登记的同一条;**本轮 `bots/`+`game/`+`tests/` 一行未改** ⇒ 不 stash 复核;
- `lua-coverage` = GH #806 欠条,**本轮零新测试**(Lua 与 python 都是),不在这条里;
- `queue-rulings` / `owed-executions` = 总监与批测台的欠条行;
- `trunk-red(python)` 是 **UNCERTIFIABLE(exit 2)不是 FINDING**,横幅逐字带
  `this is NOT a pass, and it is NOT evidence that trunk is red` ⇒ ⛔ **不读成 main 红**。

⚠️ **别组的洞顺手登记**:看守自检自己的三条 python 用例
(`test_lua_gate.py` / `test_luacheck_gate_soakswitch.py` / `test_selfcheck_lua_leg.py`)
**连续第三轮** `UNCERTIFIABLE (did NOT run -- this is not a pass and not a failure)`;属 [harness]/总监,本组只登记。

### 8.3 issue

**净增 0**(先搜过重两次,命中的都是既有单)。**2 条评论**:
- **GH #54**(`#issuecomment-5683799454`)—— 本轮主产出(`odaoe` 第四格);
- **GH #629**(`#issuecomment-5683807512`)—— §8.1 第 3 次那个红的现场(第二次实测复发 + 「跑到第 11 条才断」这一格新证据)。

两条都**发在四次 push 之后**(GH #290)。发帖前读完各自正文 + 全部评论(#54 **11 条**、#629 **0 条**);
发帖用 `add_issue_comment`,事后 `issue_read` 复核 —— 两个 issue 的**正文与发帖前逐字相同**、`state` 仍 open、评论数 **11→12** 与 **0→1**;⛔ **全程没碰 `issue_write`**。

### 8.4 AWS 与成本

`AWS_SETUP_EXIT=0`。**成本三段(RULING 48)**:
**零 EC2 / 零 CE / S3 读取 24 个 `.dem`(约 530 MB)+ 24 个 `.analysis.json` + 若干次列目录(出网未计价)**。
⛔ **不写「零支出」**。dumper `get_dumper.sh` **缓存命中**,未重建。

### 8.5 token 用量

```
TOKENS total_in=16,102,957 out=76,763 turns=86
```

(到统计时刻为止;其后的收尾回合不计入。本轮**零 `requires approval`**。)
