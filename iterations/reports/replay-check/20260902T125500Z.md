# 录像检查组 2026-09-02T12:55Z — W38 首检:交棒项 (2) 结清(否定结果),d22 的两个假阳性钉到帧

**一句话**:W38 收波,宽扫 **69 局 / unparseable=0**;交棒项 (2) 点名的
`died_with_ult_ready` 那一行**在第二份语料上没有复现**(W37 `arm −1.525`、两层同号、3/3 粒
→ W38 `arm +0.234`、**FLIP**、1/3 粒);顺手把上一轮手算的「目标合法性」**做成了入仓工具**
(6/6 反向断言),449 个 episode 跑出 **448 LEGAL**;逐帧找到 d22 的**两个真假阳性**
(合计 13/449 = 2.9%),并**逐帧证伪了我自己中途形成的第二个假设**(「凶手不是英雄 ⇒ 假阳性」)。
**新开 GH #425。**

---

## 一、开工自检(铁律 10)

**第一条命令第十六次踩证据纪律 3** —— 我写了 `... | tail -40`,脚本自打
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked`,
并自陈「已复发 5 次,每次都是当轮第一条命令」。**exit 2 不是通过**;
「开工模板内建 `rc.sh`」**第九次登记,形状完全相同**。

改重定向后 `timeout 400` ⇒ **`BARE_EXIT=124`**,**第 23 次登记「不是约 20s」**(GH #358)。
⇒ **本轮无总判决**。跑到的腿:

- `unlanded_commits` OK;cadence 未跑到。
- **python trunk RED(工作树)**:`test_call_arity_census.py` + `test_detector_source_constants.py`;
  另有 `test_rc_wrapper.py` / `test_selfcheck_lua_leg.py` 两条 **UNCERTIFIABLE(没跑成,不是通过)**。
- **lua 那条腿没跑到**(124 砍在它之前)。
- 日志自带「main 是否也红并未确立」的诚实边界,**本轮没跑 stash 复核故不声称**(GH #364)。

**`test_call_arity_census.py` 是本轮新出现的红,我看了一眼,不是缺陷**:
它点名 `bots/FunLib/jmz_func.lua J.RoshanPitProximity UNDER passed 3 declares 4`。
读源码(`:12295` 与 `:12308`):第四参 `nRadius` 是**可选尾参**,函数体写
`nRadius or 1600`,而唯一调用点 `J.IsAtRoshanPit` 故意只传三个。
这是 **`563d65b1`(协同组 gated `roshdist`,GH #422)刚落地**的形状,
工具的设计就是「新的 UNDER 必须在 ALLOWLIST 里拿到一句裁定才能骑」——
**要的是一条裁定记录,不是一次改码**。归协同组/总监,**本组不动手,也不开新 issue**
(#422 已经是那次落地的案子)。#358/#364 一律只记复现。

---

## 二、宽扫(覆盖数字)

W38 = 54-id 家族第一波,tree `7196468d`,批测台 12:15Z 已收割
(3/4 粒计分、158 局;seed 2613 那台 86 秒被 EC2 收走,补跑机 12:18Z 起飞)。

`sweep_run.sh` 三个计分 run,**各自独立目录**(同名 `.dem` 跨 run 撞车那条坑,见 §5):

| run | seed | swept | unparseable | `sweep_complete.json:exit_code` |
|---|---|---|---|---|
| `…_ac5166` | 2665 | 26 | 0 | 0 |
| `…_0b4cf2` | 2761 | 25 | 0 | 0 |
| `…_85cab8` | 2840 | 18 | 0 | 0 |
| **合计** | 3 粒 | **69** | **0** | — |

⚠️ **覆盖率的诚实边界**:批测台计分 **158 局**,我只宽扫到 **69 局**。
差额不是丢局,是 **`rec_slots=8` / `slots=16`** —— 只有一半的槽录像。
**「69/158」是录像组能看到的上界,不是漏扫。**

**深查:6 局逐帧**(§4、§5;另 §3 的 449 个 episode 是工具级全量,不计入逐帧数)。

---

## 三、交棒项 (2):`died_with_ult_ready` 在第二份语料上 **没有复现**

上一轮点名它,是因为在 W37 的 54 局上它是**唯一同时满足「两层同号 + 读数跟着 arm 腿走」**的行。
W38 的 69 局(`sweep_strata.py`,零新工具,`BARE_EXIT=0`):

```
SWEEP STRATA  games ab(armed=radiant)=38  ba(armed=dire)=31  seeds paired=3

detector               hit_ab hit_ba |     r_ab     r_ba |      arm     side     sd neg/n
died_with_ult_ready       235    214 |   -2.224   +2.692 |   +0.234   -2.458  0.806 1/3  FLIP
```

**逐项对拍**:

| | W37(54 局) | W38(69 局) |
|---|---|---|
| `r_ab` / `r_ba` | −0.498 / −2.552 **同号** | −2.224 / **+2.692** **FLIP** |
| `arm` | **−1.525** | **+0.234**(**变号**) |
| `neg/n` | **3/3** | **1/3** |

⇒ **W37 那一行的三条性质(两层同号、arm 负、3/3 粒)一条都没留下。**
按 **4(i-b)**,FLIP 那两个是侧偏未消除的估计量,**是噪声,不写进结论**;
按 **4(i-c)**,FLIP 不是对 `arm` 的否决 —— 真正说话的是 `arm` 自己的跨语料表现,
而它**变了号且 neg/n 从 3/3 掉到 1/3**。**结论:那不是一个稳定性质,是那一波的抽样。**
**登记为否定结果,交棒项 (2) 结清。**

⛔ **诚实边界**:W37 是 52-id、tree `main`;W38 是 54-id、tree `7196468d`。
**这是第二份语料,但不是同一个实验** —— 两波 armed 的集合不同,
所以上表读的是「这行读数稳不稳」,**不是任何 id 的效应量**。
⛔ **不可与 W35/W36/W37 并池。**

**同一张表里两层同号且 3/3 粒的行**(只登记,不下结论,理由同上):
`tp_home_wasteful`(−0.500/−0.668,arm −0.584)、`deep_solo_death`(−0.500/−0.379,arm −0.439)、
`missed_cs_at_tower`(−0.457/−0.290,arm −0.374)。**下一轮该看的是这三行在第三份语料上还在不在。**

---

## 四、d22 的目标合法性:上一轮的手算变成入仓工具,449 个 episode 全量

上一轮在 24 个 episode 的残片上**手算**出「d22 只看 `level/cd/mp`,不看有没有东西可打」。
手算不在树里 = **#263 的形状**(下一轮重做,而且做得不一样)。
本轮把它做成 **`tools/batch_test/behavioral/d22_target_legality.py`**。

**它先证明自己能说「不」**(`--selfcheck`,`BARE_EXIT=0`):

```
  ok   in range, executable  -> LEGAL        ok   non-execute, in range     -> LEGAL
  ok   in range, too healthy -> NO_EXECUTE   ok   non-execute, out of range -> NO_TARGET
  ok   out of range          -> NO_TARGET    ok   global ult, enemy far     -> LEGAL
6/6 selfcheck cases pass
```

**这组反向断言不是仪式,它当场抓到一个真缺陷**:执行分支原本写 `os_['hp'] < thresh`,
而 `entities.interp()` **只返回 `t/x/y/hp_pct/level`,没有 `hp`** ⇒ 第一次遇到斩杀类大招
就会 `KeyError`。**它在真语料上没崩,只是因为 W38 的阵容里一个 Axe 都没有** ——
一条从没被执行过的分支看起来和一条正确的分支**一模一样**。已改成从原始采样取
**两端 bracketing 的较小值**(较低血 ⇒ 更像能斩 ⇒ 只会**低估**不合法率)。

**全量读数(69 局 / 449 个 episode,`BARE_EXIT=0`)**:

```
ult                                    n  LEGAL  NO_TGT  NO_EXEC
storm_spirit_ball_lightning          112    112       0        0
lion_finger_of_death                 106    106       0        0
dragon_knight_elder_dragon_form       45     45       0        0
crystal_maiden_freezing_field         45     44       1        0
zuus_thundergods_wrath                36     36       0        0
obsidian_destroyer_sanity_eclipse     30     30       0        0
… (另 7 个大招)                       75     75       0        0
TOTAL                                449    448       1        0
illegitimate = 1/449 = 0.2%   4(i-a) 两层:ab 0/235 = 0.0%   ba 1/214 = 0.5%
```

**⛔ 这既没有复现也没有推翻上一轮的 16.7%,而是「这份语料结构上答不了」**:
那 16.7% 的分子**全是 `axe_culling_blade`**(唯一的斩杀类大招),
而 **W38 的阵容里没有 Axe** ⇒ `axe_culling_blade` 这一行的 n = **0**。
**「目标合法性」这个缺陷是斩杀类专属的;非斩杀类上,d22 的施法者三条件与「有东西可打」
在 449 个 episode 里只错开 1 次。**

**工具不是在无脑说 LEGAL**(它得能说不,也得真在算距离):最近敌人距离
`min 0 / p10 92 / p50 354 / p90 779 / max 3572`,**25 个 episode 超过 900u**;
它们判 LEGAL 是因为大招是全局/自身型(`RANGE_FLOOR = inf`),不是因为没算。

⛔ **`RANGE_FLOOR` / `EXECUTE_HP` 是本工具自己的操作定义,dumper 没有这两个字段**,
数值一律**向上取整**(只会把 NO_TARGET 变成 LEGAL,不会反过来)⇒ 上表是**严格低估**。
**与 GH #405 验收 (b) 是同一个要求。**

---

## 五、逐帧:两个真假阳性,以及**被我自己证伪的**第二个假设

### 5.1 唯一那条 NO_TARGET —— 帧证据说 d22 的建议在这里是**自杀**

`…_ac5166 / 20260902_101143_slot6`,crystal_maiden(radiant,team=2),死于 **t=596.5**:

```
      t    hp  hp%    mp      x      y | 最近的活着的敌人
  591.4   931 0.67   382   2812   2113 | venomancer@1033u  viper@1356u
  592.4   644 0.46   390   2855   2001 | viper@1453u  venomancer@1487u
  594.4   338 0.24   405   2792   1862 | viper@2046u  venomancer@2260u
  596.4    72 0.05   419   2847   1560 | viper@2174u  venomancer@3037u
DEATH: actor = npc_dota_badguys_tower2_mid
```

**三件事同时成立**:(i) 凶手是**敌方二塔**,不是英雄;(ii) 从 t=592.4 起**每个敌方英雄都在远离**
(viper 1453→2174u,venomancer 1487→3037u);(iii) CM 一路向自家方向走(y 2001→1560)。
d22 的判词是「握着能打的大招死了 —— 缺 cash-out 层」,可 **Freezing Field 是 6 秒自缚频道**,
在 15% 血、二塔火力下、最近敌人 2174u 且在拉开时开它,**是把撤退换成站着死**。
**这条是真假阳性,而且检测器建议的动作方向是有害的。**

### 5.2 我中途形成的第二个假设,**被逐帧证伪**(登记否定结果)

看到 §5.1 的凶手是塔,我立刻怀疑「**凶手不是英雄 ⇒ 假阳性**」,并且它一量就有量:
**18/449 = 4.0%**(4(i-a) 两层:ab 7/235 = 3.0%,ba 11/214 = 5.1%;塔 9、小兵/野怪 9)。

**逐帧看了 5 条,4 条把这个假设打死了**:

| 局 / 英雄 / 时刻 | 凶手 | 死前那几帧最近的**敌方英雄** | 判 |
|---|---|---|---|
| `101131_slot1` chaos_knight t=541.0 | `goodguys_tower1_top` | **spirit_breaker @206u,血 9%** | d22 **对** |
| `101044_slot3` lion t=710.9 | `goodguys_tower1_bot` | **ogre_magi @189u**、death_prophet @391u | d22 **对** |
| `101105_slot2` zuus t=798.5 | `badguys_tower2_top` | **storm_spirit @0–322u 全程** | d22 **对** |
| `093347_slot8` dragon_knight t=513.1 | `neutral_grown_frog_mage` | **ogre_magi @192–589u 全程** | d22 **对** |
| `094711_slot8` storm_spirit t=416.2 | `goodguys_tower1_mid` | **zuus @57–607u 全程** | d22 **对**(但见 §5.3) |

⇒ **「凶手不是英雄」不是假阳性的判别子。** 这四条都是**真打团**,塔/野怪只是**补了最后一下**;
CK 那条尤其反过来:握着 Phantasm 死在一个 **9% 血、206u** 的敌人面前,正是 d22 想抓的东西。
**唯一的真假阳性是同时满足 NO_TARGET 的那条**(§5.1)。
**登记:4.0% 这个数不能当假阳性率用;§4 的 0.2% 才是。**

### 5.3 第二个真假阳性:`_CASHABLE_ULTS` 用**定值**给**按最大蓝百分比计价**的大招定价

§5.2 最后那条 storm 的蓝量是 **15→54**,而 `_CASHABLE_ULTS['storm_spirit_ball_lightning'] = 50`
—— 检测器认定它「握着能放的大招」。可 7.41 的球状闪电**激活价 = 15 + 最大蓝的 7%**,
这局 storm 的 `max_mp=948` ⇒ **真实激活价 81 > 47**。**那一帧他根本放不出来。**

全量量了这一族(112 个 episode,storm 是**最大的桶,占 449 的 24.9%**):

```
蓝量低于大招自己的激活价(15 + 7%·max_mp):12/112 = 10.7%
  检测器的定值地板 50;这批 episode 的真实激活价中位数 = 108
  4(i-a) 两层:ab 8/61 = 13.1%   ba 4/51 = 7.8%   (两层同号)
  最极端:mp=44 / max=948 / 需要 81;mp=67 / max=1776 / 需要 139
```

**缺陷是通用的,域是窄的**:`_CASHABLE_ULTS` 是一张 `{大招: 定值}` 表,
对固定蓝耗的大招是对的,对**按最大蓝百分比计价**的大招随等级越走越偏 ——
表里踩到这条的实际上只有球状闪电,**而它恰好是本波最大的桶**。
⛔ 「15 + 7%」同样是**我的操作定义,不是 dumper 字段**;只算**激活价、不算位移耗蓝**,
是**对检测器有利**的一侧 ⇒ 10.7% 是**严格低估**。

### 5.4 顺带:同名 `.dem` 跨 run 撞车,**本轮当场发生了一次**

章程那条坑(「必须按 run 分目录」)不是历史:`20260902_094711_slot8` **在 ac5166 和 0b4cf2 里各有一个,
是两局不同的比赛**。我的临时逐帧脚本一开始按 `*/timelines/<名字>*` 取 `[0]`,
拿到的是 **0b4cf2 那局 —— 那局根本没有 storm_spirit**(`team=None`)。
脚本里的 `assert len(tps)==1` 把它挡下来了;`sweep_run.sh` 与 `d22_target_legality.py`
**本来就按 run 分目录,没有中招**。**登记:这条坑仍然活着,临时脚本是它的入口。**

---

## 六、核验状态(`VERIFY` 行)

⛔ **本轮没有任何 id 级 WORKING/BUGGY/SILENT 判决,54 个 armed id 全部未核。**
理由不是没干活,是**本轮的对象是仪器不是 gate**:§3/§4/§5 全部在量 `detect.py` 自己的读数
可不可信,而**一个被证明会 2.9% 误报的检测器,不能拿来买任何 id 的条件 (a)**。
按章程 4a 的归属纪律,**不把仪器读数记到 id 名下**。

```
(本轮无 VERIFY 行)
```

---

## 七、开的 issue

**新开 GH #425**([harness],d22 的两个假阳性,§4/§5 全部帧证据 + 两条改法 + 验收方式)。
**#425 是本轮唯一新开的 issue。**

交棒项回音:**(1) GH #421 无回音(0 评论);(3) #419 / #414 / #405 / #389 均无回本组的新评论**
(09:00Z 之后有动静的是 #424/#423/#422/#421/#420/#418/#417/#415/#413/#229)。

---

## 八、验证(退出码全部裸读,无管道)

- `session_setup.sh` **BARE_EXIT=0**(`arn:…:user/dota2bot-agent`)。
- `sweep_run.sh` ×3 **BARE_EXIT=0**,三个 `sweep_complete.json:exit_code=0`、`unparseable=0`。
- `sweep_strata.py` **BARE_EXIT=0**;`d22_target_legality.py` **BARE_EXIT=0**;
  `--selfcheck` **BARE_EXIT=0(6/6)**。
- 修掉 `os_['hp']` 之后在真语料上**重跑,输出与修前逐字相同** —— 符合预期
  (语料里没有 Axe,那条分支两次都没被走到)。**这不是「修复被验证了」,
  验证它的是 §4 的反向断言。**
- 铁律 6 静态半:`bash tools/agent/luacheck_gate.sh` ⇒ **BARE_EXIT=0**,
  `luacheck bots game: 0 warnings`(冷启,自己装的 `lua-check`)。
  `arm_push_gate.sh` ⇒ **0**(本容器已上膛)。
- **未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行可抄。**
- **未改 `bots/` / `game/` 任何一行**(本轮只新增一个 `tools/` 下的 python 工具)
  ⇒ **不声称跑绿 Lua 全量**(GH #124)。

---

## 九、欠账变化

- ✅ **交棒项 (2) 结清**(顺延一轮后,W38 收波当轮做完)——**结论是否定的**。
- ✅ 交棒项 (1)(3) 查完:**全部无回音**。
- ✅ 上一轮新欠账 (d)「§3 的施法距离/斩杀线要真从数据来」**部分结清**:
  口径已入仓成工具并带反向断言;**但数值仍是操作定义,字段仍要 dumper 给**(并入 #405 验收 (b))。
- ✅ 上一轮新欠账 (c)(26717d 的 3 份 timeline 随容器回收)——**不再需要**,本轮语料换成 W38。

**新欠账**:
- (a) **`axe_culling_blade` 那一行 n=0** ⇒ 上一轮的 16.7% **仍未被复现也未被推翻**,
  要等**一个阵容里有 Axe 的波次**。这是**语料的结构性缺口,不是顺延惯性**。
- (b) §3 那三行(`tp_home_wasteful` / `deep_solo_death` / `missed_cs_at_tower`,
  两层同号 + 3/3 粒)**要在第三份语料上复看**。
- (c) W38 的 69 份 timeline **随容器回收**(重跑约 4 分钟,dumper 缓存命中;约 1.4 GB)。
- (d) **补跑机 f65fb4(seed 2613,按需)本轮起飞不到 30 分钟、未收波**,它的录像**没扫**。

**继承未动**:`tp_out` 那 311 条;`stayfield2_whynot.py` 待下一个 44-id 波;
`pullcad_beat.py` 在 W25 剩两 run;`unk` 那一列**第十六轮**;窗口常数只读未量;
W33 0.748 与 W32 0.401 合并成案;`fieldbuy_silence.py`/`stayfield2_margin.py` 分层;
「静止在小兵火力里」检测器;W25 只并 2/4 run;W26–W28 与 W25 从未池化;
`seed 975` **第三十七轮**;`wandlimbo` 因 #293 **第三十五轮**;GH #265 被 #272 阻塞;
`blinkflee` 卡 #304/#305;WK rank-3 全语料复测;载体侧别提案 GH #389;
探针入仓待总监(GH #405);GH #421(攻守归属)仍等裁定,裁完要重扫对照语料
并回答 `state.json:491` 怎么办。

---

## 十、下一轮第一件事

**(0) 先读本节,不要抄过期的交棒行。**
1. ⭐ **扫补跑机 f65fb4(seed 2613)的录像**(新欠账 (d))——它是 W38 的第四粒,
   扫完 W38 才是四粒齐的语料,`sweep_strata.py` 的三粒读数才有第四粒可加。
2. **§3 那三行在第三份语料上复看**(新欠账 (b),零新工具)。
3. **查 GH #425 / #421 / #419 / #414 / #405 / #389 回音**;
   #405 验收 (b) 与 §4 的 `RANGE_FLOOR` 字段**是同一个要求**。
4. **盯下一个阵容里有 Axe 的波次**(新欠账 (a))——只有那种波次能回答上一轮的 16.7%。

---

## 十一、Token 用量

```
TOKENS total_in=6,485,122 out=42,484 turns=57
```

(统计时刻之后的收尾回合不计入。)
