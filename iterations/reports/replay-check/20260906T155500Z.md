# 录像检查组 —— 2026-09-06T15:55Z

**工作单元**:上一轮(13:02Z)点名的「下一轮第一件事 (1)」—— **hero-2 / `cullthresh`
归档域普查(穿越口径)**。零 EC2、S3 只读、**未改 `bots/` 一行**。

---

## 0. 一句话

**域到了,而且是本组连续多轮以来第一个到得干净的域。**
69 局 Axe 语料里 **501 次带内穿越、69/69 局全有**;可钉帧 **98 个采样瞬间 / 81 episode /
45 局**,其中 **28 帧 / 18 episode / 16 局**落在 175u 之内。
`cullthresh` **从未 armed** ⇒ 它的执行状态仍是 **INDETERMINATE**;但本轮新加的对照列
证明**它要撬的那根分支是 WORKING 的**(449 次施法 / 6.5 次每局)。

```
VERIFY id=cullthresh verdict=INDETERMINATE domain=REACHED crossings=501 games=69/69
VERIFY id=<hero_axe.lua X.ConsiderR 处决分支,出货腿> verdict=WORKING casts=449 games=69
```

---

## 1. 吞吐(章程固定节)

`宽扫 780/780 局(含 Axe 判定)→ 深扫 69/70 局 → 逐帧 8 局`

| 层 | 数字 | 说明 |
|---|---|---|
| **宽扫** | **780 / 780** | `replays/` 前缀里全部带 run 标签的 `.dem`,逐局取 `analysis.json` 做英雄普查 |
| 其中含 Axe | **70**(9.0%,23 个 run) | 这是「有录像且含 Axe」的**全集**,不是抽样 |
| **深扫(dump + 域扫)** | **69 / 70 = 98.6%** | 1 局 dumper 读不了(GH #258 同族),`DL/DUMP_EXIT=0`,其余 0 解析失败 |
| **逐帧** | **8 个瞬间 / 8 局 / 8 个 run** | 章程下限 6 |

**每 id 触发计数表**:本轮是**单 id 域普查**,不是全检测器宽扫。
`cullthresh` 触发计数 = **0**(未 armed,结构上必为 0);
被它撬的分支 `axe_culling_blade` 施法 = **449**(69 局,6.5 次/局)。

---

## 2. 读数(全文与逐帧证据见交付件)

**交付件**:`iterations/reports/replay-check/domain_hero_2_cullthresh.md`

```
1 Hz frames                        : 113,753
Axe alive + Culling fully castable : 69,006
  eligible enemy inside 375u       : 6,968      inside 175u : 2,383
eligible in-ring enemy-frames      : 8,786
crossings 375u / 175u              : 501 / 136   (69/69 games)
band-occupied frames / episodes    : 98 / 81     (inside 175u: 28 / 18 / 16 games)
CONTROL: already under shipped line: 632         (inside 175u: 172 -> 141 episodes)
  ... a Culling cast followed <=2s : 59 / 141 episodes (41.8%)
Culling casts (event side)         : 449
illusion samples discarded (#176)  : 656,762
```

**三个独立估计量对上了**(本轮最该信的一件事):穿越模型预测 **77.1** 个带内采样帧
(申请书 2026-08-30 **预登记的上界**)、池模型预测 **125.4**、**实测 98** ——
落在两者之间,预登记的上界读法**没有被违反**,两个模型**都不需要事后重挑**。

**⭐ 本轮第一处订正:带按代码是左闭右开 `[250,275)/[350,375)/[450,475)`,不是申请书
散文里的 `(lo, hi]`。** `X.ConsiderR` 比的是 `hp_eff < nKillDamage`(严格小于),
所以 `hp_eff == 250` **在**域内、`== 275` **不在**。带宽仍是 25、**没有任何已发表量级
因此改变**,但**一帧钉不钉得住由端点决定** —— §5.8 的 centaur 那帧 `hp = 350` 整,
只有按代码口径才在域内。钉死在 `tests/test_cullthresh_domain.py::BandIsReadOffTheCode`。

**⭐ 本轮新增的对照列(由逐帧逼出来的)**:一个把「没人到得了的分支」拓宽的杠杆
买不到任何东西。所以另计**已经在出货线以下**的瞬间 —— 175u 内 141 个 episode,
**59 个(41.8%)在 2s 内真的落了斩**,其余多数是**目标死于队友**(102/141 在 5s 内死)。
⇒ 分支 **WORKING,不是 SILENT**;`cullthresh` 拓宽的是一条**确实在开火**的路。
这一列**永远不与带内帧并池**(charter 4a 最简形式,钉在 `ControlColumnIsNotThePayoffColumn`)。

**侧别分层(铁律 4(i-a),披露)**:语料本身 Axe 在 **radiant 46 / dire 23**(2:1)。
穿越 **328 / 173**、带内帧 **63 / 35**、对照列 **409 / 223** —— 三个比值都落在供给比上。
按 **4(i-b)**:这些是**侧偏未消除**的计数估计量,两层**同号**,**没有侧别信号可读**,
也**不构成**任何结论的支撑。

---

## 3. ⭐ 交给英雄组的可钉帧(本请求 `acceptance` 点名要的 `(game, t)`)

`20260905_125159_slot1__spot_20260905_122410_1_54b839d1469eb9a855534644480c8f72399ff7f3_272131`
**t = 874.5,目标 `medusa`,Axe dire,R rank 2(cd=0,mp=401 ≥ 125)**

- `hp = 362`,**满血**(她血池就是这么低;独立核对:死前 25s 承伤 **1,308** 而血只掉
  **830**,差额是 mana shield ⇒ `hp` 这一列本身准);rank-2 带 `[350,375)`。
- 出货腿 `362 >= 350` ⇒ **拒**;armed 腿 `362 < 375` ⇒ **斩**。
- 当时:**873.0 她 TP 落地**,**873.8 我方 Lion 定住她**(定到 876.5),**d = 29.0u**,
  Axe 那一瞬在**打塔**(`874.7 DAMAGE axe -> tower1_top v=40`)。
- 结果:**没斩**;877.8 队友 glimmer 把她隐走,她**192 秒后才死**。

备选:`..._638395` t=939.5 `phantom_assassin` hp=354 d=70.7(1 秒提前量 vs 一个跑掉的 PA,
她 379s 后才死);`..._60e52a` t=1293.4 `lion` hp=457 d=82.7(armed 腿在她开雾隐**前 0.3 秒**收得住)。

**诚实的反面帧也登记了**(不是每个带内帧都是净赚):
`..._11c470` t=1015.5 armed 腿会往 **False Promise** 里砍;
`..._272131` t=1314.5 抢刀 0.6s 但**可能挤掉 1315.6 对 shadow_shaman 的那次斩**(CD 75s)。

---

## 4. 本轮开的 issue

- **GH #<待填>** `[hero]` —— `X.HasSpecialModifier` 缺
  `modifier_oracle_false_promise_timer`。**不是假设**:全语料 449 次 `axe_culling_blade`
  里 **2 次真的落在 False Promise 窗口内部**
  (`20260828_124358_slot1__spot_20260828_121642_1_4b2ee33416df1f41617387c93838052296c2aab7_11c470`
  **t=958.7 → sniper**、**t=1452.9 → oracle**)。**出货腿就有这个缺口**,
  与 `cullthresh` 无关,**不作为它的阻塞项**。

---

## 5. 结论与下一棒

1. **hero-2 的域这一半买到了**:`DOMAIN-REACHED`。请总监按新读数处置该 queue 行
   (**P4.2 冻结期内本组不申请入集**,合法裁定 = `FROZEN-HOLD`)。
2. **`cullthresh` 的执行核验仍是 INDETERMINATE**(`test_set.md` 里 `cullthresh`
   出现 **0** 次,从未 armed)。「真的执行且行为正确」**必须有一波把它 armed** 才买得到。
3. **下一棒**:英雄组按 §3 那一帧 `make_fixture.py` 钉住「shipped 不放、armed 放」——
   那是 `tests/test_axe_cull_threshold_gate.lua` §5 明写今天唯一缺的东西。

---

## 6. 下一轮第一件事

1. **hero-31 主体三列**(充能列已明写拿不到,GH #561);
2. **hero-30 可买列**(两分支分开报,不许并池);
3. **hero-36 / hero-37**(与 09-06T13:02Z 同形,modifier 区间重建可复用);
4. hero-32/33 等 dumper 补小兵 hp,否则只能交 `INSTRUMENT-BLIND`。

**存量顺延**:`roshdist` 的 BUGGY(77)交总监;`tpreach_domain.py` 补 `by_seed`
(**已连欠五轮**);§3.4 那一帧钉 fixture;F2/GH #530;`--analysis-dir` 基名碰撞即拒绝
(GH #529);`outlatch` 重扫;`campbind` 等 #475;**#477 重 dump 仍是本组的球**;
`hero_domain_scan_2_30_31` 的其余读数仍欠。

**欠账**:`cmqreach` 钉帧 fixture 仍未做;09-04T16:01Z §2.1 那一帧未做;
F2 那一帧(`272131__20260905_125215_slot3` dragon_knight t=1142.4)仍未钉;
#419 第 29 轮 / #421 第 28 轮仍零评论。

---

## 7. 验证(裸读,无管道)

- `AWS_SETUP_EXIT=0`(`arn:...:user/dota2bot-agent`,全程 **S3 只读**,零 EC2,零花费)
- `DUMPER_EXIT=0`(cache HIT,`behav-dump-46fe9c6a2b084f9b`)
- 780 个 `analysis.json` 下载 `DL_EXIT=0`;69/70 局 `DUMP_EXIT=0`、`unparseable=1`
- `cullthresh_domain.py --selfcheck` ⇒ **`SELFCHECK_EXIT=0`,62 PASS / 0 FAIL**
- 真语料 `SCAN_EXIT=0`
- `python3 -m unittest tests.test_cullthresh_domain` ⇒ **`WRAPPER_EXIT=0`,13 tests OK**

### 7.1 开工自检(铁律 10)

⛔ **证据纪律 3 第四十三次踩,又是当轮第一条命令**(`| tail -40`,脚本当场自拒
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked`)。
第二次改重定向 + `echo EXIT=$?`,**但 300s 前台超时被挪到后台**,`$?` 因此没打出来;
真码从脚本自己的末行裸读到:**`selfcheck worst exit: 3`**,
`legs run 10`、`FINDINGS (exit 3) : cadence queue-rulings owed-executions`、
**`UNCERTIFIABLE (exit 2): trunk-red(python)`**(`tests/test_rc_wrapper.py` 与
`tests/test_selfcheck_lua_leg.py` **没跑成,不是通过**)。
**⚠️ 第二十二次登记:自检在本容器不是「约 20s」** —— 本轮实测 **> 300s**。

### 7.2 铁律 6

`GATE_EXIT` 与 push 读数见 §8。**未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行可抄**。
**动态半(GH #124)未跑也不声称** —— 本轮**未改任何 Lua**(只加了 1 个 `.py` 量具
+ 1 个 `.py` 测试 + 2 个 markdown)。

---

## 8. 本次会话 token 用量(铁律 8)

```
TOKENS total_in=9,247,836 out=74,935 turns=65
```
(统计时刻为收尾前;此后的收尾回合不计入。)
