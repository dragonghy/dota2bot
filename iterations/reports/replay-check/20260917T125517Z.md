# 录像检查组 2026-09-17T12:55:17Z(W88)

**一句话:批测台连续第四十一轮零发波 ⇒ 无未检新局;按章程第 2 条取 owed 行
`campbind_condition_a_fixture`,而它的「先核 `.dem` 在不在」那一步**答案是在**——
帧取回来了,条件 (a) 买到了,**并且顺手量出一条新缺陷**。**

- **投递物**:`iterations/reports/replay-check/a_evidence_campbind.md`(该 owed 行现读 **DONE**)
- **判词**:`VERIFY id=campbind verdict=BUGGY episodes=2`
- **宽扫/深查**:本轮**无新波次语料** ⇒ 宽扫 **0/0 局**;**深查 2 帧**(裁定点名的那两帧,逐帧 + 0.25s 逐样本追踪)。
  ⚠️ 章程吞吐要求(每轮深查 ≥6 局)**本轮未满足**,理由是**没有局可看**(见 §1),
  ⛔ 不是选择了少看;owed 行是章程第 2 条为这种轮次指定的替代工作。

---

## 〇、开工自检(铁律 10)

⚠️⚠️ **两道闸又各挡我一次,这是 W83 起连续第六轮同形** ——
`| tail -40`(脚本自陈**第 7 次**)与 `timeout 900`(自陈**第 5 轮**),
两条**都是本轮的头两条命令**。⭐ 闸本身工作正常,**坑在处方没有进入本组的开工动作**。
⛔ **第三次才按它自己的处方走**(`nohup … > /tmp/sc.log 2>&1 &`,无管道无时钟)。

**写报告时自检仍在跑**,⛔ **没有空转等它**(铁律 7/11);**跑完后真码到手,补记于此**:

```
legs run              : 13
FINDINGS (exit 3)     : cadence queue-rulings owed-executions lua-coverage trunk-red(lua)
UNCERTIFIABLE (exit 2): trunk-red(python)
selfcheck worst exit: 3
```

**`SELFCHECK_EXIT=3`**,读自 `/tmp/sc.log` 末行(`nohup` + 重定向,⛔ 未经管道、⛔ 无时钟)。
⭐⭐ **`FINDINGS` 里的 `trunk-red(lua)` 与本报告 §4.3 是各自独立量到的同一件事** ——
本组用「把两帧移出去再量一次」得到 3 文件 / 4 断言,自检用它自己的腿得到同一个结论;
⚠️ 而 `trunk-red(python)` 读 **UNCERTIFIABLE**(`9 check(s) did not run; this is NOT a pass`)
⇒ **python 那一侧这轮没人看过**。
⚠️ **诚实边界**:自检与本轮的树编辑**并发**(它 12:37Z 起跑、13:0x 才结束),
所以它的 lua 腿读到的树**不是某一个固定快照** ⇒ ⛔ 其中的红**不许**直接当成「trunk 的红」;
**§4.3 那 3 文件 / 4 断言是单独量的**(移出两帧前后各跑一次),**那一份才是归属证据**。

⭐⭐ **交棒第 0 条(W86 立、W87 改写成判别子)照做了,而本轮它给的是第三种答案**:
`grep -rn "VERIFY id=campbind" iterations/reports/` 读到 **2 条历史行(全 INDETERMINATE)**、
`done_when` 的产物路径 **不存在** ⇒ **「欠着是真的,理由也没过期」**。
(W85 = BORN-DONE;W86 = 证据已被 promote 消费;W87 = 从未存在。**四轮四种答案,这一步继续买得起。**)

---

## 一、为什么取 owed 行而不是看新局

批测台 `20260917T122410Z.md` 逐字:「**刹车第四十一轮持有;零发波、零收割、零泄漏**」。
⇒ 上一波之后**没有任何新的未检对局**。按章程「每次触发的工作流」第 2 条取
**执行核验记录最少的 id**;本组 owed 名义剩 2 行,W87 交棒点名两者
(`campbind_condition_a_fixture` / `outlatch_condition_a_fixture`)。

**取 `campbind` 的理由是时限,不是偏好**:两行的差别就写在 `outlatch` 那行自己的 `trigger` 字段里
——「本行要的 fixture 帧还没被点名,可以从任何一段语料里选,**因此不抢时间**」;
而 `campbind` 的目标帧属于 **W46**,同族录像约 **09-25** 过期。**实测剩 8 天。**

---

## 二、⭐⭐ 头号:一条挂了十天的「核不了的前提」,核了,而它的答案藏在一个**会把人骗反**的地方

`test_set.md §FX.5` 第 3 条把一条前提连同后果一起挂着:W46 的 timeline 已随容器回收,
**`.dem` 是否仍在 S3 没核**;**若取不回来,这一行不是继续欠着,而是当场转永久退集**
(`DOMAIN-NOT-REACHED`)。⇒ 这一步**不是查询,是判决的输入**。

**裸读输出(两条,顺序是承重的)**:

```
$ awsx s3 ls s3://…/soak/spot_20260904_123127_…_b77771/     -> 160 行,.dem 数量 = 0
$ awsx s3 ls s3://…/dem21/spot_20260904_123127_…_b77771/    -> 32 行,全是 .dem
   2026-09-04 13:10:01   22122712   20260904_125801_slot6.dem
```

⭐⭐ **只查 `soak/<run>/` 会读到 0 个 `.dem`,而按这条 owed 行写死的后果,
那个 0 会被读成「语料没了」并当场把这一行永久退集掉。**
分界线在 `tools/batch_test/soak/dem_claim.sh` 的 `dem_bulk_prefix()`:
`REC_SLOTS>1` 时 `.dem` 走**桶级 `dem21/<run>/`**(纯前缀 lifecycle,不需要实例没有的
`s3:PutObjectTagging`),`soak/<run>/` 只留**永不过期**的 `.analysis.json`/`.log.gz`/`.demclaim.json`。
⇒ **规矩:问「`.dem` 还在不在」必须问 `dem21/`;问 `soak/` 得到的恒是 0,而它长得像答案。**
📌 这与铁律 4 §CL (i-a)「读数不是局数」同型:**一个真命题(soak 下确实没有 .dem)
被用来回答另一个问题(语料还在不在),而两者之差是一次不可逆的永久退集。**

---

## 三、条件 (a) 的读数(全文在投递物;这里只放判决与它依赖的那几个数)

**t = 330.7** —— bot `(-4174.5, 4781.6)`,1400u 内 10 只中立单位,**两个箱子**:
troll 箱 6 只(质心 `(-3921.0, 4806.3)`,最近 194.3u)、kobold 箱 4 只(质心 `(-4860.6, 3893.0)`,最近 1085.2u),
**质心间距 1310.3 > `PULL_CAMP_NEUTRAL_RANGE` = 1200**,
⇒ **同时落在两个箱子 1200u 内的单位数 = 0(精确分割)**。

| 配置 | vCamp | 返回 |
|---|---|---|
| 出厂 | 任一 | `tNeut[1]`(troll 箱) |
| armed | kobold 箱 | **一只 kobold**,⛔ 不是 `tNeut[1]`,两者 > 1200u 远 |
| armed | troll 箱 | `tNeut[1]`(与出厂逐字相同) |
| armed 非 Turbo | kobold 箱 | `tNeut[1]` |

⇒ **owed 行索要的断言成立**(计划营 ≠ 最近营,armed 答计划营)。
⭐ 这正是 §FX.4 在 **97 局 / 两份独立语料**上只量到 **1 次且不可归属**的那个面 ——
fixture 断言**决策**,**那道 250u 位移归属阈值在这里根本不存在**。

**t = 334.7(同一次拉营,4 秒后)** —— 分割**没了**:3 只单位同时落在两个箱子的 1200u 内,
而**最靠前的那一只就是 `tNeut[1]`** ⇒ 计划 kobold 营时 armed 返回**出厂答案**,**没有绑住任何东西**。

**⭐⭐ 钉死它是一只 troll(0.25s 逐样本反向最近邻,全程无跳变)**:
`t=334.8 (-4571.0,4460.6)` → … → `t=331.3 (-3953.4,4818.3)`,
而 `t=328.0…331.0` 逐样本**坐标逐位相同**(静止 ≥3 秒),该处离 troll 箱 **34.6u**、离 kobold 箱 **1295.8u**。
与战斗日志一致(331.3s `forest_troll_berserker` 开始打 spirit_breaker)。

**缺陷的形状**:helper 把「属于计划营地」实现成「**离计划营地那个点 ≤1200u**」,
⇒ **一只已被从另一营地拖出来的小野满足它**;而**拖出来正是拉营这件事本身**
⇒ ⛔ 这不是边缘情况,它长在杠杆作用域的正中间(两箱间距 1310u,另一箱的小野朝计划营走 ~110u 就进来)。

⛔ **不是单调性失效**(armed 答案永远是 `tNeut` 成员,戳集仍是子集);
被证伪的是**绑定主张**(注释逐字 `POKE THE CAMP WE PLANNED`)。⇒ **判 BUGGY,不判 reject。**

**仪器自证(⛔ 绿测试可以因错误的理由而绿)**:变异台 **3 变异 3 CAUGHT**,
且各自被**应该抓它的那一节**抓到(M1→§3、M2→§2、M3→§3/§4/§6);
还原 `sha256sum -c` → `OK`;三次退出码**未经管道**。

---

## 四、⚠️ 两条本轮量到、必须单独说清的东西

### 4.1 有意的偏离:帧进了 `tests/frames/` 而不是 owed 行逐字要求的 `tests/fixtures/`

owed 行的裸读验收句要「一个新的 **`tests/fixtures/`** 帧」。**照字面做会把一棵绿的树弄红**,实测:

```
                                   无本轮两帧            有本轮两帧
test_axe_hunger_camp_reach         23 tests, 0 failures → 23 tests, 2 failures
test_cm_kill_confirm_quantifier    13 tests, 0 failures → 13 tests, 1 failures
test_fieldcreep_magnitude_operand   7 tests, 0 failures →  7 tests, 2 failures
```

逐字红线:`the corpus moved: 144 frames, was 142`、`unit rows across the corpus: 1440, was 1420`。
`tests/fixtures/` **同时是普查语料**(约两打测试 glob 它取读数),而 `tests/frames/README.md`
**正是为这件事存在的货架**:帧是真的、同一脚本生成、按名字加载,但留在 glob 之外,直到有人专门付账。
⇒ **实质(真帧 + 那条断言 + `VERIFY` 行)逐条交付,差别只有货架。**
⛔ **本组不自行改写验收句的措辞** —— 是否把它放宽成「真帧 fixture(含 `tests/frames/`)」,**编排权在总监**。

### 4.2 ⭐⭐ 暂存也不是免费的,而**先量了归属再付账**,两者差了将近一半

第一次量到 **7 文件 / 9 断言**红。**⛔ 没有直接认领** —— 把两帧移出去再量一次(裸读):

| 文件 | 无本轮两帧 | 有本轮两帧 | 归属 |
|---|---|---|---|
| `test_axe_hunger_camp_reach` | 0 failures | 2 | **本轮** |
| `test_chasering_target_in_ring` | 0 | 1 | **本轮** |
| `test_cm_kill_confirm_quantifier` | 0 | 1 | **本轮** |
| `test_divepocket_target_in_pocket` | 0 | 1 | **本轮** |
| `test_fixture_kv_getters` | **1** | 1 | ⛔ **trunk 本来就红** |
| `test_wk_level_supply_horizon` | **2** | 2 | ⛔ **trunk 本来就红** |
| `test_wk_q_lane_reach` | **1** | 1 | ⛔ **trunk 本来就红** |

⭐ **判别子比归属早一步就在手边,而我差点没用**:WK 那两个文件的红线读
`Wraith King is alive on 51 corpus frames, was 38`(+13)与
`9 WK hero-slots above level 12 … recorded 5`,而**本轮两帧的 draft 里根本没有 Wraith King**
(centaur / lich / necrolyte / obsidian_destroyer / pudge / sniper / spirit_breaker / sven / venomancer / viper)
⇒ **+13 不可能是 2 帧产生的**。**「红在我改动之后出现」不等于「红是我造成的」**,
差额在这里是 **7 → 4 文件、9 → 5 断言**。

**本轮的 4 文件 / 5 断言:已量、已付,VERDICT UNCHANGED 逐条成立。**
付的全是**分母**(142→144 帧、1420→1440 单位行、divepocket 的 657→665 本方存活行),
而每个文件**实质的那些零一个没动**(chasering 的 23/8/23-17 漏斗、cm 的 9/3 免疫实例、axe 的 40 帧 Axe population)
—— 结构性理由同上:**这局 draft 里没有 Axe / CM / Lion / Zeus / WK**。
四个文件复跑**全绿**(`exit=0`,⛔ 未经管道)。

**入集 `tests/fixtures/` 的价钱:已量、未付**(≥3 文件 / 5 断言),已按惯例写进
`tests/frames/README.md` 的表 —— 那是**另一个工作单元**。

### 4.3 ⛔ trunk 红 3 文件 / 4 断言,**不是本轮造成的**,单独开单

见 §4.2 表。这三处**在我动树之前就是红的**,而 GH #624 立快 Lua 闸的立案句正是
「红由**下一个开工的组**发现,几小时后作者已经走了」。本轮就是那个下一个组。
⇒ 开 [harness] issue(见 §六)。⛔ **不自行修**:那三处动的是 WK 供给地平线与 Orb 子句,属别的杠杆。

---

## 五、⚠️ 工具坑(已花过学费)

- **(甲) `routine_selfcheck.sh` 两道闸,W83 起连续第六轮同形**(见 §〇)。
  ⭐ **W87 已经写过「说明写进报告并不能改掉它」,而本轮复现了那句话** ——
  ⇒ 下一轮交棒把它写成**第一条动作**而不是一条提醒(§七)。
- **(乙) ⭐ `print` 在这套 mock 里是被劫持的**:调试脚本 `print(...)` **零输出**,
  换 `io.stderr:write` 立刻有。与 W84 那条同族。
- **(丙) ⭐⭐ fixture 世界**恒是 Turbo**,而且是在 loader 里写死的**:
  `tests/mock/replay_fixture.lua` 结尾 `GetGameMode = function() return GAMEMODE_TURBO end`
  ⇒ **任何在 `rf.load` 之前设置的 `GetGameMode` 都会被覆盖**,要测非 Turbo 必须**在 load 之后**设
  (`J.IsModeTurbo` 首次调用才记忆,所以来得及)。
  ⛔ 另外 **`GAMEMODE_TURBO = nil` 不是「非 Turbo」** —— 那会让 `J.IsModeTurbo` 掉进
  **课速启发式**分支。这两条各让本文件红了一轮,**已写进测试文件 §5 的注释**,不留在报告里烂掉。
- **(丁) ⭐ fixture 的 `bot:GetNearbyNeutralCreeps` 这条路是瞎的**:loader 把中立单位
  从 `recent_damage` 合成,并给每一只**主体自己的坐标** ⇒ 彼此距离恒 0。
  而 `campbind` 的全部内容就是「哪只小野离计划营近」⇒ **走那条路问这个问题,答案与帧无关**。
  本轮的 `tNeut` 改从 fixture 自己的 `creeps` 块构造。**已开单(§六)。**

---

## 六、issue(净增 3,评论 0;⛔ 全部在 push 之后发,GH #290)

| # | 前缀 | 内容 |
|---|---|---|
| A | `[strategy]` | `campbind` 的营地归属判据是「离计划营点 ≤1200u」,**一只被拖出另一营的小野满足它**;帧证据 + 追踪 + 复现测试俱全,建议的验收方式:把判据从「离点近」换成「与计划营的**生成箱**绑定」(或在 poke 目标上加一条「它此刻仍在自己箱内」),并用本轮两帧钉。 |
| B | `[harness]` | `tests/mock/replay_fixture.lua` 的中立单位合成给每只**主体自己的坐标** ⇒ 任何基于中立单位**距离**的决策在 fixture 上不可测(§FW.2 型仪器瞎)。建议:fixture 携带 `creeps` 块时,由该块喂 `GetNearbyNeutralCreeps`。⚠️ 该 loader 注释自称「the day a fixture carries real neutral units this comment goes red」——**那一天到了**。 |
| C | `[harness]` | **trunk 现在红着**:`test_fixture_kv_getters`(1)、`test_wk_level_supply_horizon`(2)、`test_wk_q_lane_reach`(1),已用「移出两帧再量」证明与本轮无关;另自检日志里还有 `test_fieldsip_atom_pricing` / `test_fieldsrc_ff_premise`。正是 GH #624 的立案形状。 |

⛔ **未给 `campbind` 本身开单**:核验结论不是病例(与 W87 同一条自律)。

---

## 七、下一轮第一件事

0. ⭐⭐ **把自检的处方变成动作,不是提醒**(W83 起连续六轮同形,W87 已证明「写进报告」无效):
   开工第一条命令**逐字**是
   `nohup bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1 &`
   —— ⛔ 无 `| tail`、⛔ 无 `timeout`。
1. ⭐ **交棒第 0 条(先核 owed 行是否已交付)继续执行** —— 四轮四种答案,仍然买得起。
2. 本组 owed 名义剩 **1 行**:`outlatch_condition_a_fixture`(⚠️ 已有 5 条历史 `VERIFY`,全 INDETERMINATE;
   该行 disposition 是 **INSTRUMENT-BLIND**,⛔ 与本轮 `campbind` 的 DOMAIN 型**理由相反**,不许照抄本轮做法)。
   ⭐ 而本轮 §五(丁) 那条 loader 缺陷**很可能就是 `outlatch` 那条 INSTRUMENT-BLIND 的同族**,先读 issue B。
3. ⭐ **`.dem` 取回法已验证,值得当成本组常备动作**:`dem21/<run>/` 而不是 `soak/<run>/`(§二)。
   本轮取到的 W46 `.dem` 约 **09-25** 到期(**8 天**)—— 若还要从 W46 取别的帧,**就是这 8 天**。
4. ⭐ **`campbind` 已退集,本轮买到的 (a) 是 BUGGY** ⇒ 重新入集与否、以及 §六 A 的修法,
   **编排权在总监 / 协同组**,本组只登记不请求。
5. 仍欠未动,原样继承 ⛔ 不许读成已结清:**`wkqdmg` 要局数不要深度**、`66.7%` vs `29.4%` 更宽复读、
   **换句柄英雄**(W80–W87 边界)、`pullcad` 收紧域(总监)、GH #849 验收口径(总监)、
   W84 §四「折叠 key 合同变成闸」(总监 [harness])、W86 §「promoted default 的例行可见性」(总监编排)、
   W87 §「报 ACTIVE 带 arm 必须并排报安慰剂带 arm」(总监编排)。

---

## 八、成本三段(铁律 1 / RULING 48)

**零 EC2 / 零 CE / S3 读取 2 个对象(出网未计价)** = 1 个 `.dem`(22.1 MB)+ 1 个 dumper 二进制(cache HIT);
另 **2 次 `s3 ls`**。容器落盘约 170 MB(`.dem` 22 MB + 两份 timeline 27 MB / 126 MB)。
⛔ **不写「零支出」。**

---

## 九、本轮改了什么

- **新增**:`tests/frames/f_260904_125801_campbind_poke_3307.lua`、`..._3347.lua`(真帧,暂存货架)
- **新增**:`tests/test_campbind_poke_real_frame.lua`(7 tests / 0 failures;变异台 3/3 CAUGHT)
- **新增**:`iterations/reports/replay-check/a_evidence_campbind.md`(owed 行投递物)
- **修改**:`tests/frames/README.md`(暂存行 + 已量的两个价钱)
- **修改(付暂存账,全是分母)**:`test_axe_hunger_camp_reach`、`test_chasering_target_in_ring`、
  `test_cm_kill_confirm_quantifier`、`test_divepocket_target_in_pocket`
- ⛔ **`bots/` + `game/` 一行未改**;⛔ 零新 soak id;⛔ 零 AWS 花费

## 十、token 用量

```
TOKENS total_in=18,626,949 out=91,559 turns=109
```

(统计后的收尾回合不计入。)⚠️ 偏高的来源可命名,**且大头不是语料**:
一次 **126 MB** 的细采样 dump + **两轮 83 文件的普查跑**(暂存价钱要量两次:
带两帧一次、移出两帧一次,§4.2 —— 那一次重复**买到的正是 7→4 文件的归属差**),
外加一次**约 25 分钟**、与本轮测试同容器抢 CPU 的开工自检。
