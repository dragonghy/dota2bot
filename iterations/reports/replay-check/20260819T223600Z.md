# 录像检查组 — 2026-08-19T22:36Z(第十二次触发)

**一句话**:22:11Z 的 (a) 取证波里,`zusult` 拿到了第一份执行核验(**Q 侧压住了、W 侧被
没上门的 `ConsiderW2` 抵消**,帧证据在 GH #47),而 `axebuyblink` 的取证位**空转** ——
224 局 mirror 有效局里 **Axe 出现 0 次**,因为一个种子只产一套阵容、一波 4 种子只抽 4 套,
Axe 单种子出现率 15.8% ⇒ **一波完全缺席的概率 50.4%**(GH #46,附已 4/4 逐英雄验证的离线选种工具)。

## 0. 覆盖数字(章程固定节)

- **宽扫:224 / 224 局**(本波四个 run 的全部 mirror 有效局,`analysis.json` 级;
  另 24 局暖场局 stamp `829202a`,**批测台的必查项通过**)。
  外加 **detect.py 全检测器跑满 4 局**(现存 `.dem` 的全部含 Zeus 局)。
- **深查:4 局逐帧**(`20260819_222052/222620`=armed、`223135/223633`=baseline)。
  低于章程 6 局下限,**原因是结构性的**:本波只有 slot1 出 `.dem`,触发时四个 run 各落 4-5 个,
  而 Zeus 只在种子 869 的阵容里 ⇒ **含 Zeus 的 `.dem` 全世界只有这 4 个,我全查了**。
  (另外 8 个 non-Zeus 的 `.dem` 对本轮两个目标 id —— `zusult`/`axebuyblink` —— 结构上无信息。)
- 每 id 触发计数见 §2 / §3。

## 1. 对象

批测台 22:12Z 报告启动的 (a) 取证波,19 id 全 armed,新 armed 三个:
`tpdead` / `axebuyblink` / `zusult`。

| run | 种子 | mirror 局 | 阵容(该 run 全部局都一样) |
|---|---|---|---|
| `spot_20260819_221108_1_829202ac…` | 867 | 54 | crystal_maiden dragon_knight juggernaut lich lina medusa obsidian_destroyer queenofpain viper witch_doctor |
| `…221112…` | 868 | 58 | crystal_maiden drow_ranger jakiro lich necrolyte nevermore obsidian_destroyer ogre_magi sniper viper |
| `…221117…` | 869 | 54 | centaur chaos_knight dragon_knight shadow_shaman skeleton_king skywrath_mage slardar vengefulspirit viper **zuus** |
| `…221122…` | 870 | 58 | dragon_knight ember_spirit jakiro lina ogre_magi skeleton_king slardar sniper sven venomancer |

## 2. `axebuyblink`:**NOT-EXECUTABLE(本波)** —— 不是没触发,是英雄没上场

- **Axe 出现 0 / 224 局**;lion 同样 0 / 224;27 个英雄出场,池子里有 42 个。
- 根因不是 bug:`custom_loader.ApplySoakDraft` 用种子**确定性**推阵容(镜像 A/B 的前提),
  所以**一个种子 = 一套阵容**,224 局是 4 套阵容各重复 ~55 遍 ——
  **对"英雄是否出场"这个问题,样本量是 4,不是 224。**
- 量化:Axe 在 `hero_pool.txt` 里只挂 pos 3(12 个候选),**单种子出现率 15.8%**,
  **一波 4 种子完全缺席的概率 50.4%**。焦点英雄里最难抽的一个,恰好是本波要取证的那个。
- **补救已交付**:`tools/batch_test/soak/seed_draft.py` —— `ApplySoakDraft` 的逐行 Python 复刻
  (同一个 Lehmer LCG、同样的"最稀缺位置优先"、先 radiant 后 dire)。
  **保真度自检:`--selftest` 对本波真实的 867-870 四套阵容 40/40 个英雄槽逐位命中。**
  `--find axe --count 4` → **872 / 875 / 885 / 887**;`--find axe,lion` → 872 / 910 / 974。
- 已开 **GH #46 [harness]**,含建议:(a) 取证波按 `--find` 挑种子(成本为零),
  (b) 经济隔离波保持连号(挑种子会破坏跨波可比性)—— 裁定权留给总监。

## 3. `zusult`:**WORKING(Q 侧)+ 漏(W 侧)**,方向性,n 小 —— GH #47

Zeus 在 869 的 radiant ⇒ `:s869:radiant` 局 armed、`:s869:dire` 局基线,**同阵容天然对照**。

门(`X.zuus_ShouldSaveManaForUlt`)的全部**可离线观测**前置在真实帧上重放:
R 已学 + cd=0 + 蓝 < 225(**一级耗蓝是从本波真实开大帧量出来的**:施法瞬间 mp 603→387、332→99,
含 ~7/s 回蓝)+ 目标是 ≥60% 血的英雄;`J.IsRetreating` 不可观测,用
"Zeus 血 ≥55% 且 3s 内没挨过英雄伤害"代理 ⇒ **域内计数是真实漏帧的上界**。

| | armed(2 局) | baseline(2 局) |
|---|---|---|
| 干净机会帧 | 22 | 10 |
| **域内 Q(`arc_lightning`)施法** | **0** | **2** |
| **域内 W(`lightning_bolt`)施法** | **3** | **0** |

**结论靠的是同一侧内部的劈叉,不是两侧总数差(n 太小)**:`SkillsComplement` 里
**Q(L294)和 W(L263)走同一个门调用**。门若整体没生效,Q 也该照漏;
**Q 一次没漏、W 漏 3 次 ⇒ 缺陷在门的下游** —— W 是本文件里唯一被两个消费方共用的句柄,
而 `ConsiderW2` **完全没上门**:压住 W 的那一帧顺位落到 W2,同一发蓝原样花在同一个目标身上。
这正是**总监 §J.0.3 批准入集时自己登记的判读前提,本轮从"预判"变成"实测到了"**。

三帧(全部满足门的每一条可观测前置):

| 局 | t | 目标 | 目标血 | Zeus 血 | 蓝 | 800u 内友军 |
|---|---|---|---|---|---|---|
| `222052` | **386.2** | shadow_shaman | 0.82 | 0.73 | 163/740 | 1 |
| `222052` | **540.9** | shadow_shaman | 0.73→1.00 | 1.00 | 149/812 | 2(第 3 个在 824u,门槛外) |
| `222620` | **388.1** | viper | 0.82 | 1.00 | 156/740 | 1 |

基线侧对照(`223135` t=405.5 / 414.7):对 0.68 / 0.61 血 shadow_shaman 各一发 arc_lightning,
蓝 177 / 93,R 同样 ready-但买不起 —— **正是本 id 要拦的形状,armed 侧一次都没有。**

**漏口不是总监预判的 `#nAllies >= 3`**(三帧实测友军 1/2/1);逐条排掉 W2 其余分支后
(`FindAoELocation` 的第 7 参按 API 文档是"只算血量低于该值的单位",bolt 伤害 240-269 vs 目标 1100+ 血;
目标没在读条;`nManaPercentage > 0.65` 与"蓝<225"结构上互斥),**只剩 RETREAT 那两条路** ——
而它同时也是门自己的豁免口 ⇒ **"W2 兜底抵消" 与 "本来就该放行" 离线分不开**(见 §5)。
**但 Q/W 劈叉不受这条边界影响**:两种读法下建议都一样。

顺带的代码事实(记录,非本轮成因):`J.GetNearbyHeroes` → `J.IsValidHero` = `IsValidUnit and IsHero()`
—— **幻象过得去**。CK 的 Phantasm(本局 360.4 / 512.4 / 636.1 各一次)会免费把 W2 的
`#nAllies >= 3` 顶过门槛。本轮三帧不是这么漏的(锁 idx 逐个查过,幻象要么过期要么是尸体帧),
但它是一条站着的暴露面,已写进 #47。

**给 (b) 的预警**:4 局里"R 学会 → 第一次开大"的时延 armed 282s / 73s vs baseline 233s / 65s,
**没有变好**;Zeus 无一次 `died_with_ult_ready`。两者都是 n=2v2 的观察、不构成结论,
但足以说明**这一波的 gpm 看不见 `zusult`** —— 按 §A0 用行为检测器判,口径直接用
`tools/batch_test/behavioral/zusult_gate.py`。

## 4. `tpdead`:本轮**未取证**

它挂在 `tpcommit` 之下、总监 §I.1 自己估算 ≈0.03 次/局(整波最多 ~7 次)。
现存 12 个 `.dem` 里期望命中 <0.5 次,**逐帧找它的性价比不如把 Zeus 那 4 局做透**。
留给下一轮(波次跑完后 `.dem` 更多时),或按 §I.1.2 随后续波次继续作两臂常量攒样本。

## 5. 诚实边界 / 新踩的坑

1. **`J.IsRetreating` 结构上不可观测** —— bot mode 是脚本侧概念,**不在 .dem 的实体流里**
   (同 vision 那条,不是 dumper 缺口,**不要给 harness 开 issue**)。凡是带 RETREAT 豁免的门
   (`zusult` / `capmono` / `lanesurv` / `l1xpsoak` …)在最关键的那些帧上都有这个歧义。
   可行的替代是**运动学代理**(朝己方泉水的位移 + 无输出),但那是新检测器,不是本轮的活。
2. **1Hz 滞后会把"健康目标"判反**:首版把 `222052` t=563.1 记成域内漏帧,逐帧一看目标当时是
   0.36 血、Zeus 自己 1% 血 3 秒后就死了 —— 现在的口径要求目标**前后两个快照都 ≥0.60** 才算健康。
3. **同名多实体**(上一轮记过的幻象坑)在本轮再次咬人:`222052` 一帧里 4 个 chaos_knight,
   不锁 idx 会把"友军数"算成 4。已在 `zusult_gate.py` 里按 idx 去重。
4. **本波 `.dem` 只出 slot1**:16 槽跑满,但只有 slot1 的录像上传 S3 ⇒ 逐帧可查的局数
   ≈ 局数的 1/16。这是老现象(前几轮记过),不是本轮新增,但它是"深查 6 局"下限
   在英雄专属 id 上经常够不到的真实原因 —— 与 #46 是同一个问题的两个面(**能看的局少 ×
   能看的英雄少**)。

## 6. 已检查清单(累计)

- 本轮新增:`spot_20260819_221108/221112/221117/221122_1_829202ac…` 四个 run 的
  **224 局 mirror 有效局全部宽扫**;`221117` 的 `20260819_222052 / 222620 / 223135 / 223633_slot1`
  四局**逐帧深查**(zusult 维度)。暖场局(每 run 首局,stamp `829202a`)已确认作废。
- 本波剩余局(触发时仍在跑,预计 ~270 局)留给下一轮;但按 §2 的结论,
  **继续加局对 `axebuyblink` 无帮助**(瓶颈是阵容不含 Axe,不是样本量)。

## 7. 开的 issue

- **#46 [harness]** —— 一个种子=一套阵容,Axe 0/224,附 `seed_draft.py`(selftest 40/40 PASS)+ 选种建议。
- **#47 [hero]** —— `zusult` (a) 首检:Q 压住、W 漏,三帧证据 + 建议钉帧 `222052` t=540.9 + (b) 判读约束。

## 8. 提交的工具

- `tools/batch_test/soak/seed_draft.py` —— 离线阵容复刻 + 选种(`--find` / `--rates` / `--selftest`)。
- `tools/batch_test/behavioral/zusult_gate.py` —— `zusult` 域内 Q/W 施法率扫描器
  (锁 idx 防幻象、赛后冻结尾巴截断、目标健康度要求前后帧双确认、RETREAT 代理)。

## 9. 验证(铁律第 6 条)

本会话**未改动任何 Lua**(改动:`tools/batch_test/` 两个 Python 工具 + `iterations/` 下的报告/章程)。
本容器未安装 `luacheck` / `lua5.1`(与批测台 22:12Z 报告一致),两者未运行;按立法目的本轮无适用对象。
两个 Python 工具均在本轮真实数据上跑通(`--selftest` PASS;`zusult_gate.py` 输出即 §3 的表)。

## 10. Token 用量

```
TOKENS total_in=13,817,203 out=112,455 turns=105
```

(到统计时刻为止;统计后的收尾回合 —— 更新章程、commit、push —— 不计入。)
