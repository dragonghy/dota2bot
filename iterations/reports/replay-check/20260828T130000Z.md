# 录像检查组 2026-08-28T13:00Z — W21 首检:载体门被采纳了一半,而被滤掉的那粒种子同时扛着另外两笔欠账

**一句话**:W21 的载体门**采纳了 GH #276 的 `odaoe` 那一半**(`obsidian_destroyer` 进了 terms,
`verdict=ABSENT` 时真的换掉了种子 999→1138),**没采纳 `aimguard` 那一半** ——
`spirit_breaker` 仍不在 terms 里,于是 **`aimguard` 连续第二波 armed 进 0 载体的波次**
(**74/74 局实体流零 `spirit_breaker`**,四粒种子离线 draft 同样为零)。
本轮另一头是正面收获:**`odaoe` 的 (a) 拿到第三粒种子的复算,15.6%,与前两粒同号**,
两条逐帧承重(命中点间距 973 u / 809 u,任何以敌人为心的 500 圆都罩不住)。
顺手把判别子自己一处混淆拆开(`neither` 其实装着两种相反的东西,W21 是 **5 range / 0 shape**)。
并且**上一轮欠下的那笔预登记有了明确的解锁物**:`seed 975` 是搜索区间 975..1399 里
**唯一同时**带 `spirit_breaker` 且把 `obsidian_destroyer` 放 **dire** 的种子 —— 它没被选中,
是因为被 `--find axe` 滤掉了,而 `axe` 不是任何 armed hero-scoped id 的必需载体。

零 EC2 支出,`bots/` / `game/` **0 改动**。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` — **worst exit 3(有发现,非未核验)**:

| 腿 | 结果 |
|---|---|
| push gate | 已上膛(`core.hooksPath=.githooks`) |
| unlanded work | OK(shallow 窗口内) |
| stable 锚点 | `stable-v1` `6db5921c5945` / `stable-v2` `136a332fdea4`,EXISTS/PINNED/SHIPPED 全 **ok** |
| cadence | **1 处洞:`strategy` 08-28T01:24Z→10:35Z,9.2h(非本组)** |
| 未裁队列 | RIDESHARE none / OTHER none;total open 37 |
| trunk python | **48 passed / 0 failed / 0 uncertifiable** |
| 快 Lua 检测器 | **17** 个 tagged 文件 0 失败(FAST SUBSET,非全量) |
| 退出码来源(#267 4b) | legs=7;FINDINGS(exit 3) = **cadence**;UNCERTIFIABLE(exit 2) = **none** |

⭐ **本组自己的 6.1h cadence 洞消失了**(连续第八轮登记,本轮工具不再报它)——
上一份报告 09:52Z → 本份 13:00Z = **3.1h,无洞**。

## 1. 吞吐(章程固定节)

| 层 | 数字 |
|---|---|
| **宽扫 A(stamp 普查)** | **129/129 = 100%**(W21 四个 run **截至 12:56Z 已落盘**的全部 `analysis.json`) |
| **宽扫 B(行为 `sweep_run.sh`)** | **4/4 个 run = 74 局**(19 / 18 / 18 / 19),**四个 run 全部 `unparseable=0`、`exit_code=0`** |
| **深查(逐帧)** | **7 局**(章程下限 6):`add5f8/20260828_121911_slot11`、`123253_slot4`(两局共 5 次施法逐帧建表)+ `121921_slot12`、`123226_slot12`、`123236_slot5`、`123237_slot10`、`123256_slot3`(5 次 `neither` 施法逐帧判因) |
| 额外(实体层普查) | **74 局**逐 snapshot 建实体名集合(§2 的载体判定,**不用 draft 名单证明 draft 缺失**) |

**三个分母不要混**:stamp 结论覆盖 **129/129**;行为/实体结论覆盖 **74 局带录像正式局**;
逐帧结论落在 **7 局 + 10 个具体施法时刻**。

⚠️ **本轮是首检,不是终检**:W21 发波 12:16:34Z,本轮读的是 **26–45 分钟**时的中途落盘,
波次仍在飞。**106 个 stamped 局全部 `:radiant`**(见 §3),按批测台 §4 的 ~35–40 min 换腿点,
**dire 腿此刻尚未开始**,不是「没有 dire 腿」。终检数字由下一轮复检给出。

15 个通用检测器的四张触发计数表(`sweep_summary.md`)**本轮只用于选点,不进结论**。

## 2. ⭐⭐⭐ `aimguard`:连续第二波 SILENT(域缺失),而门这次是**被改了一半**

### 2.1 先逐帧(实体流)

四个 run 的全部 timeline 逐 snapshot 读 `snapshots[].hero`:

```
sweep_add5f8 seed=1138: games=19  distinct_names=10  spirit_breaker=0
    axe crystal_maiden lion obsidian_destroyer skeleton_king slardar storm_spirit sven vengeful_spirit witch_doctor
sweep_352458 seed= 983: games=18  distinct_names=10  spirit_breaker=0
    axe bristleback centaur jakiro lich nevermore shadow_shaman skeleton_king storm_spirit witch_doctor
sweep_460956 seed= 986: games=18  distinct_names=10  spirit_breaker=0
    axe bristleback centaur dragon_knight jakiro lion ogre_magi pudge queen_of_pain sven
sweep_11c470 seed= 995: games=19  distinct_names=10  spirit_breaker=0
TOTAL games=74   PROBE npc_dota_hero_spirit_breaker -> 0/74
```

四个 run 的实体名与各自离线 draft **逐个相同(10/10 × 4)** ⇒ 该代理量在本语料上可信,
据此把结论外推到全部 106 个 stamped 局。

### 2.2 再聚合:门这次问了什么

批测台 12:16Z §5.2 的 `terms=5` = `crystal_maiden` / `zuus` / `lion` / **`obsidian_destroyer`** / `skeleton_king`。

- ✅ **GH #276 的 `odaoe` 那一半被采纳**:`obsidian_destroyer` 进了 terms,而且 `verdict=ABSENT` 时
  **真的改掉了发波参数**(983/986/995/**999 → 1138**)。这是载体门第二次实地改参。
- ❌ **`aimguard` 那一半没有**:`spirit_breaker` 仍不在 terms 里。

⇒ **`aimguard` 核验状态维持 `SILENT`(域缺失,不是「门未为真」),连续第二波。**
本波关于 `aimguard` 的任何差分都是**其余 42 个 id 的读数**,不得记到它名下(§BW.3 归属纪律同型)。

### 2.3 ⭐ 新的失败面:选种的 `--find axe` 滤掉了唯一能解锁它的那粒种子

批测台选种是 `seed_draft.py --find axe --from 975 --count 4`。**同一个区间 975..1399**:

| 性质 | 粒数 |
|---|---|
| 带 `spirit_breaker`(`aimguard` 载体) | **58** |
| `obsidian_destroyer` 在 **dire**(`odaoe` 侧别反证,§4.3) | **38** |
| **两者同时** | **1 粒 —— `seed 975`** |

```
seed 975
  radiant  pos1=slardar, pos2=zuus, pos3=dragon_knight, pos4=spirit_breaker, pos5=vengefulspirit
  dire     pos1=sniper,  pos2=obsidian_destroyer, pos3=skeleton_king, pos4=lion, pos5=jakiro

$ seed_draft.py --assert-carrier spirit_breaker,obsidian_destroyer,lion,skeleton_king,zuus 975 ; echo $?
CARRIER_GATE terms=5 seeds=1 exit=0        # 五项全 FULL,单粒种子扛下
```

**975 就是该区间的第一粒**,没被选中是因为它**没有 axe**。而 `axe` 不是任何 armed hero-scoped id
的必需载体(§2.4)⇒ **一个非必需的过滤条件挡住了唯一同时解锁两件事的种子**。已写进 #276 追评。

### 2.4 两处推导陷阱 + 一处对本组上一轮措辞的收紧

1. **只 grep 门站点会漏 `aimguard`**(门 `jmz_func.lua:3993`,唯一消费点 `hero_spirit_breaker.lua:297`)。
2. ⚠️ **按 id 名的形态猜载体会把 `bbfight`/`bbshort` 猜成 bristleback —— 它们是 buyback,不是 Bristleback。**
   门在 `jmz_func.lua:10782`/`:10821`(`J.BuybackFightRespawnFloor`/`J.BuybackShortRespawnFloor`),
   消费点 `ability_item_usage_generic.lua:584`/`:592`,**对全部英雄生效、无载体**。
   本波 983/986/995 三粒都带 bristleback ⇒ 按名字推的实现会**报出一个满足了的假 term**。
3. ⭐ **收紧上一轮的措辞**:上一轮把 `axe`/`skeleton_king` 两个 term 写成「白问」。
   **准确的说法是「非必需」而不是「无关」** —— `abilanc` 的门在 `jmz_func.lua:1850`(`J.GetMostHpUnit`),
   消费点**确实包含** `hero_axe.lua` 与 `hero_skeleton_king.lua`(另有 ogre_magi / doom_bringer /
   templar_assassin / sven / queenofpain / phantom_assassin / mirana / luna / huskar / dazzle / bounty_hunter);
   但它**同时**有一个 generic 消费点(`ability_item_usage_generic.lua`)⇒ 任何阵容都能进它的域,
   故这两个 term **不构成 `abilanc` 的前提**,只是不必要。结论不变(terms 该从 arm 串推),理由改写。

## 3. arm 串与两腿:三方逐字节一致,**本轮单层**

129 份 `analysis.json` 全量反解(判别子是 `script_version` 字段本身):

```
warmup(裸 commit sha 4b2ee334): 23
stamped(mirror:<43id>:s<seed>:<side>): 106
bare arm string: 381 bytes  sha1 0a601389  43 ids  ×106 局逐字节相同
```

`sha1(test_set.md 第 2 行) = 0a601389`,**与 106 局的 stamp、与批测台 12:16Z 报告(43 id / 381 字节)三方逐字节相同**。

| run | seed | radiant-armed | dire-armed |
|---|---|---|---|
| `352458` | 983 | 26 | 0 |
| `460956` | 986 | 26 | 0 |
| `11c470` | 995 | 27 | 0 |
| `add5f8` | 1138 | 27 | 0 |
| | | **106** | **0** |

**全 `:radiant`,且这不是终局计数**(波次在飞,四台 12:16Z 起,本轮读数在 +26..+45 min)。
按 GH #257/#266 的 `SINGLE-LAYER`:**本轮没有任何一条结论是 armed−baseline 差分**;
铁律 4(i) 要求的 ab/ba 两层分层读数**本轮结构上不可得**,不是被跳过。

⇒ **GH #265 的「10..11 带四量同形」预登记本轮仍不执行**,阻塞原因**两条同时成立**:
(i) 本轮无 ba 层;(ii) **GH #272 仍未修**(`campfarm_target.py` 仍按 raw `level` 分带;
`git log` 该文件最新提交仍是 08-27T06:21Z 的 W16 那笔)。**连续第三轮欠着。**

## 4. ⭐⭐ `odaoe`:第三粒种子复算同号(15.6%),两条逐帧承重

### 4.1 读数

```
== W21_s1138 (add5f8 19 局全 :radiant): 32 multi-hero eclipse casts
   shipped-explainable   22  (68.8%)
   OFF-CENTRE ONLY        5  (15.6%)
   neither(range)         5  (15.6%)
   neither(shape)         0  (0.0%)
```

| 波次 | 种子 | armed 腿 off-centre-only | baseline 腿 |
|---|---|---|---|
| W20 | 947 | 7/34 = **20.6%** | 0/6 |
| W20 | 974 | 3/35 = **8.6%** | 0/17 |
| **W21** | **1138** | **5/32 = 15.6%** | **本轮无 baseline 腿(单层)** |

三粒 armed 腿都非零、落在 8.6–20.6%。按铁律 4(ii) **只报计数与占比,不报中位数**;`n=3` 不解释粒间差别。
**判别子来自源码不是阈值**:出厂 exit(`hero_obsidian_destroyer.lua:479`)`return ..., enemyHero:GetLocation()`
⇒ 500 圆**永远以某个敌人为心**;armed 分支(`:575`)候选集含**每一对的中点**。

### 4.2 逐帧承重(同一局 `add5f8/20260828_121911_slot11`,读的是首发 `DAMAGE` **之前**那一帧)

**t=331.7,OD lvl 7 @ (5890,−3359)**

```
HIT lion (5808,−2875) d_OD=491      HIT axe (5920,−3842) d_OD=484     命中点间距 = 973 u
枚举每个活敌当圆心(出厂路径只会返回 enemyHero:GetLocation()):
   lion→需半径 973   axe→需半径 973   storm_spirit d_OD=6931   skeleton_king 13303   witch_doctor 14513
中点候选 (5864,−3358):离 OD 26 u,需半径 487 < 500 ✅
```

**500 半径的圆要罩住间距 973 的两个人,圆心必须落在连线中段 —— 而那段上没有人站。**

**t=1218.4,OD lvl 17 @ (−3265,−2656)**:命中 skeleton_king(664 u)+ axe(152 u),**间距 809 u**;
任一敌人当圆心需 809,中点只需 405(离 OD 262 u)。未命中的 storm_spirit(870)/ witch_doctor(938)/
lion(1062)当圆心分别需 1018/1072/1206,**也全部排除**。
另有 `add5f8/20260828_123253_slot4` 三次(t=503.7 / 647.6 / 917.6,间距 746 / 654 / 833 u),同形。

判别子会**试遍每个活敌**(蓝量 ≥ OD 的敌人吃 0 伤、不发 `DAMAGE` 事件)⇒ **只会少报**。

**核验状态:`odaoe` = WORKING**(三粒种子;W20 两粒双层 + W21 一粒单层 armed 腿)。本组不裁 promote。

### 4.3 ⚠️ 上一轮那条「侧别天然受控」的性质:**是每粒种子的偶然,不是抽签的性质**

上一轮记的是「镜像抽签把某英雄只放一侧 ⇒ 该英雄专属 id 的对比侧别纯净」,并登记「从未被反向证过」。
本轮把它量了:**2000 粒种子的离线 draft 普查,`obsidian_destroyer` 出现 326 次,radiant 159 / dire 167
= radiant 占 48.8%**;全池最偏的英雄也只有 `silencer` 55.1% / `warlock` 46.4%。
⇒ **抽签本身无侧别偏置**,三粒带 OD 的种子(947 / 974 / 1138)全落 radiant 是**巧合**(p ≈ 0.125),
**不是结构**。那条性质因此**不能被依赖**:下一粒带 OD 的种子有约一半概率把它放 dire。
反向证据仍然缺,而**解锁物就是 §2.3 的 `seed 975`**(OD 在 dire pos2)。

### 4.4 ⭐ 顺手拆开判别子自己的一处混淆(本组自己上一轮落地的工具)

`od_eclipse_offcentre.py` 原来把「出厂圆罩不住」且「中点圆也罩不住」一律记 `neither`。
W21 的 5 例**逐帧一看,5/5 都是射程腿失败、形状腿通过**:命中集间距 119–731 u(500 圆本来罩得住),
但任何罩得住它的圆心离 OD 的**上一帧采样位置** 851–1426 u,超出 `700+60`。

| 局 | t | lvl | 间距 | 最优敌心半径 | 中点半径 | 中点离 OD |
|---|---|---|---|---|---|---|
| `121921_slot12` | 728.4 | 14 | 154 | 154 | 77 | **851** |
| `123226_slot12` | 1419.1 | 19 | 119 | 119 | 60 | **1094** |
| `123236_slot5` | 1304.4 | 21 | 242 | 242 | 121 | **1426** |
| `123237_slot10` | 953.4 | 14 | 503 | 503 | 252 | **1228** |
| `123256_slot3` | 1139.1 | 15 | 731 | 731 | 365 | **923** |

那是 **1 Hz 采样的重建误差**(首发 `DAMAGE` 不是施法帧,OD 以 ~300 u/s 合拢),
**不是一次出厂路径做不出的施法**;真正值得逐帧的是另一半(**没有任何 500 圆能罩住命中集**)。
已拆成 `neither(range)` / `neither(shape)`,W21 = **5 / 0**。
**拆分只改分类打印,不动 `shipped-explainable` 与 `OFF-CENTRE ONLY` 两桶的判据** ⇒
§4.1 表里 W20 两粒的读数不受影响(那两粒的 timeline 已随上一会话 scratchpad 释放,**未回算**,登记)。

## 5. 诚实边界

- **本轮全部读数在单层语料上**(106/106 `:radiant`),**没有一条是 armed−baseline 差分**。
  `odaoe` 的 (a) 之所以单层可判,是因为判别子是**源码派生的结构性质**(出厂圆心必是某敌人位置),
  不是阈值 —— 这一点与 `campfarm` 那种需要两腿的读数**不同型**,别混用。
- **armed 腿 = 43 id 同时开** ⇒ 任何聚合差值都是捆绑效应。本轮不报任何聚合差值。
- **首检不是终检**:换腿点按批测台 §4 是 ~35–40 min,本轮读数止于 +45 min。
- **`neither` 拆分未回算 W20**(timeline 已释放)。W20 的两粒 off-centre 占比不受影响,
  但 W20 报告里的 `neither` 计数**没有 range/shape 归因**,若要那个归因需重扫。
- 本轮 §2.3 / §4.3 的两个普查用的是**已入库的 `seed_draft.py`**(离线复现,与实测 40/40 对拍过);
  逐帧表直接读 `timeline.json`,**没有新写未入库的脚本**。

## 6. 核验状态累计(本轮触及的 id)

| id | 状态 | 依据 |
|---|---|---|
| `odaoe` | **WORKING** | 三粒种子(947/974/1138),armed 腿 off-centre-only 20.6% / 8.6% / **15.6%**,baseline 腿 0/6 与 0/17;两条逐帧承重(间距 973 u / 809 u) |
| `aimguard` | **SILENT(域缺失)** | **连续第二波**;W21 实体流 **0/74 局**,四粒种子离线 draft 零 `spirit_breaker` |
| `campfarm` | 维持 NOT WORKING(域内无可测收缩) | 本轮无新读数(单层 + #272 未修) |

## 7. 交棒

1. **⭐⭐⭐ 给批测台(GH #276 追评已发)**:`spirit_breaker` 请进 `--assert-carrier` 的 terms,
   并把 `--find` 与 `--assert-carrier` 用**同一个从 arm 串推出的集合**。
   **具体可执行**:下一波把 **`seed 975`** 放进种子集 —— 它**一粒同时**给出
   (a) `aimguard` 有史以来第一个载体,(b) `odaoe` 的**侧别反证**(OD 在 dire),
   外加 `lion`/`skeleton_king`/`zuus` 三个 term,`CARRIER_GATE exit=0`。
2. **⭐⭐ 给总监**:`aimguard` 的 (a) **连续第二波结构上买不到**;
   GH #272(raw `level` 分带)**连续第三轮阻塞** GH #265 的预登记,请排在 #270 之前。
3. **⭐ 下一轮本组第一件事:W21 复检** —— 先跑 stamp 普查(秒级)确认 dire 腿是否落地;
   **两腿都在就把 `odaoe` 的 baseline 腿计数补齐**(seed 1138 的 OD 在 radiant ⇒ dire-armed 局里它是 baseline),
   这是**本轮唯一能被证伪的预登记**:若 baseline 腿也出现 off-centre-only,§4 的结构判别子本身要重审。
4. ⭐ GH #265 的两条 fixture(建议钉出厂腿 `db92df/004757_slot1 t=786.3 venomancer`,**第二轮**);
   episode 口径入库 + 棘轮(**第六轮**);#236 裁下后 `mv` 落地 #159 的 fixture(**第十四轮**);
   GH #262 的 fixture(**第三轮**);GH #259 的 fixture(**第五轮**);GH #250 的出厂帧 fixture(**第六轮**);
   `pullthink` 英雄分层表(**第六轮**);GH #220 的 fixture(**第十四轮**);每 id 篇数独立小脚本(**第十轮**);
   `>=12` 反向哨 +0.839 ep/局(**第二轮登记**);`camp_prelit` 列(**第十八轮**);
   占用度量按 idx 去重(**第十七轮**);`tpdefend_events` 结果侧列(**第二十三轮**);
   幻象余量做进 `sweep_run.sh`(**第二十轮**);`entities.py` 推广(**第二十一轮**);
   ab/ba 回灌 `fieldbuy_silence.py`/`stayfield2_margin.py`(**连续第三十四轮登记**);
   打野反证 fixture(**四十轮**);`hero-1` 的 153 局 WK 语料(**48 轮**);
   `l5combo` 的 (a)(**第六十四轮**);`make_fixture.py` 钉 `062551 t=205.5 jakiro`(#45,**第五十一轮**)。

**球在总监**:#276、#272、#270、#265、#264、#263、#262、#261、#259、#258、#257、#253、#250、#249、
#247、#246、#245、#244、#241、#236、#235、#159、#207、#212、#220、`campfarm`;
`bbfight`/`bbshort` 与 `abilanc` 的三条件判定。
**本组不裁 promote/reject、不申请波次、不改 bot Lua、不花 AWS 钱。**

## 8. 验证(铁律 6)

- `bots/` / `game/` **0 改动**(本轮改的是 `tools/batch_test/behavioral/od_eclipse_offcentre.py` 一处分类打印)。
- `bash tools/agent/luacheck_gate.sh` → **exit 0**,`luacheck bots game: 0 warnings`
  (容器里没有 luacheck,脚本自装 `lua-check`)。**未使用 `RULE6_BYPASS`。**
- `bash tests/run_py_tests.sh` → **48 passed / 0 failed / 0 uncertifiable**(与开工自检同值)。
- 本轮未改任何 Lua ⇒ **不声称跑绿过 Lua 全量**(GH #124)。
- **AWS**:S3 **只读**(129 `analysis.json` + 97 `.dem` 流式下载),
  **未启动/未终止任何实例**,**未调用 Cost Explorer**,**零支出**。

## 9. Token 用量(铁律 8)

```
TOKENS total_in=9,147,756 out=50,387 turns=73
```

(到统计时刻为止;统计后的收尾回合不计入。)
