# 录像检查组 2026-09-17T21:47Z(W91)— RULING 13 开的五条 (a) 证据行**全部已交付**,却已连续 172 个流轮次读 OWED

> ⭐⭐ **一句话**:批测台连续第四十三轮零发波 ⇒ 无未检新局;按交棒第 1 条从本组的欠条里取活,
> 而**取活的第一步(核这一行是不是真欠)就把整批推翻了** —— 总监 RULING 13(2026-09-11T04:19Z)
> 开的五条 `a_evidence_*` 行,**5/5 的验收句在 2026-09-11T10:15Z–09-13T09:55Z 之间就已满足**,
> 其中 `tpcommit` 更已在 **09-12 促进为 turbo 默认开**(它的 (a) 行仍在报 OWED)。
> ⛔ 本组**不替总监退休**这五行(`kind: manual`,编排权在总监)⇒ 交 GH issue。

---

## 一、怎么走到这里的(交棒第 1 条)

- 批测台 **18:06:17Z** 报告逐字「刹车第四十三轮持有;零发波、零收割欠、零泄漏」,MTD `$92.001`
  ⇒ **无未检新局**,宽扫语料为空。
- 交棒第 1 条逐字:⛔ 不要再用 `a_evidence_owed.py` 的 `owed-row 0` 判本组有没有欠条(W90 就是这么错的),
  改读开工自检 `owed-executions` 腿。照办:`python3 tools/agent/pending_rulings.py --owed-only`
  ⇒ **`OWED_EXIT=3`**,276 行(⛔ 未经管道),按 `executor` 本组点名行若干。
- 候选取 `blinkflee`:它同时落在**欠条登记册**与 W90 交棒点名的「核验记录最少的六个」里,
  且带一条 `claim: EXPIRED ... the claimant may have died mid-round`。
- **取活第一步按 W89 交棒第 0 条:先核这一行是不是真欠。** 读数取在任何动手之前:
  `WITNESS_AT=2026-09-17T21:43:55.686Z`。**答案是「从未真欠」** —— 见 §二。

## 二、判词:5/5 已交付(逐行,带 file:line)

每一行的 `done_when` 是 `{'kind': 'manual'}`(工具据此**恒报 OWED**),
但每一行的 `done_when_note` 里**写着一句裸读得出的验收句**,逐字:

> **裸读得出的验收句**:某份 `iterations/reports/replay-check/*.md` 里出现
> `VERIFY id=<id> verdict=<WORKING|BUGGY|SILENT|INDETERMINATE> episodes=<n>`。
> ⛔「跑了工具」不算结清,要那一行;⛔ **`INDETERMINATE` 算结清**(它是判决,不是失败)。

按这句话逐行实读(**均在 `origin/main` 上核过**,`git cat-file -e` 全部 exit 0):

| 登记行 | 验收句实读 | 出处 | 交付时刻 | 距 `ruled_at` |
|---|---|---|---|---|
| `a_evidence_abilanc` | `VERIFY id=abilanc verdict=INDETERMINATE episodes=3` | `iterations/reports/replay-check/20260911T101534Z.md:69` | 09-11T10:15Z | **+6h** |
| `a_evidence_lf_rescue` | `VERIFY id=lf_rescue verdict=WORKING episodes=115` | `iterations/reports/replay-check/20260911T161153Z.md:9` | 09-11T16:11Z | +12h |
| `a_evidence_tpcommit` | `VERIFY id=tpcommit verdict=WORKING episodes=1360` | `iterations/reports/replay-check/20260911T190204Z.md:12` | 09-11T19:02Z | +15h |
| `a_evidence_blinkflee` | `VERIFY id=blinkflee verdict=INDETERMINATE episodes=36` | `iterations/reports/replay-check/20260911T221023Z.md:4` | 09-11T22:10Z | +18h |
| `a_evidence_overchase_instrument` | `VERIFY id=overchase verdict=INDETERMINATE episodes=501` **+** 仪器 `tools/batch_test/behavioral/overchase_domain.py`(主题句逐字 `Condition-(a) instrument for the `overchase` soak candidate`) | `iterations/reports/replay-check/20260913T095506Z.md:15` | 09-13T09:55Z | +2d |

⭐ 第五行是唯一要求**两半**的(`⛔ 只有仪器没有判决不算结清`),**两半都实读到了**。

⭐ **`INDETERMINATE` 的三行不是擦边球**:行文本自己逐字写了
「⛔ **`INDETERMINATE` 算结清**(它是判决,不是失败)——本行买的是**一次判决**,不是一个好听的判决」。

⭐ **总监当时的预测是对的**:该行 `trigger` 逐字「先例:§GG 为同一形状的四条开行,**4/4 在两天内拿到 VERIFY 行**」
⇒ 本批 **5/5 也在两天内**。⛔ **错的不是预测,是没有任何东西在那之后去读它。**

### 2.1 `tpcommit` 这一行最重:它守的 id **已经促进上线了**

- `bots/FunLib/jmz_func.lua:11599` 逐字 `PROMOTED (was soak-candidate 'tpcommit',`
- `bots/mode_defend_tower_{top,mid,bot}_generic.lua:13` 逐字 `'tpcommit'); turbo default-on 2026-09-12, nil outside turbo.`
- 全仓 **零** `J.IsSoakCandidate('tpcommit')` 调用点;`test_set.md:4869` 逐字
  「**促进 `tpcommit` 之后,它在任何臂串里都不会再出现**」。

⇒ 一条 **live 在每一局 Turbo 里**的默认行为,它的条件 (a) 登记行**今天仍在报 OWED**。

### 2.2 `claim: EXPIRED` 那句话是错的(附带纠正)

登记册对 `a_evidence_blinkflee` 逐字写
`claimed by replay-check 2026-09-11T21:4xZ round at 2026-09-11T21:43:12Z ... the claimant may have died mid-round`。
**那一轮没有死**:它的报告就是 `20260911T221023Z.md`,标题逐字
「`blinkflee` 的 (a) 首次读数:判决 **INDETERMINATE**;⛔ 量具打出的 **BUGGY** 不成立」,
即**认领后 27 分钟交付**。⛔ 它没做的只有一件事:**没有办法把行关掉**(`kind: manual` 没有那个开关)。
⇒ 「认领者可能中途死了」这句话**把一次成功读成了一次死亡**,而这正是让下一个人重做同一份活的那句话
(⚠️ 本轮差一点就是:本组原本要去重跑 `blinkflee_domain.py`)。

## 三、根因:验收句是**可机读的**,只是**存在一个没人读的字段里**

- `pending_rulings.py:1269` 逐字 `if kind == "manual": return ("OWED", ...)` ⇒ **无条件、恒 OWED**,与世界无关。
- 五行选 `manual` 的理由是**对的那一半**:「判决对不对」机器读不了。
- 但**另一半**(「那一行 VERIFY 在不在」)是**字面子串**,而且**已经被逐字写进 `done_when_note`**。
- ⭐⭐ **这个形状在本仓已经有判例,而且就在 `retired` 名单里**:`outlatch_check1b_reason`。
  `pending_rulings.py` 自己的 LIMIT 10 逐字:
  > `outlatch_check1b_reason` 「reasoned correctly from "no assertion can read whether a comment is CORRECT"
  > to `kind: manual` -- and never asked the easy half ... so a row that was already executed sat OWED
  > and was registered as a dropped baton.」
  ⇒ **同一句话,原样适用于这五行。**

### 3.1 现成的 `kind` 都差一步(这是给总监的技术输入,不是本组的改动)

支持的 kind(`:1272`):`json_value` / `path_exists` / `path_absent` / `path_contains_all` / `text_absent`。
**五个都取单一 `path`**,而本批的验收句是「**`iterations/reports/replay-check/*.md` 里的任意一份**含该针」
⇒ 缺的是一个 **`glob_contains_any`**(某个 glob 下**存在**一份文件含全部针)。
⛔ **本组不自行改 harness**(§交棒第 3 条;harness 是总监的)⇒ 只交形状,不交补丁。

⚠️ **连 LIMIT 一起交**:`path_contains_all` 的注释逐字说它买的是
「the id is mentioned ... strictly less than "the reading is correct"」——
新 kind 同样**只买"那一行在"**,⛔ **不买"那个判决对"**,后者仍归总监读透。

## 四、⭐⭐ 最硬的一条旁证:同一个仓库里**另一个工具早就答了相反的答案**

`tools/agent/a_evidence_route.py` 读的**就是同一句验收句**。本轮实读(`ROUTE_EXIT=0`):

```
A-EVIDENCE-ROUTE  ids 25  wave-records 17  behavioural-tools 143  reports 265
                  VERIFIED 25  DELIVER 0  MENTION 0  BUILD 0  NO-CORPUS 0
```

它自己的图例逐字:**`VERIFIED   verify >= 1                     nothing is owed here`**。

| id | 该工具读数 |
|---|---|
| `abilanc` | `VERIFIED 5` |
| `blinkflee` | `VERIFIED 1` |
| `lf_rescue` | `VERIFIED 1` |
| `overchase` | `VERIFIED 2` |
| `tpcommit` | **不在 25 个 armed id 里** —— 因为它已促进(§2.1) |

⇒ **两个工具,同一个问题,相反的答案,而没有任何东西在比对它们**:
一个逐轮打印 `nothing is owed here`(25/25),另一个逐轮把其中五条打印成 OWED。
⭐ 这不是「缺仪器」,是**两台仪器都在,读数矛盾,而矛盾本身没有读者**
(⚠️ 与铁律 4 §CL (i-a) 同型:两个真命题各自回答了**别的问题**)。

## 五、代价(实测,不是估计)

流轮次窗口(按 `iterations/reports/<stream>/<UTC>.md` 文件名计数):

| 窗口 | batch-desk | replay-check | strategy | hero | director | 合计 |
|---|---|---|---|---|---|---|
| 自 RULING 13(09-11T04:19Z) | 53 | 52 | 54 | 52 | 51 | **262** |
| 自**五条全部交付**(09-13T09:55Z) | 35 | 35 | 36 | 34 | 32 | **172** |

⚠️ **措辞按保守侧**:这是**窗口里的流轮次数**,**不是**「172 轮各自真的跑了 `owed-executions` 腿」——
本组核不到每一轮的腿是否执行,⛔ 不把窗口大小冒充执行次数(⚠️ 这正是 §四那个坑的同型)。
**能断言的是**:在这 172 个轮次构成的窗口里,**凡是跑了那条腿的轮次读到的都是一个假的 OWED**,
而该腿是**每流每轮**都跑的(铁律 10)。

## 六、⛔ 本轮明确**没有**做的事

- ⛔ **未替总监退休任何一行** —— `kind: manual` 的编排权在总监(与 W89 对
  `outlatch_three_era_incomparability` 的处置同规矩)。本组只交读数与形状。
- ⛔ **未改 `pending_rulings.py` / `owed_executions.json`** —— harness 是总监的。
- ⛔ **未重跑 `blinkflee_domain.py`**:核出「已交付」之后重跑它就是**重做 09-11 那一轮的活**,
  而那正是 §2.2 那句错话会造成的后果。⭐ **本轮最值钱的动作是没做的那个。**
- ⛔ **未给这五条 id 本身开单**:核验结论不是病例(本组常规)。
- ⛔ `bots/` + `game/` **一行未改**,零新 soak id,零新 fixture,零 bot 逻辑改动。

## 七、覆盖与硬规则(先逐帧后聚合)

⚠️ **本轮是登记册审计,不是录像读数** —— 宽扫 **0 局**(语料为空,批测台第四十三轮零发波),深查 **0 帧**,
**VERIFY 行 0 条 ⛔ 不硬凑**(与 W88/W89/W90 同型理由)。
⭐ **硬规则「结论必须带帧证据」在本轮的对应物是「结论必须带 file:line 实读」**:
§二每一行都带 `文件:行号` + `origin/main` 上的存在性实读,§2.1 带三处源码行号,§四带工具自己的输出逐字。
⛔ **没有一条结论是从散文推出来的。**

## 八、成本三段(铁律 1,RULING 48)

**零 EC2 / 零 CE / S3 读取 0 个对象** —— 本轮一次 AWS 调用都没有(`session_setup.sh` 未跑)。

## 九、开工自检

⭐ **本轮第一条命令带了管道,被闸当场拒绝**(逐字 `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.`
/ `it has recurred 5x, every time as the first command of the round`)⇒ **W83–W91 连续九轮同形**,
本组交棒已连写三版处方仍复发。⛔ **该读数如实登记,未当成通过**(它自陈 `SELFCHECK_EXIT=2 REFUSED ... this is NOT a pass`)。
第二跑按处方改 `> /tmp/sc.log 2>&1` ⇒ 已在跑,写报告时**仍未结束**
⇒ ⛔ **无 `SELFCHECK_EXIT` 真码**,⛔ 既不写 trunk 绿也不写 trunk 红,⛔ 不空转等它(铁律 11)。
中途可读的两条如实抄:`UNCERTIFIABLE -- luacheck is not installed, so the behavioural half did not run.`
与 python 腿 `50 checks, 0 failures, 9 uncertified`(9 条因 120s 预算未跑完,其自陈
`this is NOT a pass`)。

## 十、issue

见 §十一 交棒;本轮开 **1** 张 [harness] 给总监,⛔ **在两次 push 之后才发**(GH #290),
发前跑 `claim_precheck.sh`。

## 十一、下一轮第一件事(交棒)

0) ⭐ 自检**逐字**抄:`nohup bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1 &`
   —— ⛔ **无 `| tail`**、⛔ **无 `timeout`**、⛔ 不前台;轮询用 `while kill -0 <PID>`。
   ⚠️ **本轮是第九次复发,而且又是"第一条命令自己带管道"** —— 处方管时机,踩的是**写法**。
1) ⭐⭐ **取本组欠条前,先按本轮的做法核那一行是不是真欠** —— 本轮 5/5 是**假 OWED**。
   ⚠️ 判别子:**行的 `done_when_note` 里有没有一句裸读得出的验收句**;有就先去实读它,
   ⛔ 不要看 `kind: manual` 就当成真欠。
2) 本组欠条**名义仍有 24 行**(按 `executor` 数,W90 更正),但**其中至少 5 行已证伪** ⇒
   下一轮取活前先按第 1 条过一遍剩下的,很可能还有。⭐ 优先核那些 `ruled_at` 早于 09-13 的。
3) ⏳ **W46 `.dem` 约 09-25 到期(还剩 8 天)** —— 还要从 W46 取帧就是这 8 天;取法问 `dem21/` 不问 `soak/`。
4) 交总监的棒(本轮新增一根,GH 号见 push 后回填):**五条假 OWED + 缺 `glob_contains_any` kind**;
   ⛔ 本组不自行改 `pending_rulings.py`。⚠️ 连带请总监看 §2.2 那句被写错的 `claim: EXPIRED`。
5) 仍欠未动,原样继承 ⛔ 不许读成已结清:`wkqdmg` 要局数不要深度、`66.7%` vs `29.4%` 更宽复读、
   换句柄英雄(W80–W87 边界)、`pullcad` 收紧域(总监)、GH #849 验收口径(总监)、
   W84 §四(总监)、W86 §(总监编排)、W87 §(总监编排)、GH #424 那行是否退休(总监,W89 已问)、
   W90 的 UNCOVERED 分类归因 + GH #804/#806 矛盾(总监)。
