# replay-check 2026-08-30T04:05Z — `pullcad`:**这个 id 的可观测通道不是它住的那条分支**

**一句话**:上一轮章程点名的下一个零记录 id(`pullcad`,有工具无裁决)本轮做完 ——
它的条件 (a) **买不到,而且原因可以被算死**:唯一的可观测量(勾线者对线上对手的右键落点)
**同时被拉线分支和普通对线补刀/换血两种来源写入**,而**逐帧看,armed 腿上 40.0–51.8% 的
「短间隔」右键对,是英雄站在原地一步没动时打出来的** —— 那种间隔**两条腿的节拍都写不出来**
(分支在两次 poke 之间必须每帧发 `Action_MoveToLocation`)。

**VERIFY id=pullcad verdict=INDETERMINATE episodes=5020**

不是 WORKING / BUGGY / SILENT。**域不空**(W27 716 个 armed episode、W25 4304 个),
**gate 在串里**(两波 45/44 id 串逐字核过),**仪器分辨率也够**(战斗日志 0.1s,
比 1.2s 的节拍细 12 倍 —— 这一条正是 `wkqdmg` 输掉的那条,本条不输)。
**堵点是通道分离,不是分辨率、不是样本量、不是域。**

**宽扫 72/72 局**(W27 全部 11 局 + W25 两个 run 61 局,`unparseable 0`),
**深查逐帧 11 局**(W27 全覆盖,逐 poke 对读位移)。零 EC2 支出,S3 只读,
`bots/` / `game/` **0 改动**,gate 未动。

⭐ **本轮最该被下一个人读到的一句**:§4 那个「站着不动挨了 6 秒小兵、血从 0.96 掉到 0.37」
的帧,**同时是本轮结论的支点和上一轮 `fieldcreep` 那条前提检验的第二个独立样本** ——
它出现在 `creeppull` 的域里,而 `creeppull` 是**已 promote 的出厂行为**。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` —— **worst exit 3,8 腿全跑**。

- `exit sources`(工具自报,照抄不加工):`FINDINGS (exit 3) = cadence`,
  **`UNCERTIFIABLE (exit 2) = none`** ⇒ trunk 两侧本轮都被看过。
- trunk:python **53 passed / 0 failed / 0 uncertifiable**;fast Lua **33 个打标检测器 / 0 失败**
  (工具自称 FAST SUBSET,未打标的测试与「要一个进程跑完整套」的部分不在覆盖内)。
- cadence 2 条:`director 08-29T04:08Z → 16:28Z` **12.3h**(连续第五轮同一个洞);
  `replay-check 08-29T00:55Z → 05:30Z` **4.6h**。窗口 24h,阈值 3.5h,5 streams / 521 reports。
- `unlanded_commits` **OK**;`stable-v1`/`stable-v2` **2/2 OK**(EXISTS/PINNED/SHIPPED)。
- 未裁队列请求 RIDESHARE none / OTHER none;`ORPHAN_PROPOSAL` none;过期等待无。

⚠️ **一条给下一轮的操作学费(章程「工具坑」那条,本轮第三次踩)**:
自检第一次是用 `timeout 300 … | tail -50` 起的,**管道把输出全缓冲住**,10 分钟里
文件一直是 0 字节,看起来像挂死;随后用 `pkill -f "routine_selfcheck.sh"` 去收尸,
**那条 pkill 匹配到了发起它自己的 `bash -c`**(exit 144),把我自己的 shell 一起杀了。
章程里 2026-08-25 已经记过 `pgrep -f`/`pkill -f` 这一条,**这轮仍然踩** ⇒
本条追加一句可执行的:**长任务一律 `> 文件` 不接管道,收尸用 job id 不用 `-f` 模式**。

## 1. 为什么是这个工作单元

- **铁律 9**:`OWNER_PRIORITIES.md` 三项(P1 拉野 / P2 低血不回家 / P3 cap 10→25)
  的球分别在协同组、协同组、总监。**本轮没有属于录像组的未完成优先项**,走章程 backlog。
- ⚠️⚠️ **必须先说清楚的一条:本轮做的是第 (2) 条,没有碰第 (0) 条。**
  上一轮章程尾部把 **`hero-20`** 列为「下一轮第一件事 (0)⭐」——
  总监 01:12Z(commit `262e3257`,`test_set.md §CH`)裁定 `hero-20` APPROVED、
  `director.executor` **点名 replay-check**(归档 .dem 事件流扫描,零 EC2,
  要 WK 重生那一帧 900 半径内的敌方英雄数分布)。**本轮开工时我只读了
  `当前状态` 节的开头和上一份报告的 §9,没读到章程尾部那一行**,于是按
  报告 §9 的编号做了 (2)。**这是我的漏读,不是这一棒的问题。**
  **棒没有掉,但它现在是第二轮悬着**:本轮把它在章程里**升到 (0) 并标注
  「已悬 2 轮」**,§9 原样保留其 `acceptance_amendment`(识别子得 0 次
  必须记 `METHOD-FAILED`,不得读成「触发罕见 ⇒ t25 定价坐实」)。
- **上一轮章程「下一轮第一件事」第 (2) 条**点名:下一个零记录 id **`pullcad`** 或 `zusstatic`。
  选 `pullcad`:它是 **owner P1(拉野)家族**的成员(源码 `:235` 自标
  `[owner P1 condition (c) 20260823]`),而 `zusstatic` 2026-08-26 已有一份「域可测但
  归因不到它」的裁决在案(GH #207),不是同一意义上的零记录。
- **W27 本轮可读**:上一轮把它挂起的两个理由都消解了 —— 批测台 03:30Z 报告确认
  四台已于 `01:02–01:14Z` 全部自毁并 4/4 收割(`unparseable 0`),
  且本轮 arm 串普查通过(§2)。**它与 W25/W26 仍然不可并池**(45 vs 44 id 串),
  所以下面每一张表都**按波分层**,没有一个数字是跨波池化来的。

## 2. 宽扫与 arm 串普查:72/72 局

| 波次 | run | `games swept` | `unparseable` | armed 串 |
|---|---|---|---|---|
| W27 | `4ca0c1` | 2(跳过 1 暖场) | 0 | 45 id,sha1 `2b581faa` |
| W27 | `40db63` | 3(跳过 1) | 0 | 同上 |
| W27 | `39656f` | 3(跳过 1) | 0 | 同上 |
| W27 | `1f8a55` | 3(跳过 1) | 0 | 同上 |
| W25 | `6df84c` | 31(跳过 6) | 0 | 44 id |
| W25 | `b1386e` | 30(跳过 6) | 0 | 44 id |

- **W27 有帧的局只有 11 局**(批测台按 `analysis.json` 计分的是 **235 局**)——
  这是录制槽决定的,不是我抽的。W26 上一轮已记过同一形状(每 run 仅 2 局有帧)。
- **`pullcad` 在两波的 armed 串里都在**(逐字核,不是按登记推)。W27 的 45 id 串
  在 11/11 局上 **sha1 逐位相同**,没有半路换串。
- W25 本轮只并了**两个 run(61 局)**:另两个 run 的 sweep 在本工作单元收尾时仍在跑,
  **没有半份数据被偷偷算进任何一张表**(每张表都标了口径)。
- 本轮**未跑** `sweep_run.sh` 的 15 个通用检测器(沿用欠账,**连续第四轮**,登记不掩盖)。

## 3. 裁决:`pullcad` 条件 (a) = INDETERMINATE(通道不可分离)

### 3.0 这个 id 到底改了什么(只改了这一个数)

`bots/mode_roam_generic.lua:268-271`,在拉线执行分支里(`bot.roamCreepPull ~= nil`,
因为 `creeppull` **已 promote**,这条分支在镜像局的**两条腿上都是活的**):

```lua
local nBeat = 1.2
if J.IsSoakCandidate('pullcad') then nBeat = 3.0 end
if bot.creepPullAttackTime == nil or (now - bot.creepPullAttackTime) > nBeat then
    bot:Action_AttackUnit(pull.enemy, true)      -- POKE
    bot.creepPullAttackTime = now
elseif (now - bot.creepPullAttackTime) < 0.5 then
    -- 已 promote 的 'pullbeat':一个起手动作内不发任何指令
else
    bot:Action_MoveToLocation(pull.retreat)      -- DRAG
end
```

**它只改两次 poke 的最小间距 1.2s → 3.0s。** 所以 (a) 是一个纯时序问题。

### 3.1 仪器这一关过了(与 `wkqdmg` 的分界)

DAMAGE 事件带**自己的战斗日志时戳,0.1s 粒度**(实测:同一局里 13,639 个相邻事件
时戳差落在 (0, 0.5) 内;`t % 1.0` 有 10 个不同值 ⇒ **不是 1Hz 量化的**)。
1.2s 的节拍里有 12 个刻度,3.0s 里有 30 个。判决阈 `SHORT_MAX = 2.0s`
两边各留 **0.8s = 8 刻度**的余量。
⇒ **`wkqdmg` 那条「阻塞项是采样率」在这条上不成立**,这也是本轮值得做它的理由。

### 3.2 读数:两波、两层,**符号自相矛盾** ⇒ 按铁律 4(i) 不进结论

`gap < 2.0s` 的占比(一个 3.0s 节拍**结构上写不出**这样的间隔):

| 波 | 层 | 腿 | episode | poke | gap | **short<2s** | mean gap |
|---|---|---|---|---|---|---|---|
| W27 | ab | armed | 463 | 501 | 266 | **66.5%** | 2.48 |
| W27 | ab | baseline | 471 | 347 | 184 | 64.1% | 2.57 |
| W27 | ba | armed | 253 | 246 | 137 | **62.0%** | 2.54 |
| W27 | ba | baseline | 246 | 212 | 120 | 71.7% | 2.31 |
| W25 | ab | armed | 2949 | 2101 | 1076 | **71.9%** | 2.10 |
| W25 | ab | baseline | 2513 | 1804 | 850 | 66.8% | 2.44 |
| W25 | ba | armed | 1355 | 1225 | 614 | **64.5%** | 2.62 |
| W25 | ba | baseline | 1476 | 985 | 468 | 72.9% | 2.00 |

**两波各自的两层反号,而且反的是同一个号**:`ab` 层 armed 高于 baseline(+2.4pp / +5.1pp),
`ba` 层 armed 低于 baseline(−9.7pp / −8.4pp)。铁律 4(i):**反号 = 噪声,不写进结论。**
更要紧的是绝对值:**armed 腿光 W25 就带了 774+396 = 1170 个 sub-2s 间隔**,
**3.0s 的节拍一个都写不出来。**

### 3.3 ⭐⭐ 判别器:两次 poke 之间,这个 bot **走没走路**

分支在这件事上无法沉默 —— 两次 poke 之间它**每一帧**都在发
`Action_MoveToLocation(pull.retreat)`(armed 是 3.0s 里的 2.5s,出厂是 1.2s 里的 0.7s)。
所以**一对夹着「原地不动」的 poke,两条腿的节拍都不是它的作者**。
(路径长按 1 Hz 帧求和 ⇒ 一秒内出去又回来的走动会被漏掉 ⇒ `still` 是**非分支人口的下界**,
不会高估。)

| 波 | 层 | 腿 | short<2s | **其中原地不动** | long≥2s | 其中原地不动 |
|---|---|---|---|---|---|---|
| W27 | ab | armed | 177 | **86(48.6%)** | 89 | 10(11.2%) |
| W27 | ab | baseline | 118 | 54(45.8%) | 66 | 7(10.6%) |
| W27 | ba | armed | 85 | **34(40.0%)** | 52 | 6(11.5%) |
| W27 | ba | baseline | 86 | 48(55.8%) | 34 | 2(5.9%) |
| W25 | ab | armed | 776 | **396(51.0%)** | 300 | 35(11.7%) |
| W25 | ab | baseline | 568 | 249(43.8%) | 282 | 15(5.3%) |
| W25 | ba | armed | 396 | **205(51.8%)** | 218 | 21(9.6%) |
| W25 | ba | baseline | 341 | 161(47.2%) | 127 | 9(7.1%) |

**40.0–51.8% 的短间隔可以被直接证伪为「不是分支写的」**,两波两层四格全部复现。
剩下那一半**也证不出是分支写的** —— 走动本身不是分支的专利。
⇒ **可观测通道 ≠ 那条分支。这就是全部结论。**

### 3.4 ⭐ 还有一条不需要通道分离的读法:**杠杆自报的那个量**

源码 `:249-251` 把预期效果登记成一个**占空比**:
「armed 的 drag 占 3.0s 里的 2.5s(83%),出厂占 1.2s 里的 0.7s(58%)」
⇒ 相对提升约 **+43%**。`creeppull_domain` 的 `drag` 字段(未来 3.0s 的**朝自家泉水位移**)
**正好是这个量**,而且它**不需要**分辨哪一帧在分支里。

| 波 | 层 | 腿 | n(pull-certified 帧) | mean | p50 |
|---|---|---|---|---|---|
| W27 | ab | armed | 591 | **96.9** | 98.0 |
| W27 | ab | baseline | 593 | 95.2 | 78.0 |
| W27 | ba | armed | 371 | **118.1** | 87.0 |
| W27 | ba | baseline | 308 | 116.2 | 121.0 |
| W25 | ab | armed | 3081 | **121.9** | 107.0 |
| W25 | ab | baseline | 2226 | 128.7 | 135.0 |
| W25 | ba | armed | 1438 | **134.6** | 147.0 |
| W25 | ba | baseline | 1408 | 127.3 | 112.0 |

**均值差 +1.8% / +1.6% / −5.3% / +5.7%,四格里有一格反号,p50 在两格里反号。**
预期是 **+43%**。⇒ **这条杠杆在本语料上的可观测足迹,被压在 ±6% 以内。**
铁律 4(ii) 要求的占比栏(`>=200u` / `>=400u`)在工具输出里,同样无分离。

⚠️ **这个 null 必须带着它的警告一起走(章程 §4a)**:44 个 id 同波 armed,
所以**它界的是整个 bundle 在这个量上的足迹**;它界得住 `pullcad` 自己,
**当且仅当**没有别的 armed id 在同一个量上反向抵消它。**这一条我不主张已排除。**

## 4. 逐帧(硬规则:先逐帧后聚合)——支点那一帧

`20260830_003408_slot1`(s1828,**ab/armed**,`40db63`),necrolyte(pos=3)→ jakiro,
episode `t=250.5..260.5`,pull-certified(drag+keep+poke),
poke 落点 **250.8 / 252.4 / 254.0 / 255.4 / 257.0**,gap **1.6 1.6 1.4 1.6**。

复现命令(工具在树上,不是本轮的一次性脚本):

```
python3 tools/batch_test/behavioral/creeppull_frames.py \
    <sweep>/spot_20260830_001859_..._40db63 20260830_003408_slot1 necrolyte 248 262 --tgt jakiro
```

逐帧读数(照抄工具输出的关键列):

| t | pos | hp | d(jakiro) | dFount |
|---|---|---|---|---|
| 251.5 | (-5859, 4979) | 0.97 | 240 | +286 |
| 252.5 | (-5847, **5064**) | 0.96 | 126 | +85 |
| 253.5 | (-5847, **5064**) | 0.96 | 92 | **+0** |
| 254.5 | (-5847, **5064**) | 0.90 | 133 | **+0** |
| 255.5 | (-5847, **5064**) | 0.77 | 106 | **+0** |
| 256.5 | (-5847, **5064**) | 0.71 | 304 | **+0** |
| 257.5 | (-5847, **5064**) | 0.52 | 426 | **+0** |
| 258.5 | (-5847, **5064**) | 0.37 | 615 | **+0** |
| 259.5 | (-5920, 4852) | 0.30 | 648 | −217 |

**坐标在 252.5–258.5 逐位不变,整整 6 秒零位移**,同期每一秒都在吃
`DAM npcdotacreepbadguysmelee> v=14/15`(4 只近战小兵的仇恨),
**血从 0.96 掉到 0.37**。四次 poke(252.4/254.0/255.4/257.0)全部落在这段静止里。

⇒ **这不是拉线节拍**。armed 的 3.0s 节拍要求这 6 秒里有 ~5 秒在走路;
出厂的 1.2s 节拍也要求有 ~3.5 秒在走路。**两条腿都写不出「零位移 + 1.5s 右键」。**
这一帧就是 §3.3 那 40–52% 的人格化,也是本轮判 INDETERMINATE 而不是 BUGGY 的理由:
**看不出 gate 有没有到位,因为看到的根本不是那条分支。**

**顺带一条独立观察(不作为本轮结论,也不开 issue)**:同样形状(静止 ≥5s、
每秒被非英雄单位打、t≤360)在 W27 的 11 局里用一次性脚本数到 **10 例**
(armed 7 / baseline 3),但**其中 8 例掉血 < 0.25**,只有本帧(−0.59)与
`20260830_004615_slot1` drowranger t=219.5..225.5(−0.37)算严重。
**这个 10 是一次性脚本的数,不在树上 ⇒ 按 #263 只作指示性读数登记,不进任何裁定。**
它与上一轮 `fieldcreep` 的前提检验同族(bot 站在小兵火力里),**留给下一轮做成检测器**。

## 5. 交棒(不许掉)

**GH #326 [harness]** —— `pullcad`(以及任何「节拍/时序型」id)的条件 (a) 在当前
dump 上不可买,堵点是**通道分离**:唯一可观测量被分支和普通对线两种来源共写,
本轮把非分支人口的**下界**测到 40.0–51.8%。issue 里给了两条候选路与预登记读法。
**球交给总监。**

**本组不主张**:不主张 `pullcad` 该退集、该 promote、该改。本轮买到的是
「**为什么买不到**」+ 一个 ±6% 的足迹上界,不是这条杠杆的对错。

## 6. 进树的东西(#263:不在树上的读数,下一轮会重做且做得不一样)

`tools/batch_test/behavioral/pullcad_beat.py`(新),四个模式一个入口:

- 默认:in-domain poke 间隔表 + **阴性对照**(同英雄同敌人、**episode 之外**的右键);
- `--still`:§3.3 的判别器;
- `--duty`:§3.4 杠杆自报量;
- `--selfcheck`:**9 条真实数据断言,W27 与 W25 上各 9/9 PASS**
  (右键通道被 10/10 英雄写过、也落在小兵上 n=1547/1833;日志时戳细于 0.5s;
  非 1Hz 量化;`SHORT_MAX` 严格夹在两个节拍之间;两腿平衡 55/55 与 305/305;
  ab/ba 两层都在;阴性对照非空 189/805;无负间隔)。
- `--episodes N`:逐 episode 打 poke 序列与间隔,供逐帧接手。

§3.3/§3.4 两张表本轮**先用一次性脚本算出、再并进工具重算,读数逐位相同** ——
先做后并是本轮的顺序,不是推荐的顺序。

## 7. 诚实边界(不主张的东西)

- **不主张 `pullcad` = WORKING / BUGGY / SILENT。** 三值都买不到,理由在 §3.3。
- **不主张 §3.2 的任何符号**(两波两层各自反号)。
- **不主张 §3.4 的 null 是 `pullcad` 自己的** —— 44 id 同波,它界的是 bundle(§4a)。
- **不主张阴性对照本身是干净的**:它只排除「差异在 episode 之外也一样存在」,
  不排除「差异来自另一个 armed id 在 episode 之内」。本轮四格对照也是反号的。
- **不主张条件 (b)** —— 不归本组。
- **最薄的格**:W27 `ba/baseline` 的 long≥2s 只有 34 个,`ba` 层整体 4 局。
  W27 的每一格都比 W25 对应格薄一个量级;**结论靠的是两波复现,不是 W27 单独。**
- **W25 只并了 2/4 个 run**;W26 未并;**两波之间从未池化**。
- 未跑 `sweep_run.sh` 的 15 个通用检测器(欠账,第四轮)。
- `seed 975` **第十轮**;`wandlimbo` 预登记因 #293 未落地**第八轮**不可执行;
  **GH #265 仍被 #272 阻塞**(本轮无新增)。

## 8. 验证与发表

- `bash tools/agent/luacheck_gate.sh` → **exit 0 / `luacheck bots game: 0 warnings`**
  (脚本自装 `lua-check`,本容器冷启)。**未使用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行。**
- `bash tools/agent/arm_push_gate.sh` → `core.hooksPath=.githooks`,**already set**。
- 本轮未改 Lua ⇒ **不声称跑绿过 Lua 全量**(GH #124)。
- AWS:**S3 只读,零 EC2,零 CE 调用**;`session_setup.sh` 身份
  `arn:aws:iam::581436534924:user/dota2bot-agent` 已核。
- **MCP 未触发 `requires approval`。**
- push:`git push -u origin claude/lucid-pascal-lmiz69` + `git push origin HEAD:main`。

## 9. 下一轮第一件事

0. ⭐⭐ **`hero-20`(总监 §CH 点名本组当执行方)——已悬 2 轮,先做它,别再往后推。**
   零 EC2、零新局、无 gated id;读的是 WK 重生那一帧 900 半径内的敌方英雄数分布。
   **`acceptance_amendment` 必须一起读**:预登记识别子在 109 份 fixture 里出现 0 次 ⇒
   **任何识别子得 0 次记 `METHOD-FAILED`**,不得记成「触发罕见 ⇒ t25 定价坐实」。
1. **`pullcad` 的棒已交出去**(GH #326 → 总监),不掉在本组;
2. 下一个零记录 id:**`blinkflee`** 或 **`liondrainstop`**
   (`campvoid` 已裁 SILENT、`fieldcreep` 已测域、`pullcad` 本轮已裁 INDETERMINATE、
   `zusstatic` 有 GH #207 在案);
3. **把 §4 那条「静止在小兵火力里」做成树上的检测器**(本轮只有一次性读数),
   与上一轮 `fieldcreep` 的前提检验并案;
4. **W25 剩下两个 run**(`a29ed3`/`ecbb41`)本轮未并,`pullcad_beat.py` 可直接跑;
5. 两笔老欠账仍挂着:`sweep_run.sh` 15 检测器(第四轮)、`--escape-w 12` 敏感度。

## 10. Token 用量

`python3 tools/agent/token_usage.py` 最后一行,原样:

```
TOKENS total_in=8,328,263 out=68,075 turns=62
```

(统计时刻为止;之后的收尾回合不计入。)
