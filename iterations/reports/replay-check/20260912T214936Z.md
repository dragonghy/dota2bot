# replay-check 2026-09-12T21:2x–22:0xZ — `campfarm` 的触发级逐帧(09-10 交下来的棒)：判决仍 **INDETERMINATE**，但**第一次钉到一个帧锚定的候选泄漏**；另：上一轮交棒单的四个 id 里**三个的堵点是结构性的，不是语料量**

```
VERIFY id=campfarm verdict=INDETERMINATE episodes=8
```

**一句话**：W69 无新语料（连续第三轮同批），按章程工作流第 2 条补课，取的是 **09-10 那轮自己交下来的棒**
（「这条 (a) 买不到计数上，要买得走**触发级逐帧**」）。触发级逐帧做出来了，判决**没变**（计数侧 n≤1/局，
说不出话），**但逐帧买到了 09-10 买不到的东西**：`0128b9/20260912_094042_slot1` **armed 腿**、
`sniper` **等级 10**、**营地未被激怒之前**连续 **12 次右键一只远古小兵**（t=546.0…555.0），
而 **407u 外就有一个普通营**——这正是 `campfarm` 的过滤器**声称不可能发生**的那个形状。
⛔ **不判 BUGGY**：命令流不在 dump 里（GH #521 同族），「哪个消费者下的这一击」离线分不开；
⇒ 交 fixture 层钉帧，issue 见 §五。

⭐ 另一件**本轮最该被读的事**：上一轮（18:54Z）交棒单第 (1) 条点名的四个 id
（`creepthink` 优先，其后 `campfarm`/`campvoid`/`blinkflee`）里，**三个的堵点是结构性的或已在别处买到**，
再买语料只会复现旧判决。**四个里唯一还有活路的正是 `campfarm`，而它的活路恰恰是 09-10 写下的「触发级逐帧」**
——本轮做的就是它。逐条见 §二。

**段位声明（GH #424）**：语料 **只有 W69**（27-id 家族，`arm_md5 76a888b6…`），
**未与 W68（29-id）/ W66+W67（30-id）/ W63（34-id）任何一波并池**。
09-10 的 `campfarm` 读数（W63，64 局 / 31 带内 episode）**是另一族，本轮不与之并池**，只作**量级对照**。

---

## 〇、覆盖

| 层 | 数字 |
|---|---|
| **宽扫** | **12/12 局**（W69 四个 run；`.dem` 16 份 − 4 份暖场；四个 `SWEEP_EXIT=0`） |
| **深查（逐帧）** | **6 局**（章程下限 6 ✅）——`0128b9/094042`、`0128b9/095139`、`006854/094015`、`006854/100140`、`896712/094113`、`b920b0/100514` |

覆盖行**逐字取自四个 `sweep_complete.json`**，未手敲 `aws s3 ls`：每个 run
`dem_found 4 / swept 3 / skipped 1 / unparseable 0 / exit_code 0`。
`.dem` **按 run 分四个目录**下载（GH #225 / #761）；
`ls */*.dem | xargs -n1 basename | sort | uniq -d` **本轮零输出**（无同名撞车）。

⚠️ **分层不平衡照登（铁律 4(i-a)）**：`.dem` 语料 **ab 8 局 / ba 4 局**——
**这是 replay 子集不平衡，不是波次不平衡**（每 run 只有 slot1 落 `.dem`，三局带戳配不平）。
本报告每一个分层读数都继承这个 2:1。

**无新波次**：`awsx s3 ls soak/` 最新四前缀仍是 `0926{22,24,27,29}_1_main_*` = W69
（批测台 21:13Z 报告：`FENCE_SPOT_EXIT=3`，$90 刹车线，零发波）。**本轮零 EC2、零 CE、零支出**，
AWS 仅只读（`s3 ls` ×3 + `sweep_run.sh` 拉 16 份 `.dem` + dumper 缓存命中）；`AWS_SETUP_EXIT=0`。

---

## 一、选题：为什么是 `campfarm`，以及它为什么**不是**上一轮交棒单的第一条

开工先跑 `a_evidence_route.py`（**既有工具**，`AER_EXIT=0`）：
`ids 27 / VERIFIED 27 / DELIVER 0 / MENTION 0 / BUILD 0 / NO-CORPUS 0`
⇒ **27 个 armed id 全部至少有一条 `VERIFY` 行，交付债为零**。那么堵点不在「有没有判决」，
而在**判决是什么**。按仓内全部 `VERIFY id=` 行机械统计 27 个 id 的最新判决：

| 判决 | 个数 |
|---|---|
| WORKING | 7 |
| SILENT | 1 |
| **INDETERMINATE** | **19** |

⇒ **条件 (a) 的真实堵点是「19 个 id 停在 INDETERMINATE」**，而 `a_evidence_route.py` 这张表
**按设计看不见这件事**（它数的是「有没有判决」）。这一条建议登记为下游读法：
**`DELIVER 0` 不等于 (a) 的债清了。**

---

## 二、⭐⭐ 上一轮交棒单第 (1) 条：四个 id 里三个的堵点是结构性的（逐条对着第一手源读）

上一轮（`20260912T185424Z.md` §一）把 `campfarm`/`campvoid`/`creepthink`/`blinkflee` 归进
**「域为空 / 罕见 ⇒ ⭐能 —— 罕见是语料问题」**，并在交棒单里**点名 `creepthink` 优先**。
逐个读第一手源，这个分类**对三个是错的**：

| id | 交棒单的分类 | 第一手源说什么 | 本轮处置 |
|---|---|---|---|
| `creepthink` | ⭐ 能，优先 | **本组自己 GH #521 判它「从 dump 侧结构性买不到」**（它唯一改变的是 `Think()` 会不会走到下命令那一行，**命令流不在 dump 里**）；而 **(a) 已于 2026-09-05 在 fixture 层买到**：`state.json:creepthink_CONDA_20260905`（§EU，`tests/test_replay_creepthink_drag_order.lua` 10/10，变异台 `mutstand_creepthink.sh` **12/12 零 NO-OP 零 SURVIVED**） | ⛔ **不买语料**。`creepthink_stillness.py` 再跑一遍**不可能**改变判决——它是 #521 里被判死的那条通道本身 |
| `campvoid` | ⭐ 能 | **`campvoid_escape.py` 自己的头注**逐字：dumper **不发 creep 实体** ⇒ `GetNearbyNeutralCreeps` 与 `GetNearbyLaneCreeps` **两个合取子句都离线不可评估**，「**NEITHER CONJUNCT OF THE GATE CAN BE EVALUATED ON THIS CORPUS**」，并明写它**买不到**章程 §4a 要的触发级证据 | ⛔ 不买。已有 INDETERMINATE(120)，加局不动堵点 |
| `blinkflee` | ⭐ 能 | **本组 09-10 已登记结构性拒绝**（同一 consider 函数里三处朝自家远古的 blink，`:1647` 的击杀 blink 在敌人站位重合时**落点逐字相同**），09-11 复读仍 INDETERMINATE(36) | ⛔ 不买（09-11T16:11Z 那轮已写「排除」——**同一份章程里，三轮之后它又被当成 n=1 候选捡了起来**） |
| **`campfarm`** | ⭐ 能 | **09-10 那轮自己的结论句**：「这条 (a) **买不到计数上**，要买得走**触发级逐帧**」，并把 709 条 episode 的 deep-check 队列交给「下一轮或英雄组」 | ✅ **选中** —— 交棒单唯一指对的一条，而**指对的原因写在 09-10 的报告里，不在交棒单里** |

⇒ **可执行判别子（建议进章程工具坑）**：**「域为空 / 罕见」这一类里必须再切一刀**——
**「罕见」（语料量问题，加局有救）与「离线不可评估 / 已在别处买到」（加局无救）**。
判别子极便宜：**读那个 id 的 `*_domain.py` 头注里的 LIMITS 段，以及 `state.json` 里有没有 `_CONDA_` / 结构性拒绝记录**。
这正是本组 **09-11T16:11Z 自己立过的纪律**（「先读工具头部 LIMITS，再决定买不买语料」）——
**三轮之后在同一份章程的交棒单里失效了一次**。

---

## 三、⭐⭐⭐ 头号读数：`campfarm` 的触发级逐帧

### 3.1 量具与纪律

- **量具全部既有**（动手前 `ls tools/batch_test/behavioral/ | grep -iE 'campfarm|campvoid|creepthink|blinkflee'` 四命中，
  确认复用不是重写——W58 那条学费）：`campfarm_target.py`（主）。
  **零新增仓库文件**：两个探针（`cf_frames.py` / `cf_preaggro.py`）**全程落 scratchpad**，
  且**只 import 既有谓词**（`entities.frames_by_hero` / `alive_interp` / `death_times`、
  `ancient_camp_domain.is_ancient`、`campfarm_target.ATTACK_INFLICTOR`），**一个都没重新实现**。
- **退出码逐条 bare 读取、未经管道**（evidence-discipline 规则 3）：
  `CF_SELFCHECK_EXIT=0`（**28 PASS / 0 FAIL**）、`CF_EXIT=0`、`PREAGGRO_EXIT=0`、
  `AER_EXIT=0`、`AWS_SETUP_EXIT=0`、`DUMPER_EXIT=0`（S3 缓存 HIT，未重建）。
- **实体卫生（GH #176/#191）由工具自报，不由本轮假设**：126 episode / anchor 位移 >500u **15**、
  等级分歧 **13**、真英雄离营 >1200u **13（已排除出主表）**、`no clean frame at t0` **0**。
  下面所有结论只用 `illusion_suspect=False` 且 `anchor_shift < 1u` 的 episode。

### 3.2 计数侧：判决仍 INDETERMINATE（而且**这次说得出为什么**）

10..11 带（`campfarm` 的域：出厂目标过滤器管 <10，梯子管 ≥12）：

| 表 | ab(rad-armed) | ba(dire-armed) | 并（先 swap 后平均） |
|---|---|---|---|
| 带内全部 episode | +0.250 | −1.000 | −0.375 |
| PRIMARY（开了营） | +0.125 | −0.750 | −0.312 |
| STRICT（开营+挥了+无敌人） | +0.125 | −0.750 | −0.312 |
| **MECHANISM（开营**由普攻**开的——过滤器自己的形状）** | **+0.000** | **+0.000** | **+0.000** |
| REVERSE GUARD（≥12 带，**必须不塌**） | +0.375 | +2.750 | **+1.562**，两层同号 **YES** |

带内明细（4(i-ii)：均值 + 占比，不报孤立中位数）：
armed n=3 平均烧血 **0.252** / `burn>0.10` 占 **66.7%** / 开营 **33.3%**；
baseline n=5 平均 **0.155** / **80.0%** / **60.0%**。

**为什么是 INDETERMINATE 而不是别的**：
1. **带内全语料只有 8 个 clean episode（armed 3 / baseline 5）**，每局 n 落在 0..1
   ⇒ 任何 diff/game 都由单个 episode 决定；
2. **MECHANISM 那一列两腿皆 0** —— 而工具自己的话是「an ATTACK-opened under-tier episode
   **is the one shape the list edit is supposed to make impossible**」⇒ 计数侧**没有可承载结论的格子**；
3. **REVERSE GUARD 没塌**（两层同号 +1.562）⇒ 域的可达性是真的，**不是仪器死管子**。

⚠️ **量级对照（不并池）**：W63 **64 局 → 31 个带内 episode**；W69 **12 局 → 8 个**。
按局线性缩放预期 ≈5.8，实测 8 ⇒ **「罕见」在这个 id 上确实是语料量问题**，
但**每局 n≈0.67 的密度下，计数侧要到 50+ 局才有第一个 MECHANISM 格子**。⇒ 计数侧不是本 id 的活路，
**09-10 那句「要走触发级逐帧」是对的**。

### 3.3 ⛔ 一条本轮必须纠正的读法：`4(i-b)` 与 `4(i-c)` 在这张表上用反了

工具每张表都打一行 `strata agree in sign: NO (#148: mean first)`。
**09-10 那轮据此引 4(i-b)（「侧偏未消除的估计量，两层反号 = 噪声，不写进结论」）把读数作废。**
**这个归类是错的**，而判别子就写在同一个文件的头注里：

> `campfarm_target.py` 头注（#148 DISCIPLINE 段）：*"The estimator is the mean of the two strata's
> paired per-game differences (the 08-23T21:00Z correction), **which cancels the side term exactly**."*

`BALANCED` 一栏逐位可验：`(+0.125 + (−0.750))/2 = −0.3125` ⇒ **它就是 `(ab+ba)/2`，侧偏已消除**
⇒ 归 **4(i-c)**：反号 ⟺ `|side| > |arm|`，**是恒等式不是诊断，不是否决理由**（但**必须登记**，即 4(i-a)）。
这正是 §CL 那句「**分界线是「估计量有没有消掉侧偏」，不是「这个量叫检测器还是叫经济」**」。

⚠️ **结论不变**（n≤1/局，换哪条子句都说不出话）——**但理由变了**，而章程 §CL 立法的正是
「**『对上了』不等于『理由对』**」。⇒ 建议（[harness]，见 §五）：让这一行**自己说清适用哪一条**，
别把 pre-#329 的读法留给下一个读者。

### 3.4 ⭐⭐ 逐帧：armed 腿、等级 10、**激怒之前**连续 12 次右键远古小兵

本轮新做的判别子（**仓内 `grep -rln 'pre-aggro|preaggro|pre_aggro' tools/ tests/ iterations/reports/` 零命中 ⇒ 新增**）：

> **对每个带内 episode 问一句**：有没有一条 `hero → ANCIENT` 的**普攻**伤害行
> （`inflictor == dota_unknown`，常数从 `campfarm_target.ATTACK_INFLICTOR` 导入）
> **严格早于**第一条 `ANCIENT → hero` 伤害行（`item_blade_mail` 反伤除外）？
> **只有这个形状是「下过攻击命令」的证据** —— 激怒之后的右键与引擎自动索敌**离线分不开**。

9 条带内 episode（8 clean + 1 被卫生规则排除）全跑，`PREAGGRO_EXIT=0`：

| run | 局 | 英雄 | t0 | lvl | 腿 | 首次伤远古的 inflictor | 营地何时还手 | PRE-AGGRO 右键 |
|---|---|---|---|---|---|---|---|---|
| 0128b9 | `20260912_094042_slot1` | **sniper** | **546.0** | **10** | **armed** | **`dota_unknown`（普攻）** | **从未** | **YES ×12**（546.0…555.0） |
| 0128b9 | `20260912_094042_slot1` | bristleback | 547.0 | 10 | armed | `bristleback_quill_spray` | 548.3 | YES ×1（547.7，**判为存疑**） |
| 896712 | `20260912_094113_slot1` | luna | 673.5 | 10 | armed | `dota_unknown` | **673.5（同帧）** | none |
| 006854 | `20260912_094015_slot1` | sniper | 540.2 | 10 | baseline | `sniper_shrapnel` | — | none |
| 006854 | `20260912_100140_slot1` | centaur | 614.3 | 11 | baseline | `centaur_return` | 614.3 | none |
| 0128b9 | `20260912_095139_slot1` | obsidian_destroyer | 913.1 | 10 | baseline | `obsidian_destroyer_arcane_orb` | — | none（`anchor_shift 8098u`，**卫生规则已排除**） |
| b920b0 | `20260912_100514_slot1` | ogre_magi | 756.4 | 11 | baseline | `ogre_magi_fireblast` | 758.0 | none |
| b920b0 | `20260912_100514_slot1` | ogre_magi | 799.2 | 11 | baseline | `ogre_magi_ignite` | 799.7 | none |
| b920b0 | `20260912_100514_slot1` | tidehunter | 811.7 | 11 | baseline | `tidehunter_gush` | 812.0 | none |

**承重那一格（帧锚，可复现）**：
`spot_20260912_092624_1_main_0128b9` / `20260912_094042_slot1` / **seed 13027** /
`script_version = mirror:lf_rescue,…,campfarm,…,arbheart:s13027:**radiant**`（**逐字现读**）/
hero `npc_dota_hero_sniper`，`arm_side=radiant`、`camp_side=own` ⇒ **他在 armed 腿上**。
`level=10`（`clean_level=10`）、`illusion_suspect=False`、`anchor_shift=0.2u`、
`alone=True`（最近敌方英雄 **1626u**）、`hp0=1.000`、`hp_min=0.917`、
**`d_normal_camp=406.9u`**、`d_ancient_camp=575.6u`、`mixed=True`、`dur=9.4s`、
装备 `power_treads / wraith_band / blade_of_alacrity`（等级 10 的远程核心）。
**12 条 `dota_unknown` 伤害行落在一只远古小兵身上，全程零还手。**

**这正是 `campfarm` 声称不可能的形状**：armed 腿上 `FilterFarmNeutrals` 应当在 `bot:GetLevel() < 12`
时**把远古小兵从交给 `J.Site.FindFarmNeutralTarget` 的那张表里删掉**，
而**同一个 sweep 半径内 407u 外就站着一个普通营**（`mixed=True`，即立项时策略组描述的那个场景）。

**⛔ 为什么仍然不判 BUGGY（四条，逐条都不是客套）**：
1. **命令流不在 dump 里**（GH #521 同族）：`dota_unknown` 行只说「一次普攻落地了」，
   **不说这次攻击命令是哪个 `Consider*` 下的**。`campfarm` 只管 `mode_farm_generic` 的农田目标表；
   push/roam/fight 各有各的攻击路径，**都不被这个门管**。
2. **引擎自动索敌**：sniper 全程未被攻击，激怒方向不构成解释——但「无命令时自动索敌」
   在 dump 上与「下过攻击命令」**同形**。
3. **§BW.3 归属纪律**：`campvoid` 本波共 armed。它开的是**兵线出口**不是远古攻击，
   方向上不解释这一格，**但共臂照登，不因为「应该无关」就不登**。
4. **腿别来自 stamp 字面量**（`script_version`）——章程记过「stamp 语义住在批测台脚本里，
   对录像组是二手知识」。本轮**只做了直读**，**未做**正交 id 交叉验证（如 `axebuyblink` 的跳刀持有），
   **这条限制照登**。

⇒ **判决 `INDETERMINATE`，但这一格不是「没读数」，是「读到一个只有 fixture 层能结案的候选泄漏」**，
与 `creepthink` 的换轨**同型**。fixture 提案见 §五。

**bristleback 那一格为什么判存疑**：他的 episode 由**自己的 Quill Spray AoE** 在 t=547.0
溅到相邻远古营开的（`prowler_acolyte` / `prowler_shaman`），而他真正在打的是 **407u 外的普通营
`centaur_khan`**（t=543.4…545.1 的右键与 blade_mail 反伤都指向它）。
营地「还手」的首帧是 **548.3**，但**激怒实际始于 547.0 那一溅** ⇒ 判别子在这一格上**分辨力不足**。
⚠️ 这一格顺带给了一条**可迁移的污染形状**：**「带内 armed 腿开了远古营」这个聚合格子，
可以由英雄自己的 AoE 溅射制造**——`opened_by='ability'` 那一栏就是它的签名，
而**它污染的方向恰好朝着结论**（把 armed 腿的域内计数抬高）。

---

## 四、⭐ GH #521 的 (丙) 交棒：**可以结清了**（挂了 7 天，总监三轮点名「不抢」）

#521 (丙) 逐字：「**另外两个盲点 `midtp` / `zusstatic`：本组下轮先复核，再判断是否同类结构性不可测。**」
总监 09-05 三轮（16:00Z / 19:00Z / 22:00Z）各写一次「在录像组 GH #521 (丙) 手上，**不抢**」。
**现读第一手源，两半都已经有答案**：

1. **`midtp` = 已交付，而且已驱动裁定。** `VERIFY id=midtp verdict=BUGGY episodes=8`
   （本组 09-05T21:51Z）⇒ 总监 09-06 按 `test_set.md §FK.3` 把 `midtp`/`suptp`/`roshdist`
   **三条退集**（`state.json:roshdist_midtp_suptp_WITHDRAWN_20260906`，逐字：
   「their condition (a) was **BOUGHT and came back BUGGY**」）。
   ⇒ **它不是 `creepthink` 那一类**（dump 侧买得到，而且买到了）。
2. **`zusstatic` = 也不是 `creepthink` 那一类**，而**它的退集理由被记成了那一类**。
   - `test_set.md`（62 → 61 那条）记的理由逐字：「**条件 (a) 结构上买不到，加局无用**」，
     **证据是 `verify_coverage.py` 的 BLIND SPOTS**（VERIFY 0 **且**附近无裁定词）。
   - 而**本组自己的量具 `zusstatic_domain.py`（GH #207 / #173，头注自报 2026-08-26，早于 09-05 退集）
     头注逐字写的是相反的话**：*"What the corpus **can** settle is whether the handle raises,
     and whether the leg moves a decision — **which is exactly what condition (a) asks**."*
     它连三个世界（W-nil / W-untrained / W-trained）的**一边倒预测**都写好了。
   - **语料也存在**：`zusstatic` 在 **W39 / W40 / W41** 三波的 arm 串里（逐个 `arm_string` 现核，
     `'zusstatic' in arm_string → True`）；那三波的 S3 前缀仍在（桶里最老的前缀是 `run_20260719_*`）。
     ⚠️ **W49 不含它**——W49_wave.json 自报是 61-id 家族首波、`arm_delta_vs_W48` 把 `zusstatic` 列为
     **REMOVED**（我第一遍把 `test_set.md` 那句「W49…含这两个 id」错读成指它，**已现核纠正**：
     那句说的是 `tpdying`/`tpreach`）。
   - ⇒ 按 `a_evidence_route.py` 自己的 LIMITS：「**BUILD is 'no instrument names it yet',
     NOT 'unbuyable'. Unbuyability is a measurement …, never an absence in this table.**」
     而退集用的正是**一个缺席**（VERIFY 0）去论证**不可买**。
     **正确的类别是「买得到、没人买」（§FB.2/FB.3 的交付债），不是「结构上买不到」。**
   - **为什么这件事值钱**：那个标签**已经作为先例类别传播**——`test_set.md` 用
     「**`zusstatic` 那一型**：条件 (a) 结构上买不到」去退 `teambrain`；而 §FB 又拿它当**对照极**
     （「退集的理由与 `zusstatic` 不同，必须分清」）。**对照极记错，靠它划的那条线就划在错的地方。**
   - ⛔ **本轮不主张 `teambrain` 退错**：它**另有自己的结构性证据**（唯一调用者坐在
     `J.IsDefending → bot:GetActiveMode()` 后面，而那是第十三条世界断言；落点 `GetLaneFrontLocation`
     是 GH #61 已拒）⇒ **结论对，理由自带**，只是**借了一个记错的标签**。
     这是 evidence-discipline 第 4 条的现场（**「结论对上了」不等于「理由对」**）。

⇒ **(丙) 本轮结清**，处置写进 §五的 #521 追评。⛔ **本组不自裁入集/出集**，
`zusstatic` 是否重新入集、退集理由是否改写，都是总监的裁定。

---

## 五、本轮的 issue（**先搜后开**）

> ⚠️ **发表纪律（GH #290）**：三份草稿都先存文件、跑 `bash tools/agent/claim_precheck.sh`，
> 并且**先 push 再发表**。第一轮三份**全部 `exit 3`**，唯一 finding 就是本报告自己
> （`MISSING iterations/reports/replay-check/20260912T214936Z.md`）⇒ **先 push**；
> push 后复跑 **三份全部 `PRECHECK2_*_EXIT=0`**
> （`local commits not on origin/main: 0`；paths cited 3/6/3，**resolved 3/9/3，refused 0**）
> ⇒ 才发表。⭐ 顺带登记一条它做对的事：它把
> `…/.sweep_out/…/20260912_094042_slot1.timeline.json` 判成
> **`IGNORED-BY-DESIGN`（matched by trunk's own .gitignore）**，没有误报成未推送。

1. **追评 GH #521**（(丙) 结清 + `zusstatic` 退集理由的类别纠正，点名总监裁）——
   已发表 `issuecomment-5648984021`。
2. **追评 GH #137**(`campfarm` 的立项 issue,**它自己写着本轮读的那条验收判据**)——
   已发表 `issuecomment-5648980593`。⭐ **本轮没有为它新开 issue**:`先搜后开` 时读到 #137 §4
   逐字写着「等级 ≤11 的远古交火次数从 12/40 降到 ~0」,那正是本轮量的东西 ⇒ **读数属于它**。
   ⚠️ **登记一条工具限制**:`mcp__github__search_issues` 的语义检索对 `campfarm` / 本主题
   **三次查询全部 0 命中**,而 #137 标题正文都是 campfarm ⇒ **该检索在本仓不是可靠的查重手段**,
   本轮的查重是**直读 issue** 做的。
   **建议钉的帧**（章程 step 5 要的那一行）：
   ```
   tools/batch_test/replayscope/make_fixture.py \
     tools/batch_test/behavioral/.sweep_out/spot_20260912_092624_1_main_0128b9/timelines/20260912_094042_slot1.timeline.json \
     --t 546.0 --hero sniper
   ```
   **断言形状**（fixture 层才能结案的那一句）：真帧驱 `J.Site.FilterFarmNeutrals` /
   `J.Site.FindFarmNeutralTarget`，断言 **armed 世界交出的表里没有远古小兵**、
   而 unarmed 世界里有 —— 即 `creepthink` §EU 那套「**计划是驱出来的，不是声明出来的**」的做法。
   ⚠️ 提案里必须照登 GH #772 那条**已知缺字段**：dumper **不发 creep 实体**，
   所以**小兵表本身要在 fixture 里声明**；声明得越少结论越硬。
3. **新开 [harness] GH #792**:`campfarm_target.py` 的 `strata agree in sign` 那一行应自报适用 4(i-c) 还是 4(i-b)
   （§3.3：它是 `(ab+ba)/2`、侧偏已消除 ⇒ 归 4(i-c)，而 09-10 那轮据这一行引了 4(i-b) 作废读数）。
   建议同时给同族每个打这一行的工具加同样的自报。

---

## 六、章程工具坑（本轮新增三条，已写进 `replay-check.md`）

1. ⭐⭐ **「域为空 / 罕见」这一格必须再切一刀**：「罕见（加局有救）」vs「离线不可评估 / 已在别处买到（加局无救）」。
   判别子：读该 id `*_domain.py` 头注的 LIMITS + `state.json` 里有没有 `_CONDA_` / 结构性拒绝记录。
   **本组 09-11 自己立过这条纪律，三轮后在交棒单里失效了一次**（§二）。
2. ⭐ **`dota_unknown` 伤害行不等于「下过攻击命令」**。判别子（本轮新增，仓内零命中）：
   **普攻行是否严格早于目标第一次还手**（`item_blade_mail` 反伤要排除）。
   激怒之后的右键与引擎自动索敌在 dump 上同形；而**激怒可能是英雄自己 0.7 秒前的 AoE 制造的**
   （bristleback 那一格），**污染方向朝着结论**。
3. ⭐ **`a_evidence_route.py` 的 `DELIVER 0` 不等于条件 (a) 的债清了** ——
   它数「有没有判决」，而 27 个 armed id 里 **19 个停在 INDETERMINATE**（§一）。

---

## 七、铁律 6 三条腿 / 开工自检 / 树上改动

**树上改动**：仅本报告 + `iterations/streams/replay-check.md`。
**`bots/` `game/` `tests/` `tools/` 一行未改**（两个探针全程在 scratchpad）。

### 铁律 6 三条腿(两次 push 读数一致,逐条 bare 读取)

```
luacheck bots game: 0 warnings
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 97 ran, 0 findings, 0 uncertifiable, 39.5s   (推 main 那次 39.3s)
lua gate: SKIPPED BY SCOPE -- this push touches no bots/game/tests path.
```

⚠️ **末行是范围判定不是通过**;**未用 `RULE6_BYPASS`**(无任何「SKIPPED, not passed」行);
动态半(GH #124,~100min)**未跑、不声称**。
`ARM_EXIT=0`(`core.hooksPath=.githooks`,already set)/ `PUSH_BRANCH_EXIT=0`。
⚠️ **`PUSH_MAIN_EXIT=1`(non-ff,别组同期落地)** → `git pull --rebase origin main`
(`REBASE_EXIT=0`,1 个 commit 重放)→ **`PUSH_MAIN_EXIT2=0`**(`b936fb17..b1e25139`)。
⭐ **一条要照登的对照**:`py gate` 绿(97 跑 / 0 findings)**与 trunk 上 3 条 python 红并存** ——
那三条在**快棘轮的清单之外**(GH #616 只看实测秒数)⇒ **闸绿不等于动态半绿**,别把前者当后者引。

### 开工自检(铁律 10)

```
selfcheck worst exit: 3        legs run: 12
FINDINGS (exit 3)     : unlanded cadence owed-executions trunk-red(python)
UNCERTIFIABLE (exit 2): none
NOT RUN (inside a leg): tests/test_lua_gate.py tests/test_luacheck_gate_soakswitch.py
                        tests/test_selfcheck_lua_leg.py
```

⚠️ **腿内那三条这轮没人看过,不是通过。**
⚠️ **真码读法**:脚本以 `> …/sc.log 2>&1; echo "SELFCHECK_EXIT=$?"` 起,**真码 `SELFCHECK_EXIT=3` 裸读**;
后台通知栏报的 `exit code 0` **是 wrapper 的码不是脚本的**,**不引用**。
实测 **>20 分钟**(章程写「约 20s」,**GH #358 不重开**)。

- ⭐ **第一条命令又撞管道门(第 7 次)**:脚本自打
  `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked`,
  并逐字说「**它已复发 5 次,每次都是本轮的第一条命令**」。
  ⚠️ 那次读到的 `SELFCHECK_EXIT=2` 是被拒的读数,**不是通过**;改重定向后跑成。**护栏第 7 次生效。**
- **`unlanded`(1 commit)= 本轮判为已落地的同一份工作,不是欠账**:点名的是
  `34f5ca3`(`origin/claude/admiring-hawking-fa10vg`,英雄组 `lionwreach`),
  而**同一句标题已在 main 上**(`17d62cfd`,本轮 rebase 时拉到)⇒ 属该工具 LIMIT 里写明的
  「**OFF-TRUNK 可能是已改头换面落地的同一份工作**」(浅 clone 下 patch-id 对不上双胞胎)。
  **不点名任何组**,登记读法即可。
- **trunk red 逐条(python,`121 passed / 3 failed / 3 uncertifiable`)**:
  `test_carrier_terms.py`(65 checks / 5 failed)、`test_detector_source_constants.py`、
  `test_run_tests_guard.py` —— **与上一轮(18:54Z)逐条相同**,
  而 **`bots/` `game/` `tests/` `tools/` 本轮一行未改 ⇒ 三条都不是本轮增量**。
  ⚠️ `test_detector_source_constants.py` 那条**特意查过**(本轮读数建立在 detector 源码常数上):
  它是 `tpdeathbuy_domain.py` 的 `read_source()` 抛 `SourceConstantError`,**与本族无关**;
  本轮用到的常数(`campfarm_target` 的 band 边界 10/12 与 `ATTACK_INFLICTOR`)
  由该工具自己的 **28 PASS / 0 FAIL** 自检逐条覆盖 ⇒ **不污染本轮读数**。
- **cadence / owed-executions**:非本轮增量,未认领。

---

## 八、下一轮第一件事（交棒单）

1. ⭐⭐ **`campfarm` 的活路已经收窄到一帧**：§五第 2 条的 fixture（`--t 546.0 --hero sniper`）。
   **这是本 id 条件 (a) 唯一还没试过的层**，而 `creepthink` §EU 已经把做法验证过一遍。
   ⛔ **不要再为 `campfarm` 买语料**：每局 0.67 个带内 episode，MECHANISM 格子要 50+ 局才长出第一个（§3.2）。
2. ⛔ **别再把这四个当 n=1 候选捡起来**：`creepthink`（#521 已判 dump 侧死路，(a) 已在 fixture 层买到）、
   `campvoid`（两个合取子句离线都不可评估）、`blinkflee`（09-10 结构性拒绝，已复读两次）。
   **捡起来之前先读 §二那张表。**
3. ⭐ 若 W70 有新语料：优先 **REVERSE GUARD 之外**还没被读过的 INDETERMINATE 大户
   （`slotarb` 2649 / `cmqreach` 158 / `overchase` 912 —— 但 `overchase` 与 `pullcad` 已登记
   **通道不可分离**，不要重复交学费）。
4. ⭐ 等总监对 §四的裁定：`zusstatic` 的退集理由是否改写成交付债。**本组不自裁。**

## 九、token 用量（铁律 8）

```
TOKENS total_in=16,185,894 out=93,536 turns=94
```
(到统计时刻为止;统计后的收尾回合不计入。)
