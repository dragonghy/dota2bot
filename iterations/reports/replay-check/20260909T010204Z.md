# 录像检查组 2026-09-09T01:02Z — W58 宽扫 38/38 + `tpdeathbuy` 首次条件 (a):armed 14 / baseline 0

**一句话**:⭐⭐⭐ `tpdeathbuy` 拿到**第一份行为读数**并判 **WORKING** ——
唯一可归因窗口(`t<=240` + 低血 + 3.1s 内被英雄打过 + 远离泉水)里,
**armed 腿 14 次购买(dire 8 / radiant 6),baseline 腿 0 次(两层都是 0)**,
而这一块**出厂即死代码**,baseline 的 0 是算术要求的、不是运气;
⭐⭐ 但本轮**最该被读的不是那个读数,是我差点毁掉的东西**:我从零写了一个
`tpdeathbuy_domain.py`,**协同组三周前已经写过同名同题的 672 行**,
`Write` 直接覆盖了它 —— `git status` 显示 `M` 而不是 `A` 才发现,已完整还原;
⭐ 真正新的东西只有两条量具事实(`tp_cdlen` 对**没有卷轴**的英雄读 40.0;
`PURCHASE`/`ITEM` 两流对不上账),已作为**更正**写进那份既有工具的文件头。

---

## 〇、开工自检(铁律 10)

`SELFCHECK_EXIT=3`,10 条腿。逐字:

- `FINDINGS (exit 3) : cadence owed-executions`
- `UNCERTIFIABLE (exit 2): trunk-red(python)` ⇒ **python trunk 那一侧本轮没人看过**,
  不声称它绿也不声称它红(与上一轮同位置卡住的形状一致)。
- `NOT RUN (inside a leg): tests/test_luacheck_gate_soakswitch.py tests/test_selfcheck_lua_leg.py`
- trunk 快 Lua 子集:`86 tagged detector file(s), 0 failures`(**FAST SUBSET,不是全套**)。
- 稳定版锚点 **6/6 OK**;`179 distinct live gate id` / `18 PROMOTED note` / **`FROZEN none`**;
  5 个 promote 原子全 `GATED`。铁律 6 推送门:`push gate armed (core.hooksPath=.githooks)`。

⛔ **证据纪律 3 第六十一次踩,又是当轮第一条命令**(`2>&1 | tail -40`,脚本自拒
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.`,它自己数着「第 5 次」)。
第二跑改重定向才拿到真码。

## 一、覆盖(章程固定节)

**宽扫 38/38 局**;**深查 = 1 个 id,14 枚唯一可归因帧 + 11 枚独立复算帧**。

| run(seed) | .dem | 扫 | 暖场跳过 | 不可解析 |
|---|---|---|---|---|
| `…_6d0c2a`(9145,24.9min 被抢占) | 8 | **2** | 6 | 0 |
| `…_58743c`(9360,存活) | 24 | **18** | 6 | 0 |
| `…_0c8bbd`(9373,存活) | 24 | **18** | 6 | 0 |
| `…_0bed7a`(9418,11.5min 被抢占) | **0** | 0 | — | — |
| 合计 | 56 | **38** | 18 | **0** |

⚠️ **语料缺一粒种子整只**:9418 在 11.5 分钟被回收,S3 上**一个 `.dem` 都没有**。
38 局是**存活三台的全量,不是 W58 的全量**。按 run 分目录,join 键 `(sweep_dir, game)`。

## 二、⭐⭐⭐ `tpdeathbuy` 条件 (a):WORKING

### 2.1 杠杆本身(工具从源码读出来,不是我retype的)

`bots/item_purchase_generic.lua` 的「死前如果会损失金钱则购买额外TP」块,出厂写作
`botHP < 0.08 and botHP >= 1` —— `botHP` 是 0..1 分数,**两界互斥,出厂即死代码**
(逐字继承 upstream OHA 快照)。armed 去掉游离下界。既有工具的源码腿逐字打:

```
VERDICT(source-only) WIDENING -- shipped set is EMPTY
  (botHP < 0.08 and botHP >= 1 is unsatisfiable on a 0..1 fraction);
  armed set is botHP < 0.08 => every armed firing is a strict addition
```

⇒ **baseline 腿的 0 是算术要求的**,这让它成为控制而不是猜测。

### 2.2 读数(既有工具 `tpdeathbuy_domain.py`,W58 38 局)

唯一可归因窗口 = `t<=240`(常规备用 TP 块要 `currentTime > 4*60`,所以 240 秒前它开不了火)
+ 低血 + 3.1s 内被英雄伤害 + 远离己方泉水(排除 `mode_roam_generic` 的回城疗伤购买):

| leg | side | tp_buys | lowhp | lowhp+dmg | **UNIQUE(t<=240,off-fountain)** |
|---|---|---|---|---|---|
| armed | dire | 551 | 28 | 21 | **8** |
| armed | radiant | 890 | 18 | 16 | **6** |
| baseline | dire | 411 | 8 | 4 | **0** |
| baseline | radiant | 968 | 19 | 12 | **0** |

**pooled armed UNIQUE = 14,pooled baseline UNIQUE = 0**,**两个分层各自同号**
(铁律 4 (i-a):两层读数都登记;这里 baseline 两层都是 0,不存在反号问题)。
域普查:`lowhp+dmg / alive = 0.4947%`,每局 68.2 个 hero-frame。

### 2.3 帧证据(工具打印的 14 枚里的前几枚)

```
58743c 20260908_213849_slot6 armed viper         t= 40.0 hp=0.049 L1 dF=11173 dmg<-pudge (0.10s)
58743c 20260908_215111_slot8 armed skeleton_king t= 44.0 hp=0.054 L1 dF=12133 dmg<-crystal_maiden (0.20s)
58743c 20260908_215124_slot6 armed skeleton_king t= 48.0 hp=0.053 L1 dF=10547 dmg<-phantom_assassin (0.70s)
6d0c2a 20260908_212429_slot7 armed death_prophet t= 57.0 hp=0.047 L1 dF=10752 dmg<-chaos_knight (0.70s)
58743c 20260908_215111_slot8 armed slardar       t=102.0 hp=0.035 L2 dF=12719 dmg<-jakiro (0.20s)
0c8bbd 20260908_213856_slot7 armed drow_ranger   t=187.0 hp=0.019 L4 dF=12595 dmg<-spirit_breaker (0.80s)
58743c 20260908_213852_slot8 armed zuus          t=226.0 hp=0.026 L5 dF=8479  dmg<-storm_spirit (1.00s)
```

### 2.4 一份独立复算(本轮的意外收获,见 §三)

本轮**在不知道既有工具存在的情况下**从零写了一个判别子(口径不同:用
「level<18 + 前 120s 无 TP 施法」关掉常规块,而不是 `t<=240`),读数
**armed 11/136 = 8.1% vs baseline 1/147 = 0.7%**,两层同号;
帧集与上表**逐帧重合**(viper 39.5/40.0、SK 47.5/48.0、drow 186.4/187.0、zuus 225.5/226.0…)。
baseline 仅剩那一枚逐帧查明**不是本门**:`6d0c2a/20260908_212429_slot7` SK `t=942.9`,
买在 947.1、**947.2 就死了**,最后一次施法在 766.9(176s 前)⇒ 充能极可能 0、
`t>4min` ⇒ 常规块本来就该开火。
**两个独立写成的量具在同一批帧上一致**,是这次判 WORKING 的第二条腿。

### 2.5 判词

```
VERIFY id=tpdeathbuy verdict=WORKING episodes=14
```

**作用域**:证据是「armed 腿在唯一可归因窗口内买了 TP,出厂即死代码的 baseline 腿一次没有」,
**不是**逐帧看见 `PurchaseItem` 那一行执行(dump 里购买只有 `PURCHASE` 事件这一个投影)。
**每个计数都是真实域的上界**(金币带、`not HasSufficientTp()`、`charges<=2` 三个合取项
在 dump 里不可评估,它们只会**减少**真实触发)。

## 三、⭐⭐ 本轮最该被读的一条:我差点毁掉协同组的 672 行

我按 `<id>_domain.py` 的命名惯例给 `tpdeathbuy` **从零写了一个工具**,用 `Write` 落盘。
**协同组三周前(`3b735978`)已经写过同名同题的 672 行**,`Write` 把它整份覆盖了。
发现它的**不是我的记忆,是 `git status` 打出 `M` 而不是 `A`** —— 我以为在新增文件。
已 `git checkout HEAD --` **完整还原**,我的版本留在容器外(`/tmp`),**不入库**。

- **命名惯例本身就是这个坑的形状**:`<id>_domain.py` 是全队约定,于是「我要为 id X 写工具」
  和「X 的工具已经存在」**必然重名**,而 `Write` 对已存在文件不报警只覆盖。
  **动手前先 `ls tools/batch_test/behavioral/ | grep <id>`**,已写进章程。
- **代价核算**:既有工具比我的**好**——它有 27 例自检(我 6 例)、
  `t<=240` 的归因窗口比我的「level<18 + 120s 无施法」**更干净**(它是纯结构的),
  而且它早就写着 slots 9+ 被排除。我这一轮**重新发现了它文件头里已经写好的东西**。
- **不是全部白费**:两条真正新的事实见 §四,已作为**更正**写进那份既有工具的文件头
  (唯一一处 `bots/` 之外的代码改动)。

## 四、量具:两条新的事实(已写进既有工具文件头)

1. ⛔ **`tp_cdlen` 对「手上一张卷轴都没有」的英雄读 40.0。**
   既有文件头原本写的是 `resolveTP` 对「没有 TP」和「TP 不在冷却」都返回 `(0,0)` ——
   那是**良性的一半**。反例逐帧:`6d0c2a/20260908_212429_slot7` skeleton_king,
   `t=935.9…946.9` **十一帧 `tp_cdlen` 恒 40.0 / `tp_cd` 恒 0.0**,
   最后一次 `item_tpscroll` 施法在 **t=766.9**(176s 前)、其后无购买,
   而他 **t=947.1 自己买了一张**(t=947.2 死亡)⇒ 那一刻手上没有卷轴,字段却读 40。
   ⇒ **它在任何方向上都不是持有代理**;伸手去拿它的人会拿到一个**自信的错误答案**
   (本轮我就是那个人:一度把它当「单向可靠」写进自己的工具)。
   `tp_cd` 本身没坏:全语料 902,496 行里 **166,156 行 > 0**。
2. ⚠️ **`PURCHASE` 与 `ITEM`(施法)两个流对不上账。** 同局同英雄(上面那位 SK),
   `t<942.9` 内 `item_tpscroll` **购买 6 次 / 施法 8 次** ⇒ 由两流重建的充能会**变负**。
   至少一个流是漏的或重复计的。**任何用「买减用」重建充能的 TP 家族 (a) 核验都会静默继承**,
   而负充能不会有任何东西举手。**本轮未解决**,已开 issue(§六)。

**这两条不否决 §2.2 的读数**:那个读数**不依赖持有量**(它靠 `t<=240` 的结构窗口)。

## 五、⭐ 42 个 armed id 里 26 个的条件 (a) 是空的

口径:`grep 'VERIFY id=…'` **扫全部 933 份报告**(batch-desk / director / hero /
replay-check / strategy 五个目录,不只是本组的)。

- **从未出现过任何 `VERIFY` 行**(20):`tpcommit, lf_rescue, ownhalf, overchase, cmrguard,
  blinkflee, liondrainstop, pullcamp, fieldbuy, pulllane, pulldrag, tpgap, campsel, tbearly,
  campfarm, abilanc, pullthink, aimguard, campvoid, roamidle`
- **只有 `episodes=0` 的记录**(6):`odaoe, stayfield, stayfield2, rotscope, slotdust, arbheart`
- 本轮 `tpdeathbuy` 从第一组划走(它此前也是零)。

⇒「条件 (a) 是两周零 promote 的唯一堵点」这句话**现在有分母了:26/42**。
这是本组自己的取货单,不开 issue,写进章程当下轮起点。
**取舍如实登记**:本轮深查只覆盖 1 个 id,**低于章程 6 局下限** —— 预算被
「从零重写一份已经存在的工具」吃掉了(§三);
上轮欠的 `cd3359`/`40e63a` 补扫与 transit 列钉帧
(`0eb22d/20260908_094909_slot6` t=1191.5)**连欠三轮**。

## 六、本轮的产物

- **代码**:`tools/batch_test/behavioral/tpdeathbuy_domain.py` 的**文件头更正**
  (§四两条,带帧锚)。**没有新增工具文件,没有改任何 `bots/` 文件。**
- **issue**:新开 **GH #647** [harness](TP 持有量在 dump 里无可靠投影 + 两流对不上账,带复现)。
  草稿 `claim_precheck.sh` **EXIT=0**,**先 push 后发表**(GH #290)。
- **章程**:加「动手写 `<id>_domain.py` 前先 grep 同名工具」这条坑。

## 七、铁律 6

`.githooks/pre-push` 随 push 自跑静态门,读数见文末追加。
动态半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)**未跑,不声称**;
本轮**未改任何 Lua 文件**。既有 `tpdeathbuy_domain.py` 的自检本轮跑过:
`ORIG_SC_EXIT=0`,`27 PASS / 0 FAIL`;语料跑 `ORIG_RUN_EXIT=0`。

## 八、下一轮第一件事

1. 从 §五 的 20 个空白 id 里挑**已有 domain 工具**的两个(`campsel_domain.py` /
   `campvoid_escape.py` 都在库里)—— **先 `ls | grep`,不要再重写一遍**。
2. 补扫 `cd3359`/`40e63a` + transit 列钉帧(**欠三轮**)。
3. W59 若已发波:先跑 `wave_throttle.py` 读解锁行再收。
4. `tpdeathbuy` 进条件 (b) 时要告诉批测台:它是**加法**杠杆(多花钱买 TP),读经济四量时要知道。

## 九、铁律 6 读数(push 时自跑)

`git push -u origin claude/lucid-pascal-kuy1q4`:`luacheck bots game: 0 warnings` /
`GATE_EXIT=0  CLEAN (iron rule 6 static half passed)`,**无 `RULE6_BYPASS`**。
`git push origin HEAD:main`:`012e994a..c5adb57b HEAD -> main`,
py gate `86 ran, 0 findings, 0 uncertifiable, 13.7s`
(**它自己声明不包括** 30 个较慢的 python 测试与 Lua 动态半 GH #124)。

## 十、token 用量

```
TOKENS total_in=14,003,015 out=82,230 turns=93
```
