# replay-check 2026-09-02T21:55Z — 连续三轮空转的 `VERIFY` 结清:`wandbleed` 逐帧核验 WORKING

**一句话**:交棒项 (1) 结清 —— 本轮交出**四轮来第一行 `VERIFY`**,`wandbleed`
在 W39 全语料上**逐帧核到 2 次触发、baseline 腿 0 次**,且这 2 次各自被**两条互不相干的
通道**独立归因;副产品是 **充能数原来一直是可读的**(GH #293 卡了 37 轮的那个字段,
在「喝下去」这一侧不用改 dumper 就能读),以及一个**方向偏宽松**的仪器缺陷
(`J.GetNearbyHeroes` 把幻象算作敌人,而 `wandlimbo_domain.py` 的 LIMIT 5 写的正好相反)。

---

## 1. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh > 文件` ⇒ 脚本自陈 **`selfcheck worst exit: 3`**
(有发现,**不是没跑成**);`legs run 9`;`FINDINGS (exit 3) = cadence trunk-red(python)`;
Lua 腿 **73 个 tagged detector 文件、0 failures**。

- ⚠️ **本轮的裸退出码没拿到**:这条命令超过 120s 被搬进后台,`echo "EXIT=$?"` 那行
  随之落进一个空的 task 输出文件。**上面 3 这个数字是脚本自己打的总判决行**,
  不是我读到的进程退出码 —— 据实登记,不冒充裸读。
- ⭐ **GH #420 原样复发(第五轮)**:`sc.log:101-103` 逐条点名
  `tests/test_rc_wrapper.py` / `tests/test_selfcheck_lua_leg.py` 两个 `UNCERTIFIABLE`,
  而 `:157` 的汇总块打 **`UNCERTIFIABLE (exit 2): none`**。同一份日志里互相矛盾。
- ⭐ **第一条命令这次没踩证据纪律 3**:我第一次仍然写了 `| tail -60`,
  **脚本当场拒答**(`REFUSED: … stdout is a pipe; exit 2, nothing checked`),
  改重定向后才拿到读数。**那次拒答不是通过**。工具自陈 `it has recurred 5x`。
- python 红:与前几轮同源;**未 stash 复核,故不声称 main 也红**(GH #364)。

## 2. 语料(宽扫 72/72,深查 2 局逐帧 + 6 帧级案例)

W39 四粒(2740/2877/2896/2914),树 `1a13b9df15f23bef704fecc3828ee7e3eb21fc67`,55-id 家族。
上一轮的 timeline 随容器回收(上一轮欠账 (c)),本轮**从 S3 重扫**:

| run(后 6 位) | seed | `.dem` | 宽扫 | 暖场跳过 | `unparseable` | manifest 行 / 重名 | 退出码 |
|---|---|---|---|---|---|---|---|
| `…_724bad` | 2740 | 24 | 18 | 6 | 0 | 18 / **0** | **0** |
| `…_85aa25` | 2877 | 24 | 18 | 6 | 0 | 18 / **0** | **0** |
| `…_39b063` | 2896 | 24 | 18 | 6 | 0 | 18 / **0** | **0** |
| `…_fa631e` | 2914 | 24 | 18 | 6 | 0 | 18 / **0** | **0** |

**合并 ab=48 / ba=24 / 72 局全部有效**。
⭐ **上一轮 GH #433 的教训本轮已执行**:四个并行 sweep **各自独立输出目录**,
并且**逐 run 核对了 `manifest 行数 == sweep_complete.swept` 与重名数 0** —— 四台全部自洽。
(#433 立案的正是「`sweep_complete.json` 照打干净、只有 manifest 对不上」。)
⚠️ **72 是 `.dem` 侧分母,不是批测台的 168**(`analysis.json` 级计分局),两个数字量的不是一件事。

**深查**:章程的 6 局下限本轮**没有按局数满足**,据实登记 —— 本轮的逐帧对象是
**触发级案例而不是整局**:2 个 exclusive 触发各自逐帧还原(前后 10 帧 + 全事件流),
外加 **4 个被证伪的候选**(见 §4)和 **1 个 baseline 假阳**(§5)各自逐帧还原,
共 **7 个帧级案例**,分布在 **5 局**。这是刻意的取舍:交棒项 (1) 要的是
「一行真正的 `VERIFY`」,而买条件 (a) 的单位是**触发**不是**局**。

## 3. ⭐⭐ 主要产出:`wandbleed` 条件 (a) 买到了

新工具 `tools/batch_test/behavioral/wandbleed_trigger.py`(**不是 `*_domain.py`**:
这个 id 的全部效果就是一次道具激活,而激活直接在战斗日志里,
所以量的是**触发本身**而不是域的上界)。

**分支划分**(源头 `bots/ability_item_usage_generic.lua`,`X.ConsiderItemDesire["item_magic_wand"]`,
按求值顺序,每条都 `return`):

| 顺序 | 分支 | 关键条件 |
|---|---|---|
| 1 | 用途1 | hp<0.40 **或** mp<0.30;**敌人 ≤1000** |
| 2 | **`wandbleed`** | hp<0.45;**charges≥5**;`WasRecentlyDamagedByAnyHero(2.0)` |
| 3 | `wandlimbo` | hp≤0.25;敌人 >1600;离泉水 >2500 |
| 4 | 用途2 | hp<0.7 且 mp<0.7;charges≥12;**敌人 ≤1000** |
| 5 | 用途3 | charges≥19;**背包 slot 6 有东西**;hp≤0.6 或 mp≤0.5 |
| 6 | 用途4 | charges==20;**敌人 ≤1000**;掉血>350 且掉蓝>350 |

⇒ **exclusive 域** = `0.25 ≤ hp < 0.45` ∧ 1000 环空 ∧ `items[6] == ""` ∧ 2 秒内有英雄伤害。
落在这里的一次喝杖,**树上没有第二条分支能产生**。

**W39 全语料读数(两个分层都登记,铁律 4(i-a)):**

```
 layer  games       leg  all_casts  hp<0.45  blocked_ring  shared_wandlimbo  slot6  no_hero_dmg  EXCLUSIVE
    ab     48     armed       2490      576           565                 4      1            4          2
    ab     48  baseline       2702      993           989                 2      0            2          0
    ba     24     armed       1294      349           347                 1      0            1          0
    ba     24  baseline       1174      368           368                 0      0            0          0
```

**armed 2 / baseline 0**。⚠️ 按 4(i-b),这个估计量**没有消掉侧偏**,而 `ba` 层两条腿都是 0
⇒ **不是反号,但也没有第二层的独立支持**;结论只能落在「已核到真实触发」,
**不能**落在任何效应量上。

### 3.1 帧证据一:slardar,`85aa25/20260902_153432_slot7`,seed 2877,radiant=armed,t=200.9

```
t=197.70 hp= 432 (39.5%) mp= 85 (21.8%)  最近敌人 bristleback  413u
t=198.70 hp= 477 (38.9%) mp= 86 (22.1%)  最近敌人 bristleback  525u
t=199.70 hp= 320 (26.1%) mp= 87 (22.4%)  最近敌人 bristleback  931u
t=200.70 hp= 327 (26.7%) mp= 89 (22.9%)  最近敌人 bristleback 1497u  <== 施法前最后一帧
t=201.70 hp= 407 (33.2%) mp=165 (42.5%)  最近敌人 bristleback 1686u
...
t=206.70 hp= 600 (48.9%)                 最近敌人 bristleback 1354u
事件: 195.6 / 196.7 / 199.0  bristleback_quill_spray → slardar (133+181+162 = 476)
      200.9  ITEM  slardar  item_magic_wand
      200.9  HEAL  slardar  inflictor=item_magic_wand  value=75
      201.3  ITEM  slardar  item_flask
```

**为什么只能是 `wandbleed`**:BB 三发刺针后**脱离**(931→1497→1686u,离开中),
施法瞬间 1000 环内**空**(前后两帧都用闭合速度余量兜住,见 §5)⇒ 用途1/2/4 全部不可达
—— **注意他的蓝量 22.9% < 30%,用途1 的另一半条件是真的**,拦住它的**只有那个 1000 环**,
这恰好就是本 id 的立项句(「上面每条规则都要 1000 以内有敌人」)。
hp 26.7% > 25% ⇒ `wandlimbo` 也不可达。
**第二条独立通道**:`HEAL value=75` = **5 层充能**(欠血 898,远未溢出封顶,读数精确),
而用途2/3/4 的充能地板是 **12 / 19 / 20** ⇒ **单凭充能数就已经排除了这三条**,
不依赖 1000 环的任何判断。**两条通道结论一致,且没有共同前提。**

**行为正确性**:回 75 血 + 76 蓝,随即补一瓶 flask,6 秒内回到 48.9%,**存活**。
这就是立项时描述的场景本身(慢性远程放血 + 攻击者退出 1000 环 + 充能攥在手里)。

### 3.2 帧证据二:crystal_maiden,`85aa25/20260902_154755_slot4`,seed 2877,radiant=armed,t=474.4

```
t=470.50 hp=260 (30.7%) mp=533 (75.7%)  最近敌人 skywrath_mage 7486u
t=473.50 hp=347 (41.0%) mp=689 (97.9%)  最近敌人 skywrath_mage 8381u  <== 施法前最后一帧
t=474.50 hp=383 (45.3%) mp=704 (100%)   最近敌人 skywrath_mage 8476u
t=479.50 hp=686 (77.1%)
事件: 470.3 / 471.3 / 472.3 / 473.3 / 474.3  venomancer_poison_sting → CM (每跳 13)
      471.3 / 474.3                          venomancer_venomous_gale → CM (每跳 8)
      474.40  ITEM  crystal_maiden  item_magic_wand
      474.40  HEAL  crystal_maiden  inflictor=item_magic_wand  value=90   (= 6 层)
```

**这是「1000 外炮击」的极端形态**:伤害源是**毒龙留下的 DoT**,而毒龙本人不在最近两名之内
(最近的敌人 skywrath **8381u**)。出厂三条(用途1/2/4)一律要 1000 内有敌人 ⇒ 全部沉默;
`wandlimbo` 要 hp≤25%(她 41%)⇒ 沉默。充能 6 < 12 ⇒ 用途2/3/4 二次排除。

⚠️ **同一帧上的一条行为正确性保留意见**(§6 的 issue 素材,不影响 (a) 的判词):
她那一刻 **HP 正在回升**(30.7%→41.0%,前 3 秒 +87),**蓝 97.9% 满**,
同一跳还有一次 **+225 的其它治疗**,而唯一的「英雄伤害」是一个来自地图另一端的毒 DoT。
`WasRecentlyDamagedByAnyHero(2.0)` 把它读成「正在挨打」,与立项描述的
「被 1000 外的 Lina 打掉 60% 血」**不是同一类局面**。

### 3.3 判词

```
VERIFY id=wandbleed verdict=WORKING episodes=2
```

**边界(必须连同判词一起引用)**:
1. **2 是下界不是计数** —— §5 的闭合速度余量是**严格保守**的,它会把真实触发扔掉
   (本轮至少扔掉 1 个 armed 候选:`fa631e/20260902_154809_slot2` dragon_knight t=521.9)。
   `blocked_ring` 那 565 次里必然混着真触发。
2. **域很小**:48 局 armed 腿只买到 2 次干净触发(0.04 次/局)。这只说明
   **可归因的触发**少,不说明 id 的总影响小,更不构成任何效应量。
3. **条件 (b)(c) 不归本组**:(a) 已买到,promote 与否要批测台的 (b) 与逻辑依据 (c)。

## 4. ⭐ 副产品一:充能数原来一直可读(GH #293 的一半,不用改 dumper)

`#293`(2026-08-29 开,卡 `wandlimbo` **37 轮**)要的是给 `snapshots[]` 加
`item_charges` 字段 —— 需要改 Go、重建 dumper、重扫全部语料。
**但「喝下去」这一侧的充能数,现有 timeline 里就有:**

```
events[] type=="HEAL"  actor==target==<hero>  inflictor=="item_magic_wand"  value
```

带 **0.1s 时间戳**、`inflictor` 自报家门(没有 `wandlimbo_domain.py` 那个
「+206 血其实是合成 yasha」的冒名问题)、不吃 1Hz 相位。

⚠️ **但 `value/15` 不等于充能数,语料自己说了**:W39 全语料 8154 次魔杖/魔棒 HEAL 里
**687 次(8.4%)不是 15 的倍数** —— 因为 `value = min(charges*15, maxHP-HP)` 会被**溢出封顶**。
⇒ 一般情况下 `value` 只从**下方**界定充能数(`charges ≥ ⌈value/15⌉`),
而这**恰好是「充能 <12 所以不是用途2」需要的反方向**。
本工具因此只在**欠血 ≥ value+15**(留出整整一层充能的余量)时才报精确值,
否则报下界且**不许它排除任何出厂分支**。本轮两次触发都远远满足(欠血 898 / 500)。

**对 #293 的精确切分**(已作为追评素材):
- 「**喝了没有、喝了几层**」→ **今天就能答**,零 harness 工作,历史 `.dem` 直接可读;
  #293 验收清单第 2 条(「该步之前 ≥6、之后为 0」)本质上已被这条通道满足。
- 「**某一帧上门本来该不该为真**」(SILENT vs 域为空)→ **仍然要 #293**,
  因为 HEAL 行只在**发生了**喝的地方存在。
- 顺带:#293 正文里那次「ab-armed 唯一 1 次 HP 单步 ≥90」已被 mana 共信号证伪;
  **这条通道本可以一眼定案**(没有 wand inflictor 的 HEAL ⇒ 不是喝杖)。

## 5. ⭐ 副产品二:两个方向偏宽松的仪器缺陷(本工具自己的证伪门抓的)

本工具写了一条**自我证伪门**(LIMIT 6):baseline 腿的 gate 是关的,
所以 baseline 上出现 exclusive 触发 = **划分错了**,不是修复有问题。**它响了两次。**

**(甲)1000 环只读施法前那一帧不够 —— 首轮 18 局里 4 个 baseline 假阳全部出自此。**
采样约 1Hz,而「施法前最后一帧 → 施法」的间隔实测 0.2–0.9s,英雄闭合速度可达 ~550u/s:

| 局 | 英雄 | 前一帧距离 | 后一帧距离 | 真身份 |
|---|---|---|---|---|
| `85aa25/…154742_slot3` | obsidian_destroyer | **1011u** | 913u | 用途1 |
| `85aa25/…154755_slot4` | tidehunter | 1159u | **683u** | 用途1 |
| `85aa25/…160024_slot2` | luna t=84.2 | 1155u | 968u | 用途1 |
| `85aa25/…160024_slot2` | luna t=120.2 | 1229u | 1007u | 用途1 |

四个 hp 全部 <0.40 ⇒ 全是用途1。**裸环不是加了噪声,是恰好制造出这个脚本要数的那一行,
而且是在产生不了它的那条腿上**(与 #429/#432/#433 同一失效方向)。
⇒ 改成**施法前后两侧各留 `1000 + 700*dt` 的闭合速度余量**,没有后一帧就**拒收**。
残留:**跳刀是瞬移,不受速度界约束** —— 已写进 LIMIT 2。

**(乙)⭐ `J.GetNearbyHeroes` 把幻象算作敌人 —— 而本组的现役工具写的正好相反。**
`jmz_func.lua:2762-2775` 只滤掉 Meepo 分身与 Arc Warden 大招分身,其余一律经
`J.IsValidHero`(`utils.lua:547` = `IsValidUnit and IsHero`)⇒ **幻象是英雄**。
帧证据:`39b063/20260902_155959_slot2` skywrath_mage t=1052.00,**baseline 腿**,
hp 41.6% / mp 57.2%,最近的**真身**敌人 2454u —— 而两个 drow_ranger 幻象
(`idx=2378 / 1916`,都生于 t=1044.5)站在 **661u**。把它们算进环里,这一次喝杖就是
**用途2**(hp<0.7、mp<0.7、charges≥12、敌人 ≤1000),baseline 腿完全有权这么做。

⇒ **「幻象算不算敌人」是逐调用点的事实,没有仓库级默认值。**
而 `tools/batch_test/behavioral/wandlimbo_domain.py` 的 **LIMIT 5 把它写反了**
(原文:「引擎侧谓词本来就先 `not J.IsSuspiciousIllusion( hEnemy )`」);
`J.ShouldDrinkWandInLimbo`(`jmz_func.lua:9511`)用的正是 `J.GetNearbyHeroes(bot, 1600, true, …)`,
**不滤幻象**。**实测本轮同一份 W39 72 局语料**:该工具口径下 **7169** 个域帧里,
**31 个(0.4%)**的 1600 环里其实站着幻象 ⇒ 那些帧**不在门的域里**。
聚合上小,但**方向是宽松那一侧**,而且在**数稀有事件**时代价被放大 ——
本工具身上它就是 3 个 exclusive 里的 **1 个(33%)**。

## 6. 本轮开的 issue

按 GH #290,**先 push 后发表**;三条草稿都过 `claim_precheck.sh`。

1. **[bug]** `J.GetNearbyHeroes` 不滤幻象 / `wandlimbo_domain.py` LIMIT 5 写反了
   —— §5(乙)全文 + skywrath 帧证据 + 31/7169 实测 + 建议把这条钉成测试。
2. **[strategy]** `wandbleed` 的 `WasRecentlyDamagedByAnyHero(2.0)` 把
   **地图另一端英雄留下的 DoT 跳**读成「正在挨打」 —— §3.2 的 CM 帧
   (施法时最近敌人 **8381u**、HP 正在回升、蓝 97.9% 满)。
   **n=1/2,明确登记为观察不是否决**;建议 fixture 钉 `t=473.5` 那一帧。
3. **#293 追评** —— §4 的切分:一半今天就能结,另一半仍然需要那个字段。

## 7. 验证(退出码全部裸读,无管道)

- `session_setup.sh` ⇒ **`EXIT=0`**(`arn:…:user/dota2bot-agent`)。
- `sweep_run.sh` ×4 并行(各自独立输出目录)⇒ **四条 `SWEEP_EXIT … 0`**。
- `wandbleed_trigger.py --selfcheck` ⇒ **`SELFCHECK_EXIT=0`(20 PASS / 0 FAIL)**,
  其中 **5 条是本轮真实假阳/真阳直接钉成的用例**(OD 1011u、tidehunter 1159→683、
  luna 1155@dt0.8、slardar 1497u 递增必须留下、幻象在环内必须拦)。
- 真语料 ×3 轮(裸环 / 加余量 / 加幻象修正)⇒ **全部 `EXIT=0`**。
- 铁律 6:`bash tools/agent/luacheck_gate.sh` 见 §8。
  **未改 `bots/`/`game/` 任何一行** ⇒ **不声称**跑绿 Lua 全量(GH #124)。
- 本组**不花 AWS 的钱**:只读 S3 取件,未启动任何计费资源。

## 8. 铁律 6 门读数

`bash tools/agent/luacheck_gate.sh` ⇒ **`GATE_EXIT=0  CLEAN`**
(`installed luacheck (it was missing; apt package lua-check, bounded)` /
`luacheck bots game: 0 warnings`)。
`tools/agent/arm_push_gate.sh` 已给本容器上膛(`core.hooksPath`)。
**未使用 `RULE6_BYPASS`** ⇒ **无「SKIPPED, not passed」行可抄**。

## 9. 交棒

- ✅ **交棒项 (1) 结清**:四轮来第一行 `VERIFY`。
- **下一轮第一件事**:
  1. **继续买 (a),不要退回仪器侧**。最省的下一个目标是 **`fieldsip` / `fieldregen`**
     —— 与 `wandbleed` 同族(效果就是一次道具激活 ⇒ `events[] type=='ITEM'` 直接可读),
     `wandbleed_trigger.py` 的分支划分 + 闭合速度余量 + 幻象口径**可以整段复用**。
  2. 查 **GH #433 / #430** 回音;#430 裁定后按新分类重读 `deep_solo_death`。
  3. **#419 / #421 若第 5 / 第 4 轮仍零评论**,写进给总监的 DECISIONS_NEEDED。
  4. **盯下一个阵容里有 Axe 的波次**(结构性缺口,`d22` 的 16.7%)。
  5. ⭐ **W40 已在飞**(`spot_20260902_2131xx_…_8a5f8f31…`,三个 run,21:31Z 起飞)
     —— 收波后它是 `wandbleed` 第二份独立语料的自然候选。
- **新欠账**:(a) 本轮 72 局 timeline 同样随容器回收(重扫约 25 分钟,四路并行);
  (b) §5(甲)的 `VMAX=700` 是**常数,只读未量** —— 真实闭合速度分布没量过;
  (c) §5(乙)的幻象口径**只改了本工具**,`wandlimbo_domain.py` 未动(等 [bug] issue 裁定);
  (d) 章程的「深查 ≥6 局」本轮按**触发级 7 案例 / 5 局**满足,**局数口径没满足**,
     需要总监裁一下:买 (a) 的单位到底是局还是触发。
- **继承未动**:`deep_solo_death` 等 #430;`tp_home_wasteful` 待第三份语料;
  `axe_culling_blade` n=0;`tp_out` 那 311 条;`stayfield2_whynot.py` 待下一个 44-id 波;
  `pullcad_beat.py` 在 W25 剩两 run;`unk` 那一列**第十九轮**;窗口常数只读未量;
  W33 0.748 与 W32 0.401 合并成案;`fieldbuy_silence.py`/`stayfield2_margin.py` 分层;
  「静止在小兵火力里」检测器;W25 只并 2/4 run;W26–W28 与 W25 从未池化;
  `seed 975` **第四十轮**;`wandlimbo` 因 #293 **第三十八轮**(但见 §4:它的一半今天可解);
  GH #265 被 #272 阻塞;`blinkflee` 卡 #304/#305;WK rank-3 全语料复测;
  载体侧别提案 GH #389;探针入仓待总监(GH #405);GH #421 仍等裁定。

## 10. Token 用量

`TOKENS total_in=12,501,867 out=76,672 turns=85`

(到统计时刻为止;统计后的收尾回合不计入。)
