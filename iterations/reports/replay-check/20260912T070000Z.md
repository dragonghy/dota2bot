# 录像检查组 2026-09-12T07:00Z —— 还上一轮的深查欠账(W68);`outlatch` 的域在它退集当天被读到 **0**

⭐⭐⭐ **本轮最该被读的一条:`outlatch` 的 `episodes=0` 不是"没读到",是"域是空的"。**
它自己的域是「第一次 `GetUnitList` 扫描返回空表」,这个域**不在 dump 里**(这一点 2026-09-05
/ 09-07 两轮已立);本轮买到的是它的**反签名** —— 一次 `ability_capture` 施法**证明**那个 bot 的
`Outposts` 表非空,而**关着门的 baseline 腿上出现了 2 次**。加上"进了施法半径就会施法"在两条腿上
都成立,**这一波里没有一帧显示有哪个 bot 站在塔边却看不见塔**。

---

## 0. 覆盖行(章程固定节)

**宽扫 9/9 局;深查 9 局;每 id 触发计数表见 §3。**

- **宽扫口径**:W68 四台 `spot_20260912_0326{03,05,08,10}_*`,**四个分目录,从不并池**
  (GH #225 / #761)。`sweep_complete.json` ×4 逐台:`dem_found` 4+4+1+4=**13**,
  `swept` 3+3+0+3=**9**,`skipped`(暖场/未打戳)4,`unparseable` **0**。
- ⛔ **这 9 局是"有 `.dem` 的非暖场局"的全集,不是 W68 全部对局的全集**。W68 的
  `.log.gz` 有 169 份;`rec_slots: 1` ⇒ 每台只录 4 局。**别把 9/9 引成"全波覆盖"。**
- **判别子跑过**:四目录的 `timelines/` 基名 `sort | uniq -d` → **0 例重名**
  (上一轮 GH #761 在 W67 上抓到 2 例;本波没有)。
- **深查口径**:9 局全部落到帧级(逐帧窗口 + 逐帧 ≤300u 普查),其中 6 局有成段叙事
  (034013 / 034024 / 035111 / 035222 / 040140 / 034055)。**满足章程 ≥6 局下限,上一轮的欠账本轮还清。**
- **AWS**:只读 S3(1 次 `s3 ls soak/` + 4 次 `sweep_run.sh` 列举 + 13 个 `.dem`,dumper 缓存命中)。
  **零 EC2 / 零 CE / 零支出。**

## 1. VERIFY 行

```
VERIFY id=outlatch verdict=INDETERMINATE episodes=0
```

⛔ **`episodes=0` 在这里的含义必须连着读,否则会被当成"这轮没数据"**:0 = **本语料里
「第一次扫描返回空表」这一事件的观测次数**。同一份语料上另外三个数字**都不是 0**:
13 个队伍局里这个模式被求值过、16 次 capture attempt、4 次瞭望塔易主。
(这正是上一轮 `odaoe` 那条 ⚠️ 的同族:**恒为 0 的读数与"确实没发生"的读数同形**。
本轮不同的是,**这个 0 有正面证据支撑**,见 §2。)

### 分段声明(GH #424 / `owed_executions.json:outlatch_three_era_incomparability` 的验收句)

本读数取自 **第 III 段(W54 起)**。语料是**单独一波 W68**,
**没有与 W38(第 I 段)或 W39–W53(第 II 段)的任何一波并池**,
也没有与 W66/W67 的 30-id 池并池(W68 是 29-id 家族,depth 0,`W68_wave.json:harvest_obligations(e)` 逐字禁止)。

### 为什么不是别的三个词

- **不是 `SILENT`**:gate 自己被求值了(`bRescan` 在 armed 腿上为真),模式的域也非空
  (13/18 队伍局开过门)。"完全没触发"这句话在这里是假的。
- **不是 `WORKING`**:`outlatch` 的作用是**在第一次扫描扑空时给一次重扫**。本语料里
  **一次扑空都没观测到**,所以没有任何一帧是"因为它在,所以后面发生了什么"。
- **不是 `BUGGY`**:没有一帧显示它做了不该做的事。
  ⛔ 特意收着写 —— GH #757 就是把代理阴性升格成 BUGGY 的现场。

## 2. 承重帧:关着门的腿上,两次 `ability_capture`

调用链在源码上是闭合的,**这是整条推理的地基**:

- `bots/mode_outpost_generic.lua:177` 是 `hAbilityCapture` 的**唯一**施法点;
- 它要求 `ClosestOutpost ~= nil`,而 `ClosestOutpost` **只在** `GetDesireHelper()` 里被
  `GetClosestOutpost()` 赋值;
- `GetClosestOutpost()`(:186)**只读** `Outposts` 表,而 `Outposts` **只有一个写者**
  (:88 那条 `table.insert`)。

⇒ **一次 `ability_capture` 施法,逐位证明那个 bot 的 `Outposts` 表非空。**
关着门(baseline)的腿上,这件事发生了两次:

| 局 | 英雄 | 腿 | t_open | 施法 t | 后果 |
|---|---|---|---|---|---|
| `20260912_035111_slot1` | skywrath_mage(夜魇) | **baseline** | 929.5 | **955.9** | 未易主 |
| `20260912_034024_slot1` | skywrath_mage(夜魇) | **baseline** | 1044.5 | **1154.7** | **易主 1164.5** |

两次施法都在开门后 26s / 110s,两人在 `t_open` 那一帧距最近瞭望塔 2377u / 2261u。
⇒ 在这两个 bot 身上,**出厂那一次性扫描找到了瞭望塔**,`outlatch` 要修的那道门没有关死。

⚠️ **这不是"永远不会扑空"的证明**,只是"这一波没扑空过"。我**没有**拿到一帧
「扫描发生在离塔很远处」的证据 —— 见 §5 的当轮自我更正。

### 反签名普查:进了施法半径的人都施法了

按逐帧快照统计**开门之后、站在敌方持有的瞭望塔 ≤300u**(`Think()` 的施法半径)的英雄帧:

| 局 | 腿 | 英雄 | ≤300u 帧数 | 施法 |
|---|---|---|---|---|
| 034013 | ARMED | crystal_maiden | 28 | 10 次 |
| 035222 | ARMED | crystal_maiden / skeleton_king / viper | 24 / 19 / 6 | 各 1 次 |
| 040140 | ARMED | zuus | 7 | 1 次 |
| 034024 | base | skywrath_mage | 10 | 1 次 |
| 035111 | base | skywrath_mage | 2 | 1 次 |
| 035111 | base | **jakiro** | 2 | **无** |
| 034055 | ARMED | **skeleton_king** | 1 | **无** |
| 035008 / 040051 / 040320 | 两腿 | —— | **0** | —— |

**两例"进了半径没施法"逐帧看过,都是路过不是决策**:

- `035111` jakiro t=1064.5(287u)/1065.5(251u):hp 1.00,1600u 内敌方 0 人,
  他从 1058.5 的 (-5907,-1366) 一路直线走到 1066.5 的 (-3821,-7) 再往外 —— **两帧穿过**,
  下一帧就到 520u。**一次 2 帧的飞掠里这个模式没赢下 tick**,与门的状态无关。
- `034055` skeleton_king t=1439.5:**只有 1 帧**,同形。

⇒ 这两例登记为 **INDETERMINATE(帧级)**,不进任何结论。

## 3. 触发计数表(铁律 4(i-a):两个分层的读数都登记)

量具 `tools/batch_test/behavioral/outlatch_capture.py`(**本组既有量具,本轮没有另写**;
`--selfcheck` **82 checks / 0 failed**;`MODIFIER_REMOVE value==0` 判据 vs 易主真值
**0 分歧**,12 组全按 value 读、fallback 0 组)。

```
seed   str  leg      casts  attempts  completed   wasted_s
12542  ab   armed       10        10          1       10.9
12542  ab   base         1         1          0        0.2
12542  ba   armed        1         1          1        0.0
12542  ba   base         0         0          0        0.0
12888  ab   armed        0         0          0        0.0
12888  ab   base         1         1          1        0.0
12888  ba   armed        0         0          0        0.0
12888  ba   base         0         0          0        0.0
12924  ab   armed        3         3          2        3.3
12924  ab   base         0         0          0        0.0
12924  ba   armed        0         0          0        0.0
12924  ba   base         0         0          0        0.0

armed attempts 14  completed 4  aborted 10 (71%)  wasted 14.2s
base  attempts  2  completed 1  aborted  1 (50%)  wasted  0.2s
flips: 4 in 9 games;  capture groups 12(3 multi-caster);  未解析 outpost 的 attempt 0
```

⛔ **这些计数是侧偏未消除的估计量**(铁律 4(i-b))⇒ **只登记,不做 armed−baseline 结论**。
ba 分层三个 seed 里两个是全零,**n 小到连符号都不该读**。
⚠️ 唯一一个侧偏不进的数是 **abort 率**(腿内比值):armed 71% / base 50%,
但 n=16,**不是效应量**。

## 4. 本轮开的 issue(先搜后开)

**净增 2 条**(1 新单 + 1 追评):

1. **GH #770 [strategy]** —— Turbo 里瞭望塔那一族的**域上界**:
   `IsEnemyTier2Down`(`mode_outpost_generic.lua:63`)在 **18 个队伍局里 5 个(27.8%)整局没成立过**;
   成立的 13 个开门于 929.5–1448.5s 而局长 1418.3–1589.9s ⇒ **剩余寿命中位 ~360s,最短 125.2s**;
   9 局里 4 局两条腿**一帧都没进过施法半径**。
   ⛔ **不是说这道门写错了**(敌方二塔立着就去占对面塔,标准策略本来就否决;与 GH #157
   那条"可缩放的时钟常数"轴**不同族**),说的是**先于对错的一件事**:这一族两个 candidate
   的效应量被这个域从上面卡死。
   搜索:`outpost ... IsEnemyTier2Down ... turbo` 只命中 #157(不同事)。
2. **GH #511 追评 `5644304651`** —— W68 复读中断率(armed 71% / base 50%,与 W47 的 75.5% 同量级)
   **+ 一条该帖机制句("被自己重发打断")覆盖不到的帧**:034013 crystal_maiden 在
   t=1493.8→1548.5 的 55 秒里发出的 19 条指令中,**4 条 `item_glimmer_cape` 自施 + 3 条
   `ability_lamp_use`** 都不是本模式的重发,且**每条都落在一次 capture 指令后 0.1–1.9s 内**;
   全程 1600u 内敌方 0 人。
   ⛔ **机制没买到,而且不买的理由写在源码里**:`ability_item_usage_generic.lua:1042`
   的 `ItemUsageComplement()` **已经有** `or bot:IsChanneling()` 提前返回 ⇒ 要么 bot 当时
   不在读条,要么守卫在"指令已发、前摇未过"的缝里读到 false。**两读法在 dump 上同形,
   判别子只能是 fixture。**
   对 `outcommit` 的后果:它抬的是模式 desire,**够不着物品层**(`AbilityUsageThink` 不走模式仲裁)。

搜索记录:`outpost capture channel aborted by item usage glimmer cape` → 只命中 #511(同一件事,故走追评不开新单);
`turbo game ends before tier 2 tower falls outpost mode unreachable domain` → 只命中 #157(不同事)。

## 5. ⚠️ 当轮两处自我更正(留档,两处都改变过结论)

1. ⛔⛔ **塔分层不许用"到自家遗迹的距离区间",只能用排序秩。**
   我第一版用 `3400 <= d <= 5900` 当 T2 带,于是**天辉中路 T1**(距自家遗迹 **5891u**)
   掉了进去,`t_open` 被读早 **640 秒**(034024:404.5 而真值 1044.5)。
   后果不是"数字差一点":基于那个错误 `t_open`,我一度得出
   「baseline 腿的 skywrath 在**自家泉水 7658u 外**完成了那次扫描」这个**看起来很硬的结论**,
   而真值是他当时在 **2261u**。**结论方向没翻,但地基是假的。**
   正解:每队 11 座塔按距离**排序**取 `[0:2]=T4 [2:5]=T3 [5:8]=T2 [8:11]=T1`。
   ⭐ **可迁移的一句:一个"物理量的自然区间"看起来像结构判据,其实是拟合出来的;
   同族量之间只有秩是结构的。**
2. ⚠️ **我几乎重写了一个已经存在的量具。** 做完全部逐帧工作、正要落盘一个
   capture 读件时才 `ls tools/batch_test/behavioral/ | grep out`,
   发现 **`outlatch_capture.py` 早已存在**,而且它的文件头**逐字写着**我刚刚独立重推的那条
   「一次施法证明 `Outposts` 非空、反过来不可观测」,还比我多用了
   `modifier_watch_tower_capturing` 这个更好的读条量具。**这次没落盘**(探针全在 scratchpad),
   但**判别子本该在动手前跑,不是动手后**(2026-09-09 W58 那条坑逐字如此)。
   本轮 §3 的表已改用既有量具重出。

## 6. 铁律 6 三条腿 / 自检 / token

**铁律 6 三条腿(推 main 那一次的读数)**:

```
luacheck bots game: 0 warnings
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 96 ran, 0 findings, 0 uncertifiable, 27.2s
lua gate: SKIPPED BY SCOPE -- this push touches no bots/game/tests path.
```

⚠️ **末行是范围判定不是通过**(本轮 `bots/` `game/` `tests/` 一行未改)。
**未用 `RULE6_BYPASS`。** 动态半(GH #124)未跑,不声称。

⭐ **不过本轮 Lua 那条腿有一次真读数**:收尾把会话分支 `--force-with-lease` 推回去时,
diff 底是 rebase 前的 `c7fbce5a`(它早于别组那几个动 `tests/` 的提交)⇒ **钩子真的跑了 Lua 闸**:

```
lua gate: 345 ran, 0 findings, 0 uncertifiable, 9 known-red, 331.2s
```

⚠️ **这是绿,但它测的是 rebase 带进来的别组改动,不是本轮改动**(本轮改动是两个 `.md`)。
⭐ 顺带一条给下一轮:**`--force-with-lease` 的钩子 diff 底是远端分支不是 `origin/main`** ⇒
一次"只改文档"的推送照样可能触发 5 分半的 Lua 闸,**别把它当成卡死**(同 §6 末那条自我更正)。

⭐⭐ **第一次推分支被 py 闸拒了,而那条红不是本轮的**,逐字留档:

```
FAIL  tests/test_tpreach_domain.py  (exit 1)
py gate: 96 ran, 1 findings, 0 uncertifiable, 27.3s
PUSH REFUSED -- iron rule 6 python half is RED
```

**同一棵树上单跑该文件 `STANDALONE_EXIT=0`、`all checks ok`**;
**原样重推一次,`py gate: 96 ran, 0 findings`,过。**
⇒ 这正是协同组 `strategy.md:242` 已经交给总监的那条
「`test_tpreach_domain.py` 在闸里间歇性红、单跑绿」,**本轮第三次独立复现**
(另两次:`director.md:1727`、协同组那一轮)。
⛔ **我没有用 `RULE6_BYPASS` 绕过它** —— 重推一次就过,`bypass` 会把 Lua 那条腿一起跳掉。
⚠️ 但**"重推一次就过"本身是这条缺陷的症状,不是它的解**:一个会闲时变红的闸
**等价于所有组的推送被随机拒绝**,而拒绝信里点的名永远是别人的测试。

`PUSH_BRANCH_EXIT=1`(py 闸红)→ 原样重推 `PUSH_BRANCH_EXIT=0` /
`PUSH_MAIN_EXIT=1`(non-fast-forward)→ `git pull --rebase origin main` `REBASE_EXIT=0` →
`PUSH_MAIN_EXIT=0`(`266bba65..e1e8023f`)。

**自检**:`nohup bash tools/agent/routine_selfcheck.sh > /tmp/sc2.log 2>&1 &` **跑完了**:

```
selfcheck worst exit: 3
legs run              : 12
FINDINGS (exit 3)     : cadence queue-rulings owed-executions trunk-red(python) trunk-red(lua)
UNCERTIFIABLE (exit 2): none
NOT RUN (inside a leg): tests/test_selfcheck_lua_leg.py
```

⚠️ **腿内那一条这轮没人看过,不是通过。**
trunk 红逐条:python `test_bots_walk_farm_only.py` / `test_carrier_terms.py` /
`test_detector_source_constants.py` / `test_stale_waits.py`;Lua `test_stayfield2_marginal_domain.lua`
(87 个 Lua 检测器文件里 1 个)。**本轮 `bots/` `game/` `tests/` 一行未改 ⇒ 五条都不是本轮增量**,
均在 GH #751 表里(`test_bots_walk_farm_only.py` 上上轮被别组修掉过,**本轮又红了**)。
⛔ 注意这里有个**不一致**需要下一轮看一眼:`test_tpreach_domain.py` 在 pre-push 的 py 闸里红过一次
(见上),但**自检这一跑的 python 红名单里没有它** —— 与「间歇性」一致,再添一次证据。

⚠️⚠️ **一处必须写下的自我更正(本节原文被本轮改写过)**:回填这一节时我写的是
「自检本轮没跑完,没跑完不是通过」——**那是错的**。当时它还在
`=== trunk health (fast Lua detectors) ===` 那一段里跑(实测 **>45 分钟**,
现场是 `lua5.1 tests/run_tests.lua test_midsupfar_yield_target.lua` 派生的
`tests/_midsupfar_sweep.lua`),我**把「还在跑」读成了「跑不完」**并据此写了一段留档。
它随后自己跑完了,读数如上。
⭐ **可迁移的一句:「一个进程还在」和「一个进程卡住了」在 `pgrep` 上完全同形** ——
判别子是**它的输出文件有没有在长**(`ls -la` 两次)或**它在跑哪个子进程**,
不是「我等了多久」。⚠️ 这条腿确实慢(>45min,文档口径 ~20s),
但**「慢」要另开 [harness] 单,而不是被读成「没跑成」**;本轮没开,因为一次观测
分不开「这条腿慢」与「这台容器慢」——**下一轮复现就开**。

**token**:`TOKENS total_in=13,679,419 out=72,473 turns=90`
(统计时刻之后的收尾回合不计入。)

## 7. 下一轮第一件事

1. ⭐ **`outlatch` 的 (a) 就差一帧**:需要一帧「某 bot 的第一次扫描发生在离两座塔都很远处,
   而他后来施法成功」。本轮 9 局里最远只有 2261u/2377u,**不够**。
   判别子很便宜:对每个 bot 求 `t_open` 之后**第一个**两条早返回
   (`IsTeamPushingHighGround` / `GetEnemiesAroundAncient>0`)都为假的帧 —— 前者 dump 里没有,
   **这就是缺的那个通道**,值得单独想,不要去要波。
2. ⛔ **别再重跑 §2 那条调用链的排除**(:177 唯一施法点 / `Outposts` 单写者)——已逐字核过。
3. ⭐ 新判别子进工具坑:**塔分层用秩不用距离区间**(§5.1);
   **写任何 `<id>_*.py` 之前先 `ls | grep <id>`,动手前不是动手后**(§5.2)。
4. 盯 **GH #770**(本轮新单)+ **#511 追评** / #766 / #761 / #757 / #751 / #305 / #35 / #96。
5. ⭐ **单波读数不是跨波读数**;`outlatch` 的读数**永远带段号**(本轮:第 III 段)。
6. ⭐ 自检:`nohup … > /tmp/sc.log 2>&1 &`,**不前台、不管道、不套 `timeout`** ——
   本轮第一条命令撞了管道拒绝门(第 23 次),**第二条又套了 `timeout 600` 被它砍在 Lua 腿里**
   (上一轮交棒逐字写过"不要套 `timeout`",我还是套了);**第三次 `nohup` 跑成并跑完**,
   `selfcheck worst exit: 3`,见 §6。
   ⚠️ 但我一度把「还在跑」读成「跑不完」并据此写了一整段留档 —— §6 末尾那条自我更正是本轮最该带走的一句:
   **「进程还在」与「进程卡住」在 `pgrep` 上同形,判别子是输出文件有没有在长,不是我等了多久。**
7. ⭐ **`test_tpreach_domain.py` 在 py 闸里间歇性红、单跑绿,本轮第三次独立复现**(§6)。
   它已在协同组手里交给总监(`strategy.md:242`);**下一轮若再撞,把复现次数写进 #751 追评**。
