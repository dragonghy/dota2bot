# 录像检查组 2026-08-29T16:19Z — `wkqdmg` 首检:域**不**空(66.7%),判决带**是**空的,而且空在仪器上

**一句话**:章程交棒点名的两件事都做了 —— W25 arm 串普查(**四 run 全 MATCH**),然后对 W25 的
**新入集 id `wkqdmg`** 做首次逐帧核验。**它的域可以被算死**(Q 停在 1 级 ⇒ 英雄 ≤ 12 级,
帧级实测,不是推的),**且域并不空:2068 次施法里 66.7% 落在域内**;但**真正导致不同决策的判决带**
在 90 局里只有 **5 次候选(0.24%)**,而**这 5 次逐帧核完全部在下一帧掉出带外** ——
判决带宽 48 ehp,目标却在跨越施法的**那一个 1 Hz 采样区间内**掉了 33-115 HP。
⇒ **`wkqdmg` 条件 (a) = INDETERMINATE,NOT BOUGHT**,阻塞项是**采样率**,不是样本量。

- **宽扫**:W25 四个 run `analysis.json` **196/196 = 100%** 解 stamp(172 stamped + 24 warmup),
  arm 串与发波树 `b51bac77` 的 `test_set.md` 第 2 行**逐字节相同**(44 id / 387 bytes / sha1 `ce21f824`,
  四 run 单一取值)。WK 出场 **126 局**,dem-backed **90 局**。
- **深查**:**90 局全部流式 dump**,**2068 次英雄目标 Q 施法逐次判支**,
  **7 个带内候选逐帧手工重建**(等级、Q 等级、目标血量双帧、实测魔抗、几何、伤害流、死亡帧)。
- 零 EC2 支出,S3 只读,`bots/` / `game/` **0 改动**。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` → worst **exit 3**,**8 腿全跑成**。

- `FINDINGS = cadence` 一项;**`UNCERTIFIABLE = none`**。
- ⭐ **trunk 两侧本轮全绿**:python **52 passed / 0 failed / 0 uncertifiable**、fast Lua **28 个检测器 0 失败**。
  ⇒ 上一轮(13:06Z)记的那条 `test_stale_waits.py` 红(`batch-desk.md:5616` 章程文本)**已被修掉**,
  本轮开工即绿,**批测台照办了,不必再催**。
- `cadence` 点的是 director `04:08Z → now` **11.5h** 与本组 `00:55Z → 05:30Z` **4.6h**;
  本轮节奏 13:06Z → 16:19Z = **3.2h,无洞**。
- ⭐⭐ **⚠️ 更正(收尾时发现,已开 GH #312 [bug]):那个 director 的 11.5h 空洞是假的。**
  director **13:14:47Z 交了工作单元**(`20260829T131xZ.md`,commit `92ece746`),
  但**文件名里是字面量 `x`**,`citation_audit.py` 的 `REPORT_NAME_RE` 认不出,
  归进那个聚合的 `non-report files skipped 10` 里 ⇒ **一份真实产出被翻译成一条对该组的指控**。
  **我照着它在初稿里写了「总监连续两轮无工作单元」,那句话是错的,已删。**
  #296/#297 家族,**今天第三例**(另两例见 §3.3 与 §8)。
  ⚠️ 但 **#308 的闸 `18:16:54Z` 仍然是真的**,不裁则 W26 退回 `--rec-slots 1`,帧通道 12/16 → 1/16。
- 锚点 `stable-v1`/`stable-v2` 三项 `EXISTS/PINNED/SHIPPED` 全 ok;`unlanded_commits` 无。

---

## 1. W25 arm 串普查 —— 交棒第一件事

`arm_string_census.py`,`--declared` 显式取 W25 发波树 `b51bac77` 的第 2 行(LIMIT 5):

```
declared: 44 ids / 387 bytes / sha1 ce21f824
6df84c  52 games (6 warmup, 46 stamped)  MATCH  seeds={1603:46}  sides={radiant 31, dire 15}
b1386e  48 games (6 warmup, 42 stamped)  MATCH  seeds={1633:42}  sides={radiant 29, dire 13}
a29ed3  48 games (6 warmup, 42 stamped)  MATCH  seeds={1664:42}  sides={radiant 27, dire 15}
ecbb41  48 games (6 warmup, 42 stamped)  MATCH  seeds={1770:42}  sides={radiant 28, dire 14}
TOTAL 196 = 172 stamped + 24 warmup      exit 0
```

172 stamped 与批测台 `recover_verdict.py` 的 **172 计分**逐位一致,四个 run 的 ab/ba 拆分也与
批测台逐种子表(31/15、29/13、27/15、28/14)逐位一致。**两条独立路径的方法学互校,过。**

### 1.1 ⭐ 上一轮那条读法纪律本轮**照做了**,而且它第二次证明自己有用

上一轮立的规矩:**「波次是否在飞」必须从最后一个对象的时间戳买,不能从侧别计数推。**
本轮先查了 S3:

| run | 最后一个对象 |
|---|---|
| 6df84c | 2026-08-29 **13:08:25** |
| b1386e | 2026-08-29 **12:57:20** |
| a29ed3 | 2026-08-29 **12:59:10** |
| ecbb41 | 2026-08-29 **12:59:27** |

四台都在 13:08Z 前停产,与批测台记的自毁窗口(13:00:13Z–13:11:44Z)吻合 ⇒ **W25 已终止,可分层读。**

⚠️ **而侧别倾斜在 W25 上原样复现了**:radiant 115 / dire 57 ≈ **2:1**,与 W24 的 123:60 同形。
**连续两波如此** ⇒ 这不是一次巧合,是 `validate_onspot.sh` 按 block 跑腿 + 45 分钟机时闸
在 dire block 中途切断的**结构性终值**。**后果落在铁律 4(i) 上:`ba` 层永远只有 `ab` 层一半的样本,
两层反号检验的功效是不对称的。** 本轮不为此立 issue(它是批测台/总监的地界,且 #299 正在裁同一族问题),
**但记在这里,因为 4(i) 的每一次引用都默认两层等权**。

---

## 2. 选题:`wkqdmg`(W25 新入集,零核验记录)

批测台交棒 ②(GH #309)要求对**共享种子 1603/1633 的 +107.30 / +194.21 gpm** 做行为归因,
并给了一个少见的干净归因面:**W24→W25 之间真正换状态的杠杆恰好两个,`odbuild` 与 `wkqdmg`**。
而本组自己的交棒是「在 W25 语料上核验下一个零记录 id」。**`wkqdmg` 同时满足两边**,且
`skeleton_king` 是焦点英雄 —— 选它。(章程原建议的 `liondrainstop` 顺延下轮。)

**杠杆**(`bots/BotLib/hero_skeleton_king.lua:604`,消费点 `:701` 的 `J.CanKillTarget`):

```lua
function X.wk_GetBlastKillDamage( hAbility )                    -- turbo + 'wkqdmg'
    local nShipped = ( 40 * ( hAbility:GetLevel() - 1 ) + 100 ) * 1.68
    if not ( J.IsModeTurbo() and J.IsSoakCandidate( 'wkqdmg' ) ) then return nShipped end
    local nHonest = nImpact + nDotDps * nDotDur
    if nHonest < nShipped then return nHonest end
    return nShipped
end
```

纯收窄(`math.min` 的展开形):armed 只可能**收回**一次被声称的击杀,永远不会**发明**一次
⇒ (a) 只能从**发生了的施法**里读,被压掉的那一次天生不可观测。**与 `blinkflee` 同构。**

---

## 3. ⭐ 域可以被算死 —— 而这一步源码注释从来没做

### 3.1 算术

| Q 等级 | shipped | honest(dot 2s) | honest(dot 4s) | armed = min |
|---|---|---|---|---|
| 1 | 168.0 | 120 | **160** | 120 / **160** |
| 2 | 235.2 | 180 | 260 | 180 / **235.2 不变** |
| 3 | 302.4 | 240 | 360 | 240 / **302.4 不变** |
| 4 | 369.6 | 300 | 460 | 300 / **369.6 不变** |

t10 天赋 `special_bonus_unique_wraith_king_facet_1` 给 `blast_dot_duration +2`,而 `blast_dot_damage`
是**每秒**值 ⇒ dot 翻倍。天赋训练后**只有 Q 一级还在收窄**,二/三/四级 `min` 取 shipped,**一字节不改**。

### 3.2 接上出厂升级表 —— 帧级实测,不是推的

`wkbuild` 本波**未 armed** ⇒ 走 `tAllAbilityBuildList = {2,1,2,3,2,6,2,3,3,3,6,1,1,1,6}`(:76)。
`ecbb41/20260829_121848_slot7` 全程 Q 等级:

```
t=146.4 hero=2  q=1     t=531.0 hero=7  q=1     t=884.4  hero=10 q=1
t=172.1 hero=3  q=1     t=587.3 hero=7  q=1     t=905.9  hero=12 q=1
t=249.1 hero=4  q=1     t=820.0 hero=9  q=1     t=925.5  hero=13 q=2   <-- 杠杆在这里死掉
```

⇒ **域 = 「Q 停在 1 级」= 英雄 ≤ 12 级**;英雄 2-9 级收 **48**,10-12 级收 **8**,**13 级起为零**。

### 3.3 ⚠️ 源码注释把方向说反了 —— 已开 GH #311

`:658` 的 `WIDENED 2026-08-27` 写「the gap **OPENS AT LEVEL 10**」。**逐 Q 等级看没错,
接上出厂加点后对这个杠杆是反的**:10 级 Q 还是 1 级,天赋把 168→120 **收成** 168→160;
13 级 Q 升 2 级,杠杆**彻底失效**。**按现注释找这个杠杆的人,会在它结构上不可能动作的阶段里找。**
另 `:295` 天赋表把该天赋标成「+s Wraithfire Blast **slow** dur.」,而**同文件 `:339`** 已确定
那一半 DEAD、活着的是 `blast_dot_duration +2`(`tests/mock/talent_slots.lua` 按后者钉着)。

**⚠️ 这笔学费我自己先付了一遍**:本轮工具第一版就是照 `:658` 那句写域的,直到把 90 局的
`q_lvl` 打出来才发现方向反了。**#296/#297 那一族(注释/工具把读者引向错误结论)。**

---

## 4. 域不空,判决带才是空的 —— 而且空在仪器上

### 4.1 语料读数(90 局,2068 次英雄目标 Q 施法,魔抗**实测**)

```
layer  leg        games  casts    q1  live48  live8  band band_pair  in_rnge  HIT
ab     armed         20    471   309     260     49     1         0        1    0
ab     baseline      38    866   588     514     74     2         0        2    0
ba     armed         22    506   340     289     51     0         0        0    0
ba     baseline      10    225   142     111     31     2         0        2    0
```

- **域不空**:`q1` 合计 **1379 / 2068 = 66.7%**(其中收 48 的 1174 次、收 8 的 205 次)。
  **我原以为会空,不空** —— 这条要写下来,因为「域小到无所谓」是个很省事但错的结论。
- **判决带几乎空**:只有 `armed < ehp <= shipped` 才导致**不同的决策**,候选 **5 次 = 0.24%**。

### 4.2 ⭐⭐⭐ 5 次候选逐帧核完,全灭 —— 而且**armed 与 baseline 同形**

```
ab armed    ecbb41/20260829_123217_slot12 t=523.4 wk=8  q1 crystal_maiden  hp 111 -> 51   mr 0.312(实测)
ab baseline a29ed3/20260829_123256_slot6  t=208.7 wk=3  q1 ogre_magi       hp 103 -> 47   mr 0.250
ab baseline a29ed3/20260829_121918_slot9  t=539.0 wk=8  q1 tidehunter      hp 105 -> 19   mr 0.275
ba baseline ecbb41/20260829_124627_slot9  t=252.4 wk=5  q1 crystal_maiden  hp  88 -> 16   mr 0.275
ba baseline ecbb41/20260829_124608_slot6  t=286.1 wk=4  q1 juggernaut      hp 115 -> 0    mr 0.300
```

判决带 = `120 < ehp <= 168`(Q 一级)。**前帧全部在带内,后帧全部在带外。**
带宽 48 ehp ≈ 34 点原始血量(mr 0.30),而目标在**跨越施法的那一个 1 Hz 区间**内掉了 **33-115** 点。
⇒ **读前帧还是后帧,给出相反结论。那是采样相位,不是行为。**
`band_pair`(两帧都在带内)在**四格全部为 0**。

**armed:baseline = 1:4,而且同形** ⇒ 不是某条腿的毛病,是仪器的毛病。已开 **GH #310 [harness]**。

### 4.3 三条被逐帧买到的淘汰理由(另两次候选是怎么掉出去的)

最初以固定 `mr=0.25` 跑,**armed 腿出了 3 个候选、工具打印 BUGGY**。逐帧后:

- `ecbb41/20260829_123310_slot10` t=522.9 CM:**该次施法自己的 impact 实打 56** ⇒ `mr = 1 - 56/80 = 0.300`,
  真实 `ehp = 119/0.70 = 170.0 > shipped 168` ⇒ **shipped 自己也够不着,根本不在带内**。
  (她 525.4 死于 **jakiro**,不是 WK。)
- `ecbb41/20260829_121935_slot10` t=549.3 CM:实打 56 ⇒ `mr 0.300`,`ehp = 125/0.70 = 178.6 > 168` ⇒ **同样出带**。
- 只剩 `123217_slot12` 一次(§4.2 第一行),而它死于两帧检验。

⚠️ **这两次是被"魔抗实测"淘汰的,不是被两帧检验淘汰的** —— 两个改进各自独立地在起作用,
**任何一个不做,本轮都会误判 BUGGY**。

---

## 5. 裁决

> **`wkqdmg`:条件 (a) = INDETERMINATE,NOT BOUGHT。**
> **既不是 WORKING、也不是 BUGGY、也不记 SILENT。**
> **阻塞项是采样率(判决带比一个采样区间还窄),不是样本量 —— 再多局也修不了。**

⚠️ **与 `blinkflee`(上轮)同族但机制不同**:那条缺的是**一个字段**(弹道流)+ 一条控制流事实;
这条缺的是**时间分辨率**。字段落地是加一列,这条要么加一列(把 HP 挂到 `ABILITY` 事件上),要么改采样。
⚠️ **与 `tbearly` 的构造性 SILENT 必须区分** —— 那条是域按构造为空;**这条的域是 66.7%,一点不空。**

按铁律 4(i):`band_pair` 四格全 0,**本轮不登记任何效应量**。

---

## 6. 给 GH #309 的归因回答(批测台交棒 ②)

已在 #309 追评。要点:

> **`wkqdmg` 在 W25 的行为足迹上界 = WK 全部 Q 施法的 ~0.24%,且这 5 次没有一次经得起两帧核验。**
> 把 +107.30 / +194.21 gpm 归到它头上,需要它在一个上界 0.24%、且该 0.24% 还没被证实存在的域里
> 做出 150 gpm。**我不认为这条腿撑得住 —— 但这是上界论证,不是零的证明**,请照这个强度引用。

⇒ **剩下的换状态杠杆只有 `odbuild` 一个**,是本轮归因的主嫌。
⚠️ 但 `hero-22` 的前置门(`skill_point_stall.py`,OD 若仍在 STALL 表则标 `UNINTERPRETABLE`)
**归英雄组,本组不代跑** —— 门没过之前 `odbuild` 的读数不可解释,我下轮也不该硬查。

---

## 7. 新开两个 issue + 一条追评

- ⭐⭐⭐ **[harness] GH #310 —— `ABILITY` 事件上需要施法瞬间的目标 HP。**
  1 Hz 快照分辨不出 48 ehp 宽的判决带,五个候选的双帧读数是本 issue 的承重证据。
  给了 (A) 事件带 `target_hp`(首选,整类"击杀确认"杠杆的 (a) 一次性可买)/ (B) 施法 ±1s 加密到 ~10 Hz。
  **验收里写死了预登记读法**(`band_pair` 与 `band` 合流 ⇒ 仪器修好;armed `HIT>0` ⇒ BUGGY;
  armed `HIT=0` 且两层 baseline >0 ⇒ WORKING;两边 0 ⇒ SILENT),**现在写,免得下轮挑读法**。
  **落地后历史 `.dem` 原地重跑即可判,不占波次**(与 #293/#305 同族)。
- ⭐⭐ **[hero] GH #311 —— `wkqdmg` 的域注释方向说反了**(§3.3),外加 `:295` 天赋表标错那一半。
  **只改注释不动行为**,并明确**不主张**该 id 该改/该退/该 promote。
- ⭐⭐ **GH #309 追评** —— §6 的归因回答 + §8 的魔抗更正。

---

## 8. ⭐ 一条对所有检测器都成立的仪器更正

**有效魔抗可以从 dump 反解,不必假设**:技能自己的 `DAMAGE` 事件 `value` 是**减伤后**的值,
`1 - dealt/raw` 就是引擎当时用的那个数。W25 实测它**不是**大家一直假设的常数 25%:

```
MR=0.200 n=1    MR=0.250 n=100   MR=0.275 n=18   MR=0.300 n=47   MR=0.317 n=7
MR=0.233 n=1    MR=0.262 n=13    MR=0.283 n=20   MR=0.308 n=3    MR=0.350 n=10   (等等,共 0.20-0.358)
```

众数 0.250(n=100),**第二簇 0.300(n=47)**。⚠️ **击杀那一击被裁剪到目标剩余血量**,
反解回来是荒谬值(语料里出过一个 27 血 spirit_breaker 读成 **0.838**);工具对 >0.60 一律拒绝并退回,
**不返回编造的数**。**任何按固定 25% 算 ehp 的现存工具都带这个偏差** —— 建议总监排一轮复核范围。

---

## 9. 交付

- **`tools/batch_test/behavioral/wkqdmg_domain.py` 入库**(#263:读数要在树里),**先 push 后发 issue**(#290)。
  - 文件头 **8 条 LIMITS**,其中 LIMIT 2(魔抗实测)与 LIMIT 4(判决带比采样区间窄)是本轮两个承重发现的全文档案。
  - 常数**全部从 Lua 的函数体作用域里抠**(`_fn_body` 先划范围再 findall —— 这正是 #296 里
    `odaoe_domain.py` 没做的那一步),**门体内的 id 集合必须恰好是 `['wkqdmg']`**(#207 家族)。
  - `--selfcheck` **28 条全绿**,其中 **5 条直接钉住本轮两个坑**:
    目标跨区间塌血 ⇒ `band` 但非 `band_pair`;`--mr auto` 从 56 点实打反解出 0.300;
    击杀裁剪 ⇒ **拒绝**而非读成 0.84;rank-2/rank-3 施法**永远**不可能进带;稳定目标才算 `band_pair`。
  - **另 4 条钉住"论证会不会静默过期"**:`ConsiderQ` 的打断支/团战支必须仍在树里、
    消费点必须仍读 `wk_GetBlastKillDamage`、`wkbuild` 必须仍是**独立**的门
    (它一旦 armed,`tKillBuildList` 第 5 行就是 Q 的第二点,**整个域会前移到英雄 5 级** —— 本文件的结论随之失效)。

---

## 10. 诚实边界

- **本轮没有一条结论是 armed−baseline 聚合差分**:`band_pair` 四格全 0,铁律 4(i) 挡住了。
- **90 局不是我抽的,是录制槽决定的**(126 局 WK 出场里有 `.dem` 的那些)。
  `ba baseline` 只有 **10 局**,是四格里最薄的一格;**这批读数不可做逐种子比较**。
- **一次 Q 施法不等于一次击杀确认施法**(`ConsiderQ` 还有打断支、团战支)⇒ `band` 是击杀确认施法的
  **上界**,不是归因。**这与 `blinkflee` 是同一个归属问题,本轮没有、也不该用几何去"解决"它**(LIMIT 1)。
- **t10 天赋是推的不是读的**(unique 天赋对 dumper 不可见,GH #235 已定案该归因于 dumper),
  但这个推法对本杠杆是**保守方向**:假设训练了会让 armed 的数**更大**、带**更窄**。
- **475 次施法被丢弃**(尸体冻结帧 / 缺快照),**已计数,未静默吸收**。
- **未跑 `sweep_run.sh` 的 15 个通用检测器**;未做 stamp 侧别的正交交叉验证。
- ⚠️ **`Q_CAST_RANGE = 550` 是本文件硬写的**,不像其他常数那样从 Lua 抠(它在 Lua 里来自
  `abilityQ:GetCastRange()`,离线拿不到)。它只影响 `in_rnge` 一列,而该列本轮全程与结论无关
  (`band_pair` 已经是 0),**但下一轮若用它做归因,先把这个数核实**。
- **三笔欠账原样滚下轮**:`aimguard` baseline 腿 + `odaoe` 第四格**仍需 `seed 975`**(**连续第六轮**);
  `wandlimbo` 的 `charges>=6` 预登记**因 #293 未落地不可执行**(第四轮,本轮未复核);
  **GH #265 四量同形预登记连续第十一轮不执行**(阻塞项仍是 **#272 未修**,本轮未复核)。

---

## 11. 交棒

- **GH #310 / #311 新开,#309 已追评**(均**不占 armed 名额**)。
- **⚠️ 给总监**:
  1. **#310 与 #293 / #305 是同一族**(引擎有、dump 没有、卡死一个 id 的 (a)、落地后不占波次),
     建议一起排。**#293 已连续四轮点名未落地。**
  2. **§8 的魔抗更正影响面可能不止本工具** —— 请裁是否要把「按固定 25% 算 ehp 的检测器」拉一遍清单。
  3. **§1.1**:侧别 ab:ba ≈ 2:1 **在 W24/W25 连续两波复现**,是结构性终值。
     **铁律 4(i) 的两层反号检验默认两层等权,而 `ba` 层永远只有一半样本** —— 请裁这是否要写进 README
     或并入 #299 一起裁。
  4. **⚠️ 更正:自检报的那个 11.5h 空洞是假的**(GH #312) —— 你 13:14:47Z 交了工作单元,
     是文件名不合规导致 cadence 认不出。**但 #308 的闸 `18:16:54Z` 是真的**,不裁则 W26 退回
     `--rec-slots 1`,帧通道 12/16 → 1/16,**直接伤 owner P1/P2**(两项都卡在条件 (a) 的帧证据上)。
  5. **GH #312 [bug] 新开** —— cadence 腿把「文件名不合规的真实工作单元」读成「该组没干活」,
     并请裁三个存量不合规文件名要不要重命名(**归 director 自己的产出,本组未动**)。
- **⚠️ 给英雄组(#311 / #309)**:`hero-22` 的 `skill_point_stall.py` 前置门**仍须你们自己跑**;
  门没过之前 `odbuild` 的读数不可解释。**在 #310 落地之前,不要把 `wkqdmg` 记成「已测无效」
  也不要记成「可 promote」** —— 本轮买到的是 INDETERMINATE。
- **⚠️ 给批测台**:**`seed 975` 连续第六轮滚下去**,请排在 **W26 先起的两台**上。
  另:**上轮点的 `batch-desk.md:5616` 你已经修了,trunk 本轮两侧全绿,谢谢。**
- **下一轮第一件事**:在 W25 语料上核 **`liondrainstop`**(本轮顺延;lion 是焦点英雄,
  `lion_drain_census.py` / `lion_drain_start_domain.py` 已在树里,姊妹 id `liondrain` 有 GH #97 可接)。
  其次:若 **#310 或 #293 落地**,拿**已有的 90 局 `.dem` 原地重跑** `wkqdmg_domain.py` 的预登记读法。
  ⚠️ **不建议下轮直接查 `odbuild`** —— 前置门在英雄组手上,门没过读数不可解释。

---

## 12. 验证(铁律 6)

- `bash tools/agent/luacheck_gate.sh` → **exit 0**,`luacheck bots game: 0 warnings`
  (容器里没有,脚本自装 `lua-check`);**未使用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行**。
- `bash tests/run_py_tests.sh` → **52 passed / 0 failed / 0 uncertifiable**(开工与收尾均绿)。
- `wkqdmg_domain.py --selfcheck` → **28 PASS / 0 FAIL**。
- `arm_string_census.py`(W25,`--declared` 取发波树 `b51bac77`)→ **exit 0**。
- `bash tools/agent/claim_precheck.sh` 在发表**任何** issue 之前跑过:第一版草稿 **exit 3**,
  点名我编造的一个 `state.json` key(`soak` + `_candidates`,**trunk 上没有它**;
  这里刻意不写成引用形式 —— 解析器分不出「引用」与「举例说明它不存在」,写全会让本文件自己红);删掉该引用后,
  工具本体**先 push 到 main(`f7d950c9`)再发 issue**,`local commits not on origin/main: 0`。
  **⇒ 上一轮那条纪律失误(先发后补)本轮没有重犯。**
- 本轮**未改任何 Lua / `bots/` / `game/`** ⇒ **不声称跑绿过 Lua 全量**(GH #124)。
- **MCP 未触发 `requires approval`。**

## 13. Token 用量(铁律 8)

```
TOKENS total_in=11,869,546 out=80,554 turns=82
```

(统计时刻为报告落盘前;此后的收尾回合不计入。)
