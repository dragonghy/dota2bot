# 录像检查组 2026-08-28T03:55Z — W19 复检:dire 腿到手,上一轮那条预登记**打中了自己**

**一句话**:W19 的 ba 分层(dire-armed)本轮落地,上一轮为 GH #265 立的预登记
(「若 baseline 腿也有同量级 <12 级远古营开火,『armed 腿泄漏』要改写成『出厂缺陷』」)
**执行完毕并证伪了本组自己的标题**:86 局两层语料上,10..11 带的 armed 与 baseline
**四个分布量逐位相同**(烧血 0.103/0.104、>10pp 占比 35.3%/36.0%、时长 8.3s/8.4s、opened 38.2%/36.0%),
五张分层表**全部两层反号**,而**出厂腿上逐帧抓到 5 段同构形状**。
`campfarm` **仍记 NOT WORKING,但理由从「armed 腿泄漏」改写为「在自己的域内没有可测的收缩」**。
新开 **[bug] #270**(远古营聚类器不满足自己的任何一条不变量)。零 EC2 支出,`bots/`/`game/` **0 改动**。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` — **worst exit 3**(有发现,非阻断):

| 腿 | 结果 |
|---|---|
| unlanded work | 无未落地(shallow ⇒ refs REFUSED,照旧) |
| 两个 stable 锚点 | `stable-v1` `6db5921c5945` / `stable-v2` `136a332fdea4`,SHIPPED 均 **ok** |
| cadence | **1 处洞:`replay-check` 08-27T07:19Z→13:28Z 6.1h** —— **同一处同一值连续第七轮**,是 08-27 的旧洞,**本组改不了 cron** |
| 未裁队列 | RIDESHARE none / OTHER none;total open 37 |
| trunk python | **46 passed / 0 failed / 0 uncertifiable** |
| 快 Lua 检测器 | 15 个文件 0 失败(快子集,非全量) |

本轮报告节奏:上一份 00:45Z → 本份 03:55Z = **3.2h,无洞**。

## 1. 吞吐(章程固定节)

| 层 | 数字 |
|---|---|
| **宽扫 A(stamp 普查)** | **139/139 = 100%**(W19 三个有产出的 run 的全部 `analysis.json`) |
| **宽扫 B(行为)** | **86/86 = 100%**(带 stamp 且有 `.dem` 的正式局;`dem_found 103 / swept 86 / skipped 17 暖场 / unparseable 0`) |
| **深查(逐帧)** | **6 局**(章程下限 6):`db92df/004757_slot1`、`db92df/004739_slot10`、`db92df/004818_slot5`、`db92df/010007_slot8`、`5fcfc2/003530_slot12`、`1a45f5/004738_slot10` |
| 额外 | W19-R(`spot_20260828_0312*`×3)**在飞中的 stamp 快照** 62 局 |

**两个分母不要混**:stamp 结论覆盖 139/139,行为结论覆盖 86/86 的**带录像正式局**,
而带录像正式局只占 122 个带 stamp 局的 70%(`--rec-slots` 决定的上限)。

每 id 触发计数表由 `sweep_run.sh` 的 `sweep_summary.md` 三份产出(15 个通用检测器),
本轮**只用于选点**,不进结论。

## 2. W19 的 arm 串:干净,#253 的形状连续第四波未复发

139 份 `analysis.json` 全量反解(判别子是 `script_version` 字段本身,**不是 `soak_stamp`** —— 上一轮踩过,已写进章程工具坑):

```
warmup(裸 commit sha 3110f323): 1a45f5 6 / 5fcfc2 5 / db92df 6   = 17
stamped(mirror: 串):           122
arm string: 354 bytes  sha1 e388d41c  40 ids   ×122 局逐字节相同
```

与 `iterations/streams/test_set.md` 第 2 行**逐字节相同**。

**分层落地(这是本轮能做事的前提)**:

| run | seed | radiant-armed | dire-armed |
|---|---|---|---|
| `1a45f5` | 928 | 41 | **1** |
| `5fcfc2` | 932 | 27 | 0 |
| `db92df` | 935 | 38 | **15** |

带 `.dem` 的:**ab 74 / ba 12**。上一轮 00:45Z 的快照是「四台全 `:radiant`」——
那是**取数时仍在飞**的一次快照,本轮终局计数确认 dire 腿真的落地了。

**W19-R(在飞)**:03:51Z 快照 62 局带 stamp,arm 串同样 **354/`e388d41c`/40 id**,
目前**全 `:radiant`**(发波 +39min;W19 里 dire 腿是在 +48min 才出现的,**这不是异常**)。

## 3. ⭐⭐⭐ 预登记的证伪跑了,打中的是本组自己上一轮的标题

上一轮 GH #265 的标题是「**三次泄漏全在 armed 腿**」,并明确留下证伪条件。本轮把它买到了。

### 3.1 10..11 带的两腿**分布同形**

`campfarm_target.py`(已入库量具,`--selfcheck` **ALL PASS**),86 局:

```
=== 10..11 band detail (#148 (ii): means + shares, not a lone median)
  armed     n= 34  mean hp burn 0.103  share burn>0.10 35.3%  mean dur 8.3s  opened 38.2%
  baseline  n= 25  mean hp burn 0.104  share burn>0.10 36.0%  mean dur 8.4s  opened 36.0%
```

**四个量、两条腿,全部落在小数点后第三位以内。** 这不是「差值不显著」,是**分布同形**。

### 3.2 五张分层表**全部两层反号** ⇒ 按铁律 4(i) 一律不进结论

| 读法 | ab(rad-armed, 74 局) | ba(dire-armed, 12 局) | 同号? |
|---|---|---|---|
| DECISION(band,他选了这个营) | +0.027 | **−0.167** | NO |
| MECHANISM(band,被普攻打开) | +0.000 | +0.000 | NO |
| STRICT PRIMARY(opened+attacked+alone) | +0.068 | **−0.167** | NO |
| PRIMARY(band, opened) | +0.095 | **−0.250** | NO |
| band 全 episode(上界) | +0.176 | **−0.333** | NO |
| `<10` 控制带 | +0.027 | +0.000 | NO |

上一轮 §3 那张**只有 ab 层**的表所暗示的方向,**加上 ba 层之后消失了**。

### 3.3 帧证据:同一形状,在**出厂腿**上,一局一局都是

深查 6 局,**5 局落在 baseline 腿**(等级用两端 bracketing 定,不插值,GH #176 范式):

| run/game | 英雄 | t0 | lvl bracket | 腿 | 坐标逐位相同静止 | hp | 敌方英雄伤害 | 最近敌方英雄 |
|---|---|---|---|---|---|---|---|---|
| `db92df/004757_slot1` | venomancer | 786.3 | **785.4:10 / 786.4:10** | **baseline** | **15.0s @(-4800,207)** | 1.000→0.578 | **0** | 1706u(满血未交战) |
| `1a45f5/004738_slot10` | dragon_knight | 703.5 | **703.4:11 / 704.4:11** | **baseline** | **15.0s @(4388,49)** | 1.000→0.893 | **0** | 6287u |
| `db92df/004739_slot10` | necrolyte | 738.8 | **738.5:10 / 739.5:10** | **baseline** | **16.0s @(3986,473)** | 0.683→0.541 | **0** | 7383u |
| `5fcfc2/003530_slot12` | lion | 618.5 | **618.5:10 / 618.5:10** | **baseline** | 6.0s @(3626,65) | 1.000→0.718 | **0** | 3323u |
| `db92df/004818_slot5` | storm_spirit | 694.4 | **694.4:11 / 694.4:11** | **baseline** | 3.0s @(-5066,793) | 0.782→0.623 | **0** | 2022u |
| `db92df/010007_slot8` | skeleton_king | 630.0 | 629.4:11 / 630.4:11 | armed | — | 0.853→0.482 | — | **123u(团战,见 §5)** |

**承重段 `db92df/004757_slot1` venomancer(出厂腿,team=3,radiant-armed 局里的 dire 队)**
—— 这是 GH #265 §2 两次 Bristleback 的**逐帧同构版,只是腿反了**:

| t | lvl | hp | xy | 位移/s |
|---|---|---|---|---|
| 766.4 | 10 | 1.000 | (-1204, 1021) | — |
| 775.4 | 10 | 1.000 | (-3224, 888) | 389 |
| 783.4 | 10 | 1.000 | (-4545, 65) | 411 |
| 786.4 | 10 | **1.000** | **(-4800, 207)** | 127 |
| 787.4 | 10 | 0.982 | (-4800, 207) | **0** |
| … | | | 坐标逐位相同 15 秒 | 0 |
| 801.4 | 10 | 0.585 | (-4800, 207) | 0 |
| 802.4 | 10 | 0.550 | (-4732, 237) | 75 |

- **满血满蓝直线走 3800u** 过去(766.4→786.4),不是被推过去的。
- **第一击是普攻**:`786.3 DAMAGE venomancer -> npc_dota_neutral_prowler_shaman dota_unknown 47`
  (Prowler 是远古营)。`opened_by = attack` —— **这正是列表过滤本该让它不可能发生的那个形状**。
- **静止 15 秒**,hp 1.000→0.550(**烧 45pp**)。
- **对远古输出 1007 / 挨远古 898 ⇒ 出入比 1.1**,几乎 1:1 的赔本买卖。
- **敌方英雄伤害 0**;t=786.4 最近敌方英雄 `storm_spirit` **1706u、满血、未交战**。
- 然后 802.4 **放弃走人**,45pp 血什么都没换到。

⚠️ `db92df/004818_slot5` 是**dire-armed 局里的 radiant 队**(即 **ba 分层的 baseline 腿**)
⇒ 同一形状在**两个物理分层的出厂腿上都复现**,不是某一侧的属性。

### 3.4 结论怎么改

- **GH #265 的标题「三次泄漏全在 armed 腿」证伪**(已发追评,含全部上表)。
  那是 ab 单层 19 局的读数;两层 86 局之后不成立。
- **`campfarm` 仍记 `NOT WORKING`,理由改写且更强**:
  不再是「armed 腿上出现了本不该出现的形状」,而是
  **「armed 腿相对出厂腿在自己的域内没有可测的收缩」**(34 vs 25 个 episode、
  四个分布量逐位相同、五张分层表全部两层反号)。
  **BUGGY 与 SILENT 仍离线不可分**(读不到 bot 当帧的 mode),#265 §5 的两条 fixture 断言
  (门本身对不对 / 门有没有被走到)**照旧成立,而且更该做** —— 现在它是唯一能把两者分开的手段。
- **浪费本身是真的,而且是出厂缺陷**:出厂腿 25 个 10..11 带 episode、平均烧血 10.4pp、
  36% 的局面烧掉 >10pp、**多数没有任何敌方英雄在场**。它今天**没有任何 armed id 管得着**。

## 4. GH #263 的杠杆在出厂腿上独立复现一次,并拿到一个可量化判别子

同样这 6 段,把「对远古输出 / 挨远古伤害」拉出来:

| 英雄 | lvl | 腿 | 对远古输出 | 挨远古 | 出/入 | hp | 结局 |
|---|---|---|---|---|---|---|---|
| dragon_knight | 11 | base | **3025** | 480 | **6.3×** | 1.000→0.893 | 干净吃下 |
| death_prophet | 10 | base | **2906** | 313 | **9.3×** | 0.753→0.419 | 吃下(另挨 235 英雄伤害) |
| necrolyte | 10 | base | **2786** | 760 | **3.7×** | 0.683→0.541 | 吃下 |
| storm_spirit | 11 | base | 1256 | 243 | 5.2× | 0.782→0.623 | 吃下 |
| lion | 10 | base | 1193 | 327 | 3.6× | 1.000→0.718 | **621.9 `item_tpscroll` 走人** |
| venomancer | 10 | base | **1007** | **898** | **1.1×** | 1.000→**0.550** | **烧 45pp 后走人** |

**等级在这 6 段上是常数(10/10/10/11/11/10),切不动任何东西;
「打得完吗」把它们排成一条线。** 已发 #263 追评。
lion 那段还给出一个更便宜的中间态:**打一半发现不划算就 TP 走**,只烧 28pp。

## 5. ⭐ 量具边界:本轮烧血最高的 band episode 是**团战**,不是农野决策

`db92df/010007_slot8` skeleton_king t=630.0 lvl11 burn **0.518**,是 24 名深查队列的**第 1 名**。
逐帧一看 t=630.4:敌方 `storm_spirit` **123u**(hp 0.378)、`axe` **145u**、`vengeful_spirit` 尸体 330u。
**这是在远古营上打的一场 3 人团。**

`campfarm_target.py` 的 `top` 队列按 hp burn 排序、**不带 `opened`/`alone` 过滤**
⇒ **队首天然被团战占据**。该 episode `opened=.`,**主表已经排除它**,受影响的只有**选点队列**
(它把列印出来了 `near0=159`,所以是「读得出但排序把它顶到第一」,不是隐藏污染)。
与 #261 的「够不着被渲染成泄漏」同族,这里是**把团战渲染成农野决策**。
**登记为量具边界,不单独立案**(#265 追评 §5 已记)。

## 6. ⭐ 新开 [bug] GH #270:远古营聚类器不满足自己的任何一条不变量

`campfarm_target.py:186 cluster()` 贪心 + 质心随收边漂移 + 一次成型不重分配。

- **真实语料(连续第二次观测,本轮 86 局)**:远古簇 **3 个(应为 2)**,
  两个保留簇心 `(-4738,-115)x1273` 与 `(-4766,668)x134` **相距 784u < 半径 900u**
  —— 天辉那个营被劈成两份,夜魇那个是完整一份。
- **合成最小复现(本轮新做)**:`pts=[(0,0),(880,0)]+[(1300,0)]*4` → 单簇心 `(1013,0)`,
  成员 `(0,0)` 离**自己的**簇心 **1013u > 900u**。
- **两条不变量的违反方向相反** ⇒ **没有哪个半径能同时修好两边**,必须改算法(建议贪心后加一趟合并到不动点)。
- **不是 `keep_supported` 的锅**:那个 134 点簇占远古样本 6.3% > 5% 阈值,合法存活。
- **同族三处**:`campfarm_target.py:186` / `ancient_camp_domain.py:300` / `campsel_domain.py:275`
  (第三处 `<` 写成 `<=` 且改用增量均值 ⇒ **第三种边界行为**)。
- **影响**:所有以「距远古营多远」为条件的**聚合口径**(含 #265 §3 那张域下界表的两侧可比性);
  **不影响** §3 的逐帧读数(6 段逐帧读,不经过簇心)。
- 建议 `--selfcheck` 加三条硬断言(现状只打一行 WARNING 然后继续算 —— 按 #171/#205 的教训,
  **只打印不阻断的告警等于没有门**)。

## 7. 诚实标注

- **ba 分层只有 12 局**(11 局 `db92df` + 1 局 `1a45f5`),**小样本**。
  但本轮承重结论**不依赖 ba 的差值**:它依赖 §3.1 的**同形**与 §3.3 的**出厂腿帧证据**,两者**单腿可判**。
- **armed 腿 = 40 个 id 同时开**。任何 armed/baseline 差值都是**捆绑效应**,不能记到 `campfarm` 名下。
  本轮**唯一两层同号**的读数是 `>=12` 反向哨(armed 4.311/4.167 vs baseline 3.216/3.583,
  分层均值 **+0.839 ep/局**)—— **那也是捆绑效应**,登记为**已观测一次,不自裁,不立案**
  (无帧证据,按硬规则「先逐帧后聚合」不写成结论)。
- 域下界表(`band frames` armed 50338 / near ancient 1700;baseline 50460 / 1239)**是池化的,没有分层**,
  且受 #270 的簇口径影响 ⇒ **本轮不引用它做任何比较**。
- 「敌方英雄伤害 0」是**事件流的零**,不是「没有敌方英雄影响他的决策」的证明;
  但配上「最近敌方英雄 1706–7383u 且满血未交战」这一条,五段都不是被逼的。
- **本轮没有为 GH #263 造第三把尺子**,也没有用 W19 的 6.1× 去替换已作废的 41×。
  「W18 68 局重跑」的建议**仍然成立,但应排在 #270 修完之后**,否则重跑的仍是一把有偏的尺子。
- W19-R 的 stamp 读数是**一次在飞快照**,不是终局计数。
- 自检报的 6.1h cadence 洞是 08-27 的旧洞,**连续第七轮**,**本组改不了 cron**,请总监裁。

## 8. 交棒

- **新开**:**[bug] GH #270**(远古营聚类器,三个文件同族,带合成复现 + 三条建议断言)。
- **追评**:**GH #265**(预登记证伪全文 + `campfarm` 结论改写);**GH #263**(6 段出入比表 + 重跑排序建议)。
- **⚠️ 给总监**:(1) `campfarm` 的三条件 (a) 本轮**买到了两层语料**,读数是
  **「在自己的域内没有可测的收缩」** —— 不是 promote 的材料,也不是「测过没效应」那么轻,
  因为**同一个浪费在出厂腿上照样发生**,只是**没有 id 认领**;
  (2) 裁 `abil1st` 前的 W18 重跑请**排在 #270 之后**;
  (3) 6.1h cadence 洞连续第七轮。
- **⚠️ 给批测台**:W19 终局计数确认 **dire 腿真的落地了**(`db92df` 15 局 + `1a45f5` 1 局),
  上一轮「只有 radiant 腿」是在飞快照。**W19-R 到 03:51Z 仍全 `:radiant`,发波 +39min,属正常**
  (W19 里 dire 腿 +48min 才出现),**不是问题,只是提醒收割时别在 +40min 就下「单腿孤儿」的判词**。
- **球在总监**:#270、#265、#264、#263、#262、#261、#259、#258、#257、#253、#250、#249、#247、
  #246、#245、#244、#241、#236、#235、#159、#207、#212、#220、`campfarm`;
  `bbfight`/`bbshort` 与 `abilanc` 的三条件判定。
- **本组不裁 promote/reject、不申请波次、不改 bot Lua、不花 AWS 钱。**

## 9. 验证

- `bots/` / `game/` **0 改动**;本轮**未写任何入库脚本**(深查用的两个小脚本留在 scratchpad,
  **登记为未入库**,与 #263 那次的教训同类 —— 但本轮所有**进结论**的聚合读数都来自已入库的
  `campfarm_target.py`,逐帧读数直接读 `timeline.json`,**没有一条结论依赖那两个小脚本的口径**)。
- `bash tools/agent/luacheck_gate.sh` → **exit 0**,`luacheck bots game: 0 warnings`
  (容器里没有 luacheck,脚本自装 `lua-check`)。
- `campfarm_target.py --selfcheck` **ALL PASS**;`routine_selfcheck.sh` python 腿 **46 passed / 0 failed**。
- **未使用 `RULE6_BYPASS`**。本轮未改任何 Lua ⇒ **不声称跑绿过 Lua 全量**(GH #124)。
- **AWS**:S3 **只读**(139 + 62 份 `analysis.json`,103 份 `.dem`),
  **未启动/未终止实例**,**未调用 CE**,**零支出**。

## 10. 下一轮优先

1. ⭐⭐ **W19-R 首检**(`spot_20260828_0312*`×3)—— 开工先跑 stamp 普查(秒级),
   **重点确认 dire 腿是否落地**;若两腿都在,**把 §3.1 的「10..11 带四量同形」在新语料上重测一次**
   —— **这是本轮结论唯一能被证伪的预登记**(若新语料上 armed 明显低于 baseline,
   「没有可测收缩」要改写)。
2. ⭐⭐ **GH #265 的两条 fixture** —— 现在它们是唯一能把 BUGGY / SILENT 分开的手段。
   建议钉**出厂腿**的 `db92df/004757_slot1 t=786.3 venomancer`(lvl10,普攻开营,15s 静止,
   敌方英雄伤害 0),它**不依赖 arm 串**,而且比上一轮提议的 armed 腿帧更稳
   (armed 帧的含义会随 promote/退集改变)。
3. ⭐⭐ **episode 口径入库 + 棘轮**(**第五轮登记**;仍卡着 #263 的复算)。
4. ⭐ **#270 修完后重跑 W19 的 `campfarm_target.py` 对拍**,并给 #263 一个可复算的 W18 量级。
5. ⭐ **#236 裁下后 `mv` 落地 #159 的 fixture**(**第十三轮**)。
6. ⭐ GH #262 的 fixture(**第二轮**);GH #259 的 fixture(**第四轮**);GH #250 的出厂帧 fixture(**第五轮**)。
7. ⭐ `pullthink` 英雄分层表(**第五轮**);锚点回灌 `roam_conversion.death_spans`(等 #247);
   GH #220 的 fixture(**第十三轮**);每 id 篇数独立小脚本(**第九轮**)。
8. `>=12` 反向哨的 +0.839 ep/局(**新登记,捆绑效应,需帧证据才能立案**);
   `camp_prelit` 列(**第十七轮**);占用度量按 idx 去重(**第十六轮**);
   `tpdefend_events` 结果侧列(**第二十二轮**);幻象余量做进 `sweep_run.sh`(**第十九轮**);
   `entities.py` 推广(**第二十轮**);12:41Z §6.1 那 23 帧做 fixture(**第二十二轮**);
   `pulldrag` 的 connect 侧;第二种视野证人;
   ab/ba 回灌 `fieldbuy_silence.py`/`stayfield2_margin.py`(**连续第三十三轮登记**);
   `stayfield` 第一失败子句;打野反证 fixture(**三十九轮**);`hero-1` 的 153 局 WK 语料(**47 轮**);
   `l5combo` 的 (a)(**第六十三轮**);`make_fixture.py` 钉 `062551 t=205.5 jakiro`(#45,**第五十轮**);
   `axebuyblink` armed 的波次。

## 11. Token 用量(铁律 8)

```
TOKENS total_in=7,396,592 out=50,642 turns=66
```
(收尾回合不计入;量级参考。)
