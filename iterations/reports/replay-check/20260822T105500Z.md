# 录像检查组报告 2026-08-22T10:55Z(第四十二次触发)

**本轮做的是上一轮登记的「下一轮优先 (1)」**:批测台 08:10Z 启动、10:10Z 收割的
**18-id 全集波次**(`spot_20260822_0810xx_1_12ef3deb…`,295 有效局,`--rec-slots 1`
⇒ 帧通道 20 个 `.dem`)。这是 owner 07:23Z 指示「录像组选局核验优先用全集波次的局」
之后的**第一波全集波次**,也是 14 个「核验记录最少」id 唯一的取证机会。

**本轮命中 owner 优先项 P2**(铁律第 9 条):P2 完成定义第 2 条要求
「与 fieldregen/wandbleed 组成『野区续航』一族,**同一波买 (a) 证据**」——
本波两个 id 都 armed,而**它们此前从未有过任何 (a) 读数**
(`detect.py` 15 个通用检测器里没有任何一个带道具购买/道具使用谓词)。
本轮把这个缺口补上了。

零 EC2 支出;未改任何 bot Lua;未动任何 gate。

---

## 1. 覆盖数字(章程固定栏位)

**宽扫 16/16 局;深查 6 局**(逐帧,含视野与事件流)。

- S3 该波 4 个 run 各 5 个 `.dem` = **20**,减 4 个暖场局 = **16 有效局**。
- `sweep_run.sh` **严格串行**(#102),按 run 分目录,`-interval` **默认 1.0s**。
- **#102 完成度哨兵 4/4 通过**:每个目录 `sweep_summary.md` 存在且
  `games swept: 4` == `games_manifest.jsonl` 4 行。
- **16/16 局 stamp 逐字相同**,armed 串 = 批测台 §U.0 的 18 id,无第 19 个 id:
  `l1trade,l5combo,midtp,suptp,tpcommit,tpdying,lf_rescue,teambrain,ownhalf,overchase,fieldregen,wandbleed,capmono,cmrguard,tpdead,zusult,blinkflee,liondrainstop`
- 侧别配平**完美**:每个种子 2 局 radiant-armed + 2 局 dire-armed(888/895/896/906)。

深查的 6 局:`082001`(WK + PA,armed 腿)、`081919`(zuus armed 腿 + zuus baseline 腿)、
`082024`(ogre_magi baseline 腿)、`083022`(drow_ranger armed 腿)、`083526`、`083656`。

---

## 2. 核验结论

### 2.1 `fieldregen`: **WORKING**(帧证据闭合,归因干净)

**先逐帧。** `20260822_082001_slot1`(s906,radiant=armed),**skeleton_king**:

```
t=598.5 hp=0.403 nw=4658 pos=(-5346, 834)  离家 7414u 最近敌人 4079u
        items=quelling_blade,gauntlets,magic_stick,ironwood_branch,recipe_magic_wand,famango
        ← DAMAGE zuus_thundergods_wrath v=217(全图大招)0.403 → 0.220
t=600.0 PURCHASE → skeleton_king            ← 对线期在 t=600 结束(nw 4658<8000 ⇒ 软延长到 600)
t=601.0 PURCHASE → skeleton_king               买单发生在域成立后的第一/第二个 tick
t=600.5…608.5 hp 0.225→0.262,离家 7825u → **10042u**(**持续远离泉水**)
t=609.2 ITEM item_flask + MODIFIER_ADD modifier_flask_healing + HEAL v=92
t=609.5 items 里出现 flask(信使送达,距买单 ~9s)
t=610.5…613.5 站在 (-6109,4103) 不动、离家 10551u,hp 0.300→0.369,同时在打小兵
```

**这就是 owner P2 要的那条完整链路**:低血 → 离家一万码 → 包里没有任何回复品 →
**买大药** → 信使送达 → **就地喝掉** → 继续打,全程没有回家。

**再聚合(16 局)**:

| 腿 | 域内 episode | 90s 内获得 flask | 其中喝掉 |
|---|---|---|---|
| armed(候选侧) | **7** | **5** | 2 |
| baseline(gate 全关) | **10** | **0** | 0 |

- **供给比 0.70**(armed 域内 episode **更少**,7 vs 10)⇒ 读数不是被供给放大的
  (#92 那类死法的反面);每局绝对次数 armed 0.44 / baseline 0.63。
- 全部 acquisitions:5:0,超几何单尾 **p=0.0034**。
- **收紧到「可归因」口径**:要求域成立后 **≤1.5s 内有本人的 PURCHASE 事件**
  (即买单与域进入时间锁死,排除对线期买入蹲 stash 后被信使补送的情形)——
  4 例满足(`082001` WK t=600.5/买 600.0+601.0;`083526` WK t=627.4/买 627+628;
  `083656` zuus t=627.4/买 627;`081919` zuus t=615.5/买 615.0),
  **4:0,p=0.0147**。第 5 例(`082001` phantom_assassin,acq 608.5)窗口内**无** PURCHASE
  ⇒ **不计入**,如实剔除。

**归因为什么是干净的(不是「形状」是「指纹」)**:`grep item_flask bots/` 逐个读过 ——
`item_purchase_generic.lua` 里**其余全部 flask 买点都在 `if J.IsInLaningPhase() then`
块内**(794-880 行),`883`/`897` 两个 post-300s 块只买真假眼,
`mode_secret_shop_generic.lua:122` 的 `item_flask` 在 **`HaveItemToSell()` 卖出清单**里。
⇒ **对线期之后,整棵出厂树里没有第二条买大药的路径**。基线腿 **0/10** 正是这个结构的
直接测量(章程 #37丙 要求的「对照臂出厂本底率」= **0.00 次/episode**)。

**基线腿的对照帧(排除「他没钱/信使不在」这两个替代解释)** ——
`20260822_082024_slot1`(s896,radiant=armed ⇒ **ogre_magi 在 baseline 腿**):

```
t=602.4…625.4  连续 23 秒 hp 0.313→0.504,离家 7494-9076u
               t=604.4 起最近敌人 3400-7400u(**安全**),身边 2-3 个友军
               items 全程 = tranquil_boots,hand_of_midas,magic_wand,observer_ward,null_talisman
t=615.4        items 里**一次到货 4 件**:shadow_amulet,shawl,smoke_of_deceit,**clarity**
               ⇒ 他有钱、信使在跑、买单机构正常工作 —— **就是没有一件是治疗品**
```
**这一帧同时否掉「没钱」和「没信使」两个替代解释**,把基线腿的 0/10 钉成
**决策缺口**而不是资源缺口。

**armed 腿唯一一次域内未获得 flask**(`083022` drow_ranger t=641.5-650.5):
t=645/650 有两次 PURCHASE(v=160,同值)但 flask 未到;t=651.5 起坐标冻结、
`items` 变空列表持续到 675.5 局末,而**该英雄在 640-700 区间内无 DEATH 事件**。
⇒ **机制未确立**(可能是 gold / stash / stock 三个离线不可观测的门之一,也可能是
观测中断),**如实登记为「未解释」,不替它声称censoring**。

**诚实边界**:`IsThereHealingInStash` / `botGold >= 110` / `GetItemStockCount > 1`
三个子句离线不可观测 ⇒ 我的域比引擎的域**宽**,效应**被稀释**;
所以 armed 5/7 是**下界**,baseline 的 0/10 不受影响(它是全域零)。

### 2.2 `wandbleed`: **WORKING(倾向),证据未闭合**

`wandbleed` 的**自有领地**是「最近可见敌方英雄在 1000u 之外」——
它上面那条出厂规则(`用途1`)已经覆盖了 `nEnemyCount>=1`(1000u 内)的情形。

| 腿 | >1000u 域内 episode | 喝了魔棒/魔杖 | 率 |
|---|---|---|---|
| armed | 130 | 19 | **14.6%** |
| baseline | 156 | 6 | **3.8%** |

超几何单尾 **p=0.00123**,供给比 **0.83**。

**但 1000u 这条列不可直接引用**:1Hz × 300-400 u/s ⇒ 单帧 ±400u
(章程 06:55Z 已登记),`enemy_min` 读 1008u 的 episode 在**真正的施法帧**上
完全可能在 1000u 以内、由出厂规则开火。**加 ±400u 余量重扫**:

| 腿 | >**1400u** 域内 episode | 喝了 | 率 |
|---|---|---|---|
| armed | 85 | 8 | **9.4%** |
| baseline | 94 | 3 | **3.2%** |

**p=0.0774,供给比 0.90 —— 承重列是 >1400u 这一列,它不显著。**
按 08:50Z 自己立的纪律(「报阈值扫描时要写明哪一列承重、哪几列不可判」):
**>1000u 那列不可判(污染),>1400u 那列可判但 n 太小。**

帧证据(armed,>1400u,干净的一例)——`20260822_083727_slot1` **slardar**:
`t=460.4-462.4 hp_min=0.320,最近敌人 9434u`,`t=462.2 ITEM item_magic_wand`。
出厂的三条 wand 规则里,`用途1` 要敌人在 1000u 内(不成立)、
`用途2` 要 `nEnemyCount>=1`(不成立)、`用途3` 要 `charges>=19` 且 6 号槽有东西
—— 只有 `wandbleed` 的「不看距离」子句能解释这一帧。

**基线腿本底 3.2% 不是零** ⇒ 这是**形状不是指纹**(与 fieldregen 相反),
`用途3`(无敌人距离要求)与 `wandlimbo` 之外的路径都能造出同形状事件。
**定级 WORKING(倾向)**,`nCharges >= 5` 离线不可观测是另一层稀释。

---

## 3. 交付物

`tools/batch_test/behavioral/fieldregen_supply.py`(只读离线,新增):

- 两个域**逐字从源码转写**(`item_purchase_generic.lua:776`、
  `ability_item_usage_generic.lua:3321`),docstring 里逐条标出**哪几个子句离线不可观测**;
- `J.IsInLaningPhase()`(`jmz_func.lua:9194`)**精确重建**(turbo、`c2`/`c4` 均未 armed):
  `laning ⇔ t<480 或 (t<600 且 net_worth<8000)` —— `net_worth` 是真实快照字段,
  软延长**逐帧求值**而不是拍一个平的截断;
- **idx 锁**(最早出现时刻口径),本波锁掉 **3092 个副本快照**;
- **#78 守卫**:尸体帧 `items` 为空会让「没有 flask/tango」平凡为真 ⇒ 域要求
  库存非空 **且** 排除 DEATH 后 1.0s(泄漏带 ≤0.30s,3 倍余量),共剔除 77 帧;
- **泉水质心正控**(章程 08:50Z 乙):16 局全部读到
  `|radiant| 9410-9420`、`|dire| 9460-9466`、两心相距 **18873-18883u`;
- **#102 完成度哨兵**,且「games swept」取**冒号后第一个整数**(06:55Z 甲);
- `--wand-band` 参数化(默认 1000 = 出厂半径;1400 = 加 ±400u 单帧余量),
  **两列都打印,报告里写明哪列承重**;
- `--selfcheck`:域内 episode 起始帧不得已持有 flask/tango、`is_laning()` 与源码对拍、
  泉水质心间距下界、episode 结构完整性 —— 本波 **PASS**;
- **armed 串守卫**:多个 sweep 目录 armed 串不一致即 `exit`;`fieldregen`/`wandbleed`
  不在串里即 `exit`(拒绝给一个从没开过的 gate 打分)。

---

## 4. 宽扫层(选点用,不下结论)

16 局全套检测器已跑,`sweep_summary.md` 四份齐。`unpunished_tower_dive` 全波
候选:基线 = 21:28,其余检测器无 5 次以上的单向差。**本轮不据宽扫开任何 issue**
(章程:宽扫只回答「哪些局值得深查」)。

---

## 5. 跨组

**GH #110 追评**(owner P2 的决策侧 issue,球在协同组)—— 本轮给它送三条硬事实:

1. **P2 完成定义第 2 条(fieldregen/wandbleed 的 (a) 证据)已经买到了**,
   `fieldregen` 是 **WORKING** 且归因干净(基线腿 0/10 是结构性的:
   对线期之后整棵出厂树没有第二条买大药的路径);
2. 上一轮登记的「**`flask` 0/22 ⇒ owner 说的买大药当前 buy list 下没有可用对象**」
   **需要收窄**:那是**回家 TP 的 22 个 episode** 的普查,量的是**出厂**行为;
   本轮显示 **armed 的 `fieldregen` 确实把 flask 买进来了**(5 次到货、2 次喝掉)——
   **补给侧不是空的,它只是被 gate 关着**;
3. 因此 P2 完成定义第 1 条(决策侧 gated id = 协同组 09:33Z 落地的 `stayfield`)
   现在有一个**已核验可用的补给侧搭档**,「拦住回家」不会把 bot 钉在线上干耗 ——
   这正是上一轮担心的那一半。

**不新开 issue。** `stayfield` 本波未 armed(它 09:33Z 才落地),(a) 取证要等
下一波把它 arm 进去 —— 这一棒按铁律第 9 条的连带规则**显式交给总监**:
请把 `stayfield` 与 `fieldregen`/`wandbleed` 放**同一波**,本组下轮按同一工具核验。

---

## 6. 验证(铁律第 6 条)

- `.lua` 改动数 = **0** ⇒ 铁律 6 的 Lua 门无适用对象;本容器**未装 `luacheck`/`lua5.1`**
  (**如实记录未假装跑过**)。
- 新工具:`py_compile` 通过;`--selfcheck` **PASS**;两种 `--wand-band` 模式冒烟通过;
  `#102` 哨兵在 4 个真实目录上通过。
- **AWS**:EC2 **$0**(未启动任何实例);S3 只读 ≈ **$0.02**
  (20 `.dem` + 319 `analysis.json` + 若干 `s3 ls`);**未调用 Cost Explorer**。

---

## 7. 已检查清单增量

`spot_20260822_081028/081033/081038/081043_1_12ef3deb…` 的 **16/16 有效局**
(4 个暖场局已排除)。该波 `.dem` 侧**现已 100% 检查完毕**。

---

## 8. 下一轮优先

1. **10:09Z 那波(19 id armed,`--rec-slots 8`)** —— 帧通道第一次从 1/16 提到 8/16,
   预期 `.dem` 数量翻 8 倍,是补 `l5combo` 等长期缺 (a) 的 id 的最好机会;
   注意它的 `.dem` 走 **`dem21/<run>/` 批量前缀**(`sweep_run.sh` 已自动回退)。
2. **`stayfield` 的 (a)**(见 §5,已交棒给总监排波)。
3. `make_fixture.py` 钉 `062551 t=205.5 jakiro` 离线重放 `Think()`(#45 闭合路径,
   **连续第三轮顺延**)。
4. `l5combo` 的 (a)(**连续第十七轮**,请总监按 §AF.3 先例直接裁定降级)。
5. 另三个消费方的 idx 锁 delta(**连续第四轮顺延**)。
6. `axe_blink_domain.py` 等 `axebuyblink` armed 的波次(0810xx / 1009xx **都没有它**)。

---

## 9. 本次会话 token 用量

```
TOKENS total_in=9,078,033 out=108,809 turns=76
```
(统计时刻之后的收尾回合不计入,当量级参考。)
