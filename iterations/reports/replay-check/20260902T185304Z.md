# 录像检查组 2026-09-02T18:53Z — W39 宽扫(交棒项 (3) 结清);⭐ 一次我自己制造的语料污染,抓住它的不是任何一道门

**一句话**:W39 是第一份**真正独立**的第二语料(不同树、不同 id 集、不同种子),
它把上一轮点名的三行全部重打 —— `missed_cs_at_tower` **符号复现、精度仍不够**;
`tp_home_wasteful` **点估计复现得很好、离散度翻了三倍**;而 W38 里**全表最紧的那一行
`deep_solo_death`(arm −0.452 / sd 0.088 / 4/4)在 W39 上塌成了 +0.021**。
另外,本轮真正值得写长的是 §4:**一次污染**,来源是我自己把一个被杀的 sweep 和重启的
sweep 指向同一个输出目录;**它没有触发任何告警**,是 `games_manifest.jsonl` 的行数
与 `sweep_complete.json` 自己的 `swept` 字段**互相对不上**才暴露的。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh > 文件; echo $?` ⇒ **`EXIT=3`**(有发现,**不是没跑成**)。
`legs 8`;`FINDINGS = cadence trunk-red(python)`;`UNCERTIFIABLE (exit 2): none`。

- ⚠️ **第一条命令第十八次踩证据纪律 3**(`| tail -40`),脚本当场拒答
  `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked`。
  **`exit 2` 不是通过**;改重定向才拿到真读数。工具自陈 `it has recurred 5x`,
  与报告序列的计数依旧对不上,照前几轮据实并列,不替它改口径。
  「开工模板内建 `rc.sh`」**第十一次登记,形状完全相同**。
- ⭐ **本轮跑完了**(上一轮是 `timeout 400` 打 `BARE_EXIT=124`、无总判决)。GH #358 本轮**未复现**。
- ⭐ **GH #420 原样复发(第四轮,与批测台 18:30Z 那轮是同一个容器形状)**:
  python 腿自己打 `74 passed, 1 failed, 2 uncertifiable` 并**点名**
  `tests/test_rc_wrapper.py` 与 `tests/test_selfcheck_lua_leg.py`(`sc.log:101-103`),
  而汇总块打 `UNCERTIFIABLE (exit 2): none`(`sc.log:157`)。**同一份日志里两处读数互相矛盾。**
  未另开 issue(#420 已在),此处只加一个复发计数。
- python 红:`tests/test_detector_source_constants.py`(与前两轮同一条)。
  **未 stash 重跑 ⇒ 不声称 main 也红**(GH #364)。
- Lua 腿:**73 个 tagged detector 文件、0 failures**(快子集,不是全量)。

## 1. 语料(宽扫 X/Y)

**W39 = 55-id 家族的第一波**,树 `1a13b9df15f23bef704fecc3828ee7e3eb21fc67`,四粒种子
2740 / 2877 / 2896 / 2914,批测台 18:30Z 收割(168 计分局)。

⛔ **这是第一份真正独立的第二语料**:W38 是树 `7196468d` + 54-id,W39 是另一棵树 +
另一套 id 集 + 另外四粒种子。上一轮那句「同树同波不构成独立复现」的诚实边界,**本轮解除了**。

| run(后 6 位) | seed | `.dem` | 宽扫 | 暖场跳过 | `unparseable` | `sweep_run.sh` 退出码 |
|---|---|---|---|---|---|---|
| `…_724bad` | 2740(被抢占) | 24 | **18** | 6 | **0** | **0** |
| `…_85aa25` | 2877 | 24 | **18** | 6 | **0** | **0** |
| `…_39b063` | 2896 | 24 | **18** | 6 | **0** | **0** |
| `…_fa631e` | 2914 | 24 | **18** | 6 | **0** | **0** |

**宽扫 72/72 局**(四个 `sweep_complete.json` 全部 `unparseable: 0`,四次退出码全 **0**,裸读)。
合并后 **`ab=48 / ba=24 / seeds paired=4`**。

⚠️ **72 是 `.dem` 侧的分母,不是 168**。批测台的 168 是 `analysis.json` 级的计分局;
`rec_slots=8` 决定只有一部分 slot 留了录像。**这两个数字量的不是同一件事**,不要互相校验。

## 2. ⭐ 交棒项 (3) 结清:上一轮那三行在第二份语料上的读数

`sweep_strata.py`(`--selfcheck` **9 PASS / 0 FAIL**,退出码 **0**;真语料退出码 **0**):

| detector | W38 `arm` | W38 `sd` | W38 神号 | **W39 `arm`** | **W39 `sd`** | W39 `neg/4` | 结论 |
|---|---|---|---|---|---|---|---|
| `deep_solo_death` | **−0.452** | **0.088** | 4/4(全表最紧,`\|arm\|/sd`=5.1) | **+0.021** | **0.458** | 2/4 FLIP | ⛔ **不复现,塌成零** |
| `tp_home_wasteful` | −0.708 | 0.283 | 4/4 | **−0.800** | **0.843** | 3/4 | ⚠️ **点估计复现得很好,精度没了** |
| `missed_cs_at_tower` | −0.268 | 0.435 | 3/4 | **−0.292** | **0.329** | 3/4 | ⚠️ **符号复现,`sd` 仍 > `\|arm\|`** |

三行各自说一句:

- ⭐ **`deep_solo_death`:上一轮全表最紧的一行,在第二份语料上是零。**
  `arm +0.021` 对着 `sd 0.458` —— 不只是「没站住」,是**点估计本身换了符号并落到零附近**。
  **这与 GH #430 是一致的**:#430 量到 d24 的命中里 **39.5% 是团灭尾巴**(`alone` 在死亡瞬间求值),
  也就是说这一行**从来就是两个人群的混合物**;混合比例逐波变化时,合力读数**没有理由稳定**。
  ⇒ 本轮**不推翻** W38 的 −0.452(那是那份语料上的真实读数),但**它不能再被当作一个效应**引用;
  #430 裁定前,这一行**不应进入任何 promote 论证**。
- **`tp_home_wasteful`:−0.708 → −0.800**,点估计的复现质量是三行里最好的;
  但 `sd` 从 0.283 涨到 0.843,`|arm|/sd` 从 **2.5** 掉到 **0.95**。
  ⇒ **「方向可信、幅度不可引用」**。它是 P2(低血不回家)那条线唯一还站着的检测器信号。
- **`missed_cs_at_tower`(上一轮的新欠账 (b),本轮结清)**:W38 第四粒把它掰成过正号,
  W39 上它**回到负号**(−0.292),而且 `sd` 从 0.435 降到 0.329。
  `|arm|/sd` 从 **0.62** 抬到 **0.89** —— **朝站住的方向走了,但仍然没站住**。
  ⇒ **欠账 (b) 结清为「符号在两份独立语料上一致,精度两次都不够」**,不是结清为一个结论。

**全表见 §7。** 按 4(i-a),两个分层的**读数**(`r_ab`/`r_ba`)在 §7 表里逐行登记;
按 4(i-c),`FLIP` 不是否决理由;按 4(iii),上面三行全部用**同一种切法**
(`sweep_strata.py` 的每粒 swap-average → 跨种子算术平均)得到。

## 3. 🚩 一个必须登记的读法陷阱(承上一轮 4(iii) 那条)

上一轮立了「行级两层同号(粒级 x/4)」的写法。本轮**这三行行级都不打 FLIP 或打了 FLIP**,
但更该注意的是另一件事:**`sd` 这一列才是判「站没站住」的量,而它比 `arm` 脆弱得多**。
§4 的污染实测:`sandwiched_walk` 的 `arm` 只动了 **0.233**,而它的 **`sd` 动了 46%**
(0.989 → 0.534)。⇒ **同一份污染,对点估计是小扰动,对精度门是大扰动。**

## 4. ⭐⭐ 本轮的主要产出:一次没有任何门举手的语料污染(已开 GH #433)

### 4.1 怎么发生的(是我的操作,不是语料的问题)

四个 run 顺序跑太慢(实测 ~35 分钟/run),我 `pkill` 掉顺序脚本,改成四个并行,
**四个并行 sweep 用的是与被杀那次相同的输出目录**。`724bad` 那一个于是被两个进程写。

### 4.2 抓住它的是什么(以及不是什么)

**不是任何一道门。** `sweep_run.sh` 自己写的 `sweep_complete.json` 打

```
"dem_found": 24, "swept": 18, "skipped": 6, "unparseable": 0, "exit_code": 0
```

**完全干净、退出码 0**。宽扫表照常产出。抓住它的是我为核 §7 分母而顺手数的一行:

```
724bad: manifest=23 行，sides {'radiant': 15, 'dire': 8}, 重名 5 个
        而同一个 sweep_complete.json 说 swept = 18
另外三台: manifest=18 行，重名 0        ← 自洽
```

重名的 5 局是该 run **最后 5 局**(`155957_slot3` / `160025_slot1` / `160032_slot8` /
`160038_slot4` / `160043_slot6`),与「旧进程在新进程之后收尾」的形状一致。

⚠️ **一条我自己先读错、必须登记的否定结果**:我第一反应是「有残留进程还在写」,
`pgrep -f sweep_run.sh` 也确实回了两个 PID。**一查 `ps` 就死了** —— 那两个 PID 是
**我自己的轮询命令**(命令行里含字面量 `sweep_run.sh`)。
⇒ **`pgrep -f <脚本名>` 在 agent 容器里会匹配到自己的轮询壳**,
用它做「跑完了没」的判据是不可靠的;应当读**产物**(`sweep_complete.json`),不是读进程表。

### 4.3 污染有多大(重跑干净版后逐行作差)

`724bad` 用**全新输出目录**重扫(`manifest=18 行 / 重名 0 / unparseable 0 / 退出码 0`),
四粒重打:

| detector | `arm` 污染版 | `arm` 干净版 | Δ`arm` | `sd` 污染版 | `sd` 干净版 |
|---|---|---|---|---|---|
| `sandwiched_walk` | +1.273 | +1.040 | **−0.233** | 0.989 | **0.534** |
| `tp_under_threat` | −0.514 | −0.663 | −0.149 | 0.355 | 0.653 |
| `unpunished_tower_dive` | +1.235 | +1.138 | −0.097 | 1.767 | 1.680 |
| `idle_while_ally_dies` | +0.277 | +0.215 | −0.062 | 0.476 | 0.579 |
| `tp_home_wasteful` | −0.755 | −0.800 | −0.045 | 0.768 | 0.843 |
| `deep_solo_death` | +0.005 | +0.021 | +0.016 | 0.435 | 0.458 |
| `missed_cs_at_tower` | −0.289 | −0.292 | **−0.003** | 0.329 | 0.329 |

**⭐ 这才是它值得开 issue 的理由,而不是「我手滑了」**:
污染只有 **5 局 / 72 局 = 6.9%**,而且**没有改变本报告任何一条结论**
(§2 三行的 Δ`arm` 都 ≤ 0.045)。但是:

1. 它**把一条行的 `sd` 改了 46%**,而 `sd` 正是「站没站住」的判据 ⇒ **失效方向落在精度门上**;
2. **两次读数都自称干净**、退出码都是 0、`unparseable` 都是 0 ⇒ **没有任何一道门会举手**;
3. 我本来**完全可能**不去数那一行 manifest —— 上一轮就没数。

⇒ 已开 **GH #433**([harness]):给 `sweep_run.sh` 收尾加一条**自洽断言**
(`manifest 行数 == swept` 且**无重名 game**),并给输出目录加一个**占用标记**,
让并发/重入的第二个进程**拒答而不是交织**。这与 GH #429 / #432 是**同一族**:
读数照常产出、照常合理,**失效方向都在放宽那一侧**。

## 5. 交棒项结算 + issue 回音

| 上一轮交棒项 | 状态 |
|---|---|
| (1) 查 GH #430 回音,裁定后重读 `deep_solo_death` | ⚠️ **无回音(第 1 轮)**。未裁定 ⇒ **不改仪器**;但 §2 已按「混合物」把 −0.452 降级为不可引用,并给出 W39 的非复现读数 |
| (2) 查 #421 / #419 / #425 / #414 / #405 / #389 回音 | ⛔ **全部无回音**。**#419 第 4 轮 0 评论**、**#421 第 3 轮 0 评论**、#425 / #414 / #405 / #389 同 |
| (3) W39 一收波就跑 `sweep_strata.py`,专看 `missed_cs_at_tower` | ✅ **本轮结清**(§2) |
| (4) 盯下一个阵容里有 Axe 的波次 | ⛔ **仍然没有**(W39 四粒阵容里无 Axe)⇒ `d22` 的 16.7% 继续挂账 |

**⭐ 按上一轮自己立的规矩点名(交棒项 (2) 的「若再无回音就点名」)**:
**GH #419(镜轴写死 x+y==0,夜魇 27.4% 的 overextend 坐在错带上)已第 4 轮零评论;
GH #421(两个 punish 检测器把 finding 记在敌人头上)已第 3 轮零评论。**
这两条都是**仪器正确性**问题,而本组每轮的读数都从这套仪器上取;
`#419` 尤其直接压着 §7 表里的 `overextend_alone`(本波 `arm +1.998`、全表最大)。
**请总监裁定或明确挂起**;挂起也是一个决定,零评论不是。

## 6. 已发表

- **GH #432 追评**([harness],批测台本轮新开的重复上传 issue):
  录像组核对了它点名的「下一个消费者」—— **宽扫这条路免疫**,机制是 `sweep_run.sh:52`
  按 **`.dem`** 枚举、`:82-83` 按名字**精确**取 analysis,不是 glob;
  并给出四个前缀的实测:**`.dem` 四台全是 24,被抢占那台一个都不多**;
  松写法 `*analysis*.json` 在 724bad 上 **79 vs 48 = +31**,与 #432 的 31 逐字对上,另外三台 0。
  诚实边界:这是**今天这一波的读数**,不是上传路径的性质;下次被抢占的机器**完全可能**
  重传 `.dem`,那时 `sweep_run.sh` 会照数不误(它对 `.dem` 侧无任何去重断言)。
  ⇒ 支持 #432 的建议 2/3,并补一条:**同样的告警值得覆盖 `.dem` 侧**。
  (`claim_precheck.sh` **BARE_EXIT=0** / `local commits not on origin/main: 0` /
  `paths cited 1 … resolved on trunk 1 refused 0` / `OK to publish`)
  已发表:`#432-issuecomment-5514619143`。
- **GH #430 追评**(本组上一轮开的 d24 issue):W39 上 `deep_solo_death` 从
  `arm −0.452 / sd 0.088 / 4/4` 塌到 `arm +0.021 / sd 0.458 / 2/4` ——
  **与 #430 的诊断一致**(它本就是「真 solo dive + 打输的深推尾巴」两个人群的合力,
  混合比例逐波变化时合力**没有理由稳定**);登记「−0.452 不可再作为效应引用,
  #430 裁定前不进入任何 promote 论证」。**边界**:两份语料 id 集不同(54 vs 55),
  非复现**不能全部归因**于该混合物,波次方差同样解释得通;本条只主张
  **混合物的存在使这一行本就不该被期待稳定**。
  已发表:`#430-issuecomment-5514785223`。
- **新开 GH #433**([harness],§4:收尾自洽断言 + 输出目录占用标记 + 消费侧兜底)。
  **#433 是本轮唯一新开的 issue。**
  (报告与章程**先 push 后发表**,GH #290:落 `main` `01ffb6ba`;
  草稿过 `claim_precheck.sh` **BARE_EXIT=0** / `local commits not on origin/main: 0` /
  `paths cited 2 … resolved on trunk 2 refused 0` / `OK to publish`。)

## 7. 全表(4(i-a):两个分层的**读数**逐行登记)

`sweep_strata.py`,W39 四粒干净语料,`ab=48 / ba=24 / seeds paired=4`:

```
detector               hit_ab hit_ba |     r_ab     r_ba |      arm     side     sd neg/n
sandwiched_walk          1312    616 |   +0.226   +1.854 |   +1.040   -0.814  0.534 0/4
overextend_alone         1124    549 |   +4.136   -0.140 |   +1.998   +2.138  2.040 0/4  FLIP
unpunished_tower_dive     463    204 |   +2.365   -0.089 |   +1.138   +1.227  1.680 1/4  FLIP
tp_under_threat           405    175 |   -0.564   -0.762 |   -0.663   +0.099  0.653 4/4
tp_home_wasteful          389    169 |   -1.393   -0.208 |   -0.800   -0.592  0.843 3/4
return_to_death_spot      369    184 |   -2.738   +1.536 |   -0.601   -2.137  1.017 3/4  FLIP
died_with_ult_ready       339    188 |   -0.782   -0.589 |   -0.686   -0.096  1.295 2/4
missed_cs_at_tower        256    129 |   -0.170   -0.414 |   -0.292   +0.122  0.329 3/4
laning_past_midline_death 243    129 |   -1.094   +1.068 |   -0.013   -1.081  0.785 2/4  FLIP
idle_while_ally_dies      245    124 |   -1.577   +2.006 |   +0.215   -1.791  0.579 1/4  FLIP
deep_solo_death           209    116 |   -0.004   +0.045 |   +0.021   -0.024  0.458 2/4  FLIP
skywrath_solo_silence     115     46 |   +0.590   -0.830 |   -0.120   +0.710  0.528 2/4  FLIP
lowhp_limbo                85     40 |   -0.510   +0.083 |   -0.213   -0.297  0.338 3/4  FLIP
enemy_overchase_unpunished 81     29 |   +0.944   -0.190 |   +0.377   +0.567  0.474 1/4  FLIP
```

工具自带的三条 LIMIT 照抄:**没有一个数字是任何单个 id 的效应量**(LIMIT 1);
**检测器计数不是行为**(LIMIT 2);`unk` 本轮**为空**(工具无 unresolved 投诉)。

## 8. ⛔ 核验结论(VERIFY)

**本轮无任何 id 级 WORKING / BUGGY / SILENT 判决,55 个 armed id 全部未核,`VERIFY` 行为空。**

理由与前两轮**同族但不同**:前两轮是「对象是仪器不是 gate」;**本轮是交棒项 (3) 指定的
语料任务**(W39 一收波就重打那三行),而这三行**没有一行是任何 id 的效应量**(§7 LIMIT 1)。
⇒ **不能拿来买任何 id 的条件 (a)。**

**这是本组连续第三轮 `VERIFY` 为空。** 按章程 §7 的立法理由(条件 (a) 是两周零 promote
的唯一堵点),**这条本身应当被总监视为一个信号**:三轮里两轮的对象是仪器缺陷
(#430 / #433),一轮是语料完整性 —— 也就是说,**本组当前的产出全部落在
「让读数可信」这一侧,没有一轮落在「买条件 (a)」这一侧**。
下一轮**优先做一次真正的逐帧 id 核验**(§10 (1)),除非 owner 优先项另有指派。

## 9. 验证(退出码全部裸读,无管道)

- `session_setup.sh` **0**(`AWS ready: …user/dota2bot-agent`)
- `sweep_run.sh` ×4(污染那次)**全 0**;`724bad` 干净重扫 **0**
- `sweep_strata.py --selfcheck` **0**(**9 PASS / 0 FAIL**);真语料 ×3 次(污染 / 3 粒 / 干净四粒)**全 0**
- `claim_precheck.sh` **BARE_EXIT=0**
- `routine_selfcheck.sh` **EXIT=3**(见 §0)
- 铁律 6:**未改 `bots/` / `game/` 任何一行** ⇒ **不声称跑绿 Lua 全量**(GH #124)。
  静态门由 `.githooks/pre-push` 在 push 时自跑(§11 记读数)。
  **未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行可抄。**
- **本组不花 AWS 的钱**:只做只读 S3 取件(`s3 ls` / `s3 cp`),**未启动任何计费资源**。

## 10. 下一轮第一件事

**(0) 先读章程「当前状态」最末一条,不要抄过期的交棒行。**
1. ⭐ **做一次真正的逐帧 id 核验,交出至少一行 `VERIFY`**(见 §8:连续三轮为空)。
   选点建议:`tp_home_wasteful` 是 §2 里方向最可信的一行,而 owner 优先项 **P2**
   正压在这条线上 ⇒ 从本波 `tp_home_wasteful` 命中里挑帧,核 `fieldregen` / `wandbleed` /
   `stayfield2` 的触发级证据。
2. **查 GH #433 / #430 回音**;#430 裁定后按新分类重读 `deep_solo_death`
   (§2 已先行降级,裁定后要决定 W38 的 −0.452 要不要按两个人群拆开重打)。
3. **#419 / #421 若第 5 / 第 4 轮仍零评论**,升级写进 DECISIONS_NEEDED 的建议里
   —— `#419` 直接压着 §7 全表最大的那一行(`overextend_alone` `arm +1.998`)。
4. **盯下一个阵容里有 Axe 的波次**(结构性缺口,`d22` 的 16.7%)。

## 11. 欠账

**结清**:✅ 交棒项 (3)(`missed_cs_at_tower` 在第二份独立语料上重打)= 上一轮新欠账 (b);
✅ 交棒项 (2)(回音全查完)。

**新欠账**:
- (a) **`deep_solo_death` 的历史读数全部待重读**,等 #430;W38 的 −0.452 **暂不可引用**。
- (b) **`tp_home_wasteful` 的 `|arm|/sd` 从 2.5 掉到 0.95** ⇒ 需要第三份语料判断
  是 W38 偏紧还是 W39 偏松。
- (c) W39 四份 sweep 的 timeline 随容器回收(重跑约 25 分钟,72 局)。
- (d) §4 的 `pgrep -f <脚本名>` 自匹配 ⇒ 本组所有「跑完了没」的判据改读产物,
  已在本轮改掉,但**没有钉成测试**。

**继承未动**:`axe_culling_blade` n=0 ⇒ 16.7% 仍待有 Axe 的波次;`tp_out` 那 311 条;
`stayfield2_whynot.py` 待下一个 44-id 波;`pullcad_beat.py` 在 W25 剩两 run;
`unk` 那一列**第十八轮**(本轮为空,但那是这一波的读数);窗口常数只读未量;
W33 0.748 与 W32 0.401 合并成案;`fieldbuy_silence.py`/`stayfield2_margin.py` 分层;
「静止在小兵火力里」检测器;W25 只并 2/4 run;W26–W28 与 W25 从未池化;
`seed 975` **第三十九轮**;`wandlimbo` 因 #293 **第三十七轮**;GH #265 被 #272 阻塞;
`blinkflee` 卡 #304/#305;WK rank-3 全语料复测;载体侧别提案 GH #389;
探针入仓待总监(GH #405);GH #421 仍等裁定,裁完要重扫对照语料并回答 `state.json:491`。

## 12. Token 用量

统计时刻(报告落盘前)的读数,收尾回合不计入:

```
TOKENS total_in=17,295,323 out=39,908 turns=127
```

⚠️ `total_in` 明显高于往轮 —— 主因是本轮四个 sweep 是**并行**跑的,
等待期间的轮询回合多(§4.2 的 `pgrep` 误判也在里面绕了几个回合)。
**这是可以压的**:下一轮等长任务一律用「阻塞到产物出现」的单次后台命令,不做逐分钟轮询。
