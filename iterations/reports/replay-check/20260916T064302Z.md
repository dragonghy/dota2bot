# 录像检查组 W78 — 2026-09-16T06:43Z

**工作单元**:执行上一轮(W77)点名的第一件事 —— 对 **`d8_return_to_death_spot`** 与
**`d20_enemy_overchase_unpunished`** 做同款域补课(W77 §一表里唯二「读快照流但当轮未测」的两个)。

**语料**:`spot_20260911_212512_1_6b933bacf447197782a8b6200eb74546c81e5cf8_65af1b` /
局 `20260911_212703_slot7`(seed **11574**)。与 W76/W77 **同一局**,为的是让读数逐位可比。
`.dem` **24,103,680 B**、dump **32,747,756 B**、dumper cache key **`46fe9c6a2b084f9b`** ——
三个都与 W77 逐位相同。**宽扫 1/1、深查 1/1**(逐条记录级)。

⛔ **本轮零 `VERIFY` 行**:买的是量具,不是任何 armed id 的条件 (a)。
`bots/` + `game/` **一行未改**,零新 soak id,零 fixture。
⛔ **零 armed/baseline 对比** ⇒ 铁律 4 §CL (i-a) 的两层读数**本轮不适用**,
**不编造分层**;本轮不给任何 id 定 WORKING/BUGGY/SILENT。

---

## 一、结论先说

**两个都不干净,两个都进禁令。** 当前可用(不被幻象污染的)检测器**仍然只有 2 个**:
`skywrath_solo_silence` 与 `missed_cs_at_tower`。

| 检测器 | as-is | 去幻象 | 机制 |
|---|---|---|---|
| `d8_return_to_death_spot` | **14** | **16** | 截断(假阴 3)+ `state_at` 队友误读(假阳 1) |
| `d20_enemy_overchase_unpunished` | **2** | **1** | `state_at` 误读队友/援军(假阳 1)+ 锚点漂移 3.0s |

⭐ 另外撞出**第三条、与幻象无关**的缺陷(§四):d8 的 `respawn_t` 把**尸体泄漏帧**
读成复活 ⇒ **as-is 14 个命中里 6 个(43%)是「把这次死亡本身报成走回死亡点」**。
⛔ **GH #849 那一修对它效果精确为零**(去幻象后仍是 6/16 = 38%)。

---

## 二、d8:两条机制,各带帧证据

### 2.1 截断 —— `detect.py:471-473` 的 `break`,与 W77 `d23` 的 `run = None` 同型

```python
for s in snaps:
    if s["t"] < respawn_t or s["t"] > respawn_t + RETURN_WINDOW: continue
    if s["hp"] <= 0: break      # <-- 任何一条幻象尸体记录都掐断整个 90s 窗口
```

**4 个死亡的扫描范围被改变,其中 3 个 as-is 在 `respawn_t` 当场夭折、去幻象后跑满 90s**:

| hero | death_t | respawn_t | as-is 扫到 | 去幻象扫到 | 窗口应到 |
|---|---|---|---|---|---|
| luna | 1116.1 | 1121.7 | 1121.7 (截断) | 1124.7 (截断) | 1211.7 |
| luna | 1125.2 | 1174.7 | **1174.7 (截断)** | **1264.7 (跑满)** | 1264.7 |
| pudge | 1330.0 | 1389.7 | **1389.7 (截断)** | **1479.7 (跑满)** | 1479.7 |
| luna | 1420.4 | 1483.7 | **1483.7 (截断)** | **1573.7 (跑满)** | 1573.7 |

**帧证据**(pudge,`respawn_t=1389.7`,发射序逐字):

```
t=1389.7 hp=0    pos=(2474,1823)    <- 幻象尸体(冻住的陈旧槽)
t=1389.7 hp=0    pos=(2318,1584)    <- 幻象尸体
t=1389.7 hp=2738 pos=(-6715,-6574)  <- 真 pudge,在自家泉水复活
t=1390.7 hp=0    pos=(2474,1823)    <- 同一对坐标逐位不变
t=1390.7 hp=0    pos=(2318,1584)
t=1390.7 hp=2738 pos=(-6430,-6309)  <- 真 pudge 在走
```

⇒ 窗口内**第一条**记录就是 hp=0 的幻象 ⇒ `break` ⇒ **这次死亡的 90 秒窗口一帧都没看**。
⭐ 判别子在同一段输出里:`(2474,1823)` 与 `(2318,1584)` **跨帧逐位不变**(冻住的陈旧槽),
而真 pudge 的坐标每帧都在变。

### 2.2 `state_at` 队友误读 —— GH #849 的机制,在 d8 身上的独立第二例

`skeleton_king t=1417.7` 是 **as-is 独有**的命中(去幻象后消失)。同一帧两个读数:

| | luna 的 `state_at` 读数 | 1500u 内队友 | 判定 |
|---|---|---|---|
| as-is | `(6487,-1196) hp=0` **判死** | `[obsidian_destroyer 201u]` = 1 | **FLAG**(<2) |
| 去幻象 | `(4186,3261) hp=41` **活着** | `[luna 587u, obsidian_destroyer 201u]` = 2 | **SKIP**(≥2) |

⇒ 真 luna 活着、距 SK **587u**,却被一条幻象尸体记录顶掉 ⇒ 队友数 1 vs 2 ⇒ 命中真假翻转。
**与 W76 在 `d6` 上量到的完全同一个机制**(`state_at` 因 `:102-122` 的严格 `<` 恒返回同 `t` 的第一条),
本轮是它在**另一个检测器**上的独立复现。

---

## 三、d20:一个假阳 + 一次锚点漂移(⚠️ 这里更正我自己的第一版读法)

⚠️ **按 `(hero, t)` 键直接做差会读成「2 vs 1、零重叠、100% 换血」——那是错的。**
逐帧查过之后,真实构成是:

**(甲) `necrolyte t=1217.7`(as-is 独有)= 货真价实的假阳**,卡在 **solo** 谓词上:

| | necro 自己的记录 | 1400u 内同侧援军 | 判定 |
|---|---|---|---|
| as-is | `(-1696,-5825) depth=7522`(两边**逐位相同**) | `[]` | **solo ⇒ 继续** → 命中 |
| 去幻象 | 同上 | `[drow_ranger]` | **有援军 ⇒ 否决** |

⭐ **判别子**:necro 与 victim `sniper` 的记录在两个时间线上**逐位相同**
(`sniper (-2247,-6109) hp=417 hp_pct=0.139`,两边各只有 1 条记录)⇒
翻转**不可能来自 necro 那一帧**,只能来自**别人**(drow_ranger 的幻象)。
这正是 W76 §一那句「flag 时刻由别人的状态变化决定」的第二个现场。

**(乙) `drow_ranger` 1223.7(去幻象独有)vs 1226.7(as-is 独有)= 同一个 episode,锚点差 3.0s**:
同 chaser、同 victim(`obsidian_destroyer`)、`OVERCHASE_DEDUP = 8.0s` > 3.0s
⇒ **两者互斥,第二个只可能在第一个没触发时出现**。根因仍是幻象:

| t | | defenders(需 ≥2) | 判定 |
|---|---|---|---|
| 1223.7 | as-is | `[skeleton_king]` | FAIL |
| 1223.7 | 去幻象 | `[luna, pudge, skeleton_king]` | PASS |
| 1226.7 | as-is | `[luna, skeleton_king]` | PASS(于是成了 as-is 的锚点) |

⇒ luna 与 pudge 被幻象尸体记录读成「不在/已死」,把 episode 的**起点往后推了 3 秒**。
⛔ **所以 d20 本局的净效应是「一个假阳 + 一次 3.0s 锚点漂移」,不是「整体换血」。**
⭐ 这一条登记下来,因为**键级差分天然会把锚点漂移显示成一去一来两个事件**,
而那正好长得像最强的那种证据。

---

## 四、⭐⭐ 第三条:与幻象无关,且 GH #849 修完仍在

`detect.py:462-466` 取 `respawn_t` = **DEATH 事件之后第一条 `hp>0` 的记录**,
**没有跳过尸体泄漏窗**。本局 **60 个死亡里 12 个(0.20 帧/死亡)**的「复活」距死亡事件
只有 **0.1–0.3 秒**。

**帧证据**(六个命中各一行;`hp_pct` 是那条「复活」记录自己的血量):

| hero | death_t | "respawn_t" | 该帧 hp_pct / 坐标 | 下一条 alive 记录 |
|---|---|---|---|---|
| lich | 595.6 | 595.7 (+0.1s) | **2.2%** @(-1179,-833) | t=621.7 @(7103,6180) hp=1000 |
| necrolyte | 775.6 | 775.7 (+0.1s) | **3.4%** @(-3496,642) | t=812.7 @(6659,6333) hp=1439 |
| skeleton_king | 1005.5 | 1005.7 (+0.2s) | **0.1%** @(-2027,4419) | t=1069.7 @(-6700,-6700) hp=3293 |
| lich | 1065.5 | 1065.7 (+0.2s) | **19%** @(-2907,-203) | t=1106.7 @(7094,6147) hp=1638 |
| necrolyte | 1225.5 | 1225.7 (+0.2s) | **22%** @(-1301,-5309) | t=1289.7 @(6902,6420) hp=2790 |
| skeleton_king | 1417.4 | 1417.7 (+0.3s) | **3.8%** @(4112,3844) | t=1420.7 @(4107,3839) hp=3301 |

**判别子不需要引用复活计时器**:这六条「复活」记录的坐标**就是死亡点**、血量 0.1%–22%;
而**真正的**下一条 alive 记录在**自家泉水**、满血。
⇒ 它们是将死英雄自己的**最后一条活人采样**(1s 采样间隔跨过了死亡瞬刻),不是复活。

于是 d8 拿**死亡帧**当窗口起点,英雄当然「0 秒后、距死亡点 0u」⇒ **这次死亡本身被报成一次走回死亡点**:

- **as-is:14 个命中里 6 个(43%)**是这个形状(`return_dist=0` 且 `t == respawn_t`);
- **去幻象:16 个里 6 个(38%)** —— ⛔ **一个都没少**。

⚠️ **一条当场自我更正**:我的探针把「下一条 alive 记录」一律标注成 `real respawn = fountain`,
**对 `skeleton_king t=1417.4` 那一行是错的** —— 它的下一条 alive 记录在 `(4107,3839)`(原地)、满血 3301,
那是**冥魂大帝的重生(Reincarnation)**,不是泉水复活。主判别子不受影响(3.8% 血的那一帧仍然不是复活),
但那个标注本身是错的,登记在此。⭐ 顺带:重生英雄让「什么算 `respawn_t`」**本身**有歧义,
任何一修都要先说清楚它对重生取哪一条。

### 归属:这**不是**新缺陷,是 **GH #752 已登记的泄漏窗**的第三个消费者

GH #752 的「⚠️ 顺带一条」逐字写着同一个机制,并给了本组自己量过的血:
**0.250 帧/死亡、滞后上界 0.30s、p99 0.370、max 0.386**,修法建议 **`td + 0.6` 起步扫**。
本局独立读数 **0.20 帧/死亡、gap 全部 ≤ 0.3s** ——**同一量级,与那个 0.30s 上界一致**
(⛔ 不写成「复现」:两者的定义不完全相同,#752 数的是死亡区间内的 `hp_pct>0` 帧)。
⭐ **#752 点名的是 `tp_attribution.py` 与 `tpdefend_events.py` 两个文件,
`detect.py` 不在其中** ⇒ 本轮的贡献是**第三个消费者 + 它的血量(43%)**,
不是一个新单。**搜重省下的又是一次错误的归属**:开成新单的话,
「尸体泄漏窗没人跳」这个**真信号**会被拆成两条各自看起来很新的单。

---

## 五、对 GH #849 的补充(范围证据)

W77 已把修点从 `state_at` 上移到 `Timeline.__init__`。本轮为那一修**再添两个受益者**:
`d8`(§2.1 截断 + §2.2 队友误读)与 `d20`(§三 solo/defenders 误读)。
⛔ 但 §四那一条**不在它的射程内**,两修必须**分别验收**,
否则 `d8` 会从「43% 假阳 + 若干假阴」变成「38% 假阳」,而**看起来像是修好了**。

---

## 六、禁令状态(本轮再收紧)

| 状态 | 检测器 |
|---|---|
| ✅ 可用 | `skywrath_solo_silence`、`missed_cs_at_tower` |
| ⛔ 禁用(幻象) | `d6`、`d8`、`d20`、`d23_lowhp_limbo`,+ W76/W77 已列的其余 |

⚠️ `d23_lowhp_limbo` **正是 owner 优先项 P2 的那把尺子**(W77 §六),本轮不变。
⛔ 在 GH #849 与 GH #752 两修落地前,**不得用 d8 / d20 的计数给任何 id 定 WORKING/SILENT**。

---

## 七、成本三段(RULING 48)

**零 EC2 / 零 CE / S3 读取 3 个对象(出网未计价)**
= 1 `.dem`(24,103,680 B)+ 1 `.demclaim.json`(436 B)+ 1 dumper 二进制(**cache HIT**,
key `46fe9c6a2b084f9b`);另 **9 次 `s3 ls`**(其中 3 次 `--recursive`;⚠️ LIST 不是对象,单独登记)。
⛔ **不写「零支出」。**

---

## 八、工具坑 / 开工自检

- ⛔ **开工自检连续第三轮被它自己挡下**:第一条命令误接管道
  (`REFUSED: … stdout is a pipe; exit 2, nothing checked`,**它自报第 5 次复发**);
  改重定向后**又**撞第二道闸(`REFUSED: … running under timeout`,**它自报第 3 轮复发**)。
  ⭐ **这两道闸是同一条纪律的两个包装**,而我一轮之内两个都踩了。
  最终按它自己给的处方跑:`nohup … > /tmp/sc.log 2>&1 &`,**不套 `timeout`、不接管道**。
- ⚠️ **harness 后台任务通知报的是 wrapper 的退出码**(W77 §八已立):本轮那条
  `completed (exit code 0)` 指的是 `nohup` 那一行,**不是自检的判决**;判决只在日志末行。
- ⛔⛔ **本轮最贵的一条,是我自己制造的:我判自检「已经死了」,而它当时活得好好的。**
  现场:日志停在 `=== trunk health (python test suite) ===` 不再增长,我跑
  **`pgrep -c routine_selfcheck.sh`** 得到 **0**,于是写下「进程已消失 + 无终行 ⇒ 它死了」,
  并**据此重新起了第二个自检**。⭐ **判别子是 `pgrep -f`**:`pgrep` 不带 `-f` 只匹配
  **进程名**,而这些进程的名字是 **`bash`** ⇒ 那个 `0` 从来不是「没有自检在跑」,
  是「没有进程叫 routine_selfcheck.sh」。实测 `ps` 打脸:**PID 1567 已跑 9 分 06 秒、
  `/proc/1567/fd/1` 逐字指向 `/tmp/sc.log`**。
  📌 **这正是 W76 与 W77 连续两轮登记的同一条纪律的第三次触发** ——
  用「**我此刻看不到它**」去满足「**它不在了**」;前两次的包装是「还没打出终行」,
  这次的包装是**一个测错了变量的 `pgrep`**。⭐ **两次的正解是同一句:轮询到终行真的出现。**
  ⚠️ **而这一次它不只是读错,它让我动了手**:第二个自检(PID 7731)与第一个并发了
  **约 11 秒**,**那正是 GH #848 的染红机制**(两个自检抢写 `bots/Customize/soak_side.lua`)。
  发现后立刻 `kill -TERM 7731`,复核:**7731 gone**、1567/3000 仍在、
  **`bots/Customize/soak_side.lua` 不存在(树是干净的)** ⇒ 本轮**没有**产生 #848 那类假红,
  但**那是因为杀得快,不是因为判断对**。⛔ 不许把这一条读成「并发无害」。
- ⛔ **`behav-dump` 不收 `-in/-out` 旗标**(试了,`flag provided but not defined: -in`,EXIT=2);
  正确用法是**位置参数 + stdout 重定向**。⭐ 判别子是 `wc -c`:EXIT=2 那次 `tl_A.json` **根本不存在**。
- ⛔ **`get_dumper.sh` 必须用绝对路径调用**:在它自己的目录里 `bash get_dumper.sh` 会
  `flock: failed to execute get_dumper.sh: No such file or directory`(EXIT=**69**)——
  它内部用 `flock` 重入自身,而工具会把 cwd 重置。
- **自检读数**:见 §九(收尾时补)。

---

## 九、收尾:自检判决 / push 三行 / issue / token

(收尾时填。)
