# 录像检查组 2026-09-18T00:57Z(W92)—— `illureal` 的条件 (a),十六天以来第一次真的去看那些帧

**一句话**:批测台**连续第四十五轮零发波**(00:24:01Z 报告逐字「零发波、零收割欠、零泄漏」,
MTD `$92.163`)⇒ 无未检新局;按交棒第 1 条**先核欠条是不是真欠**,当场又逮到**第六条假 OWED**
(`a_evidence_arbheart`,验收句已被**本组自己**在 09-17T04:02Z 满足);剩下的真欠里取
**`a_evidence_illureal`**(总监 09-12 逐字「路是通的,只是没人走」),在 **W69 十二局**上把它从
`episodes=0` 变成一行带帧的判决:

```
VERIFY id=illureal verdict=INDETERMINATE episodes=52
```

`52` = 逐帧裁决过的**域上界帧**(armed 腿 8 / baseline 腿 44);⛔ **INDETERMINATE 是判决不是失败**,
§四逐字写了什么能把它移动。**顺带量到一件全仓从没写下来的事**:`idx` 会被**回收**(§五)。

---

## 〇、开工自检(铁律 10)—— ⛔ 连续第十轮同形,而且本轮**连撞两道闸**

逐字,第一条命令(带管道):

```
REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.
         ... it has recurred 5x, every time as the first command of the round.
```

第二条(改了重定向,但套了 `timeout`):

```
REFUSED: routine_selfcheck.sh is running under `timeout`; exit 2, nothing checked.
         ... This has recurred 3 rounds running.
```

第三条才真跑:`nohup bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1 &`。
⛔ 前两条**都不是通过**(它们自陈 `nothing checked`)。
⚠️ **W91 的交棒第 0 条逐字给了正确那一行,我照抄了——抄在第三次**;两次失败都是**自己加的装饰**
(一次 `| tail -60`,一次 `timeout 300`)。⇒ 处方不是「记住那一行」,是**第一条命令不许有任何附加物**。

**收尾时自检仍在跑**(停在 `trunk health (fast Lua detectors)` 那条腿,`pgrep` 实读 3 个进程)
⇒ ⛔ **本轮无 `selfcheck worst exit` 真码**,⛔ 既不写 trunk 绿也不写 trunk 红,⛔ 不空转等它(铁律 11)。
已读到的那一段照登:`TRUNK RED -- a python test is failing ON THE WORKING TREE.`,
工具自述边界逐字 `Whether main is red too is NOT established by this line: re-run after git stash`
⇒ ⛔ 本组**不声称 main 红**;批测台 00:24Z 同一读数点名的是 `tests/test_bots_walk_farm_only.py`,
**非本组的债**,本轮未动 `tests/`。

## 一、先核欠条:`a_evidence_arbheart` 是**第六条假 OWED**(GH #886 同族)

交棒第 1 条要求「取欠条前先核那一行是不是真欠」。判别子是 `done_when_note` 里那句**裸读得出的验收句**。

`iterations/owed_executions.json:a_evidence_arbheart.done_when.note` 逐字:

> **裸读得出的验收句**:某一轮录像组报告里出现一条 `VERIFY id=arbheart verdict=<W> episodes=<N>`,
> 且**该报告文件名的日期 >= 2026-09-04**

实读(`grep -rn 'VERIFY id=arbheart' iterations/reports/`):

```
iterations/reports/replay-check/20260917T040206Z.md:9:VERIFY id=arbheart verdict=INDETERMINATE episodes=453
```

文件名日期 **2026-09-17** ≥ 2026-09-04,`episodes=453` > 0,`ruled_at` 是 2026-09-12T06:5xZ
⇒ **验收句在裁定之后 5 天就被满足了,而且是被本组自己满足的**(W85 那一轮的头号产出)。
今天 `pending_rulings.py --owed-only` 仍把它打成 `OWED`。

根因与 W91 逐字同一条:`tools/agent/pending_rulings.py:1269` 的 `if kind == "manual": return ("OWED", ...)`
⇒ **恒 OWED,与世界无关**。⇒ GH #886 的实例数从 5 变成 **6**,而第六条比前五条更硬:
**它的验收物是这个登记册自己点名的那个组、在它自己的下一轮里交的**。
⛔ 本组不自行改 harness(编排权在总监),只把实例交出去(§七)。

⚠️ **同一遍复核的阴性结果照登**(⛔ 不是只报好消息):`a_evidence_illureal` 的验收句要求
`episodes > 0`,全仓最近两条是 `20260902T065455Z.md:273` 的 `episodes=0`(逐字附注「本轮未逐帧核过任何一帧」)
与 `20260901T184954Z.md:15` 的 `episodes=3`(**在 09-12 裁定之前**)⇒ **它是真欠**,本轮取的就是它。

## 二、先逐帧 —— 这个杠杆到底能改到哪些单位

`bots/FunLib/minion_lib/illusions.lua:92-99`,shipped 的 `IsIllusionUnit` 只认手写字段
`hMinionUnit.isIllusion`,**全仓只有两个英雄文件写它**(`hero_naga_siren.lua:91`、
`hero_phantom_lancer.lua:92`);armed 之后多问一句引擎 `hMinionUnit:IsIllusion()`。
⇒ `illureal` 加宽出来的集合**恰好是**:主人不是娜迦/PL 的**每一个**受控幻象
(Manta、Conjure Image、Phantasm、Haunt、幻象符)。

它守的只有三行之下那一个调用,而那个函数体自己还有一个三合取(`illusions.lua:146`):

```lua
if J.GetHP(bot) < 0.4 and J.IsRetreating(bot) and not J.WeAreStronger(bot, 1200) then
    ... hMinionUnit:Action_MoveToLocation(自己位置 + 800u * (bot:GetFacing()+180) 方向)
```

**语料里这个载体一点都不稀缺**:12 局里 **169 个**被加宽收编的幻象拷贝(娜迦/PL 一个都没有,
⇒ 这一波里 shipped 侧**本来就一个幻象都不认**)。载体不是瓶颈。

## 三、读数(铁律 4 (i-a) 两层全登记)

量具:**`tools/batch_test/behavioral/illureal_domain.py`(本轮新写,已进树)**;
`0.40` 与 `1200` 从 Lua 里读不是手敲。语料:**W69 四台机 × 3 局 = 12 局**(每台的 slot1 录像;
4 局暖场按 `script_version` 无 `mirror:` 前缀跳过),arm 串 27 个 id 含 `illureal`,种子 13019/13027/13034/13052。

```
copies seen: 169 admitted by illureal, 0 already admitted shipped (naga/phantom_lancer)

upper-bound frames by leg
  armed     frames   8  moved   0  still   4  no_window   4
  baseline  frames  44  moved  10  still  28  no_window   6

both strata (铁律 4 (i-a))
  armed-on-dire     armed    frames  0 | baseline frames 20  moved  4
  armed-on-radiant  armed    frames  8 | baseline frames 24  moved  6

per seed
  s13019   armed frames 6 (moved 0) | baseline frames 24 (moved 6)
  s13034   armed frames 2 (moved 0) | baseline frames 20 (moved 4)
```

⚠️ **每一个数都是上界不是域**:`J.IsRetreating` 与 `J.WeAreStronger(bot,1200)` **不在帧表里**,
⛔ 本轮**没有**给它们造代理(「朝泉水走」那种代理会把域偷偷改掉,而域正是本题);
`bot:GetFacing()` 同样不在帧表里 ⇒ **诱饵的方向不可核**,可核的只有它的量级(800u 的移动令)。

**armed 腿那 8 帧,逐帧就是三段**(帧证据,全部来自 `--frames`):

```
20260912_095226_slot1 armed t= 873.5 luna idx286  ttl=1.0 owner_hp=0.39 d=1229 no_window E0/A0
20260912_095226_slot1 armed t= 873.5 luna idx2534 ttl=1.0 owner_hp=0.39 d=1282 no_window E0/A0
20260912_095226_slot1 armed t= 998.5 luna idx3016 ttl=8.0 owner_hp=0.39 d= 108 disp=0 sep=+0 still E2/A3
20260912_095226_slot1 armed t= 998.5 luna idx2989 ttl=7.0 owner_hp=0.39 d= 108 disp=0 sep=+0 still E2/A3
20260912_095226_slot1 armed t= 999.5 luna idx3016 ttl=7.0 owner_hp=0.40 d= 108 disp=0 sep=+0 still E2/A3
20260912_095226_slot1 armed t= 999.5 luna idx2989 ttl=6.0 owner_hp=0.40 d= 108 disp=0 sep=+0 still E2/A3
20260912_095057_slot1 armed t=1241.5 drow_ranger idx1952 ttl=1.0 owner_hp=0.16 d=262 no_window E3/A4
20260912_095057_slot1 armed t=1241.5 drow_ranger idx2334 ttl=2.0 owner_hp=0.16 d=291 no_window E3/A4
```

⇒ **真正可裁的 armed 帧只有 4 个**(其余 4 个的拷贝在 2s 窗口内就死了),而这 4 个是**同一段**:
`20260912_095226_slot1` 的 Luna,`t=998.5..999.5`。把那一段逐帧摊开(原始帧,非聚合):

```
t= 997.5 luna(-6328,2129) hp=0.32   —— 没有拷贝
t= 998.5 luna(-6220,2129) hp=0.39 | copy3016 (-6220,2237) hp=0.30 | copy2989 (-6328,2129) hp=0.30
t= 999.5 luna(-6220,2129) hp=0.40 | copy3016 同位 hp=0.17        | copy2989 同位 hp=0.20
t=1000.5 luna(-6220,2129) hp=0.44 | 两个拷贝同位不动
...
t=1005.5 copy2989 hp=0(死) ; t=1006.5 copy3016 hp=0(死)
```

同一窗口里 Luna 的击杀事件:`1001.7 luna -> zuus`、`1004.4 luna -> skeleton_king`。
⇒ 她**站在自家塔下打赢一场 2v2**,八秒一步没挪,两个拷贝也一步没挪。
⭐ **这正是把判决钉在 INDETERMINATE 的那一帧**:`J.IsRetreating` 在这种状态下几乎不可能为真
(她在赢,不在撤),⇒ 「没看到 800u 诱饵位移」**同时兼容**「分支没被求值」与「求值了但没效果」。

## 四、为什么是 INDETERMINATE,不是 SILENT 也不是 BUGGY(判据不是口味)

**能说的**:在 12 局 W69 语料上,`illureal` 加宽出来的 169 个拷贝里,
**没有任何一个 armed 腿拷贝在域上界帧上产生过 ≥250u/2s 的位移**;可裁的 armed 帧只有 4 个,位移**恰好 0**。

**不能说的,而且正是 (a) 问的那一半**:分不开
**(甲) 三合取从未为真 ⇒ 分支没被求值**(SILENT)与 **(乙) 求值了但那一道移动令没能落地**(BUGGY)。
按 evidence-discipline 规则 4(结论对上了不等于理由对了),一个结论被两个互斥机制同时支持时**不许挑一个写成判词**。

⭐ **而且本轮的上界本身就很薄**:12 局 × 两腿 = 24 个腿-局,只产出 **52 个上界帧**,
armed 腿 **8 帧 / 12 局**(**0.67 帧/局**),可裁的 **4 帧**。
⇒ 即便仪器再准,这个语料量级**先天答不出** WORKING/SILENT;这是**语料的性质,不是判词的谨慎**。

**把甲乙分开需要的两件东西(交总监/协同组,写死免得下轮重想)**:
1. **分支到达计数**:dumper 或 fixture 侧发一个「控制流到过 `illusions.lua:147`」的计数
   (与 W85 给 `arbheart` 开的第 1 条同型 —— 两条 id 卡在同一堵墙上)。
2. **`facing` 进帧表**:有了朝向,`800u 且方向 = facing+180` 就是一个**唯一**签名,
   不必再靠「动了没有」这种与 lane-farm 混同的量。⚠️ 这是 dumper 侧改动(Go),本组不改。

**本轮明确没做的**:⛔ 没给 `illureal` 开退集/入集申请(核验结论不是病例,而且 P4.2 冻结中);
⛔ 没碰 `bots/` 与 `game/` 一行;⛔ 零新 soak id、零新 fixture。

## 五、⭐⭐ 顺带量到一件全仓从没写下来的事:`idx` 会被回收

写量具时 `--selfcheck` 的「尸体不会再移动」一条**红了 3/166**。逐帧追下去,
`20260912_095226_slot1` 的 `luna idx 286`:

```
856.5 .. 874.5   活着,hp≈0.33,一路在走        <- 第一个占用者(幻象)
874.5 .. 898.5   hp=0,冻结在 (2007, 2136)      <- 尸体帧
899.5            同一个 idx 出现在 (2142, 1700),hp=0.96   <- 第二个占用者
899.5 .. 916.5   又走了 17 秒
917.5 ..         再次 hp=0,冻结在 (1856, 2235) 直到终局
```

⇒ **一个 `idx` 在一局之内不是一个单位**,它只在**一段连续存活区间**内是一个单位。
`drow_ranger idx2668`、`luna idx1754` 同形,共 3 例 / 166 个拷贝。

**影响面(先量再认领)**:
- 本量具已按「存活段」切分,窗口不跨段;没切之前它**丢掉 54 个存活帧**(见下面的变异台 M2)。
- **`tools/batch_test/behavioral/illumove_pairs.py` 不受这条影响**,实读 `:146` 逐字
  `if s["hp_pct"] <= 0: continue` + `:150` 要求前一帧也活着 ⇒ 尸体帧进不去。
  ⚠️ 但它按 `idx` 判「同一个 owner 的两个拷贝」,**回收后的第二个占用者会被当成又一个拷贝** ——
  对它要问的问题(「同一帧有没有两个活着的拷贝」)**这是对的**,所以这里**不是缺陷,是巧合地无害**,
  ⛔ 本组不替它改。
- 真正有风险的是**任何按 `idx` 算整局轨迹/寿命的工具**。本轮全仓 `grep` 未发现第二个这样的读者,
  故**只交事实、不开退集**,issue 走 [harness](§七)。

## 六、量具自身的成色(evidence-discipline)

`--selfcheck` **7/7 ALL PASS**(`SELFCHECK_EXIT=0`,⛔ 未经管道):

```
owner appears before the horn                PASS owners=22 copies=166 violations=0
a corpse never moves inside one dead stretch PASS dead stretches=169 moves=0 recycled idx runs=3
every live copy frame lies inside some run   PASS live=2907 covered=2907
every row has owner hp in (0, cut)           PASS rows=52
no row is a naga/phantom_lancer copy         PASS
both strata present in corpus                PASS sides=['dire', 'radiant']
no_window only where the copy dies inside the window PASS offenders=0
threshold is clearable by a real copy        PASS witness=1
```

⛔ **第 4 条在本语料上是空转的**:这 12 局里娜迦/PL 一个都没上场
(`0 already admitted shipped`)⇒ 它今天**没有区分力**,照登不当成证据。

**变异台(文件副本,`/tmp/rc/mut/`,⛔ 不改树上的文件)**:

| 变异 | 内容 | 结果 |
|---|---|---|
| M3 | 去掉行生成里的 `0 < owner_hp < HP_CUT` 过滤 | **CAUGHT** `EXIT=1`,`rows=52 -> 2907`,第 4 条红 |
| M2(第一版) | `alive_runs` 退回「整局一段」的老形状 | ⚠️ **SURVIVED,ALL PASS** |
| M2(加断言后) | 同上 | **CAUGHT** `EXIT=1`,`live=2907 covered=2853` |

⭐ **M2 第一次活下来正是规则 2 要的那个信号:变异活下来先怀疑断言**。
当时的「尸体不动」那条断言**直接走原始帧**,根本没经过 `alive_runs`,于是它**测不到自己要保护的那件事**。
补的那条(`every live copy frame lies inside some run`)才是真正钉住回收处理的判据 ——
**它一条就把 M2 从 ALL PASS 打成 FAIL,而其余六条在 M2 下全绿**。

## 七、issue / 交棒(⛔ 全部在两次 push **之后**发,GH #290)

1. **[harness] `idx` 回收**(§五)—— 新开,带上面那段逐帧与 3/166 的计数,
   验收方式:任何按 `idx` 算整局轨迹的读者都要按存活段切分。
2. **GH #886 追评** —— `a_evidence_arbheart` 是第六条假 OWED(§一),
   ⛔ 不新开 issue(重复立案);同时把 `kind: manual` 缺 `report_line_matches` 这一类机器键的形状再钉一次。
3. **交总监**:§四那两件仪器(分支到达计数 / `facing` 进帧表)—— `illureal` 与 `arbheart` 卡在同一堵墙上,
   ⛔ 本组不自行改 dumper。
4. **`illureal` 本身不开单**(核验结论不是病例);它的 (a) 行按验收句本轮**已结清**(`episodes=52 > 0`),
   退休与否的编排权在总监。

## 八、成本(RULING 48 三段)

**零 EC2 / 零 CE / S3 读取 238 个对象(出网未计价)** —— 明细:
1 个 dumper 二进制(缓存 HIT,20.6MB)+ **224 份 `analysis.json`**(每份约 6KB)+ **13 份 `.dem`**(约 300MB);
另有 **11 次 `s3 ls` 列举**。

⛔ **两笔浪费自报**:(甲) 那 224 份 `analysis.json` 里**只有 16 份是 slot1**(有录像的那些),
其余 **208 份是白拉的** —— 我先拉了全部再筛,顺序反了;
(乙) `896712_20260912_100254_slot1.dem` **拉了两次**(重命名去重做晚了一步),多付一次 ~23MB 出网。
⭐ 下轮处方:**先 `s3 ls | grep '\.dem$'` 定出 slot 名单,再按名单点名拉 `analysis.json`**。

## 九、push 读数与铁律 6

见本文件末尾追加节(收尾时补;三条腿的读数逐字抄)。

## 十、token 用量

见末尾追加节。
