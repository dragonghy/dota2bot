# 录像检查组 2026-09-03T07:20Z — 收批测台的 `ckpush` 交棒:**域非空,SS-DT.4 的 UNINTERPRETABLE 解除**,
# 并逐帧钉住一次**只有推塔分支能产生**的 Phantasm;同时**证伪了源码注释里那句立案句**

**本轮一句话**:批测台 06:15Z 说「**缺的是检测器,不是载体**」,本轮把那个检测器建出来
(`ckpush_domain.py`,12 PASS 自检 + 20 条钉死测试 + **6 个变异体全部被杀**),在 W41 的
**82 局 `.dem` 语料**上读到 **40 个域帧 / 15 局** ⇒ **`ckpush` 的域非空,SILENT 被否决**;
并在 `20260903_040014_slot4` t=335.5 逐帧排除掉 `X.ConsiderR` 的**另外三条分支**,
钉住一次**只能由推塔分支产生**的 Phantasm(t=335.8 施法)—— 那正是 `ckpush` armed 会移除的行为。

**`VERIFY id=ckpush verdict=WORKING episodes=40`**(判据与诚实边界见 §4.4)。
**`roshdist` 本轮未动**,棒仍在本组(§7)。

---

## 0. 语料与取件

W41,57-id 家族**第一波**,树 `694c31fe16b782091c7ea2d21be7160b1b2b0ebf`,四台按需机:

| run | seed | swept | manifest 行 | unparseable | 退出码 |
|---|---|---|---|---|---|
| `spot_20260903_033154_…_15a2fe` | 3017 | **23** | 23 | **0** | **0** |
| `spot_20260903_033158_…_0af9fd` | 3030 | **19** | 19 | **0** | **0** |
| `spot_20260903_033202_…_1197cd` | 3161 | **22** | 22 | **0** | **0** |
| `spot_20260903_033207_…_d87d45` | 3329 | **18** | 18 | **0** | **0** |
| 合计 | | **82** | 82 | **0** | 全 0 |

四路并行、**各自独立输出目录**(GH #433)。`manifest 行数 == timelines 数`逐 run 自洽。

⚠️ **82 是 `.dem` 侧分母,不是批测台的 223 / 199**(`analysis.json` 级计分局)——
`rec_slots=8` 只录一半槽位,两个分母不要互相校验。

**宽扫 82/82 局(100%);深查(逐帧)4 局全帧还原 + 15 局域帧级核对。**

### 0.1 ⛔ GH #444 在 W41 **原样复发**(独立第二例,新波次)

上一轮为跨 run 同名开的 #444,在 W41 上**又出现 4 例**:

```
20260903_033347_slot7   sw_15a2fe(seed 3017, side=radiant)  32,848,642 B
                        sw_0af9fd(seed 3030, side=radiant)  31,717,583 B
20260903_033357_slot8 / 20260903_034727_slot6 / 20260903_040032_slot7   同形
```

**是不同的对局**(不同种子、不同字节数),只是不同实例在同一挂钟秒开同一 slot。
**本轮的工具对此免疫**:`--run <timelines_dir>:<manifest>` 把每份 manifest **绑定到它自己的目录**,
一局只与自己 run 的 manifest 相遇,后覆盖先在构造上不可能发生。
⇒ **#444 不是 W40 的偶发,是这套取件方式的稳定属性**;已在 #444 追评(§6)。

顺带一个**巧合不是免疫**的登记:本波这 4 例里,`20260903_040032_slot7` 两份**都是 baseline 腿**,
所以即便用旧的合并口径也不会串号。**「这次没打到」不是「这条缺陷无害」**(与上一轮 `EXCLUSIVE 14/9`
恰好没变是同一句话)。

---

## 1. `ckpush` 是什么 —— **它是一个 SELECTION,不是一个 disjunction**

`bots/BotLib/hero_chaos_knight.lua:468`:

```lua
function X.GetPushCommitTime()
    if J.IsModeTurbo() and J.IsSoakCandidate( 'ckpush' ) then return 8 * 60 end
    return 8 * 30
end
```

唯一消费点在 `X.ConsiderR` 的推塔分支(`:521`):

```lua
if J.IsPushing( bot ) and DotaTime() > X.GetPushCommitTime() then
    if ( #nNearbyEnemyTowers >= 1 or #nNearbyEnemyBarracks >= 1 )
        and #nNearbyAlliedCreeps >= 2 then return BOT_ACTION_DESIRE_HIGH end
end
```

⇒ armed 与 baseline **只在 `240 < DotaTime() < 480` 这一段有分歧**,别处逐位相同。

**⭐ 方向要说清楚,否则整张表会被读反**:armed 返回的是**更大**的阈值(480),
所以 **armed 腿是收紧的那一腿** —— 上 `ckpush` 是**移除**这一段里的推塔大招。
「armed = 行为更多」在这个 id 上是**反的**。已把这一条钉进 `tests/test_ckpush_domain.py`,
源码若把 selection 翻向,测试会红。

---

## 2. 结构事实:**chaos_knight 在 82/82 局都是天辉** —— 这不是 swap-and-average

```
ck 所在物理队计数(2=天辉 3=夜魇): {2: 82}
(armed 侧, ck 队) -> {('radiant', 2): 48, ('dire', 2): 34}
```

这是镜像牌序的设计(英雄→物理队固定,swap 的是 armed 腿),与上一轮
「辉耀 20,167 条伤害 100% 出自夜魇」是**同一个结构**的另一面。后果**必须连读**:

- `ab` 层 ⇒ CK **armed**;`ba` 层 ⇒ CK **baseline**。**CK 自己的侧偏在两腿间是常数**
  (他永远天辉),这一点对本 id **有利**;
- 但因此 **armed/baseline 的对比就是 `ab 层 vs ba 层`**,而两层里 **CK 的对手**的 armed 状态
  **同时翻转**(ab 层敌方是 baseline,ba 层敌方是 armed)⇒ **整串 57 个 id 都混在这个对比里**;
- 两层局数还不等(48 vs 34)。

⇒ 按铁律 4(i-b),下面每个计数**两层都登记**,但**任何跨腿差值都不写进结论**;
本轮的结论**只建立在帧证据上**,不建立在这两列的差上。**这一段请连引。**

---

## 3. 宽扫读数(逐 run 口径,`ckpush_domain.py`,裸退出码 0)

```
=== ckpush domain, 240 < t < 480 (LIMIT 1: superset on IsPushing) ===
stratum  leg        games  band_fr  learned   ready  struct  creeps  DOMAIN shadowed>=1 shadowed>=2
ab       armed         48    11520     6340    2717      27      18      18          15           6
ab       baseline       0        0        0       0       0       0       0           0           0
ba       armed          0        0        0       0       0       0       0           0           0
ba       baseline      34     8160     4080    1402      23      22      22          17           7

=== Phantasm CASTS inside the band (events, not frames) ===
  ab  armed     casts=62      (48 局 => 1.292 / 局)
  ba  baseline  casts=44      (34 局 => 1.294 / 局)

grid holes (LIMIT 4): no_building_sample=0  no_creep_sample=0
fountain proxy removed (LIMIT 5): 0
LIMIT 3 mana-blind: domain frame mp_pct min=0.16 med=0.74 max=1.00
```

**`ab baseline` / `ba armed` 两行的 0 是 §2 那个结构事实,不是缺数据。**

**⭐ 域非空:40 个域帧,分布在 15 局**(逐局见 §5)。这直接回答 SS-DT.4 的问题:
批测台按义务返回的 `UNINTERPRETABLE`(「若该格帧数为 0,不许读成 tested-no-effect」)
**现在有读数了,而且不是 0**。

**⚠️ 聚合层面的施法率是一个死平的 null**:1.292 vs 1.294 每局。
**这不矛盾** —— 段内施法包含 `ConsiderR` 全部四条分支,推塔分支只占极小一份(§4)。
**这个 null 是效应量的上界证据,不是 SILENT 的证据。**

---

## 4. 逐帧(硬规则:先逐帧后聚合)

`X.ConsiderR` 一共只有**四条**返回 HIGH 的路(`:481`–`:540`,已逐行读过):

| 分支 | 判据 | 需要的敌人距离 |
|---|---|---|
| B1 `IsGoingOnSomeone` | 目标在 `nCastRange` 内 | ≤ **1200**(有 W 时)/ 900 |
| B2 `IsInTeamFight(bot,1200)` | **≥2 名 ATTACK 模式队友在 1200 内**,**且** `#J.GetEnemyList(bot,1600) >= 2` | **≥2 敌人 ≤ 1600** |
| **B3 推塔(本 id)** | `IsPushing` × 时间门 × 塔/兵营 × ≥2 己方小兵 | — |
| B4 撤退 | `RETREAT` × hp≥0.5 × 最近敌人 ≤700 | ≤ **700** |

⚠️ **B2 的 `IsInTeamFight` 数的是队友不是敌人**(`jmz_func.lua:1453`,`J.GetNearbyHeroes(bot,r,false,…)`),
敌人那一半来自**同一行的 `#nNearbyEnemyHeroes >= 2`,半径 1600 不是 1200**。
本轮初稿的遮蔽代理写的是 1200,**在钉帧前改成 1600 重算**;下面的排除按 1600 做。

### 4.1 ⭐ 钉帧:`20260903_040014_slot4` t=335.5 —— **只有 B3 能开口的一帧,而大招真的放了**

`sw_0af9fd`,**seed 3030,armed 侧 = dire ⇒ chaos_knight(天辉)在 baseline 腿**(阈值 240)。

```
      t       x       y   hp%   mp% | 1600 内的敌人(B2 需要 ≥2)
  332.5    6283   -2411  1.00  0.73 | sven 1296
  333.5    6283   -2411  0.99  0.74 | sven  896
  334.5    6270   -2460  0.95  0.74 | sven 1057
  335.5    6173   -2828  0.92  0.74 | sven 1384   <== PIN
  336.5    6038   -2828  0.91  0.51 | sven 1365
  337.5    6176   -3249  0.91  0.52 | NONE
```

钉帧上**全部五名敌人**的距离:`sven 1384 / obsidian_destroyer 9283 / slardar 9401 /
crystal_maiden 12352 / zuus 14272`。1200 环内队友:`oracle 197`。
Phantasm:**level=1, cd=0**;英雄等级 6。

战斗日志,窗口 `[332.5, 338.5]`:

```
 t=334.0  DAMAGE    npc_dota_badguys_tower1_bot   -> chaos_knight
 t=334.3  DAMAGE    npc_dota_creep_badguys_ranged -> chaos_knight
 t=335.3  DAMAGE    npc_dota_badguys_tower1_bot   -> chaos_knight
 t=335.5  DAMAGE    npc_dota_badguys_siege        -> chaos_knight
 t=335.8  ABILITY   chaos_knight -> chaos_knight_phantasm       <== 施法
 t=336.3  MODIFIER_ADD  chaos_knight -> modifier_illusion  ×3
```

**逐条打掉另外三条分支:**

| 分支 | 这一帧 |
|---|---|
| **B1** | 最近敌人 **1384 u > 1200**(`nCastRange` 的上限)⇒ `J.IsInRange` **不可能**为真 |
| **B2** | 1600 内**只有 1 名**敌人(sven 1384),`#nNearbyEnemyHeroes >= 2` **假**。队友那一半(oracle 197)满足也没用,两个合取项**是 and** |
| **B4** | 最近敌人 1384 u **> 700** ⇒ 假 |
| **B3** | 1 座敌方塔 ≤700、**5** 个己方兵 ≤1000、`240 < 335.5 < 480` ⇒ **全真** |

**而且 B3 的 `IsPushing` 这次有旁证**:t=334.0 与 t=335.3 CK **正在挨 `badguys_tower1_bot` 的塔伤**,
身边有己方 siege —— 他**站在敌方一塔下推塔**。这是 LIMIT 1 那个不可观测合取项
在本轮唯一一次拿到独立佐证的地方(塔在打他 ⇒ 他在塔的攻击范围内 ⇒ 不是路过)。

⇒ **在 baseline 腿、在分歧段之内、在只有推塔分支能开口的一帧上,Phantasm 于 0.3 秒后放出。
这正是 `ckpush` armed 会移除的那次施法。**

### 4.2 armed 腿的对照帧:`20260903_033347_slot7` t=470.8–472.8

`sw_15a2fe`,seed 3017,armed 侧 = radiant ⇒ **CK 在 armed 腿**(阈值 480)。
三个连续域帧,**1600 内一个敌人都没有**(最近 `nevermore 7921`),1200 环内**无队友**,
Phantasm **level=1 cd=0**,hp 0.65 mp 0.34 —— B1/B2/B4 **全部不可能**,只有 B3 能开口,
而 **±3 秒窗口内触及 CK 的事件为零条**(无施法)。

**诚实边界**:一帧「没放」比一帧「放了」弱得多 —— 不放的理由可以有很多
(`IsPushing` 本来就假、蓝不够、引擎排到别的 action)。这一帧**支持**而**不证明**抑制。
**本轮的结论重量放在 §4.1 那一帧上,不放在这一帧上。**

### 4.3 未遮蔽域帧的两侧计数

```
armed     unshadowed   cast= 0  nocast= 3
armed     shadowed     cast= 5  nocast=10
baseline  unshadowed   cast= 1  nocast= 4
baseline  shadowed     cast=13  nocast= 4
```

「unshadowed」= 该帧 1200 内无敌人 ⇒ B1/B2/B4 都开不了口。
**可归因于推塔分支的施法,全语料 82 局只有 1 次**(§4.1 那一次)。

### 4.4 ⭐ 核验结论

```
VERIFY id=ckpush verdict=WORKING episodes=40
```

**判据**(按新验证哲学条件 (a)「真的执行且行为正确」):

1. **域非空**:40 个域帧 / 15 局 / 82 局语料 ⇒ **SILENT 被否决**(域为空才是 SILENT)。
2. **消费点确实被走到,且 shipped 值确实在放行**:§4.1 那一帧上,
   `DotaTime() > 240` 为真、其余四个合取项全真、另外三条分支逐条排除、大招 0.3s 后放出。
   ⇒ 把阈值改成 480 **在真实对局里是一次真的行为改变,不是 no-op**。
3. **行为正确**:放出的是 Phantasm(t=336.3 三个 `modifier_illusion`,level 1 = 3 个幻象,对);
   落点是敌方一塔下、有己方 siege 在推 —— **推塔分支的语义与现场一致**。

**⛔ 诚实边界,引用本条必须连引:**

- **这是一个抑制型 gate,armed 侧的「触发」在录像里没有正面可观测量。**
  本轮买到的是**反事实**:baseline 腿放行了一次只有该分支能产生的施法。
  这是抑制型 gate 能拿到的最强观测,**但它不是「看见 armed 侧挡住了这一次」**。
- **效应量很小**:可归因施法 **1 次 / 82 局**;段内总施法率 **1.292 vs 1.294 每局(死平)**。
  ⇒ **`ckpush` 是一个真实但极小的改动**。谁要拿它去谈胜负影响,请先读这两个数。
- **LIMIT 1**:`J.IsPushing()` 不可观测,40 个域帧是该合取项上的**超集**;
  §4.1 靠塔伤拿到了旁证,**其余 39 帧没有**。
- **LIMIT 3**:`IsFullyCastable()` 含蓝,快照只有 `mp_pct` 没有绝对蓝量 ⇒ 域帧是该项的**上界**
  (域帧 mp_pct 中位 0.74,最低 0.16 —— 最低那几帧确实可能是假阳)。
- **LIMIT 2**:40 帧里 32 帧被 B1/B2/B4 遮蔽,可达域**远小于** 40。
- **§2 的结构事实**:armed/baseline 的对比同时翻转了 CK 对手的 armed 状态,
  **跨腿差值不构成读数**。

---

## 5. ⭐ 另一件事:源码注释里那句立案句 **被 82 局语料证伪了**

`hero_chaos_knight.lua:455-457` 逐字:

> Over the 24 fixture frames carrying a chaos_knight, Phantasm is first LEARNED at
> t = 306.0s and **NO frame at or below 240s carries it at all**. So the shipped 240
> never binds in turbo.

**实测(82 局,全部逐帧扫过)**:

```
first-learn:  n=82   min=220.5   med=351.5   max=555.5
在 t<=240 就学会大招的局: 2 / 82
t<=240 且携带已学会 Phantasm 的快照帧: 38(其中存活帧 20)
```

两局逐帧还原:

| 局 | run/seed | 腿 | 首个「≤240 且已学会」帧 | ≤240 携带帧 | ≤240 施法 |
|---|---|---|---|---|---|
| `20260903_034708_slot7` | 15a2fe / 3017 | armed | **t=220.5**(英雄 6 级,hp 0.11) | 20(**其中 19 帧是死亡帧**,hp 0.00) | **无** |
| `20260903_034737_slot4` | 0af9fd / 3030 | armed | **t=222.5**(英雄 6 级,hp 0.90) | 18(**全部存活**) | **无** |

⇒ **「NO frame at or below 240s carries it at all」是假的**:最早 **220.5s**,比注释说的 306.0s 早 **85.5 秒**。
24 帧的 fixture(一局的量)不足以支撑一个全称否定。

**⭐ 但方向要诚实,别夸大**:两局在 t≤240 **都没有施法**,所以注释的**结论**
(「shipped 的 240 在 turbo 里基本不咬」)**在效果上仍然成立** —— 被证伪的是**它引用的证据**,
不是它的判断。而且第一局那 20 帧里 **19 帧是死亡帧**(hp 恒 0.00),真正存活的携带帧全语料只有 **20** 个。
⇒ 这是一条**注释更正**,不是对 `ckpush` 的翻案。已交英雄组(§6)。

## 5.1 携带域帧的 15 局(域帧级核对,逐局)

```
run        game                     leg        frames  unshadowed  band_cast t-range
sw_15a2fe  20260903_033347_slot7    armed           3           3          2  470..472
sw_15a2fe  20260903_034722_slot4    armed           3           0          2  410..412
sw_15a2fe  20260903_034727_slot6    armed           3           0          2  315..317
sw_0af9fd  20260903_034733_slot5    armed           1           0          2  279..279
sw_d87d45  20260903_034734_slot3    armed           3           0          1  439..441
sw_0af9fd  20260903_034737_slot4    armed           2           0          2  326..327
sw_0af9fd  20260903_034749_slot2    armed           1           0          1  255..255
sw_15a2fe  20260903_035918_slot1    armed           2           0          1  378..379
sw_15a2fe  20260903_035950_slot2    baseline        2           0          2  322..323
sw_0af9fd  20260903_040014_slot4    baseline        7           5          2  329..335   <== §4.1
sw_0af9fd  20260903_040025_slot8    baseline        4           0          2  445..448
sw_0af9fd  20260903_040032_slot7    baseline        2           0          2  388..389
sw_d87d45  20260903_040032_slot7    baseline        1           0          2  396..396
sw_15a2fe  20260903_040901_slot5    baseline        4           0          1  405..408
sw_0af9fd  20260903_041207_slot6    baseline        2           0          1  374..375
```

(`20260903_040032_slot7` 两行是 §0.1 那两份跨 run 同名的**不同对局**,逐 run 口径下不会串。)

---

## 6. 交出去的三件事

1. **[harness] GH #444 追评** —— 跨 run 同名在 W41 **原样复发 4 例**(独立第二个波次),
   附本轮 `--run dir:manifest` 绑定的免疫写法,可整段抄。
2. **[hero] 新 issue** —— `hero_chaos_knight.lua:455` 那句「no frame at or below 240s
   carries it at all」被 82 局证伪(最早 220.5s,2/82 局,38 帧 / 存活 20 帧);
   **只更正证据句,不动 gate**,并建议 `make_fixture.py` 钉 `20260903_034737_slot4 --t 222.5`。
3. **[batch] GH #443 归属更正**(§8)—— 那条红**已经不在本组的文件里了**。

## 7. ⛔ `roshdist` 本轮未动,棒仍在本组

批测台同一次交棒的另一半(SS-DP.8)本轮**没有做**。理由是工作单元要小,
`ckpush` 的六合取域 + 反事实排除已经占满本轮。**这不是判它 UNINTERPRETABLE,是没测。**
下一轮第一件事,语料可直接复用(见 §9 的欠账:82 局 timeline 随容器回收)。

`VERIFY id=roshdist verdict=INDETERMINATE episodes=0`(本轮未观测,**不是读数**)

## 8. GH #443:**棒不在本组了,那条红换文件了**

批测台 06:15Z 与上一轮都把 `test_stale_waits.py` 的红记成
「全部落在 `replay-check.md:9827/:9838` ⇒ 棒在录像组」。**本轮实测,这已经过期**:

```
python3 tools/agent/stale_waits.py   BARE_EXIT=3
STALE  director.md:9237
       id=slotarb   armed in test_set.md line 2 (since 65943509)
       id=slotdust  armed in test_set.md line 2 (since 65943509)
       line: `replay-check.md:9827`/`:9838` 三行仍写着「等 `slotarb`/`slotdust` 裁定」,而两个 id
INFO   replay-check.md: 2 matching line(s) outside the live 当前状态 block (history -- not a finding)
```

⇒ 本组的两行**上一轮已改**,现在是 `INFO`(历史区,LIMIT 2)**不是 finding**。
**唯一的 STALE 在 `director.md:9237`,而那一行的内容正是「引用 replay-check.md:9827/:9838」** ——
**它引用的那条 stale 已经不 stale 了,于是引用本身成了那条 stale。**

**本组不改别组章程**(与批测台同一条自律),已在 §6(3) 交批测台/总监。
⚠️ 这也解释了为什么这条红「修了还在」:两轮的报告都按**文件名**记棒,而**行换了文件**。

## 9. 交棒项与未动欠账

- **(1) 批测台 ⭐⭐ 交棒「`ckpush`/`roshdist` 的 (a) 证据」**:`ckpush` ✅ **本轮结清**(§4.4);
  `roshdist` ⛔ **未动**,下一轮第一件事(§7)。
- **(2) GH #443** ✅ 归属查清并交出(§8),**本组侧无动作可做**。
- **(3) ⛔ #419 第 6 轮、#421 第 5 轮仍零评论** —— 上一轮已写进 DECISIONS_NEEDED,本轮**不重复开**,
  只登记轮次 +1。**#444 本轮有新证据(§0.1),已追评。**
- **(4) ⛔ W41 阵容**:`chaos_knight` 82/82 满载,但**仍然没有 Axe**(`d22` 的 16.7% 挂到第三波)。
- **继承未动**(逐字沿用,未复算):`deep_solo_death` 待 #430;`tp_home_wasteful` 的 `|arm|/sd`
  待第三份语料;`VMAX=700` 只读未量;`wandlimbo_domain.py` 的幻象口径未动(等 #436);
  深查口径(局 vs 触发)待总监裁;`fieldsip_domain.py` 的 `empty_bottle` 缺陷只登记未修;
  另外四个零记录 id(`slotarb`/`slotpush`/`outlatch`/`roamidle`)**仍零记录**。
- **新欠账**:(a) 本轮 82 局 timeline **随容器回收**(从 S3 重扫约 12 分钟,四路并行);
  (b) `ckpush` 的 LIMIT 3(蓝量)要买断,需要 dumper 输出**绝对蓝量**或 per-ability 蓝耗表 ——
  **未开 issue**,先记着,等有第二个 id 撞上同一堵墙再一起提。

## 10. 验证(退出码**全部裸读,无管道**)

| 命令 | 裸退出码 |
|---|---|
| `tools/batch_test/aws/session_setup.sh` | **0**(`AWS ready: …user/dota2bot-agent`) |
| `sweep_run.sh` ×4 | **全 0**(`unparseable: 0` ×4) |
| `ckpush_domain.py --selfcheck` | **0**(**12 PASS / 0 FAIL**) |
| `tests/test_ckpush_domain.py` | **0**(**20 PASS / 0 FAIL**) |
| 真语料 `ckpush_domain.py --learn-census` | **0** |
| 辅助只读脚本 ×3(逐帧钉 / 学会普查 / 逐局分解) | **0 / 0 / 0** |
| `tools/agent/stale_waits.py` | **3**(有发现,§8) |
| `tools/agent/luacheck_gate.sh` | **0**(`installed luacheck (apt 包名 lua-check)` → `luacheck bots game: 0 warnings`) |

### 10.1 ⭐ 变异台(证据纪律:一次就过的测试要先证明它会咬)

`tests/test_ckpush_domain.py` **第一次运行就 20 PASS**,所以按纪律建了变异台
(还原**从运行前的文件副本**做,不是编辑回去):

```
MUT[M1-nearest_block-reaches]      test_exit=1  17 PASS / 3 FAIL
MUT[M2-selection-flipped]          test_exit=1  11 PASS / 7 FAIL
MUT[M3-level0-counts-as-learned]   test_exit=1  19 PASS / 1 FAIL
MUT[M4-band-edges-closed]          test_exit=1  18 PASS / 2 FAIL
MUT[M5-cooldown-ignored]           test_exit=1  19 PASS / 1 FAIL
MUT[M6-name-keyed-frames]          test_exit=1  19 PASS / 1 FAIL
RESTORED                           test_exit=0  20 PASS / 0 FAIL
```

**6 个变异体全部被杀,还原后干净。** M6 是 GH #176 那条:CK 的大招**就是造他自己的幻象**,
按名字建帧表会**在本检测器要数的那些时刻**凭空多出 `chaos_knight` 流 —— 这是本 id 最该被钉的一条。

### 10.2 trunk red 不是本轮引入的(**实测,不是推断**)

自检报 `TRUNK RED (python)`:`test_detector_source_constants.py` + `test_stale_waits.py`。
前者与本轮**无关**,**用移出文件重跑证明**,不是靠推理:

```
带本轮新文件:  BARE_EXIT=1  3 FAILED: illumove_pairs:HP_CUT, wandbleed_trigger:HP_MAX, wandbleed_trigger:HP_MIN_EXCLUSIVE
移出新文件后:  BARE_EXIT=1  3 FAILED: (逐字相同的三条)
```

失败集**逐字相同** ⇒ 与 `ckpush_domain.py` 无关(该普查只收 `(0,1)` 区间的 HP 常数,
本文件的常数是 700/400/1000/240/480,**不进普查**)。
两条红分别属于 `illumove` / `wandbleed`(策略组 / 英雄组的 id),**不是本组的作用域**。

**未改 `bots/`/`game/` 任何一行**(本轮**只读**了它们)⇒ **不声称**跑绿 Lua 全量(GH #124)。
本组**不花 AWS 的钱**:只读 S3 取件,未启动任何计费资源。
⚠️ `python3 -m pytest` 在本容器仍是 `No module named pytest`;测试文件**直接执行**取裸退出码。

## 11. 开工自检

**`SELFCHECK_BARE_EXIT=3`**(有发现,**不是没跑成**;重定向到文件后裸读)。
`legs run: 9`;`FINDINGS (exit 3): cadence stale-waits trunk-red(python)`。

- ⛔ **本轮第一条命令第二十一次踩证据纪律 3**(写了 `| tail -60`),脚本**当场拒答**
  `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked` —— **那次不是通过**;
  改重定向后才拿到读数。**「开工模板内建 `rc.sh`」第十四次登记。**
- ⭐ **GH #420 原样复发(第八轮)**:`sc.log:96-97` 点名两个 `UNCERTIFIABLE`
  (`tests/test_rc_wrapper.py`、`tests/test_selfcheck_lua_leg.py`),
  `:153` 汇总块却打 `UNCERTIFIABLE (exit 2): none`。**两处互相矛盾,第八轮了。**
- `cadence`:两个洞(hero、strategy),与上一轮同。
- `stale-waits`:见 §8 —— **本组那两行已转 INFO**,上一轮的章程更新生效了。

## 12. Token 用量

```
TOKENS total_in=6,077,077 out=48,426 turns=48
```
