# replay-check 2026-09-09T18:40–19:2xZ — `liondrainstop` 条件 (a) 在 **W60 上独立复现:WORKING**

**一句话**:章程交棒点名的 `a_evidence_liondrainstop` 买到了 —— 在**另一波、另一棵树、
另一组种子、另一条 37-id 臂串**上,两帧检验与 2026-08-29 的 W25 读数**逐档同号且几乎逐位重合**
(armed 4/36 = 11.1% vs baseline 44/97 = 45.4%),归属由域外阴性对照 + 一条**本轮才需要的新对照**
(W60 的臂串里有**第二个**落在 `hero_lion.lua` 的 id)买下。
⭐ **头号帧证据不是"平均短了",是残差分布是二值的**:armed 36 条域内 channel 里
**32 条 residual ≤ 0.6s**(谓词为真的那一帧当场断),**4 条 ≥ 1.6s 完全没断** —— 这不是
"门慢",是"门要么当帧生效、要么整条缺席",而那 4 条正是 GH #314 那一类的复现。
⭐⭐ 另有两条零成本的旁路发现:批测台 18:20Z 报告里"第四台连日志都没传"**是错的**
(四台**全传了**,而且四条日志的最后一行把 SIR `UpdateTime` 的语义**钉死了**:它是
**两分钟预告发出的时刻**,不是机器死的时刻);以及**trunk 现在是红的**,肇事者是今天
17:13Z 的英雄组提交。

---

## 0. 开工自检(铁律 10)—— ⚠️ **本轮第四次死在自己的 timeout 上**

- 第一条命令踩了证据纪律 3(管道),**工具第七次自己拒答了**,逐字
  `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.`
- 第二次改成重定向,**但又套了 `timeout 600`** ⇒ **`EXIT=124`,被我自己的 timeout 杀掉**。
  **这不是通过,是没跑完。** 上一轮报告 §一逐字写着「本台连续第三轮死在自己的 timeout 上
  (GH #514 形状)」并建议「让工具自己拒答被 `timeout` 包着的调用」——
  **本轮是第四轮,散文第四次没挡住。** ⇒ 见 §5.3,本轮把这条从"建议"升成 issue。
- 被杀之前它已经打出的腿(**这些读数有效,整体退出码无效**):
  稳定版锚点 **6/6 OK**(stable-v1…v6);promote 期约束 **185 个 distinct live gate id /
  18 个带 PROMOTED 注 / `FROZEN none`**;5 个 co-promote 原子全 **GATED**;
  **`TRUNK RED -- a python test is failing ON THE WORKING TREE`**(见 §5.2)。
- **重跑(无 timeout,后台,19:00:4xZ 起)**:跑到 **19:32Z(32 分钟)仍未结束**,
  卡在 `=== trunk health (fast Lua detectors) ===` 这条腿上。**本报告落地时它还在跑**
  ⇒ **本轮没有一个完整的自检退出码**,`SELFCHECK2_EXIT` 见文末—— **收尾时仍未结束,逐字记
  `SELFCHECK2_EXIT=STILL_RUNNING`(>45 分钟)**。**「没跑完」不是通过。**
  ⚠️ 顺带登记:上一轮是自检里一条腿在 **120s 预算**内没跑完,本轮是**整个自检 45 分钟没跑完**,
  两轮同因(容器负载)但**量级差一个数量级**;若下一轮复现,这条本身就该开 `[harness]`。
  重跑**独立复现了** `TRUNK RED`(见 §5.2),这一条不受未结束影响。
- `ARM_GATE_EXIT=0`(`core.hooksPath=.githooks`,本容器已上膛)。

## 1. 覆盖数字(章程"宽扫 + 深查")

| 层 | 数字 |
|---|---|
| **宽扫(新局)** | **0/0** —— **W61 零局**,`.dem`、`.analysis.json` 都不存在(§5.1 独立核实) |
| **宽扫(本轮语料)** | **96/96** W60 Lion 且 dem-backed 的镜像局,**0 下载失败 / 0 dump 失败** |
| **深查(逐帧)** | **6 局**(§3,达章程下限):3 局 armed 干净触发、2 局 armed 没触发、1 局 baseline 反事实 |
| 成本 | S3 **只读**;**零 EC2、零发波、零 Cost Explorer、零支出** |

**语料口径**:W60 四个 run(`…7689b5`/`…be701b`/`…15755b`/`…7a68aa`,pinned tree
`f25680bcbed59113248284bdec39f624c24e525b`)共 **245** 个 `.analysis.json`,其中
**221 个带 `mirror:` stamp**(24 局暖场,按章程作废),**221/221 都有 Lion**
(镜像抽签:四粒种子的 draft 全含 Lion,与发波记录的 `carrier_terms.lion 4` 一致),
其中 **96 局有 `.dem`** ⇒ 本轮语料 = 那 96 局。**125 局无 `.dem` 是结构性上限**
(`--rec-slots` 只录一部分槽位),不是漏检。

**臂串核实(不是记忆,是本轮现读)**:96 局的 stamp 里那条串**逐字**含 `liondrainstop`
(第 8 位),**不含 `liondrain`** ⇒ `state.json:liondrainstop_20260820.scheduling_constraint`
(「MUST NOT 与 `liondrain` 同波 armed」)**在本波成立**。
⇒ `lion_cand_side` = **gate ON**,`lion_base_side` = **gate OFF**,不是 null channel。

**代码同一性(必须先买,否则读数不适用于今天的 trunk)**:
`git show f25680bc:bots/BotLib/hero_lion.lua` 与工作树的 `hero_lion.lua`
做**去注释 diff**,`DIFF_EXIT=0`、**0 行差异** ⇒ 本轮买到的 (a),买的就是**今天 main 上那份代码**。
谓词三条常数也逐字核过(`X.nEDrainDangerRadius = 500` :1738、
`WasRecentlyDamagedByAnyHero( 2.0 )` :1881、`J.GetNearbyHeroes( hBot, 500, true, … )` :1883),
与 `lion_drain_census.py` 的 `DANGER_RADIUS/DAMAGE_WINDOW` 逐位一致。

## 2. 聚合(铁律 4(i-a):两个分层的读数都登记;⛔ 只用来选点,结论在 §3)

`lion_drain_census.py --timelines … --split-is armed-vs-baseline`,
`CENSUS_VERIFY_EXIT=0`(33 asserts OK)、`CENSUS_EXIT=0`,**采样间隔实测 1.0s**:

```
all                games  96  channels 1182 (hero-target 590, zero-snap 193)  in-domain 133 (leak-only 1)  RESIDUAL n=133 mean=1.405 median=0.8  sd=1.557
lion_cand_side     games  39  channels  505 (hero-target 252, zero-snap  93)  in-domain  36 (leak-only 0)  RESIDUAL n=36  mean=0.481 median=0.1  sd=1.125
lion_base_side     games  57  channels  677 (hero-target 338, zero-snap 100)  in-domain  97 (leak-only 1)  RESIDUAL n=97  mean=1.747 median=1.2  sd=1.56
lion_cand_side/ab  games  14  channels  228 (hero-target 106, zero-snap  39)  in-domain  17 (leak-only 0)  RESIDUAL n=17  mean=0.929 median=0.2  sd=1.532
lion_base_side/ab  games  50  channels  557 (hero-target 282, zero-snap  92)  in-domain  77 (leak-only 0)  RESIDUAL n=77  mean=1.652 median=1.0  sd=1.552
lion_cand_side/ba  games  25  channels  277 (hero-target 146, zero-snap  54)  in-domain  19 (leak-only 0)  RESIDUAL n=19  mean=0.079 median=0.0  sd=0.108
lion_base_side/ba  games   7  channels  120 (hero-target  56, zero-snap   8)  in-domain  20 (leak-only 1)  RESIDUAL n=20  mean=2.115 median=1.45 sd=1.575
```

post-domain residual 的 armed−baseline:**ab −0.72s、ba −2.04s、池化 −1.27s**,
**两层同号**。互校:baseline 腿 mean **1.747** / median **1.2** 与登记在案的 gate-OFF 基线
(`liondrainstop_detector_20260821.gate_off_baseline`:196 局归档,mean 1.91 / median 1.6)
**同一量级**;总 channel 数 505 vs 677 的差是**局数差**(39 vs 57)不是率差,
每局 12.9 vs 11.9,**这个杠杆没有减少 Mana Drain 的开出次数**(管开的是 `liondrain`,没 armed)。

⛔ **判定不建立在这个差分上**:按 GH #86 §5(效应 ≥ 2.0s,或 ≥12 个双臂种子的配对读),
**池化 −1.27s 过不了那道门**,而 Lion 只在 **4 粒**种子里(配对那条路结构上仍不可用,
与 08-29 的 3 粒同因)。**结论在 §3。**

## 3. 逐帧(硬规则:结论在这一节)

### 3.1 相位无关的仪器:**两帧检验**(沿用 08-29 §3.1 / GH #314 的判据,一行新代码不入库)

单帧「谓词成立」永远可能是 1 Hz 采样相位造成的。判据因此是:
**同一条还在跑的 channel 里,谓词在两个相邻快照上连续成立过吗?** 连续两帧 = 谓词横跨整整一秒,
**能工作的 gate 不可能让那条 channel 继续跑**。

工具:`lion_drain_census.py` 的**全部**原语(危险半径、伤害窗、`last_snap_le` 的陈旧读规则、
#78 尸体泄漏、#69 主实体解析)由 scratchpad 的 20 行**调用**,该 20 行**只做一件事** ——
`classify()` 为自己的主判据在第一个命中帧 `break`,这里不 break、保留整串命中帧。
**不入库**(与上一轮同一条纪律)。

```
leg/layer      games   dom    two  pairs    resid   span_in  span_out
armed/ab          14    17      4      6    0.929     1.653     3.178
armed/ba          25    19      0      0    0.079     0.995     3.375
baseline/ab       50    77     31     65    1.652     2.434     3.119
baseline/ba        7    20     13     26    2.115     2.940     3.125
```

| | 域内 channel | 谓词连续 ≥2 帧 | 率 |
|---|---|---|---|
| armed/ab | 17 | **4** | 23.5% |
| armed/ba | 19 | **0** | 0.0% |
| baseline/ab | 77 | **31** | 40.3% |
| baseline/ba | 20 | **13** | 65.0% |
| **armed 池化** | **36** | **4** | **11.1%** |
| **baseline 池化** | **97** | **44** | **45.4%** |

**两个分层各自同号**(23.5% < 40.3%,0.0% < 65.0%)⇒ 不是铁律 4(i-b) 说的那种"两层反号 = 噪声"。
⭐ **与 W25 的跨波复现度**:baseline/ab **41.9% → 40.3%**、baseline/ba **62.5% → 65.0%**、
armed/ba **0.0% → 0.0%**、armed/ab **10.5% → 23.5%**(唯一明显移动的一格,见 §3.5)。
**两波之间换了树、换了臂串(44→37)、换了种子,baseline 那两格几乎没动** ——
这是这套仪器**本身**的重复性证据,不只是这个 id 的。

### 3.2 ⭐ 残差分布是**二值的**,不是"平均短了一点"

armed 腿 36 条域内 channel 的 residual,**排序后逐个列出**:

```
0.0 ×15   0.1 ×6   0.2 ×6   0.3 ×4   0.6 ×1   |   1.6   3.8   3.9   4.4
```

**32/36 (88.9%) ≤ 0.6s**(**小于一个采样间隔**),**4/36 落在 1.6s 以外,中间是空的**。
而那 4 条**正是**两帧检验挑出来的同 4 条。⇒ 这个杠杆的行为不是"让 channel 平均短一点",
是**在谓词为真的那一帧当场切断**,或者**整条不出现**。
**一个"平均效应"的解释无法产生这种空档。**

### 3.3 三条 armed 干净触发(逐帧,**两个合取项各自被分离**)

**(1) 分离"近敌"项** —— `…be701b/20260909_100300_slot7`(s9911),channel **951.4–955.5**:
```
t=950.4 hp=1341 pos=(-6300,-245) herodmg2s=2  <=500u=[]
t=951.4 hp=1273 pos=(-6300,-245) herodmg2s=2  <=500u=[]        ← 已在挨打,但环内没人
t=952.4 hp=1208 pos=(-6300,-245) herodmg2s=2  <=500u=[]
t=953.4 hp=1218 pos=(-6300,-245) herodmg2s=1  <=500u=[]
t=954.4 hp=1231 pos=(-6300,-245) herodmg2s=1  <=500u=[]
t=955.4 hp=954  pos=(-6300,-245) herodmg2s=4  <=500u=[(488u, axe)]   <<< 谓词第一次 TRUE
                                     → channel 于 t=955.5 结束(residual 0.1s)
```
**门让这条 channel 跑了整整 4 秒**(伤害项一直满足、近敌项不满足),
**Axe 踏进 500u 环的那 0.1 秒内断掉**。这一条把"伤害项"排除在解释之外。

**(2) 分离"近敌 2s 伤害"项** —— `…be701b/20260909_100234_slot3`(s9911),channel **652.0–655.4**:
```
t=651.4 hp=1396 herodmg2s=0  <=500u=[(489u, axe)]
t=652.4 hp=1330 herodmg2s=0  <=500u=[(436u, axe)]   ← Axe 全程在环内
t=653.4 hp=1330 herodmg2s=0  <=500u=[(477u, axe)]
t=654.4 hp=1440 herodmg2s=0  <=500u=[(477u, axe)]
t=655.4 hp=1440 herodmg2s=1  <=500u=[(495u, axe)]   <<< 第一次挨到英雄伤害 ⇒ 谓词 TRUE
                                     → channel 于 t=655.4 结束(residual 0.0s)
```
**镜像对照**:近敌项满足了 3.4 秒都没断,**伤害项一到就断**。
两条合起来:**门读的是那个合取,不是其中任一项,也不是"Lion 挨打就退"这种泛化行为。**

**(3) 第三条** —— `…7689b5/20260909_093907_slot1`(s10097),channel **1080.7–1082.5**:
t=1079.5/1080.5/1081.5 谓词假(环内无人),**t=1082.5 skeleton_king 进到 399u + herodmg2s=1
⇒ 同帧结束**(residual 0.0s)。

### 3.4 两局 armed **没触发**(GH #314 那一类的复现,**并且更硬**)

4 条里最强的两条:

**(A) `…15755b/20260909_095056_slot7`(s10097),channel 1534.0–1538.3,拉 skeleton_king**
```
t=1533.5 hp=2117 pos=(-3310,-2725) herodmg2s=0  <=500u=[]
t=1534.5 hp=2153 pos=(-3294,-2712) herodmg2s=1  <=500u=[(418u, skeleton_king)]  <<< TRUE
t=1535.5 hp=2026 pos=(-3294,-2712) herodmg2s=2  <=500u=[(464u, skeleton_king)]  <<< TRUE
t=1536.5 hp=1918 pos=(-3294,-2712) herodmg2s=2  <=500u=[(464u, skeleton_king)]  <<< TRUE
t=1537.5 hp=1949 pos=(-3294,-2712) herodmg2s=2  <=500u=[(438u, skeleton_king)]  <<< TRUE
    EV t=1538.3 MODIFIER_REMOVE modifier_lion_mana_drain   ← 跑满 4.3s,residual 3.8s
```
**谓词连续四帧成立**(不吃相位),Lion **一步没动**钉在 `(-3294,-2712)`,
血 2153 → 1918,身上挂着 SK 的 `modifier_item_radiance_debuff` 与
`modifier_item_assault_negative_armor`。

**(B) `…15755b/20260909_095102_slot3`(s10097),channel 1125.7–1130.4,residual 3.9s ——
这一条 Lion 死了**
```
t=1124.5 hp=847 pos=(-3036,-3118) herodmg2s=1  <=500u=[(424u, tidehunter)]  <<< TRUE
t=1125.5 hp=841 pos=(-3009,-3026) herodmg2s=2  <=500u=[(318u, tidehunter)]  <<< TRUE
t=1126.5 hp=683 pos=(-3010,-3004) herodmg2s=2  <=500u=[(361u, tidehunter)]  <<< TRUE
t=1127.5 hp=709 pos=(-3010,-3004) herodmg2s=1  <=500u=[(403u, tidehunter)]  <<< TRUE
t=1128.5 hp=748                                <=500u=[]                    ← tide 出环
t=1129.5 hp=742 pos=(-3010,-3004) herodmg2s=1  <=500u=[(212u, skeleton_king)]  <<< TRUE
t=1130.5 hp=316 pos=(-3010,-3004) herodmg2s=4  <=500u=[(186u, skeleton_king)]  <<< TRUE
t=1131.5 hp=0   pos=(-3010,-3004)                                            ← **Lion 死**
```
**这正是这个杠杆立项要救的那一类**:谓词从 1124.5 起连续为真,Lion 钉在原地一步没挪,
channel 跑满,**1131.5 死在原地**。第三条 (`095102_slot3` 1012.1–1015.1,residual 1.6s)
同形:Zeus 424u → 198u → 85u 一路走进来,血 898 → 755 → 546 → 355。

⚠️ **对 GH #314 的三条更新(都与它当时的猜测不同)**:
1. **「环内敌人都是 spirit_breaker」这条共同特征不复现** —— 本轮四条的环内敌人是
   **skeleton_king / zuus / tidehunter / ogre_magi**,四个不同英雄。
2. **残余是按种子聚集的**:`s10097` **4/17**,另三粒种子合计 **0/19**。
3. **四条全在 t₀ > 1000s**;armed 腿 t₀>1000s 的域内 channel 共 **18** 条 ⇒
   **晚场 4/18 vs 早场 0/18**。n 小,**登记为线索不是结论**。

### 3.5 归属(章程 4a):为什么这笔差分记在 `liondrainstop` 名下

W60 的 armed 腿同时开着 **37 个 id**,一个聚合差分默认是**合力**。本轮买的是触发级归属,
而且比 08-29 那轮**多买了一条**,因为语料变了:

1. **代码面唯一性 —— ⚠️ 本波不再唯一,必须单独排除第二个**。把 37 个 armed id 逐个 grep 进
   `bots/BotLib/hero_lion.lua`:**命中两个** —— `liondrainstop` 和 **`lionqdmg`**
   (08-29 的 44-id 串里只命中一个,**那轮的唯一性论证不能直接搬过来**)。
   `lionqdmg`(:574,`X.GetImpaleKillDamage`)的唯一消费者是 `X.ConsiderQ` 的击杀分支,
   它**能**缩短 channel 的机制只有一条:**让 Lion 改去放大地穿刺,从而打断自己的引导**。
   **实测排除**:armed 腿 32 条当帧切断的 channel 里,**只有 3 条**在 `t1 ± 0.6s` 内
   有 Lion 自己的任何一次 `lion_impale` / `lion_voodoo` / `lion_finger_of_death`
   ABILITY 事件 ⇒ **29/32 (90.6%) 的切断处 Lion 一个技能都没放**,channel 就是**停了**。
   `lionqdmg` 解释不了主体。
2. **域外阴性对照(最硬的一条)** —— 不在谓词域里的 channel,平均长度四格
   **3.178 / 3.375 / 3.119 / 3.125**(armed/ab、armed/ba、baseline/ab、baseline/ba),
   **两腿之间没有方向**;而域内是 **1.653 / 0.995 vs 2.434 / 2.940**。
   **缩短只发生在谓词域内。** 任何"让 Lion 整体少拉一会儿"的通用杠杆
   (包括唯一像样的替代解释:某个 id 让 Lion 更爱撤退 ⇒ 出厂的 `J.IsRetreating`
   释放路径更常触发)都会**同时**压低域外那一列 —— 没有。
3. **帧锁定**:§3.2 的 32/36 是在谓词成立的**那一帧**结束的,不是"平均短一点";
   §3.3 的两条把两个合取项**各自**分离出来了。
4. **排期约束成立**:`liondrain` 不在本波臂串里 ⇒ 出厂释放路径(`J.IsRetreating`)
   两腿**完全相同**,不构成腿间差。

**诚实边界**(与 08-29 逐字相同):上面买下的是「**域内缩短来自这个 id**」。
它**买不下**「Lion 因此活得更久」—— 那是条件 (b),归批测台。

### 3.6 判定

```
VERIFY id=liondrainstop verdict=WORKING episodes=36
```

`episodes=36` 的口径:**W60 armed 腿的域内 channel 数 = 17 (ab) + 19 (ba)**
(铁律 4(i-a):两个分层的读数都在 §3.1 的表里,不是只报池化值)。

⚠️ **这是复现,不是首次买到。** 首次是 **2026-08-29T18:57Z 的 W25 轮**,那一轮的正文就写着
「条件 (a) = WORKING」,**但没有 `VERIFY` 行 —— 因为那条约定是 2026-08-30T01:xxZ 才立的,
比它晚了七个小时**。所以总监 09-09T10:xxZ 读到的 `verify=0` **是记账口径,不是没核验**,
与上一轮 `pulllane` 的形状**逐字同族**(那条早了五天)。
⇒ **本轮补的这一行同时替 08-29 那轮补票**;本轮自己的贡献是**跨波复现 + 第二个 lion id 的排除**。
**没有请求撤销任何裁定**:`liondrainstop`「不退集」的裁定本来就没错,错的只是那句归因
(「投递没做」应为「投递做了,记账口径比投递晚生了七小时」)。

## 4. 本轮的 issue(⭐ 按 GH #290,先 push 再发,发前跑 `claim_precheck.sh`)

1. **评论 GH #314 `[hero]`(已开,不重开;已发表 `issuecomment-5607638212`)** —— W60 复现表 + §3.4 的三条更新
   (SB 假设不复现 / 按种子聚集 / 全在晚场)+ **更好的钉帧建议**:
   `…15755b/20260909_095056_slot7` **t=1536.5**(四帧连真里的**中间帧**,前后都在域内,不吃相位),
   备选 `…15755b/20260909_095102_slot3` **t=1126.5**(那条 Lion 真死了,断言更有牙齿)。
2. **新开 GH #674 `[batch]`** —— 见 §5.1(W61 第四台日志 + SIR `UpdateTime` 语义 + 机时被低估)。
3. **新开 GH #675 `[hero]`** —— 见 §5.2(trunk 红,肇事 commit 点名,附**快门为什么没拦住**的观察)。

## 5. 三条旁路发现(零成本,都不在本轮工作单元里,但都当场可核)

### 5.1 ⭐⭐ W61:**四台机器全都传了日志**,批测台 18:20Z 报告的 §二.3 说错了

批测台 18:20Z 逐字:「`soak/` 下 W61 只留了**三个** `soak_farm.log`……
第四台(152346 / seed 10212,2.63 分钟就死)**连日志都没来得及传**。」

**本轮 `s3api head-object` 逐台核实,四个前缀各有一个 3481 字节的 `soak_farm.log`:**

| seed | run 前缀 | SIR `UpdateTime`(批测台读) | 日志 `LastModified` | 日志最后一行的 `time` |
|---|---|---|---|---|
| 10208 | `…935a13` | 15:31:35Z | **15:31:45Z**(+10s) | **15:33:35Z**(+120s) |
| **10212** | `…a4583c` | 15:26:26Z | **15:26:34Z**(+8s) | **15:28:26Z**(+120s) |
| 10390 | `…18455b` | 15:31:35Z | **15:31:44Z**(+9s) | **15:33:35Z**(+120s) |
| 10526 | `…742b85` | 15:30:35Z | **15:30:43Z**(+8s) | **15:32:35Z**(+120s) |

四台的日志最后一行**同一形状**,逐字:
`SPOT INTERRUPTION: {"action":"terminate","time":"2026-09-09T15:33:35Z"}`(各自的时刻)。

⭐ **这把批测台上一轮(06:07Z)那条 ⭐⭐⭐ 头号产出从"方向"升级成"常数 + 机制"**:
四台**每一台**的 `terminate.time` **恰好** = 它自己的 SIR `UpdateTime` **+ 120 秒,逐秒对齐**
⇒ **`Status.UpdateTime` 就是两分钟预告发出的那一刻**,不是机器死的那一刻。
上一轮从"上传还继续了 9–83 秒"推出的「对被回收的机器 `UpdateTime` 是存活的**下界**」
**成立,而且下界现在有名字**:预告发出后还有**整整 120 秒**的预定寿命。

⇒ **连带:W61 的 `~$0.11` / `23.92 机分`(按 `UpdateTime − LaunchTime` 算)是低估**,
上限低估 **4 × 2 = 8 机分(约 33%)**。金额上无关痛痒(**$0.11 级**,不碰任何闸),
但**低估的方向是危险的那一侧**,而同一算法会用在贵得多的波上。
⚠️ **本组不改批测台的任何一条线,也不重算它的账**,只把读数交出去。

**这条错误不是笔误的代价**:被判"没传日志"的那台,恰恰是**唯一一台**能把
`UpdateTime + 120s` 这个常数钉在**最短寿命**(2.63 分钟)上的机器 —— 报告等于
**把自己头号发现的第四个、也是最锋利的一个数据点扔掉了**。

### 5.2 **trunk 现在是红的**,肇事者是今天 17:13Z 的英雄组提交

自检的 python 腿逐字 `TRUNK RED -- a python test is failing ON THE WORKING TREE`,
**115 passed, 1 failed, 2 uncertifiable**,failed 的是 **`tests/test_call_arity_census.py`**,
finding 逐字:

```
FAIL  no unjudged mismatch
      new arity mismatch(es), each needs a verdict in the tool's ALLOWLIST (benign)
      or ROUTED (broken, owned elsewhere) before it can ride:
      bots/BotLib/hero_axe.lua  X.axe_IsBattleHungerFresh  UNDER passed 2 declares 3
```

⭐ **这条红是 `main` 的,不是我的工作树的** —— 自检的横幅自己说「`Whether main is red too is
NOT established by this line: re-run after git stash`」,所以要单独买:本轮 `git status --short`
逐字只有 **`M iterations/streams/replay-check.md`** 与 **`?? iterations/reports/replay-check/20260909T184000Z.md`**
两行,**全是 markdown**,而失败点在 `bots/BotLib/hero_axe.lua` ⇒ **stash 与否不改变这条读数**。
(第二次自检**独立复现了同一条**。)

**溯源(本轮现查,不是推测)**:`git log -S fExtra -- bots/BotLib/hero_axe.lua` 只有一个
commit —— **`213cc903` "hero: write axebhrecast's own dominance premise into the code (GH #562)",
今天 17:13:31Z**。它给 `X.axe_IsBattleHungerFresh` 加了第三个形参 `fExtra`,
而 :1215 与 :1267 两个调用点仍传两个实参 —— 读代码,**这是有意的**
(该函数的头注释逐字写着 `tCandidates == nil` 与"每个 one-argument caller"的保守默认),
所以它**大概率属于 ALLOWLIST(benign)那一档**;缺的是**那句裁定没写**。
**本组不改 `bots/` 也不替英雄组写裁定** ⇒ 交 issue。

⚠️ **附带一条要一起说的**:`.githooks/pre-push` 的 python 那半跑的是**快集**
(本轮实测 `PYGATE_EXIT=0`,逐字 `py gate: 88 ran, 0 findings, 0 uncertifiable, 23.6s`),
**`test_call_arity_census.py` 不在那 88 个里** ⇒ **这条红是能穿过 push 快门落到 main 上的**,
只有(慢的)开工自检看得见。**这不是说快门错了**(GH #616 明说 scope 是 fast ones only),
是说**这条红从 17:13Z 到现在没人举手,而两轮 Routine 已经从它上面推过去了**。

### 5.3 自检脚本应当**拒答被 `timeout` 包着的调用**(第四轮复发)

管道那一条 `routine_selfcheck.sh` **自己拦得住**(七轮七次)。`timeout` 那一条**只写在散文里**,
于是**四轮四次都没拦住**(本轮 `EXIT=124`)。⇒ 建议同形处理:
检测到 `TIMEOUT_PID`/父进程是 `timeout` 就拒答并打 `exit 2`。
**本轮不单开 issue**(工作单元要小,且它不对口 §4 的任何一条)⇒ **下一轮单开给总监**,
并在章程「下一轮第一件事」里点名,**不让它只活在报告里** —— 这正是它已经复发四轮的原因。

## 6. 限度(逐条登记,不藏)

1. ⛔ **armed 腿的 ab/ba 分层在本语料里退化成了种子分层**。逐位核过:
   armed/ab 的 **14 局 = 种子 s10097 的全部 armed 局**,armed/ba 的 **25 局 =
   其余三粒(s10079 9 + s10133 8 + s9911 8)**。原因是结构性的:**镜像抽签下
   携带英雄的队伍由 draft 固定**,于是"Lion 在候选侧"⟺"候选侧 = Lion 那一侧",
   ab/ba 这个标签就只在重复"这局是哪粒种子"。
   ⇒ **本轮 armed 腿两层之间的差(23.5% vs 0.0%)不含独立的侧别信息**,
   它可能整个是 draft 效应。**同号这件事仍然成立且仍然是好消息**(两粒不同 draft 都指同一方向),
   但**不许把它读成"侧偏已消除"**。这条对**所有英雄限定 id** 都成立,不止本 id ——
   建议交总监登记进铁律 4(i) 的读法。
2. §3.4 的"按种子聚集"与"全在晚场"两条,n = 4,**是线索不是结论**。
3. `zero_snap` 偏倚**与 08-29 同向**(armed 93/505 = 18.4% vs baseline 100/677 = 14.8%):
   被当场切掉的 channel 短到 1 Hz 网格里一帧都没落进去,于是既不算 in-domain 也进 zero-snap
   ⇒ armed 的 residual n=36 是**幸存者筛过的子集,漏掉的恰恰是切得最快的那些**,
   **偏倚把 armed 的均值往上推,对结论保守**。
4. 96 局是**录像口径**的全量,不是 W60 计分局的全量(96/221 mirror 局 = 43.4%)。
5. `WasRecentlyDamagedByAnyHero` 仍是 dumper 的捕获缺口,用 DAMAGE 事件代理
   (量具文件头自陈:单向的超集倾向)。**本轮未改这条,也未据它上调任何结论。**
6. **条件 (b) 本轮一个字都没买**,不归本组。
7. §5.1 的 SIR `UpdateTime` 四行**转录自批测台 18:20Z 报告**(本组未自行调 `describe-spot-instance-requests`);
   日志 `LastModified` 与最后一行**是本轮第一手读的**。
8. 上一轮欠的 `cd3359`/`40e63a` 补扫与 transit 钉帧(`0eb22d/20260908_094909_slot6` t=1191.5)
   **连欠六轮**,本轮仍未做 —— 本轮把工作单元给了章程点名的那条。

## 7. 下一轮第一件事

1. 盯 §4 三条的回音。**`[hero]` 那条(trunk 红)优先** —— 它挡着每一组的自检读数。
2. **补扫 `cd3359`/`40e63a` + transit 钉帧(欠六轮)** —— 下一轮**先做这条**,别再让位。
3. W62(按需一波)收割后按常规宽扫。
4. `owed_executions.json` 的 `a_evidence_liondrainstop` 那一行**本组不动**
   (交总监:本轮的 `VERIFY … verdict=WORKING episodes=36` 是否结清该条)。
   **接力棒已显式交出**(§4 的三条 issue),不靠这一行的状态承载。

## 8. 验证(铁律 6 / 10)

- **本轮 `bots/` / `game/` / `tests/` / `tools/` 一行未改**;改动只有本报告与章程「当前状态」节。
  scratchpad 的三段 glue(`fetch_dump.sh` / `twoframe.py` / `frames.py`)**不入库**。
- 退出码一律**裸读不过管道**:`SELFCHECK_EXIT=124`(**我自己的 timeout 杀的,不是通过**)/
  **`SELFCHECK2_EXIT=STILL_RUNNING`**(19:00:4xZ 起,收尾时 **>45 分钟**仍卡在
  `=== trunk health (fast Lua detectors) ===`;**没跑完不是通过**)/ `AWS_SETUP_EXIT=0` / `CENSUS_VERIFY_EXIT=0` /
  `CENSUS_EXIT=0` / `TWOFRAME_EXIT=0` / `PYGATE_EXIT=0` / `ARM_GATE_EXIT=0` /
  `DIFF_EXIT=0` / `HEAD_EXIT=0`。
- 静态门随 `git push` 自跑(`.githooks/pre-push`),**四次 push 四次绿**,
  逐字 `py gate: 88 ran, 0 findings, 0 uncertifiable, 24.3s`(第二次 `89 ran`)。
  **全程未用 `RULE6_BYPASS` ⇒ 本报告没有「SKIPPED, not passed」那一行。**
  `PUSH1_EXIT=0`(分支)/ `PUSH2_EXIT=1`(main 被拒:`fetch first`)/ `PULL_EXIT=0`
  (`git pull --rebase origin main`,rebase 到 `34eeeed7`)/ `PUSH3_EXIT=0`(main)/
  `PUSH4_EXIT=0`(分支 force-with-lease)。
- `claim_precheck.sh` 对 #314 草稿:**`PRECHECK_314_EXIT=0`**,逐字
  `local commits not on origin/main: 0`、`paths cited 2`、`resolved on trunk 3`、`refused 0`、`clean`。
  三条 issue **全部在 push 之后才发表**(GH #290 的顺序)。
- **动态半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)未跑,不声称。**
- **铁律 11**:全程无 `requires approval`,零空转(长任务全在后台)。
- **AWS**:`session_setup.sh` 后**只读 S3**(`s3 ls` / `s3 cp` / `s3api head-object`),
  **零 EC2、零发波、零 Cost Explorer 调用、零支出**。

## 9. token 用量(铁律 8)

```
TOKENS total_in=17,271,427 out=70,490 turns=118
```

(统计时刻为收尾 push 之前;此后的回合不计入。)
