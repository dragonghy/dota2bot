# 录像检查组 2026-08-25T01:35Z(第六十四次触发)—— owner P1 DoD 第 3 步交卷:`pulllane` 的 (a) = **WORKING**

**一句话**:批测台 00:19Z 发的 **W8**(owner P1 取证波,28-id,钉 `aadd993`)本轮**整波落地并整波检完**
(**宽扫 211/211 非暖场局** + **深查 6 局逐帧**),而且**批测台 §7.3 预登记的 26-id 对照腿 W7 也在同一次会话里
用同一份工具重跑了 211/211** —— 于是 `pulllane` 的条件 (a) 拿到了本组做过的**最干净的一次执行核验**:
决定性证据不是任何速率,而是**哪些营被打**:对照腿里 **connect 记录为 0 的营在 armed 腿被清零**
(radiant 层 3 个营 9 次 poke → 0,dire 层 4 个营 ~20 次 → 0),
而**两层各自唯一产生 connect 的那个安全线拉野营一个没动**(16.1→15.6 / 10.8→8.8 每百局),
**两个物理层各自独立复现同一形状**。批测台交办的三条验收量全部达标,
但连接率那一格的刀口被本轮量出来并写进结论:**分子没动(两波两层 connect 绝对次数全是 2),动的是分母** ——
它没有造出新的成功拉野,它删掉了结构上不可能成功的那些。
零 EC2 支出,`bots/`/`game/` **0 改动**,gate 未动。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` ⇒ **worst exit 3**。

- **UNLANDED**:窗口内只报 1 条(`11d5878` director PROMOTE,`origin/claude/busy-bardeen-u45ms4`)。
  **本组 08-24T21:48Z 已独立复核过这条的内容已落 main**(`jmz_func.lua:7124` 是
  `PROMOTED (was soak-candidate 'creeppull')`),工具点的是 SHA 不是内容 ⇒ **不重复开 issue**。
- **CADENCE 9 finding**:五条流在 08-24 白天有过停摆,含本组「08-24T15:57Z → 21:48Z 5.8h」那条;
  本轮产出即补上,不阻塞。
- trunk python 套件 **18 passed / 0 failed**(开工与收尾各一次一致)。

## 1. 覆盖(章程固定节)

**本轮是"新语料优先"那一条 + 铁律 9(owner 优先项凌驾 issue 流)的合流**:W8 就是 owner P1 的取证波,
DoD 第 3 步(录像组核验)是本组的棒。

| 波 | 角色 | S3 `.dem` | 宽扫 | 有效局(两层) |
|---|---|---|---|---|
| **W8** `spot_20260825_0016{28,32,37,41}`,armed **28** | 取证腿 | 235 | **211/211** | 118 radiant-armed / 93 dire-armed |
| **W7** `spot_20260824_1812{41,45,49,52}`,armed **26** | **预登记对照腿**(批测台 §7.3) | 235 | **211/211** | 118 / 93 |

两波**同 4 个种子(888/895/896/906)、同采集配置(`--slots 16 --rec-slots 12 --games 22`)**,
armed 串**只差 `pulllane` + `towerfear`**。两波都在**本次会话里用同一份工具跑出来**,口径无漂移。

**深查 6 局逐帧**:`ae722d/003105_slot3`(zuus)、`c1eace/002545_slot12`(CM)、
`6a2b32/003057_slot9`(zuus pos4 + CM 两个 episode)、`364fb1/002538_slot6`(witch_doctor)、
`364fb1/001909_slot12`(witch_doctor)、`c1eace/001906_slot11`(CM)。

**⚠️ 覆盖的诚实边界(两条,都是真的)**
1. **第一次扫的是半波**:00:37Z 开工时 S3 只有 144 个 `.dem`(其中 120 局非暖场),
   而且**119/120 是 radiant-armed** —— 单物理层,按铁律 4(i) 不构成结论。
   01:13Z 复点发现已涨到 **235**(波次已自毁,`describe-instances` running/pending **0**),
   于是**整波重扫**拿到两层。**第一版 120 局的读数与最终版同结论**,两版都记在下面。
2. **W8 全 211 局跑 `pullcamp_domain` 会被工具自己的 approach guard 中止**(§4,已开 **GH #169**)⇒
   最终读数是**去掉 run `..._364fb1` 的 158 局**(其余三 run 全量,**两层齐备**:90 radiant / 68 dire)。
   宽扫仍是 211/211;受影响的只有 domain/quality 这一层的分母。

## 2. ⭐⭐ 决定性证据:不是速率,是**哪些营被打**

n≈20 的速率讲不清楚;**营地分布讲得清楚**。把每个 poke episode 的开拉点归到 28 个复现营
(`derive_camps`,跨局复现 worst 89u),按每 100 局归一化:

**radiant-armed 层**(W7 118 局 → W8 90 局)

| 营 (x,y) | W7 /100局 | W8 /100局 | 该营产生的 connect |
|---|---|---|---|
| **(4000, −5000)** 安全线拉野营 | 16.1 | **15.6** | **2 → 2** |
| (200, −5200) | 5.9 | 3.3 | 0 → 0 |
| (−4000, 1000) | 3.4 | **0.0** | 0 → 0 |
| (−1400, −3400) | 3.4 | **0.0** | 0 → 0 |
| (−5000, −200) | 0.8 | **0.0** | 0 → 0 |
| (−4800, 4000) | 1.7 | 1.1 | 0 → 0 |
| (−2600, 3800) | 0.0 | 1.1 | 0 → 0 |
| **合计** | **31.4** | **21.1** | **2 → 2** |

**dire-armed 层**(W7 93 局 → W8 68 局)—— **独立复现同一形状**

| 营 (x,y) | W7 /100局 | W8 /100局 | connect |
|---|---|---|---|
| **(−4000, 4800)** 安全线拉野营 | 10.8 | **8.8** | **2 → 2** |
| (−800, 5000) | 6.5 | 13.2 | 0 → 0 |
| (−2600, 3800) | 8.6 | **0.0** | 0 → 0 |
| (1000, 2600) | 5.4 | **0.0** | 0 → 0 |
| (3400, −1400) | 4.3 | **0.0** | 0 → 0 |
| (1200, 4200) | 3.2 | **0.0** | 0 → 0 |
| (−4800, 4000) | 1.1 | 0.0 | 0 → 0 |
| **合计** | **39.8** | **23.5** | **2 → 2** |

**两层同一句话**:被清零的营,**在对照腿里的 connect 记录全部是 0**;
**唯一产生 connect 的那个营(两层各一个)一个没动**。
这正是 `J.IsCampBesideLane`(`jmz_func.lua:8189`)声称要做的事,
而且是**在 dump 里根本读不到 lane 几何的前提下**,靠"哪些营消失了"把它读出来的 ——
这条读法本身是本轮的方法学收获:**当谓词的输入不可观测时,去看它的输出集合的形状**。

⇒ **`pulllane` = WORKING**。它 armed、它执行、它的行为逐格符合子句语义,且**没有副作用**
(见 §3 的代价栏)。

## 3. 批测台交办的三条验收量,逐条给(两层 + 平衡估计量,GH #148(i))

| 量 | W7 ab | W7 ba | **W8 ab** | **W8 ba** | 两层同号? |
|---|---|---|---|---|---|
| poke episode /100 局 | 31.4 | 39.8 | **21.1** | **23.5** | ✅ 同降(平衡 35.6 → **22.3**) |
| **connect (i) 率** | 10.5%(2/19) | 8.0%(2/25) | **14.3%**(2/14) | **20.0%**(2/10) | ✅ 同升(平衡 9.3% → **17.1%**) |
| connect (ii) 率 | 15.8% | 8.0% | 14.3% | 20.0% | ✅ 同升(平衡 11.9% → 17.1%) |
| 跟随小野→最近线兵**中位** | 1,172 | 1,690 | **933** | **1,021** | ✅ 同降 |
| 15s 掉血中位(pp) | 18 | 18 | 21 | — | 代价栏,未见恶化 |
| 20s 内死亡 | 0/37 | 0/37 | 1/24(第一版 120 局) | — | n 太小,只登记 |

批测台交办的三条**全部达标**:连接率没塌(升)、`poke_episodes` 没塌向 0(19 + 16)、
跟随小野→最近线兵中位**两层同降**。

⚠️ **连接率这一格必须连着刀口一起读,否则会被高估 —— 分子没动,动的是分母。**
connect 的**绝对次数**两波两层**全部是 2**(每 100 局:radiant 1.7 → 2.2,dire 2.2 → 2.9)。
17.1% 这个数**全部来自 drag episode 从 44 降到 24**。正确读法:
**它没有造出新的成功拉野,它删掉了结构上不可能成功的那些**。
这恰好是这条子句自己声称的价值(不浪费辅助的时间),但**不是**"拉野变好了"。
按 #148(ii),率和绝对数同时给,让刀口自己暴露。
**同样重要的是它没有误伤**:两层的 connect 绝对次数各自 2→2,**被删掉的 poke 里一个成功的都没有**。

⚠️ **两个量两层反号,按纪律不写进结论,只登记**:
- 决策侧「开拉点→最近线兵间距」中位:ab 1,540 → **1,741(升)**,ba 2,146 → **1,587(降)**;
- leash(跟随小野离营最远)中位:ab 747 → 490,ba 711 → **796**。

**⭐ 这一条是 #148(i) 当场救回来的,值得单记**:本轮第一版只有 radiant 层
(S3 当时只到货 144 个 `.dem`,119/120 是 radiant-armed),我在那一版上**本来要写出
「间距反而变大了」这个假发现**,并且已经为它准备好了一套机制解释(lane 折线延伸到中路 ⇒
存活的营偏向 lane 远端)。dire 层一到货就反号 ⇒ **那是噪声,机制解释是给噪声编的故事**。
「永远 swap-and-average」这一条在**决策侧读数**上同样成立,而且比经济读数更隐蔽。

⚠️ **混淆项,如实登记**:W8 同时新 arm 了 `towerfear`(26 → 28),**无法完全隔离**。
但 `towerfear` 没有任何一条关于选营的子句,而 §2 的消失形状**逐格符合 `IsCampBesideLane` 的几何**
⇒ 归因给 `pulllane` 很强,但**不是证明**。

## 4. ⚠️ 工具侧新发现,已开 **GH #169 [harness]**:一帧 spirit_breaker 让整份 211 局语料读不出任何数

`pullcamp_domain.py` 在 W8 全量上直接 `[fatal]` 退出:
`approach guard: max |clean approach| = 2030 u (cap 1980)` ——
`WALK_CAP = 660.0`(`:134`)× `APPROACH_S = 3.0`,**只超 50u(2.5%)**,代价是整份语料零产出。

**这一帧不是 dash,也不是位移损坏**(`WALK_CAP` 注释 `:127-133` 把超标解释为 dash,这次两者都不是):

```
 65.5 (-4537, 3635) hp 0.77
 66.5 (-4124, 3344) hp 0.78     ← guard 报的帧
 67.5 (-3499, 2917) hp 0.80
 68.5 (-3078, 2635) hp 0.81
 69.5 (-2455, 2180) hp 0.82
 70.5 (-2061, 1852) hp 0.83
```

逐秒步长 **505 / 757 / 507 / 772 / 513 u/s,连续 5 秒**(单帧尖峰才是 dash 或坐标损坏的形状),
同窗口 t=67.9 有 `modifier_spirit_breaker_bull_rush_intrinsic` 的 STACK_EVENT 与
`spirit_breaker_greater_bash` 的 DAMAGE ⇒ **Bull Rush(innate)+ Greater Bash 的移速加成**,
两者都是持续增益不是位移技能;血量单调回升、方向单调 ⇒ 一次带增益的正常行军。
⇒ **问题在常数不在豁免名单**:660 u/s 不是"英雄能走多快"的上界。

**本轮实付的代价**:只能丢掉一整个 run(`..._364fb1`,53 局)才拿到两层齐备的读数,
覆盖率从 211 局掉到 158 局。#169 里给了三条修法建议(首选:把 guard 从"绝对位移上界"
改成"窗口内有没有该英雄自己 author 的事件能解释这次位移",没有解释的超标才 `[fatal]`)
+ 一条**必须保留牙齿**的验收断言。

## 5. 帧证据(深查 6 局,`foll→wave` = 跟随小野到最近线兵)

**(甲) 一次连上的拉野** `ae722d/20260825_003105_slot3` **zuus pos5(armed,radiant)**:

```
103.5  hp0.70 d(camp) 897  neut 0                    ← 走向营
106.5  hp0.74 d(camp) 396  neut 5  foll→wave 2469    DAMAGE zuus->forest_troll_berserker [zuus_static_field]  ← 仇恨
109.5  hp0.68 d(camp) 250  neut 6  foll→wave 1589
112.5  hp0.61 d(camp) 440  neut 6  foll→wave 1111
115.5  hp0.59            neut 3  foll→wave  276      ← 回拖
118.5  hp0.63            neut 6  foll→wave  115
121.5  hp0.66            neut 4  foll→wave   99      ← ⭐ 接上了
120.5                                                 GOLD/XP -> zuus
```

**(乙) 第二例同形状** `c1eace/20260825_002545_slot12` **crystal_maiden pos5**:
t=345.5 开拉(d(camp)=321),`foll→wave` 1722 → 1046 → 811 → **91 @356.5**。

**(丙) MISS 的形状同样清楚,而且指向下一条杠杆**
`6a2b32/20260825_003057_slot9` **crystal_maiden pos5 t=77.4**:78.4 仇恨(d(camp)=23,4 只跟),
一路走向兵线,**她自己**在 86.4 就到了 `d(wave)=234` —— 但 `neut` 在 **84.4 已经归 0**:
**小野先脱缰,她后到线**,`foll→wave` 最好只有 661。
同局 **zuus pos4 t=305.4**:开拉点离最近线兵 **3,377u**,他拖了 2,000u(d(camp) 2005→410),
`foll→wave` 最好 1,499,317.4 起 `neut=0`,而他自己到了 `d(wave)=256`。

**CONN 与 MISS 在这个量上不重叠**(第一版 120 局的 24 个 episode 全表):
CONN 的 `min(foll→wave)` = **99 / 91**,其余 MISS **全部 ≥661**
(与 08-23T09:09Z 那轮 CONN ≤282 / MISS ≥734 同形,阈值稳定)。

**(丁) 另两局**(`364fb1/002538_slot6` witch_doctor NODRAG、`364fb1/001909_slot12` witch_doctor MISS、
`c1eace/001906_slot11` CM MISS)是同一形状的重复:开拉后 `d(fount)` 反复上下、
小野在 6→1→0 之间掉光,`foll→wave` 停在 827–961。

## 6. 交棒

1. **GH #117 追评**((a)=WORKING 全文:两层营地表 + 三条验收量 + 刀口 + 承重帧 + 复现命令)。
2. **GH #169 新开 [harness]**(approach guard 常数,承重帧 + 三条修法 + 保留牙齿的验收断言)。
3. **给协同组的下一条杠杆(写在 #117 §4,不新开 issue)**:`IsCampBesideLane` 管的是
   **垂直于 lane 的间距**,而拉野真正要够到的是**兵线当前站在 lane 的哪一点** —— 这两件事正交,
   **纵向那一格现在没人管**。§5(丙) 的两个 MISS 都是"营在线边上、但兵线在几千 u 之外",
   小野的 leash(本语料中位 **490–796u**,p90 757–964)结构上覆盖不了。
   建议的下一条子句是**沿 lane 的偏移**(营到我方兵线当前位置 / 到我方一塔),
   **常数从 leash 的实测分布取,不要从垂直间距的 1200 取**。本组不越位定方案。
4. **不申请波次、不改 bot Lua、不裁 promote/reject、不花 AWS 钱。**
   `towerfear`(`strategy-12`)的 (a) **本轮未做**,语料已在本地/S3,归下一轮。

## 7. 交付与验证

**代码改动(dev-only,非 Workshop 交付物,`bots/`/`game/` 0 改动)**:
- `pullcamp_quality.py`:新增 `--side {radiant,dire}`(GH #148(i) 的 ab/ba 分层;
  **过滤器而非内建拆分**,pooled 代码路径逐字未动 ⇒ 分层跑不可能改变不分层跑的输出;
  **先滤局再滤 episode**,否则 episode/局 的分母会用整波的、差 ~2 倍看起来像效应);
  新增决策侧一列「开拉点→最近线兵间距 @ poke 帧」(带 `n=x of y`,陈旧采样跳过不补 0)。
- `pullcamp_frames.py`:新增 `d(wave)` / `foll→wave` 两列 —— connect 判断依赖的那**一个**数
  以前每次深查都要现写脚本重算(两个估计量当初就是这样漂开的);
  无跟随小野时打 `-` 而不是 0 或裸的最近兵距,否则会凭空造出 connect。

**回归**:pooled 路径复核 —— W7 不分层跑 **74 poke / 44 drag / connect 4**,
与两层相加(37+37 / 19+25 / 2+2)**逐位一致**。

**验证**:`bots/`/`game/` **0 改动** ⇒ 铁律 6 的 Lua 两条无适用对象
(容器无 luacheck/lua5.1,**不声称跑绿过 Lua 全量**);`tests/run_py_tests.sh`
**18 passed / 0 failed**(开工与收尾各一次一致)。
**AWS**:S3 只读,EC2 **$0**(`describe-instances` running/pending **0**,W8 已自毁),
**未调用 Cost Explorer**。

## 8. 下一轮优先

1. ⭐ **`towerfear`(`strategy-12`)的 (a)** —— 同波语料已在手,含批测台点名的**不许省的反向判据**
   (死亡数不许上升 / 塔下总撤退不许塌零);
2. ⭐ **W6 解锁棒:`strategy-5b` 卡的事件轴量级**(批测台**第四轮催**,零 AWS,语料已交付)
   —— 这是唯一还挡着 W6 的东西,**本轮又没做,连续第二轮**;
3. **#169 落地后在 W8 全 211 局上复核 §2/§3**(现读数是 158 局);
4. ⭐ **第二种视野证人(单体指向技能)**(#159 §3 最薄的一环,目前只有 23.4% 的贴脸按下有证人)
   —— **本轮为 owner P1 让路,顺延一轮**;
5. **ab/ba 分层回灌 `fieldbuy_silence.py`/`stayfield2_margin.py`**
   (**连续第八轮登记**;本轮把纪律用在了 `pullcamp_quality.py` 上,§3 那条"救回假发现"就是它的回报);
6. `stayfield`(TP 腿)第一失败子句分解;
7. 打野反证 fixture 钉 `002103_slot5 t=634.2 skeleton_king`(**已顺延十四轮**);
8. `hero-1` 的 153 局 WK 语料(**22 轮**);`l5combo` 的 (a)(**第三十八轮**);
   `make_fixture.py` 钉 `062551 t=205.5 jakiro`(#45,**第二十五轮**);`axebuyblink` armed 的波次。

## 9. 本次会话 token 用量

```
TOKENS total_in=13,434,882 out=74,055 turns=90
```
(统计时刻之后的收尾回合不计入。本轮的量级主要来自**两波各 211 局的宽扫 + 两次 domain/quality 全量跑**
—— W8 因 §1 的覆盖边界扫了两遍,这一次是真的多花了一遍。)
