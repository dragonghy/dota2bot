# 录像检查组 2026-08-21T22:05Z(第三十五次触发)

**对象**:上一轮登记的「下一轮优先 (2)」= 把 18:45Z 的 null 通道分解补做到 `lf_rescue`(+8.57),
也就是总监 §AG.2 点名的重算(「历史上用 DiD 登记过的 (a) 全部需要按本节重算」)。

**宽扫 32/32 局**(8 个 run 全部重下重跑 `sweep_run.sh`,**按 run 分目录**,`-interval` 默认 **1.0s**);
**深查:32 局 × 2 条腿 = 64 次全帧门重放(不抽样)+ 24 个归属事件的逐帧还原(落点 / 落地时刻
1600u 内敌人 / 落地后 45s 最近接近距离 / 施法前 20s 己方塔血量按坐标配对)+ 4 档阈值的完整反事实重扫。**
零 EC2 支出,未改 bot Lua,未动 gate。

**开 issue `#96 [bug]`(总监),评论 `#37`。交付**新工具
`tools/batch_test/behavioral/lf_rescue_null_channel.py`。

---

## 0. 语料:批测台 20:06Z §2 的建议口径,独立复核通过

| 臂 | run | id 数 | 局 | r/d |
|---|---|---|---|---|
| A | `spot_20260819_121038/121044/121050/121056_1_main` | 13(含 `lf_rescue`) | 16 | 8/8 |
| B | `spot_20260819_121122/141128/141131/141134_1_main` | 12 | 16 | 8/8 |

从每个 run 自己的 `games_manifest.jsonl` stamp 读出 cand 串(不从目录名猜):
**A−B = `{lf_rescue}`,B−A = ∅**,唯一变量成立;两臂各 4 种子(859-862)、每种子 4 局、2r/2d。
批测台点名的 3 个残 run(`121105/121111/121117`)**已剔除** —— 它们与 14:11Z 的整种子重跑
重复计数,且会把臂 B 变成 11r/8d(方向已知的侧向混杂)。**批测台的归档侧结论逐条复现。**

## 1. ⭐ 主结果:四个格子,其中两个 #37 从来没有

签名 =「施法瞬间全部可离线观测的 `J.GetRescueTpTarget` 前置条件成立」**且**
「落点落在 `J.GetNearbyLocationToTp` 预测点(最近存活己方塔 + 575 朝友军)1000u 内」。

| seed | A_armed | A_base | B_armed | B_base |
|---|---|---|---|---|
| 859 | 3 | 0 | 2 | 1 |
| 860 | 2 | 0 | 1 | 1 |
| 861 | 7 | 0 | 1 | 1 |
| 862 | 4 | 0 | **0** | 1 |
| **合计(每格 16 局)** | **16** | **0** | **4** | **4** |

- **`ARMED A−B`(§AG.2 规定的唯一估计量)= +0.750 次/局**,种子配对 SE 0.306,**|t| 2.45**,
  逐种子 4/4 同号 `[0.25, 0.25, 1.5, 1.0]`;「至少一次的局数」**10/16 vs 3/16,Fisher p=0.0290**。
  ⇒ **条件 (a) 站得住,`lf_rescue` 确实在开火。**
- **`NULL` 通道(两条基线腿,同一份出厂代码,该读零)= −0.250 次/局**,逐种子恒 −0.25
  (A 基线 16 局**全零**、B 基线**每种子恰好 1 次**),Fisher **p=0.1012**。
  按 §AG.2 自己的措辞这是「小格噪声」,但**两种情形都取消 DiD 的资格**:
  被禁的 DiD 会读 **+1.000**,其中 **25% 住在两条本该相等的基线腿之间**。
- **供给(按 #92 必报)**:armed 腿 A 26.56 vs B 25.12 机会 episode/局(比 **1.06**),
  base 腿 21.69 vs 22.12(比 **0.98**)。**不是供给伪影。**
- **阈值不是自由参数**(#92 的教训):落点-预测距离双峰,间隙 **922u → 3069u**;
  1000/1500/2000/3000u 四档**四个格子逐位不变**。与 `l1trade` 的承重子句(沿阈值单调翻号)
  形成对照 —— **同一族检查,这次的答案是「稳」,并且是量出来的不是断言的。**

## 2. ⭐ #37 那句「arm B 0 次,p≈0.008」不成立了(但主结论不倒)

#37 的臂 B 是 **7 局**(12:12Z 那波三台被 `instance-terminated-no-capacity` 回收,
14:11Z 的整种子重跑当时还不存在)。那 7 局里确实 0 次 —— 本轮**逐位复现**:
种子 862(即 `121122` 那 4 局)armed 侧仍读 **0**。新增的 12 局贡献了全部 4 次。

**决定性的是 `B_base` 那一格:臂 B 的基线侧一个 gate 都没开,是纯出厂代码,它同样读 4 次
(0.25 次/局)。**
⇒ **这个签名不是 `lf_rescue` 的指纹,是一个形状**;出厂代码里另有分支以 ~0.25 次/局 产出
同样的形状。#37 用「臂 B 观测 0、期望 4.8」算出的 p≈0.008,**分母是一个恰好为空的 7 局样本**。

### 逐帧核验:那 8 次(臂 B 两条腿)到底是什么?

**防守 TP 的解释不成立**:24 次归属事件(含臂 A 的 16 次)里,落点最近的那座己方塔在
施法前 20s 内 **hp 变化全部为 0.00**。没有一座塔在挨打。

三帧样本(全部 `lf_rescue` **未 armed**):

| 局 | t | 责任者 | 友军 | 距离 | divers | 落点差 | 落点→友军 |
|---|---|---|---|---|---|---|---|
| `141128_1_main/20260819_143611_slot1`(859,臂B **armed**,dire) | 525.9 | lich 0.90 | lina **0.12** | 7882u | ogre_magi + venomancer | 311u | **439u** |
| `141134_1_main/20260819_143116_slot1`(861,臂B **base**,radiant) | 503.5 | lina 1.00 | phantom_assassin **0.12** | 13933u | skeleton_king | 235u | 1397u |
| `121122_1_main/20260819_123519_slot1`(862,臂B **base**,radiant) | 355.0 | skywrath_mage 1.00 | sniper **0.20** | 13842u | lina + OD | 440u | 658u |

第一行值得单独说:**本语料里最像「按设计工作」的一次救援(落点→友军 439u,落地时落点
1600u 内有 venomancer 677u / ogre_magi 923u)发生在 `lf_rescue` 关着的时候。**
第三行:落地后 +28.0s 才走到友军 61u,而友军 15s 内已经死了。

## 3. #37 的 BUGGY 那一半:复现,而且第一次有对照臂

落点→友军距离(施法瞬间):

| 格 | n | min | 中位 | max |
|---|---|---|---|---|
| A/armed(`lf_rescue` 开) | 16 | 699 | **2009** | 5858 |
| B/armed(关) | 4 | 439 | 1431 | 3658 |
| B/base(出厂) | 4 | 658 | 1397 | 2536 |

A/armed 中位 2009u 与 #37 登记的 2289u 同阶 ⇒ **「门在算一场 4 秒结束的仗,派出的人要走
8 秒」这条机制结论不受本轮影响。**
新增:**出厂代码产出的同形状 TP 落得一样远。** ⇒ 这个病**不属于 `lf_rescue`,属于
`J.GetNearbyLocationToTp` 这个落点选择器本身**,`lf_rescue` 只是又一个消费方。
**这是对 #37「与 #23 同根因」那句猜测的独立支持** —— 现在有对照臂,不再只是猜测。
连带:#37 §5 的三条改法不受影响,但**验收帧应从两个消费方各取一帧**。

## 4. 保真度边界(如实申报,不是脚注)

**本轮不复现 #37 的臂 A 格子**:读到 **26 casts / 16 归属**,#37 登记 **20 / 11**,同样 16 局。
成因定位到一半:`tools/batch_test/behavioral/tp_attribution.py` **是 08-21 才进仓库的**
(`183ea2a`,`git log --diff-filter=A` 确认),**#37 当时跑的脚本从未提交**;仓库这一版的
英雄位置取自 `seed_draft`(GH #57,08-20),而 #37 用的是 `team_slot % 5 + 1`。
把位置源强制切回 `team_slot` 重跑得 **23 / 12** ⇒ 约一半漂移是这个,其余是未归因的工具漂移。
**跨轮引用本语料请用 26 / 16;#37 的 20 / 11 不可从仓库内任何产物复现。**
(与 18:45Z 对 `roamstale` 的 1855→2117 同类,但更重:那次工具在仓库里有历史,这次没有。)

## 5. 建议的处置(裁定权归总监,已写进 #96)

1. `lf_rescue` (a) 改为 **`WORKING(差分证实,+0.75 归属/局)+ BUGGY(效果)`**,
   撤回「arm B 0 次 / p≈0.008 / 独占」的措辞,换成本轮 2×2 + null 通道。**id 不动**
   (与 §AG.3 给 `roamstale` 的第五种处置同形:证据换掉,处置不变)。
2. **通例建议**:**「另一臂读零」只有在另一臂的样本量足以让零有意义时才是证据。**
   凡是用「对照臂 = 0」立 (a) 的,必须同时报对照臂局数 + 该签名在**出厂代码**上的本底率
   —— 后者在镜像装置里是**免费**的,就是基线腿。
3. #37 的验收帧从两个消费方各取一帧。

## 6. 交付

`tools/batch_test/behavioral/lf_rescue_null_channel.py`(只读,不碰 AWS):
- **臂守卫**:从 `games_manifest.jsonl` 读 cand 串,`A−B != {lf_rescue}` 或 `B−A != ∅`
  即 `exit 1`,**在打印任何数字之前**。**三次变异全部咬住**:两臂同给臂 A → 咬
  (`A-B=[] B-A=[]`);两臂交换 → 咬(`B-A=['lf_rescue']`);传无 manifest 的目录 → 咬。
- `--selfcheck`:16 个格子全非空 + 计数从 `tp_attribution.scan` 原始返回重数一遍,漂移即 `exit 2`。
- `--sweep-threshold`:四档阈值重跑整表(#92 要求的敏感性)。
- 常驻横幅「ARMED A−B 是估计量,基线腿是诊断,不要差成 DiD」;供给表按 #92 默认打印。

## 7. 本轮新踩的坑(已写进章程)

- **dumper 的 `buildings[].name` 只有四个字面量**(`tower` 3806 / `barracks` 2076 /
  `watch_tower` 346 / `ancient` 346),**没有塔的身份**。我第一版「哪座塔在挨打」按 `name`
  建字典 ⇒ 全场塔塌成一座,每个事件只打印一行、且是任意一座。**按 `(round(x), round(y))`
  配对**才是对的。这是「先量宽度再断言」那族的又一例:错法不报错,只是安静地少报。
- **`--diff-filter=A` 要作为引用工具产出数字前的例行检查**:一个检测器脚本可能比它
  产出的结论**晚**进仓库,那么「用仓库里的工具复现历史读数」这件事从一开始就不成立。

## 8. 验证

- `.lua` 改动数 = **0** ⇒ 铁律 6 的 `luacheck bots game` 无适用对象;本容器**未装
  `luacheck` / `lua5.1`**(fresh container,**如实记录未假装跑过**)。
- `py_compile` 通过;`--selfcheck` 通过(16 格全非空 + 逐格从原始返回复算一致);
  臂守卫三次变异全部 `exit 1`;阈值敏感性四档不动;批测台 20:06Z 的归档口径(8 run /
  16 vs 16 / 8r-8d / A−B={lf_rescue})逐条独立复现。
- **AWS**:EC2 **$0**,S3 GET 40 `.dem` + 40 `analysis.json`(≈350MB)+ 11 次 `s3 ls`
  ≈ **$0.02**,**未调用 Cost Explorer**。

## 9. 局数

- **宽扫 32/32 局**(8 run × 4 镜像局;每 run 的暖场局由 `sweep_run.sh` 自动跳过)。
- **深查**:64 次全帧门重放(32 局 × armed/base 两条腿)+ **24 个归属事件逐帧还原**,
  分布在 **17 个不同的对局**(臂 A 10 局 / 臂 B 7 局),远超章程 6 局下限。
- 无其它新对局(批测台 20:06Z:`soak/` 前缀总数 140,**连续第十二轮**不变)。

## 10. 下一轮优先

1. `l5combo` 的 (a),仍等去掉它的 bisect 臂(**连续第九轮** —— 18:45Z 已建议总监按
   §AF.3 先例直接裁定降级,本轮重复该建议)。
2. **本轮方法的下一个对象:`capmono`(#72,DiD −7.9pp ± 9.3)** —— §AG.2 点名的第三个、
   也是最后一个用 DiD 登记过的 (a)。它的两臂语料是 `1609xx`/`1807xx` 那 32 局,
   `capmono_refusal.py` 已有现成 scanner,套本轮的 null 通道分解即可,**免费**。
3. `cmrguard` 按 #63+#66 收窄后用**施法前帧**口径复跑四条验收 + #66 recall。
4. `axe_blink_domain.py` 锁 idx 后的复跑(**仍缺含 Axe 的语料**)。
5. 引用帧级率跨轮比较前先核对 `-interval`。

## 11. 本会话 token 用量

```
TOKENS total_in=9,860,817 out=81,921 turns=81
```
(统计时刻为收尾前;此后仅剩章程更新与 commit/push 两回合,不计入。
本轮 64 次全帧重放 + 4 档反事实重扫,长链路分析,量级正常。)
