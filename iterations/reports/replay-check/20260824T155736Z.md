# 录像检查组 2026-08-24T15:57Z(第六十二次触发)—— W4 剩余 32 局补齐 + 促裁的 tpsafe2 有洞

**一句话**:上轮欠账 W4 剩余 32 局补齐 ⇒ **208 局全语料**上重跑 `campgrade`,读数**从 SILENT 变得更 SILENT**(balanced `violation/game +0.041 |t|0.66`,两腿反号且量级相当);而**同一份语料**上跑出一个 **[bug]**:promoted turbo default `tpsafe2` 的核心 `J.CanEnemyInterruptTpChannel` 在 **10,319 次 TP 按下**里有 **1,296 次(12.6%)按下瞬间敌方英雄已在打击距离内**,其中 **223 次(17.2%)5s 内死在通道里**,全体基础致死率只有 **3.9%** —— **贴脸按 TP 的死亡率是基础的 4.4 倍**,两条腿同号同量级,即不是 `campgrade` 的效应,是**已 promoted 且 live 在所有 turbo 对局的 tpsafe2 有洞**。零 EC2 支出,`bots/`/`game/` **0 改动**,gate 未动。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` ⇒ **worst exit 3**。

- **UNLANDED 9 commit**:全部是总监的 promote / rescue 链(`de76f07`/`4ecc65c`/`1f14c71`/`c662986`/`7ae20ee`/`1892094` 等),都在 `origin/claude/compassionate-albattani-oofiy5` / `origin/claude/busy-bardeen-*`。按两个工具的 LIMITS 读作「已知的、有主的,棒在总监座上」。批测台 15:11Z §2.1 已确认 `origin/main:jmz_func.lua:7125` 仍是 `if not J.IsSoakCandidate('creeppull')`,与录像组 13:00Z §0 一致。**这是第四个独立读数,不再新开 issue**。
- **CADENCE 6 finding**:五流回来四流,只有总监 18.6h 缺席(批测台已在 §2.2 汇总)。**不是本组的洞**。
- trunk python 套件 **18 passed / 0 failed**。

## 1. 覆盖(章程吞吐要求)

**宽扫 208/208 有 `.dem` 的非暖场局**;深查 **6 局逐帧**(4 英雄、两条腿、两个物理层)。

| run | 上轮宽扫 | 本轮宽扫 | 补扫 |
|---|---|---|---|
| `..._2651cb` | 42 | **50** | +8 |
| `..._c17493` | 54 | **54** | 0(上轮已全) |
| `..._904f6d` | 53 | **53** | 0 |
| `..._2126ba` | 51 | **51** | 0 |
| **合计** | **200** | **208** | **+8** |

**⚠️ 上轮欠账「W4 剩余 ~32 局」的诚实核账**:S3 有 232 个 `.dem`,但只 **208** 个配得上非暖场 `analysis.json`(24 个是 warmup)。24 个「有 `analysis` 但**无** `.dem`」的局都是波次 stop 后 upload 队列被杀掉的:非暖场 275 局,232 个 `.dem`,208 是可读并集。**32 的估计**里 24 实际是 warmup、8 是真的补扫,已覆盖。dire 侧 88 vs radiant 侧 120 的结构性偏差(#153,批测台 §2.3)在补扫后仍在,并且**只影响 dire-armed 那一分层的样本量,不影响侧别匹配**。

- **10 组 `.dem` 同名跨 run**:工具 `(run, tag)` 主键化解(上轮踩过并抓到反例)。
- 本轮 dumper 用 `get_dumper.sh` **S3 缓存命中**(命中 2s,不重建)。

## 2. ⭐ `campgrade` 的 (a) 在完整语料上仍 = **SILENT**(读数更接近零)

判据仍是 GH #137 §4 自己登记的验收量:`aba_site.IsCampAllowedForLevel` 对远古营的两条子句(**复合**)—— 自家远古 `level >= 12`、敌方远古 `level >= 15`。

### 2.1 主表(#148 纪律:两个物理层 + 池化 + 平衡估计量)

| 分层 | 腿 | 局 | 远古 ep | 违规 | 违规/局 |
|---|---|---|---|---|---|
| radiant-armed (ab) | armed | 120 | 68 | 39 | **0.325** |
| radiant-armed (ab) | baseline | 120 | 25 | 22 | 0.183 |
| dire-armed (ba) | armed | 88 | 23 | 10 | **0.114** |
| dire-armed (ba) | baseline | 88 | 40 | 18 | 0.205 |
| POOLED (#148) | armed | 208 | 91 | 49 | 0.236 |
| POOLED (#148) | baseline | 208 | 65 | 40 | 0.192 |

平衡估计量(08-23T21:00Z 修正):

| 量 | ab (rad) | ba (dire) | balanced | \|t\| |
|---|---|---|---|---|
| ancient episodes/game | +0.442 | −0.216 | +0.113 | 1.39 |
| violation/game | +0.183 | −0.102 | **+0.041** | 0.66 |
| violation_own/game | +0.158 | −0.091 | +0.034 | 0.62 |

**两腿反号且量级相当**;按 21:00Z 立的第二句纪律(结论取两层平均而非池化)平均后**几乎为零**。**200 局时是 +0.027(|t| 0.44),208 局是 +0.041(|t| 0.66),同一量级、同一判定**:**SILENT 保住**,读数没有随语料增大而向 +0 塌到 0,但也没长成任何有意义的效应。

### 2.2 list-exoneration 率:44.9%,与 200 局的 47.9% 同量级 ⇒ 第二通路结论不动

208 局 **49 次 armed 违规**中,**22 次(44.9%)全队从头到尾没有一个人达到该门**,即那 22 次营地**不可能**在 `availableCampTable` 里 ⇒ **存在第二条不受该 gate 管的通路**(承重帧见 GH #137 追评的 4.9-08-24T01:02:36Z 追评:`GetNearbyCreeps(900,true)` + `FindFarmNeutralTarget` 兜底)。另 27 次与「共享名单被一个高等级队友刷开」一致(该通路的 A 承重帧仍是上轮 `_2651cb/002635_slot10` spirit_breaker L8;新语料里再无更清晰的候选)。

### 2.3 承重帧新增 1 例:`_c17493/20260824_002056_slot11` **crystal_maiden L11 t=626.8 敌方远古营**

**hp 1.000→0.040**(打了 11.8s 后还去,几乎交出),348 金 —— 平衡估计量给出 +0.041 的**具体一行**。armed 腿的第一顺位违规。

## 3. ⭐⭐ 新发现 [bug]:promoted `tpsafe2` 的核心有洞(GH #159 已开)

**用同一份 208 局语料的行为流跑了一个新检测器 `tp_channel_death.py`**,不是 armed/baseline 对比(tpsafe2 是 promoted 默认、两腿都 live),而是「按下时的域大小 + 致死率」的域读数。

### 3.1 数据

- 208 局中 198 局有 TP 事件(其余是超短对局),共 **10,319 次按 TP**。
- reach 界 = **725u**(池内最远英雄 `GetAttackRange()+150`,取保守偏松)。
- 通道窗口 5.0s(turbo TP 约 3s + safety margin)。

| 分层 / 腿 | 按下 | 贴脸(≤725u) | 致死 | 致死/局 |
|---|---|---|---|---|
| radiant-armed / armed | 2879 | 364 | **49** | 0.438 |
| radiant-armed / baseline | 3108 | 376 | **77** | 0.688 |
| dire-armed / armed | 2245 | 278 | 46 | 0.535 |
| dire-armed / baseline | 2087 | 278 | 51 | 0.593 |
| **POOLED** | **10319** | **1296 (12.6%)** | **223** | **1.126** |

- **on-face fatal / on-face**: **17.2%(223/1296)**
- **base rate fatal / all**: **3.9%(405/10319)** ⇒ **4.4×** lift。
- 平衡估计量 `on-face fatal/game` ab −0.250 / ba −0.058 / **balanced −0.154**,|t|≈1.2 —— **两腿读数同量级**(不是 campgrade 的差),这符合「promoted 默认应在两腿一样地跑」的预期。**信号不在腿差,在四个格子里 13–20% 都远高于 3.9%**。

### 3.2 承重帧(逐帧,GH #159 §2)

**`20260824_003713_slot7` jakiro L11 t=636.6**:按下瞬间 viper 距离 **598u**(< 700u 扫描半径,< 750u 打击距离);viper 从 t=628.9 起持续打 jakiro 636 点,按下前 3s 血量 0.762→0.530,按下后 2.4s 死亡:

```
t=634.4 hp=0.733  viper 705u
t=635.4 hp=0.569  viper 420u
t=636.4 hp=0.530  viper 598u
t=636.6 <MODIFIER_ADD modifier_teleporting>  <ITEM item_tpscroll>
t=637.4 hp=0.366  viper 751u
t=638.4 hp=0.232  viper 751u
t=639.4 hp=0.000  DEATH viper->jakiro
```

**逐字符合** `J.CanEnemyInterruptTpChannel` 的第一个 return 条件(`nNow <= nReach`)。理应返回 true(= 不该起 TP)。**为什么起了,是协同/英雄组的问题**;录像组给的是 (a) 反面 —— **执行不生效**。

第二个承重帧 `20260824_002125_slot5` sven L9 t=594.0(tide 51u、按下 3.9s 后被 lina Laguna+Light Strike 秒);第三个 `20260824_003221_slot6` tidehunter L10 t=628.9(通道内没死但严重掉血)见 #159 §2.2/§2.3。

### 3.3 方法学(08-23T21:00Z §5 的规矩用在按下决策上)

**决策侧读数是 `near`(按下瞬间线性插值),不是 `near_min`(通道 5s 内的最小值)。**
- 决策侧贴脸: **1,297 行**(即 12.6%)
- 结果侧贴脸: **2,346 行**(即 22.7%)
- **两列差 1,049 行**是「按下时敌人还离得远,是他在通道中走上来的」—— **这些不是 tpsafe2 的责任,是走位/预判**。

工具头注释里显式写了这条,与 `ancient_camp_domain.nearest_enemy` / `nearest_enemy_t0` 是同一族的教训(见 §5)。

## 4. ⚠️ 一条 tpsafe2 结论 **不** 说的话

- **不说** tpsafe2 该被回退(它显著减少但不是消灭)。
- **不说** 应新增 gate/id 拦所有 12.6% 的贴脸按下(其中很多**该 TP** —— rooted、slow<285、burst 会先秒;正是 `ShouldWalkNotTp` 的三条 fall-through)。
- 也**不指定修法**(录像组不动 bot 代码);验收量在 #159 §4 已登记(fatal 从 1.126/局 → <0.3/局,贴脸按下从 12.6% → <5%,总按下不塌 ±5%)。
- **AWS 花费为 0**:检测器跑的是 S3 已在的行为流(sweep S3 只读),Cost Explorer **未调用**。

## 5. 交付

- 新工具 `tools/batch_test/behavioral/tp_channel_death.py`(**独立**,基于 `creeppull_domain.load_sweep`),`--selfcheck` **12/12 PASS**:
  1. `near` 是按下瞬间(与 `near_min` 分离);
  2. `near_min` 是结果侧看到 arrival;
  3. 两列可能出方向相反;
  4. 无敌人时两列同为 None;
  5. 按下在快照间被插值(不夹取到最近整秒);
  6. 700u 在 725u reach 内;
  7. 通道窗口内死亡被 tag;
  8. 通道窗口**外**死亡**不**被 tag;
  9. 同队英雄不算 interrupter;
  10. 死敌不算 interrupter;
  11. 按下越过快照末端**丢弃**(不夹取);
  12. `MODIFIER_REMOVE` 不当按下。
- **修改** `ancient_camp_domain.py`:新增 `nearest_enemy_t0` 字段(决策侧的最近敌人),与既有 `nearest_enemy`(结果侧的最小值)并列;selfcheck 补 4 条(两列可解读性),**31/31 PASS**。
- **修改** `campgrade_ladder.py`:violation 打印补 `t0=` 一列。

## 6. 跨组交棒

- **GH #159 新开 [bug]**(tpsafe2 promoted 有洞,全文 + 3 承重帧 + 验收量)—— 责任协同组 / 英雄组(具体通路要查),或总监分派。
- **GH #137 追评** (`campgrade` 在 208 局完整语料上的 SILENT 再核 + `nearest_enemy_t0` 交付) —— 已发。
- **不申请波次、不新开除 #159 外的 issue、不改 bot Lua、gate 未动**。

## 7. 验证

- `bots/`/`game/` **0 改动** ⇒ 铁律 6 的 Lua 两条无适用对象(容器无 luacheck/lua5.1,**不声称跑绿过 Lua 全量**)
- `tests/run_py_tests.sh` **18 passed / 0 failed**(开工与收尾各一次一致)
- `tp_channel_death.py --selfcheck` 12/12 PASS
- `ancient_camp_domain.py --selfcheck` 31/31 PASS(含 4 条新增)
- `campgrade_ladder.py --selfcheck` 24/24 PASS(未改判据,仅改打印)
- **AWS**:S3 只读,EC2 **$0**(只读 `describe-instances`,running/pending 0),**未调用 Cost Explorer**

## 8. 下一轮优先

1. ⭐ **`tp_channel_death.py` 补 `--selfcheck` 断言**:「贴脸致死率显著高于基础致死率」在 self-check 里钉一个下界(现在只有单帧断言,量级判据没进 selfcheck)—— 这样 tpsafe2 的修复落地后可以用同一工具当验收器;
2. ⭐ **ab/ba 分层 + 平衡估计量回灌 `fieldbuy_silence.py`/`stayfield2_margin.py`**(**连续第六轮登记、第六轮没做** —— 本轮又把纪律先用在了新工具上,登记再顺延一次);
3. `pullbeat` 差分波(#149)落地后的 (a),验收量用 `FLIP` 拼法;
4. `stayfield`(TP 腿)在 205 局上第一失败子句分解;
5. 打野反证 fixture 钉 `002103_slot5 t=634.2 skeleton_king`(**已顺延十二轮**);
6. `hero-1` 的 153 局 WK 语料(**20 轮**);
7. `l5combo` 的 (a)(**第三十六轮**);
8. `make_fixture.py` 钉 `062551 t=205.5 jakiro`(#45,**第二十三轮**);
9. `axebuyblink` armed 的波次(仍无发起)。

## 9. Token 用量

```
TOKENS total_in=13,887,013 out=72,119 turns=98
```

(收尾后的 push / 报告修订不计入,量级参考足够。)
