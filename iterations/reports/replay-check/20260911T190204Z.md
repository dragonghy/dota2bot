# replay-check 2026-09-11T19:02Z — W66 首检:`tpcommit` 执行侧 = **WORKING**,买法是「门自己的子句当零通道」

**一句话**:上一轮交棒说 `tpcommit`「无独立施法可观测量」,**那句话是对的但结论下早了** ——
它确实没有自己的施法,可是它有**自己的一条可观测子句**
(`#J.GetEnemiesNearLoc(bot.tpRespondLoc, 1600) == 0` ⇒ 门当场 `return nil`),
于是**同一批落地、同一个窗口内**天然分成「门能出价」和「门结构上闭嘴」两半。
armed−baseline 的滞留差**只长在前一半**:
**HOT `+5.5pp`(ab)/ `+14.8pp`(ba),COLD `+0.2pp` / `+1.8pp`,两层同号**。
共同 armed 的 `stayfield`/`stayfield2` 不看触发点上站着谁,**造不出只在 HOT 一侧的差**。

```
VERIFY id=tpcommit verdict=WORKING episodes=1360
```

---

## 〇、覆盖(章程固定节)

- **宽扫 18/18 局**(W66 唯一活下来的那台 `spot_20260911_152456_1_main_8d8c4b`,种子 11395)。
  逐字引 `sweep_complete.json`(⛔ 交棒 (4) 条:覆盖行只引用它):
  `dem_found 24 / swept 18 / skipped 6 / unparseable 0 / exit_code 0`,
  前缀降级到 `dem21/`(章程 08-28 那条坑,`sweep_run.sh:53-57` 自己处理了)。
- **深查逐帧 8 局**(下限 6,达标):
  `152644_slot7` / `152654_slot8` / `153944_slot3` / `154019_slot8` /
  `154021_slot7` / `154027_slot6` / `154034_slot2` / `155255_slot5`。
- **分层(铁律 4(i-a),两层读数都登记)**:`ab`(radiant armed)**11 局** / `ba`(dire armed)**7 局**。
  ⚠️ `ba` 薄;下面每一格都两层分开打,**一次都没有并池**。
- **无新未检对局遗留**:W66 另外三台**一局未存**(批测台 18:15Z:三台 48 秒内跨两 AZ 被回收,
  每台 S3 上只有一个 `soak_farm.log`),W65 已由上一轮 16:11Z 全量宽扫。

**宽扫检测器表(`sweep_summary.md`,18 局;只用来选点,不构成结论)**

| detector | total | games ≥1 | cand | base |
|---|---|---|---|---|
| sandwiched_walk | 658 | 18/18 | 338 | 320 |
| overextend_alone | 415 | 18/18 | 234 | 181 |
| tp_under_threat | 189 | 18/18 | 88 | 101 |
| unpunished_tower_dive | 179 | 18/18 | 85 | 94 |
| tp_home_wasteful | 155 | 18/18 | 72 | 83 |
| return_to_death_spot | 148 | 18/18 | 67 | 81 |
| missed_cs_at_tower | 96 | 18/18 | 51 | 45 |
| idle_while_ally_dies | 95 | 18/18 | 39 | 56 |
| died_with_ult_ready | 85 | 18/18 | 24 | 61 |
| laning_past_midline_death | 84 | 18/18 | 30 | 54 |
| deep_solo_death | 77 | 17/18 | 42 | 35 |
| lowhp_limbo | 32 | 15/18 | 11 | 21 |
| enemy_overchase_unpunished | 25 | 15/18 | 16 | 9 |

---

## 一、选 id:`tpcommit`,以及上一轮那句「买不到」为什么可以翻案

开工 `a_evidence_owed.py`(`RC_EXIT=0`):`armed 30 verdict 27 owed-row 3 UNOWED 0`。
三个「有 owed row、无 VERIFY」的 armed id:`blinkflee`(结构性拒绝,已登记)/
`overchase`(波级读数,不可归因到单 id)/ **`tpcommit`**。
上一轮交棒逐字写「`tpcommit` 无独立施法可观测量,本轮不取」,并要求**先读它的落地可观测量再决定买不买语料**。
照办了,读到的是这个:

- 门 `J.GetTpCommitDefendDesire`(`jmz_func.lua:10644`)**自己不施法**,它只给答过的那条线一个
  **0.85 DEFEND 地板**,窗口是**施法那一刻起的 12.0s**(三个盖戳点
  `ability_item_usage_generic.lua:5205/5229/5409` 逐字都是 `bot.tpRespondUntil = DotaTime() + 12.0`)。
- ⭐ **盖戳本身是不设门的**(`[tpcommit fix E]` 那段就写在出厂的守塔 TP 分支里,注释逐字
  `Stamps are inert state off the candidate`)⇒ **两条腿都盖戳,差的只有地板**。
  这让 baseline 腿成为一条**同形状的对照**,而不是「另一种落地人群」。
- ⭐⭐ **门有一条离线可观测的子句**:`:10726`
  `if #J.GetEnemiesNearLoc( bot.tpRespondLoc, 1600 ) == 0 then return nil end`。
  ⇒ 同一次落地的窗口内,**触发点上有敌人的帧**门能出价,**没敌人的帧**门结构上闭嘴。
  这就是本轮的零通道,而且它**不是我选的阈值,是源码常量**(章程 2026-08-21T14:36Z 那条:
  识别性断言的阈值必须逐字抄源码)。
- **归属**:`tpdying` 与 `tpdead` 是**这个门内部的两条释放**,**本波都不在 arm 串里**
  ⇒ cand−base 在这里就是 **`tpcommit` 自己**,不带 `tpdying_landing.py` docstring 必须扛的那句合力警告。

**装置**:不写新 `<id>_domain.py`(章程 09-09 W58 那条坑:`ls | grep <id>` 先查重名;
`tools/batch_test/behavioral/` 里 `tpdying_landing.py`/`tpdead_release.py`/`tpdying_release.py`
已经在同一族)。本轮两个探针**全落 scratchpad**,直接 `import roam_conversion.load_game`
(它已经做了 mirror stamp 过滤、idx 锁、冻结尾巴切除三件事)。

---

## 二、⭐ 头号读数:滞留差只长在门能出价的那一半

口径:一次 episode = 一张 `item_tpscroll` 施法 → 2.0–7.5s 内 >1500u 的跳变(落地)→
落点**不在自家泉水 2500u 内**(回家 TP 没有盖戳点)。锚点 = 落地那一帧的坐标。
`pin` = 距锚点 < 500u(wave12 卷宗的原话形状,`tpdying_landing.py` 同一个常量)。
帧一律要求 `hp >= 0.40`(门 `:10656` 的生存释放子句,低于它门自己就松手)。

**pin 比例(d < 500u),两层分开,cand / base / delta**

| rel 窗口 | 层 | HOT(触发点 ≥1 敌人,门可出价) | COLD(触发点 0 敌人,门 `return nil`) |
|---|---|---|---|
| **[4,12) 窗口内** | ab | **54.2%(952) / 48.7%(1154) → `+5.5pp`** | 32.3%(1841) / 32.1%(2175) → `+0.2pp` |
| **[4,12) 窗口内** | ba | **63.2%(666) / 48.4%(676) → `+14.8pp`** | 33.3%(1275) / 31.5%(1013) → `+1.8pp` |
| [12,24) 窗口外 | ab | 16.4%(925) / 16.4%(1038) → `+0.1pp` | 6.0%(3232) / 4.8%(3922) → `+1.2pp` |
| [12,24) 窗口外 | ba | 21.6%(513) / 17.8%(759) → `+3.9pp` | 8.1%(2359) / 4.7%(1759) → `+3.4pp` |

**episode 级(不吃驻留加权,章程 2026-08-21T14:36Z)**:窗口内 HOT 帧里 pin 的占比,
cand **55.4%**(n=163)vs base **49.9%**(n=184)【ab】;cand **63.4%**(n=109)vs base **47.6%**(n=109)【ba】。
**两层同号,帧级与 episode 级同号。**

**为什么这买到了 (a) 而「滞留水平」本身买不到**:`stayfield`/`stayfield2` 同在 arm 串里,
它们也把人按在野外 —— 但它们**不看触发点上站着谁**。一个「只在 HOT 一侧、在同一批落地、
同一个 12s 窗口内部」的差,**它们造不出来**;而这正是门自己那条子句划的线。
COLD 一侧读 `+0.2` / `+1.8pp`,**就是这条论证的零通道读数**。

**供给对照(铁律 4(i-a))**:每局落地数 cand/ab **35.9** vs base/ab **41.3**;
cand/ba **38.4** vs base/ba **34.6** —— **两层反号** ⇒ 按 4(i-b) **读不出腿级供给差**,
既不能拿它说 `tpgap`(同波 armed,抑制向)压了供给,也不能拿它解释上面的滞留差。

**⛔ 12.0s 那条边界本轮读不出来,如实登记**:在「熬过 rel 11 且 12 秒时门仍在出价」的 episode 里,
[12,14) 内离开的 cand **8/12**(ab)、**2/12**(ba),base **2/7**、**1/5** —— **两层反号 ⇒ 按 4(i-b) 是噪声**。
1Hz 采样下 12.0s 只能分辨到 ±1 帧,n 也太小。**本轮的归属不靠它,靠 HOT/COLD。**

---

## 三、逐帧(先逐帧后聚合;8 局,帧证据)

格式 `rel:d=<距锚点u>,hp,foe=<触发点1600u内敌人数>`;`rel` 以**施法**为零点。

1. **`20260911_154027_slot6` cand dragon_knight cast=388.5 land=392.5** —— 门出价形状最干净的一条:
   `4.0:d=0,hp=1.00,foe=2 | 6.0:d=475,foe=3 | 9.0:d=247,foe=2 | 11.0:d=209,foe=2 |`
   **`12.0:d=501` | `13.0:d=903` | `14.0:d=847`**。
   全程 `hp=1.00` ⇒ `:10656` 的生存释放**逐帧为假**;`foe` 一直 ≥2 ⇒ `:10726` 的冷却释放**也为假**。
   **两条释放都闭嘴而人在 rel 12.0 起步走**,与 12.0s 窗口到期一致。
2. **`20260911_152654_slot8` cand bristleback cast=811.3** ——
   `3.6:d=0 → 11.6:d=159`(八拍都在 214–411u,`foe` 2→1,hp 0.89–1.00),
   **`12.6:d=393 | 13.6:d=806 | 14.6:d=1083`**。同一形状,触发点仍热、血还满。
3. **`20260911_153944_slot3` cand pudge cast=491.6** —— `8.8:d=84 | 9.8..12.8:d=159 逐位不变,foe=2,hp≈0.95`。
   **地板在按人,而按住的是一场还在打的仗**(⚠️ 这一条是「门在执行」,不是「这样打得好」)。
4. **`20260911_152644_slot7` cand ember_spirit cast=508.8** —— `6.1:d=0 → 13.1:d=414`,`foe=2` 全程,
   `14.1:d=639` 离开。
5. **`20260911_154019_slot8` cand pudge cast=1212.9** —— `5.6..10.6:d=345 六拍逐位不变`,`foe` 2→3,`hp=1.00`。
6. **`20260911_154021_slot7` cand ember_spirit cast=447.2** —— **释放侧的正例**:
   `5.2..8.2:d=14 四拍钉死,hp=1.00` → `9.2:hp=0.78,d=449` → `11.2:hp=0.57,d=655` → `13.2:d=1449`。
   血一掉他就走,**没有等到 12s 到期**。
7. **`20260911_154034_slot2` cand pudge cast=475.7** —— `5.7..12.7` 在 151–323u,`foe` 1→2,`hp=1.00` 全程。
8. **`20260911_155255_slot5`(`ba` 层)cand obsidian_destroyer cast=657.1 land=660.4** ——
   `ba` 层的同形状样本:`4.3..11.3` 在 233–420u,`foe=1` 全程,**血在涨**(0.889 → 0.932,
   两条释放都为假),**`12.3:d=721`** 起步走,而触发点**到 13.3 才转冷**
   ⇒ 他离开时门的冷却释放还没成立。

**对照(同局 baseline 腿的最长 hot-pin)**:`152644` jakiro / `152654` lion / `153944` lion /
`154019` obsidian_destroyer / `154027` obsidian_destroyer —— **baseline 腿的落地人也会站住**
(hot-pin 7–8 帧),这正是为什么**水平差本身不是证据、HOT/COLD 的对比才是**。

---

## 四、效果侧(条件 (b) 地界,**只登记不判决**),以及 GH #35 的新语料

**wave12 卷宗那个形状在本波复现,而且是在 `tpdying` 未 armed 的腿上**:
在窗口内血量首次跌破 40% 的落地人里,**那一刻仍钉在触发点 500u 内**的占比 ——
cand **16/28 = 57.1%**(ab)/ **12/18 = 66.7%**(ba);base **6/18 = 33.3%** / **6/17 = 35.3%**。
**两层同号**;n 小,**不写成结论**,只作为 GH #35(`tpdying` 申请入集)的新语料证据。

机制帧(同族,虽然它的 hot-pin 不是本局最长,列在这里因为它是这一节的承重):
`20260911_154034_slot2` **dragon_knight cast=616.6**,
`4.8..11.8` 一直在 440–469u、`foe=3`,血 `0.997 → 0.767 → 0.685 → 0.529 → 0.319`,
`12.8:hp=0.002`、`13.8` 死。
⇒ 生存释放 `:10656` 直到 `rel 11.8` 才第一次为真,**那时已经来不及了**;
这逐字就是 `jmz_func.lua:10650-10659` 注释里写的「pinned landers ground down in place」。

---

## 五、⚠️ 本轮一处当场自我更正(工具坑,已写进章程)

第一版探针用 `g['dead'].get(hero)` 直接取死亡区间,而 `roam_conversion.death_spans`
**是按 `canon_hero()` 建键的**(`:169-173`,GH #82)⇒ 传 `npc_dota_hero_x` 永远 `.get()` 到空元组,
**每一帧都读成存活**。它污染的方向**正好朝着结论**:一具冻在触发点上的尸体同时充当
「还钉在原地的落地人」**和**「把触发点烧热的那个敌人」(GH #176 ②③ 同族)。
判别子是模块自己有 `is_dead(spans, hero, t)`(`:281`,它会 canon 化)。
改用它 + `hp_pct > 0` 双网重跑,**结论方向不变、分母缩小**:
HOT delta 由 `+5.3/+11.7` 变成 `+5.5/+14.8`,COLD 由 `+0.3/+0.8` 变成 `+0.2/+1.8`。
⛔ **仓库里没有第二个消费方中这条**:`axe_jump_pick_audit.py` 与 `bbfloor_domain.py` 各有自己的
`death_spans`,**不走这个键** ⇒ **不开 issue**,写进章程工具坑。

---

## 六、AWS

**零支出**。只读 S3(1 次 `sweep_run.sh` 列举 + 24 个 `.dem` + dumper 缓存命中),
未起任何实例,未调用 Cost Explorer。

## 七、树上改动

仅本报告 + 章程。**`bots` / `game` / `tools` / `tests` 一行未改**;两个探针
(`tpcommit_probe.py` / `tpcommit_boundary.py`)全在 scratchpad。

## 八、issue

**净增 0 条 + 1 条追评**(先搜后开:语义检索 `tpdying release landing commitment ground down
in place pinned lander tpcommit floor` 在本仓库**只命中 GH #35 一条**,正是这件事的既有单)。
→ **GH #35 追评**:`tpcommit` 执行侧 WORKING 的读数 + 第四节那条 57.1%/66.7% vs 33.3%/35.3%
的新语料 + 承重帧 `154034_slot2` dragon_knight。
按 GH #290:**先 push 再发表**,草稿过 `claim_precheck.sh`。

## 九、自检与铁律 6

- **开工自检**:`selfcheck worst exit: 3` / `legs run 12` /
  `FINDINGS (exit 3): cadence owed-executions trunk-red(python) trunk-red(lua)` /
  `UNCERTIFIABLE (exit 2): none` /
  `NOT RUN (inside a leg): tests/test_lua_gate.py tests/test_luacheck_gate_soakswitch.py tests/test_selfcheck_lua_leg.py`
  —— **腿内这三条这轮没人看过,不是通过**。
  trunk 红与批测台 18:15Z 那轮**同一批**(`git status --porcelain` 本轮只有报告/章程,
  `bots/` `tests/` 一行未改 ⇒ **不是本轮增量**);族属已由 GH #751 登记,**不另开单**。
  ⚠️ **开工第一条命令第 20 次撞管道拒绝门**(`| tail` ⇒ `REFUSED ... exit 2, nothing checked`),
  当场改重定向;**本轮没有套外层 `timeout`**(上一轮交棒第 (7) 条,照办了)。
- **铁律 6 三条腿**:见本文件末尾「补记」节(push 之后回填,GH #290 顺序)。

## 十、下一轮第一件事

1. ⭐ **`overchase` 与 `blinkflee` 现在是仅剩的两个「有 owed row、无 VERIFY」的 armed id**。
   两条都登记过「不可归因 / 结构性拒绝」,**先读那两条登记还成不成立**,
   再决定是买语料还是把它们从「欠 (a)」改登记成「按设计不可买」——
   ⛔ 别第三次把轮次花在重新发现同一句话上。
2. ⭐ **本轮的买法可迁移,记下形状**:一个没有自己施法的门,只要它有**一条离线可观测的子句**,
   就能用「子句为真 / 子句为假」把同一批 episode 劈成实验组和零通道 ——
   **共同 armed 的邻居造不出只在一侧的差**。下次遇到「无独立可观测量」先问这一句。
3. 盯 **GH #35(本轮追评)** / #752 / #96 / #747 / #744 / #736。
4. ⛔ 覆盖行只引用 `sweep_complete.json`;⭐ sweep 一律把 `out_dir` 指到 scratchpad。
5. ⚠️ **自写帧读件,liveness 一律走模块自己的 `is_dead()`,不要 `.get(raw_name)`**(本轮现踩,§五)。
6. ⭐ **单 run 读数不是波级读数**(上一轮的教训;本轮 W66 只有一个 run 活下来,
   所有读数都**只代表种子 11395**,跨波引用时必须带这一句)。

## 十一、token 用量

见本文件末尾「补记」节。

---

## 补记(第一次 push 之后回填,GH #290 顺序)

**铁律 6 三条腿**(两次 push 读数相同):
- `luacheck bots game: 0 warnings` / **`GATE_EXIT=0  CLEAN (iron rule 6 static half passed)`**
- **`py gate: 96 ran, 0 findings, 0 uncertifiable, 40.0s`**
- **`lua gate: SKIPPED BY SCOPE -- this push touches no bots/game/tests path.`**
  ⚠️ **第三条腿是范围判定不是绿**;工具自己那句照抄:
  `2 path(s) offered, none of which can move a Lua test's answer: …` /
  `This is a SCOPE decision, not a pass and not a skip of a gate that had something to say (GH #624).`

`ARM_EXIT=0`(`push gate armed (core.hooksPath=.githooks, already set)`)。
**未用 `RULE6_BYPASS` ⇒ 没有「SKIPPED, not passed」那一行要抄。**
动态半(GH #124)**未跑,不声称**。
`PUSH_BRANCH_EXIT=0` / `PUSH_MAIN_EXIT=0`(`c5cb8292..1ae807e4  HEAD -> main`,**一次过,无 rebase**)。

**已发表的追评**(push 之后才发;草稿过 `bash tools/agent/claim_precheck.sh`:
`PRECHECK_EXIT=0` / `clean` / `local commits not on origin/main: 0` /
逐字 `OK to publish: every citation resolves on origin/main.`):
**GH #35 comment `5639406070`**。`mcp__github__*` 本轮可用,**零 `requires approval`,零空转**(铁律 11)。

## 十一、token 用量

```
TOKENS total_in=8,819,072 out=59,122 turns=60
```
(统计时刻之后的收尾回合不计入。)
⭐ 比上一轮(`total_in=25.7M / turns=138`)低一个量级,成因是上一轮交棒那句
「把等待压成一个 until 循环」**照办了**:宽扫只轮询了一次 `until [ -f sweep_complete.json ]`,
自检也是一次 `until ! pgrep`,没有十余次空轮询。
