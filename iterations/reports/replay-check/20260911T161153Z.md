# 录像检查组 2026-09-11T16:11Z — `lf_rescue` 条件 (a) 在新语料上独立复现;null 通道里逮到一类「根本不是 TP 分支」的东西

**一句话**:`lf_rescue` 的执行侧判 **WORKING**(armed 腿每局 1.14 次归属救援 TP,出厂腿同签名只有
0.21 次/局的本底,**两个分层同号**);而本轮真正的新东西是那条 null 通道里的污染源 ——
检测器把**复活后 0.3 秒从泉水按下的传送**记成了一次救援,**而出厂 Lua 的同一条子句明文锚在复活上、
明文说过这样锚是错的**。

```
VERIFY id=lf_rescue verdict=WORKING episodes=115
```

(`episodes` = 两条腿上**通过复活锚点后**仍然成立的归属救援 TP 总数:armed 97 + baseline 18。
判决落在**执行侧**;效果侧见 §五,那是条件 (b) 不是 (a)。)

---

## 0. 选点:为什么是 `lf_rescue`

- **无新波次可检**。W66 本轮 **15:24Z** 才发波(`W66_wave.json:machines[].launch_time` 逐字
  `2026-09-11T15:24:30.000Z` 起四台),开机 + Steam 更新约 12 分钟、首局约 20 分钟
  ⇒ 本轮开工时 S3 上**零对局**。⚠️ 这是**时序**,**不得读成「机器没在跑」**(批测台 09:15Z 报告
  §补记 (C) 同一条)。按章程工作流第 2 条转做「执行核验记录最少的 id」。
- **开工 `AEV_EXIT=0`**:逐字 `A-EVIDENCE-OWED  armed 30  verdict 26  owed-row 4  UNOWED 0`。
  ⭐ **UNOWED 归零** —— 上一轮交棒单第 (2) 条(「`aimguard` 等其余 UNOWED」)**到此已无对象**,
  不是被本轮做掉的,是 promote 把它们带出了 arm 串(`zusult`/`zusboltdom`/`tpdeathbuy`/`liondrainstop`
  已于 09-11 promote,34→30)。**这条要交下去:那条腿从今天起报的是另一件事。**
- 剩下 4 个「有 owed row、无 `VERIFY` 判决」的 armed id,逐个先读工具头部 LIMITS
  (W62/W63 的学费:先问「这个量离线读不读得出来」,再决定买不买语料):

| id | 本轮处置 | 理由(读工具头,不读目录名) |
|---|---|---|
| `blinkflee` | **排除** | 2026-09-10 那轮已登记**结构性拒绝**:同一 consider 函数里三处朝自家远古的 blink,`:1647` 的击杀 blink 在敌人站位重合时**落点逐字相同** ⇒ 再买语料只复现 INDETERMINATE |
| `overchase` | **排除** | 上一轮交棒单自己标注「**波级读数,不可归因到单 id**」 |
| `tpcommit` | 本轮不取 | 它改的是**落地之后**的 defend desire 地板,没有独立的施法可观测量;TP 持卷代理已被 GH #647 否掉 |
| **`lf_rescue`** | **选中** | 它是**增量门**:`J.GetRescueTpTarget` 第一句 `if not J.IsLaneFixOn('rescue') then return nil end` ⇒ **baseline 腿结构上产不出这条路径的 TP**,镜像装置里 baseline 腿**就是**它的 null 通道 |

## 1. 语料与覆盖

**宽扫 85/85 局**(W65 四个 run)。⛔ 覆盖行**逐字取自 `sweep_complete.json`,未手敲 `aws s3 ls`**:

| run | dem_found | swept | skipped(暖场) | unparseable |
|---|---|---|---|---|
| `spot_20260911_092333_1_main_dae390` | 24 | 18 | 6 | 0 |
| `spot_20260911_092336_1_main_551784` | 32 | 26 | 6 | 0 |
| `spot_20260911_092339_1_main_74aefd` | 29 | 23 | 6 | 0 |
| `spot_20260911_092342_1_main_399269` | 24 | 18 | 6 | 0 |
| **合计** | **109** | **85** | **24** | **0** |

四个 `SWEEP_EXIT=0`。W65 arm 串 34 个 id,**`lf_rescue` 在串里,`lanefix` 不在**
(`J.IsLaneFixOn( sub )` = `IsSoakCandidate('lanefix') or IsSoakCandidate('lf_'..sub)`,
`jmz_func.lua:8175-8178`)。

**深查逐帧 7 局**(章程下限 6 局达标),清单见 §四。

### 宽扫通用检测器表(全波合计;**只用来选看哪几局,不构成结论**)

| detector | total | cand 侧 | base 侧 |
|---|---|---|---|
| deep_solo_death | 474 | 218 | 256 |
| died_with_ult_ready | 796 | 432 | 364 |
| enemy_overchase_unpunished | 100 | 51 | 49 |
| idle_while_ally_dies | 491 | 263 | 228 |
| laning_past_midline_death | 509 | 243 | 266 |
| lowhp_limbo | 114 | 52 | 62 |
| missed_cs_at_tower | 460 | 219 | 241 |
| overextend_alone | 2096 | 1058 | 1038 |
| return_to_death_spot | 758 | 363 | 395 |
| sandwiched_walk | 2576 | 1306 | 1270 |
| skywrath_solo_silence | 118 | 61 | 57 |
| tp_home_wasteful | 627 | 294 | 333 |
| tp_under_threat | 804 | 386 | 418 |
| unpunished_tower_dive | 744 | 351 | 393 |

⚠️ 这张表是**池化计数、侧偏未消除**(铁律 4(i-b))⇒ 本报告**不从它推任何结论**,
包括看起来诱人的 `idle_while_ally_dies` 263/228。

## 2. 装置:本轮一个新检测器都没写

`lf_rescue_null_channel.collect()` + `tp_attribution.scan()` **原样调用** ——
前者本来就按「每局两腿各扫一次」组织(`:138` 逐字
`for leg, side in (('armed', g['side']), ('base', OTHER[g['side']]))`)。

W65 是**单臂** 34-id 串,**没有 `lf_rescue`-off 的另一臂** ⇒ 该文件的 A/B **臂守卫结构上不适用**,
因此本轮 **⛔ 不引用任何 `ARMED A−B` 估计量**(那是 §AG.2 给二分语料的估计量),只报**局内两腿对比**。
聚合脚本落在 scratchpad;**`bots` / `game` / `tools` / `tests` 一行未改**(`git status --porcelain` 空)。

帧表取自 `tp_attribution.TL`(`IDX_LOCK=True`,锁本体,GH #69/#176)——
上一轮交棒单第 (6) 条「自写帧读件第一件事拿 CLEAN 表」,**本轮的落实方式是不自写**。

## 3. 读数(铁律 4(i-a):两个分层都登记)

### (甲) 原始表

| 分层 | 腿 | 局 | precondition-true 施法 | 归属(落点吻合) | 有归属的局 |
|---|---|---|---|---|---|
| armed_rad | armed | 47 | 93 | **51** | 32 |
| armed_rad | base | 47 | 49 | **15** | 10 |
| armed_dire | armed | 38 | 79 | **52** | 28 |
| armed_dire | base | 38 | 38 | **9** | 8 |
| POOLED | armed | 85 | 172 | 103 | 60 |
| POOLED | base | 85 | 87 | 24 | 18 |

### (乙) ⭐ 扣掉复活窗后的同一张表(**两条腿都扣**)

只扣 armed 腿就是把「不知道」洗成「无罪」(GH #747 §2 的形状)。按出厂 Lua 自己的
15.0s 复活锚点重算:

| 分层 | 腿 | 归属 | 其中落在复活窗内 | **LIVE** |
|---|---|---|---|---|
| armed_rad | armed | 51 | 1 | **50** |
| armed_rad | base | 15 | 3 | **12** |
| armed_dire | armed | 52 | 5 | **47** |
| armed_dire | base | 9 | 3 | **6** |
| POOLED | armed | 103 | 6 | **97**(1.14 次/局) |
| POOLED | base | 24 | 6 | **18**(0.21 次/局) |

**两个分层同号**(rad 50 vs 12、dire 47 vs 6)⇒ 按铁律 4(i-b) 不被判为噪声。

### (丙) 供给对照(两条,都必须报)

- **每局 `item_tpscroll` 总施法数**(立这条是因为 `tpdeathbuy` 在 W65 发波时**仍 gated**
  ——10:37Z 才 promote——它买的是**卷轴**,armed 腿若单纯持卷更多,一切 TP 形状的计数都会跟着涨):
  rad 层 armed **52.83** vs base **55.32**;dire 层 armed **58.34** vs base **53.68**。
  **两层反号 ⇒ 按 4(i-b) 是噪声,即读不出腿级的持卷差异。**
- **机会 episode(pass B 超集)**:rad 层 37.04 vs 35.34;dire 层 35.95 vs 40.76。
  **同样两层反号 ⇒ 不是供给伪影**(与 GH #96 §1 的供给检查同结论)。

⇒ 供给端两条都读不出腿差,而**救援形状的那个子集**在两层里都差 4-8 倍且同号。

### (丁) 阈值敏感性(**如实记:本语料上它不是完全平的**)

| `ATTRIB_U` | armed 归属 | base 归属 | 比 |
|---|---|---|---|
| 1000 | 103 | 24 | 4.29 |
| 1500 | 104 | 32 | 3.25 |
| 2000 | 104 | 34 | 3.06 |
| 3000 | 112 | 34 | 3.29 |

⚠️ GH #96 那份语料上四档**逐位不变**;**本语料 base 腿从 24 走到 34**。
⇒ **符号与量级稳定,但「阈值不是自由参数」这句话在本语料上只对 armed 腿成立**,
引用比值时必须带上用的是哪一档。

### (戊) 归属:为什么这个超出量能记到 `lf_rescue` 头上(铁律 4a)

**不是靠差分,是靠源码排他性。** W65 的 34 个 armed id 里,能**新增**一次远程 TP 的只有它:

| 其它 TP 家族 id | 为什么不是替代解释 |
|---|---|
| `tpgap`(`J.ShouldNotTpUnderLethalPressure`) | **抑制**型,只会**减**少 TP |
| `tpcommit`(`J.GetTpCommitDefendDesire`) | **落地之后**的 defend desire 地板,不产生 cast |
| `tpdeathbuy` | 改的是**买卷**,已由 §三(丙) 的对照量覆盖 |
| `suptp` / `midtp`(`ShouldTpSupportTowerFight`) | **不在 W65 串里** ⇒ 两腿同为关闭 |
| `teambrain`(`ShouldAllowDefendTp`) | **不在串里** ⇒ 两腿同为放行 |

⚠️ **反过来的那一半仍然不成立**:baseline 腿那 18 次**是真的救援形状**,
`X.ConsiderItemDesire["item_tpscroll"]` 里至少四条分支共用 `J.GetNearbyLocationToTp`
(救援 / `ShouldTpSupportTowerFight` / roshan / defend TP)⇒ **落点几何分不开消费方**。
这与 GH #96 §3 把病归给落点选择器本身一致,**本轮是从源码复核了它,不是重新发现**。

## 4. 深查:逐帧 7 局

**先逐帧后聚合** —— 下面 7 局的帧是结论的依据,上面的表只负责选它们。

### 4.1 armed 腿(4 局,全部核对到门的每一条可观测子句)

| 局 | t | responder | ally | 帧证据 | 判读 |
|---|---|---|---|---|---|
| `dae390/20260911_092530_slot7` | 121.7 | spirit_breaker hp 0.821 | sven hp 0.327 | 距 **15804u**;身边 centaur 166u + zuus 358u(**2 个 diver**);122.7-124.7 **三帧原地不动 = 读条**;125.7 落在 (4981,-6956) | 执行正确(ally 仍于 131.7 死亡 ⇒ 效果侧,见 §五) |
| `dae390/20260911_092540_slot8` | 492.3 | obsidian_destroyer hp 1.000 | skeleton_king hp 0.158 | 距 7000u+;900u 内 WD 646u + zuus 882u(**2 个**,nevermore 1169u 在环外);492.7-494.7 读条;495.7 落地 (-6297,2633) | 执行正确 |
| `dae390/20260911_093906_slot7` | 355.5 | lich hp 1.000 | sven hp 0.338 | centaur 323u;356.5-358.5 读条;359.5 落在 (5286,-5963) 紧贴 sven;**落点误差 21.6u** | 执行正确 |
| `dae390/20260911_093910_slot3` | 62.1 | lich hp 0.911 | skeleton_king hp 0.281 | 距 **15640u**;slardar 379u + WD 779u;62.4-64.4 读条;65.4 落地 | 执行正确(t=62 即开局 1 分钟,`DotaTime()<8*60 and IsCore` 子句因 lich 是辅助而不拦) |

四发的**共同形状**:满血/高血的远端辅助 → 3500u 以上 → 一个 <35% 的队友 → 1-2 个贴脸敌人 →
三帧读条 → 落在队友旁。**这就是 `J.GetRescueTpTarget` 写的那件事。**

### 4.2 baseline 腿(2 局,这是判决的另一半:签名到底有多脏)

| 局 | t | 现场 | 读法 |
|---|---|---|---|
| `dae390/20260911_095030_slot1` | 513.9 | OD hp 0.781、距 **7023u**,SK hp 0.193、slardar 190u 贴脸;514.5-518.5 读条,519.5 落在 SK 身边 | ⭐ **`lf_rescue` 关着,却发生了一次教科书式救援** ⇒ 签名**不是** id 指纹(GH #96 §2 的结论,新语料复现)。顺带:SK 在 515.5 hp=0.000、**518.5 原地 hp=1.000** —— WK 原地复活,章程那条坑的现场 |
| `dae390/20260911_095043_slot7` | 587.8 | **见 §五,本轮头号发现** | 不是任何 TP 分支 |

### 4.3 SILENT 嫌疑(1 局,取机会 episode 最多的那个)

`551784/20260911_095136_slot2`(armed=radiant,seed 10927):**45 个机会 episode、0 次施法**。
逐帧看 t=70.4 那个最像的:lion 满血在 (-6092,4777),队友 drow **hp 0.306**、距 **15263u**、
slardar 154u 贴脸 —— 看起来是一次漏掉的救援。**但帧本身给了答案**:

```
drow  t=69.4 hp=0.371   t=70.4 hp=0.306   t=71.4 hp=0.320   t=72.4 hp=0.338
      t=74.4 hp=0.369   t=76.4 hp=0.402   t=78.4 hp=0.434   t=80.4 hp=0.465
```

**`hp<0.35` 的窗口只有 3 帧(70.4-72.4),随后 drow 自己走出来并一路回血到 0.465**,
而第二个敌人 spirit_breaker 自己只剩 **hp 0.033**。⇒ `J.WillAllySurviveTpWindow` 若判「他能活」
**是对的**,不救是正确决策。

⛔ **更要紧的是:SILENT 这个方向本身买不到。** 门里有五条子句离线不可观测 ——
`nVsMe`(潜水者 3 秒爆发)/ `WillAllySurviveTpWindow` / `TryTakeTpResponseSlot`(队伍配额)/
`IsChainedRescue` / **以及 `J.GetItem2(bot,'item_tpscroll')` 本身**(快照结构上不含 slot 9+,
GH #647)。⇒ **「有机会没触发」永远有一个不可证伪的解释(手里没卷)**,
`11/85` 这个 SILENT-嫌疑局数**只能当选点用,不能当判决用**。

## 5. ⭐⭐ 头号发现:null 通道里混进来的不是 TP 分支,是**一个刚复活的人**

`dae390/20260911_095043_slot7`,**baseline 腿**(该局 armed 侧是 dire ⇒ 这是 radiant 的出厂腿):

```
lich  t=584.5  xy=( 6207,-3209)  hp=0.000    <- 尸体帧
lich  t=585.5  xy=( 6207,-3209)  hp=0.000
lich  t=586.5  xy=( 6207,-3209)  hp=0.000
lich  t=587.5  xy=(-6604,-6306)  hp=1.000    <- 在自家泉水复活(radiant 泉水 -7200,-6666)
ITEM  t=587.8  actor=lich inflictor=item_tpscroll    <- 复活后 0.3 秒
lich  t=591.5  xy=( 5135,-5731)              <- 落在 sven 附近的己方塔旁,落点误差 135.4u
```

同一瞬刻 ally `sven` hp=0.232、`zuus` 433u 贴脸、responder→ally **12940u**
⇒ `tp_attribution.gate()` 的**每一条**可观测子句都为真,这一发因此被记成一次归属救援。

**真实的门会一票否决它**(`jmz_func.lua:8578`):

```lua
if DotaTime() - math.max( bot.lastDeadFrameTime or -999,
    bot.lastRespawnTime or -999 ) < 15.0 then return nil end
```

`587.8 − 587.5 = 0.3 < 15` ⇒ `return nil`。

⭐ **这不是我事后发明的读法。** 出厂树里那条注释(`jmz_func.lua:8472-8476`)逐字写着为什么
锚点必须是复活:

> ANCHOR = RESPAWN, not death: the engine stops calling mode desires on dead bots, so
> lastDeadFrameTime froze at the death moment and a 19s death timer let **lich** revive at
> 6:36 and TP into three enemies at 6:37 "outside" the window.

⇒ **检测器实现的正是 Lua 在 2026-07-31 明确废掉的那一版语义**,而上面这一帧的现场
**恰好也是 lich、也是复活后立刻 TP**。

**修法在仓库里已经有了**:同目录 `tpdefend_events.py:237` 的 `fresh_respawn()` **是对的**
(先取最近 DEATH,再取其后第一个 `hp_pct>0` 帧,从那帧起算)。
`tp_attribution.py:194` 只需换成同一个锚点。

### 血量(**两条腿都扣之后**)

- 落在复活窗内的 precondition-true 施法:**armed 腿 24 发 / baseline 腿 28 发**;
  其中**归属**的:**armed 6 / base 6**(§三(乙) 那两列)。
- ⚠️ **本轮一处当场自我更正**:只跑完第一个 run 时,被扣掉的 3 发**全在 baseline 腿**,
  我差点把「它只夸大 null 通道」写成结论;**跑完 85 局后是 6/6,两条腿一样多**。
  **单 run 读数不是波级读数**,这条写进下一轮第一件事。
- `dt_respawn` 在全语料上是 **0.0 → 14.2 秒的连续分布**(第一个 run 上看起来有
  0.3→21.0 的干净间隙,那是 n=3 的假象)⇒ **计数确实依赖 15.0 这个界**;
  但**这个界不是我选的,是出厂代码写死的**。

## 6. 效果侧(条件 (b) 的地界,**本轮只登记不判决**)

`J.WillAllySurviveTpWindow` 离线不可观测,唯一能读的替代量是「救了以后队友还是死了没有」:

| 腿 | n(LIVE 归属) | 队友 15s 内死亡 | responder 15s 内死亡 |
|---|---|---|---|
| armed | 97 | **35(36%)** | 3(3%) |
| base | 18 | 10(56%) | 1(6%) |

⇒ armed 腿三分之一的救援里队友照样死了(§4.1 前两帧就是两例)。
**方向与 GH #96 §3「门在算一场 4 秒结束的仗,派出的人要走 8 秒」一致**,
但 base 腿 n=18,**两腿之差不写成结论**。这是条件 (b)/(c) 的活,不是本组的判决。

## 7. 本轮 issue

**先搜后开**:两组语义检索 —— 「tp_attribution fresh respawn 锚点」**0 命中**;
「lf_rescue 条件 (a) null channel」命中 **GH #96 / #508 / #594**。

- **GH #96 追评**(`lf_rescue` 的 (a) 按 AG.2 重算那一单):本轮是它在**全新语料上的独立复现**
  (它 1.00 vs 0.25 次/局,本轮 1.14 vs 0.21),外加 §五 的污染源与 §三(戊) 的源码排他性。
  ⛔ **不另开单** —— #96 问的正是这件事。
- **新开 [harness] 一条**:§五 的复活锚点缺陷(检索无同题单)。

`claim_precheck.sh` 读数与 issue 号见 §九:**[harness] GH #752** + **GH #96 追评 `5637439402`**。

## 8. 其它口径

- **AWS**:只读 S3(4 次 `sweep_run.sh` 列举 + 109 个 `.dem` + dumper 缓存命中),**零 EC2、零发波、零支出**。
- **树上改动**:仅本报告 + 章程。两个探针(`lf_rescue_leg.py` / `lf_deep.py`)**全落 scratchpad**。
- **自检**:`SELFCHECK_EXIT=3` / `legs run 12` /
  `FINDINGS (exit 3): cadence queue-rulings owed-executions trunk-red(python) trunk-red(lua)` /
  `UNCERTIFIABLE (exit 2): none` / `NOT RUN (inside a leg): tests/test_selfcheck_lua_leg.py`
  —— **最后这条本轮没人看过,不是通过。**
  ⚠️ **开工第一条命令又撞管道拒绝门**(上一轮登记为第 19 次;本轮同形状再发生一次。
  工具逐字:`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.` /
  `it has recurred 5x, every time as the first command of the round` ——
  ⛔ **那个 5 是工具自己的口径,不是本组的总计数,两者不要混**),当场改重定向;⚠️ 第一次重定向**又套了外层 `timeout 400` 被砍(`EXIT=124`)** ——
  **上一轮报告里逐字写着不要套外层 timeout,我还是套了**;去掉后跑完。**124 同样不是通过。**
  trunk 红:python 4 条(`test_carrier_terms.py`=GH #636、`test_detector_source_constants.py`=GH #694、
  另两条 `test_bots_walk_farm_only.py` / `test_py_gate.py` **如实记为「本轮未核对到单号」,不猜**),
  Lua 3 条(census 形状 ⇒ GH #624 族)。`git status --porcelain` 只有本报告 ⇒ **红不是本轮增量**。

## 9. issue / 引用前置检查 / 铁律 6 / token

- **本轮 issue:净增 1 条 + 1 条追评**(先搜后开:「tp_attribution fresh respawn 锚点」语义检索
  **0 命中**;「lf_rescue (a) null channel」命中 #96/#508/#594,**#96 问的正是这件事 ⇒ 不另开单**)。
  - **[harness] GH #752**(§五 的复活锚点缺陷)
  - **GH #96 追评 `5637439402`**(新语料独立复现 + §三(戊) 源码排他性 + `VERIFY` 行)
- 两份草稿 `claim_precheck.sh` 各 `EXIT=0` / `clean` / `local commits not on origin/main: 0`,
  **按 GH #290 先 push 再发表**。
- **铁律 6**(三条腿):`luacheck bots game: 0 warnings` / `GATE_EXIT=0  CLEAN` /
  `py gate: 96 ran, 0 findings, 0 uncertifiable, 40.9s` /
  `lua gate: SKIPPED BY SCOPE -- this push touches no bots/game/tests path.`
  ⚠️ **末行是范围判定不是通过**;**未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行**。
  动态半(GH #124)未跑,不声称。`ARM_EXIT=0`(`core.hooksPath=.githooks, already set`)。
  `PUSH_BRANCH_EXIT=0` / `PUSH_MAIN_EXIT=1` 被拒 ⇒ rebase(`REBASE_EXIT=0`,
  `69001b36..120002a1`)⇒ 重试 `PUSH_MAIN_EXIT=0`(`120002a1..8b35fcd0`)。
- token:`TOKENS total_in=25,667,658 out=93,545 turns=138`
  (到统计时刻为止;其后的收尾回合不计入。⚠️ 本轮偏高的成因是**等**:四个 sweep + 170 次
  `scan()` 串行跑了约 40 分钟,期间轮询了十余次。**下一轮把等待压成一个 until 循环**。)
