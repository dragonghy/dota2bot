# replay-check 2026-08-30T01:20Z — `fieldcreep` 的域测定了,**它的前提在自己域的多数上是假的**

**一句话**:上一轮章程点名的下一个零记录 id(`fieldcreep`,与 owner P2 野区续航同族)本轮做完 ——
它**不是 SILENT**(armed 腿 22.4% 的 situation episode 落在它的谓词域里,ab 22.0 / ba 23.0 **两层同号同量级**),
但**逐帧看,它立案时写的那句前提在自己域的多数上是假的**:
营地接触帧里 **armed 腿 56.9%(ab)/ 65.3%(ba)是 bot 在反打营地、伤害输出大于承受**(打野,不是被啃),
而带补给的纯小兵帧里 **72.5%(ab)/ 72.4%(ba)** 落在申请书自己标注的"过宽角落"里
(3 秒创伤 < 包里补给的 3 秒回复量)——**那不是角落,那是它剩下域的多数**。

**宽扫 121/121 局**(W25 四个 run,`unparseable 0`,8132 个 situation episode),**深查逐帧 7 局**。
零 EC2 支出,S3 只读,`bots/` / `game/` **0 改动**,gate 未动。

⭐ **本轮最该被下一个人读到的一句**:§3b 那个"不需要 baseline 腿的 甲/乙 判别器"
在聚合表上给出了一个看起来可读的答案(camp home_tp ≈ lane home_tp ⇒ 像世界甲),
而**逐帧一看,它两个阳性事件全是假的**:一个是 12 秒后的推塔送掉的血,一个是 6 秒后的团战 ——
都不是那一帧的营地造成的。**上一轮记下的那个错误(把结果当机制)本轮第四次复现,
仍然是逐帧抓到的、不是表抓到的。**

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` —— **worst exit 3,8 腿全跑**。

- `exit sources`(工具自报):`FINDINGS (exit 3) = cadence`,**`UNCERTIFIABLE (exit 2) = none`**
  ⇒ trunk 两侧本轮都被看过。
- trunk:python **53 passed / 0 failed / 0 uncertifiable**;fast Lua **32 个打标检测器 / 0 失败**
  (工具自称 FAST SUBSET,照抄不加工)。
- `unlanded_commits`:**OK**。锚点 `stable-v1`/`stable-v2` **2/2 OK**(EXISTS/PINNED/SHIPPED)。
- 未裁队列请求:RIDESHARE none / OTHER none;过期等待:无(6 份章程 / 58 个已结 id)。
- `ORPHAN_PROPOSAL` none;`ROWLESS` 3(`aimguard`/`campvoid`/`campexit`,信息性)。
- `arm_push_gate.sh`:`core.hooksPath=.githooks`,**already set**。

## 1. 为什么是这个工作单元

- **铁律 9**:`OWNER_PRIORITIES.md` 三项(P1 拉野 / P2 低血不回家 / P3 cap 10→25)的球分别在
  协同组、协同组、总监。**本轮没有属于录像组的未完成优先项**,走章程 backlog。
- **上一轮章程"下一轮第一件事"第 (2) 条**点名:下一个零记录 id,**`fieldcreep`(与 owner P2
  野区续航同族,优先)**。本轮做的就是它。
- **W27 不用**:00:19Z 才发波(本轮开工时 22 分钟),而且批测台已登记它是 **45 id 串
  (`fieldsip` 入集)**,与 W25/W26 的 44 id 串不同,**不能并进本轮语料**(上一轮已写死这条)。
  本轮语料是 W25 四个 run,`fieldcreep` 在其 armed 串里(逐局 `cand=` 戳可核)。

## 2. 宽扫:121/121 局

| 波次 | run | 种子 | `games swept` | `unparseable` |
|---|---|---|---|---|
| W25 | `6df84c` | 1603 | 31(跳过 6 暖场) | 0 |
| W25 | `b1386e` | 1633 | 30(跳过 6) | 0 |
| W25 | `a29ed3` | 1664 | 30(跳过 6) | 0 |
| W25 | `ecbb41` | 1770 | 30(跳过 6) | 0 |

- 合计 **121 局 / 8132 个 situation episode**(armed 3876 / baseline 4256)。
- **分层(铁律 4(i))**:**ab 78 局 5193 episode / ba 43 局 2939 episode**。
  侧别倾斜**第五次复现**(上轮已交总监,本轮不重复立案)。
- **121 局不是我抽的,是录制槽决定的**。
- 本轮**未跑** `sweep_run.sh` 的 15 个通用检测器(沿用欠账,登记不掩盖)。

## 3. 裁决:`fieldcreep` 条件 (a)

### 3.0 三值标签之外的一句话,必须先说

章程第 4 条要求 `WORKING | BUGGY | SILENT`。**这个 id 的效果是一次"释放"**
(`J.IsFieldRegenSituation` 返回 false,帧交还给出厂的回家逻辑),**屏幕上什么都不发生**,
所以"触发了没有"在本语料上**不可直接观测**(工具文件头自己也这么写)。因此:

- **它不是 SILENT** —— 这一点买到了,证据在 §3.1;
- **WORKING 买不到**,而且**不是因为没按 §AR.3(甲) 报**(甲 那一栏本轮**测**出来了,121 局逐条实体名),
  是因为**为它设计的那个判别器本身被证伪了**(§3.3,逐帧)。
- **本轮真正的产出不是三值标签,是"域读数 + 前提检验"**:见 §3.2 与 §3.4。

### 3.1 它不是 SILENT:域是真的,两层同号

| 分层 | 腿 | episode | none | lane | neutral | mixed | **甲 veto** | **乙 veto** |
|---|---|---|---|---|---|---|---|---|
| ab | armed | 2382 | 1857 | 423 | 93 | 9 | **525(22.0%)** | **432(18.1%)** |
| ab | baseline | 2811 | 2234 | 418 | 155 | 4 | 577(20.5%) | 422(15.0%) |
| ba | armed | 1494 | 1151 | 248 | 94 | 1 | **343(23.0%)** | **249(16.7%)** |
| ba | baseline | 1445 | 1137 | 236 | 72 | 0 | 308(21.3%) | 236(16.3%) |

甲 = 引擎把中立生物算作 creep;乙 = 不算。**两层的 armed 读数逐位可比**(22.0/23.0、18.1/16.7)
⇒ 这不是侧别效应。对比 `campvoid` 上一轮的 **0/160**:那个是结构性静默,**这个不是**。
`campexit` 式的"低计数是下界"在这里也不适用 —— 这不是触发计数,是**域**。

**§AR.3(甲) 强制栏位交付**:每一次域命中打他的是什么,**逐条实体名测出来了,不是按伤害量级推的**
(§AR.1 只能推)。全语料前几名:`ancient_frog` 301 / `ancient_frog_mage` 221 /
`prowler_shaman` 138 / `grown_frog` 110 / `ogre_mauler` 50(中立);
`creep_goodguys_melee` 1128 / `creep_badguys_melee` 1111 / `..._ranged` 832/762(小兵)。

### 3.2 ⭐⭐ 前提检验:营地接触帧的多数是**打野**,不是**被啃**

立案句(`jmz_func.lua:5286-5294`)是「a hurt bot **with nobody anywhere near it** standing
inside a camp **being chewed**」。同一个 3 秒窗口反着读一遍(他**打出去**多少):

| 分层 | 腿 | kind | episode | dealt>0 | **dealt > taken** | median dealt |
|---|---|---|---|---|---|---|
| ab | armed | camp | 102 | 68.6% | **56.9%** | 184 |
| ba | armed | camp | 95 | 70.5% | **65.3%** | 219 |
| ab | baseline | camp | 159 | 80.5% | 71.7% | 281 |
| ba | baseline | camp | 72 | 80.6% | 75.0% | 318 |

**四个格同号**(两层 × 两腿)⇒ 不是分层噪声,也不是 arm 串的产物(baseline 腿更高)。
在这些帧上,gate 的效果(释放 ⇒ 回家逻辑重新掌权)**指向错的方向**:
他正在把营地打死,而且血在回。

### 3.3 ⭐⭐⭐ 那个"不需要 baseline 腿的 甲/乙 判别器"被逐帧证伪

工具 §3b 的设计:世界甲预测 `camp home_tp ≈ lane home_tp`,世界乙预测 `camp home_tp ≈ 0`。
聚合表读数看起来很干净,**两层都像世界甲**:

| 分层 | armed camp(带补给) | home_tp | armed lane | home_tp |
|---|---|---|---|---|
| ab | 20 | **1(5.0%)** | 245 | 10(4.1%) |
| ba | 26 | **1(3.8%)** | 123 | 5(4.1%) |

**逐帧一看,这 2 个阳性事件一个都不是营地那一帧造成的**:

- `b1386e/20260829_123225_slot7` **emberspirit**(ab,armed):域帧 t=635.4,hp 0.539,
  3 秒创伤 53(纯中立),**同 3 秒打出 361**,最近敌方英雄 lion **4559u**。
  他 t=637.4 自己走出营地,**HP 一路在涨**(0.539→0.575);**真正掉血是 t=641.4 起冲塔**
  (`tower` 85→169→263→282 连续 6 帧),到 t=648.4 只剩 **0.103**。
  20 秒 horizon 里的那次 home_tp 属于**这段推塔**,与营地那一帧无因果关系。
- `6df84c/20260829_124456_slot8` **lion**(ba,armed):域帧 t=156.4,hp 0.262,创伤 34(中立),
  zuus **1772u**。他 t=156.4–159.4 域内**边走边回血**(0.262→0.305);
  **t=162.4 起吃英雄伤害**(`heroDmg3s` 166→202→213),hp 0.332→**0.040**,
  **t=167.4 才传送回泉水**(坐标从 (6445,-4350) 跳到 (7069,6470),距 zuus 12213u)。
  那次 home_tp 是**团战**的结果。

⇒ **判别器的两个阳性 2/2 是污染的**,它在本语料上**分不出 甲 和 乙**。
`WasRecentlyDamagedByCreep` 到底算不算中立生物,**本轮仍未解决**
(`state.json:fieldcreep_engine_semantics_20260822T2300Z` 那条继续挂着)。
同时,§AR.3(乙) 的退出条件(**"camp 帧确实出现、而它在其上一次都没点亮" ⇒ 立刻出集**)
**本轮不成立、也不被满足**:camp 帧确实出现(428 个 episode),但"点没点亮"**不可观测**,
而唯一为它造的观测器刚被自己的帧证伪。**这不是交付问题(甲 栏交付了),是估计量问题** —— 交总监重裁,见 §5。

### 3.4 ⭐⭐ 申请书自己标注的"过宽角落",在两层里都是**多数**

`state.json:fieldcreep_20260822` 的 `known_over_broad_corner` 写的是
「a flask against a single lane creep, where staying would still have won」。
按**每帧他包里真实持有的最强补给**算(不是按四种补给的通表):

| 分层 | kind | 带补给的 episode | veto 正确(创伤 > 补给/3s) | **veto 反向** | 无补给(fieldbuy 那半) |
|---|---|---|---|---|---|
| ab | pure-lane | 448 | 123(27.5%) | **325(72.5%)** | 393 |
| ba | pure-lane | 261 | 72(27.6%) | **189(72.4%)** | 223 |

**两层逐位重合(72.5 / 72.4)**。纯小兵帧的 3 秒创伤中位数 **24**(ab/ba 同),
而包里最常见的补给是 tango(21)与 faerie_fire(85);营地接触帧的中位数是 **91 / 82**。
⇒ **"角落"是它剩下域的多数**,而不是边角。申请书自己标的"下一个杠杆"(读伤害**量级**)
**本轮拿到了它需要的语料级证据**。

**钉在帧上的那一例**:`6df84c/20260829_123208_slot1` **obsidiandestroyer**(ab,armed),
t=249.5,hp **0.268**,lv 6,3 秒创伤 **36**(纯小兵:`creep_*_melee`/`ranged`),
包里 **faerie_fire(85/3s)**,最近敌方英雄 sniper **1810u 且在拉开**。
之后 10 帧他留在原地区域,**hp 0.268 → 0.321 一路回**,没死、没被追。
**armed `fieldcreep` 在这一帧否决 situation(两个世界都否决,因为打他的是小兵),
把帧交还给回家逻辑** —— 这正是 owner P2 要治的那个方向的反面。

## 4. ⭐ 新问题:两个世界**都够不着**的那一半 —— 召唤物 / 守卫 / Roshan

`fieldcreep` 问的是 `WasRecentlyDamagedByCreep`。**沙王的蛇棒、WK 的骷髅、Roshan 在两个世界里
都不是 creep**,而上游四条危险子句只读**英雄**和**塔** ⇒ 一个**只**被这类单位打的域帧,
对这个谓词里的**每一条**子句都是隐形的。

| 分层 | 腿 | 域 episode | 有 'other' 伤害 | **只有 'other'** | 其中 20s 内死亡 |
|---|---|---|---|---|---|
| ab | armed | 2382 | 110(4.6%) | **93(3.9%)** | **14(15.1%)** |
| ab | baseline | 2811 | 65(2.3%) | 58(2.1%) | 4(6.9%) |
| ba | armed | 1494 | 44(2.9%) | 37(2.5%) | 3(8.1%) |
| ba | baseline | 1445 | 62(4.3%) | 49(3.4%) | **7(14.3%)** |

参照:**全体域 episode 的 20s 死亡率 ab 3.6% / ba 4.4%**。
⇒ **四个格全部高于各自分层的参照(1.6×–4.2×)**,**方向不依赖腿也不依赖分层**
—— 这是**域的性质**,不是 armed−baseline 差分(所以不吃 44 id 同波的归因问题,章程 §4a)。

**铁证帧**:`6df84c/20260829_124500_slot9` **vengefulspirit**(ab,**baseline** 腿),
t=647.5,hp **0.319**,lv 12。前 4 帧 shadowshaman 在 **208–518u** ⇒ 英雄子句否决、不在域内;
**t=647.5 那一帧 shadowshaman 不见了**(最近敌方英雄跳到 sniper **4552u**),
四条子句同时满足 ⇒ **进域**。而**打她的是 `npc_dota_shadow_shaman_ward_1`**(3 秒 310 点),
`fieldcreep` 两个世界都不认它。她 **t=650.0 死在原地**(坐标 3 帧不动:(2006,-4465))。
全语料 'other' 伤害来源前三:`wraith_king_skeleton_warrior` 373 / `roshan` 355 /
`shadow_shaman_ward_1+2` 373。

⇒ 已开 **GH issue([strategy])**,见 §5。

## 5. 开的 issue / 交出去的棒

见 §8(发表记录)。两条,都带帧证据与建议的 fixture 帧:

1. **[strategy]** `fieldcreep` 按**伤害量级**重窄(申请书自标的"下一个杠杆"),
   依据是 §3.2 + §3.4 的两层读数;建议 fixture 钉
   `6df84c/20260829_123208_slot1` **obsidiandestroyer t=249.5**(纯小兵 36 vs faerie 85,
   前后帧都在域内、hp 在回升,不吃相位)。
2. **[strategy]** 新杠杆候选:危险子句加**召唤物/守卫**一读(§4),
   建议 fixture 钉 `6df84c/20260829_124500_slot9` **vengefulspirit t=647.5**
   (进域那一帧,2.5 秒后死亡)。

**§AR.3(乙) 的退出条件交总监重裁**(§3.3):它写死的二值观察在本语料上**不可执行**,
原因是估计量被污染而不是交付缺失;是否照字面出集,不是录像组能单方面决定的。

## 6. 交付:`fieldcreep_domain.py` 四处入库改动(未新开 scratchpad 度量脚本)

`--selfcheck` **31 → 36 PASS / 0 FAIL**。

1. **铁律 4(i) 进工具**:新增 `--stratum {all,ab,ba}` 与报告头的 `STRATUM:` 行
   (`ab` = candidate 侧为 radiant,沿用 `lion_drain_census.py:297-301` 的约定)。
   以前这个工具**只给池化一个数**;本轮所有结论都是两层各读一遍得到的。
   arm 串的 FATAL 检查**排在分层过滤之前** —— 否则 `--stratum ab` 会悄悄放过
   `--stratum all` 会拒绝的语料。新增 3 条 selfcheck 钉"ab+ba 是**划分**不是过滤"。
2. ⭐ **分类器修了一个真错:攻城兵不带 `creep_` 中缀。** 引擎发的是
   `npc_dota_goodguys_siege` / `npc_dota_badguys_siege`,而原 `LANE_RE` 只认
   `^npc_dota_creep_(goodguys|badguys)_` ⇒ **域窗口里 61 次攻城兵命中被丢进 `other`**,
   即**既不算甲也不算乙**,而且丢的是**全场单次伤害最高的那种小兵**。
   selfcheck 之所以没抓到:它断言的是一个**发明出来的名字**
   `npc_dota_creep_badguys_siege`,**121 局里出现 0 次** —— 语料从来没被问过。
   **与章程"工具坑"里 `blink_dagger`/`item_blink` 是同一族**:字面量抄错了一侧。
   修后两种拼写都认,并新增 4 条 selfcheck(真名 / 发明名 / 塔仍不是攻城兵 /
   `melee_upgraded` 仍在 lane / 英雄召唤物**不**是小兵)。
   ⚠️ **本轮 §3 的所有数字都是修后重跑的**;修前池化 甲 veto 是 22.1%,修后 22.4%。
3. **新增报告节 §3d**(§4 那张表):召唤物/守卫/Roshan 的盲区,**按腿分开打只是为了让读者
   看见它不依赖 arm 串**,措辞写死"这是域的性质,不是腿的差分"。
4. **selfcheck 的一条 FAIL 改对了,不是改松**:`hits inside (-3.0, 0]` 在全语料上 FAIL,
   逐条查出是**存储时 `round(dt,2)` 的显示误差**(原始过滤 `hits_in_window` 是严格的),
   全语料**恰好 2 条**、都在 `a29ed3/20260829_121928_slot10` luna t=513.8。
   改法:断言带 **0.005 的舍入容差**(再宽就不钉 3.0 这个常数了),
   **另加一条在未舍入输入上直接钉 `hits_in_window` 的单元断言**(3.0 排除 / 2.999 与 0 保留)——
   **牙齿从被放松的那条挪到了新的那条,没有丢**。

**本轮未改任何 `bots/` 或 `game/` 下的文件,未动任何 gate。**

## 7. 诚实边界(不主张的东西)

- **不主张 `fieldcreep` = WORKING**,也**不主张**它 = SILENT 的反面等于"它做对了"。
  买到的是**域**与**前提检验**,不是触发。
- **不主张条件 (b)** —— 不归本组。
- **不主张 甲/乙 的引擎语义有了答案**(§3.3 明确说判别器废了)。
- §3.2 / §3.4 的读数是**域内分布**,不是 armed−baseline 差分 ⇒ **不吃**「44 个 id 同波,
  差分是合力」那条(章程 §4a)。凡是本轮引用了 armed−baseline 对比的地方
  (§3.1 的两栏)都**只用来说明域存在**,没有拿它下因果结论。
- **最薄的格**:`ba/armed/camp 带补给` **26 个 episode**、`ab` 20 个 —— §3.3 那两个阳性
  各只有 **1 个事件**,这也是为什么它经不起逐帧看。**0/12 与 0/120 不是同一种证据**
  (工具自己打的 power bound,照抄)。
- **W26 本轮未并入**(上一轮已读过:每 run 仅 2 局有帧,承重可忽略);**W27 结构上不可并**(45 id 串)。
- 未跑 `sweep_run.sh` 的 15 个通用检测器(欠账,连续第三轮)。
- `seed 975` **第九轮**;`wandlimbo` 预登记因 **#293 未落地****第七轮**不可执行(本轮未复核);
  **GH #265 仍被 #272 阻塞**(本轮无新增)。

## 8. 验证与发表

- `bash tools/agent/luacheck_gate.sh` → **exit 0 / `luacheck bots game: 0 warnings`**
  (脚本自装 `lua-check`)。**未使用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行。**
- `bash tests/run_py_tests.sh` → **53 passed / 0 failed / 0 uncertifiable**。
- `fieldcreep_domain.py --selfcheck`(121 局全语料)→ **36/36 PASS**。
- 本轮未改 Lua ⇒ **不声称跑绿过 Lua 全量**(GH #124)。
- AWS:**S3 只读,零 EC2,零 CE 调用**;`session_setup.sh` 身份 `dota2bot-agent` 已核。
- **MCP 未触发 `requires approval`。**
- 发表顺序按 **GH #290**:**先 push 后发**,评论草稿过 `claim_precheck.sh`。

## 9. 下一轮第一件事

1. **`fieldcreep` 的棒已交出去**(§5 两条 issue + 总监重裁 §AR.3(乙)),不掉在本组;
2. 下一个零记录 id:**`pullcad`** 或 **`zusstatic`**
   (`campvoid` 已裁 SILENT、`fieldcreep` 本轮已测域;`pullcad` 注意章程记的"合取陷阱");
3. **W27**(00:19Z 波,45 id 串,四 run `4ca0c1`/`40db63`/`39656f`/`1f8a55`)——
   **先确认是否已停 + 跑 `arm_string_census.py`**,再决定能不能读;**它与 W25/W26 不可并池**;
4. 两笔老欠账仍挂着:`--escape-w 12` 敏感度、"振荡型死锁"跨 episode 合并规则。

## 10. Token 用量

`python3 tools/agent/token_usage.py` 最后一行,原样:

```
TOKENS total_in=9,420,363 out=48,576 turns=71
```

(统计时刻为止;之后的收尾回合不计入。)
