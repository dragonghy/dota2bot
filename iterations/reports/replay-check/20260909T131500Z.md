# replay-check 2026-09-09T12:3x–13:3xZ — W60 宽扫 96/96 + `tpgap` 条件 (a) 取证

**一句话**:W60(今天 12:13Z 刚收割的 4/4 波)**96/96 局宽扫、0 不可解析**;
本轮买的是三条今天 10:xxZ 落到本组头上的 `a_evidence_*` 欠账里的一条 —— **`tpgap`**;
读数是 **INDETERMINATE**,而 ⭐⭐ **头号产出讲的是仪器不是杠杆**:
子域里 **11 枚 armed 按下逐枚定案,10 枚倒在恰好两个 dump 里没有的字段上**
(4 枚移速 / 6 枚能见度),**0 枚能被豁免** —— 这台仪器**不但看不见「它拒了」,
连「它放行了」也定不了案**,除非那一帧同时满足「采样步长 ≥285」且「见证命中」,
96 局只出现 **1 次**(而那一次就是反例)。

---

## 一、开工自检(铁律 10)

⭐ **第一条命令又踩了证据纪律 3 的形状,但工具自己拒答了**,逐字:
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.`
⇒ 那道自拒门**第五轮起作用**。改走重定向后**第二次踩了上一轮自陈过的 GH #514**:
`timeout 600` 把它砍了(`EXIT=144`)—— **本台连续第二轮在自己的 timeout 上摔**。
第三次**去掉 timeout、后台跑**,拿到了工具自己的读数(见文末 §六)。

## 二、宽扫:W60 全波 **96/96 局**

| | |
|---|---|
| 波次 | **W60**,`--on-demand`,pinned tree `f25680bcbed59113248284bdec39f624c24e525b` |
| run | `…7689b5`(9911) / `…be701b`(10079) / `…15755b`(10097) / `…7a68aa`(10133) |
| 局数 | **26 + 23 + 21 + 26 = 96/96** 非暖场局,暖场 24 局跳过 |
| unparseable | **0**,`SWEEP_EXIT=0` ×4 |
| 成本 | S3 **只读**;零 EC2、零新波、零 Cost Explorer |

⚠️ **96 是可播放录像的全量,不是 W60 的全量**:批测台收割的是 **245 局 / 221 计分**,
`.dem` 只对 `--rec-slots` 的槽位落盘(每 run 32 个,减 6 暖场)。
**宽扫覆盖率按录像口径是 96/96(100%),按计分局口径是 96/221(43.4%)** —— 两个都登记,
因为**只有前者是本组能看的东西**。

**通用检测器 96 局汇总**(⛔ 侧偏未消除的计数量,按铁律 4(i-b) **只用于选点,不构成结论**):

| 检测器 | 总触发 | candidate 侧 | baseline 侧 |
|---|---|---|---|
| deep_solo_death | 489 | 245 | 244 |
| died_with_ult_ready | 717 | 369 | 348 |
| enemy_overchase_unpunished | 156 | 79 | 77 |
| idle_while_ally_dies | 412 | 214 | 198 |
| laning_past_midline_death | 585 | 266 | 319 |
| lowhp_limbo | 139 | 61 | 78 |
| missed_cs_at_tower | 543 | 260 | 283 |
| overextend_alone | 2396 | 1160 | 1236 |
| return_to_death_spot | 773 | 343 | 430 |
| sandwiched_walk | 3127 | 1558 | 1569 |
| tp_home_wasteful | 653 | 291 | 362 |
| tp_under_threat | 761 | 363 | 398 |
| unpunished_tower_dive | 863 | 445 | 418 |

## 三、深查:`tpgap` 条件 (a) —— 全文见 `a_evidence_tpgap.md`

```
VERIFY id=tpgap verdict=INDETERMINATE episodes=26
```

**为什么是这一条**:总监今天 10:xxZ 一次性把三条 `a_evidence_*`(`pulldrag` /
`tpgap` / `liondrainstop`)写进 `owed_executions`,executor 全是本组,
且**零 AWS、语料已存在**。本轮取 `tpgap` 一条,**一个工作单元只取一条**。

**动手前先按 W58 的教训 `ls tools/batch_test/behavioral/ | grep tpgap`** ⇒
`tpgap_domain.py` **已在 trunk**(08-26 本组自己写的),本轮**零新代码**,
只跑 `--selfcheck`(**50 PASS / 0 FAIL**)、`--source`(exit 0)与真语料。

**可达性(先验证再读数)**:`git show f25680bc:…/test_set.md` 第 2 行 ⇒
`tpgap` 是 **37 个里的第 16 个**,串 **335 字节 / md5 `b525d51d…`**,
与 `W60_wave.json:arm_md5` **逐位一致** ⇒ 预登记的第一嫌疑「arm 串漏了它」**排除**。

### 3.1 逐帧在先 —— 承重帧(手查,不是照抄工具)

**`20260909_093852_slot7` seed 10097 `queenofpain` t=549.6,ARMED 腿**:
zuus 在 **563 u**(gap 带内唯一敌人),按下瞬间血量 **371**;
zuus 的连招在 3 s 通道内**实打 396**(arc 119 + static 11/7/7/5 + jump 17 + 平 A 47 + bolt 183),
**t=551.3 死,通道第 1.7 秒**。三条离线可查的 fall-through **全排除**:
最近敌 563 > 350;窗口内采样步长 **401 / 401 / 346 u/s** 全 ≥ 285(**观测反驳**,不是假设);
按下帧无 stun/root/hex 类 modifier。**能见度见证也命中**(己方单位正在打 zuus)。
⇒ **armed 腿没有拒**,唯一剩下的解释是引擎 `GetEstimatedDamageToTarget` 自己读低了。
**这是 08-26 `slardar t=1382.2` 之后第二枚反例**,也是本轮唯一三条全排除的一枚。

另两枚手查(同为 armed 子域,方向相反):`100233_slot6 chaos_knight t=593.9`
(axe 372 u,3 s 实打 **368 ≥ 326**,t=596.8 死)与 `100313_slot4 lion t=345.3`
(lina 542 u,`lina_slow_burn` 28/s,t=347.3 死)—— 两枚最快采样 **279 / 254 u/s < 285**
⇒ **移速 fall-through 排除不掉**,按 UNSETTLED 记,**不当反例用**。

### 3.2 聚合(铁律 4 (i-a):两层读数都登记)

子域(mid_gap ∧ retreat ∧ realized-lethal)armed **11** vs baseline **15**,
`d(share)` radiant **−0.0074** / dire **+0.0088** ⇒ **两层反号**,
计数类且侧偏未消除 ⇒ 按 **(i-b) 噪声,不写进结论**;工具独立判 **REFUSE**。
全带(mid_gap ∧ retreat)150 vs 198 两层同号下降,⛔ **但那不是 `tpgap` 的域**,
37-id 捆绑波里任何撤退杠杆都能造成它,且对照层自己不干净 ⇒ **不记到 `tpgap` 头上**。
**反向哨兵没塌**:撤退按下 1011 vs 1154(两层同号),撤退 TP 存活率 **86.3% vs 85.2%**。
**逐种子子域格子 0–5**,密度 armed **0.11 / 局** —— 预登记的第二嫌疑「子域太稀」
**被量出来了**,而预登记逐字禁止「那时放宽谓词」。

### 3.3 头号读数:仪器,不是杠杆

11 枚 armed 子域按下逐枚定案:**0 EXCUSED / 1 SHOULD-HAVE-REFUSED / 10 UNSETTLED**,
10 枚全倒在**恰好两个 dump 没有的字段**上 —— **4 枚移速**、**6 枚能见度**。
**`0 EXCUSED` 不是「没有一枚该被豁免」,是这台仪器一枚都豁免不了。**
08-26 记过「拒绝是缺席,离线看不见」;本轮补硬的一句是:**放行也定不了案**。

## 四、开的 issue / 追评

- **新开 GH #662 `[harness]`**:请求 dumper snapshot 补 `move_speed` 与 `visible_to_team`。
  验收写死:同一份 W60 语料重跑 `tpgap_domain.py`,**10 枚 UNSETTLED 全部落到
  EXCUSED 或 SHOULD-HAVE-REFUSED**。⭐ **不是一条 id 的债** —— `tp_channel_death.py`
  全家(`tpreach`/`tpcommit`/`tpdying`/`tpdead`)与任何以「可见敌人」或「移速」
  为子句的杠杆卡在同一堵墙上。
- **GH #159 追评**(`issuecomment-5602539699`):`a_evidence_tpgap.md` 全文读数 + §3.1 承重帧 + §3.3 仪器读数。
- **建议 fixture 钉**(给协同组/总监,不是本组判定):
  `20260909_093852_slot7 t=549.6 queenofpain`。⚠️ 落地价目表见 GH #659 的教训,**本组不代付**。

## 五、限度(登记,不藏)

1. **96 局是录像口径的全量,不是 W60 计分局的全量**(96/221 = 43.4%)。
2. 子域 26 个 episode 在 4 粒种子上是 **0–5 每格**;本轮**不**用它下任何腿间结论。
3. §3.1 的三枚手查帧里,**只有 QoP 那枚**三条 fall-through 全排除;
   另两枚的移速借口**排除不掉**,报告里按 UNSETTLED 记,**没有拿它们凑反例数**。
4. `lion` 那枚里「打死他的 DoT 名字带 slow、而移速地板恰好排除被减速者」是**形状不是结论** ——
   dump 无移速字段,本组**没有** `modifier_lina_slow_burn` 是否真减速的独立证据。
5. 另两条同批欠账(`a_evidence_pulldrag` / `a_evidence_liondrainstop`)**本轮未取**,
   仍 OWED —— 一个工作单元一条,下一轮接着取。

## 六、验证(铁律 6 / 10)

- **本轮 `bots/` / `game/` / `tests/` / `tools/` 一行未改**(改动只有本报告、
  `a_evidence_tpgap.md` 与章程节)。
- **开工自检**:**`SELFCHECK_EXIT=3`**,`FINDINGS: cadence owed-executions`,
  `UNCERTIFIABLE (exit 2): trunk-red(python)`,`NOT RUN (inside a leg): tests/test_selfcheck_lua_leg.py`。
  三条**全是已知项**(cadence 的洞是 09-08 strategy 的;owed-executions 正是本轮在还的那张表)
  ⇒ **无新发现,不开 issue**。稳定版锚点 **6/6 OK**、`FROZEN none`、5 个 co-promote 原子全 ok;
  **fast Lua 腿 `87 tagged detector file(s), 0 failures`**。
  python 腿 **117 passed / 0 failed / 1 uncertifiable** ——
  那 1 条是 `test_selfcheck_lua_leg.py` 在 **120s 预算**内没跑完
  (`87 file(s) discovered in 120.1s`),⭐ **归因是本台自己**:四个 sweep 正在满载这台容器,
  工具自己也逐字提示 `re-run on a quiet tree`。**不是 trunk 红,也不是通过。**
- 静态门随 `git push` 自跑(`.githooks/pre-push`),**无 `RULE6_BYPASS`**。
- **动态半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)未跑,不声称。**

## 七、下一轮第一件事

1. **取 `a_evidence_pulldrag`**(`pulldrag_walk.py`,先读它的 docstring ——
   它自己解释了为什么 `pullcamp_domain.py --drag` **结构上**答不了这个问题);
2. 盯本轮那条 `[harness]` 的回音;若已落地,**同一份 W60 语料重跑 `tpgap_domain.py`**
   就能把 10 枚 UNSETTLED 一次性定案(零新波、零 AWS);
3. `a_evidence_liondrainstop` 排第三(判据已被总监 08-21 改过,**别用旧的 span≥2.0s**)。

## 八、token 用量(铁律 8)

```
TOKENS total_in=19,218,613 out=60,169 turns=116
```

(统计时刻为 push 之前;此后的收尾回合不计入。)
