# replay-check 2026-09-12T18:5xZ — 还上欠的 6 局深查,并把 GH #342 交给本组的第 (1)(2) 两条棒答了

**一句话**:上一轮欠的深查(≥6 局)本轮还清并超额(**深查 12/12 局**,W69 全部带戳局)。
目标选的是**核验记录并列最少(n=1)的 `stayfield` / `stayfield2` 对**,
理由是它们的堵点是**域为空(罕见)**,而**罕见是语料能买的**——
不像 `aimguard`(creep 流无谓词无身份,离线结构性拒绝)和 `overchase`/`pullcad`(通道不可分离)那三个。

⭐⭐ **头号产出不是那两个 INDETERMINATE,是一条 13 天没人接的棒**:
**GH #342 §9 把三条任务逐字点名交给录像组**(2026-08-30 开单,至今 open),
其第 (1) 条要的正是「第一失败子句分解**必须先与调用点自己的进入条件求交**」。
本轮的 funnel 探针**就是那个求交后的分解**,而它的读数**推翻了 `stayfield_domain.py` 头注自己写的机制**:

- 头注逐字说 **「`itemFlask == nil` is the load-bearing one and it is why STRICT is much smaller than SITUATION」**;
- 求交后实测(1,421 个 SITUATION 帧,两腿两层):承重的是**调用点自己的 HP/法力帽**
  (`hp>=0.34 and hp+mp>=0.43`,armed **72.1%** / baseline **70.7%**,**四个 leg×层单元里全是第一名**),
  其次 **`level<9`**(22.9% / 27.2%);
  **`holds a salve` 只有 2.6% / 1.0%** —— 它**在 `itemFlask` 被求值之前就已经没人了**。

⭐ 并且**独立证实了 #342 §3 的预言**:它算死 `hp>0.55` 在 TP 腿上**不可满足**(sup botHP = 0.43),
并预测「**走路腿上两条边都是活的,`hp>0.55` 在那里是真桶不是伪影**」。
W69 走路腿实测:`hp>0.55` 是**压倒性第一桶**,四个单元 **82.9%–95.8%**。
⇒ #342 的闭式推导第一次拿到了**帧证据**,两条不同来源同一条指令。

```
VERIFY id=stayfield  verdict=INDETERMINATE episodes=1
VERIFY id=stayfield2 verdict=INDETERMINATE episodes=0
```

**段位声明(GH #424)**:语料 **W69 单波**(27-id),**未与 W68(29-id)/ W66+W67(30-id)并池**。

---

## 〇、覆盖(章程固定节)

- **宽扫 12/12 局**,四个 run 各 3 局带 `mirror:` 戳(各 4 份 `.dem`,各 1 份暖场被
  `sweep_run.sh` 自动跳过),**四个 `sweep_complete.json` 逐字**:
  `dem_found 4 / swept 3 / skipped 1 / unparseable 0 / exit_code 0` ×4。
  ⛔ 覆盖行只引 `sweep_complete.json`(GH #735 立的口径),不手敲 `ls`。
- **深查(逐帧)12/12 局** —— 上一轮欠 ≥6,本轮还清并超额。
  逐帧口径:12 局的**每一个** SITUATION 帧(1,421 个)与**每一次**回家 TP(218 次)
  与**每一趟**走路回家(168 趟)都逐帧求值过子句阶梯;
  另有**一帧完整逐帧还原**写进 §3(W69 语料里 `stayfield` 唯一的 STRICT episode)。
- **本波无新语料**:`s3 ls soak/` 最新四前缀仍是 `0926{22,24,27,29}_1_main_*` = W69,
  与前两轮同批(批测台预算刹车 $90.058 零发波)⇒ 按章程工作流第 2 条走
  「核验记录最少的 id 补课」。
- ⚠️ **分层不平衡,登记(铁律 4(i-a))**:W69 的 `.dem` 语料 **ab 8 局 / ba 4 局**(每 run 带戳三局是
  radiant, radiant, dire)。**这不是波次不平衡,是 replay 子集不平衡** —— 每 run 只有 slot1 落 `.dem`,
  三局带戳无法配平。**录像组看到的每一份 W69 分层读数都继承这个 2:1**,不要读成波次属性。

---

## 一、选题:为什么是 `stayfield` / `stayfield2`

W69 armed 串 27 个 id,按全仓 `VERIFY id=` 记录数排序,**并列最少的是 11 个 id(各 1 条)**。
逐个看它们那 1 条为什么没往下走,**堵点分三类,只有第三类是语料能买的**:

| 堵点 | id | 能否用更多语料买到 |
|---|---|---|
| **离线结构性拒绝** | `aimguard`(creep 流只有 `t,team,x,y`:`CanBeAttacked` 的谓词全不在流里,且 creep 流无实体身份 ⇒ 恢复不出谁是 `[1]` 谁是 `[2]`) | ⛔ 不能,再多语料也一样 |
| **通道不可分离** | `overchase`(「回身惩罚」与「被追上」写进同一个量)、`pullcad`(勾线者右键落点同时被拉线分支与普通补刀写入) | ⛔ 不能 |
| **已 WORKING** | `fieldbuy`、`fieldsip` | — 已买到 |
| **域为空 / 罕见** | **`stayfield`(W29 STRICT=0)、`stayfield2`(W29 predicate-TRUE=0)**、`campfarm`、`campvoid`、`creepthink`、`blinkflee` | ⭐ **能** —— 罕见是语料问题 |

⇒ 取 `stayfield`/`stayfield2`:同族、同一条 owner P2 主线(TP 腿 + 走路腿)、
上一次读数在 **2026-08-30(W29,44-id 串)**,**13 天前、跨波不可并池** ⇒ 本波必须自己重读。

⭐ **本波有一个 W29 没有的干净条件**:`fieldcreep` **不在 W69 的 armed 串里**
(两个工具的披露头都自打 `NOT ARMED in this wave's cand string`)
⇒ GH #341 那一族「armed 腿谓词比 baseline 腿**严格**、而这个差与 `stayfield` 无关」的混杂
**本波结构上不存在**,两腿同用四子句语义。**披露块可见 = 工具查过了;看不见才必须读成没查。**

## 二、量具与纪律

- 量具**全部是既有的**(动手前 `ls tools/batch_test/behavioral/ | grep -i stayfield` 四命中,
  确认是复用不是重写):`stayfield_domain.py`(TP 腿)、`stayfield2_whynot.py`(走路腿)、
  `stayfield_frames.py`(单帧还原)。
- **三次自检退出码逐条 bare 读取、未经管道**(evidence-discipline 规则 3):
  `SF_SELFCHECK_EXIT=0`(15 checks / 0 failures)、`SELFCHECK12_EXIT=0`、
  `SF2_SELFCHECK_EXIT=0`(**27/27**)、`SF_EXIT=0`、`SF2_EXIT=0`、`FRAMES_EXIT=0`。
- 本轮新写的只有**两个判别用探针**,**全程落 scratchpad,零新增仓库文件**:
  `sf_hpband.py`、`sf_strictfunnel.py`。两者**只 import `stayfield_domain` 的谓词**
  (`load_sweeps` / `Game` / `situation` / `has_field_regen_source` / `usable_items` /
  `stratum_of` / 七个源码常数),**一个谓词都没有重新实现** —— 两份拷贝正是两个工具
  开始对同一帧各说各话的方式。`PROBE_EXIT=0` / `FUNNEL_EXIT=0`。
- funnel 探针**按帧记分不按 episode 记分**:`scan_game` 用 **OR** 把 episode 内各帧的
  `strict` 折起来,**折完就说不出是哪条子句在失败**。

---

## 三、`stayfield`:域仍然为空,但这次**空的原因**是量出来的

```
STRICT domain (stayfield 唯一调用点能到达的)   armed 1 episode / baseline 0
STRICT leaks(门 armed、分支条件成立、却照样回家)  armed 0 / baseline 0
分层:ab(8 局)STRICT 0 · ba(4 局)STRICT 0
```

⚠️ **按 `stayfield_domain.py` 自己的纪律,这个零只能读成「本语料无表面」,不许读成 SILENT**
(头注逐字:god's-eye 的 1600 环比引擎的可见性测试**更严**,attributed damage 也**否得更频**
⇒ 它的域是引擎域的**子集**,子集里的零是「真零,但只在子集上」)。故判 **INDETERMINATE**。

### 3.1 唯一那个 STRICT episode,逐帧还原

`0128b9/20260912_095139_slot1`(**armed 腿 = radiant**),`skywrath_mage`,L9。
`stayfield_frames.py` 逐帧(1.0s 网格,idx 锁丢弃 3018 个拷贝快照;自家泉水质心 `(-6836,-6479)`):

| t | hp | mp | d(泉水) | 最近敌人 | 包里 | 判定 |
|---|---|---|---|---|---|---|
| 499.5 | **0.229** | 0.024 | 12343 | 1694 **CLEAR** | clarity, **faerie_fire**, wand… | ⬅ **STRICT(门该否掉回家 TP)** |
| 500.5 | 0.235 | 0.029 | 11945 | 2184 CLEAR | 同上 | ⬅ **STRICT** |
| 501.5 | 0.239 | 0.033 | 12160 | 2200 CLEAR | **clarity 消失** | SITUATION(掉出 STRICT) |
| 509.5 | 0.213 | 0.128 | 13157 | 4929 CLEAR | faerie_fire 仍在 | SITUATION |
| 513.5 | **0.074** | 0.082 | 12666 | 6271 CLEAR | faerie_fire **仍在** | — (hp < 0.18,SITUATION 假) |
| 514.5 | 0.074 | 0.114 | **199** | 9605 | faerie_fire 仍在 | TP 已落地泉水 |

**逐帧读出来的因果链,三步**:

1. t=499.5–500.5 门**真的开着**(hp 0.229、无敌人、包里有 faerie_fire、无大药 ⇒ `itemFlask == nil` 成立、L9)。
   **这两帧它没有回家 TP,也没有喝** —— 门无事可做。
2. t≈501 它**喝掉了 clarity**(法力,不是血) ⇒ `modifier_clarity_potion` 上身 ⇒ **掉出 STRICT**。
3. 之后 12 秒在 hp 0.21–0.27 徘徊(全程最近敌人 2200–4900u,**CLEAR**),被磨到
   **hp=0.074**,**这才**回家 TP。而 0.074 **< 0.18** ⇒ **按下 TP 的那一帧 SITUATION 已经假了**
   ⇒ **门在结构上够不着这次 TP**。这正是 `STRICT leaks = 0` 与 `STRICT episodes = 1` 并存的原因:
   **episode 是按首个域帧记的,门是在按下那一帧求值的,两者不是同一帧。**

⭐ **顺带一条与 owner P2 同向的观察(不占本轮判决位,n=1)**:
`faerie_fire` 从 t=469.5 到 t=524.5 **一次都没被喝**,而 hp 在这段里从 **1.000 走到 0.074**;
中间喝的是 clarity(法力)。全程最近敌人 ≥1694u、大段 CLEAR。
⚠️ **不主张这是 `stayfield` 的锅** —— `stayfield` 只否 TP,**从不下令喝**;
喝不喝归 `fieldsip`/`fieldbuy` 那一族(自检显示二者**嵌在 `stayfield`/`stayfield2` 的 gate 下**,
早返回守卫 `jmz_func.lua:6013` / `:6257`)。**一个聚合差分不许同时记到三个 id 名下(§BW.3)。**

### 3.2 ⭐⭐ 求交后的第一失败子句分解 —— 答 GH #342 §9(1),并订正工具头注

#342 §9(1) 逐字要求:重跑这张表时**先把人口与调用点进入条件求交**。
本轮的 funnel 探针就是这个形状:人口 = **SITUATION 帧**,阶梯 = **「撤退:3」分支自己的子句、按源码顺序**。

**1,421 个 SITUATION 帧,两腿两层分开登记,从不并池:**

| 第一失败子句 | armed ab (n=581) | armed ba (n=153) | baseline ab (n=446) | baseline ba (n=241) |
|---|---|---|---|---|
| `hp>=0.34 and hp+mp>=0.43`(**调用点自己的帽**) | **413 / 71.1%** | **116 / 75.8%** | **335 / 75.1%** | **151 / 62.7%** |
| `level<9` | 132 / 22.7% | 36 / 23.5% | 97 / 21.7% | 90 / 37.3% |
| `holds a salve (itemFlask ~= nil)` | 18 / 3.1% | 1 / 0.7% | 7 / 1.6% | 0 |
| `modifier_clarity_potion` | 13 / 2.2% | 0 | 0 | 0 |
| `modifier_flask_healing` | 2 / 0.3% | 0 | 0 | 0 |
| `tp scroll on cooldown` | 1 / 0.2% | 0 | 7 / 1.6% | 0 |
| **到达 STRICT** | **2 / 0.3%** | **0** | **0** | **0** |

每腿合计(**两层已分别登记,这是登记不是加权并池,铁律 4(i-d)**):
armed n=734 → HP/法力帽 **72.1%**、`level<9` **22.9%**、salve **2.6%**;
baseline n=687 → **70.7%** / **27.2%** / **1.0%**。**HP+level 合计 armed 95.0% / baseline 97.9%。**

⇒ ⛔ **`stayfield_domain.py` 头注那句机制在人口层面是错的**:
`itemFlask` **不是**让 STRICT 远小于 SITUATION 的那条子句,**它甚至排不进前两名**;
在 95% 的 SITUATION 帧上,**分支自己的 HP/法力帽和 `level<9` 已经把人清空了,
`itemFlask` 根本没被求值**。

⭐ **但要把话说准,头注是「条件正确」不是「全错」**:在**清过 HP 帽与 level 之后**
剩下的那一小撮里,salve 确实是第一名 —— armed ab 剩 **36** 帧,其中 salve **18(50.0%)**、
clarity_potion 13(36.1%)、到达 STRICT 2、flask_healing 2、tp_cd 1。
⚠️ **这张条件表不能单独拿去下结论**:剩余人口 armed ba **1** 帧、baseline ba **0** 帧
⇒ **整条条件结论压在 ab 单层上(36 of 37)**,而本波 ab:ba 本来就是 2:1(§〇)。
按 4(i-b),**登记为选点信号,不写进结论**。**能写进结论的是那张无条件表**:
四个单元**同序、同方向**,没有反号。

### 3.3 ⛔ 本轮**不**引用的那个桶,以及为什么

`stayfield_domain.py` 的回家-TP 阶梯在 armed 腿给出 `hp>0.55 = 25 / 26.6%`(baseline 37 / 29.8%)。
**本报告不把这个桶算作 `stayfield` 的域外量** —— #342 §1 已经**闭式**证明:
「撤退:3」的进入条件蕴含 `sup(botHP) = max(0.34, 0.43) = 0.43 < 0.55`
⇒ `hp>0.55` 在这个调用点上**不可满足** ⇒ 那些行**是别的按下点发出的 TP**,
记到 `stayfield` 名下是 #342 §4 那条可复用判据的实例
(**「一条子句的实测命中率是它被评估的那个人口的属性,不是它所在守卫的属性」**)。
⚠️ 本轮**独立复算了那个 sup**:`BRANCH_HP=0.34` / `BRANCH_HPMP=0.43` 由
`stayfield_domain.py:177-178` 从源码取(`ability_item_usage_generic.lua:5514`),
`J.GetMP` 返回非负比值 ⇒ 第二个析取项蕴含 `botHP < 0.43` ⇒ sup = 0.43。**成立。**
⇒ §3.2 那张表**已经是求交后的**,故不受此影响;§3.3 这个桶**弃用**。
⛔ **#342 §9(3)(那 52 行究竟从哪个按下点发出)本轮未买** —— 需要在树上枚举其余回家-TP
按下点并逐个求交,是**源码工作不是帧工作**,留给协同组;本组只复证「不是『撤退:3』」。

---

## 四、`stayfield2`:走路腿 —— #342 §3 的预言拿到帧证据

`stayfield2_whynot.py`,**168 趟连续走路回家**(>4000u → 进 2000u 环,按**出发帧**记分;
TP 趟按构造排除,那属于 `stayfield`)。

```
predicate-TRUE(出发帧)  ab: armed 0.0% (n=60) / baseline 0.0% (n=49)
                         ba: armed 0.0% (n=24) / baseline 0.0% (n=35)
                         balanced delta +0.00pp
ANY-FRAME 上界(走到一半才成立也算) ab: armed 3.3% / baseline 0.0%
                                     ba: armed 0.0% / baseline 0.0%
```

⚠️ ANY-FRAME 那 **+1.67pp** 的 balanced delta 来自 **radiant 单层的 2 趟**,`ba` 层 0.0%
⇒ **单层、n=2,按 4(i-b) 登记不入结论**(计数类量未消侧偏)。

**第一失败子句(出发帧),四个单元:**

| 第一失败子句 | armed ab (n=60) | armed ba (n=24) | baseline ab (n=49) | baseline ba (n=35) |
|---|---|---|---|---|
| **`hp>0.55`** | **54 / 90.0%** | **23 / 95.8%** | **46 / 93.9%** | **29 / 82.9%** |
| `hp<0.18` | 3 / 5.0% | 1 / 4.2% | 1 / 2.0% | 3 / 8.6% |
| `enemy hero 1600` | 1 / 1.7% | 0 | 1 / 2.0% | 3 / 8.6% |
| `no heal in bag` | 2 / 3.3% | 0 | 1 / 2.0% | 0 |
| **predicate TRUE** | **0** | **0** | **0** | **0** |

⇒ ⭐ **#342 §3 预测的「走路腿上 `hp>0.55` 是真桶不是伪影」成立,而且它是压倒性第一桶**
(四个单元 82.9%–95.8%,**同序同方向,无反号**)。
#342 那条是**闭式、零语料**的推导(走路腿从 `GetDesireHelper()` 到
`J.ShouldRegenNotWalkHome()` 之间唯一那处 `botHP` 比较锁在 `DotaTime() < 0` 且
`not enemy:IsBot()` 的对人局梯子里 ⇒ 全 bot 批测局取不到 ⇒ 两条边都活)。
**本轮给它配上了帧。**

**再加一条 `stayfield2` 自己的量具打出来、方向一致的读数**(时机,不是子句):

```
168 趟里进过 hp 带 [0.18,0.55] 的:31 趟(18.5%)
带打开时已经走完的比例:med 0.17 / p25 0.00 / p75 0.33 / ≥半程 6 趟(19.4%)
带打开时距自家泉水:med 6640u / p25 4465u / p75 7678u
趟内 MIN hp 进过带、但谓词始终没成立过的:11 趟
```

工具自己那句该被抄走:**「a veto that only becomes available after the walk is mostly
done cannot prevent the walk -- it can only stop the tail」**。

### 4.2 ⚠️ 本轮**查过并排除**的一个反对意见:`hp>0.55` 用的是不是错的天花板?

开工自检的 `test_detector_source_constants.py` 里有一条 `ok` 逐字写着
**「the two regen helpers really do carry different ceilings (stayfield2 widens the band on purpose)」**,
而 `stayfield2_margin.py:95` 的 `SHIPPED_HP_HI = **0.75**`(`jmz_func.lua:4674`)。
⇒ 若 `stayfield2` 走的是 0.75 那条带,则 §4 那个压倒性的 `hp>0.55` 桶**有一半是伪影**
(`stayfield2_whynot.py:117` 用的是从 `stayfield_domain` import 的 `HP_HI = **0.55**`)。
**逐条追源码,排除了这个反对意见**:

```
J.ShouldRegenNotWalkHome (:6256-6259, gate 'stayfield2')
  └─ J.ShouldRegenNotGoHome (:5999-6007)
       └─ J.IsFieldRegenSituation (:5903)  →  nHP < 0.18 or nHP > 0.55   ← 就是 0.55
J.ShouldStayAndRegen (:5284-5289)          →  nHP < 0.18 or nHP > 0.75   ← 另一个 helper
```

⇒ **0.75 不在 `stayfield2` 的链上**:它是 `J.ShouldStayAndRegen` 的带,而那个 helper
**已经 promote(每局 turbo 都活)**,是 `stayattr` 住的地方,不是 `stayfield2` 住的地方。
`stayfield2_whynot.py` 用 0.55 **是对的**,§4 的读数**不需要订正**。
⭐ **登记进工具坑**:同一族里**两个天花板(0.55 / 0.75)、两个 sibling 工具各自合法地用一个**,
而自检那句 `ok` 读起来像是在说 `stayfield2` 用宽的那个。**判别子是调用链不是工具名。**

### 4.1 两个 id 合起来的那句话

**TP 腿与走路腿的堵点是同一件事的两半,而且方向相反地夹住同一个窗口**:

- **走路**回家:**82.9–95.8%** 发生在 **hp > 0.55** —— 机器人**走回家时基本没受伤**。
- **TP** 回家:调用点自己的帽把 hp 压到 **< 0.43**,而门在**按下那一帧**求值;
  §3.1 那一帧按下时 hp 已经 **0.074**,**穿过 0.18 地板掉出域**。
- 中间那条 `[0.18, 0.34)` × `level ≥ 9` × **无大药**的窄缝,就是 `stayfield` 在
  **12 局 / 1,421 个 SITUATION 帧**里只剩 **1 个 episode、0 次 leak** 的原因。

⇒ **这一族的 (a) 不是「再跑几波就买到」** —— 按现有域,armed 腿在 12 局里
**一次都没走到过可以被观测的执行**。⚠️ 但**本组不主张改域、不主张出集**:
域该不该动是协同组的设计判断 + 总监的裁定,本组只交**测量**。

---

## 五、本轮 issue

**净增 1 条 + 1 条追评。** 先搜后开:两次语义检索
(「stayfield HP 带太窄 / 机器人高血走回家」**1 命中 = #342**、
「stayfield2 走路谓词从不为真 / level 9 地板」**0 命中**)⇒ 同题已有单走追评,
工具头注那条是**新题**,另开。

1. **追评 GH #342**(`issuecomment-5648043419`)—— 答 §9 交给本组的第 (1)(2) 两条:
   §3.2 的求交后分解(第 (1) 条,并附它推翻的头注原文)+ §4 的走路腿 `hp>0.55` 帧证据
   (第 (2) 条,证实 §3 预言)。
   并如实登记 **§9(3) 未买** + 理由 + 该归谁。**该单 13 天 open,本轮是第一次有人接。**
2. **新开 [harness] GH #789** —— `stayfield_domain.py` 头注把 STRICT 远小于 SITUATION 归给
   `itemFlask == nil`,求交后实测它排第三(2.6%/1.0%),承重的是调用点自己的 HP/法力帽
   (72.1%/70.7%)与 `level<9`(22.9%/27.2%)。**这条错归不是无害的**:它把想扩
   `stayfield` 域的人指向大药交换(那是 `bagsalve` 的地盘),而真正夹住它的是另两条。
   建议:头注改成求交后的排序,并把 §3.2 那张表钉成该文件 `--selfcheck` 的一条断言。

## 六、AWS

只读:`s3 ls soak/` ×1 + 单 run `s3 ls` ×4 + `s3 cp` 一份 6KB analysis.json + 16 份 `.dem`
由 `sweep_run.sh` 拉取(读取,非计算)。**零 EC2 / 零 CE 调用 / 零支出**;`AWS_SETUP_EXIT=0`。

## 七、树上改动

仅**本报告** + **本文件章程「当前状态」节**。
`bots/` `game/` `tests/` `tools/` **一行未改**;**零新增仓库文件**(两个探针留在 scratchpad)。

## 八、下一轮第一件事

1. ⭐ **`campfarm` / `campvoid` / `creepthink` / `blinkflee`** —— §一那张表里「域为空/罕见」
   剩下的四个,是本组还没补课的、语料能买的 n=1 id。**优先 `creepthink`**
   (`creepthink_stillness.py` 现成,且它不像营地那两个受 §BW.3 归属纪律的三方合力限制)。
2. ⛔ **别重跑本轮三条**:`stayfield`/`stayfield2` 的 W69 域读数(已 12/12 满覆盖)、
   #342 那个 `sup botHP = 0.43`(已独立复算成立)、`aimguard`/`overchase`/`pullcad`
   的结构性拒绝(§一表,别再当 n=1 候选捡起来)。
3. ⭐ **W70 一有语料**:`stayfield` 这一族**不必**再首检 —— 12 局 1 episode 已经说明
   不是样本量问题;把新语料花在第 (1) 条那四个 id 上。
4. ⭐ **两条新判别子进工具坑**(§九):求交纪律 + `.dem` 子集分层不平衡。

## 九、工具坑(本轮新增两条)

1. ⭐⭐ **「第一失败子句分解」必须先与调用点的进入条件求交,否则最大的桶可能属于
   一条在被测 id 所在处已经死掉的子句。** 这是 #342 §4 立的判据,本轮是它的**第二个实例**,
   而且这次**被错归的是工具自己的头注**,不是某一轮的报告 ⇒ **错归会随工具一起传播**。
   可执行判别子:读任何 `*_domain.py` 的「why-not / first-failing-clause」表之前,
   先问**「这张表的人口,是不是已经与该 gate 唯一调用点的进入条件求过交?」**
   —— `stayfield_domain.py` 的**回家-TP 阶梯没有**(§3.3 弃用),**funnel 那张有**(§3.2)。
2. ⚠️ **`.dem` 语料的分层不平衡,不等于波次的分层不平衡。** 每 run 只有 slot1 落 `.dem`,
   W69 每 run 带戳三局是 radiant/radiant/dire ⇒ 录像组看到的是 **ab 8 / ba 4**,
   而波次本身(224 局)可能是平衡的。**任何「ba 层读数偏小/为零」的判读都要先减掉这个 2:1。**
   本轮 §3.2 条件表的 `armed ba n=1` / `baseline ba n=0` 就是被它放大的。
3. ⭐ (复现旧坑,第 6 次)`routine_selfcheck.sh` **第一条命令又撞管道门** ——
   脚本自打 `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked`,
   并逐字说「**它已经复发 5 次,每次都是本轮的第一条命令**」。
   ⚠️ **那次读到的 `SELFCHECK_EXIT=0` 是 `tail` 的退出码不是脚本的**,护栏说的正是这句;
   改 `> /tmp/sc.log 2>&1` 后跑成。**护栏第 6 次生效,那次不是通过。**

## 十、开工自检(铁律 10)

```
selfcheck worst exit: 3        legs run: 12
FINDINGS (exit 3)     : cadence queue-rulings owed-executions trunk-red(python)
UNCERTIFIABLE (exit 2): none
NOT RUN (inside a leg): tests/test_lua_gate.py tests/test_luacheck_gate_soakswitch.py
                        tests/test_selfcheck_lua_leg.py
```

⚠️ **腿内那三条这轮没人看过,不是通过。**
⚠️ **真码读法**:脚本以 `> /tmp/sc.log 2>&1; echo "EXIT=$?"` 起,**真码 `EXIT=3` 裸读**;
后台任务通知栏报的 `exit code 0` **是 wrapper 的码不是脚本的**(`echo` 成功 ⇒ 复合命令 0),
**不要引用它** —— 这与 §九第 3 条是同一族(读数被中间那层换掉了)。

- **trunk red 逐条(python,`120 passed / 3 failed / 3 uncertifiable`)**:
  `test_carrier_terms.py`(65 checks 5 failed)、`test_detector_source_constants.py`、
  **`test_run_tests_guard.py`**。
  **`bots/` `game/` `tests/` `tools/` 本轮一行未改 ⇒ 三条都不是本轮增量。**
- ⭐ **与上一轮对照三处变化,两处朝好的方向**:
  (i) 上一轮 Lua 红的 `test_stayfield2_marginal_domain.lua`(87 里 1 个)**本轮不红了** ——
  **87 个打标检测器 / 0 失败**,`trunk-red(lua)` **整条从 FINDINGS 里消失**。
  ⭐ 这条恰在**本轮这一族**里,别组两轮之间修掉了;
  (ii) 上一轮红的 `test_stale_waits.py` **本轮不红了**;
  (iii) ⚠️ **新红一条 `test_run_tests_guard.py`**(上一轮不在名单里)——
  **本轮不认领**(非本组增量),但**登记点名给总监**。
- ⚠️ **`test_detector_source_constants.py` 那条本轮特意查过,因为本轮整份读数都建立在
  detector 源码常数上**:失败点是 `tpdeadthbuy_domain.py:150` 的 `read_source()` 抛
  `SourceConstantError: item_purchase_generic: the tpdeathbuy arm is no longer ...`
  —— **与 stayfield 这一族无关**,而且**本轮用到的四个常数在同一次运行里逐条 `ok`**:
  `stayfield_domain.HP_LO / HP_HI mirrors J.IsFieldRegenSituation`、
  `stayfield2_margin.SHIPPED_HP_HI mirrors J.ShouldStayAndRegen`、
  `the two regen helpers really do carry different ceilings`。
  ⇒ 那条红**不污染本轮读数**(而它自己是 fail-loud 合同**正常工作**的样子)。
- ⚠️ 实测 **>14 分钟**(章程写「约 20s」,**GH #358 不重开**)。

## 十一、token 用量(铁律 8)


```
TOKENS total_in=10,886,898 out=66,508 turns=75
```
(到统计时刻为止;统计后的收尾回合不计入。)
