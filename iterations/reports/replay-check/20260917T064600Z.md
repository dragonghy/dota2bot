# 录像检查组 W86 — 2026-09-17T06:46Z

**一句话**:按 W85 交棒第 0 条取 `a_evidence_liondrainstop`,**当场发现这一行欠的东西
2026-09-09 就已经交付、并且已经被 09-11 的 promote 消费掉了** —— 它还挂着 OWED 的唯一原因是
`done_when` 探的是一个**从来不会被创建的文件名**;于是本轮把工作单元换成一个**此前没人问过**
的问题:**promote 之后那个没有 gate 的默认,在真实对局里还在执行吗?** 答案是**在**,
有帧级判别子,但**效应量本轮买不到**。

投递物:`iterations/reports/replay-check/a_evidence_liondrainstop.md`(结清该行)。

```
VERIFY id=liondrainstop verdict=WORKING episodes=8
```

---

## 1. 取活与当场的口径更正

`pending_rulings.py --owed-only`(`OWED_EXIT=3`,266 行,⛔ 未经管道)读到
`a_evidence_liondrainstop` = **真 OWED**,`executor=录像组`,行内逐字「已经可以做,
不等任何新波次」。W85 交棒也点的是它。**取活正确。**

⭐⭐ **但它欠的东西不存在** —— 三处实读,**没有一处是转述**:

1. `iterations/reports/replay-check/20260909T184000Z.md:242` 逐字带
   `VERIFY id=liondrainstop verdict=WORKING episodes=36`;
2. `git show 92648c81 -- bots/BotLib/hero_lion.lua` 逐字:
   `- if not ( J.IsModeTurbo() and J.IsSoakCandidate( 'liondrainstop' ) ) then return false end`
   `+ if not J.IsModeTurbo() then return false end`,commit 时刻 **2026-09-11T10:54:30Z**;
3. `bots/BotLib/hero_lion.lua:2523` 现挂 `PROMOTED 2026-09-11 (was soak-candidate 'liondrainstop')`,
   `:2553` 即上面那个 `+` 行。

⇒ **条件 (a) 早买到,而且正是它支撑了 §GU.2 的 promote(armed 34 → 32,`stable-v7`)。**

### 1.1 ⛔ 本节初稿的结论是错的 —— 被本轮自己的推送闸当场证伪

初稿写的是:「`done_when` 探的是一个文件名,投递物落在日期报告里
⇒ **该行在构造上永远读 OWED**,每个取活的轮次都会被它烧掉一轮」。**这是错的。**

创建投递物之后第一次 `git push` 被 `tests/test_pending_rulings.py` 拒了
(`997 checks, 1 failed`),逐字:

```
FAIL: 7 live registry rows read BORN-DONE (a_evidence_liondrainstop, …) but the ceiling is 6
```

⇒ **这一行可以被结清,我刚刚就结清了。** ⇒ **registry 没说谎,读 OWED 是对的** ——
它要的是**一份放在那个路径上的产物**,而那份产物在今天之前确实不存在。

⭐ **真正的缺陷比初稿那条窄,而且在记账链路上,不在判据上**:
本行 `ruling` 把「不退集」建立在三个并排机器读数上,第一个逐字是 **`verify=0`**。

| 时刻 | 事件 | `verify=0` 这个前提 |
|---|---|---|
| 2026-09-09T10:xxZ | 立行 | 成立 |
| 2026-09-09**T18:40Z**(+8h) | `20260909T184000Z.md:242` 写下 VERIFY 行 | **变假** |
| 2026-09-11T10:54:30Z | 总监**以那份证据 promote 该 id** | 假 |
| …至 2026-09-17(**六天**) | 该行**没被关、没被更新**,继续索要已交付且已被消费的证据 | 假 |

⇒ **缺陷 = 一次 promote 消费了某条 owed 行索要的证据,而没有任何东西把两者连起来。**
⛔ 不是「判据不可满足」,⛔ 不是「registry 读错」,⛔ 也**不是** W85 那个 BORN-DONE 的变体
(初稿那张对照表已作废删除)。

⭐ **一条对 registry 有利的现场,照登**:LIMIT 14 的 BORN-DONE 棘轮**工作正常**,
在我结清的同一刻抓住了「这一行从没被人看见过 OWED」这个风险,
并逼我把 **2026-09-17T06:36:23Z** 那次真实 OWED 读数记进 `unmet_at_ruling`
(投递物创建于 07:07:33Z,晚 31 分钟;判据单调 ⇒ 该读数蕴含它在 `ruled_at` 也未满足)。
**这个记账动作是被棘轮逼出来的,不是我自觉做的。**

⚠️ **但取活路径的风险仍然成立(这半没被证伪)**:若不核 §1 那三处实读,
正常动作是「跑 `lion_drain_census.py` 买 (a)」—— 而 (a) 早有,
且 promote 之后该量具的 armed/base 劈分对这个 id **已在构造上失效**(§2),
于是那一轮会产出一个**看着正常、实则无意义**的读数。

## 2. 于是本轮买的是另一个问题

> promote 把 `IsSoakCandidate` 去掉 ⇒ 行为在**两条腿上都开着**
> ⇒ 任何 armed/baseline 对比对它**在构造上失效**
> ⇒ 它从此**掉出所有波次的 arm 串**(本轮 27-id 串里确实没有它)
> ⇒ **一个 promoted 默认此后没有任何例行读数在看它。**

**本轮补的就是这一眼。** 这不是补课,是一个 promote 之后**必然出现却没人安排**的缺口。

## 3. 语料

- **W69**,4 个 run(`spot_20260912_0926{22,24,27,29}_1_main_*`),`SWEEP_EXIT=0` ×4;
  16 局中 warmup 4、**入集 12**、`unparseable 0`;**9 局有 Lion**;4 粒种子。
- **post-promote 成立(实测)**:promote `92648c81` 落于 2026-09-11T10:54:30Z,
  W69 发波 2026-09-12T09:26Z,**晚 ~22.5 小时**;run 名 `_main_` = clone 自 main。
- arm 串 **27 id / 12 局逐字同一串**(`distinct arm strings: 1`):
  **无 `liondrainstop`**(已 promote)、**无 `liondrain`**(出厂释放路径两腿相同)、
  **有 `lionqdmg`**(09-09 那轮必须排除的同一混杂项)。
- 量具 `lion_drain_census.py --verify` **33 asserts OK**、`CENSUS_EXIT=0`、采样 **1.0s**。

## 4. 结论与证据(全文在投递物;此处只摘判别子)

**钉帧 `20260912_094133_slot1`(seed 13052,BASE 腿 = 零 soak id armed)**:

- `277.9` 起引导 tidehunter,**此刻目标已在 500u 环内(458u)**;
- `277.9→282.5` **4.6s 不切**(环内始终有敌人,但 Lion 未被英雄伤害 ⇒ 第二合取项假)
  —— ⭐ **段内自带阴性对照**;
- `282.5` `DAMAGE tidehunter→lion / tidehunter_anchor_smash / 139 / actor_hero=true`,目标 263u
  ⇒ **谓词翻真**;
- `282.7` `MODIFIER_REMOVE modifier_lion_mana_drain` ⇒ **0.2s 后释放**。

**五条判别子逐条实测排除**(281.5–283.5 全部事件逐条看过):
① Anchor Smash **不带控制**,而 Dota 引导**不被单纯伤害打断** ⇒ 引擎侧无机制在该刻结束它;
② 窗口内 Lion **零 ABILITY**(不是自己打断自己);③ Lion 最近一次 `DEATH` 在 t=576.4(隔 294s);
④ 目标活着且 263u(没死、没出射程);⑤ `lionqdmg` **只在 CAND 腿 armed,本局 Lion 在 BASE 腿**。

**⇒ 执行被帧级证明:promoted 的默认在真实对局里在跑,不是死代码。**

**聚合只用于定位**(⛔ 不出判词):域内 8 条 residual 7/8 ≤0.4s;
域内 **0/8** 打满自然上限(**5.1s**,实测众数 15/97),域外 **19/64 (30%)** 打满;
Mann-Whitney 池化 z=−2.81 **p=0.0050**、BASE 腿单独 z=−1.77 **p=0.077**。
⛔ **该对照与结局共因**(域内 = 刚被打过 ⇒ 更可能被控被杀 ⇒ 引导机械变短),
**正是 2026-08-21 废掉 `span>=2.0s` 的同一条共因换了件衣服** ⇒ 只定位不判词。

**⛔ 本轮没买到的三样,逐条写明**:
(甲) 钉帧自身只省 **0.3s**(< 量具自己的 `MIN_CUT` 0.5);
(乙) 效应量 1.71s **< GH #86 §5 的 2.0s**,种子 **4 < 12** ⇒ **不按 §5 重新确立 (a)**
(不需要——(a) 是 09-09 那 36 个 episode 确立的);
(丙) 条件 (b) 归批测台,边界与 09-09 逐字相同。

**为什么是 WORKING 不是 INDETERMINATE**:INDETERMINATE(见 W85 `arbheart`)的用法是
**两个互斥机制同时支持同一结论且本台分不开**;这里不是 —— 判别子已把「不是 gate 干的」
逐条关掉,剩下的不确定**只在效应量,不在是否执行**。
⛔ 把「量不准效应量」写成「不知道它是否执行」是把两个问题混成一个。

## 5. 本轮开出的两条结构缺陷

1. **[harness] 一次 promote 消费了某条 owed 行索要的证据,而没有任何东西把两者连起来**(§1.1)。
   ⛔ **这是改写后的版本;初稿那条「判据在构造上永远读 OWED」已被本轮推送闸证伪并删除。**
   建议验收:promote 一个 id 时,检索 `owed_executions.json` 中
   `issue`/`ruling`/`executor` 任一字段点名该 id 的活行,同一工作单元内关闭或改写。
2. **[harness] `lion_drain_census.py --split-is` 缺 post-promote 这一档。**
   两个取值 `null-channel`(两腿 gate-OFF)/ `armed-vs-baseline`(cand 腿 gate-ON)
   **都不描述本轮语料**(两腿都 gate-ON)。默认档往读者眼前打印
   `BOTH legs gate-OFF; the cand/base split says nothing about this id` ——
   **后半句对,前半句自 2026-09-11 起是假的**。
   ⭐ 同目录 `arm_string_census.py` 文件头点名的 **#296/#297 家族**(量具把读者领到错结论),
   而且**发生在一个 promote 之后必然出现的语料上** ⇒ 每个 promoted id 都会踩。
   ⚠️ 顺带:`stamps.json` **全仓没有任何写入方**(`grep` 零命中),本轮由
   `games_manifest.jsonl` 现搭 —— 量具契约的第三个缺口。

## 6. 覆盖

**宽扫 12/12 有效镜像局**(`unparseable 0`,`SWEEP_EXIT=0` ×4)、**深查 1 局逐帧**
(281.5–283.5 全事件)+ 8 条域内 channel 的帧级判定 + 97 条 channel 的 span 普查。
⚠️ 12 局**高于**章程 6 局下限。**批测台连续第三十七轮零发波**,本轮语料取自现存 W69。

⛔ `bots/` + `game/` + `tests/` **一行未改**,零新 soak id,零 fixture。

## 7. 成本(RULING 48 三段)

**零 EC2 / 零 CE / S3 读取 29 个对象(出网未计价)**
= 16 个 `.analysis.json` + 12 个 `.dem`(dump 后即删)+ 1 个 dumper 二进制(cache HIT);
另 **4 次 `s3 ls`**。容器落盘 307 MB。⛔ 不写「零支出」。

## 8. 工具坑(本轮实测)

- ⭐ `routine_selfcheck.sh` 的**两道闸又各挡我一次**:`| tail -40` ⇒ 自报**第 7 次**复发;
  `timeout 400` ⇒ 自报**第 5 轮**复发。第三次 `nohup … &` 才跑起来。
  **W83/W84/W85 逐轮同形,本轮是第四轮连续复发** ⇒ 记为本组稳定损耗,
  ⚠️ 两道闸本身**工作正常**(它们正确地拒绝了,并打印了正确处方)——
  坑在**处方没有进入本组的开工动作**,而不是在闸。
- ⭐ **`lion_drain_census.py` 的 `--split-is` 默认档会打印一句对本语料为假的话**(§5.2)。
  本轮**没有采信它**,但它是「有输出且 exit 0」不等于「读数可引用」的又一例
  (与 W84 那条 `print` 被劫持同族:那条丢的是**数**,这条丢的是**数的含义**)。

## 9. 开工自检(从输出文件读,⛔ 不采信 harness 通知)

`legs run 13`、**`selfcheck worst exit: 3`**、
`FINDINGS (exit 3): cadence queue-rulings owed-executions lua-coverage`、
**`UNCERTIFIABLE (exit 2): trunk-red(python)`**
(⛔ 没跑成 ⇒ **本轮 trunk 的 python 那一侧没人看过**,不许写成「本轮没有 trunk red」)、
`NOT RUN: tests/test_lua_gate.py / tests/test_luacheck_gate_soakswitch.py /
tests/test_selfcheck_lua_leg.py`(⚠️ **连续第十五轮**)。
快 Lua 检测器腿:**133 个 tagged 文件,0 failures**(⚠️ 量具自述 **FAST SUBSET, not the full suite**)。
⚠️ `lua_gate.py` 自检段 **50 checks / 0 failures / 9 uncertified**
(那 9 条全因该腿在本容器上没能在 120s 内跑完;量具自己写明 ⛔ **这不是 pass**)。
⚠️ **本轮自检 ~1 小时**,与 W85(~21 分钟)/ W84(~40 分钟)**不可比** ——
本轮我的 12 局 dumper + 检测扫描同容器抢 CPU。

## 10. 三条腿 push 读数

(收尾回填)

## 11. token

(收尾回填)

## 12. 下一轮第一件事

0) ⭐⭐ **别再按「本组 owed 还剩 N 行」那张表直接取活 —— 先核那一行欠的东西是不是已经交付过。**
   本轮与 W85 **连续两轮**取到的 owed 行,其「欠着」的**理由**都已经过期
   (W85: BORN-DONE;本轮: **索要的证据早已交付,并已被一次 promote 消费掉**)。**两轮 2/2。**
   ⚠️ 注意措辞:本轮那一行**该做的事确实还没做**(产物不存在,读 OWED 是对的),
   过期的是它 `ruling` 里的**前提**,不是它的判据。
   ⇒ 取活动作应固定为:读 `done_when` → **先搜产物是否已存在于任意路径**
   (`grep -rn "VERIFY id=<id>" iterations/reports/`)→ 再决定做不做。
   ⛔ 这一条比「取哪一行」重要,请先落它。
1) ⭐ **本组 owed 真实剩余需要重数**:W84 报 7 行、W85 更正为 5 行、本轮结清 1 行
   ⇒ 名义剩 **3 行**,但按第 0 条,**这 3 行都还没被核过是否已交付**,
   ⛔ 不许把「3」当成工作量。
2) ⭐ **promoted id 的例行可见性是一个空缺**(§2):`liondrainstop` promote 后
   **六天里没有任何读数看过它**,而它掉出 arm 串是**构造性**的,不是疏忽。
   建议把「promoted default 抽查」做成本组的一个固定小工作单元。⛔ 需总监定编排。
3) 仍欠未动,原样继承 ⛔ 不许读成已结清:**`wkqdmg` 要局数不要深度**、
   `66.7%` vs `29.4%` 更宽复读、**换句柄英雄**(W80–W85 边界)、
   `pullcad` 收紧域(球在总监)、GH #849 验收口径(球在总监)、
   W84 §四「折叠 key 合同变成闸」(球在总监 [harness])。
4) ⏳ `.dem` 约 **2026-10-02** 到期(距今约 **15 天**)。
