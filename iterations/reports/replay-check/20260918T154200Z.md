# 录像检查组 2026-09-18T15:42Z(W97)— 条件 (a) 的**唯一计数器**把一句**引文**记成了判词,而那句引文正是「⛔ 不许拿 `verify > 0` 当促进依据」那一行**自己的验收句**

> ⭐⭐ **一句话**:批测台刹车第 **49** 轮、零发波 ⇒ 无未检新局;按章程第 2 条走补课支,
> 而补课支的**选人规则本身**(`verify_coverage.py`,「核验记录最少的 id」)本轮被实测为**坏的**。
> 上一轮(W96)为了审 `ownhalf_promote_bar_thickness` 这条欠条,把它的验收句
> **`VERIFY id=ownhalf verdict=WORKING`** 逐字抄进了报告表格;
> ⭐ **计数器把那句引文读成了一次新判决** ⇒ 今天它报 `ownhalf … verify=7 last_verdict=**WORKING** last_report=20260918T124101`,
> 而 **W96 自己的 §五逐字写着「VERIFY 行 0 条 ⛔ 不硬凑」**。
> ⛔ **被冒领的那个字符串,恰好是那条欠条 `done_when_note` 点名要的那一个**,
> 而同一行的裁定逐字写着 **「⛔ 不许拿 `verify > 0` 当促进依据」** —— **审这条欠条的动作,喂饱了这条欠条的验收句。**
> ⭐ 顺带一个**反方向**的实例:`abilanc` 今天读 `INDETERMINATE episodes=3`(09-11 的旧判),
> 把它盖掉的是 **09-15 的 `WORKING episodes=2`** —— 一条**后写的引文**把**先写的新判决**压回了旧的。
> 交 **[harness] issue**(⛔ 本组不改 harness,只交实例 + 形状 + 建议验收)。

---

## 〇、开工自检(铁律 10)

W96 交棒第 0 条(「当轮第一条命令 = 逐字那一行,零附加物」)本轮**仍然没做到**,如实登记:

1. 第一跑 `… | tail -40`,逐字 `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.`
   + `it has recurred 5x, every time as the first command of the round.` ⛔ **未当成通过**。
2. 第二跑改重定向但加了 `timeout 400`,逐字
   `REFUSED: routine_selfcheck.sh is running under \`timeout\`; exit 2, nothing checked.`
   + `This has recurred 3 rounds running.` ⛔ **未当成通过**。
3. 第三跑才用对:`nohup bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1 &`(PID 532),⛔ 无管道、⛔ 无 `timeout`。

⛔ **自检真码本轮不可得,是实证不是推断**:收尾回读 `/tmp/sc.log` **650 行**,停在
`=== trunk health (python test suite) ===`,`kill -0 532` 仍返回 0(进程活着)⇒ **无 `selfcheck worst exit` 行**,
⛔ 不写、不读成通过;**未跑完的腿这轮没人看过**。按铁律 7/11 ⛔ 不空转等它。
⚠️ **这是本组连续第三轮拿不到自检真码**(W95 / W96 / 本轮),形状完全相同 —— 归因不是工具,是
**处方要求它整轮在后台跑**,而它比一个工作单元还长。**GH #898 已立案的正是这一族**(并发把 push 闸染假红)。

已跑完的腿(逐字):
`push gate armed (core.hooksPath=.githooks); \`git push\` now runs the rule 6 static gate`;
`OK: no unlanded work in the certifiable window.`;
`uncovered set unchanged from the baseline -- OK`。

---

## 一、覆盖(章程固定节)

**宽扫 0/0 局;深查 0 局;VERIFY 行 0 条。**

- 批测台 `20260918T121242Z.md` 逐字「**刹车第四十九轮持有**」「**零发波、零收割欠、零泄漏**」
  ⇒ **无未检新局**,宽扫语料为空。⛔ **不硬凑 VERIFY 行**:本轮没有任何一帧被逐帧看过,
  因此本报告**不含帧证据**,这是**有意的** —— 本轮的问题是**计数器的算术**,不是游戏里的行为。
- 铁律 9 先读 `OWNER_PRIORITIES.md`:P1 / P2 球都在协同组 ⇒ 本组无未完成的 owner 优先项。
- 按章程「每次触发的工作流」第 2 条走**补课支**:挑「执行核验记录最少的 id」。
  ⭐ **而本轮的产出就发生在这一步** —— 决定「谁最少」的那把尺子,自己是坏的。

---

## 二、⭐⭐ 头号:`verify_coverage.py` 把**引文**计成**判词**,今天在语料里有 8 个实例

### 2.1 现场(裸读,⛔ 未经管道,`WITNESS_AT = 2026-09-18T15:42:00Z`)

```
$ python3 tools/agent/verify_coverage.py --all      # VC_EXIT=0
armed ids: 24   reports scanned: 271
ownhalf               7 WORKING               -   20260918T124101      10
```

⇒ 计数器说:`ownhalf` 最新判决是 **WORKING**,出自 **W96 那一轮**。

**而 W96 那一轮的报告里,`VERIFY id=ownhalf` 只出现一次**,在第 **78** 行,是一张审欠条的表的一格
(逐字 `sed -n '78p'`):

```
| `ownhalf_promote_bar_thickness` | `{'kind':'manual'}` | (1) `VERIFY id=ownhalf verdict=WORKING` 且 episodes 来自 ≥6 局,或 (2) `ownhalf` 退集 | 全仓 `ownhalf` 最好只到 `INDETERMINATE`;`ownhalf` **仍在** armed 串 | **真欠** |
```

**同一份报告的 §五逐字**:「**VERIFY 行 0 条** ⛔ **不硬凑** —— 本轮零新语料,没有任何一帧被逐帧看过。」

⇒ ⭐ **计数器给一轮自陈「零判决」的轮次记了一条判决,而且判词是 `WORKING`。**

### 2.2 ⭐⭐ 为什么这一格特别贵:被冒领的字符串是**那条欠条自己索要的那一个**

`iterations/owed_executions.json` 的 `ownhalf_promote_bar_thickness` 行,`done_when_note` 逐字:

> **裸读得出的验收句,二选一**:(1) 任一份 `iterations/reports/replay-check/` 报告为 `ownhalf` 打出
> `VERIFY id=ownhalf verdict=WORKING`,且**同一行/同一节写明 episodes 来自 ≥6 局**
> (⛔ 不是 ≥6 帧 —— 09-10 那次是 7 帧 1 局,正是本行要区分开的那个数)

同一行的 `ruling` 逐字:

> 条件 (a) 只有 **1 局 7 帧**,而录像组自己的报告逐字写着这**低于章程 ≥6 局下限**。
> ⛔ **不许拿 `verify > 0` 当促进依据**

⇒ **这条欠条存在的全部理由,就是挡住「一个 WORKING 字符串 + 一个大于零的计数」被当成条件 (a)。**
而**审它**这个动作,把那个字符串写进了语料,于是计数器现在**同时**给出了它禁止的两样东西:
`verify=7`(> 0)与 `last_verdict=WORKING`。
⭐ **闭环是实测的,不是修辞**:`done_when` 是 `{'kind':'manual'}` ⇒ 每轮必报 OWED ⇒ 每轮都有人来审
⇒ 每审一次,就往计数器里再喂一次同一句引文。**这个循环是自维持的。**

⚠️ **诚实边界,必须同时写**:`done_when` 是 `manual`,机器**不读** `verify_coverage.py`
⇒ ⛔ **本轮没有证明任何一次促进因此发生**,也没有证明那条欠条被错误退休过(它今天仍是 OWED)。
被证明的是**显示与选人**两件事:(甲) 周度台账的「本周完成条件 (a) 的 id 数」读这把尺子;
(乙) 章程第 2 条的**补课选人**读这把尺子(见 §2.5)。

### 2.3 ⛔ 顺带订正 W96 的一句话:「全仓 `ownhalf` 最好只到 `INDETERMINATE`」是**假的**

一条 grep 就推翻(逐字):

```
iterations/reports/replay-check/20260910T095406Z.md:14:VERIFY id=ownhalf verdict=WORKING episodes=7
```

⭐ **W96 的判词(`真欠`)是对的,理由是错的。** 正确的理由写在那条欠条自己身上:
WORKING 那一行**存在**,但它的 episodes 来自 **1 局**,而验收句要 **≥6 局**。
产出那一行的 `20260910T095406Z.md` 自己逐字登记了这件事(第 25 / 33 行):

```
**宽扫 0/0 局;深查 7 帧(1 局);每 id 触发计数表见 §3。**
⚠️ **深查 7 帧低于章程的「≥6 局」下限**(6 局 vs 本轮 1 局)。**不辩解,登记**:
```

⇒ 这正是那条欠条名字里的 **thickness**(促进门槛的**厚度**)。
⛔ **这条订正有牙**:下一轮若沿用 W96 的理由去复核,会先 grep 到那条 WORKING 行、发现「理由是假的」,
然后很容易**把行判成已满足** —— 而真正没满足的是**第二个合取项**(≥6 局)。
📌 与 `evidence-discipline` 的第四条同型:**一个对的结论配一个错的理由,下一轮会顺着错理由翻案。**

### 2.4 八个实例,按失效方向分三类(⛔ 逐条手读过,不是脚本说了算)

判别子(候选规则,**本轮用来产生名单,不是用来下结论**):同一行里出现**另一份报告的路径**
⇒ 引文;同一行里出现 `done_when` / `'kind'` / `验收句` ⇒ 验收句引文。

| # | 出处 | id / 判词 | 类型 |
|---|---|---|---|
| 1 | `20260917T214735Z.md:36` | `abilanc` INDETERMINATE 3 | 引用另一份报告 |
| 2 | `20260917T214735Z.md:37` | `lf_rescue` WORKING 115 | 引用另一份报告 |
| 3 | `20260917T214735Z.md:38` | `tpcommit` WORKING 1360 | 引用另一份报告(⚠️ 该 id 已不在 armed 串) |
| 4 | `20260917T214735Z.md:39` | `blinkflee` INDETERMINATE 36 | 引用另一份报告 |
| 5 | `20260917T214735Z.md:40` | `overchase` INDETERMINATE 501 | 引用另一份报告 |
| 6 | `20260918T005725Z.md:71` | `arbheart` INDETERMINATE 453 | 一行 `grep` 输出,逐字含源路径 |
| 7 | `20260918T034508Z.md:75` | `illureal` INDETERMINATE 52 | 验收句引文(自引 W92) |
| 8 | `20260918T124101Z.md:78` | `ownhalf` **WORKING**(无 episodes) | **验收句引文,判词与真值不同** |

**三类失效,代价递增**:

- **(甲) 只挪日期(5 例:2/3/4/5/6/7 中的判词相同者)** —— 判词没变,但 `last_report` 前进了
  **最多 6 天**。这把尺子的另一半语义是「这个 id 的核验有多旧」,于是**陈旧度被系统性低估**。
- **(乙) ⭐ 时间倒流(1 例:`abilanc`)** —— 09-17 的**引文**引的是 **09-11 的旧判**
  (`INDETERMINATE episodes=3`),而语料里**更新的真判决是 09-15 的 `WORKING episodes=2`**
  (`20260915T125314Z.md:125`)。⇒ **后写的引文把先写的新判决盖掉了**,
  今天 `abilanc` 读 INDETERMINATE,真值是 WORKING。**⛔ 这一类不是"多算一次",是读反。**
- **(丙) ⭐⭐ 凭空判词(1 例:`ownhalf`)** —— 见 §2.1/§2.2。**方向是「制造一次条件 (a) 的满足」。**

⭐⭐ **与工具自己修过的那个缺陷方向相反,这一点是承重的。** `verify_coverage.py` 的注释逐字记着
上一次修的是「**made verified ids read as unverified, i.e. the counter manufactured condition-(a) debt**」
—— 那次**制造欠债**(保守侧)。本轮这三类**制造满足**(激进侧),而条件 (a) 是铁律 2 的**促进前置**。

### 2.5 选人规则被实测改写:7/24 个 armed id 的读数变了

排序键是 `(verify 计数, narrat, id)` ⇒ **计数被引文抬高的 id 会离「最该补课」的队头更远**。

| id | 今天的计数器 | 手读订正 | 差别 |
|---|---|---|---|
| `ownhalf` | 7 / **WORKING** / `-` / 09-18T12:41 | 6 / **INDETERMINATE** / 7088 / 09-13T12:59 | 判词 + 计数 + 日期 |
| `abilanc` | 6 / **INDETERMINATE** / 3 / 09-17T21:47 | 5 / **WORKING** / 2 / 09-15T12:53 | **判词读反** |
| `illureal` | 5 / INDETERMINATE / 52 / 09-18T03:45 | 4 / 同 / 同 / 09-18T00:57 | 计数 + 日期 |
| `arbheart` | 4 / INDETERMINATE / 453 / 09-18T00:57 | 3 / 同 / 同 / 09-17T04:02 | 计数 + 日期 |
| `overchase` | 3 / INDETERMINATE / 501 / 09-17T21:47 | 2 / 同 / 同 / 09-13T09:55 | 计数 + 日期 |
| `blinkflee` | 2 / INDETERMINATE / 36 / 09-17T21:47 | **1** / 同 / 同 / 09-11T22:10 | 计数 + 日期 |
| `lf_rescue` | 2 / WORKING / 115 / 09-17T21:47 | **1** / 同 / 同 / 09-11T16:11 | 计数 + 日期 |

⇒ **队头(计数 = 1)从 4 个 id 变成 6 个**:今天是
`creepthink` / `fieldsip` / `fieldbuy` / `campvoid`;订正后**加上 `blinkflee` 与 `lf_rescue`**。
⭐ 这不是抽象的:`a_evidence_blinkflee` 与 `a_evidence_lf_rescue` **都是 RULING 13 开的、executor 写着本组的 owed 行**,
而它们在队列里被自己的**引用表**推离了队头。

### 2.6 第三条,独立且轻:`verdict=([A-Z]+)` 把 `NOT-ARMED` 截成 `NOT`

语料里 **12 行** `verdict=NOT-ARMED`(`skillstall` 9 行、`outcommit` 3 行)被记成判词 **`NOT`** ——
一个**不在章程词表** `{WORKING,BUGGY,SILENT,INDETERMINATE}` **里的 token**。
两个 id 今天都不在 armed 24 串里,所以**表上看不见** ⇒ ⛔ **低严重度,但它会在这两个 id 任何一天入集时现身**,
且**没有任何断言拦它**。一并交出去,不单开。

---

## 三、根因:判别子**存在**,而钉它的那条断言只钉了**省略号那一种写法**

`tests/test_verify_coverage.py:143` 的用例逐字:

```
chk("a row describing the format is not a verdict for any id", ...)
```

它的注释自陈这正是分界线(逐字):

> This is the case that separates "loose enough to see markup" from "loose enough to invent verdicts"

**而它喂进去的语料是省略号形式**(逐字 `next round write \`VERIFY id=theta verdict=…\` on its own line.`)。
⇒ ⭐ **分界线钉住了,但只钉住了它的一边**:一个**省略号**的元句子不匹配(对),
一个**具体**的引文(真 id + 真 CAPS 判词)**必然匹配**,而语料里今天就有 8 条。
📌 这与本组反复撞的那一族同型:**「我量到的是不是我以为的那个东西」** ——
这次量到的是**这一行在语料里出现过**,不是**这一轮做出过这个判决**。

⚠️ **诚实边界**:`^` 锚**不能**加回去 —— 工具注释逐字记着去掉它是因为
「**26 of 59** VERIFY lines (44.1%)」写在强调/代码跨度里。**修法必须在锚之外。**

---

## 四、交出去的棒

1. **[harness] 新 issue(见 §六)** —— §二/§三 全套:8 个实例、三类失效方向、
   建议的验收方式(一条**新的**断言用例:一份报告**引用另一份报告的具体 VERIFY 行**,
   断言它 (i) **不计数**、(ii) **不成为该 id 的 `last_verdict`**;外加 `NOT-ARMED` 那条)。
   ⛔ **本组不改 harness**,只交实例与形状(与 GH #879 / #880 同一条自律)。
2. **[总监]** `ownhalf_promote_bar_thickness` **仍是真欠**(判词与 W96 一致),
   但 **W96 给的理由是假的**(§2.3)。⇒ 复核时请用**第二个合取项(≥6 局)**,
   ⛔ 不要用「全仓没有 WORKING 行」—— 那句话一条 grep 就被推翻,而推翻它很容易被读成"已满足"。
3. **[总监]** ⭐ 一条**处方性**的:只要 `ownhalf_promote_bar_thickness` 保持 `kind: manual`
   (⇒ 每轮报 OWED ⇒ 每轮有人审 ⇒ 每审一次就往计数器喂一次引文),**§2.2 那个循环就是自维持的**。
   这与 GH #886 是**同一行代码的第三个后果**(前两个:满足了清不掉、比立案还老的存档看不见)。
4. **[本组,下一轮]** ⏳ W46 `.dem` 约 **09-25** 到期(**还剩 7 天**),取法**问 `dem21/` 不问 `soak/`**。
   ⚠️ 连续第 **4** 轮原样交下去 —— 再拖两轮就只剩"来不及"这一个结局。

---

## 五、⛔ 本轮明确没做

- `bots/` + `game/` **一行未改**;零 soak id、零 fixture;⛔ 未改 `tools/agent/verify_coverage.py`
  或任何 harness 文件;⛔ 未改 `owed_executions.json` / `test_set.md` / `queue.json` / `state.json`(编排权在总监)。
- ⛔ **未替总监退休任何一行**;⛔ 未拉任何语料;⛔ 零 AWS 调用(`session_setup.sh` 都没跑)。
- **VERIFY 行 0 条** ⛔ 不硬凑 —— 本轮零新语料,**本报告不含帧证据**(理由见 §一)。
- ⛔ **没有证明**任何促进/退休因本缺陷发生过(§2.2 的诚实边界)。
- ⛔ §2.4 的候选判别子**只用来生成名单**;八条都**手读过**,结论建在那八行上,不建在规则上。

---

## 六、成本三段(铁律 1 / RULING 48)

**零 EC2 / 零 CE / S3 读取 0 个对象** —— 一次 AWS 调用都没有。

---

## 七、⭐⭐ 本报告**自己**把 `ownhalf` 从 7 推到 9 —— 实测,不是预言

⛔ **不掩盖、不回避引文**:evidence-discipline 与本组章程都要求**逐字整行引**(W89 那条「别手挑字段」的学费)。
于是**写这份报告这个动作,本身就是缺陷的第三次触发**,而这正好把 §2.2 那个自维持循环**演示**出来。

**裸读(⛔ 未经管道),同一棵树,唯一差别是本文件存在):**

| # | 条件 | 命令 | `ownhalf` 那一行 |
|---|---|---|---|
| 1 | 本报告**写出之前** | `python3 tools/agent/verify_coverage.py --all` | `ownhalf  7 WORKING  -  20260918T124101` |
| 2 | 本报告**写出之后** | 同上 | `ownhalf  **9** WORKING  **7**  **20260918T154200**` |

`reports scanned: 271 → 272`。本文件贡献的两条**互不相同**的元组(同一份报告内按设计去重,
**不同元组不去重**):`(ownhalf, WORKING, None)`(§2.1/§2.2 的验收句引文)与
`(ownhalf, WORKING, 7)`(§2.3 那行 `grep` 输出)。

⭐⭐ **第 2 行比第 1 行更坏,而这一点必须被读到**:W96 留下的那格**没有 episodes**(列里是 `-`),
一个警觉的读者也许会因此起疑;本轮的引文**带着 `episodes=7`** ⇒
计数器现在显示的是一条**格式完整、看起来毫无破绽**的 `ownhalf … WORKING … 7`。
⛔ 而那个 `7` 是 **7 帧(1 局)**,正是那条欠条 `done_when_note` 逐字要求**区分开**的数
(「⛔ 不是 ≥6 帧 —— 09-10 那次是 7 帧 1 局」)。

⇒ **给 [harness] 的验收方式因此可以写得更硬**:修好之后,在**本文件存在**的树上重跑,
`ownhalf` 必须读 **`6 / INDETERMINATE / 7088 / 20260913T125915`**,⛔ 不是 9、⛔ 不是 WORKING。
**本文件是一份天然的回归语料**,不需要造 fixture。

⚠️ **诚实边界**:本组**不**因此改写自己的报告去躲开正则(那会把证据删掉,且下一轮的引用会失效);
也**不**动 harness。两条都是编排权在总监的动作。

---

## 八、issue / push / token

- **issue 净增 1、评论 0**:**GH #903 [harness]** 新开(§二/§三/§七 全套 + 建议验收方式)。
  ⛔ **按 GH #290 在两次 push 之后发**,发前跑 `bash tools/agent/claim_precheck.sh <草稿文件>`。
  ⛔ **未给 `verify_coverage.py` 之外的任何东西开单**;⛔ 未追评 GH #886(两件事:#886 是
  `kind: manual` 恒 OWED,本轮是**计数器读引文**;但 §四 第 3 点把两者的连带交给了总监)。
- **⚠️ 本轮杀掉了后台自检再 push**,理由是 **GH #898**(自检与 push 闸并发 ⇒ python 闸假红
  ⇒ 闸的提示语会让人给别的组发错帐单)。**代价已在 §〇 登记:未跑完的腿这轮没人看过。**
- **push 读数:先分支后 main(RULING 69),⛔ 未用 `RULE6_BYPASS`。**
  - **① 分支推** `PUSH1_EXIT=0`:`GATE_EXIT=0 CLEAN` / `py gate: 135 ran, 0 findings, 0 uncertifiable, 47.7s` /
    `lua gate: SKIPPED BY SCOPE -- this push touches no bots/game/tests path.`
  - **② main 推(第一次)`PUSH2_EXIT=1`** —— ⛔ **不是闸拒绝**:钩子逐字
    `RULE6_MEMO=REUSE  a green three-leg reading taken 2026-09-18T15:48:13Z on THIS EXACT tree.`(按设计命中),
    拒绝来自远端:`error: RPC failed; HTTP 403` + `! [rejected] HEAD -> main (fetch first)`
    —— **别的组在这中间推了 main**(`a89aa6c7..a77c7f9d`)。
    `git pull --rebase origin main` 后重推 **`PUSH2_EXIT=0`**(`a77c7f9d..93d711e7 HEAD -> main`,
    `py gate: 136 ran, 0 findings, 48.1s` / `lua gate: SKIPPED BY SCOPE`)。
  - **③ 分支 ref 同步** `PUSH3_EXIT=0`(`--force-with-lease`,本会话独占分支,W75 的处置照做)。
    ⚠️ **⭐ 这一推付了 RULING 69 那笔钱,实测**:rebase 之后分支 ref 落在 main 之后 ⇒
    `lua gate: scope = 4 path(s) changed vs the pushed refs' base (base=stdin-remote)` ⇒
    **`lua gate: 430 ran, 0 findings, 0 uncertifiable, 9 unanswered, 5 known-red, 648.0s`**
    —— **同一棵 markdown-only 的树,前两推都读 `SKIPPED BY SCOPE`,这一推跑了全集 648 秒**。
    ⛔ **归因不是推的顺序**(顺序照 RULING 69 走了):是**别的组插队改了 main ⇒ 我方 rebase ⇒
    分支 ref 必须被强推一次**,而那一次的 base 是远端分支的旧值。
    📌 章程 RULING 69 那一节记的是「反序会让第二推恒定跑全集」;**本轮记的是它的第三种触发路径**
    (顺序正确、但中途被别人的 main 推迫使 rebase)—— 交给总监,⛔ 本轮不改任何规则文字。
  - **远端权威读数(`git ls-remote`,⛔ 不看本地缓存)**:
    分支 = `93d711e7`;main = `198d02e2`(⭐ **比我方 commit 更新** —— main 在我推完之后又被别的组推进了,
    `git merge-base --is-ancestor 93d711e7 origin/main` = **yes**,本轮产物确实在 main 上)。
  - ⚠️ **stop-hook 在 ③ 在飞时喊「3 个未推送 commit」** —— 按 W90 那条**没有就地提交**,等这一推落地;
    实读是**假警报**:那 3 个 commit(本轮 1 + rebase 带进来的别组 2)**当时已全部在 `origin/main` 上**,
    落后的只有分支 ref 自己。**判别子仍是 `git ls-remote`,不是 `git status`(W73)。**
- **token 用量**:`TOKENS total_in=5,890,624 out=39,497 turns=45`(⚠️ 到统计时刻为止,收尾回合不计入)。
