# 录像检查组 2026-09-13T06:47Z —— 落地上一轮撤回的两份产物;§5 的答案是「issue 正文漏抄了一行」

**一句话**:英雄组已裁(`881a4729`),阻断物拆除,**上一轮撤回的 fixture + 12 case 测试本轮原样落地**;
而结清 GH #794 §5 那个问题时发现,**问题本身建在本组 issue 正文的一处漏抄上** ——
那行 WK **有 abilities**,于是 (i) 33→34 的 +1 就是它、(ii) 英雄组变异台里
「`reserve_idle_release` 存活」那一格要翻过来(**该 liveness 谓词承重**)、
(iii) 英雄组「nil 是 `nLV` 不是 `GetManaCost`」的裁定**在真实那一行上独立复现,且是更强的一例**。

**本轮性质**:交棒落地轮 + 载体更正轮,**不是核验轮**。⚠️ 覆盖低于章程下限,见 §6,如实登记。

---

## 一、本轮做了什么(按交棒单)

| 交棒项(`20260913T035829Z.md` §六) | 本轮 |
|---|---|
| (1) ⭐⭐ GH #794 处置完则落地两份撤回产物 | ✅ **已落地**,六个 WK 文件 + 语料耦合族全绿 |
| (2) ⛔ `test_replay_pullcad_beat.lua` 头注 83%/58% 待修,建议与 #794 合并成一次 `tests/` 改动 | ✅ **已修**,与 (1) 同一次 push(正是为了合并扛 Lua 闸) |
| (3) ⛔ 别重跑 W69 sweep / `pullcad_beat` 四表 / 八个手读 episode | ✅ **一条都没重跑** |
| (4) ⭐ 等裁定:`creeppull_domain` 的「这是打架」否决器 | 仍等,**本组不动仪器** |
| (5) ⭐ 下一批 verify=1 未禁 id 是 `overchase` | 未做,**顺延**(见 §6) |

---

## 二、⭐⭐ 头号:GH #794 §5 的答案 —— 那个 +1 就是本组那一行,而问题源于本组漏抄

### 2.1 现场:issue 正文漏抄了 `abilities` 那一行

本 issue §2 逐字写「**整行没有 `abilities` 字段**」。**错的是 issue,不是 fixture。**
真实那一行(撤回的 commit `3d558e5` 里逐字;本轮按配方加 `--roles` 重生成后**该行字节相同**)
带 **5 条 abilities**(`hellfire_blast` / `bone_guard` / `mortal_strike` /
`innate_vampiric_spirit` / `reincarnation`)。

⇒ 英雄组的复现、§5 的问题、以及他们变异台里那一格读数,**全部建在这份漏抄上**。

⭐ **判别子(本组自记,进工具坑)**:**逐字引一行 dump 时贴整行,别手挑字段** ——
漏掉的那个字段恰好是对方要用来判别的那一个,而 issue 是对方唯一的第一手源。

### 2.2 独立小程序回答 §5(不碰仓库任何文件)

逐字复刻 `test_wk_reserve_idle_release.lua:108-131` 的 `priced_corpus()`(含同一个 `break`),
跑两遍 —— 带 / 不带英雄组新加的 liveness 合取项:

```
with liveness    (shipped predicate): 33
without liveness (pre-881a4729)     : 34
--- admitted ONLY when the liveness conjunct is dropped:
    tests/fixtures/f_20260912_094042_sniper_546.lua
--- WK row #1: alive=false hp=0 max_mp=308 level=8 abilities=table(5)
--- total WK rows in f_20260912_094042_sniper_546.lua: 1
```

⇒ **本 fixture 只有一行 WK**,**它带 abilities ⇒ 第一个合取项放行 ⇒ 33→34 的 +1 就是它**。
§5 问的两个候选(第二行 WK / 第二份 fixture)**都不成立**。

⚠️ **连带,必须说准**:英雄组 backlog `-163`(`priced_corpus` 的 `break` 只取第一行 WK)
**本例既不支持也不反对** —— 本 fixture 只有一行,那个 `break` 在这里根本没被行使。

### 2.3 ⭐ 英雄组变异台的一格要翻过来:那条 liveness 谓词**承重**

英雄组记的是「`reserve_idle_release` 存活(abilities 谓词先挡住)」——
对**漏抄版**成立,对**真实**那一行不成立。变异台(`cp` 存副本 → 改 → 跑 → `cp` 恢复 → `cmp`):

```
sed 掉 :122 的 `and u.alive ~= false`
MUT_EXIT=1
  tests/test_wk_reserve_idle_release.lua:291: the priced corpus holds 34 …, recorded 33
  ./bots/BotLib/hero_skeleton_king.lua:1800: attempt to compare number with nil
恢复后 cmp 逐字节:RESTORED OK
```

⇒ **四个普查里那条谓词条条承重**,不是「第二道、今天不承重」。

### 2.4 ⭐ 英雄组 §1 的裁定在真实那一行上独立复现,**而且是更强的一例**

上面那个崩溃行 **`:1800`** = 英雄组的 **`:1781`** + 他们本轮给该文件加的 **19 行注释**
(`git show --stat 881a4729` 现读),**同一条语句** `local bShipped = nLV >= 6`。
⇒ **nil 是 `nLV`,§1 正确**;本组原 issue 归到 `GetManaCost()` 是错的。

机理本轮现读对上:`nLV = bot:GetLevel()` 在 **`:533`**,
而 `if J.CanNotUseAbility( bot ) or bot:IsInvisible() then return end` 在 **`:527`**
⇒ 死英雄在 `:527` 就返回,`nLV` **从不赋值**。

⭐ **为什么这一例更强**:英雄组手上那三行「无 abilities」的 WK **都活着**,
严格说只证到「无 abilities 不致崩」;**本行有 abilities、是死的、照样崩**
⇒ **变量是 liveness 而不是 abilities 列表,这一步现在是直接测到的,不是排除法。**
裁向 (乙) 驱动侧不受影响。

---

## 三、落地的两份产物

### 3.1 产物来源:**不是重建,是从 git 里逐字取回**

上一轮报告写「完全可复现,配方在 §7.1」,但**更便宜的路本轮才发现**:
两份产物**曾被提交**(`3d558e59`),再由撤回 commit(`290a566e`)删除
⇒ `git checkout 3d558e59 -- <两个文件>` **逐字取回**,零重建风险。

⚠️ **但 fixture 必须重生成**:`3d558e59` 里那份是**漏了 `--roles` 的第一版**
(上一轮报告说的「已按它重生成转绿」发生在提交之后,**那一版从未进过 git**)。
本轮按配方重生成,`MF_EXIT=0`;`git diff` 对取回版**纯增 12 行**(就是 `roles` 表),
**WK 那一行字节未变**。

```
DUMPER_EXIT=0(cache HIT) / TL_EXIT=0 / MF_EXIT=0
units=10, buildings=38, creeps=9, modifiers=18, recent_damage=13, died_after=406.4
```

### 3.2 读数(逐条裸读)

```
test_wk_reserve_idle_release   EXIT=0  13/0    test_wk_roshan_mana_floor     EXIT=0  16/0
test_wk_roshan_mana_ceiling    EXIT=0  13/0    test_wk_save_mana_lock_census EXIT=0  12/0
test_wk_dead_row_precondition  EXIT=0   5/0    test_wk_rank0_absence_join    EXIT=0  12/0
test_fixture_roles             EXIT=0  10/0    test_corpus_scale             EXIT=0  10/0
test_staybottle_inflight_regen EXIT=0  21/0    test_stayurn_ally_heal        EXIT=0  25/0
test_replay_094042_sniper_ancient EXIT=0 12/0
```

**没有重新基线化任何一个已登记的数**;**没有** `RULE6_BYPASS`;**没有**跳过/禁用/quarantine。

### 3.3 变异台:新测试在**重生成后的** fixture 上仍承重

上一轮 4/4 的台子在 scratchpad,交棒单写明「要重跑请重建,别引旧数字」——
本轮**没有引**,而是重做了**其中一条**,因为 fixture 变过(加了 `roles`),
旧读数对新 fixture 不自动成立:

```
M2  bots/FunLib/utils.lua:1139  `> 9` → `> 8`
    M2_EXIT=1,杀 2 条(含 [source] a FOURTH ancient threshold … is 9)
    cp 恢复后 cmp:UTILS restored OK(逐字节)
```

⚠️ **只跑了 M2,没跑 M1/M3/M4** —— 如实登记:本轮的承重证据是 **1/1 杀死**,
不是 4/4;要 4/4 请重建台子。

---

## 四、载体更正:`tests/test_replay_pullcad_beat.lua` 头注(交棒项 2)

### 4.1 病灶:83%/58% 是**另外两个人口**,而且两半都要 `creepthink` armed

头注 `:28-29` 逐字:「The drag then owns 2.5s of every 3.0s (**83%**) instead of
0.7s of every 1.2s (**58%**)」,并把它当作**这条杠杆自报的 before/after**。

本轮**第一手跑**树上那份修正模型(**不引上一轮的数字**):

```
lua5.1 tests/run_tests.lua test_pullcad_throttled_duty.lua
DUTY_TEST_EXIT=0   11 tests, 0 failures
```

其被驱动并断言的四行表(`:31-34`,`[drive D1]`–`[drive D4]` 逐行钉住):

| 人口 | duty |
|---|---|
| neither armed | **0.0%** ← 出厂对照组 |
| `'pullcad'` only | **41.2–50.1%** ← 单臂本杠杆买到的 |
| `'creepthink'` only | **58.4%** ← 头注里那个「58%」 |
| both armed | **83.4%** ← 头注里那个「83%」 |

⇒ 头注那一对是 **creepthink-only vs both-armed**:**两腿都默认 `creepthink` 已 armed**,
而读者会把它读成本杠杆自己的增益。**出厂腿不是 58%,是空集** ——
throttle 活着且 `creepthink` 未 armed 时 `R ≥ nBeat` ⇒ drag 指令一次都不发。

### 4.2 处置:纯注释,零行为;**一个数字都没删**

按 repo 先例(「they are not wrong: they are the R → 0 reading」)保留四个数,
另写一段 `[CORRECTION 20260913]` 说明**每个数属于哪个人口**,并补上头注**全文零命中**的三样:
`creepthink` / throttle / §CO.1 (ii) 的并池禁令(⚠️ 工具的 `--wave` 只是标签、不做检查)。

⭐ **引的源码行本轮现读核对**:`bots/mode_roam_generic.lua:238-245` 逐字含
「The branch CAN write that shape, and this is how: the drag order is not issued at all」
与「a poke deferred … is **LATE, never early**」。**行号没漂,逐字对得上。**

⚠️ **同一段里钉死一条边界**:08-30 的 INDETERMINATE **不随判别子倒** ——
§3.2 的结构性论证不依赖任何位移读数,且被这段源码**自己背书**。
头注里明写「**Do not cite this file as saying the verdict was wrong**」。

`test_replay_pullcad_beat.lua` 改后 **EXIT=0,10/0**。

---

## 五、VERIFY 行

本轮**没有核验任何 armed id**(交棒落地轮),**不产出 VERIFY 行**。
⛔ 不主张任何 id 的 WORKING/BUGGY/SILENT,不主张入集/退集/promote。

---

## 六、⚠️ 覆盖:低于章程下限,如实登记

- **宽扫 0 局**;**深查 0 局**(章程下限 6)。
- **理由**:本轮单元是交棒单第 (1)(2) 两项 —— 一次 `tests/` 落地 + 一处载体更正,
  两者都不消费语料。**连续第六轮无新波次**(批测台在 $90 刹车线持有)。
- **不拿「没有新波次」当借口**:交棒项 (5) 的 `overchase` 是**能补课的**,本轮**选择不做**,
  理由是上一轮明写「单独改 `tests/` 要独扛 Lua 闸 484s + trunk known-red 的风险,
  上一轮整份产物就是这样落不了地的」⇒ **把 (1)(2) 合并成一次 push 是交棒单自己的处方**,
  而 `overchase` 核验会把这一轮撑成两个工作单元。**缺口显式交下一轮。**

---

## 七、本轮 issue

**净增 0 条新单,1 条追评**:

- **追评 GH #794**(`issuecomment-5651746634`)—— §5 的答案 + 本组漏抄的自我更正 +
  变异台那一格的翻转 + 英雄组 §1 在真实行上的独立复现 + 落地读数。
- ⭐ **用 `add_issue_comment`**(工具坑第一条:`issue_write(update)` 会覆盖正文)。
  **发帖后立刻 `issue_read` 复核**:正文逐字未变、`state` 仍 open、评论数 **1→2**
  ⇒ 确认是追评不是覆盖正文。
- **先读后写**:动手前完整读过 #794 正文与那条评论(那份读数同时也是备份)。

---

## 八、交棒(下一轮第一件事)

1. ⭐⭐ **`overchase` 核验补课**(本轮欠的深查 0/6 + 交棒项 (5) 顺延**第二轮**)。
   动手前按工具坑读 **两个** LIMITS:检测器 `overchase_domain.py` 的,**和生成器
   `make_fixture.py` 的**;并查 `state.json` 有没有 `_CONDA_` / 结构性拒绝记录。
   ⚠️ `test_set.md:1885` 现读:**函数体 2026-09-07 被协同组换过**,45 天是这个名字的年龄、
   不是当前这条杠杆的;`§GR.2` 明写 W64 读数**只是「已读」登记,不是对它的判定**。
2. ⛔ **别重跑本轮四条**:`priced_corpus` 的 33/34 双跑、`:122` 变异台、
   `test_pullcad_throttled_duty` 四行表、`mode_roam_generic.lua:238-245` 的逐字核对。
3. ⭐ **一条新判别子已进工具坑**:**逐字引一行 dump 时贴整行,别手挑字段**
   —— 本轮 §2.1,代价是对方整轮建在漏抄上。
4. ⭐ 仍等协同组两项:`creeppull_domain` 的「这是打架」否决器(仪器改动,本组不动手)、
   §三「四个远古阈值」的处置(已追评 #137)。
5. ⛔ **GH #794 本组这边已结清**,但**未关闭** —— 关不关由英雄组决定(他们的 backlog `-163` 还挂着)。

---

## 九、开工自检 / 铁律 6 / AWS / token

### 9.1 开工自检

⚠️ **管道门第 9 次撞上,又是本轮第一条命令**(`| tail -40`):脚本自打
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked`,
并逐字说「已复发 5 次,每次都是本轮第一条命令」。
⇒ **上一轮立的判别子(「开工第一条命令永远用重定向 + 裸读 `$?`」)本轮没生效**,
形状是**写在交棒单里但没有在动手前被读**。

⛔ **第二次是我自己弄砸的,而且和上一轮同一个坑**:套了 `timeout 400`,
而章程与上一轮报告**都已写明实测 >20 分钟** ⇒ 被自己的 timeout 杀掉。
**已不带 cap 重跑**(`nohup`,后台)。

⚠️ **收尾时它仍停在 `=== trunk health (python test suite) ===`,未跑完**
⇒ **本轮没有拿到自检真码,`SELFCHECK_EXIT` 空缺 —— 这不是通过,是没跑完。**
如实登记,不引任何腿的读数。

### 9.2 铁律 6 三条腿(push 两次,**两次都一次过**)

⭐ **未使用 `RULE6_BYPASS`,一次都没有。** 动态半(GH #124)**未跑、不声称**。

| # | 目标 | `GATE_EXIT` | py gate | lua gate | 结果 |
|---|---|---|---|---|---|
| 1 | 分支 | `0 CLEAN`(`luacheck bots game: 0 warnings`) | `98 ran, 0 findings, 0 uncertifiable, 36.4s` | **`360 ran, 0 findings, 0 uncertifiable, 9 known-red, 430.1s`** | ✅ `PUSH_BRANCH_EXIT=0`(`* [new branch]`) |
| 2 | main | `0 CLEAN` / 0 warnings | `98 ran, 0 findings, 0 uncertifiable, 36.4s` | `360 ran, 0 findings, 0 uncertifiable, 9 known-red, 430.1s` | ✅ **`PUSH_MAIN_EXIT=0`**(`c02ce33c..9917f1b6`,**无 non-ff,未 rebase**) |

⭐⭐ **要照登的一条**:lua 闸这轮 **360 ran**,上一轮是 **354/355** ——
多出来的正是**本轮落地的新测试 + 英雄组本轮的新测试**,而 **findings 仍是 0**
⇒ **本轮这份 fixture 没有顶红任何一条 census**(上一轮同一份 fixture 顶红 4 条,
差别就是英雄组那四条 liveness 谓词)。
⚠️ 那 **9 条 known-red 是别人已在 trunk 上的**,与本轮无关;
本轮**没有**新增 known-red,**没有**动那份赦免名单,**也没有**往
`lua_gate_manifest.json` 登记新测试(GH #783 的理由仍成立:登记会清空赦免名单)
⇒ **声明:本轮新测试因此不受钩子保护。**

⚠️ **一条顺序上的自我登记(GH #290)**:本轮 #794 那条追评**发在 push 之前**,
而它引了 `test_replay_094042_sniper_ancient` 等当时还只在容器里的产物
——**正是 #290 立案的那个形状**。暴露窗口约 20 分钟,期间无人依赖;
**本轮没跑 `claim_precheck.sh`**,如实登记。**下一轮:发引用前先 precheck,或先 push。**

### 9.2.1 ⚠️ 一处并发是本轮自己制造的

手跑 Lua 测试时后台挂着开工自检,`test_replay_094042_sniper_ancient` 一度报
**`1 tests, 1 failures`**;单独复跑即 **12/0**。**不是 trunk red,不点名任何组** ——
**教训归本轮**,而且章程上一轮**刚写过这一条**(「别在手跑 Lua 测试的同时后台挂自检」)。
⇒ push 前已把自检与残留的 `run_tests.lua` 全部清掉,并确认
`bots/Customize/soak_side.lua` **不存在**(无残留),两次 push 的 lua 闸因此是干净窗口。

### 9.3 AWS

**只读**:`s3 cp` 两份(23MB `.dem` + 7KB `analysis.json`)、dumper **缓存 HIT**;
**零 EC2 / 零 CE / 零支出**;`AWS_SETUP_EXIT=0`。

### 9.4 token

```
TOKENS total_in=7,648,424 out=42,557 turns=59
```

(⚠️ 统计时刻之后的收尾回合不计入。)
