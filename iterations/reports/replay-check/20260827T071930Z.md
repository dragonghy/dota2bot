# 录像检查组 2026-08-27T07:19:30Z(第八十二次触发)

**一句话**:`odaoe` 首检 —— **条件 (a) BOUGHT,判 `WORKING`**(armed 腿 **10 次 ARMED-ONLY 施法 / 7 局**,
baseline 腿 **1 次 / 1 局**),这是它入集六轮后**第一次拿到有 OD 的语料**;
而本轮真正的方法学产出是**本组自己踩中了章程写在纸上的那条坑**:
「谓词在施法前最后一帧上评估」这条规矩我用在了位置上、**没用在生死上**,
于是 **61 次 armed 施法里 30 次**被读成「施程内 0 个敌人」。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` → **worst exit 3**(非阻断)。

| 腿 | 读数 |
|---|---|
| 推送门上膛 | `core.hooksPath=.githooks` 已设 |
| unlanded work | **OK**(443 refs,407 因浅克隆 REFUSED = 不可判,不是干净) |
| 报告节奏 | 1 处洞:`strategy` 08-26T10:34Z→14:23Z **3.8h** —— **不是本组的**,登记交棒 |
| 未裁队列请求 | RIDESHARE none / OTHER none |
| stable 锚点 | `stable-v1`(6db5921c)/ `stable-v2`(136a332f)两个 **EXISTS/PINNED/SHIPPED 全 ok** |
| trunk python | **41 passed / 0 failed**(上一轮那条 `test_selfcheck_lua_leg.py` 已绿) |
| 快 Lua 检测器 | **11 文件 / 0 失败** |

## 1. 选点:为什么是 `odaoe`

上一轮(04:05Z)把「每个 armed id 在本组报告里的篇数」做成了固定输出,
37 个 armed id 里 **`odaoe` 与 `campsel` 并列 2 篇、都从未被裁定过**;
`campsel` 上一轮已裁(`REFUSE`),`odaoe` 是剩下的那一个。
它一直没被裁的原因写在 hero 组连续六轮的报告里,逐字都是「**已获批入集但无语料**」——
因为它要 obsidian_destroyer 进 draft。**本轮先量分母。**

## 2. 分母:不是零,但形状特殊

W15/W15-R(`79f32c92`,7 个 run,4 种子)**248 局计分局**(288 个 `analysis.json` − 40 局暖场)里:

| 种子 | 阵容含 OD? | 局数 |
|---|---|---:|
| 888 / 895 / 906 | 否 | — |
| **896** | **是** | **68** |

- OD 在种子 896 里 **恒为 dire(68/68)**,与「同种子内英雄→队伍是死的」一致;
  **位置每局变**(slot 0/1/2/3/4 全出现过,众数 slot1),与 #57 一致。
- stamp 语义从**一手来源**读:`tools/batch_test/soak/mirror_ab.sh:77-79` 逐字
  `RS="mirror:$CAND:s$SEED:radiant"` + `WAVE 1: fix=$CAND on RADIANT`
  ⇒ **stamp 的那一侧就是 armed 侧**(不是照抄别人的散文)。
- ⇒ stamp=dire 的 **22 局**里 OD 在 **armed** 腿;stamp=radiant 的 **46 局**里 OD 在 **baseline** 腿。

### 2.1 ⭐ 锁英雄的 id 在 mirror 里**没有 ab/ba 两层**(登记在案)

铁律 4(i) 要求「任何 armed/baseline 对比必须同时给 ab / ba 两个分层」。
对 `campsel` 那种**每局每个英雄都过**的 id 这是可做的;对 `odaoe` 这种**锁在一个英雄身上**的 id,
swap 把 armed 腿挪到了**OD 不在的那一侧**,于是两条腿的 OD **都站在同一个物理侧(dire)**。

- **好消息**:铁律 4(i) 要防的侧别混淆在这个 id 上**结构性不存在** ——
  armed 与 baseline 的 OD 是同一个英雄、同一个物理侧、同一份 draft。
- **代价**:**没有第二层可以对拍**,4(i) 的「两层反号 = 噪声」这道保险在这里用不上。
- **替代分层用 run**:`7b6b1f` 一个 run 内两条腿都有(armed 18 局 / baseline 24 局),
  读数在该 run 内独立复现(§4)。

**这一条对所有锁英雄的 gated id 都成立**(`liondrain`、`zusult*`、`cmr*`、`esaftershock`…),
不是 `odaoe` 独有;登记在本报告,供总监决定要不要写进铁律 4 的注脚。

### 2.2 20 局 `.dem` 是 404

68 局里 **20 局**在 `dem21/<run>/` 下取不到(`analysis.json` 在、`.dem` 不在)。
实测语料 **48 局**(armed 18 / baseline 30)。**不是本组能修的**,也**不新开 issue**
(与批测台 08-26 报告里 215 `.dem` vs 248 计分局的差同源);写在这里作为分母的诚实标注。

## 3. 观测量:这个门**严格追加**,所以它可以被直接读

`X.od_GetEclipseAoeLocation` 坐在出厂单体循环**下面**(源码级棘轮已钉,见 §7),
头注自己也写着 armed **只能把 NONE 变成一次施法,永远不能改向**。于是每一次
Sanity's Eclipse 施法都可以在**施法前最后一帧**上裁成三类:

| 桶 | 判据 |
|---|---|
| **ARMED-ONLY** | 施程 700 内**没有任何敌人**能被出厂 `CanKillTarget` 单杀,且 **≥2 个** worth-hitting 敌人可被同一个 500(/525/550)半径圆覆盖 ⇒ **出厂那条循环没有出口**,只可能是门放的 |
| shipped_explains | 有人可被单杀(含 §5 两道收紧闸) |
| unexplained | 两条都不成立(§6) |

**baseline 腿出现 ARMED-ONLY 按构造是不可能的** ⇒ 它就是本工具的**假阳率**,和读数一起报。

## 4. 读数(48 局,136 次施法)

| 腿 | 局 | 总施法 | 施法/局 | **ARMED-ONLY** | /局 | shipped | unexplained |
|---|---:|---:|---:|---:|---:|---:|---:|
| **armed** | 18 | 61 | **3.39** | **10** | **0.56** | 41 | 10 |
| **baseline** | 30 | 75 | **2.50** | **1** | **0.03** | 60 | 14 |

- 有 ≥1 次 ARMED-ONLY 的局:**armed 7/18**,**baseline 1/30**。
- **run 内复现**(`7b6b1f` 单个 run,同实例同种子):armed **0.61/局** vs baseline **0.08/局**。
  (`4f267b` 只有 baseline 腿的 6 局,0 次。)
- 总施法量 armed 3.39/局 > baseline 2.50/局,**方向与「严格追加」一致**;
  差 0.89/局,与 ARMED-ONLY 的超出 0.53/局 同量级。
- 与 hero 组**预注册的域**(2026-08-19 的 19 局语料:10 episode / 7 局 ≈ 1.1 ep/局,
  后来引作 0.76 ep/局)**同一量级**。这里数的是**真实施法**不是差异帧,
  施法还要过 140s cd 与 mode,所以 0.56 < 1.1 是自洽的。

**⇒ 条件 (a) BOUGHT,`odaoe` = `WORKING`。**

### 4.1 strict 读数并列(收紧前后)

| 腿 | ARMED-ONLY(收紧后,报表用) | strict(仅施法前帧 HP) |
|---|---:|---:|
| armed | **10**(0.56/局) | 17(0.94/局) |
| baseline | **1**(0.03/局) | 5(0.17/局) |

## 5. 承重帧(逐帧;深查 10 局)

**深查覆盖**:10 次 ARMED-ONLY 分布在 **7 局**、baseline 1 枚 1 局、
另外逐帧裁定的 2 枚 baseline 反例与 1 枚 unexplained 各 1 局 ⇒ **逐帧触及 10 局**
(章程下限 6 局,达标)。

### 5.1 ⭐⭐ 承重帧 `7b6b1f/20260827_004433_slot2`,seed 896,**dire=ARMED**,OD **L19**,**t=1154.7**

施法前最后一帧 t=1154.4,OD 在 (−2654,−2191),hp 1536,mp 1335:

| 敌人 | 距 OD | HP | mp | worth-hitting? | 出厂能单杀? |
|---|---:|---:|---:|---|---|
| **tidehunter** | **344u** | 2087 | 279 | 622 ≥ 522 ✔ | 467 ≥ 2087 **✘** |
| **zuus** | **382u** | 1251 | 1053 | 313 ≥ 313 ✔ | 235 ≥ 1251 **✘** |
| **crystal_maiden** | **655u** | 1548 | 431 | 562 ≥ 387 ✔ | 421 ≥ 1548 **✘** |
| lina | 5265u | — | — | ✘(施程外) | — |
| luna | 5687u | — | — | ✘(施程外) | — |

三人**同一个 500 圆全覆盖**(`covered=3`)⇒ 门开;出厂循环**三个敌人都答否** ⇒ **无出口**。
最近的死亡在 **6.2s 后**(CM t=1160.9)与 **9.3s 后**(zuus t=1164)⇒ §5.3 的两道收紧闸也都不救出厂。

**落点证据在下一帧**(这是「门放了」与「这一发真落在三个人身上」的**第二条独立证据**):

| 帧 | CM | tidehunter | zuus | lina(5265u) | luna(5687u) |
|---|---:|---:|---:|---:|---:|
| t=1153.4 | 1539 | 2059 | 1300 | 1638 | 1990 |
| t=1154.4(**施法前**) | 1548 | 2087 | 1251 | 1638 | 1990 |
| t=1155.4(**施法后**) | **1373** | **1695** | **1223** | 1638 | 1990 |

**圈里三个人同帧一起掉血,圈外两个人两条曲线全平。** 10s 内 CM 与 zuus **双双阵亡**。
这正是头注说的那一档:「median 620 有效伤害劈给两个人 ≈ 近乎双杀,而出厂那个单体
`CanKillTarget` 问题把它整个扔掉」。

### 5.2 十枚 ARMED-ONLY 全表

| 局 | t | OD 等级 | ult 等级 | 施程内 | worth | 覆盖 | 最低 HP | fog-thin |
|---|---:|---:|---:|---:|---:|---:|---:|---|
| `…004427_slot3` | 383.1 | 8 | 1 | 3 | 2 | 2 | 828 | ✔ |
| `…004433_slot2` | 759.7 | 13 | 1 | 2 | 2 | 2 | 618 | ✔ |
| **`…004433_slot2`** | **1154.7** | **19** | 1 | 3 | **3** | **3** | 1251 | — |
| `…004436_slot12` | 792.5 | 13 | 1 | 3 | 3 | 3 | 1211 | — |
| `…004436_slot12` | 1604.5 | 26 | **2** | 4 | 3 | 3 | 1911 | — |
| `…005559_slot7` | 328.2 | 7 | 1 | 2 | 2 | 2 | 523 | ✔ |
| `…005644_slot9` | 394.6 | 7 | 1 | 2 | 2 | 2 | 778 | ✔ |
| `…005644_slot9` | 1283.6 | 27 | **3** | 2 | 2 | 2 | 2408 | ✔ |
| `…005655_slot4` | 1103.1 | 15 | 1 | 3 | 2 | 2 | 1432 | ✔ |
| `…005656_slot10` | 358.4 | 8 | 1 | 2 | 2 | 2 | 813 | ✔ |

`fog-thin` = worth-hitting 集恰好 2 个:timeline v1 **不带逐队视野位**,
两个里若有一个当时在雾里,门那半的「≥2」就不成立。**7/10 是 thin**,
但**不 thin 的 3 枚(含承重帧)已足以支撑 (a)**,且 thin 只影响「门那半」,
不影响「出厂无出口」那半(不可单杀与雾无关)。

### 5.3 ⭐ 三处近似「往哪边错」,其中一条**原本是反的,已修**

工具的每个近似都选了**让 ARMED-ONLY 更难成立**的方向:

1. `CanKillTarget` 用平 25% 魔抗(与既有 `od_ult_gate.py` 同一常数),**忽略所有魔抗装** ⇒ 高估出厂伤害;
2. 出厂那条 `#我方援军 >= #他方援军` 只会**否决**,不重建 ⇒ 又一次高估出厂;
3. ⚠️ **但快照 ~1Hz,施法前帧的 HP 最多陈旧 1 秒,这一条把误差推向「制造假 ARMED-ONLY」** ——
   与前两条**反向**。

第 3 条不是理论顾虑,**两个 baseline 反例全部是它**:

> `7b6b1f/20260827_003144_slot7` **t=956.4**:lina 在 232u,血 1497(t=953.4)→ 1090 → **612**(施法前帧 955.4)→ **238**(施法后帧 956.4)。
> 判据要 358 ≥ HP:对 612 **否**,对 238 **是**。**施法那一刻她在 `CanKillTarget` 里**。

已加两道闸:(i) 施法后 **2s 内死亡**的施程内敌人算给出厂;
(ii) 用**施法前 3 帧的最快掉血速率外推**到施法刻再问一次单杀。
两道闸把 baseline 假阳 **5 → 1**、armed **17 → 10**。**§4 表里报的是收紧后的数。**
残留的那 1 枚(`…004413_slot11` t=385.8)同形而更极端(CM 540→218、zuus 571→218 **一帧之内**),
外推用的是**前几帧的较慢速率**,够不着 —— 如实计为假阳。

### 5.4 ⭐⭐ 本组自己踩中了写在章程里的那条坑(同族第七例)

第一版工具把生死判定问在**施法刻 `t`**,而不是**施法前那一帧**。
章程「**谓词要在施法前的最后一帧上评估**」这条(`cmrguard_counterfactual.py` 那次的教训,#66 §0),
我把它用在了**位置**上,**没用在生死**上。后果:

> `7b6b1f/20260827_004427_slot3` **t=585.1**:tidehunter 在 **490u**、**354 HP**、活着,
> 而他的 DEATH 事件**正好戳在 585.1** —— `is_dead(spans, h, 585.1)` 判他死,
> 于是**这一发打死的人从这一发自己的判据里消失**,该帧读回「施程内 **0** 个敌人」。

**61 次 armed 施法里 30 次**被这样读空(baseline 56/75)。修法:生死一律问在 `od["t"]`。
**同族第七例**(#171 丢读数 / #213 丢门 / #207 接线门 / #230 丢半个域 / #234 丢地址 / #241 丢前提 / **本条:规矩只用了一半**)。
与前六例的区别:**工具、规矩、教训全都在树里而且我读过** —— 丢的是**它的适用面**。
已加断言(`enemy killed BY the cast still counts at the pre-cast frame`)钉住。

同一轮还踩了 **GH #176**(英雄名不是实体键):第一版按 `hero` 名分组快照,
luna 有 **7149** 行而其他英雄 1671 行(幻象共用类名),`death_spans` 的复活检测被打乱、
英雄从第一次死亡起**永远是死的**。修法**不是自己再写一遍**,是改成直接调
`roam_conversion.death_spans` / `is_dead` / idx 锁 —— 这三样**本来就在树里且已被审计过**。
(这也把章程 backlog 的「`entities.py` 推广」往前推了一格:本轮的做法是**复用而非推广**。)

## 6. 残留 `unexplained`:两条腿等量 ⇒ 不是本 id 的事(**GH #245 新开**)

| 腿 | unexplained | /局 | 其中施程内 **0 活敌** |
|---|---:|---:|---:|
| armed | 10 | 0.56 | 5(0.28/局) |
| baseline | 14 | 0.47 | 7(0.23/局) |

**两条腿等量**,且门那半连「≥2 可命中」都不成立 ⇒ **不是 `odaoe`**,是出厂行为。
逐帧裁了**其中 1 枚**:

> `7b6b1f/20260827_004427_slot3` **t=1178.6**,OD **L22**:
> luna 在 **167u** 但 **DEATH 戳在 t=1178.3(施法前 0.3 秒)**,血 506→192→0;
> 唯一还活着的 CM 在 **776u** —— **施程 700 外 76u**;其余三人 6900u+。
> **决策那一刻 luna 还活着且在 `CanKillTarget` 里**(505 ≥ 192)⇒ **出厂判据没错**,
> 错的是**执行落在 0.3 秒后**。这一发 **140s cd + 200 蓝打在空气上**。

**只裁了 1/12 枚**,机制不能直接推广到另外 11 枚 —— 已如实写进 issue,下一轮补裁。

## 7. 交付

- **新工具** `tools/batch_test/behavioral/odaoe_domain.py`
  - `--selfcheck` **41 PASS / 0 FAIL**;`--source` 零语料复现;`--extract` 单局归约(28MB dump 立即删,守住磁盘配额);
  - 常数**全部从 Lua 读**:`nRAoeMinTargets` / `nRAoeMinDamagePct` / 门里**只有一个** `IsSoakCandidate`
    (#207 那个形状的反向检查)/ 调用点 **== 1** / **门必须仍在出厂出口下面**;
    **四条各有反 selfskip** 用合成源码证明真会红,其中「上移」那条的变异是**外科式**的
    (`if J.IsGoingOnSomeone(bot)` 在本文件出现多次,`str.replace` 全替也会红,但**红的理由是错的**——
    一个碰巧变红的变异测试什么都没证明,已加一条断言先确认变异确实只挪了那一行);
  - 生死与实体身份**不自己重写**,直接调 `roam_conversion`(审计过的那份);
  - 等级从快照读(`ULT_BY_LEVEL`),**不假设 1 级**;`abilities` 为 null 的帧**回溯**到上一帧有的,不落默认值。
- **`tests/test_detector_source_constants.py` 新增 5 条棘轮**,其中承重的一条是
  **「门仍在出厂出口下面」** —— 它一旦上移,armed 就会开始**改向**而不只是**追加**,
  §4 的每一个 ARMED-ONLY 计数就都不再是它字面的意思。

## 8. 交棒

- **GH #54 追评**:全文读数 + 承重帧 + 三处近似方向 + **(a) BOUGHT / `WORKING`**。
  ⇒ 三条件判定的球在**总监**((b) 要批测台的胜负粗读;(c) #54 §4 已给)。
- **GH #244 新开 [hero]**:头注那句「Sanity's Eclipse is level 1 in **9 games of 9**」
  **已被真实帧证伪**(本轮观测到 ult 2 级 ×1、**3 级 ×2**)。它不是随手一注 ——
  紧接着「retuning 两个 tunable 几乎买不到东西」的整张算术都跑在 radius=500 / base=200 上。
  归因是 **owner P3(#108,cap 10→25)落地**,与 **GH #236 同族**;
  区别是 #236 的载体是**会变红的测试**,本条的载体是**散文,不会有任何东西举手**。
- **GH #245 新开 [hero]**:大招打在空气上的那一档(0.28 / 0.23 每局,两腿等量),
  含承重帧与**「若走多目标落点这条路请新开 id,不要改 `odaoe`」**的警告
  (改了它就不再严格追加,#54 的读法随之失效)。
- **球在总监**:`odaoe` 三条件判定(另有 #245、#244、#241、#236、#235、#159、#207、#212、#220、`campfarm` 仍未裁)。
- **本组不裁 promote/reject、不申请波次、不改 bot Lua、不花 AWS 钱。**

## 9. 验证

- `bots/` / `game/` **0 改动**(改的是 `tools/` 与 `tests/`)。
- `bash tools/agent/luacheck_gate.sh` → **exit 0**,`luacheck bots game: 0 warnings`
  (容器里没有 luacheck,脚本自己装的 `lua-check`)。
- `bash tests/run_py_tests.sh` → **41 passed / 0 failed**(与开工自检逐位相同)。
- `odaoe_domain.py --selfcheck` → **41 PASS / 0 FAIL**;
  `tests/test_detector_source_constants.py` **全绿**(含新增 5 条)。
- 本轮**未改任何 Lua** ⇒ **不声称跑绿过 Lua 全量**(GH #124)。
- **AWS**:S3 **只读**(288 `analysis.json` + 51 `.dem` 流式下载即弃),
  **未启动/未终止任何实例**,**未调用 Cost Explorer**,**零支出**。

## 10. 下一轮优先

1. ⭐⭐ **W16 首检**(`spot_20260827_0616xx_1_main_{5e1086,c1d1cf,8ca428,654032}`,
   06:17Z 起飞,arm 串 **40 id / 354 字节** —— 比 W15 多 3 个,`bbshort`/`pullthink`/`bbfight` 首次有语料);
2. ⭐⭐ **#236 裁下后 `mv` 落地 #159 的 fixture**(**第六轮**,成本 = 两条 `mv`);
3. ⭐ **#245 的另外 11 枚补裁**(本轮只裁了 1/12);
4. ⭐ **把「每 id 篇数」那张表做成独立小脚本**(**第二轮登记**,上一轮是手跑 grep);
5. ⭐ **GH #220 的 fixture**(`094054_slot3 t=163.0 drow_ranger`,**第六轮**);
6. `camp_prelit` 列(**第十轮**);占用度量按 idx 去重(**第九轮**);`tpdefend_events` 结果侧列
   (#191 后,**第十五轮**);幻象余量做进 `sweep_run.sh`(**第十二轮**);`entities.py` 推广(**第十三轮**);
   12:41Z §6.1 那 23 帧做 fixture(**第十五轮**);`pulldrag` 的 connect 侧;第二种视野证人;
   ab/ba 回灌 `fieldbuy_silence.py`/`stayfield2_margin.py`(**连续第二十六轮登记**);`stayfield` 第一失败子句;
   打野反证 fixture(**三十二轮**);`hero-1` 的 153 局 WK 语料(**40 轮**);`l5combo` 的 (a)(**第五十六轮**);
   `make_fixture.py` 钉 `062551 t=205.5 jakiro`(#45,**第四十三轮**);`axebuyblink` armed 的波次。

## 11. Token 用量(铁律 8)

```
TOKENS total_in=14,790,715 out=84,866 turns=99
```

(到统计时刻为止;统计后的收尾回合不计入。)
