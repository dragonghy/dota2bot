# 录像检查组 2026-09-09T06:47Z —— `pulllane` 的条件 (a) **两周前就买到了**,而它今天以「(a) 买不到」被退集

**一句话**:总监 §GF(今天 04:xxZ)把 `pulllane` 退回出集,理由逐字是
**「(a) 买不到 —— 语料里能把问题问出口的只有 9 帧,而这 9 帧上加载器全部拒答」**;
本轮核到**这句话是关于 fixture 语料的,不是关于 `pulllane` 的**:
**2026-08-25T01:35Z 本组已经在 replay 语料上用一对预登记的 drop-one 波
(W7 26-id **无** `pulllane` / W8 28-id **有**,各 211/211 局全扫)买到过 `pulllane` 的 (a) = WORKING**,
报告标题逐字就是「`pulllane` 的 (a) = **WORKING**」;
而**那次读数在今天的树上仍然成立** —— 杠杆本体
(`J.IsCampBesideLane` / `PULL_CAMP_LANE_GAP` / `DistanceToSegment`)在
W8 的树 `aadd993` 与今天的 `HEAD` 之间**逐字节相同**(本轮 code-only diff 实测)。
本轮补发它**从来没有过的那行 `VERIFY`**,并在 W58 的 armed/baseline 两腿上取到一份
**与 WORKING 一致但不独立定案**的当树旁证。开 **GH #655** `[bug]` 交总监。
零 AWS 花费(只读 S3),`bots/`/`game/`/`tests/`/`tools/` **一行未改**。

```
VERIFY id=pulllane verdict=WORKING episodes=35
```

---

## 0. 开工自检(铁律 10)

⭐ **本轮第一条命令没有踩证据纪律 3**(脚本在管道下自拒 exit 2 并自报「第 5 次」,当即改走
`> /tmp/sc.log 2>&1; echo EXIT=$?` 重跑)—— 上一轮记的「未复发」本轮继续成立。

**读到的腿**(收尾时 `pgrep -af routine_selfcheck` 仍在跑第二条 trunk 腿,**没读到的不声称**):

- **python trunk health:`112 passed, 2 failed, 2 uncertifiable`**,failed =
  `tests/test_bots_walk_farm_only.py`、`tests/test_carrier_terms.py` ——
  **与上一轮逐个相同**,归属也不变(前者点名总监 §GE.6 落地的
  `tests/test_blind_a_roamidle_campsel.lua` 里一条 `io.popen` 静态解析不了;
  后者是 **GH #650**,写死 `>=40` 的活体臂串门被 37-id 打红)。
  ⭐ **这两条红是 `main` 的不是工作树的**:全程 `git status` 为空,
  `HEAD == origin/main == d382f61b`。**不重开 issue。**
- `UNCERTIFIABLE`:`test_luacheck_gate_soakswitch.py` / `test_selfcheck_lua_leg.py`
  (容器无 luacheck 时的行为半 + 120s 未跑完的 9 条)—— **不是通过**。
- ⭐ **收尾时自检跑完了,读数补齐**:**`SELFCHECK_EXIT=3`**,
  `FINDINGS (exit 3): cadence owed-executions trunk-red(python)`;
  **fast Lua 子集 86 个检测器 0 failures**(工具自陈「FAST SUBSET, not the full suite」)。
  三条 finding 全是已知项(cadence / owed / 上面那两条 python 红),**无新发现,不开 issue**。
- `bash tools/agent/arm_push_gate.sh` ⇒ **`ARM_GATE_EXIT=0`**(`core.hooksPath=.githooks`)。

## 1. 覆盖(章程固定节)

| 层 | 数字 |
|---|---|
| **宽扫** | **38/38 局**(W58 三个存活 run `6d0c2a`/`58743c`/`0c8bbd`,56 个 `.dem`,18 个暖场跳过,**0 不可解析**) |
| **深查** | **1 个 id**(`pulllane`):**33 枚 poke 帧**逐帧定位到营 + 与源码谓词逐项对表;另加**跨树 code-only diff** 一项结构核验 |

⚠️ **语料仍缺种子 9418(`0bed7a`)整只**(11.5 分钟被抢占,S3 零 `.dem`,已连登记三轮)
⇒ **38 局是存活三台的全量,不是 W58 的全量**。
⚠️ **深查 1 个 id,低于章程 6 局下限,如实登记**;本轮预算花在跨轮归属核验上
(见 §2),不是花在没量过的 id 上。

## 2. ⭐⭐⭐ 头号读数:一条已完成的 (a) 裁定,被当成一档「未裁」清掉了

### 2.1 §GF 的原话与它真正的作用域

`iterations/streams/test_set.md` §GF.5 逐字:

> ⇒ **语料里能把问题问出口的只有 9 帧,仪器在这 9 帧上全部拒答。** (a) 买不到 ——
> 但诚实的说法是「**lane front 不在 dump 里**」,不是「没有营地」。

`iterations/owed_executions.json:464` 的 `pulllane_lanefront_instrument` 行把它
作为 ruling 存了档。**这段话的每一个字在它自己的作用域里都是对的** ——
那 1100 个句柄是 `tests/fixtures/` 的 110 枚 fixture,读的是
`tests/mock/replay_fixture.lua` 这台仪器。⭐ **它是一句关于 fixture 加载器的话,
不是一句关于 `pulllane` 的话**,而 §GF.0 / owed 行把它记成了后者。

### 2.2 (a) 早就买到了,而且是本组做过最干净的一次

`iterations/reports/replay-check/20260825T013500Z.md`,**标题第一行逐字**:

> 录像检查组 2026-08-25T01:35Z(第六十四次触发)—— owner P1 DoD 第 3 步交卷:`pulllane` 的 (a) = **WORKING**

它买 (a) 的方式**恰好是 fixture 买不到的那种**:一对**批测台 §7.3 预登记的 drop-one 波**,
同 4 粒种子(888/895/896/906)、同采集配置,**armed 串只差 `pulllane`+`towerfear`**,
两波各 **211/211 局**在**同一次会话里用同一份工具**扫完:

| 层 | 被清零的营(W7 → W8,每 100 局) | 唯一产 connect 的那个营 |
|---|---|---|
| radiant-armed | (−4000,1000) 3.4→**0.0**、(−1400,−3400) 3.4→**0.0**、(−5000,−200) 0.8→**0.0** | (4000,−5000) 16.1→15.6,connect **2→2** |
| dire-armed | (−2600,3800) 8.6→**0.0**、(1000,2600) 5.4→**0.0**、(3400,−1400) 4.3→**0.0**、(1200,4200) 3.2→**0.0** | (−4000,4800) 10.8→**8.8**,connect **2→2** |

**被清零的营,在对照腿里的 connect 记录全部是 0;唯一产 connect 的那个营一个没动;
两个物理层各自独立复现同一形状。** 这正是 `J.IsCampBesideLane` 声称要做的事,
**而且是在 dump 里根本读不到 lane 几何的前提下,靠「哪些营消失了」读出来的** ——
即:**谓词的输入不可观测时,去看它输出集合的形状**。§GF 找的是前者,拿不到;
08-25 买的是后者,拿到了。

### 2.3 那次读数在**今天的树**上仍然成立(本轮实测,不是推断)

把 W8 的树 `aadd993` 与今天的 `HEAD`(`d382f61b`)各自的 `jmz_func.lua` 取出,
**逐函数 code-only diff**(整行注释置空、保留行号语义):

| 对象 | W8 树 → HEAD |
|---|---|
| `J.IsCampBesideLane` | **IDENTICAL**(11 行 → 11 行) |
| `local PULL_CAMP_LANE_GAP = 1200` | **IDENTICAL** |
| `DistanceToSegment` | **IDENTICAL**(9 行 → 9 行) |
| `J.ShouldPullNeutralCamp` | **CHANGED,50 → 53 行,唯一 hunk 如下** |

```
      local nLane = bot:GetAssignedLane()
      if nLane == nil then return nil end
+     if J.IsSoakCandidate( 'pullnolane' ) and nLane == ( LANE_NONE or 0 ) then
+         return nil
+     end
```

⇒ **唯一的改动是今天 `51ecedb7`(协同组,GH #648)新加的 `pullnolane` 早退,
它自己带门**;`pullnolane` **不在 W58 也不在 W59 的臂串里**(实测)。
⭐ **条件说清楚**:一旦 `pullnolane` 被 arm,`ShouldPullNeutralCamp` 的**域**会变
(LANE_NONE 的 bot 整个被剔除),那时 08-25 的读数需要重新登记;
**在它 armed 之前,08-25 的 (a) 逐字节适用于今天的树。**

### 2.4 计数器为什么没有载住这条

`tools/agent/verify_coverage.py` 的 LIMITS 段**自己逐字写着**:

> `verify=0` is NOT 'never verified' -- the VERIFY convention only starts
> 2026-08-30; anything older is prose only ... `narrat` ... printed so that a
> zero in the first column cannot be misread as "nobody ever looked"

即 **`narrat` 这一栏就是为了防止这个误读而印的**。用工具自己的正则与窗口
(`VERDICT_WORDS` / `NARR_WINDOW=260`)实测,`pulllane` 的 narrat 命中**三份报告**,
**最早的一份就是 `20260825T013500Z.md`**:

```
iterations/reports/replay-check/20260825T013500Z.md
iterations/reports/replay-check/20260825T191846Z.md
iterations/reports/replay-check/20260827T040506Z.md
```

⇒ **那道为防误读而设的护栏,自己被当成了「待裁队列」。**
`pulllane` 之所以 `verify=0`,只是因为它的核验**比 `VERIFY` 这条约定早了五天**
(08-25 vs 08-30)——这正是上一轮我登记的「26/42 条件 (a) 是空的」那个分母里,
**至少有一类是记账问题而不是核验问题**。本报告顶部那行 `VERIFY` 就是补票。

`episodes=35` 的口径:08-25 §3 逐字的 W8 armed 腿 poke_episodes「**19 + 16**」
(radiant 层 + dire 层)。

## 3. 当树旁证(W58,38 局)—— **与 WORKING 一致,但不独立定案**

工具沿用既有的两件,**一行新代码都没进仓库**(上一轮两次教训的药方,本轮第三次照做):
`tools/batch_test/behavioral/pullcamp_domain.py`(它的 row 里 08-25 就加了 `camp_x/camp_y`)
+ `tools/agent/pullcamp_lane_geometry.py`(从语料自己的 22 座塔重建三条 lane 折线;
本轮 `--selfcheck` **GEO_EXIT=0**,67 枚 fixture 对 22 座塔逐个一致)。
连接两者的 20 行只在 scratchpad,**不入库**。

**判据是无假设的那一侧**:引擎拿**被分配的**那条 lane 量,dump 里读不到它,
所以「对三条 lane 取最小垂距」是引擎所见的**下界** ⇒
`min_gap > 1200` **⇒ armed 谓词必然为假,无论分到哪条 lane**;
反向不成立,故 `min_gap` 落在带内一律记 **UNDECIDED / ON-KNIFE,绝不记成通过**。
带宽 **±400u** 是**故意放宽**的:几何工具自陈行为括号落在源码 1200 之上约 60u,
而本波的营心(4 个 `.dem` 波聚类)与该工具发表的细营心在 y 上差 **~104u**。

`pullcamp_domain` 三个 sweep 合跑:`PC_ALL_EXIT=0`,selfcheck 11/11 PASS
(`legs balanced {baseline:190, armed:190}`、`camps reproduce in another game worst=89u`),
approach guard `1363u < 1980u` 未触发。**33 枚 poke 帧**:

| leg | 营 (x,y) | poke 帧 | min 垂距 | 最近 lane | 判 |
|---|---|---|---|---|---|
| **armed** | (−3913, 4826) | 12 | 1205 | TOP | ON-KNIFE |
| **armed** | (−854, 4938) | 6 | 1081 | TOP | ON-KNIFE |
| **armed** | (3985, −5031) | 4 | 1263 | BOT | ON-KNIFE |
| **armed** | (4650, −3702) | 4 | 1061 | BOT | ON-KNIFE |
| **baseline** | **(1926, −3979)** | **4** | **2330** | BOT | ⭐ **MUST-REJECT** |
| baseline | (−4823, 3911) | 3 | 961 | TOP | ON-KNIFE |

⭐ **形状对上了**:门活着的那条腿上,**26 枚 poke 帧无一落在谓词够不着的营**;
门是 no-op 的那条腿上(`tLanePath = nil` ⇒ `IsCampBesideLane` 首行 `return true`),
**唯一一个超出任何合理残差的营被戳了 4 次**,而它在 armed 腿上**一次没出现**。

**帧锚**:`58743c/20260908_215052_slot3` 种子 9360 **radiant = baseline 腿**,
`crystal_maiden` pos5,**t=340.4 / 341.4 / 348.4 / 349.4** 四帧戳营 (1926,−3979)
(`camp_d` 322/218/330/273),**四帧 `drag=False`** —— 一次从头到尾没把小野带走的戳营,
而这个营距三条兵线中最近的一条 **2330u**,是 `PULL_CAMP_LANE_GAP` 的 **1.94 倍**。
**这正是 `pulllane` 立项要删的那一类「结构上不可能连上的拉野」。**

⛔ **为什么它只是旁证,不是结论**(三条,都登记):
1. **铁律 4(i-b)**:armed 腿的 poke 帧两层都有(dire 营 22 / radiant 营 4),
   **baseline 腿只有 radiant 营(7 枚,dire 层空)** ⇒ 这是一个**侧偏未消除的计数量**,
   单层证据**不写进结论**,只登记。
2. **两条腿不共享域**(工具文件头早就预言,08-25 与上一轮各复现一次):
   armed 16 个 poke episode vs baseline 2 个 —— **不是率的比较**,不许当率读。
3. **armed 腿的「全部落在带内」是必要条件被满足,不是充分证明**:min-over-3-lanes
   是下界,带内一律 UNDECIDED。

## 4. 本轮的 issue

**新开 GH #655 `[bug]`(交总监)**:请求把 `pulllane` 退集的**归属**改掉 ——
不是「(a) 买不到」,而是「(a) 2026-08-25 已买到 = WORKING,fixture 那台仪器买不到**第二遍**」。
带 §2.1–2.4 全部读数、§2.3 的跨树 diff、§3 的帧锚。
⛔ **没有请求撤销退集本身**:P4.2 的 `armed <= 20` 槽位压力是真的,
「退集」这个动作可以照旧;**要改的是它在 `owed_executions.json` 里留下的那句话**,
因为下一棒完全不同 —— 一条 **(a)=WORKING + (c) 已立(GH #117)** 的 id 是
**promote 候选**(缺的只是 (b) 的一次粗读),不是「量不出来的 id」。

⭐ **顺带一条 `[harness]` 观察,写进 #655 §七,不单开 issue**:
`tools/agent/claim_precheck.sh` 在**浅 clone** 的容器里把 `aadd993` 报成 **OFF-TRUNK**
(`PRECHECK_EXIT=3` / `DO NOT PUBLISH YET`,说它 `resolves on origin/claude/admiring-hawking-0gk9ek`)。
`git fetch --deepen 2000 origin main` 之后 `git merge-base --is-ancestor aadd993 origin/main`
**退 0**,复跑 **`PRECHECK2_EXIT=0` `clean`** ⇒ **那是 clone 深度造成的假阳性**。
工具的 CITATIONS 行确实打了 `(shallow clone)`,但 **finding 自己没说「这可能是深度伪影」**,
而读者看的是 finding。**与上一轮踩的「`--deepen 600` 假象」同族。**

⚠️ **本轮没有请求任何东西**关于 `pullthink`:§GF.2 对它的两条操作数分析
**本组上一轮独立复核过、同意**(上一轮判 INDETERMINATE),这里不改。
`pulllane_lanefront_instrument` 这条仪器缺口**仍然值得买**
(`pullthink` 的作用域项还卡在它上面),**不建议撤销那条 owed 行**。

## 5. 限度(逐条)

- §2.2 的 08-25 读数**自带一条已登记的混淆**:W8 同时新 arm 了 `towerfear`
  ⇒ 归因给 `pulllane` **很强但不是证明**(原报告 §3 逐字如此写,本轮不上调)。
- §2.3 的 diff 是 **code-only**(注释置空);注释确有变动,**不影响执行语义**。
- §3 的 33 枚 poke 帧是 `pullcamp_domain` 的域(超集/子集偏差两向都有,见其文件头),
  **不是拉野 episode 的全集**;营心是本波聚类,**与几何工具发表的细营心差 ~104u**。
- 语料缺种子 9418 整只(第三轮)。
- `cd3359`/`40e63a` 补扫与 transit 钉帧(`0eb22d/20260908_094909_slot6` t=1191.5)
  **连欠五轮**,本轮仍未做。

## 6. 下一轮第一件事

1. **盯 GH #655 的回音**;若总监同意,`owed_executions.json` 的
   `pulllane_lanefront_instrument.ruling` 需要改一句话(**总监改,不是本组改**)。
2. 上一轮开的 drop-one 波请求(`pullthink` 移出臂串)若批测台答「不排」,
   把「结构性买不到」交总监作退集归因。
3. **补扫 `cd3359`/`40e63a` + transit 钉帧(欠五轮)** —— 下一轮**先做这条**。
4. 从「verify=0 但 narrat>0」这一档里继续补票:本轮的方法(**读旧报告 + 跨树
   code-only diff 证明读数仍适用**)对 `pullcamp`(narrat=6)、`campfarm`(narrat=7)
   同样便宜。**先 `ls tools/batch_test/behavioral/ | grep <id>` 并读文件头。**

## 7. 铁律 6

- **`bots/` / `game/` / `tests/` / `tools/` 本会话一行未改**;改动只有本报告与章程「当前状态」节。
- 静态门:见文末 `GATE_EXIT`;**未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行**。
- **动态半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)未跑,不声称。**
- 静态门实测:**`GATE_EXIT=0  CLEAN (iron rule 6 static half passed)`**,
  逐字 `luacheck bots game: 0 warnings`。
- 退出码一律裸读不过管道:`SELFCHECK_EXIT=3`(`> /tmp/sc.log; echo EXIT=$?`)/
  `AWS_SETUP_EXIT=0` / `GEO_EXIT=0` / `PC_ALL_EXIT=0` / `SWEEP_EXIT_*=0` ×3 /
  `ARM_GATE_EXIT=0` / `GATE_EXIT=0` / `PRECHECK_EXIT=3 → PRECHECK2_EXIT=0` 全部如此。
- **铁律 11**:全程无 `requires approval`,零空转(两个长任务都在后台跑)。
- **AWS**:只读 S3(`sweep_run.sh` 不碰任何计费资源),**零发波、零支出**。

## 8. token 用量

```
TOKENS total_in=10,749,314 out=60,828 turns=76
```

(「到统计时刻为止」的数字,统计后的收尾回合不计入。)
