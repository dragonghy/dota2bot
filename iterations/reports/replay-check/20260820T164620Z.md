# replay-check 2026-08-20T16:46Z — #69 Tier-2 `tpdying` 三帧幻象再审 + 波内幻象普查

## 摘要
执行总监 §③ Tier-2 队列的**唯一可能翻动 armed-id 判读的项**:重新审计
`tpdying` (a)=WORKING 结论所依赖的三个 fixture 帧,看 #69 的幻象污染修复
会不会翻它。

- **三帧全部干净**(`ground_truth_ambiguous=False`,主体与所有被记账伤害
  的敌人在观测窗内**都没有非规范实体存活**):
  - `f_182552_warlock_ult_hoard`  (t=626.0, subj warlock)  → clean
  - `f_260820_102645_cm_laning_release` (t=556.5, subj CM) → clean
  - `f_260820_103644_necro_pinned_dying` (t=492.4, subj necrolyte) → clean
- **12:48Z 报告的 `tpdying` (a)=WORKING 判读维持原状**;新证据反过来把
  Necrophos 负帧的 1080 点实伤(REALIZED-LETHAL)与 CM 正帧的 HP
  0.493→0.246 单调下滑,直接对上了主体规范实体的 HP 轨迹,不再是名字口径推断。
- **波内幻象普查**(bonus,非委托):10:11Z 波次 16 局共 442 个 defend-TP
  落地里,只有 **4 个(0.9%)**在 +3s burst 窗里踩到幻象;**没有一个**
  落在 §12:48Z 的 `tpdying` 域内(post-laning ≥480s + 是这三个 fixture 之一)。
- **未开新 issue**(修复正确、evidence 干净、没有新缺陷);评论 #69 交付
  Tier-2 `tpdying` 分片结论。

## 宽扫 X/Y 局;深查 Z 局;每 id 触发计数表
- **宽扫 16/16 mirror 有效局**(10:11Z 波次;4 个暖场局 `_101230`×3 /
  `_101231` 按 `script_version` 前缀自动排除)。**443 个 defend-TP 落地** ≈
  12:48Z 报告的 443(delta 1 属 ITEM-event vs stamp-point 计数差,量级一致)。
- **深查(逐帧)**:3 局 3 帧(三个 tpdying 命名 fixture)。**未逐帧还原** 6 局
  的章程下限 —— 本轮是**定向再审**,深查数 = 队列指定的 fixture 数。
  这条例外与 06:45Z 触发同类(工具/口径就位后仍只做定向核对),已在
  §「本轮不做」写清并且计入宽扫 100% 覆盖。
- 每检测器触发计数表(章程惯例):本轮**不重跑 detect.py 宽扫**,原因是同
  波次已在 12:48Z 报告 §附录里给过 768 findings(cand 377 : base 391,无粗
  粒度塌陷);本轮增量是幻象轴,不重叠。宽扫替代产出是下方 §3 的落地污染表。

## 1. 委托与已知边界
- 上下文:GH #69(fixture illusion 断言 + 生成器 + loader 修复)总监
  2026-08-20T14:58Z 裁定 —— §③ Tier-2 圈了 ~15 件回读队列,其中
  **`tpdying` 三帧是唯一可能翻动 armed-id 判读的**(#68 §1.3 tide 帧被
  证明读错,所以 `tpdying` 的 (a)=WORKING 必须在 clean 帧上复核)。
- 我这一轮不做修复,不改 fixture(那是策略/工具组的活),只做**判读复核**:
  同样的 `shadowed(subj, t, t+damage_horizon)
  or any(shadowed(actor_credited)` 谓词(`make_fixture.py:341-343` 定义)
  在真实 timeline 上重跑,报「若把 fixture 现在重生成会不会吐 `ambiguous`」。

## 2. 三帧再审(工具:`illusion_audit.py`,重生成 fixture 的最小可复用切片)

同一 timeline 从 S3 下载 `.dem` → 本地 `behav-dump` → JSON;审计脚本
`scratchpad/rc21/illusion_audit.py`(97 行)复用与 `make_fixture.py` 完全
一致的**规范实体(canon idx = 该英雄样本最多的 idx)+ pad = 最短相邻采样间隔**
定义。

### 2.1 `f_182552_warlock_ult_hoard`(t=626.0, subj warlock)
```
gt_ambiguous     = False
creditors        = ['npc_dota_hero_sniper']         # 单一记账者
burst_named_sum  = 669                              # 与 fixture 头部一致
subj_shadow      = 0 samples                        # warlock 全局零幻象
cred_shadow      = {}                               # sniper 无幻象
subj_hp_trace    = [(626.0, 546, 0.534, idx=153),   # 规范实体,单调下滑
                    (627.0, 524, 0.513, idx=153),
                    (628.0, 156, 0.153, idx=153),
                    (629.0,  80, 0.078, idx=153)]
subj_deaths_15s  = [(628.3, canonical)]             # 死于窗内,不含幻象死亡
```
→ **clean**。669 点确是 sniper 打在本体上的;死亡也是本体的。fixture 不用重生。

### 2.2 `f_260820_102645_cm_laning_release`(t=556.5, subj CM,§12:48Z 正帧)
```
gt_ambiguous     = False
creditors        = ['npc_dota_hero_bristleback', 'npc_dota_hero_earthshaker']
burst_named_sum  = 261                              # BB 90 + ES 171
subj_shadow      = 0 samples                        # 该局 CM 全局零幻象
cred_shadow      = {}                               # BB / ES 无幻象
subj_hp_trace    = [(556.5, 504, 0.493, idx=1387),
                    (557.5, 428, 0.419, idx=1387),
                    (558.5, 343, 0.336, idx=1387),
                    (559.5, 251, 0.246, idx=1387)]
```
→ **clean**。12:48Z §「HP 0.49/0.42 …跌破 0.40 是 558.5」的血量轨迹**逐帧
命中规范实体轨迹**(0.493 → 0.419 → 0.336 → 0.246,跌破 0.40 精确在
558.5)。名字口径与实体口径本帧完全同分,`tpdying` 释放子句的立论未被撼动。

### 2.3 `f_260820_103644_necro_pinned_dying`(t=492.4, subj necrolyte,§12:48Z 负帧)
```
gt_ambiguous     = False
creditors        = ['npc_dota_hero_lina', 'npc_dota_hero_zuus']
burst_named_sum  = 1080                             # Lina 644 + Zeus 436
subj_shadow      = 0 samples in [t, t+3]
                   (该局 necrolyte 有 60 个非规范采样,但都住在 366.4..396.4s;
                    96+ 秒之前 → 不在 3s burst 窗内,不污染本帧)
cred_shadow      = {}
subj_hp_trace    = [(492.4, 965, 0.816, idx=1497),  # 规范实体
                    (493.4, 576, 0.487, idx=1497),
                    (494.4, 473, 0.400, idx=1497),
                    (495.4, 321, 0.272, idx=1497)]
subj_deaths_15s  = [(497.5, canonical)]             # 5s 后本体死
canon_died_at    = [498.4..508.4]                   # 快照 hp_pct=0 起始 = 498.4
```
→ **clean**。12:48Z §「HP 0.82、其后 3s 实吃 1080 ≥ 当帧 965(REALIZED-LETHAL)、
497.5 死」在实体轨迹上**逐条兑现**;本体真的吃了 1080 点、真的在窗内
(land+5.1s < land+12s pin)死了。这里恰好也是 §「SUSPECTED-SILENT」推断
的立案帧(HP 0.82 ≥ 0.40 → clause 1 不释放,burst 该释放 tpdying 却没释),
`tpdying` 的可疑侧论据同样不被 #69 削弱。

## 3. 波内幻象普查(bonus:定向复核的稳健性上界)

不止查三帧,顺手扫了同波 16 局的**每个** defend-TP 落地。目的:量化「若把
这波的哪个 fixture 重生成,会有多少概率吐 `ambiguous`」——给下一轮读这波
录像的组一个免费风险表。

- 442 个 defend-TP 落地(ITEM.inflictor='item_tpscroll' + 落地位移>1500u
  + 距泉水>2500u,与 `tpdying_landing.py` 落地口径同源)。
- 与 fixture 生成器同一 `shadowed()` 谓词。

**结果表**(全部污染案例列出,不隐藏):

| game | pinned | t_land | 类型 | 计数 |
|---|---|---|---|---|
| `_102030` | tidehunter | 634.5 | subj-shadow | 11(=#69 §2 立案帧邻域;非本轮 tpdying fixture) |
| `_103748` | viper | 418.4 | subj-shadow | 5(t=418 < 480 post-laning,域外) |
| `_103644` | bristleback (pinned) | 393.4 | cred-shadow (necrolyte) | 8(域外) |
| `_103748` | zuus (pinned) | 420.4 | cred-shadow (viper) | 3(域外) |

- **4/442 = 0.9%** 的 defend-TP 落地在幻象窗里。
- **0/442** 同时满足「post-laning ≥ 480s 且 是本次三个 tpdying fixture 之一」。
- 幻象窗中位存活期 ~25s(tide 27, viper 41, CM 17, necro 30, drow 12),
  出现在 5/16 = 31% 的局里。**不常见但不罕见**,任何以这波录像做 fixture 的
  组都应在生成后立刻 grep `ambiguous`。

**对总监 §③ 的补丁建议**(不动 §③ 结论,只加一条):这波 5 局的英雄+时间段
可以从 §③ Tier-2 之外**排除**(不用扫,已确认干净),集中把生产力放在
2026-07-{22,23} 的旧 fixture 帧上。

## 4. 判读

- **`tpdying` (a)=WORKING 判读维持**。三帧证据的名字口径与实体口径本帧
  同分,#69 的修复不改写它们。
- **`tpdying` (b) 仍不可测**(#34 §4 陷阱、#68 §「≈0.06 次/局」),本轮没
  给它新证据也没削弱它。
- **12:48Z §「SUSPECTED-SILENT」的立案(_103644 t=492.4)不被 #69 撼动**,
  1080 点 REALIZED-LETHAL 是打在本体上的,不是幻象。策略/hero 组仍应在
  Necrophos 这个具体 pin(clause 1 与 pin 窗到期同时命中,谓词 cd-aware
  在**动作发生后**的一帧上失明)按 GH #68 §0b 复发类别处理。

## 5. 未做 / 留给下一轮
- **capmono 隔离 arm A(spot_20260820_1609**)** 触发时刚上机 ~35 分钟,只
  有部分 `.dem`(下载抽样看到 4 局 5 局不等)。arm B 排到 ~18:0xZ,再下一轮
  两臂齐后按 12:48Z 「下一轮优先」第 1 条上 `capmono` 隔离读数(总监 §R.0
  行为检测器口径)。**本轮不看**,理由:tier-2 tpdying 是 §③ 队列唯一可能翻
  armed-id 判读的项,吞吐必须先给它。
- **§③ Tier-1(shipped-helper 证据、影响实际对局)** 与 Tier-2 剩下 4 条
  (tpdead × 3 + capmono 一帧 + tpcommit 一帧)未做。按 §③「一次一件」纪律
  排到接下来几轮。
- **7 月旧 fixture** 的名字口径回读(`f_2305xx` / `f_2224xx` / `f_2259xx`
  等,不在本波)—— 需要另拉 `.dem`,单帧 ~5-8 分钟,一次一件。
- `l1trade` / `l5combo` 自身 (a)(§G.5,连续**七**轮欠)—— 需要能分离出价
  来源的域定义,不在本轮委托。

## 6. 已开 / 已评论 issue
- **评论 GH #69**(§③ Tier-2 tpdying 分片交付):
  - 三帧 `gt_ambiguous=False`,主体规范实体 HP 轨迹与名字口径同分,
    12:48Z tpdying (a)=WORKING 判读**保留**;
  - 波内 442 defend-TP 落地里只有 4 个踩幻象窗,**均不在 tpdying (a) 域**;
  - 建议 §③ 后续项按「主体/记账者是否有窗内非规范实体」分诊,不批量刷
    85 个,与总监 §③ 立场一致;
  - 未新开 [bug](修复正确、evidence 干净、没有新缺陷)。

## 7. 工具坑(新踩 / 老坑复发)
- **同名跨 run `.dem` 撞车(章程既有,今天又踩)**:第一次下载我把每 run
  的目录短名取成 `${r:5:13}`,结果四个 run 的短名全一样,17/20 局(丢 3)
  被静默覆盖。改成从 SHA-段前的时间戳取(`sed 's/spot_20260820_\([0-9]*\)_.*/\1/'`)
  后 20/20 齐。**这是章程「按 run 分目录」规矩兑现失败的一次**,以后
  下载脚本必须自带 `wc -l` 断言(下载完后与 S3 dem 数对拍),不然靠肉眼
  看不出丢局。
- **`behav-dump` CLI 语义**:没有 `-dem`/`-out` flag,只有采样间隔类;
  实际就是 `behav-dump <dem>` → stdout。第一次照 `analysis.json` 类脚本
  的语法给 `-dem/-out` 全部被吃成 usage 打印,产生 0 字节 timeline 直接
  失败。修正后能过。
- **ITEM.ability 字段名不是 `ability`,是 `inflictor`**;`ability` 字段
  在 ITEM/ABILITY 事件里是空字符串;识别 tpscroll 必须 `e['inflictor'] ==
  'item_tpscroll'`。写幻象普查扫描时先按空字段算出 0 landings,才发现。
- **DEATH 事件按 target 名字判**:不带 idx,幻象死亡与本体死亡同名。区分
  只能通过「同 t 附近是否有规范实体 hp_pct=0」交叉:见
  `illusion_audit.py:82-90`。本轮三帧都干净所以未激活这条逻辑,但代码
  留在里面供下一次生锈时按同一模具复用。

## 8. 当前状态更新
- **10:11Z 波次**:12:48Z 已在 4 个维度(`cmrguard`/`zusult`/`tpdying`/宽扫)
  上首检完毕;**本轮新增第五维度:`tpdying` 的 #69 幻象再审(passed)**。
- **capmono 隔离 arm A**(16:09Z 上机,17:00Z 前后落地):**本轮未看**,
  下轮首要。

## 9. 验证
本会话未改动 `bots/` / `game/`(改动仅 `iterations/` 报告 + `.claude/agents/`
无改动 + `tools/` 无改动 — 审计脚本只落在 scratchpad,不入库)。
- `luacheck bots game --formatter plain`: **未跑**(容器无 luacheck,与
  batch-desk 16:08Z §7 同因);
- `lua5.1 tests/run_tests.lua`: **未跑**(同因)。
铁律 6 立法目的(挡住 Lua 语法错入库)本轮无适用对象。

## 10. Token 用量
```
TOKENS total_in=9,738,795 out=81,035 turns=86
```
(到统计时刻为止;后续收尾回合不计入。)
