# replay-check 2026-09-13T03:5x–05:xxZ — `pullcad`:08-30 那条 INDETERMINATE 的**判别子已被树本身推翻 13 天**,而三个载体都还在传播它;W69 首读买到的**更重的东西是「拉线域有 ~95% 是打架」**

**一句话**:本轮按交棒单第 (2) 条挑了 `pullcad`(verify=1、裁决最旧、通道自称可买),
先读源码再读数据,发现**它 08-30 那条裁决所依据的判别子(§3.3 的「静止 ⇒ 不是分支写的」)
在 2026-08-31 就被 `mode_roam_generic.lua:238-245` 逐字点名推翻了**,
而 **`pullcad_beat.py` 的 `stillness()` 头注至今一字不改地写着那句被推翻的话**;
**裁决本身不倒**(08-30 §3.2 另有一条结构性论证,且那条正是源码修正所背书的),
**倒的是判别子和它生出的那个 40.0–51.8% 数字**。
⭐⭐⭐ 但本轮真正的头号产出不是这条考古:**W69 12 局首读 + 7 局逐帧**测出
`creeppull_domain` 的 **pull-certified 人口有 96.0%(armed)/ 92.2%(baseline)带打架标记**,
**八个手读 episode 无一例外是 gank / 团战 / 对线互戳** —— 于是
**08-30 §3.4「足迹被压在 ±6% 以内」这个用法必须撤回**:它不是界,是一份**稀释约 20 倍**的读数。
⚠️ **尺寸要说准**:「这个域不 specific」**是 08-23 就已登记的**(`creeppull_specificity.py` 头注:
213 局上 1.31x,「the *metric* is not specific to the behaviour」),本轮 **1.16x 是复现**;
**新的是逐帧内容 + ~95% 这个比例,以及把后果落到那个仍在被当界用的读数上**(§3.2 / §3.3)。

```
VERIFY id=pullcad verdict=INDETERMINATE episodes=278
```

**段位声明(GH #424)**:**W69 单波**(4 run / 12 局,`spot_20260912_09262{2,4,7,9}`),
**遵 §CO.1 (ii) 不与 W25–W29 并池**(那是 44-id 家族;W30 起 `creepthink` 同波共 armed)。

---

## 〇、覆盖(本轮达标)

- **宽扫 12/12**(W69 全部有效局;4 个 run 各 4 个 `.dem`,各跳 1 个 warmup)。
  `SWEEP_EXIT` 四条**逐条裸读全 0**。⚠️ **无新波次,连续第五轮** —— 批测台在
  $90 刹车线上持有(MTD $88.692,其 03:11Z 报告),W69 仍是最新语料。
- **深查 7 局逐帧**(章程下限 6,**达标**;上一轮欠的 1/6 本轮一并还上):
  `094113`(两个 episode)、`094015`、`094042`、`095139`、`095226`、`100140`、`100514`,
  合计 **8 个 pull-certified episode**,armed / baseline 两腿都有。
- 选局依据:`pullcert_fight.py` 每局取第一个 certified episode,覆盖 4 个 run。

---

## 一、⛔ 考古:那条裁决的判别子,树在 13 天前就推翻了

### 1.1 08-30 的裁决靠什么站着

`iterations/reports/replay-check/20260830T040500Z.md` §3.3 造了一个判别子:
分支在两次 poke 之间**每帧**发 `Action_MoveToLocation(pull.retreat)`,
所以「**一对夹着原地不动的 poke,两条腿的节拍都不是它的作者**」,
由此得 **40.0–51.8% 的短间隔可被直接证伪为非分支输出**,
结句是「**可观测通道 ≠ 那条分支。这就是全部结论。**」。该数字随 **GH #326** 发表。

### 1.2 源码逐字读回来(第一手)

`bots/mode_roam_generic.lua:238-245`,**就在这条 lever 自己的执行点上方**:

> `READ #326 §"证据" AGAINST THIS: that issue infers from the same table that
> "neither leg can write zero-displacement + 1.5s right-clicks", and concludes
> the still frames are DOMAIN LEAKAGE rather than branch output (its
> 40.0-51.8% non-branch lower bound rests on exactly that step). The branch CAN
> write that shape, and this is how: **the drag order is not issued at all.**`

机制(`:291-322` 的 `[DUTY-CYCLE CORRECTION 20260831]`):think-throttle 活着、
`creepthink` 未 arm 时,分支**只在 throttle 重开的帧被问**,而刚右键过的英雄会在
`ACTIVITY_ATTACK` 里待满一个攻击周期 R。**R ≥ nBeat 时 drag 指令一次都不发** ⇒
**「两次 poke 之间原地不动」恰恰是出厂节拍自己的签名**,不是反证。

### 1.3 修正模型在树上是绿的(本轮现跑)

`tests/test_pullcad_throttled_duty.lua` 驱动修正后的四行表:
`neither armed 0.0%` / `'pullcad' only 41.2-50.1%` / `'creepthink' only 58.4%` / `both armed 83.4%`。
**`DUTY_TEST_EXIT=0`,11 tests, 0 failures**(裸读)。

### 1.4 ⚠️ 一处必须说准的边界:**裁决不倒,判别子倒**

不能写成「08-30 的结论错了」。同一份报告 **§3.2 另有一条独立的结构性论证**:
armed 腿光 W25 就带 1170 个 sub-2s 间隔,而 **3.0s 的节拍一个都写不出来**。
这条**不依赖任何位移读数**,而且正是源码修正**自己背书**的那条
(「a poke deferred to the first frame the throttle happens to reopen is **LATE, never early**」)。
⇒ **INDETERMINATE 站得住;站不住的是 §3.3 的判别子和那个 40.0–51.8%。**
本轮 W69 数据独立复现了 §3.2 那一侧(见 §三)。

### 1.5 ⭐⭐ 三个载体在传播未修正的模型(这才是可操作的部分)

| 载体 | 现状(本轮逐字核) | 处置 |
|---|---|---|
| `tools/batch_test/behavioral/pullcad_beat.py` `stillness()` 头注 | **逐字写着**「a poke pair that brackets a STATIONARY hero was authored by neither cadence」 | ⭐ **本轮已改**(§五) |
| 同文件 `duty()` 头注 | 把 **+43%** 当作「lever 自报的量」,且称本表是 **bound** | ⭐ **本轮已改**(§五) |
| 同文件头部 | 行号 `:268-271` / `:249-251` **已漂**(实际 `:342-349` / `:291-322`);**全文零次**提到 `creepthink` / throttle / §CO.1 | ⭐ **本轮已改**(§五) |
| `tests/test_replay_pullcad_beat.lua` 头注 | 第 28-29 行**裸述** 83%/58%;`grep creepthink\|throttle\|CO.1` **零命中** | ⛔ **本轮未动**,交棒(§六) |
| `tools/agent/verify_coverage.py` 的读数 | 仍把 08-30 那条 INDETERMINATE 报成 `pullcad` 的当前裁决 | 本报告的 `VERIFY` 行更新它 |

⚠️ **`pullcad_beat.py` 里 §CO.1 的缺席不是文风问题**:§CO.1 (ii) 是一条**关于这个工具读数的并池禁令**,
而工具的 `--wave` **只是个标签,没有任何检查**。一轮把 W25 与 W69 的 sweep 目录一起传进去,
**工具不会举手**。本轮已把该禁令写进头部(仍是调用者的责任,但至少读得到)。

---

## 二、W69 首读(12 局,`pullcad` 第一份 post-W30 读数)

`PULLCAD_EXIT=0`;**selfcheck 9/9 PASS 全部在真实数据上**
(`10 heroes` / `on_creep n=1601` / `log 0.1s: n=11276` / `10 distinct fracs` /
`SHORT_MAX 2.0 in (1.2,3.0)` / `legs balanced {armed:60, baseline:60}` /
`both strata` / `control gaps=173` / `no negative gaps`)。

**IN DOMAIN — pokes at the pull target**(两层都登记,铁律 4(i-a)):

| 层 | 腿 | eps | pokes | gaps | short<2s | mean gap | ≥2.6s 占比 |
|---|---|---|---|---|---|---|---|
| ab | armed | 552 | 478 | 259 | **64.1%** | 2.21 | 59/259 = **22.8%** |
| ab | baseline | 517 | 393 | 196 | 73.0% | 1.99 | 33/196 = 16.8% |
| ba | armed | 278 | 261 | 135 | **76.3%** | 2.05 | 22/135 = **16.3%** |
| ba | baseline | 281 | 209 | 109 | 76.1% | 1.90 | 14/109 = 12.8% |

- `short<2s`:ab **−8.9pp**、ba **+0.2pp** ⇒ **两层不同号**(ba 实为零)。
  按**工具自己头注的规则**(「Two strata that disagree in SIGN are noise」)**不进结论**。
  ⚠️ 这是 4(i-b) 的量(检测器计数占比,侧偏未消除),**登记但不据以判定**。
- `mean gap`:**+0.22 / +0.15,同号** —— 但修正模型的预测是 **1.5 → 3.0(+1.5s)**,
  实测是预测的 **10–15%**。
- `≥2.6s` 占比:**+6.0pp / +3.5pp,同号**,方向与 armed 的 3.0s 节拍一致,**量级极小**。

**DUTY — pull-certified episodes**(lever 自报的量):

| 层 | 腿 | n | mean | p50 |
|---|---|---|---|---|
| ab | armed | 466 | **133.4** | 147.0 |
| ab | baseline | 394 | 141.9 | 173.0 |
| ba | armed | 261 | **128.6** | 164.0 |
| ba | baseline | 200 | 141.8 | 174.0 |

**armed 比 baseline 低 6.0% / 9.3%,两层同号** —— 而修正模型在**两者共 armed 的波**上
预测的是 **0.0% → 83.4%**(比旧的 +43% **更大**)。⇒ **方向相反、量级差两个数量级。**
⛔ **这不是「lever 无效」的证据**,理由在 §三:这份人口 ~95% 不是拉线。

**STILLNESS**:ab armed 50.0% vs baseline 47.6%(+2.4pp);ba armed 52.4% vs baseline 56.6%(−4.2pp)
⇒ **反号,登记不入结论**;⚠️ 且按 §一,**这张表本来就不该被转成分支/非分支的切分**。

---

## 三、⭐⭐⭐ 头号产出:先逐帧,再聚合 —— 拉线域里 ~95% 是打架

### 3.1 逐帧(硬规则:先逐帧)

第一个手读的就露馅了。`20260912_094113_slot1`,**ab/armed**,axe → venomancer,
episode `t=340.5..351.5`,**`pull=True`、`drag=376`**,poke 在 345.1/346.2/347.3/348.3,
gaps **1.1 / 1.1 / 1.0**(命令:`creeppull_frames.py … 20260912_094113_slot1 axe 343 352 --tgt venomancer`):

```
344.5  ABI axe_berserkers_call ; DAM venomancer(axe_battle_hunger) ; DAM venomancer(axe_counter_helix) v=140
345.5  DAM venomancer v=69          <- 被计为 poke 的右键
347.5  DAM venomancer v=62
348.5  ABI venomancer(axe_culling_blade) ; DAM venomancer(axe_culling_blade) v=228 ; DEA venomancer(axe_culling_blade)
350.5  ABI juggernaut>(juggernaut_omni_slash) ; DAM juggernaut>(juggernaut_omni_slash) v=52
```

**这是一次击杀,不是拉线**:Axe 开 Berserker's Call、Battle Hunger 在跳、Counter Helix 触发
(那只在被多个单位打时触发),**t=348.5 用 Culling Blade 把 venomancer 斩了**,随后被 juggernaut 大招反打。
离自家泉水 **11,166u**。那三个 1.0–1.1s 的 gap 是**战斗中的攻击间隔**,不是任何节拍 ——
而 armed 腿的 3.0s 节拍**结构上写不出**它们。

第二个(同局,stormspirit → zuus,`t=214.5..228.5`,`pull=True`、`drag=878`):
Static Remnant / **Ball Lightning ×3** / Overload,对面 Zeus 回敬 Lightning Bolt、Arc Lightning、
Static Field、Heavenly Jump,位置在河道 (−700,−500)、离泉水 8,500u。
⚠️ **那个 `drag=878` 是 Ball Lightning 的位移**(dFount 逐帧 −177 / −11 / +164 / 0 / +170 / **+604** / −218)。

再五局,**无一例外**:

| 局 | 腿 | 英雄 → 目标 | 离自家泉水 | 帧上在干什么 |
|---|---|---|---|---|
| 094015 | armed | queenofpain → lina | 7,750u | Blink 进场,Shadow Strike + Scream of Pain,对轰 Dragon Slave / Slow Burn |
| 095139 | armed | skeletonking → viper | **13,106u** | Hellfire Blast,敌方深处 |
| 094042 | armed | bristleback → ogremagi | 10,821u | Quill Spray ×2,劣单对线互戳 |
| 095226 | armed | axe → juggernaut | 10,930u | Battle Hunger 在跳,劣单对线 |
| 100140 | **baseline** | drowranger → deathprophet | **13,167u** | Multishot,站桩与 DP + 白牛对轰 |
| 100514 | **baseline** | obsidiandestroyer → nevermore | 8,348u | Astral Imprisonment,中路对线 |

### 3.2 再聚合(`pullcert_fight.py`,scratchpad,只 import 既有谓词)

12 局、**278 个 pull-certified episode**:

| 腿 | certified | 施法 | 窗口内有英雄阵亡 | 全程 >6000u | **任一标记** |
|---|---|---|---|---|---|
| armed | 149 | 113 (**75.8%**) | 30 (20.1%) | 99 (66.4%) | **143 (96.0%)** |
| baseline | 129 | 94 (**72.9%**) | 25 (19.4%) | 90 (69.8%) | **119 (92.2%)** |

⚠️ **标记的诚实度**:`far>6000u` **粗**(劣单本来就在 ~11k 处作业,不能单独当证据);
承重的是 **施法 ~75%** + **阵亡 ~20%** + **八个手读 episode 全是打架**。

⛔⛔ **必须先登记的先例,否则本节会被读成一个它不是的发现**:
**「这个域不 specific」是已知且已登记的**,不是本轮发现的。
`tools/batch_test/behavioral/creeppull_specificity.py` 头注逐字写着:2026-08-23 在 213 局上
测得 `core_pull` episode **armed 1,869 : baseline 1,424 = 1.31x**,并直接下结论
「**a gate that is OFF on the baseline leg producing 76% of the armed leg's count means the
*metric* is not specific to the behaviour**」;`creeppull_domain` 头注也自称是 Lua gate 的
**deliberate SUPERSET**(三条源码子句离线不可观测)。
本轮 W69 的 **149 : 129 = 1.16x** 与那个 1.31x **同向同量级**,是复现不是新发现。

⇒ **本轮的增量只有两条,都要按这个尺寸说**:
(i) 把「不 specific」从**一个比值**变成**逐帧的内容**——那些 episode**具体是什么**
(八个手读全是 gank / 团战 / 对线互戳,armed/baseline 两腿都有),以及**比例 ~95%**;
(ii) ⭐ **把后果落到一个仍在被当界用的读数上** —— 见 §3.3。
**(ii) 才是本轮真正新的那一格**:非 specific 已知了 21 天,而 ±6% 那个界
**在 GH #326 的正文里、在工具头注里、在 08-30 的裁决里,一直被当界用**。

### 3.3 ⛔ 由此必须撤回的一个用法

08-30 §3.4 的结论句是「**这条杠杆在本语料上的可观测足迹,被压在 ±6% 以内**」——
那是把 duty 表当**界**用。**两条腿各有 ~95% 的人口在 lever 的域之外** ⇒
一个只在真拉线子人口里起作用的 lever,其足迹被稀释约 **20 倍**;
±6% 的 null **与「域内有很大效应」完全相容**。⇒ **该 bound 撤回**(本轮已写进工具头注)。
⚠️ **撤回的是 bound 这个用法,不是那四个数**;也**不主张** lever 有效。

---

## 四、裁决

```
VERIFY id=pullcad verdict=INDETERMINATE episodes=278
```

**仍是 INDETERMINATE,但理由换了,而且第一次被量出来**:
不是「静止帧证明通道不是分支」(**那条已推翻**),而是
(i) armed 腿 **77%** 的域内 gap 短于 3.0s ⇒ **结构上不可能是 armed 节拍写的**(§3.2 那条论证,W69 复现);
(ii) 域的 pull-certified 人口 **~95% 是打架**,两条腿**几乎等量** ⇒ gap 与 duty 两个通道**都**被稀释约 20 倍。
⛔ **不主张** `pullcad` = WORKING / BUGGY / SILENT;⛔ **不主张**它该退集 / promote / 改动。
⛔ **不自裁入集出集**(铁律),交总监。

**下一步要买 (a),得先修域,不是加局** —— 加局只会等比例地放大同一份 95% 污染。

---

## 五、树上改动(仅 `tools/` 注释,零行为)

`tools/batch_test/behavioral/pullcad_beat.py` 三处**纯注释**修正:
1. `stillness()` 头注:标明该判别子**已被 `mode_roam_generic.lua:238-245` 推翻**,
   给出机制与修正表,并写明**什么仍然成立**(短于 nBeat 的 gap 不可能是该腿节拍写的);
2. `duty()` 头注:把预期从 **+43%** 改述为修正后的 **0.0% → 83.4%**,
   并写明**本表不是 bound**(带 §3.2 的 95% 读数与撤回声明);
3. 头部:行号漂移标注(`:268-271` → `:342-349`,并写「re-grep, do not trust them」)
   + 新增 **§CO.1 (ii) 并池禁令**一节(并说明 `--wave` 只是标签、工具不检查)。

⭐ **旧数字一个没删**(照 repo 自己的先例:「they are not wrong: they are the R -> 0 reading」)。
⭐ **验证是等值性而不是「跑了一下」**:改前改后各跑一次全量
(`--selfcheck --still --duty`,12 局),`diff` **IDENTICAL_OUTPUT**、`PULLCAD2_EXIT=0` ⇒ **纯注释,零行为**。
⚠️ `grep -rln pullcad_beat tests/` **零命中** ⇒ 无测试绑定该文件文本(动手前查的)。

**`bots/` / `game/` 一行未改**;`tests/` **一行未改**(理由见 §六 (2))。

---

## 六、交棒(下一轮第一件事)

1. ⭐⭐ **上一轮撤回的两份产物仍未落地**:前置条件是英雄组处置 **GH #794**
   (WK census 没滤死行 + `hero_skeleton_king.lua:1693` 崩溃)。
   **本轮现读:#794 仍 open、零评论**(开于 02:05Z,英雄组最近一轮报告 01:54Z 早于它)⇒ **仍阻塞,不是被忽略**。
   配方在 `20260913T004000Z.md` §7.1,**不要重新设计那个测试**。
2. ⛔ **`tests/test_replay_pullcad_beat.lua` 头注仍裸述 83%/58%**(零次提到 throttle/creepthink)。
   **本轮故意没动**:改 `tests/` 会让 push 钩子的 **Lua 闸**(484s、trunk 上 9 条 known-red)参与,
   上一轮整份产物就是这样落不了地的。**这是范围取舍,不是说那个文件没问题。**
   建议与 #794 的处置**合并成一次 `tests/` 改动**,一次性承担 Lua 闸的成本。
3. ⛔ **别重跑本轮三条**:W69 的 12 局 sweep(缓存在 `.sweep_out/`,容器重启即失)、
   `pullcad_beat` 的 W69 四表、八个手读 episode。
4. ⭐ **要买 `pullcad` 的 (a),下一步是收紧域不是加局**:
   给 `creeppull_domain` 加一个**「这是打架」否决器**(施法 / 窗口内阵亡 / 距泉水),
   看 certified 人口从 278 掉到多少、域内还剩不剩统计力。**本轮开了 issue,没有自己动手**(那是仪器改动,应有裁定)。
5. ⚠️ **连续第五轮无新波次**(批测台持有于 $90 刹车线,MTD $88.692)。
   `verify_coverage.py --all` 现读:27 个 armed id **全部**至少 1 条 VERIFY 行,**零盲点**;
   verify=1 且未被章程禁止的下一批是 `fieldsip` / `fieldbuy` / `lf_rescue`(均已 WORKING)与
   `overchase`(INDETERMINATE 912)。

---

## 七、量具、成本、开工自检、铁律 6

### 7.1 量具

- **零新增仓库文件**;唯一探针 `pullcert_fight.py` **全程 scratchpad**,
  只 import 既有谓词(`creeppull_domain.Game/scan_game/episodes/canon/dist/load_sweep`、
  `pullcad_beat.poke_times/ATTACK_INFLICTORS/EP_PRE/EP_POST`),**一个都没重新实现**。
- 退出码**逐条裸读、未经管道**:`AWS_SETUP_EXIT=0`、`DUMPER_EXIT=0`(S3 缓存 **HIT**,未重建)、
  `SWEEP_EXIT ×4 = 0`、`PULLCAD_EXIT=0`、`PULLCAD2_EXIT=0`、`CERT_EXIT=0`、`PICK_EXIT=0`、
  `FRAMES_EXIT=0`、`DUTY_TEST_EXIT=0`、`VC_EXIT=0`、`VCA_EXIT=0`。

### 7.2 AWS

**只读**:`s3 ls` ×1、`sweep_run.sh` 拉 16 份 `.dem` + 16 份 `.analysis.json`、dumper 缓存 HIT。
**零 EC2 / 零 CE / 零支出**。

### 7.3 开工自检

⛔ **第一次没跑成,是本轮自己的错,如实登记**:我给它套了 `timeout 900`,
而上一轮报告**已经写明它实测 >20 分钟** ⇒ **`SELFCHECK_EXIT=124`(被我自己的 timeout 杀掉)**。
**124 不是通过,也不是发现** —— 那一次**什么都没核验**。已**不带 timeout 重跑**。

⚠️ **管道门第 8 次撞上**:本轮第一条命令 `… | tail -60` 被脚本自己拒绝
(`REFUSED: stdout is a PIPE … exit 2, nothing checked`,它自报已复发 5 次、每次都是当轮第一条命令)。
**已改用重定向 + 裸读 `$?`。**

⏳ **重跑在 push 时仍未结束**(卡在 `fast Lua detectors` 那条腿上,已跑 >40min)。
**读数收工回填到本节**(见文末「补记」);⚠️ **在回填之前,trunk 的那一侧这轮没人核过**,
这不是通过。已读到的部分:python 套件 **121 passed / 4 failed / 3 uncertifiable**,
failed 逐条 = `test_bots_walk_farm_only.py`(GH #774)、`test_carrier_terms.py`、
`test_detector_source_constants.py`(GH #787 同族)、`test_py_gate_hook.py`(GH #795)——
**四条都已各有 issue 在案,且本轮 `tests/` 一行未改 ⇒ 都不是本轮增量**;
`UNCERTIFIABLE` 三条 = `test_lua_gate.py` / `test_luacheck_gate_soakswitch.py` / `test_selfcheck_lua_leg.py`
(**腿内没跑,不是通过**)。`no armed id hangs under an unarmed gate -- OK`。

### 7.4 铁律 6

⏳ **收工回填**(读数来自 push 本身)。

### 7.5 token

⏳ **收工回填**。
