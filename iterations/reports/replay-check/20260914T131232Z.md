# replay-check 2026-09-14T13:12Z — 第七十五轮

**一句话**:交棒第 (2) 条(把 §三 钉成 fixture)动手后**当场被否掉了一半** ——
`#817` 自己建议的那个 nevermore 帧对,**结构上买不到它要断言的东西**:英雄组在
`#817` 评论里定谳,nevermore 的 t10 选中行是 **hero-unique**,而 dumper 把那一类
**在「保留已升级天赋」之前就丢掉** ⇒「10 级零消费」是这把尺子在那一行上**必然**打出的读数,
与点没点无关。**验收改钉在英雄组点名的那个可测主体上**:`obsidian_destroyer`
(t10 = `special_bonus_mp_200`,generic、可见)——**10 级整级活着攥着 4 个点,
而 `objurgation` 这条普通能力全场 1543 s 停在 0 级**。

```
VERIFY id=skillstall verdict=NOT-ARMED episodes=1778
```
(`episodes=1778` = 8 局 × 80 具身体的**完整英雄等级数**。⚠️ 按铁律 4(iii) 连切法登记:
与上一轮 `episodes=80`(身体数)、再往前 `episodes=10`/`6` **都不是同一个量,别混引**。
`skillstall` 在 W40 那棵树上不存在 ⇒ NOT-ARMED 不等于「armed 了没触发」。)

---

## 一、覆盖

- **宽扫 8 局**(达章程 6 局下限),语料 W40 = `dem21/spot_20260902_214528_1_8a5f8f31…_ab6c0d/`
  的 `2146xx` 批 slot1–8。连续第**十四**轮无新波次(S3 `soak/` 最新仍 `spot_20260912_0926*` = W67)。
- `DL_EXIT=0` 8/8,`DUMP_EXIT=0` 8/8,unparseable 0/8。
- **深查(逐帧)**:2 具身体、5 个决策瞬间钉成 fixture(§二、§三),
  外加 §四 的按等级普查是**逐帧口径**(每个等级的**每一帧**都查活/死与账本),不是聚合窗口统计。
- ⚠️ **与上一轮同波同树**(`8a5f8f3`),仍是**波内复现不是跨波复现**。slot1 与上一轮重叠,
  slot2–8 是上一轮读过的同一批;本轮读的是**不同的量**(按等级,不是按身体),不是重跑。

## 二、⛔ 先说被否掉的那一半:`#817` 建议的验收 1 **原样建不出来**

`#817` 的「建议的验收方式 1」逐字点名 nevermore `t=376.1`(lvl 9)/ `t=504.1`(lvl 11),
要断言「两帧之间英雄升到 10 级、点数增加、天赋数不变」。**帧本身全部复现**
(`#9 t=376.1 herolvl=9 requiem1` → `#10 t=504.1 herolvl=11 frenzy1`,窗口内等级 9/10/11
逐帧出现、0 死帧),**但那个断言不成立**,理由在**建议写下之后**才落进同一条 issue:

> 英雄组 `#817` 评论(`tests/test_talent_uptake_visibility.lua` 已钉):nevermore 的 build
> 在 t10 选中 `special_bonus_unique_nevermore_4`;**hero-unique 行共用 `Special_Bonus_Base`
> 类,dumper 的 `isRealAbility()` 在「level > 0 就保留」那一支之前把它丢掉**。

⇒ 点在那一行上的点**不抬能力账本、也不出现为天赋行**。
**「nevermore 10 级没花点」是这把尺子在 unique 行上必然打出的读数**,
「攥着」与「花了但看不见」**在帧上无法分辨**。
⛔ 我原本写好的第一版测试正是断言「10 级攥着 1 个点」——**那一版在断言仪器的盲区**,已作废重写。

⭐ **教训(与 W71「我取到的是不是我以为的那具身体」同族,换了一个判别子)**:
一个看起来像发现的读数,除了问「取到的是不是那具身体」,还要问
**「这个量在这个主体上**能不能**取到非零值」** —— 本轮的判别子极便宜:
**动手钉帧前先读一遍该 issue 的评论**(英雄组的答案 11:30Z 就在那里,我 12:4xZ 才读)。

## 三、⭐⭐ 验收改钉在可测的主体上:OD 的 10 级

英雄组同一条评论点名了唯一可测的那一格(「**OD 那一格仍然可测**(它的 t10 可见),
验收 3 建议**收窄到 OD**」)。本轮把**验收 1** 也收窄到它。

`20260902_214630_slot2`,`npc_dota_hero_obsidian_destroyer`,**idx=1462(全局唯一一具,已按 W71 判别子查过)**:

| 帧 | 英雄等级 | 账本 | 可见已花 | 活帧 |
|---|---|---|---|---|
| `t=467.1`(10 级**第一帧**) | 10 | orb 1 / astral 4 / **objurgation 0** / eclipse 1 | **6** | 该级 **30/30 全活** |
| `t=496.1`(10 级**最后一帧**,29.0 s 后) | 10 | **逐位相同** | **6** | 同上 |

⇒ **10 级授予 10 点、可见已花 6 点 ⇒ 手上 4 点**,整级一动不动,**全程活着**。
⭐ **这个减法在 OD 上是干净的**:10 级之前**只有能力没有天赋**(天赋从 10 级才有),
而 OD 的 t10 选中行 `special_bonus_mp_200` 是 **generic、有自己的实体类、dumper 看得见**
⇒ 不存在「花了但看不见」这条出路。

⭐⭐ **而攥着的那个点有一个完全可见的去处**:`obsidian_destroyer_objurgation` 是**普通能力不是天赋**,
**整局 1543 s 停在 0 级**。OD 这局**最后一次账本变动是 `t=342.1`(英雄 7 级)**,
此后一路打到 **24 级**,可见已花**仍是 6 点**。这与 GH #286 头注那句
(`OD stalled at 6 spent points in 5/6 games`)**同形**。

⛔ **本节不声称**:没读过 `GetHeroLevelRequiredToUpgrade()`(dumper 离线买不到),
所以「objurgation 1 级在 10 级合法」是 **Dota 档位规则不是读数**;
读到的是**授予数 > 已花数**且**一条能力在 0 级**,不是「引擎当时会接受这次点击」。

## 四、⭐ 把它从「一个瞬间」抬成「按等级的普查」:停摆堆在一个**带**里

新尺子 `tools/batch_test/behavioral/skipped_alive_levels.py`(切法与**四条 LIMIT**逐字在 docstring)。
**`skipped-alive` = 一个完整英雄等级,账本零变动,且该级**每一帧**都 `hp_pct>0`。**

```
games 8  bodies 80  complete levels 1778
  spent                 1075
  SKIPPED-ALIVE          588   (no ledger movement, alive on 100% of frames)
  skipped, had a corpse  115
  of the skipped-alive: 146 terminal / 442 non-terminal (a spend came later)
```

**按等级(terminal / non-terminal)**,这是本节真正的读数:

```
 1: 0/27 ⚠️artifact   9: 5/0    15: 13/47   21:  7/47
 4: 0/7              10: 5/23   16: 13/47   22:  2/47
 6: 0/5              11: 3/1    17: 13/48   23:  3/1
 7: 0/2              12: 5/2    18: 11/40   25: 13/2
 8: 6/0              13: 4/1    19: 11/45   26+: 14/1
                     14: 5/1    20: 12/45
```

⭐ **形状**:停摆不是均匀铺开的,**15–22 级每一级都有 ~47/80 具身体零消费且全程活着**
(非终局,即它们后来确实又花过点),**10 级是 28**,而 11–14 级只有个位数。
⛔ **不把这个形状读成解释**:15 级以上恰好既是「4 条能力见底」又是「天赋档位」,
本轮**没有判别证据**说是哪一个,而且 LIMIT 2 正好咬在这里(unique 行不可见)。

**四条 LIMIT(引任何一个数必须连它一起引)**:
1. ⛔ **它数的是「攥着」不是「合法」**——不证明那一级存在合法可花的目标;
   「非终局」比看上去弱(12 级跳过、15 级才花,可能就是在等档位)。
2. ⛔ **天赋盲区按英雄不均匀**(unique 行看不见)⇒ **各英雄的计数彼此不可比**,
   那张「每英雄」表只作选点用,**不作结论**。
3. ⛔ 同树全员普查、每局由构造含两队 ⇒ **无腿可分层,如实登记「没有 ab/ba 读数」**(铁律 4(i-a))。
4. ⚠️ **1 级那一行是 artifact,引用前删掉**:spend 定义为「比上一帧涨」,
   而 **80 具里 50 具的第一帧已经带着训练过的技能**。含 pre-horn 帧把假阳从 **77 压到 27**,
   **没有压到 0**。其余等级干净。

## 五、落地与闸

- **新测试** `tests/test_skillstall_banked_point_frames.lua`,**5 绿**:
  §1 两个主体各自「整级账本冻结 + 两端活着」(纯读数,**不碰攥没攥着**);
  §2 **验收本体**,OD 4 点在手 + objurgation 0 级(带正控制:同一个谓词在同一世界读到 astral=4);
  §3 nevermore 边界帧(账本恰好动一次,作 §1「冻结」的正控制);
  §4 **⛔ 拒绝本身钉成断言** —— 从源码重算两个主体的 t10 选中行,
  断言 OD 的是 generic、nevermore 的是 unique。**dumper 哪天看得见 unique 行,这条自己变红,
  问题就从散文变成可买的读数。**
- **5 个 fixture** 落 `tests/fixtures/skillstall/`(3 nevermore + 2 OD)。
- ⭐ **变异台 7/7 全杀,但那是第二版**:第一版**两个幸存**,两个都被采纳为改进而不是解释掉 ——
  (M2) 把两端指向同一个文件时「账本没动」**平凡为真** ⇒ 补断言两帧**相隔 29.0 / 59.0 s**;
  (M3) 把一条 shadowraze 塞进 solo 名单**双计**而无人发现 ⇒ 补断言 nevermore 可见已花 **== 9**
  (§3 原先只读 delta)。**连锁技能不变式此前是白写的。**
- **census 两向实测**:5 个 fixture 贡献 **47** 个物品名,**47 个全部已在语料里**
  ⇒ `test_slotdust_dust_arbitration` 的两个棘轮**守在 124 / 25 未动**(与上轮 bristleback 对把洞从 23 顶到 25 不同)。
- **上闸**:`lua_gate_manifest.json` **手工加一行**(`in_gate: true, fast, 0.300s`,三次实测 .310/.292/.296);
  ⛔ 没重跑 `lua_gate_measure.py`(它会重写整份清单,非本组范围)。
- ⛔ **`bots/`/`game/` 一行未改,零新 soak id**。

**铁律 6 三条腿(裸读)**:
```
luacheck bots game: 0 warnings
GATE_EXIT=0  CLEAN
py gate: 86 ran, 0 findings, 0 uncertifiable, 12.1s
lua gate: 374 ran, 0 findings, 0 uncertifiable, 8 unanswered, 7 known-red, 593.5s
```

## 六、issue

**净增 0**。`#817` **追一条评论**(验收 1 已交货、主体换人的理由、§四 的按等级读数)。
⛔ 全程**不碰 issue 状态、不 reopen、不用 `issue_write(update)`**(W71 那次把正文覆盖成
`PLACEHOLDER` 的事故就是用错了它)。评论**在 push 之后**发(GH #290 顺序)。

## 七、AWS

`AWS_SETUP_EXIT=0`。**只读 S3(8 个 `.dem`,~180 MB),零 EC2 / 零 CE / 零支出。**

## 八、开工自检

⛔ **第一跑踩管道门**(`SELFCHECK_EXIT=2 REFUSED`,**没跑成不是通过**)。
⛔⛔ **第二跑踩了章程 09-13 那条逐字写着的坑**:我写了 `timeout 600 bash …`,得 **`EXIT=124`**
—— **124 不在它的退出码词表里**(0 干净 / 2 未核验 / 3 有发现),它是**跑了一半被砍**。
章程那条写的是「**不前台、不管道、不套 `timeout`**」三条,**我一轮之内踩了后两条中的两条**
(第二次踩 `timeout`,**连续两轮**)。第三跑按唯一正确那一行
(`nohup … > /tmp/rc/sc2.log 2>&1 &`)重开,**收工时仍在跑**(python 腿之后、Lua 腿中途),
⇒ ⚠️ **本轮自检未出横幅,如实登记为「这一侧本轮没人看过」,不读成通过**;
下一轮**第一件事**先看 `/tmp/rc/sc2.log` 的横幅(⭐ 上一轮的教训仍然成立:
「还没打横幅」不是「不会打」,结论要等进程真的退出)。

## 九、下一轮第一件事

1. ⛔ **别重做本轮任何一项**;`#817` 验收 1 **已交货**(在 OD 上),尺子已进仓。
2. ⭐⭐ **最便宜的下一格是把 §四 那个「15–22 级带」切开**:
   对**每个身体**算「最后一次能力(非天赋)账本变动的等级」,
   看 15+ 的停摆是「4 条能力见底、只剩天赋」还是「能力还没满也不点」。
   **纯离线、不需要新 `.dem`、不需要新波次**,8 局 timeline 已在手(⚠️ 容器会回收,要重下重 dump)。
   ⛔ 结论必须配 LIMIT 2(unique 行不可见 ⇒ 只有 generic 主体能下结论)。
3. ⚠️ **OD 的 24 s 残差**(上一轮 `span` 1163.0 vs 1139.0)**连续第二轮挂账**,便宜但不值得单独下 `.dem`。
4. ⚠️ 09-13T16:30Z 的 **n=1 复现连续第七轮挂账**,等新波次(已第十四轮无波)。
5. ⛔ **引本轮任何数必须连切法一起引**;§四 的**每英雄那张表不作结论**(LIMIT 2)。
6. ⛔ **动手钉任何帧之前,先把该 issue 的评论读完** —— 本轮就是这一步省下来的钱最贵。

## 十、Token 用量

见下节收工回填。
