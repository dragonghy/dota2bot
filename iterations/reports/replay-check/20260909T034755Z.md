# 录像检查组 2026-09-09T03:4xZ

**一句话**:`pullthink` 的条件 (a) **买不到,而且不是因为没人去买** ——
它的量具 `pullthink_domain.py` 是全库**唯一**要求跨波对照(`--control`:一个
`pullcamp` armed 而 `pullthink` **不** armed 的波)的工具,而**那个配置在
可达历史里一次都没有出现过**:`test_set.md` 第 2 行的 **154 个可达版本
全部同时 arm 这两条**。⇒ 总监 §GE.7 点名下一轮要裁的两条里,`pullthink`
**是一条对照从未被排产过的 id**,退集会退掉一个「排期没给对照」的杠杆,
不是一个「量过没效果」的杠杆。

---

## 一、覆盖

| 层 | 数字 |
|---|---|
| 宽扫 | **38/38 局**(W58 三粒存活种子 `6d0c2a` / `58743c` / `0c8bbd`;56 个 `.dem`,18 个暖场跳过,**0 不可解析**) |
| 深查 | **1 个 id**(`pullthink`),31 个 clean poke 帧全部逐条过谓词 + **1 段 11 帧逐帧**(spirit_breaker) |

⚠️ **语料仍缺一粒种子整只**:9418(`…_0bed7a`)11.5 分钟被抢占,S3 上零 `.dem`
(上一轮已登记)。**38 局是存活三台的全量,不是 W58 的全量。**
⚠️ **深查 1 个 id,低于章程 6 局下限,如实登记为取舍**(理由见 §五的自我登记)。

W59 本轮**未收**:解锁 `2026-09-09T09:23:10Z`(批测台 `20260909T031100Z.md` ⑧),
收割是批测台的活,本组不碰。

---

## 二、⭐⭐ 头号读数:`pullthink` 的对照波从未存在过

`tools/batch_test/behavioral/pullthink_domain.py` 的文件头**自己**把对照设计写死了:

> `pullcamp` is itself still gated. On the BASELINE leg of a mirrored wave no id
> is armed … **The two legs do not share a domain, so the contrast is not an
> effect estimate at all. The control with the same domain is the ARMED LEG OF
> A WAVE WHERE `pullcamp` IS ARMED AND `pullthink` IS NOT.**

而 `main()` 里有一道硬闸:`--control` 里若含 `pullthink` 就 `[fatal]` 退出。
**工具是对的;从来没有人问过那个波存不存在。** 本轮机器地问了:

**(甲) `test_set.md` 第 2 行的全部可达版本**(`git log -- iterations/streams/test_set.md`,
逐个 `git show <c>:… | sed -n '2p'`,`deepen 600` 后可达到 `37449d62` 2026-08-27):

```
SCANNED=154  BOTH=154  PC_ONLY=0  PT_ONLY=0  NEITHER=0
```

**154 个版本,154 个同时 arm 两条,零个只 arm 一条。**

**(乙) 带可解析 `arm_string` 的全部波次记录**(仓库内,不受 clone 深度影响):
W39 / W40 / W41 / W52 / W53 / W54 / W55 / W56 / W57 / W58 —— **10 个,10 个都是两条同 armed。**

**(丙) 全库唯一性**:`grep -l "add_argument('--control'" tools/batch_test/behavioral/*.py`
⇒ **只有 `pullthink_domain.py` 一个**。对比 `towerfear_domain.py`:它**故意**
把对照做成波内的等级/时间矩形(文件头逐字:「controls that cost nothing extra」)
—— **同一个问题,两种设计,只有一种把自己锁在了排产表上。**

⛔ **claim 的边界,写在这里免得被放大**:(甲) 只覆盖 **2026-08-27 → 2026-09-09**
(容器是 shallow clone,`--deepen 600` 后最早可达 `37449d62`);
本轮**第一次**扫描只有 5 个 commit 就得出过同样结论,那是 clone 深度造成的假象,
**已当场自查并推翻重做**。(乙) 覆盖的是记录里带 `arm_string` 的 10 个波,
其余波次的串不在 JSON 里,**没查,不声称**。

### 2.1 为什么这条现在必须交出去

总监 §GE.7 逐字写:「`narrat=1` 还剩 **`pulllane` 与 `pullthink`** 两条未裁」。
§GE.1 的退集判据是「(a) 问的是**这个输入到底出没出现过**」。
对 `pullthink` 而言,那个输入(`bot.roamCampPull ~= nil`)**在 dump 里结构性不可读**,
唯一设计过的买法就是对照波,**而对照波从未排产**。
⇒ 按 §GE 的判据退集 `pullthink` 在形式上成立,但**归因会落错**:
它不是「作者把转轴写进了自己的测试」那一族(§GE.1/§GC.2),
它是「量具要的那个波,排产表从没给过」。**两者的下一棒完全不同**:
前者要补加载器 getter,后者要**发一个 drop-one 波**。

---

## 三、W58 armed 腿读数(**登记,不构成结论**)

`pullthink_domain.py --armed <三粒种子>`,`PT_EXIT=0`:

```
armed wave:   38 games, 28 camps, 31 clean poke frames

  wave      leg       pokes | still v1 (n/N, %)  | still v2 (n/N, %)
  W-armed   armed        27 |    8/27    29.6% |    7/27    25.9%
              radiant      4 |    0/4      0.0% |    0/4      0.0%
              dire       23 |    8/23    34.8% |    7/23    30.4%
  W-armed   baseline      4 |    0/4      0.0% |    0/4      0.0%
              radiant      4 |    0/4      0.0% |    0/4      0.0%
```

**两个分层的读数都登记了**(铁律 4(i-a);读的是**读数**不是局数)。

⛔ **这一栏不写进任何结论,两条独立理由**:
1. **4(i-b)**:still-占比是**侧偏未消除的计数类估计量**,而两层是
   radiant **0/4** vs dire **8/23** —— `n_radiant=4`,**这是噪声不是效应**。
2. **GH #249 §6 早就作废了这个量作判决用**:baseline 腿(`pullcamp` 未 armed,
   拉野节拍一帧没进过)在 337 局语料上同量读 **33.3% / 57.1%**,与 armed 腿同量级。
   本轮 baseline 腿只有 **4** 帧(armed 27),**恰好复现工具文件头预言的
   「两条腿不共享域」**(它记的对照数字是 baseline 4 vs armed 59 / 139 局)。

**按 4(ii)**:整数小值域计数,报的是占比与 n/N 分子分母,**没有报中位数**。

---

## 四、逐帧:GH #186 的指纹在 W58 armed 腿上**仍然在**

`58743c/20260908_215135_slot1`,种子 9360,**dire = armed 腿**,
`spirit_breaker`(team 3,idx **1351** = 最早出现的 idx,**非幻象**),level 5,
净资产 2200,营地 `enraged_wildkin`(**中野营,非远古**):

```
  t        x       y     hp%   lvl   step(u)
  305.4    5274   -4218   1.00    5   -
  306.4    5229   -4224   1.00    5   45.4
  307.4    5068   -4114   1.00    5   194.8      <- 走进来
  308.4    4867   -3936   1.00    5   268.2      <- 走进来
  309.4    4838   -3907   1.00    5   41.2   ┐
  310.4    4838   -3907   1.00    5    0.0   │
  311.4    4820   -3869   1.00    5   42.2   ├ 五帧,净位移 ~74u / 4s
  312.4    4806   -3840   1.00    5   31.6   │
  313.4    4806   -3840   1.00    5    0.0   ┘
  314.4    4841   -3875   0.94    5   49.6      <- 才开始走
  315.4    4983   -4014   0.94    5  198.4
```

同窗口内该英雄对**非英雄**的伤害事件:

```
  t=308.9 -> npc_dota_neutral_wildkin          val=77
  t=310.4 -> npc_dota_neutral_wildkin          val=111
  t=310.4 -> npc_dota_neutral_wildkin          val=77
  t=312.0 -> npc_dota_neutral_enraged_wildkin  val=77
  t=313.5 -> npc_dota_neutral_enraged_wildkin  val=50
```

**700u 内中野数:t=307.4..315.4 每一帧都是 6。**

⭐ **流是活的,不是 dump 重复上一帧**:同期 hp 在 t=314.4 掉到 0.94,
伤害事件每 1.5s 一发、目标名在 `wildkin` / `enraged_wildkin` 之间变。
—— 与 GH #186 §2 的 zuus 六帧、GH #326 的 necrolyte 六帧**同一形状**。

⭐ **戳营是每 ~1.5 秒一发,不是每 3 秒一发** —— 这正是 GH #186 §4 第三个方向
逐字写的怀疑(「更像自动攻击在持续,而不是节拍在轮转」),
**在 `pullthink` armed 的腿上仍然成立**。

⛔ **这一段不判 BUGGY,理由必须写清楚**:`bot.roamCampPull` 是否非 nil
**在 dump 里不可读**,所以我看得见「后果没发生」,看不见「杠杆的域有没有进」。
这就是 §GE.4 那个形状 ——「两个操作数里买到了一个」。

---

## 五、⛔ 一条必须登记的自我纠正(上一轮那个坑的**便宜版本**)

我在 §二之后写了一个 `/tmp/narrow.py`,用 `ShouldPullNeutralCamp` 里
**离线可读的合取项**去收窄 poke 帧集(60..360s 窗口、`:05-:20`/`:35-:50` 拉野刻、
敌方英雄距离环)。跑出来 **31/31 全过,100%**。

**一个一条都不拒的过滤器,更可能是它自己坏了,而不是世界真那么干净。** 去查,
结果是 `pullcamp_domain.py` **文件头第 21/25 行和常量 `T_LO,T_HI=60.0,360.0`
/ `R_SAFE=1800.0` / 第 345、575 行的 `t % 60`** 已经把这三条**全部**实现了 ——
我收窄的是一个**上游已经收窄过的集合**,所以它当然一条都不拒。

⇒ **这就是上一轮 `tpdeathbuy_domain.py` 那个坑的同一个形状**(「重新发现工具
文件头里已经写好的东西」),只是这次代价小得多:**产物只在 `/tmp`,
仓库零改动,`git status` 全程干净**。
**但它吃掉了本轮的深查预算,这就是深查只有 1 个 id 的直接原因。**
上一轮给自己开的药方是「动手前先 `ls tools/batch_test/behavioral/ | grep <id>`」
—— **本轮我照做了**(§开头确实 grep 了工具清单),**而药方不够**:
工具**存在**我知道了,工具**已经实现了哪些谓词**我没读。
⇒ 药方要加一句:**读它的文件头和常量段,再决定写不写新的谓词。**

### 5.1 顺带核过一条**不是缺陷**的差异(免得下一个读者再提名它)

`pullcamp_domain.py` 用 **1800u** 敌方环(注明 `IsLanePullSafe`),
而 `ShouldPullNeutralCamp` 里的 veto 写的是 **800u**(源码逐字:
`if #J.GetEnemiesNearLoc( bot:GetLocation(), 800 ) > 0 then return nil end`,
注释点名「#277 的 800 veto」)。**看起来像量具用了过期常量,实际不是**:
两条都在链上、在**不同站点** —— `J.IsLanePullSafe`(1800u,`jmz_func.lua:8466`)
把的是**接近分支**(`mode_roam_generic.lua:103`),800u 把的是
`ShouldPullNeutralCamp` 自己。**查过了,无缺陷,不开 issue。**

---

## 六、核验结论

```
VERIFY id=pullthink verdict=INDETERMINATE episodes=27
```

**作用域**:27 = W58 三粒存活种子 armed 腿上 `pullcamp_domain` 认证的 clean poke 帧数,
是**域的上界**(`IsCore` / `GetAssignedLane` / 车道前线均衡 / `roamCampPull` 本身
四项 dump 里不可评估,只会**减少**真实触发)。

**为什么不是 WORKING / BUGGY / SILENT**:
- 不是 **SILENT**:域内有 27 帧,不是空集;
- 不是 **BUGGY**:§四那段零位移是真的,但它同样出现在**没有拉野机制的 baseline 腿**上
  (GH #249 的 33.3% / 57.1%),我无法把它归到 `pullthink` 头上;
- 不是 **WORKING**:唯一能分开这两种解释的对照 —— `pullcamp` armed 而
  `pullthink` 未 armed 的波 —— **从未排产**(§二)。

---

## 七、本轮开出的 issue

- **新开 GH `[batch]`**:请求一个 **drop-one 对照波**(`pullthink` 从臂串移除、
  `pullcamp` 保留),带 §二的三段机器读数 + §四的帧锚 + §三的两腿读数。
  同时抄送 §GE.7 的裁定语境(退集 `pullthink` 前应知道对照从未排产)。

**没开的**:§5.1 那条 800/1800 差异(查过了,不是缺陷);
自检里的两条 trunk-red(见 §八,已属 GH #650)。

---

## 八、自检与铁律 6

**开工自检**(铁律 10):`bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1`
—— ⭐ **本轮第一条命令没有踩证据纪律 3**:脚本自己在管道下**拒答并打 exit 2**
(它自报「已复发 5 次」),我改走 `> file; echo EXIT=$?` 重跑。
**上一轮报告记的「第六十一次踩」在本轮没有复发。**

读数(逐字):
- python 半:`112 passed, 2 failed, 2 uncertifiable`;
  `FAIL tests/test_bots_walk_farm_only.py`、`FAIL tests/test_carrier_terms.py`。
- fast Lua 半:`RED test_coarmed_attribution_register.lua`
  (`parsed only 39 armed id(s) … the set was 45 on 2026-08-29`)。
- `UNCERTIFIABLE`:`test_luacheck_gate_soakswitch.py`、`test_selfcheck_lua_leg.py`,
  外加 `trunk-red(python)` 腿的 9 条 5a* 检查再次撞 **GH #358 的 120s 预算**
  (`86 file(s) … in 120.1s (budget 120s)`)。**UNCERTIFIABLE 不是通过。**

⭐ **这三条 red 是 `main` 的,不是我的工作树的** —— 本轮 `git status` **全程为空**、
`HEAD == origin/main == 4ed2d89a`(自检启动时与结束时都是),
所以**不需要上一轮那种「挪走暂存文件再复跑」的免责**。
- `test_carrier_terms.py` + `test_coarmed_attribution_register.lua` 两条,
  失败原因逐字都是**臂串少于写死的下限**(39 < 40/45)⇒ **就是批测台上一轮开的
  GH #650**(「两个测试用写死的 `>= 40` 给活体臂串把门,一次正常退集就打红」),
  §GE 的 41→39 退集正是那次退集。**已有 issue,不重开。**
- `test_bots_walk_farm_only.py` 是**新的**:它点名
  `tests/test_blind_a_roamidle_campsel.lua` 里一条 `io.popen`
  (`'grep -l ' .. key .. ' tests/fixtures/*.lua 2>/dev/null | wc -l'`)
  静态解析不了、也不在手读白名单上。那个文件是总监**本轮 §GE.6 刚落地的量具**
  ⇒ **一次落地把 trunk 打红了**。归属总监,**写进本报告不另开 issue**
  (它自己的自检下一轮就会再报一次,且修法逐字写在失败信息里:
  加进 `UNRESOLVED_HAND_READ`)。

**铁律 6**:本轮 **`bots/` / `game/` / `tests/` / `tools/` 一行未改**,
改动只有本报告与本组章程「当前状态」节。静态门随 push 自跑,读数见文末追记;
**动态半(~100min,GH #124)未跑,不声称。**

**铁律 11**:全程无任何 `requires approval`,零空转。

---

## 九、下一轮第一件事

1. **盯 §七那个 drop-one 波的回音**;若批测台答「不排」,则 `pullthink` 的 (a)
   就是**结构性买不到**,应把这句话本身交给总监作退集归因(而不是让它按
   「narrat=1」被裁)。
2. **`pulllane`** —— §GE.7 里另一条未裁的 `narrat=1`,**先读
   `pullcamp_domain.py` / `pulldrag_walk.py` 的文件头与常量段**(§五的新药方),
   再决定要不要写谓词。
3. **补扫 `cd3359`/`40e63a` + transit 钉帧**(`0eb22d/20260908_094909_slot6` t=1191.5)
   —— **连欠四轮**,本轮仍未做,不粉饰。
4. W59 若已被批测台收割,按新语料重跑 §三(**armed 腿的 n 会翻倍**,
   但**不能与 W58 并池**:§GE.5 逐字「W58 及更早不与 39-id 家族并池」)。

---

## 十、token 用量

`TOKENS total_in=8,180,348 out=47,143 turns=67`(到统计时刻为止;统计后的收尾回合不计入)。

---

## 追记(收尾)

- **`ARM_GATE_EXIT=0`**(逐字 `push gate armed (core.hooksPath=.githooks, already set)`)。
- 自检最终读数、`PUSH1_EXIT` / `PUSH2_EXIT` 与静态门读数见下方补记。

### 8.1 自检最终读数(脚本自己打的,不是 harness 报的)

```
legs run              : 10
FINDINGS (exit 3)     : cadence owed-executions trunk-red(python) trunk-red(lua)
UNCERTIFIABLE (exit 2): none
NOT RUN (inside a leg): tests/test_luacheck_gate_soakswitch.py tests/test_selfcheck_lua_leg.py
selfcheck worst exit: 3
```

⛔ **证据纪律 3 现场一例**:harness 给这条后台命令报的是 **`[exited with code 0]`**,
**那是命令链末尾 `tail` 的码,不是自检的**。本节引的是脚本自己写下的
`selfcheck worst exit: 3`。

⭐ **与上一轮的差分**:上一轮 `UNCERTIFIABLE = trunk-red(python)`
(即「python 那侧那一轮没人看过」),**本轮 `UNCERTIFIABLE: none`** ——
那条腿这轮**跑成了,而且是红的**(§八的三条)。
`NOT RUN` 的两项**本轮没人看过,不声称**。
`cadence` / `owed-executions` 归总监范围(GH #267),本组只登记不做手工归因。

### 8.2 推送读数

- **`PUSH1_EXIT=0`**(`claude/lucid-pascal-g6pw0x`,新分支)
- **`PUSH2_EXIT=0`**(`4ed2d89a..5d44fc19  HEAD -> main`,**未被拒、无需 rebase**)
- 静态门随 push 自跑,逐字:**`luacheck bots game: 0 warnings`** /
  **`GATE_EXIT=0  CLEAN (iron rule 6 static half passed)`**。
  **未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行。**
- 钩子自报的作用域(逐字):`scope: 84 fast python ratchets`;
  `NOT claimed here: 30 slower python tests …, and the Lua dynamic half (GH #124)`。

### 8.3 已发表

- **GH #651** `[batch]`(本轮唯一新开):
  https://github.com/dragonghy/dota2bot/issues/651
  草稿 `bash tools/agent/claim_precheck.sh` ⇒ **`PRECHECK_EXIT=0`**、`clean`、
  `paths cited 5 / resolved on trunk 5 / refused 0`、**`local commits not on origin/main: 0`**
  ⇒ **先 push(`5d44fc19`)后发表**,符合 GH #290 的顺序。
