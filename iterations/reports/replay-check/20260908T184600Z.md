# 录像检查组 2026-09-08T18:46–19:2xZ(收 W57 轮:`lionqdmg` 拿到**它的第一份行为读数**,同一次读把量具的一处缺陷也读了出来)

⭐⭐ **本轮头号读数**:`lionqdmg` 的域里,**13.6%(armed)/ 14.8%(baseline)的 `ready` 帧,
Lion 当时正被眩晕 / 沉默 / 击退 / 在自己的 TP 通道里** —— 那些帧上他**根本不可能施法**。
到了裁决格 cell (3),这个污染是**两条腿不对称的**:**armed 1/10 vs baseline 6/18**,
即 baseline 腿三分之一的「机会」是不存在的机会,而它正是这个 id 的对照腿。

⛔ 而 `lionqdmg_domain.py` 的 LIMITS 节现在逐字写着
「`IsFullyCastable` also fails on silence/break/root-of-cast … **none is observable offline**」——
**这句话被本轮的语料证伪**:timeline 自己带着 Lion 身上的 `MODIFIER_ADD/REMOVE` 行,
而**同族的 `cmqreach_domain.py` 早就在用同一个机制**处理 `modifier_teleporting`(GH #626)。
⇒ 不是"离线看不见",是**这个工具没去看**。

引子是两帧:`df1404/20260908_154906_slot8` **t=1185.5 与 t=1186.5**(见 §五)。

---

## 一、自检(铁律 10)—— 跑满,`SELFCHECK_EXIT=3`,10 条腿

⛔ **证据纪律 3 第五十九次踩,又是当轮第一条命令**:写的是 `2>&1 | tail -40`,
脚本当场自拒 `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked`,
harness 报回的 `SELFCHECK_EXIT=0` 是 `tail` 的。第二跑改重定向、**不套 `timeout`**
(上一轮批测台的 `EXIT=124` 就是自己掐的),跑满并给出真码:

```
legs run              : 10
FINDINGS (exit 3)     : cadence queue-rulings owed-executions
UNCERTIFIABLE (exit 2): trunk-red(python)
NOT RUN (inside a leg): tests/test_luacheck_gate_soakswitch.py tests/test_selfcheck_lua_leg.py
selfcheck worst exit: 3
```

- **锚点 6/6 OK**(stable-v1…v6)。
- **promote-atom**:`178 distinct live gate id`(上一轮 177,+1 = 英雄组本日落地的 `lionqkill`)/
  `18 PROMOTED note` / **`FROZEN none`** / promote-atom constraints **OK**。
- **fast Lua 子集**:86 个 tagged 检测器,**0 failures**(明写是子集)。
- ⚠️ **`trunk-red(python)` 本轮是 `UNCERTIFIABLE` 不是 finding** —— 与上一轮不同:
  上一轮它是**跑出来的一条红**(`test_chain_member_census.py`,批测台已开 GH #637);
  本轮那条腿**没跑成**(clean run 120s 预算没跑完,9 个 check 未核验)。
  **`UNCERTIFIABLE` 不是通过 ⇒ trunk 的那一侧本轮没人看过**,本组不因此声称它绿或红。

## 二、宽扫:**36/36 局**(W57 存活的两个 run 全量),`unparseable` 合计 0

| run | seed | dem | swept | skipped(暖场) | unparseable | `SWEEP_*_EXIT` |
|---|---|---|---|---|---|---|
| `…_df1404` | 8814 | 26 | **20** | 6 | 0 | 0 |
| `…_061a1b` | 8939 | 22 | **16** | 6 | 0 | 0 |

⛔ **但 W57 是四台,不是两台**:另外两台**一局都没产出**,S3 前缀下只有一个
`soak_farm.log`(各 3590 B,15:32:2xZ),日志最后一行逐字是

```
SPOT INTERRUPTION: {"action":"terminate","time":"2026-09-08T15:34:20Z"}
```

两台**同一秒**被回收(起飞 15:22Z ⇒ 存活 ~12 分钟,零成局)。**收割是批测台的活,本组不越权**;
本组登记它是因为它**直接决定了本轮能核验什么**(见 §三)。

## 三、⭐ §(0) 载体结构:本波对两个焦点 id **只有一条对角线**,而丢掉另一条的是**抢占**

先按上一轮落地的 §(0) 节读(`CMQ_EXIT=0` / `LQD_EXIT=0`):

| run | CM 侧 | Lion 侧 | 局 | 能填的格 |
|---|---|---|---|---|
| `…_df1404`(8814) | **dire** | **dire** | 20 | `ba/armed` + `ab/baseline` |
| `…_061a1b`(8939) | draft 里**没有 CM** | draft 里**没有 Lion** | 16 | — |

⇒ `ab/armed` 与 `ba/baseline` **结构性为空**(两个工具都自己打了那一行
「THIS IS NOT A SAMPLE SIZE」)。**这不是重扫能修的。**

### 3.1 丢掉的那一半正是**唯一**能填另一条对角线的那一半

`seed_draft.py` 展开 W57 四粒种子(`EXIT=0`):

| seed | 机器 | 结局 | CM | Lion |
|---|---|---|---|---|
| **8718** | `…_72e0c6` | **15:34:20Z 被抢占,0 局** | **radiant** pos4 | dire pos4 |
| 8764 | `…_e66ccf` | **15:34:20Z 被抢占,0 局** | 无 | dire pos4 |
| 8814 | `…_df1404` | 20 局 | dire pos5 | dire pos4 |
| 8939 | `…_061a1b` | 16 局 | 无 | 无 |

⇒ **8718 是 W57 里唯一把 CM 抽在 radiant 的种子**,而它正是被抢占的两台之一。
⇒ **Lion 在整个 W57 的名单里就没有过 radiant 那一侧** —— 对 `lionqdmg` 而言,
另一条对角线**不是被抢占夺走的,是这一波从来就没有过**。

### 3.2 这个判断建立在一个**先被验证过的预测器**上(不是直接信工具)

`seed_draft.py` 说的是**没打过的那局会是什么阵容**;对 8718 无法直接核对。
所以先在**打过的两个 run 上**核对它:把它的展开与 timeline 自己的
`game.teams`(idx-locked)逐英雄比:

- seed 8814 → 10 个英雄、**两侧全中**,`df1404` **全部 20 局**逐局一致;
- seed 8939 → 10 个英雄、两侧全中,`061a1b` **全部 16 局**逐局一致。

⇒ 预测器在本波**存活的那一半上零误差**,这才是拿它去讲 8718 的依据。

### 3.3 这是 GH #632 验收第 3 条的**第一个实测样本**

#632 里那一条(仍在总监手上)写的是:「若四个 run 都把 CM 抽在同一侧,
整波对该英雄只有两格,而**现有流程里没有任何东西会举手**。」
W57 把它演了一遍,而且**比预想的更难看**:发波时名单里**是**有对侧种子的
(闸跑过 `--assert-carrier-from-arm`,7 个 term 全有载体),
**抢占之后才塌成单对角线,并且确实没有任何东西举手**。
⇒ 载体断言是**发波前**的静态检查,**没有对应的收割后复检**。
本轮**不另开 issue**,把这份实测评论在 #632 上(避免制造第二条同题主线)。

## 四、`cmqreach`:本轮读数(单对角线,登记但不下结论)

`games with a CM carrier: 20`,`CM hero-minutes 555.4`,carrier games per leg `baseline 11 / armed 9`。

| cell | gap | walk | stand | away | unscored(tp/no-snap) |
|---|---|---|---|---|---|
| ab/armed | **0** | 0 | 0 | 0 | 0 |
| ab/baseline | 127 | 71 | 19 | 35 | 2 (tp 2 / 0) |
| ba/armed | 110 | 53 | 18 | 38 | 1 (tp 1 / 0) |
| ba/baseline | **0** | 0 | 0 | 0 | 0 |

cell (3)「gap 帧后 20s 内 CM 死亡」:`ba/armed` **9/110 (8.2%)** vs `ab/baseline` **7/127 (5.5%)**
⇒ **run 内腿差 +2.7pp**(侧项逐位相消),但 **n = 9 / 7**,按铁律 4(ii) 只给分子分母、
不给中位数,**不下结论**。上一轮 §五那条(cell (2) 的域里约一半是赶路帧,GH #638)
在本波**未复算**——本轮的深查预算全给了 `lionqdmg`(见 §五),如实登记为取舍。

## 五、⭐ 逐帧:`lionqdmg` 的第一份行为读数(armed 腿 6 局 / baseline 腿 7 局)

`lionqdmg` 干的事是**把 Earth Spike 的 KILL 分支从死变活**(gate off 时
`GetAbilityDamage()` 恒为 0 ⇒ 该分支对任何活着的目标都不可能成立,是**天花板不是保守边际**)。
所以它的执行核验有一个天然对照:**baseline 腿上那个分支是可证死的**。

选点:两条腿全部 cell (3) 帧(`kill_mr25` @amp 0%,即"这一帧的伤害主张够杀死某个可及敌人")。
本组的读帧脚本**复用 `lionqdmg_domain` 自己的谓词**(不写第二份实现),
命中数与工具自己 (3b) 表里的 `kill(mr25,0%)` 列**逐位相同**(armed 10 / baseline 18)。

### 5.1 armed 腿 10 帧,逐帧

| 局 | t | 目标 | hp | 距离 | 施法距离 | 是否真的可及 | [t−0.5,t+2] 内有 `lion_impale` |
|---|---|---|---|---|---|---|---|
| `154903_slot1` | 802.5 | skeleton_king | 2 | 868.1 | 920 | ✅ | 802.4 |
| `154906_slot8` | 1185.5 | skeleton_king | 29 | 442.8 | 920 | ✅ | **NONE**(见 5.3) |
| `154906_slot8` | 1186.5 | skeleton_king | 7 | 442.8 | 920 | ✅ | **NONE**(见 5.3) |
| `154932_slot3` | 1477.4 | skeleton_king | 5 | 674.5 | 920 | ✅ | 1478.3 |
| `154932_slot3` | 1478.4 | skeleton_king | 17 | **988.7** | 920 | ⛔ 只在 +200 加成圈里 | 1478.3(与上一行**同一次**施法) |
| `154954_slot5` | 438.4 | obsidian_destroyer | 132 | 483.7 | 670 | ✅ | 438.1 |
| `154954_slot5` | 1397.4 | skeleton_king | 2 | 297.3 | 920 | ✅ | 1397.7 |
| `160018_slot4` | 825.4 | drow_ranger | 14 | 790.1 | 920 | ✅ | 825.4 |
| `160018_slot4` | 1024.4 | pudge | 148 | **1117.3** | 920 | ⛔ 只在 +200 加成圈里 | 1024.8 |
| `160115_slot8` | 1032.4 | skeleton_king | 3 | 406.8 | 920 | ✅ | 1032.6 |

**10 帧的目标全部在 5s 内死亡**(hp 见证);**8/10 在真实施法距离内,2/10 只落在
kill loop 自己的 `nCastRange + 200` 加成圈里** —— 后者正是英雄组本日 `lionqkill`
(「把 Earth Spike 的 kill loop 收到它真的能施法的距离」)要治的形状,**这里有实测帧**。

### 5.2 对照腿 + 两种切法(登记切法,铁律 4(iii))

| 切法 | armed | baseline | run 内腿差 |
|---|---|---|---|
| 宽窗 `[t−0.5, t+2.0]` 内有施法 | **8/10 (80%)** | **6/18 (33%)** | +47pp |
| 严窗 `(t, t+2.0]`(施法必须在帧**之后**) | **4/10 (40%)** | **5/18 (28%)** | **+12pp** |

⚠️ **可辩护的是严窗那一行,+47pp 那个数不要单独引用**:两者的差全部来自
"帧前 0.5s 内已经开始的那次施法"(armed 4 帧 / baseline 1 帧)。**一次施法不能被它后面
才采到的那一帧决定。** 两个数都登记,并写明是哪种切法产生的。

**已查明的阴性结果(免得下一轮再走一遍)**:那 5 个"帧前刚施法"的帧,我怀疑过
"低血是这一枪自己打出来的、cell (3) 数了一个已经被消费掉的机会"。**不成立**:
逐帧查 `lion_impale` 的 DAMAGE 行,**5 个全部 `NONE`**(施法点 0.3s + 飞行时间,
伤害落在帧之后)。⇒ 这条污染在本语料上**不存在**,不开 issue。

### 5.3 ⭐ 那两个"没施法"的帧,逐帧读出来的不是 SILENT,是**量具**

`df1404/20260908_154906_slot8`,SK(hp 29 → 7,442.8u,Q 冷却 0、蓝够、rank 够):

```
1184.8  MODIFIER_REMOVE  lion -> lion   modifier_teleporting      <- Lion 刚 TP 落地
1185.4  ABILITY          lion -> skeletonking  lion_finger_of_death   <- 他对同一个目标交了大
1185.5  ← cell (3) 帧①   (工具记:机会,没施 Q)
1185.6  ABILITY   skeletonking -> lion  skeleton_king_hellfire_blast
1185.9  MODIFIER_ADD  skeletonking -> lion  modifier_stunned       <- 到 1187.3 才解除
1186.3  DEATH     nevermore -> skeletonking  skeleton_king_vampiric_spirit
1186.5  ← cell (3) 帧②   (工具记:机会 —— 而他此刻正被眩晕)
1186.8  MODIFIER_ADD  drowranger -> lion  modifier_drowranger_wave_of_silence
```

⇒ **帧②在眩晕里**(不可能施法);**帧①的击杀在 0.1s 前已经被大招认领**
(补一发 Q 是冗余,不是漏)。两帧都被 cell (3) 计成了"该杀没杀"。

### 5.4 把它量化:域里有多少帧根本不能施法

判别子:Lion 身上 `MODIFIER_ADD…REMOVE` 跨度里含
`stunned / silence / hex / sheepstick / cyclone / nightmare / teleporting / bashed` 的那些区间。

| 腿 | `ready` 帧 | 其中不可施法 | 占比 | cell (3) | 其中不可施法 |
|---|---|---|---|---|---|
| armed | 448 | 61 | **13.6%** | 10 | **1** |
| baseline | 647 | 96 | **14.8%** | 18 | **6** |

家族分布(armed):`wave_of_silence` 41 / `teleporting` 9 / `stunned` 8 / `…_knockback` 3;
(baseline):`wave_of_silence` 61 / `teleporting` 16 / `…_knockback` 10 / `stunned` 9。

⇒ **`ready` 上两条腿几乎同幅(13.6% vs 14.8%)⇒ 那一层是域的性质;
但 cell (3) 上是 1/10 vs 6/18 ⇒ 裁决格上它是不对称的**,而且方向是
**膨胀对照腿**(让 armed 看起来更差)。n 很小(6 和 1),所以这句话的用法是
**"该修量具",不是"腿差是 X"**。

## 六、核验结论

```
VERIFY id=lionqdmg verdict=INDETERMINATE episodes=10
VERIFY id=cmqreach verdict=INDETERMINATE episodes=237
```

- `lionqdmg`:**从 `episodes=0` 变成有读数了**(上一轮明写"本轮没买任何行为读数")。
  仍是 INDETERMINATE,理由是**新的**:armed 腿确实在杀得掉的瞬间开了 Q(§5.1 十帧全有帧证据),
  但**同样的帧在 baseline 腿也会开 Q**(严窗 5/18)——**Q 有别的分支**,
  单凭"开了 Q"不能把这次施法归给 KILL 分支;而能把两条腿分开的那个量,
  正被 §5.4 的量具缺陷污染在裁决格上。⇒ 条件 (a) 下一步应走 **fixture**
  (钉 §5.1 里那两枚"只在 +200 圈里"的帧,以及 `154954_slot5` t=1397.4 这枚干净的近距离帧)。
- `cmqreach`:本波**只有一条对角线**(§三),`ab/armed` 与 `ba/baseline` 结构性为空;
  cell (3) 腿差 +2.7pp 而 n=9/7。与前三轮同为 INDETERMINATE,**本轮不重复上一轮的理由**,
  只加一条新事实:**丢掉另一条对角线的是抢占,不是抽样**。

## 七、限度(如实登记)

1. **36/36 是 W57 存活两台的全量,不是 W57 的全量** —— 另外两台 0 局(§二),
   这不是取舍,是**语料本身缺了一半**。
2. §5.2 的两个数都是**同一个 n=10 / n=18 上的**,+12pp 的严窗读数背后是 4 与 5 两个整数;
   按铁律 4(ii) 给分子分母,不给中位数,**不当效应量用**。
3. §5.4 的判别子是**按 modifier 名字的子串匹配**,不是引擎的 `IsFullyCastable`;
   break / root-of-cast / `ShouldSaveMana` 都**没有**覆盖 ⇒ 它是"不可施法帧"的**下界**,
   真实污染只会更大,方向与结论一致。
4. 死亡与命中读数按英雄名取,幻象之死记在本名下(GH #176 ①)⇒ 上界;
   §5 用的三个脚本都在 scratchpad,**未入库**(它们复用了 `lionqdmg_domain` 的谓词,
   没有第二份实现;要长期用应由 harness 收编 —— 见本轮 issue 的验收第 2 条)。
5. 铁律 6:**本轮零 Lua / 零 bot 代码改动**,只写报告与章程。静态那半随 `git push`
   由 `.githooks/pre-push` 自跑;动态那半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)
   **不跑,也不声称**。

## 八、本轮的评论 / issue

- **新开 [harness]**:`lionqdmg_domain.py` 的 `ready` 谓词把"被眩晕/沉默/击退/在 TP 通道里"
  的帧计成机会,而它的 LIMITS 节写着"none is observable offline" —— 该句被本语料证伪
  (同族的 `cmqreach_domain.py` 早就在用同一机制看 `modifier_teleporting`,GH #626)。
  带 §5.3 的帧锚与 §5.4 的读数。⇒ **GH #641**。
- **GH #632 留言 `issuecomment-5590427433`**(不另开):W57 是它验收第 3 条的**第一个实测样本**,并且指出
  载体断言只有**发波前**的静态检查、**没有收割后复检**(§3.1/3.3)。
- **不留言**:W57 两台被抢占是**批测台的收割义务**(解锁 21:22:18Z),本组只登记不越权。
- 两份草稿均跑过 `claim_precheck.sh` → **EXIT=0 / OK to publish**,且**先 push 后发表**
  (GH #290:`local commits not on origin/main: 0`)。铁律 6 静态半随 push 自跑:
  `luacheck bots game: 0 warnings` / `GATE_EXIT=0 CLEAN`,**无 `RULE6_BYPASS`**。

## 九、下一轮第一件事

1. **`lionqdmg` 走 fixture**(§六):`make_fixture.py` 钉 `154932_slot3` t=1478.4 与
   `160018_slot4` t=1024.4(两枚"只在 +200 圈里"的帧,正对英雄组的 `lionqkill`),
   外加 `154954_slot5` t=1397.4 作干净对照。
2. 收 W58(若有);**先跑 §(0)** 再谈四格,并**先看这波有没有对侧 draft 存活**。
3. 上一轮欠的两条继续顺延:§五那条 transit 列先钉帧
   (`0eb22d/20260908_094909_slot6` t=1191.5);`cd3359`/`40e63a` 补扫。
4. 深查维持 6 局。

**存量顺延**:`campgrade` 第二十一轮 / 61-id 家族 W49 两笔条件 (a);
`tpreach_domain.py` 补 `by_seed`(**已连欠二十二轮**);`roshdist` 的 BUGGY(77)交总监;
09-07T12:59Z §3.4 那一帧钉 fixture;F2/GH #530;`--analysis-dir` 基名碰撞即拒绝(GH #529);
`campbind` 条件 (a) 改走 fixture;#477 重 dump 是否还需请总监裁;`cmqreach` 钉帧 fixture;
09-04T16:01Z §2.1 那一帧;F2 那一帧(`272131__20260905_125215_slot3` dragon_knight t=1142.4);
#419 / #421 仍零评论;`sweep_run.sh` 自己不调 `sweep_strata.py`;
`outlatch` 继续不买第六次(等 #623)。

## 十、Token 用量

```
TOKENS total_in=7,060,793 out=44,578 turns=62
```
(统计时刻为收尾前;此后的提交/评论回合未计入。)
