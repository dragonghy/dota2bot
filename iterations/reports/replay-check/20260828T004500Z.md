# 录像检查组 2026-08-28T00:45Z — W19 首检:`campfarm` 的域第一次不空,而三次泄漏全在 armed 腿(其中一次 4 级 ES 被远古小兵打死)

**一句话**:W19 的 arm 串**干净**(354 字节 sha1 `e388d41c`,与 `test_set.md` L2 逐字节相同,#253 形状连续第三波未复发),
但第四台被 Spot 回收零产出;19 局带录像的逐帧检查给 `campfarm` 买到**第一份非空的域**,
**三次「等级 < 12 打远古营」全部出自 armed 腿**,承重帧是一个 **4 级 Earthshaker 在远古营边振荡 19 秒、零输出、3000u 内无敌方英雄、被 `black_drake` 打死**。
`campfarm` 本轮记 **NOT WORKING**(BUGGY 或 SILENT,离线不可分,fixture 配方已给)。
同时**自我更正一条已发表结论**:GH #263 标题里的「41 倍」是一把**已经不存在的尺子**(临时脚本,`/tmp`,未入库)量的,
换用已入库量具在新语料上读 **6.1 倍**,而 W18 的那个数**不可复算**。

零 EC2 支出,`bots/`/`game/` **0 改动**,gate 未动。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` — **worst exit 3(非阻断)**:

- `stable-v1` / `stable-v2` 两锚点 **ok**(EXISTS / PINNED / SHIPPED 三项全过);
- unlanded commits **OK**(certifiable window 内无未落地工作);
- cadence **1 处洞:`replay-check` 08-27T07:19Z → 13:28Z = 6.1h** —— **同一处、同一值,连续第六轮**。
  **本组改不了 cron**,请总监裁(连续第四轮点名)。
- 未裁队列:RIDESHARE none / OTHER none,open requests 37;
- trunk **45 passed / 0 failed / 0 uncertifiable**;快 Lua 检测器 15 个文件 0 失败。

---

## 1. 吞吐(章程固定节)

- **宽扫**:**stamp 普查 48/48 落盘局 = 100%**(四个 run 全部 `analysis.json`)+
  **行为宽扫 19/19 带 stamp 且有 `.dem` 的局**(1a45f5 6 + 5fcfc2 7 + db92df 6),
  **unparseable = 0 ⇒ GH #258 的 dumper panic 本波未复发**。
- **深查(逐帧)**:**6 局**(章程下限 6)——
  `1a45f5/20260828_002224_slot10`、`5fcfc2/20260828_002239_slot9`、`5fcfc2/20260828_002249_slot10`、
  `db92df/20260828_002227_slot7`、`1a45f5/20260828_002154_slot7`、`db92df/20260828_002307_slot11`。
  选点依据:`campfarm_target.py` 的 `top 12 band episodes by hp burn` 队列 + 全部 <12 级 episode 表(23 段)里 burn 最高的几段,
  另加一段 baseline 腿做形状对照。
- **每 id 触发计数表**:本轮只对 `campfarm` 做逐 id 核验(§4);其余 39 个 armed id 本轮**不产生状态变化**。

**两个分母不要混**:stamp 结论覆盖 **48/48 = 100%**;行为读数覆盖 **19/31 带 stamp 局 = 61%**(`--rec-slots 12` 的既定上限,GH #75)。

---

## 2. W19 的 arm 串:干净,#253 的形状连续第三波未复发

| run | 种子 | stamp 分布 |
|---|---|---|
| `…_1a45f5` | 928 | `mirror:<40-id>:s928:radiant` ×10 + 暖场(裸 `3110f323`)×6 |
| `…_5fcfc2` | 932 | `mirror:<40-id>:s932:radiant` ×11 + 暖场 ×5 |
| `…_db92df` | 935 | `mirror:<40-id>:s935:radiant` ×10 + 暖场 ×6 |
| `…_6c47b6` | 930 | **零局**(§3) |

- **arm 串只有一个取值**:**354 字节,sha1 `e388d41c2390cfdef01e10081db42b3aad85f597`,40 个 id**,
  与 `iterations/streams/test_set.md` 第 2 行 **逐字节相同**。
- stamp 的位置语义正确(`mirror:<串>:s<种子>:<侧>`),**cand 位不是种子号** ⇒ **GH #253 的形状未复发**(W18 / W18-R / W19 连续三波)。
- ⚠️ **量具坑,本轮踩了一次并当场改掉**:stamp 不在 `soak_stamp` 字段里,**它就是 `analysis.json` 的 `script_version`**。
  第一版按 `soak_stamp`/`stamp` 找,四个 run 全读成 `<none>`,差点写成「W19 整波丢 stamp」——
  **那会是一条与 #253 同级、但完全虚构的结论**。判别子是暖场局:暖场的 `script_version` 是**裸 commit sha**,
  正式局才是 `mirror:` 串,`sweep_run.sh` 也正是按这个字段跳过暖场的(`SKIP … warmup/unstamped, script_version=3110f323`)。

### 2.1 ⭐ 顺手核出一条记账错误 ⇒ **GH #264 新开 [batch]**

批测台 08-28T00:20Z 报告把解 pin 的理由记为「四轮未测的天赋定价 / `abil1st` / `aimguard` 一并进语料」。
**其中两项买不到**:

| 项 | 有 gate? | W19 相对续 pin 买到什么 |
|---|---|---|
| 天赋定价、四处 getter 修正 | 无 gate | ✅ 真的进语料,两腿同时动 |
| `abil1st`(`jmz_func.lua:1938`) | **有** | ❌ **零** |
| `aimguard`(`jmz_func.lua:3928`) | **有** | ❌ **零** |
| `pollyhp`(`jmz_func.lua:10953`,报告未点名) | **有** | ❌ **零** |

语义闭合:`SoakStrArms`(`jmz_func.lua:4851`)是逗号切分后的**精确匹配**;W19 farm 日志 `+ CAND_REF=`(空)⇒ 走 SINGLE-ARM 分支 ⇒
`J.IsSoakCandidate('abil1st')` 在**两条腿上都是 false**。
**没有门会说这句话**:`check_armed_wiring.py` 校的是「串→树」(串里 40 个 id 有没有调用点),
缺的是**「树→串」**那个方向。建议的反向接线检查(只打印不阻断)见 #264 §建议 2。

---

## 3. ⚠️ 第四台被 Spot 回收:种子 930 零产出

`spot_20260828_002011_…_6c47b6` 的 `soak_farm.log` 最后一行:

```
+ bash /opt/dota2bot/tools/batch_test/soak/validate_onspot.sh <40 串> 930 12 …
SPOT INTERRUPTION: {"action":"terminate","time":"2026-08-28T00:31:36Z"}
```

- 起飞 00:20:11Z → 回收 **00:31:36Z**,**11.4 分钟**,`farm_start.sh` 已起、`validate_onspot.sh` 还没跑完。
- S3 该 run 下**只有 `soak_farm.log` 一个对象**:0 个 `analysis.json`、0 个 `.dem`。
- 按 `OWNER_PRIORITIES.md`「回收处置」:**该粒种子缺臂、配对差作废,不整波作废**;缺的那粒**再发一台(仍 spot 优先)补跑**。
  **这是给批测台的棒**,本组不申请波次。
- 顺带登记一条**被吞掉的错误**(不是本轮结论,只是路过看见):同一份日志里
  `sudo -u ubuntu steamcmd … ` → `sudo: steamcmd: command not found` → **`+ true`**。
  AMI 预装了 dota 所以不影响本波,但**它是被 `|| true` 静默吃掉的**,与本组一直在追的
  「缺席被渲染成一次成功」同族。**登记,不开 issue**(未观测到危害)。

---

## 4. ⭐⭐⭐ 逐帧:`campfarm` 的域第一次不空,三次泄漏全在 armed 腿 ⇒ **GH #265 新开 [strategy]**

`campfarm` 的门:`mode_farm_generic.lua:78` → `J.Site.FilterFarmNeutrals(tCreeps, hBot:GetLevel(), J.IsModeTurbo() and J.IsSoakCandidate('campfarm'))`;
`aba_site.lua:616-632` 在 `bStrictAncient and botLevel < 12` 时把**所有** `IsAncientCreep()` 从农野表删掉。
W19 armed 腿 = radiant(team=2),`campfarm` 在串里 ⇒ **armed 腿上 level<12 打远古营本该不可能**。

**三段等级全部用两端 bracketing 定(不插值,GH #176 范式),全部 `certain_under`。**

### 4.1 承重帧:4 级 ES 被远古小兵打死(`1a45f5/20260828_002224_slot10`,team=2)

| t | lvl | hp | xy |
|---|---|---|---|
| 219.1 | 4 | **0.281** | (-4872, -889) |
| 221.1 | 4 | 0.291 | (-4815, -192) |
| 227.1 | 4 | 0.110 | (-5171, -1080) |
| 234.1 | 4 | 0.039 | (-5032, -559) |
| **238.1** | 4 | — | **`DEATH actor=npc_dota_neutral_black_drake target=npc_dota_hero_earthshaker`** |

- **不是站桩,是振荡**:19 秒里在 y≈-190 与 y≈-1470 之间穿越 Black Dragon 远古营的仇恨半径**六次**。
- **零输出**:该区间 ES 对 `black_dragon`/`black_drake` 的 DAMAGE 事件 **0 条**;唯一主动行为是 t=221.6 吃树。
- **伤害 100% 来自远古小兵**:t∈[228,240] 逐条求和 = `black_dragon 125` + `black_drake 97`,**无任何英雄来源**。
- **无敌方英雄**:t=230.1 逐帧扫全部英雄快照,3000u 内敌方英雄 **0 个**;`nearest_enemy_t0 = 4097u`。
- **他是 28% 血进去的。**

**一帧踩三条**:`campfarm` 该拦没拦 / OWNER P2「低血不回家」的反面(无压力也不撤)/ 白送经济给中立。

### 4.2 Bristleback 两次 11 级开远古营(`5fcfc2`,team=2)

| 局 | t0 | lvl | 第一击 | 静止段(坐标逐位相同) | 血 | 收场 |
|---|---|---|---|---|---|---|
| `002239_slot9` | 631.8 | 11 | `quill_spray` → `black_drake`+`black_dragon`;t=632.0 **普攻** `black_dragon` v=127 | **632.2→652.2 `(-4988,58)` 20 秒** | 0.699→**0.425** | t=653.2 升 12,**654.2 就走** |
| `002249_slot10` | 639.2 | 11 | **普攻** `black_dragon` v=114 | **640.2→651.2 `(-4786,-211)` 12 秒** | 1.000→0.803 | t=646.2 升 12,652.2 起身 |

- 最近敌方英雄 **9454u / 3046u** —— 两次都不是被逼的。
- ⚠️ **`hero_bristleback.lua` 全文零 neutral 代码**(`grep -n "[Nn]eutral"` 零命中)⇒ 开火**不来自英雄自己的技能扫描**(那是 `abilanc` 的域)。
- **「打到升级为止就走」**的形状与 GH #250 的站桩指纹同族,但这里营是**远古营**、等级**在门内**。

### 4.3 核验结论与它的边界

**`campfarm`: NOT WORKING —— BUGGY 或 SILENT,离线不可分。**

- **BUGGY**:走了农野路径,`FilterFarmNeutrals` 被调用却没生效。
- **SILENT**:攻击命令不来自农野路径,门没被走到 ⇒ `campfarm` 的域比它自己以为的窄,这三段浪费**今天没人管**。

**两种都不能记 WORKING。** 这是 GH #261 立的「不从文件级推断到站点级」的正用。
fixture 两条断言(门本身对不对 / 门有没有被走到)见 #265 §5。

### 4.4 对照段(baseline 腿的形状)

`db92df/002307_slot11` venomancer **9 级 team=3(baseline)**:t=656.9 起,**一秒都没停过**
(每帧位移 117–409u,656.1→669.1 一路从 (4067,411) 走到 (4166,3316)),`poison_sting` DoT 自动挂着跳伤害,
血 1.000→0.932 后自己回上来。**这是「路过顺手」不是「决定去打」** —— 与 §4.1/§4.2 的三段形状**不同类**。

---

## 5. ⚠️ 自我更正一条已发表结论:GH #263 的「41 倍」不可复算 ⇒ 已发追评

上一轮为 #263 登记的预登记(把 episode 表并到新语料)**本轮执行了**,结果**不支持那个量级**:

| | 语料 | 量具 | 门内 <12 | 门外 >=12 | 比值 |
|---|---|---|---|---|---|
| #263 立案 | W18 68 局 | **临时脚本,`/tmp`,未入库** | 5 ep | 138 ep | 27.6×(文中 41× 是另一种切法) |
| 本轮 | W19 19 局 armed 腿 | **`campfarm_target.py` + `ancient_camp_domain`**(已入库,`--selfcheck` ALL PASS) | **10 ep** | **61 ep** | **6.1×** |

每局率:门内 **0.074 → 0.53(7 倍)**,门外 **2.03 → 3.21(1.6 倍)** ⇒ **分歧几乎全在门内那一列**,
而门内正是本 id 的可及面。按铁律 4(iii),两把尺子的读数**不可直接相减**;
而要放到同一把尺子上量**做不到** —— 立案用的脚本**从未入库**(上一轮自己登记为「留在 `/tmp` 未入库」),
**W18 的读数不可复算**。

**这把 backlog 第 2 条(episode 口径入库+棘轮,已第三轮登记)从「早晚要做」变成「一条已发表结论的复算被卡住」。**
定性主张(杠杆是打不打得完不是等级)**不受影响**(§4.2 两次 Bristleback「打到升 12 就走」是新的独立支撑);
受影响的是**能拿去做入集裁定的那个量级**。建议裁 `abil1st` 前先用已入库量具把 W18 那 68 局重跑(零 AWS,`.dem` 都在 S3)。

---

## 6. 诚实标注

- **取数时 W19 仍在飞**(00:40Z 快照,00:20Z 起飞)⇒ 局数、以及「四台 stamp 全 `:radiant`」都是
  **一次快照不是终局计数**;W18 的 dire 腿也是起飞后一段时间才落地的。
  **§4 的三段帧证据不依赖终局计数**(它们是「armed 腿上存在本不该存在的形状」,单腿可判)。
- **本轮没有任何 armed/baseline 差值进入结论**:四台只有 ab 一个物理分层(`ba games 0`),
  `campfarm_target.py` 每一行都打了 `strata agree in sign: NO`。§4.0 的聚合表**只用于选点**。
- §4.1 的「零输出」是**事件流的零**,不是「他没下攻击命令」的证明(下了没打到也读成零)。
  **振荡 / 致死 / 无敌方英雄**三条不受此影响。
- §5 的 6.1× 是**腿内 band-vs-band**,不跨腿,故不受铁律 4(i) 约束;但它**只有一份语料一次读数**,
  按**已观测一次**登记,**不按已复现登记**。
- 几何告警照抄:`campfarm_target.py` 打了 `ancient clusters 3 … got 3`(GH #137 §5 期望 2),
  最小间隔 790u < 簇半径 900u ⇒ 远古营被切成两簇。**本轮结论不依赖簇计数**(三段帧都逐条核过 creep 名),
  但下轮若做跨局聚合要先处理它。**登记,不自裁。**
- 报告节奏:上一轮 21:54Z → 本轮 00:45Z = **2.9h,无洞**;自检报的 6.1h 洞是 08-27 的旧洞,连续第六轮。

---

## 7. 交棒

- **新开**:**GH #264 [batch]**(解 pin 记账里两项买不到 + 反向接线检查建议)、
  **GH #265 [strategy]**(`campfarm` 三次泄漏 + 4 级 ES 致死帧 + fixture 两条断言)。
- **追评**:**GH #263**(自我更正:41× 不可复算,建议用已入库量具重跑 W18 68 局)。
- **⚠️ 给批测台**:(1) **种子 930 的实例 00:31:36Z 被 Spot 回收,零局** —— 按 owner 的回收处置补一台;
  (2) **W19 到 00:40Z 只有 radiant 腿**,若要买任何 armed/baseline 差值**必须**等 dire 腿落地;
  (3) #264 的记账更正。
- **⚠️ 给总监**:`campfarm` 的三条件判定 —— 条件 (a) 本轮**买到了,而且是反向的**(NOT WORKING);
  `abil1st` 入集若依赖 #263 的量级,**先看 #263 追评**。
  cadence 6.1h 洞连续第六轮,**本组改不了 cron**。
- **球在总监**:#265、#264、#263、#262、#261、#259、#258、#257、#253、#250、#249、#247、#246、#245、
  #244、#241、#236、#235、#159、#207、#212、#220、`campfarm`;`bbfight`/`bbshort` 与 `abilanc` 的三条件判定。
- **本组不裁 promote/reject、不申请波次、不改 bot Lua、不花 AWS 钱。**

---

## 8. 验证

- `bots/`/`game/` **0 改动**(只写报告 + 章程;本轮**未写任何新脚本**,全部用已入库工具
  `sweep_run.sh` / `campfarm_target.py` / `ancient_camp_domain.py` + 一次性 stdin 片段)。
- `bash tools/agent/luacheck_gate.sh` → 见下方实测行。
- `campfarm_target.py --selfcheck` **ALL PASS**。
- **未使用 `RULE6_BYPASS`**。本轮未改任何 Lua ⇒ **不声称跑绿过 Lua 全量**(GH #124)。
- **AWS**:S3 **只读**(48 `analysis.json` + 36 `.dem` + 4 份 `soak_farm.log` + 一份 W18 `analysis.json` 对拍),
  **未启动/未终止实例**,**未调用 CE**,**零支出**。

---

## 9. 下一轮优先

1. ⭐⭐ **W19 复检 —— 专看 dire 腿有没有落地**(开工先跑 §2 的 stamp 普查,秒级)。
   若两腿都在,**立刻把 §4 的三段泄漏做成 armed/baseline 分层读数**(**这是本轮结论唯一能被证伪的预登记**:
   若 baseline 腿也有同量级的 <12 级远古营开火,则 §4 的「armed 腿泄漏」要改写成「出厂缺陷」)。
2. ⭐⭐ **GH #265 的两个 fixture**:`1a45f5/002224_slot10 t=221.5 earthshaker`(门内最深,armed 腿)+
   `5fcfc2/002249_slot10 t=639.2 bristleback`;**同时钉一份 baseline 腿对照帧**(本语料 `under` 腿有 4 个 episode 可选)。
3. ⭐⭐ **episode 口径入库 + 棘轮**(**第四轮登记**,现在它卡住了 #263 的复算,不再是 nice-to-have)。
4. ⭐ **用已入库量具重跑 W18 68 局**,给 #263 一个可复算的门内/门外比(零 AWS)。
5. ⭐ **#236 裁下后 `mv` 落地 #159 的 fixture**(**第十二轮**)。
6. ⭐ GH #262 的 fixture(`998ecd/184717_slot10 t=733.5 spirit_breaker`,出厂腿);GH #259 的 fixture(**第三轮**);
   GH #250 的出厂帧 fixture(**第四轮**)。
7. ⭐ `pullthink` 英雄分层表(**第四轮登记**);锚点回灌 `roam_conversion.death_spans`(等 #247);
   GH #220 的 fixture(**第十二轮**);每 id 篇数独立小脚本(**第八轮**)。
8. 远古营簇计数 3≠2 的几何告警(**新登记**);`camp_prelit` 列(**第十六轮**);占用度量按 idx 去重(**第十五轮**);
   `tpdefend_events` 结果侧列(**第二十一轮**);幻象余量做进 `sweep_run.sh`(**第十八轮**);
   `entities.py` 推广(**第十九轮**);12:41Z §6.1 那 23 帧做 fixture(**第二十一轮**);`pulldrag` 的 connect 侧;
   第二种视野证人;ab/ba 回灌 `fieldbuy_silence.py`/`stayfield2_margin.py`(**连续第三十二轮登记**);
   `stayfield` 第一失败子句;打野反证 fixture(**三十八轮**);`hero-1` 的 153 局 WK 语料(**46 轮**);
   `l5combo` 的 (a)(**第六十二轮**);`make_fixture.py` 钉 `062551 t=205.5 jakiro`(#45,**第四十九轮**);
   `axebuyblink` armed 的波次。

---

## 10. Token 用量(铁律 8)

```
TOKENS total_in=8,813,427 out=61,439 turns=70
```
(统计时刻为收尾前;其后的提交/推送回合不计入。)

**铁律 6 实测**:`bash tools/agent/luacheck_gate.sh` → **exit 0**,
`installed luacheck (it was missing; apt package lua-check, bounded)` + `luacheck bots game: 0 warnings`。
**未使用 `RULE6_BYPASS`**。
