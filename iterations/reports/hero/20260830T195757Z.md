# 英雄组 2026-08-30T19:57Z —— backlog -43a 的 **Crystal Maiden 方向**(连续四轮各自记着「本轮仍然没动」的那一个)

**一句话**:`X.ConsiderQImpl` 找小兵 AoE 落点的环是 **1157**,而她**能施法的环是 732**;
十三个把落点变成施法点的站点里,**英雄那 5 个全在环内,小兵那 8 个一个都没查**。
修了(gated `cmqreach`,turbo-only,**未 arm、未 promote、不是 live**),
而**「超出会怎样」本轮明确不主张** —— 那一格从 bot VM 里问不出来,走 `queue.json:hero-25`。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` —— **worst exit 0**(**裸读**,不经管道:
本轮把整条命令后台跑进文件再 `cat`,退出码由任务框架报回,
**没有**重演上一轮 `| tail` 吃掉退出码那一形)。

- `legs run 8`,`FINDINGS (exit 3): none`,`UNCERTIFIABLE (exit 2): none`
- trunk 两侧全绿:python **62 passed / 0 failed / 0 uncertifiable**;
  快 Lua 腿 **43** 个 tagged 文件 0 失败(**FAST SUBSET,不是全套**)
- `stable-v1` / `stable-v2` 锚点 EXISTS/PINNED/SHIPPED 全 ok

⚠️ 那个 **43** 是**开工那一刻**的读数,**跑在本轮新文件之前**。收尾时同一条腿重跑是 **44 / 0 red**,
**44 里包含本轮新文件**。**不要拿 43 和 44 做差去推别的事** —— 这两个数取自不同时刻的取集,
上上轮(39 vs 36)和上一轮(42 vs 39)已经各在这个坑里绊过一次,本段是第三次照实写清楚。

## 1. 选题:为什么是 CM,而不是某个 issue

- **Owner 优先项**(`iterations/OWNER_PRIORITIES.md`):四条,球分别在批测台 / 协同组 / 协同组 / 总监,
  **没有一条在本组**。
- **open `[hero]` issue**:`#173` / `#328` / `#330` 是本组前三轮自己的活;
  `#309` / `#311` / `#314` 是语料读数(等波次);`#54` / `#287` 等 `hero-22` 那批帧。
  **本轮没有带新帧证据、点名本组的新 `[hero]` issue** ⇒ 走章程工作流第 1 条的兜底路径。
- 兜底路径的轴 = backlog **-43a** 三个方向里的 **Crystal Maiden**。
  Zeus 方向:`zusstatic` / `zusbind` / `zusboltcap` / `ConsiderW` 第二消费方,已连着做了几轮;
  Lion 方向:上一轮 `lionqdmg`;**CM 方向被连续四轮各自写下「本轮仍然没动」**。本轮动它。

## 2. ⭐⭐ 主读数:这一节量的不是一个伤害数,是**一个点在不在允许集里**

`bot:FindAoELocation( bEnemies, bHeroes, vBase, nMaxDistanceFromBase, nRadius, fTimeInFuture, nMaxHealth )`
返回 `{count, targetloc}`,而**第 4 个参数是 `targetloc` 落点的唯一约束**
(`docs/BOT_API_REFERENCE.md:1366`)。`X.ConsiderQImpl` 从 `bot:GetLocation()` 跑四次:

| 搜索 | `nMaxDistanceFromBase` | 无以太之镜时 |
|---|---|---|
| 英雄-击杀 | `nCastRange` | **732** |
| 英雄-命中 | `nCastRange` | **732** |
| 小兵-击杀 | `nCastRange + nRadius` | **1157** |
| 小兵-命中 | `nCastRange + nRadius` | **1157** |

寒霜新星的 KV **每一级都是平的**(`AbilityCastRange 700`、`radius/value 425`;d2vpkr 镜像,本轮重新拉取),
`nCastRange = GetCastRange() + aetherRange + 32` ⇒
**超出量恰好是 `nRadius = 425 = 施法距离的 58.06%`**,而且**不随等级、不随出装移动**
(以太之镜给两项各加 250,**相消**;这一条不是论证,是测试里放一枚以太之镜量出来的)。

**这四个数不是手填的**:§2 的用例在**真实帧**上驱动**真的** `X.SkillsComplement`,
在 `FindAoELocation` 上挂探针,把四个环的读数**从出厂代码真正发出的引擎调用里读回来**。

## 3. ⭐⭐ 真正的立案句在**消费侧**,而且是一个 5-vs-8

十三个 `return DESIRE, <结果>.targetloc` 站点,按填它的那次搜索分:

- **英雄 5 个 —— 5 个全在环内**:4 个自己再查一次
  `GetUnitToLocationDistance( bot, loc ) <= nCastRange`(其中一个 +50);
  第 5 个读的是**另一次**跑在 `nCastRange - 300` 的搜索,**按构造**在环内。
- **小兵 8 个 —— 一个都没查。**

而 `X.SkillsComplement` 把拿到的点**原样**送进
`bot:ActionQueue_UseAbilityOnLocation( abilityQ, castQLoc )`。

**同一个函数、同一件事:英雄那半从不肯做,小兵那半八处全做。**
这个 5-vs-8 不是数出来讲的,是 `tests/…:§5` 从源码里**解析**出来的,而且**两个方向都钉了** ——
给一个小兵站点**加**守卫会红,把一个英雄站点的守卫**拿掉**也会红(变异 M7 / M8)。

## 4. ⚠️ 「超出会怎样」**本轮明确不主张**,而这是刻意的

引擎拿到一个超出施法距离的地面点,是**拒绝命令**还是**先走进射程再放**,
**从 bot VM 里问不出来**(`AGENTS.md`:`print()` 到不了服务器控制台、引擎错误处理器坏了)。
两种读法都坏,但**坏法不同**:

- **拒绝** ⇒ 小兵分支**赢下 desire 竞争然后什么也不做**,只要线上有兵就一帧一帧地空转;
- **走过去** ⇒ 一个位置 5 辅助为了清兵**主动往敌方兵线里走最多 425 码**。

**支持本 id 的论据不是猜哪一种**,而是:**同一个函数里的英雄分支两种都不肯做**,
小兵分支与它们的差别只有一个参数 + 八处缺失的检查。
域读数(四格,含**预先接受的否定结果**)走 `queue.json:hero-25`,零 EC2、零新局。

## 5. 修法,以及为什么长这个样子

`bots/BotLib/hero_crystal_maiden.lua`:新 helper `X.cm_GetCreepAoESearchRange( nCastRange, nRadius )`,
**一个调用点**(局部 `nCreepSearch`,被两次小兵 `FindAoELocation` 共读)。
armed(turbo **且** `cmqreach`)答 `nCastRange`,否则逐字答出厂的 `nCastRange + nRadius`。
`game/` **零行**;`tTalentTreeList` / 出装表**一字未动**;**没有 arm/promote/加宽任何已在集的东西**。

**为什么缩搜索半径,而不是给那八个站点补距离守卫**:补守卫会**丢掉**这次施法;
缩半径**按构造**给出一个可施法的点 —— 和第 5 个英雄站点是**同一种正确性**,
不引入新谓词、没有新守卫可写错。

**代价写在门旁边而不是藏起来**:732 的搜索**看不到比 1157 更多的兵** ⇒
每个 `count >= 2/3/4/5` 门槛**只会更难**,armed 那边清兵放新星**严格不多于**出厂;
它**从不挪动一次本来就合法的施法**。

## 6. 验证

新文件 `tests/test_cm_q_creep_aoe_reach.lua`,**16 例全绿**,带 `[ratchet]` ⇒ 进快 Lua 腿。
**端到端**:驱动的是 `X.SkillsComplement`(引擎真正调的入口),不是从中间插进去的 `ConsiderQImpl`。
真实帧 `tests/fixtures/f_260820_102645_cm_laning_release.lua`(t=556.5,CM 7 级、504/1022 血、363 蓝,
**「没有以太之镜」是从帧上断言的**,不是假设)。

- §1 ground truth + **唯一一处声明的变异**(新星冷却 7.7 → 0),并**量了**它的必要性:
  不做这个变异,真的 `X.SkillsComplement` **一次搜索都不发**。
- §2 出厂 732/732/1157/1157;425 的超出;58.06%;以太之镜反事实里超出**仍然是 425**。
- §3 armed 把两次小兵搜索压到 732;断言的是**性质**(「每一次搜索都只能返回可施法的点」),
  armed 成立、出厂**恰好 4 之 2 不成立**。
- §4 gate off **三向**:仅 turbo / armed 但非 turbo / **三个别的 id armed**(无打包泄漏)。
- §5 5-vs-8 普查从源码解析,外加「那个按构造安全的英雄站点确实是 `...Nearby` 那一个」。
- §6 helper 契约(**两个不同的施法环上各验一遍**,证明它是算术不是查表)+ 两条源码绊线
  (调用点恰好 1、两次小兵搜索都读它、turbo-only、body 里 `IsSoakCandidate` 恰好 1 个)。
- §7 两条 LIMIT,**量出来的**(见 §7 本报告)。

**变异 8 条,8 条条条见红,且只红在依赖它的节**:
M1 拆掉 helper 接线(3 红)/ M2 helper 去掉门(8 红)/ M3 helper 去掉 `IsModeTurbo`(3 红)/
M4 armed 路径变成 no-op(3 红)/ M5 两次小兵搜索里只有一次读门控半径(3 红)/
M6 一次**英雄**搜索被悄悄放宽到小兵环(3 红)/ M7 给一个小兵站点**加**守卫(1 红,§5)/
M8 拿掉一个英雄站点的守卫(1 红,§5)。对照 16/0 绿。
**还原走盘外文件副本 `cp`**(不是 `git checkout`),每次还原后 `cmp` **逐字节相同**。

**铁律 6**:
- 静态半 `bash tools/agent/luacheck_gate.sh` —— **exit 0**(**裸读**),`luacheck bots game: 0 warnings`
  (冷启容器,脚本自己装的 `lua-check`)。**一次都没用过 `RULE6_BYPASS`**。
- 动态半是**子集不是全套**(GH #124,全套 ~100min 在例行容器里跑不完),照实登记跑了什么:
  快 Lua 腿 **44 个 tagged 文件 / 0 red**(含本轮新文件);
  CM 全家 + smoke + gate 一致性 **12 个文件逐个 exit 0**
  (`cm_q_creep_aoe_reach` 16、`cm_arcane_aura_passive` 16、`cm_ability_index_binding` 20、
  `cm_cask_cap` 15、`cm_r_selfstate` 24、`cm_r_range` 15、`cm_r_selfpreserve` 5、
  `cm_t20t25_payoff` 10、`cm_pos5_boots` 19、`cm_t10_payoff` 11、`smoke_load` 3、
  `gate_claim_consistency` 10);python 全套 `tests/run_py_tests.sh` **62 passed / 0 failed /
  0 uncertifiable**(**exit 0 裸读**)。
  ⇒ **不主张「主干全绿」**,只主张上面这些腿绿。

## 7. ⚠️ 明说没做 / 证不了的

1. **没有开火侧读数,而这个「没有」是量出来的**:两枚归档 CM fixture 的
   `crystal_maiden_crystal_nova` **都是 rank 1**,而每条小兵分支都要 `nSkillLV >= 3`,
   前面还压着冷却中必假的 `IsFullyCastable()` ⇒ 本语料的分支人口**是空的,原因与冷却无关**。
   所以本文件把缺陷钉在**搜索站点**(它在每条分支的上游,也正是那个错数被写下的地方)。
   与 `lionqdmg` 同族的缺口。
2. **armed 那边少看到多少兵,本轮不测**:`FindAoELocation` 的 `count` 在本文件里是桩,
   只主张**符号**(结构性:小环看不到更多兵),不主张**大小**。
3. **两个 KV 数是外部锚**(施法距离 700、半径 425):dumper 对二者都报 0(GH #27 同族)。
   它们**通过出厂读法被断言**(§2),所以 patch 一动就会在这里红;
   但**值本身**来自游戏 KV,不来自录像。
4. **「拒绝还是走过去」没答**,见 §4;这是 `hero-25` 的主判据。
5. **⚠️ 交出去的第二个缺陷,只登记不修**:`bots/BotLib/hero_silencer.lua:304` 有同族的另一处 ——
   ```lua
   if nCanHurtCreepsLocationAoE == nil
       or J.GetInLocLaneCreepCount( bot, 1600, nRadius, nCanHurtCreepsLocationAoE.targetloc ) <= 2
   then
        nCanHurtCreepsLocationAoE.count = 0
   ```
   **那个 nil 守卫的 then 分支第一件事就是索引它刚判过可能为 nil 的那个值**;
   而且它**少了** CM 那句 `targetloc == nil` 检查,于是可以把 nil 传进 `J.GetInLocLaneCreepCount`。
   **Silencer 不在焦点五里,一次一根杆** ⇒ 只登记在
   `state.json:cmqreach_20260830.known_gap` 第 (5) 条,**不修、不开新 issue**。
6. **一条被查过之后放弃的线索,照实记下来免得下一轮重推**:`X.ConsiderW` 把冰封禁制的伤害
   **手写**成 `nDamage = ( 100 + nSkillLV * 50 )`。查了 KV:`damage_per_second` 是**平的 100**、
   `duration` 是 `1.5/2/2.5/3` ⇒ 150/200/250/300,**四个 rank 逐位相同,不是缺陷**。
   唯一能让它错的是 `special_bonus_unique_crystal_maiden_1`(+1.0s 时长),
   而 `tTalentTreeList['t20'] = {0, 10}` 取的是 `..._2`(+300 新星伤害)那一半
   ⇒ **在出厂天赋构筑下不可达**。这一条**上一轮的 `cmt20t25` 已经写在文件头 76–85 行了**,
   本轮只是独立复核到同一结论,**没有重复劳动地再写一遍**。
7. **不主张 promote**,也不主张 arm 的档期;`test_set.md §CN` 只申请**入集**。

## 8. 交付物

- `bots/BotLib/hero_crystal_maiden.lua`:新 helper + 一个调用点(**可执行改动 2 处**);`game/` 零行。
- `tests/test_cm_q_creep_aoe_reach.lua`(新,16 例,`[ratchet]`)。
- `iterations/state.json`:新增 `cmqreach_20260830`(`gated:true`、`armed:false`、`promoted:false`)。
  语义 diff 核过:**+1 键,0 删除,0 修改**。
- `iterations/queue.json`:新增 `hero-25`(归档扫描,**零 EC2、零 AWS 增量**,不申请专波)。
  语义 diff 核过:**+1 请求,已有 51 条一条未动**。
- `iterations/streams/test_set.md`:新增 **§CN**(入集提议,等总监裁)。
- 本报告 + 章程「当前状态」/ backlog 更新。

**零 AWS(连一次 S3 GET 都没有)。不申请波次。不开新 issue。不 arm、不 promote。**

## 9. 下一棒

- **本组下一棒**:`hero-25` 的域读数回来之前不再碰这条线;
  backlog 新 **-56** 已把「Silencer 那处同族 nil 守卫」和「-43a 的 Zeus 方向仍在 `ConsiderW` 第二消费方之后」
  分别写清楚,不让棒掉(铁律 9 连带规则)。
- **球在别人手上的**:`hero-25` → 批测台搭车 + 录像组读 (2);§CN → 总监裁入集。

## 10. Token 用量

`python3 tools/agent/token_usage.py` 最后一行,原样:

```
TOKENS total_in=12,143,302 out=71,104 turns=78
```

(这是**到统计时刻为止**的数字;统计之后的收尾回合 —— 本节回填、commit、push —— 不计入。)
