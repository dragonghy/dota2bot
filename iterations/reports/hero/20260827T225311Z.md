# 英雄组 2026-08-27T22:53Z —— 那个「从 dump 里完全等价」的岔口,在同一个仓库里已经有帧判掉了

**轴**:`TALENTBLIND`(续)。**棒 ③(`tests/` 等级前提登记表)开工,清掉第一行**。
**`bots/` 零可执行行改动**(`hero_skeleton_king.lua` 只加注释块);**无新 gate id**;
`state.json` 新增 `talentvis_20260827`(`gated:false`);**零 AWS**;**不申请入集**;
**不新提 queue 请求**(改为把已 routed 的 `hero-21` 就地收窄)。

---

## 0. 开工自检

`bash tools/agent/routine_selfcheck.sh` → **worst exit 3**:
UNLANDED **0**;两条稳定版锚点(`stable-v1` / `stable-v2`)**ok**;
py **45/0**;Lua 快速检测器 **14** 文件 0 失败;待裁 queue 请求 **0**(open 37);
cadence 洞**只有 replay-check(6.1h)**,**本组无洞**。
`iterations/OWNER_PRIORITIES.md` 四条:常设运维项球在批测台、P1/P2 在协同组、P3 在总监
⇒ **本组无优先项**。开着的 `[hero]` issue 里带帧证据的仍是 **#245/#244(OD)**,不在焦点五
⇒ 按章程取 backlog:**棒 ③,连续第三轮未动**。

---

## 1. 入口:棒 ③ 的一行,和四小时前自己留下的一个岔口,是同一件事

`tests/test_level_premise_registry.lua` 的 `PENDING` 表有 9 行,7 行归本组。
挑 `test_wk_bone_guard_talent_bypass.lua`(注释:`hero -- talent6 bypass reachability`),
因为它是**唯一一行的重读会改变一个已发表判决的状态**的:它的 HONEST BOUNDS 写着

> …是一个关于 **turbo 从没到过的 20 级世界**的问题(GH #84:210 个英雄槽里 0 个 ≥20)

而它的 §5 更进一步,**自己写下了重开条件**:「GH #108 把批测局时上限从 10 抬到 25 分钟,
是最可能让它变成可支付问题的改动,也是重开本条的触发器」。

**那个触发器已经扣动了,而且没人回来看。** GH #235 那枚后期帧就停在
`iterations/pending/tpgap_159_fixture/`(被 GH #236 的排序决定挡住,**不是本组的球**),
里面十个英雄 **22–27 级**,其中就有 Wraith King(26 级)。
**读一个停着的文件不花钱,也不需要 #236 先落地。**

顺手一看,这枚帧同时是本组四小时前(19:58Z / GH #260)**自己留着不判**的那个岔口的判决书。

---

## 2. ⭐ 主产出:H1(量具)成立,H2(世界)被证否 —— 而买它的是零成本

19:58Z 把两种解释并列写下、明说「从 dump 里完全等价、本轮不下结论」:

- **H1 量具**:`dumper/main.go` 的 `isRealAbility()` 在「已升级就保留」那条分支**之前**
  无条件丢弃类名含 `Special_Bonus_Base` / `_Attributes` 的实体;英雄专属天赋没有自己的
  C++ 类,共用基类 ⇒ 全被丢。
- **H2 世界**:bot 从来没点过专属天赋,点数被压着。

19:58Z 的语料**够不着判决的原因是等级**:105 个 fixture 顶到 **19 级**,而那个 19 是
10 分钟经济 cap 的伪影(GH #84 的零)。后期帧一到,问题**自己回答自己**:

| | |
|---|---|
| 十个英雄槽,等级 | **22 – 27** |
| 按出货升级队列必然已花掉的天赋点 | **36** |
| 那一帧实际显示的天赋行 | **8** |
| 其中 `special_bonus_unique_*` | **0** |
| 保守读法(只数 t10+t15) | **20 中缺 12** |

**H2 造不出这个。** H2 说 bot 只点通用行 —— 而**通用行是看得见的行**。
一个点了四个通用天赋的 26 级英雄,dump 里就该有四行。

**最尖的三格**:`jakiro` 24 级、`necrolyte` 25 级、`venomancer` 24 级,**一行都没有**。
不是「少了几行」,是**整个为空**,而这三个都已经走过 t10 / t15 / t20。

**而且 H1 是充分的**,不只是幸存的那个:`isRealAbility()` 的丢弃规则**不需要任何
关于世界的假设**就把 28 行的缺口整个解释完。

### 逐行读数(`tools/agent/lategame_talent_census.py`)

```
hero                 lvl  must  seen  rows
bristleback           25     4     1  m_p_regen150
crystal_maiden        22     3     1  h_p200
dragon_knight         26     4     2  attack_damage15, h_p300
jakiro                24     3     0  -
lina                  27     4     1  attack_damage25
necrolyte             25     4     0  -
skeleton_king         26     4     1  h_p300
slardar               27     4     1  h_p250
venomancer            24     3     0  -
zuus                  23     3     1  h_p200
```

**旁证,而且是本轮最便宜的一条**:活下来的 8 个名字**全是引擎类名的蛇形**
(`h_p200` / `m_p_regen150`,而 KV 写的是 `hp_200` / `mp_regen_150`)。
`snakeFromClass(cn, "CDOTA_Ability_")` 就在 `main.go:378`。
「通用行有自己的类、专属行共用基类」这条解释**在名字的形状上留着指纹**。

---

## 3. `must` 这一列不是观察,是模型 —— 所以它被拿去跑了出货代码

缺口的分子是观察,**分母是论证**,所以论证要能被执行:

- `X.GetSkillList`(`bots/FunLib/aba_skill.lua`)对一个 15 项技能 build,
  把**四个天赋选择放在队列位置 10 / 15 / 18 / 19**(后四项是**镜像行**,同一档的另一半);
- `bots/ability_item_usage_generic.lua` **只在 `botLevel >= GetHeroLevelRequiredToUpgrade()`
  时弹出队首**,否则把点数压着下一帧再试
  ⇒ **约束是档位等级本身**,队列位置 18 就是「20 级点上」。

所以 `must(L) = min(4, (L-10)//5 + 1)`。§1 的断言**驱动出货的 `GetSkillList` 本身**读位置,
并且从**真正执行升级的那条分支的条件里**取 `GetHeroLevelRequiredToUpgrade` 与
`CanAbilityBeUpgraded`——**不是从文件里 grep 字符串**(见 §5 的 M6)。

不信这个模型的读者也不用改判:**只数 t10+t15 的保守读法仍是 20 中缺 12**。

---

## 4. 这一条买到了什么(以及**没有**买到什么)

1. **TALENTPRICE 五轮不被作废。** 那五轮怕的正是 H2(「我们给没人点的行定了价」),
   而**被证否的就是 H2**。Axe 与 WK 的出厂 t10 都在专属那一侧,这一条对他们尤其。
2. **五轮各自写的「本仓没有一枚帧能证实它触发过」全部仍然成立**,但从**观察升格成机制**:
   量具在写之前就把这一行丢了,所以那个零**永远**是零。
3. **棒 ③ 清掉一行**:`test_wk_bone_guard_talent_bypass.lua` 的 HONEST BOUNDS 与 §5 都重读,
   §5 的重开触发器**记为已扣动**;登记表天花板 **9 → 8**。
   清行的写法按登记表自己的规矩:**不是删句子,是写下重读的结论**(新增 `CLEARED` 节)。
4. **一条已发表推断的更正,写下来而不是悄悄改掉**:GH #260 说「没有任何一帧带两个天赋」。
   **对 `tests/fixtures/` 仍然成立**;但它**不是管道的性质** —— dragon_knight 26 级在这里带了两行。
   「每个英雄最多一行」又是那个 10 分钟 cap。
5. **没有买到**:哪几行专属天赋被点了。dump 丢掉的实体**不在文件里**,离线数不出来。

---

## 5. 反真空与变异(**12 抓 + 1 对照**)

本轮**所有**结论的形状都是「某个计数偏小」——**空解析自动满足**。三道:

- **工具自己拒绝出报告**(exit 2):files <50 / 槽位 <500 / 天赋行 <20 / ≥20 级槽位 <10
  任一不满足就不打印任何 headline(实测:把扫描根指向 `tools/agent/` → `EXIT 2`)。
- **快照自洽**:`seen` 列 vs `rows` 实际长度、`must` 列 vs 档位模型、总和 == 8、每一行 ≥20 级。
- **§1 驱动出货函数**,不复述它。

| # | 变异 | 结果 |
|---|---|---|
| M1 | 快照某行 `seen` 与 `rows` 不一致 | 抓 |
| M2 | 删掉 dragon_knight 的第二行 | 抓(4 处) |
| M3 | 把一行改成 `special_bonus_unique_*` | 抓 |
| M4 | 给三个空行英雄补满天赋 | 抓(5 处) |
| M5 | `GetSkillList` 的 `i >= 10` → `i >= 12` | 抓 |
| M6 | 训练分支的 `botLevel >= GetHeroLevelRequiredToUpgrade()` → `true` | **先逃后抓** |
| M6b | 训练分支的 `CanAbilityBeUpgraded()` → `true` | 抓 |
| M7 | `main.go` 把 `Special_Bonus_Base` 丢弃**移到**保留分支之后 | **先逃后抓** |
| M8 | 快照把 zuus 降到 19 级 | 抓(3 处) |
| M9 | `GetSkillList` 的 `i % 5 == 0` → `i % 4 == 0` | 抓 |
| M11 | 出品名不再走 `snakeFromClass` | **先逃后抓** |
| M12 | 整条丢弃规则删掉 | 抓 |
| M10 | (对照)census 工具改一句注释 | 不红 ✅ |

**三条先逃的,是同一族错误的三个面,值得单独记**:

- **M6**:`GetHeroLevelRequiredToUpgrade` 这个字符串在文件里**出现两次**,第二次在
  「还是试一下」的兜底分支里。**grep 整个文件在真正的门被拿掉之后照样绿。**
  修法:**把真正执行 `ActionImmediate_LevelAbility` 的那条 `if` 的条件抠出来再问**。
- **M7**:`isRealAbility()` **上面那段文档注释按正确顺序写着这两个字符串,而且它不会动**
  ⇒ 扫原始文件等于**把自己的文档读回来当代码**。修法:**只扫函数体**
  (`func isRealAbility%b()%s*bool%s*{(.-)\n}`)。这与 `test_wk_facet_settlement.lua`
  的 `live_source` 是同一课,**换了一门语言又交了一次**。
- **M11**:`snakeFromClass` 的**定义**还在,所以「文件里提到它」永远为真。
  修法:钉**调用点**(`Name:%s*snakeFromClass%(cn`)。

**共同形状**:*断言必须运行/读取它谈论的那一个东西,而不是它的名字出现过的地方。*

---

## 6. 落地清单

| 文件 | 性质 |
|---|---|
| `tools/agent/lategame_talent_census.py` | 新;离线普查,带 exit 2 地板 |
| `tests/mock/lategame_talent_frame.lua` | 新;十行快照(停放帧的读数,带日期与来源) |
| `tests/test_lategame_talent_visibility.lua` | 新;8 条断言,`[ratchet]` 标签(进快速子集) |
| `tests/test_wk_bone_guard_talent_bypass.lua` | 重读;HONEST BOUNDS + §5,**注释** |
| `tests/test_level_premise_registry.lua` | 清一行,天花板 9 → 8,新增 `CLEARED` 节 |
| `tests/test_fixture_talent_blindness.lua` | 头注加一段「同日已判、断言不动」,**注释** |
| `bots/BotLib/hero_skeleton_king.lua` | **注释块**,把「未定」改写为已定 + 后果 |
| `iterations/state.json` | 新增 `talentvis_20260827`(`gated:false`) |
| `iterations/queue.json` | `hero-21` **就地收窄**(不改裁定、不改路由) |

**`hero-21` 为什么收窄而不关**:总监已裁 `ROUTED_ARCHIVE_REDUMP`、路由给录像组。
岔口没了,但**重新导出仍是唯一能看见 `special_bonus_unique_*` 名字的路**,
而那是唯一能回答「我定价的那一行是不是游戏里真拿到的那一行」的读数。
验收里 H2 的那一列与「若 H2 成立 ⇒ 焦点五活 bug、优先级抬到 1」那一支**已经关掉**,
已写进请求正文,免得执行方按旧判据准备。

---

## 7. 诚实边界(会跟着结论走,请连着引)

- **一帧、一局。** 36 不是速率,是十个英雄槽在一局 24.9 分钟里必然花掉的数。
- **`must` 是模型**:队列因别的原因卡住会**压低** `must`、**缩小**缺口 —— 这正是保守地板
  (只用 t10+t15)单独断言的原因。
- **机制那一条是源码读数,不是测量**:被丢掉的实体在 dump 里不留痕迹,
  **没有任何 fixture 能目击这次丢弃**;§4 钉的是「代码还在这么说」。
- **快照来自停放文件**。GH #236 的排序落地、fixture 搬进 `tests/fixtures/` 之后,
  census 会从新位置读出同样的行,**本文件不需要动**。
- **本轮没有碰模拟器,没有花一分钱,没有新 gated id。**

---

## 8. 下一棒

- **棒 ③ 继续**:登记表还剩 8 行,本组欠 6 行
  (`test_focus_build_level_legality` / `test_lion_hex_talent_slot` / `test_wk_fact_anchor` /
  `test_wk_roshan_mana_ceiling` / `test_wk_bone_guard_thresholds` / `test_wk_q_aim_preflight`),
  另 2 行(`corpus_scale` / `test_level_gate_census`)归 harness/director。
  **优先 `test_wk_fact_anchor.lua`**:它的 §4 是 t20/t25 的 STRUCTURAL 读数普查,
  **本轮的判决直接改写它的前提**。
- **`hero-19` / `hero-20` / `hero-21` 仍 pending**(21 已收窄,19/20 未动)。
- **`ConsiderQ` 的 `nDamage` 修复**(WK,域在本地语料为空)仍等归档扫描。

---

## 9. 铁律 6 的两道门

```
bash tools/agent/luacheck_gate.sh   -> luacheck bots game: 0 warnings   (exit 0)
lua5.1 tests/run_tests.lua talent   -> 70 tests, 0 failures
lua5.1 tests/run_tests.lua wk       -> 155 tests, 0 failures
lua5.1 tests/run_tests.lua level    -> 57 tests, 0 failures
lua5.1 tests/run_tests.lua premise  ->  5 tests, 0 failures
lua5.1 tests/run_tests.lua smoke    ->  3 tests, 0 failures
lua5.1 tests/run_tests.lua gate_claim -> 10 tests, 0 failures
lua5.1 tests/run_tests.lua test_lategame_talent_visibility.lua -> 8 tests, 0 failures
```

**动态那半是过滤子集,不是全量**(全量 ~4h,GH #124):跑的是本轮触及的文件及其邻域
(天赋族、WK 全族、等级族、登记表、smoke、gate-claim)。**未跑过的文件本轮没人看过**,
按 GH #171/#216 的措辞规矩明写在这里,不写成「全绿」。

## 10. Token 用量

```
TOKENS total_in=14,076,693 out=71,028 turns=91
```
