# 英雄组 — 2026-09-10T20:19Z

**工作单元**:章程 backlog `-143`(反查 Lion 的 15 级帧)。
**OWNER_PRIORITIES**:P4.4 (i) —— 主体是一个 `bots/` 改动(`bots/BotLib/hero_lion.lua`)
外加它现在**真的有了**的本地验证。P4.2 冻结期内**不请求入集**,只更新
`iterations/state.json:lionqfight_20260910`。
**AWS**:S3 只读(1 个 `.dem` + 8 个 `analysis.json`),**零 EC2、零花费**。

---

## 1. 一句话

上一轮 `lionqfight` 落地时**域是空的**,并把「拿这条支路自己的谓词回时间线反查」
写成了下一棒。本轮执行了那一棒:从 soak 局 `20260910_124853_slot1` 切出 **4 个 Lion
施法帧**,域从 **0 变成 4**,armed **扣下其中 3 发 Impale**、第 4 帧一字不改
—— `lionqfight` 从「钉空操作」变成**本地验证过的杠杆**。

⭐ **而这 4 帧买到的第一件东西,是那把正在找它们的尺子有缺陷**:两个 Lion 普查文件
都在用 **670** 的环去量一条在 **920** 上开火的支路,新帧里 **3/4 就坐在那 250 单位的
环带里**。两处都已修成逐帧的环。

---

## 2. 主体:`lionqfight` 的本地验证(§3.2 / §3.4)

**配方(hero-54 的那条,零 EC2,约 5 分钟一局)**:
`session_setup.sh` → `get_dumper.sh`(S3 cache HIT) → 用 `analysis.json` 先按
**「哪一局有 Lion 且他到了 15 级」**筛(7 局里选 Lion 24 级那局) → `behav-dump -interval 0.5`
→ **拿支路自己的合取式扫 `snapshots[]`**(`level>=15` ∧ `lion_impale.cd==0` ∧ 920 内有敌人)
→ 11 个瞬间聚成 4 簇,**每一簇的末尾都是一次真实的 `lion_impale` 施法**
(`events[]` 里 t=1129.8 / 1344.5 / 1357.1 / 1416.0)→ `make_fixture.py --t <施法戳>`。

⭐ **先量后切,又便宜了一个工作单元**:先按 `analysis.json` 的 Lion 等级选局,
比先切帧再发现「这局 Lion 只有 12 级」便宜得多。上一轮买的那句「先量后切」在这里复用。

**驱动读数(真实帧驱动 `X.SkillsComplement`,全程无注入)**:

| 帧 | Lion | 出货 | armed | 3 秒内被英雄打过? |
|---|---|---|---|---|
| `..._slardar_1129` | lv19 满血 | `lion_impale` | **沉默** | 否 |
| `..._sb_1344` | lv23 满血 | `lion_impale` | **沉默** | 否 |
| `..._slardar_1416` | lv24 96% | `lion_impale` | **沉默** | 否 |
| `..._sb_1357` | lv23 满血 | `lion_finger_of_death` | **不变** | 否 |

- 全语料 **42 个活体 Lion 瞬间,armed 恰好移动 3 个**,且**每一个都是变沉默**
  —— 兜底支路是最后一条 Impale 支路,下面没有别的接手。`armed ⊆ shipped`
  是 §3.4 **驱动出来的**,不是论证出来的。
- ⭐⭐ **豁免帧 `sb_1357` 是这一节的一半价值**:SB 19% 血,**说得出理由的那条支路
  (Finger of Death)先赢**,兜底支路根本没跑到 ⇒ armed 一字不改。**没有它,
  「armed 收窄」和「armed 是关断开关」是同一个读数。**

### 地面真相(同一局录像),故意是混合的

被扣下的 3 发 Impale **全部命中**(205 / 199 / 201 伤害,目标身上都出现
`modifier_lion_impale`)—— **这条支路不是往空地上扔**。它做的是把一个 14 秒的控
花在 3 秒内没碰过自己的人身上:

- t=1129.8 控**满血** Slardar → Lion **1140.3 死在同一个 Slardar 手上**;
- t=1416.0 又控 Slardar → **1420.7 死**;
- t=1344.5 那发,活下来了(76 秒后才死)。

⛔ **三分之二是一个读数,不是一个率。**「扣下来是不是更好」是波次的问题,不是本文件的。
报告和源码注释都逐字这么写。

---

## 3. ⭐ 尺子缺陷(本轮的第二个产物,两个实例)

`X.ConsiderQ` 的环是 `abilityQ:GetCastRange() + aetherRange + 20`,而
`X.SkillsComplement` 对任何带 `item_aether_lens` 的 Lion 把 `aetherRange` 设成 **250**
—— 本语料里**每一个** Lion 都带。两个普查文件都把这一项丢了:

| 文件 | 它用的环 | 支路真实的环 | 现场 |
|---|---|---|---|
| `test_lion_q_field_engagement.lua` §3.1 | `GetCastRange() + 20` = 670 | 920 | 新帧 **3/4** 坐在环带里,普查看不见 |
| `test_lion_q_kill_reach.lua` `RING` | `650 + 20` = 670 | 920 | 把一发 **861.99u** 的合法施法判成「outside 670」 |

第二个的注释还逐字写着「**X.ConsiderQ's own arithmetic**」——它不是;它是那套算术**减去
aether 项**。两处都已修成**逐帧的环**(`lens_bonus` / `frame_ring`),
`test_lion_q_kill_reach.lua` 的固定帧台(`f_222428_lion_lich_burst`,那个 Lion **没有**透镜)
**原样保留 670**,所以 6.42u 的余量读数不受影响。

⚠️ **但它没有制造出上一轮那个 0,这一半必须照实说**:上一轮就存在的 4 个 15 级帧
在两种环下**答案相同**(已钉成断言 `nOldLv15Gap == 0`)。
**一把碰巧同意的坏尺子仍然是坏尺子,但那不等于上一轮的结论错了。**

---

## 4. 暂存语料价钱(净增红 **0**,并**退掉 2 条存量红**)

4 个新帧每个都带着活体 Zeus / CM / Lion,顶红 4 个普查文件。**逐条对 `git worktree
add --detach` 的干净 HEAD 复现**后:

| 文件 | 干净 HEAD | 本轮树 | 归属 | 处置 |
|---|---|---|---|---|
| `test_zuus_jump_landing_reach` | 0 红 | 1 红 | **本轮** | 已重取(3 个数) |
| `test_cm_ult_reach_meter_domain` | 0 红 | 2 红 | **本轮** | 已重取(**11 个数**) |
| `test_lion_q_kill_reach` | 2 红(baselined) | 3 红 | 1 条本轮 | **修到 12/0,并把它从棘轮 baseline 里删掉** |
| `test_lion_ult_reserve_domain` | 2 红(baselined) | 3 红 | 1 条本轮 | 已付本轮那条,回到 2 条存量红 |

⛔ **「在第一条红消息处停手」这个坑,本轮当场踩到并当场量了出来**:
`test_zuus_jump_landing_reach` 第一条红只是 `nLive 57→60`,我照着写了一行注释说
「**band/safe 都没动**」—— 下一次运行立刻反证:**band 10→12**(两个新帧把 Zeus 送进了环内),
`nBandFrames` 9→10。注释已改成正确的,并把这件事写进了那个文件。
上一轮的报告就写着这条教训(该文件前两次重取都只动分母),**它复发了,而复发点是注释不是数字**。
`test_cm_ult_reach_meter_domain` 因此改成**一次读到底**:11 个数一起登记
(5 个 funnel + `nInstants` + 5 个 mock 常量),并登记 `pre - post = 16` **没动**
—— 4 个新 CM 是「可施放且被保留」,只长分母不长撤销。

**棘轮 baseline 缩小 1 条**(`known_red` 16 → 15):`test_lion_q_kill_reach` 现在 **12/0**,
它那 2 条 baselined case 是被 `frame_ring` 修复顺带修掉的。
闸自己写着「⛔ this list is not an exemption and it is meant to SHRINK」。

⚠️ **`test_lion_ult_reserve_domain` 剩的 2 条红不是本轮的,也不是本轮能关的**:
干净 HEAD 上就红着(rank-2 瞬间早已 2→4,最小池帧早已移走)。本轮的帧把 rank-2 推到 **7**、
rank-3 推到 **2** —— 即**退休 HONEST BOUND (B) 的供给翻了一倍,而 (B) 已经开着 4 天**。
这一棒随 issue 交出去,不在本组一个工作单元内做。

---

## 5. 验证读数(铁律 6,三条腿)

```
GATE_EXIT=0            luacheck bots game: 0 warnings
py gate:  95 ran, 0 findings, 0 uncertifiable, 29.8s
lua gate: 326 ran, 0 findings, 0 uncertifiable, 15 known-red, 284.0s
```

- 新/改测试:`test_lion_q_field_engagement` **12/0**、`test_lion_q_kill_reach` **12/0**、
  `test_cm_ult_reach_meter_domain` **8/0**、`test_zuus_jump_landing_reach` **15/0**、
  `test_lion_ult_reserve_domain` 8/**2**(两条均为存量,见上表)。
- **变异台三发全红**(每发都从文件副本恢复):
  helper 恒真 → 4 红;窗口 3.0→300.0 → 1 红;从 `nCastRange` 里删掉 `aetherRange` → 3 红。
  未变异 12/0,恢复后 12/0。
- ⚠️ **`RULE6_BYPASS` 没有用到**;三条腿都是真跑出来的读数。
- 上面是**带 `bots/` 改动那次 push**(`53caa09f`)的读数。之后还有一次
  **只改 markdown/json 的 push**(`94b23165`,把预判的 GH #724 改正成 #725),
  那次钩子把 Lua 腿判成 **SKIPPED BY SCOPE**,它自己打的原话是
  「**This is a SCOPE decision, not a pass and not a skip of a gate that had
  something to say (GH #624)**」。照抄在此,不读成「又绿了一次」。
- ⚠️ 动态半(全套 ~100 分钟,GH #124)**未在本进程内跑完**,本报告不声称它。
- ⚠️ **自检 `EXIT=0`**,但它自己打了 **`UNCERTIFIABLE — 9 check(s) did not run; this is NOT a
  pass`**(python 腿的 5a–5g 在本容器 120s 内没跑完)。它同时打的 `TRUNK RED`
  来自**工作树**(GH #704),而那一行是在本轮开工、工作树一个字没改时打的。

---

## 6. 已发表

- **GH #725**(本轮主发现:`lionqfight` 的域从空变成 4 + 尺子的 aether 缺陷两个实例)。

⚠️ **一条便宜的教训**:本报告/章程/state.json 最初按序号**预判**写的是 #724,
而在本轮切帧、跑闸的同一时段,**协同组抢先把 #724 用掉了**(anyhero)。
铁律 6 的 GH #290 要求「**先 push 再发表引用**」,本轮照做了(产物先上
`origin/main` 再开 issue),但**它管不住「预判 issue 号」这一步** —— 并发的另一个
stream 会把号取走。⇒ **要么开完 issue 再写号,要么开完立刻回填**(本轮走的是后者,
`94b23165` 一次改正 5 处)。

## 7. 交出去的下一棒

- `iterations/queue.json` 无新增请求(本轮不需要批测数据)。
- `test_lion_ult_reserve_domain` 的 2 条存量红 + HONEST BOUND (B) 的退休,
  在 GH #725 里点名交给下一个认领 `lionult` 的轮次(**不是**本组本轮的作用域)。

## 8. Token 用量

```
TOKENS total_in=16,156,992 out=73,064 turns=101
```
(统计时刻之前的数字;之后的收尾回合不计入。)
