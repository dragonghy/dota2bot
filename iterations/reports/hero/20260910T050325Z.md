# 英雄组 2026-09-10T05:03Z — `cmqpoke`:CM 唯一那条真的开火的新星支路,问的是「我有多少蓝」而不是「这一发买到什么」

**工作单元主体**:一个 `bots/` 行为改动(OWNER_PRIORITIES **P4.4 (i)**)。
gated / turbo-only / 真实帧钉住,**armed 在语料里真的移动了一个决策**。
零 AWS,零波次,不请求入集(P4.2 冻结)。

---

## 0. 开工自检(铁律 10)——**一条腿没读到,如实登记**

`bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1`(不接管道 —— 直接跑会被
它自己的 REFUSED 挡住,那正是本轮第一条命令踩到的,形状与 `-138` 记的一致)。

- 挂在后台跑,**未精确计时**;量级与 `-138` 记的 ~30 分钟一致(`-137` 记 18 分钟、`-136` 记 13 分钟)。
- 读到的发现:**`FINDING: 11 armed id(s) with neither a verdict nor an owed row.`**
  (条件-(a) 义务节);unlanded / cadence / expired-waits / stable 锚点 / promote-atom 五节全 OK,
  `FROZEN none`(没有活 gate 点名已 promote 的 id)。
- ⛔ **观测到的事实(只写观测到的)**:日志最后一行是 `=== trunk health (python test suite) ===`
  这个抬头,**其下零输出**,而**我那行 `echo "EXIT=$?"` 也没有出现在输出里**
  ⇒ **本轮没有观测到自检自己的退出码**。harness 报的 `exit code 0` 是**读者的**不是脚本的
  (与那条 REFUSED 横幅讲的是同一个失效方向,不是同一处代码)。**机制本轮没有查清,不猜。**
  **按铁律 10 / 证据纪律:这不是通过 —— trunk 的 python 那一侧本轮没人看过。**
  与 `-138`/`-137` 记的「python 腿 9 项 UNCERTIFIABLE(GH #507/#358)」同族,但这次
  **连横幅都没打出来**,比 UNCERTIFIABLE 更靠前一格。没有重跑(再花 ~28 分钟会吃掉整个工作单元)。

---

## 1. 选件:先跑仪器(执行 backlog `-138` 的指示)

`python3 tools/agent/queue_reading_census.py` → **EXIT=0**,`hero` 流 55 行 / 20 行有 result /
**15 行未花**。仪器给的下一批候选就是 `-138` 写的那三行,而 `-138` 要求「要么去读
`hero-1`/`hero-2` 的原文,要么去做 `hero-10`」。**本轮读了原文,两条都不是待花的读数**:

| 行 | 仪器分类 | 读原文之后 | 处置 |
|---|---|---|---|
| `hero-1` | DOMAIN-READING | **是裁定不是读数**。`result` 第二段(2026-08-22T15:47Z 英雄组)已写死 **`wkqaim` 不写**,两条独立理由(供给饥饿:100 个 fixture 里 6 个环帧 6/6 的 Blast 没学或在冷却;上游同胞:`X.ConsiderQ` 上方无 gate 的分支先无条件拿走)。⭐ **这正是仪器 LIMITS 第一条的现场** —— class 是关于**文本**的证据,而这行文本带着分母,所以它读作 DOMAIN-READING;marker 就印在旁边,一眼可推翻。 | 不花。仪器分类**没错**,`-137`/`-138` 的手写警告**也没错**,两者说的是不同的东西。 |
| `hero-2` | DOMAIN-READING | **是真请求,但球不在本组**。第一顺位障碍(批测台 2026-08-23「归档里 Axe 0/306 局」)已于 2026-09-05 被 `tests/fixtures/tl_260905_010226_axe_outchan.json` 证伪,消费者 `cullthresh` 已 gated 落地;缺的是**归档 timeline 上按「穿越」口径的一次扫描**(总监 §BF.2 早已放行,零 EC2)。 | 不花。球在**批测台**。 |

⛔ **顺带钉死一条:本仓那份归档 timeline 切片答不了 `hero-2`。**
`tl_260905_010226_axe_outchan.json` 实测是 **18 个 1Hz 快照 / 18 秒窗口(t=1014.4–1031.4)/ 10 个英雄位**,
每位只有 `{t, hero, idx, team, x, y, hp, hp_pct}` —— **没有技能等级、没有冷却、没有蓝**,
而「穿越」口径要求「Culling ready」。按 `acceptance_amendment_hero_20260830` 预登记的量级
(中位 burst 下 2.3 次穿越/命中),**18 秒的窗口期望命中 ≈ 0**。
⇒ 谁都不要再拿这一份切片去替代那次归档扫描,它连**上界**都算不出来。

⇒ 回落到焦点英雄自查。选 **Crystal Maiden**,理由是 GH **#658** 量到全 fixture 语料里
焦点五英雄的出货树只做 **4 个技能决策**,而 CM 的**那唯一一个**此前只登记了「下了哪个技能」
(`crystal_maiden_crystal_nova`),**没有登记是哪条支路** —— 而那一格正是本组任何 CM lever
唯一能拿到非零决策效应的地方。

---

## 2. 那条支路是哪一条(本轮第一次登记)

驱动 `f_260820_182906_lion_drain_survived.lua`,CM 当 subject,gate 全关:
`X.ConsiderQImpl` 答 **0.75**,落点 `(6046.8, -2271.9)`。逐条排除:

```
GetActiveMode()                     0        （所有 == BOT_MODE_* 支路暗）
J.IsGoingOnSomeone / IsDoingRoshan / IsDoingTormentor   false
FindAoELocation(hero, kill/hurt)    count 0  （loader 按构造拒绝英雄 AoE 搜索）
FindAoELocation(creep, kill/hurt)   count 0
GetNearbyNeutralCreeps              0        GetNearbyLaneCreeps  nil
```

⇒ 开火的是 `--非撤退的逻辑` 块里那条**钱包支路**:

```lua
if bot:GetActiveMode() ~= BOT_MODE_RETREAT then          -- 唯一的 mode 项是否定式
    if J.IsValid( nWeakestEnemyHeroInBonus ) then
        ...
        if nMP > 0.8 or bot:GetMana() > nKeepMana * 2     -- ← 开火的就是这条
        then  ... J.GetCastLocation( bot, nWeakestEnemyHeroInBonus, ... ) ...
        if ( nWeakestEnemyHeroInBonus 血量比 < 0.4 )      -- ← 六行之下的姊妹支路
```

**两条支路要的是同一个施法、同一个 subject、同一个 `J.GetCastLocation`,只差一项:
上面问「我有多少蓝」,下面问「这一发买到什么」。上面那条是下面那条把质量测试
换成钱包测试 —— 而且它坐在上面,所以永远先赢。**

⭐ 与 `cmrcrowd`(GH #649)**同族不同形**:那条是不合格的析取项在同一个 `if` 里
**短路**掉合格的那项;这条根本没有可短路的东西 —— 它是**更早的一条独立支路**,
合格那条永远轮不到。

**并且钱包测试随局势变松。** `nKeepMana = 220`,绝对项是**平的 440**,池不是。
算术(不是读数):两项在 **550 池**处交叉,1100 池上有效储备降到 **40%**,2200 池上 **20%**。

---

## 3. 落地(gated,`cmqpoke`,turbo-only)

`bots/BotLib/hero_crystal_maiden.lua`:新 `X.cm_ShouldSpendSurplusNova( hTarget, bShippedSurplus )`
+ 调用点替换。armed 追加的判据是**下面那条姊妹支路自己的 0.4**,不是新阈值 ——
armed 之后这条路**塌到合格那条上**,而不是发明一个本文件没用过的门槛。

- **方向是代码属性不是数据属性**:出货析取式先算,为假原样返回 false ⇒
  armed **只能把出货的 TRUE 翻成 FALSE,永远加不出一发施法**。gate off 逐位等于出货。
- **退化读保护**:`GetMaxHealth()` 读不到(非数 / <= 0)时**回落到出货答案**。
  反方向的静默回归(一个答 0 的 getter 把整条新星停掉,`zusboltcap` / GH #175 那一族)
  是这里唯一不可接受的失效方向。
- **⛔ 只点名一个 id**,不与 `cmqreach` / `cmrcrowd` / `cmwface` 合取(`pullcad` 陷阱)。

### ⛔⛔ 本轮花掉的那个坑,值得抄走

第一版把出货析取式**搬进了 helper**(参数改名成 `nManaPct`/`nManaNow`/`nKeep`)。
本 lever 自己的 5 节断言**全绿**,而 `tests/test_cm_t10_payoff.lua` **红了**:

```
tests/test_cm_t10_payoff.lua:280: mana thresholds parsed: 7      （断言要 >= 8）
```

`tests/_cm_t10_payoff_sweep.lua` 的 `parse_thresholds()` 是**按源码文本**扫本文件的蓝量闸
(`nMP <op> <n>` / `GetMana() <op> nKeepMana * <n>`)。把表达式藏进改名的形参,
**等于从那份普查的模型里静默删掉一个闸**,而它自己那份断言一句都不会红。
⇒ 改成**出货析取式留在调用点逐字不动**,只把结果传进 helper。理由写进了函数头。

**一般规则(带走)**:动一处 `bots/` 表达式之前,先问**谁在按源码文本读它**。
本仓至少有三份普查这么读(`_cm_t10_payoff_sweep`、`test_dead_manacost_binding_census`、
`test_detector_source_constants`)。**重构成 helper 是行为不变的,但对「按文本读」的读者是删除。**
同族:GH #650 / #689(普查里的数字写死)、GH #694(注释被逐字钉死)——
那两族是**普查的数**跟不上,这一族是**普查的主语**从它底下走掉了。

---

## 4. 域(全语料,两个目录都扫)

`tests/fixtures/` + `tests/frames/`(**glob 外的 staged 帧也扫**,GH #236 / #281):

```
53  CM 活体瞬间
→ 28  冰霜新星 IsFullyCastable
→ 18  满足出货钱包测试
        · 绝对项(> 440)单独拍板     6
        · 比例项(nMP > 0.8)单独拍板  0     ← ⭐
→  1  出货 X.ConsiderQImpl 下单
→  0  armed 下单                              （唯一移动的一帧 = 钉帧）
```

⭐ **在这份语料上,比例项一次都没有自己说了算** ⇒ 真正在跑的规则就是「蓝 > 440」。
这条读数钉在 `tests/test_cm_nova_surplus_poke.lua` §5,**它变成非零的那天该节变红并要求
重写函数头那一段,而不是重钉一个数**。

**钉帧**:`tests/fixtures/f_260820_182906_lion_drain_survived.lua` ——
CM **11 级,543/831 蓝(65%,所以拍板的是那个平的 440 不是 0.8)**,1200 内 **1 个队友**,
三个敌人在 **625.2u / 749.5u / 885.1u**,血量 **0.62 / 0.94 / 0.97**,桌上没有人头。
出货把冰霜新星丢给 0.62 血的 Lion;armed 拒掉,下面的支路也拒,`X.SkillsComplement` 一发不下。

**这是本仓第一条 armed 腿真的移动了语料能到达的决策的 Crystal Maiden lever。**

### 条件 (c)(理论依据,可检索)

冰霜新星是 CM 的主力 nuke + 唯一的 AoE 减速,标准辅助打法把它留给**能跟手的**局面
(Frostbite 连招 / 队友开团 / 收人头),而不是当消耗技丢给一个满血、无跟手、
最大射程上的目标 —— 她的蓝是后手的约束。**成本也如实写下**:CD 11/10/9/8s,
Turbo 里拒掉一发回得快,所以单发损失小;总账要一波来结,这也是它 gated 的原因。

---

## 5. 验证

| 门 | 读数 |
|---|---|
| `bash tools/agent/luacheck_gate.sh` | **`LUACHECK_EXIT=0`** / `luacheck bots game: 0 warnings` / `GATE_EXIT=0  CLEAN` |
| `lua5.1 tests/run_tests.lua cm_` | **332 例 0 失败** |
| `lua5.1 tests/run_tests.lua cm_nova_surplus_poke` | **6 例 0 失败** |
| `lua5.1 tests/run_tests.lua smoke_load` | **3 例 0 失败** |
| `bash tools/agent/mutstand_cmqpoke.sh` | **12/12 CAUGHT**(见下,首跑 10/12) |
| 全套 `run_tests.lua`(~100 分钟,GH #124) | **本轮未跑完,如实登记** |

### 变异台:首跑 **10/12**,两处缺口当场补上,重跑 **12/12**

12 发都锚在**本轮移动过的东西**上:M1/M2 gate 本身;M3 方向反转;M4 死接线;
M5 调用点回退;M6「第三把尺」(把这条支路换到 进攻 支路的 0.75);M7 支路变暗;
M8 退化读回落方向被翻;M9 lever 被放宽成**能加施法**的那种;M10 `pullcad` 合取;
M11/M12 是对**本轮发表的两个读数**(比例项单独拍板 0、钉帧的 440)的对照。

**⭐ 首跑漏掉的两发,以及它们各自说明了什么(这才是台子的产出):**

- **M2(turbo-only 被摘掉、候选检查留着)—— SURVIVED。**
  本文件**没有任何一条断言看 `J.IsModeTurbo()`**:语料驱动 armed 腿本来就要强制打开 turbo,
  所以一条**悄悄不再 turbo-only 的 gate 会在每一处读回绿**,而它在正常模式里已经是活的。
  已在 §1 补上对 helper 体的 turbo 断言。
- **M8(退化读的回落方向被翻:读不到容量时改成「拒绝」)—— SURVIVED。**
  语料**驱动不到**它 —— 每一帧的目标都答得出真实 `GetMaxHealth`,那条分支按构造不可达。
  ⇒ 补 §6,**直接对 helper 断言**(0 / -1 / `'x'` / `nil` 四种读不到的形状都必须回落到出货答案),
  而不是去削弱别的断言。**削断言只能证明断言可以被删掉。**

⛔ 首跑还暴露了台子自己的两处瑕疵,一并修掉:M6 的 `echo` 里嵌引号造成 bash 语法错误
(M6–M12 当轮根本没跑,`trap restore EXIT` 正常回滚,`git status` 已核对);
M2/M8 的期望字符串是照旧版断言写的,红了却被判成 `WRONG MESSAGE`。

---

## 6. 交棒 / 升级

### ⭐ 铁律 9 升级:`hero-54`(帧供给)**仍未裁**,这是第三轮点名

`-138` 写「下一轮若仍未裁,按铁律 9 在报告里升级并指名下一步」。**本轮仍未裁。**
GH **#658** 的那句话本轮**第三次**被独立验到:本轮为了给一条新 lever 定价,
逐条排除了 CM 的每一条支路,能到达的只有那条钱包路;
`FindAoELocation(hero, ...)` 按构造答 0、`GetActiveMode()` 答 0,**其余全暗**。

**下一步指名(不是征求意见)**:
1. **总监**:裁 `hero-54`(优先级 1,零 EC2,只要「在焦点五英雄**真正施放技能**的瞬间取帧,
   每英雄 >= 5 帧,优先 WK 的 0 帧与 Axe」)。验收判据现成:让
   `tests/test_focus_decision_reachability.lua` §2.1 变红。
2. **批测台**:`hero-2` 的归档扫描(§BF.2 早已放行,零 EC2)——
   口径按 `acceptance_amendment_hero_20260830` 数**穿越**不数带内帧;
   **别用 `tl_260905_010226_axe_outchan.json` 代答**(本报告 §1 已量:18 秒 / 18 帧 / 无技能字段)。

### 入集

**P4.2 冻结期,本组不请求入集** —— 唯一合法裁定是 FROZEN-HOLD。`cmqpoke` 登记在
`iterations/state.json:cmqpoke_20260910`,`armed: false`,等冻结解除(armed 集 <= 20)后再提。

---

## 7. 本轮当场判死、不要重查的候选

| 候选 | 位点 | 拒的理由(实测,本轮) |
|---|---|---|
| Lion Hex 的打断支路收窄射程 | `hero_lion.lua` `X.ConsiderW` 「打断」循环:`nInBonusEnemyList` 是 `nCastRange + 300`,**IsChanneling 那半一个距离项都没有**,而同一个循环体里 `IsCastingAbility` 那半要求 `nCastRange + 50` | 形状是真的(同族 `axecullreach`/`lionrreach`),**但方向站不住**:打断 TP(3s 引导)本来就值得走 300u,而 `X.ConsiderW` 是 `SkillsComplement` 的**最后一条**臂,没有 dominance 成本。收窄大概率是中性到负。**不写。** |
| WK 打断支路同款 | `hero_skeleton_king.lua` `X.ConsiderQ`:`nEnemysHerosInBonus` 是 `nCastRange + 330`,`IsChanneling` 无距离项,**同一循环体**的击杀确认那半有 `<= nCastRange + 80` | 同上,方向同样站不住;另 `IsChanneling` 在语料里恒 false(GH #658 §4)⇒ 连定价都买不到。**不写。** |
| CM `X.ConsiderQImpl` 的 Roshan 支路 | `bot:GetActiveMode() == BOT_MODE_ROSHAN and bot:GetMana() >= 400` 直接返回 `botTarget:GetLocation()`,**不过 `J.GetCastLocation` 也不测距**,而同函数下面的 `J.IsDoingRoshan` 支路**测**(`J.IsInRange(bot, botTarget, nCastRange)`) | 缺陷成立,但第 13 条世界断言:`GetActiveMode` 不出现在任何 .dem ⇒ **fixture 里永远验不了**;且真打肉山时人就站在坑里,量级近 no-op。**不写。** |
| Zeus `X.ConsiderE` 补蓝储备(`test_dead_manacost_binding_census` 的 class 戊) | 文件自己写着「Wiring this is a gated-candidate + real-frame job」 | **两个开火点全在 `GetActiveMode` 后面**(`J.IsRetreating` / `J.IsGoingOnSomeone`)⇒ 语料里 `X.ConsiderE` 在 45 个活体 Zeus 瞬间上一次都不下单(GH #658 的表),决策效应结构性为 0。与 Lion 那个同名候选(2026-09-05 预检 0 域)**同因不同英雄**。按 `-131` 的规矩**不写**。 |
| 焦点五的伤害/射程硬编码再抽查 | — | GH #658 已抽查四处全部与 KV 对得上;本轮另查 CM `+32` / Lion `+20` / DP `+99/+100` 这一族**是本仓通用的越界余量写法**,不是 `cullthresh` 那一族。**不重查。** |

---

## 8. 章程/待办变更

- backlog 新增 `-139`(本轮),`-138` 划掉并保留原文。
- `iterations/state.json` 新增 `cmqpoke_20260910`。
- `iterations/queue.json` **未改一个字**(本轮没有花掉任何一行,理由见 §1)。
- **rebase 记录**:push 到 `main` 时与协同组同轮的 `owhs_clear_20260910` 在 `iterations/state.json`
  **文件尾同位置**撞车(两边都在末尾追加新 key)。**两个 key 都保留**,无内容取舍;
  `json.load` 复核通过。

---

## 9. token 用量(铁律 8)

```
TOKENS total_in=29,139,843 out=109,742 turns=135
```

(到统计时刻为止;统计之后的收尾回合不计入。)
