# 英雄组 2026-08-31T01:55Z

**轴:认领 `[hero]` GH #348 的**静态那一半** —— 把「拼错的字段让一条分支恒假」从散文变成
一条全树不变量;顺手抓到 **issue 里没有的第二个站点(Muerta)**,而它与 Sniper 那个是
**同一次上游复制粘贴**。**焦点五在这条不变量上是干净的,而且那个「干净」是数出来的(14 个站点)。**

**零行为改动**(`bots/` 与 `game/` **各 0 行**)、**零新 gate id**、**零 arm/promote**、
**零 AWS**(连一次 S3 GET 都没有)、**不申请波次**、**不开新 issue**。

---

## 1. 认领依据(章程工作流第 1 条的**主路径**)

- `iterations/OWNER_PRIORITIES.md` 四条:常设发波 spot(球在**批测台**)、P1 拉野(**协同组**)、
  P2 低血不回家(**协同组**)、P3 cap 10→25(**总监**)。**没有一条球在本组。**
- open `[hero]` issue:**#348**(本组上一轮 22:56Z 自己开的,总监未接)、#346(Silencer 本体,
  一般性裁定已交总监)、#330 / #328(语料读数)。**#348 带源码证据、点名本组、且是本组自己
  交出去后无人接的那根棒** ⇒ 认领它。
- 上一轮把 Sniper 记成「登记不修(非焦点五)」并开了 #348。**只有本组能服务 `[hero]` 前缀**,
  所以这样交出去的棒**掉在地上**(铁律 9 连带规则点名的形状:拉野死分支曾因此消失 37 轮)。
  本轮取的是它**不需要焦点五豁免的那一半** —— 见 §4 为什么另一半不能顺手做。

## 2. ⭐⭐ 主读数:`.cout` 让整条 `and` 链恒假,而它**在两个英雄文件里逐字相同**

```lua
local nCanHurtHeroLocationAoE = bot:FindAoELocation( true, true, botLocation, nCastRange, nRadius-30, 0.8, 0 )
if  #nEnemysHeroesInSkillRange >= 2
    and ( nCanHurtHeroLocationAoE.cout ~= nil and nCanHurtHeroLocationAoE.cout >= 2 )
    and bot:GetActiveMode() ~= BOT_MODE_LANING
    ...
then
    return BOT_ACTION_DESIRE_HIGH, nCanHurtHeroLocationAoE.targetloc
end
```

- `FindAoELocation` 的返回表**只有两个键**:`count` / `targetloc`
  (`docs/BOT_API_REFERENCE.md:1377`,连文档自己的示例也只读这两个)。
- `cout` 不是键 ⇒ **nil** ⇒ `cout ~= nil` **恒假** ⇒ 整条合取恒假 ⇒
  **这条「对多个敌方英雄放 AoE」的分支在本仓历史上从未开过火,也开不了火**。
- **为什么它看不见**:Lua 读不存在的键不报错;而 bot 侧没有任何错误可见性
  (`print()` 到不了服务器控制台,引擎错误处理器坏了 —— AGENTS.md)。**症状只有
  「一个从不被做的决策」**,正是没人会注意到的那一种。与 GH #162
  (`splash_radius_scepter` 键改名读成 0)、`hero_lion.lua` / `hero_zuus.lua` 的
  `GetAbilityDamage()` 恒零同族:**一个静默的 nil/0 掐死一条分支**。

**⭐ issue 里没有的那个:`bots/BotLib/hero_muerta.lua:360`。** GH #348 只点了
`hero_sniper.lua:282`。Muerta 那一段是**同一块代码的逐字复制**(同样的
`#nEnemysHeroesInSkillRange >= 2`、同样的 `.cout ~= nil and .cout >= 2`、同样的
`BOT_MODE_LANING` 排除、同样的 `return ..., .targetloc`)⇒ **一次上游复制粘贴,
不是两次独立笔误**。这一条已钉进测试(第三个节点),并会追评到 #348。

## 3. 落地物

| 文件 | 内容 |
|---|---|
| `tests/lua_source_scan.lua` | 新增 `M.AOE_RESULT_KEYS` + `M.aoe_result_fields(path)`(**只加不改**,两个既有调用方零改动) |
| `tests/test_aoe_result_field_names.lua` | 新文件,4 个 `[ratchet]` 节点 |

`bots/` **0 行**,`game/` **0 行**,`iterations/state.json` **无新增**(没有新 gate id),
`iterations/queue.json` **无新增**(零行为差 ⇒ 没有可测的东西),`test_set.md` **无新增**。

**扫描器的保守方向是单向的**(只会漏报、不会误报):只跟 `local X = ...FindAoELocation(...)`
这一种声明(全仓 353 个调用点里 **332** 个是这种);遇到下一个 `function` 行停;遇到
`X` 被再次赋值就停。

**四个节点各自钉什么**:
1. `[ratchet] 扫描器自己` —— 合成输入、已知行号(见 §5,这一节是唯一能抓到两个变异的地方);
2. `[ratchet] 全树普查` —— 名单**恰好两条**,**两个方向都红**(新增一条 = 有人刚拼错;
   少一条 = #348 被修了,连同本文件的散文一起改)。另加 `sites >= 300` 的**空转闸**:
   一个指向空处的扫描器会报 0 个发现并干净退出,正是 GH #345 在 `arm_string_census.py`
   上抓到的「0 games + exit 0」;
3. `[ratchet] 两个站点是同一块` —— 钉住让它们成为**一个**发现的那三处结构;
4. `[ratchet] 焦点五` —— 本组签的那句话,而且**是数出来的**:五个文件 **14** 个可跟踪声明、
   **0** 个越界字段;`sites == 14` 一起断言,免得这句话在零个站点上**空真**。

## 4. ⭐ 明说**没**做的,以及为什么

- **没有修那两处 `.cout`。** 修它**复活**一条本仓历史上从未跑过的分支 ⇒ 是**加动作**的改动,
  按纪律要 gated + 定过域;而 Sniper / Muerta **都不在焦点五**。本轮做的是**不需要那份预算的
  那一半**:让这个类**长不大**。
- **没有为它申请波次。** 域是两个非焦点英雄的一条分支,不该由本组的语料预算买。
- **没有主张焦点五的 AoE 代码是干净的。** 这条不变量只说「读的键名对不对」。它**不说**
  搜索半径对不对(那是 `cmqreach`)、**不说**结果有没有判 nil(353 个站点里 **350** 个没判,
  `tests/test_nil_guard_then_body.lua`)、**不说** `.count` 守的那条分支是不是对的分支。
- **顺手复核、结论是「不是缺陷」的两件**(过程写在这里,免得下一轮重做):
  - `tools/agent/special_value_key_census.py` 本轮跑过一次,焦点五**唯一**命中仍是
    `hero_lion.lua:1290 splash_radius_scepter`,而它**已经**被 `lionsplash` 门管着(GH #162)。
    我另外把该脚本自认的**单向缺口**(「键存在证明不了什么,因为它不解析句柄指向哪个技能」)
    在焦点五上**手工补齐**了:把 5 个文件的 20 处 `GetSpecialValue*` 读数逐个对到**具体那个技能**
    的 KV 键集(d2vpkr 镜像现拉),**除已知的 Lion 那条外全部命中**。没有落地成工具,
    因为句柄→技能的解析要复刻 `X.GetAbilityList` 的槽位过滤,**从 bot VM 问不出来**,
    我不愿把一个没验证过的槽位序当事实签进仓库。**登记为下一棒的候选,不是本轮的结论。**
  - `talentN` 可训练性:按各文件自己的 `tTalentTreeList` + `J.Skill.GetTalentBuild`,
    Axe t25`{0,10}`→[7]、Zeus t20`{0,10}`→[5] / t25`{0,10}`→[7]、WK t20`{10,0}`→[6]、
    Lion t25`{0,10}`→[7]、CM t10`{0,10}`→[1]。**Axe:930 的 `talent8` 与 Lion:1385 的
    `talent8` 都不是 `nTalentBuildList[1..4]` 里的那一半。** 但 `GetTalentBuild` 还返回
    **[5..8] = 另一半**,而 `GetSkillList` 会消费到第 8 个 ⇒ **「结构上永不训练」这句话
    我复核不下来**(它取决于 26 级以后能不能拿对侧天赋,而这一点我没有权威依据)。
    ⇒ **不改 Axe 那段注释,不写进测试。** 记在这里,给能查证 26+ 级天赋规则的下一棒。

## 5. ⚠️⚠️ 本轮最该记的一条:**第一版的两个合成对照挡不住它们该挡的变异**

变异台(`evidence-discipline`:盘外 `cp` 复本还原,逐字节 `cmp` 校验)跑了 **9 个变异**。
**头两轮里有两个变异活下来了**,而且都活在**同一个节点**上:

- **M4(去掉字段模式的词边界)** —— 第一版对照写的是 `counted` / `countedThings.nope`。
  没有边界时 `counted` 确实在 `countedThings` 里匹配上了,但**它后面紧跟的不是点**,
  所以模式还是不成立 ⇒ **对照根本没测到边界**。换成 `n` / `fn.wibble`(`n` 在 `fn` 里,
  **点紧跟其后**)才红。
- **M8(去掉 `function` 边界)** —— 第一版没有任何跨函数的合成输入 ⇒ 直接绿。补了一个
  `function Other( b )` 的**参数遮蔽**(没有赋值,所以只有函数边界能拦住它)才红。

**两个变异在全树普查上也都是绿的** —— 也就是说,如果只有普查节点,这两处扫描器缺陷
**没有任何断言看得见**。这正是 `test_nil_guard_then_body.lua` 头部记的同一件事,
**在本轮的新扫描器上又发生了一次**:普查节点的裁定对扫描器的细节**不敏感**,
所以扫描器必须在**合成输入 + 已知行号**上单独钉。

**最终变异台(9/9 见红,且只红在该红的节)**:

| # | 变异 | 期望红的节点 | 实测 |
|---|---|---|---|
| M1 | 在 **Zeus** 里新造一处 `.cout` | 普查 + 焦点五 | ✅ 两个都红 |
| M2 | 把 Sniper:282 修好 | 普查 + 同一块 | ✅ 两个都红 |
| M3 | 扫描器不再在重新赋值处停 | 扫描器 | ✅ |
| M4 | 字段模式去掉词边界 | 扫描器 | ✅(**第一版绿,见上**) |
| M5 | 任何键都不算发现 | 普查(+ 扫描器) | ✅ |
| M6 | `sites` 恒 0 | 普查(空转闸)+ 焦点五 + 扫描器 | ✅ |
| M7 | 改掉 Muerta 那块的 `>= 2` | 同一块 | ✅ |
| M8 | 扫描器不再在 `function` 处停 | 扫描器 | ✅(**第一版绿,见上**) |
| M9 | 去掉同行去重 | 普查(+ 扫描器) | ✅ |

对照:未变异时 **4/0 全绿**;每次变异后用盘外复本 `cp` 还原并 `cmp` 逐字节确认
(`tests/lua_source_scan.lua`、`hero_sniper.lua`、`hero_muerta.lua`、`hero_zuus.lua` 四份,
每次都 ok),收尾 `git status` 只剩本轮的两个文件。

## 6. 铁律 6 的门(读数,不是声称)

- **静态半**:`bash tools/agent/luacheck_gate.sh` ⇒ **exit 0**,`luacheck bots game: 0 warnings`。
  **裸读**(`$?` 直接取自脚本,没有经过管道)。容器冷启,脚本**自己**装了 `lua-check`。
  **一次都没用过 `RULE6_BYPASS`。**
- **动态半是子集不是全套**(GH #124,~100min 跑不完一个进程),照实登记,**不主张主干全绿**:

  | 过滤词 | 结果 |
  |---|---|
  | `aoe_result_field_names`(本轮新文件) | 4 tests, 0 failures, **exit 0** |
  | `nil_guard_then_body`(共享模块的调用方) | 5 tests, 0 failures, **exit 0** |
  | `activemode_call_site_census`(共享模块的另一个调用方) | 2 tests, 0 failures, **exit 0** |
  | `smoke_load` | 3 tests, 0 failures, **exit 0** |
  | `gate_claim_consistency` | 10 tests, 0 failures, **exit 0** |

  每个退出码都是 `$?` 裸读(先重定向到文件再取码),**没有重演 `| tail` 吃退出码那一形**。
  python 那套本轮**一行没碰**。

## 7. 开工自检

`bash tools/agent/routine_selfcheck.sh` ⇒ **worst exit 3**,`legs run 8`,
`FINDINGS: unlanded trunk-red(python)`,`UNCERTIFIABLE: none`;
stable-v1 / stable-v2 锚点 ok;快 Lua 腿 **47** 个 tagged 文件 0 失败(FAST SUBSET,
**开工那一刻**的取集,**不要**跟收尾或上一轮的数做差 —— 这个坑本组连踩三轮)。

⚠️ **那条 TRUNK RED 我复核不出来,而这件事本身要登记**:自检说
`tests/test_selfcheck_lua_leg.py` 在工作树上 FAIL;我**当场单跑同一个文件**,
拿到 `43 checks, 0 failures, 0 uncertified`、**exit 0**。工作树当时是干净的
(`git status` 空),本 diff 也**一行 python 都没碰**。⇒ 两个读数不一致,
**归属不在本组,也不敢替谁下结论**;登记给总监。本组这一侧:动手前后的
`git status` 都只含本轮的两个 `tests/` 文件。

## 8. 下一棒

1. **GH #348 保持 open**,追评补上 Muerta 这个站点 + 本轮的静态钉子;
   **修复那一半**(复活分支,要 gate + 域)留给愿意为非焦点英雄花语料预算的人。
2. **句柄→技能的 KV 键解析**(§4 第一条):把 `special_value_key_census.py` 自认的
   单向缺口在焦点五上**做成工具**,前提是先把 `X.GetAbilityList` 的槽位过滤
   在 KV 上复刻并**验证**(innate / hidden / not-learnable 的判定是承重墙)。
3. **26+ 级天赋规则**(§4 第二条):查证后才能决定 Axe:930 / Lion:1385 那两段注释要不要改。
4. `-43a` 的 Zeus 方向仍然欠着(本轮**没动**)。

## 9. token 用量

```
TOKENS total_in=10,137,950 out=63,079 turns=75
```

(`python3 tools/agent/token_usage.py`,统计时刻 = 收尾提交前;此后的收尾回合不计入。)
