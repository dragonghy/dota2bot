# hero 2026-09-14T11:08Z — GH #817 的「按英雄取值的常数」是三件事共用一个句子,最大的那件是仪器

**主体**:`tests/test_talent_uptake_visibility.lua`(**8 绿**)+ 变异台
`tools/agent/mutstand_talent_visibility.sh`(**7/7 全杀**)。
**零 AWS、零波次、`bots/` 未改一字。** 认领的是 **GH #817**(录像组 09-14T09:55Z 开,
比本轮开工早 1 小时),它点名了两个焦点英雄(`skeleton_king`、`crystal_maiden`)。

⚠️ **P4.4 自评:本轮主体是 (ii) 不是 (i)** —— 不是一个 `bots/` 行为改动,是一条
**即将驱动一次波级返工的读数**的判别子。这已是连续第三轮非 (i);理由写在 §7,
**不当作达标**。

---

## 1. #817 读到了什么

8 局(W40 = `dem21/spot_20260902_214528_1_…_ab6c0d` 的 `2146xx` 批 slot1–8,
**该树 OFF-TRUNK**,是批测实例当时 clone 的树,不是 `origin/main` 上能解析的引用):

| 英雄 | 终局天赋(8 局) | 10 级有花点的局数 |
|---|---|---|
| obsidian_destroyer / vengefulspirit / nevermore | **0 ×8** | **0/8** |
| skeleton_king | 1 ×8 | **0/8**(第一个天赋在**英雄 18–20 级**) |
| shadow_shaman / crystal_maiden | 1 ×8 | 7/8 |
| witch_doctor / skywrath_mage / bristleback | 1 ×8 | 8/8 |
| death_prophet | 2 ×8 | 8/8 |

#817 的一句话是「**天赋摄取是一个按英雄取值的常数,8 局零方差**」,并**明写不点名分支**。
它没说那个「按英雄取值的输入」是什么。

## 2. 本轮找到的输入:该英雄**自己的 build 在 t10 选中的那一行是不是 hero-unique**

判别子是**一个属性**,离线可算,零 AWS:

```
选中行 = tests/mock/hero_slots.lua[hero][ 8 + pick ]
pick   = aba_skill.X.GetTalentBuild 的规则(源码读出,不重打):
         tTalentTreeList['t10'][1] == 0  →  1(左/Ability10/槽 9)
                                否则     →  2(右/Ability11/槽 10)
```

| 英雄 | pick | 选中的 t10 行 | kind | #817 lv10 |
|---|---|---|---|---|
| vengefulspirit | [1] | `special_bonus_unique_vengeful_spirit_swap_damage` | **UNIQUE** | 0/8 |
| nevermore | [2] | `special_bonus_unique_nevermore_4` | **UNIQUE** | 0/8 |
| skeleton_king | [2] | `special_bonus_unique_wraith_king_facet_1` | **UNIQUE** | 0/8 |
| obsidian_destroyer | [2] | `special_bonus_mp_200` | generic | **0/8 ← 唯一的漏** |
| shadow_shaman | [2] | `special_bonus_mp_regen_175` | generic | 7/8 |
| crystal_maiden | [1] | `special_bonus_hp_200` | generic | 7/8 |
| witch_doctor | [2] | `special_bonus_hp_200` | generic | 8/8 |
| skywrath_mage | [1] | `special_bonus_mp_regen_150` | generic | 8/8 |
| bristleback | [2] | `special_bonus_mp_regen_150` | generic | 8/8 |
| death_prophet | [2] | `special_bonus_hp_200` | generic | 8/8 |

**10 个里对 9 个。** 机制不是新的,是本仓库 2026-08-27 **已经定谳**的 H1
(`tests/test_fixture_talent_blindness.lua`):dumper 的 `isRealAbility()`
把类名带 `Special_Bonus_Base` / `Special_Bonus_Attributes` 的实体
**在「保留已升级天赋」那一支之前**丢掉,而 hero-unique 行共用那个基类、
generic 行有自己的类。**本轮在源码上复核过**
(`tools/batch_test/behavioral/dumper/main.go:231–237`,不是引注释):
丢弃在 `return level > 0` 之前,顺序就是账要的那个顺序。

⇒ 一个被丢掉的天赋**不抬能力账本**,于是「10 级没花点」正是 #817 的尺子在这些行上
**必然**打出来的读数。

## 3. 三个「0 天赋」的英雄是**三本不同的账**,#817 把它们并成了一句

- **nevermore / vengefulspirit** —— 四档选中行**全是 unique** ⇒ **可见天花板 = 0**。
  观测到的 0 **就是天花板**,不是关于 bot 的读数。
  ⛔ 这**不是**在说它们拿到了天赋,是在说**这份语料答不了**。
- **obsidian_destroyer** —— t10 选中行 `special_bonus_mp_200` 是 **generic,可见**,
  却仍然 0/8。**盲区解释不了它。** 仓库里已有另一本账:
  `sAbilityLevelUpList` 的 nil-hole 塌缩(GH #286,`bots/ability_item_usage_generic.lua:32–50`
  原话「OD stalled at 6 spent points in 5/6 games」),而 **#817 自己的数就是佐证**——
  OD 终局只有 5–6 个能力点、停摆中位数 **1279.5 s**、≥900 s 的 8 具**全是 OD**。
  ⇒ **唯一的那个漏,漏在第二个已立案缺陷预测的方向上。**
- **skeleton_king** —— t10 unique(不可见)、**t15 `special_bonus_hp_300` generic(可见)**、
  t20/t25 unique ⇒ **可见天花板恰好 = 1**,而 #817 读到 **恰好 1,8/8 局**。

## 4. ⛔ 三条本文件**不**声称的(写进文件,不只写进报告)

1. **不声称任何 bot 真的点了 unique 天赋。** 被 dumper 丢掉的实体**不在文件里可数**;
   H1 说的是语料沉默,不是答案为是。
   ⇒ **#817 验收 3(「同一把尺子重跑,看 34/80 降下来」)对那三个 unique-t10 英雄
   在构造上不可测**:那几行的盲区与修没修无关,数字**不可能**为它们动。
   **这是本轮最该被下游听见的一句,也是它必须赶在有人发波之前落地的原因。**
2. **WK 的残差没有被解释。** 盲区预测他第一个**可见**天赋在**英雄 15 级**
   (t15 pick 是 generic);#817 读到 **18–20**。这 3–5 级的滞后**本轮不解释、留开**;
   §4 只钉天花板,**绝不钉时点**。一个离线settle不了的候选:
   `special_bonus_hp_300` 到底有没有自己的 C++ 类,还是和 unique 行一样落回被丢的基类
   ——语料里**被真正看见过的只有 `special_bonus_hp_200`,`hp_300` 没有**。
3. **对其余 117 个英雄零声称。** 十个英雄、一波、一棵树。

## 5. 顺带买到的一条焦点英雄 LIMIT(新)

四档选中行全部 unique 的焦点英雄是 **Axe**:
`unique_axe_8` / `unique_axe` / `unique_axe_4` / `unique_axe_2`。
⇒ **Axe 的天赋摄取对 dumper 100% 不可见**,「Axe 没点天赋」这句话
**从本仓库持有的任何 dump 里都说不出来**。Zeus / Lion / CM 三个是 t10 可见、其上全盲
(天花板各 1)。已钉成断言,以后 TALENTPRICE 类回合不必再各自撞一次。

## 6. 读数与门

- `lua5.1 tests/run_tests.lua talent_uptake` → **8 tests, 0 failures**。
- 变异台 `tools/agent/mutstand_talent_visibility.sh` → **7/7 全杀**。
  - ⚠️ **第一轮 6/7:M5 被记成 WRONG MESSAGE** —— 与 `-170`/`-172` 同形第三次。
    `want` 串指的是「规则形状变了」那条 assert,而正则**捕获两个数字**,
    翻转后形状仍匹配 ⇒ 真正先开火的是**下一行的取值 assert**。改 `want` 后复跑。
  - ⛔ **两个杀不掉的已写进台子抬头**:翻转 Axe 的 t10(两行都是 unique,天花板不动,
    断言本就该绿)、重打除 WK 外任何英雄的 `talents`(只有 WK 那个数被接到算出来的
    天花板上,其余是转录且文件已如实说明)。
- ⭐ **py 闸抓到本轮一个真缺陷,记下来**:`tests/test_mutstand_restore_trap.py` 判
  本台**没有证明自己的 restore**(只 `cp` 不校验)。已加:每次 restore 后对每个文件
  做 `sha256sum` 对拍,不一致就 **abort(exit 2)** 而不是继续在没人信得过的树上跑。
  **这不是我自己发现的,是闸发现的**,而它正是「拿变异台换走真修复」那类事故的入口。
- **铁律 6 三条腿(三行都抄在这里)**:
  - `GATE_EXIT=0` — `luacheck bots game: 0 warnings`
  - `py gate: 86 ran, 0 findings, 0 uncertifiable, 10.3s`
  - `lua gate: 323 ran, 0 findings, 0 uncertifiable, 7 known-red`(exit 0)
  - `RULE6_BYPASS` **未使用**。动态那半(~100 min,GH #124)**没跑,也不被声称**。
- **新测试未登进 lua 闸 manifest**:登记的唯一动作是 re-measure,而裸
  `lua_gate_measure.py` 被 GH #810 挡着且会清空 `known_red`(GH #783)。
  它**照样跑了**(fail-open,在「57 个未登记但仍执行」里),本轮不动名单。
- **开工自检 `EXIT` 见 `/tmp/sc.log`**:`FINDINGS (exit 3) : cadence queue-rulings
  owed-executions lua-coverage trunk-red(python)`,`UNCERTIFIABLE: none`。
  ⚠️ 容器开工时 **`lua5.1` 与 `luacheck` 都不在**,自检里九条腿因此 UNCERTIFIABLE;
  本轮**先 `apt-get install -y lua5.1 lua-check`**(注意包名是 **`lua-check`**)再跑门。
- ⚠️ **证据纪律 3 同形第 11 次**:本轮**第一条命令**又是
  `routine_selfcheck.sh | tail`,又被工具自己拒绝。第 10 次是上一轮。

## 7. 为什么本轮主体不是一个 `bots/` 行为改动(P4.4 自评)

⛔ **不辩解,登记。** 连续第三轮非 (i)。本轮选它的理由只有一条,而且是时序的:
#817 开在开工前 **1 小时**,它的验收 3 明写要**再发一波**,而那一波对四个点名英雄里的
三个**在构造上量不到东西**。判别子完全离线、零 AWS、当轮可交;发波之后再交,
省不下那一波。
**下一轮必须回到 (i)** —— 候选见 §8 第 1 条。

## 8. 下一轮最该做的,按顺序

1. **⭐ 回到 P4.4 (i):一个 `bots/` 行为改动。** 现成的落点是本轮撞出来的
   **WK t10 选的是 `special_bonus_unique_wraith_king_facet_1`** ——
   `hero_skeleton_king.lua:290–345` 自己已经记下:WK 的**两个 facet 都是
   `"Deprecated" "true"`**,该行 KV 里的一半(`cursed_damage_pct` / `curse_cooldown`)
   落在**只有那个已废弃 facet 才授予**的 `skeleton_king_spectral_blade` 上、**是死的**,
   活下来的只有 `blast_dot_duration +2`。
   ⇒ 值得**定价**「t10 换到 [1] `special_bonus_unique_wraith_king_2`(吸血)」。
   ⛔ **本轮没有改它**:半行死掉**不等于**另一行更值钱,而定价要的是 Turbo 里
   `blast_dot_duration +2` 与吸血%的对比,那是一个 polish-hero 工作单元,不是本轮的尾巴。
2. **不要**替 #817 去「修」nevermore / vengefulspirit 的 0 天赋,也不要用波级重跑验收它们
   ——§4 第 1 条,量不到。要动就先解决**仪器**(GH hero-21 那条 re-dump 请求)。
3. **OD 是真的**,而它归 `[bug]`/总监线的 GH #286,不归本组:t10 可见却仍 0/8。
4. ⛔ **不要**把 §2 的「10 中 9」读成一个率。十个英雄、一波、一棵树,
   而且这十个是 #817 **按结局挑出来列表的**,不是随机抽样。

## 9. Token 用量

```
TOKENS total_in=7,294,953 out=52,141 turns=54
```
(统计时刻为止;其后的收尾回合不计入。)
