# 英雄组 2026-09-01T22:55:50Z — GH #392 第三格:`test_wk_considerq_level7_dominance` 的红是**量具换了世界**,不是数字过期

## 认领与范围

- Owner 优先项(P1/P2/P3)本轮**无英雄组的球**(三条分别在协同组/协同组/总监)。
- 取**最新的 open [hero] issue**:**GH #392** 的**最后一格**
  (`test_wk_considerq_level7_dominance.lua:451`,章程 backlog `-71` 第二条)。
  另两格已于 16:51Z / 19:48Z 结案;`-72`(全仓 `GetManaCost` 普查)本轮**不动**,
  它自己是一个工作单元。
- **改动面**:`tests/test_wk_considerq_level7_dominance.lua` **独一份**,+205/−16。
  **`bots/` 与 `game/` 零行**;零新 gate id、零 arm/promote、**零行为改动**;
  零 AWS(连 S3 GET 都没有);不申请波次;`state.json` / `test_set.md` /
  `queue.json` 均无新增。

---

## 0. 先 settle backlog 点名的那个「分歧」——它是个错名字,不是过期的 ground truth

`-71` 记的起点是:「按名字取 `skeleton_king_wraithfire_blast` 读到 **rank 0 / cost 0**,
而该文件自己的 ground-truth 节断言 **rank 1** ⇒ 两者读的不是同一个句柄,或 ground
truth 过期。」

**settle:是前者,而且原因平淡**——引擎里这个技能叫
**`skeleton_king_hellfire_blast`**(测试文件顶上的 `Q_NAME` 就是它,`bots/BotLib/
hero_skeleton_king.lua:568` 引的 KV 键也是它)。`wraithfire_blast` 是**技能的显示名**,
不是内部名。mock 对一个不存在的名字返回一个空白句柄 ⇒ **rank 0 / cost 0 是「没有这个
技能」的读数**,不是「这个技能没升级」。ground truth **没有过期**(本轮实测 rank 1、
cd 0、cost 95,见 §2)。那条线索到此作废,真正的诊断在下面,与它无关。

> 归类:铁律 6 家族的「**0 和『没走到』是同一个整数**」——这次是「0 和『没有这个句柄』
> 是同一个整数」。探针按显示名取句柄时**没有任何东西会红**。

---

## 1. 诊断:`c386d5f3` 把一条从来没能为真的合取项唤醒了

`X.ConsiderQ` 的**第一条语句**是
```lua
if not abilityQ:IsFullyCastable() or X.ShouldSaveMana( abilityQ ) then return NONE end
```
而 `X.ShouldSaveMana`(`hero_skeleton_king.lua:1163`)的最后一个合取项是
```lua
and ( bot:GetMana() - nAbility:GetManaCost() < abilityR:GetManaCost() )
```

`c386d5f3`(09-01 修法力)之前,fixture 句柄上每一个 `GetManaCost()` 都答 **0**,
于是这一项在**历来每一帧上**读作 `272 - 0 < 0` = **恒假**。**这条保蓝规则被冻在关位**。
阶梯接上线后它在这一帧上为真,**在两个英雄等级上都为真** ⇒ 第 451 行的
「等级 6→7 就开火」读数变红。

**这不是「数字过期」,是量具换了世界**,所以修法不是把数字挪一格,而是**在两个世界里
各自重新推导一遍**(§2–§5)。

### 1.1 顺带坐实的一件更危险的事:一个**绿着**的断言,理由已经死了

同文件 `[real frame] shipped ConsiderQ is silent at level 6` **跨这次变更一直是绿的**,
但它写的理由是「every branch of ConsiderQ correctly declines」。**那句话 09-01 当天变成
假的**:今天它静默是因为**保蓝在任何分支跑之前就短路了**。
**绿的断言 + 死的理由 = 会被下一个读者继承的那一种。** 本轮把「理由」单独钉在 §5c,
并在该用例上写明它今天是**被多重决定**的。

---

## 2. Ground truth(全部是这一帧的真实数据,新增一节)

`f_232320_wk_od_burst`(20260721_232320,t=380.0):

| 量 | 读数 |
|---|---|
| WK 等级 / 蓝 | 6 / **272 of 272**(满蓝) |
| Q `skeleton_king_hellfire_blast` | rank 1、cd 0、**cost 95** |
| R `skeleton_king_reincarnation` | **rank 1**、**cd 0(就绪)**、**cost 220** |
| OD | 377u、1180/1374(86%) |

价钱来自 `tests/mock/special_value_shapes.lua` 的 `AbilityManaCost`
(`95 110 125 140` / `220 110 0`)。

**R 是 rank 1 且就绪**这一条是新加的 ground truth,而且**必须**加:loader 把 rank 0
**钳到 rank-1 那一档**(`replay_fixture.lua:486`),所以「R 没学」和「R 学了」在价钱上
**不可区分**。若 R 其实未学,§5c 量的就会是「为一个放不出来的技能保蓝」——**另一个 bug**。
本帧实测 rank 1,所以**这一次的保蓝是诚实的**。

---

## 3. 重新推导(替换第 451 行那一句「一个整数」)

### §5b — 把阶梯前的量具重建出来,记录的读数**逐字回来**
把这一帧**槽位 0–5 的每个技能价钱强制回 0**(即 `c386d5f3` 之前 loader 的答案):
- lv 6 → **静默**;lv 7 → **`BOT_ACTION_DESIRE_HIGH`,目标 OD**。
- **2026-08-22 记录的全部内容原样复现** ⇒ **记录没错,是它的世界不在了**。
- 重建按**槽位枚举**而不是名字清单(改名的技能不能偷偷留着价钱),并**自我核验**:
  零技能可零 ⇒ 自己红;Q/R 两个句柄没读到 0 ⇒ 自己红。

### §5c — 真实量具下:**两个等级都静默**,而且**是保蓝答的**
- lv 6 / lv 7 都是 0;
- 并**逐个等级断言 `X.ShouldSaveMana(q) == true`** —— 钉的是**理由**,不是结果。

### §5c(闸位)— 门槛正好是 **R 价 + Q 价**,从句柄读出来不是打进去的
`need = R:GetManaCost() + q:GetManaCost() = 220 + 95 = **315**`;
**314 静默 / 315 开火**(逐个实测)。判据就是 `GetMana() - Qcost < Rcost`,别无其他。

### §5d — §1–§4 的 dominance 结论**完好**
把这一帧的蓝给到 315,等级台阶**原样回来**:**5,6 静默 / 7,8 开火,目标都是 OD**。
⇒ 死掉的**只是**「**一个**整数就能从这一帧到达它」这句话,**不是那个台阶本身**。

### §5e — 刀口:315 是这个 WK **拿不到**的蓝
**max mana = 272 < 315**。给他**满池**再问一次,lv 7 **仍然静默**。
⇒ 在这个英雄等级上、R rank 1 且就绪时,**shipped `ConsiderQ` 构造性地放不出 Q**。

**这不是 mock 的产物**:引擎从来都给技能标价,**所以这个后果在每一局真实对局里一直是活的**;
答 0 的只有我们的 fixture,而且只到 `c386d5f3` 为止。
⇒ **09-01 之前所有「ConsiderQ 在帧 F 上决定了 X」的存档读数,都是在这条保蓝被关着的
情况下取的。**

**诚实边界**:(A) 本轮**不动 `bots/`**——「这条保蓝在真实对局里是不是好行为」是另一个
问题,需要语料而不是一帧;(B) 315/272 是**这一帧**的读数,不是 WK 的通用结论(蓝池随
等级/物品长,R 升级后价钱降到 110→0);(C) `GetCastRange` 仍是量具零(GH #391),
本文件照旧手工锚 525。

---

## 4. 变异台:8 个变异,7 红 1 绿(no-op 控制),条条只红在该红的节

还原**一律走文件外副本**,每个变异之后 **`sha256sum -c` 全部 OK**(4 个文件逐字节相同)。

| # | 变异 | 结果 |
|---|---|---|
| M1 | `hero_skeleton_king.lua` 兜底 `nLV >= 7` → `>= 6` | 红:**§5b、§5d** + 既有 §2/§6(4 处)。§5c/§5e **绿**——保蓝在两个等级上都压着,与兜底等级无关 ✓ |
| M2 | `ShouldSaveMana` 的 `nLV >= 6` → `>= 8` | 红:**§5c×2、§5e**(3 处) |
| M3 | 保蓝末项 `< abilityR:GetManaCost()` → `< 0`(**打回阶梯前的形状**) | 红:**§5c×2、§5e** |
| M4 | 保蓝的 `R cd <= 3.0` → `<= -1.0` | 红:**§5c×2、§5e** |
| M5 | **控制**:`replay_fixture.lua` 的 `local steps = mana_ladder(...)` → `nil`(**把 `c386d5f3` 整个撤销**) | 红:ground truth + **§5c×2、§5e**;**§5b 绿**——§5b **就是**那个世界,这是最强的一个控制 ✓ |
| M6 | **控制**:`§5b` 重建的槽位循环 `0,5` → `0,-1`(什么都不清零) | **只红 §5b**,红在它自己的自核验断言上 ✓ |
| M7 | **控制**:`special_value_shapes.lua` 的 R 价 `220 110 0` → `100 110 0` | 红:ground truth + §5c×2、§5e;**§5b/§5d 绿** ⇒ 315 确实是从阶梯**读**出来的 ✓ |
| M8 | **no-op 控制**:`X.ConsiderQ` 上方插一行注释 | **19/19 全绿** ✓ |

M3 与 M5 一起构成本轮的主证据:**把量具按回 09-01 之前,红的正好是新的四节,而复现记录
的 §5b 一动不动**。

---

## 5. 门(铁律 6)

- **静态**:`bash tools/agent/rc.sh -n 15 bash tools/agent/luacheck_gate.sh`
  → `GATE_EXIT=0 CLEAN`,**`luacheck bots game: 0 warnings`**,
  **`RC_EXIT=0`(裸读,未经管道)**。容器冷启:gate 自己装了 `lua-check`。
  **没有用 `RULE6_BYPASS`。**
- **本文件**:`lua5.1 tests/run_tests.lua test_wk_considerq_level7_dominance.lua`
  → **19 tests, 0 failures**,**`RC_EXIT=0`(裸读)**。改动前同一命令 **`RC_EXIT=1`,
  14 tests / 1 failure**(:451),红先于本轮改动、独立复现。
- **动态(WK 全集)**:`lua5.1 tests/run_tests.lua wk_` → **225 tests, 0 failures**,
  **`RC_EXIT=0`(裸读)**。对照:**上一轮同一命令是 220 / 1 failure**,那唯一一红
  **就是本格**(#392 第二格),本轮清零;新增的 5 个用例是 §5b/§5c×2/§5d/§5e + ground truth。
- **动态全量**:`lua5.1 tests/run_tests.lua` 本轮**同时在跑**(GH #401 说它在 routine
  容器里能跑完);结果见文末「全量套件」一节。**若那一节写着未跑完,它就是
  `UNCERTIFIABLE` 不是通过** —— 本轮的动态证据以 WK 全集 225/0 为准,
  而 trunk 上已知的那一红(`test_incoming_damage_callsite_census.lua` = GH #394)
  **先于本轮、不属英雄组、且本 diff 与它零交集**(它数的是 `bots/` 行,本 diff `bots/` 零行)。
- **开工自检**:`SELFCHECK_EXIT=3`(**裸读,重定向到文件,未经管道**)。
  三类发现**全部先于本轮、且都不属于英雄组**:
  (i) trunk 红 `test_incoming_damage_callsite_census.lua`(published 43→44)= **GH #394**,总监;
  (ii) 2 个 python 检查 **UNCERTIFIABLE**(`test_rc_wrapper.py` / `test_selfcheck_lua_leg.py`,
       都是「跑它时 `lua5.1` 还没装」)= **GH #383/#384** 那一形,**本 diff 零行 python**;
  (iii) 7 条 `SKIPPED-IN-STREAM` 报告节奏洞,最新一条 08-29,与本轮无关。
  ⚠️ **自己翻的车(第一枪)**:本轮第一次跑自检**又是** `| tail -40` + `echo $?`,
  读回 0 —— 那是 `tail` 的码,**什么都没检查**。裸重跑得 **3**。
  这是 evidence-discipline 第 3 条的第 N 次现场,**记在这里是因为它又发生了**。

---

## 6. 下一棒(不许掉)

1. **GH #392 三格全清,本轮已追评并 CLOSE**(`test_wk_roshan_mana_floor` 16:51Z、
   `test_wk_bone_guard_talent_bypass` 19:48Z、本格 22:55Z)。追评 `#392 issuecomment-5501622441`。
2. **已开 [hero] GH #407:§5e 的构造性不可施放**——「保蓝在 R rank 1 时锁掉 315 蓝,
   而 WK 这一带的蓝池够不到」是一条**真实对局里一直活着**的行为读数,需要**语料**
   (不是一帧)来判断它是好是坏。**本轮不改 `bots/` 一行**,只把问题立案。
   验收方式写在 issue 里:按语料统计「R rank≥1 且 cd≤3s 且 maxmana < Rcost+Qcost」
   的活 WK 帧占比,以及这些帧里 Q 的实际施放次数。
3. **backlog `-72`(全仓 `GetManaCost` 普查)保持在最上面**:本轮的 §1.1 给它加了一条
   **判据**——不能只筛「断言里有 mana」的测试,**要筛「理由句里写着某个分支做了决定」而
   实际上保蓝短路了的**;这一类**不会红**,所以 grep 不到,只能靠读理由句。
   Zeus / CM / Axe / Lion 仍**一个都没扫过**。
4. GH #391(`GetCastRange` 量具零)、#386(等录像组)、#390(`wkqdmg` 维持 gated &
   unarmed)状态不变。

---

## 全量套件

`lua5.1 tests/run_tests.lua`(无过滤)本轮**起跑于 22:45Z**,到收尾时(23:05Z,**约 20 分钟**)
**输出文件仍为 0 字节、没有跑完** ⇒ **`UNCERTIFIABLE`——这不是通过,也不是红。**
GH #401 报的「全量在 routine 容器里跑完了(3000/6)」本轮**没有复现到**;
本轮的动态证据以 **WK 全集 225 / 0 failures(`RC_EXIT=0` 裸读)** 为准。

**为什么这一格不影响本轮的结论**:本 diff **`bots/` 与 `game/` 零行**,只动了
`tests/test_wk_considerq_level7_dominance.lua` 一个文件 —— 该文件是**叶子**
(没有任何别的测试 require 它),所以它对套件其余部分的**唯一**影响面是
「它自己红不红」,而那一格是 `RC_EXIT=0` 19/0 直接量到的。
trunk 上已知那一红(`test_incoming_damage_callsite_census.lua` = **GH #394**)
先于本轮、不属英雄组,且它数的是 `bots/` 的行,与本 diff **零交集**。

## Token 用量

```
TOKENS total_in=8,071,749 out=48,965 turns=65
```
(统计时刻之后的收尾回合不计入。)
