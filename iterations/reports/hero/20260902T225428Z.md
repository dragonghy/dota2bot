# 英雄组 2026-09-02T22:54Z — backlog `-78` 第三批(7 → 5),外加 GH #365 §2 的根因

**改 9 个文件,全在 `tests/`。`bots/` 0 行、`game/` 0 行 ⇒ 零行为改动、零新 gate id、
零 arm/promote、零 AWS、不申请波次;`state.json` / `test_set.md` / `queue.json` 本轮无新增。**

- 迁移 2 个:`test_aegis_grouping.lua`、`test_tpreach_band.lua` ⇒ raw **7 → 5**、migrated **15 → 17**
- 棘轮:`test_soakside_shared_switch.lua` 的 `RAW_CEILING` 7 → **5**、`MIGRATED_FLOOR` 15 → **17**
- 新修 6 个:`tests/lua_source_scan.lua`(共享扫描器)+ 5 个**观测到过红**的走查
  (`test_gate_claim_consistency` / `test_gated_helper_nesting_census` / `test_item_name_census`
  = **GH #365 §2** 公布的三个,加本轮两次自检各新加的 `test_coarmed_attribution_register` /
  `test_activemode_call_site_census`)—— 它们把 gitignored 的开关文件当「shipped 源码」扫,
  列目录与打开之间有窗口。
- 撞上一条**别人推出来的 trunk 红**:`test_item_name_census.lua` 的 MOVED 钉
  `bots/ability_item_usage_generic.lua:6876 → 6886`(协同组 `77e18be9` 在钉子上方加了 10 行)。
  ⚠️ **协同组在几分钟内自己也重锚了同一条(`f00226b2`)** ⇒ rebase 冲突,
  **保留他们的注释**(它点名了确切的那十行 :3405),我这边只留一句独立见证。详见 §7。
- 已发表:GH **#365** 追评(根因)、GH **#438** 新开(剩余作用域 + 自检横幅)、
  GH **#410** 追评(两个新载体)。

---

## 1. 选题

- `iterations/OWNER_PRIORITIES.md` 三条常设项(发波 Spot 优先 / P1 拉野 / P2 低血不回家)
  当前球分别在**批测台 / 协同组 / 协同组**,**无本组项**。
- open `[hero]` issue 三条,与前三轮同因:**#417** 判定权在总监(且本轮的读数正好又打在它身上,见 §2)、
  **#416** 上一轮已把它自己写的三条验收执行完并追评、**#407** 被「拿到读数之前不许改 `bots/`」挡住
  (读数是 `queue.json:hero-27`,`executor=batch-desk`,硬时限 2026-09-22)。
- ⇒ 取 backlog 最上面的 `-78`,并按它自己点名的顺序取**第三批**:`aegis_grouping` / `tpreach_band`。
  这一批**上一轮顺延过一次**,本轮执行掉,棒没掉。

## 2. 主产物一:GH #417 的机制在第三、第四个文件上复现,而且**两个文件的刀口是同一句「字母序」**

`-78` 迁移的价值论证一直是构造性的,本轮两个文件各给一格**两格对照**(种下一枚继承残留开关,
一次跑整文件、一次只跑 unarmed 用例):

| 文件 | 整文件(种残留) | 只跑 unarmed 用例(同一枚残留) | 只跑 unarmed(无残留) |
|---|---|---|---|
| `test_aegis_grouping` | **6/6 绿,EXIT=0**,残留被**删掉** | **红**:`:128: off the candidate side the guard must stay inert` | 绿 |
| `test_tpreach_band` | **7/7 绿,EXIT=0**,残留被**删掉** | **红**:`:92: shipped default scans only 700 -- the [700, reach] band is unreachable` | 绿 |

两边的机制一模一样,而且**都是用例名的字母序在决定答案**:`FIRE`/`NO-FIRE` 排在 `OFF` 前面,
`armed:` 排在 `unarmed:` 前面 ⇒ 先跑的 armed 用例结尾那句**无条件 `os.remove`** 替后面的
unarmed 用例把前提「修好」了。`tpreach_band` 这一格尤其难看:红掉的那条正是**钉住缺陷本身**的
case 1(「shipped 默认扫不到 [700, reach] 带」),也就是说**继承残留会让这个文件把自己要证明的
缺陷读成已经修好了,还打 EXIT=0**。

迁移后同样两格:两个文件**在装载时就红**,而且**不再删陌生人的开关**(实测 `leftover_after=PRESENT`):

```
setup error: bots/Customize/soak_side.lua already exists (test_aegis_grouping), so "gate off"
is NOT what this measures. ... Contents: return { side = 'radiant', cand = 'aegisgroup' }
```

## 3. 主产物二:并发 `rm` 台的**逐断言**前后对照 —— 「变红」不是收益,「点名」才是

同一条并发 `rm` 循环,每个文件跑 8 轮:

| 文件 | HEAD:失败断言数 | HEAD:点名 | 迁移后:失败断言数 | 迁移后:点名 |
|---|---|---|---|---|
| `test_aegis_grouping` | **8** | **0** | **40** | **40**(全部) |
| `test_tpreach_band` | **8** | **0** | **40** | **40**(全部) |

⚠️ 计数口径:`run_tests.lua` 按 GH #216 把每条失败打**两遍**(即时行 + 收尾块),所以原始
grep 计到 80 行 —— **40 条失败、每条都带点名诊断**,不是 80 条。这一行是本报告里唯一一个
需要除以 2 的数,写出来是因为铁律 4(iii):**登记效应量要连切法一起登记**。

**8 → 40 不是回归**,是同一件事从「看不见」变成「看得见」:HEAD 上一轮只有 1 条断言察觉到
开关被删,另外 4 条 armed 用例**在开关已经不在的情况下照样通过**(它们读到的是未武装的树,
而那正好等于它们期待的值之一)。迁移后 `assert_still_armed` + 写后读回把这 5 条全部变成
**指名开关的失败**:

```
bots/Customize/soak_side.lua was REMOVED under this case, so the armed leg read no switch and
any value it produced is the UNARMED value -- do not "fix" the expectation. ... (GH #365 §3, GH #229)
```

HEAD 那 8 条失败的文本则是纯值不匹配(`a lone aegis-carrier deep in enemy territory must be
told to regroup` / `armed, an enemy that can strike us at 900 must break the channel read`)——
**一句都不提开关**,正是 GH #365 §3 当年花掉一轮去争论「期望值对不对」的形状。

## 4. 棘轮:两个常数都改了,而且各自被变异证明是承重的

`RAW_CEILING` 7 → **5**,`MIGRATED_FLOOR` 15 → **17**;实测普查 `RAW(5) / MIGRATED(17)`,
剩下的 5 个恰好是 `-78` 点名「最后迁」的那 5 个(`bbfight` / `bbrespawn` / `bbshort` /
`pollyhp` / `salveally`)。

- **M-C(迁移没落地)**:把 `test_tpreach_band.lua` 还原成 HEAD ⇒ 棘轮**红**,而且数字对得上:
  `6 files carry their own unchecked write+delete of the shared switch, up from 5`。
- **M-D(假迁移)**:给一个 raw 文件(`test_pollyhp_getter_axis.lua`)加上
  `require('mock.soak_side')` 但**保留它自己的 `io.open`** ⇒ 普查读数**纹丝不动**
  `RAW(5) / MIGRATED(17)`。⚠️ **这一格必须看普查读数,不能看棘轮的绿**:棘轮在 M-D 下是
  16/16 绿,但如果假迁移真的被算进 migrated,读数会变成 4/18,而 `4<=5` 与 `18>=17`
  **也全都通过** ⇒ **棘轮的绿在这一格上零信息量**,只有读数本身能答。
- 负控:还原后棘轮 16/16 绿。

另外把 `RAW_CEILING` 上面的注释改写了一句要紧的:剩下的 5 个**不是 backlog 余数,是 S2 自己
那条读数的活载体**(它们的 `if sCand == nil then os.remove` 才是「unarmed 腿本身就是删除者」
的证据)。它们迁走的那一次,**必须在同一个 change 里**把 S2 改成引用档案而不是引用活文件。

## 5. ⭐⭐ 主产物三(计划外):**GH #365 §2 的根因**,而且它从来不需要等 GH #229

**怎么撞上的**:开工自检在后台跑,我在前台跑并发 `rm` 变异台。自检的 Lua 检测器腿于是报
**`TRUNK RED -- 6 of 73`**,其中三条长这样:

```
RED  test_coarmed_attribution_register.lua
      tests/test_coarmed_attribution_register.lua:95: cannot open bots/Customize/soak_side.lua
RED  test_gate_claim_consistency.lua        (:42, 同一句)
RED  test_gated_helper_nesting_census.lua   (:72, 同一句)
```

**⭐ 这不是新缺陷 —— 这是 GH #365 §2 里那三条「假红」,逐字相同。** #365
(2026-08-31)公布的载体是 `gate_claim_consistency:42` / `gated_helper_nesting_census:72` /
`item_name_census:60`,把它们**归因给 GH #229 的「两个 gate 测试抢同一个开关」**,
并把修复**路由进 #229 的作用域**。#229 至今 open 且卡住 —— 它要的是**读侧**的改动
(`jmz_func` 经 `GetScriptDirectory` 解析开关,而那个函数还负责加载每一个模块)。
本轮的读数是:**那三个文件根本不是 gate 测试,一行都不写开关,它们只是走过它**;
⇒ **这一半从来不需要等 #229**,修法在它们自己的 walk 里,一行。

**机制**:这些文件各自 `io.popen('find bots -name "*.lua" | sort')` 列出全树,再对每个路径
`assert(io.open(path))`。`bots/Customize/soak_side.lua` 是 **gitignored 的、farm-only 的、
本套件里每一个 gate 测试都在创建和删除的**文件 ⇒ **列目录与打开之间有一个窗口**,
落进窗口就红,而红的文本**指着一个跟它们的主题毫无关系的文件**。
共享扫描器 `tests/lua_source_scan.lua:M.bots_files()` 的 docstring 写的是
**「Every *shipped* Lua file under `bots/`」** —— 开关文件按定义就不是 shipped,
**这一句从来没被 walk 兑现过**,而那正是缺陷的整个形状。

**观测到过红的载体共 5 个,本轮全修**:#365 §2 的三个
(`gate_claim_consistency:42` / `gated_helper_nesting_census:72` / `item_name_census:60`)
+ 本轮两次自检各新加一个(`coarmed_attribution_register:95`,以及第二次自检里
`activemode_call_site_census:94` 的 `bad argument #1 to 'lines'` —— **同一个窗口的
`io.lines` 变体**,#365 那次都没跑到它们)。

**⚠️ 域没有量准,如实写**:第一版用 `grep "io.popen('find "` 数出 18 个走 `bots/` 全树的文件,
**那个 grep 太窄**;放宽后光是 `io.popen` + `find` 的调用点就有 **23 处**,其中
**至少 17 处**仍走 `bots/`(或一个可能等于 `bots` 的 `dir` 变量)且没有排除。
`test_talent_value_read_anchor` 只走 `bots/BotLib` ⇒ **结构上碰不到**。
几个用 `dir` 变量的要逐个读才能定,**这件事本身进 `-79`**,不在本轮声称。
本轮的取舍是**「先修有观测到过红的」**(`-77`/`-78` 的同一条排序原则),不是「修完所有的」。

**证的是「窗口」不是「内容」**:开关在场 vs 不在场,我量到的那 18 个文件读数**逐位相同**
(`tests=158 failures=0`,修前两次 + 修后一次共三次)⇒ 把这两个 farm-only 文件排除出 walk,
**对读数是 no-op**,只拿掉窗口。

**复现与修复读数**(churn 循环 + 每个文件 6 轮):

| | `coarmed` | `gate_claim_consistency` | `gated_helper_nesting` | `item_name_census` | `activemode_call_site` |
|---|---|---|---|---|---|
| 修前 | **2/6 红**(点名开关) | #365 §2 + 本轮自检各 1 次 | 同左 | #365 §2 观测 1 次 | 本轮自检观测 1 次 |
| 修后 | **0/6** | **0/6** | **0/6** | **0/6** | 定向 2/0 绿 |

修法:`find ... ! -path "bots/Customize/soak_*.lua"`(两个 gitignored 文件 `soak_side.lua` /
`soak_pool.lua` 一起,`.gitignore:75-76`),落在**共享扫描器 + 那 5 个载体**上。

**失效方向说清楚:这是假红不是假绿**,所以它吵而不骗。代价不是它骗了谁,是
**它把一件一行能修的事路由进了一个卡住的 issue,然后在那儿躺了两天又复发一次**——
#365 §2 自己就是这笔代价的读数。

**⚠️ 修这四个文件时差点自己踩一次同族的坑,登记**:第一版注释里**逐字写了开关的字面路径**,
于是 `test_soakside_shared_switch.lua` 的普查(它按字面量 grep `tests/test_*.lua`)
把这四个普查文件**当成「碰开关的文件」收编进了 RAW 集**:`RAW(5) → RAW(9)`。
棘轮**照样 16/16 绿**(S2 数的是 write+delete 都有的文件,这四个一个都没有)⇒
**又是一次「读数已经错了而门是绿的」**。改成 `cannot open <that switch>` 后回到
`RAW(5) / MIGRATED(17)`,驱动也回到 `DRIVER_FILES=23 DRIVER_TESTS=302 DRIVER_FAILURES=0`。
这正是该普查自己 header 里为 `SELF` 写下的理由(「它在自己的散文里提到这个路径,
所以必须排除自己」)—— **那条理由对第二个文件同样成立,而没人写下来**。
抓到它的是**重跑一次普查探针**,不是任何一道门。

## 6. ⚠️ 自伤两笔,都登记

1. **我自己的变异台污染了并发跑着的开工自检。** 那次 `TRUNK RED -- 6 of 73` 里,
   **六条全部**由我的旁路进程造成:三条是上面的 TOCTOU,另外三条
   (`test_salvepool_missing_floor` / `test_salveyield_arbitration` / `test_soakside_shared_switch`)
   的文本里直接印着我 churn 循环写的字节 **`cand = 'x'`**。
   ⇒ **那一次自检的 Lua 腿读数作废,不是 trunk 判决**。自检自己那句免责写的是
   「Whether **main** is red too is NOT established by this line」——它提醒了「main 可能是绿的」,
   **没有提醒「你自己容器里的兄弟进程可能就是原因」**。这一格给总监,见 §8。
   本轮已在**安静的树上重跑一次自检**取真读数(§7)。
2. **`pkill -f <标记>` 把我自己的 shell 杀了三次(每次 `EXIT=144`)**,因为标记字符串
   **同时出现在调用方自己的命令行里**(第三次是 `pkill -f "routine_selfcheck.sh"`)。
   上一轮的教训是「别用 `kill $!`,用 `pkill -f`」;**本轮补上它的下一半,而且这一半更贵**:
   **`pkill -f` 的模式绝不能出现在你自己的命令行里** —— 否则它是一条**自杀命令**,
   而且失败的样子是「命令莫名其妙返回 144」,不是「pkill 报错」。
   安全形式:`ps -eo pid,ppid,args --no-headers` 挑出 pid 再 `kill <pid>`,或让循环脚本
   自己写 pid 文件。
   而且 `setsid ... & $!` 拿到的是 setsid 自己(立刻退出)的 pid ⇒ `kill -- -$!` 打空,
   **留下 2 个孤儿 churn 循环**,靠按 pid 点杀才收干净
   (已确认 `bots/Customize/` 连续 2s 无开关文件)。

## 7. 门

- **开工自检**:第一次调用**又被拒答**(`SELFCHECK_EXIT=2 REFUSED`,stdout 接进了 `tail`)——
  证据纪律 3 的**第 9 次**现场,依然是本轮第一条命令。改成重定向到文件后跑完:
  **`worst exit: 3`**,`legs run 9`、`UNCERTIFIABLE: none`,findings = `cadence` +
  `trunk-red(python)` + `trunk-red(lua)`。
  - **⭐ 前两次自检的 Lua 腿读数都作废**(`TRUNK RED -- 6 of 73`,§6.1:六条全是我自己造的)。
    **第三次在安静树上、push 之后重跑**(容器里没有任何我的前台 lua 进程),这是本轮唯一
    可用的 Lua 腿读数:**`TRUNK RED -- 1 of 73`**,`SELFCHECK3_EXIT=3`。
    **6 → 1 之差全部是污染**,不是修好了什么。
  - 那 **1 条是真的,而且不是本轮的**:`test_item_name_census.lua:57` 的 MOVED 钉
    `LOOKUP item_new bots/ability_item_usage_generic.lua:6876 -> 6886` ——
    协同组 `77e18be9`(GH #437,gated `wandbleed2`)在同一文件里那个钉子上方加了 10 行。
    **本组本轮 `bots/` 零行**;钉子在本容器里 **rebase 前是绿的、rebase 后是红的**,
    而两个文件本轮都没被本组改过。已按该测试自己的指示**重锚**(6876 → 6886,
    站点逐字不变 `X.ConsiderItemDesire["item_new"] = function( hItem )`),
    这是该钉子第 13 次、**连续第 3 次**被一个没理由读这个文件的协同组回合推走 ——
    文件自己 :265 那句「**开工自检认证的是你到达的那棵树,不是你推出去的那棵树**」
    本轮从另一侧又得了一个证人。
  - `trunk-red(python)` **3 条,全部先于本轮,且本轮零 `.py` 改动**:
    `illumove_pairs:HP_CUT`(= **GH #410**,已 open)+ **两条新的**
    `wandbleed_trigger:HP_MAX` / `wandbleed_trigger:HP_MIN_EXCLUSIVE`,
    来自录像组 21:55Z 的 `9cf795bb`(同一个 `HP_CENSUS` 未登记类)。⇒ 追评 #410,不新开。
- **静态(铁律 6 第一门)**:`bash tools/agent/rc.sh bash tools/agent/luacheck_gate.sh` ⇒
  **`GATE_EXIT=0` / `luacheck bots game: 0 warnings` / `RC_EXIT=0`**,冷启自装,
  **没有用 `RULE6_BYPASS`**。
  - ⚠️ 但 `tools/agent/ensure_lua_toolchain.sh lua5.1 luacheck` 在本容器**先失败了一次**
    (`could not provide lua5.1` / `could not provide luacheck`,退出 1),而**紧接着**
    一句裸 `apt-get install -y lua5.1 lua-check` **成功**(~2s)。当时开工自检正在后台跑,
    最可能是 dpkg 锁被占;**这是假设不是结论**(没有复现,`>/dev/null 2>&1` 把 apt 的
    错误吞了)。记在这里,因为它的失效方向和 GH #205 立案那件事**同族**:
    一个「买不到工具」的读数会被下一轮当成既成事实读回去。
- **动态定向**:
  - 两个迁移文件各自 **6/0** 与 **7/0**;棘轮 **16/0**。
  - **同进程按套件序跑完所有碰开关的文件**:`DRIVER_FILES=23 DRIVER_TESTS=302
    DRIVER_FAILURES=0`,**跑完开关文件不存在**(与上一轮同口径同数字)。
  - 18 个走 `bots/` 全树的文件:开关在场 / 不在场 / 修完,三次都是 **`tests=158 failures=0`**。
  - 三个 TOCTOU 载体修后 churn 台 **0/6 红**(修前 `coarmed` 2/6)。
- **全量套件:没跑成,不声称。** 起过一次,但那次与我的 churn 循环有时间重叠 ⇒ 读数被污染,
  **主动掐掉了**,没有拿它当证据。按「只有同一轮里读到的那行总结才构成证据」记为未跑。
  动态那半的完整读数本轮**不成立**;上面的定向读数是本轮实际买到的东西。

## 8. 交出去的棒

1. **GH #365 已追评**(`#issuecomment-5517689021`,`claim_precheck` `RC_EXIT=0`,发表在 push 之后):
   §2 那三条「假红」的**根因**是 walk,不是 #229 的竞争,**修复不必等 #229**;本轮修了
   共享扫描器 + 5 个观测到过红的载体。顺带把 §6.3 那条建议(「跑完任何 gate 测试后
   `soak_side.lua` 不存在」的普查)与本轮的 `DRIVER_FILES=23 ... 跑完开关文件不存在` 对上 ——
   那条不变式现在由属主的 `disarm`+`assert_clean` 承担,已经有人在跑了。
2. **GH #438 已开**([harness],给总监):剩余作用域(至少 17 处调用点,**第一件事是把域量准**)
   要裁修法 (a) 逐个加同一行 / (b) 都收到共享扫描器上;外加开工自检 `TRUNK RED` 横幅
   **不区分「trunk 红」与「同容器兄弟进程造成的红」**(与 GH #420 同族,不同格),
   以及 `pkill -f` 自杀那条操作教训。
3. **GH #410 已追评**(`#issuecomment-5517692858`,`claim_precheck` `RC_EXIT=0`):
   同一个 `HP_CENSUS` 未登记类**又多了两个载体**
   (`wandbleed_trigger:HP_MAX` / `HP_MIN_EXCLUSIVE`,来自 `9cf795bb`)。
   **只登记载体,不做分类** —— #410 自己的立案句就是「分类只有作者答得了」,
   而这两条的作者不是本组。
4. **backlog**:`-78` 只剩最后那 5 个(**要连 S2 改写一起做**,已写进棘轮注释);
   新增 `-79` = 剩余的 `find bots` 走查,**第一件事是把域量准**。

## 9. token 用量

```
TOKENS total_in=32,099,189 out=105,835 turns=181
```

(统计时刻之后的收尾回合不计入。)
