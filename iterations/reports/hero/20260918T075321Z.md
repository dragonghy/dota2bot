# 英雄组 2026-09-18T07:53Z — 一条自称「读树」的扫描,枚举的是「语料」:`wkqaim` 的前置读数重取

**P4.4 自评:(ii) 判定完结所需的最后一块证据** —— 更准确地说,是**一条支撑现行判定的
已发表证据被重取,并且被证伪**。判定本身不动,**理由换成更窄的一条**。
**`bots/` 本轮改动是纯注释、零行为。**
**零 gate id / 零 arm / 零 promote / 不申请波次 / 不申请供帧。**
**成本:零 EC2 / 零 CE / S3 读取 0 个对象(出网未计价)。**

**工作单元**:章程 `-202` 交出的「下一轮主体候选·第 1 条」(**已连排两轮**)——
`X.ConsiderQ` 通用兜底出货点(点 10)的 `wkqaim` 候选。前置**逐字照做**:先读
`bots/BotLib/hero_skeleton_king.lua` 那份 PRE-FLIGHT 笔记。
读完**没有写候选**,而是**重取了那份笔记赖以成立的读数**——因为它不成立。

---

## 一 头条:不是「枚举器瞎了」,是**作用域与枚举不一致**,而仓库为这件事立过规矩

PRE-FLIGHT 笔记(以及 `queue.json:hero-1` 的 result、`tests/test_wk_q_aim_preflight.lua`
的 section 1 TRIPWIRE)发表的是这条**全称命题**,逐字:

> every frame carrying a living WK with two or more living enemies inside 568u
> has the Blast unlearned or on cooldown. **The branch is not reached on a single one.**

⛔ **它是假的。** 但**不是**「GH #281 那条 glob 地平线又犯了一次」——
`tests/frames/README.md` 把这件事写成了**设计**,并立了规矩,逐字:

> A frame staged here is **invisible to every corpus scan BY DESIGN**, and that cuts both ways.
> … **Any scan that claims to read "the tree" rather than "the corpus" has to enumerate
> `tests/frames/` too.**

⭐⭐ **而这个文件早就自称读树了。** 它抬头那节 §THE HORIZON 就是干这个的:
**2026-08-28** 那一轮**按名字 append** 了 `iterations/pending/` 那一帧,理由逐字是
「the tripwire's universal was, **strictly, a statement about one directory**」。
⇒ **它把作用域改了,没有把枚举一起改。** `tests/frames/` 从头到尾没进过它的枚举。
于是 **2026-09-17(`907dd7bf`)** WK blast 六帧与 Zeus 八帧落地那一刻起,
**树里有三帧证伪它,而它还在发表那句话**。

⇒ 精确的时间线(⛔ 不要写成「错了三个星期」):
**2026-08-28 起这句话无根据**(作用域已改、枚举没改);
**2026-09-17 起这句话为假**(证伪它的帧进了树)。

### ⭐ README 自己那道开工检查在原理上抓不到它 —— 这才是可复用的那一条

README 两处**加粗**写着开工前要跑 `rg -l 'tests/frames' tests/`。
**那条 grep 找的是「已经枚举该目录」的扫描**(今天 89 个),
**它在原理上找不到「应该枚举而没有」的那一个** —— 缺席对「搜索缺席的那个字符串」不可见。
⇒ **2026-09-17 那轮把 staging price 量成「1 file(paid)」是量对的,本文件被正确地排除在外。**
**缺陷在检查的方向上,不在任何人的尽责上。**

⚠️ **同一个英雄、同一族、同一周的旁证**(本轮实测):

| 文件 | 枚举 `tests/frames/`? |
|---|---|
| `test_wk_level_supply_horizon.lua` | 是(2026-09-07 补上,**代价是 trunk 红了一天**,README 有记) |
| `test_wk_q_castrange_meter_domain.lua` | 是(staged 当轮结清) |
| `test_wk_q_teamfight_reach_pricing.lua` | 是(**2026-09-17,本组自己**) |
| `test_wk_q_aim_preflight.lua` | **否** ← **偏偏是扛着那条已发表全称命题的这一个** |

## 二 重取的读数(⛔ 只动枚举,判据一行没动)

| 读数 | 旧(`tests/fixtures` + PARKED) | 新(再加 `tests/frames/`) |
|---|---|---|
| 枚举到的帧文件 | 113 | **145** |
| 其中有**存活 WK** 的帧 | 37 | **52** |
| 568u 内 **≥2 活敌**的环帧 | 6 | **12** |
| 供给(未学/冷却)挡下的环帧 | 6 = **全部** | **9 / 12** |
| **到达分支门口(Blast 可施法)** | **0** | **3** |

三帧全部在 `tests/frames/`:
`f_260909_215040_wk_blast_lane_121`(英雄 3 级,最近 spirit_breaker 388u / 最残 slardar 429u)、
`f_260909_215040_wk_blast_sb_1052`(20 级,最近 = 最残 = slardar 203u)、
`f_260909_215227_zeus_arc_od_79`(2 级,最近 = 最残 = sniper 245u)。

⚠️ 与 `tests/frames/README.md` 那一行**独立对得上**:它写着这六帧里
「**three of the six drive the shipped tree to order that spell** — against 0 of 36 for
Wraith King in the whole corpus」。

## 三 ⛔ 处置不动(`wkqaim` 仍不写),但**理由换了、而且更窄**,请引新的那条

**到达分支 ≠ 见证候选。** 候选的**全部**行为差是把 `nEnemysHerosInRange[1]`(最近)
换成血量最低的那个,所以一个见证帧还要再过两关:

1. **这条分支自己的 `nLV >= 7`**(⭐ 从源码解析,不抄数字)——三帧里两帧是 **3 级 / 2 级**;
2. **最近 ≠ 最残**——唯一过了等级门的那帧(20 级)最近与最残是**同一个单位**,
   候选在那里与出厂行为**逐字节相同**。

⇒ **见证帧 = 0**,在一个比旧零大 **28%** 的语料上。

⭐ **真正该被引用的是这一句**:那个**本来会**成为见证帧的帧(3 级那个)
**被挡住的是等级门,不是供给、也不是瞄准**。
旧笔记把整条域的空说成「供给饥饿」——在宽语料上供给只解释 12 帧里的 **9** 帧。
**而等级门是关于这条分支的事实,不是关于语料的事实。**

⭐⭐ **它顺带把 `queue.json:hero-1` 从「再扫一遍看看」变成「档案能答、fixture 库在结构上答不了」**:
本仓可达 WK 帧是 **3 / 2 / 20 级**(兄弟定价文件同记 band 帧 1..9 级)⇒
**fixture 库在 `nLV >= 7` 这根轴上几乎是空的**;而批测台 2026-08-22 在 hero-1 上
免费量到的分母是 **153 局含 WK、153/153 局终 level ≥ 7(min 7 / median 11 / max 16)**,
并逐字写着「level >= 7 这一层**几乎不构成过滤**」。
⇒ **唯一挡住见证帧的那根轴,正是档案那 153 局管够、而 fixture 库没有的那根。**

## 四 落地(四个文件;`bots/` 那份是纯注释)

1. **`tests/test_wk_q_aim_preflight.lua`**(9 例 → **12 例**,全绿):
   - `fixture_files()` 枚举 `CORPUS_DIRS = { tests/fixtures, tests/frames }`,
     **按 basename 去重**(同一帧 staged 一份、landed 一份必须只读一次,
     否则 `wk_frames` 这个本文件当下界用的量会虚高),`PARKED` 仍照旧 append;
   - **新** `section 1 COVERAGE`:任一 `CORPUS_DIRS` 条目枚举到 0 帧就红。
     ⭐ **它是那条 grep 反过来指的方向**:问的是「**本文件的枚举**盖住这些目录了吗」,
     不是「这个目录在哪儿被提到过」;
   - **TRIPWIRE 重新瞄准**:从「没有环帧到达分支」改成
     「没有环帧**同时**到达分支、过 `nLV >= 7`、且最近 ≠ 最残」。**红 = 好消息**,
     红出来的那一帧就是 `hero-1` 要的那一帧;
   - 旧的「supply every time」改写成 **三桶 PARTITION 且断言覆盖全集**
     (supply / 等级门 / 零差)。⛔ **没有松成「大多数是 supply」**——
     那会把三个例外留在没人数的地方,**旧的零正是被那种松法养活的**;
   - `catchall_level_gate()` **解析源码** `and nLV >= (%d+)`;
   - **新 section 5 退役守卫**:`bots/` 里出现 `IsSoakCandidate('wkqaim')`、
     或 PRE-FLIGHT 笔记被删,本文件就红(一份**判决文件**最坏的失效是
     「候选真的落地了,而它还在解释为什么没人落地」)。
2. **`bots/BotLib/hero_skeleton_king.lua`**:PRE-FLIGHT 笔记就地更正,
   ⛔ **旧句子保留原文并标注为假**,不是删掉。**纯注释,零行为。**
3. **`tests/frames/README.md`**:在 WK blast 那一行补一句——
   「staging price 1 file(paid)」**量对了**,而**第二个短了一个目录的文件
   那道检查在原理上找不到**。⛔ 明确写清**没有任何东西被 admit 进 `tests/fixtures/`**。
4. **`tools/agent/mutstand_wkqaim.sh`**(新):M1–M8,**M5 是实测等价体、不计分**。

⛔ **本轮没有 admit 任何帧到 `tests/fixtures/`。** staged 帧仍然 staged;
动的只是**这一个文件自己的作用域追上它自己的声明**。
代价因此只落在本文件自己的读数上,**且在同一轮内重新推导完毕**——README 的标准。

## 五 变异台(`tools/agent/mutstand_wkqaim.sh`)

**7/7 caught**(M5 是**实测等价体**,报告但不计分),baseline 79 例绿,每轮 ~123s。

| 变异 | 结果 |
|---|---|
| M1 `CORPUS_DIRS` 缩回单目录 | caught — PARTITION 说「supply now refuses all 6 ring frames again」 |
| M2 见证判据丢掉 `nLV` 门 | caught — TRIPWIRE 点名那个 3 级帧 |
| M3 见证判据丢掉 aim delta | caught — TRIPWIRE 点名那个 20 级零差帧 |
| M4 **控制项**:每个环帧都声称有 delta | caught — 证明 TRIPWIRE **能**响,不是散文 |
| M5 **实测等价体**:`aim_delta` 恒 false | **SURVIVED as recorded** — ⛔ **不许读成「半径没被覆盖」**:它说的是**过了等级门的帧一个 delta 也没有**,即本轮的结论本身 |
| M6 枚举器在常量背后只走第一个目录 | caught — COVERAGE 说「enumerated 0 frames out of tests/frames」 |
| M7 **改 `bots/` 源码**把等级门 7 → 1 | caught — 消息里逐字印出 `hero level >= 1` ⇒ **读数真的取自源码,不是抄的** |
| M8 真落一个 `wkqaim` gate | caught — section 5 退役守卫 |

## 六 交棒(两条,同一工作单元内交出)

1. **`queue.json:hero-1`**(status 保持 `pending`,result 追加本轮一段):
   请求原文的判据**本来就是对的**;本轮**没有改口径**,补了一条 **(iv):
   扫的时候必须把「到达分支」与「见证候选」分开计数** ——
   只报到达数会把一个零差帧记成一次见证,把补丁的收益读高。
   改的主要是**为什么现在值得去扫**:fixture 库在 `nLV >= 7` 这根轴上结构性地空,
   那 153 局在这根轴上管够。⛔ 仍 **NO NEW WAVE NEEDED / 零 AWS**。
2. **GH #281 追评**(它问题 1「53 个文件里有多少在拿扫不到当零用」二十一天零推进):
   本轮给它一个**实测实例 + 一个可抄的钉子形状**,并且**更正它的诊断方向**——
   本例不是 glob 写法的问题,是**作用域声明与枚举不一致**,
   ⇒ 建议把普查的键从「用了哪句 glob」换成
   **「这个文件的全称命题,量化在几个帧目录上」**。
   ⛔ 没有代总监做全仓那一份;本组只把自己作用域那一片排进下一轮候选第 3 条。


## 七 ⚠️ 限制 —— 引用上面任何一个数之前先读这一节

1. **语料是有偏的裁剪集,不是 turbo 的随机样本。** 每一帧都是为**别的**调查冻的,
   WK 的 52 帧是搭便车的。**「见证帧 = 0」是存在性阴性,不是密度估计** ——
   §Y.2:桌面/fixture 读数能证 **EMPTY,不能证 RARE**。本请求因此**不算 done**。
2. **环忽略视野。** 引擎的 `GetNearbyHeroes` 是受视野限制的,本扫描把每个活敌都算进环
   ⇒ 每个环都是**上界**,所以「到达帧 = 3」也是上界,方向安全;
   而「见证帧 = 0」建立在这个上界之上 ⇒ **结论在安全的一侧**。
3. ⚠️ **568u 这个环在另一个方向上是下界。** `X.ConsiderQ` 上面有一条把 `nCastRange`
   抬 **+260** 的条件分支(单个敌人、朝向本体、攻击距离带);本扫描按固定 525 建环,
   那条分支真发生时环是 828。**本轮没有量它**,记在这里,⛔ 不要把「12 个环帧」
   当成环帧的全集。
4. **到达帧的三条属性是 fixture 上的读数,不是引擎读数。** 本扫描复现的是
   `X.ConsiderQ` 进入任何分支前那道 castability 守卫(`IsFullyCastable` + `X.ShouldSaveMana`),
   **不是**兜底分支那五个合取项的全部(`#nEnemysHerosInView > 0 or WasRecentlyDamagedByAnyHero`、
   非撤退、`X.wk_IsCatchAllOddsOk`)。⇒ 「到达分支门口」逐字就是**门口**,
   **3 仍然是上界**。⛔ 不要把它读成「这三帧上兜底分支真的开火了」。
5. **等级数字取自 fixture 的 `level` 字段**,与 `nLV = bot:GetLevel()` 同源但不是同一次读取。
6. ⚠️ **`aim_delta` 用的是 fixture 的 `hp` 字段选「最残」,而 shipped 的
   `nEnemysHerosInRange[1]` 是引擎按距离排序的第一个** —— 本扫描用的是自己算的距离排序。
   两者在**顺序**上一致是 2026-08-22 已经核过的事(`tests/test_mock_nearby_heroes_order.lua`
   之前的字母序缺陷已修),但那是一次**历史核对**,不是本轮重测的。

## 八 闸(三条腿,逐字抄)

- `luacheck_gate.sh` → **`GATE_EXIT=0` CLEAN**,`luacheck bots game: 0 warnings`
- `py_gate.py` → **EXIT=0**,`py gate: 134 ran, 0 findings, 0 uncertifiable, 59.1s`
- `lua_gate.py` → **EXIT=0**,`lua gate: 425 ran, 0 findings, 0 uncertifiable, 10 unanswered, 5 known-red, 769.5s`

⚠️ **三条腿的退出码都是无管道读的**(每条 `> 日志; echo EXIT=$?`,或后台任务自身的退出码),
evidence discipline 3。
⚠️ `lua_gate.py` 报 `the hook's REAL cost this run is 769.48s against a 540.0s budget`
(88 个不在 manifest 里的新测试被顺带跑了,10 个超预算被排除)——**这不是本轮造成的**,
本轮没有新增 Lua 测试文件,只改了一个已在 manifest 里的文件。照实登记。

⚠️ **开工自检(铁律 10)本轮的实测形状要照实写**:第一次调用**按设计拒绝执行**
(`REFUSED: stdout is a PIPE ... this is evidence discipline 3; it has recurred 5x,
every time as the first command of the round`)——**第 6 次**,又是本轮第一条命令。
改用 `> /tmp/sc.log; echo EXIT=$?` 重跑后它跑了 **>25 分钟未完**,
而此时变异台已经开始**就地改写 `bots/` 与测试文件**;
按 `mutstand_*.sh` 自己的警告(⚠️ 不得与全套件/自检并发,GH #507 / #848),
**本轮主动 `pkill` 了自检**(退出码 144),以免它读到一个变异体并报出假发现。
⇒ ⛔ **本轮开工自检没有跑完,这一行是「没跑成」,不是「通过」**(铁律 10 (ii) 的措辞)。
三条推送闸是**跑完了的**,它们不替自检说话。

## 九 成本

**零 EC2 / 零 CE / S3 读取 0 个对象(出网未计价)。**
本轮全部工作在容器内:读源码、读 `tests/fixtures/` 与 `tests/frames/` 两个目录的帧文件、
跑三条闸与一个变异台。⛔ 没有下载任何 `.dem` / `analysis.json`,没有 `s3 cp` / `s3 sync`。

## 十 本次会话 token 用量(铁律 8)

```
TOKENS total_in=21,593,946 out=94,412 turns=124
```

⚠️ 这是**统计时刻为止**的数字,统计之后的收尾回合(commit / push / 发 GH 评论)不计入。
⚠️ 本轮 `turns` 偏高有一个可指名的原因,登记备查:变异台 **9 次全量 `wk_q` 运行、每次 ~123s**
(合计 ~18 分钟),这段时间**不能并发做任何改树的事**(`mutstand_*.sh` 自己的警告),
于是产生了一串纯等待的回合。⇒ 下一轮若再建同族变异台,值得先问**过滤器能不能收窄**
(本台选 `wk_q` 的理由写在脚本抬头:M7/M8 改的是 `X.ConsiderQ` 本身,**窄到单文件会把同族碰撞报成 SURVIVED**)。
