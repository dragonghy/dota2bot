# 英雄组 2026-08-31T05:02Z —— 认领 GH #354 §5:把 `cmqreach` 的到达性问题钉在真实帧上

**轴**:`[hero]` **GH #354**(录像组 04:07Z 开,点名 `queue.json:hero-25` 的 requester =
本组)的 **§5「最便宜的下一步:钉一枚 fixture,别等帧通道」**,以及它点名的那一帧
`69e067 / 20260831_004433_slot1, t = 1190.4`。

**改动面**:`bots/` **0 行**、`game/` **0 行**;**零新 gate id、零 arm/promote、
不申请波次、不开新 issue**;`state.json` / `queue.json` / `test_set.md` **本轮均无新增**。
AWS:**只读 S3**(1 个 `.dem` 24.2 MB + 1 个 `analysis.json`,零 EC2,egress ≈ $0.002)。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh`,**worst exit 3**,`legs run 8`,
`FINDINGS: trunk-red(python)`,`UNCERTIFIABLE: none`;stable-v1/v2 锚点 ok;
快 Lua 腿 **49** 个 tagged 文件 0 失败(FAST SUBSET,**开工那一刻**的取集)。

⚠️ **那条 TRUNK RED 在动手之前就在树上**:`tests/test_rc_wrapper.py` 与
`tests/test_selfcheck_lua_leg.py`。本 diff **零行 python 生产代码**(只新增了
`tests/test_fixture_creeps.py`),两条红与本轮无关;`test_selfcheck_lua_leg.py`
那条正是总监 04:27Z 开的 **GH #355**(「一次超时被洗成 trunk 红」)。**只登记,不替谁下结论。**

owner 四条优先项**仍无一条球在本组**(批测台 / 协同组 / 协同组 / 总监)。

---

## 1. ⭐⭐ 主读数:在这一帧上,出厂搜索能交回的每一个 `count >= 2` 的点都在射程外;armed 是 0 个

语料:`spot_20260831_001657_1_0d58ce09…_69e067 / 20260831_004433_slot1`,
`script_version = mirror:…,cmqreach:s2315:**radiant**` ⇒ **CM 是 dire 队(team 3),
在 baseline 腿上** ⇒ 这一帧上真正跑的就是**出厂的 1157 搜索**,armed 那一列是同一个
真实世界上的反事实。(顺带:这一波 `cmqreach` 的 armed 腿**根本没有 CM** —— #353 说的
构造性空格,本局是一个实例。)

`.dem` 用 `-creep-interval 0.5 -interval 0.5` 重跑(农场默认 3.0),所以这一帧的小兵采样
`dt = 0.0`,**#354 §4 第 2 条的分辨率异议在这一帧上不成立**(不是辩解,是 fixture 里
`creep_interval = 0.5` + 每行 `dt = 0.0` 断言出来的)。

CM 位置 `(140.9, 1106.2)`,17 级,Nova **rank 4、冷却 0、512 蓝**,无以太之镜 ⇒
可施法环 `nCastRange = 700 + 0 + 32 = 732`;出厂搜索环 = `X.cm_GetCreepAoESearchRange`
交回的 **1157**,armed 交回 **732**(两个数都是**从发货 helper 里读回来的**,不是测试里重打的常数)。

八个敌方兵(team 2),精确解(见 §2):

| 覆盖 ≥ k 个兵的**最近**合法圆心到 CM 的距离 | k=1 | k=2 | k=3 | k=4 | k=5 | k=6 |
|---|---|---|---|---|---|---|
| | **805.9** | **904.5** | **925.1** | **1152.4** | 1312.3 | 无解 |

- **出厂(1157)**:能交回 count 2 / 3 / 4 的点(5 连宽环都够不着),而这些点**最近的也在 904.5**,
  即**比她能施法的 732 远 172.5–425 码**。⇒ **在这一帧上不存在任何一种引擎 tie-break
  能让出厂搜索交回一个可施法的打兵点。**
- **armed(732)**:**覆盖哪怕一个兵**的最近圆心是 805.9 > 732 ⇒ **count = 0** ⇒
  八个 `count >= 2..5` 站点**全假** ⇒ armed 这一帧**不对兵线放新星**。

这正是修复自己写在 helper 文档块里的那句话(「armed 清兵放新星**严格不多于**出厂,
**从不挪动一次本来就合法的施法**」)在真实帧上的一次观测。

### 1.1 而且它不是被后置过滤器救回来的

`nCanHurtCreepsLocationAoE`(`nMaxHealth = 0`,**唯一不需要小兵血量、因而唯一可定价的那一路**)
在进入八个站点前要过
`J.GetInLocLaneCreepCount( bot, 1600, 425, targetloc ) <= 2 → count = 0`。
本帧:最近的 4-覆盖圆心(1152.4)覆盖的 **4 个兵全部同时在 CM 1600 内** ⇒ 过滤器**不清零** ⇒
源码里三个 `nCanHurtCreepsLocationAoE.count >= 4` 的站点**在这一帧上是可满足的**,
它们交出去的点比她的射程远 **420.4** 码。(`>= 5` 那个站点够不着:1312.3 > 1157。)

### 1.2 明说没有主张的

- **不主张 armed 在这一帧更好**。它说的是:armed 什么都不放,出厂只能瞄一个够不着的点。
  哪一个更值钱取决于「引擎拿到越界指令会怎样」,那个**从 bot VM 问不出来**(AGENTS.md)。
- **不主张开火**。执行有没有**走到**某个打兵站点,要小兵血量(kill 那一路按血量过滤,
  且每个站点还要一个有效的「最弱兵」)和 `IsFarming` / `IsPushing` / `IsDefending` /
  `IsGoingOnSomeone` —— **dump 一样都不带**。§6 把这条写成了对 fixture 的事实断言,不是对英雄的结论。
- **不主张这是条件 (a)**。#354 判 INDETERMINATE,本轮**不动那个判定**:这是一帧上的
  机制读数,不是「真实对局里 armed 腿被观测到执行且行为正确」。

---

## 2. ⭐ 为什么 §5 昨天还建不出来:缺的那个基准面在 fixture 写盘时就被丢掉了

`make_fixture.py` 只写英雄和建筑,**把 `timeline["creeps"]` 扔在地上**。于是
`tests/fixtures/` 里**每一枚 fixture 描述的都是一个没有小兵的世界**,而
`tests/mock/replay_fixture.lua` 对 `FindAoELocation` 的答复是保守替身
`{count = 0, targetloc = <self>}`。发货代码里每一个打兵 AoE 决策都挂在
`.count >= 2..5` 上 ⇒ 那些分支**不是「本语料没练到」,是在每一枚 fixture 里按构造不可达**。
**问题问的那个数据,在写 fixture 的那一刻被丢了。**

本轮把它补上:生成器现在写 `creeps = { { team, x, y, dt } }` + `creep_interval`
(**位置和队伍,dump 只有这些** —— 没有 id、没有名字、没有血量),取**离 t 最近**的一次采样
(不是「t 之前最后一次」),半径 3000 内(最宽的发货搜索 + 一个半径也才 1582)。
**旧 dump / 无小兵的 dump 一个字节都不变**(整块不发射),这条是被钉住的。

同族先例:这与 `test_nil_guard_then_body.lua`、`test_aoe_result_field_names.lua`
两轮记的是同一件事的第三种形态 —— **一个静默的缺失让一整条分支不可见**。前两次缺的是
一个键;这次缺的是**观测面本身**。

---

## 3. 落地物

| 文件 | 性质 |
|---|---|
| `tools/batch_test/replayscope/make_fixture.py` | 新增 creep 块发射(只加不改;无小兵的 dump 输出逐字节不变) |
| `tests/frames/f_20260831_004433_cm_creepreach.lua` + `tests/frames/README.md` | **新** 真实帧(10 英雄 / 38 建筑 / **19 个小兵** / 51 modifier),**staged 在语料之外**,理由见 §6 |
| `tests/test_cm_creep_reach_real_frame.lua` | **新**,18 例,`[ratchet]`;§1 帧事实 / §2 环从发货 helper 读 / §3+§3b 主读数 / §4 tie-break 无关性 / §5 解算器自标定 / §6 三条 LIMIT |
| `tests/test_fixture_creeps.py` | **新**,生成器合约的合成输入钉子(4 条性质) |
| `tests/test_cm_q_creep_aoe_reach.lua` | §7 那条 LIMIT 收窄并写明收窄到什么程度(语料里仍然只有那两枚 rank-1 的 CM 帧;变的是**仓库里**第一次有了一枚打兵分支活着的 CM 帧) |
| (无) | 语料侧**一个文件都没改** —— 见 §6:帧没有进语料,所以那些 ratchet 一个都没被碰过 |

解算器是**精确**的,不是网格:固定半径 r 下,若干圆盘之交到 C 的最近点只能是
C 自己、C 到某个圆的投影、或两圆的交点 —— 三族就是全部候选。离线用 2 码暴力网格交叉验证:
k = 2/3/4 得 904.86 / 925.18 / 1152.48,**各比精确值高一点点,正是网格必须偏的方向**。

---

## 4. ⚠️ 本轮最该记的:第一版解算器少了「投影」那一族,而**只有网格看得见**

第一版候选集只有「点本身 + 两圆交点」。它把 k=4 读成 **1157.0**(恰好压在搜索环边界上,
**看起来像个合理答案**),真值是 **1152.4**。抓到它的**不是任何一条断言,是那次暴力网格**。
——`test_aoe_result_field_names.lua`(M4/M8)和 `test_nil_guard_then_body.lua`(M6)记过两次的
同一条:**扫描器/解算器必须在合成输入 + 已知答案上单独钉**。所以 §5 有一节
「投影那一族是承重的,不是装饰」,把这次的具体缺陷做成了永久断言(变异 M2 见红)。

## 5. 变异台(证据纪律)

盘外 `cp` 建台,跑完逐个还原,`cmp` 三个文件**逐字节相同**。

**测试侧 9/9 见红,且都红在该红的节:**

| | 变异 | 见红节 |
|---|---|---|
| M1 | armed helper 改回宽环 | §2、§3 ARMED |
| M2 | 解算器去掉投影族 | §3、§3b×2、§5×3 |
| M3 | 解算器去掉两圆交点族 | §3、§5×2 |
| M4 | coverage 排除边界(`<= r` → `< r`) | §3、§3b×2、§5×4 |
| M5 | `nearest_centre` 忽略 k | §3、§3b×2、§5×3 |
| M6 | 敌方兵集合放进中立(team 4) | §1、§3 |
| M7 | 敌方兵集合放进**她自己的**兵 | §1、§3×2、§3b×2、§4 |
| M8 | 帧上凭空多一件以太之镜 | §1、§2、§3×2、§4 |
| M9 | loader 替身开始报 count 9 | §6(LIMIT 那一节,**正是它该红的地方**) |

**生成器侧 6/6 见红:** G1「t 之前最后一次」取代「最近一次」、G2 去掉半径过滤、
G3 写盘时丢中立、G4 不发 `creep_interval`、G5 无小兵时也发空块、G6 行里丢 `dt`。
G6 起初是**崩在 KeyError 上而不是打出具名失败**(退出码一样是 1,但读起来是两回事),
已改成 `.get()` 让它按具名断言失败 —— 顺手补的,不是本轮的发现。

## 6. ⭐⭐ 把这一帧放进 `tests/fixtures/` 要付多少钱 —— 量出来了,而且**没有付**

`tests/fixtures/` 同时是两样东西:一个测试能加载的**世界**,和**语料的样本** ——
二十来个测试 glob `tests/fixtures/*.lua`,对它们找到的一切取读数
(「没有一枚 fixture 的 Axe 到过 15 级」「全语料零个黑黄杖」「档案等级天花板 19」
「语料里一个小兵都没有」)。那些读数**是 ratchet,故意的**:在通道为空时做的决定,
通道不空了就该重做。

⇒ 「加一枚 fixture」在**不同世代**之间不是同一个价钱。同世代的一帧不要钱;
**这一帧是 19:50、英雄最高 22 级,而此前语料的天花板是 19 级 / t≈790**。

**实测**(把它放进 `tests/fixtures/` 跑全套,在 a–f 段被我主动掐断,
所以下面这张表是**下界不是普查**):**9 个红**。

| # | 文件 :: 节 | 性质 |
|---|---|---|
| 1 | `test_activemode_world_assertion` :: `[reverse]` | 记账(`IsInTeamFight` 93/1000 → 97/1000) |
| 2 | `test_alchemist_rage_objective_clock` :: `[corpus]` | **重新判定** —— 新帧 t=1190.4 落在该改动移动的 `[900,1800)` 带里,语料**第一次**能分辨 armed 与出厂 |
| 3 | `test_axe_cull_immune_veto` :: section 2 | **重新判定** —— 「全语料零个黑黄杖」变成 **2 个** BKB 槽;「这个杠杆不能就地定价」正是靠那个零 |
| 4 | `test_axe_t15_payoff` :: corpus health | 记账(Call uptime 上限 2.33 → 2.52) |
| 5 | `test_axe_t15_payoff` :: corpus count | 记账(live-Axe 帧 28 → 29),但读数要跟着重取 |
| 6 | `test_axe_t15_payoff` :: OUT OF DOMAIN | **重新判定** —— 语料 Axe 最高 14 级 → **21 级**;t15 读数**第一次能在域内取**,严格优于本文件现在钉的代理 |
| 7 | `test_bbfight_turbo_respawn_ceiling` :: `[W2]` | 记账(19 → 22;命题 `< 25` 仍成立) |
| 8 | `test_campfarm_ancient_target` :: `[world W1]` | 记账 + **前向后果**(见下) |
| 9 | `test_focus_innate_index_anchor` :: `[2]` | 记账(`zuus_lightning_hands` 1 → 2 帧,活分母,同 GH #274) |

**三个是真正的重新判定,分属三个不同的杠杆。** 在一个工作单元里把它们赶完,
正是 AGENTS.md 那条「locally-correct ≠ emergently-good」警告的东西 ——
所以**没有赶**:帧落在 **`tests/frames/`**(新目录 + README,写明它为什么在那儿、
以及付这份清单的代价),由本轮的测试**按名字加载**。

**这不是隐藏。** 代价被量出来、写进 README、写进本报告、并开成 issue;
帧本身在仓库里、可读、可 review。**把它搬进 `tests/fixtures/` 是一个工作单元:
先付清单,再搬。**

⚠️ **一个前向后果,现在就登记**:`make_fixture.py` 从今天起给**每一枚新 fixture**
写 `creeps` 块,而 `test_campfarm_ancient_target.lua` 的 `[world W1]` 断言
「语料里一个小兵都没有」⇒ **下一枚进语料的 fixture 就会让它见红**。
那是正确的、该看一眼的通知,不是回归。

## 7. 铁律 6

- 静态半:`bash tools/agent/luacheck_gate.sh` → **exit 0 / `luacheck bots game: 0 warnings`**
  (**裸读退出码**,冷启自己 `apt` 装的 `lua-check`)。**一次都没用过 `RULE6_BYPASS`**;
  `.githooks/pre-push` 在本轮每一次 push 上各跑一遍并放行。
- 动态半:**是子集不是全套**(GH #124,~100min)。收尾读数:
  `cm_creep_reach_real_frame` **18/0**、`cm_q_creep_aoe_reach` **16/0**、
  `test_fixture_creeps.py` **exit 0**,外加**所有 glob 语料的测试族**
  (alchemist / ancient_hp / bbfight / bbrespawn / bbshort / campexit / campfarm /
  campgrade / campsel / campvoid / corpus_existence / focus_t15 / focus_innate /
  lion_drainstop / mock_nearby_heroes / pollyhp / salve×4 / stayfield_callsite /
  turbo_ternary / axe_cull / axe_t15 / activemode_world / `cm_` 全家)
  **逐族 0 失败**(退出码裸读,聚合 rc=0)。全套在提交后台跑,读数见 §8。

## 8. 收尾读数

- 静态门 `luacheck_gate.sh`:**exit 0 / 0 warnings**(裸读;冷启自己 `apt` 装的 `lua-check`)。
- 动态:见 §7 的子集读数。**不主张主干全绿** —— 开工时 trunk 上就有两条 python 红(§0)。
- **AWS**:`session_setup.sh` 一次 + `s3 ls` 两次 + `s3 cp` 两次(24.2 MB),**零 EC2**。

## 9. 交出去的棒(**本组下一棒**)

1. **loader 还没读 fixture 里的小兵** —— 生成器带了,`replay_fixture.lua` 的
   `FindAoELocation` 仍是 `count = 0` 替身(§6 第一条 LIMIT 就是钉这个的,M9 见红)。
   接上它,`X.ConsiderQImpl` 才能在 fixture 世界里**端到端**跑到打兵站点 ——
   那才是 #354 §5 字面上要的「驱动真函数、把点读回来」。**要连带解决的两件事**:
   (i) kill 那一路的 `nMaxHealth > 0` 在没有小兵血量时该拒绝还是保守答 0(本轮取的是「测试自己算,不动 loader」);
   (ii) 小兵单位需要 `GetHealth`,而 dump 没有 —— 按 loader 既有的
   `GetLaneFrontLocation` 先例,应当是**拒绝并要求测试显式声明**,不是编一个数。
2. `#354` **保持 open**:条件 (a) 仍是 INDETERMINATE,本轮没有买到它。已在 issue 追评说明
   §5 为什么原样建不出来、改成了什么形状、以及读数。
3. `-43a` 的 **Zeus 方向**仍欠(连续多轮记着);`#348` 的修复半(要 gate + 域,非焦点五)仍无人接。

## 10. token 用量

(见提交时追加。)
