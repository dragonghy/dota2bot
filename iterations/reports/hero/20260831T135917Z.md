# 英雄组 2026-08-31T13:59Z —— 付 GH #357 第 6 行:Axe t15 第一次在域内取数

**轴**:认领 GH #357 的三条真判定之一(第 6 行,「Axe t15 finally measurable IN
domain」)。这是章程「当前状态」里点名、且**连续三轮没人付**的一棒;#357 原文写
「英雄组可以按 backlog 顺序接」。

**结论(第 6 行,付清)**:**VERDICT UNCHANGED** —— t15 仍取 `[3]` +8 战斗饥饿
dps。但**不是因为它看起来对**:域内那一帧的 rank 对上了,原因是**另一个缺陷**,
下面 §3 是本轮真正该被拿走的东西。

## 落地物

- **新** `tests/test_axe_t15_in_domain.lua`(8 例,全绿)
- `tests/test_axe_t15_payoff.lua`:**只改注释与断言文案**,两条断言的**条件逐字未动**
  (`CORPUS.max_level == 14` / `CORPUS.in_domain == 0` —— 语料本身仍然到不了 15,
  这句话**还是真的**)

**`bots/` 0 行、`game/` 0 行;零新 gate id、零 arm/promote、零 AWS(连 S3 GET 都没有)、
不申请波次;`state.json` / `queue.json` / `test_set.md` 均无新增。**

帧按名字加载(`tests/frames/f_20260831_004433_cm_creepreach.lua`),**没有进语料 glob**
⇒ #357 表里那 **9 个 ratchet 一个都没动**,只付了第 6 行。另两条真判定
(`test_axe_cull_immune_veto` 的黑黄杖零、Alchemist 时钟带)**仍然欠着**。

## §1 主读数:域内的 rank 对是 Call 3 / Hunger 4

`69e067/20260831_004433_slot1` t=1190.4(19:50),`npc_dota_hero_axe` **21 级、活着**。
此前全语料 Axe 天花板 **14 级**,所以 `test_axe_t15_payoff.lua` 那条承重 HONEST BOUND
(「NO FRAME IN THIS CORPUS IS IN DOMAIN」)**第一次可以退休**。

观测到的 rank 对 = **Call 3 / Hunger 4**,**正是那份结构天花板算术所用的同一对**
(Call 2.7s/14s = 0.19,Hunger 12s/5s → 封顶 1.00,~5 倍比值)。⇒ 比值在**真实帧上**
被佐证,而不只是被推导。

## §2 若把这枚帧收进语料,裁定也不动(顺带给 #357 第 4/5 行定价)

计算写在测试里而不是散文里:`axe_frames 28→29`、`call_ceiling 2.33→2.52`、
`hunger_ceiling 16→17`;`call_live` / `hunger_live` **不动**(§5:这帧是干的)。
方向判据 `call_live·hunger_ceiling > hunger_live·call_ceiling` 读作 **17.00 > 12.61**,
**仍然成立**。

⚠️ **12.61 不是 12.60**:我散文里先写了 12.60,是这个文件自己的精度断言把它顶红的
(5 × 2.522857 = 12.6143)。已改。

## §3 ⭐⭐ 本轮最该被拿走的:rank 对成立的原因不是「构筑照做了」

那个 Axe **21 级只点了 13 个技能点**,而引擎给 18。13 正是 **15 级的构筑状态**。

**而且这不是 Axe 一个人**:这帧上 **9 个 ≥18 级的英雄,9 个大招还卡在 2 级**
(6/12/18 梯子允许 3)。十个不同的英雄脚本、两个队伍。
**Crystal Maiden 是帧内对照组**:她 17 级、大招 2 级是**对的** —— 所以这个读数
分得开「短了」和「就该是 2」,不是对谁都答 2。

**构筑行也没有被违抗**:行把 Hunger 第 4 点放在第 **10** 个技能点、Call 第 4 点放在第
**14** 个。13 个点 ⇒ 过了 Hunger 的第 10、差 Call 的第 14 **一个点** ⇒ **观测到的 3/4
恰好就是 13 点预测的那一对**。行在被一个**停止点技能的英雄**照做。

⚠️ **LIMIT(承重)**:十个单位、**一局、一个瞬间**。一场比赛的十个 hero-slot 是
相关性最强的样本 —— 这是**指针不是频率**。要变成频率需要第二局域内语料,现在没有。
**所以本轮把它交出去,不在这里动手**(同一工作单元里既付 t15 又修加点,正是
`AGENTS.md` 那条 lanefix 教训的形状)。

## §4 替换掉「out of domain」的那条界:**t15 的选择本身仍然观测不到**

进入域内升级的是 **rank 与 uptime** 读数,**不是「他选了哪个天赋」**:

- 全档案 **1,080 个英雄单位,每个英雄最多带 1 条 `special_bonus_*`** —— 连 22 级
  (应有 3 条)的英雄也只有 1 条。⇒ **缺一条天赋从来不是「没点这条天赋」的证据。**
- **Axe 在它全部 29 帧上带 0 条**,域内那一帧也是 0。

⇒ 姐妹文件的裁定**仍然**建在可达性算术 + 构筑行上,**没有任何帧佐证过那个选择**。
把「没有帧在域内」退休而不换上这条,会让证据**看起来变强了**,而关于**选择**的证据
一点没动。这条已写进 `test_axe_t15_payoff.lua` 的 HONEST BOUNDS。

## §5 两次险些翻车(都是读数在量自己,值得记)

1. **⚠️ 手写 facet 名单当分类器。** 第一版「十个英雄都停了」的节点是**把所有技能
   rank 求和、再减去一张手写的 6 个 facet 名字的表**。拿它跑 108 枚**生成**
   fixture 的 855 个英雄单位:**359 个(42%)被判成「不可能」**(点数超过等级允许),
   因为档案里的 facet 远不止那 6 个(`earthshaker_slugger`、
   `phantom_assassin_immaterial`、`centaur_horsepower`、
   `slardar_slardar_seaborn_sentinel`…)。**开放集合上的名字表不是分类器。**
   要命的是**它给 Axe 的答案是对的**(Axe 没有 facet),所以**结论会通过 review,
   而它底下的量具五个单位里错两个**。已换成**每个英雄读一个大招 rank**(facet 动不了它)。

2. **⚠️⚠️ 差点把一条已裁定的结论反向重开。** 第一版还断言了一个「源码半」:
   「构筑行 15 条 ⇒ 驱动出的 skill list 停在 17 级 ⇒ 照做的 bot 到 25 级还剩 6 个
   技能点 2 个天赋没花」。**这正是 GH #238 §5**,它驱动同一个函数、裁定
   **「看着错,其实是对的」** —— 列表下标是**花掉的第 N 个技能点**不是英雄等级,
   位置 18/19 就是 20/25 级,什么都没被搁浅。我那句「停在 17」读的是 **mock 的**
   天赋列表(mock 把天赋名解析成 nil),当成了发货的那个。
   **一个绿着的测试,会把一条关闭的裁定朝错误方向重开。**
   节点**删掉**(不是调松),§1 那个问题改成**按技能点数**问 —— 也就是 #238 立的
   那套下标 —— 于是同一个混淆没法再从那儿回来。

## §6 变异台(evidence-discipline:先存副本、从副本恢复,绝不从 git 恢复)

**10 条变异,10 条见红,且只红在该红的节点**:

| # | 变异 | 见红节点 |
|---|---|---|
| M1 | Axe 21→14 级 | 5 个(域/点数/rank/十英雄/天赋面) |
| M2 | Call rank 3→4 | 3 个(rank 对 / 点数 / 收编算术) |
| M3 | 给 Axe 加一条 t15 天赋条目 | 天赋面(隔离) |
| M4 | 给敌人挂上战斗饥饿 debuff | 干帧(隔离) |
| M5 | `BEFORE.call_ceiling` 2.33→2.10 | 收编算术(隔离) |
| M6 | **把 staged 帧收进 `tests/fixtures/`** | 「帧是 staged 不是 admitted」(隔离) |
| M7 | 某个大招 2→3 | 十英雄(隔离) |
| M8 | CM 17→19(对照组消失) | 十英雄 |
| M9 | 给 Axe 加 innate 条目 | 点数节点(`points_spent` 拒绝运行) |
| M11 | 换构筑行让 Call 第 4 点移位 | rank 对(隔离) |

还原后 `cmp` 逐字节相同(帧、`hero_axe.lua` 各一次),`tests/fixtures/` 无残留副本,
还原后重跑 **exit 0**。

## §7 门(如实)

- **铁律 6 静态**:`bash tools/agent/luacheck_gate.sh` → **exit 0 / 0 warnings**,
  **裸读,没用 `RULE6_BYPASS`**(容器里没有 luacheck,脚本按 `lua-check` 自己装的)。
- **动态:是子集不是全套**(GH #124,全量 ~100min 跑不完):
  `axe` **127/0**、`t15` **44/0**、`fixture` **93/0**、`talent` **72/0**、
  `smoke` **3/0**、`gate_claim` **10/0**。新文件单跑 **8/0**。
- ⚠️ **开工自检 exit 124(超时,没跑完)** —— 按铁律 10 的措辞,**没跑成不是通过**。
  它跑出来的部分报了 `tests/test_rc_wrapper.py` TRUNK RED;**那条已由 GH #364 立案
  为 flaky(裸跑 7:1)**,且本 diff **零行 python**,所以只登记不替谁下结论。
  另:自检第一次是被 `a557852` 那条新守卫拒的(我把它接到了管道上,正是纪律 3 的
  第六发)——改成写文件后才真跑。

## §8 下一棒(显式交出,别掉棒)

1. **GH #357 追评(已发表)**:第 6 行**已付、裁定不变**;第 2 行(Alchemist 时钟带)与第 3 行
   (黑黄杖零)**仍然欠着**,各值一个工作单元。#357 请裁的两件事(staging 目录要不要
   成惯例)**仍在总监手上**。
2. **新 issue = GH #366(§3 的加点停摆)**:9/9 大招短一级 + Axe 13/18 点。**入口不是新题** ——
   GH #238 已把「turbo 到不了 20 级」定性成 10 分钟 cap 的伪影,并给十个 t20/t25
   定了价;本轮是**它的反面第一次有帧证据**:等级现在够得到了,而**构筑没跟上**。
   需要第二局域内语料才能从指针变频率 ⇒ 已往 `queue.json` 提交 **`hero-26`**
   (**语料请求不是波次**:只要一局 25 分钟局的晚期 dump,零 EC2 / 零 AWS 增量;
   acceptance 把根因二选一写死 —— (a) bot 侧真没花点 vs (b) dump 侧 rank 陈旧,
   **不许把 (b) 当成 (a) 就动 `bots/`**)。
3. 未动:`-43a` 的 Zeus 方向;#354 的 `GetNearbyLaneCreeps` 观测面(上一轮已交回)。

Owner 四条优先项**仍无一条球在本组**(P1/P2 在协同组,Spot 在批测台,#108 在总监)。

## Token 用量

```
TOKENS total_in=7,587,536 out=68,406 turns=57
```
(统计时刻之后的收尾回合不计入。)
