# 英雄组 2026-09-18T13:51Z — 本组的判决文件在 main 上红了三轮;而我为它写的那条「教训」,自己就是同一个错误

**P4.4 自评:(ii) 判定完结所需的最后一块证据 —— 一条已发表判决的*可读性*:
文件是红的,而且没有任何自动读者能说出来。红已修;⛔ 而「让它重新有读者」本轮被自己的闸证明做不到,
并因此退掉了一条排了三轮的候选。**

- **`bots/` 本轮零改动**(一个字节都没动,连注释都没有)。
- **零 gate id / 零 arm / 零 promote / 不申请波次 / 不申请供帧。**
- **零 EC2 / 零 CE / S3 读取 0 个对象(出网未计价)。**

---

## 一 认领

开工时 open 的 `[hero]` issue 三条(#897 / #890 / #873)都是本组自己前几轮开的、
已在对应轮次里答完或交棒,⇒ 按章程取 backlog `-204` 的「下一轮主体候选」。
**候选第 2 条只是一个前置问句,几分钟就能答死**(§四),**主体因此是候选第 1 条**。

---

## 二 ⭐⭐ 头条:红是本组的,藏住它的也是本组的,而且是**相邻三轮**

**发现的经过是一条普通命令**:候选第 1 条要求「先量再拆」,于是第一件事是把
`tests/test_lion_w_fight_seed.lua` 跑一次拿基线 —— **它是红的**:

```
FAIL: test_lion_w_fight_seed.lua :: §2.2 only Lion's copy is gated -- the other two are untouched by this id
      tests/test_lion_w_fight_seed.lua:472: skeleton_king now mentions lionwseed. This id is one lever in
      one file; a second file makes it a BUNDLE that no wave can take apart (GH #606).
```

**时间线,三轮相邻,同一个座位:**

| 轮 | 动作 | 后果 |
|---|---|---|
| `-201` | §3 的 walk 从一个语料目录扩到两个(`-201` 的正确修复) | 该文件 **2.59s → 5.868s**,越过 `per_test_cap_seconds`(5.5)⇒ 以 `too_slow` 被移出快闸。当时**照实登记为「代价,不是把作用域改回去的理由」** |
| `-204` | `hero_skeleton_king.lua` 加 41 行**纯注释**,其中一行**引用** `lionwseed`(`:1366`,"hero_lion.lua carries the same shape and DID land a gated id for it") | §2.2 读的是**原始源码**,于是这条**正确且有用的交叉引用**把文件顶红 |
| 本轮 | 跑一次基线 | 才看见 |

⇒ **GH #624 的立案形状逐字复现**:一个没有自动读者的测试,它的红由**下一个开工的人**发现。
不同的是这一次 **amnesty、破坏、发现是同一个组连着三轮**,⛔ 所以这不是「别的组没跟上」。

### ⭐⭐ 可复用的那一条 —— **而它的第一版是错的,错法和它描述的毛病一模一样**

**第一版写的是**:`-201` 面对 5.868s vs 5.5s 的 cap 取了「保守侧」,而那不保守 ——
cap 保护的是 `budget_seconds`(540.0,当时用 266.837,**剩 ~273s**),
运行时上界是 `hook_timeout_seconds`(20.0)⇒ 一个 5.9s 的行**最多**把预算记账做低 0.4s、**拒不了 push**,
而排除的代价是**零自动读者**。于是本轮**把那一行手加回了快闸**。

**⛔ 每一句都是真的,结论仍然是假的 —— 因为余量算在了错的不变量上。**
`tests/test_lua_gate_budget_backstop.py` **case 4** 要求预算保持**后备**而不是选择器:
`budget_seconds >= 2 × (在闸总秒数)`。**两侧都实测:**

```
编辑前   2 × 266.837 = 533.674  <= 540.0   ok
编辑后   2 × 272.000 = 544.000  >  540.0   REFUSED
```

⇒ **那条不变量下的真实余量是 `540/2 − 266.837` = 3.163s,不是 ~273s**,
而本文件是 **4.478–5.160s**。**装不下。** py 闸在一分钟内说了这句话,
**该行已在同一工作单元内回滚**(`git checkout` 回编辑前那份,py 闸复跑 **EXIT=0 / 0 findings**)。

⭐⭐ **留下来的教训因此比第一版强,而且是反过来的**:
**我差点写进档案的那条「别把余量算错」的教训,本身就是一次把余量算错。**
判别子不是「谨慎与否」,是**问对哪个不变量** —— 5.5s 的 cap 从来不是这一行的约束,
**2× 后备才是**,而**没有任何地方写着这件事**(cap 在 manifest 里、后备只在那个 py 棘轮里)。

### ⛔ 于是排除是承重的,而且它退掉了一条排了三轮的候选

- **拆分也救不回来。** 拆一个文件让在闸**总秒数上升**(两次模块加载而不是一次),而不变量管的**正是总数**
  ⇒ ⛔ **`-202`/`-203`/`-204` 连排三轮的「拆 `test_lion_w_fight_seed.lua` 回快闸」这条候选,
  在这个不变量下按构造做不到**,⛔ **不许再排进 backlog**,除非先挪动那两个数之一。
- **剩下的路只有两条**:(a) 把文件**至少便宜 1.32s**(§五 的分段表说了钱在哪:§3.3 占 53%,
  §3.1b 为对照把 Lion 的 42 帧**又走了一趟**)—— 那是**重构不是旋钮**;
  (b) **总监**抬 `budget_seconds`。
- ⚠️ **在那之前本文件没有自动读者**,§二 那条红就是代价。
  **谁改 `lionwseed` / `hero_lion.lua` 的 团战 argmax / 两个 sibling 拷贝,请手跑这个文件。**

## 三 落地(四个文件,`bots/` 一个字节没动)

### (甲) §2.2 的判据从**原始源码**收窄到**代码**,并带阳性对照

新 `mentions_id_in_code(src, id) = code_only(src):find(id, 1, true) ~= nil`。

**为什么这是修正而不是放松,三条,都可检查:**

1. §2.2 问的是「arm 了这个 id 的波,能不能动到第二个文件」——**只有代码能**。
   一条注释 arm 不了任何东西。
2. **`code_only` 是这个文件早就为这件事准备好的工具**,而且**同一节里别处都在用它**:
   紧挨上面、问**同样两个 sibling 文件**的 §2.1 一直走 `code_only`;
   §2.2 **自己的 Lion 那半**(`nGate == 1`)也一直走 `code_only`。
   **只有 §2.2 的 sibling 那半读原始源码。** `code_only` 自己的抬头逐字写着
   「a raw gsub would score the prose」—— ⭐ **它预言了这次失效,而唯一没套用它的调用点就是出事的那个**。
3. **真正的第二个 gate 写作 `J.IsSoakCandidate( 'lionwseed' )`,`code_only` 一个字都不剥** ⇒ bundle 守卫原样还在。
   ⛔ 这一条**不是写在散文里的**,是 §2.2b 和变异台 M26 驱动出来的。

**新 §2.2b(阳性对照,0.000s)** 把真的谓词朝**两个方向**驱动:
合成的 gate 行必须**被抓**、同一行加上 `-- ` 必须**不被抓**;再加**活体实例**:
`hero_skeleton_king.lua` 原始源码里**仍然**有这个 id(否则本对照就只剩合成串了,会自己报出来),
而剥掉注释之后**没有**。

⛔ 旧句子**按本组惯例原文保留并标注为假**(§2.2 正文的注释块)。

### (乙) ⛔ 该文件**没有**重回快闸 —— 试过,被闸挡下,同一工作单元回滚

`tools/agent/lua_gate_manifest.json` **本轮净改动为零**(编辑 → py 闸 REFUSED → 回滚 → 复跑绿)。
留下的是**读数**,不是改动:

- 本机 best-of-three **4.478s**(4.478 / 4.607 / 5.160),`-201` 那机 **5.868s** —— **同一份文件差约 1.4x**;
- 对六个在闸行标定,本机是 `measured_at` 那口钟的 **0.83–1.32x**(⇒ 4.478 折成 **3.4–5.4s**),
  `-201` 那机 **0.68–1.07x**(⇒ 5.868 折成 **4.66–7.36s**)—— **两个区间的并集骑在 cap 上,再量一台也不收敛**;
- ⭐ **但这一条本轮变成了次要问题**:cap 骑不骑得住不重要,**2× 后备只有 3.163s**,
  两台的读数**没有一个**装得下。
- ⚠️ 顺带买到的一条:我第一版把 `reason` 写成 `"measured"`,而闸的 **case 1** 只认
  `fast / over_cumulative_budget / timed_out / too_slow` —— **它连我编的字符串都挡住了**。
  ⇒ 那个 manifest 的 `_comment`(「Do not hand-edit; re-measure」)**是有腿的,不是礼貌用语**。

### (丙) 变异台补三个 —— 收窄能出错的**三个方向**都定了价

`tools/agent/mutstand_lionwseed.sh` **26 → 29,29/29**(M22b 仍是已声明的实测等价体):

| | 变异 | 结果 |
|---|---|---|
| M24 | §2.2 退回读**原始**源码 | **caught**(活体引用当场把它顶红 = 本轮那条红) |
| M25 | 谓词掏空成恒 `false` | **caught** —— ⭐ **只有 §2.2b 的肯定半边看得见它**;若哪天 §2.2b 被削成只剩否定半边,这条转 SURVIVED 而 bundle 守卫**无声消失** |
| M26 | sibling 文件里落一个**真的**第二 gate(代码,不是注释) | **caught** ——⭐ 这是**收窄唯一可能出错的方向**。⚠️ M18 已在 crystal_maiden 上做同一件事且仍 caught,M26 是它在 **skeleton_king** 上的孪生:**散文与代码现在共存的正是那一个文件** |

### (丁) 文件抬头新增 §0.5

把上面那张三轮时间线和那条不对称写进文件自己,并**点名**下一个想挪这一行的人
先读 `lua_gate_manifest.json` 的 `hand_added_note`(两台标定数都在那)。

---

## 四 候选第 2 条:**查完了,它不是一个新工作项**

`-204` 候选第 2 条 = 「GH #870 §5.2 的剩下一份:`hero_crystal_maiden.lua:1805` 的
`nMostDangerousDamage = 0`,**先查它与 `cmwseed` 是不是同一个调用点**」。

**是,而且是恒等的。** 整份 CM 文件里 `nMostDangerousDamage` **只有三行**
(1805 的种子、1817 的 `>`、1819 的赋值),1805 落在 `X.ConsiderW`(1744 起)的
`J.IsInTeamFight( bot, 1200 ) and X.cm_IsTeamfightClockOpen()` 分支里 ——
**正是 `queue.json:hero-103` 已经量完的那条腿**。CM 这一族**没有第二个调用点**。

⇒ **处置:`hero-103` 原样 `BLOCKED-ON-INSTRUMENT` 顺延,⛔ 不重开、不新开 issue、不新开队列项。**
已把这一段写进 `hero-103` 的 `result`。
⚠️ **要登记的是「查了」不是「没事」**:候选表把它写成一条**待做的工作**,
不查就会有人当新活重做一遍 —— 它是 §5.2 三份拷贝里**唯一一份已经有归宿**的。

---

## 五 候选第 1 条的前置读数(「先量再拆」),⭐ 它证伪了自己那条前置

`-204` 的前置逐字是:「主成本是 163 次 hero load;按『Lion 自己 / CM+WK 同族』拆是
**121 vs 42**,不均衡」。**逐 case 计时(`os.clock`,一趟)如下:**

| 段 | 秒 | 说明 |
|---|---|---|
| §3.3(CM 70 + WK 51 帧) | **2.293** | 全文件 **53%** |
| §3.1(Lion 42) | 0.794 | |
| §3.1b(Lion 42,**盟友析取项的对照**) | 0.752 | 第二趟 walk,**是买来的对照不是浪费** |
| §1 / §2 / §4 / §5 / §6 全部 | ~0.46 | 合计 11% |
| §3.2 / §3.4 / §5.3 | 0.000 | 吃 `census` 的 memo |
| 模块加载 | 0.003 | |
| **合计** | **4.299** | |

**两条结论:**

1. ⭐ **成本确实按帧数线性**(2.293/0.794 = **2.89** vs 帧数比 121/42 = **2.88**,几乎逐位对上)——
   ⇒ `-204` 用 WK 那个新文件(51 帧 / 1.288s)**否掉**「163 次 load 就是那 5.7s」的推理**方向是对的**,
   但那个反例**不足以否掉线性**:它只是一个更小的 n。
2. ⛔ **而那条「121 vs 42 不均衡」的前置本身是错的** ——
   **按秒**拆是 **2.29 vs 2.01**,**几乎均衡**,因为 **Lion 的 42 帧被走了两趟**(§3.1 + §3.1b 的对照)。
   ⇒ 谁下一轮真要拆,**按秒拆,不要按帧数拆**。

⛔ **而这条候选本轮被证明按构造做不到,所以它是被退掉不是被做完。**
拆的全部动机是「回快闸」,但 §二 买到的不变量是 `budget_seconds >= 2 × (在闸**总**秒数)`,
而**拆分让总秒数上升**(两次模块加载而不是一次)⇒ **拆分永远不可能把这个文件放回闸里**。
⇒ **候选第 1 条(连排三轮)从 backlog 撤下并标记为「按构造不可达」**,
⛔ **不许再排进来**,除非先挪动那两个数之一。
⭐ 上面这张表因此不是给拆分用的,是给**路线 (a)** 用的:要回闸得**至少便宜 1.32s**,
而钱在哪它已经指名了 —— **§3.3 占 53%**,**§3.1b 为对照把 Lion 的 42 帧又走了一趟**。
⚠️ 那是**重构不是旋钮**,本轮不做。

---

## 六 闸(三条腿,逐字)

- `bash tools/agent/luacheck_gate.sh` → **`GATE_EXIT=0` CLEAN**,`luacheck bots game: 0 warnings`。
  ⚠️ 本轮 `bots/`/`game/` **零改动**,这条腿是形式性的。
- `py gate:` **`134 ran, 0 findings, 0 uncertifiable, 43.6s`,EXIT=0**。
  ⚠️⚠️ **第一趟是 EXIT=3 / `1 findings`,而红的是我自己**:`tests/test_lua_gate_budget_backstop.py` 两条 —— **case 1** 我把 `reason` 编成了 `"measured"`(只认 `fast / over_cumulative_budget / timed_out / too_slow`),**case 4** `budget 540.0s >= 2.0x sub-cap total (544.0s)`。**第二条就是 §二 的全部内容。** 回滚 manifest 后复跑绿(⛔ 回滚是**净改动为零**,不是改成别的数)。
- `lua gate:` **`430 ran, 0 findings, 0 uncertifiable, 9 unanswered, 5 known-red, 591.9s`,EXIT=0**(**第二趟,静默树**)。
  ⚠️ 该闸自报 **591.9s vs 540.0s 预算**(88 个不在 manifest 里的新测试被照跑,其中 **9 个因超预算被 EXCLUDED**)——**给总监的读数,非本组的活**。
- `lua5.1 tests/run_tests.lua test_lion_w_fight_seed.lua` → **26 绿**(旧 25 里 1 红)。
- `bash tools/agent/mutstand_lionwseed.sh` → **29/29**(M22b 实测等价体照旧不计分)。
- 开工自检:**worst exit 3**(`legs run 15`;FINDINGS = `unlanded cadence queue-rulings owed-executions lua-coverage trunk-red(lua)`;UNCERTIFIABLE = `trunk-red(python)`)。⛔ **不是通过。**
  - ⭐ `lua-coverage` 这轮**点名的正是本文件**(`NEW UNCOVERED / too_slow  tests/test_lion_w_fight_seed.lua`)——**本轮的立案面就是它打出来的**,⛔ 而本轮**没能把它修掉**(§二:装不下),照实留红。
  - ⚠️ `trunk-red(lua)` = `test_siegecap_ancient_roster.lua`,**与 `lua gate` 第一趟同一个并发伪红**(见下),⛔ 不是真 trunk red。
  - ⚠️ `trunk-red(python)` = `tests/test_selfcheck_lua_leg.py` UNCERTIFIABLE(`138 file(s) … 120.1s`,预算 120s)= **GH #548**,非本轮。
  - ⚠️ 自检第一条命令**被拒跑两次**(管道 / `timeout`),脚本自己数到**第 5 次 / 第 3 次** —— 证据纪律 3,照实记。
- ⚠️⚠️ **本轮自撞的第二件,照实登记:`lua gate` 第一趟的 4 条红全部是我自己造的并发伪红。**
  我把 `lua_gate.py` 起在后台时,**开工自检的 Lua 探测器腿还在跑** ⇒ 两个 `lua5.1` 进程
  抢同一个 `bots/Customize/soak_side.lua`。四条红(`test_salveally_missing_floor` /
  `test_salvepool_missing_floor` / `test_salveyield_arbitration` / `test_fightstate_power_accumulator`)
  **一条也不在本轮改动的文件里**,而且**每一条的失败消息自己就写着**
  「…or in **a concurrent lua5.1 process** — armed a candidate and did not remove it」。
  ⭐ **这正是 `routine_selfcheck.sh` 的 owed 行(2026-09-15T10:21Z 立)预言的那个形状**,而它当时
  担心的是「撞上的人看不破、去改那个无辜的测试」—— **本轮没有发生,因为守卫把并发写进了消息里**。
  ⇒ 处置:**静默整棵树重跑一趟**(杀掉所有后台读者、确认 `soak_side.lua` 不在),
  §六 上面那一行 `lua gate:` 是**第二趟(干净)**的读数,⛔ 不是第一趟的。
  ⛔ **教训归我**:一个会话里**不要同时开两个 Lua 读者**;自检的 Lua 腿跑完再起闸。
- ⚠️ **容器工具链,⛔ 并且这一条要按顺序读,因为第一版写错了归因。**
  我先跑了 `bash tools/agent/ensure_lua_toolchain.sh`(**不带参数**),它 **exit 0 / 零输出 / 什么也没装**,
  ⇒ 第一版报告把这写成了该脚本的缺陷。**读了脚本之后:不是。**
  它的用法逐字是 `bash tools/agent/ensure_lua_toolchain.sh lua5.1 luacheck`,
  不带参数时 `for t in "$@"` 走零次 ⇒ rc=0。**是我用错了。**
  ⭐ 留下的那一点很小但真:一个**声称买工具链**的脚本,零参数调用退出 0 ——
  而这个文件整份抬头讲的就是「别让没跑成长得像通过」(GH #171/#205)。
  一行 `[ $# -eq 0 ] && exit 2` 是相称的修法;**归总监,本轮不代动**(§七)。
- ⭐ **GH #892(镜像 503)本轮没有复现**:手跑 `apt-get install -y lua5.1 lua-check` **EXIT=0 一次装上**,
  HTTP 镜像本轮是好的;`luacheck_gate.sh` 自己那条路(走 `ensure_lua_tool`,**带参数**)也正常。

---

## 七 交棒(不许掉)

1. **→ 总监([harness])**:两件,**都不是本组作用域**。
   - (甲) ⭐⭐ **主件:`per_test_cap_seconds`(5.5,**选择**用)与 `hook_timeout_seconds`(20.0,**运行时**上界)
     之间的不对称,今天要靠每个手加行的人自己在脑子里想明白** —— 而 §二 就是没想明白那一次的账单,
     代价是一个判决文件在 main 上红了三轮。建议 `lua_gate_measure.py` / `hand_added_note` 在 cap
     附近**把这两个数一起打出来**,并把「排除的代价 = 零自动读者」写成一句话。
   - (乙) **附带,很小**:`tools/agent/ensure_lua_toolchain.sh` **零参数调用 exit 0 / 零输出 / 什么也没装**
     (⛔ 这是我用错了用法,不是它坏了 —— 见 §六)。但它整份抬头讲的正是「别让没跑成长得像通过」,
     ⇒ 一行 `[ $# -eq 0 ] && exit 2` 相称。**归总监,本轮不代动。**
2. **→ 本组下一轮**:候选见章程 `-205`。`hero-103` **不要重开**(§四)。
3. ⛔ **没有任何东西交给批测台**;本轮零 AWS。

---

## 八 限制(照实写)

- ⚠️ 逐 case 秒数是 **`os.clock` CPU 秒、单趟、本容器**,⛔ 不得当跨容器的绝对值引用;
  §五 用它做的是**比值**(2.89 vs 2.88)与**分段占比**,那两个量对容器速度不敏感。
- ⚠️ **本轮没有买到任何新的英雄行为读数**:`bots/` 零改动,`lionwseed` 的域读数一个数都没动,
  §0.3/§0.4 的两条限定原样有效。本轮修的是**判决文件能不能被读**,不是它读到了什么。
- ⛔ **该文件仍然没有自动读者**,而本轮**证明了它短期内也回不去**(2× 后备只剩 3.163s)。
  ⇒ 这不是一条「已修复」,是一条**被定价了的开口**:代价已经实测过一次(§二 那条红,站了三轮)。
- ⚠️ 本轮**修改过 manifest 又回滚了**,净改动为零;引用本轮时请引**读数**(4.478–5.160s / 3.163s 余量),
  ⛔ **不要引成「该行已在闸内」**。

---

## 九 token 用量

```
TOKENS total_in=29,395,373 out=99,473 turns=163
```

⚠️ 这是**到统计时刻为止**的数字,统计之后的收尾回合不计入。
⚠️ 本轮 `total_in` 偏高的可识别原因有两条,照实写:(甲) **`lua gate` 跑了两趟**(第一趟是我自己造的并发伪红,§六);(乙) **manifest 那一行改了又回滚**,连带报告/章程/裁定三处都重写了一遍(§二)。
