# 英雄组 2026-08-28T19:53Z —— 棒 ③ 的最后两行 WK:premise 从「够不到」换成「早花完了」

轴:charter backlog **-38** —— `tests/test_level_premise_registry.lua` 的 PENDING 上
最后两条**属于本组**的债:`test_wk_bone_guard_thresholds.lua`(untrained-stub-is-turbo-reality
claim)与 `test_wk_q_aim_preflight.lua`(level distribution note)。
**两条都清了,ceiling 4 → 2,剩下的两行全在 harness/director 名下(GH #236)。**

- `bots/`/`game/` **零行改动**(shipped 那 9 处 08-27 已改完,本轮不欠)。
- **无新 gate id**;`wkbuild` / `wkqaim` 的门、turbo-only 子句、armed 状态**一个字没动**
  (`wkqaim` 从来没被写出来,本轮也没写)。
- `state.json` 新增 `wkpremise_REGISTRY_CLEARED_20260828`(`gated:false`)。
- **零 AWS**;不申请入集;**不提 queue**;不开新 issue。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` —— **worst exit 3**:legs run **8**,
`FINDINGS (exit 3): cadence`,`UNCERTIFIABLE (exit 2): none`。
待裁 queue 请求 **0**(open 37);expired waits **none**;
stable-v1 / stable-v2 锚点各三项 **ok**;
trunk python **51 passed / 0 failed**,快速 Lua 检测器 **17 文件 0 失败**(FAST SUBSET)。

owner 四条优先项**没有一条球在本组**(常设运维球在批测台;P1/P2 球在协同组;P3 球在总监)
⇒ 按章程走 backlog,而 **-38 明写「下一棒做这条」**。

---

## 1. 两条债是**同一条**债的两种藏法,而藏得深的那条不在散文里

> **⚠️ 先记一条:-38 的预判对了一半,错的那一半正是重点。**
> 它写「那两个文件**大概率也用** `ls tests/fixtures/f_*.lua` 枚举语料 ⇒ 它们的零同样
> 可能是 glob 的地平线」。**q_aim 是**(§4.2),**thresholds 完全不读语料** ——
> 纯 mock 文件,**一个 glob 都没有**。
> 按上一棒的形状去找,**会正好在 thresholds 上找不到东西**;那边的前提藏在
> **造帧函数**里。**上一棒的机制是线索,不是模板。**

registry 把它们并排记了一年份的措辞,但两处的**承重方式完全不同**:

| 文件 | premise 藏在哪 | 塌了会怎样 |
|---|---|---|
| `test_wk_q_aim_preflight.lua` | 一条**注释里的 cross-check** | 断言本身不动,只掉一句修饰 |
| `test_wk_bone_guard_thresholds.lua` | **造帧函数里**(`make_frame`) | 整个 sections 2-4 的扫法失去理由 |

thresholds 那条才是真的:`make_frame` 给 `X.ConsiderW` 递一个 **untrained 的 talent6 桩**,
理由原文就是「an untrained stub is the turbo reality (GH #84: level >= 20 on 0 of 210
hero-slots)」。而 `X.ConsiderW` 的两条分支写的是

```lua
( nStack / maxStack >= 0.6 or talent6:IsTrained() )   -- 分支 1
( nStack == maxStack      or talent6:IsTrained() )    -- 分支 2
```

—— **桩是 untrained,才让两个 disjunct 惰性,才扫得出 threshold**。
天花板一退休,那个桩就没理由了,**sections 2/3/4 全部三节一起悬空**。
「注释里的一句话」和「实验装置的前提」看着一样,**代价差一整个文件**。

---

## 2. thresholds:替代理由**根本不看等级带**,是量本身早就花完了

条件 (c) 的命题是「**帽子什么时候抬起来**」。而两条 shipped build row
把四个 Bone Guard 点**在 12 级前全部花完**:

- `tAllAbilityBuildList` = **1/3/5/7**
- `tKillBuildList`(`wkbuild`)= **1/9/10/12**

t20 那一行**最早 20 级才可能存在**。⇒ **本文件扫的每一次帽子跃迁,都发生在 bypass
还不可训练的世界里**,桩就该是 untrained —— 这个理由**与可达范围无关**,天花板回来也好、
再退休也好,都不动它。

**新 section 7 把这条从散文变成机器检查**:直接读 shipped build row,
断言两行的**最后一个** Bone Guard 点 `< TALENT_FIRST_LEVEL (20)`,并断言各花 4 点。
把 20 改成 10,它红。

---

## 3. ⭐ 主产出:**修正的代价是条件 (c) 有了寿命,而这是天花板一直遮着的**

天花板在的时候,「trained 的 talent6」是个**不存在的世界**,所以没人问过它值多少。
本轮把桩**变成参数**(`make_frame(where, boneLevel, stacks, talentTrained)`),
拿 shipped `X.ConsiderW` 跑同一张网格,读回来的是:

**talent6 trained ⇒ section 4 读出的每一个 threshold 全部塌到 0,两条分支都是。**
最响的一格:**rank-4 lane branch,untrained 要 8 格,trained 要 0 格。**

那个 **8** 就是「早点 Bone Guard 会让自己的 Bone Guard 变稀」的全部本钱,
也就是 `wkbuild` **延迟加点所买的东西**。⇒

> **条件 (c) 有寿命:它在 hero level 12-19 这一段成立,20 级 t20 行一训练就作废。**

而**打到 20 之后是真事**:GH #235 那枚 post-cap 帧里十个 hero-slot 在 22-27,
**本英雄 26 级、Bone Guard 已经 rank 4**。
所以这不是一句理论上的边界,是**这仓唯一一枚后期帧就站在寿命外面**。

**不撤销 `wkbuild`,也不提请重审** —— 条件 (c) 在它被论证的那段等级里没被动过,
只是**第一次知道了它到哪儿为止**。新 section 6 把这条读出来,不是断言出来。

**诚实边界(写进文件的 HONEST BOUNDS)**:section 6 证明 bypass **解除** threshold,
**不证明 bypass 在任何一局里真的触发过** —— 也证明不了:dumper 在写盘前丢掉
hero-unique 天赋行(GH #260:960 hero-frames / 67 talent sightings / **unique 行 0 条**),
slot 6 正是 `special_bonus_unique_*`,所以 `talent6:IsTrained()` 的语料读数**训没训都是 0**。
另外 section 6 把 `modifier_skeleton_king_bone_guard` **按构造置真**,
所以它定的是**两条分支测试**的价,不是 `X.ConsiderW` 顶上那道 guard 的价 ——
那个 residual 留在 `test_wk_bone_guard_talent_bypass.lua`,本轮不搬。

---

## 4. q_aim:天花板的修正**是把结论收窄,不是放宽**;而 -37 的 glob 机制这次**查了**

### 4.1 census 那句是修饰,断言本身一直在量 build

原文:「the shipped build leaves Wraithfire Blast at rank 1 through level 11,
**and GH #84's census read a turbo high-water of 19, so rank 1 is what WK plays with
for most of a turbo game**」。
它底下的断言量的是 **rank 4 不早于 14 级**,是个 **build 事实**,和 census 无关 ⇒
**断言一字不动,只划掉那句 cross-check 的修饰**(测试名也跟着改成它真正量的东西)。

丢掉的是「**for most of a turbo game**」那半句:一局 turbo 有多少时间花在 14 级以下,
是本文件不问的语料问题。**收窄,不是放宽** —— 天花板退休后,
「Q 不再稀缺」的那段等级**是达得到的**,所以 supply 现在是**前中期**的判词,
对后期**沉默**,而不是像原来那样读起来覆盖全局。

**结论不动**:理由 (2)(`受到伤害时保护自己` 分支在 catch-all **上游**,吃掉所有战斗帧)
**从来不看等级**,`wkqaim` 该不写还是不写。

### 4.2 ⭐ glob 地平线:这次是**量出来的「没有」,不是扫不到的「没有」**

-37 的发现是两个 roshan sibling 用 `ls tests/fixtures/f_*.lua` 枚举语料,
**把「扫不到」渲染成量到的极值**。本文件 section 1 的 TRIPWIRE 是个**全称命题**
(「语料里没有一帧走到 catch-all」),枚举用的是 `ls tests/fixtures` ——
**同一个形状**:那枚唯一的 post-cap 帧停在 `iterations/pending/`(GH #236)。

**这次先查了再说,答案是全称命题活着**,而且活得很有意思:

那枚帧的 WK 是**全语料里唯一一个 supply 不是阻塞项的位** ——
**level 26 / Wraithfire Blast rank 4 / cd 0 / mp 762 / Reincarnation rank 3**
(所以 `R_MANA[3] = 0`,`X.ShouldSaveMana` 的预留也是 0,**四个 conjunct 一个不挡**)。
**挡住它的是几何**:568u 环里 **0 个活敌人**,最近的活敌人 **10,309u** —— 隔了大半张图。

⇒ **它不是一行 section 1 row,tripwire 不该红,也确实没红。**

**但那是量出来的,不再是没问过。** 本轮把这枚帧**接进 `fixture_files()`**
(存在性 + basename 双重保护:GH #236 落地那天,不管是 move 还是 copy,**都只读一次**
—— 重复计数会虚抬 `wk_frames` 这条 floor),
并新开 **section 4** 把读数**对着文件断言**,而不是抄成常量:
`ring == 0`、`nearest >= 10309`、rank 4 / cd 0 / mana 够 / R 预留为 0 全部逐条读回来。
文件搬走那天,section 4 自己 `return`,section 1 仍然覆盖它。

**section 4 的红是好消息**,消息就写在断言里:环里 ≥2 个人的那天,
**就是 queue.json hero-1 要的那枚真帧**,去重读 pre-flight,别把它按掉。

---

## 5. registry 记账

`PENDING` 4 行 → **2 行**,`CEILING` 4 → **2**
(08-28 立的 `CEILING == pending_count()` 绑定**强制**这两件事在同一个改动里发生 ——
还债只能删行)。两条 CLEARED 条目写的是**重读判了什么**,不是「句子删了」。

**剩下的两行全不是本组的**:`tests/corpus_scale.lua`(harness)、
`tests/test_level_gate_census.lua`(harness/director),GH #236 管排序。
**棒 ③ 在本组这一侧到此结清。**

---

## 6. 变异测试:8 个,7 抓 1 对照

| # | 变异 | 结果 |
|---|---|---|
| M1 | `bots/` 里删掉分支 2 的 `or talent6:IsTrained()` | **抓**(3 failures) |
| M2 | `TALENT_FIRST_LEVEL` 20 → 10 | **抓** |
| M3 | 桩改回恒 untrained(`IsTrained = false`) | **抓**(2 failures) |
| M4 | `fixture_files()` 不再接那枚 parked 帧 | **抓** |
| M5 | 记录的 `ring` 0 → 2 | **抓** |
| M6 | 记录的 `blast_rank` 4 → 1 | **抓** |
| M7 | 记录的 `reincarn_rank` 3 → 2 | **抓** |
| M8 | 对照:改一句注释里的日期 | **按设计逃逸** |

**⚠️ M7 第一版本来是对照,而它逃逸了 —— 那是个真洞不是对照。**
`reincarn_rank` 当时**没有任何断言读它**,可它是 section 1 复现的**第四个 conjunct**
(`X.ShouldSaveMana` 给 Reincarnation 留蓝)的全部依据。
「supply 在这帧没问题」少了这一条就不完整。**补上断言,M7 立刻变成抓到**,
再另找了一个真正没人读的量(注释日期)当对照。
**一个逃逸的对照,先问它是不是该逃逸。**

沿用 -36/-37 的两条工序教训:**每次变异先 `diff` 确认真的改到了文件**
(no-op 变异和漏网变异长得一模一样);**还原一律用备份文件,不用 `git checkout`**
(会把本轮自己对该文件的改动一起还原)。
`bots/` 在 M1 后逐字节还原 —— `git status` 里 `bots/` **干净**。

---

## 7. 诚实边界(汇总)

- **一枚帧**。26 级的读数足以杀死全称命题(全称命题死于一个反例),**不是分布**;
  本轮**没有一句**说 turbo 有多少时间花在 20 级以上。
- section 4 的**环读数是一局里的一帧**。它说的是「这枚帧不开 domain」,
  不是「后期不会有这种帧」。
- section 6 定的是**分支测试**的价,**不是** `X.ConsiderW` 顶上那道
  `HasModifier` guard 的价(residual 仍在 bypass 文件里登记着)。
- **bypass 在真实对局里会不会触发,本仓永远答不了**(GH #260 的 dumper 缺口),
  这是机械原因不是等级原因。
- 本轮**完全没碰任何 gate、任何 armed 状态、任何 `bots/` 行为**。

---

## 8. 验证(铁律 6)

- `bash tools/agent/luacheck_gate.sh` → `luacheck bots game: 0 warnings`,**exit 0**
  (容器本来没有 luacheck,gate 自己装的 —— apt 包名 `lua-check`)。
  **未用 `RULE6_BYPASS`。**
- **全量 Lua 套件单进程跑不完(GH #124),本轮未跑全量 —— 是没跑,不是跑绿了。**
  影响面按 filter 单跑,全绿:
  `wk_` **177/0**、`level_premise` **5/0**、`corpus_scale` **8/0**、
  `gate_claim` **10/0**、`level_gate_census` **15/0**、`smoke_load` **3/0**、
  `focus_build` **10/0**、`fixture_talent` **9/0**、`lategame_talent` **8/0**。
  (`wk_bone_guard` 21 → **24**,`wk_q_aim` 9 → **11**。)
- `bash tests/run_py_tests.sh` → **51 passed / 0 failed / 0 uncertifiable**。

---

## 9. 下一棒

**backlog -39**:section 3 记了一条**没归因的账** —— 条件 (c) 的寿命结束在 20 级,
而 `wkbuild` 的取证波次(以及 `wkrosh` 的 24/31 帧)**全部测自 ≤12 级**。
-37 已经为 `wkrosh` 点名过同一个形状(「撑着它的帧全在最不可能打肉山的等级带」)——
**两个 lever 现在都缺同一段语料:12 级以上的取证帧**。
下一棒把这两条并成**一个**语料请求(域仍是 queue `hero-10`),
而不是各提各的 —— 它们要的是同一批帧。

`hero-19`/`hero-20`/`hero-21` 仍 pending;WK `ConsiderQ` 的 `nDamage` 修复仍等归档扫描;
**GH #268 那一格未被认领,不由本组扩**;GH #281(glob 地平线,53 个测试文件)在
harness/总监手上,本轮**没有替它扩范围**,只在本组这一个文件上把地平线量掉了。

---

## 10. Token 用量

```
TOKENS total_in=7,815,988 out=46,459 turns=62
```
(统计时刻为止;之后的收尾回合不计入。)
