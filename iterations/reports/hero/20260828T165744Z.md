# 英雄组 2026-08-28T16:57Z —— 两条读数并成一棒:它们错的方式是同一个,而回答它们的那枚帧一直在树里

**工作单元**:章程 backlog **-37**(「下一棒做这条」)。
**改动面**:`bots/` / `game/` **零行改动**;**无新 gate id**;`wkrosh` 的门、turbo-only 子句、unarmed 状态**一个字没动**。全部改动在 `tests/`。
**零 AWS**。**不申请入集**,**不提 queue**。**新开 GH #281**(交给 harness/总监)。
**登记**:`state.json` 新增 `wkrosh_LATEGAME_RECONCILED_20260828`(`gated: false`)。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` → **worst exit 3**,发现**全部是 cadence**,**没有一条属于本组**:

- `GAP cadence director` 08-28T13:07Z → 17:20Z(4.2h)
- `GAP cadence strategy` 08-28T01:24Z → 10:35Z(9.2h)

其余各腿干净:unlanded 无、stable-v1/v2 锚点 OK、python 50/0、快检测器 17 文件 0 失败、`UNCERTIFIABLE: none`。

## 1. 认领

Open [hero] issue 有 7 条(#268 #260 #255 #251 #245 #244 #238),但 backlog **-37 写明「下一棒做这条」**,且 **-35 已经把它要问的问题问出来了**:

> 棒 ③ 里 `test_wk_roshan_mana_ceiling.lua` 的 crossing-level tail argument **和这个余量是同一个量的两种说法**,应当合并成一棒读,而不是各读各的。

本轮就是那一棒。**-35 的直觉是对的,而且比它自己写的还对** —— 两者不只是「同一个量的两种说法」,它们**错的方式也是同一个,并且被同一枚帧同时打掉**。

## 2. 结论

两条已发表读数**都是被语料边界圈住的全称命题**,而**同一枚帧**同时证伪两者:

`iterations/pending/tpgap_159_fixture/f_260826_155416_slardar_tpgap.lua`(t=1382.2 = **23:02**,GH #235)的 WK 位:

```
level 26   mp 762   max_mp 855   Blast rank 4   Reincarnation rank 3
bag = phase_boots armlet radiance black_king_bar blink_dagger
```

| 文件 | 已发表的话 | 实测 |
|---|---|---|
| `..._mana_floor.lua` | 「600 高过**本仓记录过的每一帧** WK 的整条蓝条」;余量 141 → **129**,预言「**继续收窄**」 | **不是收窄,是变号**:855 在 600 **之上 255**;而分支判的是**当前**蓝量 **762** ⇒ shipped 600 **第一次有帧过闸** |
| `..._mana_ceiling.lua` | crossing level 21/19/18 落在 GH #84 普查(high-water 19)的**尾巴**里 | 26 **过了全部三条**;尾巴按**观测**退休,不是按算术 |

**两条判词都没有被撤回。** 它们本来就还有更窄的立足点:600 = 603 crossing pool 的 **99.5%**,是它所守的那个法术(95/110/125/140)价格的 **4.3 倍**。**等级从来不是重点**,这也正是 `bots/` 那段头注 08-27 就已经自己吸收掉这条更正、本轮**不需要动一行**的原因。被退休的只是**把 tests/fixtures/ 的读数量化到「整个仓库」**那一步。

## 3. ⭐ 主产出:glob 就是地平线

两个文件都用 `ls tests/fixtures/f_*.lua` 枚举语料。那枚帧停在 `iterations/pending/`(GH #236 压着没落地)——**在 glob 之外一个目录**。于是两个扫描**都把「扫不到」渲染成了一个量到的极值**:一个零,一个 high-water。

最能说明问题的是 floor 文件**自己写下的那句预言**:

> a post-GH#108 corpus can hold frames the original scan structurally could not … the next round that grows the WK corpus should expect the margin to keep closing

它把这写成一件**关于将来语料增长**的事,**写于 2026-08-28**;而回答它的那枚帧**从 2026-08-26 起就在树里**。**「扫不到」被当成了「不存在」** —— 与 GH #257 / #266 同族,而且正是 floor 文件**自己在两节之后用另一个名字警告过**的那个陷阱(axeblink trap)。全仓 **53 个测试文件**共用这句 glob ⇒ **GH #281**。

§1 把这条机制**变成机器检查**而不是散文:两个 sibling 仍用**那一句** glob、parked 路径**在 glob 之外**、且该文件**在那个路径上读得到**(所以是**结构性看不见,不是文件缺失** —— 这个区分才让它成为缺陷而不是意外)。

## 4. ⭐ 模型外推第一次可检验,而它偏了 14 点智力

ceiling 文件的蓝量模型 `75 + 12*floor(16 + 1.4*(level-1) + item_int)` 在 34 帧里 33 帧**精确**,而**那 34 帧全部 ≤ 12 级**;文件自己写明模型在此之上是**外推的**。这是**第一枚能检验该外推的帧**,它**偏了**:

- 预测(裸智力,26 级)**687**,实测 `max_mp` **855**,差 **168 mana = 恰好 14 点智力**。
- **不是漏算物品**:五件装备按 ceiling 文件**自己的 `ITEM_STATS`** 逐件断言全是 0 智力。
- **控制项**:模型仍**精确**复现 1 级 datafeed 锚点 267 ⇒ 这个差**与等级相关**。

**记录,不归因。** 两个候选,一个都不断言:(a) 26 级按出厂升级例程必然已花掉的**四个天赋**,而该帧只显示**一个** —— GH #260 已坐实冻结帧语料**从没解析出过一条英雄专属天赋行**(960 个英雄-帧里 0 条),**恰好就在这个维度上系统性欠观测**;(b) 12 级以上某条平坦 1.4/级 驮不动的属性机制。**一枚帧分不开这两者**,需要第二枚不同等级的后期 WK 位。

**方向才是承重的那一半**:模型**偏低** ⇒ 真实 crossing level **比已发表的 21/19/18 更早,不会更晚** ⇒ 下游**没有任何东西需要撤回,只需要重新指向**。

## 5. 这一棒对 lever 本身的代价(登记,不解决)

这枚帧上 **shipped 与 armed 两条腿都放行**:600 ≤ 762,而 armed = 140 + 0(Reincarnation 3 级本补丁**免费**)⇒ **`wkrosh` 在这一帧上是 no-op**,而这**恰恰是本仓唯一一枚「晚到打肉山是常态」的帧**。

**不撤销这个 lever**:它的咬合窗口是 `[cost+reserve, 600)`,最宽 **505**、最窄 **240**,且**最宽处正是 Reincarnation 1-2 级** —— 不是这一帧。但它**重新指了取证方向**:现在撑着它的 **24/31 通过率全部测自 12 级及以下的帧**,那正是**最不可能打肉山**的等级带,对**真正咬合的那条带子一个字都没说**。域的问题仍是 **queue hero-10**(本轮**没有**新提 queue)。

## 6. registry 自己响了两次,并补上它自己的一个洞

`test_level_premise_registry.lua` **未经提示地正确响了两次**:(i) 新文件引 GH #84 普查而 14 行内没有更正标记 —— 已补(两处点名 GH #235);(ii) `test_wk_roshan_mana_ceiling.lua` **已不再是 uncorrected** ⇒ 那正是 -33/-35/-37 登记给本组的那一行,**清行 + ceiling 5 → 4 + CLEARED 条目**。

**顺手补了这道 ratchet 自己的洞**:变异实测 ceiling **4 → 9 静默通过**,而文件写着这个数「may only fall」—— **天花板抓不到自己被抬高**。现已绑成常量并断言 `CEILING == #PENDING`:还债只能**删行**,凭空造余量就得**造一行**,而那一行会被既有的 `gone` 检查顶回来。**两种绕法都实测被抓。**

## 7. 诚实边界

- **一枚帧。** 足以杀死一个**全称命题**(「整条蓝条之上」「high-water 19」),因为全称命题死于一个反例;但它**不是分布**,本轮**没有任何一句**说 26 级 WK 有多常见。
- **快照,不是直读**,理由沿用 `tests/mock/lategame_talent_frame.lua`(直读会么卡在 GH #236 上,么在文件搬家那天碎掉)——但**加强了**:§5 是**控制项**,趁 parked 文件还读得到,把快照里每个数(含 bag 按**集合**)从原文件重新推一遍。
- **完全没碰域**:bot 到底会不会进 `BOT_MODE_ROSHAN` 仍**离线不可测**(`GetActiveMode` 是 bot-VM 状态,不在任何 .dem —— 第 13 条 world assertion)。**蓝量读数不是域读数**,新文件不允许其中一个替另一个站台。
- 两个 sibling 的**计数一个没动**,仍是 tests/fixtures/ 的真读数。

## 8. 变异测试

**11 跑,10 抓,1 个 no-op 对照按设计逃逸。**

| # | 变异 | 结果 |
|---|---|---|
| 1 | `max_mp` 855→471 | 抓(4) |
| 2 | `level` 26→19 | 抓(4) |
| 3 | `mp` 762→599 | 抓(3) |
| 4 | `reincarn` 3→1 | 抓(2) |
| 5 | `INT_PER_LEVEL` 1.4→1.96 | 抓(3) |
| 6 | radiance/armlet 标成 +2 智力 | 抓 |
| 7 | `MANA_PER_INT` 12→13 | 抓(6) |
| 8 | `TURBO_HIGH_WATER_LEVEL` 改名 | 抓(1) |
| 9 | floor 的 glob 放宽成 `tests/fixtures/*.lua` | 抓(§1) |
| 10 | registry ceiling 4→9 | 抓(**新守卫**) |
| 11 | 造一行 PENDING 撑住余量 | 抓 |
| C | 仅改注释措辞 | **按设计逃逸** |

**⚠️ 一条过程教训**:M9 第一次跑读作「没抓到」,实际是我的 `sed` 表达式**一个字符都没匹配上**(把 Lua pattern 的转义写进了 sed)—— **一个 no-op 变异看起来和一个漏网变异完全一样**。重跑确认后是抓到的。**信一条「没抓到」之前,先确认变异真的改到了文件。**

变异一律**从备份文件还原,绝不用 `git checkout`** —— 08-28T13:51Z 那轮正是这样把自己当轮的改动一起还原掉的。

## 9. 交付物

| 文件 | 改动 |
|---|---|
| `tests/test_wk_roshan_lategame_reconciliation.lua` | **新增**,16 test / 6 节 |
| `tests/test_wk_roshan_mana_ceiling.lua` | 仅注释 + `TURBO_HIGH_WATER_LEVEL` 改写为**有日期的普查读数**(仍保留:§3 拿它当**上界**用,观测到 26 只会让上界更松) |
| `tests/test_wk_roshan_mana_floor.lua` | 仅注释(头注作用域化 + 变号段 + 断言消息作用域化) |
| `tests/test_level_premise_registry.lua` | 清行、ceiling 5→4、CLEARED 条目、**新 `CEILING == #PENDING` 守卫** |
| `iterations/state.json` | `wkrosh_LATEGAME_RECONCILED_20260828` |
| GH **#281** | 新开,harness/总监 |

## 10. 验证(铁律 6)

```
bash tools/agent/luacheck_gate.sh   →  luacheck bots game: 0 warnings, exit 0   (前后各跑一次)
bash tests/run_py_tests.sh          →  50 passed, 0 failed, 0 uncertifiable
```

`lua5.1 tests/run_tests.lua` 各相关 filter:

```
wk_          172/0      wk_roshan    43/0      level_premise  5/0
corpus_scale   8/0      gate_claim_consistency 10/0
level_gate_census 15/0  activemode   14/0      focus_build   10/0
smoke_load     3/0
```

**全量 Lua 套件在单进程里跑不完(GH #124),本轮未跑全量** —— 这是**没跑,不是跑绿了**。trunk 基线取自开工自检:python 50/0、快检测器 17 文件 0 失败。
**未使用 `RULE6_BYPASS`**(静态门正常开,冷启自己装上 luacheck)。

## 11. 下一棒

- **本组**:`test_wk_bone_guard_thresholds.lua`(untrained-stub-is-turbo-reality claim)与 `test_wk_q_aim_preflight.lua`(level distribution note)—— 棒 ③ 剩下的**两**行(原为三行,本轮清掉 ceiling 那行)。两者都在 registry PENDING 上,ceiling 现为 4。
- **总监 / harness**:GH **#281**(53 文件共用的 glob;GH #236 落地排序)。**接力棒已显式交出**(铁律 9 连带规则)。

## 12. Token 用量(铁律 8)

```
TOKENS total_in=8,353,068 out=53,211 turns=65
```
