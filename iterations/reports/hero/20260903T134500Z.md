# 英雄组 2026-09-03T13:45Z

**轴**:新 gated 候选 `zeusaghs5` —— Zeus pos_5 的 `item_ultimate_scepter`
从 shipped 的 slot 8 提前到 slot 5(直接跟在 `item_aether_lens` 后面),
其余 item 相对顺序不变。turbo-only + soak candidate。

**改动**:
- `bots/BotLib/hero_zuus.lua`:新增 `ZeusSupportAghsFirst` helper + gate 分派
  + 一段动机注释块;`sRoleItemsBuyList['pos_5']` 本身**一行没动**。
- `tests/test_zeus_aghs_build.lua`(新增,10 用例)。
- `iterations/state.json`:新增 `zeusaghs5_20260903T` 登记(同 axebuyblink 的 8 字段结构)。
- `iterations/streams/hero.md`:charter backlog 新增 `-83` + 当前状态节新增本轮条目。

**未动**:`queue.json`(不申请波次)、`test_set.md`(等总监入集裁定)、
`bots/BotLib/hero_zuus.lua` 的 pos_1/pos_2/pos_3/pos_4 buy list、`game/`。

**净成本**:0 gate id 冲突,0 AWS,0 波次,0 pos_5 之外行为改动。

---

## 1. 选题(为什么是这一件)

按工作流步骤 1 逐条筛:

1. **OWNER_PRIORITIES**:四条常设项(Spot 优先 / P1 拉野 / P2 低血不回家 / P3 cap)
   的球分别在批测台 / 协同组 / 协同组 / 总监,**无本组项**。
2. **open `[hero]` 五条**:
   - #447(CK ckpush 注释更正)—— 本组认为可关,球在**总监**。
   - #417(CM boots 顺序依赖红)—— 根因归 GH #229(并发争用),已落地阳性对照,
     本组认为可关,球在**总监**。
   - #416(zusult 阶梯前 fixture 域塌陷)—— 三条验收全落地,本组认为可关,球在**总监**。
   - #451(tinker 8 处 arity)—— 修完并加了普查断言,本组认为可关,球在**总监**。
   - #453(tinker combo 层接线)—— tinker 不在焦点五英雄,优先级低于任何焦点英雄的活。
3. **backlog 前三条**:
   - `-80`(两个普查在数同一个类)—— 等 GH #438 裁定(总监)。
   - `-81`(GH #447 fixture)—— 球在**录像组**(需要 dumper 跑 `.dem`)。
   - `-82`(tinker combo 接线)—— tinker 不在焦点五英雄,`-82` 自己就写着排在焦点后。

⇒ 走**工作流步骤 1 的兜底**:「backlog 也空 → 挑一个焦点英雄看最近批测归档录像
找个体问题」。

**没走 dumper 逐帧那一条**:本容器没有装 behav dumper,S3 timelines 也不在树里
(`rs/` 空),挖 dumper + 跑 `.dem` 是**录像组**的工作,英雄组当轮不做那件活。
兜底策略换成**扫五个焦点英雄的 buy list / talent tree**,量哪一格与元/主流构筑
差距最大且理论论证清晰。

## 2. 域(为什么是 Zeus pos_5 的 Aghs)

扫过五个焦点英雄的每个 role buy list:

| 焦点英雄 role | Aghs slot | Aghs 之前累计金 | 备注 |
|---|---|---|---|
| Axe pos_1 | 6 | ~5.5k | 已修(gated `axebuyblink` 把 blink 提到 slot 2) |
| Axe pos_3 | 5 | ~5k | 同上 |
| Zeus pos_1 | 10 | ~10k | 中/carry 定位,phylactery/kaya 是合法节奏 |
| Zeus pos_2 (mid) | 12 | ~13k | 同上,mid 有对线需求 |
| Zeus pos_3 | 8 | ~11k | 中排 |
| Zeus pos_4 | 8 | ~8k | 中排 |
| **Zeus pos_5** | **8** | **~11.2k** | **支援位却背着 janggo/BoB/pipe 的 aura stack** |
| Lion pos_5 | 9 | ~12k | 有 boots_of_bearing + pipe;blink 也在 slot 8(可能是下一个候选) |
| Lion pos_4 | 10 | ~11.5k | 同上,mekansm/greaves 优先 |
| CM pos_5 | (~scepter_2 only) | — | 已修 gated `cmboots`,pos_5 已改造 |
| WK pos_1 | 14 | ~15k | 物理 carry 走 armlet/radiance/blink,late Aghs 合理 |
| WK pos_3 | 7 | ~10k | 同上 |

**Zeus pos_5 是差距最大且论证最简洁的一格**:支援位 Zeus 的团战贡献主要是
Nimbus(Aghs 解锁的 15s 团战 zone + vision),而 shipped 顺序把它埋在
janggo → aether → glimmer → pipe → BoB 之后,累计金 ~11.2k,pos_5
支援 ~350 GPM 需要 ~32 min ⇒ Turbo(~20 min 结束)**结构上够不到**。

**这不是「哪个 GPM 高」的争论**,是 Turbo 时间窗内 Aghs 到不到手的争论。

## 3. 修法

`bots/BotLib/hero_zuus.lua`:

```lua
local function ZeusSupportAghsFirst( tList )
    if tList == nil then return tList end
    local nAetherIndex = nil
    local nAghsIndex = nil
    for i, sItem in ipairs( tList ) do
        if sItem == 'item_aether_lens' then nAetherIndex = i end
        if sItem == 'item_ultimate_scepter' then nAghsIndex = i end
    end
    if nAetherIndex == nil or nAghsIndex == nil then return tList end
    if nAghsIndex == nAetherIndex + 1 then return tList end
    local tOut = {}
    for i = 1, nAetherIndex do tOut[#tOut+1] = tList[i] end
    tOut[#tOut+1] = 'item_ultimate_scepter'
    for i = nAetherIndex + 1, #tList do
        if tList[i] ~= 'item_ultimate_scepter' then tOut[#tOut+1] = tList[i] end
    end
    return tOut
end

if J.IsModeTurbo() and J.IsSoakCandidate( 'zeusaghs5' ) then
    sRoleItemsBuyList['pos_5'] = ZeusSupportAghsFirst( sRoleItemsBuyList['pos_5'] )
end
```

**是纯排列**(与 axebuyblink 的 `BlinkFirstBuild` 同形):从 shipped list
派生,不复制 —— 两者不可能 drift 分开。gate-off / non-turbo / 别的 candidate
armed 时,`X['sBuyList'] = sRoleItemsBuyList[sRole]` 拿到的是 shipped 那份
`pos_5` 表,**逐字节相同**(测试文件 §1 tripwire 断言了)。

**幂等**:如果 Aghs 已经在 Aether Lens 后一格,函数原样返回 —— 测试 §10 断言了。

**scope 严格 pos_5**:pos_2 中路 phylactery-first 是合法的对线节奏,不由这一闸管。
测试 §8/§9 两条 SCOPE 断言(`pos_2 Aghs index == 12`、`pos_1 Aghs > 5`)
会红,如果谁后续想扩,得自己开新闸 + 新理由。

## 4. 验证(逐帧 fixture 不适用,走 axebuyblink 同形的 Lua unit test)

`tests/test_zeus_aghs_build.lua`,10 用例:

| # | 断言 |
|---|---|
| 1 | gate off:pos_5 == 蒋制版逐字节 |
| 2 | armed in NORMAL mode:pos_5 == shipped(turbo-only 门) |
| 3 | 另一个 candidate(`wkbuild`)armed:pos_5 == shipped |
| 4 | armed in turbo:pos_5 == armed 版逐字节,Aghs 直接跟 Aether |
| 5 | armed 排列:multiset 相等(没加没丢) |
| 6 | armed 子序列:去掉 Aghs 后与 shipped 去掉 Aghs 一致 |
| 7 | shipped tripwire:Aghs index == 8, Aether index == 4, BoB index == 7 |
| 8 | scope(pos_2 mid 不动):Aghs index == 12(shipped) |
| 9 | scope(pos_1 不动):Aghs index > 5 |
| 10 | 确定性:同输入两次 load 输出一致 |

**变异台(3 变异,全杀)**:

| # | 改动 | 抓到 |
|---|---|---|
| M1 | `if false and J.IsModeTurbo() and ...`(禁 gate) | 只红 §4(armed in turbo) |
| M2 | gate id → `zeusaghs5x`(不存在的 id) | 只红 §4 |
| M3 | 去掉 `J.IsModeTurbo()`(泄漏到 normal mode) | 只红 §2(armed in NORMAL) |

还原后 `cmp bots/BotLib/hero_zuus.lua /tmp/hero_zuus_orig.lua` **逐字节相同**。

## 5. 门

**开工自检**:第一次调用又被 `tail` 吃了 stdout ⇒ REFUSED(证据纪律 3,
连续第 N 次都是本组当轮第一条命令,GH #442 的同族)。识别得早,立刻改成
`>/tmp/sc.log 2>&1; echo EXIT=$?`,真跑完 `worst exit: 3`,findings =
- **cadence**(hero / strategy)
- **trunk-red(python) = GH #410**(`illumove_pairs:HP_CUT` 未登记,**先于本轮、本轮无 `.py` 改动**)
- **UNCERTIFIABLE** × 2(`test_rc_wrapper.py` / `test_selfcheck_lua_leg.py`,腿内被跳过 —— **不是通过**)

**静态**:`bash tools/agent/luacheck_gate.sh` → **`GATE_EXIT=0` / `luacheck bots game: 0 warnings`**
(冷启自装 `lua-check` 5s,**没用 `RULE6_BYPASS`**)。

**定向动态**:
- `test_zeus_aghs_build` **10/0**(本轮新增)
- `zuus` **139/0**(包含新文件 + `test_zusult_pre_ladder_claim_retake` + `test_replay_260819_zuus_ult_manalock` 等旧 Zeus 用例)
- `axe_blink` **53/0**(gate 结构同形,验证未回归)
- `cm_pos5` **19/0**(gate 结构同形,验证未回归)
- `smoke_load` **3/0**、`gate_claim` **16/0**、`soakside` **16/0**

**动态全量套件本轮未跑**(~100min,GH #124,markdown/纯注释未触发时按 CLAUDE.md 可以跳)。

## 6. Known gaps(state.json 里也写了,这里再复述一次是要下游别错读)

1. **没有 Zeus pos_5 逐帧语料**:域论证走 GPM+推理,不是 axebuyblink 那种 4 局
   逐帧 `nw_at_boots` 读数。这一条是主要的 KNOWN GAP,waves 到时**必须**先跑一个
   pos_5 Zeus 行为扫描配对读数。
2. **350 GPM baseline 也是推理**:不是批测读数,需要 pos_5 Zeus 语料校准。
3. **Emergent 问题没答**:一个 Aghs-first pos_5 Zeus 把 glimmer/pipe/BoB 全部
   往后推,是不是**真的**在批测里帮团队赢 —— lanefix 教训适用:局部正确的
   支援构筑改动必须先赚到 condition (b)。
4. **SCOPE 严格 pos_5**:pos_2/pos_1/pos_3/pos_4 各自的对线节奏合法,不由这一闸管。

## 7. 交接

- **`zeusaghs5` 入 `test_set.md`**:球在**总监**。同族先例(axebuyblink /
  cmboots)已经建立,应该是套路 admission。
- **behavioral detector**:condition (b) 的读法要走
  - Aghs 获取时间(t_aghs_first_own,粒度到局)
  - Nimbus 施法数/局(需要新写 detector,当前没有)
  - Aghs owned → first Nimbus cast 的时延
  GPM/XPM 只当 do-no-harm check。这个 detector 的写作大概率落在**协同组 / 录像组**。
- **charter backlog `-83`**:登记这个交接,别掉。
- **open `[hero]` 五条**:球都还在**总监**(#447/#417/#416/#451)或
  优先级下(#453 tinker)。本组的 5 条 open 里 4 条已经交出裁定,只等关闭。

---

## TOKENS

TOKENS total_in=12,639,394 out=60,220 turns=73
