# 英雄组 2026-08-31T19:51Z — 付清 GH #357 第 2 行(最后一条真判定):Alchemist 时钟带

**一句话**:那枚 staged 帧确实第一次落进 `alchrage` 移动的两条带里(t=1190.4 ∈
[900,1800) 且 ∈ [960,1920)),**但时钟是每个调用点五条合取里的最后一条**;另外四条在
这一帧上**全是 0,而且那四个 0 是 mock 的默认值不是观测**——dump 里根本没有承载它们的
通道。⇒ **`alchrage` VERDICT UNCHANGED**,而且**比第 3 行更糟一级**:第 3 行的堵点还在
dumper 已经写的 schema 里(以后的帧原则上能动),这一条的堵点在**生成器**里——
**`make_fixture.py` 产得出的任何一帧、任何世代,都钉不了这个决定**。

`bots/` 0 行、`game/` 0 行。零新 gate id、零 arm/promote、零 AWS(连 S3 GET 都没有)、
不申请波次、不开新 issue;`state.json` / `queue.json` / `test_set.md` 均无新增。

---

## 1. 认领依据

- `iterations/OWNER_PRIORITIES.md`:P1/P2 当前球在**协同组**,P3 在**总监**,常设运维在
  **批测台**——**本组无未完成优先项**(铁律 9 走完)。
- open issue 里 `[hero]` 前缀:#366(本组上一轮自己开的,在等总监)、#359 / #354 /
  #357 / #348 / #330 / #328。章程 backlog `-63` 的「下一棒 (1)」逐字点名:
  **「#357 第 2 行(Alchemist 时钟带)是最后一条真判定」**。取它。
- 帧**按名字**加载(`tests/frames/`,不进 `tests/fixtures/` 的 glob)⇒ #357 表里
  **9 个 ratchet 一个都没动**。

## 2. ⭐⭐ 主读数:五条合取,只有最后一条被这帧满足

Roshan 调用点(`bots/BotLib/hero_alchemist.lua:616-625`,Tormentor 那条同形)读的是

```lua
J.IsDoingRoshan(bot) and J.IsRoshan(botTarget)
    and J.IsInRange(bot, botTarget, 500) and J.IsAttacking(bot)
    and DotaTime() < X.GetRageObjectiveClock(15, 30)
```

在 `f_20260831_004433_cm_creepreach`(t=1190.4 = 19:50)上,**通过真实 loader、用出货
helper 逐个求值**,10 个英雄:

| 合取 | 读数 | 0 的来源 |
|---|---|---|
| 载体是 Alchemist / Rubick | **0 / 1,080 hero-instant**(108 帧、41 个英雄) | 语料真的没有 |
| `J.IsDoingRoshan` | **0 / 10** | `GetActiveMode()` 答 **0**;`BOT_MODE_ROSHAN = 1020` |
| `J.IsDoingTormentor` | **0 / 10** | 同一个 reader;`BOT_MODE_SIDE_SHOP = 1007` |
| `J.IsAttacking` | **0 / 10** | `GetAnimActivity` / `GetAttackPoint` / `GetAnimCycle` **三个**都答 0 |
| `GetAttackTarget`(喂给 `J.IsRoshan`) | **0 / 10**(nil) | dump 里没有 |
| `DotaTime() < clock` | **✅ 唯一为真的一条** | t=1190.4:出货说 FIRE、armed 说 HOLD |

**那四个 0 全是 mock 默认值,不是观测**——这正是发现本身,不是对发现的免责声明。

## 3. ⭐⭐ 最该拿走的:堵点在**生成器**,不在世代

第 3 行(黑黄杖)找到的是「物品 0 是**代理**,承重的是免疫 0」,但**两者都在 dumper
已经写的 schema 里**,后来的帧原则上都能动。这一条不是:

`make_fixture.py` 只发三类记录 —— `units`(**只有英雄**;1,080 个单位 0 个非英雄)、
`buildings`(tower / barracks / watch_tower / ancient)、`creeps`(**`team,x,y,dt`,
连 name 都没有**;108 帧 19 条 creep 记录,**0 条带 name**)。而 `J.IsRoshan` /
`J.IsTormentor` 判的是 `string.find(GetUnitName(), "roshan"/"miniboss")`。
⇒ **这个 schema 里不存在任何一种记录,它的 `GetUnitName()` 可能命中那两个词。**

**⇒ 于是 `[corpus]` ratchet 自己的失败文案是错的**:它写「pin the decision on that
frame instead of resting on the arithmetic alone」,**那条指令不可执行**。ratchet 本身
留着(它就是把这次读数逼出来的东西,而且**条件仍然为真**:语料 tMax = **790.4** <
armed 900,它现在是**绿的**),**`assert(tMax < ARMED, ...)` 的条件逐字节未动**,只把
**文案**改指到新文件。买 `alchrage` 条件 (a) 的路仍然只有**一条**:波次
(`[domain]` 节已经说了 `SOAK_CAP_MIN=25 > 15`,可买)。

## 4. ⚠️ 变异台上自己翻的车(M11 幸存)

14 个变异,**13 个第一次就见红且只红在该红的节**;**M11 幸存**:把 rubick 孪生体的
`function X.ConsiderChemicalRage` 改名成 `...RageXX`,我的断言
`src:find('function X%.ConsiderChemicalRage')` **照样命中**——因为改名后的串**包含**原串。
**这就是 backlog `-63` 那条「裸 `black_king_bar` 是 `modifier_black_king_bar_immune`
的子串」一周内的第五次同形**,而且这次它**是在正好要证明「载体集合就这两个文件」的那行
上**失效的。已改成锚在左括号上(`function X%.ConsiderChemicalRage%s*%(`),`grep` 那条
同改为 `grep -rlE "function X\.ConsiderChemicalRage *\("`;重跑 **M11 见红**。

另一次小翻车,是控制组自己抓的:`[control]` 第一版只给 `GetAnimActivity` 上戳,
`J.IsAttacking` **仍然 false**——因为它读**三个**引擎字段
(`GetAttackPoint() > GetAnimCycle() * 0.99`,两者默认 0,`0 > 0` 为假)。
控制组**该红的时候红了**,而它红出来的东西**加强了主结论**(缺的通道是三个不是一个)。

变异清单:M1 帧时刻移出带 → `[frame]`;M2 帧里出现 Alchemist → `[carrier]`;
M3 单位改名 `npc_dota_roshan` → `[schema]`;M4 creep 记录长出 name → `[schema]`;
M5 生成器长出 name 字段 → `[schema]`;M6 `J.IsDoingRoshan` 恒假 → `[control]`;
M7 `J.IsRoshan` 恒真 → `[control]`;M8 姊妹 ratchet 被删 → `[sister]`;
M9 姊妹条件被放松 → `[sister]`;M10 调用点常数漂移 → `[source]`;
M11 孪生体改名 → `[carrier]`(修后);M12 第三个文件定义载体函数 → `[carrier]`;
**M13 谓词本身清空(`CARRIERS = {}`)→ `[carrier]`**(-63 那条「空谓词的 0 和空语料的 0
是同一个整数」的专门变异);M14 语料扫描瞎掉 → 红。

**先存副本、从副本恢复,绝不从 git 恢复**(#238 那笔学费)。还原后 7 个文件 `cmp`
逐字节相同,`bots/FunLib/rubick_hero/_m12_tmp.lua` 等残留 0 个,`git status` 只有本轮
两个意图内的文件。

## 5. 改了什么

| 文件 | 改动 |
|---|---|
| `tests/test_alchemist_rage_clock_staged_frame.lua` | **新增**,11 个节点(`[frame]` `[corpus]` `[carrier]`×3 `[domain]` `[control]` `[schema]`×2 `[source]` `[sister]`) |
| `tests/test_alchemist_rage_objective_clock.lua` | **只改注释与文案**;`assert(tMax < ARMED, ...)` 的**条件逐字节未动** |
| `tests/frames/README.md` | reopen 清单第 2 行 OPEN → PAID;三行全清;帧的 used-by 补一项 |

`bots/` / `game/` **0 行**。

## 6. 门(铁律 6)

- **静态**:`bash tools/agent/luacheck_gate.sh` → **exit 0 / `luacheck bots game: 0 warnings`**
  (裸读,**没用 `RULE6_BYPASS`**;容器缺 luacheck,gate 自己装的 `lua-check`)。
- **动态是子集不是全套**(GH #124,~100min 跑不完一进程):按文件名过滤跑了
  `alchemist` 21/0、`activemode` 14/0、`axe_cull` 36/0、`axe_t15` 21/0、`bbfight` 20/0、
  `campfarm` 16/0、`focus_innate` 13/0、`cm_creep` 19/0、`smoke` 3/0、`gate_claim` 10/0、
  `fixture` 93/0 —— 覆盖了 #357 表点名的每个文件。**全部 exit 0。**
- **开工自检**(铁律 10):第一次 **REFUSED(exit 2)= 什么都没检查不是通过**(我把 stdout
  管给了 `tail`,脚本自己拦下并指出这是 evidence-discipline 第 3 条、第 5 次复发);
  重定向重跑 **exit 3**,findings = `cadence` / `queue-rulings` / `trunk-red(python)`。
  那条 python 红是 `tests/test_rc_wrapper.py`,**已由 GH #364 立案为 flaky**,
  **本 diff 零行 python**。快 Lua 检测器 60 个文件 0 失败。

## 7. 下一棒

1. **#357 的三条真判定全部付清** ⇒ **把帧搬进 `tests/fixtures/` 现在解锁了**,它是
   下一个干净的工作单元,代价是表里剩下的 5 条记账行(计数与分母在收编那一刻重取)。
2. **`alchrage` 只剩波次一条路**买条件 (a)。本轮**不**提 queue 请求:该 id 还没进测试集,
   入集要走 `test_set.md` 提议 + 总监批准(#357 请裁的两件事**仍在总监手上**),
   先入集再谈波次;本轮把这条写进 issue 追评交回,不越位。
3. **`-43a` 的 Zeus 方向仍欠**(连续多轮)。
4. GH #366 + queue `hero-26` 仍在等总监 / 批测台。

## Token 用量

```
TOKENS total_in=6,942,080 out=43,951 turns=54
```
