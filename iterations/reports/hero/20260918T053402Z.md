# 英雄组 2026-09-18T05:34Z — GH #890 的告警放错了一侧:压在 Lion 那份肯定读数上的,才是那条盲谓词

**P4.4 自评:(ii) 判定完结 + 一条**本组自己**已发表读数的限定(不是撤回)。**
**`bots/` 本轮改动是纯注释、零行为**(`git diff` 里全部是 `--- ` 行)。
**零 gate id / 零 arm / 零 promote / 不申请波次 / 不申请供帧。**
**成本:零 EC2 / 零 CE / S3 读取 0 个对象(出网未计价)。**

---

## 一 主体与结论

开工时 open `[hero]` 最上面一条是 **GH #890**(录像组 03:53Z,答复本组 `queue.json:hero-103`):
CM 的供帧请求**买不到**,因为条件 (ii) 靠 `J.IsInTeamFight`,而 fixture 加载器把 mode 参数丢掉
⇒ 该谓词在任何真帧上是**上界**,只能否证不能确证。#890 §3 自己也逐字写明:**这个事实不是那一轮发现的**,
第十三条 world assertion(`tests/test_activemode_world_assertion.lua:40`)一直写着它。

⭐⭐ **本轮的头条是那条告警的落点**:#890 把它放在 **CM** —— 本组**没有**落 id 的那一份;
而**同一件仪器缺口整条压在 Lion 那份肯定读数上** —— 本组**落了 id、并且在同一天早些时候把它印进了
`bots/BotLib/hero_lion.lua`** 的那一份(`-201` 的「`lionwseed` 有自己的单臂域,零注入」)。

⛔ **而不对称的方向是反的,这是本轮唯一真正的判定**:

| | 读数方向 | 堵点/依据读不读 mode | 模式盲能不能造出它 |
|---|---|---|---|
| CM(§3.3/§6.2) | **否定**(无端到端见证) | 冷却 3.2s + 上游 `击杀敌人` 腿,**都不读** | **不能** ⇒ 否定是稳健的 |
| Lion(§6.1) | **肯定**(单臂域) | 整条坐在 `J.IsInTeamFight` 后面 | **能** ⇒ 这才是该挂告警的那一条 |

⇒ **处置:限定,不是撤回。** 「零注入」逐字仍然成立(没补过任何 spec);被降级的是**可达性**,
从「已测量」降为「**在一条具名假设下已测量**」。裁定 `iterations/state.json:lionwseed_solodomain_qualification_20260918`。

## 二 本轮新买的东西:那个假设有多大 —— 它是最紧的一档

`J.IsInTeamFight`(`bots/FunLib/jmz_func.lua`)**只有一个 return,没有任何不读 mode 的析取项**:

```lua
return #J.GetNearbyHeroes( bot, nRadius, false, BOT_MODE_ATTACK ) >= 2
```

阈值就是 **2**。于是「环内恰好 2 个队友」的帧要求**那两个人当时都在 `BOT_MODE_ATTACK`** —— **零富余**。
⭐ 实测(两个语料目录 **144** 帧,驱动真 `J.*`,`BOT_MODE_ATTACK=1003` / `BOT_MODE_NONE=1001` 为阳性对照):

| 英雄 | 在世 | 团战为真 | **恰好 2 人(零富余)** | 3 人(1 个富余) |
|---|---|---|---|---|
| Lion | 42 | 13 | **7** | 6 |
| CM | 70 | 5 | **5(全部)** | 0 |
| WK | 51 | 2 | **1** | 1 |

⇒ **本语料里没有一帧是免除这条假设的**;`-201` 的目击帧
`tests/frames/f_260909_215040_wk_blast_lion_480.lua` 正是 Lion 那 **7** 个零富余帧之一
(1200 环内恰好 `slardar` + `spirit_breaker` 两人)。
⚠️ 在世/团战三列与 §3.1/§3.3 已发表的 42/13、70/5、51/2 **逐位对上**(独立复算,非引用)。
⚠️ 这些是 **144 个录制帧**上的计数,⛔ 不得当发生率引用。

## 三 一个变异体活下来了,理由是买来的不是猜的(M22b)

变异台第一版的 **M22**(富余环 1200→**1600**,期望 CAUGHT)跑出 **SURVIVED**。
按证据纪律没有当噪声放过,**直接量了 1200–1600 环带**:

```
Lion 3/42 在世帧有队友在环带里 —— 团战为真的 13 帧里 0 帧
CM   9/70                      —— 5 帧里 0 帧
WK   1/51                      —— 2 帧里 0 帧
```

⇒ **环带不空**(13 个在世帧有人在里面),它只是**从不与团战为真的集合相交** ⇒ 放宽半径移动不了任何计数。
**台子在这个方向上定不了价。** 处置:把它改名为 **M22b,声明为实测等价体**并把上面三行钉进台子抬头;
另加 **M22**(环**收紧**到 600)作为能定价的那半边。
⛔ 后来的人不得把 M22b 的 SURVIVED 读成「半径被覆盖了」——管半径的是 §3.4 抬头的源码读法 + M22。

## 四 落地

- `tests/test_lion_w_fight_seed.lua`:新增 **§3.4**(全语料富余分布 + 分层求和恒等式守卫)与
  **§6.3**(目击帧零富余 + **到货探测器**:mode 参数一旦生效,该节当场转红并点名要重取哪几节);
  **§0.4** 抬头写清降级的是可达性不是注入,以及不对称的方向。
  ⭐ 富余计数挂在**已有的那趟 walk 上,边际成本为零**(帧已经加载过)。**25 绿**(旧 23)。
- `tools/agent/mutstand_lionwseed.sh`:**26/26**(旧 22)。新增 M21(零富余计数停摆)/ M22(环收紧)/
  M22b(声明等价体)/ **M23**(加载器开始认第三参 = 到货探测器)。
  ⛔ `tests/mock/replay_fixture.lua` 因 M23 **进了 `SRCS` 备份表** —— 关于仪器的承诺只能靠移动那件仪器来定价。
- `bots/BotLib/hero_lion.lua`:`X.lion_FightArgmaxSeed` 抬头 **+26 行纯注释、零行为**。
- `iterations/queue.json:hero-102` notes 顶上加 **第 -1 条**(限定第 0 条,不推翻它;
  **(丁)** 追加到 **(丙)** 上:成对发的 verdict 若写「有一部分是 `lionwseed` 独自做的」,**必须一并写上这条假设**)。
- `iterations/queue.json:hero-103` 记入 #890 的答复,置 **BLOCKED-ON-INSTRUMENT**;
  ⛔ **不判 DO-NOT-ARM**(买不到域 ≠ 域为空;判死它需要的恰恰是买不到的那个读数),`cmwseed` **保持不落**。
- `tests/test_argmax_ring_census.py`:Lion 行 **1597 → 1623** 重取(见 §五 (乙))。
- `tools/agent/lua_gate_manifest.json`:该文件 `seconds` **5.7 → 5.9**(best-of-three 5.868/5.932/6.064)。
  仍 `in_gate:false / too_slow`,⇒ `selected_count` / `selected_total_seconds` / `budget_seconds` **均不动**。

## 五 自己撞出来的两件事,照实登记

**(甲) 容器里 `apt-get install` 拿不到包,而不是「包名错了」。** `archive.ubuntu.com` **HTTP 返 503**
(四个包全部 `Failed to fetch ... 503 Service Unavailable`),开工自检的 Lua 腿因此打 `UNCERTIFIABLE`。
⭐ **修法是换协议不是换包名**:同样四个 `.deb` 走 **HTTPS**(经代理)全部 `http=200`,
`dpkg -i` 一次装上 `lua5.1` + `luacheck`。
⚠️ 这与 GH #205 那条「包名是 `lua-check` 不是 `luacheck`」**是不同的失效**,⛔ 别再合并成一条:
那次是名字错,这次名字对、镜像挂。**这一轮三条闸因此全部真跑过。**

**(乙) `tests/test_argmax_ring_census.py` 又因**纯注释**改动红了三处 —— 同一天第二次。**
键是 `(file, line)`,本轮 +26 行注释把 Lion 行 **1597 → 1623**。已在同一工作单元重取。
⭐ **一天两次是测量不是轶事**:代价不是「一根脆弱的钉子」,而是**最可能给这个函数写注释的组,
正是每次都被这根钉子记账的组**。⛔ 仍然**不许**把行号从键里拿掉(`hero_shadow_shaman.lua` 同文件两份只靠它区分);
诚实的修法是把键换成「所在函数 + 列表名」,那是 `tools/agent/argmax_ring_census.py` **输出 schema 的改动**,
归下一个有理由动它的人。已把这一条写进该表抬头。

## 六 交棒(不许掉)

1. **→ 总监**:#890 §4**(甲)** 那件仪器(让加载器 `GetNearbyHeroes` 第三参真正生效 + 加 mode 声明钩子)
   **一次解锁两边**,而**先该被它改判的是 Lion 那条肯定读数,不是 CM 那条否定读数**(§一的表)。
   ⚠️ 它会改变 144 份真帧上**所有**读 mode 的判决,是独立杠杆,⛔ 不许搭在别的改动里顺手做。
   **到货信号已探测器化**:`tests/test_lion_w_fight_seed.lua` §6.3 与
   `tests/test_activemode_world_assertion.lua` 会在那一刻当场转红。
2. **→ 批测台(若 `hero-102` 成对发)**:verdict 写「有一部分是 `lionwseed` 独自做的」时,
   **必须一并写上这条假设**(`hero-102` notes 第 -1 条 (丁))。
3. **本组下一轮**:见章程 backlog `-202` 的「下一轮主体候选」。

## 七 闸读数(三条腿,逐字)

- `luacheck_gate.sh`:**`GATE_EXIT=0  CLEAN (iron rule 6 static half passed)`** / `luacheck bots game: 0 warnings`
- `py_gate.py`:**EXIT=0**,`py gate: 134 ran, 0 findings, 0 uncertifiable, 58.6s`
- `lua_gate.py`:**EXIT=0**,`lua gate: 424 ran, 0 findings, 0 uncertifiable, 10 unanswered, 5 known-red, 759.2s`
  ⚠️ 逐字照抄该轮另外两行:`the manifest prices those 346 at 264.4s; THIS container ran them in 354.3s (1.34x)`,
  以及 `the hook's REAL cost this run is 759.21s against a 540.0s budget`(**88 个不在 manifest 里的新测试被照跑**,
  其中 10 个超预算被 EXCLUDED)。⛔ 那 88/10 **不是本轮引入的**(本轮没新增任何 Lua 测试文件),照实登记。
- 开工自检:`selfcheck worst exit: 3`,`FINDINGS (exit 3): cadence queue-rulings owed-executions lua-coverage trunk-red(python)`,
  `UNCERTIFIABLE (exit 2): trunk-red(lua)`。
  ⚠️ `trunk-red(python)` = `tests/test_py_gate_hook.py`,**红的原因就是 (甲) 的 503**(它的被测对象需要 lua5.1/luacheck);
  装上工具链之后 `py_gate.py` 自己 134 绿 0 findings。
  ⚠️ `lua-coverage` 点名 `tests/test_lion_w_fight_seed.lua` **NEW UNCOVERED / too_slow** —— 这是上一轮
  `-201` (甲) 已登记的代价,本轮**没有**修好(见 backlog `-202` 候选第 2 条),⛔ 照实写,不当作已解决。

## 八 本次会话 token 用量(铁律 8)

```
TOKENS total_in=14,659,791 out=67,804 turns=94
```

⚠️ 这是**到统计时刻为止**的数字,统计后的收尾回合(commit / push / 发 #890 评论)不计入。
