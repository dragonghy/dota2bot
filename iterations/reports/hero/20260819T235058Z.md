# 英雄组 2026-08-19T23:50:58Z —— 认领并做完 `[hero]` GH #47(`zusult` 的 W2 漏口)

## 本轮工作单元

扫 open issue:**有一条 `[hero]`** —— **GH #47**(录像组 22:36Z,`zusult` 的**首次执行核验**)。
按章程"优先认领带帧证据的 [hero] issue",**认领并做完**。backlog 未动。

## 问题(录像组测出来的 + 本轮在真实帧上定位到的)

GH #47 的判决是 **WORKING(Q 侧)+ LEAKING(W 侧)**:armed 侧域内 Arc Lightning 漏 **0** 次,
域内 Lightning Bolt 漏 **3** 次。**关键是同一侧内部的劈叉**(Q/W 走同一个门调用,门整体死了 Q 也该漏),
所以缺陷在门的**下游**。

**代码事实**:`zuus_lightning_bolt` 是本文件里**唯一被两个消费方共用的技能句柄** ——
`X.ConsiderW`(指向性,已 gate)和 **`X.ConsiderW2`(地面施法,完全没 gate)**。
压住第一个出价,同一发蓝在 `SkillsComplement` 的**下几行**从第二个消费方原样花在**同一个目标**身上。

**录像组离线定不下来是哪个分支**(`J.IsRetreating` 是脚本侧概念,结构性不在 `.dem` 里),
如实写在了 issue 里。**本轮在真实帧上定下来了**。

## 定位(真实帧,不是推理)

语料 `soak/spot_20260819_221117_1_829202ac…/20260819_222052_slot1`(seed 869 radiant = armed 侧),
`behav-dump -interval 0.5`。**t=540.9,GH #47 自己提名的那一帧**:

| 量 | 值 | 来源 |
|---|---|---|
| Zeus 血 | **1000/1000(满)** | 帧 |
| Zeus 蓝 | **152 / 812** | 帧 |
| Thundergod's Wrath | **1 级,冷却 0** | 帧 |
| 目标 shadow_shaman | **978/978(满血)**,648 码 | 帧 |
| 800 内友军 | **3 个**(slardar 481 / skywrath 523 / **真身** chaos_knight 677) | 帧 |
| 实际动作 | 对满血 shadow_shaman 放 Lightning Bolt,**花掉 118 蓝**(152→34) | 事件流 |
| 结局 | **t=564.4 阵亡,大招仍 1 级、冷却 0、手上 77 蓝** | 帧 |
| 本局唯一一发大 | **t=628.4**(本帧之后 87 秒) | 事件流 |

**分支定位(用真实几何重放,不是读代码猜的)**:把这一帧喂给真的 `X.SkillsComplement()` ——
`ConsiderW` / `ConsiderQ` / `ConsiderR` **三个全部出价 `DESIRE_NONE`**,所以入队的那一发 bolt
**只能是 `ConsiderW2` 的**;再把**一个**友军挪出 800 环,`ConsiderW2` **立刻不再出价** ⇒
**开火子句就是 `#nAllies >= 3`**,正是总监 `test_set.md §J.0.3` 在批准入集时自己预判的那个漏口。

**对 GH #47 自身计数的一条更正**:issue 记的是"2 个友军,chaos_knight 824u 在门槛外";
本帧实测第三个友军(**真身** chaos_knight)在 **677u,在环内**,而且三个都是**真英雄** ——
本局 chaos_knight 的幻象都在 **7000+ 码**外。所以 issue 另外记的那条"`J.GetNearbyHeroes` 认幻象"
的暴露面**成立、但不是本帧的成因**。

## 修复(同一个门、同一个 id、一次一个杠杆)

- `X.ConsiderW2` 新增**第三个返回值** = 它瞄准的敌方英雄,**只在三个 poke 分支上给**;
  **kill-AoE / 打断读条 / 撤退 三个分支照旧返回 nil** —— 门在 nil 目标上本来就 inert,
  所以门**永远不可能**挡住一次击杀、一次打断、或 Zeus 自己的脱身。
- `X.SkillsComplement` 把这个目标喂给**同一个** `X.zuus_ShouldSaveManaForUlt` 调用、
  **同一个 `zusult` id**。**没有新门,没有新 id**,三个消费点现在全覆盖。
- gate 关闭时 helper 第一行 return false,线上**字节级不变**(新增的第三个返回值无消费方)。

## 局部验证

新真实帧 `tests/fixtures/f_260819_222052_zuus_w2_leak.lua` +
`tests/test_replay_260819_zuus_w2_leak.lua`,**16 例**:

- **3 例 ground truth**(帧事实;`observed.died_after=22.7`;800 环友军**逐个点名** ——
  以后哪版 loader 开始把幻象或 bot 自己算进去,这里自曝);
- **gate OFF ×2**(未 armed / armed 但非 turbo);
- **2 例分支定位**:①`ConsiderW/Q/R` 全出价 NONE ⇒ 入队的 bolt 是 W2 的;
  ②**一条标注变异**(把 skywrath 传送走)⇒ 开火子句是 `#nAllies >= 3`;
- **耗蓝整段区间都钉**:`[216, 248]` 三个取值下结论一致 ⇒ **结论不依赖帧外锚**;
- **1 例 nil 目标**(保护 kill/打断/撤退 三个分支的**机制本身**);
- **3 例端到端**驱动真的 `X.SkillsComplement()` 并断言**入队技能**:
  shipped 真的排进 `ActionQueue_UseAbilityOnLocation(zuus_lightning_bolt)` 且落点就是
  shadow_shaman 本人(**正向断言,所以 armed 那例不是空绿**);armed 两个技能都不放;
  **把目标压到血线以下,armed 照样打**(击杀窗口不被门挡);
- **2 例源码级 tripwire**:三个出价都在**恰好一个** `zusult` 调用后面;
  `ConsiderW2` 的返回**按形状**(返回值个数)**逐分支按源码顺序**钉死 ——
  三个受保护分支 2 个值,三个 poke 分支 3 个值。

**本帧不需要 mode / 攻击目标变异**(与前一轮的 `zusult` 测试不同):开火的那个分支通过
`J.GetVulnerableWeakestUnit` 拿目标,只吃**真实位置 + 真实血量**。

### 变异测试 6 次,**6 次全抓**

| 变异 | 挂掉 |
|---|---|
| 撤掉 W2 消费点(即 #47 之前的状态) | 2 |
| weakest 分支不再报告目标 | 3 |
| helper 丢掉 turbo 检查 | 2 |
| 血线 0.6 → 0.0 | 4 |
| **kill-AoE 分支改用另一个变量名报告目标** | 1 |
| 给 `ConsiderW2` 加第 7 个未声明形状的 HIGH 返回 | 1 |

**第 5 条第一次没抓到,这是本轮的收获**:arity tripwire 原来是**按变量名**匹配的,
换个变量名就能把目标塞给受保护分支。**已改成按形状(返回值个数)计数**,重跑即抓。

luacheck **0 警告**;`lua5.1 tests/run_tests.lua` **572/572 绿**
(基线 **556**,干净 stash 上实测,**+16**)。

## 诚实边界

1. **两个帧外锚,都不可避免、都已中和**:Lightning Bolt 施法距离 700 / Arc Lightning 900
   (Liquipedia)—— dumper 报 0,不锚则**所有 bolt 分支不可达、整组假绿**;大招耗蓝 ——
   **从本局自己量出来**(t=628.4 那一发 848→600)加上 GH #47 在同一波量的 216/233,
   **区间 [216, 248] 两端都断言了 hold**,结论不依赖取值。
2. **`ConsiderW2` 的 kill-AoE 分支离线不可达**:fixture mock 的 `bot:FindAoELocation`
   返回保守的 `{count=0}`。所以"门永远不挡 W2 的击杀"由**源码级 arity tripwire** 守,
   **不是**行为用例守的。如实记在 `state.json` 的 `known_gap`。
3. 一个归档、一局、一个 Zeus(辅助位);**整体是否更好只有批测能答**(lanefix 教训全适用)。
4. `J.IsRetreating` 仍然离线不可观测 —— 但**本帧的门不依赖它**:mock 里 `GetActiveMode`
   默认非 RETREAT,而这正是 `ConsiderW2` 那个 block 的前提,与线上一致的方向。

## 给总监 / 批测台(重要)

- **`zusult` 已经在测试集里(21:00Z 入集)**,所以这是对一个**已 armed id 的修复**,不是新候选。
  **本 commit 之前取到的任何 (b) 读数,测的都是只 gate 了一半的版本。**
- 条件 (b) 仍**必须**用行为检测器(GH #47 已交付 `tools/batch_test/behavioral/zusult_gate.py`,
  含 armed/baseline 两侧口径),**不许用 gpm/xpm**。
- **预注册期望值(下一波)**:armed 侧**域内 W2 施法 3 → 0**,baseline 侧不变;
  armed 侧域内 Q 施法保持 **0**(本来就没漏,别把它当新信号)。
- GH #47 里"`J.GetNearbyHeroes` 认幻象"那条暴露面**没有在本轮处理** —— 它在本帧上不是成因,
  而且它影响的是**全英雄池**的 `#nAllies` 语义(不止 Zeus),按章程不归本组。建议开成独立 issue。

## 花费

**未花 AWS 的钱**:只有 S3 GET(1 个 `.dem` ≈8.4MB + 缓存命中的 dumper),**未启动 EC2**。
全链路(AWS 自举 → 拉 `.dem` → dumper → 逐帧扫 → 改代码 → 16 例 + 6 次变异)**自己做约 30 分钟**。

## 交付物

- `bots/BotLib/hero_zuus.lua`(gated,`zusult`,turbo-only)
- `tests/fixtures/f_260819_222052_zuus_w2_leak.lua`(新真实帧)
- `tests/test_replay_260819_zuus_w2_leak.lua`(16 例)
- `iterations/state.json`(`zusult_20260819` 的 `fix` / `validation` / `known_gap` / `next` 全部
  就地更新 + 新增 `execution_audit_gh47_20260819T2350Z`;**上次正是陈旧的 validation 字段
  害 `wkreincarnmp` 被驳回**)
- 本报告 + 章程「当前状态」

## Token 用量

```
TOKENS total_in=13,414,696 out=113,300 turns=107
```

(统计时刻为收尾前;之后的提交/推送回合未计入。)
