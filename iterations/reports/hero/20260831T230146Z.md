# 英雄组 2026-08-31T23:01Z —— GH #366 第 2 步的分叉判掉了:是 (a),而且墙在第 15 格

**轴**:认领 [hero] GH #366(唯一带 `hero` 标签、球在本组的 open issue),付它
「**建议的验收方式**」第 2 条:**根因二选一先判掉** —— (a) bot 侧真的没花点,
(b) dump 侧 `abilities[].level` 在后期是陈旧读数。#366 要求「**判据要能分开这两条**」。

**判掉了:是 (a)。而且不是十条构筑行各自出错,是一条共享的队头阻塞。**

`bots/` 0 行、`game/` 0 行;零新 gate id、零 arm/promote、零 AWS(连 S3 GET 都没有)、
不申请波次;`state.json` / `queue.json` / `test_set.md` 均无新增。
新文件一个:`tests/test_skill_point_stall_frame.lua`(8 个节点,`[ratchet]`)。
帧**按名字**加载(`tests/frames/`,未进语料 glob)⇒ GH #357 表里 9 个 ratchet 一个都没动。

---

## 1. ⭐⭐ 主读数:十个英雄、两个队伍、十份英雄脚本,**同一个截断点**

帧:`tests/frames/f_20260831_004433_cm_creepreach.lua`(`69e067/20260831_004433_slot1`,
t=1190.4 = 19:50),就是 #366 自己用的那一枚。

| 英雄 | 等级 | 非天赋技能前四名的 rank | 和 | 天赋数 |
|---|---|---|---|---|
| axe | 21 | 4,4,3,2 | **13** | **0** |
| crystal_maiden | 17 | 4,4,3,2 | **13** | **1** |
| lina | 22 | 4,4,3,2 | **13** | **0** |
| lion | 20 | 4,4,3,2 | **13** | **1** |
| necrolyte | 18 | 4,4,3,2 | **13** | **0** |
| shadow_shaman | 20 | 4,4,3,2 | **13** | **1** |
| skeleton_king | 21 | 4,4,3,2 | **13** | **1** |
| skywrath_mage | 19 | 4,4,3,2 | **13** | **1** |
| sven | 18 | 4,4,3,2 | **13** | **0** |
| zuus | 22 | 4,4,3,2 | **13** | **1** |

**10/10 拿的是同一个 rank 多重集 `{4,4,3,2}`,和 13;六个各有一个天赋,四个一个都没有,
没有任何一个有两个。** 前四名之外的非天赋条目**全部是 rank 1**(innate / facet 赠送)。

⇒ **13 个构筑技能点 + 至多 1 个天赋 = 十四格技能表条目**,谁都一样,不管它多少级。

**量具没有用任何名字表**(这是 #366「量具」一节点名的坑:手写 facet 名单在开放集合上
不是分类器,它对 Axe 答对、对语料里 42% 的单位答错):
- **天赋数**按引擎自己的前缀 `special_bonus_` 数 —— 那也是 dumper 自己的过滤键
  (`dumper/main.go:isRealAbility`),**不分类任何东西**;
- **构筑点数**取非天赋条目 rank 排序后的**前四名**(三个基础技 + 大招),并**断言**
  其余条目都 ≤ rank 1。十份不同的构筑行不会碰巧对齐同一个多重集。

## 2. ⭐⭐ 墙在哪儿:第 15 格,而且是**源码可证**的队头阻塞

两条都从源码读出来,不需要第二局语料:

**(i) 发货的 `J.Skill.GetSkillList` 把天赋放在第 10 / 15 / 18 / 19 格**
(15 条技能行 + 8 条天赋行;用**真实函数**跑,天赋名是声明输入 —— mock 自己的
`GetTalentList` 答八个 nil,这一点是**测出来的不是假设的**,也正是「到底哪一条升级条件
不满足」在离线世界里够不着的原因)。
⇒ 13 个构筑点 + 1 个天赋 = 消费掉 14 格 ⇒ **停下来的那一刻,队头就是第 15 格 = t15 天赋**。

**(ii) `bots/ability_item_usage_generic.lua` 的末尾 `else` 分支不删任何东西,除非
`botLevel > 25`**:

```lua
else
    print("[WARN] Skipped to level up ability ... because it may fail.")
    if botLevel > 25 then
        ...
        table.remove( sAbilityLevelUpList, 1 )
    end
end
```

⇒ 一个过不了上面三条分支的队头,**把它后面的一切挡到 26 级**。Turbo 局不到 26 级。
这与 **GH #286 同族**:#286 落地的 `CompactSkillList` 只覆盖 **nil 队头**这一形;
**非 nil 但升不了**的这一形没人管。

**⚠️ 而且第二堵墙已经在图纸上**:第 18 / 19 格是 **t20 / t25** 天赋,分别在
**第 18、19 个技能点**上被端出来 —— 比它们各自的 20 级 / 25 级门槛**早 2 级和早 6 级**。
就算第 15 格修好了,同一个 `else` 会在 18 级和 21 级各卡一次。这一条已写进
`test_skill_point_stall_frame.lua` §5 的断言里,**不是散文**。

## 3. ⭐ (b) 没有机制

- dumper 的 `resolveAbilities` 逐格走 `m_vecAbilities`,**每次采样**从活的 ability entity
  上读 `m_iLevel`,**没有缓存、没有 first-seen memo**(`main.go:352-384`,快照点
  `Abilities: resolveAbilities(h.idx)`)。测试断言这三件事,并断言函数体里**没有 `map[`** ——
  哪天它长出一个缓存,(b) 就重新上桌,而那会当场变红。
- 数据那半:同一帧上**有 rank 4、rank 3,还有 rank 1 的天赋**,t=1190.4。
  字段在这个钟点**没有被截断也没有被夹住**。

## 4. ⭐ 顺带把 #366 自己的对照组退了役(这是本轮唯一动到既有裁定的地方)

#366 用 **Crystal Maiden 当帧内对照组**:她 17 级、大招 rank 2「**是对的**」,
所以那个读数「分得开短了和就该是 2」。

**按大招 rank 读,这话成立;按技能点读,不成立。** CM 17 级、13 个构筑点 + 1 个天赋
= 14 格,而 t15 那一档在**两级之前**就已经端给她了。**对照组在 #366 用的量具下存活,
在这一把量具下溶解。** 这正是把结论写成「点数」而不是「大招 rank」的理由 ——
损失不是「九个大招各短一级」,是**每个英雄欠 3 到 8 个技能点**(等级 17→22)。

## 5. ⚠️ 本文件**没有**确立的三件事(引用时请一起带上)

1. **第 15 格具体栽在三条升级条件的哪一条**(`IsHidden()` /
   `GetHeroLevelRequiredToUpgrade()` / `CanAbilityBeUpgraded()`)—— **没判**,而且离线判不了:
   mock 的 `GetTalentList` 答八个 nil,没有任何离线世界能把一个真天赋句柄放到队头。
   **好在阻塞本身与「哪一条」无关**,是从源码证出来的。
2. **一条相关性,不是机制**:六个学到的天赋**全部是通用天赋**
   (`special_bonus_h_p200` / `h_p300` / `movement_speed20` / `m_p_regen150` / `m_p_regen175`),
   **全帧零个 hero-unique 天赋**。Axe 是这里唯一一个自己章程注释写明 t10 取的是
   hero-unique 的英雄,而他正是零天赋的四个之一。**这对配对 n = 1**,
   是给接修复那一棒的人的**线索**,不是发现。第一个出现的 unique 天赋会让这条断言变红。
3. **一局、一瞬、十个 slot** —— #366 的 LIMIT 原样继承,任何**频率形状**的说法都不成立。
   **不是频率形状的那部分,也正是本轮不等第二局的理由**:§2 的两条是从**源码**读的,
   源码放在第 15 格的那堵墙,不管有没有第二份 dump 都在每一局里。
   帧的职责是**让人看见撞墙**,不是估计撞的频率。

## 6. 变异台:12 个,全部一次见红且只红在该红的节

**先存副本、从副本恢复,不从 git 恢复。**

| # | 变异 | 见红的节 |
|---|---|---|
| M1 | 帧里 Axe `counter_helix` 4→2 | §2 和 §2 多重集 |
| M2 | 给 zuus 加第二个天赋 | §3(并连带 §4/§7 的双边计数) |
| M3 | 把一个通用天赋改名成 `..._unique_...` | §4 |
| M4 | rank-1 的 innate 改成 rank 2 | §2 的 `rest_max` |
| M5 | 抹掉一个 hero 单位 | §1 + 三个计数节 |
| M6 | 删掉 `botLevel > 25` 守卫 | §6 守卫断言 |
| M7 | **把 `table.remove` 挪到守卫的 `end` 之外**(负控制) | §6 **顺序**断言,报的是顺序那句话 |
| M8 | `aba_skill` 的 `i >= 10` → `i >= 12` | §5 天赋位置 |
| M9 | dumper `m_iLevel` → `m_iCachedLevel` | §7 |
| M10 | dumper 的 `resolveAbilities` 里长出一个 `map[]` | §7 缓存断言 |
| M11 | **读数器**只保留前 0 名(空集伪装) | §2 三个节点 |
| M12 | **谓词清空**:`is_talent` 恒 false | §3/§4/§7 三个节点 |

M11 / M12 是专门为「**空谓词的 0 和空语料的 0 是同一个整数**」那条一周内复发五次的
教训造的:**每一条经过天赋计数的读数都同时双边断言天赋面是满的**(`== 6`、`== 1`、
`6 with / 4 without`),所以读数器自己坏掉不会长成一个发现。
还原后 5 个文件 `cmp` **逐字节相同**,`tests/fixtures/`(109)与 `tests/frames/` 零残留。

## 7. 门

- **开工自检**:第一次调用被脚本自己 **REFUSED(exit 2,我把 stdout 管给了 `tail`)——
  那次什么都没检查,不是通过**(脚本指出这是 evidence-discipline 第 3 条**第 6 次**复发)。
  重跑 **exit 3**,findings = `cadence` / `queue-rulings` / `trunk-red(python)`;
  那条 python 红是 `tests/test_rc_wrapper.py`,**已由 GH #364 立案为 flaky**,
  **本 diff 零行 python**。另报两条 ORPHAN_PROPOSAL(§CW `roamidle` / §CX `outlatch`),
  **都是协同组的**,本轮不代办,记录并指名。
- **静态**:`bash tools/agent/luacheck_gate.sh` → **exit 0 / `luacheck bots game` 0 warnings**
  (裸读,**没用 `RULE6_BYPASS`**;冷启自己装了 lua-check)。push 门已上膛
  (`arm_push_gate.sh`:`core.hooksPath=.githooks`)。
- **动态**:**子集不是全套**(GH #124,全套 ~100min 在容器里跑不完一进程)。
  逐个跑过:`skill_point_stall` 8/0、`axe_t15` 21/0、`skill_list` 9/0、`gate_claim` 10/0、
  `smoke` 3/0、`fixture` 93/0、`corpus_existence` 4/0、`nil_head` 9/0 —— 全部 exit 0。

## 8. 下一棒(铁律 9:同一工作单元内显式交出去)

1. **修复是一个独立的、带 gate 的工作单元,而且它不在焦点五英雄的文件里** ——
   落点是 `bots/ability_item_usage_generic.lua`,**127 个英雄全跑**。
   本轮**没有动手修**:同一工作单元里既付诊断又改所有英雄的加点,正是 `AGENTS.md`
   那条 lanefix 教训(locally-correct ≠ emergently-good)的形状。
   ⇒ 已开 **[bug] issue**(队头阻塞 + 18/19 格的第二堵墙),球交给**总监**分派。
2. **#366 的第 2 条验收已付**(分叉判掉 = (a)),**追评本轮发表**;
   #366 请求的那份**第二局语料**仍然有价值(把 §1 从「一局十个 slot」变成频率),
   但**已经不是修复的前置**了 —— 前置在源码里,已经证完。
3. `-43a` 的 **Zeus 方向仍欠**(本轮沿 Zeus 走了一段:`abilityASBonus` 的两个消费方都已
   定价完毕,`X.ConsiderW` 的那一支因 `GetAbilityDamage()` 恒零而**健康无关地全死**,
   已由 `test_zuus_static_field_second_consumer.lua` 钉住;**没有新增读数**)。
4. GH `#357` 的「把帧搬进 `tests/fixtures/`」仍然解锁着,代价是剩下的 5 条记账行。
5. owner 四条优先项**仍无一条球在本组**(逐条核过:常设运维「发波 Spot 优先」=**批测台**;
   P1 拉野 / P2 低血不回家 = **协同组**;P3 cap 10→25 = **总监**)。

## Token 用量

```
TOKENS total_in=9,269,821 out=78,473 turns=65
```
(统计时刻之后的收尾回合不计入。)
