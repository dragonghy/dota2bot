# 英雄组工作单元报告 — 2026-08-19T01:51:21Z

## 范围

Backlog #6(接 #1):给 `wkreincarnmp`(WK Reincarnation mana-gate 修复,
`J.IsWkReincarnationArmed`,`bots/FunLib/jmz_func.lua:7504`)补真实帧 fixture
验证。本组自身无本地录像归档/AWS 访问,委托 `replay-analyst` 子代理代为
在最近批测归档里挖帧(见 hero.md 里"可能需要请 replay-analyst 代找帧"的
预案)。

## 找到的真实帧

`replay-analyst` 在较早的归档(`s3://dota2bot-batch-results-4924/soak/
spot_20260725_102532_1_main/20260725_105305_slot1.dem`,先于 `wkreincarnmp`
gate 存在,不影响——挖的是地面真相状态,不是测 armed 行为)里找到一局
WK 在团战中的干净样本:

- t=373.5(6:13),WK 英雄等级 6,Reincarnation 技能等级 1,冷却 0(就绪)。
- **mp = 189/387** —— 正好落在旧硬编码阈值 160 与真实 1 级耗蓝 220
  (Liquipedia 2026-08)之间的 gap 里:旧逻辑读"已备好"(误判),真实
  逻辑读"未备好"。
- Ground truth:该 WK 在 t=386.8(13.3s 后)阵亡,Reincarnation 确实触发
  复活(t=390.5 满状态复活)。死亡前后的 mp 快照(386→167,花费约 219)
  **实测印证了约 220 的真实耗蓝**,与修复 docstring 引用的数字吻合。
  本局里 WK 蓝量在真死前已回涨超过 220,所以这一局的复活本身是"真备好"
  的正确触发;但 t=373.5 这一帧如果被 `mode_retreat_generic` 评估,旧逻辑
  会在蓝量真正够用前 13 秒就误判"有安全网",这正是修复要堵的窗口。

Fixture:`tests/fixtures/f_260725_105305_wk_reincarn_gap.lua`
(`make_fixture.py` 产出,10 单位,burst={warlock:639, bristleback:378},
died_after=13.3)。

## 新增测试

`tests/test_replay_260725_wk_reincarn_gap.lua`(真实帧,`mock/replay_fixture.lua`
加载,真实 `J.IsWkReincarnationArmed` 在真实帧状态上跑,无 J.* 桩):
- ground truth 断言(帧数值 + died_after 与 fixture 一致);
- gate OFF(非 turbo / turbo 但未 arm)→ 189>=160 读 ARMED(与线上字节级
  不变,即使这是"错"的读数);
- gate ON(turbo + `wkreincarnmp`,真实耗蓝 220)→ 正确读 **NOT armed**。

fixture 本身不含技能法力消耗字段(`make_fixture.py` 只钉 level/cd),按
`hero_skeleton_king.lua` 里 `X.ShouldSaveMana` 的既有用法,在测试里显式
`GetManaCost=220` 补上。

## 验证

- `luacheck bots game --formatter plain`:0 警告。
- `lua5.1 tests/run_tests.lua`:**363/363 绿**(新增 4 条真实帧测试全过;
  容器全新,先装了 lua5.1 + luarocks + luacheck)。

## 状态判定(验证哲学三条件,对照 README 铁律2)

- (a) 录像组"改动在实际对局中真的执行且行为正确" —— 本轮**首次拿到真实帧
  证据**验证 `IsWkReincarnationArmed` 的判定逻辑(此前只有 mock 单测)。
  真实帧下 gate ON 给出正确读数,gate OFF 字节级不变,达成 (a)。
- (b) 批测无明显负面 —— **尚未测**。`wkreincarnmp` 目前不在
  `iterations/streams/test_set.md` 的 14-id 现行测试集里,也不在本轮
  batch-desk 已启动的波次里(那波跑的是 commit 96f49dc 的既有 14-id
  组合,不含 wkreincarnmp)。
- (c) 逻辑依据 —— 已有(Liquipedia 耗蓝数字 + 本局死亡帧蓝量花费实测
  印证)。

**未 promote,仍是 gated 状态。** (a)(c) 已满足,(b) 待办 ——
建议下一次 director 触发时把 `wkreincarnmp` 作为 test_set.md 的新提议
一并考虑纳入下一轮波次(本组按章程无权自行改 test_set.md,只在报告里
提出;director 章程里"测试集变更提议审批"读的就是本报告)。

## 涉及路径

- `bots/FunLib/jmz_func.lua`(`J.IsWkReincarnationArmed`,未改,#1 已完成)
- `tests/fixtures/f_260725_105305_wk_reincarn_gap.lua`(新增,replay-analyst 产出)
- `tests/test_replay_260725_wk_reincarn_gap.lua`(新增)
- `iterations/streams/hero.md`(本次更新"当前状态"+ backlog)
