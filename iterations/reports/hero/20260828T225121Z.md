# 英雄组 2026-08-28T22:51Z —— 两个 lever 缺的是同一批帧,而那批帧的「没有」是窗口不是世界

**轴**:章程 backlog **-39**(明写「下一棒做这条」)。
**产出**:`tests/test_wk_level_supply_horizon.lua`(新,5 节)+ queue `hero-10` 申请方自改(合并两个 lever 的语料需求,第一次带分母)+ `state.json` 记一条 `wksupply_LEVEL_HORIZON_20260828`(`gated:false`)。
**`bots/` / `game/` 零行改动;无新 gate id;`wkrosh` / `wkbuild` 的门与 armed 状态一个字没动;零 AWS;不申请入集;不开新 issue;不加波次、不涨优先级。**

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` → **worst exit 3**,legs run **8**,
`FINDINGS: cadence stale-waits trunk-red(python)`,`UNCERTIFIABLE: none`。
- stable-v1 / stable-v2 锚点各三项 ok。
- `STALE strategy.md:2569`(等 `campexit` 重裁,而裁定已落地)—— **协同组的**,不是本组。
- trunk python **50 passed / 1 failed**,失败项 `tests/test_stale_waits.py`,
  它红的**原因就是上面那条 strategy.md:2569** ⇒ **本轮开工前就红,与本轮改动无关**;
  收尾复跑仍是 **50/1,同一条**。本组不代改别组章程(§AW.1 纪律),
  **记录并指名:这一条在协同组名下**。
- owner 四条优先项**没有一条球在本组**(常设运维球在批测台,P1/P2 球在协同组,P3 球在总监)
  ⇒ 按章程走 backlog 顶端,而 -39 明写「下一棒做这条」。

---

## 1. 立案:-37 和 -38 撞的是同一堵墙,而两边把它写成了两种毛病

- **`wkrosh`**(`X.GetRoshanManaFloor`):撑着它的 **24/31 帧全在 ≤12 级** ——
  **最不可能打肉山的等级带**(报告 `20260828T165744Z.md`)。
- **`wkbuild`**(GH #17,同一个英雄文件):本周量到条件 (c) **有寿命** ——
  t20 bypass 一旦 trained,`test_wk_bone_guard_thresholds.lua` §4 读出的每个 threshold
  **全塌到 0**(最响一格 rank-4 lane **8 → 0**)⇒ **(c) 在 12-19 级成立、20 级作废**,
  而买下它的帧**同样全在 ≤12**(报告 `20260828T195320Z.md`)。

**两条要的是同一批帧**:同一个英雄、同一段等级、同一份归档语料。-39 的指令是
**并成一条**,域仍是 queue `hero-10`。本轮照做,并且**第一次给这条墙量了分母**。

---

## 2. ⭐ 量出来的东西,和两边散文说的都不一样

`tests/test_wk_level_supply_horizon.lua` 逐条断言(零 AWS,只读已有语料):

| 读数 | 值 |
|---|---|
| `tests/fixtures/` | **107 帧 / 77 局 / 1070 个英雄-位** |
| 语料**最晚一帧** | **t = 790.4s = 13:10** |
| Wraith King 占其中 | **36 个位**,高水位 **12 级**,**≥13 级 0 个** |
| **全仓** 12 级以上的 WK 位 | **恰好 1 个**,是 `iterations/pending/tpgap_159_fixture/` 那枚(t=1382.2 = 23:02,WK **26 级**,该帧 **10/10 个位 ≥20 级**) |

### ⭐⭐ 主产出一:这个零**不是**「turbo 到不了」

同一份语料里 **≥13 级的位有 48 个、分布在 22 局,高水位 19 级**(一个 Viper)。
**语料到得了 13+,只是 Wraith King 的帧全是早期切的。**

这一条改的是**该向谁提问**:两个 lever 都是**条件在这个英雄身上**的,
所以 **GH #84 的全局等级曲线回答不了它们**。上一轮的散文写成「我们的证据都 ≤12」,
听起来像英雄升级慢;量出来是**语料构成的事实**,不是游戏的事实。
新文件 §4 把这个对照做成机器断言,就是为了让 §1 的零**永远不能**被读成「turbo 没这一段」。

### ⭐⭐ 主产出二:790.4s 不是 turbo 局的长度,是**旧 10 分钟上限的影子**

批测 harness 在 P3 / GH #108 之前把每局在 10 分钟经济上限自杀,
所以**我们切的帧**在 13:10 就没了 —— 而**游戏没有**。抬上限后取的第一枚帧
就是那枚 parked 的,**24.9 分钟自然结束的局,十个英雄 22–27 级**。

⇒ **本条要扫的是归档 timeline 里 13:10 之后的段,不是再扫一遍 fixture。**
这句话已经照抄进 `hero-10` 的更新块,免得执行方拿 fixture 代答。
(同一处误读本周已被 `test_focus_talent_anchor.lua` 与
`test_focus_build_level_legality.lua` 各自退休过一次 —— 本轮复核:两个文件**都已自行吸收**,
本组在那一侧**不欠债**,不必动它们一行。)

### ⭐ 一枚帧,两个 lever,**答案方向相反**

那枚 26 级的 WK:
- 对 `wkrosh`:**26 过了全部三条 crossing level 21/19/18** ⇒ 域**存在**;
- 对 `wkbuild`:**26 在条件 (c) 的寿命(12-19)之外** ⇒ (c) 在那儿**作废**。

§2 把这两条**同时**断言在同一枚帧上。这也是「并成一条」在技术上成立的理由:
**一次扫描、同一批位、多读两个字段**,边际成本 ≈ 0。

---

## 3. 落地了什么

### 3.1 `tests/test_wk_level_supply_horizon.lua`(新,5 节,182 个 `wk_` 用例里的 5 个)

1. **供给零**,在唯一能变红的形式里:分母 `cs.ratchet`(36,可增不可减),
   **零留成硬等式** —— 语料里哪天出现一个 13 级的 WK,它**必须**红,
   因为那正是 `hero-10` 要买的帧。
2. **全仓 12 级以上的 WK 位恰好 1 个,且 glob 看不见它**。
   §1 没有 §2 就是**把地平线写成事实**。
3. **时间地平线**:`cs.ceiling(fx_max_t, 900)` —— 后期帧一落进 `tests/fixtures/`,
   这个文件就该退休,而它会**自己说出来**。
4. **48 个位的对照**(见 §2)。
5. **消费者还在**:`wkrosh` / `wkbuild` 两个门 + `X.GetRoshanManaFloor` 都还在出货源码里且仍 gated
   —— 申请**不许活得比它的消费者久**。

语料枚举用 **-38 立的「存在性 + basename 双保险」**:GH #236 落地那天,
不管是 move 还是 copy,那枚帧**只读一次**(读两次会虚增 WK 分母,而分母是 §1-§3 的地基)。

### 3.2 queue `hero-10` 申请方自改(**不动裁定 / 路由 / status / priority**)

沿 `hero-21` 的先例(申请方给**自己**的条目追加带日期的更新块)。两处都改了,
**是故意的**:`question` 承载论证与分母,而**执行方读的是 `acceptance`** ——
只写进 `question` 就是铁律 9 说的掉棒(裁定既已作出又「未送达」的那个形状)。

- `question`:合并论证 + 上面那张分母表 + 「按英雄条件化,别用全局曲线代答」+
  「扫 13:10 之后的归档段,不是再扫 fixture」+ 口径两条照旧
  (第 13 条世界断言不变 ⇒ (3) 仍是位置性代理;(3) 读到零仍**不是**域为空的证据)。
- `acceptance`:自行追加**第 (4) 项读数** —— 同一批 WK 位再读两个数:
  **13..19 级**的位数与 **≥20 级**的位数,**各带分母**。
  开工前预登记:**13..19 那一格的占比就是 (c) 还值多少钱,≥20 那一格是它作废的那部分**;
  **两格都要,不许只报一格**;按铁律 4(ii) **不报中位数**(整数小值域)报均值 + 分布或阈值占比;
  分层可得时按铁律 4(i) **ab / ba 两层各给一次**。
  并且预登记:**(4) 读到 ≥20 为 0 不是 (c) 永久成立的证据** —— 先问是不是窗口地平线。

`status=pending`、`priority=3`、`director.ruling=ROUTED_ARCHIVE_SCAN` **一个字没动**;
零 EC2、零新波次。

### 3.3 `iterations/state.json`

新增 `wksupply_LEVEL_HORIZON_20260828`(`gated:false`,带 `measured` / `the_finding` /
`baton` / `honest_bounds` / `mutation_testing`)。

---

## 4. 变异测试(4 个:3 抓 1 对照)

| # | 变异 | 结果 |
|---|---|---|
| M1 | glob 里一个 WK 位抬到 13 级 | **抓到**,§1 先红(§2、§4 同时红)—— 正是「帧到货了」该有的形状 |
| M2 | parked 帧路径打断 | **抓到**,§2(证人没了)+ §3(t=0)双红 |
| M3 | 出货源码里 `wkrosh` gate id 改名 | **抓到**,§5 红 |
| M4 | **对照**:测试文件里一行注释 | **逃逸**(0 failures),符合预期 |

纪律照 -36/-37/-38:每个变异**先 `diff` 确认不是 no-op**,
**还原一律用备份文件、不用 `git checkout`**;事后 `bots/` 与 `tests/fixtures/` **字节干净**。

---

## 5. ⚠️ 顺手看到、**没有立案**的一条(交出去,不占本组)

`tests/corpus_scale.lua` 自己写明:**零形状的断言**(点名 `ge20 == 0`)
**不softening,就是要在语料长出反例的那一刻变红** —— 那是它存在的全部意义。
而 `test_level_gate_census.lua:514` 的 `ge20 == 0` 正是这样一道**设计成大声失败**的绊线。

**它绊不到**:反例(**10/10 个位 ≥20 级**)**从 2026-08-26 起就在树里**,
停在它枚举的那句 `ls tests/fixtures` **外面一个目录**。
⇒ 一道**为了失败而写**的保险,结构上**保证沉默**。

**本组不开新 issue**:这两个文件按 -38 的结论**全在 harness/director 名下(GH #236)**,
机制也已在 **GH #281**(53 个测试文件共用那句 glob)里立过案。
这里**只记一笔可引用的读数**给那两条,不重复立案、不制造噪音。

---

## 6. 诚实边界

- **一枚帧**。它杀得死一个全称命题,**它不是分布**:本轮没有任何一句说
  turbo 有多大比例走到 20 级,也没有一句说 18 级的 WK 带着 600 蓝站在肉山坑里的频率
  —— **那正是 `hero-10` 要买的数,本轮买不到**。本轮能做的只是**不让缺的那些被渲染成量到的零**。
- **WK 的那个零是「我们冻结的帧」上的零,不是「我们打的局」上的零**。§4 的对照就是为了让这个区分一直可见;
  它**不是**「Wraith King 升级比同局慢」的证据(顺手量过:36 个 WK 位里只有 2 个高于本帧中位数,
  但 fixture 是**为了某个决策瞬间**手切的,这个偏差**不能**当行为读数用,故不入断言)。
- 第 13 条世界断言不变:`GetActiveMode` 不出现在任何 .dem ⇒ 即使扫完,
  `wkrosh` 的肉山那一半**仍是位置性代理**。申请不许被读成买到了比这更多的东西。
- **不撤销 `wkrosh`,不撤销 `wkbuild`,不提请重审,不提请入集。** 本轮没有任何一句改变两条判词。

---

## 7. 验证(铁律 6)

- `bash tools/agent/luacheck_gate.sh` → **`luacheck bots game: 0 warnings`,exit 0**
  (容器本来没有 luacheck,gate 自己装的;apt 包名 `lua-check`)。
- `bash tools/agent/arm_push_gate.sh` → `core.hooksPath=.githooks`(已上膛)。
- Lua filter:`wk_` **182/0**(新文件 +5)、`level_premise` **5/0**、`corpus_scale` **8/0**、
  `corpus_existence` **4/0**、`level_gate_census` **15/0**、`focus_talent_anchor` **22/0**、
  `focus_build_level` **10/0**、`smoke_load` **3/0**。
- `bash tests/run_py_tests.sh` → **50 passed / 1 failed**,失败项 `tests/test_stale_waits.py`,
  **开工前即红、根因在 `strategy.md:2569`(协同组)**,与本轮无关(§0)。
- **全量套件单进程跑不完(GH #124),本轮未跑全量 —— 是没跑,不是跑绿了。**
- **未用 `RULE6_BYPASS`。**

### ⚠️ 过程中被自己的检测器抓了一次,值得记

第一次跑 `level_premise` 时,**新文件被 `test_level_premise_registry.lua` §2 判为
「argues from the GH #84 level ceiling」(6 处)** —— 而这个文件的全部内容
**是在反驳那个天花板**。检测器是**词法**的,分不清「靠着它论证」和「引用它来打掉它」,
而一个论证「天花板是 harness 产物」的文件**必然**要印出天花板自己的话。

**处理方式是照它的规矩来,不是给自己开豁免**:在每处引用 14 行窗口内点名
**GH #235 / 2026-08-27**,并**明写**这些数字是**反证不是援引**。
**没有**往 registry 的 PENDING 里加行 —— 加行等于承认欠一笔本组不欠的债,
会把 `CEILING` 从 2 顶回去,而 -38 刚把它降到 2 并结清本组这一侧。
6 处 → 2 处 → **0**,`level_premise` 5/0 复绿。

---

## 8. 下一棒

**backlog -40**:等 `hero-10` 的读数回来。**在此之前不要为这两个 lever 再开语料请求**
—— 本轮已经把它们并成一条,再开一条就是把刚合并的东西拆回去。
可做的替代工作单元(不依赖那批帧):焦点五里 **Zeus / Lion / Crystal Maiden** 三个
**从未被本组逐帧看过的**方向,从归档语料挑一局逐帧找个体问题
(章程工作流第 1 条的兜底路径)。

---

## 9. Token 用量(铁律 8)

```
TOKENS total_in=7,909,457 out=52,071 turns=61
```

(到统计时刻为止;统计后的收尾回合不计入。)
