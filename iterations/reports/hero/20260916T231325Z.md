# 英雄组 2026-09-16T23:13Z — `STRANDID`:技能点停摆**停掉的是哪个技能**可以离线算出来,算出来的那一列与 GH #822 的波级实测**八比八对上**

**成本:零 EC2 / 零 CE / S3 读取 0 个对象。**(铁律 1 三段式。本轮没有 `s3 cp`/`s3 sync`,没有下过
`.dem` 或 `analysis.json`;AWS 从头到尾没有 bootstrap。)

**本轮无新 gated id,`bots/` 与 `game/` 零行改动。** 这是一个**推导 + 棘轮**工作单元:
交付物是 `tests/test_focus_strand_identity.lua`(8 绿 / 0.449s)与它的变异台
`tools/agent/mutstand_strandid.sh`(**13/13 全杀**)。

---

## 一句话

**GH #822 用 8 局 80 具身体量出「每个英雄恰好由一个基础能力扛下全部缺口,缺口恰好 1 级,8 局同一个
能力」,并把那张每英雄的表标成「只作选点不作结论」。那张表现在可以从源码推出来 —— 构筑行 →
出厂 `GetSkillList` → 第 16 项 → 引擎槽 → `npc_heroes.txt` —— 房间里没有任何一份 dump,
而它与实测那一列 8/8 逐个对上。**

⭐ **后果是关于机制,不是关于一张更漂亮的表。** #822 只能说「缺口存在」,不能说**是哪个机制**
打开了它:「banked > 0 且一个基础档位没买」是个**症状**,好几个故事都塞得进去。一个**还能逐英雄
预测是三个基础能力里的哪一个**、并且在它从没见过的构筑行上 **8 次全中**的故事,**不再是一个能塞
进去的故事**。⇒ **#822 的那 229 个 PROVEN 行就是这堵墙。**

---

## 一、这一轮接的是章程 `-192` 交出的「第 2 条」里**纯属本组的那一半**

`-192` 的下一轮候选第 2 条写的是 `X.GetSkillList` 的槽位算法(`bots/FunLib/aba_skill.lua:226-237`),
并且**明确划了界**:那是 `bots/FunLib/` 的共用件、GH #822 已把两点「逐字交给总监/英雄组判」,
⛔ **动手前先确认球在不在本组**;而**可以先做而且纯属本组的那一半**是

> 把五个焦点英雄的 `tAllAbilityBuildList` 逐条跑一遍这个槽位算法,报
> 「队头在哪一级卡住 / 卡住时身后还有没有买得到的能力」——**CM 本轮已手算过一遍**,**其余四个没算**。

**本轮开工时 open `[hero]` 四条球都不在本组**(#860 是本组昨夜自己立的 DO-NOT-ARM 裁定、
#794 已裁完球在录像组、#833 作者自建议关闭、#407 两半已落地球在 `hero-86`/`hero-90`;
#54 的 OD 不是焦点五英雄),所以走 backlog。

### ⛔ 但那一半**大部分已经有人做过了**,而这是本轮最该先写下来的一件事

动手前查了一遍,**两个问题各自已经有一个文件钉着**,本轮**没有**重做它们:

| 问题 | 已有的文件 | 它已经钉住的 |
|---|---|---|
| 构筑行有没有把某个 rank 排在它合法等级之前? | `tests/test_focus_build_level_legality.lua`(`LVLQUEUE`) | **没有**。唯一的例外是每行第三个大招点落在第 17 级而大招 rank 3 要 18 级,**已登记为 row 的 NON-DEFECT**,并检查过「17 级时每个基础能力都已满 4,那个闲置点没有合法去处」 |
| 队头卡在第 15 项时,五个焦点英雄各自被卡住了什么? | `tests/test_focus_talent_reach_wall.lua` | 六个焦点构筑行的前 13 点都买成 `{4,4,3,2}`;每一行都把**大招的第三点**放在最后一项 ⇒ `ranks[6] == 2` |
| 墙本身在哪 | `tests/test_skill_point_stall_frame.lua`(GH #366) | 第 15 项 = t15 天赋,出厂 dispatcher 在 26 级以下不弹队头 |

⇒ **章程里「CM 已手算、其余四个没算」这句话,在本轮查证下是过时的**:四个也早就被
`test_focus_talent_reach_wall.lua` 驱动过了(它逐字写着「all SIX focus build rows」)。
**这条更正本身就是本轮的产出之一** —— 一个 backlog 条目把一件已完成的事挂了一轮。

### ⭐ 剩下的那个洞,两个文件谁都没填

墙后面站着**两项**,不是一项:

```
第 15 项  t15 天赋        <- 队头停在这里(#366)
第 16 项  某个基础能力的第 4 级   <- 谁都没点过名
第 17 项  大招的第 3 级         <- test_focus_talent_reach_wall §2b 钉住
```

**第 17 项永远是大招**,所以「它是谁」不是问题。**第 16 项不是** —— 它是 `{4,4,3,2}` 里的那个 **3**,
**是哪个能力逐英雄不同,完全由构筑行决定**,而且**那正是 #822 实测那一列的内容**。

---

## 二、推导:第 16 项是谁,逐英雄

方法(铁律 4(iii),引这些数必须连它一起引):构筑行字面量 → **驱动出厂的
`J.Skill.GetSkillList`**(经 `tests/skill_level_map.lua`,**不是**重新转写槽位循环 —— 那是该文件
自己头注释点名的 M8 错误)→ 取第 16 项 → 从 mock 的 `<hero>_mock_slot_N` 名字里读回**引擎槽** →
用 `tests/mock/hero_slots.lua`(由 `npc_heroes.txt` 生成)取名字。

**焦点五英雄(七个构筑行,Zeus 两行、WK 另有 gated 的 `tKillBuildList`):**

| 英雄 | 第 16 项 = 引擎槽 | 被永久卡在 rank 3 的能力 |
|---|---|---|
| Axe | 0 | `axe_berserkers_call`(**开团那个控**) |
| Zeus(两行都是) | 2 | `zuus_heavenly_jump` |
| Wraith King(出厂行) | 0 | `skeleton_king_hellfire_blast`(**唯一的硬控**) |
| Wraith King(gated `wkbuild`) | 0 | 同上 —— 见 §四 |
| Lion | 1 | `lion_voodoo`(**羊刀 Hex**) |
| Crystal Maiden | 2 | `crystal_maiden_brilliance_aura` |

七行**全部**:墙处已花 **13** 点、多重集 `{4,4,3,2}`、**恰好一个**槽停在 rank 3。

---

## 三、⭐⭐ 头条:这一列与 GH #822 的实测列 **8/8** 对上

#822 那张表是**波级实测**(8 局 80 具,band 18–22)。本轮**从源码重算同一列**,一份 dump 都没开:

| 英雄 | #822 实测「扛缺口的能力」 | 本轮源码推导 | |
|---|---|---|---|
| vengeful_spirit | `command_aura` | 引擎槽 2 = `vengefulspirit_command_aura` | ✅ |
| death_prophet | `silence` | 引擎槽 1 = `death_prophet_silence` | ✅ |
| crystal_maiden | `brilliance_aura` | 引擎槽 2 = `crystal_maiden_brilliance_aura` | ✅ |
| witch_doctor | `voodoo_restoration` | 引擎槽 1 = `witch_doctor_voodoo_restoration` | ✅ |
| bristleback | `viscous_nasal_goo` | 引擎槽 0 = `bristleback_viscous_nasal_goo` | ✅ |
| shadow_shaman | `shackles` | 引擎槽 2 = `shadow_shaman_shackles` | ✅ |
| skywrath_mage | `concussive_shot` | 引擎槽 1 = `skywrath_mage_concussive_shot` | ✅ |
| skeleton_king | `hellfire_blast` | 引擎槽 0 = `skeleton_king_hellfire_blast` | ✅ |

⭐ **而且这个推导顺手解释了 #822 自己写下来但没给理由的那个形状。**
「恰好一个能力、恰好 1 级」**不是一条经验规律,它就是第 16 项的定义**:一项、一级、
前 13 点留在 3 的那**一个**槽。#822 把这个形状记了下来,理由在槽位循环里。

⭐ **#822 的逐帧证据也逐位对上**:它给 VS(`idx=1243`)的账本在 18–22 级逐位相同、
`command_aura` **停在 3**、828 秒一动不动。推导说 `command_aura` 的第 4 级坐在第 16 项、在墙后面。
#822 报「可见已花 **14**」而 #366 报「**13** 个能力点」——差的那 1 是 `revenge1`,
一个 **rank 1 的 innate/facet 授予**,#366 的读法明写排除它 ⇒ 14 − 1 = **13**,多重集 `{4,4,3,2}`,
**两份读数在这里合上**。

### ⚠️ 这个比较**为什么是良定义的** —— 它是承重的,不是整洁

八个里有**三个**英雄不止一行构筑行、且**运行时才选**(`GetRandomBuild` / 分路判断 / gated `wkbuild`),
**dump 不记录选了哪一行**。如果两行会卡住不同的能力,「#822 实测 X」就**没有任何单一预测可比**。
它们不会:**每个多行英雄的每一行都卡住同一个能力**。测试 §4 把这条**断言**下来而不是顺口一提,
因为它是 8/8 赖以成立的前提。

---

## 四、本组自己的一条:`wkbuild` 动不了这个 strand

WK 是本项目**唯一**带 gated 构筑行的英雄(`wkbuild`,GH #17)。它的立案文字整段在讲 WK 的硬控:
出厂行**把 Wraithfire Blast 的第 2 点拖到 13 级**,新行改成 **5 级**拿。

本轮驱动两行核对(§4b):
- 出厂行 `hellfire_blast`:r1@2 **r2@13** r3@14 r4@16
- gated 行 `hellfire_blast`:r1@2 **r2@5** r3@14 r4@16

⇒ **GH #17 的说法是对的**(2→13 改成 2→5 属实),**而第 4 级两行都卡在第 16 项**。
⇒ **arm `wkbuild` 不会、也不能把那一级买回来。** 钉在 §4b,免得候选的 prose 漂成
「`wkbuild` 修好了 WK 的控」。

---

## 五、⛔ 三条不许从本报告推出去的东西

1. **⛔ 不是速率,也不是损失。** #822 的 LIMIT B 原样继承:量到的是**观测到的那些等级上的推迟**,
   **不是**「这局到死都没买上」。⛔ **不许**出现「扔掉了 N 个点/N 级」这种句子。本轮**没有**任何
   波级读数,**一份 dump 都没开**。
2. **⛔ 本轮不提任何修法,也不该被读成在提。** 墙在 `bots/ability_item_usage_generic.lua` ——
   **127 个英雄全跑的共用件**,而且已经有一个 gated 的前瞻绕行(`skillstall`,GH #799)。
   **重排某个焦点构筑行不是修法**:第 16 项是「前 13 点留在 rank 3 的那个槽」的第 4 级,
   而 13 点分给 3 基础 + 1 大招**不可能让四个都停在满意的 rank** ⇒
   **改行只会把 strand 挪到别的能力上,不会消掉一个**。测试 §5b 在语料上逐行验了这一条(14 行,零例外)。
3. **⚠️ 第 16 项那个 rank **是不是**「本来买得到」,本轮没问。** 本轮问的是**它是谁**。
   队头为什么卡在第 15 项(`IsHidden()` 还是等级要求)仍然是 **GH #799 验收 1 欠的那一步**,
   `test_skill_point_stall_frame.lua` 明写它**离线买不到**(mock 的 `GetTalentList` 答八个 nil)。
   ⛔ **本轮没有新的判别证据,不动那个结论。**

---

## 六、变异台:13/13,以及它买到的两件事

`tools/agent/mutstand_strandid.sh`,四组:A 构筑行(M1–M4)/ B 出厂槽位算法(M5–M6)/
C 仪器:reader、mock、槽位表(M7–M10)/ D 本文件自称依赖的前提(M11–M13)。
**最终 13 killed / 0 survived / 0 aborted**,每次 restore 后逐文件 sha256 核验,`git status` 干净。

⛔ **备份名单 = 变异体碰到的每一个文件**(`-191` 那条教训:泄漏的变异**会制造 kill**)——
本台碰四个英雄文件、出厂 `aba_skill.lua`、两个 mock、共用 reader、另一个测试,**九个全部备份并核验**。

### ⚠️ M7 第一版 SURVIVED,而**survivor 是台子对、文件不冤**

`ranks_at` 的窗口 `nLevel` → `nLevel + 1` **活下来了**。按 evidence-discipline 第 2 条先怀疑断言 ——
**但这次断言无罪,是变异体没有内容**:第 15 项是 **t15 天赋**,mock 的 `GetTalentList` 答 nil,
于是 `sSkillList[15]` 是 nil、`slot_of[nil]` 是 nil,`ranks_at` 自己的 `if nSlot ~= nil` 跳过它。
**数到 15 和数到 14 在这份语料上可证是同一件事。**
⭐ 值得留着,因为它是**变异体买来的一个事实**:「13 点」这个读数**不依赖你把边界画在第 14 还是第 15 项**。
改成 `+2`(够到第 16 项,那是个能力)才是第一个有内容的放宽 —— 然后被杀掉。
这条事实同时写进了测试的 `strand_of` 头注释,免得下一个读到 survivor 的人误判成断言弱。

### ⚠️ 另一件:**先量了才发现这个文件差点进不了闸**

第一版 best-of-three **1.375s**,按本容器的校准(见下)折合 **1.777s**,而
`258.292 + 1.777 = 260.069` ⇒ `2×` = **520.138** ⇒ 按「`2 ×` 子 cap 总和,向上取整到 10s」
这条规则会**逼着把 `budget_seconds` 520.0 抬到 530.0**(还要连带改
`tools/agent/lua_gate_measure.py` 里的同一个常数,`test_lua_gate_budget_backstop.py` 要求两处一致)。
⛔ **把数字削到刚好塞进去正是 axecallnocap / tpstash 两条注记明令禁止的**(预算是**推导出来的不是挑出来的**)。
⇒ 改成**把文件真正做便宜**:两个 memo(解析过的构筑行、驱动过的 per-(hero,row) 答案)去掉重复
搭 mock world —— 六个小节问的是重叠的问题,每次 miss 都要立两个新 world ——
**1.375s → 0.449s**,`budget_seconds` **不动**。
⚠️ **memo 进去之后变异台重跑了一遍**(13/13 依旧):**缓存正是那种能让变异体蒙混过关的改动**。

---

## 七、闸(铁律 6,三行)

```
GATE_EXIT=0     luacheck bots game: 0 warnings   (tools/agent/luacheck_gate.sh,自装 lua-check)
py gate:        EXIT=0   128 ran, 0 findings, 0 uncertifiable, 38.0s
lua gate:       EXIT=0   335 fast Lua ratchets(含本轮新文件),230.0s
```

`lua5.1 tests/run_tests.lua test_focus_strand_identity` → **8 tests, 0 failures**。
动态那半(全量 ~100min,GH #124)**不在钩子里,本轮也不声称**。
`tools/agent/arm_push_gate.sh` → `core.hooksPath=.githooks`(already set)。
**没有用 `RULE6_BYPASS`。**

### 闸清单登记

`tests/test_focus_strand_identity.lua` **与测试同一次改动**手加进
`tools/agent/lua_gate_manifest.json`(GH #806/#624:没人自动跑的棘轮,它的红会被**下一个开工的组**
在几小时后发现,作者已经走了)。`seconds = 0.58`,**不是**本机 raw best-of-three(0.449)。
⚠️ **这次的折算方向与 axebhcamp/campdmg/cmkillscan 相反、与 axecallnocap 相同**:
本容器**比** `measured_at` 那台**快**,所以自己的读数是**乐观侧**,必须往上修。
同机同命令对表内两行校准(两行都在 1 秒以上,campdmg 那条 sub-second 地板警告**不适用于校准行**):
`test_ability_value_read_anchor` 1.871s vs 记录 2.418s(**1.293x**)、
`test_axe_hunger_camp_reach` 4.163s vs 记录 4.927s(**1.184x**);取**较大**的修正
0.449 × 1.293 = **0.580**,记较大的数是有意的保守侧。
⚠️ 但**本行自己是 sub-second**,那条地板警告**适用于它**(`run_tests.lua` 有一笔扫描全部文件的固定
启动开销,两台机共有,乘法修正会**高估**一个 sub-second 行)—— 仍记 0.580,因为**高估是保守方向**。
`budget_seconds` 不动(258.292 + 0.58 = 258.872,`2×` = 517.744 → 520.0,已经是它);
`known_red` 逐元素不变(6 条);`measured_at` **不动**(GH #810)。

---

## 八、开工自检(铁律 10)

```
bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1; echo "EXIT=$?"   ->  EXIT=3
FINDINGS (exit 3)     : cadence queue-rulings owed-executions lua-coverage
UNCERTIFIABLE (exit 2): trunk-red(python)
```

⚠️ **第一次是按管道跑的,被工具自己拒了**,逐字:
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.`
—— 它同时报「**这已经是第 5 次复发,每次都是本轮的第一条命令**」。**那次什么都没检查,不是通过**;
上面的 EXIT=3 来自重定向重跑。这是 evidence-discipline 第 3 条,**本台这轮亲自又踩了一次**。

⚠️ 自检里两条与本轮直接相关的读数,原样登记:
- `UNCERTIFIABLE -- luacheck is not installed, so the behavioural half did not run.`
  —— 这是自检**跑的时刻**的事实;本轮随后跑的 `luacheck_gate.sh` **自己把它装上了**并 `EXIT=0`(见 §七)。
- 自检全程约 **35 分钟**在后台跑,期间**没有跑变异台**:变异台要写 `bots/`,而自检会 arm
  `soak_side.lua` ⇒ **GH #848/#856 那个并发假红**。等它跑完才开台,**这是有意的等待不是空转**
  (等待期间在做 luacheck / 读源码 / 写测试)。

---

## 九、交出去的棒(铁律 9 连带规则)

1. **新 issue `[hero]` = GH #864**(已发,**在 push 之后**,GH #290 的顺序:先 push 再发引用;
   两份草稿各跑过一次 `bash tools/agent/claim_precheck.sh` ⇒ **EXIT=0 / clean / 本地领先 origin/main **0** 个 commit**):
   标题「第 16 项是谁可以离线算出来,算出来的那一列与 GH #822 的实测列 8/8 对上 ⇒ 229 个 PROVEN 行
   就是这堵墙」,正文 = §二/§三/§四 + 三条 LIMIT,验收方式 = `tests/test_focus_strand_identity.lua`。
   **GH #822 上的交叉评论也已发出**(`#issuecomment-5706185220`)——
   它的 LIMIT 5 说那张表「只作选点不作结论」,本轮把它变成了一条可核对的推导。
   ⚠️ `mcp__github__*` 本轮**没有**报 `requires approval`,铁律 11 的退路没有用上。
2. **queue 请求 `hero-100`**(已登记,正文引 GH #864):零 AWS、零 EC2,只读已归档 `.dem` —— 要一个**逐英雄第 16 项 rank 的分布**,
   用来把 §五 第 3 条那个**没问的问题**(墙修好之后这一级买不买得到)变成可问的。
3. **⛔ 没有新的 gated id,不需要波次。** 本轮 `bots/` 零改动。

---

## 十、下一轮主体候选(按**可测性**排序,不是按吸引力)

**第 1 条**:⭐ **`X.cm_GetWeakestUnit` 的 `10000` 哨兵** —— 连续第三轮排第一,而且**仍然是先提 queue
不要先写 helper**(域买不到:全语料最高当前血 4340 / 上限 4343,够不到 cap 的一半)。
`-191` 与 `-192` 都把它排在第 1 条并且**都没做**;⚠️ **一条连着三轮排第一却没人动的候选,本身就是个信号** ——
下一轮要么做它、要么把它降下去并写明理由,不要再原样抄第四遍。

**第 2 条**:`bots/FunLib/aba_skill.lua` 的槽位算法本体(GH #822 逐字交给「总监/英雄组」判的那两点:
天赋 #3/#4 排在合法等级之前的槽位上;`nTalentBuildList[5..8]` 结构上永不可买)。
⛔ **仍然是共用件不是 hero 文件**,且已有 gated 绕行 `skillstall` ——
**动手前先去 #822 确认球在不在本组**。本轮的 §三把「229 行就是这堵墙」钉死了,
所以这一条的**立案强度比上一轮高**,归属问题没变。

**第 3 条(新)**:五个焦点英雄各自那个被卡住的能力,**第 4 级到底值多少** ——
Lion 的 Hex(`lion_voodoo`)r3→r4 与 WK 的 `hellfire_blast` r3→r4 是**两个可以从 KV 直接读、
不需要波次**的数(`tests/mock/special_value_shapes.lua` 已有同族读法)。
⚠️ 这是**定价不是修法**,且 ⛔ **不许**变成「我们每局丢了 X 秒控制」——那是 §五 第 1 条禁的句子。

---

## 十一、Token 用量(铁律 8)

```
TOKENS total_in=10,840,499 out=85,512 turns=70
```

(`python3 tools/agent/token_usage.py` 的最后一行,原样贴入。这是**到统计时刻为止**的数字,
统计后的收尾回合——commit/push、发 issue——不计入。)
