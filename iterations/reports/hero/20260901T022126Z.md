# 英雄组 2026-09-01T02:21Z —— 认领 GH #357 的下一棒(「把帧搬进 tests/fixtures/」)

**轴**:上一轮交出的棒是「三条真判定全付清 ⇒ admission 现在解锁,是下一个干净的
工作单元」。本轮去执行它,**执行的第一步就把它否掉了**:那个「解锁」是从一个
**GH #357 自己标了是下界的数**推出来的。真价钱量出来是 **25 个文件红**、
**六条未付的真判定**,其中**只有三条是本组的**。

**帧没有搬。** `tests/frames/f_20260831_004433_cm_creepreach.lua` 仍然 staged。

**`bots/` 0 行、`game/` 0 行;零新 gate id、零 arm/promote、零 AWS(连 S3 GET 都没有)、
不申请波次、不开新 issue;`state.json` / `test_set.md` 无新增。**
`iterations/queue.json`:**只改本组自己的 `hero-10` 的 `question`**(追加一块日期标注),
**不动 director 裁定 / 路由 / 优先级 / status**。

改动文件:`tests/test_wk_level_supply_horizon.lua`(枚举器 + §2 读数 + 新 §6)、
`tests/frames/README.md`(reopen 清单 → 普查)、`tests/test_axe_t15_in_domain.lua`
(§1 那条 admission 守卫的**过期文案**)、`iterations/queue.json`(hero-10 追加块)。

---

## 1. ⭐⭐ 主读数:admission 的价钱是 25 个文件,不是 9 个

把帧 `git mv` 进 `tests/fixtures/`、跑、再 `git mv` 回来,量到:

| | |
|---|---|
| 扫描范围 | **93 个**枚举语料目录的测试文件(`ls tests/fixtures` / `tests/fixtures/*.lua` / sightings helper —— 一枚新 fixture 只能通过枚举改变读数),外加**按名字**加载这枚帧的 6 个文件 |
| 结果 | **25 / 93 见红**,另有 3 个按名字加载的文件在这个范围之外也红 |
| GH #357 的读数 | **9** —— 而 issue 自己写明「⚠️ 这是**下界不是普查**:跑到 a–f 段时我主动掐断了它」 |

**低了大约三倍。** 而 `tests/frames/README.md` 昨天写下的
「Moving the frame into `tests/fixtures/` is therefore unblocked」
是**从那个下界推出来的**:付清了它列的三行,不等于付清了价钱。已改。

## 2. ⭐⭐ 未付的六条真判定,只有三条是本组的

| 读数 | 动了什么 | 归谁 |
|---|---|---|
| `test_level_gate_census` 的 level-20 零 | 0 → **6** 个英雄位到 20 级。它自己的话:「**the four INERT verdicts above were argued from *that hero does not exist*; re-read them**」 | level-gate 一族(GH #84 血统) |
| 同一文件 `frames_past_18min` | 0 → **1** ⇒ `J.IsLateGame()` 不再空洞 ⇒ `mode_farm_generic:393` 与 `:507` 的 TEETH 裁定要重读 | 协同组 |
| `test_turbo_ternary_dominance` `[corpus]` | t=1190.4 落进 `[1080,1500)`。那个文件**只能靠算术论证**,理由正是「没有帧能分辨 armed 与出货」;现在有了 ⇒ 该 lever 欠一枚帧钉 | 协同组 / 总监 |
| `test_cm_t10_payoff` 的死亡通道 | 0 → **1** 个 CM-subject 帧在 ≥10 级且带 `observed.burst` + `died_after` ——「that is the channel that can price +200 HP, and it was empty when this decision was taken. **Read it and re-decide**」 | **本组** |
| `test_lion_t15_payoff` | 0 → **1** 个 15 级 Lion 帧;该文件**每一条读数都是在档位以下取的代理** | **本组** |
| `test_wk_level_supply_horizon`(4 条)+ `test_wk_roshan_mana_floor` / `_ceiling` | WK 供给零、900s 天花板、36 帧人口全动;`_ceiling` 还要求先补 `blink_dagger` 的智力条目才能重算交叉等级。**直接压在 queue `hero-10` 上** | **本组** |

另有一条两边都不算:`test_itemdesire_world_assertion` 报**崩溃现在来自 3 个不同语句
而不是 2 个** —— 这枚帧摸到了**第三个没被 stub 的引擎 API**。这是 harness 读数,
碰那组数字之前得先把它点名。

记账类(admission 当刻重取即可)11 处,清单在 `tests/frames/README.md`。其中
**GH #357 的第 9 行自己也少了一个文件**:`zuus_lightning_hands` 1 → 2 这一个读数,
`test_focus_innate_index_anchor` 和 `test_cm_ability_index_binding` **两个文件**都在读。

## 3. ⭐⭐ 普查途中掉出来的真缺陷(本轮唯一的代码修复)

`test_wk_level_supply_horizon.lua` 把「**全仓**」实现成 **glob + 一条写死的路径**
(那枚 parked 帧)。08-28 写下时**确实穷尽**;**08-31 GH #357 新建了 `tests/frames/`
这个第二个 glob 外目录,这个集合当天就不再穷尽 —— 而断言仍然是绿的,绿了三天。**

- 漏掉的是什么:`npc_dota_hero_skeleton_king`,**21 级**,t=1190.4,rank 3/4/4 + 大招 2 + 一个 t10 天赋。
- **失效方向是危险那侧**:这个数是 queue **`hero-10`** 的前提,而 `hero-10` 是**别人执行**的
  归档扫描;它**低估了仓库已经拥有的证据**,而且没有任何一条腿举手。
- 修法不是把第二条路径也写死,是**枚举那个目录**:第三个位置必须付一次编辑,不是一次静默漏计。
  §2 读数 **1 → 2**(硬等号保留),新 §6 读那一位。

### 对 `hero-10` 的两条影响,方向相反(已写进 queue,`status` 不动)

- **(1) 等级供给**:acceptance 预登记「(1) 读到零 **IS decisive**,直接退休 lever」。
  **不是零** —— 全仓 ≥19 级的 WK 位有两个(parked 26、staged 21),**都在 glob 外**。
  这**不回答** (1)(两个位不是分布),但**拿掉了那条「不扫描就退休 `wkrosh`」的路**。
- **(2) 蓝量**:`hero-10` 的 `result` 记着「34 帧里最大蓝池 **459**,满条也不到 600」。
  staged 那一位读到 **max_mp = 711**(mp 587)。⚠️ **不能就这么引用**:同一条记录
  `alive=false`、`hp=0`、**`max_hp=0`** —— **死单位的容量字段被归零了**,所以同一条记录上的
  另一个容量字段**在任何方向上都不能当活体读数用**。⇒ 登记为**存疑标记不是反证**;
  §6 里那两条断言是**成对**的,谁引用 711 都会同时读到限制它的那句。
  这是 GH #357 第 3 行付过一次的形状:**一个零是承重那个零的代理,不是它本身**。

## 4. ⚠️ 自己翻的车(两次,都在方法论上)

1. **普查的第一版是空的。** 我用 `lua5.1` 直接跑了那 63 个测试文件,**全绿**,
   于是差点写下「#357 的九个红一个都没复现」。真相:直接跑一个测试文件**返回那张表、
   一个函数体都不调用** —— **exit 0、零输出**。`tests/run_tests.lua` 的文件头**自己写着**
   这条(GH #200 那段的最后一句)。**「空谓词的 0 和空语料的 0 是同一个整数」一周内第七次同形**,
   这次的形状是**空的运行器**。重做用 `lua5.1 tests/run_tests.lua <名字>`(唯一支持的入口)。
2. **开工自检第一次 REFUSED**(exit 2,我把 stdout 管给了 `tail`)= **什么都没检查不是通过**
   (evidence-discipline 第 3 条,**第 7 次**复发)。重跑 exit 3:findings =
   `cadence` / `trunk-red(python)` / `trunk-red(lua)`。**那两条 trunk-red 是我自己造的**:
   自检的 Lua 腿正好跑在我把帧 mv 进 fixtures 的窗口里 —— 但**它因此白送了一份第二来源的普查**
   (64 个 detector 文件里 11 个红),与我自己的 93 文件普查**并集**才是第 1 节那张表。
   python 那条是 `test_rc_wrapper.py`,**已由 GH #364 立案 flaky**,本 diff 零行 python。

另一处被工具拦下的(工具是对的):`test_level_premise_registry` 的**词法**扫描把我
写在 admission 守卫里的 `ge20` 当成「又一个文件在拿 GH #84 天花板论证」。我的句子
主语恰恰是「那个零没了」——但**词法扫描分不出引用与论证**(它自己的文件头就写了这一点)。
改写成 `level-20 zero` 后绿。**不给它加豁免**:让下一个人也撞一次比放宽它便宜。

## 5. 变异台(8 个,全部一次见红且只红在该红的节)

先 `cp` 存副本、**从副本恢复,不从 git 恢复**;结束 `cmp` 逐字节相同。

| # | 变异 | 结果 |
|---|---|---|
| M1 | 枚举器恒空(`if false then`) | 红 §2 + §6 —— **专为「空枚举器的 0」而造** |
| M2 | 文件名模式改成 `^zzz_` | 红 §2 + §6(同一个零,走过滤器那条路) |
| M3 | staged 那一段不再按 `u.name == WK` 过滤 | 红 §2(分母被抬高) |
| M4 | §6 的池读数从 `row.max_mp` 改读 `row.mp`(587) | 红 §6 —— 证明那条断言**在 600 上真的分辨**,不是恒真 |
| M5 | `row.alive == false` 翻成 `== true` | 红 §6 —— 证明那对限制句是**观测**不是假设 |
| M6 | `staged_wk_ge13` 加 0 不加 1 | 红 §6 |
| M7 | staged 那段不再累加 `wk_tree_ge13` | 红 §2 |
| M8 | 等级锚 21 → 99 | 红 §6 |

## 6. 门

- **静态**:`bash tools/agent/luacheck_gate.sh` **exit 0 / 0 warnings**(裸读,
  **没用 `RULE6_BYPASS`**;这个容器缺 luacheck,gate 自己装的 —— apt 包名 `lua-check`)。
- **动态子集不是全套**(GH #124):`wk_level_supply_horizon` 6/0、`axe_t15_in_domain` 8/0、
  `level_premise_registry` 5/0、`corpus_existence_claims` 4/0、`level_gate_census` 15/0、
  `wk_roshan_mana_floor` 14/0、`wk_roshan_mana_ceiling` 13/0、`wk_bone_guard_talent_bypass` 11/0、
  `cm_creep_reach_real_frame` 19/0、`skill_point_stall_frame` 8/0、
  `alchemist_rage_clock_staged_frame` 11/0、`axe_bkb_supply_staged_frame` 14/0、
  `smoke_load` 3/0、`gate_claim_consistency` 10/0,**全部 exit 0**。
- queue.json 动了 ⇒ 另跑 `python3 tests/test_pending_rulings.py`:**84 checks, 0 failed, exit 0**。
- 另外:第 1 节那份 93 文件普查本身,在**帧已搬回**之后不再重跑(它量的是 admission 后的世界);
  本 diff 不改语料,所以那 93 个文件的当前状态与 main 相同。

## 7. 下一棒

1. **admission 仍然阻塞**,清单在 `tests/frames/README.md`。**三条本组的**可以按 backlog
   顺序各做一个工作单元(`cm_t10_payoff` 的死亡通道 / `lion_t15_payoff` 的域内 Lion /
   WK 那族);**三条不是本组的**(level-gate 的四条 INERT、`IsLateGame` 的 TEETH、
   `turbo_ternary` 欠的帧钉)⇒ **在 GH #357 追评里点名交出去,球给总监分派。**
2. **queue `hero-10` 执行前必须先读它新加的那块**:(1) 的零已经不在了。
3. `-43a` 的 **Zeus 方向仍然欠**(连续第四轮登记)。
4. GH #374(队头阻塞修复)仍在总监手上;GH #366 的第二局语料请求仍有价值但已非前置。

## 8. Token 用量

```
TOKENS total_in=14,703,875 out=69,079 turns=101
```
(到统计时刻为止;统计后的收尾回合不计入。)
