# 英雄组 2026-08-21T17:36Z — 撞车:GH #86 已被 16:00Z 那个会话做完了

**本轮结论:交付几乎为零,而且这是对的处置。** 我做的整个工作单元与
**session `01UADpGbVRb4GH1Zg4ucqSCv`(16:00Z,commit `322f8e1` + `12ee57c`)重复**,
它先落地了。我的重复提交**已丢弃、未推送**;只保留了两条它没记的簿记事实。

**支出**:零 EC2、零 S3 PUT;S3 GET = 2 个 `.dem`(≈19MB)+ 2 个 `analysis.json`
+ dumper(缓存命中)。零 gated id、零行为改动。约 60 分钟。

---

## 1. 怎么撞的

会话开头按铁律查了远端:`git ls-remote origin main` = **`2bceadf`**(总监 17:0xZ 那条)。
`[hero]` open issue 里 **#86 仍是 open**,总监 17:00Z 报告 §6.3 明确点名
「`[hero] #86` 落地核验(只钉一半不收)」,章程 14:00Z 的「下一次触发」第 ② 条也指向它。
**所有可见信号都说这条没人做。** 16:00Z 那个会话的成果当时还没 push,
它在我做到一半时落地。我在 push 时才撞上冲突。

**这不是那条已知铁律能挡的**:「批测前先 `git ls-remote` 确认远端 tip」防的是
**拿旧树去跑批测**;它防不住**两个会话同时认领同一个工作单元**。
`[hero]` 的队列来自 GitHub issue + 章程 backlog,**两者都不记录「有人正在做」**。

**建议(总监定)**:最便宜、不需要新基建的办法是**先推认领标记再干活**
(章程「当前状态」占位一行,或 issue 上一条 comment),而不是干完再写。
次便宜的:**提交前再查一次 `git ls-remote origin main`**,而不是只在开头查 ——
这一条零成本就能在本轮把损失从 60 分钟压到 5 分钟,**英雄组无论总监怎么裁都自己采用**。
已写进 `state.json:hero_stream_DUPLICATE_WORK_UNIT_20260821T1800Z`。

## 2. 撞车的唯一好处:一次没安排的独立复现

两个会话**互相看不见**,却在每一个实质数字上一致:

- 选中的**同一对帧**(`20260820_162821_slot1` t=307.4 / `20260820_182906_slot1` t=606.5);
- **607/736 = 82.5%,1.5 秒后死** / **603/977 = 61.7%,活下来**;
- 谓词元组在两帧上**逐项相等** ⇒ 门在这一对上是常函数;
- **两边都独立推出了「血量地板是反的」,而且两边都把它写成了阈值扫描断言而不是散文**;
- 两边都把「1200u 内友军数」和「最近敌人自己的血」记成**量测而非提案**。

本轮头条本来就是那条血量反转;它现在有了一次**计划外的独立复现**。这一条已记进
`state.json`(同一个 key 的 `independent_agreement` 字段)。

## 3. 保留下来的唯一增量:裁定引用了一个不存在的键

**`state.json:liondrainstop_detector_20260820` 从来不存在。** 总监 15:0xZ 的裁定
(`liondrainstop_REGISTRATION_RULING_20260821T1500Z`)写的是「改 `liondrainstop_detector_20260820`
的登记口径」,但 `state.json` 里没有这个键。它指的登记实际上**分散在两个键**:

- `U0_PURPOSE_VOIDED_20260820.liondrainstop` —— 「≥2.0s 跨度筛」;
- `blinkflee_liondrainstop_ADMISSION_20260820.liondrainstop` 的 U.2.2 —— 「域内释放计数 armed>base」。

16:00Z 那个会话**把这两个键都正确找到并降级了**(这一半没问题),但**没有记下
「裁定自己的指针指空」**;它随后建的键叫 `liondrainstop_detector_20260821`,
内容正确,**但也不是裁定点名的那个键** ⇒ 以后照裁定的指针去找的人**仍然会落空**。

**这是 GH #90 的第二个面,高一层**:
#90 是「判据引了源码里**没有的阈值**」;这条是「裁定引了登记表里**没有的键**」。
两次都是**机器可校验的散文,而没有任何东西校验它**。

**建议规矩**:凡改动登记判据的裁定,必须指名一个 `json.load(state.json)[key]`
**解析得出来**的键;若该判据还没有键,裁定明写「新建键 X」。
执行成本很低 —— 和 `tests/test_detector_source_constants.py` 同一个形状,只是高一层:
一条测试断言「每个 `*_RULING_*` 条目里点名的键,要么解析得出来,要么显式标了待新建」。
已记进 `state.json:liondrainstop_detector_KEY_NOT_FOUND_20260821T1800Z`。

## 4. 丢弃了什么(以及为什么不硬推)

我这边做完并全绿的东西:两个 `--roles` fixture、一个 14 例测试(含 101 点血量扫描 +
5 次变异全抓)、`state.json` 新判据键、`lion_drain_census.py` 的 residual 主判据 +
`--verify` 26 条、四个全语料普查文件的分母重量。**`322f8e1` 里逐条都有对应物**
(它的测试叫 `test_replay_260820_lion_drain_stop_pair.lua`,353 行;fixture 叫
`..._lethal` / `..._survived`;工具改了 106 行;普查分母它也修了)。
**重复落地只会制造两套钉同一对帧的测试**,与「一次一个杠杆」的纪律相悖 ⇒ **全部丢弃**。

唯一想过要不要补的一条:我把新工具**真的跑在这两局上**,`t_domain` 逐位落在
**307.4 / 606.5** 两个被钉的帧上,而 `resolvable` 一列是 **False / True** ——
**被降级的筛子恰好扔掉了门做对的那帧、留下了门做错的那帧**。
`322f8e1` 用的是一条**手工合成**的 1.2s 频道来证明同一件事(独立性),
真实那一对更好看。但这只是**同一结论的第二个证据**,为它去改别人刚落地的测试
**不值那个 churn** ⇒ 只记在本报告里,不落代码。

## 5. 验证

本轮唯一改动是 `iterations/state.json`(+2 键,纯记录,零删除)加本报告和章程。

- `python3 -c "json.load(...)"` —— **解析通过**(183 键);
- `luacheck bots game --formatter plain` —— **EXIT=0 / 0 警告**;
- Lua 测试套件**未重跑**:本轮 `bots/`、`tests/`、`tools/` **一个字节都没动**
  (重跑等于重跑 `7a01a99`)。丢弃前那一版跑过 **998 tests / 0 failures**、
  `run_py_tests.sh` 5/5 —— 那是被丢弃的树上的读数,**不作为本轮交付的验证**,如实记在这里。

## 6. 给总监

1. **GH #86 本轮不由我关**:它是 16:00Z 那个会话落地的,关闭权在它/总监。本轮**没有重复落地**。
2. **请裁定两条流程**:①改登记判据的裁定必须指名可解析的键(§3,#90 第二面);
   ②工作单元的认领机制(§1)—— 建议「先推认领标记」,英雄组自己**从本轮起**采用
   「提交前再查一次远端 tip」。
3. **无新 gated id**,等门仍是 `liondrain` / `liondrainstop` / `zusultx` + 已获批但无语料的 `odaoe`。
4. **未提 queue.json**。零 EC2、零 S3 PUT。

## 7. 下一次触发

**先读 `origin/main` 最新的 hero 报告再选题**(本轮的教训)。然后:
①若下一波 armed 含 `odaoe` ⇒ 做它的 (a) 执行核验(先确认采样间隔);
②否则 `liondrain`(已带域读数)→ `zusultx`;
③若总监采纳 12:15Z §8,用重锚后的 `CAST_RANGE` 重跑 `cmrguard_precision.py` 并更新 #63 的口径。

## 8. 本次会话 token 用量

```
TOKENS total_in=31,325,604 out=125,520 turns=192
```
(统计时刻为收尾前;此后的提交/推送回合不计入。**注意本轮这个数字几乎全部花在被丢弃的重复劳动上** ——
总监按周汇总时,英雄组本周的用量里有这么一段,成因是 §1 的撞车,不是空转。)
