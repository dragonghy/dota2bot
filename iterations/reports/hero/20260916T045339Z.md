# 英雄组 hero — 2026-09-16T04:53Z

**工作单元主体**:`axecallnocap`(Axe,turbo-only,gated,未 armed)——
`X.ConsiderQ` 带线 出货点的人数帽**该不该存在**。
**P4.4 自评:(i)** 一个 `bots/` 行为改动。
**成本:零 EC2 / 零 CE / S3 读取 0 个对象**(本轮只读本地语料,未下载任何 `.dem` / `analysis.json`)。

认领依据(铁律 9 → 章程 backlog):`iterations/OWNER_PRIORITIES.md` 四条优先项
(P1 球在协同组、P2 卡在 `wandlimbo_charge_instrument` 两端仪器、P3/P4 非本组)
均无属于本组的未完成项;open `[hero]` issue 中 #833 等一条机制裁读(总监)、
#820 是 `promote_atoms.json` 栏杆(总监),#805/#808/#811 的落地都已完成。
⇒ 取 backlog `-186` 自己写下的「下一轮主体第一候选」第 1 条。

---

## 1 线索:一条 **带验收物** 的欠条

`axecallcrowd` 的 ARMED 段逐字写着

> Raising the cap to 5 would delete the conjunct, and whether this branch
> should carry a crowd cap AT ALL is a second question

—— 而它**当轮就把验收物一起留下了**:`f_260828_124358_axe_cull_promise`
(t=1452.8,五人全活在 1600u 内)是「帽子取 4」与「没有帽子」**唯一分得开的一帧**。

⭐ **这是 `-185` 立下的那条(「欠条写成可执行的更难被忘掉」)的第三例,而这一次
写下的不是一条断言,是一个具名的帧。** `-185` 留下的是一条活着的断言,
`-184` 留下的是 header 里的一句点名;本轮拿到的是**一个可以立刻去读的坐标**,
于是「这条欠条值不值得接」在开工前五分钟就能判定,不需要先做一轮探索。

## 2 杠杆与出货形状

出厂支路(`bots/BotLib/hero_axe.lua`,`X.ConsiderQ`):

```
( IsPushing or IsDefending or IsFarming )
  and IsAllowedToSpam( bot, nManaCost )
  and bot:GetAttackTarget() ~= nil
  and <钟 axecallclock>
  and ( <人数帽 axecallcrowd>  or  <本 id axecallnocap> )
  and #hEnemyList == 0
→ #laneCreepList >= 4 → 嘲讽 laneCreepList[1]
```

落地:新 helper `X.axe_IsLanePushCrowdCapOff()`(turbo-only,armed 恒真 /
未 armed 恒假),出货点改成**两项析取**。
**闸关(或非 turbo)析取整体塌回 `X.axe_IsLanePushCrowdOpen( #hAllyList )`,
即 `#hAllyList <= 2` 逐字节不变。** 两个 helper 各持一个 gate、互不点名。

## 3 ⭐ 本轮头条:唯一还没被排除的那条理由,指向**反方向**

`axecallcrowd` 的 header 排除了「法力配给」(同 `if` 已有 `IsAllowedToSpam`)
和「安全项」(同 `if` 已有 `#hEnemyList == 0`,而且那才是直接量度)。
它**没有必要**排除第三条 —— 因为帽子取 4 时那条还有地方躲:

**冷却。** Berserker's Call 花一个真实冷却(`AbilityCooldown` **18/16/14/12**,
读自 `tests/mock/special_value_shapes.lua`;本文件自己的加点到英雄 7 级是 4 级
⇒ **12s**),所以在接战前几秒把它扔给一波兵是**真代价**,而
「五人抱团、视野无敌人」**貌似**正是接战前夜。⇒ 一个诚实的反对意见。

⛔ **但出厂帽子不是在配给这个冷却,它把配给方向搞反了**:
shipped 在 `<= 2` 时**开火** —— Axe 单人或带一个,**最容易被抓、因此最可能
需要 Call 来打架**的状态;而在四个队友时**拒绝** —— 全场**最安全**的状态。
**一个对五人成立的冷却论证,对一人成立得更强。**
⇒ 帽子无论是什么,它不是在保护冷却;而这条反对意见的存在本身
**不是**保留合取项的理由,是**重新排它的方向**的理由。

条件 (c) 的另一半照旧是 `docs/PROJECT.md` 自己的 Turbo 教条逐字
(「weaker towers, shorter games, grouped pushing pays off more」)+ 机制项
(Counter Helix 按**挨打次数**触发,队友在旁不摊薄它的触发率 ⇒ 队友数不是
这条支路的收益项)。

## 4 域:四个数,不许当成一个

| 层 | shipped | `axecallcrowd` armed | 本 id armed |
|---|---|---|---|
| 合取层(16 个 Axe-subject 瞬间) | **11** | **15** | **16** |

⇒ 对 shipped 移动 **5** 个瞬间;对 `axecallcrowd` 移动**恰好 1** 个,
即 `f_260828_124358_axe_cull_promise`。

⛔ **而那一帧只在合取层分得开,理由在同一个 `if` 上**:该瞬间 1600u 内有
**1 个**敌方英雄 ⇒ `#hEnemyList == 0` 无论本合取项答什么都拒掉支路。
⇒ **语料里有 0 帧是本 id 改变 Axe 行为的**;把那个「1」报成行为差
**就是凭空的执行核验**。`§3.2` 把那个 `enemy == 1` 钉成断言,并写明
「若它读到 0,那是 NEWS —— 语料终于有了支路层承载者,LIMIT 2 要**改写**不是放宽」。

**支路层 = 0,而理由是选样不是稀少**(`§6.1`):`#hEnemyList == 0` 在 16 个里
只成立 **1** 个 —— 这批帧是为研究 Call / Culling Blade **决策**采的,即**有敌人
在场**的时刻。⛔ 这一条与 `axecallcrowd` bound 3 **是同一条**,
**本轮不把它当成新证据再数一遍**。

**端到端 = 0 且今天不可能是别的数**:还需 `#laneCreepList >= 4`,
而语料一个非英雄单位都没有(GH #772 / `state.json:CORPUS_HAS_NO_NONHERO_UNITS_20260912`)。

## 5 方向

**WIDENING,而且是它上方两条腿的严格超集** —— armed 后该合取项是常真。
`§4.1` 在全部 16 个 Axe 瞬间上驱动;`§4.2` 再在 **0..6 全值域**上对
**shipped 与 `axecallcrowd` 两条腿各**驱动一遍(语料只能给出它恰好含有的计数)。
⇒ 负向读数只能读成「那些抱团带线嘲讽不值 Call 的冷却」,
**永远不能**读成「lever 吃掉了一次出厂释放」。

## 6 ⛔ 支配性:本 id 与 `axecallcrowd` 不得同波 arm,理由比「一次一根」更强

armed 后析取**在帽子被问到之前就短路为真** ⇒ **同 arm 的一波测的是本 id 一个,
而裁定表上会写两个名字**。`§7.4` 在全部 16 帧上把这条支配性驱动出来
(而不是写成散文),并在断言消息里写明「若它哪天不再支配,是这条『不得同 arm』
的指令过严了 —— 先重读 LIMIT 5 再改断言」。
也不得与 `axecallclock` 同波(同一个 `if` 的第三个合取项)。

## 7 本地验证

- `tests/test_axe_q_lane_push_nocap.lua` —— **22 例 / 0 失败**。
- `tools/agent/mutstand_axecallnocap.sh` —— **13 killed / 0 survived-or-wrong-message**。
  每个变异点都带 `||` 闸 + 锚点计数断言(GH #846)。
- 邻居未回归:`axe_q_lane_push_crowd` 20 绿、`axe_q_lane_push_clock` 14 绿、
  `axe_call_ring_anchor` 19 绿、`axe_hunger_camp_reach` 23 绿、
  `gate_claim_consistency` 16 绿、`smoke_load` 3 绿。

### 7.1 ⭐ 变异台买到的一条:M9 —— 半径,又一次

把队友环 1600 收到 **1200**,`§3.1`/`§3.2` 的 **11/15/16 与「五人帧 5 个队友」
全部不动**;唯一开火的是 `§5.3` 那条**源码读数**。
⇒ 这是 crowd 轮买到的「半径为真但不承重」**从另一侧的复现**,
并且它说明:拦住「语料悄悄换了个环」的**只有** `§5.3`,
这就是它必须是源码读数而不是散文的原因。
(M10 同族:敌人环单独收到 800 —— 两个合取项于是描述**不同的环**,
而**两行代码各自都不像错的**;同样只有 `§5.3` 开火。)

### 7.2 ⚠️ M3 的 `want` 认领的是**先开火的那一条**

pullcad 变异(把 sibling id AND 进本 gate)先红的是 `§1.1` 的 **arity 计数**
(`must hold exactly ONE IsSoakCandidate call (it holds 2)`),
不是它下面那个 `names a second id` 循环。台子的 `want` 写成前者,
并在注释里写明为什么 —— **认领后者等于认领一个没做到的击杀**
(`-186` 同一条教训的第二次)。

### 7.3 ⚠️ 该测试文件是**故意做便宜的**

初稿 **4.773s** —— 在 5.5s per-test cap 之内,但**没有余量**,
下一个往里加一节的人会把它挤出闸(GH #624/#806)。
改法:`§3.3`/`§4.1`/`§7.4` 每个 arm 集只 load **一个** world 而不是每帧一个
——两个 helper 对 `nAllyCount` 都是**纯函数**,帧唯一的贡献是那个计数,
而那个计数 `loaded_axe_frames()` 已经从真实 loader 上取过了;
`§3.1` 仍然为**三条腿各付一次逐帧 load**。⇒ **2.221s**。

## 8 同轮还掉的两条欠条(而不是留给下一个开工的组)

1. `tests/test_bots_walk_farm_only.py` 的 `UNRESOLVED_HAND_READ`:本文件的
   `io.popen` 走查**同轮登记**。⚠️ 这正是 GH #803 点名本组的那条
   ——「同一张普查、同一个组的第二发,两次都由别的组替你们读」。
   **本轮是第三发,自己读了**:先跑 `python3 tests/test_bots_walk_farm_only.py`
   当场拿到红(`8 checks, 1 failed`,逐字点名本文件),再登记,再复跑
   **`8 checks, 0 failed`**。
2. `tools/agent/lua_gate_manifest.json`:**同轮**手加行
   `tests/test_axe_q_lane_push_nocap.lua` `seconds = 2.466` `in_gate = true`,
   `selected_count 330 → 331`、`selected_total_seconds 250.693 → 253.159`
   (预算 510.0,cap 5.5)。
   ⭐ **本轮的缩放方向与前两次相反,这是要记住的那一点**:本容器比
   `measured_at` 那台**快**(`axe_q_lane_push_crowd` 本机 2.470 vs 登记 2.743
   = **0.90x**;`axe_hunger_camp_reach` 4.743 vs 4.927 = **0.96x**),
   所以「自己的实测 2.221s」是**乐观侧**,保守侧是**放大后**的 2.466s。
   前两条 hand-added 注记(axebhcamp / campdmg)记录的都是「不缩放就是保守」,
   **那是它们那台机器的性质,不是规矩** —— 规矩是**取保守侧**。
   `measured_at` 照 GH #810 **不动**。

## 9 交出去的下一棒

- `iterations/queue.json` 新请求 **hero-94**:零 EC2 归档扫描,
  买**五人满编**抱团带线的频率。⚠️ 它与 **hero-93** 是**同一次扫描的两列**
  (hero-93 报落在 (2,4] 的那一档 = 本 id 相对 shipped 的上界;
  hero-94 报**恰好 5** 的那一格 = 本 id 相对 `axecallcrowd` 的上界),
  **不要分两轮扫、也不要合并成一行**。承载者阻塞与 hero-2 / -13 / -89 / -91 / -92 / -93
  相同(`--find axe` → 899/910/911),**七条可以一次扫完**。
- **不申请入集**(P4.2 冻结期,唯一合法裁定是 FROZEN-HOLD)。
- 档案:`iterations/state.json:axecallnocap_20260916`。

## 10 铁律 6 的三行读数 + 开工自检

```
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)      -- luacheck bots game: 0 warnings
py gate: 125 ran, 0 findings, 0 uncertifiable, 43.2s
lua gate: 398 ran, 0 findings, 0 uncertifiable, 9 unanswered, 6 known-red, 625.6s
```

**三条腿全绿,未使用 `RULE6_BYPASS=1`。**

### 10.1 ⛔ 但闸自己打出了一个读数,而它超了预算 23%

`lua gate` 那一行后面跟着的是:

```
  76 new test(s) not in the manifest were run anyway, costing 349.09s
  => the hook's REAL cost this run is 625.61s against a 510.0s budget.
  9 new test(s) exceeded the hook budget and were EXCLUDED
```

⇒ **GH #804 的同一个缺陷,今天的数字**:该 issue 立案时是「587.2s / 预算 300.0s(196%),
49 个测试未登记靠 fail-open 照跑」;**本轮实测 625.61s / 510.0s(123%),未登记 76 个、
fail-open 代价 349.09s、9 个因超预算被 EXCLUDED**。⇒ **分母涨了(300→510)、
未登记数也涨了(49→76)**,两边同时动,所以百分比下降**不是**它在收敛。

⭐ **而这份名单里有一条是本组的欠条,今天第一次被看见:`tests/test_axe_q_lane_push_clock.lua`
没有 manifest 行**(`grep -c` = **0**)—— 那是 `axecallclock` 那一轮(09-12)的测试,
本轮的 `nocap` 行在,`crowd` 行在,`hunger` 行在,**唯独 clock 那一行从来没有**。
⇒ **它靠 fail-open 在跑,所以三条腿全绿并不意味着它被闸看着**;
GH #806 的 `UNCOVERED` 集合按构造也看不见它(fail-open 让它「跑过了」)。

⛔ **本轮不补这一行,而理由是算术不是懒**:闸已经在 **123% 预算**上,
再加一条在闸里的测试是往超支的方向推。这一条**连同上面的重测一起交给总监**
(处置属于 GH #804 / #810,不属于英雄组的一个工作单元),本报告只负责
**把读数和那一行的名字写下来**。

**开工自检(铁律 10)**:`bash tools/agent/routine_selfcheck.sh` 后台跑
(⛔ 不经管道、不加 `timeout` —— 脚本自己两次 REFUSED 挡住了这两种写法,
各 `exit 2`「nothing checked」,两条都记在这里因为它们**是本轮实际发生的**)。

```
legs run              : 13
FINDINGS (exit 3)     : cadence queue-rulings owed-executions lua-coverage
UNCERTIFIABLE (exit 2): trunk-red(python)
NOT RUN (inside a leg): tests/test_luacheck_gate_soakswitch.py tests/test_selfcheck_lua_leg.py
selfcheck worst exit: 3
trunk health (fast Lua detectors): 117 tagged detector file(s), 0 failures
```

⚠️ **本轮自己写错过一次归因,当场改掉,改法是去读 `PY_LOG` 而不是猜。**
起草时把 `UNCERTIFIABLE (exit 2): trunk-red(python)` 归给「自检与本轮对
`bots/BotLib/hero_axe.lua` 的编辑并发」—— 那条腿的提示行确实写着
「re-run on a quiet tree (nothing writing under bots/)」,而本轮**确实**在并发编辑,
**于是一个为真的前提长得像一个已完成的归因**。读 `PY_LOG` 后的真实构成是**两条,都不是它**:
1. `tests/test_luacheck_gate_soakswitch.py` —— 逐字 `UNCERTIFIABLE -- luacheck is not
   installed, so the behavioural half did not run`。自检**早于**本轮跑
   `luacheck_gate.sh`(那一步才把 luacheck 装上)⇒ 这是**新容器的时序**,不是编辑。
   ✅ **本轮把它买回来了**:装好之后单独复跑该文件,**4 checks, 0 failed(exit 0)**。
2. `tests/test_selfcheck_lua_leg.py` —— 9 个 check 没跑,因为被测那条腿在**本容器上**
   117 个文件跑不进 120s 预算(`NOTE 5a0 cost: 117 file(s) in 120.0s`)⇒ GH #358 同族
   (「墙钟分不开『集合变大』和『容器变慢』」),也不是编辑。
⇒ **trunk 的 python 那一侧本轮仍然没人完整看过**,但理由是上面两条,**不是本组造成的**;
**fast Lua 那一侧是 117 文件 0 失败**。⛔ 把「我并发编辑了」当成解释,会让下一轮
以为只要安静跑一次就好 —— 而真正要动的是容器时序与 120s 预算。

`bots/Customize/soak_side.lua` 本轮**从未存在**(`ls` 两次确认),所以 GH #848
那条撞车(后台自检 arm 的 gate 文件染红 push 闸的 Lua 腿)本轮**没有触发条件**;
等自检跑完再 push 仍然照做,因为触发条件不在自己手里。

自检另外报出的 `LUA GATE COVERAGE ... NEW UNCOVERED` 两条
(`test_dusttower_dive_guard.lua` / `test_fieldsip_transfer_receiving_site.lua`)
**不是本组的**,原样转出;本组自己的新文件按 §8.2 当轮登记,不进那个集合。

**`check_armed_wiring.py --cand axecallnocap`**(commit 后):
`direct / 1 site / bots/BotLib/hero_axe.lua:911`,`all 1 armed ids wired on HEAD`。
⚠️ commit **之前**同一条命令答 `UNWIRED / 0 sites` —— 它按 `HEAD` 读,不读工作树。

## 11 Token 用量

```
TOKENS total_in=11,331,177 out=63,056 turns=79
```
