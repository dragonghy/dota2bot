# hero 2026-09-03T02:45Z — backlog `-79`:把域量准,然后一次修完 18 个,并把量具留下

**改 20 个文件全在 `tests/`**(18 个走查载体 + 共享扫描器 + 1 个新常设普查);
**`bots/` 0 行、`game/` 0 行 ⇒ 零行为改动、零新 gate id、零 arm/promote、零 AWS、不申请波次;
`state.json` / `test_set.md` / `queue.json` 本轮无新增。**

## 0. 选题

- **OWNER_PRIORITIES 无本组未完成项**:三条常设项的球分别在批测台(Spot 优先)、
  协同组(P1 拉野)、协同组(P2 低血不回家)。
- 三条 open `[hero]` issue **与前四轮同因**:**#416** 上一轮已执行完它自己写的三条验收、
  本组认为可关(裁定权在总监);**#417** 判定权在总监;**#407** 被它自己那句
  「**拿到语料读数之前不改 `bots/` 一行**」挡住,读数在 `queue.json:hero-27`
  (`status=running`,`executor=batch-desk`,**硬时限 2026-09-22**,W34 `dem21/` 语料到期删除)。
- ⇒ 取 backlog **`-79`**,并且**照它自己的第一句话做**:「**先把域量准再动手**,
  别再从一个窄 grep 出发」。

## 1. ⭐⭐ 域量出来了:**18 个走查,18 个文件**,而前两次数的都不是这个量

这个类**被数过三次,三次答案都不一样**:

| 轮次 | 方法 | 答案 |
|---|---|---|
| 09-02T22:54Z | `grep "io.popen('find "` | 「18 个调用点」(**太窄**) |
| 同轮修正 | 放宽成 `io.popen` + `find` | 「23 处里**至少 17 处**」(**下界,不是数**) |
| 本轮 | **仪器** | **18 个走查 / 18 个文件**,外加 14 个静态不可解的逐个手读 |

**方法(这一段是本轮的主产物,不是 18 个修法)**:把 `tests/*.lua` 里每一个
`io.popen(...)` 的**实参表达式**用**括号/引号平衡**的扫描取出来(不是行 grep),
解析文件内的字符串常量,然后把每条**完全解析出来的命令真的执行**
——在一份**带着两个开关文件的一次性树副本**里。**域 = 输出里真的出现了开关路径的那些命令。**

- **一个命令是不是载体,是跑出来的,不是看出来的。** 这就是为什么前两次都错:
  `test_ancient_hp_unit.lua` 用的是 `ls bots/*.lua bots/*/*.lua bots/*/*/*.lua`,
  **两个 grep 都够不到它**,而它是载体;反过来 `test_itemtrip_supply_gap.lua`
  的 `ls "bots"` 长得像走查但**不递归**,一次都到不了 `bots/Customize/`。
- **不可解的一个都没有丢掉,全部点名手读**:14 条(11 条来自函数**形参**
  `'find ' .. dir ..`,值取决于调用方;2 条 `grep -rn` 拼 pattern;1 条 `sCmd`)。
  逐个读出调用方实参后,其中 **4 条**(`botsinit_env_namespace` / `ckpush_minute_unit` /
  `tormself_identity_domain` / `tpclaim_stamp_on_commit`,全部 `dir == 'bots'`)
  **是载体**,并入 18。
- ⚠️ **自伤一笔,登记**:第一版提取器**把注释里的 `io.popen (` 也算成调用点**
  (`_propertarget_sweep.lua:5` / `_towerstale_sweep.lua:5`),208 个「调用点」里
  至少 2 个是散文。留下来的量具已经先剥注释再扫,读数 208 → 199 → 执行 165 条。

**18 个载体**(`bots/` 全树走查,而后读回被枚举出来的路径):
`ability_value_read_anchor` / `activemode_world_assertion` / `ancient_hp_unit` /
`botsinit_env_namespace` / `ckpush_minute_unit` / `hpbool_dead_conjunct` /
`immguard_dead_filter` / `incoming_damage_callsite_census` / `isvalidtarget_fuse` /
`level_gate_census` / `name_predicate_multireturn` / `no_undefined_jmz_refs` /
`roshdist_pit_truth_operand` / `talent_name_binding_anchor` / `tormself_identity_domain` /
`tpclaim_stamp_on_commit` / `turbo_ternary_dominance` / `wk_q_castrange_meter_domain`。

## 2. 修法:10 条收到共享扫描器上,8 条就地带子句 —— 而这个二分**不是设计选择**

`-79` 把「一个个改 vs 都收到共享扫描器上」路由给了总监(GH #438,**至今 0 条评论**)。
按铁律 11 不空转等待,取**不预判裁定的保守默认**,而分界线是**量出来的**:

- **10 条的命令与 `scan.bots_files()` 的命令逐字同形**(`find bots -name "*.lua" | sort`)
  ⇒ 改调**已有的、已被 `test_aoe_result_field_names` / `test_nil_guard_then_body`
  用着的**那个入口。这不是选边,是同一条命令别写第二遍。
- **8 条形状不同**(`-type f`、没有 `sort`、`ls` 通配、或根是形参)⇒ **就地**带上子句,
  **形状问题留给总监裁**。
- ⭐ **子句与谓词都只定义一次**(`M.FARM_ONLY_FIND_CLAUSE` / `M.is_farm_only`),
  **被引用,永不复制**。理由是上一轮量到的那笔账:**四个只是在注释里逐字写了开关路径的文件
  被开关普查收编成 RAW(`RAW(5) → RAW(9)`),而棘轮照样 16/16 绿**。
  这个字面量是承重文本,18 份副本就是缺陷本身。
- `ancient_hp_unit` 用 `ls` 通配枚举,**没有 `! -path` 可用** ⇒ 它在**读侧**用
  `is_farm_only` 关窗;它的命令**仍然合法地到达** `bots/Customize/`,这一条被**点名登记**。

## 3. 两个读数:**改动是 no-op**,而**窗口真的关上了**

两个读数分别回答两个不同的问题,**都在隔离树里跑,没有碰活树**
(上一轮的教训:变异台污染了并发跑着的开工自检)。

- **(甲) no-op,逐位**:18 个文件在 **HEAD 树**与**修完的树**上
  `tests=N failures=0` **18/18 逐字相同**(共 172 个用例)。
  ⚠️ 中间抓到过一处**真回归**:`test_level_gate_census.lua` 留了一句孤儿 `p:close()`
  ⇒ `attempt to index global 'p'`,**2 条失败**。**是这条 no-op 对照抓到的,不是门。**
- **(乙) 窗口(并发 `rm` 台,每棵树自带一个 churn 循环)**:
  **HEAD 4 条失败 → 修完 0 条**(`botsinit_env_namespace` / `level_gate_census` /
  `name_predicate_multireturn` / `roshdist_pit_truth_operand` 各 1)。
  ⚠️ **4 不是 18**:窗口窄,一趟只咬到一部分 —— 这正是**为什么域必须量而不能靠撞红去数**。
- ⭐ **`pkill` 教训已落地**:churn 脚本**自己写 pid 文件**,按 pid 点杀。
  收尾时 `pgrep -c -f 'churn.sh'` 报「orphans: 2」,`ps -eo pid,args` 一读
  **两个都是调用方自己的命令行**(标记出现在 caller 里)⇒ **假阳性,零孤儿**,
  两棵树的 `bots/Customize/` 均已复原。**上一轮被这一手咬了两次,这一轮先量后信。**

## 4. 主产物:量具留下来了 —— `tests/test_bots_walk_farm_only.py`

**立它的理由不是缺工具,是这个类的大小没人说得出来。** 三次计数三个答案的类会回来;
本轮把仪器**留成常设普查**,于是下一次到来是**红**,不是又一次重新发现。

**它怎么量(以及它不量的那一格)**:同一套平衡扫描 + 常量解析,然后**真的执行**每条命令;
判据是输出里有没有 **`bots/Customize/general.lua`** —— 一个**永远在场的、同目录同后缀的
已提交文件**,给两个开关**当替身**。⭐ **这样它就永远不需要自己造一个 `soak_*` 文件**:
造一个正是这条规则要消灭的那种争用,而且**会先打断所有还没修的走查**,方向恰好反了。

四条自证(全部裸退出码):

| 台 | 做法 | 读数 |
|---|---|---|
| **M0 负控** | HEAD 的 18 个走查 + 新扫描器 | **4 条失败,18 个载体全部点名**(13 个直接点名 + `ancient_hp_unit` 被读侧那条抓到 + 4 个形参走查被「不可解未登记」那条抓到)—— **没有一个是静默的** |
| **M3 空转** | 删掉替身 `general.lua` | **5 条失败**,含 `the proxy ... no longer exists, so this ratchet would pass vacuously` ⇒ **不会因为「什么都没扫到」而发绿证** |
| **M4 新走查** | 给 `test_smoke_load.lua` 加一条不可解的 `find ' .. d ..` | **1 条失败并点名该文件** ⇒ 新走查**加入人口要付一次手读**,不能悄悄进来 |
| 还原 | 撤掉 M4 | 与 HEAD **逐字节相同**,`EXIT=0` |

⭐ **两处自己踩了自己写下的规矩,当场改掉,登记**:
1. **第一版用行号做键** —— 而 GH **#442**(前一天立案)就是这条:
   「一次无关的注释插入让两个普查工具同晚变红」。我的第一稿**一小时内复现了它**
   (剥注释的 bug 让每条注释多算一行,`:455 → :606`)。改成 **文件 + 表达式文本**做键。
2. **`.strip("'\"")` 把子句自己的收尾 `"` 一起吃掉了** ⇒ 打印出来的修复指令
   `! -path "bots/Customize/soak_*.lua`(缺右引号)**是一个不能 parse 的 shell 片段**,
   而**棘轮照样工作** —— 静默地只坏了给人看的那一半。改成只剥外层引号。

## 5. 门

- **开工自检**:`SELFCHECK_EXIT=3`(`legs run 9`、**`UNCERTIFIABLE: none`**)。
  findings = `cadence` + `stale-waits` + `trunk-red(python)`。
  ⚠️ **第一次调用又被拒答**(`SELFCHECK_EXIT=2 REFUSED`,stdout 接进 `tail`)——
  **证据纪律 3 的第 10 次现场**,已按它自己给的写法改对重跑。
  **Lua 腿:73 个 tagged detector 文件,0 失败**(上一轮那条 `test_item_name_census`
  的 MOVED 钉子已在 trunk 上绿)。
  **python 那 3 条红全部先于本轮,且本轮 `.py` 改动只新增一个文件**:
  `illumove_pairs:HP_CUT` / `wandbleed_trigger:HP_MAX` / `HP_MIN_EXCLUSIVE` 三个
  `UNREGISTERED`(GH **#410**,上一轮已追评),加 `test_stale_waits.py` 1 条。
- **静态**:`bash tools/agent/luacheck_gate.sh` ⇒ **`GATE_EXIT=0` /
  `luacheck bots game: 0 warnings`**(冷启自装,apt 包名 `lua-check`);
  `bash tools/agent/rc.sh` 裸读 **`RC_EXIT=0`**。**没有用 `RULE6_BYPASS`。**
- **定向动态**:18 个载体文件 **172 用例 / 0 失败**,且与 HEAD **逐位相同**(§3 甲)。
  新普查 `test_bots_walk_farm_only.py` 在活树上 **`8 checks, 0 failed`
  [165 commands executed, 14 unresolved]**,`RC_EXIT=0`。
- ⛔ **全量套件没跑成,不声称。** 起过一次(后台),**容器重启把它掐了**,读数永久丢失 ——
  与 09-02T19:48Z 那次同型。**按「没跑成」记,不是通过。** 覆盖本轮 20 个改动文件的是
  §3(甲) 的 18 文件定向对照 + 新普查自身,不是全量。

## 6. 交出去的棒

- GH **#438**(本组上一轮开的,当时 0 条评论)**已追评**(`#issuecomment-5519204661`,
  在 push 之后、`claim_precheck` `RC_EXIT=0` / `clean` 之后):本轮的**域读数(18/18)**、
  **10+8 的二分依据**、以及**剩下要总监裁的那一格**(形状不同的 8 条要不要也统一到
  共享扫描器上;本轮**没有替总监做这个决定**)。
- GH **#365** **已追评**(`#issuecomment-5519207920`):§2 的载体**全部关窗**,附 M0 负控读数,
  并写下那句「**看见的数量是撞红的采样,不是类的大小**」——#365 当初只看见 3 个的原因。
- **backlog**:`-79` 划掉(域已量准 + 18 个全修 + 量具留下);`-78` 的最后 5 个
  (`bbfight` / `bbrespawn` / `bbshort` / `pollyhp` / `salveally`)**仍然不动,棒没掉**
  ——它们要连 `test_soakside_shared_switch.lua` 的 S2 改写一起做。
- **`#407` 的棒仍在批测台**(`hero-27`,时限 2026-09-22),本组无从推进。

## 7. Token 用量

```
TOKENS total_in=13,469,367 out=76,198 turns=75
```
(统计时刻之后的收尾回合不计入。)
