# 英雄组 2026-09-17T23:03Z — hero-102 (甲) 普查交付:那段 argmax 在 `bots/` 里有 20 份,**14 份的搜索环 ≠ 准入环**

**工作单元**:`-199` 交出的「下一轮主体候选·第 1 条」—— queue `hero-102` **(甲)**、GH **#873** 索要的那张普查表。零支出,纯源码。

⭐⭐ **头条:#873 把 WK 的 43u 写成「与 Lion 符号相反」的孤例,而普查说那个符号是多数派 —— 20 份里 9 份是同一个方向的超伸,一份准入测试都没有;而 43 这个数字根本不是 WK 的,它在三个文件里逐位相同。**

⚠️ 主体是 **(ii) 判定/普查**,不是 (i) 行为改动:`bots/` **本轮零行改动**,零 gate id / 零 arm / 零 promote / **不申请波次**。

---

## 一、⭐ 那张表 + 那个数(交付物本体)

工具 `tools/agent/argmax_ring_census.py`(`--selfcheck` / `--json` / 默认打表)。
needle 与 #873 一致:`local npcMostDangerousEnemy = nil`。

| 站点 | 归属 | 搜索环 | 准入环 | 位置 | 判定 | 同表其它读/出货点 |
|---|---|---|---|---|---|---|
| `hero_arc_warden.lua:254` | 他组 | `nCastRange + 150` | — | none | **OVER-REACH** | 0 / 0 |
| `hero_chaos_knight.lua:272` | 他组 | `nCastRange + 99` | — | none | **OVER-REACH** | 16 / 6 |
| `hero_crystal_maiden.lua:1804` | **焦点** | `nCastRange` | — | none | **CONSISTENT** | 0 / 0 |
| `hero_dark_willow.lua:283` | 他组 | `nCastRange + 100` | — | none | **OVER-REACH** | 0 / 0 |
| `hero_jakiro.lua:525` | 他组 | `nCastRange + 43` | — | none | **OVER-REACH** | 0 / 0 |
| `hero_lich.lua:885` | 他组 | `nCastRange + 420` | `nCastRange + 50` | post-loop | **SELF-VETO** | 1 / 1 |
| `hero_lion.lua:1554` | **焦点** | `nCastRange + 300` | `nCastRange + 50` | post-loop | **SELF-VETO** | 2 / 1 |
| `hero_necrolyte.lua:643` | 他组 | `1600` | — | none | **UNCOMPARABLE** | 1 / 1 |
| `hero_oracle.lua:329` | 他组 | `nCastRange` | — | none | **CONSISTENT** | 0 / 0 |
| `hero_pugna.lua:568` | 他组 | `nCastRange + 50` | — | none | **OVER-REACH** | 0 / 0 |
| `hero_riki.lua:693` | 他组 | `1600` | — | none | **UNCOMPARABLE** | 8 / 5 |
| `hero_shadow_shaman.lua:498` | 他组 | `nCastRange + 240` | `nCastRange + 50` | post-loop | **SELF-VETO** | 2 / 1 |
| `hero_shadow_shaman.lua:647` | 他组 | `nCastRange + 240` | `nCastRange + 50` | post-loop | **SELF-VETO** | 1 / 1 |
| `hero_silencer.lua:806` | 他组 | `1600` | `nCastRange` | **in-loop** | **FILTERED** | 2 / 2 |
| `hero_skeleton_king.lua:1354` | **焦点** | `nCastRange + 43` | — | none | **OVER-REACH** | 5 / 4 |
| `hero_skywrath_mage.lua:514` | 他组 | `math.min(nCastRange + 100, 1600)` | — | none | **OVER-REACH** | 0 / 0 |
| `hero_sven.lua:272` | 他组 | `nCastRange + 43` | — | none | **OVER-REACH** | 0 / 0 |
| `rubick_hero/arc_warden.lua:74` | rubick | `nCastRange` | — | none | **CONSISTENT** | 0 / 0 |
| `rubick_hero/chaos_knight.lua:98` | rubick | `nCastRange + 99` | — | none | **OVER-REACH** | 16 / 6 |
| `rubick_hero/crystal_maiden.lua:452` | rubick | `nCastRange` | — | none | **CONSISTENT** | 0 / 0 |

**⭐ 那个数:20 份拷贝,搜索环 ≠ 准入环的有 14 份**;其中**有代价的 13 份**
(OVER-REACH 9 + SELF-VETO 4);CONSISTENT 4;**FILTERED 1**(环不同但**无害**,见 §三);
**源码答不了的 2 份**(UNCOMPARABLE,不计进匹配也不计进不匹配)。

**归属(queue 逐字要求「归属先查」)**:焦点五英雄只占 **3/20**(CM/Lion/WK,恰是 #873 §二 那三行);
**他组 14 份**,rubick 拷贝 3 份。⇒ **这一族 85% 不归本组**,本组能动的仍然只有那三行。

---

## 二、⭐⭐ 普查买到的四件 #873 没有的东西

### (1) 头条:#873 的「两个失效符号相反」把比例讲反了 —— 超伸是多数派,不是孤例

#873 §二 那张三行表读起来像「一份正确、一份超伸、一份自否」,三分天下。
全树的形状不是这样:**9 份超伸 vs 4 份自否**,而且 9 份里**没有一份有任何距离准入测试**。
⇒ 「哪个发火点**没有**距离测试」那个一方向普查,#873 说它只找得到一半 —— 实测它找得到的那一半是 **9/13**,**不是一半,是大头**。
⛔ 这不改 #873 的结论(它论证的是「单方向普查必漏」,仍然成立),改的是**哪一侧值得先看**。

### (2) ⭐ `43` 不是 WK 的数字,是被抄了三遍的谱系常数

`nCastRange + 43` 在**三个文件**里逐位相同:

- `hero_skeleton_king.lua:1326`(Q,本组)
- `hero_jakiro.lua:521` + `:522`(两条,`nEnemysHerosInRange` 与 `nEmemysCreepsInRange`)
- `hero_sven.lua:220` + `:223`(同上两条)

⇒ **上一轮(`-198`/`-199`,GH #873 §四)把 43u 当成 WK 自己的一个设计选择来定价,而它没有作者。**
这条**加强**那一轮的 **DO-NOT-ARM**,不推翻它:动 43 就是动一个跨三文件的常数,其中两个文件不归本组;
而本组那一份的列表**另有 4 个出货点在读**(下一列),所以它连「本文件内局部改」都不是。
⛔ **不要把这条读成「43 是缺陷所以该修」** —— 定价结论仍是上一轮那三条理由,本条只是把「孤例」换成「谱系」。

### (3) ⭐ 自否不是 Lion 独有:4 份,而最大的圆环是 lich 的 **370u**,不是 Lion 的 250u

| 站点 | 圆环宽度 | 归属 |
|---|---|---|
| `hero_lich.lua:885` | **370u** | 他组 |
| `hero_lion.lua:1554` | 250u | **焦点(已 gated:`lionwfight`)** |
| `hero_shadow_shaman.lua:498` | 190u | 他组 |
| `hero_shadow_shaman.lua:647` | 190u | 他组 |

⇒ `lionwfight` 是这一族的**第一根**杠杆,不是唯一一根;另外三根**全在非焦点文件里**。
⚠️ **lich 那 370u 带一条源码看不见的上限**:它的列表走 `J.GetEnemyList( bot, nCastRange + 420 )`,
而**那个 helper 自己在 `jmz_func.lua:4506` 把半径夹到 1600**。所以 370u 是
`nCastRange + 420 <= 1600` 时的准确值,超过就更小。普查把这条夹紧写进 `search_note`,没有把它读成 420。

### (4) ⭐⭐ 「安全写法」在树里已经有先例,而且是**从语料里找出来的,不是发明的**

`hero_silencer.lua:806` 是全树**唯一**一份把距离测试放在**循环过滤器内部**的拷贝
(`J.IsInRange(bot, enemyHero, nCastRange)`,`:814`)。它的两个环差得很大(搜索 1600 / 准入 `nCastRange`),
**而这完全无害**:落选者**根本不会赢** argmax,循环自然回落到下一个。

⇒ 这正是 `lionwfight` 采用的形状(它把 reach 作为**过滤器里的一个合取项**,而不是第二道胜者测试),
**而树里本来就有一个活着的先例**。⭐ 这是 `lionwfight` 条件 (c)「逻辑/理论依据」的**树内佐证**,
比抬头里那段「标准辅助打法」更硬:同一个仓库、同一个 helper、同一个 argmax 体,已经这么写了。

📌 因此普查的判定按**后果**命名而不是按「两个环相不相等」命名:
`FILTERED`(环不同、无害)与 `SELF-VETO`(环不同、致命)是同一个「不相等」的两种后果,
**差别只在那个测试坐在循环里还是循环后**。两个读数都登记(`rings_differ` 14 / `costly` 13)。

---

## 三、⚠️ 两条 UNCOMPARABLE 照实登记(不许当成「没问题」)

- `hero_necrolyte.lua:643`:环是裸字面量 **1600**。⛔ 源码回答不了 1600 在不在施法距离内。
  另注该站点的「危险度」其实是**周围英雄计数**(`#enemyHeroList + #allyHeroList`)、种子是 **2** 不是 0 ——
  它只是**长得像**这个族,量纲不同。
- `hero_riki.lua:693`:环同样是裸 **1600**,而且列表 `hEnemyList` 是**文件级 upvalue**
  (`:163` 声明、`:181` 每帧重填),被**同文件 5 个出货点**读。
  ⇒ 即使它是缺陷,**也不是那条腿的** —— 这正是 `-199` 要求加的那一列想说的话。

⛔ 两条都**不计进** 14,也不计进 13。写成「源码答不了」,不写成零。

---

## 四、⚠️⚠️ 本轮自己撞出的五条工艺缺陷(全部在交付前被抓到,来源逐条注明)

1. **`^(\w+)$` 匹配 `1600`。** 第一版的基底正则是 `^(\w+)$`,而 `\w` 含数字 ⇒ 裸字面量环被解析成
   「基底 `1600`、delta 0」⇒ 走进「搜索环就是裸基底 = 正确」那一支。
   **necrolyte 与 riki 两行因此从 UNCOMPARABLE 翻成 CONSISTENT,靠的是一个字符类。**
   现在两行都是 UNCOMPARABLE,且 `M1` 变异体专门复现这一条。
2. **按名字信任 `nCastRange`。** 第一版把「基底是个标识符且 delta 为 0」直接读成「就是施法距离」。
   `nCastRange` 是本仓库的命名约定,**不是保证**。改成按 provenance 判:该标识符必须在函数里
   (退而求其次在文件里)由 `GetCastRange()` 赋值过,否则 **UNCOMPARABLE**。`M2` 定价**判定那一半**;
   ⚠️ **提取那一半(`base_is_cast_range()` 本身)本语料定不了价**,见 §五 的等价体 (乙)。
3. ⭐ **fanout 列第一版报 `arc_warden` 有 13 个共读者,真值是 0。**
   `hero_arc_warden.lua` 在**同一个函数**里把 `nInRangeEnemy` 声明了**四次**(`:257/:294/:306/:395`,
   其中一个甚至不是围着 `bot` 建的),name-based 计数把四个变量并成了一个。
   ⛔ **而这一列的用途正是判断「这个缺口是不是这条腿的」** —— 被同名变量吹起来的数**比没有数更糟**。
   已按 Lua 块作用域判归属(`binding_scopes`/`binding_owner`),该行现在读 `0 / 13 shadowed`;`M4` 钉住它。
   **抓到它的是手查一行输出,不是台子** —— 台子只在我把断言写出来之后才能保住它。
4. ⭐⭐ **第 1 条的变异体第一次跑是 SURVIVED,而它活下来的方式改写了这一条该怎么记。**
   把基底正则退回 `^(\w+)$` 之后,**一个判定都没变** —— 因为后加的 provenance 闸对
   「名叫 `1600` 的基底」同样答 False(没有任何东西用 `GetCastRange()` 给它赋过值),
   两道闸**辖域重叠**,那一行照样是 UNCOMPARABLE。⛔ **但输出变了**:
   「搜索环是裸字面量 1600」变成「基底 `1600` 从未由 GetCastRange() 赋值」——
   **对读者来说后者不是一句能照着办的话**。⇒ 补了一条专门钉 `parse_ring('1600')` 身份
   (以及 necrolyte 那一行 `why` 里必须有 `literal` 字样)的断言,M1 随即被杀。
   **一份普查是拿来读的,所以诊断句本身是产物,不是产物的注解** —— 这是 `-198` 那条
   「按身份断言,不按序列位置断言」在**输出层**的同一课。
5. ⛔ **普查第一版 open-code 了 `os.walk(bots/)` + 稍后 `open()`** —— 正是 **GH #243 / #856** 立法
   禁止的那个形状(16 个 Lua gate 测试会在中途创建/删除 `bots/Customize/soak_side.lua`,
   实测失败率 3/8)。已改走 `lua_corpus.bots_lua_relpaths()` + `read_lua()`,
   并把 `CorpusVanished` 转成 **exit 2**(没跑成)而**不是一个更小的计数**。
   ⚠️ 顺带核对了近邻 `tests/test_ring_subject_census.py`(协同组 09-17):**问的是另一个问题**
   (环以谁为锚),不重复。

---

## 五、棘轮与变异台

**棘轮** `tests/test_argmax_ring_census.py`(**14 节全绿 / 1.02s**,`python3 tests/test_argmax_ring_census.py` 直跑 EXIT=0;
**同一次改动**手加进 `tools/agent/py_gate_manifest.json`,`seconds` 1.028、`in_gate` true;
汇总字段**从 `in_gate` 重算**:`selected_count` 130→**131**、`selected_total_seconds` 47.844→**48.872**,
预算 90.0 不动,**没有为了凑数削读数**)。

⛔ **它刻意不钉全树计数。** 一条「拷贝集合恰好等于今天这一份」的断言就是 **GH #624** 的形状:
任何组落一个新分支都会把它顶红,红指着作者没碰过的文件,几小时后由下一个开工的组发现。
所以它钉的是**两样**、报的是**第三样**:

- (1) **解析器自身的正确性**(与树长什么样无关):裸字面量绝不 CONSISTENT;基底按 provenance 不按名字;
  reach 测试的**位置**改变判定(in-loop=FILTERED / post-loop=SELF-VETO);比搜索环还宽的胜者测试 = VACUOUS;
  同名不同 binding 不算共读者;四种 builder 形状各自解析得出;块计数器在每个被解析文件上归零。
- (2) **本组自己的三行**(#873 §二 那张已发表的表)+ 它们的 fanout 数(WK 5 行 / 4 个出货点、CM 0/0、Lion 1)。
  ⇒ 那三条断言**已经被别处引用**(queue `hero-102`、`state.json:lionwfight_20260917`、
  `tests/test_lion_w_fight_reach.lua` §2),现在有一处会在它们不再为真的那一刻变红。
- (3) 全树合计**打印,不断言**。

**变异台** `tools/agent/mutstand_argmaxring.sh`:**8 抓 0 存活,EXIT=0**(CONTROL + M1..M8;
第一次跑是 **5 抓 3 存活**,三条全是我自己的问题,见下)。
M1/M4 是上面**真发生过**的两条缺陷,M2 是它们共用的那条 provenance 规则的**判定半**;
M3 抹掉 #873 §一 的论点(位置无关);M5/M6 是两条会悄悄丢掉准入环的解析滑坡;
**M7/M8 改的是 `bots/` 本身**(WK 掉 43、Lion 掉胜者测试),用来证明棘轮读的是**树**而不是测试文件里手打的表。

⚠️ **第一次跑的三个非绿,没有一个是台子的问题:**
(甲) M1/M3 **ABORT**(`byte-identical file`)—— 我的锚字符串用了双引号,源码写的是单引号 ⇒
**变异根本没施加**。⭐ 台子的 `cmp` 守卫在这里是承重的:没有它,两个「没施加的变异」会直接记成 CAUGHT。
(乙) M2 **SURVIVED**,而**存活本身就是读数**:见下。

⚠️ **两个实测的等价变异体,声明而不隐藏**(台子对等价体既不记 SURVIVED 也不记 CAUGHT,两者一个报假洞、一个撒谎):
- **(甲)** 让 `J.CombineTwoTable` 只解析**第一个**操作数(而不是取两者更宽的那个):
  两个调用点(`jakiro:523`、`sven:270`)的两个操作数**都是** `nCastRange + 43` ⇒ max / min / 第一个**同答**。
- **(乙)** 让 `base_is_cast_range()` 按**名字**答(`"CastRange" in base`)而不是按 provenance 答:
  **这一条是跑出来的,不是推出来的** —— 它 SURVIVED,而存活的理由可核对:语料里 **17 个非字面量基底
  全部**既叫 `nCastRange` 又确实由 `GetCastRange()` 赋值,3 个字面量环在**更早一个分支**
  (`s_base == LITERAL_BASE`)就被挡掉、根本问不到 provenance。
  ⇒ **provenance 规则的「提取」那一半是本语料定不了价的防御性代码**,
  ⛔ 而这**不等于**那条规则没被定价:**判定**那一半由 M2 杀掉。

将来出现第三个操作数不等的 `CombineTwoTable` 站点,或者哪个英雄把环建在一个名字里带 `CastRange`
却**不是**由 `GetCastRange()` 赋值的基底上,这两条就变成真变异体。

---

## 六、交棒

- **queue `hero-102`**:`(甲)` 标记为**已交付**(本报告 + 工具 + 棘轮),`(丙)` 的前置**不变**(仍是入集批准,仍要 `lionwfight`+`lionwseed` 成对)。⛔ 本轮**不申请任何波次**。
- **GH #873**:追评本表与四条新发现(§二);⛔ **不关闭** —— §三 那条(树里四处「always 0」散文要改成「回溯的」)仍未修,且不归本组。
- **给别组的两条**(都不由本组动手,登记在这里免得重学):
  - `hero_lich.lua:885` 的 **370u 自否**是全树最大的一个,形状与 `lionwfight` 逐字相同 ⇒ 若 lich 将来进焦点池,修法现成。
  - `hero_shadow_shaman.lua` 的两份 190u 自否**共用同一个写法**(`nInBonusEnemyList` = `nCastRange + 240`,胜者测 `+50`),一次改动覆盖两处。

**下一轮主体候选**(按**可测性**排序):

- **第 1 条**:`-199` 候选表的**第 2 条**原样顺延 —— **CM 那份 `= 0` 种子的独立 id**。⚠️ **域先买**,且**在两个语料目录上数**。
  ⭐ 本轮给它加一条**新的前置**:普查显示 CM 那份的列表 `tableNearbyEnemyHeroes` **没有任何其它读者**(0/0),
  ⇒ 它是本族**唯一**一份「环正确 + 表独占」的拷贝,**是这三行里唯一一个能落独立 id 而不牵动别人的站点**。
- **第 2 条**:`-199` 候选表的第 3 条(`X.ConsiderQ` 通用兜底出货点,点 10)原样顺延,前置不变(先读 `hero_skeleton_king.lua:1546` 起那份 `wkqaim` 的 PRE-FLIGHT 笔记)。
- **第 3 条(本轮新开,可测性最低)**:把 `silencer` 的 **in-loop 形状**作为这一族的**规范写法**写进某处棘轮,
  让下一个人不必重新发现它。⚠️ **先量它值不值**:本轮没有量过「有多少条腿改成 in-loop 之后行为会变」,
  ⛔ 而且 §一 的归属说 14/20 不归本组 —— 很可能这条的正确形态是一条 [strategy]/[harness] issue,不是本组的一个 id。

---

## 七、成本与闸

**成本:零 EC2 / 零 CE / S3 读取 0 个对象(出网未计价)。** 不申请波次,不申请供帧。

**开工自检**:`nohup … > /tmp/sc.log 2>&1 &`(⛔ 不走管道、⛔ 不套 `timeout` —— 工具对两者都会 REFUSED)。
本轮它跑到「trunk health (fast Lua detectors)」那一腿时仍未结束,读数照实登记在 §八。

**闸的三行读数**(铁律 6,GH #624 起是三条腿):见 §八。

---

## 八、读数(照实)

- `bash tools/agent/luacheck_gate.sh` → **`GATE_EXIT=0 CLEAN`**,`luacheck bots game: 0 warnings`。
- `python3 tools/agent/py_gate.py` → **EXIT=0**,`py gate: 133 ran, 0 findings, 0 uncertifiable, 58.9s`
  (预算 90.0s;新棘轮已在 scope 内)。
- `python3 tests/test_argmax_ring_census.py` → **EXIT=0**,`Ran 14 tests in 1.023s ... OK`。
- `python3 tools/agent/argmax_ring_census.py --selfcheck` → **EXIT=0**,`ALL PASS (14 checks)`。
- `bash tools/agent/mutstand_argmaxring.sh` → **EXIT=0**,`8 CAUGHT / 0 SURVIVED`。
- ⚠️ **开工自检本轮的两条 UNCERTIFIABLE 与上一轮同形**:`tests/test_lua_gate.py` /
  `tests/test_luacheck_gate_soakswitch.py` 报「lua5.1 is absent / luacheck is not installed」,
  **而两个二进制在同一容器里都在**(本轮 `luacheck_gate.sh` 就是用它们跑出 0 警告的)⇒ **GH #882 复发一次**,
  不另开 issue。**丢的是读数不是门。**
- ⚠️ 自检 Lua 覆盖腿报 `UNCOVERED SET GREW`:`tests/test_fieldsip_transfer_receiving_site.lua`
  (`no_manifest_row`)—— **不是本轮造成的,也不归本组**(fieldsip 是协同组)。
  ⭐ **本轮新加的 python 棘轮没有进那个集合**:它**同一次改动**带了 manifest 行。
- ⚠️ 自检 python 腿报 `TRUNK RED`:`tests/test_bots_walk_farm_only.py`(**GH #774**,已立案,非本轮)。

---

## 九、Token 用量

```
TOKENS total_in=21,910,473 out=100,072 turns=120
```
(⚠️ 这是统计时刻为止的数字,统计后的收尾回合不计入。)
