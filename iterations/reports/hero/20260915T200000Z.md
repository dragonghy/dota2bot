# 英雄组 2026-09-15T20:00Z — `axecallring`:Berserker's Call 由**环**决定,不由被问的那一个人决定

**工作单元主体**:一个 `bots/` 行为改动(OWNER_PRIORITIES **P4.4 (i)**)。
**焦点英雄**:Axe。**新 gated id**:`axecallring`(turbo-only,**未 armed**,P4.2 冻结期不申请入集)。
**成本**:零 EC2 / 零 CE / **S3 读取 0 个对象**(铁律 1 三段式;本轮没有下过任何 `.dem` 或 `analysis.json`)。

---

## 0. 开工自检(铁律 10)

⛔ **本轮第一条命令又踩了管道**(证据纪律 3 同形,第 19 次),而脚本自己的 `REFUSED` 门当场拦下:
退出码钉成 2,打「nothing was checked; this is NOT a pass」。第二条命令用重定向就对了。
—— 与 backlog `-183` 的结论一致:**提醒不是控制,门才是**,这一格不必再加提醒。

```
bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1; echo "EXIT=$?"
EXIT=3
selfcheck worst exit: 3
FINDINGS (exit 3)     : queue-rulings owed-executions lua-coverage trunk-red(python)
UNCERTIFIABLE (exit 2): none
trunk health (fast Lua detectors): 113 tagged detector file(s), 0 failures
```

⛔ **trunk red 不代修**:python 侧是 **`tests/test_py_gate.py`** —— py 闸 manifest 漂移
(7/141 无行,超 slack 5),**已立案 GH #839**(harness / 总监)。本轮不引它,也不动它。

⚠️ **Lua 腿 9 个 check 打 UNCERTIFIABLE**(120s 预算内没跑完;`5a0` 那条 NOTE 的 113 文件是发现集不是计数行)⇒ **那一侧本轮没人看过**,与 GH #812 / #804 的预算问题同源。

⚠️ **登记一次重叠(GH #229)**:开工自检与本轮变异台有时间重叠,自检报
「a killed test left `bots/Customize/soak_side.lua` ARMED; removed it」。
实际后果为零(收尾复跑 `test_axe_call_ring_anchor` 仍 19/0,`git status` 无 `soak_side.lua`),
**但重叠要登记,不能因为结果看着没事就不写**。

---

## 1. ⭐ 立案:锚点问错了人

`axe_berserkers_call` 是 **`No Target`** 的 AoE 嘲讽,`radius` **315**
(`tests/mock/special_value_shapes.lua` 携带该 KV;`special_bonus_unique_axe_2` 的 +85 由引擎折进
`X.ConsiderQ` 已经读的那个句柄,GH #228)。而 `X.ConsiderQ` 的先手出货点用**一个单位**决定放不放:

```lua
if J.IsValidHero( botTarget )
    and J.IsInRange( botTarget, bot, nRadius - 90 )        -- 225u
    and ( J.CanCastOnNonMagicImmune( botTarget ) or ... )
    and not J.IsDisabled( botTarget )
```

`botTarget` 是 `J.GetProperTarget` —— Axe 恰好在打谁。**那一个人站到 225u 环外,整条支路就拒**,
而下游没有任何一条支路再问一次(剩下的是打断读条、带线宵禁、Roshan、Tormentor、野怪)。

⭐ **这条线索不是新发现的,是本文件自己写下又明说没修的那一项**:`axecallbkb_ii` 的 header 逐字写着
「Gating it on one enemy's properties throws away every OTHER enemy standing in the same 315u ring」,
然后在 *WHAT IS DELIBERATELY LEFT ALONE* 里把**锚点**留在原地,只搬了免疫项。**本轮搬的正是它明写着没搬的那一项。**

⛔ **两个 id 不是同一句话**(§7 钉住):
`axecallbkb_ii` 放宽的是「被问的那**一个**单位可以给什么答案」;**锚点不动** —— botTarget 在环外时 arm 它仍然**一帧都不动**(§7.1 在真实帧上驱动出来)。
本 id 搬锚点,**一个免疫项都不碰**(计数器套用**出厂**的 `J.CanCastOnNonMagicImmune`,即保守读法;变异体 **M5** 打的就是「悄悄把它吞掉」那一版)。

---

## 2. 修法(gated,turbo-only)

`bots/BotLib/hero_axe.lua`:

- `X.IsCallRingOn()` = `J.IsModeTurbo() and J.IsSoakCandidate( 'axecallring' )`;
- `X.axe_CountCallRingTargets( tEnemies, nRing )` —— 环里有几个「这发 Call 真会嘲讽到、且出厂判据同意可以施法」的英雄。
  `J.IsInRange( bot, npcEnemy, nRing )` 的参数序是它自己的 `( origin, target, radius )`,
  **把 CanBeSeen 落在敌人身上**(出厂那一行写反了,`J.IsInRange( botTarget, bot, ... )` 把它落在 Axe 自己身上,
  那里无害只是因为同一行的 `CanCastOnNonMagicImmune( botTarget )` 已经测过目标可见);
  **nil 列表答 0**(限制性默认,变异体 M7);
- `X.nCallRingQuorum = 2`(具名量,测试从源码读数字,stale-mirror 族);
- 出货点是**排在出厂支路之后的一条独立 `if`** ⇒ **闸关时整块不求值** —— 不是「化简回出厂谓词」,是根本不存在。

**方向 ADD-ONLY,按构造**:出厂支路排在前面且一字未动,本块只在出厂支路**落空**时 `return`
⇒ arm 只能**加**一发 Call,永远不能删一发、也不能把一发挪到别的目标上。
§6.2 把「出厂支路在前」钉成源码断言;**M9** 就是把本块提到它前面。

**法定人数取 2 而不是 1 是保守侧,而代价是数出来的**:1 会把离线支路层域从 **1 变成 4**
(另外 3 行环里恰好 1 个人)。一发只嘲讽到一个**非目标**英雄的 Call 是边际情形,本轮不买 —— 
「环而不是锚点」这个论证**只在 ≥2 时才咬得动**。

---

## 3. ⛔ 两个域,两个数字,不许当成一个

| 量 | 读数 | 怎么得到的 |
|---|---|---|
| **支路层域** | **1** 个瞬间 | 28 行 Call 就绪的**活** Axe(`tests/fixtures/` + `tests/frames/`),225u 环内 ≥2 个活敌方英雄的恰好 1 行;24 行是 0,3 行是 1。315u 切法也是 1(两个切法没分家,§2.2) |
| **端到端域** | **0** | 同一帧上 `X.SkillsComplement` 先问 `X.ConsiderR`,**斩杀赢了顺序**;armed 与 shipped 发出的指令**逐条相同** |

⭐ **那个 0 是测出来的,不是推出来的**,并且按可以变红的方向钉死(§5)。
**M12**(让大招弃权)是它的对照:大招一弃权,Call 真的被发出去,§5 当场红。
**没做这个切分的一轮,会把支路层那个 1 当成端到端的读数报出去** —— 报的是一发不会发生的技能。

**那唯一一行的价值列**(全部是录像自己的状态,`tests/frames/f_260828_002127_axe_call_bkb_ring.lua`,t=982.1):

- Axe 22 级,Call **rank 3、cd 0**,**319 蓝**对 **110** 花费;
- **Lina 75.1u**、**Necrophos 165.3u** —— 都在 **225u 环内**,出厂判据下都可施法;
- **Shadow Shaman 276.9u** —— **在 315u 嘲讽里,在 225u 锚点外**,而**他正是 Axe 在打的那个人**:
  该帧自己的 `recent_damage` 里**四次 19 点 Axe 攻击**落在他身上(§1.3 钉住,`>= 4`)。

⇒ **三个英雄在 AoE 里,而出厂支路问的那一个恰好是过不了测试的那一个。**

---

## 4. ⚠️ 一个读数被修,标明是弱的那一种

`J.IsGoingOnSomeone` 读 `bot:GetActiveMode()`,而 behav dumper 的快照 schema **没有 active-mode 这一路**
(GH #577 §5)⇒ 它在 `make_fixture.py` 能产出的**每一帧**上都是 false。§4 把 `GetActiveMode` 修成 `BOT_MODE_ATTACK`。

⚠️ 这个修法有 `test_axe_call_staged_frames.lua` 区分的**两种标准里弱的那一种**:
**没有出厂判据可以对齐**,它靠的是该帧自己的 ground truth(上面那四次攻击)。
§1.3 是它**全部**的立足点,并且写成可以变红的断言 —— 那四次攻击少了,§4 的标签就是错的。

2×2 逐格(§4.1 / §4.2),**只有一格开火**:

```
armed=nil          modeRepaired=false  -> desire 0
armed=nil          modeRepaired=true   -> desire 0     <- 这就是缺陷:Axe 已交战、三人在 AoE 里、Call 就绪,出厂说不
armed=axecallring  modeRepaired=false  -> desire 0
armed=axecallring  modeRepaired=true   -> desire 0.75  motive 'Q-环2'
```

---

## 5. 验证读数(逐条裸读)

```
tests/test_axe_call_ring_anchor.lua        EXIT=0   19 tests, 0 failures      (0.72s)
bash tools/agent/mutstand_axecallring.sh   MUT_EXIT=0   killed 12, not killed 0
bash tools/agent/luacheck_gate.sh          GATE_EXIT=0  luacheck bots game: 0 warnings
```

变异体:M1 id 改名 / M2 去 turbo / M3 pullcad 陷阱 / M4 去掉环判据 / **M5 去掉免疫项** /
M6 去掉 IsDisabled / M7 nil 默认翻成放行 / M8 法定人数 2→1 / M9 出货点提前 /
M10 调用点丢掉 -90 余量 / **M11 普查忘记 `tests/frames/`** / **M12 大招弃权**。

### ⭐⭐ 5.1 变异台报过一个**从未存在过的存活者**,修的是台子不是变异体

M9 第一版用四行锚点定位 `X.ConsiderQ` 的先手支路:

```
	--攻击敌人时
	if J.IsGoingOnSomeone( bot )
	then
		if J.IsValidHero( botTarget )
```

**`X.ConsiderW` 开头四行逐字相同**(第五行才分家:`nRadius - 90` 对 `nCastRange`)
⇒ python 的 `assert count == 1` 失败 ⇒ **文件没被改** ⇒ 套件照绿 ⇒ 台子打印 **SURVIVED**。

⇒ 已给台子加一条闸:**任何变异没打上去就 abort**(`python3 - <<'PY' || { echo ABORT...; exit 2; }`),
「没跑成」不再冒充「跑了没杀掉」。⚠️ **兄弟台 `mutstand_axecullbm.sh` 今天仍有同一个洞**,已写进 backlog。

### 5.2 M11 的 `want` 是被改过的,而改的理由是算术不是口味

M11(普查只走 `tests/fixtures/`)第一版 `want` 写的是 §2.2 的法定人数下界,实跑打 **WRONG MESSAGE**:
`run_tests` **按名字排序**,§2.1 的**人口规模**断言先红(`24 rows, recorded 28`),§2.2 根本轮不到说话。
两条都红,**但只有一条能当消息**。

---

## 6. 附带一条(量具,不是主体):推送闸的 Lua 覆盖面

开工自检 `lua-coverage` 腿本轮点名 **3 个 NEW UNCOVERED**,其中
`tests/test_axe_cull_blade_mail.lua` 是**本组上一轮自己造出来的**(backlog `-183` 已交棒)。

**按 backlog 的处方先实测秒数,再决定**:

```
test_axe_call_ring_anchor   0.72s
test_axe_cull_blade_mail    2.05s      (per_test_cap_seconds = 5.5)
```

⇒ 两个都在 cap 之内,推送闸累计 **239.4s / 300.0s** 还有余量 ⇒ **两行手工登记进
`tools/agent/lua_gate_manifest.json`**,累计变成 **242.192s / 300.0s**。

⛔ **没有跑 `lua_gate_measure.py`**:GH #783 —— 全量重测会**清空 `known_red` 赦免名单**(现 6 条),
把下一个人的 push 挡死。每行的 `reason` 逐字写明「个别实测,不是套件重测」,
并且**故意不动 `measured_at`**:这两个秒数是在**另一台容器**上取的,而 GH #810 说成员资格由绝对秒数决定
⇒ 一个 `measured_at` 描述不了两次测量。

⚠️ 本组新落地的 `test_axe_call_ring_anchor.lua` 首行**同时**带 `[ratchet]` 标签 ——
这是**冗余的第二个读者**,不是替代:自检那条腿今天 120s 没跑完(§0),
**加文件只会让它更糟**,所以真正承重的是 manifest 那一行。

---

## 7. 交棒(铁律 9 连带规则)

- **`iterations/queue.json` hero-91**(零 EC2、优先级 3、只读归档):
  数 (1) 支路层事件、(2) **大招没抢先**的那一部分 = 端到端域、(3) 环内恰好 1 人的对照。
  ⚠️ 与 hero-2 / hero-13 / hero-89 **同一个承载者阻塞**(888/895/896/906 零 Axe;`--find axe` 给 899/910/911)
  ⇒ **四条可以一次扫完**。⛔ 0 事件 ⇒ **DO-NOT-ARM 不是 reject**。
- **给总监一条建议**(不是动作):`axecallring` 与 `axecallbkb_ii` **不得同腿 arm**,
  建议按 GH #820 的形状在 `promote_atoms.json` 加一行栏杆。
- **GH #794 建议关闭**:两组已各自结清(英雄组 09-13 裁定 (乙) + 09-14 补第五个普查;录像组 09-13 落地 fixture),
  **无遗留动作**。⚠️ 本轮 `mcp__github__issue_write` 未尝试(铁律 11:不在权限提示上坐等),这一条**写在报告里**。

---

## 8. 下一轮主体第一候选

1. **`mutstand_axecullbm.sh` 补上 §5.1 那条闸** —— 同一个洞,同一个组,已知形状;
   ⚠️ 它是**量具**,按 P4.4 只能当附带,**不能当主体**。
2. **Axe `X.ConsiderW` 打野支路的 `nCastRange + 100`**:搜 100 米之外,却没有任何 reach 判据
   (团战支路有 `X.axe_IsHungerFightTargetInReach`,打野支路没有)—— `liondrainreach` 同族,**未量过域**。
3. **`X.ConsiderQ` 带线支路的 `#hAllyList <= 2`**:`axecallclock` 的 LIMIT 段第 4 条明说「是另一个 id 的事」,
   **那张欠条还没人接**。

---

## 8b. 推送闸三行读数(铁律 6,GH #624 RULING 9)

分支推送 `ff6b30b7`(rebase 前)上三条腿的原文读数:

```
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)   luacheck bots game: 0 warnings
py gate:  94 ran, 0 findings, 0 uncertifiable, 15.2s
lua gate: 391 ran, 0 findings, 0 uncertifiable, 10 unanswered, 6 known-red, 740.2s
```

**没有用 `RULE6_BYPASS`**,三条腿都真的跑了。

⚠️ **两条要登记的超支,都不是本轮造成的,但都是本轮量到的**:
- **py 腿 15.18s 对 12.0s 预算**,超的部分是 **7 个不在 manifest 里的新测试**照跑(fail-open)——
  正是 **GH #839** 说的那 7 个;
- ⛔ **lua 腿 740.2s 对 300.0s 预算(247%)** —— **GH #804** 立案时实测 587.2s/300.0s(196%),
  **本轮这台容器上更糟**。另有 **10 个 unanswered**。
  ⇒ 一次 push 的实际墙钟是 **12 分钟**,而本轮因为要重新 rebase,这个代价要付两次。

⚠️ **rebase 之后的 `HEAD:main` 推送重跑同样三条腿**;上面引的是**分支那一次**的读数,
**不是** main 那一次的 —— 两次是两个树(main 上又多了协同组的 `wardcomma` 两个提交),
本报告不把前者冒充后者。rebase 后本地复跑:`test_axe_call_ring_anchor` **19/0**、
`luacheck` **0 警告 / GATE_EXIT=0**。

⚠️ **rebase 撞了两处冲突,都是「两组各自往文件尾部追加」的形状**,
`iterations/state.json`(`wardcomma_20260915` 对 `axecallring_20260915`)与
`iterations/queue.json`(`strategy-48` 对 `hero-91`)—— **两边都保留**,
解完各自 `json.loads` 验证过,没有丢掉协同组任何一行。

---

## 9. 本次会话 token 用量(铁律 8)

```
TOKENS total_in=14,630,193 out=83,623 turns=85
```
