# 英雄组 2026-09-06T20:03Z —— GH #570 的立案证据在真实帧上被证伪:那 2 次「砍进 False Promise 窗口」都是击杀

| | |
|---|---|
| **工作单元** | 认领 GH **#570**([hero],带帧证据,球在本组)。焦点英雄 **Axe**。 |
| **OWNER_PRIORITIES** | **P4.4 (ii)**「一个判定完结所需的最后一块证据」。**不是 (i)** —— 本轮**故意没有往 `bots/` 落任何东西**,理由就是下面 §2–§4。 |
| **AWS** | 只读 S3(**1** 个 `.dem` 下载 23.5MB + 缓存的 dumper 二进制,cache HIT),**零 EC2、零波次**。 |
| **落地** | `bots/` **零改动**。新 `tests/test_axe_cull_promise_premise.lua`(**10/10**)、新 staged 帧 `tests/frames/f_260828_124358_axe_cull_promise.lua`、新 `tools/agent/mutstand_cullpromise.sh`(**9 变异 9/9 CAUGHT**)、`tests/frames/README.md` 新增一行、`state.json:cullpromise_20260906`、`queue.json:hero-39`。 |
| **建议标签** | **PREMISE-FALSIFIED**,归总监裁。 |

---

## 0. 先说结论

GH #570 要把 `modifier_oracle_false_promise_timer` 加进
`bots/BotLib/hero_axe.lua` 的 `X.HasSpecialModifier` 否决名单,立案证据是
**「全语料 449 次 `axe_culling_blade` 里,2 次落在目标的 False Promise 窗口内部」**。

**那 2 次都是击杀。**

判据是**闭区间**「目标名下 `[MODIFIER_ADD, MODIFIER_REMOVE]` 区间包含施法时刻」,
而这 2 次**都恰好压在右端点上** —— 那个 `MODIFIER_REMOVE` 就是这一刀自己的击杀
把 buff 摘掉。**半开区间下计数是 0,不是 2。**

而且这不只是「证据不成立」,是**符号反了**:全树唯一一帧能让这条否决触发的真实帧上,
它删掉的那一刀**是一次人头**。

我按 -108 撤下 `liondrainbkb` 的同一条规矩办:**不解释那 2 帧,撤案**;
真实的那一半(遗漏本身)保留并**钉成断言**。

---

## 1. 证据是本组自己从 `.dem` 重导的,不是转述

⚠️ 立案方(录像组)的表格没有被引用为证据。#570 为它那 2 次命中**点名了同一局**,
而那 2 次是**全部**的正面证据 —— 所以重导这一局就能结掉这 2 次,这不是抽样。

```
bash tools/batch_test/aws/session_setup.sh
BIN=$(bash tools/batch_test/behavioral/get_dumper.sh)          # cache HIT
awsx s3 cp s3://dota2bot-batch-results-4924/replays/\
20260828_124358_slot1__spot_20260828_121642_1_\
4b2ee33416df1f41617387c93838052296c2aab7_11c470.dem g.dem      # 23.5 MB
"$BIN" -interval 0.1 g.dem > timeline.json
```

## 2. 那局的 5 次 Culling,**5 次都是击杀**

| 施法 t | 目标 | 同一 tick 的 `DEATH ... inflictor=axe_culling_blade` |
|---|---|---|
| 958.7 | sniper | ✅ 伤害 65,Axe +586 +222 金、+6882 经验 |
| 963.9 | oracle | ✅ 伤害 447 |
| 1043.9 | bristleback | ✅ 伤害 221 |
| 1203.9 | phantom_assassin | ✅ 伤害 16 |
| 1452.9 | oracle | ✅ 伤害 223,Axe +650 +170 金 |

**#570 抽取证据的这一局里,一次白刀都没有。** 白刀正是这条 issue 需要而没有的东西。

## 3. 破绽在窗口时长里

那局全部 6 个 `modifier_oracle_false_promise_timer` 窗口:

| 目标 | 区间 | 时长 | 闭区间命中 | 半开命中 |
|---|---|---|---|---|
| bristleback | [374.4, 381.4] | **7.0s** | 0 | 0 |
| oracle | [605.2, 613.7] | **8.5s** | 0 | 0 |
| oracle | [799.5, 808.0] | **8.5s** | 0 | 0 |
| sniper | [953.8, **958.7**] | **4.9s** | **1** | **0** |
| ember_spirit | [1174.8, 1183.3] | **8.5s** | 0 | 0 |
| oracle | [1452.7, **1452.9**] | **0.2s** | **1** | **0** |

⭐ **没被砍的四个跑 7.0 / 8.5 / 8.5 / 8.5;被记为命中的两个跑 4.9 和 0.2。**
命中的恰好就是**被所计数的那个事件本身掐断**的两个。第二个尤其无从辩解:
oracle 在 1452.7 给**自己**上 False Promise,**0.2 秒后**被斩,死了 —— 0.2 秒
不可能是任何等级的自然时长(该局 oracle 的 `oracle_false_promise` 是 **3 级**)。

## 4. 装上这条否决要赔一次人头 —— 在真实帧上量的

新 staged 帧 `tests/frames/f_260828_124358_axe_cull_promise.lua`
(`make_fixture.py --t 1452.8 --hero axe`,即施法**前 0.1 秒**的决策瞬间):

* oracle:**437 血**、活着、可见、距 Axe **153.7u**(施法距离 175 内)、
  `HasModifier('modifier_oracle_false_promise_timer') == true`;
* Axe:**30 级**,Culling **3 级**、`IsFullyCastable()`;
* **出货 `X.ConsiderR()` 给 `BOT_ACTION_DESIRE_HIGH` + 目标 oracle**(§3 断言);
* 把 #570 要的否决作为**带标签的注入**装上去(§4,先断言注入**真的读到了这一帧**
  再下结论)⇒ desire 变成 **NONE**;
* 而战报说这一刀 **0.1 秒后杀死了 oracle**。

⚠️ **方向纪律**:被提议的杠杆是 **NARROWING**,armed 只会**删除**施法。
负读数**只能**读成「否决删掉了 N 次施法」,**绝不能**读成「少浪费了 N 次大招」。
本轮量到的就是被删掉的那两次都是人头。

## 5. 真实的那一半,保留并钉成断言(§2)

`X.HasSpecialModifier` 确实**只列了七个名字**,确实**没有** Oracle 那一个,
而 `bots/` 下另有 **228 处**调用点在否决 `modifier_oracle_false_promise_timer`
(另有 3 处 hero 文件写的是不带 `_timer` 的拼法 —— 语料里**不出现**那个拼法,
属另一个问题,本轮不答)。

被推翻的**不是遗漏本身,是「这个遗漏曾经付出过代价」**。供给现在是 **0/449**,
而在本仓,只有理论依据 (c) 不足以发一次行为改动。

## 6. ⚠️ 量具层面的同族缺陷 —— 不归本组修,点名交出去

同一个仓,两个量具,**两种边界约定**,都在源码上钉住了(§7 读源码不重打):

| 量具 | 判据 | |
|---|---|---|
| `tools/batch_test/behavioral/cullthresh_domain.py:215` | `a <= t <= b` | **闭** ← 产出 #570 那个 2 |
| `tools/batch_test/replayscope/make_fixture.py` `active_modifiers` | `s <= t < e` | **半开** |

⭐ **一般形状:凡是「某 modifier 窗口内发生了 X」的计数,只要 X 会让那个 modifier
被摘掉(击杀是最常见的一种),闭区间就会把「X 终结了窗口」记成「X 发生在窗口里」——
而这两件事符号相反。** 建议总监按 [harness] 立案给量具侧。

## 7. 验证(铁律 6)

| 门 | 命令 | 结果 |
|---|---|---|
| 静态 | `bash tools/agent/luacheck_gate.sh` | **EXIT=0 CLEAN**,`luacheck bots game: 0 warnings`(容器缺 luacheck,gate 自己装的) |
| 动态(相关子集) | `lua5.1 tests/run_tests.lua axe` | **211 例 0 失败** |
| 动态(新文件) | `lua5.1 tests/run_tests.lua axe_cull_promise_premise` | **10 例 0 失败** |
| 变异台 | `bash tools/agent/mutstand_cullpromise.sh` | **9/9 CAUGHT**,stand EXIT=0 |
| 动态(全量) | `lua5.1 tests/run_tests.lua` | 见 §9 |

变异台里值得读的三个:

* **M1 = 复活绊线**。后人若真按 #570 的散文把名字加进出货名单,红的那一行直接叫他
  **去 reopen #570**,而不是改断言。
* **M5 = 「能让这个杠杆复活的那一帧」的模拟**。把一个窗口延长到施法之后,
  §6 的半开零**当场变红并点名那一帧**。一个「前提被证伪」的裁定,只有在它**能被新供给
  反过来推翻**时才是诚实的;M5 就是这件事的证明。
* **M3 = 注入控制**。把 §4 注入的否决改成空转 ⇒ §4 必须停止通过,否则它断言的是
  「一条从没读过这一帧的否决会改变这一帧」。

(M1 第一版报 `RED but with the WRONG MESSAGE` —— 触发的是名单**计数**那条断言而不是
点名那条。已把点名那条挪到计数之前,并让「帧上 `HasSpecialModifier` 为 false」那条
自己带上 reopen 指引。这是变异台**按设计**该抓的东西:红得对不等于红得**说得清**。)

## 8. 诚实边界(引用本报告任何数字前先读)

1. **一局。** 所有读数出自 #570 自己点名的那一局。它结掉那 2 次命中,**没有**重扫
   另外 68 局,不许当成重扫过。**449 / 69 这两个数是请求方的**,原样引用。
2. **promise 为什么结束,本轮不判。** 引擎是否让 Culling Blade 穿透 False Promise
   击杀,是本台离线 settle 不了的引擎问题(同 GH #564 的「读法无关」)。
   **两种读法下结论相同**:目标死了、战报把这一刀写成 inflictor、否决会删掉这一刀。
   **结论对机制不敏感,这正是它写成这样的原因。**
3. **§5 的战报行是冻结成数据的读数,不是对活文件的断言。** 只有**帧**是对着一个
   被加载的世界断言的。
4. **帧是 staged 不是入语料的。** 放 `tests/frames/`、按名字读。它是全树第一帧
   **t > 1400** 的帧(语料到 t≈790 / 19 级),**入语料的价目表本轮没有量**,
   也**没有**声称任何数字 —— 按 `tests/frames/README.md` 的老规矩,付那张单子是
   **另一个工作单元**。
5. **供给是两句话不是一句**:`tests/fixtures/` 109 帧**零个**单位挂过这个 modifier;
   `tests/frames/` 里现在**恰好有一个**。§8 把这两句分开断言。

## 9. 开工自检 / 全量套件

* `bash tools/agent/routine_selfcheck.sh`(**非管道读退出码**)⇒ **worst exit 3**。
  FINDINGS: `cadence` / `queue-rulings` / `owed-executions`;
  **UNCERTIFIABLE(exit 2)**: `trunk-red(python)` —— `tests/test_rc_wrapper.py` 与
  `tests/test_selfcheck_lua_leg.py` **没跑成**(GH **#548**,总监 19:25Z 已在跟)。
  **这三项都不是本组的**;`unlanded-commit` 与推送门两条干净(push gate 已上膛,
  `core.hooksPath=.githooks`)。
  ⚠️ 第一次调用被脚本**主动拒绝**(`stdout is a PIPE`,退出码是 `tail` 的),
  改成写文件再读 `$?` —— 证据纪律第 3 条,脚本自己挡住了。
* 全量套件按 GH #124 在容器里跑不完一个进程,后台跑到会话结束的读数记在这里:
  **未跑完**(相关子集 `axe` 211 例已全绿)。**这不是通过,是没跑完。**

## 10. 下一棒(不许掉)

* **批测台** `queue.json:hero-39`(零 EC2 归档只读,与 hero-2/30…38 同形,
  **十一条可并成同一次遍历**):用**半开**判据在 69 局上重数,并把「击杀刀」与
  **「窗口内空刀」**(施法在窗口内 **且** 目标其后 3 秒无 DEATH 行)分成两列。
  第 (2) 列才是 #570 想说的那件事,而闭区间那一列**从构造上分不出它**。
  ⚠️ 执行前先确认脚本换成半开判据,否则会原样复现 #570 的那个 2。
* **总监**:裁 GH #570 的标签(建议 **PREMISE-FALSIFIED**);另请按 §6 给量具侧
  立一条 [harness](闭区间计数在「X 会摘掉该 modifier」时的一般性高估)。
  **本轮没有任何入集提议要裁** —— P4.2 冻结期内,本组这一轮没有要往 armed 集里加的东西。

## 11. Token 用量

见下节(收尾时 `python3 tools/agent/token_usage.py` 的最后一行,原样粘贴)。
