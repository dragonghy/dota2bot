# hero 2026-08-28T07:47Z — 棒 ③ 第 4 行:被判掉的不只是那个 0,还有「队列后面什么都没有」

轴 **`LEVELPREMISE` 重读续**。`bots/` **零行改动**(一个英雄文件都没碰)。无新 gate id。
`state.json` 新增 `levelpremise_focusbuild_20260828`(`gated:false`)。**零 AWS**。
不申请入集,**不提 queue**。**新开 GH #274**(trunk 红,非本轮造成)。

---

## 1. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` → **worst exit 3**。

- UNLANDED **0**;两条稳定版锚点(`stable-v1` / `stable-v2`)**ok**;
  待裁 queue 请求 **0**(open 37)。
- cadence 洞:**replay-check 6.1h**、**strategy 6.2h**;**本组无洞**。
- **trunk RED**:`tests/test_write_only_local_census.py` 1 例失败(46 passed)。
  **这一条不是本轮造成的,详见 §4。**
- Lua 快速检测器 **16** 文件 0 失败。

选题(章程工作流 1):`iterations/OWNER_PRIORITIES.md` 四条优先项**没有一条球在本组**
(常设运维在批测台、P1 在协同组、P2/P3 不在本组)。`[hero]` open issue 里带帧证据且属焦点五的
**一条都没有**。⇒ 按 backlog 取**上一棒点名的** `tests/test_focus_build_level_legality.lua`
(棒 ③ 第 4 行,登记理由 `hero -- scope claim argued from the zero`)。

---

## 2. 这一行为什么归本轮:它的登记理由正好是被上一条判据改写得最直接的那句

登记表 `PENDING` 里这一行的理由写着 **scope claim argued from the zero** ——
文件把 t20/t25 那两个队列位**排除在自己的 scope 之外**,而排除的论证里有 GH #84 的 0。
08-28T01:58Z 那轮换上来的替代判据是**「能不能取到 = 出装,不是等级」**。
这一行是全表里**唯一一处「用等级论证排除了一段自己本可以定价的东西」**的行。

原文(`WHAT IS NOT CLAIMED` 第四条,已删):

> Beyond level 17 the queue holds only talents, and the rows put t20 at queue
> position 18 and t25 at 19 — both blocked until 20 / 25. That is out of this
> test's scope AND out of turbo's domain (GH #84 read level >= 20 on 0 of 210
> hero-slots), and it costs nothing for the same reason the level-17 park does:
> **by then there is nothing else in the queue**.

**两条论证,两条都退休了。**

---

## 3. 本轮买到的两个读数

### (a) 第一条论证:那个 0 是量具,而且这里特别刺眼

GH #235 把「够不着」拿掉是旧闻。**新的是它落在哪里**:那枚已录快照
(`tests/mock/lategame_talent_frame.lua`,源自停在 `iterations/pending/` 的
24.9 分钟自然结束局 t=1382.2)里 —— **crystal_maiden 22、zuus 23,
正正落在这条 bullet 宣称够不着的 21–24 停车带里面**,skeleton_king 26 在带子外侧。
两个焦点英雄,不是路人。§4d 把这三个数钉住了。

### (b) 第二条论证:**「队列后面什么都没有」是可测的假**

驱动出货的 `J.Skill.GetTalentBuild` + `J.Skill.GetSkillList`,**七行焦点构筑全跑**
(axe / zuus pos_2 / zuus pos_4/5 / wk 默认 / wk 的 gated `wkbuild` / lion / crystal_maiden):

- 七行的技能行**都是 15 条** ⇒ `totalSlots = 15 + 8 = 23`;
- **四个选中的天赋在队列位 10 / 15 / 18 / 19**(**不是** 10/15/20/25 ——
  `i >= 10 and (i % 5 == 0 or ability_idx > #nAbilityBuildList)`,技能行用完会提前插);
- **四个被放弃的半边在 20 / 21 / 22 / 23**;24 位之后为空。

⇒ 队列后面**有四个东西**,而且**出货的 bot 每一个都向引擎要过**。
这把 05:xxZ 那轮在 **lion 一个英雄**上的读数**推广到全部七行**。

### (c) 换上去的判据:一句等级都不看

结论(两处停车都不花钱、都在 scope 外)**活下来了,它的理由没有**。新的理由是:

> **停着的队首后面的每一个条目,都是「同一条队列里更早已经点掉的那一档的另一半」;
> 一个英雄一档只点一个 ⇒ 它在任何等级都取不到。**

§4b 把这一条**断在 25 级**上 —— 天赋阶梯的顶。所以它**不可能靠等级上限回来而被满足**。
level-19 那处停车确实需要一个等级事实,于是**只给它一个**:它后面**只有一个条目**,
就是 t25 的选中项,而 25 > 19。**过去用一份语料普查去豁免一整个带子,现在用出装。**

---

## 4. 顺手抓到、并且交出去的:trunk 现在是红的,不是本轮造成

`b50a7727`(replay-check 07:05Z)加了两枚 venomancer fixture。一帧十个 hero-slot,
于是**四个登记过的语料计数同时移位,三个文件变红**:

| 文件 | 断言 | 登记值 | 现值 |
|---|---|---|---|
| `test_axe_t15_payoff.lua:378` | live-Axe 帧数 | 26 | **28** |
| `test_axe_t15_payoff.lua:398` | Call uptime ceiling 求和 | ~1.94 | **2.33** |
| `test_wk_bone_guard_talent_bypass.lua:269/:384` | WK 帧数 | 34 | **36** |
| `test_wk_bone_guard_talent_bypass.lua:410` | 只给 HasModifier 就越过门的帧数 | 20 | **22** |
| `test_write_only_local_census.py` §4 | carrier fixtures | 62 | **64** |

**`git stash` 后在 HEAD 上逐位复现**,与本轮改动无关。

**刻意不就地改数字。** 其中两个数**驮着已发表的判决**:Axe 那个 ceiling
**正是让「Battle Hunger 只在 1 帧上活着」读作 SATURATED 而不是 NEGLECTED 的那个数**
(失败信息自己这么写);WK 那个文件在 `:269` 明文要求
「**re-read the whole census before quoting any number from this file (GH #106)**」。
把新数字抄进去 = 用便宜的编辑顶替昂贵的重读 —— **这正是 level-premise 登记表存在的理由**。
⇒ 开 **GH #274**,写清三步验收(hero 重取两份读数 / harness 改那个纯分母 /
以及「加 fixture 的那一轮要不要在同一改动里重取计数」这个第三次出现的形状问题)。

---

## 5. 变异测试(5 抓 + 1 对照)

| id | 变异 | 结果 |
|---|---|---|
| M1 | `GetTalentBuild` 不再返回第 [5] 行 | **CAUGHT**(4a + 4b) |
| M2 | `GetSkillList` 去掉 `ability_idx > #nAbilityBuildList` 子句 | **CAUGHT**(4a + 4b) |
| M3 | `tier_of_slot` 分组错一位 | **CAUGHT**(4b) |
| M4 | 判据不再把「非天赋条目」当活的 | **CAUGHT**(4c 合成 offender) |
| M5 | 判据丢掉档位等级那一测 | **CAUGHT**(4c near miss) |
| M6 | 只重写头部注释(对照) | **ESCAPED**,设计如此 |

**M1 第一轮先逃过 4a**:`tQueue[p] == sTalentList[nTalentBuild[k]]` 在两边都是 `nil`
时**恒真**,而「天赋构筑不再返回放弃的半边」产生的正是两边都 nil。
⇒ 就地补了非空断言(`#nTalentBuild == 8` + 尾部四位必须是 string),现在 4a/4b 双抓。
**这是本文件 §3 那条老课的又一面:合成 offender 之外,还要防断言被空值满足。**
变异后 `bots/` 全部还原(`git diff --stat` 只剩两个 tests 文件)。

---

## 6. 诚实边界

- 构筑行读自**源码字面量**(`skillmap.build_row`,与 §2 同一个读法),不是从调用截获 ——
  七行里有两行(zuus pos_4/5、gated `wkbuild`)是**文件加载时根本不会传的备选行**,没有调用可截。
  §4a **在 axe 上把 routing 钉了一次**(`dofile` 期间拦截真的 `GetSkillList`),
  所以「这里构造的队列」与「bot 真正消费的队列」至少有一行是同一个对象。
- 天赋**名**来自 `tests/mock/talent_slots.lua`(mock bot 的技能槽 > 5 为空,
  同 `test_lion_hex_talent_slot.lua` 记的限制)。槽→档是快照自己写明的顺序上的算术。
- 「永远非法」是游戏的**一档一个天赋**规则,在这里是**常量**,和 `tBasicReq`/`tUltReq` 一样 ——
  被**驱动**的是「20–23 位真的是已点档位的另一半」。
- 25 级时 spender 发出的那四条注定失败的 `ActionImmediate_LevelAbility`,
  **引擎接不接受不判也判不了**(`print()` 到不了控制台)。
- **本轮无真实帧**;停车带的可达性靠已录快照(GH #235 那枚帧仍停在 `iterations/pending/`,被 #236 挡着)。
- 队列位是**技能行长度的算术**,长度一变 18/19/20–23 全动 —— §4a 因此单独断言 `nRowLen == 15`。

---

## 7. 登记表(棒 ③)

- 行删除,天花板 **6 → 5**,`CLEARED` 节写下**重读决定了什么**(登记表自己的规矩:
  一行离开是因为判决被重读,不是因为句子被删)。
- **棘轮自己先响的**:头部一改好,`test_level_premise_registry.lua` 立刻变红并点名
  「这一行不再带未更正前提了,去删它并降天花板」。**机制在工作。**

---

## 8. 铁律 6

- `bash tools/agent/luacheck_gate.sh` → **`luacheck bots game: 0 warnings`,exit 0**
  (容器本来没有 luacheck,gate 自己装的)。push gate 已上膛
  (`core.hooksPath=.githooks`)。**未使用 `RULE6_BYPASS`。**
- 全量套件一进程跑不完(GH #124)⇒ 影响面按过滤器单跑:
  `focus_build_level_legality` **10/0**、`level_premise` **5/0**、`lion` **99/0**、
  `zuus` **110/0**、`smoke_load` **3/0**。
- **`axe` 2 红 / `talent` 3 红 = §4 的 GH #274,已在 HEAD 上复现,非本轮。**

---

## 9. 下一棒

1. **优先 GH #274 的第 1 步**(本组那半):在 64 文件语料上**重取** Axe t15 读数与
   WK Bone Guard 普查,并明确记下**判决是否变化**(Battle Hunger 在新 ceiling 下
   还算不算 saturated)。这现在比登记表剩下的行更值钱 —— **trunk 是红的**。
2. 之后回棒 ③:剩 **5 行**(本组欠 **3**:`test_wk_roshan_mana_ceiling.lua` /
   `test_wk_bone_guard_thresholds.lua` / `test_wk_q_aim_preflight.lua`;
   `corpus_scale` 与 `test_level_gate_census` 归 harness/总监)。
   三行**全是 WK**,而 GH #274 也要重读一个 WK 语料文件 ⇒ **建议并成一棒做**。
3. `hero-19` / `hero-20` / `hero-21` 仍 pending;WK `ConsiderQ` 的 `nDamage` 修复仍等归档扫描;
   **GH #268 交出去的那一格未被认领,不由本组扩。**

---

## 10. Token 用量

```
TOKENS total_in=5,220,400 out=41,921 turns=45
```
(到统计时刻为止;统计后的收尾回合不计入。)
