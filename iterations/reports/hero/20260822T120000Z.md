# 英雄组 2026-08-22T12:00Z — 回收上一轮(和上上一轮)丢掉的树

**一句话**:本轮不是一次发现,是一次**回收**。英雄组连续两轮的产出都不在 `main` 上 ——
08:00Z 只把结论发到 GH #104、一行代码都没推;10:00Z 把那件事重建、独立复算、提交成
`e940d31` 推到分支 `claude/vibrant-heisenberg-2z3akc`,**也没落 main**。本轮没有付钱重做第三遍,
而是把 `e940d31` 接上来,**但故意剔掉其中一个别的组已认领的钉数文件**;顺手给
`[harness] GH #112` 交了它正需要的那个隔离读数。

**零行为改动、零新 gated id、零 EC2/S3**(`bots/` 与 `game/` diff 为空)。

---

## 1. 认领判断

- **Owner 优先项**:`iterations/OWNER_PRIORITIES.md` 的 P1(拉野)与 P2(低血不回家)责任链
  当前都指向**协同组**,本组无未完成优先项 ⇒ 按铁律 9 走 issue 流。
- **`[hero]` open issue 扫描**:#97 / #85 / #73 / #59 是本组自己的 DO-NOT-ARM / park 记录;
  #63 已于 00:30Z 做完;#56 卡在 `[harness] #60`;#54 的 (a) 买不到;#66 要语料;
  #104 是本组这三轮的主线。
- **在 #104 上发现了本轮真正该做的事**:它最新一条评论(08:25:52Z)描述的交付物
  **在仓库里一件都不存在**,而 10:00Z 那一轮已经查明并重建了它 —— 结果**那一轮自己也丢了**。
  ⇒ 本轮的工作单元 = 把它接上来,而不是让第三轮再付一次同样的钱。

## 2. 会话中途远端动了,以及一个差点写出错误 A/B 的坑

开工时 `git ls-remote origin main` = `1443e2a`;建 worktree 时已经是 **`37c0a0e`**
(总监 11:00Z 收 `stayfield`、11:5xZ 补收 `pullcamp`)。本轮所有读数与 rebase 都按 `37c0a0e` 重做。

**坑(值得别的组抄走)**:本容器的**远端跟踪引用 `origin/main` 是陈旧的** —— 它指向
`46d381d`,一百来个提交以前。于是

```
git worktree add --detach <path> origin/main
```

**静默**给了一棵 **39 个 fixture** 的旧树(今天是 100)。第一次 A/B 就是在那上面跑起来的,
如果不是顺手 `ls tests/fixtures/*.lua | wc -l` 核了一下,本轮会发表一份完全错的隔离读数。

> **做任何隔离测量之前先 `git fetch origin main`,并核对 worktree 的 fixture 数。**
> `git rev-parse HEAD` 与 `git ls-remote` 一致**不蕴含** `origin/main` 这个本地引用是新的。

## 3. 落地了什么(`e940d31` 原样,rebase 到 `37c0a0e`)

| 文件 | 内容 |
|---|---|
| `tests/mock/replay_fixture.lua` | `bot:GetNearbyHeroes` 按**距离升序**返回(unit name 破平),依据 `docs/BOT_API_REFERENCE.md:1229` 对整个 `GetNearby*` 家族的契约 |
| `tests/test_mock_nearby_heroes_order.lua` | 新增,10 例 |
| `tests/test_wk_considerq_level7_dominance.lua` | GH #104 §4 的三处措辞更正(「引擎列出的第一个」→「最近」)+ 新增一例把该帧钉成最近优先;14 例 |
| `iterations/reports/hero/20260822T100000Z.md` | 10:00Z 的报告本体 |
| `iterations/streams/hero.md` / `state.json` / `queue.json` | 章程条目、`mock_nearby_hero_order_20260822T1000Z`、`hero-1`(与协同组的 `strategy-1` 保留双方) |

缺陷本身(10:00Z 的原始诊断,本轮复核通过):`make_fixture.py` 用 `for h in sorted(per)`
按**英雄名字母序**写 `fx.units`,loader 原样发回 ⇒ 每个 fixture 驱动的测试里 `list[1]`
是**字母序第一**而不是**最近**。

## 4. 故意**没**落地的那一半

`e940d31` 还把 `tests/test_itemdesire_world_assertion.lua` 的三个等式重钉成
**209→207 / 179→177 / 672→674**。**本轮把这个 hunk 丢掉了**,三条理由都**晚于** 10:00Z:

1. **协同组已经认领了那个文件**(`[harness] GH #112`,其 backlog `0R`),并在 issue 上
   **白纸黑字要求**:在 `crash_2597` 的逐 fixture 分解出来之前,**任何人不要只把 179 改成 178**
   —— 「那会让下一次真的行为变化再次伪装成漂移」。总监 09:xxZ 出于同一理由也拒绝代改
   (`state.json:main_RED_itemdesire_pins_20260822T09xxZ` 的 `why_not_fixed_here`)。
2. **那三个值本身已经过期**:#112 自己的评论记着同一个数在 **96 / 98 / 100** fixture 上读
   **179 / 177 / 178**,而今天的语料是 **100**。10:00Z 是在 98 上量的。

**而在本轮动手期间,第三条理由变成了最硬的一条:那个文件已经不红了 —— 它的主人自己修好了。**
协同组 11:13Z / 11:21Z(`280b716` + `1443e2a`)把它重钉到 100-fixture 语料上;
本轮在 `37c0a0e` 的**纯净 worktree** 上实测 **24 例 0 失败**。两套钉数逐位对照:

| | `driven` | `crash_total` | `no_action` | `crash_2597` |
|---|---|---|---|---|
| `e940d31`(98 语料) | 882 | 207 | 674 | 177 |
| **今天的 main**(100 语料) | **901** | **210** | **690** | **178** |

⇒ **把丢掉的树整棵接上来,会让四条断言当场变红,而且是把主人八分钟前刚修好的文件重新弄坏。**
「回收一棵丢掉的树」不是 `git cherry-pick` 一下就完事:**丢的那段时间里,世界会继续动。**
每一个等式钉数都必须在**今天的**树上重跑,而不是相信提交信息里那句「1150 tests, 0 failures」。

## 5. 本轮自己量的四件事(不是抄 10:00Z 的)

### ① 两个恢复的测试文件在**今天的 100-fixture 语料**上仍绿

提交前重跑:`nearby_heroes_order` **10 例 0 失败**、`level7_dominance` **14 例 0 失败**。
它们写在 **98** 上却没坏 —— **正因为计数写成了上界而不是等式**,即 GH #106 要的那条纪律。
这是它写下来的同一周里第一次收到回报。

> 对照组就在隔壁:**写成一串等式的那个文件,恰恰就是今天早上红掉的那个**(§4)。

### ② 调用点重测:**2280**,不是 2277

`e940d31` 记的是 2277。今天的树上:

| | 数 |
|---|---|
| `GetNearbyHeroes` 全部出现 | **2280** |
| 其中 `J.GetNearbyHeroes` 包装引用 | 1929 |
| 其中直接 `:GetNearbyHeroes(` 方法调用 | 346 |
| 定义行 + 注释掉的调试行 | 5 |

差额是别的组这几小时的提交,不是谁算错。**记的是重测,不是抄** —— 结论(loader 是唯一能弄错
也唯一能修好的地方)不变。

### ③ `J.GetNearbyHeroes` 只过滤不重排 —— 但有一条没人记过的脚注

重读 `bots/FunLib/jmz_func.lua:2517` 确认承重那句是真的:它 `table.insert` 到新表,全程无 sort。

**新脚注**:它用 **`pairs` 而不是 `ipairs`** 遍历引擎返回的列表。在 PUC-Rio Lua 5.1 上,
序列的数组部分按序遍历,所以顺序**事实上**保住了 —— **但语言不保证**。
⇒ 所有关于「`J.GetNearbyHeroes` 返回值顺序」的论断(包括本轮刚落地的那条),
靠的是 **VM 的实现细节**而不是契约。改成 `ipairs` 很便宜,但那是 `bots/` 改动,
要它自己的工作单元和自己的帧证据,本轮不顺手做。

### ④ 给 GH #112 的隔离读数(本轮的真交付)

本组把一个**台架改动**落进了协同组即将拿来做「逐 fixture 分解」的那棵树。所以在它落地**之前**
先把它自己排除掉:

- 同一个提交 **`37c0a0e`**、同一份 **100 fixture / 901 可驱动帧**语料;
- 两个 detached worktree,**只差那一个排序**(`wtB` = `wtA` + `tests/mock/replay_fixture.lua`);
- 各跑 `lua5.1 tests/run_tests.lua itemdesire`。

**结果:两侧都是 `24 tests, 0 failures`,输出逐字节相同。**
即在今天的 100-fixture / 901 可驱动帧语料上,**这个排序对该普查的每一个读数影响 = 0**
(`driven` 901、`crash_total` 210、`no_action` 690、`crash_2597` 178 四个等式两侧全过)。

⇒ **协同组做 `crash_2597` 的逐 fixture 分解时,不需要把本轮这个台架改动列进混杂因子。**
它在落地**之前**就被排除掉了,不是事后辩解。

这比 10:00Z 那次的控制更硬:那次跨的是 **98** 语料上的**不同树**,混进了 `bots/` 差异;
本轮只变一个文件、只在一棵提交上。**诚实边界**:这只证明「排序不动**这个文件的这些读数**」,
不证明「排序不动任何东西」—— 它当然会动瞄准类读数,那正是它存在的理由(见
`test_mock_nearby_heroes_order.lua` 那个真实帧:同一发停晕从 36% 血的 lich 移到 76% 血的 nevermore)。

## 6. 验证(铁律 6)

- `luacheck bots game --formatter plain` — **0 警告**(退出码 0,无输出)。
- `lua5.1 tests/run_tests.lua` 全套 — **本次提交时仍在跑**(本容器单次约 55-65 分钟,总监 09:06Z
  的独立观测一致)。**本轮故意先提交先推**:全套结果在收尾的第二个提交里补上,理由就是本轮的主题 ——
  上一轮和上上一轮的产出正是因为"等一等再推"而全丢了。已跑完的部分:
  `nearby_heroes_order` **10/0**、`level7_dominance` **14/0**、`itemdesire` 在两个纯净 worktree 上
  各 **24/0**;`bots/` 与 `game/` diff 为空 ⇒ 出货行为不可能移动。

## 7. 交接

1. **总监 / harness — 丢树这个失败模式本身。** 今天全队 ~27 轮里有 **3 轮**产出不在 main 上:
   hero 08:00(**只发表,没推**)、hero 10:00(**推了分支,没落 main**)、strategy 07:30
   (推了分支,后来自己和总监补齐了)。总监 03:00Z 那个「pushed but unlanded」检测器
   (`12ef3de`)只看得见**第三种**形状,看不见第一种。**一个便宜且完备的检查**:
   一个 agent 在 GitHub 评论里引用的每一个报告路径 / commit,在发评论那一刻都必须能在
   **某个远端 ref** 上解析出来。
2. **协同组 — GH #112**:排序在它落地**之前**就被排除成混杂因子了,读数见 §5④,本轮已追评。
3. **英雄组下一轮**:`wkqaim` 仍等 `queue.json: hero-1` 的域读数(扫归档语料,**不要新波次**)。
   读数回来之前**一行都不写** —— 改瞄类改动代价侧结构性为零,但域可能是空的(`axeblink` 陷阱)。

## 8. 诚实边界

- 本轮**没有**新的英雄行为知识。§5 的四条里,①②③是对**别人已发表读数**的复核与补脚注,
  ④是一次控制实验;没有一条是关于「在一局里多久发生一次」。
- `e940d31` 的原始诊断(字母序 vs 最近、39.8%、真实帧 `f_225947_wk_trade_kite`)
  **本轮没有从头复算**,只复核了它的三个锚(契约行、`sorted(per)`、包装只过滤)和两个测试文件的绿。
  引用那些数字时请引 10:00Z 的报告,不是本轮的。
- `test_itemdesire_world_assertion` **本轮没修,也不该由本组修** —— 而且在本轮动手期间它的主人
  已经自己修好了(§4)。本轮对它的唯一贡献是那次 A/B 控制,以及**没有**把过期钉数盖回去。

## 9. Token 用量

```
input_tokens (uncached): 320
cache_read_input_tokens: 24,832,345
cache_creation_input_tokens: 485,630
output_tokens: 90,166
TOKENS total_in=25,318,295 out=90,166 turns=160
```

统计时刻在收尾之前(commit / push / GitHub 追评之后的回合不计入)。**给总监的效率账本一条注解**:
本轮的 `out=90,166` 里**没有**新的英雄逻辑 —— 它买的是「把两轮丢掉的产出接回 main」+ 一次控制实验。
按工作单元记账时,本轮应当记在**流程损耗**那一栏,不是英雄打磨那一栏。
