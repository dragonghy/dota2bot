# 英雄组 2026-08-27T13:55Z —— `TALENTPRICE` 第四轮:Crystal Maiden 的 t20/t25 第一次定价,**两行都翻**

**入口不是自选**:GH #238 §六 的棒 ②(「焦点五的十个 t20/t25 选择全是上游默认值,一轮一个英雄,skeleton_king 最后」)。
顺序:lion 05:30Z → axe 08:15Z → zuus 10:55Z → **crystal_maiden 本轮**。棒 ② 之后只剩 skeleton_king。

**本轮 GH:#251。** 登记:`iterations/state.json` → `cmt20t25_20260827`(`gated: false`)。**零 AWS。零新 gate id。不提新 queue 请求**(条件 (a) 骑已 pending 的 `hero-19`)。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` → **worst exit 3**(= 有发现,不是没跑成):

- UNLANDED **0**;稳定版锚点 `stable-v1` / `stable-v2` 不变量 **ok**;
- python 套件 **43 passed / 0 failed / 0 uncertifiable**;Lua 快速检测器 **12 个文件 0 失败**(FAST SUBSET,不是全量);
- 待裁 queue 请求 **0**(open 36);
- cadence 洞:**replay-check 6.3h**、**strategy 3.8h**,**本组无洞**;
- **owner 四条优先项没有一条球在本组**(常设运维在批测台,P1/P2 在协同组,P3 在总监)⇒ 走 issue 流 / 章程棒。

开着的 `[hero]` issue 里带帧证据的三条(#245 / #244 / #54)**都是 Outworld Destroyer**,不在焦点五;本组的棒 ② 明确指到 crystal_maiden,**按章程取棒**。

---

## 1. 改了什么(`bots/` 可执行行改动 = 两个 table 字面量)

`bots/BotLib/hero_crystal_maiden.lua`:

```lua
['t25'] = {0, 10},   ->   ['t25'] = {10, 0},
['t20'] = {10, 0},   ->   ['t20'] = {0, 10},
```

| 层 | 原(上游快照默认,从未被 argue) | 现 | 定价理由的类型 |
|---|---|---|---|
| t20 | [6] `special_bonus_unique_crystal_maiden_3`(冰封禁地 +50 伤害) | **[5] `..._glacial_guard_mana_multiplier`(+20 法力转护盾 %)** | **REACHABILITY** |
| t25 | [7] `..._crystal_maiden_1`(冰封禁锢 +1.0s) | **[8] `..._crystal_maiden_2`(水晶新星 +300)** | **决策层 / 陈旧读数** |

**天赋行不可 gate ⇒ 这是真行为改动**,在每一局打到 20 / 25 级的 turbo 里执行(GH #235:P3 解 cap 后第一枚后期帧十个英雄 22-27 级,**CM 自己 22 级**)。
**这是棒 ② 里第一次两行同时动** —— 它们**为两个不同的理由**动,这也是它们可以同轮动的原因:t20 那一翻**对决策层惰性**(只移动战斗力),t25 那一翻**移除一条已经在活着的失明带**。

## 2. t25:被翻掉的那一半今天正带着一条**活的**失明带

`X.ConsiderW` 用手算重建冰封禁锢的伤害:

```lua
local nDamage = ( 100 + nSkillLV * 50 )     -- 150 / 200 / 250 / 300
```

KV(`tests/mock/special_value_shapes.lua`,由 `special_value_shape_census.py --snapshot` 从游戏 KV 生成):
`damage_per_second = 100`,`duration = 1.5 2 2.5 3` ⇒ 真伤 **150/200/250/300**。**四级逐位相等 —— 硬编码是对的,而且只在「没有天赋碰时长」时对。**

而 t25 原本取的 [7] **正是把 4 级时长抬到 4.0s** ⇒ 真伤 **400**,kill-check 仍报 **300**:
**25 级起系统性低估 25%,方向是「以为杀不掉」** ⇒ `J.WillMagicKillTarget` 不触发 ⇒ **放掉本来拿得到的人头**。
解 cap 之前这条够不到;**现在够得到**。

[8] 落在 `crystal_maiden_crystal_nova/nova_damage` 上,而 `X.ConsiderQImpl` **实时读这个 key**,并把它交给 `bot:FindAoELocation` 做英雄击杀搜索与小兵击杀搜索。引擎把已学天赋折进基数(GH #228)⇒ **260 → 560,而且决策层自己知道**:击杀分支按放大后的阈值出价,**零代码改动**。

⇒ **取 [8] 是把失明带拿掉,不是造一条**。与 **Axe t25 同形、符号相反**(那边取 [8] 会把斩杀盲带乘以七,于是留 [7])。

**放弃了什么(写在前面,不留作以后的意外)**:1 秒单体禁锢 —— 引擎本来白送,本文件也量不到它。取舍是「决策层已经在读的那个数 **+115%**」压过「它看不见的那个时长 **+33%**」,**不是说禁锢不值钱**。

## 3. t20:同一把 REACHABILITY 尺子,而两边对决策层都是瞎的

- **[6] 的计价单位是「channel-seconds held」**:+50 **每一次爆炸**付一次,频道一断就停付。
  **本仓逐帧读过的两次冰封禁地频道**(都钉在这个文件自己的守卫注释里):
  - `20260819_003005_slot1` t=373.4:开引导 **0.6s** 被 Jakiro 晕住(**最大时长的 6%**),**5.9s 后死**;
  - `20260820_103216_slot1` t=473.5:**26% 血、身上带着晕**就开了 10 秒自缚引导,**1.0s 后死**。

  而三个会守住「什么时候开大」的候选 —— `cmrguard` / `cmrcap` / `cmrself` —— **全都还是 soak candidate** ⇒ **发布默认恰恰就在那些情形下开大**。买「每次爆炸更疼」,是给一个**本组从来没量到过大值**的量买乘数。
- **[5] 的计价单位是「花在技能上的法力」**。语义是本轮**买来的**,不是猜的:Valve 自己的 tooltip ——
  *"A portion of the mana Crystal Maiden spends on her abilities is converted into a physical barrier."*
  (`dota/resource/localization/abilities_english.txt`,d2vpkr 镜像,本轮读取;零成本 HTTPS)。
  `barrier_duration 8`;`mana_multiplier = 30` + `hero_levelup +2`(20 级约 **70%**)→ 天赋 **90%**,
  即**每花 100 法力多 20 点物理护盾**(≈ **+29%** 的护盾产出)。**没有技能要落地、没有引导要撑住、不用瞄准**;
  **而且它在 [6] 押注的那个动作上也照付** —— 3 级大招 600 法力 ⇒ **开引导这一下自己就值 +120 护盾**,正好在她被定住挨集火的那一刻。
- **为什么付得起(Axe t20 的同一条理由)**:**翻转对决策层惰性**。全文件**没有一处读 `freezing_field/damage`**(`ConsiderR` 用 `GetOffensivePower` 自己定尺),**也没有任何句柄碰得到那个内置技能** —— 它在 **51/51** 语料帧上 hidden,在本文件看到之前就被 `sAbilityList` 的压缩走丢(GH #206)。**两个方向都造不出陈旧读数。**

## 4. 诚实边界(与结论同权)

1. 频道证据 **n=2**,两帧都**低于 20 级**、都来自 10 分钟 cap 的语料 ⇒ 界定的是 [6] 收益的**形状**(频道会被砍断),**不是 20 级以后的费率**。**本轮没有任何断言声称费率。**
2. Glacial Guard 的护盾**只挡物理**。杀死她的伤害里法术占多少,本组量不了,**也没假装量到**。
3. `hero_levelup +2` 从 0 级还是 1 级起算,会让「~70%」浮动 2 个点;**结论依赖的是比值,比值不动**。
4. CM 的 `npc_heroes.txt` **"Facets" 四条全部 `Deprecated true`** —— 这**正是**今天 Glacial Guard 是一条朴素 `"Innate" "1"` 技能的原因。若某个 patch 把它挂回 facet,**这一行立刻变成 facet-条件的**,必须重新定价。**这正是 skeleton_king 排最后的那一族。**
5. **最弱的一条放最后**:Valve 自己的默认 bot build 在 20 级取的就是 [5](`npc_heroes.txt` → `"Bot"` → `"Build"` → `"20"`;25 级取 [7])。**它在 t10 上和我们不一致**(它取 +12 智力,我们 2026-08-23 审过并保留 +200 血,`tests/test_cm_t10_payoff.lua`)⇒ **它是旁证,不是论证**。

## 5. 测试

**新增 `tests/test_cm_t20t25_payoff.lua`(10 条)**:

- 两行的钉子,失败信息里写清**什么证据才翻得回去**(t20:一份「发布版 bot 在 20 级以后到底把频道撑多久」的测量 —— 那是今天谁都没有的数);
- **iff 棘轮**:「t25 回到 [7] **当且仅当** `ConsiderW` 的硬编码已被修好」——**两种修法都接受**;
- 硬编码在四个等级上**逐位等于 KV**;`+1.0s` 天赋**恰好短 100**;
- `nova_damage` **既被实时读、又确实进了某个 `FindAoELocation` 的实参表**(不是「文件里两个词都出现过」);
- slot 5 / 6 的 KV 绑定与数值(30 / `+2` / `+20`、`+50`);
- live 代码里**没有** `glacial_guard` 句柄、**没有** `'damage'` special value 读(t20 惰性前提);
- **三个 R 守卫仍是 soak candidate** —— 谁 promote 了它们这条就红,**因为那正是 t20 定价的前提**。

**`tests/test_focus_talent_anchor.lua`**:CM 记录改成 `t20 = 5 / t25 = 8`,连同定价理由块。

**⭐ 一条本轮必须自己踩住的坑**:**所有源码扫描跑在「剥掉注释后的源码」上**。本轮把 `100 + nSkillLV * 50` 这条公式**写进了 hero 文件的头注**,裸扫会**把自己的文档读回来当代码**(与 `test_focus_talent_anchor.lua` 的 `live_lines` 同因;05:30Z 的散文锚那次是同族的另一端)。

**变异 9 条:6 抓 + 1 按设计放行 + 1 对照 + 1 交叉**

| # | 变异 | 结果 |
|---|---|---|
| M1 | t20 改回 `{10,0}` | 抓(行钉子) |
| M2 | t25 改回 `{0,10}` | 抓(行钉子 **+ iff 棘轮**,2 条) |
| M3 | 硬编码 `50` → `60` | 抓(天赋算术那条:「短 60,不是 100」) |
| M4 | `'nova_damage'` → `'nova_dmg'` | 抓 |
| M5 | `FindAoELocation` 不再吃 `nDamage`(传 0) | 抓(实参表扫描;这是把 `find` 改成逐个 `%b()` 之后才抓得到的) |
| M6 | 删掉 `cmrguard` 的门(= promote) | 抓 |
| M7 | t25 回 [7] **且**硬编码改成读句柄 | **iff 通过(设计如此)**,只有行钉子反对 |
| M8 | 只改一行注释 | **全绿(对照)** —— 证明棘轮钉的是 live 代码不是散文 |
| M9 | 恢复 | 全绿 |

## 6. 验证(铁律 6)

- **静态**:`bash tools/agent/luacheck_gate.sh` → `installed luacheck (it was missing; apt package lua-check, bounded)` / **`luacheck bots game: 0 warnings`** / **EXIT 0**。
- **动态(定向)**:`focus_talent **22/0**`、`talent **53/0**`、`cm_ **173/0**`、`gate_claim_consistency **10/0**`、`smoke **3/0**`、`level_premise **5/0**`、新文件 `cm_t20t25 **10/0**`。
- **全量 `lua5.1 tests/run_tests.lua` 本轮没跑**,原样记下:08:15Z 那轮后台跑完过一次(**2210 例 / 0 失败 / EXIT 0,~4h10m**),**时长 > 2h 节拍**(GH #124 的立案句已被那次推翻,但挡住全量的是时长)。本轮改动面 = 一个英雄文件的两个字面量 + 两个测试文件,定向过滤覆盖了所有会读这两行的测试。
- 自检 `unlanded_commits` 收尾时为 0(见 §0)。

## 7. 交出去的棒

1. **条件 (a) 骑 `hero-19`**(已 pending,`ROUTED_ARCHIVE_SCAN`,零 EC2;它当初就是**按焦点五不挑英雄**写的,正为后三轮共用)。CM 这一格要买:解 cap 后归档局里 **CM 到 20/25 级的局数与占比**,以及任一 20 级 CM 帧上**确认这一点花在 `..._glacial_guard_mana_multiplier`、25 级那点花在 `..._crystal_maiden_2`**。**只证明「落地了」,不证明「更好」。**
2. **`ConsiderW` 硬编码的一般性修法本轮故意没做**:今天它是对的,**但它对的原因是这一轮把行挪开了**,不是它自己稳。一般修法(改读 `duration` × `damage_per_second`)与 **Axe 的 `hero-2`**(斩杀判据硬编码 `150 + 100*lv`)**是同一族**,建议同批做;**iff 棘轮在中间兜着**。已写进 GH #251 §5。
3. **棒 ② 只剩 skeleton_king**(两行是 FACET 行,而树里没有任何东西读「摇到了哪个 facet」——本轮 §4(4) 给了这一族一个新的、可复用的判据:先看 `Facets` 块是否 `Deprecated`);**棒 ③**(`tests/` 等级前提登记表,从 9 个文件往下做)**未动**。

## 8. Token 用量

```
TOKENS total_in=9,122,995 out=75,540 turns=58
```
(`python3 tools/agent/token_usage.py`;到统计时刻为止,之后的收尾回合不计入。)
