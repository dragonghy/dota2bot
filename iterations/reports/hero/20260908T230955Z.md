# 英雄组 2026-09-08T23:09Z — GH #593:创建帧已经存在了,而两处限度还写着相反的话

**工作单元主体:OWNER_PRIORITIES P4.4 *(ii)*** —— 一个判定完结所需的最后一块证据,
不是 (i) 的新行为改动。`bots/` 有 diff,但**只有注释**(零可执行 diff),这一点写在最前面
免得被读成一次行为改动轮。

**一句话**:`zusultstrand` 的创建帧 2026-09-07 已由录像组钉成 fixture,而
`tests/test_zuus_ult_strand.lua` §6 与 `bots/BotLib/hero_zuus.lua` 头部那两处
「corpus holds **NO** creation frame」限度还在原地写着相反的话。本轮把限度
**换成它自己要的那个读数**(不是删掉),并按 GH #593 自己写的验收方式收尾。

---

## 1. 认领:`[hero]` open issue,**这次把分页翻完了**

`-128` 上一轮自捉的开工步骤(`list_issues` 返回的是**第一页**,而 open 总数 534、
`hasNextPage: true`)本轮**执行了**:翻到第二页(cursor `…QDl5oBs=`)才做判断。

- 第一页(#644 … #603)的 `[hero]`:#644 / #643 / #640 / #634 / #630 / #625 / #621 /
  #617 / #615 / #612 —— **全是本组自己开的「已 gated 落地」交棒帖**,等的是总监裁定,
  不是可认领的活。
- **第二页**(#602 … #561)的 `[hero]`:#599 / **#593** / #587 / #577 / #570 / #567 /
  #566 / #564 / #563 / #562。
  - **#593 = 本轮认领**:它点名要英雄组做两件事,并且**自己写了验收方式**。
  - #577(`axecallbkb` 拆 id):**本组这一侧已经付掉了** —— 拆分 2026-09-06
    (`state.json:axecallbkb_split_20260906`,`X.IsCallPierceInterruptOn` /
    `X.IsCallPierceInitiateOn`,旧 id 退役),两个坐标 2026-09-07 已钉成
    `tests/frames/f_260831_061811_axe_call_tp_channel.lua` 与
    `tests/frames/f_260828_002127_axe_call_bkb_ring.lua`(`tests/test_axe_call_staged_frames.lua`)。
    余下的「标签与登记」是总监的。**⚠️ `-128` 把 #577 排成「看起来最像本组的活」是过期的**,
    因为它是按标题排的,而那两棒在标题之后落的地。
  - #570 仍按 `-124` 的禁令不碰。

## 2. GH #593 要的两件事,以及为什么是「换成读数」不是「删掉句子」

| 产物 | 之前 | 本轮 |
|---|---|---|
| `tests/test_zuus_ult_strand.lua` §6 | 单向绊线 `nCandidate == 0`,失败信息自称 GOOD NEWS | 换成**指名道姓**的正向断言:域内 `#tCandidate == 1` **且**那一帧是 `CREATION_FRAME` |
| `bots/BotLib/hero_zuus.lua` 头部 COVERAGE 段 | 「It holds NO creation frame …」 | **更正**(旧句被引用过,所以留原文再写更正)+ 三条限度 |
| `tests/test_replay_260827_zuus_ultstrand_creation.lua` §4 HANDOVER | 断言两处限度**同进同退**、且都还在 | **删掉**(该节自己的失败信息要求的处置,不是重新上膛) |

新 fixture 进 `ZUUS_FRAMES`,**退役因此是一次读数而不是一次改句子** —— 绊线是对**列表**开火的。

## 3. 帧读数(零注入的那一半)

`tests/fixtures/f_20260827_091703_slot12_zuus_473_1.lua`,soak 局
`20260827_091703_slot12`,**t=473.1**,radiant,**baseline 腿**(该局 `zusultstrand` **未 armed**
—— 这正是它能给一个 widening 当证据、而不是给一次 armed 运行当核验的原因)。

| 合取项 | 读数 |
|---|---|
| `nHealthPercentage <= 0.28` | **0.176**(177/1006) |
| `abilityR:IsFullyCastable()` | true,rank 1,cd **0** |
| 蓝够 | mp **405** ≥ 施法消耗 **250** |
| `WasRecentlyDamagedByAnyHero( 2.0 )` | true(slardar / ogre) |
| 1600 环内活着的追击者 | **1**:slardar **304.9u** |

**地面真相,两台互不相干的仪器**:fixture 自带 `observed.died_after = **8.3**`;
同局 events 全表里 `zuus_thundergods_wrath` 的**第一条** ABILITY 事件在 **t=600.8** ——
这次死亡之后 **119.2 秒**。他揣着大招死了,场上还有 4 个活着的敌方英雄,而这个技能是全局的。

## 4. ⭐ 本轮真正的看门狗:**一条断言「零」的绊线不需要控制,它的替代品需要两条**

旧 §6 断言 `nCandidate == 0`。那种形状**没法空过** —— 只要它绿着,它就在说一件可证伪的事。
换上去的是**正向计数 + 具名帧**,而这种形状有**两条**空过的路,所以变异台各配一条控制:

- **M10(供给控制)**:把创建帧从 §6 遍历的列表里拿掉。helper、闸、具名常量、散文**全部
  存活**,而退役就落在一帧「该节从来没加载过」的 fixture 上 —— **那正是本轮之前的状态**。
  绿了就说明退役是改了句子不是买到读数。→ **caught**,信息是那条 REGRESSION 断言。
- **M11(合取项控制,也是为什么计数写 `== 1` 不写 `>= 1`)**:把 §6 自己施加的 28% 血线
  放宽到 1.00。域里于是混进**不是创建帧**的 Zeus 帧,而具名那一帧仍在其中 ⇒
  **`>= 1` + 具名会照绿**,精确计数不会。→ **caught**(「2 frames … not 1」)。

`tools/agent/mutstand_zusultstrand.sh` 因此从 9 变异长到 **11**,重跑 **11/11 CAUGHT**,
还原后 `sha256sum -c` 通过(该 stand 的 `trap restore EXIT` 在第一个变异体之前就装好)。

## 5. ⚠️ 限度,一条都不许合并

1. **买到的是助手函数三个合取项在真实帧上同时成立,不是「分支在真实对局里放过大招」。**
   `X.ConsiderR` 外层的 `J.IsRetreating` 是 bot VM 的 mode,任何 `.dem` 都不带;端到端
   0 → 0.75 是带**一处具名注入**买到的,**不注入时 armed 侧同样是 0**,这句已经写成断言
   (`tests/test_replay_260827_zuus_ultstrand_creation.lua` §3/§5)。
2. **1 帧不是 257 个 episode**,更不是频率。域读数在
   `iterations/reports/replay-check/20260907T042001Z.md` §3/§4。
3. **与同文件 `ultcash` 的重叠没有消掉**:`J.IsDyingUnderAttack` 在这一帧答 false,
   只是因为 `GetEstimatedDamageToTarget` 录像里没有。
4. **§6 的 respawn 绊线原样保留**:`GetRespawnTime` 仍然没有任何东西接线,比较式左端
   仍是 loader gap、不是帧数据。**本轮只退役了两处限度中的一处所指的那件事**,另一条限度
   没有被这份证据碰到。

## 6. 接力棒(铁律 9:同一工作单元内交出去)

`zusultstrand` 按新验证哲学现在缺的**只有条件 (a)** —— 一波 **armed** 的波次,
让录像组能核验这条撤退分支**真的开火**。那是批测台的球。
⇒ 登记 `iterations/queue.json:**hero-51**`(**接棒登记,不是新扫描**),连同解冻后要读的
四列与归因边界一并预登记。
**⚠️ 本条不请求入集**:P4.2 冻结期内合法裁定是 **FROZEN-HOLD**;这里登记的是解冻后的
**arm 顺序**,免得接力棒像拉野死分支那样掉 37 轮。

## 7. 门与自检(读数,不是判决)

- **铁律 6 静态门**:`bash tools/agent/luacheck_gate.sh` → **GATE_EXIT=0 CLEAN(0 warnings)**,
  **没有用 `RULE6_BYPASS`**。
- **动态半(本组这一侧)**:`lua5.1 tests/run_tests.lua zuus` 改前 **237 例 0 失败** →
  改后 **236 例 0 失败**(-1 = 删掉的 §4)。另跑 `gate_claim` **16 例 0 失败**、
  `smoke` **3 例 0 失败**。⚠️ 全量套件本轮**没跑**(~100min,GH #124)。
- **开工自检**:`EXIT=3`(**有发现,不是通过**)。发现是 unlanded(别的座位的一条
  `origin/claude/compassionate-albattani-xnmogh`)、cadence(strategy 两个洞)、
  queue-rulings(`hero-50` 的 rideshare 待总监裁)、owed-executions —— **都不是本轮的**。
  另有 UNCERTIFIABLE:`test_selfcheck_lua_leg.py` 等 9 个 check 120s 没跑完
  (**与前三轮逐字同一条**),以及一条 `trunk-red(python)` 的 UNCERTIFIABLE,
  它明说要「在安静的树上重跑(bots/ 下没有写入)」—— **本轮那半确实不安静:我在自检
  跑 python 腿的时候改了 `hero_zuus.lua` 的注释**,所以这一条**本轮不能读作 trunk 红,
  也不能读作通过**。fast Lua detector 那一腿在**改动后的树**上跑完:**86 个,0 失败**。
- ✅ **自检退出码用文件重定向读**(`> /tmp/sc.log 2>&1; echo "EXIT=$?"`)。
  ⚠️ 但**第一条命令仍然踩了管道**(`| tail -40`),被脚本当场 REFUSED —— **第 13 次**,
  **连续第三轮**,而且**又是本轮第一条命令**。`-128` 已经把它写成「第一条命令的模板」了,
  **写下来没有挡住它**;下一轮的形状见 `-129`。
- ✅ **「自检与变异台并行」(GH #507)本轮没有复发**:变异台在
  `until ! pgrep -f "[r]outine_selfcheck.sh"` 阻塞等到自检结束之后才起跑。

## 8. 顺手看到、没做的(写下来免得下一轮重查)

- **Zeus `X.ConsiderD`(Nimbus)的队友循环没有任何 bot→目标的距离项,而这不是缺陷**:
  `zuus_cloud` 的 `AbilityCastRange` base = **`0`** = 全局。**这是一条已经查过的否定结果。**
- **五个焦点英雄的 `GetSpecialValue*( "<key>" )` 逐个对过 KV 快照,没有新的结构性零**:
  `zuus_arc_lightning/arc_damage`、`/radius`、`skeleton_king_bone_guard/max_skeleton_charges`、
  `lion_impale/width` **全部存在**。(`GetAbilityDamage()` 那一族的三个消费点
  `wkqdmg`/`lionqdmg`/`zusboltdmg` 已各自有 id;`hero_lion.lua` 的
  `X.ConsiderW`:823 与 `X.ConsiderE`:1010 两处 `nDamage` 是**赋了值就没人读**的死局部,
  不是行为杠杆。)
- **⚠️ 一条对「到达」这一族有普遍影响的观察,登记为待核实、本轮不据此改任何东西**:
  出货把目标交给 `ActionQueue_UseAbilityOnEntity`,射程外的后果是**先走一段路再放**,
  不是「什么都没发生」。所以「搜索环比射程宽」这件事的代价故事,**默认应该按一次走位来
  写,不是按一次空转**。本组已落地的到达族 id 各自有自己的代价论证
  (例如 `axebhreach` 是**最小值搜索把射程内的合法候选挤掉** = 一次**交换**,不受这条影响),
  但**下一次写到达族的 (c) 论证时要先回答这一条**。这是一条**观察**,不是对既有裁定的推翻。

## 9. 本轮零花费

零 AWS、零 EC2、零 S3、零 CE。没有 arm 任何 id,没有提任何入集提议。

## 10. token 用量

```
TOKENS total_in=12,107,195 out=64,974 turns=75
```

(统计时刻之后的收尾回合不计入 —— 提交、push、以及 GH #593 的一条结案评论。)

**本轮发表的评论(供 `citation_audit.py` 解析)**:
<https://github.com/dragonghy/dota2bot/issues/593#issuecomment-5593185613>
—— 发表前跑过 `bash tools/agent/claim_precheck.sh`(GH #290),**exit 0 / clean**:
`paths cited 10`、`state.json keys cited 1`、`refused 0`、`local commits not on origin/main: 0`。
⚠️ 那次 precheck 是在 **push 之后**跑的,顺序正是 #290 立的那条(先 push,再发表引用)。
**#593 本轮不由本组关闭** —— 标签与结案是总监的,评论已把验收读数摆齐。
