# 英雄组 2026-09-09T21:16Z — `wkbonefight` 收窄到交战环

**焦点英雄**:Wraith King(skeleton_king)
**OWNER_PRIORITIES**:**P4.4 (i)** —— 工作单元主体是一个 `bots/` 行为改动
**backlog**:`-135` 逐字执行(「去找**第二份没人消费的域读数**,像本轮花掉 GH #562 那样花掉它」)
**认领**:`iterations/queue.json:hero-31`(`wkbonefight` 的域读数,录像组 **2026-09-06T19:1xZ** 交付,
**三天无人消费**)。判据就是 `-135` 给的那一条:`status != done` 而 `result` 已经有东西的 `hero-*` 行。

---

## 0. 一句话

**读数没有证伪这根杠杆,它证伪了杠杆量的那个圈。** `wkbonefight` 的 armed 腿写的是
`nEnemies >= 1`,而 `nEnemies` 是 **1600** 视野环的人数;可这条杠杆的支配性论证——
helper 抬头逐字写着的那句「a release that joins a **3v3** buys strictly more **hero-facing**
attack-seconds than one that joins a 1v1」——是一句关于**40 秒近战骷髅够得着的英雄**的话。
**那句话从来没进过 `if`。** 于是在 1600 上,一个 1550 开外、正往反方向走的敌人,
也算进了那个「3v3」。收窄后合取项读的是分支自己下一行就在用的 **650**。

这与 `-135` 花掉 GH #562 的那一轮**同型而不同因**:上一轮读数是 4:1 的负读数,
本轮读数是**正**的(域没空,`DOMAIN-NOT-REACHED` 退回门不触发);
**两轮的共同点是「先核对抬头写的前提有没有落在代码里」,而两轮的答案都是没有。**

## 1. 花掉的那份读数(录像组量的,不是本轮量的)

`iterations/reports/replay-check/domain_scan_hero_2_30_31.md` §6,零 EC2、S3 只读,
**118 局 / 四个 run**:

| 列 | 读数 |
|---|---|
| (1) W 就绪 | **112,933** 帧(`ShouldSaveMana` 压掉 11,184;复活读条 0 血帧拒 421) |
| (2) 1600 内敌人数分布 | 0 / 1 / ≥2 = **65.6% / 14.5% / 19.9%**(不报中位数,铁律 4(ii)) |
| (3) 就绪 + 650 内有敌人 | `n1600==1` **5,093** 帧 / 1,365 ep;`n1600>=2` **12,656** 帧 / **2,244 ep / 118 局全覆盖** |
| (4) 充能层数 | **买不到,未用 0 顶替**(`snapshots[]` 无 modifier 表)⇒ INSTRUMENT-BLIND |

⇒ 预登记的 **`DOMAIN-NOT-REACHED` 退回条件不触发**(2,244 ep,最少一局 11 ep,均值 19.0)。

**⭐ 本轮给这份读数加的第二个上界,原报告只写了第一个。** §6 自己说了 (4) 让前三列成为上界
(充能门 `nStack/maxStack >= 0.6` 从未被求值)。第二个上界是本轮的:
**12,656 是按 1600 环切的**,所以它界定的是**旧 armed 腿**会吃下的机会数,
**不是收窄后这条腿会吃下的**。⇒ 引用 12,656 给新腿定价的人引错了列;
**该重读的是 (3) 列本身,因为它是唯一按环分了层的一列**。

**⭐ 读数里那条构造性归属,本轮把它读成了机制而不是数字。** 1,024 次释放里
「释放时 1600 内 ≥2 人」有 **153** 次 —— 而分支 1 出货写着 `== 1`,所以**这 153 次只能来自分支 2**
(`nStack == maxStack` 满仓 + 近兵线/打钱)。⇒ 出货代码**并不是**「团战里绝不放 Bone Guard」,
而是「团战里要放,得先攒**满**仓、并且人得在兵线附近」。`wkbonefight` 真正改的是
**这件事的次序**:让战斗分支在 **60%** 仓位就能放,而不是被赶去走那条要满仓的推线分支。

## 2. 改动(`bots/BotLib/hero_skeleton_king.lua`)

**无新 id。** `wkbonefight`(turbo-only,**仍未 armed**,STANDALONE)**就地收窄**。

1. 新 `X.wk_CountBoneGuardEngaged( tEnemies, nRadius )` —— 走调用方自己遍历的那张表,
   数**环内**的敌方英雄。参数序用 `J.IsInRange( origin, target, radius )` 自己的序,
   于是 `CanBeSeen` 落在**敌人**身上(这个计数关心的正是敌人的可见性)。
2. `X.IsBoneGuardEnemyCountOk( nEnemies )` → `( nEnemies, tEnemies, nRadius )`;armed 腿:
   ```lua
   return nEnemies == 1
       or X.wk_CountBoneGuardEngaged( tEnemies, nRadius or 650 ) >= 2
   ```
3. `X.ConsiderW` 新增 `local nEngageRange = 650`,**两个合取项从此读同一个数**
   (计数环与 `J.IsInRange( npcTarget, bot, nEngageRange )` 的够得着环)。

**gate 关时逐字节仍是出货行为**:交战计数合取项**只能从 armed 分支内部到达**,
结构上不可达。⇒ **P4.2 入集冻结在这里没有东西可冻**(本轮不提入集、不动 armed 串、
不动 `test_set.md`)。

**⭐ 方向由构造保证,而且它在收窄后仍然成立** —— 因为出货判据是一个**字面析取项**:
`nEnemies == 1` 被 armed 腿按构造接受 ⇒ armed 仍是 shipped 的**超集**,
arming 只能**增加**释放。负读数仍只能读作「放多了不好」,永远不能读作「杠杆吃掉了一次释放」。
(变异 **M7** 就是删掉这个析取项。)

**⛔ 缺表默认是 RESTRICTIVE(退回出货单挑),不是放行。** 放行会让任何忘记传参的调用方
**静默恢复 1600 那条腿**,而且没有任何计数器会报出来——一次**放了**的 Bone Guard
和一次**没放**的一样不留痕迹。(变异 **M4**。)

## 3. 真实帧读数(13 个 WK-subject fixture,实测)

| frame | view@1600 | engaged@650 |
|---|---|---|
| `f_080225_wk_lane` | 2 | 2 |
| `f_080225_wk_revive` | 2 | 2 |
| `f_225947_wk_trade_kite` | 2 | 2 |
| `f_260820_181711_wk_l1trade_333` | 2 | 2 |
| **`f_230545_wk_laning_safe`** | 2 | **1** |
| **`f_260725_105305_wk_reincarn_gap`** | 2 | **1** |
| **`f_230545_wk_sven_burst`** | 2 | **0** |
| `f_232320_wk_od_burst` | 1 | 1 |
| `f_232228_wk_ownhalf_standoff` | 1 | 0 |
| 其余四帧 | 0 | 0 |

- 出货接受 **2 / 13**;**旧** armed 接受 **9 / 13**;**收窄后** armed 接受 **6 / 13**。
- **被丢掉的 3 帧正是「按 1600 定价、按 650 够不着」的那三帧**,而且它们不是同一种:
  两帧(`laning_safe` / `reincarn_gap`)**环内只有一个人、第二个人在远处** —— 一场被 1600
  读成团战的**单挑**;`sven_burst` **环内一个人都没有**(2 in view, **0 in reach**),
  是这条缺陷最干净的病例。
- 分支几何内部(650 内有敌人的 **7** 帧):出货 **1/7**,收窄后 armed **5/7**
  ——**仍是出货的五倍**,丢掉的两帧正是环内只有一人的那两场单挑。
- engaged 直方图 **0→6 / 1→3 / 2→4**;并断言 `engaged <= view` 逐帧成立
  (这是「armed 在 n=0 放不出来」在真实分支里**不可能**而不仅仅是**没观测到**的依据)。

⚠️ **计数受引擎视野限制**(`J.GetNearbyHeroes` 包 `bot:GetNearbyHeroes`),所以「环内 2 人」
是 WK 真看得见的两人;这让上表对本改动是**保守**的,不是有利的。
⚠️ **分支本身仍然不可 fixture 驱动**(GH #274 两条堵点未变:充能 modifier 在 36 帧上是 0;
`J.GetProperTarget` 结构性 nil)。**本文件驱动的是被改动的那一项**,§5 把两条堵点各钉成断言,
修好那天它红并点名。**定价仍然要波次,不要 fixture** —— GH #274 的判词不动。

## 4. 测试与变异台

- `tests/test_wk_bone_guard_enemy_count.lua` **17 → 21 例**,新增 §2 的收窄读数三例
  与 §3 的缺表回退一例,§6 新增三条源码棘轮。
- **⛔⛔ 上一轮那个坑本轮正面撞上并绕开了**:§2 的出货普查、§3 的阶梯、§4 的三个 no-op 对照
  原来**都是不传表**调用 —— 收窄后它们会落到**限制性回退**上,于是
  **无论 gate 开不开都返回出货答案,对照什么也证明不了**(而「21 例全绿」看不见这件事)。
  §4 因此新增 `opener_table()`:它先**断言这张表真的能打开 armed 腿**,再拿它去做对照。
  §3 的阶梯改成扫 **(count, table) 对**,13 帧 × 11 个计数 = **143 对**,widening **40 对**
  (= 环内 2 人的那 4 帧 × 除 n=1 外的 10 个计数;环内不足 2 人的帧 widening **0** —— 这正是收窄的全部意思)。
- 新变异台 `tools/agent/mutstand_wkbonefight_ring.sh`:**10 发,10/10 CAUGHT**,
  baseline 先证 GREEN,`trap restore EXIT` 在第一发之前,锚点缺失**或歧义**都 abort,
  退出码不过管道。**每一发都锚在收窄 MOVED 过的读数上**(9→6、点名的 3 帧),
  没有一发锚在它没动的东西上 —— 这正是上一轮三发「因为正确的修复而 SURVIVED」的成因。
  M1 恢复旧腿 / M4 缺表放行 / M7 删掉出货析取项是最值得读的三发。

## 5. 验证(铁律 6)

- `bash tools/agent/luacheck_gate.sh` → **EXIT=0 CLEAN(0 警告)**,**没有用 `RULE6_BYPASS`**。
- 分组:`wk_` **293** / `focus` **92** / `smoke_load` **3** / `gate_claim` **16**,**全 0 失败**。
- python:`test_hero_export_reachability`(**孤儿天花板 34 未动** —— 新谓词是被**调用**的,
  不是只当函数值传,上一轮的第 35 个孤儿没有重演)、`test_lua_corpus_stability`、
  `test_guard_implication_census`、`test_gated_getter_stub_census` **四个全 EXIT=0**。

## 6. ⛔⛔ 自检:GH #507 的一个**新形状**,而且它比旧形状更会骗人

- 第一条命令**踩了管道**(脚本 REFUSED,exit 2,什么都没检查)——`-133` 点名过,仍然复发;
  第二次改用重定向,但**加了 `timeout 300`**,而实测要 **13 分钟** ⇒ EXIT=**124**,又一次什么都没读到。
  (`-133` 逐字写过「不接管道、不加 `timeout`」,本轮两条**分两次各犯一条**。)
- 第三次跑对了,**真实 EXIT=3**,findings = `cadence owed-executions` + `a-evidence-owed` +
  **`trunk-red(python)`**。python 腿另有 **9 项 UNCERTIFIABLE**(5a0 的 **120s 预算**
  跑不完 **87 个文件**的发现集,与 `-135` 记的同因)。
- **⭐⭐ 那个 TRUNK RED 是我自己造的,而且成因不是 `-134` 记的那一个。**
  红的是 `tests/test_lua_corpus_stability.py` 的
  「the gate switch really was on disk during that listing」。**单独重跑 EXIT=0。**
  成因:我在自检跑着的时候跑了 `lua5.1 tests/run_tests.lua wk_bone_guard_enemy_count`,
  而两边**都写同一个文件** `bots/Customize/soak_side.lua`(gate 开关)——
  python 那条腿写下开关、列语料、断言开关当时在盘上,我的 lua 跑把它删了。
  ⇒ **`-134`/`-135` 把这条规矩记成了「争 CPU」,本轮的实际故障是「争一个共享文件」**,
  而且**后果严重得多**:争 CPU 产出 9 个 UNCERTIFIABLE(诚实地说「这一侧没人看过」),
  争文件产出一个 **TRUNK RED** —— 一个看起来像「主干坏了」的**假阳性**。
  **规矩要改的不是理由是范围:自检跑着的时候,不要跑任何 `tests/` 套件,不只是不要跑重活。**

## 7. 台账

- **新 gated id 0**;armed 串 / `test_set.md` **一字未动**(P4.2 冻结,本轮**不提入集**)。
- `state.json` 新条目 **1**:`wkbonefight_ring_20260909`。
- `queue.json:hero-31` → `delivered-and-consumed`,并交出**下一棒**(见 §8)。
- **零 AWS / 零 EC2 / 零 S3 / 零 CE。**

## 8. 下一棒(铁律 9 连带规则:不许掉棒)

`hero-31` 的球本来写着「**下一棒在总监**(是否入集另受 P4.2 冻结线约束)」。本轮**不动那个路由**,
但把交给总监的东西**改小了**:要裁的不再是「12,656 值不值得入集」,而是

> **按 (3) 列的哪一层给这条腿定价** —— 12,656(1600 环,旧腿)已经**不是**这条腿的域;
> 收窄后的域是「650 环内 ≥2 人」,**离线买不到**(录像组的 (3) 列按 `n1600` 分层,
> 没有按 `n650` 的人数分层),⇒ **下一次遍历只要多一列**:
> (3′) 就绪 + 650 内敌方英雄数的分布(1 / ≥2 分开),**不要并池**。
> 这一列零 EC2、可并进 hero-2/30..49 那次遍历,**不申请专波**。

⇒ 本轮已把 (3′) 写进 `queue.json:hero-31.next`。**充能门(4)仍然 INSTRUMENT-BLIND,
GH #274「用批测不用 fixture 定价」的判词不动。**

## 9. Token 用量

```
TOKENS total_in=9,231,905 out=69,377 turns=63
```

(统计时刻为止;此后的收尾回合不计入。本轮的 `total_in` 偏高有一个可指名的原因,不是空转:
变异台按 `wk_` 过滤跑 **11 次**全组(baseline + 10 发),加上自检**跑了三次才跑对**
(管道 REFUSED → `timeout 300` 超时 → 正确跑完 13 分钟),这两项占了大部分等待回合。
`-136` 里那条「第一条命令只准是这一行」就是冲着后者写的。)
