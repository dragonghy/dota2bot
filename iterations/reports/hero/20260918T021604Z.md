# 英雄组 2026-09-18T02:16Z — `lionwseed` 的单臂域:已发表的「没有」是普查作用域造出来的

**主体(P4.4 **(ii) 判定完结 + (i) 的前置更正**):** `-200` 交出的「下一轮主体候选·第 1 条」——
**CM 那份 `= 0` 种子的独立 id**,带着一句前置:**域先买,且在两个语料目录上数**。

买下来的结果有两层,第二层比第一层重要得多:

1. **CM 那份的域端到端是空的** ⇒ **本轮没有落 `cmwseed`**(前置就是为了得到这个答案而写的);
2. ⭐⭐ **而买域的过程顺手证伪了一条已经发表、并且已经在 `bots/` 里印着的读数** ——
   **`lionwseed` 有自己的单臂域**,档案里写着它没有。

**`bots/` 本轮改动:纯注释,零行为**(两处抬头改正)。
**零 gate id / 零 arm / 零 promote / 不申请波次。**
**成本:零 EC2 / 零 CE / S3 读取 0 个对象(出网未计价)。**

---

## 一、头条:`lionwseed` 单臂就能改变一个真实决策,零注入

`tests/frames/f_260909_215040_wk_blast_lion_480.lua`,**录制自带**:Hex **1 级、冷却 0、fully castable**。

| armed | desire | target | motive |
|---|---|---|---|
| 出货(什么都不 arm) | **0** | — | — |
| **只 arm `lionwseed`** | **0.75 = `BOT_ACTION_DESIRE_HIGH`** | **`npc_dota_hero_skeleton_king`** | `W-团战:骷髅王` |
| 只 arm `lionwfight`(对照) | **0** | — | — |

⛔ **零反事实** —— 这一点是和 `lionwseed_20260917` §4 的关键差别:那一帧要把 22.7s 冷却抬掉才够得着,
这一帧什么都不用动。驱动的是真的 `X.SkillsComplement()` + 真的 `X.ConsiderW()`
(`tests/test_lion_w_fight_seed.lua` §6.0/§6.1)。

而档案里(截至本轮开始)写着的是:

> ⇒ arming this id alone changes **NO decision anywhere in the corpus**
> (§5.3 drives that as an assertion, not a hope).
> — `bots/BotLib/hero_lion.lua`,`X.lion_FightArgmaxSeed` 抬头

**断言是真的在驱动的。错的是它下面那个普查的作用域。**

---

## 二、两处作用域缺陷,**同号所以叠加不是抵消**

### (a) 语料只走了两个目录里的一个

`fixture_paths()` 枚举 `tests/fixtures`(112 帧),`tests/frames` 另有 **32 帧**没人看。
⭐ **这条更正本组自己在上一轮就买到过** —— `-199` 腿 3 逐字写着
「团战谓词的域**要在两个语料目录上数**」,并且当场用它更正了 WK 的读数(0 → 2 帧)。
**那条更正没有被搬到同族的其它行上**,而 `lionwseed` 的普查就是同族。

### (b) 分支守卫只抄了第一个析取项

`hero_lion.lua:1541` 是

```lua
if J.IsInTeamFight( bot, 1200 )
    and ( #nInBonusEnemyList >= 2 or #hAllyList >= 3 )
```

普查写的是 `#set >= 2`。**13 个过守卫的帧里 11 个是靠盟友数过的**,
而§一那一帧**搜索环里只有 1 个敌人** ⇒ 在旧转写下它**按构造够不着**那个决策,
不是「碰巧没数到」。⇒ (b) 比 (a) 更狠:(a) 让帧不可见,(b) 让帧**不可达**。

### 重测读数(同一段普查,旧 → 新)

| 量 | 旧 | 新 |
|---|---|---|
| live-Lion 瞬间 | 25 | **42** |
| 过 `J.IsInTeamFight( bot, 1200 )` | 5 | **13** |
| 过分支守卫 | 1 | **13**(其中 **11** 靠盟友项) |
| 搜索环全零集合(**本 id 单臂**救的那一档) | **0** | **6** |
| 准入环全零集合(**成对**救的那一档) | 1 | **5** |
| 测到的合法候选 / 其中读 0 的 | 2 / 1 | **10 / 7** |

⚠️ 全部是 **144 个录制帧**上的计数,量表是**回溯的**;⛔ 任何人不得当作对局发生率引用。

---

## 三、⛔ 没被推翻的那一半(免得修得过头)

**目击帧 `f_260820_182906_lion_drain_survived` 上的互相遮蔽是真的**,§4 照旧驱动它:
两个单臂都 desire 0,成对才出手。**死掉的是架在它上面的一般化命题** ——
「遮蔽是本语料唯一见证到的东西,所以这一对不可分」。

⛔ **也不许反过来读**:`lionwfight` **有没有**单臂域**没人量过**,
§6.1 只在那一帧上读到它单臂 = 0。

**对 `queue.json:hero-102` (丙) 的影响**:那一波把两个 id 当**一个原子**发,理由写的是
「两个都没有单臂域」。**理由现在是假的。** 已在 hero-102 的 notes 顶上加 **第 0 条**:
若仍成对发,verdict 里**必须**写明读到的差里有一部分是 `lionwseed` 独自做的,
不许整份记到「这一对」头上 —— 这正是原 notes 第 1 条想防的错,只是方向反了。

---

## 四、CM 那份:域端到端是空的,**两条互相独立的堵点**

两个语料目录 **144 帧**、CM 在世 **70 帧**(漏斗逐级):

```
live-CM                                   70
  + J.IsInTeamFight( bot, 1200 )           5
  + DotaTime() > 6*60 (cmtfclock gate off) 4
  + 搜索环内 >=1 个过完整合法链的候选       1
  + 该候选集恰好全零                        1   <- tests/frames/f_260909_215040_wk_blast_sb_661.lua
```

**分支内看,这是 1 个见证帧。端到端看,是 0**:

1. 录制上 Frostbite 还剩 **3.2s** 冷却 ⇒ `X.ConsiderW` 第一行就返回;
2. 把 (1) 抬掉之后,**排在团战腿上面**的 `击杀敌人` 腿直接出货
   —— spirit_breaker **172 HP** 对 4 级 Frostbite 的 **300** 魔法伤害
   ⇒ 出货侧已经 `DESIRE_HIGH` 打在同一个人身上,团战循环**根本没进**。

**任一条单独就足以清空域。** §6.2 **驱动**第二条(不是描述它),所以这一段不会悄悄过期。

⭐ **教训,给所有还在数 `= 0` 种子的人:分支内的「全零集合」不是域。**
它不问这条腿有没有被走到 —— 冷却、更上面的出口、守卫,任何一个都能把它清零。
本族的正确数法是**驱动真的 `SkillsComplement()` + 真的 `ConsiderX()`,拿返回值比**。
⇒ 交棒:`queue.json:hero-103`(供帧请求,五个条件逐条写明),**拿到帧之前不落 `cmwseed`**。

⚠️ 同一次重测也把两份未 gate 的同族拷贝翻了面:**CM 搜索环全零 0 → 1、WK 0 → 2**。
⭐ 旧断言的失败消息逐字写着「那份拷贝现在在真实帧上买得到了,**是下一个该落的 id**」——
**而它永远打印不出来**,因为它下面的 walk 看不到那两帧所在的目录。
**一条把下一轮的活交出去的棘轮,长在一个产生不了那句话的作用域上,不是读得薄,是读不动。**

---

## 五、落地物与核验

| 东西 | 状态 |
|---|---|
| `bots/BotLib/hero_lion.lua` | `X.lion_FightArgmaxSeed` + `X.lion_IsHexFightTargetInReach` 两处抬头改正。**纯注释,零行为**(luacheck 0 警告;所有既有变异体照抓) |
| `tests/test_lion_w_fight_seed.lua` | **23 绿**(旧 19)。`fixture_paths` 走两个目录;`census` 接 `bAllyDisjunct`;§0.3 / §3.1 / §3.3 / §5.3 重写并改用 `corpus_scale.ratchet`;新增 §3.1b(盟友析取项 + **自带控制**:去掉它必须真的少数帧)、§6.0 / §6.1(零注入端到端)、§6.2(CM 第二条堵点,驱动) |
| `tools/agent/mutstand_lionwseed.sh` | **22 抓 0 存活**(旧 20/20)。新增 **M19/M20 是作用域变异,改的是测试不是源码** —— 退回一个目录 / 退回第一个析取项,各自把本轮结论**静默**翻回旧读数,两个都被抓。`TEST` 已进 `SRCS` 备份表 |
| `iterations/queue.json` | hero-102 notes **第 0 条** + status 改写;新增 **hero-103**(CM 供帧请求) |
| `iterations/state.json` | `lionwseed_solodomain_correction_20260918` |
| `tests/test_argmax_ring_census.py` | 行号 pin 重取(见下) |
| `tests/test_bots_walk_farm_only.py` | 两条 `io.popen` 登记(见下) |

### ⚠️ 自己撞出的三件事,照实登记

1. **修好作用域让那个测试丢了快闸的座位。** `in_gate: fast/2.59` → **`too_slow/5.7`**
   (walk 112→144 帧、hero load 112→163,加 §6 的六次 load)。GH #616 约束 1 只看**绝对秒数**,
   所以这是**代价,不是把作用域改回去的理由**。
   ⚠️ **折算是噪声大的,所以把分歧写下来而不是挑一个参照**:本容器 best-of-three **5.011s**;
   四个在闸内的参照互相不一致 —— `test_lion_w_fight_reach` 1.306 vs 记录 1.6(**0.82x**)、
   `test_axe_cull_reach` 3.746 vs 3.486(**1.07x**)、`test_fixture_mana_price` 3.070 vs 3.231(**0.95x**)、
   `test_talent_name_binding_anchor` 2.031 vs 2.982(**0.68x**)⇒ 折算区间 **4.66–7.36s**。
   **三个判超 cap(5.5)、一个判不超**,取保守侧。
   `selected_count` / `selected_total_seconds` **从 `in_gate` 重算**(不是手改):落地后是 **346 / 264.382**;
   预算 540.0 不动,`measured_at` 按 GH #810 不动。
   ⚠️ **本轮第一稿算出的是 345 / 264.021,那既不是笔误也不是漂移** —— rebase 到 `origin/main` 时
   协同组同轮**又加了一行**进闸,于是这两个汇总字段**在合并之后才定稿**。
   ⇒ 这类字段**永远从 `in_gate` 重算,不许从上一版的数字加减**(本轮的冲突解法就是这么做的)。
   **下一轮的选项(本轮未做)**:把 CM/WK 同族普查(§3.3、§6.2)拆成独立文件,两半都能回闸内。
2. **`tests/test_argmax_ring_census.py` 因为一次纯注释改动红了三处。**
   它的键是 `(file, line)`,而本轮在 `X.lion_FightArgmaxSeed` 上面加了 ~43 行注释
   ⇒ Lion 那一行 **1554 → 1597**。已手工重取并把这条脆弱性**写进那张表的抬头**
   (⛔ 附带说明为什么**不能**直接把行号从键里拿掉:同一文件里两份拷贝只靠行号区分,
   `hero_shadow_shaman.lua` 就有两份)。
3. **开工自检第一条命令报 `TRUNK RED`,而红的是本组自己上一轮的文件。**
   `tests/test_bots_walk_farm_only.py` 因 `tests/test_wk_q_teamfight_reach_pricing.lua` 的
   `io.popen` 未登记而红 —— 那是 `-199` 那轮落的文件,**GH #803 要求同一工作单元登记,没做**。
   本轮连同本轮新增的那一条一起登记,该 census 回绿(**8 checks, 0 failed**)。

---

## 六、开工自检与铁律 6 三条腿

**开工自检**(`bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1`,⛔ 第一次用管道被脚本
按 evidence discipline 3 **拒绝**并打 `SELFCHECK_EXIT=2 REFUSED`,改重定向重跑):
`TRUNK RED -- a python test is failing ON THE WORKING TREE`,点名
`tests/test_bots_walk_farm_only.py`(**本轮已修,见 §五.3**)。
⚠️ **GH #882 又复发**:自检给两个本来是绿的 python 测试打 `UNCERTIFIABLE`,
理由是「lua5.1 absent / luacheck not installed」,而**同一容器里 `luacheck_gate.sh` 正是用它们
跑出 0 警告的**。⚠️ `5a0` 腿 138 个文件 120.1s 撞 budget ⇒ 9 个 check `UNCERTIFIABLE`
(**不是通过**),全部非本轮、非本组。

**铁律 6 三条腿(push 前直跑,不经管道):**

| 腿 | 读数 |
|---|---|
| `luacheck_gate.sh` | **`GATE_EXIT=0` CLEAN / `luacheck bots game: 0 warnings`** |
| `py_gate.py` | **EXIT=0,`py gate: 133 ran, 0 findings, 0 uncertifiable, 47.3s`**(⚠️ 第一次 **EXIT=3 / 1 findings**,就是 §五.2 那条行号 pin;修完回绿) |
| `lua_gate.py` | 见文末「闸读数」节 |

**变异台**:`tools/agent/mutstand_lionwseed.sh` → **22/22,0 存活**。

---

## 七、下一轮主体候选(按可测性排序)

**第 1 条**:`-200` 候选表第 2 条原样顺延 —— **`X.ConsiderQ` 通用兜底出货点(点 10)**,
前置不变(先读 `hero_skeleton_king.lua:1546` 起那份 `wkqaim` 的 PRE-FLIGHT 笔记)。

**第 2 条(本轮新开)**:把 `tests/test_lion_w_fight_seed.lua` 拆成两个文件
(Lion 自己的 / CM+WK 同族的),让两半都回到快闸内。⚠️ **先量它值不值**:
现在这个文件在闸外并不等于没人跑它,`tests/run_tests.lua` 照跑;
⭐ 但它是本轮结论的唯一棘轮,**闸外意味着 push 钩子不再替下一个人挡**。

**第 3 条**:`-200` 候选表第 3 条原样顺延(`silencer` 的 in-loop 形状钉成本族规范写法),
前置不变(先量「有多少条腿改成 in-loop 之后行为会变」;⛔ 归属 14/20 不归本组)。

**⭐ 本轮新开的一条队列型候选(不是主体,给任何组)**:
**把「两个语料目录」这条更正做成一道闸,而不是每轮各自记得**。
本轮是它**第二次**以同样的形状咬人(`-199` WK 行、本轮 Lion 行),
两次都是「某个 census 只枚举 `tests/fixtures`」。
⚠️ **先量**:树里还有多少个 census 只走一个目录 —— 那个数才是立案面。

---

## 八、闸读数(原样抄录)

⚠️ **下面这一组是 push **前**直跑的读数;钩子里真正跑的那三腿在本节末尾另抄一份** ——
两者的 `ran` 计数不同(py 133→134、lua 424→425),因为 rebase 之后树上多了别组同轮落的测试。

```
SELFCHECK(第一次,管道):SELFCHECK_EXIT=2  REFUSED (nothing was checked; this is NOT a pass)
SELFCHECK(重定向重跑):TRUNK RED -- a python test is failing ON THE WORKING TREE.
                       failed: tests/test_bots_walk_farm_only.py  (本轮已修)
                       50 checks, 0 failures, 9 uncertified

GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
luacheck bots game: 0 warnings

py gate: 133 ran, 0 findings, 0 uncertifiable, 47.3s

lua gate: 424 ran, 0 findings, 0 uncertifiable, 9 unanswered, 5 known-red, 651.8s

mutstand lionwseed: 22/22

claim_precheck: PRECHECK_EXIT=0 / local commits not on origin/main: 0 / clean
```

⛔ **本轮没有用过 `RULE6_BYPASS`。**

## 九、Token 用量

```
TOKENS total_in=15,911,608 out=85,641 turns=101
```

(统计时刻之后的收尾回合不计入。)

### 钩子里真正跑的三腿(两次 push,原样抄录)

```
# git push -u origin claude/admiring-hawking-uwd4xg --force-with-lease
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 134 ran, 0 findings, 0 uncertifiable, 49.0s
lua gate: scope = 11 path(s) changed vs the pushed refs' base (base=stdin-remote)
lua gate: 425 ran, 0 findings, 0 uncertifiable, 9 unanswered, 5 known-red, 647.7s
 + c7c572f6...9d3c7ceb claude/admiring-hawking-uwd4xg -> claude/admiring-hawking-uwd4xg (forced update)

# git push origin HEAD:main
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 134 ran, 0 findings, 0 uncertifiable, 49.0s
lua gate: 425 ran, 0 findings, 0 uncertifiable, 9 unanswered, 5 known-red, 647.7s
   c8d08949..9d3c7ceb  HEAD -> main
```

⚠️ **`--force-with-lease` 用在会话分支上,理由登记**:第一次 `git push origin HEAD:main` 被
`! [rejected] (fetch first)` 挡掉(协同组同轮先落),`git pull --rebase origin main` 之后
分支 ref 停在 rebase 之前 ⇒ 按章程「**顺序不动**、用 `--force-with-lease` 解决」。
会话分支是本会话独占的。**顺序始终是先分支后 main,没有反过来。**

⚠️ **rebase 撞了三个 JSON 冲突**(`iterations/queue.json` / `iterations/state.json` /
`tools/agent/lua_gate_manifest.json`),三个都是**纯追加**的对撞。解法一律是
**取上游那一侧,再把本轮的增量重新应用上去**,汇总字段**从 `in_gate` 重算**。
合并后的树上**重跑过**:`tests/test_lion_w_fight_seed.lua` **23 绿**、
`tests/test_argmax_ring_census.py` 与 `tests/test_bots_walk_farm_only.py` 各 **EXIT=0**。
