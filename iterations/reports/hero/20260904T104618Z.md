# 英雄组 2026-09-04T10:46Z —— 兑现 backlog `-88 (甲)`:两个 KV 读数的重取

**认领**:`iterations/OWNER_PRIORITIES.md` 本轮**没有属于英雄组的未完成项**(P1/P2 球在协同组,
P3 在总监)。开着的 `[hero]` issue 逐条看过,**没有一条是可动的**(§7 逐条记了理由),
所以按章程取 backlog 最上面一条**可动的**:`-88 (甲)`。
`-89` 的两格((甲) 球在总监→批测台→录像组,(乙) 是 `zusboltcap` 自己的域)按它自己的话不碰。

**一句话**:两个文件的 armed 腿一直在**用抄进测试里的那把梯子驱动它自己**;
`kvgetters` 上周把那堵墙拆了,梯子没跟着换。重取之后**一个数都没变** ——
而那个「没变」正是本轮唯一买不到的读数:**在 getter 答 0 的世界里,没有任何东西能告诉你伪造得对不对。**

---

## 1. 缺陷:一个断言不能证伪它自己的驱动源

`tests/test_lion_q_kill_damage.lua` §4 把 `Q_KV = { 105, 170, 235, 300 }` ——
**一张抄在测试文件第 82 行的四格梯子** —— 喂给 `J.WillMagicKillTarget`,
然后断言「差 45 血」「15% 法强才够」。
`tests/test_zuus_bolt_kill_cap.lua` §2 同形:`make_bolt(0, 140)` / `make_bolt(0, 380)`。

两处都是**当时唯一能做的事**:mock 对 `GetSpecialValue*` 恒答 0,没有任何一帧能供出 armed 腿的读数。
`kvgetters`(2026-09-04T04:56Z,`state.json:kvgetters_20260904T`)把 loader 接到了 KV 快照上,
**墙没了,数字留在原地**,而两个文件的 LIMIT 节各自留了一句「谁捡这根杠杆谁先重取」。
本轮就是那个人。

**为什么这不是洁癖**:一个用 `Q_KV[4]` 驱动、又拿 `Q_KV[4]` 当真值的断言,
**在树动的时候不会红** —— 对它而言那把梯子**就是树**。

## 2. 重取:改的是数字的来源,不是数字

- **Lion §4 + 全语料扫描**:改成驱动 `X.GetImpaleKillDamage( hQ )` ——
  **`X.ConsiderQ` 逐字调用的那一个 helper**,在每一帧自己的句柄和自己的等级上。
  `Q_KV` 留着,**从驱动源降级成逐帧的对照**(不一致就点名,而不是让文件悄悄改基线)。
- **Zeus 新 §2b**:在**每一份 Zeus fixture 的真句柄**上驱动 `X.GetBoltKillHealthCap`。

**`bots/` 与 `game/` 本轮零行改动。** `lionqdmg` / `zusboltcap` 一个字没动,
仍然未 arm、未 promote。本轮不产生新候选,不申请波次(见 §6)。

## 3. 读数(全部是帧上的真数)

| | 帧 | 等级覆盖 | armed helper vs 梯子 | shipped 腿 |
|---|---|---|---|---|
| Lion | **8** | 2、4 | **8 一致 / 0 不一致** | `GetAbilityDamage()` 全 0 |
| Zeus | **10** | 1×4、3×1、4×4(**四级里的三级**) | **9 全部读到真梯子** | 全 0 |

Lion 侧 11 个「射程内活敌」对:**零命中**,**最近一次差 45 血**
(Luna,`f_260820_182906_lion_drain_survived`)。
⇒ **§4 的每一个数与重取前逐位相同**,包括那个 45 和那个 15%。

**那个「相同」是本轮的实质读数,不是安慰**:它说的是当年的伪造**忠于 KV**,
而这件事**在 getter 答 0 的世界里没有任何东西能核**。

## 4. 变异台:重取到底买到了什么(对照做在**旧文件**上)

手法:把 `tests/mock/special_value_shapes.lua` 里的梯子挪一格,
然后**同一棵被污染的树上**分别跑**新文件**与 `git show HEAD` 取出的**旧文件**。

| 变异 | 新文件 | **旧文件(对照)** |
|---|---|---|
| Lion `…235 300` → `…235 **290**` | **EXIT=1,3 条失败**,含点名的 `armed helper reads 300 at rank 4 on this frame, got 290` | **EXIT=1,1 条失败** —— 而那 1 条是 `:226` 上**早就有的**快照检查 ⇒ **§4 自己的贡献是 0 → 2** |
| Zeus `…300 380` → `…300 **375**` | **EXIT=1,1 条失败**,点名 | **EXIT=0,`11 tests, 0 failures`** ⇒ **整个文件对「KV 动了」完全瞎** |

Zeus 那一格是本轮最硬的一条:**梯子挪了,文件全绿**。
`make_bolt(0, 380)` 里的 380 是文件**发给自己**的,树答什么它都不知道。

**还原**:两次都用文件副本还原,`cmp` **逐字节相同**,`git diff --stat` 对快照文件**空**;
还原后两个文件各自重跑 **EXIT=0**。

## 5. 交出去的下一棒(铁律 9:修好 ≠ 做完)

- **`-88 (乙)` 从散文变成了断言**。Zeus §2b 扫到 `f_073148_zuus_lina` **等级 0**,
  因为它**整个 `abilities` 数组都没有**(v1 fixture,`grep -c abilities` = **0**)。
  新断言 `nNoAbilities == 1` **点名这一份**,并在消息里写清两个方向:
  **涨了 = 又有 fixture 需要重 dump;归零 = 录像组的重 dump 落地了,该改指向而不是删断言。**
  ⇒ 那根球**仍在录像组**,但它现在被一条会响的断言拿着,不再靠 backlog 里的一行字。
- **`state.json:kvretake_20260904T`** 登记,并在里面写明它结清了
  `state.json:kvgetters_20260904T:owed` 的第 (1) 条。
- **不申请批测**。本轮零行为改动,`queue.json` **不加行** —— 加一行会让判读台
  去买一个不存在的效应。
- **顺手量到的一条 trunk 红,已立案 GH #484,球在总监**。
  `tests/test_coarmed_attribution_register.lua` **EXIT=1**,点名三对同腿共 armed:
  `creepthink` / `pullcad` / `pullthink` **×** `campbind`(今天 10:xxZ 入集的那个 id)。
  **做过差分**:`origin/main`(`b161d6af`)的 worktree 上跑同一个文件 ⇒ `BASE_EXIT=1`,
  失败正文与工作树**逐字节相同** ⇒ **基线自带,不是本轮的**。
  总监 10:xxZ 报告 §3 的「两条 trunk 红」数的是**开工自检 python 腿**的三条;
  **这一条是 Lua 腿的**,而且正是**那一轮 `campbind` 入集本身**触发的 ——
  登记器是为「入集当天」设计的,它按设计响了。本轮收尾的开工自检
  **独立复现了同一份点名**(见 §9)。
  调用点已读并写进 issue:闸点 `bots/FunLib/jmz_func.lua:8993`(在
  `J.GetCampPullPokeTarget`,`:8986` 内),唯一消费者 `bots/mode_roam_generic.lua:404`
  (在 `Think()` 内),而到达它的每条路径都在 `pullthink`(`:264`)/ `creepthink`(`:265`)
  / `pullcad`(`:343`)的门里面。
  ⚠️ **这与 GH #475 那句「`campbind` 是独立门」不矛盾**:那句说的是**门的写法**
  (条件里不点名兄弟 id ⇒ 不会被 promote 冻死),登记器说的是**归因**
  (同腿 armed ⇒ 每-id (a) 量的是 `outer AND inner`)。**两件事,别互相当成对方的答复。**

## 6. 本轮没做的(故意)

- **没碰 `bots/`**。重取是仪器的活;顺手改 Lion 或 Zeus 的逻辑就是在一个未裁的杠杆上再叠一个。
- **没有替 `lionqdmg` / `zusboltcap` 做任何裁定**,也没给它们买条件 (a)。
- **没有把 §2/§3 的伪造拆掉**。它们覆盖的是**帧按构造供不出的那一格** ——
  一个**非零的 shipped 读数**(fallthrough、负 KV 守卫、闸关等价),
  而 `GetAbilityDamage()` 在每一个 Lion / Zeus 句柄上离线都答 0。
  两个文件的头注释因此**改成写清哪半边还站着**,而不是整段删掉。
- **没有花 AWS 一分钱**(铁律 1)。
- **没有动 `AbilityCastPoint` / `AbilityCooldown`**(那是 `-87`,下一小撮)。

## 7. 开着的 `[hero]` issue 为什么一条都没认领(逐条,免得下一轮重走)

| # | 状态 |
|---|---|
| #451 | 英雄组 09-03 已落地,**等总监裁关闭**,不是新工作单元 |
| #447 | 已落地 —— `hero_chaos_knight.lua:470-481` 现在就是更正后的措辞 |
| #465 | 录像组已落地 —— `test_replay_260820_zuus_static_band.lua` 已是 `nBand == 1` |
| #459 / #471 | 剩余站点**球在总监**(作用域),且 `-86` 明写「先买 `hero-28` 那格读数再谈接线」 |
| #463 / #453 | 都是**非焦点英雄的作用域题**,按 `-84`/`-85` 的先例先问总监 |
| #416 | `zusult` fixture 域,已随 `zusboltdom` 那轮处理 |

## 8. 铁律 6 的两道门

- **静态**:`bash tools/agent/luacheck_gate.sh` → **见收尾节的实测行**。
  ⚠️ 这一门**不能与全量套件并发跑**(GH #439:全量在跑时静态门读到假红,
  而 GH #213 的 pre-push 钩子调的就是它)⇒ 本轮**等分片跑完再开这道门**。
- **动态**:全量套件**分片跑**(每文件一进程、严格串行、180s 封顶),
  读数见收尾节。分片从一开始就用,**没有付上一轮那笔「跑两遍半」的账**(上一轮 §6 的教训)。
  开工时确认 `bots/Customize/soak_side.lua` **不存在**(上一轮 §6 (ii) 那条),
  分片脚本每格结束顺手 `rm -f` 它。

### ⚠️ 本轮踩到的第三个 GH #229 现场:**开工自检自己就跑一条 Lua 腿**

上一轮写的禁忌是「**变异台**与全量套件不许并发」。本轮我遵守了那一条,
却**把开工自检与全量套件并发起了** —— 然后 `ps` 里同时看见两个 `lua5.1 tests/run_tests.lua`:
我的分片在跑 `test_itemdesire_world_assertion`,**自检的「fast Lua detectors」腿在跑
`test_stayfield2_marginal_domain`**。两边都碰**同一个物理开关** `bots/Customize/soak_side.lua`
(GH #229),而且我的分片脚本**每格结束还 `rm -f` 它**。

**失效方向可以论证,而且是单向的**:残留开关会被装载守卫点名 ⇒ **红**;
被我的 `rm` 抢走开关的门测试会读到「未 armed」而 ⇒ **红**。
两条路都指向**假红**,构造不出假绿。
⇒ **绿的格子可信;红的格子必须单独重跑一遍才能引用** —— 收尾节按这条处置。

**⭐ 可迁移的(比这次的读数值钱)**:「不许与全量并发」的清单上**不只有变异台**,
**开工自检也在上面** —— 它不是一个只读的巡检,它跑测试。
本轮之所以还能救,是因为唯一那条红**恰好不读那个开关**(它读源码与 `test_set.md`),
而且我**已经在干净的 `origin/main` worktree 上单独证过它**。换一条红就没这么走运。

---

## 9. 收尾实测

### 9.1 开工自检(铁律 10)

第一次跑**没跑成**,而且**是我自己造成的**:我给它套了 `timeout 300`,它死在
「trunk health (python test suite)」那一节,**整条 python 腿一个读数都没有**。
`echo "EXIT=$?"` 打出来的 0 **是那个 echo 的**,不是自检的(铁律 6 那条管道教训的同族)。
⇒ 重跑,把真退出码写进文件本身:

```
SELFCHECK_TRUE_EXIT=3          # 0 干净 / 2 未核验 / 3 有发现
```

| 腿 | 读数 | 判 |
|---|---|---|
| unlanded work | `OK: no unlanded work in the certifiable window.` | 干净 |
| stable 锚(铁律 3) | `2 anchor(s) checked -- OK` | 干净 |
| trunk health(python) | `89 passed, 1 failed, 1 uncertifiable`;红的是 `tests/test_wave_gate_keys.py` | **不是本组的,也不是新的** —— GH #479,总监 10:xxZ §3 已判「已退回批测台,**故意仍红**」 |
| trunk health(Lua) | `RED test_coarmed_attribution_register.lua` | **本轮立案 GH #484**(见 §5),已用 `origin/main` worktree 证明是基线自带 |
| `test_selfcheck_lua_leg.py` | **`UNCERTIFIABLE`**(120s 预算内没跑完,`75 file(s) in 120.1s`) | **⚠️ 大概率是我造成的** —— 分片全量正在同一台容器上抢 CPU。**`UNCERTIFIABLE` 不是通过**:自检那条腿**这一轮没人看过**。 |

### 9.2 铁律 6 —— 静态那半

```
bash tools/agent/luacheck_gate.sh
  installed luacheck (it was missing; apt package lua-check, bounded)
  luacheck bots game: 0 warnings
  GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
GATE_TRUE_EXIT=0
```

本容器冷启,gate **自己把 `lua-check` 装上**(不是 `luacheck`,AGENTS.md 那条)。
**排在分片全量跑完之后**开的这道门(GH #439:全量在跑时它读到假红),
开门前确认 `bots/Customize/soak_side.lua` 不存在、容器上零个 lua 进程。

### 9.3 铁律 6 —— 动态那半(分片全量)

**303 个分片全部有读数**,`rc=0` **300** 个,非零 **3** 个。

⚠️ **我的脚本自己打的 `RED=4` 是错的,差一个**:那行 awk 写成
`$1 != "SHARDS_RUN"`,而文件里那行是 `SHARDS_RUN=303`,**两个串不相等**,
于是它把自己的汇总行也数成了一条红。**真数是 3。**
(留在这里而不是改完就算,是因为它正是本仓反复记的那类:**一个看起来很正常的数字**。)

三条全部**单独重跑**(容器上没有别的 lua 进程),并在
**`origin/main`(`b161d6af`)的 `git worktree`** 上跑同一个文件做 mod-vs-base 差分:

| 文件 | 我的树 | `origin/main` | 判 |
|---|---|---|---|
| `test_coarmed_attribution_register` | `EXIT=1`,`11 tests, 1 failures` | `EXIT=1`,`11 tests, 1 failures` | 失败正文 **`diff` 逐字节相同** ⇒ 基线自带 ⇒ **GH #484**(§5) |
| `test_towercreep_stale_domain` | `EXIT=1`,`5 tests, 4 failures` | `EXIT=1`,`5 tests, 4 failures` | 失败正文 **逐字节相同** ⇒ 基线自带,backlog `-87` 早已登记 |
| `test_itemdesire_world_assertion` | `EXIT=124`(独跑给到 **420s** 仍超时,只打了 **4** 个点) | `EXIT=124`,**同样 4 个点** | 两棵树**同样跑不完** ⇒ 不是本轮的。**本轮自己量的**,不是引上一轮的结论 |

**⭐ 顺带把 GH #124 的形状再收紧一格**:「全量在一个进程里跑不完」里,
**至少有一个文件在 trunk 上 420 秒都跑不完**,而且**两棵树的进度逐点相同**
(各 4 个点)—— 这不是「整体慢」,是**一个有名字的文件**。
上一轮在 400s 上量到同一件事;本轮把预算抬到 420s **仍然复现**。

**⭐ 为什么这三条重跑不是形式主义**:§8 那条并发事故的失效方向指向**假红**,
所以**红的格子在单独重跑之前不可引用**。三条都在独跑下**原样复现**
⇒ 并发**没有制造**这三条里的任何一条,它们都是真的、而且都不是本轮的。

### 9.4 本轮改动的两个文件

```
lua5.1 tests/run_tests.lua test_lion_q_kill_damage    EXIT=0   13 tests, 0 failures
lua5.1 tests/run_tests.lua test_zuus_bolt_kill_cap    EXIT=0   12 tests, 0 failures
```

七个兄弟绊线各自单独跑过,全部 `EXIT=0`:`test_replay_260819_zuus_w2_leak`(16)、
`test_zusult_pre_ladder_claim_retake`(9)、`test_replay_260819_zuus_boltdom`(15)、
`test_fixture_kv_getters`(17)、`test_gate_claim_consistency`(16)、
`test_zuus_static_field_second_consumer`(14)、`test_smoke_load`(3)。

---

## Token 用量

```
TOKENS total_in=19,436,282 out=67,327 turns=116
```

(统计时刻 = 收尾前;统计之后的 commit / push 回合不计入。本轮 `total_in` 的
主要可归因项是**等**:分片全量约 15 分钟 + `test_itemdesire_world_assertion`
在两棵树上各 420 秒的独跑差分。**分片从第一次就用对了**,没有重跑全量 ——
上一轮那笔「跑两遍半」的账本轮没有再付。)
