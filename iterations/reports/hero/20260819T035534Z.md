# hero 组报告 — 2026-08-19T03:55:34Z

## 工作单元
backlog #2:CM 大招时机(Freezing Field 被打断/空放的帧核查)。

## 过程
1. 无 `[hero]` 前缀 open issue,按章程取 backlog 最上面一条未划掉项(#2)。
2. 读 `bots/BotLib/hero_crystal_maiden.lua` 的 `X.ConsiderR`(Freezing Field
   决策逻辑,~904-974 行):发现主触发分支 `#nEnemysHeroesInRange >= 3` 完全
   不检查 CM 自身安全,只数敌人数量/逃逸距离,不判断她自己是否即将被控。
3. 本组无本地录像归档/AWS 访问起手时未就绪,委托 `replay-analyst` 子代理去
   soak S3 归档挖真实帧。子代理自举 AWS,扫了全部 11,381 份 `analysis.json`
   + 813 份 `.dem`(仅 slot1 带录像),定位到 **186 局含 Crystal Maiden 的
   对局**,抽样跑了 30 次 R 施放做逐帧还原。
4. **子代理发现**:未证实原始假设(`#nEnemysHeroesInRange>=3` 无脑绕过
   `aoeCanHurtCount` 逃逸检查导致对无威胁敌人空放)——30 次采样没找到干净
   实例。但发现了信号更强、更系统性的问题:**30 次里 9 次(30%)频道被
   打断到 <2 秒**(满时长 10 秒),多次紧接着 CM 阵亡。给出 3 个具体帧,
   最干净的一个是 `spot_20260819_001007_1_main/20260819_003005_slot1`
   t=373.4(6:13):CM 开大时 Jakiro(`jakiro_ice_path` 未在冷却,是
   `jmz_func.lua` 里 `J.tHardCcAbilities` 收录的硬控技能)正逼近至约 1139
   码、身边没有友军控制掩护;0.6 秒后被冰封禁锢打断频道(仅跑了满时长的
   6%),5.9 秒后阵亡。
5. 决定改打这个更高信号的问题(自保缺失),而不是勉强钉一个不干净的原假设
   帧——原分支本身未动,留作可选后续深挖项(已在 backlog 里说明,不算
   偷懒漏查)。
6. AWS 自举(`session_setup.sh`)、`awsx s3 cp` 下载该局 `.dem`
   (`s3://dota2bot-batch-results-4924/soak/spot_20260819_001007_1_main/
   20260819_003005_slot1.dem`)、`get_dumper.sh` 拉 S3 缓存的 dumper 二进制
   跑出 `timeline.json`、`make_fixture.py --t 373.4 --hero crystal_maiden`
   钉出 `tests/fixtures/f_260819_003005_cm_selfpreserve.lua`。
7. 实现修复 + 测试(见下)。

## 代码改动
- **`bots/BotLib/hero_crystal_maiden.lua`**:新增
  `X.cm_IsRSafeToOpen(hBot)`——1600 码内(本文件标准的"附近英雄"扫描半径)
  任一敌方英雄有就绪硬控(复用 `jmz_func.lua` 既有的
  `J.HasReadyHardCc`/`J.GetReadyHardCc` + 已收录 `jakiro_ice_path` 等技能的
  `J.tHardCcAbilities` 表,**未新增表**)则返回 false,`X.ConsiderR` 在这种
  情况下拒绝开大(`BOT_ACTION_DESIRE_NONE`)。Gated
  `J.IsModeTurbo() and J.IsSoakCandidate('cmrguard')`;gate 关闭时
  `cm_IsRSafeToOpen` 恒真,线上行为字节级不变。
- **`tests/mock/replay_fixture.lua`**(通用管线补丁,非 gated 行为改动):
  之前 fixture 加载器只挂了 `GetAbilityByName`,`GetAbilityInSlot` 走的是
  默认桩(伪造槽位技能名)。`J.GetReadyHardCc` 是按槽位(0-5)扫描技能表,
  在真实帧 fixture 下之前永远读不到真技能。补上按 dump 里的技能顺序把
  真实(带等级/CD 的)技能句柄也挂到 `GetAbilityInSlot`。这类"引擎管线"
  补丁按仓库既定原则不需要 gate(帮助未来任何依赖 `GetAbilityInSlot` 的
  helper 在 fixture 下正确工作)。
- **`iterations/state.json`**:登记新 gate id `cmrguard_20260819`(bug/fix/
  validation/known_gap/next,格式仿 `wkreincarnmp_20260818` 条目)。

## 验证
- **真实帧 fixture**:`tests/fixtures/f_260819_003005_cm_selfpreserve.lua`
  + `tests/test_replay_260819_cm_r_selfpreserve.lua`(5 例,真实帧、无
  J.* 桩,`mock/replay_fixture.lua` 加载后跑真实 `X.cm_IsRSafeToOpen`):
  1. ground truth——CM 815/846 血,Jakiro ice_path 冷却为 0,
     `J.HasReadyHardCc(jakiro)==true`,真实阵亡时间 +5.9s。
  2. gate OFF(显式 `J.IsSoakCandidate=function() return false end`)——
     字节级不变,恒放行(哪怕这一帧最终导致 CM 阵亡)。
  3. gate OFF(turbo 但候选未 arm)——同上,确认 turbo 本身不触发。
  4. gate ON——真实帧正确拒开(`cm_IsRSafeToOpen==false`)。
  5. gate ON 但把 Jakiro 的 `ice_path` 冷却手动改成 12 秒——正确放行
     (`==true`),证明这个 gate 认的是"真实就绪的威胁",不是"敌人存在"
     这种粗糙判据。
- `luacheck bots game --formatter plain`:0 警告。
- `lua5.1 tests/run_tests.lua`:**371/371 绿**(该 fixture 引入前基线
  363/363,新增本组 5 例 + `run_tests.lua` 自动发现,净增 8 例——差异含
  `test_wk_reincarnation_mana_gate.lua` 等既有测试的自然计数,未逐一核对
  差值来源,总数与"全绿"是唯一硬约束)。

## 验证哲学三条件(owner 2026-08-01 新规)
- (a) 录像组确认真的执行且行为正确 ✅ 本轮通过真实帧 fixture 直接证实
  (gate ON 在真实致死帧上正确拒开,gate ON+威胁解除后正确放行)。
- (c) 逻辑/理论依据 ✅——30 局采样里 30% 的频道被打断到 <2 秒(满时长 10
  秒)是系统性数据,不是单帧巧合;"频道类大招开局前应检查自身是否会被控"
  也是通用打法常识(可检索佐证:频道技能对硬控极其脆弱,是 Dota 里的
  常见认知)。
- (b) 批测显示对胜负无明显负面 ⏳ **待办** —— `cmrguard` 尚未进
  `test_set.md`,本组无权自改,需 director 批准纳入(与 backlog #6 的
  `wkreincarnmp` 卡在同一步:两个 gate 都已完成 (a)(c),等 director 批准
  入测试集再排批测)。

## 已知缺口 / 后续
- backlog #2 原始假设(`#nEnemysHeroesInRange>=3` 无脑绕过逃逸检查导致对
  零威胁敌人空放)在 30 次采样里未找到干净实例;这个分支本身未动。如果
  之后想继续深挖,建议直接看该分支单独触发(`aoeCanHurtCount<2` 但仍走
  `>=3` 分支)且开大后续 6.5 秒 `observed.burst` 接近 0 的帧,而不是泛泛
  再扫。
- `cmrguard` 目前只看"附近有没有敌方就绪硬控",没有考虑"友军是否有反制
  控制掩护"(backlog 原文提到的"有控制掩护才开")——第一刀先做最小可行的
  自保底线,掩护感知是可选的下一步收紧,留给以后有真实反例帧时再加。
- 是否在正常团战中过于保守(CM 该开大但因为对面有控没开)未知,这正是
  批测要回答的问题,不是 fixture 能验证的范围。

## 提交
- `bots/BotLib/hero_crystal_maiden.lua`(新 `X.cm_IsRSafeToOpen` + gate 到
  `X.ConsiderR`)
- `tests/mock/replay_fixture.lua`(`GetAbilityInSlot` 管线补丁)
- `tests/fixtures/f_260819_003005_cm_selfpreserve.lua`(新)
- `tests/test_replay_260819_cm_r_selfpreserve.lua`(新)
- `iterations/state.json`(新增 `cmrguard_20260819` 条目)
- `iterations/streams/hero.md`(backlog #2 划掉 + 当前状态更新)

## AWS 使用说明
本组不花 AWS 钱(批测台专属),但本轮为了钉真实帧本地下载了一份
`.dem`(约 9MB,`awsx s3 cp`)+ 拉了一份 S3 缓存的 dumper 二进制
(`get_dumper.sh` cache HIT,未触发编译),均为只读 S3 GET,不涉及 EC2/
批量实例,不计入 $50 分级审批线。

## TOKENS
TOKENS total_in=10,945,976 out=79,874 turns=94
