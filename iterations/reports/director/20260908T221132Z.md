# 总监 2026-09-08T22:11Z —— `cmrguard` 退回出集(42 → 41),判定完结 **1**(⛔ 不达 ≥2)

**一句话**:上一轮排的 ① 取掉了(判 `cmrguard`);而本轮最该被读的是 ——
**这条 gate 的 veto 环读的是敌方技能的 cast range,本仓库的 fixture 加载器从不读它,
于是 armed 的 gate 在它自己的立案帧上放行了那条要了 CM 命的通道**;
底下还有一件更贵的:**同一个加载器在三十行之下,已经为相邻的那个 key 诊断并修好了一模一样的危险**。

零 AWS、零波次、**`bots/`+`game/` 零 diff**、不发 owner 邮件、`DECISIONS_NEEDED` +0。
裁定全文档案 `iterations/streams/test_set.md §GD`(§GD.0–§GD.7),机器键 `state.json:cmrguard_RETURNED_20260908`。

---

## 0. 开工自检(铁律 10)

⛔ **本轮第一条命令带了 `timeout 400`,被砍在 `trunk-red(python)` 那条腿上,真码 `EXIT=124`,
而 harness 报 `exit code 0`** —— 「后台包装吞掉真码」**第七次兑现**。躲过去的仍只是「把真码 echo 进文件」这个习惯
(上一轮 ④ 要立的守卫仍未立,顺延到本轮 §8)。GH #358 的 120s 预算问题原样存在。
去掉 `timeout` 重跑一遍(§8 给最终读数)。

⚠️ **纪律 3 本轮一发**:本轮第一条命令是 `routine_selfcheck.sh … | tail -60`,§22 守卫当场拒
(`REFUSED: stdout is a PIPE … SELFCHECK_EXIT=2 REFUSED`)。**不新立措辞**,登记而已。
此后本轮所有要读退出码的命令都走 `tools/agent/rc.sh`。

`queue-rulings` 腿本轮点名 **hero-50**,**当轮清零**(§5)。

---

## 1. 选取依据(不是偏好)

上一轮 §GC.6 第 3 条逐字写着「**没有判 `cmrguard`** —— 同一档同一读数的第三条,
它的 (a) 买不买得到**本节没有量**,原样留给下一轮」,并把它排成「下次触发 ①」。本轮就是它。

复读:`arm_since.py` **armed 42 / pinned 42**,`verify_coverage.py` 里 2026-08-19 的 20 天档
`verify=0` 三条中前两条已于 §GC 退集,**`cmrguard` 是最后一条**(`narrat=3`)。

---

## 2. ⭐⭐⭐ 主轴:**armed 的 gate 在它自己的立案帧上放行**

`cmrguard`(`bots/BotLib/hero_crystal_maiden.lua:1612`)的判据是

```
GetUnitToUnitDistance(cm, e) <= ( hCc:GetCastRange() or 0 ) + X.nRGuardCloseBuffer   -- 400
```

最后一项是 **GH #34 那次收窄的全部内容**(收窄前 gate 是 range-blind 的),
而它读的是**敌方**英雄的技能句柄 —— 本仓库加载器唯一不服务的地方。

立案帧 `tests/fixtures/f_260819_003005_cm_selfpreserve.lua`
(hero.md backlog #2 / GH #34:Jakiro 持 ready `ice_path` 距 CM **1138.6u**,0.6s 后把她打断、她随即死亡):

| 读什么 | 读到 | 来源 |
|---|---|---|
| Jakiro 距离 | **1138.6** | 真帧 |
| `ice_path:GetLevel()` / `GetCooldownTimeRemaining()` | ≥1 / 0 | 真帧 |
| `J.GetReadyHardCc(jakiro)` | **非 nil** | 真帧(**能力那一半买到了**) |
| `ice_path:GetCastRange()` | **0** | ⛔ `tests/mock/bot_api.lua:184` 的 `^Get -> 0` 兜底 |
| `X.cm_IsRSafeToOpen`,armed,**不写入 cast range** | **true = 放行** | `1138.6 > 0 + 400` |
| 同上,写入 `GetCastRange = 1000` | **false = 拦下** | **杠杆本身没坏** |

⇒ **仪器坏了不是杠杆坏了**;但 (a) 问的是「这个输入到底出没出现过」,而在 fixture 路径上**一次都没有**。

---

## 3. ⭐⭐ 塌缩是**全量**的,而且量的是**来源**不是值

`tests/_blind_a_cmrguard_sweep.lua`(110 份 fixture,~3s,按房规拆出 fast leg / GH #358 的 120s 预算):

```
G KV_ROSTER 5 (axe,crystal_maiden,lion,skeleton_king,zuus)
C FIXTURES 110   C HARDCC_HANDLES 487
C CR_ZERO 350    C CR_NONZERO 137
C KV_SERVED 137  C CATCHALL 350   C ACCIDENTALLY_RIGHT 100
C READY 304      C READY_ZERO 217
```

⭐ **`CR_NONZERO 137` 与 `KV_SERVED 137` 逐位相同,`CR_ZERO 350` 与 `CATCHALL 350` 逐位相同** ——
**整份语料里没有一个 0 是「读了 KV、发现没声明」得到的**。读得出来的四个载体
(`crystal_maiden_frostbite` 600 / `lion_impale` 650 / `lion_voodoo` 575 / `skeleton_king_hellfire_blast` 525)
**全部是焦点五自己的技能**;其余 14 个载体(jakiro / witch_doctor / ogre_magi / chaos_knight /
shadow_shaman ×2 / sven / dragon_knight / storm_spirit / earthshaker / centaur / slardar / tidehunter / axe)一律读 0。

⛔ **而 0 是分不出来的**:`axe_berserkers_call` 的真答案**就是** 0(无目标、自身半径),
它那 28 个 0 与 `jakiro_ice_path` 那 33 个 0 **走同一行代码**。100/350 碰巧是对的 ——
**对的值、错的理由**,从读数上不可分辨。剩下 250 个方向一致:**把远程 CC 一律当成自身半径**,
把环缩到 400,**恰好是掐死这条 gate 的那一边**。

---

## 4. ⭐⭐ 底下那件更贵的:**一次没有推广的修复**

`tests/mock/replay_fixture.lua` 里 `AbilityCastRange` 的 getter **只在该 key 被声明时才装**
(`if cast_range ~= nil then …`)。**三十行之下**,`AbilityDamage` 的 getter 是给 KV 英雄的**每一条技能无条件装**的,
加载器自己的注释逐字写着理由:一条没有 `AbilityDamage` 的技能必须答 0 **因为加载器读了 KV 没找到**,
而不是因为**什么都没装、通用的 `^Get` 兜底替它答了**;两者**「从读数上不可分辨」**,
且**「作为证据并不等价:`lionqdmg` 与 `zusboltcap` 都压在那个 0 上」**。

⇒ **房子已经把这个失效模式诊断清楚、写下来、并对一个 key 修好了;紧挨着它上面的那个 key 没有修,
而一条 armed 的 id 正压在后者的 0 上骑了十个波次。** 这不是新发现的问题,**是一次没有推广的修复**。

⚠️ **债不是这一条 id 的**:读敌方 cast range 的调用点**恰好两个**(断言钉住),另一个是
`jmz_func.lua:7108` 的 `ccburst` 窗口 —— **已发行、无门、每一局都在跑**。
今天**没有任何 fixture 能测那条已发行路径的 delivery 项**,错的方向一样。

---

## 5. 裁定与同轮义务

### 5.1 `cmrguard` 退回出集(42 → 41)—— **判定完结 1**

三堵墙(全文 §GD.6):

1. **fixture 路径**买不到 (a):§2 / §3。
2. **波次路径**从来没有过读数:W39/40/41/52/53/54/55/56/57/58 里 `cmrguard`
   **只以全量成员串(380–501 字节)的一员出现,一次都没有被单臂 arm 过** ⇒ 条件 (b) 也没有读数。
3. **它自己的 promote 门槛结构上够不着**:`cmrguard_NOT_PROMOTED_20260820` 把 promote 挂在
   「#63 + #66 的修订落地之后」,而那两条修订就是 **`cmrcap`** 与 **`esaftershock`** ——
   `armed_since.json` 里**两条都没有行**(从未 armed),`cmrcap` 还要求与 `cmrguard` 同臂。
   ⭐ 本轮另量到更强的一句并钉成断言:**只要 range 项是兜底的 0,`cmrcap` 在任何帧上都改变不了判决**
   (`math.min(0,200)==0`,两份 fixture 上 armed±`cmrcap` 判决逐位相同)。

⛔ **退集不是 reject**:gate、helper 体、`X.nRGuardCloseBuffer = 400`、`X.nRGuardRangeCap = 200`
**逐字保留**,`bots/`+`game/` **零 diff**,(c) 成立且未被取代。重新入集条件进 owed 行
`cmrguard_castrange_instrument`,**不留在散文里**。

⚠️ **不掉进 `pullcad` 陷阱,查过了**:`promote_atoms.json` 里 `cmrguard` **零次出现**(断言钉住)
⇒ 本次退集**没有冻死任何一个 armed 杠杆**。
⚠️ **载体项 7 → 7 逐字不变,量出来的**:`carrier_terms.py --arm` 对 42-id 与 41-id 两串各跑一次,
`TERMS` 行**逐字节相同**(`crystal_maiden` 由 `cmqreach` 承载),`0 unresolved` 两次,`10 hero → 9 hero`。
⛔ **W58 及更早不与 41-id 家族并池。**

### 5.2 章程 §BB.4 的同轮义务:`hero-50`

`hero-50`(`axebhreach`,GH #642,20:2xZ 到,本轮 `queue-rulings` 唯一一条)裁 **APPROVED-SCAN**
(与 hero-41..49 同档;`bundle` 逐字写着「NOT armed —— P4.2 冻结期内本条不请求入集」)。

⛔ **理由逐条核过源码不是照抄请求**:(i) `X.axe_IsHungerFightTargetInReach`(`hero_axe.lua:1041`)第一行
`if not ( J.IsModeTurbo() and J.IsSoakCandidate('axebhreach') ) then return true end` ⇒ 未 armed **原样放行**;
(ii) 团战落单点(`:1111-1140`,在 `X.ConsiderW` 内)确实遍历 `nInBonusEnemyList`(`nCastRange + 200`,`:1069`),
四条过滤之外**没有任何距离项**,且是**最小值搜索**而非首次命中 ⇒ 「环外赢家挤掉射程内的合法候选」
**是闭式的源码事实**;(iii) `nCastRange = abilityW:GetCastRange() + aetherRange`(`:1056`)⇒ 验收里
「逐帧按 rank 与背包算」那条必须照做。
⚠️ **没核的一件事**:请求称团战点是 ConsiderW 里**唯一**这种形状的落单点 —— 我只核到 `:1468` 那个同形循环
属于 `X.ConsiderR`(另一个技能),**没有逐个走完八个落单点**;该「唯一性」按请求方自述采信,读数不依赖它。
**本请求不夹带总监活**(与 hero-49 不同)。

---

## 6. ⛔ 判定完结 **1**,不达 owner P4.2 的 ≥2 —— 不粉饰

本轮预算花在**买下让这条裁定成立的读数**上(sweep + 11 条断言 + 8 发变异台 + 三处侧效核对),
而 §GC 的 ① 逐字只点了这一条 id。**不为凑数去裁一条没量过的 id** —— 那正好是 P4.2 要禁的方向
(「推进的度量是集合变小」不是「裁定条数变大」;把没量过的 id 判掉既不缩集也不产生证据)。

**下一轮的 ① 强制是再取两条**,从 `verify_coverage.py` 的 `narrat=1` 四条清起:
`campsel` / `pulllane` / `pullthink` / `roamidle`(核验记录最少的一档)。

---

## 7. 量具与验收

* `tests/test_blind_a_cmrguard.lua` —— **11 checks / 0 failed**(`RC_EXIT=0`)。
* `tests/_blind_a_cmrguard_sweep.lua` —— 110 fixture / 487 句柄,~3s,读数见 §3。
* `tools/agent/mutstand_blind_a_cmrguard.sh` —— **8 CAUGHT / 0 SURVIVED / control_ok=1**(`RC_EXIT=0`),
  六个文件 `git diff --quiet` **逐字节还原 YES**;`tests/test_mutstand_restore_trap.py` 对新台**三项全 ok**。
  **八发全部朝「盲区被解除 / 理由被消解」的方向**(M1 KV roster 长出 jakiro、M2 getter 无条件装、
  M3 兜底不再答 0、M4 buffer 拉宽到 1200、M5 收窄被回滚、M6 绿测试不再写入、M7 加载器的声明被改写、
  M8 已发行的消费者长出一道门)。
* ⚠️ **M6 第一轮 SURVIVED,而它是对的、断言是错的**(纪律 2 的正面兑现,与 §GC.4 同型):
  `GetCastRange = 1000` 这个子串在那份测试里**出现两次**(另一处是 Centaur 的「假装它是远程」变异),
  宽针脚在 ice_path 那行被删时**仍然是绿的**;改钉整条 `rawget(icePath, '__spec').GetCastRange = 1000` 后 CAUGHT。
* **owed 行的三条 needle 在裁定时刻逐字验在场(各 1 次)** —— §GC.7 的直接教训;
  `pending_rulings.py --owed-only` 复读该行 **OWED**(`still contains 3 of 3`),19 → **20 行**。

**铁律 6**:`bash tools/agent/luacheck_gate.sh` → `luacheck bots game: 0 warnings` / **`GATE_EXIT=0 CLEAN`**;
`python3 tools/agent/py_gate.py` → **86 ran / 0 findings / 0 uncertifiable / `RC_EXIT=0`**;
两份新 Lua 文件单独 `luacheck` **0 警告**。**未用 `RULE6_BYPASS`。**
⛔ **动态半(Lua 全量,~100min / GH #124)未跑,不作声称** —— 本轮 `bots/` 零 diff。

---

## 8. 自检最终读数与顺延项

去掉 `timeout` 重跑,**真码 `SELFCHECK_REAL_EXIT=3`,10 条腿全跑完**:
`FINDINGS (exit 3): cadence queue-rulings owed-executions trunk-red(python)` /
`UNCERTIFIABLE (exit 2): none` / `NOT RUN (inside a leg): tests/test_selfcheck_lua_leg.py`。
fast Lua **86 个 detector 文件 / 0 failures**(门自己写着这是 FAST SUBSET)。

四条 FINDINGS 本轮**都交代了**:

* **`queue-rulings`** —— 唯一一条 `hero-50`,**本轮清零**(§5.2)。
* **`owed-executions`** —— 设计中(20 行);本轮**新增一行**(`cmrguard_castrange_instrument`,读 OWED)
  并给 `hero_domain_scan_2_30_31` **补了一根针**(下条)。
* **`cadence`** —— 2 个洞**都在 strategy**(09-07T19:00→23:30 的 4.5h、09-08T10:51→14:31 的 3.7h),
  该组本轮有实产出(`20260908T194001Z`)⇒ **不升级**。
* ⛔ **`trunk-red(python)` —— 是我自己本轮弄红的,并在同一工作单元修掉了。**
  ⭐⭐ **而这件事最该被读的地方是:红它的正是上一轮(§GB.2)为「裁定搭车了、针没人加」这个缺陷立的那根针,
  而本轮我又一次只做了两个手工动作里的第一个 —— 机器当场抓住。**
  形状:我把 `hero-50` 的 `director` 写成了**纯字符串**,而 `tests/test_domain_scan_pass_pins.py`
  读的是 `director.get("owed_row")` ⇒ `AttributeError: 'str' object has no attribute 'get'`,
  三条断言同时 ERROR(**读数是 `114 passed, 1 failed, 1 uncertifiable`,那 1 failed 就是它**)。
  修法:`director` 改成与 hero-49 同形的 dict(带 `owed_row` / `deliverable`),
  并把 `hero-50` **同轮加进** `hero_domain_scan_2_30_31` 的 `done_when.contains`(针数 **21 → 22**)。
  反向核验:**单边删除 `hero-50` 那根针 ⇒ 红(`RC_EXIT=1`)**,还原后绿(`RC_EXIT=0`,5 checks),
  `git diff --stat` 只剩本轮的真实改动。
  ⚠️ **这条红不是「有人在无关 finding 上方插了几行」那一类(GH #637),是一条真缺陷,方向也对**
  —— 它拦下的正是「批准搭车但读数永远没人欠」的那个失效。**§GB.2 立的针,在它落地的第二轮抓到了立它的人。**

**顺延**:①**给「后台包装吞掉真码」立守卫**(**第七次**兑现,连续第四轮顺延)
②GH #358 的 120s 预算要人裁 ③`hero_domain_scan` 九份读数逐份读通
④backlog 100(`text_absent` 注册时守卫) ⑤backlog 99(桩转轴普查;本轮是它的**第三个实例**,不是落地)
⑥GH #633 没买到的那一半 ⑦**为本轮的 cast-range 仪器缺口开 [harness] issue**(本轮 MCP 未试,owed 行已登记,issue 号仍空)

---

## 9. 本次会话 token 用量(铁律 8)

```
TOKENS total_in=19,772,232 out=88,312 turns=108
```
(统计时刻之后的收尾回合不计入;当量级参考。)
