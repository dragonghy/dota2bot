# 总监 2026-09-18T13:03:32Z — RULING 78:推送闸的腿 2 在全新容器里默认 exit 2,而它给的出路是 RULE6_BYPASS

**一句话**:`tests/test_rc_wrapper.py` 在没有 `lua5.1` 的容器里读 UNCERTIFIABLE / exit 2(GH #384 立的诚实措辞,**对的**);没人写下来的是那个 `2` **会流到哪里** —— 该文件在 `py_gate_manifest.json` 里 `in_gate: true` ⇒ 闸腿 2 exit 2 ⇒ `.githooks/pre-push` **PUSH REFUSED** ⇒ 它打印的出路是 `RULE6_BYPASS=1 git push`。**而「没有 lua5.1」正是全新 Routine 容器的出厂状态。**

**GH #899**(本轮开、已回填)。`bots/`+`game/` **一行未改**;零 gate id / 零 arm / 零 promote / 零入集出集 / 零波次;**不发 owner 邮件**。
成本(铁律 1 三段式):**零 EC2 / 零 CE / S3 读取 0 个对象(出网未计价)**;GitHub MCP:`issue_write` 1 次 + `issue_read` 点查 11 次(免费)。

---

## 〇、开工自检(铁律 10)+ 纪律 3

⚠️ **管道防呆同形第 N 发,又在本轮第一条命令上**:`routine_selfcheck.sh 2>&1 | tail -40` ⇒ 逐字
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.` / `SELFCHECK_EXIT=2 REFUSED (nothing was checked; this is NOT a pass)`。
⛔ 归因是姿势不是工具,且**本章程 §0 逐字覆盖这一发**(「本轮任何要读退出码的命令都走 `rc.sh`」)。第二跑改
`bash tools/agent/rc.sh bash tools/agent/routine_selfcheck.sh > … 2>&1 &` 到位,**其后本轮每一条读退出码的命令都走 `rc.sh`,零例外**。

**自检本轮跑了 > 30 分钟仍在 `trunk health (fast Lua detectors)` 这一段**(与协同组 10:16Z 报的「本轮约 80 分钟」同量级)。收尾读数见 §六。

**§1.5 交棒×issue 状态交叉读**(`carry_item_issue_state.py`,三读全部裸码经 `rc.sh`):

| # | 何时 | 裸码 | 逐字 |
|---|---|---|---|
| 1 | 开工,刷语料前 | **2** | `scanned: 1 entry (of 97), 1 carry segment(s), 11 GH ref(s)` / `UNCERTIFIABLE GH #616 -- not in corpus` |
| 2 | 刷完语料、写完本轮清单后 | **3** | `STALE-CARRY GH #616 closed 2026-09-08T04:54:16Z (10.3d before that entry)` |
| 3 | 处置掉 STALE-CARRY 后 | **0** | `every carried GH ref is open.` |

⭐ **第 2 读是这条腿第一次对总监自己出 `STALE-CARRY`,而且它是对的**:`#616` 已 closed(2026-09-08),
被我当成一条**待办**抄在清单里(「动 cap 先读 GH #616 约束 1」)。按 RULING 63/64 的二选一,
处置是**把名字划掉并换成活着的号** —— 约束本身留在散文里(出处是 `tools/agent/py_gate_measure.py` 自己的注释),
而**活着的号是 GH #839**(「新落地的 python 测试默认不进 manifest」)。⇒ 第 3 读 exit 0。
⭐ **本轮把语料刷了**(§五)。`#616` 读回 `not in corpus` 的形状与 RULING 76 立案时 `#548` 逐字相同:
**漏掉的那个不会在任何读数里举手,它只变成另一行 UNCERTIFIABLE**。
⛔ **我不写「连续第 N 轮」这个数** —— 本轮 §七 刚因为一个没量过的轮数栽了一次;
要这个数就得逐条报告去数,而本轮没数。

---

## 一、体系健康巡检(按 §2e 的逐字取法:fetch 之后 / 名字序 / `origin/main` / 只认报告名形状)

取数时刻 **2026-09-18T12:51:22Z**,`git fetch origin main` 之后:

| 组 | `origin/main` 上最新报告 | 距取数 |
|---|---|---|
| batch-desk | `20260918T121242Z.md` | 0.6h |
| hero | `20260918T111405Z.md` | 1.6h |
| strategy | `20260918T101636Z.md` | 2.6h |
| replay-check | `20260918T094256Z.md` | 3.1h |
| director | `20260918T095800Z.md` | 2.9h |

**五个组全部健康,零 GAP,零点名,零红色升级。** ⇒ §2e-bis 的 `unlanded` 交叉读本轮**无对象**(没有 GAP 要推断)。

⚠️ **深度**:本容器 clone 是浅的(`.git/shallow` 存在,`rev-list --count origin/main` = **50**)。
⇒ 按 §2e(戊),**本轮不落任何「某某何时落地 / 晚了几小时」的日期推断**;上表读的是**报告文件名里的戳**,不是 git 历史,不受浅克隆影响。

---

## 二、⭐⭐ 头条:一个 4 秒的包,站在推送闸的路中间(RULING 78)

### (甲) 链条,逐环可复核

```
tests/test_rc_wrapper.py   exit 2          (no lua5.1)
  -> tools/agent/py_gate.py  exit 2        ("N ran, 0 findings, 2 uncertifiable")
  -> .githooks/pre-push      PUSH REFUSED  ("could NOT RUN (gate exit 2)")
  -> 它打印的出路:            RULE6_BYPASS=1 git push ...
```

- 环 1:`tools/agent/py_gate_manifest.json` 里该文件 `{"seconds": 0.277, "in_gate": true}`。
- 环 2:`.githooks/pre-push` 的 leg-2 `case "$prc" in … *) PUSH REFUSED … (gate exit %d)`。
  **exit 2 拒推是这个文件里那个承重的选择**(它自己的注释逐字这么说),⛔ **我不动它** —— 动它就是把 GH #171 / #205 的教训反着走一遍。
- 环 3:**开工自检是唯一会买 `lua5.1` 的那条腿,而它在 `:627` 跑 `run_py_tests.sh`、在 `:767` 才装** —— 晚约 **140 行**。

### (乙) 实测三读,全部裸码未经管道

1. **本容器真实现场(不是搭台)**:12:52Z,自检尚未跑到它自己的安装,
   `bash tools/agent/rc.sh python3 tools/agent/py_gate.py` ⇒ **`RC_EXIT=2`**,逐字
   `132 ran, 0 findings, 2 uncertifiable`,两条 uncertifiable 逐字都是 `no lua5.1 on PATH`。
2. 约 9 分钟后 `command -v lua5.1` = `/usr/bin/lua5.1`(自检买的)。
   ⇒ **这一对读数正是 `tests/test_rc_wrapper.py:54-61` 那段注释所说的 CONFOUNDED 复跑**,只是这次两侧都留了戳。
3. **受控复现**(PATH 上 1287 个符号链接、独独不放 `lua5.1`):
   `python3 tests/test_rc_wrapper.py` ⇒ **`RC_EXIT=2`**,同两条 uncertifiable。

### (丙) 为什么这不是「拿诚实换绿」

GH #384 的 exit-2 措辞**一个字不改,而且被钉住了**(见 (丁) 的案 (B))。补的只是 GH #205
在这个仓库**别处早已强制**的那一步:**「容器里没有」不是事实,是一笔已经写好脚本的 4 秒采购。**

⭐ **而这笔钱在这条路径上是零新增**:闸腿 3 `tools/agent/lua_gate.py:ensure_lua()`
**一条腿之后**发的是**同一个 helper、同一份契约**的调用 ⇒ 本改动把 4 秒**往前挪一腿**,不是加上去。
工具已在时是一次 `command -v`(`ensure_lua_tool` 的第一行就 return);买不到时 helper 静默返回 1、
`HAS_LUA` 仍为 False、#384 的 UNCERTIFIABLE 原样站着。

### (丁) 改动与钉子

- `tests/test_rc_wrapper.py`:`HAS_LUA = shutil.which('lua5.1') is not None` → `HAS_LUA = _buy_lua()`。
- 钉子 `tests/test_rc_wrapper_buys_lua.py`(**6 检查,实测 0.54s / 0.51s**)。密闭:PATH 上无 `lua5.1` +
  一个**假 `apt-get`**,**零网络、零 dpkg、零容器改动**。**成对才是那个命题**:
  - **(A)** 缺 + 买得到 ⇒ 买到、两条检查**真的跑**、exit 0;
  - **(B)** 缺 + 买不到 ⇒ **仍 UNCERTIFIABLE、仍 exit 2**。
  - ⛔ **(B) 不是装饰**:一个「让检查变绿而没有解释器」的修法会通过 (A),且**比 bug 严格更坏** ——
    did-not-run wearing a pass,GH #171 / #198 / #200 同族。
  - 另有一条**防台子塌掉**的检查(`(A) is answered by the purchase, not by a leaked interpreter`):
    读假 `apt-get` 留下的调用标记 ⇒ (A) 不能被「真 PATH 漏进来一个解释器」满足。
- 变异台 `tools/agent/mutstand_rcwrap_lua.sh`:**CONTROL 绿、M1–M4 四红**;
  还原走**文件副本 + `sha256sum -c` 每次 OK**(零 `FATAL`),退出码**全部裸读未经管道**。

  | 变异 | 内容 | 谁杀的 |
  |---|---|---|
  | M1 | 改回 #899 之前那一行(只看 PATH,永不买) | (A) |
  | M2 | **窄**:只把 `if shutil.which(...)` 换成 `if True`(结构检查保持绿) | (A) + **(B),且 (B) 只在这一发亮** |
  | M3 | 买**错**工具(`luajit`);`ensure_lua_tool` 只认两个名字,静默 no-op | (A) |
  | M4 | 买了但**不重读**,返回采购前的答案(看容器看不出来,看决定才看得出) | (A) |

  ⚠️ **M2 的注释里记了一处自我订正(纪律 4)**:M2 第一版把整个函数删掉,于是**结构检查也红**,
  而我差点据此写下「只有 (B) 反对」—— **那个读数没这么说**。改窄之后读数才真的支持那句话。

### (戊) 本轮的闸(裸码,未经管道)

- `GATE_EXIT=0  CLEAN (iron rule 6 static half passed)` / `luacheck bots game: 0 warnings`
- `py gate: 135 ran, 0 findings, 0 uncertifiable, 50.1s`,`RC_EXIT=0`
- `lua gate:` 见 §六(随 push 一起打)

---

## 三、残留:manifest 落后于磁盘(已登记欠条,⛔ 不留在散文里)

`py_gate.py` 本轮逐字:`4 new test(s) not in the manifest were run anyway, costing 1.47s`
(1 个是本轮新增的,另外 3 个来自别的组)。⛔ **这不是红** —— 未登记的测试**照样跑**,闸的**判决**是对的;
漂的是**预算**那一格,方向是**让闸看起来比实际便宜**。`py_gate_measure.py` **无子集参数**(只有 `--dry-run`),
全量重测数分钟 ⇒ 本轮不做。

⇒ 按 §2.6 (乙) 登记 `iterations/owed_executions.json:pygate_manifest_behind_disk_20260918`,
`executor` = **总监自己,恰好一个**。**⛔ 只写进『下次触发』不算数** —— §2.6 的立案句逐字是
「**一条只活在别人报告散文里的欠裁,平均寿命是一轮**」,而一条只活在**我自己**清单里的残留并不更结实;
⚠️ **本轮 §七 那处订正正是这句话的现场版**:我抄清单时抄错了条目,**清单这个载体本身会出错**,
而 owed 腿是**每组每轮**真跑到的那条。

⭐⭐ **登记时当场撞到一条,值得记下来免得重学**:`done_when` 先写成 `json_value`,键
`["tests", "tests/test_rc_wrapper_buys_lua.py", "in_gate"]` —— 而那个键**此刻根本不存在**,
于是 `pending_rulings.py --owed-only` 逐字读回 **`UNCERTIFIABLE … carries no key`**。
⛔ **那是错的状态**:按本章程 RULING 67 的分界线,**「没有语料」= 这一轮没人能看(环境问题,下一轮自己会好),
「棒掉了」= 下一轮不会自己好**,两者**不许共用一个退出码** —— 而这里「键不存在」**正是未完成本身**。
改用 `path_contains_all`(读一个**存在且可读**的文件里有没有那一串)后读回 **`OWED`**。
📌 **同型第 N 例**:一个真命题(这个键读不出来)满足了一个别的问题(这件事做没做),差额**没人看见**。

---

## 四、promote / reject / 入集 / 成本

- **promote:0,reject:0,入集:0,出集:0。** 本轮没有任何测试集 id 的三条件到齐
  (批测台 09-18T12:12Z 报**刹车第 49 轮持有**,零发波 ⇒ 条件 (b) 结构上买不到新读数)。
- **成本**:本轮**零 EC2、零 CE、零 S3 对象**。MTD 以批测台报告为准(刹车持有中)。
  ⛔ 本座位**不发波、不花钱**。
- **owner 邮件**:不发(本周决定类邮件配额留给 §DECISIONS_NEEDED 里攒着的那几条)。

---

## 五、issue 语料刷新(RULING 76 的第二次实战,顺带补一条)

刷新集**不是手抄的**,是 `carry_item_issue_state.py --refresh-set` 打出来的:
逐字 `REFRESH-SET #856 #867 #240 #843 #859 #810 #528 #616 #290 #899 #548 #806`(12 个),
`REFRESH-MISSING #616 #899`。

⭐⭐ **本轮补一条,而它是 RULING 48 的同型**:语料里**另有 4 个号(#466 #538 #839 #865)不在刷新集里**。
只重读刷新集、却把 `fetched_at` 写成本轮,**等于替那 4 行声称了一次没发生的读取** ——
`fetched_at` 是**整份语料一个字段**,它管的问题是「这份语料多新」,不是「刷新集多新」。
⇒ **规矩**:**刷新集决定「必须读哪些」,`fetched_at` 决定「必须读多少」——语料里的每一行都要读,否则那个戳是假的。**
本轮 **17 行全部点查**(`issue_read(method=get)`,⛔ 不走 `list_issues`):**12 open / 5 closed**。
新增两行:`#616`(连续多轮 `not in corpus`,实测 **closed** 2026-09-08T04:54:16Z)、`#899`(本轮新开,open)。
4 个 closed 的分别是 `#616`(2026-09-08)、`#523`(2026-09-08)、`#538`(2026-09-05)、`#466`(2026-09-16)、`#865`(2026-09-17)(共 5 个)。

⚠️ **第一次写 `fetched_at` 时写成了未来**(比真时刻晚 12 分钟),那条腿当场逐字打
`corpus `fetched_at` is in the future (-0.2h)` 并把 12 个号**全部**读成 `no usable corpus` ——
**失效方向是安全的那一侧**(全体 UNCERTIFIABLE,不是全体 OK),已按真实时刻改正。

---

## 六、收尾读数(落笔前回读输出文件,取法照 W94)

### 6.1 开工自检(裸码经 `rc.sh`,**跑了约 75 分钟**)

```
legs run              : 15
FINDINGS (exit 3)     : cadence queue-rulings owed-executions lua-coverage
UNCERTIFIABLE (exit 2): trunk-red(python)
NOT RUN (inside a leg): tests/test_luacheck_gate_soakswitch.py tests/test_selfcheck_lua_leg.py
selfcheck worst exit: 3
```

- python 腿:`149 passed, 0 failed, 2 uncertifiable` ⇒ ⭐ **`trunk-red(python)` 本轮是 UNCERTIFIABLE 不是 FINDING**
  (协同组 10:16Z 那轮是 FINDING;⛔ 两者不是同一句话,不许互相引用)。
- Lua 检测器腿:**`138 tagged detector file(s), 0 failures -- FAST SUBSET, not the full suite.`**
- 四条 FINDINGS(`cadence` / `queue-rulings` / `owed-executions` / `lua-coverage`)都是**常驻**的那几条,
  其中 `owed-executions` 本轮**多了一行是我自己加的**(§三)。

### 6.2 ⭐⭐ 自检当场交出 RULING 78 的同族第二例(未修,已交棒)

```
UNCERTIFIABLE  tests/test_luacheck_gate_soakswitch.py
  UNCERTIFIABLE -- luacheck is not installed, so the behavioural half did not run.
  Buy it with: apt-get install -y lua-check
      1 checks, 0 failed (source half only)
```

它在 manifest 里是 **`{"seconds": 0.106, "in_gate": true, "reason": "fast"}`** —— 与 `test_rc_wrapper.py`
**同一个形状**:一个闸内测试,把「容器里没有这个工具」写下来而不去买它,尽管
`tools/agent/ensure_lua_toolchain.sh` **认得 `luacheck` 这个名字**(它就是为此写的)。

⛔ **但它不是同一个后果,这一点必须分开写(纪律 4:同样的结论不等于同样的理由)**:

| | 丢的是什么 | 为什么 |
|---|---|---|
| `test_rc_wrapper.py`(#899) | **一道闸** | 买 `lua5.1` 的是**腿 3**,排在腿 2 **之后** ⇒ 钩子里真的会被拒 |
| `test_luacheck_gate_soakswitch.py` | **一个读数** | 买 `luacheck` 的是**腿 1**,排在腿 2 **之前** ⇒ 钩子里它是绿的;丢的是**开工自检**那一侧(`:627` 早于任何采购) |

⇒ 前者是 GH #205 的形状(丢门),后者是 **GH #171 的形状(丢读数)** —— 本仓库为这两条各自立过一次法。
**本轮不顺手修**(它需要自己的钉子 + 变异台,而本工作单元已经够大),
已写进 **GH #899** 正文的「同族第二例」一节交棒,**⛔ 不只留在本报告散文里**。

### 6.3 三条闸腿(裸码,未经管道)

- 腿 1 `luacheck_gate.sh`:`luacheck bots game: 0 warnings` / `GATE_EXIT=0  CLEAN (iron rule 6 static half passed)`
- 腿 2 `py_gate.py`:`py gate: 135 ran, 0 findings, 0 uncertifiable, 50.1s`,`RC_EXIT=0`
- 腿 3 `lua_gate.py`:随 push 打,读数抄在 §6.5

### 6.4 变异台

`bash tools/agent/mutstand_rcwrap_lua.sh` ⇒ `RC_EXIT=0`;**CONTROL 绿 / M1–M4 四红**;
还原走**文件副本 + `sha256sum -c`**,四次全 OK(零 `FATAL: restore ... did not verify`)。

### 6.5 push 读数

(push 时回填)

### 6.6 token 用量

(收尾回填)

---

## 七、下次触发

⚠️⚠️ **本节的第一版是错的,订正连同理由一起留在这里(§2e 同族,第 N 例)。**
我用 `tail -5 iterations/streams/director.md` 取「上一轮的清单」,抄回了 `①GH #523(连续第四轮未取)` 这一串。
**那不是上一轮的清单,那是本文件最老的一条条目** —— 「当前状态」节是**新的在上**,`tail` 给的是**最旧的那一条**
(`status_entries()` 实测 **98 条**,带 `GH #523` 那张清单在 **index 97**,戳 **2026-09-06T01:19Z**,即 12 天前)。
⇒ 「连续第四/第五轮未取」这个数**是我凭一次错的取法造出来的**,本轮撤回。
📌 与 §2e 的 `ls -t` / 落后 clone / 浅克隆三例**逐字同族**:取法错了,而**错出来的东西长得和答案一模一样**;
差别只在这一例的失效方向是**凭空造出一根不存在的棒**,而不是凭空造出停摆。
⛔ **`carry_item_issue_state.py` 从头到尾是对的** —— 它读的 11 个号(`#856 #867 #240 #843 #859 #810 #528 #616 #806 #290 #548`)
与 09:58Z 那条真清单**逐项对上**;举手的是工具,读错的是我。

**真正的上一轮清单(09:58Z)+ 本轮新增:**

① **GH #856** 剩 9 候选
② **GH #867**
③ **GH #240** 余下
④ `carry_mark_prose_vs_list` 剩唯一一格
⑤ **GH #843** 剩 (乙)
⑥ **GH #859**
⑦ **GH #810** 待裁 1 + (乙)
⑧ **GH #528**
⑨ `walk_census_out_of_push_gate_so_rule803_cannot_bind` 开号 + 三选一带读数
   (⛔ 动 cap 之前先读 `tools/agent/py_gate_measure.py` 自己写下的那条约束 ——「成员资格只看实测秒数,永远不看文件名」;
   立案存档在**已关闭的 616 号**里,而**活着的号是 GH #839**「新落地的 python 测试默认不进 manifest」)
⑩ 看录像组答没答 `gh290_od_execution_verification_needs_postfix_corpus`(**GH #290**)
⑪ **新**:付掉本轮欠条 `pygate_manifest_behind_disk_20260918`(全量 `py_gate_measure.py`,顺带收编另外 3 个未登记测试)
⑫ **新**:**GH #899** 验收 —— 在一个**没有 `lua5.1`** 的容器上跑 `python3 tests/test_rc_wrapper.py`,裸码 **0**(而不是 2)
⑬ **GH #548** / **GH #806**(`lua_coverage_uncovered_grew_3files` 那条欠条的号)照旧挂着;**GH #523 划掉**(本轮点查:`state=closed`,`closed_at=2026-09-08T13:12:53Z`;
   其验收句要的 `tools/agent/mutstand_text_absent.sh` **在盘上**,`owed` 表里 `text_absent` 这种 kind 已有 **8** 行在用 ⇒ 残余为空,按 RULING 63/64 的二选一**划掉名字**)
