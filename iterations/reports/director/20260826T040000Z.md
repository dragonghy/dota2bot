# 总监 2026-08-26T04:0xZ —— `campdanger` 退回先量分母,而这一条同时把 §BB.4 自己的立法前提修了

**一句话**:本轮把自检唯一的红(`strategy-19` 的 §BB.4 当轮义务)裁完,裁定是
**RETURNED(不入集)+ 可证伪的自动入集条款**,理由是 **§BB.4 的「搭车提议唯一的成本是不裁」
在这一族上两小时前刚被 `campfarm` 的账单推翻**;顺带把 GH #198 交给总监的另两条
(全队工具加深 / 自检 banner 误导)与 GH #200(发波前 smoke 门是空跑)**一并裁完并落地**。
**零 AWS 调用、零支出;`bots/`/`game/` 零改动。**

---

## ① 开工自检(铁律 10)

```
UNLANDED         0(certifiable window;REFUSED 359/397,浅克隆常态)
CADENCE          5 洞(3.5–8.9h),点名 batch-desk 8.9h —— 与前四轮同型,不升级
RIDESHARE(§BB.4) 1: strategy-19  ⇐ 本轮唯一的红,且是当轮义务 ⇒ 选题被它定死
stable anchors   stable-v1 / stable-v2 各三条不变量 EXISTS/PINNED/SHIPPED 全 ok
python 套件      31 passed, 0 failed
Lua 检测器       起手 SKIP (no lua5.1);本轮付了那笔 apt(数秒)⇒ 8 file(s), 0 failures
```

选题**不是我挑的**:自检唯一的红恰好带着一条「当轮批或退」的硬时限。

---

## ② ⭐⭐ 主裁定:`strategy-19` / `campdanger` = **REJECTED + `readmit_on`**

投递三处齐(章程 2.5):`queue.json:strategy-19.director` ✅ / `test_set.md §BJ` ✅ / GH #198 追评 ✅。
棒子(铁律 9)= **GH #201**(录像组,离线数帧、零 EC2),**结案前不关闭**。
`state.json:campdanger_RULING_20260826T0400Z`。

**先说清楚它不是驳回申请方**:`strategy-19` 验收乙自己写着「分母 < 每局 1 帧 ⇒ 直接说不值得,
不要凑判据」,GH #198 §1 写着「我不主张现在就排波」。**本裁定与申请方一致。**

### ⭐ 立法级的那一条:§BB.4 的依据被测量推翻了(§BJ.2)

§BB.4 的**全部依据**是「搭车提议**唯一的成本是不裁**」。**那句话对钱是真的,对别的东西不是。**
两小时前 `campfarm` 把账单开出来了(replay-check 01:07Z):它入集时**分母同样没量过**,
armed **125/125 局**(第一嫌疑「arm 串漏了」已被排除),而它能管的那条通路全语料
**armed 2 次 / baseline 2 次** ⇒ **(a) 买不到,不是坏掉,是域几乎不存在**。

**账单 = 一条腿的读数 + 录像组一个工作单元(新建 35KB 工具)+ 总监一个裁定位。三样都稀缺,
而 §BB.4 的措辞里一样都没算。** `campdanger` 与它**同文件同函数**(`mode_farm_generic.lua`
的 `Think()`)、同一类「换不换营」的决定、**同样没量过分母** ⇒ 同形复发的先验很高。

⇒ **补丁(§BJ.2 立)**:§BB.4 的**当轮义务不变**;但对「域未量过的行为 id」,
**「退回 + 指名那个零成本测量 + 可证伪的自动入集条款」本身就是合格的当轮裁定**。
**判别标准是可操作的**:拖延**不指名**任何会终结它的读数,本裁定**指名了**,
且指名的读数落地后总监有**强制的实质裁定义务**。
⚠️ 边界照抄不要外推:不放宽时限、不适用于域已量过的提议;§BF.2 的 `ROUTED_ARCHIVE_SCAN` 仍有效。

### 时机把「退回」的机会成本量到了 0(§BJ.3)

W12 已于 `03:10:52Z` 发出,**`campdanger` 不在 arm 串里** ⇒ 本轮批准**最早也只能上 W13**,
而离线测量完全放得进 W12→W13 的空档 ⇒ **先量再排,零波次延迟代价。** 这是本裁定最硬的一条腿。

### 一条自核出来的源码事实,它一次买到三样(§BJ.4)

那个合取项**不是每帧求值**:外层被 `bot._farm_repick_at` 节流到 **1.0 秒一次**(`:741`),
且该节流**先于修复存在**(`git show 48ff29fe`:只改了合取项那一行 + 注释)。

- **收窄分母**(1 Hz 而非帧率);
- **更正一处机器可读记录**:GH #193 §3 写对了「每 1.0 秒一次」,而 `queue.json:strategy-19`
  的 `question` 散文写成「每一个…的帧上都发生」—— **散文转写成机器字段时掉了一个限定词,
  而掉了的那个恰好是分母**;
- **证明测量干净**:决策率 1 Hz 与语料采样率 1 Hz **同频** ⇒ 离线计数**不吃混叠代价**。

### ⭐ 枚举缺一档,所以补键而不是硬塞(§BJ.5)

`REJECTED` 单写会被读成终局(= 铁律 9 的掉棒:GH #193 一结案,它交出去的三根棒就从队列里消失);
`APPROVED_CONDITIONAL` 会让批测台**真的 arm 它**(§AW.1 那个病的第三发)。
⇒ `_protocol` 新增 **`director.readmit_on`**:`REJECTED` 的机器语义**始终**是「不许 arm」,
本键只回答「什么读数会让它**自动**回到排期轴、且申请方不需重新申请」。

⚠️ **诚实边界**(已写进 #201 §2):`preferedCamp` 是 bot 内部状态、**不在 .dem 帧表里**
⇒ 离线只买得到**上界**。**而上界单向够用**:< 1 帧/局 ⇒ 判决成立;≥ 1 帧/局 ⇒
**不确定,不是通过**。**不许把「上界 ≥ 1」写成「域够大」。**

---

## ③ 同轮裁完并落地的两条 harness —— 共同形状:**一条不抬退出码的话**

### GH #200(发波前 smoke 门是空跑)→ 采纳 (B),驳回 (A),(C) 已由批测台自办

先原样复现,再修:

```
修前:  lua5.1 tests/run_tests.lua tests/test_smoke_load.lua  →  0 tests, 0 failures  exit 0
修后:  ... NO TESTS RAN -- filter "…" matched 0 of 190 files.               exit 2
对照:  lua5.1 tests/run_tests.lua smoke_load  →  3 tests, 0 failures        exit 0(不受影响)
```

`matched==0`(调用点笔误)与 `total==0`(那个文件的缺陷)**分开命名**,因为修法不同。
**驳回 (A)**:每文件自执行守卫 = 190 份同一条规则,runner 执行一次就够。
**(C) 已由批测台自行落地**(`batch-desk.md:4104` 划线、`:4214` 记录)⇒ **总监不代改别组档案**;
全仓 grep 确认没有第三处。

**断言 `tests/test_run_tests_guard.py`(9 检查),刻意分两层,而分层就是重点**:行为层要 `lua5.1`,
而全新容器没有 ⇒ **整文件 skip 会把本 issue 的缺陷原样复制一遍**。所以源码层(4 条)**到处都跑**,
守卫被删就红;行为层(5 条)有 `lua5.1` 时叠加。**缺 `lua5.1` 只削弱,不静默击穿。**
变异两条均被抓:删守卫 → 红;**守卫照打印但 `os.exit(0)` → 红**(后者正是本 issue 的形状)。
**残留如实登记**:`lua5.1 tests/test_x.lua` 直接跑仍静默 exit 0,**未修**。

### GH #198 §2(要不要加深 `test_no_undefined_jmz_refs.lua`)→ **不加深,答案是第四种**

协同组问「加深 / 不加深 / 排给谁」。**那个加深已经做完了,只是不在那个文件里。**
`call_form_census.py` 已按各子表自己的模块解析 `J.<sub>.<name>`,
`test_call_form_census.py:128` 对**任何没有 ALLOWLIST 判决的新发现**直接红,
每轮在 `run_py_tests.sh` 与自检 python 腿里跑 ⇒ **深的那一半已经是门,不只是普查**。

再实现一遍 = **一条规则、两种语言、六套导出约定的第二份实现**,而漂移正是「哪份都不再被信任」的来路;
**而且协同组自己已经把这条钉死了** —— `test_call_form_census.py` §0 断言「它仍停在第一个点」,
**我真去加深,那条断言当场红**。

⇒ 实际缺的只有一样:**那个 Lua 测试的抬头吹了一个比它断言更大的牛**。抬头已改写(射程 =
**第一个分量且只有第一个**、盲区覆盖六张子表、这次的代价、**普查拥有第二个分量、两边不许互相长进去**)。
⚠️ 改的时候正对着 #198 §4 那颗雷(注释与代码在整文件子串匹配面前不可分):
**用散文而不是原样抄那行调用**,`test_call_form_census.py` 复跑 **exit 0**,棘轮没红。

### GH #198 §3(自检 banner)→ 协同组是对的,已修,**而且它有个双胞胎**

`routine_selfcheck.sh` 的 **python 腿 `:166` 有逐字相同的罐头字符串**。
**只修被报的那一句会留下同一个缺陷的另一半** ⇒ 两句一起改:

```
TRUNK RED -- %d of %d Lua detector file(s) failing ON THE WORKING TREE.
  Whether main is red too is NOT established by this line: re-run after `git stash`.
```

协同组的诊断连同具体代价(`test_level_gate_census.lua` 按行号钉的 row 被 `:599 → :637` 顶掉,
banner 却说 main 早就红了;`git stash` 复跑 main **15/15 绿**)已抄进代码注释。
**驳回「让自检自己跑 stash 对照」**:开工自检是**每个组动任何东西之前都跑的那一条命令**,
一个会改动工作树的自检若在 stash 与 pop 之间死掉,会**扣住某个会话未提交的工作**;
打印判据让读者自己选着跑是便宜的那一半,替他跑不值那个失效模式。

---

## ④ 验证(铁律 6)

- **`bots/` `game/` 零改动** —— `git diff --stat -- bots game` 为空 ⇒ **铁律 6 的 luacheck 门
  不适用于本轮改动面**(容器无 `luacheck`,如实登记;本轮为 Lua 检测器腿付了 `apt install lua5.1`)。
- 自检 **python 腿 31 → 32 passed, 0 failed**;**Lua 检测器腿 8 file(s), 0 failures**
  ⇒ 新守卫**没有**把任何现有检测器变红。
- **CI 路径不受影响**:无过滤器 ⇒ `matched = 190 > 0`,守卫惰性。
- `pending_rulings.py` 复跑:**RIDESHARE none / OTHER none** ⇒ §BB.4 义务**机械地**清掉了。
- `queue.json` **逐字段 diff 断言 = 变更恰好两处**(`strategy-19.director` + `_protocol` append),
  **UNEXPECTED FIELD CHANGES: NONE**;批测台真正读的 8 个字段
  (`question`/`acceptance`/`status`/`result`/`stream`/`bundle`/`seeds`/`priority`)**逐位未动**。
- `state.json` 顶层键 **286 → 288**,**APPEND-ONLY 已程序化断言**(每个原有键逐位相等)。

**诚实边界**:(a)(b) 两侧的读数**采信录像组与批测台的测量**,本轮**没有重跑那 125 份 `.dem`**;
总监自核的是节流的存在与其提交出处、`campdanger` 的 gate 接线、W12 的 arm 串、以及三处 harness 的实测。

---

## ⑤ 体系健康 / 成本

五组均有产出(batch-desk 03:15Z / strategy 01:38Z / replay-check 01:07Z / hero 01:52Z / director 本轮)。
CADENCE 5 洞与前四轮同型,**不升级**,仍点名 batch-desk 8.9h。
owner 优先项 P1/P2 球在协同组,**未触 12 轮红线**。
总监**零 AWS 调用、零支出**;有效 MTD ≈ **$45**(批测台本轮 W12 自报 ~$2.05–2.15 + $0.01 CE),
**$50/$60/$90/$100 四线均未触及**,owner 本轮不会收到 Budget 告警。
`DECISIONS_NEEDED` **无新增**(三条裁定都在自主授权内)。**W35 周日(08-30)邮件未发。**

## ⑥ 下次触发

1. **`hero-18` 读数回来后的实质裁定**(四格判据已预登记);
2. **GH #171**(自检 `SKIP (no lua5.1)` 不抬退出码)—— 与本轮 #200 是**同一族的另一半**,
   **第三轮**未裁,本轮已在 #200 追评里点名归属,**下轮必须给结论**;
3. `_protocol` 的 `answers` 反向指针 + `pending_rulings.py` 新不变式
   (「三条件已齐/已裁但无归宿」;**本轮新增一个用例:带 `readmit_on` 的 REJECTED,
   其 readmit 读数已落地却无后续实质裁定**);
4. `[bug] #156` / `[harness] #161/#152/#169`;
5. **W35 周日(08-30)邮件**(`DECISIONS_NEEDED` 第 9b 条是重点);
6. GH #190 读回后按 `cmboots_RESOLVE` 的可证伪条款决定是否重开 (c);
7. GH #201 读数回来 ⇒ **`campdanger` 的强制实质裁定**(§BJ.5 的义务,不许以「已经裁过了」续命)。

## ⑦ token 用量(铁律 8)

```
TOKENS total_in=5,862,191 out=48,105 turns=50
```
