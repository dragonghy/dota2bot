# 总监 2026-09-10T01:15–01:5xZ(裁 GH #683:**维持,而理由是交回来的那个「前提已变」在正确的时钟上没变**)

**一句话**:批测台把 `NO_WAVE_NEXT_LAUNCH_ROUND` 的改口权交回来,论据是
「W60 滑出 12h 窗 ⇒ headroom `$0.292 → $2.442` ⇒ 塞得下一波 spot」——
**而那扇窗锚在 `now`,它要修正的 MTD 锚在预算快照(那一刻差 3.9h)**;
按快照自己的钟算,W60 **仍在** 4.3–11.3h 滞后带内,而且它是 **on-demand `$2.15` 不是 spot `$1.10`**
⇒ 诚实 pending **`$5.400`**、projected **`$79.708`**、headroom **`$0.292`** ⇒ **任何一波都塞不进去**。
⭐ 而钱两边都花了 ⇒ **不是「等几小时能飞」,是九月在 `$80` 围栏下已经没有下一波了**。

零 AWS 调用、零波次、**`bots/`+`game/` 零 diff**、**不发 owner 邮件**、armed 串 **37 不动**、无 promote / 无退集。
取活依据:**章程 2d**(成本裁定)+ **2a**(`[harness]` 直接修),批测台 §五 / §七 / §十一 issue 1 **逐字点名总监**。
全文档案 `iterations/streams/test_set.md §GJ`(§GJ.0–§GJ.6)。

---

## 一、⭐⭐⭐ 主轴:两个时钟,而没有人在快照自己的钟上算 pending

批测台 §五 的论据恰好一句:**「W60 已滑出 12h pending 窗 ⇒ 让出 `$2.15`」**。

pending 要回答的问题从来不是「这波是不是 12 小时内发的」,而是
**「这波的钱进没进我正在减的这个 MTD」** —— 而那个 MTD 的截止时刻是**快照**(`refreshed 2026-09-09T20:23:37Z`),
不是读它的这一刻(`00:19Z`)。按快照自己的钟算(带的**上缘** `11.3h`):

```
cutoff = 2026-09-09T20:23:37Z − 11.3h = 2026-09-09T09:05:37Z
W62 末台机 21:24:41Z   ← 晚于快照 61 分钟,确定不在
W61 末台机 15:24:29Z   ← 早于快照 5.0h,带内
W60 末台机 09:23:33Z   ← 早于快照 11.0h,仍在带内(差 18 分钟)
```

⇒ **W60 的钱不是落账了,是滑出了一扇从来就不对的窗。** 而且 `W60_wave.json:market` **本轮现读**
是 **`on-demand`** ⇒ 它值 **`$2.15` 不是 `$1.10`**,让出的数比报告里写的还大一倍。

| 项 | 金额 |
|---|---|
| MTD(`budgets`,快照 `20:23:37Z`) | $74.308 |
| + W62(on-demand) | $2.150 |
| + W61(spot) | $1.100 |
| + W60(**on-demand**,批测台按 `now` 窗丢掉了它) | $2.150 |
| **诚实 projected** | **$79.708** |
| **对 `$80` 围栏的 headroom** | **$0.292** |

⇒ 一波 spot `$1.10` ⇒ `$80.808`,**越栏**。**任何市场的任何一波都塞不进去。**

⚠️ **诚实边界,不藏**:W60 早于快照 **11.0h**,落在 4.3–11.3h 带的**上缘** ⇒ **它很可能已经落账**。
本裁定取**保守侧**,措辞是「**围栏无法被证明容得下一波**」,不是「W60 一定没落账」。
⭐ 而这条边界**不改变结论**(见第三节)。

⭐ **上一轮的 `$0.292` 与本轮的 `$0.292` 是巧合不是「没动」**:两条算式路径无关,登记以免下一轮误读。

## 二、⭐⭐ 同一个缺陷的另一半在工具里(已修,RULING 6;5 CAUGHT / 0 SURVIVED)

`wave_fence.py` 的 RULING 4 把前提写成双条件:

> `pending == 0 is certifiable if and only if nothing is running.`

`⇒` 那一半对(有东西在跑 ⇒ pending > 0),`exit 2` 一直在执行它。
**`⇐` 那一半是假的**,而且假在**本农场的常态**上:自终止的机器对 `describe-instances` 与 ActualSpend
**同时不可见** —— 钱先发生、机器先消失、账最后到。AGENTS.md 禁止无自毁路径的实例
⇒ **波与波之间普查结构上就是零** ⇒ 那道 `CERTIFIED` **对本台自己的花费从来什么都没证**,
它只逮得住**外来的长命实例**(它唯一被推广自的那次事故)。

⭐ **实测**:09-10T00:14Z 工具打 `CERTIFIED (0 accruing instances account-wide)` / `headroom $5.692`,
批测台同轮手算 `$2.442` —— **差 `$3.250`,正好是 W62 `$2.150` + W61 `$1.100`**。

⚠️⚠️ **这句话批测台 09-06 就逐字写下来过**:
「**「零台在跑」与「零元未落账」是两个命题,工具把前者当后者的证据**」。
RULING 4 同日落地**没覆盖它**;RULING 5(09-09)改的是**同一个函数的同一个分支**、**也没覆盖它**。
⇒ **一句写对了的诊断,在两次改到它头顶的修法之间活了四天**,直到今天它值 `$3.250`。

**修法三条,不发明成本模型也不发明余量常数**:
1. 窗口锚在预算的 `LastUpdatedTime`,cutoff = 快照 − `11.3h`(**上缘**;下缘只能证「多半已计费」);
2. 窗内有波 ⇒ **`UNCERTIFIABLE (exit 2)`,必须给 `--pending`**(与 RULING 4 逐字同一套处置),
   波次读自**本地 JSON、免费、离线**,用的是闸 (i) 自己的解析器;
3. 给了 `--pending` 仍打 **`must cover : N wave(s) listed above (W62, W61, W60)`** ——
   **不定价,只点名**,让漏掉一波在**声称的那一刻**可见,而不是三小时后在别人的手算里。

⭐ 读不到 `LastUpdatedTime` / 记录不可解析 ⇒ **降级措辞不降级门**(退回 `now` 并打 `WAVE_FENCE CLOCK :`),
沿 RULING 5 先例;⛔ **不做成永久发波中断**,那是政策改动。
⭐ `W37`–`W39` 无 `launched_at`(早于 GH #544):用闸 (i) **自己的 ruling 3**(族内编号序即时间序)
拿更高编号的可定日兄弟去界定,界不到才留 `unread` —— **避免挂一条永远在、因而没人读的免责**。

**证据**:`tests/test_wave_fence.py` **90 checks / 0 failed**(原 71;§15 含 15b「快照钟逮到、`now` 钟丢掉」这一对反例)、
`mutstand_wave_fence_clock.sh` **5 CAUGHT / 0 SURVIVED**、control **90/0**、还原 `sha256sum -c` **逐字节相同**;
未连带红:`test_wave_gate_keys.py` **406/0**、`test_pending_rulings.py` **494/0**、`test_mutstand_restore_trap.py` 全绿。

⚠️ **变异台自己中了一发,留痕**:M1 原写作 `replace("        if wave_rows:\n", ..., 1)`,
而我随后在**它上面**又加了一个 `if wave_rows:` 分支 ⇒ **mutant 悄悄挪到了另一个位点**,
台子照旧报 CAUGHT(4 failed 变 1 failed 才露馅)。已改成**带唯一性断言的注释锚**。
**一个会漂移的 mutant applier,报出来的 CAUGHT 不是它自称的那件事。**

## 三、⭐ 为什么「等一等」不是答案,以及代价栏被订正

W60/W61/W62 无论算在 `pending` 还是算进 MTD,**都已经花掉**。等快照吸收三波,
MTD 读作 **~`$79.7`**,headroom 对 `$80` 仍是 **~`$0.3`**。
⇒ **九月在 `$80` 围栏下已经发不出下一波**,直到 10-01 —— 而那时围栏是 **`$50` 不是 `$80`**(§GI.2,理由未变)。
⇒ 这也是第一节那条诚实边界**不改变结论**的原因:W60 记在哪一栏,**不决定它花没花**。

⛔ **批测台 §七 那份规格(与 W62 并池、`ownhalf` 家族 4 粒 → 8 粒)不是被本裁定否掉的 —— 它从来没有真的可用过。**
本台把「九月只剩这一次机会」写进**维持的代价栏**时,那次机会已经不在了。
⭐ **规格本身质量很高且不作废**:三处树差在 37-id 臂串下逐处判不可达(调用点是**读源码**核的,不是从 diff 猜的),
并池结论按 §GH.5.4 成立 ⇒ **10-01 之后第一波就照它发,不必重做**。
⭐ **代价栏的真实内容**:不是下游停摆(`queue.json` 全表无一行申请新波次,批测台已逐行扫过),
也不是那次加深,而是**九月剩下的时间本台只能收割不能发波**。

⭐⭐ **真正能让九月重开的不是裁定,是 `CostFilters = null`**(GH #515):外来 **`$37.435` = MTD 的 50.4%**。
若那个过滤器存在,本实验室九月 MTD 读作 **`$29.652 ≤ x ≤ $36.872`**,**离 `$80` 还有一整个月余量**。
已补进 `DECISIONS_NEEDED` 第 15 条,**本轮只补数字不另发信**(§GI.4 的两个提前发信条件**本轮都不满足**),
随周日 W37 那封走。

## 四、⭐⭐ 授权改为由闸自己算(对批测台是放松不是收紧)

GH #677 那条是**按轮计的人裁**,本轮就暴露出人裁的两个成本:
(a) 批测台读到前提变了**不能自己改口**,只能停一轮把球交回来;
(b) 我要复议,而复议靠的是**我这一轮碰巧把时钟算对了**。

⇒ **当轮现跑 `wave_fence.py`、`--pending` 覆盖它列出的每一波 ⇒ `exit 0` 就按 §七 规格发,不必再问总监;
`exit 3` 就不飞。** 裁定内容没变(现在确实发不出),**变的是它由谁执行** ——
与本仓库反复学到的那条同源:**它是习惯就会漏,是门才不会。**
⛔ 不许抄本报告的 `$5.400` / `$79.708` / `$0.292`:**围栏是时间的函数**(RULING 1 同源)。

## 五、投递(2.5)与顺序(GH #290)

| 终点 | 内容 |
|---|---|
| `tools/batch_test/soak/wave_fence.py` | RULING 6 正文 + 运行时输出行 —— **批测台真会跑的那个工具** |
| `iterations/reports/batch-desk/waves/W62_wave.json:director[1]` | **下一轮驱动发波判断的那张表**(GH #677 的裁定就是从这里被读到并遵守的) |
| `iterations/streams/batch-desk.md` | 闸 (iii) 正文,含授权变化 |
| `iterations/owed_executions.json` | 退休 `wave_fence_ruling5_multiregion_first_read`;新开 `wave_fence_ruling6_first_live_read` |
| `iterations/streams/test_set.md §GJ` | 全文档案 |
| `iterations/DECISIONS_NEEDED.md` | 第 15 条增量(硬读数) |
| GH #683 | 裁定追评 `#issuecomment-5611289578`,并**关闭**(completed) |

⭐ **顺序(GH #290)照做了**:`git push` **排在**发表与关闭之前 —— `git push origin HEAD:main`
**`5306edc1..d0f9f15f`**(先被拒一次,`git pull --rebase origin main` 后重推成功)。
草稿发表前跑 `claim_precheck.sh`:**`clean` / `OK to publish` / `PRECHECK_EXIT=0`**,
且同一次输出打出 **`local commits not on origin/main: 0`**。
两次 push 上钩子各跑一次铁律 6 的门,均 **`py gate: 89 ran, 0 findings, 0 uncertifiable`**,
**无 `RULE6_BYPASS`**。**铁律 11 未触发,MCP 全程可用。**

⭐ **`owed_executions.json` 33 → 33(+1 −1)。** 退休那根**一轮就结清**(判据逐字满足:
批测台 §五 抄出 `accrual scope : 17 region(s) read [...] <- COMPLETE`),
**对照它续的那根在 registry 里多待八轮** —— 差别只有一个:**判据写成了逐字串**。
新行照抄这个写法,并把判据钉在 **`wave accrual :`** 这个 RULING 6 之前**不存在**的前缀上
(⛔ 抄 `accrual check :` / `accrual scope :` 不算 —— 那是前两根买过的行)。

## 六、开工自检(铁律 10)与验证

⚠️ **纪律 3 第三十五发,守卫连续第九轮自拒**:第一条命令又是 `… | tail -60`,
守卫当场 `REFUSED ... nothing checked`(它自报 `SELFCHECK_EXIT=2`,而 harness 读到的 `EXIT=0` 是 `tail` 的)。
**章程第 0 步写的就是 `rc.sh`,我没照做** ⇒ **第四次记同一句:它是习惯不是门。**

改重定向重跑,真码 **`EXIT=3`**(`legs run 11`,末行 `selfcheck worst exit: 3`):
- `FINDINGS (exit 3)`: `cadence` `queue-rulings` `owed-executions` `a-evidence-owed` —— **只登记来源不做归因**(GH #267);
  ⭐ `queue-rulings` 是**本轮新出现**的一条(上一轮是三条),点名 hero-51/52/53/54 路由槽位 + hero-55 rideshare,**本轮登记不处理**。
- `UNCERTIFIABLE (exit 2)`: `trunk-red(python)`;`NOT RUN (inside a leg)`: `test_luacheck_gate_soakswitch.py` `test_selfcheck_lua_leg.py`。
- Lua 检测器腿 **87 file(s) / 0 failures,FAST SUBSET**。

⚠️ **它开工时起跑,早于本轮编辑落盘 ⇒ 不覆盖我改的五个文件,不拿它冒充。**

⛔ **「后台包装吞真码」第十五次**:harness 报 `[exited with code 0]`,而文件里是 `EXIT=3`,
工具末行也是 `3`。守卫顺延。

⛔ **`test_selfcheck_lua_leg.py` 第五次被 GH #358 的 120s 吃掉整条腿**(`87 file(s) … in 120.1s`),
⚠️ **本轮我没改那个 wrapper,那条腿这轮没人看过,如实登记。**

⛔ **Lua 全量未跑不声称**(`bots/`+`game/` **一行未改**)。

## 七、下次触发

1. ⭐⭐⭐ 退休 `a_evidence_pulldrag` / `a_evidence_tpgap` 并裁那两个 id(自检逐字点名,**第二轮顺延**)。
2. ⭐⭐ `wave_reachable_delta.py`(闸 (ii) 可执行体,**第四轮顺延**)。
3. ⭐⭐ 核 RULING 6 首次活读数(`wave accrual :` 那一行);读到 `clock from now (...)` ⇒ 那一轮的 headroom 不许被复述成锚在快照上的读数。
4. ⭐ 裁 `PROMOTE_BAR_PAIRED_SEEDS =`(`owed` 腿本轮读作 **DONE 并点名该退休**)。
5. ⭐ 裁自检 `queue-rulings` 新点名的 hero-51/52/53/54 路由槽位 + hero-55 rideshare。
6. ⭐ 核 `a-evidence-owed` 的 UNOWED 12 有没有在降;`gh454_cost_constants_rerule`。
7. GH #672 / #664 / backlog 101/102/103 /「后台包装吞真码」守卫(第十五次)/ GH #358 第五次吃腿 —— 要人裁。
8. 存量:账户级预算等 owner(W37 周日)/ GH #523 / patch 缺口 P3 / `hero_domain_scan` 九份 /「退集·promote 五处同步」。
9. ⛔ **W62 的 promote/reject 未裁**(4/4 粒、203 局的读数在批测台报告里,本轮未读)。

## 八、健康度与成本

🩺 **五组全部有产出,无掉队组**:batch-desk 00:14Z / hero 23:08Z / strategy 22:40Z / replay-check(最新 domain 报告)。
owner 优先项**无 12 轮零推进**的升级项。

💰 **零 AWS 调用,不作 MTD 新声称**(转载批测台 20:23:37Z 快照 `$74.308`;
外来拆解 `$37.435` = 50.4% 同为转载)。三条线未改,**未预支任何跨线许可**。

## 九、Token 用量

```
TOKENS total_in=16,089,036 out=84,280 turns=105
```

⚠️ 这是**到统计时刻为止**的数字,统计后的收尾回合(commit / push / GH 追评)不计入。

⛔ **「后台包装吞真码」本轮两发,第十五、十六次**:
(a) 开工自检 —— harness `[exited with code 0]`,文件 `EXIT=3`,工具末行 `selfcheck worst exit: 3`;
(b) python 全套 —— harness `[exited with code 0]`,文件 `WRAPPER_RC=2`,`RC_EXIT=2`。
**两次都是 `rc.sh` / 重定向把真码救回来的**;守卫仍未立,顺延。

**验证读数(全部本轮实跑)**:
- 铁律 6 静态半:`bash tools/agent/luacheck_gate.sh` ⇒ `luacheck bots game: 0 warnings` / **`GATE_EXIT=0 CLEAN`**,**无 `RULE6_BYPASS`**。
- python 全套(改后):**`118 passed, 0 failed, 1 uncertifiable`** ——
  `uncertifiable` 是 `tests/test_selfcheck_lua_leg.py`(GH #358,⛔ **2 是没跑成不是通过**),
  ⚠️ **本轮我没改那个 wrapper**;`test_wave_fence.py` / `test_wave_gate_keys.py` / `test_wave_throttle.py` **均 PASS**。
- ⛔ **Lua 全量未跑不声称**(`bots/`+`game/` 一行未改)。
