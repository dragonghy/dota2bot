# director 2026-08-20T19:00Z — 第二十二次触发:清测试集审批积压(+2 入集)

## 一句话
优先级 2(a) **空**(49 条 open issue,自 17:00Z 起 0 条新 `[bug]`/`[harness]` 归总监);走优先级
2(c),把两个刚落地的入集申请复核并批准,排进下一波,并改掉其中一个的 (a) 检测器口径。
**未花 AWS 钱、未做任何 AWS 调用、本轮零 Lua 改动**(产出全是 `iterations/` + 一条 GitHub 评论)。

## ① `blinkflee` 批准入集(GH #71,协同组 17:15Z)。eligible 17 → 18
§O.3 五项清单逐项齐全(2 真实帧 fixture / 反向断言 / 5 次标注变异 / 可分离性 / (a) 口径),
默认批准,复核通过。(c) 成立:blink 是 15s CD 的保命+起手道具,满血无压迫时烧成一段位移是
标准道具误用。**复核里自己查出两处必须改的 (a) 判读口径**(已评论进 #71 + 写进 `test_set.md §U.1`):
- **U.1.1**:#71 的检测器桶(5s 无英雄伤害、HP>70%)**比 gate 严**(gate 是 2.0s、HP≥70%)。
  查 fixture `f_260820_043124_axe_blink_flee_529` 第 12 行:Axe 有 SK 三下英雄伤害
  `dt=3.2/4.2/5.2` ⇒ **动机帧 t=529.6 在 5s 口径下会被检测器排除**,armed 侧读数天然偏
  「没生效」。→ 检测器窗口逐字改 2.0s、HP 边界改 `>=0.70`。归 §0b 第十七例同族第二型
  (检测器谓词比规则严 → 把规则真作用域挡在观测外)。
- **U.1.2**:被 hold 的撤退帧继续往下走,`item_blink` 的 `IsProjectileIncoming(1200)` 分支
  **不要求任何模式**,落点(朝家门 1199)与撤退腿几何不可分,dumper 看不见弹道 ⇒ 一次正确
  hold 仍可能有一次朝家门闪现落进同一个桶。→ (a) **只用两臂配对差**判读;单局「2→0/1」绝对
  预测作废。
- U.1.3 (b) 用行为检测器(抑制型,§R.3 推论 ②);U.1.4 (a) 域按全部持 blink 英雄统计,不只 Axe。

## ② `liondrainstop` 批准入集(英雄组 18:12Z)。eligible 18 → 19
§O.3 清单齐全(2 真实帧 fixture、8 次变异 8 次全抓、gate-OFF 反向断言、可分离性、(a) 口径)。
(c) 成立:Mana Drain 频道把 Lion 钉在原地,而 shipped 释放路径唯一是 `J.IsRetreating`,扎根英雄
进不了退却模式 ⇒ 那条腿结构性打不出。复核自己查两件:
- **U.2.1 ✅**:先证否会让本 id 哑火的可能 —— `X.IsAbilityEChanneling`(hero_lion.lua:305-328)
  **同时扫小兵和英雄**的 mana_drain modifier,动机帧吸的 viper(英雄)被认出,不是死代码。
- **U.2.2 ⚠️ 事前登记(§H)**:起手门 `lion_IsDrainSafeToStart` 在自己 gate 关时返回 true,
  且 `liondrain` 连 eligible 都不是 ⇒ `liondrainstop` **必然单独 armed**,释放后没有东西拒绝重开。
  这是读数的前提:主判据 = 域内频道释放次数 armed>base;**必须一起报**释放→重开对与间隔;
  每局频道总数上升**不算证伪**;BUGGY 形状 = 重开<冷却 或 域外释放。
- U.2.3 (b) 行为检测器;`liondrain`/`liondrainstop` 永不同臂(共用 `nEDrainDangerRadius`+2s 窗口)。

## ③ 下一波(§U.0):18 id = §R.0 那 16 逐字不变 + 两个新 id
种子 888/895/896/906。申报目的 = 买 `blinkflee`+`liondrainstop` 的 (a)(两者调用点/英雄/检测器
互不相交,可同波同臂)。**`capmono` 留在串里**(它的两臂对照已由 18:08Z arm B 收官,下一波不再是
它的测量波)。新申请的 `retnear`/`towerreach`/`defclose`/`defstale`/`defnum`/`esaftershock` 一个不加
(eligible-only)。**不是两臂波**(不测 (b))。

## 批测台状态(只读转述,未介入)
`capmono` 隔离两臂:**arm A(16 id,capmono ON)已 16:09Z 上机、18:06Z 收割** —— 280 局,gpm −18.36
四指标 0/4(**单臂读数,§R 明确不给单 id 判 (b)**);**arm B(15 id,capmono OFF)18:08Z 上机**,
4 台 on-demand,树钉 `11a8de33`(main 已漂到 71b510c),ETA ~18:45-19:00Z。下次触发收 arm B
并出 `capmono` (b) 的配对差(按 §R.3 推论 ② 口径,按种子配对+按局归一化,不许 gpm/xpm)。

## 其他章程项
- **测试集变更**:第 2 行 eligible 串 +`blinkflee`,`liondrainstop`(17→19);下一波串换成 §U.0 的 18 id。
- **promote/reject**:无新判定(13:00Z 已 double-not-promote `zusult`/`cmrguard`)。
- **成本**:MTD **$11.6958**(批测台 18:06Z 自报,与 14:07Z/16:08Z 逐位一致;arm A on-demand 计费
  滞后未入账),在跑 4 台是有意的 arm B 自毁 on-demand(2h 看门狗,最迟 20:08Z 自毁),无泄漏。
  ≪ $45 月度围栏 ≪ $90 owner 刹车线。总监本轮 0 AWS 调用。
- **Routine 健康**:五组本轮均有产出无空转。
- 今日周四,效率台账(仅周日,08-23 那次做)跳过;patch 11:10Z 已分片定级不重复;
  `wandlimbo` 机会普查连续第九轮无人做(仍是 arm 前置硬阻断);`DECISIONS_NEEDED.md` 已有
  `[batch] #70` 8-25 围栏条目(建议选项 A),今日不新增(两个入集决定全在 §O.3 自主授权内),
  本周 owner 邮件仍为零。
- **待复核队列** `#44`/`#45`/`#55`/`#58`/`#62`/`#63`/`#65`/`#66`/`#67`:`#45` 第九次顺延
  (本轮工作单元用在有时限的下一波串上 —— arm B 一收就要定 §U.0,不能让两个新 id 悬着)。

## 验证
本轮**零 Lua 改动**(只改 `iterations/` + 一条 GitHub 评论)。仍按铁律 6 在本容器自装工具链
复核了当前树:**luacheck bots game = 0 警告(exit 0)**、**lua5.1 tests/run_tests.lua = 857/857 绿**
—— 确认两个新 id 落地的树是干净的(它们的 Lua 由协同组/英雄组已 push,总监只做入集判定)。

## 下次触发
① **收 18:08Z arm B**,出 `capmono` (b) 两臂配对差(§R.3 推论 ② 口径);② 排 §U.0 那波
(收 arm B 后)/ 视批测台拓扑;③ 清待复核队列(`#45` 最老);④ `#66`/`#63` 修订版落地后按 §R.2
复判 `cmrguard`(零支出);⑤ 都在等就做 backlog #1 帧语料检索工具 / patch 分片 P3 / `[harness] #27`。

## Token 用量
```
TOKENS total_in=10,410,515 out=60,682 turns=86
```
(为"到统计时刻为止"的数字,统计后的收尾回合不计入。)
