# 总监 2026-09-08T16:12Z —— 八条一次裁完;而本轮最该被读的两条都不是裁定

**一句话**:①「下次触发 ①」那七条 RIDESHARE 到本轮已是**八条**,全部裁 **APPROVED-SCAN**
(不是 FROZEN-HOLD —— 申请方逐字写着不请求入集),裁定里加了**三处已知的墙**的预贴标签;
②顺手清点发现**针脚表在它被立起来的下一轮就漏了三根**(hero-38/39/40 批了从没进表),
立**机器不变量 + 变异台**;③开工自检的 **trunk 红([bug] GH #637)当轮修掉**。

零 AWS、零波次、**`bots/`+`game/` 零 diff**、不发 owner 邮件、`DECISIONS_NEEDED` +0。
⛔ **判定完结 0 —— 本轮不达 owner P4.2 的 ≥2**,原因与补法见 §6,不粉饰。

裁定全文档案:`iterations/streams/test_set.md` **§GB**(§GB.1–§GB.5)。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1; echo "EXIT=$?"` —— **真码 `EXIT=3`**,
`legs run 10`,`FINDINGS (exit 3): cadence queue-rulings owed-executions trunk-red(python)`、
`UNCERTIFIABLE (exit 2): none`、`NOT RUN (inside a leg): tests/test_luacheck_gate_soakswitch.py
tests/test_selfcheck_lua_leg.py`。

⛔ **「后台包装吞掉真码」本轮第五次兑现**:自检超时被移到后台后,harness 汇报的是
`exit code 0`,而脚本自己的真码是 **3**。躲过去的仍然只有「把 `$?` echo 进文件」这一个习惯
(上一轮 ⑤「给这个立守卫」**本轮仍未立**,顺延)。
⚠️ 纪律 3 本轮**零发**:第一条命令就走了 `rc.sh`,同回合并发的第二条也走了
(`… > /tmp/sc.log; echo EXIT=$?`),这是措辞从「第一条命令」改成「任何要读退出码的命令」之后
的第一轮零发。**登记,不立新措辞。**

四条 finding 逐条:

| leg | 读数 | 本轮 |
|---|---|---|
| `queue-rulings` | 8 条 RIDESHARE 未裁(hero-41..48) | **清零**(§1),复跑 `RC_EXIT=0`、`RIDESHARE: none` |
| `trunk-red(python)` | `test_chain_member_census.py` FAIL | **修掉**(§3),GH #637 |
| `owed-executions` | 18 行 / 2 RESIDUAL | 设计中;本轮把其中一行从**假绿**改成**点名 OWED**(§2) |
| `cadence` | 3 个洞,**全部是 strategy**(09-07T13:40→17:15Z 3.6h、19:00→23:30Z 4.5h、09-08T10:51→14:31Z 3.7h,阈值 3.5h) | **不升级**:该组本轮有实产出(`f7c77b9f` tpquiet 等三个提交碰 `bots/`),洞是节奏抖动不是停摆;登记 |

---

## 1. 八条裁定:**APPROVED-SCAN**,不是 FROZEN-HOLD

上一轮排的「下次触发 ①」写着「P4.2 冻结期的合法裁定是 FROZEN-HOLD」。**照请求原文读,那句是错的**:
八条在 `bundle` 里逐字写着「**NOT armed —— P4.2 冻结期内本条不请求入集**」。
P4.2 冻的是 armed 集变大;这八条一条也不进集,要的是**零 EC2 的只读档案遍历**。
先例从 hero-31 起就统一是 APPROVED-SCAN,hero-40(09-07T10:xxZ)的判词逐字说明了理由。
**按先例裁,并在 §GB.1 把 hero-40 的判词抬成这一档的引用点。**

| id | 杠杆 | 方向 | 本轮加的限定 |
|---|---|---|---|
| hero-41 | `wksaveidle` | WIDENING | (2)(3) **同轮交付**;地平线(WK lv6–9)先说清 |
| hero-42 | `cmfarcreep` | WIDENING | ⛔ **排在 GH #581 之后,本次遍历跳过**(见下) |
| hero-43 | `lionultcash` | WIDENING | 两个析取分开报;(2)(3) 同轮 |
| hero-44 | `lionrreach` | NARROWING | gap 分桶,不许均值/中位数;(4) 泄漏列分开报 |
| hero-45 | `wkqlane` | NARROWING | ⛔ **(4) 预贴 BLIND-BY-KNOWN-WALL**(见下) |
| hero-46 | `axecullreach` | NARROWING | 供给已解封(190 局有 `.dem`);gap 分桶 |
| hero-47 | `cmlaneband` | NARROWING | ⛔ 移速字段**执行前自检**,拿不到不许用常数顶替 |
| hero-48 | `zusjumpland` | NARROWING | ⛔ 朝向字段**执行前自检**,不许用「朝向=指向目标」顶替 |

执行方 **replay-check**(§CH,恰好一个流),产物**不改名**,acceptance **一字未改**,
METHOD-FAILED 按 §CJ 回总监重裁。投递三处齐(§2.5):`director` 字段 + `test_set.md §GB` + 本报告;
issue 线追评见 §7。

### ⭐ 本轮裁定真正的内容:**三堵已知的墙,每一堵递出来的都不是空白,是一个指向「不入集」那一边的错数**

1. **hero-42 结构上买不到,今天就知道**:它的 (2) 要两条列表各自的**最强兵血量**,(1) 要按
   lane creep / neutral / 玩家随从**分层** —— 都要 `creeps[]` 的 **hp 与 name**,而 `creepSnap`
   只有 `{t,team,x,y}`(产物 §7/§9.1:**18,709,698 行单一键形**;[harness] **GH #581** open)。
   **同一堵墙已经卡死 hero-32/33/36。** ⇒ 跳过它、留一行点名 #581 的状态行,
   **不要交一份把 (2) 写成 0 的报告**。#581 落地后同一个量具一次解锁四条腿。
2. **hero-45 的 (4) 列:墙不给空白,给一个错的 0** —— (4) 要 Bone Guard 充能 ≥60%,
   而充能层数是产物 §6.5 明写买不到、§4 实测 **dump 递过来一个正好错的 0** 的那一列
   ⇒ `nStack/maxStack >= 0.6` 恒假 ⇒ (4) 会读成「**从来没压掉过一个满充能的嘲讽**」。
3. **hero-47 / hero-48 的两个默认值方向相同** —— 「用出厂移速顶替」让前置恒真或恒假,
   「朝向=指向目标」让 gap 恒为最优、读出来是 `DOMAIN-NOT-REACHED`。**都恰好掐死本杠杆。**

§GA.1 刚立过「买不到与没人去买在 verdict 表里长得一模一样」;本轮补的是更窄也更贵的一格:
**撞墙的列常常不是交白卷,而是交一个看起来正常的数字,而且三次的方向是同一边。**
一份「(2)=0 / (4)=0 / gap 恒 0」的报告读起来像三条独立阴性证据,实际是同一台量具在三处失明。

---

## 2. ⭐⭐ 顺手撞到、比裁定更贵:**针脚表在被立起来的下一轮就漏了三根**

`owed_executions.json:hero_domain_scan_2_30_31` 的 `done_when` 09-06(hero-36)从 `path_exists`
换成 `path_contains_all`,**立案句就是「产物读 DONE 而它欠的读数大多不在里面」**。清点结果:

- **hero-38 / hero-39(09-06T22:xxZ 裁)、hero-40(09-07T10:xxZ 裁)三条都批准搭这次遍历,三条都没进针脚表**;
  产物里 `hero-38`/`hero-39`/`hero-40` 各出现 **0** 次 ⇒ 这条腿在**三份从未开工的读数之上照样读 DONE**。
- **换针脚之后的第一轮和第二轮,各漏一次。**

⛔ 缺陷不是裁定也不是工具,是**「裁定搭车」与「加一根针」是两个分开的手工动作,而第二个没人拿着**。
本轮立两条腿(都写在失效方向上):

- 机器键 **`director.owed_row`**:八条新裁定自带,**hero-2/30..40 十二行回填**。
- **`tests/test_domain_scan_pass_pins.py`**(5 checks,全绿):
  `{queue 里 director.owed_row == 本行}` **≡** `{本行 contains 里形如 hero-N 的针脚}`。
  **等式不是包含**:左→右抓这次漂移(裁了不钉),右→左抓镜像(钉了没人欠 ⇒ 永远红)。
  另钉「两侧不许同时为空」、`done_when.kind/path` 仍指着那份产物、rider 的 `deliverable`
  必须以该路径**开头**(startswith 不是等号:hero-31 那行把路径和理由写在同一字段里)。
- 变异台 **`tools/agent/mutstand_domain_scan_pins.sh`**:
  **5 CAUGHT / 0 SURVIVED / control_ok=1**,还原 `sha256sum -c` 逐字节 **YES**。
  五发**全部**是「这次遍历欠得比裁定少」的方向,**M1 就是 hero-40 今天之前的真实状态**。
  `tests/test_mutstand_restore_trap.py` 对新台**五项全 ok**(EXIT trap / 最后一个 trap 会还原 /
  还原有证明 / 找得到 applier / trap 在第一发之前上膛)。

针数 **9 → 20**。改完当场点名:
`11 of 20 required mention(s) are absent: hero-38, hero-39, hero-40, hero-41 … hero-48`。
⚠️ **针数变大不是记账倒退** —— 那三份读数本来就欠着,只是这条腿看不见它们。

---

## 3. [bug] GH #637:**一条每轮点名的 trunk 红,归因是「插入了几行」**

自检报 `FAIL every judged row's recorded line matches the finding it names TODAY --
[(…'1585a9b8'), 8445, 8458)]`(`tests/test_chain_member_census.py`)。
批测台 15:10Z 已立案 **GH #637** 并写明建议(**把这行从绝对行号上摘下来**,而不是把数字改成 8458
续成第五项),同时正确地拒绝改别组台账。**章程 2a,总监的活,当轮做掉。**

裁定按测量:那个字面量移动了**五次**(`8246→8256→8341→8423→8435→8445→8458`),
**没有一次是因为 finding 变了** —— 全是**同一文件里它上方的插入**。键
`(文件, 定位串, 8 位 hex)` 五次全吸收(GH #442 的设计),**只有这条 freshness 检查在收费**,
收费对象是碰巧编辑那个文件的那一轮。GH #574 早就问过;#637 把账收齐了。
⛔ **失效方向比浪费更贵**:五次里**两次没人付**,每次都把 `origin/main` 留成红的,
而一条写着「有人在无关 finding 上方插了几行」的红 trunk **教会下一个读者整份 ratchet 是噪声** ——
那份文件另外十二条检查正是「真丢了一个 chain member 就当天变红」的那些。

**修法**:drift 打 `LINE NOTE` 并**保持绿**(**与工具本身的语义对齐**,是这条检查在跟它唱反调);
顶替它的是那个数字真正代表的性质 —— **每条 judged 行仍有一个可用的导航行号**
(缺了/垃圾值 ⇒ finding 找不到,**且无法被另一个文件的插入触发**)。
⚠️ `8445` 这个字面量**不改**(改它就是把第五次通行费也付了)。

**验收(反向,不是空断言)**:
- `python3 tests/test_chain_member_census.py` ⇒ **all checks passed**,并逐字打出
  `LINE NOTE judged row 1585a9b8 records :8445, found at :8458 today`。
- 把该行导航值改成 `0` ⇒ **红(exit 1,2 个 FAIL)**;**纯注释编辑 ⇒ 绿(exit 0)**;
  两次之后 `git diff --quiet -- tools/agent/chain_member_census.py` **逐字节 YES**。

---

## 4. 成本

**零 AWS**:本轮没有任何 EC2/S3 写动作,不对 MTD 作新声称。
转载批测台 2026-09-08T15:10Z 的读数:MTD **$66.105**,`$80`/`$90`/`$100` 三条线均未跨。
八条裁定全部零 EC2 ⇒ 它们对 MTD 的增量**结构上是 0**,不是「预计很小」。

## 5. 各组健康度

四组本轮均有产出(batch-desk 15:10Z 发 W57 + 立 #637;strategy 三个碰 `bots/` 的提交;
hero 连续提请求到 hero-48;replay-check 有报告)。cadence 三个洞全在 strategy 且都是抖动量级,
**不升级**。owner 优先项:P4.2 的「退集 stayattr」在更早的总监轮已执行(63→61 的载体项复核在 §ET.4);
armed 串当前 **44**(目标 ≤20),最近数轮持续在缩。

## 6. ⛔ 本轮**不达标**的一项,以及为什么不粉饰

owner P4.2 把总监的产出指标定成 **判定完结数 ≥2/轮**(promote / reject / 退回出集)。
**本轮 0。** 取活依据是上一轮自己排的 ①(自检**每轮**点名的 `queue-rulings`),
中途又撞上章程 2a 的 [bug](trunk 红,优先级高于自选 backlog),两件都做完之后本轮已满。
**把「批了八条扫描」记成优先项推进是方向搞反的** —— P4.2 逐字写着推进的度量是**集合变小**,
而本轮 armed 串 **44 → 44**。
⇒ **下次触发 ① 强制是「≥2 判定完结」,排在任何新裁定之前**,从核验记录最少、最难买 (a) 的 id 清起。

## 7. 投递(§2.5)

- `director` 字段 ×8(hero-41..48)+ `owed_row` 回填 ×12 —— 已落 `iterations/queue.json`。
- `owed_executions.json:hero_domain_scan_2_30_31` —— 针数 9→20,`ruling`/`done_when_note`/`residual` 三处登记。
- `test_set.md §GB`(§GB.1–§GB.5)全文档案。
- **issue 线追评**:GH #637(修法与验收)、hero 组活线程(八条裁定摘要)。
  ⚠️ 按 GH #290 的顺序律,**先 push 再发评论**;发之前跑 `claim_precheck.sh`。发不出去(MCP 需批准)
  则按铁律 11 立刻放弃并把该写的内容留在本节。

## 8. 下次触发

1. ⭐**≥2 判定完结**(P4.2 的产出指标,本轮 0;强制排第一)
2. `hero_domain_scan` 的**九份读数逐份读通**(上一轮 ②,顺延;`path_contains_all` 永远买不到那一层)
3. `test_gated_helper_nesting_census.lua` 当 GH #622 的仪器读一遍(顺延)
4. GH #358 的 120s 预算要人裁(顺延)
5. **给「后台包装吞掉真码」立守卫**(顺延;本轮第五次兑现)
6. GH #633 没买到的那一半:`I/O error` 形状的行与真警告分开,前者走 `verdict 2`
7. `text_absent` 的第一个真实用户(顺延)

## 9. 铁律 6

- 静态半:`bash tools/agent/luacheck_gate.sh` ⇒ `luacheck bots game: 0 warnings`、**`GATE_EXIT=0 CLEAN`**。
- python 半:`python3 tools/agent/py_gate.py` ⇒ **`86 ran, 0 findings, 0 uncertifiable, 12.0s`**
  (含 2 个未进 manifest 的新测试照跑:`tests/test_domain_scan_pass_pins.py`、`tests/test_luacheck_gate_soakswitch.py`)。
- 慢腿单跑:`python3 tests/test_chain_member_census.py` ⇒ **all checks passed**(§3)。
- **未用 `RULE6_BYPASS`。**
⛔ **动态半(Lua ~100min)未跑,不作声称** —— 本轮 `bots/`+`game/` **零 diff**。

## 10. 投递已完成(补录,push 之后)

- `git push` 两条:分支 + `origin HEAD:main`(第一次被拒 ⇒ `git pull --rebase origin main` ⇒ 重推成功;
  分支因 rebase 用 `--force-with-lease`)。两个 ref 都在 **`0733fa00`**。
  ⛔ 按 GH #290 的顺序律,**评论在 push 之后发**,两份草稿都先过 `claim_precheck.sh`:
  **两份都 `clean` / `OK to publish` / `RC_EXIT=0` / `local commits not on origin/main: 0`**。
- **GH #637**:追评(修法 + 反向验收 + 限度)并**关闭**(state_reason=completed)。
- **hero 组活线程 GH #634**:追评八条裁定摘要 + 四处预贴标签。

## 11. TOKENS

```
TOKENS total_in=15,624,985 out=68,926 turns=100
```
(到统计时刻为止;统计之后的收尾回合不计入。)
