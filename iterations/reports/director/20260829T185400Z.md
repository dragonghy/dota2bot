# director 2026-08-29T18:54Z —— 欠了三轮的成本核查 + `fieldsip` 入集裁定,而**两件事查出来的是同一个形状**

**一句话**:成本核查清白(MTD **$66.19**,4 台 spot 全部 `shutdown=terminate`、`one-time`,零泄漏);
`fieldsip` **入集**(成员串 44 → 45)—— 而本轮最该被读走的不是裁词,是
**它为什么迟了三轮:它从来没在机器读的那张表上出现过**,于是开工自检连续三轮报 `none`
而一条搭车提议躺了 8 小时无人裁。**一次"干净"读数,不是一次干净现场。**

**零 AWS 支出**(只读调用,**未跑 CE**,`budgets` 免费);**`bots/` / `game/` 逐字节零 diff**
(本轮改动全在 `iterations/`)。

---

## 〇、开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` —— **worst exit 3**;
`FINDINGS (exit 3) = cadence`(**只有 cadence**);`UNCERTIFIABLE (exit 2) = none`。

- **锚点**(铁律 3):`stable-v1` / `stable-v2` 两个 EXISTS/PINNED/SHIPPED 全 ok。
- **trunk**:python **53 passed 0 failed 0 uncertifiable**;快 Lua **29 文件 0 失败**。
- **cadence 2 findings**:director `04:08Z → 16:28Z` **12.3h**、replay-check `00:55Z → 05:30Z` 4.6h。
  director 那条是**已知的**(16:28Z 那轮自己记过),不重复立案。
- **queue**:`un-ruled` **none**、`expired waits` none。⚠️ **这一行本轮被证伪了一半 —— 见 §二。**

---

## 一、⭐ 成本核查(欠三轮,本轮第一优先,**清白**)

`bash tools/batch_test/aws/session_setup.sh` → `arn:aws:iam::581436534924:user/dota2bot-agent`(受限用户,正确)。
以下全部是**只读**调用,**没有花 CE 的 $0.01**(批测台 18:18Z 那轮已经付过一次并逐位一致)。

| 项 | 读数 | 来源 |
|---|---|---|
| MTD | **$66.19** | `budgets describe-budgets`(免费) |
| 预算限额 | $100/月 | 同上 |
| **月末预测** | **$80.585** | 同上(**本轮新读数,批测台报告里没有这一行**) |
| 快照刷新时刻 | **2026-08-29T12:39:06Z** | 同上 |

- **与批测台 18:18Z 的自报逐位一致**($66.19,快照时刻同为 `12:39:06Z`)⇒ **独立核对通过**。
- **⭐ 月末预测 $80.585 是本轮唯一的新信息**:它**低于刹车线 $90**、**低于 owner 批准线 $100**,
  但**高于 $80 这一档**(owner 可见告警档是 50/80/100% = $50/$80/$100)。
  ⇒ **按当前烧钱速率,月末会跨 $80 那一档告警,但不会触及 $90 刹车。**
  这不需要 owner 拍板($100 档早已批到),**但值得写进明天的 W35 周日邮件当背景**,
  免得 owner 收到 $80 告警邮件时以为出了事。
- ⚠️ **裸 MTD 不可直接用**:快照时刻 `12:39:06Z` 早于 W25 自毁(13:00–13:11Z)与 W26 起飞(18:17Z)
  ⇒ 它**不含**这两波;批测台的围栏算术($66.19 + $0.80 + $0.80 = **$67.79**,最坏 $69.14)本轮**照准**。

### 1.1 泄漏检查:**零泄漏**,而且**自毁路径是我自己核的,不是信报告的**

| 检查 | 读数 |
|---|---|
| running / pending 实例 | **4 台**,全部 `dota2bot-soak-spot-1`,`c6i.4xlarge`,起飞 `18:17:04–18:17:36Z` |
| lifecycle | **4/4 `spot`** —— 零 on-demand 降级,**符合 owner GH #158 拍板的「Spot 优先」** |
| `instanceInitiatedShutdownBehavior` | **4/4 `terminate`** ⇐ **自毁路径实测,不是转述** |
| spot 请求 type | **4/4 `one-time`**(**不是 `persistent`**)⇒ 自毁后**不会重开** |
| `InstanceInterruptionBehavior` | 4/4 `terminate` |
| 可用(未挂载)EBS 卷 | **0** |
| 弹性 IP | **0** |
| open/active spot 请求 vs 实例 | **4 : 4,一一对应**,无多占容量 |

⇒ **没有任何一台长命实例、没有一个孤儿卷、没有一个会自动重开的请求。**
`one-time` 那一格是本轮**特意去查的**:一个 `persistent` 请求会在自毁后**静默重开**一台,
而"实例列表看着正常"完全掩盖它 —— 与本轮 §二 是同一种**静默分母缺陷**。

---

## 二、⭐⭐ `fieldsip` 入集(44 → 45),裁定全文 `test_set.md` **§CG**

裁词 **`ROUTED_RIDESHARE / ADMITTED`**。投递按章程 2.5 的三处**本轮全部落地**:
(i) `queue.json` 的 `director` 字段(**请求行本轮由总监代建**,见下);
(ii) `test_set.md` **§CG** 全文档案 **+ 第 2 行同步改毕**(§BM 的规矩:未改第 2 行 = 裁定未完成);
(iii) GH **#300** 追评 —— **push 之后才发**,见 §四。

### 2.1 立案迟到的归因:**不是「没人有空」,是「没人被点名」**

§CF 刚写下「**§BB.4 的执行记录到今天为止是 0/8**,每一次失守的直接原因都不是有人反对,是**没人有空**」。
`fieldsip` 把记录推到 **0/9**,**但它的失守机制是另一种**:

| | `odbuild` / `wkqdmg`(§CF) | `fieldsip`(本轮) |
|---|---|---|
| `queue.json` 里有请求行 | **有**(hero-22 / hero-23) | **没有**(grep 计数 **0**) |
| 开工自检 `pending_rulings.py` | **连续两轮点名** `OTHER (routing/slot ruling still owed)` | **报 `none`** |
| 迟到 | 3 轮(**有人喊,没人有空**) | 3 轮(**没人喊**) |

本轮开工自检那一段逐字是 `RIDESHARE (§BB.4: rule this round): none` /
`OTHER (routing/slot ruling still owed): none` —— **而此刻一条搭车入集提议已经躺了 8 小时未裁**。

⇒ `pending_rulings.py` 扫的是 `queue.json` 的 `director` 字段;**一份只存在于 `test_set.md` 的提议
在它眼里不存在**。它的失效方向是**漏报**(与 GH #276 那条"多问"的检测器**反号**,
所以不能拿"它一向偏保守"安慰自己)。

**这是 §AW.1 / §BM 那条投递纪律的镜像**:那三次丢的是**裁定**,这一次丢的是**请求**;
章程 2.5 规定了裁定要落到 `director` 字段,**从来没有任何一条规定提议必须先有请求行**。
本轮唯一的发现路径是**有人恰好读到 `test_set.md` 的那一节** —— **运气不是机制**。

### 2.2 入集资格:总监在源码**独立复核**的三条(不是照抄提议自述)

1. **未 armed 逐字节惰性,且是结构性的**:`jmz_func.lua:5197-5204`,首行
   `if not J.IsSoakCandidate('fieldsip') then return true end` ⇒ 留守侧 `A and true ≡ A`、
   购买侧 `not (A and true) ≡ not A`,**两侧同时**退化成出厂表达式。
   `GetMaxHealth()` 为 `nil`/`<=0` **同样 `return true`** ⇒ 引擎读数失败时退回出厂,**不改判「释放」**。
2. **`pullcad` 陷阱已避开**:谓词体内 `IsSoakCandidate` **恰好 1 次**,
   `stayfield`/`stayfield2`/`fieldbuy`/`fieldcreep`/`bagsalve` **一个都不出现**(grep 实测 0)。
3. **只有两个消费方,没有第三个**:全 `bots/` 树 `IsFieldSipEnough` 命中 **4 处全在 `jmz_func.lua`**
   (定义 :5197 / 注释 :5414 / 调用 :5348 留守 / 调用 :5419 购买)⇒ **分区是定理不是巧合**。
   **这一条必须自己数** —— 一个第三消费方会让"分区"从定理降级。

**wiring 实测**(45-id 串):`check_armed_wiring.py --cand <45 串>` = **45/45 WIRED,exit 0**;
`fieldsip` **direct、1 站点、`bots/FunLib/jmz_func.lua:5198`**;裸串 **396 字节**(44 id 时 388),
S3 key 上限 1024 仍有余量。

### 2.3 §CE.6 把一问交给总监,本轮的裁法

提议自陈:本杠杆在**留守侧是收窄**(21/23 帧不再被留住),价值**全压在**同一批帧转由 `fieldbuy` 接手那一侧。
- **事实**:`fieldbuy` **在集里**(第 2 行第 24 位),且测试集是**单腿** ⇒ **同腿 arm 是自动的**,
  §CE.6 提的那条备选裁法**此刻已被满足**,不必另加机制。
- **⚠️ 但依赖只能是书面的,不能是代码**:写成
  `IsSoakCandidate('fieldsip') and IsSoakCandidate('fieldbuy')` 会在 `fieldbuy` promote 当天
  **冻结为 FALSE**,而 `check_armed_wiring.py` 仍报 WIRED、verdict 读回来是「测过了没效果」
  (`pullcad` 原案,CLAUDE.md 已立)。⇒ 立**书面条件 D**(§CG.3):
  `fieldbuy` 一旦出集/promote/拆分,**同一工作单元内**必须一并处置 `fieldsip`。
  **漏做不会自己举手** —— 与 §2.1 同病。

### 2.4 ⚠️ 一条自我更正:我第一次"跑测试"跑了个寂寞

我先跑 `lua5.1 tests/test_fieldsip_magnitude.lua` 得 `EXIT=0`,**差点把它写成"验证通过"**。
这些文件结尾是 `return tests` —— **直接跑只是定义并返回一张表,一条断言都不执行**,
任何文件都会 `EXIT=0`。**正确跑法**是 `lua5.1 tests/run_tests.lua <basename>`:
- `test_fieldsip_magnitude.lua` → **15 tests, 0 failures**(与 §CE.5 自述的 15 例对上);
- 它带 `[ratchet]`,已被开工自检快腿的 **29 文件 0 失败**覆盖。

**记这条,是因为"exit 0"和"测试通过"在这个仓库里是两件事**,而前者读起来完全像后者
—— 与 GH #171「`SKIP` 不是通过」、#213「没跑成不是通过」**同族**。

---

## 三、GH #300 交给总监的五件事,本轮处置

| # | 交办 | 本轮 |
|---|---|---|
| ① | 裁 `fieldsip` 入不入集 | **裁毕:入集**(§CG) |
| ② | 连 §CE.6 最后一段一起裁(同腿 arm) | **裁毕**(§CG.3 条件 D,见 §2.3) |
| ③ | 裁 §3 的主判据(相反极性消费方) | **未裁**,留下轮 |
| ④ | 建议一次跨 id 的「相反极性消费方」普查 | **未做**,留下轮(它是 ③ 的下游) |
| ⑤ | §9 那条 trunk RED 请派人 | **已开 `[bug]` #315**,见下 |

### 3.1 `[bug]` #315:**没复现,而"没复现"本身是那条 issue 的内容**

协同组在**分块全量**里、在净 `HEAD 1ccdeaa7` 上复现了
`tests/test_cm_arcane_aura_passive.lua:226`。本轮总监在树 `da658254` 上三次实测:

| 跑法 | 读数 |
|---|---|
| 单文件走 runner | **16 tests, 0 failures** |
| `test_cm_` 同族同进程 | **76 tests, 0 failures** |
| 单进程全量 `test_` | **900s 超时未跑完**(GH #124,**不是读数**) |

且 `git log --since=10:00 --` 那个测试文件与 `hero_crystal_maiden.lua` = **空**
⇒ **被点名的文件和它的被测对象一行没动过,而它现在是绿的。**
⇒ 两个假说都还活着:**(甲)跨文件单进程状态耦合**(⇒ 成因在别的文件里,标题点错了位置;
本轮已把嫌疑收窄到 **6 个**同时读写 KV `BEHAVIOR` 并给句柄挂 `GetBehavior` 的测试文件);
**(乙)已被某次落地顺带修好**(⇒ 结案前要指出是哪一次)。

**⚠️ 立案句不是这两条,是第三条**:该文件**没有** `[detector]`/`[ratchet]` 标签(grep 计数 0)
⇒ 不在快腿的 29 个文件里;而单进程全量在容器里跑不完 ⇒ **没有任何一条每轮会跑的腿看得见它**。
**GH #267 / #216 §5 同族第三例。**
**本轮把它跑绿了,这不构成"它没坏",只构成"没人每轮在看它"** —— 这两句在 #315 里分开写了。

---

## 四、⚠️ 发表纪律(铁律 6 的 2026-08-29 补丁,GH #290)本轮**当场生效了一次**

给 #300 的追评草稿存文件后跑 `bash tools/agent/claim_precheck.sh`:

```
MISSING   section test_set.md §CG
          no §CG heading in iterations/streams/test_set.md (off-trunk refs not searched)
DO NOT PUBLISH YET -- the citations above do not resolve on origin/main.
```

**它是对的**:§CG 此刻只在容器里。⇒ **#300 的追评 push 之后再发**,本报告写作时**尚未发出**。
(#315 不受此限:它一处也没引用 §CG,引用的 `tests/…` 路径与树 `da658254` 都在 trunk 上。)
**这正是 #290 立案的那个顺序 —— push 排在"让下游依赖它"之前。**

---

## 五、验证

- 开工自检:worst exit **3**,findings **只有 cadence**,`UNCERTIFIABLE none`;
  trunk python **53/0/0**、快 Lua **29 文件 0 失败**。
- `pending_rulings.py` 落地后复跑:`un-ruled RIDESHARE none / OTHER none`,
  `total open requests` **39 → 40**(新行被收录,且**因带 `director` 字段而不算未裁**)。
- `tests/run_py_tests.sh` **53 passed 0 failed 0 uncertifiable**。
- `run_tests.lua test_fieldsip_magnitude.lua` **15/0**;`test_cm_arcane_aura_passive.lua` **16/0**;
  `test_cm_` **76/0**。
- `check_armed_wiring.py --cand <45 串>` **45/45 WIRED,exit 0**。
- **`bots/` / `game/` 逐字节零 diff** ⇒ 无 Lua 行为改动;静态门由 pre-push 自跑,
  **未用 `RULE6_BYPASS`**。**全量 Lua 套件未跑完**(900s 超时,GH #124),**不声称**。
- `queue.json` 改动是**外科式的**:第一次用 `indent=1` 重写整个文件(**989 insertions / 970 deletions**),
  当场 `git checkout` 回滚;先验证 `indent=2` + 末尾换行**逐字节 round-trip 还原**,再重做
  ⇒ 最终 diff **19 insertions, 0 deletions**。**记这条:改结构化文件前先证明 round-trip。**

---

## 六、本轮明说没做的

- **promote/reject 无可判**(本轮无新 verdict;W26 起飞 37 分钟,未收割)。
- **W35 周日邮件未发** —— 今天**周六**,邮件是**明天**。
  **给明天的那封准备好一条**:月末预测 **$80.585**,会跨 $80 告警档、不会触及 $90 刹车(§一)。
- **低频 patch 检查未做**(连续多轮)。
- `DECISIONS_NEEDED.md` **本轮无新增**(两个决定都在自主授权内)。
- **#300 的 ③④ 未裁**;**#302 / #221 §3 / #266 / #140 等长队未动**。
- **`ORPHAN_PROPOSAL` 检测器未落地** —— 它是 §2.1 的补救,**下一轮第一优先**;
  在它落地之前,**任何组提入集必须同时开 queue 请求行**(已写进 §CG.5 与 `test_set.md` 第 4 行)。

## 七、下次触发

① **`ORPHAN_PROPOSAL` 检测器**(`pending_rulings.py` 反查 `test_set.md` 入集提议小节,无对应
请求行则报;§CG.5,总监类 `[harness]`,**第一优先**) ② **W35 周日邮件(今天就是周日,别再顺延)**
③ #300 的 ③ 主判据裁定 + ④ 相反极性普查 ④ 盯 #315 的(甲)/(乙)判别 ⑤ 盯 W26 收割与
§CF 两条(`odbuild`/`wkqdmg`)的退回门**真被执行** ⑥ 盯 `fieldsip` 的条件 (a)(**不得**从
`stayfield` 留守率差分读) ⑦ 成本核查(本轮已做,下轮按例) ⑧ DECISIONS_NEEDED §14 第三腿排期
⑨ #302 正题 ⑩ GH #221 §3 ⑪ **GH #266**(第十二轮) ⑫ W20 早发裁定的书面销号
⑬ #271 的 `c6a` n=1→3 ⑭ #204 的 n=8 ⑮ `[bug] #283` / `#249` ⑯ **GH #140**(第三十四轮)
⑰ #289 顺带 1/2 ⑱ 给 `claim_precheck.sh` 找自动触发点 ⑲ backlog §16/§17/§18
⑳ **「下一个空字母」分节方式该换了**(§CE 撞号是第三次,本轮 §CG 靠"恰好没人同时取"躲过)
㉑ 把「变异台还原必须走文件副本不走 `git checkout`」写进某处(上轮欠,本轮的 `queue.json`
回滚是同一课的第二次现场)。

---

## 八、token 用量

```
TOKENS total_in=5,712,755 out=45,152 turns=49
```
(统计时刻之后的收尾回合不计入,量级参考足够。)
