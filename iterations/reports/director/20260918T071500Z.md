# 总监 2026-09-18T07:15Z — RULING 76:刷语料的那个集合不再是第二份手抄

**一句话**:`issue_state_snapshot.json` 的刷新集**一直是用眼睛从清单里抄的**,
而同一份清单里审计腿自己抽出的是 **9 个号**、上一版 `source` 逐字写着「**全部 8 个号**」——
差的那个是 `GH #548`,于是它**连续五轮**读回 `not in corpus`,
**每一轮都把它当「下次补上」写进报告,没有一轮怀疑过刷新集本身**。
本轮把刷新集变成工具打的(`--refresh-set`,与审计腿**同源**),并把语料真刷了一遍。

零 AWS、零波次、`bots/`+`game/` **零 diff**、不发 owner 邮件、无 promote / 无退集 / 无入集。
**成本(RULING 48 三段式):零 EC2 / 零 CE / S3 读取 0 个对象(出网未计价)。**

---

## 〇 取活依据:仍然是 §1.5 那条腿在我眼前失效,不是从 backlog 里挑的

本轮开工它逐字读回:

```
corpus   : ... fetched 2026-09-17T13:44:00Z (17.3h old), 12 issues
scanned  : 1 entry (of 95), 1 carry segment(s), 9 GH ref(s)
UNCERTIFIABLE GH #548 -- not in corpus -- NOT the same claim as `open`
UNCERTIFIABLE GH #806 -- not in corpus -- NOT the same claim as `open`
OK            GH #856, GH #867, GH #240, GH #843, GH #859, GH #810, GH #528
VERDICT  : UNCERTIFIABLE (exit 2)
```

⭐ **上一轮(RULING 75)刚把 segment 选取修对,清单从 0 个 ref 变成 9 个** ——
于是这一轮才第一次看得见:**9 个里有 2 个语料根本没有**。
`#548` 是老面孔(上一轮记的是「**第四次**以 `not in corpus` 现身」,本轮第五次);
`#806` 是 RULING 75 那轮 ⑲ 新写进清单的,**一进来就落在语料外面**。

⛔ **而这不是语料陈旧**(17.3h < 48h 的门,腿没有降级);是**语料里压根没有这两个号**。

### 成因逐字可读,写在语料自己的 `source` 行里

上一版 `source`:

> 逐号点查(2026-09-17T13:4x–13:5xZ 总监 RULING 70 轮重读 865/856/867/240/843/859/810/528 ——
> **即本轮『下次触发』清单点名的全部 8 个号**)

**那句话是错的,而且是可当场证伪的**:同一份清单,本腿自己的抽取器给出 **9 个**。
第 9 个是 `#548`(它在清单第 ⑨ 项)。**刷语料的那一轮用眼睛数了一遍,数掉了一个。**

⭐⭐ **失效方向是单侧的,这是本条立法的全部理由**:

| | 代价 | 谁会发现 |
|---|---|---|
| 多抄一个号 | 多**一次免费点查** | —— |
| 漏抄一个号 | 那一格**每一轮**读不出来 | **没有人** |

漏掉的那个号**不会在任何读数里举手** —— 它只变成另一行 `UNCERTIFIABLE`,
而那一行按这条腿自己的措辞(RULING 67)读作「**这一轮没人能看,下一轮自己会好**」。
**它不会自己好:刷新集是手抄的,下一轮会照抄同一份。**
⇒ 与 RULING 75 同型:**一个真命题(点查了 8 个号)满足了一个别的问题(清单点名了几个号)**,
而**两者之差正是五轮里没人看见的那一个**。

---

## 一 ⚖️ RULING 76:刷新集由工具打,⛔ 不许用眼睛从清单里抄

**判据**:刷语料的那一轮,第一步跑

```
python3 tools/agent/carry_item_issue_state.py --refresh-set   # 只读、离线、<0.2s
```

它打两行:

```
REFRESH-SET #856 #867 #240 #843 #859 #810 #548 #528 #806
REFRESH-MISSING #548 #806
```

**`REFRESH-SET` 与审计腿读的集合同源** —— 两边都走新抽出的 `select_entries()` + 既有的
`refs_in()`。⭐ **抽 `select_entries()` 出来是这条裁定的承重部分,不是重构**:
两边各自敲一遍「读哪几条 entry / RULING 67 要不要回落」的逻辑,
**等于把手抄的失效方式换个地方重演一遍**。

### 落地四件

1. **`tools/agent/carry_item_issue_state.py`**:新增 `select_entries()`(entry 选取那一半,
   **原块逐字搬过去**,`audit()` 改调它)、`refresh_set()`、CLI `--refresh-set`。
   `--selfcheck` 仍 **ALL PASS**。
2. **`tools/agent/carry_item_issue_state.py` 的 `--help` 订正**:旧 epilog 逐字教人
   「dump `list_issues(state=all)`」,**而语料文件自己的 `_refresh` 写的是点查** ——
   ⛔ **同一个仓库里两份配方,而滞后的那份在 `--help` 里**(RULING 55 实测列表读数可滞后 8.8min)。
   新 epilog:先 `--refresh-set`,再对**每一个**号 `issue_read(method=get)`,
   ⛔ never list_issues、never a hand-copied subset。
3. **`tests/test_carry_item_issue_state.py` claim 12**:**90 checks / 0 failures**(落地前 **68**)。
   ⭐ **12a 是承重那条,而它的写法本身是本条的应用**:refresh 集不与**写在测试里的字面量**比,
   而与**审计腿自己那一次输出**里解析出来的号比 —— 写字面量等于**在测试里放第三份手抄**。
   另有 12b(缺号必须点名)、12c(语料坏/缺/陈旧时**集合照打,且全员待取**)、
   12d(anti-empty-match,exit 2)、12e(作用域随 `--entries`)、12f(配方与 `--help` 都得说点查)。
4. **变异台 `mutstand_carry_item_issue_state.sh` M20–M24**:**24 CAUGHT / 0 SURVIVED / control_ok=1**,
   `RESTORE: YES`。M20 就是本条的事故本身(refresh 集只取每条 entry 的第一个 ref ⇒ 与审计腿分岔)。

⚠️ **M23 第一版打的是 `ANCHOR MISS` 不是 `CAUGHT`,与 RULING 75 那轮 M17 逐字同因**:
`perl -0pi` 按**字节**跑,`\x{26d4}` 里的花括号被读成字面量(`Unescaped left brace in regex is illegal here`),
锚一次都没落。⭐ **没有那个台子自己的 ANCHOR MISS 守卫,它会印 `SURVIVED`,而我会去改一条根本没错的断言。**
改成纯 ASCII 锚(`issue_read\(method=get\)`)后 24/24。
📌 **同一个坑两轮内第二次**,已把「ASCII 锚」写进该台子 RULING 76 那一节的注释里。

---

## 二 语料真刷了:9 个号逐号点查,9 个全 open

⛔ 本轮**没有**用 `list_*`(RULING 55)。逐号 `mcp__github__issue_read(method=get)`,
`dragonghy/dota2bot`,**REFRESH-SET 的全部 9 个**:

| 号 | state | 备注 |
|---|---|---|
| #856 | open | [harness] 自检与 push gate 抢 `soak_side.lua` |
| #867 | open | [harness] open-coded walk 棘轮盲于裸字符串语料根 |
| #240 | open | [harness] 语料规模检测器只认「等于 fixture 数」 |
| #843 | open | [bug] python 闸红,抓它的测试按设计跑不进钩子 |
| #859 | open | [strategy] `bots_reach_closure.py` 引用不存在的测试 |
| #810 | open | [harness] Lua 闸成员资格由容器速度决定 |
| **#548** | **open** | **五轮来第一次进语料** |
| #528 | open | [bug] 账单里那台非本项目实例 |
| **#806** | **open** | **新进清单即缺,本轮补上** |

刷后审计腿逐字:

```
corpus   : ... fetched 2026-09-18T07:08:58Z (0.0h old), 14 issues
scanned  : 1 entry (of 95), 1 carry segment(s), 9 GH ref(s)
OK            GH #856, GH #867, GH #240, GH #843, GH #859, GH #810, GH #548, GH #528, GH #806
every carried GH ref is open.
VERDICT  : OK (exit 0)
```

⭐ **这条腿五轮来第一次 exit 0**,且**没有 STALE-CARRY** —— 上一轮那 9 条待办没有一条挂在已关闭的号上。
⛔ **「全 open」不等于「都在推进」**:它只说这些号还活着,推进由 owed / STALL 那几条腿管。

⚠️ **旧号 466/523/538/865(closed)与 839(open)本轮未重读**,按 `_direction` 保留 ——
它们**不在本轮的 REFRESH-SET 里**。两种情形的安全性理由不同,已在 `source` 里**分开写**
(closed 行靠 `fetched_at` + `--max-age-hours` 挡「重开被说成 closed」;open 行的滞后只让腿少说一句话)。

### ⭐⭐ RULING 76 的第一次实战在同一轮里:它又抓到一个

写完本轮『下次触发』清单后重跑审计腿,集合从 9 变成 **10** —— 第 10 个是 **`GH #290`**,
它是我在清单 ⑨ 里当**论据**引用的(「⛔ 先 push 再发,GH #290」)。

抽取器**不区分论据与待办** —— 这是 claim 7 的既定口径(`GH #<n>` 算、裸 `#77` 不算),
**不是缺陷**。⛔ **处置是照集合点查,不是回头改清单措辞** —— 后者正是本条要废掉的
「跟刷新集讲道理」:手抄之所以出错,从来不是因为抄的人不认字,是因为**判断哪个号「该算」
这件事本身发生在人脑里,不留读数**。

`#290` 点查 = `open`,已补进语料(15 issues)。刷后:

```
scanned  : 1 entry (of 96), 1 carry segment(s), 10 GH ref(s)
OK            GH #856, GH #867, GH #240, GH #843, GH #859, GH #810, GH #528, GH #548, GH #290, GH #806
every carried GH ref is open.
VERDICT  : OK (exit 0)
```

⇒ **本轮自己写的清单,交出去之前就是 exit 0 的。** 上一轮交出去的那份不是,而写它的人不知道。

### ⚠️ 顺带从 #290 正文读到一条**今天到期**的时限,照登

#290 §6 末尾逐字:`⚠️ 语料保质期:dem21-expire-21d ⇒ W20–W22 约 **2026-09-18** 前取;W23 约 2026-09-19。`
**今天就是 09-18。** ⛔ 本轮**不去取**(总监不发波、也不替录像组花 S3 的钱),
但**这条不属于任何人的清单**,所以在这里点名:`#290` §6 的执行核验(OD 停摆压实后的
`skill_point_stall.py` 复跑)所依赖的 W20–W22 语料**今明两天过期**;
过期之后那条验收线要么改用新波(而刹车第 47 轮持有 ⇒ 无新波),要么作废重设。
⇒ 已进下次触发,归口 **录像组 / 批测台**,由下一轮以 issue 评论交棒(本轮 push 之后)。

---

## 三 章程 §1.5 已改:刷新那一步从散文变成命令

`iterations/streams/director.md` §1.5 的 📌 行后新增 ⭐⭐ 段:刷之前**必须**先跑 `--refresh-set`,
⛔ 不许用眼睛抄;并把「8 vs 9」这个立案读数和单侧失效方向钉在那里。

---

## 四 巡检 + 成本

**五组全活,无掉棒**(最新报告):
batch-desk 09-18T06:10Z / replay-check 03:45Z / strategy 04:24Z / hero 05:34Z / director 04:09Z。

- **batch-desk**:刹车**第 47 轮持有**,零发波、零收割欠、零泄漏;
  结转 MTD **`$92.163`**(> `$90` 刹车线)⇒ 本轮仍**零发波**。
  ⚠️ `forecast $118.091` > `$100` 仍在 `DECISIONS_NEEDED.md`,**W38(09-20)邮件带它**。
- **replay-check**:零 `.dem`、零 AWS;逮到**第七条假 OWED**;答掉 `hero-103` 供帧请求(结论:买不到)。
- **strategy**:`helpself` gated 落地(`#allies + 1` 重复计自己),未提入集,成员串仍 25。
- **hero**:纯注释、零行为;把 GH #890 的告警落点从 CM 改判到 Lion 那份肯定读数上。

**本轮总监自己的成本**:零 EC2 / 零 CE / **S3 读取 0 个对象(出网未计价)**;
GitHub MCP 点查 9 次(免费)。**不花 AWS 的钱**(章程:总监不发波)。

---

## 五 开工自检(铁律 10)与铁律 6

⭐ **纪律 3 本轮没发**:第一条命令就是 `bash tools/agent/rc.sh bash tools/agent/routine_selfcheck.sh > <文件> 2>&1`
—— **重定向到普通文件,不是管道,不加 `timeout`**(抄自批测台 06:10Z 报告钉下的取法)。
⚠️ 它再次跑过 600s 被移到后台,**读数从 `RC_LOG` 取,不从终端取**。

### 自检读数(`RC_EXIT=3`,`RC_LOG=/tmp/rc.0Hnp7X`,15 条腿)

```
legs run              : 15
FINDINGS (exit 3)     : cadence queue-rulings owed-executions lua-coverage
UNCERTIFIABLE (exit 2): trunk-red(python)
NOT RUN (inside a leg): tests/test_lua_gate.py tests/test_luacheck_gate_soakswitch.py tests/test_selfcheck_lua_leg.py
selfcheck worst exit: 3
```

- `trunk health (python)` = **`148 passed, 0 failed, 3 uncertifiable`**;
  `trunk health (fast Lua detectors)` = **138 tagged file(s), 0 failures**(自述边界:FAST SUBSET);
  `unlanded-commit scan` = `OK: no unlanded work in the certifiable window.`
  (自述边界照抄:`shallow clone: YES`)。
- ⚠️ **RULING 74 的作用域本轮我又踩在边上,照实记**:python 套件那条腿跑的时候,
  我正在写 `tests/test_carry_item_issue_state.py` ⇒ **`148 passed` 对这个文件的那一格不可引用**。
  **静止树上单独复跑**:新版 **90 checks / 0 failures**;
  ⭐ **HEAD 那份旧测试跑在新工具上 `68 checks, 0 failures`** ⇒ **没有推翻任何既有断言**。
  其余 150 个 python 文件本轮我一个字节都没碰。
- ⚠️ **`UNCERTIFIABLE -- a python test did NOT run (could not read its input)`** 本轮又现身
  —— 这正是 **GH #856** 的并发形状(自检与别的读者抢 `bots/Customize/soak_side.lua`)。
  ⛔ **不读成 trunk 红**(横幅自己就这么写);登记为该号的又一次现场。
- ⭐⭐ **GH #548 本轮拿到一个新读数,而且它比立案时大得多**:
  `test_selfcheck_lua_leg.py` 逐字 `NOTE 5a0 cost: **138 file(s)** [discovered -- the leg never
  reached its count line] in **120.0s** (budget 120s)` ⇒ **9 条 check 没跑,UNCERTIFIABLE**。
  立案(09-06)时是 **84 文件 / 120.1s**,今天是 **138 文件 / 120.0s** ——
  ⛔ **这不证明「集合涨了 64%」就是全部原因**(GH #358:墙钟分不开集合变大与容器变慢),
  但 #548 验收里逐字要的就是「跨若干轮的 NOTE 行读数」,**本轮这一条是其中一份**。
  ⇒ 进下次触发,由**有 GitHub MCP 的下一轮**贴进 #548(本轮 push 之后才发,铁律 6 / GH #290 的顺序)。
- `lua-coverage`:`disk 512 | push gate 347 | 开工自检 leg 138 | known_red 5`,
  `UNCOVERED 114 of 512 (22%)` `{'no_manifest_row': 51, 'timed_out': 60, 'too_slow': 3}`;
  **`UNCOVERED SET GREW -- 2 file(s)`**:`test_fieldsip_transfer_receiving_site.lua`(**连续第五轮**,
  RULING 71 §三已登记)+ `test_lion_w_fight_seed.lua`(`too_slow`,英雄组名下,GH #806 族)。
  ⛔ **两条都不是本轮的产物**(本轮 `bots/`+`game/` 零 diff,且没落任何 Lua 测试)。
- `cadence`:2 finding(`director` 09-17T04:20Z→13:30Z 9.2h;`hero` 09-17T11:18Z→17:12Z 5.9h)——
  **都是历史窗口,不是本轮**。
- `queue-rulings` / `owed-executions`:存量(RIDESHARE 42 条待裁、
  `py_manifest_no_carry_baseline_port` / `stayfield2_readmit_when_fieldsip_moves` 按设计 OWED)。
  ⛔ `stayfield2` 那条**不是掉棒**,是被裁定挂起的重新入集问题的活证。

### 铁律 6(push 前)

见文末「同轮收尾追加」。**本轮 `bots/`+`game/` 零 diff**,改动面是
`tools/agent/*.py` / `tools/agent/mutstand_*.sh` / `tests/*.py` / `iterations/`。

---

## 六 token 用量(铁律 8)

```
TOKENS total_in=9,095,207 out=43,576 turns=71
```

(统计时刻之后的收尾回合不计入。)

---

## 七 [同轮收尾追加,push 之后]

**push 记录(顺序按 RULING 69:先分支、再 main)**:`git pull --rebase origin main` 干净(`Rebasing (1/1)`,
`PULL_EXIT=0`)⇒ 分支 `* [new branch] claude/compassionate-albattani-j56hbx`(`PUSHBR_EXIT=0`)
⇒ `HEAD:main` **`a52a097d..37ca954d`**。`git ls-remote origin main` = `37ca954d` = 本地 HEAD。

**第一推三条腿逐字**:

```
luacheck bots game: 0 warnings
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 134 ran, 0 findings, 0 uncertifiable, 46.0s
lua gate: 426 ran, 0 findings, 0 uncertifiable, 9 unanswered, 5 known-red, 622.9s
```

**第二推**打 `RULE6_MEMO=REUSE`(同树 `fbb7cc67` + 同 `origin/main=a52a097d`)逐字重复同三行。
**⛔ 无 `RULE6_BYPASS`。**

⚠️⚠️ **存量,GH #810 名下,本轮读数与上一轮逐位相同**:lua 腿 **9 条**新测试超预算被 EXCLUDED
(上一轮也是 9 条 / 645.2s,本轮 9 条 / 622.9s)⇒ ⛔ 仍不顺手重测(RULING 72 逐字要求先读 GH #810)。
名单:`test_cm_w_creep_clock` / `test_cm_w_selfdefense_damager` / `test_fieldsip_transfer_receiving_site` /
`test_overchase_pursuit_tense` / `test_rescpost_outpost_narrow` / `test_runring_target_in_ring` /
`test_stayfield2_live_domain` / `test_tpchew_channel_creep` / `test_wk_reserve_rank_blind`。
⭐ 顺带一条:`lua gate: scope = 6 path(s) changed vs the pushed refs' base (base=fallback-merge-base)`
—— **第一推的 scope 不是空表**(RULING 69 的整个立法理由就是第二推的 scope 恒为空 ⇒ 跑全集),
本轮**按文档顺序推,memo 命中,第二推 0 秒**。

**`claim_precheck.sh`**(push 之前跑的那次,对本报告)逐字 `paths cited 12 … resolved on trunk 8  refused 0` /
`IGNORED-BY-DESIGN path bots/Customize/soak_side.lua` / `clean` / `OK to publish`,`RC_EXIT=0`。

### ⭐⭐ 两条棒本轮就交出去了,不留给「下一轮」(铁律 9)

两条草稿各自先过 `claim_precheck.sh`(两份都 `clean` / `OK to publish`),push 之后才发(GH #290 的顺序):

1. **GH #548**:跨轮 NOTE 读数(84→138 文件 / 120s 预算)已发表 ——
   `https://github.com/dragonghy/dota2bot/issues/548#issuecomment-5726744042`。
   ⭐ 顺带给了验收选项二(切片)一条约束:`leg 138` 与 `lua-coverage` 的 `开工自检 leg 138` **是同一个集合**,
   切片要同时改两条腿的横幅,否则 22% 的 UNCOVERED 变成按轮漂移的数。
2. **GH #290**:今天到期的语料时限已交棒给**录像组 / 批测台**,二选一并写下选择 ——
   `https://github.com/dragonghy/dota2bot/issues/290#issuecomment-5726747149`。

⇒ 下次触发 ⑨ 与 ⑨b **本轮划掉**;⛔ 不写成「这两件事做完了」——
交出去的是**棒**,#548 的验收仍未满足(它要的是跨**若干**轮的读数),#290 等的是那两组的回答。
