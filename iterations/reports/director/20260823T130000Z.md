# 总监 2026-08-23T13:00Z —— 第五十五次触发:W3 的「三个 claimant 一个名额」裁给 `creeppull,pullbeat`;
# 外加把「载体不在」变成发波前的**第二道** exit code 门

**一句话**:批测台 12:09Z §8 交上来一道带截止时刻(18:09:32Z)的排期裁定,三条 claimant 今天全部就绪;
裁给 `strategy-5`(`creeppull,pullbeat`),三条互相独立的理由;裁的过程里发现
**三方都以为它需要 §AU.2 的例外,而它不需要** —— `creeppull` 早就在集合里;
顺带交付载体门 `--assert-carrier`,它**当场把 `cmboots` 的域从 306/306 改成 4 个种子里的 3 个**。

**`bots/`/`game/` 逐字未动 ⇒ 稳定版零漂移。总监零 AWS 调用,自身支出 $0。未 promote、未 reject 任何 id。**

---

## 1. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` ⇒ **worst exit 0**。
- UNLANDED **无**(certifiable 25 refs;274/299 refs 因浅克隆无 merge-base 被**拒答**,工具照旧不猜);
- cadence **clean**(streams 5 / reports 272 / 窗口 24h);
- trunk python **16 passed / 0 failed**(收尾时 17/0,+1 = 本轮新增)。

## 2. 输入:四组最新报告 + 一道带截止时刻的裁定

| 组 | 最新报告 | 对总监的 ask |
|---|---|---|
| batch-desk | 12:09:32Z | **§8:W3 一个名额三个 claimant,明说不自裁,截止 18:09:32Z** |
| hero | 11:45:55Z | `cmboots` 申请入集(§AU.4 补 gate 已落地,`6a199f9`) |
| strategy | 11:33:45Z | `pullbeat` 申请入集 + 专波 |
| replay-check | 10:54:16Z | 无(GH #145 世界断言 21,不需要裁定) |

**没有一条新的 `[bug]`/`[harness]` 需要总监立刻动 Lua。** 24-id 归因波 12:09Z 刚发出,
**约 14:1xZ 才收割** ⇒ §AT.1 的处置这一轮**不可能做**,不是被挤掉的。

## 3. 裁定:W3 = `creeppull,pullbeat`(全文 `test_set.md` §AV)

三条**互相独立**的腿,任何一条单独都够:

1. **铁律 9** —— `strategy-5` 是 **owner P1** 的下一棒;owner 优先项凌驾于 issue 流。
   `campgrade`(GH #137)与 `cmboots`(GH #144)**都不在** `OWNER_PRIORITIES.md` 里。
   P1 的责任链此刻正卡在「一个带申报目的的批测波次」这一格 —— 那格就是 W3。
   而 P1 掉过一次棒(2026-08-19 修好后从队列消失 37 轮),铁律 9 的连带规则就是为它立的。
2. ⭐ **它根本不需要 §AU.2 的例外,而三方都以为它需要。** 批测台把它记成「两个 id 同波 =
   §AU.2 的明写例外申请」,协同组自己也是这么提的,我差点就照着这个前提去裁「批不批例外」。
   **实际上 `creeppull` 早就在集合里,而且就在 §AT.0 那个 24-id 可 arm 串的第 19 位**
   ⇒ 本波**只有一个新 id**(`pullbeat`),`creeppull` 是它的**结构性使能项**
   (`pullbeat` 只活在 `bot.roamCreepPull` 里,单独 arm 是逐字节 no-op)。
   **§AU.2 逐字成立** —— 少一条例外就是少一个以后会被援引成先例的口子。
3. ⭐ **名额算术:三条里唯一会把名额还回来的一波。**

| claimant | 花名额 | 这一波能 resolve 掉集合里的谁 | 净名额 |
|---|---|---|---|
| **`creeppull,pullbeat`** | 1(`pullbeat`) | **`creeppull`** —— 在集合里、在串里,而它的条件 (a) **从来没买到过**;W3 无论 WORKING 还是 SILENT/BUGGY 都会 resolve 它(promote 出集 _或_ 退回出集,§AT.3 两者都算) | **0** |
| `campgrade` | 1 | 无 | **−1** |
| `cmboots` | 1 | 无 | **−1** |

按 §AS.3 冻结**自己的**立法目的(集合的可判读性,不是省钱),W3 走这一条是唯一
**不让集合更难判读**的选择。这条腿与铁律 9 那条互相独立:即使 owner 从没点过拉野,
名额算术单独也会给出同一个答案。

**顺带分开了一对一直混着说的东西**:**名额是集合增长的闸门,不是发波的闸门**。
若 W3 的核验结论迟到而 W4 时刻已到,`campgrade` **照发**。

**排期**:W3 `creeppull,pullbeat` / W4 `campgrade`(条件性批准**不变**,让路的理由是
owner P1 与名额算术,**不是这一包的质量**)/ W5 `cmboots`。
波次参数见 §AV.0;`--rec-slots 12` **不许降** —— 本波主判据是帧级检测器,
**帧通道就是这一波要买的东西本身**。

## 4. [harness] 交付:载体门(发波前的第二道 exit code)

`check_armed_wiring.py`(§AU.6)证的是「**这棵树上有调用点**」。
**没有任何一样东西证过「这一波的牌里有那个英雄」** —— 而两者失败时的现场**一模一样**:
armed 上去逐字节 no-op、读数呈现为「无影响」、**没有任何计数会报警**。
这不是假想:`seed_draft.py` 自己的文件头记着一次 **224 局镜像局、零 Axe** 的波
(为 `axebuyblink` 买 (a),买了个空)。**工具早就在,缺的是把它变成一道门。**

- `tools/batch_test/soak/seed_draft.py --assert-carrier <hero[:pos][,...]>`;
- `tests/test_seed_draft_carrier.py`:**29 检查,变异 M1–M4 四红且红对地方**
  (absent 返回 0 / 零种子当通过 / 位置那一半被忽略 / 拼错的英雄被静默丢弃),还原后逐字节相同;
- **exit 1** = 某 term 零个种子带得动(不发);**exit 2** = 什么都没查(未查 ≠ 通过);
  **exit 0 也必须打印 `satisfied/seeds`**(PARTIAL 不是失败,但它是预登记的域要照抄的分数)。

⭐ **M4 的红是被自己的第二道守卫挡住才发现的**:第一版探针用**单个**拼错的 term,
而「拼错 ⇒ 静默丢弃」之后 term 列表变空,`no carrier terms given` 那条守卫**照样返回 2**
—— 变异存活、测试却是绿的。改成 `axe,crystal_maidne`(**拼错的挨着一个真的**)才抓到:
静默丢弃会留下一个好 term 站着,门就过了。**教训**:一个「为了正确的理由返回正确退出码」的探针
和一个真探针长得一模一样;**变异测试的探针必须走到被变异的那一行**。

**LIMITS(引用干净结果时照抄)**:载体门证的是**牌桌上有这个英雄(且在那个位)**,
不代表该分支可达、谓词能为真、或该 id 做了任何事。**接线门 + 载体门 = 条件 (a)/(b)
的两块地板,不是任何一个的替代品。**

## 5. ⭐ 这道门当场改写了 `cmboots` 的域,和一次**没花掉**的 $1.5

```
$ seed_draft.py 888 895 896 906 --assert-carrier crystal_maiden:5
CARRIER seed=888 term=crystal_maiden:5 present=yes side=dire pos=4 satisfied=no
CARRIER term=crystal_maiden:5 seeds=4 satisfied=3 verdict=PARTIAL carriers=895,896,906

$ seed_draft.py 974 986 1024 --assert-carrier crystal_maiden:5
CARRIER term=crystal_maiden:5 seeds=3 satisfied=0 verdict=ABSENT carriers=none   # exit 1
```

- `cmboots` 只改写 `sRoleItemsBuyList['pos_5']`(`hero_crystal_maiden.lua:121–123`)⇒
  **种子 888 把 CM 发在 pos 4,那一整个种子的局里这个 id 是惰性的**。
  ⇒ **§AT.2 那个「CM 306/306」是「CM 在场」,不是「CM 在 5 号位」**;真实域 ≈ 四分之三的局。
  (§AU.1 早已把 `9fa4898` 从 −50.81 的嫌疑里机制性摘掉,**本条不复活那个嫌疑**,只把域写准
  —— 而域正是 hero-8 验收里**必须先报**的那个数。收割分母按 **3 个种子**算,
  否则载体稀释会被读成效应变小。)
- **提议的 axe+lion 新种子集对 `cmboots` 是 ABSENT。** §AU.3 允许独占波顺带把它登记成
  **常设**集(每纪元最多换一次)⇒ 一旦登记,`cmboots` **永远买不到 (b)**,
  而它整个存在的理由就是让 `9fa4898` 可测(§AU.4)。**⇒ 登记推迟到真正需要 Axe 的那一波**,
  届时必须同时登记 `crystal_maiden:5` 的载体读数。顺带:提议的这套只有 **3 个种子**,而一波是 4 台 × 1 种子。
- **立为规矩**:任何一套候选种子集**在登记为常设之前**,必须对每一个 hero/position 专用 id
  各跑一次载体门 —— 常设集是**所有**未来独占波的地基,**为一个 id 选的地基会静默地废掉另一个 id**。

## 6. 入集审批

- **`pullbeat` 批准**(成员串 27 → **28**),**与 W3 发波同生共死**:归因波若没能成功收割,
  §AS.3 的名额就没开,`pullbeat` 一并回退。依据(§AV.6):(a)-local 8 例全绿 **5 变异 5 抓**、
  真实帧、未 armed 命令流**逐条比对断言**(`A` + 36 个 `M`,一个 hold 都不许有);
  (c) 人类勾线是**起手之后**才 attack-cancel,而修复前在起手**之前**就发 move;
  三条「本地买不到」写死在头注里,且 W2(`GetAttackRange` 966/966 帧读 mock 默认 150)
  直接解释了那条最顺手的子句**为什么没写** —— **买不到就不写,而不是写了当买到**,这正是我要的形状。
- **`campgrade`**:条件性批准不变,W3 → W4。**`cmboots`**:排期 W5,发波前必过载体门。
- `itemtrip` 逐字未动(§AT.4);其余 24 id 逐字未动。

## 7. 健康度 / 成本 / 沟通

- **五组本轮均有产出**,无空转,无观察项。cadence 自检 clean。
- **owner 优先项巡检**:**P1 本轮有推进**(球从「协同组补 fixture」走到「批测台 W3 波次」,
  是 40 轮以来第一次真正排到波次上),不触发 12 轮红色升级;**P2** 的下一棒仍在录像组
  (用 `corpus_query.py` 给 #110/#111 算适用帧人口,§1 那条交出去的棒),本轮无新阻塞。
  **未编辑 `OWNER_PRIORITIES.md`** —— 该文件写明由主会话维护、各组不得增删;
  球的位置记在 §AV.8、state.json 与本报告里。**这一条建议 owner 或主会话择机确认**:
  文件里的「当前球在」行会随排期变化而变旧,而唯一被授权改它的角色不在 Routine 循环里。
- **成本**:MTD **$23.578**(批测台 12:09Z 自报,免费 budgets 读数;该读数仍是
  2026-08-22T21:26:04Z 那份快照的第六次复读,批测台已给出可证伪形式:
  晚于 12:09Z 的第一次真刷新应 ≈ $28.1)。$45 fence / $90 brake / $100 owner 线**全未触及**。
  **总监本轮零 AWS 调用**;已批准的 12:09Z 一波 ~$1.5 在预算内,W3 同量级。
- **DECISIONS_NEEDED.md 无新增**(本轮决定全在自主授权内)。**本周 owner 邮件已于今日稍早发出,本轮不发。**
- 周日效率台账 **W34 已出**(`efficiency_202634.md`),本轮不重做;patch 检查 02:51Z 刚做过,跳过。

## 8. 验证(铁律 6)

本轮改动:`tools/batch_test/soak/seed_draft.py`(+96)、`tests/test_seed_draft_carrier.py`(新增)、
`iterations/`(test_set.md §AV / queue.json 三条附注 / state.json 一条 / 本报告 / 章程)。
**`bots/`/`game/` 逐字未动 ⇒ 两道 Lua 门无适用对象**;容器无 `luacheck`/`lua5.1`(已复核)
⇒ **不声称跑绿过 Lua 全量**。`bash tests/run_py_tests.sh` ⇒ **17 passed / 0 failed**;
`tests/test_seed_draft_carrier.py` 单独 **29 检查 0 失败**;`seed_draft.py --selftest` 仍 PASS(40/40 槽位)。

## 9. 跨组交棒(铁律 9 连带规则)

- **批测台**:W3 串/树/种子/两道门已写死在 §AV.0 与 GH #140 追评里;
  **`--assert-carrier` 请并进发波流程,两道门都非 0 不发**。
- **录像组**:W3 收割后按 `creeppull_specificity.py` 的**预登记两个数**核验
  (poke 后小兵尾巴中位应变长、零尾巴占比应下降),**判据是波内 armed vs base**;
  GH #143 的 2.2s / 26.8% 是跨波跨树参照,**按 §AF.3 不作判据**。
- **英雄组**:`cmboots` 的预登记域改为 **3/4 种子**,收割分母按 3 个种子算。
- **下一轮总监**:①收 12:11Z 归因波按 §AT.1 处置(**不许挪门柱**);②W3 发波后确认两道门都跑了;
  ③**GH #141**(`cand_ref`,先写四条验收测试再动 `jmz_func.lua`;会触发 #124 的 83 分钟门);
  ④**GH #131**(**第四次被挤**,这次是被硬时限裁定挤的;下轮无硬时限项则**必须**做它);
  ⑤backlog 6b(`unlanded_commits.py` 并排打印 ref 活跃度)/ §9(扩 `detect.Timeline` 索引小兵野怪)。

## 10. Token 用量(铁律 8)

```
TOKENS total_in=7,378,484 out=50,601 turns=59
```
(统计时刻为止;此后的收尾回合不计入。)
