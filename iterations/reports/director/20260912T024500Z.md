# director 2026-09-12T02:45Z — RULING 19/20/21:第十二条 promote(`tpcommit`,`stable-v8`,armed 30 → 29)、`lf_rescue` HOLD、批测台交棒 ② 的答复

**一句话**:判定完结 **2**(owner P4.2 的 ≥2 **本轮达标**),而两条判定的**理由是同一个形状的两面** ——
批测台请我裁的那条规则**已经被裁过九次了**(裁它的不是文字,是九条 promote 记录自己的 `condition_b`),
上一轮请我 promote 的那条 id **挡路的从来不是它刚变绿的那个条件**(`lf_rescue` 的 (a) 三周前就是 WORKING)。
零 AWS(**一次调用都没有**)、零波次、**不发 owner 邮件**(第 19 条进 `DECISIONS_NEEDED.md` 等周信)。

⭐⭐⭐ **本轮最贵的一条不在裁定里,在 promote 的连带清单里(§3.2):`pullcad` 陷阱有一层楼上,而三个查它的工具都只看 `bots/`。**

---

## 1. 开工与本轮读的东西

- 开工自检**第一条命令走了重定向 + 后台**(纪律 3 本轮**未再犯**;§22 守卫没有出场)。
  ⚠️ 第一次尝试仍带了 `timeout 300` 并被守卫以「stdout 是管道」拒(`SELFCHECK_EXIT=2 REFUSED`),
  第二次改为 `> /tmp/sc.log; echo EXIT=$?` 后真码 **`124`**(`timeout 600` 咬掉)。
  ⇒ **上一轮登记的「开工自检不许带任何 `timeout`」这一条,本轮我又用了两次 `timeout`。**
  第三次(后台 `timeout 1500`)跑到 Lua 检测器腿时被我**主动杀掉**,理由见 §5:它开工时读的树
  已经在我脚下改了一个多小时,**它的读数不可用**。真正算数的那次在 §5,**零 `timeout`、安静树**。
- 读了五组最新报告(batch-desk 00:11Z / hero 22:55Z / strategy 23:37Z / replay-check 22:10Z / director 22:45Z)。
- 上一轮「下次触发 ①」= 判定完结 ≥2,第一格 `lf_rescue`、第二格 `tpcommit`,前提 W67 已收割。
  **W67 已收割**(批测台 00:11Z,四台按需全配对,与 W66 并池深度 5 / 239 局)⇒ 前提成立,本轮做 ①。

## 2. RULING 19 —— 铁律 2(b) 的胜负半边:不改写铁律,登记既有判例

批测台交棒 ② 请裁两问(证据从 222 局抬到 **1115 局、跨三个臂串家族**:
W62..W67,radiant **18** : dire **1097**,少数侧占比 **0.0161**,`1115/1115` 全部 `engine_natural`)。

**⭐⭐⭐ 裁定前先去读了一遍判例,而那一步就是答案。**
`state.json:tpdeathbuy_PROMOTE_20260911` 的 `condition_b` **逐字**写着
`The winrate channel is DEGENERATE in all five, so NO win/loss reading is cited. This clears iron
rule 2(b)'s coarse 'no clear negative' and is NOT positive evidence`;
`liondrainstop_PROMOTE_20260911` 逐字 `winrate DEGENERATE throughout`;两条都点名 §FT.2
「刻意沿用的措辞」⇒ **这条读法至少可以追到 §FT,而昨天的 `stable-v7` 就是按它过的门。**

- **(a) 不改写铁律 2(b)。** 它是 **owner 2026-08-01 立的**;改写它得到的唯一新东西是「字面与执行一致」,
  **那不值一次对 owner 规则的单方面重写**。执行读法登记进 `state.json:winrate_channel_2b_RULING_20260912`
  与 `DECISIONS_NEEDED.md` 第 19 条(随周信),**owner 表态前判例继续有效**。
- **(b) `≥0.20` 不裁,证据不足以裁**,⛔ **既不放宽也不收紧**。放宽等于把一条报警改成恒真的行。
  前置是**成因**,归录像组(批测台交棒 ③)。
- ⚠️ **边界**:判例只说 coarse「无明显负面」可由经济代理量满足,**不说胜负通道无关紧要**。
  1097 局 dire 压倒性获胜、方向与 CLAUDE.md 的 radiant +1.5k 金**相反**、与 armed 什么**无关**
  —— 这是一等线索,可能是本月最大的一条。

📌 *一个组请你把一条规则改写成它已经被执行了九次的样子时,先去读判例 —— 「要不要改」和「已经这么做了没有」是两个问题,而第二个问题是免费的。*

## 3. RULING 20 —— `tpcommit` PROMOTE(第十二条,锚点 `stable-v8`,armed 30 → 29)

### 3.1 三条件(全文 `test_set.md §GZ.2`,机器键 `state.json:tpcommit_PROMOTE_20260912`)

| 条件 | 读数 | 边界(⛔ 不合并进结论) |
|---|---|---|
| (a) | **WORKING `episodes=1360`**,录像组 09-11T19:02Z,语料 **W66**。买法=门自己的子句当零通道;HOT **+5.5pp**(ab)/ **+14.8pp**(ba) vs COLD **+0.2pp** / **+1.8pp**,**两层同号** | `ba` 仅 7 局;两层从不并池。⭐ 语料是 **30-id 家族**,与 (b) **同一个家族** —— 这是本条比 `stable-v7` 那两条更硬的地方 |
| (b) | W66+W67 并池,**深度 5 / 239 局 / 5 粒全计分 / `thin_arm_seeds: []`**;gpm **−11.78**,`comps_better` 2/5,`suggested: hold_or_reject` | 家族级不可归因;胜负半边缺席**不引任何读数**;`sign_flip` 四个指标全 true,**按铁律 4(i-c) 登记不入结论** |
| (c) | owner 亲眼看过的病灶:`tp_audit_20260723`(局 175703/233217),**一帧 5 次 TP 应答、0 次出手,Sven 照样死**。外部依据「传送进防守之后就防守」 | 这条 floor **不新增任何 TP**,只保留 12 秒 DEFEND;自带三条释放(视野空 / HP<40% / `ShouldRetreatLaneBurst`) |

⭐ **(b) 这一格本轮自己算了一遍离散度,没有照抄「跨种子极差 64.5」这个形容词**:
每粒 gpm `−13.28 / −39.12 / +5.53 / −37.43 / +25.42` ⇒ 样本 **SD 27.8**、均值 **SE 12.4**
⇒ **−11.78 ± 12.4**,`|t| = 0.95`。铁律 2(b) 要**粗粒度**「无明显负面」,
一个与零无法分辨的漂移**满足**它。⛔ **这不是正面证据**(措辞刻意沿用 §GU / §FT.2)。

**落地形状**:删 `bots/FunLib/jmz_func.lua` 里 `if not J.IsSoakCandidate( 'tpcommit' ) then return nil end` 一行。
⭐ **非 turbo 逐字节不变是算术不是承诺** —— 它紧挨在 `if not J.IsModeTurbo() then return nil end` **下面**,
非 turbo 分支**永远到不了它**。§DU.6 红线遵守:代码与臂串同一个 commit。

### 3.2 ⭐⭐⭐ 头号:`pullcad` 陷阱的**工具层**变种,本仓库第一次,而且**没有任何东西会举手**

`tools/batch_test/behavioral/tpdying_release.py` 有一个常量 `REQUIRED_PARTNER = 'tpcommit'`,
`--assert-arm` 时**它不在臂串里就 `[fatal]` 拒绝**(写它是为了不把「没 armed」印成一个干净的零)。
**而一个 promoted 的 id 在任何臂串里都不会再出现** ⇒ 这条检查**从今天起会拒绝每一波真波**,
拒绝的话术还是「`tpdying` 是 byte-for-byte inert」——**一句在树上已经不成立的话**。

- **改法不是删检查,是把它写成对「不可达性」而不是对「一个名字」**:`REQUIRED_PARTNER = None`;
  `tpdying` 自己不在串里**仍然拒绝**;`--selfcheck` 的源码钉从**消失的 id** 换到**围住它的那个函数**,
  并**新增一条**钉「那个闸没有回来」。工具 `--selfcheck` **24 PASS / 0 FAIL**。
- 📌 *CLAUDE.md 记的「promote 一条 id 会悄悄掐死任何点名它的 gate」有一层楼上:被冻住的不在 `bots/` 里,
  在**量具**里 —— 而 `check_armed_wiring.py` / `inverse_gate_census.py` / `promote_atoms.py` **三个都只读 `bots/`**。*
  ⚠️ 本轮是**靠一个碰巧引用它的 python 测试**炸出来的,不是靠任何普查。**这是个缺口,不是一次胜利。**

### 3.3 连带的另外三件(没有一件是可选的)

1. **原子 `tp_response_releases_need_commit` 按它自己的 RELEASE CONDITION 退休**
   (逐字 `tpcommit promoted, or either release lifted out of ...`),`direction` 也逐字允许
   (`one-way: promoting tpcommit ... is NOT constrained by this row`)⇒ 促进 `tpcommit` **不是绕过它,是兑现它**。
   ⛔ **不删行**,移进 `promote_atoms.json:retired` 并留下理由。现跑 `promote_atoms.py` **EXIT=0**,`atoms 5`。
2. **变异台 M6 反转。** 原 M6 是「**删掉** tpcommit 闸」,promote 之后**打不进去了** ——
   ⭐ **是那个台子自己的 `ANCHOR MISS` 守卫让它可见的**,否则会安静记一个 `CAUGHT`。
   反转成「**把闸装回去**」(以后会误漂进来的形状),本轮 **control ok / 10 CAUGHT / 0 SURVIVED / 0 ANCHOR MISS**。
   ⚠️ 该台收尾的 `RESTORE:` 行拿**工作树 vs git index** 比,未提交的树上两个文件读 `RESTORE: NO`
   —— **那是我的未提交编辑,不是没还原**(还原走文件副本,逐条复核过,13 个文件的 diff 全在)。
3. **六处断言翻面,一条都没删。** 按「退休一条 id 不许连带退休守着它的断言」:
   四个 Lua 用例 + 一个 python 用例原本断言「不 armed ⇒ 门不响」,**全部改成断言 promote 本身**
   (不 armed ⇒ 门就是 0.85 floor)⇒ **将来任何一次误把闸装回去,它们会红而不是安静地过**(M6 实测抓到)。
   两处「无 floor 的对照」改用**去掉 commitment 戳**买(那才是没应答过 TP 的帧长的样子),原命题一字未改。
   `test_tpdead_release.lua` 的 `[separability] tpdead alone ... changes nothing` **前提随原子消失**,
   换成**更强**的:`tpdead` 只许降不许升,且死队友帧上**必须严格更低**。

## 4. RULING 21 —— `lf_rescue` **HOLD**(不 promote、不 reject、**不出集**),退回协同组盯 GH #96

**上一轮把它排进 promote 第一格,理由是它的 (a) 刚变绿。那个排序是错的,本轮撤回。**

- ⭐⭐⭐ **挡路的从来不是 (a)。** `state.json:lf_rescue_CONDITION_A_RERULED_20260821T2300Z` 的 `verdict`
  **逐字**是 `WORKING (...) + BUGGY (effect)`;`lf_rescue_CONDITION_A_20260819` 的处置逐字是
  `Neither promote nor reject: hand to the strategy stream`。⇒ **(a) 三周前就是 WORKING**,
  09-11 那次是**新语料上的独立复现**,报告自己也把效果侧明写成「只登记不判决」。
- **条件 (c) 不成立**:`J.WillAllySurviveTpWindow` 按 ~4 秒编预算,落点却是**最近一座活塔前 575u**;
  队友被越塔杀恰恰因为**他离塔远** ⇒ 两个条件**负相关**。原始 11 次救援 **1 次**准时、**7 次 45 秒没到**;
  09-11 新语料同方向:armed 97 次归属救援里 **35 次(36%)**队友仍在 15 秒内死亡
  (⚠️ baseline n=18 为 56%,**两腿之差不写成结论**,只引方向)。
- ⛔ **不 reject**:动作本身是标准打法,坏的是落点,而 GH #96 §3 已把病定在
  `J.GetNearbyLocationToTp` **自己**头上 —— 它 9 个调用点里 **7 个是出厂常开的**。
- **接力棒交出去了**(铁律 9 连带规则):`owed_executions.json:lf_rescue_landing_point_fix`,`done_when` 裸读,
  并**逐字带上**「验收帧必须来自两个消费者」这条约束。
  ⭐ **立这行的理由就是本轮这件事本身**:上一轮之所以排错,是因为**没有任何一行在说这条 id 欠的是效果侧修复**
  —— 08-19 的「退回协同组」只活在 `state.json` 的一个键里,而 **`state.json` 不是任何人选下一步时会读的表**。

📌 *一条 id 的 (a) 变绿,不等于这条 id 的路通了 —— 当它的 (a) 三周前就绿着的时候,今天这次变绿**什么都没解锁**。
RULING 15 是「钱紧的时候先确认挡路的是钱」;这一条是它的同构:**排 promote 之前先确认挡路的是 (a)。***

## 5. 铁律 6 的三条腿 + 开工自检(安静树,零 `timeout`)

**推送闸三条腿(分支 push,`PUSH3_EXIT=0`,逐字)**:
```
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)     [luacheck bots game: 0 warnings]
py gate: 96 ran, 0 findings, 0 uncertifiable, 37.2s
lua gate: 341 ran, 0 findings, 0 uncertifiable, 9 known-red, 424.1s
```
⛔ **未用 `RULE6_BYPASS`**。`origin HEAD:main` 那次三条腿同样全绿,被 non-fast-forward 拒
(录像组两条先到)⇒ `git pull --rebase origin main` **REBASE_EXIT=0,零冲突**,复核见 §5.3。

**开工自检(安静树、后台、⛔ 零 `timeout`)`SELFCHECK_EXIT=3`**,逐字:
`legs run : 12` / `FINDINGS (exit 3) : cadence trunk-red(python) trunk-red(lua)` /
**`UNCERTIFIABLE (exit 2): none`** / `NOT RUN (inside a leg): tests/test_selfcheck_lua_leg.py`。

**trunk red 逐条归属(⛔ 不写成「四条同族」,上一轮就是这么错的)**:

| 红 | 归属 | 依据 |
|---|---|---|
| `test_carrier_terms.py` `65 checks, 5 failed` | **既有 trunk 红**,不是本轮 | 逐字点名 `zusult -> zuus` / `liondrainstop -> lion`,与上一轮同数同名 |
| `test_detector_source_constants.py` | **既有 trunk 红**,不是本轮 | 上一轮已登记(`tpdeathbuy` 臂形状) |
| `test_bots_walk_farm_only.py` `8 checks, 1 failed` | ⭐ **英雄组上一轮落的**,不是本轮 | 逐字 `tests/test_cm_w_teamfight_clock.lua :: 'ls ' .. dir .. ' 2>/dev/null'` —— 那个文件是 `0e7ec30 hero: cmtfclock` 带进来的,而上一轮总监报告刚把这条记成「**已经绿了**」⇒ **它是新红不是旧红** |
| `test_stale_waits.py` | **未归属**,本轮没查 | ⛔ 不猜。下轮或所有者组单跑一次读它的 FAIL 行 |
| `test_stayfield2_marginal_domain.lua`(Lua 腿唯一一条) | **既有 trunk 红** | 上一轮登记为 `in_gate: false`,不挡 push |

⛔ **四条 python 红里没有一条属于本轮的改动**;我碰过的 `tests/test_tpdying_release.py` 单跑 **exit 0**,
`tools/batch_test/behavioral/tpdying_release.py --selfcheck` **24 PASS / 0 FAIL**。

### 5.2 ⭐⭐ 一条本轮实测出来的运维事实:**推送闸与开工自检抢 `bots/Customize/soak_side.lua`**

第二次 push(`PUSH2_EXIT=1`)读到两条红:`test_luacheck_gate_soakswitch.py`
(逐字 `CONTROL BROKEN: luacheck is happy with the deliberately broken switch`)与
`test_inverse_gate_census.py` **UNCERTIFIABLE**(逐字 `cannot read bots/Customize/soak_side.lua`)。
**两条都不是我的改动,也不在 trunk 上** —— 那一刻开工自检正在后台跑它的 Lua 腿,
而那条腿反复创建/删除同一个开关文件(GH #365 / #229 的争用)。
⛔ **推送闸打印的那句指路话在这里是错的**:它逐字说
`If the named test is not about your change, the red was ALREADY on trunk` ——
**存在第三种情况:红既不是你的,也不是 trunk 的,是你自己另一个工具的。**
📌 *一条「不是你的就是 trunk 的」的二分法,在两个工具会同时跑的那一刻起就漏了一格,
而漏掉的那一格长得跟 trunk 红一模一样。* ⇒ 处置:**自检跑完再 push**(本轮照做,第三次全绿),
并把这一格写进下轮 backlog(建议:推送闸检测到有并发自检时点名说出来)。

### 5.3 rebase 后的复核(⛔ 不承诺,现跑)

`git pull --rebase origin main` 零冲突,但我**仍然逐键核了一遍** `state.json`:
`mine 550 / origin/main 547`,**`MISSING from mine: []`**、`ADDED by me:` 恰好我那三条、
**`CHANGED existing: []`** ⇒ 零键丢失、零既有键被改。
⚠️ **顺带修掉一个我自己造的隐患**:我第一次写 registry 用了 `json.dump(indent=1)`,
而这四个文件原本是 **2 空格缩进**(`state.json` 也是)⇒ diff 变成 **8255 增 / 8212 删的整文件重排**。
键没丢,但**那种 diff 会跟任何人的并发追加必然冲突**,而它长得像一次正常的提交。
已按原缩进 + 原尾换行约定重写,四个文件的 diff 塌回 **43 / 3 / 53 / 16 行**;
四个 registry 检查器重跑 **全部 exit 0**。
📌 *一个 registry 的格式约定是它的并发协议 —— 用对键、写错缩进,损失不在这一轮,在下一个人的 rebase 上。*

## 6. 成本 / 体系健康

- 💰 **零 AWS**(本轮**一次 AWS 调用都没有**),不作 MTD 新声称。转述批测台 00:11Z **当轮现跑**:
  MTD **`$83.035`**(CE 复核 `83.0348379061` 逐位一致),`operative ceiling $90.00 = min(fence, brake)`
  且其下**无** `DIRECTOR CROSSING:` 行 ⇒ **RULING 15 的退休第二次在下游闸上兑现**;`headroom $3.715`。
  三条线 `$60` / `$90` / `$100` **一字未动**。⛔ 这些数**下轮不得抄作前提**。
- ⚠️ owner 可见:`$50` / `$80` 两条 ACTUAL 告警**均在 ALARM**,`budget filters : none`
  ⇒ 外来支出仍不受本台任何闸约束(`DECISIONS_NEEDED` 第 15 / 18 条,只有 owner 点得动)。
- 🩺 五组(按文件名 UTC)batch-desk 00:11Z / hero 22:55Z / strategy 23:37Z / replay-check 22:10Z /
  director 22:45Z ⇒ **3.5h 内全部有产出,无掉队组**。
- **armed 30 → 29**,离 P4.2 解冻线(≤20)差 **9** 条。**判定完结 2**,P4.2 的 ≥2 **达标**。
  ⛔ 按 id 记 2 是诚实的:`tpcommit` 与 `lf_rescue` 是**两个独立的判断**,不是一个原子的两半
  (与 §GW 那次 `zusult`/`zusboltdom` 记 1 的理由正好相反)。

## 7. 下次触发

1. **判定完结 ≥2 继续**。⛔ **`lf_rescue` 不再算一格**(它现在欠的是 `owed_executions` 那一行,不是判定)。
   下一格从「(a) 已 WORKING 且未判」的 armed id 里选,先跑 `a_evidence_owed.py` 现读,不要抄本报告。
2. ⭐⭐ **补上 §3.2 那个缺口**:三个 promote-time 普查(`check_armed_wiring` / `inverse_gate_census` /
   `promote_atoms`)**都只读 `bots/`**,而 `tools/` 里至少还有别的按 id 名硬编码的前提。
   建议做一条 `tools/` 侧的 id 普查腿并进推送闸;**本轮只发现,没有做**。
3. **两条仍红的 python 腿**(`test_carrier_terms.py` / `test_detector_source_constants.py`,均 `in_gate: false` 不挡 push)。
4. **GH #584 三条测试第三分支 + AGENTS.md 加宽**(**第四轮顺延**)。
5. **批测台交棒 ①(§二 收割义务可以写成不可满足的,GH #759)只有我能裁**;
   `batch-desk.md` 节序(交棒 ① 第二轮未裁);GH #663 / GH #469。
6. `py_gate_budget_premise` + `consequence_polarity_census` + `pullcamp_atom_readmission` +
   五条 `a_evidence_*`(**第八次顺延**)。
7. 存量 GH #523 / patch 缺口 P3 / 账户级预算等 owner(`DECISIONS_NEEDED` 15 / 18 / 19,随 W37 周信)。

## 8. Token 用量

```
TOKENS total_in=31,319,506 out=102,077 turns=168
```
(到统计时刻为止;此后的收尾回合不计入。)
⚠️ **本轮 `total_in` 偏高,归因如实写出来,不是「活多」**:三次开工自检
(两次被我自己的 `timeout` 咬掉、第三次才是算数的那次)+ 三次 push(每次 `lua gate` ~424s)
+ 一次为了等自检而空转的等待窗口。**可省的部分是前两次自检**,那正是 §1 登记的那条纪律。
