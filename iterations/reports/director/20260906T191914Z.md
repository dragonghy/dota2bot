# 总监 2026-09-06T19:19Z — trunk 双红结清;其中一条是 GH #442 关掉的红在测试层原样长了回来

## 一句话

开工自检读到 **trunk RED(python 2 条)**,两条都不是 `bots/` 的问题、都是别的组
本轮落地的产物在**别人的门**上碰响的;两条都结清、trunk 回绿。**值得读的是第一条**:
它是 **GH #442 花钱关掉的那个红,在上面一层原样复活** —— 工具层判「navigation,
永不红」,测试层判「逐位相等,否则红」,**两层对同一个数字给了相反的处置**。

## 做了什么

### 1. `test_chain_member_census.py` — 行号漂移把 trunk 判红(→ 立 GH #574)

```
FAIL every judged row's recorded line matches the finding it names TODAY
  -- [(('bots/ability_item_usage_generic.lua',
        'notallyHero:HasModifier("modifier_juggernaut_healing_ward_heal")',
        '1585a9b8'), 8256, 8341)]
```

归因:strategy 的 `23e80a4`(`urnself` gate)往同一文件那条 chain **上方**插了
~85 行。**`bots/` 里那条 chain 一个字符没动。**

⭐ **这正是 GH #442 的立案形状,而 #442 已经在工具层关掉了它。**
`chain_member_census.py` 的 docstring 逐字写着「when it drifts the tool prints
`LINE NOTE` and stays green」,`build_judged` 那条用例的 docstring 写着
「Navigation, never a red」。工具本轮**确实照做了**:`TOOL_RC=0`,
`FINDINGS 13 (dup 12, parity 1; judged 13, new 0, ambiguous 0)`。
红的是**测试层的另一条主张**(`:307` 的新鲜度棘轮)。

最能说明问题的一点:**那条 check 自己的注释就写着它所违反的契约** ——
注释说「when one drifts the tool prints LINE NOTE and stays green」,
下一行就断言逐位相等。

**本轮只做诚实的那一半**:`8256 → 8341`(今天的真读数),trunk 回绿。
**没有动那条 check** —— 它是 `ef0ea24c` 随文件一起落地、**另一个组刚写下的**主张,
新鲜度棘轮本身有独立动机(导航数字烂掉确实没用),**不该由我按自己的读法单方面翻面**。
三个处置选项(保留 / 降为 NOTE / 自愈)写进 **GH #574** 交回给写它的人。
⚠️ 选项 2 的附注也写进去了:**静默跳过不算修好**,「没有漂移」与「我看见漂移并放行」
必须是两份不同的输出。

**负控做过(不靠"绿了"当结论)**:`8341 → 8340`,该 check **CAUGHT**(exit 1);
改回 `8341` 绿。⇒ 8341 是**工具自己的读数**,不是凑出来的绿。

### 2. `test_bots_walk_farm_only.py` — 未登记的 `io.popen`(按处方结清,不另立 issue)

```
FAIL: a tests/ io.popen command cannot be resolved statically and is not on the
hand-read list -- ["tests/test_lion_drain_immune_target.lua :: 'ls ' .. dir .. '/*.lua 2>/dev/null'"]
```

hero 的 `44329f4` 新落的文件。**这条 check 的设计就是收一次手读费**,处方写在它自己的
报错里。手读了:`:399-400` 是
`for _, dir in ipairs({ 'tests/fixtures', 'tests/frames' })` —— 两个值都是**同文件里的字面量**,
比上面四条姊妹**更窄**(glob 是 `<dir>/*.lua` 而非裸目录),`bots/` 两种读法下都不在枚举里。
⇒ 登记进 `UNRESOLVED_HAND_READ`,附手读日期与行号。**这是门按设计收费,不是缺陷。**

## 顺带确认

- hero `44329f4` 那次「revert the census edits」**是对的**,而且它**没有**碰
  `test_chain_member_census.py`(本轮的红与那次回滚无关,是 strategy 的插行)。
  该 commit message 引 `tests/frames/README.md` 拒绝「为了让套件变绿而快速改别组的裁定」——
  **本轮第 1 条走的是同一条原则**:能诚实更新的数字更新,别人的判据留给别人。

## 铁律 6

- 静态半:`GATE_EXIT=0 CLEAN` / `luacheck bots game: 0 warnings`
  (容器缺 luacheck,gate 自己装的 —— apt 包名 `lua-check`)。
- push 门已上膛:`core.hooksPath=.githooks`(`ARM_RC=0`)。
- python 全量:**106 passed / 1 非 0**,唯一非 0 是 `test_selfcheck_lua_leg.py`
  **`rc=124` —— 那是我 240s 上限打的,是 UNCERTIFIABLE,既不是红也不是绿**
  (GH #548,欠了五轮的第五个读数)。⇒ 本轮**拿掉上限重跑了一次**,读数见下节。
- Lua 半:**本轮 `bots/`+`game/` 一行未动**(改的两个文件是 `tests/*.py` 与
  `tools/agent/*.py`),`test_smoke_load.lua` **`SMOKE_RC=0`**。
  ⛔ **全量 Lua 套件(~100min,GH #124)本轮没跑,我不声称它绿。**

## 开工自检(铁律 10)

⚠️ **开工第一条命令又误用管道(第三十六发)** —— `routine_selfcheck.sh … | tail -60`,
被 §22 守卫当场拒(`REFUSED … nothing checked; this is NOT a pass`)。
重定向重跑拿到真读数。**章程第 0 条写的是 `rc.sh`,我没照做。**
⇒ 与上一轮的结论逐字相同:**这一条是习惯不是门**。本轮不新立措辞
(第 34/35 发已经证明改措辞不解决它),**登记发数**。

自检本身 `EXIT=124`(挂在 `trunk health (fast Lua detectors)` 上,与上一轮同族);
它在挂之前**已经交付了本轮全部价值** —— 两条 trunk 红、锚点 3/3 OK、
promote-atom 3/3 ok、`FROZEN none`(没有 live gate 点名已 promote 的 id)。

## 成本

**零 AWS 增量。** 本轮一次 AWS 调用都没有(总监不花钱)。MTD 仍以上一轮付费 CE
量到的 **$50.80** 为准,**越线部分多数不是本项目** —— 该 owner 动,已在
`DECISIONS_NEEDED #15`,**不新开重复条目**。

## Owner 邮件

**本轮不发。** 本周 W36 那封已发(09-06 07:xxZ),配额用完;本轮两条都是
[harness],自己定完即可,无待决。

## 交棒(下轮)

1. ⭐⭐⭐ **回到 owner P4.2:九个 WORKING 里剩下的八条逐条判**
   —— 已连续两轮被别的工作单元挤掉(上轮 P4.3,本轮 trunk 红)。
   ⛔ **「有 WORKING」不等于「可以 promote」**(measured set ≠ promotable set)。
2. ⭐⭐ **GH #450 按 `gated-fix` 落新 soak candidate** —— **已挂三轮**
   (`owed_executions:roshan_pit_daynight_fix` 每轮替它举手)。
3. ⭐ **GH #574 回到写那条 check 的人手上**,三选一定下来。
4. ⭐ P4.3 后半:铁律/章程条数减法(**从没开工**)。
5. 登记项:82 条 pre-existing MISSING 引用要不要逐条判。

## Token 用量

见文件末尾追加节。
