# 总监 director — 2026-08-20T14:50Z(第二十次触发)

## 0. 一句话

优先级 2(a) **不空**——协同组 13:30Z 把 `[harness] #69`(illusion 污染 fixture 的
`burst`/`died_after`)修好后**把回读范围留给总监定**。本轮:**核验修复(830/830、
luacheck 0)+ 把「回读 85 个 fixture?」裁成一张 ~15 件的有序队列 + 路由给录像组**。
未花 AWS 钱、未做任何 AWS 调用、本轮零 Lua 改动(产出全是 `iterations/` + 一条 GitHub 评论)。

## 1. `[harness] #69` 处置(issue 保持 open 作回读队列工单)

### 1.1 修复核验(本容器复核,不是转述)
协同组的生成器修复 + loader 闸门 + `tests/test_fixture_illusion_ground_truth.lua`(10 例)
在本容器 **830/830、luacheck 0**(容器初始无 Lua 工具链,已自行 apt+luarocks 装)。
断言本体成立并记为本项目**第 11 条「未声明的世界断言」**:`.dem` 战斗日志按裸英雄名记账,
幻象用其复制品的名字 ⇒ `burst`/`recent_damage`/`died_after` 会混进打在幻象身上的伤害。

### 1.2 关键事实:这不是火警
86 个 fixture **只有 1 个**(`f_260820_102030_tide_illusion_burst`)带 `ambiguous` 标志,
其余 **85 个**仍名字口径。但协同组 §4 已证污染**在观测帧上未翻掉任何消费方**(5 个记账英雄里
只有 lion 在 1100 估计半径内)⇒ **没有已下的 promote/reject 结论当前建立在被证伪的证据上**。
定性为**正确性卫生队列**,不发 owner 邮件、不停工。

### 1.3 回读范围裁定:不批量刷 85 个
污染只在「`burst`/`died_after` 真承载结论」× 「窗口内有相关英雄幻象存活」两条同时成立时咬人;
后者不重生成不可知,但前者是**免费的分诊**。总监扫消费方
(`IsIncomingBurstLethal`/`WillAllySurviveTpWindow`/`ShouldRetreatLaneBurst` + `died_after`),
圈成 **~15 件有序队列**(远小于 85),按 §0b/§0c 一次一件:
- **Tier-1(已发布未 gated helper 的证据,最先回读)**:8 件(见 `test_set.md §S`);
- **Tier-2(armed id 的 (a)/(b) 证据)**:`tpdying`(**最优先**)/ `tpdead` / `capmono` / `tpcommit`。

**队列里唯一可能翻动一条 armed-id 判读的项**:`tpdying` 的 (a)。**#68 §1.3 已被 #69 §1 证明
把 tide 帧读成「一秒吃 1063、门正确没释放」——那 849/1063 点其实是幻象吃的** ⇒ 昨天/今早
刚下的 `tpdying` (a)=WORKING 结论**必须在 clean 帧上复核**。其余各件预期是「回读后仍 clean、
结论成立」。

### 1.4 路由
回读需 S3 的 behav-dump timeline,**本容器无 `.dem`/timeline**(只有
`timeline_synthetic.json`)⇒ **执行归录像组**(有 S3/dumper)。协同组 §6 点名的三个名字口径
检测器(`tpdying_landing.py`/`tp_attribution.py`/`watch_deaths.py`)有同一漏洞,一并归录像组。
决定已评论进 #69,记账 `test_set.md §S` + `state.json:fixture_illusion_reread_scope_20260820`。

## 2. 其他章程项

- **promote/reject**:无新判定。`zusult`/`cmrguard` 的 (b) 在 13:00Z 已判(前者不可测、
  后者「不是该发布的版本」),`capmono` 的 (b) 隔离波次仍在批测台排期(§R 追加)。测试集
  **逐字未变**(17 eligible / §R.0 的 16 id 下一波 cand,种子 888/895/896/906)。
- **open issue 扫描**:49 条 open。本轮新增的归总监的只有 #69(已处置);#70([batch]
  成本/围栏,批测台自报,无需总监动代码,见 §3);无新增 `[bug]`。
- **成本**:批测台 14:07Z 自报 **MTD $11.696**(连续七轮的计费滞后本轮追平 08-19 全天;
  **不是泄漏**,开工/收尾 `describe-instances` 均 0 台)。**≪ $90 owner 刹车线**。总监本轮
  未做任何 AWS 调用、未启动计费资源。
- **`[batch] #70` 记一笔排期信息(非刹车事件)**:每波实测 **$1.56**(on-demand 4 台),是
  章程一直用的 $0.6-0.8 估计的约 2 倍;**$45 月度围栏 ETA ≈ 8-25/26**,本月约剩 20 波例行,
  §R 的 `capmono` 两臂占 2 波。围栏是 launch-time 硬检查(先于 $50 Budget / $90 刹车线生效),
  机制正常。**总监据此排期口径改为 $1.6/波、$3.2/两臂波**;若 owner 希望 8 月最后一周仍有
  批测能力,需在 8-25 前决定优先级——**攒进 `DECISIONS_NEEDED.md` 的第一条**(见下)。
- **DECISIONS_NEEDED.md**:本轮**首次创建**,记入围栏 ETA 一条(无时限到 8-25,不单独发邮件)。
- **Routine 健康**:四组本轮均有产出无空转;批测台零支出运维轮是正确节流。
- 今日周四,效率台账(仅周日,08-23 那次做)跳过;patch 已分片(§backlog #0),不重复。
- 本周 owner 邮件仍为零。`wandlimbo` 机会普查连续第九轮无人做。

## 3. 验证

- **luacheck 0 警告 + 830/830**(本容器复核,自装工具链);本轮零 Lua 改动,产出为
  `test_set.md §S` + `state.json` 一键 + `DECISIONS_NEEDED.md` + 一条 GitHub 评论。

## 4. 下次触发

1. **收 16:11Z 那波的 `capmono` (b) 两臂差**(按 §R.3 推论 ②:按种子配对 + 按局归一化,
   不许用 gpm/xpm);
2. **`[harness] #61`**(卡着协同组 #62,已顺延三轮,(a) 若空则**没有借口**);
3. 清待复核队列(`#45` 最老,已第八次顺延);
4. `#66`/`#63` 修订版落地后按 §R.2 两条门槛复判 `cmrguard`(零支出);
5. 都在等就做 backlog #1 帧语料检索工具 / patch 分片 P3 / `[harness] #27`。

## 5. token 用量

```
TOKENS total_in=7,169,233 out=57,236 turns=70
```
(注:统计后的收尾回合不计入,量级参考足够。)
