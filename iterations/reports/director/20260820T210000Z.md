# director 报告 — 2026-08-20T21:00Z(第二十三次触发)

**一句话**:优先级 2(a) 空 ⇒ 走 2(b),做完了从 19:00Z 就排在第一位的 **`capmono` promote/reject 判定**:
**NOT-PROMOTE、不 reject、留在 armed 集**;并且**从源码里量出了为什么两个通道都读不出来** ——
检测域跨过了 `capmono` 自己算术上的 no-op 边界,于是交出一次**零支出**的臂内复读代替再花钱。
**未花 AWS 钱、未做任何 AWS 调用、本轮零 Lua 改动**(产出 = `iterations/` + 一条 GH 评论)。

---

## 0. 本轮输入

| 组 | 最新报告 | 与本轮的关系 |
|---|---|---|
| batch-desk | 20:10Z | **收割 §R.0 arm B**(capmono OFF,292 局)+ **A−B 逐种子配对差**;未上机(例行门槛 22:09:02Z) |
| replay-check | 18:36Z | **`capmono` 隔离两臂 (a) 核验**:SILENT + **检测器经验零点实测 ±15pp/种子**;开 `[strategy] #72` |
| strategy | 19:25Z | pin `DefendThink:1441` 第三腿恒真(post-#61) |
| hero | 20:08Z | Lion Finger 0/1/2/0/0 casts/game,开 `[hero] #73`(两个结构性缺口) |

**优先级 2(a):空。** 51 条 open issue,自 19:00Z 以来新增 **#73(hero)** 与 **#72(strategy)**,
**0 条 `[bug]`/`[harness]` 归总监**(`[bug] #64` 已于 11:00Z 处置并保持 open 作工单,不重复)。
⇒ 走 2(b) promote/reject,正是 19:00Z「下次触发 ①」。

---

## 1. `capmono` 判定:**NOT-PROMOTE,不 reject,留在 armed 集**

三条件:**(c) 成立;(a) 隔离证明未通过;(b) 不可判**。

### 1.1 条件 (a):**FAILED**,且 14:37Z 的记录**正式撤回**

录像组 18:36Z 的隔离两臂(32 局 / 806 clean 域帧,arm A=16id 含 capmono,arm B=15id 去 capmono,
其余逐字相同、同种子同树):

- **DiD = −7.9pp ± 9.3(|t| 0.84)**,逐种子符号 **2/4**(掷硬币);去重后 −4.8pp,符号一致。
- **阴性对照成功**:arm B(capmono OFF)内差 **+0.3pp** ≈ 0。
- **副产品才是本轮最值钱的东西**:arm B 的内差逐种子照样抖 ±18pp ⇒ **该行为检测器的经验零点
  σ≈±15pp/种子、4 种子 MDE ≈ ±21pp**。这是 GH #30(经济通道 σ≈30 gpm)的**行为通道版本**,
  我们此前从未测过。

⇒ **14:37Z 的 `WORKING(方向性、未隔离)` + 16.6pp 落在这条实测零通道之内,从一开始就不构成信号。**
作者自行撤回,总监确认撤回,`test_set.md` 的 (a) 已核标记已删除。

### 1.2 条件 (b):**不可判**(不是「通过」,也不是「有害」)

批测台 20:10Z 的经济 A−B(arm A 280 局 / arm B 292 局,四种子配对):

| 指标 | A−B | SE | z | 同号 |
|---|---|---|---|---|
| gpm | **−5.98** | 10.55 | −0.57 | 2/4 |
| xpm | −0.90 | 6.01 | −0.15 | 1/4 |
| deaths | −0.03 | 0.03 | −1.10 | 3/4 |
| last_hits | −0.21 | 0.20 | −1.05 | 3/4 |

|A−B| ≪ 4 种子 MDE ≈ 35.6 gpm。按 **08-19T12:50Z 立的规矩:null 只是上界,永远不是 (b) 通行证**。
而本该判 (b) 的**行为通道被它自己的作者在同一波里作废** ⇒ **两个通道同时不可判**。

**不 reject**:没有任何证据说它有害 —— 它结构上只降不升,两臂 commit 帧 8s 内死亡率均 ~2%,
armA-armed 的 dedup retreat 结局 18 回血 / 15 平 / **0 死亡**。

---

## 2. 总监本轮的源码发现(**自己读的,不是转述**):**检测域跨过了 `capmono` 自己的 no-op 边界**

读 `bots/mode_team_roam_generic.lua:102-135`:

```lua
if bLaneKill then return desire end            -- L106  上游豁免
if J.IsInLaningPhase() or J.IsPushing(hBot) then
    if J.IsModeTurbo() and J.IsSoakCandidate('capmono') then
        if desire > 0.72 then return 0.72 end  -- armed
        return desire
    end
    if desire > 0.9 then return 0.72 end       -- shipped
end
```

⇒ **`capmono` 的 active set 恰好是 `raw ∈ (0.72, 0.9]`**:`raw>0.9` 两侧**逐位相同**,
`raw≤0.72` 两侧都是恒等。把它和六条被 cap 分支的**真实出价公式**求交:

| 分支 | 出价 | 在 clean 域 HP∈[0.42,0.50] 里 `capmono` 生效当且仅当 |
|---|---|---|
| `laneKill`(l1trade,L287-297)/ `combo`(l5combo,L302-312) | 1.84·HP | **永不** —— 置 `bLaneKillCommit`,**L106 整条豁免 cap**(GH #31 的修复) |
| `helpCoreTargeted`(L232)/ `punish-dive`(ownhalf,L254)/ `overchase`(L268) | **1.96·HP** | **HP < 0.9/1.96 = 0.4592**;高于此**严格 no-op** |
| `helpAlly`(L242) | 1.633·HP | **HP > 0.72/1.633 = 0.4408**;低于此 `raw≤0.72`,无可 clamp |

**⇒ 三分支的主家族在采样 HP 带的上半(≈54% 宽度)是可证的 no-op,却被平均进了每一次读数。
−7.9pp 是一个被稀释的估计,不是干净的 null。**

**顺带更正一条既有口径**:`test_set.md` 早就因为同一理由排除了 HP≥0.55(「那个血段与 shipped 逐位相同」)
—— **真实边界是 ≈0.459,那条排除线定高了约 9pp**,于是历次 capmono 读数(含 14:37Z 的 +16.6pp)
都掺着可证不生效的帧。

**诚实边界**:线 114-118 的源码注释**自己就写了**「effective bid 在 HP 非单调、最大值在 ~46% HP」——
这个事实一直躺在注释里。本轮新增的不是这个事实,而是**把它和检测域的定义求交**,
并发现 clean 域正好骑在边界上。

---

## 3. 下一步:**零支出的臂内 HP 梯度复读**(归录像组)

在**已有的 32 局语料**上把 806 帧按 **HP = 0.4592** 切两段。
**高段是一个臂内 placebo** —— 同臂、同局、同种子、同 `teambrain`,只差 `capmono` 自己的算术
⇒ **不受那 ±15pp 跨种子噪声影响**,而那正是聚合读数卡住的地方。

**§H 事前登记(数字出现之前写下,不许事后改口)**:
1. **主判据**:arm A 内差应**集中在 HP∈[0.42, 0.4592)**;在 HP∈(0.4592, 0.50] 点估计贴近 0 且无特定符号。
2. **阴性对照**:arm B 两段都**不应**有梯度。
3. **必须一起报**两段的 n **和分支构成的不确定性**(dumper 看不见是哪条分支出的价,不许假装看得见)。
4. **事前声明的不对称性**:出现预测方向的梯度 = **支持 (a)**;**梯度缺席 = 弱证据**
   (`helpAlly` 家族预测**相反**的梯度,构成可互相抵消)⇒ **这个检验只能平反,不能进一步定罪。**
   定罪已经是默认状态(不存在隔离的 (a) 证据)。

**否掉录像组 §13.2 的「去 `IsPushing` 腿(t>600 推塔窗)另建观测」作为下一步**:那要新建观测域,
且现有语料 806 帧 **100% 在 t≤600**(极薄)。**先做已经付过钱的那一批**;`IsPushing` 腿留作备选。

---

## 4. 排期(硬约束)

- **`capmono` 留在 §U.0 串里逐字不变**(18 id、种子 888/895/896/906、单臂,批测台 20:10Z 已静态核对):
  (i) 对 §U.0 的申报目的**中性**(`blinkflee`/`liondrainstop` 调用点/英雄/检测器都不相交);
  (ii) §3 是**臂内**检验 ⇒ 每一波 capmono-ON 的语料都在**免费给它加 n**;
  (iii) 在批测台已核对过的串上、为**零测量收益**改动,正是过去「一波两个变量」事故的来源。
- **`capmono` 不得再作为任何付费波次的申报目的。** 它已用掉 08-19T15:00Z 欠它的那次隔离波(≈$3.2),
  而录像组自己证明**瓶颈是检测器分辨率、不是样本量** ⇒ 再加局的期望收益为零。等 §3 的零支出结论。

---

## 5. §0b **第十九例(新亚型)**:**检测域比规则的 active set 宽**

与**第十七例第二型**(检测器谓词比规则**严** → 把规则真作用域挡在观测外)**成对、方向相反**。

**通用形式**:凡是「对 desire 做**纯算术变换**」的 gate,**它的 no-op 集可以从源码闭式解出**;
检测域一旦跨过那条边界,可证不生效的帧就被平均进来 ⇒ **得到的 null 是衰减后的 null,
不构成「无效应」的证据**。

推论(已写进 `test_set.md §V.4`):
1. **算术型 gate 的检测域必须先与 active set 求交**再读数;
2. **补集(可证不生效的子域)必须作为臂内 placebo 一起报** —— 免费,且免疫跨种子噪声;
3. **上游豁免算数**:在变换之前就 early-return 的分支根本不在 active set 里,不管它出价多少
   (这里 `bLaneKillCommit` 把 `l1trade`/`l5combo` 整条摘了出去)。

前十八例是「作者的推理被下游/消费方作废」「测量看不见抑制」「检测器比规则严」「隐式默认成为断言」
—— 这一例补上**「检测器比规则宽」**这一侧,`capmono` 是样板。

---

## 6. 其他章程项

- **测试集**:**逐字未变**(19 eligible;下一波仍是 §U.0 的 18 id + 种子 888/895/896/906)。
  本轮新增 §V(capmono 判定 + 臂内复读 + §0b 第十九例)。
- **成本**:MTD **$11.695767137**(批测台 20:10Z 自报,与 14:07Z/16:08Z/18:06Z **逐位一致**;
  16:09Z arm A + 18:08Z arm B 两波 on-demand 计费滞后未入账,**不是泄漏** —— 开工与收尾两次
  `describe-instances` 均 0 台在跑)。≪ $45 月度围栏 ≪ $90 owner 刹车线。
  **总监本轮 0 次 AWS 调用、未启动任何计费资源。**
- **Routine 体系健康**:四组报告全部在最近 ~2.5 小时内(18:36 / 19:25 / 20:08 / 20:10),
  **无空转**。批测台按「距上一波例行上机 ≥2h」不启动是**正确节流**(门槛 22:09:02Z),
  且把 A−B 的判读权交回总监/录像组而不自行下结论,正是章程要的分工。
- **今日周四**,效率台账(仅周日)跳过,**08-23 那次要做**;patch 11:10Z 已分片定级,不重复。
- **`DECISIONS_NEEDED.md` 今日不新增**:本轮两个决定(NOT-PROMOTE、用零支出复读代替再花钱)
  全在自主授权内。已有的一条(`[batch] #70` 的 8 月围栏,8-25 前 owner 不回信按选项 A 执行)不变。
  **本周 owner 邮件仍为零。**
- **`wandlimbo` 机会普查连续第十轮无人做**(§J.1.4 是它 arm 的硬前置)。
- **待复核队列**:`#44`/`#45`/`#55`/`#58`/`#62`/`#63`/`#65`/`#66`/`#67`,**`#45` 第十次顺延**
  —— 本轮工作单元用在从 19:00Z 就排第一、且有 22:09Z 上机时限背景的 promote/reject 判定上。

---

## 7. 验证(铁律 6)

**本轮零 Lua 改动**(`git status` 改动仅 `iterations/` 下的报告/章程/state.json),按铁律 6 的
立法目的本轮**无适用对象**,未装 Lua 工具链、未跑 `luacheck` / `run_tests`。
上一轮(19:00Z)在本容器复核过当前树:**857/857 + luacheck 0**,树自那以后无 Lua 变更。

源码事实(§2 的三条)是我**自己 grep + 逐行读**出来的,不是转述报告:
`mode_team_roam_generic.lua` 的 L102-135(两条 cap 的分支)、L106 的 `bLaneKill` 上游豁免、
L232/242/254/268/287-297/302-312 六条被 cap 分支的出价公式、L296/311 两处 `bLaneKillCommit = true`。

---

## 8. 下次触发

1. **收 §U.0 那波(≥22:09:02Z)** 的 `blinkflee` / `liondrainstop` 条件 (a)
   —— 注意 §U.1.1 的检测器窗口必须改成 gate 的 **2.0s / HP≥0.70**(不是 5s / >70%),
   否则动机帧被自己筛掉。
2. **收 §V.2 的 capmono 臂内梯度复读**(零支出),按 §H 登记表**逐条对,不许改口**。
3. **清待复核队列**(`#45` 最老,已第十次顺延)。
4. `#66`/`#63` 修订版落地后按 **§R.2** 两条门槛复判 `cmrguard`(零支出)。
5. 都在等就做 **backlog #1 帧语料检索工具** / patch 分片 P3 / `[harness] #27` / `#43` /
   收拾 `GetLaneFrontAmount` / `GetLocationAlongLane` 同族 stub。

---

## 9. Token 用量

```
TOKENS total_in=5,739,919 out=96,501 turns=52
```
(到统计时刻为止,收尾回合不计入。)
