# 总监 2026-09-04T04:15–04:5xZ

**把顺延八轮、本轮到期的那条腿修了 —— 而修完它,它当场点名了一条正欠裁的请求,
本轮第二件事就是裁那一条。零 AWS、零发波、`bots/` 与 `game/` 零改动。**

裁定全文:`iterations/streams/test_set.md §DY`。

**本轮最该被读的一句**:`RIDESHARE` 桶存在的唯一理由是 §BB.4「搭车提议必须在到达那一轮被裁」,
而它的判据 `is_rideshare` **只读 `question`** —— 真实队列 71 行里,**59 行自陈搭车,
其中 23 行(39%)把这句话只写在 `axis` 或 `acceptance` 里**。
那 23 行**从来没有进过这个桶**。`none` 与「队列干净」是同一行字。

---

## 1. 自检(铁律 10)

⚠️ **第三十二发,而且又是本轮第一条命令**:我先写了
`timeout 300 bash tools/agent/routine_selfcheck.sh 2>&1 | tail -60`,
脚本**自拒**:`SELFCHECK_EXIT=2 REFUSED (nothing was checked; this is NOT a pass)`,
并逐字点名「it has recurred 5x, every time as the first command of the round」。
上一轮刚把工作流第 0 步的措辞从「本轮第一条命令走 `rc.sh`」改成「**任何要读退出码的命令**」,
**而这一发说明改措辞没有改掉它** —— 与上一轮自己的结论一致:**这是习惯不是门**。
改成重定向后重跑。

⚠️ **读码陷阱同轮第二次**:重定向那次超时被移到后台,harness 的完成通知报
`exit code 0` —— **那是末尾 `echo` 的码,不是自检的**。真码读脚本自打的第一行:
**`SELFCHECK_EXIT=3`**,以及末行 `selfcheck worst exit: 3`。

| 腿 | 结论 |
|---|---|
| 未落地提交 | OK(窗口内无未落地工作) |
| 引用审计 | 无新 `UNRESOLVED` |
| cadence | **FINDING**:`strategy` 09-03T19:00Z → 22:45Z **3.8h**,越线 0.3h |
| owed executions | **OWED**:`gate_i_executable_first_run`(GH #469,executor=batch-desk) |
| stable 锚点 | `stable-v1` / `stable-v2` 双双 `EXISTS/PINNED/SHIPPED` ok |
| trunk(python) | **86 passed, 0 failed, 2 uncertifiable**;`tests/test_rc_wrapper.py` / `tests/test_selfcheck_lua_leg.py` **NOT RUN**(120s 预算内没跑完 + `lua5.1` 当时不在 PATH)。**这不是通过,也不是 trunk 红** |
| trunk(fast Lua 检测器) | 75 个 tagged 文件 **0 失败**(**快子集,不是全套**) |

按 GH #267 4b **照抄工具自己的归因**,不手写「exit 3 全是 cadence」:
`legs run 9` / `FINDINGS (exit 3): cadence owed-executions` /
`UNCERTIFIABLE (exit 2): trunk-red(python)`。

两条 UNCERTIFIABLE 是**上一轮已定案的腿序问题**(自检 python 腿在第 350 行、
买 `lua5.1` 的 `ensure_lua_tool` 在第 492 行),**非本轮引入**;修它要它自己的承重证明,仍在清单上。

## 2. ① 顺延八轮的 `RIDESHARE` 分类腿:落地(§DY.2–§DY.4)

**到期条款是上一轮自己写的**:章程 2026-09-04T01:00Z 条 ⑤
「`RIDESHARE` 分类腿第八轮顺延 —— **下一轮不做就写进 `DECISIONS_NEEDED`**」。
本轮是那一轮,**选择做而不是登记**。

### 2.1 缺陷,量出来的

真实 `iterations/queue.json`,71 行:

| 量 | 读数 |
|---|---|
| 在自己散文里自陈搭车的行 | **59** |
| 其中把自陈写进 `question` 的(唯一被读的字段) | 36 |
| **只写在 `axis` / `acceptance`,一个字不在 `question`** | **23**(59 的 **39%**) |
| 这 23 行里真的在申请专波的 | **0**(逐条手核,无负式/引用式用法) |

⚠️ **一处自我更正,方向对我不利**:起草时先写成「这个桶从未响过」,**那是错的** ——
对那 36 行它是响的。缺陷的真实形状是 **59 中漏 23**,不是全漏。
登记它是因为**夸大的立案句更容易被后来的人推翻,连带推翻真的那部分**。

⭐ **同一个文件里就有对的那条**:`subjects_of()`(域腿)读的是
`SUBJECT_FIELDS = ("axis","question","acceptance")` —— **同一份三元组**。
这个文件**早就知道**自陈会写在三个字段的任何一个;`is_rideshare` 是那条知识**没传到**的一行。

### 2.2 修法,以及为什么「读所有字段」是错的

`DECLARATION_FIELDS = ("axis","question","acceptance")`,三个字段拼起来再匹配。

**⛔ 没有读 `director` / `result` / `notes`,这是判据不是省事**:那里的「零 EC2」是
**别人写的关于这条请求的句子**(批测台的成本核查、总监的裁词),而且通常写在**它被排期之后**。
读了它们,一条申请专波的请求会因为批测台自己的记账变成搭车件。
**这个过度修正长得和修复一模一样。**

**扩宽的正当性是失效方向**:桶的语义是「本轮必裁」——**过宽**多看一眼,
**过窄**让这条义务失去全部可见性。与 GH #317 给 `is_open` 的论证逐字同构。

### 2.3 承重是变异台

`tools/agent/mutstand_rideshare_field.sh`(新建,从**文件副本**还原 + 每只后 `sha256sum -c`
+ `trap on_exit EXIT` 先还原再删副本,GH #418/#468 的形状)。
裸读 **`RC_EXIT=0`**,**`mutants: 4/4 CAUGHT`**,M0 基线 `exit=0`:

| 变异 | 内容 | 结果 |
|---|---|---|
| M1 | 退回修前形态(只读 `question`) | CAUGHT |
| **M2** ⭐ | **读 `req` 所有字段**(含 `director`/`result`) | **CAUGHT** |
| M3 | `any` → `all`(要求七个 marker 全中) | CAUGHT |
| M4 | 加了 `axis` **忘了 `acceptance`**(半落地) | CAUGHT |

⭐ **M2 是这台子存在的理由**:只有 M1/M4 的台子会被 `is_rideshare = lambda req: True` 通过
—— 那会把 `OTHER` 桶整个清空并自称修好。M3 同理,`all` 读起来「更严格」却让桶重新恒为 `none`,
**从另一侧回到同一个静默**。

**新断言里有一条不按字段列表写**(不变量 1):扫**申请方写的每一个字符串值**
(只排除上面那三个字段),断言 `OTHER` 桶里不许有自陈搭车的行。
理由是**棘轮的形状** —— 按 `DECLARATION_FIELDS` 写只能锁住这次挑中的三个字段;
这一条**在某个流发明第四个散文字段并往里写「搭车」的那天就红**。

**读数**:`tests/test_pending_rulings.py` **148 checks, 0 failed**,裸读 `RC_EXIT=0`
(修前 129 检查;新增 19 条)。

### 2.4 修完之后,工具自己的读数变了

- 修前:`RIDESHARE … none` / `OTHER … 1`(`hero-28`),裸读 **`RC_EXIT=0`**
- 修后:`RIDESHARE … 1`(`hero-28`)/ `OTHER … none`,裸读 **`RC_EXIT=3`**
- 裁完 `hero-28` 之后:两桶皆 `none`,裸读 **`RC_EXIT=0`**

⇒ **§BB.4 的「本轮必裁」第一次对一条 `axis` 自陈的请求响了,而它响的当轮就被执行了。**

## 3. ② `hero-28` ROUTED_ARCHIVE_SCAN(§DY.5)

**它是修完腿之后唯一进桶的行 ⇒ 本裁定是那次修复的直接后果,不是另找的活。**

沿 §BF.2 / §AZ.5 统一路由,**与 `hero-27` 合成同一次遍历**(申请书自己提的,采纳),
`executor` = **`batch-desk`**(§CH:恰好一个流)。零 EC2 / 零新局 / 零 AWS 增量 / 不申请 arm
⇒ **卡住它的既不是名额也不是钱,只是一个路由裁定**,当场放行。

**总监独立核过的两条**(核的是**买不买得着**,不是机制对不对):

1. `grep -rl aether tests/fixtures/` 裸读 **0**(目录 111 项)⇒ 申请书「条件 (a) **结构上**
   买不到第二份 fixture」**成立**。这是它值一次语料、而不是值一份 fixture 的理由。
2. 域是活的:`sRoleItemsBuyList` 在 pos_4(`:199`)/ pos_5(`:216`)**都买** `item_aether_lens`;
   闸 `AetherReach()`(`hero_zuus.lua:381-390`)单条独立 gate、turbo 合取在场、
   off 路径 `return 0` = 出厂表达式,**且注释点名回避了 `pullcad` 陷阱**。

**⛔ 边界(照抄 §BF.2)**:这是**路由裁定,不是对请求前提的背书**。
总监**没有**复核机制论证(高估/低估代价那段),**没有**跑
`tests/test_zeus_aether_cast_range.lua` 或它的变异台。

**⭐ 一条执行方不许照抄的数**:同一轮两处普查对不上 —— 申请书写
「33 声明,**26** 接了、7 不读,Zeus 是第 8 个」(26+7=33,而 Zeus 又是第 8 个 ⇒ 34);
`hero_zuus.lua:349-351` 写「33 里 **22** 个接进至少一处 cast-range 读,Zeus 是**读它 nowhere 的 8 个之一**」。
分子口径不同(「接了」vs「接进 cast-range 读」)**而两处都没说明**。
⇒ 引用前必须重算并说明口径;**本裁定不依赖其中任何一个数**。

**⚠️ `status` 没动,留在 `pending`**:裁定投递在 `director` 字段(工具读的那一格),
执行方没开工之前写 `running` 是一句假话。`hero-14`/`hero-17` 那次的病是 **`executor` 空**,
不是 status —— 本条 `executor` 已填。

## 4. ⚠️ 同一个形状,同一天,第二条腿(§DY.6,本轮不裁)

批测台 03:15Z §4.1 交上来的 GH #473 甲:**载体门看不见 `rotscope`** ——
域-载体从**文件路径**(`bots/BotLib/hero_*.lua`)读,而 `rotscope` 改 generic 文件、
域却是一个具体英雄 ⇒ 连续八波恒零而**没有任何门举手**。

⭐ **这与本轮修的是同一句话的两个实例**:`subjects_of()` 的
`HERO_FILE_IN_PROSE` 扫的正是那三个散文字段 —— 本轮修的是**字段选错**,
那一条是**同一个函数里模式选错**;**两条腿的沉默都长得像好消息**。
**本轮不裁**(需要自己的普查 + 变异台,是一个完整单元),列为下一单元第一项。

## 5. owed / 成本 / 巡检

- **`owed` 行不退休,而且是裸读判的**:`gate_i_executable_first_run` 的 trigger 是
  **下一次发波轮**;批测台 03:15Z 是**收割轮**(W44 harvest,自报零发波),
  ⇒ **该棒未到期**,不动。上一轮交棒 ① 说「要裸读波次记录,`kind: manual` 判不了它」——
  本轮读的是批测台自己的发波自述,**结论是"未到期"不是"已完成"**。
- **成本**:本轮**零 AWS 调用、零发波、零计费资源**。MTD **照抄**批测台 00:15Z 自报
  **$12.519**、围栏 **$15.57** ≤ $80(03:15Z 报告自陈本轮零新增支出,无更新的自报可抄,**不写「顺延」**)。
  九月未到 $50 档,刹车线/批准线未触及未动。
- **巡检**:四组本轮均有产出。`cadence` 单条 3.8h(strategy,越线 0.3h),**单发不升级**。
  `UNKNOWN STATUS` 仍 20 行(`OPEN_STATES` 不认识 `admitted`/`harvested_pending_verification`)——
  **失效方向安全**(GH #317 让未知状态计为 open 并点名),本轮不改工具,仍在清单。
- **owner 沟通**:本周邮件 **08-31 已发** ⇒ 本轮无邮件。`DECISIONS_NEEDED.md` **无新增**
  (本轮两个决定都在自主授权内;⑤ 的到期条款**用"做掉"满足,不用登记满足**)。
  今日周四,效率台账(仅周日)跳过。

## 6. 铁律 6

- **`bots/` + `game/` 零 diff** ⇒ **全量 Lua 套件未跑,也不声称**(GH #124)。
- **静态门**:`bash tools/agent/luacheck_gate.sh` 裸读 **`GATE_EXIT=0 CLEAN` / 0 警告**
  (容器初始无 luacheck,门自己 apt 装的 `lua-check`)。**未用 `RULE6_BYPASS`**。
- `arm_push_gate.sh` exit 0(`core.hooksPath=.githooks`)。
- **python 全套**读数见 §8 收尾追记。
- **引用门(GH #290)**:见 §8 —— 本轮**没有发表任何带引用的 GitHub 评论**,
  所有引用只落在本仓文件里,且 `test_set.md §DY` 与本报告**同一次 push**。

## 7. 下次触发

1. **GH #473 甲 = §DY.6 的载体门**(与本轮同族;需要自己的普查 + 变异台)
2. **GH #473 乙**:按需机时常数 0.745 h/台 落在 W44 下界 0.765 之外,**且它身上没有自动失效条款**
3. **自检腿序**(`ensure_lua_tool` 提前 —— 能把两条 `UNCERTIFIABLE` 变成真读数,实测装 4s)
4. `OPEN_STATES` 学会 `admitted`
5. GH #449(57-id 家族 8 粒并池合法性)/ GH #460 四件(等下一个 spot 波)
6. GH #410 / #436 / #285 / **patch 缺口 P3** / `ckpush` 的 filler-英雄政策(有时限)

## 8. 收尾追记

(push 与 token 读数见下方追记节。)
