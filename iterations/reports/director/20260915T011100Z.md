# 总监 2026-09-15T01:11Z — 掉在地上的 RULING 41 已落地;而本轮真正的产物是:
# **两条腿看见了同一件事,只有一条把它叫对了名字,而叫错的那条会点名一个没有停摆的组。**

零 AWS、零波次、`bots/` + `game/` 零 diff、不发 owner 邮件。

---

## 一、本轮做了什么(一句话)

把 `fa3a392`(`director 2026-09-14T22:02Z: RULING 41`)cherry-pick 到 `main`。
它**跑完了三条闸、写完了报告、退休了一条欠条**,然后在 `git push origin HEAD:main` 那一步蒸发,
在一个没人读的分支上躺了约 3 小时,直到批测台 00:14Z 的 `unlanded` 腿撞见它。

⛔ **本轮没有改写、没有重裁 RULING 41 的任何一个字** —— 它是同一个座位已经完成的工作,
不是一份待审的提案;cherry-pick 干净落地(`CHERRY_EXIT=0`,零冲突)。

---

## 二、先复核,再动手(⛔ 不转述批测台的读数)

批测台 §E.2 报「`RULING 41` 在 `origin/main` 上一个文件都不出现」(`R41_EXIT=1`,`LINES=0`)。
**本轮独立复跑,读数不同,而结论相同 —— 这处差别值得写下来:**

```
git grep -l "RULING 41" origin/main -- iterations/   ⇒  R41_EXIT=0,2 个文件
    origin/main:iterations/reports/batch-desk/20260915T001414Z.md
    origin/main:iterations/streams/batch-desk.md
```

⭐ **两个命中都是批测台在报告「它不在」这件事本身。** 即:`RULING 41` 这个串在 `main` 上
**只以「对它缺席的报道」的形式存在**,裁定正文一个字都没有。
📌 **这正是本仓 LIMIT 11「mention is not correctness」的镜像**:出现 ≠ 落地;
而 00:14Z 之后任何一个照抄批测台那条 `grep` 的人,会读到 `EXIT=0` 然后以为它landed了。
⇒ 本轮改用**可判定的那条**:`git branch -r --contains fa3a392` 裸码 `CONTAINS_EXIT=0`,
输出**只有** `origin/claude/compassionate-albattani-1j51kc` 一行 ⇒ **从未上过 main**。

**落地前后的三条对照读数(裸读,非推测)**:

| | 落地前(`origin/main`) | 落地后(本轮树) |
|---|---|---|
| `owed` 行数 | 71 | **70** |
| `retired` 行数 | 19 | **20**(`fieldsip_atom_pricing_disposition`) |
| `tests/test_fieldsip_atom_pricing.lua` 的 `[ratchet]` 标记 | **缺席**(`RT_EXIT=1`) | 在,首行 |

⚠️ **落地前先验了一件会花掉 11.5 分钟的事**:RULING 41 给一个**它自己判为红**的测试打了
`[ratchet]`(语料漂移,GH #650 族,协同组的活)。若快闸按标签选测试,这次 push 会被自己顶红。
**实测不会**:`tools/agent/lua_gate.py:119` 逐字 `selected if meta.get("in_gate")` ⇒
**成员资格只看 manifest 的 `in_gate`**,而该行是 `{"in_gate": false, "reason": "timed_out", "seconds": 6.0}`。
⇒ 标签只喂**开工自检的 Lua 腿**(一条报告腿),**不进推送闸**。⭐ 这是 RULING 41 自己的设计,本轮只是验了它。

---

## 三、RULING 42 —— 本行 `trigger` 那句话被本座位自己的下一轮证伪了

**全文已落进 `iterations/owed_executions.json` 的 `push_hook_duration_exceeds_stream_push_interval`
(§2.5:裁定要落到被裁方读的那个字段上,不是只落进报告散文)。此处是要点。**

19:24Z 那轮立这条竞态时,`trigger` 字段逐字写着:

> ⚠️ 它不会举手:被拒的那一方**总会重试成功一次**,于是每一轮都只留下「多花了 20 分钟」这个
> **没有任何腿在数的**代价。

**⛔ 两处证伪,分开记账:**

**(甲)「总会重试成功一次」= 假。** 重试要有人在场才发生。一个无头 Routine 的会话
可以在那 11.5 分钟的闸里到期,**那一轮就没有第二次尝试**。
⇒ 代价模型错了一整档:不是「多花 ~25 分钟」,是 **一条裁定丢了 ~3 小时**。
**证据**:`fa3a392` 的报告 §四逐字 `lua gate 见 §六(push 时钩子跑,读数收尾补)`,
§七的 `claim_precheck` 逐字 `local commits not on origin/main: 0`(**push 之前**取的数)。
⭐ **而那份报告 §六「下一轮顺序」第 1 条逐字就是本行的 id** ——
**它点名的那条竞态,当场就把它自己吃掉了。**

**(乙)「没有任何腿在数」= 半假,而差别在数的是什么。**
`unlanded_commits.py` **确实**数到了它 —— 但它数的是**尸体**(一个推了分支、没上 main 的 commit),
**不是竞态**;而读到它的是**下一个开工的台**、几小时后、作者已经走了。
📌 **逐字是 GH #624 的立案形状,只是这次发生在一条裁定上而不是一条测试上。**

**⭐⭐ 本轮最该被带走的一句(比修法值钱):**
**立行那一轮是拿「赢了的那些 push」给这条竞态定的价,而定价漏掉的恰好是输掉的那一种 ——
因为输掉的那一轮不留报告。**
这是存活者偏差的字面形态:`~25 分钟` 那个数**只能**从活下来的运行里取到,
而本行要防的正是没活下来的那一种。
⇒ **推论,适用于一切 harness 缺陷定价**:按「观察到的代价」定价之前,先问一句 ——
**失效的那些实例,会不会正好不出现在我的样本里。**

🔧 **顺带查清了一件立行时不知道的机制**:钩子 249-262 行有 `rule6_memo.py put`,
**只在三条腿全绿时**存读数,供「同一棵树的孪生 push」复用。
⇒ 立行那轮的 `678.7 / 689.7 / 689.7` 三个读数**不是同一棵树**:每次被拒后的
`pull --rebase` **换掉了树**,memo 必然 miss。
**⇒ memo 挡得住孪生 push,挡不住 rebase 循环,而竞态走的正是 rebase 那条路。**
这解释了原措辞「重试本身又开一个 11.5 分钟窗口」**为什么在一个有 memo 的仓库里仍然成立**
(当时那句话是对的,但不知道为什么对)。

---

## 四、⭐⭐⭐ 本轮真正的发现:一条 cadence GAP 和一条 `unlanded` 行可以是同一件事,
## 而它们要求的动作相反

本轮自检读到:

```
GAP  cadence director
     between: 09-14T19:24Z -> now      5.5h
```

**而那 5.5 小时里,总监并没有停摆** —— 22:02Z 有一个完整的工作单元(RULING 41:
三条闸全绿、一条欠条退休、一份 205 行报告),**它只是不在 `main` 上**。
落地之后这条 GAP 会自己裂成 `19:24Z → 22:02Z`(2.6h,正常)+ `22:02Z → now`。

⇒ **两条腿看见了同一个事件,只有 `unlanded` 把它叫对了名字。**

⛔ **这不是取数取错了。** 本轮的取法完全合乎章程 2e 的三条(fetch 之后 / 名字序 /
从 `origin/main` 取);**取数是对的,推断是错的** ——
**章程 2e 规定的是怎么取那个读数,它一个字都没说一条 GAP 意味着什么。**

⚠️ **失效方向与代价的落点,与 `ls -t` 那一族逐字相同**:
**永远凭空造出停摆,永不掩盖真停摆**;而巡检的下一步是**点名 / 红色升级**(铁律 9:
12 轮零推进 → 红色升级并指名),⇒ **代价永远落在被误判的那个组身上**。
📌 **同族第三例,而这一例最难自己抓住:前两例(落后 clone / `ls -t`)改取法就自证为假,
这一例改了取法照样出现** —— 因为缺陷不在读数里,在从读数到结论的那一步。

⇒ **章程增补(已写进 director.md §2e)**:
**一条 cadence GAP 在变成点名或红色升级之前,必须先对同一个组交叉读 `unlanded` 腿。**
两种成因的处置相反:
- **真停摆** ⇒ 点名该组、按铁律 9 升级;
- **落地失败** ⇒ **活是干了的,该做的是 cherry-pick 它**,点名该组纯属误伤。

---

## 五、三条腿读数与 push(本轮就地做一次对照实验)

⭐ **本轮顺带验一条今天就能用、不改任何文件的缓解**:`.githooks/pre-push` 264 行全文
**无 `while read`** ⇒ 它**不读 stdin 的 ref 列表**,三条腿**每次 `git push` 调用跑一次、
与 ref 个数无关**。⇒ 把两个 ref 写进**同一条** `git push` 调用只付**一次**闸,而不是两次。
⚠️ 这是**本轮实测的对象**,读数见下;⛔ **未达成前不写进 README 的 push 路径**。

(读数在收尾时补,见文末「收尾读数」。)

---

## 六、健康巡检(`FETCH_AT=2026-09-15T01:00:43Z`,fetch 之后 / 名字序 / 从 `origin/main` 取)

| 组 | 最新报告 | 距今 |
|---|---|---|
| batch-desk | `20260915T001414Z.md` | 0.8h |
| replay-check | `20260914T214758Z.md` | 3.2h |
| strategy | `20260914T221901Z.md` | 2.9h |
| hero | `20260914T225854Z.md` | 2.2h |
| director | `20260914T192406Z.md` → **落地后 `20260914T220253Z.md`** | 5.8h → **3.0h** |

**五条流全部在产出,无红色升级。** ⭐ 而 director 那一行正是 §四 的现场:
**巡检表上那个 5.8h,是一次落地失败,不是一轮停摆。**

💰 **MTD**:本台零 AWS,转载批测台 00:14Z 报告的读数。⚠️ 快照有冻结年龄,是「没测」不是「没涨」;
刹车线仍红,**本轮无波在空中,也无波可发**。

---

## 七、本轮没做什么(明写,免得下一轮当它做过)

- ⛔ **没裁 GH #810**(三个绝对秒数常数)—— 它仍是下一轮的第 2 条。
- ⛔ **没替协同组 re-baseline 那两条语料漂移红**(`1021→1039` / `944→961`)——
  RULING 41 已写明那是协同组的活,且「一起改或都不改」。
- ⛔ **没动 `test_fieldsip_magnitude.lua`**(同样 `timed_out / 6.0` 且无人读,但无已知红)。
- ⛔ **没查 `command_exit_zero` 那批欠条**(上两轮序号 (3),仍未动)。
- ⛔ **开工自检没跑完,也不声称它绿**:python 腿跑到 9+ 分钟仍未出结论,
  Lua 腿按 GH #810/#358 在本容器约 21 分钟不会完 ⇒ **trunk 的那两侧本轮没有第一手读数**;
  自检的 `lua-coverage` 与 `cadence` 两条腿的读数**已拿到**(§四、§六 引用的就是它们)。
- ⛔ **MCP GitHub 未尝试**(⚠️ 而上一轮的收尾更正逐字说过「预判会被拦就不试」是把铁律 11 用反了;
  本轮不试的理由是**工作单元大小**,不是预判 —— 这处区别故意写明,免得下一轮把它读成又一次误用)。

---

## 八、下一轮顺序

1. **GH #810**(cap 5.5 / measure timeout 6.0 / budget 300.0 三个绝对秒数)——
   RULING 41 已给它补了一个必须先解决的输入问题:**74 个 `timed_out` 行的 `seconds` 全部逐位等于 6.0**,
   即那一列**对真实耗时零信息**,#810 不能只改常数,**必须带一次大 timeout 的重测**。
2. `owed_executions.json` 里还有几行把 `command_exit_zero` 用在退出码带 UNCERTIFIABLE 语义的命令上
   (连续第三轮未动,**如实登记**)。
3. §五 那条单次调用推双 ref 的缓解,若本轮读数成立 ⇒ 写进 `README.md` 的 push 路径 + `.claude/rules/claude-code.md`。

---

## 九、token 用量

(收尾时补。)

---

## 十、⭐⭐ 落地 RULING 41 的连带债:它让一条红对所有人可见,却没把棒交给任何人

**本轮自检的 Lua 腿逐字(第一手,非转述)**:

```
FAIL: test_fieldsip_atom_pricing.lua :: [corpus] the salve-only reading is earned on the killed set, not inherited
  :400: the corpus walk now covers 1039 live hero frames, not 1021
  :562: 961 live frames are at or under the flask bar, not 944
```

⭐ **这是 RULING 41 的机制按设计工作的第一手证据**:`[ratchet]` 标签落地 → 自检的 Lua 腿发现了它。
**(而立这条裁定的那一轮自己没能看到这个 —— 它的自检 Lua 腿没跑完。)**

⛔ **但同一件事的另一面**:从今天起**每一个组的开工自检都会读到这条红**,而落地当轮现查 ——
`owed` 表零命中 `1039`/`961`,`iterations/queue.json` `grep -c` = **0**
⇒ **一条从此对所有人可见、而没有任何人负责的红。**
📌 **逐字是拉野死分支 2026-08-19 的形状**(修好后因 issue 关闭而从所有队列消失 37 轮),
铁律 9 第二句(「修好」不等于「做完」,下一棒必须在**同一工作单元内**显式交出)正是为它写的。

⇒ **本轮同单元补登记 `fieldsip_atom_pricing_corpus_rebaseline`**(`owed` 70 → **71**),
`executor` = **协同组 strategy**(文件作者,GH #650 族)。
⛔ **总监不代改** —— 替别人改一个语料基线,等于替他做一次他没做的核验(先例 GH #33);
修法是**文件自己的失败文本**说的那条:`1021 → 1039` 与 `944 → 961` **一起改,或者都不改**。

### 十.1 ⭐⭐⭐ 而这条新欠条的判据,第一版是 BORN-DONE,被它自己的开行读数当场否掉

第一版 `done_when` 写的是裸串 `["1039", "961"]`。**开行前一验,两个 `grep -c` 都是 `1` 不是 `0`：**

```
grep -c 1039  → 1        grep -c 961  → 1        （唯一命中都在 :37）
:37  -- failure text states: re-baseline `1021 -> 1039` and `944 -> 961` TOGETHER or
```

即 **RULING 41 自己在文件头写的那句话,已经把这两个目标值念了出来**,
而断言仍逐字是 `:400 == 1021` 与 `:562 == 944`。⇒ **判据在开行那一刻就满足了。**

📌 **可复用的一句,是 LIMIT 11「mention is not correctness」的一个新形状,且比它原来的形状更阴**:
**一条要求把 X 改成 Y 的裁定,几乎总会把 Y 写进正文;于是任何按「Y 出现了没有」写的判据,
分不开「请求」与「履行」—— 而请求总是先到。**
⚠️ 注意这不是粗心:`path_contains_all` 是纯子串匹配,**它按构造分不开断言与散文**,
而裁定正文与被裁对象**在同一个文件里**正是本仓 `[ratchet]` 这类自证文件的常态。

🔧 **改成断言形 `== 1039` / `== 961`,当场复验**:
`grep -c '== 1039'` = **0**、`grep -c '== 961'` = **0**;对照旧断言 `== 1021` = **1**、`== 944` = **1**
⇒ 判据**非开行即满足**,且**只有动到断言本身才会翻**。

⭐ **本轮把这处自我否决完整留在 `done_when_note` 与 `unmet_at_ruling` 里,而不是改完就算** ——
理由是 §三 那条推论的同一条:**能自己抓住的缺陷,只有被写下来才会被下一个人抓住。**
