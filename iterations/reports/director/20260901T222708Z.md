# 总监 2026-09-01T22:27Z —— 三条裁定,一个形状:载体买不到时,三条通道全都读成「没事」

**本轮零 AWS 调用、零 S3 访问、零 EC2、零 CE 请求;`bots/` 与 `game/` 逐字节零 diff。**

---

## 0. 开工自检(铁律 10)

第一条命令**又是 `| tail`**,§22 守卫第十次上场并第十次拦下
(`REFUSED: stdout is a pipe; exit 2, nothing checked`)。改重定向后干净那次裸读:

```
legs run              : 8
FINDINGS (exit 3)     : cadence trunk-red(python) trunk-red(lua)
UNCERTIFIABLE (exit 2): none
selfcheck worst exit  : 3
```

- `ORPHAN_PROPOSAL: none`(第六轮);`stable_anchors` 两个锚点三条不变量全 ok。
- cadence 一个 5.0h 洞(strategy,13:55Z→18:55Z)**不升级** —— 该组随后 18:55/19:21 两份报告都在。
- `OTHER: 1` = `strategy-33`,**本轮已裁**(§1)。
- 两条 trunk 红本轮**一条修好一条留给归属方**,见 §3、§5。

---

## 1. 裁定一:`strategy-33` / `hpbool`(GH #397,提议 §DG)—— **HOLD-DOMAIN-EMPTY**

**不入集、不 reject、不花波次槽。** gated 代码留在树上,门关着,出厂行为字节级不变。

条件 (b)(c) 按提议方所写成立。条件 (a) **买不到**,而这个读数是**总监自己取的**,不是照抄提议:

```
python3 tools/agent/corpus_hero_census.py --file bots/BotLib/hero_tiny.lua
  tiny  DOMAIN-EMPTY  files=0  games=0     # 108 份帧文件 / 43 个英雄
  exit 3
```

armed 它只会买到一次 `DOMAIN-EMPTY` 收割,而按 §CJ 那**不得**读成「无效应」——
那正是一条杠杆无声死掉的方式。裁定全文档案 **test_set.md §DH.1**,
已按章程 §2.5 投递到 **`queue.json:strategy-33.director`** 的**叶子键**(`ruling`/`note` 均回读断言过)。

> 上一轮的自伤是**六个键写成了行的顶层键**,JSON 合法、棘轮照绿。本轮补丁写完
> **断言到 `r['director']['ruling']`,并额外断言 `'ruling' not in r`** —— 落错层级这次会被抓住。

---

## 2. 裁定二:GH #402 载体门**恒真通过**,**甲案已修**(批测台 21:14Z 交办)

`carrier_terms.py` 的 `hero_of()` 只认 `bots/BotLib/hero_<x>.lua` 的路径形状;
`immguard` 的门在 `bots/FunLib/minion_lib/primal_split.lua:128`,不匹配 ⇒ 走可达性反查 ⇒
minion 文件全都经通用派发器 `aba_minion.lua` 被 require ⇒ 答案是**全表 128 个英雄** ⇒
`seed_draft.py` 按**一条析取**求值 ⇒ 任意十人阵容必中 ⇒ `verdict=FULL satisfied=4/4`。
**失效方向是危险那侧**:不是拒发(拒发会举手),是**凭空满足**。

**修法取甲,并加一条约束:映射必须由文件自己举证。**
`primal_split.lua` 有 `npc_dota_brewmaster_earth/fire/storm/void`;`familiars.lua` 有
`visage_summon_familiars`;`vengeful_spirit.lua` 有 `vengefulspirit_nether_swap`。
**`jugg.lua` 故意不映射** —— `X.HealingWardThink` 是谁的,那是**读者的 Dota 知识**,
文件里既无单位名也无技能名;而「按文件名认英雄」正是 `rubick_hero/` 那条特例当年立起来警告的读法。
未映射的 summon 文件一律 `unresolved`(响,退出码 2),不再继承全表答案。

**⭐ 中途改过一次,值得记**:第一版把未映射 minion 文件**一刀切**判 `unresolved`,于是
`illumove`/`illureal` **两条正确的 generic id 变成 unresolved**,会让发波门以 exit 2 拒发。
幻象是构造上通用的(任何英雄买玛尔斯之障都有幻象)⇒ 加 `MINION_GENERIC`。
**响只有在同时为真的时候才比静默好** —— 差一点就把一个乐观的错答案换成一个响亮的错答案。

**验收(#402 自己写的判据,逐条对上,裸读)**:

| 判据 | 修前 | 修后 |
|---|---|---|
| `immguard` 那行 | `verdict=FULL satisfied=4/4` | `term=brewmaster verdict=UNDRAFTABLE`(与同轮 `tormself` **逐字一致**) |
| 派生 term 数 | **128** | **8**(单位数) |
| `unresolved` | 0 | 0(中途曾为 2,已修) |
| `tests/test_carrier_terms.py` | 40 checks **1 failed** | **52 checks 0 failed** |

**乙案(`seed_draft.py` 侧 `OVER-BROAD` 闸)未做,总监下轮认领,#402 保持 open 当接力棒。**
理由是算术不是偏好:甲修对了这一例,乙才挡得住下一例 —— 同族的 `rotscope`(朝**另一侧**失效,
**漏报**载体)**已登记且未修**。

---

## 3. 裁定三:`tormself` + `immguard` **出集**(54 → 52),退回协同组,**不是 reject**

条件 (a) 在**当前 47 人 `hero_pool.txt`** 下**结构上买不到**:`ringmaster`/`brewmaster` 都不在池里。

**不出集的代价不是零**:修好载体门之后,机械发波门会因这两条的 `UNDRAFTABLE` **永远读 exit 1**,
批测台得每一波用手写门覆盖它。**一道永远说不的门和一道永远说是的门一样没用,而且更贵**
(这句是 `carrier_terms.py` 自己的注释)。裸读:

```
52-id 串:10 hero-scoped / 42 generic / 0 unresolved / 6 terms
CARRIER_GATE ids=10 seeds=4 exit=0
check_armed_wiring.py --cand <52-id> --ref HEAD  ⇒  WIRING_EXIT=0  all 52 armed ids wired
```

且那 6 个 term(`crystal_maiden,lion,obsidian_destroyer,skeleton_king,spirit_breaker,zuus`)
**与批测台手写六项门给出同一个集合** —— GH #276 想要的收敛,本轮才第一次真的发生。

**⛔ 明确否掉 #402 丙案第一臂:不把 brewmaster/ringmaster 加进 `hero_pool.txt`。**
池子是 W1–W36 全部读数共用的**测量基底**。这个取舍不是我一个人拍的,已写进
`iterations/DECISIONS_NEEDED.md`(方向题:焦点五之外的英雄修复,是「可以做但暂时验不了」、
「干脆不接」,还是「周期性池外专波」)。**本轮不发 owner 邮件**(本周 08-31T01:20Z 那封已发)。

⚠️ **W36 正在飞**,本轮**没有**动它:实例在启动时已 clone 并记下自己的 54-id arm,
成员串的改动**只对 W37 起作用**。批测台 21:14Z 已判定 W36 的 `CARRIERARM_EXIT=1`
完全来自 `tormself`,那一波为另外 52 条买的域不受影响。

---

## 4. 立的规矩:**域价要打在裁定那一行上,不是打在备忘清单里**

协同组 19:21Z 建了 `corpus_hero_census.py` 并请总监「把语料出场普查提成入集前的一道读数」。
**一道读数就是一条清单**,而本章程上一轮刚量过清单的落实率:**六轮 1/6**
(结论原话:**措施是守卫,清单只是记录**)。

⇒ 因此把它接到 **`tools/agent/pending_rulings.py`** 上,打在**未裁行本身**旁边 ——
写裁定的人唯一必看的那个视图。**总监不需要想起来去查,价钱就在被裁的 id 边上**:

```
OTHER (routing/slot ruling still owed): 1
  strategy-33  status=open  prio=3  bundle=hpbool
      DOMAIN-EMPTY  tiny: corpus 0 -- condition (a) CANNOT be bought at the fixture level
```

- `corpus 0` ⇒ fixture 级条件 (a) **不可能**(载重);`corpus N` ⇒ 只是**没被排除**(弱);
- 普查跑不起来 ⇒ `UNCERTIFIABLE`,**不是通过**;
- 主语只从请求自己写的 `bots/BotLib/hero_<x>.lua` **路径**读,不从 `bundle`/`id` 猜、
  不从散文里的英雄名猜(共享代码**不欠域价**);
- **域价不抬退出码**(LIMIT 8):它是**裁定要权衡的事实**,不是请求的缺陷。

**而 `HOLD-DOMAIN-EMPTY` 会把自己的行从未裁桶里清掉、活却还欠着** —— 铁律 9 拉野死分支
消失 37 轮的形状。所以配 **domain watch**:持有中的行每轮点名,`STILL BLOCKED` 安静,
语料补上就翻 **`UNBLOCKED`(退出码 3,欠一次重裁)**,普查跑不起来就说没读过。现读:

```
=== domain watch (rows held until their subject enters the corpus) ===
  strategy-33  tiny  corpus 0  STILL BLOCKED -- the hold stands
```

---

## 5. 铁律 6 与测试(全部裸读,不经管道)

- 静态门 `luacheck_gate.sh` **`GATE_EXIT=0` CLEAN / 0 警告,未用 `RULE6_BYPASS`**
  (容器初始无 luacheck,gate 自己按 `lua-check` 装上);`arm_push_gate.sh` 上膛 exit 0。
- **`bots/` + `game/` 零 diff ⇒ 全量 Lua 套件未跑,也不声称。**
- `tests/test_pending_rulings.py` **84 → 110 checks / 0 failed**(exit 0)。
- `tests/test_carrier_terms.py` **40 checks 1 failed → 52 checks 0 failed**(exit 0)。
- **python 全量 74 文件:74 passed / 0 failed / 1 uncertifiable**
  (uncertifiable = `test_selfcheck_lua_leg.py`,无 `lua5.1`,**GH #358,非本轮引入,不折进通过数**)。
  上一轮是 72/1/1 —— **那条 python trunk 红是本轮修掉的,而且是为对的理由修的**(见下)。
- **lua trunk 红 `test_incoming_damage_callsite_census.lua`(published 43 现读 44)本轮未动** ——
  **GH #394 已开,归属方是那三处同改的作者,不是总监。**

### 5.1 纪律 4 现场:**同一个绿,不是同一个理由**

那条 python 红的断言是 `check("axe" not in terms, ...)`。**把它删掉就能变绿** ——
而它变绿的正当理由只有一个:`axe` 是因为 `immguard` 派生出 128 个载体才混进 term 表的。
本轮**没有碰那一行断言**,`axe` 是被 §2 的修复挤出去的。
测试文件自己的注释早写好了先例(`wkqdmg` 入集时 `skeleton_king` 从这行移走),
**照着先例改断言**会是一次「用错误理由达成的正确结论」,并且会把恒真的载体门一起留下来。

### 5.2 两个变异台,各自抓到自己的一次说谎

| 台 | 结果 | 它自己的那次说谎 |
|---|---|---|
| `tools/agent/mutstand_pending_rulings.sh`(新) | **9/9 CAUGHT**,baseline 复原后 110/0 | 第一轮把 **M8 报成 caught —— 用的却是 M7 的失败签名**:测试 `import` 模块,**旧 `.pyc` 让台上的变异体不是解释器里的那个**。⇒ 每个变异体之间 purge `__pycache__` |
| `tools/agent/mutstand_carrier_minion.sh`(新) | **5/5 CAUGHT**,baseline 复原后 52/0 | 第一版 **N5 报成 SURVIVED,其实是 INERT**:`hero_of` 先读 `MINION_OWNER` 就返回,`MINION_GENERIC` 那行根本没被求值 —— **改了文本没改程序**。⇒ 台子现在**给行为按指纹**,指纹没动就打 `INERT` 并**自称是台子的缺陷**,不赖测试 |

两台都走**文件副本 + `sha256sum -c` 复原**(每次 OK),退出码**裸读**(测试写日志,`$?` 与命令之间无管道)。
M4/M5、N4/N5 是按纪律 2 专门补的方向:**「检查还在、但已不可能失败」和「响亮但答错」都长得和通过一模一样。**

> 顺带一条自伤登记:变异台第一版我自己写成 `out=$(… | tail -6); rc=$?` —— **读的是 `tail` 的状态**,
> 九行 `exit=0` 全是废数。**纪律 3,同一轮里我先被守卫拦一次、又自己复发一次。**

---

## 6. 成本

零 AWS 调用、零 S3、零 EC2、零 CE 请求。MTD **照抄**批测台 21:14Z 自报:**`$3.637 + ~$0.90 ≈ $4.8`**
(九月已跨月重置;围栏 $60 / 刹车 $90 / 批准线 $100 未动)。**不写「顺延」。**
批测台本轮**没有**再为确认八月的数付 CE —— 上轮登记的那条连续四轮的浪费**已停**。

---

## 7. 各组健康度

四组本轮均有产出无空转(batch-desk 18:10Z/21:14Z、hero 19:48Z、replay-check 18:49Z、
strategy 18:55Z/19:21Z)。strategy 那个 5.0h cadence 洞已自行闭合,不升级。
`DECISIONS_NEEDED.md` **新增一条**(扩池方向题)。本周邮件已发 ⇒ **本轮无邮件**。
今日周二,效率台账(仅周日)跳过。

---

## 8. 本轮明说没做的 / 下次触发

**没做**:#402 **乙案**(下轮认领,issue 保持 open 当接力棒);`rotscope` 同族缺陷未修;
**patch 缺口 P1/P2/P3(P3 优先)**;`strategy-5b` 未裁;**GH #395 三问未裁**;
**按需常数第四轮重裁未做**;`#252`/`#256`/`#282`/`#295` 关闭建议未发;**`§24` 未并入 `#229`**;
**`§CE` 已挂二十四轮**;`state.json` round-trip 未修;backlog §19/§20/§21 未动;
`#348`/`#326`/`#324`/`#328`/`#329`/`#321`/`#285`/`#271` 未处理;§CY 三条 + §CV 两条限定待收割;
**GH #384 的自检腿序那半**;**§3.3 那四个重文件连续第四轮没看有没有人接**;GH #394(非总监归属)。

**下次触发**:
1. **#402 乙案**:`seed_draft.py` 侧 `OVER-BROAD` 闸(一条析取 term 宽到十人阵容必中就抬退出码)——
   **它挡的是下一个同形 id,`rotscope` 已经在等**;
2. **把 `hero_pool.txt` 成员资格接进 §DH.4 的域价** —— 让「池外英雄」在**提议**时举手,
   而不是在**发波门**上举手(#385/#393 走完了入集、armed、发了一波才被看见);
3. **§3.3 四个重文件有没有人接**,没接就自己修 `test_stayfield2_marginal_domain.lua` 一个
   (**修它一个就把 233.9s 拉回 136s**,连续第四轮写);
4. **patch 缺口 P3**;
5. `#252`/`#256`/`#282`/`#295` + `strategy-5b`、`§24` 并入 `#229`、`§CE` 第一步。

---

## 9. Token 用量(铁律 8)

```
TOKENS total_in=15,765,939 out=88,635 turns=103
```
