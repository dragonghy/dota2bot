# 总监 2026-09-09T10:12Z — `narrat=2` 四条:一条也不退集

**一句话**:四条 id 的条件 (a) **不是买不到,是没投递** —— 全 37 条 armed id 的路由普查读作
`VERIFIED 22 / DELIVER 12 / MENTION 3 / BUILD 0 / NO-CORPUS 0`,**15 条没有判决的 id 里,
零条属于「买不到」**;而前四轮各花一整轮定的价,量的全是**另一条路**(fixture 加载器)。

零 AWS、零波次、**`bots/`+`game/` 零 diff**、不发 owner 邮件、`DECISIONS_NEEDED` +0、
armed 串 **37 不动**、无 promote、无退集。裁定全文档案 `iterations/streams/test_set.md §GG`。

取活依据:上一轮「下次触发」的 **①**(逐字点名 `liondrainstop`/`ownhalf`/`pulldrag`/`tpgap`,
并逐字要求 `pulldrag` **先查再裁**)。那句警告本轮**兑现了**,见 §3。

---

## 1. 落地四件

| 产物 | 是什么 |
|---|---|
| `tools/agent/a_evidence_route.py` | 路由普查:对每条 armed id 并排给 `verify` / `waves` / `tools`,判五类 |
| `tests/test_a_evidence_route.py` | **32 检查 0 失败**(分母棘轮 / 五类分区 / 两条判据 / 活树只断言不变量) |
| `tools/agent/mutstand_a_evidence_route.sh` | **6 CAUGHT / 0 SURVIVED**,`control_ok`,`RESTORE` 逐字节还原 |
| `iterations/owed_executions.json` **+4 行** | 三条投递行 + 一条建仪器行(25 → 29) |

普查读数(37 条 armed id,分母 `wave-records 11 / behavioural-tools 129 / reports 192`):

```
A-EVIDENCE-ROUTE  ids 37  wave-records 11  behavioural-tools 129  reports 192
                  VERIFIED 22  DELIVER 12  MENTION 3  BUILD 0  NO-CORPUS 0
```

## 2. ⭐⭐⭐ 本轮最该被读的一条

**(a) 有两条路,而前四轮只量了一条。** §GD/§GE/§GF 三轮的退集判据都出自 **fixture 加载器**
(它答不出 ⇒ 买不到);另一条路是**批测录像上的行为检测器**,不经过加载器,而对 12 条未判决的 id
**它已经躺在 trunk 上,文件的主题句里就写着那条 id 的名字**。15 条未判决的 id **全部**在 ≥7 波
(实测全为 10 波,最近 W58)的 arm 串里活过 ⇒ 语料存在。⇒ **`BUILD 0 / NO-CORPUS 0`**:
今天没有任何一条 armed id 满足「(a) 结构上买不到」。

⛔ 这**不推翻** §GE/§GF 的三条退集(那三条判的是各自转轴子句在语料里**问不出口**,读数仍成立)。
它改的是**提问顺序**:先问「哪条路」,再决定要不要花一整轮去 fixture 侧定价。

**这个结论有先例撑着,不是推理**:09-05 对 `tpdying`/`tpreach` 开的两行同形 owed,
**09-08 两行都结清了**(`a_evidence_tpdying.md` `VERIFY … INDETERMINATE episodes=4527`;
`a_evidence_tpreach.md` `VERIFY … WORKING episodes=59`)——**2/2,约 3 天**。
而那两条当时**已经退集**才去买的 ⇒ **退集不是买到 (a) 的前提**,所以本轮选「不退集」。

## 3. 「先查再裁」本轮真的救下了一次搬结论

上一轮逐字警告:`pulldrag` 同属拉野家族、**大概率同一堵 lane-front 墙**,不要搬 §GF 的结论。
查了:`gated_getter_stub_census.py --id pulldrag --all` 打出 `GetAssignedLane` **STUB0**
—— 墙确实在,**但它是 fixture 那条路上的墙**,而 `pulldrag` 的仪器(`pulldrag_walk.py`)不走 fixture。
若照搬,本轮会写下一条**理由正确、结论相反**的退集。

## 4. 四条的裁定(全文 §GG.3)

* `pulldrag` **DELIVER** → owed `a_evidence_pulldrag`,买法 `pulldrag_walk.py`
  (⛔ 不许用 `pullcam_domain --drag` 代替:那个签名按**朝泉水的分量**认证拖拽,而本 id 的全部内容
  就是「别再朝泉水走」,四营地余弦 ≤0.048 —— 工具自己写着)。
* `tpgap` **DELIVER** → owed `a_evidence_tpgap`,买法 `tpgap_domain.py`,
  读数方向早在 `queue.json:strategy-14` 预登记(**收窄**;⛔ 不是「按压数必须下降」)。
* `liondrainstop` **DELIVER** → owed `a_evidence_liondrainstop`,买法 `lion_drain_census.py`,
  ⚠️ 判据用 **post-domain residual**(2026-08-21 换过,旧的 `span>=2.0s` 与结局共因)。
* `ownhalf` **MENTION**,与上面三条**不同类** → owed `instrument_ownhalf_a_evidence`:
  全仓没有检测器在主题句点名它,唯一出现是 `capmono_refusal.py` 把它当 capmono 的**混杂因子**
  (「push the other way」)。⇒ 采购是**建仪器**;⛔ 仍不是「买不到」。

**为什么这份义务会掉(§FB.3 的形状再次兑现)**:`strategy-14`/`strategy-16` 的 `director` 字段里
躺着的是**入集**裁定(`APPROVED_ADMITTED`),`status` 停在 `harvested_pending_verification` ——
波已收割、核验待办,而**没有任何一张按轮驱动的表载得下这一步**。owed 行就是那张表。

## 5. 顺手修掉一条 trunk 红([harness],章程 2a)

自检 `trunk-red(python)` 唯一那条 = `tests/test_stale_waits.py`。根因:`wait_scopes` 的借用规则
把「不点名 id」实现成「没有 **id 形状**的反引号」,于是 batch-desk 章程里
``…`FROZEN none`(`pullcad` 陷阱未复发);入集等待 `no expired admission wait`…``
的第二个子句(主语是**自检腿的逐字输出**,而且那句话的意思是**没有等待**)被判「无主语」⇒ 借来 `pullcad`。
**危害是它的处方**:测试逐字要求「fix the charter line」,指向**另一个组**的、**完全正确**的一行散文。
修法:借用规则改判**任意反引号**(新 `ANY_BACKTICK`),id 抽取仍用 `BACKTICKED`。
钉法:新增 INVARIANT 8(f)/(f2);把修改还原 ⇒ 两条同时红(`MUT_EXIT=1`),还原后 **49 检查 0 失败**;
`mutstand_stale_waits_report.sh` M1–M5 全 CAUGHT、`RESTORE ok`。代价写进 LIMIT 5b(少报不是造假)。

## 6. 量具自己的两次出错(都被自己的分母抓住)

* 第一版按**文件名**判归属 ⇒ `liondrainstop` 判错(它的仪器按英雄命名 `lion_drain_census.py`);
* 第二版按**头部任意位置** ⇒ `ownhalf`/`overchase` 被混杂因子句抬成 `DELIVER`。
* **两个错误方向相反**,`主题句`(docstring 首段)是同时挡住它们的那条线,判据是四份**手读的** docstring。
* 分母棘轮:空 wave 目录 / 空检测器目录 / 无 `arm_string` 的 wave 记录 → **exit 2 且不打表**,
  因为这三种错误都长得像「跑完了、发现债很多」。M1/M2/M6 就是它们。

## 7. 巡检 / 成本 / 健康

* **成本:零 AWS 花费,MTD 不作新声称**,转载批测台 09-09 09:09Z 报告口径;三条线均未跨(围栏 $60 / 刹车 $90 / 批准线 $100)。本轮**没有调用任何 AWS**。
* **五组产出**:batch-desk 09:09Z / hero 08:08Z / strategy 08:02Z / replay-check 06:47Z / director 07:15Z —— **均有产出,无空转**。
* **自检真码 `EXIT=3`**(legs 10,`UNCERTIFIABLE: none`):`cadence` / `owed-executions` /
  `stale-waits` / `trunk-red(python)`。其中 **`stale-waits` 与 `trunk-red(python)` 是同一条,本轮已修**;
  `owed-executions` 里有**本轮自己新开的 4 行**(按定义即 OWED)。
  ⚠️ `git stash` 复跑「main 是不是也红」**本轮没做,不声称**。
  Lua 快检测器腿 **86 文件 0 失败**(快子集,非全量);`NOT RUN`:`test_luacheck_gate_soakswitch.py`
  / `test_selfcheck_lua_leg.py`(GH #358 的 120s,**UNCERTIFIABLE 不是通过**)。
* ⭐ **「后台包装吞掉真码」第十次兑现,而本轮两个数字并排可读**:后台任务的
  `[exited with code 0]` 是包装的,自检真码由 `; echo "EXIT=$?"` 打出 **3**。守卫仍未立(顺延第十一次)。
* ⚠️ **纪律 3 本轮第一条命令又踩管道**(`… | tail -60`),`routine_selfcheck.sh` 的 §22 守卫**当场拒**
  (逐字 `REFUSED: … exit 2, nothing checked.`),改重定向后拿到真码。**守卫第二十四次拦下,老形状。**

## 8. 下次触发

1. ⭐ **给另外 11 条 DELIVER/MENTION 自动开 owed 行**(GH #540;本轮只手开了读过 docstring 的 4 条)——
   一条覆盖 11 个 id,比一轮清两条更接近解冻线。
2. ⭐ **P4.2 的下一格现在换了形状**:`BUILD 0 / NO-CORPUS 0` ⇒ **靠「(a) 买不到」退集来把 armed 压到 ≤20
   这条路今天走不通**;能压的只有 **promote**(投递 → 判决 → 三条件)。这一句要在下一轮开头被读到。
3. backlog 101(条件性穿透探针)/ backlog 102(下界断言守卫)——上一轮 ②③,②已落地,③顺延。
4. 「后台包装吞掉真码」守卫(第十一次,顺延)/ GH #358 的 120s 要人裁(顺延)/
   `hero_domain_scan` 九份读数(顺延)/ 「退集/promote 的五处同步」清单(顺延)。
5. 存量:账户级预算等 owner 回复 / `$0.90` 常数重裁 / GH #523 / patch 缺口 P3。

## 9. token 用量

```
TOKENS total_in=14,792,466 out=73,119 turns=89
```
(统计时刻为收尾前;其后的 push / 章程回填回合不计入。`total_in` 由 cache_read 主导,只作量级参考,
常设指标是 `out/报告` —— 本轮 **73,119 out / 1 份报告**。)
