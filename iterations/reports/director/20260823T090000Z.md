# 总监 2026-08-23T09:00Z(第五十三次触发)— 归因波定案,以及**我给自己写的逃生口没有实现**

**一句话**:裁了两条欠着的决定(GH #140 有 12:11:11Z 硬时限、GH #46 欠了**四天**),
过程里查出 **§AS.3 的解冻条件 (乙) 结构上买不到** —— 那是我立的冻结唯一不靠运气的出口,
它此刻正卡着 `campgrade`。

- **`bots/`/`game/` 逐字未动** ⇒ 稳定版零漂移;两道 Lua 门**未被触发,不声称跑绿过**。
- **总监零 AWS 调用**;批准 12:11Z 那一波(~$1.5),钱由批测台花。
- **未 promote / 未 reject 任何 id**;`itemtrip` **暂时出串但仍在集合内**。
- 开工自检 **worst exit 3**(一条 cadence GAP,见 §6),trunk python **15 passed / 0 failed**。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` — **worst exit 3**:

- **UNLANDED:无** —— `OK: no unlanded work in the certifiable window`
  (trunk 50 commits / 288 refs / certifiable 23 / REFUSED 265)。
- **cadence:1 finding** —— `GAP replay-check 08-23T05:10Z -> now 3.7h`(阈值 3.5h)。见 §6。
- **trunk python:15 passed / 0 failed。**

---

## 1. 选题:两条都不是我挑的,是时限挑的

| | 来源 | 时限 |
|---|---|---|
| **GH #140** | 批测台 08:15Z 明写「**12:11:11Z 之前给结论即可,未裁则默认 (A)**」 | 硬 |
| **GH #46 §4.2** | 结尾写着「**这条请总监裁定**」,**开于 08-19T22:49Z** | 软,但已欠 **4 天** |

而 (A) 是三条路里最差的一条(**再买一遍同一个不可归因的数**),默认生效 = 我不裁就等于选了它。
GH #46 那条更难看:期间 `axebuyblink` 的取证位空转过一次,**08:15Z 的 306 局普查里 Axe 又是 0**
—— **卡住它的是我,不是工具没交。**

---

## 2. GH #140:走 (B),但改三处 —— 改完它就不再是「跨波比较」

**读数**:25 id @ 树 `7159ff3` @ 种子 888/895/896/906,282 有效局 ⇒
`gpm −93.30 / xpm −96.97 / deaths +0.71 / last_hits −6.16 / winrate 0.243`,**五指标 0/4**。
上一波 24 id @ `cde1d6c` @ **同一套种子** = `−42.49 / 0.394`。**两波之间 armed 串只差 `itemtrip`。**

### 2.1 改动 1(要害):树钉 **`7159ff3`**,不是 `cde1d6c`

批测台写的是「树钉**当时的 tip**」。照字面执行 = **24 id @ `cde1d6c` @ 同种子** =
00:11Z 那一波的**逐位复刻**,它测的是**跑与跑之间的噪声**;而相对我们**已经买到**的 −93.30,
它同时动了**两个**变量(串 + 树)——正是这一波要消掉的那个混淆。

钉 `7159ff3` ⇒ 相对 −93.30 **只差 armed 串这一件事**。

**另有一条与设计无关但会当场炸掉波次的机械理由**:`cde1d6c`(08-22T23:49Z)落在本容器
浅克隆 graft 点 `b86c82d`(08-23T00:57Z)**之下**,`git merge-base --is-ancestor` **答不出来**;
`7159ff3`(08-23T05:42Z)在 graft 之上,**已实测 YES,是 `origin/main` 的祖先**。

### 2.2 改动 2:种子集**强制**不变 —— 而且我先去核了它

我没有假设两波种子相同,我去查了:**00:06Z 报告的启动表逐台列着 888 / 895 / 896 / 906**
(`spot_20260823_0011{27,31,36,40}`),与 06:11Z 波**同一套**。

一个种子 = 一套固定的 10 人镜像阵容 ⇒ 种子集钉死,这个对照就是 **paired**,
而 paired 的均值差**与配对顺序无关**:−93.30 − (−42.49) = **−50.81**,怎么配都是这个数。

### 2.3 改动 3(报告口径):per-seed 必须**带种子号**

我这轮是**靠推断**认定 24-id 的 `-55.24/-31.38/-38.32/-45.03` 按 888/895/896/906 排 —— 报告没写。
**均值差与配对无关,所以裁定不依赖那个推断**;但**逐种子 Δ 的离散度**依赖它,
而那是将来唯一能说明 −50.81 是不是真的东西。以后一律 `seed=888 gpm=−122.10`。

### 2.4 预登记读法(数据到货**之前**写死,免得到货后挪门柱)

已有 25-id @ `7159ff3` = **−93.30**;要买 24-id @ `7159ff3` = **X**。

- **X ≈ −40** ⇒ `itemtrip` 吃下 ~−50 gpm ⇒ **退回协同组**(退回≠reject:它的 (a) 从没买过);
- **X ≈ −90** ⇒ `itemtrip` 聚合层面**被开脱** ⇒ 走 §2.5;
- **两者之间** ⇒ 报逐种子 Δ 离散度,**不归因**。宁可说不知道。

**买不到什么**(同样先说):**不能开脱另外 24 个 id**。−40 仍然是 −40。

### 2.5 ⭐ 树漂移先收窄一半 —— 把批测台自己报告里**没接在一起**的两节接起来

§5.1 诚实边界 1 点了三个 ungated 英雄文件;**同一份报告** §7.1 的普查给它们标了价:

| 漂移 | 语料分母 | 判读 |
|---|---|---|
| `hero_axe.lua`(`349860a` / `a3f3456`) | **axe 0/306 局** | **可证明地惰性** —— 不是「大概小」,是**零**。划掉。 |
| `hero_crystal_maiden.lua` + `aba_item.lua`(`9fa4898`,pos_5 改 arcane boots) | **CM 306/306** | 头号嫌疑 |
| `hero_lion.lua`(t10 +20 移速) | lion 77/306 | 次级 |

⇒ 若这一波开脱了 `itemtrip`,**嫌疑就落在一笔已 ship 进稳定版、ungated、每局都跑、
改的还是焦点英雄出装的改动上**。那比测试集里任何 id 都严重 —— **测试集里的是暗的,`9fa4898` 是亮的。**
并且 **CM 那笔与 `itemtrip` 同属物品层**,「两者交互」是活假设,不是托辞。

**这两节本来就都在批测台的报告里,谁也没写错**;缺的是把它们**并排读**。
和自检那三条腿的毛病同形。

---

## 3. ⭐ 本轮真正的发现:**我写了一个没有实现的逃生口**

§AS.3 冻结了测试集增长(21→22→23→24→25,每波读负,而按我们自己的 §AF.3
这个读数不能归因到任何一个 id),留了两个解冻条件,**(乙) 是唯一不靠运气的那个**:

> (乙) 跑过一次 bisect 波(把集合对半拆成两臂**同树内部对照**)。

**这个波发不出来**,逐层查:

| 层 | 现状 |
|---|---|
| 门文件 | `bots/Customize/soak_side.lua` = `{ side, cand, seed }` —— **一个 side,一个 cand 串** |
| 读它的 | `J.IsSoakCandidate`(`jmz_func.lua:4634`)先匹配 `conf.cand`,再 `return GetTeam() == nSideTeam` |
| 写它的 | `validate_onspot.sh:50` / `mirror_ab.sh:27` / `mirror_multi.sh` —— 都只写那一个串 |

⇒ **一个 id 要么在候选腿上 armed,要么哪儿都不 armed。基线腿没有任何办法带自己的串。**
「两臂同树内部对照」在这套门面前**不是难,是表达不出来**。
而这道冻结此刻**正卡着 `campgrade`(GH #137)**。

**和上一轮记的是同一个病**:注释里写「summary 会印分母」、然后并没有印 ——
**写下的规矩和落地的代码差一层。上一轮那次差一行,这次差一整个字段。**

### 3.1 处置(两步,已分别落地)

**(i) (乙) 即刻改写为买得到的形式**:**同树同种子的配对波对** ——
两波钉同一个 40 位 SHA、同一套种子,**只差 armed 串**。
§AF.3 作废的是「跨树 **+** 跨集合」的残差比较,两样都钉死就都不在它射程内。
⇒ **12:11Z 这一波就是这样一个配对的后半**(前半 25-id @ `7159ff3` 已买)
⇒ **它收割时 (乙) 达成,冻结开一个名额。**

**开一个名额,不是开闸**:集合的增长速度不许超过它变得可判读的速度。
**一次归因波 resolve 一个 id(退出集合 _或_ 被开脱,两者都算判读)= 一个入集名额。**

**`campgrade`(GH #137)排第一,条件性批准**,进归因波**之后那一波**的串。
理由:协同组 07:55Z 那一包满足 (a)-local 与 (c) —— 四支 if/elseif 每支都只给 `botLevel`
**定上界**⇒ 打不动的营地掉进更宽松的下一档,这是**读出来的**死代码不是**测出来的**;
14 例全绿、3 变异 3 抓、未 armed 逐字节等价。**而它排的队是我立的。**

**(ii) 真修法开 GH #141**(`cand_ref`:基线腿也能带串)。严格更好:
**跑间噪声整个消掉**(06:11Z 逐种子 gpm −122/−120/−106/−25,`sd 45.81`,**这不是小项**)、
一次 bisect 成本减半、并解锁「两个候选集合直接对打」这类今天做不了的实验。
**归总监,写死了「不许顺手做」** —— 爆炸半径是**每一个 gated fix**,
验收第一条是「未设 `cand_ref` 时对四种 `cand` 形态逐字节不变」,那是前提不是附赠;
另有一条安全要害:**两臂读数必须在 verdict 里带 `cand_ref` 并标 `contrast=two_arm`**,
否则它会被归档成普通读数并**污染此后每一次比较**,且事后分不出来。**数学不用改,记账必须改。**

---

## 4. GH #46:欠了四天的选种裁定

### 4.1 选种的代价被记错了 —— 不是**偏倚**,是**外推范围**

§4.2 的顾虑是「挑种子让跨波经济读数可比性变差」。拆开:

- 镜像 A/B 是**波内自比**(同种子、同阵容),**阵容在减法里整个消掉**
  ⇒ **挑种子不给 (b) 引入偏倚**。这一半可以放下。
- 真代价在别处且真实:专挑含 Axe 的种子集,量到的是「**在含 Axe 的阵容上**的效应」,
  不是池子级效应。那是**外推范围**的损失。
  **外推范围的损失只要标注就不骗人;偏倚不标注就会骗人。**

⇒ **(a) 波与 (b) 波都允许挑种子**,两个条件:
(1) **要放在一起比的两波,种子集必须逐位相同**(§AT.3 的配对波对全靠它);
(2) **verdict 带选种谓词**,带谓词的读数**不许**被当池子级数字引用。

**排期**:12:11Z 这一波**不换**(它是已买前半的配对后半);**从归因波之后那一波起**换,
由批测台离线算一套**含 axe(最好同时含 lion)**的四种子集,**发波前登记**,
此后作为**常设种子集**不动。**每个纪元最多换一次,先登记后发波** ——
否则会得到一堆两两不可配对的波。

### 4.2 否掉一个旋钮

§4.3「给 Axe 在 `hero_pool.txt` 加位置把出现率抬到 ~28%」——**不动**。
选种改的是**抽到哪几套阵容**;改池子位置改的是 **Axe 被抽中时打哪个位**,即**它跑哪套角色逻辑**
⇒ **为了让一个东西更好测而改掉被测的东西**。免费无损的旋钮先用满。

### 4.3 一条选种解决不了的,以及它已经开始退回工作请求

lion **77 局,局终等级 min 8 / med 11 / max 14 —— 0/77 到 15**。
⇒ lv≥15 才进域的东西在 10 分钟裁判上限下是**结构性空集**,不是「样本稀少」
(与 GH #115 不冲突:那条讲连续量上的窄带,**这里是准入条件从未满足**)。
英雄组的 `hero-4`(Lion t15)就是被这条退回的。

买它只有抬 `SOAK_CAP_MIN` 一条路,代价是 **$/局上升 + 与现存全部语料不可比**。
⇒ **保守默认:不抬**,lv≥15 归 fixture 验。已作为 **4b 追加进 `DECISIONS_NEEDED.md` 的第 4 条**
(那条已经存在,**追加而不是新开** —— 免得周日邮件里同一件事出现两遍)。

### 4.4 Axe 的 0/306 现在是**四天两次**

`349860a`(Axe t15 裁定)**已 ship 进稳定版**,而它在现行四种子下**结构上既测不到也录不到**。
§4.1 的换种子从下一波起堵这个洞;**在此之前,任何 Axe 改动的 (b) 都不许声称已测。**

---

## 5. `itemtrip` 的身份(免得被读成 reject)

**暂时出串,仍在集合内。** 不是 reject、不是退回,是**为了给它买归因而暂时不 arm**。
`test_set.md` 头部的成员串(27 id)**不变**,变的只有 §x.0 的**可 arm 串**。

**连带核对(铁律 9)**:`itemtrip` 在 state.json 里登记为 owner **P2 家族**里
「**健康带以上**」那一支。摘它**不动 P2 自己的取证** —— P2 决策侧的
`stayfield` / `stayfield2` 都在 §AT.0 的 24 id 里,**照常 armed**;
P1 的 `creeppull` / `pullcamp` 同样在串里。**两条优先项的棒都没掉。**

---

## 6. 健康度 / 成本 / 沟通

- **五组本轮均有产出**:批测台 08:15Z(收 25-id 波 + 补交欠 37 轮的档案侧分母)、
  hero 08:00Z(GH #136 WK 魔棒)、协同组 07:55Z(GH #137 野点阶梯是死代码)、
  录像组 05:10Z(#123 承重帧被自己推翻,但「卡住」3/3 成立)。
- **一条观察项(不升级)**:自检报 `GAP replay-check 3.7h`(阈值 3.5h)。
  录像组两小时一触发,05:10Z 之后漏了约一次。**单次、幅度 0.2h ⇒ 记录不动作**;
  若下一轮仍是同一条 GAP 且拉长到 ≥6h,按章程 2e 记为空转并指名。
- **owner 优先项**:P1 / P2 **两条的 armed id 都在 §AT.0 的串里,均在动,不升级**(见 §5)。
- **patch(章程 2f)**:**本轮跳过** —— 02:51Z 本人刚查过
  (`patchnoteslist` 最新仍是 `7.41a..7.41e`,`PATCH_UPDATE_GUIDE.md` 停在 7.41a,
  缺口未变大、无新 patch),周内不重复。
- **效率台账(章程 2e2,周日项)**:**本周已出** —— `efficiency_202634.md`,本轮不重做。
- **成本**:MTD **$23.578**(批测台 08:15Z 自报,`budgets` 免费口径),
  `$45 / $90 / $100` 三线全未触及。批准 12:11Z 一波 **~$1.5**。**总监本轮零 AWS 调用。**
- **沟通**:本周 owner 邮件已发,**本轮不发**;新增内容进 `DECISIONS_NEEDED.md` **第 4 条的 4b**
  (追加,非新条目)。本轮四个决定(树钉哪个 SHA、种子不换、(乙) 改写 + 一次一个名额、
  否掉池子旋钮)**全在自主授权内**,做了记录即可。

## 6b. 顺手关掉的一条 backlog:`suggested` 的政策裁定(章程 §8)

它的解锁条件写的是「**第一波带 `winrate` 的真实读数到手**」——**到手了,而且是两波**
(24-id 0.394 / 25-id 0.243),`winner_by` 分布两波都拿到了。所以本轮裁完并关闭(细则 §AT.8):

**`winrate` 不进 `suggested`,也不算 `comps_better` 的第五个独立指标。**
GH #135 的构造性论证(`SOAK_CAP_MIN=10` 下 `engine` 档恒 0)已被**两波实测兑现**
(`engine 0` / `winrate_independent_of_gold` 0/301 与 0/306,旁证:只有 1 局 < 9.5 分钟)
⇒ **胜负在 305/306 局里就是 10 分钟金钱上限判出来的**,`winrate` 是 gpm 的**符号化粗读**;
当第五个证人会让**一个 gpm 赤字看起来像两次失败**,那正是自动 promote 建议最不该犯的错。

**但它保留一个不可替代的位置**:owner 铁律 2(b) 的原话点名的就是**胜负**,
「四局输三」比「gpm −93」更贴 owner 的口径 ⇒ **条件 (b) 的首选报告量仍是它**。
**重新解锁条件写死**(免得将来靠印象翻案):某一波 `winner_by` 的 **`engine` 档真的非零**。

⇒ `suggested` 逐字不动 —— 但这一次是**裁过之后**不动,不是**还没裁**所以不动。

## 7. 下次触发

1. **收归因波(12:11Z 那一波)** —— 按 §AT.1 的**预登记读法**处置,不许挪门柱;
   同时 (乙) 达成 ⇒ 给 `campgrade` 开那个名额,写进下一节的 §x.0 串。
2. **GH #141**(`cand_ref`)—— 但**先写 §4 的四条验收测试再动 `jmz_func.lua`**;
   它会触发两道 Lua 门(注意 `#124`:全量 `run_tests.lua` 约 83 分钟)。
3. **`#131`** —— core 侧 smoke 列表硬编码 11 元、不随树生长([harness] 归我,小)。
   **这是第二次被更硬的时限挤掉**,下轮若无硬时限项就做它。
4. backlog **6b**(`unlanded_commits.py` 并排打印 ref 活跃度)/ backlog **§9**(扩 `detect.Timeline` 索引小兵野怪)。

## 8. token 用量(铁律 8)

```
TOKENS total_in=3,862,441 out=53,739 turns=33
```

`out/报告` = **53,739**(上一轮 47,269)。同量级,**同样没有 Lua 门的等待**
(本轮 diff 里一行 Lua 都没有)。这是 `#124` 印在 token 账上的第三个同向读数:
**动 Lua 的轮次贵一倍,贵的不是写代码,是等门。** GH #141 下轮会真的触发它,
到时那个数字应回到 ~10 万量级 —— **这是一条可证伪的预测**,不对就说明归因错了。
