# 总监 2026-09-14T16:0x–16:3xZ — RULING 37(`stayfield` 退集 26 → 25)+ RULING 38(`hero-82` 的 promote atom)

**一句话**:结清了上一轮按 §2.6 自登记的 `stayfield_tp_disposition`,**而本轮真正的产物不是那条退集,是被否决的另一半** —— 协同组要我写进 owner 优先项 P2 的那句话(「P2 在 TP 腿上本来就没有病例」)**建立在一个被树自己声明为不完整的数上**,而它缺的那一格就摆在同一帧的 slot 1。
零 AWS 调用、零波次、`bots/` + `game/` **零 diff**、不发 owner 邮件。

---

## 〇、开工自检(铁律 10)与三条腿读数(铁律 6,GH #624:三行不是两行)

- **自检:⛔ 不声称绿,也不声称跑完。** 起手第一条命令**第 20 次**被 `REFUSED: routine_selfcheck.sh stdout is a pipe` 挡回(守卫是对的,只为计数);改重定向后自检跑到 **python 腿**读出 `TRUNK RED`(见 §三),随后在 fast-Lua 腿上**被我主动杀掉** —— 理由是 `selfcheck_recursive_fork_amplifier`(上一轮自登记的欠条)在本容器上重现,而我需要一棵安静的树来量 `lua_gate`。⚠️ **杀掉的那一刻起,trunk 的 Lua 那一侧本轮没有被自检看过**;`lua_gate` 的 375 条是另一回事,不替它作数。
  - ⭐ **`timeout` 守卫本轮第一次上岗并且没派上用场**:我起手用的是重定向不是 `timeout`(上一轮登记的第三发就此止住)。但**harness 自己**在 600s 把它挪进后台,那条通知的 `exit code` 是**启动器的码不是自检的码**,⛔ 未引用。
  - ⚠️ **自登记一发**:`pkill -f 'routine_selfcheck.sh'` 把**我自己的 shell 也杀了**(exit 144)—— 模式匹配到了执行它的那层 bash。改 `pkill -x -f 'bash tools/agent/routine_selfcheck.sh'` 后干净。**零损失,但值得记:一个用来清场的命令把下命令的人也算进了场。**
- **铁律 6 三条腿(最终树,裸码,均未过管道;未用 `RULE6_BYPASS`)**:

```
GATE_EXIT=0   CLEAN (iron rule 6 static half passed)   luacheck bots game: 0 warnings
py gate:  87 ran, 0 findings, 0 uncertifiable, 11.1s   RC_EXIT=0
lua gate: 375 ran, 0 findings, 0 uncertifiable, 8 unanswered, 7 known-red, 581.3s   RC_EXIT=0
```

⚠️ **`lua gate` 的第一次不是 0,是 `RC_EXIT=3`,而那条红是本轮的真产物之一** —— 见 §四。
⚠️ 动态那半(`lua5.1 tests/run_tests.lua` 全量,~100min,GH #124)**未跑**,三条腿也不声称它。

---

## 一、⚖️ RULING 37 —— `stayfield` 退集(armed **26 → 25**),处置 `CALLSITE-EMPTY`

档案全文 `iterations/streams/test_set.md §HK`。成员串三元组**现算现验**:旧串 26 id / **230 字节** / md5 `3a438e729b3960382dfd51a89a7da54e` —— **与记录逐位相同**(这是「我在用同一种算法读同一行」的证据,不是装饰);新串 **25 id / 220 字节 / md5 `850bdee1fde2c42a00eb3c23fd3e04fb`**,落盘后复读一致。

**发波侧无风险,现读而非假设**:批测台 15:08Z MTD `$89.858`、对 `$90` 刹车 headroom **`$0.142`** ⇒ 最便宜的一波 `$1.10` 也起不飞;`W69` 距今 >48h。**改成员串期间没有任何波在空中**,这正是该欠条 `trigger` 逐字要求的「不晚于下一次发波」。

**理由(协同组 10:26Z 的 1039 帧普查,零 AWS)**:`margin_live = 0 / 1039`。
⭐ **而分解比那个 0 重要,因为分解才说明这条退集与 `fieldsip` 无关**:24 个 solo-S 帧里 **23 个被调用点 `撤退:3` 自己的合取吃掉**(`fieldsip` 没上场),另两个 live-S 帧被分支自带的 `itemFlask == nil` 否掉 ⇒ **有没有 `fieldsip`,它们都不是这条腿的域**。上界 `branch_open` 仅 4。⇒ **即使 `fieldsip` 明天消失,这条腿的域也只有 1 帧。**

**处置名 `CALLSITE-EMPTY` 是新的**,按 §HA.3「名字要编码什么能解开它」与另两种域为空区分开(§HK.1 有表):`DUMP-SCHEMA-BLIND` 要加 dump 字段,`wandlimbo` 的仪器墙要改两端仪器,而**本条什么都不用买 —— 它是一个已经答完的问题,答案是 0**。三者里唯一「读数是完备的」那一个。

⛔ **不是 reject**:gate、`J.IsFieldRegenSituation`、`J.ShouldRegenNotGoHome`、`J.ShouldRegenNotTpHome`、两份测试**逐字保留**,`bots/`+`game/` 零 diff,(c) 成立。

⛔ **`fieldsip` 的量级门限本轮特意没动**(§HK.3):树上三处**各自独立地**写着动它会在同一次编辑里移动 `stayfield`/`stayfield2`/`fieldbuy` 三条,逐字 `which is the lanefix bundle mistake`,账单 gpm **−74.5 / −88.7**。它是独立定价的工作单元,**不是本次退集的副作用也不是前提**。

**判定完结 1 < owner P4.2 要的 2,如实登记,没凑第二条**(§HK.4):唯一够格的 `stayfield2` 的 0 与本条**不同型**(它是「有过域、被 `fieldsip` 拿走」),拿本条的理由去裁它是**用一条对它不成立的理由**。

---

## 二、⭐⭐⭐ 本轮最该被读的一段:被**否决**的那半,以及一个数怎样在完整的伪装下掐死一条 owner 优先项

协同组交棒出口 (2) 的后半逐字要求总监把这句话写进 `OWNER_PRIORITIES.md`:

> 「若量级条款优先,那 P2 在 TP 腿上**本来就没有病例**,应当由总监把这句话写进 P2 的「现状」段。」

**⛔ 否决,不写。** 理由是同一帧上的一次现读,三步:

1. 否掉 P2 铁证帧的算术是 `J.FieldRegenSipValue(lina) = 85` vs `0.25 * 1088 = 272`。
2. `85` 来自 `J.FIELD_SIP_HEAL`,**那张表里根本没有魔棒这一格**;而该帧 **slot 1 正是一根魔棒** —— fixture 现读
   `tests/fixtures/f_260822_063722_lina_tp_home.lua:21`:
   `items = { 'faerie_fire', 'magic_wand', 'arcane_boots', 'blitz_knuckles', 'null_talisman', 'empty_bottle', … }`,
   而 P2 自己的铁证帧散文也逐字写着「包里有 faerie_fire + **魔棒充能**」。
3. 不收它的理由写在表上方五行(`bots/FunLib/jmz_func.lua:5692-5696`),逐字:
   `Deliberately NOT counted: magic_wand / magic_stick. … its charge count -- the thing that decides whether it heals at all -- is not in the dump.`

⇒ **`85` 不是「这一帧能喝到多少」的测量值,它是一个下界**,而且**短的那一格恰好就是这一帧真正持有、且读不出来的那一格**。

⭐ **被提议的那句话,把一个关于 faerie fire 的真命题说成了一个关于这一帧的真命题**:
「一个 faerie fire 救不回 31.8% 血的 lina」——**对**;「⇒ 这一帧不是病例」——**买不到**。

⚠️ **而我不让这个未知含混过去,把救回带算出来了,并且它对我的结论不利**:魔棒 **15 HP/充能**(`J.ShouldDrinkWandInLimbo` 注释逐字 `15 HP per charge, both stick and wand`),`FieldRegenSipValue` 取**单次最大**一口(不求和)⇒ 魔棒单独清过 272 需 **≥19 充能**(18 充能 = 270 < 272,上限 20)。
⇒ **救回带很窄。但「窄」不是「空」,而两者的差别恰好就是那个买不到的数。**

📌 **可复用的一条(§GB.2 形状第三例)**:**一个被声明为不完整的数,不会因为它算出来的方向唯一就变完整** —— 而这一次它的方向恰好是掐死 owner 优先项的那一边。

**⇒ 写进 P2「现状」段的是诚实那一句**:铁证帧今天读 `85`,**已知是下界**,短缺量未知、救回带 ≥19 充能 ⇒ 该帧对量级条款是不是病例 = **UNCERTIFIABLE**,卡在 `owed_executions.json:wandlimbo_charge_instrument`。**P2 完成定义第 3 条在 TP 腿上因此不是「无从买起」,是「先买那两端仪器」。**

### 2.1 连带:一条没有触发器的欠条,本轮接上了一个会响的东西

`wandlimbo_charge_instrument`(总监 09-08 立)的 `trigger` 逐字是「**没有外部触发器** … 任何一轮有余量的工作单元;或有人提 `wandlimbo` 重新入集的那一刻」——**两个至今一个都没响**。本轮量到它挡的**不止是一类 id**:**它挡着 owner 优先项 P2 的铁证帧本身**。已就地把 `trigger` 从「有余量就做」抬到「**owner 优先项的前置**」,并写明它同时是 §HK.3「不动 `fieldsip` 门限」的解锁条件。
⛔ **`done_when` 一字未动** —— 加强触发器和改判据是两件事,后者会把针脚挪到别的东西上。

---

## 三、⚖️ RULING 38 —— `hero-82` = APPROVED(`promote_atoms.json` 落行),**同时是本轮 trunk red 的正主**

档案 `test_set.md §HL`。开工自检 python 腿的那条红逐字是:

```
FAIL: rows in the OTHER bucket that declare a rideshare in their own prose --
      §BB.4's 'rule it this round' cannot fire for these: [('hero-82', ['零 EC2'])]
```

⇒ 一条**自称零 EC2、却落在裁定机器够不着的桶里**的请求。它既是铁律 2a 的 [harness] 红,**也是一条本座位自己欠的裁定** —— 两件事同一个动作。裁完实读 **`836 checks, 0 failed`,裸码 `RC_EXIT=0`**。
⚠️ **检查数 837 → 836,而下降的原因要说清楚**:本轮退休了一条 owed 行(71 → 70),与「一行一 check」一致。⛔ 这是**一致,不是证明** —— 掏空断言同样会让计数下降,所以它和 `0 failed` 一起读才有意义。

**前提逐条复核在源码上,没有照抄请求散文**(该文件 README 的要求,先例 `field_hold_needs_magnitude` 的提议前提曾被现测证伪三波)。四条现读全中:`hero_skeleton_king.lua:85-87` 的 t10 改写 ✓;72-84 行 COUPLING 声明块逐字 `min(honest, shipped)` / `byte-for-byte no-op from hero level 13` / `the two ids must be armed and ruled together or not at all` ✓;耦合**故意没写成代码合取**(写成合取即 `pullcad` 陷阱)✓;现测 armed 串 `wkqdmg` **在**、`wkt10ls` **不在** ✓。

⭐ **`direction = one-way` 这次是量出来的不是照抄的**(§HL.2):`wkqdmg` 今天的证据本来就长在「出货 t10 被点、dot=4.0」的世界里,**那正是单独 promote 它之后的世界**;⛔ 反方向才是静默陷阱(先 promote `wkt10ls` ⇒ dot 永远 2.0 ⇒ `wkqdmg` 作用等级数 **11 → 24**,此前所有读数描述的都是另一个世界,**而 `check_armed_wiring.py` 查的是调用点存在、不是谓词能不能为真**)。

⚠️ **两处如实登记**(§HL.3):(甲) 本行**比源码那句话弱** —— 源码要两条「一起 arm、一起裁」,atom 只管 **promote 日**、管不了 arm 日 ⇒ 将来给 `wkt10ls` 买证据时的 co-arm **本行不保证**,要在发波请求里自己写明;(乙) **不是入集批准**,armed 25 且 `wkt10ls` 不在其中。

---

## 四、⭐⭐⭐ 退集把协同组当天早上刚落的棘轮顶红了 —— 而修它的过程抓到一个「绿得没有道理」的绿

`lua_gate` 第一次跑读 **`RC_EXIT=3`,1 finding**:

```
FAIL tests/test_stayfield_tpleg_live_domain.lua ::
  [ratchet][precondition] both ids are armed TODAY, or the census is about a dead world
  :312: stayfield left the member string; re-run tests/_stayfield_tpleg_sweep.lua
```

**这是本轮最健康的一件事**:守卫按设计开火,**开在推的人自己的闸上、在移除它的那同一个工作单元里** —— 这正是 GH #624 要买的位移(与上一轮那条**假红**形成对照:那次是并发进程抢 `soak_side.lua`,病根在进程表里)。

**⛔ 没有删守卫,也没有把它弱化成 `true`。** 两处修法:

1. **精确开一个出口,并且要求它被证明**:`stayfield` 缺席现在只在**档案里能读到裁定**时被放行(`test_set.md` 同时出现 `stayfield_tp_disposition` 与 `RULING 37`)。**静默移除仍然红。** `fieldsip` **没有出口**(P2 的更正就压在它的归因上)。
   ⚠️ 为什么不照守卫注释说的「re-run the sweep」:退集之后 `live_S = 0` 是**按构造**成立的(门直接关了)——那是**一个顶着旧名字的空洞数**,正是本仓反复付账的「绿得没有道理」。
2. **⭐⭐ 第二处才是真发现,而它一开始骗过了我**:同文件那条「live 串按算术拿走那一帧」的用例驱动 `arm(J, armed_list())`。退集之后它**仍然 PASS** —— 但 wrapper 是在**自己的 gate 行**答的 false,**算术一步都没跑**。⇒ **结论对了而理由没了**,且**计分板上看不出来**。改成驱动**普查真正测的那个世界**(live 串 **+** `stayfield`)。

### 4.1 变异台:先证明修法是**可被看见的**,再相信那个绿

⛔ 每条变异先打计数确认**落地**(「存活」与「没打进去」在计分板上同形)。基线 `11 tests, 0 failures`。

| 变异 | 落地 | 结果 |
|---|---|---|
| **M1** 档案里 `stayfield_tp_disposition` 抹掉(1 处) | ✓ | **CAUGHT**(1 failure) |
| **M1b** `RULING 37` → `RULING 99`(1 处) | ✓ | **CAUGHT**(1 failure) |
| **M2** 驱动集里去掉 `stayfield`(还原成 `armed_list()`) | ✓ | **CAUGHT**(1 failure) |
| **M2′** 去掉我新加的那两条 pin **并且** 去掉 `stayfield`(977 字节) | ✓ | **⛔ SURVIVED,`rc=0`,`11 tests, 0 failures`** |

⭐⭐⭐ **M2′ 是本轮最值钱的一个读数,因为它是对着我自己写下的一句话做的核验。** 我在修法注释里写了「没有这条 pin,这个变异会活下来」。**那句话如果不跑就只是个说法** —— 跑完它是 `rc=0` 的**绿**。
⇒ 两件事同时被证明:(i) 第 2 处修法**确实**只改「为什么绿」不改「绿不绿」,所以它**必须**自带一条 pin,否则下一个人把它改回去**永远不会有东西举手**;(ii) 那条 pin **是**抓住 M2 的东西,而不是 M2 恰好被别的断言拦下(「结论对了而理由不对的 CAUGHT 不是 CAUGHT」,上一轮 M2 的教训)。
复原后 `11 tests, 0 failures`,`lua gate` 重跑 **375 ran, 0 findings, `RC_EXIT=0`**。

---

## 五、§2.5 三条投递 —— **两条买到,一条买不到,且不记成已投递**

| 投递 | 状态 |
|---|---|
| (ii) `test_set.md` 档案全文 | ✓ §HK(RULING 37)/ §HL(RULING 38) |
| (i) `queue.json` 的机器字段 | ✓ **但形状要说明**(见下) |
| (iii) 协同组活 issue 线程追评 | ⛔ **本轮买不到**,按铁律 11 不空转等待 |

⚠️ **(i) 的形状**:RULING 37 **没有对应的请求行** —— 这一棒是**报告交棒**(协同组 10:26Z §3)不是 queue 请求;`grep stayfield` 命中的四行没有一行的标的是本处置(`strategy-1` done / `strategy-7` 是 `bagsalve` / `strategy-23` 是 `fieldsip` 入集 / `strategy-44` 是 `stayattr`)。
⛔ **没有编一个 `director` 字段挂到不相干的请求上。** 改为把本裁定的**下游约束**落到**真正会读到它的那张表**:`strategy-23`(`fieldsip`,`harvested_pending_verification`)新增 `director_note_20260914`,⛔ 不动它原有的 `director`(那是它自己的入集裁定)。**§DR / GH #413 的形状:终点必须是那一轮真正被驱动的那张表。**
RULING 38 则是正常形状:`hero-82` 的 `director` 字段 + `status` → `ruled`。

**(iii) 欠下的追评内容(下轮 MCP 可用时补,原文如下)**:
> 协同组 10:26Z 交棒已裁(总监 RULING 37,全文 `test_set.md §HK`)。出口 (1) **采纳**:`stayfield` 退集,armed 26 → 25,处置 `CALLSITE-EMPTY`,代码/gate/两份测试逐字保留。出口 (2) 的门限改动 **⛔ 不动**(三处源码声明:动它会同时移动 `stayfield`/`stayfield2`/`fieldbuy` = `lanefix` 捆绑,账单 gpm −74.5/−88.7)。
> ⛔ **出口 (2) 里要写进 P2 的那句话被否决**:`FieldRegenSipValue = 85` 是**下界不是测量值** —— `J.FIELD_SIP_HEAL` 没有魔棒那一格(`jmz_func.lua:5692-5696` 声明原因是充能不在 dump 里),而该帧 slot 1 正是魔棒;救回带 **≥19 充能**,窄但非空 ⇒ 该帧状态 **UNCERTIFIABLE**,卡在 `wandlimbo_charge_instrument`。P2「现状」段已按诚实版本更正。
> ⚠️ 你们 09-14 早上落的 `test_stayfield_tpleg_live_domain.lua` 被本次退集顶红(precondition 按设计开火),**已由总监在同一工作单元修好并加固**,不是要你们返工:出口只对**有档案的裁定**开,静默移除仍红;另修了一条会「绿得没有道理」的用例(详见本报告 §四,含 M2′ 的 SURVIVED 读数)。

---

## 六、巡检(铁律 e:`git fetch` 后 / 按名字序 / 从 `origin/main` 取)

取数时刻 **2026-09-14T16:04:55Z**,命令 `git ls-tree -r --name-only origin/main iterations/reports/<组>/ | sort | tail -1`:

| 组 | 最新报告 | 距今 |
|---|---|---|
| replay-check | `20260914T154849Z` | 0.3h |
| batch-desk | `20260914T150800Z` | 0.9h |
| strategy | `20260914T140942Z` | 1.9h |
| hero | `20260914T135322Z` | 2.2h |
| director | `20260914T131000Z` | 2.9h |

**五条流全部在产出,无红色升级。** ⭐ 本轮**没有**误判停摆 —— 上两轮各栽一次(落后 clone / `ls -t` 反序),规定的取法**第一次在一个完整轮次里被照做**。

**成本**:MTD **`$89.858`**(转载批测台 15:08Z,**本台零 AWS 调用**),对 `$90` 刹车 headroom **`$0.142`**。⚠️ 批测台登记该快照自 10:54:23Z **冻结 ≈4.2h**,是「没测」不是「没涨」;本轮无需为此动作 —— `$0.142` 下任何波都起不飞,而 GH #779 的 accrual 判据上一轮已落地并在本轮批测台第一次现跑就走了「不买」分支。

**owner 沟通**:本周邮件档期**未欠**,本轮**不发**。`DECISIONS_NEEDED.md §15`(GH #779)第十五轮零表态,已挂不重复立条。

---

## 七、欠条账本

- **退休 1 条**:`stayfield_tp_disposition`(`owed` 71 → **70**,`retired` 16 → **17**),带 `retired_because` 全文证词,含**买不到的那条投递**的如实登记(⚠️ LIMIT 照抄不加强:读 DONE 只承诺**裁定已作出并归档**,不承诺裁得对)。
  判据现读:`grep -c stayfield_tp_disposition iterations/streams/test_set.md` = **1**(`unmet_at_ruling` 记的是 **0**)。
- **加强 1 条**:`wandlimbo_charge_instrument` 的 `trigger`(见 §2.1),`done_when` 一字未动。
- **未动**:`selfcheck_recursive_fork_amplifier`(上一轮登记,`trigger` 写的是「下一轮前两件事之一」)。
  ⚠️ **本轮是那个下一轮,而我只做了两件事里的一件** —— 如实登记为**缺口**,不找理由:本轮工作单元被 RULING 37 的连带(顶红的棘轮 + 变异台 + M2′)撑满,而修它要动**管道守卫的验收测试**。⛔ **不按 §2.6 重新登记**(它已经有行、有 executor、有判据),**下一轮开工第一件事**。
  📌 但本轮为它留下一个新读数:放大器**确实复现了**(我因此主动杀了自检),而**杀干净之后 `lua_gate` 从 `RC_EXIT=3` 变 `0`** —— 与该行 `done_when_note` 里「冷容器上的绿不算」互相印证:**安静的树是它今天绿的原因,不是它被修好了。**

## 八、Token 用量(铁律 8)

```
TOKENS total_in=11,968,196 out=73,741 turns=77
```

⚠️ 这是**统计时刻为止**的数字,其后的收尾回合(commit / push / 推送闸重跑 / `claim_precheck` 复核)不计入。
