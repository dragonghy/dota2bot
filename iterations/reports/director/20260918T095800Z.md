# 总监 2026-09-18T09:58Z —— RULING 77:两件事都是「真命题满足了别的问题」,一件是我自己上一轮干的

**一句话**:本轮开工自检读回 **`TRUNK RED — failed: tests/test_bots_walk_farm_only.py`**,
成因是今早 `75b0cf4b` **在同一个 commit 里**既补登了上一个未登记走查、又在那条登记的正文里
逐字复述「GH #803 说要同轮登记,而那正是被跳过的那一步」、**又落下了一个新的未登记走查**;
而**我上一轮发出去的那条限时交棒(GH #290,今天到期)本身是错的** —— 会过期的那份语料
按构造答不了它索要的那条验收线,**离线一分钟就能查出来,而那一分钟没花**。

**本轮产出**:trunk 红修复(登记 + 2/2 变异台)、两行机器可读欠条、GH #290 订正评论、
GH #548 跨轮读数续贴。
**零 EC2 / 零 CE / S3 读取 0 个对象(出网未计价)**;GitHub MCP 点查 2 次(免费)。
**`bots/`+`game/` 零 diff**;无 promote / 无退集 / 无入集 / 不发 owner 邮件 / 不发波。

---

## 〇、开工自检(铁律 10)

⚠️ **纪律 3 第一跑踩了,当场改姿势**:首条命令是 `bash tools/agent/routine_selfcheck.sh 2>&1 | tail -60`,
脚本自卫逐字 `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.` +
`it has recurred 5x, every time as the first command of the round.` ⇒ `SELFCHECK_EXIT=2 REFUSED`。
⛔ **不记成工具的问题**:章程 §0 逐字覆盖这一发,我没照做。改 `> /tmp/sc.log 2>&1` 后真跑。
⚠️ 自检再次 > 600s 被移到后台,**读数从输出文件取,不从终端取**。

**已读到的腿(静止树,本轮当时 `bots/`/`tests/`/`tools/` 零 diff ⇒ 这些读数可引用)**:

- `push gate armed (core.hooksPath=.githooks)`;`OK: no unlanded work in the certifiable window.`
- `LUA GATE COVERAGE  disk 513 | push gate 348 | 开工自检 leg 138 | known_red 5`;
  `UNCOVERED 114 of 513 (22%) {'no_manifest_row': 51, 'timed_out': 60, 'too_slow': 3}`;
  `UNCOVERED SET GREW -- 2 file(s)`:`test_fieldsip_transfer_receiving_site.lua`(上一轮总监读数记作「连续第五轮」,本轮同一条;⛔ 跨组计数不合并)
  + `test_lion_w_fight_seed.lua` —— ⛔ 与前两轮**同两个**,非本轮引入。
- `PY GATE COVERAGE  disk 151 | push gate 131 | leg 151`;`UNCOVERED 0 of 151 (0%)`。
- **`trunk health (python)` = `147 passed, 1 failed, 3 uncertifiable`;
  `failed: tests/test_bots_walk_farm_only.py`** ⇒ **本轮头条,见 §二。**
- `INVERSE-GATE CENSUS  live gate ids 255  armed 24`;`FROZEN 0  COUPLED 1  UNRESOLVED(armed) 0`;
  `promote-atom constraints: OK`;`A-EVIDENCE-OWED armed 24 verdict 24 owed-row 0 UNOWED 0`。
- ⭐ **GH #548 的跨轮读数,本轮这一份**:`NOTE 5a0 cost: 138 file(s) [discovered -- the leg
  never reached its count line] in 120.1s (budget 120s)`;同一条腿 **9 条 UNCERTIFIABLE**
  (`5a0/5a/5a2/5b/5c/5d/5e/5f/5g`),自述逐字 `this is NOT a pass`。
  ⛔ 不据此断「集合涨了」——`GH #358` 逐字:墙钟分不开集合变大与容器变慢。立案(09-06)时是 84 文件 / 120.1s。
- ⚠️ `luacheck is not installed, so the behavioural half did not run` —— 容器冷启常态,
  push 时由 `.githooks/pre-push` 的 `luacheck_gate.sh` 自己买(见 §五)。

**§1.5 交棒腿(RULING 64/67/75/76)**:`CARRY_EXIT=0`,逐字
`fetched 2026-09-18T07:14:48Z (2.7h old), 15 issues` / `scanned: 1 entry (of 96), 1 carry segment(s), 10 GH ref(s)` /
`OK  GH #856, #867, #240, #843, #859, #810, #528, #548, #290, #806` / `every carried GH ref is open.` /
**`VERDICT: OK (exit 0)`** —— ⭐ **RULING 76 落地后第二轮,仍 exit 0、零 `not in corpus`**。

**巡检(按 §2e 的逐字取法,`git fetch` 后、名字序、`origin/main`、只认报告名形状;取数时刻 2026-09-18T09:58:17Z)**:

| 组 | 最新报告 |
|---|---|
| batch-desk | `20260918T091720Z.md` |
| replay-check | `20260918T094256Z.md` |
| strategy | `20260918T075500Z.md` |
| hero | `20260918T075321Z.md` |
| director | `20260918T071500Z.md`(本轮之前) |

**五组全活,无掉棒。**

---

## 二、头条(甲):一个 commit 复述了规矩,并在同一个 commit 里再次违反它

**红的那一行,逐字**:

```
FAIL: a tests/ io.popen command cannot be resolved statically and is not on the hand-read
list -- read it, then add it to UNRESOLVED_HAND_READ with the value its callers pass
(do not delete this check): ["tests/test_wk_q_aim_preflight.lua  ::  'ls ' .. dir .. ' 2>/dev/null'"]
```

**溯源(离线,三条命令)**:

- `git log -S"'ls ' .. dir" -- tests/test_wk_q_aim_preflight.lua` ⇒ 唯一一条:
  **`75b0cf4b  2026-09-18 08:32:52 +0000  [hero] wkqaim 前置读数重取`**。
- **同一个 commit** 给 `tests/test_bots_walk_farm_only.py` 补上了
  `test_wk_q_teamfight_reach_pricing.lua` 那条登记,而**那条登记的正文逐字写着**:
  > GH #803 says register in the SAME work unit, and that is exactly what was skipped
- ⇒ **复述规矩的那次提交,就是再次违反它的那次提交。**

⭐⭐ **所以本轮不再重申 GH #803** —— 该规矩自 09-12 起在这一个普查里至少被重申**五次**
(`UNRESOLVED_HAND_READ` 里逐条的 ⚠️ 注可数),其中**过半数的登记是作者以外的人补的**
(strategy 两次、director 两次、本轮第三次)。**重申已经被实测否决了。**

**机械成因,写在这个文件自己的价签上**:

```
tools/agent/py_gate_manifest.json:
  "tests/test_bots_walk_farm_only.py": {"seconds": 4.493, "in_gate": false, "reason": "over_per_test_cap"}
  per_test_cap_seconds: 3.0   budget_seconds: 90.0   selected_total_seconds: 48.872
```

⇒ **唯一会发现「新落的走查没登记」的读者,自己在 push 闸外** ⇒ **没有任何一条 push 腿能拒绝
那次 push**。实测佐证:红由 08:32Z 引入,其后**批测台 09:17Z、录像组 09:42Z 各推过一次,两次都没被拒**,
红由**第三个开工的组(我)**买单。⛔ 这与 GH #624 立案句同型,只是换到了 python 腿上。

**本轮落地**:

1. **登记**(修红):`tests/test_bots_walk_farm_only.py:UNRESOLVED_HAND_READ` 新增
   `tests/test_wk_q_aim_preflight.lua :: 'ls ' .. dir .. ' 2>/dev/null'`。
   手读依据:`fixture_files()` 把 `dir` 循环在 `CORPUS_DIRS == {'tests/fixtures','tests/frames'}`
   上(`:153/:157/:350`),该表是**文件作用域 local、由两个字面量赋值一次、无任何写入**;
   **裸 `ls` 不递归 ⇒ 结构上够不到 `bots/Customize/`**(与上面四条同形)。
2. **变异台(evidence-discipline 1/2)**:从文件副本恢复,`sha256 -c` **OK**。
   - control(未改)= **exit 0**,`8 checks, 0 failed [307 commands executed, 76 unresolved]`
   - **M1** 删掉新条目 ⇒ **exit 1,CAUGHT**(打回原来那一行 FAIL)
   - **M2** 把登记的 key 改一个字符(`2>/dev/nul`)⇒ **exit 1,CAUGHT** ⇒ 键是逐字匹配,不是前缀
   - ⇒ **2 CAUGHT / 0 SURVIVED / control_ok=1 / RESTORE: YES**;落地后重跑 **exit 0**。
3. **⛔ 本轮不修成因**(把 4.493s 塞进 3.0s 的 cap:加速 / 拆快子集 / 动 cap 三选一,
   第三条按 GH #616 约束 1 影响每个组每次 push 的延迟,**不是顺手能做的决定**)⇒
   登记成一行机器可读的欠条:
   **`owed_executions.json:walk_census_out_of_push_gate_so_rule803_cannot_bind`**
   (executor = **总监自己**;`unmet_at_ruling` 带上面三读)。
   ⛔ **未开 GH issue**:不猜号(§EX.11 实测两次写错),开号留下一轮。

---

## 三、头条(乙):**我上一轮那条限时交棒是错的,本轮撤回** —— RULING 77

上一轮(07:29:59Z,`#issuecomment-5726747149`)我在 GH #290 上发了一条**限时交棒**:
称 `dem21-expire-21d` 使 W20–W22 **今天(09-18)**到期、W23 明天到期,请**录像组 / 批测台二选一**
今天内取语料,否则 §6 的执行核验作废。

**两处都错,而且方向不同。**

### (甲) 判据本身:会过期的东西 ≠ 验收线消费的东西

- §6 的验收线逐字是「**下一波**带 OD 的语料上跑 `skill_point_stall.py`,**OD 行不再出现在 STALL 表里**,
  且 `pts` 直方图 7–12 不再是空的」—— 这是一个**修复之后**的绝对读数,**不是差分**。
- 压实修复落地在 **`8cf5ae0c` 2026-08-29 02:45:26 +0000**;
  `git grep -c CompactSkillList origin/main -- bots/ability_item_usage_generic.lua` = **2**(已落地)。
- W23 从 **`72a8cf75` 2026-08-28 22:54:50 +0000** 发出,且
  **`git merge-base --is-ancestor 72a8cf75 8cf5ae0c` 为真** ⇒ **W23 的树严格早于修复**,W20–W22 更早。

⇒ **W20–W23 全是修复前语料,拿它们跑那条线只能复现旧读数** —— 花多少钱、多快去取都一样。

⭐ **而要保的那一半根本不会过期**:修复前的读数 08-29 就**逐字写进了 GH #290 正文 §5**
(56 局 / 560 个(英雄,局)读数 / 34 个英雄;停摆行 `pts=6`,其余 ≥13,直方图 7–12 空;
`frozen_frac` min 0.775 vs 非停摆 max 0.549)。**基线在仓库里,不在 S3 上。**

⇒ **真正欠的是**:在**任意一份修复后的、带 OD 的**语料上跑一次 `skill_point_stall.py`。
这类语料存在且在更新(录像组 09-18T09:42Z 报告逐字:**W46 `.dem` 约 09-25 到期**)⇒
**时限是那份语料自己的,不是今天**;⛔ 也**不需要批测台发波** ⇒ 刹车(MTD `$92.163` > `$90`)不挡它。

⚖️ **RULING 77(甲)**:⛔ **升级一条时限之前,先把「会过期的东西」与「验收线真正消费的东西」对一次账。**
这一次两者不是同一样东西,**而差额是一次会被当天执行的花钱请求**。
离线代价是两条 `git log` 加一条 `git grep`、**不到一分钟**;07:29Z 那条没跑,
**而它跳过的恰好是会让它自己退休的那一步**。
📌 与 RULING 75/76 同型:**一个真命题(语料 21 天到期)满足了一个别的问题(验收线还剩几天)。**

### (乙) 载体:issue 评论不在任何一个流的开工程序里,而且它点了两个名

- 07:29Z 那条写的是「**录像组 / 批测台二选一**」= **两个流**。
  `queue.json:_protocol` 里 `director.executor` 的立案句(`hero-20`)逐字:
  **两个流 = 一个都没点**。
- 实测就是这样:那之后**批测台 09:17Z、录像组 09:42Z 各跑了一轮**,
  两份报告 `grep -nE '#290|dem21|expire'` **没有一行命中该交棒**
  (命中的三行分别是 push 顺序引用与 W46 的另一条时限)⇒ **零回应、零提及。**
- ⛔ **而我上一轮给它的理由正是「这条不属于任何人的清单」** —— 我看对了病,开错了药:
  **把它从 issue 正文脚注搬到同一个 issue 的评论里,读它的人没有变多一个。**

⚖️ **RULING 77(乙)**:限时交棒的载体**必须是每组每轮真的会跑到的那条腿**,
且 **`executor` 恰好一个流**。⇒ 本轮把它登记成
**`owed_executions.json:gh290_od_execution_verification_needs_postfix_corpus`**:
`executor = 录像组(replay-check)`,`done_when = path_exists
iterations/reports/replay-check/gh290_od_postfix_stall_read.md`,
立案时刻实测 **该路径不存在**(LIMIT 14 要的那种「立案时见证为假」的单调判据)。
⚠️ **诚实边界照抄**:owed 腿自己的已知弱点写在本表另一行里 ——
「自检会一直喊,而**喊的对象是所有人,即没有人**」⇒ **`executor` 那一格是本行唯一的止损,不是装饰。**
⚠️ **owed 表没有时限字段**,时限只能逐字写在 `done_when.note` 里(**这是本行自己的已知洞**,照登)。

**`done_when.note` 里预先裁掉的一格**:若那一波语料里**没有 OD**(镜像草稿由 seed 决定,
OD 不保证出场),**不算不满足** —— 照样落文件、写明「该波无 OD 局」与所查波次,
本行保持 OWED 等下一份。⛔ **不许硬凑,也不许因此沉默。**

---

## 四、本轮**不做**的事(逐条说明,免得读成掉棒)

- **不发波、不花 AWS**:结转批测台 MTD **`$92.163`** > 刹车 `$90` ⇒ **第四十八轮持有**。
  ⚠️ `forecast $118.091` > `$100` 仍在 `DECISIONS_NEEDED.md`,**W38(09-20)邮件带它**。
- **不代取语料**:总监不替别的组花 S3 的钱(铁律 1);本轮 S3 调用 **0 次**。
- **不修 py 闸的 cap**(见 §二 第 3 条),**不动 `test_set.md`**,**不改别组档案**(§AW.1)。
- **不开 GH issue**:无把握的号不猜。
- **不发 owner 邮件**:本周额度留给 W38(09-20)。

---

## 五、铁律 6 验证

本轮改动:`tests/test_bots_walk_farm_only.py`(python)、`iterations/owed_executions.json`、
本报告、章程。**`bots/`+`game/` 逐字节零 diff。**

- 目标测试:`python3 tests/test_bots_walk_farm_only.py` ⇒ **`WALK_EXIT=0`**,
  `8 checks, 0 failed [307 commands executed, 76 unresolved]`(落地前 `8 checks, 1 failed`)。
- 欠条工具复读:`python3 tools/agent/pending_rulings.py --owed-only` ⇒ `OWED_EXIT=3`,
  两行新欠条各自打出 `OWED`,B 行逐字
  `done_when: iterations/reports/replay-check/gh290_od_postfix_stall_read.md does not exist yet`。
- 三条 push 腿的真码见 **§七 收尾读数**(按既定取法:push 之后回读再落笔)。

---

## 六、下次触发

① GH #856 剩 9 候选 ② GH #867 ③ GH #240 余下
④ `carry_mark_prose_vs_list` 剩唯一一格 = NO-HANDOFF 改不改挂 anchor(判据二选一见 `owed_executions.json`,**都要带读数**)
⑤ GH #843 剩 (乙) ⑥ GH #859 ⑦ GH #810 待裁 1 + (乙) ⑧ GH #528
⑨ **新**:`walk_census_out_of_push_gate_so_rule803_cannot_bind` —— 开 GH issue(号在本轮之后才拿得到),
并在三条路里选一条带读数(加速 / 快子集 / 动 cap);⛔ 动 cap 要先读 GH #616 约束 1
⑩ **新**:看录像组答没答 `gh290_od_execution_verification_needs_postfix_corpus`
(⛔ 判据是那个 `.md` 在不在,**不是**它们报告里提没提;⚠️ W46 约 **09-25** 到期,别等到最后一天)
⑪ GH #806 族:`test_lion_w_fight_seed.lua` / `test_fieldsip_transfer_receiving_site.lua`(上一轮总监读数记作「连续第五轮」,本轮同一条;⛔ 跨组计数不合并)
⑫ (a) 判定完结 ≥1,第一候选仍是给 `fieldsip` 单独定价的那个工作单元;(b) 已交付,每轮打 `STALL`
⑬ P4.2 narrat 1 / `$0.90` 重裁 / patch 缺口 P3 ⑭ `path_contains_any` 那两行
⑮ `py_manifest_no_carry_baseline_port`
⑯ **W38 邮件(09-20)**:15/16/18/19 条 + 17 条已撤回 + RULING 70+71+72+73+74+75+76+**77** + ⚠️`forecast $118.091`
⑰ `lua_gate_manifest.json` 已陈旧,⛔ 重测先读 GH #810
⑱ ⛔习惯:`lua5.1 tests/test_x.lua` = 假绿,唯一入口是 `tests/run_tests.lua <filter>`
⑲ ⛔习惯:自检跑着时不要写它读的数据 / 它自己 / 它正在跑的套件里的文件(RULING 74)
⑳ ⛔习惯:变异台的 perl 锚**只用 ASCII**(M17、M23)
㉑ ⛔习惯:**本轮第一条命令又踩了纪律 3** —— `rc.sh` 或 `> 文件 2>&1`,⛔ 不加 `| tail`、⛔ 不加 `timeout`
㉒ `stayfield2` 重新入集的机器行 = `owed_executions.json:stayfield2_readmit_when_fieldsip_moves`,按设计仍 OWED,**不要当成掉棒**
㉓ ⚠️ **`UNRESOLVED_HAND_READ` 里 `tests/test_cm_lane_fallback_wallet.lua` 有两条同键条目**
(落地后 `:159` 与 `:320`,dict 静默只留后一条)—— 本轮**发现但未合并**(不在本工作单元内),
下一轮顺手合;该文件自己的注释已经写过这个手法一次

---

## 七、收尾读数(第一次 push 之后回读)

**三条 push 腿,两次 push 各一份,逐字**(⛔ 未经管道,重定向到文件后回读):

```
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 134 ran, 0 findings, 0 uncertifiable, 39.5s
lua gate: scope = 4 path(s) changed vs the pushed refs' base (base=fallback-merge-base)
lua gate: 428 ran, 0 findings, 0 uncertifiable, 8 unanswered, 5 known-red, 553.2s
```

⛔ **没有 `RULE6_BYPASS`,没有 `SKIPPED, not passed` 行。**
- `PUSH1_EXIT=0` ⇒ `* [new branch] claude/compassionate-albattani-39xfpo`
- `PUSH2_EXIT=0` ⇒ `d26f6a29..b0b6edbf  HEAD -> main`
- ⭐ **顺序按章程(先分支、后 main),memo 因此命中**:第二次 push 逐字
  `HEAD^{tree}=3a64b97f… with origin/main=d26f6a29… and a clean working tree.`,
  三条腿读数与第一次**逐位相同**(⇒ 免费),而不是重跑 553.2s。

**开工自检最终真码(后台跑完后回读输出文件,⛔ 不从终端取)**:

```
legs run              : 15
FINDINGS (exit 3)     : cadence queue-rulings owed-executions lua-coverage trunk-red(python)
UNCERTIFIABLE (exit 2): none
NOT RUN (inside a leg): tests/test_lua_gate.py tests/test_luacheck_gate_soakswitch.py tests/test_selfcheck_lua_leg.py
selfcheck worst exit: 3
```

`trunk health (fast Lua detectors)` = `138 tagged detector file(s), 0 failures -- FAST SUBSET, not the full suite.`
⚠️ `trunk-red(python)` 那一格**就是本轮修掉的那条**(§二);其余四条 finding 是既有台账。

**本轮发表的评论(⛔ 两次 push 之后才发,GH #290 顺序)**:

- GH #290 `#issuecomment-5728663999` —— RULING 77,撤回 07:29Z 的选项 1,交棒改挂 owed 行
- GH #548 `#issuecomment-5728667038` —— 跨轮 NOTE 读数第三份(84/120.1s → 138/120.0s → 138/120.1s)

⚠️ **`claim_precheck.sh` 对 #290 草稿打 `PRECHECK_EXIT=3`,两条 `OFF-TRUNK commit`
(`72a8cf75` / `8cf5ae0c`)** —— 同一次读数逐字 `local commits not on origin/main: 0`
⇒ **不是「有东西没 push」,是浅克隆**(`git rev-list --count origin/main` = 52,
最老可达 2026-09-17)。**仍然发表,并把工具的话原样贴进了那条评论**,
同时把三条关键读数改写成 main 上的**内容读数**(`git grep -c … origin/main`)。
⛔ 这一格是判断不是通过,登记在此备查。

---

## 八、Token 用量(铁律 8)

```
TOKENS total_in=9,445,253 out=58,227 turns=71
```

(到统计时刻为止;统计后的收尾回合不计入。)
