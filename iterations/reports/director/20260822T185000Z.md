# 总监 2026-08-22T18:5xZ(第四十六次触发)

**主题**:两条定级(`pullcamp` 回集但驳回专波 / `l5combo` 的 (a) 挂二十轮后正式降级)
+ 一条方法记录:**我按自检的 UNLANDED 报告去抢救协同组 17:30Z 的工作单元,
抢救到一半它的主人自己落地了,我的那份丢弃**(§3)。

**`bots/`/`game/` 的**执行行**零改动**(落地的是协同组的 gated 修法,总监未改一行 Lua);
**一次 AWS API 都没打,自身支出 $0。**

---

## 0. 开工自检(铁律 10)——报得对,但抢救晚了十分钟

`bash tools/agent/routine_selfcheck.sh`(~20s,只读):

```
UNLANDED WORK (3 commit(s)) -- committed and pushed, never on origin/main:
  17:33Z 6325e96  strategy 17:30Z addendum ...   on origin/claude/dreamy-feynman-tgfpc0
  17:27Z a053082  strategy 17:30Z: owner P1's pull camp is the wrong camp ...
  15:45Z 4e8972f  strategy 14:00Z ...            on origin/discarded/strategy-...-fieldbuy-duplicate
CADENCE  clean        TRUNK HEALTH  11 passed, 0 failed
```

- 第三件在 `origin/discarded/…` 上,**分支名自证是有意丢弃的**(14:00Z 那份 fieldbuy 重复件),
  **不动**——这正是两个工具 LIMITS 里写的「报出来的是问题不是判决」。
- 前两件在 **18:51Z 那一刻确实不在 main**:#117 的选点修法 + 11 例新用例 + 报告 + 入集提议。
  我据此开抢救——**结果十几分钟后它的主人自己落地了**,处置与教训见 §3。

---

## 1. 裁定一:`pullcamp` 批准重新入集(22 → **23 eligible**),**但驳回它申请的专波**

**批准的依据是我 16:5xZ 自己写下的回程条件,逐字兑现**——摘它的理由是
「(a) 的证据是版本专属的,#117 的修法必然改写选点代码」。那份修法现在存在了。

**我自己核的四件事(不是转述他们的报告)**:

| 核什么 | 结果 |
|---|---|
| 是不是**一个变量** | `git show` 的 Lua 差分只有**一条子句 + 注释块**;`1500` reach / 拉野窗口 / `IsLanePullSafe` 的 `>= 0.5` 血量门**逐字未动** |
| 是不是仍然 **dark** | `jmz_func.lua:7506` 的 `IsSoakCandidate('pullcamp')` 在新子句**之前**,函数首行还是 `IsModeTurbo()` ⇒ **shipped 行为零改动** |
| 门 | `luacheck bots game` **0 警告 EXIT=0**;`test_pullcamp_ownside_camp` 11/0 + `test_pull_camp` 16/0 + `test_pullcamp_trigger_census` 21/0 + `test_gate_claim_consistency` 7/0 = **55/0**(全套见 §7) |
| 承重算术 | 热点营 `(3994,-5137)` → radiant 远古 **9,916u** / dire 远古 **10,252u** ⇒ **「在我方半场」这条判据放它过**,只有「比车道中点(7,717u)更近」拒得掉 —— **三条听起来都对的几何只有一条咬得住**,复核通过 |

**驳回的那一半(`queue.json:strategy-2` 要的 4 种子专波)**:三条验收判据
——连接率、开拉点离泉水中位数、深野营 poke 帧占比——**全部是 armed 腿的臂内读数**,
不需要隔离臂;12:12Z 买到的 115:11 同样是在 **21-id 全集波**上买的。
§AF.3 裁死的是「拿波间残差判单 id」,**没有**禁止「在全集波的 armed 腿上读一个 id 自己的域内行为率」
——后者正是本项目买到过的**每一份** (a)。⇒ **搭 §AP.0 那一波顺路买,省 ≈$1.2。**
另加一条**免费正控**:两个深野营的 poke 帧占比(12:12Z 那波 **51%**)必须塌下去,
**同波自读、不跨波比较**,因此不受 §AF.3 约束。

---

## 2. 裁定二:`l5combo` 的 (a) 正式降级 —— 他们等的那条臂,**§AF.3 一天前就判死了**

录像组从第十三轮到 17:09Z 的**第二十轮**,每轮都交同一句「仍等去掉它的 bisect 臂」。
**这条等待结构上是空的,而且错在我这边**:**§AF.3(08-21T17:0xZ)已经裁定
单-id bisect 两臂差的 MDE 是 26.0 gpm、按构造只有上界** ⇒ 那条臂**即使跑了也回答不了 (a)**。
二十轮里没人把这两条裁定并排读,**包括我自己**。这是「棒子没掉、只是等在一个不存在的条件上」的新形态。

**降级后的口径(依据是 `lanekill_commit.py` 自己的设计,我读了源码不是转述)**:

1. 检测器**已经**把两个 helper 的门逐条离线转写;**角色分割**(`l1trade`=`IsCore` / `l5combo`=`not IsCore`)
   来自 `roam_conversion.load_game` 的 **`seed_draft.positions_for_game`** ——
   **就是 14:5xZ 我给 `creeppull_domain.py` 换上的那个唯一正确来源**,不是被禁的 `team_slot % 5 + 1`
   ⇒ **两个 id 的域在检测器层面本来就是分开的**,「域分不开」那条 08-19 的顾虑不适用于这个检测器。
2. **VETO 带**(`l5combo` 独有:≥2 敌人在我 700u 内 ⇒ 门恒假)= **波内安慰剂对照**。
3. **OUT 带**(`t ∈ [520,900)`,非对线期两个 helper 都硬返回 nil)= 第二条对照,
   **在这里成立**(与 `roamstale` 的 §I.8.4 不同:这两个 helper 按构造只在对线期活)。

**预登记**(读之前定死):承重 = ACTIVE 带 `commit`/`switch` 的 **ARMED A−B**(不许 DiD,§AG.2);
`creep` 率应下降;**供给底线** armed 腿 ACTIVE episode **< 30 ⇒ INDETERMINATE(供给不足),不许记 SILENT**;
**上界声明必须写进报告**(`GetEstimatedDamageToTarget` 不在 dumper 流 ⇒ 致命性/自风险不可观测,
每个 episode 都是超集,**阳性可信、零读数不可读成 SILENT**);
**`actor_hp` 分层强制**(`1.84*HP > 0.75 ⇔ HP > 0.4076` 是算术不是假说)。
**零成本**:18:06Z 那波(22 id,含 `l5combo`,`--rec-slots ≥ 8`)和 12:12Z 那波(283 局)都能直接扫。

**硬期限**:自本节起**三次触发**内交读数;**逾期 ⇒ 出 armed 集**(非 reject,gate 保留),
理由与摘 `pullcamp` 的第二条相同:**买不到自己 (a) 的 id,只会把份额摊进其余 22 个的 (b) 残差**。
到期由总监自己扫,并记进效率台账的返工项。

---

## 3. 抢救到一半,主人自己落地了 —— 我的那份丢弃

| 时刻 | 事件 |
|---|---|
| 17:27Z / 17:33Z | 协同组提交 `a053082` / `6325e96`,推到会话分支,**不在 main** |
| **18:51Z** | 我开工跑自检 ⇒ `UNLANDED WORK (3 commit(s))`,前两件正是这个工作单元 |
| 18:5x–19:0xZ | 我 cherry-pick 到 main,手工合两处 JSON 冲突,跑门(`luacheck` 0 警告 + 四个套件 **55/0**) |
| **19:0xZ** | **协同组自己把同一份推进了 main**(`01ab933`/`b33b796`/`ac403c0`,含 1259/0 收尾提交) |
| 19:1xZ | 我发 GH 评论前**重新 fetch**,发现主人已落地 ⇒ **丢弃自己的 cherry-pick**,只留复核结论 |

**核过才敢说的一句**:`git diff HEAD origin/main -- bots/ tests/` **空** ⇒
两份**逐字节相同**,所以我在自己树上跑出来的门对**现在 main 上的这份同样成立**,不是白跑。

**这不是「自检报错了」** —— 18:51Z 那一刻它报的是真的(那份工作已经离主干 1.5 小时,
离主人自己落地还有 10 分钟)。工具的 LIMITS 早写着「OFF-TRUNK 可能是已改头换面落地的同一份工作」,
**本轮是这条 LIMITS 的第一个实例,只是时间方向反过来:不是已经落地,是正在落地。**

**立一条规矩(比这次的结论值钱)**:**替别的组落地它的工作之前、以及 push 之前,各 `git fetch` 一次**
—— 自检报告是**开工那一刻**的事实,而抢救要花十几分钟,在这个团队的节奏下足够主人自己回来。
同一形状今天已出现两次(英雄组 18:00Z 那条 `landed concurrently, not rescued` 是第一次)⇒
**这是团队的系统性形状,不是谁的失误。** 代价量级也记下:本轮浪费十几分钟;
而**不跑自检**那次(08-22 上午)浪费的是**两整轮** ⇒ **自检照跑,只是抢救前要再 fetch 一次。**

**顺带记一个真的盲区**:cadence 腿本轮报 `clean`,**因为协同组 17:30Z 的报告文件那时也在 main 之外**
—— cadence 只数 main 上的报告,**OFF-TRUNK 的报告对它是「这一轮没写」**。三条腿都在,
只是没人把它们**并排读**(已进 backlog)。

## 4. 各组健康度

| 组 | 最新报告 | 状态 |
|---|---|---|
| batch-desk | 18:06Z | 22-id 波在飞(4 台 on-demand,种子 888/895/896/906),有产出 |
| hero | 18:00Z | 有产出(`axecull` 判 DO-NOT-WRITE + 15:47Z 那件的落地更正) |
| replay-check | 17:09Z | 有产出(`stayfield` 判 INDETERMINATE) |
| strategy | 17:30Z(19:0xZ 落地) | 有产出;**离主干 1.5 小时**后由**他们自己**落地(§3)。cadence 腿因此误报 clean —— 登记为自检的第二个盲区 |

**owner 优先项**:P1 本轮**动了两格**(修法落地 + 回集批准);P2 的三件套波次在飞 ⇒ **两条都在动,不升级**。
**成本**:本轮 $0、零 AWS 调用;MTD **$18.741**(批测台 12:10Z 自报)+ 在飞波次,未付费 refresh。
`DECISIONS_NEEDED.md` 不新增,**本周 owner 邮件仍为零**(今日周六;**效率台账明天做**,本周第一次)。

---

## 5. 下次触发

1. **周日效率台账**(本周第一次,已延一天,不许再挤);
2. **看 §AP.1 那波有没有把 `pullcamp` 串进去**(23 id;它是 owner P1 的第 4 棒,本日已五次同形掉棒);
3. **`l5combo` 三轮期限的第一轮计数**;
4. `#101` 的 (iv) + `0.40` 四处副本收敛进 REGISTRY(已延三轮);
5. 自检的两个盲区(16:5xZ 记的 NOWHERE + 本轮记的 **OFF-TRUNK 报告对 cadence 不可见**)进 backlog。

---

## 6. 我这轮自己的边界

- 我**没有**替协同组重新验证 #117 的域普查(15/12 那组数)——我核的是**修法本身**
  (一个变量 / 仍 gated / 门绿 / 承重算术),域普查的正确性由那一波的 (a) 读数去判,**不是我签的字**。
- `l5combo` 的降级**只改口径不改结论**:它现在既不是 WORKING 也不是 SILENT,是**未读**。
- **本轮 main 上没有一行是我写的 Lua**:我的 cherry-pick 已丢弃,落地的是协同组自己那三个提交;
  我留下的只有裁定文件(`test_set.md` §AP / `queue.json` / `state.json` / 本报告)。

## 7. 门(铁律 6)

```
luacheck bots game --formatter plain     -> 0 警告 EXIT=0(推送前在最终树上又跑了一次)
定向:test_pullcamp_ownside_camp 11/0 · test_pull_camp 16/0 ·
      test_pullcamp_trigger_census 21/0 · test_gate_claim_consistency 7/0  = 55/0
JSON:queue.json OK(4 条 requests) · state.json OK(231 键)
```

**诚实标注**:**本轮推送的树里没有一行 Lua/`game/` 改动**(§6)⇒ 铁律 6 的 Lua 门**无适用对象**;
上面这些是我为**复核协同组的修法**而跑的,跑在与现在 main **逐字节相同**的 bots/tests 上,所以结论可转。
`lua5.1 tests/run_tests.lua` 全套本轮**由我起在后台**;提交那一刻还没跑完,我按「不写没看见的数字」
留了空并引了协同组 `ac403c0` 的 1259/0。**[收尾追加]** 它跑完了:**1276 tests, 0 failures, EXIT=0**
(约 40 分钟)。**对得上**:hero 18:00Z 在 main 上报的 **1263** + 协同组本轮新增的 **13** = **1276**;
`ac403c0` 的 1259 是**在 hero 那批用例落地之前**量的,所以两个数不矛盾。
**这一跑的诚实边界**:它跨越了我 19:2xZ 的 `git reset --hard`(从我的 cherry-pick 树回到 `origin/main`)——
但那一刻 `git diff -- bots/ tests/` 为**空**,即被测文件在两棵树上逐字节相同,
**测试文件没有在跑的中途换过**;报告这个数时把这条边界一并写出来。

## 8. 本次会话 token 用量

`TOKENS total_in=17,366,971 out=106,202 turns=138`

(统计时刻之后还有收尾提交/推送的几个回合未计入。)
