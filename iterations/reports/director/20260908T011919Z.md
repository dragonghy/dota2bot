# 总监 2026-09-08T00:5x–01:2xZ — trunk 的 python 半边从「1 红」回到 0 红,两条红同一个根

零 AWS、零波次、零 `bots/`+`game/` diff、不发 owner 邮件、无 promote/reject。
本轮 diff **只有两个 python 测试文件**(注释 + 一条谓词加宽)。

取活依据:上一轮章程 ⑨③ 自己排的「**trunk 红逐条 + 单独复跑 python 半边**」。

---

## ① 开工自检:纪律 3 第六发,又是本轮第一条命令

`bash tools/agent/routine_selfcheck.sh 2>&1 | tail -60` 被 §22 守卫当场拒:
`SELFCHECK_EXIT=2 REFUSED (nothing was checked; this is NOT a pass)`。
守卫自己的文案写着 `recurred 5x, every time as the first command of the round`
—— 本轮是**第六发**,而且**仍然是本轮第一条命令**。**是守卫拦下的,不是我记住的。**
被拒那次没有执行任何检查 ⇒ 无并发写 `soak_side.lua` 风险。
改走 `> <file> 2>&1; echo EXIT=$?` 后跑成。

⚠️ **自检本身没跑完**:我给了 600s 的 timeout,它 `EXIT=124` 被砍。
砍之前已经打出可引用的腿,其中就是本轮的活(trunk health python 套件)。
**没打印的腿(Lua 检测器腿完整读数 / `unlanded_commits` / `citation_audit`)⇒ 那一侧本轮没人看过。**
(与上一轮 ⑪/⑫ 同因:这个容器上自检约 40–50 分钟。)

## ② 开工时本地落后 origin/main,`git pull --rebase` 后才开始读

`51ced474`(本地)→ `df8bc058`(远端)。**两条红都在 `df8bc058` 上复现**,
所以下面说的「trunk 红」是**远端 main 的红**,不是我工作树的红。

## ③ 红 A:`test_bots_walk_farm_only.py` —— 新语料 walk 没付手读

- 归属:`tests/test_lion_ult_cash_weakest.lua`,**英雄组 2026-09-07T23:18Z `10b474b9`** 落地。
- 形状:`io.popen('ls ' .. dir .. ' 2>/dev/null')` 静态解析不了,且不在 `UNRESOLVED_HAND_READ` 上。
  **这是那个 ratchet 设计成要收的过路费,不是它坏了**(它的 LIMIT 2 逐字写着
  「A new walk therefore costs a hand read, which is the whole point」)。
- **我付了手读**(不是照着姊妹条目抄):`corpus_paths()` 在 `:156-158` 写
  `for _, dir in ipairs({ FIXTURE_DIR, STAGED_DIR })`,两者是 `:136-137` 的**文件内字面量**
  (`tests/fixtures` / `tests/frames`);唯一调用点 `:249` **不传参**。⇒ `bots/` 不在枚举里。
  该文件另一条 popen 在 `:473`(`grep -rl "lionultcash" bots`),`CAND` 是 `:130` 的字面量
  ⇒ **静态可解析,由执行来量,不归这张表管**。
- 修法:按检查自己开的方子加一条 `UNRESOLVED_HAND_READ`,**不删检查**。
- 读数:`8 checks, 0 failed [217 commands executed, 19 unresolved]`,`RC_EXIT=0`。

## ④ ⭐ 红 B:`test_mutstand_restore_trap.py` —— **这一条的 finding 是假的**

- 归属:`tools/agent/mutstand_outpost_block.sh`,**录像组 2026-09-08T00:58Z** 落地。
- 报的是 `FAIL ... verifies its restore [a stand that cannot prove it put the tree back
  may have eaten the fix]`。**但那台变异台是验证了的** —— `:174-179` 先 `restore`,
  再 `git diff --quiet -- "${TOOL#$REPO/}"`,脏了就打
  `restore: DIRTY -- do not trust the run` 并 `exit 1`。
- 根:检查体是 **`"sha256sum" in src`**,一个字面串。
  ⭐ **这正是同一个文件在它下面第二条检查里已经修过一次的形状** —— 那条注释逐字写着
  「THE APPLIER IS NOT ALWAYS CALLED `apply_mutant`… **this repo's recurring shape:
  a detector that sees one spelling of the thing and calls the other spellings absent**」。
  **同一份文件,同一个病,隔了 5 天在隔壁那条检查上原样复发。**
- 而且被判死的那个拼法**更强不是更弱**:hash 比对证明的是**台子自己的备份往返一致**,
  `git diff` 比的是 **index** ⇒ 连「备份本身就是从已变异的文件取的」也一起抓。
  它只会朝**吵**的方向错(在脏树上开跑会报 DIRTY 并 exit 1),不会朝**静**的方向错。
- 普查(加宽当时):94 台里 **93 台 hash / 1 台 git diff / 0 台什么都不证**。
- 修法:**按拼法加宽,不是把门放松**。两支仍然都要求一次**指名被改文件的逐字节比对**;
  只调 `restore` 然后自称成功的台子**仍然是 finding**。失败文案现在**把两种被接受的拼法写出来**,
  下一台的作者不必再撞一次。
  非空泛证据(直接对谓词跑的):`hashes→True` / `gitdiff→True` /
  **`proves nothing (restore only)→False`** / **`git diff` 少了 `--quiet`→False** / `empty→False`。
- 读数:`mutation stands: every one restores the tree on any exit`,`RC_EXIT=0`。

## ⑤ 复跑 python 半边(章程 ⑨③ 的后半)

`bash tools/agent/rc.sh bash tests/run_py_tests.sh` ⇒ **`111 passed, 0 failed, 1 uncertifiable`**。
⛔ **`RC_EXIT=2`,不是 0,所以我不说「绿」** —— 2 是本仓库的 could-not-run,不是通过。
唯一那条 uncertifiable 是 `tests/test_selfcheck_lua_leg.py`:它的 Lua 检测器腿
**`86 file(s) ... in 120.1s (budget 120s)`** 顶着预算超时,9 条检查没跑成。
**本轮之前就在(GH #358),不是我引入的**,而且是**上一轮 84 文件之后的又一次增长** ——
⚠️ **这条预算正在被语料自然增长挤爆,它会越来越常态化地 uncertifiable**;
`NOTE` 行两个数(集合大小 / 容器速度)分开登记正是为了看这个,**本轮的读数是集合在涨**。

## ⑥ ⭐⭐ 结构性发现:这两条红,只有总监看得见

两条红都是**别的组落地一件正常产物时,撞响了一条 ratchet**,而且:

- 铁律 6 的 push 闸(`.githooks/pre-push` → `luacheck_gate.sh`)**只管 Lua 静态那半**;
  **python 那套 ratchet 不在任何 push 闸里**。
- 于是它们只能等**总监开工自检**的 `trunk health (python test suite)` 腿。
  五个组里只有总监跑那条腿 ⇒ **平均要红上小半天**。实测:
  红 A 在 main 上红了 **约 2 小时**(23:18Z → 01:1xZ),红 B **约 20 分钟**(00:58Z → 01:1xZ),
  而这只是因为我恰好在 01:00Z 触发;上一轮(09-05)那次是 **7 条同时红**。
- 两条涉事的 ratchet 都**很便宜**:`test_bots_walk_farm_only.py` **实测 1.88s**,
  `test_mutstand_restore_trap.py` 同量级。
- ⇒ 处方(**本轮不实现**,它动的是五个组共用的那道闸,值得单独一轮带验证):
  把**快的** python ratchet 挂进 `.githooks/pre-push`,**不是**整套 `run_py_tests.sh`
  (后者含 120s+ 的 Lua 腿,进不了钩子)。判据必须机器可读:按**单文件实测耗时**取,不按名字。
- 已开 issue(见 ⑧)。

## ⑦ 巡检 / 成本

- **零 AWS ⇒ 不对 MTD 作任何新声称。**
- owner 邮件本周未发(上一封 08-31),**本轮不发**:两条 harness 红修绿不到 owner 决定类的门槛。
- `DECISIONS_NEEDED` +0。
- **patch 检查未做**(低频顺延,已连续顺延多轮 —— 记在这里以免它悄悄消失)。
- 自检未跑完 ⇒ **registry / cadence / stable 锚点 / armed 串本轮无新读数,不作声称**(见 ①)。

## ⑧ 下次触发

1. ⭐ **⑥ 的处方**:快 python ratchet 进 push 闸(单独一轮,带验证)。
2. **GH #358 / `test_selfcheck_lua_leg` 的 120s 预算**:⑤ 量到集合从 84 → 86 还在涨,
   这条 uncertifiable 会变成常态。要么抬预算要么把发现集分片 —— 得有人裁。
3. 上一轮 ⑨ 原样顺延:`text_absent_done_when_kind`(GH #523,**先写认领再开工**)、
   GH #517、`kind:"ruling_request"` 轻量 queue 行、GH #454/#487/#460/#473 乙/#489/#486/#496、
   GH #513/#514、GH #449/#410/#436/#285、**patch 缺口 P3**、`ckpush`(有时限)。
4. **串行重跑一次完整自检**(本轮和上一轮都没跑完;别再给它 600s 的 timeout)。

## ⑨ 铁律 6 / GH #290 合规

- 本轮 diff **零行 Lua**(两个 `.py` + 本报告 + 章程)。
- `.githooks/pre-push` 的静态门在 push 时自跑,读数抄在下面。
- **GH #290(push 排在发表引用之前)本轮遵守**:先 push,再开 issue。

## ⑩ token 用量

见文末追加。
