# 总监 2026-09-09T04:24Z —— `pulllane` 与 `pullthink` 退回出集(39 → 37);`narrat=1` 一档清空

**本轮最该被读的不是裁定,是:同一个 fixture 加载器里同时住着两种失败模式,而只有一种会举手 ——
而会举手的那一种,就是前三轮各烧掉一整轮才挖出来的那三条的解药。**

零 AWS、零波次、**`bots/`+`game/` 零 diff**、不发 owner 邮件、`DECISIONS_NEEDED` +0。
取活依据:上一轮「下次触发」的 **①**(逐字点名 `verify_coverage.py` 的 `narrat=1` **仅剩这两条**,
并逐字警告 `pulllane` 的门**带空格**)。
裁定全文档案 `iterations/streams/test_set.md §GF`(§GF.0–§GF.7);
机器键 `state.json:pulllane_RETURNED_20260909` / `pullthink_RETURNED_20260909`。

---

## 1. 判定(完结 2,达 owner P4.2 的 ≥2)

| id | 裁定 | (a) 为什么买不到 |
|---|---|---|
| `pulllane` | **退回出集**(39 → 38) | 转轴子句 `J.IsCampBesideLane` 在语料上**一次也没被调用过**;真正的墙是 `GetLaneFrontLocation`,加载器**拒答**(GH #61) |
| `pullthink` | **退回出集**(38 → 37) | **两个操作数各自独立地死**:作用域项与 `pulllane` 同因;节流项 `GetAnimActivity` 落 `^Get -> 0` 兜底,1100/1100 读 0 |

⛔ **退集不是 reject**:两条的 gate、helper、`PULL_CAMP_LANE_GAP = 1200`、`pullthink` 的两个半边
**逐字保留**,`bots/`+`game/` **零 diff**(`[3c]` 钉住)。两条的逻辑依据 (c) 都成立且未被取代
(`pulllane` = GH #117 的拖拽实测;`pullthink` = GH #186 的「42% 的 poke 帧从不下拖拽令」)。

armed 串 **39 → 37**(目标 ≤20),354 字节 → **335 字节**,md5 `433070c6…` → **`b525d51d…`**。
⭐ **`verify_coverage.py` 的 `narrat=1` 档从四条降到零。**

---

## 2. ⭐⭐⭐ 主轴(§GF.3):两种失败模式,一个加载器,只有一种举手

| 输入 | 加载器行为 | 后果 |
|---|---|---|
| `GetLaneFrontLocation` | **拒答并自报家门**:`LOADER REFUSES … unresolved (GH #61). The dump does not carry lane fronts; do not compare against (0,0,0).` | 9 帧当场报错,**看得见** |
| `GetAnimActivity` | **静静答 0**(`bot_api.lua` 的 `^Get -> 0`) | 1100/1100 读 0,杠杆静默失效,**没人举手** |

**同一个加载器、同一类缺失数据、相反的失败方向 —— 而拒答那一种就是解药,并且已经实现好了。**

连续三轮的主轴都是「一次没有推广的修复」:§GD.5(`AbilityDamage` 无条件装、紧邻的
`AbilityCastRange` 没装)、§GE.3(`GetItemSlotType` 修了、`GetCurrentActionType` 没修)。
**本轮升级了这个诊断**:漏掉的不再是一个姊妹 getter,而是**一条加载器只对一个 getter 执行、
对其余一概不执行的策略**。

⇒ **可迁移处方**:凡是有 gate 压在上面的 getter,加载器应当**拒答**(像 `GetLaneFrontLocation`),
**不是答 0**(像 `GetAnimActivity` / `GetCurrentActionType` / `AbilityCastRange`)——
而这三条正好是前三轮各自的主角。

---

## 3. ⭐⭐ 第二件(§GF.4):源码注释自己的理由已经过期

`bots/mode_roam_generic.lua:210-211` 逐字写着
「ACTIVITY_* are undefined globals under the mock, so utils.lua builds meaningfulActivities
as an **EMPTY table**」,并把它当作「this line reads false locally」的**两条独立理由之一**。

**实测为假。** `api.install` 把未知 ALL_CAPS 解析成 ≥1001 哨兵 ⇒ `ACTIVITY_RUN` **1153**、
`ACTIVITY_ATTACK` **1154**,那张表是**满的**。判据是唯一能定案的那种:
**注入一个真哨兵,节流项当场翻 true**(`[2c]`)。

**结论活着,两条理由死了一条** —— 纪律 4 的野生标本(对的答案骑在一条**已经不成立**的理由上)。
⭐ 实际后果:表既然是满的,`GetAnimActivity` 就是加载器**够得着**的 getter ⇒
`pullthink` 的节流项是这四轮挖出的四个仪器缺口里**最便宜的一个,dumper 一行都不用动**。
`[2c]` 特意断言那句过期注释**仍在源码里**,所以**修好它的那天这条断言会自己变红** ——
断言与主题一起退休,不会悄悄活得比主题久。

---

## 4. ⚠️ 第一个假设是错的,是**阳性对照**说的(§GF.5)

`GetNeutralSpawners()` 在 **110/110 帧为 `{}`**(0 个营地句柄),于是「营地名册就是那堵墙」
自己送上门 —— 而那**正是 §GE 判 `campsel` 用的同一堵墙**,顺手复用极其诱人。

**阳性对照否掉了它**:同帧、全上门、强制 turbo,**再喂一个距 bot 600u 的合成己方营地**,
读数**纹丝不动**(non-nil **0/1100**,`J.IsCampBesideLane` 被调用 **0 次**)。

**归属清单(全 1100 句柄)**:dead **79** / `IsCore` **929** / 时间窗 **67** / :12:42 刻 **14** /
800 内有敌 **2** —— 五条**诚实的域过滤**;`nLane == nil` **0**(**GH #648 的论点实测成立**);
**够到 lane front 的恰好 9 条,而这 9 条上加载器全部拒答**。

⇒ **语料里能把问题问出口的只有 9 帧,仪器在这 9 帧上全部拒答。**
这个 9 同时是给买单人的报价:**一笔小而准的采购,不是全语料改造**。

---

## 5. 顺手修掉的两条 trunk 红([harness],章程 2a)

1. **`test_coarmed_attribution_register.lua` 的 arm 串断言是错的形状** —— 它写作 `n >= 40`
   (「the set was 45 on 2026-08-29」)。它想抓的是**短读**(解析器只匹配到半行,悄悄缩小 join),
   **而地板表达不了这件事**:armed 集**本来就该缩** —— owner P4.2 的目标是 `armed <= 20`,
   连续四轮裁定(§GC/§GD/§GE/§GF)就是在往那儿走。
   ⇒ **这条断言是在为「团队照 owner 说的做」而变红**:开工自检里它**在 39 上已经是红的**,
   我这轮会把它推到 37,再往下 30、20 只会更红,而解析器**从头到尾没坏过**。
   改成它那句话真正描述的检查:**独立于 `parse_arm` 数一遍 arm 行上的逗号**,要求两者相等。
   短读被精确抓住,而集合合法地缩小不再误报。**变异验过**:让解析器丢掉最后一个 id ⇒
   `SHORT READ: parse_arm returned 36 … the arm line carries 37`,还原后 11/0 绿。
2. **`test_corpus_scale` 当场逮到本轮自己的新文件**(`assert(nFrames == 110)`,GH #106/#127 的耦合面),
   已按它指定的 `tests/corpus_scale.lua` 改写(`corpus`/`universal`/`ratchet`);
   **零 claim 保持等式**(模块自己写明 deliberately not softened)。检测器复跑 **10/0**。

另修一条**我自己触发的**红:`test_blind_a_roamidle_campsel.lua [1d]` 写作
「exactly one test **writes** it」而实现是裸 `grep -l` ⇒ 本轮 §GF 文件**只在注释里交叉引用**了
`GetCurrentActionType` 就把它打红了。改为用该文件自己的 `codeOnly()` **在代码里数**。
**针脚比它要钉的东西宽,与 M8 / §GD-M6 / §GE-M5 同形,本轮第四次。**
§GE 的变异台复跑 **12 CAUGHT / 0 SURVIVED**,确认没被削弱。

---

## 6. 量具与自查

* `tests/test_blind_a_pulllane_pullthink.lua` —— **13 checks / 0 failed**
* `tools/agent/mutstand_blind_a_pulllane_pullthink.sh` —— **13 CAUGHT / 0 SURVIVED / control_ok=1**
  (五文件还原走树外副本 + 每轮 `git diff --quiet` 校验)
* 语料 sweep `tests/_blind_a_pulllane_pullthink_sweep.lua`(110 fixture / 1100 句柄)
* ⚠️ **M8 第一次 SURVIVED,而它是对的、断言是错的**(纪律 2,**本轮第四次**)。
  `[1e]` 原本**把 helper 的五条域过滤逐条抄进测试**再数存活者;M8 把 helper 自己的拉野窗口
  6min 拉到 12min,测试**纹丝不动地绿着** —— **抄来的域看不见域在动**。
  改成**驱动真 helper、数够到拒答的帧**(够到拒答 ⟺ 通过全部过滤),一个字都不抄,当场 CAUGHT。
* ⚠️ **新形状的纪律 3,登记备查**:`lua5.1 <sweep>` **退出 0 且零输出,那不是通过** ——
  mock 的 `api.install` 把 `_G.print` 换成 no-op(引擎没有控制台),于是 print 式 sweep
  **长得和干净跑完一模一样**。改走 `io.stderr` 才见读数。
  这不是「管道吃掉退出码」,是**被测环境吃掉 stdout**。
* ⛔ **「后台包装吞掉真码」第九次兑现**:开工自检第一条命令走管道被 §22 守卫当场拒(纪律 3,
  第三十二发);改重定向后真码 **2 (REFUSED)**;加 `timeout 400` 重跑真码 **124**;
  最终后台跑完真码 **3**。守卫**仍未立**,顺延。

## 7. 门(铁律 6)

* 静态半:`bash tools/agent/rc.sh bash tools/agent/luacheck_gate.sh` ⇒ **GATE_EXIT=0**,
  `luacheck bots game` **0 警告**。
* 动态半:**全量套件未跑**(~100min,GH #124),**不作声称**。`bots/`+`game/` **零 diff**。
  跑了针对性 7 份:`test_blind_a_pulllane_pullthink` 13/0、`test_blind_a_roamidle_campsel` 12/0、
  `test_blind_a_cmrguard` 11/0、`test_blind_a_wandlimbo_tpdead` 11/0、
  `test_coarmed_attribution_register` 11/0、`test_corpus_scale` 10/0、
  `test_gate_claim_consistency` 16/0。
* ⭐ **push 闸抓到本轮裁定漏掉的一步,这是它第一次替总监抓 bug**:
  `tests/test_arm_since.py` 报 **STALE ROW: `pulllane` / `pullthink` has a row but is not in
  the armed string; a 退集/promote must stamp `retired_at` on its row** ——
  我更新了 arm 串、`state.json`、owed 行、档案,**唯独漏了 `iterations/armed_since.json` 的退休戳**。
  ⚠️ 值得记下的是**它为什么会漏**:退集的动作散在**五个文件**里,而其中只有这一个有机器检查;
  §GE 那轮做对了,靠的是照着上一轮抄,**不是靠有东西举手**。补戳后 **18 checks / 0 failed**。
  ⇒ 这条进「下次触发」:**退集/promote 应当有一张单一的清单或一个脚本**,而不是五处手工同步。
* 开工自检真码 **3**:`cadence` / `owed-executions` / `trunk-red(python)` / `trunk-red(lua)`。
  其中 `trunk-red(lua)` 的三条本轮**修掉两条**(见 §5);剩 `test_fieldsip_magnitude.lua`
  (corpus sweep 子进程超时,**与本轮无关,未动**)。
  `trunk-red(python)` 再撞 **GH #358 的 120s**,9 项打 `UNCERTIFIABLE` ——
  **未核验不是通过**,那一侧这轮没人看过。

## 8. 成本

**不作新声称**,转载批测台 2026-09-09T03:11Z 报告:**MTD $66.105**,
围栏 $60 / 刹车 $90 / 批准线 $100 —— 本轮零 AWS 调用,三条线均未跨。

## 9. 各组健康度

| 组 | 最新报告 | 状态 |
|---|---|---|
| batch-desk | 03:11Z | 正常(W59 已发,4x spot,4/4 AZ) |
| hero | 02:02Z | 正常 |
| strategy | 01:35Z | 正常 |
| replay-check | staged 20260908T2200Z | 正常 |
| director | 本轮 | 正常 |

## 10. 下次触发

1. ⭐ **`narrat=1` 已清空 ⇒ 换判据取活**:从 `verify_coverage.py` 的 `narrat=2` 四条
   (`liondrainstop`/`ownhalf`/`pulldrag`/`tpgap`)清起,仍按 P4.2 的 **≥2 判定完结**。
   ⚠️ `pulldrag` 与本轮两条**同属拉野家族**,大概率同一堵 lane-front 墙 —— **先查再裁**,
   不要把 §GF 的结论直接搬过去(本轮 §GF.5 就是这么差点搬错的)。
2. ⭐ **把 §GF.3 的处方落成一条检测器**:枚举「有 gate 压在上面、而加载器答兜底 0」的 getter。
   四轮四条(`AbilityCastRange`/`GetCurrentActionType`/`GetAnimActivity` + `camp.cattr`)
   都是一条一条撞出来的,**这类东西该被普查而不是被撞见**。与 backlog 99(桩转轴普查)是同一件事,
   本轮是它的**第四个实例,仍未落地**。
3. **给「后台包装吞掉真码」立守卫**(第九次,顺延)。
4. GH #358 的 120s 预算要人裁(顺延)。
5. `hero_domain_scan` 九份读数逐份读通(顺延)。
6. **为两处仪器缺口开 [harness] issue**(本轮 MCP 未试,owed 行已登记,issue 号仍空):
   `pulllane_lanefront_instrument` / `pullthink_animactivity_instrument`。
7. ⭐ **把「退集/promote 的五处同步」收成一张清单或一个脚本**(本轮现场:arm 串 / `state.json` /
   `armed_since.json` 退休戳 / owed 行 / 档案 §,只有第三处有机器检查,而我恰好漏的就是它)。

## 11. Token 用量

```
TOKENS total_in=12,788,516 out=78,914 turns=79
```
注:这是**到统计时刻为止**的数字,统计后的收尾回合(commit/push/本节)不计入。
