# director 2026-09-16T13:18Z — RULING 65,外加一条活着的 trunk 红

**一句话**:GH #466 请总监在「维持豁免表」与「(a) census 上下文判据」之间二选一,搁了 13 天;
本轮**先量再裁**,量出来 **(a) 在文件粒度上一个误报都修不掉 —— 它自己把两个误报都圈在里面**,
而同一次测量把 GH #240 那条「系统性漏报」从假说变成**十一行可点名的 trunk 现存代码**。

零 AWS、零波次、`bots/`+`game/` 零 diff、不发 owner 邮件。
成本(RULING 48 三段式):**零 EC2 / 零 CE / S3 读取 0 个对象(出网未计价)**。

---

## 〇、先做的那件事:一条活着的 trunk 红(不是本轮计划内的)

开工自检打 **`TRUNK RED -- a python test is failing ON THE WORKING TREE`**,
`tests/test_bots_walk_farm_only.py`,点名 `tests/test_lion_hex_panic_level.lua`。
工作树干净(`git status --porcelain` 空),⇒ **这是 trunk 红,不是我的并发产物**。

录像组 12:45Z 报告 §10.6 已经**完整诊断并正确地拒绝代修**(`tests/` 改动会拉进 push 钩子的 Lua 腿,
他们本轮已因并发吃过一次亏),按 README 铁律 5 归 `[harness]` ⇒ **归总监**。**本轮修掉。**

- **修法**:把该 `io.popen` 连同调用方传的值加进 `UNRESOLVED_HAND_READ`(⛔ 没删那条 check,
  失败输出自己写着 `do not delete this check`)。
- **逐字读过才登记**:`tests/test_lion_hex_panic_level.lua:89-90` 两个文件作用域字面量
  (`'tests/fixtures'` / `'tests/frames'`),`:120-125` 的 `corpus_paths()` 在这两个上循环,
  **没有参数能到达该循环**,只留 `^f_.*%.lua$`;`ls` **不递归**,够不到 `bots/Customize/`。
- **读数**:修前 `8 checks, 1 failed` / `RC_EXIT=1` ⇒ 修后 **`8 checks, 0 failed` / `RC_EXIT=0`**。

⭐⭐ **这是两天里第四次由总监登记别人的 walk**(09-15 两次 wardcomma / axecallring,
09-16 两次 axebhcamp / 本条),而**这一条带来的新事实是最尖的**:

> **作者把这条义务写下来了,然后仍然没能履行它。**
> `tests/test_lion_hex_panic_level.lua:119-120` 逐字:
> 「⚠️ io.popen directory walk: this file belongs on the hand-read list of
> `tests/test_bots_walk_farm_only.py` (GH #774).」

⇒ **不是没看见。** 作者知道规矩、按号引用了 issue、照样推了一棵红树 ——
因为**这条普查 4.065s 对 3.0s 的 per-test cap,是 `over_per_test_cap`,从来不在 push 闸里**,
**他的钩子没有任何东西能告诉他**。红由下一个开工的组(录像组)一小时后读到,作者已经走了。

⇒ **GH #803 那句「落地的工作单元登记自己的 walk」是散文,而本条是散文第四张收据。**
真正的修法是欠条 `walk_farm_census_admitted_to_push_gate_or_priced`(**GH #843**),
**不是第五次提醒**。归属(`5cc18d6d` / `lionwpanic` / 11:30Z)**取自录像组 §10.6,不是我自己的 git 推断**。

---

## 一、立案:GH #466 与 GH #240 是同一个缺陷的两面

`tests/test_corpus_scale.lua` 的判据是 **`字面量 == 当前 fixture 数 N`**,
而它声称守护的命题是 GH #106 / #127 的「**追加一枚 fixture 会把这条断言顶红**」。

| | |
|---|---|
| **不充分** ⇒ 误报 | **GH #466**。且**误报率是语料规模的函数**:每落一枚 fixture,它被重新瞄准到新的那个数碰巧命中的任何 `== N`。 |
| **不必要** ⇒ 漏报 | **GH #240**。凡是「对 fixture 求和」但值 ≠ N 的 pin,它一个也看不见。 |

两者今天重合的部分**只有一个量**(`c.files`:既是语料规模,又是对 fixture 的求和)。

⭐ **该检测器的实测终身精度是 0/2** —— 它历史上只响过两次,两次都是误报
(`total == 109` 三次小兵伤害之和;`nova_damage == 110` Crystal Nova 的 KV 值)。
`NOT_A_CORPUS_PIN` 那两条**就是收据**。

⚠️ **今天 N=112,它的候选集是 0 条,它是绿的** —— 这个绿只说明「今天没有字面量碰巧等于 112」。

## 二、裁定前的测量(本轮承重部分)

### (甲) ⛔ 否决 GH #466 (a) / GH #240 方案 1 的**文件粒度**版

对今天这棵树量 `grep -ln "tests/fixtures\|scan_corpus\|corpus_scale" tests/test_*.lua`:

- **488 个测试文件里选中 400 个(82%)**;
- ⛔ **两个实测误报都在这张面里面**:`test_fieldcreep_veto.lua`、`test_cm_q_creep_aoe_reach.lua`。

⇒ 文件粒度上 (a) **一个误报都去不掉**,同时把 finding 面放宽到「82% 的文件里每条 `assert(<expr> == <整数>)`」。

📌 **失效原因一句话:上下文是文件,而缺陷长在行上。**
这个套件里几乎每个测试都加载 fixture ⇒ 文件级上下文几乎不携带信息。

### (乙) ✅ 采纳 GH #466 (a) 的**行粒度**版(issue 原文写的就是这个)

原文逐字「同一行/邻近行出现 `fixtures`、`corpus`、`#files` 之类的记号」⇒ 判据落成:

> **`<expr> == <非零整数字面量>`,且等式左边出现 `corpus` / `fixture` / `archive`。**

**它一个字都不读 N。** 于是:
1. ⭐ **误报率不再是语料规模的函数**(从不读那个计数的判据,落 fixture 没法重新瞄准它);
2. ⭐ **两条 `NOT_A_CORPUS_PIN` 按构造被拒** ⇒ 豁免表停止每枚 fixture 长一条;
3. ⭐ **GH #240 的漏报当场变成 11 行可点名代码**。

### (丙) `== 0` 豁免,且有理由

`assert(X == 0)` 主张集合为空,新 fixture 顶红它是**有意义的红**。20 条裸命中里 **8 条是 `== 0`**,余 11。

## 三、11 行现存债务(N=112 实测)

| 文件 | 行 | pin |
|---|---|---|
| `test_axe_t15_payoff.lua` | 471/475/478/480/594 | `CORPUS.axe_frames == 29` 等 5 条 |
| `test_cm_cmqreach_transit_frame.lua` | 321/369 | `corpus.max_shown == 1`、`worst_deficit == 2` |
| `test_pullchew_camp_commit.lua` | 288 | `#corpus == 3` |
| `test_salveyield_arbitration.lua` | 513 | `corpus().heal_units == 9` |
| `test_wk_bone_guard_stock_gate.lua` | 335 | `#corpus == 33` |
| `test_wk_reserve_idle_release.lua` | 291 | `#corpus == 33` |

⭐ **两条在源头核过,不是靠模式匹配相信的**:`test_axe_t15_payoff.lua:237` 的 `scan()` 与
`test_wk_bone_guard_stock_gate.lua:138` 的 `wk_corpus()` **都是 `ls tests/fixtures` 再逐 fixture 累加**。

⭐ **`test_salveyield_arbitration.lua:513` 是最干净的一例,而且正是 GH #240 的立案文件**:
同文件兄弟行 464(`cs.corpus(c.files, …)`)、484(`cs.ratchet(#corpus().pairs, 73, …)`)**已迁移**,
**只有 513 没有** —— 因为 9 ≠ 当时的 N,检测器从没点到过它。
**「不是谁忘了做,是这道门点不到」,这一行就是它的证据。**

## 四、落地形状:门今天关,存量列成债

⛔ **没有把这 11 行直接判红** —— 那会一次把 trunk 对五个组同时顶红,由下一个开工的组几小时后读到,
正是 GH #624 / #806 本仓已付过四次钱的形状(本轮 §〇 就是第四次)。

⇒ `KNOWN_RAW_CORPUS_PIN` 基线表(**按行内容索引**,与 `NOT_A_CORPUS_PIN` 同约定:
行**移动**保留、行**改了**失效变回 finding)+ **死条目守卫** ⇒
**新的裸语料 pin 当场红**,而这 11 行的债**只能被数下去,不能悄悄停在 11**。

## 五、GH #240 方案 3 本轮交付(免费那条)

两个检测器的失败文案现在**写明自己的匹配面**。原文案读起来像扫过了全部语料 pin,**而它扫的是其中一个量** ——
与 GH #171 的 `SKIP`、GH #200 的空跑 smoke 同族:
**一条看起来覆盖了全部的绿线,和一条真的覆盖了全部的绿线,长得一模一样。**

## 六、验收:变异台(⛔ 先看着它红,再信它绿)

`lua5.1 tests/run_tests.lua test_corpus_scale` ⇒ **13 tests / 0 failures / `RC_EXIT=0`**。
但**绿本身不构成接线证据**,所以四发变异,还原**逐字节 YES**(`cmp -s`),控制组前后各绿一次:

| | 变异 | 期望 | 实测 |
|---|---|---|---|
| **M1** | 删掉一条 `KNOWN_RAW_CORPUS_PIN` | 新腿红并点名该行 | ✅ 红,`salveyield_arbitration.lua:513` |
| **M2** | 新增一条裸语料 pin(`corpus_total == 77`) | 新腿红(门) | ✅ 红,`pullchew_camp_commit.lua:368` |
| **M3** | 老形状误报(`mana_cost == 112`,左边不读语料) | **新腿绿** | ✅ **新腿绿** |
| **M4** | 把一条基线条目改死 | 死条目守卫红 | ✅ 红,`baseline entry matches no line …` |

⭐⭐ **M3 是最有价值的一发,而且它一开始被我读错了**:整文件退出码是 **1**,我差点记成负控失败;
查了是**哪条腿**在红之后 —— 红的是**老腿**,逐字
`1 assertion(s) compare a literal to the current fixture count (112)`。
⇒ **M3 同时做了两件事:证明新面对这一形状免疫,并把 GH #466 的误报按需复现了一次。**
📌 教训与纪律 3 同族但不同层:那条管「退出码是谁的」,这一发管「**红的是哪条断言**」——
**一个文件级退出码不回答「哪条腿」,而我的负控问的正是后者。**

## 七、闸(铁律 6 三条腿)

| 腿 | 读数 |
|---|---|
| `luacheck_gate.sh` | `luacheck bots game: 0 warnings` / **`GATE_EXIT=0 CLEAN`** |
| `py_gate.py` | **`RC_EXIT=0`**,`the hook's REAL cost this run is 39.87s against a 90.0s budget` |
| `lua_gate.py` | 见文末(本轮超 120s 前台上限,转后台取真码) |

⚠️ **本轮开工自检的读数是干净可用的**(与前三轮不同):**在自检跑完之前我没有动过工作树**,
中途发现两个等待进程 `pgrep -f routine_selfcheck.sh` **匹配到了自己的命令行**因而永不退出
(自检其实早已结束)——**这是我的等待方式的缺陷,不是自检的**,已 kill。
自检 Lua 侧读数 **`122 tagged detector file(s), 0 failures`**,承重。

⚠️ **纪律 3 第三十六发,连续第六轮同一发**:本轮第一条命令仍是 `routine_selfcheck.sh … | tail -60`,
被 §22 守卫当场拒(`SELFCHECK_EXIT=2 REFUSED`);改重定向后**又**被第二道守卫拒
(`timeout` 祖先进程 ⇒ 会被腰斩),第三次 `nohup` 才跑成。**两道守卫,两次拦下,同一轮。**

## 八、巡检(§2e 含 (丁),`git fetch` 后取数,时刻 **12:51:20Z**)

batch-desk 0.6h / replay-check 0.1h / strategy 2.5h / hero 1.4h / director 2.6h
⇒ **五组均在 3h 内有产出,无停摆,不点名任何组。**

(戊) 本轮**不含任何 git 历史的日期推断**:#466/#240 的年龄来自 GitHub 点查 `created_at`
(09-03 / 08-27),`5cc18d6d` 的落地时刻取自录像组报告。故未 deepen;
照登本容器 `.git/shallow` 存在、`rev-list --count origin/main` = **53**。

## 九、§1.5 清单交叉读

`carry_item_issue_state.py` ⇒ `4 GH ref(s)`(#810 / #466 / #240 / #528)**全 open**,
`RC_EXIT=0`,语料 2.8h 龄。**本轮处理掉其中 ⑦ 的两条。**

## 十、下次触发

① **`queue.json` 22 条未裁 RIDESHARE**(⛔ 逐条看过再写)
② **GH #810 待裁 1**(open)+ 欠条 `lua_gate_stale_manifest_refusal_port`
③ `gh806_lua_manifest_remeasure` 的重测
④ `walk_farm` 归因读数(distinct 命令数 + 逐条耗时)—— ⭐ **本轮 §〇 是它的第四张收据,优先级上调**
⑤ 看守自检那三条 python 用例(**第十一轮**;本轮自检它们打 `UNCERTIFIABLE`,未跑)
⑥ `github_read_staleness_…`
⑦ **GH #240 余下部分**:11 行的迁移 + 方案 2(合成 fixture 判红),已登记欠条
⑧ P4.2 narrat 1 / `$0.90` 常数重裁 / **GH #528**(open)/ patch 缺口 P3
⑨ `lua-coverage` 那 3 个 `no_manifest_row` + 本轮自检新增的 2 个
  (`test_dusttower_dive_guard.lua` / `test_fieldsip_transfer_receiving_site.lua`)
⑩ **刷新 `iterations/data/issue_state_snapshot.json`**(有 MCP 的那一轮;>48h 自动降级)
