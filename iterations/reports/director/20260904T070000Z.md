# 总监 2026-09-04T07:00–07:4xZ

**开工自检报 `trunk-red(python)`,而 origin/main 上是**两条**红,不是批测台 06:30Z 报的一条。
一条修掉、一条明确退回,外加退休了一行当轮到期的 owed。零 AWS、零发波、`bots/` 与 `game/` 零改动。**

裁定全文:`iterations/streams/test_set.md §EA`。

**本轮最该被读的一句**:第二条红(`test_wave_gate_keys`)是**那个测试文件立案时逐字写下的剧本的重演** ——
W35 曾经「三把闸改名、第四把消失,同一个 commit,全程静默」;W45 是「两把闸消失,同一个 commit」。
**唯一的差别就是这个文件存在的全部理由:这一次红了。**
而它的修法**不是我的** —— 失败横幅自己写着 `Owner: the batch desk (it is the only seat that knows whether the gate ran)`。

---

## 1. 自检(铁律 10)

⚠️ **第三十三发,还是本轮第一条命令**:我先写了
`bash tools/agent/routine_selfcheck.sh 2>&1 | tail -60`,脚本**自拒**——
`REFUSED: stdout is a pipe; exit 2, nothing checked`,并逐字点名「every time as the first command of the round」。
改走 `bash tools/agent/rc.sh bash tools/agent/routine_selfcheck.sh` 重跑。

⚠️ **同轮第二个读码陷阱,而这次没踩**:重跑超时被移到后台,harness 的完成通知报
`failed with exit code 3` —— **这一次那 3 是真码**(`rc.sh` 透传被包命令的退出码,末尾没有 `echo` 覆盖它),
与脚本自打的末行 `selfcheck worst exit: 3` 对上。**读的仍然是脚本自己那一行,不是通知。**

按 GH #267 4b 照抄工具自己的归因,不手写「exit 3 全是 cadence」:

```
legs run              : 9
FINDINGS (exit 3)     : cadence owed-executions trunk-red(python)
UNCERTIFIABLE (exit 2): none
NOT RUN (inside a leg): tests/test_rc_wrapper.py tests/test_selfcheck_lua_leg.py
```

| 腿 | 结论 | 本轮处置 |
|---|---|---|
| 未落地提交 | OK | — |
| 引用审计 | 无新 `UNRESOLVED` | — |
| cadence | **FINDING**:`strategy` 09-03T19:00Z → 22:45Z **3.8h**(越线 0.3h) | 单发不升级(与上一轮**同一条**,非新增) |
| owed executions | **OWED**:`gate_i_executable_first_run`(GH #469) | **本轮退休,见 §4** |
| stable 锚点 | `stable-v1`/`stable-v2` 双双 `EXISTS/PINNED/SHIPPED` ok | — |
| trunk(python) | **85 passed, 2 failed** | **见 §2 / §3** |
| trunk(fast Lua 检测器) | 75 个 tagged 文件 **0 失败**(**快子集,不是全套**) | — |

⭐ **`UNCERTIFIABLE (exit 2): none` 是本轮与上一轮的实质差别**:上一轮两支 python 因
`lua5.1` 不在 PATH + 120s 预算而 `NOT RUN`/`UNCERTIFIABLE`;本轮它们仍在 `NOT RUN` 名单里,
但腿本身没有抬到 2。**`NOT RUN` 仍然不是通过**,自检腿序那件事(上一轮清单第 3 项)**本轮没做,仍在清单**。

## 2. ① `test_bots_walk_farm_only`(GH #476)—— 修掉

修法工具自己写在失败句里:把静态解析不了的 `io.popen` 登记进 `UNRESOLVED_HAND_READ`,**带调用方实际传的值**。

**手读**(这是那条检查要买的东西,不是形式):`tests/test_fixture_kv_getters.lua:122`
`io.popen('ls ' .. d .. ' 2>/dev/null')` 里的 `d`,取值由 `:121`
`ipairs({ 'tests/fixtures', 'tests/frames' })` 穷尽 ⇒ 属于**从不进入 `bots/` 的语料遍历**那一类,
与已在册的四条逐字同形。该文件另一处 popen(`:393` `'ls tests/fixtures'`)是常量、静态可解,不在本类。

| | 裸读 | 读数 |
|---|---|---|
| 修前 | `RC_EXIT=1` | `8 checks, 1 failed [170 commands executed, 15 unresolved]` |
| 修后 | `RC_EXIT=0` | `8 checks, 0 failed [170 commands executed, 15 unresolved]` |

**`170 / 15` 两侧逐位相同** ⇒ 动的是「这 15 条里有没有一条没登记」,**不是扫描面**
(扫描面塌掉会让「零发现」变成「什么都没扫」,那是 GH #345 的形状,这里排除掉了)。

⭐ **这条登记自带承重,没有另搭变异台的必要**:同一文件的下一条检查是
`stale = [u for u in UNRESOLVED_HAND_READ if u not in unresolved]` ——
**键打错一个字符就会立刻以 `stale` 红回来**。所以「加完变绿」在这里**同时**证明了
键匹配到一个**真实存在的**调用点,「写了一条谁也对不上的豁免」这个失效方向被文件自己堵死。

## 3. ② `test_wave_gate_keys` —— **不修,退回批测台**(§EA.2–§EA.4)

**这条不在批测台 06:30Z 的报告里,而那不是漏报**:他们自检读到 `86 passed, 1 failed`,
我读到 `85 passed, 2 failed`,差的那一条**就是他们那一轮自检之后才落地的 `W45_wave.json``。
按时序,它不可能出现在自己那一轮的读数里。

`217 checks, 2 failed`,两条都在 W45:
- `gate (iv) first half, inputs -- no harvest owed`(键名需匹配 `inputs`/`harvest`)
- `gate (iv) second half, GH #271 reclaim_blind`(键名需匹配 `reclaim_blind`)

**形状**:W44 的 `gates` 两条都在;W45 把 `(iv)` 换成**载体门**、加 `(v)` **接线门**,
而载体门与接线门**在 W44/W45 里本来就各有一个顶层键**(`carrier_gate` / `wiring_gate`)
⇒ **换进来的没丢读数,掉出去的那两把才是本轮唯一读数无处可寻的**
(W45 顶层也没有 W44 那个 `reclaim_blind_note`)。

**⛔ 别名那条路在这里不开。** 文件自己写:若是**故意改名**,修法是 `REQUIRED_GATES` 加一行别名。
本例不是改名 —— 载体门/接线门是**另外两把闸**,证据是它们与被替掉的那两条**同时**存在于 W44 记录里。
走别名 = 用一行把两把闸的记录义务注销掉,**那正是「不许编一个值把它变绿」的另一种写法**。

**⛔ 也不由总监代填,而这是判据不是推诿。** 两把闸的读数**确实存在于批测台自己 06:30Z 报告的散文里**
(收割义务在第三节:`pending,running` 为空,`seed_roster_index --build` 的 `4 to scan` 是独立佐证;
`reclaim_blind` 在第七节:**本轮没有重跑**,是上一轮预登记 `VERDICT: not blinded / NEXT WAVE: spot` 的兑现),
所以我**买得起**一次转抄。不转抄的理由是**失效方向**:转抄一旦有一处读错,产物是
**一个看起来完整、内容却是二手且错的 `gates` 记录** —— 与 `W28_wave.json`
那个「三把闸的键 + 一份看起来完整的记录」同形,而那正是这个文件立案要防的东西。
**红着比二手绿着便宜。**

**投递(§DR:终点是那一轮真正被驱动的那张表)**:这条义务**属于 W45**、不是常驻义务
⇒ 不进 `owed_executions.json`,写进 **`W45_wave.json:harvest_obligations`**
(批测台下一轮就是 **W45 收割轮**,那张表是那一轮真正被自审的清单),
裁词带一个**裸读得出**的 `done_when`:`python3 tests/test_wave_gate_keys.py` 退出码 0。

⭐ **投递之后重跑了那个测试:仍然 `RC_EXIT=1`。** 这一步是故意的 ——
裁词写进的是**另一个键**;若它把红洗绿了,那说明这条检查认的是
「文件里出现过这几个词」而不是「`gates` 里有这个键」。**它没有。**

## 4. ③ 退休 `owed_executions:gate_i_executable_first_run`(GH #469)

该行的 `done_when` 是 `kind: manual`,**没有机器键**,而它的 `done_when_note` 自己写了
裸读得出的验收句并指定「总监下一轮据此退休本行」。**本轮就是那一轮**(06:30Z 是发波轮 = 它点名的 trigger)。

**按验收句逐条裸读 `W45_wave.json`**:
- `gates['(i) >=6h routine-wave throttle']` 带的是 `wave_throttle.py` **自己打的那一行**
  `WAVE_THROTTLE: UNLOCKED (exit 0)`,**加上跑它的时刻** `2026-09-04T06:30:34Z`,
  以及两个锚点读数(`first machine up` / `last machine up <- the anchor`)——
  **不是一个手打的解锁时刻**,这正是验收句的全部内容;
- 另有一个 `(i) owed-execution discharge` 键**点名本行**。

⭐ **没有停在键在不在上**(evidence-discipline 4:相符的结论不能替代正确的理由)。
这条裁定的**实质**是「在动作时点跑,不是决定时点」——三次突破(W12 差 21s / W20 差 3m31s / W44 差 3m00–4m19s)
是同一个机制。裸读:工具跑在 **06:30:34Z**,`machines[0].launched_at` = **2026-09-04T06:30:38Z**
(该字段自带 `launched_at_source: AWS describe-instances LaunchTime, not the run_id stamp`)⇒ **工具在起飞前 4 秒**。

⚠️ **顺带记一个不影响结论的口径差,免得下一个人重推一遍**:批测台的报告与那一格都写
「第一台 1 秒后 06:30:35Z 起飞」,那是 **run_id 戳**(`spot_20260904_063035_…`);
而 `machines[].launched_at_source` 把 **LaunchTime** 钉为权威来源,读作 **06:30:38Z**。
**两种读法都把工具排在起飞之前**,所以退休成立;但两处**在同一份文件里**给了同一件事两个数。

退休后:`tools/agent/pending_rulings.py` 裸读 **`RC_EXIT=0`**(退休前该腿使它 exit 3);
`tests/test_pending_rulings.py` **148 checks, 0 failed**,裸读 `RC_EXIT=0`。
这是**第三行**通过这条腿退休的(前两行 `recslot8_baseline` / `wandbleed2_cond_a_frame`),
也是**第一行 `done_when` 完全没有机器键的**。

## 5. ⚠️ 一条结构观察(§EA.6,登记,本轮不裁)

铁律 6 的**静态门在 `.githooks/pre-push` 里**(GH #213),它跑的是 `luacheck_gate.sh`。
**python 那半不在钩子里** —— GH #124 豁免的是 ~100min 的 **Lua** 全套,没说 python。
⇒ 本轮这两条红**在结构上都不可能被 push 时刻的任何东西拦住**,唯一发现渠道是**落地之后**某一轮的开工自检。
两条红分别由英雄组(`be232487`)与批测台(`W45_wave.json`)在**各自自检之后**推出,**都是这个形状**。

是否把 python 那半(或一个快子集)接进 pre-push,需要它自己的承重(冷启耗时实测 + 假阳性面),
**列为下一单元第一项**。⚠️ 不外推:两条红都**没有**坏到 `bots/`,实例 clone 的是 `bots/`,W45 不受影响。

## 6. 成本 / 巡检 / owner 沟通

- **成本**:本轮**零 AWS 调用、零发波、零计费资源**。MTD **照抄**批测台 06:30Z 自报 **$12.647**
  (免费 `budgets describe-budgets`,快照 `refreshed 2026-09-04T04:11:48Z`),
  围栏 **$16.60**(备选全按需估值 $17.85)≤ $80。九月未到 $50 档,刹车线 $90 / 批准线 $100 未触及未动。
  ⚠️ 那张快照落在 W44 的 4.3–11.3h 滞后带内,**按构造只能算部分计入**;本轮不据 MTD 增量反推任何一波是否计入。
- **巡检**:四组本轮均有产出(batch-desk 06:30Z 发 W45 四台 spot、hero 04:56Z、replay-check 04:25Z、strategy 04:50Z)。
  `cadence` 仍是 strategy 那**同一条** 3.8h,**不是新增的一发**,单发不升级。
  `UNKNOWN STATUS` 那 20 行(`OPEN_STATES` 不认识 `admitted`/`harvested_pending_verification`)仍在清单,
  **失效方向安全**(GH #317 让未知状态计为 open 并点名),本轮不改工具。
- **owner 沟通**:本周邮件 **08-31 已发** ⇒ 本轮无邮件。`DECISIONS_NEEDED.md` **无新增**
  —— 本轮三个动作(修一条红 / 退回一条红 / 退休一行 owed)**全在自主授权内**。
  今日周四,效率台账(仅周日)跳过。
- **patch 检查**:低频项,本轮未做(`patch 缺口 P3` 仍在清单)。

## 7. 铁律 6

- **`bots/` + `game/` 零 diff** ⇒ **全量 Lua 套件(~100min,GH #124)未跑,也不声称**。
- **静态门**:见 §9 收尾追记(两条 push 各跑一次)。**未用 `RULE6_BYPASS` ⇒ 没有「SKIPPED, not passed」那一行要抄。**
- `arm_push_gate.sh` 由开工自检上膛(`core.hooksPath=.githooks`)。
- **python 侧**:见 §9。
- **引用门(GH #290)**:见 §9。

## 8. 下次触发

1. **把 python 那半(或快子集)接进 pre-push 的承重**(§5;本轮登记未裁)
2. **GH #473 甲 = §DY.6 的载体门**(域是一个英雄、文件是 generic 的 id,如 `rotscope`;
   ⚠️ 这条上一轮就排第一并且**没做**,本轮被两条 trunk 红顶掉 —— **第二次顺延,再顺延一次就写进 `DECISIONS_NEEDED`**)
3. **GH #473 乙**:按需机时常数 0.745 h/台 落在 W44 下界 0.765 之外,且它身上没有自动失效条款
4. **自检腿序**(`ensure_lua_tool` 提前;实测装 4s)/ `OPEN_STATES` 学会 `admitted`
5. GH #449(57-id 家族 8 粒并池合法性)/ GH #460 四件(等下一个 spot 波)
6. GH #410 / #436 / #285 / **patch 缺口 P3** / `ckpush` 的 filler-英雄政策(有时限)

## 9. 收尾追记

（push / 门 / 引用门 的裸读数在这一节回填。）

## 10. token(铁律 8)

（收尾时回填 `TOKENS` 行。）
