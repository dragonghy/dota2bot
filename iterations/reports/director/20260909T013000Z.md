# 总监 2026-09-09T01:30:00Z — `roamidle` 与 `campsel` 退回出集(41 → 39),判定完结 2(达标)

**本轮最该被读的不是两条裁定,而是 §3:本仓库的 fixture 加载器在 2026-09-02 就把本轮这个
机制诊断出来、命名了它、并把它修好了 —— 修的是隔壁那个 getter。这是「一次没有推广的修复」
的第二个实例,而两次的形状逐字相同。**

零 AWS、零波次、**`bots/`+`game/` 零 diff**、不发 owner 邮件、`DECISIONS_NEEDED` +0。
取活依据是上一轮「下次触发」的 **①**(逐字点名 `verify_coverage.py` 的 `narrat=1` 四条:
`campsel`/`pulllane`/`pullthink`/`roamidle`)。
裁定全文档案 `iterations/streams/test_set.md §GE`(§GE.0–§GE.7),
机器键 `state.json:roamidle_RETURNED_20260909` / `campsel_RETURNED_20260909`。

---

## 1. 判据:(a) 问的不是「决策对不对」

两条 id 各有一份**绿的、钉在真实帧上的**测试,而两份都由作者**亲手写进了该杠杆的转轴输入** ——
与 §GC.2 完全同型。这类测试问「**给定这个输入**,决策对不对」;(a) 问「**这个输入到底出没出现过**」。

⭐ 与 §GC 的差别值得记下来:**本轮两份测试都自己把这件事写在头部**。所以本节不是抓错,
是**采信它们的自述,然后去量**。`campsel` 那份写的是「The CAMP half is not in the corpus and
**is not pretended to be**」;`roamidle` 那份把它的三处合成输入逐条标成 S-A/S-B/S-C 并写明
「FREQUENCY IS UNKNOWN … That is a replay question」。**两份都是诚实的测试,只是它们不是 (a)。**

## 2. `roamidle`:三条析取在语料每一帧全假,而且**失败向关**

gate(`mode_team_roam_generic.lua:651`)只在 `bRelocated` 上开火,而 `J.CheckBotIdleState`
只经一条析取写 true:

```lua
if bot:GetCurrentActionType() == BOT_ACTION_TYPE_IDLE
or botMode == BOT_MODE_ITEM
or botMode == BOT_MODE_FARM then
```

`api.install` 把未知 ALL_CAPS 全局解析成**互不相同的 ≥1001 哨兵**(实测 IDLE **1174** /
ITEM **1021** / FARM **1017**),而未 spec 的 `^Get` 兜底答 **0**
⇒ 三条比较在语料**每一帧**都是 `0 == 非零`,**全假**,`bRelocated` **构造性不可达**。

**实测读数**(均真跑):

| 读数 | 值 |
|---|---|
| `GetCurrentActionType` 出现在几份 fixture | **0 / 110** |
| `GetActiveMode` 出现在几份 fixture | **0 / 110** |
| 全套测试里提到该 getter 的文件 | **2**(它自己的绿测试 + 本轮盲测) |
| dumper 里 `action_type` / `ActionType` / `NumQueuedActions` | **各 0 次** |

**真帧对照实验**(盲测 [1b]/[1c]):在 `f_260819_181742_ss_chase_start` 上,
不写转轴 ⇒ 助手**确实 latch 了 idle**(`idle == true`,它读得到的谓词全部成立)而 `bRelocated` **假**;
由作者写入转轴 ⇒ **同一帧**开火。⇒ **这一读数量的是仪器,不是一条死杠杆。**

⭐ **方向与 §GD 相反,而这正是它更难被发现的原因。** `cmrguard` 的兜底 0 把 veto 环**顶开**
(它**放行**了那条要了 CM 命的通道);这一条**失败向关** —— 杠杆干脆不开火,波次读数回来是
「测了,没效果」,**没有任何计数会举手**。两个都静默,**只有一个看起来像个结果**。

## 3. ⭐⭐⭐ 一次没有推广的修复(第二个实例)

`tests/mock/replay_fixture.lua:847-863`(strategy 2026-09-02 留)**逐字诊断了本轮这个机制**:

> `api.install` auto-resolves every unknown ALL_CAPS global to a distinct sentinel >= 1001.
> What was missing is the GETTER above — and unspecced, `^Get` defaults to 0, so
> `GetItemSlotType(slot) == ITEM_SLOT_TYPE_MAIN` was **`0 == 1174`**, FALSE on every frame of
> the corpus. Every branch behind one was constructively unreachable, **failing CLOSED and silently**

**同一个哨兵数字(1174)、同一种比较、同一个失败方向。**
那一轮为 `GetItemSlotType` 补了 getter;**`GetCurrentActionType` 没有补**,而一条 armed 的 id
就压在没补的那个上面。与 §GD.5(`AbilityDamage` 无条件装、紧挨其上的 `AbilityCastRange` 没装)
构成**同一族的第二例**:

> **缺陷不是没人诊断过,是诊断只落到了它被看见的那个 key 上。**

⛔ 本轮**不**顺手补这个 getter:补它会改变 110 份 fixture 上**所有**读该 API 的判决,
是一个独立杠杆,属于工作单元边界之外。登记为 owed 行 `roamidle_actiontype_instrument`。

## 4. `campsel`:营地记录不在任何一条仪器里

转轴是 `aba_site.lua` 的 `rec = camp.cattr`,喂给 `IsEnemyCamp`(读 `.team`)与
`IsAncientCamp`(读 `.type`)。

* **fixture 侧**:语料 `cattr` **0 命中**,且**它自己的绿测试在头部写明**了这件事
  (「a **DECLARED STAND-IN**」「No count in this file is claimed to be corpus data」)。
* **replay 侧**:dumper 的 `creepSnap` 机器读出并排序后**恰好是 `{t, team, x, y}`** ——
  **无 name 无 type** ⇒ 一个营地的 `.type`(ancient)从 .dem **也重建不出来**。
  与卡死 hero-32/33/36/42 的 **GH #581 是同一堵墙**。

⚠️ **SUBJECT 半边是真的**(真英雄、真等级,而 level 是两个谓词读的唯一 bot 操作数)——
所以它不是「什么都没有」,它是**两个操作数里买到了一个**。**(a) 要的是两个。**

## 5. 退集不是 reject;`slotarb` 特别核过

⛔ 两条的 gate、helper、调用点**逐字保留**,`bots/`+`game/` **零 diff**(由 [3c] 双向钉住)。
⚠️ **`pullcad` 陷阱**:`promote_atoms.json` **零次**点名这两条;两条的门行上都没有第二个 id。
⭐ **特别核过 `campsel` 与 `slotarb` 同住一个 wrapper**(`ClosestCamp`)—— 但它们是
`GetClosestNeutralSpwan` 的**两个独立实参、各占一行**,故 `slotarb` 留在集内且**功能逐字不受影响**。
⚠️ **载体项 7 → 7 逐字不变,量出来的**:`carrier_terms.py --arm` 对 41-id 与 39-id 两串各跑一次,
`TERMS` 行**逐字节相同**,`0 unresolved` 两次;`9 hero / 32 generic` → `9 / 30`(两条都是 generic)
⇒ **选种解空间不受影响**。
⛔ **W58 及更早不与 39-id 家族并池。**

## 6. 同轮义务:hero-51 裁 APPROVED-SCAN,理由逐条核过源码

自检 `queue-rulings` 腿点名 hero-51(`zusultstrand`,GH #593 / #564)。裁 **APPROVED-SCAN**
(与 hero-41..50 同档):它**不请求入集**,登记的是 P4.2 解冻后的 arm 顺序与分母。
**理由核过源码不是照抄请求**:`zusultstrand` 不在 39-id 串内;`X.nUltCashChaseRadius = 1600`
在 `hero_zuus.lua:1293`;`ultcash`/`J.IsDyingUnderAttack` 确在同文件(:1338/:1551)故分层做得出;
创建帧确已进 `test_zuus_ult_strand.lua:85` 的 `ZUUS_FRAMES`。
⭐ **widening 方向由源码结构直接成立**:`if bShipped then return true end` 排在 gate **之前**,
故 armed 只能在 shipped 的 TRUE 集上**加**,永不可能把一个 shipped 的 true 变 false
⇒ 请求方那条归因边界(负读数只能读作「多放的那几次放亏了」)**成立**。
⛔ **不**把 hero-51 挂进 `hero_domain_scan_2_30_31` 的针脚:它按自述**不是扫描请求**,
而 `tests/test_domain_scan_pass_pins.py` **两个方向都判相等**,错挂会把那条腿钉红。
下一棒登记为 owed 行 `zusultstrand_postthaw_arm_order`。

## 7. 量具与纪律

* `tests/test_blind_a_roamidle_campsel.lua` —— **12 checks / 0 failed**
  (`lua5.1 tests/run_tests.lua`,退出码 **0 未经管道**,走 `rc.sh`)。
* `tools/agent/mutstand_blind_a_roamidle_campsel.sh` —— **12 CAUGHT / 0 SURVIVED / control_ok=1**
  (退出码 **0 未经管道**)。八个文件还原走**树外文件副本** + `git diff --quiet` 每轮校验;
  收尾 `git status` 只剩两个新文件,**还原逐字节干净**。
  ⭐ **每个变异都朝同一个方向**:要么**买到**缺失的读数(M1/M3/M7/M8/M11/M12),
  要么**溶掉盲的理由**(M2/M4/M5/M6/M9/M10)—— 这个文件不会因为太严而错,
  只会因为**活得比它的主题长**而错。
* ⚠️ **M5 第一次 SURVIVED,而它是对的、断言是错的**(纪律 2,**第三次**):
  needle 写成裸串 `S-B`,而该串在那份测试里**出现四次**,于是删掉**承重的那一处**后仍绿。
  改钉**标签连同它所标注的操作数**后 CAUGHT。三个现场的**共同形状是:needle 比它要钉的东西宽**。
* ⚠️ **纪律 3 本轮两发,两次都当场被工具拦下或被我自己拦下**:
  (i) 第一条命令 `routine_selfcheck.sh … | tail -60`,§22 守卫**当场拒**(`SELFCHECK_EXIT=2 REFUSED`);
  (ii) 我一度把 `lua5.1 tests/test_roamidle_recovery_clobber.lua` 退出 0 读成「测试通过」——
  那是**模块加载成功**,一条断言都没跑(纪律 3 的第三种同形物,skill 里逐字写着)。
  改走 `tests/run_tests.lua <basename>` 后才是真读数。**不新立措辞,登记而已。**
* ⛔ **「后台包装吞掉真码」第八次兑现**:自检被移入后台,harness 报 `exit code 0`,
  **真码 3**(仅因我在命令里写了 `; echo "EXIT=$?"` 才留住)。守卫**仍未立**,顺延。
* 自检 10 条腿:`114 passed / 0 failed / 2 uncertifiable`;FINDINGS = `cadence`(strategy
  09-08 一个 3.7h 洞)+ `owed-executions`(全部是常驻提醒行);
  `trunk-red(python)` 再次撞 GH #358 的 **120s 预算** —— **UNCERTIFIABLE 不是通过**,
  **trunk 的那一侧这轮没人看过**。
* ⛔ **动态半(Lua 全量 ~100min,GH #124)未跑,不作声称**;本轮 `bots/` 零 diff。

## 8. 下次触发

1. ⭐ **`narrat=1` 还剩 `pulllane` 与 `pullthink` 两条未裁** —— 本轮预算花在买读数与建变异台上,
   **不为凑数去裁没量过的 id**。下一轮 ① 强制是它们。
   ⚠️ 顺带记:`pulllane` 的 gate 写作 `J.IsSoakCandidate( 'pulllane' )`(**带空格**),
   不带空格的 grep 会把它读成「无调用点」—— 本轮第一次搜索就踩了,当场核出来了。
2. **给「后台包装吞掉真码」立守卫**(**第八次**兑现,顺延)。
3. GH #358 的 120s 预算要人裁(顺延)。
4. `hero_domain_scan` 九份读数逐份读通(顺延)。
5. backlog 100(`text_absent` 注册时守卫)/ backlog 99(桩转轴普查)——
   ⭐ 本轮是**桩转轴普查的第四个实例**(wandlimbo / tpdead / cmrguard / 本轮两条),仍未落地。
6. **为两处仪器缺口开 [harness] issue**(本轮 MCP 未试,owed 行已登记,issue 号仍空)。

## 9. token 用量

```
TOKENS total_in=10,970,638 out=70,818 turns=77
```

(统计时刻为止;其后的收尾回合不计入。)

## 10. 收尾核验(全部退出码**未经管道**,走 `rc.sh`)

| 检查 | 读数 |
|---|---|
| `luacheck_gate.sh`(铁律 6 静态半) | `0 warnings` / `GATE_EXIT=0 CLEAN` |
| `tests/run_tests.lua test_blind_a_roamidle_campsel` | 12 tests, 0 failures |
| `mutstand_blind_a_roamidle_campsel.sh` | 12 CAUGHT / 0 SURVIVED / control_ok=1 |
| `tests/test_domain_scan_pass_pins.py` | Ran 5 tests — OK |
| `tests/test_stale_waits.py` | 47 checks, 0 failed |
| `tests/run_tests.lua test_gate_claim_consistency` | 16 tests, 0 failures |
| `verify_coverage.py` | **armed ids: 39**;`narrat=1` 现**恰为** `pulllane` / `pullthink` |

⛔ **动态半(Lua 全量 ~100min,GH #124)未跑,不作声称**;本轮 `bots/`+`game/` 零 diff。
