# 总监 2026-08-25T13:1xZ —— 我立的那条法,对它管的每一条都失效了(0/6),外加一个不存在的阻断

**第七十次触发。** 接的是章程「下次触发」清单第 ①②③ 条(#158 裁定 / `tpdeathbuy` 入集 / `pulldrag` 入集)。
本轮把三条一起做完,**而做的过程中发现它们是同一个问题的三个切片**。

**一句话**:§BB.4 说「搭车入集提议当轮批或退」,**从它写下那天起,它对每一条新到的提议都失效了 ——
执行记录 0/6**;而同一天,两张桌子**各自独立**把预算余额读少了一波,把「还剩一波」写成了「发不出去」。
**两件事同型:失效是静默的。**

---

## ① ⭐ 最重要的一条:我自己立的法,执行记录 0/6

§BB.4 立于 **2026-08-24T19:xxZ**,原文:一个自称「搭车、不申请专波、零 AWS 增量」的入集提议,
**其唯一成本是不裁**,因此总监**不得**以「不加急 / 让下一轮按顺序读」处置它。

| 提议 | 到达 | 逾期轮数 |
|---|---|---|
| `campsel`(GH #137) | 08-23T23:3xZ | **4** |
| `tbearly`(GH #157/#165) | 08-24T2x:xxZ | 3 |
| `tpgap`(GH #159) | 08-24T22:5xZ | 3 |
| `tpdeathbuy`(GH #168) | 08-25T02:xxZ | 3 |
| `zusstatic`(GH #173) | 08-25T04:xxZ | 2 |
| `pulldrag`(GH #117) | 08-25T07:5xZ | 1 |

**六条,零例外。** 而这六轮的总监报告**每一份**都在「下次触发」里写着「②`tpdeathbuy` 入集裁定」——
**知道、写下来、然后不做**,连续六轮。

**归因不是懒。** 每一轮总监都在做一件更大更显眼的事(P3/#108、抢救 UNLANDED、#158 临时规则),
而这六条的共同点是**它们不会变红**:

- `test_set.md` 的「待裁区」是**散文**,散文不举手;
- `queue.json` 的 `director` 字段为空,**在任何一份自检输出里都不出现**;
- 批测台每轮照发全集波,**armed 串少六个 id 不会报错、不会少一局、verdict 照样算得出来**。

⇒ 这是铁律 10 立法现场那句话的第二次上演 ——「**立这条的原因不是缺工具,是工具没人跑**」,
只不过这一次**连工具都没有**。

**因此本轮产出是两件而不是一件:存量(六条裁定)+ 流量(检测器)。**
只做前者,这张表下周会重新长出来。

## ② 六条裁定,全部 APPROVED_ADMITTED(成员串 29 → 35)

六条门**全部无合取依赖**(逐条核过)⇒ 不踩 §BA.2;**全部零 AWS 增量、搭界后第一波、无一申请专波**。
完整 note 在各自 `queue.json` 的 `director` 字段,档案 `test_set.md` §BF.1,并已在六条 issue 线程追评。

| id | 方向 | **总监自己加的**那一条(不是复述申请书) |
|---|---|---|
| `pulldrag` | 改向 | **存续条件**:`pullcamp` 一旦**退回/出集**,本 id 当场变逐字节 no-op 且无计数报警 —— §BA.2 的**反向**孪生(触发条件从 promote 换成退回) |
| `tpgap` | 收窄 | **反向哨兵定向**:是「撤退 TP 的**成功率**不许塌」,**不是**「按下次数不许降」—— 次数降本来就是它要买的东西 |
| `campsel` | 修 bug | 写进 **`campgrade` 未来入集裁定的前置检查**:同 arm 时 `campgrade` 支配远古那一半;它现不在成员串 ⇒ 本波两半都读得到 |
| `tbearly` | 收窄 | **生效条件**:只能读界后波次;界前读数**结构性作废** |
| `tpdeathbuy` | **加宽** | **域标注 UNMEASURED-POST-BOUNDARY**:4/966 全取自 cap=10 语料,界后密度是升是降没测过且方向不显然 |
| `zusstatic` | 收窄 | **零触发 ⇒ 记 DOMAIN-NOT-REACHED,不得记作「已测试且中性」**;同一个读数**顺带就是 `hero-12` 要的数** |

**总监独立复核了其中一条的算术核心而不是照抄**:`jmz_func.lua:3720-3729` 的 `J.GetHP` 返回
`nCurHealth / nMaxHealth`、死亡早退 0 ⇒ 值域 [0,1] ⇒ `botHP < 0.08 and botHP >= 1` **对任何实数为假**。
**确认不可满足。**

**⭐ 第四条(`tbearly`)是唯一一条「逾期反而没造成损失」的,而它给出的判据要留下来。**
申请书自评「域在 #108 落地前为零」——**那个自评当时是对的**:带是 18:00-25:00 而局时上限 10 分钟
⇒ armed 腿域**构造性为空**,当时入集只会给 bundle 添一条零腿,**正是 08-19 `creeppull`/`pullcamp`
双侧 SILENT 被当作「两个已测且中性的 id」计入 −34.59 判定的形状**。09:xxZ 的 §BE 把前提翻了面。
⇒ **判据:一条提议的「域」可以被别人的改动翻面;逾期期间世界变了时,重裁的依据是新世界,
不是补一个旧裁定。**

**另十条归档扫描请求统一 `ROUTED_ARCHIVE_SCAN`**(`hero-1/-2/-3/-4/-7/-12/-13/-14/-16/-17`,沿 §AZ.5 先例)。
**边界照抄不要外推**:这是**路由**裁定,**不是对任何一条前提的背书** —— 总监没有逐条复核它们的机制论证,
放行依据是「唯一成本是不裁」,不是「它们都对」。

## ③ ⭐ 一个不存在的阻断,已经写进章程并即将自己生效

批测台 06:17Z 报告与其章程写着:

> $45 围栏余 **$4.21 < $1.48 ⇒ 下一波按需发不出去**

**`$4.21 < $1.48` 为假。** 用批测台自己的数与公式:$40.79 + $3.05(界后单价)= **$43.84 ≤ $45**
⇒ **界后第一波过闸,余 $1.16**;第二波 $46.89 **才**不过。
**⇒ 正确结论是「还剩恰好一波」,不是「发不出去」。**

**而总监自己 10:1xZ 报告 ⑫ 第 1 条写的「界后第一波已经发不出去」同样错、同一个方向。**
**两张桌子在同一天各自把余额读少了一波** —— 这不是一次笔误,是同一个数在两处被同样地读错。

**为什么它比一般笔误贵**:它写进的是**章程**,而章程是每一个全新批测台会话开工必读的东西
⇒ 一个**不存在的阻断**会在下一轮被当作既定事实执行,而结果(不发波)**看起来完全正常**:
没有报错、没有少一局、**没有任何读数会说「本可以发」**。**与 ① 的六条逾期同病:失效是静默的。**

**⇒ 授权(已写进批测台章程与 GH #158)**:界后第一波**照发,走 on-demand,不必等 #158**;
它同时载着本轮新入集的六个 id 与 `queue.json:director-2` 的 GH #108 验收读数。**发完即停到 9-1。**
#158 的三条临时规则其余各条**一字未改**。
**两条边界**:$3.05 是**外推不是账单**(模型在旧 cap 上与实测差 9%),真实到货若越线是围栏事后接住;
**W7 滚出 12h 窗口的「凭空 $1.48」仍不许据以放宽。**

**连带修掉一处危险方向的失效**:批测台纪律(甲)把单波价写死成 `× $1.48`,§BE 之后那不再是常数
⇒ 每条界后波在围栏算术里**少记 ~$1.57**,与它自己反对的「滞后让 MTD 系统性偏低」**同向叠加**。
已改成逐波按界别取价。

## ④ 流量侧:`tools/agent/pending_rulings.py` + `tests/test_pending_rulings.py`

读 `queue.json`,列出 `status ∈ {pending,running,harvested}` 且 `director` 为空的请求,分两桶
(RIDESHARE = 请求自称搭车/零 AWS/零 EC2 ⇒ §BB.4 当轮批或退;OTHER = 其余),RIDESHARE 非空 ⇒ **exit 3**。
已进 `routine_selfcheck.sh`(开工)与 `tests/run_py_tests.sh`(每轮)。
**本轮裁定前它报 17 条,裁定后报 1 条。**

**测试是棘轮而不只是单元测试**:`tests/test_pending_rulings.py` **24 检查 0 失败**,
**9 个变异 8 抓 + 1 控制正确保持绿**(M1 `harvested` 移出开放集 / M2 `is_unruled` 只认 `None` /
M3 partition 丢一桶 / M4 rideshare 标记清空 / M5 `first_seen` 编一个日期 / M6 partition 漏过滤 /
M7 一个 armed id 的裁定被删 / M8′ 给一个开放且未裁的请求 arm 上 id / M10 成员串静默缩水;
M9 = 纯注释改动,控制组)。

**真正值钱的是它的第一条不变式**,而它不是我原本设计的那条:
**「`test_set.md` 成员串里每一个有 queue 请求的 id,那条请求必须带一个已投递的 `director` 裁定」**
—— 即**一个 id 不许在裁定未送达的情况下被 armed**。§BA.4 的立法现场是「裁定存在于散文和档案里,
但不在批测台读的那个字段上」;**一个 armed 而请求无裁定的 id 是同一形状再往前一步:波次已经在为它花钱了。**

⚠️ **写这条不变式时它当场变红,而红得有道理**:`pullcamp` 经 `strategy-1`/`strategy-2` 命中,
两者 `status=done` 且无 `director` —— 但那两条**早于 `director` 字段本身**(08-23T15:xxZ 才加)。
它们不是未投递的裁定,是**在还没有字段可投递时就关闭的请求**。⇒ 不变式收窄到**开放**请求,
**理由写进测试注释而不是悄悄放宽**。

**LIMITS 写在工具文件头,引用时照抄**:① 报的是问题不是判决(`RECEIVED_NOT_SCHEDULED` 是真裁定,
工具分不出「没裁」与「裁定属于未来某轮」);② RIDESHARE 是**文本匹配**请求自己的声明;
③ **年龄常常拿不到** —— Routine 容器浅克隆(本轮 50 commit),graft 点之前引入的请求没有首次出现的 commit,
**诚实输出是 `age=unknown` 而不是编一个 0**(backlog §6b 在 `busy-bardeen` 误判后立的那条:分不开就说分不开);
④ 它只看字段存不存在,**不看裁定对不对** —— 而这正是重点。

## ⑤ 本轮**故意**没有清掉的两条,以及为什么不用空裁定把检测器糊绿

- **`hero-6`(harvested,无裁定)**:欠的是**实质 resolve**(读数已随 W7 到货),需要三条件证据,不是路由决定。
- **`hero-5`(pending)**:一份**读数请求**绑在 `hero-8` 的 `cmboots` 波上,而 `hero-8` 已 harvested ⇒ 读数已经欠着。

给这两条一个 `RECEIVED` 就能让 exit 归 0 —— **而那正是 §BA.4 那个病的镜像**:
从「裁定既已作出又未送达」变成「**检测器既已变绿又什么都没决定**」。
**一个内容为空的裁定比没有裁定更坏,因为它还关掉了报警。** 故意留红,并指名下一轮。

## ⑥ 成本与体系健康

**总监本轮零 AWS 调用、零支出、未批准任何新支出**(界后第一波的授权是**围栏内**的既有额度,不是新增)。
MTD **$37.828**(批测台 06:17Z 自报,`budgets` 免费读数),有效 **$40.79**,$45 围栏余 **$4.21**,
$50/$80/$100 三线全未触及。

**开工自检**:UNLANDED 2 条(`08ed7c2`/`fc79986`,`4zcohy` 分支)—— **上上轮已判永久假阳**
(内容在 main 上,rebase 解过冲突 ⇒ patch-id 不同),**按指示未再抢救**,亦未重复内容侧复核。
cadence 13 条,全是 08-24 白天停摆的尾巴 + 正常间隔。trunk python 25 passed 0 failed(本轮后 26)。

**优先项核对**:**P1 球在协同组**(`pulldrag` 本轮入集,球回到批测台/录像组);
**P2 球在协同组**;**P3 验收 1 已交批测台(`director-2`)、验收 2 已交录像组**,
本轮的授权(界后第一波照发)**恰好是解开 P3 验收的那一步**。三项均无 12 轮零推进。

**`stable-v1`/`stable-v2` 的 tag 在远端仍不存在**(批测台第三轮提醒,总监**第七轮积压**)——
本轮仍未打,记在下次触发第 3 条。

## ⑦ 验证(铁律 6)

- **`bots/`、`game/` 零改动**(本轮改的是 `queue.json` / 四个 `.md` / 一个 `.py` / 一个 `.sh` / 一个新测试)
  ⇒ luacheck 与 smoke 的对象逐字节未变。**诚实边界**:全量 Lua 套件(GH #124,单会话跑不完)本轮未整跑。
- 自检 **Lua 检测器腿:8 文件 0 红**(本轮自行安装 lua5.1 后跑的)。
  **顺带核过一件本可能出事的事**:我改的是 `test_set.md` 的成员串,而 20 个 Lua 测试文件提到 `test_set` ——
  逐条看过,**全部是错误消息里的散文引用,没有一个解析成员串** ⇒ 成员串 29→35 对 Lua 侧结构上无影响。
- `tests/run_py_tests.sh` — **26 passed, 0 failed**(含本轮新增的 `test_pending_rulings.py`)。
- `tests/test_pending_rulings.py` — **24 检查全绿**,**9 变异 8 抓 + 1 控制保持绿**(清单见 ④)。
- `python3 tools/batch_test/check_armed_wiring.py --cand <新 35 串>` — **35/35 WIRED,exit 0**,
  六个新 id 全部 `direct` 各 1 站点。**这条是本轮最该跑的一个门**:成员串是我改的,
  而下一波的钱花在它上面。
- 裸 cand 串 **实测 311 字节**(29 id 时 259)。S3 key 上限 1024 **仍有余量**;
  ext4 的 255 早在 29 id 时就跨过去了(GH #167),本轮**没有让它变得更坏**,只是把数写进了 §BF。
- `queue.json` 改动做了逐字段 diff 断言:**除 `director` 外所有字段与顺序逐字未动**。

## ⑧ 下次触发

1. ⭐ **`hero-6` 的实质 resolve**(读数已随 W7 到货)+ **`hero-5` 的读数**(绑在已 harvested 的 `hero-8` 波上)
   —— 这两条是 `pending_rulings.py` 现在唯一还红着的东西,**清掉它就是 exit 0**。
2. `[bug] #156` / `[harness] #161/#152/#169`(#167 已由批测台降级,不阻塞收割)。
3. **`stable-v1`/`stable-v2` 打 tag**(第七轮积压,批测台第三轮提醒)。
4. backlog §6b:「内容侧复核」正式升级为惯例 + 追加「UNLANDED 行并排打印该 ref 最新报告的自述状态」。
5. W35 周日(08-30)邮件汇总 —— `DECISIONS_NEEDED` 第 9b 条(#158 的两条反向更新)是本周新增的重点。

---

**TOKENS total_in=8,381,660 out=70,214 turns=55**
(统计时刻在收尾之前,收尾回合不计入;当量级参考。)
