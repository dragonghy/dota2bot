# 总监 2026-09-04T13:11Z — 一次入集点亮三行,而三行是三个不同的答案

**一句话**:本轮真正的产出不是那三行登记,是**变异台上的一次自我更正**——
M5 与 M7 第一版就报 `CAUGHT`,而**抓住它们的是两条源码形状检查,本文件头部广告为
承载这两个 claim 的那两条驱动,在变异过的树上双双通过**。「变异台全 CAUGHT」是一句
关于集合的话,不是关于每一条检查的话。

零 AWS 调用、零发波、零计费资源。`bots/` + `game/` **逐字节未动**。
裁定全文 `iterations/streams/test_set.md §EE`。

---

## 一、自检(铁律 10)

⚠️ **第三十四发,又是本轮第一条命令**:`routine_selfcheck.sh … | tail -60` 被脚本自拒
(`REFUSED: stdout is a pipe … 已复发 5 次,每次都是当轮第一条命令`)。改重定向重跑。
上一轮(10:00Z)是改完措辞后第一轮没踩;**本轮又踩了** ⇒ 与两轮前的结论一致:
**这是习惯不是门**,而且措辞改了两次都没改掉它。

**真码读脚本自己那行**:`selfcheck worst exit: 3`。
⚠️ 后台完成通知报 `exit code 0` —— **那是我拼在命令末尾的 `echo` 的码,不是自检的**
(同轮第二个读码陷阱,认出来了)。

`exit sources`(**照抄工具自己那行,不手工归因**):

```
FINDINGS (exit 3)     : cadence owed-executions trunk-red(python) trunk-red(lua)
UNCERTIFIABLE (exit 2): none
```

已跑完的腿:`stable-v1/v2` 锚点 `EXISTS/PINNED/SHIPPED` 全 ok;`unlanded_commits` 无 OFF-TRUNK;
`owed executions` 1 行 `OWED campgrade_wave_type_premise_reading`(GH #480/#241,
trigger 是 `campgrade` 独占波,**它此刻还不存在 ⇒ 未到期,本轮不认领也不退休**);
`queue-rulings` **零行欠裁**(上一轮结清的三行仍是 `ruled=True`)。

---

## 二、本轮做的:GH #486(`[bug]`,批测台 12:15Z 交总监/协同组)

### 2.1 为什么它抢在 GH #473 甲前面

批测台 12:15Z 的 trunk 红 **红在 W46 本波刚 armed 的 `campbind` 上**,而 `campbind` 是
**我上一轮(§EC.1)自己裁进去的**;`test_set.md` 入集节**只有总监能碰**(批测台报告里
自己写了「本台不自行改入集节,也不撤 id」)。W46 正在飞,
`creepthink`/`pullcad`/`pullthink` 的逐 id (a) 解释权悬在这条上。

⇒ 抢占成立。**代价是 GH #473 甲第四次被顶掉,已按我上一轮自己写下的条款
写进 `iterations/DECISIONS_NEEDED.md`**(见第五节)。

### 2.2 三行,三个不同的答案(全文 §EE.1)

| 行 | 判定 | 一句话依据 |
|---|---|---|
| `creepthink > campbind` | **空** | 与已在册的 `creepthink > pulldrag` 同一条闭式蕴含(调用点在同一个 camp-pull 分支);本轮**在 campbind 自己的调用点上驱动**,不是继承 |
| `pullcad > campbind` | **空,且是升级** | 兄弟行 `pullcad > pulldrag` 在册状态是 WIDE「读过并判断」——**判断,不是驱动**;本轮驱动了它,只多花一次现成 harness 调用 |
| `pullthink > campbind` | **真,且单调** | 本登记器里**第一条「真且单调」**:只有 :264 的门控提前 return 够得着(只加不减),0.5s 起手保持够不着,因为 `local hPoke = …` 坐在 `if bCampHere` 分派**之上** |

**同 90 帧并排量的读数**:drag 点 **89 → 75**,poke 点 **90 → 90**。

### 2.3 ⭐ 最尖的一条合流**反着跑**,登记器的键按构造印不出它(§EE.2)

armed `campbind` 答 nil ⇒ 不发 `Action_AttackUnit` ⇒ **`bot.campPullAttackTime` 不被写** ⇒
poke 臂自己的 `campPullAttackTime == nil` **永远认领每一帧** ⇒ pullthink 的起手臂
**和它下面的 drag 臂再也走不到**。裸读数:两个 id 同时 armed、野怪在计划营地 1500u 外,
**drag 计数 75 → 0**,90 帧**一条指令都没发**。

⇒ **`pullthink` 自己的 (a) 被「`campbind` armed 在旁边」压低**,方向是
`outer > inner` 这个键**印不出来**的那一边。

⚠️ 那个「站着不动」**是有界的,不是卡死,而且上一轮就裁过**(§EC.1:选点器窗口
⇒ 上界 15 秒、只在 1:00–6:00)。本轮把形状**钉进测试**,**不是重开那条裁定**。

### 2.4 ⚠️ 对批测台读数的一处更正(不影响它的结论)

批测台 §1.1 写「helper 被读在 `mode_roam_generic.lua:264/265/343/412` **四个 armed 门内**」。
逐点核过:**调用点只有一个**(`:404`,`local hPoke = J.GetCampPullPokeTarget(...)`);
那四个是**caller 函数体里出现的四处 id 字面量**,不是四个调用点。
差别是承重的:**`:412` 在调用点下面**,而这正是 `pullthink > campbind` **单调**的原因 ——
把它读成「调用点在四个门内」就会把这一行写成和 `pullthink > pulldrag` 一样的双向行。

---

## 三、⭐ 本轮最该被读的一段:变异台自己出的岔(§EE.5)

M5(poke 被扣下时照样写时间戳)与 M7(调用点挪进 poke 臂)**第一版就报 `CAUGHT`,
退出码 1**。看起来是干净的一台。**抓住它们的是 `[source S3]` / `[source S2]`
两条源码形状检查;而本文件头部广告为承载这两个 claim 的那两条驱动,
在变异过的树上双双通过**:

- `[drive D3]` 断言「poke 消失了」。M5 下 poke **本来就已经消失**(hPoke 仍是 nil)⇒ 照过。
- `[control C2]` 断言「两个计数**相等**」。M7 下它们**仍然相等**(起手保持偷的是 drag 臂的帧,
  不是 poke 臂的)⇒ 照过。

两条 claim 各自被改写到**变异真的会动的那个量**上 —— drag 臂计数**归零**、poke 计数是
**90/90 而不是每 3 秒一次** —— 之后驱动才抓得住。

**族谱**:GH #205 丢的是门 / GH #171 丢的是读数 / §EC.8 丢的是一条检查的内容 /
**本条丢的是一条 claim 的承重方,而外观是一台干净的变异台。**

---

## 四、产物与裸读数

| 产物 | 裸读数 |
|---|---|
| `tests/test_campbind_coarm_vacuous.lua`(新) | `RC_EXIT=0` **12 tests, 0 failures**(4 drive + 4 control + 4 source) |
| `tests/test_coarmed_attribution_register.lua` `ACKNOWLEDGED` +3 行 | `RC_EXIT=0` **11 tests, 0 failures**(修前 `RC_EXIT=1`,点名三行) |
| 变异台 `scratchpad/mutate.sh` | **7 真发 / 7 CAUGHT**;M8 范围对照 **SURVIVED,正确**;还原后 `sha256sum -c` **OK**、基线 12/0 绿 |
| 登记行是不是承重(M9) | 抽掉 `['pullthink > campbind']` ⇒ **bare exit=1 并点名那一行**;还原 **exit=0** ⇒ 绿不是「加三行洗绿」 |
| `iterations/streams/test_set.md §EE` | 裁定全文 |
| `iterations/DECISIONS_NEEDED.md` | GH #473 甲第四次顺延的止损条款(见下) |

**验收对表(批测台 GH #486 写下的那条)**:「检测器转绿**且**绿是因为先落地了
`test_set.md` 入集节的 caveat,不是因为直接往 `ACKNOWLEDGED` 里加三行」——
§EE 是那个 caveat,M9 是「不是直接加三行」的裸读证据。

---

## 五、GH #473 甲:第四次顺延,已按自己的条款止损

上一轮(07:00Z ⑧②)我自己写下:「**再顺延一次就必须先写进 `DECISIONS_NEEDED`**」。
本轮它**第四次**被顶掉 ⇒ 已写入 `iterations/DECISIONS_NEEDED.md`(**不是请 owner 裁**,
是让顺延有一个 owner 看得见的地方)。

**下一轮的做法(已写进那条,不需要 owner 批准)**:把 GH #473 甲当**整个工作单元**,
不再排在别的活后面 —— 即使开工自检又报红,**先登记并交棒,不当轮修**。
理由:trunk 红有别的门会在下一轮再举一次手,**载体门这条按定义没有任何东西会举手**。

---

## 六、成本(总监不花钱)

- 零 AWS 调用、零发波、零计费资源;**本轮未做任何 AWS 调用**(连 `budgets` 都没读)。
- 九月 MTD **照抄批测台 12:15Z 自报 `$14.981`**(快照 `refreshed 11:29:50Z`),
  围栏 `$20.03` ≤ `$45/$80/$90/$100` ⇒ **九月未到第一档 $50**,刹车 $90 / 批准线 $100 未触及未动。
- ⚠️ 该快照落在 W45 关机后 ~3.5h,**仍在 4.3–11.3h 滞后带内** ⇒ 不据 MTD 增量反推任何一波。
- **W44 账单侧对账第四次顺延**(批测台自报,按构造结不了),总监确认不追。

---

## 七、体系健康巡检

- **四组本轮均有产出**(batch-desk 12:15Z / hero 10:46Z / strategy 10:45Z / replay-check 09:55Z),无空转。
- `cadence` 仍是 strategy **那同一条** 3.8h(**不是新增的一发**)⇒ **不升级**。
- `UNKNOWN STATUS` 桶仍在(`OPEN_STATES` 不认识 `admitted`/`registered`),**失效方向安全**,本轮不改工具 ——
  ⛔ 理由同上一轮:**在一条裁定里顺手教漂移检测器接受这次漂移,正是那个检测器死掉的方式**。
- **OWNER_PRIORITIES P1(拉野)本轮又推进一格**:`campbind` 的共臂账已结清,
  W46 的逐 id (a) 解释权不再悬着。
- 无 owner 邮件(本周 08-31 已发,周上限 1 封未用尽但本轮无决定类内容)。
- `DECISIONS_NEEDED` **本轮 +1 条**(第五节,止损条款,非待决问题)。
- 周四 ⇒ 效率台账(仅周日)跳过。**patch 检查本轮未做**(低频项,连续顺延中,列在下次触发)。

---

## 八、⚠️ 交给批测台的一条更正:python 那条红**不是** `walk_farm_only`

批测台 12:15Z 写:「上一轮亲手复现的 `tests/test_bots_walk_farm_only.py` 红,
本轮自检 `trunk-red(python)` 仍在 FINDINGS 里 ⇒ **仍红,未修**(第三轮点名)」。

**本轮裸读,两条都读了**:

- `python3 tests/test_bots_walk_farm_only.py` ⇒ **bare exit=0**,`8 checks, 0 failed [175, 15]`
  (10:00Z 那轮修的,**没有回退**;`175` 比当时的 `173` 多两条是别组新落地的命令,`15 unresolved` 逐位相同)。
- 自检 python 腿的失败清单逐字:`88 passed, 1 failed, 2 uncertifiable` / `failed: tests/test_wave_gate_keys.py`。

⇒ **唯一那条 python 红是 `test_wave_gate_keys.py` = GH #479,批测台自己欠的那笔**
(W45 `gates` 缺 `inputs`/`harvest` 与 `reclaim_blind` 两把闸的键;
上一轮 07:00Z 已按 §DR 投递进 `W45_wave.json:harvest_obligations`,**下一轮就是 W45 收割轮**)。

**这正是 GH #267 立案的那个形状**:读了 `FINDINGS` 桶的**名字**,当成了桶里的**同一个成员**。
上一轮报告第 ⑦ 条自己写着「不写『exit 3,全是 cadence』—— 那句手工归因在 08-27 错过 22 小时」,
**本轮同一台在同一处付了一次同族的钱**:它把自己欠的一笔读成了别人没修的一笔。

---

## 九、铁律 6

- `bots/` + `game/` **逐字节零 diff**(`git diff --stat -- bots game` 空)
  ⇒ **全量 Lua 套件未跑,也不声称跑过**(~100min,GH #124)。
- 静态门裸读:`GATE_EXIT=0 CLEAN` / **`luacheck bots game: 0 warnings`**
  (容器初始无 luacheck,**门自己装的 `lua-check`**);**未用 `RULE6_BYPASS`**。
- `luacheck tests/test_campbind_coarm_vacuous.lua` **0 警告**。
- 相关支裸读:`test_campbind_coarm_vacuous` **12/0** / `test_coarmed_attribution_register` **11/0** /
  `test_bots_walk_farm_only` **exit 0** / `test_pending_rulings` **154/0** /
  `test_detector_source_constants` **exit 0** / `test_verdict_strata` **17/0**。
  **python 全套不声称全绿**(`test_wave_gate_keys` 故意仍红,GH #479,非本轮的活)。

---

## 十、下次触发

1. ⭐⭐⭐ **GH #473 甲 = §DY.6 的载体门 —— 第五次排第一,按第五节的条款这一轮它就是整个工作单元**,
   自检若报红只登记交棒不当轮修。
2. **协同组 §ED.5 的共同 promote 约束**(10:45Z 交总监,散文交棒无 queue 行):
   在 `stayfield`/`stayfield2`/`fieldsip` 任何一个被裁定之前必须收下,并裁 §ED.6 的 (甲)/(乙)。
3. **GH #487**(批测台 12:15Z 交总监):闸 (iv) 的结构缺口三选一 + `yield` vs「最终配对」在 #271 里定名。
4. GH #473 乙(按需机时常数 0.745 落在 W44 下界 0.765 之外,身上没有自动失效条款)。
5. 自检腿序(`ensure_lua_tool` 提前)/ `OPEN_STATES` 要不要学 `admitted`(**要有自己的承重**)。
6. GH #449 / GH #460 四件(等下一个 spot 波);GH #410 / #436 / #285 / **patch 缺口 P3** /
   `ckpush` 的 filler-英雄政策(有时限)。

---

## 十一、token 用量(铁律 8)

```
TOKENS total_in=6,968,314 out=57,139 turns=54
```

(统计时刻之后的收尾回合不计入。)
