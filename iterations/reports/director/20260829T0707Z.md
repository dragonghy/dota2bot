# director 2026-08-29T07:07Z — 主干红了三个小时,三个流在它上面落地,W24 从它上面发波

**工作单元:** trunk red on `main`,两个独立根因,都修完,主干回绿。
顺带收掉一个被 import 崩溃遮住的第三个发现。
**没有花 AWS 的钱**(总监本轮零 AWS 调用)。

---

## 0. 一句话

开工自检(铁律 10)第一次读就是 `selfcheck worst exit: 3`,
`FINDINGS (exit 3): cadence trunk-red(python) trunk-red(lua)` ——
**工作树是干净的**(`git status` 空),所以那两条 trunk-red 说的就是 `main`。
往回查:红是 **04:20Z** 和 **04:51Z** 两次落地各自带进来的,
在这之后 **hero 落地了两个 commit、replay-check 落地了一轮、batch-desk 06:20Z 发了 W24**,
**没有一个环节把这条读出来。**

两个根因**是同一个形状**:一道门盯的是**代理量**,不是它真正想护的**性质**。
一个盯行号(不是分类),一个盯文件里的 id 列表(不是同一个谓词里的合取)。
两个都是**假阳性率 100%**。

---

## 1. 根因 A —— 行号 census(GH #221,本轮结案)

`tests/test_level_gate_census.lua` 把 22 条 `GetLevel() >= N` 门按
`file:line` 钉死。**任何在它上面插入的编辑都会把它变红。**

`bc2ff86f`(strategy 04:20Z,`tphome` veto)在
`bots/ability_item_usage_generic.lua` 上方插了 14 行,
把最后两条 aiug 行推成 `:5858 -> :5872` / `:5898 -> :5912`。
分类一个字没动。**主干红。**

这不是第一次。文件自己的注释就是控方证据 —— 我把它数了一遍:

| 变更 | 位移 | 分类是否真的漂移 |
|---|---|---|
| `campsel` | +11 / +36 | 否 |
| `tbearly` | +30 | 否 |
| `campfarm` | +16 / +59 | 否 |
| `campvoid` (#265) | +41 | 否 |
| `campdanger` (#193) | +38 | 否 |
| `bbshort` (#222) | +6 | 否 |
| `salvepool` (#227) | +3 | 否 |
| `salveally` (#231) | +2 | 否 |
| `salveyield` (#237) | +12 | 否 |
| GH #286 | +34 / +41 | 否 |
| **本轮 `bc2ff86f`** | **+14** | **否 —— 但它让主干红了 ~3 小时** |

**10 次重新锚定,0 次真发现。** 而且假阳性和真阳性**在读者那里长得一模一样**:
铁律 6 的 push 钩子和自检横幅都写着「failing before you changed anything」,
那是**罐头字符串不是判定**(GH #198 §3 已经为此改过措辞);
唯一一次真的去判定的是 GH #193,它靠**手工 stash 再跑一遍**才知道 main 是绿的。

> 一道真阳性率 0/10、而且假阳性与真阳性对读者不可区分的绊线,不是绊线。

**改法:key 从行号换成 `file .. '||' .. 去空白源文本`。**
旧 key 承载的每一条性质都还在,而且多了两条:

| 情况 | 结果 |
|---|---|
| 新增一道门 | 文本未登记 → **红** |
| 门被改写 | 登记的文本解析不到 → **红** |
| 门被删除 | 同上 → **红** |
| 门被复制到第二处 | 该文本出现两次 → **红(新增)** |
| 两行共用同一文本 | key 有歧义 → **红(新增)** |
| **在它上面插入代码** | **绿** |

落地那棵树上验过:**22 道门,22 个互异 `(file, text)` key,零碰撞。**
这跟 GH #106 §4 / #127 给语料规模等式开的药方(`cs.ratchet`)是同一个 ——
**一个手工维护的数,任何无关的追加都会打破它,换成它本来要代表的性质。**
22 行的 `line = NNNN` 字段整个删掉了(留着不检查就会烂),
行号只留在历史散文里,那里本来就标着「不是当前值」。

---

## 2. 根因 B —— 读者把「同一个谓词」写成了「同一个文件」

`tools/batch_test/behavioral/odaoe_domain.py:read_source_constants()`:

```python
gates = re.findall(r"J\.IsSoakCandidate\(\s*'([a-z0-9_]+)'\s*\)", text)
if gates != ["odaoe"]:
    raise RuntimeError("expected exactly one soak gate 'odaoe' in the OD file, ...")
```

它要防的是 **GH #207 那一族**:`IsSoakCandidate('odaoe') and IsSoakCandidate('X')`
—— X 被 promote 的那天整道门冻成 FALSE(promote 掉的 id 不在任何 armed 串里)。
**那是「同一个谓词里的第二个 id」。** 代码写的是「**整个文件里**的第二个 id」。

`67633eb`(hero 04:51Z,GH #287 §2)落地 `odbuild`:
`hero_obsidian_destroyer.lua:67`,技能加点表那儿,
离 `odaoe`(`:626`)**约 560 行,不共享任何表达式**。
两道**互相独立**的门 —— 一个英雄文件带两个独立候选是**正常的**。
读者 raise,`tests/test_detector_source_constants.py` **在 import 处就死了**,
主干为一个**正确的改动**红了几个小时。

**改法:把读取范围收到 `X.od_GetEclipseAoeLocation` 函数体内**,
跟 `bbfloor_domain._gate_ids` 已经在用的形状一致。
两个方向都钉了回归:

- 独立的第二个候选(今天的 `odbuild`)**必须绿**;
- 合取进 `odaoe` 那道门的第二个 id **必须还 raise** ——
  收窄作用域不许把它本来要看的东西看瞎。

---

## 3. 被遮住的第三个发现

修好 B 之后,同一个文件立刻吐出**另一条从 05:30Z 起就存在的红**:

```
FAIL wandlimbo_domain:HP_FRAC is classified (UNREGISTERED)
```

`bb00ea75`(replay-check 05:30Z)带模块一起落地,没登记进 `HP_CENSUS`。
**它整天都是隐形的** —— 这个文件在**下面约 500 行处 import 时就崩了**,
于是每个看到这片红的人都只看见一条 traceback,**没有一个人看见这一行的名字**。

> 两个发现,一条横幅。记下来,因为「反正套件已经红了」正是第二个发现拿到免票的方式。

登记为 **MIRROR**(`J.ShouldDrinkWandInLimbo`,`jmz_func.lua:9348`,
`GetHealth() > GetMaxHealth() * 0.25`)。
并且**顺手把这个 mirror 变成真的 mirror**:
只写在散文里的 MIRROR 是注释不是镜像,所以在 section 2 加了两条钉:
`WL.HP_FRAC` 对着源码读出来的 `0.25`,`WL.MIN_DRAUGHT` 对着 `6 * 15 = 90`。
任何一边动,红。

---

## 4. 时间线(这才是本轮真正的发现)

| UTC | 事件 |
|---|---|
| 04:20Z | `bc2ff86f` strategy 落地 → **Lua census 红** |
| 04:51Z | `67633eb` hero 落地 `odbuild` → **python 套件红** |
| 04:51Z+ | hero 又落地 2 个 commit(`2d1024e`、`93ef8c2`) |
| 06:20Z | **batch-desk 发 W24** —— 从这棵红树上 |
| 06:48Z | replay-check 落地一轮 |
| 07:0xZ | 总监开工自检读到 exit 3,**本轮第一个读到的人** |

W24 本身没有被这两条红污染(两个红的都是**测试/工具侧**,`bots/` 的
Lua 是干净的 —— 本轮 `luacheck bots game` 0 警告,且 A 的位移不改任何 shipped 行为,
B 是纯读取器),**所以不需要杀波重发**。但那不是因为有谁核过,是运气。

**这一条要给全队记住的是顺序问题,不是速度问题**(跟 GH #290 立案句同型):
铁律 10 的自检是每个流开工第一件事,**它当天早上被跑过至少三次**,
每次都应该打出 `TRUNK RED`。三个流在红树上落地,一个流从红树上发波。
本轮修的是那两道门,**没修的是「读到 exit 3 之后会发生什么」** —— 见 §6。

---

## 5. 验证(铁律 6,两半都跑了)

```
bash tools/agent/luacheck_gate.sh
  installed luacheck (it was missing; apt package lua-check, bounded)
  luacheck bots game: 0 warnings          → exit 0

bash tests/run_py_tests.sh
  52 passed, 0 failed, 0 uncertifiable    (改前:50 passed, 2 failed)

bash tools/agent/routine_selfcheck.sh
  === trunk health (python test suite) ===
  52 passed, 0 failed, 0 uncertifiable
  === trunk health (fast Lua detectors) ===
  21 tagged detector file(s), 0 failures
  FINDINGS (exit 3) : cadence          ← 只剩 cadence,两条 trunk-red 都没了
```

`tools/agent/arm_push_gate.sh`:`push gate armed (core.hooksPath=.githooks)`。
**没有用 `RULE6_BYPASS`。** 动态那半(~100 min 全量 Lua 套件,GH #124)本轮没跑,
不声称 —— 覆盖我这次改动的 `test_level_gate_census.lua` 在 21 个 tagged
detector 文件里,那一条跑了且绿。

---

## 6. 交出去的下一棒(铁律 9 连带规则:修好 ≠ 做完)

本轮修完了两道门,但**没有**修「自检读到 exit 3 之后没有下一步」这件事。
今天的形状是:自检**正确地报了红**,而三个流**照常落地**、一个流**照常发波**。
这是 §4 那张表的全部内容,而且它**不是本轮两个根因的子集** ——
就算 A 和 B 都不存在,下一个真红也会这样穿过去。

**开成 `[harness]` 待办,写进 DECISIONS_NEEDED**(不是 owner 决定题,是排期题):
`routine_selfcheck.sh` 的 trunk-red 腿目前只**打印**;
它应该像铁律 6 的 pre-push 钩子那样**有牙齿**,或者至少让
「本轮读到 exit 3」成为报告里的**必填栏**,这样它会在报告节奏里留下痕迹。
两条路各有代价(前者会挡住「我正在修这条红」的那一轮),
所以是排期题不是我一轮能定的。

本轮**没有**给 owner 发决定类邮件(本周额度留着;没有需要 owner 拍板的事)。

---

## 7. 本轮没做的事(明说)

- **promote/reject 判定:无。** 本轮没有落地的 verdict 可判。
- **test_set.md 变更审批:无。** 本轮没有新入集申请。
- **成本核查:未跑。** 总监本轮零 AWS 调用;MTD 读数以 batch-desk W24 报告为准,
  本轮修主干占满了工作单元,**没有**去读 `budgets describe-budgets`。
  下一轮总监补。
- **`hero-22`(queue,`odbuild` 路由/排期裁定)未裁。** 自检点了它的名
  (`OTHER (routing/slot ruling still owed): 1`)。它和本轮根因 B 是同一个 id,
  但裁定是排期题,和修红不是一件事,**留给下一轮总监,别让它掉棒**。
- **patch 检查:未做**(低频项)。

---

## 8. 改了哪些文件

| 文件 | 改动 |
|---|---|
| `tests/test_level_gate_census.lua` | key 换成 `(file, text)`;删掉 22 个 `line = NNNN`;新增「一行文本只能解析到一处源码行」和「两行不许共用 key」两条断言;表头换成证据表 |
| `tools/batch_test/behavioral/odaoe_domain.py` | soak-gate 读取范围收到 `X.od_GetEclipseAoeLocation` 函数体内 |
| `tests/test_detector_source_constants.py` | `odaoe` 收窄的两向回归;`wandlimbo_domain:HP_FRAC` 登记为 MIRROR;该 mirror 的 section 2 钉子(HP_FRAC / MIN_DRAUGHT) |

---

## 9. Token 用量(铁律 8)

```
TOKENS total_in=5,079,683 out=29,654 turns=54
```
(统计时刻为止;之后的收尾回合不计入。)
