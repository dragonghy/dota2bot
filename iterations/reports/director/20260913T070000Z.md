# director 2026-09-13T07:xxZ(第十轮)— RULING 33:`aimguard` 退集(27 → 26),处置 `DUMP-SCHEMA-BLIND`;RULING 34/35/36:三条零 EC2 扫描一次裁完

**一句话**:连续九轮判定完结 0 之后本轮 **1**(⚠️ owner P4.2 要 ≥2,**没达标,我没凑**,理由在 §4);
退的那条 id 的**载体问题与 `abilanc` 逐字同型**,而 §HA.0 给 `abilanc` 开的那味药(载体门)
对它是**必要但不充分** —— 把两条写成同一个名字,下一个人会拿**一道已经装好的闸**去救
**一个不是被它挡住的 id**。

**零 AWS(一次调用都没有)、零波次、`bots/`+`game/` 零 diff、不发 owner 邮件、无 promote、无 reject。**
armed **27 → 26**,离 P4.2 解冻线(≤20)**差 6**。判定完结 **1**;裁定 **4** 条(RULING 33/34/35/36)。

---

## 〇、⚠️ 先登记本轮自己踩的一件,因为它是章程逐字警告过的那一发

**开工自检第一条命令我加了外层 `timeout 900`,`TRUE_EXIT=124` —— 是我自己掐的。**
录像组 `20260911T010816Z.md` §九逐字写着「⭐ **单独跑、且不加外层 `timeout`**
(交棒单第 (4) 条;**上一轮的 124 是自己掐的**)」⇒ **这条教训写在别人的报告里,我重蹈了一遍。**
⚠️ 更该被读的是**它长什么样**:`124` 与一次真的失败**在退出码上不同**,
但在**「这一侧这轮没人看过」这件事上完全相同** —— 而我差一点把那份被掐断的日志
(里面确实有 `carrier_terms: 65 checks, 5 failed`、`py_gate_hook: 10 checks, 6 failed`)
当成一次跑完了的自检来引用。**一份跑到一半的日志比一份空日志更危险,因为它有内容。**
⇒ 当场改成**无外层 timeout 的后台重跑**,读数见 §6。

📌 与铁律 10 的 `SKIP/UNCERTIFIABLE 不是通过`同族:**`124` 也不是通过**,而且它是三者里
**唯一一个会交出部分读数的**。

---

## 一、RULING 33:`aimguard` 退集,处置 **DUMP-SCHEMA-BLIND**

全文档案 `iterations/streams/test_set.md §HJ`;机器键 `state.json:aimguard_RETURNED_20260913`。

### 1.1 选取不是我挑的,是被裁方自己列的表

章程「下次触发」①(硬性):**判定完结 ≥1,正面处理 armed 27**。
`verify_coverage.py --all` 现读 27 行(`RC_EXIT=0`),而**排序不是我做的** ——
录像组 `20260912T185424Z.md` §一自己给了一张按堵点分的三档表:
**离线结构性拒绝 ⛔ 不能**(`aimguard`)/ **通道不可分离 ⛔ 不能**(`overchase`、`pullcad`)/
**域为空·罕见 ⭐ 能**(`stayfield`/`stayfield2`/`campfarm`/`campvoid`/`creepthink`/`blinkflee`)。

第三档**不动**(罕见是语料买得到的,退它等于把还能买的证据丢掉);第二档**特意没裁**(§3)。
⇒ 第一档只有一条。而它是一根**已经停在裁定形状里的棒**:录像组 `20260911T010816Z.md` §七的
标题逐字是「`aimguard` 结构性拒绝(**顺带交付,不占本轮判决位**)」,§八逐字
「⛔ **不为它开新 issue**」「**不计入「买到」**」「⛔ **不补 `owed_executions.json` 登记行**」
—— **交付了理由、主动不为它再要资源**,棒就停在总监这一格(§HA.1 `outlatch` 同型)。

### 1.2 ⛔ 三条堵点,本轮**裸读源码现证,一条都不转载**

1. **门是单向的 ⇒ 分歧集是一个「缺席」不是一个「出现」。**
   `bots/FunLib/jmz_func.lua:4246` 先 `if not J.CanBeAttacked(hGuard) then return false end`(两腿逐字相同),
   `:4248-4251` 未 armed 直接 `return true`,armed 才追加 `return J.CanBeAttacked(hTarget)`
   ⇒ **armed 只能扣下一次出厂会打的火,永远造不出新火。**
   ⇒ 分歧集 = 到站 ∧ `CanBeAttacked([1])` 真 ∧ **`CanBeAttacked([2])` 假**。
2. **那个谓词的每个合取项都不在 dump 里。** `J.CanBeAttacked`(`:4191-4210`)读
   `HasForbiddenModifier` / `IsAlive` / `CanBeSeen` / `IsNull` / `IsAttackImmune` /
   `IsInvulnerable` / **七个具名 modifier** / 外加一条自家单位的 `GetHealth()/GetMaxHealth() < 0.5`;
   而 `creepSnap` **结构体只有四个字段** `{T,Team,X,Y}`
   (`tools/batch_test/behavioral/dumper/main.go:108-113`,schema 注释 `:14`,构造 `:475-476`)。
   ⭐ **本轮比录像组多读到的一条**:那条 `<0.5` 要 HP,creep 流**没有 HP**
   ⇒ **分歧集的 `[1]` 那一端同样重建不出来**,不只 `[2]`。
3. **实体身份也不在流里** ⇒ 「谁是 `[1]` 谁是 `[2]`」(引擎对 `GetNearbyNeutralCreeps` 的排序)恢复不出来。
   ⛔ **中立兵确实在流里**(`main.go:467-471` 逐字「Neutrals (team 4) … **always emit them**」)
   ⇒ **这不是域为空**,是**流在位置上完整、在语义上全空**。
   ⛔ **`episodes=0` 一律不得读成「测过了,无影响」。**

### 1.3 ⭐ 处置名为什么必须是新的一个

§HA.3 要求处置名**编码「什么东西能解开它」**。四条并排:

| 处置名 | 解开它的是什么 |
|---|---|
| `DOMAIN-NOT-REACHED`(§FW.2 `campbind`) | 更多语料 |
| `INSTRUMENT-BLIND`(§HA.1 `outlatch`) | 一份真帧 fixture(**route 2 是开的**) |
| `UNOBSERVABLE-BOTH-ROUTES`(§HA.2 `rotscope`) | 两件互不相干的事 |
| **`DUMP-SCHEMA-BLIND`(本条)** | **一次 `creepSnap` 的字段扩充**(两条路同堵、堵点只有一个) |

⭐ **本条独有的形状**:聚合检测器与真帧 fixture **吃的是同一份 timeline dump**
⇒ creep 流缺字段**同时**关掉两条路;补上字段**同时**打开两条。`rotscope` 是两个堵点,本条是一个。

### 1.4 ⭐⭐ 它与 `abilanc` 的分野 —— 本轮最该被读的一条

`tools/batch_test/soak/carrier_terms.py:4-13` 的头注逐字记着:`aimguard` 是 **hero-scoped**,
**唯一载体 `spirit_breaker`**;**W20 在 180/180 带戳局里 drafted zero spirit_breaker**,闸 `exit 0`,
波照跑,**没有任何东西举手**;**W21 逐字节重演**;`--rates` 给 **P(spirit_breaker 缺席于 4 粒种子波)= 0.518**。
⇒ 这**正是** §HA.0 写给 `abilanc` 的那句「域没被走到的原因是牌桌上没有能走到它的英雄」。

⛔ **而两条不能同名,理由是算术不是措辞。** §HA.0 给 `abilanc` 的处置是**载体门**
(`seed_draft.py --assert-carrier`),那道闸**今天已经装好**(GH #276 之后机械派生,不再手写焦点五)
⇒ 对 `abilanc` 它**必要且可能充分**。**对 `aimguard` 必要但不充分**:
把 `spirit_breaker` 保证进每一波、把站点走到,**分歧集仍然读不出来**,
因为 §1.2 的三条堵点**一条都不被载体门碰到**。

📌 **可迁移的一句**:*两条 id 可以卡在**同一个症状**(`episodes=0`)、**同一个近因**(载体缺席),
而**远因不同**;按症状或近因命名处置,会把一道装好的闸推荐给一个不是被它挡住的 id。*
⇒ **`abilanc` 本轮照旧不退**(§HA.0 的裁定不动),`aimguard` 退。

### 1.5 条件 (b) / (c) 与账

- **(b)**:**无可主张的负面**,也**不作正面证据**。armed 腿本波带 27 个 id ⇒ 按章程 4a(§BW.3)
  任何聚合差分**不许记到这一条名下**;`winrate` 通道按 GH #352 不引用(六波 1115 局,少数侧占比 **0.0161**,DEGENERATE)。
  粗粒度门**以「没有任何可归因于它的负面读数」通过,措辞照抄不得加强**。
- **(c)**:**成立,这正是它不被 reject 的理由** —— `jmz_func.lua:4213-4215` 头注逐字
  「一道被问了 A、而实际动手的是 B 的守卫,不是守卫,是一个大多数时候恰好成立的巧合」。
  **退的是证据路径,不是这条改动的道理。**
- **账**:成员串 **27 → 26**,**230 字节**,md5 `3a438e729b3960382dfd51a89a7da54e`
  (退集前 27 / **239** / `76a888b622124fc5488503aa36ef6b25`,**开工时逐位复核过**)。
  armed **16 天 exact**(`arm_since.py`),买回**一条 VERIFY 行、`episodes=0`**。
  ⛔ **不是 reject**:gate / 唯一调用点(`hero_spirit_breaker.lua:297`)/ `tests/test_aimguard_target_axis.lua`
  **逐字保留**,`bots/`+`game/` **零 diff**。

### 1.6 交棒,以及**一个差点 BORN-DONE 的针脚**

`owed_executions.json:aimguard_creep_schema_identity`,executor = **协同组 / [harness]**
(⛔ 不派录像组 —— 他们已主动声明不为它再要资源,派回去是把一根放下的棒塞回手里)。

⭐⭐ **针脚的第一版是错的,而它错的方式正是 `pending_rulings.py` LIMIT 14 点名的那一种。**
我本来写 `path_contains_all: json:"idx"` —— 而**那个字符串今天就在 `main.go:79`**
(英雄流的 `heroSnap.Idx`,注释逐字「entity index; disambiguates a hero from its illusions」)
⇒ **那一版会在开行的同一秒读 DONE**,成为 LIMIT 14 的**第四例**,
而 LIMIT 14 记的前三例里有一例(`late_epoch_corpus_reopen_list`)逐字写着
「**the round that registered it had to be told by prose, twice, that this was the shape it had just dodged elsewhere**」。
⇒ 改成 **`,"idx"`**(逗号引号,只会出现在 `main.go:14` 那行 schema 注释被扩写时),
**本轮实测 ABSENT**,见证逐字:
`tools/batch_test/behavioral/dumper/main.go exists but 1 of 1 required mention(s) are absent: ,"idx"`
(`RC_EXIT=3`,完整日志 `/tmp/rc.FOA9fz`,时刻 `2026-09-13T07:10:17Z`)。

⚠️⚠️ **`done_when` 变绿 ≠ 可以再入集,这一句写进了行里。** 本轮**没有**核实 Source2 录像中
中立兵的 modifier 集合 / `IsAttackImmune` / `IsInvulnerable` 是否可取;
`main.go:794-797` 的 `vision_note` 还逐字写着「**No per-team fog bitmask exists in Source2 replays**」
⇒ `CanBeSeen` **即便对英雄也是几何重建的,不是读出来的**。
⇒ `done_when` **只承诺身份那一半**;**谓词那一半是未证的**,再入集要两半都成立。
⛔ 行里加了一条:**谁退休这一行,谁必须在同一轮写下谓词那一半的答案(可取 / 不可取 / 仍未查),三选一,不许留空。**

---

## 二、RULING 34/35/36:`hero-68` / `hero-69` / `hero-70` = APPROVED-SCAN(零 EC2)

三条都是**归档只读扫描**(不是入集提议)⇒ **不是 FROZEN-HOLD**。
批的理由与 RULING 32(§HG)逐字同一句:**不是「这个 id 值得」,是「这道滤网与 P4.2 同向」** ——
出口 (i)(域 ~0 ⇒ 按域为 0 退场)让这些 id 在**从未占用 armed 位**的情况下出局;
不裁只会让它们停在一个默认下一步是「申请取证波」的状态里,**而那是把集合变大的方向**。
⛔ **入集仍不批**(armed 26 > 20),⛔ **不许为任一条单独发波**。

⭐ **三条的 acceptance 罕见地自己都写对了铁律 4(i-a) 的 ab/ba 分层**(`STRATA_SILENT` 不适用),
`hero-69`/`hero-70` 还自己写对了 4(ii)(均值 + 分布 + 占比,而非只报中位数)⇒ **请求原文一字不改**。
本台各补一条下游约束(全文在 `queue.json:<id>.director.note`):

- **`hero-68`(`lionwreach`)**:`nCastRange` **逐帧随 rank 与 Aether Lens 变**
  ⇒ **切点是一个每帧不同的数**,必须逐帧算切点、逐帧归层,报告写明这一句(铁律 4(iii))。
  方向:**严格收窄** ⇒ 打断次数必然 armed ≤ baseline,**次数不是观测量**;
  要读的是「环带外侧那些瞬间圈内还有几个可打的敌人」——它分辨**换人(收益)**与**放弃(成本)**,
  而这两件事在「次数下降」这一个数里长得一模一样。
- **`hero-69`(`lionpushclock`)**:⭐⭐⭐ **前提更正,而且是 §HG.2 那个形状的第二发**。
  请求逐字引用「归档语料一个非英雄单位都没有(GH #772,1410/1410 是英雄)」——**这句是错的**,
  本台 RULING 30(§HD.2)已量掉:1410/1410 逐位正确,**但小兵不住在 `fx.units`,而在兄弟键 `fx.creeps`**
  (`make_fixture.py:618-626`;现量 141 帧里 **32 帧**带 `creeps = {`)。
  **结论(fixture 层买不到端到端域)仍成立,但理由相反**:真堵点是
  `tests/mock/replay_fixture.lua` **今天没有 `GetNearbyLaneCreeps` 读取器**,
  未接线的 `GetNearby*` 走 mock 默认 `{}`(`tests/mock/bot_api.lua:175`)⇒ `#laneCreepList` **恒为 0**,
  **与语料里有多少小兵无关**;而那个 reader 就是 RULING 30 的 `creeps_schema_gh581` **第 (C) 半、且不依赖 GH #581**。
  ⇒ 「测不了」应改写成「**接上 (C) 半就能测**」。📌 *用错的理由写下对的结论,下一个人继承的是理由。*
  ⚠️ **归因不是纪律,是投递** —— 这条更正上一次只落进 `state.json` 与 `test_set.md`,
  **没落进英雄组读的任何字段**,于是四小时后被逐字重新引用一次;**本轮落进 `director.note`(他们读的那一格)**。
- **`hero-70`(`wkbonebank`)**:⭐ **它的来历本身值得读** —— `hero-31` 的读数在自己的 §6 写着
  「(4) 充能层数买不到 ⇒ 前三列是上界」,**那个未测滤波器就是本合取项**
  ⇒ 这不是新发明的 id,是**一条旧读数自陈的上界长出来的针脚**,**正是 P4.2 想要的形状**。
  硬约束:比率阈值**随 rank 变**(rank 1/2/3/4 → 2/3/4/5)⇒ **必须先按 rank 分层再按存量切三段**,
  池化会把 **rank 1(域恒为空)**与 **rank 4(域最宽)**混成一个没有意义的比例。

---

## 三、本轮**特意没裁**的两条(写下来免得下一轮当遗漏补上)

- **`overchase`**(verify 1 / INDETERMINATE / **episodes 912** / armed **50 天**,最久的一档):
  录像组 `20260912T010526Z.md` **昨天刚把仪器 `overchase_domain.py` 落树**,逐字写下
  「**`INDETERMINATE` 是判决不是失败;缺的是判别子,不是样本**」与
  「这一条**留在本组自己的 backlog 里**(下一轮清单第 (1) 条),**不外派**」。
  ⇒ 与 `outlatch` **形状相反**:那条的被裁方**主动停手**,棒才停在我这一格;
  这条的被裁方**明确认领了下一步,而且一轮都还没到期**。
  ⛔ **退一条正在被认领的 id,是把还在走的那条路砍掉。**
  ⚠️ 但**这条豁免下一轮就用完了**:若届时仍无进展,它是 armed 最久的一条,优先处置。
- **`pullcad`**(verify 2 / episodes 278 / 今天 03:58Z 还有新读数):§HA.0 **已裁不退**,
  理由是「`promote_atoms.py` 读 `FROZEN none`,闸已是独立门 ⇒ 不是空操作」。
  ⚠️ **那条理由与「(a) 买不买得到」是两条轴** —— 它回答「这条杠杆活着吗」,不回答「(a) 买得到吗」;
  而录像组的表把它归进「通道不可分离 ⛔ 不能」。**两条轴今天指向不同方向。**
  本轮不替它们定谁赢,但**登记这件事本身**:一条 id 被一条**不回答本问题的理由**留在集合里,
  是 §HA.3 那一族。⛔ **下一轮正面处理,不许当作「§HA.0 已经裁过了」跳过。**

---

## 四、⚠️ 判定完结 **1** < owner P4.2 的 **≥2**,我没凑第二条

**不许把这一行读成 2。** 唯一的第二候选是 `overchase`,而凑它需要**推翻 §3 的理由** ——
即在被裁方认领的**次日**,拿「他们还没做完」当退集依据。
那样买到的是**一个达标的计数**和**一条用错理由做出的处置**,
而 §HA.3 / §1.4 两节反复在纠的正是后者。

📌 *一个为了满足配额而做出的处置,和一个正确的处置,在 `state.json` 里长得一模一样* ——
这是 §HA.3 的立案句,本轮把它用在**我自己的产出指标**上。
⇒ 连续九轮 0 之后,本轮 **1**;按 P4.2 口径记缺口 **1**。armed **26**,离解冻线 **差 6**。

---

## 四·五、⭐⭐⭐ 本轮的第二件活不是我挑的:退集当场顶红了一个测试,而那条红里藏着一条挂了四轮的

**这一节比 RULING 33 本身更值钱,因为它是「一条裁定的连带成本」第一次被当场量出来。**

### 4.5.1 退集把 `test_carrier_terms.py` 从 5 红打到 10 红 —— 而那 5 条新红是我造的

退集落地后现跑:`carrier_terms: 65 checks, **10 failed**`(退集前 **5 failed**)。
新增五条全部指向同一件事:`spirit_breaker IS asked about`、
`W20/W21: gate REFUSES`、`W20/W21: names aimguard ABSENT`。

成因是**这个文件自己的注释已经点名过两次的那个缺陷**:
> *a test that goes red every time the arm string it reads is edited is
> re-stating the arm string, not checking the deriver*

它被打过两次补丁 —— **2026-08-29 补「增长」**、**2026-09-05 补「成员循环里的移除」** ——
而**两次补的都是一个实例**。本轮是**第三个方向:词表(term list)里的移除**。
`check("spirit_breaker" in terms, ...)` 是一张**冻结快照**,只在 `aimguard`
(唯一 spirit_breaker-scoped 的 id)armed 时为真;它退集之后,闸**本就应该不再问 spirit_breaker**
——**理由与它不问 `axe` 逐字相同**,而那条旧断言把这个**正确行为**叫作失败。

⇒ 改写成它一直想说的那个**双条件**:**一个英雄被问到,当且仅当有 armed id scoped 到它**
(这正是 #276 的那一半:「the gate must not ask about a hero NO armed id needs」),
并补一条 `terms ⟺ claimants` 的集合相等。**两个方向都成立,id 来去都不红。**

### 4.5.2 ⭐⭐ 第一条红后面那条,是挂了**四轮**、被当成别人家的

剩下的 5 条(`zusult -> zuus`、`liondrainstop -> lion`、两条 `still hero-scoped off the tree`、
`W20: liondrainstop 1/4`)**在我动手之前就红着**,而且**连续四轮被记成「英雄组的」**。
本轮裸读之后,**它们不是英雄组的,是一条判决语义题,而且五条是同一个根**:

**一个 promoted 的 id 没有 gate 字面量 —— 删掉它就是 promote 的定义** ⇒
`derive_id` 解析不出它(现读 `liondrainstop → kind=unresolved, why='no gate literal found in bots/'`),
**而这正是 deriver 答对了**。`zusult`(§GW)与 `liondrainstop`(`stable-v7`)都在 **2026-09-11** promote,
这两行**从那天起红到今天**。
📌 **它为什么被传了四轮**:红的**外观**是一句「`zusult -> zuus` 失败」——
**长得像一个派生 bug,在每一份报告里也读起来像**,于是每一轮都把它按英雄组的 hero-scope 问题交出去。
⇒ **与 2026-09-13T04:3xZ 那轮点名的是同一族**:*任何钉在 `J.IsSoakCandidate('<id>')` 上的断言,
都断言一个**该 id 自己的 promote 会删掉**的形状*;那轮量的是检测器,本轮量的是**测试**。

⛔ **不能靠删掉这几行来修** —— 本文件自己的教条逐字:
「**Retiring an id must never silently retire the assertion that guards it**」。
⇒ 换成**对 promoted id 仍然有牙的那一条**:**gate 字面量已经没了(promote 做完了)**
且 **`PROMOTED (was soak-candidate ...)` 注记在(promote 被记录了)**。
**半个 promote —— 闸删了没写注记,或写了注记闸还立着 —— 在这里会红**,而那才是真正的风险。
成员循环也从**两分支**(armed / 不 armed)改成**三分支**:
2026-09-05 那次拆分里「不 armed」**默默地只指「退集,闸还在 `bots/` 里」**,
而一个 **promoted** 的 id 同样不 armed、闸却按设计没了。

### 4.5.3 section 4 喂的是今天的臂串,而它问的是两个 2026-08 的波

`W20`/`W21` 的重放拿 `rows`(**今天**的臂串派生)当输入,而它们是**冻结的历史**。
⇒ 每一次 promote / 退集都在**悄悄改一道固定答案题的输入**。
改成由三个 id(`aimguard`/`odaoe`/`liondrainstop`)自己派生历史 rows;
`liondrainstop` 因 promote 已不可派生 ⇒ **唯一一处手写的冻结行**,并逐字写明
「⛔ 只给树已经说不出话的 id 手写;还能派生的一律派生,否则这一节就从测 deriver 变成复述我自己的模型」。

### 4.5.4 验收:**五发语义变异体 5/5 CAUGHT**,以及**一发不算数的**

台子从**文件副本**恢复(evidence-discipline 规则 1),每发之间 `cp` 还原,**最后一发之后逐字节 `diff` 确认还原**。

| 变异体 | 手法 | 结果 |
|---|---|---|
| M1 | `promoted = set()`(假装什么都没 promote) | **CAUGHT** 4 failed |
| M2 | 丢掉冻结历史行 | **CAUGHT** 2 failed |
| M3 | section 4 重新喂 live `rows`(**我刚修掉的那个缺陷**) | **CAUGHT** 4 failed |
| M4b | 双条件退回旧的冻结快照 `check(hero in terms)` | **CAUGHT** 2 failed(逐字 `term=False claimant=False`) |
| M5 | 给 `claimed` 塞一个 `pudge`(集合相等的另一半) | **CAUGHT** 1 failed(`claimants-without-term=['pudge']`) |

⚠️ **M4 的第一版 SURVIVED,而它不构成证据 —— 它是删除不是变异。**
我把循环的可迭代对象换成 `()`,那**删掉了检查**而不是**弄坏了断言**:
检查数 **69 → 67**、`exit=0`。⇒ **只看退出码的读者看不见它**,
看见它的是**检查数**。改成 M4b(语义反转)才是真变异体,当场被抓。
📌 **可迁移的一句**:*一个把断言删掉的变异体和一棵干净的树,在退出码上完全相同;
变异体必须攻击**答案**,不是**算答案的那几行**。*(与 §ES 的 M2 同一句话,本轮自己又撞了一次。)

### 4.5.5 读数

`test_carrier_terms.py`:**65 checks / 5 failed(开工) → 65 / 10(退集落地后) → 69 checks / 0 failed(修完)**,
`BARE_EXIT=0`。连带复核(§EX.6 的教训,本轮主动查):
`test_arm_since.py` **18/0**、`test_pending_rulings.py` **783/0**、
`test_gate_claim_consistency.lua` `BARE_EXIT=0` —— **无连带红**。
⇒ **一条挂了四轮的 trunk red 本轮清掉**,`bots/`+`game/` 仍**零 diff**(改的是 `tests/`)。

---

## 五、巡检 / 成本 / owner 沟通

- 💰 **零 AWS:本轮一次 AWS 调用都没有**,不作任何 MTD 新声称。
  ⚠️ 上一轮登记的**账户级预算问题**(两个预算 `CostFilters` 都是 `null`,
  `dota2bot-batch` 报的不是我们的账)**仍等 owner 回复**,期间按保守侧(继续用账户级数字当刹车)。
- 🩺 **五组 24h 内全部有产出,无掉队组、无空转。**
  最新报告:batch-desk `20260913T061500Z`、strategy `20260913T050052Z`、
  hero `20260913T045952Z`、replay-check(域扫描产物)、director 本轮。
- 📮 **不发 owner 邮件**(本周配额留给 §7 的 W37 汇总)。**本轮无新增 `DECISIONS_NEEDED` 条目** ——
  第 15/16 条仍挂着,第 16 条(`test_set.md` 拆文件)**未回复即按 (甲) 执行**,见 §7。
- ⛔ **GitHub MCP 未使用**(铁律 11:无头 Routine 不在权限提示上坐等)。本轮无需追评的已发表引用。

---

## 六、铁律 6 / 自检

- **开工自检**:第一次 `TRUE_EXIT=124`(**我自己加的外层 `timeout 900`**,见 §0),
  **整份作废**;**无外层 timeout 重跑**,读数补在本节(收尾回填)。
- **自检真码 `TRUE_EXIT=3`**(第二次、无外层 timeout,实测约 13 分钟),`legs run 12`,
  **`UNCERTIFIABLE: none`**,`NOT RUN(inside a leg): tests/test_selfcheck_lua_leg.py`;
  fast Lua 检测器 **87 个文件 0 失败**;两个 stable 锚点 `EXISTS / PINNED / SHIPPED ok`。
  `FINDINGS (exit 3)`:`cadence` / `queue-rulings` / `owed-executions` / `trunk-red(python)`。
  **逐条归因(不手搓「exit 3,全是 cadence」—— GH #267 那句错了 22 小时)**:
  `queue-rulings` 与两条 `trunk-red(python)` 是**上面那棵会动的树**的产物;
  `owed-executions` 是**本轮自己新登记的那一行**(按定义即 OWED);
  `trunk-red(python)` 真实的那一条(`test_carrier_terms.py`)**已在 §4.5 修到根上**。
- 🚦 **铁律 6 三条腿(两次真跑,安静树,读数逐字)**:
  推分支那次 `GATE_EXIT=0  CLEAN`(0 警告)/ `py gate: 98 ran, 0 findings, 0 uncertifiable, 29.2s` /
  `lua gate: 359 ran, 0 findings, 0 uncertifiable, 9 known-red, 361.9s`;
  rebase 之后推 `main` 那次 `GATE_EXIT=0  CLEAN` /
  `py gate: 98 ran, 0 findings, 0 uncertifiable, 29.8s` /
  `lua gate: 360 ran, 0 findings, 0 uncertifiable, 9 known-red, 365.4s`。
  ⛔ **全程未用 `RULE6_BYPASS`。**
  ⚠️ **推 `main` 第一次被拒**(`non-fast-forward`,录像组 06:4xZ 的两个 commit 先落地),
  `git pull --rebase origin main` `REBASE_EXIT=0` 后重推成功:`39b4c29d..f612e640`。
  本轮 `bots/`+`game/` **零 diff**(改的是 `tests/` 一个文件)⇒ 动态那半**未全量跑,不声称**。
- ⚠️ **开工自检读的是一棵会动的树,本轮第二件该登记的自伤**:自检 07:07Z 起跑,
  而我 07:08–07:15Z 一直在改 `test_set.md` / `armed_since.json` / `queue.json`
  ⇒ 它的 `queue-rulings` 腿读到的是**改之前**的三条 pending,
  `test_arm_since.py` / `test_pending_rulings.py` 两条 FAIL **是半改状态的产物**
  —— **定型后串行重跑,两条都是 0 failed**(18/0 与 783/0)。
  ⚠️ 与 §EP.7 / §ER.6 / 上一轮 ⑤ 同族**第四发**:上一轮的成因是**并发写同一个开关**,
  本轮是**树在变**。**上一轮逐字记过「前者靠先定型再跑」,而我这一轮正是没先定型。**
- ⚠️ **`trunk-red(python)` 本轮已清零**:自检那一趟报的三条
  (`test_arm_since` / `test_carrier_terms` / `test_pending_rulings`)里,
  **两条是上面那棵会动的树的产物**,第三条 `test_carrier_terms.py` 是真的
  —— **5 条挂了四轮 + 5 条我造的,本轮全部修到根上(§4.5),69 checks / 0 failed**。
  ⚠️ 仍**未 `git stash` 复跑,不声称 main 是否同样红**;但 `bots/`+`game/` 零 diff,
  `tests/` 只动了 `test_carrier_terms.py` 一个文件。

---

## 七、下次触发

1. ⭐⭐⭐ **W37 汇总邮件 + 周日效率台账 `efficiency_202637.md`(章程 2e2,今天到期,已顺延两轮)** ——
   今天余下的任一次触发**必须**做掉;`DECISIONS_NEEDED` 第 15/16 条已备好当邮件正文。
2. ⭐⭐ **判定完结 ≥2(P4.2)**:`overchase` 的**认领豁免下一轮到期**(§3),
   `pullcad` 的**两条轴**要正面定一次(§3)。
3. ⭐ **`test_set.md` 拆文件(P4.3,DECISIONS_NEEDED 第 16 条,未回复即按 (甲) 执行)** ——
   ⚠️ 本轮它**又长了**:`§HJ` 一节加进去之后现量见收尾;拆文件**不删一个字节**。
4. **promote 期三个普查只读 `bots/`**(第九轮顺延)/ GH #523(第八轮未取)/
   `creeps_schema_gh581` 的 (C) 半(**本轮被 `hero-69` 第二次点名,它是那条读数的唯一堵点**)/
   GH #584 / 五条 `a_evidence_*` / patch 缺口 P3。

---

## 八、Token 用量

```
TOKENS total_in=15,665,466 out=88,526 turns=96
```
(统计时刻为收尾回填前;此后只剩这一次回填与推送,量级参考足够。)
⚠️ **本轮 `total_in` 偏高的归因是可命名的,不是空转**:两次等自检
(第一次被我自己的 `timeout 900` 掐掉、整份作废重跑)+ 一次五发变异台
+ 两次 `lua gate` 各约 6 分钟的推送门。**turns 96 与上一轮 118 相比是降的**
—— 涨的是 `total_in` 不是 `turns`,与上一轮同形。
