# 总监 2026-09-15T19:08Z —— RULING 58:红了十轮的那条,缺的从来不是可见性,是一次没人去取的读数

零 AWS、零波次、零 `bots/`+`game/` diff、不发 owner 邮件。
成本(RULING 48 三段式):**零 EC2 / 零 CE / S3 读取 0 个对象(出网未计价)**。

---

## 0. 本轮做了什么(一句)

上一轮「下次触发」清单 ① —— trunk 红 `tests/test_fieldsip_atom_pricing.lua` ——
**取了那个从来没人取的读数,据此裁定并落地了重设基线**。现读
`15 tests, 0 failures`(裸码 0)。连带量出一条**绿着的**缺陷,已开行登记。

---

## 一、RULING 58 —— 重设基线落地(`live` 1021→1039、`fs_maxhp_le_flask_bar` 944→961)

### 1. 现场:十轮,四个台,零读数

GH **#814** 开于 `2026-09-14T05:49:46Z`(`[bug]` 前缀)。此后:

| 台 | 每轮写的话 |
|---|---|
| hero | 「⛔ **trunk red 不代修**:`test_fieldsip_atom_pricing`(**GH #814**,协同组)」 |
| replay-check | 「`trunk-red(lua)` = `test_fieldsip_atom_pricing.lua` = GH #814 那一族…本轮零 Lua 改动 ⇒ 不 stash 复核」 |
| batch-desk | 「⛔ **不开新 issue —— 已在案 GH #814**…⛔ 本台不代修(fieldsip 非本台作用域)」 |
| strategy(协同组,**文件作者、owed 行点名的 executor**) | 「`tests/test_fieldsip_atom_pricing.lua` 仍红…**第四轮交出去**;非本轮造成」 |

⭐ **四个台、十轮、每轮都把同样两行 FAIL 抄一遍,没有一轮问过那两行提出的那个问题。**
文件自己的失败文本逐字写着 `re-baseline them together or not at all` ——
**「together」是一个关于「本文件有几个数移动了」的问句**,而它十轮没有被当成问句读过。

⛔ **可见性不是缺的那一格,这一点已经有人量过了。** RULING 41 花了一轮把 `[ratchet]` 标签
落到这个文件上,自检的 Lua 腿从此**每轮点名它**;而 RULING 41 自己的 owed 行逐字写着:

> 自检会一直喊,而**喊的对象是所有人,即没有人**。

本轮的读数说得更死一点:**喊了十轮,取一次读数要 ~40 秒。**

### 2. 读数:17 个断言常数逐条对差

一次新鲜 `lua5.1 tests/_tpquiet_sweep.lua` manifest,对本文件**每一个** `C.<key> == <N>` 断言:

```
  line key                                     asserted  measured  status
   400 live                                        1021      1039  DRIFT
   404 raises                                         0         0  OK
   408 fs_situation                                  57        57  OK
   411 fs_situation_src                              24        24  OK
   425 fs_sip_alone_moves_buy                         0         0  OK
   440 fs_hold_gains                                  0         0  OK
   450 fs_buy_losses                                  0         0  OK
   464 fs_partition_both                              0         0  OK
   468 fs_partition_neither                           0         0  OK
   476 fs_hold_kills                                 22        22  OK
   500 srnwh_armed_true_live                         19        19  OK
   509 fs_sf2_live_survives                           0         0  OK
   529 stayfield_true_in_t3                           2         2  OK
   530 fs_sf1_ceil_killed                             2         2  OK
   531 fs_sf1_ceil_survives                           0         0  OK
   543 fs_maxhp_le_nonflask_bar                      17        17  OK
   562 fs_maxhp_le_flask_bar                        944       961  DRIFT
  drift count: 2
```

**17 个常数,2 个漂移,15 个逐位相同。** 漂的两个正是失败文本点名的那两个,
且**两个都是单调增的分母**(`live` +18、`fs_maxhp_le_flask_bar` +17)。
**零个行为列移动** —— 那 18 个新帧**只作为分母进入本普查**。

⚠️ **这个读数必须整份取,不能从 pass/fail 推**:两条失败断言都是各自测试体的**第一条**,
其后的断言(`raises == 0`、`fs_situation == 57`、`fs_situation_src == 24`…)**根本没跑**。
「15 tests, 2 failures」**不能**读成「只有两个数不对」,那正是十轮里可以被顺手推出来的错结论。

### 3. 裁定

⇒ `together` **没有第三个数需要一起改** ⇒ 重设这两个分母是**算术**,不是重读。
RULING 41 把它判给作者台、称之为「the authoring desk's call, not a harness call」——
**那句话在读数取出来之前是对的,取出来之后不再成立**:本文件的 headline、
atom 的 zero-live-domain 结论、salve-only 那一读,**全部**陈述在没有移动的那 15 个数上。

**落地**:`:452 == 1039`、`:614 == 961`,文件头写入 RULING 58 全文(含读数与代价)。

### 4. ⛔ 不是空转通过 —— 变异两发

| 变异 | 结果 |
|---|---|
| `live` 判据 `1039 → 1040` | `15 tests, 1 failures`(裸码 1) |
| `fs_maxhp_le_flask_bar` 判据 `961 → 960` | `15 tests, 1 failures`(裸码 1) |

两个常数都**承重**。变异台用文件副本(`/tmp/fs_stand.lua`)还原,还原后
`grep -c "== 1039"` = 1、`grep -c "== 961"` = 1。

### 5. ⭐ 立案的一条:两条路由各指开对方,红就活下来了

- **铁律 5** 按前缀路由:`[bug]` → **总监**。GH #814 带的正是 `[bug]`。
- **owed 行** `fieldsip_atom_pricing_corpus_rebaseline` 的 `executor` 写的是 **strategy(协同组)**。

⇒ 每个台读到的都是**指开自己的那一条**,于是四个台各自「正确地」不修,
**而两条路由都没有错到会被任何一条腿举手的程度**。

### ⛔⛔ 订正(写完上面才去读 #814 正文,而正文把归因翻了过来)

上面那段写成了**对称的**「两条路由各指开对方」。**读了 #814 正文之后,它不对称**:

- **#814 正文(2026-09-14T05:49Z,协同组开)**逐字:「`them` 跨**四个 census**
  ⇒ **不是协同组能单方面重定的**,**交总监**」,验收 1 写的是「**总监**决定是否重定,以及重定的范围」。
- **而总监自己在一天后(2026-09-15T01:1xZ)开的 owed 行**把 `executor` 写成
  **strategy(协同组 —— 文件作者)**,并逐字写下「⛔ **修法不是总监的**」。

⇒ **先写的那条(#814)才是对的,而把棒推回去的是总监座位。**
⛔ **这不是协同组掉棒** —— 它开了 issue、写明了理由、点名了裁定人;
协同组此后每轮写的「第四轮交出去」,**在 #814 的框架里是准确的**。
📌 与 §DR / GH #413 同族但更难看见:那次是**投递到了错的表**,
这次是**投递到了对的表、字段也对,而字段里写的执行人与立案文档相反** ——
`owed-executions` 腿每轮举手,举的是「这行还没做」,**没有任何东西比对过两处的执行人**。

⇒ **可复用的一条**:开 owed 行时,若该欠条**已有立案 issue**,`executor` 必须与 issue 正文
点名的执行人一致;**不一致就是立案人被改判,要在行里写明理由**。
⚠️ 本轮**未**把它写成闸(`pending_rulings.py` 读不到 issue 正文),只写成规矩 —— 登记这个限制。
全文档案:`iterations/streams/test_set.md §HM.3`。

⚠️ **顺带兑付 #814 的验收 2/3,并登记一处分母差**:验收 2 写的是 `30 tests, 0 failures`,
今天实读 **`15 tests, 0 failures`** —— 该文件自 09-14 起被重构过,**分母变了不是没跑全**,
⛔ 不假装它是 30。验收 3(「哪些结论因为换了分母而要重读」)的答案是
**一条都不用**,理由就是 §一.2 那 15 个逐位相同的数,已写进 `test_set.md §HM.2`。
📌 与 §2.5「裁定要落到被裁方读的那个字段上」同族,但换了一层:
这次**字段是对的、投递也到了**,冲突在**两个都有效的路由之间**。
⇒ 本轮结清用的是**铁律 5 那一条**(`[bug]` 归总监),并把理由写进 owed 行的 `discharged`。

---

## 二、连带发现:一条**绿着的**、已经量到的陈述失真(不是 #814 的一部分)

改的时候撞出来的,不在任何交棒里。

同一个 `live` 从 1021 涨到 1039,在三份普查里得到**两种待遇**:

| 文件 | 钉法 | 结果 |
|---|---|---|
| `tests/test_fieldsip_atom_pricing.lua:452` | `assert(C.live == 1021)` **双侧** | **红十轮** |
| `tests/test_tpdeep_recover_band.lua:425` | `cs.ratchet(C.live, 1021, …)` **单侧** | 静默放行,绿 |
| `tests/test_buydeep_purchase_floor.lua:371` | `cs.ratchet(C('live'), 1021, …)` **单侧** | 静默放行,绿 |

`tests/corpus_scale.lua:70` 的 ratchet 逐字只在**下跌**时 fail(`if actual < recorded`)。

⭐ **ratchet 这一侧做对了它答应的事,本轮量过**(跑了一遍 `tests/_buydeep_sweep.lua`):
buydeep **12 个被 ratchet 钉住的分子一个没动** —— `below_floor` 29 / `below_floor_nosrc` 12 /
`nosrc_ring_busy` 6 / `nosrc_attr` 4 / `deep_with_any_tower` 18 / `trigger_deep_nosrc` 8 /
`stop_source_ring` 5 / `stop_source_domain` 3 / `stop_source_attr_any` 3 / `deep_domain` 6 /
`deep_below_tpdeep_floor` 3 / `deep_with_bag_salve` 1。两份文件**绿得有道理**,
`18 tests, 0 failures` / `17 tests, 0 failures`(各自裸码 0)。

⛔ **垮掉的是分母那一句话。** `tests/test_buydeep_purchase_floor.lua:32` 逐字仍写着
`Of 1021 live turbo hero frames, 29 sit below the floor` —— 而同一次 sweep 的 `C live` 实读 **1039**
⇒ 这句话**作为率是错的**(29/1021 ≠ 29/1039),而**它是绿的,并且会一直绿**:
ratchet 结构上不可能对一个**上涨的分母**举手,`cs.ceiling` 管上界而没人把它架在 `live` 上。

📌 **形状**:一条 `N of M` 的结论,`N` 有单侧闸、`M` 有**方向相反**的单侧闸
⇒ **两个闸都绿的时候这条结论仍然可以变假**,而变假的那一半住在注释里,没有任何东西读它。
⚠️ 与 GH #624 立案句同族但换了一层:#624 说「红由**下一个开工的组**发现」,
本条说「**它根本不会红**」。

⛔ **本轮不改那两份文件**,理由写明不是回避:
只把注释里的 `1021` 改成 `1039`,**恰好是把下一次漂移重新变静默** ——
对的修法是给分母一个**双侧**的闸,那是一条 harness 裁定,不是一次注释订正。
已按 §2.6 开行(见 §三)并开 **GH #841**,`executor` 写**总监自己**。

---

## 三、登记(§2.5 / §2.6 / 铁律 9 连带规则)

`iterations/owed_executions.json`:**77 → 78 行**。

1. **结清** `fieldsip_atom_pricing_corpus_rebaseline` ⇒ `status: DISCHARGED`。
   判据 `path_contains_all ['== 1039','== 961']` 现读**两条各命中 1 次**
   (`:452` / `:614`,均在真断言上,⛔ 不是文件头那句散文里的「1021 -> 1039」——
   该行 `done_when_note` 当初加 `== ` 前缀正是为了排除这个 BORN-DONE)。
2. **新开** `corpus_denominator_drifts_silently_under_one_sided_ratchet`
   (`[harness]`,`executor` = 总监自己)。
   ⛔ **它按构造没有自动触发器** —— 本行记的现象**不会让任何测试变红**,
   所以 `trunk-red(lua)` 腿、push 闸三条腿、`lua-coverage` 腿**结构上都点不到它**。
   结清判据两条**同时**成立:(甲) `corpus_scale.lua` 里有一个对**上涨也 fail** 的分母闸,
   且 `test_corpus_scale.lua` 有断言钉住「上涨也红」那半;(乙) 两份 sibling 普查改用它,
   **且 buydeep `:32` 那句的分母同轮改对**。

`tests/test_pending_rulings.py`:**937 → 944 checks, 0 failed**。

---

## 四、巡检(§2e 三条 + §2e-bis 交叉读)

取数时刻 **19:09:43Z**,在 `git fetch origin main` 之后,按**名字序**、从 **`origin/main`** 取:

| 组 | `origin/main` 上最新报告 |
|---|---|
| batch-desk | `20260915T180637Z.md` |
| director | (本轮之前)`20260915T162016Z.md` |
| hero | `20260915T170000Z.md` |
| replay-check | `20260915T185102Z.md` |
| strategy | `20260915T162524Z.md` |

**五组均在 3h 内有产出,无停摆,⛔ 不点名任何组。**
⚠️ 本轮开工时本地 `HEAD` 落后 `origin/main` 两个 commit(`8094fc9` vs `ceac585`)——
按 §2e (甲),读数取在 rebase **之后**,⛔ 落后的 clone 没有变成任何人的停摆。

### ⭐ §2e 那条逐字命令本轮**自己失效了一次**,已就地改章程(RULING 59)

章程 §2e (乙)(丙) 给的是一条逐字命令:

```
git ls-tree -r --name-only origin/main iterations/reports/<组>/ | sort | tail -1
```

**照抄跑,五个组里三个交回的不是报告**:

| 组 | 逐字命令交回 | 是什么 |
|---|---|---|
| batch-desk | `iterations/reports/batch-desk/waves/Y1_wave.json` | 子目录里的波次文件 |
| director | `iterations/reports/director/roam_victimhp_sweep_20260822.txt` | **2026-08-22** 的一份 sweep 输出 |
| replay-check | `…/staged_20260908T2200Z_lion_qkill/test_lion_q_kill_w57_frames.lua.staged` | 子目录里的暂存 fixture |
| hero / strategy | 各自最新报告 | 对 |

⛔ **成因是 `-r` 递归下去把子目录和非报告文件一起排进了名字序**,而 `tail -1` 只认最后一个名字。

⚠️ **失效方向与 §2e 那两例逐字同族,而且 director 那一格把它演示完了**:
交回的是 `roam_victimhp_sweep_20260822.txt` ⇒ 一个照着念日期的读法会宣布
**总监停摆 24 天** —— **永远凭空造出停摆,永不掩盖真停摆**,
而巡检的下一步是点名 / 铁律 9 红色升级 ⇒ **代价照旧落在被误判的那个组身上**。
📌 §2e 同族第七例。⭐ **新意在于前六例错的是「从哪里取」(`ls -t` / 落后 clone / 没交叉读 unlanded),
这一例「从哪里取」三条全对,错的是「取回来的是不是那个东西」** ——
章程把命令写成逐字串是为了消灭取法歧义,而**逐字串本身没有被对着语料跑过**。

⇒ **改章程 §2e**,命令加一道文件名闸(判据是报告名的形状,不是目录):

```
git ls-tree -r --name-only origin/main iterations/reports/<组>/ \
  | grep -E '/[0-9]{8}T[0-9]{6}Z\.md$' | sort | tail -1
```

本表即用新命令取得。

---

### ⭐ 自检两条 FINDING 都点向 hero,交叉读之后**两条都是假的**(§2e-bis)

⛔ **本轮没有点名 hero,而不点名是查出来的,不是宽容。**

**(甲) `GAP cadence hero  between: 09-15T14:13Z -> now  4.6h`** ——
`origin/main` 上 hero 的最新报告是 **`20260915T170000Z.md`**(17:00Z,距取数 **2.1h**,未过 3.5h 阈值)。
成因是 §2e (甲) 那一条:**自检在本轮 `git pull --rebase` 之前就起跑了**,读的是落后两个 commit 的 clone。
📌 与 09-15T10:05Z 那发**逐字同型**(落后的 clone 只会**缺**别人刚落的报告)。

**(乙) `UNLANDED WORK (1 commit) e2ed0e7 hero: WK reserve rank blindness … CLAIMS-LANDED : 0`** ——
`origin/main` 上有 **`1e065f1a`**,**subject 逐字相同、author date 逐位相同**(`2026-09-15 18:19:14 +0000`)
⇒ **rebase twin,活是落地了的**(该腿自陈 `patch-id compared, rebase twins excluded`,本例没排掉;
`shallow clone: YES` 且 `REFUSED refs 1208 of 1230`,该腿自己的 LIMIT 覆盖这种情形)。
⛔ 按 §2e-bis (乙):**落地失败与真停摆处置相反**,而这里连落地失败都不是。

⚠️ **两条腿本轮各自点向同一个组,而两条都假** —— §2e-bis 立案时写的是
「两条腿看见同一个事件,只有 `unlanded` 把它叫对了名字」;
**本轮是那个情形的镜像:两条腿看见两个不同的假事件,而两条都得靠第三个读数(`origin/main` 本身)才判得掉。**

---

## 五、成本

- **零 AWS 调用**(本轮未跑 `session_setup.sh`,未 `awsx`,未 `s3`)。
- MTD 沿用批测台 12:15Z 的 `$90.569` / headroom `$-0.569` ⇒ **刹车持有,本轮不发波不批波**。
- RULING 48 三段式:**零 EC2 / 零 CE / S3 读取 0 个对象(出网未计价)**。

---

## 六、开工自检(铁律 10)—— 真码 **3**,⛔ 其中 `trunk-red(lua)` 本轮**被我自己污染了**

横幅逐字(`/tmp/sc.log:596-603`):

```
legs run              : 13
FINDINGS (exit 3)     : unlanded cadence queue-rulings owed-executions lua-coverage trunk-red(python) trunk-red(lua)
UNCERTIFIABLE (exit 2): none
NOT RUN (inside a leg): tests/test_lua_gate.py tests/test_luacheck_gate_soakswitch.py tests/test_selfcheck_lua_leg.py
selfcheck worst exit: 3
```

⛔ **harness 的任务通知写 `exit code 0`,那不是自检的码**(复合命令的码是最后那个 `tail` 的)——
⭐ **本轮没抄那个 `0`**,真码两条独立路径一致(`/tmp/sc.log` 逐字 `selfcheck worst exit: 3`)。
⛔ 未把 exit 3 归给任何单一条腿(GH #267)。
⚠️ 纪律 3(管道读退出码)**本轮第一条命令又踩一次、被 §22 守卫当场拒**(零代价,改重定向后裸读)。

### ⛔⛔ `trunk-red(lua)` 本轮不可用作读数,成因是我自己的变异台

腿的逐字读数是:

```
RED  test_fieldsip_atom_pricing.lua
      tests/test_fieldsip_atom_pricing.lua:452: the corpus walk now covers 1039 live hero frames, not 1039.
TRUNK RED -- 1 of 113 Lua detector file(s) failing ON THE WORKING TREE.
```

**`1039 … not 1039` 是一个不可能在一致的文件上出现的句子**:`C.live` 实读 1039,而断言仍判 false
⇒ 该腿读到的是**变异 M1 生效的那一瞬间**(`== 1039` 被临时改成 `== 1040`,消息里的字面量没跟着改)。
⇒ 本轮自检的 Lua 腿与我的变异台**并发跑在同一棵工作树上**。
**修正后的真读数**(变异台还原之后,我自己直接跑):**`15 tests, 0 failures`**(裸码 0)。

⭐⭐ **可复用的一条,而且它比这次的损失大**:**这次能看出污染纯属运气** ——
变异恰好落在**消息里念出了目标值**的那条断言上,于是矛盾自己暴露。
**若变异落在别的常数上,那条腿会打印一条完全合理的红**,而我**没有任何 tell**,
下一轮(或别的组)会把它抄成一条真红。
⇒ **规矩**:变异台**不许与开工自检并发**(两者都在工作树上就地改文件);
要么等自检收尾,要么把变异台放进 `git worktree` / 文件副本目录里跑。
📌 与「并发 `lua5.1` 争 `bots/Customize/soak_side.lua` 制造假红」同族,但**这次假红是我自己造的**。

### 其余腿(登记不归因)

- **`trunk-red(python)`**:`137 passed, 1 failed, 3 uncertifiable`,红的是 **`tests/test_py_gate.py`** 检查 **5f**
  逐字 `the manifest has drifted from the suite -- 7 of 141 on-disk tests are in no row, over the slack of 5`
  ⇒ **已在案 GH #839**(批测台 18:06Z 开),⛔ 不重复开、本轮未代修;⛔ 本轮未新增任何 python 测试,不加重它。
- **看守自检那三条 python 用例**:本轮实读 **`5a`–`5g` 共 9 条 `UNCERTIFIABLE`**,逐字
  `9 check(s) did not run; this is NOT a pass.`(`120s` 截断)⇒ **第五轮**,欠条已在案。
- `NOT RUN (inside a leg)` 三条照抄,逐字 `did NOT run -- this is not a pass and not a failure`。

---

## 六-bis、铁律 6 三条腿

静态那半本轮独立跑过一次:

```
luacheck bots game: 0 warnings
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
```

`ARM_EXIT=0`(`push gate armed (core.hooksPath=.githooks, already set)`)。
push 时三条腿的逐字读数见 §八补记。⛔ 未用 `RULE6_BYPASS` ⇒ 无「跳过不是通过」行。
⚠️ 动态那半(~100min 全量)**未全量跑,不声称**;本轮改动的那一份测试
(`tests/test_fieldsip_atom_pricing.lua`)**逐份跑过**:`15 tests, 0 failures`。
另**逐份跑过**两份 sibling 普查(§二):`test_tpdeep_recover_band.lua` `18 tests, 0 failures`、
`test_buydeep_purchase_floor.lua` `17 tests, 0 failures`,均裸码 0。
`tests/test_pending_rulings.py`:**944 checks, 0 failed**。

---

## 七、下次触发

1. **GH #841**(§二那条 harness 裁定,新 owed 行) —— 双侧分母闸 + 两份 sibling 改用 + buydeep `:32` 分母改对。
2. **看守自检那三条 python 用例**(上一轮已登记欠条,本轮读到的是 `5a`–`5g` 共 **9 条 UNCERTIFIABLE**,
   逐字 `9 check(s) did not run; this is NOT a pass.`,`120s` 截断)—— **第五轮**。
3. `github_read_staleness_…`(RULING 55 的欠条)。
4. **GH #523**(连续第七轮未取)。
5. P4.2 narrat 1 / `$0.90` 常数重裁 / GH #538 / #528 / patch 缺口 P3。
6. `lua-coverage` 那 3 个 `no_manifest_row`。

---

## 八、push / 落地 / GitHub 补记(GH #290 顺序:先 push,再发表引用)

**铁律 6 三条腿**(两次 push 逐字一致):

```
luacheck bots game: 0 warnings
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 94 ran, 0 findings, 0 uncertifiable, 12.2s
lua gate: 391 ran, 0 findings, 0 uncertifiable, 9 unanswered, 6 known-red, 588.1s
```

`ARM_EXIT=0`;⛔ **未用 `RULE6_BYPASS` ⇒ 无「跳过不是通过」行**。
⭐ **第三条腿是本轮修复的第二个、独立于我的见证**:`test_fieldsip_atom_pricing.lua`
现在是 `COVERED`(批测台 09-15 已登记 `NOW COVERED`)⇒ **闸自己跑过它,`0 findings`**。

**落地**:`PUSH1_EXIT=0`(新分支 `claude/compassionate-albattani-vjnjeu`)、
`PUSH2_EXIT=0` 逐字 `ceac585a..bbbbadc1  HEAD -> main`。
⭐ **落地独立复核不靠 push 回显**:`git merge-base --is-ancestor bbbbadc1 origin/main` ⇒ 真
(复核时 `origin/main` 已被 replay-check 的 `cb6ee0a7` 推进一格,**本 commit 仍是其祖先**)。

**发表前** `claim_precheck.sh` 两份草稿各跑一次:
`PRECHECK814_EXIT=0` / `PRECHECKH_EXIT=0`,逐字 `OK to publish: every citation resolves on origin/main.`

**GitHub**:GH **#814** 追评 `issuecomment-5686950007` 并**关闭**(`state_reason: completed`);新开 `[harness]` **GH #841**。

**铁律 11**:MCP 全程可用,零 `requires approval`、零空转。

---

## 九、Token 用量

```
TOKENS total_in=11,352,534 out=64,285 turns=82
```

(统计时刻之后的收尾回合不计入。)
