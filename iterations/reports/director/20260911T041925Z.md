# director 2026-09-11T04:19Z(RULING 13 — `pullcad` 陷阱的**倒像**)

**一句话**:**判定完结 2**(连续四轮 0 之后的第一轮达标)——`tbearly` 退集、`pullcamp`+`pulldrag`
作为**一个原子**退集,armed **37 → 34**;而本轮最该被读的不是那两条裁定,是
**§二:树里替 `pullcad` 陷阱写的两条注释,逐字只防住了正像 —— 促进杀掉「点它名的门」,
退集杀掉「只能经过它的门到达的域」,而没有任何东西会替第二种举手。**

零 AWS(**一次调用都没有**)、零波次、`bots/`+`game/` **零 diff**、**不发 owner 邮件**、
**无 promote / 无 reject / 无入集**(P4.2 冻结)。取活依据:上一轮「下次触发 ①」逐字
「**判定完结,主体,不许再让位**」+ owner **P4.2**。全文档案 `iterations/streams/test_set.md §GS`。

---

## 一、自检(铁律 10)

真码 **`SELFCHECK_EXIT=3`**(`legs run 11`),工具自打的来源行:

```
FINDINGS (exit 3)     : cadence owed-executions a-evidence-owed trunk-red(python) trunk-red(lua)
UNCERTIFIABLE (exit 2): none
NOT RUN (inside a leg): tests/test_lua_gate.py tests/test_luacheck_gate_soakswitch.py tests/test_selfcheck_lua_leg.py
```

⭐ **`queue-rulings` 本轮不在 FINDINGS 里**(上一轮清空的那条腿保持干净)。
⭐⭐ **纪律 3:本轮守卫拦下的那一次,拦对了,而我这次第一时间照做了** ——
第一条命令仍带 `| tail`,工具逐字 `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.`
/ `it has recurred 5x, every time as the first command of the round.`;改重定向重跑拿到真码。
⛔ **仍然记一笔:`rc.sh` 是习惯不是门(第十二次)** —— 本轮之后的每一条读退出码的命令都走了
`rc.sh` 或重定向,但那是我记得,不是有东西拦着。

⛔ **「后台包装吞真码」第十八次,如实登记**:harness 报 `[exited with code 0]`,
而落盘文件写的是 `SELFCHECK_EXIT=3`。**那个 0 是复合命令末尾 `echo` 的。**
本轮没被它骗到的唯一原因是我去读了文件。

两条 trunk-red **本轮不归因、不销案**(`bots/`+`game/` 我一行未改):
`test_focus_mana_cost_consumer_census.lua`(n 26→27)、`test_lion_ult_reserve_domain.lua`(rank-2 实例 2→7)
—— 都是**语料变大打红棘轮**那一族(GH #718/#650/#624),与上一轮的分辨结论一致。
`trunk-red(python)` 是 `tests/test_py_gate.py` 的 `12.03s > 12.0s` 预算行,**GH #728 已在手**。

⭐ **`a-evidence-owed` 这条腿本轮被我清成绿的**(见 §四),`RC_EXIT` 由 **3 → 0**。

---

## 二、⭐⭐⭐ 主轴:`pullcad` 陷阱的倒像

`jmz_func.lua:10936` 与 `:11085` 是本仓库**做对了一件事**的证据 —— 它们记住了 `pullcad` 陷阱,
于是 `pulldrag` 与 `campbind` 的门都写成 STANDALONE,**不与 `pullcamp` 合取**。理由逐字:

> Gated STANDALONE, not conjoined with 'pullcamp': a gate written as
> `IsSoakCandidate('pulldrag') and IsSoakCandidate('pullcamp')` would freeze
> FALSE the day 'pullcamp' is promoted … **Turbo and the pullcamp gate are already
> structural here anyway -- this code is only reached through J.ShouldPullNeutralCamp.**

**加粗那半句是真的,而它就是倒像的载体。** 调用图(本轮逐条核过,不是引注释):

| 步 | 读数 |
|---|---|
| `J.GetLanePullDragTarget` 在 `bots/` 里的调用点 | **恰好 1 个**:`mode_roam_generic.lua:454` |
| 那个调用点的作用域 | `if bot.roamCampPull ~= nil`(`:383`) |
| `bot.roamCampPull` 的赋值点 | **只有** `mode_roam_generic.lua:104`,值来自 `J.ShouldPullNeutralCamp` |
| `J.ShouldPullNeutralCamp` 的前两行 | `if not J.IsModeTurbo() then return nil end` / `if not J.IsSoakCandidate( 'pullcamp' ) then return nil end`(`jmz_func.lua:10642-10643`) |

⇒ **`pullcamp` 一退集,`pulldrag` 的域结构上归零。** 而:

* `check_armed_wiring.py` 仍报 **WIRED**(它检查调用点存在,不检查谓词能不能为真 —— **`pullcad` 陷阱的原始立案句,逐字适用**);
* `verify_coverage.py` 仍把它列作 armed 的 `INDETERMINATE`;
* 下一波的 verdict 仍回来「测过了,没效果」,**没有任何东西举手**。

**⇒ 房规补一条(RULING 13 立):动一个 id 的 armed 状态之前,两个方向都要查。**
(正像)**谁的门行点了它的名** —— `promote_atoms.json` + 门行 grep;
(倒像)**谁的代码只能经过它的门到达** —— 被门的那个函数的**调用图**。
房规此前只写了第一个方向,而这两个方向的失效读数**长得一模一样**:都是「WIRED、armed、没效果」。

⛔ **处置不是「所以不退集」**:`pullcamp` 是量到的错决策(下节),留着的代价是**每一波所有 id 的 (b) 都掺沙子**;
正确处置是**把被带走的那条一起退,并把重新入集绑成一条**。

---

## 三、两条裁定

### 3.1 `tbearly` 退集(37 → 36)

录像组 2026-09-10T22:01Z:`VERIFY id=tbearly verdict=SILENT episodes=0`,并逐字建议退集
(「P4.2 下**最便宜的一格**」)。**本轮独立复核的是判据,不是转载判决** ——
而复核把结论收紧了一格:

* 外层合取项 `mode_farm_generic.lua:506` 是 `not J.IsLateGame()`;
* `jmz_func.lua:4757-4762` 在 turbo 下是 `DotaTime() > 18 * 60`;
* ⇒ 进到 `:555` 那一行**必有** `DotaTime() <= 1080`;
* armed 把 `nEarlyClock` 由 `25*60` 选成 `18*60`,判据是 `DotaTime() < nEarlyClock`;
* ⇒ 两条腿**只在 `DotaTime() == 1080.0` 这一个瞬间不同**(armed 假 / baseline 真),其余每一帧逐字节相同。

⭐ **「域是空集」与「域是帧网格上的一个零测单点」不是一回事,而差别有实际后果**:
前者干净、好引、**可被一帧反驳** —— 下一个读者拿那一帧当反例,会以为整条裁定塌了;
后者同样支持退集,而且**反例已经写进结论里**。
同族第三发(§FW.2「子句为假 vs 仪器看不见」、§GF.4「结论活着理由死了」):
**一个对的结论骑在一句稍微过强的话上,过强的那一句才是下一轮的坑。**

⛔ **不删代码**:那次 `a and b or c` 惯用法改写修掉的是真缺陷,
`tests/test_turbo_ternary_dominance.lua` 的全仓棘轮继续有价值。退的是测试集里那一格。

### 3.2 `pullcamp` + `pulldrag` 一个原子退集(36 → 34)

录像组 2026-09-10T16:01Z:`VERIFY id=pullcamp verdict=BUGGY episodes=28`,**W62 独立复现**。

| 读数 | W63 | W62 |
|---|---|---|
| 连接率 | **1/15 = 6.7%** | 4/16 = 25.0% |
| 开戳后 15s 掉血中位 | **17pp** | 12pp |
| 掉到 35% 以下 | 3/29 | 2/39 |
| EDGE CONTROL(跟随小野离营地最远)中位 / max | 833 / **1272** | 826 / 1120 |

**两波 EDGE CONTROL 几乎逐位相同 ⇒ 低连接率是拖拽的物理上限,不是估计量看漏。**
而被选中的营地到**任一**兵线的最小垂距是 **2912 / 2372** —— **开戳那一瞬间就不可能连上**。
帧证据:`…715cc0/20260910_124850_slot4` CM t=345.4–356.4(要价 **19pp 血 + 13 秒对线时间**)、
`…16a195/20260910_123421_slot4` pudge t=99.4(戳完就走,连拖都没拖)。

⇒ **这不是「没测出效果」,是量到的、带价钱的错决策。**
`pulldrag` **没有独立裁定**,它随原子走(§二);它自己的读数仍是 `verify=2 / INDETERMINATE / 159 episodes`,
仪器 `pulldrag_walk.py` 活着,**重新入集的条件就是 `pullcamp` 重新入集**。

### 3.3 计数纪律

**判定完结按原子记 2,不按 id 记 3。** `pulldrag` 没有被判有罪,把它算成第三个完结是虚报
—— 而 P4.2 的产出指标正是这个数,**在自己的指标上放宽定义是最没有意义的一种放宽**。

---

## 四、投递与登记(章程 2.5)

* **armed 串**:`test_set.md` 第 2 行 **37 → 34**,`309 字节`,md5 `7be691dcd2c2fd1ee73a630274007044`
  (旧:`335 字节` / `b525d51d4b4957e0e40f22f203aea641`)。**W64 及更早不与 34-id 家族并池。**
* **载体项 7 → 7 逐字不变,量出来的**:`carrier_terms.py --arm` 对两串各跑一次,`TERMS` 行逐字节相同
  (`crystal_maiden,lion,obsidian_destroyer,pudge,skeleton_king,spirit_breaker,zuus`),`0 unresolved` 两次;
  计数 `9 hero / 28 generic` → `9 / 25`(三条都是 generic)⇒ 选种解空间不受影响。
* **正像陷阱查过了**:三条各**恰好一个** gate 点(`mode_farm_generic.lua:555` / `jmz_func.lua:10643` / `:10943`;
  `pullcamp` 另有的两处是**注释**不是门行),门行上没有第二个 id;`promote_atoms.json` **一行都没点名**这三条。
* **`state.json`** +2 键:`tbearly_RETIRED_20260911` / `pullcamp_atom_RETIRED_20260911`。
* **`owed_executions.json`**:44 → **49**(见下),其中 `pullcamp_atom_readmission` 是 **owner P1 的那一棒**。
* ⚠️ **`queue.json` 本轮无对应请求**(这两条不是谁提的申请,是录像组判决触发的主动裁定)⇒ 无 `director` 字段可写。
  **登记这一句免得下一轮把它读成掉棒**(§AW.1 的反方向,上一轮刚吃过一次)。

### 4.1 ⭐ 登记册当场抓到我自己做了一半的退集

py gate 在我改完 `test_set.md` 之后**当场红**:

```
FAIL  tests/test_arm_since.py  (exit 1)
  STALE ROW: `pullcamp` has a row but is not in the armed string; a 退集/promote must stamp `retired_at` on its row.
  STALE ROW: `pulldrag` …    STALE ROW: `tbearly` …
```

`iterations/armed_since.json` 的 `_maintenance` 逐字要求「退集或 promote 时给该行盖 `retired_at`(**不要删行**)」,
而我只改了 armed 串。补三行 `retired_at` + `retired_note` 后 `18 checks, 0 failed`。
⭐ **这正是 §GN(GH #624)那条腿买的东西第一次替总监自己工作**:红由**推的人**当场看见,
而不是由下一个开工的组几小时后发现。

### 4.2 五条 `a_evidence_*` owed 行(顺手清一条自检腿)

`a_evidence_owed.py` 报 **UNOWED 5**,并逐字给出最便宜的修法(「ONE row … not the analysis itself」)。
按 §GG 的先例开行(那四条 **4/4 在两天内拿到 VERIFY 行**;更早 §FB 的两条 **2/2 约三天**):

| 行 | id | 类 | 采购内容 |
|---|---|---|---|
| `a_evidence_abilanc` | abilanc | DELIVER | 跑 `abilanc_domain.py` |
| `a_evidence_blinkflee` | blinkflee | DELIVER | 跑 `blinkflee_domain.py` |
| `a_evidence_lf_rescue` | lf_rescue | DELIVER | 跑 `lf_rescue_null_channel.py`(⭐ 五条里唯一自带 baseline-vs-baseline 零通道的) |
| `a_evidence_tpcommit` | tpcommit | DELIVER | 跑 `tpdying_landing.py`(⚠️ **文件名按 `tpdying` 命名,主题句点的是 `tpcommit`** —— §GG.4 那一课) |
| `a_evidence_overchase_instrument`(`covers_ids`) | overchase | **MENTION** | **建仪器**,不是跑现成的 —— 全仓没有任何检测器在主题句点名它,唯一出现是 capmono 工具正文把它当**混杂因子**(反过来的意思) |

⛔ **第五条单列就是它的内容**:把一笔「建仪器」混进四笔「跑现成的」里,
会让下一个读清单的人按最便宜那笔的价钱给它排期。
⭐ `ownhalf` 在 §GG 时同样是 `MENTION`,今天读 `WORKING episodes=7`、仪器在树上 —— **这条路走通过一次。**
每行的 `done_when` 都是**裸读得出的**:一行 `VERIFY id=<id> verdict=… episodes=…`;
⛔ 「跑了工具」不算结清,⛔ `INDETERMINATE` **算**结清(本行买的是一次判决,不是一个好听的判决)。
读数:`a_evidence_owed.py` 由 `FINDING: 5 armed id(s)` / `RC_EXIT=3` → `every armed id carries a verdict or an owed row -- OK` / **`RC_EXIT=0`**。

### 4.3 退休一根棒,并更正它自己的验收句

`gh736_throttle_anchor_unreadable`(上一轮我开的)——**批测台 03:13Z 那一轮已执行**:选 **(A) 改记录不改门**,
把 `launched_at` 写进 `W64_wave.json`,取值逐字抄发波轮自己记的 `00:21:17Z` 而**不抄 AWS 的 `00:21:16Z`**
(**取晚的 = 解锁更晚 = 波更少 = 保守侧**),⛔ 没有为了让门变绿去加别名;(C) 交在 **GH #738**。

⛔ **而这一行自己的验收句写窄了,更正留在行里**:原文要 `THROTTLE_EXIT=0` **且** `anchor wave : W64`。
本行买的是「**闸能不能读到自己的锚点**」,`exit 0` 额外要求六小时节流窗已经开 —— **两件无关的事被写进同一个合取**。
批测台交的是 `exit 3 THROTTLED` 并逐字打出 `anchor wave : W64`:**闸读到了锚点、给出了裁决**,
`exit 3` 恰恰证明它在作答而不是拒答。⇒ 按实质结清。
⭐ **与 §GQ(甲)、§三 3.1 同族**:**验收句里的多余合取项,和结论里的多余强度,是同一个缺陷的两种写法。**

---

## 五、交棒(⭐ 先查重,结果是**不开我原本要开的那个号**)

`list_issues` 三十条读回来,**该开的两个号已经开着**,而且都是**点名总监请裁**的:

| issue | 内容 | 本轮处置 |
|---|---|---|
| **GH #712** `[bug]` | 「owner P1 第 3 棒交付:pullcamp 的 (a) = 机制 WORKING、选点 BUGGY;**请裁 DoD 4**」 | 追评本裁定并**关闭** |
| **GH #730** `[bug]` | 「tbearly 的 (a) = SILENT(构造性);并推翻 cap25 普查那一句」 | 追评本裁定并**关闭** |

⛔⛔ **而查重同时救下一次撞号:我在 owed 行草稿里预填过 `GH #739`,而 `#739` 今天 04:04Z
已被协同组自己的一条 issue 占用**(TP 在被仇恨的野营里读条)。已改成不预填号码。
**这是「上一轮刚踩过撞号 ⇒ 本轮先查重」连续第二次兑现。**

**新开一个 `[strategy]` issue**(两条合并,频率纪律:不为一轮开两个号):
1. `pullcamp` 营地选择器缺一条**兵线垂距上限**(录像组 §4 的 **1350** 提案;⚠️ 软肋是 n —— 连接分子 n=4、
   空带只被两波看过,录像组下一轮用 `pullcamp_camp_gap.py` 在 W64 上复读)。
   **重新入集 = 几何过滤落地 + 一份不再命中 2912/2372 那类营地的读数,`pulldrag` 同批回来。**
2. `mode_farm_generic.lua` 的出厂注释「The band this moves is 18:00-25:00」**是假的** ——
   该 band 整条在 `:506` 的 `not J.IsLateGame()` 之外,**而它正是让下一个读者相信这个 id 有域的那句话**。

⭐ **顺手修掉一句会被继续引用的错话**:`state.json:cap25_boundary_20260825.domains_unblocked` 写
`tbearly` 的域「constructively empty and **is now reachable**」,把挡住它的东西认成了 cap。
在同一个 key 上追加 `correction_20260911`,⛔ **原文一字不删**(删了就看不出当年入集依据长什么样)。
⚠️ 同 key 里关于 `alchrage` 的那半句**本轮未复核**,不在更正的声称范围内。

---

## 六、成本与巡检

💰 **本轮 AWS 调用 0 次,不作 MTD 新声称**(它是时间的函数;抄别人的快照正是围栏禁止的)。
转载批测台 03:13Z 的读数以供参照,⛔ **不据它决策**;三条线未改(围栏 $60 / 刹车 $90 / 批准线 $100)。
⭐ 批测台本轮**自己没发波**(闸 (i) `THROTTLED`,`Wait 11072s`),与 GH #721 的 `$85` operative ceiling 一致。

🩺 巡检:五组 24h 内全部有产出(batch-desk 03:13Z / hero 01:46Z / replay-check 01:08Z / strategy 01:31Z),
**无掉队组**。

⚠️⚠️ **owner 优先项核对(铁律 9)**:
* **P4.1 upstream 标尺波** —— 上一轮逐字写「再空一轮即按铁律 9 写进 DECISIONS_NEEDED」。
  **本轮核对:批测台 03:13Z 那一轮仍未发标尺波**(理由是闸 (i) `THROTTLED`,**不是拒绝**)。
  ⇒ **本轮按承诺执行,写进 `DECISIONS_NEEDED.md`**(见 §七),⛔ 不再顺延。
* **P1 拉野** —— **本轮推进了一格**:DoD 1(pullcamp 根因)**有结论了**(BUGGY,几何),
  DoD 2/3/4 的棒交在新开的 `[strategy]` issue + `owed_executions.json:pullcamp_atom_readmission`。
* **P4.2 armed ≤ 20** —— **37 → 34**,距目标差 **14**;判定完结 **2**(四轮 0 之后第一轮达标)。
* **P3 cap 10→25(GH #108)** —— 本轮未动,存量。

---

## 七、`DECISIONS_NEEDED` +1

按上一轮自己写下的承诺登记 **P4.1 标尺波连续未认领**(不是新问题,是计数到点)。
⛔ **不发邮件**(每周最多 1 封需要 owner 决定的邮件;本周额度留给更贵的问题,
且 P4.1 的处置是排期不是决策 —— owner 早就拍过板,缺的是执行)。

---

## 八、门与纪律

**铁律 6 三条腿,全绿,无 `RULE6_BYPASS`**:

```
GATE_EXIT=0  CLEAN (iron rule 6 static half passed) / luacheck bots game: 0 warnings
py gate: 95 ran, 0 findings, 0 uncertifiable, 23.9s
lua gate: 331 ran, 0 findings, 0 uncertifiable, 15 known-red, 239.9s
```

⭐ `known-red` **15,与上一轮持平**;⛔ **本轮没让它变小**(我的改动不碰 `bots/`)。
⚠️ py gate **第一次跑是红的**(§4.1),那条红是我自己的半个退集,**不是存量**。
⛔ **Lua 动态全量未跑不声称**(~100min,GH #124;`bots/`+`game/` 零 diff)。
**铁律 11 未触发**(GitHub MCP `list_issues` 调用成功,无 approval 提示)。
**GH #290 顺序**:先 push 再发表;发表前跑 `claim_precheck.sh`。

---

## 九、下次触发

1. ⭐⭐⭐ **判定完结继续做主体,目标 ≥2** —— 本轮之后最便宜的一档**不再是 `verify=0`**,
   是 **`WORKING` 那十条**(`fieldbuy` 785 episodes / `liondrainstop` 36 / `fieldsip` 15 / `tpdeathbuy` 14 /
   `zusult` 11 / `ownhalf` 7 / `zusboltdom` 6 / `lionqdmg` 3 / `wandbleed` 1 / `wandbleed2` 1):
   (a) 已买到,**卡住 promote 的是 (b) 与 (c)**。⇒ 下一轮从 `fieldbuy` 起做三条件判定。
   ⚠️ **promote 的红线是 §DU.6**:动作是**改那一行 gate 实参**,不是把 id 从串里删掉;
   并且**先查倒像**(§二):谁的代码只能经过它的门到达。
2. ⭐⭐ **倒像的全仓普查本轮没做** —— 还有哪些 armed id 挂在**另一个 armed id 的门**下面?
   这是一个可以做成检测器的问题(被门函数的调用图 × armed 串),**做成腿比再记一发便宜**。
3. ⭐⭐ 核 `pullcamp_atom_readmission` 与五条 `a_evidence_*` 的首次活读数(⚠️ 都是别组执行,提醒不是证明)。
4. ⭐ 存量:GH #707(闸把不相干失败教人 bypass)/ GH #358 第九次吃腿 / `a_evidence_pulldrag`、`a_evidence_tpgap`
   两行退休(**第六轮顺延** —— ⚠️ 注意 `pulldrag` 本轮已退集,那一行的处置要一起重读)/
   `wave_reachable_delta.py`(第八轮顺延)/ P3 GH #108 / `rc.sh` 是习惯不是门(第十二次)。

---

## 十、token 用量

```
TOKENS total_in=10,960,502 out=67,284 turns=71
```
(到统计时刻为止;统计后的收尾回合不计入。)
