# 总监 2026-09-12T10:3xZ(第四轮)

**RULING 26/27 —— hero-62 并入 hero-38 的既有遍历(不开第二次)+ hero-63 分母更正。判定完结 2,满足 owner P4.2 的 ≥2。**

零 AWS(**一次调用都没有**)、零波次、**`bots/` + `game/` 零 diff**、**不发 owner 邮件**、
无 promote / reject / 入集 / 退集。**armed 27 一字未动**(离解冻线 ≤20 差 7)。
档案 `iterations/streams/test_set.md §HC`(§HC.0–§HC.4);
机器键 `state.json:zusfightquorum_SCAN_MERGED_20260912` / `zusarcimm_SCAN_DENOMINATOR_RULING_20260912` /
`strata_clause_efficacy_UNTESTED_20260912`。

---

## 1. 上一轮 ⑨① 交下来的两条,裁完了

| | 裁定 | 实质 |
|---|---|---|
| `hero-62`(`zusfightquorum` 域) | **APPROVED-SCAN-MERGED** | **不开第二次遍历** —— 同域同 id 的 hero-38 **09-06 就已批准、至今未执行** |
| `hero-63`(`zusarcimm` 频率) | **APPROVED-SCAN** | 批扫描;**实质是把分母钉死**,预登记的两条阈值线**挂错了分母** |

⭐ **先回答上一轮提的那个问题**(「买的是不是已 armed 的 id 的读数」):**两条都不是** ——
`zusfightquorum` 与 `zusarcimm` **都不在 armed 27 串里**(`arm_since.py` 本轮现读)。
**但这不是拒的理由,而是批的理由**:两条请求的预登记判读**各自都带一条退场支路**
(hero-62「联合占比 ~0 ⇒ 退回/重写」、hero-63「<0.1% ⇒ 域太小,不占 armed 位,退场」),
⇒ 它们是**入集之前的那道便宜滤网**,**方向与 P4.2 同向不是反向**。
不裁它们并不能把 id 挡在集合外 —— 只会让它们停在一个**默认下一步是「申请取证波」**的状态里。

## 2. RULING 26 的核心:合并不是新批(§HC.1)

hero-38 与 hero-62 是**同一条 id 的同一个域**,读数互相包含(hero-38 买联合量的 `==5` / `==3,4`;
hero-62 买同一联合量的 `>=3` 占比**外加两个边缘分布**)⇒ 共用 owed 行 `hero_domain_scan_2_30_31`。

⭐ **边缘分布保留,理由是算术不是周到**:联合量读到 0 时**分不出是计数腿为零还是 fight 腿为零**,
而两者的下一棒完全不同 —— 后者正是 hero-38 自己第 (3) 列预登记、**且明写不许与 (2) 合并**的那个岔路。

⛔ **请求方带来的前提更正只登记不据以行动**:hero-62 称当初那 1012 个视角数的是**对面那一队**,
只在 Zeus 身上重测后 shipped 合取式 **45/45 恒假**。**不退集,两个独立理由**:
(i) 该 id **gated 且不在 armed 串里** ⇒ 真实对局 byte-for-byte inert,没有东西需要撤;
(ii) **45 帧是 fixture 语料不是关于 Turbo 的陈述** —— 而「这个域在真实 Turbo 里多大」**正是这次扫描要买的那句话**。
用语料的窄去判域的窄,就是 §HA.3 逐字警告的「零是仪器的还是域的」。

## 3. RULING 27 的核心:阈值一个数字没动,动的是它跟谁比(§HC.2)

- ⭐ **尺子已存在**:请求点名的四种法免**逐字就是** `axecallbkb_domain.py:132-137` 的 `IMMUNITY_MODIFIERS`。
- ⭐⭐ **分母**:acceptance 第 (2) 列的分母不是这条杠杆的域。现读源码,
  `J.GetVulnerableWeakestUnit(bot, true, true, nCastRange)` 的 **`bHero=true`** ⇒ 只有英雄没有小兵
  (`jmz_func.lua:253-262`),再经 `GetAttackableWeakestUnitFromList` 滤一道。
  请求自己的 fixture 把这个差量了出来:**60 帧 → 15 次出目标 → 1 次法免 = 4 倍**。
  ⇒ 同一枚帧读作 **1.7%** 或 **6.7%**,而两条线是 **≥1% 买 / <0.1% 退场** ⇒ **4 倍足以搬过买入线。**
- ⚠️ **本轮自己的近失,照登记**:第一版的更正理由是「最弱单位多半是小兵」——
  `bHero=true` 把它否掉了。更正活下来但量级从「数量级」缩到 4 倍,
  **而 4 倍之所以仍值得裁,恰恰因为去跟阈值比了一次**。
  📌 *不比的话,我会用错的理由写下对的结论,而下一个人继承的是理由。*

## 4. ⭐⭐⭐ 本轮最该被读走的一条:4(i-a) 的条款写进 acceptance 有没有用,今天是 **0/12 未经检验**(§HC.4)

现读 42 条 approved-SCAN 行:**12 条**点名条款 / **30 条**沉默 / **4 条**带真正的收割,
**而那 4 条的交付里点名 ab/ba 的是 0**。

看起来是「沉默 ⇒ 沉默」4/4 的相关性,**它不是** —— **十二条写了条款的行一条都还没被收割过**。
📌 *一个只看「有没有写」的普查,会把「写了的那些还没到期」读成「写了的那些都合格」。*

⭐ **`hero-1` 是最值钱的一条,而它是工具数出来的不是我眼睛看出来的**(手点时只看了 `delivered-*` 状态):
它的 `result` 里**确实**写着 **`radiant 77 / dire 76`** —— **那是局数不是读数**,
而同段里真正的读数(`153/153 局终 level ≥ 7`,min 7 / median 11 / max 16)**是并池的**。
**GH #329 的立案失效方式,逐字复现在一条没人举过手的行上。**

**落地的 instrument(`pending_rulings.py` 新一节,两条棘轮):**
- `STRATA_SILENT` —— **09-12 及以后**裁的行 acceptance 沉默 ⇒ finding。⛔ 用截断日不全扫:
  一条对着 30 行开火的腿就是 GH #276 的「永远满的小节」;存量**打印 id 但不驱动退出码**。
- `STRATA_ORDERED_NOT_DELIVERED` —— 写了条款而收割没带 ⇒ finding。**今天人口 0 是构造性的**,
  第一条落进去的行**就是本轮买不到的那份证据**。

⛔ **两处差点写错的数,都是现量出来才改的**:(甲) 裸 `分层` 把 `hero-33`(**Frostbite 等级**分层)
与 `hero-35`(「按 (4) 的分层」)**数成了带条款的行** —— 松标记数出 12(真值 10),
**两个假货全落在「这一行没问题」那一侧**;(乙) 想用**长度**分「交付」和「便条」——分不开
(`hero-10` 指路便条、`hero-51` 裁定全文,**都长,都不是交付**),改按 `deliver*` 状态 ∪ 「交付」判,真实队列 **4:2 分干净**。

## 5. 已知缺口,登记但本轮不修(§HC.3)

hero-38(09-06 批)与 hero-62(09-12 提)是同一个域,**而没有人错**:
§DR 管的是**裁定的下行**,对**请求的上行重复**结构上无话可说。
**不修,代价先算过**:重复的成本是一次合并裁定;防它要么再加一张靠人更新的表(§GB.3 已量过那个失效方式),
要么在工具里按 id 查重 —— 而 **`bundle` 不是 id**(hero-62 的 `bundle` 是
`zusfightquorum (取证对象是这个已落地 id 的**域**,不是一个新 id)`,hero-38 的是 `zusfightquorum`),
**词法查重会漏掉这一对,而这一对正是立案现场**。⇒ 第二个数据点出现时再修。

## 6. 变异台

`mutstand_pending_rulings.sh`:**19/19 CAUGHT / 0 SURVIVED**,还原走文件副本 + `sha256sum -c`,
基线 **727 checks / 0 failed**。新增 M14–M19,**M14 就是我真犯过的那个错**(把 `分层` 塞回标记串)。
⚠️ **M12 先 APPLY-FAILED 中止过一次,而那是台子在干活**:它引的 `%-9s`/`state` 在 IN-FLIGHT claim 落地时
已改成 `%-11s`/`shown`,**靶子早就飘了**;默默跳过的台子会报「12 个全中」而其中一个**从没上过台**。已重新锚定。

## 7. 连带落地的机器不变量

给 hero-62/63 写 `director.owed_row` 之后,`tests/test_domain_scan_pass_pins.py` **当场变红**
(`['hero-62','hero-63'] != []`)—— **§GB.3 立的那条双向不变量按设计开火**,
已把两根针脚加进 `done_when.contains`(24 条)并在 owed 行的 `ruling` 里写下两条**执行方照抄**的硬约束
(hero-63 的双分母 + 这次遍历按写了 4(i-a) 的 acceptance 交付)。复跑 **OK**。

## 8. 健康度 / 成本 / 闸

- 💰 **零 AWS**,一次调用都没有,**不作 MTD 新声称**。三条线(围栏 $60 / 刹车 $90 / 批准线 $100)一字未动。
- 🩺 五组 24h 内全部有产出,无掉队组。**armed 27**,离 P4.2 解冻线(≤20)差 **7**。
- `a_evidence_owed.py` 本轮现读:**`PRE-ARM` 人口 1**(`arbheart`,与上一轮同),`every armed id carries a verdict or an owed row -- OK`。
- ⚠️ **开工自检本轮没有可引用的读数**:09:50Z 起跑的那次在 Lua 腿上仍在跑时被我**主动杀掉**,
  理由是它开工读的树即将在我脚下改动(与 02:45Z 那轮同一处置)。**这不是通过,是没跑。**
  收尾在安静树上重跑,读数见下。
- 🚦 **铁律 6 三条腿**:见文末「收尾读数」节(push 钩子自己打的那三行为准)。

## 9. 下次触发

1. ⭐ **本轮期间新到的四条 queue 行,我没裁**(现读,**不是我造成的**):
   RIDESHARE `hero-64`(`wkqcommit`)/ `harness-corpus-creeps`(**[harness] 归总监**);
   OTHER `hero-60`(`cmwhit`)/ `hero-61`(`cmtfclock`)。
   ⚠️ 按 §BB.4 rideshare 应当轮裁;本轮工作单元已满(判定完结 2 + 一条 instrument),
   **它们在 `pending_rulings.py` 的第一节里每轮都会被点名,掉不了**。
2. 判定完结 ≥2 继续,**先跑 `a_evidence_owed.py` 现读**,`PRE-ARM` 计数一起抄。
3. ⭐ **入集裁定落地时看一眼 PRE-ARM**(上一轮 ③,仍然成立):**搭车入集**是最容易造出 PRE-ARM 的路径。
4. ⭐ **新增待观察**:`STRATA_ORDERED_NOT_DELIVERED` 人口从 0 变 1 的那一轮,
   **那一行就是「条款不充分」的证据**,要当场重裁「这个检查该住在哪一层」(裁定时还是收割时)。
5. 存量顺延:三个 promote-time 普查只读 `bots/`(**第四轮顺延**,人口读数在 `20260912T054500Z.md §10`,不要重量)/
   两条仍红的 trunk 腿 / GH #584(**第七轮顺延**)/ 批测台交棒 ① / 五条 `a_evidence_*`(**第十一次顺延**)/
   patch 缺口 P3 / `DECISIONS_NEEDED` 15/18/19 等 owner(随 W37 周信)。

## 10. 收尾读数

### 10.1 铁律 6 三条腿(`git push` 钩子自己打的,四次 push 读数一致)

```
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate:  96 ran, 0 findings, 0 uncertifiable, 36.9s / 37.6s / 37.4s / 38.0s
lua gate: 346 ran, 0 findings, 0 uncertifiable, 9 known-red, 465.1s / 459.3s / 457.5s / 468.6s
```

⛔ **未用 `RULE6_BYPASS`**(四次都没有)。手动另跑过一次 py gate:`96 ran, 0 findings, 0 uncertifiable, 38.2s`。
动态半(GH #124,~100min)**不在钩子里也不被它声称**;`bots/`+`game/` 零 diff。

**push 退出码**:`PUSH1_EXIT=0`(分支)/ `PUSH2_EXIT=1`(**被拒:`fetch first`**)→
`PULL_EXIT=0`(`git pull --rebase origin main`,`ee42587c..91a16b83`,rebase 1/1 干净)→
`PUSH3_EXIT=0`(**落 main:`91a16b83..4a4f7e50`**)→ `PUSH4_EXIT=1`(分支 non-fast-forward,
rebase 改写了历史)→ `PUSH5_EXIT=0`(`--force-with-lease=…:e1e622c0`,`e1e622c0...4a4f7e50` forced update)。
⚠️ **那次 force 只动分支指针,内容此前已经在 main 上**;lease 钉的是 rebase 前的 sha,不是 `--force`。

### 10.2 开工自检(收尾重跑,安静树,**零 `timeout`**,退出码不经管道)

```
SELFCHECK_EXIT=3
legs run              : 12
FINDINGS (exit 3)     : unlanded cadence queue-rulings owed-executions trunk-red(python) trunk-red(lua)
UNCERTIFIABLE (exit 2): none
NOT RUN (inside a leg): tests/test_selfcheck_lua_leg.py
```

⭐ **本轮新落的那一节在自检里跑到了**(`=== iron rule 4(i-a): ab/ba disclosure on approved scans ===`,
`42 / 12` · `STRATA_SILENT: none` · `STRATA_ORDERED_NOT_DELIVERED: none` · `BACKLOG 30` · `DELIVERED-SILENT 4`)。

**逐条归因(不许写成「exit 3,全是 cadence」——GH #267 那句手工归因错过 22 小时):**

- `unlanded` —— **查过了,是瞬态不是掉棒,与上一轮同形**。点名协同组 `6c35d00`
  (`strategy: arbheart 的宿主 mode 在它自己那枚钉帧上十个出口一个都没到达(GH #775)`),
  在 `origin/claude/intelligent-hopper-zwdlgf` 上。收尾复查:**同一条工作已以 `69581984` 落在 `origin/main`**
  (标题逐字相同的 rebase 孪生),自检**撞进了那个组「推分支」与「push HEAD:main」之间的窗口**。
  📌 *这条腿对任何**正在跑**的组都会亮一次;`CLAIMS-LANDED: 0` 是文本匹配,**不是**「没落地」的判据。*
- `queue-rulings` —— **是我没裁的那四条,不是新缺陷**:RIDESHARE `hero-64` / `harness-corpus-creeps`,
  OTHER `hero-60` / `hero-61`(见 §9 第 1 条)。⚠️ 其中 **`harness-corpus-creeps` 按铁律 5 是总监自己的**。
- `cadence` / `owed-executions` / `trunk-red(python)` / `trunk-red(lua)` —— **与前三轮同一组存量**,本轮未新增。
- ⚠️ **`tests/test_selfcheck_lua_leg.py` 打了 `NOT RUN`(腿内)** —— 按铁律 10 的措辞,
  **那不是通过**;但它没把整轮抬到 exit 2(`UNCERTIFIABLE (exit 2): none`)。

### 10.3 本轮 issue

**新开 GH #776 `[harness]`** —— §HC.4 那条读数与两条棘轮的立案全文
(<https://github.com/dragonghy/dota2bot/issues/776>,评论 id `5432897971`)。
⭐ **顺序按 GH #290:先 `push`(`4a4f7e50` 已在 main)再发表引用**,不是反过来。

## 11. Token 用量

```
TOKENS total_in=15,984,062 out=83,293 turns=98
```
(统计时刻之前的数字;其后的收尾回合不计入。)
