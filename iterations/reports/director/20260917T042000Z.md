# 总监 2026-09-17T04:20Z —— RULING 69

**一句话**:上一轮给自己写的默认条款(「未回复 = 下一轮把推送顺序反过来」)差一轮就要执行,
而**否掉它的那份读数比它自己晚两小时到**;反序的两笔代价都是恒等式,其中一笔今天正挂在
GH #865 上,以四次复现的形式。**本轮裁定:顺序不动,并把它从散文变成一条会拒 push 的闸。**

零 AWS、零波次、`bots/`+`game/` **零 diff**、不发 owner 邮件。
成本(RULING 48 三段式):**零 EC2 / 零 CE / S3 读取 0 个对象(出网未计价)**。

---

## 一、本轮处理的是清单第 ② 条,而它的正确处置是「不执行」

上一轮(`20260917T010000Z`)交棒 ② 逐字:**「⑫ 的裁定(覆盖句 or owner 邮件,
⛔ 不许第三次只在报告里重复)」**,并在 `DECISIONS_NEEDED.md` 第 17 条写下
**「未回复 = 我按 (甲) 执行(下一轮)」** —— (甲) 即把**先 main 后分支**写成全队正路。

**本轮读到的东西把这条默认条款整个否掉了,而且不是靠偏好,是靠两个恒等式:**

| # | 机制(均现场核过源码) | 反序之后会怎样 |
|---|---|---|
| 1 | `.githooks/pre-push:215` 的第三腿 scope = `git diff --name-only origin/main...HEAD`,**空表按设计 = 跑全集**(fail-closed,逐字 `skipping needs a positive answer, never the absence of one`) | `git push origin HEAD:main` 把本地 `origin/main` 挪到 HEAD ⇒ **后一推 diff 恒为空** ⇒ 必跑全集 |
| 2 | `tools/agent/rule6_memo.py` 的键 = `(HEAD^{tree}, origin/main)`(`_git("rev-parse", "origin/main")`) | 同一个 ref 移动换掉键的后一半 ⇒ 孪生 push **恒定 MISS** |

⭐ **两条都不是概率,是恒等式** —— 不需要复现次数就能读出来,只需要读那两处源码。

⭐⭐ **而 GH #865(批测台 09-17T00:16Z 立)量到的比「慢」严重得多**:
反序之后那一推**四次复现全部挂死**(`timeout` 回 `124`,一次 >20min 未返回),
**第五次加 `--no-verify` 同一个 ref、同一棵树 ~10s 建好** ⇒ **网络归因被同一份读数证伪**。
判别子是**第三腿一行都没打**:`main` 那两推逐字打了 `lua gate: SKIPPED BY SCOPE`,
分支那四推日志停在 py gate,`lua gate:` 那一行**从未出现** ⇒ 进去了没出来。
⚠️ **规模**:远端 1,279 个 head,**1,246 个是 `claude/*`** —— 每个 Routine 会话建一个。

### 时间线才是本条的要害

- `2026-09-17T00:57Z` GH #865 立案(四次复现)
- `2026-09-17T01:00Z` **第 17 条写下**,含「未回复 = 下一轮执行 (甲)」
- `2026-09-17T03:15Z` 批测台 `batch-desk.md:12517` 交出三条互斥候选,逐字**「本台不代修,写入权在总监」**
- `2026-09-17T04:xxZ` 本轮

⇒ **第 17 条不是错的,它是过期的**:写它的时候那两笔代价还没被量出来。
⛔ **危险的不是那个提议,是它的默认条款** —— 「沉默 = 执行」让一份**写于读数之前**的文档
在**没有人再看一眼**的情况下变成全队正路。📌 与本章程 2.6 同族:
2.6 管「推迟的裁定会消失」,这一条管「**推迟的裁定会自己执行**」,后者更安静。

---

## 二、⚖️ RULING 69

**(甲)(乙) 都不执行;文档顺序(先分支、后 main)保持不变,第 17 条作为待 owner 拍板的问题撤回。**
⛔ **owner 不必去碰调度器,五条提示词里的那半句本来就是对的**,也不需要覆盖句。

**第 17 条真实存在的那一笔代价照修,但不用动顺序**:分支 ref 停在 rebase 之前 ⇒ 要
`--force-with-lease`。修法是**两推之前先 `git pull --rebase origin main`**,已写进
`.claude/rules/claude-code.md`。真撞上时会话分支是本会话独占的,`--force-with-lease` 安全。

⛔ **顺序不是 GH #856 那条读写竞态的对策**(#856 (丙) 已禁止这个推广);管那件事的是
读方走 `read_lua()` 判 exit 2,与谁先谁后无关。本轮**又撞了它一次**,见 §五。

### 落地三件(⛔ 不是只裁不修)

1. **`.claude/rules/claude-code.md` 的 `## Push path`**:顺序不动 + **写明为什么**
   (两个恒等式 + `639.7s` 实测 + GH #865)+ 前置 `pull --rebase`。
2. **钉子 `tests/test_push_order_contract.py`**(新,**0.017s**):断言**规则文件 / memo 的引用 /
   钩子的 scope** 三处必须一致。**13 checks / 0 failures**。
3. **`.githooks/pre-push` 落 GH #865 的 (C)**:第三腿**进去之前先打一行判词**,
   空 scope 那条分支明写「这是跑全集的慢路,而且如果你刚推完 main,这个空的意思是
   **『main 已经含有这些提交』而不是『没碰 Lua』**」。

### 为什么必须有第 2 件(而不是再写一段散文)

这条规矩的载体此前**只有散文**,而它**已经在两轮之内被反向理解了两次**:
第 17 条把它读成「写反了」,GH #865 正文逐字写着「本台按 `.claude/rules/claude-code.md`
的既定路径推送,**先 `git push origin HEAD:main`**」—— 而那个文件当时写的是**先分支**。
⇒ **两个独立的组,在同一天,把同一个文件读成了相反的意思。**
📌 本仓自己的账:一条只活在散文里的规矩,平均寿命约一轮。

### 变异台(⛔ 绿的测试在这里不算读数)

`control` 绿 ⇒ 6 个变异**各自死在自己那条断言上** ⇒ `restore` 复绿、`git status` 干净:

| 变异 | 死在哪条断言 |
|---|---|
| M1a 规则文件同行反序 | **BRANCH push must come before `HEAD:main`** |
| M1b 规则文件**两行**反序(现实中更可能的那种编辑) | 同上 |
| M2 memo 的引用反序 | memo **quotes a path that puts `HEAD:main` first** |
| M3 抹掉 `GH #865` 出处 | section **must name GH #865** |
| M4 钩子 scope 改成 `HEAD~1...HEAD` | scope **still computed against `origin/main...HEAD`** |
| M5 memo 的键不再读 `origin/main` | key **still reads `origin/main`** |
| M6 钩子丢掉 run-everything 默认 | empty scope **still means run-everything** |

⭐⭐ **第一版 M1/M2 死得不对,这件事本身是本轮第二个读数**:它们死在
「两条命令的那一行不见了」这条**形状**断言上 —— **红是对的,理由是错的**。
现实里的反序编辑会把两条命令拆成两行,那时旧版会打出一句**真话**
(「那一行不见了」)去描述**另一件事**(顺序反了)。⇒ 顺序断言已改为**只看两个裸 token 的位置**,
与周围标点无关;M2 同理(memo 那一侧原来的正则会在**它存在的理由**那个变异上停在第一个 push 上,
于是顺序检查**恰好被跳过**)。📌 这正是纪律 4「匹配的结论不等于正确的理由」的形状。

---

## 三、⛔ 更正一句我差点写下的话:#865 §四 的答案**不是我给的**,批测台一小时前就登了

我是先读源码、独立推出那两个恒等式的,写报告草稿时也顺手写了「已替 #865 §四 收口」。
**那句话是错的**:批测台 `2026-09-17T03:34:51Z` 的追评
(`issuecomment-5708068813`)已经把这件事**量完并登完**了,而且比我的推导更硬 ——
他们在 push 前后各打了一次 `rule6_memo.py key`:

```
PRE-PUSH   key=77e91482…  tree=1b4c320a…  base=0eb03ee6…  scope 4 path(s)
POST-PUSH  key=cf143a95…  tree=1b4c320a…  base=4e2fcf11…  scope 0 path(s)
```

`tree` 逐位相同、唯一变的输入是 `base`,再加 `ls .git/rule6_memo/` **只有 PRE-PUSH 那个键**
⇒ **`put` 存了、`get` MISS**,#865 §四 那个二选一**有了实测答案**,不是推理。
⭐ **我的独立推导只值一件事:它是同一结论的第二条路径**(源码 vs 现场),⛔ 不值一次署名。
📌 记在这里是因为**顺号抄功劳的失效方向很安静**:下一个读报告的人会以为这是总监买的,
于是**真正付钱的那一轮不会被计入任何台账**。

**而批测台在 §五 的 (A)/(B)/(C) 之外补了第四条,并点名交总监裁**:

> **(D) 只改顺序**:回到文档顺序(分支先、main 后)。GH #290 的「引用先可解析再发表」
> 改由 `claim_precheck.sh` 保障 —— 它本来就按 `origin/main` 解析。**零代码改动,
> (A) 与 (B) 同时不再需要。**

⇒ **RULING 69 = 采纳 (D)**。⭐ **这不是巧合地一致,是两条独立路径撞在同一个答案上**:
批测台从「memo 为什么没命中」往回推到顺序,我从「第 17 条要不要执行」往前推到同一处。

**#865 §五 + (D) 四条的处置**:
- **(D) 采纳,本轮落地**(见 §二):顺序不动 + 写明为什么 + 钉子。
- **(A) 仍然采纳,但降为深度防御,本轮不修**,登记欠条
  `owed_executions.json:gh865_prepush_scope_from_stdin`。**(D) 之后它仍有值**:
  空 scope 在**任何** `origin/main == HEAD` 的推送上都会出现(别的座位反序推、
  rebase 之后补推分支、推一棵没有新提交的树),**(D) 管的是我们的正路,不是所有路**。
  ⭐ 前提现场核过:**当前钩子一行 stdin 都没读**,那四个字段可用。
- **(B) 不本轮买,但⛔ 不是「并进 (A)」** —— 这一句我草稿里也写错了,批测台 §三 已经
  逐字否掉它:**(A) 只动第三腿的作用域,`memo_key()` 一个字没动 ⇒ 孪生 push 照样 MISS**。
  ⇒ (B) 要动的是**键本身**。⭐ **而 (D) 之后 (B) 的收益变成零**:按文档顺序,
  `base` 根本不动,键逐位相同,memo **本来就 HIT**。
- **(C) 本轮落地**(见 §二)。⭐ **(C) 的价值不是省时间,是让归因不再可能写错**:
  #865 自己第一版把四次可复现的挂死归因成「新建远端 ref 的网络/代理超时」,
  **把一个可修的 harness 缺陷写成了不可行动的环境事实** —— 而当时第三腿**一行都不打**,
  于是日志里「挂住」和「跳过」长得一模一样。

---

## 四、顺手结清清单第 ③ 条(`py_gate_manifest.json` 全量重测)

上一轮交棒 ③:`test_carry_item_issue_state.py` **0.102s 却没有 manifest 行**(GH #839 同族)。
本轮新写的钉子同样需要进闸,**两件事共用一次重测**:

`python3 tools/agent/py_gate_measure.py` → `MEASURE_EXIT=0`,`measured_at 2026-09-16T01:15:53Z → 2026-09-17T04:08:45Z`

| 读数 | 前 | 后 |
|---|---|---|
| measured | 141 | **146** |
| in_gate | 124 | **130** |
| selected_total_seconds | 43.571 | **41.824** |

**新进闸 6 条**:`test_carry_item_issue_state.py`(0.092s,= 交棒 ③)、`test_push_order_contract.py`(0.017s,本轮)、
`test_citation_audit_forward_ref.py`、`test_dem21_census_anchor.py`、`test_lua_gate_budget_backstop.py`、`test_spot_az_spread.py`。
⭐ **`dropped: []` —— 一条都没被挤出去**,这一行是特意读的:重测**可以**因为计时漂移把测试踢出闸,
而那种损失不会有任何东西举手(闸只会变小,`selected_count` 照样健康)。本次 `41.824 < 43.571`,
是 6 条新成员**加上**旧成员集体变快。

---

## 四之二、⭐ 本轮的闸**拒了我自己一次**,而拒的理由是我新登的那条欠条

第一次推分支:`GATE_EXIT=0 CLEAN`(`luacheck bots game: 0 warnings`)之后
**`py gate: 130 ran, 1 findings, 41.9s` ⇒ `PUSH REFUSED`**,`PUSH_BRANCH_EXIT=1`。
红的是 `tests/test_pending_rulings.py`,逐字:

```
File "tools/agent/pending_rulings.py", line 1252, in _machine_key_status
    kind = cond.get("kind")
AttributeError: 'str' object has no attribute 'get'
```

**成因是我**:`done_when` 有 schema(`kind` ∈ `manual` / `json_value` / `path_exists` /
`path_absent` / `path_contains_all` / `text_absent`),而我把它写成了**散文字符串** ——
章程那句「带一个**裸读得出**的 `done_when`」我按人读的意思做了,而那一行是**机器**读的。
⇒ 已改成 `{"kind":"path_exists","path":"tests/test_prepush_scope_from_stdin.py"}`,
散文移进 `done_when_note`(与 `roshan_pit_daynight_fix` 同形)。

⭐⭐ **而真正值得记的是第二件事:一条写坏的行会把整条 py 闸打崩,且错误信息指着别人的文件。**
`AttributeError` 不是 `UNCERTIFIABLE` —— 它**没有点名那一行**,`FAIL` 行打的是
`tests/test_pending_rulings.py`,**一个与作者无关的名字**。任何一个组只要登一条形状不对的欠条,
**全队每一次 push 都会被这个 traceback 拒掉**,而看到它的人第一反应是去查那个测试。
📌 这正是本仓反复记的那个形状(GH #624 / #616):**红由下一个开工的组发现,而线索指向错的地方**;
本次唯一的区别是**作者当场就被拒了**,所以只花了五分钟。

⇒ **同轮加固**(`pending_rulings.py:_machine_key_status`):不是 dict 的 `done_when`
判 **`UNCERTIFIABLE` 并点名那一行的 `id`** —— 用的是这个工具**自己已有**的词表
(「could not read」本来就归 UNCERTIFIABLE),⛔ 不新增语义、不改任何判据。

**变异台(⛔ 第一次做错了,照登)**:第一次的「控制组」是 `cp` 出来的备份 ——
而我是**在改完之后**才 `cp` 的 ⇒ **控制组里带着补丁**,于是它「没崩」,
而我差点把这读成「本来就不崩」。改用 `git show HEAD:` 取真·修前版本后:

| 版本 | 同一行散文 `done_when` |
|---|---|
| **PRE-FIX(控制组)** | **`CRASHED: AttributeError: 'str' object has no attribute 'get'`** |
| **POST-FIX** | `UNCERTIFIABLE \| row 'campgrade_wave_type_premise_reading' has a done_when of type str …` |

📌 **纪律 1 逐字**:变异台要从**文件副本**恢复 —— 而「副本取自哪一刻」是它没写、我今天买到的那半句。

**复跑**:`tests/test_pending_rulings.py` **997 checks / 0 failed**;
`python3 tools/agent/py_gate.py` ⇒ **`130 ran, 0 findings, 0 uncertifiable, 42.2s`**。

⚠️ **顺手看见但本轮不改**:`owed` 里有 **2 行**的 `done_when.kind` 是 **`path_contains_any`**,
而 `_machine_key_status` 的白名单里**没有这个 kind** ⇒ 它们每轮读 `UNCERTIFIABLE`
(不崩,只是**从来没被真正读过**)。⛔ 不在本轮顺手改:要么补实现、要么改那两行,
两者都是别人的裁定域。**进清单,见 §七 ⑭。**

## 五、⚠️ 本轮自己撞了 GH #856 一次,而且是**新的一对当事人**

开工自检还在跑时,我并发起了 `py_gate_measure.py`。后者会写那个 gitignored 的
arming 开关(`bots/Customize/soak_side.lua`),自检的普查腿正在读它 ⇒ 自检日志逐字:

```
bots/Customize/soak_side.lua already exists (S5 precondition), so "gate off" is NOT what this measures.
Some other case -- here, in one of the other test files that write this path, or in a concurrent
lua5.1 process -- armed a candidate and did not remove it.
```

⭐ **这是 #856 的第三份现场,而前两份的当事人都是 `git push` 的腿**
(#856 原始现场:两条并发 push;RULING 68 §三:自检 ↔ push gate)。
⇒ **写方不是「push 的某条腿」,是「任何写那个开关的东西」** —— 本次是 `py_gate_measure.py`,
它既不是 push 也不是自检。📌 这一份把 #856 修法第 3 条(**读方**统一走 `read_lua()`)
的分量又加重一次:**枚举写方是永远枚举不完的,读方只有一处。**
⛔ **归责在我**:是我并发起的第二个进程,不是工具的缺陷。

**代价是可量的,而且我把它买回来了**:本轮自检 `trunk-red(lua)` 报
**`7 of 131 Lua detector file(s) failing ON THE WORKING TREE`**,七个文件的失败正文
**全部**是上面那句「开关已存在」。⇒ **树静下来之后逐个复读,7/7 全绿**:

```
test_pushtier_min_tie exit=0   test_roshdps_team_sum exit=0   test_runecamp_contest_source exit=0
test_salvepool_missing_floor exit=0   test_salveyield_arbitration exit=0
test_siegecap_ancient_roster exit=0   test_soakside_shared_switch exit=0
```

⭐ **逐个复读而不是「大概是并发吧」**:这七条红如果照抄进报告,就是**七条凭空造出来的 trunk 红**,
而下一个开工的组会去修它们。⛔ **反过来也不许**:没有这七行读数,「是我污染的」同样只是一句辩解。
⚠️ **另查一件不会自己举手的事**:那个开关是 gitignored 的,**泄漏了 `git status` 也看不见**,
而一个残留的 armed 开关会让下一轮约 20 条棘轮变红。**实测 `ls` = `No such file or directory` ⇒ 未泄漏**
(写方的 `finally` 收干净了)。
⇒ **`trunk(python)` 那一侧本轮判 `UNCERTIFIABLE`,不作任何声称**;
Lua 那一侧的七条红**已具名撤回**,其余 124 条在自检当次即为绿。

---

## 六、巡检(§2e 甲–丁;(戊) 先 deepen)

取数时刻 **2026-09-17T03:58:27Z**,`git fetch` 之后、按名字序、从 `origin/main` 取、只认报告名形状。
(戊):`.git/shallow` 存在 ⇒ `git fetch --deepen=200` 后 `rev-list --count origin/main` = **250**。

| 组 | 最新报告 | 距取数时刻 |
|---|---|---|
| batch-desk | `20260917T031535Z.md` | 0.7h |
| replay-check | `20260917T004719Z.md` | 3.2h |
| strategy | `20260917T021500Z.md` | 1.7h |
| hero | `20260917T015334Z.md` | 2.1h |
| director | `20260917T010000Z.md` | 3.0h(本轮收口) |

⇒ **不点名任何组**;无 GAP,无需走 §e-bis 的 `unlanded` 交叉读。

**清单第 1.5 步(RULING 64/67)**:`carry_item_issue_state.py` `RC_EXIT=0`,
`1 entry (of 88), 1 carry segment(s), 6 GH ref(s)`,`OK GH #839/#240/#843/#859/#810/#528`
—— **每一个被抄写的号都是 open**,无 `STALE-CARRY`、无 `NO-HANDOFF`。
语料 `2026-09-16T19:14:00Z`(8.7h,在 48h 之内)。

**成本(读批测台 09-17T03:15Z 报告,本轮未自跑 AWS)**:MTD **`$91.448`**,
**已在 `$90` 刹车线之上**(headroom `-$1.448`),`forecast 102.657` vs budget limit `100.0`。
批测台连续第三轮确认**什么都发不了**,刹车生效中。⛔ 本轮不发 owner 邮件:
W37 配额已用完(台账),而燃烧率这条已是 `DECISIONS_NEEDED` 第 18/19 条的存量,
**本轮没有新信息**——账面唯一的变化是快照解冻,方向与结论都不变。

---

## 七、下次触发

① **GH #865 (A)**(欠条 `gh865_prepush_scope_from_stdin`,`done_when` 三条;本轮唯一新欠条)
② `py_gate_manifest.json` 本轮已全量重测 ⇒ **交棒 ③ 结清**,⛔ 不要再抄
③ GH #856 修法第 3 条剩下的 9 个候选(本轮 §五 又加一份写方现场)
④ 棘轮加宽 `open_coded_walk_ratchet_blind_to_bare_string_root`(= GH #867)
⑤ GH #240 余下 ⑥ `carry_mark_prose_vs_list` ⑦ GH #843 (丙)+(乙) ⑧ GH #859
⑨ GH #810 待裁 1 + 两条欠条 ⑩ 自检那三条 python 用例(**第十四轮**)
⑪ `lua-coverage` `no_manifest_row` 存量(本轮自检又报 2 条新增:
`test_dusttower_dive_guard.lua` / `test_fieldsip_transfer_receiving_site.lua`)
⑫ P4.2 narrat 1 / `$0.90` 重裁 / GH #528 / patch 缺口 P3
⑭ `owed` 里两行 `done_when.kind: path_contains_any` 不在 `_machine_key_status` 白名单内(每轮读 UNCERTIFIABLE,从未被真读过)
⑬ **W38 周日汇总邮件**(09-20):带第 15/16/18/19 条 + 第 17 条**已撤回**的告知

---

---

## 十、【同轮收尾追加,push 之后】

### 铁律 6 三条腿 × 三次 push(⛔ 全程无 `RULE6_BYPASS`,无一行「SKIPPED, not passed」)

| # | push | 静态 | py 闸 | 快 Lua 闸 | 结果 |
|---|---|---|---|---|---|
| 0 | 分支(第一次) | `GATE_EXIT=0 CLEAN` | **`130 ran, 1 findings, 41.9s`** | 未到 | **`PUSH REFUSED`**(见 §四之二,红是我自己的欠条行) |
| 1 | 分支 | `GATE_EXIT=0 CLEAN` | `130 ran, 0 findings, 42.2s` | `scope = 9 path(s)` → `415 ran, 0 findings, 7 unanswered, 6 known-red, **578.3s**` | `* [new branch]` |
| 2 | `HEAD:main` | `GATE_EXIT=0 CLEAN` | `130 ran, 0 findings, 42.2s` | `scope = 9 path(s)` → `416 ran, 0 findings, 6 unanswered, 6 known-red, **570.5s**` | **`c51a8eae..44e75146`** |
| 3 | 分支(补推,`--force-with-lease`) | `GATE_EXIT=0 CLEAN` | `130 ran, 0 findings, 41.3s` | **`scope EMPTY`** → `416 ran, 0 findings, 6 unanswered, 6 known-red, **567.4s**` | `+ 51a6ab95...44e75146 (forced update)` |

⚠️ **`HEAD:main` 第一次被拒**(`remote contains work you do not have`,录像组 `c51a8eae` 落在中间)
⇒ `pull --rebase` `RC_EXIT=0` 后重推成功。⭐ **我在两推之前已经 `pull --rebase` 过了** ——
**前置 rebase 缩小那个窗口,但消不掉它**:10 分钟的 Lua 腿跑完,远端已经不是起跑时那个了。
⇒ **`.claude/rules/claude-code.md` 里那条前置 rebase 的措辞照留**(它买的是常见情形),
⛔ **但不许把它写成「就不会被拒了」**。
⚠️ rebase 换了树 ⇒ memo **MISS** ⇒ 第 2 推**重跑了一遍 570.5s**。
⭐ **这恰好证明 memo 的 HIT 有前提:两推之间什么都不动。** ⛔ 它**不是**反对 (D) 的论据 ——
反序让第二推**必定**空 scope,而这里是**别人落地**造成的、可观测的、偶发的。

### ⭐⭐ 第 3 推是 (C) 的验收现场,而且是空 scope 那一半

补推分支时 `origin/main` 已经等于那棵树 —— **正是 GH #865 §二 描述的那个形状**。
取数逐字:`origin/main == HEAD ? 44e75146… vs 44e75146…`、`scope preview: 0 path(s)`。
于是第三腿打出了 **(C) 新增的那一行**:

> `lua gate: scope EMPTY -> running the WHOLE manifest (fail-closed: a skip needs a positive answer, never the absence of one). This is the slow path, minutes not seconds. NOTE: if this push followed a successful 'git push origin HEAD:main', then origin/main already equals HEAD and this empty answer means 'main already has these commits', NOT 'no Lua was touched' …`

然后**真的跑了全集 567.4s,而那棵树 `main` 上已经有了**。
⭐ **(C) 之前,这一推在日志里是「一行都不打然后不返回」** —— 也就是 #865 把它归因成网络超时的那个形状。
**现在它自己说出了它在干什么、以及为什么。** ⛔ **(C) 没有省掉那 567.4s** ——
省它是 (A) 的事,本行仍欠;(C) 买的是**这 567.4s 不再伪装成挂死**。

### GH

- **#865 追评 `issuecomment-5708689837`**(RULING 69 全文)。发布前跑
  `claim_precheck.sh` ⇒ **`RC_EXIT=0` / `local commits not on origin/main: 0` /
  `resolved on trunk 3  refused 0` / `OK to publish`** —— **先 push 再发表**(铁律 6 / GH #290)。
- ⛔ **#865 不关**:(A) 仍欠。
- **本轮 MCP 可用,未触发铁律 11。**
- ⚠️ **一处手滑照登**:我第一次想追评时误用了 `issue_write`(update),**打出了一次只是重设标签的空更新**
  (`labels: ["harness"]`,与原值相同)⇒ 对 issue 内容零影响,但**它是一次多余的写**。正确的工具是 `add_issue_comment`。

### 欠条账

新增 **1**:`gh865_prepush_scope_from_stdin`(= #865 (A))。退休 0。**owed 82 → 83**,retired 26 不变。
⭐ 改动走**字节级拼接**(先按结构锚定 `owed` 列表的收口,⛔ 不是行号);
⚠️ **第一次锚错了**:`  }` + ` ],` 这个形状在 `owed` 与 `retired` 的结尾**逐字相同**,
于是断言通过、行却插进了 **`retired`** —— **一条新欠条被放进了「已完成」那一堆,而 JSON 照样合法**。
⇒ 复核 `len(owed)` 才发现(82→82,不是 83)。📌 **形状对不等于位置对**,与 §二 的 M1/M2 同型。

⚠️ **同族第二发,就在本轮收尾**:章程那段追加第一次也**锚错了**(锚串写的是我自己几分钟前改过的那一行的**旧样子**),
python 断言当场失败 —— **而同一条命令里的 `git add`/`commit` 照常跑了**(`&&` 链没跨过 heredoc)
⇒ **报告的追加进了提交,章程的没有**。📌 **失败的命令不等于失败的提交**,靠 `grep` 复核锚串才发现。

### ⭐⭐⭐ 收尾那一对 push 把这条裁定**正面验了一次**(不是推理,是读数)

最后一次补推是 markdown-only,且**严格按文档顺序**(分支先、main 后),两推之间**什么都没动**:

```
分支推:lua gate: scope = 2 path(s) changed vs origin/main
        lua gate: SKIPPED BY SCOPE -- this push touches no bots/game/tests path.
        44e75146..e7fd6dbd
main 推:RULE6_MEMO=REUSE  a green three-leg reading taken 2026-09-17T04:56:59Z on THIS EXACT tree.
        lua gate: SKIPPED BY SCOPE
        44e75146..e7fd6dbd  HEAD -> main
```

⭐ **`RULE6_MEMO=REUSE` 就是 RULING 69 的正面证据**:按文档顺序,分支推**不移动** `origin/main`
⇒ 第二推的键与第一推**逐位相同** ⇒ **HIT,第二推免费**。
📌 把它和 §十 第 3 推的 `scope EMPTY -> 567.4s` 并排看,就是这条裁定的全部内容:
**同一套机器,顺序对了免费,顺序反了付 567 秒并且看起来像挂死。**

## 九、token(铁律 8)

`TOKENS total_in=22,349,961 out=104,024 turns=119`

⚠️ 本轮偏高,归因明确且**大部分不是思考**:三次 push × 每次 ~10 分钟的快 Lua 腿
(`578.3 + 570.5 + 567.4 = 1716s`),等待期间的轮询与监视占了可观的 turns;
外加一次被拒的 push(§四之二)与一次 rebase 重跑。
⭐ **其中 567.4s 那一次是本可避免的**(补推分支撞空 scope),**而它换回了 (C) 的验收现场**,
本轮判为值得;⛔ **下轮不要把它当常规做法** —— (A) 落地后它会自己消失。
