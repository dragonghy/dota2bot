# director 2026-09-07T07:19Z(GH #450 落 gated fix + 一条新 trunk 红结清)

**一句话**:上轮交棒 ③ ——**已挂六轮**的 GH #450([bug],`J.GetCurrentRoshanLocation()`
昼夜映射反了,**无闸、25 个调用点、每一场真实对局都在读**)——本轮按 §FK.3 落成 gated soak
candidate `roshpit`(11 检查 + 变异台 8 CAUGHT/1 量程对照/0 ABORTED);顺带结清了 04:45Z
之后**新出现的**一条 python trunk 红(`zusultstrand_domain` 的两个常数未登记)。
⭐ **本轮最该被读的不是修复,是修复过程中买到的两件反向证据**:
(i) #450 建议的验收句**在装置层面买不到**(检测器不读 Lua),而它要的读数**已被现有测量在算术上买下**;
(ii) 三份 7.38 二手记述说**出厂映射是对的**,与 77/77 实测正相反 —— 两边都有说法,
**而 gate 正是唯一能让两边同时活着的形状**。零 AWS、零波次、不发 owner 邮件。

---

## 一、自检(铁律 10)与章程第 0 条

- ⚠️ **第一条命令又是管道**(`routine_selfcheck.sh … | tail -40`),被 §22 守卫当场拒:
  `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked`。
  **章程第 0 条逐字覆盖这一发,我没照做**(第三十八发)。**不新立措辞**(按 09-06 / 09-07 两轮的
  登记,改措辞已被证明不解决);本轮其余所有要读退出码的命令**全部**走 `rc.sh`。
- 开工自检真码 **`SELFCHECK_EXIT=3`**。逐腿照抄工具自己的行:
  - `unlanded work`:**OK, no unlanded work in the certifiable window**(886 refs 扫描,859 条
    因 shallow graft 判 uncertifiable 并如实打出来)。
  - `stable anchors`:**5 anchor(s) checked -- OK**。
  - `promote-time constraints`:`FROZEN none`,co-promote 原子 4 条全 ok。
  - `FINDINGS (exit 3)`:`cadence queue-rulings owed-executions trunk-red(python) trunk-red(lua)`。
  - ⚠️ **Lua 那条 trunk-red 是自检抓到了我自己写到一半的树**(`jmz_func.lua:10315: 'end' expected`)——
    自检与我的 Edit 并发。**这不是发现,是并发假象**,收尾复跑时已不复现;记在这里是因为
    「工具跑在一棵正在被改的树上」这件事本身会污染读数,下一轮谁看见这行不要去追它。

## 二、⭐ 本轮主体:GH #450 落 gated soak candidate `roshpit`

按 §FK.3 的裁定走 `.claude/skills/gated-fix/`,**不是把两个常量对调**(那正是 §FK.3 拒绝的形状,
也是 GH #352 归因给 23 个 commit 的形状:一次看着显然的非门控修复整体挪动了侧别平衡,没有任何东西举手)。

**代码**(`bots/FunLib/jmz_func.lua`):

- 新增**无闸** worker `J.RoshanPitForTimeOfDay( bArmed )` —— 两条映射在同一帧上都读得出来,
  于是检测器/后续裁定不必自己拥有一个开关文件才能对比两条腿;
- `J.GetCurrentRoshanLocation()` 收缩成**唯一闸址**:
  `J.RoshanPitForTimeOfDay( J.IsModeTurbo() and J.IsSoakCandidate('roshpit') )`;
- **25 个调用点一行未改**,未 armed 时**逐值等于出厂**。
  ⚠️ 出厂那一支刻意写成 `if/else` 而不是 `bDay and A or B` —— 后者在 `A` 为 nil 时与出厂分支
  **不同**;「off-candidate 就是出厂路径」要**因为理由成立,不是因为运气**。

**钉子**(`tests/test_roshan_pit_daynight.lua`,**11 tests / 0 failures**):两条腿各自在
**昼夜两侧**扫过(8+8 个时刻,含 0 / 300 两个边界)⇒ 一个「无条件答某一个坑」的修复过一半死一半;
turbo-only;错侧(switch 写 dire、bot 是 radiant);错 id(armed `roshdist` 不许打开本条);
真 fixture 帧上**两个相位都可达**(否则本文件是空的);闸只解析一次;worker 不读闸也不读游戏模式;
以及 `[call sites]`:**`bots/` 里没有任何文件单独点名一个坑常数**(今天唯一在 `jmz_func` 之外
点名的是 `mode_roshan_generic.lua`,它**两个都点**、是个析取 ⇒ 与映射无关)——
这条是 promote 的承重件:**若哪天有文件自己选坑,拆掉这道闸会修好 25 处而漏掉那一处。**

**牙齿**(`tools/agent/mutstand_roshan_pit_daynight.sh`):**8 CAUGHT / 1 SURVIVED / 0 ABORTED**,
`baseline after restore: exit 0 :: 11 tests, 0 failures`。SURVIVED 的那一发是 **M9 量程对照**
(纯注释改动)——它若也死,整台读数作废。⭐ 并且按 §EE.5 的读法,这台**打印每个变异体杀掉的
全部 case 名**(不是前三条),逐发核过**瞄准的那个 case 都在死者名单里**:
M1→`[flip]`、M2→`[gate]`+`[turbo-only]`、M3→`[off-candidate]`+`[gate]`+`[corpus]`、
M4→`[off-candidate]`、M5→`[premise]`、M6→`[coupling]`、M7→`[gate]`、M8→`[call sites]`(**独中**)。
第一版这台只打前三行,M5 的读数当场就长得像「被邻居杀的」——**截断是怎么把邻居的战功记到瞄准者头上的**,
现场就在本轮。

**连带**(`tests/test_gated_helper_nesting_census.lua`):`J.GetCurrentRoshanLocation` 一旦有闸,
census 立刻报三条新的 gate-inside-a-gate 并**问对了那个问题**(未 armed 时内层是不是该合取的幺元)。
按该文件的分类逐条读并登记为 **(P)**:未 armed 时它返回**出厂常量**,从不返回 nil、从不掐死分支
⇒ 外层 id 单独 armed 仍然量的是外层 id。**并回答了 GH #576 追加的那半问**:`roshpit` **不是**
只有外层 armed 才够得着 —— 25 个调用点里大多数(英雄文件、`aba_push`、`aba_defend`、`global_cache`)
**根本没有闸**,所以单臂 `roshpit` 波有自己的域。

## 三、⛔ #450 建议的验收句买不到,而它要的读数已经在手上

原文:「把两个分支对调后重跑 `roshdist_domain.py`,§A 应读作 `agree 77 / DISAGREE 0`」。

**`roshdist_domain.py` 不读 Lua。** 它的 `current_pit()` 是工具自己硬编码的公式
(`:128` 的 `time_of_day` + 文件头的 `RADIANT_PIT`/`DIRE_PIT`)。改完 Lua 重跑,§A **一个数都不会变**。

⚠️ **但翻转后的读数已经被现有测量在算术上买下了**:观测二值且 `unresolved == 0` ⇒
`agree_flipped ≡ DISAGREE_shipped = 77`、`DISAGREE_flipped ≡ agree_shipped = 0`。**恒等式,不需要重跑。**

⛔ **而那仍然不是条件 (a)。** §A 量的是**公式指对了坑**,不是 **bot 的行为变了** ——
「适用于此」与「在此生效」必须留在 fixture 边界两侧(GH #138 LIMITS 同一条)。
⇒ 剩余义务**收窄成一句**并写进 `owed_executions.json:roshan_pit_daynight_fix`:
一波 armed `roshpit` 的**行为读数**(BOT_MODE_ROSHAN 的 bot 是不是走向 roshan 真在的那个坑),
按铁律 4(i-a) 给 ab / ba 两层。**owner P4.2 冻结期内 `roshpit` 不入集**(FROZEN-HOLD),挂在解冻之后。

## 四、⚠️ #450 结尾那条提醒:买到了,答案指向反面

原话:「本 issue 只证明了当前实现与实测相反,没有独立证明昼夜就是引擎的全部规则」。

- **三份 7.38 二手记述**都说 Roshan「**top at night, bottom at day**」(top = `RadiantRoshanLoc`,
  y > 0),外加「开局固定在 bottom pit,**15:00 起**才随昼夜换坑」——**那正是出厂映射**。
- **一份 7.33 记述**说反面(day = Radiant 侧)——**那才是 armed 腿**。
- 两份互相矛盾 ⇒ 至多一份描述 7.41,而 `AGENTS.md` 本来就拒绝拿 patch note 摘要当证据。
- 唯一能救出厂映射的**技术**解释也排除了:dumper 的 `t` 与 `DotaTime()` **同源** ——
  `behavioral/dumper/main.go:500-506` 抓 `m_pGameRules.m_flGameStartTime`(号角),`:735-742` 把它
  从引擎时间里减掉 ⇒ `t == DotaTime()` 按构造成立,「半周期相位偏移」不存在。

⇒ **实测压二手**,而这恰恰是它落成 gated candidate 的理由:**两边都有说法时,gate 是唯一能让
两边同时活着的形状。**

**未买到的那一半照实登记**:7.38 记述里「**15:00 之前恒在 bottom pit**」这条规则,**两条腿都没有建模**。
若为真则昼夜之外还有一个开关;而 77 次死亡里 47 次白天观测全在 Radiant(top)坑,与之不相容,
**除非那 47 次全部发生在 15:00 之后**。**本轮没有逐次核过死亡时刻,所以这句话是登记不是结论。**

## 五、⭐ 同轮登记的第二根杠杆(刻意不并进 `roshpit`)

`J.CheckTimeOfDay()`(`jmz_func.lua:11114`)**从不问引擎**:它算 `DotaTime() % 600 < 300` 并把
结果叫「day」。而引擎自己的 `GetTimeOfDay()`(`docs/BOT_API_REFERENCE.md:346`)在 `bots/` 里
**零调用点**。**若那个模运算相位错了,两条腿的坑都是错的,该修的根本不是映射。**

按 lanefix 教训**不并进本 id**。`tests/test_roshan_pit_daynight.lua` 的 `[coupling]` case 钉住
「两条腿共用同一个相位读数」,好让这根杠杆哪天落地时**必须自己举手**(变异 M6 打得中)。

## 六、⛔ trunk 红(python):`test_detector_source_constants.py` —— 04:45Z 之后新出现的

`2 FAILED: zusultstrand_domain:HP_GATE is classified (UNREGISTERED),
zusultstrand_domain:ULTCASH_HP_GATE is classified (UNREGISTERED)`。

**不是我打的**:本轮工作树只动了 4 个文件,没有一个是 python;`zusultstrand_domain.py` 由
**录像组 04:20Z(`6eaebe38`)**随 hero-37 域普查落地,而上一轮 04:45Z 的 python 腿读作
`109 passed / 0 failed`。**这是同一个形状的第四发**(先例:`illumove_pairs` / `wandbleed_trigger`
两条,director 2026-09-03 那批三行),所以修法照抄先例:**分类,并且钉住**。

两个常数都在自己的注释里点了源码子句 ⇒ 都是 **MIRROR 主张,而只写在散文里的 mirror 主张是注释**:

| 常数 | 源码子句 |
|---|---|
| `HP_GATE = 0.28` | `hero_zuus.lua` `X.ConsiderR` 的 `nHealthPercentage <= 0.28`(全文件唯一) |
| `ULTCASH_HP_GATE = 0.45` | `jmz_func.lua` `J.IsDyingUnderAttack` 的 `J.GetHP( bot ) > 0.45` |

登记 + 钉住(`literal()` 对源码取值,两侧相减),外加一条**次序断言**
(`HP_GATE < ULTCASH_HP_GATE`:反过来的话该工具的「与 ultcash 共享」那一桶就不再是子集)。
**牙齿实测(不是我自己说的)**:把 `HP_GATE` 改成 `0.29` ⇒ 当场红且**只红那一条**
(`got 0.29, want 0.28`),还原后 `sha256sum -c` **OK**、复跑 `RC_EXIT=0`。
⇒ 我这份改完后 `python3 tests/test_detector_source_constants.py`
**`all detector source constants match their shipped sites`,`RC_EXIT=0`**。

### ⛔ 6.1 更正:这一格**不是我修好的**,而这次撞车值得读

push `HEAD:main` 被拒 ⇒ `git pull --rebase` ⇒ **`tests/test_detector_source_constants.py` 冲突**:
**录像组(落地那个检测器的人)在同一段时间里把同一条红修掉了,而且修得更好**。
两份并排读完之后我**整份取了它的**(`git checkout --ours`,我这一侧的改动**全部丢弃**):

- 它把 `0.28` 锚在 **`X.zuus_ShouldCashUltBeforeDeath( bot )` 这个调用**上(并单独断言该调用**全文件唯一**),
  我锚的是 `X.ConsiderR` + `literal()` 的**首个匹配** —— 而它自己写着「`nHealthPercentage` 在同一个
  函数的好几个分支里都被比较,**『首个匹配』正是锚点存在要防的那种读法**」。**它是对的,我的锚更弱。**
- 它一并钉了**四个**常数(多出 `ULTCASH_RADIUS` / `CHASE_RADIUS` 两个半径),我只钉了两个。
- 次序断言两份都有,措辞不同,内容一致。
- 归因它也比我准:它写「**三发、跨两个组**」并点名 08-29 与本次是它自己的、09-03 那批三行是随
  strategy commit 落的;我按报告时间线数成了「第四发」。**以它的为准。**

⭐ **这次撞车的形状,和 `outlatch_check1b_reason`(§EV / GH #523)是同一族的第二发,方向相反**:
那一次是「一根棒**三小时前就被别人做完了**,而登记它的人读的是一棵已经不存在的树」;
这一次是**两个会话在同一个小时里做同一件活,谁都没有办法知道**。差别在于代价:
那次的代价是一次多余的登记,**这次的代价是我这一侧的整块工作**(含一次变异实测)。
**GH #518 的认领字段对这两发都是瞎的** —— 认领挡的是「我要开始做」,而这条红**不属于任何一行
owed/queue 记录**,它只是自检打出来的一行。⇒ **登记(未做)**:trunk 红没有认领面。
一个会话看见 `trunk-red(python)` 就动手,是**正确的**默认(红要有人修),而**并发是它的常态失效**,
不是意外。最便宜的处方不是加锁,是**让红在落地那一刻就出现在落地者面前**(见下)。

⚠️ **交棒(不是本轮的活,而且现在有两份独立证据支持它)**:「**新检测器的模块级常数由落地者登记**」
这条约定**在落地那一刻没有任何东西举手** —— 它只在下一个人跑全量 python 套件时才红,
而那个人**通常不是落地者**(本次:落地者 04:2xZ,红被两个**别的**会话在 07:xx 各自撞见)。
**建议**(登记,未做):把这条检查搬进 `routine_selfcheck.sh` 的某条快腿,或让
`run_py_tests.sh` 对新增的 `behavioral/*.py` 单独报一行。

## 七、铁律 6 的门

- 静态半:`bash tools/agent/luacheck_gate.sh` ⇒ **`GATE_EXIT=0 CLEAN`**,
  `luacheck bots game: 0 warnings`(容器缺 luacheck,gate 自己装的)。**未用 `RULE6_BYPASS`。**
- push 门已上膛:`core.hooksPath=.githooks`(自检第一腿打的)。
- 动态半(逐条裸读,全部经 `rc.sh`):
  - `test_roshan_pit_daynight.lua` **11 tests / 0 failures**;
  - `test_gated_helper_nesting_census.lua` **10 / 0**(登记前 1 failure);
  - `test_roshdist_pit_truth_operand.lua` **11 / 0**、`test_gate_claim_consistency.lua` **16 / 0**、
    `test_soakside_shared_switch.lua` **16 / 0**、`test_smoke_load.lua` **3 / 0**;
  - `bash tests/run_py_tests.sh` ⇒ **109 passed, 0 failed, 1 uncertifiable**(修前 109/1/1)。
- ⚠️ **本轮抓到的一个「没跑成穿着通过」**:`lua5.1 tests/run_tests.lua A.lua B.lua C.lua D.lua`
  **只跑第一个**,却照样打 `0 failures` 并退 0。我第一次就是这么跑四个邻居文件的,
  读数 `11 tests`——**恰好等于第一个文件自己的数**。逐个重跑才拿到真读数(上面四行)。
  与 `rc.sh` 头部记的「模块当脚本跑,exit 0 什么也没断言」**同族**:多出来的参数被静默丢弃,
  而"通过"的形状一模一样。**登记,未做**:让 runner 对无法解释的多余参数报错而不是丢弃。
- **收尾复跑整份自检(经 `rc.sh`,不是管道)⇒ `SELFCHECK2_EXIT=3`,两条 trunk 红都不再出现:**
  - python 腿:**`110 passed, 0 failed, 1 uncertifiable`**(开工时 `109 passed, 1 failed`);
  - Lua 检测器腿:**`84 tagged detector file(s), 0 failures`**(开工那次是被我写到一半的树污染的);
  - `FINDINGS (exit 3)`:**`cadence queue-rulings owed-executions`** —— **两条 `trunk-red(*)`
    都已从 findings 里消失**。
- ⚠️ **措辞更正,而这条区别正是本仓库的纪律**:上一句对 Lua 腿成立,**对 python 腿不成立** ——
  工具把它记在 **`UNCERTIFIABLE (exit 2): trunk-red(python)`** 上,因为
  `tests/test_selfcheck_lua_leg.py` **没跑成**(它自己的 9 条检查在 120s 预算内没跑完;
  工具明说「wall clock 分不开『集合变大』与『容器变慢』」,GH #358 / #548,与本轮无关)。
  **「没跑成不是通过」** ⇒ 准确说法是:**红消失了,而 python 腿这一轮没有人能替它签绿。**
- **全量 Lua 套件(~100min)没跑,不声称它绿。**

## 八、没做的事(交棒,写清楚为什么)

- ⛔ **判定完结 0**,连续第三轮低于 owner P4.2 的 ≥2。**不粉饰**:本轮整轮给了
  上轮交棒 ③(GH #450,**已挂六轮的 live [bug]**)。它满足 owner **P4.4**(工作单元主体
  = 一个 `bots/` 行为改动),但**不满足 P4.2 的产出指标**。⇒ **下一轮的第一件事必须是判定,不是修 bug。**
- `owed_executions` 里那 **3 行已标 DONE 等退休**(`hero_domain_scan_2_30_31` /
  `a_evidence_tpdying` / `a_evidence_tpreach`)——**连续第二轮未做**。
- `slotpush` 判定:**连续第三轮被挤掉**。
- **`roshan_pit_daynight_fix` 这一行本轮不退休,虽然它的机器键从此读 DONE。**
  已在行内写明为什么、剩什么、以及**不许据键退休**——`path_exists` 只买「钉子落了地」。
- P4.3 后半(`test_set.md` 头部瘦身):从没开工;本轮 **`test_set.md` 一字未动** ⇒ 100KB 无回弹
  (本轮的裁定全文落在 `owed_executions.json` 的行内与 GH #450 追评里)。
- `cadence` GAP(strategy,两处)与 7 条 `SKIPPED-IN-STREAM` 仍在,本轮未处理。

## 九、成本

**本轮零 AWS 调用、零波次、零增量。**未跑 `session_setup.sh`(不需要)。
抄批测台最新读数供存档:**MTD `$62.042`**(来源 `budgets`,免费),`$50` 档已跨、
`$80`/`$100` 未跨,刹车线 `$90` 未触发,owner 批准线 `$100` 未触发。**本台不花钱,亦不复核该读数。**

## 十、Owner 沟通

**本轮不发邮件**(本周配额已用)。**无新增待决项写进 `DECISIONS_NEEDED.md`** ——
§四的二手/实测冲突**不是 owner 的分叉**:它已经被 gate 这个形状消化掉了(两边同时活着,
由后续行为读数裁决),没有需要拍板的东西。

## 十一、token 用量(铁律 8)

```
TOKENS total_in=22,311,508 out=80,531 turns=117
```
(统计时刻为收尾前;其后的 commit/push/追评回合不计入。)
