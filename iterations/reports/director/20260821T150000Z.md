# 总监报告 2026-08-21T15:00Z(第三十二次触发)

**一句话**:优先级 2(a) 本轮有两条归总监的 `[harness]`(#90 / #89),都做完了;
主产出是 **#90 的修法不是修那个数,而是让这一类数不再能被手写** ——
识别性阈值今后**从出厂 Lua 里运行时读出来**,并进一张会拦人的登记表。
外加三条只有总监能下的裁定(#88 收编第四种处置 / #86 登记口径 / GH #30 的 σ 争议)。

**未花 AWS 钱、未做任何 AWS 调用、未启动计费资源。零 `bots/` Lua 改动**
(改动全在 `tools/batch_test/behavioral/` + `tests/` + `iterations/`)。

---

## 1. `[harness] #90` 已修并关闭 —— 而且没按 issue 建议的方式修

录像组 14:36Z 查出 `capmono_refusal.py` 的承重子句引了一个**代码里不存在的数**:
docstring 说「>850u 保证 `lanesurv` 的爆发撤退不会自己开火」,而
`J.ShouldRetreatLaneBurst`(`jmz_func.lua:4892`)扫的是 **1100**,字面量 `850` 在
`jmz_func.lua` 里**零出现** ⇒ **登记域 793 帧里 423 帧(53.3%)坐在它承诺要排除的那条规则里面**。
钉帧:`armA_160904/20260820_162351_slot1` t=124.5 jakiro,最近敌 852u(比旧底线高 2 个单位),
**那一帧 lanesurv 看见两个敌人**。

**issue 的建议是「逐字抄那条规则的源码常量」。我判它是严格更弱的做法** ——
抄来的数在写下的那天正确,在别人改动那条规则的那天失效,而**全树没有一处会红**;
这正是本 issue 自己的成因,照抄等于把同一个陷阱重新上膛。改成**运行时读**:

| 交付 | 内容 |
|---|---|
| 新 `tools/batch_test/behavioral/source_constants.py` | `call_arg()` / `literal()` 按「函数体 + 调用点」从 Lua 取值。**无 `default=`、无静默回退,零命中与多命中一律 raise** |
| `capmono_refusal.py` | `ENE_LO` = 读出来的 `LANESURV_REACH`;`--legacy-domain` 保留 850,归档读数**逐位可复现** |
| `filter_outcome_coupling.py` | **故意保留 850**(它审计的就是旧域,`--selfcheck` 的 193/219/178/203 只对旧域有意义),但它自己硬写的 `1100` 也改成读出来的 |
| 新 `tests/test_detector_source_constants.py` | **28 例三节**,第 2 节是登记表 |

**为什么「兜底取值」被明令禁止**:一个会返回默认值的取值器**正好复现它要杀死的失败模式**
——一个看着合理的数,加上没有红。同理**歧义故意报错**:`J.ShouldInitiateLaneKill` 里有
**两个** `J.GetNearbyHeroes`(队友 1000 / 敌人 800),「取第一个」会**静默拿到队友半径**;
`where=` 强制调用方说清要哪一个,变异 M4 实测正是返回 1000。

**登记表把手工核对变成机器核对**:录像组 14:36Z **手工**核过 `lanekill_commit.py` 的四个
字面量(800/900/700/400),结论是「缺陷不外溢」。那个结论是对的,但**手工核对活不过下一次编辑**。
四个字面量 + 两条 depth leash 现在全部登记,Lua 一动这个文件报红并点名站点。

**判读后果(#72 判决不变)**:可归因带 `[1100,1500]` 重算 **DiD −11.90pp / SE 18.5 / |t| 0.64**,
n 减半噪声翻倍,仍与零不可区分 ⇒ **`capmono` NOT-PROMOTE 不变**。
变的是**可引用范围**,两句话已写进 docstring 与 #72:可以说「在 lanesurv 结构上够不着的带里
没测出 capmono 的拒扑签名」,**不能**说「capmono 对生存没用/无害」——
**166 个独立死亡里 153 个(92%)落在域外**,这个检测器从来没往那里看过。

### 1.1 我自己的 M5 是 MISSED,照 §AD.3 留疤

**五次变异**,每次先 `assert 源码真的变了`、回滚后复跑确认全绿:

| 变异 | 结果 |
|---|---|
| M1 出厂 Lua 1100→1150 | 第 1 节红,**点名站点**(派生值跟着动是对的,**钉住期望值的是第 1 节**) |
| M2 `ENE_LO` 抄回 850 | 第 2 节红 2 条 |
| M3 `lanekill DEPTH_SUP` 400→500 | 登记表红 |
| M4 取值器改成「取第一个」 | fail-loud 契约红,**返回 1000 = 队友半径** |
| M5 删掉注释剥离 | **第一版全绿 = MISSED** |

M5 第一版**什么都没抓到**:今天的真实注释恰好不匹配今天的正则,我那条
「decoy 在注释里」的断言**是空的**——**一个变异跑绿的测试,在确认变异真的改变了被测量之前,
不是通过的测试,是没测过的主张**。改成在**合成 Lua 体**上钉(注释里放
`J.GetNearbyHeroes( bot, 9999, ... )` 和 `-- was <= 999`),M5 现在报 `found 2 (9999, 500)`。
理由写在测试文件里,不是默默改掉。这与协同组同轮的假变异 M8(重复键后写的赢)、
我 13:00Z 的 M4/M5 是**同一条规矩的第三、四次触发**。

## 2. `[harness] #89` 裁定:不教 loader 认 mode(驳回路 1),已关闭

协同组第十三条世界断言:`GetActiveMode()` 是 bot VM 自己的状态,不在 `.dem` 里 ⇒
**872/872 英雄帧全答 0**,八条 J 谓词恒 FALSE,210 个已发布比较行在 fixture 上无一为真。

**驳回**,三条独立理由:(i) mode **不能恢复只能建模**「当时 bot 在想什么」,正是 fixture
纪律要禁止的东西,**比 #81 更糟**(那是口径选择,这是发明数据);(ii) 一次性移动
30 个团队读数 + 71 个 `IsInTeamFight` 读数,**没有任何地面真值**能判它移对了;
(iii) 世界断言本身已是资产,**会拦人的缺口比猜出来的答案值钱**。
**语料请求撤回批准** —— 那一帧不在任何 `.dem` 里,别让录像组去归档里找。

**但 §2 那条不对称我单独钉住,它比路 1 重要**:脚本侧比较恒 FALSE(谓词永不触发),
引擎侧的 mode 参数被 loader **收下然后扔掉** ⇒ 那一族**触发得太多**(71/872 帧
`IsInTeamFight` 读 TRUE)。**一个缺失的数,两个方向相反的错。**
**本轮不改默认行为**(改成返回空表会一次性翻转 71 帧,消费方未普查,是 #81/#82 同款的
「一改就动一片」);归口协同组,**下一轮前置 = 先普查哪些现存断言消费了 mode-过滤过的返回值**。
在那之前,**fixture 上任何用 `IsInTeamFight` 的肯定式断言按上界读**。

`mode_farm_generic:285` 的 (乙) 从 backlog 0i **划掉**(结构上做不了,不是排期问题),
**批准协同组自选的 `:535`**。§4 那条(`GetCenterOfUnits({})` 返回原点、35 个消费方只测
`~= nil`、1500/1400 两个半径不一致)接收为**候选杠杆 + 前置条件**,本 harness 判不了。

## 3. `[hero] #88` 结案 + 收编第四种处置 `CARRIER-UNAVAILABLE`

英雄组按我「先量域再改」的指示量完了,**答案是这个问题问不出来**:Tiny 不在
`hero_pool.txt` 的 41 行池子里,112 个种子 / 11,048 局的阵容并集**恰好是那 41 个**
(前瞻与地面真值两条独立证据逐个对上)。**同意删掉恒真子句、不做那个「显然的修复」**:
修成 `J.GetHP(bot) > 0.15` 是**新增否决** ⇒ 要 (a) ⇒ (a) 买不到 ⇒ **armed ≡ shipped**,
`axeblink` 的坑且**开工前就知道**;留着也不行,恒真门在测试里长得跟正常通过一模一样。
M1/M2 那一对变异是这份交付的全部价值:**加回旧子句什么都不变,装上「正确」子句就翻转决策。**

**`CARRIER-UNAVAILABLE` 收为第四种处置,并按他们建议放在流程最前面**(与域为空 /
`DOMAIN-NOT-REACHED` / `DOMAIN-REACHED-BUT-VANISHING` / `HARNESS-BLIND` 并列)。
**任何 gate 提案的第一道闸:这个英雄在 `hero_pool.txt` 里吗?** 成本 0。
英雄组前三轮都是**掏了语料之后**才知道读数买不到,这一条在掏之前就能知道。
顺带收下他们的查法警告(TS 生成的 Lua 表按小写内部名 grep **静默返回 0**),**他们自己抓住了
一个差点写反的结论并如实记下,记为健康信号**。

## 4. `[hero] #86` 批准(issue 保持 open,英雄组落地)

登记口径改 **post-domain residual**(共因,Fisher p=0.0040,两组读数无分歧)。两条约束:
**旧登记行保留为历史**(同 §1 对 850 的处理:登记数字降级不删除,否则归档读数不可复现);
**必须同时钉 §6 那两帧**并把「现谓词分不开这两帧」钉成断言 ——
**改口径而不钉帧,等于把一个已知缺陷改个名字。**

**跨 issue 的观察**:#86(筛子与结局共因)与 #90(筛子引了不存在的阈值)是**同一件事的两面
—— 域定义本身从来没有被任何东西核对过**。#90 那半可以用登记表机械钉住,#86 那半是统计性质,
钉不住,所以它的对应机制就是钉帧。

## 5. GH #30 的 σ 争议:判读线不动(±30),功效算术改用 15.63

批测台 14:15Z 第一次把噪声底拆开:局内差逐局 sd 156.1,bootstrap 预测单种子 sd **15.63**
对上实测 16.63 / 19.62 ⇒ **镜像 draft 已把种子/draft 方差消干净**。
他们同时**证伪了自己两条长期担忧**(referee 抖动局内构造性抵消 corr +0.042;
42/30 不对称只值 1.9% 噪声)并撤回措辞 —— **主动更正记为健康信号**。

**裁定分两半**:(1) **promote/reject 判读线维持 ±30 不下调** —— 非对称代价
(假 promote 的价钱本项目实测过:`lanefix` 两次 −74.5 / −88.7;假 hold 只是延迟),
且 15.63 是单 run 内 bootstrap、按他们自己的诚实边界不是严格同分布对照,与 30.24 的
4 点 CI(0.57×–2.2×)**并不硬冲突**;(2) **排期/功效算术改用 15.63 与那张 MDE 表** ——
「要多少局才看得见」**查表回答,不许再开波次去试**。采纳三条推论:种子与局数统计上可互换
(4 种子是机器拓扑不是统计门槛)、唯一功效杠杆是买更多局或换低方差指标
(调 referee / 换边 / 选种**降级**)、**逐 id 经济二分继续判死**(第二种独立方法复算一致)。

## 6. 章程其他项

- **下一波仍不启动**:批测台三条件形式上满足但「无目的不启动」无一条解除,保守默认正确。
- **成本**:MTD **$18.474**(批测台 12:00:14Z 免费 Budgets 读),forecast $23.11,
  **0 台在跑**,四层泄漏全 0,$45 围栏余 $26.53。**总监本轮零 AWS 调用。**
- **四组健康度**:全部有产出、无空转,报告落在 13:48 / 14:00 / 14:15 / 14:36,窗口 ~48 分钟。
  本轮**三组各自主动更正了自己写过的东西**(批测台两条担忧、英雄组的 grep 反结论、
  我自己的 M5),这是本轮最该记的健康信号。
- **本轮无新增 `[bug]`;`[harness]` 两条已清空。** 待复核队列
  `#44`/`#45`/`#55`/`#58`/`#62`/`#63`/`#65`/`#66`/`#67`,**`#45` 第十九次顺延**;
  `wandlimbo` 机会普查连续第**十九**轮无人做。
- 今日周五,**效率台账(仅周日)跳过,08-23 那次要做**;patch 已分片不重复。
- `DECISIONS_NEEDED.md` 本轮**不新增**(五个决定全在自主授权内),**本周 owner 邮件仍为零**,
  周日连同第 5 条(`HARNESS-BLIND` 一族的策略问题)一并发。

## 7. 验证

**零 `bots/` Lua 改动**(`git diff --stat` 只有 `.py`),按铁律 6 的立法目的,
Lua 门无适用对象,**本容器未装 lua5.1/luacheck,如实记录,未假装跑过**。
Python 侧:

```
$ bash tests/run_py_tests.sh
PASS  tests/test_canon_hero_join.py
PASS  tests/test_detect_overchase.py
PASS  tests/test_detector_source_constants.py
PASS  tests/test_hero_position.py
PASS  tests/test_storyboard_smoke.py

5 passed, 0 failed

$ python3 -m py_compile tools/batch_test/behavioral/*.py tests/*.py   # OK
$ python3 -c "import json;json.load(open('iterations/state.json'))"    # parses
```

`tests/test_detector_source_constants.py` 单跑 **28 例全 ok**;五次变异见 §1.1。

## 8. 下次触发

1. **`[strategy] #84` 的下一条杠杆改成 `mode_farm_generic:535`**(§AE.3 已批,`:285` 划掉)
   —— 交了就当场判;
2. **`[hero] #86` 落地核验**(登记口径 + §6 两帧是否一起钉了,只钉一半不收);
3. **协同组的 mode-过滤消费方普查**(§AE.3 的下一轮前置)交了就判;
4. 下一波申报目的仍按 §W.5 判;清待复核队列(**`#45` 已顺延十九次,下轮优先**);
5. 都在等就做 **backlog #1 帧语料检索工具** —— 本轮的 `source_constants.py` 是它的
   **第四块砖的另一半**:前三块(`fixture_level_census.py` / `[corpus]` / `[class]` 扫描)
   是「按谓词批量取帧」,这块是「**让谓词自己对着出厂代码保持真**」。下一步仍是把谓词参数化。

## 9. 本次会话 token 用量

```
TOKENS total_in=10,641,729 out=94,105 turns=86
```
(统计时刻之前的数字;之后的收尾回合不计入。)
