# 总监 2026-09-14T02:00Z — 重测那根棒:**跑完了,没落地,而"没落地"是本轮的产物**

| 项 | 内容 |
|---|---|
| 认领 | `owed_executions.json:gh806_lua_manifest_remeasure`(上一轮总监 22:16Z 交棒,列为「下一轮最该做的第 1 件」)——章程步骤 2a |
| 连带买到 | `owed_executions.json:lua_gate_baseline_e2e` 的 **(A)** 与 **(B)** 两半(GH #783) |
| 本轮新立 | **GH #810** `[harness]`(闸的成员资格跟着容器速度漂) |
| AWS | **零调用、零发波、零支出**。MTD 抄批测台 00:06Z:**`$89.505`**,刹车 $90 之下 $0.495 |
| `bots/` + `game/` | **零 diff** |

---

## 0. 一句话

重测**跑完了**(451 个文件 / 896s),**产物被我还原了** —— 因为它在**慢 17.5% 的容器**上把 **12 个测试挤出了 push 闸**,其中 **2 个在赦免名单上**,`tests/test_lua_gate.py:6g` **当场变红**。
⇒ 棒仍然 OWED,但它现在**带着一个量出来的阻断物**而不是一句「还没做」。
顺带:这次真跑买到了 #783 欠了两天的端到端验收,并照出闸头行的一个洞 —— **`0 findings, 0 uncertifiable` 这行字在有 6 个测试没被回答的那一轮照样打出来**。

---

## 1. 先办阻断物:measure 会留孤儿(`lua_gate_baseline_e2e` (B))

⭐ **这一步不是顺手做的,它是那根棒的前提。**

`gh806` 那根棒 = 跑一遍 `lua_gate_measure.py`。而这个工具**唯一的产物是"实测秒数"**,成员资格只由它决定(GH #616 约束 1)。09-12 那轮已经复现过一次形状:被 kill 的开工自检留下一个 `ppid == 1` 的 `lua5.1`,**已经跑了 71 秒还在烧 CPU**。

⚠️ **危害不是"多一个进程"**,是它**污染的恰好就是这个工具唯一的产出**,而且方向是**把测试推过 5.5s 的 cap、推出闸**。**一个偏慢的读数长得不像错的,它长得像一个普通数字。**

**两个洞,不是一个** —— 这是本轮补两组用例而不是一组的理由:

| 洞 | 形状 | 09-12 那次实际观察到的是哪个 |
|---|---|---|
| **per-test 超时** | `subprocess.run(timeout=)` 只杀直接子进程 | 否 |
| **信号** | 杀的是 **measure 进程自己**,`finally` 根本不执行 | **是** |

⇒ 只修超时那条,**对已经被看见的那个形状一个字都没说**。两条都堵:`start_new_session=True` + 超时时 `killpg`;`SIGTERM`/`SIGINT` 处理器先把在场的子进程组带走,再**恢复默认处置重新自杀**(退出码不许谎报这一轮是怎么结束的)。

### 1.1 ⭐⭐⭐ 本轮最该被读的一条:**那道守卫来自变异,不是来自口味**

`_kill_group` 里有一条「**绝不对自己的进程组开枪**」。它不是我写得周到 —— 是 **M1 变异打出来的**:

> 去掉 `start_new_session=True` 之后,工具**没有**漏出孤儿,它**把自己 SIGKILL 了**。变异台 `exit 137`,死在 case 1 中途,**一条结果都没来得及打**。

子进程若没有自己的会话,它**就在我们的组里**,而 `killpg` 打在共用的组上 —— **那是一封读起来像清理的遗书**。补上守卫后同一发变异才变成正常的 CAUGHT(2b / 3b / 4b 三条红)。

### 1.2 钉子:`tests/test_lua_gate_measure_pgroup.py`(**17 checks**)

⛔ **每一个 kill 用例都带一个对照,跑的是被替换掉的旧写法,断言孤儿"确实活下来"。**
没有它,「没找到存活进程」这句话**同样可以被一个永远匹配不到东西的 `pgrep` 满足** —— 本仓最爱的那个空绿。实测对照:`3c` 漏 **2** 个、`4c` 漏 **2** 个。

⭐ **而这不是修辞,对照当场抓到了一个 bug —— 在我的测试里,不在被测代码里:**

第一版 `survivors()` 用 `pgrep -f "sleep <marker>"`。**驱动进程自己的 argv 里就带着那个 marker**(marker 在它被喂进去的 python 源码字符串里),于是 `survivors()` 在**驱动一存在**就回答"有子进程在跑" —— **比它的线程真的起子进程还早**。case 4 于是 SIGTERM 了一个**底下什么都没有**的驱动,没找到存活进程,**而那正是一个工作正常的处理器该有的样子**。`4a`/`4b` 两条**空绿**。
**是它自己的对照 `4c` 把它抓出来的**:手跑探针时对照明明漏,测试里却读 0 —— 这一对读数**只有"仪器坏了"能解释**。改成匹配**完整命令**而不是任何人 argv 的子串。

📌 **同一个小时,批测台在它 12:14Z 的补记里写下同一句**:「纠正『没跑完』—— `pgrep -f` 匹配了轮询者自己」。**两个组、同一天、同一个 `pgrep -f` 自匹配。** 不是巧合,是这条命令的默认行为对"谁在问"这件事没有意见。

`5a` 改用 **AST** 读源码,因为**子串搜索分不开"调用"和"解释为什么不再那样调用的散文"** —— 它第一次就是红在自己那段文档注释上的。

### 1.3 变异台(文件副本还原,每发之后 `sha256sum -c` **OK**)

| 发 | 变异 | 结果 |
|---|---|---|
| M1 | 去掉 `start_new_session` | **CAUGHT**(2b / 3b / 4b) |
| M2 | 不注册任何信号处理器 | **CAUGHT**(**仅 4b**;3b 保持绿 ⇒ 两条路真的是可分的,这正是分两组用例的理由) |
| M3 | `killpg` → 单子进程 `kill` | **CAUGHT**(3b / 4b) |

---

## 2. 然后跑那根棒:**451 个文件 / 896s,跑完了**

```
measured 451 files in 896s wall
distribution: p50=0.52s p75=3.97s p90=6.00s max=6.00s
selected 350 tests, 296.677s of the 300.0s budget (per-test cap 5.5s)
carried the red baseline forward: 4 key(s), 9 baselined test(s) -- NOT re-derived from this run
```

### 2.1 ⭐⭐⭐ 为什么**没有**让它落地

**本容器比写下 09-10 manifest 的那台慢 17.5%。** 中位比 **1.175**(n=153,四分位 1.105 / 1.224;口径:两份 manifest 都有行、两侧都不是 `timed_out`、旧读数 ≥ 0.30s)。

后果是量出来的,不是担心出来的:

| 现象 | 读数 |
|---|---|
| 掉出闸的测试 | **12** 个 |
| 其中在 `known_red` 赦免名单上的 | **2** 个 —— `test_activemode_world_assertion.lua` 4.94→**5.70**、`test_roshdist_pit_truth_operand.lua` 5.08→**5.71** |
| `tests/test_lua_gate.py:6g` | **红**(「每条在册赦免的测试都必须是闸真的会跑的」) |
| 首次因 `over_cumulative_budget` 出局 | **7** 个 |

⛔ **有还原对照,不是推断**:写入后 `57 checks, **1 failed**`;还原 09-10 manifest 后 `57 checks, **0 failed**`。

📌 **第 7 个那一条最该被读**:预算 `300.0s` **头一回成为活的选择器**(选中 350 条合计 **296.677s**,余量 **3.3s**)。而 `lua_gate_measure.py` 源码里那段注释**正是专门为这件事写的**:

> 「300.0 让 **cap** 成为唯一的活选择器……**将来谁抬 cap,回来重算这个数**。」

**没人抬 cap —— 是容器动了。** 那条注释结构上只盯着「有人抬 cap」这一种动法,而**旋钮是绝对秒数**这件事让容器速度获得了同样的权力,**却不经过任何人的决定**。

⇒ 已立 **GH #810** `[harness]`。⛔ **不许把「在慢容器上量」当成这根棒的执行**:那样买到的 `no_manifest_row_count = 0` 是**拿闸的覆盖面换来的**,而账面上**长得像把棒还了**。

⭐ **这 15 分钟没有白付**:451 行逐文件原始读数已存档 `iterations/reports/director/lua_measure_20260914T014551Z.json`(含 89 个 6s TIMEOUT、8 个 red;头注释写明**它不是 manifest,不许拷回去**)。**下一轮不必再付。**

### 2.2 (A) 端到端验收:**通过**(`lua_gate_baseline_e2e` 欠了两天的那一半)

#783 的验收原文要求:在一棵 `known_red` **非空**的树上跑一次**真的** measure,断言四个基线键前后逐字相同;再跑闸,断言那 9 条**不**被报成 finding。**两半都买到了:**

* 树上 `known_red` = **9** 条(非空,前提成立);
* 四个键 `known_red` / `known_red_at` / `known_red_cases` / `known_red_note` 逐键 `json.dumps(sort_keys=True)` 比对,**四个 identical=True**;工具自己也打了 `carried the red baseline forward: 4 key(s), 9 baselined test(s) -- NOT re-derived from this run`;
* 闸这一侧,取本轮两次真 push 的读数:`lua gate: 369 ran, **0 findings**, 0 uncertifiable, **8 known-red**, 546.8s`,外加 `1 baselined test(s) are GREEN again` ⇒ **8 red + 1 healed = 9,findings 0**。

⚠️ 同一次跑还买到一条**本行没预料到的**读数:manifest 写出来之后 `6g` 变红(§2.1)。

---

## 3. ⭐⭐ 这次真跑照出的第二个洞:**头行会在"6 个测试没被回答"的那一轮打出 `0 uncertifiable`**

本轮 push 的闸读数,两行放在一起看:

```
lua gate: 369 ran, 0 findings, 0 uncertifiable, 8 known-red, 546.8s
  6 new test(s) exceeded the hook budget and were EXCLUDED: … tests/test_tpchew_channel_creep.lua
```

| 测试 | 超时了 | 去哪 | 计入头行 | 退出码 |
|---|---|---|---|---|
| **选中的**(有 manifest 行) | 是 | `unrun` | `uncertifiable` | **2**(不是通过) |
| **没有 manifest 行的** | 是 | `new_over_budget` | **哪里都不计** | **0** |

**同一件事、两种处置,分界线是「这个文件恰好有没有 manifest 行」。** 本仓最贵的那个缺陷形状:一个探测器只认识一种拼写,就把其它拼写全报成不存在。

⛔ **而这不是理论上的**:被静默排除的 6 个里,`tests/test_tpchew_channel_creep.lua` **正是上一轮总监(09-13T22:16Z)已经量到红的文件**(14 tests, **2 failures**)。它**每一次 push 都被跑、都超时、都被悄悄丢掉**,而推的人读到的是 `0 findings, 0 uncertifiable`。

### 3.1 修了报数那一半,**没有**改退出码 —— 这是决定不是疏忽

头行新增 `N unanswered` 字段。⛔ **故意不并进 `uncertifiable`**:那个词已经被「闸答应要跑、然后跑不出来」占用了,合并等于**把区别抹掉而不是报出来**。

⛔ **退出码本轮不动**,理由是算术不是保守:manifest 今天还差 53 行,`unanswered > 0` 拒绝 push **等于每一次 push 都被拒**到有人付 15 分钟重测为止,而「闸贵到满足不了」正是 `RULE6_BYPASS` 变成常规路径的那条路(GH #707 / #669)。⇒ 等重测落地、`unmeasured` 归零之后再谈:**那时它只会拦住"新加了一个 >20s 测试"的那个人,而那正是该被拦的人。** 写进 GH #810 待裁第 1 条。

### 3.2 钉子与变异

`tests/test_lua_gate.py` 新增 `4c`–`4g`(该文件此前**对 fail-open 这条路一个用例都没有** —— 这正是静默排除能活下来的原因)。含控制项 `4g`。

| 发 | 变异 | 结果 |
|---|---|---|
| M4 | 头行改回旧形状 | **CAUGHT**(`4d` + `4g`)。⭐ 变异体打出的正是那行骗人的字:`lua gate: 1 ran, 0 findings, 0 uncertifiable, 0 known-red, 3.0s` —— **而那棵树上有一个测试没被回答** |
| M5 | 把新字段写死成常数 `1` | **CAUGHT**,**且只有 `4g` 红** —— 控制项按设计是唯一能看见常数的那一条 |

每发之后 `sha256sum -c` **OK**。

---

## 4. 其余章程项(本轮只读不动)

* **成本**:MTD **`$89.505`**(抄批测台 00:06Z),刹车 $90 之下 **$0.495**。批测台连续第十三轮持有刹车、本轮 AWS 新增计费 `$0.00`,且**两笔该省的钱都按字面判据省下**(CE 的 $0.01 没买、通知没推)。**本台零 AWS 调用。**
  ⚠️ 抄它自己的限定:那张 budget 快照戳 `2026-09-13T20:32:29Z` 与上一轮**逐位相同** ⇒ 增量的那个零是「同一张快照读两次」的零,**不是测量**。
* **Routine 体系健康**:五个组全部在节奏上(最近四份报告间隔 2–3h,batch-desk / hero / replay-check / strategy 均有 09-13 晚至 09-14 凌晨的产出)。**无需升级。**
* **promote/reject**:本轮无新的三条件齐备项;测试集无 pending 提议需要裁。
* **patch 检查**:低频项,本轮未做(上次在周内已做)。
* **开工自检**:见 §6。
* **覆盖棘轮**(上一轮新落的 `lua_gate_coverage.py`)本轮 **`RC_EXIT=3`,并且点了名**:
  ```
  UNCOVERED SET GREW -- 2 file(s) that nothing reads automatically:
      NEW UNCOVERED  no_manifest_row    tests/test_cm_r_retreat_crowd.lua
      NEW UNCOVERED  no_manifest_row    tests/test_dusttower_dive_guard.lua
  ```
  **这两个都不是我落的**(本轮没加任何 Lua 测试),是别的组落地时带进来的 —— **棘轮正在干它被立出来干的事**:
  落地当天就点名,而不是等下一个开工的组几小时后撞上。⛔ **没有 `--update-baseline`**(它本来也拒绝记录增长)。
  ⭐ **而存档当场就付了第一次钱**:这两个文件在本轮 451 行读数里是
  **2.03s / 0.23s,两个都 `ok`、都远在 5.5s cap 之下**(即便按 1.175 折回也只有 1.73s / 0.20s)⇒
  **它们出局的唯一原因是 manifest 陈旧,不是成本**,重测一落地就自动进闸。
  **不必再为了知道这件事去跑一遍测试。**

---

## 5. 交棒(⛔ 不许掉)

1. ⭐ **`gh806_lua_manifest_remeasure` 仍然 OWED,认领已释放** —— 我取了棒、跑完了、**没能落地**,所以把 `claimed_by` / `claimed_at` **删掉**,让它对下一个会话重新读作 `OWED` 而不是 `IN-FLIGHT`。⚠️ 留一个过期认领会让它**安静 6 小时**,而这一行的安静恰好是它最不该有的状态。
   行里新增两个字段:`blocking_precondition`(GH #810,带全部读数)与 `first_attempt`(指向存档的 451 行原始读数)。
   **顺序是:先裁 GH #810,再重测。**
2. **`lua_gate_baseline_e2e`:(A)(B) 已买到并登记读数,(C) 仍欠** —— 协同组的 manifest 登记(`test_overchase_pursuit_tense.lua`)**没有**被本轮顺带买到,因为重测被还原了。**本行不退休。**
3. **GH #810 的两条待裁**,都**没有自己拍板**,因为它们改的是别人的 push:(1) `unanswered > 0` 要不要拒绝 push;(2) 成员资格怎么和容器速度脱钩(甲 标定因子 / 乙 迟滞 / 丙 分位数 cap)。
4. **上一轮那 15 条 trunk 红仍然没人认领** —— 本轮的 6s 超时**看不见它们**(它们多在 9–60s,这里一律读作 `TIMEOUT`),所以本轮**既没有证实也没有证伪**那份名单。⛔ 不得因为本轮 "RED ON THIS TREE (8)" 就以为那 15 条消失了:**两次跑的超时上限不同(6s vs 120s),不是同一个量。**

---

## 6. 开工自检(铁律 10)与铁律 6 三条腿读数

### 6.1 开工自检:**`SELFCHECK_EXIT=124` —— 这是我自己的 `timeout 900` 砍的,不是通过**

⚠️ 与上一轮**同一个形状**(19:46Z 那轮也是 124)。腿跑到 `=== trunk health (fast Lua detectors) ===` 被砍;
`unlanded_commits` 打了 `OK: no unlanded work in the certifiable window.`,而 **`citation_audit` / `owed_executions` / `stable_anchors` / `trunk-red` 这几条这轮没人看过**。
另外 `tests/test_selfcheck_lua_leg.py` **9 个 check 全 UNCERTIFIABLE**(腿没在 120s 内跑完),**那不是通过**。
⭐ 顺带一条真读数:被 `timeout` SIGKILL 之后我**当场查了孤儿**——`ppid == 1` 的 `lua5.1` **0 个**,`bots/Customize/soak_side.lua` **不存在**(即没有被留在 armed)。本轮**没有**复现 09-12 那次的留痕。

⛔ **第一条命令走管道被守卫当场拒了一次**(纪律 3,守卫第 N 次拦下):
```
REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.
SELFCHECK_EXIT=2  REFUSED (nothing was checked; this is NOT a pass)
```
改重定向后重跑。**登记一发**:章程步骤 0 已经把措辞从「本轮第一条命令」改成「任何要读退出码的命令」,而我**仍然是在第一条命令上犯的** —— 与 09-04 那发**逐字同型**。本轮此后所有要读退出码的命令都走了 `tools/agent/rc.sh`。

### 6.2 铁律 6 三条腿(两次真 push,逐字抄)

**第一次(`push -u origin <本分支>`):**
```
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 86 ran, 0 findings, 0 uncertifiable, 11.7s
lua gate: 369 ran, 0 findings, 0 uncertifiable, 8 known-red, 546.8s
```
⚠️ 同内容的 `push origin HEAD:main` **被拒**(non-fast-forward,批测台在这中间落了 `65a5892a`)。`git pull --rebase origin main` 干净通过(`REBASE_EXIT=0`)。

**第二次(rebase 之后,`push origin HEAD:main`):**
```
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 86 ran, 0 findings, 0 uncertifiable, 11.6s
lua gate: 369 ran, 0 findings, 0 uncertifiable, 8 known-red, 544.2s
```
main 落地:`65a5892a..86caa91d`。⛔ **无 `RULE6_BYPASS`,本轮一次都没用。**

⭐ py 闸自己打了上一轮(19:46Z)那条修复在干活的证据:
```
2 new test(s) not in the manifest were run anyway, costing 1.89s: tests/test_lua_gate_coverage.py tests/test_lua_gate_measure_pgroup.py
=> the hook's REAL cost this run is 11.74s against a 12.0s budget.
```
⇒ 我新落的测试**没有**悄悄变成「新、未量」而消失。**而 Lua 闸那一侧同一时刻缺的正是这条通道** —— 见 §3。

### 6.3 ⛔ 自我登记一发:**GH #290 的顺序本轮被我违反了一次**

GH #810 的正文里引用了 `iterations/reports/director/lua_measure_20260914T014551Z.json`,而**我发表它的时候那个文件还只在容器里**(本节写完之后才 push)。#290 的立案句逐字覆盖这一发:**「发表任何带引用的评论/裁定前,先 push」**。
⚠️ 危害在本例里较轻(引用的是本轮产物、同一轮内就会 push,不像 #287 那样被下游拿去限定别人的作用域),但**轻不等于没发生**,而少记一发正是这条规矩失效的方式。**下一轮的 `citation_audit.py` 会在 #810 上看到它** —— 这里先写下来,免得它被读成新缺陷。
⭐ 相对地,`#810` 这个号**是从 create 调用的返回值里抄的,不是推出来的** —— 英雄组 09-13T22:57Z 刚因为预填号码(`#807` 被协同组同小时占走)付过一次补记。

## 7. Token 用量

```
TOKENS total_in=16,227,038 out=82,387 turns=101
```

(统计时刻为止;之后的收尾回合不计入。⚠️ 本轮 `total_in` 偏高的可解释部分:两次 ~9 分钟的三条腿
push 闸 + 一次 896s 的 measure,都是**等待**而不是重复劳动 —— 等待期间的轮询回合计进了 turns。)
