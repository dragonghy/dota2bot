# 总监 2026-09-08T19:23Z —— 两条退回出集(44 → 42),判定完结 **2**(达标)

**一句话**:上一轮排的 ① 取掉了(≥2 判定完结,上一轮 0);而本轮最该被读的不是裁定 ——
是**两条 id 各有一份绿的、钉在真实帧上的 fixture 测试,而两份都由测试作者亲手写进了该杠杆唯一转轴的那个输入**。

零 AWS、零波次、**`bots/`+`game/` 零 diff**、不发 owner 邮件、`DECISIONS_NEEDED` +0。
裁定全文档案 `iterations/streams/test_set.md §GC`(§GC.0–§GC.7)。

---

## 0. 开工自检(铁律 10)

真码 **`EXIT=3`**,10 条腿全跑完(不带 timeout)。
`FINDINGS: cadence queue-rulings owed-executions` / `UNCERTIFIABLE: trunk-red(python)`(GH #358 的
120s 预算,**不是通过**)/ `NOT RUN: tests/test_luacheck_gate_soakswitch.py
tests/test_selfcheck_lua_leg.py`。fast Lua **86 文件 / 0 failures**(门自己写着这是 FAST SUBSET)。

⛔ **「后台包装吞掉真码」第六次兑现**:harness 报 `[exited with code 0]`,自检自己打的
`selfcheck worst exit: 3`。躲过去的仍只是「把真码 echo 进文件」这个习惯。守卫**仍未立**,顺延(见 §8)。

⚠️ **纪律 3 本轮一发**:本轮第一条命令是 `routine_selfcheck.sh … | tail -60`,§22 守卫当场拒
(`REFUSED: stdout is a PIPE … SELFCHECK_EXIT=2 REFUSED`)。**不新立措辞**(章程 0 的措辞已覆盖这一发),登记而已。
此后本轮所有要读退出码的命令都走 `rc.sh`。

三条 FINDINGS 本轮**都交代了**:`queue-rulings` 清零(§4)、`owed-executions` 新增一行并当轮修正(§3)、
`cadence` 无升级(四组本轮均有产出)。

---

## 1. 选取依据(P4.2 的排序键,不是偏好)

`arm_since.py` + `verify_coverage.py`:最老一档(45 天)现存 4 条**已在 §FX/§FZ 逐条判过或具名排除**。
**下一档是 2026-08-19 的 20 天档**,其中 `verify=0` 的三条是 `cmrguard` / `tpdead` / `wandlimbo`。
本轮取后两条,因为两条的 (a) 都能在**零 AWS、零新局**下判死或判活;`cmrguard` 不能(要一波单臂读数),
**原样留给下一轮并写进 §GC.6**。

⭐ 一条此前没被登记的读数:两条 id 在 **W39/40/41/52/53/54/55/56/57 九个波次里全部只以成员串内的一员出现,
一次都没有被单臂 arm 过**(命中长度 397–501 字节 = 全量成员串,无一条短串)。
⇒ 条件 (b) 对这两条**从来没有过读数**,不是读数不好。

---

## 2. ⭐⭐⭐ 本轮的主轴(§GC.2):**一份绿的真帧测试,可以由作者供给该杠杆唯一转轴的那个输入**

| 测试 | 真读自 dump 的 | **作者写进去的** |
|---|---|---|
| `tests/test_replay_181441_wand_limbo.lua` | HP 214/1354、最近敌人 ~2017u、泉水距 ~4600、1600 内无敌人 | `rawget(wand,'__spec').GetCurrentCharges = n`(触发用 **12**,不触发用 **3**) |
| `tests/test_tpdead_release.lua` | 落地帧、盟友句柄、死亡时刻 | `bot.tpRespondLoc` / `tpRespondUntil` / `tpRespondAlly` **三行赋值** |

**两个被写进去的输入,恰好都是该 id 唯一多出来的那一维**:充能是 `wandlimbo` 相对已发行 magic wand 规则
**唯一多出来的子句**;commitment state 是 `J.GetTpCommitDefendDesire` **可达性本身**。
本轮实测:充能在 **953/953** 个句柄上读 0;commitment state 在 **0/1021** 帧上存在。

⇒ **两份测试都没有错,错的是把它们读成 (a)。** 这类测试问「**给定这个输入**,决策对不对」,
(a) 问「**这个输入到底出没出现过**,出现时机器人决策对不对」。
**在一帧其余子句全部真读自 dump 的真实帧上,这两者极难分辨,而树里今天没有任何东西替人分辨。**

⚠️ **AGENTS.md 那句「Gate-plumbing tests are NOT local validation」覆盖不到这个形状** ——
它说的是纯桩测试,而这两份**不是**纯桩测试:它们是真帧测试**加一个桩输入**,且桩的正是转轴。
`verify_coverage.py` 的 LIMITS 也只写了「verify=0 不等于从未核验」,没写「有一份绿的真帧测试也不等于核验过」。
**这就是两条 id 一边看起来「已 fixture 验证」、一边挂着 `verify=0` 二十天的机制。**

⭕ 处方(**桩转轴普查**)**本轮不落地**,登记进章程 backlog **99** —— 工作单元边界,且这是「登记而非发明」。

---

## 3. 两条裁定

### 3.1 `wandlimbo` 退回出集(44 → 43)

(a) **在两条仪器路径上都买不到**,不是没人去买。helper 第一条合取是 `hItem:GetCurrentCharges() < 6`:

* **fixture 侧** —— `replay_fixture.lua` 用 `api.MakeAbility(name,{IsFullyCastable=true})` 造句柄,
  不服务 charge getter,落到 `tests/mock/bot_api.lua:184` 的 `^Get -> 0` 兜底。实测 **953/953 读 0**。
* **replay 侧** —— `dumper/main.go` 只发 `Items []string`,全文件 `charge` 出现 **0** 次
  ⇒ 行为检测器也重建不出这条子句。

⭐ **`wl_full 0` 是仪器的零,`wl_nocharge 17` 是它盖住的域;两个数缺一个都什么都不说。**
**17 帧里就有它自己的立案帧** `f_181441_zuus_lowhp_limbo`(hp 0.1581 / 泉水 4598.5)——
作者为证明这条杠杆而冻下来的那一帧,仪器答不出来。

⭐⭐ **墙早就写在同一个文件里,helper 上方约 5800 行**:`stayfield` 注释逐字写着
「the mock's GetCurrentCharges default is 0. Declared, not hidden.」与
「its charge count -- the thing that decides whether it heals at all -- is not in the dump.」,
并**因此**拒绝把 wand 计入、把它交给「its own lever ('wandbleed')」。
`wandlimbo` 仍于三周后以充能计数为第一条合取入集,并骑了九个波次。
**一堵写下来但没有仪器的墙拦不住任何人。**

### 3.2 `tpdead` 退回出集(43 → 42)

它是 **§GA.1 逐字点名**的「两个释放(`tpdying`/`tpdead`)」之一,而那一轮**只裁了 `tpdying`**。
同一函数体、同一道 `tpcommit` 门(`jmz_func.lua:10062` 门 / `:10126` 子句):
单独 armed **逐位 no-op**,同时 armed 则读数**连符号都不可分解**(§GA.1,W49,4527 次落地 / 70 game-legs)。

⭐ 本轮另买到**第三堵墙**:该函数第五行要 `bot.tpRespondLoc`/`tpRespondUntil`,
那是**活局响应 TP 分支才写的状态**,实测 `td_state_present` **0/1021 帧** ⇒ **语料路径结构上不可达**。
两条路径、两个不同的原因。

⚠️ **`promote_atoms.json:tp_response_releases_need_commit` 不动** —— 它约束 **promote 日**的配置,
退集不是 promote;删它才会悄悄放开它存在的理由。它同时是这处嵌套的**第三个独立见证**
(**2026-09-05** 立,比 §GA.1 早三天)。已钉进新测试 §2f。

### 3.3 两条共同的边界

⛔ **退集不是 reject**:两个 gate 与两个 helper 体**逐字保留**,`bots/`+`game/` **零 diff**,
两条的 (c) 都成立且未被取代。重新入集的条件写进 owed 行,不留在散文里。

⚠️ **不掉进 `pullcad` 陷阱,断言过的**:两条各自**只有一个** gate 点,门行上没有第二个 id;
`promote_atoms.json` 里没有任何一行的 **prereq** 点名这两条。

⚠️ **载体项 7 → 7 逐字不变,量出来的**:`carrier_terms.py --arm` 对 44-id 与 42-id 两串各跑一次,
`TERMS` 行**逐字节相同**,`0 unresolved` 两次,`10 hero / 34 → 32 generic` ⇒ **选种解空间不受影响**。

⛔ **W57 及更早不与 42-id 家族并池。**

---

## 4. ⭐ `text_absent` 的第一个真实用户,当场找到该 kind 的洞

上一轮(09-08T13:00Z)落地 `text_absent` 时逐字登记「此刻没有任何一条活着的 owed 行用它」,
并写下「下一条『把这句写错的话改掉』形状的裁定就是它的第一个真实用户」。**本轮就是那一条。**

⛔ **新行 `wandlimbo_charge_instrument` 在被写下的同一分钟读 DONE**,下面还印着
`-> executed; the director should retire this row` —— 在一件**没有任何人开工**的仪器采购上。
两条 needle 分别错在:

1. **一个字母的大小写**(`the substring` vs 文件里的 `The substring`);
2. **跨注释折行**(`serves no charge getter` 在文件里被折成两行,从来不是一个字符串)。

⇒ **needle 从未匹配过**与**needle 所在的句子被删掉了**,在这个 kind 里**逐字节不可分辨**,
而失效方向是 **DONE**。该 kind 已经拒绝**空的/写坏的** needle 集合(它对任何路径都成立),
但**打错的 needle 不属于那一类**。

修正:两条 needle 改成在**裁定时刻逐字断言在场**再写入,行现读 **OWED**(`still contains 2 of 2`)。
限度写进 `pending_rulings.py` **LIMIT 11** 新增段(带这个现场);守卫进 backlog **100**。

⚠️ **抓到它靠的是当轮跑了一次 `--owed-only`。** 换一个把 registry 编辑放在工作单元末尾的轮次,
**这一行会生下来就是退休的**,而且两种打错方式都是**人肉复核最不容易发现的那种**
(两条 needle 看起来都和它们抄自的句子一模一样)。这条经验已写进 LIMIT。

---

## 5. 章程 §BB.4 的同轮义务:`hero-49`

`hero-49`(`lionqkill`,GH #640,**17:56Z 到**,本轮 `queue-rulings` 唯一一条)裁 **APPROVED-SCAN**
(与 hero-41..48 同档:请求 `bundle` 里逐字写着「NOT armed —— P4.2 冻结期内本条不请求入集」)。

⭐ **并当轮做掉它夹带的那件明写「接力棒:总监」的活**:`promote_atoms.json` 新行
`impale_kill_needs_damage`(`no_promote_without`,subject=[`lionqdmg`]、prereq=[`lionqkill`],**单向**)。
`promote_atoms.py` 复读 **5 atoms / OK / lionqdmg=GATED lionqkill=GATED**。

⛔ **理由逐条核过源码,不是照抄请求**:
* 击杀循环确实走 `nInBonusEnemyList`(`nCastRange + 200`)并原样 `return npcEnemy:GetLocation()`,**自身无距离测试**;
* `X.GetImpaleKillDamage` 未 armed 时返回 `hAbility:GetAbilityDamage()`,而 `lion_impale` 无顶层 `AbilityDamage` ⇒ **恒 0** ⇒ 该分支在出货默认下是死的;
* `X.lion_IsImpaleKillTargetInReach` 未 armed 时**原样返回** `bShippedLethal` ⇒ 纯收窄守卫 ⇒ **单向成立**。

⭐ **独立佐证(非请求提供)**:`tests/mock/replay_fixture.lua` 的 AbilityDamage 注释**从另一侧**
点名 `hero_lion.lua X.GetImpaleKillDamage` 属于「调用者所在能力不声明该字段」的那一类,并称该落点 'provably inert'。

⚠️ **针脚(§GB.3 立的机器键)两半同轮都加了**:queue 行的 `director.owed_row` +
owed 行 `contains` 加 `hero-49`。**并实测单边删除会红**(`test_domain_scan_pass_pins.py` exit **1**),还原后绿 ——
即这条针脚**真的覆盖了本轮这次追加**,不是「加了就算」。

---

## 6. 量具与变异

* `tests/_blind_a_sweep.lua` —— 重语料 sweep(110 fixture / 1021 live turbo 帧),`_` 前缀,**不进快腿**(GH #358 的 120s)。
* `tests/test_blind_a_wandlimbo_tpdead.lua` —— **11 checks / 0 failed**,毫秒级,语料读数以定值引在头里。
* `tools/agent/mutstand_blind_a.sh` —— **10 CAUGHT / 0 SURVIVED / control_ok=1**,
  五个文件还原 `git diff` **逐字节 YES**;`test_mutstand_restore_trap.py` 对新台全 ok。
  **十发全部朝同一个方向:每一发都「解除一堵墙」或「拆掉一条理由」,即每一发都让本裁定变错。**

⚠️ **M10 第一次跑 SURVIVED,而它是对的、断言是错的**(纪律 2 的正面兑现):
§2e 原本断言文件里存在 `bot.tpRespondUntil =`,而**那是 `bot.tpRespondUntil ==` 的前缀**,
同一文件恰有两处 `== nil` 断言 ⇒ **把三行赋值全删掉,断言照样绿**。
一条**比较**能满足的断言不是关于**赋值**的断言;改成钉整条赋值语句后 CAUGHT。

⚠️ **半 2 的对照组失败,那一半的零因此不作为证据用**:arm `tpcommit` 单臂当对照,
`tc_alone_nonnil` **也是 0**。按 GH #171 家规,对照为零时第一个零什么都不是。
成因**量出来不是猜出来**(`td_state_present 0`)。半 2 的裁定依据是**源码读数**(已如此标注)
**加** §GA.1 的已有读数 —— 半 2 买到的是第三堵墙,不是读数。

---

## 7. 成本 / 各组健康度 / 投递

* **成本**:零 AWS 支出。不作 MTD 新声称,转载批测台 15:10Z:**$66.105**;
  `$80` 围栏 / `$90` 刹车 / `$100` 批准线**均未跨**。
* **健康度**:四组本轮均有产出(batch-desk 15:10Z W57 收割、hero 14:31Z + `lionqkill` 落地、
  strategy 影子普查、replay-check 638 线)。`cadence` 三个洞不升级。
  无待总监插手的 [bug]/[harness];无待审批的测试集变更(hero-49 已裁)。
* **投递(§2.5,三处齐)**:
  * **机器字段**:`queue.json:hero-49.director`(含 `owed_row` / `deliverable`)。
  * **档案**:`test_set.md` 表头 + §GC 全文。
  * **常驻义务**:`owed_executions.json` 新行 `wandlimbo_charge_instrument`;
    `tpdying_isolation_leg` **补点名 `tpdead`**(原文只写了不 arm `tpdying`,
    **只不 arm 一个释放的隔离腿买不到任何一条的 (a)**);
    `hero_domain_scan_2_30_31` 针数 20 → **21**。
  * ⚠️ **issue 线追评本轮未发**:按 GH #290 的顺序律先 push 再发,而 `mcp__github__*` 本轮**未试**;
    按铁律 11 不空转等待,该写的内容留在本节与 §5,**issue 号仍空**,排进下次触发 ⑧。

---

## 8. 下次触发

1. **判 `cmrguard`** —— 20 天档 `verify=0` 的第三条,§GC.6 明写留给下一轮;先判 (a) 买不买得到。
2. `hero_domain_scan` 的**九份读数逐份读通**(顺延)。
3. GH #358 的 120s 预算要人裁(顺延)。
4. **给「后台包装吞掉真码」立守卫**(**第六次**兑现,顺延)。
5. GH #633 没买到的那一半:`I/O error` 形状的行与真警告分开,前者走 `verdict 2`。
6. backlog **100**(`text_absent` 注册时守卫)。
7. backlog **99**(桩转轴普查)。
8. **为 `wandlimbo` 的仪器缺口开 [harness] issue** —— owed 行已登记,issue 号仍空。

---

## 9. 铁律 6

* 静态半:`bash tools/agent/luacheck_gate.sh` ⇒ **`GATE_EXIT=0 CLEAN`**,`luacheck bots game: 0 warnings`。
* python 半:`py gate: 86 ran, 0 findings, 0 uncertifiable, 15.3s`
  (2 个未进 manifest 的测试照跑)。
* 本轮改到的 Lua 测试逐个复读,**全绿**:
  `test_blind_a_wandlimbo_tpdead`(11)、`test_replay_181441_wand_limbo`(9)、`test_tpdead_release`(9)、
  `test_gated_helper_nesting_census`(10)、`test_gate_claim_consistency`(16)、`test_smoke_load`(3)。
* `test_pending_rulings.py` **384 checks / 0 failed**;`promote_atoms.py` **5 atoms / OK**;
  `test_domain_scan_pass_pins.py` **5/5**(并实测单边删除会红)。
* **未用 `RULE6_BYPASS`。**
* ⛔ **动态半(Lua 全量,~100min,GH #124)未跑,不作声称** —— 本轮 `bots/`+`game/` **零 diff**。

---

## 10. TOKENS

```
TOKENS total_in=24,581,511 out=111,008 turns=137
```
(铁律 8:这是"到统计时刻为止"的数字,统计后的收尾回合不计入。)
