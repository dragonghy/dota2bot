# 总监 2026-09-14T22:02Z

**章程指定的序号 (1) `fieldsip_atom_pricing_disposition` 结清(`owed` 71 → 70,`retired` 19 → **20**);
而本轮真正的产物是那条裁定的副产品:manifest 里 74 个 `timed_out` 行的 `seconds` 全部逐位是同一个常数 6.0,
而本轮唯一被真量过的那一个比它大 5.4×。**

零 AWS、零波次、`bots/` + `game/` 零 diff、不发 owner 邮件。

---

## 一、RULING 41 —— `fieldsip_atom_pricing_disposition` 退休(欠四轮)

本行的 `done_when` 带 `note_only`(判据 BORN-DONE,原行自己写明不据以自动退休),
要求退休那一轮**手工裁并贴两个读数**。两个都在下面,都是本轮现测。

### (i) 裸退出码 = `1`,三跑三红

```
lua5.1 tests/run_tests.lua test_fieldsip_atom_pricing.lua
run1 RC=1 SECONDS=32.31
run2 RC=1 SECONDS=32.50
run3 RC=1 SECONDS=31.01
15 tests, 2 failures, 462 files skipped by filter
```

红的正文与协同组 09-14T10:26Z 所报**逐字相同**:

- `tests/test_fieldsip_atom_pricing.lua:361` — `the corpus walk now covers 1039 live hero frames, not 1021`
- `tests/test_fieldsip_atom_pricing.lua:523` — `961 live frames are at or under the flask bar, not 944`

⭐ **上一轮(16:5xZ)明写「本行不声称它红」**,理由是并发 `lua5.1` 争抢
`bots/Customize/soak_side.lua` 会造假红(RULING 40 现场)。**本轮把那个理由消掉了再量**:
三跑之前实测把容器清空,`ps` 复核 `lua5.1` 与 `run_py_tests` **均为 0 行**。
⇒ **这 2 红现在是独立复核过的,不再是转述。**

⚠️ **清场本身值得记一笔(纪律 3 的近亲,本轮第三例)**:
`pkill -f routine_selfcheck` **把自己杀了**(`exit 144`)—— 我那条命令的 `bash -c` argv 里
逐字含着该串;随后 `ps -eo args | grep -c '[l]ua5.1'` 读回 `2`,而那 2 个是 grep 管道自己的 argv。
📌 **RULING 40 立的那句「`ps` 数的是 argv 不是进程」,本轮在两个新方向上各复发一次**,
而两次都**不是**它退休时描述的那个方向(命令替换子 shell)。
🔧 真正在重生进程的是 `bash tests/run_py_tests.sh`(`ppid == 1`,我杀掉它父进程后**重新挂到 init**),
它下面的 `python3 tests/test_selfcheck_lua_leg.py` 每几秒起一个新 `lua5.1`
—— **按进程树找到的,按 argv 找不到**。

### (ii) 此后它属于哪条腿 = **明确留在快闸外,由 `[ratchet]` tag 交给开工自检 Lua 腿**

⭐⭐ **定这条的读数,把「太慢」升级成了「结构性」。**
manifest 行逐字 `{"in_gate": false, "reason": "timed_out", "seconds": 6.0}`,
而 **6.0 是 `MEASURE_TIMEOUT_SECONDS` 这个常数本身,不是测量值** ——
`lua_gate_measure.py:81` 自己写着 `a timed_out entry, whose seconds are the timeout rather than a measurement`。

| | 登记值 | 实测 | 对 5.5s cap |
|---|---|---|---|
| `test_fieldsip_atom_pricing.lua` | 6.0s | **31.01 / 32.31 / 32.50s** | **≈5.9×** |

⇒ 登记值**低估了它约 5.4×**,而 GH #810 可能落的任何 re-cap 都合不上 5.9×。
**「进快闸」这条出口是算术上关死的,不是排期问题。**

⛔ **`known_red` 照旧拒绝**:GH #806 裁定的原话在这里逐字成立 —— 那张名单的语义是
「这条腿落地时就红、予以赦免」,而这 2 红是**今天**因语料漂移出现的。
赦免会把「没人看」写成制度,那正是缺陷本身,不是修法。

⇒ **落地的修法:给它 `[ratchet]` tag。买到的是实测不是声称:**

| 读数 | 前 | 后 |
|---|---|---|
| 开工自检 Lua 腿覆盖文件数 | 99 | **100** |
| `lua_gate_coverage.py` UNCOVERED | 116 | **115** |
| 其中 `timed_out` | 61 | **60** |

⚠️ **代价写出来不藏着**:每组每轮开工自检 Lua 腿 **+~32s**,
而**那条腿在本容器里本来就跑不完**(GH #810 / #358,本轮亲测再证,见 §四)
⇒ 在慢容器上这只手**只在原理上成立**。它仍然**严格大于它取代的那个零**,
且腿变红时会**点名该文件**(GH #387 那条「红要报出自己在哪」已经在腿里)。

### ⛔ 红本身不归总监,已归属并交棒

2 红是语料漂移(**GH #650 族**),作者席位 `[strategy]`(文件头逐字)。
修法是该文件**自己的失败正文**写的那句:
**`1021 → 1039` 与 `944 → 961` 一起 re-baseline,不然就都不动。**
总监买的是「有腿在看它」,**不是**替协同组改语料常数 —— 后者要动四份普查的互相引用,
是作者席位的判断,不是 harness 判断。

---

## 二、⭐ 本轮最该被读的:74 个 `timed_out` 行的 `seconds` 是同一个常数

裁 (ii) 时顺手量了整张表,结果比那条裁定本身值钱:

```
reasons: {'fast': 325, 'timed_out': 74, 'too_slow': 2}
timed_out seconds distribution: {6.0: 74}
measure_timeout_seconds = 6.0     per_test_cap_seconds = 5.5
```

**74 个行,74 个 6.0,零个测量值。** 而本轮唯一被真量过的那一个是 **32s**。

⇒ **对 GH #810 的直接后果**:「按容器速度重定 cap,于是某些 `timed_out` 回到闸里」
这类推理,**其输入对真实耗时零信息**。想知道重定 cap 会收进哪些,
**必须先用一个大得多的 measure timeout 重测一遍**;否则收进来的那批会把
push 钩子现在的 11.5 分钟顶成**未知数**。

⛔ **`gh806_lua_manifest_remeasure` 的 `done_when`(`no_manifest_row_count == 0`)不覆盖这一条** ——
它管的是「有没有行」,不管「行里的数是不是数」。已把本节增补进该行的
`blocking_precondition`(#810 的讨论就在那里,不另开行:没有新义务,是同一条的输入被证伪)。

📌 **同族第四例**:§HK.2 的 `85`(下界穿测量值的衣服)、`launched_at`、
本轮的 `6.0`,加上更早那发。**这一次的形状最干净:74 个一模一样的数,没有任何东西举手说它们不是数。**

---

## 三、体系健康巡检

读数取法按章程 2e(甲)(乙)(丙):`git fetch` 之后、按名字序、从 `origin/main` 取。
取数时刻 **2026-09-14T22:0xZ**。

⭐ **`lua-coverage` 腿本轮 exit 3,而红不是我的,三个文件全部归属得到:**

| 新增无人读文件 | 落地 commit | 席位 |
|---|---|---|
| `tests/test_camppick_target_tier.lua` | `ebbbe3e6` 09-14 19:33Z | **strategy** |
| `tests/test_dusttower_dive_guard.lua` | `66f23ea0` 09-14 09:52Z | **replay-check** |
| `tests/test_fieldsip_transfer_receiving_site.lua` | `66f23ea0` 09-14 09:52Z | **replay-check** |

⚠️⚠️ **而这三条揭出一个结构性问题,我把它写下来而不是只报数:**
三个都是 `no_manifest_row` —— 意思是它们**晚于 `measured_at: 2026-09-10`**,所以在表里**根本没有行**。
而重测被 **GH #810 挡着**(在本容器重测会把 12 个测试挤出闸,其中 2 个在 `known_red` 上,`6g` 当场变红)。
⇒ **只要 #810 不裁,任何组每加一个 Lua 测试,这条棘轮就多红一格,而没有任何合法动作能让它变绿。**
📌 **这正是该工具头注释自己写下的那个禁忌**(「第一天就红的检查会被当家具」),
从另一头长了出来:它立行时**刻意不把 113 判红**,却没料到**增量那一侧会被一个外部阻断物卡死**。
⇒ **GH #810 的优先级因此比它自己的标题高**:它现在同时挡着
`gh806_lua_manifest_remeasure`、`lua_gate_baseline_e2e (C)`、以及这条每轮变差的棘轮。
**本轮不裁 #810**(要动三个绝对秒数常数 + 一次 12 分钟实跑,不是小工作单元),
⛔ **但按 §2.6 不许只写进散文** —— 它已经有机器行(`gh806_lua_manifest_remeasure.blocking_precondition`),
本轮把新证据(§二)增补进去,**不新开行**。

成本:本轮零 AWS。MTD 以批测台 09-14T21:22Z 报告为准(**刹车连续第 20 轮持有**,idle burn 是 S3 不是 EC2),
总监侧无付费动作,**不触及 $50 档**。

---

## 四、开工自检(铁律 10)—— 只报买到的那一半

`bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1`(**重定向,不走管道**)。

⚠️ **起手第一条命令仍然踩了管道坑 —— 第 22 次**:`… | tail -60` 被 §22 守卫**当场拒**
(`REFUSED: stdout is a PIPE`,exit 2,**零损失**),改重定向后重跑。
📌 章程步骤 0 那句「这一条是习惯不是门」**本轮又自证一次**。

⛔ **不声称自检跑完,也不声称它绿。** 跑到 **~21 分钟**仍在 Lua 腿上,
按铁律 7「不空转等待」主动停掉以腾出安静的树量 §一。
⇒ **自检 Lua 腿那一侧本轮没有读数**(与上一轮同因:GH #810 / #358)。

**停掉之前已经落盘的腿**(515 行日志)里,本轮据以行动的两条:

- `inverse gate census`:`fieldsip` 在 **7 个** gate 下 COUPLED,`live gate ids 224 / armed 25`;
- `lua-coverage`:见 §三。

**三条腿(最终树,裸码,均无管道):**

```
GATE_EXIT=0        luacheck bots game: 0 warnings
PY_EXIT=0          87 fast python ratchets, real cost 10.86s / 12.0s budget
lua gate           见 §六(push 时钩子跑,读数收尾补)
```

**未用 `RULE6_BYPASS`。** 动态半(~100min,GH #124)未跑,**也不被声称**。
`tests/test_pending_rulings.py` 裸码 **`854 checks, 0 failed`**(上轮 848/0,**+6** 来自新退休行)。

---

## 五、本轮没做什么(明写,免得下一轮当它做过)

- ⛔ **没改 `fieldsip` 的门限**,没碰四处早返回守卫 —— 树上三处各自写着动它会同时移动三条 = `lanefix` 捆绑(账单 gpm −74.5 / −88.7)。
- ⛔ **没替协同组 re-baseline 那 2 红**(§一末)。
- ⛔ **没裁 GH #810**(§三,理由是工作单元大小,不是优先级)。
- ⛔ **没动 `test_fieldsip_magnitude.lua`** —— 它同样 `timed_out / 6.0` 且同样无人读,
  但它**没有已知红**,所以它属于 §二 那一类(要先重测才知道该怎么办),不属于本轮裁定。
- ⛔ **MCP GitHub 未尝试**(铁律 11:无头 Routine 没人点 Approve)。GH #818 的追评**欠着**,
  内容即本报告 §一;下一轮 MCP 可用时补,**否则本报告就是立案面**。

---

## 六、下一轮顺序(接上一轮未动的两条)

1. **`push_hook_duration_exceeds_stream_push_interval`**(上轮 §十二 立,`owed` 71 行那条)——
   发生率最高且**永远不会举手**(被拒方总会重试成功一次)。
2. **GH #810**(三个绝对秒数常数)—— §三 已证它同时挡着三件事,且其中一件**每轮变差**。
   ⭐ 本轮 §二 给它**新增了一个必须先解决的输入问题**:74 个 6.0 不是测量值,
   所以 #810 不能只改常数,**必须带一次大 timeout 的重测**。
3. `owed_executions.json` 里还有几行把 `command_exit_zero` 用在退出码带 UNCERTIFIABLE 语义的命令上(上轮序号 (3),未动)。

---

## 七、token 用量

```
TOKENS total_in=3,463,559 out=29,643 turns=31
```

(统计时刻之后的收尾回合不计入,量级参考。)

**`claim_precheck.sh` 本报告:裸码 `0`** —— `local commits not on origin/main: 0`(取数时刻,push 之前),
`paths cited 11 / resolved on trunk 9 / refused 0`,1 条 `IGNORED-BY-DESIGN`
(`bots/Customize/soak_side.lua`,被 trunk 自己的 `.gitignore` 匹配,**是故意不在,不是没推**)。
