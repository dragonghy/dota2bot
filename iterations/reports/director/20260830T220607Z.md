# 总监 2026-08-30T22:06Z —— 三条搭车提议同轮全裁(44 → 47),spot 围栏价上调,并且指出「4 粒不够裁」用错了尺子

**上一轮点名的第 1 优先做完了**:`strategy-25`(`creepthink`)与 `hero-24`(`lionqdmg`)裁定落到
`queue.json` 的 `director` 字段;**本轮到达的 `hero-25`(`cmqreach`)一并裁掉**,
所以 §BB.4「搭车提议在到达那一轮被裁」**第一次真的做到**(`odbuild`/`wkqdmg` 当初用了三轮)。
裁定全文:`iterations/streams/test_set.md` **§CO**;登记:`state.json:admit_three_20260830T22`。

## 0. 门与自检(退出码一律裸读或读工具自打的横幅)

- 开工自检:**8 腿,`selfcheck worst exit: 3`,唯一 FINDING = `queue-rulings`** —— 点名的正是本轮裁的三条;
  `UNCERTIFIABLE: none`;锚点 `stable-v1`/`stable-v2` 三条不变量全 ok。
- trunk 健康:**python 62 passed / 0 failed / 0 uncertifiable**;**44 个 tagged Lua 检测器 0 失败**(快子集,不是全套)。
- 本轮 **Lua 零改动**(`git diff --stat bots game` 空),改的是 `queue.json` / `test_set.md` /
  `batch-desk.md` / `state.json` / 报告 / 本章程。`luacheck_gate.sh` 结果见 §7,**未用 `RULE6_BYPASS`**。
- 三条 id 的测试各自裸读退出码:`creepthink` **12/12**、`lionqdmg` **13/13**、`cmqreach` **16/16**,
  外加 `gated_helper_nesting_census` 10/10、`gate_claim_consistency` 10/10,**全部 exit 0**。
- **本轮零 AWS 调用、零支出。** 成本读数照抄批测台 21:12Z:MTD **$71.458**、围栏 $72.26(保守取法 $73.06)
  **≤ $80**、实例空、零泄漏;⚠️ 其 budgets 快照自标 `2026-08-30T11:33:48Z`(**已连续四轮未前进**),**本轮未重复核查**。

## 1. 三条入集裁定(ROUTED_RIDESHARE / ADMITTED,成员串 44 → 47,414 字节)

资格**按源码核过,不是按摘要核过**:三条各一个独立 gate(全文件计数 = 1,**无与任何 id 的合取**
⇒ `pullcad` 陷阱不适用)、turbo 在场(`creepthink` 是**结构性**的:`J.ShouldCreepPullLane`
`jmz_func.lua:7793` 开篇即 `IsModeTurbo`)、gate-off 路径逐字复现出厂。

三条各自的边界、UNINTERPRETABLE 退回门、预登记否定结局,**逐字写进 `queue.json` 的 `director` 字段**
(§2.5:裁定要落到被裁方读的那个字段上,不落进档案、不落进申请方的 `question` 散文)。
`lionqdmg` / `cmqreach` 的 **`executor` = `replay-check`,恰好一个流**(§CH 立案句:写两个流 = 一个都没点)。

## 2. ⭐ 三条总监自己的读数,一条抬 `creepthink`,两条约束它

1. **它不在 `pullthink` 的处境里。** `pullthink_domain.py` 的表头立过:营地那半**两腿不共域**
   (`roamCampPull` 要仍 gated 的 `pullcamp` ⇒ baseline 腿一次营地拉都没有,armed/baseline 比的是
   「拉营 vs 顺路打野」),只能退到跨波对照。**`J.ShouldCreepPullLane` 没有任何 soak gate**
   (`creeppull` 已 promote,`stable-v2`)⇒ 勾线**两腿都跑**,`creepthink` 的 (a) **有波内同域镜像对照**。
   **不要把那份跨波致歉抄过来。**
2. ⚠️ **`pullcad` 与它交互,而这道界落在一个没人提议改动的 id 上。** 两者动同一段
   (`mode_roam_generic.lua:270–315`):`pullcad` 移 `nBeat` 1.2 → 3.0,`creepthink` 改的是
   **这段代码多久被问一次**。⇒ 合力不可加;**W30 起的 `pullcad` 读数不得与 W25–W29 的并池**。
3. ⚠️ **`pullcad` 源码注释里的条件 (c) 算术是在没有节流阀的模型上算的**
   (「83% of 3.0s vs 58% of 1.2s」预设 Think 体每帧被问到,而 §CK 的不等式否掉的正是这个预设)
   ⇒ 那两个百分比**不是 W25–W29 测的那个世界**。不否掉 `pullcad`,但**交棒协同组重推或改写措辞,不许静默删数字**。

**(a) 的归属判别子(结构性、可执行)**:节流阀在时 drag 只能发在被重开的帧上,armed 后可以发在
`ACTIVITY_ATTACK` 内的帧上 ⇒ **帧表若带 anim activity,一波之内就能把两根杆分开**;
若不带,退到跨波增量并**按跨波对照报,永不报成 A/B**。

## 3. ⭐⭐ 本轮最值钱的一条:「4 粒不够裁」把 owner 2026-08-01 废掉的检验又请了回来

批测台 W29 §3.6:四粒 gpm arm SD 53.12 / **SE 26.56**,均值 **+23.55 < SE** ⇒
「W29 单独既不支持 promote 也不支持 reject,需要第二波做厚到 8 粒」。
**四个数、SD、SE 复核都对,结论用错了尺子**:

- 铁律 2 逐字:小改动**不做数据显著性检验**;条件 (b) = 「对胜负**没有明显负面影响**(**粗粒度,非显著性**)」。
- `+23.55 gpm` / `winrate 0.503` / `deaths 0.00` / `comps_better gpm 3/4` —— **没有任何负面**。
  「均值 < SE ⇒ 不够裁」问的是**效应量是否显著异于零**,而那正是 owner 三十天前废掉的那一步。
- ⇒ W29 **不是**「买了四粒语料没买到结论」:它买到的正是 (b) 要的那个结论。
  真正卡住 44/45-id 家族 promote 的**从来是条件 (a)**(本章程连续多轮自记)。
- **代价可计价**:按那把尺子,每个家族都要 8 粒才敢说话 ⇒ 每次入集都得先冻结成员串两波,
  而本轮就有三条零成本搭车提议在等,§BB.4 说它们唯一的成本就是不被裁。
  **⇒ 本裁定同轮改串 44 → 47,接受 W29 成为 44-id 家族的唯一一波。**
- ⚠️ **不是说 SE 无用**:它仍是 (a) 之后排序候选、判断单个 id 效应量、以及任何**定量**主张的前提。
  废掉的只是**「显著才算测过」**。**交棒批测台**:`suggested` 与 SE 照常打,
  但「够不够裁」这句话**不归 SE 管,归三条件管**。

## 4. spot 单波围栏价 `~$0.8` → `~$0.90`(不取批测台建议的 $0.85)

W29 四台 **3.424 机时**,价带 $0.231–0.250/h ⇒ 实测 **$0.79–0.86**(复核:0.791 / 0.856,两端都对),
`~$0.8` 压在最低点。**$0.85 仍落在该区间之内**,即在 W29 自己身上就可能少记,而少记是本台自己
点名的危险方向;**`max(费率)×max(价)×4 = $0.948` 也不取**,那是两个从未同时出现的极大值之积,
与 cap-25 那个高估 52% 的 `$3.05` 同族(朝安全侧失效 ⇒ 静静地少发波)。
⇒ 取覆盖全部账单侧实测点($0.795 / $0.799 / $0.747 / $0.79–0.86)的 **$0.90**,围栏代价 ≈ 每波 $0.10。
**并装上自动失效条款**(与 GH #332 的 `BRACKET_HI` 42.6 → 40.63 同族):
**任一波实测单波成本 > $0.90 ⇒ 批测台在该轮点名,总监下一次触发必须重裁。常数不许靠人记得去复查。**
已改 `batch-desk.md` §2 的两处**活规则**行(历史段未动)。

## 5. 从 `cmqreach` 里捡出一件不属于它的事(交棒英雄组)

`bots/BotLib/hero_silencer.lua:301-305` 的 nil 守卫**已按源码复核属实,且比 §CN.6 的措辞更硬**:
`or` 短路 ⇒ **第一条子句为真的那一刻,then 体第一件事就是索引刚被判为 nil 的值**。
**不需要知道引擎会不会返回 nil**:要么该子句**永远为假**(死守卫,而作者写它说明他认为不是),
要么它**为真过一次,那一帧 Silencer 的 think 静默炸掉**(`error in error handling` 吞掉全部文本)。
一个住在**别的 id 的 `known_gap` 键里**的 nil 解引用,**对每一条队列都是隐形的**(与 **§CG** 的 `CG.1`
节所记的 `ORPHAN_PROPOSAL` 同种隐形)⇒ **英雄组开自己的 `[bug]` issue**。
**本轮总监不修**:Silencer 不在焦点五,且改的是出厂默认,按 `gated-fix` 纪律要各自的证据与 id。

## 6. ⚠️ 纪律 3 在 skill 落地的下一轮复发 —— 上一轮**预登记**过这个判据

上一轮把四条纪律做进 `.claude/skills/evidence-discipline/` 并预先写下:
「若纪律 3 在 skill 已在盘上的情况下复发第五次,说明补法必须是**机械的**不是文档的。」
**本轮第一条命令就是 `bash tools/agent/routine_selfcheck.sh 2>&1 | tail -60`**,
harness 报回的 `exit code 0` 是 **`tail` 的**(自检自己打的是 `worst exit: 3`)。
**该读数已丢弃**,改读工具自打的横幅;本轮其余退出码一律裸读。
⇒ **预登记条件已满足:补法必须机械化。本轮没建**(工作单元是三条裁定 + 一个常数),
**下一轮第一优先**:一个拒绝「管道后读退出码」的包装或扫描器,带自己的变异台。
**按纪律 4 记法登记:本条走的是「丢弃读数 + 改读工具自打横幅」这条路,不是「重跑一遍」。**

## 7. 门与推送

- `bash tools/agent/luacheck_gate.sh` —— 见提交前一次执行,**裸读 `$?`**;结果与 `git push` 的钩子一致。
- `.githooks` pre-push 门自 `arm_push_gate.sh` 起对本容器生效;**未用 `RULE6_BYPASS`**。
- 铁律 6 的动态半(`lua5.1 tests/run_tests.lua` 全套 ~100 min)**本轮未跑**:零 Lua 改动,
  已跑的是**与本裁定相关的五个文件 + 自检的 44 个 tagged 检测器**。**这是范围说明,不是全套通过的声明。**
- 发表任何带引用的评论前跑 `claim_precheck.sh`(§290),**三份草稿全跑过,裸读退出码**:
  Silencer 交棒 issue 草稿**第一次 exit 3**(`MISSING section test_set.md §CG.1`)⇒ **没有发**,
  改成可解析形式并**先 push 后发**,复跑 **exit 0 / `local commits not on origin/main: 0`**;
  两份裁定评论草稿 **exit 0 / clean**。
- **投递(§2.5 三处齐)**:(i) `queue.json` 三个 `director` 字段;(ii) `test_set.md` §CO 全文档案;
  (iii) 活线程追评 —— GH **#326**(`creepthink`)`issuecomment-5471595914`、
  GH **#175**(`lionqdmg`)`issuecomment-5471597388`;`cmqreach` 无活 issue,走前两处。
  **交棒(铁律 9,同一工作单元内交出)**:GH **#346** 新开
  (`hero_silencer.lua` 的 nil 守卫,交英雄组),批测台那条裁定已写进 `batch-desk.md` §3
  的 verdict 读法(**只写档案就是 W3 那次「已作出而未送达」的形状**)。
- **⚠️ 同轮捡到的第二件 [harness]**(全文 §CO.8):`claim_precheck.sh` 要标题里那个 `§`,
  而 `§CG` 一族的子节标题写作 `### CG.1`(无 `§`)⇒ **归档里 287 个互异 `§XX.N` 引用中
  143 个指向无 `§` 的标题**(正则口径,记作量级不是判据;工具口径已用探针草稿实测:
  `§AR.0` ⇒ exit 3)。**失效方向与别的漂移相反**:门在发表前拦,
  于是它不让错引用出去,而是**让正确的裁定发不出去**,最省事的绕法是把引用删掉。
  **本轮不改**(要自己的变异台),列 backlog **§20**,倾向让解析器同时认两种写法。

## 8. 各组健康度

四组本轮均有产出,**无空转**:批测台(W29 收割 + §2.1 单价实测)、英雄组(CM `cmqreach` 落地)、
协同组(`stayfield` 闭式空域、GH #342 交棒)、录像组(18:57Z 报告)。
Owner 优先项本轮无属于总监的未完成项被跳过;`DECISIONS_NEEDED.md` 无新增;
本周邮件与效率台账 01:20Z 那轮已做 ⇒ **本轮无邮件、不重做台账**。

## 9. 下次触发(按序)

1. **⭐ 给「管道量退出码」建机械补法**(§6,预登记条件已满足,不许再用文档补)
2. **§CE 正则**(已挂八轮)
3. **backlog §19**(cadence 腿改读 git 提交时刻)
4. 验收:下一份批测台报告应出现 **`iv_reclaim_blind`** 那一栏,且围栏算术用 **$0.90**
5. 验收:英雄组是否为 `hero_silencer.lua` 开了 `[bug]` issue(§5 交棒)
6. 验收:协同组是否处置了 `pullcad` 条件 (c) 的算术(§2 第 3 条)
7. `#326` / `#324` / `#328` 未裁;`state.json` round-trip 未修
8. 45-id 家族 **+26.60 / 7/8** 的条件 (a) 逐 id 帧核验(§3 之后,这是唯一的真瓶颈)
9. **低频 patch 检查**(连续多轮欠)
10. backlog §16 / §17 / §18

## 10. Token 用量

```
TOKENS total_in=5,968,947 out=49,556 turns=49
```
(`python3 tools/agent/token_usage.py`,到统计时刻为止;统计后的收尾回合不计入。)
