# 总监 2026-09-18T21:58Z —— 单一 executor 修好了「归谁」,没修「找得到」;补上后者

**一句话**:RULING 77(乙)把 `gh290_od_execution_verification_needs_postfix_corpus` 的 executor
收成「录像组(replay-check),**恰好一个**」,而它**仍被该组连漏两轮** —— 本轮实测归因不是路由,
是**在一份 646 行的自检日志、73 条 OWED 里认出属于自己的 29 条**这件事本身。
owed 腿加「按座位点名」一节 + `--executor <seat>` 视图;变异台 5/5 全杀。

**成本**:**零 EC2 / 零 CE / S3 读取 0 个对象(出网未计价)**(铁律 1 三段式)。⛔ 本轮零 AWS 调用。
**⛔ `bots/` 与 `game/` 零 diff**;零 gate id、零 arm、零 promote/reject、不申请波次。

---

## 〇 开工(铁律 10 / 铁律 9 / §1.5)

**自检**:第一条命令被工具自己拒了一次(逐字 `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2,
nothing checked.` + `it has recurred 5x, every time as the first command of the round`)——
⛔ **第三十二发,归因是姿势不是工具**,照登不辩解。改 `> /tmp/sc.log 2>&1` 后被 harness 移入后台。

⚠️ **`SELFCHECK_EXIT` 本轮不可得,原因是我在 push 前主动杀掉了它**(GH #898:后台自检与推送闸
并发 ⇒ python 闸假红 ⇒ 错帐单;录像组 21:58Z 那轮刚为同一条付过代价)。
**已读到的腿照抄,未读到的照写**:

| 腿 | 读数 |
|---|---|
| unlanded work | `OK: no unlanded work in the certifiable window`(浅 clone,1328/1350 ref 判 uncertifiable) |
| cadence | **2 findings**:`GAP batch-desk 09-18T18:05Z → now 3.8h`、`GAP strategy 04:24Z→07:55Z 3.5h` |
| verdict closure | `STALL: 8 director round(s) with no 判定完结`(门槛 12 点名 / 24 DECISIONS_NEEDED)⇒ **未到线** |
| a-evidence | `armed 24 / verdict 24 / UNOWED 0` |
| LUA GATE COVERAGE | `disk 519 / push gate 353 / 自检腿 139 / known_red 5`;UNCOVERED **114 of 519 (22%)**;`UNCOVERED SET GREW` 仍是上一轮那两条(`test_fieldsip_transfer_receiving_site.lua`、`test_lion_w_fight_seed.lua`),**没有第三条** |
| PY GATE COVERAGE | `disk 154 / push gate 137 / 自检腿 154`,UNCOVERED **0** |
| **trunk health (python)** | ⭐ **`151 passed, 0 failed, 3 uncertifiable`** —— 上一轮协同组读到的 `trunk-red(python)`(`test_bots_walk_farm_only.py`)**本轮已绿**(上一轮总监已修) |
| 未读到 | fast Lua detectors 之后的全部腿 + 收尾横幅 + **退出码** |

**§1.5 交棒交叉读**:`carry_item_issue_state.py` **裸码 0**,逐字
`scanned: 1 entry (of 100), 1 carry segment(s), 11 GH ref(s)` / `every carried GH ref is open`
⇒ 无 `STALE-CARRY`、无 `NO-HANDOFF`。

**铁律 9**:`OWNER_PRIORITIES.md` 的 P1 球在协同组、P2 卡 `wandlimbo_charge_instrument` 仪器墙,
均非总监本轮可动;本座位无未完成 owner 优先项。

---

## 一、头号:被单一 executor 修好的那根棒,又被漏了两轮 —— 缺的是「找得到」

### 1.1 现场(录像组 `20260918T184821Z.md` §2.2 交回的 [harness] 棒 1)

RULING 77(乙)2026-09-18T09:5xZ 把该行的 `executor` 从「录像组 / 批测台**二选一**」
(按 `queue.json:_protocol` 立案句 = **一个都没点**)收成 **「录像组(replay-check),恰好一个。」**。
**换了载体之后,它又被漏了两次,漏它的是本该取它的那一组**,裸读:

```
20260918T094256Z  grep -cE "gh290_od|od_postfix|skill_point_stall" -> 0
20260918T124101Z                                                   -> 0   <- 立案之后第一轮
20260918T154200Z                                                   -> 0   <- 立案之后第二轮
```

⇒ 归因**不是路由**:自检的 owed 腿**每组每轮都打印它**。是**找得到** ——
`73 条 OWED / 其中 29 条属录像组 / 整份日志 646 行`,而 `pending_rulings.py` 里
**没有任何筛选开关**(录像组现读:只有 `:1527-1528` 把 `executor` 打进表头)。
📌 与本仓最常复发的那一族同形:**一条规矩以散文形式存在于它本该约束的地方**。

### 1.2 落地

`tools/agent/pending_rulings.py`:

- `SEAT_PATTERNS` / `executor_head` / `executor_seats` / `ruled_day` / `render_seat_rollcall`;
- **点名表无条件打印**(接在 owed 节末尾)——⭐ **这半是承重的**:一个**要被主动调用**的
  开关对「打印了但没人读」**结构上无话可说**,而那正是本条的缺陷形状;
- `--executor <seat>` 只配 `--owed-only`;单独给时 **exit 2 并说明什么都没筛**,⛔ 不静默。

**实测(本树,裸码 3)**:

```
--- owed by seat (assignment head only; a row can name 2+ seats) ---
  batch-desk    owed  16   newest: aws_run_dryrun_and_watchdog_pricing (ruled 2026-09-18, 0d ago)
  replay-check  owed  27   newest: gh290_od_execution_verification_needs_postfix_corpus (ruled 2026-09-18, 0d ago)
  strategy      owed  10   newest: fieldsip_atom_pricing_corpus_rebaseline (ruled 2026-09-15, 3d ago)
  hero          owed   6   newest: gh_hero56_admission_atom (ruled 2026-09-11, 7d ago)
  director      owed  36   newest: stayfield2_readmit_when_fieldsip_moves (ruled 2026-09-17, 1d ago)
  UNROUTED      owed   8   head names NO seat
  MULTI-SEAT    owed  17   head names 2+ seats
```

⇒ **录像组这一轮要读的不再是 73 行里认 29 条,是一行数字 + 一个 id**,
而那个 id 恰好就是被漏了两轮的那一条。

### 1.3 ⭐ 匹配只读「派工头」,而且**有意过匹配** —— 这一条是全改动的刀口

立案那一行的 `executor` 正文里含 **批测台**、**`director.executor`**、**`hero-20`** ——
三处**全部长在解释「旧的二选一等于没点人」的从句里**。整字段匹配会把
**RULING 77 刚刚消歧的那一行重新塞进 5 个座位里的 4 个**,而且照样打出一张完整、像样的表。
⇒ 只读到第一个 `。`/`⛔`/`⚠`/`⭐`/`📌` 为止;真语料上该行读回 **`('replay-check',)`**,恰好一个。

**过匹配的方向是选的,不是漏的**:`LIMITS` 行把本轮实测的**三例**逐字写进输出 ——
`gh721_…` 从「总监不花 AWS 的钱」吃进 director、`aimguard_creep_schema_identity` 从**否定句**
「不属录像组」吃进 replay-check、`creeps_schema_gh581` 在写着「未指派」时仍吃进两席。
⇒ **87 行里 3 行**。多列一个座位**花一眼**;漏掉真正欠它的那个座位**正是本条要修的缺陷**。

### 1.4 filter 不许改判决

`--executor` 只改**要读的量**,⛔ 不改**算的量**:点名表与**退出码一律按全表算**,
隐藏行数与其中的 MULTI 数**打在筛选行里**,且 **UNROUTED 行对每个座位都打**(fail-open)。
⭐ **理由是失效方向**:「我这一席是干净的」若能印成「这张表是干净的」,就是把本条的缺陷
**往下搬了一层**。钉在 7e / 7f。

### 1.5 变异台(evidence-discipline 1/2/3)

`tools/agent/mutstand_owed_seat_rollcall.sh`,**文件拷贝 + `sha256sum -c` 还原**、退出码**裸读**、
`trap restore EXIT` 在第一次变异**之前**上膛。

```
CONTROL  green (exit 0)
CAUGHT   M1 executor matched over the whole prose field          (suite exit 1)
CAUGHT   M2 queue-id citations read as assignments               (suite exit 1)
CAUGHT   M3 roll-call + exit code computed over the filtered set (suite exit 1)
CAUGHT   M4 DONE rows inflate a seat's owed count                (suite exit 1)
CAUGHT   M5 --executor hides the UNROUTED rows                   (suite exit 1)
STAND caught=5 survived=0 aborted=0                              STAND_EXIT=0
```

`tests/test_pending_rulings.py` INVARIANT 7(+18 检查):**1067 checks, 0 failed**(改前 1049)。

---

## 二、同轮付掉录像组的棒 3:`gh290_…` 的 note 订正

该行 `done_when.note` 原先括注「录像组 09-18T09:42Z 报告逐字记 W46 约 **09-25** 到期」——
按**本句自己的判据**(「取当时还没过期的**最新一份**」)那个 W46 只是**举例**,**不是答案**:
正确语料是 **W69**(发波 2026-09-12,`dem21` 到期 **2026-10-03**,**T+15d**)。
⛔ **差额不是修辞**:按错的日期,这一格会被当成 09-25 前必须抢,而真正 09-26 到期的是
**另一根棒**(`tpreach_bc4_cell_reread`,录像组 §1.4 手读证成)。

同轮写进 note 的还有两样,**都是录像组已经买断、下轮直接抄的**:

1. 第 (i) 项:波次 **W69** / 发波 commit `52ea2be45696c7d547ea00754ece1e32c0090061` /
   `git grep -c CompactSkillList 52ea2be456… -- bots/ability_item_usage_generic.lua` ⇒ **`…lua:2`** /
   **该波有 OD**(seed **13027** dire pos2、**13052** radiant pos2)⇒ ⛔「该波无 OD 局」那条退路对 W69 **不适用**;
2. **浅 clone 警告**:核「是不是修复后」要用 `git grep -c`,⛔ **不要**用 `--is-ancestor` ——
   Routine 容器每轮浅 clone(2 graft / 可达 50 commit),跨 graft 的**否**意思是
   「在已取深度内不可达」,会把 W69 这份合格语料**误判成修复前**。

⛔ **本轮不创建 `gh290_od_postfix_stall_read.md`** —— 第 (ii) 项要 `.dem`,而 S3 对录像组
连续两轮被 harness 权限分类器拒。**该行保持 OWED**,这是有意的:`done_when` 是 `path_exists`,
在 (ii) 缺席时把文件造出来**就是给一条欠条制造满足**。

---

## 三、顺带量到的一条:**入闸成员资格随容器速度漂**,本轮三个测试被挤出去

按规矩(⛔ 手写 manifest 不算)跑 `py_gate_measure.py` 重测,`PGM_EXIT=0`,读回:

```
134 of 154 selected, 53.34s total (per-test cap 3.0s, budget 90.0s)
⚠️  3 test(s) LEFT the hook -- they were gating before this re-measure and are not any more
```

⭐⭐ **而这不是第一次,是同三份文件的第二次翻转 —— 而且是翻回去**。上一轮(09-18T19:00Z)
章程里照实登记过同样三条,方向相反:

| 测试 | 容器 A(~09-17) | 容器 B(上一轮 19:00Z) | **容器 C(本轮)** | cap 3.0s |
|---|---|---|---|---|
| `tests/test_ability_value_key_census.py` | 3.259s | 2.590s(−20.5%) | **3.242s**(+25.2%) | 出 → 入 → **出** |
| `tests/test_call_form_census.py` | 3.136s | 2.430s(−22.5%) | **3.086s**(+27.0%) | 出 → 入 → **出** |
| `tests/test_guard_implication_census.py` | 3.623s | 2.897s(−20.0%) | **3.606s**(+24.5%) | 出 → 入 → **出** |

⇒ **容器 C 的读数与容器 A 逐条相差 <1.5%**,三条**方向一致、幅度一致** ⇒ 漂的是**容器**,
⛔ 不是那三份测试长胖或变瘦。**「在 cap 附近,成员资格取决于哪台容器跑了上一次重测」
不再是推测,是一个已经来回翻了两次的实测事实** —— 上一轮把它挂成『下次触发』⑬ 时
只有单向的一半。

⇒ 入闸数 **137 → 134**。而本轮**唯一改过的**那个测试
(`tests/test_pending_rulings.py`,0.639s → **0.788s**)**在两次测量里都是 `in_gate: true / fast`**,
**一条边界都没跨**。

**处置:回滚整份 manifest(`git checkout`),不提交。** 理由是**作用域**:
一轮没碰过那三份普查的工作单元,不该顺手把它们挤出推送闸;而「按容器速度驱逐」
正是上一轮 §五(1) 点名、**尚未裁**的那个问题(carry ⑬)。⭐ **本轮的价值是它把那条从
推测变成第三个实测读数**,且三条**方向一致、幅度一致(+24.5%~+27.0%)**
⇒ 漂的是**容器**,不是那三份测试长胖了 —— 这正好支持上一轮提的处置 (甲)(**相对基准**)。
⚠️ 旁证:本轮 push 的 Lua 腿实测也比上一轮同样的全集慢(见 §五)。

---

## 四、健康巡检与成本(总监本职)

- **成本**:批测台 18:05Z 逐字 **`actual (MTD): $92.462`** / `operative ceiling: $90.00` /
  `WAVE_FENCE: THROTTLED (exit 3) -- brake, not fence`,**刹车第五十轮持有;零发波、零收割欠、零泄漏**。
  批测台同轮把空转速率从「重叠窗口」重估成「不相交独立区块」,读到 **`$1.0157/天`**,
  于是 **owner 的 `$100` 批准线**最坏落点 **`09-26T17:47Z` → `2026-09-25T23:16Z`**。
  ⛔ **总监不裁这一条**($90 刹车与 $100 批准线是 owner 的地界),**它随 W38 周日那封邮件走**
  (DECISIONS_NEEDED 第 15 条已挂两条状态更新,本轮**不新发信** —— W37 配额 09-13 已用完,
  且 09-20 的 W38 那封**早于** 09-25 的最坏落点)。
- **报告节奏**:`GAP batch-desk 3.8h`(> 3.5h 门槛)。⛔ **本轮不升级**:这是**一次**读数,
  且该台上一轮产出正常(18:05Z 有完整报告);下一轮若仍无新报告 = 连续两格,再点名。
- **`判定完结` 停滞**:8 轮(门槛 12 点名 / 24 写 DECISIONS_NEEDED)⇒ **未到线**。
  ⚠️ 但归因要写清:条件 (b) 需要批测,而**刹车已持有五十轮** ⇒ 这 8 轮的停滞
  **不是没人做判定,是判定所需的那一半买不到**。
- **patch**:本轮未查(低频项,上次检查后无新 datafeed 触发条件)。
- ⭐⭐ **本轮新读到、且有硬日期的一条(已写进 `iterations/DECISIONS_NEEDED.md`,随 W38 周日那封走)**:
  录像组 21:58Z 开的 **GH #909** —— `tools/batch_test/aws/session_setup.sh`(**唯一**写
  `~/.aws/credentials` 与 `awsx` 的路径)在录像组容器里**连续两轮被 harness 权限分类器拒**,
  且**理由串换了一次**(`[Real-World Transactions]` → `[Interfere With Workloads]`)。
  ⭐ **总监本轮补的判别子**:**同一个脚本在批测台 18:05Z 那轮跑成了**(`AWS_SETUP_EXIT=0`)
  ⇒ **分类器的判决与会话相关,不与脚本相关** —— 问题不是「脚本坏了」,是
  **同一条命令在不同座位上答案不同**。
  代价有日期:`tpreach_bc4_cell_reread` 的 88 个种子槽 **09-27 起为 0**,而买它只需下载已存在的
  `.dem`(⛔ **不需要发波,刹车不挡它**)⇒ **挡着它的是权限不是钱**。
  **总监建议 C(批测台代取语料,零 EC2)并行准备 A(09-26 前按「语料不可得」重述验收线)**;
  ⛔ 本轮不自行派 C 给批测台(它持刹车第五十轮,派活要先确认不算花钱工作),随 09-20 那封确认 —— **09-20 早于 09-26,窗口够**。

---

## 五、闸(三条腿,裸码,⛔ 未经管道;顺序照 RULING 69 先分支后 main)

**① 分支推** `PUSH1_EXIT=0`(`* [new branch] claude/compassionate-albattani-74uwqb`):

```
luacheck bots game: 0 warnings
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 138 ran, 0 findings, 0 uncertifiable, 62.8s
lua gate: scope = 4 path(s) changed vs the pushed refs' base (base=fallback-merge-base)
lua gate: 432 ran, 0 findings, 0 uncertifiable, 10 unanswered, 5 known-red, 776.9s
```

**② main 推** `PUSH2_EXIT=0`(`6510ac01..39d90abe  HEAD -> main`):

```
RULE6_MEMO=REUSE  a green three-leg reading taken 2026-09-18T22:27:32Z on THIS EXACT tree.
GATE_EXIT=0  CLEAN / py gate: 138 ran, 0 findings / lua gate: 432 ran, 0 findings, 776.9s
```

⭐ **RULING 69 的顺序本轮兑现了它自己的理由**:第二推**命中 memo**,776.9s 的 Lua 全集
**只付了一次**。⛔ 未用 `RULE6_BYPASS`;`PULL_EXIT=0`(rebase 到 `6510ac01`)。

⚠️ **Lua 腿跑了全集而不是 SKIPPED BY SCOPE,这是对的不是浪费**:本轮改动含
`tests/test_pending_rulings.py`,而 `lua_gate.py:IN_SCOPE_PREFIXES` 含 `tests/` ⇒ 判 in-scope。
`base=fallback-merge-base` 是新分支一次性的代价。

⚠️ **同一次 push 的 manifest 差额,只交读数不代修**(归 GH #806 / #804,已追评 GH #839):
`89 new test(s) not in the manifest were run anyway, costing 411.87s` /
`the hook's REAL cost this run is 776.89s against a 540.0s budget` /
`10 new test(s) exceeded the hook budget and were EXCLUDED`。

---

## 六、本轮改了什么

| 文件 | 改动 |
|---|---|
| `tools/agent/pending_rulings.py` | `SEAT_PATTERNS` / `executor_head` / `executor_seats` / `ruled_day` / `render_seat_rollcall` / `--executor` |
| `tests/test_pending_rulings.py` | **INVARIANT 7**(+18 检查,7a–7h) |
| `tools/agent/mutstand_owed_seat_rollcall.sh` | **新**:5 变异 + CONTROL |
| `iterations/owed_executions.json` | `gh290_…` 的 `done_when.note` 订正(棒 3) |
| `iterations/streams/director.md` | 「当前状态」新条目 |
| `iterations/reports/director/<本文件>` | 本报告 |

⛔ `bots/` + `game/` **零 diff**;⛔ `py_gate_manifest.json` **有意不提交**(§三)。

---

## 七、⑨ 下次触发

①**GH #856** 剩 9 候选 ②**GH #867** ③**GH #240** 余下 ④`carry_mark_prose_vs_list` 剩唯一一格
⑤**GH #843** 剩 (乙) ⑥**GH #859** ⑦**GH #810** 待裁 1 + (乙) ⑧**GH #528**
⑨**GH #548** / **GH #806**(#806 是 Lua 腿上的同形;RULING 81 是 py 侧先例,可照搬)
⑩**GH #839** 本轮已追评(cap 漂的第三、第四个读数);**下一步是选 (甲) 还是 (乙),要先量爆炸半径**
⑪**新:GH #910**(本轮开)—— owed 表的 `corpus_deadline`,executor 写死总监自己
⑫**新:GH #908**(录像组 21:58Z 开,[bug])—— `pullthink_animactivity_instrument` 的价签与**三选一请裁**,⭐ **点名要总监裁**,下一轮优先
⑬**新:GH #909**(录像组 21:58Z 开,[harness])—— S3 连续两轮被 harness 权限分类器拒
(逐字 `Permission for this action was denied by the Claude Code auto mode classifier. Reason: [Real-World Transactions]`)
+ `tpreach` **09-26 硬悬崖**。⭐⭐ **这一条有日期**:`tpreach` 的 88 个种子槽 09-27 起永久为 0,
而买它要 `.dem` ⇒ **S3 通不了 = 那条杠杆的 (a) 永久买不到**。⛔ 不是钱的问题(刹车不挡它),是**通路**的问题
⑭核 `aws_run_dryrun_and_watchdog_pricing`(executor = 批测台)—— ⛔ 刹车持有中不构成豁免
⑮在一个**不跑开工自检**的容器上复跑 `python3 tests/test_rc_wrapper.py`(前提 `command -v lua5.1` 为空),
消掉 GH #899 验收残余的亚秒并发窗口(⛔ 故意不写那个号:它已 closed,抄进清单正是 §1.5 `STALE-CARRY` 要拦的)
⑯**报告节奏**:若 batch-desk 下一轮仍无新报告(本轮 `GAP 3.8h`),= 连续两格,**点名并写进报告红色段**
⑰**W38 周日邮件(09-20)**:DECISIONS_NEEDED 第 15 条两条状态更新 + 批测台 09-18T18:05Z 的
`$100` 最坏落点提前到 **2026-09-25T23:16Z**;⛔ 本轮不发信(W37 配额 09-13 已用完)

---

## 八、发表(⛔ 全部在两次 push 之后)

两份草稿各跑 `bash tools/agent/claim_precheck.sh`,**均裸码 0**,逐字
`local commits not on origin/main: 0` / `refused 0` / `OK to publish: every citation resolves on origin/main.`

- **GH #910**(本轮**新开**,[harness])—— owed 表的 `corpus_deadline`(录像组棒 2);
  executor 写死**总监自己**。⚠️ `search_issues` 本轮未跑 ⇒ 正文里**写明了若已有请合并**,
  ⛔ 不声称同族 issue 不存在(`owed_executions.json:github_read_staleness_makes_negative_existence_claims_unsound`)。
- **`issues/839#issuecomment-5736964079`** —— cap 漂的第三、第四个读数,**不关号**。

⇒ **issue 净增 1、评论 1**。

⚠️ **本报告自己跑 `claim_precheck.sh` 读回 `PRECHECK_EXIT=3`,照写,而它不是错**:
唯一的 finding 逐字是 `OFF-TRUNK commit 52ea2be45696c7d547ea00754ece1e32c0090061` /
`resolves on origin/claude/admiring-hawking-04r07p`。那是 **W69 的 `ref_pin`** ——
**发波 commit 本来就是会话分支的 tip,不在 main 上**(录像组 18:48Z §2.3 引的是同一个 SHA)。
⛔ **不因此删掉引用**:它解析得到、指向正确,`claim_precheck.sh` 报的是「问题不是判决」
(与 `unlanded_commits.py` 的 OFF-TRUNK 同一族措辞)。**两条已发表的评论草稿各自裸码 0**,
本条只影响这份**仓内**报告。

---

## 九、Token 用量(铁律 8)

```
TOKENS total_in=19,735,960 out=75,069 turns=112
```

⚠️ 这是**到统计时刻为止**的数字,统计后的收尾回合不计入。
按 §7b 的定论,常设指标读 `out/报告`:本轮 **75,069 out / 1 份报告**。
