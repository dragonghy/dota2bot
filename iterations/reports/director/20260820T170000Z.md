# director 2026-08-20T17:00Z — `[harness] #61` landing:loader 拒答 lane front,#62 前置解除

## 摘要

- **优先级 2(a)** 空(49 条 open issue,无新增 `[bug]`)⇒ 走章程 backlog:**#61(harness)已顺延三轮,必须做**。
- **本轮工作单元**:GH #61 按 issue 自己列的建议 2("loader 让 GetLaneFrontLocation 抛错")landing,GH #62(defnum)的前置阻断顺带解除。
- **未花 AWS 钱、未做任何 AWS 调用、未启动计费资源。**
- **Lua 改动 7 个文件**:1 个 loader、6 个测试(把隐式 (0,0,0) 改成显式声明)。
- **验收**:`luacheck bots game --formatter plain` **0 warnings**,`lua5.1 tests/run_tests.lua` **830 / 830 green**(容器初始无 Lua 工具链,已 apt 装);**变异测试**:把 loader 的 raise 换回静默 `Vector(0,0,0)` **恰好挂** `test_defnum_recent_hit_parity :: [limitation]`(loader refusal 契约本身被这条 case 钉住)。
- **测试集逐字未变**(17 eligible / §R.0 的 16 id 下一波 cand),`DECISIONS_NEEDED.md` 未新增,本周 owner 邮件仍为零。

## 1. 为什么这一轮是 #61

- **优先级 (a) 空**:49 条 open,新增归总监的 0 条 `[bug]`/`[harness]`(11:00Z 之后 open 的 #66/#67/#68/#69/#70/#71 分别归 hero/strategy/strategy/harness(#69 已处置)/batch/strategy,#69 我 14:50Z 那轮已裁完回读范围)。
- **优先级 (b) promote/reject 无新输入**:13:00Z 已把 `zusult`/`cmrguard` 都判 NOT-PROMOTE(理由不同,见 §R.1/§R.2);16:11Z 那波 arm A 12:08Z 收全、arm B 16:08Z 才刚上机,verdict 属批测台下轮领域,不强等。
- **优先级 (c) 测试集变更**:16:08Z 批测台的 arm A 拓扑照 §R.0 逐字执行、无新变更请求。
- **优先级 (d) 成本**:MTD $11.6958 未变,batch-desk 16:08Z 明确"$45 月度围栏与本波共 $13.3,数量级余量",无异常。
- **优先级 (e/e2) 健康度/效率台账**:五组均有产出无空转;今日周四,效率台账(周日)跳过,08-23 那次做。
- **优先级 (f) patch**:11:10Z 已分片降级,不重复。

⇒ 章程 backlog **#61 顺延三轮**(11:10Z 立、13:00Z / 14:50Z 各"若空则没有借口",两轮又都被 issue 挤掉),本轮 (a) 真的空 —— **今天没有借口**。做。

## 2. 修法选择:issue 建议 2(refuse),不是建议 1(reconstruct)

Issue #61 自己列了两条:

- **建议 1**:从 replay 重建 per-team per-lane 前线,dumper 补路别 + 采样密度,loader 只答 `frontOffset==0` 且 `≠0` 显式拒答。
- **建议 2**:loader 直接对 `GetLaneFrontLocation` 抛错,逼每个测试自己声明。

**选建议 2**,两条硬理由:

1. **§0b / #0b 已经立过一条线**:「dumper 每 3 s 一次采样、不带路别、不带存活位 —— 前线是 MODEL 不是 restoration,同 §0b 里三次栽的『世界本身是退化的』一族。」issue 自己也写了这一点。走建议 1 = 把这道线跨了。
2. **本轮可以低成本走建议 2 而不改任何已下的 conclusion**:6 个受影响测试的 assertion 要么**机制上不读前线**(defclose/defstale/tpwatch),要么**已经带 mutation probe 证明 flip 不吃前线值**(retnear/towerreach)。改成显式声明 (0,0,0) = pre-#61 世界的 EXPLICIT continuation,mechanism 一字不动,结论一字不变。

## 3. Landing 内容

### 3.1 Loader(`tests/mock/replay_fixture.lua`)

在 `api.install()` 之后装 raise-override:

```lua
_G.GetLaneFrontLocation = function()
    error(
        'LOADER REFUSES: GetLaneFrontLocation is unresolved (GH #61). '
        .. 'The dump does not carry lane fronts; do not compare against '
        .. '(0,0,0). Declare your assumption in the test with '
        .. '`GetLaneFrontLocation = function(_, lane) return Vector(...) end`.',
        2)
end
```

**Bare-mock 用户不受影响**(直接 `require('mock.bot_api')` 且不走 `rf.load` 的测试仍拿 (0,0,0) stub)。

### 3.2 6 个隐式压在原点上的测试(逐个说清为什么 origin 声明合法)

- **`test_defclose_defender_arbitration`**:arbitration 走 `#J.GetLastSeenEnemiesNearLoc(tower_loc, 1600)`,**根本不读前线** ⇒ origin 声明是 tautological。
- **`test_defstale_defend_bail`**:DEFECT 是 `ds.enemyDamageOn` 相对**自己的写入**是 stale 的,与前线值正交 ⇒ origin 声明合法。
- **`test_tpwatch_channel_bid`**:tpwatch 是退却出价链的守护,退却链不读前线;driver 里那些也需要驱动的 mode 文件是 auction 层的副产物 ⇒ origin 声明合法。
- **`test_retnear_radius_parity`**:已有 `opts.lane_front` MUTATION PROBE(3 个非原点值上跑一遍,flip 一致)⇒ 结论 stub-independent 已被证过,default (0,0,0) 只是提供一个可运行世界。
- **`test_towerreach_out_of_range_veto`**:同 retnear。
- **`test_defnum_recent_hit_parity`**:`[limitation]` case **扩写**成双端护栏:先 `rf.load(POKED)` 后**在无声明世界断言 loader 真的抛错**(拆掉 loader 的 raise 会当场挂),再在 `world()` 声明后断言 shape(ds.defendLoc = 原点 / distanceToLane 三路同数)—— **测试成了 loader refusal 契约的守门人**。

### 3.3 变异测试(证明护栏活着)

```bash
# 把 loader 的 raise 换成静默 (0,0,0) stub
lua5.1 tests/run_tests.lua test_defnum 2>&1 | tail -8
# 输出:
# .........F.
# FAIL: test_defnum_recent_hit_parity.lua :: [limitation] this file declares
#   lane fronts at the map origin -- the auction rides on that choice
#       tests/test_defnum_recent_hit_parity.lua:385:
#       rf.load must REFUSE GetLaneFrontLocation by default (GH #61).
#       Got: true / nil
# 11 tests, 1 failures, 105 files skipped by filter
```

**报错文本直指契约**,不是模糊 assertion 失败 —— 未来任何试图放宽 loader 的 change 立刻挂,且看得到原因。

## 4. GH #62(defnum)前置解除,已评论明路

Strategy 组下一轮拿到解锁通路:

1. 在 `test_defnum_recent_hit_parity.lua` 的 `world()` 里加 lane-front 声明(pattern 直接抄 retnear/towerreach 的 `opts.lane_front` 覆盖式)。
2. 加 **`Defend.GetDefendDesire(bot, lane)` 最终出价断言**(§8 mandatory)。
3. 配一条 **mutation probe**(3 个非原点值,断言 flip 一致或说清"哪一半吃前线值")。
4. 完成后申请入集,§O.3 走。

**issue 保持 open,由 strategy 关闭** —— 它现在是 strategy 的活。

## 5. §0b 第十八例(新亚型,通例)

**「隐式默认(implicit default)让 stub 悄悄成为 assertion 的一部分」**。前十七例是"作者/下游/消费方作废 helper 的推理"或"测量侧看不见抑制的效果";这一例是**世界给了一个默认值,谁都没主动选它,但测试都在压它** —— 一旦某天有人"修好"这个默认(不知情地),或"稍微改一改",一批 conclusion 沉默地失效。

**推论**(已写进 test_set.md §T,全组读):**任何用 `rf.load` 的测试触到 lane-front 读者,必须自己声明假设(三种模式择一,含 MUTATION PROBE 作为 stub-independent 证据)**。

这一条与本周三次可测性事故(#28/#34/#37)是同一族的下一层 —— 前面三次是"消费方作废 helper 的推理",这一次是"世界的沉默默认作废了测试的独立性主张"。

## 6. 未来工作(不阻塞,记 backlog)

- **同族 stub 尚未处理**:`GetLaneFrontAmount` / `GetLocationAlongLane` 也是静默返回。总监探过:让它们抛错只挂 1 个 pre-existing 测试(`test_replay_071903_sven_idle`)。**本轮不做**是为保持工作单元小 + 没有 armed id 的判读依赖它俩;下轮无 `[bug]`/`[harness]` 抢占再一并处理。
- **真要 reconstruct**(建议 1 的完整版):需要 (i) dumper 输出按路别、按存活的己方最前沿小兵位置(GH #27 家族),(ii) loader 只答 `frontOffset==0` 且 `≠0` 显式拒答,不猜路径点。这是 fixture-schema 升级,归入 backlog #3(执行核验标准化)之后再讨论。

## 7. 章程项账

- **测试集**:逐字未变(17 eligible / §R.0 的 16 id 下一波 cand,种子 888/895/896/906)。
- **promote/reject**:本轮无新判定(13:00Z 已把 `zusult`/`cmrguard` 都判 NOT-PROMOTE)。待复核队列 `#44`/`#45`/`#55`/`#58`/`#62`(**本轮解锁**)/`#63`/`#65`/`#66`/`#67`,`#45` 第八次顺延 —— 有时限的活优先。
- **成本**:MTD **$11.6958**(批测台 16:08Z 自报,与 14:07Z 逐位一致),在跑 4 台是有意的 §R.0 arm A on-demand(2h 看门狗,最迟 18:09Z 自毁),无泄漏。**总监本轮 0 AWS 调用**。
- **健康度**:五组均有产出无空转;批测台 16:08Z 主动 log "形式差 3-4 分钟"是正确的自我披露(按 06:07Z 先例照常启动,可追踪)。
- **效率台账**:今日周四(仅周日,08-23 那次做),跳过。
- **patch**:11:10Z 已分片降级,不重复。
- **`wandlimbo` 机会普查**:连续第九轮无人做(§J.1.4 硬前置,阻塞 `wandlimbo` 入集)。
- **`[harness] #61`**:本轮 CLOSED(顺延三轮后的兑现)。
- **`DECISIONS_NEEDED.md`**:今日不新增(本轮决定全在自主授权内 —— 建议 2 vs 1 是 §0b 已立线的直接推论)。本周 owner 邮件仍为零。

## 8. 下次触发优先级

1. **收 16:11Z 波次的 `capmono` (b) 两臂差**(按 §R.3 推论 ② 的口径:按种子配对 + 按局归一化,不许用 gpm/xpm)。
2. **清待复核队列**(`#45` 第九次顺延如再无 `[bug]`/`[harness]`;`#44`/`#55`/`#58`/`#63`/`#65`/`#66`/`#67` 按 age)。
3. **`#66`/`#63` 修订版落地后按 §R.2 复判 `cmrguard`**(零支出)。
4. 都在等就做 **backlog #1 帧语料检索工具**(owner 点名;本周多次可测性事故说明收益在涨)/ patch 分片 P3 / `[harness] #27` / `#43`(赛后 24-25 s 冻结未过滤,可能污染窗口检测器)/ 收拾 `GetLaneFrontAmount` / `GetLocationAlongLane` 同族 stub。

## Token usage

```
TOKENS total_in=16,672,866 out=100,816 turns=121
```
(到统计时刻为止;统计后的收尾回合 —— commit / push / 本节 —— 未计入。)
