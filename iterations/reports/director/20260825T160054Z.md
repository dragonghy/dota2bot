# 总监 2026-08-25T16:00:54Z —— 清掉 §BF.4 故意留红的两条,**其中一条的答案已经躺了 26 小时**

**一句话**:上一轮把 `hero-5` 故意留红,理由写的是「读数已经欠着了」;
**逐条查下去发现留红是对的、理由是反的** —— 读数不是欠着,是**已经交付了 26 小时**,
只是没有一个字段知道。连同 `hero-6` 放行与 `campfarm` 入集,三条裁定全部投递,
`pending_rulings.py` **3 → 0,exit 0**。

**成员串 35 → 36**(`campfarm`)。**零 AWS 调用、零支出、未批新支出。**
档案:`test_set.md` **§BG**(§BG.1/.2/.3/.4)。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` —— worst exit 3(有发现,非失败)。

| 腿 | 读数 | 处理 |
|---|---|---|
| UNLANDED | **2 条**(`fc79986` / `08ed7c2`,均在 `origin/claude/compassionate-albattani-4zcohy`) | 上上轮已判定的**永久假阳**(同一份工作已改头换面落地),未再抢救 |
| CADENCE | 9 个洞(batch-desk 6.1h/8.9h、hero 6.1h、replay-check 5.8h/3.8h/3.8h、strategy 3.5h、director 3.6h/4.2h) | 见 §3 体系健康 |
| 待裁队列 | **RIDESHARE 2 + OTHER 1** | **本轮全部裁完 ⇒ 0** |
| python 套件 | **26 passed, 0 failed** | ✅ |
| Lua 快检测器 | SKIP(容器无 lua5.1) | 本轮 `bots/`/`game/` **零改动**,见 §5 |

---

## 1. ⭐⭐ `hero-5`:**§BA.4 的第三种形态 —— 不是裁定没送达,是答案没回填**

### 1.1 现场

上一轮 §BF.4 把 `hero-5` **故意留红**,写的理由是:
> 它是一份**读数请求**绑在 `hero-8` 的 `cmboots` 波上,而 `hero-8` 已 harvested ⇒ **读数已经欠着了**。

**留红这个动作是对的(不给空裁定糊绿检测器,§BF.5 立得好),给的理由是反的。**

| 时刻 | 发生了什么 | 哪个字段记录了 |
|---|---|---|
| 08-23 | `hero-5` 写下「Re-pointed at hero-8's wave. **Left pending**」 | `result` —— **写于它所指向的那一波之前** |
| 08-24T03:10:28Z | W5 发波,`cmboots` 独占 armed | `hero-8.result` |
| 08-24T12:17Z | 批测台收割:273 有效局 + 24 暖场,`.dem` **233 份**,§6 逐 run 对账**零差** | 批测台 `20260824T121706Z.md` |
| **08-24T13:00Z** | **录像组把 `hero-5` 的三问逐条读完**,写进报告 + `replay-check.md` + GH #144/#100/#156 | 录像组 `20260824T130015Z.md` |
| 08-25T13:xxZ | 总监读到 `status=pending`,判「读数已经欠着」 | — |
| **08-25T16:0xZ** | **总监回填** | `hero-5.director`/`status`/`result` |

### 1.2 三问的答案(录像组读的,总监核过边界后采信)

- **(1) 阻断项 = WORKING** —— armed 队 CM **106/106 持过 arcane**,两层同号
  (radiant **0/60**、dire **0/46** 持过 tranquil);承重帧 `1d9ae5/20260824_031904_slot7`
  **t=428.5**,同帧吃掉 `boots_of_speed + ring_of_basilius + wizard_hat`
  ⇒ 本条自己点名的头号失败模式(「新宏 `item_mage_arcane_outfit` 解析不了」)**排除**。
- **(2) 缺陷检查 = 干净** —— armed 腿 **0/106 持过第二双**(tranquil 0、`boots_of_bearing` 0)
  ⇒ Bearing 剔除没有从反面被推翻。
  ⚠️ 非 armed 腿那 **1/103** 局漏成 arcane 是 **GH #156**(对照腿污染),**不是本条的缺陷,别混**。
- **(3) 不触发 UNDERPOWERED 分支** —— 五行两层同号:自放 Replenish **+2.355**/局、收到 **+1.500**、
  技能施放 **+2.04**、存活帧 `mp<100` **−3.5pp**、平均 `mp_pct` **+5.9pp**;SILENT 率 **2/106**;
  阴性对照(**队友**放的次数)平衡 **−0.38 且两层反号** ⇒ 装置无整体漂移。

### 1.3 ⭐ 总监独立核过通道边界(这一步是**必须**的,不是加分)

本条验收**预登记**了:「Replenish 为 0 必须记 UNDERPOWERED,**除非能证明 dumper 根本发得出 item action**」。
所以采信那五行之前必须先答这个。**核的是源码不是转述**:

- `tools/batch_test/behavioral/dumper/main.go:90` 的 `Items []string` 是**快照通道**
  (1Hz 的 `m_hItems` 栏位状态,只答「持有什么」)—— **它确实答不了施放**。
- 录像组用的是**第二条通道**:`.dem` 的 `events[]`(`type=='ITEM'` / `MODIFIER_ADD`,
  `inflictor='item_arcane_boots'`)。

⇒ **GH #100 的「物品层完全不可观测」在结果侧过宽**(录像组已追评),
本条的 UNDERPOWERED 分支**不触发**,那五行是真读数。
(注:`mp<100` 那一行**本身**仍是严格下界 —— 离线没有 per-ability 蓝耗表,
100 低于 CM 任何等级的任何一个技能 ⇒ 真实的「有就绪技能却付不起」只会更高。**当地板读,不当比率读。**)

### 1.4 ⭐ 判据(比读数本身值钱)

**一张请求单的 `status` 是机器读的那一行,而它记录的是「有没有人回来改过它」,
不是「这件事有没有做完」。**

这一次**没有任何一方做错事** —— 录像组交付了、批测台收割了、GH 追评了、章程写了 ——
**只是没有一个字段的名字叫「谁问的」**。

前两种 §BA.4 的现场都有人**在写裁定**(08-19 落错行、08-23 落错字段);
**这一种连一个该写的人都没有**,所以它比前两种更静默:
`pending_rulings.py` 看得见「没裁定」,**看不见「已经答了」** ——
对它而言「答案不存在」与「答案存在但没回填」**逐位相同**。

⇒ **连带请求(下次触发)**:给 `queue.json:_protocol` 增补一条**反向指针**
(请求单记下「谁的报告答了我」),让「答案已到、单子没动」能变红。

### 1.5 字段变更(三处,全部有意,已逐条说明)

`director` 新增;`status` `pending`→`done`(协议允许值;本条从来不是波次申请,
故**不进** `done` 列表 —— 那是批测台对波次条目的机制);`result` **追加**一行日期指针。
**`question` 散文未触碰**(§AW.1)。逐字段 diff 断言见 §5。

---

## 2. `hero-6` 放行,并**给它加一个它自己没有的对照**

`APPROVED_READ_OFF_W7`,交**录像组**(帧读 —— 无波次可发,不是批测台),**零 AWS、零新波次**。

载体 **W7**(`spot_20260824_1812*`,钉树 `03fb378…`,池化 302 局 / 278 计分 / `.dem` **235 份**),
钉树**含** GH #136 的修复 `8426438` 且该改动 **UNGATED ⇒ 两条腿都带着它** ⇒ 读的是**已出厂的构建**,单腿即可。
**铁律 4(i) 的 ab/ba 强制项在这里不适用,理由是没有腿差可读,不是可以豁免** ——
两件事必须分开写,否则下次会被引用成豁免先例。载体门:skeleton_king 在 **s895**(dire pos1)与 **s906**(radiant pos3)。

### 2.1 ⭐ 核过前提之后,SILENT 的含义变了

`item_double_branches` **不是新宏**:`bots/FunLib/aba_item.lua:897` 把它解析成
`{item_branches, item_branches}`,而 `bots/BotLib/` 下**约三十个英雄文件在本次修复之前就已经引用它**
(venomancer / ember_spirit / pudge / spectre / juggernaut / phantom_assassin / tinker /
witch_doctor / riki / centaur / hoodwink / abyssal_underlord …)。
⇒ 申请单把 SILENT 解释成「买表不是真正驱动他购买的东西」,**在这个前提下是低估的**。

**故预登记一条对照,且必须先于主读**(事后再加就买不到东西了)——
同一份 W7 语料,对**任意一个修复前买表就已写着 `item_double_branches` 的英雄**
跑同一支「同时最多几根 `ironwood_branch`」直方图:

| 对照 | WK | 结论 |
|---|---|---|
| **2 根** | **{1: N}** | 宏与买表通路都好,问题**是 WK 特有的** ⇒ 按验收退回英雄组重开诊断 |
| **{1: N}** | {1: N} | 失败**不是 WK 的**,是采购层对**几十个英雄**同时失效 ⇒ 就是 GH #136 里单独立案的通用缺陷 ⇒ **升级为 [bug] 交总监**,不是让英雄组重开 WK |
| 2 根 | 2 根 | (1) = WORKING,直接走 (2)(3)(4) |

⇒ **无论哪一支,「WK 读到 1」都不再能被单独解释成任何一件事。这正是预登记要买的东西。**

**通道已核**:`dumper/main.go:90` 每帧发 9 个随身栏位的真实解析 ⇒ (1)(2) **都是快照通道直接可答的状态量**,
本条**不存在** §1.3 那种 UNDERPOWERED 分支。

### 2.2 它为什么会挂一天

`priority=1`、`status=harvested`、语料 08-24T18:12Z 就到了,
而录像组最近**五份**报告里**一次都没出现** skeleton_king 的树枝/魔棒读数
(只反复出现「打野反证 fixture 已顺延十七轮」那条**不相干**的顺延)。
**与 §1 同病:没人做错事,只是没有一个字段在为它举手。**

---

## 3. `campfarm` 入集 + 体系健康

### 3.1 `campfarm`(`strategy-17`,GH #137 §3 建议 2)

`APPROVED_ADMITTED`,成员串 **35 → 36**,搭**界后第一波**,**零 AWS 增量**。
**§BB.4 立法以来第一条零逾期的提议**(14:xxZ 到达,16:0xZ 裁完)——
上一轮 §BF.1 记的是 6/6 全逾期,本轮 1/1 零逾期,**`pending_rulings.py` 上线后的第一条就没掉**。

- **门(§BA.2 冻结风险)**:`mode_farm_generic.lua:78` = `IsModeTurbo() and IsSoakCandidate('campfarm')`
  ⇒ **单一合取项、不指名任何其它 id ⇒ 无冻结风险**。
- **常数**:`aba_site.lua:427` `ANCIENT_MIN_LEVEL = 12` ⇒ 提议里「三比一错位」的 12 **源码对得上**。
- **排波前置**:`campgrade` **不在**串里 ⇒ 界后第一波能干净读到;
  `campgrade` 将来入集时**两者不得同腿 arm`。已写进 `campgrade` 未来入集裁定的前置检查。
- **总监自加三条约束**(申请单没有):**(甲)** 反向护栏是**硬条件**——远古交火总数塌成 0 则主判据达成也不算通过
  (40 局 40 次里 **28 次在 12+**,那是**被保护人口不是噪声**);**(乙)** 域的下界收割时必须报真实局计数,
  否则「读数不动」分不清**修法无效**还是**域太小**;**(丙)** 野怪那一半**语料里没有**,提议**诚实标成
  「声明的替身」而不是假装端到端** ⇒ **收割前不许把本地绿(16 例 + 11 变异全抓)读成已验证**。

### 3.2 体系健康

五组本轮**均有产出**。cadence 9 个洞里最大的两个都在 batch-desk(6.1h / 8.9h),
与上一轮记的「两个批测台会话并发发同一波(GH #180)」是同一段时间 —— **不另开条目,并入 #180 观察**。
`director` 自己两个洞(3.6h / 4.2h)如实登记。
**owner 优先项**:本轮未新增零推进项;`cmboots` 悬空(§4)是本轮新发现的、需要总监自己动手的一条。

---

## 4. ⭐ 本轮发现、**没有**在本轮解决的两条(明确交棒,不写进「下次触发」就算了)

1. **`cmboots` 测过、通过、然后悬空。** (a) WORKING(§1.2)+ (b) 批测台 12:17Z 判过
   (3 种子口径 gpm **+7.03**、wr **0.497**,而同波结构性零腿 |−5.41| gpm **同量级**
   ⇒ 买到的是「**没有明显负面**」,买不到「它让经济变好」),
   **而它既不在 36 成员串里、也没 promote**。(c) 与 promote 归总监。
   resolve 的账本必须带录像组 §5 的**代价**:鞋子完成中位 **+70~85s**(394–420s vs 313–344s)、
   `ancient_janggo` 完成率 **−21.9pp**(两层同号 −17/−27)⇒ **买到的是蓝,卖掉的是一件装备的进度**;
   **死亡数两层反号,按铁律 4(i) 不读**。
   ⚠️ **这类「三条件已齐却无归宿」的 id,当前没有任何检测器看得见** —— 登记为
   `pending_rulings.py` 的候选新不变式,与 §1.4 的反向指针一起做。
2. **行号是会漂的引用,id 不会。** `campfarm` 的插入把 `tbearly` 的站点从 **493 推到 509**
   ⇒ §BF 记的 493 从本轮起是旧值。**同一个病第二次**(08-24T22:3xZ `tpreach` 注释块行号漂移,
   靠一个 followup commit 补的)。已写进 §BG.4:**排波与核对一律以 `check_armed_wiring.py`
   当轮实跑的输出为准,不引用历史报告里的行号。**
3. **「待总监裁定的入集提议」这个小节标题从本轮起是假的** —— 下面已一条待裁都没有,
   却还挂着「待裁定」,**读它的人会以为全是待办**。已改名「入集提议档案」+ 每条自带裁定行 +
   写明**待裁真实清单去跑 `pending_rulings.py`**。**散文不举手,而这次散文还举错了手。**

---

## 5. 成本与验证

**成本**:总监**零 AWS 调用、零支出、未批新支出**。有效 MTD **$40.79**($45 围栏余 **$4.21**),
三线(围栏 $45 / 刹车 $90 / owner 批准 $100)**均未触及**。`campfarm` 搭界后第一波,**零增量**。

**验证**:
- `bots/` / `game/` **零改动** ⇒ 铁律 6 的 Lua 门不适用(容器无 lua5.1,已如实登记;全量套件 GH #124 本就跑不完)。
- `check_armed_wiring.py --cand <36 串>` = **36/36 WIRED,exit 0**;`campfarm` direct、1 站点
  (`mode_farm_generic.lua:78`)。裸 cand 串**实测 320 字节**(35 id 时 311;S3 1024 仍有余量,
  ext4 255 早在 29 id 时跨过 GH #167 —— **本轮没让它更坏**)。
- `queue.json` **逐字段 diff 断言**:顶层键 / `_protocol` / `done` / 请求集合与顺序**全等**;
  变更**恰好** `hero-5.{director,status,result}` + `hero-6.director` + `strategy-17.director`,
  **UNEXPECTED FIELD CHANGES: NONE**;三条 `question` 逐字未动(§AW.1);`result` **append-only**。
- `pending_rulings.py`:裁定前 **3**(RIDESHARE 2 + OTHER 1)→ 裁定后 **0,exit 0**。
- 自检 python 腿 **26 passed, 0 failed**。
- 头部成员串:**36 id、无重复、320 字节**,`campfarm` 在内。

**诚实边界**:§1.2 的读数是**采信录像组的测量**,总监**核的是它的通道边界与推理**(§1.3),
**没有重跑那 233 份 `.dem`**;若日后重算与之不符,以重算为准。

**裁定投递(章程 2.5 三处齐)**:(i) `queue.json` 的 `director` 机器字段 ✅
(ii) `test_set.md` §BG 全文档案 ✅ (iii) 活 issue 线程追评 ✅
—— GH #144(`hero-5`)、#136(`hero-6`)、#137(`campfarm`)。

---

## 6. 下次触发

1. ⭐ **`cmboots` promote/reject 三条件判定**(a✅ b✅,**c 待裁**)—— 它现在**测过、通过、悬空**;
   连带 `state.json` / `stable-vN` tag / 成员串归宿。**账本带 §4.1 的代价。**
2. **`_protocol` 增补 `answers` 反向指针** + `pending_rulings.py` 新不变式
   (「三条件已齐却无归宿的 id」)—— §1.4 与 §4.1 是同一个洞的两端。
3. `[bug] #156` / `[harness] #161/#152/#169`。
4. **`stable-v1`/`stable-v2` 打 tag(第八轮积压)**。
5. backlog §6b「内容侧复核升级为惯例」+ UNLANDED 行并排自述状态。
6. **W35 周日(08-30)邮件** —— `DECISIONS_NEEDED` 第 **9b** 条(#158 的两条**反向**更新)是本周重点。

---

## 7. 本次会话 token 用量

```
TOKENS total_in=3,731,430 out=36,277 turns=34
```
