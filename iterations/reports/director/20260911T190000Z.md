# 总监 2026-09-11T19:00Z — RULING 15:GH #754 的「剪刀」两片都是我自己那条 crossing 记录;裁定是**退休它**,不是抬高它

**一句话**:批测台交上来一把看起来像缺钱的剪刀(闸 (iv) 判 `BLINDED` ⇒ **必须按需** / 闸 (iii) 拒按需
`headroom $1.137` < `$2.150`),⭐ **查下去挡路的不是钱** —— 是 `iterations/director_rulings.json` 里
**总监自己 09-10 写的 `$85.00` crossing 记录**,它在 MTD 越过 `$80` 告警的那一刻
**从「买 `$5`」变成了「卖 `$5`」**,而**记录一字未动**。⇒ **退休该记录,一分新钱没批**,
天花板回到工具自己 derive 的 `$90` 刹车线,按需波当场付得起(`headroom $3.987 after this wave`)。

本轮:**零 AWS(一次调用都没有)**、零波次、`bots/`+`game/` **零 diff**、armed 串 **30 一字未动**、
**不发 owner 邮件**、无 promote、无 reject、无入集。
档案 `iterations/streams/test_set.md §GY`(§GY.0–§GY.7),机器键 `state.json:fence_crossing_RETIRED_20260911`。

⛔ **判定完结 0,不达 owner P4.2 的 ≥2,不粉饰不抵账** —— 理由见第 7 节,且**那个理由不是「没时间」**。

---

## 1. ⭐⭐⭐ 主轴:一条 crossing 可以在自己一字未动的情况下**变号**

crossing 是按 `operative = min(ruling, brake)` 施加的,而**这个 min 没有下界**。

| 时刻 | MTD | 最低**未越过**告警 = derived fence | `min(fence, brake)` | 带 `$85` 记录的 operative | 记录的作用 |
|---|---|---|---|---|---|
| 09-10T19:08Z(立记录时) | `$78.253` | **`$80`** | `$80` | **`$85`** | **买了 `$5`** |
| 09-11T18:15Z(剪刀那轮) | **`$80.413`** | **`$100`** | **`$90`** | **`$85`** | ⭐ **卖了 `$5`** |

中间**没有发生任何一件关于这条记录的事**。发生的是 **MTD 越过了 `$80` 告警** ⇒
「最低未越过告警」从 `$80` 跳到 `$100` ⇒ `min(fence, brake)` 从 `$80` 抬到 `$90` 刹车 ⇒
**同一个 `$85` 从地板上方掉到了天花板下方**。⭐ **树在记录底下移动,而记录的符号跟着翻。**

⭐ **而这条记录自己的 `note` 里就写着变号的时刻**,逐字:
「Buys roughly $7 of headroom over the **derived $80 fence**」 ——
它把自己定价在一个**会移动的参照物**上,却没有任何东西在参照物移动时举手。

📌 *一条为「抬高一道过低的 fence」而写的裁定,会在 MTD 越过告警的那一瞬间变成一顶帽子 ——
而变号的时刻恰好是没有人在看这条记录的时刻,因为那一刻大家都在看钱。*

## 2. ⭐⭐ 判据是实测不是论证

离线重放批测台自己 18:15Z 的第一手数字(⛔ **零 AWS 调用**;`--actual/--pending` 是我喂的,**不是账单**):

```
--actual 80.413 --limit 100 --thresholds 50,80,100 --pending 3.45 --no-accrual-check \
  --snapshot-instant 2026-09-11T13:49:53Z
```

| `--planned` | registry | 读数 | 退出码 |
|---|---|---|---|
| `2.15`(闸 (iv) 裁的按需波) | `$85` 记录在力 | `Headroom was $1.137, this wave needs $2.150.` | **`3`** |
| `2.15`(同一波、同一瞬间、同一份钱) | `--no-crossing-file` | `operative ceiling: $90.00` / **`headroom $3.987 after this wave`** | **`0`** |

⭐ **唯一的差别是那条记录。剪刀的两片都是它。**

⚠️ **`$80.413 > $80` 的连带事实**:`$80` 那封 Budget 告警**已经发给 owner 了**
⇒ 天花板回到 `$90` **不产生任何新的 owner 邮件**,下一封在 `$100`(= 批准线本身)。
MTD `$80.413` < `$100` ⇒ **本裁定无需 owner 批准**,且它**不批新钱**。

## 3. ⛔ 我**没有**走的两条路,以及为什么

**(甲) 不许用「付得起」去撤销闸 (iv)。** 围栏排的是**价格**,闸 (iv) 排的是**产出**。
W66 就是那次测量:四台 spot,三台在 **48 秒内跨两个 AZ** 被 `no-capacity` 收走(存活 6.4/7.3/7.6 min),
yield **1 paired seed of 4 machines**。当前容量下一发 `$1.10` 的 spot 波期望产出≈0
⇒ **每粒配对种子的成本不是更便宜,是无穷大**。⭐ **一发什么都买不到的波不是省钱,是纯损失。**
批测台拒绝自行挑那一边是**对的**(逐字「那等于用省钱的理由撤销一条闸的裁定」),本轮明确背书。

**(乙) 也不许抬高天花板 —— 而这是我本来准备写的裁定。**
草稿是 `$85 → $86.50`(仍在 `$90` 之下,留 `$3.50` 给已知最大低报 GH #742 的 `$1.25`)。
⛔ **它会是一条错的裁定**:它默认了「钱不够」这个前提,**而前提是假的**。
⭐ **把它救回来的不是想得更久,是去跑了一次工具** —— 我本来只是想复核批测台的算术。

📌 *钱紧的时候,先确认挡路的是钱。*

## 4. ⭐⭐ 为什么三轮没人发现:**每一行打印出来的话都是真的**

闸在剪刀那轮逐字打的是:
`crossing registry: ruling GH#721/director-20260910 IS IN FORCE ... It is applied below.` 与
`derived fence $100.00 -> operative ceiling $85.00 = min(ruling, brake). The brake is untouched.`

**每一句都为真,合起来读像一次恩赐**:「裁定在力」「已施加」「刹车没动」。
`$100` 与 `$85` 两个数**并排印在同一行上**,差额 `$15` 摆在那里 ——
而**没有任何一行说「这条裁定此刻正在花掉你 `$5`」**。

⭐⭐ **更贵的一层在拒绝路径上,那里的话是假的**:
`The ruling is being obeyed, not overridden: it bought a band, and this wave is past the top of it.`
当裁定是帽子时,**每个分句都是假的**:它没买任何带,这一波也没越过 fence/brake 会拦的任何东西。
⭐ **这就是被抄进 GH #754 当作缺钱证据的那一句。**

📌 *一个工具只在「裁定不够用」那一侧写了台词,在「裁定过紧」那一侧沿用同一句 ——
于是它在唯一会误导人的那个分支上说反话。*

## 5. harness 半边(本轮已落地,带变异台)

`tools/batch_test/soak/wave_fence.py`:
1. **`*** RESTRICTIVE RIGHT NOW` 横幅**:`ruling < min(derived fence, brake)` 时点名
   **它此刻在花掉多少**(`COSTING $5.00 of headroom, not buying any`)、没有它天花板会是多少、
   以及机制本身(`min has no floor`),并给出两种合法读法(本意是帽子/本意是 crossing 而它已做完)。
   ⛔ **不把「过紧的 crossing」判为错误** —— §17c 早把 `a ruling can tighten` 写成**设计意图**;
   横幅只负责**让它不再沉默**。
2. **拒绝路径分叉**:帽子那一支改说 `THE MONEY IS THERE and a ruling is what is refusing`,
   并**明令禁止**下游做批测台当时被推着做的那件事:`do NOT pick a cheaper market to fit under it`。
   ⭐ **既有断言 `17c3` 一字未改仍然全绿** —— 帽子那一支**照样**带
   `the ceiling the director's own ruling <ref> named` 与 `being obeyed, not overridden`。
   **「退休一条 id 不许连带退休守着它的断言」本轮正面遵守了一次**:改法是让代码满足旧断言的**意图**,
   不是把断言改成迁就新代码。
3. `tests/test_wave_fence.py` **§19a–§19e**,终读 **`176 checks, 0 failed`**(`RC_EXIT=0`,走 `rc.sh`)。

⭐ **变异台三发全红**(逐条实测,基线复位后 `176 checks, 0 failed`):

| 变异 | 落点 | 被抓 |
|---|---|---|
| **M1** 删掉横幅(算术一字不动) | 恩赐/帽子再次不可区分 | `19b` `19b2` `19b3`(连带 `17b4`) |
| **M2** 帽子分支退回旧台词 | 那句反话复活 | `19c` `19c2` `19c3` |
| **M3** 把 `$85` 记录放回在力 | 出厂 registry 又压天花板 | `19e` |

⭐ **M1/M2 的存在理由不是覆盖率**:这两条缺陷**整个都在散文里**,算术在缺陷发生时**始终是对的**
⇒ 没有 M1/M2,第 1 节整条裁定背后**空无一物**。

## 6. 投递(章程 2.5)

⭐ **本裁定的主投递不是一个字段,是一个被工具自己读的记录** —— RULING 11 的机制:
闸每轮自己读 `director_rulings.json` 并**打印它读到了什么(包括「什么都没有」)**,不靠操作员重打三个 flag。

| 处 | 落点 |
|---|---|
| **主投递** | `iterations/director_rulings.json` —— `crossings: []`,记录**连退休理由**留在 `_retired`(⛔ 不删:删掉就再也审计不了它当初为什么在力) |
| 档案 | `iterations/streams/test_set.md §GY`(§GY.0–§GY.7) |
| 执行方每轮读的那份 | `iterations/streams/batch-desk.md` 末尾(与其最近 47 条同一节,**保证下轮 tail 读得到**) |
| 机器键 | `state.json:fence_crossing_RETIRED_20260911` |
| 常驻义务 | `iterations/owed_executions.json:fence_ceiling_restored_to_brake` |
| GitHub | GH #754 追评(按 GH #290 **留到 push 之后**;⛔ MCP 若要审批按铁律 11 立刻放弃并留到下轮) |

**常驻义务的 `done_when`**:批测台任一轮**当轮现跑**逐字抄回两行 ——
(1) `crossing registry:` 读到 **none in force**(⛔ 不是 `IS IN FORCE`,也不是 `NOT CONSULTED`,后者是 SKIP);
(2) `operative ceiling:` 读到 **`$90.00 = min(fence, brake)`** 且其下**没有** `DIRECTOR CROSSING:` 行。
⚠️ 它防的失效形状很具体:**退休只改了总监这边的文件,而下游的闸读到的仍是旧天花板** ——
或者我的离线重放算错了/活跑的 `--pending` 构成与我喂的 `3.45` 不同。**只有活跑那一行能证否。**

## 7. ⛔ 判定完结 0 —— 理由,不粉饰

owner P4.2 要 **≥2**,本轮 **0**。理由**不是没时间**:

- ⭐ **唯一一条条件 (a) 今天刚齐的 id 是 `lf_rescue`**(录像组 16:11Z,`VERIFY id=lf_rescue verdict=WORKING episodes=115`,
  深查 7 局达标)—— 而它的**条件 (b) 要一波批测**,⭐ **那波正是被本轮这把剪刀卡住的那一波**。
  ⇒ **本轮做的就是给判定完结解锁**,不是绕开它。
- `ownhalf`:上一轮已写明「这一格今天已经用掉理由了」,它要的是**录像组一份 ≥6 局的 (a)**,
  ⛔ 不是总监再读一次同一份 7 帧。录像组 16:11Z 那轮去做了 `lf_rescue`(理由正当:`ownhalf` 没有新波可检)。
- ⇒ **判定完结的瓶颈今天是波次,不是判定。** 这条要交下去:**下一轮若按需波已收割,`lf_rescue` 是第一格。**

⛔ **本轮不拿一条「还不够厚」的重复裁定去凑数** —— 那会变成一条每轮复述自己的裁定(上一轮已举过这个手)。

## 8. 🩺 巡检 / 💰 成本 / ⚠️ 本轮自己的两处失手

**🩺 五组节奏(⛔ 按文件名的 UTC 时间戳排序,不按 mtime —— 上一轮在这里方向读反过一次)**:
batch-desk 18:15Z / hero 17:00Z / strategy 16:30Z / replay-check 16:11Z / director 16:16Z
⇒ **最近 3 小时内全部有产出,无掉队组。**

**💰 成本**:本轮 **AWS 调用 0 次,不作 MTD 新声称**。转述批测台 18:15Z 第一手:MTD **`$80.413`**,
快照 `13:49:53Z`(**4.4h 未刷新**,诚实 cutoff ≈ `02:31Z` ⇒ **W65/W66 都没落账**),
本轮全队新增支出 **`$0.01`**(一次 CE 请求),**零 EC2**,泄漏三路全零、17 区 `CERTIFIED ZERO`。
三条线:围栏 `$60` / 刹车 `$90` / 批准 `$100` **一字未动**;**operative ceiling `$85` → `$90`(= 回到刹车,不是新钱)**。
**armed 30,离 P4.2 解冻线(≤20)还差 10 条。**

⚠️ **(甲) 开工自检本轮第一次跑成 `EXIT=124`,是我自己的 `timeout 400` 杀的** ——
上一轮实测它要 **~16 分钟**,`400s` **结构上不可能跑完**,死在 `=== trunk health (python test suite) ===` 里。
⛔ **124 是容器的判决不是自检的判决,不是 SKIP 也不是通过,该轮读数作废。**
⭐ 与批测台 09-10 那轮**逐字同形**(他们也是先 `timeout 400` 得 124、再 `timeout 900` 仍没跑完)
⇒ **这已经是跨组第二发**:⛔ **自检不许带短于其实测时长的 `timeout`**。本轮处置见第 9 节(改为**编辑全部做完后**跑到底)。

⚠️ **(乙) 上一轮那条污染警告本轮兑现了一半**:上一轮写「下一轮必须在动工前跑完自检」,
本轮把自检放在开头**是对的**,但它被我杀掉之后,**我在没有自检读数的情况下开始动工** ——
形状变了(不再是「边扫边改」),代价相同(**动工时 trunk 侧没人看过**)。
⭐ **真正的修法是顺序不是时刻**:本轮改为**所有编辑落定后、push 之前**跑一次**安静树**上的完整自检,
既无污染又有读数。⇒ **下一轮起照此执行**(登记在第 9 节)。

⭐ **`rc.sh` 本轮零违规**:所有要读退出码的命令都走了 `rc.sh` 或重定向
(⭐ 开工第一条 `... | tail -60` **被 §22 守卫当场拒**并打 `SELFCHECK_EXIT=2 REFUSED`,
**守卫第二十四次拦下,`rc.sh` 是习惯不是门(第十七次)**)。

## 9. 下次触发

1. ⭐⭐⭐ **判定完结 ≥2,第一格是 `lf_rescue`**(条件 (a) 已 WORKING/episodes=115,只欠 (b) 的那一波)——
   ⚠️ **前提是按需波已收割**;若仍未收割,**不要把 (b) 再判一次**,换别的格。
2. ⭐⭐ **[harness] GH #584 三条测试各加第三条分支 + AGENTS.md 的「grep OTHER GATES」加宽到
   「id 在 `bots/` 之外的一切引用」**(上一轮已裁未做,**要安静容器**;⛔ 不得再挤掉判定完结)。
3. ⭐ **批测台交棒 ① 未裁**:`batch-desk.md` 的「当前状态」被劈成两段,其活状态落在
   `routine_selfcheck.sh` 眼里的「历史」区 ⇒ 该腿**结构上看不到他们的活状态**,他们差点重复开 GH #721。
   节序是总监的编排权,**这条只有我能裁**。
4. ⭐ `py_gate_budget_premise`(与 2 同一把旋钮,合并成一个工作单元)+ `consequence_polarity_census`
   + `pullcamp_atom_readmission` + 五条 `a_evidence_*`(**第六次顺延**)。
5. 存量:GH #523 / patch 缺口 P3 / 账户级预算过滤器等 owner(DECISIONS_NEEDED 第 15/18 条)。
6. ⭐ **新登记的小纪律(本轮学费)**:⛔ **开工自检不许带短于其实测时长(~16min)的 `timeout`**;
   ⭐ **且它的正确位置是「所有编辑落定后、push 之前」的安静树**,不是开工那一秒 ——
   放开头会二选一地要么污染读数、要么在无读数时动工,**两种上一轮/本轮各犯了一次**。
7. ④b 那条仍有效并**本轮遵守**:写 `iterations/*.json` 先读原文缩进再写回 ——
   `owed_executions.json`(indent=1)**21 insertions / 1 deletion**、`state.json`(indent=2)**9 insertions / 0 deletions**,
   **无整文件重排**。

## 10. 铁律 6 三条腿 / 自检 / token

(见下方「补记」节 —— 按 GH #290,push 之后回填。)

---

## 补记(第一次 push 之后,GH #290 顺序)

### (A) 铁律 6 三条腿(两次 push 读数,**未用 `RULE6_BYPASS`**)

| 腿 | 分支 push | main push |
|---|---|---|
| `luacheck bots game` | **`0 warnings`** / `GATE_EXIT=0  CLEAN (iron rule 6 static half passed)` | 同 |
| `py gate` | `96 ran, 0 findings, 0 uncertifiable, 32.2s` | `96 ran, 0 findings, 0 uncertifiable, 31.9s` |
| `lua gate` | `339 ran, 0 findings, 0 uncertifiable, 12 known-red, 366.4s` | `339 ran, 0 findings, 0 uncertifiable, 12 known-red, 357.5s` |

`ARM_EXIT=0`(`core.hooksPath=.githooks, already set`)。
**落地**:分支 `763be512`(首推);main **`010fd6ec..c68abce4`**,`RC_EXIT=0`。
⚠️ 第一次 `HEAD:main` **被拒**(`fetch first` —— 录像组 19:02Z 那轮先落地),
按章程 `git pull --rebase origin main` 后重推成功;⭐ **rebase 零冲突**
(④b 的缩进纪律本轮兑现了一次:两个 JSON 都没整文件重排)。

### (B) 自检(铁律 10)—— **安静树上跑完,真码到手**

**`SELFCHECK_EXIT=3`**,`legs run : 12`,`selfcheck worst exit: 3`。
`FINDINGS (exit 3) : unlanded cadence owed-executions trunk-red(python) trunk-red(lua)`;
`NOT RUN (inside a leg): tests/test_selfcheck_lua_leg.py`。
⭐ **这是本轮改法的兑现**:自检放在**所有编辑落定后、push 之前**的安静树上
⇒ **既没有污染(上一轮的病),也没有「无读数就动工」(本轮第一次的病)**。

- **python 腿 TRUNK RED**:`121 passed, 4 failed, 1 uncertifiable`;
  `failed: tests/test_bots_walk_farm_only.py tests/test_carrier_terms.py
  tests/test_detector_source_constants.py tests/test_py_gate.py`
  ⇒ **与上一轮 (庚) 逐条同一族(GH #584,本轮已裁未做,列为下轮第 2 项)**,**非本轮引入**。
- **Lua 腿 TRUNK RED 2/87**:`test_lion_ult_reserve_domain.lua`、`test_stayfield2_marginal_domain.lua`
  ⇒ GH #718/#650 族,**不开重复 issue**。
- ⛔ **归因的证据而不是声称**:本轮 `bots/` `game/` **一行未改**(`git diff --stat` 逐字);
  改动只在 `tools/batch_test/soak/wave_fence.py` + `tests/test_wave_fence.py` + `iterations/*`,
  而**四条 python 红与两条 lua 红没有一条是这两个文件**;
  且 `tests/test_wave_fence.py` 实测 **0.053s**,⇒ **`test_py_gate.py` 的 12s 预算红与本轮无关**
  (它在批测台 09-11T15:14Z / 18:15Z 就已经红着)。

### (C) ⭐ push 之后读到的一件**改变下一轮排序**的事

录像组 **19:02Z**(我写完报告之后才落地)交出 **`VERIFY id=tpcommit verdict=WORKING episodes=1360`**。
⇒ ⭐⭐ **条件 (a) 已齐的 id 现在是两条,不是一条**:`lf_rescue`(16:11Z,episodes=115)与 `tpcommit`(19:02Z,episodes=1360)。
**两条欠的都是 (b),而 (b) 要的正是被本轮这把剪刀卡住的那一波。**
⇒ **第 9 节第 1 项因此更硬**:按需波一收割,**判定完结 ≥2 在同一轮内是够材料的** ——
⛔ 但仍以当轮现跑的 (b) 读数为准,**不得拿本节这句话当已经过门**。
⚠️ 同轮录像组还对 `tpcommit` 用了「**用门自己的子句当零通道**」的装置(与 `lf_rescue` 同形)——
下一轮判 (b) 时要连这个装置一起读,**别把增量门的 null 通道读成普通 baseline 腿**。

### (D) GitHub 追评

草稿 `gh754.md` 已过 `claim_precheck.sh`(见下),按 GH #290 **在 push 之后**发表于 GH #754。
⛔ 若 MCP 返回需审批,按铁律 11 **立刻放弃并留到下轮**,不空转等待。

### (E) 本次会话 token 用量(铁律 8)

```
TOKENS total_in=16,287,383 out=74,862 turns=103
```
⚠️ 这是**统计时刻为止**的数字,其后的收尾回合不计入。
⚠️ `total_in` 含 cache 读,量级参考:本轮是**单一裁定 + 一处 harness 改动 + 变异台**,
`out=74,862` 与近几轮总监轮同量级;⛔ 其中**有一段是我自己制造的**:
自检被 `timeout 400` 杀掉后**重跑了一遍完整的 16 分钟**(第 8 节 (甲)),
以及 main 被拒后**整条三腿闸多跑了一轮**(约 6 分钟)。
