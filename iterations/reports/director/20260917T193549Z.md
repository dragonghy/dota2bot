# 总监 2026-09-17T19:35:49Z — 结清交棒 ⑨(`lua-coverage` 新增 3 条),并把 GH #548 从「骑在刀口上」改测成「5.6 倍预算」

**本轮实质:交棒 ⑨ 要的是「3 条各跑一次确认不红再打标签」。跑完发现只有 2 条能进闸;
第 3 条 91.3s。而为了照章程规定「打标签前 TIME the resulting set」,量到的那个数
不是关于我这 2 条的 —— 是关于整条腿的:138 文件 / 671.8s,而验收它的预算是 120s。**

零 AWS、零波次、`bots/` + `game/` 零 diff、不发 owner 邮件。
成本(RULING 48 三段式):**零 EC2 / 零 CE / S3 读取 0 个对象(出网未计价)**。

---

## 〇、开工自检(铁律 10)

⚠️ **管道坑第 35 次,且仍是当轮第一条命令** —— 但这次是**脚本自己拦下的**,不是事后才发现:

```
REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.
         ... This is evidence discipline 3; it has recurred 5x,
         every time as the first command of the round.
SELFCHECK_EXIT=2  REFUSED (nothing was checked; this is NOT a pass)
```

⛔ 我那一行 `echo "SELFCHECK_EXIT=$?"` 打出来的 `0` 是 **`tail` 的码**,不是自检的码。
改重定向重跑,**真码**(从 task 输出裸读):`EXIT=3`,与日志末行 `selfcheck worst exit: 3` 一致。

```
legs run              : 14
FINDINGS (exit 3)     : cadence queue-rulings owed-executions lua-coverage
UNCERTIFIABLE (exit 2): trunk-red(python)
NOT RUN (inside a leg): tests/test_lua_gate.py tests/test_luacheck_gate_soakswitch.py tests/test_selfcheck_lua_leg.py
```

⭐ **交棒 ⑯ 结清:`legs run : 14`**(上一轮是 13 且**不含** `py-coverage`)⇒ 新腿的
**第一次活读到手**,接线成立。⛔ 上一轮逐字禁止写「新腿已验证」,现在可以写了,
**而且是被下一个组的自检核的**,正是它要求的那种核法。

⚠️ `trunk-red(python)` 是 **UNCERTIFIABLE 不是红**;成因即 GH **#882**(hero 组本日 18:13Z 已开)——
那两个用例跑在自检自己装 `lua5.1` 之前,**是顺序不是缺工具**。本轮不重复开。

---

## 一、交棒 ⑨:3 条各跑一次 —— 其中一次是假绿,照登

⛔ **第一跑三条全是假绿,而假绿的形状是 exit 0 + 零输出**:

```
test_campbind_poke_real_frame EXIT=0 SECONDS=.006440354      <- 0 字节输出
test_dusttower_dive_guard     EXIT=0 SECONDS=.006061366      <- 0 字节输出
test_fieldsip_transfer_receiving_site EXIT=0 SECONDS=.005476626  <- 0 字节输出
```

成因**仓库自己写在** `tests/run_tests.lua` 头部,逐字:

> `lua5.1 tests/test_x.lua` run directly returns the test TABLE without calling
> anything (exit 0, zero output). ... the runner is the only supported entry point.

⇒ 我把**「三个 fixture 测试」当成「三个可执行脚本」**跑了一遍,拿到三个 exit 0。
**0.006s 是它交还一张表的时间,不是它做了任何断言的时间。**
📌 这与铁律 6 的 `SKIP 不是 pass`、GH #200 的「零测试体也 exit 0」是**同一族**;
本轮它没造成损害的唯一原因是 **0.006s 这个数太离谱**,而不是我有什么闸拦着 ——
⛔ 如果这三个文件恰好是重量级的,假绿会长得完全正常。

**改走 runner 的真读数**:

| 文件 | 真秒数 | 读数 |
|---|---|---|
| `test_campbind_poke_real_frame.lua` | **0.293s** | 7 tests, 0 failures |
| `test_dusttower_dive_guard.lua` | **0.171s** | 11 tests, 0 failures |
| `test_fieldsip_transfer_receiving_site.lua` | **91.307s** | 8 tests, 0 failures |

⇒ 三条**都绿**,但**只有两条便宜**。

### 落地:2 条打 `[ratchet]`,第 3 条不打 —— 理由是实测不是口味

`test_campbind_poke_real_frame.lua` / `test_dusttower_dive_guard.lua` 各加 `[ratchet]` 前缀标签。
**这是诚实用法不是借道**:同族的 `test_campbind_coarm_vacuous.lua`(campbind 亲兄弟)与
`test_cm_creep_reach_real_frame.lua` 早就是 `[ratchet]`,而「真实帧 fixture 钉住一个决策」
正是**任何人改 `bots/` 都能顶红**的那个子集 —— 自检腿逐字说它拥有的就是这个子集。

⛔ **`test_fieldsip_transfer_receiving_site.lua` (91.3s) 不打标签**,依据是自检腿自己的头注释逐字:

> the tag marks what a test CLAIMS, not what it COSTS ... the tagged set went
> from 4.2s to **7m08s**, which is not a 开工 check any more ... If you are about
> to add a tag here, TIME the resulting set first.

91.3s × 五个流 × 每 2h 一次触发 = 每个容器每轮都付。**`[census]` 当年因为 7m08s 被否掉,
本条比它单条还贵。**

⚠️ **顺手排掉了那条头注释预言的成因,排掉了**:头注释点名四个「光 `require` 就 >25s」的文件
(`test_creeppull_zone_clause` / `test_fieldcreep_veto` / `test_fightback_world_assertion` /
`test_towerfear_clock_leg`)并说「None of them is tagged today」。**逐一核对:四条今天仍然全未打标签。**
而 `fieldsip` 的 **load-only 是 0.006s** ⇒ 它的 91.3s **在测试体里,不在 require 时**
⇒ 头注释开的那副药(`move the slow file's sweep behind a function`)**对它不适用**。

**读数(`lua_gate_coverage.py`,纯集合运算不跑测试)**:

```
前:开工自检 leg 136 | UNCOVERED 115 of 508 (23%) {'no_manifest_row': 53, ...}
后:开工自检 leg 138 | UNCOVERED 113 of 508 (22%) {'no_manifest_row': 51, ...}
UNCOVERED SET GREW -- 1 file(s):  NEW UNCOVERED no_manifest_row tests/test_fieldsip_transfer_receiving_site.lua
```

⇒ 交棒 ⑨ 的 3 条**结清 2 条**,第 3 条**原样留红并留名**。
⛔ **没有 `--update-baseline`**:工具**自己拒绝**把增长记进基线(`REFUSED to update the baseline:
%d file(s) BECAME uncovered` / `The baseline records shrinks, never growth`)——
⭐ **这道拒绝是对的,本轮受益于它**:我手上正好有一个「银掉它就清净了」的动机,
而那正是基线该拦的那件事。`fieldsip` 那一条**第三个选项**(genuinely watched elsewhere)
**也不成立** —— 它今天唯一的读者就是 ~100min 全集,而「只有全集读它」恰恰是 `uncovered` 的定义。
⛔ 附带:`NOW COVERED tests/test_fieldsip_atom_pricing.lua` 这一格也**因此银不掉**,继续挂账。

---

## 二、⭐⭐ 本轮真正的发现:GH #548 不是「骑在刀口上」,是 5.6 倍预算

章程要求「TIME the resulting set」。量了,**而那个数不是关于我这 2 条的**
(它们合计 0.464s,占 0.07%):

**静默树上干净重量(138 文件,红 0):`674.004s`**;
**逐文件复量(与 674s 那轮有并发重叠):`671.8s` —— 两次独立读数一致,⇒ 并发没有实质污染。**

GH **#548**(2026-09-06 开)记的是 **84 文件 / 120.1s**,标题逐字「**骑在 120s 预算刀口上**」。
今天:**138 文件 / 671.8s = 预算的 5.6 倍**。⛔ **这不是同一个问题的恶化,是换了一个 regime**:
120.1s 那天,修法是「砍一点」;671.8s 这天,「fast subset」这个设计前提本身没了。

**而它已经在静默收费,票据就在本轮自检日志里**:`test_selfcheck_lua_leg.py` 打了
**9 条 UNCERTIFIABLE、0 条 failure**,逐字理由 `the clean run did not finish inside 120s`。
该文件自己写死 `BUDGET_S = 120`,并且**在源码注释里预言了这个读数**(逐字):

> a timeout produces NINE uncertified and ZERO failures

⇒ **证成闭合**:腿长到 671.8s ⇒ 自检对腿的自测必然超时 ⇒ 9 条 UNCERTIFIABLE ⇒
⛔ **而 UNCERTIFIABLE 不长得像红**,所以没人追。
📌 **代价说清楚**:那 9 条里有 `5d 一条被弄红的检测器会让腿打 TRUNK RED` /
`5e 腿会点名失败文件` / `5g 腿会把退出码抬到 3` —— 也就是说,
**「腿会不会在 trunk 红的时候举手」这件事本身,今天是未核验的**,
而那正是这条腿存在的全部理由(GH #624 / #806 立案句)。**腿慢**与**腿没人验**是同一个根因。

**修法是集中的,不是弥散的(逐文件读数摆在这里就是为了这个)**:

| 秒 | 文件 |
|---|---|
| **119.087** | `test_stayfield2_marginal_domain.lua` |
| 32.126 | `test_wk_reserve_rank_blind.lua` |
| 31.885 | `test_mockscalar_return_shape.lua` |
| 30.712 | `test_stayfield2_live_domain.lua` |
| 30.591 | `test_fieldsrc_ff_premise.lua` |
| 29.852 | `test_midsupint_mirror_interrupt.lua` |
| 29.679 | `test_midsupmirror_checkability.lua` |
| 29.669 | `test_midsupfar_yield_target.lua` |
| 29.336 | `test_fieldsip_atom_pricing.lua` |
| 29.266 | `test_propertarget_corpus_domain.lua` |

- **Top 10 = 392.2s = 全腿的 58%**;**>10s 的只有 17 个文件**;其余 121 个合计 ~280s。
- ⭐ **`test_stayfield2_marginal_domain.lua` 一个文件 119.1s,几乎正好等于自测的整条 120s 预算** ——
  **一个成员单独把预算吃光。**
- ⇒ 这与 GH **#358** 的形状**逐字相同**(那次:133.3s/50 文件,**78.4s 集中在两个 `[ratchet]` 文件**)。
  **同一个缺陷第三次以同一种方式出现:标签按 CLAIM 发,成本按 COST 收,中间没有闸。**

⛔ **本轮不修它**(工作单元要小,且修法要动别人的测试);已把上面全部读数**追评进 GH #548**。
⚠️ **我自己这 2 条也在这条腿上**,虽然只占 0.07% —— **照登,不因为占比小就不说**。

---

## 三、巡检与成本(只读)

- **五组全活**(自检 cadence 腿,窗口 24h / 阈值 3.5h):`GAP` 5 条 —— director 6.0h 与 9.2h、
  hero 4.1h 与 5.9h、strategy 3.9h。⚠️ **均为触发间隔类空档,非掉棒**;无组失联。
- **成本**:本轮**零 AWS 调用**(总监不花钱)。批测台 09-17T15:19Z 报告读数结转:
  MTD **`$92.001`** > 刹车 `$90` ⇒ **零发波(第四十二轮)**;
  ⚠️ **`forecast $118.091` > 批准线 `$100`** 已在 `DECISIONS_NEEDED.md:1264`,**W38(09-20)那封邮件带它**。
- **owner 邮件**:本周**不发**(决定类每周 ≤1 封;下一封是 W38 周日汇总)。

---

## 四、下次触发(交棒)

① GH #856 修法第 3 条剩 9 个候选 ② 棘轮加宽(GH #867) ③ GH #240 余下
④ `carry_mark_prose_vs_list` ⑤ GH #843 剩 (乙) ⑥ GH #859 ⑦ GH #810 待裁 1 + (乙)
⑧ ⛔ **交棒 ⑧ 结清**:自检那三条 python 用例 = GH **#882**(hero 组 09-17T18:13Z 已开),**本座位不再挂账**
⑨ ⛔ **交棒 ⑨ 结清 2/3**;**余 `test_fieldsip_transfer_receiving_site.lua`(91.3s)**,
  ⛔ 三条修法**全不适用**(超 cap / 太贵不能打标签 / 无他处读者)⇒ 它是 **GH #548 的一个实例**,
  不是一条独立欠条;**#548 修好之前不要单独处理它**
⑩ P4.2 narrat 1 / `$0.90` 重裁 / GH #528 / patch 缺口 P3 ⑪ `path_contains_any` 那两行
⑫ `py_manifest_no_carry_baseline_port`(与 ⑤⑦ 同一条线)
⑬ **W38 周日汇总邮件(09-20)**:第 15/16/18/19 条 + 第 17 条已撤回 + RULING 70 + 71 + **72**
  + ⚠️ `forecast $118.091` > `$100` 那一行
⑭ `lua_gate_manifest.json` 已陈旧,⛔ 重测先读 GH #810
⑮ ⛔ **新(习惯,不开 issue)**:**「直接 `lua5.1 tests/test_x.lua`」= 假绿**(exit 0 + 零输出)。
  本轮栽了一次,三个文件一起。**唯一入口是 `tests/run_tests.lua <filter>`**;
  读到 `SECONDS` 是毫秒级且输出 0 字节 ⇒ 先怀疑自己没跑,再怀疑测试快
⑯ ⛔ **交棒 ⑯ 结清**:`legs run : 14`,`py-coverage` 腿第一次活读到手
⑰ **新**:**GH #548 已改测并追评**(84/120.1s → 138/671.8s,5.6×;Top10 占 58%;
  `test_stayfield2_marginal_domain.lua` 单条 119.1s)。⭐ **下一个动它的人先读 #548 追评**:
  修法是**砍 Top 10**(集中),不是**砍成员数**(弥散);
  ⛔ 且 `BUDGET_S=120` 那 9 条 UNCERTIFIABLE **不是噪声**,是「腿会不会举手」今天未核验

---

## 五、本次会话 token 用量(铁律 8)

```
TOKENS total_in=3,497,671 out=29,305 turns=36
```

⚠️ 这是"到统计时刻为止"的数字,统计后的收尾回合(commit / push / #548 追评)不计入。
