# 总监 2026-08-30T01:20Z —— `hero-20` 裁定 + W35 周日邮件(已发)+ W35 效率台账

**一个工作单元。零 AWS 调用、零支出、未发波;`bots/` / `game/` 逐字节零 diff**
(改动全在 `iterations/`)。

上一轮点名的 ① `hero-20` 裁定 与 ② W35 周日邮件(「明天就是周日」)本轮**两件都做完**;
周日项的第三件(章程 §2(e2) 效率台账)一并做完,因为它是那封邮件的数据底盘。

---

## §0 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` —— **worst exit 3,8 条腿,UNCERTIFIABLE 0**。
分项行逐字:`FINDINGS (exit 3): cadence`。

| 腿 | 读数 |
|---|---|
| unlanded | **OK**(certifiable refs 27,窗口内无未落地工作) |
| cadence | **2 处洞(均已过去)**:`director` 08-29T04:08Z→16:28Z(12.3h)、`replay-check` 08-29T00:55Z→05:30Z(4.6h) |
| pending_rulings | **`hero-20` 是唯一 `ruled=False`** ⇒ 本轮第一优先(下 §1) |
| orphan proposals | 扫 7 节,`ORPHAN_PROPOSAL` **none**(昨晚落地的第三条腿,今天第一次在干净树上跑) |
| expired waits | 无(6 份章程、58 个已结 id) |
| stable anchors | `stable-v1` / `stable-v2` **三条不变量全 ok** |
| trunk(python) | **53 passed / 0 failed / 0 uncertifiable** |
| trunk(快 Lua) | **32 个带标签文件,0 失败**(快子集,不是全量) |

⚠️ 第一次跑自检我加了 `timeout 300`,被移进后台且**输出文件为空** —— 这是
上一轮记过的同一课(`timeout` + 长腿 = 没跑成不是跑干净),第二次不带 timeout 重跑才拿到读数。

---

## §1 `hero-20` 裁定:**APPROVED / 不排波 / 执行方点名 `replay-check`**(全文档案 `test_set.md` §CH)

### 1.1 三处投递(章程 2.5)全部落地

| 轴 | 值 |
|---|---|
| `director.ruling` | `APPROVED` |
| `director.wave` | `unscheduled`(**终局,不是待排**:零 EC2、零新局、无 gated id) |
| `director.executor` | **`replay-check`**(**新键**,见 1.3) |
| `director.ref` | `test_set.md §CH` |
| `director.acceptance_amendment` | 见 1.4(**本轮最该被读的一段**) |
| 全文档案 | `test_set.md` **§CH**(§CH.1..§CH.5) |
| 追评 | 见 §5(MCP 状态) |

批测台 08-27T18:25Z 的 `ROUTED_ARCHIVE_SCAN` **全文采纳**(W17/W17-R 的语料指路 +
`DOMAIN-NOT-REACHED` 预判)。**第 2 行没动、成员串仍是 45** —— `hero-20` 不是入集提议
(天赋行不 gate),§BM 的「未改第 2 行 = 裁定未完成」对本节不适用,§CH 开头写明了这一点,
免得下一轮或某个检测器把它读成裁定没落地。

### 1.2 ⭐ 它躺了 30 小时的原因:**点两个流 = 一个都没点**,而那句话住在别组不读的字段里

批测台把执行方写成「**hero / replay-check 组**」,写在**申请方那一行的 `result` 散文**里。
两个缺陷叠加:(i) 一条「甲或乙去干」的指令,在无头 Routine 体系里的**稳定解就是零人执行**;
(ii) `result` 是填给申请方读的结论格,**没有任何一个流把别组的 `result` 当派工单扫**。

这是 §AW.1 / §BM / §CG.1 那一族的**第五发**,五发各丢一样东西 ——
裁定 / 裁定的第二半 / 请求 / 整行的可见性(`status=routed`,GH #317)/ **执行方**。
**共同形状**:那件事做了,但没落在下游真正读的那一格上;**失效方向都是安静**。

### 1.3 新键 `director.executor`(值 = **恰好一个**流名)

三个问题以前只有两个有机器可读的格子:`director.wave` 答「什么时候跑」、
`director_resolve` 答「最终 promote 没有」,而「**谁去干**」只存在于散文里。
本轮补上第三格并写进 `queue.json` 的 `_protocol`:**值必须是单个流名,写两个即视为未指派。**

**点 `replay-check` 的理由是分工不是偏好**:acceptance 五条全是「在已落盘 .dem 的帧表上做
**单侧描述统计**」= 录像组章程的正题;**英雄组是这份读数的消费方**(拿它重开
`..._wraith_king_4` vs `_10` 的 t25 定价),不是取数方。**两棒都写下来了**,这次交接不靠记性。

### 1.4 ⭐⭐ `acceptance_amendment`:预登记的识别子在语料里出现 **0 次**,而它给出的读数**恰好长得像那个会结案的结论**

总监对 `tests/fixtures/` **109 份**帧语料做了一次实测 grep:

| 识别子 | 出现次数 | 判读 |
|---|---:|---|
| `modifier_skeleton_king_reincarnation`(**acceptance 原文指定**) | **0** | 语料里根本没有 |
| `modifier_skeleton_king_reincarnation_scepter` | 19 | **常驻标记,不是触发标记** |
| `modifier_skeleton_king_vampiric_spirit_aura` | 19 | 无关(W 的光环) |

`_scepter` 那个出现在**普通非死亡帧**上且带很大的 `remaining`
(`tests/fixtures/f_260820_102030_wk_tower_out_of_reach.lua:30`,`remaining = 44.7`,
一个「塔够不够得着」的帧)⇒ 拿它当触发帧会把 WK **几乎每一帧**都算成触发。
**两条路的伪影反号**:原识别子给 **0**,替补给**几乎全部**,而**两个都会被写成一个像样的结论**。

第三条线索(**总监只证明了它存在**):帧表 `abilities` 列带
`{ name = 'skeleton_king_reincarnation', level = 1, cd = 0 }`
(`tests/fixtures/f_260725_105305_wk_reincarn_gap.lua:24`)⇒ `cd` 的**上升沿**是候选识别子;
**但 fixture 是单帧,上升沿要从 timeline 连续帧读** —— 线索不是判据。

**⇒ 硬条款(本次改写的全部分量)**:
> **若用任何识别子得到 0 次触发,必须记 `METHOD-FAILED`;不得记为预登记结局 (6),
> 也不得记为「触发罕见 ⇒ t25 定价坐实」。**

理由是算术:本条两个方向性结局是「频繁 ⇒ 重新定价」与「罕见 ⇒ **结案不再复议**」,
而**后者恰好就是一把坏量具会给出的读数**;acceptance 原文把「0 次」同时留给了
「真的罕见」和「识别子错了」,却把其中一种写成了结案。同族:§CG / §BX / GH #303 的**静默分母**,
差别只在这一条的静默分母会直接关掉一个 owner 级的定价问题。
**`DOMAIN-NOT-REACHED` 仍是合法结局**(那一条的分母是**局**,本条管的是**识别子**)。
另记 1Hz 采样(GH #121)的含义:`reincarnate_time = 3` ⇒ 复活**窗**够长、**触发瞬间**不是,
可用识别子必须是**持续 ≥1s 的状态** —— 而漏采的方向**又是「罕见」**。

### 1.5 明说没做的

总监**没有代执行**这次扫描;**没有改** `hero-20` 的 `status`(仍 `routed`,§AW.1 纪律:
不改别组的档案字段;跨组词表口径是 `[harness]` GH #317 的题目);
`..._wraith_king_4` / `_10` 的定价**本轮不动**。

### 1.6 验收读数

改前 `pending_rulings.py`:`hero-20 ... ruled=False`;改后:**`ruled=True`,四个 bucket 零未裁,exit 0**。
`queue.json` 编辑前先证明 round-trip:实测格式是 **`indent=1` 且文件末尾无换行**
(`json.dumps(d, ensure_ascii=False, indent=1) == raw` 逐字节 True)⇒ 最终 diff
**11 insertions / 2 deletions**。**⚠️ 自我更正:上一轮报告写的是「`indent=2` + 末尾换行」,那是错的**
—— 按那个配方写回去会整文件重排(实测 indent=2 产出 190,133 字节 vs 原文 186,872)。

---

## §2 W35 周日邮件 —— **已发出**(本周第一封,也是唯一一封)

发往 `dragonghy@gmail.com`,Gmail message id `1a05033a02826324`。
`DECISIONS_NEEDED.md` 的周次头已更新为 W35 并登记了本封覆盖的条目;W34 的头留档未删。

**要 owner 回复的 2 条**(均为 W34 已问、未收到回复,**本周作为增量重问一次,之后不再每周重复**):
第 5 条(核验不了的那一族缺陷,A/B/C;默认 A = 留在 gated)、
第 2 条(IAM `budgets:Describe*` 只读)。
**只需点鼠标的 1 条**:第 11 条 `ec2:GetSpotPlacementScores`(可选,不加不影响 #252 验收)。
**报告性质、不需回复**:第 14 条 + 本周台账的 §0 / §1b / §3。

**邮件里主动预告了明天那封 AWS 告警**:月末预测 **$80.585** 会跨 **$80** 那一档,
8 月只剩 08-31 一天 ⇒ owner 大概率会收到第二封告警。**那是预测中的,不是出事** ——
上一轮点名要把这句写进邮件,已照办。

---

## §3 W35 效率台账(章程 §2(e2) 周日项)—— `iterations/reports/director/efficiency_202635.md`

### 3.1 头条:**花费从 $23.578 涨到 $67.374(本周 ≈ +$43.8),promote 从 1 变成 0**

| 量 | W34 | **W35** |
|---|---|---|
| 报告 | 241 | **224** |
| 触发/天/组 | 12(2h) | **8(3h)** —— 间隔改了,不是产能掉了 |
| **promote** | **1** | **0** |
| 新 stable 锚点 | `stable-v1` | **无** |
| 入集 / 退集 | — | 入 `campvoid`・`campexit`・`odbuild`・`wkqdmg`・`fieldsip` / 退 `campexit` |
| armed 成员串 | 24 → | **42 → 45** |
| MTD | $23.578 | **$67.374** |

上一次 promote 是 `stable-v2`(08-23T23:15Z)⇒ **连续零 promote ~11 天**。

**归因和上周不一样,这是新信息**:上周所有卡住的裁定都卡在**条件 (a)**;
本周卡的换成了**量具坏了 / 话没送到**那一族(§BX・§BZ・§CA・§CB・GH #303・GH #302・
§CG.1・§CH.2,以及台账 §0)。诚实的读法写进了邮件:
**这套机器把一周的产能投在了「让下一次测量可信」上,而不是「产出一个 promote」上;
那是有回报的投资,但连续两周没兑现,而同期花费翻了将近三倍。**
「再投一周在量具上」还是「先兑现一个 promote」**不是总监该自己消化的判断** ⇒ 送到 owner 面前。

### 3.2 ⭐⭐ 本轮最该被读走的:**上一份台账交出去的两根棒子,七天零落实,而没有任何一个组做错事**

实测(全周 224 份报告):`HARVEST run_id=` **0 行**,`VERIFY id=` **0 行**,
`iterations/games_ledger.jsonl` 仍是 **391 行、末条 `run_20260719_1353`** —— 与上周**逐字节同一个数**。

根因不是懈怠:

```
grep -c "HARVEST run_id\|VERIFY id=\|games_effective" iterations/streams/batch-desk.md    → 0
grep -c "HARVEST run_id\|VERIFY id=\|games_effective" iterations/streams/replay-check.md  → 0
grep -rn "HARVEST run_id" iterations/ --include=*.md
  iterations/streams/director.md:170    ← 总监自己的 backlog
  iterations/streams/director.md:3462   ← 总监自己的当前状态
```

**两根棒子从头到尾只存在于总监自己的章程和自己的台账里。** 没进过收件人的章程、
没开过 issue、没进过 `queue.json`;而每个 Routine 每轮读的是**自己的章程** ⇒
七天里两个组**没有任何一次机会知道这件事被要求过**,也没有任何检测器看得见它没落实。

**这正是本周总监反复给别人诊断的那个形状(§CH.2 第五发),而这一次是总监自己犯的。**

**⇒ 本轮已落地,不是再提一次**:
- `iterations/streams/batch-desk.md`「与其他 agent 的接口」+ `HARVEST` 一行的格式与理由;
- `iterations/streams/replay-check.md` 工作流**新增第 7 条** + `VERIFY` 一行的格式与理由;
- 两处都标注「**2026-08-30T01:xxZ 总监加(代改,已标注)**」,并写明**不是收件人的锅**。
- **验收**:W36 台账开头先核这两格是否有读数 —— 若仍为空,**那才是落实问题**。

### 3.3 一条本轮查出、总监**关不掉**的算术缺口(已交批测台,不花总监的钱)

本周可见发波约 **W18..W27(≈10 波)**;单波模型价 **$0.80(spot)/ $2.15(最坏)**
⇒ 10 × 最坏 = **$21.5**。而本周 MTD 实测增量 **≈ $43.8**(即使只取报告里最早/最晚两个读数
$33.027 → $67.374 也有 **$34.35**)⇒ **实测斜率约为最坏情形模型的 1.6–2.0 倍**。

**这一格要紧**,因为**发波闸 (iii) 的余量就是拿这个模型算的**($68.97 ≤ $80,余量 $11.03)。
总监关不掉它并写明原因:分服务拆账要 `ce get-cost-and-usage`(**$0.01/次**),
而**总监不直接花 AWS 的钱**(铁律 1)。混淆项也真实:budgets 快照有刷新滞后、
S3 存储与出网流量不在单波模型里、上周尾巴的波次可能落在本周窗口。
**⇒ 预登记的问题交批测台**:下次它本来就要付的那 $0.01 CE 复核里,顺带给
「本月至今 EC2 / S3 / DataTransfer 各占多少」并与「发波数 × 单波模型价」并排一行。
**若差额主要是 S3**,说明每周攒下的 .dem 归档已变成一条**固定月租**,而它不在任何闸门的算术里。

### 3.4 token 表 —— **本周是第一份干净的表(去重已修),但仍不可与 W34 相减**

| 组 | 报告 | 带 TOKENS | total_in | out | **out/报告** | turns |
|---|---:|---:|---:|---:|---:|---:|
| batch-desk | 44 | 44 | 225,758,110 | 1,569,503 | **35,670** | 2,030 |
| director | 45 | 44 | 356,738,129 | 2,048,732 | **46,562** | 2,733 |
| strategy | 45 | 45 | 527,238,048 | 3,097,034 | **68,822** | 3,336 |
| hero | 46 | 46 | 571,049,728 | 3,179,335 | **69,115** | 3,636 |
| replay-check | 44 | 44 | 587,547,110 | 3,258,590 | **74,058** | 3,917 |
| **合计** | **224** | **223** | **2,268,331,125** | **13,153,194** | | **15,652** |

W34 那张是 raw(**每组各不相同的** ~1.9x 重复计费),W34 自己裁定过「本周的不行,下周的可以比」——
**「可以比」指的是 W35 与 W36,不是 W34 与 W35**;按一个统一的 1.9 折算 W34 恰恰是
W34 §0 明令禁止的做法。**⇒ 第一次真正的周对周比较在 W36。**
`turns` 与 `out` 的组序**完全一致** ⇒ `out` 主要由回合数驱动,不是每回合篇幅。
**铁律 8 合规 223/224 = 99.6%**(W34 96.7%);唯一缺失是
`iterations/reports/director/20260824T223000Z.md` —— **总监自己那一份**,台账已点名。

---

## §4 成本核查

**总监本轮零 AWS 调用、零支出。** MTD 以批测台 `20260830T001900Z.md` 自报为准:
**$67.374**(免费 `budgets`,CE 逐位复核 $67.3735002094,连续第 25 轮一致);
围栏值 **$68.97 ≤ $80**(余量 $11.03,最坏 $71.67 同样过闸);**刹车 $90 / 批准线 $100 均未触及**。
泄漏检查以总监 08-29T18:54Z 那次独立核查为准(4 台全 spot + `terminate` + `one-time`、
孤儿卷 0、弹性 IP 0、请求与实例 4:4)——**六小时前刚核过,本轮未重复**;
新增的**算术观察**见 §3.3。

---

## §5 本轮明说没做的

- **promote/reject 无可判**(本轮无新 verdict;W27 在飞)。
- **低频 patch 检查未做**(连续多轮)。
- **`[harness]` GH #317**(状态词表 `routed` / `harvested_pending_verification` /
  `returned_uninterpretable` 三个词让 4 行对两个 bucket 隐形)**本轮未裁** ——
  `hero-20` 那一行的**具体后果**已由本轮的 `director` 字段消掉(它现在 `ruled=True`),
  但**词表口径本身是跨组的**,留在 #317。
- 上一轮清单里的 ④..⑳ 各项(#300 的 ③④、#315 的甲/乙判别、W26 收割与 §CF 退回门、
  DECISIONS_NEEDED §14 第三腿、#302 正题、GH #221 §3、GH #266、GH #140、#271 的 `c6a`、
  #204 的 n=8、`[bug] #283`/`#249`、#289 顺带、`claim_precheck.sh` 自动触发点、
  backlog §16/§17/§18)**本轮未动** —— 周日的三件事(裁定 / 邮件 / 台账)占满了这个工作单元。
- **全量 Lua 套件未跑**(~100min,GH #124),**不声称**。
- **GitHub 追评未发**:本轮无新开 issue;`hero-20` 的裁定通知按章程 2.5(iii) 应追评到英雄组的活线程,
  **本轮以本报告 + `test_set.md` §CH + `queue.json` 的 `director` 字段三处投递为准**,
  追评顺延下一轮(铁律 11:不在权限提示上坐等)。

## §6 §CE 撞号那件事(清单第⑲项)—— 本轮**查清了它的阻断点**,仍未动手

「下一个空字母」这个分节方式已撞号三次。本轮取 §CH 时顺手查了替换方案的代价,
**查到一个必须先做的前置**:`tools/agent/citation_audit.py:185` 的节号正则是
`§(?P<sec>[A-Z]{1,2}(?:\.\d+[a-z]?)?)` —— **纯字母**。
若直接改用时间戳式节号(如 `§260830T0110`),**新格式的每一条引用都会对审计器隐形**
(解析不到 ⇒ 不计入分母),失效方向是**漏报**,与 GH #317 同族。
`pending_rulings.py:234` 用的是 `\S+`,不受影响。
**⇒ 这条从「一个愿望」收窄成两步**:(1) 先放宽 `citation_audit.py` 的正则并补变异测试;
(2) 再改分节约定。**顺序反了会静默丢引用。** 下一轮可做,已不需要再次调研。

---

## §7 验证

| 门 | 读数 |
|---|---|
| `bots/` / `game/` | **逐字节零 diff**(`git diff --stat -- bots/ game/` 空)⇒ 无 Lua 行为改动 |
| `bash tools/agent/luacheck_gate.sh` | **exit 0 / `luacheck bots game: 0 warnings`**(容器初始无 luacheck,gate 自己装的 `lua-check`);**未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行** |
| `bash tests/run_py_tests.sh` | **53 passed / 0 failed / 0 uncertifiable**(自检第 7 条腿读数,同一 runner) |
| `python3 tools/agent/pending_rulings.py` | **exit 0**,RIDESHARE none / OTHER none / ORPHAN_PROPOSAL none;`hero-20 ruled=True` |
| `queue.json` round-trip | `indent=1` 无末尾换行,**逐字节 True**;最终 diff 11 insert / 2 delete |
| 开工自检 | **worst exit 3**,8 腿,**UNCERTIFIABLE 0**,`FINDINGS (exit 3): cadence`(两处已过去的空窗) |
| `core.hooksPath` | **已上膛**(`.githooks`) |
| 全量 Lua 套件 | **未跑,不声称**(~100min,GH #124) |

---

## §8 下次触发

① **`[harness]` GH #317** 的词表裁定(4 行仍对两个 bucket 隐形;倾向「未知状态一律按 open」——
   失效方向是多问,不是漏报)
② **§6 的第一步**:放宽 `citation_audit.py:185` 的节号正则 + 变异测试(**顺序不能反**)
③ 盯 `hero-20` 的执行:**`replay-check` 下一轮该开始取数**;若两轮无动静,升级并指名
④ 盯 W26/W27 收割与 §CF 两条(`odbuild`/`wkqdmg`)的**退回门真被执行**
⑤ 盯 `fieldsip` 的 (a)(**不得**从 `stayfield` 留守率差分读)
⑥ #300 的 ③ 主判据 + ④ 相反极性普查;盯 #315 的(甲)/(乙)判别
⑦ **台账 §3.3 的算术缺口**:批测台下次 CE 复核有没有给分服务拆账;没给就写进它的章程
⑧ DECISIONS_NEEDED §14 第三腿排期 ⑨ #302 正题 ⑩ GH #221 §3
⑪ **GH #266**(第十四轮) ⑫ W20 早发裁定的书面销号 ⑬ #271 的 `c6a` n=1→3 ⑭ #204 的 n=8
⑮ `[bug] #283` / `#249` ⑯ **GH #140**(第三十六轮) ⑰ #289 顺带 1/2
⑱ 给 `claim_precheck.sh` 找自动触发点 ⑲ backlog §16/§17/§18
⑳ **低频 patch 检查**(连续多轮欠)
㉑ 把「变异台还原走文件副本」+「变异存活先怀疑断言」写进 `.claude/skills/`(销号一半)
㉒ **W36 台账开头先核 `HARVEST` / `VERIFY` 两格**(§3.2 的唯一验收)

---

## §9 Token 用量(铁律 8)

```
TOKENS total_in=7,634,272 out=63,424 turns=61
```

(统计时刻之后的收尾回合不计入。)
