# director 2026-08-25T05:0xZ — 我抢救了一份**正在飞**的工作,然后修掉了让四条红没人看见的那件事

**第六十七次触发。** 本轮的产出和本轮的教训不是同一件事,分开写:

- **产出**:开工自检对「`main` 的 Lua 侧是红的」**结构性失明**,已修(**GH #171**)。
  覆盖 08-25 当天 trunk 上**真实存在的 4 条红中的 4 条**,8 文件 / 78 检查 / **4.9s**。
- **教训**:我花了本轮**大半个工作单元**去抢救一份**并没有掉、只是卡在 3 小时门后面**的工作。
  抢救品全部作废、分支重建。**这不是零成本**,而档案里写着它是零成本 —— 那一句要改。

---

## 1. 我做了什么,以及它为什么白做

开工自检(铁律 10)报 **17 条 UNLANDED**,其中 `compassionate-albattani-j8efg3` 上 6 条(00:5xZ 那一轮的 `tpreach` GH #159 + GH #167 + gated-fix skill)。按 08-24T17:xxZ §④「**查内容不查 SHA**」核:`grep -rn tpreach bots/` 在 `origin/main`(`9d8747e`)上 **0 命中** ⇒ **判真 UNLANDED,抢救**。

cherry-pick 6 条(`test_set.md` 一处冲突:main 侧 02:xxZ 长出 `tpdeathbuy` 待裁块,抢救侧写 `tpreach` 入集,**两边都要**),过门,推分支。**判断没错,证据也没错 —— 但结论错了。**

**04:2xZ 再 fetch 时,那 6 条已经在 main 上了**,连同 `438fc10`(**和我同一轮修的同一条 trunk 红,同一个处方**)。00:5xZ 那个会话没有死:它**在等自己的全量套件**,`00:52:18Z` 启动、`03:53:36Z` 结束(**3h01m**),醒来就 rebase + 推了 main。

⇒ 本轮**丢弃**:6 条抢救 + 我自己那条 census 修复(`1189f52`)。分支从 `origin/main` 重建,只保留本轮**独有**的工作。

### 1.1 §④ 的判据不够,而缺的那一格档案里已经写着

§④ 让我查的是「**提交主题是不是你自己这一轮写的**」—— 这条挡的是**并行会话写同名提交**,我照做了,它**正确地**告诉我「不是我写的」。但它答不了真正的问题:**那个会话还活着吗。**

这正是 backlog **§6b** 2026-08-23T21:xxZ 用实测推翻自己时写下的那句:

> **一个在等慢门的会话和一个死掉的会话,在「最新提交时间」这一个量上逐字节相同。**
> 而且有因果:**GH #124(全量套件 83 分钟\~2 小时)就是假弃置的制造机制。**

**本轮是那句话的第二个实例**,而且更贵:上一次的代价是「抢救了一份已经落地的工作」(cherry-pick 冲突让 patch-id 变了),这次的代价是**抢救了一份正在飞的工作**,并且**独立重做了它的 trunk 红修复**。

### 1.2 ⚠️ 要更正档案里的一句话:抢救**不是**零成本

§6b 现在写着:

> 而在「无法区分」下**抢救仍是低风险正确默认**(本轮实测:cherry-pick 落地 + 主人改落读数 = **零重复**)。

**那个「零重复」是那一次的运气,不是这个动作的性质。** 本轮同样的动作产生了:6 条重复提交、一条**完整重做**的 trunk 红修复(处方与 `438fc10` 逐条相同,包括保留 partition 与 zero 断言的理由)、以及一次 `test_set.md` 冲突的重复消解。**抢救的默认仍然对**(漏掉一份真掉的工作更贵),但代价要按真实的写:**「低风险」不等于「零成本」**。

### 1.3 一个**已经存在**、没人读的判别器

上一轮的报告 §6 **自己写着**:

> **如果本会话在全量套件跑完前结束**,下一轮的抢救指令是一条命令 …

**一个把「我卡在慢门上、可能撑不到收尾」写进报告的会话,是在宣布自己在飞。** 我读了那份报告(为了拿抢救指令),**读到了这句话,没有把它当成活性信号**。这比 §6b 想要的「容器/任务仍在跑」便宜得多 —— 它就是纯文本,而且**当事人有动机写它**。

**已写进章程 backlog §6b 的修正**:`unlanded_commits.py` 的 UNLANDED 行旁边,应当并排打印**该 ref 最新提交所属报告里的自述状态**(有没有「等全量套件 / 未收尾」这类句子)。这一条**不是**本轮做的,是本轮**登记**的 —— 见 §5「下次触发」。

---

## 2. ⭐ 本轮真正的产出:开工自检看不见红 trunk(GH #171,已修)

### 2.1 现场:四条红,六个 trigger,没有一个看见

00:5xZ 那轮的全量套件读数(3h01m / **1796 tests / 4 failures**)记录了 **origin/main 上同时存在四条红**:

| 红 | 文件 |
|---|---|
| 语料大小被等号钉住(GH #106/#127 第八次) | `test_corpus_scale` ×1 |
| `ability_item_usage_generic.lua:5768/5823` | `test_level_gate_census` ×2 |
| `hero_lion.lua` 普查漂移(GH #166) | `test_wk_fact_anchor` ×1 |

其中 `test_corpus_scale` 那条从 **22:55Z 躺到 03:5xZ**。这期间**六个 stream trigger 按铁律 10 跑了自检** —— batch-desk 00:19Z、strategy 01:24Z、replay-check 01:35Z、hero 01:51Z、batch-desk 03:12Z、replay-check 04:16Z —— **每一个都读到一行干净的 trunk-health**。

因为 `routine_selfcheck.sh` 的那条腿**只跑 python 套件**,而且是**写明的豁免**:

> NOT the Lua suite: that one needs lua5.1 + minutes, and is the push gate's job (README rule 6), not 开工's.

**这是 GH #116 的形状第三次,而且是这个文件里的第二次** —— 它自己的注释写着「the ratchet caught it within 100 minutes and sat red on `origin/main`; the only missing step was somebody running the suite」,然后在下一段给 Lua 侧开了豁免。

### 2.2 豁免的前提在关键那一半上是错的

「Lua 套件要几分钟」对**全量**成立(**3h01m** 实测)。但**会被别人的落地弄红的**,只有**扫树那一类** —— 读源码、读 `tests/` 目录,而不是加载 fixture:

| | 文件 | 检查 | 耗时 |
|---|---|---|---|
| 扫树检测器 | **8** | 78 | **4.9s** |
| 全量套件 | 177 | 1796 | **3h01m** |

自检本身约 20s。**4.9s 是噪声。** 全量仍归 push gate(铁律 6);开工拿走的是**扫树子集**,而那**恰好就是别人能弄红的那个子集**。

### 2.3 ⭐ 「按 tag 发现」对了一半 —— tag 标的是**声称什么**,不是**花多少钱**

第一版按 `[detector]|[ratchet]` 发现,**4/4 条红只覆盖了 1 条**。
`test_level_gate_census` 打的是 `[census]`,顺手把 `[census]` 加进 tag 集看起来是显然的修法。

**实测:4.2s → 7m08s。** `[census]` 把 GH #124 的 sweep 家族(`test_itemdesire_world_assertion` 那一族)整个拖了进来。

⇒ **判据**:tag 标的是一个测试**声称什么**,不是它**花多少钱**;这两件事在本仓是**不相关**的。两个快的 census 文件**点名**(276ms / 345ms),tag 集只留从没超过毫秒级的那两个。注释里写死一句:**要加 tag,先给整个集合计时。**

### 2.4 验收:先拿真实现场跑

- **在未修的 `origin/main`(`9d8747e`)上跑这条腿** ⇒ `TRUNK RED -- 1 of 6 Lua detector file(s)`,点名 `test_corpus_scale`。**不是合成用例,就是 08-24 那一条。**
- `tests/test_selfcheck_lua_leg.py`,**25 检查**;**变异 7/7 红**,控制组绿。

### 2.5 ⭐⭐ 变异抓到了**我自己测试里的两条**,两条都是本仓的老形状

**(甲)覆盖断言对脚本是空的 —— 它在读自己的镜子。**
第一版的测试**把发现命令重打了一遍**,而不是从脚本里取。于是「`test_level_gate_census` 被覆盖」这类断言**与脚本无关**:把那个文件从 wrapper 里删掉,测试**照样全绿**。
**是变异说出来的**:M5/M6 两条**存活**。现在测试用正则**把脚本里的 `files=$(...)` 整块提出来跑**。
**一个镜像了被测对象的测试,测的是镜子。**

**(乙)加宽发现让测试变成 14 分钟的挂起,而不是一条红。**
M7(把 `[census]` 加进 tag)**没有变红,它把测试跑挂了**,我手工 kill 的。
**挂起读起来像「还在跑」,红读起来像「你弄坏了」** —— 这条腿的全部主张就是「它很快」,所以那个主张**必须是一条断言**:端到端现在带 **120s 预算**,超了就红并说明原因。M7 因此从「挂起」变成「9 条红」。

---

## 3. 我这一轮改了什么(以及**没**改什么)

**改的只有两个文件**:`tools/agent/routine_selfcheck.sh`(加腿)、`tests/test_selfcheck_lua_leg.py`(新建)。

**`bots/` 零改动、`game/` 零改动。** 本轮**没有**碰任何 hero 逻辑、任何 gate、任何 armed 串 —— `tpreach` 的那处 `bots/FunLib/jmz_func.lua` 是**别的会话**落的 main(gated 且未 armed ⇒ 今天每一局真实 Turbo 逐字节不变)。

无 promote/reject;不发波不请求发波;无 owner 邮件(W35 周日 08-30);`DECISIONS_NEEDED` 无新增;**不代 Cursor 改 `OWNER_PRIORITIES.md`**。

**GitHub**:开 **GH #171**([harness],本条);在 **GH #159** 追评把 census 的可复用判据交回协同组。⚠️ **两处措辞需要更正**(#171 的「落地」段与 #159 的评论都把 census 修复写成了我的):**真正落 main 的是 `438fc10`(00:5xZ 那个会话)**,我那份是并行重复、已丢弃。已在两处追评更正 —— 判据本身两份逐条相同,更正的是**归属**,不是内容。

---

## 4. 成本 / 体系健康

- **成本**:总监**零 AWS 调用**。MTD **$36.245** + W7 $1.48 + W8 $1.48 = **$39.21**(批测台 03:12Z 自报,免费 `budgets` 源,**带 EC2 滞后**,引用照带)。$45 围栏余 **$5.79 ≈ 1 波**;三线($45/$90/$100)全未触及,**未批准任何新支出**。⚠️ W7 即将滚出 12h 窗口 —— **那是窗口滚动不是省下的钱**。
- **自检**:开工 17 条 UNLANDED(6 条「真」——**事后证明是在飞**,其余假阳/重复)。CADENCE 10 条,08-24 白天全队停摆的残影 + 2h 节奏抖动。**收工时 trunk 两条腿都绿**:python **22/22**、Lua 检测器 **8 文件 0 红**(四条红均已由各组在本轮期间修掉)。
- **`stable-v2` 引用核对**:连续第**四**轮积压。

---

## 5. 下次触发(按优先级)

1. ⭐ **owner P1 的 (b) 裁定 —— 球在总监手里,已连续两轮没接**。批测台 03:12Z 交了读数:28-id 全集 vs `stable-v2`,池化 gpm **−23.27** / winrate **0.432**、comps **0/4 + 0/4**、277 计分局;(a) 已由录像组 01:35Z 判 `pulllane` = WORKING、04:16Z 判 `towerfear` = WORKING。**关键约束批测台自己写明了**:这是**全集的合成读数,不是 `pullcamp`+`pulllane` 的读数**;且 `winrate` 与 `gpm` 是**同一读数的两种粒度**(`winner_by = economy_10min_cap` 301/301)**不是第二个证人**。要单 id 的 (b) 需一次隔离波($1.48,围栏余 $5.79),**发不发由总监裁,批测台不自行发**。**这条值一个完整工作单元,不要顺带做。**
2. **`tpdeathbuy` 入集裁定**(协同组 02:xxZ,GH #168;**方向是加宽不是收窄**,反向哨兵是「TP 花费不许暴涨」,**一份「无变化」读数不能验证它**)。
3. **backlog §6b 追加一格(本轮登记,未做)**:`unlanded_commits.py` 的 UNLANDED 行旁边并排打印**该 ref 最新提交所属报告里的自述状态**(「等全量套件 / 未收尾」这类句子)。**理由见 §1.3:这个信号已经存在、当事人有动机写它、而且是纯文本**,比 §6b 原本想要的「容器/任务活性」便宜一个量级。同时把 §6b 里「抢救 = 零重复」那句按 §1.2 更正。
4. **`[bug] #156`(`cmboots` 漏成秘法鞋)**、**`[harness] #161`**、**`[harness] #152`**。
5. **GH #147**(第十次积压);**GH #140 收口**;**`stable-v2` 引用**(第四轮)。
6. backlog §15 / §13 / §12 / §14 / §9。

**给下一个总监**:上一轮的教训是「**门要 3h 而会话不够长**」(GH #161)。本轮是它的**两个镜像**,一个贵一个便宜:

- **贵的**:那条 3h 的门不只让工作掉在半路,**还让活着的会话看起来是死的** —— 我据此重做了它一半的工作(§1)。**#124 和 #161 不是两条效率问题,是同一条,而且它的第二个受害者是抢救流程本身。**
- **便宜的**:另一条门只要 **4.9 秒**,而它之所以四条红没人看见,**仅仅是因为没有人被要求去看**(§2)。

合起来是同一句话:**一个门的价值 = 它的严格性 × 真的有人跑它的概率**。本仓在第二个因子上已经付了三次钱(#116、铁律 10 的立法现场、本轮);§2 那条腿把其中一格推到了 1。

## 6. Token 用量

```
TOKENS total_in=21,124,733 out=88,341 turns=138
```

(统计时刻为收尾 push 之前;其后的回合不计入。本轮用量偏高,**归因明确**:§1 那半个白做的工作单元 —— 6 条 cherry-pick、一次冲突消解、一份重复的 census 修复与其变异测试。这正是 §5 第 3 条那个工具改动想要省下的东西。)
