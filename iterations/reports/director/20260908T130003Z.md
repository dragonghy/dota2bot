# 总监 2026-09-08T13:00Z —— `text_absent` 落地:一条裁定的「反面那一半」永远买得到

**一句话**:GH #523 的 owed 行 `text_absent_done_when_kind` 本轮**执行并退休**(19 → 18 行) ——
`pending_rulings.py` 现在认得 `done_when.kind = "text_absent"`,
**文件在 且 这几段字面文本都不在里面 ⇒ DONE;文件读不到 ⇒ UNCERTIFIABLE,不是 DONE**。

**第二件(顺手撞到,见 §2b,比第一件更贵)**:铁律 6 的静态门**在一棵零 Lua diff 的树上读 RED** ——
`bots/Customize/soak_side.lua`(gitignored 的 arming switch)在开工自检并发跑时被创建又删除,
`luacheck` 先走树后打开 ⇒ I/O error ⇒ **`.githooks/pre-push` 拒绝一次与它无关的 push**。
按 `lua_corpus.py`(GH #229/#243)已经用过的修法**按名字排除**,两半都钉。

零 AWS、零波次、**`bots/` + `game/` 零 diff**、不发 owner 邮件、无 promote/reject、`DECISIONS_NEEDED` +0。

取活依据:上一轮(10:1xZ)「下次触发」的 **④**(`text_absent` GH #523,**原样顺延已 3 轮**,
且上一轮当场为它找到了第二个真实用户 —— `hero_domain_scan` §7 那句字面 `NOT YET`)。
它也是 registry 里**唯一一条 executor 是 director、trigger 写着「任何一轮有余量的总监工作单元都可以取」**
的行 —— 换句话说,没有别人会取它,而它已经顺延了三轮。

机器键:`state.json:text_absent_kind_20260908`。**GH #523 已按它自己的验收句逐条追评并关闭**;
§2b 的那件另开 **[harness] GH #633**(窄修已落地,留 open 的是没买到的那一半)。
两条评论发表前都跑了 `claim_precheck.sh`:**`local commits not on origin/main: 0`、`refused 0`、`OK to publish`**
(#633 的草稿有一条 `IGNORED-BY-DESIGN bots/Customize/soak_side.lua` —— 那正好是**它讲的那个文件**,
被 trunk 自己的 `.gitignore` 认出来,是「按设计不在树上」不是「没 push」)。裁定全文档案仍是 `test_set.md §EV`(本轮**不新增章节**:
这是一次**执行**,不是一条新裁定;执行的落点是 registry 的 `retired` 行 + state 键 + 本报告)。

---

## 1. 缺陷(复述,因为它是这个 kind 的全部形状)

`outlatch_check1b_reason` 那一行从「**注释说得对不对没有断言读得出来**」正确地推出了 `kind: manual`,
**却从来没问过容易的那一半:那两句写错的话还在不在**。它们是字面串
(`the whole 75%-abort finding is about that missing guard` / `if this fails, the guard landed`),
`text_absent` 会在 07:01Z 当场读 DONE ⇒ 那一行**根本不会被登记成一根掉在地上的棒**。

⇒ 通则,现在写进工具的 LIMIT 11 而不是只活在档案里:
**凡是「把这句写错的话改掉」形状的裁定,正面那一半(替换得对不对)不可读,反面那一半(错话还在不在)永远可读。**

## 2. 落地物

**(1) `tools/agent/pending_rulings.py`** —— `_machine_key_status()` 多一个 kind,白名单同时放行
(白名单与实现是两处,漏改任何一处这条分支就是死代码;§4 有一条断言专门钉这个)。

| 读到的东西 | 状态 | 为什么 |
|---|---|---|
| 文件在、几句都不在里面 | **DONE** | detail 仍写着「absence 不等于替换它的东西是对的,总监还要把文件读一遍」——LIMIT 11 的边界不许被这个 kind 吃掉 |
| 文件在、还剩几句 | **OWED** | detail 点名**还剩哪几句、几分之几**(半截活是最容易被挥手放过的那一格) |
| **文件读不到 / 被删了** | **UNCERTIFIABLE** | ⛔ **这是本 kind 唯一的假阳**:删掉文件会让每一条 needle 都「不在里面」。「文件没了」是 `path_absent` 的格子,已经有家了 |
| `text` 空 / 写坏 | **UNCERTIFIABLE** | 空 needle 集合对**每一个文件**都成立,它会在任何路径上通过 —— 那正是旧缺陷换了个新 kind 的名字 |

`text` 可以是一个串列表,也可以是**一条裸串**(赶时间的人会那么写;拒掉它只是在拒一条读得懂的行)。

**(2) `tests/test_pending_rulings.py`** —— **361 → 378 checks / 0 failed**。
承重的断言是**同一个文件上的一对**:错话在里面读 OWED、错话没了读 DONE;
外加「两句删了一句」仍读 OWED、删掉文件读 UNCERTIFIABLE(并且 detail 要点名 `path_absent` 那个格子)、
目录当路径读 UNCERTIFIABLE、六种写坏的 needle 集合逐一读 UNCERTIFIABLE。

**(3) `tools/agent/mutstand_text_absent.sh`** —— **5 CAUGHT / 0 SURVIVED / control_ok=1**,还原逐字节 **YES**。
本行 `done_when_note` 点名要的那句话在这里兑现:**五发全部是假阳方向**(错误地读成 DONE),没有一发是快乐路径。

- **M1** 删掉存在性前置 ⇒ 被删掉的产物读 DONE(§EV 在写第一行代码之前就点名了这一发)
- **M2** 读不动的文件读 DONE(同一个谎,另一扇门)
- **M3** 空 needle 集合在任何文件上通过
- **M4** 错话还在却读 DONE
- **M5** 只查第一条 needle ⇒ **半截活退休**(最像真 bug 的一发:它在 registry 里每一条单 needle 的行上都是绿的)

`tests/test_mutstand_restore_trap.py` 认得新台(trap / ordering / restore 三条 ok,GH #418 的形状)。

## 2b. ⭐⭐ 顺手撞到的第二件,而它比第一件更贵:**铁律 6 的门被那个 arming switch 弄红了,在一棵零 Lua diff 的树上**

跑 push 前的静态门时,**一棵 `bots/`+`game/` 零 diff 的树**给出:

```
bots/Customize/soak_side.lua: I/O error (couldn't read: No such file or directory)
LUACHECK RED -- iron rule 6 requires 0 warnings on the WORKING TREE.
GATE_EXIT=3  RED
```

当时开工自检正在**另一个进程**里跑。`ls -la` 抓到了现行:`-rw-r--r-- **0** root root 50 ... soak_side.lua`
—— **硬链接数 0**,文件在被打开的同时已经被删掉。

**缺陷**:`bots/Customize/soak_side.lua` 是 gitignored、farm-only 的 arming switch,
**十六个 Lua gate 测试和开工自检会边跑边创建/删除它**;`luacheck bots game` **先走树、后打开走到的东西**。
失效方向是「吵」不是「静默」,但代价不小:(i)`iron rule 6 requires 0 warnings on **the WORKING TREE**`
是**一句关于这棵树的假话**;(ii) 自 GH #213 起这道门坐在 `.githooks/pre-push` 里 ⇒ **它拒绝的是一次 push**,
理由与被推的东西无关,而逃生口 `RULE6_BYPASS=1` 会逼一个本来干净的轮次写下「这是跳过不是通过」。

⭐ **立案句:GH #229 / #243 已经为 python 普查修过同一个竞态,修法是「按名字排除」** ——
`lua_corpus.py` 的 repair (1) 逐字写着 *removes the race at the source rather than making it survivable*。
**唯独这道门没拿到那个修法**,而它是唯一一条**能拒绝 push** 的腿。

**本轮窄修**:`luacheck_gate.sh` 的调用加 `--exclude-files bots/Customize/soak_side.lua`(同一条理由:
那个开关从来不是 corpus)。钉子 `tests/test_luacheck_gate_soakswitch.py` **两半都钉** ——
源码半(命令里真的带着它;只钉源码会被「flag 拼对但放在 `--` 后面」骗过)+ **行为半**
(磁盘上放一个**语法坏掉**的 switch,门仍读 `GATE_EXIT=0`;**控制先跑**:先证明 luacheck 自己对这份内容确实会红,
否则「门还是干净的」什么都不证明)。行为半跑**窄 target** `bots/Customize` 而非默认 `bots game`:
排除写死在脚本里、不是调用方传的 ⇒ 同一条代码路径,而全树一跑 ~13s,在一个未测量的 ratchet 里
正好撞上 GH #616 预算下的 push-gate 超时 —— **一个会把它所防守的门弄红的测试不是防守**。

⚠️ **没买到的那一半(issue 留着的理由)**:`lua_corpus.py` 还有 repair (2) —— **任何别的**文件在
「走树」与「打开」之间消失时,诚实的答案是 **exit 2(没跑成)**,不是一个不同的计数。
**这道门没买这一半**:它对任何 I/O error 仍读 **exit 3(RED)**,而这两件事**许可的下一步动作相反**
(重跑 vs 修代码),在退出码上却逐字节相同。不在本轮做 —— **已开 [harness] GH #633**
(窄修写在里面;**它留 open 的理由正是没买到的这一半**:把 `I/O error` 形状的行与真警告分开,前者走 `verdict 2`)。

## 3. ⭐ 本轮最该被读的一条:那条断言把不该出现的串写成了字面量,于是它出现在了自己正断言其缺席的文件里

「这个 kind 必须穿得过白名单」的断言跑的是**真仓库**,path 指向 `tests/test_pending_rulings.py` 自己,
needle 写的是一句「这个文件里没有的话」——**写成字面量的那一刻它就在那个文件里了**,当场自己红。

这不是一个笑话,它是这个 kind 的 LIMIT **在它自己的测试里兑现了一次**:
`text_absent` 分不清「改对了」和「只是换了个说法绕过 needle」,也不知道 needle 会不会在别处出现;
⇒ needle 要长到唯一,**并且仍然要把文件读一遍**。修法是把 needle **在运行时拼出来**,并把这段经过写进注释,
好让下一个人在写 `done_when` 时先想一想他的 needle 会落在谁的文件里。

## 4. ⚠️ 一条登记的限度,而不是一行发明出来的 registry 行

**此刻没有任何一条活着的 owed 行用 `text_absent`** —— 它的立案案例 `outlatch_check1b_reason`
在这个 kind 存在之前就退休了。房子里的惯例是「一个没人够得到的 kind 什么都不证明」
(`path_contains_all` / `residual` 两处都有这么一条断言),而那两条**当时都有真实的活行**。

这一条没有。**我没有为了让断言变绿去发明一行** —— 那正好是 LIMIT 9 的形状
(registry 是手写的,登记本身就是被要求的动作;一行为了满足工具而写的行,是往里面注水)。
限度写在两处读得到的地方:`tests/test_pending_rulings.py` 的注释,和 `pending_rulings.py` 的 LIMIT 11。
**下一条「把这句写错的话改掉」形状的裁定就是它的第一个真实用户**,写 `done_when` 的人该伸手去拿它。

## 5. registry:19 → 18 行,零行打印「请退休我」

退休行带 `retired_at` / `retired_by` / `outcome`(落地物三件 + 上面 §3 的现场教训 + §4 的限度)。
复读:**18 行,2 行 RESIDUAL**(`hero_domain_scan_2_30_31` / `roshan_pit_daynight_fix`,两条都按 GH #627 的
`residual` 字段挡着退休那一行)、16 行 OWED、**0 行打印「the director should retire this row」**。

## 6. 各组健康度(本轮只巡不判)

四个组本轮都有产出,节奏无洞:批测台 12:20Z(W56 四粒全收 204 局,头号产出是 `recover_verdict.py` 的静默零,
**已由他们自己钉住**)、英雄组 11:14Z(`cmlaneband`,gated 未 armed)、协同组 10:51Z(`buydeep`,gated;
并结清了批测台的 GH #622)、录像组 09:48Z(W55 两条根因上 pin;**并且正确地把 W56 那 8 局
「全是 radiant / 只有 ab 一层」的读数按铁律 4(i-b) 一条都不写进结论**)。
本轮没有需要总监插手的 [bug]/[harness],也没有等待审批的测试集变更。

**成本(转载,零 AWS 动作)**:批测台 12:20Z 报 MTD **`$66.105`**(免费 `budgets`,`refreshed 08:06:09Z`,
**不含 W56**),诚实重建 ≈ **`$68.105`**;`$80` / 刹车 `$90` / 批准线 `$100` **均未跨**。
`$50` 档早已跨且已在 W35/W36 两封邮件里对 owner 报过。本轮不发新的成本声称。

**owner 邮件**:W36 那封已于 09-06 发出,下一封 W37 定在周日 09-13;本轮 `DECISIONS_NEEDED` **+0**。

## 7. 验证(铁律 6)

- `tests/test_pending_rulings.py`:**378 checks / 0 failed**(`RC_EXIT=0`)
- `tools/agent/mutstand_text_absent.sh`:**5 CAUGHT / 0 SURVIVED / control_ok=1**,RESTORE **YES**
  (⚠️ 用法提醒已写进台子的头注:它按 **index** 比,所以在**未 `git add`** 的改动上跑,最后那行 RESTORE 会假红)
- `tools/agent/py_gate.py`:动手前 **84 ran / 0 findings / 0 uncertifiable / EXIT=0**;
  push 时(新测试已在树上)**85 ran / 0 findings / EXIT=0**,多的那条是门自己点名的
  `tests/test_luacheck_gate_soakswitch.py`(未进 manifest ⇒ 照跑,见 §9)
- `tools/agent/luacheck_gate.sh`:见 §9(push 前跑;**本轮 `bots/`+`game/` 零 diff**)
- ⛔ **动态半(`lua5.1 tests/run_tests.lua`,~100min)未跑,不作任何声称** —— 本轮零 Lua 改动

## 8. ⛔ 开工自检:第一次带 `timeout 400` **被砍在 python trunk 那条腿上**(`EXIT=124`),已重跑

第一次的真码是 **124**,不是 0 —— 而 harness 给那个后台任务报的是 **`exit code 0`**。
**「后台包装吞掉真码」本轮第四次兑现**,躲过去的仍然只是那个习惯:我把真码 `echo` 进了输出文件。
这正是上一轮交棒 ⑥ 点名要立的那道守卫,**本轮仍然没有立**(登记,不新立措辞)。
⚠️ **一条本轮新学到的**:上一轮报「约 55 分钟」,所以 `timeout 400` 从一开始就不够;
自检**不能带一个比它自己短的 timeout 跑** —— 那样得到的不是一个结论,是一条被砍在半路的日志,
而它前半截看起来完全正常(anchors OK、promote-atom OK),**砍点恰好落在最容易红的那条腿之前**。

⚠️ 纪律 3 本轮也踩了一发(**第一条命令又是 `| tail -60`**,`routine_selfcheck.sh` 的 §22 守卫当场拒、
`nothing checked`)——**不新立措辞**(09-04 那条逐字覆盖它),登记而已:**拦下它的是守卫,不是我记住了**。

重跑(不带 timeout)的读数见 §9。

## 9. 自检重跑与 push 门读数

**push 门(两次 push,都真跑了)**:`GATE_EXIT=0 CLEAN`(luacheck `bots game` 0 警告)+
`py gate: 85 ran, 0 findings, 0 uncertifiable`。**本轮未用 `RULE6_BYPASS`。**
那个 `85` 比常规的 `84` 多一条,门自己说了是哪条:
`1 new test(s) not in the manifest were run anyway: tests/test_luacheck_gate_soakswitch.py`
—— 新测试**没有进 manifest**,而 `py_gate` 对未测量的测试的默认动作是**照跑**(保守方向)。
实测 **0.14s**,远在预算内,所以本轮**不重跑 `py_gate_measure.py`**(它会重测全部 114 条并改写整张表,
噪声远大于收益;⚠️ 顺带登记一次自己的失手:`py_gate_measure.py --help` **不是** `--help`,
它当场开始测全表,被 `| head` 截断才停 —— 那也是本轮唯一一次与自检并发的重活)。

**开工自检重跑(不带 timeout)** 到 13:19Z 仍在最后一条腿(fast Lua detectors)上,
**真码本轮没有读到 ⇒ 对它不作整体声称**(这正是 §8 的另一面:一个读不到的退出码不许被当成通过)。
已经打印出来的腿逐条登记:

| 腿 | 读数 |
|---|---|
| push gate 上膛 | `core.hooksPath=.githooks`,already set |
| unlanded work | **OK** —— certifiable 窗口内无未落地工作(937 refs,914 条 REFUSED 为不可判,shallow graft 之下不作声称) |
| report cadence | **3 findings**:director 09-07T10:00→13:31Z(3.5h)、strategy 两处(3.6h / 4.5h)。**都在昨天**,本轮四组齐全,不构成新问题 |
| 已发表引用 | 无解析不了的 |
| un-ruled queue | ⭐ **RIDESHARE 7 条待裁**(`hero-41`…`hero-47`),见 §10 ① |
| orphan proposals | **none**(ROWLESS 3 条是别处已裁的,信息性) |
| owed executions | 18 行、2 RESIDUAL、**0 行「请退休我」**(§5) |
| expired waits | 6 份章程 62 个已结 id,**无过期等待** |
| stable anchors | **6 个锚点全 OK**(EXISTS / PINNED / SHIPPED 三条不变量逐个过) |
| promote-atom | **OK** —— 175 个活 gate id、18 个带 PROMOTED 注释,**FROZEN none**(没有活 gate 点名一个已 promote 的 id;pullcad 陷阱本轮不响) |
| trunk health(python) | ⛔ **UNCERTIFIABLE** —— `tests/test_selfcheck_lua_leg.py` 顶爆 120s 预算,**9 个检查没跑成**(**GH #358**,与上一轮同一条,已常态化)。**这不是通过** |
| trunk health(fast Lua) | 本轮收尾时仍在跑,**不作声称** |

## 10. 下次触发

① ⭐ **把 `hero-41`…`hero-47` 七条 RIDESHARE 一次裁掉** —— 自检的 `queue-rulings` 腿**每轮都点名它们**,
而它们的 `director` 字段至今是空的。**裁定内容不难**(owner P4.2 冻结期,合法裁定是 **FROZEN-HOLD**;
七条自己的 `question` 里都逐字写着「本条不请求入集」),**难的是投递**:按 §2.5 一条裁定要同时落到
(i) 每条请求的 `director` 字段、(ii) `test_set.md` 的全文档案、(iii) 该组活 issue 线程。
七条一起做是一节档案 + 七个字段 + 一条评论,**一轮做得完**,而拖着它就是让一条每轮都响的腿继续响。
② **`hero_domain_scan` 九份读数逐份读通** —— 上一轮 ① 原样顺延(**这是那条 RESIDUAL 退休的唯一前置里
总监自己那一半**,另一半 hero-32 / hero-33 卡在 dumper schema 上,不在总监手里)
③ 把 `tests/test_gated_helper_nesting_census.lua` 当 GH #622 的仪器读一遍(上一轮 ②)
④ **GH #358 的 120s 预算要人裁** —— 自检里唯一那条 exit 2 已经常态化(上一轮 ③,顺延)
⑤ **给「后台包装吞掉真码」立一道守卫**(上一轮 ⑥;**本轮第四次现场兑现**,四次都是靠习惯躲过的)
⑥ **`text_absent` 的第一个真实用户**:下一条「把这句写错的话改掉」形状的裁定落地时,
`done_when` 写 `text_absent` 而不是 `manual` —— 这不是一根棒(没有活可做),是一个**该被伸手去拿的工具**
⑦ **GH #633 没买到的那一半**:让这道门把 `I/O error` 形状的行与真警告分开(前者 `verdict 2`),
并在 `tests/test_luacheck_gate_soakswitch.py` 的行为半加第三条

## 11. Token 用量

```
TOKENS total_in=13,526,158 out=63,977 turns=95
```

(统计时刻 13:14Z;其后的收尾回合不计入 —— 铁律 8 的口径。)
