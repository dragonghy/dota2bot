# 总监 2026-09-05T19:xxZ —— **闸 (i) 对整整一波完全失明,失明的方向是「允许更早花钱」;GH #534 已修**;本轮最该被读的是 **§1.2:两个各自平平无奇的缺陷叠起来,让一台跑了 94 分钟、花了 ~$1.09 的机器,在管钱的那道闸面前不存在**

全文档案:`iterations/streams/test_set.md` **§EZ**(最该读 **§EZ.1**)。
`bots/` + `game/` **零 diff**(变异台从文件副本还原,md5 逐位相同);**零 AWS**;
无 promote;owner 邮件本周未发(条目已进 `DECISIONS_NEEDED`)。

---

## 0. 开工自检(铁律 10)

- ⭐ **纪律 3 第一发被工具自己拦下,而拦下它的是本轮的第一条命令**:
  `routine_selfcheck.sh … | tail -60` ⇒ `REFUSED: stdout is a pipe; exit 2, nothing checked`,
  守卫文案自己写着「已复发 5 次,每次都是当轮第一条命令」。改重定向后重跑。
  本轮此后**每一条要读退出码的命令都走 `rc.sh` 或重定向**(章程步骤 0)。
- 真码到手:**`EXIT=3`**,`UNCERTIFIABLE: none`,`legs run 10`,
  findings = `cadence` / `queue-rulings` / `owed-executions` / **`trunk-red(python)`**。
  - `queue-rulings`:唯一一条未裁 = **hero-31**(§4 本轮已裁,复跑 `RIDESHARE: none`);
  - `trunk-red(python)`:**95 passed / 5 failed / 2 uncertifiable**,失败的五条与批测台 18:07Z
    读到的**同一组**(`test_bots_walk_farm_only` / `test_detector_source_constants` /
    `test_lua_corpus_stability` / `test_mutstand_restore_trap` / `test_wkqdmg_domain`),
    **本轮零新增**。⚠️ 「main 是不是也红」这一行**未确立**(要 `git stash` 后复跑),**不声称**;
  - 两个 stable 锚点三不变量 **OK**;promote-atom 约束 **OK**;`FROZEN none`;
    `expired waits` 无;`cadence` 的洞仍是 director 09-04T19:10Z→23:06Z(历史)。

---

## 1. 本轮主活:GH #534 —— gate (i) 的 ruling 3(**[harness],章程 2a 第一优先**)

### 1.1 交来的读数

批测台 18:07Z:`wave_throttle.py` 锚在 **W48(12:24:12Z)** 打 unlock **18:24:12Z**,
而 **Y1 15:16:56Z 上机、花了 ~$1.09**,保守真值 **21:16:56Z**。**工具一个字都没提到 Y1。**

### 1.2 ⭐ 两条缺陷,都不报错

1. 记录扫描器是 `^W(\d+)_wave\.json$` ⇒ `Y1_wave.json` **根本没进候选**;
2. 读法是「`machines[]`,且只读它」—— 而 ref-vs-ref 路径(`aws_run.sh`)是单机,
   记在 `instance` / `instance_actual` 下。

**这两条各自都平平无奇**,叠起来的效果是**一整波对管钱的那道闸不存在**,
而且**失效方向是危险的那一侧**:工具打的解锁比真值早三小时,即**允许更早花钱**。
与「shallow clone 空输出被读成无漂移」「#205 装不上的 linter」「`describe-instances` 答 `[]`」
同一族:**不报错、不少给,只是静静放宽。**

### 1.3 修法(ruling 3)

- 闸 (i) 管的是**钱** ⇒ **任何把计费机器抬起来的记录都开窗**,不管哪套 harness 写的;
  无 `machines[]` 时从 `instance` / `instance_actual` 取,**两个都取**(Y1 的 `instance`
  是那台 167s 就死的、~$0.03,`instance_actual` 才是产数据的那台)。
  **`rerun` 仍不看 —— GH #469 ruling 2 一字未动。**
- **排序一并裁掉**:「波号越大越晚」只在**一个族内**成立(Y1 与 W48 之间根本不是一次比较)⇒
  walk-back **按族各走一次**,跨族取**最晚那台机器**(与 ruling 1 同一个保守侧,高一层),
  每个族的候选连同它自己的 unlock **都打印**。

**实测(真记录,`--now 2026-09-05T19:10:00Z`,`RC_EXIT=3`)**:

```
family candidate : Y1   last machine 2026-09-05T15:16:56Z -> unlock 2026-09-05T21:16:56Z  <- ANCHOR (ruling 3: the latest)
family candidate : W48  last machine 2026-09-05T12:24:12Z -> unlock 2026-09-05T18:24:12Z
WAVE_THROTTLE: THROTTLED (exit 3) -- do not launch. Wait 7616s.
```

**与批测台手算的 21:17Z 一致 —— 那个手算现在有工具替它做。**
⇒ **批测台下一轮不必再走「不要只读工具那一行」的临时指令**,读工具即可。

### 1.4 非空洞性(变异台)

`tests/test_wave_throttle.py` **44 → 55 检查,0 失败**;新加的第 6 节
**直接用 W48 的真 slate + Y1 的两个真单机块重建 09-05 那个瞬间**(问 18:30Z),旧工具在那里答 UNLOCKED。

| 变异 | 打掉的那条腿 | 结果 |
|---|---|---|
| M1 | 正则退回 `^W(\d+)` | **7 红** |
| M2 | 去掉单机键回退 | **6 红** |
| M3 | 跨族取第一个候选而非最晚 | **2 红** |

三发全 **CAUGHT**;每发都从**文件副本**还原,md5 `c3633ab5655d268c7f1928dc0cce4795` 逐位相同,
还原后复跑 55/0。

⚠️ **诚实边界**:本修法**没有**给发波路径加登记义务 —— 一个**根本不写 wave 记录**的路径,
闸 (i) 仍然看不见,**而且仍然不会报错**。

---

## 2. GH #535 裁定 —— P4.1「买到了两样,第三样没有量具」

1. **胜率 + 局数:收下,登记为 P4.1 的正式读数**。`arm(swap-average) = 0.550`(44/80),
   分层 fwd **0.625** / rev **0.475**,side 项 **+0.075**(该估计量已消侧偏 ⇒ 反号归铁律 **4(i-c)**,
   登记不作否决理由)。两臂经 `batch_run.log` 核过是真的两棵树,不是 GH #527 那种默认 AI 互打。
2. ⛔ **gpm:没有量具,不是「测了没效应」**。`Gold per min: 0` **800/800**、`Level: 1` 800/800、
   `KDA 0/0/0` 800/800,引擎自己写着 `Not submitting stats. Both teams must have human players.`
   **`.log.stdout` 那个修复治不了它**(零是引擎输出里的零)。
   ⇒ 任何人**不得**把 gpm 那一格写成读数,**也不得写成「无差异」**。
3. **⇒ P4.1 = 部分完成**,「标尺波排在任何 arm 波之前」的排序条款**释放**;
   ⛔ **不许复发同一形状的标尺波**,直到记分板问题被当成**量具任务**解决 ——
   再发一遍按构造买回来的还是同样的三分之二,每次 ~$1.09。
   主会话 17:42Z 写在 `DECISIONS_NEEDED` 里的那句「下一波读数会更干净」
   对**胜负口径**成立、对 **gpm 不成立**,已就地追加更正。

---

## 3. Y1 记录里请求的两条裁定(批测台明写「Ruling requested」)

- **(甲) ref-vs-ref 路径的 on-demand:批准为路径域的声明式例外**,不是对 GH #158 的改判。
  依据是这条路径**只在两个方向都跑完后同步一次 S3**(无逐局上传)⇒ 被抢占 = **全损、零可恢复**;
  免疫 ~$0.42/波,而近期抢占实测 W47 丢 2/4、W43 丢 4/4。
  **失效条件写死**:这条路径一旦有增量上传,例外**当场作废**。例行波/镜像波**一律仍是 spot 优先**。
  已进 `DECISIONS_NEEDED` 供 owner 追认,**不阻塞发波**。
- **(乙) 批测台改 `aws_run.sh`:批准,并给长期边界**。那 25 行只动目录权限 + 一道拒绝门
  (`ab_guard`),**测量语义逐字未动**,且**首飞就干了活**。⇒ 边界收窄为:
  **可以改它自己的发射器,条件是 (i) 不碰测量语义(局数/timescale/并行度/镜像方向/ref 解析)、
  (ii) 波次记录里声明、(iii) 静态检查过;碰测量语义仍须先交总监。**

---

## 4. hero-31:APPROVED-SCAN(投递到三处,不是写在报告里)

`wkbonefight` 的条件 (a) 域请求 —— 它自己写着「先扫档案,不要为它单发波」,
四列读数**没有一列需要新对局** ⇒ 批准的是**遍历**不是波次;**P4.2 冻的是入集不是取证**。
**与 hero-2 / hero-30 并成同一次遍历**(hero-30 的裁定 15:5xZ 已写「与 hero-2 并成」,本轮把第三个挂上)。

- 投递 (i):`iterations/queue.json:hero-31.director`(**被裁方读的那个字段**);
- 投递 (ii):`iterations/streams/test_set.md §EZ.4`(全文档案);
- 投递 (iii):`iterations/owed_executions.json:hero_domain_scan_2_30_31` ——
  **§DR 的常驻义务**:不挂在任何一波上,没有 `harvest_obligations` 的行可以写它。
  `done_when` = `path_exists: iterations/reports/replay-check/domain_scan_hero_2_30_31.md`
  ⇒ **产物路径是裁定的一部分**,因为 owed 腿只读得出「文件在不在」。
- **status 故意留在 `pending`**:队列里已有 **34 行**落在 `pending_rulings.py` 的已知词汇之外
  (GH #317),总监不该再造一个 `approved_scan` 拼写。

---

## 5. 新 promote 原子 `tp_response_releases_need_commit`(`tpdying`/`tpdead` ⟸ `tpcommit`)

**今天没有任何东西是坏的**(三者同 armed)。拦的是 **promote 那一天**:两个闸址都在
`J.GetTpCommitDefendDesire`(`bots/FunLib/jmz_func.lua:8655`)**函数体内**,
而它第一句是 `if not J.IsSoakCandidate( 'tpcommit' ) then return nil end`(`:8657`)⇒
单独 promote 其中一个,promote 掉的代码**落在真实对局里永远关着的闸后面** ——
**一次什么都没改的 promote,记成一次落地的改进。**

**此前没人看得见的原因**:依赖是**结构性嵌套**不是合取(读闸的工具看不到),
而它被写下来过**两次,两次都是散文**(函数注释 + `test_set.md:3625`)。
**`zusstatic` 被 armed 半个月测不到任何东西时,带的也正是散文。**

**变异台**:把 `tpdying` 闸址改成 PROMOTED 形状 ⇒ `promote_atoms.py` **exit 3**,
报错逐字点名两个 id;从文件副本还原后 `jmz_func.lua` md5 `96582ff2990736dc7f1c507cf9ecbdfd`
**逐位相同**,复跑 exit 0,三条原子全 ok。

---

## 6. 单独收下协同组 §EY.5:P2 取证波的**成对约束**

在 owner P2 自己钉的那一帧,shipped / 单 armed `stayattr` / 单 armed `staysrc` **三次都是 false**,
**两个都 armed 才 true**,全语料 `flips_pair_only = 1` 且就是那一帧(变异 M12 直接打这条腿)。
⇒ **任何单臂隔离波在那一帧上读到的零,每一次都是正确的零**,而波次会写成「杠杆无效」。
登记为常驻否决行 `iterations/owed_executions.json:p2_pair_arm_stayattr_staysrc`
(`claimable: false`,没有完成态)。⚠️ 与 lanefix 教训冲突之处**不粉饰**,裁定见 §EZ.6:
共同 arm 时**必须在波次记录里声明为一个 PAIR**,负读数归给这个对,不许拆归。

`staysrc` 本身:**FROZEN-HOLD**(P4.2 冻结期唯一合法裁定;协同组本轮**没动 `queue.json`**,正确)。

---

## 7. 巡检、成本、健康度

- **五个流本轮都有产出**:批测台 18:07Z(收 Y1)、录像组 18:38Z、协同组 17:xxZ、
  英雄组 17:01Z、总监 16:00Z→本轮。**无掉队组。**
- **零 AWS**:本轮未做任何计费调用,**不对 MTD 作新声称**(批测台 18:07Z 读到的是九月 MTD **$19.597**,
  且 W48/Y1 两波尚未落账 —— 那是章程写死的 4.3–11.3h 滞后,不是省钱)。
- **闸 (i) 现状**:解锁 **21:16:56Z**(工具打的,不再是手算)。
- **patch 检查**:低频项,本轮**未做**(顺延)。
- **`DECISIONS_NEEDED`**:+3 条(P4.1 gpm 缺量具的更正与拍板题;P4.2×P2 结构互锁;on-demand 例外追认)。
- **⭐ 本轮新发现的一条结构互锁**:P2 的取证按 §6 需要**同时 arm** 两个 id,
  而 P4.2 的冻结让一个已退集、一个进不来 ⇒ **解冻前 P2 的取证路径是关着的**。
  保守默认(冻结优先,P2 桌面证据继续买)已执行,例外与否交 owner。

---

## 8. ⛔ 本轮未达成的(不许被上面盖过去)

1. ⭐ **P4.2 的「每轮 ≥2 个判定完结」= 0,未达成。** 原因不辩解:工作单元给了 GH #534,
   一个**失效方向是「允许更早花钱」**的量具缺陷,而下一个批测台轮(~21:0xZ)正要按它的读数
   决定发不发波。**下轮的前两个**:`tpdying` / `tpreach`
   (`verify_coverage.py` 的 verify=0 长尾里 narrat 最低的两条);
   `midtp` 仍是唯一 BLIND SPOT,在录像组 GH #521 (丙) 手上,**不抢**。
2. **trunk python 侧 5 红**,与批测台读到的同一组,零新增;`git stash` 复跑那一步**本轮没做,不声称**。
3. **录制代价第二通道本波反号**(W48 每槽局数:8 个录制槽全部低于对照槽,缺 ~7%,
   立论那次是 +2.33 局)⇒ **登记,未裁**:要裁需要第二波同向读数。
4. `$0.90` 自动失效常数**仍未重裁**;GH #528 降级后的长期规则**未写**;
   `run_batch.sh` 的 `.log.stdout` 修复(`25d237b2`)**至今没在农场跑过**(下一波是它的首飞)。

---

## 9. 下次触发

1. **P4.2 优先**:`tpdying` / `tpreach` 两条判定完结(先做,再做别的)。
2. `text_absent_done_when_kind`(GH #523,owed registry 里**总监自己**那一行,连续第三轮未取)。
3. trunk 红逐条(#506 先做)+ `git stash` 后单独复跑 python 半边,确立「main 是否也红」。
4. `$0.90` 重裁 / GH #528 长期规则 / 录制代价第二通道等第二波读数。
5. patch 缺口 P3 / GH #449/#410/#436/#285 / `ckpush`(有时限)。

**距离 P4.2 解冻线(armed ≤ 20)仍差 41(当前 61,本轮未变)。**

---

## 10. 本次会话 token 用量(铁律 8)

```
TOKENS total_in=10,951,966 out=53,240 turns=75
```
(统计时刻之后的收尾回合不计入。)
