# 总监 2026-09-03T16:05–16:2xZ

**一个工作单元:裁 `strategy-39` / `wandbleed2`(GH #437)—— ADMITTED,测试集 57 → 58,自 W43 起生效。**
裁定全文 `iterations/streams/test_set.md §DU`。**零 AWS 调用;`bots/` 与 `game/` 逐字节零 diff。**

**本轮真正的产出是两件,ADMITTED 只是第一件**:
① 这条在「下次触发第一项」上顺延了**四轮**,本轮把它买齐了 —— 上一轮**明说**不裁并给了理由
(六到七项带裸退出码的独立读数是整整一个工作单元,「半个裁定比没有更坏」),那不是懈怠;
② **§DU.4:本轮的裁定 LIMIT 与本轮的 trunk 红是同一个形状** ——
一个**没人能重新导出的分母**,一处在档案里(那个「155」),三处在测试里(`10946` / `16088` / `619`)。

---

## 一、自检(铁律 10 + 工作流第 0 步)

⚠️ **工作流第 0 步(上一轮刚立)本轮自己没遵守**:开工第一条命令写成了
`bash tools/agent/routine_selfcheck.sh 2>&1 | tail -60`,`§22` 守卫当场自拒:
`REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.`
**纪律 3 第二十八发,守卫第二十二次拦下,仍是当轮第一条命令。**
⇒ 上一轮把「第一条命令走 `rc.sh`」写进章程,**下一轮(本轮)就被违反了** ——
这直接给那条条款自己的措辞背书:**它是习惯不是门**。改走重定向后台重跑。

**自检真退出码 `EXIT=3`**(`legs run 9` / `FINDINGS = cadence trunk-red(python) trunk-red(lua)` /
**`UNCERTIFIABLE: none`**)⇒ 本轮**没有「没人看过」的那一侧**。逐腿:

| 腿 | 读数 |
|---|---|
| unlanded | `OK: no unlanded work in the certifiable window`(certifiable refs 28 / 738,shallow) |
| cadence | **2 条 GAP**:hero 3.8h(09-02T22:54Z→09-03T02:45Z)、strategy 4.0h(09-02T16:25Z→20:25Z);门限 3.5h,越线 ≤0.5h,**不升级** |
| 未裁 queue | 进场 `OTHER: 1`(`strategy-39`)、`RIDESHARE: none` ⇒ **§DT.0 的分类缺陷本轮第二次复发**(见 §三.4) |
| domain watch | `strategy-33 tiny corpus 0 STILL BLOCKED`(照旧) |
| orphan proposal | `ORPHAN_PROPOSAL: none`,`ROWLESS: 3`(§BR/§BT/§BV,信息性) |
| owed executions | 进场 `registry rows: 0 / OWED_EXECUTION: none`;**出场 1 行 OWED**(本轮登记,见 §四) |
| expired waits | 三组 INFO 全在历史块外 ⇒ **无发现**(上一轮修的「记录 vs 等待」判别本轮保持零误报) |
| stable anchors | `stable-v1` / `stable-v2` **三条不变量全 ok** |
| trunk health | **红,见 §三** |

---

## 二、裁定:`wandbleed2` ADMITTED(57 → 58,自 W43 起生效)

裁定全文 **`test_set.md §DU`**;机器可读的投递在 **`queue.json:strategy-39.director`**
(`ruling=ROUTED_RIDESHARE / ADMITTED`,`wave=W43 起的任一常规波次`,`executor=replay-check`,`status=admitted`)。
七项独立读数全部裸退出码,不在这里重抄(§DU.1 一张表);只把**总监自己加的两格**摘出来:

### 2.1 载体项的价:这次是**结构性的零**

§DT.3 立的规矩是「便宜不能靠感觉说,要有一个数」。本条的数比 `ckpush` 那次更硬:
`carrier_terms.py --arm <58>` 自判 `wandbleed2 kind=generic`,
输出 `11 hero-scoped, 47 generic, 0 unresolved => 7 term(s)`,裸读 **`RC_EXIT=0`**;
`TERMS` 逐字不变(`chaos_knight,crystal_maiden,lion,obsidian_destroyer,skeleton_king,spirit_breaker,zuus`)。
⇒ **不是「在紧的那条边上是零」,是「在任何边上都是零」** —— 本条入集**不必重算 `BEST min-per-term`**。
⚠️ 但载体供给自己的账不因此变好:W41 起飞时 `[2601,3000]` 上 `>=2/term` 已搜出 **0**、窗口被迫右移。
那是 GH #408 的账,**下一波仍要自己重算**。

### 2.2 ⭐⭐ 那个「155」复现不出来,而它和本轮的 trunk 红是同一个形状

§DS.6 承重的那句是「`bots/` 里另有两条同样朝『无操作』失效的闸 —— **全仓 155 个
`IsSoakCandidate` 调用点里的 3 个**」。独立复核:

| 树 | 原始 grep 命中 | 去掉定义行与纯注释行 |
|---|---|---|
| 提议自己落地的树 `77e18be9` | **157** | **146** |
| 本轮 HEAD `d0874472` | **159** | **148** |

**四个数里没有一个是 155**;**分子 3 在两棵树上逐字复现**
(`fieldsip:5198` / `teambrain:6773` / `wandbleed2:9585`,全仓恰好三行)。
⇒ **承重的一半对,装饰的一半复现不出来。** 那个比值不参与任何判定 ⇒ **入集不受影响**,
但它必须被登记,理由见 §三.2:**本轮 trunk 红是同一个形状的三发**。

### 2.3 为什么是 ADMITTED 而不是 ADMITTED_CONDITIONAL

对照 `ckpush`(§DT.2):那一条**形状与域价钱指向相反**,所以裁定必须连方向一起裁。
本条两者**同向**,而且域价钱买到的比形状多 —— 它把要写进代码的常数从 issue 建议的 **2000**
抬到 **4000**(依据:语料里攻击者仍活着时的最大距离 **3011.7**,CM 4.9% HP 挂 `modifier_maledict`、
巫医在 3011 外;取 2000 会拿走那一帧的魔杖,取 4000 挡掉 **0/101** 对)。
**提议自己把最贵的那格买了,没有留给总监一个方向问题。**

### 2.4 抬进裁定的两条红线

- **§DU.5 收割前必读**:本条是 **NARROWING** ⇒ **`wandbleed` 触发计数下降本身不是负面信号**;
  承重的阴性面(保留下来的触发,4000 环内活敌人数必须 ≥1)与主判据**同等必读**;
  域为零按 `DOMAIN-NOT-REACHED` 退回,**不得**读成「无效应」;两个分层各自登记读数(铁律 4(i-a)/(i-b))。
- **§DU.6 promote 关口红线**:promote 本 id 的动作**不是**把它从 armed 串里删掉,
  **是删掉 `jmz_func.lua:9585` 那一行**。闸朝「无操作」失效 ⇒ 只删串不删代码,
  **收窄会在 promote 当天静悄悄取消而没有任何东西变红**(`pullcad` 的倒像)。
  `mutstand_wandbleed2.sh` 的 M7 守着失效方向,**但没有任何仓库级棘轮守着「promote 时删了那一行」**,
  同族的 `fieldsip` / `teambrain` 到那一天会踩同一个坑。

---

## 三、trunk 红:三条,分两族,**本轮不修**(理由与下一单元的第一项)

### 3.1 读数(裸)

- **python**:`81 passed, 2 failed, 2 uncertifiable`。红的是
  `tests/test_chain_member_census.py`(`RC_EXIT=1`)与 `tests/test_threshold_chain_census.py`(`RC_EXIT=1`)。
  两条 uncertifiable(`test_rc_wrapper.py` / `test_selfcheck_lua_leg.py`)是 120s 预算没跑完的容器速度腿,非本轮引入,**2 不是通过**。
- **Lua 快检测器**:`RED test_gated_helper_nesting_census.lua`(74 个文件里的 1 个)。
- ⚠️ **未 stash ⇒ 不声称 main 也红**(进场时工作树干净,但这句仍按工具自己的措辞据实写)。

### 3.2 第一族(2 条,**冻死的分母**)—— 与 §DU.4 是同一个形状

| 断言 | 钉住的值 | 本轮实读 |
|---|---|---|
| `test_chain_member_census.py:241` | `and/or chains scanned == 10946` | **10950** |
| `test_chain_member_census.py:243` | `locals with an initializer == 16088` | **16094** |
| `test_threshold_chain_census.py:360` | `2 frames of 619` | `frames=620 late=3 max=850.1` |

**归因(可查,不是猜)**:三个数在 **09-03T01:30Z**(`846fb455`,上上轮总监自己那次)被钉住;
此后落地的 `36e7bb3`(hero `zeusaghs5`)与 `ea4efee`(strategy `arbheart`)都动了 `bots/`,
`f_260903_101254_cm_farm_stealcamp.lua`(hero 10:48Z)给语料加了一帧。
⇒ **三个数钉的都是保证会长大的东西,14 小时内被本队自己的例行工作推红。**

**判据(§DU.4,已进档案)**:**一个分母只有在「它不该动」的时候才配被冻住。**
钉一个每次落地都会变的分母,买到的不是棘轮,是**一个按日程变红的闹钟**,
而闹钟响的时候没有人能从红色里读出「是不是真出事了」。
修法方向:**要么钉住不该动的那个量(比值 / 集合 / 分子),要么把分母写成「≥ 且解释增量」而不是「==」。**

**本轮不修的理由**:正确的修法是**改断言的形状**,不是把三个数字改大 ——
后者保证明天再红一次,而且会把这条判据自己埋掉。那是一个独立工作单元。
⇒ **下一单元第 ① 项**(见 §六)。

### 3.3 第二族(1 条,**不是同族,而且它是对的**)—— `arbheart` 的嵌套闸

```
a NEW gate-inside-a-gate appeared:
  arbheart,campexit | Think | J.IsCampSwitchSafe | campdanger | bots/mode_farm_generic.lua
```
这条**不是冻死的分母**,是一个**正确开火的检测器**,而且它问的问题正是 `pullcad` 家族的问题:
**未 armed 时,内层 helper 是不是它刚加入的那个合取的单位元?** 若不是,
**单独 arm 外层 id 测到的是一个 no-op**,而 `check_armed_wiring.py` 仍会把它叫做 WIRED。
主体是协同组 13:30Z 刚落地的 `arbheart`(GH #455),**尚未进入测试集,尚未提请入集**。
⇒ **本轮不裁**(没有入集提议可裁),但**在 `arbheart` 提请入集之前必须先有这个答案** ——
已写进 §六交棒,并按铁律 5 开 issue 通知协同组(见 §五)。

---

## 四、§DR 登记:录像组那一棒进了 `owed_executions.json`

`wandbleed2` 条件 (a) 唯一还缺的那一面是**把 GH #437 的帧冻成 fixture**
(`spot_20260902_154755_slot4`,seed 2877,CM t≈473.5–474.4)。
本地语料的两个阳性证人吃的是**死掉的** ember spirit 的残留,立案帧的残留来自**走开的**施法者 ——
**同一个谓词、同一个答案,但本地没有第一种**。

这条**不属于任何一波**(语料是 W39 的归档,不是某一波的收割义务)
⇒ 按 §DR 它在 `W<N>_wave.json:harvest_obligations` 里**没有行可以缺席**,
所以除了投递进 `queue.json:strategy-39.director.executor` 与 §DU.7,
**还加了 `iterations/owed_executions.json` 一行**:

- `id=wandbleed2_cond_a_frame`,`executor=replay-check`,`issue=GH #437`
- `done_when`:`path_exists tests/fixtures/f_260902_154755_cm_wandbleed_residue.lua`(**裸读得出**)
- 落地后立刻验:`pending_rulings.py --owed-only` ⇒ `registry rows: 1` /
  `OWED wandbleed2_cond_a_frame GH #437 executor=replay-check` /
  `done_when: … does not exist yet`,`rc.sh` 裸读 **`RC_EXIT=3`** ⇒ **这条腿现在每轮替它举手。**
- ⚠️ 照抄 LIMIT 11:**它 grade 产物不 grade 工作** —— 读不出那一帧是不是 t≈473.5、
  是不是 CM、残留是不是来自走开的施法者。内容那一面由录像组落地时给
  `tests/test_replay_437_wandbleed_source.lua` 新增的断言买(该帧上 `J.IsWandBleedSourcePresent`
  必须答 false,且 4000 环内活敌人数为 0),**这一句写在 `done_when_note` 里**。

---

## 五、成本、沟通、issue

- **零 AWS 调用、零 S3 访问、零 EC2。** 九月 MTD 照抄批测台 15:15Z 自报 **`$7.056`**
  (免费 `budgets` 通道,快照 `refreshed 2026-09-03T04:41:39Z`);批测台自己的围栏算术
  `7.056 + W41 2.15 + W42 0.90 + 补跑机 0.65 = $10.76 ≤ $80`。
  **围栏 $60 / 刹车 $90 / 批准线 $100 全部未触及**,不欠解释行。
- **无邮件**(本周 08-31 已发,每周最多 1 封);`DECISIONS_NEEDED.md` 本轮**无新增**
  (§DU.6 的 promote 红线已写进档案与裁定,不需要 owner 决定)。
- **周四 ⇒ 效率台账(仅周日)跳过。**
- **四组本轮均有产出,无空转。**
- **发表(排在 push 之后,见 §七)**:GH #437 追评裁定摘要;
  新开 `[harness]` issue 一条(§3.2 的冻死分母,两条 python 红)与
  `[strategy]` issue 一条(§3.3 的 `arbheart` 嵌套闸,`arbheart` 入集前必答)。

---

## 六、下次触发

① **§3.2 的两条 python trunk 红**(冻死的分母)—— 改断言的形状,不是改数字;带变异台。
   **这是本轮唯一明说推给下一单元的活,不许再顺延**(它已经是「连续被报告、零轮被修」那条曲线的第二发)。
② **自检 `RIDESHARE` 分类腿**(§DT.6 起**第五轮顺延**,本轮**第二次**对该裁的行没响)——
   `strategy-39` 逐字写着「搭车、零 AWS 增量、不申请专波」,仍被分进 `OTHER`。
③ **§3.3 的 `arbheart` 嵌套闸**:协同组答完「未 armed 时内层是不是单位元」之后裁它的入集。
④ GH #410 的带期限保守默认 / GH #436 若仍无执行者则写进 `DECISIONS_NEEDED`。
⑤ GH #285 第三十二轮 / patch 缺口 P3。

---

## 七、铁律 6 与收尾

(收尾时回填)
