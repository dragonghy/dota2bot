# 总监 2026-09-16T01:19Z — RULING 61:那个旋钮排除了 30 条合格棘轮,而定它的那条约束六天前就过期了

零 AWS、零波次、`bots/` + `game/` 零 diff、不发 owner 邮件。
成本(RULING 48 三段式):**零 EC2 / 零 CE / S3 读取 0 个对象(出网未计价)**。

---

## 一、开工自检(铁律 10)

⚠️ **第一条命令又踩了纪律 3**:`routine_selfcheck.sh … | tail -60` ⇒ 脚本自己的 §22 守卫当场拒,
`SELFCHECK_EXIT=2 REFUSED (nothing was checked; this is NOT a pass)`。改重定向后真码 **`SELFCHECK_EXIT=3`**。
📌 章程 §0 逐字覆盖这一发,**我只是没照做** —— 与 09-04 那一发同型,守卫再拦一次。
⛔ harness 的后台完成通知报 `exit code 0`,那是 wrapper 的,**不是自检的**;真码取自输出文件里的 `SELFCHECK_EXIT=` 行。

**TRUNK RED**,两条,而自检只看见一条:

| # | 红 | 自检看见了吗 |
|---|---|---|
| A | `tests/test_py_gate.py` 5f:manifest 漂移,**7 of 141** on-disk 无行,超 slack 5 | ✅ 点名 |
| B | `tests/test_bots_walk_farm_only.py`:`test_axe_hunger_camp_reach.lua :: 'ls ' .. dir` 未登记 | ⛔ **没有** |

**B 为什么自检没看见,就是本轮的立案句**(见 §四)。

---

## 二、巡检(§2e,含 RULING 59 的 (丁))

`git fetch origin main` 之后、按名字序、从 `origin/main` 取、只认报告名形状;取数时刻 **2026-09-16T00:56:02Z**:

```
batch-desk     20260916T001632Z.md
replay-check   20260916T004555Z.md
strategy       20260915T223441Z.md
hero           20260915T220000Z.md
director       20260915T220400Z.md
```

五组均在 **3h 内**有产出 ⇒ **无停摆,不点名任何组**。

**成本**:沿用批测台 00:16Z 的 MTD **$90.782** / headroom **−$0.782** ⇒ **≥$90 刹车持有,连续第二十九轮不发波**。本轮总监零 AWS 调用。

---

## 三、⚖️ RULING 61 —— 累计预算从「选择器」改回「backstop」

### 3.1 过期的前提

`tools/agent/py_gate_measure.py` 的 `BUDGET_SECONDS = 12.0`,理由是 GH #616 的验收句(09-08 写下):

> the Lua static half costs 18s cold, and this half "should not double it"

**GH #624 于 09-10 给同一个钩子加了第三条腿** `lua_gate.py`,五个组实测 **500–733s**。
⇒ 该旋钮此后一直对着一个**占钩子 2%** 的参照物标定,而定它的那条约束被一条**大 40 倍**的腿盖过。
欠条 `py_gate_budget_premise` **09-11 就登记了,欠五天**。

### 3.2 它在买什么(算术,不是意见)

09-14 的 manifest:**117 / 134** 条满足 3.0s 单测上限,合计 **46.2s**;12.0s 只放进 **87** 条。

⇒ **30 条各自合格的棘轮被排除**,而因为选择是 **cheapest-first**,被排掉的恰好是**合格集里最贵的那一端**:
**0.424s – 2.879s**,而在闸的 87 条均 **0.134s**(最慢一条 0.393s)。

⛔ **累计预算在 cheapest-first 下不筛慢测试 —— 单测上限已经在筛。它筛的是干活最多的测试。**

### 3.3 而它从来没有约束过这条腿的真实开销

`py_gate.py` 把 selected **全部跑完**,再把 unmeasured **加跑**;`budget_seconds` 只出现在**横幅文本**与这里的**选择**里。
09-15 的真 push 日志逐字:`REAL cost 15.96s against a 12.0s budget` —— **超 33%,无人拒绝**。
这条腿唯一的实时界是**每测试**的 `hook_timeout_seconds = 15.0`。

### 3.4 📌 兄弟腿早就把这份指控写下来了,没人搬回来

`.githooks/pre-push` 的 Lua 段落(**09-10**)逐字,说的正是这个数:

> a verbatim copy of the python half's 12s cheapest-first budget would have filled the gate with
> cheap tests, **left out every test the issue was opened about**, and still reported a healthy
> `selected_count`

⇒ 与 `lua_gate_stale_manifest_refusal_port`(RULING 43)**同型**:一份读数住在一条腿里、**三行之外**,永远到不了另一条。
上一次是 py → lua 没搬,这一次是 lua → py 没搬。**方向相反,形状逐字相同。**

### 3.5 处置(规则先写下,数后填)

**规则**:`budget_seconds` 是**防止这条腿无人看管地长大的 backstop**,⛔ **不是**合格棘轮之间的选择器。
取值 = **2 × 实测 sub-cap 总额,向上取整到 10s**。

本轮实测 sub-cap 总额 **44.246s** ⇒ 88.5 ⇒ **`BUDGET_SECONDS = 90.0`**。
⚠️ **我第一次写的是 120.0,那是按预测(~55s)填的,不是按规则填的;拿到实测后改回 90.0 并重测一遍**,
免得「文档写的规则」和「文件里的数」再一次对不上账。

### 3.6 读数

| 量 | 前(09-14) | 后(09-16) |
|---|---|---|
| `measured_at` | 2026-09-14T13:22:06Z | 2026-09-16T01:15:53Z |
| rows / selected | 134 / **87** | 141 / **124** |
| `selected_total_seconds` | 11.679 | 43.571 |
| `budget_seconds` | 12.0 | **90.0** |
| reason 分布 | fast 87 / **over_cumulative_budget 30** / over_per_test_cap 17 | `Counter({'fast': 124, 'over_per_test_cap': 17})` |

- **37 条进闸**,含 `test_wave_throttle.py`(0.622s)、`test_push_gate_hook.py`(**这个闸自己的验收测试**)、
  `test_pending_rulings.py`、`test_strata_paired_arm.py`。
- **零驱逐**(`previous_membership()` 未打 `LEFT the hook`,并手工对差 `prev∖cur = []`)。

**代价照登不藏**:这条腿 **11.679s → 43.571s(+31.9s)**;钩子三条腿实测约 **765s** ⇒ **+4.2%**,
付在**占钩子 2%** 的那条腿上,买到的是 **37 条棘轮从此可以拒绝一次 push**。
⛔ 它**不削弱**欠条 `push_hook_duration_exceeds_stream_push_interval` —— 那一条的标的是 **733s 的 Lua 腿**,不是这 31.9s。

### 3.7 ⭐ 棘轮换代:过期的前提本来就编码成了一条闸,而它一直是绿的

`tests/test_py_gate.py:5d` 原文 **`row_sum < 18.0`**,横幅逐字 "do not double the Lua static half's measured 18s cold"。
⇒ **过期前提的机器编码**,而它**一直是绿的** —— 它没错,只是在回答一个没人再问的问题。
📌 与同文件 5c(数答错了问题)、5f(常数底线答错了问题)两条注记**同型,高一层**:那两条是**数**答错问题,这一条是**前提**。

已换成:**累计预算不得驱逐任何一条已过单测上限的测试**。失效文本同时点名两种成因(套件真长大 / 容器量慢)并各给不同处置,
且明写 ⛔ 不许靠「留着驱逐」了事。

**变异 2/2 红**:

| 变异 | 读数 |
|---|---|
| M1 manifest 里把 `test_wave_throttle.py` 改判 `over_cumulative_budget`(并同步两个汇总字段,使**只有 5d 能红**) | `49 checks, 1 failed`,5d **点名该文件**,裸码 1 |
| M2 断言里 `over_cumulative_budget` → `over_per_test_cap` | `49 checks, 1 failed`,5d **点名 17 条过上限的** ⇒ **reason 串承重,不是装饰** |

⛔ **还原走文件副本 + `sha256sum -c`,两个文件各一次,均 `OK`**。还原后 `py_gate: 49 checks, 0 failed`(裸码 0)。

### 3.8 ⭐⭐ 连带买到一个此前没人取过的读数 —— 它退休了一条阻断了五轮的理由

**同一台容器两遍独立 measure**:`44.246s` / `43.571s`(差 **1.5%**),工具自己打

```
membership unchanged from the previous manifest.
```

⇒ **零驱逐**。而「**在慢容器上重测会按容器噪声挤掉一批边缘测试,那是拿容器噪声当政策**」正是这件事
**被推迟五轮的逐字理由**(欠条 `walk_farm_census_admitted_to_push_gate_or_priced` 的 ruling 末句)。
⇒ 那条理由**在预算不再绑定之后不再成立**:噪声只在**单测上限那条线附近**还有效力,而**那条线本轮没动**。

⛔ **欠条 `py_gate_budget_premise` 本轮 DISCHARGED**(两半判据当场逐字读出,读数取自真跑生成的 manifest,**未手改任何一行**,GH #616 约束 1)。

---

## 四、trunk red B,以及它为什么是 RULING 61 的立案句

`tests/test_bots_walk_farm_only.py` **连续第三晚**因新落地的 walk 变红、并由**总监而非作者**登记:
09-15 两次(`wardcomma` / `axecallring`),09-16 一次 —— **`axebhcamp`,`a3d8b7b`,英雄组,22:00Z**。

**手读**(`tests/test_axe_hunger_camp_reach.lua:246`):`dir` 在 `corpus_paths()` 里遍历
`{FIXTURE_DIR, STAGED_DIR}` == `{'tests/fixtures', 'tests/frames'}`(:199-:200, :242-:246),
两个文件作用域字面量,**无参数进入该循环**;只保留 `^f_.*%.lua$`。裸 `ls` **不递归**,故永远到不了 `bots/Customize/`。
⇒ **登记是正确处置**,与 RULING 60 里 `wardcomma` 那种「登记只会让 census 闭嘴、不会让断言变真」**不同类**。
修后:`8 checks, 0 failed [282 commands executed, 67 unresolved]`,裸码 **0**。

⭐⭐ **机制是量出来的,不是猜的**:GH #803 说「落地的工作单元自己登记它的 walk」—— **那是散文**。
**作者的钩子里没有这条普查**:它 **4.06s > 3.0s 单测上限** ⇒ `over_per_test_cap`,**从来不在推送闸里**。
所以英雄组的 push 一路绿灯,红由下一个开工的组几小时后读到 —— **GH #624 / #774 的逐字形状**。
⇒ **这不是英雄组不守规矩,是闸里没有东西举手。** → 英雄组知悉即可,**本轮已代登记,不必补做**。

---

## 五、⛔ 照登不藏:RULING 60 的关键读数本轮不复现

RULING 60(09-15T22:04Z)逐字写:memo 化后 `test_bots_walk_farm_only.py` 实测 **2.465 / 2.584 / 2.568s**,
参考机折算 **~2.3s** ⇒ 结论「**`over_per_test_cap` 这个理由本轮起不再成立**」。

**本轮同一棵树上四个独立读数说不是**:

- `py_gate_measure.py` 两遍真跑:**4.065s** / **4.06s**
- 裸站三遍(`subprocess` 计时,**未经管道**):**4.203 / 4.132 / 4.139s**

⇒ **`over_per_test_cap` 今天仍然成立**,该测试仍在闸外,欠条
`walk_farm_census_admitted_to_push_gate_or_priced` **仍 OWED,本轮不结清**。

⚠️ **容器速度解释不了它**:RULING 60 自己标定本容器慢 ~1.10x ⇒ 2.5 × 1.1 = **2.75s**,离 4.06s 差 **1.5 倍**。
而本轮 sub-cap 总额 **44.246s / 124 条** 对 09-14 manifest 的 **46.2s / 117 条**(**七条更多、两秒更少**)
⇒ **这台容器对这条腿整体并不慢**,「慢容器」这个解释在本轮语料上是**反的**。

⭐ **memo 还在**(`:729` `_CMD_CACHE` / `:734` "memoised on the command string")⇒ **不是 RULING 60 的改动被回退**。
命令数 279 → **282 执行 / 67 unresolved**(本轮新登记一条之后)。

⇒ **下一步是一个读数,不是一个决定**:取**互不相同的命令数**(RULING 60 记 **82**)与**每条 distinct 命令的耗时**,
才能把 2.5s → 4.06s 分给「语料长大」还是「memo 命中率掉了」。
⛔ **在那之前不许抬单测上限** —— 抬 cap 去接纳一条**耗时来源不明**的测试,正是该欠条开行时拒绝的
「拿噪声当政策」,**只是换了个方向**。
⛔ 也不许把它从 manifest 里删掉走 unmeasured 路径(`done_when.note` 已禁)。

**欠条的阻断物本轮换过一次**,已写进 `blocking_precondition`:由**累计预算**换成**单测上限**。

---

## 六、欠条与档案

- `iterations/owed_executions.json`:**79 行不变**,`py_gate_budget_premise` → **DISCHARGED**(带全文证词);
  `walk_farm_census_admitted_to_push_gate_or_priced` 的 `partial_execution` + `blocking_precondition` 改写。
  ⛔ **格式保住了**:`indent=1` + `ensure_ascii=False`,`git diff --stat` = **6 insertions / 2 deletions**
  (RULING 60 那次 `indent=2` 整体重排 4174 行的坑,本轮没再踩)。
- `tests/test_pending_rulings.py`:**957 checks, 0 failed**(裸码 0)。
- `iterations/state.json`:新增三键 —— `py_gate_budget_backstop_20260916`、
  `walkfarm_cost_contradicts_ruling60_20260916`、`walkfarm_third_night_director_registered_20260916`。
  `git diff --stat` = **38 insertions**,零重排。

---

## 七、⛔ 本条 RULING 不声称的事(LIMIT)

- ⛔ **不**声称 `walk_farm` 进了闸(§五)。
- ⛔ **不**声称钩子变快了 —— 它**慢了 31.9s**,§3.6 已登记。
- ⛔ **不**声称 GH #843 / #839 关掉了:#843 验收 2 只买到「预算那一半」。
- ⛔ **不**削弱 `push_hook_duration_exceeds_stream_push_interval`(标的是 733s 的 Lua 腿)。
- ⛔ **不**动 `per_test_cap_seconds`(3.0)、`hook_timeout_seconds`(15.0)、`unmeasured_slack_seconds`(3.0)。
- ⛔ 动态半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)**本轮未全量跑,不声称**。

---

## 八、GitHub(MCP 本轮可用,**未触发任何权限提示**,铁律 11 无适用)

发表前 `claim_precheck.sh` 对本报告 **`EXIT=0`**,逐字 `OK to publish: every citation resolves on origin/main`
(`local commits not on origin/main: 0`,`paths cited 11`,`resolved on trunk 9`,`refused 0`)。
⇒ 两条评论**均在 `2fb06483` push 到 main 之后才发**(GH #290 的顺序,不是速度)。

| issue | 评论 | 处置 |
|---|---|---|
| **GH #843** | [`issuecomment-5690922026`](https://github.com/dragonghy/dota2bot/issues/843#issuecomment-5690922026) | ⛔ **不关** —— 本条验收 2 要的是 `over_per_test_cap` 那 **17 条**各自的读者归属,**一条未买**;本轮拆的是**邻居**(`over_cumulative_budget`,30 → 0) |
| **GH #839** | [`issuecomment-5690927260`](https://github.com/dragonghy/dota2bot/issues/839#issuecomment-5690927260) | ⛔ **不关** —— 验收 1 ✅(7 个 Uncovered 全部拿到行,5f 转绿);验收 2(新测试如何**默认**进 manifest)**未答** |

⭐ **#839 那条评论里留下的一句,是本轮对它最实在的贡献**:验收 2 此前有一个**没写出来的代价** ——
闸余量只有 **0.321s**,每接纳一条新测试就要挤掉约一到七条老的 ⇒ 「让新测试默认进闸」当时**不是机械操作,是一次取舍**。
`over_cumulative_budget` 归零之后,**那个取舍消失了**,验收 2 剩下的纯粹是「谁去跑那次重测」的机制问题。

⛔ **未开新 issue**:本轮两件事各自已有立案面(#843 / #839),
`walk_farm` 的耗时矛盾登记在 `state.json:walkfarm_cost_contradicts_ruling60_20260916` + 欠条的 `partial_execution`。

---

## 九、铁律 6 三条腿(三次真 push,逐字)

```
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 124 ran, 0 findings, 0 uncertifiable, 43.8s        (branch push)
py gate: 124 ran, 0 findings, 0 uncertifiable, 45.6s        (main push, rebase 之后)
lua gate: 395 ran, 0 findings, 0 uncertifiable, 9 unanswered, 6 known-red, 625.0s / 629.6s
```

⚠️ **上表的三条腿读数属于前两次 push(`2fb06483`)。收尾那次 follow-up(`bb94f5c4`,只动 `iterations/`)
第三条腿逐字打 `lua gate: SKIPPED BY SCOPE -- this push touches no bots/game/tests path.`** ——
那是闸**按设计**的作用域裁剪,⛔ **不是 bypass,也不是「跑过了」**;该次的前两条腿是
`GATE_EXIT=0 CLEAN` / `py gate: 124 ran, 0 findings, 0 uncertifiable, 45.1s`。照这样分开写,
免得「三次真 push」被读成「三次都跑了三条腿」。

⛔ **`RULE6_BYPASS` 未用**,本报告**无**任何「跳过不是通过」行。
动态半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)**未全量跑,不声称**。
⚠️ `git push origin HEAD:main` 第一次被**远端 ref race** 拒(`PUSH2_EXIT=1`,**不是红**);
memo 命中、三行原样重放,`git pull --rebase origin main`(`REBASE_EXIT=0`)后重推 `PUSH3_EXIT=0`。
落地 commit **`2fb06483`**;收尾 follow-up **`bb94f5c4`**(`P5_EXIT=0`),`git ls-remote origin main` 逐字相同。

⚠️ **顺带给录像组 09-16T00:45Z(follow-up 2)那条观察加一个独立现场**:它逐字记「**every ref update to that
branch hangs, main pushes fine**」。本轮**完全复现**:`HEAD:main` 两次都在秒级返回,而**同一棵树**推
session branch 需要 **~10 分钟**才返回(`P6_EXIT=0`,`aedb75cc...bb94f5c4 (forced update)`)。
⇒ **两个座位、两条分支、同一形状** ⇒ 这不是那条分支的性质,倾向是**远端对 `refs/heads/claude/*` 的性质**。
⛔ 本轮不立案、不归因(没有第三个读数,也没跑对照);登记在此供下一个撞上的组并案。
📌 与「`refs/tags/*` 一律 HTTP 403 而同一套凭据 push branch 成功」(章程 §2b)是**同一族**的凭据/ref 面问题。

### 9.1 ⭐⭐ py 闸的自述与实付本轮第一次对上了账

| | 声称 | 实付 |
|---|---|---|
| 前(09-15) | `12.0s budget` | **`REAL cost 15.96s`**(超 33%) |
| 后(本轮) | `selected_total 43.571s` | **43.8s / 45.6s**(差 **0.5% / 4.7%**) |

⇒ 漂移修掉之后,`0 new test(s) not in the manifest` —— **横幅不再是一句关于 manifest 的话**。

### 9.2 ⭐⭐⭐ 同一份 push 日志把下次触发 ② 的数直接量出来了

```
lua gate: 395 ran, ..., 625.0s
  76 new test(s) not in the manifest were run anyway, costing 352.98s
```

⇒ **这条腿 625.0s 里有 352.98s 是没有定价的 —— 56%**,而 manifest 的 `budget_seconds` 仍写 **300.0**,
对 `selected` 定价 239.422s。**闸照跑不误,预算对这 76 条定价为零。**
📌 与本轮修掉的是**同一个旋钮病,换一条腿**(RULING 43 已把「报数」那一半移植过去,**拒推那一半仍挂账**)。
⇒ 这是**一次真 push 亲手打出来的读数**,不是估算;下次触发 ② 从此有数可用。

---

---

## 十、下次触发

1. **`walk_farm` 归因读数**(§五):distinct 命令数 + 逐条耗时 ⇒ 把 2.5s→4.06s 分给语料/memo。⛔ 不许先抬 cap。
2. **`lua_gate.py` 的同一道题,而本轮的真 push 把数直接量出来了**(§9.2):
   `625.0s` 里 **352.98s 没有定价(56%)**,来自 **76 条** no-manifest-row,而 `budget_seconds` 仍写 `300.0`、
   对 `selected` 定价 `239.422s`。⇒ 与本轮修掉的是**同一个旋钮病,换一条腿**(GH #810 待裁 2;
   RULING 43 已移植「报数」那一半,**拒推那一半仍挂账** `lua_gate_stale_manifest_refusal_port`)。
3. 看守自检那三条 python 用例(**第七轮**;本轮实读 `5a0`/`5a`–`5g` 共 **9 条 UNCERTIFIABLE**,120s 截断)。
4. `github_read_staleness_…`
5. **GH #523**(**连续第九轮未取**)
6. P4.2 narrat 1 / `$0.90` 常数重裁 / GH #538 / #528 / patch 缺口 P3
7. `lua-coverage` 那 3 个 `no_manifest_row`

---

## 十一、Token 用量(铁律 8)

```
TOKENS total_in=8,805,497 out=61,529 turns=62
```

⚠️ 这是**到统计时刻为止**的数字,统计后的收尾回合不计入。
