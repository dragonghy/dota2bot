# 总监报告 2026-08-22T05:29Z(第三十八次触发)

**处理对象**:`[harness] #92` + AG.4 第 (1) 步(`VICTIM_HP` 扫描)。
按 08-21T23:0xZ 自己立的规矩,这条连续四轮被 issue 挤掉后**单独排一次不处理 issue 的触发**。
裁定全文:`iterations/streams/test_set.md` §AJ(含 AJ.7 的 `#92` 结案)+
`iterations/state.json:roamstale_VICTIM_HP_SWEEP_20260822T050xZ`。
下一轮/其他组读那两处即可,**不需要回翻本报告**。

**`bots/`/`game/` 零改动。未启动任何计费资源。** 唯一支出是只读 S3 拉取语料
(见 §5),这是**总监首次产生非零 AWS 支出**,按铁律 1 逐笔申报。

---

## 1. 先纠正 AG.4 自己的一个前提:那不是一份语料

AG.4 把「`#92` 的扫描」和「`roamstale` 域内成本的扫描」合并成一个工作单元,
理由是**同一个常数 `0.40`**。常数确实同一个,**语料不是,而且不可能是**:

| | run 前缀 | `cand` 串的 A-only |
|---|---|---|
| `#92` 跑的 | `spot_20260820_1609xx` / `1807xx`+`1808xx` | **`capmono`** |
| `roamstale` 域内成本住的 | `spot_20260819_1808xx` / `2009xx` | **`roamstale`** |

`roamstale` 已 shipped,**根本不在前一份语料的候选串里**(`#92` §4.2 自己写了
「`l1trade` 在两臂都 armed」)。**我第一次就跑错了**,是新工具继承自
`roamstale_null_channel.py` 的臂身份守卫 `exit 2` 当场拦下的 ——
**那道守卫今天第一次在真实使用中挡住一次错误归因**,不是装饰。

## 2. 保真度

`--selfcheck` 在 T=0.40 上把缓存重扫的 episode 集与 `roamstale_null_channel.collect()`
**逐字段比对**(游戏/窗口/时刻/actor/victim/队伍/armed/四个结局位):
arm A **985**、arm B **1132**,**全等**,合计 **2117** —— 与 AG.3 的
`episode_count_correction`(2117,不是登记的 1855)逐位吻合。
登记行逐位复现:域内 **n=409/417、供给比 0.98、`hit_hero` −7.3pp、`no_dmg` +7.3pp**
(登记 −7.2/+7.1,配对口径末位差)。

## 3. 主结果:域内成本**通过**扫描 —— `#92` 的死法在这里不成立

| T | nA | nB | **供给比** | `hit_hero` A−B | `no_dmg` A−B |
|---|---|---|---|---|---|
| 0.25 | 207 | 227 | 0.91 | −7.4 (t2.62) | +6.6 (t2.09) |
| **0.40(登记)** | 409 | 417 | **0.98** | **−7.3 (t2.70)** | **+7.3 (t2.70)** |
| 0.55 | 708 | 706 | 1.00 | −3.3 | +3.5 |
| 0.70 | 1112 | 1124 | 0.99 | −0.8 | +0.9 |
| 0.85 | 1490 | 1537 | 0.97 | −1.5 | +1.3 |
| 1.01 | 2098 | 2156 | 0.97 | +0.2 | −1.0 |

`#92` 那边供给比 **1.26 → 0.83 单调穿过 1**,读数 **−17.5 → +9.3pp 跟着翻号**。
这里供给比**全程 0.91–1.00**(镜像装置,两臂同种子同阵容),读数**不翻号**,
而是随阈值放宽**单调衰减到零** —— 分母没动,是**分子集中在紧带里,被不适用的
episode 稀释掉**。方向也对得上机制:真门是绝对致死比较(`jmz_func.lua:6932`),
**代理越紧越接近真门**,而信号恰好在最紧的两档最强。

**⇒ AG.4 第 (1) 步完成,域内成本 `PROVISIONAL` → 成立;阶梯解锁到第 (2) 步(上 `roamreach`)。
第 (3) 步(把 `roamstale` 退回 gate 后面)仍不许跳步。**

**一条必须一起引用的诚实边界**:不能用「绝对超额 episode 数应守恒」加强它。
`#92` §6 的去重时钟让各带**非嵌套**(放宽 T 会**删掉**已登记的低血 episode),
所以 T=1.01 的 2098 条**不包含** T=0.40 的 409 条,守恒检验在这套装置上**没有定义**。

## 4. 两条新发现(都不是 issue 要求的,是打印全表才看见的)

**(甲)`killed` 指标在这份语料上不可用。** 基线腿(两臂**基线侧**,gate 全关、
跑逐字相同的出厂代码)读 **−12.5 / −11.8 / −9.3 / −7.1 / −5.8 / −3.4 pp**,
全程同号、随 T 单调,**比 `killed` 自己的 ARMED A−B(−1.7pp @0.40)大一个量级**;
同表的 `hit_hero`/`no_dmg` 基线腿贴着零。AG.3 写「域内基线腿 +0.4pp,干净」——
**那句话只对承重那两个指标成立**。无追溯损害(AG.3/#41/#45 的承重数都在
`hit_hero`/`no_dmg` 上),**但 #41 的「域内转化率」正是 `killed`,差一点就承重了**。

**(乙)域外读数是供给伪影。** 域外供给比 **0.73/0.75/0.87/0.79/0.73/0.70**,
全程 ≠1 且不收敛 —— 那正是 `#92` 的死法;基线腿同时也脏
(`hit_hero` −9.5、`no_dmg` +5.9 @0.40)。AG.3 撤回域外 `+7.7pp` 原本只靠**多重比较**论证,
现在多了一条**独立且机械**的理由,**过度充分,不需复议**。

**(丙,顺带)`0.40` 不是一处的自由参数,是至少 4 处独立复制**:
`roam_conversion.py:61` / `lanekill_commit.py:86` / `find_kill_windows.py:28` /
`detect.py:945`,外加 `detect.py:608` 的 `OVERCHASE_VICTIM_HP = 0.45`(同族、取值不同、无人解释)。
**最该在意的是 `find_kill_windows.py`** —— 它不产生读数,它**选帧做 fixture**,
所以这个未溯源常数定义了**整个执行核验计划的取样面**。登记为下一个 `[harness]`。

## 5. 验收与成本

- **`luacheck bots game --formatter plain`:EXIT=0,0 警告**(容器初始无 Lua 工具链,已自行
  apt+luarocks 安装)。
- **`tests/run_py_tests.sh`:7/7**(新增前 6/6)。
- **`lua5.1 tests/run_tests.lua`:1082 tests, 0 failures(EXIT=0)** —— 与英雄组 04:00Z 读到的
  基线**逐位相同**(本轮 `bots/`/`game/` 零改动,该 runner 只加载 `.lua`,对本次 diff 本就无
  适用对象,仍照跑并核对基线)。**运行耗时 ~48 分钟 vs 01:00Z 记录的 ~20 分钟** ——
  归因是我自己:同一容器里并发跑了 16 个 run 的 dumper sweep 把 CPU 抢干净了。
  **给各组的操作事实:重活与整套 Lua 测试不要并发,否则你会以为套件挂死了。**
- **rebase 后重跑(推送时 `origin/main` 已前进到 `ad10ef6`,录像组 05:05Z + 协同组 03:30Z)**:
  `luacheck` **EXIT=0**、`run_py_tests.sh` **7/7**、
  **`lua5.1 tests/run_tests.lua` 1088 tests, 0 failures** —— 与协同组 03:30Z 自报的 1088
  **逐位相同**(+6 来自他们新加的用例)。**`state.json` 冲突按「两边键都保留」解**
  (stage2 202 键 / stage3 202 键 → 合并 **203** 键;唯一各自独有的分别是
  `sschase_ROLE_HEAL_GH45_20260822` 与本轮的 `roamstale_VICTIM_HP_SWEEP_20260822T050xZ`,
  **逐键核对过没丢**)。
- **两次变异两红**:**M1** 把实现改成「按最松阈值扫一次再过滤」(=`#92` §6 点名的错误实现)
  → **3 红**,报错直接打印 `tight=[76.4] loose=[76.4]`;**M2** 去掉 `finally` 的全局还原
  → **2 红**,打印 `0.4 -> 0.99`。还原后全绿,`diff -q` 与变异前**逐字节相同**;
  **变异还原走文件级备份,不走 `git checkout --`**(08-22T01:0xZ 的 (甲) 教训)。
- `state.json` 201 → **202** 键。
- **成本**:未启动任何计费资源;只读 S3 拉 16 个 run 的 `.dem`(两份语料各 40 局),
  **egress ≈ $0.06 + 请求费 ≈ $0.001**。批测台自报 MTD $18.587(00:06Z),
  加本轮 ≈ **$18.65**,$45 围栏余 ≈ $26.35。**不占 $50 档**,但按铁律 1 逐轮申报。

## 6. 交付物

- `tools/batch_test/behavioral/roam_filter_coupling.py`(只读,无计费面):
  按 **ARMED A−B** 估计量扫 `VICTIM_HP`,**每个 T 全量重扫**(去重时钟不可分离),
  并排打印**四个指标各自的基线腿 NULL**、**供给比**、**每局 episode 数**;
  两道 `exit 2` 守卫(臂身份 / 登记 T 必须在扫描里),**今天都真的挡住过东西**。
  `#92` §7 的三项验收要求(供给比、每局绝对次数、阈值)**内建到每一行**;
  末尾的 `READING RULE` 是**看数之前**写进源码的。
- `tests/test_roam_filter_coupling.py`(4 节 9 例)。
- `iterations/reports/director/roam_victimhp_sweep_20260822.txt`(完整原始输出)。

## 7. 体系巡检 / 例行

- **四组本轮均有产出无空转**(batch-desk 04:06Z、hero 04:00Z、replay-check 04:05Z、
  strategy 01:30Z)。批测台 04:06Z 报告的「85% 归档局的基线腿与今日的树不同、
  26 个波次的树已永久丢失」**本轮未处理**,登记为下轮候选(它影响跨波次可比性,
  但不影响本轮结论 —— 本轮两臂**同一份语料内部**对照)。
- **`#98`/`#101` 定级、`#100` 补推、`capmono` 的 (a) 免费重算** 本轮均未做(单议题触发)。
- **今日周六;效率台账明天(08-23,周日)要做。铁律 8 的 token 口径本轮顺带定掉了**
  (§AJ.8):两组怀疑的「统计了容器内全部 transcript」**不成立** —— 本容器
  `~/.claude/projects/` 下**只有 1 个 transcript**,就是本会话。**真问题在别处**:
  `total_in` 的 **97.96%** 是 `cache_read_input_tokens`(21.12M / 21.56M,uncached 仅 312),
  即 `total_in ≈ Σ(每轮上下文大小)`,**随 turn 数近似二次增长**,跨组不可比。
  ⇒ 台账主指标改用 **`out`**(逐轮相加、不重复计数),`turns` 作次指标,
  `total_in` 照报但只比派生量 `total_in/turns`(平均上下文大小);缺报记缺报,不记 0。
- patch 已分片(P3 优先),本轮不重复。
- `DECISIONS_NEEDED.md` **不新增**(本轮裁定全在自主授权内);**本周 owner 邮件仍为零**,
  周日随台账一并发。

## 8. 下次触发

1. **周日效率台账**(口径已由 §AJ.8 定掉,直接按 `out` / `turns` / `total_in÷turns` 做);
2. `[harness]`:`0.40` 代理的 4 处副本收敛进 `test_detector_source_constants.py` 的 REGISTRY
   (§AJ.5;`find_kill_windows.py` 那处最要紧);
3. `#98`/`#101` 定级 + `#100` 是否已补推;
4. 免费重算 `capmono`(#72)的 (a) 取 ARMED A−B(8 run / 16 vs 16);
5. 批测台 04:06Z 的「归档局基线腿漂移」定级。

## 9. token 用量

```
TOKENS total_in=21,563,306 out=130,932 turns=156
```

**按本轮 §AJ.8 刚定的口径读**:可比的是 `out=130,932`;`total_in` 的 97.96% 是
`cache_read`,派生量 `total_in/turns ≈ 138k` = 平均每轮上下文大小。
本轮 turn 数偏高的已知原因:两次 16-run 语料 sweep + 一次跑错语料重来 + 等待 ~48 分钟的
Lua 套件时的轮询。
