# 总监 2026-09-05T21:5x–22:3xZ

**一句话**:按 owner P4.2 完成**两个判定完结**(`tpdying` / `tpreach` 退回出集,armed **61 → 59**)——
**但本轮真正该被读的不是那两个 id,是我核这两条时顺手核出来的一件事:
`dota2bot-batch` 这个预算没有任何过滤器,它量的是整个账户,而账户上另一个项目的一台机器
已经连烧了 17.3 小时** ⇒ 我们的刹车线、发波围栏、owner 的告警邮件,读的都不是我们自己的账。

全文档案 `iterations/streams/test_set.md §FB`。`bots/`+`game/` **零 diff**,**零 AWS 花费**,无 promote。
**P4.2 产出指标:判定完结数 = 2(达成;上一轮是 0)。距解冻线(armed ≤ 20)还差 39。**

---

## 一、自检(铁律 10)与纪律 3

第一条命令走 `rc.sh`(章程第 0 步),**没有 `| tail`** —— 纪律 3 本轮**未复发**。
⭐ **上一轮 §EZ.8 记的「trunk-health(python)那一腿超时未打印 ⇒ 那一侧我没看过」本轮不复现**:
放后台跑满 **~73 分钟**后拿到**真码 `RC_EXIT=3`**,`legs run 10`,**`UNCERTIFIABLE: none`**。
(写报告时它还在跑,我先按「空缺不是通过」写了一版;**它随后跑完,这一节按真码改写** ——
记这一笔是因为**「还没跑完」与「跑完了是 3」在报告里长得不一样,而我差点交出前者**。)

`FINDINGS (exit 3)`:**`cadence` / `queue-rulings` / `owed-executions` / `trunk-red(python)` / `trunk-red(lua)`**。
`NOT RUN (inside a leg)`:`tests/test_rc_wrapper.py`、`tests/test_selfcheck_lua_leg.py`。

- **`owed-executions` 这一条是本轮自己造的**:我新登记的两行按定义就是 OWED(第三节)。
- **`trunk-red(lua)` = `test_corpus_scale.lua`**,与批测台 21:1xZ 报的是**同一条**(GH #538),**零新增**;
  报错逐字点名 `tests/test_staybottle_inflight_regen.lua:223` 把 fixture 数写成字面量 `109`
  —— 即 GH #106 / #127 那个「下一个 fixture 落地就变红」的缺陷,**不是本轮改动引起的**
  (本轮 `bots/`+`game/` 零 diff,也没动 `tests/`)。
- **`trunk-red(python)`** 与上一轮同一组,**零新增**。
  ⚠️ 自检自己写了诚实边界:**「main 是不是也红,这一行不能确立,要 `git stash` 后重跑」——
  那一步本轮没做,不声称。**(批测台 21:1xZ 用结构性论证结清过同一个问题:
  它那轮工作树对 `bots/ game/ tests/ tools/` 与 `origin/main` 逐字节相同;
  **我这轮不能照搬那个论证**,因为我改了 `iterations/` 下三个文件 —— 虽然那三个都不是 python 测试读的东西。)

其余各腿:

| 腿 | 读数 |
|---|---|
| unlanded work | `OK: no unlanded work in the certifiable window.` |
| stable anchors | `stable-v1` / `stable-v2` **两个都 `EXISTS ok`**,三条不变量未报异常 |
| promote atoms | `FROZEN none`(没有活闸点名已 promote 的 id) |
| orphan admission | 1 条(`§BV id=campexit`,存量,非本轮新增) |
| owed executions | 入轮 **5 行 OWED**;本轮 **+2**(见第三节)⇒ 收轮 **7 行** |
| cadence / queue-rulings | 报 FINDING(存量) |

---

## 二、主活:两个判定完结(`tpdying` / `tpreach` 退回出集,61 → 59)

选取**不是我挑的**:上一轮 §EZ.9 已把「下轮的前两个 = `tpdying` / `tpreach`」写死
(`verify_coverage.py` 的 verify=0 长尾里 narrat 最低的两条;`midtp` narrat 0 但它是录像组
GH #521 (丙) 的棒,**不抢**)。本轮照单执行。

`verify_coverage.py` 本轮读数:**armed 61 / 扫 159 份报告 / 有机器可读 VERIFY 行的 id = 21**。

- `tpdying`:§A' **2026-08-19** 入集,armed **17 天**,verify **0**。
- `tpreach`:§BC **2026-08-24** 入集(总监自写自批),armed **12 天**,verify **0**。

### ⭐⭐⭐ 本轮唯一值得被别人读走的一条:退集的**理由**与上一轮的两条**不同**

上一轮退 `zusstatic` 的判据是**条件 (a) 结构上买不到**(句柄在八个世界里没有一个对,加局无用)。
**这两条不是。它们的 (a) 买得到,而且验收形状在入集那天就写好了:**

| id | 入集当天写下的 (a) 验收形状 | 现状 |
|---|---|---|
| `tpdying` | §A'.3 约束 3:**必须用行为检测器,不许用 gpm/xpm**;点名两个 ——「响应 TP 落地后 10s 内死亡率」「落地后仍钉在 DEFEND 的帧数」 | **两个检测器从没被建过** |
| `tpreach` | §BC.4:`tp_channel_death.py` **已经算了 `near`**,**只差按 mode 分层**;连降级读法都写了 | 工具在树上(`--help` 本轮跑通),**从没有人对任何一波跑过它** |

⇒ **失效的不是证据,是投递。**

**为什么这份义务会掉:** 铁律 §2.5(GH #413 加固)说「裁定要落到被裁方读的那个字段上」,
**它覆盖的是「裁定」**。而 (a) 验收义务是**「批准入集」这个动作的附随物** ——
`queue.json` 上那一行是**申请方**开的、**在入集被批准的那一刻就 resolved 了**,
录像组从来不是那一行的被裁方,于是这份义务**没有任何一行可以被写进去**。
与 GH #332 / #413 的立案句逐字同型:**「对着 N 条自审、N 条全中、答案是对的,而第 N+1 条没有地方可以缺席。」**

**量出来的规模:armed 61 条,有 VERIFY 行的 21 条 ⇒ 40 条 armed id 的 (a) 今天没有任何一行替它举手。**
这一条**单独解释**了 P4.2 立项引用的那个数字(13 天零 promote):不是没人干活,
是每一条卡住的裁定都卡在 (a)(W34 台账原话),而 **(a) 的义务结构上是不可见的**。

### 退集**不销毁**买 (a) 的能力(先例不是推理)

(a) 要求 armed 腿 vs baseline 腿在同一帧上翻转 ⇒ 必须有 armed 过它的波。**已经有了**:
W39/W40/W41 与 W48/W49 的 dump 里两条都 armed(W49 于 21:19:57Z 起飞、`--ref` 钉 `066219d6`,**含这两条,不作废**)。
与 `stayattr` **09-05 当天的先例逐条同型**:§EX.1 退集,录像组同一天照样在 W48 上买到了 (a1) WORKING。
⇒ **退集冻的是它在未来每一波里稀释读数的那一格,不是取证。**

### 核验读数(跑出来的)

- 串 **61 → 59**,**540 → 524 字节**,md5 `824ec2842e…` → **`572b60753699bc68b2ad5ec9020f2512`**。
- `carrier_terms.py --arm <新串>` ⇒ **`CT_EXIT=0`**,`12 hero-scoped / 47 generic / 0 unresolved => 8 term(s)`,
  **`TERMS` 行与 W49 逐字节相同** ⇒ **选种解空间不受影响**(两条都是 generic)。
- `check_armed_wiring.py --cand <新串>` ⇒ **`ARM_EXIT=0`**,`all 59 armed ids wired on HEAD`。
- `promote_atoms.py` ⇒ **exit 0**,`tp_response_releases_need_commit`(subject `tpdying`/`tpdead`,prereq `tpcommit`)
  **一字不动**:**退集不是 promote**,该原子读的是树里的闸址、不读 arm 串。`tpdead`/`tpcommit`/`tpgap` **留在集内**。
- ⚠️ **上一轮 §EX.6 的教训本轮主动查过**(「退集一个 id 绝不能顺手把守它的断言一起退休」):
  `test_carrier_terms.py` **66 checks / 0 failed**,`test_pending_rulings.py` **231 / 0**,
  `test_gate_claim_consistency.lua` **16 tests / 0 failures** —— **本轮没有出现 §EX.6 那种连带红**。

---

## 三、交棒:两条 (a) 义务这次是**机器可读**的

不新增规则,用现有 registry(`iterations/owed_executions.json`,自检第 9 条腿每轮举手):

| 行 | executor | `done_when`(裸读得出) |
|---|---|---|
| `a_evidence_tpdying` | 录像组 | `path_exists: iterations/reports/replay-check/a_evidence_tpdying.md` |
| `a_evidence_tpreach` | 录像组 | `path_exists: iterations/reports/replay-check/a_evidence_tpreach.md` |

两行都**零 AWS 增量**(语料是已落地的 W39–W49 dump,不需要任何新波次),
`done_when_note` 里写死了「什么样的内容才算买到」,并各自重申了入集当天的约束
(`tpdying` ⛔ 不许用 gpm/xpm;`tpreach` 的撤退 mode 那一格**只报数不设阈值**,
因为那是 GH #3 花 −15 GPM 学会不要压的东西)。
⚠️ **`path_exists` 只判产物存在不判内容**(LIMIT 11)—— 已写进 note,**不假装它更强**。

**⛔ 通用化(给全部 40 条自动开 owed 行)不在本轮范围**,已立案 **GH #540**
(`[harness] An admission's condition-(a) obligation has no row anywhere`)。
⭐ **按 §EX.11 的教训执行了「先拿掉号 → push → 开 issue → 回填」**:草稿里**不写猜的号**
(上一轮初稿顺号写 `#529`、实际拿到 `#531`,而 `claim_precheck.sh` 对尚不存在的号**无话可说**);
本轮实际拿到的是 **#540**,**顺号会写成 #539(已被录像组占用)** —— 同一发又中一次。

**⑫ 铁律 6 补款(GH #290)本轮遵守**:发表前把草稿存文件跑 `claim_precheck.sh` ⇒
`local commits not on origin/main: 0`,**先红一次**(`MISSING section test_set.md §A` ——
它把 `§A'.3` 里的撇号当分隔符,解析成不存在的 `§A`;⭐ **这是工具的一条新盲区:
`§A'` 在 `test_set.md` 里是 `### A'.` 形式的真章节,不是 `## §` 形式**),
改成引用字面标题后复跑 ⇒ `clean` / **`OK to publish`** / `PRECHECK_EXIT=0`,然后才发。
**GH #35 / #159 追评均已发出,GH #540 已建**(本轮 MCP 可用,未触发铁律 11)。

---

## 四、⛔⛔ 顺手核出来的那件事:**我们的刹车量的不是我们的账**

复核批测台 21:13Z 那条「外来实例」时,我**不带过滤器**独立查了一次,并顺手读了预算的结构:

1. **那台机还在跑**:`i-0ce7ddf08ea24cc7f` / `c7a.16xlarge` / spot / `running` /
   `LaunchTime 2026-09-05T05:02:24Z` / `Name=fft-cache-v1-attempt3` / **`Project=final-table-trainer`**
   ⇒ 到 22:2xZ **连续 17.3 小时**。
2. ⭐ **账户上有两个预算,而且两个的 `CostFilters` 都是 `null`**:

   | 预算名 | 限额 | ACTUAL | FORECAST | CostFilters |
   |---|---|---|---|---|
   | `FFT monthly budget` | $150 | **$19.597** | **$150.822** | **`null`** |
   | `dota2bot-batch` | $100 | **$19.597** | **$150.822** | **`null`** |

   **两行逐位相同,因为两个预算量的是同一个东西:整个账户。**

⇒ **`check_costs.sh` 的 MTD、`wave_fence.py`(闸 (iii))、$45/$60 围栏、$90 刹车、$100 批准线,读的全是这个账户级数字。**

- **(甲)** 那 17.3 小时按 `c7a.16xlarge` spot 价带($1.008–1.210/h)⇒ **已发生未落账 ~$17–21,且以 ~$1.1/h 增长**。
  一旦落账,`dota2bot-batch` 的 ACTUAL 从 $19.6 跳到 ~$38–41 ⇒ **我们的例行波会在自己一分钱没多花的情况下被自己的围栏停掉。**
- **(乙)** owner 会收到写着 `dota2bot-batch` 的 $50/$80/$100 告警,**而它报的不是 dota2bot 的账**(三档此刻都还没跨)。
- **(丙) ⛔ 存量决策题「九月燃烧率会顶穿 $100」里,`ForecastedSpend` 那一路作废。**
  那张表把 AWS 的 forecast 当成与批测台独算**互相独立**的第二条路径 —— **它不独立,它是账户级的**;
  今天 forecast 从 $119.286 涨到 **$150.822** 的那 $31,方向上正是这台 c7a 带来的。
  **批测台独算那一路($4.51/天 × 30 ≈ $135)仍然是我们自己的,不受影响** ⇒ (A)/(B)/(C) 三个选项的代价数字按 $135 重算。

**⛔ 我没有做任何处置**:那台机带别人的项目标签,终止既越权又不可逆;**我也不主张它不该跑**。
`dota2bot-agent` 也没有 `budgets:Modify*`,加过滤器只能 owner 点。
**已写进 `DECISIONS_NEEDED.md`(本轮 +1 条,三个互相独立的问句),并已推送通知给 owner。**
**在 owner 回复之前按保守侧执行**:继续用账户级数字当刹车(它只会让我们更早停,不会更晚),
并要求各轮报告把「本台自己的花费」与「账户数字」**分两行写**。

⚠️ **诚实边界**:我只看得见实例和标签,**看不见它在做什么**;「17.3 小时」是 `LaunchTime` 到本轮的时差,
**不是**「它还需要多久」。**本轮零 AWS 花费** —— 读数全走免费的 `budgets` + 两次 `describe-instances`,
**没有**为这条付 Cost Explorer 的 $0.01。

---

## 五、巡检 / 存量

- **W49 在飞,状态正常**:四台里 **1 台 `running`、3 台 `terminated` 且 `StateTransitionReason = "User initiated"`**
  —— **不是 `Server.SpotInstanceTermination`**,即自毁路径而非抢占。⇒ **不是泄漏、不是掉臂**;
  收割是批测台下一轮的活。(我特意查了这一条,免得把「三台已经不在」误报成抢占。)
- **五组本轮均有产出**(batch-desk 21:13Z / hero 20:02Z / replay-check 18:38Z / strategy 19:31Z),无空转。
- **P4.1 标尺波**:GH #535 已裁「部分完成、gpm 无量具」,排序条款释放,**存量未动**。
- **上一轮 ⑬ 的 `test_wave_throttle.py` 对 W48 的 `UNCERTIFIABLE`**:批测台 21:1xZ 报告里
  **闸 (i) 已按修好后的工具跑通**(锚 Y1、unlock 21:16:56Z、`THROTTLE_EXIT_AT_ACTION=0`)⇒ **该项自然结清,不再开 owed 行**。
- **patch 检查**:低频项,本轮**未做**,顺延。
- **owner 邮件**:本周尚未发(上一封 08-31);本轮**新增 1 条 DECISIONS_NEEDED**,连同存量攒进下一封。
- **效率台账**:今日周六,**周日跑**,本轮跳过。

---

## 六、铁律 6

- 静态半:`bash tools/agent/luacheck_gate.sh` ⇒ **`0 warnings` / `GATE_EXIT=0 CLEAN`**
  (容器初始无 luacheck,gate 自己装的,apt 包名 `lua-check`)。**无 `RULE6_BYPASS`**。
- 动态半:**`bots/`+`game/` 零 diff**(`git diff --stat -- bots/ game/` 为空)⇒ 未跑全量套件(~100min,GH #124),
  **逐个跑了读被改文件的测试**:`test_pending_rulings.py` 231/0、`test_carrier_terms.py` 66/0、
  `test_gate_claim_consistency.lua` 16/0、`promote_atoms.py` exit 0。**不声称跑了全量。**

---

## 七、下次触发

1. **GH #523 `text_absent_done_when_kind`** —— 总监自己那根棒,**连续第四轮未取**(本轮又被 P4.2 主活占满)。
   ⚠️ 它已经在 owed registry 里,不会掉,但**四轮**这个数字本身要被读。
2. **继续 P4.2 库存清算(每轮 ≥2 判定完结)** —— verify=0 长尾里 narrat 1 的一档:
   `campsel` / `pulllane` / `pullthink` / `roamidle` / `rotscope` / `teambrain` / `tpdead` / `tpgap`
   (`midtp` 仍是录像组 GH #521 (丙) 的棒,**不抢**)。
   ⚠️ **`tpdead` 与 `tpgap` 先看第二节那条**:它们大概率也是「(a) 买得到但义务从没投递」而不是「买不到」,
   **两种理由不许混着写**。
3. **通用化 (a) 义务自动开 owed 行**(本轮立案,GH 号收尾回填)—— 它一条覆盖 40 个 id,
   比一轮清两条更接近 P4.2 的解冻线。
4. **账户级预算那条**:等 owner 回复;在此之前每轮报告分两行写花费。
5. 存量:`$0.90` 单波成本常数重裁(**第四轮连续点名**)/ GH #538 Lua 腿红 / GH #528 / patch 缺口 P3。

---

## 八、Token 用量(铁律 8)
cache_creation_input_tokens: 164,047
output_tokens: 49,411
TOKENS total_in=9,453,576 out=49,411 turns=69
