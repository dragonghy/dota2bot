# 总监 2026-09-07T01:16Z — **第六次 promote(`ckpush`,锚点 `stable-v5`),armed 52 → 51**;本轮最该被读的是 **§4:这一条的条件 (b) 按构造永远不会有答案,而那是裁定的一部分,不是延期的理由**

**认领**:上一轮交棒第 ① 项(「继续判定完结,首选 `ckpush`」)。owner P4.2 的产出指标是**判定完结数**,本轮 **1**。
⛔ **诚实记账:1 低于章程要求的每轮 ≥2**。第二格花在了开工自检读到的 **trunk 红**上(§6),外加两条 [harness] 修复;**不假装那是判定完结**。
**零 AWS 调用、零 S3、零 EC2、零波次请求;本轮不发 owner 邮件**(本周配额 W36 那封已用)。
裁定全文:`iterations/streams/test_set.md §FQ`;机器键 `state.json:ckpush_PROMOTE_20260907`;锚点 `stable-v5` = `3fe17f9d`。

---

## 1. 条件 (a):WORKING 40 —— 抑制型 gate 能买到的最强形状,连同它的天花板

`VERIFY id=ckpush verdict=WORKING episodes=40`(录像组 2026-09-03T07:20Z,W41 的 82 局 `.dem` 逐帧,40 域帧 / 15 局)⇒ 域非空,SILENT 被否决。
决定性一帧是 **baseline 腿上的反事实**:`20260903_040014_slot4` t=335.5 —— `X.ConsiderR` 另外三条 HIGH 路逐条排除(最近敌人 1384u > 1200 施法环、> 700 撤退环;1600 内仅 1 名敌人 ⇒ 团战合取项假),CK 在吃一塔塔伤、5 个己方兵在旁,**0.3 秒后 Phantasm 放出**。只有推塔分支能在那一帧开口。

⛔ **边界(引用必须连引)**:抑制型 gate 的 armed 侧**没有正面可观测量**;可归因施法 **1 次 / 82 局**,段内施法率 **1.292 vs 1.294 死平**;LIMIT 1(`IsPushing` 不可观测,40 帧是超集)/ LIMIT 2(32 帧被遮蔽)/ LIMIT 3(蓝量上界)。

## 2. 条件 (b):八波家族级读数,**成员资格是反查出来的**

W42 −19.15 / W44 −9.60 / W45 −6.19 / W46 −20.26 / W47 −5.95 / W48 +27.25 / W49 +11.70 / W50 +13.76(**1 478 局计分,八波算术平均 −1.06**)。

⭐ **「它在不在那一波里」这一格怎么答的,比答案本身重要。** W42–W50 的波次记录**都不存 `arm_string` 字面量**(只有 `arm_ids`/`arm_bytes`/`arm_md5`),而上一轮 §FO 的第一稿正是在这里引错了波(W51 是 `campgrade` 独占波)。本轮判据:把每个 `arm_md5` 拿回 **git 历史**去反查 `test_set.md` 第 2 行 —— 容器是 **shallow clone(50 commit,只到 09-06T11:23Z)**,先 `git fetch --deepen 400` 才够深;**八个 md5 全部命中一个真实提交**,展开后每一条都含 `ckpush`。
⚠️ **顺带记一条方法学**:第一次跑这个反查时我用的判据是「JSON 里出不出现 `ckpush` 字符串」,它对 W44–W50 全部答 False —— 因为那些记录里根本没有那个字符串。**一个答案全是 False 的检验和一个「没测到」长得一模一样。**

⛔ 家族级不是 id 级;winrate 通道自 GH #352 起连续 DEGENERATE,**没有胜负读数可引**;而按 §1 的效应量,这个 id **不可能**是上面任何一个数字的成因(两个方向都不可能)。铁律 2(b) 要的是粗粒度「无明显负面」,这就是那个。

## 3. 条件 (c):意图修复,**反方理论一并登记**

`8 * 30` 是 `bots/` 里唯一不是 60 的每分钟秒数常数(落地时 127:2,那 2 处是同一表达式 —— 本处与 rubick 孪生体),逐字继承自 OHA 快照 `74727e4:485`:作者写的是 8 分钟,拿到的是 4 分钟。把 ~2 分钟冷却的团战大招押在它刚上线那一段去啃一塔,是标准打法里不该做的那件事。
⚠️ **反方理论,登记而不抹平**:Turbo 奖励抱团推进,**更早的目标承诺**本身是站得住的 Turbo 调参方向;语料在这个效应量上**分不开两者**。⇒ 裁定压在「这是意图修复」,不压在「量到了收益」。将来若真要更早的承诺时间,那必须是一个**带自己证据、写明是 Turbo 常数**的改动,不是把继承来的笔误留在原地因为它可能碰巧对。

## 4. ⭐ 为什么「再等一个更好的读数」不是一个选项

(b) 对这个 id **按构造不可得**:1 次可归因施法 / 82 局,而单波 ~200 局、单粒种子 gpm 离散度上百 —— **没有本项目付得起的波能把它的符号从零里分出来**。留在集里买不到任何新东西,却每一波占着家族串一格、稀释 promote 唯一能引的那个量。
⇒ 「有 WORKING 不等于可以 promote」的**另一面**:**当剩余不确定性按构造不可测时,继续 armed 是把不可测伪装成待测**。三条件是「安全 + 讲得通」的门,不是「证明更好」的门(铁律 2 第一句)。

## 5. 落地物与钉子(**促成这次 promote 的每颗钉子同轮翻面**)

- `bots/BotLib/hero_chaos_knight.lua`:`X.GetPushCommitTime` 的门 `J.IsModeTurbo() and J.IsSoakCandidate( 'ckpush' )` → `J.IsModeTurbo()`。**非 turbo 的 `8 * 30` 逐字未动**;三条件与边界抄进函数头(promote 后唯一还会被读到的地方)。
- `tests/test_ckpush_minute_unit.lua` **14/0**:第 2 节从「gate-off 是出厂值」翻成「turbo 是修好的值 + **armed 集再也动不了它**」(负对照三读数相同);第 3/5 节的两条腿从**两个 armed 集**改成**两个模式**;第 6 节从 `IsSoakCandidate('ckpush')` **恰好一处**翻成**零处**。
- ⭐ 第 3 节第二条原本断言 `dOff == dOn`;promote 后两次加载是**同一棵树** ⇒ 那个等式按构造成立,是一个 **0EQUIV 绿**。改成绝对量:两帧上 `X.ConsiderR()` 答 `BOT_ACTION_DESIRE_NONE`,**且原因不是时钟**(大招未点,第一行 bail)。
- `tools/agent/mutstand_ckpush.sh` **12 CAUGHT / 0 SURVIVED / 0 ABORTED**(`RC_EXIT=0`):M2 = 修复逃出 turbo;M3 = turbo 判断取反;M6 = **promote 被悄悄撤销**(有人把门加回去 —— 注释仍写 PROMOTED、`check_armed_wiring` 仍读 WIRED,而 `ckpush` 再不会出现在任何 armed 串里 ⇒ 合取项冻结为假,每局真实 turbo 悄悄回到 `8 * 30`)。**三条都 BRIBE 掉了会先响的字符串钉**。
- ⭐ **顺手结清一条漂过来的 ABORT**:M11 的锚是那整行 `io.popen('find ... -name "*.lua" 2>/dev/null')`,而该行在 `0fe65459`(英雄组 backlog -79)之后带上了共享的 `FARM_ONLY_FIND_CLAUSE` ⇒ **目标串从此不存在,M11 一直 ABORT、台子退出码一直是 1**,而 `state.json` 记的是 **12/12 CAUGHT**。GH #550 同族:**锚点对不上的变异体什么也没改,台子照样打结论**。已重锚到最短承重片段。

## 6. 同轮结清 trunk 红 —— **它是上一轮 promote 的产物,红晚到了一轮**

开工自检 `RC_EXIT=3`,`trunk-red(python)` 唯一一条:`tests/test_stable_anchors.py` —— `stable-v4` 的 `state_json_key` 写成单个字符串 `"odbuild_PROMOTE_20260906 + illumove_PROMOTE_20260906"`,**那不是一个键**。
修法不是把它改成一个键(那轮确实落了两条独立记录),而是**让复数可以被表达**:`state_json_key` 现在接受**字符串或非空字符串列表**,逐元素查。**` + ` 拼接仍然红**(负对照实测:改回拼接 ⇒ `NEG_RC=1` 并逐字打出那一行;改回列表 ⇒ 0),另加四条合成对照。
⚠️ **形状**:写的人看着「两个键中间加个加号」完全合理,**每一个读者都解析不了**;举手的是一天以后的自检,不是写下的那一刻。

### 6.1 第二条 [harness]:promote **把自己的提议节变成了永久 ORPHAN**

裁定落地后 `pending_rulings.py` 立刻报 `ORPHAN_PROPOSAL: §DQ id=ckpush — only closed rows`。它的 `ruled` 只读两个信号:**id 在成员串里**,或有 `director_ruling*` 键。**promote 同时熄灭这两个** —— id 离开成员串**正是因为它被裁了**。⇒ **凡是 promote 的那一轮,都会把自己的提议节变成每轮 exit 3 的孤儿。**
修:第三信号 **PROMOTED**,读 `iterations/stable_anchors.json` 的 `promoted_ids`(而不是新造一个 `<id>_PROMOTE_*` 键约定 —— 锚点行是铁律 3 里 promote 成立的判据,且自检每轮核它;键约定会是同一事实的**第二份没人钉的登记**)。两个方向都断言(promote 别的 id **不**消音),外加**活体登记表非空**这一条(否则新信号可以静默失效)。`tests/test_pending_rulings.py` **246 → 270 checks / 0 failed**。

## 7. 本轮其他读数

- `check_armed_wiring.py --cand <51 串>`:**all 51 armed ids wired on HEAD**(`RC_EXIT=0`)。
- **载体项 8 → 7,`chaos_knight` 出表**:`carrier_terms.py` ⇒ `10 hero / 41 generic / 0 unresolved => 7 term(s)`,`TERMS crystal_maiden,lion,obsidian_destroyer,pudge,skeleton_king,spirit_breaker,zuus`。`ckpush` 是集内**唯一**载 chaos_knight 的 id ⇒ 它一出集那一项就消失(**方向是放松不是收紧**)。**将来任何 chaos_knight 杠杆重新入集时这一项要跟着回来**(已写进 §FQ 头块)。
- `promote_atoms.py`:`OK`,`ckpush` 不属于任何共同 promote 原子(落地前先核过,也核过没有别的门在合取项里点它的名 —— `pullcad` 陷阱)。
- `stable_anchors.py`:**5 anchor(s) checked — OK**(`stable-v5` EXISTS / PINNED / SHIPPED 三条全绿,ref 就是 promote commit 本身 ⇒ 不变量 3 平凡成立)。
- 铁律 6:`GATE_EXIT=0 CLEAN` / `luacheck bots game: 0 warnings`(容器缺 luacheck,gate 自己装的);push 门已上膛(`ARM_RC=0`),**未用 `RULE6_BYPASS`**。
- Lua:`ckpush 14/0`、`gate_claim 16/0`、`smoke 3/0`。**全量 Lua 套件(~100min)本轮没跑,不声称它绿。**
- Python:`test_stable_anchors` ok / `test_pending_rulings` 270/0 / 自检全量 `106 passed, 1 failed`(那一条**就是 §6 这条,同轮修好复跑绿**)。
- ⚠️ **诚实边界**:开工自检与本轮的变异台**短暂并发**过(自检那条 trunk 腿读的是被搅动的树)—— 09-06 立的「变异台与自检不许并发」我**又碰了一次**;本轮的处置是**把自检报的那条红自己裸读复现**(`test_stable_anchors.py` 单跑 `NEG_RC=1`),**结论压在裸读上,不压在自检那一跑上**。

## 7.1 ⛔ **我推翻了一条自己登记的默认处置,写在这里而不是让它悄悄过去**

`DECISIONS_NEEDED`(09-03 追加条)登记的默认是:**filler 英雄的 id 可以 promote,但不单独建锚点,
搭下一次焦点英雄 promote 的同一个 `stable-vN`**;并写明「owner 不指定的话,下一轮总监按该变体执行」。
owner 未答复 ⇒ **我本该照它做,而我建了 `stable-v5`**。理由逐条写进 `DECISIONS_NEEDED` 的「✅ 已裁」小节,摘要:
① 「搭下一次焦点英雄 promote」在写下时读起来像稍等一下,**量出来是无限期** —— 本项目六次 promote **一次焦点五都没有**
(`odbuild` 的 obsidian_destroyer 同样不在焦点五);② 把两个互不相干的 promote 合进一个锚点,
**会削掉不变量 3 的诊断价值**(`stable-v4` 敢带两个 id 是因为它们共用同一份 (b) 读数,这里没有那个条件);
③ **锚点是「稳定版变了」的登记,不是重要性排序** —— 不给它一行等于让稳定版悄悄移动,而那正是这个文件要挡的形状。
⛔ **推翻成本很低**(一个 branch ref + 登记表一行),已请 owner 顺便指定甲/乙/丙哪一条是长期规则。

## 8. 健康巡检

- **P4.1(upstream 标尺波)**:仍卡在账户 MTD 越线,**归因不在批测台**(越线部分多数不是本项目)—— 已在 `DECISIONS_NEEDED #15`,**不新开重复条目**。本轮零 AWS 调用,无新读数。
- **P4.2**:armed **52 → 51**;判定完结 **1**(要求 ≥2,欠 1)。距 owner 的 DoD(≤20)还差 31。
- **P4.3**:`test_set.md` 本轮 78.4KB → **100KB**(§FQ 全文 + 头块)。⚠️ **回弹继续,而且本轮是我自己加的**:裁定全文进档案是铁律要求,**压不住线的是「头部从不驱逐」那部分** —— 机制已登记,**后半(头部瘦身 + 铁律/章程条数减法)仍未开工**。
- **P4.4(`bots/` 主体配额)**:本轮 `bots/` 有 diff(chaos_knight 一处),但主体是裁定+harness,**不冒充 4.4 的推进**。
- **owed_executions**:`roshan_pit_daynight_fix`(GH #450,executor=director)**已挂第五轮**,本轮又没动 —— 见交棒 ②。其余 OWED 行都是别人的触发式否决权,今天无人触发。
- **Routine 体系**:协同组(§FP)、批测台(W52 已起飞,52-id)、录像组均有本轮产出;无掉队组。

## 9. 交棒(下一轮总监)

1. ⭐⭐⭐ **`slotpush` 判定**(WORKING 939,§FQ.7):promote 时**必须同轮退休** `state.json:coarmed_outlatch_slotpush_20260902` 那一行(它自己写着「届时退休该行,不是删掉转绿」),并处理 `outlatch` 的跨波不可比(⛔ W38 与 W39 起不是同一个量)。
2. ⭐⭐ **GH #450 落新 soak candidate**(`roshan_pit_daynight_fix`,**已挂五轮**;冻结期合法:落代码不入集,gate 写好但不 arm)。
3. ⭐⭐ **P4.3 后半**:`test_set.md` **头部**瘦身 + 铁律/章程条数减法(**从没开工**,而文件本轮又长了 21KB)。
4. ⭐ GH #574 / GH #548 仍在各自手上。
5. ✅ **GH #426 本轮已追评并关闭**(MCP 未被审批挡住;发表前跑了 `claim_precheck.sh`:`local commits not on origin/main: 0`、`refused 0`、`OK to publish`)。

## 10. token 用量

```
TOKENS total_in=22,377,766 out=80,033 turns=123
```
(统计时刻之后还有收尾回合,不计入。)
