# 总监 2026-09-12T05:4xZ — RULING 22/23:两条退集(`outlatch` + `rotscope`),armed 29 → 27

**一句话**:owner P4.2 的「每轮 ≥2 判定完结」本轮达标(两条退集);
**而本轮最该被读走的不是这两条裁定,是落地它们时被顶红的那条棘轮 ——
`tests/test_pending_rulings.py` 把 armed 集的下界写成了 `>= 29`,
即 owner 优先项的反面,并且它的值恰好卡在今天这一步上。**

- **零 AWS(一次调用都没有)**、零波次、**`bots/`+`game/` 零 diff**、不发 owner 邮件、无 promote、无 reject、无入集。
- 判定完结 **2**;成员串 **29 → 27**(239 字节,md5 `76a888b622124fc5488503aa36ef6b25`)。
- 档案 `test_set.md §HA`(§HA.0–§HA.5),机器键 `state.json:outlatch_RETURNED_20260912` / `rotscope_RETURNED_20260912`。

---

## 1. 选取不是我挑的(章程「下次触发」① 逐字要求「先跑 `a_evidence_owed.py` 现读别抄报告」)

```
A-EVIDENCE-OWED  armed 29  verdict 29  owed-row 0  UNOWED 0     RC_EXIT=0
```

⭐ **这一行本身是个里程碑**:09-05 立 P4.2 时量到的「armed 61,有 VERIFY 行的 21
⇒ 40 条 id 的 (a) 今天没有一行替它举手」**已经结清**。
**瓶颈从「投递」移回了「裁定」** —— 也就是移回了我自己这一格。

`verify_coverage.py --all` 现读 29 行,按「核验次数最多、产出最少」排:
`outlatch` **7 次 / episodes 0**、`cmqreach` 6/158、`lionqdmg` 5、`ownhalf` 5。

**⛔ 特意没裁的三条,理由各自不同(写下来免得下一轮当遗漏补上)**:

| id | 表面 | 真实原因 | 正确处置 |
|---|---|---|---|
| `illureal` | verify 3 / episodes 0 | 那个 0 **不是域的 0**:报告逐字「**本轮未逐帧核过任何一帧**」,域上界 152 帧 | **掉棒**,交棒;⛔ 不是退集 |
| `abilanc` | verdict **SILENT** / 122 episodes | **载体缺席**:`GetMostHpUnit` 有 **13 个**英雄文件调用(含 axe/skeleton_king/luna),而那六发 under-tier 施法来自 skywrath/zuus/spirit_breaker,**三个文件各 0 次调用** | **载体门**(`seed_draft.py --assert-carrier`,从没对这条 id 用过);⛔ 不是退集 |
| `pullcad` | verify 1 / 5020 episodes | AGENTS.md 记的 `pullcad` 陷阱**在树上已修好**:`mode_roam_generic.lua:343` 已是独立门,`promote_atoms.py` 现读 `FROZEN none` | 不动 |

📌 `abilanc` 这一条差点被我裁掉 —— **SILENT 看起来是最硬的退集理由**,
而它这一次量的是牌桌不是代码。

---

## 2. RULING 22 — `outlatch` 退集,处置 **INSTRUMENT-BLIND**(§HA.1)

**why_a**:七条 VERIFY 行,**后五条连续 `episodes=0`**(09-07 ×2 / 09-08 ×3)。
域 =「**第一次** `GetUnitList(UNIT_LIST_ALL)` 扫描返回空表」,**纯 bot 内部事件**;
其唯一下游可观测后果是「一个 mode 永远不出价」,
而这在世界状态快照里与「mode 出价本来就低」**逐字节相同**。

⭐ **§FW.2 检查(「仪器是不是瞎的」)的答案与 `campbind` 相反,这就是两条不同名的原因**:

| | `campbind`(§FX,09-07) | `outlatch`(本轮) |
|---|---|---|
| §FW.2 | **通过**(同一归因腿两方向都非零) | **不通过** |
| 那个零是谁的 | **域的** | **仪器的** |
| 处置名 | `DOMAIN-NOT-REACHED` | `INSTRUMENT-BLIND` |

而且**是被裁方自己先说的**:录像组 `20260908T064838Z.md` §六逐字
「它的域…**不在 dump 里**…37 次 attempt 只是**活性上界**」,并据此主动停手
「本组不再自行发起同型核验」⇒ **一份交到总监手上、被裁方已经停在裁定形状里的棒**。

**cost_of_staying_armed**:不是免费默认。五轮波次各买回一个相同的零,
而 GH #424 还给它加了**三段不可并池**(I ≤ W38 / II W39–W53 / III W54 起),
**只有第 III 段能干净买** ⇒ **每多留一轮,能用的语料比总语料少一段。**

**下一棒**:`owed_executions.json:outlatch_condition_a_fixture`。
⚠️ `tests/test_outlatch_scan_postcondition.lua` **已在树上但是静态源码棘轮,不构成 (a)** ——
这是本行最可能的失效方式,已写进 `done_when_note`。
⭐ 退集反而让 fixture 路更干净:**fixture 钉一帧不钉一池**,三段分界对它不适用。
⇒ `outlatch_three_era_incomparability` **不退休**(它约束的是任何回到波次路径的读数)。

---

## 3. RULING 23 — `rotscope` 退集,处置 **UNOBSERVABLE-BOTH-ROUTES**(§HA.2)

**这是第一条「两条路同时堵死」的 id,而两条堵点性质不同。**

- **波次路**(录像组 `20260904T042500Z.md §4`,三条缺一不可):命令不可观测
  (combat log 不记 `ActionQueue_AttackUnit`,`.dem` 无 mode 竞价结果、无 `J.GetProperTarget` 取值,
  唯一下游可观测量每个 mode 都产生)/ 两层反号判噪声(4(i-b))/ 58-id 腿不许分摊(§BW.3)。
- **fixture 路**:**GH #474**,`make_fixture.py` 不产出攻击目标字段(**109 个 fixture 里 0 个**),
  `tests/mock/bot_api.lua` 对三个 target getter 一律答 `nil`(**而那是正确的默认**)
  ⇒ `J.GetProperTarget` **每一帧恒 `nil`**,而它被 `bots/` 下 **165 个文件**引用。
  **这个堵点不是本 id 的。**

⛔ 三个**不适用**的词照抄不改:不是 `DOMAIN-NOT-REACHED`(域每局走到 87–114 次)、
不是 `SILENT`(「它动没动」本身不可观测)、不是 `BUGGY`(逐帧两帧都正确)。

📌 **§CU.5 把这件事记成了「本 id 的一条 `[limit]`」,而它其实是「fixture 路线整条堵死」——
一个被记在错误作用域里的限制,会让每一个后来者各自重新发现它一次。**
`owed_executions.json:rotscope_readmission_on_gh474` 存在的主要目的就是防这一次重复发现。

---

## 4. ⭐⭐ 本轮最该被读走的一条:一条棘轮把 owner 的优先项写反了(§HA.4)

落地退集时 `tests/test_pending_rulings.py` **当场顶红**:

```
FAIL: member string shrank below its 2026-08-24 size (29)
```

断言原文 `check(len(members) >= 29, ...)` —— **一条 2026-08-24 的下界**,
写它时 armed 集只会变大。而 **owner P4.2(09-05)把方向掉了头**:
集合必须缩到 ≤20,**总监的产出指标就是集合变小**。⇒ **它断言的正是优先项的反面。**

⭐ **它为什么偏偏今天才红**:下界值 **29** 恰好等于它写下之后集合到达的最小值
(32 → 30 → 29,**每次都刚好满足 `>= 29`**)⇒ 它对**第一条真正把集合推过 29 的裁定**开火,
而此后 **29 → 20 这 7 条退集会条条踩到它**。

⛔ **修法不是把 29 改成 27。** 这是 GH #106/#127/#538 同一族(把总体规模写成字面量),
§7d 给这族定的规矩是「**改成断言记录的内部自洽**」——
把 29 改成 27 只买到一轮,**并把同一个陷阱重新上膛**。

**实际修法**:line 3 本来就用散文写着 line 2 的**条数 / 字节数 / md5**,
让 line 2 **对着它自己的记录核** —— 严格强于任何下界(截断抓得到、
「改了串忘了改记录」抓得到、**永不过期**),顺带把三个装饰性数字变成被检查的值。
新增 `raw_member_line()` / `declared_member_record()`(条数必需,字节与 md5 **可选** ——
**「没记」与「记错」是两种发现**)。

**变异台(还原走文件副本,md5 逐位复核 `OK`)**:控制 `682 checks / 0 failed`;
**M1** 条数 27→28 红;**M2** md5 改零 红;**M3** line 2 砍掉最后一个 id、记录不动
(**老下界真正想守的那件事**)⇒ **三条断言同时红**;**M4** 字节 239→240 红。
**4/4 CAUGHT,零 NO-OP。**

📌 *一条写成字面量的规模断言,会在它所守护的那个量**第一次按计划移动**的那天开火 —— 而那天,开火的是计划,不是缺陷。*

---

## 5. 两条被工具当场拦下的自造红(都已修,记下来因为两条都是**procedure 的一部分我没做**)

1. **`pending_rulings.py` 的 `ORPHAN_PROPOSAL`**:我先把 `strategy-26`/`-28` 的 `status`
   写成了 `done` ⇒ 工具报「§CG.5:only closed rows — **a ruling has nowhere to land**」。
   现查先例:`campbind` 退集时 `strategy-42` **保持 open**(`harvested_pending_verification`)。
   ⇒ **退集不是这条 id 的终点**,它的 (a) 仍欠着、将来还要重裁,
   **一关闭接力棒就从所有队列里消失**(铁律 9 拉野死分支同形)。已回改为 open,orphan 归零。
2. **`arm_since.py` 的 `STALE ROW`**:`armed_since.json` 要求退集时给该行盖 `retired_at`
   —— **这是退集流程的一步,我漏了**,`tests/test_arm_since.py` 在 py gate 里把它顶红。
   已给两行盖章并写 `retired_note`(含「重新入集会把 `pudge` 载体项一起带回来」这条代价)。

📌 *这两条都不是我读出来的,是工具举的手。* 它们也是**退集流程事实上的清单**,
下一个总监照着做可以少两个来回。

---

## 6. 连带读数:载体项 6 → 5,选种解空间**变宽**(§HA.5)

```
29-id: 6 term(s)  TERMS crystal_maiden,lion,obsidian_destroyer,pudge,skeleton_king,spirit_breaker
27-id: 5 term(s)  TERMS crystal_maiden,lion,obsidian_destroyer,skeleton_king,spirit_breaker
```

`pudge` 出局(`rotscope` 的唯一载体)⇒ 往后的镜像波**不再被迫排出 Pudge**。
此前几轮的记法是「`TERMS` 逐字节相同 ⇒ 解空间不动」;**本轮它动了,方向是变宽**。
⚠️ 反向也记住了:`rotscope` 将来重新入集,**这条 Pudge 约束会一起回来**。

---

## 7. 成本与体系健康

- 💰 **零 AWS,一次调用都没有;不对 MTD 作任何新声称。**
  转述批测台 03:13Z 的当轮现跑仍是本仓库最新的 MTD 读数;三条线(围栏 $60 / 刹车 $90 / 批准线 $100)**一字未动**。
- 🩺 五组巡检:batch-desk(00:11Z / 03:13Z)、hero(02:06Z)、strategy(01:50Z)、
  replay-check、director 均在 24h 内有产出,**无掉队组**。
- **armed 27,离 P4.2 解冻线(≤20)差 7 条。**
- ⛔ **不发 owner 邮件**;`DECISIONS_NEEDED` 存量 15/18/19 随 W37 周信一并交。

---

## 8. 验证(裸读,全部走 `rc.sh`,无管道读退出码)

**铁律 6 三条腿**:
- `GATE_EXIT=0  CLEAN` — `luacheck bots game: 0 warnings`
- `py gate: 96 ran, 0 findings, 0 uncertifiable, 25.8s`
- `lua gate: 343 ran, 0 findings, 0 uncertifiable, 9 known-red, 311.9s`
- ⛔ **未用 `RULE6_BYPASS`。**

**开工自检**:`RC_EXIT=3`,`legs run : 12`,
`FINDINGS (exit 3) : cadence queue-rulings owed-executions trunk-red(python) trunk-red(lua)`,
**`UNCERTIFIABLE (exit 2): none`** —— 与前两轮同一组存量,**零新增**。

**零新增是量出来的不是断言的**(`git stash` 复跑,前几轮点名跳过的那一步本轮做了):
- `pending_rulings.py` 基线 `RIDESHARE 1 / ORPHAN none / ROWLESS 3`,改动后**逐项相同**。
- `test_carrier_terms.py` **基线就 5 failed**(存量 trunk-red(python)),改动后仍 5,**零新增**。
- `test_pending_rulings.py` 基线 `665 checks / 0 failed` → 改动后一度 `679/1`(§HA.4 那条),
  **修好后 `682 checks / 0 failed`**。

**其它现读**:`a_evidence_owed` `RC_EXIT=0`;`verify_coverage --all` 29 行;
`check_armed_wiring --cand <27串>` ⇒ **`all 27 armed ids wired on HEAD`**,`RC_EXIT=0`;
`carrier_terms` 新旧串各一次,均 `0 unresolved`,`RC_EXIT=0`;
`promote_atoms.py` `RC_EXIT=0`,**`FROZEN none`**,atoms 5;`test_arm_since.py` `18/0`。

⚠️ **纪律 3(管道读退出码)本轮第一条命令复发了一次**:开工自检写成
`routine_selfcheck.sh … | tail -60`,**被 §22 守卫当场拒**(`REFUSED … exit 2, nothing checked`),
改走 `rc.sh` 后拿到真码。**这是守卫第 N 次拦下同一形状,而它仍然只长在自检这一条命令上。**
本轮此后所有读退出码的命令一律走 `rc.sh`。

⚠️ **动态半(全量 Lua 套件 ~100min,GH #124)本轮未跑,不声称** —— `bots/`+`game/` 零 diff,
改动只碰 `iterations/` 与 `tests/test_pending_rulings.py`,而后者已在 py gate 里逐条跑过。

---

## 9. 下次触发

1. **判定完结 ≥2 继续**(armed 27,离解冻线差 7)。**下轮前两个候选**:
   `cmqreach`(verify **6** / 158 episodes,六次全 INDETERMINATE ⇒ 先问「是仪器还是域」再裁)、
   `arbheart`(verify 1 / episodes 0 / 09-03 起 9 天未动)。
   ⛔ **`illureal` / `abilanc` 不算判定完结的候选** —— 见 §1,两条各自欠的是交棒和载体门,不是裁定。
2. ~~⭐ **把 §1 那两根棒交出去**~~ **同轮收尾补做了**(见 §12):`owed_executions.json` +2 行
   `a_evidence_illureal` / `abilanc_carrier_gate`。**理由是我自己刚写下的那句话** ——
   报告散文里的「下一轮该做 X」**不算投递**(铁律 9 拉野死分支 37 轮;§FB.3「失效的不是证据,是投递」),
   而我本来正要用散文交这两根棒。
3. ⭐⭐ **上一轮 ② 那个缺口仍未补**(第二轮顺延):三个 promote-time 普查
   (`check_armed_wiring` / `inverse_gate_census` / `promote_atoms`)**都只读 `bots/`**,
   `tools/` 侧的 id 硬编码前提无人查。**本轮量过人口了,读数写在下面 §10,不要从零再量一次。**
4. 两条仍红的 python/lua trunk 腿(不挡 push)/ GH #584 三条测试第三分支(**第五轮顺延**)/
   批测台交棒 ① / `py_gate_budget_premise` 等五条 `a_evidence_*`(**第九次顺延**)/
   patch 缺口 P3 / `DECISIONS_NEEDED` 15/18/19 等 owner。

## 10. 给下一轮省一次测量:上一轮 ②(`tools/` 侧硬编码 id)的人口读数

本轮顺手量了,**结论是「先别急着做成普查」**,理由是判据还不成立:

- 19 个 promoted id × `tools/`+`tests/` 下的 `.py`/`.sh`/`.json`(跳过注释行)⇒
  **58 行命中 / 10 个 id**。
- 抽查的现场**全部合法**:`mutstand_*.sh` 是**故意**注入 `pullcad` 陷阱(那是它们的全部意义);
  `creeppull_domain.py:462` / `roamstale_null_channel.py:158` 是**历史语料读取器**
  (语料是 promote 之前录的,那时 id **确实**在 arm 串里);
  `test_roam_conversion_arm_guard.py` 自己造合成 arm 串。
- ⇒ **一条「promoted id 出现在 `tools/`」的词法普查会从出生起亮 58 个假阳**,
  正是「红从出生起 ⇒ 三轮后没人看」那条立法理由要防的东西。
- **真正的判别式是「对未来波次的前提」vs「对已存语料的前提」**(`tpdying_release.py` 的
  `REQUIRED_PARTNER` 属前者、会**拒绝每一波真波**;上面三个属后者、无害),
  **而这个区别在词法层不可得** —— 两者读的都是一个叫 `cand` 的字符串。
- **候选收窄方向(未验证,留给下一轮)**:只看**失败关闭**的路径
  (promoted id 出现在一个会 `sys.exit` / `[fatal]` / raise 的分支里)。
  ⚠️ `roamstale_null_channel.py:158` 在 `--selfcheck` 下**确实 `sys.exit(2)`**,
  ⇒ **这条收窄也不是零假阳,做之前要先量它。**

---

## 11. token 用量

```
TOKENS total_in=14,634,662 out=72,659 turns=89
```

⚠️ 按 §7b 的定论:`total_in` 由 cache_read 主导、随回合数近似二次增长,**只作量级参考**;
台账常设指标是 **`out/报告`** —— 本轮 **72,659 out / 1 份报告**。
注:统计时刻之后的收尾回合不计入。


---

## 12. [同轮收尾追加,第一次 push 之后] 两根棒改成机器可读的

第一次 push(`3236912a..6c0ccbf8`)之后补了 `owed_executions.json` 两行,理由见 §9.2:

- **`a_evidence_illureal`**(executor = 录像组):`done_when` 的验收句写死为
  「`VERIFY id=illureal … episodes=<N>` **且 N > 0**,或**逐字说明这 152 帧里一帧都不可判读**」。
  ⛔ **再来一条 `episodes=0` 且不带理由的行不结清本行** —— 那正是本轮让它进不了裁定的那种行。
  `trigger` 里记了**时间本身就是理由**:最后一次报 VERIFY 是 09-02,**10 天**,而它一直 armed
  ⇒ **每一波都在为它付载体与并池成本,却没有一帧被看过**。
- **`abilanc_carrier_gate`**(executor = 批测台):**零 AWS 增量** —— 要求的不是新波次,
  是**既有发波流程里多跑一道已经存在的门**(`seed_draft.py --assert-carrier`)。
  ⭐ 立项依据是**一次已经付过款的失败**:backlog §11 的文件头逐字记着
  「**224 局镜像局、零 Axe**(为 `axebuyblink` 买 (a),买了个空)」—— **与今天的现场同形**。
  `done_when` 要求原样登记 `satisfied/seeds` 分数(§11 写死:exit 0 **也必须**打印它),
  并预先裁好了 **0/N 的读法**:那**本身就是结论**(当前选种解空间下 (a) 买不到,交总监重裁),
  **不是再发一波的理由**。⚠️ 载体名单要求**现取不抄**(`grep -rln GetMostHpUnit bots/BotLib/`)——**树会变**。

⚠️ **为什么这一条值得单独记**:§DR 的立案句是「**一条常驻义务没有行可以被写进去**」
(GH #413 第二次掉棒:补投递进了收割清单,而那张表是**发波当时**写的**波次局部**清单)。
这两根棒**都不属于任何一波**,所以它们只能进 registry。
📌 *我在同一份报告里先写下「散文里的棒会掉」,然后差一点用散文交出两根棒。*

`a_evidence_owed.py` 复跑:`armed 27 verdict 27 owed-row 0 UNOWED 0`,`RC_EXIT=0`。
