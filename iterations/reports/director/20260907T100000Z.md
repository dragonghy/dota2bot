# 总监 2026-09-07T10:00Z — **第七次 promote(`slotpush`,armed 51 → 50,锚点 stable-v6)**

**一句话:上一轮章程写的「下一轮第一件事必须是判定,不是修 bug」照办了 —— `slotpush`
PROMOTE 落地。但本节最该被读的不是那条 promote,是买它的过程里买到的一条反向证据:
我按房规写的 promote 行为钉子,在「有人把闸偷偷加回去」这个变异体下 SURVIVED,
而原因是这个仓库的 fixture 语料在结构上分不开这条杠杆的两条腿。
⇒ 这条 promote 今天的守卫是一个字符串钉,那是限制不是优点,已写进三处。**

- **判定完结 1**(owner P4.2 的产出指标)。⛔ **低于 P4.2 的 ≥2,不粉饰;理由与我为什么
  不去凑第二条,写在 §5。**
- 裁定 **2**:`slotpush` PROMOTE;`hero-40` APPROVED(零 EC2 归档扫描)。
- **零 AWS 花费**(整轮没碰 AWS;总监按铁律 1 本就不花钱)。**不发 owner 邮件。**
- 本轮开的 issue:**0**(GH #594 已由录像组开好并点名总监,见 §6)。

---

## 1. 判定:`slotpush` PROMOTE(51 → 50,锚点 `stable-v6`,commit `f0474dbb`)

上一轮 §FQ.7 把它点名为「**未裁,下轮首选**」。裁定全文 `test_set.md §FT`;
三条件与各自的边界另抄在 `bots/FunLib/jmz_func.lua` 的函数头(promote 之后唯一还会被读到的地方)。

**动作**:`J.IsTeamPushingHighGround` 的实参 `J.IsModeTurbo() and J.IsSoakCandidate( 'slotpush' )`
→ `J.IsModeTurbo()`。**`bots/` 全部行为 diff 就是这一行**(实测:`git diff --cached` 去掉注释后
只剩这一条)。**非 turbo 逐字未动** —— 下游 flag 形参**刻意保留**就是为了这句成立,`utils.lua` 零 diff。

| 条件 | 读数 | 边界 |
|---|---|---|
| (a) | **WORKING 939**(录像组 09-03T22:05Z,W42,78/78 局宽扫 + 2 局逐帧) | 夜魇腿 **43.80%(armed)/ 39.96%(baseline)** 的高地推进几何帧,出厂扫描答不出 TRUE;天辉 **1.85% / 1.44%**。⭐ **判 WORKING 而非 INDETERMINATE 的理由是消费者不是计数**:那些窗口里 armed 腿插眼速率 **0.49x** 自身基线、baseline 腿 **1.61x**,正是 `mode_ward_generic.lua:37` 预测的方向。 |
| (b) | **十波家族级,1 795 局,均值 −4.88 gpm** | ⛔ **轻微为负,本裁定不粉饰** —— 见下。 |
| (c) | **实参类型错误** | **承重的那一条**,见下。 |

### 1.1 (b):成员资格是量出来的,而那个均值是负的

W39 −12.58 / W40 −27.81 / W42 −19.15 / W44 −9.60 / W45 −6.19 / W46 −20.26 /
W47 −5.95 / W48 +27.25 / W49 +11.70 / W50 +13.76。

反查照 §FQ.2 逐字做:每波 `arm_md5` → git 历史里的 `test_set.md` 第 2 行,**十个 md5 全部命中**,
每条都含 `slotpush`(它 W39 入集,此后每一波已收割的波都含它;W41 从未收割、W43 报废、
W51 是 `campgrade` 独占波)。
⚠️ **容器是 shallow clone,不 `git fetch --deepen=400` 只能反查出 6 个 md5,十波里八波读
`UNRESOLVED`** —— 这一步不是形式,是这条读数能不能成立的前提。

⛔ **均值 −4.88,我不把它读成正面。** 铁律 2(b) 要的是粗粒度的「无明显负面」,家族级
不可归因的 −4.88 gpm 是那个 —— **但它不是正面证据,永远不许当正面证据引**。
家族级不是 id 级;全开波无法归因;**十波里 arm 串的成分几乎每波都在变**;
winrate 通道自 GH #352 起连续 DEGENERATE,**没有任何胜负读数可引**。
分层读数按铁律 4(i-a) 登记(W47 ab −45.27 / ba +33.37;W48 +127.24 / −72.75;
W49 +58.07 / −34.66;W50 −131.61 / +159.12;四波 `sign_flip` 全 true)——
按 4(i-c),这些是 swap-average 后的估计量,**反号是 |side| > |arm| 的恒等式,不是诊断**。

⭐ **与 stable-v5(`ckpush`)的分界线,必须读清楚,不许搬用**:§FQ.4 论证的是
「`ckpush` 的 (b) **按构造**永远不会有答案(1 次施法 / 82 局,低于本项目任何一波的噪声底),
所以等更好的读数不是选项」。**`slotpush` 不是那样** —— 它的效应量不在噪声底下,
**一波独占波能说得更多**。⇒ **压住本裁定的是 (c),不是「等不到」。**

### 1.2 (c):这一条是承重的

`GetTeamMember(n)` 取的是**队伍槽位 1..5**(`docs/BOT_API_REFERENCE.md:223`);
`GetTeamPlayers(team)` 交回的是**玩家 id**(天辉 0-4 / 夜魇 5-9)。出厂那一行把后者喂给前者:
越界返回 nil ⇒ 扫描按侧静默收缩(**天辉 4/5,夜魇 1/5**),并且从第 2 步起
`IsHeroAlive(playerdId)` 这道守卫问的是**另一个英雄**,不是它随后去量的那个 `teamMember`。
**这是实参类型错误,不是调参选择** —— 没有任何一种读法能让出厂那一行是作者的本意。
失效方向**闭合**:七个调用点**全部**用 TRUE 去压制一件分心事(眼 / 神符 / 前哨 / 边路商店 /
秘密商店 / Roshan / 回线)⇒ 扫得少 = **把 bot 从高地攻坚上摘下来去购物**。
同缺陷族第四个被修、**第二个被 promote**(前一个是 `slotwait`,09-06)。

---

## 2. ⭐ 本轮最该被读的:**一条 promote 的行为钉子,在这个仓库买不到**

我按房规写了标准的 promote 行为用例 —— turbo 下无论 armed 串是什么,读数都必须相同
(三条腿:本 id armed / 无关 id armed / 什么都不 armed)。**它在变异体「有人把闸偷偷加回去」
下 SURVIVED。**

原因**文件里早就写着**,而我是被变异台按着头才去读的:`[domain price]` 那条断言
**`nFlip == 0`,94 个 subject-load 上两条腿从不分歧**。⇒ 出厂扫描与按槽位扫描在**本仓库
拥有的每一帧**上答案相同,那条行为断言是 **0EQUIV 绿** —— 闸被加回去之后它照样绿。

**处置(不是删掉那一发,是把结论写下来,三处)**:

1. **变异台**:M7(偷偷加回闸)/ M13(模式取反)**不 BRIBE 源码钉**,并逐字写明
   「**这两发是被字符串钉抓住的,那是限制不是优点**」,以及那次幸存是**实测**不是顾虑。
2. **测试文件**:`[promote]` 用例上方写明它在本语料上不可能失败、`[domain price]` 变红那天
   它才开始承重、**真正的行为证据在录像语料上**(939 帧,不在这里)。
3. **`state.json` / 锚点 note**:`limit_the_most_important_one` 一条,引用锚点时连引。

⇒ **这条杠杆今天的守卫是一个字符串钉。** 哪天有人删了那条钉,**没有任何东西会发现
promote 被撤销**。这句话现在有地方可读,而不是三轮以后被重新发现。

---

## 3. 树自己抓住的四件事(都不是我记得的)

1. **`test_gated_helper_nesting_census.lua` 变红并点名要删哪三行** —— promote 删掉了
   `J.IsTeamPushingHighGround` 的闸,那三条 `| slotpush |` 的合取行就不存在了。
   该文件的失败信息自己写着「要么代码变了(删行),要么提取器瞎了(修提取器)」。
   ⭐ **一个把「读过什么」钉下来的普查,是 promote 拿到待办清单而不是三轮后拿到惊吓的方式。**
2. **变异台 M6/M7/M8 的锚点全部失效** —— 它们瞄的是 promote 刚删掉的那个闸串,
   不重锚就会**三发一起 ABORT,而台子照样打结论**(GH #550 / `mutstand_ckpush` M11 同型)。
3. **M11 从 09-03 起就一直在 ABORT** —— 它钉的 `pidShaped == 7` 在 `slotwait` 落地那天
   合法地变成了 5。⭐ **棘轮自己的数字是一个会动的锚点**,任何钉它的变异体都会在棘轮
   下一次合法移动时失效;**这次被发现只因为 ABORT 与 CAUGHT 分开计分**。
4. **我自己造了一个 GH #418**:新增的 M14 瞄 `tests/test_slotpush_highground_scan.lua`,
   而那个文件**不在台子的 `FILES` 备份清单里** ⇒ 变异体活过了运行、活过了 `trap`、
   活过了收尾的 `restore`,**留在工作树里**,下一次运行读到 `BASELINE RED` 才暴露。
   已修(补进 `FILES`)并把这条实测写进文件头:**「变异体的目标文件不在备份清单里」
   与「没有 trap」是同一个错误,不是更小的那个。**

最终:**14 CAUGHT / 0 SURVIVED / 0 ABORTED,`RC_EXIT=0`**。

---

## 4. 裁定二:`hero-40`(`liondrainmi` 量域)APPROVED

本轮唯一一条未裁的 queue 请求(`pending_rulings.py` 开工时 `RIDESHARE: 1`)。
**APPROVED(零 EC2 归档只读扫描),不是入集** —— 与 owner P4.2 冻结不冲突,
因为申请方自己写明「本条不请求入集」,不触发「冻结期唯一合法裁定是 FROZEN-HOLD」。
⭐ **批准的是量域,不是杠杆**;对将来是否入集**零表态**。

**总监加的第 (丁) 条限定两处,申请里都没有**(全文在 `queue.json:hero-40.director`):

- **(丁-1) (2) 那一列单独一个数字不构成收益。** 申请自己写了本杠杆是 **WIDENING**,
  所以 (2) 的命中数**恒等于**「armed 会多放出来的抽蓝次数」的**上界**,不是它的价值 ——
  Mana Drain 是多秒定身引导、引导期间 `J.CanNotUseAbility(bot)` 为真,残血近身多放一次
  是**代价**。⇒ **(2) 与 (3) 必须同轮交付**;单交 (2) 是一个只有分子的比。
- **(丁-2) (4a)/(4b) 读到 0 的退回门。** 「读到 0 就升级成读数」**只有在扫描窗口被证明
  覆盖到野区时才成立** —— 那一支的域按定义是「附近没有敌方英雄」(多半就是野区),
  而 fixture 语料 13 帧实测 `#GetNearbyCreeps(1600,true)` 合计 **0**。
  **先给覆盖率,再给那个 0**;拿一个盲窗的 0 去关一条通道,与 `zusult_gate.py`
  那台「第一帧就 TypeError、整轮零输出」的死量具是同一种账(GH #478 全族)。

⚠️ 顺带:我**没有**新造 `promoted` / `approved_archive_scan` 两个状态词,改用既有的
`done` / `pending` —— `pending_rulings.py` 的 `UNKNOWN STATUS`(GH #317 词汇漂移)
按未知状态计开,新词每轮多两行噪声。裁定本身住在 `director` 字段里,`ruled` 由它判。

---

## 5. ⛔ 判定完结 1 < P4.2 的 ≥2,以及我为什么不去凑第二条

上一轮连续第三轮为 0,并写下「下一轮第一件事必须是判定」。本轮做到了那一件,**没做到两件**。

**我找过第二条,三条路都走不通,写下来免得下一轮重走**:

1. **`zusult` 看起来是现成的一条** —— `grep VERIFY` 打出 `verdict=BUGGY episodes=3`,
   而「(a)=BUGGY 就退集」有 `midtp`/`suptp`/`roshdist` 三个先例。
   ⛔ **那条读数是过期的**:同一天 18:54Z 的报告**明确撤回**了它
   (「**`zusult` 不是 BUGGY**,那 7 发是门按设计不管的施法」⇒ INDETERMINATE)。
   ⭐ **教训是工具与手工的分工**:`verify_coverage.py` 有 `last_verdict` 一列、答的是
   INDETERMINATE(**对的**);是我图快用 `grep | sort | uniq -c` 去读,而那个管道
   **按出现次数排序,不按时间**,于是一条被撤回的裁决排在了它的撤回件上面。
   **一个按频次排的清单读起来像一个按时效排的清单。**
2. **按 P4.2「从核验记录最少的 id 清起」退集** —— 需要「armed 了多久」。
   ⛔ **这个量在本容器里是左删截的**:`--deepen=400` 之后 `test_set.md` 的历史最早只到
   **08-30**,于是 51 个 id 里有 40 个的「首次出现」全都并成 08-30,读出来是 `7d`
   而真值只是 **≥7d**。§FB 退 `tpdying`/`tpreach` 时用的是**入集章节**(§A' 08-19 /
   §BC 08-24)不是 git —— 那是对的做法。**把一个下界当成测量值写进退集理由,
   正是本轮 §2 那条教训的另一面**,所以没写。
3. **剩下的 WORKING** —— `fieldsip` 是结构性 HOLD(共同 promote 原子,`stayfield`/
   `stayfield2` 仍 INDETERMINATE);`wandbleed`/`wandbleed2` 的 (a) 太薄且两条读数相反。

⇒ **本轮的诚实读数是 1。** 凑第二条的唯一可行做法是拿一条我刚证明不可靠的证据去退集,
那比 0 更贵。**下一轮的第一条判定线索已经具体化**:第 2 条的堵点是「入集章节 → id」
的映射没有机器可读的形式,而那正好是一件总监自己能做的基建活。

---

## 6. 交棒(下一轮第一件事,按序)

1. **GH #594 `[bug]`(点名总监)** —— 录像组 09:49Z 开的、**先于本轮存在**的 trunk 红
   (`test_wk_q_castrange_meter_domain` 一族之外的那条),带两台独立归属证据、
   二分区间 `15b0d9fd..3426d6a5`(46 个 commit)、复现命令。
   ⚠️ 验收提醒照抄:**先分清「行为回归」还是「枚举例外该扩」**,该文件自己写了
   「a NEW one is a red rather than noise」,**不要只把 `<= 1` 改成 `<= 2`**。
2. **第二条判定的堵点(§5 第 2 条)** —— 给每个 armed id 一个机器可读的**入集时刻**
   (来源是 `test_set.md` 的入集章节,不是 git 历史),让「armed 了多久」这个量
   在 shallow clone 的容器里也是一个测量值而不是下界。
3. **`outlatch` 的三段不可比性** —— 已登记 `owed_executions.json:outlatch_three_era_incomparability`。
   III 段(W54 起)是**唯一能干净买 (a) 的一段**,因为两条腿从此都带 `slotpush` 的否决。

---

## 7. 开工自检与铁律 6

- **开工自检 `RC_EXIT=3`**。`FINDINGS: cadence queue-rulings owed-executions trunk-red(lua)`;
  `UNCERTIFIABLE: trunk-red(python)`。
  - `queue-rulings` **本轮结清**(`RIDESHARE 1 → none`,`pending_rulings.py` 由 `RC_EXIT=3` 转 **0**)。
  - ⚠️ **那条 `trunk-red(lua)` 是我自己造的假红,写下来因为它会再犯**:自检在**后台**跑,
    而我同时在编辑 `jmz_func.lua` ⇒ 它读到了一个写到一半的文件,报
    `jmz_func.lua:3256: 'then' expected near '<eof>'`。**那是语法错误不是测试失败**,
    静树重跑 `test_wk_q_castrange_meter_domain` **8 tests / 0 failures,`RC_EXIT=0`**。
    自检自己那行提示写着「re-run on a quiet tree (nothing writing under bots/)」——
    **它是对的,而我是先看见红才去读那行的。**
- ⚠️ **开工第一条命令又误用管道**(`… | tail -60`),§22 守卫当场拒并打 `SELFCHECK_EXIT=2 REFUSED`。
  **不新立措辞**(章程第 0 条已经逐字覆盖这一发),只登记:**第三十九发**。
- **铁律 6**:`luacheck bots game: 0 warnings`,`GATE_EXIT=0 CLEAN`,**未用 `RULE6_BYPASS`**;
  push 时 `.githooks/pre-push` 又独立跑了一次,同样 `GATE_EXIT=0 CLEAN`。
- **动态那半(~100 min,GH #124):启动了,没跑完,⛔ 不声称它绿。** 本轮能声称的是
  **受影响的那几个文件**:`test_slotpush_highground_scan` **19/0**、
  `test_gated_helper_nesting_census` **10/0**、`test_wk_q_castrange_meter_domain` **8/0**,
  以及变异台的四文件基线(`test_slotpush_highground_scan` / `test_slotarb_camp_arbitration` /
  `test_slotdust_dust_arbitration` / `test_gated_helper_nesting_census`)在第一个变异体之前
  **被证明 GREEN**(0CORP)。
- `git pull --rebase origin main` 一次(录像组 5 个 commit 抢先落地),**无冲突**;
  rebase 后逐字复核那一行 diff 仍在。

## 8. 本轮所有裸退出码

| 命令 | 码 | 读数 |
|---|---|---|
| `routine_selfcheck.sh`(经 `rc.sh`) | **3** | 见 §7 |
| `luacheck_gate.sh` | **0** | `0 warnings` CLEAN |
| `pending_rulings.py`(开工 / 收尾) | **3 → 0** | `RIDESHARE 1 → none`;orphan `§DL slotpush` 在锚点登记后消失 |
| `verify_coverage.py --all` | **0** | 51 armed / 23 有 VERIFY 行 |
| `mutstand_slotpush.sh` | **0** | **14 CAUGHT / 0 SURVIVED / 0 ABORTED** |
| `run_tests.lua test_slotpush_highground_scan` | **0** | **19 / 0** |
| `run_tests.lua test_gated_helper_nesting_census` | **0** | **10 / 0**(删三行后) |
| `carrier_terms.py` 旧 51 串 / 新 50 串 | **2 / 0** | `1 unresolved` → `0`;`TERMS` 行逐字节相同,载体项 7 → 7 |
| `check_armed_wiring.py`(committed tree) | **0** | `all 50 armed ids wired on HEAD` |
| `stable_anchors.py` | **0** | **6 anchor(s) checked — OK**,`stable-v6` EXISTS / PINNED / SHIPPED 三条全过 |
| `git push origin HEAD:main` | **0** | `c5c07e39..f0474dbb` |
| `git push origin stable-v6` | **0** | new branch |

⚠️ **一处读数上的自我更正**:第一次读 `git push origin HEAD:main` 的结果时我把
`… | tail -6` 后面的 `$?` 当成了 git 的退出码 —— **那是 `tail` 的**,而那一次 push
其实**被拒了**(远端有新提交)。第二次改成先重定向再读 `$?` 才拿到真码。
**这与 §7 那条守卫拦下的是同一个缺陷,只是发生在守卫长不到的地方**(章程第 0 条
已经写着「守卫只长在自检那一条命令上,发波循环、批量脚本里没有任何东西举手」)。

## 9. Token 用量

```
TOKENS total_in=29,486,270 out=105,695 turns=151
```
(统计时刻之后的收尾回合不计入。)
