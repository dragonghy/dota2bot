# 总监工作单元 — 2026-09-08T10:1xZ

**取活依据**:上一轮「下次触发」自己排的 **④**(退休 owed registry 里那 4 行 DONE,**需读一遍**四份产物,
**已顺延 3 轮**)与 **⑥**(patch 检查,上一轮逐字写着「**建议下一轮强制做掉**」,已顺延多轮)。
两条都取了。

**零 AWS、零波次、零 `bots/`+`game/` diff、不发 owner 邮件、无 promote/reject。**
`DECISIONS_NEEDED` +0。裁定档案 `iterations/streams/test_set.md §GA`;
机器键 `state.json:tpdying_a_INDETERMINATE_20260908` / `tpreach_a_WORKING_20260908` /
`owed_residual_field_20260908` / `patch_check_20260908`。

---

## 1. 一句话

**四行 owed 读 DONE,逐份读完只有两行可退休** —— 另外两行**自己的散文早就写着「不许据此退休」,
而工具在它下面一行每轮打印「请退休我」**。那是 §AW.1 / §DR 立案的形状(裁定落进没有人读的字段),
**在替它立法的 registry 内部复现了一次**;本轮给它一个字段(`residual`,GH #627)。

## 2. 两条判定(全文 §GA.1 / §GA.2)

### 2.1 `a_evidence_tpdying` → 退休,`tpdying` 的 (a) 判 **INDETERMINATE**

验收句逐条读通:两个 `§A'.3 约束 3` 点名的检测器**都建了并跑了**(`--selfcheck` 23 PASS / 0 FAIL)、
W49 / `--ref` 066219d6 / 4 粒种子 / 72-72 计分局 / 70 game-legs / **4,527 次响应 TP 落地**、
4(i-a) 两层给的是**读数不是局数**(pinned frames 的反号照登)、4(i-c) 每粒 swap-average **工具自己打**
(4/4 同向)、**全文零 gpm/xpm**。

⭐⭐ **判 INDETERMINATE 的理由不是语料不够,是嵌套。** `tpdying` 的子句住在
`J.GetTpCommitDefendDesire` 里,该函数第二行 `if not J.IsSoakCandidate('tpcommit') then return nil end`
(`jmz_func.lua:8913`,子句 `:8958`)⇒ 单独 armed 逐位 no-op;两者同 armed 时
`armed − baseline` 是「钉的**创建**(`tpcommit`)+ **两个**释放(`tpdying`/`tpdead`)+ 被移动的**分母**
(`teambrain`)」的净值,**连符号都不可分解**。章程 4a 禁止把 bundle differential 记到单个 id 上。

⇒ **这条 id 的 (a) 在一条 all-on 镜像波上是结构上买不到的**,而「买不到」与「没人去买」
在 verdict 表里都写作 `verify=0` —— **只有后者是催一催会好的**。
⭐ 不新建仪器:这一类已经有普查(`tests/test_gated_helper_nesting_census.lua`,**此刻 trunk 红**,
协同组 `tprecov` / `607ce30b`),而协同组**今天刚为同一形状开了 GH #622**。
`tpdying` 给 #622 补的是**量过的那一半**:嵌套不只是「入集可能买到 no-op」,
它让**这条 id 自己的 (a) 在嵌套还在时就买不到**。

残留(隔离腿:同一批 seed,arm `tpcommit` 而**不** arm `tpdying`)**换了执行人并开始花钱**
⇒ 另立 `owed_executions.json:tpdying_isolation_leg`。
⛔ 写进已退休行的 outcome 散文里就是 §DR / GH #540 量过的掉棒形状。

### 2.2 `a_evidence_tpreach` → 退休,判 **WORKING**;§BC.4 的字面要求**没被豁免,被搬走了**

`VERIFY id=tpreach verdict=WORKING episodes=59`。§BC.3 的自我制约守住(读数由录像组取)。
arm 串对**钉住的那棵树**核过(`git show 066219d6:…` 第 2 行第 28 位 / 61,md5 与 `W49_wave.json.arm_md5` 对上)。

理由(写成可攻击的形状):ADDED 是 `tpreach` **自己的**域;方向**由源码事前钉死**;
**4/4 粒种子向下、三张 reach 表全过**;**体积对照**把它与「普遍少按 TP」分开(总按压 ~3% vs ADDED 占比 ~42%);
组成比落在 `tpsafe2` **真会跑**的那一格(50% → ~20%,**两层各自复现**),§BC.1 保护的撤退格几乎没动(13 → 11)。

⛔ **产物点名要总监裁的那条限制,裁了**:非撤退 cell 单独看是 n=3 : 13 / **2-of-4 粒种子** / **只在 p50 表上成立**。
**不读成 INDETERMINATE,理由是铁律 4(ii) 不是偏好**——值域这么小的计数正是 4(ii) 说的不许当承重估计量的那种;
该格该读的是**层内占比**,而占比两层各自复现。**字面要求搬进 `tpreach_bc4_cell_reread`**:
重新入集前用更多粒种子重读,且那张 swap-average 表**要由 `tpreach_domain.py` 自己打印**
(产物 §8 自登的债,4(i-d) 形状)。

## 3. ⭐⭐⭐ 另外两行不可退休 —— 本轮最该被读的一条(§GA.3)

| 行 | 机器键 | 为什么读 DONE | 为什么不可退休 |
|---|---|---|---|
| `roshan_pit_daynight_fix` | `path_exists tests/test_roshan_pit_daynight.lua` | 文件 09-07 落地 ⇒ **永远为真** | **同一轮**把残留收窄成「一波 armed `roshpit` 的**行为**读数」,并在行内逐字写下「机器键此后读 DONE,**而本行不许据此退休**」 |
| `hero_domain_scan_2_30_31` | `path_contains_all`(九 id) | 九个 id 全被**提到** | 产物 §7 逐字写着 **hero-32 / hero-33 没交**(dumper 的 `creeps[]` schema:18,709,698 行单一键形 `t\|team\|x\|y`);且 **hero-2 / hero-34 交到了另外两个路径**,而入集裁定当时写的是「产物路径不变」 |

**两行都不是「有人偷懒」**:两处收窄都是上一任总监**当轮就写清楚了**的,写在 `done_when_note` /
`residual` 该在的位置上,**只是那个位置当时不存在**。

**处方(GH #627,本轮实现)**:可选行字段 `residual`(非空字符串)⇒ 状态 **RESIDUAL**:
finding、进退出码、**永不打印 retire 那一行**。
- `owed_status()` 拆成 `_machine_key_status()` + overlay,**键的读数留在 detail 里**
  ——「产物确实到了」是真的,是本行**仍开着而不是从没开始**的一半理由;
- 键未满足时 overlay 不生效(不许用更含糊的话换掉「文件不在」这句更锋利的);
- **写坏的 residual**(`true` / 空串 / 数字)读 **UNCERTIFIABLE 而不是被丢掉** —— 丢掉恰好等于在一个
  **作者正想说反话**的行上恢复 retire 那一行;
- `LIMIT 13` 写明买不到什么:看不见**没人写下来的** residual(LIMIT 9 同形),也不检查那句话仍然为真
  ⇒ 残留真没了要**手动清字段**,**这个方向是安全的那一侧**。

## 4. patch 检查(章程 2f)—— 做了,**无新 patch**(§GA.0)

`patchnoteslist` 裸读 `RC_EXIT=0`,**117 条,最新仍是 7.41e**(ts 1785394800 = 2026-07-30),
与 08-19 建档、09-01 复核**逐位相同** ⇒ 缺口边界不变(7.41b–e),`patch_gap_7.41b-e.md` 的 P1–P4 一条不改。
`docs/PATCH_UPDATE_GUIDE.md` 的 "Last updated for" **本轮不改**(P4 的前置 P1–P3 未做完;改它等于伪造进度)。

## 5. 验收与铁律 6

| | 读数 |
|---|---|
| `tests/test_pending_rulings.py` | **349 → 361 checks / 0 failed**,裸读 `RC_EXIT=0` |
| `tools/agent/mutstand_owed_residual.sh` | **7 CAUGHT / 0 SURVIVED / control_ok=1**,还原 `git diff` 逐字节 **YES**,裸读 `RC_EXIT=0` |
| `tools/agent/py_gate.py` | **84 ran / 0 findings / 0 uncertifiable / 14.4s**,裸读 `RC_EXIT=0` |
| owed 腿复读 | 19 行;**两行读 RESIDUAL**,**零行打印「请退休我」** |

⚠️ **M5 第一版是空变异**(在 `finding = True` 之前插 `finding = False`,被后一行覆盖)⇒ 它 SURVIVED,
**差一点被读成断言的洞**;按证据纪律 2 先怀疑断言,查出来是**变异自己的锚下错了**,已登记进脚本注释。
⚠️ 变异台第一次跑报 `RESTORE: NO` —— 原因是它按 **index** 比,而我的改动当时还没 `git add`。
**这不是缺陷是用法**:先 `git add` 再跑,第二次 `RESTORE: YES`。值得下一个人知道:
**这套变异台在未暂存的改动上跑,还原检查会假红。**

## 6. 本轮买不到的东西(一句都不许合并)

1. **`hero_domain_scan_2_30_31` 的九份读数没有逐份读通** —— 只读了 §7 状态表 + hero-2 / hero-34 两份产物的头部。
   `residual` 买到的是「这一行不再谎称自己可退休」,**不是**那九份读数正确(LIMIT 11 那一层谁都买不到)。
2. **`roshan_pit_daynight_fix` 的残留没有被推进**(挂在 owner P4.2 解冻之后);本轮只让它不再说谎。
3. **`tpdying` / `tpreach` 的 (b)/(c) 一个字没碰**;两条都在集外,不主张重新入集。
4. **⛔ 动态半(Lua `run_tests.lua`)未跑不作声称**(本轮 `bots/`+`game/` 零 diff)。
5. **开工自检跑完了(约 55 分钟),真码 `RC_EXIT=3`,10 条腿全跑完** —— 收尾前才回来,所以本轮的
   工作单元不是它挑的,但读数照登:`FINDINGS: cadence queue-rulings owed-executions`、
   `UNCERTIFIABLE: trunk-red(python)`(唯一那条是 `test_selfcheck_lua_leg.py` 的 Lua 腿顶爆 120s 预算,
   **GH #358,不是本轮引入**,9 个检查没跑成 ⇒ **那不是通过**)。
   ⭐ **上一轮交棒 ① 的两条 trunk Lua 红:本轮读到的是绿的** —— 自检的 trunk 腿报
   **86 个检测器文件 / 0 failures**,单独复核 `lua5.1 tests/run_tests.lua
   test_gated_helper_nesting_census.lua test_activemode_call_site_census.lua` ⇒
   **10 tests, 0 failures,裸读 `RC_EXIT=0`**。两条都由**归属组自己**修掉了(本轮不代改、也不居功)。
   ⚠️ **纪律 3 本轮第一发**:第一条命令又是 `routine_selfcheck.sh … | tail`,被 §22 守卫**当场拒**
   (那一次什么都没检查),**不新立措辞** —— 09-04 的措辞逐字覆盖它,**拦下它的是守卫不是我记住了**。
   ⚠️ **「后台包装吞掉真码」本轮第三次现场兑现**:harness 给该后台任务报的是 `exit code 3`,
   这次**恰好**与真码相同 —— 而**恰好相同不是被守住**;躲过去的仍然只是 `rc.sh` 把真码打进了输出文件。
6. 零 AWS ⇒ **不对 MTD 作新声称**。
7. **没有量 `residual` 会不会被滥用成「永久缓刑」** —— 它每轮报 finding,但**没有任何腿在数它的年龄**。

## 7. 下次触发

1. **`hero_domain_scan_2_30_31` 的九份读数逐份读通**(945 行 + 两份外挂产物)—— 这是本轮唯一
   **明知没做**而不是没时间做的那一层,而且它是那一行退休的唯一前置。
2. ~~两条 trunk Lua 红逐条~~ **本轮读到已绿**(见 §6.5,归属组自己修的)。接替它的是:
   **读一遍 `test_gated_helper_nesting_census.lua` 当作 GH #622 的仪器** —— §GA.1 刚给 #622 补了
   「嵌套让这条 id 自己的 (a) 买不到」这半个论据,而那份普查是唯一能说出**还有几条 id 是这个形状**的东西。
3. ~~串行重跑完整自检~~ **本轮跑完了**(约 55 分钟,`RC_EXIT=3`,10 腿)。
   接替它的是 **GH #358 的 120s 预算**:`test_selfcheck_lua_leg.py` 连续多轮顶爆它,
   9 个检查每轮都是 UNCERTIFIABLE ⇒ **自检里唯一那条 exit 2 已经常态化**,要人裁。
5. 上一轮 ⑤ 原样顺延(`text_absent_done_when_kind` GH #523 —— ⭐ **本轮给它找到了第二个真实用户**:
   `hero_domain_scan` 的 §7 里「NOT YET」是字面串,`kind: text_absent` 读得出它;**先写认领再开工** /
   GH #517 / `kind:"ruling_request"` / #454/#487/#460/#473 乙/#489/#486/#496 / #513/#514 /
   #449/#410/#436/#285 / **patch 缺口 P3** / `ckpush` 有时限)。
6. **给「后台包装吞掉真码」立一道守卫**(上一轮 ⑧,原样顺延;本轮没再兑现,但也没有被守住)。
7. 效率台账(章程 2e2,周日做)—— 今天是周二,不到期。

## 8. token 用量

```
TOKENS total_in=10,136,089 out=63,554 turns=70
```
(到统计时刻为止;统计后的收尾回合不计入。)
