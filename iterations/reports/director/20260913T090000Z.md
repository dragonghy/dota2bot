# 总监 2026-09-13T09:xxZ —— W37 效率台账(欠两轮,今天到期)+ 补一个从未存在过的锚点

**一句话**:章程 §2(e2) 的周日项写完了(`efficiency_202637.md`,第四份),
W37 邮件已发(Gmail `1a09a3cb7bea27df`,本周唯一一封,配额用完);
数 promote 数的时候**对出一个缺陷:本周 7 个 promote 里有 2 个从来没有锚点**,
补建 `stable-v9` 并给测试加了**反向读**的覆盖度一节,五发变异体 5/5 CAUGHT。
零 AWS、零波次、`bots/`+`game/` **零 diff**。

---

## ① 本轮的工作单元是被前两轮点名的那一件

上一轮(07:xxZ)「下次触发」清单第一条逐字:
「⭐⭐⭐ **W37 邮件 + `efficiency_202637.md`(章程 2e2,今天到期,已顺延两轮 ——
今天余下任一次触发必须做掉)**」。04:3xZ 那轮也写了同一句。
⇒ 本轮不挑活,做它。**今天是 W37 最后一天,再顺延就不存在「本周」可写了。**

## ② 台账本身(全文 `iterations/reports/director/efficiency_202637.md`)

一句话:**零 promote 的两周结束了 —— 本周 promote 7 个 id、armed 52 → 26,
是本项目开张以来产出最高的一周;而同一周里发波的钱被别人花到了刹车线上
($88.917 / $90),批测台空转八轮。瓶颈从「判不出来」变成了「买不起」。**

| 量 | W37 | W36 | W35 |
|---|---:|---:|---:|
| promote | **6 次裁定 / 7 个 id** | 0 | 0 |
| 退回出集 | **17** | 3 | — |
| **新入集 ADMITTED** | **0** ⚠️ | 2 | — |
| armed | **52 → 26** | — | — |
| 计分局 | **1300**(W62–W69 八波) | — | — |
| token 合计 in | 3.70 B / 256 份 | 3.03 B / 251 份 | 2.59 B / 263 份 |

⭐ **W36 §4 的归因本周被推翻了一半,而推翻的方式值得记**:上周写「瓶颈是波次配置」,
本周证明瓶颈确实是**可判性**,但打开它的**不是重排波次**(要花钱,这周没钱),
**是把买不到 (a) 的 id 退出集** —— 17 次退集与 7 次 promote 是**同一个动作的两面**:
61 条腿压到 26 条,剩下的每条才分得开。**便宜的那条路先走通了,而 W36 没把它列为选项。**
⚠️ 代价一起登记:**ADMITTED = 0**,只出不进;靠退集走到解冻线 20
**是一个达标的计数,不是一个变好的项目**(§HA.3 立案句,这次用在测试集规模上)。

✅ **W36 §5 乙(i) 交来的那件事办了,结论是「不动」**:strategy 的 in/报告
17.50 → 18.04 M(**+3%,上升停了**)⇒ W36 预置动作的触发条件(「仍上升」)
**没有成立**,按它自己写下的判据不改章程。📌 **W36 那句「按错归因改章程比不改更贵」
被自己的读数兑现了。** 下周那一格换成 **hero(+30%)**。

## ③ ⭐⭐⭐ 本轮量出的缺陷:**一条从未被写下的锚点,在每一轮里都是 ok**

`zusult` + `zusboltdom` 09-11 作为一个原子 promote(闸从 `bots/BotLib/hero_zuus.lua`
删除,机器键 `state.json:zusult_zusboltdom_PROMOTE_20260911`,裁定 `test_set.md §GW`)。
**`iterations/stable_anchors.json` 里没有它的行,origin 上没有 `stable-v9`。**
两天、十余轮触发,**全绿**。

⛔ **静默是结构性的,不是谁忘了**:`stable_anchors.py` **先读登记表、再问每一行健不健康**。
一条**从未被写下**的锚点**不是一行 ⇒ 没有东西检它 ⇒ 打 ok**。
`stable_anchors.json` 的 `_doc` 写着「**漏登记不会自己举手 —— 加那一行就是让它会**」,
📌 **而这句话只对已经加了的那一行成立,对下一个被忘掉的一个字都没说。**

📌 **它不是在一个混乱的周里被忘的**:同一周 v5/v6/v7/v8 四个锚点**登记全对**,
六次裁定只漏一次 ⇒ **不是流程没人执行,是流程没有反向的门。**
⚠️ **发现方式是运气** —— 为写台账去数 promote 数,顺手把 `state.json` 的
`*_PROMOTE_*` 键与 `stable_anchors.json` 的 `promoted_ids` 对了一次账。

**两件都做了:**
1. **`stable-v9` 已推到 origin**,指着 promote commit `2a82d6bc` 本身 ⇒ 不变量 3 平凡成立。
2. **`tests/test_stable_anchors.py` 新增 §coverage**,**把登记表反过来读**:
   从 `bots/`+`game/` 的 `PROMOTED … (was soak-candidate '<id>')` 注记出发,要求每个 id
   **要么被锚点覆盖、要么在一张冻结的 pre-registry 白名单上**(10 条,锚点表建立前 promote 的,
   凑不出 ref_sha,不可能补);外加**反方向**(锚点不许声称树上没有的 promote)、
   **白名单不许当停车场**(每条必须仍是树上的 live promote,且不许同时被锚点覆盖)、
   以及一条**语料非空地板**。**那一节若 09-11 就存在,这条会在当轮红。**

### ③.1 ⛔ **而这一节的第一版,自己就是它要抓的那个形状 —— 一道比它点名的缺陷更窄的棘轮**

第一版只匹配 `was soak-candidate '<id>'`,打出 **`23 promoted id(s) (13 anchored, 10 pre-registry)`**,
**23 = 13 + 10 零余数**,看起来是一个漂亮的完全划分。**它漏了两个已 promote 的 id:**

- **`pushguard`** 的注记是 `was soak **candidate**`(**没有连字符**,三个 mode 文件里各一处);
- **`tpsafe2`** 同样没有连字符(`bots/ability_item_usage_generic.lua:5270`)。

⇒ 那个「零余数」**是假的**:真值是 **25 = 13 + 12**,而第一版把两个漏掉的 id
连同它们的缺席一起算平了。📌 **这正是 04:3xZ 那轮的立案句用在它自己作者身上:
「一道比它点名的缺陷更窄的棘轮,读起来和一道正拦着的棘轮一模一样」——
它每轮打 ok,而 ok 的含义是「在我看得见的地方没有」。**

⛔ **而正则自己永远报不出这一条**(一个模式匹配不到它匹配不到的东西)。
抓到它的是**后加的第二个独立信源**:`AGENTS.md` 那张**由人手维护**的
「Promoted turbo defaults」清单 —— 它里面每一个 id **必须被扫描看见**(单向:
散文允许落后于一次新 promote,而扫描漏掉一个**已登记**的才是缺陷)。
**如果没有加这条对账,本轮会把一道有盲区的棘轮当成完工交出去。**

⚠️ **这条对账的第一版自己也错了一次,而错法值得记**:段落捕获跑到空行,
**把清单后面那句「`nodive2`/`ownhalf` 这两个扩展**仍然 gated**」一起吞了**
⇒ 当场报两条假缺陷。📌 *一个连「什么不在清单上」那句解释也一起刮的清单刮取器,
比没有刮取器更坏:它制造发现,而永远错的发现会被忽略 —— 连同底下那条真的。*
已把捕获收到第一个 `(` 为界,并加一条「清单不许缩水」的地板(现 15,断言 `>= 15`)。

✅ **最终验收:八发变异体 8/8 CAUGHT**(台子从**四个**文件副本恢复,收尾逐一 `diff`
**全部逐字节还原**,post-restore `BARE_EXIT=0`):
M1 删掉 `stable-v9` 行(**本周真缺陷**)/ M2 把漏掉的 id **停到白名单上**而不登记
(被「冻结在 12 条」拦下)/ M3 锚点声称树上没有的 promote / M4 **攻击答案不是删检查**
(`SRC_DIRS` 指向改过名的目录 ⇒ 扫描静默变空,被语料地板拦下)/ M5 两锚点抢同一 id /
**M6 把正则退回只认连字符那一版**(= **本节自己的盲区**,被 `AGENTS.md` 对账抓住)/
**M7 让对账的捕获变空**(表头改一个字 ⇒ 被「不许缩水」地板抓住)/
**M8 删掉 `tpsafe2` 的真注记**。
⭐ **M4 的写法是上一轮教训的直接产物**:上一轮 M4 第一版 SURVIVED 且**不算证据,因为它是删除不是变异**
——「一个把检查删掉的变异体和一棵干净的树,在退出码上完全相同」。本轮从一开始就攻击答案。
读数:**`ok -- 9 anchors, 25 promoted id(s) in source (13 anchored, 12 pre-registry)`**。

⚠️ **M8 前两版是坏变异体,而坏在同一个地方:它们没有攻击答案。**
第一版删的是我**当轮自己往 `jmz_func.lua` 加的**一条注记 —— 而我在同一段解释文字里
**把那个标记原样又写了一遍**,于是删掉标记之后扫描**从解释里**照样读到它 ⇒ SURVIVED。
第二版换了删法仍 SURVIVED,**逼我去数**,才发现 `tpsafe2` **本来就有一条合法注记**
(只是没连字符)⇒ **我加的那条 `bots/` 注记从头到尾就是多余的,已整条 revert**。
📌 两条教训:*(i) 一段引用了自己所解释的模式的注释,本身就是第二个匹配;
(ii) 一个删掉两份冗余标记之一的变异体不是缺陷,它的存活是对的* ——
真正的 M8 要删**唯一**那一份,删了之后 **exit 1,CAUGHT**。
⇒ **`bots/` + `game/` 全程零 diff 得以保持**,而保持它的不是克制,是那两发坏变异体。

## ④ ⚠️ 补登记时差点在登记表里写下一个**没有发生过的事件**

`git merge-base --is-ancestor 2a82d6bc origin/main` 第一次答 **no**,
`git log origin/main -- bots/BotLib/hero_zuus.lua` **只回一条**。
按 `stable-v7` 的先例这**看起来**正是「promote commit 被 rebase 挪过」——
那一条 note 里逐字记着同型现场,**差一点就照抄**。

真因是**浅克隆**:routine 容器默认 depth 50,promote commit 在 graft point 以下 ——
**这正是 `stable_anchors.py` 的 LIMITS 第 2 条写着的那个条件,而我是在读到自己工具的文档之前先信了先例。**
`git fetch --deepen=200`(depth **50 → 404**)之后两条读数**都翻面**:ancestor **yes**、
first-parent **yes**,`2a82d6bc` 就是 origin/main 上的那一个,**没有任何 rebase 发生过**。
📌 **立案句:一个浅克隆的「查无此物」,和一次真实的历史改写,在命令行上长得一模一样;
而前者会诱使你在登记表里写下一个没有发生过的事件。** 已写进 `stable-v9` 的 note。

## ⑤ ⛔ **一条我差点用错误归因交出去的 trunk red —— 而纠正它的是「在安静树上重跑一次」**

**我先写下的版本是错的,原样留在这里,因为它错得有形状。** 那一版说:
自检报 `test_py_gate_hook.py` **10 checks / 6 failed** 而**单独跑是 10/0 全绿**,
所以它是「我的量具被我自己搅了」—— 归因给我在自检跑着时并发跑的变异台
(同族第五发,上一轮 (庚) 刚记过「先定型再跑」)。

⚠️ **然后安静树上的第二次自检把它复现了:仍然 `10 checks / 6 failed`,而这一次没有任何并发。**
⇒ **并发归因被证伪。** 隔离出来的真触发条件是**另一件事,而且它是一条真缺陷**:

| 工作树 | `RULE6_MEMO` | `test_py_gate_hook.py` |
|---|---|---|
| **脏**(我改到一半时) | 不生效 | **10/0 PASS** |
| **干净**(commit 之后) | **命中** | **10 checks / 6 failed** |

失败文本里逐字 `py gate: 1 ran, 0 findings, 0 uncertifiable, 0.0s`,
键那一行自称 `… and a clean working tree`。
⇒ **这个测试把一个 python 棘轮**故意**弄红、再要求钩子拒绝 push;而 memo 在干净树上
把一份 CLEAN 直接发还给它,钩子于是「通过」,断言 1a/1b/2a/2b 全倒。**

📌 **立案句:上一轮为提速加的记忆化,悄悄让「守着 push 闸的那个测试」不再测任何东西 ——
而它只在树干净的时候这样,也就是只在 push 真正会发生的那些树上。**
(上一轮报告还把 memo 记作「当场兑现」的收益;收益是真的,这是它的价钱。)
⚠️ 它**不是本轮造成的**:上一轮 (戊) 那份被 `timeout` 掐断的日志里就有
`py_gate_hook 10/6 failed` 这一行 —— 当轮修了同批的 `carrier_terms`,**这条留下了**。

⛔ **本轮不修它**,理由是**排期不是能力**:修法要动 `rule6_memo` 与 push 闸本身,
而这是仓库里在一轮收尾时最不该动的东西。**已按铁律 5 交出去(见 ⑥ 己),
附完整复现配方** —— 复现配方是这件事里贵的那一半,它已经买到了。

⭐ **本轮真正该被读的,是纠正它的机制**:两次自检的差别只有「树静不静」,
而我原以为那是**噪声源**,结果它是**信号源** —— 安静树上的第二次跑不是走过场,
**它是唯一一次真的在看这条腿**。📌 *一个「被并发搅了」的归因,和一条真缺陷,
在第一次读数上长得一模一样;区别只有重跑一次才买得到。*

⚠️ **顺带,「先定型再跑」那条本轮仍然违反了**(变异台确实与第一次自检并发),
只是它这次**不是** `py_gate_hook` 的成因。两件事本轮同时为真,不许合并成一条。
⚠️ **纪律 3 第三十六发,连续第三轮**,仍是本轮第一条命令的
`routine_selfcheck.sh … | tail -60`(守卫拒,`SELFCHECK_EXIT=2 REFUSED`),
**章程第 0 条逐字覆盖它**。

⚠️ 另记一笔**没有**复发的:纪律 3(管道读退出码)本轮**第一条命令仍然踩了**
(`routine_selfcheck.sh … | tail -60` → 守卫拒,`SELFCHECK_EXIT=2 REFUSED`,**第三十六发,连续第三轮**),
改重定向后拿到真码。**章程第 0 条逐字覆盖它,而它连续三轮由同一条命令打出来。**

## ⑥ 交棒(全部落到机器可读的字段,不只写报告)

- **甲 → owner**:W37 邮件**已发**(Gmail `1a09a3cb7bea27df`)。带出去的唯一动手项仍是
  **第 15 条:给 `dota2bot-batch` 预算加 `Project` 过滤器**,与 W36 同一条、**已 7 天无回复**,
  而本周它**从预判变成读数**(余量 `$1.083`,spot 波都过不去,批测台空转八轮)。
- **乙 → 批测台**:`iterations/owed_executions.json` 新增一行
  `games_ledger_cross_wave_accounting`。`games_ledger.jsonl` 连续第三周 **391 行 /
  `run_20260719_1353`**,而同周产出 1300 局 ⇒ 跨波机器可读账**从来没存在过**。
  ⭐ **本行的全部新意是换载体**:W35 记「棒掉了」、W36 记「棒在但没人捡」,
  **两次都只写进了台账**;这是 §DR 的常驻义务形状(不属于任何一波 ⇒ 任何**波次局部**
  的收割清单里**没有行可以被写进去**)。`done_when` 裸读 = `"game_id":"202609` 出现在账里
  (今天 **0 次命中**,全部 391 行都是 `202607`)。开行见证 `RC_EXIT=3`,逐字
  `1 of 1 required mention(s) are absent: "game_id":"202609`,日志 `/tmp/rc.w5OA38`。
- **丙 → `DECISIONS_NEEDED.md` 新增「邮件投递台账」一节**:把「本周配额用了吗」
  从**一次 grep 27 份报告**(本周实测 65 处 `不发 owner 邮件` + 3 处 `W36 邮件`)
  变成**一行表**。📌 与 ③ 同型:**判据在,载体是散文。**
  同节更新第 15 条(预判 → 读数)与第 16 条(已告知 owner「未回复即按 (甲)」)。
- **丁 → 下周总监**:(i) **hero 的 in/报告复查**(+30%,W36 那一格从 strategy 换成它);
  (ii) **director 自己的 in/报告 与 promote 数配对看** —— promote 回落而投入不回落就是空转,
  **这一条是给我自己的,不许略过**;(iii) **ADMITTED = 0 的补给侧**;
  (iv) 每次 promote **先 push 成功、再写 ref_sha**,而现在 §coverage 会替你举手。
- **戊 → 不改任何组的章程**(strategy 触发条件未成立;hero 归因不够硬)。
- **己 → 总监自己(`owed_executions.json` 新增第二行)**:
  `rule6_memo_defeats_py_gate_hook_test`(§⑤)。**`RULE6_MEMO` 让守着 push 闸的那个测试
  不再测任何东西,且只在树干净时如此** —— 也就是**只在 push 真正会发生的那些树上**。
  复现配方两个方向都实测到(脏树 `10/0 exit 0` ↔ 干净树 `10 checks, 6 failed exit 1`;
  手动 `py_gate.py` 预热同样复现),**这是这件事里贵的那一半,已经买到**。
  `done_when` 裸读 = 该测试文件里出现 `memo`(今天 `grep -c` = **0**,它对架空自己的机制一无所知);
  ⛔ 退休它的人**必须同时贴出「memo 预热之后跑、仍然 CAUGHT」的读数** —— 冷 memo 下的绿不算。
  ⚠️ 同族已有 01:0xZ 开的 `rule6_memo_fast_gate_membership`,**两行不同**,不要合并。

## ⑦ 成本

💰 **本轮零 AWS 调用**(一次都没有)。MTD `$88.917` 与逐日 CE 全部**引自批测台已付费的读数**,
**未重复购买**。总监本周自身支出 **$0**。账户级预算问题等 owner 回复,期间按保守侧。
🩺 五组 24h 内全部有产出,无掉队组;批测台空转八轮是**正确的持有**(钱不够,不是没活)。

## ⑧ 铁律 6 与自检读数

**开工自检跑了两次,而第二次不是走过场 —— 它是本轮唯一一次真的在看那条腿(§⑤)。**

- **第一次**(07:0xZ 起跑):与我的变异台并发 ⇒ **该次读数本轮不引用**。
- **第二次(安静树,所有文件编辑完成之后)**:**真码 `TRUE_EXIT=3`**(裸读,无外层 `timeout`
  —— 上一轮 (戊) 的 `124` 是自己掐的,本轮没有再犯),`legs run 12`,
  **`UNCERTIFIABLE (exit 2): none`**,`NOT RUN (inside a leg): tests/test_selfcheck_lua_leg.py`。
  `FINDINGS (exit 3)`:`unlanded` / `cadence` / `queue-rulings` / `owed-executions` / `trunk-red(python)`。
  逐条归因:**`owed-executions` 是本轮自己新登记的两行**(按定义即 OWED);
  **`trunk-red(python)` = `test_py_gate_hook.py`,已在 §⑤ 定性并交出去,非本轮造成**;
  `unlanded` / `cadence` / `queue-rulings` 为存量。
  fast Lua 检测器 **87 文件 0 失败**。
- ⭐ **九个 stable 锚点全部 `EXISTS / PINNED / SHIPPED ok`,含本轮新建的
  `stable-v9`**(`EXISTS ok (refs/heads/stable-v9)` / `PINNED ok (2a82d6bc9d50)` /
  `SHIPPED ok (ref IS the promote commit -- trivially identical)`)⇒ **补登记被工具自己复核过了**,
  不是我说它对。
- ⚠️ **`git stash` 重跑那一步本轮没做,「main 是不是也红」不声称。**
  但 §⑤ 的两方向复现已经独立说明:`test_py_gate_hook` 的红**不依赖本轮任何改动**
  (它依赖的是「树干净」),而本轮 `bots/`+`game/` **零 diff**、`tests/` 只动了
  `test_stable_anchors.py` 一个文件。

**铁律 6 三条腿(两次真跑,安静树)** —— ⛔ **全程未用 `RULE6_BYPASS`**:

| 推送 | `luacheck_gate.sh` | `py_gate.py` | `lua_gate.py` |
|---|---|---|---|
| 推分支 | `GATE_EXIT=0 CLEAN`(0 警告) | `98 ran, 0 findings, 0 uncertifiable, 34.9s` | `360 ran, 0 findings, 0 uncertifiable, 9 known-red, 460.4s` |
| rebase 后推 main | `GATE_EXIT=0 CLEAN` | `99 ran, 0 findings, 0 uncertifiable, 34.5s` | `361 ran, 0 findings, 0 uncertifiable, 9 known-red, 460.3s` |

⚠️ **推 main 第一次 `non-fast-forward` 被拒**(录像组与协同组三个 commit 先落地:
`0258f179..7ccdcdd3`,含 `strategy: roshpost` 一条 `bots/` 改动);
`git pull --rebase origin main` **`REBASE_EXIT=0`** 无冲突,落地 **`7ccdcdd3..ec747fa0`**。
⭐ **rebase 之后我重跑了本轮新增的那个测试再推**,不是假定它还绿 —— 新 main 带进了
`bots/` 改动,而 §coverage **正是扫 `bots/` 的**;读数逐字未变
(`25 promoted id(s) (13 anchored, 12 pre-registry)`),`BARE_EXIT=0`。
⚠️ 第一次(被拒的那次)打了 `RULE6_MEMO=REUSE … on THIS EXACT tree` 复用读数;
rebase 改了树 ⇒ 第二次**真跑**(`py gate` 98 → 99、`lua gate` 360 → 361 就是新进来的测试)。
📌 **顺带给 §⑤ 加一条现场证据:memo 确实按树内容失效** —— 缺陷不在「memo 会乱复用」,
而在「`test_py_gate_hook` 自己的子调用也吃这份 memo」。

⚠️ 动态那半(`lua5.1 tests/run_tests.lua`,~100min,GH #124)**未全量跑,不声称**
(`bots/`+`game/` 零 diff)。

## ⑨ 下次触发

① ⭐⭐ **判定完结 ≥2**(`overchase` 豁免**本轮已到期** / `pullcad` 的两条轴要正面定一次)
② ⭐ **`test_set.md` 拆文件**(第 16 条 (甲) **已告知 owner 为默认动作**,可直接开工,不必再等;**不删一个字节**)
③ **ADMITTED = 0 的补给侧**(§②)/ **hero in/报告复查**(§②)
④ promote 期三个普查只读 `bots/`(第十轮顺延)/ GH #523(第九轮未取)/
   `creeps_schema_gh581` 的 (C) 半 / GH #584 / 五条 `a_evidence_*` / patch 缺口 P3

## ⑩ token 用量(铁律 8)

`TOKENS total_in=16,123,785 out=91,480 turns=97`(收尾回填前)。

⚠️ **归因可命名,不是空转**:一次 **8 发变异台**(其中 M8 做了三版,前两版是坏变异体)
+ **两次开工自检**(第一次与变异台并发而作废,第二次安静树跑满)
+ **三次推送门**(每次 `lua gate` 约 7.7 分钟)+ 一次 `--deepen=200`。
📌 与上一轮(`total_in=15,665,466 / turns=96`)几乎持平,而本轮多交付了一份周台账
和一封邮件 —— **涨的那部分买到了 §③.1 与 §⑤ 两条更正**,两条都是「先写下的版本是错的」。
