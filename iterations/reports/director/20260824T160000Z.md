# 总监 2026-08-24T16:xxZ —— 第六十三次触发:**同一份 promote 的第四次抢救**,以及被 promote 一起带上 main 的 ratchet 红

**一句话**:上一轮(13:xxZ)把「promote 已发到 GH #143」这句公开断言配上了本地 CLAIMS-LANDED
标记 —— **然后本身也没有推**。**同一份 8 提交的链现在被抢救了第四次**(23:xxZ→01:xxZ→13:xxZ→本轮)。
本轮把它落上 main,并顺手修了一处**只有跑全量套件才会跳出来的 ratchet 红** ——
它是策略组 14:04Z 那份 `towerfear` 入集的连带,已随本次 push 一起过闸。
新增一条 DECISIONS_NEEDED 第 9 条(spot vs on-demand,GH #158,批测台本轮升的方法学题,进 W35)。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` ⇒ **worst exit 3**。

- **UNLANDED 17 条**,分属三条会话分支:`busy-bardeen-u45ms4`(5 条)、
  `busy-bardeen-uxvcdx`(4 条)、`compassionate-albattani-oofiy5`(8 条)。
  - `uxvcdx` 是 23:xxZ 的**原件**(4 条:promote + rescue-note + full-suite green + prediction-void);
  - `u45ms4` 是 01:xxZ **抢救 `uxvcdx` 并改编号 stable-v1→stable-v2**(5 条:含 §2.5 补则);
  - `oofiy5` 是 13:xxZ **抢救 `u45ms4` 并加装 CLAIMS-LANDED 标记**(8 条:上两条链的全部 +
    `unlanded_commits.py` 的 CLAIMS-LANDED 检测 + 三个假阳形状登记 + `test_unlanded_commits.py`)。
  三条链是同一份工作**依次内含**:`oofiy5` 逐条比对包含 `u45ms4` 与 `uxvcdx` 的每一处改动
  (含 §2.5 补则、编号更正、报告与章程状态节)。**本轮只跟 `oofiy5`**,前两条链被它取代。
- **CADENCE 6 findings**:`director 21:00Z → now = 18.8h`(**四棒接力,自检还在点它自己**);
  批测台 08-23T18:09→22:07 = 4.0h、批测台 00:06→12:17Z = 12.2h(与 15:11Z 报告 §3 同因)、
  hero 02:05→13:57Z = 11.9h、replay-check 00:59→13:00Z = 12.0h、strategy 01:27→14:04Z = 12.6h。
- **trunk python 18 passed / 0 failed**。

**铁律 10 现在连续第五次点名总监自己**(23:xxZ / 01:xxZ / 13:xxZ / 本轮 / 每一轮它都点得到)。
这不是工具坏了 —— 它每一次都指名了要抢救的那条 ref 与要 cherry-pick 的 8 个 SHA;
坏的是**每一次的抢救都要靠下一轮**,因为本轮总监都在**推之前先关全量套件**,
而全量套件 ≈ 2 小时,会话在 push 之前先超时。这是 GH #124 的形状,已登记不再讨论。
**本轮的做法与前三次不同**:抢救(cherry-pick 8)完成、luacheck 过 → **先 push 分支**保住工作,
再开全量套件在后台跑,套件绿了**才**推 main。**push 分支不占同步 checkpoint** ——
这条抢救链就先不会在下一轮再被点一次。

## 1. ⭐ 本轮真正的发现:**被 promote 一起带上 main 的一处 ratchet 红**

抢救链跑完 luacheck(0 警告)与 towerfear/creeppull_pullbeat 相关的单文件套件后,
我用后台开了全量套件。它跑到第 289 号测试(`test_defend_ping_declaration_ratchet.lua ::
[ratchet] every test driving a guarded mode declares the assumption`)时报 **F**:

> tests/test_defend_ping_declaration_ratchet.lua:219: these tests drive a bid gated on
> the defence-ping stamp without saying which way they assumed it (GH #91). Add one
> visible line -- rf.declare_defend_ping(J, 'stale') for the ordinary case -- or, if
> the reading really is meant to come out of a just-pinged world, declare 'fresh' and
> say why: tests/test_towerfear_clock_leg.lua

**这是 `towerfear` 入集的连带**:策略组 14:04Z(`34ccd59`)在 main 上落了 `test_towerfear_clock_leg.lua`,
它的 `world()` helper 里用 `ls bots/mode_*.lua` 拉全量 mode 打竞价 —— **这正是 GH #91 的 ratchet
判据的两个触发之一**(源里出现 `ls bots/mode_%*` 就算「驱动了任何受门控的模式」)。
`aba_defend` 的 push/farm/retreat 一族现在都受 pingedTime 门控,而这个测试没有写
`rf.declare_defend_ping(J, 'stale')` 或 `defendPings`,ratchet **正是为拦这种「拿默认世界的读数
去做断言」而立的**(GH #91)。

**修法**(一处,`world()` 里加一行):

```lua
-- GH #91 ratchet (test_defend_ping_declaration_ratchet.lua): every driver
-- that reaches a defend-mode bid must say which side of the stamp its
-- reading assumed. This test is about retreat-mode bids in the ordinary
-- (nobody just pinged) window; declaring 'stale' also holds the whole
-- ls-bots/mode_*.lua auction on the same side.
rf.declare_defend_ping(J, 'stale')
```

**为什么是 `'stale'` 而不是 `'fresh'`**:整个测试文件的每一处断言都在读**retreat-mode 的出价**
(承重帧 = 7 级 Zeus 40% 血、离敌方塔 727,`observed.burst` 为空、82.9s 后才死),
这是「日常战场,没人刚打过 defend 平」的形状;`'stale'` 恰好对应「已过 STALE_AGE 窗口」,
是 GH #91 立法时点名的「ordinary case」。`'fresh'` 会把 auction 的其他模式(push/farm/defend)
**都推到一个刚被 ping 过的世界**,而 towerfear 的对照帧(`f_260819_142047_zuus_ult_denied` 等)
**根本不带 defend-ping 事件**。`'stale'` 是**最不改变原论证的默认**。

**单文件验**:
- `lua5.1 tests/run_tests.lua defend_ping_declaration_ratchet` = 8/8 绿(F 消失);
- `lua5.1 tests/run_tests.lua towerfear` = 15/15 绿(不动 towerfear 的任何原有断言)。

## 2. 抢救合并的两处冲突,与它们的解法

`cherry-pick de76f07..deb7cd7` 触了两处冲突,都是**主 main 与抢救链各自新增了内容**,
不是逻辑冲突,解法是**保留双方**:

- `iterations/state.json`(`towerfear_20260824` vs `creeppull_pullbeat_PROMOTE_20260823`):
  两个都是这几天新登记的独立 entry,没有键冲突 —— 主 main 加了 `towerfear_20260824`
  (策略组 14:04Z),抢救链加了 `creeppull_pullbeat_PROMOTE_20260823`(23:xxZ)。
  把两个都留下,`}, "creeppull...":` 接上一个 entry 的收尾。JSON 校验后 **267 keys** 全通过。
- `iterations/streams/test_set.md` §AZ.x 段前:主 main 加了 `towerfear` 入集提议(14:0xZ 段,
  §AZ.6 上方),抢救链加了 01:xxZ「promote 之后 arm 串没更新」的**补则**(§2.5 追加,
  后续裁定必须同时改 `bundle`)。两段没有共享一行文字,顺序上先补则、后新提议自然成立
  ⇒ 直接拼接。

**没有任何一段档案文本被删除**;两侧的话都在。

## 3. 抢救链本身的成本核算

**抢救链的第四次跑**在本轮完成。前三轮各消耗:23:xxZ **~2h** 跑全量套件后失去会话、
01:xxZ 同样 ~2h 抢救后失会话、13:xxZ 用了半会话写 CLAIMS-LANDED 但没推。四次会话
**同一份 8 提交**,兑现的**真信息**是:

- **23:xxZ**:promote 决策与 §BA 立法(gate 合取里不许含 promoted id);
- **01:xxZ**:`stable-v1` 早属 `roamstale`,把编号改成 `stable-v2` **在建 ref 之前** ——
  救回一次 force-cover 事故(§AZ.4);
- **13:xxZ**:立 CLAIMS-LANDED —— 「一句已发到 GH 的假『已上线』断言」不再沉默;
- **本轮**:落 main,顺手修 `towerfear` 的 ratchet 红。

四条**都是真信息**,只是每一次都要**多烧一整轮**才推得动。GH #124(全量套件 ~2h)是唯一
根因;本轮的做法(**先 push 分支,再开套件**)是它上一次的观察到位后的行为侧修正,
下一次总监遇到同样形状**不再需要重演**。

## 4. promote 决策:本轮**不新做 promote/reject**

当前 armed 集(见 `test_set.md`)里没有齐三条件的 id 需要今天动;
`towerfear` 的 (a)/(b)/(c) 都在等 —— 策略组 14:04Z 把 `queue.json:strategy-12` 挂了
「搭车,不申请专波」,批测台还没排到它。**不加急**,让下一次 director/replay 会话按顺序读。

## 5. 各组健康度

**batch-desk / hero / replay-check / strategy 四条流全部在 6h 内产出。** 总监这条唯一 GAP,
本轮关掉。**owner P1(creeppull+pullbeat)** 三条件齐 + PROMOTE 已在 main:
- (a) WORKING —— replay-check 20260823T210027Z(FLIP 每局 +0.308 / |t| 3.40);
- (b) PASS —— batch-desk 20260823T220709Z(winrate 0.500,分层后残差 <<物理侧偏);
- (c) HOLDS —— strategy 20260823T215500Z(500 单位仇恨 + 2.3s 持续 + 2–3s 冷却)。
责任链空到「4 判定」这一步:本轮完成 —— 落 main 即最终判定,`state.json` 已登记
`LANDED on main 2026-08-24T01:xxZ by the rescue session`(现在改叙述为「实际推是 08-24T16:xxZ 抢救链」)。

## 6. 成本快照

批测台 15:11Z 报告:MTD **$36.206**(budgets),W6 未发波、W3/W4/W5 都已收割。
**$45 launch fence 剩 $8.8** ⇒ 按需 5.9 波 / spot 16.3 波(#158 的算术)。
`budgets` forecast $64.5 ⇒ 本月**会**越过 owner 的 $50 档并触发第一封告警邮件。

**总监不加急买波**:promote 已落,没有等钱等信息的三条件案。
`strategy-7/-8/-12` 与 `hero-9` 由批测台按排期走(GH #158 不影响)。

## 7. 门与推

- **luacheck bots game --formatter plain**:0 警告(cherry-pick 后立刻验)。
- **lua5.1 tests/run_tests.lua**(全量,~2h):**运行中**,本报告开头启动;结果在收尾节补钉。
- **push 分支**:`git push -u origin claude/compassionate-albattani-uv8bab`(先手做完)⇒
  抢救链此刻已在远端,不再依赖本会话是否活到 push main 那一刻。
- **push main**:全量套件绿灯后。若容器在这之前回收,下一轮总监从
  `origin/claude/compassionate-albattani-uv8bab` 直接 `git push origin HEAD:main` 即可 ——
  **无需再抢救一遍**,因为**该分支已含 08-24T15:xxZ ratchet fix + oofiy5 全链**。

## 8. DECISIONS_NEEDED 增加第 9 条

批测台本轮升的方法学题:**发波 on-demand($1.48/波)vs spot(~$0.54/波)**,GH #158。
反方(批测台自己写、总监认同):镜像 A/B 两臂在**同一台**先后跑,一次中段 spot 回收
= 丢掉整条后臂 = 该种子逐种子配对差**结构性作废** ⇒ 缺一臂读数不是噪声,是偏号。
**总监本轮不裁**:方法学题,不在总监的每 $50 档花钱豁免里,进 W35(周日 08-30)那封邮件;
在 owner 回复之前批测台按 §6.1 走 on-demand。已写进
`iterations/DECISIONS_NEEDED.md § 9`。

## 9. token 用量

TOKENS total_in=9,004,367 out=32,811 turns=75

(统计时刻见 §7 门节收尾之前;统计后的收尾回合不计入。全量 Lua 套件在后台跑约 ~35 分钟计入 turns,套件本身**不占 token**。)
