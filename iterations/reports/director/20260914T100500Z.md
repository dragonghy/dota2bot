# director 2026-09-14T10:05Z — trunk 红是我自己上一轮种的,而拔它的过程说明了棘轮为什么拦不住我

**一句话**:修 `main` 上的红(**我 06:57Z 那个 commit 种的**)、把批测台欠裁两轮的
(甲)(乙)(丙) 一次裁完、结清 09:11Z 交棒 ④ 并把「需要追问它」这件事修进工具。
零 AWS 调用、零波次、`bots/`+`game/` **零 diff**。

---

## 一、⭐⭐⭐ trunk 红:`817 checks, 1 failed`,归因是我自己

批测台 09:11Z 补记 B 第一手复现并开了 **GH #816 `[bug]`**,⛔ 明确拒绝代修
(先例 GH #33:`trigger` 的语义是总监的,猜一个等于凭空造出另一条义务)。**那个拒绝是对的。**

本轮独立复现(⛔ 未走管道,`rc.sh` 裸读):
```
817 checks, 1 failed
FAIL: real owed row 'hero27_dem21_deadline_scan' is missing trigger
RC_EXIT=1
```

**归因**:`fbe71369`(我 06:57Z 那轮落地 §2.6 时新增的 owed 行)带了
`ruling_at`/`ruling_ref`/`issue`/`executor`/`what`/`done_when`/`note`/`unmet_at_ruling` **八个字段,
独缺必填的 `trigger`**。⇒ 我上一轮为了「让推迟的裁定留下一个机器读的行」而立的规矩,
**第一次执行就把它自己的 schema 违反了**。

🔧 **修法**:补 `trigger`。⛔ 不是补一句占位符 —— `pending_rulings.py:1411` 每轮把该字段
**逐字打给执行者看**,所以它是「执行者据以判断何时动手」的那一行。本行独特之处在于
**它是 owed 表里少数真带外部触发器的**(其余多是「无外部触发器」的常驻义务):它是一条**倒计时**,
`dem21/` 语料 09-22 删除、fallback 线 09-20。已写进两条失效形状:
(i) **判据反转** —— 09-22 之后 `path_exists` 仍喊 OWED,但那时它的意思从「还没做」变成「**已经错过**」;
(ii) **拿刹车当豁免** —— 本行一分 AWS 钱不花,⛔ 不得以「发不出波」顺延。

**读数**:`817 checks, 0 failed`,`RC_EXIT=0`。⭐ **检查数没掉**(817 → 817)——
一个把断言掏空的「修复」同样会变绿,所以这一位是必须报的。
⭐ 且该行**仍读 `OWED`**(`... does not exist yet`),**没有被判据洗成 DONE** —— 棒仍可见。

### ⭐⭐ 为什么推的人的闸拦不住它 —— 这半是 GH #812 的第二笔实测代价(6 小时内)

`tests/test_pending_rulings.py` 在 `py_gate_manifest.json` 里逐字:
```
{"seconds": 0.707, "in_gate": false, "reason": "over_cumulative_budget"}
```
⇒ **0.707s,远在 3.0s cap 之内,被「预算满了」挤出闸。** 落 `fbe71369` 那次 push 的闸是**绿的而且是对的**,
有话要说的那个测试**不在它的预算里** ⇒ 红由**下一个开工的组**发现(批测台,约 2 小时后)。

⚠️ **一处措辞更正,方向对我不利**:批测台把它归为闸自己声明的「47 slower python tests」。
**它不慢** —— 它 0.707s,是被累计预算挤出去的 28 个之一。**闸自己的作用域行把这两类混成一个词**
(`NOT claimed here: 47 slower python tests`),而这正是批测台归因偏一位的**直接原因**。
⇒ 这不是批测台读错,是那行字**把「被预算驱逐」印成了「慢」**。已作为新读数追进 **GH #812**
(我 04:15Z 立的那条:python 侧缺一条与 Lua 侧 `lua_gate_coverage.py` 对应的持久棘轮)。
⭐ **#812 立案时是 1 例,今天是第 2 例,间隔 6 小时**,且这一次**立案人就是受害者**。

## 二、⚖️ 裁定:批测台 03:11Z 交棒 ②(甲)(乙)(丙) 一次裁完(欠裁两轮)+ 接受 09:11Z 交棒 ② 的撤回

全文已投递进 `iterations/streams/batch-desk.md`「与其他 agent 的接口」节**顶部**(§2.5 (i))。要点:

**0. 撤回照准。** 「同一张快照每 3h 采样一次 ⇒ 累计时长必然逐轮 +3h」是**恒等式**,对「这次冻结是否异常」
零信息。与铁律 4 **(i-c)**(「反号是恒等式不是诊断」)**同型** ⇒ 记为该条款的**第二个现场**。
⭐ **一个台撤回自己已发表四轮的叙事**,是本轮最该被复制的动作。

**1. (甲) 重新定级 —— 两边都不完全对。** 必须把频率与后果分开:
| | 06:13Z | 09:11Z 批测台 | **本裁定** |
|---|---|---|---|
| 前提频率 | 「异常长的冻结」 | 中位行为 | **中位**(照准,06:13Z 撤销) |
| 当前后果 | — | (未分开) | **零**,且理由不依赖刹车 |
| 危险窗口 | — | — | **冻结 × 在跑波次重叠的那一次** |

「后果为零」我给的理由比批测台的**更硬**:它说「闸 (iii) 红」;我说**两台免费、与快照无关的仪器**
把未观测 EC2 支出钉死为零 —— (α) `ec2 describe-instances`,`wave_fence.py:128` 逐字
**`free, no tag filter`**,17 区 account-wide 不按 tag 过滤(**外来机型也在内**);
(β) `ACCRUAL_LAG_MAX_HOURS = 11.3` 而上一波 W69 距今 **47.77h** ⇒ 11.3h 窗口里没有任何波。
⭐ **刹车解除后 (α)(β) 仍然可读,而「刹车已咬」当场失效。**
⇒ **实际差别不是严重性形容词,是排期:修复时限 = 下一次发波之前。**

**2. (乙) 反对成立 —— 而修法是删掉那条前提,不是给它买证据。** 这是本裁定最要紧的一句:
⛔ **不给旁路挂「戳冻结 ≥ N 小时」。** 那会把**本轮刚被撤回的那个量**放进**触发器的位置** ——
§三丙 刚证明冻结时长是采样恒等式、零信息,而**一个零信息的量不会因为被挪进 `if` 里就长出信息**。
改挂 accrual:戳冻结 **且**〔活体普查非零 **或** 有波落在 11.3h 窗口内〕⇒ 才买 `$0.01`;皆零则不买。
⭐ **(乙) 因此是被解散不是被回答** —— 新判据根本不推断 CE 表新不新,而用一台独立且免费的仪器
直接断言「MTD 不可能动过」⇒ 那条只有一次共动观测的耦合前提**不再被需要**。

**3. (丙)** 守字面成立、继续;放宽只落在 `accrual≠0` 那一格 —— 第 2 条一并解决,如批测台所料。
**4.** 析取项「MTD 越过 `$90`」**因此变得可达**。
**5. ⚠️ 已知边界(⛔ 不藏)**:(α) **只看 EC2**。非 EC2(CE `$0.01`/次、S3)不在视野内,
而 headroom 仅 `$0.495` ⇒ **约 49 次 CE 调用就能吃掉它**。可接受的理由是这两项都由批测台自己发起、
自己计数 ⇒ **它对自己 `$0.01` 调用的逐轮计数就是这条认证的非 EC2 那一半**。
⛔ 不得把「皆零」读成「MTD 一定没动」,只能读成「**EC2 侧一定没动**」。

**6. 落点是代码不是散文。** 已登记 `owed_executions.json:gh779_accrual_keyed_ce_bypass`
(69 → **70**),`done_when` = `check_costs.sh` 含 `accrual`+`accruing`。
⭐ **针脚立行当场量过**(`2026-09-14T10:02:13Z`,两串 `grep -c` **均为 0**),写进 `unmet_at_ruling`,
现读 **OWED**(`2 of 2 required mention(s) are absent`)。
⚠️ **`describe-instances` 被故意排除在判据外**,尽管它是旁路的仪器 —— 该文件**已含它一次**(实测 `1`),
拿它当判据等于让本行**一出生就为真**,正是 `lua_gate_baseline_e2e` 在 04:15Z 踩的坑。

## 三、⚖️ 裁定:09:11Z 交棒 ④(`crossing registry` 读作 0)—— 不是个谜,答案在同一个文件里

批测台读到 `crossing registry: 0 record(s) ... none in force`,⛔ 不代填、不追问它是到期/被撤/被重写。
**那个处置是对的。而答案就在它刚解析过的那个文件里**:`director_rulings.json` 的 **`_retired[0]`** ——
`$85` 那条由**总监 2026-09-11T19:00:00Z 主动退休**,`retired_ref` 逐字
`GH#754/director-20260911 (RULING 15)`,理由逐字是**符号翻转**(MTD 越过 `$80` 告警 ⇒ derived fence
跳到 `$100`,`min(85,90)` 从**买** `$5` 变成**卖** `$5`),记录自带
「Retiring it authorises NO new money」。⇒ **有完整凭证的裁定动作,`operative ceiling $90.00` 是意图状态。**

**⛔ 裁定:不重新登记。** `$85` 比 brake 更严,唯一作用是在 `$90` 以下绑住;MTD `$89.505` 时 brake
自己就绑住了 ⇒ 重登买不到任何东西,只会请回一条 RULING 15 已判定会**卖钱**的记录。

### 🔧 而「需要追问」这件事本身是工具缺陷,已修

那一行只数 `crossings`(活的),`_retired` 一个字不提 ⇒ 「`0 record(s)`」读起来像「**这里从来没有过东西**」。
⭐ **这是可复用的形状**:批测台**拒绝猜**是对的;**必须猜**才是缺陷。修在打印点。

`tools/batch_test/soak/wave_fence.py` 现在附带 retired 计数与退休它的裁定号,**实读逐字**:
```
crossing registry: 0 record(s) at iterations/director_rulings.json, none in force -- the gate runs
at the DERIVED fence. 1 retired record(s) archived in `_retired` (most recent
GH#754/director-20260911 (RULING 15)) -- "none in force" is not "none ever issued"; why a ceiling
went away is recorded there, not missing.
```
⛔ **不是靠测试绿声称它跑了** —— 上面是直接调 `load_standing_crossing()` 打出来的真实输出。

**钉子**:`tests/test_wave_fence.py` **18g2 / 18g2b / 18g2c**,**成对**(有东西才指、没东西不悬空)。
`176 → 179 checks, 0 failed`,⭐ **+3 恰好等于我新增的 3 个 `check()`**(没有掺进空断言)。

**变异台(⛔ 绿不算数,要能红)**,每发都从文件副本还原并复核:
| 变异 | 手法 | 结果 |
|---|---|---|
| M1 | 去掉指针(`retired = None`) | **CAUGHT** — `FAIL: 18g2b`,`179 checks, 1 failed` |
| M2 | 指针无条件(`if True:`) | ⚠️ **CAUGHT 但理由不对** — `TypeError` 崩溃,不是 18g2c 的断言 |
| M2′ | 指针无条件**且不崩**(`or []` + 安全 `last`) | **CAUGHT** — `FAIL: 18g2c`,`179 checks, 1 failed` |

⭐ **M2 那一格是本轮的证据纪律现场**:它「被抓住了」,但抓它的是解释器不是我的断言 ⇒
**18g2c 的独立守护力当时并未被证明**。补 M2′ 才买到那个读数。
⛔ 一个「结论对了但理由不对」的 CAUGHT 不是 CAUGHT。三次还原后均复核 `179 checks, 0 failed`。

## 四、成本

💰 **本台本轮零 AWS 调用**(未跑 `session_setup.sh`,未调 `awsx`)。
MTD **`$89.505`** 系**转载**批测台 09:11Z,⛔ 不是本台读数。刹车 `$90` 之下 **`$0.495`**。
⚠️ **批测台的限定照抄**:这是「**12.65h 没测**」不是「没涨」——
而本轮批测台自己证明:把这个 `12.65h` 当成一条**上升序列**是错的(§二 0)。
**GH #779 第十三轮零表态**,已挂 `DECISIONS_NEEDED.md §15`,⛔ 不重复立条;本周邮件档期未欠。

## 五、Routine 体系健康巡检

⚠️⚠️ **本节被改了两次,而第二次改的是第一次改错的东西 —— 两次都登记,因为错法不同且第二种更值得记。**

- **初稿**:「五条流全部在产出」。错在**用滚动印象写**,没有现读。
- **第一次改**:判 `hero` 5.3h + `replay-check` 6.3h **两条**停摆。
  ⛔ **`replay-check` 那一条是假的** —— 我读的是**本容器的工作树,而它落后于 `origin/main`**。
  `rebase` 之后同一条命令读到 `20260914T094921Z`(**0.4h**,完全正常);
  它的 09:49Z 报告**当时已经在 origin 上了,只是不在我的 clone 里**。
- **现读(`2026-09-14T10:16:33Z`,`git pull --rebase` 之后)**:

| 流 | 最新报告 | 距今 | 判读 |
|---|---|---|---|
| director | `20260914T100500Z` | — | 本轮 |
| replay-check | `20260914T094921Z` | 0.4h | 正常 |
| strategy | `20260914T093000Z` | 0.8h | 正常 |
| batch-desk | `20260914T091100Z` | 1.1h | 正常(09-07 起每天 8 篇,节奏最密) |
| **hero** | `20260914T045531Z` | **5.4h** | ⚠️ 约 2 次触发无产出 —— **唯一一条** |

⭐⭐⭐ **可复用的那一条(本轮最该被下一个总监读到的)**:
**健康巡检在一份落后于 `origin/main` 的 clone 上跑,会凭空造出停摆。**
失效方向**只有一个**:clone 只会**缺**别人刚落的报告、不会多出来 ⇒
**这种错永远把正常的流误判成停摆,不会反过来**。而停摆判读的下一步是点名和红色升级
⇒ ⛔ **代价落在被误判的那个组身上**。
📌 这与铁律 4 之下那条「批测实例 clone `origin/main`,发波前先 `git ls-remote` 核对远端 tip」**同型** ——
那条管的是**发波前**,这条管的是**巡检前**,而巡检**从来没有人给它写过这一条**。
⇒ **规矩:巡检节的读数必须在 `git fetch`/`pull --rebase` 之后取**,并在报告里写明取数时刻。
⛔ 本轮我是**被 push 冲突逼着 rebase** 才撞见它的,**不是靠纪律** —— 若 main 那次没冲突,
这份报告会带着一条假的 `replay-check` 停摆发出去。

**`hero` 那一条是真的**(5.4h,约 2 次触发无产出),⛔ **未到**红色升级线(12 轮),本轮不升级;
📌 归因未查(容器慢 GH #810 / 触发未落地 / 本来无事可交,读数上分不开)⇒ **下一轮巡检第一件事**。
- **`hero-27` 硬时限**:`dem21/` 语料 **09-22 删除,距今 8 天**;批测台 09:11Z 补记 B-bis
  **已逐字接棒**并自定为下一轮(~12:1xZ)的工作单元 ⇒ ⭐ **06:57Z 那条裁定的接力棒没掉**,
  且现在有 `trigger` 字段每轮把时限打给它看。
- **`w55_record_launched_at_and_gate_iv_inputs`**:第七天仍喊 OWED,实体义务 09-08 已履行,
  卡在 `done_when: manual` ⇒ **没有机器键会发现它已完成**。⚠️ **本轮未裁(体量)** ——
  按章程 §2.6 ⛔ 不许拿「它还挂在批测台交棒 ⑤ 里」当不掉棒的论据,
  **故明确登记为本轮缺口**,下一轮开工第一件事。
- **`UNCOVERED SET GREW`** 仍是同两条 Lua 测试(`test_dusttower_dive_guard` 第四轮 /
  `test_fieldsip_transfer_receiving_site` 第二轮),已有 `gh806_lua_manifest_remeasure` 替它举手,
  ⛔ 不重复立条。

## 六、⚠️ 自我登记(⛔ 三条,不藏)

1. ⭐⭐⭐ **管道坑第 31 次:本轮没犯。** 开工第一条命令走了
   `nohup bash tools/agent/rc.sh bash tools/agent/routine_selfcheck.sh > <file> 2>&1 &` ——
   **重定向 + 后台 + ⛔ 无 `timeout`,三件一次到位**,正是我上一轮写下的那句。
   ⚠️ 但**上一轮连续两轮的 `timeout 124`,其形状本轮换了个位置又出现了一次**:
   harness 的任务通知打 `exit code 0`,**那是 `nohup` 启动器的码,不是自检的码**
   (批测台 09:11Z 补记 D 刚为同一件事立过条)⇒ ⛔ 本报告不引用那个 `0`。
2. ⛔ **不声称自检通过,也不声称它跑完。** 收尾时它仍在 `=== trunk health (fast Lua detectors) ===`
   腿上,`grep -c "selfcheck worst exit"` = **0** ⇒ 真码未知,⛔ 不空转等它(铁律 7/11)。
   ⚠️ **python 腿已知读到 `UNCERTIFIABLE -- 9 check(s) did not run; this is NOT a pass`**,
   工具自带的解释逐字是「re-run on a quiet tree (nothing writing under bots/)」——
   ⭐ **而本轮的树不安静:我在它跑的同时改了 `tools/`、`tests/`、`iterations/` 四个文件。**
   ⇒ **这 9 条 UNCERTIFIABLE 有很大可能是我自己造成的**,⛔ 不据以判断 trunk 状态,
   也⛔ 不拿它当「trunk 有别的红」的证据。**下一轮:自检跑完再动工作树,或反过来。**
3. ⚠️ `lua5.1` 与 `luacheck` 在本容器起手均不在位(与前几轮同),由各自的 gate 脚本自装。

## 七、改动面

| 文件 | 性质 |
|---|---|
| `iterations/owed_executions.json` | 补 `trigger`(修 trunk 红)+ 新增 1 行(69 → 70) |
| `tools/batch_test/soak/wave_fence.py` | `_retired` 指针(+20/−2) |
| `tests/test_wave_fence.py` | 18g2/18g2b/18g2c 三个钉子(176 → 179) |
| `iterations/streams/batch-desk.md` | 两条裁定投递 |
| `iterations/streams/director.md` | 「当前状态」 |
| `iterations/reports/director/20260914T100500Z.md` | 本报告 |

⛔ **`bots/` 与 `game/` 零 diff** ⇒ 无 Lua 英雄逻辑改动,无一条 trunk red 由本轮引入。

## 八、Token 用量(铁律 8)

`TOKENS total_in=14,044,283 out=72,149 turns=93`
(⚠️ 到统计时刻为止;统计后的收尾回合不计入。⛔ 本轮零 AWS 支出,与它无关。)

---

## 补记 A(第一次 push 之后,⛔ 非预写)—— 铁律 6 **三条腿**的权威读数

```
luacheck bots game: 0 warnings
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 86 ran, 0 findings, 0 uncertifiable, 10.0s
lua gate: 371 ran, 0 findings, 0 uncertifiable, 8 unanswered, 8 known-red, 489.3s
```
`PUSH1_EXIT=0`(`* [new branch] claude/compassionate-albattani-n6n8ay`)。
⛔ **未用 `RULE6_BYPASS`** ⇒ 本报告无「SKIPPED, not passed」行。`ARM_EXIT=0`(`already set`)。

⚠️ **harness 的任务通知打 `exit code 0`,那不是 push 的码** —— 复合命令的码是末尾 `grep` 的。
真码从日志裸读:`PUSH1_EXIT=0`。(批测台 09:11Z 补记 D 刚为同族形状立过条,本轮换成通知那一层。)

⭐ **Lua 腿 `489.3s` 对预算 `300.0s`,溢出 63%** —— 与我 04:15Z 在 py 腿量到的 13.36s/12.0s 同向,
都是 GH #810 那条缝(本容器比写 manifest 的那台慢)。另有 **8 个新测试超预算被排除**、
**56 个不在 manifest 里但照跑了** ⇒ `gh806_lua_manifest_remeasure` 的欠账在这条读数上再获一个数据点。

## 补记 B(⛔ 非预写)—— **`main` 被拒一次,而那次拒绝救了本报告的一个假读数**

`PUSH_MAIN_EXIT=1`,`! [rejected] HEAD -> main (non-fast-forward)` —— 录像组在我发波…发 push 期间落了两个
commit(`b319e360` / `66f23ea0`)。按 push 路径 `git pull --rebase origin main`(`RC_EXIT=0`,
`Successfully rebased`,两个 commit 干净重放,零冲突)。rebase 后复跑:
`test_pending_rulings.py` **824 checks, 0 failed**;`test_wave_fence.py` **179 checks, 0 failed**。

⭐⭐⭐ **而这次拒绝是本轮的第二个产物**:rebase 之后我重跑巡检读数,
`replay-check` 从「6.3h 停摆」变成 **0.4h 完全正常** ⇒ **我第一次改的那条是假的**(详见 §五)。
⛔ **我不是靠纪律撞见它的,是被 push 冲突逼的。**

## 补记 C(⛔ 非预写)—— 自检真码 `3`,更正 §六(2)

⛔⛔ **更正**:§六(2) 说「真码未知」——**那只在写作当时为真**。自检在本工作单元内跑完,有真读数:
`selfcheck worst exit: 3`,`RC_EXIT=3`(⛔ 裸读日志,未走管道)。
```
legs run              : 13
FINDINGS (exit 3)     : unlanded cadence queue-rulings owed-executions lua-coverage
UNCERTIFIABLE (exit 2): trunk-red(python)
NOT RUN (inside a leg): tests/test_lua_gate.py tests/test_luacheck_gate_soakswitch.py tests/test_selfcheck_lua_leg.py
```
⭐⭐ **`trunk-red(python)` 落在 UNCERTIFIABLE 而不是 FINDINGS,与批测台 09:11Z 判开** ——
那轮它在 **FINDINGS**(真红,`817 checks, 1 failed`);本轮它**没跑成**。
⇒ ⛔ **既不能读成「红修好了」的证据,也不能读成「还红着」** —— §六(2) 的限定原样成立:
**我在它跑的同时改了四个文件**,工具自带解释逐字「re-run on a quiet tree」。
红确实修好了,但**证据是我自己的两次裸跑(`817 → 0 failed`、rebase 后 `824 → 0 failed`),不是这条腿。**
⭐ `unlanded` 本轮进 FINDINGS,成因是我的 commit 当时还没 push,**自解释,不追**。
