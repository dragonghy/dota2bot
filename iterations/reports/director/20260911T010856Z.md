# 总监 2026-09-11T01:08Z —— RULING 12:一条顺延了**五轮**的裁定,而拦住它的是**一句 Lua 断言消息**

**工作单元**:裁 `queue.json:hero-56`(入集裁定),并一并清掉三条无 `director` 字段的存量行
(`hero-52` / `hero-53` / `hero-55`)。
取活依据:上一轮「下次触发 ②」逐字点名 `hero-56`(**第五轮顺延,下轮先做**)+ **章程 2c**。

零 AWS 支出(**一次 AWS 调用都没有**)、零波次、**`bots/`+`game/` 零 diff**、
**不发 owner 邮件**、armed 串 **37 不动**、**无 promote / 无退集**、`DECISIONS_NEEDED` +0。
全文档案:`iterations/streams/test_set.md §GQ`(§GQ.0–§GQ.7)。

---

## 一、⭐⭐⭐ 主轴:五轮顺延不是拖延,是**每个读到它的人都正确地拒绝了独自动手**

`hero-56` 自陈有两条「不是英雄组一家能改的」行。第一条的依据**逐字**来自
`tests/test_salvetarget_axis_undecidable.lua` 的 `[ties]` 断言消息:

> `the archive now carries a NON-TIED disagreement between the two axes (n) --`
> **`the ruling in GH #242 rests on there being none, re-read it`**

**这句话是错的,而且是可判定地错。** GH #242 的裁定建立在同一个文件的
`[dominance]`(:394 / :428)与 `[criterion]`(:478)上,那两条是**算术**:

> dominance 需要一个候选在**两个轴上同时**严格劣于在位者;而在位者**就是**绝对值轴的 argmin
> ⇒ 没有任何候选能在该轴上严格更低 ⇒ 交集对**每一个存在的或可能存在的**候选集为空。

**非平局分歧** = `argmin_abs ≠ argmin_ratio` **且比值轴有严格赢家**。代进去:
在位者仍是绝对值轴的 argmin,交集仍然为空。**⇒ 非平局分歧结构上够不到那条裁定。**
它够得到的是本行自己的**次级判据**(「原始分歧计数在把平局分出去之前不是分歧计数」),
而那条判据引的例子——「归档里唯一的那一条是平局」——是**本行语料的性质**,语料一长就过期。

⭐ **缺陷的形状是「正确的结论骑在一条已经死掉的理由上」(纪律 4),而且比前几轮高一层。**
§GF.4 里死掉的理由住在**源码注释**;这一次住在**断言的失败消息**里——
一根**没有任何东西会去类型检查的字符串**,而它偏偏**点了一条已发表裁定的名**。
⇒ 每个读到它的人的反应都是**正确**的(「已发表的裁定不是我一家能重开的」),**五轮**都是。

⛔ **不是投递掉棒**:测量在 `hero-54.result` 里、问题在 `hero-56` 里、两条行在测试自己的消息里,
三处都在。**被裁方读的是第四份东西,而那份是错的。** §GP 那条缺陷的**第三次现场**。

**本轮修的是理由,不是断言**:头部加 DIRECTOR CORRECTION 段,`[ties]` 消息改成
**`RE-TAKE THIS ROW, NOT THE VERDICT`** 并把算术写进消息本身;`hero.md` 抄过去的那句就地划掉改正。
⛔ **`nRealDisagree == 0` 一个字没放宽** —— 它该举手,只是该喊对话。
读数:`lua5.1 tests/run_tests.lua test_salvetarget_axis_undecidable.lua` → **17 tests, 0 failures**。

## 二、(a) 入集:**问题自己的措辞被它自己的测量推翻了**

`hero-56` 问「**先入哪一局**」。它同一段给出的实测是:12 帧全入 ⇒ 19 个普查文件里 **17** 个红;
**只放 6 个 WK 帧也是同样 17** ⇒ 7eb1ba 压在 5313f5 之上的**边际价钱 ≈ 0**。
扣着它买不到任何东西,却要丢掉那六帧顺带载着的 CM(2 nova)与 Lion(1 mana drain)活行。

**⇒ 裁定:`5313f5` + `7eb1ba` 作为一个原子一起入**,重推导按 **WK 半边先做**
(那是载着收益的半边:语料里 WK **0/36**,这六帧 **3/6**)。

⚠️ **但那个 ≈0 是对一棵已经不存在的树的读数。** 它测于 2026-09-10,之后落了
8 个 zeus 暂存帧(`364764`)、4 个 lion 暂存帧(`20260910_124853`),
以及 09-10T23:06Z 那轮跨 4 个普查文件的 aether 环修复。暂存帧在 `tests/fixtures/*` 的
glob 之外、自己不动普查,**但普查文件本身动了** ⇒ **挪之前按同一配方重测一次**;
若不再 ≈0,**只入 5313f5** 并回填读数。
⛔ `364764` 与 `20260910_124853` **不在本裁定范围内**(入集价钱**都未测**)。

## 三、(b) 两条「裁定级」行:**都不是重裁,都归英雄组**

| 行 | 裁定与理由 |
|---|---|
| `test_salvetarget_axis_undecidable` | **不是重裁**(见 §一)。过期的是次级判据的**例子** ⇒ 按移动它的那些帧改写例子并点名是哪几帧,**裁定不动** |
| `test_blind_a_pulllane_pullthink` | **不是重裁**。§GF 的退集判据是**仪器**不是计数(`GetLaneFrontLocation` 在**全部**候选帧上拒答;`IsBotThinkingMeaningfulAction` 在 **1100/1100** 句柄上 false)。那个 9 是**报给未来买单人的价**,9→12 把采购**变大**不是把裁定**变弱** ⇒ 重推导数字、更新 §GF 引用,**退集不动** |

⛔ **「断言恢复绿色」永远不是验收** —— 把 `nRealDisagree == 0` 或那个 `9` 改宽就能绿,
而那恰好是这两条禁止的事。已写进两条 owed 行的 `done_when_note`。

## 四、(c) 第三行:**谁也没提,而登记册的全部意义就是让它不必被想起来**

`tests/frames/README.md` 自陈存在是为了让入集「**是有人特意做的决定而不是副作用**」。
实测:四个 `f_260910_124853_lion_spike_*` 在里面**没有行** —— 暂存四局里唯一没有行的一组
(WK / Axe / Zeus 三组都有,zeus 那行还老老实实写着「入集价钱**未测**」)。
⇒ 英雄组在同一工作单元里补上(暂存价钱 / 入集价钱「已测 or **明确未测**」/ 谁按名字读它们)。

⛔ **不是对暂存那一轮的指责**:登记册背后**没有任何断言**,缺一行**不会举手** ——
而 `hero-56` 问「先入哪**一**局」时,暂存区已经是**四局**了。与 GH #624、§DR **同形**。

## 四点五、⭐⭐ 顺手清空了一整条自检腿,**而清的过程里抓到 §AW.1 的反方向第一例**

开工自检的 `un-ruled queue requests (director field)` 腿开工时读:
**`RIDESHARE 1`(hero-55)+ `OTHER 5`(hero-51/52/53/54/56)**。
本轮裁完四条之后还剩两条,**而那两条剩下的理由各不相同,都值得写下来**:

- **`hero-54`:`director` 是空串 `""`。** 它空着的原因是「**已经做完了**」——
  帧供给请求由英雄组自己执行(2026-09-10T07:55Z),没有任何东西需要总监裁。
  ⛔ **但空字段和没裁过一模一样。** 已补 `NOTED-EXECUTED` 一行。
- **`hero-51`:裁定写进了 `result`,而 `result` 是批测台填的字段。**
  2026-09-09 的那一轮确实裁了(APPROVED-SCAN,理由还逐条核过源码),
  但它落在了**裁定轴之外的字段**上。
  ⭐⭐ **这是 §AW.1 的反方向第一例,而反方向更难被看见**:
  §AW.1 与 §DR 的形状是「**裁了,而下游按未裁走**」;这一次是
  「**裁了,而审计按未裁读**」—— 自检从那天起每轮都把 hero-51 报成「裁定仍欠」,
  于是**一条已经完成的裁定,长期长得像一根掉了的棒**。
  失效方向看似温和(不会让谁做错事),**代价是每轮都有一条假的欠债占着注意力**,
  而真欠债和假欠债在同一份清单里读起来一模一样。
  ⇒ 裁定**镜像**进 `director`,**`result` 原文一字未动**(它是证据,不是错误)。

**清空后的实测读数**(`bash tools/agent/rc.sh python3 tools/agent/pending_rulings.py`,
`RC_EXIT=0`):`RIDESHARE: none` / `OTHER (routing/slot ruling still owed): none`。

⭐ **顺带一条收束**:自检的 Lua 腿在 `test_wk_q_castrange_meter_domain.lua:509` 打出
「**That is the frame GH #390 asked for**」,并**点名了三个帧** ——
`f_260909_215040_wk_blast_lion_480` / `_sb_1052` / `_sb_661`,**全部来自 `5313f5`**,
也就是本轮 (a) 裁定要入的那一局。
⇒ **上一轮「下次触发 ①⭐⭐⭐」那件「已到手却第三轮顺延」的交付物,和本轮的入集裁定是同一批帧。**
入集那个工作单元落地时,GH #390 的写作材料**一并到位**。

## 五、⚠️ 一条本轮**只登记、不动手**的新发现:**闸 (i) 的锚点在 trunk 上读不出来,而下一轮批测台会撞上它**

开工自检的 python 腿打出来的原话:

```
FAIL: the REAL waves dir is UNCERTIFIABLE -- gate (i) cannot read its own anchor on trunk:
UNCERTIFIABLE: W64 machine 0 (seed 10890) has no launched_at
gate (i) DID NOT RUN. That is not a pass -- do not launch on it.
WAVE_THROTTLE: UNCERTIFIABLE (exit 2)
```

**逐条核过,不是转述**:`W64_wave.json` 四台机器**全部**带 `launch_time`
(`2026-09-11T00:20:55Z` 等)而**不带 `launched_at`**;`W63_wave.json` **两个键都带**。
`wave_throttle.py:160` 读的是 `launched_at` ⇒ 抛 `Uncertifiable`。
⭐ **而 `launched_at` 在 `tools/` 里没有任何写入者**(`grep -rn launched_at tools/` 只命中
`wave_fence.py` 的注释与 `wave_throttle.py` 的读取)—— **它是一个靠手写维护的字段**,
**写的那一刻没有任何东西要求它**。

⭐⭐ **这不是一条抽象风险,它已经是一对互相矛盾的活指令**:批测台 00:15Z 报告 `②` 逐字写着
「闸 (i) 锚点 = W64 末台 `2026-09-11T00:21:17Z` ⇒ **解锁 `06:21:17Z`**」——
那个时刻是**手算**出来的,而闸自己**读不到这个锚点**。
⇒ 下一轮批测台跑闸 (i) 会拿到 `exit 2`,按它自己的规矩「**不许在这上面发波**」,
而它自己的报告在叫它 06:21:17Z 发。**RULING 11 的形状第四次:两句话不能同时为真。**

⛔ **本轮不动手,这是有意的保守默认**:两条修法各有代价 ——
(A) 把 `launch_time` 补进 W64 的记录 = **替别的台子重构它自己的发波证据**;
(B) 让闸接受 `launch_time` 作后备 = **动一条钱路径上的安全闸**,而我没有批测台那一侧的上下文。
**一个会拒答的闸是安全的;一个被我放宽的闸不是**(RULING 10 的 M8 就是这条)。
⇒ 交批测台:**GH #736 已发表**(push 之后才发,GH #290 顺序;
`claim_precheck.sh` `PRECHECK_EXIT=0` / `local commits not on origin/main: 0` / `OK to publish`),
owed 行 `gh736_throttle_anchor_unreadable`(**43 → 44**)。
立案里带**第三个问题 (C)「谁让它下次自己举手」** —— 闸 (i) 本来每轮都跑,
**只是跑在读的那一侧,不在写的那一侧**。
⚠️ 本行的**执行方与触发时机这次都特定**(批测台每轮都跑闸 (i))⇒
**连着两轮没有读数 = 真的有问题**;⛔ **不许拿总监自己的运行结清**(RULING 11 立案的那个缺陷)。
**下一轮总监核对它有没有被接住。**
⚠️ 另有 `test_py_gate.py`(**GH #728**,`5c: selected total 12.03s > budget 12.0s`)trunk 仍红,
上一轮已登记、不挡 push;本轮**未修,也不声称修了**。

## 六、⚠️⚠️ 本轮自己举的手:**判定完结 0,而 owner P4.2 说总监的产出指标就是它**

P4.2 逐字:「总监每轮至少完成 **2 个**判定完结(promote / reject / 退回出集)」,
「总监的产出指标即日起 = **判定完结数**,不再是裁定节数」。

**本轮 0。而且连续第四轮 0**:RULING 9(09-10T12:50Z)、RULING 10(19:08Z)、
RULING 11(22:06Z)、本轮。armed 串自 §GF(09-09T04:xxZ)起 **37 一动没动**,
距解冻线 **20** 还差 **17** 条。
⛔ **不拿「本轮清了四条裁定」抵账** —— 那正是 P4.2 逐字禁止的换算。

**成因照实登记,不辩护**:后三轮取的都是 `[harness]`(闸自己的缺陷),三条各自都真、
且都在**钱**的路径上;本轮取的是一条顺延五轮的掉棒。
**每一轮的取活单独看都站得住,合起来是四轮没有动 owner 的那个指标。**

⇒ **下一轮总监的工作单元主体必须是判定完结**,从 `verify_coverage.py` 的 `verify=0` 七条取:
`abilanc`(narrat **3**,最薄)> `blinkflee` / `fieldbuy` / `overchase` / `tpcommit`(各 4)
> `aimguard` / `lf_rescue`(各 5),按 §GC/§GD/§GE/§GF 同一把尺问 (a) 买不买得到。
本节即该计数的**起点**;铁律 9 的 12 轮线尚未到,满 12 轮写进 `DECISIONS_NEEDED.md`。

## 七、投递(章程 2.5)与验证读数

**投递**:(i) `queue.json` 四行 `director` 字段全部写入,**并把 `status` 一起改成下游读得懂的值**
(`hero-52`/`hero-53` → `frozen_hold`,`hero-55` → `ruled_approved_scan`,`hero-56` → `ruled`)——
⛔ 只写 `director` 不改 `status` 就是 §AW.1 那次「裁定写了而下游按未裁走」的形状;
(ii) 档案 `test_set.md §GQ`;(iii) **对方那张真正被驱动的表**:`hero.md` 那一节**就地改正**;
(iv) 常驻义务 `owed_executions.json` **40 → 43**(三行,全部带裸读判据,
且**三行都显式写明「断言变绿不是验收」**)。⚠️ 三行执行方特定(英雄组)、**触发时机不特定**
⇒ **提醒不是证明**,行内已写。

**铁律 6 三条腿**(三次 push 钩子读数一致):`GATE_EXIT=0 CLEAN`(`luacheck bots game: 0 warnings`)/
`py gate: 95 ran, 0 findings, 0 uncertifiable, 28.5s` /
`lua gate: 329 ran, 0 findings, 0 uncertifiable, **15 known-red**, 282.0s`,**无 `RULE6_BYPASS`**。
⭐ `known-red` **15**,与上一轮持平 —— **本轮没让它变小,如实登记**。
⛔ **Lua 全量未跑不声称**(`bots/`+`game/` 一行未改;本轮只跑了改动的那一个文件:17/0)。

**开工自检**:第一条命令**又带管道**,守卫第十六次自拒(`SELFCHECK_EXIT=2 REFUSED`,纪律 3 再一发,
**而章程 §0 逐字覆盖它** —— `rc.sh` 是习惯不是门,第十一次);改走 `rc.sh` 后真码
**`SELFCHECK_EXIT=3`(有发现)**,`legs run 11`。
工具自己打的来源行(GH #267 4b,非手工归因):
`FINDINGS (exit 3) : cadence queue-rulings owed-executions a-evidence-owed trunk-red(python) trunk-red(lua)`;
`UNCERTIFIABLE (exit 2): none`。
⛔ `NOT RUN (inside a leg)` 三条(`test_lua_gate.py` / `test_luacheck_gate_soakswitch.py` /
`test_selfcheck_lua_leg.py`)**不声称**。

- `queue-rulings` —— **本轮清空**(见 §四点五)。
- `cadence` 两条 GAP 都是**总监自己的**(09-10T01:15→05:30Z 4.2h、12:50→16:22Z 3.5h),登记。
- `trunk-red(python)` —— **两条**:`test_py_gate.py`(**GH #728**,已知,不挡 push)
  与 **`wave_throttle`**(§五,本轮新查清成因)。
- `trunk-red(lua)` —— **4 / 87**:`focus_mana_cost_consumer_census` / `lion_ult_reserve_domain` /
  `stayfield2_marginal_domain` / `wk_q_castrange_meter_domain`。
  ⭐ **本轮把上一轮「未分辨」那一条分辨了**:四条的失败正文**没有一条说机器人做错了决定**,
  逐条都是「钉住的数过期了,去重推导」(`n=26→27`、`rank-2 instants 2→7`、
  `negative-natural-bid 56 移动`、`live-WK 38→51`)⇒ **族属 GH #718 / #650,不是行为缺陷**,
  分派**不变**(hero 13 / strategy 3)。⛔ **本轮没修,也不声称修了**;
  ⚠️ 其中 `lion_ult_reserve_domain:512` 与 `wk_q_castrange_meter_domain:509` 两条
  **点名的就是本轮裁定要入的那批帧** ⇒ 它们会在**入集那个工作单元里**被一起付掉,
  这正是 §二「价钱是每新入一局」的另一面。
- `owed-executions` / `a-evidence-owed` —— 登记,本轮未取活。
- 跑绿的腿:`stable anchors OK` / `promote-atom constraints OK` /
  `orphan admission proposals: none`。

**🩺 巡检**:五组在过去 24h 全部有产出(batch-desk 00:15Z / hero 23:06Z / strategy 22:39Z /
replay-check 有域报告产出),**无掉队组**。
⚠️ **strategy P1 与语料**:**第六轮顺延**(本轮取了 hero-56,那条已顺延五轮)——
下一轮若仍不裁,按铁律 9 计数。
⚠️ **owner P4.1 标尺波**:批测台 W64 发的是普通 arm 波,标尺波**仍未认领**;
P4.1 原文写着「再空一轮即按铁律 9 写进 DECISIONS_NEEDED 向 owner 说明」——
**下一轮总监核对,若仍未发即照此执行**。

**💰 成本**:本轮 **AWS 调用 0 次**,**不作 MTD 新声称**(⚠️ 它是时间的函数;
抄 09-10T21:18Z 的 `$78.253` 正是围栏「不许抄」的那种数)。

---

## 下次触发

1. ⭐⭐⭐ **判定完结,主体,不许再让位**(§六):`abilanc` 起,目标 ≥2。
2. ⭐⭐⭐ **闸 (i) 锚点**(§五):批测台接住了没有?下一轮若仍 `UNCERTIFIABLE` 而波已发,
   那是钱路径上一道**跑都没跑**的闸,总监直接接手修(并在报告里写明选了 (A) 还是 (B) 及代价)。
3. ⭐⭐ 裁 strategy P1 与语料(**第六轮顺延**)+ owner P4.1 标尺波的铁律 9 计数。
4. ⭐⭐ RULING 11 的首次活读数:`grep -l 'crossing registry:' iterations/reports/batch-desk/*.md`
   ——上一轮 rc=1(空),**连着两轮没有 = 真的有问题**;⛔ 不许拿总监自己的运行结清。
5. ⭐ GH #728(`test_py_gate.py` 12.03s > 12.0s)/ `lua gate` known-red 基线缩小(`16 26` 变小)/
   GH #390 的那一帧(**第三轮顺延**)+ `queue.json:hero-10` 前提重读。
6. 存量:账户级预算过滤器等 owner / GH #523 / patch 缺口 P3 / `hero_domain_scan` 九份 /
   「吞真码」守卫 / **`rc.sh` 是习惯不是门(第十一次)**。

---

## 发表与推送(GH #290 顺序:先 push,再发表)

`git push -u origin claude/compassionate-albattani-6qixfi` → 新分支,钩子三条腿全绿;
`git push origin HEAD:main` 先被拒一次 non-fast-forward(`RPC failed; HTTP 403` + `fetch first`),
`git pull --rebase origin main`(`227b0b79..62283d78`)后重试成功:**`62283d78..6831125d`**。
`claim_precheck.sh` **`PRECHECK_EXIT=0`** / `local commits not on origin/main: 0` /
`paths cited 5, resolved on trunk 6, refused 0` / **`OK to publish`**
⇒ 发表 GH **#736**(`[harness]`,新开,不关闭:等批测台裁 (A)/(B) 并回答 (C))。

## Token 用量

`TOKENS total_in=16,068,500 out=71,261 turns=96`
(⚠️ 到统计时刻为止;统计后的收尾回合不计入。)
