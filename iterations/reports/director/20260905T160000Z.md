# 总监 2026-09-05T15:5x–16:0xZ —— **armed 集第一次在冻结期真的变小(63 → 61)**;本轮最该被读的是 **§EX.3:`zusstatic` 的条件 (a) 在全部八个世界里都买不到,而买不到的理由不是语料稀缺 —— 是它被递进去的那个句柄在八个世界里没有一个是 Static Field**

全文档案:`iterations/streams/test_set.md` **§EX**(最该读 **§EX.3**)。
`bots/` + `game/` **零 diff**;**零 AWS**;无 promote;owner 邮件本周未发。

---

## 1. 本轮做了什么(P4.2 的新产出指标:**判定完结数 = 2**)

`OWNER_PRIORITIES.md` P4.2 于 2026-09-05T14:5xZ 补的「优先级澄清」(commit `ffeb0aa6`,主会话)
是本轮的**唯一**输入,按铁律 9(owner 优先项凌驾于 issue 流)直接认领。它写了三件事,三件都办了:

| P4.2 要求 | 本轮 |
|---|---|
| 把 09-05T10:xxZ 违例入集的 `stayattr` **退集回 62** | ✅ §EX.1 |
| 冻结期收到入集提议,唯一合法裁定是 **FROZEN-HOLD** | ✅ §EX.6(`hero-30` / `axecallbkb`) |
| **从当轮起每轮 ≥2 个判定完结**,从核验记录最少、最难买 (a) 的 id 清起 | ✅ 2 条,见下 |

**armed 串 63 → 61**(540 字节,md5 `824ec2842e234693cb3c4094f73d6a14`)。
距离 P4.2 的解冻线(≤ 20)还差 **41**。

---

## 2. 判定完结 ①:`stayattr` 退回出集(63 → 62)——**执行 owner 裁定,不是改判 §ET**

§ET(11:xxZ)买到的证据**逐条仍然成立**(`tests/test_stayattr_global_ult.lua` 11/0 真帧;
`mutstand_stayattr.sh` 13 CAUGHT + 1 声明式 UNMEASURABLE + 对照 SURVIVED,零 NO-OP)。
**被推翻的是时机,不是证据** —— §BB.4「搭车提议当轮放行」已被 P4.2 废止,而 §ET 那次入集
发生在澄清落地之前。gate 与 `bots/FunLib/jmz_func.lua:5116` 的代码**一字未动**。

⭐ **可核验的还原**:退集后的串与 §EC 09-04T10:xxZ 的 62-id 串**逐字节相同**
(550 字节,md5 `c7e1f92c739f8f8dc0ee0c9484288628`),**已逐位核过**。
`stayattr` 是唯一被追加的项,删掉它必然回到原串 —— 这不是巧合,是构造。

**W48 不作废**(它起飞时 clone 的 `origin/main` 含 `stayattr`),读数照收,按 §ET 归档为额外 (a) 证据。
**重新入集路径**:解冻后凭现有证据直接重提,**不需要重买 (a)**。

---

## 3. 判定完结 ②:`zusstatic` 退回出集(62 → 61)—— ⭐ 本轮的实质发现

**选取不是我挑的**:P4.2 写「从核验记录最少的 id 清起」,`tools/agent/verify_coverage.py`
打的 **BLIND SPOTS**(VERIFY 计数 0 **且**任何报告里 id 附近从来没出现过裁定词)恰好两条:
`midtp` / `zusstatic`。`midtp` 已是录像组 GH #521 (丙) 的棒(§EV 交出),**不抢**。

### 为什么是「买不到」而不是「还没买」

`zusstatic` 的唯一闸址在 `X.GetStaticFieldBonus( hAbility )`,而 `hAbility` 是**参数**,
由唯一调用点 `hero_zuus.lua:652` 经 `X.GetBoundAbility` 递进来 —— 后者自己 gated 在 **`zusbind`** 上,
而 **`zusbind` 从来不在成员串里**。⇒ armed 波里那一层短路成 `return hShipped`,
递进去的是 `abilityAS = bot:GetAbilityByName( sAbilityList[5] )`。

**活的读数(不是散文)**:`tests/test_zuus_ability_index_binding.lua` 用**出厂的**
`J.Skill.GetAbilityList` 枚举 2³ = **8 个丢弃世界**,断言
`[hero] index 5 is Static Field in NONE of the eight drop-worlds`,
并把 index 5 实际 carrying 的东西钉成计数(`zuus_lightning_hands` 2 / `nil` 4 / `generic_hidden` 2)。
本轮复跑 ⇒ **15 tests, 0 failures**,`RC_EXIT=0`。

八个世界逐个走 armed 腿:`nil` 的 4 个世界被未门的 nil 检查先 `return 0`(两腿同,结构性 no-op);
未 trained 的 `return 0`(两腿同);已 trained 的读**别的技能**的 `damage_health_pct`,
无此键 ⇒ 静默 0(GH #162 家规)⇒ `return 0`,而 gate-off 是 `0.09`。

⇒ **八个世界里 armed 腿全部得到 0,从来没有一次得到它被写出来要测的 ~3.45%。**
它唯一可观测的效应是把 Static Field 这一项**整个删掉**(0.09 → 0),而那**不是登记在册的杠杆**。

⛔ 退的是「**登记的杠杆在这个串里不可达**」,不是「读数不好」—— 一个读数都还没有,
而且**再多的局也买不到**:阻断项是串的形状,不是语料的稀缺。与 `l1xpsoak`(08-19)同型。
**GH #173 的事实没有被否掉**(`damage_health_pct` = 3.45 + 0.05/级,出厂 0.09 确实约是真值两倍且偏乐观)。

### ⭐⭐ 它不是 `pullcad` 陷阱,而是**第三种形状**,更隐蔽

`pullcad` 陷阱的谓词**恒假**(闸冻死,`check_armed_wiring.py` 仍叫它 WIRED)。
这一条的谓词**恒真而答案恒 0** —— 它每帧都开火,只是拿到的句柄是错的。
**一个开火的闸带一个恒定的错答案,在任何聚合读数里都长得跟「已测试且中性」一模一样。**

依赖 `zusbind` 此前**写在两个函数的注释里、不写在同一个布尔表达式里**
(`hero_zuus.lua:575` 逐字:*"`zusstatic` armed without `zusbind` armed measures the wrong
ability's missing key, i.e. 0"*)——**散文正是它被 armed 十几天没人发现的原因**。

### 交棒:这一次是**机器可读**的

登记为共同 promote 原子 **`static_field_needs_binding`**
(`iterations/promote_atoms.json`,`rule=no_promote_without`,subject `zusstatic` / prereq `zusbind`)。
⛔ 我**没有**把这条依赖再写成第三句散文。

**非空洞性验收(变异台)**:临时把 `zusstatic` 闸址改成 PROMOTED 形状 ⇒ **CAUGHT**,`exit 3`,
报错逐字点名两个 id;从**文件副本**还原后 md5 逐位相同(`0af7092e…`),
`bots/`+`game/` 零 diff,工具复跑 `exit 0`。

---

## 4. ⚠️ 本轮顺带撞红一条测试,而那条红**不是**它守的东西

`tests/test_carrier_terms.py` 因退集打 `FAIL zusstatic is still hero-scoped`。
**归因查实:是我的改动,但那条断言的红与它要守的东西无关** —— `derive_id` 仍然把
`zusstatic` 解析成 `zuus`(同文件第 2 节直接对树断言,一直是绿的)。

⭐ **这正是该文件注释块(76–89 行)自己描述的缺陷,只是方向差一个**:
08-29 的重述把这个循环**对增长**加固了(原话 *"so growth cannot renumber it"*),
**却没有对移除加固**,而 `scoped` 仍然从**活的 arm 串**导出。
⇒ 「一个每次 arm 串被编辑就变红的测试,是在复述 arm 串,不是在检查推导器」。

**修法**(narrow,把断言按它能有的两种含义拆开,两侧都保留牙齿):
armed ⇒ 仍要求经**活的推导**得出 hero-scoped(原有牙齿不变);
未 armed ⇒ arm 串**没有资格替它说话**,改为直接向推导器 + 树要这个断言。
**退集一个 id 绝不能顺手把守它的断言一起退休。**

**变异验收**:删掉 `zusstatic` 的唯一闸址 ⇒ 新分支**按名字打红**
(`FAIL zusstatic is still hero-scoped off the tree`),`RC_EXIT=1`;还原后 66 checks / 0 failed。

---

## 5. FROZEN-HOLD 的第一次行使:`hero-30`(`axecallbkb`)

开工自检打 `RIDESHARE (§BB.4: rule this round): 1 -> hero-30`。
⛔ §BB.4 已被 P4.2 废止 ⇒ 本轮裁 **FROZEN-HOLD**,落在 `queue.json:hero-30.director`,`status=frozen_hold`。

- **不是否定证据**:英雄组 13:5xZ 已落地 gated `axecallbkb`(`c1cce307`,turbo-only 未 armed),
  桌面证据完好(`bkbpierce: "Yes"`;仓内 `special_value_shapes.lua` 对上 radius 315 / cd 18 16 14 12)。
- **也不是掉棒**:它要的是**零 EC2 的只读档案扫描** —— **冻结冻的是入集,不是取证**。
  裁定明写:证据继续买(两支分开报、不许并池),解冻后凭现成证据重提。

---

## 6. 计量与巡检

- **载体项 8 → 8 逐字不变,而且是量出来的**:`carrier_terms.py` 对 63-id 与 61-id 各跑一次,
  `TERMS` 行**逐字节相同**,`0 unresolved`;计数 `13 hero-scoped / 50 generic` → `12 / 49`。
  机械理由:`zuus` 仍由 `zusult`/`zusboltdom` 供着,`stayattr` 本就是 generic。
  ⛔ 与 §ET.4 / GH #522 同一条警告:**载体项会在 arm 串一字不动时改变**,不要把差额读成本次退集带来的。
- **铁律 6**:`luacheck_gate.sh` ⇒ `0 warnings`,`GATE_EXIT=0 CLEAN`(容器初始无 luacheck,门自己装的)。
  动态那半**未全量跑**(~100min,GH #124);`bots/`+`game/` **零 diff**,改动集中在
  `tests/` + `iterations/`,已**逐个**跑过读这些文件的测试:
  `test_promote_atoms.py` exit 0 / `test_carrier_terms.py` 66 checks 0 failed /
  `test_pending_rulings.py` 203 checks 0 failed / `test_gate_claim_consistency.lua` 16 tests 0 failures。
- **开工自检**:`unlanded` OK(certifiable window 无未落地);`orphan 0`;两个 stable 锚点三不变量全 ok;
  `FROZEN none`;`RIDESHARE 1`(本轮裁掉);owed registry 3 行(退 0 增 0)。
  ⚠️ **cadence 唯一的洞仍是 director 09-04T19:10Z→23:06Z(3.9h,历史)**。
  ⚠️ **trunk health(python 套件)那一腿本轮超时未打印**(400s 上限),⇒ **那一侧本轮我没看过**,
  **不声称一份完整的自检**;上一轮的全量结算是 `93 passed / 7 failed / 2 uncertifiable,零条本轮引入`。
- **零 AWS** ⇒ **不对 MTD 作任何新声称**。`DECISIONS_NEEDED` +0。**patch 检查未做**(低频顺延)。
- ⚠️ **纪律 3 本轮又中一发,且又是第一条命令**(`| tail -60` 被 §22 守卫当场拒,
  `SELFCHECK_EXIT=2 REFUSED`,守卫文案自己写着 *"recurred 5x, every time as the first command"*)
  —— **守卫拦的,不是我记住的**;被拒的那次**没有执行任何检查**,所以没有重演并发写 `soak_side.lua` 的事故。

---

## 7. 交给别人的、现在就活的读数

- **批测台**:P4.1 upstream 标尺波已被 09-05T15:2xZ 那轮**认领并发出(Y1)** —— P4.2 的
  「再空一轮即写进 DECISIONS_NEEDED」**本轮不触发**。
- **上一轮 ⑬ 的结转**:`test_wave_throttle.py` 对 W48 打的
  `UNCERTIFIABLE: W48 machine 0 (seed 5025) has no launched_at / gate (i) DID NOT RUN` ——
  批测台 15:0xZ 报告已收 W48(4/4 干净)。⚠️ **未见它对这条闸 (i) 读数作答**;
  按铁律 9 连带规则,**若下一轮批测台报告里仍没有它,总监把它变成一条 owed 行**(本轮先记,不提前登记)。

## 8. 下次触发

1. **`text_absent_done_when_kind`(GH #523)** —— 上一轮排的 ①,本轮被 P4.2 抢占;
   **先写认领并 push 再开工**,这是真要花一轮的棒。
2. **继续 P4.2 的库存清算**(每轮 ≥2 个判定完结):`verify_coverage.py` 的 `verify=0` 长尾,
   从 `tpdying`/`tpreach`(narrat 2)起;`midtp` 等录像组 GH #521 (丙) 回话。
3. **GH #517**(只在全量跑里存在的泄漏)/ trunk 红逐条(#506 先做)+ 单独复跑 python 半边。
4. `kind:"ruling_request"` 轻量 queue 行 / GH #454/#487/#460/#473 乙/#489/#486/#496 / #513/#514。
5. GH #449/#410/#436/#285 / **patch 缺口 P3** / `ckpush`(有时限)。

---

## 9. 本次会话 token 用量(铁律 8)

```
TOKENS total_in=7,390,904 out=40,104 turns=61
```
(统计时刻之后的收尾回合不计入。)

---

## 10. 收尾登记(push 之后,按铁律 6 补款 GH #290 的顺序)

- **GH #531 立案** —— §EX.4 的「第三种形状」(谓词恒真而答案恒 0)。
  ⚠️ 本轮登记的共同 promote 原子**只管 `zusstatic` 一个 id**,是手写的一行不是检测器;
  #531 要的那一半是**通用检测**:还有几个 armed id 的答案由一个 gated 在别的 id 上的
  句柄/返回值决定,**没有人知道**。
- ⭐ **顺带验证了本轮自己的纪律**:初稿在 `promote_atoms.json` 的 `ref` 里写的是 **`GH #529`**
  (当时最高 #528,顺号推的),实际拿到的是 **#531** —— 两个流在同一小时里各开了一个。
  ⇒ **顺号推测的引用号会错,而且错得像个普通数字**;`claim_precheck.sh` 对尚不存在的
  issue 号**无话可说**(它不解析 GH 号)。做法:**先把号从引用里拿掉、push、开 issue、再回填**。
- **铁律 6 补款(GH #290)本轮遵守了**:发 issue **之前**跑 `claim_precheck.sh` ⇒
  `MISSING section test_set.md §EX` + `DO NOT PUBLISH YET`;push 之后复跑 ⇒
  `resolved on trunk 8 → 9`、`0 finding(s)`、`OK to publish`、`PRECHECK_EXIT=0`。
  ⚠️ **上一轮(§EV.7 甲)在这里是违反的,本轮是同一个工具、同一个顺序、结果相反。**
- **push 记录**:`git push -u origin claude/compassionate-albattani-b6ykqx` ⇒ 新分支,`EXIT=0`;
  `git push origin HEAD:main` 首次 **rejected(non-fast-forward)** —— 录像组 `fe2fbf90` 抢先落地;
  `git pull --rebase origin main` 干净(1/1),复核 arm 串仍是 61 / md5 `824ec2842e…`、
  `§EX` 唯一(录像组那次**没碰 test_set.md**,零冲突),再 push ⇒ `fe2fbf90..a40c6014`,`EXIT=0`。
  **铁律 6 的静态门在两次 push 上各跑一次,均 `0 warnings / GATE_EXIT=0 CLEAN`,无 `RULE6_BYPASS`。**

## 11. ⚠️ 并发落地的一条读数,方向与 §EX.1 一致,不改变裁定

录像组 `fe2fbf90`(16:0xZ):**W48 全波重跑 `stayattr` 条件 (a) —— 域第一次抵达,
(a1) WORKING / (a2) 仍 INDETERMINATE**。

- **不改变 §EX.1**:退集的理由**从来不是证据不足,是时机**。新证据把
  「§ET 买到的证据逐条仍然成立」**加强**了,没有动摇它。
- **它正是 §EX.1 预告的那一笔**(「W48 不作废,其读数按 §ET 归档为额外 (a) 证据」)。
- **对重新入集**:解冻后重提**不需要重买 (a1)**,但 **(a2) 仍欠**,不得说成已买。
- ⛔ **只做指针不做转述**(§EV 的教训:读的树可能已不是他们写时的那棵)。
