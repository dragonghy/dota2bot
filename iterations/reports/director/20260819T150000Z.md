# 总监工作单元报告 — 2026-08-19T15:00Z(第八次触发)

**一句话**:上一轮计划的 ①(收 `lf_rescue` bisect verdict)**本轮仍不可能做**
(臂 B 三个种子 14:11Z 才补跑,ETA 14:45-15:00Z,批测台下轮收割),于是走章程
优先级 (c),把**唯一一件成熟的待批事项**做完并**自己复核了证据**:
`cmrguard` **批准重新入集**(armed 集 14 → **15**),GH #34 关闭;顺带把协同组
撞出来的一个**影响面比它本身大**的验收保真度缺口量化并立规矩(§0b 新亚型)。
**未改任何 Lua,未做任何 AWS 调用,未启动计费资源。**

## 1. 输入(读了什么)

| 来源 | 关键内容 |
|---|---|
| `batch-desk/20260819T141222Z.md` | 臂 A 4 种子齐(gpm **-24.06**,0/4);臂 B **3 台被 spot 无容量回收**,只有种子 862 可计分;**A−B 本轮不定论**;14:11Z 用 **on-demand** 补跑 859/860/861,2h 看门狗;MTD **$4.0579**,无泄漏 |
| `replay-check/20260819T125022Z.md` | `lf_rescue` = WORKING + BUGGY(11 次归属 vs 臂 B 的 0,p≈0.008;10/11 到不了) |
| `strategy/20260819T134500Z.md` | #37 三帧钉住;**候选改法 1(落点距离门)被真实帧证伪**;两处 mock 保真度缺口 |
| `hero/20260819T134402Z.md` + GH #34 | `cmrguard` 收窄 + 端到端 5 例,申请重新入集 |
| open issue 扫描(26 条,其中 open 26) | **无新增 `[bug]`/`[harness]` 需总监动代码**;`#33`/`#27` 仍是已知 backlog |

## 2. 本轮决定 ①:`cmrguard` **批准重新入集**(armed 集 14 → **15**)

11:10Z 我把它在第一次 armed 之前退回英雄组(不是 reject),钉了两条重新入集条件:
按 `ccburst` 已付费买过的写法做**投送距离**检查 + **两帧一起钉**。英雄组 11:53Z
收窄、13:44Z 在 `#36` 修好之后补上端到端 —— **两条都做到了**,而且做到的形状正是
§0b 那条规矩要求的(断言**最终出价**,不是 helper 返回值)。

### 2.1 总监自己复核的三件事(不是转述报告)

1. **全套 455/455 绿 + `luacheck bots game --formatter plain` 0 警告**,在本容器
   重跑(容器初始无 Lua 工具链,已自行 apt 装)。
2. **变异复现**:把 `cm_IsRSafeToOpen` 退回 range-blind 的布尔包装(= GH #34
   立案时那一版),**恰好 5 条 FAIL,其中 2 条是端到端的**:
   ```
   FAIL: end-to-end [MUTATED frame]: armed equals shipped on that frame
   FAIL: end-to-end [MUTATED frame]: the GH #34 false positive no longer eats the bid
   FAIL: gate ON: still allows if the engine reports hoof_stomp cast range as its 325 radius
   FAIL: gate ON: the 1326u retreating Centaur no longer vetoes
   FAIL: wiring: nothing calls the range-blind boolean wrapper any more
   ```
   改回来 455/455 全绿。**英雄组报的数字核对无误**(它自报"挂 5 条含 2 条端到端")。
3. **可证性的结构前提 —— 这条英雄组没查,是本轮新增的**:
   `X.ConsiderR` 在 `hero_crystal_maiden.lua` 里**只有一个调用点**
   (`SkillsComplement` L209 → L214 `ActionQueue_UseAbility(abilityR)`),而门在
   `ConsiderR` 的**第一个 if**。所以门覆盖 `ConsiderR` 的**全部分支**,包括英雄组
   指出的「CM 现实里的大招其实走 `IsGoingOnSomeone`/目标制导这些次要分支」那些。
   **推论(写进 test_set §E 给录像组)**:上机后若测到 `cmrguard` **SILENT**,
   那是**真实发现**,不是管线假象 —— 与 `creeppull`/`pullcamp`/`l1xpsoak` 那三个
   「结构上不可证」的情形不同,**这里没有第二条施法路径可以绕开门**。

**为什么值得花这一步**:本周已经三次栽在"可测性"上(#28 / #34 / #37)。批准一个
id 入集,实质上是在承诺"它上机后能被核验";在此之前先证明**没有旁路**,比事后
拿一个 SILENT 读数再猜是"没触发"还是"管线看不见"便宜得多。

### 2.2 四条约束(已写进 `test_set.md` §E + `state.json:cmrguard_READMISSION_20260819`)

1. **条件 (b) 用行为检测器判,不许用 gpm/xpm**(11:10Z 已定的前置约定,理由不变:
   ≈0.36 次改判/局 vs GH #30 的 σ≈30 / SE≈15)。补了**正确性指标**:
   **被门拦下的帧里,敌方硬控在 10s 内真的落到 CM 身上的比例** —— 这道门的失败
   模式是**误报**,只数"开大次数少了多少"读不出对错。
2. **对 14:11Z 那三台补跑零影响**(cand 串启动时已固定);随**读完 bisect verdict
   之后的第一波**上机,与 `tpdying` 同批;**若那一波仍是 bisect,它必须两臂完全相同**
   (同 `capmono` 的处理 —— 它不是被测变量,只随波取证 (a))。
3. 执行核验必须回答"被拦的帧是不是真威胁帧"。
4. **case #4 有意留白**(skeleton_king 620u、威胁 12.0s 后才兑现):单帧快照里没有
   "他在不在逼近"这个信号,任何能放行它的 buffer(<70)会连带放掉录像组判为**正确**
   的 shackles 否决。**要新证据、新 issue,不要在这个 id 上悄悄扩面。**

**诚实边界(和批准一起记)**:两个真实帧上 shipped 的 `ConsiderR` 本来就出价 NONE
(开局分支要 ≥3 敌,两帧都只有 1 个敌人站得那么近),所以端到端各例把该帧上
**真实存活的两个敌人**走进 500u 作**标注过的变异**,并把"他们不持就绪硬控"
**写成断言**。门的决策层效果是在**变异帧**上证明的 —— 批准不受影响(变异标注过、
方向保守),但**上机核验时不能拿这两帧当"线上会发生什么"的证据**。

GH #34 **已关闭**(残留 case #4 记在 state.json,关 issue 不丢信息)。

## 3. 本轮决定 ②:§0b 立**新亚型** —— 「验收世界本身是退化的」

协同组 13:45Z 做 #37 三帧验收时撞上一个**比 #37 本身影响面更大**的东西:
`tests/mock/bot_api.lua` 把 `GetTower` 桩成 `nil`、`UNIT_LIST_ALLIED_BUILDINGS`
返回 `{}`,于是

- `J.GetNearbyLocationToTp` 的塔循环**永远空转** → 直接落到「一座塔都不剩」的兜底
  分支 → **返回泉水**。此前本仓库**任何**关于 TP 落点的 fixture 断言,测的都是一条
  真实对局里(塔还立着时)**不会发生**的路径;
- `J.GetRescueTpTarget` 里「友军站在自家塔下就别救」的否决
  (`jmz_func.lua:5722-5731`)**恒为 false**。

**这是 §0b 的第七例,但类型不同**:前六例是**消费方把 helper 的推理作废**,这一例是
**验收环境给了一个真实对局里不存在的世界**。通用形式已写进 test_set:
**mock 的每一个 `return nil` 都是一条未声明的世界假设。**

**总监本轮量的残留缺口(免得被当成"已修完")**:**46 个 fixture 里只有 3 个带
`buildings`**(协同组本轮新生成的那三个),**其余 43 个仍活在退化世界里**。据此立了
一条规矩 + 点了一条要重看的既有结论:

- **规矩**:今后关于 **TP 落点**或**「友军在自家塔下」否决**的 fixture 断言,**必须
  用带 `buildings` 的 fixture**;在老 fixture 上做这两类断言是**假绿**。协同组留的
  「老 fixture 逐字节不变」回归护栏是**存档**,不是**豁免**。
- **要重看的结论(一条)**:`tests/test_replay_071423_sky_rescue.lua` 里
  「`GetRescueTpTarget(sky)` 必须选中 Luna」这类**肯定式**断言,是在「塔下否决结构上
  不可能触发」的世界里做出的 —— **是触发集的超集,没被证伪,但比字面读起来弱**。
  录像组 12:50Z 那 467 个 episode 是 **Python 侧独立重建**的,**不受影响**
  (#37 的结论不需要重看)。

## 4. 未做 / 为什么

- **收 bisect verdict(上轮计划的 ①)**:臂 B 三个种子 14:11Z 才起跑,ETA
  14:45-15:00Z,**收割是批测台的活**(且我不花 AWS 钱)。§A0 的裁定已经预写好
  ——**A−B 若为 null 只当上界,不构成条件 (b) 通过** —— 下轮拿到数字直接照用。
- **patch 分片 P3 / `[harness] #33` 第二级 / `#27` / backlog #1(帧语料检索工具)**:
  本轮优先级 (c) 有成熟待批事项,按章程先做它;这四项无时限。**backlog #1 是
  owner 点名方向,建议下轮若无待批/待判事项就专门做它**(它是"执行核验规模化"的
  地基,而本周已经三次栽在可测性上,收益正在变大)。

## 5. 成本 / 体系健康

- **成本**:MTD **$4.0579**(批测台 14:12Z 自报),刹车线 $90 / owner 批准线 $100 /
  批测台自设围栏 $45 —— 均未触及,无泄漏。在跑的 3 台是**有意启动的 on-demand 补跑机**
  (2h 看门狗,最迟 16:11Z 自毁),批测台下轮(~16:12Z)会复查。
  **总监本轮未做任何 AWS 调用、未启动任何计费资源。**
- **Routine 健康**:五组本轮均有产出、无空转。批测台"补跑而非开新例行波次"的判断
  正确(6h 节流 + 成本条件都记在报告里)。
- **运维事实采纳**(批测台 §3.1,值得所有人知道):**一波实际只要 ~30 分钟,不是 3 小时**;
  **短波次不要用 spot**(半小时省 $0.2/台,丢一个种子要补跑一整轮)。
- 今日周三,**效率台账(仅周日)跳过**;patch 检查 11:10Z 刚做过分片+定级,不重复。
- `iterations/DECISIONS_NEEDED.md` 仍未创建 —— 本轮两个决定都在自主授权范围内,
  **本周 owner 邮件仍为零**。

## 6. 验证

```
luacheck bots game --formatter plain   -> 0 警告
lua5.1 tests/run_tests.lua             -> 455/455 绿
```
(容器初始无 Lua 工具链,已自行 apt 安装。本轮**未改任何 Lua** —— 变异测试用的临时
改动已还原,`git diff bots game` 为空;改动面是 `test_set.md` / `state.json` /
本报告 / 章程。)

## 7. 下次触发优先级

1. **收 `lf_rescue` bisect verdict** —— **先读 `test_set.md` §A0 再看 A−B 数字**,
   null 按**上界**处理,不是通行证;按 §C 两臂**逐种子配对差**读,**不许比历史 -34.59**。
2. 结论落地后排下一波:`tpdying` + `cmrguard` 随集上机(§A' 四条 + §E 四条约束)。
3. 若上面都在等:**backlog #1 帧语料检索工具**(owner 点名,且本周三次可测性事故
   说明它的收益在涨)/ patch 分片 P3 / `[harness] #33` 第二级 / `#27`。

## 8. 本次会话 token 用量

```
TOKENS total_in=7,100,293 out=58,404 turns=66
```
(统计时刻为止;之后的收尾回合不计入。)
