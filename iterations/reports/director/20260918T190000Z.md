# 总监(director)—— 2026-09-18T19:00Z 轮

**一句话**:RULING 81 —— 走查登记普查入闸(欠条 `walk_census_out_of_push_gate_so_rule803_cannot_bind`
按 **(甲)** 付清并退休);落地途中撞出并修掉第二个缺陷 —— 手读表里 **4 个键写了两遍**,
dict 字面量静默只保留最后一个,**四份真做过的手读一直被丢弃**,而普查自己没有任何一条检查管它。
另:开工自检抓到的 **TRUNK RED 已修**(今天第二条未登记走查);交棒 ⑪(GH #899 验收)**已付**。

零 AWS、零波次、`bots/` + `game/` **零 diff**、不发 owner 邮件、无 promote / 无退集 / 无入集。
成本(铁律 1 RULING 48 三段式):**零 EC2 / 零 CE / S3 读取 0 个对象(出网未计价)**;
GitHub MCP:`issue_read` 点查 2 次(#899、#839,免费)。

---

## 〇、开工自检与交棒腿

**铁律 10(开工自检)**:`nohup bash tools/agent/routine_selfcheck.sh > /tmp/sc.log 2>&1 &`。
⚠️ **本轮头两次取法都被守卫当场拒**,两次都不是「忘了」而是**换了一种错**:

| 取法 | 守卫逐字 | 真码 |
|---|---|---|
| `routine_selfcheck.sh 2>&1 \| tail -60` | `REFUSED: stdout is a PIPE … 纪律 3,第 5 次复发,每次都是本轮第一条命令` | 2(什么都没检查) |
| `timeout 600 bash …> /tmp/sc.log` | `REFUSED: an ancestor process is 'timeout' … 实测 EXIT=124,537 行,快 Lua 检测器腿从没跑过` | 2 |
| `nohup … > /tmp/sc.log 2>&1 &` | —— | 跑完 |

📌 **这两条守卫是章程 §0「本轮任何要读退出码的命令都走 `rc.sh`」立案时点名的同一族**,
而本轮**第二发是新形状**:`timeout` 不经过管道,`rc.sh` 也拦不住它 —— 拦住它的是脚本自己长的第二道守卫。
⇒ 本节按实测把两条取法并列写下,供下一轮直接照抄第三行。

**§1.5 交棒腿**(`carry_item_issue_state.py`,离线只读,走 `rc.sh`):`RC_EXIT=0`,逐字
`scanned : 1 entry (of 99), 1 carry segment(s), 12 GH ref(s)` / `every carried GH ref is open.`
(语料 `fetched 2026-09-18T13:10:35Z (5.7h old)`,在 48h 内)⇒ 无 `STALE-CARRY`、无 `NO-HANDOFF`。

**§2e 体系健康(按 (甲)(乙)(丙)(丁) 逐字取法,`git fetch` 之后,取数时刻 2026-09-18T18:50:48Z)**:

```
batch-desk    iterations/reports/batch-desk/20260918T180555Z.md
replay-check  iterations/reports/replay-check/20260918T154200Z.md   (其后 18:48Z 那份本轮 rebase 才到)
strategy      iterations/reports/strategy/20260918T165529Z.md
hero          iterations/reports/hero/20260918T170516Z.md
director      iterations/reports/director/20260918T162000Z.md
```

五组当日均有产出,**无停摆、无点名、无红色升级**。

---

## 一、TRUNK RED(开工自检 python 腿)—— 今天第二条未登记走查

**读数逐字**(`/tmp/sc.log`,`150 passed, 1 failed, 2 uncertifiable`):

```
FAIL  tests/test_bots_walk_farm_only.py
  8 checks, 1 failed  [310 commands executed, 78 unresolved]
  FAIL: a tests/ io.popen command cannot be resolved statically and is not on the hand-read
  list -- read it, then add it to UNRESOLVED_HAND_READ with the value its callers pass
  (do not delete this check): ["tests/test_wk_q_flee_reach.lua  ::  'ls ' .. dir .. ' 2>/dev/null'"]
```

**归因(深历史已确认不浅:`git pull --rebase` 后取)**:走查落在 `df1353ed`
(**英雄组 2026-09-18 17:05Z 轮**,`wkqflee`),即**在总监 16:20Z 那一轮补登记 `test_wk_q_aim_preflight.lua` 之后**。

⭐⭐ **它带着自己的义务注释落地**(`tests/test_wk_q_flee_reach.lua:106-107`,逐字):

```lua
-- UNRESOLVED_HAND_READ: io.popen, the habit registered in
-- tests/test_bots_walk_farm_only.py (GH #596 / GH #803).
```

⇒ **作者知道这条义务、把它写下来了、没有做**。这是「看起来已登记而其实没登记」的**第三例**
(前两例见普查内 `test_cm_lane_fallback_wallet` / `test_lion_hex_panic_level` 两条 ⚠️ 注)。

**处置**:手读并登记(`CORPUS_DIRS` = `{'tests/fixtures','tests/frames'}`,`:79` 是唯一赋值,
`:234-236` 只在一句消息里提到它;裸 `ls` **非递归**,够不到 `bots/Customize/`)。
普查此刻 **`8 checks, 0 failed`**。

⛔ **但本轮不止于此** —— 见 §二。**再重申一次 GH #803 已被实测否决**:
这条规矩自 09-12 起至少被重申六次,**过半数登记是作者以外的人补的**,
而 09-18 一天之内它被破坏了**两次**,其中一次由**那条重申自己所在的 commit**(`75b0cf4b`)完成。

---

## 二、⭐⭐⭐ RULING 81 —— 把那条断言送进 push 闸(欠条按 (甲) 付清)

### 2.1 为什么这是价格问题不是习惯问题

普查是**唯一**会发现新落走查未登记的读者,而它自己 `in_gate: false`
⇒ **没有任何一条 push 腿能拒绝那次 push**,红由下一个开工的组买单。

**本轮把这个价格拆开量了(Routine 容器,2026-09-18)**:

| 半 | 实测 | 它能回答的问题 |
|---|---|---|
| 读 + 静态解析全部 `tests/` `io.popen` | **1.679s** | **「每条解析不出的走查都在手读表上吗」** ← 八次红全在这里 |
| 执行 89 条互异的只读走查 | **2.556s** | 「走查够到 `bots/Customize/` 了吗」 |
| 合计(manifest 行) | **4.413s** vs cap **3.0s** | ⇒ `in_gate: false` |

⇒ **红的那一半本来就装得下**。

### 2.2 落地:一个模式 + 一个子集,**不是**第二份实现

- 普查新增 `--static-only`:跳过执行半,**逐条打印它没跑的检查**
  (`NOT RUN (--static-only, this is not a pass): …`),摘要行以 `STATIC-ONLY: ` 开头。
- 新文件 `tests/test_walk_registration_static.py`:**运行**普查(不重写它),读退出码 + 读输出。

⛔ **不重写的理由是实测的,不是整洁**:提取器有一条「丢掉末尾 `, <message>` 实参再试一次」的重试,
它把 **98 个静态未解析里的 20 个**挪进已解析列(**98 − 20 = 78**,与普查自报的 `78 unresolved` 逐位对上)
⇒ **一份忘了这条重试的第二拼写,会在首跑就报 20 条假发现**。

**读数(裸码,⛔ 未经管道)**:

```
python3 tests/test_bots_walk_farm_only.py                 8 checks, 0 failed  [310 commands executed, 78 unresolved]   4.44s  exit 0
python3 tests/test_bots_walk_farm_only.py --static-only   STATIC-ONLY: 5 checks, 0 failed  [310 call sites resolved, 78 unresolved]  1.76s  exit 0
      NOT RUN (--static-only, this is not a pass): the farm-only reach check (needs the 89 walks executed)
      NOT RUN (--static-only, this is not a pass): the READ_SIDE_FILTERED still-reaches checks (2 of them, same reason)
python3 tests/test_walk_registration_static.py            12 checks, 0 failed   1.731s  exit 0
```

### 2.3 入闸读数(`done_when` 要的两样)

`python3 tools/agent/py_gate_measure.py`(⛔ 不手写 manifest —— 该文件首行逐字 `Do not hand-edit; re-measure.`):

| | 前(`measured_at 16:24:49Z`) | 后(`19:14:33Z`) |
|---|---|---|
| `tests/test_walk_registration_static.py` | 不存在 | **`{"seconds": 1.756, "in_gate": true, "reason": "fast"}`** |
| `tests/test_bots_walk_farm_only.py` | `4.493s / false` | `4.413s / false`(**设计如此**,执行半留在开工自检) |
| `selected_total_seconds` | `50.468` | **`53.236`**(`budget_seconds` 90.0,余量 **36.8s**) |
| `selected_count` / `measured_count` | `133 / 153` | **`137 / 154`** |

**驱逐**:`py_gate_measure.py` 的 `⚠️ … LEFT the hook` 块**没有打印**,逐字只有
`4 test(s) entered the hook: …` ⇒ **零驱逐**。

⚠️ **另外三个「入闸」的不是我的活,而且它们说明一件事,照实登记**:
`ability_value_key_census` `3.259 → 2.590`、`call_form_census` `3.136 → 2.430`、
`guard_implication_census` `3.623 → 2.897` —— **三条一致下降约 20%**,
⇒ 这是**容器变快**,不是测试变快。**闸的成员资格在 cap 附近取决于哪台容器跑的上一次重测**
(与 GH #358「wall clock 分不开『集合变大』和『容器变慢』」同族)。
⭐ 对本轮的结论**无影响**:`1.756s` 距 cap 有 **41%** 余量,不在那个边界带里。
⛔ 但它是一条真实的结构观察,已写进 §五作为下一轮的开号候选,**不在本轮顺手改**。

### 2.4 变异台(evidence-discipline:能不能红,且为对的理由红)

`bash tools/agent/mutstand_walk_registration.sh` → **CONTROL 绿,5 CAUGHT / 0 SURVIVED / 0 ABORTED**
(还原走**文件副本 + 双文件 `sha256sum -c`**,退出码全部裸读;`trap restore EXIT` 在第一次变异**之前**上膛):

| 变异 | 它伪装成的样子 | 结果 |
|---|---|---|
| M1 删掉本轮登记的那一条 | **本轮这条真缺陷本身** | CAUGHT(exit 1) |
| M2 `NOT_RUN = []` | **跳过冒充通过**(GH #384 族) | CAUGHT |
| M3 同一个键写两遍 | **一份手读被静默丢弃** | CAUGHT |
| M4 提取器不匹配 | **什么都没扫 = 没有发现**(GH #345 族) | CAUGHT |
| M5 真发现 + 吞掉退出码 | 只读退出码的守卫会读成绿 | CAUGHT |

⭐ **M5 是被子集的第二条通道抓住的**:退出码是一条,打印出来的 `FAIL:` 行是另一条,两条都读。

⚠️ **闸当场替我抓了一次真错**:第一版变异台**没有 `trap restore EXIT`**,
`tests/test_mutstand_restore_trap.py`(GH #418)立刻顶红 `py gate: 137 ran, 1 findings`,逐字
`tools/agent/mutstand_walk_registration.sh installs an EXIT trap`。补上 trap(并在 `rm -rf` 前 `trap - EXIT`)后转绿。
📌 **这正是本轮要买的那个性质在我自己身上生效了一次**:闸拒绝的是**推的人**,不是下一个开工的人。

---

## 三、⭐⭐ 落地途中撞出的第二个缺陷:手读表里 4 个键写了两遍

插入新条目时 `s.count(anchor) == 2` —— 一查,`UNRESOLVED_HAND_READ` 里**四个键各写了两遍**:

| 重复键 | 两条各自的作者 |
|---|---|
| `test_cm_lane_fallback_wallet.lua` | 总监 09-16 / 策略组 09-16(第七例 GH #774) |
| `test_cm_kill_confirm_quantifier.lua` | 两份独立手读 |
| `test_wk_q_teamfight_reach_pricing.lua` | 两份独立手读 |
| `test_lion_hex_panic_level.lua` | 两份独立手读 |

⛔ **dict 字面量静默只保留最后一个** ⇒ **四份真做过的手读一直在被丢掉**,
而**普查自己没有任何一条检查管它** —— 尽管它在 `axe_hunger_camp_reach` 那处缺口的注释里
**逐字讲过这个危险**(「a dict literal with the same key twice keeps the LAST value silently」)。
📌 **本仓最常复发的形状又一次**:**一条规矩以散文形式存在于它本该约束的文件里**。

**处置**:四条**合并**(保留双方各自的事实,⛔ 不删其一),键数 `80 → 77`;
判重复键的检查放进子集,用 **`ast` 读源码** —— 等 dict 建好,证据已经没了。
变异台 **M3** 就是问这件事。

---

## 四、交棒 ⑪ 付清:GH #899 验收

验收句逐字要求:「在一个**没有 `lua5.1`** 的容器上跑 `python3 tests/test_rc_wrapper.py`,
裸码 **0**(而不是 2),且 `command -v lua5.1` 之后非空」。

**本轮实测(同一条命令内先量前提,再跑,裸码)**:

```
LUA51_ABSENT                     ← command -v lua5.1 为空
RCW_EXIT=0                       ← 34 checks 全 ok,含三条需要真解释器的 .lua 检查
command -v lua5.1  →  /usr/bin/lua5.1
```

⚠️ **诚实边界,照写**:本轮开工自检在后台并发跑着,它自己在 `:767` 也会买 `lua5.1`。
测量时刻自检日志只有 4 行(停在 `unlanded work` 腿,远在那一行之前),
且三条需要真解释器的检查**在同一次运行里全部为 ok** ⇒ 解释器是在 `command -v` 与该测试之间出现的。
**残余不确定性是亚秒级的并发窗口**,不是一个待办;若要彻底消掉它,下一轮可在一个
**不跑自检**的容器上复跑同一条命令。

---

## 五、下一轮的开号候选(⛔ 本轮不开:无把握的号不猜,§EX.11 实测两次写错)

1. **cap 边界的成员资格依赖容器速度**(§2.3 的三条 −20%):
   `py_gate_manifest.json` 的 `in_gate` 在 2.4–3.6s 这个带里**跟着跑重测的那台机器变**。
   两条候选处置:(甲) 用**相对基准**(同容器上一条基准测试的秒数)代替绝对秒数;
   (乙) 把 cap 附近的行标出来,让重测**打印**「这一行是被容器速度挪进/挪出的」。
   ⛔ 两条都要先量爆炸半径再选,**不是可以顺手做的决定**(与 GH #616 约束 1 同一条理由)。
2. **RULING 81 本身**:本轮已按 (甲) 付掉 `walk_census_out_of_push_gate_so_rule803_cannot_bind`
   并退休,开号只为给 GH #839 §2 那个结构问题留一条可引用的现场。

---

## 六、闸(三条腿,裸码,⛔ 未经管道)

```
luacheck bots game: 0 warnings
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 137 ran, 0 findings, 0 uncertifiable, 55.3s
```

(`lua gate` 与两次 push 的读数在 §八补记。)

---

## 七、本轮改了什么

| 文件 | 改动 |
|---|---|
| `tests/test_bots_walk_farm_only.py` | 登记 `test_wk_q_flee_reach.lua`(修 TRUNK RED);合并 4 个重复键;新增 `--static-only` 模式 + `NOT RUN` 披露 |
| `tests/test_walk_registration_static.py` | **新**:入闸的快子集(1.756s),12 checks |
| `tools/agent/mutstand_walk_registration.sh` | **新**:变异台,5 变异 + CONTROL |
| `tools/agent/py_gate_manifest.json` | `py_gate_measure.py` 重测(⛔ 未手写) |
| `iterations/owed_executions.json` | 退休 `walk_census_out_of_push_gate_so_rule803_cannot_bind`(按 (甲)) |

⛔ `bots/` + `game/` **零 diff**。

---

## 八、⑨ 下次触发

①**GH #856** 剩 9 候选 ②**GH #867** ③**GH #240** 余下 ④`carry_mark_prose_vs_list` 剩唯一一格
⑤**GH #843** 剩 (乙) ⑥**GH #859** ⑦**GH #810** 待裁 1 + (乙) ⑧**GH #528**
⑨**GH #839** 追评本轮 RULING 81 的现场(§2.3 的入闸前后读数正是它 §2 要的「新落测试怎样默认进 manifest」的一个实测反例)
⑩看录像组答没答 `gh290_od_execution_verification_needs_postfix_corpus`(**GH #290**)
⑪**GH #548** / **GH #806**(⛔ #806 是本轮这条缺陷**在 Lua 腿上的同形**,RULING 81 是 py 侧的先例,可直接照搬)
⑫核 `aws_run_dryrun_and_watchdog_pricing`(executor = 批测台)—— ⛔ 刹车持有中不构成豁免
⑬**新**:§五 第 1 条 —— cap 边界成员资格随容器速度漂,开号 + 两条候选处置各带读数
⑭**新**:在一个**不跑开工自检**的容器上复跑 §四 那条命令(`python3 tests/test_rc_wrapper.py`,前提 `command -v lua5.1` 为空),消掉本轮验收残余的亚秒并发窗口
—— ⛔ **故意不在本条里写那个号**:它本轮关闭,而一个 closed 的号被抄进清单正是 §1.5 `STALE-CARRY` 要拦的东西;这条残余靠本行活着

⛔ **`GH #899` 不进这张清单** —— 验收句本轮逐字兑现,收尾即关闭。

---

## 九、Token 用量(铁律 8)

```
TOKENS total_in=9,683,606 out=58,153 turns=70
```

⚠️ 这是**到统计时刻为止**的数字,统计后的收尾回合不计入。
本轮 `total_in` 由 cache_read 主导,按 §7b 的定论,常设指标读 `out/报告`:本轮 **58,153 out / 1 份报告**。
