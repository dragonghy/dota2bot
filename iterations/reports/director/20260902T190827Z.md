# director 2026-09-02T19:08Z — 同一条裁定第二次掉棒,而这一次它是从「已经投递到位」的字段里掉的

**一句话**:GH #413 在 W39 收割轮**第二次**被漏掉 —— 上一轮按章程 2.5 把它补投递进了
`batch-desk.md` 的收割清单、就在**同一轮被遵守了的 GH #412 那条下面一行**,它**还是掉了**;
本轮的产出不是第三次投递,是**找出 2.5 不够用的那一半并把它补上**:
常驻义务需要一个**机器读、非波次局部、每组每轮都跑**的家,全仓只有一个那样的面。

本轮**零 AWS 调用、零支出**;`bots/` 与 `game/` **逐字节零 diff**(改动只在
`tools/agent/` / `tests/` / `iterations/`)。

---

## 0 开工自检(铁律 10)

⚠️ **纪律 3 第二十二发,§22 守卫第十七次上场并第十七次拦下** —— 本轮第一条命令又是
`| tail -60`,当场吃到 `REFUSED: ... exit 2, nothing checked`。**照例登记**:
harness 同一行回报的 `SELFCHECK_EXIT=0` 正是守卫拒绝的那个东西(reader 的退出码)。
重定向重跑拿到裸读:

```
EXIT=3 ; legs run: 8 ; FINDINGS: cadence trunk-red(python) ; UNCERTIFIABLE: none
lua 腿:73 tagged detector file(s), 0 failures
```

- python 红 = `tests/test_detector_source_constants.py`(**GH #410**,录像组归属,见 §3)。
- **lua 腿 0 failures** —— 上一轮总监修的 GH #394 保持绿。
- `stable_anchors` / `ORPHAN_PROPOSAL` 见完整日志;`ORPHAN_PROPOSAL: none`(第十二轮)。

⚠️ **本轮的并发,据实登记(与上一轮同因,但这次的对象更贵)**:自检 lua 腿在跑时,
我**编辑了 `tools/agent/routine_selfcheck.sh` 本身**(加第 9 条腿)。bash 是边读边执行的,
这次那一跑还是走完并打出了自洽的汇总(`legs run: 8` —— 也就是说**它跑的是加腿之前的那份**),
但**这一跑不能被称作"对当前树的自检"**。收尾干净重跑分开登记(§6)。
**下次:改自检脚本前先等自检跑完。**

## 1 ⭐⭐⭐ 本轮主产出:GH #413 第二次掉棒的归因,以及它落地成一条腿

### 1.1 现场

| 轮次 | 那时 #413 投递在哪 | 结果 |
|---|---|---|
| 10:16Z | GH #413 追评 + `test_set.md §DO` | 裁定成立 |
| **W38 收割 12:15Z** | 同上(未落到批测台读的字段) | **掉棒**;当轮归因 = 章程 2.5 第四例 |
| 13:xxZ | 补投递进 `batch-desk.md` 收割清单,**紧挨 GH #412** | — |
| **W39 收割 18:30Z** | 收割清单里**有**它 | **又掉**(报告 §7 只在「存量」留了个编号) |

裸读佐证,不采信任何一方的叙述:`tools/batch_test/soak/rec_slot_baseline.json` 至今
**`"rec_slots": 1`**,`rec_slot_baseline_recslots1.json` **不存在** ⇒ 重建**一次都没跑过**。

### 1.2 ⭐ 归因:分界线不是「机器读不读」,是「那一轮真正被谁驱动」

W39 收割轮报告 §8 是一张逐条核对表:`W39_wave.json:harvest_obligations` **十二条,条条有答**。
**GH #412 是那张表的第 10 条** —— 它被遵守,**不是因为它在 `batch-desk.md` 里,是因为它在那张表里**。
#413 不在,而且**进不去**:`harvest_obligations` 是**发波当时**写的**波次局部**清单,
一条**常驻义务**没有行可以被写进去。于是批测台对着十二条自审、**十二条全中、答案是对的**,
第十三条**没有地方可以缺席**。

> 与 GH #332 立案句同型(`W28_wave.json:skip_not_pass_lines` 列了七条「没用到的开关」,
> 那张清单里没有第八条的位置)。**一张看起来完整的清单,正是一根掉了的常驻棒的样子。**

⇒ **章程 2.5 成立但不完备**,补一句:**投递的终点必须是那一轮真正被驱动的那张表;
波次局部的表接不住常驻义务。**(已写进章程 2.5 与 `test_set.md §DR`。)

### 1.3 落地(不是第四份副本)

- **登记表** `iterations/owed_executions.json`:`id` / `issue` / `executor` / `trigger` /
  `ruled_at` / `delivered_to` / `missed` / **`done_when`**。
- **读它的** `tools/agent/pending_rulings.py --owed-only` —— 同一把工具的**第三端**
  (一端没人裁的请求、一端没有行的提议、这一端**裁了没人做**)。
- **自检第 9 条腿 `owed-executions`**,**自己一条腿**不并进 `queue-rulings`:
  GH #267 4b 的立法句就是汇总通道必须说出**是哪条腿**。
- **三态 DONE / OWED / UNCERTIFIABLE**,失效方向选在贵的一侧:
  **读不到的 `done_when` 永远不许读成「已执行」**(GH #171)。

本轮首读(裸退出码 **3**):

```
OWED      recslot8_baseline      GH #413    executor=batch-desk
    trigger: the next harvest round   ruled_at: 2026-09-02T10:16Z
    done_when: tools/batch_test/soak/rec_slot_baseline.json:rec_slots = 1
               (the ruling's acceptance criterion is 8)
    already missed 2 round(s): W38 harvest 12:15Z / W39 harvest 18:30Z
```

### 1.4 它买不到的三件(引用时照抄)

1. **没登记的裁定它看不见**(表是手写的,不从任何东西推导)。登记那一行**本身就是被要求的动作**。
2. **一行的价值上限是它的 `done_when`**。`kind: manual` 诚实可用但只买到一句提醒(报成 OWED 并写在行上);
   指错文件的 `done_when` 会在棒还在地上时读出 DONE,**仓库里没有测试分得出来**。
3. **判产物不判工作**:问的是提交的 profile 头是不是 `rec_slots: 8`,**问不了**语料是不是 W37+W38。

### 1.5 验收

- `tests/test_pending_rulings.py` **不变量 4**,钉在它自己的立案现场:`rec_slots: 1` 的 profile
  + 该裁定自己写下的验收判据 ⇒ 必须出 OWED **且点名 executor**。**129 检查 / 0 失败**(裸退出码 0)。
- 变异台 `mutstand_pending_rulings.sh` 扩到 **M1–M13**:**13/13 CAUGHT,各报各的缺陷**
  (M10 读不到读成 DONE / M11 不再变红 / M12 不点名 executor / M13 manual 读成 DONE),
  **每次从文件副本复原,复原后基线绿**(`baseline after restore: exit 0 :: 129 checks, 0 failed`)。
- **不断言实树上 owed 为零** —— 那正是这张表存在的理由;让每个组的测试因为总监的待办变红,
  是让检测器停止被读的老路(不变量 3 同一条理由)。报它的是自检那条 exit 3 的腿。
- `bash -n tools/agent/routine_selfcheck.sh` ⇒ 0。

### 1.6 投递(§2.5,四处)

`test_set.md §DR` 全文档案 / `batch-desk.md` 收割清单那一条**补一段说明这条腿会替它举手** /
`state.json:owed_execution_leg_20260902` / GH #413 追评。**GH #413 仍不关闭**,
状态「已裁,待执行」,执行方批测台,触发点下一个收割轮。

## 2 §DO 那一棒的核对(上一轮「下次触发 ②」)

**核了,结论是掉了第二次**,详见 §1。上一轮的自纠(把它写进收割清单)**已落地且可见**
(`batch-desk.md:414` 附近),**但没有起作用** —— 这次不是投递没做,是投递的**终点选错了层**。

## 3 GH #410 第二次催(上一轮「下次触发 ①」)

录像组 16:02Z 报告**第三次**把这条红登记进自己的自检节(§一,逐字点名
`tests/test_detector_source_constants.py`)、**第三次没动它**。形状与 #410 成本说明所述完全一致:
trunk 红是五个流每 2h 各付一次的钱。**按设计不代猜**(那个 `0.40` 的类别只有常量作者答得了)。
已追评第二次。**距铁律 9 的 12 轮线还有约 5 轮**,下轮继续计数。

## 4 未裁的两行(下轮第一优先)

自检 `queue-rulings` 腿:`strategy-37`(`roshdist`)、`strategy-38`(`ckpush`)**均未裁**
(工具按文本判为 OTHER,故 §BB.4 的当轮死线不咬,但路由裁定仍然欠着)。
`ckpush` 的 §DQ.2 是**本档案第一条「形状与域价钱指向相反」的杠杆**,
**入集与否本身就是一个判断,不能顺带做** —— 本轮不半做,列为下轮第一件事。
`roshdist` 今晨已随代码落地而它的行仍 `ruled=False`,属 §CH 同族(裁定到了别处),一并补。

## 5 成本

**零 AWS 调用、零支出**。九月 MTD **照抄**批测台 18:30Z 自报 **`$3.637`**(免费 `budgets` 通道,
快照 `2026-09-01T17:58:37Z`,**连续第五轮同一张快照 ⇒ 不携带新围栏信息**)。**不写「顺延」。**
围栏 $60 / 刹车 $90 / 批准线 $100 **未动**;批测台自算含 W37/W38/补跑机/W39 的围栏算术
**~$5.6 ≤ $80**,未跨任一 owner 可见告警档。

## 6 铁律 6

- 静态半 `bash tools/agent/luacheck_gate.sh` ⇒ **`GATE_EXIT=0` CLEAN / `0 warnings`**
  (容器冷启,gate 自己装 luacheck;apt 包名 `lua-check`)。**未用 `RULE6_BYPASS` ⇒ 本轮无「SKIPPED, not passed」行。**
- `arm_push_gate.sh` ⇒ exit 0(`core.hooksPath=.githooks` 已上膛)。
- **`bots/` + `game/` 逐字节零 diff ⇒ 全量 Lua 套件未跑,也不声称**(GH #124)。
- 定向重跑:`python3 tests/test_pending_rulings.py` ⇒ **裸 exit 0 / 129 检查 0 失败**;
  `python3 tools/agent/pending_rulings.py --owed-only` ⇒ **裸 exit 3**(这是它该有的读数)。
- **收尾干净重跑自检**(§0 那一跑与我的编辑并发,不算数):见文末登记。

## 7 交棒

| # | 给谁 | 事 |
|---|---|---|
| ① ⭐⭐⭐ | **批测台** | **GH #413 / §DO 的 baseline 重建**,下一个收割轮执行。现在自检每轮会打 `OWED ... executor=batch-desk`;做完请在报告里说一声,总监按 `done_when` 转 DONE 并退役那一行。 |
| ② ⭐⭐ | **录像组** | **GH #410 第二次催**(第三轮被看见没被动)。`illumove_pairs:HP_CUT` 的类别只有常量作者答得了。 |
| ③ ⭐⭐ | **全体** | 有**常驻义务**(不属于任何一波、不属于任何一个 id)要交给别的组时,除了投递到对方的字段,**往 `iterations/owed_executions.json` 加一行**,写一个能裸读的 `done_when`。 |
| ④ ⭐ | 总监(自己) | 下轮:裁 `strategy-38`/`ckpush`(§DQ.2 方向问题)与 `strategy-37`;GH #423 / #409;§3.3 四个重文件;GH #285 第二十八轮;patch 缺口 P3。 |

## 8 本轮明说没做的

`strategy-38`/`strategy-37` 未裁;**GH #423** 未跟进;**GH #409** 未修;
**§3.3 四个重文件连续第十一轮没看有没有人接**;`hero_pool.txt` 成员资格未接进 §DH.4 域价;
**patch 缺口 P1/P2/P3(P3 优先)**;`strategy-5b` 第十九轮未裁;**GH #395 三问未裁**;
**GH #285 第二十八轮未裁**;`#252`/`#256`/`#282`/`#295` 关闭建议未发;**`§24` 未并入 `#229`**;
**`§CE` 已挂三十一轮**;backlog §19/§20/§21 未动;`#348`/`#326`/`#324`/`#328`/`#329`/`#321`/`#271`;
§CY 三条 + §CV 两条限定待收割;**GH #384 的自检腿序那半**;**GH #412 仍只做一半、未关闭**;
新开的 **#432**(重名副本副产物)/ **#427** / **#429** / **#408 第七次被容量咬、仍零评论未裁**。

`DECISIONS_NEEDED.md` 无新增;本周邮件 08-31T01:20Z 已发 ⇒ **本轮无邮件**;
今日周三,效率台账(仅周日)跳过。四组均有产出无空转(W39 已收割,本轮批测台按闸 (i) 正确不发波)。

## 9 Token 用量

见下方。
