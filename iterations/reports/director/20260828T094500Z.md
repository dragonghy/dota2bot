# 总监 2026-08-28T09:45Z — GH #267 §4 的 (b) 与 (c) 双双落地,而落地过程本身抓到了同一个文件里的第二个 22 小时红

**一句话**:上一轮已裁未落地的第①(#267 §4b 退出码来源分列)落地了,**并且它在自己的验收里立刻多点了一个源**;
顺带做的 (c) 拆分打开那个文件时,发现 `test_activemode_world_assertion.lua` **此刻正红在 trunk 上**——
不是 #267 修好的那条 census 断言,是**同一文件里的另一条 corpus 钉**,由 replay-check 07:05Z 的两个 fixture 推动,
**已经红了约 2.5 小时而没有任何一条腿能看见它**。**零 AWS 调用、零支出;`bots/`/`game/` 一行未改;`queue.json` 一字未动。**

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` → **worst exit 3**。

| 腿 | 读数 |
|---|---|
| push gate 上膛 | ok(`core.hooksPath=.githooks`) |
| unlanded work | **OK**,可核验窗口内无未落地工作(certifiable refs 47 / REFUSED 441,shallow) |
| cadence | **2 findings**:`replay-check` 6.1h(同一历史窗口**第九轮**,不升级)、`strategy` 8.5h |
| 队列未裁请求 | RIDESHARE none / OTHER none;total open 37 |
| `stable-v1`/`stable-v2` | 两个全 **ok**(EXISTS / PINNED / SHIPPED) |
| trunk python | 47 passed 0 failed(**落地后 48**) |
| 快 Lua 检测器 | 16 files 0 failures(**落地后 17**) |

⚠️ **本轮开工时这一行是「exit 3 全部来自 cadence」——而它是错的**,见 §2。

---

## 1. ① GH #267 §4(b) 落地:退出码按来源分列

**已裁于上一轮(06:5xZ),本轮只欠落地。** 实现:

- 新文件 `tools/agent/selfcheck_tally.sh`(可 source),把 `worst` 累加器换成
  **「谁把它抬上去的」也一并记录**;`routine_selfcheck.sh` 的 `note()` 保名保签名(**7 个调用点一个没改**),
  每条腿前加一句 `sc_leg '<名字>'`,收尾 `sc_report`。
- **`selfcheck worst exit: %d` 那一行逐字节不动**(各组 grep 它;测试断言这一点)。

**为什么抽成单独文件而不是就地写**:验收要同时制造两个源,而整个自检 ~20s 且要联网。
抽出来后测试直接 source 它,零成本。这也是 `ensure_lua_toolchain.sh`(GH #205)走过的同一步。

**承重断言不是「分列打印出来了」**,而是 **test 3:cadence 与 trunk-red 同时红时两个都要点名**——
那正是 08-27 的形状,也是唯一一条「装饰性实现」过不了的检查(只打第一个 / 只打最后一个 / 只打 `worst` 自己那条腿,
其余每一条都能过)。第二条承重:**test 6,量具不许悄悄自相矛盾**——
有人绕过 `sc_leg`/`sc_note` 抬 `worst`,收尾打 `ATTRIBUTION BROKEN`,而不是让
`FINDINGS: none` 和 `worst exit: 3` 并排站着装权威(**那就是原缺陷穿上修复的衣服**)。

**验收**:`tests/test_selfcheck_exit_attribution.py` **26 检查 0 失败**,行为式(真的 source 并驱动)。
**变异 7/7 全红**、控制绿、还原后逐字节相同,变异脚本带「没落上就判失败」守卫:

| 变异 | 结果 |
|---|---|
| M1 只保留第一个源 | RED |
| M2 去掉 ATTRIBUTION BROKEN | RED |
| M3 把 level 2 记成 finding | RED |
| M4 `worst` 永不抬升 | RED |
| M5 wrapper 不调 `sc_report` | RED |
| M6 wrapper 少一句 `sc_leg` | RED |
| M7 分列打在 summary 之后 | RED |

### 1.1 ⭐ 它在自己的验收里立刻多点了一个源 —— 而那个源是我自己

落地后第一次跑全自检,分列打出 **`FINDINGS: cadence trunk-red(python)`**。
`trunk-red(python)` 是 `tests/test_selfcheck_lua_leg.py` 红了,**祸首是本轮改动**:
该测试把 wrapper 的一段源码切片出来单跑,并**自己手搓一份 `worst=0` + `note()`**;
累加器变成真文件后,那份**镜像**对不上了,于是两个端到端用例红在 harness 上而不是红在腿上。

修法**不是把镜像改新**,是**让它 source 同一个文件**——该测试自己的记录里就有这条
(用例 2a2:「a test that mirrors the thing it checks is checking the mirror」),
只是当时说的是发现块,这次轮到累加器。**并核了它没被改空**:把腿里的 `note 3` 改成 `note 0`,
5g 照旧红(exit 3 → 0),说明端到端用例仍在真的量东西。

---

## 2. ⭐ (c) 的拆分打开那个文件,发现它此刻正红在 trunk 上

裁定 (c) 是「**只以拆分的形式采纳**」。做拆分要先打开
`tests/test_activemode_world_assertion.lua` —— 它**当场是红的**:

```
tests/test_activemode_world_assertion.lua:397:
J.IsInTeamFight(bot,1500) read TRUE on 93/993 hero-frames (89 when pinned)
```

**不是 #267 修好的那条 census 断言**,是同一文件里的 **corpus 钉**。
**这是 #267 的立案形状在同一个文件里的第二发**,而且**新装的分列也点不到它**——
因为那条腿按 tag 发现文件,而这个文件**没有 tag,也不能有**(它把整个 fixture 档案加载两遍)。
**(c) 因此不是顺带做的收尾,是这一半的正解。**

**归因是量出来的不是猜的**:把两个 fixture 各自拿掉重跑,

| 拿掉 | in_teamfight | hero-frames |
|---|---|---|
| 两个都拿掉 | 89 | 974(= 原钉,**14 tests 0 failures**) |
| 只留 `_785` | 92 | 983 |
| 只留 `_790` | 90 | 984 |
| 两个都在 | **93** | **993** |

⇒ `_785` 贡献 +3/+9,`_790` 贡献 +1/+10,**两者可加**(89+3+1=93)。
hero-frames 是 +19 不是 +20:**`_785` 帧上 lina 是死的**(`hp = 0, alive = false`),
而该 sweep 只数 `u.alive` —— 与该文件既有的记账体例逐条对上。
祸首是 **replay-check 07:05Z(`b50a7727`)的两个 GH #271 venomancer fixture**,
**世界断言本身没动**(`mode_nonzero` 仍是 0,filter 仍被忽略),**动的只有分母**。
按该文件的体例**重新取钉 89 → 93 并把上面这段记账写进去**。

### 2.1 拆分怎么做的

新文件 `tests/test_activemode_call_site_census.lua`,带 **`[ratchet]`** 标签,于是自检快腿**发现得到**(16 → 17 个文件)。
**扫描器是 MOVE 不是 COPY**:`strip_line_comment` / `scan_call_sites` 现在**只在新文件里**,
老文件留引用它们数字的散文 + 一句指路。**留两份就是上面 §1.1 刚踩过的那个镜像陷阱**,
而且是踩在**「读得过宽」这件事本身开出 #267 的那把量具**上。

### 2.2 ⚠️ 价钱不是裁定里引的 0.144s —— 我改了那个数

裁定写的是「census 那部分单独跑 **0.144s**」。**那是扫描本身的开销。**
自检实际付的是**发现集合多一个成员**的边际成本,而**每个成员都要各付一次 runner 启动**
(glob 218 个测试文件 + 加载 mock Bot API)。**本容器实测**,用真实发现循环跑带/不带:

| 集合 | 用时 |
|---|---|
| 快腿 16 文件 | 8.86s |
| 快腿 17 文件 | 10.34s |
| **边际成本** | **1.48s** |
| (若改为整文件打 tag) | 4.1s(冷 5.7s) |

⇒ **拆分仍然是对的**(约整文件的三分之一,对 ~20s 的自检),**但它是 1.5s 不是 0.14s**。
按铁律 4(iii)(登记效应量要连切法一起登记)**把实测和切法一起写进新文件的头**,
并留话给下一个要加 tag 的人:**按这个数做预算,不要引上一轮的数字**。
该文件自己的长注释本来就写着「加标签前先计时」——**这一次是照做的结果与被引的数不一致**。

---

## 3. 验证(铁律 6)

| 项 | 读数 |
|---|---|
| `luacheck_gate.sh` | **exit 0 / 0 警告**(自己装了 luacheck;**未用 `RULE6_BYPASS`**) |
| `run_py_tests.sh` | **48 passed, 0 failed, 0 uncertifiable**(落地前 47) |
| 快 Lua 检测器全集 | **17 files, 0 red** |
| `activemode_world_assertion` | 12 tests 0 failures(census 两条搬走后) |
| `activemode_call_site_census` | 2 tests 0 failures |
| `smoke_load` / `corpus_scale` | 3/0、8/0 |
| 变异 | **7/7 红**,控制绿,还原后逐字节相同 |

**全量 Lua 套件未跑**(~100min,GH #124):`bots/`/`game/` 一行未改 ⇒ **不跑,也不声称它绿**。

落地后自检:`FINDINGS (exit 3): cadence` / `UNCERTIFIABLE: none` ——
**「exit 3 全部来自 cadence」从本轮起是读数,不是推断。**

---

## 4. #267 的状态

**保持 open。** (b) 与 (c) 均已落地,但 #267 §3 那一半(census 口径)是上一轮结的,
而本轮新暴露的 §2 那个 corpus 钉说明**同一个文件还会再红**——
真正的验收量是**下一轮有别的组在报告里贴出带 `FINDINGS:` 分列的那一行**,
以及**快腿 17 文件里出现 census 红时有人当轮看见**。两者都要等下一轮别人的开工自检,
**总监不自己关它**。

---

## 5. 体系健康 / 成本 / 沟通

- **五组均有产出**,无空转。CADENCE:`replay-check` 6.1h 是**同一历史窗口第九轮**(不升级);
  `strategy` 8.5h(01:24Z → now)本轮**首次**出现,**列为观察项**,下一轮若仍在则升级。
- **owner 优先项**未触 12 轮红线。
- **成本**:总监**零 AWS 调用、零支出**。批测台 08:48Z 自报 **MTD $59.749**(CE 逐位一致,连续第十五轮),
  围栏 **$61.543** ≤ 闸 (iii) **$80**(余量 $18.46);**刹车 $90 / 批准线 $100 未触及**。本轮未发波。
- **`DECISIONS_NEEDED` 无新增**(本轮两项均在自主授权内:(b)/(c) 都是上一轮已裁,trunk 红按章程 2(a) 归总监)。
- **W35 周日(08-30)邮件未发**(今日周五)。

---

## 6. 下次触发

① **GH #271**(值一个完整工作单元,已顺延一轮)
② **GH #266**(已顺延三轮)
③ **W20 早发 3分31秒的裁定**(批测台 06:17Z 自报,已欠两轮)
④ 盯 #269 的验收(W19-R 零可用种子 ⇒ 仍未买到)
⑤ 盯 #267 的验收(别组报告里出现 `FINDINGS:` 分列那一行)
⑥ #204 的 n=8(第十六轮) ⑦ 批测台交棒⑤/⑥/⑦/⑩ ⑧ `[bug] #249` ⑨ #236 甲 ⑩ #240
⑪ #207(第十六轮) ⑫ #218 后续 ⑬ #217(第十五轮) ⑭ #211(第十六轮) ⑮ backlog §18
⑯ §16/§17 ⑰ §BL.4(i) ⑱ #75(第十八轮) ⑲ **W35 周日邮件** ⑳ `aimguard` 的 (a)
㉑ `bbrespawn` 的 `readmit_on` ㉒ 盯 #257 的验收 1
㉓ **`strategy` cadence 8.5h 若复现则升级**

---

## 7. Token 用量(铁律 8)

```
TOKENS total_in=5,769,207 out=37,195 turns=50
```
(统计时刻为止;之后的收尾回合不计入。)
