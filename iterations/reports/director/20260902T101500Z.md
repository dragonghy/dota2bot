# 总监 2026-09-02T10:15Z —— 两条裁定,和一条**在同一天、同一族改动里第二次塌掉的「关闸即出厂」**

**本轮零 AWS 调用、零支出、零 EC2、零 S3。`bots/` 与 `game/` 逐字节零 diff** —— 本轮唯一的代码改动在 `tests/`。

---

## ⓪ 开工自检(铁律 10)

**纪律 3 第十九发,§22 守卫第十四次上场并第十四次拦下**:本轮第一条命令又是
`routine_selfcheck.sh 2>&1 | tail -40`,守卫拒答并打
`REFUSED: stdout is a PIPE ... SELFCHECK_EXIT=2 REFUSED (nothing was checked; this is NOT a pass)`。
改成重定向后裸读:

```
EXIT=3
legs run              : 8
FINDINGS (exit 3)     : cadence trunk-red(python) trunk-red(lua)
UNCERTIFIABLE (exit 2): none
ORPHAN_PROPOSAL: none
selfcheck worst exit: 3
```

- **lua trunk 红 2 个文件**:`test_incoming_damage_callsite_census.lua`(GH #394,**第八次复现**,非总监归属)
  与 **`test_corpus_scale.lua`(新,本轮由总监修掉,见 ②)**。
- **python trunk 红**与上一轮同(GH #410 录像组);`UNCERTIFIABLE: none`。
- ⚠️ **并发披露**:自检的 lua 腿在我编辑之前就已打出 `RED test_corpus_scale.lua` 那几行
  (我是读到它之后才动手的)⇒ **那条读数是修前的、干净的**。cadence 与 python 腿同样在编辑前跑完。

---

## ① GH #415 `slotpush` 入集裁定:**ADMITTED / ROUTED_RIDESHARE,搭 W39**

成员串 **54 → 55**(485 字节,md5 `bfe60fcdfbe466440213541c8f37a254`)。全文档案 **`test_set.md §DN`**;
`queue.json` 的 `strategy-36.director` 已填(**投递纪律 §2.5:裁定落在被裁方读的那个字段上**);
`state.json:slotpush_admit_20260902`。

**⚠️ 搭 W39 不是 W38** —— W38 已于 09:31Z 起飞并 clone 了 54-id arm。
这是**连着第三次**裁定落在一波起飞之后(§DH/W36、§DK/W37、本轮/W38)。
三次同型就不再是巧合,是**排期节奏与裁定节奏的相位差**;不构成任何一方过错,
但下次想让入集赶上某一波,**必须赶在批测台发波窗口之前裁**。已登记进 §DN.1 与 state.json。

**总监独立复核六条,全部裸退出码,全部自己重跑而不是引用提议**:

| # | 检查 | 读数 |
|---|---|---|
| (i) | 单条独立门(`pullcad` 陷阱) | `jmz_func.lua:12264-12265` 合取项只有 `IsModeTurbo` + 一个候选 id ✅ |
| (ii) | 门关逐字节等于出厂 | 读全 diff:`____`→`i` 是 Lua 语义零变化;`nSlot` 与 cacheKey 后缀都在闸内 ✅ |
| (iii) | armed 零新引擎 API 面 | 同一个 `GetTeamMember`,实参从越界值移进 1..5 ✅ |
| (iv) | 域 = 共享代码 | `corpus_hero_census.py` ⇒ **SHARED / exit 0**(108 帧文件 / 43 英雄)✅ |
| (v) | 载体门 + 接线 | `carrier_terms.py --arm <55>` ⇒ `10 hero-scoped / 45 generic / 0 unresolved => 6 term(s)` **exit 0**,6 个 term 与 54 串**逐字不变**;`check_armed_wiring.py --cand <55>` ⇒ `all 55 armed ids wired on HEAD` **exit 0** ✅ |
| (vi) | 提议自己的验收 | `slotpush_highground_scan` ⇒ **16 tests, 0 failures, 裸 exit 0** ✅ |

**(vii) 第七条是本轮新加的,起因是 GH #418**:逐行读整份 `bots/` diff 找闸外改动。
`fcdaf53a` 动 9 个 `bots/` 文件,**逐行读过,闸外零行为改动**。七个 mode 换调用点
**不引入新的作用域要求**(旧调用本来就走 `J.Utils`,`J` 早在作用域内);
memo key 全仓只有一处定义(`utils.lua:1709`),无第二消费者。

**条件 (a) 的限定逐字继承提议、总监不放宽**:域价钱**在选杠杆之前**跑,答的是**「fixture 级买不到」**
(517 个 member-frame 里只有 4 个两合取项同真、全在一份 fixture 里;双侧 94 次载入两腿一次没分歧)
⇒ **必须从新局录像取**。**按铁律 4(i-a),dire / radiant 两个分层各自登记** ——
这条缺陷本身就是按侧不同的量(dire 够到 1/5,radiant 4/5),**池化是结构性错误不是精度问题**。

---

## ② ⭐ 本轮主要价值:**同一天、同一族里,「关闸即出厂」第二次塌掉,两次塌的都不是论证**

- **第一次(GH #418,批测台 06:21Z 抓)**:`slotdust` 落地把闸外那行 `<` 写成了 `<=`,
  上方注释「Unarmed this is byte-for-byte the shipped function」**假了六个小时**,
  而那正是下游并池判据所依赖的那句话。
- **第二次(本轮自检抓,总监修)**:**修 #418 的那一次提交**(`77d6c795`,07:42Z)在
  `tests/test_slotdust_dust_arbitration.lua:645` 写下 `assert(nFixtures == 107, ...)` ——
  **GH #106 / #127 原样复发**:下一份 fixture 落地就把它变红,而它测的东西一点没动。

**形状是同一个:承重的那句话是对的,而让它继续为真的那行代码没有人在看。**
第一次伤的是**行为**,第二次伤的是**量具**。
⇒ **§DN.4 立为法(全队)**:凡写下「关闸时逐字节等于出厂」的改动,
**验收必须含一次逐行读整份 `bots/` diff**,并在报告里写出「闸外零行为改动」及其依据。

**修法(只动 `tests/`)**:`cs.corpus` 管尺寸、`cs.ratchet` 管三个已填格
(都是对 fixture 的求和,append 只能变大;`armed[2]` 是**下界不是计数**)。
⛔ **`shipped[2] == nil` 保持等号,故意的** —— 按 `corpus_scale.lua` 自己的教义
「内容就是一个零的断言保持等号」:它承载的裁定是**一个域是空的**,
两载体的关闸帧一到货**必须变红**。**把它一起棘轮掉会删掉本轮的发现本身。**

**验收(裸退出码)**:`slotdust_dust_arbitration` **16/0 exit 0**、`corpus_scale` **8/0 exit 0**(修前 RED)。
**变异台 4/4 CAUGHT + 一条负控**,基线先证绿、复原后仍绿:

| 变异 | 读数 |
|---|---|
| M1 `shipped[0]` 掉 1 | **CAUGHT** `unarmed 0-carrier frames FELL to 100 (registered 101)` |
| M2 `armed[2] = nil` | **CAUGHT** `armed two-carrier frames FELL to 0 (registered 1)` |
| M3 `shipped[2] = 1` | **CAUGHT** `a frame with two reachable UNARMED carriers has arrived (1)` |
| M4 `nFixtures = 3` | **CAUGHT** `corpus SHRANK to 3 fixtures (floor 100)` |
| **负控** 语料长 5 份、三个计数同步变大 | **绿**(增长免费 —— 这正是修它的目的) |

⭐ **每条变异的报错都点到它自己那条缺陷,而不是撞上别人的诊断** ——
这是上一轮 §DM.4 那次自伤(结论对了而理由不是被测的那一个)的直接应用。

---

## ③ GH #413 重裁(连续两轮被顺延,本轮做掉):**采纳建议 2 的形状,否掉它的参数,而且不必新开一波**

全文档案 **`test_set.md §DO`**;`state.json:gh413_recslot_baseline_20260902`。

**先背书批测台**:总监读源码确认 `rec_slot_cost.py:176-181` 是一条**语料同质性检查**
(一份语料只许申报一个 `rec_slots`),而 baseline 头写着 `"rec_slots": 1, "rec_set": [1]`
⇒ §DA.2 的「把 W25(12)并进这份 profile」**按构造不可能,是设计不是语料**。
批测台**没把 exit 2 转成裁定、没绕过、没生产假读数** —— **三件都对,明确背书**。

**裁定(理由是算术不是偏好)**:
1. profile 自带 `rec_slots` ⇒ **12 的 baseline 去判跑在 8 上的波,
   就是把 `:180` 拒绝的那种不同质性挪到比较的两端之间**,与今天 `rec_slots=1` 判 8 录制者
   **是同一个毛病**,而那正是 §DA.3 诊断出的东西。⇒ **重建语料必须跑在被判的配置上:8,不是 12。**
2. **而跑在 8 上的语料不需要去 commission:它已经在收了。**
   W37(`--rec-slots 8`,已收 126 局)与 W38(09:31Z 起飞,`20260902T093000Z.md:98` 逐字写着 `--rec-slots 8`)
   **都是**。⇒ **零额外 EC2、零新采集配置、零波次代价** —— 比批测台三个选项里最便宜的那个还便宜。

**执行**:W38 收割后用 W37+W38 跑 `--emit-baseline`,产出 `"rec_slots": 8` 的新 profile;
旧文件 `git mv` 成 `rec_slot_baseline_recslots1.json` **保留不删**。
**否掉建议 1**(造承载混合语料的机器,而 `:180` 已正确执行同一条不变量;
**更硬的一条:混合比较器在同质 baseline 存在之前没法被验证** ⇒ 顺序上必须排在后面)。
**否掉建议 3**(它把 §DA.3 发现的缺陷改成「不看」,而 §DA.3 的全部内容就是 slot 1 那一格)。
**总监明令**:新 baseline 落地前**不跑**验收门 —— 拿另一个物理配置的 baseline 冒充,
是「事后改判据」的镜像;批测台本轮主动拒绝生产这种读数是**对的,继续保持**。
**#413 不关闭**,状态「已裁,待执行」,执行方批测台、触发点 W38 收割当轮;
**总监下一轮核对这一棒有没有掉**(铁律 9 连带规则)。

---

## ④ 顺带关掉一条挂了很久的 backlog:`state.json` round-trip

章程「下次触发」清单里 `state.json` round-trip 未修**已挂多轮**。本轮实测出了配方:
`json.dumps(d, ensure_ascii=False, indent=2) + '\n'`(配 `object_pairs_hook=OrderedDict`)
对当前文件**逐字节恒等**(先用 `indent=1` 写坏过一次,`git diff --stat` 报
**5228 insertions / 5204 deletions** 的整档重排,当场 `git checkout` 撤回、量出配方再写)。
按配方重写后本轮三个新键的 diff 是 **24 insertions / 0 deletions**。
⇒ **今后任何 `state.json` 的程序化写入照抄这三个参数**;写完**必看 `git diff --stat`**,
一个「只加了三个键」的改动打出四位数的 diff 行,那是格式漂移不是内容。

---

## ⑤ 铁律 6

- 静态半:`bash tools/agent/luacheck_gate.sh` **裸读 `GATE_EXIT=0` CLEAN / 0 警告**,
  容器初始无 luacheck,脚本自己装上(apt 包名 `lua-check`);**未用 `RULE6_BYPASS`**。
- `tools/agent/arm_push_gate.sh` **exit 0**(钩子已上膛)。
- 动态半:**`bots/` + `game/` 逐字节零 diff ⇒ 全量 Lua 套件未跑,也不声称**(GH #124)。
  本轮改动涉及的两个文件定向重跑,均裸退出码:16/0 与 8/0。

## ⑥ 成本

**本轮零 AWS / 零 S3 / 零 EC2 / 零 CE 调用。** 九月 MTD **照抄**批测台 09:30Z 自报的 **$3.637**。
**不写「顺延」。** 围栏 $60 / 刹车 $90 / 批准线 $100 未动。
⚠️ §DM 那条(补跑机走按需)本轮仍未有第一次实际适用 —— W38 起飞 4/4 干净、无补跑。

## ⑦ 体系健康

四组本轮均有产出无空转(batch-desk 06:15Z+09:30Z、hero 07:47Z、replay-check 06:54Z、strategy 07:42Z)。
`ORPHAN_PROPOSAL: none`(第十轮)。`DECISIONS_NEEDED.md` 无新增;
本周邮件 08-31T01:20Z 已发 ⇒ **本轮无邮件**;今日周三,效率台账(仅周日)跳过。

## ⑧ 本轮明说没做的

**GH #409**(`rotscope` 漏报载体)未修;**§3.3 四个重文件连续第八轮没看有没有人接**;
**`hero_pool.txt` 成员资格未接进 §DH.4 域价**;**patch 缺口 P1/P2/P3(P3 优先)**;
`strategy-5b` 第十六轮未裁;**GH #395 三问未裁**;**按需常数第七轮重裁未做**;
**GH #285 第二十五轮未裁**;`#252`/`#256`/`#282`/`#295` 关闭建议未发;
**`§24` 未并入 `#229`**;**`§CE` 已挂二十八轮**;backlog §19/§20/§21 未动;
`#348`/`#326`/`#324`/`#328`/`#329`/`#321`/`#271`;§CY 三条 + §CV 两条限定待收割;
**GH #384 的自检腿序那半**;GH #394(第八次复现)/ #410(均非总监归属);
**GH #412 仍只做一半、未关闭**。

## ⑨ 下次触发

① **核对 GH #413 那一棒有没有掉**(W38 收割当轮该执行 §DO 的 baseline 重建)
② **GH #409**(`rotscope` 漏报载体)
③ **§3.3 四个重文件有没有人接**,没接就自己修一个(连续第八轮写)
④ **GH #285 第二十五轮**(离咬人只剩一波)
⑤ **patch 缺口 P3**
⑥ `#252`/`#256`/`#282`/`#295` + `strategy-5b`、`§24` 并入 `#229`、`§CE` 第一步。

## ⑨.5 已发表的评论(回填 id;三份草稿均先过 `claim_precheck.sh` 裸读 **exit 0**「every citation resolves on origin/main」,且都在 `git push` **之后**发表 —— 铁律 6 的 ⭐2026-08-29 条 / GH #290)

| issue | 内容 | comment id |
|---|---|---|
| **#415** | `slotpush` 入集裁定(ADMITTED,54 → 55,搭 W39) | [`5508007847`](https://github.com/dragonghy/dota2bot/issues/415#issuecomment-5508007847) |
| **#413** | 重裁(采纳建议 2 的形状、否掉它的参数;不关闭,状态「已裁,待执行」) | [`5508014058`](https://github.com/dragonghy/dota2bot/issues/413#issuecomment-5508014058) |
| **#418** | 第二发登记 + §DN.4 立法(不重开) | [`5508020334`](https://github.com/dragonghy/dota2bot/issues/418#issuecomment-5508020334) |

## ⑩ token 用量

```
TOKENS total_in=6,291,293 out=52,500 turns=49
```
(统计时刻为止;之后的收尾回合不计入。)
