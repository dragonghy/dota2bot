# 总监 2026-09-16T10:15Z —— RULING 64:把「抄写」变成一条腿,第一次读真语料,4 个号里 2 个是死的

零 AWS、零波次、`bots/` + `game/` 零 diff、不发 owner 邮件。
成本(RULING 48 三段式):**零 EC2 / 零 CE / S3 读取 0 个对象(出网未计价)**。

---

## 一句话

上一轮(RULING 63)写下「一个已完成的名字在攒紧迫感,一件真未完的事在无人看见 —— 两者是同一次
抄写的两面」,并把验收产物登记成一行 owed。本轮把那条腿造出来、**第一次读真语料**:
『下次触发』清单的 4 个 `GH #<n>` 里 **2 个是 `closed`**,而**没人看见过的那一个**
(`GH #538`,closed 10.3 天)的**残余根本不在那个号里** —— 它写在 `GH #466` / `GH #240`,
**两条都 open、都归总监、分别搁了 13 天与 20 天**。
⇒ **立案句不是一个例子,是一个类,而且当场 50%。**

---

## 一、本轮做了什么(交付上一轮登记的那一行)

owed 行 `carry_item_issue_state_crossread`(RULING 63 登记,`executor` 写死总监自己,
`done_when = {path_exists, tools/agent/carry_item_issue_state.py}`)**本轮取棒并退休**。

| 产物 | 读数 |
|---|---|
| `tools/agent/carry_item_issue_state.py` | 只读、离线、零网络;三态 **0 干净 / 2 未核实 / 3 有发现**;`--selfcheck` **ALL PASS** |
| `tests/test_carry_item_issue_state.py` | **36 checks / 0 failures**(0.10s,三次实测 0.113 / 0.104 / 0.103s) |
| `tools/agent/mutstand_carry_item_issue_state.sh` | **10 CAUGHT / 0 SURVIVED / control_ok=1**,`RESTORE: YES` |
| `iterations/data/issue_state_snapshot.json` | issue 语料,**逐号 `issue_read` 点查**取数 |
| `iterations/streams/director.md §1.5` | 流程入口(取清单之前先跑它) |

**它判什么**:章程「当前状态」最新条目的『下次触发』段里,每个 `GH #<n>` 的 issue `state`。
`closed` ⇒ `STALE-CARRY`(exit 3);语料读不到 / 缺失 / 过期 / 状态串不认得 ⇒ `UNCERTIFIABLE`
(exit 2)**不是通过**;零匹配 ⇒ exit 2(anti-empty-match)。

**它不判什么(LIMIT,写在文件头)**:① 不判活干完了没有 —— 关闭的 issue 里可以躺着真未完的
第二件(`#523` 就是);② 分不开「作为待办抄写」与「作为档案引用」;③ 默认只看最新一条 entry。

---

## 二、第一次真语料读数(2026-09-16T10:0xZ)

```
corpus   : issue corpus iterations/data/issue_state_snapshot.json, fetched 2026-09-16T10:02:00Z (0.1h old), 4 issues
scanned  : 1 entry (of 65), 1 carry segment(s), 4 GH ref(s)
STALE-CARRY   GH #523 closed 2026-09-08T13:12:53Z  (7.7d before that entry) -- carried in『下次触发』of 2026-09-16T07:02Z
STALE-CARRY   GH #538 closed 2026-09-05T23:40:50Z  (10.3d before that entry) -- carried in『下次触发』of 2026-09-16T07:02Z
OK            GH #810, GH #528
VERDICT  : FINDINGS (exit 3)
```

* `GH #523` —— 上一轮已知的那一条(立案现场)。
* ⭐ **`GH #538` —— 十一天里没有任何人看见过它。** 而且它**只在链式写法算两个号之后才读得出来**:
  清单第 ⑧ 条逐字是 `GH #538 / #528`,**只认 `GH #` 的第一版当场漏掉 `#528`**(变异 M7 钉的就是这一发)。

### 处置:⛔ 不许只划掉名字

按本腿自己的规矩二选一。逐条:

| 号 | 状态 | 残余在哪 | 处置 |
|---|---|---|---|
| `#523` | closed 09-08 | 第二件事(FORWARD-REF)**已由 RULING 63 落地** | **划掉** |
| `#538` | closed 09-05 | **不在那个号里** —— 正文自己写着族属 `GH #466` / `GH #240` | **清单里用两个活号替换死号** |

`GH #466`(open,**13 天没人动**)正文逐字在**等总监二选一**:维持它本轮加的豁免表形状
(按行内容索引 + 必带理由 + 死条目变红),还是改成 census 上下文判据 (a)。
`GH #240`(open,**20 天**)是同族的第二面(检测器只认得「等于 fixture 数」那一个量)。
⇒ **只划掉 `#538` 会把这两条一起埋掉,那正是 RULING 63 立案句的另一半第二次发生。**

**改完之后同一条腿的收尾读数**:`4 GH ref(s)` 全 open、**`RC_EXIT=0`**。

---

## 三、⭐⭐ 两个缺陷是这条腿在真语料上撞出来的,不是读代码读出来的

**照登,因为两者的失效方向都是「印出来的读数长得和正确的一模一样」。**

**(甲) entry 时刻的分钟位在本仓有一半写作 `x`。**
本仓散文时刻惯例是 `**2026-09-16T10:1xZ**`(owed 行的 `ruled_at` 也这么写),而第一版 `ENTRY_RE`
只认纯数字 ⇒ **落地当轮自己写的新条目被静默跳过,工具转头去读上一轮的清单**,
读数逐字是 `scanned: 1 entry (of 65)` —— **和一次正确的运行印出来一模一样**。
失效方向:**对着一张已经被取代的清单报干净**。(变异 M9)

**(乙) 段落起点取第一次出现的『下次触发』。**
entry 的**正文**经常要谈论这件事本身(本轮这条 entry 就写着「对『下次触发』段里每个
`GH #<n>` 交叉读」)⇒ 整段叙事进作用域,**当场造出三个假阳**(`#523` / `#538` / `#276`)。
改取**最后一次**(清单按惯例是 entry 的尾巴)。(变异 M10)
📌 `GH #276`(「稳定假阳 = 没人再读这个探测器」)**本人作为假阳出现**,是巧合,也是佐证。

**(丙) 第三发是措辞,不是代码。** 第一版清单把「替换已死的 `GH #538`」写在 ⑦ 里,
那条腿**照报不误** —— 它分不开「作为待办抄写」与「作为墓碑引用」(**LIMIT 2 逐字写着这条边界**)
⇒ 规矩:**划掉的名字一律写在清单之外,一个字都不留在清单里**。

---

## 四、变异台(10 发,按失效方向分两组)

```
CONTROL ok      : a pure comment edit is NOT caught
CAUGHT x10      : M1..M10
RESTORE         : YES -- byte-identical to the index
SUMMARY         : 10 CAUGHT / 0 SURVIVED / control_ok=1      RC_EXIT=0
```

* **ACCUSE(代价 = 把一条活着的待办划掉)**:M1 陈旧语料照答 / M3 语料里没有读成 closed /
  M4 作用域放宽到整条 entry / M8 裸 `#<n>` 全收 / M10 段落取第一次出现。
  与 §2e / §2e-bis 那句**「永远凭空造出停摆,永不掩盖真停摆」**是同一条纪律,
  代价落在被误判的那一方身上。
* **MISS(代价 = 退回 RULING 63 之前那个世界,上面顶一个绿工具)**:M2 语料里没有读成 open /
  M5 零匹配退 0 / M7 链式号丢掉 / M9 `T10:1xZ` 条目被跳过。
* ⭐ **M6 一个退出码都不动**:它只让 finding 不再否认「关闭 = 做完」,而**那正是 `#523` 的形状** ⇒
  **只有印出来的那句话能抓住它**(与 `mutstand_citation_forward_ref.sh` 的 M5 同型)。
* ⛔ **照登:M4 第一版 `ANCHOR MISS`** —— 我改了被它锚的那一行却没改锚点,
  **台子自己拒绝把没落地的变异记成 CAUGHT**(同一处护栏第二次承重,上一次是 RULING 62 的 M6)。

---

## 五、语料的取数纪律(RULING 55 的直接后果)

`iterations/data/issue_state_snapshot.json` 由**逐号 `issue_read(method=get)` 点查**生成,
⛔ **不用 `list_*`** —— RULING 55 实测列表/评论读数可滞后 **8.8 分钟**,而点查不会。

滞后方向都在安全侧:刚关的号还读作 open ⇒ 这条腿少说一句话;刚开的号不在语料里 ⇒
`UNCERTIFIABLE`。**唯一危险方向**(**重开**的号被说成 closed)由 `--max-age-hours`(默认 48)挡住:
语料过期 ⇒ 整轮降级、**不出 finding**。棘轮 claim 4c 同时钉了**两侧** —— 过期不答,
放宽 `--max-age-hours` 后同一份语料**照答**,所以那道闸是闸,不是常年拒绝。

⛔ **没有接进 `routine_selfcheck.sh`,这是决定不是遗漏**:清单是总监自己那一节,而语料会过期
⇒ 接进全队自检等于让另外四个组每轮吃一个与它们无关的 exit 2,
**那正是 `RULE6_BYPASS` 变成常规路径的那条路**(GH #707 / #669)。入口写进章程 §1.5。

---

## 六、巡检(§2e 含 (丁))

`git fetch origin main` 之后、按名字序、从 `origin/main` 取、只认报告名形状。**取数时刻 10:10:03Z**:

| 组 | 最新报告 | 距今 |
|---|---|---|
| batch-desk | `20260916T091231Z.md` | 1.0h |
| replay-check | `20260916T094853Z.md` | 0.4h |
| strategy | `20260916T072643Z.md` | 2.7h |
| hero | `20260916T081500Z.md` | 1.9h |
| director | `20260916T070202Z.md` | 3.1h |

⇒ **五组均在 3.1h 内有产出,无停摆,不点名任何组。**

自检的 `GAP cadence hero 09-15T22:00Z -> 09-16T02:10Z (4.2h)` 按 **§2e-bis** 交叉读 `unlanded`
(`OK: no unlanded work in the certifiable window.`),且巡检读到英雄组 1.9h 前有报告
⇒ **窗口内历史洞,非活停摆,不点名**(连续第三轮同一读数)。

**(戊)**:本轮**不含任何 git 历史的日期推断** —— 两个 `closed_at` 都来自 GitHub 点查,不是 `git log`
⇒ 未 deepen。照登本容器 `.git/shallow` 存在、`git rev-list --count origin/main` = **51**。

---

## 七、开工自检(铁律 10)与本轮自己栽的两处

**真码 `RC_EXIT=3`**,`legs run 13`,
`FINDINGS (exit 3): cadence queue-rulings owed-executions lua-coverage`,
`UNCERTIFIABLE (exit 2): trunk-red(python)`,
`NOT RUN (inside a leg): tests/test_lua_gate.py tests/test_luacheck_gate_soakswitch.py tests/test_selfcheck_lua_leg.py`。
Lua 腿:**120 tagged detector file(s), 0 failures**(自陈 FAST SUBSET,不是全量套件)。

⚠️ **纪律 3 第三十五发,连续第五轮同一发**:本轮第一条命令仍然写成
`bash tools/agent/routine_selfcheck.sh 2>&1 | tail -60`,被 §22 守卫当场拒
(`REFUSED: ... stdout is a pipe; exit 2, nothing checked` / `SELFCHECK_EXIT=2 REFUSED`),
改走 `rc.sh` 后才拿到上面那个真码。
📌 **守卫每次都拦住了,却一次也没让下一轮不再犯** —— 它买的是「不会读错」,不是「不会去做」。

⛔ **RULING 60 (甲) 同族第四例,照登**:自检还在跑的时候我**并发**跑了变异台,
它反复改写 `tools/agent/carry_item_issue_state.py` 并清 `tools/agent/__pycache__` / `tests/__pycache__`。
⇒ **本轮自检 python 侧的读数不构成关于 trunk 的任何陈述**,不当通过也不当发现;
欠条 `selfcheck_watchdog_python_cases_never_run` **读数作废,不结清也不加重**(**第十轮**)。
⚠️ 同时照登自检自己打的那个原因,它和并发是两件事:那 9 条 `UNCERTIFIABLE` 的印出来的理由是
`tests/test_selfcheck_lua_leg.py` 的 **120s 预算没跑完**(`5a0 cost: 120 file(s) ... in 120.1s`),
**属于容器速度/集合规模那一族**,不是并发造成的。**两条都写下来,因为我今天分不开它们。**
⇒ 承重的是**推送闸三条腿**(见下)。

---

## 八、成本

零 AWS 调用(本轮**没有**跑 `session_setup.sh`,不需要)。
MTD 沿用批测台 09:12Z 读数 ⇒ **刹车持有,连续第三十二轮不发波**。
RULING 48 三段式:**零 EC2 / 零 CE / S3 读取 0 个对象(出网未计价)**。

---

## 九、铁律 6 三条腿(push 读数)

⛔ **两次 push 均绿,未用 `RULE6_BYPASS`。**

**① 分支 push**(`git push -u origin claude/compassionate-albattani-9buar0`,`RC_EXIT=0`,
`* [new branch]`):

```
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 127 ran, 0 findings, 0 uncertifiable, 44.6s
lua gate: 401 ran, 0 findings, 0 uncertifiable, 9 unanswered, 6 known-red, 615.4s
```

⭐ `py gate` **126 → 127**:多出来的那一条就是本轮新增的 `tests/test_carry_item_issue_state.py`
(实测 0.10s,远在 3.0s 单测上限内)⇒ 它**不是靠 manifest 手改进闸的**,是 `py_gate.py` 对
「没测过的测试一律照跑」那条 fail-open 路把它带进去的(与欠条 `rule6_memo_fast_gate_membership`
当年记的同一条路),下一次 `py_gate_measure.py` 重测会给它一行。

**② `HEAD:main`**:第一次被拒(`! [rejected] HEAD -> main (fetch first)`,
另有一行 `RPC failed; HTTP 403` 与 403 同族的噪声),`git pull --rebase origin main`(`RC_EXIT=0`,
三个 replay-check commit 进来)后重推成功:**`33e23e90..b854e850`**,三条腿再跑一遍:

```
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 127 ran, 0 findings, 0 uncertifiable, 44.0s
lua gate: 401 ran, 0 findings, 0 uncertifiable, 9 unanswered, 6 known-red, 616.2s
```

⚠️ **顺带一条可复用的读数**:被拒的那一次,钩子打的是
`RULE6_MEMO=REUSE  a green three-leg reading taken 2026-09-16T10:28:07Z on THIS EXACT tree`
并逐字写明「**This is a REUSE, not a skip: all three legs really ran**」——
rebase 之后树变了,memo 随之失效、三条腿**真的重跑了一遍**(615.4s → 616.2s 两次独立读数)。

---

## 十、下次触发

以章程「当前状态」2026-09-16T10:1xZ 条目的清单为准(本轮已由 §1.5 那条腿逐号核过 `state`,
收尾读数 `RC_EXIT=0`)。两个死名字 `GH #523` / `GH #538` 已划掉,残余改挂 `GH #466` / `GH #240`。
最要紧的一条:**`GH #466` 在等总监拍板**(豁免表形状 vs census 上下文判据 (a)),已搁 13 天。

---

## 十一、Token 用量(铁律 8)

```
TOKENS total_in=16,928,524 out=73,880 turns=108
```

⚠️ 这是**到统计时刻为止**的数字(收尾回合不计入)。本轮的形状是「一条新腿 + 棘轮 + 变异台 +
两次 11 分钟的推送闸」,turns 偏高的那一段是变异台的锚点两次落空后重跑。
