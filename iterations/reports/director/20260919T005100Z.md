# 总监 2026-09-19T00:51Z —— RULING 83:GH #908 三选一判 (2)+(3);「最便宜的一个」是价签错了,不是习惯错了

**一句话**:录像组把 `pullthink_animactivity_instrument` 的**无条件拒绝**装上并取了价 ——
**18 文件 / 70 用例变红,含一个已 promote 的 id 的真帧判词** —— 然后交裁。本轮判 **(2)+(3)**:
opt-in 的**声明式**开关当场落地(`M.declare_anim_activity`,7 用例 blind-A + 变异台 **7 CAUGHT / 0 SURVIVED**),
(3) 的 UNCERTIFIABLE 范围判据点名三个谓词写进欠条的 `ruling`;
⛔ **不选 (1)**,理由是价签:`GetAnimActivity` 是 catch-all 每帧代答的 **164 个 shipped getter 之一**,
无条件形态**不是一笔 70 用例的有界采购,是 164 期里的第一期**。

**成本**:**零 EC2 / 零 CE / S3 读取 0 个对象(出网未计价)**(铁律 1 三段式)。⛔ 本轮零 AWS 调用。
**⛔ `bots/` 与 `game/` 零 diff**;零 gate id、零 arm、零 promote/reject、不申请波次、不发 owner 邮件。

---

## 〇 开工(铁律 10 / 铁律 9 / §0 / §1.5)

**§0(`rc.sh` 读裸退出码)**:本轮**每一条要读退出码的命令都走了 `rc.sh`**,零管道、零 `timeout` 祖先进程
⇒ ⭐ **纪律 3 本轮没有第三十三发**(上两轮连续在开工第一条命令上踩中)。

**自检(铁律 10)**:`bash tools/agent/rc.sh bash tools/agent/routine_selfcheck.sh > <file> 2>&1`,
00:51Z 起跑,被 harness 移入后台;**本轮等它跑完再推**(⛔ 不像上一轮那样在 push 前杀掉它),
**`SELFCHECK_EXIT` 因此是可得的:`RC_EXIT=3`**。

| 腿 | 读数(逐字) |
|---|---|
| exit sources | `legs run 15` / `FINDINGS (exit 3): cadence queue-rulings owed-executions lua-coverage` / `UNCERTIFIABLE (exit 2): trunk-red(python)` / `NOT RUN (inside a leg): tests/test_lua_gate.py tests/test_luacheck_gate_soakswitch.py tests/test_selfcheck_lua_leg.py` |
| unlanded work | `OK: no unlanded work in the certifiable window.` |
| cadence | **1 finding**:`GAP cadence strategy 09-18T04:24Z -> 09-18T07:55Z 3.5h` —— ⚠️ 这是**历史**那一格,不是当下;当下五组读数见 §五 |
| verdict closure | `STALL: 9 director round(s) with no 判定完结`(门槛 12 点名 / 24 DECISIONS_NEEDED)⇒ **未到线**,与 §五 的 9 互相印证 |
| queue-rulings | `RIDESHARE (rule this round; outcome per owner P4.2, not §BB.4): 49` —— ⭐ **49 条 `director` 字段为空的搭车请求**,多为英雄组新落地 gated id 的**零 EC2 域读数**请求 |
| **lua-coverage** | `disk 521 / push gate 354 / 自检腿 141 / known_red 5`;`UNCOVERED 114 of 521 (22%)`;`UNCOVERED SET GREW` 仍是上一轮那两条(`test_fieldsip_transfer_receiving_site.lua` / `test_lion_w_fight_seed.lua`),**没有第三条**;`NOW COVERED tests/test_fieldsip_atom_pricing.lua` |
| py-coverage | `disk 155 / push gate 138 / 自检腿 155`,`UNCOVERED 0 of 155 (0%)`,`out-of-gate rows with a written reader: 17 of 17` |
| trunk health (fast Lua) | `141 tagged detector file(s), 0 failures` |
| trunk health (python) | **`152 passed, 0 failed, 3 uncertifiable`** —— 3 条 uncertifiable 是 `test_lua_gate.py` / `test_luacheck_gate_soakswitch.py` / `test_selfcheck_lua_leg.py` |

⚠️ **`trunk-red(python)` 那条 exit 2 本轮的归因写清楚,⛔ 不许读成「trunk 红」**:
`test_selfcheck_lua_leg.py` 的 9 条检查在 120s 预算内没跑完(逐字
`the leg finishes inside 120s [it did not on THIS container]` + `141 file(s) [discovered] in 120.1s`),
而**自检跑的整段时间里我正在 `tests/` 下写文件** ⇒ 这是 GH #898 那一族的**并发**,
工具自己的措辞已经说了:`it is NOT evidence that trunk is red: re-run on a quiet tree`。
⛔ **照登为 UNCERTIFIABLE,不记成红、也不记成绿。**

**§1.5 交棒交叉读**:`carry_item_issue_state.py` **裸码 0**,逐字
`corpus: … fetched 2026-09-18T13:10:35Z (11.8h old), 20 issues` /
`scanned: 1 entry (of 101), 1 carry segment(s), 13 GH ref(s)` / `every carried GH ref is open.`
⇒ **无 `STALE-CARRY`、无 `NO-HANDOFF`**(语料 11.8h,在 48h 内)。

**铁律 9(owner 优先项)**:P1 球在协同组、P2 球在协同组(卡 `wandlimbo_charge_instrument` 仪器墙)、
P3 责任在总监但已完结、P4.1 球在批测台、P4.3 是总监的一次性工作单元(未动)。
⭐ **P4.2「每轮 ≥2 判定完结」本轮仍未达标(0)**,归因逐字写在 §五,⛔ 不许读成达标。

---

## 一 立案:录像组交来的价签(GH #908)

`iterations/owed_executions.json:pullthink_animactivity_instrument` 的 `executor` 逐字自述
**「这是四个仪器缺口里最便宜的一个,dumper 一行都不用动」**,并给出做法:
`tests/mock/replay_fixture.lua` 为 `GetAnimActivity` **无条件装** getter。

录像组 2026-09-18T21:58:06Z 把那一行装上(按拒绝形态,与 `GetLaneFrontLocation` 同形),
在**同容器、同棵树、唯一差别是那一个文件**的条件下取到:

| | `tools/agent/lua_gate.py --all` |
|---|---|
| 基线(trunk) | **0 findings** |
| 装上拒绝后 | **`434 ran, 18 findings, 0 uncertifiable, 8 unanswered, 5 known-red, 648.5s`** |

**18 文件 / 70 用例**,其中 4 个是该欠条 `trigger` 自己预告过的设计触发器,
**其余 66 个是附带损伤**,全部同一机制:拒绝**从 shipped 代码内部抛出**,
测试是在**驱动真实决策路径**时撞上的。现场逐字:

```
FAIL: test_roamstale_collapse_action.lua :: PROMOTED: the collapse frame attacks the INVADER with NOTHING armed
      LOADER REFUSES: GetAnimActivity is unresolved …
```

⇒ 其中一条是**已 promote(每局 Turbo 都活着的默认行为)**的真帧判词。

---

## 二 本轮自己复核的根:三个谓词恒假,而 catch-all 那一格不是一个

**机制**(本轮逐行复核,非转述):

- `tests/mock/bot_api.lua:184` —— `if key:find('^Get') then return 0 end`(单位方法 catch-all);
- `tests/mock/bot_api.lua:491-501` —— `_G` 的 `__index` 把每个未知 ALL_CAPS 解析成 **≥1001** 的稳定 id;
- ⇒ `bot:GetAnimActivity()` 每帧读 **0**,而 `ACTIVITY_*` 恒 ≥1001 ⇒
  `J.IsRunning`(`bots/FunLib/jmz_func.lua:3845`)、`J.IsAttacking`(`:3854`)、
  `J.IsChasingTarget`(`:3873`,两次 `IsRunning` 的合取)**在整份语料上恒为 FALSE**。

**shipped 暴露面**(录像组数,本轮未复数):`IsRunning` 169 / `IsAttacking` 574 /
`IsChasingTarget` 317 = **1060 处,散布 149 个 `bots/` 文件**。

⭐⭐ **本轮新量到的一格,它把「一个 getter」这个尺寸否掉了**:把动作状态这把锁清掉
**不足以**让 `J.IsAttacking` 变得可问 —— 它下面**还压着第二个捏造的 0**:
`GetAttackPoint` 与 `GetAnimCycle` 都走同一条 catch-all 读 0,而谓词比的是
`bot:GetAttackPoint() > bot:GetAnimCycle() * 0.99`,即 **`0 > 0` 恒假**。
钉在新测试 `[3]`(实测:声明 `attack` 后 `GetAnimActivity == ACTIVITY_ATTACK`,
`GetAttackPoint()==0`、`GetAnimCycle()==0`、`J.IsAttacking` **仍为 false**)。
⇒ 与仓里已有的那把尺子一致:`tests/test_mockscalar_return_shape.lua` 数到
**164 个 shipped getter** 由同一个 catch-all 每帧代答,`GetAnimActivity` 是其中**一个**。

---

## 三 ⚖️ RULING 83(GH #908 三选一)

### 判词:**(2)+(3);⛔ 不选 (1)。**

**⛔ 不选 (1)(接受采购单、逐个重判 66 个用例)的理由是价签,不是口味**,两条:

1. **爆炸半径已实测**:红 18 个文件 = 当场停掉**五个组**的 pre-push 钩子(`lua_gate.py` 红即拒推),
   去重问一个**那 66 个用例都没在问**的问题。
2. **它不是有界采购**:`GetAnimActivity` 是 164 个同族 getter 之一,**逐个买 = 164 期**,
   而第一期就已经是这个价。⭐ 这正是 §GF(2026-09-09)那句「不是一个兄弟 getter 被漏了,
   是一整条 POLICY 只对一个 getter 生效」的**代价面**:政策要推广,
   但**不能按 getter 逐个推广**,那是把便宜阶段的问题拿去昂贵阶段付钱。

**(2) 本轮已落地(不是交棒,是交付)**:

- `tests/mock/replay_fixture.lua` 新增 **`M.declare_anim_activity(heroes, tState)`**
  —— 与同文件的 `M.declare_defend_ping`(GH #91)同形、与 `GetLaneFrontLocation`(GH #61)同策:
  **加载器拒绝建模,测试用一行把假设说出口**。
- ⭐⭐ **opt-in 不等于「否则回落到 0」,这半是承重的**:一旦测试打开这个开关,
  **它没点名的每个单位一律拒绝**(`LOADER REFUSES: GetAnimActivity is unresolved (GH #908) for <name>`)。
  问追击的测试**必须把两边都声明**,因为 `J.IsChasingTarget` 读两边;
  要前 #908 那个桩,必须把 `'idle'` **说出口** —— 那是一个**被声明的非零 `ACTIVITY_IDLE`**,
  不是那个捏造的 0。
- ⛔ **`M.load` 自己一行都不装**,由新测试 `[5]` 钉死 ⇒ **那 66 个用例一格未动**。

**(3) UNCERTIFIABLE 的范围判据(按 #908 验收要求,点名三个谓词,已写进该欠条 `ruling`)**:

> 判词路径经过 `J.IsRunning`(`:3845`)/ `J.IsAttacking`(`:3854`)/ `J.IsChasingTarget`(`:3873`)
> **且未调用 `declare_anim_activity`** 的,⛔ **不判「结论错」**。

⭐ **这一条的措辞是本轮唯一对 #908 原文的实质修改,理由是逻辑**:那 66 个用例是在
**not-running / not-attacking 那一支**上取的判词,而**那是真实英雄大部分时间所处的状态** ——
它们**不是错的,是没声明**。所以正确的标记不是「作废」,是**作用域比措辞窄**:
**仅在该假支上成立**。任何要把这类判词读成「对全部动作状态成立」的引用,一律标 UNCERTIFIABLE。
(与 `evidence-discipline` 第 4 条同型:结论对不等于理由对 —— 这里是**理由的域**被悄悄放大了。)

**⛔ 订正 `executor` 里那句评价**(#908 点名要求):
「四个仪器缺口里最便宜的一个,dumper 一行都不用动」—— **dumper 确实一行不用动,
loader 那一侧也确实是一行,但代价不在那一行上**。已按上面两条读数改写。

**⛔ 该欠条状态不变:仍是 OWED、未认领**(⛔ 不倒填 `claimed_by`)。
(2) 买到的是**问的能力**,不是 (a) 本身;且作用域项仍卡在 `pulllane` 那堵墙上,**两端都买到才算买到 (a)**。

---

## 四 交付物与读数(裸码,⛔ 未经管道)

| 产物 | 读数 |
|---|---|
| `tests/mock/replay_fixture.lua` | +`M.declare_anim_activity`(声明 / 拒绝 / 计数 / 两处校验) |
| `tests/test_blind_a_anim_activity_declare.lua`(新) | `lua5.1 tests/run_tests.lua blind_a_anim_activity_declare` ⇒ **`7 tests, 0 failures`**,`RC_EXIT=0` |
| `tools/agent/mutstand_anim_activity_declare.sh`(新) | **CONTROL 绿,7 CAUGHT / 0 SURVIVED / 0 ABORTED**,`RC_EXIT=0` |
| `iterations/owed_executions.json` | 该行 `ruling` 追加 RULING 83 + `executor` 价签订正(**2 insertions / 2 deletions**,json 结构零重排) |

**变异台七发,逐条问的是什么**(⛔ 它存在的理由是:该测试**第一次就全绿**,
而**从没红过的闸是没人量过的闸** —— evidence-discipline 第 2 条):

| | 变异 | 谁该抓 |
|---|---|---|
| M1 | 未声明的单位**答 0** 而不是拒绝(= 前 #908 的世界穿上新 API) | `[4]` |
| M2 | `load()` 自己给每个 handle 装上(= 被实测否掉的无条件形态) | `[5]` |
| M3 | `'idle'` 悄悄变回那个捏造的 0 | `[6]` |
| M4 | 动作词打错字被**静默接受** | `[7]` |
| M5 | **单位名**打错字被静默接受(与 M4 不同:错名的单位落进拒绝支,测试会把拒绝读成自己要的那个) | `[7]` |
| M6 | 声明**计了数但没装 getter**(「结论对、理由错」形状) | `[2]` |
| M7 | 拒绝语丢掉 `LOADER REFUSES` / `GH #908` 这个可 grep 的名字 | `[4]` |

⭐ **还原走文件副本 + `sha256sum -c`,`trap restore EXIT` 在第一次变异之前就上膛**(GH #418),
`tests/test_mutstand_restore_trap.py` 管这一条。

---

### 四之二 ⭐ 新测试**当轮就登记进自动读者**,⛔ 不留给下一轮去发现

本轮自检的 `lua-coverage` 腿逐字说了它存在的理由:
`A test nobody runs cannot refuse a push, so its red is found by the NEXT desk to start work`。
一个**刚落地、没有任何自动读者**的测试,下一轮就会以 `NEW UNCOVERED` 的形状出现在这条腿上 ——
⛔ **那等于我一边裁「红由下一个开工的组发现」是缺陷,一边自己制造一条**。

处置按该腿自己列的三条出路取第二条(⛔ **不选**重测整份 manifest:
上一轮已实测 cap 附近的成员资格**随容器速度漂**,为一个文件付一次全量重测正是那条教训):
给本文件的 `[1]` 与 `[5]` 两个用例打 **`[ratchet]`** 标签 ⇒ 开工自检的快 Lua 腿**按标签发现**它。

⭐ **标签选在这两条上是有理由的,它们是本文件其余部分所倚的两条常驻不变量**:
`[1]` 未声明的世界**仍然恒假**(有人动 `bot_api` 的 `^Get -> 0` 那天它变);
`[5]` 这个开关**仍然是 opt-in**(有人把它「推广」成 RULING 83 已定价并拒绝的无条件形态那天它变)。

⛔ **按该腿自己的规矩「加标签前先 TIME 一下结果集」**,读数写下来而不是假设:
本文件整文件三次实测 **0.162s / 0.156s / 0.158s**。

## 五 体系健康巡检 + P4.2 归因

**报告节奏**(取数 2026-09-19T01:05Z,`git fetch origin main` 之后,读文件名里的戳):

| 组 | 最新报告 | 距今 |
|---|---|---|
| batch-desk | `20260919T001303Z` | 0.9h |
| hero | `20260918T225037Z` | 2.3h |
| strategy | `20260918T222029Z` | 2.8h |
| replay-check | `20260918T215806Z` → ⚠️ **订正:`20260919T004859Z`** | 3.1h → **0.3h** |
| director | `20260918T215800Z` | 3.1h(本轮补上) |

⇒ **五个组全部健康,零 GAP(门槛 3.5h)、零点名、零升级**。

⚠️ **这张表在本轮之内就过了一次期,照登而不是悄悄改数**:01:05Z 取数时
`origin/main` 上录像组最新是 `20260918T215806Z`(3.1h);push 前的 `git pull --rebase`
带回 `d454c49c`,那一轮的报告 `20260919T004859Z` **写于 00:48Z、落地于 01:08Z**
⇒ 真实间隔是 **0.3h**,不是 3.1h。
📌 **与 RULING 75/76 同族(「真命题过了期」),而本例的形状是最朴素的一个**:
巡检读的是 `origin/main`,而**报告落地比报告写成晚** ⇒ 任何一次巡检读到的都是
**下界偏老**的节奏。结论方向不变(两个读数都远在 3.5h 门槛内、都判健康),
⛔ **但不许把 3.1h 那个数留在表里当成本轮的读数。**
⭐ **上一轮交棒 ⑯ 当场结清**:batch-desk 上一轮读 `GAP 3.8h`,本轮 **0.9h**
⇒ **不构成连续两格**,⛔ 不点名、不写红色段。

**P4.2(每轮 ≥2 判定完结)**:本轮 **0**,累计停滞 **9 轮**(门槛 12 点名 / 24 写 DECISIONS_NEEDED)
⇒ **未到线,但归因要写清,⛔ 不许读成「没人裁」**:条件 (b) 要批测,
而 MTD 刹车($92.462 / 刹车线 $90)已持有**五十余轮** ⇒ 停滞**不是没人裁,是裁所需的那一半买不到**。

⭐⭐ **本轮量到一个可以在刹车下推进 P4.2 的方向,已挂交棒(⛔ 本轮不动手,那是另一个工作单元)**:
**退回出集不需要 (b),只需要证明 (a) 买不到** —— 而本轮的根正好是「(a) 在一整族谓词上买不到」。
**上界式点数**(⛔ 下面这个数是**文件作用域**的,不是路径作用域的,**有意往多了数**):
24 个 armed id 里,**11 个**的 `IsSoakCandidate('<id>')` 调用点所在文件**同时**调用了那三个谓词之一 ——
`wandbleed` / `odaoe` / `fieldbuy` / `pullcad` / `campfarm` / `abilanc` / `campvoid` /
`creepthink` / `illureal` / `slotarb` / `arbheart`。
⛔ **LIMITS,这个数不许被过读**:(i) 同文件 ≠ 同路径,真数只会更小;
(ii) 另外 13 个打 `NO-GATE-SITE` 的是**我这条 grep 的局限**(id 串没有字面出现在 `IsSoakCandidate(...)` 里,
多半走 `J.IsLaneFixOn` 一类包装或变量),⛔ **不是「这些 id 没有 gate」**;
(iii) 这是**定尺寸**用的,不是判词。

---

## 六 本轮没做的、和为什么

- ⛔ **不发 owner 邮件**:W38 配额留给 **09-20(周日)**那封(DECISIONS_NEEDED 第 15 条两条状态更新
  + `$100` 最坏落点 `2026-09-25T23:16Z`)。本轮是 harness 裁定,不属决定类。
- ⛔ **不动 `bots/` / `game/`**:RULING 83 全部落在 `tests/` 与 `tools/agent/`。
- ⛔ **不动成本判断**:$90/$100 是 owner 的地界,随 09-20 那封走。
- ⛔ **不顺手推广 policy 到另外 163 个 getter**:那是政策变更,要自己的一轮和自己的测量(与 09-18 对 cap 的处置同理)。

---

## 七 下次触发(与章程「当前状态」同步,⛔ 以章程那份为准)

①GH #856 剩 9 候选 ②GH #867 ③GH #240 余下 ④`carry_mark_prose_vs_list` 剩唯一一格
⑤GH #843 剩 (乙) ⑥GH #859 ⑦GH #810 待裁 1 + (乙) ⑧GH #528 ⑨GH #548/GH #806
⑩GH #839 选 (甲)/(乙),先量爆炸半径 ⑪GH #910 ⑫GH #909(`tpreach` **09-26 硬悬崖**)
⑬核 `aws_run_dryrun_and_watchdog_pricing` ⑭无自检容器上复跑 `tests/test_rc_wrapper.py`
⑮**W38 周日邮件(09-20)** ⑯**新**:把 §五那份**上界式 11 个**下沉到路径作用域
⑰**新**:另外 163 个同族 getter 的政策推广没有买 ⑱**新**:自检 `RIDESHARE … 49` 待裁

## 八 token 用量(铁律 8)

```
TOKENS total_in=11,859,117 out=60,464 turns=86
```
(统计时刻之后的收尾回合不计入。)
