# director 工作单元报告 — 2026-08-19T00:53:12Z

## 0. 范围
首次 director 触发(此前只有章程/routine 文档,无执行记录)。读了
`iterations/reports/{hero,replay-check,batch-desk}/` 三份最新报告(均为
2026-08-18/19 首次真实触发产出)、`iterations/state.json`、
`iterations/streams/test_set.md`、`iterations/queue.json`、open GitHub
issues(17 条,均为 2026-08-01 协议前的 `[P1]-[P4]` 旧格式,无新
`[bug]`/`[harness]` 前缀 issue)。

## 1. [bug]/[harness] issue
无。17 条 open issue 全部是旧格式(`[P1]-[P4]`,协议前建的),没有一条按
新约定用 `[bug]`/`[harness]` 前缀开给总监。本轮无需处理。

## 2. promote/reject 判定
**本轮不下判定,原因记录如下(避免下次重复分析):**

- `batch-desk` 00:11 UTC 刚启动了 test_set.md 现行 14-id 全集(首次整体
  跑)的例行波次,4 种子(851-854),commit `96f49dc`,预计 verdict 落地
  02:10-03:10 UTC —— 本次触发(00:53 UTC)时仍在跑,没有新数据。
- `replay-check` 独立核验了 `midtp`/`suptp` 执行正确(WORKING,两局帧证据
  俱全),`tpcommit` 弱证据 WORKING。这满足 promote 三条件里的 (a)。但
  (b)(批测无明显负面)现有数据是 `groupB_VERDICT`(state.json,2026-07-23
  跑的旧 bundle `midtp,suptp,tpcommit,tpsafe2,lf_rescue`,econ neutral/HOLD),
  该数据早于此后落地的 lf_rescue 收窄、chain-rescue 闸、ccburst 收窄等多次
  修复,不能确定 tree 一致性。**决定:等 00:11 这波在当前 tree 上的新
  verdict,再判定 midtp/suptp 是否三条件齐全可以 promote**,不用旧 tree
  数据拼凑判定,避免误判。
- 其余 12 个 id(`creeppull/pullcamp/l1trade/l5combo/lf_rescue/teambrain/
  ownhalf/overchase/fieldregen/wandbleed/tpwatch`)本轮录像组没有新证据
  (两局场景没覆盖到),条件 (a) 还不满足,谈不上 promote。

## 3. test_set.md 变更提议
无 pending 提议。`test_set.md` 现行 14-id 集合未变。

## 4. 成本核查
`batch-desk` 报告已核实:新计费月 MTD $3.45,刹车线 $90/批准线 $100,远
未触发。启动前后各查了一次 `describe-instances`,无泄漏。本轮无需额外
动作,也无需邮件。

## 5. Routine 体系健康巡检
- `batch-desk`/`replay-check`/`hero` 三组均已产出首份真实工作报告
  (2026-08-18 23:47 / 2026-08-19 00:11 / 00:44 UTC),质量合格(有具体帧
  证据/成本数字/决策记录,不是空转)。
- `strategy`(协同组)**尚未产出任何报告**,`iterations/streams/strategy.md`
  的"当前状态"仍停在 2026-08-01 初始化文案。按 `routine_prompts.md`,
  strategy 的 cron 是奇数小时 `:05`——本次总监触发(00:53 UTC,偶数小时段)
  之前最近一个 strategy 触发窗口是 08-18 23:05(彼时其它组也才刚开始首轮),
  之后是 08-19 01:05(还没到)。**暂不升级为问题**,记录观察;下次总监触发
  (预计 02:50 UTC)如果 strategy 仍无报告,升级排查(可能是 owner 那边
  routine 没建全,需要提醒)。
- `queue.json` 为空,没有跨组请求积压。

## 6. Patch 检查(本轮做了,发现缺口)
`https://www.dota2.com/datafeed/patchnoteslist?language=english` 显示最新
patch 是 **7.41e**;`docs/PATCH_UPDATE_GUIDE.md` 记录的 "Last updated for"
是 **7.41a**(2026年3月)。中间还有 **7.41b/7.41c/7.41d** 三个未处理的补丁,
一共落后 4 个小版本。

**本轮不展开处理**(patch 更新流程本身较大:分类 STRUCTURAL/NUMBER-ONLY/
TALENT SWAPS、抓 d2vpkr 数据、Liquipedia 核对英雄名、更新 TS 源码,不适合
塞进一个"小"工作单元),记为基建 backlog 新的最高优先级条目(见下)。这不
需要 owner 决定——PATCH_UPDATE_GUIDE.md 里的流程本身就是既定授权,只是需要
执行;焦点五优先。

## 7. 本轮未做基建 backlog 推进
本轮时间花在读三组报告 + patch 巡检上,没有推进"帧语料检索工具"。改为把
patch 补齐提到 backlog 最前面(影响面更广、更紧迫——落后 4 个版本意味着
英雄数值/技能可能已经过时),帧语料检索工具保持第二优先级,下次总监触发
(或有余量时)从 patch 缺口开始。

## 8. 结论 / 下一步
- 未做任何 Lua 改动,未 push 代码——本轮只有文档/状态更新,luacheck/单测
  门槛不适用。
- DECISIONS_NEEDED.md 未创建:本轮没有需要 owner 决定的问题(patch 缺口
  是既定流程可自主执行,不是决策分叉)。
- 下次触发:(1) 收 00:11 那波 14-id verdict,做 midtp/suptp promote 判定;
  (2) 若 strategy 仍无报告则排查;(3) 开始 7.41b patch notes 抓取与分类。
