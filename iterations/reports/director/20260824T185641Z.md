# 总监 2026-08-24T18:56Z(第六十四次触发)—— `pulllane` + `towerfear` **批准入集**(GH #164),并把「不加急」立成一条错误默认

**一句话**:上一轮总监写的「本轮不新做 promote/reject … **不加急**」在 18:12Z 被量出了代价 ——
批测台发 W7 全集波时,`pulllane` 与 `towerfear` **代码在树上、接线门 WIRED、只差一个批准**,
于是**空过了一波全集**($1.48 已花,这两条本可以免费搭上去),其中 `pulllane` 是
**owner P1(拉野)连接率**那一格。本轮**两条都批准入集,成员串 26 → 28**,
并把「搭车入集提议必须当轮批或退」立成流程补则(§BB.4)。零 AWS。`bots/`/`game/` **0 改动**。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` ⇒ **worst exit 3**。

- **UNLANDED 8 commit**:全部在 `compassionate-albattani-uv8bab` / `busy-bardeen-{uxvcdx,u45ms4}`,
  其中 2 条带 **CLAIMS-LANDED**。**本轮按 §④ 的新判据(上一轮 17:xxZ 追记立的)逐条核实,
  结论:全部是 LIMIT-4 假阳性,内容已由抢救链换 SHA 落上 main**:
  - `git merge-base --is-ancestor ec46147 origin/main` = **NO**,但它的**唯一 diff**
    (`tests/test_towerfear_clock_leg.lua` +6 行 `rf.declare_defend_ping(J,'stale')`)
    **已在 main 上**(`grep` 命中 `:151`),来源是协同组 **`4e68774` 16:41Z 自己修的**
    —— 与上一轮追记 §③ 的判读一致,**本轮独立复现该判读,不再 cherry-pick**。
  - `4af32ae` 的四个文件同理:`iterations/reports/director/20260824T160000Z.md`
    **在 main 上存在**,`DECISIONS_NEEDED #9` 已由 `9a128b2` 抢救落地。
  - promote 链(`fae4468` 等)的**树**在 main 上:`mode_roam_generic.lua:234`
    = `PROMOTED (was soak-candidate 'pullbeat')`。
  ⇒ **不开 issue、不抢救、不 cherry-pick**。判据是源码与文件本身,不是工具输出(§AG 的事实源规矩)。
- **CADENCE 6 finding**:全部是 08-24 02:00Z–13:00Z 那段停摆的历史洞;五条流本轮均在 6h 内有产出。
- trunk python 套件 **18 passed / 0 failed**。
- 容器无 `lua5.1` / `luacheck`,已装。AWS **未 bootstrap**(本轮不需要,总监不花钱)。

---

## 1. 认领:GH #164(入集裁定)。为什么它压过 `[bug]` #159

本轮桌上的总监活至少四条:`[bug]` #159(`tpsafe2` 有洞)、`[bug]` #156、
`[harness]` #161、`[harness]` #152,加上 **#164 入集裁定**。取 #164,理由是**成本不对称**,
不是它更严重:

| | #164 入集裁定 | #159 `tpsafe2` |
|---|---|---|
| 本轮不做的代价 | **每一波全集都空过**(已发生一次,$1.48) | 缺陷继续存在,但**不会因为晚一轮而变大** |
| 做它的成本 | markdown + registry,**零 AWS、不占波次** | 需要定位 + gated 修法 + fixture,**一整个工作单元** |
| 谁被卡住 | **owner P1 的责任链**(球在总监) | 已开 issue,可由协同组接 |

⇒ #164 是「零成本、正在持续流血」,#159 是「有成本、不流血」。**先做前者**。
#159 的下一棒在 §4 显式交出去(铁律 9 连带规则)。

---

## 2. ⭐ 裁定:`pulllane` + `towerfear` **两条都批准入集**,成员串 **26 → 28**

全文档案落在 `iterations/streams/test_set.md` **§BB**(裁定必须落到成员串,写在散文里不算送达 —— §BA.4)。

### 2.1 `pulllane`:合取门经**源码侧**核实是活的,不是 §BA.2 那个被冻死的形状

§BA.2 的教训是「`X and Y` 的合取会在 Y 被 promote 的那天被永久冻结为 FALSE,
而 `check_armed_wiring.py` 仍报 WIRED(它查调用点存在,**不查谓词能不能为真**)」。
**`creeppull`/`pullbeat` 本轮刚 promote,所以这个检查不是形式主义。** 逐字核实:

| 环节 | 源码 | 读数 |
|---|---|---|
| 外层函数早退 | `jmz_func.lua:7915` `if not J.IsSoakCandidate('pullcamp') then return nil end` | `pullcamp` 在成员串、**未 promote** ⇒ 可为真 |
| 新子句门 | `jmz_func.lua:8037` `if J.IsSoakCandidate('pulllane') then` | 本裁定后在成员串 ⇒ 可为真 |
| 合取项里有无已 promote 的 id | 全仓 `grep` `pullcamp`/`pulllane`/`IsCampBesideLane` | **无** —— `creeppull`/`pullbeat` 不出现 |

**⇒ 合取可以为真。** 批准。**⚠️ 隔离波 armed 串必须同时含 `pullcamp` 与 `pulllane`**
(全集波自动同时 arm,因为两者都在成员串里)。

**除机制外的批准理由**:常数 1200 = 录像组自己量到的 max 拖拽 1,170u 向上取整(`0NUM` 合规,
不是发明的数);提议**自证了可分离性**,并把反 SILENT 读法**事先登记**进 `queue.json:strategy-11`
(`poke_episodes` 塌零读作「常数太紧」而非「场景稀缺」)—— 正是 §AT.1 要求的预登记形状。

### 2.2 `towerfear`:门是单条,armed 谓词是出厂谓词的**子集**

`mode_retreat_generic.lua:907` 的门是 `IsSoakCandidate('towerfear') and IsModeTurbo()` ——
**没有第二个候选 id 做合取项** ⇒ 结构上无 §BA.2 风险,单独 arm 即有意义。
armed 只把时钟腿 `5*60` 折半到 `2:30`,等级腿 / 塔环 / 校准判据一律不动
⇒ 那个 `ABSOLUTE×1.1` 的 `return 2` **只可能少开、不可能多开**,最坏回到出厂行为的一个子集。批准。

**登记一条诚实边界(不构成退回理由,写进验收)**:本地帧域 **1/966** 是**这份语料的下界**,
不是 turbo 的估计(批测局在攻塔前自终止,该 fixture 集当初为别的问题采)。若波次回来三个读数
全不动,按 `queue.json:strategy-12` 已登记读法:先查 arm 串,再量事件率,**不许再收窄谓词**。

### 2.3 本轮实跑的核验(不是读报告)

| 检查 | 命令 | 读数 |
|---|---|---|
| 全成员串接线 | `check_armed_wiring.py --cand <28 ids> --ref HEAD` | **all 28 armed ids wired**,exit **0** |
| `pulllane` 清单 | `lua5.1 tests/run_tests.lua pullcamp_lane_gap` | **15 tests / 0 failures** |
| `towerfear` 清单 | `lua5.1 tests/run_tests.lua towerfear` | **15 tests / 0 failures** |
| GH #91 棘轮(上一轮 main 是红的) | `lua5.1 tests/run_tests.lua defend_ping_declaration_ratchet` | **8 tests / 0 failures** |
| 静态 | `luacheck bots game --formatter plain` | **0 警告** |

---

## 3. ⭐ 流程补则(§BB.4,本轮新立):**搭车入集提议,当轮批或退**

一个自称「搭车、不申请专波、零 AWS 增量」的入集提议,**其唯一成本是不裁**。
因此总监**不得**以「不加急 / 让下一轮按顺序读」处置它 —— 那不是保守默认,
**那是把一个零成本决定拖成一次空过的波次**。**批或退,当轮给结论;退回必须写理由**,
免得它每一波都排在「差一个批准」的位置上。

与已有两条补则的分工:**§BA.4** 管「裁定必须落到成员串(散文不算送达)」,
**本条**管「裁定的**时限**」。两条合起来才盖住 #164 暴露的这个洞 ——
上一轮**两条都没违反**(它没写错地方,也没拒绝),它只是**没裁**,而当时没有规矩说不裁是错的。

---

## 4. 交棒(铁律 9 连带规则:修好≠做完)

| 收件人 | 棒 |
|---|---|
| **批测台** | 成员串已是 **28**,`pulllane`/`towerfear` **下一波全集自动带上,零 AWS 增量,不请求专波**。`queue.json:strategy-11 / strategy-12` 已改 `approved_pending_wave`。**发隔离波时 `pulllane` 必须与 `pullcamp` 同时写进 armed 串。** |
| **owner P1 责任链** | DoD 第 2 步(总监批准入 armed 集)**本轮完成**;球移到**批测台(第 3 步:买连接率的取证波)**。`OWNER_PRIORITIES.md` 由 Cursor 整合者维护,本轮**不代改**,球位变动记在此处与 GH #164 评论里。 |
| **协同组 / 下一个总监** | `[bug]` **#159**(promoted 且 **live 在所有 turbo 局**的 `tpsafe2`:10,319 次 TP 按下里 1,296 次贴脸按,223 次死在通道,致死率 17.2% vs 基础 3.9%,两腿同号)—— **本轮未做,不是判它不重要**,是 §1 的成本不对称。它是**已 promote 的默认行为**上的洞,不受 gate 保护 ⇒ 下一轮总监的**默认认领项**。 |

**本轮不做的事(明说)**:没有 promote / reject 任何 id;没有发波、没有请求发波;
没有动 `bots/` 或 `game/` 一行;没有给 owner 发邮件(W35 汇总周日 08-30,`DECISIONS_NEEDED`
本轮无新增 —— #158 已由上一轮记作 #9);没有代 Cursor 改 `OWNER_PRIORITIES.md`;
`stable-v2` 的分支引用**仍未创建**(批测台 18:15Z §1 的诚实边界,本轮未处理,留给下一轮)。

---

## 5. Token 用量(铁律 8)

```
TOKENS total_in=2,970,788 out=21,713 turns=32
```

(统计时刻为收尾前;此后的提交/推送回合不计入。)
