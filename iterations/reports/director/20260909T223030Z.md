# director 2026-09-09T22:00–22:3xZ(批测台 21:08Z 交棒 2 + 3 一次裁完)

**一句话**:两条交棒都裁了(`headroom $0.292` 的下一步 / W61 四粒种子),
而本轮真正要交出去的是**裁它们时读到的第三件事** ——
**围栏那件仪器把「一个区域」的读数印成 `account-wide`,而它保护的预算既没有 tag 过滤也没有区域过滤。**

零 AWS 调用、零波次、**`bots/` + `game/` 零 diff**、**不发 owner 邮件**、
armed 串 **37 不动**、**无 promote / 无退集**。
取活依据:**章程 2d**(成本裁定)+ **2a**(`[harness]` 直接修)+ 批测台 21:08Z §八.2/§八.3
**逐字点名总监**,且 §八.3 是**第二轮**欠着(上一轮「下次触发」①,顺延过一次)。
裁定全文档案 `iterations/streams/test_set.md §GI`(§GI.0–§GI.6)。

---

## 一、⭐⭐⭐ 主轴:裁「等重置还是升级」时,读到的是围栏自己在说谎

**发现**:`wave_fence.py` 的 RULING 4 accrual 检查逐字打
`CERTIFIED (0 accruing instances account-wide, read this run)`,
docstring 写 `the enumeration is ACCOUNT-WIDE ON PURPOSE`。
**而 `ec2 describe-instances` 是区域调用**,`tools/batch_test/aws/bootstrap_creds.sh`
往 `~/.aws/config` 写死 `region = us-west-2`,`_awsx()` 不传 `--region`
⇒ **读的是一个区域,印的是账户**。它保护的 `dota2bot-batch` **没有 CostFilters,也没有区域过滤器**。

⇒ 一台在 `us-east-1` 烧钱的 `c7a.16xlarge` 对这道门**逐字不可见**,而门打 `CERTIFIED`。
**失效方向:往多花钱那一侧;形状:静默;并且恰好在花钱最多时最要紧** —— 与该文件为之而写的那个缺陷同族。

⚠️ **它和本轮的主题不是巧合**:`headroom $0.292` 唯一被点名的成因就是外来算力,
**而本农场的泄漏检查也是 `us-west-2`(区域取自 `aws.env`)**
⇒ **两条独立的「零」,覆盖的是同一个区域。**

**修法(RULING 5,已落地)**:枚举账户全部已启用区域(`ec2 describe-regions`,免费)逐区读,行上带 `region`;
枚举拿不到(受限用户可能没有 `ec2:DescribeRegions`)就**退回配置区域并降级措辞而不是降级门** ——
零打成 `CERTIFIED WITHIN SCOPE ONLY`,加一行 `accrual scope :`,
并在裁决行**之后**追一行 `WAVE_FENCE SCOPE :`(**让免责跟着那句会被抄进报告的话走**)。
⛔ **故意不做成 exit 2**:把标签缺陷升级成永久发波中断是**政策改动**,而这台子已经被围栏挡住了;诚实的标签才是修法。

**证据**:
- `tests/test_wave_fence.py` **71 checks / 0 failed**(新增 §13/§14 两组,§14 用 `_awsx` 打桩离线跑活枚举的三条路径);
- `tools/agent/mutstand_wave_fence_scope.sh` **4 CAUGHT / 0 SURVIVED**
  (M1 旧措辞放回红 3 / M2 退回分支自称完整红 2 / M3 部分失败读成完整红 1 / M4 丢掉 `--region` 红 6),
  control 绿,还原 `sha256sum -c` **逐字节相同**;
- python 全套 **118 passed, 0 failed, 1 uncertifiable**(见 §五)。

⭐ **两处必须留痕的现场,都是别人的检查器逮住我的**:
1. 第一版把「a reading, not a default」这句从降级分支里丢了,**被 2026-09-06 就写好的第 11 条断言当场逮住**。
   那句话与 scope **正交**(一个区域的零**仍然是读出来的**):RULING 5 收窄的是**声称**,不是把读数贬成假设。
   ⭐ **我没有去改那条断言,我改回了代码** —— 反过来做就是本仓库反复记的那个形状。
2. 新落的变异台**没装 EXIT trap**,`tests/test_mutstand_restore_trap.py` 当场红:
   每发之后的 `cp` 只覆盖脚本**活着走完**的路径,**中途被杀就把树留在变异态**(GH #418 立案的正是这个)。
   已按仓库既有形状补:trap 在**第一发之前**装、`sha256sum -c` 证明还原、`trap - EXIT` 排在 `rm` **之前**(GH #492 §EL.7)。

## 二、交棒 2(GH #677):两个选项都不选,而两个都有一半是对的

| 问的 | 裁的 |
|---|---|
| 等月初重置? | **作为计划被否**:重置后的天花板是 **`$50` 不是 `$80`** |
| 现在升级给 owner? | **作为一封新邮件被否**:同一问题已在 owner 手里(W36 邮件,09-06) |
| 那下一轮做什么? | **不发波**,围栏当轮现跑求值、**不许抄 `$0.292` 或任何数** |

⭐⭐ **「等重置」这句话里藏着一个数。** 围栏 = **下一个尚未跨过的 ACTUAL 告警档**(RULING 1)。
今天 MTD `$74.308` 已越 `$50` ⇒ 围栏读 `$80`;**10-01 归零后 `$50` 变回「未跨」⇒ 围栏回到 `$50`。**
**不是我的算术,是那件工具的离线读数**(操作数由我给):

```
fence            : $50.00   <- lowest ACTUAL alert not yet crossed (Ruling 1)
operative ceiling: $50.00   = min(fence, brake)
headroom         : $48.900 after this wave
```
⚠️ 同一次输出里工具自打两行免责,一并抄来不抹掉:`source : OFFLINE ... a launch must use the AWS read`、
`accrual check : SKIPPED, NOT CERTIFIED (offline mode ...) $0.000 below is an assumption, not a reading`。
⇒ **`$48.900` 是重构不是预报**;10-01 那天的数**必须当场跑活路径求**。

⭐ **为什么这一条值得当主轴之一**:「等重置」听起来像「产能全回来」,实际给的是 **`$50`,少 `$30`,
失效方向往多花钱那一侧** —— 与 `wave_fence.py` 头注释立案的缺陷**是同一个**(派生值被当字面量记住,而输入自己按月在动),
**只是晚了一个月、换了一个人**:上次是 `$80` 被缓存,这次是「重置 ⇒ 回到 `$80`」被默认。

⇒ 连带推论:若外来支出仍按 09-05/09-06 的节奏计费(上一轮实测占 MTD **54.9%**,
`$74.308 × 54.9% ≈ $40.8` / 9 天 ≈ **`$4.5`/天**,⚠️ **这是把区间平均摊开的算术,不是实测日曲线**),
十月那 `$50` 会在**十天出头**被吃光。**「等重置」不是计划,是把同一件事推迟三周再发生一遍。**

## 三、升级:**不是新问题,是旧问题到期**(§GI.4)

⛔ **本轮不发新邮件。** 这个问题已经在 owner 手里:`DECISIONS_NEEDED` **第 15 条**
(给预算加 `Project` 过滤器 —— 原话「**这一条不是钱的问题,是「我们的刹车会被别人踩下去」**」),
随 **W36 汇总邮件 2026-09-06T07:xxZ 已发出**。本轮到达的是**它的后果**。
把后果重新包装成新问题去消耗 W37 的那一封,是**用一封邮件买一个已经买过的答案**。

⇒ 以**增量**追加进第 15 条(两个数:`headroom $0.292`;重置后 `$50` 而非 `$80`),随**周日 2026-09-13 的 W37** 发。
⭐ **提前发信的触发条件写死在档案里**,免得下一轮凭感觉判:
(甲) 多区域首次读数**真的**在别的区域读到外来实例在计费;
(乙) MTD 在本农场**未发任何波**的情况下越过 `$80`。

## 四、交棒 3:W61 四粒种子**退回可用窗口**(§GI.5)

`10208 / 10212 / 10390 / 10526` **退回**,**不建任何「烧过的种子」排除表**。

**判据是语料,不是点名**:重用一粒种子**唯一**的害处是同一 seed id 下**两套臂串的局被并进一次池化读数**
(铁律 4 (i-d) 同族),而那**要求语料存在**。W61 存了**零个**对象(两条独立读数:
S3 `validation/` 末条仍是 W60 第四台 `10:22:37Z`;`seed_roster_index.py --build` 打 `401 already indexed, 0 to scan`)。

⭐ **而这个判据不需要新建** —— 选种前那次 `--build` 之后的**交集检查本身就是它**。
索引从 S3 建,而每一条把语料读回来的下游路径源头也是 S3(`recover_verdict.py` 的 pool 目录是 `aws s3 cp` 下来的)
⇒ **「不在索引里」⟺「对每一条下游读数都不存在」**,正是重用无害的**充要条件**。

⛔ **不许再维护第二份手工名单**:那会在索引之外再造一个由人维护的『用过』概念 ——
正是 `wave_fence.py` 头注释立案的那个形状。**批测台右移窗口做得对**(零成本,且拒绝用花钱的方式替总监把未裁的问题定死),
**但不构成先例**:每损失一波就永久退休四粒,种子池天花板(GH #285 已催十二轮)会因为
**一次没有产生任何语料的损失**而下降。
⚠️ 失效方向如实记:哪天有一条读语料的路径**不经过 S3**,本裁定的前提就破。

## 五、投递(2.5)与登记表

**每一条裁定都落到了对方那一轮真正被驱动的那张表上,不是只落进档案:**

| 裁定 | 机器字段 | 档案 | 通知 |
|---|---|---|---|
| 种子退回 | `W61_wave.json:director[1]`(该记录就是划掉它们的那张)+ **`batch-desk.md` 章程 5 选种正文**(每轮驱动) | `test_set.md §GI.5` | GH #677 追评 |
| 不发波 / 重置后是 `$50` | `W62_wave.json:director` + `harvest_notes`(**下一轮驱动收割的就是这张**) | `§GI.1`/`§GI.2` | 同上 |
| RULING 5 | **`wave_fence.py` 自己**(批测台真正会跑的那个工具)+ `batch-desk.md` 闸 (iii) 正文 | `§GI.3` | 同上 |

`tests/test_wave_gate_keys.py` 改完当场跑:**406 checks / 0 failed(`RC_EXIT=0`)**。

**`owed_executions.json`:33 → 33(+1 −1),而这一进一出本身是本轮的一个发现。**

⭐⭐ **退休 `wave_fence_ruling4_first_live_run` —— 它在 2026-09-08T03:16Z 就已经被满足了,在 registry 里多待了八轮。**
结清判据要的是「任一轮批测台报告贴出 `accrual check :` 那一行」;首次满足是
`iterations/reports/batch-desk/20260908T031600Z.md:76`,此后**连续八轮各贴一次**。
⚠️ **`kind=manual` 的失效形状被它自己量出来了**:门是人,而人这八轮读的是别的东西;
自检的 owed 腿每轮把它打成 OWED —— **打对了产物,打错了世界**。
⭐⭐ **而它买到的东西比它要买的多**:那八行读数**每一行都写着 `account-wide`** ——
一条为「证明活路径跑过」而设的义务,在总监**没有 AWS 凭据、一次调用都没发**的情况下,
把那条路径的**措辞缺陷**交到了手里。**§一 的来路就是这里。**
⛔ **两件事不许合并成一句**:义务达成(路径跑过了)与读数标错(它标成账户级)是两回事;
**那八行此刻仍然只覆盖 us-west-2**,退休本行**不追认**它们是账户级的零。

**新开 `wave_fence_ruling5_multiregion_first_read`**(executor 批测台,`kind=manual`)。
⭐ 结清判据写成**逐字串**(`accrual scope :` 那一行的原文,两种形状之一),
**理由就是上一根棒刚量出来的教训**:manual 的提醒被复读八轮而没人执行它 ——
一眼判得出的判据,比要人回忆自己上轮想过什么的判据便宜。
⚠️ 本行风险更高:**下一轮是收割轮,不发波,闸 (iii) 结构上可以整轮不跑** —— 已写进 `W62_wave.json` 与章程。

## 六、验证(铁律 6)

- **静态半**:`bots/` + `game/` **一行未改** ⇒ 本轮无 Lua 变更;push 时 `.githooks/pre-push` 仍会各跑一次门(读数见 §八)。
- **python 全套(改后)**:**`118 passed, 0 failed, 1 uncertifiable`**,`RC_EXIT=2`。
  ⛔ **2 是「没跑成」不是通过**:`uncertifiable` 是 `tests/test_selfcheck_lua_leg.py`,
  **GH #358 的 120s 第四次吃掉整条腿**(本轮 `87 file(s) ... in 120.1s`)。
  ⚠️ 与上一轮不同的是,**这一次我没改那个 wrapper**,所以连「结构完好有旁证」都不必声称 ——
  **那条腿这轮没人看过,如实登记。**
- ⛔ **Lua 全量(~100min,GH #124)未跑,不声称。**

## 七、自检(铁律 10)

⭐ **纪律 3 第三十四发,守卫连续第八轮自拒**:本轮**第一条命令**又是
`routine_selfcheck.sh 2>&1 | tail -40`,脚本当场自拒(`REFUSED: ... exit 2, nothing checked`),
而同一行 harness 打的 `SELFCHECK_EXIT=0` 是 `tail` 的。**章程第 0 步写的就是 `rc.sh`,我没照做** ——
与 09-04、09-09T19:07Z 两发**逐字同型**。⇒ 这条第三次记同一句话:**它是习惯不是门**。

改 `rc.sh` 重跑,**跑完了,真码 `EXIT=3`**:
- `legs run 11`,工具末行 `selfcheck worst exit: 3`;
- `FINDINGS (exit 3)`:`cadence owed-executions a-evidence-owed`(**照工具自己的警告,只登记来源不做归因**,GH #267);
- `UNCERTIFIABLE (exit 2)`:`trunk-red(python)` —— 又是 GH #358 那 120s;
- `NOT RUN (inside a leg)`:`tests/test_luacheck_gate_soakswitch.py`、`tests/test_selfcheck_lua_leg.py`;
- Lua 检测器腿 **87 file(s) / 0 failures(FAST SUBSET)**。

⚠️ **它开工时起跑,早于本轮编辑落盘** ⇒ **不覆盖我改的四个文件,不拿它冒充**。
覆盖那四个文件的是 §一 与 §六 里那几条当轮读数。

⭐ **「后台包装吞真码」本轮没有复发,如实登记正面**:harness 报 `[exited with code 3]`,
文件里 `RC_EXIT=3`,工具末行 `3` —— **三个数字并排,三个都对**。
前十四次的守卫仍然没立,顺延;**但这一次它不是被绕过的,是没发生。**

## 八、Push / 发表

(见本文件末尾「收尾」节 —— push 与 `claim_precheck` 的读数在提交后补写。)

## 九、本轮没有声称的东西

1. **零 AWS 调用**:MTD `$74.308`、`headroom $0.292`、外来支出占比 `54.9%` **全部是转载批测台的读数**。
   ⚠️ 那 54.9% 上一轮就登记为**未复查**,**本轮仍未复查**。
2. **没有证明外来支出「停了」或「还在继续」**;§二 那条 `$4.5`/天是摊平算术。
   RULING 5 让这个问题**下一轮免费可答**,本轮没答。
3. **无任何 promote / reject**,armed 串 37 未动;`PROMOTE_BAR_PAIRED_SEEDS =` 仍欠着。
4. **没有给闸 (ii) 写可执行体**(`wave_reachable_delta.py` 仍不存在)——**第三轮顺延**,
   ⚠️ 上上轮自己写过「第三轮不许再用时效性挤掉 #540」的同型自缚,这一条**现在轮到它自己**:
   下一轮若再顺延,要按「第四轮」写进报告并说明原因。
5. **没有退休 `a_evidence_pulldrag` / `a_evidence_tpgap` 两行**(自检报它们 `DONE`,逐字
   `-> executed; the director should retire this row`)—— 退休它们要连带读两份录像组报告并裁 id,
   那是**下一轮的一个完整工作单元**,不是本轮的尾巴。**这是它们的第一轮顺延。**
6. 🩺 巡检:五组均有产出(batch-desk 21:08Z / hero 21:16Z / strategy 19:28Z / replay-check 19:45Z),
   **无掉队组**;owner 优先项无 12 轮零推进的升级项。

## 十、下次触发

1. ⭐⭐⭐ **退休 `a_evidence_pulldrag` / `a_evidence_tpgap` 并裁那两个 id**(自检逐字点名,**第一轮顺延**)。
2. ⭐⭐ **`wave_reachable_delta.py`**(闸 (ii) 可执行体)—— **第三轮顺延,下轮记「第四轮」**。
3. ⭐⭐ 核 `wave_fence_ruling5_multiregion_first_read`:批测台有没有贴 `accrual scope :` 那一行;
   若读到**别的区域有外来实例在计费** ⇒ 按 §GI.4 (甲) **提前发信,不等周日**。
4. ⭐ 裁 `PROMOTE_BAR_PAIRED_SEEDS =` 那个数(卡着闸 (ii) 第三条腿)。
5. ⭐ `gh454_cost_constants_rerule`(先决条件:`check_costs.sh` 里 09-09 两行不再为空,21:30Z 之后应已入账)。
6. ⭐ 核 `a-evidence-owed` 的 UNOWED 12 有没有在降。
7. GH #672(`reclaim_blind.py` 假理由行)/ GH #664(AZ 环)/ backlog 101/102/103 /
   「后台包装吞真码」守卫(**本轮未复发,计数不进**)/ GH #358 的 120s **第四次**吃掉整条腿,要人裁。
8. 存量:账户级预算等 owner(W37 周日)/ GH #523 / patch 缺口 P3 / `hero_domain_scan` 九份 /「退集·promote 五处同步」。

## 十一、Token 用量

(收尾补)
