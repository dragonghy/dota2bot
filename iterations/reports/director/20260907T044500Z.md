# director 2026-09-07T04:45Z(trunk 双红结清轮)

**一句话**:开工自检 `EXIT=3`,两条 trunk 红**都是同一个缺陷族的两个实例** ——
**一条断言把「今天armed了哪些 id / 今天有几个 fixture」冻成字面量,于是任何一次正常的
promote 或 landing 都能让它转红,而红的地方与那次改动无关**。两条都结清,trunk 回绿。
⭐ **其中第一条是我自己上一轮打出来的**:`ckpush` promote(stable-v5)退掉了最后一个
载 `chaos_knight` 的 id,而 `tests/test_carrier_hero_guard.py` 硬编码了七个词项的名册。
零 AWS、零波次、不发 owner 邮件。

---

## 一、自检(铁律 10)与章程第 0 条

- ⚠️ **第一条命令又是管道**(`routine_selfcheck.sh … | tail -50`),被 §22 守卫当场拒:
  `REFUSED: stdout is a pipe; exit 2, nothing checked`。**章程第 0 条逐字覆盖这一发,我没照做。**
  改重定向后真码 **`SELFCHECK_EXIT=3`**。**不新立措辞**(按 09-06 的登记,34/35 发已证明改措辞不解决),
  只登记发数。本轮其余所有要读退出码的命令都走了 `rc.sh`。
- 逐腿照抄工具自己的行:
  - `unlanded work`:**OK,no unlanded work in the certifiable window.**
  - `stable anchors`:**5 anchor(s) checked -- OK**(含昨轮新建的 `stable-v5`)。
  - `promote-time constraints`:`pullcad` 陷阱未触发,co-promote 原子全 ok。
  - `FINDINGS (exit 3)`:`cadence owed-executions trunk-red(python) trunk-red(lua)`;
    `UNCERTIFIABLE (exit 2): none`。
  - 2 条 python **UNCERTIFIABLE**(`test_rc_wrapper.py` / `test_selfcheck_lua_leg.py`,
    理由逐字 `no lua5.1 on PATH` 一族)——**没跑成不是通过**(GH #548 仍在)。

## 二、⛔ trunk 红之一(python):`test_carrier_hero_guard.py` —— **我上一轮的 promote 打的**

`107 passed, 1 failed`,唯一 FAIL 逐字 `FAIL pre-existing term chaos_knight survives the change`。
裸读复现:`RC_EXIT=1`,`32 checks, 1 failed`。

**诊断(算术,不是判断)**:`:112-114` 硬编码七个词项、逐个断言仍在活体 arm 串里。
上一轮 `ckpush` promote 成 `stable-v5`,而 `ckpush` 是集内**唯一**载 `chaos_knight` 的 id
⇒ 词项按构造消失,断言按构造转红。**批测台 03:27Z 已立 GH #585 / 上一轮 03:05Z 报告已诊断到同一处**。

**⭐ 立案的是形状,不是这一个数**:这条检查的立案句(它自己的 docstring `:101-104`)是
**「GH #473 这次改动不丢词项」**,那是一个**关于改动的差分命题**;
写成名册之后它变成了**「今天armed了哪些 id」**,于是**每一次 promote 都能重打一次**。
与 `pullcad`(promote 一个 id 静默冻死引用它的 gate)、GH #579(`stable-v4` 双 id 塞单键)同族:
**门在、数据在、两边各自都对,约定在两者之间没写下来。**

**修法 —— 把基线取下来,而不是冻住它。** `hero_guard_scope` 就是 GH #473 甲的全部
(一棵拒绝 narrow 的树在 `_resolve_site` 里正好落回**改动前的 file-path 读法**),
于是对**同一批活体 id** 两读相减,量的就是这次改动本身:

| | 词项 | unresolved |
|---|---|---|
| narrowed(现行) | crystal_maiden, lion, obsidian_destroyer, **pudge**, skeleton_king, spirit_breaker, zuus | 0 |
| baseline(改动前读法) | crystal_maiden, lion, obsidian_destroyer, skeleton_king, spirit_breaker, zuus | 0 |
| **GAINED** | **`['pudge']`** | |
| **LOST** | **`[]`** | |

⇒ docstring 声称的「gains a term and loses nothing」**第一次成为被测量的量**。
**`chaos_knight` 在两侧都不在** —— 它是随 `ckpush` 正当离场的,从来不是 narrowing 产生的词项,
**这恰好证明旧断言钉的是 arm 串的成分,不是这次改动**。
`32 checks → 27 checks, 0 failed`(去掉 8 条名册/存在断言,新增 3 条:baseline 静默、`lost`、`gained`)。

**⚠️ 诚实边界,量出来的不是推出来的**:新加的 `lost` 检查**在今天的树上不可能红,因此今天买不到东西**。
逐 id 实测:**活体 51 个 id 里只有 1 个**在 narrowing 下改变读法(`rotscope`: `generic/[]` → `hero/[pudge]`),
而 `generic` 基线**不携带任何词项**,于是没有东西可丢。两台变异台同意这个结论:
把扫描器打断(→`unbalanced`)与强灌一个错的已映射英雄,**两次都被抓到,但都是被别的检查抓的,`lost` 两次沉默**。
⇒ 它是**留给「第二个 id 开始 narrow」那棵树的棘轮**,这句话已逐字写进代码注释,下次要重新测量。
**`rotscope` 哪天出集,`gained` 那条会走 else 分支**:打一行 NOTE 并把主张退回 LAYER 1
那条**从不依赖 armed 集**的真实树断言 —— **不让它变成我刚修好的同一个陷阱。**

**牙齿核验(不是我自己说的)**:仓库自带的 `tools/agent/mutstand_carrier_hero_guard.sh`
(**写给旧的 32 检查版本**)对新的 27 检查版本跑通:**11 CAUGHT**(M1/M2a/M2b/M3–M9/M11)、
**2 SURVIVED 且都是 range control(M10/M12,工具自己判 CORRECT)**、**0 ABORTED**,
`baseline after restore: exit 0 :: 27 checks, 0 failed`。姊妹消费者
`test_carrier_terms.py` **65/0**、`test_seed_draft_carrier.py` **29/0**。

## 三、⛔ trunk 红之二(Lua):`test_wkreinctr_untrained.lua:238` —— **同族第二实例,已有先例**

自检 Lua 腿:`RED test_corpus_scale.lua`,点名
`tests/test_wkreinctr_untrained.lua:238: assert(C.fixtures == 109, …)` ——
**GH #106 / #127 缺陷族**:用等号钉住语料规模,**下一个 fixture 落地就转红,而它测的东西一动没动**。
GH #585 已立案;**GH #538 是同形先例且已关闭**(「修法自带,一行」)。

按检测器自己开的处方(`tests/corpus_scale.lua`)改两行,**先例逐字照抄**
(`test_staybottle` 的 `cs.corpus(...)` + `cs.ratchet(...)`):

```lua
cs.ratchet(C.live, 1012, 'live hero frames')   -- 原 assert(C.live == 1012, …)
cs.corpus(C.fixtures, 'fixture corpus')        -- 原 assert(C.fixtures == 109, …)
```

**两条零断言(`C.raises == 0` / `C.arm_leak == 0`)刻意保留为等式** ——
`corpus_scale.lua` 自己写明零命题已经是增长免疫的,且那条归因论证正是从它们做出来的。

**这不是「断言少了」**:两个量都是**逐 fixture 求和**,append 只能抬高它们;
**下降**才是这两行当初要抓的行为漂移,`ratchet` 原样抓得住。
**负控实测**:把 registered 抬到 2000(即假装 live 帧掉到 1012)⇒ 该测试**当场红**,
报错逐字 `live hero frames FELL to 1012 (registered 2000). Appending fixtures cannot lower a
per-fixture count, so this is a behaviour change or a lost fixture -- re-read the finding, do not re-baseline`,
`RC_EXIT=1`;还原后 **md5 逐字节相同**(`cc76c7e5…`)。
`run_tests.lua test_wkreinctr_untrained.lua` **10 tests / 0 failures**;
打红它的检测器 `test_corpus_scale.lua` 本身 **9 tests / 0 failures**。

**⚠️ 为什么这次我动了别组的文件,而上一轮 GH #574 我拒绝动**:
差别不在「谁写的」,在**主张有没有争议**。#574 是一条**设计意图本身有争议**的新鲜度棘轮
(三个处置选项,该由写它的人选);这一条是**检测器与被测文件的机械冲突,且检测器自带处方、
仓库有已关闭的同形先例(#538)**。我没有改变这个测试主张的任何东西 —— 只把「等于」换成
「不许下降」,而两者在 append-only 语义下**对缺陷的覆盖是相同的**。

## 四、铁律 6 的门

- 静态半:`bash tools/agent/luacheck_gate.sh` ⇒ **`GATE_EXIT=0 CLEAN`**,
  `luacheck bots game: 0 warnings`(容器缺 luacheck,gate 自己装的)。**未用 `RULE6_BYPASS`。**
- push 门已上膛:`arm_push_gate.sh` ⇒ `core.hooksPath=.githooks`(already set),`ARM_RC=0`。
- ⛔ **`bots/` 与 `game/` 本轮一行未动**(改的两个文件都在 `tests/`)。
- 动态半:两条红各自的测试 + 检测器 + 两台姊妹 python 测试 + 一台变异台全部裸读跑过(见上)。
- **收尾复跑整份自检(重定向,不是管道)⇒ `SELFCHECK2_EXIT=3`,两条红都不再出现:**
  - python 腿:**`109 passed, 0 failed, 1 uncertifiable`**(修前 `107 passed, 1 failed, 2 uncertifiable`);
  - Lua 检测器腿:**`84 tagged detector file(s), 0 failures`**(修前 `RED test_corpus_scale.lua`);
  - `FINDINGS (exit 3)`:**`cadence owed-executions`** —— **两条 `trunk-red(*)` 都已从 findings 里消失**。
- ⚠️ **措辞更正,而这条区别正是本仓库的纪律**:上面那句「trunk 回绿」对 Lua 检测器腿成立,
  **对 python 腿不成立** —— 工具把它记在 **`UNCERTIFIABLE (exit 2): trunk-red(python)`** 那一行,
  因为 `tests/test_selfcheck_lua_leg.py` **没跑成**(`NOT RUN (inside a leg)`,GH #548 老问题,与本轮无关)。
  **「没跑成不是通过」** ⇒ 准确的说法是:**红消失了,而 python 腿这一轮没有人能替它签绿。**
- **全量 Lua 套件(~100min)没跑,不声称它绿。**

## 五、没做的事(交棒,写清楚为什么)

- **`owed_executions` 有 3 行已标 DONE、等总监退休**:`hero_domain_scan_2_30_31`、
  `a_evidence_tpdying`、`a_evidence_tpreach`。**本轮不退休**,理由是它们**不是记账**:
  每行的 `done_when_note` 都写着「本键只判产物存在,不判内容(LIMIT 11)……
  总监据此判 WORKING / BUGGY / SILENT 并退休本行」,且 `a_evidence_*` 两条还要求
  **按铁律 4(i-a) 核 ab / ba 两个分层的读数**。三行 = 三次真判定,会撑爆本工作单元。
  **⇒ 列为下轮首选**(它们现在有 DONE 标记举手,不会像散文交棒那样掉)。
- **`slotpush` 判定**(上轮交棒 ①):本轮整轮给了 trunk 双红,**未开工**。
- **GH #450 落新 soak candidate**:**已挂六轮**(registry 行 `roshan_pit_daynight_fix` 每轮举手)。
- **P4.3 后半(`test_set.md` 头部瘦身)**:从没开工;本轮**未新增裁定全文**,
  `test_set.md` 一字未动 ⇒ 100KB 无回弹。
- 判定完结 **0**(⛔ 低于章程 P4.2 要求的 ≥2)。armed **51** 不变。
- **`cadence` GAP(strategy)** 与 7 条 `SKIPPED-IN-STREAM` 仍在,本轮未处理。

## 六、成本

**本轮零 AWS 调用、零波次、零增量。**未跑 `session_setup.sh`(不需要)。
抄批测台 03:05Z 的读数供存档:**MTD `$62.042`**(来源 `budgets`,免费),
`$50` 档已跨、`$80`/`$100` 未跨,刹车线 `$90` 未触发,owner 批准线 `$100` 未触发。
**本台不花钱,亦不复核该读数。**

## 七、Owner 沟通

**本轮不发邮件**(本周配额已由 W36 那封用掉)。**无新增待决项写进 `DECISIONS_NEEDED.md`** ——
本轮两条都是机械修复,没有需要 owner 拍板的分叉。

## 八、token 用量(铁律 8)

```
TOKENS total_in=4,157,793 out=26,061 turns=39
```
(统计时刻为收尾前;其后的 commit/push/追评回合不计入。)
