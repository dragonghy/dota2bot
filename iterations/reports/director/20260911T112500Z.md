# 总监 2026-09-11T09:5x–11:2xZ —— **第八、九次 promote 同轮落地(`tpdeathbuy` + `liondrainstop`),armed 34 → 32**

树:开工 `29e1768`(= `origin/main`)。零 AWS(**一次调用都没有**)、零波次、**不发 owner 邮件**。
`bots/` 有 diff:**两处,各一句 gate**。

**判定完结 2** —— owner **P4.2** 的产出指标,**达标**;上一轮是 0,且上一轮的「下次触发 ①」逐字写着
「判定完结,主体,≥2,**不许再让位给仪器工作**」。本轮照做:仪器活(§7)**被登记而不是被执行**。

取活依据:上一轮「下次触发 ①」逐字 ——「最便宜的一档是 **WORKING 九条 CLEAR**,先读 §DU.6 红线,
再各自补 (b)(c);⛔ `fieldbuy` 在 GH #734 落地前不 promote」。

---

## 〇、一句话

`tpdeathbuy`(出厂那段代码从上游快照带来那天起**一次都没跑过**,因为它的合取**对任何实数为假**)与
`liondrainstop`(Lion 顶着英雄伤害拉满一整条 Mana Drain,而出厂的释放路径**按构造对这一类永不触发**)
**同轮 promote**,armed **34 → 32**,锚点 **stable-v7**。
两条的 (b) 是**同一份**五波家族读数 —— 这不是巧合,**正是它们必须同轮的理由**。

---

## 一、⭐⭐⭐ 本轮最该被读的一条:一个变异体连活两轮,而第二次存活不是第一次的弱化版

`mutstand_promote_20260911.sh` 的 **M9**(把 `X.nEDrainDangerRadius` 换成字面量 2000)**连续存活两轮**,
而两次教的完全不是同一件事:

1. **第一轮存活 ⇒ 根本没有使用点断言。** 本文件已有的 `radius:` 用例钉的是**声明**
   (`X.nEDrainDangerRadius ∈ [484, 781)`),M9 改的是**使用点**。
   §GU.2 条件 (c) 里那句「共用常量 ⇒ 一次重调会在**两侧**自报」,在那一刻**是一句没有任何东西守着的话**。
   —— charter 0ASYM (iii)「把事实钉在使用点不是声明」的又一发。
2. ⭐ **第二轮存活 ⇒ 我修的是另一个函数。** 补上使用点断言后 M9 **照样活着**。
   那一行 `local nCloseEnemyList = J.GetNearbyHeroes( hBot, X.nEDrainDangerRadius, true, BOT_MODE_NONE )`
   **在两个守卫里逐字节相同**(`hero_lion.lua:1886` 起始 / `:2031` 停止),而 `replace(old, new, 1)` **落在第一个**
   ⇒ 我的断言覆盖停止守卫,M9 打的是**起始守卫**。
   **第二次存活因此是一个独立的发现:第二个没被钉住的使用点** —— 而唯一让人去看它的原因是**变异体拒绝去死**。

📌 **可迁移的一句**:*一个「已经修好」的变异体再次存活时,先问它打的是不是你修的那个地方。
在一个有孪生代码行的文件里,「同一个缺陷」和「同一处缺陷」是两回事。*

处置:断言改成**两个函数体都查**(哪个都不是「附带的那个」),新增 **M13** 用唯一锚串专打停止侧
⇒ 这一对**量两个使用点,而不是把一个量两遍**。终读 **12 CAUGHT / 1 SURVIVED / 0 ABORTED**。

⛔ **M10 是登记在案的存活,留在台上说这句话**:它把 `[promote]` 用例的 id 禁令松成恒真,而 TESTS[] 里
没有邻居重新推导那条禁令 —— **一个文件抓不住自己的断言被放松**(`mutstand_ckpush` M9/M10、
`mutstand_slotpush` M7/M8 同族)。⚠️ **它不是「promote 被悄悄撤销没人发现」** —— 那是 **M1,M1 被抓住**;
M10 严格是二阶的。**照实登记,不粉饰**:删掉这一发换一个 13/13 的读数,等于删掉「这个缺口存在」的唯一记录。

## 二、为什么两条同轮 —— 这不是「攒够了」

⛔ 先说清它**不是** `lanefix` 那个形状(一次上十条互相耦合的守卫,两次把胜负打负)。
两条落在**互不相交的子系统**(采购块 / Lion 引导释放),代码上不共享一行,域各自个位数百分比。

⭐ 真正的理由是**它们的 (b) 分不开**:两条在 W55/W58/W62/W63/W64 **每一波都同时 armed**,
家族级读数**结构上**无法把经济归给其中之一。
⇒ **分开 promote 反而更糟**:先促其一,稳定版从此含前者、而后者还留在臂上 ——
**那个组合从来没有任何一波跑过**。同轮 promote 交付的恰好是被测过的那个配置。

## 三、两条裁定的三条件(全文 `test_set.md §GU.1/§GU.2`)

| | `tpdeathbuy` | `liondrainstop` |
|---|---|---|
| (a) | WORKING **14**(09-09T01:02Z,两台独立量具同批帧一致) | WORKING **36**(09-09T18:40Z);32/36 在谓词成立那一帧结束 |
| (b) | **同一份读数**:5 波 gpm −22.72 / +40.58 / −7.20 / −15.00 / −8.96,**均值 −2.66**,**722 计分局** | **同左,逐字同一份**(这正是它们必须同轮的理由) |
| (c) | **算术**:`botHP < 0.08 and botHP >= 1` 对任何实数为假 | **结构**:生根的引导不在回家路上 ⇒ 出厂 `IsRetreating` 释放永不触发 |

- ⛔ **(b) 的边界照抄不改**:家族级不是 id 级;全开波无法归因到单个成员;
  **五波 winrate 通道全 DEGENERATE ⇒ 没有任何胜负读数可引**。
  它过的是铁律 2(b) 的**粗粒度**门,**不是正面证据,不许当正面证据引**(§FT.2 措辞,故意照搬)。
- ⭐ **`liondrainstop` 的 (a) 里承重的是域外阴性对照**:域外引导平均长度
  **3.178 / 3.375**(armed ab/ba)vs **3.119 / 3.125**(baseline ab/ba)—— **两腿无方向**;
  域内 1.653 / 0.995 vs 2.434 / 2.940 ⇒ **缩短只发生在域内**,任何「Lion 整体少拉一会儿」的解释
  都会同时压低域外那一列,而没有。同臂 `lionqdmg` 单独排除:**29/32 切断点上 Lion 一个技能都没放**。
- ⭐ **`tpdeathbuy` 的 (c) 是算术不是判断**:那约 12 行**从上游 OHA 快照 `74727e4:957-958` 带来那天起
  一次都没跑过**,所以本项目**任何一次测量里都不含它**。姊妹的死前买粉块写 `botHP < 0.06` 且
  **没有配对下界** —— 这是「杂散合取项而非惯例」的判据。外部依据:死亡烧的是不可靠金,
  **已在物品栏里的卷轴不会被死亡拿走**。

## 四、⚠️ `fieldbuy` 与 `liondrainstop`:两条都带着开着的 issue,只有一条被挡在门外

这是本轮必须写清的一条分界线,因为**它很容易被下一个读者读反**。

- **`liondrainstop` 带着 GH #314 落地,而这是刻意的**:4/36 域内引导**被保持而不是被切断**。
  那是**覆盖不足,不是有害** —— 在那些帧上 promoted 腿与出厂树**逐字同为不切**,promote 不可能让它们更糟。
  根因候选是雾(门的敌人名单走 `J.IsValidHero -> CanBeSeen`,普查是全知的),
  **在 dumper 携带分队视野之前离线不可判**。
- **`fieldbuy` 今天仍不 promote**,尽管它是 (a) 最厚的一条(785 episodes):
  GH #734 给它量到了**价钱** —— 买回的药剂 **61.1% 先落背包、18.0% 卡死喝不着**。

⇒ **判据不是「有没有开着的 issue」,是「有没有量到价钱」。**

## 五、⚠️⚠️ [harness] 现场:管钱的那道闸今天在 trunk 上读不出自己的锚

`W65_wave.json` 今晨 **09:15Z** 落到 main,带 `launch_time` 而**没有** `launched_at` ——
**25 波以来第一次**(W41..W47 只有 `launched_at`,W48..W64 两个都有、逐字同一瞬)。
`wave_throttle.py` **只读 `launched_at` 且拒绝猜** ⇒ 开工自检读到
`UNCERTIFIABLE: W65 machine 0 (seed 10926) has no launched_at` / `gate (i) DID NOT RUN`。

**本轮修复**:按该记录**自己的 `launch_time`** 补回四条(每条另有 `run_id` 的 `spot_20260911_0923xx`
时间戳佐证,**没有发明任何一个瞬间**),并留 `launched_at_repair_20260911` 说明来源。
修后 gate (i) 读 `THROTTLED (exit 3) … unlock 2026-09-11T15:23:44Z` —— **正确答案,而它此前取不到**;
`test_wave_throttle.py` **55 checks 0 failed**。

⭐⭐ **而真正该被读的是下面这一层。** `tests/test_wave_throttle.py` 实测 **0.671s**,
远在 py 闸 3.0s 单测上限之下,却**不在推送闸里**,原因是 **`over_cumulative_budget`** ——
同样出局的还有 18 条,合计 **24.3s**。
⇒ **本该当场拦住那次 push 的棘轮,是被预算挤出去的**;红于是由**下一个开工的组**几小时后发现,
**逐字是 GH #624 的立案形状,发生在本该关掉它的那条腿内部。**

⛔ **前提已经过期,而这是事实不是偏好**:`py_gate_measure.py` 文件头自述 `BUDGET_SECONDS=12.0` 的理由是
「Lua 静态半冷启 18s,**这一半不该让它翻倍**」(GH #616 的验收句,**09-08** 写下);
**GH #624 于 09-10 加了第三条腿 `lua_gate.py`,本轮实测 329.9s** ⇒
旋钮仍对着一个 18s 的钩子标定,而定它的那条约束已被一条大 **17 倍**的腿盖过。

⚠️ **本轮不改,理由是容器不是时间**:重跑 `py_gate_measure.py` 需要**安静容器**
(它自己的文件头记着 09-08 并发把 `test_carrier_hero_guard.py` 翻成 rc=1;
上一轮总监刚因并发作废过一份 lua gate 读数),而本轮容器被两条 promote 与三条腿的推送闸占满。
⇒ 登记 `owed_executions.json:py_gate_budget_premise` + `state.json:py_gate_budget_premise_20260911`,
验收句**裸读得出**:manifest 里 `over_cumulative_budget` 条目数为 **0** 且 `test_wave_throttle.py` 的
`in_gate` 为 `true`,**且必须由工具重跑生成** —— ⛔ 手把那一条捞进 manifest 就是**按名字选**,
正是 GH #616 约束 1 禁的那件事。

## 六、⭐ 推送闸抓到的两件事,都是**本轮自己的**

1. **`test_gated_helper_nesting_census.lua` 当场变红**:六行的外层 id 集合是
   「每一个 gate 了本 Think 任一块的候选」,promote 掉 `tpdeathbuy` 就把它从六行里都拿走 ⇒
   `fieldregen,tpdeathbuy` → `fieldregen`,census 断言「语料里的集合恰等于已被读过的集合」于是顶红。
   **这是 GH #624 那条腿在工作,不是误报** —— 红**reached the person who caused it, in the same round**,
   不是下一个开工的组几小时后。**重钉而不是赦免**;六行的 (W) 判词一字未改,
   因为它们**从来没有一条靠 `tpdeathbuy`** —— 那些行自己的注释就是这么写的。
2. **`carrier_terms.py` 的半个状态自己变红**:代码已改、串未改的中间态上,
   旧 34 串读 **2 unresolved / 退出码 2**,新 32 串读 **0 unresolved / 退出码 0**。
   载体项 **7 → 7 逐字不变**(`lion` 由 `lionqdmg` 接住)⇒ 选种解空间不受影响。

⚠️ **另一件是我自己的读法错误,记下来**:我把三个 lion 测试**带 `.lua` 后缀**一起跑,读到
「24 tests, 0 failures」就当作三个文件全绿。**并没有** ——
`test_replay_260820_lion_drain_stop_pair.lua` 的 `gate OFF` 用例是红的,
是变异台的 **BASELINE RED** 把它逼出来的。**一个聚合的绿数字,不等于名单上每一项都绿。**

## 七、倒像与正像,两个方向都查过(RULING 13 的房规)

- **倒像**:`inverse_gate_census.py` 本轮 `live gate ids 197 / armed 34 → FROZEN 0、COUPLED 1、UNRESOLVED 0`
  ⇒ 两条都 **CLEAR**,promote 不冻死任何域。(唯一 COUPLED 仍是 `fieldsip`。)
  ⭐ 这条腿本轮**第一次在真实自检里跑出读数**(`legs run 12`)—— 上一轮「下次触发 ②」的前半。
- **正像(`pullcad` 陷阱)**:两条**各自恰好一个** gate 点,门行上**没有第二个 id**,
  `promote_atoms.json` **零次**点名它们。

## 八、同轮一条 queue 裁定:`hero-57` = **FROZEN-HOLD**

投递纪律 §2.5:裁定写进 `queue.json:hero-57.director` **机器字段**(不是 `question` 散文),
`test_set.md §GU.4` 留全文档案。P4.2 逐字:冻结期入集提议的唯一合法裁定是 FROZEN-HOLD;
该行虽写作「(a) 执行核验、搭车、零 AWS 增量」,**实质仍是入集**(armed/baseline 差要出现,`glyphany` 必须进臂串)。
⭐ **请求本身没问题,而且把最难的一半预先做完了**:它逐条量出归档扫描买不到 (a) 的**三重阻断**
⇒ **本裁定明确背书「不要按 ROUTED_ARCHIVE_SCAN 路由」**。
⚠️ **接力棒**:被 FROZEN-HOLD 的请求在 `pending_rulings.py` 里此后读 `ruled=True`,
于是**从所有待办表上消失** —— 拉野死分支 2026-08-19 那次(37 轮)就是这个形状。
已开 `owed_executions.json:glyphany_readmission_on_thaw`;解冻条件是**一个数**不是一个事件,
所以必须有东西替它举手。`queue-rulings` 腿本轮**由 exit 3 清到 exit 0**。

## 九、铁律 6 / 自检 / 成本 / 巡检

- **铁律 6 三条腿**(**未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行):

```
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
py gate: 96 ran, 0 findings, 0 uncertifiable, 33.0s
lua gate: 334 ran, 0 findings, 0 uncertifiable, 14 known-red, 329.9s
```

  ⭐ `known-red` **15 → 14**,本轮**没让它变大**。
  ⚠️ 第一次 `lua_gate.py` 是 **exit 3 / 1 findings**(§6 的 census),修后重跑取上面这一行。
- **自检**:`SELFCHECK_EXIT=3`、`legs run **12**`(第 12 条腿首次真实计入)。
  `FINDINGS: cadence queue-rulings owed-executions trunk-red(python) trunk-red(lua)`;`UNCERTIFIABLE: none`;
  `NOT RUN (inside a leg)`:`test_lua_gate.py` `test_luacheck_gate_soakswitch.py` `test_selfcheck_lua_leg.py` —— **三条不声称**。
  本轮清掉的:`queue-rulings`(§8)、`trunk-red(python)` 里的 `test_wave_throttle.py`(§5)。
  `trunk-red(lua)` 仍 **4/87**,逐条都是「钉住的数过期了」,族属 GH #718/#650,**不开新单**。
- ⛔ 开工第一条命令**又撞了管道拒绝门**(`REFUSED: routine_selfcheck.sh stdout is a pipe`)——
  **`rc.sh` 是习惯不是门,第十四次**。此后本轮所有读退出码的命令都走 `rc.sh` 或落盘重定向。
- 💰 **本轮 AWS 调用 0 次,不作 MTD 新声称**(它是时间的函数);三条线未动($60/$90/$100)。
  ⚠️ 但 §5 那条与钱有关:**下一次发波前 gate (i) 现在能读了**,此前读不了。
- 🩺 **巡检**:五组 24h 内全部有产出(batch-desk 09:15Z / hero 08:15Z / strategy 08:00Z / replay-check 01:08Z),
  **无掉队组**。⚠️ owner **P4.1 标尺波**仍未认领,已在 `DECISIONS_NEEDED` 第 16 条挂着,本轮不另发信。
- **armed 34 → 32**,离 P4.2 解冻线(≤20)还差 **12** 条。

## 十、下次触发

1. ⭐⭐⭐ **判定完结继续做主体(≥2)**。倒像半边已买好,剩下的 WORKING CLEAR:
   `zusult`(11)、`zusboltdom`(6)、`ownhalf`(7)、`wandbleed`/`wandbleed2`(1–2,太薄)。
   ⚠️ `zusult` 的 (a) 有一条**更早的 BUGGY 3**(09-04),根因是**未 armed 的 `zusboltcap` 让它持续漏气** ——
   promote 前必须先决定那条 BUGGY 是否已被 09-07 的 WORKING 11 取代,**不要只读最新一行**。
   ⚠️ `lionqdmg` 被 `promote_atoms.json:impale_kill_needs_damage` 挡住(prereq `lionqkill` 仍 gated),**不是候选**。
   ⛔ `fieldbuy` 在 GH #734 落地前仍不 promote。
2. ⭐⭐ **`py_gate_budget_premise`**(§5):一个常数 + 一次 `py_gate_measure.py` 重跑,**要安静容器**。
   ⛔ **不得再挤掉判定完结** —— 它是附带那一条,不是主体。
3. ⭐ `pullcamp_atom_readmission` 与五条 `a_evidence_*` 的首次活读数(**第三次顺延**)。
4. 存量:GH #523 / patch 缺口 P3 / 账户级预算过滤器等 owner /
   **`rc.sh` 是习惯不是门(第十四次)** / 「后台包装吞真码」。

## 十一、发表与 token 用量

- **本轮不发表任何 GitHub 评论、不开任何 issue**:§5 的 py 闸预算已进 `owed_executions` 与 `state.json`
  且族属 GH #616/#624(重复立案是 GH #718 的立案句);`trunk-red` 两族都已有单(#718/#650、#728)。
  ⚠️ **两条 promote 分别关联 GH #168(tpdeathbuy)与 GH #97/#314(liondrainstop)**,追评留给下一轮
  ——`claim_precheck.sh` 的顺序(GH #290)要求先 push 再发表,而本轮的发表窗口在 push 之后。

```
TOKENS total_in=33,990,759 out=108,474 turns=162
```

⚠️ 这是**统计时刻**的数字,统计之后的收尾回合不计入。
⭐ 与上一轮(`total_in=32,426,209 / turns=153`)相比,本轮**在两条 promote 的完整落地上多花了约 1.56M**,
而上一轮那个量级是**从零写一个普查工具 + 三次超时重跑**。**两次 `lua_gate.py`(各约 330s)是本轮
最大的一笔非思考开销**,其中第一次因 §6 的 census 变红而必须重跑 —— ⛔ **那次重跑不是浪费**:
它是推送闸在同一轮里替我抓到自己的红,正是 GH #624 要买的东西。
