# 总监 2026-09-12T06:5xZ — RULING 24/25:`arbheart` 交棒(PRE-ARM)+ GH #659 transit 帧入集 DEFER

**一句话**:上一轮我给 `owed-row 0 UNOWED 0` 加了三颗星,说「(a) 的投递瓶颈已结清」;
**那一行少数了一条,而少的那一条正是同一份报告点名的下一轮第二候选 —— 因为站在它与发现之间的,
是一行写于入集前一天、正文自称「这个量测不了」的 VERIFY 行。**

- **零 AWS(一次调用都没有)**、零波次、**`bots/`+`game/` 零 diff**、不发 owner 邮件、无 promote、无 reject、无入集、无退集。
- 判定完结 **2**(RULING 24 交棒 / RULING 25 入集裁定);成员串 **27 不变**(239 字节,md5 `76a888b622124fc5488503aa36ef6b25`,**本轮现算复核过**)。
- 档案 `test_set.md §HB`(§HB.0–§HB.4),机器键 `state.json:arbheart_BATON_20260912` / `cmqreach_transit_frame_ADMISSION_DEFER_20260912`。
- 落地一条 harness 腿:`a_evidence_owed.py` 的 **PRE-ARM**(变异台 **5/5 CAUGHT**)。

---

## 1. 选取不是我挑的(章程「下次触发」① 逐字要求先跑 `a_evidence_owed.py` 现读别抄报告)

跑出来的是上一轮那一行,**逐字节相同**:

```
A-EVIDENCE-OWED  armed 27  verdict 27  owed-row 0  UNOWED 0     RC_EXIT=0
```

上一轮点名的两个候选是 `cmqreach`(6 次核验全 INDETERMINATE)与 `arbheart`(1 次 / episodes 0 / 9 天未动)。
**去读 `arbheart` 那一条 VERIFY 行的正文时,本轮的实际发现才出现** —— 见 §2。

**⛔ 特意没做的一件事**:`cmqreach` 这条 **id** 本轮**没有**被裁(§4 裁的是那枚**帧**)。
本轮既无新证据也无新仪器,裁它等于在没有新读数的情况下动结论。

---

## 2. ⭐⭐⭐ 本轮最该被读走的一条:一条宣布结清的读数,是被一行「测不了」满足的(§HB.0)

`arbheart` 携带的**唯一**一条 VERIFY 行写于 **2026-09-03**,而它 **2026-09-04** 才入集
(`armed_since.json`,`precision: exact`)。那一行的正文(录像组 `20260903T155538Z.md §5`,逐字):

> ⚠️ `arbheart` 的 `episodes=0` **是「没法测」不是「没测」**:它**不在 armed 串里**
> (57-id 串当场核过),任何波次都还没跑过它 ⇒ 条件 (a) **在构造上买不到**。

此后它 armed 了 **8 天 / 12 波**(最后 W68),**每一波都在付载体与并池成本,而没有一帧被看过**;
`verify_coverage` 报 `verify=1`、`a_evidence_route` 报 `class=VERIFIED`、`a_evidence_owed` 报 `OK` ——
**三个工具一致地什么都没说。**

📌 *一个只问「有没有读数」的普查,会被一条「这个量测不了」的读数**最有把握地**满足。*

⭐ **工具的文件头为什么没覆盖它:它把时间只往一个方向想了。** 原文件头的 LIMIT 逐字写着
pre-convention 的裁决在这里读作「不存在」,并称**那个方向是响的不是哑的**,
还补了一句「**the opposite direction would be silence, which is the defect**」。
镜像情形(**读数看得见,但属于另一个纪元**)正是他说的那个哑方向 ——
**他自己那句话已经把判据写好了,只是没有转身看一眼。**

---

## 3. RULING 24 — `arbheart`:不裁 (a),交棒;PRE-ARM 腿落地(§HB.1)

**⛔ 不是退集。** §HA.3 的三个处置名(`DOMAIN-NOT-REACHED` / `INSTRUMENT-BLIND` /
`UNOBSERVABLE-BOTH-ROUTES`)**没有一个适用** —— 三个都是「买过、买不到」,
而 `arbheart` 是**从来没有人尝试过**。**「没人买过」不是「买不到」**;
退它就是 §HA.3 逐字警告的**用错的理由退错的 id**。

**交棒**:`owed_executions.json:a_evidence_arbheart`(录像组,**零 AWS 增量** —— 语料已在)。
`done_when` 写死两条:(i) VERIFY 行所在**报告文件名日期 ≥ 2026-09-04**(入集前那条不算,这是立案判据);
(ii) 验收半边**照抄别重想**(`state.json:arbheart_retire_20260903.next_baton` 那句
「released camp must be ABSENT from that bot's next camp pick within the same second」)。
⚠️ **(b) 不在本行范围且已被裁过**(§DX.5:与 `slotarb` 混淆且方向偏向让它好看)。

### 3.1 补丁:`a_evidence_owed.py` 新增 PRE-ARM

一条 id 若其**每一条** VERIFY 行都早于 `armed_since`,不再算 VERDICT,降级到「没有读数」应有的状态。
`a_evidence_route.py` 随之在 `--json` 里带上每条 VERIFY 行的**报告文件名**
(⛔ 文本表**故意未改**;日期取自**文件名**而非散文,理由是 `armed_since.json` 自己的文件头:
Routine 容器是 shallow clone,`git log` 给不出报告日期,而散文会被重写、文件名不会漂)。

**⛔ `lower_bound` 行豁免,这是算术不是谨慎**:下界说「**至少**从那天起 armed」,
真实入集日在它之前或相等 ⇒ 早于下界的 VERIFY 行**仍可能落在 armed 纪元内**;
两个数比大小只在其中一个是**等式**时才回答得了这个问题。
**不豁免的代价具体可数**:它会在**最老的那批 id**(prose 最多的那批)上从出生起造假阳。

**实测人口(全 27 个 armed id):命中 1,零假阳。**

**变异台**(还原走**文件副本**,`sha256sum -c` 复核 `OK`;控制 **42 checks / 0 failed**):

| | 变异 | 读数 |
|---|---|---|
| M1 | 去掉降级 | **CAUGHT**(3 条断言红) |
| M2 | 去掉 `lower_bound` 豁免 | **CAUGHT**(2) |
| M3 | 让「文件名无日期」也降级 | **CAUGHT**(2) |
| M4 | 边界 `>=` 改 `>` | **CAUGHT**(2) |
| M5 | 读不了 `armed_since.json` 时静默当空 | **CAUGHT**(1) |

**5/5 CAUGHT,0 SURVIVED,0 ANCHOR MISS。**

⭐ **M4 的用例是跑变异台之前补的,这一点值得单记**:第一版断言里没有「入集当天」那一格,
而**搭车入集的裁定与论证它的那份报告本来就落在同一个 UTC 日** ⇒ 那个边界真会被撞到。
**先补用例再变异** —— 否则 M4 会活下来,而我会把它读成「这个分支无关紧要」。

落地后复跑:`arbheart` 读 **PRE-ARM → OWED**(被新登记的行覆盖),`RC_EXIT=0`。
⚠️ 降级行仍打印 route 的 `class=VERIFIED`(**按设计逐字引用,两个普查不许各算各的**),
看着像自相矛盾 —— PRE-ARM 段就是解释它的那一段。

---

## 4. RULING 25 — GH #659:transit 帧 DEFER-ADMISSION,且那 24 行不是这枚帧的价钱(§HB.2)

**球是 09-09 明确交过来的、挂了 3 天**(`issuecomment-5603745729` §四 逐字「**球交给总监**…
建议保持 open 到总监落一个裁定为止」)。

**裁定**:帧**留在 `tests/frames/`**(staging 价 6 文件 / 9 断言**已付**、全绿、被按名消费);
**不入集 `tests/fixtures/`**。

**⛔ 不是本 issue §4 的出口 2(「结构性太贵」)。理由是归属不是价钱:**

1. **动机不在了。** 英雄组与录像组**两侧**都已写死:这枚帧**不**供给 `cmqreach` 的 (a)
   ——「(a) 是**一个波次的证据,不是一枚帧的**」。**没有别的 id 点名它。**
   ⇒ 「入集以解锁 `cmqreach`」这个选项**根本不在桌上**。
2. **英雄组自己量出的一般结论就是判据**(逐字):「**对一枚纪元帧,入集价 = 那个纪元的
   reopen list,不是这枚帧的 diff**」⇒ 那 24 行是语料对**后期纪元**本来就欠的账。
   实证:24 行里 **9 行不归英雄组**(协同 8 + harness 1),其中 **2 行从 GH #357 起就挂着未付**
   —— **一笔在这枚帧之前就已经存在的债。**

⭐⭐ **把它记成 `cmqreach` 的入集价,是产生那个 6× 错误的同一个误配再上一层楼。**
英雄组诊断自己 4→24 的六倍误差是「**用 staging 的仪表去量 admission**」(问题 / 仪表错配);
**这一层是「用一个申请方去记一笔常驻债」(债 / 债主错配)** ——
同一形状,而**第二层在第一层被修好之后仍然站着**,因为修的是数字不是归属。

⛔ **不是「不值得」**:24 行里 **3 行是升级不是成本**(`turbo_ternary_dominance` /
`zeus_aether_cast_range`「**GOOD NEWS** … retarget it」/ `slotwait_cooldown_scan`
「the ITEM leg is **no longer domain-empty**」= §HA.1 `INSTRUMENT-BLIND` 那族的反面)。
⚠️ **现查过一条免得说大**:`slotwait` **09-06 已 promote**(§FK.1,`stable-v3`),**不在 armed 串里**
⇒ 那条升级改善的是检测器语料,**不解锁任何 armed id**。
*(不查的话「它解锁了一条 armed id 的仪器」会是一个更好听、而且恰好错的理由。)*

**交棒**:`owed_executions.json:late_epoch_corpus_reopen_list`,**executor 故意留空**(跨三个组),
`trigger` = 下一枚同纪元 staged 帧 **或** 任何 armed id 的 (a) 需要一枚后期 fixture。
`done_when` 判**登记不判付款**(pending_rulings LIMIT 11):README 那节按**纪元**命名而不是按 `cmqreach` 命名、
逐行标注归属组。⛔ **付掉那 40 条不是结清条件** —— 结清条件是**这笔债不再挂在一个申请方名下**。
**两个 trigger 都不出现,这笔债就该一直欠着 —— 这是裁定的内容,不是它的失效。**

---

## 5. 成本与体系健康

- 💰 **零 AWS,一次调用都没有;不对 MTD 作任何新声称。** 三条线(围栏 $60 / 刹车 $90 / 批准线 $100)**一字未动**。
- 🩺 五组巡检:batch-desk(03:13Z / 06:16Z)、hero(04:59Z)、strategy(05:02Z)、
  replay-check(03:48Z)、director 均在 24h 内有产出,**无掉队组**。
- **armed 27,离 P4.2 解冻线(≤20)差 7 条。**
- ⛔ **不发 owner 邮件**;`DECISIONS_NEEDED` 存量 15/18/19 随 W37 周信一并交。

---

## 6. 验证(裸读,全部走 `rc.sh`,无管道读退出码)

**铁律 6 三条腿**(读数抄在 §8,push 时现取)。

**改动面**:`tools/agent/a_evidence_owed.py`、`tools/agent/a_evidence_route.py`、
`tests/test_a_evidence_owed.py`、`iterations/`(owed_executions / state.json / test_set.md / 本报告 / 章程)。
**`bots/` + `game/` 零 diff** ⇒ 动态半(全量 Lua 套件 ~100min,GH #124)**未跑,不声称**。

**现读**:
- `a_evidence_owed.py` 改动前 `RC_EXIT=0`(`UNOWED 0`)→ 改动后 `RC_EXIT=3`(`PRE-ARM 1 / UNOWED 1`,
  点名 `arbheart`)→ 登记 owed 行后 `RC_EXIT=0`(`PRE-ARM 1`,`arbheart <- a_evidence_arbheart`)。
- `tests/test_a_evidence_owed.py` **42 checks / 0 failed**,`RC_EXIT=0`(原 40,本轮 +1 节 / +12 检查中含边界那一格)。
- `tests/test_a_evidence_route.py` **32 checks / 0 failed**,`RC_EXIT=0`。
- `pending_rulings.py` `RC_EXIT=3`:`ORPHAN_PROPOSAL none` / `ROWLESS 3`(与上一轮基线逐项相同);
  **`RIDESHARE 2`**(上一轮基线 1)—— **不是我造成的**:两条都是英雄组的 queue 行
  (`hero-62` first_seen 02:09:59Z、`hero-63` 05:05:19Z),**本轮改动一行没碰 `queue.json` / 提议节**。见 §7 ①。
- 成员串现算复核:**27 ids / 239 bytes / md5 `76a888b622124fc5488503aa36ef6b25`**
  —— 与档案里写的那三个数**逐位相同**(§HA.4 立的那条「让 line 2 对着它自己的记录核」本轮第一次替我复核)。
- 两个 registry 的 diff **纯增量**:`owed_executions.json` `38/0`、`state.json` `21/0`,
  **零删除 ⇒ 没有整文件重排**(§(丁) 那条 `indent` 教训本轮没有复发;两个文件分别是 `indent=1` / `indent=2`)。

⚠️ **本轮自己的失手,而且是登记过的那一条**:开工自检写成 `timeout 900 bash …`,
**被 `timeout` 杀在 Lua 腿上,`EXIT=124`** —— 02:45Z 那一轮刚刚登记过「**开工自检不许带任何 `timeout`**」,
**我又带了一次**。那次读数**作废不引用**;§8 的读数来自收尾时在安静树上的**无 `timeout`** 重跑。
📌 *这条守则写在章程里,而我这一轮读章程时读到了它 —— 记下来不等于会照做,第二次了。*

---

## 7. 下次触发

1. ⭐ **两条 rideshare 请求等裁定**(本轮现读):`hero-62`(`zusfightquorum` 的**域**,不是新 id)、
   `hero-63`(`zusarcimm`,只要域频率、**不要发波**)。⚠️ 按 P4.2,入集是**把集合推向反方向**的动作,
   两条都要问「它买的是不是一条**已经 armed 的 id 的读数**」——`hero-62` 自称正是如此。
2. **判定完结 ≥2 继续**(armed 27,差 7)。⛔ **先跑 `a_evidence_owed.py` 现读别抄本报告**;
   **本轮起它多一条腿,读数里的 `PRE-ARM` 计数要一起抄。**
3. ⭐ **PRE-ARM 的人口是 1,而那是今天的人口**:每一条**入集前就被写过 VERIFY 行**的新 id
   都会进这一族,而搭车入集(§HB.1 M4 那一格)恰好是它最容易发生的路径 ⇒ **入集裁定落地时看一眼**。
4. 三个 promote-time 普查只读 `bots/`(**第三轮顺延**,人口读数在 `20260912T054500Z.md §10`,**不要从零再量**)/
   两条仍红的 python/lua trunk 腿(不挡 push)/ GH #584 三条测试第三分支(**第六轮顺延**)/
   批测台交棒 ① / 五条 `a_evidence_*`(**第十次顺延**)/ patch 缺口 P3 /
   `DECISIONS_NEEDED` 15/18/19 等 owner(随 W37 周信)。

---

## 8. 门与自检(收尾现取,安静树,零 `timeout`)

**铁律 6 三条腿**(本轮跑了两遍:push 前手动一遍,`git push` 时钩子又各跑一遍,读数一致):

- `GATE_EXIT=0  CLEAN` — `luacheck bots game: 0 warnings`
- `py gate: 96 ran, 0 findings, 0 uncertifiable, 37.8s`
- `lua gate: 344 ran, 0 findings, 0 uncertifiable, 9 known-red, 457.9s`
- ⛔ **未用 `RULE6_BYPASS`**。`PUSH1_EXIT=0`(分支)/ `PUSH2_EXIT=0`(`HEAD:main`,`87d931a7..2436fbbd`)。

⚠️ **本节收尾那次 push(只改 `iterations/`)的 Lua 腿读数是另一种,按批测台 `266bba6` 的先例分开登记**:
`lua gate: SKIPPED BY SCOPE -- this push touches no bots/game/tests path.`
**「按作用域跳过」不是「跑了 344 条」** —— 上面那条 344 的读数来自 `PUSH2`,那一次**确实**动了 `tests/`。
两次的另外两腿都是 `GATE_EXIT=0 CLEAN` / `py gate: 96 ran, 0 findings`。
`PUSH_MAIN_EXIT=0`(`bb128553..6df03f4d`);分支因 rebase 与远端分叉,用 `--force-with-lease` 对齐
(**自己本轮建的 session 分支**,`a7785929...6df03f4d`),收尾 `origin/main..HEAD` = **0**。

**开工自检(重跑,安静树,零 `timeout`)`SELFCHECK_EXIT=3`**:
`legs run : 12` / **`UNCERTIFIABLE (exit 2): none`** /
`FINDINGS (exit 3) : unlanded cadence queue-rulings owed-executions trunk-red(python) trunk-red(lua)`。

⚠️ **`unlanded` 是本轮新增的那一条,而它是个瞬态 —— 查过了,不是掉棒**:
点名的是**协同组**的 `e361b8f`(GH #539,防守 TP 落点从 NaN 变回坐标),
07:36:13Z 推上 `origin/claude/intelligent-hopper-ixbbcl`、当时不在 main 上。
**收尾复查:该组已 rebase 到我这次的 `2436fbbd` 之上,以 `af8f4d28` 落地,`--is-ancestor origin/main` ⇒ YES。**
⇒ 自检**恰好撞进了另一个组「推分支」与「push HEAD:main」之间的那个窗口**。
📌 *这条腿对任何一个**正在跑**的组都会亮一次 —— 它报的是问题不是判决(工具自己的 LIMIT),
**而「看一眼」这一步这次真的改变了结论**:不看的话我会把一条 `bots/` 行为修复记成掉棒并去替别人 cherry-pick。*
⭐ **我自己那条 commit 没有出现在这条腿里,是对的**:它 rebase 后在 main 上换了 sha
(`a7785929` → `2436fbbd`),而该工具逐字写着 `patch-id compared, rebase twins excluded`。
**同一件事在 `claim_precheck.sh` 那里是 EXIT=3**(它按 sha 字面解析引用)——
两个工具在同一个事实上给出不同答案,**而两个都是对的**:一个问「这份工作在不在 trunk 上」,
另一个问「我写下的这个 sha 读者解析不解析得了」。**我引的那个 sha 当时确实解析不了,改成 `2436fbbd` 后转 0。**

其余五条与上一轮**同一组存量,零新增**(`cadence` / `queue-rulings` / `owed-executions` /
两条 trunk-red)。`NOT RUN (inside a leg): tests/test_selfcheck_lua_leg.py`。

⚠️ **动态半(全量 Lua 套件 ~100min,GH #124)未跑,不声称** —— `bots/`+`game/` 零 diff,
改动只碰 `tools/agent/` 两个 py、`tests/test_a_evidence_owed.py` 与 `iterations/`,
而前三者已在 py gate 里逐条跑过。

**投递(铁律 2.5 三件)**:(i) `owed_executions.json` 两行;(ii) `test_set.md §HB` 全文档案;
(iii) **GH #659 追评 `issuecomment-5644533580` 并关闭**(出口 1 的 staging 半已满足、裁定已落;
债不随之消失,它在 `late_epoch_corpus_reopen_list` 那一行里)。
草稿 `claim_precheck.sh` 跑了两次:**第一次 `RC_EXIT=3`** —— 点名 `a7785929` `OFF-TRUNK`,
**因为 rebase 把落在 main 上的 sha 换了,而我引的是 rebase 前那个**;
改引 `2436fbbd` 后 **`RC_EXIT=0`,`OK to publish`**。**先 push 再发表,GH #290 那道门本轮是真阳性。**

---

## 9. token 用量

```
TOKENS total_in=12,759,653 out=68,451 turns=84
```

⚠️ 按 §7b 的定论:`total_in` 由 cache_read 主导、随回合数近似二次增长,**只作量级参考**;
台账常设指标是 **`out/报告`** —— 本轮 **68,451 out / 1 份报告**。
注:统计时刻之后的收尾回合(自检重跑、GH 追评、本节)不计入。
