# 总监 2026-08-25T07:01Z —— owner P1 DoD 第 4 步裁定:HOLD;$1.48 隔离波拒绝发,而**拒绝的理由是可证的**

**一句话**:接了连续两轮没接的 owner P1 (b) 裁定,结论是 **HOLD 不 promote**,
而本轮真正的产出是**两个都可证、都不靠偏好的东西**:
① **(b) 由域算术满足,不由波次读数满足** —— 该杠杆每局只发生 **0.223 次尝试 / 0.0255 次成功**,
足迹比仪器噪声底低 1–2 个数量级 ⇒「明显负面」结构上产不出来;
连带 **$1.48 隔离波拒绝发**,因为它的**每一个可据以行动的结果都导向同一个裁定**;
② **不 promote 的理由不是「读数不好」,是几何,而且源码自己把它写在注释里** ——
跟随小野**中位只拖 742u**,而营地**中位离最近线兵还有 1,068u**,
**742 < 1,068 ⇒ 中位那次尝试在几何上就连不上**;`PULL_CAMP_LANE_GAP = 1200` 取的是
**观测到的最大拖拽**而非典型值,**这个筛子按设计放行了那些不可能的尝试**。
**⭐ 顺带解锁了一个当初被判「测不了」的问题**(§5)。本轮 **`bots/`/`game/` 零改动、零 AWS 调用、MTD 不变**。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` ⇒ **worst exit 3**,逐条判读:

| 项 | 读数 | 判读 |
|---|---|---|
| UNLANDED | 2 commit(`08ed7c2` / `fc79986`,均在 `origin/claude/compassionate-albattani-4zcohy`) | **不抢救** —— 内容已在 main:`git grep tpreach origin/main -- bots/` 命中 `jmz_func.lua:5857/5878/5894`;`fc79986` 那两处行号修正也已被后续改写吸收(main 上 §BB 的引用是 `8032`/`8154`,与源码逐位相符)。工具 LIMIT 4 的「改头换面落地」形状 |
| CADENCE | 11 finding | 全是 08-24 白天五条流停摆的残影 + 发波/收割轮之间的必然空轮,五条流均已恢复,不开 issue |
| trunk python | **22 passed / 0 failed** | 干净 |
| trunk Lua 检测器 | **SKIP(容器无 lua5.1)** | 本轮 `bots/`/`game/` 零改动 ⇒ 不构成阻塞,见 §7 的诚实边界 |

**⚠️ 抢救判断按上一轮更正后的判据走,而且这次省下了那笔钱**:上一轮(05:0xZ)为一个
「没死、在等自己 3h01m 全量套件」的会话付了**大半个工作单元**的重复代价。本轮拿到 UNLANDED
的第一件事**不是**读分支活性,而是**直接对 `origin/main` grep 内容** —— 这是 backlog §6b 追加的
「内容侧复核」那一格,**成本约 10 秒,直接把该行降级成「已落地」**,没有走抢救流程。
**这一格值得从建议升级为惯例**:它在本轮和上一轮的现场都给了正确答案,而「最近提交时间」两次都给错。

## 1. 本轮做什么 / 为什么是它

章程「下次触发」清单第 ① 条:**owner P1 的 (b) 裁定,已连续两轮没接**,标着「值一个完整工作单元」。
铁律 9(owner 优先项凌驾 issue 流)指着同一条。**接了它,其余项本轮一律不做**(§8)。

P1 的完成定义第 4 条原文:
> 条件 (b) 无明显负面 + (c) 逻辑依据(检索标准拉野时机对齐实现)成立 → promote;
> **或给出明确的不 promote 理由并由 owner 知悉。**

**本轮走的是第二条路**,理由见 §3/§4。

## 2. 先把不可采信的那份读数摘出去

批测台 03:12Z 交来的是 **28-id 全集 vs `stable-v2`**:gpm **−23.27**、winrate **0.432**、
comps **0/4 + 0/4**、277 计分局。批测台自己写了「这不是 `pulllane` 的读数」并拒绝归因。
**照单接受,而且把它一般化写进档案(§BD.1)**:

> **一份合成读数对单个 id 的 (b) 既不能证成也不能证伪。**
> 负的合成量可以来自另外 27 个 id;正的合成量也可以掩盖一个 −50 的 id。
> **它测的是另一个量,不是「弱证据」。**

⇒ 全集读数在本裁定里**一次都没有被用作 (b) 的输入**。这一条**对称性**是关键:
上一轮把岔路写成「接受全集读数当 (b)」vs「买隔离波」,**前一支本来就不是一个选项**。

## 3. ⭐ (b) PASS —— 给出它的是**域算术**,不是波次;隔离波结构上买不到可判读的读数

录像组 01:35Z 的 armed 腿足迹(balanced,两层):

| 量 | 读数 | 折算每局 |
|---|---|---|
| poke episode(尝试) | 22.3 / 100 局 | **0.223 次/局** |
| connect(成功)**绝对次数** | 两波两层**全是 2** | **~0.0255 次/局** |

仪器分辨率:波间 gpm sd **45.81**(§BA.1 实测)、随机牌局 per-game sd **~600**(CLAUDE.md)。
⇒ 该杠杆的整个足迹比噪声底低 **1–2 个数量级** ⇒ **「明显负面」在结构上产不出来 ⇒ (b) PASS**。

**这个论证比任何一次 $1.48 的波次都强**,因为它同时**解释了那次波次为什么必然读到零**。

**⇒ 隔离波拒绝发。** 判据是 `strategy-5` 立的那条(不为不改变决定的信息买波次),
而在这里它**可证**,不是省钱偏好:

| 隔离波可能的结果 | 我能据以做的事 | 裁定 |
|---|---|---|
| 读到零 / 小残差 | 判 (b) 过 | 不变 |
| 读到大额负数 | 0.223 次/局产不出它 ⇒ **只能判噪声** | 不变 |

**没有一种结果改变裁定。** 买了只会得到一份「测过了,无影响」——
而这份读数与「门被冻死、逐字节 no-op」的产出**逐字节同形**(`campgrade` / §BA.2 那个形状,
`check_armed_wiring.py` 仍会报 WIRED,**没有任何计数会报警**)。

## 4. ⭐⭐ (c):时刻这一半**齐了**;欠的是几何 —— 而**源码自己把矛盾写在注释里**

**进来之前我的假设是错的,值得单记。** 我带着「10% 连接率 = 拉野时刻没对齐」这个假设开工,
而且已经准备好把它写成裁定(owner 对 (c) 的原话正是「检索标准拉野**时机**对齐实现」,
假设和 owner 的措辞严丝合缝 —— **这正是它危险的地方**)。**核过源码,假设被否掉**:

- **时刻(PASS,不欠)**:`jmz_func.lua:8046-8052`(注释)+ `:8067`(谓词)把教科书拉野时刻
  **写进了实现** —— 兵线波 **:00/:30**、经典 **:12/:42** 仇恨标记、5s 行军提前量
  ⇒ 窗口 `[5,20] ∪ [35,50]`。**这一半不是欠着的。**

**这是 §7 那条「间距反而变大了」的同族教训**:一个和 owner 措辞完美吻合的机制解释,
**在读源码之前就是给假设编的故事**。差别只在于本轮**在写进裁定之前**去读了。

- **几何(FAIL)**:`jmz_func.lua:8134-8135` 的注释**自己记着**这两个数:
  跟随小野**中位只拖 742u**(p90 992,max 1,170),
  而营地**中位离最近线兵还有 1,068u**。
  **742 < 1,068 ⇒ 中位那次尝试在几何上就是连不上的。**
  而 `PULL_CAMP_LANE_GAP = 1200`(`jmz_func.lua:8192`)取的是**观测到的最大拖拽** 1,170 向上取整
  —— 注释写明这是「measurement 支持的**最宽**常数」,**不是典型值**。
  ⇒ **这个筛子按设计放行了那些不可能连上的尝试。**

**这就是连接率停在 12.1%/9.9%、而 `pulllane` 只动了分母没动分子的算术原因**
(两波两层 connect 绝对数全是 2,17.1% 全部来自 drag episode 44 → 24)。
`pulllane` 做的事是对的且已核验 —— **它只是没有、也不可能靠自己把分子抬起来。**

### 4.1 为什么 (a)+(b) 都过了还不 promote

promote = turbo 默认开 = **上线**。要上线的这个东西的实测形状:

- **收益**:~2.2–2.9 次成功拉野 / 100 局;
- **代价栏非空**:每 100 局 ~21–23 次把 pos 4/5 拉出线,其中 **~90% 连不上**;
  15s 掉血中位 **18 → 21pp**(录像组代价栏,未见改善);
- ⇒ **命中率 ~10% 的辅助离线动作。**

owner P1 的原话是「拉野**真正跑起来**」。**0.0255 次/局不是「真正跑起来」。**
按 DoD 字面三条件齐即可 promote,但 (c) 的几何半边不齐,
**而且不齐的方式恰好就是「它为什么几乎不发生」** ⇒ 形式上可 promote,实质上不该,本轮选后者。

## 5. ⭐ 下一棒(铁律 9 连带规则)= 协同组,**零 AWS**,并且**解锁了一个当初被判「测不了」的问题**

**动作**:把 `PULL_CAMP_LANE_GAP` 从 max-drag(**1200**)收到典型值那一档(p90 **992** / 中位 **742**)。
**验收量 = 连接率的分子(connect 绝对次数),不是率** —— 率会因分母下降自动变好看,
这正是本轮 `pulllane` 踩过的刀口(#148(ii) 的形状)。

**⭐ 解锁点,这是本裁定的实质产出。** 选 1200 时,源码写着反 SILENT 那一侧**测不了**:

> `the anti-SILENT side (do the real textbook pull camps fit under it?) cannot be
> measured locally, because GetNeutralSpawners() is {} on every corpus frame.`
> —— `jmz_func.lua:8190-8192`

**这句话在波次层面已经不成立了,而且是本轮才成立的**:录像组 01:35Z 的营地表
**从已收割的 `.dem` 里**给出了两层**唯一产 connect** 的那两个营的坐标 ——
**radiant (4000, −5000) / dire (−4000, 4800)**。算它们到本 bot 被分配 lane 路径的**垂距**,
就**当场**回答了「收紧之后教科书拉野营还在不在筛子里」——
**零 AWS,数据已经买过,不需要任何新波次。**

**这一条是「本地测不了」这类登记的一般教训**:那句话在写下时是对的(帧语料里
`GetNeutralSpawners()` 恒 `{}`),**但它限定的是帧语料这一层,不是所有层**;
录像组换了一条路(**看输出集合的形状**而不是读谓词输入)之后,同一个问题在**波次层**变得可答。
⇒ **「测不了」的登记应当带上「在哪一层测不了」**,否则它会比它该有的寿命活得更久。

⚠️ **收紧前必须先跑这一步,不许跳过**:那两个营是**仅有的分子**;
把它们筛掉 = 把 (a) 一起筛掉,`pulllane` 会从「删掉不可能的」变成「删掉全部的」,
**而那份读数长得和 SILENT 一模一样。**

## 6. 顺带修一处投递缺陷(章程步骤 2.5 / §AW.1 的**第四发**)

`queue.json:strategy-11` 与 `strategy-12` 的 08-24T19:xxZ 入集裁定,
**只写进了 `result`(申请方/批测台那一格)与 `test_set.md` 档案,`director` 机器字段是空的**。

前三发:落错行(08-19 header)、落错字段(W3 裁定进 `question` 散文)、
落错机器位(`strategy-9` 的 `bundle` 没跟着散文改)。**本次是根本没落到 `director`。**
四次的现场是同一个:**裁定作出了,而下游读不到。**

**本轮未造成损失纯属侥幸** —— 批测台是从 `test_set.md` 头部成员串取 armed 的,**没走 `director.wave`**。
换句话说,**步骤 2.5 指定的那条投递通道,四次里有一次真正被下游读过**;
它现在更像一条**并行档案**而不是投递轴。**这一点本身该进 backlog §12 的判据设计**
(§12 要做的机械守卫,判据应当是「`director` 缺失」这一格,而不只是「`wave` 与 `status` 不一致」——
**缺失的字段比矛盾的字段更常见,而且更安静**)。已写进章程 backlog。

两条的 `director` 字段本轮已补齐(`strategy-12` 标注「补录」,内容与 §BB.3 逐条一致,**不是新裁定**)。

## 7. 交付物与验证

| 文件 | 改动 |
|---|---|
| `iterations/streams/test_set.md` | 新增 **§BD** 全文档案(裁定 + 四条论证 + 下一棒 + §BD.7 投递缺陷) |
| `iterations/queue.json` | `strategy-11.director` = **HOLD_NO_PROMOTE**;`strategy-12.director` = **APPROVED_ADMITTED(补录)** |
| `iterations/DECISIONS_NEEDED.md` | 新增第 10 条(**报告性质**,进 W35 周日邮件;**本轮不单独发邮件**,周一封上限) |
| `iterations/streams/director.md` | 「当前状态」+ backlog §12 判据补充 |
| GH **#117** | 追评裁定全文 + 下一棒(投递纪律第 iii 项) |

**`bots/` 与 `game/` 零改动** ⇒ 按铁律 6 的适用范围,本轮**不触发** luacheck / `lua5.1 tests/run_tests.lua`
这道门(容器无 lua5.1,**未装** —— 因为没有 Lua 改动需要它把关)。
**诚实边界**:这意味着本轮**没有**独立复核 main 的 Lua 侧健康;
自检的 Lua 检测器腿(GH #171,4.9s)也因缺 lua5.1 而 SKIP。
**若下一轮有 Lua 改动,那一腿必须先补上 `apt-get install lua5.1`。**
本轮引用的所有源码行号**已逐条对源码核过**(`8032`/`8046-8052`/`8067`/`8134-8135`/`8154`/`8190-8192`/`8192`),
其中两处初稿写错(`8180`→`8192`、`8189`→`8190-8192`)**已在提交前修正** ——
这正是 22:3xZ 那条「改 helper 的人顺手改掉行号引用」的反向:**引用的人也得核**。

## 8. 成本 / 体系健康 / 本轮不做的事

- **成本**:总监**零 AWS 调用**。MTD **$37.828**(批测台 06:17Z 读数,未刷新)。
  围栏算术:$37.828 + W8 滞后 $1.48 + W9 $1.48 = **$40.79**,**$45 围栏余 $4.21**,
  距 owner **$50** 档尚有余量。**本轮拒绝了一笔 $1.48 的支出**(§3),三线全未触及,未批准新支出。
  ⚠️ W9(06:16:33Z 发出,`--hours 2` + terminate)**本轮仍在跑**,收割归批测台下一轮。
- **体系健康**:五条流本轮全部有产出且节奏已从 08-24 白天的停摆恢复;
  owner 优先项推进核对:**P1 本轮有推进(本裁定)**;**P2 球仍在协同组**;
  **P3(cap 10→25,GH #108,责任在总监)本轮仍未认领 —— 这是第 3 轮**,
  离铁律 9 的「12 轮零推进 → 红色升级」尚有距离,但它是**总监自己欠的**,
  已写进「下次触发」第 ①(**优先于 backlog 自选项**)。
- **本轮不做**:`tpdeathbuy` 入集裁定(GH #168)、`[bug] #156`、`[harness] #161/#152/#169`、
  GH #147/#140、`stable-v2` 引用(**第五轮积压**)、backlog §15/§13/§14/§9。
  **不发波、不代批测台选波、不代 Cursor 改 `OWNER_PRIORITIES.md`、无 owner 邮件。**

**下次触发**:①⭐ **P3 / GH #108(cap 10→25)—— 总监自己欠了 3 轮,先做它**;
②`tpdeathbuy` 入集裁定(GH #168,搭车提议按 §BB.4「当轮批或退」本已逾期);
③backlog §12 的机械守卫,判据按 §6 收窄为「`director` 字段缺失」;
④`[bug] #156` / `[harness] #161/#152`;⑤`stable-v2` 引用(第五轮)。

## 9. Token 用量(铁律 8)

```
TOKENS total_in=6,372,875 out=51,818 turns=48
```

(统计时刻 = 收尾前,其后回合不计入。按 §7b 的定论,常设指标看 **`out/报告` = 51,818**;
`total_in` 由 cache_read 主导,只作量级参考。)
