# 总监 2026-08-22T16:50Z(第四十五次触发)

**一句话**:本轮是**测试集定级轮** —— 三条裁定,两个 owner 优先项各动一格:
**P1 的 `pullcamp` 买到了 (a) 的执行那一半、被我退回**(理由不是它有害,是 **(a) 的证据是
版本专属的**);**P2 挂了 5.5 小时的 `stayfield2` 和 1.5 小时的 `fieldbuy` 批准入集、
且钉死「三个 id 必须同波」**;`#112` 结案。另外**我自己犯了一个需要写在最前面的错**(§6)。

`bots/` / `game/` **零改动**;一次 AWS API 都没打,**自身支出 $0**。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` → worst exit 3。

- **UNLANDED WORK 2 条,两条都有解释,不接管**:
  - `4e8972f`(strategy 14:00Z)现在挂在 `origin/discarded/strategy-20260822T140000Z-fieldbuy-duplicate`
    —— 协同组自己给废弃树打了 ref 并在 `cfb32f0` 里说明,**这是正确处理**,检测器点名点得对但结论是「已知且已归档」;
  - `3a73e39`(hero 15:47Z,`origin/claude/vibrant-heisenberg-tpvb2n`)标题即 **"wkqaim pre-flight — DO NOT WRITE"**,
    内容已由 **GH #118** 承载(15:54Z),**结论在,载体没进 main**。登记为观察项,不点名:
    一份「不要写」的预检报告,它的**结论**已经在 issue 上交出去了,掉的只是报告文件本身。
- **CADENCE**:clean(strategy 的 3.9h GAP 已由 15:22Z 那份补上;其成因见 §5 健康度)。
- **trunk health(py)**:11 passed / 0 failed。

---

## 1. 本轮处理的四条(章程 2a/2b/2c)

### 1.1 ⭐ `pullcamp` 的 (a) 裁定 —— owner P1 完成定义第 3 条(裁定全文 `test_set.md` §AO.1)

**(a) 执行 = ✅,这是 2026-07-23 立项以来第一次。** 录像组 15:05Z 交付:283 有效局
(12:12Z 那波,`--rec-slots 16`),poke episode **armed 115 : baseline 11**、drag **74 : 4**,
承重帧逐帧闭合(witch_doctor 在 `:13.5` 仇恨 —— 设计的标记是 `:12` ⇒ **时序子句正按设计工作**
—— 然后带两只青蛙往家走 1375u)。

**(a) 行为正确 / 无副作用 = ❌。** GH #117。**我自己在源码上核过**(不是转述):
`jmz_func.lua:7576-7586` 取「离 **bot** 最近的 own-team camp (<1500u)」,
**全函数无一条子句问该野点在不在我方兵线背后**;而承重触发条件 `:7554-7563` 要求
**我方兵线越过中点**,那时辅助本人就站得靠前 ⇒「离他最近」**恰恰是远野区营地**。
**两条子句互相拆台,结构性的。** 代价:中位在离家 **10,708u**(全图 18,806u = **57%,已过中点**)
开拉,连接率 **≤20.3%**,15s 掉血中位 **19pp** / **7.8% 掉到 <35% 血** / **1.7% 20s 内死亡**;
**焦点五里 CM 40 + Zeus 25 + Lion 1 = 66/115 episode**。

**(b) = 未取得(不是失败,是读不出来)。** 21-id 波 284 局 gpm **−29.87** / xpm −30.68 /
deaths +0.25 / **0-of-4**,`suggested = hold_or_reject`。**但这是 21 个杠杆的合读**,
且该形状的负残差(12-id −65 / 14-id −34.59)**早于 `pullcamp` 被 arm**
⇒ 按「一个读数不构成一次测量」,**既不能记在它头上,也不能替它开脱**。

**(c) = 设计意图成立,实现不成立。** 教科书拉野 = 把**自己兵线背后**那个营拉进兵线,
让线兵打野怪、把线拽回自家塔下 ⇒ **「兵线被压出去时才拉」这条触发是对的,不要动它**;
错的只有选点那一格。(这一条我写进 #117,因为它是最容易修错的地方 —— 见 §1.2。)

**裁定:不 promote;退回协同组、出 armed 集(非 reject,gate 保留)。**
⭐ **摘它的核心理由,也是本轮最想让全队记住的一条:(a) 的证据是版本专属的。**
#117 的修法必然改写选点代码 ⇒ 这 283 局买到的 115:11 **不转移到修好的版本上**,
而 promote 要的是「**你要 promote 的那个版本**在真实对局里执行且正确」。
继续 armed 有两个实打实的代价:①为一个即将作废的版本重复买同一个数;
②把一个**已实锤有缺陷**的杠杆留在每一波 bundle 里,**污染其余 20 个 id 的 (b) 残差**
—— 而这个团队恰好已经追了三周这个残差。

### 1.2 #117 的修法方向(我把「不要修哪里」写在了「修哪里」前面)

两条**反向**指导,因为这一族已经栽过:

- **不要动触发条件**(`:7554-7563`)。兵线被压出去**正是拉野的适应症,不是禁忌症**。
- **不要给 bot 加「必须站在我方半场」的前置**。他站得靠前正是因为兵线在那儿
  ⇒ 那样修**会把 mechanic 重新变成 SILENT**,即 #13 的自锁环再来一次。

要改的是**选点的度量本身**:从「离 bot 最近」改成「相对我方兵线/我方远古的几何」
(甲:按 `GetAssignedLane()` 绑定该 lane 的拉野营;乙:候选营按到**我方远古**的距离升序 + 可达性上限)。
⭐ **免费的正控**:录像组已量到 armed 侧 267 个 poke 帧里 **(3994,−5137) 占 95 帧 +
其 dire 镜像占 41 帧 = 两个远野区营合计 51%** ⇒ **修法生效的最直接判据就是这两个点的占比塌下去**,
在**同一份 283 局**上就能对照,不必等新语料。
验收三条(域中位必须落回我方半场 <9,403u;连接率必须显著高于 ≤20.3%;fixture 两侧都断言**选中了哪个营**)写进了 issue。
**§3.3 的血量门定为可分离的第二个杠杆,不阻塞本条,且不许写进同一个 id** —— 一次改一个变量。

### 1.3 owner P2:`stayfield2` + `fieldbuy` 批准入集,**同波是硬条件**(§AO.2)

两条提议分别挂了 **5.5 小时 / 1.5 小时**,而它们**必须和已入集的 `stayfield` 同波**
—— 三个 id 是同一情境的三格(`IsFieldRegenSituation` ∧ 有回复品 = 决策侧两个 id 的域;
∧ **没有** = `fieldbuy` 的域),**分波 arm 会互相遮蔽**。

- **`fieldbuy` 的存在性我按提议自己的要求跑了那条命令**(因为 13:50Z 那一棒的同名产物
  在全 refs fetch 之后**一件都不在**):`a64def0` 在 `origin/main`,两个测试文件在
  `git ls-tree origin/main` 里,`jmz_func.lua:4872` 有 `J.IsSoakCandidate('fieldbuy')`,
  `state.json:fieldbuy_20260822` 在。**这一次是真的。**
- **⚠️ 预登记 `stayfield2` 的判读口径**(现在写下来,免得事后争):它的谓词在
  100 fixture / 930 帧上**只有 3 帧为真、1 帧真的动出价** ⇒ **下一波读零一律记
  「本语料无表面」,不许记 SILENT**。SILENT 是「该触发而没触发」的判词;
  对一个每 930 帧出现 3 次的域,**一波读零是分母的正常结果**。要下 SILENT,
  先给出该域在对局语料里的频率下界。
- **顺带纠一处口径**:两条提议都写「本提议不动 eligible 那一行」。**不成立** ——
  eligible 行就是「允许出现在 armed 串里的 id 全集」,而录像组的 armed 串守卫按 stamp
  逐字比对、目标 id 不在串里时直接 `exit`。**独立 gate id 必须有自己的 eligible 条目**,已补。

**下一波串 §AO.0 = 22 id**(§AN.0 的 21 减 `pullcamp`,加 `stayfield2` + `fieldbuy`),
种子 888/895/896/906,**`--rec-slots ≥ 8` 是硬条件**。

### 1.4 `#112` 结案(协同组 15:5xZ 明确问「结案还是重述」)

红确实没了,**我独立核了一次**:本轮 `origin/main` = `cfb32f0`,
`git diff --name-only 77a0bfd..HEAD -- '*.lua'` = **0 个文件** ⇒ 77a0bfd 之后的三个提交
全是报告类,**Lua 侧与协同组量到 1246/0 的那棵树逐字节同一**;另起了一次独立全套跑(§7)。
**剩下的 per-fixture 分解并进 #106 §4**,不留在一个已结束的事件 issue 上当常设待办
(那正是今天已经数过三次的掉棒形状)。三条方法学复述进了评论,以便 #106 的执行者不必回溯。
**顺带定了一条属于我的**:`_itemdesire_sweep` **暂不拆出默认套件** ——
它拦住过真问题(就是 #112),拆出去 = 没人跑,而本周铁律 10 的立法理由正是「工具没人跑」;
**要优化的是它自己的速度,不是它的可见度**。

---

## 6. ⭐ 本轮我自己的错:我用替换正文的工具去发一条评论,覆盖了 owner 的 issue 正文

**事实**:16:58Z 我发 P1 裁定时用了 `issue_write method=update` 带 `body`,
**那是替换正文,不是追加** ⇒ **GH #109(owner 06:58Z 开的 P1 执行入口)的原始正文被我覆盖掉了。**

**已做的补救**:(1) 立刻重建正文并在顶部**标注它是重建件**,指明**原文在 GitHub 的
"edited" 编辑历史里没有丢**,重建依据是本仓库内的一手来源(`OWNER_PRIORITIES.md` 的 P1 节
—— 本 issue 标题自己写着「接力链见 OWNER_PRIORITIES.md」);
(2) 裁定改用 `add_issue_comment` 重发;(3) 在裁定评论开头把这个错**公开写给全队**,
附操作纪律:**往 issue 上加东西一律用 `add_issue_comment`**。

**为什么值得单独写一节**:本仓库这两天反复在记「**测试绿了 ≠ 测的是你以为的那棵树**」
「**判存在性之前先 `pwd`**」这一类**工具语义没核对**的坑,这次是同一族的第三种形态
—— **工具名里的 `update` 指的是「更新 issue 这个对象」,不是「更新这个线程」**。
诚实边界:我没有验证过编辑历史在这个仓库的 UI 上确实可见(我这里读不到编辑历史 API),
所以**重建件是按「原文可能取不回」的前提做的**,不是按「反正历史里有」。

---

## 5. 各组健康度 / 成本

- **四组本轮全部有产出**,且都跑了铁律 10 的自检(批测台 16:06Z、录像组 15:05Z 明写读数)
  ⇒ **上一轮我留的第 ⑥ 项「看铁律 10 有没有被执行」:已被执行,观察项解除。**
- **strategy 的 cadence GAP(上轮登记的观察项)有了根因,而且不是怠工**:
  13:50Z 那一棒的五样产物**连推都没推**(全 refs `--prune` fetch 之后一件不在,
  `git log --all -- <路径>` 为空)⇒ 15:22Z 从零重做。**这是新的一类**:
  不是 OFF-TRUNK(东西在、没并),是 **NOWHERE**。两者补救**完全相反**
  (OFF-TRUNK 不许重做,去合并;NOWHERE 只能重做),而协同组自己给出了区分判据
  (fetch 全 refs + 距声称 >1h + `git log --all -- <点名路径>` 为空)。
  **观察项转为「已归因、已自愈」,不升级**;⇒ 但它暴露了自检的一个洞:
  `unlanded_commits.py` 只看**推了的**提交,**「连推都没推」它结构上看不见**
  —— 唯一痕迹是 cadence 那条洞,而洞读起来像「那轮没东西可交」。**进 backlog(§8)。**
- **owner 优先项**:P1 **本轮大幅推进**(第 4 棒交付 + 我的裁定,球明确交到协同组 #117 +
  录像组 #116),P2 **本轮解冻**(两个 id 入集,球到批测台排波)⇒ **两条都在动,不升级**。
- **成本**:MTD **$18.741**(批测台 16:06Z 自报,免费的 `budgets describe-budgets`;
  forecast $23.139,limit $100)。**$45 围栏 / $90 刹车 / $100 批准线全部未触及。**
  开工时在跑实例 0 台。**本轮总监自身 $0,一次 AWS API 都没打**(连只读的都没有,省掉 CE 的 $0.01)。
- **`DECISIONS_NEEDED.md` 不新增;本周 owner 邮件仍为零**,周日随效率台账一起考虑。

---

## 7. 验证(铁律 6)

**`bots/` / `game/` 零改动 ⇒ Lua 门无适用对象**;本轮改的是
`test_set.md` / `state.json` / 报告 / 章程。

- `iterations/state.json`:**JSON OK**(227 keys,新增 2)。
- 仍然**独立跑了一次全套** `lua5.1 tests/run_tests.lua` 作为 trunk health 复核
  (#112 结案需要一个不是转述的读数;容器本身没装 `lua5.1`,`apt-get install -y lua5.1` 装的)
  —— 结果见文末补记。

---

## 8. 基建 backlog 增补(本轮新登记)

- **[新] 自检看不见「连推都没推」**:`unlanded_commits.py` 的输入是**远端 ref 上的提交**,
  所以 NOWHERE 那一类(§5)结构上不在它的值域里。cadence 腿是唯一痕迹,但它的判词
  (「那轮没东西可交」)与 NOWHERE 不可区分。**候选判据**:报告/issue 里点名的**文件路径**
  在 `git log --all -- <path>` 上为空 ⇒ 这正是 `citation_audit.py` 已经在做的事的一个变体
  (它现在审的是**已发表引用**),把「本组自己上一轮点名过的产物」也纳入抽取源即可。
- **[承上轮未做]** ①**周日效率台账**(明天);②`#101` 的 (iv) 与 `0.40` 四处副本收敛进 REGISTRY;
  ③`#102` 消费方半 / `#93` 执行半(执行棒在协同组);④`l5combo` 的 (a) 已被录像组
  **连续第十九轮**顺延并请我直接裁定降级 —— **下轮单独排,按 §AF.3 先例出裁定**
  (第四轮定级积压的做法本轮已验证有效)。

---

## 9. 下次触发

1. **周日效率台账**(章程 2e2,本周第一次);
2. **`l5combo` 的 (a) 降级裁定** —— 挂了十九轮,单独排,不许再被挤;
3. **看 #117 / #116 两根棒子有没有落地** —— 这是 owner P1 的实际状态,**本日已四次同形掉棒**;
4. `#101` (iv) + `0.40` 收敛进 REGISTRY;
5. **§8 那条新 backlog(自检的 NOWHERE 盲区)** —— 它是本周第二次由「掉棒」自己教出来的检测器。

---

## Token 用量(铁律 8)

```
TOKENS total_in=10,493,650 out=94,263 turns=94
```
(到统计时刻为止;其后的收尾回合不计入。)

---

## 补记(收尾时刻):那次独立全套跑**没跑完**,如实记 —— 不声称全套通过

`lua5.1 tests/run_tests.lua` 在本会话收尾时仍在跑,**读数 1196 个用例、`E`/`F` 各 0**
(耗时 >55 分钟,整段卡在录像组 13:24Z 量过的那条尾巴:少数几个子进程全语料普查,
`_itemdesire_sweep` 是其中最贵的一个 —— 也正是 §1.4 我裁定**暂不拆出**的那一个,
本轮亲身付了一次它的成本)。

**所以本轮的 trunk health 读数是「跑到的 1196 例全绿」,不是「1246/0」。**
后者是协同组 15:22Z 在 `77a0bfd` 上拿到的;我在 §1.4 证明的是
**`77a0bfd..cfb32f0` 的 Lua 侧零改动**(`git diff --name-only -- '*.lua'` = 0 个文件),
⇒ 他们那个数**对本轮这棵树成立**,我这次跑只是一次独立旁证,**旁证没跑完就说没跑完**。

**本轮零 Lua 改动 ⇒ 铁律 6 的 Lua 门本就无适用对象**,这次跑是额外买的,不是门。

---

## 补记二:录像组 17:09Z 在我裁定之后落地,我更正了自己的一条口径(`test_set.md` §AO.4)

收尾同步时 `origin/main` 上多了 `77a128f`(录像组 17:09Z)。读了,**它同时印证和收紧了 §AO.2**:

- **印证**:`stayfield` 的 (a) 在本波规模上买不到(唯一调用点 283 局里 armed 腿只走到 **2 次**),
  录像组判 **INDETERMINATE 而不是 SILENT** —— **他们在不知道我这条裁定的情况下独立用了同一个判词**。
- ⭐ **收紧,而且这条是我更正自己**:§AO.2 我写「`stayfield2` 的谓词 930 帧里只有 3 帧为真
  ⇒ 读零一律记『本语料无表面』」。**这句话在对局语料上不能照搬** —— 同一份 283 局里
  SITUATION 域 episode 是 `stayfield` 142/153 对 `stayfield2` **538/564**,
  **步行那一半是 TP 那一半的 3.7 倍**。**fixture 语料的稀疏 ≠ 对局语料的稀疏。**
  ⇒ `stayfield2` 读零**不再自动免责**,要给逐条子句的第一失败点分解。
  **「无表面」这条免责只对已证明域小的 id 成立,而 `stayfield2` 恰恰是这一族里域最大的那个。**
- **两条排期裁定**:①P2 那一波的取证重心定死在 `stayfield2`,**不是给 `stayfield` 加局数**
  (283 局只出现 2 次,再加一波同规模仍然买不到 (a) = 拿钱买一个已知的分母);
  ②**GH #119**(`J.IsFieldRegenSituation` 对小兵/野怪全盲)是**三个 P2 id 共用的无 gate 谓词**,
  修它会同时改三个 id 的域 ⇒ **要么在波次之前落地,要么推迟到波次之后,不许中途改**。

**这一条印证了本轮 §1.1 那个裁定的一般形式**:域的大小是**版本/口径专属**的,
一个在 fixture 上量到的稀疏度**不构成**对局语料上的免责。
