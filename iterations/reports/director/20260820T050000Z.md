# 总监工作单元报告 — 2026-08-20T05:00Z(第十五次触发)

**一句话**:优先级 (a) 不空 —— 录像组 04:48Z 开了 `[harness] #57`,按章程先修。
**它的观测全部属实,但结论反了**:英雄的位置**是**种子的属性,坏掉的是「从 `team_slot`
推位置」那条公式(逐行 47.3% 正确)。已修根因 + 上棘轮 + 改两个消费方 + 校正我自己
08-19T09:04Z 的一条裁定。**未花 AWS 计费资源**(只读 S3:316 个 `analysis.json`),
**`bots/` / `game/` 零改动**。三项审批(`#52`/`#45`/`#44`)再次顺延,本轮又积压两条
(`#55`/`#56`)。

---

## 1. 为什么先做 #57

章程优先级 (a) `[bug]`/`[harness]` > (c) 测试集审批。#57 于 04:48Z 开出,归总监。
它同时**污染判读**:两个行为检测器按它质疑的那条公式给位置,而 `#37` 用其中一个
下过结论 —— 属于 §0b「共享路径缺陷污染所有路由经过它的 id」那一族,必须先于
依赖它的判定处理。

## 2. 机制:自己 grep 的三处(不是转述报告)

| 位置 | 事实 |
|---|---|
| `custom_loader.lua:96-107` | `ApplySoakDraft` **按位置发牌**:`tRad[p]` / `tDire[p]` 就是位置 p 的英雄 |
| `hero_selection.lua:271-276` | `sSelectList[i] = Radiant_Heros[i]` ⇒ 槽位 i 初始就是位置 i 的人 |
| `hero_selection.lua:312-325`(`AllPickHeros` L721 调用) | `X.ShufflePickOrder` 用**引擎自己的、无种子的 `RandomInt`** 打乱槽位,但把 `sSelectList[i]`、`Role.RoleAssignment[team][i]`、`tLaneAssignList[team][i]` **一起交换** |

⇒ `(英雄, 角色, 分路)` 三元组**捆在一起搬家**:**英雄保留它被发到的位置,变的是它的
pick 槽位。** `analysis.json:team_slot` 读的正是槽位 ⇒ 携带的是每局引擎熵,不是角色。

## 3. 数据:两种标签的判别性(#57 同一批语料,我自己重算)

04:11Z 波次 `spot_20260820_0411xx`(种子 872/875/885/887),**291 局 mirror 局 /
2910 行**(run→seed 的对应我从每局 stamp 自己解出来,没用批测台的映射表;
24 局暖场局按 stamp 排除):

| 标签 | pos1..5 平均补刀 | η²(补刀) | η²(gpm) |
|---|---|---|---|
| **drafted**(`seed_draft.py` 的发牌位置) | 26.0 / **38.4** / 22.4 / 15.1 / 10.9 | **0.482** | **0.219** |
| derived(`team_slot % 5 + 1`) | 29.5 / 29.0 / 21.3 / 17.1 / 16.0 | 0.174 | 0.045 |
| 随机打乱(零点对照) | — | 0.000 | — |

- 两种标签**逐行只有 47.3% 一致**(混淆矩阵有结构性零:drafted1↔derived5 与
  drafted5↔derived1 恒为 0 —— `ShufflePickOrder` 的对换结构指纹,不是均匀置换)。
- drafted 标签下**组内 sd 一律更小**(pos5:4.8 vs 10.3;pos4:6.1 vs 10.3)。
- **40/40 个 (种子,英雄) 对跨 ≥2 个 derived 位置**(#57 §2 的观测属实),
  但 **40/40 对的 derived 众数 == drafted 位置** —— 槽位是位置的**噪声代理**,
  不是一个独立的事实。

**#57 §1 引为佐证的单调梯度,正是真梯度穿过这张 47% 混淆矩阵之后被抹平的样子。**
指纹是 **pos1≈pos2**:真值 pos2(中路)明显最高(38.4 vs 26.0),抹平后成 29.5/29.0。

`.dem` 的 `player_id` 12/12 对上 `team_slot` 是对的,但它验证的是「`team_slot` 没记错」,
**不是**「`team_slot` 能推出位置」。

## 4. 落地的修改

- **`tools/batch_test/soak/seed_draft.py`**:新增 `position_map(seed, pool)` /
  `seed_from_stamp(script_version)` / `positions_for_game(aj)`;**无法归属就返回
  `None`,不猜**(#53 的教训:哨兵值不能被误读成合法答案 —— 归属失败必须与
  「位置 = 某个数」在类型上就分得开)。docstring 补上「种子定死什么、不定死什么」。
- **`tp_attribution.py`**:唯一的 `team_slot % 5 + 1` 换成种子归属;无法归属时
  **stderr 告警**后退回各调用点声明的默认值(不再静默猜)。
- **`roam_conversion.py`**:同样换掉,**无法归属的局直接丢弃**(它本来就对
  非 mirror 局 `return None`,调用方已处理)。
- **`tests/test_hero_position.py`(6 节 18 例)** + 真实语料 fixture
  `tests/fixtures/seed_position_spread_20260820.json`(40 对 (种子,英雄) × 291 局的
  槽位分布,从真数据生成)。关键的是第 3 节(**把每个 team_slot 旋转一格,答案必须不变**)
  和第 6 节(**源码级棘轮**:`tools/` 下再出现该公式立刻红)。

### 4.1 五次变异测试(各恰好点名缺陷)

| 变异 | 结果 |
|---|---|
| `positions_for_game` 归属失败时回退成槽位猜测 | 挂「roster 不符 → None」 |
| `seed_from_stamp` 无匹配时给默认种子 | 挂 2(暖场 stamp / 无种子局) |
| `position_map` 按发牌顺序而非位置建表 | 挂并指名「英雄没带上 seed_draft 打印的位置」 |
| `roam_conversion` 回退成老公式 | 棘轮挂 1,报出文件:行 |
| `tp_attribution` 回退成老公式 | 棘轮挂 1,报出文件:行 |

**第 6 节棘轮第一版是错的**:正则要求 `team_slot` 与 `% 5 + 1` **相邻**,于是
`p["team_slot"] % 5 + 1` 这种**真实写法一个都不匹配** —— 前两次消费方变异**静默通过**。
是变异测试本身抓到的,已改成「同一行分别匹配」。**记一笔**:护栏的变异测试要用
**真实写法**去变异,不能用便于匹配的写法。

## 5. 判读影响(诚实边界)

- **我 08-19T09:04Z 在 `#31 §5` 下的裁定作废**:「lane-role = `team_slot % 5 + 1`,
  不需要 dumper 增强、不需要开 [harness] 需求」—— 前半句错。当时引为验证的 15 局梯度
  31.6/31.7/20.9/15.1/13.8 有一模一样的 pos1≈pos2 抹平指纹。后半句结论仍成立,
  但理由变了:不需要 dumper,是因为**种子本来就知道答案**。
- **`#37` 部分受影响**:`tp_attribution.gate()` 的
  `if t < 480 and pos.get(R,5) <= 3` 是**活闸门**,约一半行被错分 ⇒ **467 这个机会分母
  和「4 次响应者是 p1/p2/p3」要重算**。**主结论不动**:两臂用同一个错标签 ⇒
  11 vs 0 的对照无偏(p≈0.008 仍成立);「11 次里 7 次没抵达」「落点中位 2289u」
  是几何量,与位置无关。
- **`#41` 完全不受影响**(我逐行查过):`roam_conversion` 里 `pos` 只在 L181 作为
  episode 的注释字段输出,**从不参与过滤或聚合**。30.7% vs 47.1% 那条因果结论完好 ——
  这对 §G 的路 C 假说很重要。
- **`test_set.md` §M.3 反而被扶正**:那局
  (`spot_20260720_064050_1_main/20260720_072738`)的 stamp 是
  `mirror:depthnum:s555001:radiant`,**带种子**;按发牌 zuus = pos 2 = 核心。
  结论与原来一致,但原来那条推理是 47% 的运气,现在有正当出处。
- **选种反而更强**:`--find` 不但保证英雄出场,**还保证它以打印出来的位置出场** ⇒
  「只对核心/只对辅助生效」的 id **可以按种子挑**,#57 §3.3 提的 harness 改动**不需要做**。
  批测台 04:05Z「`axebuyblink` 4/4 种子全是 pos3」按 draft 表推是**对的**。

## 6. §0b 第十四例(新亚型)+ 全组通例

前十三例是「验收世界答错」或「消费方作废 helper 的推理」。这一例是:
**用来验证某个口径的那条证据,正是那个口径抹平之后的产物。**
47% 的正确率**照样**产出单调梯度,只是被压扁。

**通例(已写进 `test_set.md` §N.2):一个「标签 → 指标有梯度」的验证,
只有在与竞争标签比较过之后才有效。** 最便宜的做法就是本轮用的:两种标签各算一次
η²,再加一条随机打乱对照锚定零点。

## 7. 其他章程项

- **promote/reject**:无新判定。测试集**逐字未变**。
- **审批顺延**:`#52`(`tpwatch` 出集)、`#45` `roamreach`、`#44` `tparrive`
  第四轮顺延;**本轮新积压两条** —— `#55`(`defstale` 入集申请)、
  **`#56`(`axebuyblink` 的 (a) 成立但代价可能很大:平均 225s 零装备进度,
  最坏一局存到终场没买到)**。`#56` 时限最强:该 id **已在 04:11Z 波次里 armed**,
  下一波例行最早 10:11Z ⇒ **下轮首位裁定**。
- **成本**:MTD **$5.2934**,**在跑实例 0 台**(04:11Z 那 4 台已自毁,无泄漏)。
  本轮只做**只读 S3**(316 个 `analysis.json`,~2MB),未启动任何计费资源。
  滞后入账仍在追平中(预计爬到 ~$9-10),远低于 $90 刹车线。
- **Routine 健康**:五组本轮均有产出无空转。记一条好的自我约束:批测台在
  `queue.json` 无 pending、选种建议未获裁定时,**照 `test_set.md` 逐字上机而不自作主张**
  —— 正是章程要的分工(那条选种建议我本轮仍未裁定,并入 `#49`,不是时限项)。
- **效率台账**:今日周四,仅周日做,跳过。
- **patch 检查**:08-19T11:10Z 刚分片定级(P3),本轮不重复。
- **`DECISIONS_NEEDED.md`**:仍未创建 —— 本轮唯一的判定在自主授权内。本周 owner 邮件仍为零。

## 8. 验证(铁律第 6 条)

```
luacheck bots game --formatter plain   → 0 警告
lua5.1 tests/run_tests.lua             → 647 tests, 0 failures
python3 tests/test_hero_position.py    → PASS (0 failures, 18 checks)
python3 tests/test_detect_overchase.py → all checks passed
python3 tests/test_storyboard_smoke.py → all checks passed
python3 tools/batch_test/soak/seed_draft.py --selftest → PASS (4 seeds, 40 hero slots)
```

(容器初始无 Lua 工具链,已自行 apt/luarocks 安装。`bots/` / `game/` 本轮零改动 ——
Lua 侧跑绿是回归确认,不是因为动过它。)

## 9. 下次触发

1. **`#56` `axebuyblink`**(时限最强:已 armed,下一波 10:11Z);
2. **`#45` `roamreach`**(第四轮顺延,`roamstale` promote 换来的已知残留)、
   **`#52` `tpwatch` 出集**(要自己复核「`mode_retreat_generic` 没有 `Think()`」)、
   `#44` `tparrive`、`#55` `defstale`;
3. `wandlimbo` 机会普查**仍无人做**(连续五轮,是它 armed 的硬前置);
4. 都在等就做 `#51` / backlog #1 帧语料检索工具 / patch 分片 P3 / `[harness] #27`。

## 10. 本次会话 token 用量

```
TOKENS total_in=15,273,604 out=114,266 turns=118
```

(统计时刻之后的收尾回合(commit/push)不计入。)
