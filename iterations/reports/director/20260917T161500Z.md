# 总监 2026-09-17T16:15Z — RULING 71:GH #843 验收 2 的 python 半结清,但 (丙) 的落地位置被一次真读数改掉

**⭐⭐ 本轮的实质:#843 逐字要求把读者归属写进 `py_gate_manifest.json` 那一行的 `reason` 旁边,
而那个位置会自己擦掉自己** —— `py_gate_measure.py:305` 从测量结果重建整张表,
且这一半**没有** `carry_baseline()`。结论(丙)采纳,落地形式改成「算出来 + 棘轮钉住」。

零 AWS、零波次、`bots/` + `game/` **零 diff**、不发 owner 邮件。
成本(RULING 48 三段式):**零 EC2 / 零 CE / S3 读取 0 个对象(出网未计价)**。

---

## 〇、开工自检(铁律 10)+ 一次我自己造的假红,照登

`routine_selfcheck.sh` **前两跑都被它自己的守卫拒掉,两次都是当轮第一条命令**(纪律 3 家族):

1. `… | tail -60` ⇒ 逐字 `REFUSED: routine_selfcheck.sh stdout is a pipe; exit 2, nothing checked.`
   ⛔ 那个 `SELFCHECK_EXIT=0` 是**管道读者的码**。
2. 改重定向但**带了 `timeout`** ⇒ 逐字 `REFUSED: routine_selfcheck.sh is running under \`timeout\`; exit 2, nothing checked.`
3. 第三跑 `nohup … > /tmp/sc.log 2>&1 &` 才真跑起来。

📌 **章程第 0 条逐字覆盖第 1 跑,我只是没照做**(与 09-04 那一发同型)。
⚠️ 第 2 跑是**新的一发**:守卫拦的是 `timeout`,而章程第 0 条只说「走 `rc.sh`」——
`rc.sh` 不带 `timeout`,所以照做本来也能躲过,**这仍然是习惯问题不是门的问题**。

### ⛔ 本轮 `TRUNK RED --(python)` 是假的,而造它的人是我

自检打了 `TRUNK RED -- a python test is failing ON THE WORKING TREE.`,**这一条我判为假红,理由是读数**:
`PY_LOG` 全文 **102 行**、`grep -E '^(FAIL|UNCERTIFIABLE)'` **零条 FAIL**、
**没有** `N passed, N failed` 汇总行,末行停在 `PASS tests/test_selfcheck_exit_attribution.py`
(字母序中段)⇒ **它是被截断的,不是红的**。

**成因是我自己**:我为了自己量一遍,另起了一个 `bash tests/run_py_tests.sh`,
随后察觉这与自检的 python 腿并发(部分 python 用例会起 `lua5.1`,即 RULING 56 的形状),
于是 `pkill -f 'run_py_tests.sh'` —— **那个模式同时打死了自检自己的那一份**。

⚠️ **这是 RULING 56 的第二例,机制不同、形状逐字相同:我自己的并发动作造出一条红,
而红出来的名字是无辜的那个**。上一例是两个进程抢同一个 gitignored 文件,这一例是
**一个 `pkill` 模式同时匹配上别人的同名子进程**。
📌 **两例的共同前提是「自检在后台跑」这件事不可见** —— `pgrep` 当时确实只回一行
`492 bash tools/agent/routine_selfcheck.sh`,**因为那一刻它的 `run_py_tests.sh` 子进程还没起**。
⇒ **教训(不新开 issue,是习惯):后台跑着自检时,任何 `pkill -f <模式>` 都要先确认那个模式
不匹配自检的子进程树**;要杀自己的东西就记下 PID 杀 PID。

⛔ **不并进 GH #856,理由写明**(那条 open issue 逐字是「开工自检与 push gate 并发抢
`soak_side.lua` ⇒ 假红拒 push;修法已在仓库里(`lua_corpus.py`)」):
#856 的机制是**两个进程抢一个文件**,修法是让读方走 `read_lua()` 判 exit 2;
本轮这一发的机制是**一个 `pkill` 模式误伤别人的同名子进程**,
`soak_side.lua` 全程 `absent (clean)`(事后 `ls` 确认)。
📌 **同一个失效家族(我的并发动作造出一条挂着别人名字的红),但不是同一个缺陷** ——
并案会让 #856 的修法看起来覆盖了它,而 `read_lua()` 对一个被 `SIGTERM` 掉的
`run_py_tests.sh` **无话可说**。

⇒ **本轮 python trunk 的真状态由我自己单独重跑确立**(本节末「§〇.1 重跑」),
⛔ 不拿自检那一行当读数,⛔ 也不写「trunk 绿」直到那一跑出来。

### §〇.1 python trunk 重跑(自检跑完、无并发之后)

自检**真的跑完之后**(见下面那条读法错误)、树上零并发、`pgrep -c lua5.1` = **0**,
单独重跑 `bash tests/run_py_tests.sh`,逐字:

```
147 passed, 0 failed, 1 uncertifiable
uncertifiable: tests/test_selfcheck_lua_leg.py
```

⇒ **python trunk 绿(`0 failed`)** —— **自检那条 `TRUNK RED` 确认为我造的假红**,§〇 的判断被这一跑证实。
⇒ 连带:自检里 `ATTRIBUTION BROKEN -- a leg declared 0 un-run test(s), 2 name(s) reached this line`
**同样是同一个 `pkill` 的下游** —— `run_py_tests.sh` 被 `SIGTERM` 掉,
它那行 `%d passed, %d failed, %d uncertifiable` **从未打印**,提取器于是读到 0。
⛔ **所以本轮自检 5 条 FINDINGS 里有 2 条是我造的**
(`trunk-red(python)`、`ATTRIBUTION BROKEN`),真发现是
`cadence` / `queue-rulings` / `owed-executions` / `lua-coverage` 四条。
⚠️ 并且 `ATTRIBUTION BROKEN` 那行自己写着「Fix the extractor, not this line」——
**本轮不照做,因为前提不成立**:提取器没有漂,是它的输入被我杀掉了。
⛔ **这正是那行警告防的那种误修**,照登免得下一轮真去改提取器。

⛔ **那 1 条 `uncertifiable` 不折进 pass 数**(纪律 3 末句):
`tests/test_selfcheck_lua_leg.py`,逐字病因是**看守腿自己的 120s 上限**:

```
NOTE  5a0 cost: 135 file(s) [discovered -- the leg never reached its count line] in 120.0s (budget 120s)
UNC   5a: a clean tree does not report TRUNK RED [the clean run did not finish inside 120s -- these four did NOT run; 5a in particular would pass vacuously]
```

📌 **这是欠条 `selfcheck_watchdog_python_cases_never_run`(RULING 57)的第十七轮**,
⛔ 本轮不处置(工作单元要小),已在交棒 ⑧ 更新为「第十七轮」+ 新信息。
⭐⭐ **而它与 §2.5 的伪证路径 (C) 是同一个东西,这不是巧合而是同族**:
「一条腿获得时钟上限 ⇒ **先砍最慢的**」—— 那里是我为 python 覆盖关系写下的**预测**,
这里是看守腿上**已经发生了十七轮**的实测。两处互为佐证。

**两个退出码,口径分开写:**

1. **那条测试自己的裸码 = `2`,这是量出来的**(走 `rc.sh`,未经管道):
   `RC_EXIT=2  (bare exit code of: python3 tests/test_selfcheck_lua_leg.py)`,
   并附工具自己那句 `2 is this repo's could-not-run code … Not a pass.`
2. **`run_py_tests.sh` 整个套件的裸码 = `2`,这是从源码推出的,不是量出来的**
   (`fail=0` 走过第一个分支、`unrun=1` 命中 `if [ "$unrun" -ne 0 ]; then … exit 2`)。
   ⚠️ 我用 `nohup … &` 起的它,拿回来的 `exited with code 0` 是**等待者的码不是套件的码**
   —— 纪律 3 的同一个坑,**本轮第四次踩它的家族**。⛔ **不拿那个 0 当读数。**

📌 **两个口径都写出来的理由**:第 1 条**证实**了第 2 条的推导(同一个 `unrun` 语义、同一个 `2`),
但**证实不等于同一个读数** —— 第 1 条量的是那一个文件,第 2 条要的是那个循环的汇总码,
⛔ 把前者当后者就是纪律 4(「同一个结论不是同一个理由」)。


### 自检其余各腿

- `push-gate`:`push gate armed (core.hooksPath=.githooks)` ✅
- `unlanded` / `a-evidence-owed` / `stale-waits` / `stable-anchors` / `promote-atoms` /
  `inverse-gates`:无新发现(`inverse-gates` 逐字 `no armed id hangs under an unarmed gate -- OK`)
- ⚠️ **`cadence` / `queue-rulings` / `owed-executions` 三条是 FINDINGS,不是「无新发现」**
  —— 我起初按前几轮的印象写成了后者,**是收尾归因块(见下)把它改回来的**。
  ⛔ **照登这次更正**:那三条各自是**常驻**发现(cadence 的历史 GAP 行、
  待裁/欠条清单非空),**每轮都在**,而「每轮都在」正是它们最容易被写成「没有」的原因
  (被试文件头对 `lua-coverage` 写的「a check that is red from its first day gets read as
  furniture」是同一句话)。⇒ 本轮不处置这三条(工作单元要小),但**不许记成绿**。
- ⚠️ 两条 python 用例 `UNCERTIFIABLE`:`test_lua_gate.py` / `test_luacheck_gate_soakswitch.py`,
  逐字 `lua5.1 is absent and this file's subject needs it` / `luacheck is not installed`。
  **它们跑在自检自己装 `lua5.1` 之前**(跑完后 `command -v lua5.1` = `/usr/bin/lua5.1`)⇒
  这是**顺序问题**,不是缺工具。📌 与交棒 ⑧「自检那三条 python 用例(第十六轮)」同族,**本轮不处置**。
- `lua-coverage`:**exit 3,见 §三**。
- `trunk-red(lua)`:**`134 tagged detector file(s), 0 failures`** ⇒ 快 Lua 检测器子集**绿**
  (该腿自己声明 `FAST SUBSET, not the full suite`,⛔ 不当全集读)。

**收尾归因块逐字**:

```
legs run              : 13
FINDINGS (exit 3)     : cadence queue-rulings owed-executions lua-coverage trunk-red(python)
UNCERTIFIABLE (exit 2): none
NOT RUN (inside a leg): tests/test_lua_gate.py tests/test_luacheck_gate_soakswitch.py
ATTRIBUTION BROKEN -- a leg declared 0 un-run test(s), 2 name(s) reached this line.
selfcheck worst exit: 3
```

⛔ **`SELFCHECK_EXIT` 真码 = `3`**(裸读,第三跑,未经管道)。
其中 2 条 FINDINGS 是我造的(§〇.1),真发现 4 条。

### ⛔ 本轮那条新腿 `py-coverage` **没有在任何一次真实开工自检里跑过**,照登

这一跑是 **15:5x Z 启动的**,而 `py-coverage` 腿是我在它**跑起来之后**才加进
`routine_selfcheck.sh` 的 ⇒ 上面那个 `legs run : 13` **不含它**,
它在本轮的**唯一**读数是我**手动**直调被试与钉子(§2.4 / §2.6)。

📌 **这与 `gh624_lua_gate_first_live_read` 逐字同型**(那条欠条的原话:
「⚠️ **覆盖不等于活读**:纯逻辑由 `tests/test_lua_gate.py` + 变异台覆盖,
而**这条腿在一次真实的 `git push` 上一次都没跑过**」)。
⛔ **所以不许写「新腿已验证」** —— 已验证的是**被试**和**钉子**,不是**腿的接线**。
`bash -n` 只买了语法,`tests/test_selfcheck_exit_attribution.py`(**46 checks**,绿)
只买了「声明的腿数与找到的名字数一致、无漂移横幅」。
⇒ 已写进交棒 ⑯:**下一个组的开工自检就是它的第一次活读**,那一轮要核
`legs run` 变成 **14** 且 `py-coverage` 出现在归因块里。

### 章程 1.5 交棒腿(RULING 64/67)

`carry_item_issue_state.py`,**裸码 `RC_EXIT=0`**,逐字:

```
corpus   : issue corpus iterations/data/issue_state_snapshot.json, fetched 2026-09-17T13:44:00Z (2.1h old), 12 issues
scanned  : 1 entry (of 90), 1 carry segment(s), 7 GH ref(s)
OK            GH #856, GH #867, GH #240, GH #843, GH #859, GH #810, GH #528
every carried GH ref is open.
VERDICT  : OK (exit 0)
```

⇒ 无 `STALE-CARRY`、无 `NO-HANDOFF`(上一条写了清单)。

---

## 一、体系健康巡检(章程 2e,逐字命令 + 四条取法)

`git fetch origin main` → `test -f .git/shallow && git fetch --deepen=200 origin main`,
**deepen 后 `git rev-list --count origin/main` = 250**(浅克隆 (戊) 已解除,本轮确有日期推断)。
取数时刻 **2026-09-17T15:51:44Z**,按名字序、从 `origin/main`、只认报告名形状:

| 组 | 最新报告 | 距取数 |
|---|---|---|
| batch-desk | `20260917T151957Z.md` | 0.5h |
| replay-check | `20260917T125517Z.md` | 2.9h |
| strategy | `20260917T133646Z.md` | 2.3h |
| hero | `20260917T111846Z.md` | 4.6h |
| director | `20260917T133018Z.md` | 2.3h |

⇒ **五组全活,无点名,无红色升级。**

⛔ **更正我自己在本节的第一版措辞:我写过「无 cadence GAP」,那是错的。**
`cadence` 腿本轮打 **5 条 GAP**(阈值 `> 3.5h`),逐字:

| 组 | 区间 | 时长 |
|---|---|---|
| **director** | 09-16T19:00Z → 09-17T01:00Z | **6.0h** |
| **director** | 09-17T04:20Z → 09-17T13:30Z | **9.2h** |
| hero | 09-16T17:21Z → 09-16T21:30Z | 4.1h |
| hero | 09-17T11:18Z → **now** | **4.6h** |
| strategy | 09-16T19:27Z → 09-16T23:20Z | 3.9h |

📌 **两条里最长的两条都是我自己这个座位的** —— 不写出来就成了「巡检别人」。
⚠️ **那个 4.6h 正是我在上面表里当成「健康」登记的同一个数字** ——
**同一个量,一个阈值说 OK、另一个说 GAP**,而我只抄了前者。
⛔ **这就是本节 (丁) 那一族的形状**:取法对了,**而我对取回来的东西挑了顺眼的那个读**。

**按 e-bis 交叉读 `unlanded`(GAP 变成点名之前的必经一步)**,逐字
`OK: no unlanded work in the certifiable window.` ⇒ **无「活干了没落地」的证据**
⇒ 按 e-bis (甲)/(乙) 二分,这些 GAP **不是落地失败**;而 4.1–9.2h 对 2h 节奏
**不构成停摆**(hero 11:18Z 真落了东西,director 13:30Z 亦然)⇒ **不点名、不升级**。

⚠️ **但 `unlanded` 这条 OK 自带不可认证性,照登,⛔ 不当成强证据**:
`shallow clone : YES`、`REFUSED refs : 1250 of 1301`(逐字 `uncertifiable, mostly stale
session branches with no merge-base against a shallow trunk`)、
`REFUSED below : 2026-09-17T02:42:07 (98ba387)` —— 即**graft 点以下一律不报**,
`certifiable refs : 51`。⇒ 它说的是**「在能认证的那 51 个 ref 里没有」**,
不是「没有」。📌 与章程 2e (戊) 同源:浅克隆在这里**不造假新近,而是直接弃权**。

---

## 二、RULING 71 —— GH #843 验收 2,python 半结清(⚖️ 本轮主交付)

### 2.1 待裁是什么

#843 第二半逐字待裁:「`over_per_test_cap` 且作用域是整个 `tests/` 目录的普查,
要不要有一条**比 cap 更早**的读者」,验收逐字:

> `py_gate_manifest.json` 里 `in_gate: false` 且 `reason: over_per_test_cap` 的行,
> 各自有一条**写下来的**读者归属(哪怕结论是「无」)。

欠条 `py_gate_evicted_spot_az_spread` 的三选一:(甲) 抬 cap、(乙) 迟滞(GH #810)、(丙) 明确接受 + 写下归属。

### 2.2 裁定:采纳 (丙),**并改它的落地位置**

⛔ **不抬 cap (甲)**:成员资格只看实测秒数、永不看文件名(GH #616 约束 1),
`py_gate_measure.py` 自己写 `do not raise the slack`,#810 亦明确不推荐。
⛔ **(乙) 迟滞不由本行代办**:仍在 GH #810 名下。

⭐ **(丙) 的结论对,而它逐字指定的那个落地位置是错的,理由是一次真读数**:

```
py_gate_measure.py:305    "tests": dict(sorted(tests.items())),
```

⇒ 每次 measure **从测量结果重建整张 `tests` 表**;而这一半**没有** `BASELINE_KEYS`、
没有 `carry_baseline()`、也没有 Lua 那半那条 `REFUSED -- these baseline keys are not in
BASELINE_KEYS` 守卫(`lua_gate_measure.py:353/357/376/462`,GH #783 买的,**从未移植**)
⇒ 手写 `reason` 旁边那个字段,**寿命 = 到下一台容器跑 measure 为止**,
**而它消失时长得像什么都没发生**。

📌 **这正是本仓最贵的那个形状**:一个修复把自己的存活寄托在一张**每轮被重写的表**上
(与 §2.6 立案句「一条只活在别人叙事清单里的欠裁,平均寿命是一轮」同型,只是这里的
「别人的清单」是一个 `json.dump`)。

⇒ **读者归属改为「算出来 + 棘轮钉住」**,落地三件 + 一条腿。

### 2.3 落地物

| 文件 | 是什么 |
|---|---|
| `tools/agent/py_gate_coverage.py` | 新。集合运算,**不跑任何测试**,实测 **0.02s** |
| `tools/agent/py_gate_coverage_baseline.json` | 新。基线(`uncovered_count: 0`, `rows_with_no_reader_count: 0`) |
| `tests/test_py_gate_coverage.py` | 新。棘轮钉子 **25 checks / 0 failures** + 变异台 **5/5** |
| `routine_selfcheck.sh` 第 `py-coverage` 腿 | 新。紧邻它的 Lua 孪生 `lua-coverage` |

### 2.4 真读数(裸码,未经管道,走 `rc.sh`)

```
PY GATE COVERAGE  disk 148 | push gate 130 | 开工自检 leg 148  (glob tests/test_*.py)
  UNCOVERED (no automatic reader runs these): 0 of 148 (0%)  {}
  out-of-gate rows with a written reader: 17 of 17  (GH #843 acceptance 2)
```

(`disk` 由 147 升到 **148** 是本轮自己那个钉子文件,它**也被那条腿读**,自洽。)

⇒ **#843 验收 2 对 python 这半成立**:17 行**全部**有读者,
**两个展品都在那 17 行里** —— `test_bots_walk_farm_only.py`(3.723s,#843 立案的那一条)
与 `test_spot_az_spread.py`(3.756s,本座位 13:xxZ 轮登记的那一条)。

### 2.5 ⭐ 答案「全覆盖」是挣来的,不是继承的;两半答案不同的理由是**选择器的形状**

这是算术不是口味:

| | 开工自检那条腿的选择器 | 后果 |
|---|---|---|
| **python** | `for f in tests/test_*.py`(**glob**,且该腿**无时钟上限**) | glob 按构造是 manifest 的**超集** ⇒ 出闸的行仍被读 ⇒ **0/148** |
| **Lua** | **tag**(`[detector]`/`[ratchet]` + 四个具名文件) | 一个 `[hero]` 标签的超 cap 文件**同时从两边漏下去**(GH #806)⇒ **115/506 = 23%** |

⛔ **所以它必须是棘轮,不是段落。** `lua_gate_measure.py` 当初用**散文**做过同一个承诺
(「超 cap 的测试只由开工自检和全集覆盖」),而 GH #806 把那句话**实测为假**。
python 这个承诺**今天为真**,而在本轮之前**没有任何东西在断言它**。三条可达的伪证路径:

- **(A) 测试落进子目录** —— 那个 glob 是**深度 1**,而 `tests/mock/`、`tests/fixtures/`、
  `tests/frames/` **已经存在** ⇒ `tests/mock/test_foo.py` 是**可达的落点不是假想**,
  它对 glob 与对闸**同时**不可见。
- **(B) runner 的 glob 被收窄** —— 增量走查正是 #843 自己列的选项之一,
  而它会**把这一半变成 Lua 那一半**。
- **(C) 该腿获得时钟上限** —— 那会**先砍最慢的**,即本关系存在的**全部人口**。
  **RULING 57 是判例**:看守腿的 120s 上限让三条 python 用例连续十六轮 `did NOT run`。

**(A) 与 (B) 已做成变异台**(§2.6)。**(C) 按构造不可由集合运算测出** ⇒
**已在被试与钉子的文件头逐字写明「不为它假装一条检查」** —— 假装一条,才是同一个缺陷上移一层。

⛔ **选择器不被重新拼写**:那个 glob 是从 `tests/run_py_tests.sh` **里抽出来**的
(`RUNNER_GLOB_RE` 匹配它自己的 `for f in …; do`),抽不出来或**有歧义**就 **exit 2**。
理由:第二份拼写是本仓记录最多的缺陷(「一份基线由一个正则写、另一个正则读,
哪一边漂了就不再匹配」)。

### 2.6 钉子与变异台

`tests/test_py_gate_coverage.py`:**25 checks / 0 failures**,被试成本 **0.02s**。
变异台 **5/5 全死**(5 个变异体各自跑全钉子):

| 变异体 | 打死它的检查 |
|---|---|
| `M1` 走查退化成深度 1(`os.walk` → `listdir`) | `2b` `2c` `5a` |
| `M2` 硬编码 glob 而不从 runner 抽 | `1e` `1f` `6d` |
| `M3` 歧义/读不出时**猜一个**而不拒绝 | `2d` `2e` `2f` |
| `M4` 归因表**无条件记功** | `6d` `6e` |
| `M5` 空语料**照常给 0** | `3b` `3c` |

⭐⭐ **`M5` 那条守卫是我在写的时候对着自己的代码读出来的,不是变异台逼出来的,照登**:
本关系**健康时的答案就是 `0 uncovered`**,于是 `tests/` 缺失或被清空时,
走查得到空集、`uncovered` 也是空集,工具会打 `0 of 0 (0%)` 并 **exit 0** ——
**一个与健康读数逐字符相同的空转通过**,而那正是本文件立法反对的那个形状。
⇒ 已加:空语料 ⇒ **`UNCERTIFIABLE`(exit 2)**,并且**根本不打那行表头**(`3c`)。
📌 **这一条是「被试自己踩中它正在立法的缺陷」,第二次出现在同一轮**(第一次是 §2.6 末那个
`;` 正则,方向相反:那次是**误判为不可认证**,这次是**误判为通过**)。

**台子建在临时目录的副本上**(`PY_GATE_COVERAGE_SUBJECT` 环境变量指向变异体),
**仓库文件全程未被写过**,还原走文件副本 + `sha256sum -c` 逐字
`tools/agent/py_gate_coverage.py: OK`(纪律 1;RULING 70 同轮栽过的那一下)。

⭐⭐ **`M4` 是纪律 2 的现场,照登:它只被合成用例杀掉。**
真语料里 17 行**全部**由那条腿读 ⇒ 「无条件记功」与正确规则**逐字同答**,
`§6a` 永远不必判别。`§6d/6e` 造了一行「出闸 **且** 在收窄后的 glob 之外」的测试,
唯一正确答案是 `NONE` —— **那恰好就是 #843 的「哪怕结论是「无」」那一格**,M4 当场死。
⛔ **没有那条合成用例,这个台子会报 3/4 并读起来像 4/4。**

⚠️ **同轮自证一次 fail-closed 是真的,照登**:`reader_glob()` 第一版正则把 `;`
吃进捕获组 ⇒ 首跑逐字 `UNCERTIFIABLE -- expected exactly one \`for f in <*.py glob>; do\`
… found 0 []`、**裸码 2**,⛔ **不是 0**。
📌 **读不出别人选择器的那一轮,没有资格给那个读者记功** —— 这是设计,不是意外。

### 2.7 ⛔ 明写限界:「被读」不等于「被计价」,另一半归 GH #839

本工具回答的是**谁读它**,**不回答**它有没有被计进闸的累计预算。
没有 manifest 行的文件 `py_gate.py` **照跑**(逐字 `N new test(s) not in the manifest were run
anyway, costing …`)⇒ 它**被读**,所以本关系把它算作已覆盖,**这是对的**;
但它**未被计价**,而那恰好是 **GH #839** 的题目(「py_gate manifest 又漂了:7/141 个测试无行」)。

⇒ **本工具绿,不等于 manifest 新鲜,⛔ 不许被当成后者引用。**
⚠️ **而本轮自己就是那个population 的一员,照登**:`tests/test_py_gate_coverage.py`
**落地时没有 manifest 行** ⇒ 它在 shipping 当天就属于 #839 名下的那 N 个。
已写进被试文件头(`STATED LIMIT` 节),**免得下一个读者把两个问题读成一个**。

### 2.8 欠条处置

`py_gate_evicted_spot_az_spread` ⇒ **退休**(`retired`,28 条),
`retired_because` 写全 (丙) 的采纳与落地位置的修改。
⛔ **(乙) 迟滞不随本行结清**,仍在 GH #810。

---

## 三、`lua-coverage` 棘轮本轮 exit 3:3 条,不是交棒里记的 2 条

自检 `lua-coverage` 腿逐字:

```
LUA GATE COVERAGE  disk 506 | push gate 341 | 开工自检 leg 134 | known_red 6
  UNCOVERED (no automatic reader runs these): 115 of 506 (23%)  {'no_manifest_row': 53, 'timed_out': 60, 'too_slow': 2}
  1 file(s) left the uncovered set since the baseline -- re-run with --update-baseline to bank it.
    NOW COVERED    tests/test_fieldsip_atom_pricing.lua
UNCOVERED SET GREW -- 3 file(s) that nothing reads automatically:
    NEW UNCOVERED  no_manifest_row    tests/test_campbind_poke_real_frame.lua
    NEW UNCOVERED  no_manifest_row    tests/test_dusttower_dive_guard.lua
    NEW UNCOVERED  no_manifest_row    tests/test_fieldsip_transfer_receiving_site.lua
```

⚠️ **交棒 ⑨ 记的是「2 条」,本轮实读 3 条** —— 数字在长,而长的方向正是「没人读」。
(同一读数里另有一条**已脱离**:`NOW COVERED tests/test_fieldsip_atom_pricing.lua`,
那是 RULING 41 打 `[ratchet]` 标签的后效,**按设计工作**。)

**归属已查**(`git log -1 -- <file>`,在 deepen 到 250 commits **之后**取,章程 2e (戊)):

| 文件 | commit | 作者 / 时刻 |
|---|---|---|
| `test_campbind_poke_real_frame.lua` | `47374084` | **录像组** 09-17T13:00Z |
| `test_dusttower_dive_guard.lua` | `3ad1a9c3` | **总监本座位** 09-15T03:55Z(RULING 43 那轮) |
| `test_fieldsip_transfer_receiving_site.lua` | `3ad1a9c3` | **总监本座位** 09-15T03:55Z |

⛔ **三条里两条是我自己落的,不许写成「退回某组」。** 三条都没有 `[detector]`/`[ratchet]` 标签。
⛔ **不在本轮顺手重测**:GH #810 逐字禁止「随便找台容器跑一遍」——
重测买到的 `no_manifest_row = 0` 是**拿闸的覆盖面换来的**,账面上却长得像把棒还了。
⇒ 便宜的正解是**打标签**(tag 选择器与 cap 无关),但**打标签前要先各跑一次确认不红**,
否则等于把三条红塞进每个组的开工自检 —— **RULING 41 的后效就是判例**。

⇒ 按 §2.6 (乙) 登记欠条 **`lua_coverage_uncovered_grew_3files`**,
`done_when` = `json_value`(`lua_gate_coverage_baseline.json:no_manifest_row_count <= 53`),
并写明「`<= 53` 是『没再长』不是『修完』」。

---

## 四、撞出来的第二条:python manifest 没有 `carry_baseline()`(GH #783 从未移植)

**今天零代价,照实登记。** 本轮普查 147 行:`reader` 字段**零命中**,
顶层只有测量元数据 ⇒ python manifest 里**当前没有任何基线性质的键**,
**所以这是潜在缺陷不是现行缺陷**。

⛔ **而它正是 §2.2 改掉 (丙) 落地位置的那条理由**:任何人往这张表里写一个
「人写的、measure 不产出的」字段(读者归属、赦免、**迟滞状态**),
下一台容器跑 measure 就**静默擦掉它**,而 Lua 那半**两周前就为这件事买了守卫**。
⇒ 失效方向:**永远静默,永不误报**。

⭐ **有一个真触发器,写明以免它被当成纯理论**:**GH #810 的 (乙) 迟滞按构造需要一个
跨 measure 存活的状态**(「已在闸里」这件事本身)⇒ **(乙) 落地的那一轮必须先付本行**。

⇒ 登记欠条 **`py_manifest_no_carry_baseline_port`**,`kind: manual`
(理由:(乙) 以「表里永远没有那种键」为正确答案,而一个 `json_value` 判据
**从落地当天就是绿的**,会把「还没做」读成「已办」)。

---

## 五、成本核查(闸 (iii),读批测台报告,本座位不花钱)

批测台 `20260917T151957Z.md` 逐字:

```
92.001	USD
   source: budgets/dota2bot-batch (free) | forecast 118.091 | budget limit 100.0 | budget refreshed 2026-09-17T13:24:58Z
   >= $35, but headroom to the $90.00 brake is $-2.001
```

⇒ **MTD `$92.001` > 刹车 `$90.00`,零发波(批测台第四十二轮持有)。** 本轮总监无异议,**不解冻**。
新速率点 `$0.192 / 9.6378h` = **`$0.478/天`**,落在既有带 `$0.40–0.92/天` 内 ⇒ 不收窄该带。

⚠️ **`forecast 118.091` > owner 批准线 `$100`** —— 这不是新发现(带已结转),
但它是 **W38 周日邮件(09-20)**里必须出现的那一行,已在 `DECISIONS_NEEDED` 待办中。
⛔ **本轮不单独发邮件**(章程:每周最多 1 封,W38 那封是 09-20)。

---

## 六、铁律 6 三条腿(push 读数)

**推之前先单独量了静态那半**(裸码,走 `rc.sh`;`luacheck_gate.sh` **按名字排除**
`bots/Customize/soak_side.lua`,所以它与后台自检的 Lua 腿**没有** RULING 56 的竞态):

```
luacheck bots game: 0 warnings
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)
```

三条腿的完整读数(`GATE_EXIT=` / `py gate:` / `lua gate:`)见文末「同轮收尾追加」节 ——
按章程顺序,**先 push 再发表任何带引用的评论**(GH #290)。
⛔ 本轮 `bots/`+`game/` 零 diff,**不跑 Lua 动态全集**(~100min,GH #124)。

---

## 七、本轮没做什么(明确写出来)

- ⛔ 未 promote / 未 reject 任何 id(测试集无三条件齐备的候选;`bots/`+`game/` 零 diff)
- ⛔ 未审批 test_set.md 的 pending 提议(本轮无新提议)
- ⛔ 未发 owner 邮件(W38 09-20 那封)
- ⛔ 未跑 patch 检查(低频,上次在带内)
- ⛔ 未跑 Lua 动态全集(~100min,GH #124;`bots/`+`game/` 零 diff)
- ⛔ 未处置交棒 ①②③④⑤⑥⑦⑧⑩⑪⑭(工作单元要小)

---

## 八、下次触发

① **GH #856** 修法第 3 条剩 9 个候选
② 棘轮加宽(**GH #867**)
③ **GH #240** 余下
④ `carry_mark_prose_vs_list`(章程 1.5 已登记的洞,发生率 0/86 但可达)
⑤ **GH #843 (乙)** —— ⭐ **本轮已结清 (丙)/#843 验收 2 的 python 半**,
  剩下的就是 **GH #810 (乙) 迟滞**,⚠️ **而它现在有一个前置**:`py_manifest_no_carry_baseline_port`
  (§四 —— 迟滞需要跨 measure 存活的状态)
⑥ **GH #859**
⑦ **GH #810**:待裁 1(`unanswered > 0` 要不要拒推)+ (乙) 迟滞
⑧ 自检那三条 python 用例(**第十七轮**;本轮又见两条 `UNCERTIFIABLE`,成因是
  **它们跑在自检自己装 `lua5.1` 之前**,是顺序不是缺工具 —— 这是新信息)
⑨ **`lua-coverage` 新增 3 条**(**不是 2 条**)—— 已登记
  `lua_coverage_uncovered_grew_3files`,两条归本座位、一条归录像组;
  ⛔ 打标签前先各跑一次确认不红
⑩ P4.2 narrat 1 / `$0.90` 重裁 / **GH #528** / patch 缺口 P3
⑪ `path_contains_any` 那两行
⑫ **新**:`py_manifest_no_carry_baseline_port`(§四),⭐ 与 ⑤⑦ 是**同一条线**
⑬ **W38 周日汇总邮件(09-20)**:带第 15/16/18/19 条 + 第 17 条已撤回 +
  **RULING 70** + **RULING 71** + ⚠️ **`forecast $118.091` > 批准线 `$100`** 那一行
⑭ `lua_gate_manifest.json` 已陈旧(89 个新测试不在册、9 个超预算被 EXCLUDED、
  真实开销 665.65s vs 预算 530.0s)。⛔ 重测**要先读 GH #810**
⑮ **新**:⛔ **`-f` 模式匹配自己的命令行 —— 本轮同一个家族栽了三发**,是习惯不开 issue:
  (i) `pkill -f 'run_py_tests.sh'` 打死了自检自己的那一份 ⇒ 假红 + `ATTRIBUTION BROKEN`(§〇);
  (ii) `pgrep -fc routine_selfcheck.sh` **匹配上我自己那几个等待壳**(它们的命令行里含这个串)
  ⇒ 自检其实早就跑完,我**多等了一大段**,判据一直读 `5`;
  (iii) `nohup … &` 拿回来的 `exited with code 0` 是**等待者的码**不是套件的码(§〇.1)。
  ⇒ **规矩**:`pgrep`/`pkill` 用 `-x -f '<完整命令>'` 或直接记 PID;要退出码走 `rc.sh`
⑯ **新**:`py-coverage` 腿的**第一次活读**欠着 —— 本轮它**没进**任何一次真实自检
  (`legs run : 13` 不含它)。下一个组的开工自检要核 `legs run` = **14** 且归因块里有 `py-coverage`

---

## 九、token 用量

```
TOKENS total_in=24,379,234 out=102,469 turns=133
```

(`tools/agent/token_usage.py`,裸码 `RC_EXIT=0`。铁律 8:这是到统计时刻为止的数字,
统计后的收尾回合不计入。)
⚠️ **本轮 `total_in` 明显偏高,归因写明免得下周汇总时当成异常**:主要不是重复劳动,
是**两段真实的串行等待**里我反复轮询 —— (i) 开工自检的 Lua 腿(而 `pgrep -f` 那一发让我
**等过了头**,§〇.1 与交棒 ⑮(ii));(ii) python 套件 148 个文件。
⇒ **下一轮的具体改法**:等长任务用 `run_in_background` 的 `until` 循环**一次**挂住,
⛔ 不要每隔一两个回合 `tail` 一次日志 —— 那些轮询每一次都要重读整个上下文。

---

## 十、[同轮收尾追加,push 之后]

**push 两次,⛔ 全程无 `RULE6_BYPASS`**,按章程顺序(先分支、再 `HEAD:main`):

**① 分支(新 ref)`git push -u origin claude/compassionate-albattani-j2mbif`,裸码 `RC_EXIT=0`**,三条腿逐字:

```
luacheck bots game: 0 warnings
GATE_EXIT=0  CLEAN (iron rule 6 static half passed)

py gate: 131 ran, 0 findings, 0 uncertifiable, 47.0s
  scope: 130 fast python ratchets (selected by measured seconds <= 3.0s, cumulative budget 90.0s)
  NOT claimed here: 17 slower python tests (run by tests/run_py_tests.sh in 开工自检), and the Lua dynamic half (GH #124).
  1 new test(s) not in the manifest were run anyway, costing 0.34s: tests/test_py_gate_coverage.py
  => the hook's REAL cost this run is 46.96s against a 90.0s budget.

lua gate: scope = 8 path(s) changed vs the pushed refs' base (base=fallback-merge-base)
lua gate: 422 ran, 0 findings, 0 uncertifiable, 9 unanswered, 6 known-red, 657.0s
```

`* [new branch]`。**`base=fallback-merge-base`** ⇒ **RULING 70 的新-ref 路径按预期走**
(本会话分支在 origin 上此前不存在)。

**② `git push origin HEAD:main`,裸码 `RC_EXIT=0`** ⇒ `3f91ef6d..05b6669b  HEAD -> main`,逐字:

```
RULE6_MEMO=REUSE  a green three-leg reading taken 2026-09-17T16:41:11Z on THIS EXACT tree.
  This is a REUSE, not a skip: all three legs really ran, on
  HEAD^{tree}=72fba3d7... with origin/main=3f91ef6d... and a clean working tree.
```

⭐ **memo 命中,而它命中的前提正是章程那条「先推分支、再推 main」**:分支推**不移动**
`origin/main` ⇒ 第二推的 memo 键 `(HEAD^{tree}, origin/main)` 与第一推**逐位相同**。
⛔ 反序会让这个键恒定 MISS(RULING 69/70 的立案算术),本轮**照顺序走,没付那笔钱**。

### ⭐⭐ 两条独立佐证,出处都不是我

1. **`py gate` 自己点名了那 17 行的读者**:逐字
   `NOT claimed here: 17 slower python tests (run by tests/run_py_tests.sh in 开工自检)`
   —— 数字与 RULING 71 **逐位相同(17)**,而这是**闸的自述**,不是我的集合运算。
   📌 **两个独立来源给出同一个归因**,不是同一句话被引用两次(纪律 4 的正面用法)。
2. **§2.7 那条限界当场被实测**:逐字
   `1 new test(s) not in the manifest were run anyway, costing 0.34s: tests/test_py_gate_coverage.py`
   ⇒ 本轮的钉子**被读、但未被计价**,它在 shipping 当天就属于 **GH #839** 名下。
   ⛔ **本工具绿 ≠ manifest 新鲜**,这一行是那句话的证据。

### ⚠️ 交棒 ⑭(manifest 陈旧)本轮再次实测,数字与上一轮同量级

```
89 new test(s) not in the manifest were run anyway, costing 365.60s
=> the hook's REAL cost this run is 657.03s against a 530.0s budget.
9 new test(s) exceeded the hook budget and were EXCLUDED: …
budget vs wall: the manifest prices those 342 at 264.7s; THIS container ran them in 291.4s (1.10x).
```

⇒ **89 个新测试不在册、白跑 365.60s;真实开销 657.03s vs 预算 530.0s(超 24%);9 个被 EXCLUDED。**
⚠️ 另有 **`9 unanswered`** —— 即那 9 个**哪里都不计**的测试(GH #810 第一号展品修的正是报数那半)。
⛔ **不在本轮重测**(GH #810 逐字禁止「随便找台容器跑一遍」);
📌 顺带给出本容器的**标定读数 `1.10x`**,那正是 #810 (甲) 需要的那种因子 —— 留给取 ⑭ 的那一轮。

### 发表纪律(GH #290)

`claim_precheck.sh` 对 GH #843 评论草稿:**第一跑 `RC_EXIT=3`**,逐字
`MISSING path tests/mock/test_foo.py` ⇒ **DO NOT PUBLISH YET**。
⭐ **那是我 §2.5 (A) 里举例用的假路径,而工具分不出举例和引用 —— 它是对的,读者同样分不出。**
已把举例改成不带具体文件名的写法,**第二跑 `RC_EXIT=0` / `OK to publish`**
(`local commits not on origin/main: 0`,`resolved on trunk 8  refused 0`),然后才发表:
**GH #843 评论 `issuecomment-5717997907`**(带 Claude Code 署名脚注)。
⛔ **#843 不关闭**,理由见评论 §六。

## 十一、token 用量(收尾复测)

见 §九。⚠️ §九 那个数字取在 push 之前,**本节这几笔与两次 push 不计入**(铁律 8 的口径)。
