# 总监报告 2026-08-21T09:00Z(第二十九次触发)

**一句话**:`odaoe` **批准入集**(eligible 19 → **20**,§AA.2 的暂缓条件被逐字满足);
`[harness] #82` 跨组裁定 —— **被我立为唯一合法存活判据的 `is_dead()` 曾对一整类英雄恒答「活着」,
而受影响的不止报告里那两个**(`antimage` 是结构上的第三个,只是不在那份语料里);
给这条**一行字符串操作、却承载全目录每一条存活判读**的修复补了护栏测试 + 两次变异;
`[harness] #81` 裁定;成本口径更正(**MTD 实为 $18.28,此前六轮的「逐位一致」是计费滞后不是节流**);
`DECISIONS_NEEDED.md` 新增两条,并**更正一条我自己连续七轮抄错的状态行**(它早就创建了,
我差点覆盖掉 owner 未答的条目)。

**未花 AWS 钱、未做任何 AWS 调用、零 Lua 改动。** 裁定全文 `iterations/streams/test_set.md` §AB。

---

## 0. 认领依据(优先级 2(a) 判读)

open issue **58 条**。归总监的新条目两条,**都在本轮处理**:`[harness] #82`(英雄组 07:49Z)、
`[harness] #81`(协同组 07:44Z)。`[bug] #78` 已在 03:1xZ / 05:00Z 两轮消化完,`[harness] #80` 是我自己
07:00Z 开的、本轮只更新优先级。四组报告全在最近 ~2.3h 内(06:46 录像 / 07:40 协同 / 08:06 批测 /
08:15 英雄),**无空转**。

上一轮定的「下次触发 ①」是**英雄组是否补上 §AA.2 两条零支出列 ⇒ 可当场批准 `odaoe` 入集** ——
他们 08:15Z 交了,所以 ① 是本轮第一件事。

## 1. `odaoe` 批准入集(GH #54,eligible 19 → 20)

§AA.2 我给的解锁条件原话:「若 10 个 episode 里长度 >= 2 的仍占多数,域可达的结论就地成立,
**我立即批准**」。两列都交、两列都过:

| 列 | 交付 | 复核 |
|---|---|---|
| episode 长度直方图 | `{1:2, 2:3, 3:2, 4:2, 8:1}` ⇒ **8/10 >= 2** | 承重论证是我给的形状(实测滞后 < 一个 0.5s 采样 ⇒ 幽灵最多活一帧 ⇒ 只能伪造长度 1)⇒ **污染上界 = 2 个 episode,是上界不是估计** |
| 换 `is_dead()` 重跑漏斗 | 4096→49→42→41→31 变成 4092→47→41→40→**30**,episode **10 不变** | 代理总共只买到 **1 帧 / 0 个 episode**,差分是精确单元素集(`20260819_223055 t=650.5`,OD 自己的快照在他的 DEATH tick 上仍读 29 血) |

头条 **30 帧 / 10 episode / 9 局里的 7 局**。承重子句(`od_eclipse_aoe_domain.py:257`)已接
`is_dead()`,§AA.3 那张表划掉一格。

**四条入集约束**(§AB.1):必须挑抽到 OD 的种子(#46);加法型门可单独占一条臂;
**条件 (b) 用行为检测器判、不许用 gpm/xpm**(1.1 episode/局,正在 §A0 通例的分母下);
**(a) 的预注册域 = 这 30 帧 / 10 episode**,且「低血顺手放」(7 帧 / 4 episode)与「健康主动开」
**分开数**。**排期零影响** —— 不在 §U.0 的 18 id 串里。

## 2. `[harness] #82` 跨组裁定:**受影响的是一个类,不是两个个体**

英雄组的成因诊断全部采信,我读了 diff 与 dumper 源码(不是转述):`snakeFromClass`
(`dumper/main.go:190`)先删类名后缀里所有下划线、再在每个 camelCase 边界插一个;DEATH 事件走
战斗日志名 ⇒ 拼法对不上 ⇒ `death_spans()` **一个 span 都产不出** ⇒ `is_dead()` 整局恒答「活着」。
**这是 #78 把上界拿掉的版本**(不是每次死亡漏 1 帧,是漏整段)。

**三件报告里没有的:**

**① `antimage` 是结构上的第三个。** 把 `bots/` 里出现的 **133 个 `npc_dota_hero_*`** 逐个对
`snakeFromClass` 的规则过一遍:`CDOTA_Unit_Hero_AntiMage` 派生 `anti_mage`,战斗日志写 `antimage`。
**它不在那 19 局语料里** ⇒ 「两个英雄」是**语料事实,不是缺陷边界**。
⇒ 凡复核历史读数,存在性按 **{queenofpain, vengefulspirit, antimage}** 查,并写「**至少这三个**」。
**这条也正是「修法必须是规则、不能是别名表」的硬理由** —— 一张覆盖那两个观察到的英雄的别名表
会看起来完整,并且对 Anti-Mage 保持全盲。

**② 合并风险已量 = 0。** join 是「删掉全部下划线」,**只在没有两个英雄坍缩到同一个 key 时才安全**;
真撞上了,#82 的修复会把两个人的死亡区间**合并** —— 比它修的缺陷更糟。实测 **133 名字 / 0 碰撞**。
**这条不是评论,是测试**(下节),新英雄进 roster 会在那里翻红,而不是在某个组的读数里。

**③ 不做 dumper 侧规范化**(采纳英雄组的克制):改 dumper 会让 S3 里所有已缓存 timeline 与二进制
失配、必须重 dump,而 `canon_hero()` 无论如何得留着(旧 timeline 还在)⇒ 那笔钱买不到任何
今天没有的东西。登记为 dumper 侧待办,**与 `[harness] #27`(`vis`)并成同一次重建**。

**复核范围与优先级**:`capmono_refusal.py:251,284` 与 `lanekill_commit.py:160` 自动受益,
**但它们此前的历史读数是在盲区里跑的** ⇒ **#82 的复核插到 `[harness] #80` 队列最前面**
(#80 那张表的前提正是「已迁移的那两个干净」,而恰恰是那两个在给裁定供数)。
复核必须报:三个名字各出现几局 + **按臂分开的被剔除 episode 数**(差为 0 也要报)。

**一条缓解,只是假说**:镜像 draft ⇒ 受影响英雄同局两臂都在场 ⇒ 一阶共模、DiD 抵消一部分;
**但行为者/受害者角色不对称**(尸体当活人单向抬高 headcount 子句,而两臂**谁在死**不同)
⇒ **抵消多少要量,不许假定**。

**§0b 第二十六例(新亚型:修复成了唯一判据之后,它自己的盲区没人再查)**。
**采纳英雄组 §8 的先验为全队口径:换判据时必须双向数分歧帧、按方向分开报。**
真正抓到这个洞的不是预期方向的那 1 帧,是**反方向的 196 帧**;只报一个「分歧 N 帧」的总数,
新判据自己的洞会原样活下来 —— 而且**顶着「已按规矩换成唯一合法判据」的标签**活下来。

## 3. 交付:`tests/test_canon_hero_join.py` + `tests/run_py_tests.sh`

**为什么值得多花这一步**:#82 的修复是**一行字符串操作**,而它现在承载本目录**每一条**存活判读。
一行代码 + 零测试 + 全组依赖 = 下一次静默失效的标准配方。

钉三件:(i) join 是**规则不是别名表**(`anti_mage` ↔ `antimage` 通,**未列名的名字也必须通**);
(ii) **133 个 roster 名字零碰撞**;(iii) **端到端** —— 合成 timeline(snapshot 一种拼法、DEATH 事件
另一种)驱动**真的** `death_spans()`/`is_dead()`,断言死前 / DEATH tick / 区间内 / 复活后四个时刻,
**两种拼法各问一遍**(检测器手里是 snapshot 名,事件侧是 event 名)。

**两次变异都按预期红**:

| 变异 | 结果 |
|---|---|
| M1 退回 #82 之前的三处 join | **挂 9 条**,报错直指「span 一个都没有」 |
| M2 改成只覆盖那两个观察到的英雄的别名表 | **挂 5 条**,且点名 `anti_mage joins antimage`(预测存在、不在 #82 语料里) |

顺带关掉一个自己的漏洞:仓库有 **4 个 `tests/*.py`,而 `run_tests.lua` 只加载 `.lua`**
⇒ **从来没有任何入口跑过它们**。按我自己在 §Z.4 立的规矩(**空转的规则是最糟的一种状态**),
补 `tests/run_py_tests.sh`:**4/4 通过**,并塞一个必挂的假测试实测 **exit 1**(两个方向都验)。
**铁律 6 的两条命令不变,python 那条是补充不是替代。**

## 4. `[harness] #81` 裁定:不改 loader,钉成 characterisation

自己读了 `bots/FunLib/aba_role.lua:392-398`:`bot.assignedRole = role` 之后敌方分支**立刻用
`GetEnemyPosition()` 覆盖**、拿不到就 `return 3`,而 loader 从不调 `UpdateEnemyHeroPositions()`
⇒ 与协同组实测(93 fixture / 465 敌方英雄全读 pos 3)一致。

裁定三条:**不让 loader 调估计器**(会一次性移动 93 个 fixture 的敌方位置,而
`test_fixture_roles.lua` 存在的全部意义就是不许发生这种事);**敌方分支保持 `return 3`,不改拒答**
(扩散地板是未测的行为改动);**立刻生效的引用禁令 —— fixture 上 fork 在敌人位置 /
`J.IsCore(敌人)` 上的断言读的是常数,不得支持任何 (a) 结论**。
同 #36 / #27 / §F 一族(**fixture 世界比真实世界退化**),**第一次有人去敌方那一侧看**。

## 5. 成本:口径更正 + 两条采信

| 项 | 值 |
|---|---|
| MTD | **$18.2767**(批测台 08:06Z 拆账;+$3.28 全是 08-19/08-20 补账,**不是新支出**) |
| 月末预测 | $21.50 |
| 在跑实例 | **0 台,无泄漏** |
| 围栏/刹车 | $45 围栏余 **$26.72**;$90 刹车线余 $71.7 |
| 总监本轮 | **零 AWS 调用、零计费资源** |

**更正记在我头上**:我连续六轮把「MTD 逐位一致」当成节流信号引用,它其实是**还没入账**。
**采信两条新事实**:(i) 我们查花费的 CE 调用 **$0.01/次、$0.24/天 ≈ $7.3/月**,零批测的日子里
比 AMI 快照常驻成本还高 1.8 倍 ⇒ 三轮「零支出」字面上不成立;批测台把默认读数改成**免费的
Budgets**、近刹车线($35)自动升级付费复核、四条路径全实跑 —— **这个修法我批准**;
(ii) 经验单价 **$0.35/台**(账单反推),此前章程「4 台 $0.6-0.8」偏低约 2 倍,今后一律 $0.35/台
(8 台两臂 ≈ $2.8)。

**`DECISIONS_NEEDED.md` 新增两条**(都归成本类、**都不阻塞**):
① Budget 的 freeze action 无权限查证(要不要补 `budgets:Describe*` 只读权限);
② MTD 过 $50 时 AWS 会**先于我们的报告**给 owner 发告警邮件。**本周 owner 邮件仍为零**,攒到周日。

**并更正一条我自己反复写错的记录**:该文件**不是本轮首次创建** —— 08-20T14:50Z 那轮就建了
(第 1 条:$45 围栏 8-25 触顶 ETA,owner 至今未回),而此后**每一轮**总监提醒都写着
「`DECISIONS_NEEDED.md` 仍未创建」,**连续七轮逐字抄了一句假话**;我这轮据此直接 `Write`,
**差点把那条 owner 未答的条目整个覆盖掉**(写文件前没读它),已从 HEAD 还原并按新数据加注
(围栏 ETA 因「无目的不启动」已不成立,**建议不变**)。**与 §AA.2「贴着 issue 编号的假修复」同形:
状态行一旦被抄进模板,就再没人去核它。** 今后章程状态行里凡带「仍未 / 连续 N 轮」的,
**引用前先看一眼那个对象本身**。

## 6. 其他章程项

**测试集**:`odaoe` 入集(19 → **20 eligible**),**§U.0 的 18 id 下一波串逐字不变**;
**下一波仍不启动**(§W.5 三条解除条件全未满足,批测台 08:06Z 判断正确);
四组无空转;今日周五,**效率台账(仅周日)跳过,08-23 那次要做**;patch 11:10Z 已分片,本轮不重复;
`wandlimbo` 机会普查连续第**十六**轮无人做;待复核队列
`#44`/`#45`/`#55`/`#58`/`#62`/`#63`/`#65`/`#66`/`#67`,**`#45` 第十六次顺延**。

## 7. 验证

**本轮零 Lua 改动**(`git diff --name-only` 里 `.lua` 计数 = 0)⇒ 铁律 6 两条命令按立法目的
**无适用对象**;本容器未装 Lua 工具链,**如实记录:没有跑**(03:00Z 上一个容器复核过 886/886 +
luacheck 0;此后英雄组自报 902/902、协同组自报 908/908)。
改动面的验证:`tests/run_py_tests.sh` **4/4 通过**(含新测试)+ **两次变异按预期红** +
运行器本身的失败路径实测 **exit 1**;`bash -n` 过;`state.json` JSON 解析通过。

## 8. 下次触发

1. **`[harness] #82` 的复核是否落地**(录像组;`capmono_refusal` / `lanekill_commit` 两个都要,
   报三个名字的出现局数 + 按臂分开的被剔除 episode 数)—— 它是 `capmono` 与 `l1trade`
   **一切现存读数**的新前置,排在 #80 其余条目之前;
2. `odaoe` 上机排期:等 §W.5 出现申报目的时,按 §AB.1 四条约束并进(**不推迟现有波次**);
3. 下一波申报目的仍按 §W.5 判,有目的即挂 16 槽(§X.1(甲) 选种子,§X.2 先 #78 后 16×);
4. 清待复核队列(`#45` 最老,已第十六次顺延);
5. 都在等就做 **backlog #1 帧语料检索工具**(owner 点名;§Z.2 的 episode 口径 + §AA/§AB 的
   「按需修」都指向同一个基建:**跨局按谓词批量取帧**)/ patch 分片 P3 / `[harness] #27`
   (**现在它多了一个搭车项**:#82 的 dumper 侧规范化)。

## 9. Token 用量

```
TOKENS total_in=9,089,374 out=81,270 turns=73
```

(铁律 8:这是到统计时刻为止的数字,统计后的收尾回合不计入。)
