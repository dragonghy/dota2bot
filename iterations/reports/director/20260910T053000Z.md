# 总监 2026-09-10T05:30Z —— 裁 W62 的 promote/reject(§GJ.6 第 3 条,本轮结清)

**裁定是 HOLD。而本轮最该被读的不是那个字 —— 是「决定性通道这一波在结构上没有读数,
而那个『没有』长得和『测过了,是负的』一模一样」;以及「GH #352 自己的立案句点名了
两个被误读的数,它修了第一个」。**

零 AWS 调用、零波次、**`bots/`+`game/` 零 diff**、**不发 owner 邮件**、
armed 串 **37 不动**、**无 promote / 无退集**。

取活依据:上一轮(01:xxZ)「下次触发」清单第 ⑧ 条,**唯一带 ⛔ 的一条**
(`test_set.md §GJ.6` 第 3 条逐字:「没有裁 W62 的 promote/reject(4/4 粒、203 局的读数
在批测台报告里,本轮未读)」)+ **章程 2b**(promote/reject 判定)+ **章程 2a**([bug] 直接修)。

全文档案 `iterations/streams/test_set.md §GK`(§GK.0–§GK.5)。

---

## 一、裁定:**W62 = HOLD**(§GK.0)

**零 id promote,零 id 退集,armed 串 37 不动。**

⛔ **HOLD 不是 reject 的委婉说法。** reject 的判据是「明显有害」,那是一个**读数**;
而本波在鉴别 promote/reject 的那个通道上**读数不存在**,不是不好看。

铁律 2 的三条件里,**(b)「批测显示对胜负没有明显负面影响」在本波既无法被满足,也无法被否证**:

- `winrate_channel: **DEGENERATE**`(`winrate_minority_side_share` **0.0529 < 0.20**;
  侧别普查 radiant **12** / dire **215**);
- `recover_verdict.py` 自己的 stderr 逐字:
  **`MUST NOT be cited as rule 2(b) support until the channel recovers`**;
- 工具的 `suggested` 字段是 **`hold_or_reject`** —— **两个字都在里面**,本裁定取 `hold` 那一半。

### 为什么是 HOLD 不是 REJECT(§GK.2)

`winrate = (r_ab + (1 − r_ba)) / 2`,`r_x` 是**同一个物理侧**在 x 波的胜率。
一侧横扫 ⇒ `r_ab = r_ba` ⇒ `winrate ≡ 0.500`,**与臂做了什么无关**。

| 粒 | ab/ba 局 | `winrate` | `winrate_headroom` | 能不能投票 |
|---|---|---|---|---|
| 10601 | 42/16 | **0.500** | **0.0** | ⛔ 不能(恒等式) |
| 10607 | 31/14 | 0.516 | 0.0357 | ✅ 能 |
| 10803 | 34/24 | **0.500** | **0.0** | ⛔ 不能(恒等式) |
| 10813 | 30/12 | 0.608 | 0.4167 | ✅ 能 |

⇒ `mean.winrate 0.531 = (0.500 + 0.516 + 0.500 + 0.608)/4`,**四项里两项是常数**。
把 0.531 读成「胜率略正」是错的;把它读成「胜率没明显负面 ⇒ (b) 满足」**同样是错的** ——
方向相反,**错法相同**:两次都是拿恒等式当测量。

⛔ **经济那一栏不能替 (b) 作证,而理由不是 gpm 是负的**:
`gpm −7.20`(2/4)/ `xpm +10.17`(4/4)/ `deaths −0.14`(3/4)/ `last_hits +0.60`(2/4);
`strata` 四量**全部** `sign_flip: true` 且 `side_gt_arm: 4/4`。
按 `§CL (i-c)` **反号不是否决理由**(那是恒等式 |side| > |arm|,对 arm 测得多准零信息);
且铁律 2(b) 的主语本来就是**胜负**不是 gpm ⇒ 拿一个侧偏未消的经济量去补一个读不到的
胜负量,是把两个 §CL 都管不住的缺陷叠在一起。

⛔ **本裁定没有声称这 37 个 id 里任何一个是好的或坏的。**
声称的只有一句:**W62 这份语料无权说它们好,也无权说它们坏。**

---

## 二、⭐⭐ 顺带量到的:GH #352 的立案句点名了两个数,它修了一个(GH **#696**,本轮落地)

`tests/test_verdict_winrate_channel.py` 头注释逐字:

> the tool printed `winrate 0.500` and **`comps_better winrate 0/4`**, six waves running,
> and **BOTH** were read as measurements

#352 落地的是 `winrate_headroom` —— 它让被冻住的那个 0.500 在同一行里读得出来。
**而 `comps_better.winrate` 照旧把生出那个 0.500 的粒计在自己的分母里。**

后果不是「数字略偏」,是**分母里坐着常数**:本波 `comps_better.winrate` **2/4**,
两张反对票 **10601 与 10803 的 `x > 0.5` 恒为假** ⇒ 形如「`comps_better winrate ≥ 3/4`」
的 promote 门在这份语料上**不是没达到,是算术上够不着**;
⭐ **而报出这件事的那个分数,和「臂输了两粒」长得一模一样。**
在**能说话的粒**里,本波战绩是 **2/2**,而 verdict 里**没有任何字段这么说**。

**修法**(`tools/batch_test/soak/recover_verdict.py`):在池化分数**旁边**加打三样 ——
`winrate_forced_seeds`(**点名,不只计数**)、`comps_better.winrate_measurable`、
以及一行 stderr(**两个分数都带**,给从不打开 JSON 的读者 —— #352 的六轮就是这么被读过去的)。

三条自缚写进代码注释:
⛔ **旁边不是替换**(与 headroom 二十行之上同规矩:悄悄丢掉被冻住的粒会**把横扫本身藏起来**,
而横扫才是发现;两个分母之间的差读出来的是**语料**,不只是臂);
⛔ **`0/0` 照打不省略**(缺键读作「没什么可报的」,而那**恰好**是一波没有任何可测粒时唯一不表示的意思);
⛔ **它是披露不是新门**(谁也没被授权拿 `winrate_measurable` 去过一个池化分数过不了的门)。

W62 的新字段读数(**离线由 verdict 已有的 per-seed headroom 导出,非重跑**):
`winrate_forced_seeds = [10601, 10803]` / `comps_better.winrate_measurable = **2/2**` / 池化仍 `2/4`。

### 证据

- `tests/test_verdict_winrate_comps.py`(新):**25 checks / 0 failed**,驱动**真脚本**读**真 stdout/stderr**,
  不 import 任何重实现(GH #67),每个 case 前跑 `check_parsed()` 反空匹配守卫。
  ⭐ 承重的是 **case 1(一个臂两个分数)** 与 **case 2(什么都没冻住时两个分数必须一致)**;
  case 2 里那粒 **007 读 0.500 而 headroom 0.5** —— 它是分「按 headroom 锚」与「按 0.500 这个值锚」的刀口。
- `tools/agent/mutstand_verdict_winrate_comps.sh`(新):**6 CAUGHT / 0 SURVIVED**,CONTROL GREEN,
  还原 `sha256sum -c` 逐字节相同;每个 anchor 先断言**唯一**(照 §GJ 记下的 M1 漂移教训)。
- 未连带红:`test_verdict_winrate` / `_channel` / `_arm_depth` / `_strata` / `_pool_lossless` /
  `test_queue_reading_census` 六个 **EXIT=0**。

### ⚠️ 变异台自己中一发,留痕(§GK.4)

**M4 第一版是「对的结论、错的理由」。** 它只给赋值加了 `if meas:`,于是 mutant 在**下面那行 stderr**
上 `KeyError` 当场崩 —— 测试确实红了,**但 case 3 的那条断言一次都没执行**。
台子照旧印 `CAUGHT`,**露馅的只有 FAIL 摘要那一栏是空的**。
⇒ **够不到断言的 mutant,测的是解释器不是测试。** 已让 mutant 自洽(stderr 改 `.get`),
重跑后 M4 的 FAIL 行逐字是 `an all-forced wave prints 0/0 rather than omitting the key, got None`。

---

## 三、⭐ 顺手修掉 trunk red(GH **#694**,章程 2a)—— 一条注释被改好,于是测试变红

开工自检的 python 腿读作 `117 passed, **1 failed**, 2 uncertifiable`,
failed 是 `tests/test_detector_source_constants.py`,**正是批测台 03:25Z 立的 GH #694**。

现场:`51e99b35` 只是**扩写**了 `jmz_func.lua:9814` 那句注释
(`(>800 ancient-distance depth)` → `(>800 ancient-distance depth, i.e. ~400u past the midline -- GH #687)`),
而测试把**带右括号的整串**逐字钉死。⇒ **改好一句注释和删掉那个诱饵,对测试逐字节相同。**

修法一行:锚点**少一个字符**,改成 `>800 ancient-distance depth`。
理由写进注释:这条断言需要为真的是「一个带 800 的诱饵在函数体里**够得着**」,
**"depth" 后面碰巧跟什么标点从来不是这个主张的一部分**。

⭐ **没有削弱这一节的承重主张,而且是量出来的不是断言的**:把 `source_constants.py` 的
`_strip_comments` 换成 `return body` ⇒ 测试仍 **exit 1**(注释剥离的变异照旧落地),
`git diff` 还原后为空。

⛔ **#694 的系统性那一半没做,不假装做了**:它的立案句是
「**pre-push 的 manifest 里没有它**」—— 把 python 测试接进推送门是动态那半(~100min,GH #124)的政策改动,
**本轮不碰**。**#694 不关**,留给那一半。

---

## 四、投递(章程 2.5)与顺序(GH #290)

| 落点 | 内容 |
|---|---|
| `iterations/reports/batch-desk/waves/W62_wave.json:director[2]` | `HOLD_NO_PROMOTE_NO_REJECT` —— **批测台下一轮真会读的那张表** |
| `iterations/state.json:W62_PROMOTE_REJECT_RULING_20260910T05xxZ` | 裁定档案(含 `condition_b: UNCERTIFIABLE,不是 FAILED`) |
| `iterations/streams/test_set.md §GK` | 全文档案 |
| `tools/batch_test/soak/recover_verdict.py` | GH #696 正文 + 运行时 stderr —— **投递终点是批测台真会跑的那个工具** |
| `iterations/owed_executions.json:gh696_winrate_measurable_first_live_read` | 常驻义务(§DR) |
| GH **#696**(新开,`[harness]`) | 立案 + 验收 + 结清判据 |

⭐ **常驻义务的结清判据钉在逐字串 `GH #696 winrate comps_better:`**(stderr 新前缀),
理由是本次改动是**「旁边」不是「替换」** ⇒ `mean.winrate` 与 `comps_better.winrate`
**逐字未变** ⇒ **判据不能钉在任何旧字段上**,钉了就会被「跑了、也贴了、贴的是改版前后
长得一样的那几行」满足。立行当日在 `test_set.md` 里出现 **0 次(已量)**。
⚠️ 执行者是批测台下一次收割那轮,而**九月在 $80 围栏下已无下一波(§GJ.4)**
⇒ 这根棒**大概率要等到 10-01 之后**;**那不是掉棒,是登记在案的等待**,已写进行里。

⚠️ **发 issue 时撞号并已改正,留痕**:草稿全程写的是 `GH #695`,而 **#695 在 03:54Z 已被
strategy 组占用**(本轮 `list_issues` 现读)。开 issue 拿到真号 **#696** 后,
在 push **之前**把七个文件里的 `GH #695`/`gh695` 全部改掉并重跑变异台(仍 6 CAUGHT / 0 SURVIVED)。
⇒ **没有一个错号落到 main 上。** 立案 issue 的正文只引用了 **origin 上已有**的路径
(`recover_verdict.py` / `W62_verdict.json`),新文件的引用留到 push 之后的追评 —— **GH #290 的顺序**。

### 本轮发表(push 之后,GH #290 的顺序)

| 落点 | 编号 |
|---|---|
| 新开 issue(`[harness]`) | **GH #696** |
| #696 落地追评 | `#issuecomment-5613009843` |
| #694 追评(**修了红,不关 issue**) | `#issuecomment-5613016276` |

- `git push origin HEAD:main` **`81fa8b24..b67ccbb8`**(先被拒一次 `fetch first`,
  `git pull --rebase origin main` 后成功);两次 push 的钩子门均
  `GATE_EXIT=0 CLEAN` + `py gate: 91 ran, 0 findings, 0 uncertifiable`,**无 `RULE6_BYPASS`**。
- 发表前 `claim_precheck.sh` 打 **`clean` / `OK to publish` / `PRECHECK_EXIT=0`**,
  同一次输出带 **`local commits not on origin/main: 0`**。
- **铁律 11 未触发,MCP 全程可用。**

---

## 五、健康巡检

🩺 **五组全部有产出,无掉队组**:batch-desk 03:11Z / strategy 02:31Z / hero 01:48Z /
replay-check(有目录产出)/ director(本轮)。owner 优先项无 12 轮零推进的升级项。

💰 **零 AWS 调用,不作 MTD 新声称**(转载批测台 20:23:37Z 快照 `$74.308`,外来 `$37.435` = 50.4%)。
三条线(围栏 $80 / 刹车 $90 / 批准线 $100)**未改**,**未预支任何跨线许可**。
**本轮不发 owner 邮件**;`DECISIONS_NEEDED` 第 15 条随周日 W37 那封走,**+0**。

---

## 六、开工自检(铁律 10)与铁律 6

⚠️ **纪律 3 第三十六发,守卫连续第十轮自拒**:本轮第一条命令又是
`routine_selfcheck.sh 2>&1 | tail -60` —— **章程第 0 步写的就是 `rc.sh`,我又没照做**。
守卫当场拒(`REFUSED: stdout is a PIPE`,并自报「**这是第 5 次,每次都是当轮第一条命令**」)。
⇒ **第五次记同一句:它是习惯不是门。**

改重定向重跑(`> /tmp/sc.log 2>&1`),**跑完了,真码 `EXIT=3`**:

```
legs run              : 11
FINDINGS (exit 3)     : cadence queue-rulings owed-executions a-evidence-owed trunk-red(python)
UNCERTIFIABLE (exit 2): none
NOT RUN (inside a leg): tests/test_luacheck_gate_soakswitch.py tests/test_selfcheck_lua_leg.py
selfcheck worst exit: 3
```

- **python 腿**:`117 passed, **1 failed**, 2 uncertifiable`;
  `failed: tests/test_detector_source_constants.py` ⇒ **就是本轮 §三 修掉的那条**
  (那也是 `FINDINGS` 里 `trunk-red(python)` 的全部来路 —— ⚠️ 但按 GH #267,
  **只登记来源不做归因**,其余四项本轮登记不处理)。
- **Lua 检测器腿**:`87 tagged detector file(s), **0 failures**` —— ⛔ **FAST SUBSET,不是全量**。
- `NOT RUN`:`test_luacheck_gate_soakswitch.py` 与 `test_selfcheck_lua_leg.py`
  —— **GH #358 的 120s 第六次吃腿**;⚠️ 本轮我没改那个 wrapper,**那条腿这轮没人看过**。
- ⚠️ 它**开工时起跑,早于本轮全部编辑落盘**,**不覆盖我改的八个文件,不拿它冒充**;
  §二/§三 的读数全部来自改后单独重跑。
- ⛔ **「后台包装吞真码」第十六次**:harness 报 `[exited with code 0]`,
  而输出文件第一行是 **`EXIT=3`**,工具末行也是 **`3`**。
  ⭐ **这一次是 `rc.sh` 式的重定向把真码送了出来** —— 守卫仍未立,顺延。
  ⚠️ 中途我曾据「日志停在 `=== trunk health ===`」在报告里写下「自检本轮未跑完」,
  **那是错的:它只是慢(>30 分钟),后来跑完了**,收尾前按真码改正。
  **教训与本轮主题同族:把「我还没读到」写成「它没发生」。**
- **铁律 6 静态半**:`bash tools/agent/luacheck_gate.sh` ⇒ `luacheck bots game: **0 warnings**`,
  `GATE_EXIT=0 CLEAN`,**无 `RULE6_BYPASS`**。
- ⛔ **Lua 全量未跑不声称**(`bots/`+`game/` **一行未改**)。

---

## 七、下次触发

1. ⭐⭐⭐ 退休 `a_evidence_pulldrag`/`a_evidence_tpgap` 并裁那两个 id(**第三轮顺延**)
2. ⭐⭐ `wave_reachable_delta.py`(闸 (ii) 可执行体,**第五轮顺延**)
3. ⭐⭐ **GH #692**(批测台 03:24Z:RULING 6 的快照时钟在本账户上 **100% 降级到 `now`**,
   float epoch 解析漏了一支,**失效方向是许可侧**,且足以把闸从 exit 3 翻成 exit 0)
   —— ⚠️ **这是我上一轮刚落地的修法自己带的洞,优先级按此**
4. ⭐⭐ **GH #693**(批测台请裁:时钟降级应否 ⇒ exit 2)
5. ⭐ **GH #694 的系统性那一半**(pre-push manifest;本轮只修了红,**issue 未关**)
6. ⭐ 核 GH #696 的首次活读数(`GH #696 winrate comps_better:`)—— ⚠️ 预期落在 10-01 之后
7. ⭐ **镜像分侧为什么产不出竞争性语料** —— ⚠️ 带 `winrate_channel` 字段的三份 verdict
   **全部 DEGENERATE**(W55 `0.0645` / W58 `0.1552` / W62 `0.0529`),而 `AGENTS.md`
   写的是 **radiant 侧偏 +1.5k gold**,读数却是 **dire 赢 215/227**。**方向相反**。
   ⛔ **本轮不立案,因为我没查过 `winner` 字段的来路**;下一轮先读 `analyze_log.py` 再决定
   是开 [bug] 还是记成 Turbo 的真实形状
8. ⭐ 裁 `PROMOTE_BAR_PAIRED_SEEDS =`(owed 腿读作 DONE 并点名该退休)
9. ⭐ 裁自检 `queue-rulings` 点名的 hero-51/52/53/54 + hero-55
10. 核 UNOWED 12 是否在降 / `gh454_cost_constants_rerule` / GH #672 / #664 /
    backlog 101/102/103 /「后台包装吞真码」守卫 / GH #358 **第六次**吃腿要人裁
11. 存量:账户级预算等 owner(W37 周日)/ GH #523 / patch 缺口 P3 / `hero_domain_scan` 九份 /
    「退集·promote 五处同步」

---

## 八、token 用量(铁律 8)

```
TOKENS total_in=10,136,201 out=50,852 turns=76
```

⚠️ 这是**到统计时刻为止**的数字,统计后的收尾回合(commit / push / 两条 GH 追评)不计入。
