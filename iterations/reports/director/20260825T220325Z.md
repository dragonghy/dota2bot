# 总监 2026-08-25T22:03Z —— `cmboots` 结案:**前两条件都过了,第 (c) 条的检索判据对这个英雄恰好反向**

**`bots/`/`game/` 零改动、零 AWS 调用、零支出、未批新支出。**
全文档案 `iterations/streams/test_set.md` **§BI**;机器记录 `iterations/state.json:cmboots_RESOLVE_20260825T2130Z`;
机器投递 `queue.json` → `hero-8.director_resolve`(新字段)。本轮开 **GH #190**,追评 **GH #144**。

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh`,worst exit 3:

* **UNLANDED**:`no unlanded work in the certifiable window`(**0 条**)。
  ⚠️ 上上轮判过的两条永久假阳本轮**没有再出现** —— 352/387 refs 因浅克隆无 merge-base 被 REFUSED,属工具已声明的 LIMITS。
* **CADENCE 9 洞**(3.5–8.9h),与前两轮**同型**,不升级。仍点名 **batch-desk 8.9h**(06:17Z→15:11Z)——
  唯一花钱的组,停摆即取证吞吐损失。18:5xZ 抬围栏($45→$60)正是消除其一个诱因,**本轮已见效**:
  批测台 21:07Z 发 W11,围栏算术 $43.75 ≤ $60,**若还是 $45 这一波发不出去**。
* **`pending_rulings.py` 0 条,exit 0**(RIDESHARE / OTHER 均 none);open 请求 32 条。
* trunk python **28 passed / 0 failed**;Lua 腿 **SKIP**(本容器无 `lua5.1`)。
  ⚠️ 批测台 21:07Z 报告说这条 `SKIP` 是「一条没人付的 `apt-get`」,数秒可装(GH #171)。
  **本轮 `bots/`/`game/` 零改动 ⇒ 铁律 6 的 Lua 门不适用,故未付这笔**;如实登记,不算数。

**选题**:自检**没有红**(`pending_rulings` 0),`OWNER_PRIORITIES` 的 P1/P2 球都在协同组
⇒ 按上一轮(§BH ⑥)自己立的规矩,**本轮必须做 `cmboots`** —— 它已顺延两轮,
§BH 明写「若下一轮再顺延,那就是 §BF.0 记的那个形状(知道、写下来、然后不做),
届时必须写进 `DECISIONS_NEEDED` 说明原因」。**本轮做掉了,那份文件不用动。**

---

## 1. 裁定:`cmboots` = **NOT PROMOTED**,判 `RETURN_ON_C`

| 条件 | 判定 | 谁给的 |
|---|---|---|
| (a) 真的执行且行为正确 | **WORKING —— 不推翻** | 录像组 `20260824T130015Z.md` |
| (b) 胜负无明显负面 | **PASS —— 不推翻** | 批测台 `20260824T121706Z.md`(W5,297 局,wr 0.497) |
| (c) 逻辑/理论依据 | ❌ **as filed 不成立** | 本轮 |

**结果**:gate 保留、**不是 reject**、不进稳定版、**不打 stable-vN tag**。
它本就不在 36-id 成员串里(走的是 §AU.2 独占首波 W5,发完即离)⇒ **本轮无出集动作**。

### 1.1 先说清楚我没有推翻什么

(a) 和 (b) **一个字都没推翻**。机制真的跑起来了(106/106 armed 腿 CM 持 arcane、
0/106 持 tranquil、0/106 持 bearing,两层同号;承重帧 `1d9ae5/20260824_031904_slot7` t=428.5),
读数是**被供电的**读数(五行两层同号),批测也确实没读出明显负面。
**卡住的是 (c),而 (c) 恰好是三条里唯一带判断的那条。**

### 1.2 (c) 不成立的四条腿

**(1) 铁律 2(c) 自己指定的取证方法,对这个英雄返回了反向结果。**
规则原文:「逻辑/理论依据(**标准策略可上网检索佐证**)」—— **检索是规则自带的门**。
检索结果:**CM 的标准鞋子是 Tranquil,而给出的理由正是 Arcane Aura(+ Tranquil 自带回血)**,
即「她自己解决蓝」。⇒ 申请书要立的那句(「pos_5 买表一件蓝装都没有 ⇒ 该买一件」)
**在标准打法里已经被显式考虑过,并且对这个英雄被否掉了**。
**这不是「查不到佐证」(那只是空白),是「查到了反向佐证」。**

**(2) ⭐ 最有力的那条反驳,总监替英雄组补全、查证,然后自己否掉。**
反驳应该是:**光环在计分窗口里长期只有 1 级,所以 (1) 的前提在我们这儿不成立**。查证:

| 出处 | 事实 |
|---|---|
| `tests/mock/special_value_shapes.lua:293` | `base_mana_regen 0.4/0.6/0.8/1`、`proximity_bonus_factor 3`、`proximity_bonus_radius 1200` ⇒ **1 级近身 1.2 mana/s,满级 3.0** |
| `bots/BotLib/hero_crystal_maiden.lua:19` | 队列 `{1,2,3,2,2,6,2,1,1,1,6,3,3,3,6}` ⇒ 光环第 1 点在英雄 3 级,**第 2 点押到 12 级** |
| 批测结算 | `economy_10min_cap`,W5 **297/297** 局 ⇒ **整段计分窗口跑在 40% 光环上** |

**事实成立 —— 但它救不了 (c)**:**人类标准 CM 同样是「一点光环吃到后期」,
而标准打法在这个前提下仍然选 Tranquil。** ⇒ 这条**不区分 bot 与人**,(1) 的建议里已经把它算进去了。
**一条不区分两种情形的事实,不能用来主张这两种情形该有不同结论。**

**(3) ⭐⭐ 唯一真正 bot-specific 的那条测量,英雄组/录像组测到了,但没把它当结论。**
录像组 §4:队友每局放 Replenish **2.8–4.4 次**,CM **收到 0.00–1.07 次**;
帧例 `1d9ae5/033503_slot9` 队友放 **11 次**、CM 收到 **0 次**。
录像组给的注解是「**她自己那双不是冗余的**」(⇒ 支持买鞋)。
**同一份数据可以反过来读,而反过来那一读更根本**:**队伍在放蓝,她不在那个圈里。**

> Replenish 半径 **1200** —— 与 (2) 里查到的 `proximity_bonus_radius` **是同一个数**。
> ⇒ **同一个站位病一次吃掉两样:队伍的 Replenish,和她自己光环的 3 倍近身加成**(0.4 → 1.2 mana/s)。

「CM 缺蓝装」是**症状**,「CM 站在队伍的蓝半径外」是**病因**;**病因零金币,症状 375g。**

**(4) 账本不是「中性」,是「中性且带两层同号的代价」。**

| | 读数 | 两层 |
|---|---|---|
| **买到** | 技能施放 **+2.04**/局、`mp<100` **−3.5pp**(地板读)、均值 `mp_pct` +5.9pp、自放 Replenish +2.355、收到 +1.500 | 五行同号 |
| **卖掉** | 鞋子完成中位 **+70~85s**(394–420s vs 313–344s) | 同号 |
| **卖掉** | `ancient_janggo` 完成率 **−21.9pp**(−17/−27) | 同号 |
| 不读 | 死亡数(−0.73/+0.08) | **反号**,铁律 4(i) |

⚠️ `ancient_janggo` 是 `sRoleItemsBuyList['pos_5']` 的**第 2 项 = 紧挨鞋子后面那一件**
⇒ **不是偶然掉了一件装备,是整条后续买表在往后滑**。
胜负侧 **+7.03 gpm 落在本波自带的结构性零腿 |−5.41| 之内**(种子 888,CM 发在 pos 4,两腿逐字节等价)
⇒ **(b) 买到的是「没有明显负面」,不是「它让局面更好」**。

### 1.3 ⭐ 判据(比这个 id 本身值钱)

> owner 三条件的立法目的,是让**有理有据、无害**的小改动**免于显著性检验**。
> **它不是一条「只要没测出变差就能上车」的通道。**
> 一个**理据反向** + **账本上有两层同号代价** + **胜负读数落在自己那一波噪声底以内**的改动,
> 三条件里过的那两条并不构成上车理由。**(c) 就是干这个用的。**

### 1.4 原子划得对,失败的是整个原子

`cmboots` 同时做两件事:①开局 tranquil → arcane;②`item_boots_of_bearing` 出表。
②**不是**捆绑进来的独立设计选择 —— arcane 在开局时,Bearing 配方会去买**第二双鞋**(移速不叠加)搁死 1500g。
**只 promote ① = 直接上那个已知坏掉的构型**,与 `creeppull`+`pullbeat` 作为一个原子 promote 同形(AGENTS.md)。
⇒ 本轮**没有、也不该有**「拆开只上一半」这个选项。

---

## 2. 棒子已交(铁律 9 连带规则:同一工作单元内显式交出)

**GH #190([strategy],本轮开)= 重新入集路线 A**,零 AWS、零新波次、跑现成语料:
检测器「**CM 存活帧中到最近友方英雄距离 > 1200 的帧占比**」,
**必须带同局其他四位的对照**(全队都散 ⇒ 是队伍站位模型问题而非 CM 的,两种结论修法完全不同),
铁律 4(i) 两层分读(本读数取自 UNGATED 语料 ⇒ **没有腿差可读,但侧别差要看**,站位与地图侧强相关)。

**路 B**(英雄组坚持 as filed):需要**标准打法里 CM 买 arcane 的正面出处**
(不是「缺蓝辅助该买蓝装」的一般论证 —— (1) 反对的正是它在**这个英雄**上的应用),
或一个**把账本打平的构造**(不牺牲 Bearing 终局、且不让 `ancient_janggo` 后滑)。

### 2.1 ⭐ 裁定是可证伪的,证伪条件已写进机器记录

`state.json:cmboots_RESOLVE_20260825T2130Z.next`:
**若 #190 读回来「CM 并不比同局其他位更远」,第 (3) 条腿被推翻,总监应凭该读数重开 (c)** ——
而不是让这条裁定靠「已经裁过了」续命。**登记这一条的目的,是让裁定可反驳而不是仅仅被断言。**

---

## 3. 顺带的基建:`queue.json` 加 `director_resolve`,**排期轴与结案轴分开**

原 `director` = {ruling, wave, at, ref, note},批测台读 `director.wave` **选波**。
结案(promote 了没有)是**另一根轴**;写进同一个字段会**覆盖排期裁定**,
而排期裁定里那句「**收割分母按 3 个种子算,不是 4 个**」(§AV.4 载体门)
**正是事后复核结论时要回去看的那一行**。
⇒ 新字段 `director_resolve` = {ruling: `PROMOTED`|`NOT_PROMOTED_RETURN_ON_C`|`REJECTED`, at, ref, issue, note};
`_protocol` 已 **append** 声明,**无 `director_resolve` = 尚未结案**。

这与 §BG.1 记的那个洞(「没有一个字段的名字叫『谁问的』」)**是同一族**:
`pending_rulings.py` 看得见「没裁定」,看不见「裁了排期但没结案」。
**下一步(仍未做,不改口径地登记为欠账)**:把「三条件已齐/已裁但无归宿」做成 `pending_rulings.py` 的新不变式 +
`_protocol` 的 `answers` 反向指针 —— 这两条与本字段是同一个洞的三端。

---

## 4. 体系健康 / 成本

* **五组本轮均有产出**,无空转(batch-desk 21:07Z 发 W11、hero 19:55Z、strategy 19:26Z、replay-check 19:18Z)。
* **CADENCE 9 洞**,与前两轮同型,**不升级**;点名 batch-desk 的 8.9h,归因与缓解见 §0。
* **Owner 优先项**:P1(拉野)、P2 的球都在**协同组**,不在总监;**未触 12 轮红线**,本轮不升级红色。
* **成本**:总监**零 AWS 调用、零支出、未批新支出**。批测台自报有效 MTD $40.79 + W11 预估 ~$2.05–2.15
  ⇒ 约 **$43**,围栏算术 **$43.75 ≤ $60**,**不跨 $50 ⇒ owner 本轮不会收到 Budget 告警邮件**。
  刹车线 $90、owner 批准档 $100,**三线均未触及**。
* **§BH.3 的配套义务核对**:该条要求「跨 $50 的那一轮必须在报告里写明原因」。
  **本轮批测台未跨 $50**(21:07Z 报告第 8 行自己写明了这一点)⇒ **该义务本轮尚未到期,无欠账**。
* **DECISIONS_NEEDED**:无新增。本周(W35)owner 邮件**尚未发**,预定 **08-30 周日**,重点仍是第 **9b** 条。

## 5. 验证

* `bots/`/`game/` **零改动** ⇒ 铁律 6 的 Lua 门不适用;容器无 `lua5.1`,**本轮未付那笔 `apt-get`**,如实登记。
* `state.json`:JSON 合法,`git diff --stat` = **12 insertions / 0 deletions**,**纯 append**,无一行被删改。
* `queue.json`:**逐字段 diff 断言** ⇒ 变更**恰好两处**(`hero-8.director_resolve` 新字段 + `_protocol` append),
  **UNEXPECTED FIELD CHANGES: NONE**;`question`/`result`/`status`/`director`/`done` **逐位未动**(§AW.1),
  `_protocol` 断言 `startswith(old)` = True。
* **裁定投递三处齐**(章程 2.5):`queue.json` 机器字段 ✅ / `test_set.md` §BI 全文档案 ✅ / **GH #144 追评** ✅。
* **诚实边界**:(a)(b) 的读数**采信录像组与批测台的测量,总监没有重跑那 233 份 `.dem`**;
  总监**自己核的**是 §1.2 的 (1)(2)(3) —— 检索、`special_value_shapes.lua:293` 的光环数值、
  `hero_crystal_maiden.lua:19` 的加点队列、以及「1200 = 两个半径同一个数」这一步。

## 6. 下次触发

1. ⭐ **`stable-v1`/`stable-v2` 打 tag(第十轮积压)** —— 已经积压到不该再顺延的程度,
   下一轮若再不做,按 §BH 自己立的规矩**写进 `DECISIONS_NEEDED` 说明原因**,而不是再写一次「下次触发」。
2. `_protocol` 的 `answers` 反向指针 + `pending_rulings.py` 新不变式(「三条件已齐/已裁但无归宿」,§3 的三端)。
3. `hero-18`(`PTSTAT`)读数回来后的实质裁定(四格判据 §BH 已预登记)。
4. `[bug] #156` / `[harness] #161/#152/#169`。
5. **W35 周日(08-30)owner 邮件**,`DECISIONS_NEEDED` 第 **9b** 条为重点。

## 7. Token 用量(铁律 8)

```
TOKENS total_in=5,333,621 out=51,259 turns=48
```

(统计时刻为止;其后的收尾回合——本行回填、commit、push——不计入。
本轮为**纯裁定轮**:零 `bots/` 改动、零 AWS、零批测,`total_in` 的绝大部分是
`test_set.md`/章程/三份跨组报告的读取,属该轮型的正常量级。)
