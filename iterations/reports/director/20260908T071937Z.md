# 总监 2026-09-08T07:19Z —— 三条判定全部落地(`fieldregen` 退集 45 → 44,`ownhalf`/`overchase` 留集且各自具名)

本轮最该被读的两条:
- **§2:一族四条臂逐字写着「disjoint by construction」、并被三份 sweep 断言为 0,而那个断言的作用域是
  **它自己驱动的那四条谓词** —— 同一笔购买的**第五个索取者**在任何一份族内 sweep 里都不出现。**
- **§3:本轮的两条候选发现**都已经在树里**,其中一条就写在我正要引用其教训的那一节(§FW.2)里。**

零 AWS、零波次、**`bots/`+`game/` 零 diff**、不发 owner 邮件、`DECISIONS_NEEDED` +0。
判定完结 **3**(连续第三轮达标)。

---

## 1. 取活依据

上一轮(04:20Z)⑨① 自己排的第一条:**「判定,而且是判定」——`ownhalf`/`overchase`、`fieldregen`,
已顺延 2 轮,理由不许照抄 §FW/§FX**。本轮把这三条**全部判掉**,顺延到此为止(实际顺延 3 轮)。

`arm_since.py --all` 最老一档(`lower_bound` 2026-07-25,**45 天**)5 条,`verify_coverage.py` 全部 **verify=0**;
`tpcommit`(§FB 明写留集)与 `lf_rescue`(GH #594/#597 正在其上买读数)沿用既有理由排除,其余三条即本轮三条。

⭐ **一条此前没被登记的共有读数**:逐波扫 `iterations/reports/batch-desk/waves/W*_wave.json`,这三条 id 在
**W39/W40/W41/W52/W53/W54/W55 七个波次里全部只以成员串内一员出现**(命中长度 410–503 字节 = 全量成员串,
**无一条短串**)⇒ 45 天里**没有任何一波在问它们的问题**。

## 2. ⭐⭐ 一句「disjoint by construction」,真值域是它自己驱动的那四条谓词

供给侧这一族在**同一笔 `item_flask` 购买**上有**五个**索取者,而族内的互斥断言驱动**四个**:

```
bots/item_purchase_generic.lua:776   `fieldregen` 内联块
bots/item_purchase_generic.lua:833   fieldbuy or buyband or buytower or buyring
```

:833 那四条是一起设计的,互斥**由构造保证**(`buytower` 塔环取反、`buyring` 英雄环取反、`buyband` 取
`fieldbuy` 0.55 天花板之上那条),四条里**三条在自己的注释里逐字这么写**;
`tests/_buytower_sweep.lua` / `tests/_buyring_sweep.lua` **断言**它(`overlap_tower_buy` /
`overlap_tower_hurt` / `overlap_ring_buy` / `overlap_ring_hurt` / `overlap_ring_tower` 必须全 0)。

**那个断言在四条之间为真,对第五条一个字也没说,而树里没有任何地方说过这件事。**
`fieldregen` 在族内**每一份 sweep 里都不出现** —— 不作为探针、不作为臂、不作为计数桶(逐份核过)。
它买**同一件**物品、在**同一个 `ItemPurchaseThink` 体内**早 57 行、**没有下界**(`< 0.45`,向下敞开)、
**三条环境子句一条都不问**(无 1600 英雄环、无 1200 塔环、无归属窗)⇒ 它够得到那四条臂各自被写出来去拥有的帧,
**包括 `buytower` 与 `buyring` 这两条整条杠杆就是一个取反子句、而它从不问那个子句的臂**。

**关系是抢先不是共现,方向由行序钉死**:两块共用 `not IsThereHealingInStash(bot)`,`fieldregen` 先跑;
它一买,药进 stash(该块只在离泉水 2500 之外跑),于是当帧及其后 :833 那个 `if` 被**它自己的 stash 子句**拒掉。
**早的块不是与晚的共享帧,它消费掉晚的存在的理由。**

`tests/test_coarmed_attribution_register.lua:293` 早就带着 `['fieldregen > fieldbuy'] = true`,
但那是一条 **WIDE 行**,注释逐字写着这次调用「is **not known** to sit inside either branch」
—— **混杂被登记了,机制没有**。本轮补的就是机制。

## 3. ⭐⭐ 诚实:本轮两条候选发现,**都已经在树里**

**这一条排在裁定之前,因为它改变别处该被怎么读。**

1. **`ohnum` 的「结构零」**。我从源码读出 `J.ShouldRefuseUnsupportedPunish` 的首个循环
   (allied building ≤1200 of target)**恰是** shipped 路径上让控制流到达它的那个条件的**补集**
   (同半径、同目标、同 unit list)⇒ shipped 路径上它不可能返回 true。实测 `ohnum` 单臂 **28 → 28(零)**、
   `ownhalf`+`ohnum` **79 → 48(−31)**。
   ⛔ **这一整段已经写在 `tests/test_ohnum_refusal.lua` 的头里,数字逐位相同**
   (`pd_shipped 28` / `pd_ownhalf 79` / `both_changed 31` / `ohnum_alone_changed 0`,由 `tests/_ohnum_sweep.lua` 量出)。
   **我重新推了一遍已经记录的结果。**
   值得登记的只有**一条口径**、不是一条发现:该文件标题说这个零「是测量不是结构零」,同时**自己写明**
   「reads zero because the shipped domain is **building-proximate by construction**」——
   两件不同的事被合在了一句里:**调用点可达**(真,这正是 `check_armed_wiring` 的 WIRED 诚实的原因)与
   **谓词在 shipped 路径上不可满足**(也真,这正是单臂 `ohnum` 波的裁决**不携带信息**的原因)。
   **本轮不为此开 issue**,该文件的口径可辩护。
2. **语料 82.5% 是对线期**(842/1021)。**已经写在 §FW.2 里**(「laning 842 = core 712 + support 130」)
   —— **就写在我正要引用其教训的那一节**。

⇒ 本轮真正新增的产物只有一件(§5)。**代价登记在此**:两条候选各花掉一次独立推导,而两份记录都在树里、都可检索
—— 1726 行 `test_set.md` + 11604 行章程,**「没找到已记录的读数」的成本就是把它重新推一遍**。

## 4. 量具,和它当场推翻的那个表面结论

`tests/_fieldregen_overlap_sweep.lua`(110 fixtures / **1021** live turbo hero-frames,4084 次单臂探针):

```
fr_pred 6      arm_fieldbuy 33   arm_buyband 20   arm_buyring 10   arm_buytower 8
overlap_fieldbuy 0  overlap_buyband 0  overlap_buytower 0  overlap_buyring 0  overlap_any 0
arm_leak 0   fr_pred_err 0   arm_err 0
```

⛔ **`overlap_any 0` 不是「它们不重叠」,是仪器的零 —— 而这正是 §FW.2 那个形状。**
漏斗逐条(独立驱动):`live/turbo 1021` → **`laning_true 842`(82.5%)/ `not_laning 179`** →
`hp45 134` → `noflask 893` → `notango 579` → **全部合取 6**。
杀死它的是 `not J.IsInLaningPhase()`:`fieldregen` 按构造是**过线后**的杠杆,而语料 82.5% 是对线期;
四条臂经 `J.IsFieldRegenSituation` 进域,**没有对线子句**,所以它们在这里有域。
⇒ **两边被量在这份语料互不相交的两片上**,那个 0 对这一对**两个方向都不构成证据**。
sweep 把 `fr_pred` 与 `overlap_*` **并排打印**就是为了这两者永远不会被混读。

**这一步是本轮的分水岭**:若把 `overlap_any 0` 读成域的零,得到的是**方向相反**的裁定
(「它与四臂族不冲突,留着无害」)。量具当场推翻了我自己的源码读数所指向的表面结论。

## 5. 裁定与落地物

| id | 裁定 | 具名理由 |
|---|---|---|
| `fieldregen` | **退回出集** 45 → 44 | (a) 从 fixture 那条路**结构上买不到**(域 6/1021);七波从未单臂;逻辑依据已被同族四臂以更细形状重写 |
| `ownhalf` | **留集** | 它是 `ohnum` 域的**唯一使能者**(单臂 28→28;+`ownhalf` 79→48);退它会冻死一条 2 天大的 id |
| `overchase` | **留集** | 函数体 2026-09-07T23:xxZ 被协同组换过(§FY)⇒ 45 天是**名字**的年龄,不是当前杠杆的 |

⛔ **两条「留集」不是「再等等」**:§FX.2 立过「再等等是唯一一格不必付代价的」,本轮按它办 ——
两条留集**各自进 `owed_executions.json`**(16 → **19** 行),带裸读得出的 `done_when`,**从今天起每轮举手**。
⛔ **`fieldregen` 退集不是 reject**:gate 与整块**逐字保留**,`bots/`+`game/` **零 diff**;
重新入集须走**波次录像**的 (a)(fixture 路本轮已量出对它结构失明)。

落地物:
- `tests/_fieldregen_overlap_sweep.lua`(重语料 sweep,`_` 前缀,**30s,不进快腿** —— Lua 检测器腿已在 120s 预算边缘,GH #358)
- `tests/test_fieldregen_family_overlap.lua`(**纯源码断言**,毫秒级,**6/6**;语料读数以定值引在头里,同 `test_ohnum_refusal.lua` 的做法)
- `tools/agent/mutstand_fieldregen_overlap.sh`:**7/7 CAUGHT,零 SURVIVED**,控制体(纯注释编辑)**未被抓到**,`git diff --quiet` 还原逐字节 **YES**
- `test_set.md` §FZ + 成员串(45 → 44,408 → **397** 字节,md5 `f4292f7b…` → **`fe7a309fc06a229e97290b2db4c3bed3`**)
- `state.json`:`fieldregen_RETURNED_20260908` / `ownhalf_KEPT_20260908` / `overchase_KEPT_20260908`
- `armed_since.json`:`fieldregen` 加 `retired_at`/`retired_note`(`arm_since.py` 现读 **44 / pinned 44**)
- `owed_executions.json`:三行(`fieldregen_condition_a_from_waves` / `ownhalf_condition_a_or_return` / `overchase_new_body_first_reading`)

⚠️ **M2 的锚不是 stash 那一行本身**:那一行在该文件里出现 **3 次**,而 `perl -0pi` 不带 `/g` 改**第一处**
—— 那正是 `fieldregen` 块 —— **一个悄悄切了别的块却照样打 CAUGHT 的变异**(GH #550 同型)。
改锚到唯一的 `RegenRing(bot) )`,并在脚本里写下这个理由。

## 6. ⭐ 新闸落地第二天,第一个被它挡住的又是总监自己 —— 这次我动了

`tools/agent/py_gate.py`(上一轮 GH #616 落地)在本轮 push 前**拒了我自己的新变异台**:

```
1 FAILURE(S):
  - tools/agent/mutstand_fieldregen_overlap.sh leaves a restoring EXIT trap in effect
```

判得对:我的 trap 体是 `cp "$BAK" "$BUY" …` **内联**,不是调用本文件定义的 `restore`;
`tests/test_mutstand_restore_trap.py` 要的正是「**最后armed的那个 trap 要够得到一个具名的 restore**」
(GH #418:trap 体是 `rm -rf "$WORK"` 而备份就在 `$WORK` 里,比没有 trap 更糟)。
**修法是把 `restore()` 定义提到 trap 之前、trap 按名字调它**,不是给闸加白名单。
修后 `py_gate` **84 ran, 0 findings, 11.8s, EXIT=0**。
⇒ **上一轮那句「bypass 变成日常的那天这道闸就死了」本轮没有兑现**:本轮**未用 `RULE6_BYPASS`**。

## 7. 铁律 6 与自检读数(逐条,未跑成的不作声称)

- 静态半:`bash tools/agent/rc.sh bash tools/agent/luacheck_gate.sh` → `luacheck bots game: 0 warnings`,
  **`GATE_EXIT=0 CLEAN`**,`RC_EXIT=0`。**未用 `RULE6_BYPASS`**。
- python 快闸:`py_gate.py` **84 ran / 0 findings / 0 uncertifiable / 11.8s / EXIT=0**。
- 变异台:`mutstand_fieldregen_overlap.sh` **control_ok=1 caught=7 survived=0**,`RESTORE YES`,`EXIT=0`。
- 新测试:`test_fieldregen_family_overlap.lua` **6/6**。
- 登记表:`arm_since.py --all` → **armed 44 / pinned 44**,`fieldregen` 已不在表内。
- ⛔ **动态半(Lua `run_tests.lua` 全套,~100min)本轮未跑,不作任何「全绿」声称**;
  `bots/`+`game/` 本轮**零 diff**。
- ✅ **读 `test_set.md` 的 49 文件子集:跑完了(提交后约 25 分钟),`SUBSET_EXIT=1`,
  读数 `656 ok, 1 failed`,唯一红是 `test_gated_helper_nesting_census.lua`。**
  这一跑是为成员串 45 → 44 买的回归读数(许多文件从 `test_set.md:2` 读成员串)。
  **那条红在我动手之前就红** —— 同一棵树上开工自检先报过它,成因是协同组 `607ce30b` 的 `tprecov`
  新嵌套,与成员串无关 ⇒ **成员串 45 → 44 在这 49 个文件上造成新红 0**。
  (本节第一版写于提交时,当时它还在跑,逐字写着「没跑完不是过了」;此处是**拿到读数后的回填**,
  两句在报告里长得不一样是故意的。)
- ⚠️ **纪律 3 的「后台包装」形状本轮第二次现场兑现**(上一轮 09-07T22:15Z 已登记为新形状):
  harness 给这条后台任务报 **`exit code 0`**,而我要的真码是 **`SUBSET_EXIT=1`** ——
  插在中间的不是 `tail`,**是后台包装本身**(它报的是复合命令里最后那条 `echo` 的码)。
  **这一次没有吃亏,唯一的原因是我按 `rc.sh` 的习惯先把真码 `echo` 进了输出文件**;
  `rc.sh` 的立法理由在这条路径上同样成立,而**目前仍无人守卫**。
- **开工自检**(串行,`rc.sh`,真码 **`RC_EXIT=3`**,10 条腿全部跑完 —— 与上一轮 `EXIT=124` 被砍不同):
  `FINDINGS (exit 3): cadence queue-rulings owed-executions trunk-red(python) trunk-red(lua)`;
  `UNCERTIFIABLE (exit 2): none`。
  **两条 trunk Lua 红,都不是本轮引入,也都不是本轮修的**:
  - `test_activemode_call_site_census.lua` —— [ratchet] GH #267,注释里 `GetActiveMode()` 提及 3 → 4,**是散文变动**,该检查自己要求**重新取这个数**而不是折进 `get_active_mode`;
  - `test_gated_helper_nesting_census.lua` —— 新出现一条 gate-inside-gate:
    `tprecov | J.ShouldSipNotTpRecover | J.HasFieldRegenSource | bagsalve`(协同组 `607ce30b`),需要读一遍再钉。
  ⚠️ 二者**归属别组的落地产物**,本轮不代改(与 §2.2 W55 那根棒同一条纪律)。

## 8. ⚠️ 纪律 3 又一发,**又是本轮第一条命令**

开工自检第一次调用写成 `routine_selfcheck.sh … | tail -60`,**§22 守卫当场拒**
(`REFUSED: stdout is a PIPE … nothing was checked; this is NOT a pass`,`SELFCHECK_EXIT=2`)。
章程 §0 的措辞(2026-09-04 改)**逐字覆盖这一发**,我只是没照做。
**不新立措辞,登记而已 —— 拦下它的是守卫不是我记住了。** 改走 `rc.sh` 后拿到真码 `RC_EXIT=3`。

## 9. 未做 / 顺延(诚实边界)

1. **没有驱动出抢先** —— 行序与共用闸是**读**出来的,这份语料驱动不了它。
2. **没有量 `fieldregen` 退集之后四条臂的读数是否真的变干净** —— 那要一波单臂波次,本轮不发波。
3. **没有核 `ownhalf` 留集的代价** —— 它继续在成员串里当混杂项;本轮只主张「退它会冻死 `ohnum`」,**不主张**留它对别的 id 无害。
4. **`overchase` 的新身体没有被任何波次读过** —— §FY 落地至今零波次,只登记不可比性。
5. **成本**:本轮零 AWS ⇒ **不对 MTD 作新声称**(读数归批测台报告)。
6. **patch 检查仍未做**(低频,已连续多轮顺延)。
7. **效率台账**:本轮非周日,不做。

## 10. 下次触发

1. **两条 trunk Lua 红逐条**:`test_activemode_call_site_census.lua`(重新取注释提及数)与
   `test_gated_helper_nesting_census.lua`(读 `tprecov | J.ShouldSipNotTpRecover | J.HasFieldRegenSource | bagsalve`
   这条新嵌套,回答「un-armed 时内层是不是该合取的单位元」再钉)。
2. **核 §2.2 那根棒**(`owed_executions.json:w55_record_launched_at_and_gate_iv_inputs`)——
   它**挡着 W56 发波**,批测台未补则 gate (i) 在 W56 上结构上只会继续 UNCERTIFIABLE。
3. **GH #358 的 120s 预算要人裁**(Lua 腿 86 文件,语料增长正在挤爆它;本轮新 sweep 已按此**不进快腿**)。
4. **退休 owed registry 里那 4 行 DONE**(**需读一遍**四份产物)。
5. 上一轮 ⑨③ 原样顺延(`text_absent_done_when_kind` GH #523 **先写认领再开工** / GH #517 /
   `kind:"ruling_request"` / GH #454/#487/#460/#473 乙/#489/#486/#496 / #513/#514 / #449/#410/#436/#285 /
   **patch 缺口 P3** / `ckpush` 有时限)。
6. **patch 检查**(已连续多轮顺延,建议下一轮强制做掉)。
7. ~~把 §7 那份 49 文件子集跑完~~ —— **本轮已完成**(见 §7 回填:`656 ok, 1 failed`,
   唯一红是既有的 `tprecov` 那条,成员串新红 **0**)。留在这里只作记录。
8. **给「后台包装吞掉真码」这条路径立一道守卫**(§7 第二条 ⚠️)—— `rc.sh` 只守着自检那一条命令,
   后台任务/发波循环/批量脚本里**没有任何东西举手**;本轮第二次现场兑现,两次都是靠习惯躲过的。

## 11. Token 用量

```
TOKENS total_in=13,563,047 out=94,876 turns=87
```

注:这是**统计时刻为止**的数字,统计后的收尾回合(commit / push / 本节写入)不计入。
本轮偏高的成因具名在 §3:**两条候选发现各花掉一次独立推导,而两份记录都已经在树里**。
