# 总监报告 2026-08-20T23:00Z(第二十四次触发)

**一句话**:优先级 2(a) 空 ⇒ 走 2(e)/基建。本轮找到的东西比一次 promote 判定值钱——
**我们每一波付钱跑 ~630 局,却只保留 ~32 局的帧级证据(5.1%),因为 `soak_loop.sh` 里
`if [ "$SLOT" = "1" ]` 只让 1/16 的 slot 录像**;而本周全队的判定体系**刚刚整体搬到了帧通道上**。
本周五次「可测性事故」的共同形状「事件率太低、一波买不到」,**分母是一个可调的 harness 设置**。

**未花 AWS 钱、未做任何 AWS 调用、本轮零 Lua 改动**(产出 = 1 条新 issue + 2 条 GH 裁定 +
`iterations/streams/test_set.md` §W + 本报告 + 章程)。

---

## 1. 输入

| 组 | 最新报告 | 本轮是否消化 |
|---|---|---|
| batch-desk | 22:18Z 纯基建轮(§V.6 seed→阵容索引,零支出未上机) | ✅ 全文 |
| hero | 21:51Z GH #73 pre-flight(`lionult` 域实测为空) | ✅ 全文 |
| strategy | 21:30Z `blinkflee` ES fixture 对 + HOLD 集不同质 | ✅ 全文 |
| replay-check | 20:49Z(21:15Z 已消化) | 无新报告;21:15Z 派的两件零支出活(§V.2 梯度复读 / `liondrainstop` 跨度筛)仍在途 |

**优先级 2(a) 空**:52 条 open issue,自 21:15Z 新增 **0** 条 `[bug]`/`[harness]` 归总监
(最新两条是 #74 `[strategy]` 20:53Z、#73 `[hero]` 20:09Z,均已在 21:15Z / 本轮处理)。

**21:15Z 派的三件零支出活的落地情况**:
- ✅ `blinkflee` fixture 对 —— 协同组 21:30Z 交付(两枚真实帧 fixture + 11 例测试,868/868,六次变异)
- ⏳ §V.2 `capmono` 臂内 HP 梯度复读 —— 录像组未报
- ⏳ `liondrainstop` ≥2.0s 跨度筛 —— 录像组未报
- ✅ §V.6 seed→阵容索引 —— 批测台 22:18Z 交付(137 run / 11,048 局 / 112 种子)

## 2. 本轮的核心发现(源码级,自己读的,不是转述)

### 2.1 事实

`tools/batch_test/soak/soak_loop.sh:26-28`:

```bash
REC=""
if [ "$SLOT" = "1" ]; then REC="+tv_enable 1 +tv_autorecord 1 +tv_delay 0"; fi
```

- `.log.gz` + `.analysis.json`:**每一局都传**(L94-97,与 slot 无关)
- `.dem`:**只有 slot 1 传**(L101-108),传完 `rm -f`(L107)
- `farm_start.sh:5` `N=${1:-16}`,批测台当前用 16(`spot_run.sh:29` 默认 14)

⇒ **经济/聚合通道 n = 全部局数;帧级通道 n = 局数 / N。**

**对上实测**:§R.0 arm B = 292 有效局 + 24 暖场 = 4 台 × ~79 局 ⇒ slot-1 ≈ 4.9 局/台
⇒ **~20 局/臂带 `.dem`**;录像组本周实际拿到 16/臂、两臂 32 局。**对得上。**
**付钱跑 632 局,留下 32 局帧证据 = 5.1%。**

### 2.2 为什么这条压过本轮其他所有事

本周方法论**整体搬到了帧通道**:
- 08-19T12:50Z 通例:每局动作数 <~1 的 id,条件 (b) **一律用行为检测器,不许用 gpm/xpm**;
- §V.7(21:15Z):新 id 首次 armed 前必须给**事件率 / 零通道 / 可分辨性**三个数;
- 条件 (a) 本来就只能在帧上证。

**我们把整个判定体系搬到了一条样本量只有另一条 1/16 的通道上,而这个 1/16 不是自然规律。**

本周五次可测性事故(#28 / #34 / #68 / #71 / #74)**全是同一个形状**:「事件率太低,一波买不到」。
按 16× 语料重算:

| id | 实测事件率 | 当前(~16 局/臂) | 16× 后(~250 局/臂) |
|---|---|---|---|
| `blinkflee` | 0.0625 撤退腿/局 | **~1 次(判不了)** | **~16 次** |
| `tpdying` | ~0.06 次/局 | ~1 次 | ~15 次 |
| `capmono` | 806 clean 帧 | 4 种子 MDE **±21pp** | ~12,900 帧 ⇒ MDE **≈±5pp** |

最刺眼的是 `capmono`:21:00Z 我判它「(a) 未通过 + (b) 不可判」,两条通道同时读不出;
录像组的结论是「**瓶颈是检测器分辨率不是样本量**」—— **那句话是在 806 帧的前提下说的**。

### 2.3 落地

**[harness] #75** 已开,含:主方案(用每个 slot **已经存在的** rcon 通道发 `tv_record <唯一名>`——
原注释的约束是**归属**不是磁盘:16 个 slot 共用 replays 目录,`ls -t|head -1` 抢不准)、
备选(flock + 认领即改名 + 用同 TAG `analysis.json` 终局比分自检)、
S3 生命周期要求(`.dem` 现在**上传两份**,16× 后 ~12.6 GB/波;每保留一波 +$0.15/月,算进 $45 围栏)、
验收三条、诚实边界四条。

**为什么不直接改代码**(章程说 `[harness]` 总监直接修):bash **只能在实例上验证**,
改错的失败模式是**整波一个 `.dem` 都没有**(比现状更差),且按 CLAUDE.md「harness 改动需要
soak-loop 重启」会跨波持续。**保守默认**:设计 + 测量协议我出,代码在能验证的地方落地。

**吞吐测量设计(零额外支出,总监指定)**:在**本来就要跑的**波次里,**只对一个种子的实例**
开全 slot 录像、**A/B 两臂都改** ⇒ 改动**对称**,A/B 比较**不被污染**,只是那台 n 变小;
另外三个种子是**同波同硬件的吞吐对照**。判据:有效局/实例·小时掉幅 <10% 推广 /
10-30% 折中(录一半 slot)/ >30% 回 slot-1 并把「帧证据稀缺」正式改写成硬约束。

## 3. 本轮的四条裁定

### 3.1 §V.5.3 **路 β(换种子)不批准现在做**(理由变了)

自己读了 `iterations/data/seed_roster_index.json` 复核批测台的载体密度表,两条补充:

1. 索引里的 `radiant`/`dire` **是固定的**(镜像换的是 cand 侧,不是阵容)。
   **885 与 974 是同一种分布**:Axe+ES 在 dire、OD 在 radiant ⇒ armed 侧载体数
   dire-cand 局 2 / radiant-cand 局 1,**均值 1.5**,对当前四种子的 ~0.5 只有 **3×**。
2. 885 归档 70 局 / 974 归档 46 局,但按 §2.1,这 116 局里**只有 ~7 局带 `.dem`**
   ⇒ **§V.7 的上机前 pre-flight 在新种子上同样做不了。**

**3× 的杠杆对上 16× 的杠杆,且后者不花 EC2 钱。** 先做 #75。

### 3.2 off-roster 局:**报数,不过滤**

- 过滤**本身是安全的**:我读了 `recover_verdict.py:46-53`,verdict 按
  `mirror:<cand>:s<seed>:<side>` 分组取**组均值**,**没有逐局配对** ⇒ 剔单局不拆散镜像对。
  (我起初担心「破坏配对」,自己查完**收回**这个担心。)
- **收益近似为零**:单局值 0.07-0.18 sd;8 月至今 1 局,859-906 全部 bisect 波次 0 局。
- **成本不为零**:在收割路径上加一个**会改变历史数字**的分支要自己验证,保护 0.02% 的事件。

⇒ 每波 `--build` 之后 off-roster 计数进批测台报告**固定栏位**,**非零当告警**
(= soak draft 没生效,是回归信号),先查原因再用那波数据,**不要静默扣掉**。
**把噪声注入变成报警器,比变成静默过滤器有用。**

### 3.3 `[hero] #73`:**批准关掉 lever(a) `lionult`**,lever(b) park,#73 保持 open

英雄组读数够硬(1216 帧 below-cost 0 / post-spend 带 0),`axeblink` 同款陷阱,上机=测空气。
**但总监加了一条边界**:Finger cost **[200,400,600]**,本轮语料它**恒在 level 1**
(三局 hero level 上限 9/11/11,第二点大招要 hero **12**)——
**cost=400 那条线对着 380 的蓝池底不是空的**。⇒ 正确记法是「**在 Finger level 1 上域为空**」,
不是「`lionult` 的域为空」;复活条件 = turbo 局能稳定到 hero level 12+。
lever(b) 的 park 同样标成**「在当前语料单价下」**(0.33 episode/局 × 16 ≈ 80 episode)。

### 3.4 例行波次「无目的不启动」**维持**,并给出三条解除条件(§W.5)

批测台 22:18Z 按 §V.5.4 不启动是**正确的**。解除:(甲)#75 吞吐测量搭在另有目的的波次上;
(乙)§V.2 梯度复读 / `liondrainstop` 跨度筛出结论;(丙)任一等门 id 过 §V.7 三个数。

## 4. §0b 第二十例(新亚型)

前十九例都是「某个判据/推理被下游或环境作废」。这一例是:
**我们围绕一个观测到的稀缺(事件率太低 ⇒ 买不到 ⇒ park / 退回 / 换种子)建立了整套排期纪律,
却没有人去问那个稀缺的分母是谁定的。**

通例:**每次得出「这个信号量不够」时,必须同时报出「n 是怎么来的、能不能改」。**
推论:①「事件率/局」这个单位**掩盖了「我们保留了几局」**,报事件率必须并排报保留局数与总局数;
②凡是「按当前单价不值得」的 park/退回,理由行**必须写「在当前语料单价下」**,
否则会被后来的 agent 当成永久性判断(#73 lever(b)、#74 `blinkflee` 已按此改写)。

## 5. 其他章程项

- **promote/reject**:本轮无新判定。`capmono` 的 NOT-PROMOTE(21:00Z)不变——
  但 §2.2 给它记了一条:**若 #75 落地,它的 (a)/(b) 都值得在 16× 语料上重问一次**(零 EC2 增量)。
- **测试集**:**逐字未变**(19 eligible;§U.0 的 18 id 仍是下一波串)。本轮只新增 §W。
- **成本**:MTD **$15.00**(批测台 22:18Z 自报;$11.70 → $15.00 是 16:08Z/18:08Z 两波 on-demand 的
  **计费回补,不是新支出**;开工/收尾 `describe-instances` 均 0 台在跑,**无泄漏**)。
  $45 围栏余 $30,$90 刹车线远未触及。**新增一笔待记账**:#75 落地后 S3 存储每保留一波 +$0.15/月。
  **总监本轮未做任何 AWS 调用、未启动计费资源。**
- **Routine 健康**:四组本轮均有产出;录像组最新报告 20:49Z(距今 ~2.1h),21:15Z 派的两件零支出活
  在途,**不算空转**。批测台的「零支出纯基建轮 + 按 §V.5.4 不启动」是**正确节流**。
- **今日周四**,效率台账(仅周日)跳过,**08-23 那次要做**;patch 11:10Z 已分片定级不重复。
- `wandlimbo` 机会普查连续第**十一**轮无人做;待复核队列
  `#44`/`#45`/`#55`/`#58`/`#62`/`#63`/`#65`/`#66`/`#67`,**`#45` 第十一次顺延**
  (本轮工作单元用在 §2 的发现上;它压过队列里任何一条)。
- `DECISIONS_NEEDED.md` 今日不新增(四条裁定全在 §O.3 自主授权内),**本周 owner 邮件仍为零**。

## 6. 验证(铁律 6)

**本轮零 Lua 改动**(`git diff --stat bots/ game/` 为空),按铁律 6 立法目的**无适用对象**,
未装 Lua 工具链。最近一次本容器全量复核:19:00Z **857/857 + luacheck 0**;
其后协同组 21:30Z 加了 11 例(自报 868/868,只加测试与 fixture,`bots/` 零改动)。

## 7. 下次触发

1. **收 §V.2 `capmono` 臂内 HP 梯度复读 + `liondrainstop` ≥2.0s 跨度筛**(录像组,零支出;
   按 §H 登记表逐条对,**不许改口**);
2. **`[harness] #75`**:批测台若已在实例上验证 `tv_record`,复核并推动落地
   (吞吐测量必须**搭车**、**两臂对称**);
3. 下一波的申报目的按 §W.5 三条解除条件判,**在这之前维持不启动**;
4. 清待复核队列(`#45` 最老);`#66`/`#63` 修订版落地后按 §R.2 复判 `cmrguard`;
5. 都在等就做 **backlog #1 帧语料检索工具**(注:#75 落地后它的收益再涨 16 倍)/
   patch 分片 P3 / `[harness] #27`。

## 8. 本次会话 token 用量

```
TOKENS total_in=5,999,861 out=84,244 turns=55
```
