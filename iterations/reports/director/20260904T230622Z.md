# 总监 2026-09-04T22:0x–23:0xZ —— GH #492 执行完毕:**一道已出厂、无闸的守卫,第一次在真帧上开口**

**工作单元**:`iterations/owed_executions.json:fixture_mock_extrapolated_location`
(§EI.5 第 5 条批准,执行方=总监本人,触发器=「下一轮总监触发」)。
**本行本轮已退休。** 裁定与读数全文:`iterations/streams/test_set.md §EL`。

**零 AWS 调用**(连 `budgets` 都没读)、**零发波**、`bots/` + `game/` **零 diff**、
**没有 promote/reject 任何 id**。

---

## 0. 一句话

fixture mock 补上 `GetExtrapolatedLocation` 之后,`J.CanEnemyInterruptTpChannel`
从**域内 257 帧全部抛错**变成**域内 257 帧全部回答**,其中 **73 帧说「别开这个 TP」**——
而这道守卫的闸在 **2026-07-23 就被 promote 成 turbo 默认**(`tpsafe2`),
也就是说它在我们发的每一局 turbo 里都跑,**却从没在一个真帧上执行过**。

## 1. ⭐⭐ 主读数(同一语料、同一棵树,mock 是唯一的差)

台子是**文件拷贝**(evidence-discipline 的还原台):把 mock 换成 trunk 版跑一遍
四把尺子,换回来再跑一遍,`cksum` 验证还原**逐字节相同**。
**`driven` 981 / `live` 1012 / `fixtures` 109 两边一致** —— 这三个数正是历史上
三次把这些计数搬动的那个混淆项(GH #106/#107/#127 的语料增长),
两边一致才使下面每一个差**可归因于 mock**。

| 尺子 / 量 | 前 | 后 |
|---|---|---|
| `J.CanEnemyInterruptTpChannel` 域内帧 | 257 | **257** |
| ↳ 抛错 | **257** | **0** |
| ↳ 答 **true**(「别开这个 TP」) | **0** | **73** |
| ↳ 答 false(域内) | 0 | 184 |
| `J.ShouldTpSupportTowerFight` 抛错帧 | **75** | **0** |
| §EF.1 `fires` | 8 | **9** |
| §EF.1 `fires_core` / `yield_domain` / 其余五个计数 | 3 / 2 / 各 1 | **一个没动** |
| itemdesire `crash_total` | 224 | **50** |
| ↳ `local x2 = sLoc.x` | **178** | **0(站点消失)** |
| ↳ `GetFarmLaneDesire( LANE_TOP )` | 46 | **50** |
| ↳ `no_action` | 754 | **928** |
| ↳ **item 动作数** | 3 | **3** |

owed 行要求的两处重取**都在同一个 commit 里**:
**(1)** `test_itemdesire_world_assertion.lua` 压着的 **178** —— 重取后那个站点**不存在了**
(旧断言 `nX2 ~= nil` 拒绝了树,**这是对的**:它要的就是有人开口说明,而不是把 `nil` 读成 0);
**(2)** §EF.1 的域 —— **8 → 9**,分母从「937 帧上量的」变回真正的 **1012**。

## 2. ⭐⭐⭐ 本轮最该被读的:**解除审查不等于换来读数,而总数会替它撒谎**

178 帧解除审查之后**不是 178 个读数**:

- **174 帧**跑到底,答「无动作」;
- **4 帧**顺着同一条链**往下掉进下一个没桩的引擎名**(`GetFarmLaneDesire`,46 → 50);
- **新增 item 动作:0 个**(3 → 3)。

而 `crash_total` **224 → 50**,一路向下 —— **只看总数,这是一次干净的胜利。**
让那 4 帧可见的唯一原因,是这个键是**按语句**记的(GH #221 那次改造)。

⇒ **一次修复可以把它解放出来的帧交给下一个洞,而聚合计数在这个方向上失效静默。**
与协同组 §EJ.2 的排序约束同源:**修复之间有拓扑,不是一张独立的清单。**

## 3. ⚠️ 收益的另一半是一句**必须被量着**的世界断言

fixture 是**一个瞬间**:`make_fixture.py` 只 dump x/y,**109 份没有一份带速度/朝向/路点**。
所以 mock **不能外推**,它答当前位置 = **把每个单位建模成原地不动**。代价精确可写:
`nSoon == nNow` ⇒ 守卫第二支「他在贴上来」**在本语料上不可达**,
第一支「他现在就够得着我」照实回答 ⇒ **73 是打断的下界,永远不是上界**,
偏向「TP 是安全的」那一侧。

**这句话不是注释,是被量着的**:新 `tests/test_fixture_extrapolation_mock.lua`(**4/4**)
把它拆成三条可证伪的断言。变异台 `tools/agent/mutstand_extrapolation_mock.sh`
(**M1/M2/M3/M4 CAUGHT + M5 控制组 SURVIVED,零 NO-OP**)里 **M3 才是承重的那一发**:
让 mock **真去外推**(+500 沿 x)—— 什么都不抛,所有域照样读得出来,
**只有那句声明变假**;抓不住 M3,这条声明就是穿了测试外衣的注释。
**M5 是诱饵**:`velocity` 一词出现在**注释**里(fixture 的生成头逐行写着
`make_fixture.py` 自己的参数),必须 SURVIVED —— 否则 [model] 腿会在下一次
生成 fixture 时开始喊狼来了。**承重用例是诱饵用例**,§EG.5 / §EE.5 的第三次现场。

## 4. 顺带:**变异台自己栽在同一族缺陷上,当场抓住**

第一版 `mutstand_extrapolation_mock.sh` **5/5 全对**,却 `exit 2`:
`rm -rf "$WORK"` 排在 `trap restore EXIT` 触发**之前**,EXIT 处理器在目录已删之后
跑 `cp`,失败,退出码被它顶掉 —— **一台 5/5 的台子穿着失败的衣服**。
与「读退出码时任何插在中间的东西都能替它作答」(管道 / `timeout` / `echo`)
**同一句话,新穿法**:这次插在中间的是**自己的清理顺序**。修法 `trap - EXIT`。

## 5. 两条裁定(协同组 §EJ.8 交上来的两件,都不要钱)

- **(甲) 名单照收,本轮**故意**只修一个。** 协同组是对的:`^Get -> 0` 的 roster 是
  **三个名字 / 六个载体**。只动 `GetExtrapolatedLocation` 的理由是**可归因**——
  同轮改两个名字就换不出第 1 节那张表。
  **`GetVelocity` 登记为下一件**(`owed_executions.json:mock_getvelocity_ultloc`):
  `J.GetUltLoc` 域内 **503 帧、503 抛、0 答**,shipped 且无闸(`hero_shredder.lua` 在调),
  **与本轮修的这条逐字同族**。`GetCurrentActiveAbility` 不另开行(今天被 `^Is` 挡着)。
- **(乙) 排序约束照收,并且**故意不写进 `AGENTS.md`**。**`^Get -> 0` 的修复必须排在
  放宽 `^Is/Has/Can/Was -> false` **之前**:先松 `^Is`,四个载体会从 **1436 个干净读数**
  变成 **1436 次抛错**,而下游两桶读者把每一次都记成「量过了,答否」——
  **正是 #492 立案的那条缺陷,一次放大四倍**。
  不进 AGENTS.md 的理由是本仓库自己的教训:「只在某人做 X 那天生效的约束,
  写成没人会在那天重读的散文」正是 §EI.3 立 `promote_atoms.json` 的形状。
  放进 `owed_executions.json:mock_isprefix_ordering`(常驻**否决权**,触发器=有人动 `^Is`)
  + 下一个人一定会读到的那个文件头。

## 6. ⚠️ 交棒(铁律 9 连带规则 —— 本轮真的有一根,而且不是我的)

`test_midsupmirror_checkability.lua` 的 `[ratchet]` 腿**按设计开火了,而那正是产物**:
修 mock 把 §EF.7 登记的四条镜像成员里的**打断守卫那一条**从「语料作不了证」
变成「作得了证」(`int_true` 0 → **73**)⇒ `J.HasAvailableSupportResponder` 缺失的
`CanEnemyInterruptTpChannel` 成员**现在可以带真帧 fixture 落地了**(73 帧里随便挑)。
**交协同组**(成员是他们写,不是总监);另三条腿原样不可作证。
⚠️ **这里的不对称是这条 ratchet 值得留着的理由**:解锁它的修复在 **harness** 里,
**协同组自己的 backlog 里没有任何东西会在它发生的那天注意到**。

## 7. 巡检 / 成本 / 邮件

- **成本**:本轮**零 AWS 调用**。MTD **照抄**批测台 21:15Z 一轮的自报口径,
  未新读 `budgets`(总监只读,不花钱);围栏 `$60` / 刹车 `$90` / 批准线 `$100` 均未及,
  九月未到 $50 档。⚠️ 未新读 ⇒ **本轮不对 MTD 数字作任何新声称**。
- **邮件**:**未发**(本周 08-31 已发,决定类邮件每周 ≤1 封)。
- **`DECISIONS_NEEDED` +0**:两条裁定都在自主授权内。
- **`OWNER_PRIORITIES`**:本轮未取属于总监的未完成项 —— 工作单元由 owed 行按
  「下一轮触发」预先指定,且它是**总监自己欠的**。
- **patch 检查**:**未做**(低频项,连续顺延中,已在下次触发清单)。
- 周五,效率台账(仅周日)跳过。

## 8. 铁律 6(⚠️ 逐条裸读,不声称没跑过的东西)

- **静态门**:`bash tools/agent/luacheck_gate.sh` ⇒ **`GATE_EXIT=0 CLEAN` / 0 警告**
  (冷启自装 `lua-check`)。**未用 `RULE6_BYPASS`。**
  ⚠️ **诚实限定**:这道门扫的是 `bots/ game/`,而本轮 diff **全在 `tests/` 与 `tools/`**——
  所以它是**真跑了且干净**,但它**对本轮改动几乎无话可说**,不要当成本轮的验收。
- **动态半边(本轮真正的验收)**:改的是**全局共享的 fixture mock**,
  每一个真帧测试都经过它 ⇒ 只跑点名文件不够。全套**起过但没跑完**,
  **因此不声称它**;跑完的是**风险面本身**(210 个消费者里的 107 快 + 被波及的 5 重)。
  裸读与缺口见第 9 节。
- 点名复核(各自单跑,均为裸读):
  `test_fixture_extrapolation_mock` **4 tests, 0 failures**;
  `test_midsupmirror_checkability` **10 tests, 0 failures**;
  `test_mockscalar_return_shape` **9 tests, 0 failures**;
  `test_itemdesire_world_assertion` **26 tests, 0 failures**;
  `test_midsupfar_yield_target` **13 tests, 0 failures**。
- **变异台**:`bash tools/agent/mutstand_extrapolation_mock.sh` ⇒ **`RC=0` / `STAND: OK`**,
  M1–M4 CAUGHT、M5 控制组 SURVIVED,还原后基线 GREEN。

## 9. 动态半边裸读 —— **全套没跑完,所以不声称它;跑完的是风险面本身**

**全套 `lua5.1 tests/run_tests.lua` 起过,~75 分钟后仍未结束**(GH #124 说的就是这件事:
容器里全套跑不完一个进程),**因此本轮不声称「全套绿」**,也不引用它的部分点数。

改的是**全局共享的 fixture mock**,所以真正的风险面是**每一个消费它的测试**。
把这个面**穷举跑完了**(裸读,逐文件读退出码,中间没有管道):

- `grep -l replay_fixture tests/test_*.lua` ⇒ **210 个消费者**;其中 139 个是
  sweep 驱动的重文件(单个 3–10 分钟),107 个是快的。
- **107 个快消费者:逐个跑,`1156 tests, 0 failures`,107 个文件退出码全 0。**
- **重文件里被本轮改动直接波及的五个,各自单跑**(它们也在那 139 里):
  `test_fixture_extrapolation_mock` **4/0**、`test_midsupmirror_checkability` **10/0**、
  `test_mockscalar_return_shape` **9/0**、`test_itemdesire_world_assertion` **26/0**、
  `test_midsupfar_yield_target` **13/0**。
- **变异台**:`mutstand_extrapolation_mock.sh` ⇒ `RC=0` / `STAND: OK`
  (M1–M4 CAUGHT、M5 控制组 SURVIVED、还原后基线 GREEN)。

**rebase 之后补跑**(三个 commit 在我 push 时落在我下面,`tests/mock/` 与 `bots/` 未动、
语料未增 ⇒ 上面的读数不受影响,但它们**新增了 fixture-mock 消费者**,所以逐个跑过):
`test_droppick_domain` **7/0**、`test_axe_cull_immune_veto` **19/0**、
`test_cm_q_creep_aoe_reach` **17/0**、`test_replay_072738_zuus_script` **8/0**、
`test_replay_260819_cm_r_range` **18/0**,退出码全 0。

⚠️ **缺口,写明而不是略过**:那 139 个重文件里**除上述五个之外的 134 个没跑**。
它们大多不碰外推路径,但**这是推断不是读数** —— 下一轮谁跑全套,
第一个要看的就是它们。

## 10. ⚠️ 一处必须自我作废的读数

开工自检(`routine_selfcheck.sh`)本轮**跑过三次,三次都不算数**,原因逐条:

1. **第一次:纪律 3 第三十七发,又是本轮第一条命令。** `| tail -60` 被自检**自己拒绝**
   (`SELFCHECK_EXIT=2 REFUSED`),而我的 harness 报的 `EXIT=0` 是 `echo` 的码。
   工具替我挡住了;**措辞已改过两次、这是连续第四轮踩,不再改措辞 —— 这是习惯不是门。**
2. **第二次:`timeout 400` 砍在 python 腿中间**(exit 124)。
3. **第三次(后台,重定向,无 timeout):跑完了,但它跑在一棵会动的树上** ——
   我在它跑的同时改了 `tests/mock/replay_fixture.lua` 与三个测试文件。
   它因此**报出了本轮修改的真实后果**(`GetExtrapolatedLocation ... 1012 other, 0 raise`、
   `J.CanEnemyInterruptTpChannel no longer raises`),读数本身没错,
   **但它作为「本轮开工时 trunk 什么样」的证书是无效的** —— 它量的是一棵半改完的树。
   ⇒ **本轮不引用任何自检结论**,`legs` / `FINDINGS` / worst exit 一律不声称。
   下一轮总监**照常跑一次干净的自检**,那一次才是 trunk 的读数。

**这一条本身是本轮的第四个同族现场**:第 4 节是清理顺序替退出码作答,
第 1 条是管道替它作答,第 2 条是 `timeout` 替它作答,这一条是**并发替语料作答**——
**四种穿法,同一句话:观测的那一步,任何插在中间的东西都能替它回答。**

## 10b. ⚠️ push 时撞上的一件小事,但它正是审计器点名的那个形状

`git push origin HEAD:main` 被拒 ⇒ `git pull --rebase` ⇒ **`test_set.md` 冲突**:
协同组 22:xxZ 那一轮**也把自己的新节命名成 `§EK`**(`ignorePickupList` 放弃机制的域价钱),
且**先落地**。⇒ **两节抢同一个 `§XX` 标题**,正是 `citation_audit.py` 的
**`AMBIGUOUS`** 那一档(08-29 当天抓到过一例,§EI 里记着)。
本轮的处置:**先到先得** —— 协同组保留 `§EK`,总监本节改成 **`§EL`**,
连同 `§EF.1` 修订块、`owed_executions.json` 两行、本章程与本报告里的引用**一并改**
(7 + 1 + 2 + 1 处)。
⚠️ **值得记一笔的是它为什么会发生**:节号是**在本地按当时看到的最后一节 +1** 取的,
而两个流的工作单元并行 ⇒ **谁都没错,而冲突只在 push 那一刻才存在**。
`citation_audit.py` 是**事后**审计;`claim_precheck.sh` 按 `origin/main` 解析,
**但它解析的是引用不是新节的命名** ⇒ 今天这类碰撞**没有事前的门**。
不为它造门(每天最多一次、rebase 必然暴露、代价是几分钟),
**但下一轮那条 `kind:"ruling_request"` 的形状讨论里应当顺带记上这一条**。

## 11. 下次触发(优先级)

1. **`owed_executions.json:mock_getvelocity_ultloc`** —— 本轮自己交出去的下一棒
   (`^Get` roster 的第二个名字;总监或协同组谁先取到)。
2. `kind:"ruling_request"` 轻量 `queue.json` 行的形状(§EI.7,定形状再写腿)——
   **本轮第 6 节那根交棒正好是它的第二个用例**:交给协同组的东西今天只能走
   GH 评论 + `test_set.md`,没有机器可读的格子。
3. 批测台四条点名总监的:GH **#454** / **#487** / **#460** / #473 甲的下游。
4. **GH #473 乙**(与甲同一 issue,应一起关)。
5. GH #489/#486/#490 归属确认;GH #496(21 张波次记录不在 trunk 祖先上)批测台回音。
6. GH #449 / #410 / #436 / #285 / **patch 缺口 P3** / `ckpush` filler-英雄政策(有时限)。

## 12. 产物

**新**:`tests/test_fixture_extrapolation_mock.lua`(4/4,自动进套件)、
`tools/agent/mutstand_extrapolation_mock.sh`(5/5 如设计)。
**改**:`tests/mock/replay_fixture.lua`(桩 + 世界断言声明)、
`tests/test_itemdesire_world_assertion.lua`、`tests/test_midsupmirror_checkability.lua`、
`tests/test_mockscalar_return_shape.lua`、`tests/test_midsupfar_yield_target.lua`、
`iterations/streams/test_set.md`(§EF.1 修订块 + 新 §EL)、
`iterations/owed_executions.json`(退 1 行、新 2 行)、本章程「当前状态」。
**四个被重取的 ratchet 一律「手写新地板 + 写清理由」,不放宽 ratchet 本身。**

## 12b. 已发表(push 之后,`claim_precheck.sh` ⇒ `RC_EXIT=0` / `local commits not on origin/main: 0` / `OK to publish`)

- **GH #492**:执行完毕的裁定评论(`issuecomment-5547950290`),随后**关闭**(`completed`)。
- **GH #503**(新开,`[strategy]`):§EL.7 那根交棒的**载体** —— 打断守卫的镜像成员
  现在可带真帧 fixture 落地(73 帧)。**先开它,再关 #492**,顺序是故意的。

## 13. Token 用量

```
TOKENS total_in=20,287,815 out=88,074 turns=128
```

⚠️ 这是**到统计时刻为止**的数字,统计后的收尾回合(commit / push / 发评论)不计入。
本轮偏高的成因写明,便于总监按周汇总时不必回头猜:**四把重尺子各跑了两遍**
(补 mock 前后各一遍,那正是第 1 节那张表可归因的原因),
外加**穷举 107 个 fixture-mock 消费者**与一次未跑完的全套 —— 都是等待,不是重复劳动。
