# 总监工作单元报告 — 2026-08-19T03:00:19Z

## 0. 本轮范围

分支已同步 origin/main(HEAD 976926b,无落后)。读了本轮四组新报告
(batch-desk 020919Z、replay-check 004401Z、strategy 011551Z、hero 015121Z)
+ 扫 open issue(19 条,2 条新:`[harness]` #25、`[strategy]` #24)。按优先级
处理了 (a) `[harness]` issue 和 (c) test_set 变更审批;(b) promote/reject
本轮仍不判定(理由见下);(d) 成本无需行动;(e) 组健康良好;patch 检查
(backlog #0)本轮未做(工作量大,不适合顺带,继续排最高优先级)。

## 1. `[harness]` issue #25 — 已修复并关闭

录像组吞吐报告里点出的两个瓶颈:

1. **dumper 每会话重建**(~10-20 分钟浪费):新增
   `tools/batch_test/behavioral/get_dumper.sh`——按 dumper 源码 + manta 补丁
   配方的哈希做 S3 缓存 key,命中直接拉二进制(实测 ≈2.6s),未命中才本地
   构建(实测 ≈30-34s,容器已装 Go)并回传缓存供下次命中。构建/拉取/回传
   三条路径都在真实 S3 桶(`dota2bot-batch-results-4924`)上跑通验证。
2. **缺批量宽扫脚本**:新增 `tools/batch_test/behavioral/sweep_run.sh`——
   一条命令扫一整个 soak run 的 S3 前缀,自动跳过暖场局(`analysis.json`
   的 `script_version` 不带 `mirror:` 前缀,复用录像组本轮报告里发现的
   同一判据),逐局跑 dumper+detect.py,产出 `sweep_summary.md`(每检测器
   触发计数,按 mirror 戳拆分 candidate侧/baseline侧命中数)。

**真实数据验证**:对 `soak/spot_20260819_001001_1_main/`(录像组本轮刚看过
的那个 run)跑 `sweep_run.sh`,25 秒处理 5 个 `.dem`——正确跳过录像组手工
发现的同一个暖场局(`96f49dc` 戳),对剩下 4 局跑出 15 个检测器 x
candidate/baseline 拆分的完整计数表。两条脚本行为与录像组报告的手工发现
完全对上,不是纸上谈兵。

已更新 `tools/batch_test/behavioral/README.md`(新增"fresh container 怎么跑"
一节)、`.claude/agents/replay-analyst.md`(工具链条目指向新脚本)、
`iterations/streams/replay-check.md`(吞吐要求节标注工具已就绪 + 说明这
只是宽扫起点、不是逐 armed-id 检测器,后者仍是 backlog #3)。issue #25
已评论详情并关闭。

**未做**(超出本次工作单元范围,已在 README/replay-check.md 里标注为
backlog #3 的地基,不是本次要交付的):按每个 test_set id 做专用触发检测器
——`sweep_run.sh` 目前只是复用 `detect.py` 现有的 15 个通用行为检测器
(TP/超前/漏杀等),不认 armed id 本身。

## 2. `[strategy]` #24 — test_set.md 变更审批:批准

协同组本轮把 `l1xpsoak` mechanism note 里明确留白的两项(绝对锚 + 退出滞回)
补齐,fixture 验证 13/13(`tests/test_l1_xpsoak.lua`,用真实帧
`f_231411_ck_zoned.lua`)+ 全套 359/359,luacheck 0 警告。这满足验证哲学
条件 (c)(逻辑依据,mechanism note 明确要求这两项)和本地 fixture 级验证
(iteration workflow 第4步),批准重新入集。

**已改** `iterations/streams/test_set.md`:加入 `l1xpsoak`(现 15-id),
留历史行,并加了一条"总监提醒":鉴于 (i) `l1xpsoak` 历史三次被拒
(-61、-49.4×2)且 owner 明确要求这是 MUST-DO 而非悄悄埋掉,(ii) 当前
14-id 组合本身正显示可疑的一致负向信号(见下节),**建议下一波把
`l1xpsoak` 单独测或至少与现有 14-id 分开**(07-31 A1/A2 bisect 的先例),
避免两个待判问题互相污染归因。这是给 batch-desk 排期用的建议,不是
强制阻塞——test_set.md 本身已经批准入集。issue #24 已评论说明,留开
(还要等条件 (a)(b) 才能真正 promote)。

## 3. promote/reject 判定:本轮仍 HOLD

14-id 全集(`creeppull,pullcamp,l1trade,l5combo,midtp,suptp,tpcommit,
lf_rescue,teambrain,ownhalf,overchase,fieldregen,wandbleed,tpwatch`)vs
稳定版的镜像批测:3 个种子(851/853/854)已收割,gpm 均值 **-27.08**,
0/3 全指标同向(候选更差)。种子 852(commit ce5c3d2 = 当前 tree)02:09 UTC
启动,预计 03:39-04:09 UTC 完成,本轮触发时(02:58 UTC)尚未完成
(`describe-instances` 查无该 tag 的实例——可能已完成自毁,也可能还没到
稳定状态窗口;没有强行等待,遵守"工作单元要小,不空转")。

**继续 HOLD,不做 promote/reject**:
- 距离 4-seed 判定门槛只差这一个种子,凑三个种子拼凑判断违反可重复性纪律。
- 但记一笔观察:这个 -27 gpm、0/3 同向的形状,与 07-31 记录的 12-id
  bundle(-65)、14-id 早期版本(-33)的持续负面残差同型——如果种子 852
  落地后依然保持这个方向,大概率是又一次 bundle REJECT(参考
  `iterations/state.json` 里 `wave12_VERDICT`/`family_bisect_VERDICT` 的
  历史模式)。下次触发(不管是总监还是录像组/协同组)优先看这个 verdict,
  如果 4-seed 确认负面,按章程走 reject 出集流程,并参考历史经验做归因
  (behavioral diff,而非单一经济读数)。

对个体 armed id 的核验(录像组本轮的 midtp/suptp WORKING、tpcommit
circumstantial WORKING)保持有效——这是执行正确性的证据,和"bundle 整体
经济是否为负"是两个独立的问题(旧的 lanefix 教训:单个修复逻辑正确不代表
bundle 聚合效果好)。

## 4. hero 组报告(`wkreincarnmp`):无需本轮行动

已验证条件 (a)(真实帧)和 (c)(逻辑依据),条件 (b) 待批测——该 id 目前
不在 test_set.md 里也不在本轮任何波次里。hero 组自己在报告里请求"下次总监
触发把 wkreincarnmp 作为 test_set.md 新提议一并考虑纳入下一轮波次"。

**本轮未加入**:test_set.md 目前已经是 15-id(刚加了 l1xpsoak),而且
14-id 主集合本身正处于可疑负面信号的判定关口(见上节)。在拿到 850 系列
4-seed 的最终 verdict、决定这个 bundle 到底 promote/reject/reject-and-
re-narrow 之前,再往里加第 16 个未经批测验证的 id 只会让下一波的归因更难
——不做与当前"先弄清 bundle 现状"冲突的扩张。下次总监触发(bundle 判定
落地后)再考虑 `wkreincarnmp` 入集,可能和 `l1xpsoak` 的单独测一起排期
(两个都是"个体验证已完成、等批测"的独立候选,适合各自单独或小批量测,
不适合都塞进主 bundle)。

## 5. 成本

MTD 仍是 batch-desk 报告的 **$3.45**(远低于 $90 刹车线),无需行动,
无需邮件。本轮总监自己只做了 S3 GET/PUT(dumper 缓存拉取/回传 + sweep
读 .dem/.analysis.json)和只读 EC2 查询,没有启动任何计费实例,符合
"总监不直接花 AWS 钱"。

## 6. Routine 体系健康

四组(batch-desk / replay-check / strategy / hero)本轮均有产出,报告质量
合格(真实帧证据、局数覆盖数字、验证哲学三条件对照齐全)。上轮标记的
"strategy 组无产出"观察项已解除——已产出 011551Z 报告。无组连续多轮空转。

## 7. DECISIONS_NEEDED / 邮件

本轮无需 owner 决策的新问题(test_set 审批、harness 修复均在总监授权范围内
自行处理)。`iterations/DECISIONS_NEEDED.md` 仍未创建,本周暂无需要攒的
待决问题。

## 8. Patch 检查(backlog #0)

本轮未做——工作量大不适合顺带,继续留作最高优先级基建项,建议下次专门
分配一个会话处理。

## 9. 涉及路径

- `tools/batch_test/behavioral/get_dumper.sh`(新增)
- `tools/batch_test/behavioral/sweep_run.sh`(新增)
- `tools/batch_test/behavioral/README.md`(新增使用说明)
- `.claude/agents/replay-analyst.md`(工具链条目更新,修了编号)
- `iterations/streams/replay-check.md`(吞吐要求节补充工具就绪状态)
- `iterations/streams/test_set.md`(加入 `l1xpsoak`,15-id;加了排期建议)
- `.gitignore`(忽略 `.dumper_cache/`、`.sweep_out/` 本地缓存目录)
- `iterations/streams/director.md`("当前状态"节本次更新)
- GitHub issue #25(已关闭)、#24(已评论,留开)

## 10. Token 用量

TOKENS total_in=11,219,613 out=76,551 turns=102
