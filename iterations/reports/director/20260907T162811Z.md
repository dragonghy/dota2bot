# 总监 2026-09-07T16:28Z —— 判定完结 2(`teambrain` + `capmono` 退回出集,armed 50 → 48)

**产出指标(owner P4.2):判定完结 2。** 上一轮 0、上上轮 1、再上一轮 0 —— **连续四轮低于 ≥2 之后第一轮达标,不粉饰:达标的是数量,而两条都是退集,没有一条 promote。**
零 AWS、零波次、零 S3、零 EC2;**不发 owner 邮件**(本轮没有需要 owner 拍板的事)。

本轮三件事,按先后:
1. **§1 起手补上排序键本身** —— 上一轮交出的那根棒(机器可读的入集时刻)。
2. **§2 结清一条先于本轮存在的 trunk 红** —— 它比看上去重:一条 armed id 的唯一调用点被覆盖掉了。
3. **§3 两条判定** —— 用 §1 排出来的序挑的。

---

## §1 先补量具:上一轮那个「不知道」是**容器**的性质,不是实验室的性质

**交棒原文(13:31Z)**:给每个 armed id 机器可读的入集时刻,来源是**入集章节**,不是左删截的 git 历史。

立这根棒的现场是 10:00Z 那轮:P4.2 说「从核验记录最少的 id 清起」,而「armed 了多久」是那个排序键的另一半。当时的答法是 `git log iterations/streams/test_set.md` —— **Routine 容器是 shallow clone**,`--deepen=400` 之后该文件的历史仍然只到 **08-30**,于是 **51 个 id 里 40 个答「不知道」**。⭐ 那个「不知道」**是 clone 的性质,不是仓库的性质**;同一形状在 §FQ.2 的 `arm_md5` 反查上出现过一次(不 deepen 只解得出 6 个 md5,十波里八波 UNRESOLVED)。

**落地物**:`iterations/armed_since.json`(50 行)+ `tools/agent/arm_since.py` + `tests/test_arm_since.py`。
**三个来源,全部是仓库里全长发行的东西,一个都不问 git**:

| 来源 | 是什么 | 覆盖 |
|---|---|---|
| A | `test_set.md` 的入集章节(交棒点名的那个,权威) | 20 |
| B | `iterations/reports/director/<UTC>.md` 的**文件名**(时刻在文件名上,散文被重写也不会漂;存档起点 2026-08-19T00:53Z) | 11 |
| C | `state.json` 的 `<id>_<YYYYMMDD>` 键(最弱,只给 A/B 看不见的 id 用) | 10 |
| 手钉下界 | 早于 B 存档起点的 9 条(七月那个 bundle) | 9 |

**覆盖 50/50,`RC_EXIT=0`。** 下界的证据是**逐字引用了当天 arm 串**的 `state.json:family_bisect_launch`(8 条,2026-07-25:FamilyB = `midtp,suptp,tpcommit,lf_rescue,teambrain,ownhalf,overchase,fieldregen`)与 `bundle14_VERDICT_20260819`(`wandbleed`)。
⚠️ **下界是地板不是等式**(GH #106 家规)—— 而它们无论如何都排在最老那一端,所以对 P4.2 的排序**够用**。

⭐ **解析器匹配的是形式不是关键词,而这一条是量出来的不是设计出来的。** 第一版匹配任何含「入集」的行并收走该行所有反引号 id,在真文件上把 §DK.3 的「**`slotarb` 的入集是条件性的**」读成一次 `slotarb` 入集事件,日期取自恰好在它上面的那一节。**一个错的日期比没有日期更坏** —— 它是 P4.2 的排序键,假老把 id 顶到退集队首,假新把它藏起来。

`tests/test_arm_since.py`(**18 checks, 0 failed**)钉住四种真实散文形式为阴性(`不提入集` / `重新入集路径` / `的入集是条件性的` / `X 退集`),外加三条失效方向:

- **armed 但没有行 ⇒ exit 3**(不是打一个「-」然后 exit 0 —— 那是 `pending_rulings.py` 的 `none` 形状:一个空答案长得像一个答案);
- **有行但不在串里且没盖 `retired_at` ⇒ exit 3**(退集时**删行**会通过其它每一条检查,并悄悄销毁「上一段 armed 了多久」);
- **散文与 `exact` 行不一致 ⇒ 报 CONTRADICTION 且不自动改**(「存档被重写了」和「钉子写错了」从工具内部看长得一样,只有人能分)。

**最老的一档(本轮两条退集就从这里挑)**:

```
l1trade / l5combo / tpcommit / lf_rescue / teambrain / ownhalf / overchase / fieldregen   2026-07-25  ≥44 天  lower_bound
wandbleed 2026-08-19 ≥19 天 | capmono / cmrguard / tpdead / zusult / wandlimbo 2026-08-19 19 天 exact
```

---

## §2 顺带结清一条先于本轮存在的 trunk 红 —— 而它比看上去重

开工自检(铁律 10)`RC_EXIT=3`,**4 条 Lua 检测器红**,全部指向 `bots/mode_retreat_generic.lua`。成因是 **`8b25217e`(协同组 09-07T13:40Z,`pgchannel`)**:它把撤退链里的

```lua
if J.ShouldRegenNotWalkHome(bot) then     -- stayfield2 的唯一消费点
```

**整行替换**成 `if J.ShouldLetTpChannelFinish(bot) then`,**把上面那段 `stayfield2` 的注释原样留着**(描述一个已经不存在的调用)。⇒ `J.ShouldRegenNotWalkHome` 在 `bots/` 里的调用点 **1 → 0**,而 **`stayfield2` 当时在 armed 串里**。

⛔ **出厂行为一个字都没变**(两个 helper 在各自 id 未 armed 时都答 false)⇒ **真实对局零影响,这不是一条行为回归。**
**死掉的是测量**:从那棵树发出的每一波都会把 `stayfield2` 读成 no-op,而在 verdict 表里那长得就像**「测过了,无效应」**(§AZ / GH #148 那一族的又一发,这次的载体是**调用点**,不是**域**)。

**处置**:`stayfield2` 那一行**按原位恢复**,`pgchannel` 的否决**平排在它下面** —— 照协同组自己落地说明里的那句 *"a VETO above the chain, **beside** the other two"*。源码里同时写下**两条否决的归因边界**:两条都返回同一个 `NONE`,所以**单臂 armed 时顺序不可能有影响**;**两条同时 armed 时同满足的那一帧归给排在前面的那条,不可归因**。

### §2.1 ⭐ 立案句:发波前那道门对此**没有举手**

```
$ python3 tools/batch_test/check_armed_wiring.py --ref bcea31e --cand stayfield,stayfield2,fieldbuy
stayfield2       direct   1     bots/FunLib/jmz_func.lua:5831
all 3 armed ids wired on bcea31e                                    RC_EXIT=0
```

`bcea31e` **就是那棵坏树**。工具答 **WIRED, 1 site** —— 它找的是 **helper 体内那一行 `IsSoakCandidate` 闸址**,不是**这个 helper 有没有人调用**。⇒ **「id 有闸」与「id 可达」是两个谓词,今天只有前者有门。** 立案 **GH #606**([harness],附两个方向的验收:坏树上必须读 `UNREACHABLE`,好树上 48/48 WIRED)。

这与 `test_gated_helper_nesting_census.lua` 自己写下的那句警告(*"...check_armed_wiring.py will still call it WIRED"*)是**同一个洞的另一种长相**:那句说的是被合取项吃掉,这一发是**连调用点都没有**。

### §2.2 第 4 条红**没修,而那是有意的**

`test_gated_helper_nesting_census.lua` 要的是 `pgchannel` 带来的两条普查行**被回答之后再钉**,而回答的形状(单臂可读性)是协同组自己那把尺子 `tests/_pgchannel_sweep.lua` 的活。**钉错一行比留着红更坏** ⇒ 整条交回,**GH #607**,并附上总监从源码读到的一半、明写**未经驱动证实**:

- **第一行**(外层 `c12/retnear/towerreach`,内层 `J.ShouldLetTpChannelFinish` 挂 `pgchannel`):**恒等性成立** —— `pgchannel` 未 armed 时该 helper 答 false,否决不开火,链逐字如出厂。
- **第二行**(外层 `pgchannel`,内层 `J.ShouldAbandonTpChannel` 挂 `tpwatch`):**不是那个形状** —— 它是 GH #576 的对偶问题,`tpwatch` 未 armed 时那条**释放**够不到,单臂 armed `pgchannel` 测得到否决本身、**测不到那条释放**。

### §2.3 顺手:「文本裁判把自己的注释读成代码」**第四发**

`tests/test_pgchannel_veto.lua:147` 用 `gsub` 数调用点而**不剥注释**,于是我写下的那段说明(它引了那句调用的**字面文本**)把**代码里恰好一个调用点的树读成两个**并变红。
**修法不是改我的措辞**(那只让下一发再来一次),是**让计数读注释剥离后的源码** —— 调用点是**代码**。
⚠️ **失效方向本来就是坏的那一侧**:它会因一句注释而红,却数得到藏在 `--[[ ]]` 里的**真**第二调用点。
变异一发(文件拷贝还原台):**真的加第二个调用点 ⇒ CAUGHT**;`pgchannel` 复跑 **7 tests, 0 failures**。

---

## §3 两条判定(armed 50 → 48)

**新串 48 id,433 字节,md5 `51eb44347f25676ec5bf767383eed244`。** 裁定全文 `test_set.md` §FV。

### §3.1 `teambrain` 退集(50 → 49)—— **`zusstatic` 那一型:条件 (a) 结构上买不到**

armed **≥ 44 天**(全集最老的一档),`verify_coverage.py` 读 **verify=0 / 178 份报告**。
依据是**协同组自己 2026-08-23 的审计**(`state.json:tpclaim_20260823.audit_verdict_teambrain`),逐字:

> the FINAL ITEM DESIRE IS **UNBUYABLE** from this corpus, and the reason is **structural, not a corpus gap** … So `teambrain` has been in the armed set with **NO evidence that it ever moves a bid**.

唯一调用者坐在 `J.IsDefending -> bot:GetActiveMode()` 后面 —— **第十三条世界断言**(语料里每个英雄每一帧恒 0,`tests/test_activemode_world_assertion.lua`);落点 `X.GetDefendTPLocation` = `GetLaneFrontLocation`,**GH #61 已拒**。
⇒ **那句结论写于 08-23,而这条 id 之后又 armed 了 15 天。**

⛔ **本裁定不说的那半(§FV.4 第一条)**:2026-07-25 `wave_teambrain_VERDICT` 里那批仲裁目标读数(TP 落地死亡 4.8x → 1.83x、TP 量 +41% → +19%、同帧三连 TP 消失)**看起来像 (a)**,而它们全部是 wave12(12-id)与 13-id 波的**跨波差** ⇒ 正是 `state.json:residual_IS_A_CONSTANT_RULING_20260821T1700Z` 明文废止的那种读法。**我没有推翻那批读数,也没有为它重新买到任何 (a)** —— 我说的只是:按今天的记账规矩,它们不能当 (a) 引。

### §3.2 `capmono` 退集(49 → 48)—— **(a) 买到了,答案是 FAILED**

armed **19 天**(exact),verify=0。`state.json:capmono_NOT_PROMOTED_20260820`:

- **(a) FAILED under isolation**:32 局镜像 / 806 帧,DiD **−7.9pp ± 9.3**,|t| 0.84,每粒符号 2/4;当初那条 `+16.6pp WORKING` **被它自己的作者与当时的总监双双撤回**。
- **(b) UNJUDGEABLE**:四个经济量全空,而空的 A-B 只是**上界**,永不是 (b) 通过。

### §3.3 ⭐⭐⭐ 本节最该被读的一条:**一个「等着被免费买到」的条件,和一个没人买的条件,在 verdict 表里长得一模一样**

08-20 的裁定明写 `stays armed`,理由 (ii) 是:

> 每一波 capmono-ON 的波都在**免费**为那次 within-arm HP 梯度再读积攒 n。

**那个论证是对的**(波内梯度对 ±15pp 的跨种子噪声免疫),**而它有一个没写下来的到期日**。**19 天、十几波之后,那次再读一次都没做过**(`next_step_zero_cost` 零执行,`verify=0`)。

⇒ **本轮裁定的不是「capmono 没通过」,是「让它继续 armed 的那条理由已经过期,而没有任何东西替它举手」。**
一个 id 停在「等一个免费读数」上和停在「没人管」上,在 `verify_coverage.py` 的表里**同一行、同一个 0**;差别**只在一句谁也没读的散文里**。这与 §FB(`tpdying`/`tpreach`:验收形状在入集当天就写好了,而那份义务只住在散文里)是**同一个缺陷的第二种长相**。

⇒ 两条 (a) 义务**从散文搬进 `iterations/owed_executions.json`**(自检第 9 条腿每轮替它举手并 exit 3),**再**退集:
`capmono_hp_gradient_reread`(录像组;**零 AWS、零新局** —— 用的是已经存在的 32 局语料;事前登记的**不对称性**照抄 08-20:梯度出现能平反,梯度缺席**不得**读成「无效应」)、`teambrain_bid_reachability`(`claimable: false`,一条**否决权**不是一件活:重新入集的前置条件是**仪器先变**)。

### §3.4 两条裁定的公共检查

- ⛔ **都不是 reject**:gate 与代码**逐字保留**,`bots/` 对这两条**零 diff**(本轮唯一的 `bots/` 改动是 §2 恢复的三行 + 注释)。
- ⚠️ **`pullcad` 陷阱查过了**:各自**只有一个** gate 点(`jmz_func.lua:7647` / `mode_team_roam_generic.lua:128`),门行上没有第二个 id;`promote_atoms.py` **exit 0**,四行没有一行点名这两个。
- ⚠️ **`tpclaim` 是唯一挂在 `teambrain` 上的东西**(源码:only reachable with 'teambrain' armed),而它**本来就不在 armed 串里** ⇒ **本次退集没有冻死任何一个 armed 杠杆**;将来 `tpclaim` 提入集必须先带 `teambrain` 回集(已登记)。
- ⚠️ **载体项 7 → 7 逐字不变,量出来的**:`carrier_terms.py` 对 50-id 与 48-id 两串各跑一次,`TERMS` 行**逐字节相同**,`0 unresolved` 两次;计数 `10 hero / 40 generic` → `10 / 38`(两条都是 generic)⇒ **选种解空间不受影响**。
- `check_armed_wiring.py --cand <48 串>`:**all 48 armed ids wired on HEAD**,`RC_EXIT=0`。
- `arm_since.py`:**48 pinned / 48 armed**,两条已盖 `retired_at`,`RC_EXIT=0`。
- ⛔ **W53(51-id)及更早不与 48-id 家族并池。**

---

## §4 铁律 6 的读数(逐条,裸退出码)

| 门 | 命令 | 读数 |
|---|---|---|
| 静态半 | `bash tools/agent/luacheck_gate.sh` | `installed luacheck (bounded)` → **`luacheck bots game: 0 warnings`** → **`GATE_EXIT=0 CLEAN`** |
| 动态:改动文件族 | `lua5.1 tests/run_tests.lua stayfield` | **56 tests, 0 failures**(此前 **3 红**) |
| 动态 | `… pgchannel` | **7 tests, 0 failures**(此前 1 红) |
| 动态 | `… retreat` | **33 tests, 0 failures** |
| 动态 | `… tp_` | **69 tests, 0 failures** |
| 动态 | `… gate_claim` | **16 tests, 0 failures** |
| 动态 | `… smoke` | **3 tests, 0 failures** |
| 动态:全部 py | `bash tests/run_py_tests.sh` | **111 passed, 0 failed, 1 uncertifiable**(含新增 `PASS tests/test_arm_since.py`) |

**未用 `RULE6_BYPASS`。**
⚠️ **动态半边不声称全套**(GH #124):跑的是「改动文件 + 反向依赖族 + 全部 `tests/test_*.py`」,**不是** `run_tests.lua` 全量。
⚠️ **一条 UNCERTIFIABLE 照实登记**:`tests/test_selfcheck_lua_leg.py` 撞自己的 120s 预算没跑成 —— **不是红也不是通过**,与本轮 diff 无因果(开工自检那一轮也是它)。
⚠️ 自检本身 `RC_EXIT=3`(4 条 trunk 红),其中 3 条本轮修掉、第 4 条按 §2.2 交回协同组。

---

## §5 各组健康度与成本

- **成本**:本轮**零 AWS 调用**(总监不花钱)。MTD 读数以批测台 15:14Z 报告为准,本轮未重取(取一次 CE 读数要 $0.01,而本轮没有任何决定依赖它)。`DECISIONS_NEEDED.md` 里那条「预算被别的项目的实例量进来,请 owner 给 `dota2bot-batch` 加 `Project` 过滤器」**仍然待办且仍然是 owner 侧**(我们没有 `budgets:Modify*`)—— **本轮不为它单独发信**(每周 1 封的额度留给下一封汇总)。
- **各组节奏**:batch-desk(12:08Z / 15:14Z)、hero(07:58 / 10:49 / 14:09Z)、strategy(07:30 / 10:40 / 13:40Z)本轮都有产出,无掉队组。
- **⚠️ 一条要盯的**:协同组 13:40Z 那次落地**红了四条检测器并让一条 armed id 变哑**,而它自己的定向过滤器(`retreat`/`tp_`/`pgchannel`/`gate_claim`/`push`,匹配文件名)**一条 `test_stayfield_*.lua` 都不命中**。这不是「下次小心」——可操作的形状写进了 GH #607:**改一行 `if <helper>(bot) then` 之前,先问这一行现在挂着谁**;而门那一侧是 GH #606。
- **owner 优先项**:P4.2 本轮**推进了**(armed 50 → 48,判定完结 2,达到 ≥2 的产出指标);P4.3(`test_set.md` <50KB)**未动**,当前 **144K**;P1/P2/P4.1 球都不在总监手上,本轮无 12 轮零推进的红色升级。

---

## §6 交棒

- **甲 → 协同组**:GH #607(两条 gate-inside-gate 普查行,trunk 仍红;附总监从源码读到的一半,**明写未经驱动证实**)。
- **乙 → 总监(下一轮或任何有余量的轮次)**:GH #606(`check_armed_wiring.py` 加**可达性**谓词,验收两个方向)。
- **丙 → 录像组**:`owed_executions.json:capmono_hp_gradient_reread`(**零成本**,语料已存在)。
- **丁 → 常驻约束**:`owed_executions.json:teambrain_bid_reachability`(`claimable: false`,在有人提 `teambrain`/`tpclaim` 入集的那天咬人)。
- **下一轮总监第一件事:判定,而且是判定**(除非又出现先于该轮存在、点名总监的 trunk 红)。现成的线索:`arm_since.py --all` 最老那一档里**还有 7 条 `lower_bound`(≥44 天)且 `verify=0`**,与本轮两条同型。

---

## §7 token 用量

```
TOKENS total_in=13,883,567 out=73,148 turns=86
```
(统计时刻为收尾前;此后的 commit/push 回合不计入。)
