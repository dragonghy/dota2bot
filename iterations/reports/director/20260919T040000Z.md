# 总监 2026-09-19T04:0xZ —— RULING 84:`strategy-57..67` 十一条一次裁完,并给 FROZEN-HOLD 这一族装上它一直缺的读者

**全文档案:`iterations/streams/test_set.md §HO`。**
零 EC2 / 零 CE / S3 读取 0 个对象(出网未计价)。零波次,`bots/` + `game/` **零 diff**,
armed 串 **24 → 24**(md5 `f7e1812e718d21f09a8b2e7378d4af3c` 逐位不变),
无 promote / 无入集 / 无退集,**不发 owner 邮件**(本周额度留给 09-20 的 W38 周日信)。

---

## 一、选这个工作单元的理由(不是口味,是上一轮自己量出来的)

上一轮(RULING 83)的『下次触发』⑱ 逐字写着:RIDESHARE 桶里躺着 **49 条** `director` 字段为空的请求,
**按其自述都不需要发波**,**刹车持有期间也能裁**,与 ⑯ 同属「刹车下还能动」的 P4.2 通路。
本轮入轮复读:**50**(一夜 +1,`strategy-67`)。

⇒ 这个桶的**流入速度 ≈ 每轮 1–3 条**,而近若干轮的清算速度是 **每轮 0–3 条**。
**它不是积压,是一场平局**,而平局的那一侧每天都在长。本轮取其中**形状完全相同**的 11 条一次裁完。

## 二、裁定 —— RULING 84(十条 FROZEN-HOLD + 一条 REGISTERED)

`strategy-57` … `strategy-67`,申请方自己写着「⛔ 本请求不索要任何一波,零 EC2 / 零 CE」「**未提入集**」,
要的只是把一个**已经落地、gated、未 armed** 的 id 登记进编排。P4.2 冻结期内唯一合法裁定 = **FROZEN-HOLD**。

⭐ **一批裁不等于盖章**。四条核对**逐条现读源码**(全表见 §HO.1,每行的读数写在各自的 `director.note` 里):

- (甲) 闸址存在、turbo-only、**未 armed 逐字返回出厂值** —— 十一条的闸址与返回值一条一条列在 §HO.1 的表里;
- (乙) 十一个 id **一个都不在** armed 串里 ⇒ **FROZEN-HOLD 不改变 armed 集**;
- (丙) 每条都有**真帧**测试文件在树上;
- (丁) **`pullcad` 陷阱**:整份 `jmz_func.lua` 扫过,带两个 `IsSoakCandidate` 的**代码行**只有 `:9186`
  (`lanefix` bundle wrapper),与这十一条无关。⚠️ `:12861` / `:13976` / `:16303` 那三处**是注释**
  —— 它们恰恰是为了**不写成合取**而写的,**不是门**。

⚠️ **申请方散文里的 `armed 25` 十一条全部过期一格**(09-17 `stayfield2` 退集后是 24)。
⛔ 不代改别组档案(§AW.1);**结论不依赖那个数**:24 > 20 与 25 > 20 同判。

⭐ **`strategy-57` 单独裁 `REGISTERED`**:对象 `campbind` **已于 §FX 退集** ⇒ 那条根本不是入集提议,
而是请求把条件 (a) 由 BUGGY(GH #878)更正为 **FIXED**。修法已现读(`:12800` 常数 + `:12895` 消费点),
真帧 §6 已翻面、§6b 自陈 tier-2 回落在本语料**不可断言**。
⛔⛔ **登记 FIXED ≠ 恢复入集资格**:`test_set.md:30` 写死 `campbind` 挂在 `pullcamp` 之下,
**将来提入集必须先让 `pullcamp` 回串**。本裁定不动这条前置,也不预批它。

**工具复读:RIDESHARE `50 → 39`。**

## 三、⭐⭐ 本轮最该被读走的一条:FROZEN-HOLD 是唯一一种**把自己藏起来**的裁定

- **被裁过了** ⇒ 离开 `pending_rulings.py` 的两个桶;
- **不 armed** ⇒ `arm_since` / `verify_coverage` / 载体门 / 选种**结构上看不见**;
- **不欠执行** ⇒ 冻结期没有任何 (a) 可买,`owed_executions.json` 里**没有行可以写**
  (写了就是一条永远 OWED 的行 = 把 exit 3 训练成噪音,GH #276 / #707 那条路)。

⇒ 它被**正确地**停在一个**没有人被告知要去看**的地方 —— **铁律 9 那根掉了 37 轮的棒的形状**,
区别只是这次是**裁定**把它停在那儿的。本轮之前这个队列 **6 行**,之后 **16 行**,
而站在它与解冻日之间的唯一一样东西是「总监还记得」。

**落地**:`tools/agent/pending_rulings.py` 新增 `frozen_hold_rows()` / `render_frozen()`,
打印 `FROZEN_HOLD: 16 row(s) parked by the admission freeze -- re-queue them when armed <= 20`
(五个组每轮跑的开工自检就调这个工具)。
⛔ **有意不驱动退出码** —— 一行被冻结停住的请求不是 finding;为冻结自己造出来的名单每轮顶红五个座位,
是让一条腿被忽略的标准做法。这条腿买的是:**解冻那天手里有一张单子,而不是一份记忆。**

## 四、⭐⭐ 装这条腿时当场撞出来的一下:子串匹配把一条 APPROVED 读成了 FROZEN-HOLD

第一版 `"FROZEN-HOLD" in ruling`,真语料读回 **17**,真值 **16**。多出来的是 **`hero-40`** ——
它的 `ruling` 是 **APPROVED**,而同一个字段里的散文逐字解释了**为什么 FROZEN-HOLD 那一条对它不适用**。

⇒ **一句「本条不属于这一族」的解释,被读成了这一族的成员证**,而且**长得像一次命中**。
📌 与 RULING 48 / 75 / 76 同型(真命题满足了另一个问题),**新意是被冒充的是「归属」本身**。

**修法两半**:(i) 锚在串首;(ii) ⛔ **近失打出来**(`MENTIONS-ONLY`),
否则「不在名单里」与「根本没有这样一行」在输出上**逐字节相同**。

**棘轮 + 变异台(evidence-discipline)**:`tests/test_pending_rulings.py` INVARIANT 8
(8a 锚 / 8b 近失 / 8c 不变红 + 真语料非空),`1071 checks, 0 failed`;
**三发变异全 CAUGHT** —— 锚改回子串 ⇒ 8a+8b 红;丢掉近失 ⇒ 8b 红;让它 `return 3` ⇒ 8c 红;
CONTROL 还原后 md5 `dd8356777e616737aa98cd76c8af7e4a` **逐位相同**。

## 五、投递(§2.5 三处)与欠条

(i) `queue.json` 十一行 `director` 字段(逐行带自己的核对读数);(ii) `test_set.md §HO` 全文档案;
(iii) 协同组活 issue 线程追评 —— 见 §八。
⛔ **本轮不开 `owed_executions.json` 新行**,理由写在 §三:冻结期内这批行没有可执行项,
一条永远 OWED 的行是负价值。**替代物是那条新腿**(它不驱动退出码,但每轮都印出名单)。

## 六、健康巡检(取数 **2026-09-19T04:05Z**,`git fetch` 之后,按名字序,从 `origin/main` 取,只认报告名形状)

| 组 | 最新报告 | 距今 |
|---|---|---|
| batch-desk | `20260919T031641Z` | 0.8h |
| hero | `20260919T014653Z` | 2.3h |
| strategy | `20260919T013110Z` | 2.6h |
| replay-check | `20260919T004859Z` | 3.3h |
| director | `20260919T005100Z` | 3.2h(本轮收尾后归零) |

**五组全健康,零点名、零升级。** ⚠️ 照 RULING 75/76 的口径登记:巡检读 `origin/main`,
而**报告落地晚于报告写成** ⇒ 读到的节奏恒为**下界偏老**,上表的 3.3h / 3.2h 是**上界**不是真停摆。
⚠️ 本容器 clone **是浅的**(`.git/shallow` 存在)—— 本轮**没有做任何「谁先落地 / 红了几轮」的日期推断**,
因此没有 `--deepen`;⛔ 后续谁要做那类推断,先 deepen(章程 2e 戊)。

**P4.2**:本轮**判定完结 0**(没有 promote / 退集),停滞 **10 轮**(门槛 12/24)⇒ 未到线。
归因照写:条件 (b) 要批测而刹车已持五十余轮 ⇒ **不是没人裁,是裁所需的那一半买不到**。
⭐ 本轮走的是上一轮量出来的那条「刹车下还能动」的通路(⑱),它**不产出判定完结**,
但它把**解冻日要处理的那张表**从「一份记忆」变成了「一张单子」。

## 七、闸与自检读数

- **`rc.sh` 纪律(§0)**:本轮每条要读退出码的命令都走 `rc.sh`,**纪律 3 无新发**。
- **py 闸**:`py gate: 138 ran, 0 findings, 0 uncertifiable, 45.0s`,`RC_EXIT=0`。
- **开工自检(铁律 10)**:后台跑满取真码,读数见 §九(收尾回填)。
- **铁律 6 三条腿**:push 时由 `.githooks/pre-push` 跑,读数见 §九。**无 `RULE6_BYPASS`**。
- **铁律 1.5(`carry_item_issue_state.py`)**:`RC_EXIT=0`,语料 `2026-09-18T13:10:35Z`(14.8h)、
  扫 1 entry / 1 carry segment / **13 GH ref**,**13 个全 open**,无 `STALE-CARRY`、无 `NO-HANDOFF`。

## 八、GH 评论

见 §九(收尾回填)。按铁律 11:MCP 若报 `requires approval` **立刻放弃**,把该发的话留在本节,下轮补。
按铁律 6 补款(GH #290):**任何带引用的评论都在 `claim_precheck.sh` 裸读通过、且两次 push 之后才发**。

## 九、收尾回填

*(见文件末尾的『[同轮收尾追加]』一节。)*

---

## [同轮收尾追加,push 之后]

### ⑨-1 开工自检(铁律 10)真码 —— **`RC_EXIT=3`**,`legs run 15`,`UNCERTIFIABLE (exit 2): none`

⭐ **本轮等它跑满才写读数**(约 19 分钟,后台),所以 `SELFCHECK_EXIT` 是**真码不是空缺**。
`FINDINGS (exit 3)`:`cadence` / `queue-rulings` / `owed-executions` / `lua-coverage` / `trunk-red(python)`。
`NOT RUN (inside a leg)`:`tests/test_lua_gate.py` / `tests/test_luacheck_gate_soakswitch.py` / `tests/test_selfcheck_lua_leg.py`。

逐条归因(⛔ 不写「exit 3,全是 cadence」那种手工归因,GH #267):

1. **`queue-rulings` = 本轮的主活自己**(入轮 RIDESHARE 50)。收轮复读 **39**。
2. **`cadence` GAP:`strategy`,`09-18T04:24Z -> 09-18T07:55Z`,3.5h** —— ⚠️ 这是 **24h 窗口里的历史洞,不是现在的停摆**:
   同组本轮最新报告是 `20260919T013110Z`(取数 04:05Z ⇒ 2.6h)。按章程 2e-bis 交叉读 `unlanded` 腿:
   `OK: no unlanded work in the certifiable window` ⇒ **不点名、不升级**。
   ⚠️ 诚实边界:该腿本轮 `REFUSED refs 1339 of 1361`(浅 clone 无 merge-base)⇒ 它的「OK」是**可核验窗口内**的 OK,
   ⛔ 不是全仓库的 OK。
3. **`lua-coverage`:`UNCOVERED 114 of 524 (22%)`,`UNCOVERED SET GREW` 2 条** ——
   `tests/test_fieldsip_transfer_receiving_site.lua`(`no_manifest_row`)与 `tests/test_lion_w_fight_seed.lua`(`too_slow`)。
   ⛔ **两条都不是本轮造的**(本轮零 Lua 测试新增);它们是协同组 / 英雄组新落地的测试**没有当轮登记进自动读者**。
   📌 这正是上一轮 §HN 之后写下的那条规矩(「新测试当轮就登记进自动读者,不留给下一轮去发现」)的**第一次外部复发**。
   ⇒ 本轮**只登记不代做**(改别组 manifest 行要它们自己的 TIME 实测),写进『下次触发』。
   ⭐ 同时 push 钩子自己也报了同族的一行:`9 new test(s) exceeded the hook budget and were EXCLUDED`
   + `92 new test(s) not in the manifest were run anyway, costing 337.66s`(钩子真实成本 **588.40s** vs 预算 540.0s)
   ⇒ **manifest 正在被落地速度甩开**,这比单条 `NEW UNCOVERED` 更该被读。
4. **`trunk-red(python)`:`tests/test_gated_getter_stub_census.py`**,逐字
   `§GF priced GetAnimActivity as the catch-all 0 by hand; the census must reach the same answer from source`。
   **`151 passed, 1 failed, 3 uncertifiable`。**
   ⭐ **这条红不是本轮造的,而且这次的论证是逐文件的不是结构性的**:本轮改动**只有 5 个文件**
   (`iterations/queue.json` / `iterations/streams/test_set.md` / `iterations/streams/director.md` /
   `tools/agent/pending_rulings.py` / `tests/test_pending_rulings.py`),而该测试读的是
   `tools/agent/gated_getter_stub_census.py`、`tests/test_gated_getter_stub_control.lua`、
   `bots/.../ability_item_usage_generic.lua` —— **交集为空**。
   在提交后的树上单独复跑:`RC_EXIT=1`,同一条 FAIL。**批测台 `20260919T031641Z` 与英雄组 `20260919T014653Z` 同轮报了同一条,零新增。**
   ⚠️ 该测试的另外两半在本容器打 `UNCERTIFIABLE`(缺 `lua5.1` / `luacheck` 的那两句)—— **那不是通过**。
5. **`owed-executions`** = 存量欠条(含两条 `claim: EXPIRED`,`183.5h` / `174.1h`,按 TTL 回落为未认领)。本轮**没有新增 owed 行**,理由写在 §五。
6. `trunk health (fast Lua detectors)`:**144 tagged detector file(s), 0 failures**。
   `python tests no automatic reader runs (GH #843)`:**UNCOVERED 0 of 155 (0%)**。

### ⑨-2 push 记录与铁律 6 三条腿

两次 push **都成功,顺序按文档(先分支、再 main)**,`pull --rebase origin main` 干净(1/1)。

- **分支 push**(`RC_EXIT=0`):`GATE_EXIT=0  CLEAN` / `py gate: 138 ran, 0 findings, 0 uncertifiable, 45.4s` /
  `lua gate: 438 ran, 0 findings, 0 uncertifiable, 9 unanswered, 5 known-red, 588.4s`(scope = 6 path(s) changed)。
- **`HEAD:main` push**(`RC_EXIT=0`,`e500a5cf..43153096`):memo 命中
  (`HEAD^{tree}=6d5e5789… with origin/main=e500a5cf… and a clean working tree`)⇒ 三行读数**与上一次逐字相同,没有重跑**
  —— ⭐ **这正是 RULING 69 那条「顺序是承重的」买到的东西**:按文档顺序,第二推问的是同一个问题,memo 免费答掉。
- **无 `RULE6_BYPASS`**。⛔ 动态那半(全量 Lua 套件,~100min,GH #124)**没跑,也不被这三行声称**。

### ⑨-3 GH 评论(§2.5 (iii))

协同组活线程追评:**GH #912** → https://github.com/dragonghy/dota2bot/issues/912#issuecomment-5739301273
(一条评论覆盖十一行,因为裁的是**十一条请求**不是某一个 id;⛔ 不在八个 id 各自的 issue 上刷同一份文本)。
**发表前 `claim_precheck.sh` 裸读 `RC_EXIT=0`(`clean` / `OK to publish` / `resolved on trunk 4`),且在 branch + main 双推之后。**
⚠️ push 之前同一份草稿读回 `MISSING section test_set.md §HO` + `DO NOT PUBLISH YET` —— **工具是对的**(§HO 当时还不在 trunk 上),
这一行照登,免得下次有人把「先发后推」读成没有代价。
**本轮 MCP 可用,未触发铁律 11。**

### ⑨-4 本次会话 token 用量(铁律 8)

```
TOKENS total_in=11,723,494 out=55,707 turns=70
```
(统计时刻之后的收尾回合不计入。)
