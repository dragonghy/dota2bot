# director 2026-09-11T13:xx–14:xxZ:**第十、十一条 promote 作为**一个原子**落地(`zusult` + `zusboltdom`),armed 32 → 30**

**一句话**:两条 Zeus 的 id 作为**一个原子** PROMOTE。理由不是「攒够了」,也不是上一轮
(§GU)那条「家族级 (b) 分不开」——**是四种组合上的算术:其中一种已经被量到是坏的,
而它恰好就是单独促进 `zusult` 会发出去的那一种。**

零 AWS(**一次调用都没有**)、零波次、**不发 owner 邮件**、无 reject、无入集。
判定完结 **1**(⛔ **按原子记 1,不按 id 记 2**;owner P4.2 的 ≥2 **本轮不达标,见 §6**)。
全文档案 `iterations/streams/test_set.md §GV`(§GV.0–§GV.7),
机器键 `state.json:zusult_zusboltdom_PROMOTE_20260911`。

---

## 1. ⭐⭐⭐ 主轴:一次 promote 只有一种合法的切法

| 组合 | 是什么 | 证据 |
|---|---|---|
| 都不 armed | 出厂树 | — |
| **只 `zusult`** | **量到 BUGGY** | GH #477 / W44 逐帧确认 3 发域内 Lightning Bolt;W45 读 8.0 泄漏/100 域内帧 |
| **只 `zusboltdom`** | **结构性空操作(证得,不是猜)** | `X.BoltAoEKillTarget` 的返回值全文件**唯一消费者**是 `castW2Target`(`hero_zuus.lua:332` 声明 / `:693` 赋值 / `:700` **唯一读点**),而 `:700` 传进的 `X.zuus_ShouldSaveManaForUlt` 当时在前几行就因两条 id 都未 armed 而 `return false` |
| **两条都 armed** | **W46 起每一波真正跑的配置**,也是 (a) 买到的那个 | W52:`VERIFY id=zusult verdict=WORKING episodes=11` / `VERIFY id=zusboltdom verdict=WORKING episodes=6` |

⇒ **只有「两条一起」既非空操作、又非已知坏的配置。**
⛔ **不是 `lanefix` 那个形状**:不是一次上十条互相耦合的守卫,是**一条修正 + 它所修正的那条规则**。

## 2. ⚠️⚠️ 本轮真正的新东西:倒像普查看不见的**第三种极性**

§GT 那条腿的判定轴是**调用点的可达性**(`FROZEN` / `COUPLED`)。
**而 `zusboltdom` 的调用点一直可达、一直执行、域一直非空** —— 被 `zusult` 掐住的是它算出来的
那个值的**去处**。所以本轮自检照旧打 `FROZEN 0 / COUPLED 1(仅 fieldsip)/ UNRESOLVED(armed) 0`,
**两条都读 CLEAR**,而单独促进任一条的后果**完全不同**。

📌 **可迁移的一句**:*一个带门的 helper 可以同时拥有活的调用点、非空的域、和零效果 ——
因为别的 id 掐住的是它的**后果**而不是它的**可达性**;而一个按「这个 helper 响了几次」
计数的检测器会把这种情况读成 WORKING。*

⛔ **今天这不是缺陷**(两条同轮促进,落地的组合正是被测过的那个)。
登记为 `owed_executions:consequence_polarity_census`,⛔ **本轮不顺手改普查工具** ——
改判定器要安静容器与自己的变异台,而**让仪器工作挤掉判定完结**正是上一轮(07:40Z)
自己举过手的那个失效形状。

## 3. 三条件

- **(a)** W52 WORKING 11 / 6。⭐ **`zusult` 更早那条 BUGGY 3 判为「被取代」,理由可命名**:
  BUGGY 3 量在 **W44**(`zusboltdom` 尚未入集)= **组合二**;WORKING 11 量在 **W52** = **组合四**。
  **两份读数不矛盾,它们量的是两棵不同的树。** ⛔ 章程逐字警告过「不要只读最新一行」——
  最新一行在这里确实成立,**成立的理由是一次代码改动,不是时间顺序**。
- **(c)** `zusult` 是标准打法(为全图处决留蓝,只防 chip);`zusboltdom` 是**算术**
  (`GetAbilityDamage()` 读一个 `zuus_lightning_bolt` **不声明**的 KV 字段 ⇒ 每级读 0;
  引擎规则「Pass 0 for no HP filter」⇒ 那个叫 `nCanKillHeroLocationAoE` 的分支**不是 kill 分支**)。
  ⭐ **依赖写成 VALUE 不写成 id,今天得到回报**:`type(nHealthCap)=='number' and nHealthCap <= 0`
  促进后**逐字保留**;**若当初写成 `IsSoakCandidate('zusboltcap')`,今天这一促进会把它冻死**
  —— `pullcad` 陷阱**预防成功**的现场。
- **(b)** 家族级,六波,**粗粒度过门,⛔ 不是正面证据**。gpm
  `−22.72 / +40.58 / −7.20 / −15.00 / −8.96 / −9.84`,**均值 −3.86,920 计分局**。
  **铁律 4(i-a) 披露**:六波 `ab/ba` 全部 `sign_flip`(读数见 §GV.5);按 **4(i-c)**,gpm 是
  swap-average 过的估计量 ⇒ **反号不是否决理由**,但**照样登记**。胜负:`winrate_neutral` 六波全 0.5,
  **22 粒里 13 粒 `headroom` 为 0**(整波一边通吃,零信息),其余 9 粒无明显方向。

## 4. ⚠️ 促进的连带后果,量出来的不是断言的

**载体项 7 → 6**,掉的是 **`zuus`**(32 串在中间态读 **2 unresolved / exit 2**,30 串读 **0 / exit 0**)。
⇒ 此后波次**不再被要求必须带 Zeus**。这是**约束变少**且**自动可逆** —— 任何一条 zuus-scoped 的 id
入集,该项自己回来。

## 5. ⭐ 推送闸与测试台账:四条红全是本轮自己的,且每一条都教了东西

`lua_gate.py` 第一次跑打 **4 findings**,全部是**钉着「门关着 ⇒ 空操作」的用例**,
促进把它们的前提抽掉了 —— **这正是那条腿该干的事**。⛔ **一条也没删**,全部改成
「OFF 侧 = 非 turbo」并**补上促进后才成立的那一半**(没有这一半,文件对一次被偷偷回滚的
promote 同样会绿)。

⚠️⚠️ **而中间踩到两件事,都记下来**:
1. **「非 turbo」在 helper 层等价于「id 未 armed」,在整帧层不等价。** 把 manalock 的端到端
   用例改成非 turbo 后它红成 `got {zuus_heavenly_jump}` —— **模式开关一次移动路径上所有
   turbo 门下的分支**,这一帧被另一条分支先拿走了。⇒ 该用例**降到 bid 层**(那里等价是真的),
   并把理由**写进文件**,免得下一个人重做一遍这个错误修法。
2. **`test_zusult_pre_ladder_claim_retake.lua` 的普查是对散文敏感的**:它把「本文件 arm 了这个 id」
   判为 `IsSoakCandidate` + 带引号的 id 字面量,于是我在**断言消息里**写下的 id 名把
   `test_gate_claim_consistency.lua` 拉进了那张「这些文件 arm 它」的名单。
   ⛔ **修法是改我的散文,不是把一个不 arm 它的文件加进那张表** —— 后者会让**登记册本身变成假的**。

⚠️ **开工第一条命令又撞管道拒绝门 —— `rc.sh` 是习惯不是门(第十五次)。**
⛔ 「后台包装吞真码」**第十九次**(harness 报 0,落盘 `EXIT=3`)。

**铁律 6 三条腿(全部在 push 前跑,无 `RULE6_BYPASS`)**:
`GATE_EXIT=0 CLEAN` / `py gate: 96 ran, 0 findings` /
`lua gate: 336 ran, 0 findings, **13 known-red**`。
⭐ **py 闸本轮又替总监自己工作了一次**:我给 `owed_executions` 新行的 `done_when` 写成了字符串
而不是 `{"kind": ...}`,`pending_rulings.py` 当场崩,**红由推的人在同一轮看见**(GH #624 的立案句)。

## 6. ⚠️ 本轮自己举的手:判定完结 1,不达 owner P4.2 的 ≥2

⛔ **按原子记 1,不按 id 记 2。** `zusult` 与 `zusboltdom` 没有两份独立裁定,它们是
**同一个判断的两半**(§1 那张表就是那一个判断)。§GS 立过的规矩逐字适用 ——
**「在自己的指标上放宽定义是最没意义的一种放宽」**。

**第二格为什么没做,说清是哪一格**:剩下最便宜的是 `ownhalf`(WORKING 7,09-10),
**没促进,理由具体**:同一份报告 §2 量到 **`ohnum`(同族、未 armed 的候选)在同样 7 帧上
删掉 `ownhalf` 开火的 4 次里的 3 次**,而那一帧的地面真相是 **13 秒内 2 杀 0 死** ——
即 `ohnum` 在该帧会**错误地**拒绝一次好 punish。⇒ 促进 `ownhalf` 会把「不被 `ohnum` 拒绝」
的那个版本设成默认,**而 `ohnum` 的裁定还没做**。
⛔ 这**不是** `fieldbuy`/GH #734 那个形状(那条是「缺陷被量到了价钱」);这条是
**「一条未裁定的姊妹候选会改写它 75% 的域」**。
⚠️ 另注:`ownhalf` 的 (a) 只有 **1 局 7 帧**,且 09-09 的 `ownhalf_margin`(800 → 1600)
**骑在同一道门上** —— 促进它就是同时促进那个门限。

## 7. 同轮 queue 裁定:`hero-58`(自检 `queue-rulings` 腿 exit 3 → 0)

⛔ **不是 FROZEN-HOLD**:本行**自己逐字写着「P4.2 冻结期内本条不请求入集」**。
**拿一条不适用的规则去挡它,是制造一次看起来合规的驳回。**
⭐ 裁定 **`ROUTED-BUT-NOT-AS-AN-ARCHIVE-SCAN`**:腿该有,但别在今天的语料上派 ——
**提议方自己量过**并写成会红的断言:持 A 杖 Lion 帧 **0/42**、两个 splash key 读 **0**、
最密敌人团 **2**(下限 3)。派了只会买回**闸的零而不是游戏的零**(§CJ)。
⚠️⚠️ **也不给 `HOLD-DOMAIN-EMPTY`,而这一条与 §2 同族**:那套自动解封按**英雄在不在语料里**
判定,而 **Lion 到处都是** ⇒ 会**每轮误报 UNBLOCKED**。本行的空域是「没带 A 杖出现」,
**那台机器量不到这个区别**。📌 *一个机制报的「通了」,可能只是它量的那个量通了。*
⭐ **棒分两半交出去**:(1) 促进期那一半**现在就生效** ——
`promote_atoms.json:lion_scepter_aoe_exit_is_one_atom`(**对称行**,任一条单独 promote 当场红;
实测 `promote_atoms.py` **exit 0**),⛔ **它此前只活在 queue 行的散文里,而被裁过的 queue 行
会从所有待办表上消失**(拉野死分支 37 轮那个形状);(2) 上臂期那一半 promote_atoms
**表达不了也不冒称表达得了**,进 `owed_executions:lion_aoe_atom_readmission_on_thaw`。

## 8. 巡检 / 成本 / 自检

🩺 **五组 24h 内全部有产出,无掉队组**:batch-desk 12:15Z / hero 11:10Z / strategy 10:49Z /
replay-check 10:15Z。
💰 **本轮 AWS 调用 0 次,不作 MTD 新声称**;三条线未改($60/$90/$100)。**总监不花 AWS 的钱。**
⚠️ 自检真码 **`SELFCHECK_EXIT=3`**(`legs run 12`);
`FINDINGS: cadence queue-rulings owed-executions trunk-red(python) trunk-red(lua)`;
`UNCERTIFIABLE: none`。⭐ 其中 **`queue-rulings` 本轮已清掉**(§7);`trunk-red(lua)` 族属 GH #718/#650,**不开新单**。
⚠️ owner **P4.1 标尺波**仍未认领,挂在 `DECISIONS_NEEDED` 第 16 条,**本轮不另发信**(决定类邮件每周至多 1 封)。
**armed 30,离 P4.2 解冻线(≤20)还差 10 条**(上一轮差 12)。
⛔ **W65(09-11T09:23Z 起飞,34 串)及更早不与 30-id 家族并池**;§GU 落地的 32 串**没有任何一波跑过**。

## 9. 下次触发

1. ⭐⭐⭐ **判定完结,主体,≥2** —— **第一格先裁 `ohnum`**,`ownhalf` 跟着它走(§6 给了具体判据:
   `ohnum` 会删掉 3/4 次开火,而那一帧的地面真相说那些 punish 是**好的**)。
   其余 WORKING CLEAR:`zusboltdom` 家族已清空;剩 `ownhalf`(7)/ `wandbleed`(1–2,太薄)。
   ⚠️ `lionqdmg` 被 `promote_atoms:impale_kill_needs_damage` 挡住,**不是候选**;⛔ `fieldbuy` 等 GH #734。
2. ⭐⭐ `py_gate_budget_premise`(一个常数 + 一次工具重跑,**要安静容器**;⛔ **不得再挤掉判定完结**)。
3. ⭐ `consequence_polarity_census`(本轮新登记)与 `pullcamp_atom_readmission` /
   五条 `a_evidence_*` 的首次活读数(**第四次顺延**)。
4. 存量:GH #523 / patch 缺口 P3 / 账户级预算过滤器等 owner /
   **`rc.sh` 是习惯不是门(第十五次)** / 「后台包装吞真码(第十九次)」。
5. **本轮两条 promote 的 GitHub 追评**(GH #47 / GH #59 / GH #477)—— 按铁律 6 的 GH #290 条款
   留到 **push 之后**;⛔ 若 `mcp__github__*` 要审批,按铁律 11 **立刻放弃**并留在这里等下轮。

## 10. Token 用量

```
TOKENS total_in=16,280,295 out=77,898 turns=99
```
