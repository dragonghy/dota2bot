# 总监 2026-09-12T19:15Z(第七轮)

**一句话**:修掉 GH #783 —— **「把一条 Lua 测试登进推送钩子」这个唯一被许可的动作,会顺带清空 `known_red` 赦免名单,于是下一个 push 的人(很可能不是你)被 9 条早于他的红挡下**;并裁掉当轮唯一一条 RIDESHARE(`hero-67` = APPROVED-SCAN),顺带用实测答掉上一轮 ⑨④ 那个问题。

零 AWS(**一次调用都没有**)、零波次、**`bots/` + `game/` 零 diff**、**不发 owner 邮件**、无 promote / reject / 入集 / 退集。**armed 27 不变**,离解冻线(≤20)差 7。

- 全文档案:`iterations/streams/test_set.md` **§HF**(GH #783)、**§HG**(RULING 32)
- 机器键:`state.json:lua_gate_baseline_CARRYOVER_FIXED_20260912` / `lua_gate_baseline_TWO_WRITERS_DISAGREED_20260912` / `py_gate_HAS_NO_BASELINE_AT_ALL_20260912` / `trunk_red_RUN_TESTS_GUARD_DECAPITATION_20260912`
- 新 owed 行:`owed_executions.json:lua_gate_baseline_e2e`
- `pending_rulings.py`:裁前 **RIDESHARE 1 / OTHER none**,裁后**两节都是 `none`**,exit 3 → **0**;`STRATA_SILENT: none`,clause **17 → 18**

---

## 1. 本轮工作单元的选择(为什么是 #783,不是 backlog 里那条)

上一轮的 ⑨① 点名 `creeps_schema_gh581` 第 (C) 半是「backlog 里性价比最高的一条」。本轮**没做它**,理由是当轮出现了一条排序更高的:

章程 2a 把 **[bug]/[harness] issue 直接修**排在基建 backlog 之前,而 16:xxZ 之后新开了四条 `[harness]`。其中 **GH #783 是唯一一条同时满足三点的**:(i) 归总监(铁律 5);(ii) **正在阻断别的组**(协同组的 manifest 登记欠了五轮,五轮都被当成纪律问题记,而本轮实测下来它是这条边);(iii) 就在本台自己这一轮的关键路径上 —— 本轮要落一条新测试,踩的是同一个坑。

⚠️ **(C) 半没有掉棒**:它在 `owed_executions.json:creeps_schema_gh581` 里(开工自检第 9 条腿每轮点名),并且本轮**在 §HG.2 里买到了一条它之前没有的论据**(见 §4)。

## 2. GH #783:缺陷、修法、以及为什么修法不是那四行

**缺陷。** `lua_gate_manifest.json` 同时装两种东西:**测量值**(每条测试的秒数,re-measure 本就该覆盖)与**基线**(`known_red*`,「你到之前就已经红的那些」,re-measure 无权碰)。三个写者:

| 写者 | 基线 | 为什么 |
|---|---|---|
| `set_known_red()` | 立基线 | 它的活就是这个 |
| `reselect()` | **保住了** | 它 `old.update(...)`,**mutate 读进来的 dict** |
| `main()`(re-measure) | **四个键全丢** | 它 **build 一个新 dict** |

而 `lua_gate.py:298` 逐字写着「A missing key is an EMPTY baseline, never a permissive one」。⇒ manifest 自己 `_comment` **唯一许可的那个动作**(“Do not hand-edit; re-measure”)**解除全组的赦免**。两条路都有代价 ⇒ 实际发生的是第三条:**没人登记**。

**⭐ 修法不是给 `main()` 补四个键。** 那样修好今天这一发,把形状原样留下:两条写路径仍然互不同意,**第五个基线键**以后照丢不误。落地的是**一个注册表**:

- `BASELINE_KEYS` —— 四个键**一处命名**;
- `carry_baseline()` —— **逐字搬运**,文件缺失/损坏都当空基线(不 raise:12 分钟跑到一半不是抛异常的地方);
- `set_known_red()` —— 把基线先建成一个 dict,写之前**当场对照注册表自检**(不在册的键 ⇒ 打印 REFUSED、**exit 2**,而不是写进去等着被丢);
- `reselect()` —— 留下逐字约束:若被改写成构造新 dict,它就欠一次 `carry_baseline()`。

⛔ **搬运必须逐字,不许从本轮重测「重新推导」。** re-measure 当然知道今天哪些测试红 —— 采纳那个集合会**把这一轮自己引入的红一并赦免**,是 fail-OPEN,**比原缺陷更坏**。基线回答的是「你来之前就坏的是哪些」,只有上一份文件说得出这句话。**钉在 case 5。**

## 3. 变异台:五个变异体各自因**自己的**理由死

`tests/test_lua_gate_baseline_carryover.py`,**31 检查全绿**。恢复用文件副本 + `md5sum` 核对(evidence-discipline 1),退出码无管道(纪律 3)。

| 变异 | 死法 |
|---|---|
| **M1** `build_manifest` 不再搬运(**原缺陷逐字**) | **8 red** — case 4 四键 + case 5 + case 8 两条 |
| **M2** `carry_baseline` 改为「并入本轮的红」(fail-open) | **2 red** — case 5 |
| **M3** 从 `BASELINE_KEYS` 悄悄摘掉 `known_red_note` | **1 red** — case 6,**逐字点名该键** |
| **M4** `reselect` 改写成构造新 dict | **4 red** — case 7 |
| **M5** `carry_baseline` 在文件缺失/损坏时 raise | **4 red** — case 2/3,**带异常类型与消息** |

⚠️ **头一遍 M1 与 M5 是靠 traceback 死的,不是靠具名断言。** 作为信号正确,作为消息没用,而且**中断了后面的 case** —— 一次变异读起来像「一处不明形状的失败」。已加 `try_carry()` 包装,并把 case 5 的 `man5["known_red"]` 改成 `.get`;复跑后五个变异体**全部打出逐条具名的 FAIL**。
📌 *一道闸的失败模式如果是堆栈,下一个人得自己把理由重新推导一遍。*

⛔ **买到的是 manifest 的契约,不是闸在活树上的行为。** 从没跑过真的 12 分钟 measure,也没端到端跑过 `lua_gate.py`;case 8 是**复现**消费者那句 `set(manifest.get("known_red") or ())`,不是调用它。#783 自己的验收**仍然欠着** ⇒ `owed_executions.json:lua_gate_baseline_e2e`。

**顺带查过、结论是否的一条**:`py_gate.py` 里 **`known_red` 一次都没出现** ⇒ python 兄弟**根本没有赦免名单**,这个缺陷在它身上不可能发生(所以登记 python 测试是安全的,本轮新测试因此没有登记压力)。⚠️ 但这不是「兄弟做对了」:它意味着哪天一条**在快 py 域内**的测试红了,py 闸会挡下所有人而没有任何赦免通道。登记为已知形状,**不开 issue**(无现场即无立案句)。

## 4. RULING 32(`hero-67`)与上一轮 ⑨④ 的答案

**裁定**:`hero-67` = **APPROVED-SCAN**,零 EC2,不发波;⛔ 入集不批(P4.2,armed 27 > 20)。请求原文**一字不改** —— 它自己写对了 4(i-a) 与 4(ii) 两处条款和三条出口(**本月第二条**,上一条是 hero-66)。批的理由是**这道滤网与 P4.2 同向**:出口 (i) 让本 id 在**从未占用 armed 位**的情况下退场。

**⭐⭐⭐ 前提更正(不改变裁定,改变下一个人继承的理由)。** 请求逐字写着「归档语料里一个非英雄单位都没有(GH #772,1410/1410)⇒ 端到端域**测不了**」。那句话**本台 13:xxZ 已经量掉**(§HD.2):1410/1410 逐位正确,**但小兵住在兄弟键 `fx.creeps` 里**,现量 141 帧里 **32 帧**带小兵采样。

结论(端到端域 = 0)**仍然成立**,而**真正的堵点含义相反**:`tests/mock/replay_fixture.lua` **今天没有 `GetNearbyLaneCreeps` 这个 reader**,未接线的 `GetNearby*` 走 mock 默认 `{}` ⇒ `#laneCreepList` 恒为 0,**与语料里有多少小兵无关**。「语料没有小兵」= 买不到;「loader 少一个 reader」= **接上就能买** —— 而那个 reader 正是 `creeps_schema_gh581` 的 **(C) 半**。
📌 *用错的理由写下对的结论,下一个人继承的是理由*(与 §HC 乙**逐字同一句话**,四轮内第二发)。

⛔ **`tests/test_axe_q_lane_push_clock.lua` §3.4 那道自曝闸是焊死的**:它承诺「语料出现非英雄单位的那天自己报红」,而 line 292-298 逐字只遍历 `(fx or {}).units`。**它承重**(docstring 自己说这道断言是 §3.1 那个「2」不被当成执行核验的唯一拦阻)。本台**不代改**(铁律 5);与 §HD.4 同形第二发,**4 小时内、换了一个组**。

**⭐⭐ 上一轮 ⑨④ 的答案,实测不是猜测**:`hero-67` 提交于 **17:04:37Z**,在更正之后**四小时**,仍逐字引用 `GH #772 / 1410` ⇒ **没有读到**。⚠️ **归因是投递不是纪律**:那次更正只落进 `state.json` 与 `test_set.md`(档案权威,**但没有人按它选杠杆**),没有落进英雄组读的任何字段。比 §DR 更隐蔽的一点:**被漏投的不是裁定,是一条被当成筛选杠杆在用的语料事实**,它不长得像一个需要投递的东西。⇒ 本次走三处:`director.note` / GH #788 追评 / 本报告。

## 5. 交棒(铁律 9 连带规则:修好 ≠ 做完)

| 交给谁 | 什么 | 落到哪个字段 |
|---|---|---|
| **协同组** | manifest 登记的**阻断物已拆**,`test_overchase_pursuit_tense.lua`(实测 4.5s < cap 5.5s)可以登记了 | GH #783 追评 + `owed_executions:lua_gate_baseline_e2e` (C) |
| **协同组** | **新 issue**:`test_run_tests_guard.py` 红 —— `test_overchase_pursuit_tense.lua` 自己 `os.exit()`(line 103 / 298,`0173aad4` 落地) | 新 GH issue |
| **英雄组** | 两条前提更正 + 焊死的自曝闸 | `queue.json:hero-67.director.note` + GH #788 追评 |
| **本台(未指派)** | #783 的端到端验收 + 孤儿进程组修法 | `owed_executions:lua_gate_baseline_e2e` (A)(B) |

## 6. 开工自检与铁律 6

⚠️ **开工自检本轮跑了两次,第一次被我主动杀掉** —— 树即将在它脚下改动(与 13:xxZ、09:50Z 同一处置)。**那不是通过,是没跑**;算数的是收尾安静树的重跑(读数见 §7)。

⚠️ **本轮第一条命令仍然踩了纪律 3**(`routine_selfcheck.sh … | tail -60`,被 §22 守卫当场拒:`SELFCHECK_EXIT=2 REFUSED`)——**第三十三发**,形状与 09-04 / 09-12T13:xxZ 那两发**逐字相同**,章程第 0 条逐字覆盖它,我只是没照做。改走 `rc.sh` 后正常。

📌 **孤儿本轮实测复现了一次**:自检被 kill 后留下 **1 个 `ppid == 1` 的 `lua5.1`**(已跑 **1 分 11 秒**),与 GH #783 §3.1 记的形状**逐字相同**。已手动清理。⇒ 它不是「多一个进程」:它**和后来的闸抢 CPU**,而这个工具的整个前提是「成员资格只看实测秒数」—— **孤儿污染的正是它唯一的产出**。进 owed 行 (B) 半。

🚦 **铁律 6 三条腿(手动一遍 + push 钩子各一遍)**:

- `GATE_EXIT=0  CLEAN`(luacheck bots game:**0 警告**)
- `py gate: 97 ran, 0 findings, 0 uncertifiable, 38.1s` —— 本轮新测试在「13 new test(s) not in the manifest were run anyway」里,**绿**
- `lua gate:` 见 §7

⛔ **未用 `RULE6_BYPASS`。**

## 7. 收尾读数(安静树上的重跑)

`bash tools/agent/rc.sh bash tools/agent/routine_selfcheck.sh` ⇒ **`RC_EXIT=3`**(0 干净 / 2 未核验 / 3 有发现)。

- **legs run: 12**;`UNCERTIFIABLE (exit 2): none`
- **FINDINGS (exit 3): `unlanded` `cadence` `owed-executions` `trunk-red(python)`** —— **四条没有一条是本轮产生的**:
  - `unlanded` **1 条**:`b216e61 strategy 19:33Z 瞭望塔是「己方建筑」`,只在 `origin/claude/intelligent-hopper-7xouf3` 上。**协同组的棒,本台不代 cherry-pick**(与 09-12T16:xxZ 对 `1455ce9` 的处置同口径)。
  - `cadence` **4 条**:director 三条是**昨夜到今晨的历史洞**(09-11T19:00→22:45、22:45→02:45、09-12T06:55→10:30),strategy 一条是 `16:44Z → now` **3.5h**(刚过 3.5h 阈值,不构成掉队)。
  - `owed-executions`:registry **62 行**,本轮**新增 1 行**(`lua_gate_baseline_e2e`,读 **OWED**,`done_when` 正确解析成「`lua_gate_measure.py` 里那三行还在」)。OWED 报出来就是它的工作方式,不是缺陷。
  - `trunk-red(python)`:见下。
- **`NOT RUN (inside a leg): tests/test_selfcheck_lua_leg.py`** —— 120s 预算跑不完(GH #358),**这一条本轮没人看过**,不是通过。
- **trunk health(python)**:`123 passed, 3 failed, 1 uncertifiable`(开工时是 `120 / 3 / 3`;+3 passed 是本轮新测试 + `lua5.1`/`luacheck` 装上后两条从 UNCERTIFIABLE 转 PASS)。**三条红与开工时逐字相同,本轮一条都没加**:`test_carrier_terms.py`(→ 英雄组)/ `test_detector_source_constants.py`(→ GH #787)/ `test_run_tests_guard.py`(→ **本轮新交出 GH #790**)。`PASS tests/test_lua_gate_baseline_carryover.py`。
- **trunk health(fast Lua detectors)**:**87 个 tagged detector 文件,0 failures**。
- **`A-EVIDENCE-OWED  armed 27  verdict 27  owed-row 0  UNOWED 0  PRE-ARM 0`**。

⚠️ **顺带读到、本轮不动的一条(第三次同形)**:owed 行 `late_epoch_corpus_reopen_list`(**本台 06:5xZ 自己开的**)读 **DONE**,提示「the director should retire this row」——**而它不该被退休**:它的 `done_when` 是 `tests/frames/README.md exists`,那个文件**开这行的时候就存在**,所以这条腿**从落地起就读 DONE**。这正是本台 13:xxZ 在 `creeps_schema_gh581` 上**当场躲掉**、19:xxZ 在 `lua_gate_baseline_e2e` 上**又躲掉一次**的同一个形状:**用一个今天已经为真的判据,去判一件还没做的事**。⇒ 登记,下轮改判据(不是退休),**别按它绿了就退休**。

## 8. 成本

💰 **零 AWS,一次调用都没有**;不作 MTD 新声称。批测台 18:20Z 报告的刹车状态(GH #779,projected $90.058 > brake $90.00,停发待 owner 表态)**本台不改动、不覆盖** —— 那条线归批测台,且 owner 表态属于 `DECISIONS_NEEDED`。

## 9. 各组健康度

五组 24h 内全部有产出,**无掉队组**。⚠️ **三条 trunk red 同时挂在 main 上**(`test_carrier_terms.py` → 英雄组、`test_detector_source_constants.py` → GH #787、`test_run_tests_guard.py` → 本轮新交出),**三条全在推送钩子的快域之外** ⇒ **GH #774 本轮加到第三个直接证据**:红全落在闸看不见的地方,由下一个开工的组发现。

## 10. 下次触发

1. ⭐ **`creeps_schema_gh581` 的 (C) 半** —— 本轮 §HG.2 给它添了一条新论据:它现在**同时**解锁 `axecallclock` 的端到端域。接 `GetNearbyLaneCreeps`(今天连 reader 都没有)+ `GetNearbyNeutralCreeps` 换实现要带**那四个测试的重开清单**。
2. ⚠️ **确认 §HG 的更正被英雄组读到了** —— 上一轮问同一个问题、答案是「没读到」;本轮把投递从两处加到三处,**下轮验的是这次投递有没有奏效**,没奏效就说明 `director.note` 也不是他们读的那张表。
3. **owner 优先项 P4.3**(`test_set.md` 现 **534KB**,目标 <50KB,责任在本台,**已欠一周**)。本轮两节都刻意写短,但方向仍是增长 —— 这条要么排一个专门的工作单元,要么按铁律 9 写进 `DECISIONS_NEEDED` 说明为什么做不了。
4. **判定完结 ≥2**(P4.2 的产出指标):本轮是 **0** —— RULING 32 是 scan 批准,不是 promote/reject/退集。**连续第七轮 armed 停在 27**,这条要正面处理。
5. 三个 promote-time 普查只读 `bots/`(**第六轮顺延**)/ GH #584(**第九轮顺延**)/ 五条 `a_evidence_*`(**第十三次顺延**)/ patch 缺口 P3 / `DECISIONS_NEEDED` 15/18/19 等 owner。

## 11. token 用量

```
TOKENS total_in=17,148,193 out=81,355 turns=104
```
(统计时刻为收尾回合之前;`input_tokens (uncached)` 仅 208,`cache_read` 16,939,602 —— 本轮几乎全部走缓存读。)

## 12. 本轮产物索引

- 代码:`tools/agent/lua_gate_measure.py`(+97/−11)、`tests/test_lua_gate_baseline_carryover.py`(新,31 检查)
- 档案:`test_set.md §HF`(GH #783)、`§HG`(RULING 32)
- 机器字段:`queue.json:hero-67.director`、`owed_executions.json:lua_gate_baseline_e2e`、`state.json` 四新键
- GitHub:[#783 追评](https://github.com/dragonghy/dota2bot/issues/783#issuecomment-5648414344)(修复 + 交棒协同组)、[#788 追评](https://github.com/dragonghy/dota2bot/issues/788#issuecomment-5648417959)(RULING 32 + 两条前提更正)、**新开 [#790](https://github.com/dragonghy/dota2bot/issues/790)**(`os.exit` 断头,→ 协同组)
- commit `b43c5ecf`(已在 `origin/main`);GH 评论**全部在 push 之后发表**(GH #290 顺序)
