# director 2026-09-03T13:06Z — trunk red 修掉了(第七轮,`test_detector_source_constants.py`)

**一句话:** 连续六轮被报告、从没被修的那条 trunk red,本轮修了 —— 三个 HP 常数
(`illumove_pairs:HP_CUT`、`wandbleed_trigger:HP_MAX`、`wandbleed_trigger:HP_MIN_EXCLUSIVE`)
**全部登记为 MIRROR 并各自钉到源站点**,三只变异体分别被打死。零 Lua diff。

---

## ① 开工自检(铁律 10)

第一条命令**又一次**误用管道被 rc 守卫拒掉(`REFUSED: stdout is a pipe`,
同形第十一次,守卫按设计开火;本轮记在这里是为了让计数继续可读)。改走
`> file; echo EXIT=$?` 后:

- 主体腿跑完,**`EXIT=124`** —— 600s 超时打断在 `=== trunk health (fast Lua detectors) ===`
  这条腿上。⇒ **自检整体退出码本轮不声称**;Lua 快检测器腿**这一侧本轮没人看过**。
- 已跑完的腿逐字:
  - `unlanded work`:**OK,certifiable window 内无未落地工作**。
  - `cadence`:**2 findings**,`hero` **3.8h**(09-02T22:54Z → 09-03T02:45Z)、
    `strategy` **4.0h**(09-02T16:25Z → 09-02T20:25Z),门限 3.5h,越线 0.3h / 0.5h。
    **不升级**(与上一轮同型;触发抖动,非停摆)。
  - `un-ruled queue`:**`strategy-39`(bundle=`wandbleed2`)`status=open ruled=False`**,
    路由/排期裁定仍欠。RIDESHARE 分类腿:none。
  - `domain watch`:`strategy-33 tiny` 语料 0,**hold 仍站得住**;18 条 UNKNOWN STATUS
    (词汇漂移,GH #317 口径计为 open)。
  - `orphan admission`:ORPHAN_PROPOSAL none;3 条 ROWLESS(§BR/§BT/§BV,信息项)。
  - `owed executions`:none。`expired waits`:6 份章程 70 个已结 id,**无过期等待**。
  - `stable anchors`(铁律 3):`stable-v1`/`stable-v2` 两个锚 **EXISTS/PINNED/SHIPPED 全 ok**。
  - `trunk health (python)`:**`81 passed, 1 failed, 2 uncertifiable`** —— 红的是
    `tests/test_detector_source_constants.py`(2 个 uncertifiable 照旧是
    `test_rc_wrapper.py` / `test_selfcheck_lua_leg.py`,GH #364 家族)。
    工作树干净(`git status --short` 空)⇒ **这一次「工作树红」就是「main 红」**,
    不需要 stash 去区分。

## ② 本轮工作单元:把那条红修了(不是又报一遍)

**为什么这是本轮该做的事。** 上一轮的「下次触发」清单第 ① 条就是这条红,而它在报告里
的形状是「**第六轮原样复发**」。六轮**报告**、零轮**修复** —— 自检把它端上来了,
铁律 6 的门也拦得住,**中间缺的是有人动手**。GH #420 本身**已于 09-02T22:38Z 关闭**
(那是汇总块少一个桶的 harness 缺陷,已修),所以这条红**没有 issue 在管它**;
「GH #420 家族」是复发时的引用习惯,不是一个还开着的所有权。

### 红的内容(逐字)

```
FAIL illumove_pairs:HP_CUT is classified (UNREGISTERED)
FAIL wandbleed_trigger:HP_MAX is classified (UNREGISTERED)
FAIL wandbleed_trigger:HP_MIN_EXCLUSIVE is classified (UNREGISTERED)
3 FAILED
```

`HP_CENSUS` 的守卫按设计开火:两个检测器模块带着新的模块级 HP 常数落地,
**没人说这些数是什么**。这与该文件里 [director 2026-08-29] 那条注记**逐字同型**
(`wandlimbo_domain:HP_FRAC` 当时也是随模块落地、未登记)。

### 三条都是 MIRROR,不是 INDEPENDENT —— 各自的源站点是读出来的

每一个常数**在自己的注释里就点了一个源子句的名**,而点了名的镜像声明**只是散文**,
不是镜像。逐条查源:

| 常数 | 值 | 源站点(本轮读到) | 源侧值 |
|---|---|---|---|
| `illumove_pairs:HP_CUT` | 0.40 | `bots/FunLib/minion_lib/illusions.lua` `X.ConfuseEnemyWithIllusions`:`J.GetHP(bot) < 0.4` —— **`illureal` 打开的那条分支** | 0.4 |
| `wandbleed_trigger:HP_MAX` | 0.45 | `bots/ability_item_usage_generic.lua` `X.ConsiderItemDesire["item_magic_wand"]` 的 `'wandbleed'` 分支:`nHPrate < 0.45` | 0.45 |
| `wandbleed_trigger:HP_MIN_EXCLUSIVE` | 0.25 | `bots/FunLib/jmz_func.lua` `J.ShouldDrinkWandInLimbo`:`GetHealth() > GetMaxHealth() * 0.25` —— **`wandlimbo_domain:HP_FRAC` 镜像的同一个字面量** | 0.25 |

第三条尤其不是巧合等值(**与两行之下 `detect:LIMBO_HP` 那条「碰巧同为 0.40」相反**):
`wandbleed_trigger.py` 的 `shared_with_wandlimbo` 这一桶**在定义上就是 wandlimbo 的域**,
源侧的 0.25 一动而这边不动,施法就会被归到错误的解释下,**"exclusive" 这个计数不再 exclusive**。

### 落地物

1. `tests/test_detector_source_constants.py`:三条 `HP_CENSUS` 行(全 MIRROR)+ **section 2 的钉子**。
2. `tools/batch_test/behavioral/illumove_pairs.py`:`HP_CUT` 从「this tool's own constants.
   **NO GATE READS THESE**」那个表头底下**搬出来** —— 它压根不属于那一类,而那句表头正是
   它当初没被登记的原因;给它单独的 MIRROR 表头并点名源站点。
3. `tools/batch_test/behavioral/wandbleed_trigger.py`:两个常数的注释改写为**指名源站点**的镜像声明。

### 钉子的实现细节(一个值得留档的障碍)

`X.ConsiderItemDesire["item_magic_wand"] = function( hItem )` 是**赋值不是 `^function`**,
`source_constants.function_body()` 够不着它。⇒ 用**注释剥离后按 gate id 锚定**的正则
(即本文件上方 `TD_GATE` 的既有手法),而不是按位置取「第一个 `nHPrate <`」——
这个函数里 `nHPrate <` **出现在好几条分支上**,「第一个」正是 `where=` 存在的理由。
顺带把「`'wandbleed'` 在该文件里有且只有一个门站点」也断言了。

## ③ 变异台(证据纪律:钉子必须能红)

三份源文件先 `cp` 成副本,逐个变异、跑测、**从副本恢复**:

| 变异 | 结果 |
|---|---|
| M1 `illusions.lua` 0.4 → 0.35 | `FAIL illumove_pairs.HP_CUT mirrors the confuse-branch owner-hp clause (got 0.4, want 0.35)`,**裸退出码 1** |
| M2 `ability_item_usage_generic.lua` 0.45 → 0.5 | `FAIL wandbleed_trigger.HP_MAX mirrors that ceiling (got 0.45, want 0.5)` |
| M3 `jmz_func.lua` 0.25 → 0.30 | **两条一起红**:`wandlimbo HP_FRAC …` + `wandbleed_trigger.HP_MIN_EXCLUSIVE …`(正是共享字面量应有的形状) |

恢复后 `git status --short` **只剩本轮三个有意的改动**,`RESTORED_EXIT=0`。
⇒ 三只变异体全部被打死,且**每一条都点名了正确的站点**。

## ④ 铁律 6

- 静态半:`bash tools/agent/luacheck_gate.sh` → `installed luacheck (apt 包名 lua-check)` /
  `luacheck bots game: 0 warnings` / **`GATE_EXIT=0 CLEAN`**。未用 `RULE6_BYPASS`。
  推送门已上膛(`arm_push_gate.sh`:`core.hooksPath=.githooks, already set`)。
- 动态半:**本轮 diff 零 Lua**(三个文件全是 Python)。`lua5.1 tests/run_tests.lua`
  仍后台起跑并跑到本轮收尾,**未跑完 ⇒ 不声称**。它不是本次改动的门:
  改的是检测器与检测器的测试,**门是 `python3 tests/test_detector_source_constants.py`,
  裸退出码 `EXIT=0`**(修前 `EXIT=1`)。

## ⑤ 没做的、和为什么

**`strategy-39`(`wandbleed2`)本轮不裁。** 自检点名它欠裁定,而本档案里一条
`ROUTED_RIDESHARE` 裁定的标准是**六到七项各自带裸退出码的独立读数**
(见 `strategy-37`/`strategy-38` 的 `director` 字段)—— 那是一个**完整工作单元**,
不是顺带。半个裁定比没有裁定更坏。**它留在下次触发的第 ① 位。**

**顺带买到的一项预检(留给下一轮,省一步):**

```
IsSoakCandidate( 'wandbleed2' ) 代码站点:jmz_func.lua:9585  —— 有且只有一处
每条门行上的 id 数:jmz_func.lua:9585 = 1 | ability_item_usage_generic.lua:3418 = 1
```
⇒ **`wandbleed2` 单一独立门,不合取第二个 id ⇒ 掉不进 pullcad 陷阱**(裁定项 (i) 已备好)。
⚠️ 这**只是** (i);(ii) 关闸逐值等于出厂、(iii) armed 零新增 API 面、(iv) 一次一个杠杆、
(v) id 串两道门、(vi) 独立重跑 —— 五项**本轮都没读,一项都不声称**。

**GH #436 不是这条红。** 本轮为确认所有权读了它:#436 是
「`J.GetNearbyHeroes` 不滤幻象而 `wandlimbo_domain.py` 的 LIMIT 5 写反了」,
是另一个缺陷,**仍然开着**,与本轮的常数登记不冲突也不被它关闭。

## ⑥ 成本

**零 AWS 调用**,未跑 `session_setup.sh`,未碰 EC2/S3。围栏 / 刹车 / $50 批准线均未触及。
本轮无邮件(决定类每周 1 封的额度本周已由 08-31 那封用掉);`DECISIONS_NEEDED.md` 无新增
(本轮没有需要 owner 拍板的分叉 —— 那条红是执行欠账,不是决策欠账)。
GitHub:**本轮未新开也未关闭任何 issue**(#420 已关且不是这条红的所有者;
开一条随即自关的 issue 是噪音)。四组均有产出,无空转。

## ⑦ 下次触发

1. **裁 `strategy-39` / `wandbleed2`**(自检连续点名;(i) 已在上面备好,补 (ii)–(vi))。
2. **自检的 Lua 快检测器腿本轮 EXIT=124 没跑完** —— 下轮先把这一侧看掉(给它更长的超时,
   或单独跑那条腿);**本轮那一侧「没人看过」。**
3. 自检 `RIDESHARE` 分类腿(第五轮顺延)。
4. GH #410 的带期限保守默认;GH #285 第三十三轮 / patch 缺口 P3。
5. GH #436(幻象口径横向排查)仍无执行者 —— 若下轮仍无人接,写进 `DECISIONS_NEEDED.md`。

## ⑧ Token 用量

见下方回填。
