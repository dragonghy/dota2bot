# 总监 2026-09-11T16:16Z — RULING 14:`ohnum` 退回协同组

**一句话**:`ohnum` 不入集,**定罪的不是那一帧,是「把圈子扩大只多出队友、一次都没多出敌人」** ——
20 次拒绝里 6 次是圆心画错造成的,而 `recenter_enemy_gain` 读 **0**;
连带 `ownhalf` **阻断解除但不促进**(理由换成它自己的分母),并退休一条验收句已被树满足的 owed 行。

**本轮**:零 AWS(**一次调用都没有**)、零波次、`bots/` + `game/` **零 diff**、armed 串 **30 一字未动**、
**不发 owner 邮件**、无 promote、无 reject、无入集。
**判定完结 1**(`ohnum`)+ 1 条 queue 裁定(`hero-59`)+ 1 条 owed 行退休(`ownhalf_condition_a_or_return`)。
⛔ **不达 owner P4.2 的「≥2 次 promote/reject 判定完结」,不粉饰不抵账** —— 见 §6。

- 全文档案:`iterations/streams/test_set.md §GX`(§GX.1–§GX.7)
- 机器键:`state.json:ohnum_RETURNED_20260911`
- 登记:`owed_executions:ohnum_recentred_readmission` / `ownhalf_promote_bar_thickness`
- 线程:GH #706(录像组提的那条「圆圈问题」;本轮是它的答复)

---

## 一、⭐⭐⭐ 主轴:一个估计量错得**有方向**,这件事本身就是判据

`J.ShouldRefuseUnsupportedPunish` 把**两边的人头都数在以目标为圆心**的 1200 圈里
(`bots/FunLib/jmz_func.lua:9299` 取 `vLoc`,`:9315` 我方,`:9322` 敌方)。

出厂域里这个圆心**是免费的**——目标按构造贴着我方某座活着的建筑,我方集群本来就在附近。
而 `ownhalf` 的**全部意义**就是那块没有建筑的死区。扩域那一手把建筑拿走了,
**却把跟着建筑一起成立的圆心原样带了进来**。

本轮给 `tests/_ohnum_sweep.lua` 加了一条腿:把圆扩成「目标圈 ∪ 本体圈」**对称地**重新数两边
(更大的圈**也被允许**多找出敌人),全语料 **111 fixtures / 1031 live hero frames**:

| 量 | 读数 |
|---|---|
| `pd_shipped` | 28 |
| `pd_ownhalf` / `pd_ownhalf_only` | 60 / **32** |
| `domain_parity` / `domain_advantage` | **20** / 12 |
| `both_changed` / `both_to_nil` / `both_switched` | 20 / 20 / **0** |
| `ohnum_alone_fires` / `ohnum_alone_changed` | 28 / **0** |
| ⭐ `recenter_flip` / `recenter_held` | **6** / **14** |
| ⭐ `recenter_ally_gain` / `recenter_enemy_gain` | **6** / **0** |

**那个 `0` 是全部的重量。** 半径不够大的估计量扩圈时会**两边都多** —— 那是噪声,是可以谈调参的;
这一个扩圈时**只多出我方**,于是它犯的每个错都指向同一边(拒绝)。

📌 *一个估计量的错误如果有方向,它就不是一个待调的阈值,而是一个对世界的误读 ——
而「有没有方向」是能用一次对称的重算问出来的,不必等一场对局告诉你。*

⚠️⚠️ **诚实的另一半,和上面一句同等重量**:**14/20 的拒绝扛过了重新居中** ⇒
`ohnum` **不是全盘伪影**,它七成的拒绝是真的「附近哪儿都没人」。缺陷占它域的**约三分之一**。
⇒ 裁定写成 **RETURNED(估计量坏,论点成立)**,⛔ **不是 reject**。

## 二、那一帧,以及一个被钉住的近似值

`f_20260909_212625_lion_235`(录像组 GH #706 承重帧):`ownhalf` → `drow_ranger`,`+ohnum` → `nil`。
目标为圆心 **1 v 1**;本体为圆心 **2 v 1**;丢掉的队友是 **lina,距本体 649.4u、距目标 1472.5u**。
同一份录像的地面真相:**13 秒 2 杀 0 死**。

⭐ **更正 GH #706 一个数,不改变对方结论**:issue 记 lina 距目标 **~1300u**,落地 fixture 实测 **1472.5u**。
值得更正的理由是它**从今天起被一个测试钉着**,而**一个钉住的近似值会被下一个人当精确值引用**。

## 三、⭐⭐ 这份判据所依赖的那个数,今天才第一次有测试站在它后面

- `tests/test_ohnum_refusal.lua` 新增 `[frame] the 2-kill refusal is produced by where the circle is drawn`(该文件 **9/9**)。
- `tools/agent/mutstand_ohnum.sh` 新增 **M8**:把人头数里**我方那一半的圆心从目标挪到本体**。
  终读 **8/8 CAUGHT**,baseline 绿(10 tests / 0 failures),M8 由新用例 **第 223 行**抓住。

**M8 存在的理由不是覆盖率**:没有它,「这一帧的拒绝是圆心画在哪儿造成的」就是**散文** ——
整条裁定压在 `recenter_flip 6/20` 上,而在 M8 之前**这套测试分不出「圆心在目标」和「圆心在本体」两棵树**,
那个数背后空无一物。

⛔ **明确写进裁定的「不接受的修法」**:把半径 1200 放大(2000 之类)。
它动的是同一个圆的**边**不是**圆心**,会对称地把敌人也数进来(实测的单边性说问题不在那里),
而且 **M8 分辨不出它**。

## 四、顺手撞到的一处陈旧读数(小,但它正是被引用的那一半)

`tests/test_ohnum_refusal.lua` 头部那列 09-09 复核过的语料数,**写下来的时候已经陈旧**,且原因不神秘:
语料 09-10 长了**一个** fixture(正是录像组自己的承重帧),它贡献**四帧域内**,**四帧全部 parity 拒绝、全部 both_to_nil**。
`pd_ownhalf_only 28 → 32`、`domain_parity 16 → 20`、`both_changed 16 → 20`;
出厂那几列(`pd_shipped 28` / `ohnum_alone_fires 28` / `ohnum_alone_changed 0`)**逐位未动**。本轮已改。

📌 **窄的那条教训**:这个文件**把每一个阈值都从出货源码里解析出来,却把每一个派生计数硬写在散文里**
—— **于是会烂的恰好是读者会引用的那一半**。

## 五、⭐ 连带:`ownhalf` 阻断解除,**挡它的东西换了一个**

14:00Z 挡它的原话是「一条未裁定的姊妹候选会改写它 **75%** 的域」。**两半今天都动了**:
姊妹已裁(不入集);那个 75% **量在收窄前的语料上**,本树是 **20/32 = 62.5%**,其中 **6 条是居中伪影**。

⇒ 此后挡它的**是它自己的分母**:(a) 只有 **1 局 7 帧**,而**录像组自己的报告逐字写着这低于章程 ≥6 局下限**。

⚠️ **上一轮那个顾虑今天可以划掉**:`ownhalf_margin`(800 → 1600)确实骑在同一道闸上,
但录像组那 7 帧**跑在 1600 已落地之后的树上**(承重帧两敌 invade depth **2885.7 / 3235.7**,都远过 1600)
⇒ **(a) 的取证对象就是带门限的那棵树**,门限不必再单独买一次证。

⭐⭐ **退休一条 owed 行,而退休它的是它自己点名的那把尺子**:
`ownhalf_condition_a_or_return` 的验收句是「`verify_coverage.py` 里 `ownhalf` 的 verify 列 > 0,或它出集」。
本轮实读 `armed ids: 30 / ids with >=1 machine-readable VERIFY line: 26`,verify=0 表里只有
`overchase` / `tpcommit` / `blinkflee` / `lf_rescue` **四条,`ownhalf` 不在其中**。
**并且本行的 `trigger` 今天也独立地响了**(「或 `ohnum` 先行退集/promote 之时」)。

⚠️⚠️ **退休它 ≠ 可以促进**:本行问「(a) 买到了没有」,答案是买到了;
下一个问题是「**买到的那份够不够厚**」——**另一道门、另一个分母**。
⇒ 继任行 `ownhalf_promote_bar_thickness`,验收要求 **episodes 来自 ≥6 局**
(⛔ 不是 ≥6 帧 —— 09-10 那次是 **7 帧 1 局**,正是它要区分开的那个数)。
⛔ 并注明:§FZ.5 的**旧留集理由**(「它是 `ohnum` 域的唯一使能者」)**今天作废**,依赖的那头已不在集里。

## 六、⛔ 判定完结 1,不达 ≥2,**第二格没做的理由是具体的**

促进候选只剩 `ownhalf`(WORKING 7)与 `wandbleed`(1–2,太薄);`zusult`/`zusboltdom` 上一轮已促进,
`lionqdmg` 被 `promote_atoms:impale_kill_needs_damage` 挡着不是候选,`fieldbuy` 等 GH #734。
⇒ **本轮唯一可能的第二格就是 `ownhalf`,而本节第五条正是不促进它的理由**:
1 局 7 帧,**低于章程下限,且这一点是取证方自己登记的**。
⛔ **不拿「阻断解除」冒充一次 promote**,也不把 queue 裁定与 owed 退休记进那个计数 —— 它们不是 promote/reject。

## 七、⚠️⚠️ [harness] 本轮最该被下游读到的一条:**促进会顶红点它名的测试,而预防规则只写了「门」**

自检 `trunk-red(python)` 读 **119 passed / 4 failed / 3 uncertifiable**,**四条里三条是最近两轮 promote 的直接连带**:

| 红的文件 | 它在说什么 | 根因 |
|---|---|---|
| `tests/test_carrier_terms.py` | `zusult` / `liondrainstop` 「still hero-scoped off the tree」 | 两条刚被促进,`bots/` 里门没了 ⇒ `derive_id` 返回 `unresolved` |
| `tests/test_detector_source_constants.py` | `tpdeathbuy` arm 不再是 `gate then bDyingWithDoomedGold = botHP < X` | `tpdeathbuy` 今晨被促进,门形状消失 |
| `tests/test_py_gate.py` | 5c:selected total 12.03s vs 记录预算 12.0s | 已登记 `owed_executions:py_gate_budget_premise` |

⭐⭐⭐ **这是 `pullcad` 陷阱的第三种极性,而 AGENTS.md 那条预防规则逐字读不到它。**
规则原文管的是 **OTHER GATES' conditions** —— 而这三条红的是**测试与检测器**。三种极性并排:

- **正像**(`pullcad`):促进 X ⇒ 写着 `IsSoakCandidate(X) and IsSoakCandidate(Y)` 的**门**冻成恒假。**静默**。
- **倒像**(§GT):一条看起来像门的行其实不支配它下面的东西。**误报**。
- ⭐ **本节这一种**:促进 X ⇒ 一条**断言 X 的门存在**的测试变红。**响亮,但响在下一个开工的组身上** ——
  逐字是 GH #624 的立案形状,只不过 #624 是「推的人的闸里没有 Lua 测试」,
  这里是「**闸里有 python,但这几条不在 12s 预算内**」(§GU.3 量到的那把旋钮,**今天第二次收到账单**)。

⛔ **本轮不修那三条,理由具体**:把「断言门存在」改绿最省事的写法是**删掉那条断言**,
而那恰好是「**退休一条 id 不许连带退休守着它的断言**」——`test_carrier_terms.py` 的注释块里**逐字写着这句**。
改对的形状是给它**第三条分支**(armed / 已退回 / **已促进**),而「已促进」那一支要从
`PROMOTED (was soak-candidate '<id>')` 的注释现场去要 hero 归属 —— **要安静容器 + 它自己的变异台**。
⇒ 开 issue + 登记,**不在本轮顺手改**(「让仪器工作挤掉判定完结」是上一轮举过手的失效形状)。

📌 *一条规则把它防的那个东西写成了「门」,于是同一个缺陷换成「测试」就从它下面走过去了。*

## 八、铁律 6 与自检

- **铁律 6 三条腿,全绿,无 `RULE6_BYPASS`**:
  - `GATE_EXIT=0  CLEAN (iron rule 6 static half passed)` — `luacheck bots game: 0 warnings`
  - `py gate: 96 ran, 0 findings, 0 uncertifiable, 38.8s`
  - `lua gate: 337 ran, 0 findings, 0 uncertifiable, **12 known-red**, 434.4s`
  ⭐ `known-red` **13 → 12**。三条都用 `rc.sh` 读的裸退出码(各 `RC_EXIT=0`),**没有一条经过管道**。
- **自检真码 `SELFCHECK_EXIT=3`**,`legs run 12`;
  `FINDINGS: cadence queue-rulings owed-executions trunk-red(python) trunk-red(lua)`;
  `UNCERTIFIABLE: none`;`NOT RUN (inside a leg): test_lua_gate.py / test_luacheck_gate_soakswitch.py / test_selfcheck_lua_leg.py`。
  ⭐ 其中 **`queue-rulings` 本轮已清掉**(裁 `hero-59` 后 `pending_rulings.py` 由 exit 3 → **exit 0**,实测)。
  `trunk-red(lua)` **2/87**(`test_lion_ult_reserve_domain.lua` / `test_stayfield2_marginal_domain.lua`),族属 GH #718/#650,**不开新单**。
- ⚠️⚠️ **必须披露的一条污染**:自检跑了约 16 分钟,**而我在它扫描期间改了三个文件**
  (`tests/test_ohnum_refusal.lua` / `tests/_ohnum_sweep.lua` / `tools/agent/mutstand_ohnum.sh`)
  —— **逐字是 GH #704 / 录像组 09-10 那条「并发写者在扫描中途弄脏工作树」的形状,而这次的写者是我**。
  ⛔ **不因此把读数作废,但也不假装没有**:两条 lua 红都不是我碰过的文件,
  四条 python 红各自有独立的、与我的改动无关的根因(见第七节)。**下一轮开工自检必须在动工前跑完。**
- ⛔ **开工第一条命令又撞管道拒绝门** —— `routine_selfcheck.sh` 的 §22 守卫当场拒绝并打 `SELFCHECK_EXIT=2 REFUSED`。
  **`rc.sh` 是习惯不是门(第十六次)。**
  ⚠️ 并且**同一个缺陷本轮又犯了第二次**,形状更朴素:`python3 tests/test_carrier_terms.py | head -40` 读回 `RC=0`,
  而 `rc.sh` 读真码 **`RC_EXIT=1`** —— **一个 `head` 的成功被读成一个失败测试的通过**。
  ⇒ 两发都发生在**同一轮、同一个人身上**,一发在开工、一发在中途。**这条确实是习惯不是门。**

## 九、🩺 体系健康与成本

- **巡检:五组全部在最近 3.5 小时内有产出,无掉队组** ——
  batch-desk **15:14Z**、strategy **14:18Z**、hero **14:07Z**、replay-check **12:59Z**。
  ⛔ **本节我先写错了一次,照实记下**:我按一份**按 mtime 排序**的目录列表读成
  「replay-check 最新是 09-10T09:54Z、落后约 30h」,并已经把「若下一轮仍无产出就升级」写进了草稿。
  按**文件名**(即 UTC 时间戳本身)重排后,真值是 **09-11T12:59Z**,差 **27 小时**,
  **结论方向完全相反**。⇒ 巡检读报告节奏**只许按文件名排序**,mtime 是 checkout/rebase 的产物不是交付时刻。
  ⭐ **而且看对之后的内容比看错更有用**:replay-check 12:59Z 那轮
  (`abilanc`,`VERIFY … SILENT episodes=122`)**宽扫 85/85 局、深查逐帧 6 局并逐字写着「章程下限达标」**
  ⇒ **≥6 局对他们是够得着的**,所以 `ownhalf_promote_bar_thickness` 要的那份取证
  **是一次排期问题,不是一次能力问题** —— 这一点值得写进交棒。
- 💰 **本轮 AWS 调用 0 次,不作 MTD 新声称**;三条线未改(围栏 $60 / 刹车 $90 / 批准线 $100)。
- **armed 30**,离 P4.2 解冻线(≤20)**还差 10 条**,本轮未动。
- ⚠️ owner **P4.1 标尺波**仍未认领,挂 `DECISIONS_NEEDED` 第 16 条;
  第 17 条(铁律 2(b) 与退化胜负通道)本轮新立、**不另发信**,随第 16 条那一封走。

## 十、下次触发

1. ⭐⭐⭐ **判定完结,主体,≥2**。⚠️ **但 `ownhalf` 这一格今天已经用掉理由了** —— 它要的是
   **录像组一份 ≥6 局的 (a)**,不是总监再读一次同一份 7 帧。⇒ 若下一轮录像组仍未交,
   **第一格应当是别的东西,而不是把 `ownhalf` 再判一次「还不够厚」**(那会变成一条每轮复述自己的裁定)。
2. ⭐⭐ **第七节那条 [harness]**:给 `test_carrier_terms.py` 第三条分支(armed / 已退回 / **已促进**),
   并把 AGENTS.md 那句「grep OTHER GATES」**加宽到「id 在 `bots/` 之外的一切引用」**。
   ⛔ **要安静容器**;⛔ **不得再挤掉判定完结**。
3. ⭐ `py_gate_budget_premise`(**与第 2 条同一把旋钮,应当合并成一个工作单元**)+
   `consequence_polarity_census` + `pullcamp_atom_readmission` 与五条 `a_evidence_*`(**第五次顺延**)。
4. 存量:GH #523 / patch 缺口 P3 / 账户级预算过滤器等 owner /
   **`rc.sh` 是习惯不是门(第十六次,且本轮一轮两发)** / 「后台包装吞真码(第二十次)」。
5. **本轮裁定的 GitHub 追评**(GH #706)—— 按 GH #290 留到 push 之后;
   ⛔ 若 MCP 要审批,按铁律 11 **立刻放弃**并留到下轮。

## 十一、token 用量

(回填见文末)
