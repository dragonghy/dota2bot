# 总监 2026-09-06T13:20Z — 本项目第三次 promote(`slotwait`),而最该被读的是它旁边那条构造性裁定:**全开镜像波测的那个配置,永远不可能被 promote**

**一句话**:连续三轮顺延的那一项(W50 家族裁定)本轮结清 —— 结果不是「这一波数字不够好」,
而是 **measured set ≠ promotable set**:条件 (a) 是逐 id 的、条件 (b) 是家族级的,
**两者的交集从来不是被测的那个配置** ⇒ **「连续两周零 promote」的归因是波次配置,不是「改动都不好」**。
同一轮按这条裁定的另一面**真的 promote 了一个** id(`slotwait`,`stable-v3`),
并把三条 **(a)=BUGGY** 的 id 退出集合(它们污染的正是 promote 唯一能引的那个量)。

**判定完结数(owner P4.2 的产出指标):4**(1 promote + 3 退回出集)。armed 集 **59 → 55**。

---

## 一、开工自检(铁律 10)

⚠️ **第一条命令又误用管道(同形第三十四发,守卫第二十五次拦下)**:
`routine_selfcheck.sh 2>&1 | tail -60` 被脚本逐字自拒(`REFUSED ... exit 2, nothing checked`),
**harness 报的 `EXIT=0` 是 `tail` 的**。改重定向 + 后台重跑,**读脚本自打的那一行**:

```
legs run              : 10
FINDINGS (exit 3)     : cadence queue-rulings owed-executions trunk-red(lua)
UNCERTIFIABLE (exit 2): trunk-red(python)
NOT RUN (inside a leg): tests/test_rc_wrapper.py tests/test_selfcheck_lua_leg.py
selfcheck worst exit: 3
```

逐条:

- **`queue-rulings`** —— `RIDESHARE 1`(`hero-37`)。**本轮裁掉了**(第五节),复跑 `RC_EXIT=3 → 0`。
- **`owed-executions`** —— 仍是 `campgrade_wave_type_premise_reading`(批测台,第六轮欠着,理由是钱)。
  本轮**新增一行**(`roshan_pit_daynight_fix`,第四节)。
- **`cadence`** —— 2 条 `GAP cadence strategy`,不是本座。
- ⛔ **`trunk-red(lua)`:`test_midsupfar_yield_target.lua` 红**
  (`live hero frames FELL to 1009 (registered 1012)`)。**本轮裸读复现失败**:
  `lua5.1 tests/run_tests.lua midsupfar` **13 tests / 0 failures,`RC_EXIT=0`**,
  且复跑时树上**已经带着本轮全部改动** ⇒ **不是本轮改动引起的**,形状属 GH #526 那一族
  (「只在全量/子进程里红,单跑全绿」)。**登记,不重新 baseline**(那条测试自己写着别 re-baseline)。
- ⛔ **`trunk-red(python)` `UNCERTIFIABLE`** —— 第 `5a0` 腿仍骑在 `120.1s (budget 120s)` 刀口上
  (GH #548,**第五个独立读数,仍未结清**)。**「没跑成」不是「绿」**;python 侧的读数改由本轮
  自己跑的那几套单独出具(第七节)。

## 二、⭐ `slotwait` PROMOTE —— 本项目第三次,距上一次 14 天

三条件、各自的边界、落地动作与全部读数写在 **`test_set.md` §FK.1**,机器键
`state.json:slotwait_PROMOTE_20260906`,锚点 **`stable-v3`**(`iterations/stable_anchors.json`,
`stable_anchors.py` 本轮 **3 anchor(s) checked -- OK**)。这里只留三句最容易读错的:

1. **(b) 是家族级的,而且这句话写进了代码注释本身**。armed 于 W47–W50(~700 局计分),
   家族 gpm `-5.95 / +27.25 / +11.70 / +13.76`。全开波**分摊不了归因**,
   而 winrate **一个可引的读数都没有**(GH #352 第九波 DEGENERATE)⇒
   铁律 2(b) 要的「粗粒度的没有明显负面」**是这个,且仅是这个**。
2. **promote 的动作是删那句门,不是删串里的 id**(§DU.6 红线)。本轮**代码先改、串后改、同一个 commit**;
   ⭐ 而**没改 `utils.lua` 的两句 `@param` 时,`test_gate_claim_consistency.lua` 当场变红**
   (`utils.lua:2160 names gate 'slotwait', which nothing in bots/ wires`)——
   **那条棘轮在本轮真的挡了一次**,over-claim 方向,这是它立案时写的第二种漂移。
3. **`pullcad` 陷阱不适用,是查过的**:`slotwait` 全仓只出现一次、不与任何 id 合取、不属任何共同 promote 原子
   (`promote_atoms.py` 19 checks 0 failures)。

**下游钉子同轮翻面**(promote 的隐性成本,值得单列):
`test_slotwait_cooldown_scan.lua` 的结构测试从「必须有门」翻成「**必须一个门都没有**」——
**承重的是 id 的缺席**(promote 后再长回一个门 = 真实对局里恒 inert,而所有 armed-wiring 检查照样读作干净);
`test_slotwait_domain_liveness.py` 第 1 组同理;`mutstand_slotwait.sh` **M4/M5 重新锚定**
(锚点对不上的变异体什么也没改,而台子照样打结论 —— GH #550)。重锚后 **9/9 CAUGHT**。

## 三、⭐⭐⭐ W50(59-id 家族)裁 **NOT-JUDGED**,理由是构造性的

数字先给全(`W50_wave.json:harvest`,187 局,四粒全过 #269 深度门):
gpm 均值 **+13.76**、`comps_better.gpm` **2/4**、工具 `suggested=hold_or_reject`。
**离散度才是那句话**:逐粒 `-39.01 / -26.67 / +1.08 / +119.64`,**去掉 6072 剩三粒均值 `-21.53`**
⇒ 按 4(i-c),说精度的是 arm 自己的跨种子离散度,而它宽到**均值不构成效应**。
四个量 `sign_flip=true` 按 4(i-a) **登记**、按 4(i-c) **不作否决理由**(那是恒等式);
侧项 `-145.37` 且相对 W49 反号,ab:ba = 131:56 ⇒ 按局加权并池会注入 `0.40 × (-145.37)`,正是 4(i-d) 禁的那一步。

**构造性的那一半**(`verify_coverage.py`,**GH #557 修后版本**,59 个 id):

| | 数量 |
|---|---|
| 有机器可读 VERIFY 行 | **31** |
| 连一行都没有 | **28** |
| 其中 WORKING | **9**(`ckpush` `slotwait` `odbuild` `fieldsip` `towerfear` `slotpush` `wandbleed2` `wandbleed` `illumove`) |
| 其中 BUGGY | **3**(`midtp` `suptp` `roshdist`) |
| 其中 INDETERMINATE | **19** |

⇒ promote 那 9 个 = 交付一个**从没被测过的配置**(9 开 50 关);promote 全部 59 个 = **50 个没有 (a)**。
**这就是零 promote 的机械理由**,上一轮台账里那句「观察,非裁定」本轮升为裁定。
⛔ **不是「(b) 买不到」** —— 08-31 §CT ③ 仍然成立,本轮 `slotwait` 就是走这条路 promote 的。

**裁定**:NOT-JUDGED;**不重发同构的波**;逐 id 的通道是 (a),而 (a) **不需要新的波**
(已有 dump 就够,`stayattr` 09-05 的先例)。投递:`batch-desk.md` 接口节首条(第六节)。

## 四、三条 (a)=BUGGY 退集 + 一条掉不了的棒

`midtp`(#539)/ `suptp`(#545)/ `roshdist`(#450),**59 → 55**。
**§FB 那两条是「(a) 从没人买过」,这三条是「(a) 买到了,答案是 BUGGY」** —— 形状不同,不要混。
留着的代价可算:(b) 是家族级读数,一个已知咬错的 id 在每一波 armed 腿上执行 ⇒ **污染的正是 promote 唯一能引的量**;
`roshdist` 尤甚(armed 腿拒掉 **167 个(局,英雄)对 / 274 帧**站在真坑里的 bot)。

⭐ **退集不修根因,所以根因单独登记**:GH #450(`J.GetCurrentRoshanLocation()` 昼夜映射反了,
**77/77,0 例外**,**没有门**,25 个调用点在每一场真实对局里读它)是一条 **live 的 [bug]**,
按铁律 5 归总监,自 09-03 立案**三天零推进**;而 `roshdist` 一退集,**再没有任何 id 行能载它**
⇒ 按 §DR 进 `owed_executions.json`(`roshan_pit_daynight_fix`,`done_when=path_exists tests/test_roshan_pit_daynight.lua`)。
⛔ **修法不是直接对调就完事**:#352 指向的那 23 个 commit 正是「看着显然的非门控修复」把侧别平衡整体挪动过一次
而没有任何东西举手 ⇒ **按 `gated-fix` 落新 soak candidate**,用 #450 自带的 `roshdist_domain.py`(比坐标不比名字)买 (a)。

## 五、GH #352 第九次 DEGENERATE —— 结案(作为交棒项,不是作为缺陷)

W50:少数侧 **1/211 = 0.0047**(历次最差)、`winner_by engine_natural` **211/211**、`headroom 0.0096`。
⭐ **这一波按构造排除了最后一个替代解释**:语料完全金钱独立 ⇒ 横扫**不可能**是 `econ_winner` 改写的产物,
而那正是 #352 立案时留的口子。裁定三条见 §FK.4:**不再每波上交**、**(b) 不因此冻结**、
**剩下那半个问题改名**为机器人质量/侧别对称性(#352 保持 open 承载归因,验收条件不变)。
⚠️ **一条诱人的假说本轮当场自否**:`slotwait` 修的正是一个按侧不对称的缺陷(天辉 4/5、夜魇 1/5),
而横扫方向恰是夜魇 —— **但 onset 检验立刻否掉它**:这个索引缺陷自 OHA 快照起就在,
**解释不了 08-25→08-27 之间才出现的拐点**。登记为「**不是解释**」,不是「待验证」。

## 五之二、hero-37 裁 APPROVED-SCAN + 一条被点名三轮的规则冲突

`hero-37`(`zusultstrand`,GH #564)与 hero-2/30/31/32/33/34/35/36 同形:**零 EC2、只读档案遍历,不排波、不入集**
⇒ **APPROVED-SCAN**,并进同一次遍历(那条腿的针数 **8 → 9**,`path_contains_all`)。
⭐ **批测台连续三轮指出 `pending_rulings.py` 还在引一条已被废止的条款**,本轮修掉,而修法不是删字:
**owner P4.2 废止的是 §BB.4 的「放行」,不是它的「当轮必裁」** ——
而这个桶自 hero-32 起装的一直是 **APPROVED-SCAN(不是入集)**。
⇒ 工具改打 `RIDESHARE (rule this round; outcome per owner P4.2, not §BB.4)`,
docstring 写清「**这个桶只说欠一条裁定,裁成什么由 P4.2 定**」。`test_pending_rulings.py` **253 checks / 0 failed**。

## 六、投递(裁定要落到被裁方读的那个字段上)

| 裁定 | 投递处 |
|---|---|
| W50 NOT-JUDGED + 不发同构波 + 胜负通道降级 | `batch-desk.md` **接口节首条**(不是叙事节) |
| arm 串 59 → 55 | `test_set.md` **第 2 行**(批测台发波时现读的那一行) |
| `slotwait` promote 三条件 | `bots/FunLib/jmz_func.lua` **函数注释**(promote 后唯一还会被读到的地方)+ `state.json` + `stable_anchors.json` |
| hero-37 | `queue.json:hero-37.director` **机器字段** |
| #450 根因 | `owed_executions.json` 新行(自检第 9 条腿每轮举手) |

## 七、铁律 6 与本轮全部读数

- 静态门:两次 `git push` 各自自打 **`GATE_EXIT=0 CLEAN`**(`luacheck bots game: 0 warnings`,
  门自己装的 luacheck);`ARM_HOOK_EXIT=0`;**未用 `RULE6_BYPASS` ⇒ 无「SKIPPED, not passed」行**。
- Lua:`slotwait` **15/0**、`slot` 家族 **87/0**、`gate_claim` **16/0**、`smoke` **3/0**、`midsupfar` **13/0**。
- Python:`test_slotwait_domain_liveness.py` **30/0**、`test_pending_rulings.py` **253/0**、
  `test_verify_coverage.py` **17/0**、`test_promote_atoms.py` **19/0**、`stable_anchors.py` **3 anchors OK**。
- 变异台:`mutstand_slotwait.sh` **9/9 CAUGHT**(M4/M5 重锚后各自仍咬),还原后基线复跑 15/0。
- 波次侧:`check_armed_wiring.py` 新串 **all 55 armed ids wired on HEAD**;
  `carrier_terms.py` **TERMS 逐字节相同**(8 项),`12/46/1` → `12/43/0`
  ⭐ **那个 `1 unresolved` 是本轮的一条真读数**:promote 之后旧串在这棵树上已解析不了 ⇒
  「代码改了串没改」的半个状态会**立刻变红**,不靠人记得。
- ⛔ **未跑全套 `run_tests.lua`(GH #124,~100min)⇒ 不声称**;`trunk-red(python)` 那条腿 UNCERTIFIABLE,**也不声称**。
- **零 AWS 调用、零支出**(本座不花钱);**本轮不发 owner 邮件**(W36 那封 07:xxZ 已发,周配额已用)。

## 八、下轮交棒

1. ⭐⭐⭐ **GH #450**(`owed_executions:roshan_pit_daynight_fix`):按 `gated-fix` 落新 soak candidate + 钉子,
   用 `roshdist_domain.py` 买 (a)。**这是一条 live 的 [bug],不是欠账美化。**
2. ⭐⭐ **owner P4.2 的第二半:P4.3 规则减法** —— `test_set.md` 本轮又长了 ~9KB(19,239 → 19,3xx 行),
   而 P4.3 要的是 **<50KB**。**这一条一直没人开工**,它本身就是一个完整工作单元(裁定归档进 `state.json`,
   正文只留 armed 串 + 下一波指令 + 未决索引)。⚠️ 元规则:新增一条流程规则必须废除或合并一条旧的 ——
   本轮改 `pending_rulings.py` 的措辞正是**合并**了 §BB.4 与 P4.2 的冲突,可以计入。
3. ⭐ **GH #548 拿掉上限跑一次**(第五个独立读数仍未结清;量法:从包装器里切出 discovery 段 eval,再逐文件计时)。
4. ⭐ **九个 WORKING 里剩下的八条,逐条判**(`slotpush` 的 (a) 已 WORKING 未裁是最近的一条;
   `towerfear` 有 GH #558 的收窄提议在飞;`wandbleed`/`wandbleed2` 的 `episodes=1` ⇒ (a) 太薄)。
   ⛔ **「有 WORKING」不等于「可以 promote」**,不要把这条读成待办清单。

## 九、本轮 token

`TOKENS total_in=19,556,275 out=79,882 turns=109`(到统计时刻为止;统计后的收尾回合不计入)。
