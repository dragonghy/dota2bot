# 总监 2026-09-06T22:20Z — **第四、五次 promote(`odbuild` + `illumove`)+ `towerfear` 退回出集**;判定完结 **3**,armed **55 → 52**

**owner P4.2 的产出指标是判定完结数,不是裁定节数。上一轮 0、上上轮 0(两轮都被 trunk 红挤掉),
本轮 3。** 交棒清单第 ① 条(「九个 WORKING 里剩下的八条逐条判」)本轮先做,做完才看别的。
本轮**零 AWS 调用、零波次请求、不发 owner 邮件**(W36 那封 07:xxZ 已发,周配额用完;
本轮没有需要 owner 拍板的新问题 —— P4.1 那道 A/B 拍板题 09-05 就已在 `DECISIONS_NEEDED` 里)。

裁定全文 `test_set.md §FO`,锚点 **stable-v4**(`0d3f857f`),机器键三条见 `state.json`。

---

## 1. 三条裁定

| id | (a) | 裁定 | 机器键 |
|---|---|---|---|
| `odbuild` | WORKING `episodes=7`(W28:armed objurgation rank 4/4/4 vs baseline 0/0/0/0) | **PROMOTE** | `odbuild_PROMOTE_20260906` |
| `illumove` | WORKING × **三份独立语料**(15 / 305 / 180) | **PROMOTE** | `illumove_PROMOTE_20260906` |
| `towerfear` | WORKING `episodes=248`,**而第二半反号** | **退回出集** | `towerfear_WITHDRAWN_20260906` |

### 1.1 ⭐⭐⭐ 本轮最该被读的一条:**我第一稿的 (b) 引错了波,而错得像对的**

两条 promote 的 (b) 是**同一份家族级读数**。第一稿写的是「W51(55-id 家族自己那一波,238 局),
gpm −2.19」—— **W51 是 `campgrade` 独占波,`arm_string` 就一个 id、`arm_bytes` 9,这两个 id
在它上面根本没 armed**。发现它靠的是去核「illumove 是不是真在那一波里」,不是靠读起来不对劲:
- `arm_ids` / `arm_md5` / `arm_bytes` 这些字段**每个波次记录里都有**,长得一模一样;
- 收割报告里那句 2(b) 措辞(「四个都远在镜像波噪声底之内」)对独占波和家族波**读起来完全一致**;
- ⇒ **只有把 `arm_string` 打开看,才分得出「55-id 家族的那一波」和「一个 id 的独占波」。**

**改正后的 (b),逐波从各自 `W*_wave.json:harvest.mean` 裸读(不是抄散文)**:两个 id 在
**W47 / W48 / W49 / W50 每一条腿上都 armed**(62/63/61/59-id 家族,**701 局计分**;那四波的
移除项是 `stayattr` / `tpdying` / `tpreach` / `slotwait`,**从来不是这两个**):

| 波 | 计分局 | 家族 gpm | deaths | comps_better.gpm |
|---|---|---|---|---|
| W47 | 162 | **−5.95** | +0.25 | 1/2 |
| W48 | 184 | **+27.25** | +0.06 | 3/4 |
| W49 | 168 | **+11.70** | +0.02 | 3/4 |
| W50 | 187 | **+13.76** | +0.07 | 2/4 |

**诚实边界逐字抄进两处源码注释**(promote 之后唯一还会被读到的地方):这是**家族级**不是 id 级,
全开波不可能把经济归给某一个成员;winrate 通道自 GH #352 起 DEGENERATE ⇒ **一个可引的胜负数都没有**。
铁律 2(b) 要的是粗粒度的「没有明显负面」—— 四波、id 每波都在、均值 +11.7 gpm,**就是它,仅此而已**。

### 1.2 `odbuild`(§FO.1)

(c) **是算术不是判断题**:那一行 15 个条目花 4+4+4+3,OD 恰有三个可学基础技能(各四级)+ 三级大招
⇒ 4× 块只能是基础技能,而出厂行**从不点名的**那个正是 index 3;index 4 是 `generic_hidden` 占位符
(2/2 drop-world 实测)。**代价已量**:出厂腿 OD 在英雄 **7 级卡死**,15 个技能点只花掉 **6**、天赋 **0**,
4/4 baseline 腿(GH #330)。
⚠️ **登记的仪器边界**:英雄组 08-29 在 W25(另一棵树)读到 1 例 `ROW_CONTRADICTS_STAMP`,
W28 上 **0/3 未复现** ⇒ 戳与实际行**可能**脱钩、频率未知;它限制**精度**不限制**方向**。
⚠️ **随 promote 一起登记的残留**:armed 腿仍丢天赋点(冻结 5–24%)—— **第二个缺陷**,不在本 id 域内,
`test_od_levelup_double_spend.lua` 第 7 节继续替它举手。

### 1.3 `illumove`(§FO.2)

(c) **是作用域不是调参**:`nNextMoveTime` 是**模块局部**,而 `aba_minion.lua` 只 dofile 这个模块一次、
把每一个幻象与无技能小兵都派进同一个 `X.Think` ⇒ 一帧里第一个到达移动分支的单位把时钟推到 0.2s 之后,
**同帧的兄弟一条命令都拿不到 —— 不是延后,是没有**。正确形状本仓库上一层就有
(`aba_minion.lua:33-35`,**每单位字段**)。
**归因边界是算出来的**:`illureal` 是同文件同路径上唯一的另一个 id 且会**缩小**本 id 的域 ⇒
W35 的交集**逐帧算过,上界 0(空)**。

### 1.4 `towerfear` 退回出集(§FO.4)——「有 WORKING」最锋利的一个反例

(a) 的**前半买到了**(248 episode:occ% +2.45 / dwell +1.41s / bounce% −23.88,三个量两层同号,
等级-only 对照是纯噪声);**后半反号**:被释放的 episode 摸进塔**自己的 700u 攻击圈**的占比
**+12.46pp(ab)/ +35.21pp(ba),两层同号**,读数取 **episode 级**(一局独占 128 个 `<700` 帧里的 90 个,
铁律 4(ii) 的刀口)。点名帧:armed 腿 sniper 扎到**距塔 179u**,连吃 5 秒塔伤 **hp 504→280**,
最近敌方英雄 >1000u —— 出厂时钟本会把他拉出来(GH #558)。

**为什么不是 HOLD**:(b) 是家族级读数 ⇒ 每留一波,armed 腿就在**promote 唯一能引的那个量**里
执行一个**已量到有害半边**的杠杆。与 13:2xZ 三条 (a)=BUGGY 退集同构(形状不同 —— 那三条咬错了,
这条咬对了域而结果一半有害;**污染一样**)。退集把 armed 腿退回**出厂时钟 = 稳定版**,保守默认。
⚠️ 录像组自己的结论与本裁定同向(「不建议按现状 promote」),本轮把它从「不 promote」推进到「不再 armed」。
**接力棒**:该 ship 的是 `towerfear`+`towerring` **那一对**,而 P4.2 冻结禁止 `towerring` 入集
⇒ 已登记 `owed_executions.json:towerfear_towerring_pair_readmit`(`kind: manual`,并写明**为什么不能**
用 `path_contains_all: 'towerring'` —— **本轮的 §FO 自己就提到它,那个键会在登记的同一秒读成假绿**)。

### 1.5 `pullcad` 陷阱:两条 promote 都查过,而查出来的第三条被机械化了

- `odbuild` / `illumove`:gate 字面量各自全仓唯一、不与任何 id 合取、不属任何原子(`promote_atoms.py` OK)。
  `illureal` 是同文件的**另一个独立 gate**,promote 后**一字未动**,仍可单独 arm。
- ⭐ **`towerfear` 的门是析取** `IsSoakCandidate('towerfear') or IsSoakCandidate('towerring')`
  (`mode_retreat_generic.lua:964`)⇒ 若哪天 promote 它,那句门一删,**`towerring` 的 arm 从此测不到任何东西**,
  而 `check_armed_wiring.py` 照样读作 WIRED、verdict 照样写「测了没效应」—— **`pullcad` 陷阱,只差一个布尔算子**。
  ⇒ 新增 `promote_atoms.json:tower_fear_ring_disjunction`(subject `towerfear` / prereq `towerring`),
  **在有人 promote 的那一天自己举手**;它与本轮的退集**分开成立**(退集删的是串里的 id,不是 `bots/` 里的门)。

### 1.6 剩下的 WORKING(**「有 WORKING」不等于「可以 promote」**)

`slotpush`(939)未裁 —— promote 时必须同轮退休 `state.json:coarmed_outlatch_slotpush_20260902`;
`ckpush`(40)**下轮首选**(门是选择不是析取);`fieldsip`(15)**结构性 HOLD**(原子
`field_hold_needs_magnitude`,两个前置都是 INDETERMINATE);`wandbleed` / `wandbleed2`((a) 太薄,
且 §DU.5 的阴性面未登记)。

---

## 2. 同轮的两条 queue 裁定(§FO.6)

`pending_rulings.py` 打 `RIDESHARE 2`,两条**当轮裁掉并写进各自的 `director` 机器字段**
(不是写进 `question` 散文 —— 那正是 13:05Z W3 掉棒的形状):
`hero-38`(`zusfightquorum` 域扫描)与 `hero-39`(GH #570 撤案复核)均 **APPROVED-SCAN**,
零 EC2、只读归档,搭已批准的同一次遍历。⛔ `hero-38` **只批扫描不预批入集**(冻结期入集提议唯一
合法裁定仍是 FROZEN-HOLD);⛔ `hero-39` 的**量具前提写进裁定本身**(必须半开判据
`add <= t < remove`;`cullthresh_domain.py:215` 现在是闭区间,原样跑会复现 #570 的那个 `2`)。
`pending_rulings.py` 退出码 **3 → 0**。

---

## 3. 铁律 6 / 铁律 10(读数,不是声称)

| 项 | 读数 |
|---|---|
| 开工自检(铁律 10) | `EXIT=3` ——**重定向跑的,不是管道**(章程第 0 条,本轮遵守);发现见 §4 |
| 静态半 `luacheck_gate.sh` | **`GATE_EXIT=0 CLEAN`**,`luacheck bots game: 0 warnings`(容器缺 luacheck,gate 自己装的);push 门两次都真跑了 |
| `run_tests.lua illumove` / `od` / `build_index` / `gate_claim` / `smoke` | **9/0 / 146/0 / 9/0 / 16/0 / 3/0**,`RC_EXIT=0` |
| python 全套 `tests/run_py_tests.sh` | **106 passed / 1 failed / 1 uncertifiable** —— 唯一那条红是 `test_detector_source_constants.py`,**是本轮改动引起的**(见 §3.1),**同轮修好并复跑绿** |
| `carrier_terms.py` | `TERMS` 行**逐字节不变**(8 项);计数 `12 hero / 43 generic / 0 unresolved` → **`11 / 42 / 0`** |
| `check_armed_wiring.py` | **all 52 armed ids wired on HEAD**,`RC_EXIT=0` |
| `promote_atoms.py` | 4 atoms OK,**`FROZEN none`**,`16 id(s) carrying a PROMOTED note`(原 14) |
| `pending_rulings.py` / `citation_audit`(测试) | **263/0** / **74/0** |
| `stable_anchors.py` | **4 anchor(s) checked -- OK**(stable-v4:EXISTS / PINNED / SHIPPED) |
| 全量 Lua 套件(~100min,GH #124) | **没跑,不声称它绿** |

### 3.1 ⭐ 那条 python 红是 promote 的隐性成本,而它长在一个**负对照**上

`test_detector_source_constants.py` 有一条检查写着「一个 OD 文件里**独立的**第二个候选**不是** GH #207
的形状,必须读绿」,而它买这句话的方式是**断言 `odbuild` 活着**。promote 一落地,这条检查就变成
**在断言它要接受的那个形状不存在**。⇒ 改成把那个形状**注入**一份文本副本(与它下面那条注入
**合取**形状的姊妹用例同法),**牙齿保住,而且不再能被下一次 promote 作废**。
**这类钉子本轮共翻面 6 个**:两条 OD 的(两条都**事先写着**「如果 odbuild 被 promote 了,
这条断言就是要改的那一条」)、`illumove` 的结构块与它的两个世界、`test_carrier_terms.py` 与
`mutstand_carrier_minion.sh` 的探针(`illumove` → 仍 gated 的 `illureal`,**同一条 illusions.lua 路径继续被探到**)。
**承重的永远是 id 的缺席**:promote 后再长回一个门 = 真实对局里恒 inert,而所有 armed-wiring 检查照样读作干净。

---

## 4. 开工自检读到的 trunk 红(**不是本轮改动**,登记并交回)

`test_gated_helper_nesting_census.lua` 红:出现一对**新的门中门**
`stayattr,staybag,staybottle,staysrc,staytower,stayurn | J.ShouldStayAndRegen | J.HasFieldRegenSource | bagsalve`。
按那条检查**自己写的**提问顺序读了一遍源码:未 armed 时 `bagsalve` 是
`J.HasFieldRegenSource` 结尾 `return false` 之前的一段**纯增量** `return true` 分支
(`jmz_func.lua:5525-5533`)⇒ **它是那个合取的单位元**,arm 外层不会测到一个 no-op。
⛔ **但我不替协同组把它登记进那张表** —— 那是**别的组本轮刚落地的**产物,而 09-06T19:19Z 那轮
(GH #574)刚立过同一条原则:不为了让套件变绿而单方面翻别组的裁定。**交回协同组登记**;
本轮把结论(单位元、可登记)写在这里,让它落地时不用重做这段推导。
⚠️ 另两条自检发现是 `cadence`(strategy 两个 3.6h 洞,不是本座)与 `owed-executions`
(`campgrade_wave_type_premise_reading`,批测台,理由是钱)。

---

## 5. 健康巡检(铁律 9)

- **P4.2**:本轮 **3 个判定完结**(要求 ≥2),armed **55 → 52**。⛔ 距离 owner 的 ≤20 还差 **32**,
  而本轮已把「(a) 明确的那批」用掉一半 —— 剩下 32 个里 **28 个连一行 `VERIFY` 都没有**
  (`verify_coverage.py`),所以**下一段的瓶颈是 (a) 的产能,不是裁定的产能**,而 (a) **不需要新的波**。
- **P4.1**:⚠️ **上一轮把它标红为「~13 循环零推进」,那个归因要更正**:标尺波 **Y1 09-05 已飞并已收割**
  (胜率 swap-average **0.550**,44/80,fwd 0.625 / rev 0.475,80 局),总监 09-05T19:xxZ 已裁
  **部分完成**(§EZ.2 / GH #535)。**真正卡住的是两件别的事**:(i) owner 那道 A/B 拍板题
  (改完成定义 vs 挂起 P4.1)在 `DECISIONS_NEEDED` 里等 owner;(ii) gpm 那格是一条**量具**任务
  (引擎在无人类玩家时不填记分板)。⇒ **不重复发同形标尺波**(裁定已明令),**也不再按「零推进」升级**。
  ⭐ 而值得 owner 看见的一句是:**+5.0pp 在 80 局上 SE ≈ 5.6pp** ⇒ 这份读数说的是
  「方向为正、与平手分不开」,**不是「已经变强了」**。
- **P4.3**:⚠️ **今早刚达标的 <50KB 已经回弹:`test_set.md` 47.9KB → 78.4KB(一天之内)**。
  机制量出来了:头部(armed 串 + 历史块 + 各波「收割前必读」)**50.1KB**,三个 § 裁定节
  **8.3 + 9.3 + 10.8 = 28.4KB** ⇒ **归档 § 节最多只能回到 50.1KB,连线都压不住**;
  真正单调增长的是**头部里从不被驱逐的〔历史〕块**。**本轮不动手**(那是一个独立工作单元,
  而且要动别组当天的产物),**登记机制与数字**,排进交棒。
- **P1 / P2**:球在协同组,本轮无总监动作。**P3 / GH #108**:08-25 已落地,无新增。
- **成本**:本轮**零 AWS 调用**。批测台 21:13Z 报 MTD **$50.804**(免费 budgets 读数),
  越线部分**多数不是本项目**(外来 16xlarge 已停)⇒ 围栏 $60 / 刹车 $90 / 批准线 $100 不动。

---

## 6. 交棒(下轮)

1. ⭐⭐⭐ **继续 P4.2 判定完结,首选 `ckpush`**(门是选择不是析取、(a)=WORKING 40、
   `mutstand_ckpush.sh` 已在树上);其次 `slotpush`(promote 时必须同轮退休那条 co-armed 登记行)。
2. ⭐⭐ **GH #450 按 `gated-fix` 落新 soak candidate**(`owed_executions:roshan_pit_daynight_fix`)—— **已挂四轮**。
3. ⭐⭐ **`test_set.md` 头部瘦身**(§5 的数字:头部 50.1KB,§ 节归档救不了它)——
   P4.3 的后半,且现在有了**机制**而不只是目标。
4. ⭐ GH #574(census 新鲜度棘轮三选一)仍在写它的人手上;GH #548(自检 5a0 预算跟着集合走)选修法。
5. 协同组:`test_gated_helper_nesting_census.lua` 的新门中门待登记(§4 已把推导写好)。

---

## 7. 本次会话 token 用量(铁律 8)

```
TOKENS total_in=33,788,335 out=92,253 turns=171
```
(统计时刻之后的收尾回合不计入。)
