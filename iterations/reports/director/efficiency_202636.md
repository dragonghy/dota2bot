# 效率台账 2026-W36(08-31 .. 09-06)—— 第三份

总监 2026-09-06T07:xxZ 触发。章程 §2(e2) 的周日项。

上一份(`efficiency_202635.md`)的一句话是「**花费涨 $43.8,promote 从 1 变成 0**」。
这一份的一句话是:

> **promote 连续第二周 = 0(距上一次 promote 已 14 天),token 投入涨 17%,
> 而本周新出现的头号风险与我们的工作完全无关:账号上另一个项目的算力
> 会在今天上午把我们自己的发波围栏关上。**

---

## §0 ⭐⭐ 先说本周唯一的红色项:**我们的刹车会被别人踩下去**

| 时刻 | 九月 MTD | 出处 |
|---|---|---|
| 09-05T11:21Z | **$19.597** | 批测台 `20260905T211300Z.md` §2(免费 `budgets`) |
| 09-06T06:02Z | **$39.328** | 批测台 `20260906T061400Z.md` §2(免费 `budgets` + $0.01 CE 逐位复核 `39.3278311289`) |

这一夜跳了 **$19.7**,而同期批测台只发了 W50 一波(登记 `~$1.02`)。
CE 按 `USAGE_TYPE` 把 09-05 那 $20.106 的 EC2 拆开,**归因是算术不是推测**:

```
USW2-SpotUsage:c7a.16xlarge   $17.109  qty=14.99   <- Project=final-table-trainer,不是本项目
USW2-BoxUsage:c6i.4xlarge     $ 2.081  qty=3.06    <- 本项目
USW2-SpotUsage:c6i.4xlarge    $ 0.917  qty=3.27    <- 本项目(W48/Y1/W49)
```

本项目的批测机型**从来是 `c6i.4xlarge`**(备选 `c6a.4xlarge`),**不是 `c7a.16xlarge`**。

**而且还有一台在跑**:`i-0a7d4c7fbeb3a6712`,`r7a.16xlarge` spot,
`Project=final-table-trainer`,02:10:36Z 起飞,现货价 **$1.82–2.79/h**。

⇒ **闸 (iii) 的 $50 围栏此刻只剩 `$9.652` headroom,÷ 约 $2/h ≈ 4.8 小时**
⇒ 约 **09-06T11:30Z**,**我们的例行波会在自己一分钱没多花的情况下停摆**,
而 owner 收到的第一封预算告警邮件**抬头会是 `dota2bot-batch`**。

**这条已经从预判(GH #515)变成读数(GH #528)。** 本周唯一需要 owner 动手的事在这里:
**给 `dota2bot-batch` 预算加一个 `Project` 过滤器** —— `dota2bot-agent` 没有
`budgets:Modify*` 权限,我们做不了。已进本周邮件。

⛔ **我们没有、也不打算动那台机器**:它带着别的项目的 tag,终止别人正在跑的算力
既越权又不可逆。**我们也不主张它不该跑。**

## §1 AWS(唯一真花钱的轴)

| 量 | 值 | 备注 |
|---|---|---|
| 九月 MTD(账户级,周末读数) | **$39.328** | 这是**账户**的数,不是本项目的数 |
| 其中**已确认不是本项目**的 | **−$17.109** | 09-05 `c7a.16xlarge`,CE 逐项 |
| ⇒ **本项目九月至今(下界)** | **≈ $22.2** | 09-01..09-04 的 EC2 行尚未逐项拆过,故记「下界」 |
| 本周发波 | **W43–W50 + Y1 标尺波 = 9 波** | `iterations/reports/batch-desk/waves/` |
| Budget limit / 三档 ACTUAL 告警 | $100 / $50・$80・$100 | 本周末**一档都没跨** |
| 围栏 / 刹车 / 批准线 | $50 / $90 / $100 | 围栏是当前 operative ceiling |
| **总监本周自身支出** | **$0** | 全周零 AWS 写调用 |

⚠️ **8 月的 $/局 序列不许续到 9 月**(章程明文),本周同理:上表的「本项目 ≈ $22.2」
是**下界**不是读数,**不要拿它去算 $/有效局再与 W35 比**。要真数就得再花 $0.01×4 拉
09-01..09-04 的逐项 CE,**本周没买,因为没有决策依赖它**。

## §2 有效局数

W50 收割:**187 局计分**(4/4 粒干净、零抢占、零 thin-arm、211 对象/211 文件零丢失)。
W49 的 (a) 取证遍历:**72/72 计分局全扫,unparseable 0**。
`stayattr` 域扫描:**100/100 局宽扫,76 局计分**。

⛔ **仍然没有跨波的机器可读账**:`iterations/games_ledger.jsonl` 停在 391 行 /
`run_20260719_1353`,**与前两份台账逐字节同一个数**。
W35 台账 §0 交出去的 `HARVEST run_id=… games_effective=…` 一行,**本周仍是 0 行**
(全周 grep)。上一份已经把请求落进了批测台自己的章程,所以**这次不再是「棒掉了」,
是「棒在但没人捡」** —— 记录在案,本周不升级(批测台本周被泄漏检查和四轮成本归因占满,
那件事的优先级确实更高)。

## §3 token 用量(铁律 8)

**逐份报告的 `TOKENS` 行,251 份报告 100% 有(W35 是 263 份 / 262 份有)。铁律 8 本周零违规。**

| 组 | 报告数 | total_in | in/报告 | 对 W35 的 in/报告 |
|---|---:|---:|---:|---|
| strategy | 50 | **874,808,460** | **17.50 M** | 11.29 M ⇒ **+55%** ⚠️ |
| hero | 50 | 650,328,192 | 13.01 M | 12.32 M ⇒ +6% |
| replay-check | 50 | 562,463,214 | 11.25 M | 13.01 M ⇒ **−14%** |
| director | 50 | 537,801,368 | 10.76 M | 7.67 M ⇒ **+40%** ⚠️ |
| batch-desk | 51 | 407,796,955 | 8.00 M | 4.99 M ⇒ **+60%** ⚠️ |
| **合计** | **251** | **3,033,198,189** | 12.08 M | 9.86 M ⇒ **+23%** |

(W35 合计 263 份 / 2,593,804,077 in;本周报告数 −4.6%,总 in **+16.9%**。)

**三个组越过「连续两周变差」的门槛,归因逐条:**

- **batch-desk +60%**:本周它做的事变了 —— 泄漏检查从「查本台 tag」扩到**全账号实例普查**,
  成本归因从「读一个 MTD 数」变成**两次付费 CE 拆解 + 逐日 × 逐 USAGE_TYPE**。
  **这是本周最有价值的一份产出**(§0 全部来自它)。⇒ **不调整,记为「买对了东西」。**
- **director +40%**:本周总监轮次里有两轮是**修 trunk 上的红**(09-06T01:19Z 修 5 个
  python 测试;04:17Z 的 GH #547 切分器普查),都要读大量源码。⇒ 一次性,不调整。
- **strategy +55%,而且是绝对值最高的一组(17.5 M/报告)**:⚠️ **本周唯一一条需要下周复查的**。
  本周它的产出是 §FF / §FE 一族的「一条否决子句的注释写着别人的理由」这类**逐子句读源码**的活,
  单份报告要通读整个 `jmz_func.lua` 域。**没有证据说明它在空转**(50 份报告全部有实质产出),
  但 17.5 M/报告 是全队最高且连续两周上升。
  ⇒ **下周动作**:总监在下一份台账里单独看 strategy 的 in/报告;若仍上升且产出形状没变,
  往它的章程里加一条「读源码前先用帧语料检索工具收窄域」。**本周不动它的章程**
  —— 归因还不够硬,而按错的归因改章程比不改更贵。

## §4 promote / reject / 入集

| 量 | W36 | W35 |
|---|---:|---:|
| **promote(进稳定版)** | **0** | 0 |
| reject(出集且永不 arm) | 0 | 0 |
| **退回出集(RETURNED)** | **3**(`zusstatic` / `tpdying` / `tpreach`) | — |
| 新入集 / ADMITTED | 2(`slotpush`、`campbind`+`zusboltdom`) | — |
| `state.json` 本周新键 | **73** | — |

⭐ **距上一次 promote 已 14 天**(`creeppull`+`pullbeat`,2026-08-23,`stable-v2`)。
**这是本台账连续第二周报同一件事,而本周第一次有了归因,不再只是数字:**

本周开的 issue 里有**一整族**是同一个形状 —— **条件 (a) 在当前的波次配置下结构性买不到**:
GH #532(`stayattr`/`staysrc` 两道各自充分的否决,单臂都动不了)、
GH #545(`suptp` 被同集 `midtp` 完全支配,16/16 波不承重)、
GH #553(5 个 armed id 在全开镜像波上不可分离)、
GH #542(`pullcad` 陷阱的第二形态:两个 id 在同一条**路径**上)、
GH #540(**40/61 个 armed id 的 (a) 义务今天没有任何一行替它举手**)。

⇒ **零 promote 的原因不是「改动都不好」,是「三条件里的 (a) 在 61-id 全开镜像波上
对大多数 id 不可判」。** 波次配置本身成了瓶颈。
⚠️ **这是一个观察,不是一条裁定** —— 本轮没有重排波次策略,那需要它自己的工作单元。
写在这里是为了让下周的台账能接着量它,而不是每周重新发现「promote 又是 0」。

## §5 交棒

- **甲 → owner(本周邮件)**:`dota2bot-batch` 预算加 `Project` 过滤器(§0)。
- **乙 → 下周总监**:(i) strategy 的 in/报告 复查(§3);(ii) §4 那条「(a) 不可判」
  的归因要不要变成一次波次策略的重排 —— 那是一个独立工作单元,不要顺带做;
  (iii) `owed_executions:a_evidence_tpdying` 退休核验(`test_set.md` §FG.3 丙)。
- **丙 → 批测台**:围栏常数本轮重裁,**spot `$0.90` → `$1.10`**,
  自动失效条款加第二条腿(机时),全文 `test_set.md` §FG / GH #454。
