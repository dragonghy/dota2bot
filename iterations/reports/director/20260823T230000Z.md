# 总监 2026-08-23T23:xxZ —— 第六十次触发:`creeppull` + `pullbeat` **PROMOTE**(`stable-v2`)

**一句话**:owner 铁律 2 的三条件首次在同一个东西上同时齐备 ⇒ **promote**,gate 从源码移除、
turbo 默认开;**单位是这一对而不是 `creeppull` 单独**((a)/(b) 覆盖的只有「两个都开」这一个构型,
而 `creeppull` 单开正是 GH #143 实测为坏掉的那个构型);
**同一次改动里拆掉了一个会被 promote 悄悄冻死的门**(`pullcad` 的合取,与 `campgrade` 逐字同形);
成员串 27 → **26**;`pullcad` 入集并**作废它的 arm 串约束**;总监零 AWS 调用。

---

## 0. 开工自检(铁律 10)

`bash tools/agent/routine_selfcheck.sh` ⇒ **worst exit 3**。

- **UNLANDED 0**(certifiable 窗口内无未落地工作;290/323 refs 因浅克隆不可判定,属工具 LIMITS)。
  ⇒ 上一轮抢救的 GH #141 两条已确在 `origin/main`,未复发。
- **CADENCE 1 finding**:`batch-desk 18:09Z → 22:07Z = 4.0h`(阈值 3.5h)。
  **不是掉棒**:批测台自己在 22:07Z 报告 §1 里解释了 —— 发波轮与收割轮之间必然有一个空档轮
  (触发 2h/轮 + 波次跑满看门狗)。**裁定不追究**,与 08-19「例行间隔差 2 分钟」同档。
- trunk python **18 passed / 0 failed**。

## 1. ⭐ 本轮的裁定:PROMOTE `creeppull` + `pullbeat`

上一轮把整个工作单元预留给了这个判定,理由是「promote 改的是每一场真实 Turbo 对局的默认行为」。
本轮三份输入齐了,逐条核过:

| 条件 | 交付方 | 读数 | 判定 |
|---|---|---|---|
| (a) 真的执行且行为正确 | 录像组 21:00Z | `FLIP` 特异量,两拼法 × 两物理层**四个 delta 全部同号为正**;balanced **+0.308 次/局**,\|t\| **3.40**;三帧逐帧钉死 | **WORKING** |
| (b) 对胜负无明显负面 | 批测台 22:07Z | 280 有效局:winrate **0.500 逐位中性**、gpm **−6.15**(波间 sd 45.81)、deaths **+0.03** | **通过** |
| (c) 逻辑/理论依据 | 协同组 21:55Z | 拉仇恨要求敌方英雄在小兵 **500** 内,实现里的常数**逐字就是 `<= 500`**;三处独立来源 | **成立(触发与几何)** |

### 1.1 (b) 的分层读法,以及它**改变了能说的话**

按本轮上一轮刚立的铁律 4(i) 强制分层:ab **+50.25** / ba **−62.55** gpm,
winrate ab 0.696 / ba 0.304、平均 **0.500**。
⇒ 侧偏 **~±56 gpm** 比残差 **−6.15** 大**一个量级**,
「creeppull 让经济变好或变坏」**这一波买不到**;买得到的只有「**买不到明显负面**」。

**而那恰好就是 owner 2(b) 要的那句话,不多不少。** 这是分层纪律第一次在一个真裁定上兑现:
它**没有**放行一个更强的结论,它**限制**了我能写的那句话 —— 这正是立它的目的。

**诚实边界照抄**:`winner_by` 的 `engine` 档 **0/304** ⇒ 本波 winrate 是 gpm 的符号粗化,
**不是独立第二证人**(§AT.8 已裁,此处兑现)。

### 1.2 ⭐ 单位是这一对,理由是构型不是偏好

`pullbeat` 嵌在 `creeppull` 的执行体里(那段 cadence 的 `elseif`),GH #149 已判**波内不可分离**。于是:

- **(a) 和 (b) 覆盖的都是「两个都开」这一个构型** —— 没有第二个构型被测过;
- **`creeppull` 单开 = GH #143 实测为坏掉的那个构型**:poke 在下单后 **33ms** 被自己的 move 取消,
  仇恨只能靠运气拉到,26.8% 的 armed episode 里没有任何小兵掉头。

⇒ 只 promote `creeppull` 会**上线一个零证据、且有实测缺陷的构型**。**要么两个一起出,要么都不出。**
源码两处都写了 `never promoted apart`,并有用例钉住两个 id 都不许再作为 gate 出现。

### 1.3 明说**没有买**的那一波,以及为什么

§AY.5 第 4 条(`--cand-ref creeppull` 两臂分离波)**不是 promote 的前置**,本轮**不排**。
GH #141 昨天刚让这一波变得买得到,很容易顺手排掉 —— 但它**对本裁定零决策价值**:
无论 `pullbeat` 单独是不是活性成分,要 ship 的都是这个原子(`creeppull` 单开我们不会 ship),
**两种结果都不改变「原子进 / 原子不进」**。按章程不为**不改变决定**的信息买波次。
要买它得给出别的理由(例如给 `pullcad` 的设计定价),那是另一个裁定,不是这一个。

### 1.4 KNOWN RESIDUAL:睁着眼睛 promote 的那一半

(c) 只买到了触发与几何,**节拍没买到**:1.2s 的补拍第 2、3 拍落在仍然活着的 **2.3s 仇恨**
与它的 **2–3s 冷却**里,结构上拉不到东西,**而且不是免费的**
(`Action_AttackUnit` 打不在攻击距离内的英雄会让 bot 走向他 ⇒ 把已拉住的兵线**往回拖**)。

**这封顶了这个行为的上限,但没有把它变成负的**,而修法(`pullcad`)已经写好并 gated。
**先 promote 已测过的版本,`pullcad` 才有一个稳定基线可比**;
反过来做的话,`pullcad` 只能跟另一个候选比,而那正是这套装置最不擅长的读数。

**这一条是我最愿意被质疑的地方**,所以它以 KNOWN RESIDUAL 的名义写进了源码头注、
`state.json` 和 §BA.1,而不是留在报告里。

## 2. ⭐⭐ 同一次改动里拆掉的那个门:promote 会**悄悄冻死**引用被 promote 者的门

`pullcad` 落地时(协同组 21:5xZ)门写的是合取:

```lua
if J.IsSoakCandidate('pullcad') and J.IsSoakCandidate('pullbeat') then
```

**当时这是对的**,理由还很好:把「`pullbeat` 是结构性前置」写成**代码而不是散文**,
让 `pullcad` 单独 arm 逐字节 no-op ——「**用例而不是承诺**」,申请书里明说这比
`pullzone`/`bagsalve` 那种「只能靠总监记得」的散文约束强。

**而 promote `pullbeat` 会让这个合取永久为 FALSE。** 已 promote 的 id 不在任何 armed 串里,于是:

- `pullcad` 在**每一个能 arm 它的波里都是逐字节 no-op**;
- `check_armed_wiring.py` **仍报 WIRED** —— backlog §10 的 LIMITS 自己写着,
  它证的是调用点**存在**,不是谓词能为真;
- verdict 回来是「**测过了,无影响**」,`pullcad` 被写死为无用杠杆;
- **没有任何计数会报警。**

**与 `campgrade` 那次擦身而过逐字同形**(唯一调用点晚于钉死的树,armed 上去是逐字节 no-op)。
差别只在:那次是**代码轴**的缺席,这次是**谓词轴**的缺席,而两者的现场一模一样。

**已在同一次改动里拆掉合取**,前置条件改由**构造**满足(hold 现在无条件执行,
**严格强于门**)。两条用例钉住新的不变量:
(i) `pullcad` 单独 arm **必须**移动节拍;(ii) `pullcad` 的门条件里**不许出现任何已 promote 的 id**。

**判据已经想清楚,登记为 backlog §15**(下一个总监不必重新推导):
**一个 id 若在 `bots/` 里只作为「别人门条件里的合取项」出现、自己不 gate 任何东西,
那它要么是已 promote 的、要么是拼错的 —— 两种都是缺陷。** 自足的源码判断,**不需要手工名单**。
⚠ 明说**不要**用「不在成员串里」当判据:合法地不在串里的 id 一大把
(`lanefix`/`lf_*` 被拒但留树、`pullzone`/`bagsalve` 已提议未入集)⇒ 那会是一盏
**每轮都亮红的灯**(§12 同一条立法理由)。
本轮那条用例**是点防御不是普查**,下一个新合取门不受它保护 —— 所以 §15 仍然是欠的。
另已写进 **CLAUDE.md 硬知识节**(promote 前先 grep 该 id 有没有出现在别的门条件里)。

## 3. ⚠️ 一个自伤,和它教出来的一条规矩:**源码普查读到了散文**

改完源码跑测试,10 条红。**6 条是真红**(我故意删掉的门,用例正在尽职告发),
**3 条是假红**,外加**第二轮里我自己新写的那条门形状用例又假红一次** —— 原因逐字如下:

我在 `mode_roam_generic.lua` 里写的 promote 说明**引用了被删掉的那个表达式原文**
(为了让下一个人知道拆的是什么),于是 `test_replay_pullcad_beat.lua` 的
`cadence()` 匹配器 `"IsSoakCandidate%('pullcad'%).-nBeat = ([%d%.]+)"`
**锚在了注释上**,再往后取第一个 `nBeat =` ⇒ 读到 **`local nBeat = 1.2`**,
把**出厂节拍当成了 armed 节拍**报出来。于是下游每一条关于 armed 的断言都在**拿 1.2 和自己比**。
**而且它咬了我两次**:我把姊妹文件那条门形状用例改写成「`pullbeat` 不许再作为 gate 出现」之后,
它**立刻在我自己刚写的那句注释里**找到了 `IsSoakCandidate('pullbeat')`,报「gate 又被接回去了」。
第二次才让我看清这不是一次手滑,是**读取面选错了**。

**这与 `test_detector_source_constants.py` §2b 是同一条**(那条之所以能全绿,
是因为它**只认赋值语句**;散文/docstring 里的数字占了原始命中的一半以上)。
**修法**:两个文件各加 `pull_code()`(剥掉 `--` 注释)—— **并保留 `pull_branch()`**,
因为有断言**故意**读散文(「分支里必须记着为什么不重复检查 turbo」是在断言**解释存在**,不是断言代码运行)。
**两个面各自命名,哪一个都不许吞掉另一个。**
外加一条**控制用例**:剥离器必须真的剥掉东西(一个悄悄返回原文的剥离器会把所有源码断言送回散文面,
**而且不会有任何东西变红** —— 就是它被加进来要修的那个失效,换了个没人看得见的形式)。

## 4. 落地清单

**源码(`bots/`)**
- `FunLib/jmz_func.lua`:`J.ShouldCreepPullLane` 去掉 `IsSoakCandidate('creeppull')` 门,
  turbo 门保留 ⇒ **normal 模式逐字不变**;补 `PROMOTED (was soak-candidate 'creeppull')` 头注
  (三条件读数 + KNOWN RESIDUAL + 「never promoted apart」)。另修一处陈旧 gate 声明。
- `mode_roam_generic.lua`:wind-up hold 去掉 `IsSoakCandidate('pullbeat')` ⇒ 无条件;
  `pullcad` 门拆掉合取;三处陈旧 gate 声明改写(GetDesire 的两处 + 分支头)。
- `mode_laning_generic.lua`:一处陈旧 gate 声明改写。

**测试(4 个文件,净 +4 例)**
- `test_creep_pull.lua` 12 例:`OFF: inert off the soak candidate` → **反转**为
  `PROMOTE: no soak candidate can turn the trigger off`;scenario 不再 arm 任何东西
  (**否则新用例是循环论证**)。
- `test_replay_pullbeat_attack_cancel.lua` 8 → **9** 例:两条 DEFECT 反转为
  「缺陷签名不得出现在已发布行为里」;GATE 用例反转为「**没有任何 soak candidate 能关掉 hold**」
  (**两腿都跑**,不只断言源码形状 —— 这才抓得住换个 id 重新 gate);新增剥离器控制用例。
- `test_replay_pullcad_beat.lua` 9 → **10** 例:GATE 用例反转为「`pullcad` 单独 arm 现在是整个杠杆」;
  「门必须也要 pullbeat」→ **「门条件里不许出现任何已 promote 的 id」**;新增剥离器控制用例。
- **保留而不是删除**这些反转用例:它们现在抓的是「**被意外重新 gate**」——
  一个会**悄悄下架已 promote 行为**、而别处不会变红的缺陷。

**工具(1 处,给录像组的连带订正)**
- `tools/batch_test/behavioral/creeppull_domain.py`:那句
  `[warn] creeppull is NOT in this wave's cand string` **promote 之后会在每一波都亮**,
  **而且说的是反话**(promote 后 `creeppull` 本来就不该在任何 armed 串里)——
  正是 §12 那种「三轮之后就没人看」的灯。改成**报告语料在 promote 的哪一侧**,
  因为那才是读数的前提:**post-promote 语料两条腿都会拉**,
  ⇒ 这条轴上的 armed/baseline 差**结构上是零**,
  **不许读成「拉野没用」**。工具从 manifest 分不出这两种(没有树戳可比)⇒
  **它把这个歧义说出来而不是猜** —— 这里有用的输出就是点名歧义本身。

**登记**
- `test_set.md`:成员串 27 → **26**;全文档案 **§BA**(4 节)。
- `queue.json`:`strategy-5` → `done` + `director.ruling=RESOLVED_PROMOTED`;
  `strategy-9` → `approved` + `director.ruling=APPROVED_ADMITTED`(**含 arm 串约束作废的警告**)。
  按步骤 2.5 三条腿:机器字段 ✅ / 档案 ✅ / issue 追评 ✅。
- `state.json:creeppull_pullbeat_PROMOTE_20260823`(含 declined 那一波的理由)。
- `CLAUDE.md`:promoted 清单 + **一条新硬知识**(§2)。
- 章程 backlog **§15**。

## 5. `pullcad` 入集(成员串 26)

批准协同组 21:5xZ 的入集提议(零 AWS 搭车)。
**但申请书 §2.1 那条 arm 串约束(「串里必须同时有 `creeppull` + `pullbeat`」)已作废** —— 见 §2。
**发波时 armed 串就是 `pullcad` 一个 id**;带上 `creeppull`/`pullbeat` 等于用**不存在的 id** 污染串
(`check_armed_wiring.py` 对它们现在会报 ABSENT,**这是正确的**)。
排期照常等名额,与 `strategy-7`/`strategy-8` 及 §AY.5 第 4 条一起排(§AZ.6 那根绳子不变)。

**给录像组的连带订正**:他们 21:00Z 「下一轮优先 (1)」写的是
「`pullbeat` 差分波落地后的 (a)」—— **那一波本轮判为不必买**(§1.3),
`pullbeat` 已 promote,该棒子**改为** `pullcad` 的 (a)(验收量协同组已预登记三条,
其中 (iii)「每 episode 右键次数必须下降」的第一嫌疑那句**要改**:
门已不是合取,漏 arm `pullbeat` 不再是嫌疑,漏的只可能是 `pullcad` 自己)。

## 6. 成本

总监**零 AWS 调用**、未启动任何计费资源、未批准新支出。
MTD **$28.462**(批测台 22:07Z 自报,W3 入账后预计 ~$33.4;那份快照的 `refreshed` 时刻
与上轮逐秒相同 ⇒ 同一份快照的复读,W3 的钱**按构造还没进去**,兑现窗口 08-24 09:xxZ 之后)。
**$45 launch fence / $90 brake / $100 owner 线全未触及。**

W4(`campgrade`)最早发波 **2026-08-24T00:09:32Z**,清单在批测台 22:07Z §8.1,
**不等任何裁定**(§AV.1),本轮亦未改动它。

**⚠️ 但 W4 会 clone 到本次 promote(它在我 push 之后发波)—— 三条后果,预先说清**:
1. **W4 的两臂对照不受影响**:promote 让拉野在**两条腿上都默认开**,而 W4 测的是
   `campgrade` 的 armed − baseline,promote 同号进入两个原始差、在差里湮灭。
2. **W4 的绝对数与更早的波不可比**(基线树变了)。这不是新规矩,§AF.3 本来就写了
   跨波读数不作数 —— 只是这次有一个**具体的、已知的**理由。
3. **发波串里绝不许出现 `creeppull`/`pullbeat`**:它们已不是候选 id,
   `check_armed_wiring.py` 会对它们报 **ABSENT(exit 1)**,**而那是正确的**,不是工具坏了。
   W4 的串是 `campgrade` 一个 id,本来就不带它们 ⇒ 实际无影响,此处只是把判读前提写在前面。

## 7. 门(铁律 6)

- `luacheck bots game --formatter plain` ⇒ **0 警告**。
- `lua5.1 tests/run_tests.lua` 全量 ⇒ 见 §9(收尾实测)。容器初始无 Lua 工具链,已自行
  `apt-get lua5.1 luarocks` + `luarocks install luacheck`。
- 逐文件复跑:`creep_pull` 12/0、`pullbeat_attack_cancel` 9/0、`pullcad_beat` 10/0、
  `creeppull_zone_clause` 16/0、`creeppull_reachable` 6/0、`gate_claim_consistency` 10/0。

**已发布行为的改动面,逐行核过(`git diff bots/` 去掉注释后**只有三行**)**:
`-if not J.IsSoakCandidate('creeppull') then return nil end`、
`pullcad` 门去掉合取、hold 的 `elseif` 去掉 `IsSoakCandidate('pullbeat')`。**没有第四行。**

**顺带排除了一个本该被问的风险**:GroupA 那次 −100 gpm 的祸首是
**laning-Think 接管**(arm `creeppull` 会 load-time 替换 Valve 原生 laning Think)。
`grep -rn 'bCreepPull' bots/` ⇒ **只剩两处注释,标志本体在 20260723 REHOME 时就已删除**
⇒ 这条路径**结构上不可达**,promote 不会把它带回来。**这是查过的,不是推的。**

**稳定版漂移:本轮是故意的,而且这是它第一次真的移动。**
`stable-v2` = 本次 promote 后的 main(章程 §D:容器 push 不了 tag,promote 打**分支引用**)。
(**编号更正**:本文原写 `stable-v1`,但那个名字属于 2026-08-19 的 `roamstale` promote;
抢救轮在建引用之前发现并改成 `stable-v2`,详见 `test_set.md` §BA.4 的更正框。)
**normal 模式逐字不变**(turbo 门保留在 `J.ShouldCreepPullLane` 第一行)。

## 8. 五组健康度 / 优先项

五组本轮均有产出,无空转。

**⚠️ P1 的完成度要说准,不许四舍五入成「完成」。** P1 的 DoD 通篇写的是
「**勾线/拉野**」「`creeppull`(**+修好的 `pullcamp`**)」—— **两个 id,不是一个**。本轮兑现的是
**勾线那一半**:`creeppull` 的第 1–4 条全部走完(根因 → 重新入集 → (a) → (b)+(c) → promote)。
**拉野那一半没走完**:`pullcamp` 的 SILENT 根因 08-22 已修、也在成员串里(26 之一),
但**它自己的 (a) 从来没被买过** —— W3 是 `creeppull,pullbeat` 独占波,`pullcamp` 不在那个串里。

⇒ **P1 判定「一半完成,不归档」**,本轮**有实质推进** ⇒ 不触发 12 轮红色。
**下一棒明写,免得掉**(铁律 9 连带规则,拉野死分支因 #13 关闭而消失 37 轮的教训):
**`pullcamp` 需要一个买它 (a) 的波次**,验收量可以直接抄录像组本轮为 `creeppull` 建好的
`FLIP` 拼法(带 10s 回看守卫)+ 分层平均 —— **姊妹拉野是同一族行为,量具是现成的**。
排期与 `pullcad`/`strategy-7`/`strategy-8` 一起看(§AZ.6 那根绳子),**由下一轮总监排**。

`OWNER_PRIORITIES.md` 的「当前球在:**协同组(1)**」**已经陈旧**(第 1 条 08-22 就做完了),
**第三次记**:唯一有权改它的主会话不在 Routine 循环里。
`DECISIONS_NEEDED.md` **无新增** —— 本轮四条裁定(promote / 单位 / 不买分离波 / `pullcad` 入集)
全部在自主授权内,**无一需要 owner**。本周尚无 owner 邮件。

## 8.5 ⚠️ 给可能来抢救这份工作的下一个总监:**main 的那一腿是我故意没走完的**

如果你在自检里看到这份工作以 **UNLANDED** 的形式躺在
`origin/claude/busy-bardeen-uxvcdx` 上,**先读这一节再决定怎么办**。

**它不是被遗弃的,是被门拦住的**:`luacheck` **0 警告**、python 套件 **18/0**、
六个直接相关的 Lua 文件逐一复跑全绿,**但 Lua 全量套件在本轮结束时仍在跑**
(GH #124:本容器约 2 小时;停笔时 **681 例 / 0 失败**)。
**promote 改的是每一场真实 Turbo 对局的默认行为,而 `origin/main` 正是批测实例 clone 的那棵树**
⇒ 我把提交推到**特性分支**保住工作,**但没有推 main**,这是**铁律 6 保护的那件事本身**。

**⇒ 抢救的正确做法不是直接 cherry-pick 落 main,是先补上那道门**:
在这份树上跑完 `lua5.1 tests/run_tests.lua`,**全绿再落 main**;
若跑出红,**先修再落**(上一轮 19:20Z 那条「按分解应该没事的推理不能替代一次真跑」的交代,
本轮明确适用于我自己)。
落 main 后还欠**两件收尾**:(i) `git push origin HEAD:stable-v2`(章程 §D 的分支引用;
**原文这里写的是 `stable-v1`,照抄会覆盖 2026-08-19 的锚点**——见 §BA.4 更正框);
(ii) 把实测 `N tests / 0 failures` 补进本报告 §9 与章程 ⑨。

**W4 不受此影响**:它 00:09:32Z 发波、armed 串是 `campgrade`,
main 若届时还没更新,它 clone 到的就是 promote **之前**的树 —— 那也**完全可用**
(两臂对照照旧成立),只是 §6 那三条「W4 会 clone 到本次 promote」的后果**要相应作废**。
**别让这条含糊**:判据是**发波时刻 `git ls-remote origin main` 的实际 tip**,不是本报告的意图。

## 9. 收尾实测与 token 用量

(全量套件读数与 token 行见本文件末尾的「收尾追记」。)
