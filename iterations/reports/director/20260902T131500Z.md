# 总监 2026-09-02T13:15Z — 收下 GH #424(卡 W39 的同臂合取),并核对 GH #413 那一棒:**掉了,而它掉的原因这一轮有同轮对照**

**一句话**:一个工作单元,两件事 ——(一)把 `outlatch > slotpush` 读完入册,解掉 15:31:32Z 的 W39 硬时限;
(二)按上轮章程去核 GH #413 的接力棒,**发现它在第一个触发点就掉了**,而**同一轮下的另一条裁定(#412)被照做了**
—— 差别只有一个:**#412 写进了批测台读的那个字段,#413 只写进了 issue 与档案**。
顺带把本轮自检抓到的**新 trunk 红**(`test_call_arity_census.py`)判掉。
**本轮零 AWS 调用、零支出;`bots/` 与 `game/` 逐字节零 diff**(改动只在 `tests/` / `tools/agent/` / `iterations/`)。

---

## ⓪ 开工自检(铁律 10)

⚠️ **纪律 3 第二十发,§22 守卫第十五次上场并第十五次拦下** —— 本轮第一条命令仍然是
`bash tools/agent/routine_selfcheck.sh 2>&1 | tail -60`,工具当场拒答:
`REFUSED: stdout is a pipe; exit 2, nothing checked`,并自陈**已复发 5 次、每次都是当轮第一条命令**。
**exit 2 不是通过。** 改 `> 文件; echo $?` 后的裸读:

```
EXIT=3
legs run              : 8
FINDINGS (exit 3)     : cadence trunk-red(python) trunk-red(lua)
UNCERTIFIABLE (exit 2): none
selfcheck worst exit: 3
```

`ORPHAN_PROPOSAL: none`(第十一轮);`stable_anchors` 2 个锚点三条不变量全 OK。

**⚠️ 本轮的并发披露,而且这次是我踩了批测台 08-30 自纠的那条**(「自检跑着的时候不许动工作树」):
自检在后台跑到 lua 腿时,我正在编辑 `tests/test_coarmed_attribution_register.lua`(含变异台的临时改写)。
⇒ **自检那一遍的 lua 腿读数对这个文件不可用**;§六 的收尾重跑是干净的那一遍,
两处读数**分开登记,不混着报**。python 腿的两条 `UNCERTIFIABLE` 同理(见 §六)。

**trunk 红四条,属主逐条分开**:

| 红 | 属主 | 本轮处置 |
|---|---|---|
| `test_coarmed_attribution_register.lua`:`outlatch > slotpush` | **总监** | **本轮做掉**,§一 |
| `test_call_arity_census.py`:`J.RoshanPitProximity UNDER passed 3 declares 4`(**新**) | 协同组落地 / **判据归总监** | **本轮判掉**,§三 |
| `test_detector_source_constants.py`:`illumove_pairs:HP_CUT` | 录像组(**GH #410**) | 登记,不代猜不代填 |
| `test_incoming_damage_callsite_census.lua`:published 43 现读 **44** | 英雄组(**GH #394**,**第九次复现**) | 登记,非总监归属 |

cadence:无新升级。

---

## 一、GH #424:`outlatch > slotpush` —— 读两个调用点、入册、**不是为了转绿**

全文档案 **`test_set.md §DN.6`**;`state.json:coarmed_outlatch_slotpush_20260902`。

**时限是硬的**:W39 是 55-id 家族第一波,gate (i) 解锁 **`2026-09-02T15:31:32Z`**;
在承认落地之前发波,等于让 W39 带着一条未登记的合取去测 `outlatch`(它正在服务 `queue.json:strategy-28`)。

### 1.1 机制:门中门里的「被闸住的提前 return」

`mode_outpost_generic.lua` 的 `GetDesireHelper` 里,`outlatch` 的闸是 `:79`
(`local bRescan = J.IsModeTurbo() and J.IsSoakCandidate('outlatch')`),
而 **`:45` 排在它上面**:`if J.IsTeamPushingHighGround(bot) then return BOT_MODE_DESIRE_NONE end`
—— 那**就是** `slotpush` 全仓唯一的闸解析包装(`jmz_func.lua:12263`)。
该谓词答 TRUE 的每一帧都到不了 `:79`
⇒ **`outlatch` 的 armed 腿触发计数从此测的是 `outlatch AND NOT slotpush 否决`**。
与 `pullthink > pulldrag` 同型(闸住的提前 return,不是嵌套的门)。

### 1.2 ⭐ 本轮主要价值之一:**方向按侧不对称,这是本册第一条这样的行**

armed `slotpush` 扫 **5/5** 个 team slot;出厂扫 **4/5**(radiant,pid 0..4)或 **1/5**(dire,pid 5..9)。
扫得更宽只会让「队伍在推」**更容易答 TRUE** ⇒ armed 腿否决更频繁 ⇒ 从 `outlatch` 调用点**拿走**帧,
**dire 侧拿走的约是 radiant 侧的 5 倍**。
(**不是严格单向**:`utils.lua` 文件头自己记着,出厂腿可以从一个**自身存活性从未被检查过**的英雄身上答 TRUE,
armed 腿拒绝 —— 两个方向都由 `test_slotpush_highground_scan.lua` 量,不靠论证。)

⇒ **`outlatch` 的 (a) 必须两个分层各自登记;它的两层反号按铁律 4(i-b) 读成噪声** ——
因为这个估计量带的是**侧相关的混杂**,不只是侧相关的抽样。
**这是本册第一条需要把 4(i-b) 读出来、而不是引用一下的行**:此前每一条 ACKNOWLEDGED 都只需要
「两条腿一起动」这句话,本条还要求收割方**换一种读法**。

### 1.3 重叠不是边缘的,这才是它值钱的地方

`:79` 在 `IsEnemyTier2Down`(`:63`)之前不可达;而 `slotpush` 谓词为真的条件
(队友靠近敌方二塔/高地塔,或距敌方遗迹 3000 内)**正是二塔倒下之后的那个后期状态**。
⇒ 否决**优先落在 `outlatch` 自己的域里面**,不是均匀摊在全局。
它与 `strategy-28` 原有的限定 (乙)(「前置条件本身罕见」)**同向叠加**,一起缩小可核验域。

### 1.4 它同时是行为耦合,不只是测量假象

armed `outlatch` 每游戏秒最多重扫一次(`OUTPOST_RESCAN_INTERVAL = 1.0`),
而 `IsTeamPushingSecondTierOrHighGround` 的 memo 也是 **1 秒** —— **同一个时间尺度**,
于是**一次落地的否决恰好吃掉一整个重扫槽**。armed `slotpush` 让 armed `outlatch` **更慢闩上**,
而且慢在 `outlatch` 唯一为之存在的那个阶段。

### 1.5 ⭐ 为什么 §DL.3 已有的那段读数不覆盖本条(**同一个结论不是同一个理由**)

提议方在 §DL.3 里读过**同样这三对**(`c3` / `outlatch` / `roshgate` 外层,`slotpush` 内层),
判为 **(P) 参数闸**并入册 `test_gated_helper_nesting_census.lua`。**那个判断是对的**,
而它回答的是**冻结**问题:「只 arm 外层,内层仍是出厂答案,所以杠杆不会被冻成常量」。
本册问的是**归因**问题,现场是**全开波次里两个 id 都 armed**,**(P) 在那里什么都没说**。
两份文件不是重复,是两个问题。
`c3` 与 `roshgate` 今天都**不在**成员串里(55-id 逐字核过),这正是本轮只有一对亮灯的原因。

### 1.6 失效条件(入册即关灯,所以点名什么能让它重新为真)

1. `:45` 的提前 return 不再排在 `:79` 之前(任一方移动);
2. **`c3` 或 `roshgate` 入集** —— 另外两对**已经躺在普查的 (P) 名单里**,
   哪天任一 id 入集那两对当天亮灯,**而本册这一行不会替它们举手**;
3. `slotpush` 被 **promote** —— 两条腿都带否决、混杂**离开差分**,
   但**跨越那一天的 `outlatch` (a) 依旧不可比**。届时**退休**该行,不是删掉它转绿。

### 1.7 投递(§2.5:裁定要落到被裁方读的那个字段上)

| 落点 | 内容 |
|---|---|
| `tests/test_coarmed_attribution_register.lua` | `['outlatch > slotpush'] = true` + 上面六节的读数与失效条件 |
| `test_set.md §DN.6` | 全文档案 |
| `test_set.md` **第 21 行**「收割前必读三条」→ 补 **(iv)** | **收割 `outlatch` (a) 的人读的就是那一行** |
| `queue.json:strategy-28.director.coarmed_caveat` | 分层登记 + 跨波不可比的机器可读限定(**不改裁词**) |
| `state.json:coarmed_outlatch_slotpush_20260902` | 跨轮档案 |
| GH #424 | 追评并关闭 |

### 1.8 验收(裸退出码,不经管道)

- `lua5.1 tests/run_tests.lua test_coarmed_attribution_register.lua`:**修前 RED**(点名 `outlatch > slotpush`)、
  **修后 11 tests / 0 failures,裸 exit 0**。
- **变异台 2/2 CAUGHT,各报各的缺陷**,基线先证绿、复原后仍绿:

| 变异 | 读数 |
|---|---|
| M1 删掉我刚加的那行 | **CAUGHT** exit 1,报 `a NEW co-armed conjunction is live: outlatch > slotpush` |
| **负控** M2 删掉**无关**的 `['midtp > tpreach']` | **CAUGHT** exit 1,报 `midtp > tpreach` —— **证明我加的行没有把这个检测器变哑** |

- diff **52 插入 / 0 删除**。
- ⚠️ **一次被工具拦下的错误读法,登记**:第一次我跑的是 `lua5.1 tests/test_coarmed_attribution_register.lua`
  —— **exit 0、零输出、什么都没跑**。`tools/agent/rc.sh:37` 恰好把这条逐字写成了它的反例。
  **一个「绿」有两种来源(通过 / 根本没跑),而它们在退出码上长得一模一样**;走 `run_tests.lua <basename>` 才是读数。

---

## 二、⭐ 本轮第二个主要价值:**GH #413 那一棒掉了,而这一轮有同轮对照**

上一轮章程「下次触发 ①」写的是「核对 GH #413 那一棒有没有掉(W38 收割当轮该执行 §DO 的 baseline 重建)」。
**核完了:掉了。** 批测台 12:15Z 的 W38 收割报告**整篇没有执行它**,`#413` 只作为一个编号出现在「存量」清单里。

### 2.1 归因不是「批测台漏了」,是投递落错字段 —— 而这次**同一轮下的两条裁定构成对照**

上一轮(10:xxZ)总监下了两条给批测台的裁定:

| 裁定 | 投递到哪 | 下一个收割轮的结果 |
|---|---|---|
| **GH #412**(SIR 词汇纪律) | **`iterations/streams/batch-desk.md` 的收割清单**(+ issue) | **当场被遵守** —— 12:15Z 报告 §六 逐字记着 `InstanceLifecycle` / SIR 码的纪律 |
| **GH #413**(`rec_slot_baseline` 重建) | **只有 issue 追评 + `test_set.md §DO` 档案** | **整条漏掉** |

**同一天、同一个总监、同一个收割轮、同一个执行方。变量只有一个:裁定有没有落到批测台读的那个字段上。**
⇒ 这是 §2.5 立法句的**第四例**(同族:§AW.1 落错行 / §BM / §CG.1 / §CH.2 / 「七天零落实」那条),
而前几例都是事后重建的归因,**这一例是受控对照**。
**`test_set.md` 是档案(权威,但没有人按它选波次),issue 是通知,`.md` 章程与 `queue.json` 的机器字段才是投递。**

### 2.2 落地(本轮就补,不留到下一轮)

把 §DO 的执行步骤**逐条写进 `iterations/streams/batch-desk.md` 的收割清单**,紧挨 #412 那条:
用 **W37 + W38 的 `.analysis.json`**(两波都是 `--rec-slots 8`,`rec_slot_cost.py:176-181` 的同质性检查按构造通过)
跑 `--emit-baseline`;旧文件 `git mv` 成 `rec_slot_baseline_recslots1.json` **保留不删**;
验收 = `--emit-baseline` **裸退出码 0** 且新 profile 头**逐字**写着 `"rec_slots": 8`;
**在它落地之前 §DA.2 的验收门不跑**;W25 的 196 局**登记不合并**。
**触发点改写为「下一个收割轮」**(原写「W38 收割当轮」,已过)——
2613 补跑机那一轮即可做,**它不依赖补跑机的产局**,W37+W38 的语料已经够。
同处**并把 2.1 的对照写成一条自纠**,让下一个读到这段的人拿到的是理由不只是待办。

**GH #413 仍不关闭**,状态维持「已裁,待执行」。

---

## 三、新 trunk 红:`J.RoshanPitProximity` 的 arity —— **判掉,不是等它骑过去**

自检 python 腿点名:`bots/FunLib/jmz_func.lua J.RoshanPitProximity UNDER passed 3 declares 4`,
工具自陈「a NEW mismatch is red the day it lands」「each needs a verdict in the tool's ALLOWLIST before it can ride」。
它是今晨随协同组 `roshdist`(GH #422 / §DP)落地的,**是四条红里唯一一条今天新出现的**。

**判据是读源码,不是读提议**:`J.RoshanPitProximity( hUnit, vRoshanLocation, bRoshDist, nRadius )`
对 `nRadius` **只有一处读**——`return nDistance <= ( nRadius or 1600 )`——
而且它排在 `if not bRoshDist then return nDistance end` **之后**
⇒ **缺席的第四个实参到不了任何一处无保护的读**。唯一调用者是闸包装 `J.IsAtRoshanPit`,
**它传三个是故意的**:1600 是本仓自己的「到达野区坑」半径(`hero_dark_seer:520` / `hero_rattletrap:475` /
`rubick_hero/rattletrap:383`),§DP.5 已记为**推导得来不是借来的**。
⇒ 判 **`DEFAULTED`**(不是 `BRANCHED`:替换在读它的那条分支里是**无条件**的)。

**并写下这条判据什么时候会错**:`J.RoshanPitProximity` **出现第二个调用者且它想要另一个半径**。
而这一条**不靠这句话守**——allowlist 的键带 `count`(本行为 1),
**第二个调用点自己会把这份普查重新变红**。

**验收(裸退出码)**:`python3 tests/test_call_arity_census.py` **修前 exit 1 / 修后 exit 0(`all checks pass`)**;
**变异 2/2 CAUGHT,各报各的缺陷**,复原后基线绿,diff **18 插入 / 0 删除**:

| 变异 | 读数 |
|---|---|
| M1 把行里的 `count` 1 改成 2 | **CAUGHT**,报 `an allowlisted row changed count ... 1 now, 2 allowlisted` |
| M2 抽掉裁定词(`DEFAULTED:` 前缀) | **CAUGHT**,报 `a row's reason does not begin with one of the six verdicts` |

---

## 四、成本

**本轮零 AWS / S3 / EC2 / CE 调用,未跑 `session_setup.sh`,未启动任何计费资源。**
九月 MTD **照抄**批测台 12:15Z 自报 **`$3.637`**(免费 `budgets` 通道,快照 `2026-09-01T17:58:37Z`),
批测台按波次自加后的围栏算术是 **`$6.04`**(含 W37/W38 各 ~$0.90 与 2613 按需补跑 ~$0.60)。**不写「顺延」。**
围栏 $60 / 刹车 $90 / 批准线 $100 **不动**;未跨任何 owner 可见告警档($50/$80/$100)。

⭐ **登记一条对总监有用的**:批测台 §2.1 用机时反解 W38 单波 **$0.60–0.64**,**低于 `$0.90` 常数**
⇒ **按需常数的自动失效条款未触发**,本轮**同样不重裁它**(第八轮顺延,理由这次是「读数在常数的安全一侧」而不是「没读」)。

`DECISIONS_NEEDED.md` 无新增。本周邮件 **08-31T01:20Z 已发** ⇒ **本轮无邮件**。
今日周三,效率台账(仅周日)跳过。四组本轮均有产出,无空转;W38 已收割,2613 补跑机在跑(应在跑的那一台)。

---

## 五、⚠️ 未解决且带时限的:W39 在 15:31:32Z 解锁

本轮解掉的是 **GH #424 这一条**(合取已承认、限定已投递到收割方读的字段)。
**批测台仍需自己确认**:W39 的 armed 串按 55-id 走,且 `outlatch` 的 (a) 按 §DN.6 (二) **分层**读。
本轮**没有**替它决定 W39 发不发 —— 那是它的 gate (i)。

---

## 六、铁律 6

- 静态半:`bash tools/agent/luacheck_gate.sh` —— 读数见下表,**未使用 `RULE6_BYPASS`**
  ⇒ 本报告**没有**「SKIPPED, not passed」行。`arm_push_gate.sh` exit 0。
- **`bots/` 与 `game/` 逐字节零 diff** ⇒ **全量 Lua 套件未跑,也不声称**(GH #124)。
  本轮动到的两个检测器**定向重跑**,读数见下表。
- python 全量**收尾干净重跑**(无并发编辑,75 个文件逐个取裸退出码):
  **74 passed / 1 failed / 1 uncertifiable**。
  红 = `test_detector_source_constants.py`(**GH #410**,录像组,非本轮引入、非总监归属);
  uncertifiable = `test_selfcheck_lua_leg.py`(**GH #358**,干净这遍同样存在)。
  ⭐ **与自检那一遍(`72 / 2 / 2`)的差异逐条对上,而这正是并发披露值钱的地方**:
  (i) `test_call_arity_census.py` 从红转绿 —— **是本轮 §三 修的**;
  (ii) `test_rc_wrapper.py` 的 `UNCERTIFIABLE` **干净这遍不存在** ⇒ **那一条是我自己的并发造的**,
  与前两轮同因、同形状。**「我制造的未核验」和「真的未核验」横幅一模一样,
  唯一能把它们分开的就是这次对照读数。**

## 七、铁律 11

**未触发任何 `requires approval`**;GitHub MCP 可用(`issue_read` 两次均返回)。**无空转等待。**

---

## 八、本轮明说没做的

**GH #409**(`rotscope` 漏报载体)未修;**§3.3 四个重文件连续第九轮没看有没有人接**;
`hero_pool.txt` 成员资格未接进 §DH.4 域价;**patch 缺口 P1/P2/P3(P3 优先)**;
`strategy-5b` 第十七轮未裁;**GH #395 三问未裁**;**按需常数第八轮未重裁**(理由见 §四,这次是主动的);
**GH #285 第二十六轮未裁**;`#252`/`#256`/`#282`/`#295` 关闭建议未发;**`§24` 未并入 `#229`**;
**`§CE` 已挂二十九轮**;backlog §19/§20/§21 未动;
`#348`/`#326`/`#324`/`#328`/`#329`/`#321`/`#271`;§CY 三条 + §CV 两条限定待收割;
**GH #384 的自检腿序那半**;**GH #412 仍只做一半、未关闭**;**GH #423**(录像组的 SS-DK.3 负控)本轮未跟进;
GH #394 / #410(均非总监归属)。

## 九、下次触发

1. **GH #423 —— `slotarb` 的条件 (a) 在录像组做出帧级检测器之前不可买**,批测台已把语料备好(158 局 / 8 槽 `.dem`),
   **再发波也买不到**;确认录像组接住了没有,没接住就交出去或自己排。
2. **核 §DO 那一棒第二次**(本轮已补投递到批测台读的字段,下一个收割轮是它的第一个新触发点)。
3. **GH #409**(`rotscope` 漏报载体)。
4. **§3.3 四个重文件有没有人接**,没接就自己修一个(连续第九轮写)。
5. **GH #285 第二十六轮**;**patch 缺口 P3**。
6. `#252`/`#256`/`#282`/`#295` + `strategy-5b`、`§24` 并入 `#229`、`§CE` 第一步。

---

## 本轮工具读数汇总(evidence discipline:退出码都不经管道)

| 命令 | 退出码 | 读数 |
|---|---|---|
| `routine_selfcheck.sh`(首跑,经管道) | **2 REFUSED** | **没跑成,不是通过** |
| `routine_selfcheck.sh`(重跑,重定向) | **3** | `legs run 8` / `UNCERTIFIABLE: none` / `FINDINGS: cadence trunk-red(python) trunk-red(lua)` |
| `lua5.1 tests/test_coarmed_attribution_register.lua`(**错误调用法**) | 0 | ⚠️ **零输出、什么都没跑** —— `rc.sh:37` 的逐字反例 |
| `run_tests.lua test_coarmed_attribution_register.lua`(修前) | **1** | `a NEW co-armed conjunction is live: outlatch > slotpush` |
| `run_tests.lua test_coarmed_attribution_register.lua`(修后) | **0** | `11 tests, 0 failures` |
| 变异 M1(删新行)/ M2(删 `midtp > tpreach`) | **1 / 1** | **2/2 CAUGHT**,各报各的缺陷;复原后 exit 0 |
| `python3 tests/test_call_arity_census.py`(修前) | **1** | `J.RoshanPitProximity UNDER passed 3 declares 4` |
| `python3 tests/test_call_arity_census.py`(修后) | **0** | `all checks pass` |
| 变异 M1(count 1→2)/ M2(抽掉裁定词) | **1 / 1** | **2/2 CAUGHT**,各报各的缺陷;复原后 exit 0 |
| `queue.json` round-trip 预量(`indent=1`,无末尾换行) | — | **逐字节相同 303254 == 303254** ⇒ 写后 diff **3 插入 / 1 删除** |
| `state.json` round-trip 预量(`indent=2` + 末尾换行) | — | **逐字节相同 1515354 == 1515354** ⇒ 写后 diff **18 插入 / 0 删除** |
| `luacheck_gate.sh` | **0** | `luacheck bots game: 0 warnings` / `CLEAN`(容器初始无 luacheck,gate 自己装,apt 包名 `lua-check`) |
| `arm_push_gate.sh` | **0** | 钩子已上膛 |
| python 全量**收尾干净重跑**(75 个文件逐个裸退出码) | — | **74 passed / 1 failed / 1 uncertifiable** |
| AWS 相关 | — | **本轮一次都没调用** |

## 本轮 token 用量(铁律 8)

```
TOKENS total_in=8,205,518 out=51,097 turns=61
```

(统计时刻之后的收尾回合不计入。)
