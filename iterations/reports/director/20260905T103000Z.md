# 总监 2026-09-05T10:xxZ —— GH #518 落地:owed registry 的认领字段

**一句话**:上一轮(§ER.2)立案的那条**会自己重演**的缺陷本轮修完 ——
`iterations/owed_executions.json` 的一行现在能说出「已经有人在飞」,
而**这个字段的每一种缺陷都长得跟它正常工作一模一样**,所以验收的重量全部压在
变异台的六发「静默被错误地授予」上,**没有一发是快乐路径**。

全文档案:`iterations/streams/test_set.md §ES`(最该读的是 **§ES.3**)。

- `bots/` + `game/` **零 diff**;零 AWS 调用;**无 promote/reject 任何 id**;
  armed 串、`iterations/queue.json` **一字未动**。
- `iterations/owed_executions.json` 动了两处:新 `_claim_note`(字段用法),
  `mock_isprefix_ordering` 加 `claimable: false`(理由见第 4 节)。

---

## 1. 取的是哪一根棒,以及为什么是它

上一轮报告(`20260905T042234Z.md`)的「下次触发」第一条:
**§ER.2 的认领字段(GH #518)—— 本轮唯一一条会自己重演的缺陷**。
本轮第一件事就是它。

它排第一的理由不是它最贵,是**它的失效方向**:`pending_rulings.py` 的 OWED 腿
**每轮对每个会话举手**,而**举手的目的就是让人把棒取走** ⇒
**这条腿工作得越好,两个会话在同一小时取走同一根棒的概率越高**。
不修它,下一次撞车不需要任何人犯错就会发生(09-05 那次就没有人犯错)。

## 2. 落地的形状

`claimed_by` + `claimed_at`(**真的 ISO-8601 UTC 瞬间**),**先 push 再干活**。
TTL 内该行对所有会话打 `IN-FLIGHT` 而不是 `OWED`,并且**不再让 owed 腿变红**;
`render_owed` 末尾单独打一行 `N in flight ... NOT a finding this round, and
NOT executed either`(这行的必要性由变异体 M6 买,见第 5 节)。

**TTL = 6 小时,数字是从它要挡的那次撞车里取的**:两个会话一个跑
03:5x–08:xxZ(~4h04m)、一个跑 06:5x–07:3xZ(~40m),**重叠 ~35 分钟**。
**TTL 短于较长的那个会话就把这个案子原样放回来** —— 6h 下第一个会话的认领
09:5xZ 才过期,3h 下 06:5xZ 就过期,**恰好是第二个会话开始读的时刻**。
两个方向不对称,这是选大不选小的理由:**认领过长是响的**(打 IN-FLIGHT + 认领人
+ 时长,而且认领从来不是锁),**认领过短是哑的**(两行长得一样的 OWED,就是缺陷本身)。

## 3. 三句话写进了工具的 LIMIT 12,而不是写进报告散文

(甲) **认领不是锁** —— 别人仍然可以取,「我认为你死了」是正当理由,取的人在报告里说一句为什么。
做成锁会把 backlog §6b「假弃置」的代价原样搬过来、只换个方向(死掉的认领人永久停一根棒,
而那种失效**无声**,比重复劳动更贵 —— issue #518 第 4 节自己就这么写的)。
(乙) **认领必须在干活之前推上去** —— 跟成品一起落地的认领是**记录不是信号**;
第二个会话读的是**它开工时 clone 的那棵树**。
(丙) **认领不是进度证据** —— `done_when` 照旧只 grade 产物(LIMIT 11 未动)。

**写坏的认领(半写 / 解析不了 / 在未来)一律读 OWED,永不读 IN-FLIGHT。**
这条腿的保守侧是**让棒保持可见**的那一侧,与 `done_when`
「读不出来的条件永不读成已执行」是同一条不对称、同一个方向。

⚠️ 特别钉住散文体时刻:本仓库报告/章程的房屋风格把分钟糊掉(`2026-09-05T07:xxZ`),
**那是最可能被真的写出来的错认领**,`parse_utc` 故意严格,它读成 UNREADABLE ⇒ OWED。

## 4. ⭐ 这次修复自己会犯的那个错,已经堵上

`mock_isprefix_ordering` 是一条**常驻否决权**:没有完成态,**每轮报 OWED 就是它的全部工作**
(它自己的 `done_when_note` 最后一句逐字这么写:「不要因为它每轮都报 OWED 就把它当噪声」)。
**一个认领会让它安静 6 小时** ⇒ 那就是**这次修复亲手制造它被立出来反对的那种安静**。

⇒ 新增可选字段 `claimable: false`,`claim_status` 对这种行**拒绝而不是采纳**认领,
并把「拒绝了、以及为什么」打出来(否则读者看到 OWED 会以为认领没写进去)。
registry 里 `mock_isprefix_ordering` 已标记。另一行
`campgrade_wave_type_premise_reading` 是真交付物,保持可认领。

## 5. 变异台:六发全部是「静默被错误地授予」的六种穿法

`tools/agent/mutstand_owed_claim.sh`,**6/6 CAUGHT**(`RC_EXIT=0`)。

| 变异体 | 授予了什么 | 结果 |
|---|---|---|
| M1 认领永不过期 | 半路死掉的会话永久停棒 | CAUGHT |
| M2 半个认领被采纳 | 只有 `claimed_by` 没有瞬间 | CAUGHT |
| M3 解析不了的瞬间被采纳 | 散文体 `T07:xxZ` | CAUGHT |
| M4 `claimable:false` 被忽略 | **主角**(第 4 节) | CAUGHT |
| M5 IN-FLIGHT 仍然变红 | **反方向**:字段什么也没买 | CAUGHT |
| M6 in-flight 汇总行被删 | 退 0 而无人说明 | CAUGHT |

⚠️ **两发在第一次尝试时是 SURVIVED,而两次的归因都是纪律 2 的逆命题(先确认编辑落在你以为的地方)**:
- **M2 第一版删的是分支**(`if not by or not at:` → `if False:`),
  而 `parse_utc(None)` 在下一行把同一个案子接住 ⇒ **纵深防御意味着那个分支本身不是门**;
  变异体必须攻击**答案**(`UNREADABLE` → `IN-FLIGHT`)而不是算出答案的分支。
- **M6 第一版的布尔表达式化简回原值** ⇒ **编辑落地了但语义没变**(SURVIVED 而非 NO-OP,
  因为文件确实变了)。改成删条件后立刻被抓。

两条都写进了变异台文件头,因为下一个写这种变异体的人会踩同一个坑。

## 6. 本轮没做 / 不声称

- **无 promote/reject**:本轮没有任何 id 的三条件被推进;armed 串未动。
- **零 AWS 调用** ⇒ **本轮不对 MTD 作任何新声称**(上一次的读数不由本轮背书)。
- **owner 邮件本周未发**(上一封 08-31);`DECISIONS_NEEDED` 本轮 +0。
- **patch 检查未做**(低频顺延)。⚠️ 按 §EM.⑨ 这句话**只替 patch 缺口作答**,
  不替别的顺延项作答。
- **test_set.md 待裁提议**:本轮无新提议入手(巡检读数见第 9 节)。

## 7. ⚠️ 本轮自己踩到的两件事(登记,不新造门)

**(一) 纪律 3,连续第六轮,而且又是第一条命令。**
本轮第一条命令是 `bash tools/agent/routine_selfcheck.sh 2>&1 | tail -60`,
被自检自己的 §22 守卫当场拒绝(`REFUSED ... exit 2, nothing checked`)。
章程第 0 条 09-04 刚把措辞从「第一条命令」改成「任何要读退出码的命令」,
**而这一发仍然是第一条命令** —— 与 §EL.0 的判断一致:**这一条是习惯不是门**,
守卫只长在自检那一条命令上。改用 `rc.sh` 后正常。

**(二) 我把开工自检和本轮的 python 套件并发跑了,于是自检的 Lua 腿读数作废。**
两个进程都写 `bots/Customize/soak_side.lua`,自检的 Lua 腿于是打出
`RED test_salveyield_arbitration.lua` / `RED test_soakside_shared_switch.lua`,
报错文案自己就点名了成因(**"or in a concurrent lua5.1 process"**)。
⇒ 那一轮自检**整份作废并已 kill**(残留开关已确认不存在:`bots/Customize/` 只剩
`general.lua` 与 `hero/`),在**定型后的树上串行重跑**,读数见第 9 节。
⚠️ 这与 §EP.7 / §ER.6 **同族第三发**(自检跑在一棵会动的树上),
但**成因是新的一种**:那两发是**树在变**,这一发是**并发进程在写同一个共享开关**。
**它不该被读成「同一发第三次」** —— 前者靠「先定型再跑」解决,后者靠「串行跑」解决,
而 09-05 我用前者的教训做事、仍然踩了后者。

## 8. 铁律 6

- **静态半**:`bash tools/agent/luacheck_gate.sh` ⇒ 读数见文末补记(**未用 `RULE6_BYPASS`**)。
  ⚠️ **诚实限定**:本轮 `bots/` + `game/` **一行未改** ⇒ 这道门对本轮改动几乎无话可说。
- **动态半(Lua)**:本轮**没有 Lua 改动**;全量 Lua 套件**未跑,因此不声称**。
- **真正的验收是 python 半边**:`tests/test_pending_rulings.py`
  **190 checks / 0 failed**(`RC_EXIT=0`;本轮之前是 167 checks),
  加上变异台 **6/6 CAUGHT**。全量 python 半边读数见第 9 节。

## 9. 巡检 / trunk 读数

见文末「收尾补记」——本节在报告落地时由收尾那一轮补齐,理由是第 7(二):
**本轮的自检必须跑在定型后的树上**,而定型发生在报告写完之后。

## 10. 下次触发

1. **§EP.5 交棒 (丙)**:`test_outlatch_capture_liveness.py` 的检查 1b 的**说明与事实相反**,
   断言永远绿 —— **比红的更难被发现**,属总监,**排第一**(上一轮也排第一,被 #518 压了一轮;
   ⚠️ 它没有外部触发器,只会被排挤不会被提醒 —— 与 `mock_getvelocity_ultloc` 同型,
   **现在可以给它开一行 owed 并当场认领**,那正是本轮买到的东西)。
2. **GH #517**(全量跑才存在的全局泄漏,倾向还原方案)。
3. trunk 红逐条(本轮读数见第 9 节)。
4. `kind:"ruling_request"` 轻量 queue 行;GH #454/#487/#460/#473 乙/#489/#486/#496;
   GH #513/#514;GH #449/#410/#436/#285/**patch 缺口 P3**/`ckpush` filler-英雄政策(有时限)。
